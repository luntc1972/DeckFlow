---
phase: 86-ui-audit-re-score-studio-stage-4-admin-flags-closeout
plan: 03
type: execute
wave: 3
depends_on: [86-02]
files_modified:
  - DeckFlow.Web/Views/Deck/DeckAnalysis.cshtml
  - DeckFlow.Web/wwwroot/css/site.css
  - DeckFlow.Web/wwwroot/css/site-abzan.css
  - DeckFlow.Web/wwwroot/css/site-bant.css
  - DeckFlow.Web/wwwroot/css/site-commander-table.css
  - DeckFlow.Web/wwwroot/css/site-esper.css
  - DeckFlow.Web/wwwroot/css/site-grixis.css
  - DeckFlow.Web/wwwroot/css/site-jeskai.css
  - DeckFlow.Web/wwwroot/css/site-jund.css
  - DeckFlow.Web/wwwroot/css/site-mardu.css
  - DeckFlow.Web/wwwroot/css/site-naya.css
  - DeckFlow.Web/wwwroot/css/site-nyx.css
  - DeckFlow.Web/wwwroot/css/site-planeswalker-dark.css
  - DeckFlow.Web/wwwroot/css/site-sultai.css
  - DeckFlow.Web/wwwroot/css/site-rakdos.css
autonomous: true
requirements: [UIAUDIT-02]

must_haves:
  truths:
    - "Each analysis-questions bucket toggle reads as a plain chevron (no standalone bordered box) with a caret that meets normal-text contrast."
    - "Each bucket toggle exposes an accessible name (aria-label) naming the bucket it toggles."
  artifacts:
    - path: "DeckFlow.Web/Views/Deck/DeckAnalysis.cshtml"
      provides: "aria-label on prompt-question-bucket__toggle"
      contains: "aria-label"
    - path: "DeckFlow.Web/wwwroot/css/site.css"
      provides: "borderless chevron bucket toggle"
      contains: "prompt-question-bucket__toggle"
  key_links:
    - from: "DeckAnalysis.cshtml prompt-question-bucket__toggle"
      to: "screen-reader accessible name"
      via: "aria-label attribute"
      pattern: "prompt-question-bucket__toggle[^>]*aria-label"
---

<objective>
Bug C — turn the analysis-questions "stray grey pill" into a plain chevron with an accessible name.

`DeckAnalysis.cshtml:306` renders an EMPTY `<button class="prompt-question-bucket__toggle">` — no text, no aria-label. `site.css:533-551` styles it as a bordered empty box (1px `--line`, radius, tiny pad) whose only content is a faint `--muted` `--fs-xs` ▶/▼ `::after` caret, so it reads as an orphan grey pill and is invisible to screen readers.

Locked fix (CONTEXT Bug C): drop the standalone border, raise the caret contrast + size, and add an `aria-label` (e.g. "Toggle {bucket} questions"). The base rule lives in `site.css`; the 12 standalone forks AND `site-rakdos.css` (an @import theme that overrides the toggle `::after` for guild styling) each duplicate it and must be mirrored.

Purpose: closes an accessibility gap (Pillar-6 Experience) and removes a visual-noise element (Pillar-2 Visuals) — both count toward the UIAUDIT-02 "enumerated gaps fixed + re-score >=20/24" bar.
Output: aria-labelled, borderless chevron toggle across base + 12 forks + rakdos.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/86-ui-audit-re-score-studio-stage-4-admin-flags-closeout/86-CONTEXT.md
@.planning/phases/86-ui-audit-re-score-studio-stage-4-admin-flags-closeout/86-RESEARCH.md
@.planning/phases/86-ui-audit-re-score-studio-stage-4-admin-flags-closeout/86-VALIDATION.md

<interfaces>
<!-- Markup — DeckAnalysis.cshtml:302-306 (@bucket.Label is the human name; @bucket.Id the key): -->
  <button type="button" class="prompt-question-bucket__toggle" data-bucket-toggle="@bucket.Id" aria-expanded="@bucketAriaExpanded"></button>

<!-- Base CSS to restyle — site.css:533-551 (drop border, raise caret): -->
  .prompt-question-bucket__toggle { background: none; border: 1px solid var(--line); border-radius: 4px; ... font-size: var(--fs-xs); color: var(--muted); ... }
  .prompt-question-bucket__toggle[aria-expanded="false"]::after { content: '▶'; }
  .prompt-question-bucket__toggle[aria-expanded="true"]::after  { content: '▼'; }

<!-- Files that duplicate the toggle rule (grep-confirmed) — mirror ALL of these: -->
<!--   12 forks: abzan, bant, commander-table, esper, grixis, jeskai, jund, mardu, naya, nyx, planeswalker-dark, sultai -->
<!--   + site-rakdos.css (@import theme that overrides the ::after caret) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: aria-label markup + base chevron restyle</name>
  <files>DeckFlow.Web/Views/Deck/DeckAnalysis.cshtml, DeckFlow.Web/wwwroot/css/site.css</files>
  <action>Per UIAUDIT-02 / CONTEXT Bug C. (1) In DeckAnalysis.cshtml:306 add `aria-label="Toggle @bucket.Label questions"` to the toggle button (keep data-bucket-toggle + aria-expanded). Touch only that line; preserve the file's existing line endings. (2) In site.css:533-551 restyle `.prompt-question-bucket__toggle`: remove the standalone box — `border: 0; background: none;` — and make it a plain chevron: raise the ::after caret to `var(--fs-base)` (or a discretion-chosen larger size) and give the caret readable contrast (`color: var(--ink)` or `var(--accent)` rather than faint `--muted`); keep the ▶/▼ `::after` content and the aria-expanded selectors. Keep it a small flex-shrink:0 hit target. Changed-lines only; preserve LF.</action>
  <verify>
    <automated>grep -n 'prompt-question-bucket__toggle"[^>]*aria-label\|aria-label="Toggle' DeckFlow.Web/Views/Deck/DeckAnalysis.cshtml; grep -n "prompt-question-bucket__toggle" DeckFlow.Web/wwwroot/css/site.css</automated>
  </verify>
  <done>The toggle button in DeckAnalysis.cshtml has an aria-label; site.css base toggle has no border and a higher-contrast, larger caret.</done>
</task>

<task type="auto">
  <name>Task 2: Mirror the chevron restyle into the 12 forks + rakdos</name>
  <files>DeckFlow.Web/wwwroot/css/site-abzan.css, DeckFlow.Web/wwwroot/css/site-bant.css, DeckFlow.Web/wwwroot/css/site-commander-table.css, DeckFlow.Web/wwwroot/css/site-esper.css, DeckFlow.Web/wwwroot/css/site-grixis.css, DeckFlow.Web/wwwroot/css/site-jeskai.css, DeckFlow.Web/wwwroot/css/site-jund.css, DeckFlow.Web/wwwroot/css/site-mardu.css, DeckFlow.Web/wwwroot/css/site-naya.css, DeckFlow.Web/wwwroot/css/site-nyx.css, DeckFlow.Web/wwwroot/css/site-planeswalker-dark.css, DeckFlow.Web/wwwroot/css/site-sultai.css, DeckFlow.Web/wwwroot/css/site-rakdos.css</files>
  <action>Each of these files duplicates `.prompt-question-bucket__toggle` (the 12 standalone forks) or overrides its `::after` caret (site-rakdos.css, an @import theme). Apply the same restyle from Task 1 in each: drop the border (`border: 0`), keep background none, raise the caret size/contrast to match. Preserve each theme's own accent for the caret color where it already uses one (raise contrast, don't flatten the guild color). The 10 bare @import themes without an override inherit the base fix for free — do not add rules to them. Changed-lines only; preserve LF; no mass reflow.</action>
  <verify>
    <automated>test $(grep -rl "prompt-question-bucket__toggle" DeckFlow.Web/wwwroot/css/site-abzan.css DeckFlow.Web/wwwroot/css/site-bant.css DeckFlow.Web/wwwroot/css/site-commander-table.css DeckFlow.Web/wwwroot/css/site-esper.css DeckFlow.Web/wwwroot/css/site-grixis.css DeckFlow.Web/wwwroot/css/site-jeskai.css DeckFlow.Web/wwwroot/css/site-jund.css DeckFlow.Web/wwwroot/css/site-mardu.css DeckFlow.Web/wwwroot/css/site-naya.css DeckFlow.Web/wwwroot/css/site-nyx.css DeckFlow.Web/wwwroot/css/site-planeswalker-dark.css DeckFlow.Web/wwwroot/css/site-sultai.css DeckFlow.Web/wwwroot/css/site-rakdos.css | wc -l) -eq 13</automated>
  </verify>
  <done>All 12 forks + rakdos render the borderless higher-contrast chevron; no bordered grey pill remains on any theme.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser ← static CSS + Razor markup | aria-label is static server-rendered text derived from a controlled bucket label; no user input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-86-03-A | Tampering | @bucket.Label in aria-label | accept | Label is server-controlled model data (fixed question buckets), not user-supplied; Razor HTML-encodes attributes by default. |
| T-86-03-B | Information Disclosure | aria-label text | accept | Names only the bucket; no sensitive data. |
| T-86-SC | Tampering | package installs | mitigate | No package installs; markup + CSS only. |
</threat_model>

<verification>
- Razor change compiles: build via `"/mnt/c/Program Files/dotnet/dotnet.exe" build DeckFlow.sln -c Debug` clean (0 warnings/errors).
- Accessible-name + no-border-box is asserted by the C-check in plan 86-05's e2e; consolidated human-verify there confirms the chevron reads correctly across themes.
- Do NOT commit compiled wwwroot/js/*.js.
</verification>

<success_criteria>
- Every bucket toggle has an aria-label naming its bucket.
- The toggle is a borderless chevron with a readable caret on all 24 themes.
- Build clean; changed-lines gate clean; LF preserved.
</success_criteria>

<output>
Create `.planning/phases/86-ui-audit-re-score-studio-stage-4-admin-flags-closeout/86-03-SUMMARY.md` when done.
</output>
