---
phase: 66-admin-tool-visibility-toggles-tool-registry
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - DeckFlow.Web/Services/Tools/ToolNavSection.cs
  - DeckFlow.Web/Services/Tools/ToolDefinition.cs
  - DeckFlow.Web/Services/Tools/IToolRegistry.cs
  - DeckFlow.Web/Services/Tools/ToolRegistry.cs
  - DeckFlow.Web/Services/Tools/ToolVisibility.cs
  - DeckFlow.Web/Extensions/ToolsServiceCollectionExtensions.cs
  - DeckFlow.Web/Program.cs
  - DeckFlow.Web/Services/FeatureFlags/FeatureFlagStore.cs
  - DeckFlow.Web.Tests/Tools/ToolRegistryTests.cs
  - DeckFlow.Web.Tests/Tools/ToolVisibilityTests.cs
  - DeckFlow.Web.Tests/Tools/ToolFlagSeedConsistencyTests.cs
  - DeckFlow.Web.Tests/Integration/ToolFlagPostgresSeedTests.cs
autonomous: true
requirements: [TOGGLE-01, TOGGLE-06]
must_haves:
  truths:
    - "A single in-memory registry lists all 13 public tools with route, nav section, label, tile copy, help slug, flag key, and core flag."
    - "The 3 already-gated tools (manabase, content-kb, categories) keep their EXISTING flag keys in the registry — no second flag is introduced for them (no double-gating)."
    - "The 10 newly-gated tools have `tool.<key>.enabled` flag keys seeded default-ON in both SQLite and Postgres."
    - "Default-ON seeding is proven in BOTH dialects — SQLite in-process and Postgres via the DECKFLOW_POSTGRES_TESTS container gate (TOGGLE-06 is not production-safe until PG is covered)."
    - "ToolVisibility groups visible tools by section preserving section order and omits sections with zero visible tools."
  artifacts:
    - path: "DeckFlow.Web/Services/Tools/ToolRegistry.cs"
      provides: "Canonical list of 13 ToolDefinition entries"
      contains: "IToolRegistry"
    - path: "DeckFlow.Web/Services/Tools/ToolVisibility.cs"
      provides: "Pure section-grouping + per-tool visibility helper"
    - path: "DeckFlow.Web/Services/FeatureFlags/FeatureFlagStore.cs"
      provides: "Seed extended with 10 tool.* keys (SQLite + Postgres)"
      contains: "tool.deck-analysis.enabled"
  key_links:
    - from: "DeckFlow.Web/Program.cs"
      to: "IToolRegistry"
      via: "AddDeckFlowTools() DI registration"
      pattern: "AddDeckFlowTools"
    - from: "DeckFlow.Web.Tests/Tools/ToolFlagSeedConsistencyTests.cs"
      to: "FeatureFlagStore seed"
      via: "asserts every registry FlagKey is seeded or a known pre-existing key"
      pattern: "tool\\."
---

<objective>
Build the Tool Registry — the single source of truth this entire phase is organized around — and
seed the new tool feature flags. Every downstream surface (nav, home tiles, help, route gating,
the new /Admin/Tools page) reads from this registry instead of scattered per-tool `@if` blocks.

Purpose: TOGGLE-01 (registry is the source of truth) and TOGGLE-06 (existing 3 ad-hoc flags folded
in with no double-gating; all tool flags default ON so existing deploys expose the same tools).
Output: `Services/Tools/*` registry abstraction, `AddDeckFlowTools()` DI, extended flag seed, tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/66-admin-tool-visibility-toggles-tool-registry/66-CONTEXT.md

<interfaces>
<!-- Existing infra the registry binds to. Executor uses these directly — do not re-scout. -->

From DeckFlow.Web/Models/DeckPageTab.cs — enum values:
  Sync=0, SuggestCategories=1, CommanderCategories=2, CardLookup=3, MechanicLookup=4,
  DeckAnalysis=5, Convert=7, DeckComparison=8, CedhMetaGap=9, Home=10, JudgeQuestions=11,
  ContentKb=12, DeckPrimer=13, Manabase=14

From DeckFlow.Web/Services/FeatureFlags/IFeatureFlagCache.cs:
  bool IsEnabled(string key);                                   // default TRUE on miss (D-13)
  IReadOnlyDictionary<string, bool> Snapshot();
  Task ReloadAsync(CancellationToken cancellationToken = default);

From DeckFlow.Web/Services/FeatureFlags/FeatureFlagStore.cs — existing seed keys (do NOT duplicate):
  scryfall.tagger.enabled, page.help.enabled, harvest.cron.enabled,
  feature.categories.enabled, content.kb.enabled, feature.manabase.enabled,
  analysis.reference.full-oracle-text, analysis.reference.deck-stats(FALSE)
  Seed pattern: const PostgresSeedSql / SqliteSeedSql, INSERT ... VALUES ... ON CONFLICT (key) DO NOTHING.

DI extension pattern to mirror — DeckFlow.Web/Extensions/FeatureFlagsServiceCollectionExtensions.cs:
  public static IServiceCollection AddDeckFlowFeatureFlags(this IServiceCollection services) { ... }
  Registered in Program.cs:99 as `builder.Services.AddDeckFlowFeatureFlags();`
</interfaces>

<tool_table>
<!-- The 13 tools. Stable key | route | section | flag key | core | help slug | DeckPageTab | primary-tile -->
deck-analysis        | /deck-analysis        | Analyze    | tool.deck-analysis.enabled        | CORE | deck-analysis        | DeckAnalysis        | no
deck-comparison      | /deck-comparison      | Analyze    | tool.deck-comparison.enabled      | CORE | deck-comparison      | DeckComparison      | yes
cedh-meta-gap        | /cedh-meta-gap        | Analyze    | tool.cedh-meta-gap.enabled        | CORE | cedh-meta-gap        | CedhMetaGap         | no
manabase             | /manabase             | Analyze    | feature.manabase.enabled (EXISTING)| no  | manabase             | Manabase            | no
deck-sync            | /sync                 | Build      | tool.deck-sync.enabled            | no   | deck-sync            | Sync                | yes
convert              | /convert              | Build      | tool.convert.enabled              | no   | (none)               | Convert             | no
deck-primer          | /deck-primer          | Build      | tool.deck-primer.enabled          | no   | deck-primer          | DeckPrimer          | no
card-lookup          | /card-lookup          | Reference  | tool.card-lookup.enabled          | no   | card-lookup          | CardLookup          | yes
mechanic-lookup      | /mechanic-lookup      | Reference  | tool.mechanic-lookup.enabled      | no   | (none)               | MechanicLookup      | no
judge-questions      | /judge-questions      | Reference  | tool.judge-questions.enabled      | no   | ask-a-judge          | JudgeQuestions      | no
content-kb           | /content-kb           | Reference  | content.kb.enabled (EXISTING)     | no   | content-kb           | ContentKb           | no
suggest-categories   | /suggest-categories   | Categories | feature.categories.enabled (EXISTING)| no | category-suggestions | SuggestCategories   | no
commander-categories | /commander-categories | Categories | tool.commander-categories.enabled | no   | commander-categories | CommanderCategories | no
</tool_table>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Tool registry model + DI + section-visibility helper</name>
  <files>DeckFlow.Web/Services/Tools/ToolNavSection.cs, DeckFlow.Web/Services/Tools/ToolDefinition.cs, DeckFlow.Web/Services/Tools/IToolRegistry.cs, DeckFlow.Web/Services/Tools/ToolRegistry.cs, DeckFlow.Web/Services/Tools/ToolVisibility.cs, DeckFlow.Web/Extensions/ToolsServiceCollectionExtensions.cs, DeckFlow.Web/Program.cs, DeckFlow.Web.Tests/Tools/ToolRegistryTests.cs, DeckFlow.Web.Tests/Tools/ToolVisibilityTests.cs</files>
  <behavior>
    - ToolRegistry.All returns exactly 13 ToolDefinition entries matching the &lt;tool_table&gt; (key, label, route, section, flag key, core, help slug, tab, primary-tile, icon key).
    - Exactly 3 tools are Core: deck-analysis, deck-comparison, cedh-meta-gap.
    - The manabase / content-kb / suggest-categories entries use the EXISTING flag keys verbatim (feature.manabase.enabled, content.kb.enabled, feature.categories.enabled) — NOT a new tool.* key (no double-gating, TOGGLE-06).
    - All routes/keys/tabs are unique across the 13 entries.
    - ToolVisibility.IsVisible(tool, cache) == cache.IsEnabled(tool.FlagKey).
    - ToolVisibility.VisibleBySection(tools, cache): returns sections in fixed order Analyze, Build, Reference, Categories; each section lists only its enabled tools in registry order; a section with zero enabled tools is omitted from the result entirely (TOGGLE-05 core logic).
    - With every flag enabled (FakeFeatureFlagCache default), VisibleBySection returns 4 sections totalling 13 tools.
    - With every Analyze-section flag disabled, the Analyze section does not appear in the result.
  </behavior>
  <action>
    Create `ToolNavSection` enum (Analyze, Build, Reference, Categories — declaration order is render order).
    Create `ToolDefinition` as a `sealed record` with init-only properties: Key (string, stable), Label,
    Route, Section (ToolNavSection), FlagKey, Core (bool), TileTitle, TileDescription, HelpSlug (string?),
    Tab (DeckPageTab), IsPrimaryTile (bool), IconKey (string — used by the Home view to pick the inline SVG
    in Plan 03; reuse the stable key as the icon key). Honor the carve-out: keep `{ get; init; }` — do NOT
    convert to get-only (System.Text.Json carve-out, .editorconfig CarveOutGuard).
    Create `IToolRegistry` exposing `IReadOnlyList&lt;ToolDefinition&gt; All`. Implement `ToolRegistry` with a
    static readonly list populated from the &lt;tool_table&gt;; tile copy comes verbatim from the existing
    Home.cshtml card titles/descriptions (see context — Plan 03 will delete those literals). For Knowledge Base
    use tile title "Expert Knowledge Base" and its existing description (matches current Home.cshtml).
    Create `ToolVisibility` static helper with `IsVisible` and `VisibleBySection` per &lt;behavior&gt;; return a
    stable shape such as `IReadOnlyList&lt;ToolSection&gt;` where `ToolSection` is a sealed record
    (Section, IReadOnlyList&lt;ToolDefinition&gt; Tools).
    Create `ToolsServiceCollectionExtensions.AddDeckFlowTools()` registering `IToolRegistry` as a singleton
    (mirror AddDeckFlowFeatureFlags). Wire `builder.Services.AddDeckFlowTools();` into Program.cs next to the
    existing `AddDeckFlowFeatureFlags()` call (~line 99). Touch only the added line (changed-lines format gate).
  </action>
  <verify>
    <automated>dotnet build DeckFlow.Web/DeckFlow.Web.csproj -c Debug 2>&1 | grep -E "error|Build succeeded" ; dotnet test DeckFlow.Web.Tests/DeckFlow.Web.Tests.csproj --filter "FullyQualifiedName~Tools.ToolRegistryTests|FullyQualifiedName~Tools.ToolVisibilityTests"</automated>
  </verify>
  <done>13 tools registered, 3 core, existing 3 flag keys reused; VisibleBySection omits empty sections; build clean; both test classes green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Seed 10 new tool flags + registry↔seed consistency test</name>
  <files>DeckFlow.Web/Services/FeatureFlags/FeatureFlagStore.cs, DeckFlow.Web.Tests/Tools/ToolFlagSeedConsistencyTests.cs, DeckFlow.Web.Tests/Integration/ToolFlagPostgresSeedTests.cs</files>
  <behavior>
    - After EnsureSchemaAsync against a fresh SQLite DB, GetAllAsync contains all 10 new tool.* keys, each enabled=true.
    - Re-running EnsureSchemaAsync on a DB where a tool flag was set FALSE does not flip it back to TRUE (ON CONFLICT DO NOTHING preserved).
    - Consistency: every ToolRegistry FlagKey is either one of the 3 pre-existing seeded keys OR present in the new seed block — no registry tool references an unseeded key.
    - Postgres (MED-1): against a real Postgres container, EnsureSchemaAsync seeds all 10 new tool.* keys enabled=true (the Postgres dialect path of the seed SQL is executed, not just SQLite).
  </behavior>
  <action>
    Extend both `PostgresSeedSql` and `SqliteSeedSql` const blocks in FeatureFlagStore with the 10 new keys —
    tool.deck-analysis.enabled, tool.deck-comparison.enabled, tool.cedh-meta-gap.enabled, tool.deck-sync.enabled,
    tool.convert.enabled, tool.deck-primer.enabled, tool.card-lookup.enabled, tool.mechanic-lookup.enabled,
    tool.judge-questions.enabled, tool.commander-categories.enabled — Postgres values TRUE, SQLite values 1,
    appended inside the existing VALUES list before `ON CONFLICT (key) DO NOTHING`. Do NOT add tool.manabase /
    tool.content-kb / tool.categories — those tools reuse their existing keys (TOGGLE-06 no double-gating).
    Add `ToolFlagSeedConsistencyTests`: (a) build a temp-file SQLite FeatureFlagStore (mirror existing store
    tests), call EnsureSchemaAsync, assert all 10 keys present+enabled and the preserve-on-conflict case;
    (b) construct `new ToolRegistry()`, assert each FlagKey appears in the union of {3 pre-existing keys} ∪
    {10 new keys parsed from the seed} so future registry edits cannot reference an unseeded flag.
    MED-1 — add `DeckFlow.Web.Tests/Integration/ToolFlagPostgresSeedTests.cs` using the existing Postgres
    harness (mirror Integration/PostgresStorageTests.cs: `IClassFixture<PostgresContainerFixture>`, `[PostgresFact]`,
    `new RelationalDatabaseConnection(RelationalDatabaseProvider.Postgres, await fixture.GetConnectionStringOrSkipAsync())`).
    Construct a `FeatureFlagStore` over that PG connection, call EnsureSchemaAsync, then GetAllAsync and assert all
    10 tool.* keys are present and enabled — proving the Postgres seed SQL branch runs default-ON. The test is
    auto-skipped unless DECKFLOW_POSTGRES_TESTS=1 (so CI/WSL default runs stay green), and is the gating evidence
    that TOGGLE-06 holds in production Postgres, not just SQLite.
  </action>
  <verify>
    <automated>dotnet test DeckFlow.Web.Tests/DeckFlow.Web.Tests.csproj --filter "FullyQualifiedName~Tools.ToolFlagSeedConsistencyTests" ; DECKFLOW_POSTGRES_TESTS=1 dotnet test DeckFlow.Web.Tests/DeckFlow.Web.Tests.csproj --filter "FullyQualifiedName~Integration.ToolFlagPostgresSeedTests"</automated>
  </verify>
  <done>10 tool.* keys seeded in both dialects default-ON; SQLite proven in-process AND Postgres proven via [PostgresFact] container gate; preserve-on-conflict intact; consistency test proves registry↔seed alignment.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator DB ↔ seed SQL | seed runs on startup against SQLite/Postgres |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-66-01 | Tampering | seed SQL | mitigate | static const string literals, no interpolation; ON CONFLICT DO NOTHING preserves operator toggles |
| T-66-02 | Elevation | new flags default ON | accept | TOGGLE-06 requires default-ON so existing deploys are unchanged; tools were always public before |
| T-66-SC | Tampering | npm/nuget installs | mitigate | no new packages introduced by this plan |
</threat_model>

<coverage_audit>
## Multi-Source Coverage Audit (phase-wide — recorded once here)

| Source | Item | Covered by |
|--------|------|-----------|
| GOAL | single registry unifies scattered @if checks | 66-01 (registry) consumed by 66-02/03/04/05 |
| GOAL | every surface appears/disappears together | 66-02 (route), 66-03 (nav+tile), 66-04 (help) |
| REQ | TOGGLE-01 registry source of truth | 66-01 |
| REQ | TOGGLE-02 admin toggle any tool | 66-05 |
| REQ | TOGGLE-03 hide tile+help+nav together | 66-03 (tile+nav) + 66-04 (help) |
| REQ | TOGGLE-04 disabled route 404 | 66-02 |
| REQ | TOGGLE-05 empty section collapses | 66-01 (logic) + 66-03 (render) |
| REQ | TOGGLE-06 fold 3 flags, no double-gate, default ON | 66-01 |
| REQ | TOGGLE-07 core-tool warning | 66-05 |
| CONTEXT | D1 gate → 404 | 66-02 |
| CONTEXT | D2 dedicated /Admin/Tools page | 66-05 |
| CONTEXT | D3 hide everywhere + drop placeholders + help header gaps | 66-03 (drop placeholders) + 66-04 (help headers) |
| CONTEXT | D4 3 core tools, inline warn-not-block | 66-05 |
All items COVERED. No unplanned items; no phase split required.
</coverage_audit>

<success_criteria>
- IToolRegistry resolvable from DI; All has 13 entries; 3 core; existing 3 flag keys reused.
- 10 new tool.* flags seeded default-ON in SQLite + Postgres; operator toggles survive restart.
- ToolVisibility.VisibleBySection omits empty sections (TOGGLE-05 logic core).
- dotnet build clean; new Tools tests green.
</success_criteria>

<output>
Create `.planning/phases/66-admin-tool-visibility-toggles-tool-registry/66-01-SUMMARY.md` when done.
</output>
