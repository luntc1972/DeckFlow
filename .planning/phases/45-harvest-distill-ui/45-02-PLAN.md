---
phase: 45-harvest-distill-ui
plan: 02
type: execute
wave: 2
depends_on: ["45-01"]
files_modified:
  - DeckFlow.Core/Content/VideoStatus.cs
  - DeckFlow.Core/Content/VideoStatusResolver.cs
  - DeckFlow.Studio/StudioDistillConfig.cs
  - DeckFlow.Studio/SessionCapOverride.cs
  - DeckFlow.Studio/Program.cs
  - DeckFlow.Core.Tests/VideoStatusResolverTests.cs
autonomous: true
requirements: [HARV-03, HARV-05]
must_haves:
  truths:
    - "A SINGLE provider decision (read once from DECKFLOW_LLM_PROVIDER) drives BOTH the registered distiller AND StudioDistillConfig.IsSubscriptionProvider — they can never disagree (HIGH-1)"
    - "The distiller is resolved via LlmDistillationProviderFactory.Resolve(providerEnv, httpClient) — NOT a hardcoded LlmDistillationService"
    - "When provider=openai the distiller is the metered LlmDistillationService AND IsSubscriptionProvider=false (cap enforced); when provider=claude both flip together ($0, subscription)"
    - "SessionCapOverride is an app-scoped (single-operator local tool) singleton whose OverrideUsd, when set, raises the ledger cap for the whole app including the orchestrator"
    - "A single ILlmSpendLedger singleton is registered with a resolver closure that reads SessionCapOverride"
    - "VideoStatusResolver (in DeckFlow.Core) maps (blocked, distilled, harvested) signals to the UI-SPEC badge vocabulary deterministically via real store queries"
  artifacts:
    - path: "DeckFlow.Core/Content/VideoStatus.cs"
      provides: "Badge state enum matching UI-SPEC vocabulary (Core so Core.Tests can reference it)"
      contains: "enum VideoStatus"
    - path: "DeckFlow.Core/Content/VideoStatusResolver.cs"
      provides: "Pure store-query badge-resolution logic, Core-located, unit-testable"
      contains: "VideoStatusResolver"
    - path: "DeckFlow.Studio/StudioDistillConfig.cs"
      provides: "Resolved subscription-provider flag (D-01), single source of truth with the distiller"
      contains: "public sealed record StudioDistillConfig"
    - path: "DeckFlow.Studio/SessionCapOverride.cs"
      provides: "App-scoped mutable cap override (D-03)"
      contains: "public sealed class SessionCapOverride"
    - path: "DeckFlow.Studio/Program.cs"
      provides: "Single provider decision driving factory distiller + StudioDistillConfig; override singleton; override-aware ledger; resolver"
      contains: "LlmDistillationProviderFactory.Resolve"
  key_links:
    - from: "DeckFlow.Studio/Program.cs"
      to: "LlmDistillationProviderFactory.Resolve"
      via: "the single providerEnv value resolves BOTH the distiller and the isSubscriptionProvider flag"
      pattern: "LlmDistillationProviderFactory.Resolve"
    - from: "DeckFlow.Studio/Program.cs"
      to: "SessionCapOverride"
      via: "ledger resolver closure reads OverrideUsd for DECKFLOW_LLM_MONTHLY_CAP_USD"
      pattern: "OverrideUsd"
    - from: "DeckFlow.Core/Content/VideoStatusResolver.cs"
      to: "IContentSiteIndexStore / IContentSourceStore / IContentVideoStore / IBlockedVideoStore"
      via: "ResolveStatusAsync queries blocked, then distilled via site index, then harvested across enabled sources"
      pattern: "IsBlockedAsync|GetByNaturalKeyAsync|GetVideoByYoutubeIdAsync"
---

<objective>
Wire the Studio composition root for the Harvest page (Wave 3/4) and extract the per-video
badge-resolution logic into a testable Core service.

This wave adds: (1) a SINGLE provider decision — read `DECKFLOW_LLM_PROVIDER` ONCE — that drives
BOTH the registered distiller (via `LlmDistillationProviderFactory.Resolve`, NOT a hardcoded
`LlmDistillationService`) AND `StudioDistillConfig.IsSubscriptionProvider`, so the distiller and
the spend-accounting flag can never disagree (HIGH-1); (2) `SessionCapOverride` — the app-scoped
in-memory cap holder (D-03); (3) an override-aware `ILlmSpendLedger` registration so a raised cap
is seen by both the page and the orchestrator's internal `WouldExceedCapAsync` (Pitfall 6);
(4) a `VideoStatus` enum + a `VideoStatusResolver` service implementing the UI-SPEC badge
vocabulary via REAL store queries (HARV-03), placed in **DeckFlow.Core** (pure store-query logic,
no Blazor dependency) so it can be unit-tested in Core.Tests without inverting the project
dependency (HIGH-2).

Purpose: Give Wave 3/4 ready-to-inject services, close the silent-LLM-spend path (HIGH-1), and
make the only non-trivial display logic (badge resolution) automatically verifiable in Core.Tests.
Output: Two new Core files, two new Studio files, a modified `Program.cs`, and one new Core.Tests file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/45-harvest-distill-ui/45-CONTEXT.md
@.planning/phases/45-harvest-distill-ui/45-RESEARCH.md
@.planning/phases/45-harvest-distill-ui/45-PATTERNS.md
@.planning/phases/45-harvest-distill-ui/45-UI-SPEC.md
@DeckFlow.Studio/Program.cs
@DeckFlow.Studio/StudioConfig.cs

<interfaces>
<!-- Confirmed contracts. Do NOT explore the codebase for these. -->

StudioConfig.cs analog (exact record pattern to copy):
  namespace DeckFlow.Studio;
  public sealed record StudioConfig(bool IsProdConfigured);

Stores already registered as singletons in Program.cs (lines 47-56):
  IContentSourceStore, IContentVideoStore, IContentSiteIndexStore, IBlockedVideoStore,
  IContentHarvestRunStore, ILlmSpendLedger (currently `_ => new LlmSpendLedger(contentKbDatabasePath)` line 52),
  IWhisperSpendLedger, IYouTubeChannelVideoLister, HttpClient (line 54),
  ILlmDistillationService (CURRENTLY HARDCODED line 55: `sp => new LlmDistillationService(sp.GetRequiredService<HttpClient>())` — HIGH-1, MUST change to factory).

LlmSpendLedger ctor (confirmed):
  public LlmSpendLedger(string databasePath, Func<string, string?>? configurationValueResolver = null);

LlmDistillationProviderFactory (CONFIRMED — DeckFlow.Core.Integration/LlmDistillationProviderFactory.cs):
  public const string EnvironmentVariableName = "DECKFLOW_LLM_PROVIDER";
  public static ILlmDistillationService Resolve(string? providerEnvValue, HttpClient httpClient);
  // null/blank/"openai" -> new LlmDistillationService(httpClient) (metered);
  // "claude" -> new CliLlmDistillationService("claude") (subscription, $0);
  // "codex" -> NotSupportedException; any other -> NotSupportedException.

Subscription derivation (CONFIRMED — copy from DeckFlow.CLI/ContentKbCommandRunners.cs:95-97):
  var providerEnv = Environment.GetEnvironmentVariable(LlmDistillationProviderFactory.EnvironmentVariableName);
  var isSubscriptionProvider = !string.IsNullOrWhiteSpace(providerEnv)
      && !string.Equals(providerEnv.Trim(), "openai", StringComparison.OrdinalIgnoreCase);
  // Use this SAME providerEnv for BOTH Resolve(providerEnv, httpClient) and isSubscriptionProvider.

Badge stores (CONFIRMED signatures):
  IBlockedVideoStore.IsBlockedAsync(string youtubeVideoId, CancellationToken ct = default) -> Task<bool>
  IContentSiteIndexStore.GetByNaturalKeyAsync(string naturalKeyType, string naturalKeyValue, CancellationToken ct = default) -> Task<ContentSiteIndexRow?>
  IContentSourceStore.ListEnabledSourcesAsync(CancellationToken ct = default) -> Task<IReadOnlyList<ContentSource>>   // each has .Id (long)
  IContentVideoStore.GetVideoByYoutubeIdAsync(long sourceId, string youtubeVideoId, CancellationToken ct = default) -> Task<ContentVideo?>

ContentSourceType.Youtube  (DeckFlow.Core.Knowledge.ContentModels.cs:183) === the string "youtube_channel".
  USE THE CONSTANT ContentSourceType.Youtube EVERYWHERE — NEVER the literal "youtube" or "youtube_channel" (LOW-1).

UI-SPEC badge vocabulary (45-UI-SPEC.md lines 110-126):
  not-harvested | harvested | distilled | blocked | duplicate
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Single provider decision — factory-resolved distiller + StudioDistillConfig + SessionCapOverride + override-aware ledger (HIGH-1)</name>
  <files>DeckFlow.Studio/StudioDistillConfig.cs, DeckFlow.Studio/SessionCapOverride.cs, DeckFlow.Studio/Program.cs</files>
  <read_first>
    - DeckFlow.Studio/Program.cs (lines 35-72 — configuration read, store registrations incl. the HARDCODED ILlmDistillationService line 55, AddContentKbOrchestrator)
    - DeckFlow.Studio/StudioConfig.cs (the exact sealed-record analog)
    - DeckFlow.Core/Integration/LlmDistillationProviderFactory.cs (Resolve signature + provider mapping)
    - DeckFlow.CLI/ContentKbCommandRunners.cs (lines 95-104 — the canonical single-providerEnv derivation + factory Resolve to replicate)
    - .planning/phases/45-harvest-distill-ui/45-PATTERNS.md (sections "StudioDistillConfig.cs" lines 239-260, "SessionCapOverride.cs" lines 264-285, "Program.cs" lines 192-235 — registration blocks)
    - .planning/phases/45-harvest-distill-ui/45-RESEARCH.md (Pattern 4 lines 384-407, Pattern 5 lines 411-447, Pitfall 6 lines 562-572 — single ledger singleton shared with orchestrator)
  </read_first>
  <action>
    Create `DeckFlow.Studio/StudioDistillConfig.cs`: a `public sealed record
    StudioDistillConfig(bool IsSubscriptionProvider);` in `namespace DeckFlow.Studio;`, with an
    XML `<summary>` matching the StudioConfig analog (one type per file, file-scoped namespace).

    Create `DeckFlow.Studio/SessionCapOverride.cs`: a `public sealed class SessionCapOverride`
    (class, not record — it has mutable state) in `namespace DeckFlow.Studio;` exposing
    `public decimal? OverrideUsd { get; set; }`. The XML doc MUST state it is an APP-SCOPED override
    (DeckFlow Studio is a single-operator local tool; this is NOT per-circuit/per-session isolation)
    that resets to env/default ($15.00) on app restart (D-03). Do NOT convert the auto-property to
    get-only (carve-out).

    In `Program.cs`, establish ONE provider decision used by both the distiller and the flag
    (HIGH-1 — single source of truth). Before the store registrations, read the provider once:
    `var providerEnv = builder.Configuration[LlmDistillationProviderFactory.EnvironmentVariableName] ?? Environment.GetEnvironmentVariable(LlmDistillationProviderFactory.EnvironmentVariableName);`
    and derive `var isSubscriptionProvider = !string.IsNullOrWhiteSpace(providerEnv) && !string.Equals(providerEnv.Trim(), "openai", StringComparison.OrdinalIgnoreCase);`
    (replicates ContentKbCommandRunners — D-01).

    REPLACE the hardcoded ILlmDistillationService registration (line 55) with the factory:
    `builder.Services.AddSingleton<ILlmDistillationService>(sp => LlmDistillationProviderFactory.Resolve(providerEnv, sp.GetRequiredService<HttpClient>()));`
    — using the SAME `providerEnv`. This closes HIGH-1: when provider=openai the distiller is the
    metered LlmDistillationService and isSubscriptionProvider=false (cap enforced); when
    provider=claude the distiller is the subscription CliLlmDistillationService and
    isSubscriptionProvider=true — they flip together and can never disagree.

    Register the resolved flag: `builder.Services.AddSingleton(new StudioDistillConfig(isSubscriptionProvider));`.

    REPLACE the single-line `ILlmSpendLedger` registration (line 52) with override-aware wiring:
    construct `var capOverride = new SessionCapOverride();`, register it via
    `builder.Services.AddSingleton(capOverride);`, then register
    `builder.Services.AddSingleton<ILlmSpendLedger>(_ => new LlmSpendLedger(contentKbDatabasePath, key => ...))`
    where the resolver closure returns
    `capOverride.OverrideUsd.Value.ToString("F2", CultureInfo.InvariantCulture)` when
    `key == "DECKFLOW_LLM_MONTHLY_CAP_USD"` and `capOverride.OverrideUsd.HasValue`, otherwise `null`
    (fall through to env var / default). This single shared singleton is also injected into the
    orchestrator, so the override reaches the orchestrator's internal `WouldExceedCapAsync`
    (Pitfall 6) — do NOT register a second ledger instance.

    Add required `using System.Globalization;` (for `CultureInfo`). `LlmDistillationProviderFactory`
    is in `DeckFlow.Core.Integration` which is already imported (line 2). Keep using-directive order
    (System.* first). Touch only changed lines; preserve LF; no mass reflow.
  </action>
  <verify>
    <automated>"/mnt/c/Program Files/dotnet/dotnet.exe" build DeckFlow.sln 2>&1 | grep -E "error|Build succeeded" | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "public sealed record StudioDistillConfig(bool IsSubscriptionProvider)" DeckFlow.Studio/StudioDistillConfig.cs` returns 1.
    - `grep -c "public decimal? OverrideUsd { get; set; }" DeckFlow.Studio/SessionCapOverride.cs` returns 1, and the SessionCapOverride XML doc says "app-scoped" (no false per-circuit/per-session claim).
    - `grep -c "LlmDistillationProviderFactory.Resolve(providerEnv" DeckFlow.Studio/Program.cs` returns 1 AND `grep -c "new LlmDistillationService" DeckFlow.Studio/Program.cs` returns 0 (hardcoded metered distiller removed — HIGH-1 closed).
    - The SAME `providerEnv` variable feeds both the factory Resolve and the `isSubscriptionProvider` derivation (single read; grep shows one `Environment.GetEnvironmentVariable(LlmDistillationProviderFactory.EnvironmentVariableName)` or one Configuration read feeding both).
    - `grep -c "new StudioDistillConfig(isSubscriptionProvider)" DeckFlow.Studio/Program.cs` returns 1.
    - `grep -c "AddSingleton(capOverride)" DeckFlow.Studio/Program.cs` returns 1, `grep -c "capOverride.OverrideUsd" DeckFlow.Studio/Program.cs` >= 1, and `grep -c "new LlmSpendLedger(contentKbDatabasePath" DeckFlow.Studio/Program.cs` returns 1 (single override-aware ledger).
    - `dotnet build DeckFlow.sln` reports Build succeeded, zero errors, zero new warnings.
  </acceptance_criteria>
  <done>One providerEnv read drives the factory-resolved distiller AND StudioDistillConfig.IsSubscriptionProvider; the hardcoded metered distiller is gone; SessionCapOverride is app-scoped + override-aware ledger is a single shared singleton; solution builds clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: VideoStatus enum + VideoStatusResolver in DeckFlow.Core with real store-query resolution + Core.Tests (HIGH-2, HIGH-3)</name>
  <files>DeckFlow.Core/Content/VideoStatus.cs, DeckFlow.Core/Content/VideoStatusResolver.cs, DeckFlow.Studio/Program.cs, DeckFlow.Core.Tests/VideoStatusResolverTests.cs</files>
  <read_first>
    - .planning/phases/45-harvest-distill-ui/45-UI-SPEC.md (lines 110-126 — Status Badge Vocabulary + resolution rules)
    - .planning/phases/45-harvest-distill-ui/45-RESEARCH.md (Pattern 3 lines 341-382 — ResolveStatusAsync rules)
    - DeckFlow.Core/Content/IBlockedVideoStore.cs (IsBlockedAsync)
    - DeckFlow.Core/Content/IContentSiteIndexStore.cs (GetByNaturalKeyAsync)
    - DeckFlow.Core/Content/IContentSourceStore.cs (ListEnabledSourcesAsync — each source has .Id)
    - DeckFlow.Core/Content/IContentVideoStore.cs (GetVideoByYoutubeIdAsync(long sourceId, string youtubeVideoId, ...))
    - DeckFlow.Core/Knowledge/ContentModels.cs (line 183 — ContentSourceType.Youtube == "youtube_channel")
    - DeckFlow.Core.Tests/LlmSpendLedgerTests.cs (xUnit sealed-class + [Fact] convention to mirror; no mocking library present)
  </read_first>
  <behavior>
    - Blocked video (IsBlockedAsync == true) resolves to VideoStatus.Blocked regardless of other signals (blocked checked first).
    - Not blocked + a content_site_index row exists for ContentSourceType.Youtube (GetByNaturalKeyAsync returns non-null) resolves to VideoStatus.Distilled (distilled implies harvested).
    - Not blocked + no site-index row + the video EXISTS in any enabled source (GetVideoByYoutubeIdAsync non-null for some source) resolves to VideoStatus.Harvested.
    - Not blocked + no site-index row + not found in any enabled source resolves to VideoStatus.NotHarvested.
    - The enum members map one-to-one to the five UI-SPEC badge labels (NotHarvested, Harvested, Distilled, Blocked, Duplicate).
  </behavior>
  <action>
    Create `DeckFlow.Core/Content/VideoStatus.cs`: a `public enum VideoStatus { NotHarvested,
    Harvested, Distilled, Blocked, Duplicate }` in `namespace DeckFlow.Core.Content;` with XML docs;
    member order mirrors the UI-SPEC vocabulary table.

    Create `DeckFlow.Core/Content/VideoStatusResolver.cs`: a `public sealed class
    VideoStatusResolver` in `namespace DeckFlow.Core.Content;`. This lives in Core (pure store-query
    logic, no Blazor dependency) so DeckFlow.Core.Tests can reference it directly — do NOT place it
    in DeckFlow.Studio (HIGH-2: Core.Tests does not and must not reference Studio). Constructor takes
    `IBlockedVideoStore`, `IContentSiteIndexStore`, `IContentSourceStore`, and `IContentVideoStore`;
    add `ArgumentNullException.ThrowIfNull` guards on each (project convention). Expose
    `public async Task<VideoStatus> ResolveStatusAsync(string youtubeVideoId, CancellationToken ct = default)`
    implementing the REAL resolution (HIGH-3 — no no-op false probe):
      1. If `await BlockedStore.IsBlockedAsync(youtubeVideoId, ct)` is true → return `VideoStatus.Blocked`.
      2. If `await IndexStore.GetByNaturalKeyAsync(ContentSourceType.Youtube, youtubeVideoId, ct)` is
         non-null → return `VideoStatus.Distilled` (distilled implies harvested).
      3. Otherwise iterate `await SourceStore.ListEnabledSourcesAsync(ct)` and for each source call
         `await VideoStore.GetVideoByYoutubeIdAsync(source.Id, youtubeVideoId, ct)`; if ANY returns
         non-null → return `VideoStatus.Harvested` (stop on first hit).
      4. If no source has the video → return `VideoStatus.NotHarvested`.
    Use the `ContentSourceType.Youtube` CONSTANT for the natural-key type — never the literal
    "youtube"/"youtube_channel" (LOW-1). Add `using DeckFlow.Core.Knowledge;` for the constant.
    The page (Plan 03) treats Distilled/Harvested presence as the duplicate signal for the paste
    queue; the resolver itself returns the four primary states above (Duplicate is a UI-layer
    presentation of an already-in-DB video, surfaced in Plan 03).

    Register the resolver in `Program.cs` as a singleton resolving the four store singletons:
    `builder.Services.AddSingleton<VideoStatusResolver>();` (DI resolves the constructor stores) or
    an explicit factory if needed. Add `using DeckFlow.Core.Content;` if not already imported.

    Create `DeckFlow.Core.Tests/VideoStatusResolverTests.cs`: `public sealed class
    VideoStatusResolverTests`. Since no mocking library is present (CLAUDE.md: do not add one), write
    small in-test fake implementations of `IBlockedVideoStore`, `IContentSiteIndexStore`,
    `IContentSourceStore`, and `IContentVideoStore` (Fake* convention — return configured values,
    throw NotSupportedException on unused members) to drive the four behavior cases as `[Fact]`
    methods named `Method_Scenario_ExpectedResult`. The fakes live in the test file. Cover:
    Blocked-wins; Distilled-when-index-row-present; Harvested-when-found-in-an-enabled-source (no
    index row); NotHarvested-when-no-index-row-and-not-in-any-source. The harvested fake must verify
    the resolver actually iterates enabled sources and calls GetVideoByYoutubeIdAsync with each
    source's Id (assert it found the video in a non-first source to prove iteration, not a no-op).

    Preserve LF; one public type per file; touch only changed lines in Program.cs.
  </action>
  <verify>
    <automated>"/mnt/c/Program Files/dotnet/dotnet.exe" test DeckFlow.Core.Tests/ --filter "FullyQualifiedName~VideoStatusResolverTests" 2>&1 | grep -E "Passed!|Failed!|error" | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "public enum VideoStatus" DeckFlow.Core/Content/VideoStatus.cs` returns 1 and the enum lists NotHarvested, Harvested, Distilled, Blocked, Duplicate.
    - `grep -c "public sealed class VideoStatusResolver" DeckFlow.Core/Content/VideoStatusResolver.cs` returns 1 (resolver is in DeckFlow.Core, NOT DeckFlow.Studio — HIGH-2).
    - `grep -c "IsBlockedAsync" VideoStatusResolver.cs` >= 1, `grep -c "GetByNaturalKeyAsync" VideoStatusResolver.cs` >= 1, `grep -c "ListEnabledSourcesAsync" VideoStatusResolver.cs` >= 1, `grep -c "GetVideoByYoutubeIdAsync" VideoStatusResolver.cs` >= 1 (real blocked → distilled → harvested resolution wired — HIGH-3).
    - `grep -c "ContentSourceType.Youtube" VideoStatusResolver.cs` >= 1 and `grep -c '"youtube"' VideoStatusResolver.cs` returns 0 (constant used, no literal — LOW-1).
    - `grep -c "AddSingleton<VideoStatusResolver>" DeckFlow.Studio/Program.cs` returns 1.
    - `VideoStatusResolverTests` contains four [Fact] methods (blocked / distilled / harvested-via-source-iteration / not-harvested), using in-file fakes (no mocking-library `using`), and the harvested case proves source iteration.
    - `dotnet test DeckFlow.Core.Tests/ --filter "FullyQualifiedName~VideoStatusResolverTests"` reports all passed. WSL fallback: `dotnet build DeckFlow.sln` clean + flag manual test run in SUMMARY.
  </acceptance_criteria>
  <done>VideoStatus enum + VideoStatusResolver live in DeckFlow.Core with real store-query resolution (blocked → distilled-via-site-index → harvested-via-source-iteration), registered in Studio DI, with four passing Core.Tests driven by in-file fakes; solution builds clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator browser → Studio app | Cap override value enters via the page (Wave 4); this wave creates the app-scoped in-memory holder it lands in |
| Provider decision → distiller + spend flag | A single DECKFLOW_LLM_PROVIDER read must drive both the distiller and IsSubscriptionProvider; a mismatch is the HIGH-1 silent-spend bug |
| Studio config → ledger / orchestrator | The single shared ledger singleton is the only cap source; a duplicate would bypass the override |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-45-03 | Elevation of privilege | SessionCapOverride raised to extreme value | accept | Override is in-memory, APP-SCOPED (single-operator local tool — not per-circuit isolation), and resets to env/default on restart (D-03). LLM-cost exposure is bounded to the local operator; no persistence. Wave 4 still gates spend behind dry-run + confirm. The XML doc states the app-scoped reality (no false isolation claim). |
| T-45-04 | Tampering | Two separate ILlmSpendLedger instances | mitigate | Register exactly one override-aware ledger singleton; the orchestrator injects the same instance so its internal WouldExceedCapAsync sees the override (Pitfall 6). Acceptance asserts a single `new LlmSpendLedger` registration. |
| T-45-17 | Spoofing / repudiation | Distiller and spend flag disagree (silent metered spend) | mitigate | HIGH-1 fix: a SINGLE providerEnv read drives BOTH the factory-resolved distiller AND isSubscriptionProvider. The hardcoded metered LlmDistillationService is removed; acceptance asserts `LlmDistillationProviderFactory.Resolve(providerEnv` present and `new LlmDistillationService` absent, so provider=claude can no longer report $0 while metering OpenAI. |
| T-45-05 | Information disclosure | Provider/cap values in logs | mitigate | No new log statements emit DECKFLOW_LLM_PROVIDER, the connection string, or cap values; existing Program.cs logs only "configured / not configured" (line 82). New code adds no secret logging. |
| T-45-SC | Tampering | npm/pip/cargo installs | accept | No package installs; no mocking library added (in-file fakes only) per CLAUDE.md. |
</threat_model>

<verification>
- `dotnet build DeckFlow.sln` — zero errors, zero new warnings.
- `dotnet test DeckFlow.Core.Tests/ --filter "FullyQualifiedName~VideoStatusResolverTests"` — all pass.
- `LlmDistillationProviderFactory.Resolve(providerEnv` present in Program.cs; `new LlmDistillationService` absent (HIGH-1).
- Exactly one `new LlmSpendLedger(...)` registration; resolver + enum in DeckFlow.Core (not Studio).
- No secret/provider/cap value logged in new code.
</verification>

<success_criteria>
- A single provider decision drives the factory distiller AND IsSubscriptionProvider (HIGH-1 closed).
- StudioDistillConfig + SessionCapOverride exist and are DI-registered; SessionCapOverride is honestly documented as app-scoped.
- The ledger singleton is override-aware and shared with the orchestrator.
- VideoStatus + VideoStatusResolver live in DeckFlow.Core, resolve via real store queries, and are unit-tested in Core.Tests (HARV-03, HIGH-2, HIGH-3, LOW-1 all closed).
</success_criteria>

<output>
Create `.planning/phases/45-harvest-distill-ui/45-02-SUMMARY.md` when done.
</output>
</output>
