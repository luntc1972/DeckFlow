# Phase 07.1 — Cross-AI Plan Review

**Current verdict: NOT CONVERGED.** Round 2 of a hard cap of 4. Ten HIGH findings survive across
five of the seven plans. `07.1-04` and `07.1-06` are CONVERGED and need no further work.

⚠ **Execution is blocked until this file records CONVERGED.** dev-agent's `plan_review` gate
(`src/decide.js:116`) tests only that this file EXISTS — it never reads the verdict — so nothing but
this line stands between a not-converged review and EXECUTE. Do not resume the run until it changes.

| plan | round 1 | round 2 |
|---|---|---|
| 07.1-01 | NOT CONVERGED | NOT CONVERGED — 2 HIGH |
| 07.1-02 | NOT CONVERGED | NOT CONVERGED — 1 HIGH |
| 07.1-03 | NOT CONVERGED | NOT CONVERGED — 2 HIGH, 1 MEDIUM |
| 07.1-04 | NOT CONVERGED | **CONVERGED** |
| 07.1-05 | CONVERGED | NOT CONVERGED — 1 HIGH, 2 MEDIUM |
| 07.1-06 | NOT CONVERGED | **CONVERGED** |
| 07.1-07 | NOT CONVERGED | NOT CONVERGED — 3 HIGH, 1 MEDIUM (1 HIGH rejected, see below) |

`07.1-05` regressed from CONVERGED to NOT CONVERGED: round 1 passed it, and the round-1 fold then
introduced a HIGH of its own — an acceptance criterion whose arithmetic cannot hold. A fold is not
automatically an improvement, which is why every round is re-reviewed.

---

## Round 2 — 2026-08-22

**Reviewer:** Codex `gpt-5.6-sol`, effort medium, one dispatch per plan document, seven dispatches,
`--turn-cap 60 --timeout 1500`. All seven finished clean, rc 0, no watchdog kill, 3-7 minutes each;
round 1 at cap 40 lost four of seven to the cap. The difference is that each round-2 brief pointed at
the preserved round-1 claim census, so the reviewer verified rather than re-discovered.

**Tree reviewed:** `23f67767` (`git diff a13b9499..23f67767`), which carries both the round-1 fold
and the blind-verification fold.

**Coordinate warning:** every `file:line` citation in this Round 2 section is valid only against `23f67767` — the tree the round-2 reviewer actually read. Later folds move lines; do not trust these citations against the current tree. Anchor by the quoted content instead.

### One round-2 finding is REJECTED, not folded

`07.1-07`'s "FOLDED WRONG — Replacement Playwright command violates the repository's mandatory
waiting protocol" is a false positive. No such repository rule exists in `AGENTS.md` or `CLAUDE.md`.
The rule quoted is `codex-run.sh:829` — "Waiting is a billed turn: do not poll or sleep to wait for
something" — a clause the dispatch wrapper appends to every Codex brief to govern the REVIEWER's own
behaviour during its run. It has no bearing on what a shell script inside a plan may do. The bounded
60-attempt readiness poll stays; `npx wait-on` would be a new dependency this phase may not add.
Recorded here so round 3 does not re-file it.

This is a general hazard of wrapper-injected operating clauses: the reviewer cannot tell which of its
instructions describe its own conduct and which describe the artifact, and will occasionally apply
one to the other.

### Review of 07.1-01-PLAN.md — NOT CONVERGED — 2 HIGH

# Round 2 review — 07.1-01-PLAN.md

## Round-1 fold status

- FOLDED — `[HIGH] Vacuous/impossible keyboard test`: Home/End are now explicitly boundary-only checks at `07.1-01-PLAN.md:114-120`; the new two-ArrowRight traversal asserts Plan state at the intermediate step at `07.1-01-PLAN.md:122-132`; `<done>` agrees at `07.1-01-PLAN.md:177`.
- FOLDED — `[MEDIUM] Invalid initial selection`: the existing case now explicitly changes to `renderFixture(2)` before asserting Decide remains selected at `07.1-01-PLAN.md:97-112`.
- FOLDED — `[MEDIUM] Missing BUILD-EOL verification`: the full-working-tree `git diff --stat` versus `git diff --ignore-all-space --stat` comparison is now an acceptance criterion at `07.1-01-PLAN.md:282-285` and repeated in final success criteria at `07.1-01-PLAN.md:314`.

## New findings

### HIGH — Home/End fold leaves must-have panel invariant impossible

`07.1-01-PLAN.md:22` now correctly says Home and End focus steps 1 and 5, but the copied truths at `07.1-01-PLAN.md:23-24` still say "After that keypress" Plan remains unselected and the previously visible panel remains visible. Task 1 explicitly calls Home/End boundary navigation at `07.1-01-PLAN.md:114-120`; the production keydown path unconditionally calls `activateStepTab(nextTab, false)`, which selects the enabled endpoint and replaces the visible panel. In particular, the described End dispatch selects step 5 and hides the prior step-1 panel. The task assertions and `<done>` at `07.1-01-PLAN.md:177` omit the stale panel invariant, so the plan's must-haves disagree with its implementation and acceptance. Scope truths 23-24 to keypresses that focus the reserved Plan tab, or explicitly define normal endpoint activation for Home/End.

### HIGH — Fold deletes every ArrowLeft proof while retaining ArrowLeft success claims

The fold removed the former ArrowLeft scenario, and `ArrowLeft` now appears only in unverified claims at `07.1-01-PLAN.md:22`, `07.1-01-PLAN.md:177`, and `07.1-01-PLAN.md:319`. Task 1's extended existing case and new traversal case exercise only ArrowRight (`07.1-01-PLAN.md:97-138`); Home, End, and click cover different paths. Consequently, a direction-specific ArrowLeft regression can satisfy every automated criterion and mutation check while the plan declares ArrowLeft proven. Add an ArrowLeft dispatch from step 4 onto Plan with the same focus, `aria-selected`, and visible-panel-set assertions; update expected counts only if this is a separate test case.

---
### Review of 07.1-02-PLAN.md — NOT CONVERGED — 1 HIGH

# Round 2 review: 07.1-02

## Round-1 fold audit

- **FOLDED** — Prior HIGH initialization-collapse path: direct `applyInitialSectionCollapseState` no-storage edit now required at `07.1-02-PLAN.md:232`, with executable gate at `07.1-02-PLAN.md:254`.
- **FOLDED** — Prior HIGH defective occurrence gate: exact remaining count corrected to `2` at `07.1-02-PLAN.md:256`.
- **FOLDED** — Prior MEDIUM localStorage leakage: `afterEach` clearing required at `07.1-02-PLAN.md:170`.
- **FOLDED** — Prior MEDIUM pass-count concern: arithmetic grounded as 6 + 3 = 9 at `07.1-02-PLAN.md:249` and 9 + 1 = 10 at `07.1-02-PLAN.md:307`; Plan 01's criterion independently states 6 passed.
- **FOLDED** — Prior MEDIUM no-op click vacuity: clicked and prior tab selection states now directly asserted at `07.1-02-PLAN.md:286-289` and `07.1-02-PLAN.md:310`, with M18 at `07.1-02-PLAN.md:356-365`.
- **FOLDED** — Prior MEDIUM M7 crash-only kill: compiling behavioral mutant now specified at `07.1-02-PLAN.md:347-354`.

## New fold findings

### HIGH — Null storage result conflates absence with failures

`07.1-02-PLAN.md:232-238` — Fold makes `readCollapsedSectionIds() === null` a no-op, but `null` represents not only absent storage: malformed JSON and storage-access exceptions also return `null`. Existing unchanged regressions at `DeckFlow.Web/ts-tests/cut-lab-section-collapse.test.ts:71-82` and `DeckFlow.Web/ts-tests/cut-lab-section-collapse.test.ts:85-98` require those failure modes to apply mobile defaults. Implementing fold verbatim leaves all three `<details>` elements open, so both tests fail and `07.1-02-PLAN.md:407`'s full-suite `0 failed` criterion is unattainable. Distinguish “key absent” from invalid/unavailable storage, preserving error fallback while leaving truly absent state unchanged, or deliberately revise behavior and tests within scope. Revise `07.1-02-PLAN.md:254-256` too: its zero-call regional gate and exact global count of `2` currently forbid a direct fallback call.

## Allocation audit

Plan claims only M4, M5, M6, M7, and M18 at `07.1-02-PLAN.md:324`, `07.1-02-PLAN.md:356-358`, and `07.1-02-PLAN.md:433`; this matches the phase-wide allocation.

## Disposition

**NOT CONVERGED** — one HIGH finding remains.

---
### Review of 07.1-03-PLAN.md — NOT CONVERGED — 2 HIGH, 1 MEDIUM

## Round-1 Concern 1 — FOLDED — HIGH resolved

`07.1-03-PLAN.md:338-363` now defines M20, mutating `applyServerPatch`'s `shouldRenderProposal` guard and requiring `cut-lab-adjust.test.ts` to fail, with the failed assertion recorded. This supplies the missing killing mutation for the changed TypeScript test.

## Round-1 Concern 2 — PARTIALLY FOLDED — HIGH remains

`07.1-03-PLAN.md:317-337` now defines M19 at `CutLabUiPatchBuilder.cs:125`, but explicitly accepts that it does not kill the new `Assert.NotNull` assertions added to `CutLabApiControllerTests.cs:869` and `:917`; those tests use `ProposalPatchBuilder` and never exercise the mutation. Task 2 still changes those test methods under its 16-site assertion sweep (`07.1-03-PLAN.md:207-220`), while Task 3 claims to prove every changed test. Add a mutation against the `ProposalPatchBuilder` fixture value that makes both new assertions fail, or avoid adding assertions to test-double paths by narrowing through an existing value guarantee.

## HIGH — M20 does not guarantee the changed content assertion is the kill

`07.1-03-PLAN.md:338-348` permits removing or negating the `shouldRenderProposal` guard, then requires the strengthened content assertion to fail. With a null proposal, either permitted edit enters the rendering block and calls `renderRoundBanner` before `renderProposalCard`; `DeckFlow.Web/wwwroot/ts/cut-lab.ts:2163-2174` immediately reads `nextProposal.isTerminal`. The mutated test therefore can fail with a `TypeError` before reaching the content assertion, contradicting both the mutation's stated rewrite/clear mechanism and `07.1-03-PLAN.md:363`'s requirement to record which assertion failed. Specify a mutation that preserves execution, such as replacing the null-proposal branch with an explicit proposal-region rewrite/clear, and require the named strengthened assertion to fail.

## MEDIUM — M20's source range excludes the guard it says to mutate

`07.1-03-PLAN.md:340-341` identifies the proposal-rendering guard as `cut-lab.ts:3245-3253`, but the census and source place `if (shouldRenderProposal)` at `DeckFlow.Web/wwwroot/ts/cut-lab.ts:3254`; the cited range ends one line before it. Change the reference to include `:3254-3259` or cite the exact `if (shouldRenderProposal)` identifier.

---
### Review of 07.1-04-PLAN.md — CONVERGED

# Round 2 Review — 07.1-04

## Round-1 disposition — FOLDED

The round-1 HIGH finding, “M10 cannot produce either required failure message,” is FOLDED at `.planning/workstreams/cycle21-cut-lab/phases/07.1-cutlab-review-findings-closeout/07.1-04-PLAN.md:104-132,174-181,205-207`. The fold explicitly prohibits separate `twins.Count > 0` guards in both repaired tests, makes `assertedEvidenceCount > 0` and `distinctEvidenceCount > 0` their sole exact-population preconditions, and retains M10’s required per-test failure messages. M10 can now reach both required lower-bound assertions.

## Result — CONVERGED

No BLOCK, HIGH, MEDIUM, or LOW defects were introduced by the fold. Plan claims only phase-wide mutation identifiers M10, M11, and M12, matching plan 04’s allocation.

---
### Review of 07.1-05-PLAN.md — NOT CONVERGED — 1 HIGH, 2 MEDIUM

## HIGH — 07.1-05-PLAN.md:210 — Fold makes acceptance arithmetic unattainable

Required edit replaces one declaration and inserts one comment, so `git diff --numstat` reports `2` additions and `1` deletion per CSS file, not “exactly 2 changed lines” and not “never more than 2.” Replace criterion with exact `2 1 <file>` numstat expectation, or use a check that explicitly counts two touched destination lines.

## MEDIUM — 07.1-05-PLAN.md:143 — Round-1 Task 1 finding only partially folded

Check now validates 24 unique theme rows and two decimal values per row, but `grep -qi 'baseline'`, `grep -qiE 'pass'`, and `grep -qiE 'fail'` do not validate required baseline per-test results/wall times, three failing themes, two nearest passes, or per-file `--muted` declaration counts at lines 147-152. Census can omit those required facts while check passes. Parse and assert these fields explicitly.

## MEDIUM — 07.1-05-PLAN.md:201 — Round-1 Task 2 finding only partially folded

New check recomputes live contrast, enforces one declaration, and limits changed CSS filenames, but never reads or validates required `## After` rows at lines 194 and 208. It also permits arbitrary extra edits inside expected theme files. Deleting `## After` entirely leaves check green. Assert post-change census rows and corrected per-file numstat/diff scope.

---
### Review of 07.1-06-PLAN.md — CONVERGED

# Round 2 Review Findings — 07.1-06

## Round-1 Fold Assessment

`07.1-06-PLAN.md:81-92,160-161,240,256` — **FOLDED**. File-wide `createElement == 0` criterion was removed; G-6-specific name and inline-position criteria replaced it; D-10, threat register, and verification summary consistently preserve G-5’s unrelated probe. Fold’s new pre-mutation count error is a separate LOW finding below.

## Finding — LOW

`.planning/workstreams/cycle21-cut-lab/phases/07.1-cutlab-review-findings-closeout/07.1-06-PLAN.md:87` — D-10 says both replacement-target greps return `1` before Task 1, but `grep -c 'Workflow control beneath pinned proposal' DeckFlow.Web/e2e/cut-lab-workflow-ux.spec.ts` returns `2` at HEAD: one hit at `cut-lab-workflow-ux.spec.ts:194` assigns button text and another at `:202` locates that button. The `style.position = 'fixed'` grep alone returns `1`. Acceptance remains attainable because Task 1 removes both name hits and takes the count to `0`; correct D-10’s pre-mutation evidence to say the greps return `2` and `1`, respectively.

## Outcome

**CONVERGED.** Zero BLOCK and zero HIGH findings. Plan claims only phase-wide mutation IDs M15, M16, and M17, matching plan 06’s allocation. Remaining LOW finding is a non-blocking evidence-count correction; acceptance criteria remain attainable.

---
### Review of 07.1-07-PLAN.md — NOT CONVERGED — 3 HIGH, 1 MEDIUM (1 HIGH rejected, see below)

# Round 2 Plan Review Findings

## HIGH — PARTIALLY FOLDED — Task 1 verifier still does not certify the regression sweep

The fold captures producer exit statuses and adds artifact-presence checks, but the automated verifier never runs Playwright and only greps `07.1-REGRESSION.md` for the two project names (`07.1-07-PLAN.md:158-159`). It also does not enforce the build's at-most-15-warning threshold, xUnit's at-least-2302-passed/nonzero-executed thresholds, Vitest's strictly-greater-than-131-test threshold, zero Playwright failures, or the required comparison-table rows (`07.1-07-PLAN.md:161-171`). Mutations: omit Playwright execution and write both project names into the report; produce 16 warnings; report 2301 passed tests; or leave the comparison table empty. Each can still reach `REGRESSION-SWEEP-GREEN`. Run the bounded Playwright command from the verifier, capture its status, and add machine assertions for every numeric and artifact criterion.

## MEDIUM — PARTIALLY FOLDED — Task 2 automated check does not prove its gate edits were committed

The fold replaces the historical `git log` criterion with exact clean-diff and `HEAD^..HEAD` path checks (`07.1-07-PLAN.md:243-244`), but neither check appears in Task 2's automated verifier (`07.1-07-PLAN.md:233-234`). Mutation: edit both gate files without committing them; all content greps can pass and emit `GATES-CLOSED`, while the task's required commit does not exist. Add both exact-path commit assertions to the automated verifier.

## HIGH — NEW — Task 4 verifier can certify an empty or incomplete Resolution section

Task 4 counts every numbered line in the entire moved capture, not the entries after `## Resolution`, and checks only the spillover todo's basename (`07.1-07-PLAN.md:329-330`). It never checks that criterion 12 cites the spillover todo by full path or that criterion 13 cites either gate path, despite requiring both at `07.1-07-PLAN.md:332-336`. Mutation: append an empty `## Resolution` heading to a capture that already contains 13 numbered acceptance criteria, mention only the spillover basename elsewhere, and omit both gate citations; `SOURCE-CAPTURE-RESOLVED` can still print. Scope the 13-entry count to the Resolution section and assert the full paths for the spillover todo and both gates there.

## HIGH — FOLDED WRONG — Replacement Playwright command violates the repository's mandatory waiting protocol

The fold replaces the blocking foreground launch with a lifecycle-owning block, but its readiness implementation is exactly the prohibited `for`/`curl`/`sleep` polling loop (`07.1-07-PLAN.md:121-127`). Repository instructions say never write a readiness loop and require one blocking waiter such as `npx wait-on` or a single `timeout ... until curl` command. The acceptance criterion then requires this prohibited pinned poll (`07.1-07-PLAN.md:166`), so a compliant executor cannot satisfy the plan literally. Replace the loop with the repository-prescribed blocking waiter while retaining the trap and both captured Playwright statuses.

## HIGH — PARTIALLY FOLDED — Task 2 verifier can erase gate evidence and still report closure

The fold fixes the discarded `grep` status, but the replacement verifier checks only zero `· OPEN` markers, generic `CLOSED` text, and coarse plan-id counts (`07.1-07-PLAN.md:233-234`). It does not enforce that the 2026-08-22 gate retains all six original rows, that every row has a resolution and mutation id, that CLOSED lines name phase 07.1 and a date, or that the F-6 census details exist, although the task requires those facts at `07.1-07-PLAN.md:196-217` and `:239-241`. Mutation: delete the six-row table and all mutation evidence, then add enough unrelated `07.1-0` strings and a bare `CLOSED`; `GATES-CLOSED` still prints. Add structural checks for preserved row ids, per-row plan/mutation evidence, dated phase closure lines, and the required F-6 census content.

## Round-1 fold dispositions

1. Task ordering creates a false completion attestation — **FOLDED** at `07.1-07-PLAN.md:228-231` and `:305-339`; Task 4 now performs the move only after Tasks 2 and 3.
2. Task 2's automated check passes while review gates remain open — **PARTIALLY FOLDED** at `07.1-07-PLAN.md:233-241`; zero-open assertions now propagate failure, but required preserved-row and mutation evidence remains unchecked.
3. Task 1's automated check certifies neither a successful build nor the regression sweep — **PARTIALLY FOLDED** at `07.1-07-PLAN.md:158-171`; producer statuses are captured, but Playwright and several numeric/artifact thresholds remain unverified.
4. Task 3's automated check is vacuous for most required todo content — **FOLDED** at `07.1-07-PLAN.md:291-300`; mechanically observable content is asserted and line/ordering judgments are explicitly assigned a targeted review.
5. Raw regression capture can commit an overridden admin password — **FOLDED** at `07.1-07-PLAN.md:89-97` and `:167`; server output stays private and tracked quotations require redaction.
6. Gate commit acceptance check can pass on an unrelated historical commit — **PARTIALLY FOLDED** at `07.1-07-PLAN.md:243-244`; exact checks replace historical `git log`, but they are absent from the automated verifier at `:233-234`.
7. Playwright step cannot execute literally and leaves server lifecycle unspecified — **FOLDED WRONG** at `07.1-07-PLAN.md:110-145` and `:166`; lifecycle handling was added, but its required readiness loop violates repository execution rules.

Mutation allocation audit: PASS. Plan 07 is rollup-only and claims ownership of no mutation id; all cited ids remain attributed to plans 01-06 according to the phase-wide allocation.

---

## Round 1 — 2026-08-22 (superseded; folded in `46241c27`)

**Coordinate warning:** every `file:line` citation in this Round 1 section is valid only against `a13b9499` — the tree the round-1 reviewer actually read. It predates the round-1 fold and the blind-verification fold, so line numbers below have already moved. Do not trust these citations against the current tree. Anchor by the quoted content instead.

**Reviewer:** Codex `gpt-5.6-sol`, effort medium, one dispatch per plan document (seven dispatches).
**Date:** 2026-08-22
**Verdict: NOT converged — approximately 13 HIGH findings across six of the seven plans.** Only
`07.1-05` returned CONVERGED. Execution is blocked until these are folded.

**Provenance note.** These seven reviews were dispatched by dev-agent's `plan_review` stage, which
then ended its own session while they were still running - its closing message was "Seven Codex
plan-review dispatches + Gemini running. Waiting." All seven completed roughly two minutes later,
with no session left to collect them, so the stage reported success having written nothing and
`decide()` re-dispatched the whole round. The run was stopped and the completed outputs were
harvested here by hand from `/tmp/gsd-review-004aMb/`. The findings below are Codex's own text,
unedited; only these headers are added.

## Cross-cutting themes

1. **Vacuous automated checks are the dominant defect class**, exactly as in the findings this phase
   exists to close. Several task checks pass against the unfixed code - `07.1-05` Task 1 and Task 2,
   `07.1-07` Tasks 1 and 2, `07.1-03` and `07.1-06` both entirely.
2. **Missing or invalid killing mutations.** `07.1-03` and `07.1-06` each ship a changed test with no
   mutation that kills it; `07.1-05`'s M14 is a control that cannot demonstrate the blind spot it
   claims.
3. **Consumer enumeration was incomplete on the F-5 fix** - see `07.1-02`'s first HIGH, which is the
   same failure mode as R2-1 itself.

---


## Review of 07.1-01-PLAN.md

# NOT CONVERGED

## Summary

Plan does not achieve stated goal as written. One HIGH vacuous-test defect makes Home/End acceptance impossible. No phantom APIs found; no later-wave dependency found.

## Strengths

- Census resolves all five location claims and all claimed symbols: `/tmp/gsd-review-004aMb/census/07.1-01-evidence.md:14`, `:30`, `:46`, `:62`, `:78`, `:113-320`.
- M1 and M2 are isolated killing mutations with fingerprint restoration; M3 is correctly documented as a non-kill (`07.1-01-PLAN.md:171-211`).
- Wave ordering is correct: plan 01 produces the mutation protocol consumed by later plans and depends on none (`07.1-01-PLAN.md:5`, `/tmp/gsd-review-004aMb/gsd-review-shared.md:21-30`).
- Scope stays within Cut Lab TypeScript/tests plus planning evidence (`07.1-01-PLAN.md:6-10`). No additional security or performance concern found.

## Concerns

- **[HIGH] Vacuous/impossible keyboard test** — Plan tab is step 3, so `Home` reaches step 1 and `End` reaches step 5. Step 2 plus two `ArrowLeft` events traverses step 1 and step 5, never Plan (`DeckFlow.Web/ts-tests/cut-lab-step-tabs.test.ts:19-23`, `07.1-01-PLAN.md:111-113`). Test still passes with `activateStepTab` guard removed. `<done>` claim that Home/End land on Plan is impossible (`07.1-01-PLAN.md:160`).

- **[MEDIUM] Invalid initial selection** — Existing case uses `renderFixture()`, selecting step 1 (`DeckFlow.Web/ts-tests/cut-lab-step-tabs.test.ts:71`), while plan requires step 2 to remain selected (`07.1-01-PLAN.md:107`). Test fails before production change.

- **[MEDIUM] Missing `BUILD-EOL` verification** — Requirement is declared (`07.1-01-PLAN.md:12`), but required `git diff --stat` versus `git diff --ignore-all-space --stat` comparison is absent (`/tmp/gsd-review-004aMb/gsd-review-shared.md:71-73`, `07.1-01-PLAN.md:147-158`, `:255-265`).

Vacuity assessment:

- Extended existing ArrowRight case: **would fail** under M1 and M2 after changing fixture to `renderFixture(2)` (`07.1-01-PLAN.md:89-108`, `:181-190`).
- Home/End/wrap-around case: **would not fail** under M1; it never activates Plan (`07.1-01-PLAN.md:110-113`).
- Direct-click case: **would fail** under M1 on selection/panel assertions (`07.1-01-PLAN.md:115-118`, `:181-184`).

## Suggestions

- Replace wrap path with step 4 → `ArrowLeft` → Plan; require unchanged `aria-selected` and visible-panel IDs. M1 must kill that case.
- Treat Home/End only as boundary-navigation checks; remove claim that they reach Plan.
- Initialize extended case with `renderFixture(2)`.
- Add final whole-diff EOL comparison.

## Risk Assessment

**HIGH** — core R2-1 fix is sound, but one claimed keyboard path is impossible and its proposed test cannot observe regression.

Persisted: [findings.md](/home/clunt/.claude/codex/runs/20260822-185859-2772039/findings.md)
---

## Review of 07.1-02-PLAN.md

# Summary

**NOT CONVERGED.** Plan cannot achieve F-5 side-effect goal: existing initialization still invokes collapse defaults (`DeckFlow.Web/wwwroot/ts/cut-lab.ts:436-444`, `:4470`).

No phantom APIs found. Census resolves current location and symbol claims; `setVisibleStepPanel` is explicitly planned as new (`/tmp/gsd-review-004aMb/census/07.1-02-evidence.md:441`).

# Strengths

- **LOW** — Panel-visibility tests for selected steps would kill missing initialization hiding via M4 (`07.1-02-PLAN.md:138-148`, `:268-273`).
- **LOW** — Paired load/click collapse controls correctly distinguish “not on load” from deleting behavior entirely (`07.1-02-PLAN.md:149-158`, `:279-286`).
- **LOW** — No dependency on Plans 03–07. Only Plan 01’s guard and mutation protocol are prerequisites (`/tmp/gsd-review-004aMb/gsd-review-shared.md:22-30`).
- **LOW** — Scope, security, and performance treatment is proportionate; no Razor/package expansion and only five tabpanels are traversed (`07.1-02-PLAN.md:161-193`, `:313-329`).

# Concerns

- **HIGH** — `initialize()` independently calls `applyInitialSectionCollapseState()`, whose no-storage branch invokes `applyDefaultSectionCollapseState(...)`. Planned edits leave that path unchanged, so first-load collapse test remains red after implementation (`DeckFlow.Web/wwwroot/ts/cut-lab.ts:436-444`, `:4470`; census `:99`).
- **HIGH** — Acceptance gate requires at least three `applyDefaultSectionCollapseState` occurrences. Third occurrence is defective no-storage initialization call, not a stored-state path (`07.1-02-PLAN.md:208`; `DeckFlow.Web/wwwroot/ts/cut-lab.ts:436-444`).
- **MEDIUM** — Tests do not clear `window.localStorage`. Added `<details>` toggle listeners can leak `deckflow.cutlab.sections` between cases, invalidating no-storage and M6 assumptions (`DeckFlow.Web/ts-tests/cut-lab-step-tabs.test.ts:45`; isolation precedent at `DeckFlow.Web/ts-tests/cut-lab-section-collapse.test.ts:38`).
- **MEDIUM** — Pass counts are unattainable. Plan 01 leaves 5 tests; Task 1 adds three, yielding 8 rather than 9. Task 2 adds one, yielding 9 rather than 10 (`07.1-01-PLAN.md:151`; `07.1-02-PLAN.md:202`, `:242`).
- **MEDIUM** — Cross-entry invariant passes a no-op click regression. Deleting `activateStepTab(button, true)` leaves initialized step 2 selected and visible, satisfying its DOM-derived comparison despite step 4 click doing nothing (`07.1-02-PLAN.md:225-236`; `DeckFlow.Web/wwwroot/ts/cut-lab.ts:549`).
- **MEDIUM** — M7 kills through undefined access/type failure, not hidden-panel observation. Removing early return leaves `selectedTab` undefined (`07.1-02-PLAN.md:288-290`; census `:117`).

Vacuity assessment:

| Proposed test | Regression failure? |
|---|---|
| Corrected selected-panel case | Yes; M4 |
| Per-selected-step visibility | Yes; M4 |
| No collapse default on load | Intended yes, but baseline remains failing |
| Collapse default on click | Yes; M6, after storage isolation |
| No-selection fallback | Yes for real hiding; proposed M7 is invalid |
| Cross-entry invariant | **No** for no-op activation |

# Suggestions

1. Remove no-storage default invocation from `applyInitialSectionCollapseState()` while retaining stored-state restoration; test both branches (`DeckFlow.Web/wwwroot/ts/cut-lab.ts:436-444`).
2. Expect exactly two `applyDefaultSectionCollapseState` occurrences: definition and activation call (`07.1-02-PLAN.md:208`).
3. Clear localStorage per test (`DeckFlow.Web/ts-tests/cut-lab-step-tabs.test.ts:45`).
4. Change pass gates to 8 then 9 (`07.1-02-PLAN.md:202`, `:242`).
5. Add clicked-tab selection assertion and no-op activation mutation (`07.1-02-PLAN.md:225-236`).
6. Replace M7 with compiling mutation: `if (!selectedTab) { setVisibleStepPanel(1); return; }`, requiring 1 visible where 5 expected (`07.1-02-PLAN.md:288-290`).

# Risk Assessment

**HIGH** — Core side-effect requirement remains unsatisfied, while structural acceptance explicitly preserves defective call. Findings persisted to [findings.md](/home/clunt/.claude/codex/runs/20260822-185859-2772065/findings.md).
---

## Review of 07.1-03-PLAN.md


===== CODEX-RUN SALVAGED INCREMENTAL FINDINGS =====
Source: /home/clunt/.claude/codex/runs/20260822-185859-2772113/findings.md
# Plan Review Findings

## Concern 2 — HIGH — C# assertion sweep also lacks killing mutation

Task 2 requires new `Assert.NotNull(patch.NextProposal)` assertions across 16 dereferences (`07.1-03-PLAN.md:207-218`), including existing unguarded examples at census lines 30-44 and 62-76. M8 restores only the type declaration and initializer while leaving `BuildAsync` values unchanged (`07.1-03-PLAN.md:293-300`), so every new assertion remains green; M9 is unrelated TypeScript. The required proof mutation is to make one applicable `BuildAsync` proposal path return `null` at `DeckFlow.Web/Services/CutLab/CutLabUiPatchBuilder.cs:125` and show each affected test class's new assertion fails, or avoid changing these tests by using compiler-safe locals only where an existing assertion already establishes non-null. The phase mandate is explicit at `/tmp/gsd-review-004aMb/gsd-review-shared.md:84`.

## Concern 1 — HIGH — changed test lacks killing mutation

`07.1-03-PLAN.md:258-268` changes `cut-lab-adjust.test.ts`, but M8 mutates only C# nullability and M9 is explicitly a non-kill (`07.1-03-PLAN.md:293-306`). Neither proves the changed DOM assertion fails when proposal-preservation regresses, violating the phase rule that every changed test has a named, demonstrated killing mutation (`/tmp/gsd-review-004aMb/gsd-review-shared.md:84`). The existing test already asserts preserved proposal text at `DeckFlow.Web/ts-tests/cut-lab-adjust.test.ts:396`; a suitable mutation is to remove or negate `applyServerPatch`'s `shouldRenderProposal` guard at `DeckFlow.Web/wwwroot/ts/cut-lab.ts:3245-3253` so null light patches rewrite or clear the proposal region, then require the test to fail. Without that named run, the phase's anti-vacuity goal is unmet.

===== END SALVAGED INCREMENTAL FINDINGS =====

===== CODEX-RUN SYNTHESIZED REMAINING WORK (inferred by the wrapper, not authored by the model) =====
Source: /home/clunt/.claude/codex/runs/20260822-185859-2772113/remaining.synthesized.md
# Remaining work -- WRAPPER-SYNTHESIZED, NOT WRITTEN BY THE MODEL

This dispatch was terminated (turn-cap, observed 40) before it wrote remaining.md.
Everything below was inferred by codex-run.sh from the watchdog checkpoint. It is
evidence of where the run stopped, NOT the model's own plan for what is left. Verify
each item against the working tree before acting on it.

## Where the run stopped (last assistant message)

### Concern 2 — HIGH — C# assertion sweep also lacks killing mutation

Task 2 requires new `Assert.NotNull(patch.NextProposal)` assertions across 16 dereferences (`07.1-03-PLAN.md:207-218`), including existing unguarded examples at census lines 30-44 and 62-76. M8 restores only type declaration/initializer while leaving `BuildAsync` values unchanged (`07.1-03-PLAN.md:293-300`), so every new assertion remains green; M9 is unrelated TypeScript. Required proof mutation: make one applicable `BuildAsync` proposal path return `null` at `DeckFlow.Web/Services/CutLab/CutLabUiPatchBuilder.cs:125` and show each affected test class’s new assertion fails, or avoid changing these tests by using compiler-safe locals only where an existing assertion already establishes non-null. Phase mandate is explicit at `/tmp/gsd-review-004aMb/gsd-review-shared.md:84`.

## Files the run may have modified -- verify each before trusting the tree

- `/home/clunt/.claude/codex/runs/20260822-185859-2772113/findings.md`
- `tools.apply`

## Artifacts this run left behind

- `brief.txt` (1832 bytes)
- `detached.out` (378 bytes)
- `dispatch-env.txt` (107 bytes)
- `findings.md` (1762 bytes)
- `job.json` (315 bytes)
- `pid` (8 bytes)
- `projection.json` (166 bytes)
- `prompt.txt` (4142 bytes)
- `read-ledger.tsv` (10645 bytes)
- `scope-before` (0 bytes)
- `scope-before-fingerprints` (2 bytes)
- `scope-head-before` (41 bytes)
- `scope-root` (59 bytes)
- `stdin.txt` (1832 bytes)
- `watchdog-state.json` (382 bytes)

## Next session

- Read `/home/clunt/.claude/codex/runs/20260822-185859-2772113/checkpoint.md` for the full stop state.
- Confirmed findings, if any, are in `/home/clunt/.claude/codex/runs/20260822-185859-2772113/findings.md`.
- Do NOT --resume: the session is at or over its cap. Dispatch fresh AGAINST these files.

===== END SYNTHESIZED REMAINING WORK =====

===== CODEX-RUN WATCHDOG CHECKPOINT =====
Bound tripped: turn-cap
Observed: 40 (threshold: 40)
Rollout: /home/clunt/.codex/sessions/2026/08/22/rollout-2026-08-22T18-59-02-01a02c20-cc31-79a2-ab32-1b18b062e154.jsonl
Checkpoint: /home/clunt/.claude/codex/runs/20260822-185859-2772113/checkpoint.md
Write the evidence gathered so far to a file and start a FRESH dispatch against that file -- do not --resume, because the session is at or over the cap.
Re-dispatch: /home/clunt/.claude/scripts/codex-run.sh --model gpt-5.6-sol --effort medium --sandbox danger-full-access --timeout 1500 --cwd /mnt/c/users/chrislunt/source/personal/deckflow-cycle21-fix < /home/clunt/.claude/codex/runs/20260822-185859-2772113/checkpoint.md
===== END CODEX-RUN WATCHDOG CHECKPOINT =====

---

## Review of 07.1-04-PLAN.md


===== CODEX-RUN SALVAGED INCREMENTAL FINDINGS =====
Source: /home/clunt/.claude/codex/runs/20260822-185859-2772171/findings.md
# Plan Review Findings

## HIGH — M10 cannot produce either required failure message

`.planning/workstreams/cycle21-cut-lab/phases/07.1-cutlab-review-findings-closeout/07.1-04-PLAN.md:94-112` inserts `Assert.True(twins.Count > 0, "...")` before both exact-population lower bounds, while `.planning/workstreams/cycle21-cut-lab/phases/07.1-cutlab-review-findings-closeout/07.1-04-PLAN.md:158-163` defines M10 as making `ComputeTwins` return an empty sequence. Therefore each test stops at `twins.Count > 0`; neither the executed-assertion counter nor `distinctEvidenceCount > 0` is reached. This contradicts the required M10 messages at `.planning/workstreams/cycle21-cut-lab/phases/07.1-cutlab-review-findings-closeout/07.1-04-PLAN.md:189` and prevents plan acceptance. Census confirms `ComputeTwins` is the shared seam and both target call sites exist at `/tmp/gsd-review-004aMb/census/07.1-04-evidence.md:80-101`. Fix by removing the redundant `twins.Count > 0` checks from these two tests, or change M10/message acceptance to target the first checks and add separate empty-evidence mutations that prove the exact-population guards.

===== END SALVAGED INCREMENTAL FINDINGS =====

===== CODEX-RUN SYNTHESIZED REMAINING WORK (inferred by the wrapper, not authored by the model) =====
Source: /home/clunt/.claude/codex/runs/20260822-185859-2772171/remaining.synthesized.md
# Remaining work -- WRAPPER-SYNTHESIZED, NOT WRITTEN BY THE MODEL

This dispatch was terminated (turn-cap, observed 40) before it wrote remaining.md.
Everything below was inferred by codex-run.sh from the watchdog checkpoint. It is
evidence of where the run stopped, NOT the model's own plan for what is left. Verify
each item against the working tree before acting on it.

## Where the run stopped (last assistant message)

## HIGH — M10 cannot produce either required failure message

`.planning/workstreams/cycle21-cut-lab/phases/07.1-cutlab-review-findings-closeout/07.1-04-PLAN.md:94-112` inserts `Assert.True(twins.Count > 0, "...")` before both exact-population lower bounds, while `.planning/workstreams/cycle21-cut-lab/phases/07.1-cutlab-review-findings-closeout/07.1-04-PLAN.md:158-163` defines M10 as making `ComputeTwins` return an empty sequence. Therefore each test stops at `twins.Count > 0`; neither executed-assertion counter nor `distinctEvidenceCount > 0` is reached. This contradicts required M10 messages at `.planning/workstreams/cycle21-cut-lab/phases/07.1-cutlab-review-findings-closeout/07.1-04-PLAN.md:189` and prevents plan acceptance. Census confirms `ComputeTwins` is shared seam and both target call sites exist at `/tmp/gsd-review-004aMb/census/07.1-04-evidence.md:80-101`. Fix by removing redundant `twins.Count > 0` checks from these two tests, or change M10/message acceptance to target first checks and add separate empty-evidence mutations proving exact-population guards.

## Files the run may have modified -- verify each before trusting the tree

- `/home/clunt/.claude/codex/runs/20260822-185859-2772171/findings.md`
- `tools.apply`

## Artifacts this run left behind

- `brief.txt` (1832 bytes)
- `detached.out` (379 bytes)
- `dispatch-env.txt` (107 bytes)
- `findings.md` (1137 bytes)
- `job.json` (315 bytes)
- `pid` (8 bytes)
- `projection.json` (166 bytes)
- `prompt.txt` (4142 bytes)
- `read-ledger.tsv` (10747 bytes)
- `scope-before` (0 bytes)
- `scope-before-fingerprints` (2 bytes)
- `scope-head-before` (41 bytes)
- `scope-root` (59 bytes)
- `stdin.txt` (1832 bytes)
- `watchdog-state.json` (382 bytes)

## Next session

- Read `/home/clunt/.claude/codex/runs/20260822-185859-2772171/checkpoint.md` for the full stop state.
- Confirmed findings, if any, are in `/home/clunt/.claude/codex/runs/20260822-185859-2772171/findings.md`.
- Do NOT --resume: the session is at or over its cap. Dispatch fresh AGAINST these files.

===== END SYNTHESIZED REMAINING WORK =====

===== CODEX-RUN WATCHDOG CHECKPOINT =====
Bound tripped: turn-cap
Observed: 40 (threshold: 40)
Rollout: /home/clunt/.codex/sessions/2026/08/22/rollout-2026-08-22T18-59-02-01a02c20-ccf6-75c2-b205-49a7244e9f9b.jsonl
Checkpoint: /home/clunt/.claude/codex/runs/20260822-185859-2772171/checkpoint.md
Write the evidence gathered so far to a file and start a FRESH dispatch against that file -- do not --resume, because the session is at or over the cap.
Re-dispatch: /home/clunt/.claude/scripts/codex-run.sh --model gpt-5.6-sol --effort medium --sandbox danger-full-access --timeout 1500 --cwd /mnt/c/users/chrislunt/source/personal/deckflow-cycle21-fix < /home/clunt/.claude/codex/runs/20260822-185859-2772171/checkpoint.md
===== END CODEX-RUN WATCHDOG CHECKPOINT =====

---

## Review of 07.1-05-PLAN.md

# Summary

CONVERGED — zero BLOCK/HIGH findings. Plan achieves F-6, but three verification controls need correction. Overall risk: MEDIUM.

# Strengths

- No phantom APIs. `workflowComponentThemes`, `workflowViewports`, `assertContrastFloor`, and workflow assertions exist at `DeckFlow.Web/e2e/cut-lab-theme-readability.spec.ts:37`, `:43`, `:166`, and `:494-498`; census confirms them at `/tmp/gsd-review-004aMb/census/07.1-05-evidence.md:81-107` and `:276-320`.
- Census “missing files” `4.5` and `4.60` are ratio-parsing artifacts, not asserted files (`/tmp/gsd-review-004aMb/census/07.1-05-evidence.md:14`, `:22`).
- Final workflow test directly observes rendered contrast and visibility. Reverting grixis to `#91889d` should fail through `assertContrastFloor` (`DeckFlow.Web/e2e/cut-lab-theme-readability.spec.ts:166-177`, `:482-499`).
- Candidate ratios independently check out: grixis 4.6259/6.4230, planeswalker-dark 4.6061/6.2543, jund 4.6061/5.7977 against `--panel`/`--bg` (`07.1-05-PLAN.md:174-182`).
- No later-wave dependency. Plan consumes Plan 01’s mutation protocol and follows Plan 04; Plans 06–07 are later, unrelated work (`07.1-05-PLAN.md:6`, `/tmp/gsd-review-004aMb/gsd-review-shared.md:22-30`).
- No new authorization, data, dependency, or production-host surface (`/tmp/gsd-review-004aMb/gsd-review-shared.md:160`).

# Concerns

## MEDIUM — Task 1 automated check is vacuous

`.planning/workstreams/cycle21-cut-lab/phases/07.1-cutlab-review-findings-closeout/07.1-05-PLAN.md:142` checks only that census file exists and counts every occurrence of `site-`; it does not verify exactly 24 table rows, either contrast ratio column, failing/nearest-pass themes, declaration counts, or baseline results required at `.planning/workstreams/cycle21-cut-lab/phases/07.1-cutlab-review-findings-closeout/07.1-05-PLAN.md:145-153`. It can pass with prose containing 24 repeated `site-` strings and no census. Prove observability by removing one generated row’s `--bg` ratio or deleting a theme row; automated check must fail. Replace count with parser/check validating exact 24-theme key set, unique rows, numeric `--panel` and `--bg` ratios, and required baseline fields.

## MEDIUM — Task 2 automated check cannot observe contrast fix

`.planning/workstreams/cycle21-cut-lab/phases/07.1-cutlab-review-findings-closeout/07.1-05-PLAN.md:199` only prints three `--muted` declarations. It exits successfully with original failing values, duplicate declarations, values below 4.5 against `--panel` or `--bg`, missing census `## After` rows, or unrelated stylesheet edits, despite required behavior at `.planning/workstreams/cycle21-cut-lab/phases/07.1-cutlab-review-findings-closeout/07.1-05-PLAN.md:202-211`. Prove observability by restoring `site-grixis.css` to `#91889d`; check currently stays green. Replace it with executable contrast assertion over every census-identified failing theme plus exact declaration-count and diff-scope checks.

## MEDIUM — M14 is not valid control for old matrix’s blind spot

Mutation M14 narrows workflow matrix while explicitly leaving all raised tokens in place, then expects test to pass at `.planning/workstreams/cycle21-cut-lab/phases/07.1-cutlab-review-findings-closeout/07.1-05-PLAN.md:261-264`. That pass observes correct production behavior, so it cannot demonstrate old three-theme matrix misses broken grixis contrast. Prove claimed blind spot by combining narrowed matrix with grixis `--muted` revert to `#91889d`; old matrix must stay green while final 24-theme matrix fails. Keep M13 as positive kill; make M14 paired negative control.

## LOW — Task 2 gives conflicting edit-scope instructions

Task 2 requires new `/* Why: ... */` line above each changed token at `.planning/workstreams/cycle21-cut-lab/phases/07.1-cutlab-review-findings-closeout/07.1-05-PLAN.md:189`, then says change only one declaration line per file at `.planning/workstreams/cycle21-cut-lab/phases/07.1-cutlab-review-findings-closeout/07.1-05-PLAN.md:196`. Acceptance permits two changed lines at `.planning/workstreams/cycle21-cut-lab/phases/07.1-cutlab-review-findings-closeout/07.1-05-PLAN.md:210`. Remove comment requirement as unnecessary scope, or change preservation sentence to permit exactly declaration plus one comment.

# Suggestions

Test observability:

| Check/test | Fails on F-6 regression? | Required mutation |
|---|---:|---|
| Task 1 automated census check (`07.1-05-PLAN.md:142`) | No | Delete theme row or `--bg` ratio |
| Task 2 automated token check (`07.1-05-PLAN.md:199`) | No | Restore grixis `#91889d` |
| Named-element sweep (`DeckFlow.Web/e2e/cut-lab-theme-readability.spec.ts:409-477`) | Not necessarily; it does not select intake summary | Force only `.cutlab-intake-summary` below 4.5 |
| Widened workflow-component test (`DeckFlow.Web/e2e/cut-lab-theme-readability.spec.ts:482-499`) | Yes | M13 |
| M14 as written (`07.1-05-PLAN.md:261-264`) | No, but behavior remains correct | Narrow matrix and revert grixis simultaneously |

# Risk Assessment

MEDIUM — implementation design and primary browser assertion achieve stated goal; weak task checks and invalid M14 control reduce execution confidence. No security or later-wave dependency issue found.

CONVERGED
---

## Review of 07.1-06-PLAN.md


===== CODEX-RUN SALVAGED INCREMENTAL FINDINGS =====
Source: /home/clunt/.claude/codex/runs/20260822-185912-2790187/findings.md
# Plan Review Findings

## Concern — HIGH

`.planning/workstreams/cycle21-cut-lab/phases/07.1-cutlab-review-findings-closeout/07.1-06-PLAN.md:147` requires `grep -c 'createElement' DeckFlow.Web/e2e/cut-lab-workflow-ux.spec.ts` to return `0`, while Task 1 says not to change any test except G-6 (`07.1-06-PLAN.md:135-139`). Census evidence at `/tmp/gsd-review-004aMb/census/07.1-06-evidence.md:214-243` shows `DeckFlow.Web/e2e/cut-lab-workflow-ux.spec.ts:145` already calls `document.createElement('div')` inside G-5. Replacing G-6 cannot satisfy this criterion. Mutation proving the mismatch: perform exactly the prescribed G-6 replacement; `grep -c 'createElement' ...` remains `1`. Narrow criterion to synthetic G-6 markup (for example, absence of `Workflow control beneath pinned proposal` and `style.position = 'fixed'`), or explicitly authorize and design removal of G-5’s separate injected scroll-depth element. Until corrected, plan cannot achieve its stated goal under its own scope.

===== END SALVAGED INCREMENTAL FINDINGS =====

===== CODEX-RUN SYNTHESIZED REMAINING WORK (inferred by the wrapper, not authored by the model) =====
Source: /home/clunt/.claude/codex/runs/20260822-185912-2790187/remaining.synthesized.md
# Remaining work -- WRAPPER-SYNTHESIZED, NOT WRITTEN BY THE MODEL

This dispatch was terminated (turn-cap, observed 41) before it wrote remaining.md.
Everything below was inferred by codex-run.sh from the watchdog checkpoint. It is
evidence of where the run stopped, NOT the model's own plan for what is left. Verify
each item against the working tree before acting on it.

## Where the run stopped (last assistant message)

## Concern — HIGH

`.planning/workstreams/cycle21-cut-lab/phases/07.1-cutlab-review-findings-closeout/07.1-06-PLAN.md:147` requires `grep -c 'createElement' DeckFlow.Web/e2e/cut-lab-workflow-ux.spec.ts` to return `0`, while Task 1 says not to change any test except G-6 (`07.1-06-PLAN.md:135-139`). Census evidence at `/tmp/gsd-review-004aMb/census/07.1-06-evidence.md:214-243` shows `DeckFlow.Web/e2e/cut-lab-workflow-ux.spec.ts:145` already calls `document.createElement('div')` inside G-5. Replacing G-6 cannot satisfy this criterion. Mutation proving the mismatch: perform exactly the prescribed G-6 replacement; `grep -c 'createElement' ...` remains `1`. Narrow criterion to synthetic G-6 markup (for example, absence of `Workflow control beneath pinned proposal` and `style.position = 'fixed'`), or explicitly authorize and design removal of G-5’s separate injected scroll-depth element. Until corrected, plan cannot achieve its stated goal under its own scope.

## Files the run may have modified -- verify each before trusting the tree

- `/home/clunt/.claude/codex/runs/20260822-185912-2790187/findings.md`
- `DeckFlow.Web/e2e/cut-lab-workflow-ux.spec.ts`
- `tools.apply`

## Artifacts this run left behind

- `brief.txt` (1832 bytes)
- `detached.out` (379 bytes)
- `dispatch-env.txt` (107 bytes)
- `findings.md` (996 bytes)
- `job.json` (315 bytes)
- `pid` (8 bytes)
- `projection.json` (167 bytes)
- `prompt.txt` (4142 bytes)
- `read-ledger.tsv` (11867 bytes)
- `scope-before` (0 bytes)
- `scope-before-fingerprints` (2 bytes)
- `scope-head-before` (41 bytes)
- `scope-root` (59 bytes)
- `stdin.txt` (1832 bytes)
- `watchdog-state.json` (383 bytes)

## Next session

- Read `/home/clunt/.claude/codex/runs/20260822-185912-2790187/checkpoint.md` for the full stop state.
- Confirmed findings, if any, are in `/home/clunt/.claude/codex/runs/20260822-185912-2790187/findings.md`.
- Do NOT --resume: the session is at or over its cap. Dispatch fresh AGAINST these files.

===== END SYNTHESIZED REMAINING WORK =====

===== CODEX-RUN WATCHDOG CHECKPOINT =====
Bound tripped: turn-cap
Observed: 41 (threshold: 40)
Rollout: /home/clunt/.codex/sessions/2026/08/22/rollout-2026-08-22T18-59-15-01a02c20-fe71-77a2-827e-4418ae8717fc.jsonl
Checkpoint: /home/clunt/.claude/codex/runs/20260822-185912-2790187/checkpoint.md
Write the evidence gathered so far to a file and start a FRESH dispatch against that file -- do not --resume, because the session is at or over the cap.
Re-dispatch: /home/clunt/.claude/scripts/codex-run.sh --model gpt-5.6-sol --effort medium --sandbox danger-full-access --timeout 1500 --cwd /mnt/c/users/chrislunt/source/personal/deckflow-cycle21-fix < /home/clunt/.claude/codex/runs/20260822-185912-2790187/checkpoint.md
===== END CODEX-RUN WATCHDOG CHECKPOINT =====

---

## Review of 07.1-07-PLAN.md


===== CODEX-RUN SALVAGED INCREMENTAL FINDINGS =====
Source: /home/clunt/.claude/codex/runs/20260822-185912-2790235/findings.md
# Plan Review Findings

## HIGH — Task ordering creates a false completion attestation

Task 2 marks acceptance criterion 12 satisfied by Task 3 and moves the capture into `todos/completed/` before Task 3 creates the required spillover todo (`.planning/workstreams/cycle21-cut-lab/phases/07.1-cutlab-review-findings-closeout/07.1-07-PLAN.md:176-181`). Task 3 then depends on the completed capture produced by Task 2 (`.planning/workstreams/cycle21-cut-lab/phases/07.1-cutlab-review-findings-closeout/07.1-07-PLAN.md:204-211`). This circular ordering means Task 2 records evidence that does not yet exist, violating the phase's evidence-first closeout goal and allowing interruption between tasks to leave a false completed artifact. Create the spillover todo before closing criterion 12, or split Task 2 so the move and resolution append happen after Task 3.

## HIGH — Task 2's automated check passes while review gates remain open

The verifier runs `grep -c 'OPEN' ...; ls ...`, so the semicolon discards `grep`'s failure status and successful `ls` determines the command result (`.planning/workstreams/cycle21-cut-lab/phases/07.1-cutlab-review-findings-closeout/07.1-07-PLAN.md:186-188`). Mutation: leave one `· OPEN` marker in either gate while ensuring the completed directory exists; this verifier still exits 0. That directly fails the gate-closure behavior the task claims to certify at lines 190-201. Replace it with explicit zero-count assertions joined by `&&`, then verify all required closure evidence rather than directory existence alone.

## HIGH — Task 1's automated check certifies neither a successful build nor the regression sweep

The verifier pipes `dotnet build` into `tail -5` without enabling `pipefail`, so a failed build can yield a successful pipeline; it then checks only diff-stat equality (`.planning/workstreams/cycle21-cut-lab/phases/07.1-cutlab-review-findings-closeout/07.1-07-PLAN.md:116-118`). It never runs or validates Web xUnit, Vitest, TypeScript, either Playwright project, generated-JS cleanliness, changed-line formatting, or `07.1-REGRESSION.md`, despite those being required at lines 119-130. Mutations: introduce a compile error before the final five log lines, break either touched Playwright spec, or omit the regression report; the automated check can still exit 0 when diff stats match. Capture commands to logs, test each producer's exit status directly, and add machine checks for every acceptance criterion.

## MEDIUM — Task 3's automated check is vacuous for most required todo content

The verifier checks only that the todo exists and contains a shortened reference to the pre-existing todo (`.planning/workstreams/cycle21-cut-lab/phases/07.1-cutlab-review-findings-closeout/07.1-07-PLAN.md:245-247`). It does not observe required front matter, four consumer names, full collision path and preferred ordering, `setVisibleStepPanel`, re-verified line citations, or LF endings required at lines 248-256. Mutation: delete all content except the shortened `2026-08-02-split-workflow-step-tabs` string; the verifier still exits 0. Add assertions for every mechanically observable acceptance criterion and a targeted review check for line-number correctness and ordering rationale.

## HIGH — Raw regression capture can commit an overridden admin password

Task 1 says to capture raw output from its commands into a tracked planning artifact and starts the app through `scripts/run-web-test.sh` (`.planning/workstreams/cycle21-cut-lab/phases/07.1-cutlab-review-findings-closeout/07.1-07-PLAN.md:84-85`, `.planning/workstreams/cycle21-cut-lab/phases/07.1-cutlab-review-findings-closeout/07.1-07-PLAN.md:99-103`). That launcher explicitly permits `FEEDBACK_ADMIN_USER` and `FEEDBACK_ADMIN_PASSWORD` overrides (`scripts/run-web-test.sh:12-14`) and prints their resolved values (`scripts/run-web-test.sh:53-55`). An executor with non-placeholder values in the environment can therefore paste credentials into `07.1-REGRESSION.md`. Capture build/test output separately from server output, suppress the launcher output, or redact the `Admin login:` line before writing any tracked artifact.

## MEDIUM — Gate commit acceptance check can pass on an unrelated historical commit

Task 2 uses `git log --oneline -1 -- .planning/reviews/` to prove the gate edit is committed (`.planning/workstreams/cycle21-cut-lab/phases/07.1-cutlab-review-findings-closeout/07.1-07-PLAN.md:198`). That command prints the latest historical commit touching the directory even when this task's gate edits remain uncommitted. Mutation: edit both gate files but do not commit them; the check still returns output. Verify a clean diff for the exact gate paths after the task commit, and verify the new commit contains those paths.

## HIGH — Playwright step cannot execute literally and leaves server lifecycle unspecified

Task 1 says to start `scripts/run-web-test.sh` and then run Playwright, but provides no backgrounding, readiness wait, or cleanup (`.planning/workstreams/cycle21-cut-lab/phases/07.1-cutlab-review-findings-closeout/07.1-07-PLAN.md:99-103`). The launcher ends with foreground `dotnet run` (`scripts/run-web-test.sh:57-58`), so literal execution blocks before either Playwright command. If an executor improvises `&`, the plan still leaves a locked server process behind despite beginning by warning that such a process causes MSB3027 (`.planning/workstreams/cycle21-cut-lab/phases/07.1-cutlab-review-findings-closeout/07.1-07-PLAN.md:81-82`). Specify one bounded command that backgrounds the server, waits for readiness, traps cleanup, runs both projects, and propagates their exit statuses.

===== END SALVAGED INCREMENTAL FINDINGS =====

===== CODEX-RUN SYNTHESIZED REMAINING WORK (inferred by the wrapper, not authored by the model) =====
Source: /home/clunt/.claude/codex/runs/20260822-185912-2790235/remaining.synthesized.md
# Remaining work -- WRAPPER-SYNTHESIZED, NOT WRITTEN BY THE MODEL

This dispatch was terminated (turn-cap, observed 41) before it wrote remaining.md.
Everything below was inferred by codex-run.sh from the watchdog checkpoint. It is
evidence of where the run stopped, NOT the model's own plan for what is left. Verify
each item against the working tree before acting on it.

## Where the run stopped (last assistant message)

## HIGH — Playwright step cannot execute literally and leaves server lifecycle unspecified

Task 1 says start `scripts/run-web-test.sh` then run Playwright, but provides no backgrounding, readiness wait, or cleanup (`.planning/workstreams/cycle21-cut-lab/phases/07.1-cutlab-review-findings-closeout/07.1-07-PLAN.md:99-103`). Launcher ends with foreground `dotnet run` (`scripts/run-web-test.sh:57-58`), so literal execution blocks before either Playwright command. If executor improvises `&`, plan still leaves locked server process behind despite beginning warning that such process causes MSB3027 (`.planning/workstreams/cycle21-cut-lab/phases/07.1-cutlab-review-findings-closeout/07.1-07-PLAN.md:81-82`). Specify one bounded command that backgrounds server, waits for readiness, traps cleanup, runs both projects, and propagates exit statuses.

## Files the run may have modified -- verify each before trusting the tree

- `/home/clunt/.claude/codex/runs/20260822-185912-2790235/findings.md`
- `tools.apply`

## Artifacts this run left behind

- `brief.txt` (1832 bytes)
- `detached.out` (379 bytes)
- `dispatch-env.txt` (107 bytes)
- `findings.md` (5651 bytes)
- `job.json` (315 bytes)
- `pid` (8 bytes)
- `projection.json` (166 bytes)
- `prompt.txt` (4142 bytes)
- `read-ledger.tsv` (10911 bytes)
- `scope-before` (0 bytes)
- `scope-before-fingerprints` (2 bytes)
- `scope-head-before` (41 bytes)
- `scope-root` (59 bytes)
- `stdin.txt` (1832 bytes)
- `watchdog-state.json` (382 bytes)

## Next session

- Read `/home/clunt/.claude/codex/runs/20260822-185912-2790235/checkpoint.md` for the full stop state.
- Confirmed findings, if any, are in `/home/clunt/.claude/codex/runs/20260822-185912-2790235/findings.md`.
- Do NOT --resume: the session is at or over its cap. Dispatch fresh AGAINST these files.

===== END SYNTHESIZED REMAINING WORK =====

===== CODEX-RUN WATCHDOG CHECKPOINT =====
Bound tripped: turn-cap
Observed: 41 (threshold: 40)
Rollout: /home/clunt/.codex/sessions/2026/08/22/rollout-2026-08-22T18-59-15-01a02c20-ffc9-7121-9b5f-1b0968fdd492.jsonl
Checkpoint: /home/clunt/.claude/codex/runs/20260822-185912-2790235/checkpoint.md
Write the evidence gathered so far to a file and start a FRESH dispatch against that file -- do not --resume, because the session is at or over the cap.
Re-dispatch: /home/clunt/.claude/scripts/codex-run.sh --model gpt-5.6-sol --effort medium --sandbox danger-full-access --timeout 1500 --cwd /mnt/c/users/chrislunt/source/personal/deckflow-cycle21-fix < /home/clunt/.claude/codex/runs/20260822-185912-2790235/checkpoint.md
===== END CODEX-RUN WATCHDOG CHECKPOINT =====

---
