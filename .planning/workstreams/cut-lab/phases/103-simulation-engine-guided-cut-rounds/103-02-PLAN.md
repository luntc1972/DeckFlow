---
phase: 103-simulation-engine-guided-cut-rounds
plan: 02
type: execute
wave: 2
depends_on: ["103-01"]
files_modified:
  - DeckFlow.Web/Models/CutLab/CutLabState.cs
  - DeckFlow.Web/Services/CutLab/CutLabStateSerializer.cs
  - DeckFlow.Web.Tests/CutLabStateSerializerTests.cs
autonomous: true
requirements: [CUT-03, SIM-02]
must_haves:
  truths:
    - "D-16: every accepted cut is recorded as a compact, individually-restorable decision record in CutLabState (card + decision kind + round + ordinal), surviving the hidden-field round-trip."
    - "D-12: the original-pool baseline metric snapshot is stored inside CutLabState as a compact numeric snapshot, survives restarts, and is never a full ManabaseReport."
    - "D-15: deferred and rejected decisions persist distinctly so the loop-around pass (Plan 103-04/07) can re-propose them in the right order."
    - "Pitfall 5: adding Decisions + BaselineSnapshot never blows the 262_144-byte MaxUploadBytes cap for a 150-card pool carrying MULTIPLE decision records per card; the Decisions list is bounded on deserialize."
    - "Old (pre-103) CutLabStateJson blobs still deserialize cleanly (empty-default initializers)."
  artifacts:
    - path: "DeckFlow.Web/Models/CutLab/CutLabState.cs"
      provides: "Decisions history + BaselineSnapshot fields"
      contains: "BaselineSnapshot"
    - path: "DeckFlow.Web/Services/CutLab/CutLabStateSerializer.cs"
      provides: "bounded/clamped deserialize for the new fields"
      contains: "MaxDecisions"
  key_links:
    - from: "DeckFlow.Web/Models/CutLab/CutLabState.cs"
      to: "DeckFlow.Web/Models/CutLab/CutLabMetrics.cs"
      via: "BaselineSnapshot typed as CutLabMetricSnapshot (Plan 103-01)"
      pattern: "CutLabMetricSnapshot"
    - from: "DeckFlow.Web/Services/CutLab/CutLabStateSerializer.cs"
      to: "CutLabState.Decisions"
      via: "Take(MaxDecisions) clamp on Deserialize, mirroring the existing Take(MaxPackages)"
      pattern: "Take\\(Max"
---

<objective>
Extend the persisted Cut Lab working-session envelope to carry the decision history (D-16 restore-any) and the original-pool baseline metric snapshot (D-12 SIM-02), with the same defensive bounding the existing serializer already applies to packages and floors — without breaching the 256KB cap even when cards accrue multiple decision records across defer→reject→accept loop-around passes.

Purpose: The decision loop (Plan 103-07) and the before/after view (Plan 103-08) both read these fields; they must round-trip through the hidden `CutLabStateJson` field for the no-JS fallback and survive restarts.
Output: Extended `CutLabState`, hardened serializer, round-trip + cap tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/cut-lab/phases/103-simulation-engine-guided-cut-rounds/103-PATTERNS.md
@.planning/workstreams/cut-lab/phases/103-simulation-engine-guided-cut-rounds/103-RESEARCH.md

<interfaces>
From DeckFlow.Web/Models/CutLab/CutLabMetrics.cs (Plan 103-01):
- record CutLabMetricSnapshot { IReadOnlyList<CutLabMetricValue> Metrics { get; init; } = []; }

Existing serializer invariants (DeckFlow.Web/Services/CutLab/CutLabStateSerializer.cs):
- const int MaxUploadBytes = 262_144;  const int MaxPackages = 50;
- Deserialize already: size-guards, deserializes, Take(MaxPackages), then CutLabFloorRules.ClampFloors(CutLabLockRules.EnforceCommanderLock(state)).
- Serialize throws InvalidOperationException("The Cut Lab working session is too large to save.") above the cap.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Decisions + BaselineSnapshot to CutLabState</name>
  <read_first>
    - DeckFlow.Web/Models/CutLab/CutLabState.cs (the record + the RoleFloors extension precedent, XML doc "recomputed per POST, never persisted" framing)
    - DeckFlow.Web/Models/CutLab/CutLabMetrics.cs (CutLabMetricSnapshot shape from Plan 103-01)
    - .planning/workstreams/cut-lab/phases/103-simulation-engine-guided-cut-rounds/103-CONTEXT.md (D-12, D-15, D-16 wording)
    - .planning/workstreams/cut-lab/phases/103-simulation-engine-guided-cut-rounds/103-RESEARCH.md (Pitfall 5 sizing guard: ~40 bytes/decision × 150 ≈ 6KB)
  </read_first>
  <action>
    In `DeckFlow.Web/Models/CutLab/CutLabState.cs`:
    (a) Add a `CutLabDecisionKind` enum (`Accepted`, `Rejected`, `Deferred`) — the three states from D-02/D-15.
    (b) Add a `CutLabDecision` sealed record with COMPACT fields only (Pitfall 5): `string CardName`, `CutLabDecisionKind Kind`, `string Round` (round key/name the card was decided in, for the "cut in {round name}" copy and D-15 loop ordering), `int Ordinal` (monotonic acceptance/decision order for D-16 order-independent restore + most-recent-first display). Do NOT store card data, delta snapshots, or `ManabaseReport` anywhere in this record. Note: a single card may carry MULTIPLE `CutLabDecision` records over loop-around passes (defer, then reject, then accept) — the list is a decision LOG keyed by (CardName, Ordinal), not a per-card map.
    (c) Add to `CutLabState`: `public IReadOnlyList<CutLabDecision> Decisions { get; init; } = [];` and `public CutLabMetricSnapshot? BaselineSnapshot { get; init; }` — both with empty/null defaults so pre-103 JSON deserializes cleanly. XML-doc each field, mirroring the RoleFloors doc's "derived defaults are recomputed per POST, never persisted" style: note that `BaselineSnapshot` is computed once at pool intake (D-12) and `Decisions` is bounded on deserialize.
  </action>
  <verify>
    <automated>"/mnt/c/Program Files/dotnet/dotnet.exe" build DeckFlow.Web/DeckFlow.Web.csproj -clp:ErrorsOnly</automated>
  </verify>
  <acceptance_criteria>
    - `CutLabState` has `Decisions` (empty-default) and `BaselineSnapshot` (nullable) properties.
    - `CutLabDecision` contains only `CardName`, `Kind`, `Round`, `Ordinal` — grep confirms no `ManabaseReport`/`CutLabMetricDelta`/card-data field on the record.
    - `CutLabDecisionKind` has `Accepted`, `Rejected`, `Deferred`.
    - Build clean, no new warnings.
  </acceptance_criteria>
  <done>State envelope carries decision history + baseline snapshot with compact, restart-safe shapes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Bound + round-trip the new fields in the serializer</name>
  <read_first>
    - DeckFlow.Web/Services/CutLab/CutLabStateSerializer.cs (MaxUploadBytes, MaxPackages, the Deserialize Take/clamp chain, Serialize cap-throw)
    - DeckFlow.Web.Tests/CutLabStateSerializerTests.cs (existing round-trip/clamp test patterns to extend)
    - DeckFlow.Web/Models/CutLab/CutLabState.cs (the fields added in Task 1)
  </read_first>
  <behavior>
    - Test 1: Serialize→Deserialize a state with Accepted/Rejected/Deferred decisions + a BaselineSnapshot; the decisions and snapshot survive byte-for-byte in value.
    - Test 2: A deserialized state with more than `MaxDecisions` decisions is truncated to `MaxDecisions` (mirrors the existing over-`MaxPackages` truncation test).
    - Test 3: A pre-103 JSON blob (no `decisions`/`baselineSnapshot` keys) deserializes to a state with an empty Decisions list and null BaselineSnapshot (backward compat).
    - Test 4 (worst-case cap guard, Pitfall 5): construct the realistic WORST case — a 150-card pool where at least 50 cards each carry THREE decision records (a defer→reject→accept loop-around history), and the remaining ~100 cards carry one Accepted record each (≈250 decision records total) — plus a full 7-family BaselineSnapshot. Assert `Serialize` does NOT throw and the serialized byte length is `< MaxUploadBytes` (262_144). This proves multi-record-per-card accrual, not just one-record-per-card, stays under the cap.
  </behavior>
  <action>
    In `CutLabStateSerializer.cs`: add `private const int MaxDecisions = 500;` (`// Why:` a 150-card pool can accrue MULTIPLE records per card over defer→reject→accept loop-around passes — up to ~3 per cut card, so ~450 worst-case records; 500 keeps legitimate history intact while staying comfortably under the byte cap per Pitfall 5's ~40 bytes/record estimate (~500 × 40 ≈ 20KB ≪ 256KB)). Extend `Deserialize`'s `state = state with { ... }` block to also clamp `Decisions = state.Decisions.Where(d => !string.IsNullOrWhiteSpace(d.CardName)).Take(MaxDecisions).ToArray()`. Leave the existing `MaxUploadBytes` size-guard and the `ClampFloors(EnforceCommanderLock(...))` tail intact (BaselineSnapshot needs no clamp — it is server-produced numeric data, but it still passes through the size cap). Extend `CutLabStateSerializerTests.cs` with the four behaviors above (do not rewrite existing tests).
  </action>
  <verify>
    <automated>"/mnt/c/Program Files/dotnet/dotnet.exe" test DeckFlow.Web.Tests --filter "FullyQualifiedName~CutLabStateSerializer"</automated>
  </verify>
  <acceptance_criteria>
    - All four new behaviors pass plus all pre-existing serializer tests stay green.
    - `MaxDecisions` clamp present (grep `Take(MaxDecisions)`).
    - Test 4 worst-case (≥50 cards × 3 records each + ~100 single-record cards + full snapshot ≈ 250 records) serializes under `MaxUploadBytes` and does not throw (proves the cap holds for multi-record-per-card history).
    - Pre-103 blob deserializes to empty Decisions + null BaselineSnapshot.
  </acceptance_criteria>
  <done>New state fields round-trip, are bounded against the 256KB cap even with multiple decision records per card, and stay backward-compatible.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| hidden field → server | `CutLabStateJson` is attacker-controllable (client hidden input); Deserialize must never trust its size or contents. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-103-02-01 | Tampering | CutLabStateJson deserialize (oversized/over-long Decisions) | mitigate | Existing `MaxUploadBytes` byte-cap guard runs first; new `Take(MaxDecisions)` bounds the list; malformed JSON returns `new CutLabState()` (existing catch). |
| T-103-02-02 | Denial of Service | Serialize of an unbounded working session | mitigate | Serialize throws `InvalidOperationException` above `MaxUploadBytes`; `MaxDecisions` keeps the realistic worst case (multi-record-per-card) under cap (test-proven). |
| T-103-02-03 | Tampering | Forged BaselineSnapshot in a submitted blob | accept | Baseline is display-only reference data; a forged snapshot only mis-renders the user's own before/after view (no privilege/data crossing). Server recomputes-at-intake authoritatively in Plan 103-06. |
</threat_model>

<verification>
- `"/mnt/c/Program Files/dotnet/dotnet.exe" test DeckFlow.Web.Tests --filter "FullyQualifiedName~CutLab"` green.
- Backward-compat + worst-case cap + truncation behaviors proven by test.
</verification>

<success_criteria>
CutLabState round-trips Decisions + BaselineSnapshot, bounded under 256KB even with multiple decision records per card, backward-compatible with pre-103 blobs.
</success_criteria>

<output>
Create `.planning/workstreams/cut-lab/phases/103-simulation-engine-guided-cut-rounds/103-02-SUMMARY.md` when done.
</output>
