---
phase: 07.1-cutlab-review-findings-closeout
plan: 07
type: execute
wave: 7
depends_on: ["07.1-01", "07.1-02", "07.1-03", "07.1-04", "07.1-05", "07.1-06"]
files_modified:
  - .planning/reviews/2026-08-22-cycle21-0706-code-gate.md
  - .planning/reviews/2026-08-16-cycle21-cutlab-code-gate.md
  - .planning/workstreams/cycle21-cut-lab/todos/pending/2026-08-22-close-six-open-cut-lab-code-review-findings.md
  - .planning/workstreams/cycle21-cut-lab/todos/pending/2026-08-22-hide-non-current-tabpanels-on-sibling-workflow-tools.md
  - .planning/workstreams/cycle21-cut-lab/phases/07.1-cutlab-review-findings-closeout/07.1-REGRESSION.md
autonomous: true
requirements: [BUILD-EOL, R2-1, R2-2, F-5, F-5-SIDE-EFFECT, F-6, F-7, DENSITY]

estimate:
  tokens: 50000
  raw_tokens: 50000
  tasks: 4
  confidence: low

must_haves:
  truths:
    - "dotnet build reports 0 errors and no more than the 15-warning baseline; every warning in the output is one of the 9 pre-existing CS8629 or an NU1903 advisory."
    - "Web xUnit reports at least 2302 passed and 0 failed, with a nonzero executed-test count and no error MSB in the output."
    - "Vitest run from DeckFlow.Web reports 33 files and 0 failed."
    - "The Playwright specs this phase touched pass headless on both configured chromium projects, with no browser opened on the Windows host."
    - "git diff --stat and git diff --ignore-all-space --stat agree across every file the phase touched, so there is zero unintended line-ending churn."
    - "Both review gates record every one of the eight items as closed, with a pointer to the plan and mutation evidence that closed it."
    - "The sibling-tool F-5 spillover exists as its own pending todo, cross-referenced to the existing partial-refactor todo it must not collide with."
  artifacts:
    - ".planning/.../07.1-REGRESSION.md — the full regression sweep with raw command output and the before/after baseline comparison."
    - ".planning/reviews/2026-08-22-cycle21-0706-code-gate.md — R2-1 and R2-2 marked closed in both the finding headings and the Open work table."
    - ".planning/reviews/2026-08-16-cycle21-cutlab-code-gate.md — F-5, F-6, F-7 and the vacuous-tests section marked closed."
    - ".planning/workstreams/cycle21-cut-lab/todos/pending/2026-08-22-hide-non-current-tabpanels-on-sibling-workflow-tools.md — the spillover follow-up."
  key_links:
    - "The gate files live at .planning/reviews/, NOT at .planning/workstreams/cycle21-cut-lab/reviews/ as the ROADMAP's Phase 07.1 section states. That path in the ROADMAP is wrong and this plan corrects it."
    - "The spillover todo must cross-reference .planning/todos/pending/2026-08-02-split-workflow-step-tabs-into-tablist-and-jump-nav.md, which already owns refactoring the shared partial."
---

<objective>
Close the phase: run the full regression sweep against the recorded baselines, prove there is no
line-ending churn, mark both review gates closed with evidence, and record the deliberately
excluded sibling-tool spillover as its own todo.

Purpose: the branch cannot leave its code gate while any of the eight items stands, and a gate
whose table still says OPEN is not closed no matter what the code does. This plan makes the
paperwork match the code and proves the code against the numbers the phase started from.

Output: `07.1-REGRESSION.md`, two updated gate files, one new todo, and the source capture moved
to completed.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/cycle21-cut-lab/PROJECT.md
@.planning/workstreams/cycle21-cut-lab/ROADMAP.md
@.planning/workstreams/cycle21-cut-lab/STATE.md
@.planning/workstreams/cycle21-cut-lab/phases/07.1-cutlab-review-findings-closeout/07.1-CONTEXT.md
@.planning/workstreams/cycle21-cut-lab/todos/pending/2026-08-22-close-six-open-cut-lab-code-review-findings.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run the full regression sweep and prove zero line-ending churn</name>
  <files>.planning/workstreams/cycle21-cut-lab/phases/07.1-cutlab-review-findings-closeout/07.1-REGRESSION.md</files>
  <read_first>
    - .planning/workstreams/cycle21-cut-lab/phases/07.1-cutlab-review-findings-closeout/07.1-CONTEXT.md (the "Baselines to regress against" section — build 0 errors / 15 warnings, Web xUnit 2302 passed / 0 failed / 16 skipped, vitest 33 files / 131 tests)
    - .planning/workstreams/cycle21-cut-lab/phases/07.1-cutlab-review-findings-closeout/07.1-MUTATION-PROTOCOL.md (the MSB3027 false-kill trap applies to every dotnet invocation in this task)
    - .gitattributes (LF by default; CRLF for .ps1/.psm1/.bat/.cmd)
    - CLAUDE.md (the formatting carve-outs and the changed-lines format gate)
    - scripts/run-web-test.sh
    - The six preceding SUMMARY files in this phase directory (07.1-01 through 07.1-06) — the mutation evidence they recorded is what this sweep sits on top of
  </read_first>
  <action>
    Stop any running `DeckFlow.Web` process before starting: a locked exe produces MSB3027, and
    `dotnet test` then exits 1 having run zero tests, which is indistinguishable from a failure.

    Run each of these, capturing each command's own output to a private log under `/tmp` first (not
    under `.planning/`), then transcribe the relevant result — command, exit status, counts, and the
    tail of the log — into the tracked
    `.planning/workstreams/cycle21-cut-lab/phases/07.1-cutlab-review-findings-closeout/07.1-REGRESSION.md`:

    **Credential handling.** `scripts/run-web-test.sh` prints the resolved
    `Admin login: <user> / <password>` line (`scripts/run-web-test.sh:55`) whenever
    `FEEDBACK_ADMIN_USER` or `FEEDBACK_ADMIN_PASSWORD` is overridden in the environment
    (`scripts/run-web-test.sh:12-14`, `:22-23`). Never redirect that script's stdout/stderr into a
    tracked file. Keep the server's own log (`/tmp/07-1-server.log` below) private, and if any
    server-startup line is quoted into `07.1-REGRESSION.md` — e.g. to prove the run was headless —
    strip the `Admin login:` line first (`grep -v '^Admin login:'`). Skipping this can commit a real
    password to this public repo if a non-placeholder override happens to be set.

    1. `dotnet build` — record the error and warning counts and enumerate every warning code so the
       comparison against the 15-warning baseline is checkable rather than asserted. Any warning
       that is not one of the 9 pre-existing CS8629 or an NU1903 advisory is a NEW warning and
       fails this task.
    2. `dotnet test DeckFlow.Web.Tests` — record passed / failed / skipped. Grep the output for
       `error MSB` and record the result of that grep explicitly, and record the executed-test
       count; a zero count is a false green regardless of the exit code.
    3. `cd DeckFlow.Web && npx vitest run` — run it from `DeckFlow.Web`, never the repo root: the
       root sweep pulls in the Playwright e2e specs and produces false failures. Record files and
       tests. The baseline is 33 files / 131 tests; this phase adds cases, so the test count must
       be HIGHER than 131 and the file count must still be 33.
    4. `cd DeckFlow.Web && npx tsc --noEmit -p tsconfig.json` — record the exit code.
    5. Run the two specs this phase touched on both chromium projects as ONE bounded command that
       owns the server's whole lifecycle — background it, wait for readiness, trap cleanup on exit,
       run both projects, and propagate their exit statuses. Do not start `scripts/run-web-test.sh`
       in the foreground and improvise backgrounding: that script ends in a foreground `dotnet run`
       (`scripts/run-web-test.sh:57-58`), so literal foreground execution blocks before Playwright
       ever starts, and an ad hoc `&` leaves a locked server process behind — the exact MSB3027 trap
       this task opened by warning about. Run exactly this, from the repo root:

       ```
       scripts/run-web-test.sh > /tmp/07-1-server.log 2>&1 &
       SERVER_PID=$!
       trap 'kill "$SERVER_PID" 2>/dev/null; wait "$SERVER_PID" 2>/dev/null' EXIT
       READY=0
       for i in $(seq 1 60); do
         CODE="$(curl --silent --show-error --location --output /dev/null --write-out '%{http_code}' http://localhost:5173/ || true)"
         echo "$CODE" | grep -Eq '^[23][0-9][0-9]$' && { READY=1; break; }
         sleep 1
       done
       [ "$READY" -eq 1 ] || { echo "server did not become ready within 60s" >&2; exit 1; }
       cd DeckFlow.Web
       env -u DISPLAY npx --no-install playwright test e2e/cut-lab-theme-readability.spec.ts e2e/cut-lab-workflow-ux.spec.ts --project=chromium-desktop
       DESKTOP_STATUS=$?
       env -u DISPLAY npx --no-install playwright test e2e/cut-lab-theme-readability.spec.ts e2e/cut-lab-workflow-ux.spec.ts --project=chromium-mobile
       MOBILE_STATUS=$?
       cd ..
       [ "$DESKTOP_STATUS" -eq 0 ] && [ "$MOBILE_STATUS" -eq 0 ]
       ```

       The readiness poll reuses the same 200/300-series-status check `scripts/run-web-test.sh`
       itself uses to detect an already-running server (`scripts/run-web-test.sh:28`), pinned here to
       60 one-second attempts. The `trap` fires on this script's own exit — success or failure — and
       kills the backgrounded server, so no locked exe survives this task regardless of outcome. If
       either project's exit status is nonzero, this step and the task fail; do not hand-report a
       pass. A browser must never open on the Windows host. Record pass/fail and wall time for each
       project in `07.1-REGRESSION.md`, and record explicitly that the run was headless — with the
       `Admin login:` line redacted from anything quoted out of `/tmp/07-1-server.log`.
    6. `git diff --stat` and `git diff --ignore-all-space --stat` — paste BOTH outputs. They must
       list the same files with the same per-file insertion and deletion counts. Any file where
       they differ has line-ending churn: for that file, compare against `git show HEAD:<path>` and
       re-normalise before continuing.
    7. `git status --short DeckFlow.Web/wwwroot/js/` — must be empty. Compiled JS is gitignored and
       recompiled at deploy; a staged `.js` only creates drift.
    8. `scripts/format-check-changed.sh staged` for the changed C# lines.

    Then write a comparison table in `07.1-REGRESSION.md`: metric, baseline, measured, delta,
    pass/fail. A metric that moved in the wrong direction fails this task and must be reported,
    not folded away.
  </action>
  <verify>
    <automated>set -o pipefail; dotnet build 2>&amp;1 | tee /tmp/07-1-build.log | tail -5; B=${PIPESTATUS[0]}; dotnet test DeckFlow.Web.Tests 2>&amp;1 | tee /tmp/07-1-xunit.log | tail -5; T=${PIPESTATUS[0]}; (cd DeckFlow.Web &amp;&amp; npx vitest run) 2>&amp;1 | tee /tmp/07-1-vitest.log | tail -5; V=${PIPESTATUS[0]}; (cd DeckFlow.Web &amp;&amp; npx tsc --noEmit -p tsconfig.json) 2>&amp;1 | tee /tmp/07-1-tsc.log; C=${PIPESTATUS[0]}; git diff --stat &gt; /tmp/07-1-a.txt; git diff --ignore-all-space --stat &gt; /tmp/07-1-b.txt; diff /tmp/07-1-a.txt /tmp/07-1-b.txt; E=$?; git status --short DeckFlow.Web/wwwroot/js/ &gt; /tmp/07-1-js.txt; scripts/format-check-changed.sh staged; F=$?; REG=.planning/workstreams/cycle21-cut-lab/phases/07.1-cutlab-review-findings-closeout/07.1-REGRESSION.md; [ "$B" -eq 0 ] &amp;&amp; [ "$T" -eq 0 ] &amp;&amp; [ "$V" -eq 0 ] &amp;&amp; [ "$C" -eq 0 ] &amp;&amp; [ "$E" -eq 0 ] &amp;&amp; [ ! -s /tmp/07-1-js.txt ] &amp;&amp; [ "$F" -eq 0 ] &amp;&amp; [ "$(grep -c 'error MSB' /tmp/07-1-xunit.log)" -eq 0 ] &amp;&amp; grep -qE '33 (test )?files' /tmp/07-1-vitest.log &amp;&amp; test -f "$REG" &amp;&amp; grep -q 'chromium-desktop' "$REG" &amp;&amp; grep -q 'chromium-mobile' "$REG" &amp;&amp; ! grep -q '^Admin login:' "$REG" &amp;&amp; echo REGRESSION-SWEEP-GREEN</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet build` reports `0 Error(s)` and at most `15 Warning(s)`, and `07.1-REGRESSION.md` enumerates every warning code present. This is tested directly against `dotnet build`'s own exit status, captured to a log — never inferred from a downstream `tail`/`diff` pipeline stage.
    - `dotnet test DeckFlow.Web.Tests` reports 0 failed and at least 2302 passed, with a nonzero executed-test count, and `grep -c 'error MSB'` over its captured log returns `0`.
    - `cd DeckFlow.Web && npx vitest run` reports `33` files, 0 failed, and a test count strictly greater than `131`.
    - `cd DeckFlow.Web && npx tsc --noEmit -p tsconfig.json` exits 0.
    - The Playwright step runs as one bounded command that backgrounds the server, waits for readiness via the pinned curl poll, traps cleanup on exit, runs both `chromium-desktop` and `chromium-mobile` for both touched specs, and propagates both exit statuses; no server process is left running after the task completes. Both projects report 0 failed, and `07.1-REGRESSION.md` records that the run was headless.
    - `07.1-REGRESSION.md` contains no line beginning `Admin login:` — any server-startup evidence quoted into it had that line redacted first, so an overridden `FEEDBACK_ADMIN_PASSWORD` can never reach the tracked artifact.
    - `diff <(git diff --stat) <(git diff --ignore-all-space --stat)` produces no output.
    - `git status --short DeckFlow.Web/wwwroot/js/` produces no output.
    - `scripts/format-check-changed.sh staged` exits 0.
    - `07.1-REGRESSION.md` contains the comparison table with a row per metric and an explicit pass/fail per row.
  </acceptance_criteria>
  <done>Every baseline the phase declared is re-measured with raw output on record, no metric moved backwards, the two diff commands agree file-for-file and line-for-line, the server lifecycle left no locked process behind, and no credential value reached the tracked regression artifact.</done>
</task>

<task type="auto">
  <name>Task 2: Close both review gates against evidence</name>
  <files>.planning/reviews/2026-08-22-cycle21-0706-code-gate.md, .planning/reviews/2026-08-16-cycle21-cutlab-code-gate.md, .planning/workstreams/cycle21-cut-lab/ROADMAP.md</files>
  <read_first>
    - .planning/reviews/2026-08-22-cycle21-0706-code-gate.md (the finding headings for R2-1 at :31 and R2-2 at :121, the "Known vacuous tests" section at :134, and the "Open work leaving this gate" table at :141-150)
    - .planning/reviews/2026-08-16-cycle21-cutlab-code-gate.md (the finding headings F-5 at :84, F-6 at :92, F-7 at :106, and the "## Vacuous tests" section at :113)
    - The six SUMMARY files 07.1-01 through 07.1-06 in this phase directory (the mutation evidence each records)
    - .planning/workstreams/cycle21-cut-lab/phases/07.1-cutlab-review-findings-closeout/07.1-REGRESSION.md (as written in Task 1)
    - .planning/workstreams/cycle21-cut-lab/ROADMAP.md (the Phase 07.1 section around :509 — its "Gates:" line points at the wrong directory)
  </read_first>
  <action>
    **Path correction first.** The ROADMAP's Phase 07.1 section states the gate files are under
    `.planning/workstreams/cycle21-cut-lab/reviews/`. They are not — both live at
    `.planning/reviews/`. Verify this yourself with `ls .planning/reviews/` before editing anything,
    and fix the ROADMAP line to the real path as part of this task. Do not create a second copy of
    either gate file.

    In `.planning/reviews/2026-08-22-cycle21-0706-code-gate.md`:
    - Change the R2-1 heading's trailing status marker from OPEN to FIXED and append a one-line
      resolution note naming plan `07.1-01`, the guard's location in `activateStepTab`, and
      mutations M1/M2 with their observed results.
    - Change the R2-2 heading's marker from OPEN to FIXED with a note naming plan `07.1-06`, the
      new target selector, and mutations M15/M16/M17.
    - Update the "Known vacuous tests" section to record the density tests as repaired by plan
      `07.1-04` with mutations M10/M11/M12.
    - Rewrite the "Open work leaving this gate" table: every one of its six rows gets a resolution
      column carrying the plan id and the mutation ids that closed it. Do not delete the rows —
      the table is the record of what the gate found, and an empty table erases that. Add one line
      above the table stating the gate is CLOSED as of this phase, with the date.

    In `.planning/reviews/2026-08-16-cycle21-cutlab-code-gate.md`:
    - Change the F-5, F-6 and F-7 heading markers from OPEN to FIXED, each with a one-line note
      naming the closing plan (`07.1-02`, `07.1-05`, `07.1-03` respectively) and its mutation ids
      (M4/M5/M6/M7/M18, M13/M14, M8/M9/M19/M20).
    - Update the `## Vacuous tests` section to record the repair by plan `07.1-04` and mutations M10/M11/M12,
      keeping the original description of what was vacuous.
    - F-6's note must additionally record the census: 24 themes measured, three failing, and the
      before/after ratio for each raised theme, with a pointer to `07.1-THEME-CONTRAST-CENSUS.md`.
    - Add a closing line stating the gate is CLOSED as of this phase, with the date.

    Do not re-open or re-litigate F-1, F-2, F-3 or F-4 — all four are FIXED and R2-1 was the
    remainder of F-2/F-3, not a re-opening of them. Say that explicitly in R2-1's resolution note.

    Commit this task's edits to the two gate files and the ROADMAP correction as this task's own
    commit before moving on — Task 4 checks that commit directly.

    Every file edited here is markdown under `.planning/` — write LF endings, matching the rest of
    the tree.

    **Do not move the source capture or append its Resolution section here.** The capture's
    criterion 12 (spillover follow-up) is not satisfiable until Task 3 creates the spillover todo;
    attesting to it now would record evidence that does not yet exist. That move and the
    per-criterion resolution are Task 4, which runs after Task 3.
  </action>
  <verify>
    <automated>G1=.planning/reviews/2026-08-22-cycle21-0706-code-gate.md; G2=.planning/reviews/2026-08-16-cycle21-cutlab-code-gate.md; [ "$(grep -c '· OPEN' "$G1")" -eq 0 ] &amp;&amp; [ "$(grep -c '· OPEN' "$G2")" -eq 0 ] &amp;&amp; grep -q 'CLOSED' "$G1" &amp;&amp; grep -q 'CLOSED' "$G2" &amp;&amp; [ "$(grep -c '07.1-0' "$G1")" -ge 3 ] &amp;&amp; [ "$(grep -c '07.1-0' "$G2")" -ge 4 ] &amp;&amp; [ "$(grep -c 'workstreams/cycle21-cut-lab/reviews' .planning/workstreams/cycle21-cut-lab/ROADMAP.md)" -eq 0 ] &amp;&amp; echo GATES-CLOSED</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '· OPEN' .planning/reviews/2026-08-22-cycle21-0706-code-gate.md` returns `0`.
    - `grep -c '· OPEN' .planning/reviews/2026-08-16-cycle21-cutlab-code-gate.md` returns `0`.
    - Each gate file contains the literal `CLOSED` on a line naming phase `07.1` and a date.
    - The "Open work leaving this gate" table in the 2026-08-22 gate still has its six original rows and each now carries a resolution naming a plan id and at least one mutation id.
    - `grep -c '07.1-0' .planning/reviews/2026-08-22-cycle21-0706-code-gate.md` returns at least `3` and the same grep on the 2026-08-16 gate returns at least `4` — every closure cites the plan that made it.
    - `grep -c 'workstreams/cycle21-cut-lab/reviews' .planning/workstreams/cycle21-cut-lab/ROADMAP.md` returns `0` — the wrong gate path is corrected.
    - `git diff --quiet -- .planning/reviews/2026-08-22-cycle21-0706-code-gate.md .planning/reviews/2026-08-16-cycle21-cutlab-code-gate.md` exits 0 — no uncommitted edits remain in either gate file, unlike `git log --oneline -1 -- .planning/reviews/`, which prints the latest historical commit touching that directory whether or not this task's own edits are committed.
    - `git diff --name-only HEAD^ HEAD | grep -qx '.planning/reviews/2026-08-22-cycle21-0706-code-gate.md'` and the same check for `.planning/reviews/2026-08-16-cycle21-cutlab-code-gate.md` both succeed — this task's own commit (HEAD) actually contains both gate paths, not merely some commit that happens to be the latest one touching the directory.
  </acceptance_criteria>
  <reversibility rating="reversible">Documentation edits; fully revertable.</reversibility>
  <done>Neither gate has an open finding, every closure cites the plan and the mutation that made it, the ROADMAP points at the real gate directory, and this task's own commit is verified to contain both gate files.</done>
</task>

<task type="auto">
  <name>Task 3: Record the deliberately excluded sibling-tool spillover as its own todo</name>
  <files>.planning/workstreams/cycle21-cut-lab/todos/pending/2026-08-22-hide-non-current-tabpanels-on-sibling-workflow-tools.md</files>
  <read_first>
    - .planning/todos/pending/2026-08-02-split-workflow-step-tabs-into-tablist-and-jump-nav.md (the existing todo that already owns refactoring the shared partial — the new todo must not contradict or pre-empt it)
    - .planning/workstreams/cycle21-cut-lab/todos/pending/2026-08-22-close-six-open-cut-lab-code-review-findings.md (its "Non-goals" section names the exact file:line list — still in `todos/pending/` at this point in the plan, since Task 4 is what moves it to `todos/completed/` after this task runs)
    - .planning/workstreams/cycle21-cut-lab/phases/07.1-cutlab-review-findings-closeout/07.1-02-SUMMARY.md (what the Cut Lab fix actually did — the sibling fix should follow the same shape)
    - DeckFlow.Web/Views/Deck/CedhMetaGap.cshtml, DeckComparison.cshtml, DeckAnalysis.cshtml, DeckPrimer.cshtml (verify the cited line numbers still point at `role="tabpanel"` sections before writing them down)
  </read_first>
  <action>
    Create
    `.planning/workstreams/cycle21-cut-lab/todos/pending/2026-08-22-hide-non-current-tabpanels-on-sibling-workflow-tools.md`
    using the same YAML front matter shape as the capture this phase was built from — `created`,
    `title`, `area`, `severity`, `files`.

    Content:
    - **Problem.** The four sibling workflow tools render their `role="tabpanel"` sections without
      `hidden`, exactly as Cut Lab did before F-5. Re-verify and cite the current line numbers
      rather than copying the stale ones: `CedhMetaGap.cshtml` (reported at `:99, 208, 389`),
      `DeckComparison.cshtml` (`:228, 356, 578`), `DeckAnalysis.cshtml` (`:150, 200, 520, 886, 966`)
      and `DeckPrimer.cshtml` (`:120`, which shares `_WorkflowStepTabs.cshtml`). If a cited line no
      longer points at a tabpanel, record the corrected line and say the original was stale.
    - **The shape of the fix.** Point at `07.1-02-SUMMARY.md` and the `setVisibleStepPanel`
      primitive: one writer of the `hidden` attribute, called from initialization without the
      collapse side effect, with the no-selection fallback preserved. Note that each sibling tool
      has its own initialization script, so the promotion has to be repeated per tool or the
      primitive lifted into a shared module — and that lifting it is the shared-file change the
      Cut Lab work deliberately avoided.
    - **Collision warning.** Cross-reference
      `.planning/todos/pending/2026-08-02-split-workflow-step-tabs-into-tablist-and-jump-nav.md`
      by full path, stating that it already owns splitting `_WorkflowStepTabs.cshtml`'s two
      incompatible interaction patterns and consolidating the keyboard contract implemented four
      times in TypeScript, and that whichever runs first must not contradict the other. Say which
      ordering is preferred and why.
    - **Also carried forward.** R2-1's fix is Cut Lab-local too: the same native-`disabled` roving
      filter defect may exist in the other tools' keyboard handlers. Name it as a second item to
      check, with the grep that finds it, rather than asserting it is present.
    - **Explicitly not in this todo.** Re-theming, the cut engine, and the Cut Lab production flag.

    Write LF endings.
  </action>
  <verify>
    <automated>F=.planning/workstreams/cycle21-cut-lab/todos/pending/2026-08-22-hide-non-current-tabpanels-on-sibling-workflow-tools.md; test -f "$F" &amp;&amp; grep -q '^created:' "$F" &amp;&amp; grep -q '^title:' "$F" &amp;&amp; grep -q '^area:' "$F" &amp;&amp; grep -q '^severity:' "$F" &amp;&amp; grep -q '^files:' "$F" &amp;&amp; grep -q 'CedhMetaGap.cshtml' "$F" &amp;&amp; grep -q 'DeckComparison.cshtml' "$F" &amp;&amp; grep -q 'DeckAnalysis.cshtml' "$F" &amp;&amp; grep -q 'DeckPrimer.cshtml' "$F" &amp;&amp; grep -q '.planning/todos/pending/2026-08-02-split-workflow-step-tabs-into-tablist-and-jump-nav.md' "$F" &amp;&amp; grep -q 'setVisibleStepPanel' "$F" &amp;&amp; ! file "$F" | grep -q CRLF &amp;&amp; echo SPILLOVER-TODO-CONTENT-CHECKED</automated>
  </verify>
  <acceptance_criteria>
    - The todo file exists at the stated path with YAML front matter carrying `created`, `title`, `area`, `severity` and `files` — checked mechanically by grepping each field name, not merely by asserting the file exists.
    - `grep -c 'CedhMetaGap.cshtml' <file>`, `grep -c 'DeckComparison.cshtml' <file>`, `grep -c 'DeckAnalysis.cshtml' <file>` and `grep -c 'DeckPrimer.cshtml' <file>` each return at least `1`.
    - `grep -c '.planning/todos/pending/2026-08-02-split-workflow-step-tabs-into-tablist-and-jump-nav.md' <file>` returns at least `1` — the FULL path, not the shortened `2026-08-02-split-workflow-step-tabs` reference string alone, which a mutation deleting everything else would still satisfy.
    - `grep -c 'setVisibleStepPanel' <file>` returns at least `1` — the fix shape points at the primitive this phase promoted.
    - `file <file>` reports no CRLF terminators.
    - The following are NOT mechanically checkable and need a targeted review pass instead of a grep: that every cited `file:line` was actually re-verified against the working tree (with both the old and corrected number recorded where a line moved), and that the collision text states a preferred ordering with a stated reason rather than merely naming the other todo.
  </acceptance_criteria>
  <done>The spillover is captured with verified line numbers, a named fix shape, and an explicit collision warning against the todo that already owns the shared partial.</done>
</task>

<task type="auto">
  <name>Task 4: Move the source capture to completed and record its per-criterion resolution</name>
  <files>.planning/workstreams/cycle21-cut-lab/todos/pending/2026-08-22-close-six-open-cut-lab-code-review-findings.md (git mv to todos/completed/)</files>
  <read_first>
    - .planning/workstreams/cycle21-cut-lab/todos/pending/2026-08-22-close-six-open-cut-lab-code-review-findings.md (all 13 acceptance criteria)
    - .planning/workstreams/cycle21-cut-lab/phases/07.1-cutlab-review-findings-closeout/07.1-REGRESSION.md (as written in Task 1 — evidence for the regression-sweep criteria)
    - .planning/reviews/2026-08-22-cycle21-0706-code-gate.md and .planning/reviews/2026-08-16-cycle21-cutlab-code-gate.md (as closed in Task 2 — evidence for criterion 13)
    - .planning/workstreams/cycle21-cut-lab/todos/pending/2026-08-22-hide-non-current-tabpanels-on-sibling-workflow-tools.md (as created in Task 3 — evidence for criterion 12)
  </read_first>
  <action>
    This task exists only because Task 2 and Task 3 must both have already run: criterion 12 (the
    spillover follow-up) is satisfied by Task 3's todo, and criterion 13 (gates closed) is satisfied
    by Task 2's gate edits. Attesting to either before both existed would record evidence that does
    not yet exist — that is why this move and its resolution note are their own task, ordered last.

    Move the source capture
    `.planning/workstreams/cycle21-cut-lab/todos/pending/2026-08-22-close-six-open-cut-lab-code-review-findings.md`
    to the sibling `todos/completed/` directory with `git mv`, and append a short `## Resolution`
    section to it listing each of its 13 acceptance criteria with the plan and evidence artifact
    that satisfied it. For criterion 12, name the spillover todo created in Task 3 by its full path;
    for criterion 13, name both gate files closed in Task 2.

    Write LF endings, matching the rest of the tree.
  </action>
  <verify>
    <automated>C=.planning/workstreams/cycle21-cut-lab/todos/completed/2026-08-22-close-six-open-cut-lab-code-review-findings.md; P=.planning/workstreams/cycle21-cut-lab/todos/pending/2026-08-22-close-six-open-cut-lab-code-review-findings.md; test -f "$C" &amp;&amp; [ ! -f "$P" ] &amp;&amp; grep -q '## Resolution' "$C" &amp;&amp; [ "$(grep -cE '^[0-9]+\.' "$C")" -ge 13 ] &amp;&amp; test -f .planning/workstreams/cycle21-cut-lab/todos/pending/2026-08-22-hide-non-current-tabpanels-on-sibling-workflow-tools.md &amp;&amp; grep -q '2026-08-22-hide-non-current-tabpanels-on-sibling-workflow-tools.md' "$C" &amp;&amp; echo SOURCE-CAPTURE-RESOLVED</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/workstreams/cycle21-cut-lab/todos/completed/2026-08-22-close-six-open-cut-lab-code-review-findings.md` exists and the pending copy does not.
    - That moved file contains a `## Resolution` section with 13 numbered entries.
    - The spillover todo `.planning/workstreams/cycle21-cut-lab/todos/pending/2026-08-22-hide-non-current-tabpanels-on-sibling-workflow-tools.md` created in Task 3 exists on disk, and the moved file's criterion-12 entry cites it by full path — the entry cannot be written truthfully until that file exists, which is why this task runs after Task 3.
    - The moved file's criterion-13 entry cites both gate files closed in Task 2 by full path.
  </acceptance_criteria>
  <reversibility rating="reversible">One `git mv` and a markdown append; fully revertable.</reversibility>
  <done>The source capture is in `todos/completed/` with a 13-entry per-criterion resolution, and criteria 12 and 13 each cite evidence that existed before this task attested to it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| planning documents → future decisions | A gate table that says CLOSED is the artifact a later phase trusts when deciding the branch may ship. |
| local dev server → Playwright run | The regression sweep starts `DeckFlow.Web` locally and enables the Cut Lab tool through the admin surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07.1-22 | Repudiation | review gate tables and the source-capture completion record | high | mitigate | Marking a gate CLOSED without evidence, or marking the source capture's criteria satisfied before the evidence for them exists, is a false attestation that a later phase will act on, and this branch has a recorded history of green tests over live defects. Task 2 requires every gate closure to cite the plan id AND at least one mutation id, and forbids deleting the original rows — the record of what was found survives alongside the record of what closed it. The source capture's move into `todos/completed/` and its per-criterion `## Resolution` section are deliberately split into Task 4, which runs after both Task 2 (gates) and Task 3 (spillover todo), so criteria 12 and 13 are never attested to before the artifacts that satisfy them exist. |
| T-07.1-23 | Spoofing | `dotnet test` / `dotnet build` results | high | mitigate | A locked `DeckFlow.Web` exe yields MSB3027, exit 1 and zero executed tests, which reads as a failure; a stale server on port 5173 makes a Playwright run measure the wrong build. Task 1 requires the process to be stopped first, requires an explicit `grep -c 'error MSB'` result of `0` on record, and requires a nonzero executed-test count. |
| T-07.1-24 | Tampering | line endings across the phase's touched files | medium | mitigate | Whole-file EOL conversion silently rewrites files no one reviewed and is this repo's recorded top churn risk. Task 1 requires `git diff --stat` and `git diff --ignore-all-space --stat` to be byte-identical, checked by `diff` rather than by eye. |
| T-07.1-25 | Information Disclosure | compiled JS under `wwwroot/js/` | low | mitigate | `wwwroot/js/*.js` is gitignored and rebuilt at deploy; a staged artifact would create drift between the repo and what ships. An acceptance criterion asserts `git status --short DeckFlow.Web/wwwroot/js/` is empty. |
| T-07.1-26 | Elevation of Privilege | `tool.cut-lab.enabled` | medium | accept | The Playwright harness enables the Cut Lab tool on the LOCAL server only. This phase changes no feature-flag row and no seed; `tool.cut-lab.enabled` stays OFF in production and flipping it is explicitly out of scope. |
| T-07.1-SC | Tampering | npm/NuGet installs | low | accept | No package is added by this plan. |
</threat_model>

<verification>
- `07.1-REGRESSION.md` exists and its comparison table has a pass row for every declared baseline.
- `diff <(git diff --stat) <(git diff --ignore-all-space --stat)` produces no output.
- Neither gate file contains an open finding marker.
- The source capture is in `todos/completed/` with a 13-entry `## Resolution` section.
- The spillover todo exists and cross-references the 2026-08-02 partial-refactor todo.
</verification>

<success_criteria>
1. Build, xUnit, vitest, tsc and both Playwright projects all meet or beat their recorded baselines, with raw output on record.
2. Zero line-ending churn, proved by `diff` of the two stat commands rather than by inspection.
3. Both gates read CLOSED, with every original finding row preserved and annotated with the plan and mutation that closed it.
4. The source capture is completed with a per-criterion resolution covering all 13.
5. The sibling-tool spillover is a pending todo with verified line numbers and an explicit collision warning.
6. The ROADMAP's Phase 07.1 gate path is corrected to `.planning/reviews/`.
</success_criteria>

<artifacts_this_phase_produces>
- `.planning/workstreams/cycle21-cut-lab/phases/07.1-cutlab-review-findings-closeout/07.1-REGRESSION.md` (new file)
- `.planning/workstreams/cycle21-cut-lab/todos/pending/2026-08-22-hide-non-current-tabpanels-on-sibling-workflow-tools.md` (new file)
- `.planning/workstreams/cycle21-cut-lab/todos/completed/2026-08-22-close-six-open-cut-lab-code-review-findings.md` (moved from `todos/pending/`, gains a `## Resolution` section)
- `.planning/reviews/2026-08-22-cycle21-0706-code-gate.md` — finding status markers change from OPEN to FIXED; the Open work table gains a resolution column; a CLOSED line is added
- `.planning/reviews/2026-08-16-cycle21-cutlab-code-gate.md` — same shape of change for F-5, F-6, F-7 and the vacuous-tests section
- `.planning/workstreams/cycle21-cut-lab/ROADMAP.md` — the Phase 07.1 "Gates:" path is corrected to `.planning/reviews/`
</artifacts_this_phase_produces>

<output>
Create `.planning/workstreams/cycle21-cut-lab/phases/07.1-cutlab-review-findings-closeout/07.1-07-SUMMARY.md` when done.
</output>
