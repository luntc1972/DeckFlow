---
phase: 21.2-pluggable-llm-distill-cli-backends-inserted
plan: 01
subsystem: integration
tags: [llm, cli, claude, openai, distillation, testing]
requires: []
provides:
  - Shared distillation system prompts in DistillationSchemas.
  - Claude CLI-backed ILlmDistillationService with structured command specs.
  - LLM distillation provider factory for openai and claude.
  - Regression tests for OpenAI prompt/schema byte identity and CLI hardening.
affects: [content-kb, distillation, phase-21.3]
tech-stack:
  added: []
  patterns: [ProcessStartInfo.ArgumentList, JSON-array command override, fake process-runner seam]
key-files:
  created:
    - DeckFlow.Core/Integration/CliCommandSpec.cs
    - DeckFlow.Core/Integration/CliLlmDistillationService.cs
    - DeckFlow.Core/Integration/LlmDistillationProviderFactory.cs
    - DeckFlow.Core.Tests/CliLlmDistillationServiceTests.cs
    - DeckFlow.Core.Tests/LlmDistillationProviderFactoryTests.cs
    - DeckFlow.Core.Tests/DistillationPromptRegressionTests.cs
  modified:
    - DeckFlow.Core/Knowledge/DistillationSchemas.cs
    - DeckFlow.Core/Integration/LlmDistillationService.cs
key-decisions:
  - "OpenAI remains the default provider; claude resolves to the CLI service."
  - "codex is explicitly deferred to Phase 21.3 / KB-12 and throws NotSupportedException."
  - "DECKFLOW_LLM_CLI_COMMAND is parsed only as a JSON string array with exactly one {instruction} placeholder."
patterns-established:
  - "CLI instruction/schema are passed as one ArgumentList element; transcript is stdin."
  - "CLI extraction timeout is enforced by a linked CTS passed into the process-runner seam."
  - "Model JSON repair extracts the first single balanced object with quote/escape awareness."
requirements-completed: [KB-10, KB-11]
duration: ~25min
completed: 2026-06-01
---

# Phase 21.2 Plan 01 Summary

**OpenAI-byte-identical distillation prompts plus a hardened Claude CLI distillation backend selected by provider factory**

## Summary

- Extracted summary, clips, and tags system prompts into `DistillationSchemas` and repointed `LlmDistillationService` to the shared values.
- Added `CliCommandSpec` and `CliLlmDistillationService` for the claude backend with `ArgumentList`, JSON-array override parsing, fail-fast override validation, timeout/kill support, cancellation-aware stdin writes, reads-first pipe handling, balanced JSON repair, retry, tag validation, sanitized errors, and zero token usage.
- Added `LlmDistillationProviderFactory` with openai default, claude CLI selection, codex Phase 21.3 `NotSupportedException`, and unknown-provider rejection.
- Added fake-runner tests covering CLI envelope unwrap, fenced/prose JSON repair, retries, sanitized `is_error`, clips/tags, ValidateTags failures, timeout with `CancellationToken.None`, JSON-array override substitution, empty-arg preservation, and fail-fast bad override validation.

## Task Commits

1. `c2df1c3` - `feat(21.2-01): extract distillation prompts`
2. `5b4602a` - `feat(21.2-01): add claude cli distiller`
3. `e15f0e4` - `feat(21.2-01): add distillation provider factory`

## Key Files Created/Modified

- `DeckFlow.Core/Knowledge/DistillationSchemas.cs` - Added shared `SummarySystemPrompt`, `ClipsSystemPrompt`, `TagsSystemPrompt`, and private allowlist formatting.
- `DeckFlow.Core/Integration/LlmDistillationService.cs` - Repointed OpenAI system-message inputs to `DistillationSchemas.*SystemPrompt`.
- `DeckFlow.Core/Integration/CliCommandSpec.cs` - Added command spec record and envelope enum.
- `DeckFlow.Core/Integration/CliLlmDistillationService.cs` - Added claude CLI-backed distillation service.
- `DeckFlow.Core/Integration/LlmDistillationProviderFactory.cs` - Added openai/claude provider selection and codex deferral.
- `DeckFlow.Core.Tests/DistillationPromptRegressionTests.cs` - L1 guard asserting exact prompt text and schema constants.
- `DeckFlow.Core.Tests/CliLlmDistillationServiceTests.cs` - CLI backend behavior tests via fake runner; no real claude process.
- `DeckFlow.Core.Tests/LlmDistillationProviderFactoryTests.cs` - Provider selection tests.

## Verification

- Core build: `"/mnt/c/Program Files/dotnet/dotnet.exe" build DeckFlow.Core/DeckFlow.Core.csproj -c Debug` → 0 warnings, 0 errors.
- Focused VSTest: `"/mnt/c/Program Files/dotnet/dotnet.exe" test DeckFlow.Core.Tests/DeckFlow.Core.Tests.csproj --filter "FullyQualifiedName~CliLlmDistillation|FullyQualifiedName~LlmDistillationProviderFactory|FullyQualifiedName~DistillationPromptRegression" 2>&1 | tail -15` → Passed: 31, Failed: 0, Skipped: 0.
- VSTest was reliable in WSL for this plan; no manual harness fallback was needed.

## Plan-Specific Notes

- L1 byte-preservation method: `DistillationPromptRegressionTests` asserts the exact shared system-prompt strings and the exact `DistillationSchemas.*Schema` strings used as OpenAI system-message and ResponseFormat inputs.
- JSON-array override: `DECKFLOW_LLM_CLI_COMMAND` is parsed with `JsonSerializer.Deserialize<string[]>`; element 0 is `FileName`, the remaining elements are `ArgumentList`, and the single `{instruction}` element is substituted in place.
- Empty-arg behavior: `BuildSpec_JsonArrayOverride_PreservesEmptyAllowedToolsArg` asserts the argument immediately after `--allowedTools` is `string.Empty`.
- Fail-fast behavior: bad override tests assert the fake runner is invoked 0 times for non-JSON, missing placeholder, duplicate placeholder, and empty-array overrides.
- Codex: no codex backend, spawn logic, isolation, temp directory, or docs were implemented. `DECKFLOW_LLM_PROVIDER=codex` resolves to a `NotSupportedException` naming Phase 21.3 / KB-12.

## Deviations

None - plan executed within the allowed file set. No `.csproj`, roadmap, state, or unrelated source files were modified.

## Self-Check: PASSED

- Allowed source/test/summary files only.
- Atomic task commits created before this summary.
- No new NuGet packages.
- CLI tests use the fake process-runner seam and do not spawn claude.
- OpenAI default path inputs are guarded by prompt and schema fixture equality.
