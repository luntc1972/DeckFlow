---
phase: 999.1-ai-agnostic-prose-adaptation-razor-views
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - DeckFlow.Web/Services/ResponseParsers.cs
  - DeckFlow.Web/Services/MetaGapService.cs
  - DeckFlow.Web/Services/DeckComparisonService.cs
autonomous: true
requirements: [D-02, D-05]
must_haves:
  truths:
    - "User-facing exception messages thrown by ResponseParsers.cs, MetaGapService.cs, and DeckComparisonService.cs use 'AI' phrasing (no hardcoded 'ChatGPT'); the messages still convey the failure (which JSON was expected, which step to paste back in)."
    - "The Phase 10 fallback Key string 'ChatGPT' passed to JsonTextFormatterService.NormalizeSingleLine / NormalizeSingleLine on lines MetaGapService.cs:266 and DeckComparisonService.cs:229 is NOT touched (Phase 10 invariant; not a user-facing message)."
  artifacts:
    - path: "DeckFlow.Web/Services/ResponseParsers.cs"
      provides: "Brand-neutral InvalidOperationException messages for deck_profile / set_upgrade_report parse failures"
      contains: "AI response did not contain"
    - path: "DeckFlow.Web/Services/MetaGapService.cs"
      provides: "Brand-neutral InvalidOperationException messages for meta_gap parse failures"
      contains: "meta_gap JSON returned from"
    - path: "DeckFlow.Web/Services/DeckComparisonService.cs"
      provides: "Brand-neutral InvalidOperationException messages for deck_comparison parse failures"
      contains: "deck_comparison JSON returned from"
  key_links:
    - from: "Controllers (DeckController error-banner path)"
      to: "ResponseParsers / MetaGapService / DeckComparisonService"
      via: "InvalidOperationException.Message → ViewModel.ErrorMessage rendered in Razor error banner"
      pattern: "throw new InvalidOperationException"
---

<objective>
Rewrite the 8 user-facing `InvalidOperationException` messages across `ResponseParsers.cs` (4 hits), `MetaGapService.cs` (2 hits at lines 838 + 855), and `DeckComparisonService.cs` (2 hits at lines 815 + 835) to use the universal noun convention (D-05). These exception messages render directly into the page error banner when a user pastes invalid JSON — same user-visible surface as Razor prose (D-02 #2). Exception messages have no access to selector state, so the universal noun applies (not `@aiPlatform.DisplayName`).

The Phase 10 fallback Key strings `"ChatGPT"` passed as the second argument to `JsonTextFormatterService.NormalizeSingleLine(...)` on `MetaGapService.cs:266` and `DeckComparisonService.cs:229` are NOT user-visible — they are the canonical Phase 10 default platform Key. DO NOT TOUCH.

Purpose: Stop the page error banner from telling Claude/Gemini users to "paste the deck_profile JSON returned from ChatGPT" — the message must read correctly regardless of which AI they used.
Output: Edited C# service files with brand-neutral exception messages; build clean.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/999.1-ai-agnostic-prose-adaptation-razor-views/999.1-CONTEXT.md
@DeckFlow.Web/Services/ResponseParsers.cs
@DeckFlow.Web/Services/MetaGapService.cs
@DeckFlow.Web/Services/DeckComparisonService.cs
@CLAUDE.md

<interfaces>
In-scope exception strings (8 total):

ResponseParsers.cs (4):
- Line 20: `throw new InvalidOperationException("Paste the deck_profile JSON returned from ChatGPT into Step 3.");`
- Line 35: `throw new InvalidOperationException("The submitted ChatGPT response did not contain a valid deck_profile payload.");`
- Line 41: `throw new InvalidOperationException("The submitted ChatGPT response did not contain a valid deck_profile payload.");` (duplicate string at different throw site)
- Line 51: `throw new InvalidOperationException("Paste the set_upgrade_report JSON returned from ChatGPT into Step 5.");`
- Line 66: `throw new InvalidOperationException("The submitted ChatGPT response did not contain a valid set_upgrade_report payload.");`
- Line 72: `throw new InvalidOperationException("The submitted ChatGPT response did not contain a valid set_upgrade_report payload.");` (duplicate)

(Actual count from grep is 6 hits in this file; "4 user-facing exception strings" in CONTEXT.md refers to the 4 distinct messages — the duplicates count once for substitution purposes.)

MetaGapService.cs (2):
- Line 838: `throw new InvalidOperationException("Paste the meta_gap JSON returned from ChatGPT into Step 3.");`
- Line 855: `throw new InvalidOperationException("The submitted ChatGPT response did not contain a valid meta_gap payload.");`

DeckComparisonService.cs (2):
- Line 815: `throw new InvalidOperationException("Paste the deck_comparison JSON returned from ChatGPT into Step 3.");`
- Line 835: `throw new InvalidOperationException("The submitted ChatGPT response did not contain a valid deck_comparison payload.");`

DO NOT TOUCH (Phase 10 fallback Key — single quoted literal `"ChatGPT"` as argument):
- MetaGapService.cs:266 `NormalizeSingleLine(request.TargetAiPlatform, "ChatGPT")` (default-value plumbing, not user-visible).
- DeckComparisonService.cs:229 same pattern.
- DeckAnalysisPacketService.cs:1309 same pattern (not in this plan's files_modified).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generalize ResponseParsers.cs exception messages</name>
  <files>DeckFlow.Web/Services/ResponseParsers.cs</files>
  <read_first>
    - .planning/phases/999.1-ai-agnostic-prose-adaptation-razor-views/999.1-CONTEXT.md (D-02 #2 exception scope, D-05 universal noun, claude-discretion phrasing note)
    - DeckFlow.Web/Services/ResponseParsers.cs (full file — confirm exact strings at lines 20, 35, 41, 51, 66, 72; confirm none of these are doc comments)
    - CLAUDE.md (preserve LF line endings, touch only lines that need touching, no Format Document)
  </read_first>
  <action>
    Substitute the user-facing exception message strings. Per D-05 Claude's Discretion: "The submitted X response..." sentences read better with `"the AI response"` (definite article) than `"your AI"`; for "Paste the X JSON returned from ChatGPT" use `"from your AI"` (possessive reads naturally here).

    Line 20: `"Paste the deck_profile JSON returned from ChatGPT into Step 3."` → `"Paste the deck_profile JSON returned from your AI into Step 3."`

    Line 35: `"The submitted ChatGPT response did not contain a valid deck_profile payload."` → `"The submitted AI response did not contain a valid deck_profile payload."`

    Line 41: same as line 35 (duplicate string) → same substitution.

    Line 51: `"Paste the set_upgrade_report JSON returned from ChatGPT into Step 5."` → `"Paste the set_upgrade_report JSON returned from your AI into Step 5."`

    Line 66: `"The submitted ChatGPT response did not contain a valid set_upgrade_report payload."` → `"The submitted AI response did not contain a valid set_upgrade_report payload."`

    Line 72: same as line 66 (duplicate) → same substitution.

    Do NOT touch:
    - Any other line in the file.
    - Doc comments mentioning ChatGPT (if any). The CONTEXT.md `<canonical_refs>` says ResponseParsers.cs has 4 user-facing exception strings; the file also has 6 actual hits from grep — verify all 6 are exception strings before substituting. If any of the 6 is a doc comment or non-user-facing literal, leave it.
    - Any `using` directive, namespace, class declaration. Preserve LF line endings. Touch only the strings.
  </action>
  <verify>
    <automated>grep -c 'ChatGPT' DeckFlow.Web/Services/ResponseParsers.cs</automated>
    <automated>grep -c 'from your AI' DeckFlow.Web/Services/ResponseParsers.cs</automated>
    <automated>grep -c 'The submitted AI response' DeckFlow.Web/Services/ResponseParsers.cs</automated>
    <automated>dotnet build DeckFlow.Web/DeckFlow.Web.csproj -c Debug --nologo -v quiet</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'ChatGPT' DeckFlow.Web/Services/ResponseParsers.cs` returns `0` (all 6 hits in this file are user-facing exception strings per CONTEXT.md inventory).
    - `grep -c 'from your AI' DeckFlow.Web/Services/ResponseParsers.cs` returns `2` (lines 20 and 51 — the "Paste the X JSON returned from your AI" form).
    - `grep -c 'The submitted AI response' DeckFlow.Web/Services/ResponseParsers.cs` returns `4` (lines 35, 41, 66, 72).
    - `dotnet build DeckFlow.Web/DeckFlow.Web.csproj -c Debug` exits 0.
  </acceptance_criteria>
  <done>ResponseParsers.cs exception messages no longer mention ChatGPT; build clean.</done>
</task>

<task type="auto">
  <name>Task 2: Generalize MetaGapService.cs and DeckComparisonService.cs exception messages</name>
  <files>DeckFlow.Web/Services/MetaGapService.cs, DeckFlow.Web/Services/DeckComparisonService.cs</files>
  <read_first>
    - .planning/phases/999.1-ai-agnostic-prose-adaptation-razor-views/999.1-CONTEXT.md (D-02 #2 exception scope; confirm lines 266 and 229 NormalizeSingleLine fallback Key string is Phase 10 invariant DO NOT TOUCH)
    - DeckFlow.Web/Services/MetaGapService.cs lines 260-275 and 830-860
    - DeckFlow.Web/Services/DeckComparisonService.cs lines 220-235 and 810-840
    - CLAUDE.md (preserve LF, no Format Document)
  </read_first>
  <action>
    Apply substitutions to user-facing exception strings only.

    `MetaGapService.cs`:
    - Line 838: `"Paste the meta_gap JSON returned from ChatGPT into Step 3."` → `"Paste the meta_gap JSON returned from your AI into Step 3."`
    - Line 855: `"The submitted ChatGPT response did not contain a valid meta_gap payload."` → `"The submitted AI response did not contain a valid meta_gap payload."`
    - Line 266 `NormalizeSingleLine(request.TargetAiPlatform, "ChatGPT")` — DO NOT TOUCH. The literal `"ChatGPT"` here is the Phase 10 default platform Key fallback for the `target_ai_platform:` zip metadata field. Confirm by inspection (look at the surrounding line: `builder.AppendLine($"target_ai_platform: {JsonTextFormatterService.NormalizeSingleLine(request.TargetAiPlatform, \"ChatGPT\")}");`). It is plumbing, not a user-facing message.

    `DeckComparisonService.cs`:
    - Line 815: `"Paste the deck_comparison JSON returned from ChatGPT into Step 3."` → `"Paste the deck_comparison JSON returned from your AI into Step 3."`
    - Line 835: `"The submitted ChatGPT response did not contain a valid deck_comparison payload."` → `"The submitted AI response did not contain a valid deck_comparison payload."`
    - Line 229 `NormalizeSingleLine(request.TargetAiPlatform, "ChatGPT")` — DO NOT TOUCH (same plumbing pattern, Phase 10 invariant).

    Do NOT touch:
    - Doc comments, log lines (if any in surrounding code), or other strings.
    - Any line not explicitly named above. Preserve LF line endings.
  </action>
  <verify>
    <automated>grep -c 'ChatGPT' DeckFlow.Web/Services/MetaGapService.cs</automated>
    <automated>grep -c 'ChatGPT' DeckFlow.Web/Services/DeckComparisonService.cs</automated>
    <automated>grep -c 'NormalizeSingleLine(request.TargetAiPlatform, "ChatGPT")' DeckFlow.Web/Services/MetaGapService.cs</automated>
    <automated>grep -c 'NormalizeSingleLine(request.TargetAiPlatform, "ChatGPT")' DeckFlow.Web/Services/DeckComparisonService.cs</automated>
    <automated>grep -c 'from your AI' DeckFlow.Web/Services/MetaGapService.cs</automated>
    <automated>grep -c 'from your AI' DeckFlow.Web/Services/DeckComparisonService.cs</automated>
    <automated>grep -c 'The submitted AI response' DeckFlow.Web/Services/MetaGapService.cs</automated>
    <automated>grep -c 'The submitted AI response' DeckFlow.Web/Services/DeckComparisonService.cs</automated>
    <automated>dotnet build DeckFlow.Web/DeckFlow.Web.csproj -c Debug --nologo -v quiet</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'ChatGPT' DeckFlow.Web/Services/MetaGapService.cs` returns `1` (only the Phase 10 fallback Key on line 266).
    - `grep -c 'ChatGPT' DeckFlow.Web/Services/DeckComparisonService.cs` returns `1` (only the Phase 10 fallback Key on line 229).
    - `grep -c 'NormalizeSingleLine(request.TargetAiPlatform, "ChatGPT")' DeckFlow.Web/Services/MetaGapService.cs` returns `1` (Phase 10 invariant preserved).
    - `grep -c 'NormalizeSingleLine(request.TargetAiPlatform, "ChatGPT")' DeckFlow.Web/Services/DeckComparisonService.cs` returns `1` (Phase 10 invariant preserved).
    - `grep -c 'from your AI' DeckFlow.Web/Services/MetaGapService.cs` returns `1` (line 838 substituted).
    - `grep -c 'from your AI' DeckFlow.Web/Services/DeckComparisonService.cs` returns `1` (line 815 substituted).
    - `grep -c 'The submitted AI response' DeckFlow.Web/Services/MetaGapService.cs` returns `1` (line 855 substituted).
    - `grep -c 'The submitted AI response' DeckFlow.Web/Services/DeckComparisonService.cs` returns `1` (line 835 substituted).
    - `dotnet build DeckFlow.Web/DeckFlow.Web.csproj -c Debug` exits 0.
  </acceptance_criteria>
  <done>Both service files' user-facing exception messages are brand-neutral; Phase 10 fallback Key plumbing preserved; build clean.</done>
</task>

</tasks>

<verification>
- `grep -c 'ChatGPT' DeckFlow.Web/Services/ResponseParsers.cs` returns `0`.
- `grep -c 'ChatGPT' DeckFlow.Web/Services/MetaGapService.cs` returns `1` (Phase 10 fallback Key).
- `grep -c 'ChatGPT' DeckFlow.Web/Services/DeckComparisonService.cs` returns `1` (Phase 10 fallback Key).
- `dotnet build DeckFlow.Web/DeckFlow.Web.csproj -c Release` exits 0.
</verification>

<success_criteria>
- Page error banner no longer says "ChatGPT" regardless of which AI the user picked.
- Phase 10 default fallback Key plumbing untouched (zip round-trip still works).
- Build clean.
</success_criteria>

<output>
Create `.planning/phases/999.1-ai-agnostic-prose-adaptation-razor-views/999.1-04-SUMMARY.md` when done.
</output>
