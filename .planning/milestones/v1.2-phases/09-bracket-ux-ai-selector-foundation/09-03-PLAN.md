---
phase: 09-bracket-ux-ai-selector-foundation
plan: 03
type: execute
wave: 2
depends_on:
  - "09-01"
  - "09-02"
files_modified:
  - DeckFlow.Web/Views/Deck/ChatGptPackets.cshtml
  - DeckFlow.Web/Views/Deck/ChatGptDeckComparison.cshtml
  - DeckFlow.Web/Views/Deck/ChatGptCedhMetaGap.cshtml
autonomous: false
requirements:
  - BRKT-01
  - AISEL-01
  - AISEL-04

must_haves:
  truths:
    - "ChatGptPackets.cshtml Step 2 shows the AI selector as the first content item, followed by the bracket callout wrapping the df-select, then remaining fields"
    - "ChatGptDeckComparison.cshtml Step 2 shows the AI selector as the first content item before deck inputs"
    - "ChatGptCedhMetaGap.cshtml Step 2 shows the AI selector as the first content item before reference deck selection"
    - "The bracket df-select in ChatGptPackets.cshtml Step 2 is wrapped in a .bracket-callout div with the Required before generating eyebrow"
    - "All three AI selector radio inputs POST TargetAiPlatform with the form submission"
  artifacts:
    - path: "DeckFlow.Web/Views/Deck/ChatGptPackets.cshtml"
      provides: "Bracket callout wrapping TargetCommanderBracket; _AiSelector partial at top of Step 2"
      contains: "_AiSelector"
    - path: "DeckFlow.Web/Views/Deck/ChatGptDeckComparison.cshtml"
      provides: "_AiSelector partial at top of Step 2"
      contains: "_AiSelector"
    - path: "DeckFlow.Web/Views/Deck/ChatGptCedhMetaGap.cshtml"
      provides: "_AiSelector partial at top of Step 2"
      contains: "_AiSelector"
  key_links:
    - from: "ChatGptPackets.cshtml"
      to: "Views/Shared/_AiSelector.cshtml"
      via: "Html.PartialAsync(_AiSelector, Model.Request.TargetAiPlatform)"
      pattern: "PartialAsync.*_AiSelector"
    - from: "ChatGptPackets.cshtml TargetCommanderBracket label"
      to: ".bracket-callout CSS"
      via: "class=\"bracket-callout\" wrapper"
      pattern: "bracket-callout"
---

<objective>
Insert the _AiSelector partial at the top of Step 2 on all three ChatGPT analysis pages, and wrap
the TargetCommanderBracket df-select in ChatGptPackets.cshtml with the bracket callout treatment.

Purpose: Delivers the user-visible half of BRKT-01 and AISEL-01. Depends on Plan 09-01 (partials
and CSS exist) and Plan 09-02 (TargetAiPlatform property exists on the request models for POST
binding).

Output: Three Razor views modified. dotnet build clean. Checkpoint confirms visual rendering.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/09-bracket-ux-ai-selector-foundation/09-UI-SPEC.md
@.planning/phases/09-bracket-ux-ai-selector-foundation/09-RESEARCH.md
@.planning/phases/09-bracket-ux-ai-selector-foundation/09-01-SUMMARY.md
@.planning/phases/09-bracket-ux-ai-selector-foundation/09-02-SUMMARY.md

<interfaces>
From ChatGptPackets.cshtml (verified line numbers):
  Step 2 section starts: line 159 (data-chatgpt-step="2")
  Step heading div: lines 160-167, closes with </div> at line 167
  chatgpt-instructions div: starts line 169
  TargetCommanderBracket label: lines 200-210
    line 200: [SP8]<label class="field">
    line 201: [SP12]<span>Target Commander bracket <small>(required)</small></span>
    line 202: [SP12]<select name="TargetCommanderBracket" required aria-required="true" data-df-select>
    line 203: [SP16]<option value="">Select a target bracket (required)</option>
    lines 204-207: @foreach bracket options
    line 208: [SP12]</select>
    line 209: [SP12]<small class="sync-column__hint">Required. The analysis prompt...</small>
    line 210: [SP8]</label>

INSERT _AiSelector: after step heading closes (after line 167 </div>), before the
  chatgpt-instructions div at line 169. Insert as the first item inside the Step 2 content area.

WRAP TargetCommanderBracket: replace lines 200-210 with:
  <div class="bracket-callout">
      <p class="bracket-callout__label">Required before generating</p>
      <label class="field">
          ... existing label contents unchanged ...
      </label>
  </div>

From ChatGptDeckComparison.cshtml (verified line numbers):
  Step 2 section starts: line 309 (data-chatgpt-comparison-step="2")
  Step heading div: lines 310-317, closes at line 317 with </div>
  chatgpt-instructions div: starts line 319
  INSERT _AiSelector: after step heading closes (after line 317 </div>), before chatgpt-instructions at line 319

From ChatGptCedhMetaGap.cshtml (verified line numbers):
  Step 2 section starts: line 172 (data-chatgpt-cedh-step="2")
  Step heading div: lines 173-180, closes at line 180 with </div>
  Conditional input summary section: starts at line 182 (@if (!string.IsNullOrWhiteSpace...))
  INSERT _AiSelector: after step heading closes (after line 180 </div>), before the conditional @if at line 182

Partial call syntax (verified from _MoxfieldBulkEditHint usage):
  @await Html.PartialAsync("_AiSelector", Model.Request.TargetAiPlatform)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Insert _AiSelector and bracket callout into ChatGptPackets.cshtml</name>
  <files>DeckFlow.Web/Views/Deck/ChatGptPackets.cshtml</files>
  <read_first>
    - DeckFlow.Web/Views/Deck/ChatGptPackets.cshtml (read lines 155-215 to see the exact Step 2 opening, step heading close, chatgpt-instructions start, and the full TargetCommanderBracket label block)
  </read_first>
  <action>
Make two edits to ChatGptPackets.cshtml.

EDIT 1 — Insert _AiSelector partial call:
Find the closing </div> of the chatgpt-step-heading div inside the Step 2 section
(the div that contains the Step 2 eyebrow, h2, and badge — closes before the chatgpt-instructions div).
Insert the partial call ON A NEW LINE immediately after that </div>, before the chatgpt-instructions div:

    @await Html.PartialAsync("_AiSelector", Model.Request.TargetAiPlatform)

The result should read:
    [step heading div contents]
    </div>
    @await Html.PartialAsync("_AiSelector", Model.Request.TargetAiPlatform)
    <div class="chatgpt-instructions">

EDIT 2 — Wrap TargetCommanderBracket in bracket callout:
Find the bare <label class="field"> block for TargetCommanderBracket (the one whose select has
name="TargetCommanderBracket"). This is in Step 2 around line 200. Wrap it with:

    <div class="bracket-callout">
        <p class="bracket-callout__label">Required before generating</p>
        <label class="field">
            [existing label contents — span, select with all @foreach options, small hint — unchanged]
        </label>
    </div>

Do NOT change the contents of the label (the span text, the select element, its options, or the
small hint text). Do NOT add any new interactive elements. Do NOT remove the existing
name="TargetCommanderBracket", required, aria-required="true", or data-df-select attributes.

CRITICAL: The bracket callout wraps only the TargetCommanderBracket label, NOT any other field.
Confirm the select you are wrapping has name="TargetCommanderBracket" before editing.
  </action>
  <verify>
    <automated>grep -n "PartialAsync.*_AiSelector\|bracket-callout" /mnt/c/users/chrislunt/source/personal/decksyncworkbench/DeckFlow.Web/Views/Deck/ChatGptPackets.cshtml</automated>
  </verify>
  <acceptance_criteria>
    - grep "PartialAsync.*_AiSelector" ChatGptPackets.cshtml returns 1 result
    - grep "Model.Request.TargetAiPlatform" ChatGptPackets.cshtml includes the partial call line
    - grep -c "bracket-callout" ChatGptPackets.cshtml returns >= 2 (opening div and the label class)
    - grep "bracket-callout__label" ChatGptPackets.cshtml returns 1 result with text "Required before generating"
    - grep "name=\"TargetCommanderBracket\"" ChatGptPackets.cshtml still returns 1 result (not duplicated or removed)
    - grep "data-df-select" ChatGptPackets.cshtml count is unchanged from before edit (df-select not removed)
    - The _AiSelector partial call appears BEFORE the bracket-callout div in the file (confirmed by line number order)
    - dotnet build DeckFlow.Web passes clean
  </acceptance_criteria>
  <done>_AiSelector rendered at top of Step 2 in ChatGptPackets; TargetCommanderBracket wrapped in .bracket-callout with eyebrow label.</done>
</task>

<task type="auto">
  <name>Task 2: Insert _AiSelector into ChatGptDeckComparison.cshtml and ChatGptCedhMetaGap.cshtml</name>
  <files>
    DeckFlow.Web/Views/Deck/ChatGptDeckComparison.cshtml
    DeckFlow.Web/Views/Deck/ChatGptCedhMetaGap.cshtml
  </files>
  <read_first>
    - DeckFlow.Web/Views/Deck/ChatGptDeckComparison.cshtml (read lines 305-325 — Step 2 section open, heading div, first content after heading)
    - DeckFlow.Web/Views/Deck/ChatGptCedhMetaGap.cshtml (read lines 170-190 — Step 2 section open, heading div, first content after heading)
  </read_first>
  <action>
Insert the _AiSelector partial call at the top of Step 2 content in both files.
The call is identical in both files.

CHATGPTDECKCOMPARISON.CSHTML:
Find the Step 2 section (data-chatgpt-comparison-step="2", around line 309).
Inside it, find the closing </div> of the chatgpt-step-heading div (the div containing
Step 2 eyebrow, h2, and badge — around line 317).
Insert IMMEDIATELY AFTER that </div>:

    @await Html.PartialAsync("_AiSelector", Model.Request.TargetAiPlatform)

The chatgpt-instructions div follows — the partial call should appear between the heading
close and the chatgpt-instructions open. No other content should be added or removed.

CHATGPTCEDHMETAGAP.CSHTML:
Find the Step 2 section (data-chatgpt-cedh-step="2", around line 172).
Inside it, find the closing </div> of the chatgpt-step-heading div (around line 180).
Insert IMMEDIATELY AFTER that </div>:

    @await Html.PartialAsync("_AiSelector", Model.Request.TargetAiPlatform)

The conditional @if block follows (around line 182). The partial call appears between
the heading close and the @if block.

Both pages do NOT have a TargetCommanderBracket bracket callout — that treatment is only
on the Packets page. Do NOT wrap any select element on these pages.

Both pages already have no TargetAiPlatform on screen before this change — do NOT move
or alter any existing content other than inserting the partial call.
  </action>
  <verify>
    <automated>grep -n "PartialAsync.*_AiSelector" /mnt/c/users/chrislunt/source/personal/decksyncworkbench/DeckFlow.Web/Views/Deck/ChatGptDeckComparison.cshtml /mnt/c/users/chrislunt/source/personal/decksyncworkbench/DeckFlow.Web/Views/Deck/ChatGptCedhMetaGap.cshtml</automated>
  </verify>
  <acceptance_criteria>
    - grep "PartialAsync.*_AiSelector" ChatGptDeckComparison.cshtml returns exactly 1 result
    - grep "PartialAsync.*_AiSelector" ChatGptCedhMetaGap.cshtml returns exactly 1 result
    - grep "Model.Request.TargetAiPlatform" ChatGptDeckComparison.cshtml returns 1 result
    - grep "Model.Request.TargetAiPlatform" ChatGptCedhMetaGap.cshtml returns 1 result
    - grep "bracket-callout" ChatGptDeckComparison.cshtml returns 0 results (no callout on comparison page)
    - grep "bracket-callout" ChatGptCedhMetaGap.cshtml returns 0 results
    - dotnet build DeckFlow.Web passes clean (0 errors, 0 Razor compilation errors)
  </acceptance_criteria>
  <done>_AiSelector partial inserted at top of Step 2 content in both Comparison and CEDH Meta Gap views; no other content altered; dotnet build clean.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    All three analysis pages now show the AI target selector (ChatGPT / Claude / Gemini radio pill
    group) at the top of Step 2. ChatGptPackets Step 2 also shows the bracket field inside a
    left-bordered callout with "Required before generating" eyebrow text.
  </what-built>
  <how-to-verify>
    Run the site locally: dotnet run --project DeckFlow.Web

    1. Open http://localhost:5173/chatgpt-packets (or the local port).
       - Navigate to Step 2.
       - Confirm: AI selector appears at the TOP of Step 2 content (before the bracket field).
       - Confirm: Selector shows three pill buttons — ChatGPT (pre-selected), Claude, Gemini.
       - Confirm: Bracket df-select is inside a bordered callout with "REQUIRED BEFORE GENERATING" eyebrow in accent color.
       - Confirm: Clicking Claude or Gemini highlights that pill (border changes to accent color, font bold).
       - Confirm: The bracket df-select still works (click it, dropdown appears with bracket options).
       - Confirm: All existing Step 2 fields below the AI selector are visually unchanged.

    2. Open http://localhost:5173/chatgpt-comparison.
       - Navigate to Step 2.
       - Confirm: AI selector appears at the TOP of Step 2 content.
       - Confirm: No bracket callout present on this page.

    3. Open http://localhost:5173/chatgpt-cedh-meta-gap.
       - Navigate to Step 2.
       - Confirm: AI selector appears at the TOP of Step 2 content.

    4. Test round-trip on Packets page:
       - Select Claude in Step 2.
       - Complete Step 2 (generate a packet).
       - Download the session zip.
       - Reload the page and upload the zip.
       - Confirm: the AI selector now shows Claude pre-selected (not ChatGPT).

    5. Test legacy zip behavior:
       - If you have an older zip (without target_ai_platform in 01-request-context.txt), upload it.
       - Confirm: AI selector defaults to ChatGPT (no error, no blank selection).

    Test in at least two guild themes (e.g., default + one non-default) and confirm the selector
    renders with theme-appropriate colors (info panel background, accent border on selected state).
  </how-to-verify>
  <resume-signal>Type "approved" if all visual checks pass. Describe any issues found.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser -> Razor partial model parameter | Model.Request.TargetAiPlatform is a server-side value passed to _AiSelector; no user-supplied input flows through the partial model at render time |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-06 | Spoofing | _AiSelector partial model binding | accept | The partial receives a server-side string from the model; no XSS vector — Razor HTML-encodes output by default |
| T-09-07 | Information Disclosure | Razor view changes | accept | No new data surface; the AI selector reveals only which platform the user chose — not sensitive |
</threat_model>

<verification>
dotnet build DeckFlow.Web — must pass clean before checkpoint.

grep -c "PartialAsync.*_AiSelector" DeckFlow.Web/Views/Deck/ChatGptPackets.cshtml    (expect 1)
grep -c "PartialAsync.*_AiSelector" DeckFlow.Web/Views/Deck/ChatGptDeckComparison.cshtml  (expect 1)
grep -c "PartialAsync.*_AiSelector" DeckFlow.Web/Views/Deck/ChatGptCedhMetaGap.cshtml  (expect 1)
grep -c "bracket-callout" DeckFlow.Web/Views/Deck/ChatGptPackets.cshtml  (expect >= 2)
grep -c "bracket-callout" DeckFlow.Web/Views/Deck/ChatGptDeckComparison.cshtml  (expect 0)
</verification>

<success_criteria>
- All three analysis pages render the AI selector at the top of Step 2 (confirmed visually in checkpoint)
- ChatGptPackets Step 2 shows bracket callout with "Required before generating" eyebrow
- Selecting Claude or Gemini and downloading/uploading zip restores the selection on Packets page
- dotnet build DeckFlow.Web clean before checkpoint
- Checkpoint human-verify: approved
</success_criteria>

<output>
After completion, create .planning/phases/09-bracket-ux-ai-selector-foundation/09-03-SUMMARY.md
</output>
