---
context: phase
phase: 81-opening-hand-mulligan-evaluator
task: planning-complete
total_tasks: 9
status: planned-not-executed
last_updated: 2026-07-03T15:54:25.799Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> Discovered through actual failure this cycle. Acknowledge before proceeding.

- [ ] CONSTRAINT: **Separate bench per execute run** — Phase 78 collided when a gsd-executor bg agent + an orchestrator Codex dispatch both ran `/gsd-execute-phase` in ONE shared worktree → interleaved/amended commits. Mitigation: run `/gsd-execute-phase 81` in a DEDICATED fresh worktree off this branch tip; never in the shared main checkout; never two execute passes in one bench.
- [ ] CONSTRAINT: **Session cwd anchors to shared main** — `cd` does not persist across Bash calls and subagents inherit session cwd. Mitigation: this session used `EnterWorktree path=/mnt/c/.../deckflow-cycle14` to re-anchor so gsd-sdk + subagents run in the cycle14 bench. A fresh session must open IN the cycle14 (or a new) worktree, not the shared main tree.

**Do not proceed until both boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Shared-worktree execute collision | Two concurrent commit sources in one worktree interleave/amend each other | blocking | One execute run per dedicated worktree; no bg executor + foreground dispatch in the same tree |
| WSL-local test pass masks CI failure | `dotnet test` in WSL went green while GitHub Actions had 2 real failures (Cycle 13) | blocking | CI (GitHub Actions) is the authoritative gate; push-and-watch before close, don't trust WSL-local green |
| Depth-index mulligan bucketing | Bucketing kept hands by London depth index mislabels the Commander free-mulligan (depth-1 kept-7) as mull-to-6 | advisory | Bucket by the RETURNED keep VALUE captured before the Math.Min tiny-deck clamp (already baked into 81-01 plan + golden test) |

<current_state>
Phase 81 (Opening-Hand / Mulligan Evaluator — the LAST Cycle 14 phase) is FULLY PLANNED and DUAL-REVIEWED APPROVE. NOT executed. No code written — plans only.

Branch `plan/cycle-14-deck-eval-depth`, tip `a6010a05`, 31 commits ahead of origin, working tree CLEAN, NOT pushed.
Plans on disk: `.planning/phases/81-opening-hand-mulligan-evaluator/{81-01,81-02,81-03}-PLAN.md`.
</current_state>

<completed_work>

Planning + review (all done this session, in the cycle14 bench):
- Draft 3 plans — commit `ef1fd560`
- gsd-plan-checker #1 → 1 blocker (OpeningHandSample forward-ref) + 1 warning (keptDepth free-mull mislabel)
- Fix both → `436b707a` → gsd-plan-checker #2 = PASS
- Codex gpt-5.5 plan review → CHANGES-REQUIRED (1 HIGH on-curve/HasPlan spell-attribution, 1 MED capture-order, 1 LOW seed reflection)
- Fix all 3 → `a6010a05` → gsd-plan-checker #3 = PASS (no regression) → Codex gpt-5.5 confirm = APPROVE
</completed_work>

<remaining_work>

- **Execute** — `/gsd-execute-phase 81` in a fresh bench. 3 waves: 81-01 Core → 81-02 flag+artifact → 81-03 on-page readout. Claude codes (sonnet gsd-executors), Codex gpt-5.4 reviews each diff.
- **Push → CI green** — GitHub Actions is the authoritative gate.
- **Live visual smoke** — /manabase desktop 1280 + mobile 390, flag `analysis.mulligan-eval` ON (lens shows) / OFF (hidden), across themes. DECKFLOW_DISABLE_AUTO_BROWSER=true, never open a browser on the Windows host.
</remaining_work>

<decisions_made>

- Re-anchored session into cycle14 via `EnterWorktree path=` (cwd/subagent isolation).
- 3-plan recipe mirrors 79/80. `/manabase` has ONE paste artifact (`ManabaseReportTextBuilder`) → **NO ADR-0001 3-variant parity test** (mirror TAP's single `AppendTapAnalysisBlock` + null-guard byte-identity). No zip / no hidden-field round-trip on /manabase → 81-03 simpler than 79.
- No-second-Simulate via TAP-02 `Turn1UntappedTrials` piggyback (cast% byte-identical, pinned golden). Reuse sim's own `LondonMulligan` + `ColorKeepCap` as the single keep def (can't contradict tool's cast rate).
- Keep-size bucket by RETURNED keep value pre-`Math.Min` clamp (singleton depth-0 AND depth-1 both return keep=7).
- On-curve/HasPlan attributed to a tracked spell (`TrackedSpellName` + `TrackedOnCurveTurn`), openers from EARLY low-MV non-commander rows; `HasPlan` = ≥2 lands AND kept-land color coverage AND tracked play OnCurveCastable (Codex HIGH fix).
- Two-stage trial loop: Stage 1 keep-size buckets after `keptSize`; Stage 2 sample after `SimulateGame` yields `firstCastableTurn`; no extra RNG/second Simulate.
- Postgres seed-consistency test reads private `PostgresSeedSql` via `BindingFlags.NonPublic | BindingFlags.Static` (matches `ToolFlagSeedConsistencyTests`), no visibility change.
</decisions_made>

<blockers>
- None technical. Human action: operator must execute in a fresh bench (see BLOCKING CONSTRAINTS).
</blockers>

## Required Reading (in order)
1. `.planning/phases/81-opening-hand-mulligan-evaluator/81-01-PLAN.md` — Core sim instrumentation + model + golden tests (MULLIGAN-01/02/03/04/05)
2. `.planning/phases/81-opening-hand-mulligan-evaluator/81-02-PLAN.md` — flag `analysis.mulligan-eval` seed both dialects + single-artifact block (MULLIGAN-01/04/06)
3. `.planning/phases/81-opening-hand-mulligan-evaluator/81-03-PLAN.md` — /manabase on-page lens + byte-identity render (MULLIGAN-01/02/06)
4. `.planning/ROADMAP.md` Phase 81 section — Goal, 5 success criteria, cross-cutting gates, resolved decisions
5. `DeckFlow.Core/Manabase/CastabilitySimulator.cs` — LondonMulligan (~1172-1232), Simulate/SimulateGame (~241-246); the reuse surface

## Infrastructure State
- Branch `plan/cycle-14-deck-eval-depth` tip `a6010a05`, 31 ahead of origin, clean, NOT pushed.
- Phase 79 EXECUTED+VERIFIED+PUSHED (CI green `28549247159`); Phase 80 EXECUTED+VERIFIED+Codex-APPROVE, ~30 ahead NOT pushed.
- Render autodeploy OFF — prod deploy is MANUAL. Flags analysis.interaction-audit / analysis.wincon-map / analysis.mulligan-eval all seeded OFF.
- No background servers running for this task. Statusline hook patched (`~/.claude/hooks/gsd-statusline.js` default `end`→`front`, syntax OK) — reverts on `/gsd:update`.

<context>
Delegation rule (2026-06-27): Claude codes AND plans; Codex reviews. Phase 81 is the heaviest Cycle 14 phase but the additive-sim half is a TAP-02 clone on the OTHER pipeline (/manabase, routing decision 3a). The whole point of the plan revisions was honesty about what a PER-SPELL sim can attribute: keepable%/keep-size are legit spell-independent averages, but each opener's on-curve read belongs to a specific tracked spell — the render names it ("{TrackedSpellName} castable on curve (turn N)"), never a generic hand-wide claim.
</context>

<next_action>
Start with: open a FRESH worktree off tip `a6010a05`, then `/gsd-execute-phase 81`. Execute wave-by-wave (81-01 → 81-02 → 81-03), Claude codes / Codex reviews each diff, then push → CI green → operator live smoke.
</next_action>
