---
phase: 103-simulation-engine-guided-cut-rounds
plan: 07
type: execute
wave: 5
depends_on: ["103-02", "103-03", "103-04", "103-05", "103-06"]
files_modified:
  - DeckFlow.Web/Models/Api/CutLabDecideApiRequest.cs
  - DeckFlow.Web/Models/Api/CutLabDecideApiResponse.cs
  - DeckFlow.Web/Controllers/Api/CutLabApiController.cs
  - DeckFlow.Web.Tests/CutLabApiControllerTests.cs
autonomous: true
requirements: [CUT-01, CUT-02, CUT-03, SIM-01]
must_haves:
  truths:
    - "CUT-03/D-09: POST /api/cut-lab/decide accepts an accept/reject/defer/restore decision + current state, applies it, and returns the next proposal, its deltas, floor warnings, cards-remaining count, and round banner — all in one JSON round-trip."
    - "D-09: same-origin is enforced as the FIRST statement (SameOriginRequestValidator) and the route is gated by [FeatureFlagGate(\"tool.cut-lab.enabled\")], exactly like CutLabController — no antiforgery-token mixing."
    - "FLOOR-02: every proposed/accepted cut is routed through CutLabFloorRules.Evaluate; floor warnings ride in the 200 response body (non-blocking), never a silent break."
    - "CUT-01/D-03/D-15: the response's next proposal + round banner come from CutLabCutRoundEngine, auto-advancing rounds and looping over deferred→rejected when still over 100."
    - "CUT-02: deltas come from CutLabSimulationService (reused engine); the endpoint emits no verdict language."
    - "D-16: a restore decision returns the card to the working list and REMOVES ALL of that card's decision records (not just the latest) so the round engine holds no stale defer/reject history, then recomputes counts/deltas."
    - "State integrity: the endpoint returns the updated serialized CutLabState (under 256KB) so the hidden field + no-JS fallback stay in sync."
  artifacts:
    - path: "DeckFlow.Web/Controllers/Api/CutLabApiController.cs"
      provides: "POST /api/cut-lab/decide (accept/reject/defer/restore)"
      contains: "SameOriginRequestValidator.IsValid"
  key_links:
    - from: "DeckFlow.Web/Controllers/Api/CutLabApiController.cs"
      to: "DeckFlow.Web/Services/CutLab/CutLabFloorRules.cs"
      via: "Evaluate before applying/presenting a cut (FLOOR-02)"
      pattern: "CutLabFloorRules.Evaluate"
    - from: "DeckFlow.Web/Controllers/Api/CutLabApiController.cs"
      to: "DeckFlow.Web/Services/CutLab/CutLabSimulationService.cs"
      via: "ComputeProposalDeltas for the next proposal"
      pattern: "ComputeProposalDeltas"
---

<objective>
Build the async decision endpoint that powers the D-09 loop: `POST /api/cut-lab/decide` takes a decision (accept/reject/defer/restore) plus the current serialized state, mutates the working list + decision history, routes the cut through the FLOOR-02 floor gate, asks the round engine for the next proposal, asks the simulation service for that proposal's deltas, and returns everything the client patches in place — plus the re-serialized state for the no-JS fallback.

Purpose: This is the CUT-03 transport. It reuses the round engine (CUT-01), simulation service (SIM-01/CUT-02), floor rules (FLOOR-02), and caches (D-10) built in prior plans.
Output: Request/response DTOs, `CutLabApiController`, controller tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/cut-lab/phases/103-simulation-engine-guided-cut-rounds/103-PATTERNS.md
@.planning/workstreams/cut-lab/phases/103-simulation-engine-guided-cut-rounds/103-RESEARCH.md
@.planning/workstreams/cut-lab/phases/103-simulation-engine-guided-cut-rounds/103-UI-SPEC.md

<interfaces>
Controller pattern to copy verbatim — DeckFlow.Web/Controllers/Api/DeckSyncApiController.cs:
- [ApiController] [Route("api/deck")] ; action: [HttpPost("diff")] [FeatureFlagGate("tool.deck-sync.enabled")] + ProducesResponseType(200/400/403)
- FIRST line: if (!SameOriginRequestValidator.IsValid(Request)) return StatusCode(403, new { Message = SameOriginRequestValidator.GetForbiddenMessage() });
- null-guard [FromBody] request → BadRequest(new { Message = ... })
- try { ... return Ok(response); } catch (Exception ex) when (ex is InvalidOperationException or ...) { log; return BadRequest(new { Message = ... }); }

Reused services (constructor-inject):
- CutLabStateSerializer.Deserialize/Serialize (Plan 103-02) — parse incoming state, re-serialize outgoing
- CutLabCutRoundEngine.BuildQueue (Plan 103-04) — next proposal + round banner + cards remaining
- ICutLabSimulationService.ComputeProposalDeltas (Plan 103-05) — deltas for the next proposal
- CutLabFloorRules.Evaluate(roleCounts, floors, candidateCutRoles, cardName, quantity) → IReadOnlyList<CutLabFloorWarning> (Phase 102)
- CutLabRoleAssigner role data / CutLabResolvedCardCache (deltas need resolved facts; cache populated at intake by Plan 103-06)

DTO analogs: DeckFlow.Web/Models/Api/DeckSyncApiRequest.cs / DeckSyncApiResponse.cs (folder + shape convention).
Copy for banners/next-state fields: 103-UI-SPEC Component Contracts 1-3 + Copywriting Contract (round banner strings, cards-remaining, cuts-made).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Decision request/response DTOs</name>
  <read_first>
    - DeckFlow.Web/Models/Api/DeckSyncApiRequest.cs / DeckSyncApiResponse.cs (namespace DeckFlow.Web.Models.Api, record DTO conventions)
    - DeckFlow.Web/Models/CutLab/CutLabMetrics.cs (CutLabMetricDelta/CutLabProposalDeltas for the response)
    - DeckFlow.Web/Services/CutLab/CutLabFloorRules.cs (CutLabFloorWarning shape)
    - DeckFlow.Web/Models/CutLab/CutLabState.cs (CutLabDecisionKind, CutLabDecision)
  </read_first>
  <action>
    Create `DeckFlow.Web/Models/Api/CutLabDecideApiRequest.cs` and `CutLabDecideApiResponse.cs` (namespace `DeckFlow.Web.Models.Api`, sealed records, `{ get; init; }`). Request: `string CutLabStateJson` (the current serialized working session), `string CardName`, and a decision discriminator (`string Decision` or a `CutLabDecideAction` enum: `Accept`/`Reject`/`Defer`/`Restore`). Response: the updated `string CutLabStateJson` (re-serialized), a `NextProposal` block (card name, round key, round label/banner text, "flagged by N findings" chip labels, or a nothing-to-cut / at-100 terminal flag), the next proposal's delta DTOs (project `CutLabProposalDeltas` → serializable DTO with per-line kind/label/before/after/delta/direction/isMeaningful + changedFamilyCount), `FloorWarnings` (from Evaluate), `CardsRemaining` (int), and `CutsMade` summary (list of {card, roundLabel, ordinal} for the restore list). XML-doc all members. Keep numbers as primitives so the response stays JSON-friendly and small.
  </action>
  <verify>
    <automated>"/mnt/c/Program Files/dotnet/dotnet.exe" build DeckFlow.Web/DeckFlow.Web.csproj -clp:ErrorsOnly</automated>
  </verify>
  <acceptance_criteria>
    - Both DTO files compile; request carries state + card + decision, response carries next proposal + deltas + floor warnings + cards-remaining + cuts-made.
    - Decision discriminator covers accept/reject/defer/restore.
    - No `ManabaseReport` type leaks into the DTOs (projected primitives only).
  </acceptance_criteria>
  <done>Typed request/response contract for the decision endpoint exists.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: CutLabApiController — POST /api/cut-lab/decide</name>
  <read_first>
    - DeckFlow.Web/Controllers/Api/DeckSyncApiController.cs (the full pattern: [ApiController], route, same-origin-first, feature gate, null guard, try/catch → { Message })
    - DeckFlow.Web/Controllers/CutLabController.cs ([FeatureFlagGate("tool.cut-lab.enabled")] key; RequestSizeLimit precedent)
    - DeckFlow.Web/Security/SameOriginRequestValidator.cs (IsValid/GetForbiddenMessage)
    - DeckFlow.Web/Services/CutLab/CutLabFloorRules.cs (Evaluate — must run before presenting/applying a cut)
    - DeckFlow.Web/Services/CutLab/CutLabCutRoundEngine.cs + CutLabSimulationService.cs + CutLabStateSerializer.cs (the reused services)
    - DeckFlow.Web.Tests/CutLabControllerTests.cs (fake-dependency wiring for a controller test)
    - .planning/workstreams/cut-lab/phases/103-simulation-engine-guided-cut-rounds/103-UI-SPEC.md (round banner + cards-remaining + error copy)
  </read_first>
  <behavior>
    - Test 1 (D-09 CSRF): a request failing `SameOriginRequestValidator.IsValid` returns 403 with `{ Message }` and mutates nothing.
    - Test 2 (null/invalid body): missing state or card returns 400 `{ Message }`.
    - Test 3 (accept): accepting a card removes it from the working list, appends an `Accepted` decision (with round + ordinal), decrements CardsRemaining, and returns a re-serialized state that round-trips.
    - Test 4 (FLOOR-02 non-blocking): accepting a card that breaks a floor still succeeds (200) AND includes the floor warning in the response body (Evaluate wired) — the warning does not turn into a 4xx.
    - Test 5 (CUT-01/D-03 auto-advance): when a round's queue exhausts, the response's NextProposal carries the next round's banner text (no dead-end); when at 100, a terminal at-100 flag.
    - Test 6 (D-15 loop): with deferred cards and still >100, the next proposal is drawn from the deferred pass with the "Second pass" banner.
    - Test 7 (D-16 restore, all-records): given a card that accrued MULTIPLE decision records (e.g. Deferred then Rejected then Accepted over loop-around passes), restoring it returns it to the working list, removes ALL of that card's decision records (not just the most recent), increments CardsRemaining, and the re-serialized state contains ZERO decision records for that card name (assert no stale defer/reject remains).
    - Test 8 (CUT-02): the response contains delta DTOs but no verdict-word copy.
  </behavior>
  <action>
    Create `DeckFlow.Web/Controllers/Api/CutLabApiController.cs` (namespace `DeckFlow.Web.Controllers.Api`, `[ApiController]`, `[Route("api/cut-lab")]`, `public sealed class`, constructor-injected `CutLabStateSerializer` usage is static so inject the round engine service surface, `ICutLabSimulationService`, `CutLabResolvedCardCache`, and `ILogger<CutLabApiController>`). Action: `[HttpPost("decide")] [FeatureFlagGate("tool.cut-lab.enabled")] [RequestSizeLimit(2*1024*1024)]` with `ProducesResponseType(200/400/403)`. Body:
    1. `SameOriginRequestValidator.IsValid(Request)` as the FIRST statement → 403 on fail (Pitfall 4).
    2. Null/shape guard the `[FromBody] CutLabDecideApiRequest` → 400.
    3. `CutLabStateSerializer.Deserialize(request.CutLabStateJson)` → working state.
    4. Resolve role counts for the candidate + `CutLabFloorRules.Evaluate(...)` to get floor warnings (FLOOR-02) — collect, do not block.
    5. Apply the decision to the working list + `Decisions` history: accept → remove card from working list + append an `Accepted` record; reject → append a `Rejected` record; defer → append a `Deferred` record; restore → return the card to the working list AND remove ALL `Decisions` records whose `CardName` equals the restored card (`Decisions.Where(d => d.CardName != cardName)`), so no stale Deferred/Rejected/Accepted history for that card survives into the round engine. Keep the commander/locks invariant throughout.
    6. `CutLabCutRoundEngine.BuildQueue(...)` for the next proposal + round banner + CardsRemaining (auto-advance + D-15 loop).
    7. `ICutLabSimulationService.ComputeProposalDeltas(currentWorkingList, nextCard)` for the next proposal's deltas (cache-backed, D-10).
    8. Re-`Serialize` the state (catch the >256KB `InvalidOperationException` → 400 `{ Message }`), build and `return Ok(response)`.
    Catch `InvalidOperationException`/`ArgumentException` → `BadRequest(new { Message = ... })` with the neutral copy from the UI spec ("Couldn't recalculate this cut — nothing changed. Try again."). No page reload; JSON only. Write `CutLabApiControllerTests.cs` covering the eight behaviors using fakes for the sim service + caches (pattern off `CutLabControllerTests`).
  </action>
  <verify>
    <automated>"/mnt/c/Program Files/dotnet/dotnet.exe" test DeckFlow.Web.Tests --filter "FullyQualifiedName~CutLabApiController"</automated>
  </verify>
  <acceptance_criteria>
    - All eight behaviors pass.
    - `SameOriginRequestValidator.IsValid` is the first statement in the action (source-order assertion); no `[ValidateAntiForgeryToken]` on the action.
    - `[FeatureFlagGate("tool.cut-lab.enabled")]` present on the action (grep).
    - `CutLabFloorRules.Evaluate` is called and warnings appear in the 200 body for a floor-breaking accept (non-blocking).
    - Restore removes ALL decision records for the restored card name — Test 7 asserts zero remaining records for that card (no stale defer/reject) and the card back in the working list.
    - Route is `POST /api/cut-lab/decide`.
    - Response state re-serializes under the cap; oversized state returns 400 `{ Message }`, not a 500.
  </acceptance_criteria>
  <done>The async decision endpoint applies decisions, gates floors, returns the next proposal + deltas + counts, restores cards by clearing all their decision history, and keeps state in sync.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| browser → /api/cut-lab/decide | Untrusted JSON (state blob + decision) crosses into server state mutation + simulation. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-103-07-01 | Spoofing / CSRF | Cross-origin POST to /api/cut-lab/decide | mitigate | `SameOriginRequestValidator.IsValid(Request)` as the first statement (403 otherwise) — the codebase JSON-API idiom, replacing antiforgery tokens (Pitfall 4). |
| T-103-07-02 | Tampering | Oversized/forged CutLabStateJson in the body | mitigate | `Deserialize` size-guards + clamps (Plan 103-02); commander/lock invariants re-enforced; re-`Serialize` caps output at 256KB → 400 on overflow. |
| T-103-07-03 | Elevation of Privilege | Bypassing the feature flag on the API route | mitigate | `[FeatureFlagGate("tool.cut-lab.enabled")]` gates the action exactly as it gates CutLabController; flag OFF in prod. |
| T-103-07-04 | Denial of Service | Repeated decide calls forcing full simulations | mitigate | Deltas served from CutLabDeltaCache (D-10); resolved cards from CutLabResolvedCardCache (no re-fetch); RequestSizeLimit 2MB; pool bounded ≤150. |
| T-103-07-05 | Repudiation | Silent floor break | mitigate | Every cut passes `CutLabFloorRules.Evaluate`; warnings are returned in the response (FLOOR-02), never dropped. |
| T-103-07-SC | Tampering | package installs | accept | No packages installed this phase. |
</threat_model>

<verification>
- `"/mnt/c/Program Files/dotnet/dotnet.exe" test DeckFlow.Web.Tests --filter "FullyQualifiedName~CutLab"` green.
- Same-origin 403, feature-gate, floor-warning-in-body, restore-clears-all-records, and state round-trip all test-proven.
</verification>

<success_criteria>
A same-origin-guarded, flag-gated decision endpoint applies accept/reject/defer/restore (restore clearing ALL of a card's decision records), enforces FLOOR-02 non-blocking, returns the next proposal + deltas + counts, and keeps state under 256KB.
</success_criteria>

<output>
Create `.planning/workstreams/cut-lab/phases/103-simulation-engine-guided-cut-rounds/103-07-SUMMARY.md` when done.
</output>
