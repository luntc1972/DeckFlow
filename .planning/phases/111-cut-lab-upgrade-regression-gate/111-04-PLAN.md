---
phase: 111-cut-lab-upgrade-regression-gate
plan: 04
type: execute
wave: 2
depends_on: [111-01, 111-02, 111-03]
files_modified:
  - DeckFlow.Web/e2e/cut-lab-nav-themes.spec.ts
  - .planning/phases/111-cut-lab-upgrade-regression-gate/111-UI-REVIEW.md
  - .planning/phases/111-cut-lab-upgrade-regression-gate/111-FINDINGS.md
autonomous: false
requirements: [CLUP-20]
must_haves:
  truths:
    - "Representative desktop AND mobile screenshots for Classic, Nyx, and Commander Table exist for the Cut Lab Lock-your-pool view, each showing the package UI (a locked deterministic package)."
    - "Each screenshot has an explicit written pass/fail note for usability, understandability, aesthetic hierarchy, and readability."
    - "All findings surfaced across Plans 01-04 are either fixed or explicitly recorded as deferred with rationale."
  artifacts:
    - path: ".planning/phases/111-cut-lab-upgrade-regression-gate/111-UI-REVIEW.md"
      provides: "Per-theme, per-viewport reviewed pass/fail notes on the four review axes with screenshot references"
      contains: "usability"
    - path: ".planning/phases/111-cut-lab-upgrade-regression-gate/111-FINDINGS.md"
      provides: "Consolidated fixed-or-deferred findings ledger for the whole phase (Success Criterion 6)"
      contains: "deferred"
    - path: "DeckFlow.Web/e2e/cut-lab-nav-themes.spec.ts"
      provides: "Desktop + mobile screenshot capture for Classic/Nyx/Commander Table"
      contains: "screenshotDir"
  key_links:
    - from: "cut-lab-nav-themes.spec.ts capture loop"
      to: ".planning/ui-design/cut-lab/screenshots/"
      via: "page.screenshot fullPage per theme x {desktop,mobile}"
      pattern: "screenshot"
    - from: "111-UI-REVIEW.md"
      to: "captured PNG files"
      via: "each axis note references the exact screenshot filename"
      pattern: "\\.png"
---

<objective>
Deliver CLUP-20 and Success Criterion 6: capture representative desktop AND mobile
screenshots for Classic, Nyx, and Commander Table of the Cut Lab Lock-your-pool view, write
an explicit pass/fail review across four axes (usability, understandability, aesthetic
hierarchy, readability), and consolidate all Phase-111 findings into a fixed-or-deferred
ledger. This is the human-in-the-loop evidence gate; it runs last (Wave 2) so the reviewed
screenshots reflect any fixes from Plans 01-03.

Purpose: Automated contrast (Plan 03) cannot judge hierarchy/understandability. A reviewed
screenshot set is the durable usability record and the phase's closeout artifact.

Output: Extended capture spec, 111-UI-REVIEW.md (reviewed), 111-FINDINGS.md (fixed/deferred).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/111-cut-lab-upgrade-regression-gate/111-COVERAGE-MATRIX.md

<interfaces>
Existing capture mechanism - DeckFlow.Web/e2e/cut-lab-nav-themes.spec.ts:
  - screenshotDir = resolve(__dirname, '../../.planning/ui-design/cut-lab/screenshots')
  - themes = [{name:'classic',cookie:'site.css'}, {name:'nyx',cookie:'site-nyx.css'},
             {name:'commander-table',cookie:'site-commander-table.css'}]  (exactly the CLUP-20 set)
  - mobileViewport = { width: 430, height: 2200 }
  - importPool(page) imports the oversized pool and reveals the Lock-your-pool view.
  - Current test captures MOBILE only: page.screenshot({ path: join(screenshotDir,
    `cut-lab-nav-${theme.name}-mobile.png`), fullPage: true }).
  - Uses admin-lock beforeEach/afterEach + setToolEnabled + serial mode (reuse as-is).

Desktop viewport to add: { width: 1280, height: 900 } (matches playwright.config chromium-desktop).

Constraints: server headless via scripts/run-web-test.sh; Playwright headless in WSL; never a
Windows browser window; no new packages.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Capture desktop + mobile screenshots for the 3 review themes</name>
  <files>DeckFlow.Web/e2e/cut-lab-nav-themes.spec.ts</files>
  <action>
    Extend cut-lab-nav-themes.spec.ts so the 3-theme loop captures BOTH viewports of the
    Lock-your-pool view. Add a dedicated test (or extend the existing capture test) that, for
    each of the 3 themes, renders /cut-lab via importPool at desktop {1280x900} and at the
    existing mobile {430x2200}, and writes deterministic filenames into screenshotDir:
      cut-lab-review-<theme>-desktop.png  and  cut-lab-review-<theme>-mobile.png  (fullPage).
    Before each capture, open the Lands role group summary so Lock All pill + chips are visible,
    and drive one JS decide (waitForResponse /api/cut-lab/decide, pattern from
    cut-lab-structure.spec.ts) so the sticky status + structural-findings panel + combo chips
    appear in the shot.
    (H2) CREATE + LOCK a deterministic package BEFORE each capture so the package UI is in the
    screenshot - package rows render only when Model.Packages.Count > 0 (CutLab.cshtml:464) and
    the imported oversized pool has none. Use the smoke-spec pattern (cut-lab-smoke.spec.ts:106-111)
    with the pool's Sol Ring + Arcane Signet: select[data-cut-lab-package-card="Sol Ring"] ->
    '__new__'; [data-cut-lab-new-package-input] -> 'Fast mana'; [data-cut-lab-new-package-save]
    click; select[data-cut-lab-package-card="Arcane Signet"] -> selectOption({ label: 'Fast mana' });
    then check [data-cut-lab-package-id](hasText 'Fast mana') input[data-cut-lab-package-toggle].
    Ensure the Packages section is expanded/visible, THEN capture. This applies to ALL 3 themes
    at BOTH desktop and mobile. Keep the existing mobile-chrome assertion test intact; do not
    delete its assertions. Preserve LF endings. Server headless via run-web-test.sh; Playwright
    headless in WSL.
  </action>
  <verify>
    <automated>bash scripts/run-web-test.sh > scratchpad-web-04.log 2>&1 & SRV=$!; until curl -s -o /dev/null http://localhost:5173/; do sleep 2; done; (cd DeckFlow.Web && env -u DISPLAY -u WAYLAND_DISPLAY npx --no-install playwright test e2e/cut-lab-nav-themes.spec.ts); RC=$?; kill $SRV 2>/dev/null || true; ls .planning/ui-design/cut-lab/screenshots/cut-lab-review-*-desktop.png .planning/ui-design/cut-lab/screenshots/cut-lab-review-*-mobile.png; exit $RC</automated>
  </verify>
  <done>Six PNGs exist: {classic,nyx,commander-table} x {desktop,mobile}, each showing the Lock-your-pool view WITH the locked 'Fast mana' package UI (package panel + Lock package toggle + helper copy) plus pills/chips/sticky/findings.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Six Cut Lab screenshots (Classic/Nyx/Commander-Table x desktop/mobile) plus a drafted 111-UI-REVIEW.md with a review row per screenshot across the four CLUP-20 axes.</what-built>
  <how-to-verify>
    1. Open the six PNGs under .planning/ui-design/cut-lab/screenshots/ (cut-lab-review-*-{desktop,mobile}.png).
    2. For each, judge the four axes: usability (can a user find/operate Lock All, chips,
       filters, the package panel + Lock-package toggle, decide buttons?), understandability
       (is the Lock-your-pool flow legible?), aesthetic hierarchy (is primary action vs
       secondary evidence and the package panel visually ranked?), readability (text - including
       the package helper copy - legible on that theme?).
    3. Confirm the drafted 111-UI-REVIEW.md pass/fail notes match what you see; correct any
       note you disagree with, and flag any usability/readability defect for the findings ledger.
  </how-to-verify>
  <resume-signal>Type "approved" if the review notes are accurate, or describe the corrections / defects to record.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Write reviewed UI-REVIEW.md + consolidated FINDINGS.md</name>
  <files>.planning/phases/111-cut-lab-upgrade-regression-gate/111-UI-REVIEW.md, .planning/phases/111-cut-lab-upgrade-regression-gate/111-FINDINGS.md</files>
  <action>
    Finalize 111-UI-REVIEW.md: a table with one row per screenshot (Theme | Viewport |
    Usability | Understandability | Aesthetic hierarchy | Readability | PASS/FAIL | Notes),
    referencing the exact PNG filename in each row and incorporating the human corrections from
    the checkpoint. Then write 111-FINDINGS.md consolidating EVERY finding surfaced across
    Plans 01-04 (test-revealed defects, contrast breaches from Plan 03, UI-review defects from
    this plan). For each finding: ID, source (plan/gate), description, and disposition = FIXED
    (link the commit/handoff to Codex) OR DEFERRED (explicit rationale + where it is tracked,
    e.g. a backlog entry). No finding may be left silently open. If zero defects were found,
    state that explicitly with the evidence that supports it (gates green, review PASS).
  </action>
  <verify>
    <automated>grep -Eq "usability|Usability" .planning/phases/111-cut-lab-upgrade-regression-gate/111-UI-REVIEW.md && grep -Eq "PASS|FAIL" .planning/phases/111-cut-lab-upgrade-regression-gate/111-UI-REVIEW.md && grep -Eq "FIXED|DEFERRED|No defects" .planning/phases/111-cut-lab-upgrade-regression-gate/111-FINDINGS.md</automated>
  </verify>
  <done>UI-REVIEW.md has a reviewed row per screenshot across all four axes; FINDINGS.md dispositions every finding as fixed or deferred-with-rationale.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness -> app under test | Headless screenshot capture; admin-lock gated. No production surface changed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-111-04 | Information disclosure | committed PNG screenshots | accept | Screenshots show local test-deck data only (public sample pool), no secrets/PII. |
| T-111-SC | Tampering | npm/NuGet installs | mitigate | No new packages; reuse @playwright/test screenshot API. |
</threat_model>

<verification>
1. Six PNGs present under .planning/ui-design/cut-lab/screenshots/ (3 themes x 2 viewports).
2. Human checkpoint completed: review notes confirmed/corrected.
3. 111-UI-REVIEW.md scores all four axes per screenshot with PASS/FAIL + notes.
4. 111-FINDINGS.md dispositions every Phase-111 finding as FIXED or DEFERRED (with rationale).
Any UI defect that is a real code/CSS bug is handed to Codex to fix (Codex codes); this plan
records the disposition, it does not author production fixes.
</verification>

<success_criteria>
- CLUP-20 evidence complete: Classic/Nyx/Commander-Table desktop+mobile captured and reviewed
  on usability, understandability, aesthetic hierarchy, readability.
- Success Criterion 6 satisfied: all findings fixed or explicitly deferred with rationale.
- No new packages; LF + format gate clean.
</success_criteria>

<output>
Create `.planning/phases/111-cut-lab-upgrade-regression-gate/111-04-SUMMARY.md` when done.
</output>
