---
phase: 77-multi-axis-deck-score
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - DeckFlow.Core/Analysis/DeckStatClassifier.cs
  - DeckFlow.Core/Analysis/DeckStatAggregator.cs
  - DeckFlow.Core.Tests/DeckStatClassifierTests.cs
  - DeckFlow.Core.Tests/DeckStatAggregatorTests.cs
autonomous: true
requirements: [SCORE-02]
must_haves:
  truths:
    - "DeckStatClassifier exposes IsTutorCard, IsFastManaCard, IsRampOrDrawUnderThreeMv, IsCounterspellCard"
    - "DeckStatSummary carries Tutors, FastMana, RampDrawUnderThreeMv, Counters as { get; init; } fields"
    - "DeckStatAggregator.Compute tallies the four new signals quantity-weighted"
    - "Demonic Tutor classifies as a tutor; Cultivate/Rampant Growth do NOT"
    - "Mana Crypt (MV 0 mana artifact) classifies as fast mana; Sol Ring (MV 1) does NOT"
  artifacts:
    - path: "DeckFlow.Core/Analysis/DeckStatClassifier.cs"
      provides: "Four new oracle-text predicates"
      contains: "IsTutorCard"
    - path: "DeckFlow.Core/Analysis/DeckStatAggregator.cs"
      provides: "Four new DeckStatSummary fields + tally"
      contains: "FastMana"
    - path: "DeckFlow.Core.Tests/DeckStatClassifierTests.cs"
      provides: "True/False theory tests for the four predicates"
    - path: "DeckFlow.Core.Tests/DeckStatAggregatorTests.cs"
      provides: "Tally test for the four new fields"
  key_links:
    - from: "DeckStatAggregator.Compute"
      to: "DeckStatClassifier new predicates"
      via: "per-card classifier calls inside the tally foreach"
      pattern: "IsFastManaCard|IsTutorCard|IsCounterspellCard|IsRampOrDrawUnderThreeMv"
---

<objective>
Add the four missing deck signals that the multi-axis scorer consumes: tutor count,
fast-mana count, ramp/draw-under-MV-2 count, and counterspell count. All four are pure
Core oracle-text/type-line predicates plus additive `{ get; init; }` fields on
`DeckStatSummary` and additive tallies in `DeckStatAggregator.Compute`.

Purpose: SCORE-02 requires Speed/Consistency/Power/Control to derive from these signals;
they do not yet exist in `DeckStatClassifier`/`DeckStatSummary`.
Output: four new predicates, four new summary fields, extended unit tests (all GREEN).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/77-multi-axis-deck-score/77-RESEARCH.md
@.planning/phases/77-multi-axis-deck-score/77-PATTERNS.md
@CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add four DeckStatClassifier predicates with tests</name>
  <read_first>
    - DeckFlow.Core/Analysis/DeckStatClassifier.cs (existing IsRampCard/IsDrawCard/IsInteractionCard predicates — the analog: single-expression `|| oracle.Contains(..., OrdinalIgnoreCase)` chains)
    - DeckFlow.Core/Analysis/DeckStatAggregator.cs lines 180+ (public static int EstimateManaValue(string?) — the mana-value helper the fast-mana / under-MV predicates call)
    - DeckFlow.Core.Tests/DeckStatClassifierTests.cs (existing `[Theory]/[InlineData]` `_TrueCases`/`_FalseCases` section pattern)
    - 77-PATTERNS.md "DeckStatClassifier.cs (MODIFIED)" section (exact predicate bodies + Pitfall 6 land-tutor exclusion)
  </read_first>
  <behavior>
    - IsTutorCard("Search your library for a card, then shuffle.") == true
    - IsTutorCard("Search your library for a basic land card ...") == false (Rampant Growth)
    - IsTutorCard("Search your library for up to two basic land cards ...") == false (Cultivate)
    - IsFastManaCard("Artifact", "{T}: Add {C}{C}.", "") == true (Mana Crypt: MV 0, artifact, produces mana)
    - IsFastManaCard("Artifact", "{T}: Add {C}.", "{1}") == false (Sol Ring: estimated MV 1, not 0)
    - IsCounterspellCard("Counter target spell.") == true
    - IsCounterspellCard("Counter target activated or triggered ability.") == false (ability counter)
    - IsRampOrDrawUnderThreeMv("Sorcery", "Draw two cards.", "{1}{U}") == true (MV 2, draw)
    - IsRampOrDrawUnderThreeMv("Sorcery", "Draw four cards.", "{4}{U}") == false (MV 5)
  </behavior>
  <action>
    Add four `public static bool` predicates to DeckStatClassifier per 77-PATTERNS.md, using the
    existing single-expression `Contains(..., StringComparison.OrdinalIgnoreCase)` chaining style and
    XML doc on each:
    - IsTutorCard(string oracleText): contains "search your library for" AND NOT "basic land" AND NOT
      "land card" AND NOT "land onto the battlefield" (Pitfall 6 land-tutor exclusion).
    - IsFastManaCard(string typeLine, string oracleText, string manaCost):
      DeckStatAggregator.EstimateManaValue(manaCost) == 0 AND typeLine contains "Artifact" AND
      (oracleText contains "{T}: Add" OR "Add {").
    - IsRampOrDrawUnderThreeMv(string typeLine, string oracleText, string manaCost):
      EstimateManaValue(manaCost) <= 2 AND (IsRampCard(typeLine, oracleText) || IsDrawCard(oracleText)).
    - IsCounterspellCard(string oracleText): contains "counter target spell".
    Then add `{MethodName}_TrueCases`/`{MethodName}_FalseCases` `[Theory]/[InlineData]` sections to
    DeckStatClassifierTests.cs covering the behavior cases above (one section per predicate, with the
    existing `// ----` section-header comment style). Use chained-if / boolean-expression style — do NOT
    introduce switch expressions (re-indent carve-out). Touch only changed lines (changed-lines format gate).
  </action>
  <verify>
    <automated>dotnet.exe test DeckFlow.Core.Tests --filter "FullyQualifiedName~DeckStatClassifier" 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "public static bool IsTutorCard\|IsFastManaCard\|IsRampOrDrawUnderThreeMv\|IsCounterspellCard" DeckFlow.Core/Analysis/DeckStatClassifier.cs` returns 4
    - `dotnet.exe test DeckFlow.Core.Tests --filter "FullyQualifiedName~DeckStatClassifier"` exits 0
    - DeckStatClassifierTests.cs contains `IsTutorCard_FalseCases` asserting a "basic land" search is NOT a tutor
  </acceptance_criteria>
  <done>Four predicates exist with the documented exclusions; classifier test filter is GREEN.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add four DeckStatSummary fields and aggregator tallies with tests</name>
  <read_first>
    - DeckFlow.Core/Analysis/DeckStatAggregator.cs (DeckStatSummary record lines 28-39; tally `var` declarations lines 67-77; classifier-call foreach lines 128-156; return statement lines 161-172)
    - DeckFlow.Core.Tests/DeckStatAggregatorTests.cs (Card() factory line 10-11; Compute_TalliesRolesViaClassifierAndRespectsQuantity pattern)
    - 77-PATTERNS.md "DeckStatAggregator.cs (MODIFIED)" section (exact additive-field + tally + object-initializer return pattern)
    - CLAUDE.md carve-out: new fields are `{ get; init; }` — never `{ get; }`, never `required`
  </read_first>
  <behavior>
    - Compute over [tutor(qty1), fastMana(qty2), counterspell(qty1), draw-MV2(qty1)] yields
      Tutors==1, FastMana==2, Counters==1, RampDrawUnderThreeMv==1
    - Default-constructed / no-matching-cards Compute yields all four new fields == 0
  </behavior>
  <action>
    Add four trailing `{ get; init; }` members to the DeckStatSummary record AFTER the last positional
    param (ClosingPower) — NOT as new positional params (backward-compat): Tutors, FastMana,
    RampDrawUnderThreeMv, Counters (all `public int ... { get; init; }` with XML doc per
    77-PATTERNS.md). In Compute(): declare `var tutors = 0; var fastMana = 0; var rampDrawUnderThreeMv = 0;
    var counters = 0;`, add four classifier-call blocks inside the existing foreach (after the land-skip
    `continue`, quantity-weighted like the ramp/draw blocks — IsFastManaCard / IsRampOrDrawUnderThreeMv
    pass `card.ManaCost` as the third arg), and set the four fields via an object-initializer on the
    returned `new DeckStatSummary(...)`. Extend DeckStatAggregatorTests.cs with
    `Compute_TalliesNewSignalFields` using the Card() factory. Touch only changed lines.
  </action>
  <verify>
    <automated>dotnet.exe test DeckFlow.Core.Tests --filter "FullyQualifiedName~DeckStatAggregator" 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - DeckStatAggregator.cs contains `public int Tutors { get; init; }` and `public int FastMana { get; init; }` (init, not get-only, not required)
    - `grep -c "required " DeckFlow.Core/Analysis/DeckStatAggregator.cs` shows no `required` added to the four new fields
    - `dotnet.exe test DeckFlow.Core.Tests --filter "FullyQualifiedName~DeckStatAggregator"` exits 0
    - `dotnet.exe build DeckFlow.Core` reports 0 warnings 0 errors
  </acceptance_criteria>
  <done>Four init fields tallied quantity-weighted; aggregator test filter GREEN; Core builds clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| (none new) | Pure Core CPU transform over already-loaded deck data; no I/O, no DI, no network |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-77-01-01 | Information Disclosure | predicate oracle-text matching | accept | Reads only in-process card text already resolved upstream; produces integer counts; no PII, no external surface |
| T-77-SC | Tampering | package installs | mitigate | No new NuGet packages introduced; in-solution code only (REQUIREMENTS.md out-of-scope) |
</threat_model>

<verification>
- `dotnet.exe build DeckFlow.Core` clean (0/0)
- Core test filters for DeckStatClassifier and DeckStatAggregator GREEN
</verification>

<success_criteria>
Four signals exist as predicates + summary fields, quantity-weighted, with passing unit tests;
land-tutors excluded; MV-1 mana rocks excluded from fast mana.
</success_criteria>

<output>
Create `.planning/phases/77-multi-axis-deck-score/77-01-SUMMARY.md` when done
</output>
