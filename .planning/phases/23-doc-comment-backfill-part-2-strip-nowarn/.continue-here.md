---
context: phase
phase: 23-doc-comment-backfill-part-2-strip-nowarn
task: 0
total_tasks: 5
status: planned-not-executed
last_updated: 2026-06-03T00:56:54.006Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> Each constraint below was discovered through actual failure/experiment this planning session.

- [ ] CONSTRAINT: **Two suppressors, not one** — CS1591/1573/1587 are silenced in BOTH `DeckFlow.Web.csproj` (`<NoWarn>` line 40) AND `.editorconfig` (lines 94-96 `severity = none`, solution-wide). Research PROVED stripping only the csproj line is a no-op — the gate stays off. DOC-02 MUST flip both (csproj delete + editorconfig none→warning). `.editorconfig` is on the Do-Not-Modify list; user approved the edit this session.
- [ ] CONSTRAINT: **`-warnaserror:CS1591` alone false-greens** — it promotes only 1591; CS1573/1587 stay warnings and the build still exits 0. 23-05's gate MUST promote all three: `-warnaserror:CS1591,CS1573,CS1587` (+ log-grep fail-check). Already encoded in 23-05.
- [ ] CONSTRAINT: **CS1587 spans 3 controllers, not 1** — research wrongly claimed all 29 in `DeckController`. ~4 live in `CommanderController` (lines 28/40/93) + `DeckSyncApiController` (line ~17). These are RELOCATIONS (move `///` above `[Attribute]`), not add-summary. 23-03 owns all of them.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| csproj-only strip | Removing `<NoWarn>` but leaving `.editorconfig severity=none` → gate is a no-op, SC3 false-greens | blocking | 23-05 flips BOTH files; proof build promotes all 3 codes |
| 1591-only gate | `-warnaserror:CS1591` exits 0 while 1573/1587 survive | blocking | 23-05 uses `-warnaserror:CS1591,CS1573,CS1587` + grep `warning CS(1591\|1573\|1587)` |
| Attribute inlining on CS1587 fix | Collapsing `[Attribute]` onto member line while relocating `///` → CLAUDE.md formatting violation | blocking | 23-03 `<cs1587_fix_shape>`: move `///` above attributes, attributes byte-identical |
| Member-coverage blind spot | Documenting only top-level type, missing public ctors/properties/co-located enums → CS1591 survives | advisory | "document every public declaration" rule in all 4 backfill plans |
| Strip runs before backfill done | Wave 2 racing Wave 1 → build breaks (Pitfall 8) | blocking | 23-05 depends_on 23-01..04 + human checkpoint |

<current_state>
Phase 23 fully PLANNED, plan-checked, Codex-reviewed, hardened, and COMMITTED (28cca16) on branch v1.4. Nothing executed yet. 0/5 plans run. This is exec #7 / LAST phase in v1.4 order; all deps (through Phase 22) complete.
</current_state>

<completed_work>

- Research → 23-RESEARCH.md (compiler-derived 475-site inventory; two-suppressor finding)
- Plan → 5 PLANs / 2 waves
- plan-checker → caught 1 blocker (CS1587 distribution), fixed → PASS
- Codex peer review → 23-REVIEWS.md (2 HIGH + 4 lower), all folded in via replan
- Commit 28cca16 (planning artifacts only; unrelated untracked files left alone)
</completed_work>

<remaining_work>

- Execute Wave 1 (parallel): 23-01 Models (144) · 23-02 Models/Api/Admin (183) · 23-03 Controllers+CS1587 (96) · 23-04 Services/Infra+CS1573 (52)
- Execute Wave 2 (serial, LAST): 23-05 two-file gate flip + all-three-code proof build
- Verify SC1-4; commit per logical change; test suite Failed:0
</remaining_work>

<decisions_made>

- LD-1: fix all three warning classes (overrides Phase 17 partial-param policy)
- LD-2: two-file flip (csproj delete + .editorconfig none→warning); user approved the Do-Not-Modify edit
- LD-3: verify-once gate; no permanent TreatWarningsAsErrors, no new CI file
- LD-4: full strip; Razor partials non-issue (.NET 10 emits 0 .cshtml.g.cs)
- 23-05 gate promotes all three codes (Codex HIGH-1)
</decisions_made>

<blockers>
- None. (.editorconfig edit at 23-05 is pre-approved; 23-05 has a human-verify checkpoint before the flip.)
</blockers>

## Required Reading (in order)
1. `23-REVIEWS.md` — Codex findings folded into the plans; the 2 HIGH explain why the gate is shaped as it is
2. `23-RESEARCH.md` — the 475-site inventory + two-suppressor experiment (the WHY behind LD-2)
3. `23-05-PLAN.md` — the gate-flip mechanics; re-derive the live inventory at execute time (HEAD may have moved since e9471fb)
4. `./CLAUDE.md` — formatting pins (no attribute inline, no raw-string re-indent, preserve {get;init;}, touch-only); Codex-codes/Claude-reviews delegation

## Infrastructure State
- Branch: v1.4. HEAD 28cca16. No dev server needed (doc-only phase, no UAT).
- Build: `/mnt/c/Program Files/dotnet/dotnet.exe` (VSTest unreliable in WSL; rely on build-clean gate).
- Unrelated untracked files present (.claude/, 19-PATTERNS, 27-RESEARCH, todos) — NOT part of this phase; do not commit.

<context>
Mechanical XML-doc backfill. Zero runtime change. The ONLY real risk is the compiler gate being genuinely real (both suppressors flipped + all three codes promoted) and touch-only discipline. Per CLAUDE.md: Codex implements, Claude reviews. Inventory snapshot was at e9471fb; if commits landed since, re-run the suppressors-off build at execute time to refresh the per-file distribution (23-05's probe build catches drift either way).
</context>

<next_action>
Start with: /gsd-execute-phase 23  (Codex implements Wave 1 four plans in parallel, Claude reviews; then Wave 2 strip lands LAST). Do NOT flip the doc suppressors during Wave 1 — 23-03 uses an awk adjacency scan, and 23-05 is the authoritative all-three-code gate.
</next_action>
