---
phase: 96-stated-rules-distiller
plan: 08
type: execute
wave: 5
depends_on: ["96-05", "96-06", "96-07"]
files_modified:
  - DeckFlow.Core/Orchestration/ContentKbOrchestrator.cs
  - DeckFlow.Core.Tests/Orchestration/DistillResultClipCountTests.cs
  - DeckFlow.Core.Tests/Orchestration/FakeOrchestratorStores.cs
autonomous: true
requirements: [CS-11, CS-11a, CS-11b, CS-11c]
must_haves:
  truths:
    - "A subscription-provider distill computes content_type, extracts stated rules, persists content_stated_rules rows, and emits both new frontmatter sections — all in one pass (the re-distill mechanism, D-05)"
    - "content_type is always set; stated-rules extraction is skipped (empty) when the video has no publish date (fail-closed provenance)"
    - "The stated-rules stage runs only on the subscription/CLI provider, reusing the existing isSubscriptionProvider gate — no new cost path on OpenAI"
  artifacts:
    - path: "DeckFlow.Core/Orchestration/ContentKbOrchestrator.cs"
      provides: "DistillVideoAsync wiring: content_type + StatedRulesExtractor + content_stated_rules inserts + metadata"
      contains: "StatedRulesExtractor"
  key_links:
    - from: "DistillVideoAsync"
      to: "StatedRulesExtractor / ContentTypeHeuristic / InsertStatedRuleAsync"
      via: "sequenced after the tags stage, before artifact write; video.PublishedUtc stamps provenance"
      pattern: "InsertStatedRuleAsync"
---

<objective>
Wire the stated-rules stage into the single production distill call site (`ContentKbOrchestrator.DistillVideoAsync`), completing the re-distill-capable mechanism (D-05, CS-11a/CS-11b). After the tags stage: compute `content_type` (ContentTypeHeuristic, no LLM), run `StatedRulesExtractor` over the transcript stamped with `video.PublishedUtc`, persist each rule via `InsertStatedRuleAsync`, and populate the new `ContentArtifactMetadata.ContentType`/`StatedRules` so `ToText` emits both frontmatter sections. The stage inherits the existing `isSubscriptionProvider` gate (the 4 stage methods are CLI-only). No mass backfill runs here (D-05); the mechanism exists and is exercised by a distill test.

Purpose: turn the substrate (schemas, extractor, store, writer, grounder) into a working per-video distill that P97 can consume once an operator runs a Snail re-distill (D-05-DEP).
Output: orchestrator wiring + an orchestrator-level distill test proving content_type/stated_rules emission + row persistence.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/96-stated-rules-distiller/96-CONTEXT.md
@.planning/phases/96-stated-rules-distiller/96-RESEARCH.md
@.planning/phases/96-stated-rules-distiller/96-PATTERNS.md
@.planning/phases/96-stated-rules-distiller/96-05-SUMMARY.md
@.planning/phases/96-stated-rules-distiller/96-06-SUMMARY.md
@.planning/phases/96-stated-rules-distiller/96-07-SUMMARY.md

<interfaces>
<!-- Wiring contract. -->

ContentKbOrchestrator ctor: add a NEW TRAILING optional param `ICardNameGrounder? cardGrounder = null` (after the existing optional `logger`), stored in a field. Factory (CLI) and AddContentKbOrchestrator (Studio) need NO change — .NET DI + the factory honor the default (null => grounding skipped). A host that registers ICardNameGrounder (e.g. Web via 96-06) gets live grounding injected automatically.

DistillVideoAsync (after the tags stage ~line 1278-1288, before ValidateSummary ~1290):
  contentType = ContentTypeHeuristic.Classify(archetypeTags, cardCategoryTags, clips.Clips)   // always set
  statedRules = (video.PublishedUtc is DateTimeOffset pub)
      ? await new StatedRulesExtractor(_distiller, _cardGrounder).ExtractAsync(transcript.Body, pub, ct)
      : Array.Empty<StatedRuleCandidate>()   // fail-closed: no provenance => no stated rules (D-04)
  After ValidateSummary/ValidateClips + existing InsertSummary/Insert clips/tags loops:
      var statedRuleSortOrder = 0;   // MEDIUM-1: SEPARATE counter — do NOT reuse the clip `sortOrder`, so stated-rule sort starts at 0 independent of clip count
      foreach rule in statedRules: await _videoStore.InsertStatedRuleAsync(video.Id, rule, statedRuleSortOrder++, ct)
  ContentArtifactMetadata initializer (~1324): add ContentType = contentType, StatedRules = statedRules
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire content_type + StatedRulesExtractor + inserts into DistillVideoAsync</name>
  <read_first>
    - DeckFlow.Core/Orchestration/ContentKbOrchestrator.cs (ctor ~50-95; DistillVideoAsync ~1167-1400: tags stage ~1263-1288, ValidateSummary/insert loops ~1290-1322 — note the clip loop advances its own `sortOrder`, ContentArtifactMetadata initializer ~1324-1339, video.PublishedUtc ~1366)
    - DeckFlow.Core/Knowledge/StatedRulesExtraction/StatedRulesExtractor.cs (96-07 — ExtractAsync signature)
    - DeckFlow.Core/Knowledge/StatedRulesExtraction/ContentTypeHeuristic.cs (96-02 — Classify signature + literals)
    - DeckFlow.Core/Content/IContentVideoStore.cs (96-05 — InsertStatedRuleAsync)
    - DeckFlow.Core/Knowledge/ContentArtifactSpec.cs (96-05 — ContentArtifactMetadata.ContentType/StatedRules)
    - DeckFlow.Core/Orchestration/ContentKbOrchestratorFactory.cs + ServiceCollectionExtensions.cs (confirm the trailing-optional ctor param needs no change to either)
    - .planning/phases/96-stated-rules-distiller/96-PATTERNS.md (section "ContentKbOrchestrator.DistillVideoAsync" — insert-after-validate + metadata-additive idioms)
  </read_first>
  <action>
    Add `ICardNameGrounder? cardGrounder = null` as the last ctor param of `ContentKbOrchestrator`; store `_cardGrounder = cardGrounder` (no null guard — it is optional). In `DistillVideoAsync`, after the tags stage and after `archetypeTags`/`cardCategoryTags` are computed: set `var contentType = ContentTypeHeuristic.Classify(archetypeTags, cardCategoryTags, clips.Clips);` and compute `statedRules` per the <interfaces> fail-closed rule (skip extraction when `video.PublishedUtc` is null). After the existing InsertSummary/clip/tag loops, introduce a SEPARATE `var statedRuleSortOrder = 0;` counter (MEDIUM-1 — do NOT reuse the clip `sortOrder`, which is already advanced by the clips loop; the stated-rule sort must start at 0) and add a `foreach` inserting each rule via `_videoStore.InsertStatedRuleAsync(video.Id, rule, statedRuleSortOrder++, cancellationToken)`. Add `ContentType = contentType` and `StatedRules = statedRules` to the existing `ContentArtifactMetadata` initializer — additive only, do not reorder existing members. The stage runs inside the existing subscription-gated distill flow (the 4 extractor stage methods throw NotSupportedException off-subscription, and DistillAsync already refuses non-dry-run distills off-subscription) — do NOT add a new provider branch. Add a `// Why:` comment on the PublishedUtc-null skip (fail-closed provenance, D-04).
  </action>
  <verify>
    <automated>dotnet build DeckFlow.Core/DeckFlow.Core.csproj</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "StatedRulesExtractor\|ContentTypeHeuristic\|InsertStatedRuleAsync" DeckFlow.Core/Orchestration/ContentKbOrchestrator.cs` >= 3.
    - MEDIUM-1: the stated-rule insert loop uses a dedicated `statedRuleSortOrder` counter initialized to 0, NOT the clip `sortOrder`.
    - The ContentArtifactMetadata initializer sets ContentType and StatedRules; existing members unchanged/unreordered.
    - `ContentKbOrchestratorFactory.cs` and `ServiceCollectionExtensions.cs` require no edit (trailing-optional ctor param); confirmed by clean solution build.
    - `dotnet build DeckFlow.sln` clean, 0 new warnings (Core + Web + CLI + Studio + tests).
  </acceptance_criteria>
  <done>DistillVideoAsync computes content_type, extracts+persists stated rules with provenance, and populates the artifact metadata; all hosts build unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Orchestrator distill test — content_type + stated_rules emitted + rows persisted</name>
  <read_first>
    - DeckFlow.Core.Tests/Orchestration/DistillResultClipCountTests.cs (existing DistillVideoAsync-through-fakes test to mirror/extend)
    - DeckFlow.Core.Tests/Orchestration/FakeOrchestratorStores.cs (fake IContentVideoStore — add stated-rule capture; and the fake ILlmDistillationService — implement the 4 new stage methods to return scripted results)
    - DeckFlow.Core.Tests/RunDistillAsyncTests.cs (how the fake distiller + subscription-provider distill is driven end-to-end)
    - DeckFlow.Core/Knowledge/StatedRulesExtraction/StatedRulesExtractor.cs (96-07)
    - .planning/phases/96-stated-rules-distiller/96-RESEARCH.md (D-05 — mechanism exists this phase, no mass backfill; D-05-DEP — P97 needs a Snail re-distill)
  </read_first>
  <action>
    Extend the fake ILlmDistillationService used by the orchestration tests to implement `SelectStatedClaimsAsync`/`DisambiguateStatedClaimsAsync`/`DecomposeStatedClaimsAsync`/`ReduceStatedRulesAsync` returning scripted rules (e.g., one land-count band). Extend `FakeOrchestratorStores` (fake IContentVideoStore) to record InsertStatedRuleAsync calls (this fake OVERRIDES the default method — expected, unlike the untouched read-only fakes elsewhere). Add a test (in DistillResultClipCountTests or a new sibling) that runs a subscription-provider distill over a fixture video WITH a PublishedUtc and asserts: (a) content_stated_rules rows were inserted (captured by the fake), (b) the written artifact frontmatter contains `content_type:` and `stated_rules:`, (c) each persisted rule's VideoDateUtc equals the video's PublishedUtc. Add a second case with a null PublishedUtc asserting zero stated-rule rows but a still-present `content_type:` (fail-closed provenance). Provide a null/fake ICardNameGrounder so no live Scryfall call occurs.
  </action>
  <verify>
    <automated>dotnet.exe test DeckFlow.Core.Tests --filter FullyQualifiedName~DistillResultClipCountTests</automated>
  </verify>
  <acceptance_criteria>
    - Test asserts content_stated_rules rows inserted on a distill with PublishedUtc, and the artifact contains both `content_type:` and `stated_rules:`.
    - Test asserts each persisted rule's VideoDateUtc == the video PublishedUtc (provenance, CS-11c).
    - Null-PublishedUtc case asserts zero stated-rule rows + content_type still emitted.
    - `dotnet.exe test DeckFlow.Core.Tests` full run green (both Core and Web test projects green in the wave merge).
    - `dotnet build DeckFlow.sln` clean, 0 new warnings.
  </acceptance_criteria>
  <done>An orchestrator-level test proves the full re-distill mechanism emits both frontmatter sections and persists provenance-stamped rows; fail-closed provenance verified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| transcript → distill pipeline → artifact + DB | Untrusted transcript drives extraction, persistence, and rendering in one call |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-96-01 | Tampering | injected rules reaching artifact/DB | mitigate | Extractor's ValidateStatedRules (96-07) gates before return; store inserts are parameterized (96-05); writer uses Quote()/JsonSerializer (96-05) |
| T-96-04 | DoS (cost) | token-heavy multi-pass on every distill | mitigate | Reuses the existing isSubscriptionProvider gate + LLM monthly-cap checks already wrapping the summary/clips/tags stages; no new metered path |
| T-96-01 | Repudiation | rule with no provenance | mitigate | PublishedUtc-null videos skip stated-rules extraction (fail closed, D-04); ValidateStatedRules rejects default VideoDateUtc |
| T-96-SC | Tampering | package installs | accept | No new packages — legitimacy gate N/A |
</threat_model>

<verification>
- `dotnet build DeckFlow.sln` clean (all projects).
- `dotnet.exe test DeckFlow.Core.Tests` + `dotnet.exe test DeckFlow.Web.Tests` green (full phase suite).
- Byte-stable artifact gate intact (no pre-existing artifact churns).
</verification>

<success_criteria>
The single production distill path emits content_type + stated_rules and persists content_stated_rules rows with provenance on subscription distills — the re-distill mechanism exists and is exercised (D-05), leaving P97 to run a Snail re-distill for real stated data (D-05-DEP).
</success_criteria>

<output>
Create `.planning/phases/96-stated-rules-distiller/96-08-SUMMARY.md` when done.
</output>
</output>
