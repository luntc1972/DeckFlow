---
phase: 112-cycle-17-code-port
plan: 05
type: execute
wave: 5
depends_on: ["112-04"]
files_modified:
  - DeckFlow.Web/Extensions/CreatorStyleServiceCollectionExtensions.cs
  - DeckFlow.Web/Program.cs
autonomous: true
requirements: [PORT-02]

must_haves:
  truths:
    - "Every creator-style service is registered in ONE new dedicated extension, AddDeckFlowCreatorStyle(), invoked from a single added Program.cs line — not spread across the four shared extension files Cut Lab has been rewriting (D-10)."
    - "The named archidekt-owner HttpClient is registered INSIDE the new extension, never by editing the shared HttpClientServiceCollectionExtensions.cs (D-17)."
    - "The seed loader is wired AND invoked at startup: both IContentKbSeedLoader and ICreatorStyleSeedLoader run as a fork-join, with faults from BOTH logged before rethrow (D-11, D-19)."
    - "Program.cs receives exactly two edits — the AddDeckFlowCreatorStyle call and the seed fork-join rewrite with its two static helpers — and nothing else (D-19)."
    - "dotnet build DeckFlow.sln completes with 0 errors and no warning ID absent from the recorded baseline."
  artifacts:
    - path: "DeckFlow.Web/Extensions/CreatorStyleServiceCollectionExtensions.cs"
      provides: "AddDeckFlowCreatorStyle() — the single registration site for all creator-style services"
      exports: ["AddDeckFlowCreatorStyle"]
      min_lines: 40
  key_links:
    - from: "DeckFlow.Web/Program.cs"
      to: "DeckFlow.Web/Extensions/CreatorStyleServiceCollectionExtensions.cs"
      via: "single AddDeckFlowCreatorStyle call after AddDeckFlowScryfallServices"
      pattern: "AddDeckFlowCreatorStyle"
    - from: "DeckFlow.Web/Program.cs"
      to: "DeckFlow.Web/Services/Content/CreatorStyleSeedLoader.cs"
      via: "ICreatorStyleSeedLoader.LoadIfPresentAsync in the startup fork-join"
      pattern: "ICreatorStyleSeedLoader"
---

<objective>
Write the dedicated creator-style DI extension and apply the two `Program.cs` edits, making the whole solution build again.

Purpose: D-10 exists because Cycle 17 spread these registrations across `Program.cs` plus three shared extension files that Cut Lab has been rewriting across Cycles 18 and 19. Concentrating them in one new file leaves a single added line in the most-contested file instead of four conflict surfaces. D-19 adds the startup fork-join so the seed loader is genuinely invoked (D-11's "wired AND invoked") and so faults from BOTH seed sources are logged rather than the first failure aborting silently.

Output: `dotnet build DeckFlow.sln` green again — including `DeckFlow.Web.Tests`, which could not build after plan 04 because `ProgramStartupTests.cs` asserts on the helpers this plan adds. No commit; plan 06 owns Commit 2.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/112-cycle-17-code-port/112-RESEARCH.md
@.planning/phases/112-cycle-17-code-port/112-CONTEXT.md
@.planning/phases/112-cycle-17-code-port/112-BASELINE.md
@CLAUDE.md
</context>

<environment>
- Branch: `gsd/cycle20-personal-tools`. dotnet is `"/mnt/c/Program Files/dotnet/dotnet.exe"`. Do NOT set `MTG_DATA_DIR`.
- `plan/cycle-17-creator-style` is READ-ONLY and no checkout from it happens in this plan. `CreatorStyleServiceCollectionExtensions.cs` is a NEW file written fresh; `Program.cs` is hunked by hand from its current content.
- `.gitattributes` enforces LF line endings.
- No new `PackageReference` anywhere in this phase.
</environment>

<hands_off_files>
## Must NOT be modified by this plan (D-10, D-12/D-18, D-02/D-16)
- DeckFlow.Web/Extensions/HttpClientServiceCollectionExtensions.cs
- DeckFlow.Web/Extensions/PacketServiceCollectionExtensions.cs
- DeckFlow.Web/Extensions/ScryfallServiceCollectionExtensions.cs
- DeckFlow.Web/Services/FeatureFlags/FeatureFlagCatalog.cs
- DeckFlow.Web/Services/FeatureFlags/FeatureFlagStore.cs
- any ToolRegistry file
- DeckFlow.Web/Models/DeckPageTab.cs
- any SeoPaths or sitemap file
- DeckFlow.Web/Services/CardLookupService.cs
- DeckFlow.Web/Services/Scryfall/ScryfallReferenceResolver.cs
No route, no nav tile, no feature flag, and no help page is added for creator-style at 112 — the admin surface is Phase 114.
</hands_off_files>

<tasks>

<task type="auto">
  <name>Task 1: Write the AddDeckFlowCreatorStyle DI extension (D-10, D-17)</name>
  <files>DeckFlow.Web/Extensions/CreatorStyleServiceCollectionExtensions.cs</files>
  <read_first>
    - `DeckFlow.Web/Extensions/CutLabServiceCollectionExtensions.cs` and `DeckFlow.Web/Extensions/ScryfallServiceCollectionExtensions.cs` — the house shape for a DeckFlow DI extension: public static class, single `IServiceCollection` extension method returning `services`, `ArgumentNullException.ThrowIfNull` guards, xmldoc on the public method. Mirror this shape; do not invent a new one.
    - `.planning/phases/112-cycle-17-code-port/112-RESEARCH.md` §"New file: DeckFlow.Web/Extensions/CreatorStyleServiceCollectionExtensions.cs" — the build-verified registration list and lifetimes. RESEARCH notes its skeleton has minimal comments; the shipped file must carry xmldoc per repo convention.
    - `.planning/phases/112-cycle-17-code-port/112-CONTEXT.md` D-10 and D-17.
    - `DeckFlow.Web/Services/Persistence/DeckFlowDatabaseConnectionFactory.cs` (as extended in plan 04) — confirm the exact names `CreateCreatorDeckCacheConnection`, `CreateCategoryKnowledgeConnection`, `CreateLocalContentKbConnection`.
    - The ported service constructors, to confirm the interface/implementation pairs and which take `IWebHostEnvironment`-derived connections: `DeckFlow.Web/Services/CreatorStyle/*.cs`, `DeckFlow.Web/Services/Content/CreatorStyleSeedLoader.cs`, `DeckFlow.Web/Services/Scryfall/ScryfallCardNameGrounder.cs`, `DeckFlow.Web/Services/Scryfall/CardGroundingGuard.cs`.
    - `DeckFlow.Web/Services/Persistence/CategoryKnowledgeStore.cs` — it constructs `CategoryKnowledgeRepository` directly today, so the container currently has NO registration for that type. Confirm this before registering it, so the new singleton cannot shadow or conflict with an existing one.
    - `CLAUDE.md` §Anti-Patterns — no `new HttpClient()`; use a named `IHttpClientFactory` client.
  </read_first>
  <action>
    Create `DeckFlow.Web/Extensions/CreatorStyleServiceCollectionExtensions.cs` in namespace `DeckFlow.Web.Extensions` (file-scoped), containing a public static class `CreatorStyleServiceCollectionExtensions` with one public extension method `AddDeckFlowCreatorStyle` taking `this IServiceCollection services` and an `IWebHostEnvironment environment`, guarding both with `ArgumentNullException.ThrowIfNull`, and returning `services`. Give the method an xmldoc summary explaining that it is the single registration site for the admin-only creator-style engine (D-10), plus `param` and `returns` tags per repo convention.

    Register, in this order:
    1. A named `IHttpClientFactory` client `archidekt-owner` with base address `https://archidekt.com/`, a `DeckFlow/1.0` user-agent, and a JSON-preferring Accept header. This lives HERE, not in the shared `HttpClientServiceCollectionExtensions.cs` (D-17). Do not call `new HttpClient()`.
    2. Singletons: `ICardNameGrounder` to `ScryfallCardNameGrounder`; `ICardGroundingGuard` to `CardGroundingGuard`.
    3. Singleton store factories, each constructing the store with a connection from `DeckFlowDatabaseConnectionFactory`: `ICreatorDeckCacheStore` to `CreatorDeckCacheStore` over `CreateCreatorDeckCacheConnection(environment)`; `ICreatorProfileSourceStore` to `CreatorProfileSourceStore` over the same connection factory method; `CategoryKnowledgeRepository` over `CreateCategoryKnowledgeConnection(environment)`; `ICreatorStyleProfileStore` to `CreatorStyleProfileStore` over `CreateLocalContentKbConnection(environment)`.
    4. Singleton `CreatorWhitelistPoolBuilder`.
    5. Singleton `ICreatorStyleSeedLoader` to `CreatorStyleSeedLoader`.
    6. Singleton `IArchidektOwnerClient` to `ArchidektOwnerClient`.
    7. Scoped: `CreatorProfileDeckCrawler`; `CreatorDeckCategoryResolver`; `MeasuredStyleProfileBuilder`; `ISubmittedDeckStatsBuilder` to `SubmittedDeckStatsBuilder`; `ICreatorStylePacketService` to `CreatorStylePacketService`.

    Add a `// Why:` comment on the `ICreatorStyleProfileStore` registration recording that creator-style profiles bind to the local-only content-kb database because production never crawls, it only reads git-shipped seeds.

    Do NOT register any feature flag, route, nav tile, or help topic. Do NOT touch any file in this plan's `<hands_off_files>` list.

    LF line endings; honor the five `.editorconfig` carve-outs.

    Do not stage or commit.
  </action>
  <verify>
    <automated>"/mnt/c/Program Files/dotnet/dotnet.exe" build DeckFlow.Web/DeckFlow.Web.csproj</automated>
  </verify>
  <acceptance_criteria>
    - `DeckFlow.Web/Extensions/CreatorStyleServiceCollectionExtensions.cs` exists, declares `namespace DeckFlow.Web.Extensions;` file-scoped, and exports a public static method `AddDeckFlowCreatorStyle`.
    - `grep -c 'archidekt-owner' DeckFlow.Web/Extensions/CreatorStyleServiceCollectionExtensions.cs` returns at least 1.
    - The file registers all fifteen entries listed in the action: `grep -c 'ICardNameGrounder\|ICardGroundingGuard\|ICreatorDeckCacheStore\|ICreatorProfileSourceStore\|CategoryKnowledgeRepository\|ICreatorStyleProfileStore\|CreatorWhitelistPoolBuilder\|ICreatorStyleSeedLoader\|IArchidektOwnerClient\|CreatorProfileDeckCrawler\|CreatorDeckCategoryResolver\|MeasuredStyleProfileBuilder\|ISubmittedDeckStatsBuilder\|ICreatorStylePacketService'` finds every one of those identifiers at least once.
    - `grep -c 'new HttpClient(' DeckFlow.Web/Extensions/CreatorStyleServiceCollectionExtensions.cs` returns 0.
    - `git diff --name-only HEAD -- DeckFlow.Web/Extensions/HttpClientServiceCollectionExtensions.cs DeckFlow.Web/Extensions/PacketServiceCollectionExtensions.cs DeckFlow.Web/Extensions/ScryfallServiceCollectionExtensions.cs` returns zero lines.
    - `grep -Ec 'FeatureFlag|ToolRegistry|SeoPaths|MapControllerRoute|tool\.creator-style' DeckFlow.Web/Extensions/CreatorStyleServiceCollectionExtensions.cs` returns 0.
    - `"/mnt/c/Program Files/dotnet/dotnet.exe" build DeckFlow.Web/DeckFlow.Web.csproj` exits 0 with 0 errors.
    - `grep -c $'\r' DeckFlow.Web/Extensions/CreatorStyleServiceCollectionExtensions.cs` returns 0.
  </acceptance_criteria>
  <done>All creator-style services are registered in one new dedicated extension, with the archidekt-owner client inside it and the shared extensions untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Apply the two Program.cs edits — DI call and seed fork-join (D-19)</name>
  <files>DeckFlow.Web/Program.cs</files>
  <read_first>
    - `DeckFlow.Web/Program.cs` in the working tree — read the registration block around the existing `builder.Services.AddDeckFlowScryfallServices();` line (near line 90), the startup sequence around `IContentSiteIndexStore.EnsureSchemaAsync()` followed by `GetRequiredService<IContentKbSeedLoader>().LoadIfPresentAsync()` (near lines 278-279), and the existing static helper `DeriveAdminPartitionKey` (near line 382) whose neighbourhood the new helpers join.
    - `.planning/phases/112-cycle-17-code-port/112-RESEARCH.md` §"Critical Findings" #4 — the exact before/after shape of the seed-load block and the two helper names.
    - `.planning/phases/112-cycle-17-code-port/112-CONTEXT.md` D-19 and D-11.
    - `DeckFlow.Web.Tests/ProgramStartupTests.cs` (ported in plan 04) — it directly asserts on `Program.AwaitStartupSeedTasksAsync`'s dual-fault-logging behavior. Its assertions define the required accessibility, signature, and logging behavior of the helpers; read it BEFORE writing them and match it exactly rather than guessing.
    - `CLAUDE.md` §Conventions → Logging — structured templates with named placeholders, never string interpolation.
  </read_first>
  <action>
    Make exactly two edits to `DeckFlow.Web/Program.cs`. Nothing else in this file may change — it is the most-contested file in the repo and the diff must stay as small as physically possible.

    Edit (a) — DI registration. Add one line, `builder.Services.AddDeckFlowCreatorStyle(builder.Environment);`, directly after the existing `builder.Services.AddDeckFlowScryfallServices();` line. Add the `DeckFlow.Web.Extensions` using directive only if it is not already present.

    Edit (b) — startup seed fork-join. Replace the two sequential awaits with a fork-join: keep `await` on the `IContentSiteIndexStore.EnsureSchemaAsync()` call unchanged (schema must exist before seeds load), then start BOTH `IContentKbSeedLoader.LoadIfPresentAsync()` and `ICreatorStyleSeedLoader.LoadIfPresentAsync()` as tasks without awaiting them individually, and await them together through a new helper `AwaitStartupSeedTasksAsync` that takes both tasks and the app logger. Keep the existing "Content site-index schema ensured and seed load completed during startup." log line after the join, unchanged. Do not reorder or remove any other startup step, in particular the body-hash backfill that follows.

    Add the two static helper methods next to `DeriveAdminPartitionKey`: `AwaitStartupSeedTasksAsync`, which awaits both tasks, logs a fault from EACH task rather than letting the first failure abort silently, and rethrows after logging; and `LogFaultedSeedTask`, which emits the per-task fault. Their accessibility (internal vs private) and exact signatures are dictated by `ProgramStartupTests.cs` — match what that test calls. Use structured log templates with named placeholders, never string interpolation.

    Do not add a route, a feature flag, a nav tile, an admin section, or a help topic. Do not touch any file in this plan's `<hands_off_files>` list.

    After both edits, run `"/mnt/c/Program Files/dotnet/dotnet.exe" build DeckFlow.sln` and confirm 0 errors across all four projects — `DeckFlow.Web.Tests` should now build again.

    Do not stage or commit. Plan 06 owns Commit 2.
  </action>
  <verify>
    <automated>"/mnt/c/Program Files/dotnet/dotnet.exe" build DeckFlow.sln</automated>
  </verify>
  <acceptance_criteria>
    - `"/mnt/c/Program Files/dotnet/dotnet.exe" build DeckFlow.sln` exits 0 with 0 errors across `DeckFlow.Core`, `DeckFlow.Web`, `DeckFlow.CLI`, `DeckFlow.Core.Tests`, and `DeckFlow.Web.Tests`, and the warning count is at or below the number in `112-BASELINE.md` with no new warning ID.
    - `grep -c 'AddDeckFlowCreatorStyle(builder.Environment)' DeckFlow.Web/Program.cs` returns 1, and it appears after the `AddDeckFlowScryfallServices();` line.
    - `grep -c 'ICreatorStyleSeedLoader' DeckFlow.Web/Program.cs` returns at least 1 and `grep -c 'IContentKbSeedLoader' DeckFlow.Web/Program.cs` still returns at least 1 — both seed loaders run.
    - `grep -c 'AwaitStartupSeedTasksAsync' DeckFlow.Web/Program.cs` returns at least 2 (call site plus declaration) and `grep -c 'LogFaultedSeedTask' DeckFlow.Web/Program.cs` returns at least 2.
    - `grep -c 'Content site-index schema ensured and seed load completed during startup.' DeckFlow.Web/Program.cs` returns 1 — the existing log line survives verbatim.
    - The `Program.cs` diff is bounded: `git diff HEAD -- DeckFlow.Web/Program.cs` piped to `grep -c '^-[^-]'` returns no more than 2 (only the two replaced sequential-await lines), and the total added-line count is under 50.
    - `git diff HEAD -- DeckFlow.Web/Program.cs` piped to `grep -Ec 'MapControllerRoute|FeatureFlag|ToolRegistry|SeoPaths|tool\.creator-style'` returns 0 — no route, flag, registry, or SEO change arrived.
    - `dotnet test` filtered to `FullyQualifiedName~ProgramStartup` on `DeckFlow.Web.Tests` exits 0 with 0 failures.
    - `grep -c $'\r' DeckFlow.Web/Program.cs` returns 0.
  </acceptance_criteria>
  <done>Program.cs carries exactly the two D-19 edits, the whole solution builds, and the ported startup test passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| composition root → DI graph | A missing or wrong-lifetime registration surfaces only at first runtime resolution, not at compile time |
| startup seed files → local database | Git-shipped JSON hydrates local stores during host boot |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-112-12 | Denial of Service | A faulted seed load aborting startup with only the FIRST failure logged, leaving the second cause invisible during incident triage | mitigate | D-19's fork-join with `AwaitStartupSeedTasksAsync` logs a fault from EACH task before rethrowing; `ProgramStartupTests.cs` asserts this dual-fault behavior |
| T-112-13 | Elevation of Privilege | An accidental route, nav tile, or feature flag exposing the admin-only creator-style engine publicly at 112 | mitigate | Explicit prohibition in both tasks plus an acceptance-criteria grep asserting the `Program.cs` diff adds no `MapControllerRoute`, `FeatureFlag`, `ToolRegistry`, or `SeoPaths` reference. The admin surface and its BasicAuth gate are Phase 114. |
| T-112-14 | Tampering | Registration sprawl across shared DI extension files creating conflict surfaces with concurrent Cut Lab work | mitigate | D-10: one new file plus one added `Program.cs` line; acceptance criteria assert zero diff on the three shared extension files |
| T-112-SC | Tampering | Package-manager installs (NuGet) | mitigate | Zero packages added; no csproj is modified by this plan, and plan 06's commit gate greps the whole branch diff for `PackageReference` |

## Not applicable at 112 (per RESEARCH §Security Domain)
- ASVS V2 Authentication and V4 Access Control: no creator-style route is wired at 112.
- ASVS V6 Cryptography: nothing here handles secrets or crypto.
</threat_model>

<verification>
- `"/mnt/c/Program Files/dotnet/dotnet.exe" build DeckFlow.sln` — 0 errors, warnings at or below baseline.
- `dotnet test DeckFlow.Web.Tests --filter "FullyQualifiedName~ProgramStartup"` — 0 failures.
- `git diff --name-only HEAD -- DeckFlow.Web/Extensions/HttpClientServiceCollectionExtensions.cs DeckFlow.Web/Extensions/PacketServiceCollectionExtensions.cs DeckFlow.Web/Extensions/ScryfallServiceCollectionExtensions.cs` — zero lines.
- `Program.cs` diff has at most 2 deleted lines and under 50 added lines.
- Nothing staged, nothing committed.
</verification>

<success_criteria>
- `AddDeckFlowCreatorStyle()` exists in one new extension file and registers every creator-style service plus the `archidekt-owner` named client (D-10, D-17).
- `Program.cs` carries exactly two edits: the DI call and the seed fork-join with its two helpers (D-19).
- Both seed loaders are invoked at startup, satisfying D-11's "wired AND invoked".
- The full solution builds with 0 errors and no new warnings.
</success_criteria>

<output>
Create `.planning/phases/112-cycle-17-code-port/112-05-SUMMARY.md` when done, listing the registrations added, the `Program.cs` added/deleted line counts, and the solution build result against the baseline.
</output>
