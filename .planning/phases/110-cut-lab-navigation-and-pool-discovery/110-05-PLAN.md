---
phase: 110-cut-lab-navigation-and-pool-discovery
plan: 05
type: execute
wave: 4
depends_on: ["110-01", "110-04"]
files_modified:
  - DeckFlow.Web/Views/Deck/CutLab.cshtml
  - DeckFlow.Web/wwwroot/css/site-common.css
autonomous: true
requirements: [CLUP-15, CLUP-16]
must_haves:
  truths:
    - "Each pool-table row exposes a text-first 'Card text' disclosure with type line, mana cost, set+collector, and oracle text"
    - "The same disclosure appears under Structural evidence chips where card context is available"
    - "The disclosure reads with no JS (native <details>) and text is primary, imagery optional"
    - "A card whose text did not resolve renders with the text simply absent, never a crash or blank field"
    - "The Packages section leads with a static 'How packages work' help block and a one-line inline hint above the pool table"
  artifacts:
    - path: "DeckFlow.Web/Views/Deck/CutLab.cshtml"
      provides: "cutlab-card-text <details> disclosures in pool rows + under evidence chips; package help block + inline hint"
      contains: "cutlab-card-text"
    - path: "DeckFlow.Web/wwwroot/css/site-common.css"
      provides: "Minimal text-first disclosure styling"
      contains: "cutlab-card-text"
  key_links:
    - from: "DeckFlow.Web/Views/Deck/CutLab.cshtml"
      to: "Model.CardTextByCardName"
      via: "TryGetValue per card name for disclosure body"
      pattern: "CardTextByCardName"
---

<objective>
Render the text-first card disclosures (CLUP-16 markup half) in pool rows and under Structural
evidence chips, fed by the `CardTextByCardName` lookup built in plan 110-01, and add the package
assignment help copy (CLUP-15). Both are native `<details>` / static markup — no new JS, no new
fetches.

Purpose: the Cycle 19 UI audit requires oracle text visible before optional imagery; this ships the
approved 2026-07-20 card-text spec's `<details>` half (the popover half is deferred, the chip-lock
half already shipped in Cycle 18). CLUP-15 clears the common "does grouping remove the card?"
confusion with copy that leads on D-34's angle.
Output: reusable `.cutlab-card-text` disclosure (two placements) + `.cutlab-package-help` block + inline hint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/110-cut-lab-navigation-and-pool-discovery/110-CONTEXT.md
@.planning/phases/110-cut-lab-navigation-and-pool-discovery/110-UI-SPEC.md
@.planning/phases/110-cut-lab-navigation-and-pool-discovery/110-01-SUMMARY.md
@.planning/milestones/ws-cut-lab-2026-07-23/captures/cardtext-per-card-lock-spec.md

<interfaces>
<!-- The lookup from plan 110-01 and the two placement sites. -->

CutLabViewModel (plan 110-01): IReadOnlyDictionary<string, CutLabCardTextView> CardTextByCardName;
  CutLabCardTextView { string? TypeLine; string? ManaCost; string? SetCode; string? CollectorNumber;
  string? OracleText; }  — all nullable, fail-open (D-31).

Pool row (CutLab.cshtml ~303-350): the Card cell renders <strong>@qty × @name</strong>; @card.Name is
  the dictionary key.

Structural evidence chips (CutLab.cshtml ~486-520): finding items render evidence chips; the lockable
  ones are <button data-cut-lab-chip-card="@evidenceCard.Name"> and Model.FindLockableEvidenceCard
  resolves the card. That card name is the dictionary key for the chip-level disclosure.

Package section (CutLab.cshtml ~354): <details class="cutlab-collapsible" data-cutlab-mobile-collapse>
  <summary>Packages</summary> ... — help block goes at the TOP of this details (D-32). The inline hint
  goes once above the pool table near the pool status text (D-33), NOT per row.

Reuse candidate: .manabase-help (site-common.css ~2600) — theme-safe help-block styling; reuse
  directly if it fits (Component Inventory Package help block row).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Text-first card disclosure in pool rows and under Structural evidence chips</name>
  <files>DeckFlow.Web/Views/Deck/CutLab.cshtml, DeckFlow.Web/wwwroot/css/site-common.css</files>
  <read_first>
    - DeckFlow.Web/Views/Deck/CutLab.cshtml (pool row Card cell ~317-330; Structural finding item + evidence chips ~486-520)
    - DeckFlow.Web/Models/CutLabViewModel.cs (CardTextByCardName + CutLabCardTextView from plan 110-01)
    - .planning/milestones/ws-cut-lab-2026-07-23/captures/cardtext-per-card-lock-spec.md (§1-§4 + §Invariants)
    - .planning/phases/110-cut-lab-navigation-and-pool-discovery/110-CONTEXT.md (D-26, D-27, D-29, D-30, D-31)
    - .planning/phases/110-cut-lab-navigation-and-pool-discovery/110-UI-SPEC.md (Copywriting Contract: "Card text" trigger; Component Inventory Card text disclosure row; Color/Typography rules)
  </read_first>
  <action>
    Add a reusable `<details class="cutlab-card-text">` with
    `<summary class="cutlab-card-text__summary">Card text</summary>` and a
    `<div class="cutlab-card-text__body">`, in TWO placements with identical class names and markup
    shape (D-26): (1) in the pool-table Card cell, under the card name; (2) under each lockable
    Structural evidence chip's finding item. Populate the body from
    `Model.CardTextByCardName.TryGetValue(cardName, out var text)` — render a meta line
    (type line · mana cost · set code + collector number) in `--muted` and the oracle text body in
    `--ink`, all at `var(--fs-sm)` (D-29, Color/Typography contract). Fail open: when the lookup has no
    entry, or individual fields are null/blank, omit just those parts — never emit an empty required
    field, never a crash (D-31); when nothing is available, render no disclosure for that card. Native
    `<details>` so it reads with no JS (D-30) — do NOT add any JS for this. Add minimal
    `.cutlab-card-text*` CSS to site-common.css (quiet secondary chrome: no accent fill, no elevated
    surface, `<summary>` stays `--ink`/`--muted` per Color contract, 44px summary touch target) using
    COMPOUND selectors so the theme forks cannot override on source order. All @-rendered card text
    goes through normal Razor HTML-encoding (no Html.Raw). LF endings; changed lines only.
  </action>
  <acceptance_criteria>
    - `grep -c 'class="cutlab-card-text"' DeckFlow.Web/Views/Deck/CutLab.cshtml` returns >= 2 (pool row
      + evidence chip placement), each with a `cutlab-card-text__summary` reading "Card text".
    - The disclosure body references `Model.CardTextByCardName` via TryGetValue and guards each field
      for null/blank (fail-open) — no unconditional `.OracleText` dereference that could NRE.
    - No `Html.Raw` / `@Html.Raw` is introduced around card text (grep the diff).
    - `.cutlab-card-text*` CSS is in site-common.css only
      (`grep -rc "cutlab-card-text" DeckFlow.Web/wwwroot/css/site.css` == 0), compound selectors, no
      literal hex, no `--theme-surface`; summary is not accent-colored (stays `--ink`/`--muted`).
    - `dotnet build DeckFlow.Web/DeckFlow.Web.csproj -clp:ErrorsOnly` is clean.
  </acceptance_criteria>
  <verify>
    <automated>dotnet build DeckFlow.Web/DeckFlow.Web.csproj -clp:ErrorsOnly</automated>
  </verify>
  <done>Both placements render a native, HTML-encoded, fail-open text-first disclosure with text primary; Razor compiles clean.</done>
</task>

<task type="auto">
  <name>Task 2: Package assignment help block + one-line inline hint</name>
  <files>DeckFlow.Web/Views/Deck/CutLab.cshtml, DeckFlow.Web/wwwroot/css/site-common.css</files>
  <read_first>
    - DeckFlow.Web/Views/Deck/CutLab.cshtml (Packages <details> ~354-357; pool status text ~240-244)
    - DeckFlow.Web/wwwroot/css/site-common.css (.manabase-help ~2600 for reuse)
    - .planning/phases/110-cut-lab-navigation-and-pool-discovery/110-CONTEXT.md (D-32, D-33, D-34)
    - .planning/phases/110-cut-lab-navigation-and-pool-discovery/110-UI-SPEC.md (Copywriting Contract: Package help-block heading/body + inline hint — use the exact strings)
  </read_first>
  <action>
    At the TOP of the Packages `<details class="cutlab-collapsible">` (D-32, above the package cards),
    add a static help block with heading "How packages work" and the exact body copy from the
    UI-SPEC Copywriting Contract (leads with D-34: grouping doesn't remove cards from the pool; what a
    package does — lock/unlock together — comes second). Reuse `.manabase-help` if its styling fits;
    otherwise add a minimal `.cutlab-package-help` class in site-common.css (`--panel-soft-bg`
    background, `--line` border, `--muted`/`--ink` body text — static copy, NOT accent, per Color
    contract) with compound selectors. Separately, add the one-line inline hint ONCE above the pool
    table near the pool status text (D-33 — not per row): the exact inline-hint string from the
    Copywriting Contract. LF endings; changed lines only.
  </action>
  <acceptance_criteria>
    - The Packages `<details>` contains a help block whose heading is "How packages work" and whose
      first sentence is about grouping NOT removing cards from the pool (D-34) — grep the exact strings.
    - Exactly one inline hint appears above the pool table (not inside the per-row package `<select>`
      loop) — verify it is outside the `@foreach` that renders `data-cut-lab-package-card` selects.
    - Any new `.cutlab-package-help` CSS is in site-common.css only, compound, no accent fill; if
      `.manabase-help` is reused instead, no near-duplicate class was added.
    - `dotnet build DeckFlow.Web/DeckFlow.Web.csproj -clp:ErrorsOnly` is clean.
  </acceptance_criteria>
  <verify>
    <automated>dotnet build DeckFlow.Web/DeckFlow.Web.csproj -clp:ErrorsOnly</automated>
  </verify>
  <done>Packages section leads with the static help block; a single inline hint sits above the pool table; both collapse with their sections; build clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server-render → browser | Oracle text is already-cached card data re-rendered via Razor; no new input crosses a boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-110-09 | Injection (XSS) | Oracle text rendered in disclosure | mitigate | Rendered through default Razor HTML-encoding (no Html.Raw); no CSP change; text is upstream Scryfall data, not user input |
| T-110-10 | Denial of Service | Missing/partial card data | mitigate | Fail-open per D-31 — absent fields omitted, no disclosure when nothing resolves; never a crash or blank required field |
</threat_model>

<verification>
- Razor compiles; disclosures render in both placements, fail-open, HTML-encoded.
- Help block + single inline hint present with exact copy; collapse with their sections.
- No JS added; native <details> reads with no script (e2e in plan 110-06).
</verification>

<success_criteria>
Card oracle context is available through a reusable text-first `<details>` in pool rows and under
Structural evidence chips (text primary, fail-open, no-JS readable), and package assignment carries a
concise static help block plus one inline hint.
</success_criteria>

<output>
Create `.planning/phases/110-cut-lab-navigation-and-pool-discovery/110-05-SUMMARY.md` when done.
</output>
