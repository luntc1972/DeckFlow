---
phase: 92-pull-hardening
plan: 03
type: execute
wave: 3
depends_on: [92-02]
files_modified:
  - DeckFlow.Studio/Pages/PullFromProd.razor
  - DeckFlow.Studio/Pages/PullFromProd.razor.cs
  - DeckFlow.Studio.Tests/PullFromProdPageTests.cs
autonomous: true
requirements: [SYNC-14, SYNC-15]

must_haves:
  truths:
    - "SC2 (SYNC-14 UI): a persistent freshness banner renders on the Pull page when the checkout is Behind (N commits) or Unverified (fetch failed), consuming PullClassifyResult.Freshness — surfaced, not buried in the log"
    - "SC3 (SYNC-15 UI): a Body-divergence badge column renders per entry (Confirmed = danger, Indeterminate = warning) and a Confirmed/Indeterminate entry requires a SEPARATE explicit per-entry opt-in checkbox before it can enter the adopt set (D-01)"
    - "The adopt-set filter excludes Confirmed/Indeterminate entries unless their key is in _divergenceOverrides; the coordinator's ApplyAdoptionsAsync receives that same acknowledged set (defense-in-depth stays wired)"
    - "The page stays thin UI glue — no hashing/git/store logic added here; it renders the coordinator's stamps and forwards the acknowledged set"
  artifacts:
    - path: "DeckFlow.Studio/Pages/PullFromProd.razor"
      provides: "Freshness banner + Body-divergence badge column + per-entry opt-in checkbox"
      contains: "BodyDivergence"
    - path: "DeckFlow.Studio/Pages/PullFromProd.razor.cs"
      provides: "PullClassifyResult consumption, _freshness field, _divergenceOverrides set, adopt-filter change, acknowledged-set forwarding"
      contains: "_divergenceOverrides"
    - path: "DeckFlow.Studio.Tests/PullFromProdPageTests.cs"
      provides: "Tests for banner render + opt-in gating of a divergent entry"
      contains: "BodyDivergenceStatus"
  key_links:
    - from: "PullFromProd.razor.cs PullAndClassifyAsync handler"
      to: "PullFromProdCoordinator.PullAndClassifyAsync -> PullClassifyResult"
      via: "_diffEntries = result.Entries.ToList(); _freshness = result.Freshness;"
      pattern: "result.Entries|_freshness"
    - from: "PullFromProd.razor.cs ApplyResolutionsAsync adopt filter"
      to: "_divergenceOverrides + ApplyAdoptionsAsync(acknowledgedDivergentKeys)"
      via: "exclude Confirmed/Indeterminate unless EntryKey in _divergenceOverrides; forward that set to the coordinator"
      pattern: "_divergenceOverrides"
---

<objective>
Surface the two guards on the `PullFromProd` Blazor page and wire the operator's decision back to the coordinator — the render tier for SYNC-14 (freshness banner) and SYNC-15 (divergence badge + per-entry opt-in). The page consumes the `PullClassifyResult` return shape and forwards the acknowledged-divergence set into `ApplyAdoptionsAsync`.

Purpose: SYNC-14 and SYNC-15 are only "surfaced to the operator" once the page renders them; this closes the operator-facing half of the phase. Live Blazor render against real prod is a manual pre-flip gate (VALIDATION Manual-Only row); the automated gate here is the non-bUnit code-behind test seam.
Output: extended page markup + code-behind + page tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/92-pull-hardening/92-CONTEXT.md
@.planning/phases/92-pull-hardening/92-RESEARCH.md
@.planning/phases/92-pull-hardening/92-PATTERNS.md
@.planning/phases/92-pull-hardening/92-02-SUMMARY.md

<interfaces>
<!-- The page + the coordinator shape 92-02 produced. Extracted — no exploration needed. -->

From 92-02 (consume these — exact shape recorded in 92-02-SUMMARY.md):
  Task<PullClassifyResult> PullFromProdCoordinator.PullAndClassifyAsync(string stagingRoot, IProgress<string> log, Action<string> onStage, CancellationToken)
  record PullClassifyResult(IReadOnlyList<SyncDiffEntry> Entries, PullFreshnessStatus Freshness)
  record PullFreshnessStatus(PullFreshnessKind Kind, int BehindCount, string Branch)
  enum PullFreshnessKind { Fresh, Behind, Unverified }
  Task<IReadOnlyList<PullApplyRowResult>> ApplyAdoptionsAsync(IReadOnlyList<SyncDiffEntry> adoptEntries, string stagingRoot, string dataRoot, IReadOnlySet<string> acknowledgedDivergentKeys, IProgress<...> progress, CancellationToken)
  enum BodyDivergenceStatus { NotApplicable, Clean, Confirmed, Indeterminate }  // SyncDiffEntry.BodyDivergence

Page code-behind touch points (DeckFlow.Studio/Pages/PullFromProd.razor.cs):
  lines 132-136: `var entries = await Task.Run(() => Coordinator.PullAndClassifyAsync(...)); _diffEntries = entries.ToList();`  — CHANGE to consume PullClassifyResult
  lines 188-192: adopt-set filter (`GetResolution(e)==AdoptProd && Kind!=LocalOnly && ProdRow is not null`) — EXTEND
  line 204: `Coordinator.ApplyAdoptionsAsync(adoptEntries, _stagingRoot, _dataRoot, progress, Cts.Token)` — add the acknowledged set arg
  line 237: `private static string EntryKey(SyncDiffEntry entry) => $"{entry.NaturalKeyType}:{entry.NaturalKeyValue}";`  — REUSE for _divergenceOverrides
  line 265: `internal Task InvokePullApplyForTest() => ApplyResolutionsAsync();`  — existing apply-side test seam

Page markup touch points (DeckFlow.Studio/Pages/PullFromProd.razor):
  lines 42-45: existing "READ-ONLY toward production" persistent banner — the precedent for a persistent freshness banner
  lines 124-128: diff table headers (Title, Natural key, Difference, Artifact, Resolution) — add a "Body" <th>
  lines 138-158: `@switch (entry.Kind)` badge cell — add a sibling `@switch (entry.BodyDivergence)` cell
  lines 173-198: per-entry Resolution radios — add the conditional opt-in checkbox

Persistent-banner analog: DeckFlow.Studio/Pages/Reconcile.razor:113-124 (seed-unavailable warning card) — the "run-level condition gets its own card" precedent.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Consume PullClassifyResult + render the freshness banner (SYNC-14 UI)</name>
  <files>DeckFlow.Studio/Pages/PullFromProd.razor.cs, DeckFlow.Studio/Pages/PullFromProd.razor</files>
  <read_first>
    - DeckFlow.Studio/Pages/PullFromProd.razor.cs (PullAndClassifyAsync handler 99-168; field block 33-63)
    - DeckFlow.Studio/Pages/PullFromProd.razor (READ-ONLY banner 42-45 as the persistent-card precedent)
    - DeckFlow.Studio/Pages/Reconcile.razor:113-124 (seed-unavailable warning card shape)
    - .planning/phases/92-pull-hardening/92-02-SUMMARY.md (exact PullClassifyResult/PullFreshnessStatus shape)
  </read_first>
  <behavior>
    - After a pull whose Freshness.Kind == Behind, the page renders a persistent warning banner naming the behind-count and branch.
    - After a pull whose Freshness.Kind == Unverified, the page renders a DISTINCT persistent banner ("could not verify freshness").
    - Freshness.Kind == Fresh renders no banner.
  </behavior>
  <action>
    In PullFromProd.razor.cs, add a nullable field `private PullFreshnessStatus? _freshness;` in the Stage-1 field group (near `_diffEntries`). Change the pull handler (lines 132-136) so the `Task.Run` result is a `PullClassifyResult`: set `_diffEntries = result.Entries.ToList();` and `_freshness = result.Freshness;`. Reset `_freshness = null;` in the same place the handler already resets `_diffEntries = new();` (start of PullAndClassifyAsync, ~line 110). Do not add any git/hash logic — the page only reads the coordinator's result.

    In PullFromProd.razor, add a persistent banner block (rendered when `_freshness is not null && _freshness.Kind != PullFreshnessKind.Fresh`) styled like the Reconcile.razor:113-124 warning card (`class="card border-warning"`, a `data-testid="freshness-banner"` for the test), placed near the existing READ-ONLY banner / above the diff table. Branch the copy on `_freshness.Kind`: Behind → "Local checkout is @_freshness.BehindCount commit(s) behind origin/@_freshness.Branch — run 'git pull' before adopting"; Unverified → "Could not verify checkout freshness (fetch failed) — proceeding against the local git tree as-is." Never render exception detail (D-07).

    Add `using`/namespace access for the coordinator record types if the `.razor.cs` does not already see `DeckFlow.Studio.ViewModels` (it does — `PullFromProdCoordinator` is injected from that namespace). Preserve LF; changed-lines-only.
  </action>
  <verify>
    <automated>dotnet.exe test DeckFlow.Studio.Tests --filter "FullyQualifiedName~PullFromProd"</automated>
  </verify>
  <acceptance_criteria>
    - The pull handler reads `result.Entries` and `result.Freshness` (grep: `_freshness =`); the old `IReadOnlyList<SyncDiffEntry>` consumption is gone.
    - The page renders a `data-testid="freshness-banner"` card only when Freshness.Kind is Behind or Unverified, with distinct copy per kind.
    - No exception detail rendered (D-07); no git/hash logic added to the page.
    - Full solution builds 0/0 (the page now compiles against the widened return type from 92-02).
  </acceptance_criteria>
  <done>The freshness result flows from coordinator to a persistent operator banner; Fresh shows nothing, Behind/Unverified show distinct warnings.</done>
</task>

<task type="auto">
  <name>Task 2: Divergence badge + per-entry opt-in gating (SYNC-15 UI)</name>
  <files>DeckFlow.Studio/Pages/PullFromProd.razor, DeckFlow.Studio/Pages/PullFromProd.razor.cs, DeckFlow.Studio.Tests/PullFromProdPageTests.cs</files>
  <read_first>
    - DeckFlow.Studio/Pages/PullFromProd.razor (Kind badge switch 138-158; Artifact cell 159-172; Resolution radios 173-198; headers 124-128)
    - DeckFlow.Studio/Pages/PullFromProd.razor.cs (adopt-set filter 188-192; ApplyAdoptionsAsync call 204; EntryKey 237; InvokePullApplyForTest 265)
    - DeckFlow.Studio.Tests/PullFromProdPageTests.cs (RenderPull harness + InvokePullApplyForTest usage)
    - .planning/phases/92-pull-hardening/92-PATTERNS.md (PullFromProd.razor divergence badge + opt-in recommended shape) + 92-CONTEXT.md D-01
  </read_first>
  <behavior>
    - A Confirmed entry renders a danger "divergent" badge + an unchecked opt-in checkbox; it is EXCLUDED from the adopt set while the checkbox is unchecked.
    - Checking the opt-in checkbox adds the entry's key to _divergenceOverrides; the entry then enters the adopt set and ApplyAdoptionsAsync receives its key in the acknowledged set.
    - An Indeterminate entry behaves identically (excluded until opted-in).
    - A Clean / NotApplicable entry needs no opt-in (adopts on the normal "adopt prod" radio alone).
  </behavior>
  <action>
    In PullFromProd.razor.cs add `private readonly HashSet<string> _divergenceOverrides = new(StringComparer.Ordinal);` near `_resolutions`. Clear it wherever `_resolutions.Clear()` runs (pull reset). Add helpers mirroring the resolution helpers: `private bool IsDivergenceAck(SyncDiffEntry e) => _divergenceOverrides.Contains(EntryKey(e));` and a toggle `private void SetDivergenceAck(SyncDiffEntry e, bool ack)` that adds/removes `EntryKey(e)`. Extend the adopt-set filter (188-192) to also require `(e.BodyDivergence is BodyDivergenceStatus.NotApplicable or BodyDivergenceStatus.Clean || _divergenceOverrides.Contains(EntryKey(e)))`. Pass `_divergenceOverrides` (as the `IReadOnlySet<string> acknowledgedDivergentKeys` arg) into `Coordinator.ApplyAdoptionsAsync(...)` at line 204.

    In PullFromProd.razor: add a `<th>Body</th>` header (after "Artifact") and a matching `<td>` per row with `@switch (entry.BodyDivergence)` — Confirmed → `<span class="badge bg-danger">divergent</span>`, Indeterminate → `<span class="badge bg-warning text-dark">unverified body</span>`, Clean → `<span class="badge bg-success">matches</span>` (or subtle), NotApplicable → nothing. In the Resolution cell, when `entry.BodyDivergence is BodyDivergenceStatus.Confirmed or BodyDivergenceStatus.Indeterminate`, render a REQUIRED opt-in checkbox (`data-testid="divergence-optin-@key"`) bound to `IsDivergenceAck(entry)` / `SetDivergenceAck(entry, ...)` with label copy like "I understand this body diverges from prod — adopt anyway", so the operator must independently check it beyond the "adopt prod" radio (D-01).

    In PullFromProdPageTests.cs add cases via the existing `RenderPull(...)` harness: supply a prod row whose `BodySha256` mismatches the written repo body (→ Confirmed) and assert (a) the `divergent` badge / `divergence-optin` control renders, and (b) driving `InvokePullApplyForTest()` with the opt-in UNCHECKED does NOT adopt that entry (its key absent from the local store's UpsertMethodCalls / apply results), while checking the opt-in first DOES adopt it. Add a null-`BodySha256` (→ Indeterminate) case asserting the same exclusion-until-opt-in. Follow the file's existing non-bUnit apply-seam pattern.

    Preserve LF; changed-lines-only. Do not add hashing/git/store logic to the page — it renders coordinator stamps and forwards the acknowledged set only.
  </action>
  <verify>
    <automated>dotnet.exe test DeckFlow.Studio.Tests --filter "FullyQualifiedName~PullFromProd"</automated>
  </verify>
  <acceptance_criteria>
    - The diff table renders a Body-divergence badge (danger=Confirmed, warning=Indeterminate) and a per-entry opt-in checkbox for Confirmed/Indeterminate rows.
    - The adopt-set filter excludes Confirmed/Indeterminate entries unless their EntryKey is in `_divergenceOverrides`; `_divergenceOverrides` is passed to `ApplyAdoptionsAsync`.
    - A page test proves a Confirmed entry is NOT adopted with the opt-in unchecked and IS adopted once checked; an Indeterminate (null hash) entry behaves identically.
    - Clean/NotApplicable entries adopt without an opt-in.
    - `dotnet.exe test DeckFlow.Studio.Tests --filter "FullyQualifiedName~PullFromProd"` green; full solution build 0/0.
  </acceptance_criteria>
  <done>Divergent/indeterminate bodies are visibly flagged and require explicit per-entry acknowledgement before adoption — never silently adopted (SYNC-15).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator (browser) → Blazor Server page | operator toggles per-entry opt-in; page forwards the acknowledged set to the coordinator |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-92-04 | Tampering | adopting a divergent body without operator awareness | mitigate | Confirmed/Indeterminate excluded from the adopt set unless the operator checks a SEPARATE per-entry opt-in (D-01); coordinator re-enforces via the acknowledged set (defense-in-depth from 92-02) |
| T-92-03 | Information Disclosure | freshness/error banner copy | mitigate | Banner renders sanitized freshness copy only (behind-count/branch or generic "could not verify") — never `ex.Message` (D-07), continuing the page's existing catch-block convention |
| T-92-SC | Tampering | npm/pip/cargo installs | accept | No package installs this phase (RESEARCH §Standard Stack); this plan edits only `.razor`/`.cs`/tests |
</threat_model>

<verification>
- Full solution build 0/0 — the page now compiles against 92-02's widened `PullAndClassifyAsync` return type and new `ApplyAdoptionsAsync` param.
- `dotnet.exe test DeckFlow.Studio.Tests --filter "FullyQualifiedName~PullFromProd"` → green (coordinator + page tests).
- Manual pre-flip gate (VALIDATION Manual-Only): live `/pull` against real prod — confirm the behind-warning banner fires on a stale checkout, a divergent entry is excluded until opt-in, and no prod write occurs. Not blocking for this plan; recorded for the operator.
- EOL clean (LF); changed-lines format gate clean.
</verification>

<success_criteria>
- A persistent freshness banner surfaces Behind/Unverified checkout state (SYNC-14).
- A per-entry divergence badge + required opt-in prevents silent adoption of a body-vs-index divergent row (SYNC-15).
- The page remains thin UI glue and forwards the acknowledged set to the coordinator.
</success_criteria>

<output>
Create `.planning/phases/92-pull-hardening/92-03-SUMMARY.md` when done.
</output>
