---
phase: 103-simulation-engine-guided-cut-rounds
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - DeckFlow.Web/Models/CutLab/CutLabMetrics.cs
  - DeckFlow.Web.Tests/CutLabMetricsContractTests.cs
  - DeckFlow.Web.Tests/CutLabEngineDeterminismTests.cs
autonomous: true
requirements: [SIM-01]
must_haves:
  truths:
    - "The 7 SIM-01 metric families exist as a shared, versioned contract every downstream plan consumes (no ad-hoc metric shapes)."
    - "D-08: two identical working lists analyzed twice through the existing ManabaseAnalyzer produce byte-identical numeric snapshots (determinism is free from the existing StableSeed)."
    - "D-11: a recorded wall-clock measurement of ManabaseAnalyzer.Analyze on a ~130-card pool exists to set the in-loop trial-count decision (never guessed)."
    - "D-05/D-08: noise-floor thresholds (percent-points and cards) are defined once as named constants, not scattered magic numbers."
  artifacts:
    - path: "DeckFlow.Web/Models/CutLab/CutLabMetrics.cs"
      provides: "7-family metric contract records + noise-floor constants + fixed category-by-turn caps"
      contains: "CutLabMetricSnapshot"
    - path: "DeckFlow.Web.Tests/CutLabEngineDeterminismTests.cs"
      provides: "D-08 determinism guard against the existing engine"
      contains: "byte-identical"
  key_links:
    - from: "DeckFlow.Web/Models/CutLab/CutLabMetrics.cs"
      to: "DeckFlow.Core/Manabase/CedhMulliganCalibration.cs"
      via: "fixed turn caps (TurnCapExplosive=3, TurnCapEngine=2, RepresentativeLineTurnCap=4) reused as Phase-103 category-by-turn defaults"
      pattern: "CedhMulliganCalibration"
---

<objective>
Establish the shared 7-family metric contract every Phase 103 plan consumes, prove the D-08 determinism claim against the existing engine, and record the D-11 timing measurement that sets the in-loop trial-count decision. This is the interface-first + discovery foundation: it produces the type vocabulary and the two measured numbers (per-analysis wall-clock, noise floor) that unblock the simulation service.

Purpose: Prevent downstream scavenger hunts and guessed trial counts. SIM-01 mandates "no new simulation math" — this plan defines only the display/contract layer over existing `ManabaseReport` fields.
Output: `CutLabMetrics.cs` contract, determinism guard test, and a recorded timing spike result in the SUMMARY.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/cut-lab/phases/103-simulation-engine-guided-cut-rounds/103-RESEARCH.md
@.planning/workstreams/cut-lab/phases/103-simulation-engine-guided-cut-rounds/103-UI-SPEC.md
@.planning/workstreams/cut-lab/phases/103-simulation-engine-guided-cut-rounds/103-VALIDATION.md

<interfaces>
From DeckFlow.Core/Manabase/ManabaseModels.cs (fields the 7 families project from — read, do not modify):
- ManabaseReport.LandDelta (double) = ActualLands - TargetLands  [flood if positive/large, screw if negative]
- ManabaseReport.ColorFindings : IReadOnlyList<ColorSourceFinding>  (ColorSourceFinding.AverageCastPercent double; ordered composite-worst first)
- ManabaseReport.MulliganEvaluation : ManabaseMulliganEvaluation? (KeepableHandPercent int, MulliganTo5Percent int, PlanPresence)
- ManabaseMulliganEvaluation.PlanPresence : ManabasePlanPresence? (PlanPresencePercent int)
- ManabaseReport.InteractionLens : ManabaseInteractionLens? (QualifyingCount int; cEDH-only)
- CardCastability.EarlyCastPercents : IReadOnlyList<int>  (commander row → commander-on-time)

From DeckFlow.Core/Manabase/CedhMulliganCalibration.cs (fixed category-by-turn caps — Open Question 1 resolved to FIXED defaults, NOT user-defined; user goals stay Phase 104):
- const int TurnCapExplosive = 3;  const int TurnCapEngine = 2;  const int RepresentativeLineTurnCap = 4;

From DeckFlow.Core/Manabase/ManabaseAnalyzer.cs:
- ManabaseReport Analyze(ManabaseDeck deck, ManabaseMode mode, ... optional flags ..., CedhLandContext cedhContext = default)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define the 7-family metric contract + noise-floor + fixed turn caps</name>
  <read_first>
    - DeckFlow.Web/Models/CutLab/CutLabState.cs (namespace DeckFlow.Web.Models.CutLab, record conventions, XML doc style)
    - DeckFlow.Core/Manabase/ManabaseModels.cs (ManabaseReport, ColorSourceFinding, ManabaseMulliganEvaluation, ManabasePlanPresence, ManabaseInteractionLens, CardCastability field shapes)
    - DeckFlow.Core/Manabase/CedhMulliganCalibration.cs (turn-cap constants)
    - .planning/workstreams/cut-lab/phases/103-simulation-engine-guided-cut-rounds/103-UI-SPEC.md (Component Contract 2 noise-floor FLAG: ±1.5pp / ±1 card; the 7-family list in the Full-metric-expander copy row)
    - .planning/workstreams/cut-lab/phases/103-simulation-engine-guided-cut-rounds/103-RESEARCH.md (Don't Hand-Roll table = family→field mapping; Open Questions 1 and 2 resolutions)
  </read_first>
  <action>
    Create `DeckFlow.Web/Models/CutLab/CutLabMetrics.cs` (namespace `DeckFlow.Web.Models.CutLab`, one file, sealed records, `{ get; init; }` — never `{ get; }` per the .editorconfig carve-out). Define:
    (a) `CutLabMetricUnit` enum: `Percent`, `Cards`.
    (b) `CutLabMetricDirection` enum: `Up`, `Down`, `None`.
    (c) `CutLabMetricFamily` enum with exactly the 7 families named in the UI spec Full-metric-expander row: `CommanderOnTime`, `KeepableHand`, `ManaColorReliability`, `EarlyInteraction`, `PlanPresence`, `CategoryByTurn`, `FloodScrewCurveRisk`.
    (d) `CutLabMetricKind` enum: the granular metric identities, one per rendered line. `FloodScrewCurveRisk` family expands to THREE kinds — `Flood`, `Screw`, `Curve` (Open Question 2 resolved: three separate delta lines, never a composite score). `CategoryByTurn` family may expand to the fixed cEDH caps (commander-by-3, engine-by-2, representative-line-by-4) per Open Question 1 (FIXED defaults). All other families = one kind each.
    (e) `CutLabMetricValue` record: `CutLabMetricKind Kind`, `CutLabMetricFamily Family`, `string Label`, `double Value`, `CutLabMetricUnit Unit`.
    (f) `CutLabMetricSnapshot` record: `IReadOnlyList<CutLabMetricValue> Metrics { get; init; } = [];` — this is the compact numeric baseline stored in `CutLabState` (D-12); it holds ONLY numeric values, never any `ManabaseReport` object.
    (g) `CutLabMetricDelta` record: `CutLabMetricKind Kind`, `CutLabMetricFamily Family`, `string Label`, `double Before`, `double After`, `double Delta`, `CutLabMetricDirection Direction`, `bool IsMeaningful`.
    (h) `CutLabProposalDeltas` record: `string CardName`, `IReadOnlyList<CutLabMetricDelta> Deltas`, `int ChangedFamilyCount`.
    (i) A static `CutLabNoiseFloor` class with `public const double PercentPoints = 1.5;` and `public const int Cards = 1;` and `// Why:` comments citing UI-SPEC A3 (display default, tunable after the Task 3 timing/variance spike; the show/hide behavior does not change).
    Add XML doc summaries on every public type/member (GenerateDocumentationFile is on). Do NOT reference any `ManabaseReport` type inside these records (keeps the state envelope free of Core simulation objects).
  </action>
  <verify>
    <automated>"/mnt/c/Program Files/dotnet/dotnet.exe" build DeckFlow.Web/DeckFlow.Web.csproj -clp:ErrorsOnly</automated>
  </verify>
  <acceptance_criteria>
    - `DeckFlow.Web/Models/CutLab/CutLabMetrics.cs` compiles with zero new warnings.
    - `CutLabMetricFamily` has exactly 7 members matching the UI-SPEC Full-metric-expander family list.
    - `CutLabMetricKind` contains `Flood`, `Screw`, and `Curve` as three distinct members (grep: `Flood`, `Screw`, `Curve` all present).
    - `CutLabMetricSnapshot` and `CutLabMetricValue` reference no `DeckFlow.Core.Manabase` type (grep the file for `ManabaseReport` returns nothing).
    - `CutLabNoiseFloor.PercentPoints == 1.5` and `CutLabNoiseFloor.Cards == 1`.
  </acceptance_criteria>
  <done>The shared metric contract exists and compiles; downstream plans reference these exact type names.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: D-08 determinism guard test against the existing engine</name>
  <read_first>
    - DeckFlow.Web.Tests/CutLabPageServiceTests.cs (fake wiring: FakeLoader, FakeResolver, FakeBanListService; xUnit conventions, namespace DeckFlow.Web.Tests)
    - DeckFlow.Web/Services/Manabase/ManabaseAnalysisService.cs (the resolve→classify→analyze pipeline shape: ScryfallCardFactMapper.ToCardFacts → ManabaseClassifier.Classify(facts, isSingleton:true, ...) → ManabaseAnalyzer.Analyze(deck, mode, ...))
    - DeckFlow.Core/Manabase/CastabilitySimulator.cs (StableSeed FNV-1a — the reason determinism is already free; do not modify)
    - DeckFlow.Core/Manabase/ManabaseClassifier.cs (Classify signature)
  </read_first>
  <behavior>
    - Test 1: Build a fixed small `IReadOnlyList<CardFact>` (hand-authored, ~8-12 cards incl. a commander + lands + a couple of spells), classify twice and Analyze twice at the SAME trial count and mode; assert the two `ManabaseReport`s yield identical projected numeric values (KeepableHandPercent, LandDelta, PlanPresencePercent, ColorFindings[].AverageCastPercent) — byte-identical, proving D-08's "fixed seed already satisfied" claim.
    - Test 2: Re-order the input card list (same set, shuffled) and assert the projected numbers are still identical (seed is a pure function of card identity, not list order) — this is the guard that later working-list-hash caching relies on.
  </behavior>
  <action>
    Create `DeckFlow.Web.Tests/CutLabEngineDeterminismTests.cs` (namespace `DeckFlow.Web.Tests`, xUnit `[Fact]`s, `public sealed class CutLabEngineDeterminismTests`). Construct `CardFact` inputs directly (no HTTP; no Scryfall resolution needed — build facts in-test). Run the exact `ManabaseClassifier.Classify(facts, isSingleton: true, ...)` → `ManabaseAnalyzer.Analyze(deck, mode)` pipeline twice and assert equality of the raw numbers this phase will project. Keep the fixture small so the test runs in well under a second. Do NOT modify any Core file — this test consumes the existing engine only.
  </action>
  <verify>
    <automated>"/mnt/c/Program Files/dotnet/dotnet.exe" test DeckFlow.Web.Tests --filter "FullyQualifiedName~CutLabEngineDeterminism"</automated>
  </verify>
  <acceptance_criteria>
    - `CutLabEngineDeterminismTests` passes: two analyses of the same facts produce equal numbers.
    - Shuffled-input test passes (order independence).
    - No file under `DeckFlow.Core/` is modified (git diff shows only the new test file + Task 1/3 files).
  </acceptance_criteria>
  <done>D-08's free-determinism assumption is locked by an automated regression guard.</done>
</task>

<task type="auto">
  <name>Task 3: D-11 timing spike — measure Analyze wall-clock on a ~130-card pool</name>
  <read_first>
    - DeckFlow.Core/Manabase/CastabilitySimulator.cs (DefaultTrials = 20_000, SourceSearchTrials = 5_000 — the cost drivers named in RESEARCH Pitfall 2)
    - DeckFlow.Web/Services/Manabase/ManabaseAnalysisService.cs (the Analyze call site + option flags to mirror)
    - .planning/workstreams/cut-lab/phases/103-simulation-engine-guided-cut-rounds/103-RESEARCH.md (Open Question 3 + Pitfall 2 — what the spike must resolve)
  </read_first>
  <action>
    Add a `[Fact]` to `CutLabEngineDeterminismTests.cs` (or a clearly-named `CutLabTimingSpike` fact in the same file) that builds a realistic ~130-card singleton pool of `CardFact`s, runs the full `ManabaseClassifier.Classify` → `ManabaseAnalyzer.Analyze(deck, mode)` pipeline once at the DEFAULT trial count, and measures elapsed milliseconds with `System.Diagnostics.Stopwatch`. The fact must `Assert` nothing about a specific ms number (hardware-dependent) — instead it writes the measured elapsed ms to test output (xUnit `ITestOutputHelper`) AND the executor must record the observed number in the plan SUMMARY. Then, based on the measured number vs D-11's ~1s target / 3s hard cap, record in the SUMMARY the chosen in-loop trial-count strategy: (a) if a single default-trial Analyze is comfortably under ~1s, note "no reduction needed for in-loop deltas"; (b) if not, record the reduced in-loop `trials` value the simulation service (Plan 103-05) must use, and confirm full-fidelity default trials stay reserved for the baseline snapshot + round summaries. This is a discovery measurement — the harness stays in the test project (never shipped to production code).
  </action>
  <verify>
    <automated>"/mnt/c/Program Files/dotnet/dotnet.exe" test DeckFlow.Web.Tests --filter "FullyQualifiedName~CutLabEngineDeterminism"</automated>
  </verify>
  <acceptance_criteria>
    - The timing fact runs green and emits an elapsed-ms value to test output.
    - The plan SUMMARY records: the measured ms for a ~130-card Analyze, and an explicit in-loop trial-count decision (either "no reduction" or a concrete reduced trial value) that Plan 103-05 will consume.
    - No production (`DeckFlow.Web` / `DeckFlow.Core`) source file is modified by this task.
  </acceptance_criteria>
  <done>Open Question 3 is resolved with a measured number; the trial-count strategy is recorded for the simulation service.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| none new | This plan adds only pure contract records + tests; no endpoint, input parsing, or persisted-state change. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-103-01-01 | Tampering | CutLabMetricSnapshot (later persisted in state) | mitigate | Snapshot holds only primitive numeric values + enum kinds (no free-form/deserialized objects); serialization hardening handled where it is added to state (Plan 103-02). |
| T-103-01-SC | Tampering | npm/NuGet installs | accept | No packages installed this phase (RESEARCH Package Legitimacy Audit: N/A). |
</threat_model>

<verification>
- `"/mnt/c/Program Files/dotnet/dotnet.exe" build DeckFlow.sln -clp:ErrorsOnly` clean.
- `"/mnt/c/Program Files/dotnet/dotnet.exe" test DeckFlow.Web.Tests --filter "FullyQualifiedName~CutLab"` green.
- SUMMARY records the measured Analyze ms and the trial-count decision.
</verification>

<success_criteria>
7-family contract compiles and is referenced by no ad-hoc metric shapes elsewhere; determinism guard green; timing number + trial-count decision recorded.
</success_criteria>

<output>
Create `.planning/workstreams/cut-lab/phases/103-simulation-engine-guided-cut-rounds/103-01-SUMMARY.md` when done. MUST include: measured ~130-card Analyze wall-clock ms, and the in-loop trial-count decision for Plan 103-05.
</output>
