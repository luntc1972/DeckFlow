---
phase: 21.2-pluggable-llm-distill-cli-backends-inserted
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - DeckFlow.Core/Knowledge/DistillationSchemas.cs
  - DeckFlow.Core/Integration/LlmDistillationService.cs
  - DeckFlow.Core/Integration/CliCommandSpec.cs
  - DeckFlow.Core/Integration/LlmDistillationProviderFactory.cs
  - DeckFlow.Core/Integration/CliLlmDistillationService.cs
  - DeckFlow.Core.Tests/CliLlmDistillationServiceTests.cs
  - DeckFlow.Core.Tests/LlmDistillationProviderFactoryTests.cs
  - DeckFlow.Core.Tests/DistillationPromptRegressionTests.cs
  - .planning/phases/21.2-pluggable-llm-distill-cli-backends-inserted/21.2-01-SUMMARY.md
autonomous: true
requirements: [KB-10, KB-11]

must_haves:
  truths:
    - "With DECKFLOW_LLM_PROVIDER unset, the OpenAI distill backend emits byte-identical system-prompt text AND OpenAI ResponseFormat schema bytes to shipped Phase 21 (regression guard, asserted by an automated byte-equality test that captures both the system messages and the schema bytes — L1)"
    - "LlmDistillationProviderFactory.Resolve selects OpenAI (default/unset/openai), CliLlmDistillationService for claude, throws NotSupportedException for codex pointing at Phase 21.3, and throws NotSupportedException for unknown values"
    - "CliLlmDistillationService builds the claude command via a CliCommandSpec using ProcessStartInfo.ArgumentList (instruction+schema = a CLI argument, transcript = stdin) — no space-split Arguments string (H1/M3)"
    - "DECKFLOW_LLM_CLI_COMMAND override is a JSON ARRAY of args parsed with JsonSerializer.Deserialize<string[]> (NOT a space-split string); element[0]=FileName, the rest=ArgumentList verbatim with empty strings preserved exactly as string.Empty; exactly one element must equal the {instruction} placeholder and is substituted with the instruction arg (ROUND-3 HIGH-1)"
    - "A non-JSON, empty-array, missing-{instruction}, or duplicate-{instruction} override throws a clear error ONCE, validated up front BEFORE the retry loop — config errors are NOT swallowed by the broad retry catch and are NOT retried 3× (ROUND-3 HIGH-1 + MED-1)"
    - "CliLlmDistillationService enforces a per-extraction timeout INSIDE the service via a linked CancellationTokenSource passed INTO the _runProcess runner-seam, and kills the process tree on timeout/cancel (H2); a caller passing CancellationToken.None still cannot hang"
    - "RunProcessAsync registers linkedToken to Kill(entireProcessTree:true) immediately after Process.Start, and writes stdin via the cancellation-aware StandardInput.BaseStream.WriteAsync(...AsMemory(), linkedToken) — so a timeout interrupts a blocked stdin write even if the child never drains stdin (ROUND-3 HIGH-2)"
    - "RunProcessAsync starts the stdout AND stderr ReadToEndAsync tasks IMMEDIATELY after Process.Start, BEFORE writing the transcript to stdin, then writes+closes stdin, then awaits reads + exit (deadlock-safe pipe ordering — MED round-2, preserved)"
    - "Tag extraction runs ValidateTags (non-null arrays, every value in ContentTagVocabulary, no duplicates); vocab-invalid output triggers the retry loop, not a silent downstream filter (H4)"
    - "JSON repair uses a quote/escape-aware balanced-object scan that extracts THE SINGLE JSON object from the claude .result envelope, recovering from fenced/enveloped/wrapper-prose wrapping; persistent garbage throws after 3 attempts (M2 — single-object extraction; no multi-object claim, ROUND-3 MED-2)"
    - "Every CLI extraction returns TokenUsage(0,0); the claude is_error path throws a bounded, sanitized message (no raw .result body); error logging is bounded (Tail(stderr)) and never logs stdout/prompts/transcripts/full command lines (L2)"
    - "No codex spawn, codex isolation, or codex docs are implemented (codex deferred to Phase 21.3)"
    - "No real claude process is spawned in any unit test (fake process-runner seam)"
  artifacts:
    - path: "DeckFlow.Core/Integration/CliCommandSpec.cs"
      provides: "Command spec (FileName, ArgumentList, EnvelopeKind) replacing space-split string args"
      contains: "ArgumentList"
    - path: "DeckFlow.Core/Integration/LlmDistillationProviderFactory.cs"
      provides: "Env-switch factory mirroring TranscriptProviderFactory (openai|claude; codex→21.3 NotSupportedException)"
      contains: "public static class LlmDistillationProviderFactory"
    - path: "DeckFlow.Core/Integration/CliLlmDistillationService.cs"
      provides: "claude CLI-backed ILlmDistillationService with ArgumentList spawn + JSON-array {instruction} override (fail-fast) + in-service timeout/kill + cancellation-aware stdin + deadlock-safe pipes + single-object JSON repair + retry + ValidateTags"
      contains: ": ILlmDistillationService"
    - path: "DeckFlow.Core/Knowledge/DistillationSchemas.cs"
      provides: "Shared static system prompts extracted from LlmDistillationService (D-01a)"
      contains: "SummarySystemPrompt"
    - path: "DeckFlow.Core.Tests/CliLlmDistillationServiceTests.cs"
      provides: "JSON-repair + retry + envelope-unwrap + ValidateTags-retry + timeout/cancel (None-can't-hang) + JSON-array override (empty-arg-preserved + fail-fast) + token-zero coverage via fake runner"
    - path: "DeckFlow.Core.Tests/LlmDistillationProviderFactoryTests.cs"
      provides: "Provider-selection coverage incl. codex→NotSupportedException(21.3) + unknown-value NotSupportedException"
    - path: "DeckFlow.Core.Tests/DistillationPromptRegressionTests.cs"
      provides: "Automated byte-identical guard: extracted DistillationSchemas prompts == original constant text AND OpenAI ResponseFormat schema bytes/system messages unchanged (L1)"
  key_links:
    - from: "LlmDistillationProviderFactory.Resolve"
      to: "CliLlmDistillationService / LlmDistillationService / NotSupportedException(21.3)"
      via: "string.Equals OrdinalIgnoreCase branch per provider"
      pattern: "LlmDistillationProviderFactory.*Resolve"
    - from: "LlmDistillationService"
      to: "DistillationSchemas.SummarySystemPrompt / ClipsSystemPrompt / TagsSystemPrompt"
      via: "shared static reference after D-01a extraction"
      pattern: "DistillationSchemas\\.(Summary|Clips|Tags)SystemPrompt"
    - from: "CliLlmDistillationService"
      to: "ProcessStartInfo.ArgumentList via CliCommandSpec"
      via: "instruction-as-argument; transcript-on-stdin; JSON-array {instruction} placeholder override"
      pattern: "ArgumentList"
    - from: "CliLlmDistillationService.ExtractWithRetryAsync"
      to: "_runProcess runner-seam"
      via: "linked CTS created in ExtractWithRetryAsync and passed into the runner (H2); override validated ONCE before the loop (MED-1)"
      pattern: "CreateLinkedTokenSource"
    - from: "CliLlmDistillationService.RunProcessAsync"
      to: "linkedToken kill-tree registration + cancellation-aware stdin write"
      via: "linkedToken.Register(Kill) after Start + BaseStream.WriteAsync(...AsMemory(), linkedToken) (ROUND-3 HIGH-2)"
      pattern: "linkedToken.Register"
    - from: "CliLlmDistillationService.InferTagsAsync"
      to: "ContentTagVocabulary allowlists"
      via: "ValidateTags inside the retry loop (H4)"
      pattern: "ValidateTags"
---

<objective>
Build the CLAUDE-ONLY pluggable LLM distill backend in `DeckFlow.Core`, folding in the round-1 + round-2 + round-3 Codex review findings:
- Extract the 3 shared system prompts into a shared static so OpenAI and CLI impls use byte-identical instructions (D-01a), guarded by an AUTOMATED byte-equality test covering BOTH the prompt text AND the OpenAI ResponseFormat schema bytes / system messages (L1).
- Add `LlmDistillationProviderFactory` mirroring `TranscriptProviderFactory` (provider switch: openai default, claude CLI; `codex` throws a clear `NotSupportedException` pointing at Phase 21.3).
- Add `CliCommandSpec` + `CliLlmDistillationService` that shells to the `claude` CLI via `System.Diagnostics.Process` using `ProcessStartInfo.ArgumentList` (instruction+schema as an ARGUMENT, transcript on STDIN — H1/M3), supports a `DECKFLOW_LLM_CLI_COMMAND` override that is a **JSON array of args** with an `{instruction}` placeholder token (ROUND-3 HIGH-1 — JSON array so the empty `--allowedTools ""` arg and Windows path pass verbatim), enforces a per-extraction timeout + `Kill(entireProcessTree:true)` via a linked CTS PASSED INTO the runner seam (H2) AND interrupts a blocked stdin write via `linkedToken.Register(Kill)` + a cancellation-aware `BaseStream.WriteAsync(...AsMemory(), linkedToken)` (ROUND-3 HIGH-2), uses deadlock-safe pipe ordering — stdout/stderr reads started before stdin write (MED round-2), repairs best-effort JSON with a quote/escape-aware balanced-object scan that extracts the single object from the claude `.result` envelope (M2; single-object — ROUND-3 MED-2), runs `ValidateTags` against `ContentTagVocabulary` inside the retry loop (H4), validates the override ONCE up front so config errors fail fast and are NOT retried (ROUND-3 MED-1), keeps logging bounded and secret-safe with a sanitized is_error message (L2), and retries ×2.

**SCOPE: CLAUDE-ONLY.** Do NOT implement codex spawn, codex tool/read isolation, codex working-directory, or codex docs — all deferred to Phase 21.3. The factory and `CliLlmDistillationService` are built so a future `codex` provider slots in, but `DECKFLOW_LLM_PROVIDER=codex` MUST throw `NotSupportedException` naming Phase 21.3.

All work is self-contained in Core + Core.Tests — no CLI wiring yet (Plan 02 does that).

Purpose: Deliver KB-10 (pluggable provider factory, openai|claude) and the CLI-backend portion of KB-11 (cross-platform claude process launch + best-effort-JSON hardening), with full unit coverage and a hard automated regression guard that the OpenAI default path is byte-identical to shipped Phase 21.
Output: 1 modified config file (prompts extracted), 1 modified service (references updated), 3 new Core types (CliCommandSpec, factory, CLI service), 3 new test files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21.2-pluggable-llm-distill-cli-backends-inserted/21.2-CONTEXT.md
@.planning/phases/21.2-pluggable-llm-distill-cli-backends-inserted/21.2-RESEARCH.md
@.planning/phases/21.2-pluggable-llm-distill-cli-backends-inserted/21.2-PATTERNS.md
@.planning/phases/21.2-pluggable-llm-distill-cli-backends-inserted/21.2-REVIEWS.md

<interfaces>
<!-- Contracts the executor implements against. Extracted from codebase — no exploration needed. -->

ILlmDistillationService (DeckFlow.Core/Integration/ILlmDistillationService.cs) — the seam CliLlmDistillationService implements directly:
  Task<SummaryResult> SummarizeAsync(string transcript, CancellationToken cancellationToken = default);
  Task<ClipsResult> ExtractClipsAsync(string transcript, CancellationToken cancellationToken = default);
  Task<TagsResult> InferTagsAsync(string transcript, CancellationToken cancellationToken = default);

Result + payload shapes (DeckFlow.Core/Integration — used by both impls):
  SummaryResult(string Summary, TokenUsage Usage)
  ClipsResult(IReadOnlyList<ClipItem> Clips, TokenUsage Usage)
  TagsResult(IReadOnlyList<string> Archetype, IReadOnlyList<string> Bracket, IReadOnlyList<string> CardCategory, TokenUsage Usage)
  TokenUsage(int InputTokens, int OutputTokens)
  ClipItem has TimestampSeconds (nullable int) + Excerpt (string)
  -- NOTE: the JSON CLI payload field is timestamp_seconds; CliLlmDistillationService deserializes into a private payload record then maps to ClipItem, exactly as LlmDistillationService does with its private SummaryPayload/ClipsPayload/TagsPayload records (LlmDistillationService.cs:257-264).

DistillationSchemas (DeckFlow.Core/Knowledge/DistillationSchemas.cs) — existing public const string SummarySchema / ClipsSchema / TagsSchema (DO NOT modify these constants; only APPEND the 3 prompt properties + FormatAllowlist helper).

ContentTagVocabulary (DeckFlow.Core/Knowledge/ContentTagVocabulary.cs) — verified surface:
  public static readonly IReadOnlySet<string> Archetypes  (ContentTagVocabulary.cs:9, OrdinalIgnoreCase HashSet)
  public static readonly IReadOnlySet<string> Brackets    (line 30)
  public static readonly IReadOnlySet<string> CardCategories (line 41)
  public static bool IsValid(string dimension, string value) (line 63)
  Consumed by the TagsSystemPrompt builder AND by the new ValidateTags (H4) — use the three sets' .Contains directly (OrdinalIgnoreCase) for membership.

TranscriptProviderFactory (DeckFlow.Core/Integration/TranscriptProviderFactory.cs) — the exact factory shape to mirror (public const EnvironmentVariableName, ArgumentNullException.ThrowIfNull, IsNullOrWhiteSpace default branch, OrdinalIgnoreCase, NotSupportedException listing supported values).

FfmpegAudioChunker (DeckFlow.Core/Integration/FfmpegAudioChunker.cs) — the Process.Start pattern to mirror (UseShellExecute=false, CreateNoWindow=true, async stderr read BEFORE WaitForExitAsync, exit-code check, Tail helper with ErrorTailLength=800). NOTE: it uses the Arguments STRING; CliLlmDistillationService deliberately diverges to ArgumentList per H1/M3.

LlmDistillationService.cs source-of-truth anchors (verified 2026-06-01; re-locate by symbol if drifted):
  - 3 prompt references at lines 58 (SummarySystemPrompt), 77 (ClipsSystemPrompt), 96 (TagsSystemPrompt).
  - FormatAllowlist private static at line 233.
  - SummarySystemPrompt raw-string literal at line 236; ClipsSystemPrompt at 242; TagsSystemPrompt computed at 249-255.
  - ValidateSummary at 209; CountWords at 217; ValidateClips at 220 (copy verbatim).
  - private payload records: SummaryPayload at 257, ClipsPayload at 259, TagsPayload at 261.
  - JsonOpts (snake_case + case-insensitive) at lines 24-28 (copy verbatim).
  - using DeckFlow.Core.Knowledge; at line 4 (references to DistillationSchemas already resolve).

OpenAI schema/system-message capture for L1 (LlmDistillationService.cs): the OpenAI path builds ChatCompletionOptions with a ResponseFormat carrying the JSON schema (from DistillationSchemas.*Schema) and passes the system prompt as a system message. The L1 test must capture BOTH the schema bytes the OpenAI path emits AND the system messages, then assert byte-equality against fixtures. If the schema bytes are awkward to capture without a live OpenAI client, assert (a) DistillationSchemas.*Schema constant bytes == fixtures AND (b) DistillationSchemas.*SystemPrompt bytes == fixtures — together these are the exact inputs the OpenAI ResponseFormat + system message are built from, so byte-equality of the inputs proves the emitted bytes are unchanged. Record the method chosen in SUMMARY.

Test seam grant: DeckFlow.Core/AssemblyInfo.cs already has [assembly: InternalsVisibleTo("DeckFlow.Core.Tests")] — no change needed.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extract shared system prompts into DistillationSchemas (D-01a) + repoint OpenAI impl + automated byte-identical regression test covering prompts AND schema bytes (L1)</name>
  <read_first>
    - DeckFlow.Core/Knowledge/DistillationSchemas.cs (the file being modified — existing 3 schema constants)
    - DeckFlow.Core/Integration/LlmDistillationService.cs line 233 (FormatAllowlist), 236/242/249-255 (the 3 prompts to extract), 58/77/96 (the 3 reference sites to repoint), 24-28 (JsonOpts), 209-231 (validators) — AND the ChatCompletionOptions/ResponseFormat construction that consumes DistillationSchemas.*Schema + the system prompt (for the L1 capture)
    - DeckFlow.Core/Knowledge/ContentTagVocabulary.cs (Archetypes/Brackets/CardCategories sets consumed by TagsSystemPrompt and by ValidateTags in Task 2)
    - .planning/phases/21.2-pluggable-llm-distill-cli-backends-inserted/21.2-PATTERNS.md lines 376-431 (exact extraction guidance, byte-preservation constraint)
    - .planning/phases/21.2-pluggable-llm-distill-cli-backends-inserted/21.2-REVIEWS.md L1 finding (round 2: PARTIAL — must assert schema bytes + system messages, not just prompt strings; round 3: RESOLVED — preserve verbatim)
  </read_first>
  <behavior>
    - After extraction, the bytes of the 3 system prompts are unchanged: SummarySystemPrompt + ClipsSystemPrompt (raw-string literals) and TagsSystemPrompt (computed string) produce text identical to shipped Phase 21.
    - LlmDistillationService.SummarizeAsync/ExtractClipsAsync/InferTagsAsync still pass the same prompt text + the same schema to the OpenAI ChatCompletionOptions/ResponseFormat.
    - Existing LlmDistillationServiceTests stay green (no behavior change).
    - NEW automated guard (L1): a test asserts (1) the extracted DistillationSchemas.*SystemPrompt values equal original literal fixtures (string equality, ordinal); AND (2) the DistillationSchemas.*Schema bytes (the exact bytes the OpenAI ResponseFormat is built from) equal original fixtures. Any future drift in either the prompts or the schema fails the build.
  </behavior>
  <action>
    Move the 3 system prompts and the `FormatAllowlist` helper from `LlmDistillationService.cs` (FormatAllowlist line 233; prompts at 236/242/249-255) into `DeckFlow.Core/Knowledge/DistillationSchemas.cs` as the new shared static surface (D-01a). APPEND after the existing schema constants — do NOT modify `SummarySchema`/`ClipsSchema`/`TagsSchema`.
    Add three public static string properties: `SummarySystemPrompt { get; }`, `ClipsSystemPrompt { get; }`, `TagsSystemPrompt { get; }`. Byte-copy the raw-string literal CONTENT for `SummarySystemPrompt` and `ClipsSystemPrompt` exactly — do not re-indent, reflow, or alter whitespace inside the `"""..."""` delimiters (CLAUDE.md invariant #9 / R-6). For `TagsSystemPrompt`, move the computed string-concatenation expression verbatim and move `FormatAllowlist` (currently `private static` at LlmDistillationService.cs:233) into `DistillationSchemas` as `private static`. `ContentTagVocabulary` is already in `DeckFlow.Core.Knowledge` (same namespace) — no new using needed.
    Keep `{ get; }` style — NEVER convert to `{ get; init; }` or drop `get;`.
    In `LlmDistillationService.cs`: DELETE the 3 prompt property declarations (236-255) and the now-moved `FormatAllowlist` (233), and repoint the 3 references at 58/77/96 from `SummarySystemPrompt`/`ClipsSystemPrompt`/`TagsSystemPrompt` to `DistillationSchemas.SummarySystemPrompt`/`DistillationSchemas.ClipsSystemPrompt`/`DistillationSchemas.TagsSystemPrompt`. `LlmDistillationService.cs` already has `using DeckFlow.Core.Knowledge;` (line 4). Touch ONLY these lines — no Format Document, no reflow of surrounding members.
    Create `DeckFlow.Core.Tests/DistillationPromptRegressionTests.cs` (L1 — round-2 hardened): a `public sealed class DistillationPromptRegressionTests` with [Fact] cases that capture the original prompt text AND the original schema text as `const`/literal fixtures (the exact bytes), and assert with ordinal string comparison: `Assert.Equal(expectedPrompt, DistillationSchemas.SummarySystemPrompt)` (and Clips, Tags), AND `Assert.Equal(expectedSchema, DistillationSchemas.SummarySchema)` (and Clips, Tags). These are the exact inputs the OpenAI ResponseFormat + system message are constructed from, so byte-equality of the inputs is the regression guard the review demanded (round 2: schema bytes + system messages, not just prompt strings). Document in SUMMARY that the guard asserts the OpenAI-path inputs (prompts + schema constants) for byte-identity.
  </action>
  <verify>
    <automated>"/mnt/c/Program Files/dotnet/dotnet.exe" build DeckFlow.Core/DeckFlow.Core.csproj -c Debug 2>&1 | grep -iE "error|warning" | grep -v "0 Warning" || echo "BUILD CLEAN"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "SystemPrompt { get; }" DeckFlow.Core/Knowledge/DistillationSchemas.cs` returns 2 (Summary + Clips raw-string props); `grep -c "TagsSystemPrompt" DeckFlow.Core/Knowledge/DistillationSchemas.cs` >= 1 (computed property present).
    - `grep -cE "(Summary|Clips)SystemPrompt \{ get; \}|TagsSystemPrompt =" DeckFlow.Core/Integration/LlmDistillationService.cs` returns 0 (prompts removed from OpenAI impl).
    - `grep -cE "DistillationSchemas\.(Summary|Clips|Tags)SystemPrompt" DeckFlow.Core/Integration/LlmDistillationService.cs` returns 3 (references repointed).
    - `grep -c "private static string FormatAllowlist" DeckFlow.Core/Integration/LlmDistillationService.cs` returns 0 (helper moved) AND `grep -c "private static string FormatAllowlist" DeckFlow.Core/Knowledge/DistillationSchemas.cs` returns 1.
    - L1 GUARD (round-2 hardened): `DeckFlow.Core.Tests/DistillationPromptRegressionTests.cs` exists; `grep -c "DistillationSchemas\." DeckFlow.Core.Tests/DistillationPromptRegressionTests.cs` >= 6 (3 prompt assertions + 3 schema assertions); `grep -ci "Schema" DeckFlow.Core.Tests/DistillationPromptRegressionTests.cs` >= 1 (schema-byte assertions present).
    - `git diff DeckFlow.Core/Integration/LlmDistillationService.cs` shows only prompt-removal + reference repoint + FormatAllowlist removal — no reformatting of unrelated members.
    - `DeckFlow.Core` builds clean (0 new warnings/errors).
  </acceptance_criteria>
  <done>Prompts live once in DistillationSchemas; OpenAI impl references them; an automated byte-equality test guards drift of BOTH prompts and schema bytes (L1); build clean; raw-string bytes preserved.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add CliCommandSpec + CliLlmDistillationService (claude-only): ArgumentList spawn H1/M3 + JSON-array {instruction} override ROUND-3 HIGH-1 (fail-fast MED-1) + in-service timeout/kill via runner-seam H2 + cancellation-aware stdin + kill-on-register ROUND-3 HIGH-2 + deadlock-safe pipes MED + single-object JSON repair M2/ROUND-3 MED-2 + ValidateTags-in-retry H4 + bounded sanitized logging L2</name>
  <read_first>
    - DeckFlow.Core/Integration/FfmpegAudioChunker.cs (process spawn analog: Process.Start, async stderr read, exit-code check, Tail helper; note it uses Arguments STRING — we diverge to ArgumentList)
    - DeckFlow.Core/Integration/LlmDistillationService.cs lines 13-47 (class/const/ctor + test-seam shape), 24-28 (JsonOpts), 209-231 (ValidateSummary/ValidateClips/CountWords to reuse), 257-264 (private payload records to mirror)
    - DeckFlow.Core/Integration/ILlmDistillationService.cs (the interface to implement)
    - DeckFlow.Core/Knowledge/DistillationSchemas.cs (after Task 1 — the shared prompts + schemas to combine)
    - DeckFlow.Core/Knowledge/ContentTagVocabulary.cs (Archetypes/Brackets/CardCategories — used by ValidateTags H4)
    - .planning/phases/21.2-pluggable-llm-distill-cli-backends-inserted/21.2-RESEARCH.md lines 366-457 (verified claude .result envelope contract — single inner JSON object), 632-672 (deadlock/stdin pitfalls), 674-735 (JSON repair guide)
    - .planning/phases/21.2-pluggable-llm-distill-cli-backends-inserted/21.2-REVIEWS.md ROUND-3 section (HIGH-1 JSON-array override, HIGH-2 cancellation-aware stdin + kill-register, MED-1 fail-fast, MED-2 single-object) + H1/H2/H4/M2/L2 (round 1) + MED pipe ordering (round 2)
    - .planning/phases/21.2-pluggable-llm-distill-cli-backends-inserted/21.2-CONTEXT.md D-02 / D-02a / D-02a-override / D-02b / D-02c / D-02d / D-02e (the authoritative claude-only corrected decisions — D-02a-override is now JSON-array + fail-fast)
  </read_first>
  <behavior>
    - Test 1 (clean JSON, claude envelope): canned stdout is the claude envelope wrapping clean `{"summary":"..."}` in `.result` → SummarizeAsync returns the summary, TokenUsage(0,0).
    - Test 2 (fenced JSON inside .result): the .result text is wrapped in ```json fences → fences stripped, summary returned.
    - Test 3 (claude envelope unwrap): stdout is the full `{"type":"result","is_error":false,"result":"{\"summary\":\"...\"}",...}` → .result unwrapped, the single object parsed, summary returned.
    - Test 4 (wrapper prose / brace-in-string M2): the .result text = `Here is the result: {"summary":"a {brace} inside the string"} thanks!` → quote/escape-aware balanced-object scan extracts the single complete object including the brace-in-string; summary returned.
    - Test 5 (missing/null field M2): .result = `{"not_summary":"x"}` → null/missing-field validation fails → retried; persistent → throws.
    - Test 6 (retry): queue [garbage-result-envelope, valid-result-envelope] → succeeds on 2nd attempt.
    - Test 7 (persistent fail): queue 3× garbage → throws InvalidOperationException after 3 attempts.
    - Test 8 (claude is_error sanitized L2): envelope with `"is_error":true` and a `.result` containing transcript-like text → treated as a failed attempt (retried, then thrown if all fail); the thrown message is bounded/sanitized and does NOT echo the raw .result body.
    - Test 9 (clips): valid clips payload in .result → ExtractClipsAsync returns 3-8 clips, TokenUsage(0,0).
    - Test 10 (tags clean): valid tags payload, all values in ContentTagVocabulary → InferTagsAsync returns 3 tag dimensions, TokenUsage(0,0).
    - Test 11 (ValidateTags vocab-invalid triggers retry — H4): attempt-1 has a tag value NOT in ContentTagVocabulary (e.g. archetype "banana"); attempt-2 is all-valid → succeeds on retry (proves vocab-invalid output is NOT silently accepted). A 3×-invalid queue → throws (NOT a silent empty-tag success).
    - Test 12 (ValidateTags duplicates / null array — H4): a tags payload with a duplicate value or a null array fails validation → retried.
    - Test 13 (timeout/cancel via runner seam — H2): a fake runner that respects the linked token and never returns (Task.Delay(Timeout.Infinite, ctPassedIn)) → SummarizeAsync with a tiny injected timeout throws OperationCanceledException/TimeoutException promptly rather than hanging; CRITICAL — the caller passes CancellationToken.None and the call STILL times out (proves the in-service timeout is created in ExtractWithRetryAsync and passed INTO the runner, not only inside the real RunProcessAsync). The timeout must surface ONCE (not loop 3× and triple the hang budget).
    - Test 14 (JSON-array override substitutes {instruction} ROUND-3 HIGH-1): with DECKFLOW_LLM_CLI_COMMAND set to the JSON array `["wsl.exe","claude","-p","{instruction}","--output-format","json","--allowedTools",""]`, BuildCommandSpec parses it with JsonSerializer.Deserialize<string[]>, sets FileName=="wsl.exe", and the ArgumentList contains the instruction at the `{instruction}` position (NOT appended at the end).
    - Test 15 (JSON-array override preserves the empty arg ROUND-3 HIGH-1): for the same override, assert the ArgumentList element IMMEDIATELY AFTER `--allowedTools` is exactly `string.Empty` (proves the empty tool-disable arg is passed as an empty string, not a literal `""` and not dropped).
    - Test 16 (bad override fails fast ROUND-3 HIGH-1 + MED-1): with DECKFLOW_LLM_CLI_COMMAND set to (a) non-JSON text, (b) a JSON array with NO `{instruction}` element, (c) a JSON array with a DUPLICATE `{instruction}` element, or (d) an empty JSON array `[]` → SummarizeAsync throws a clear exception ONCE and the fake process-runner is invoked 0 times (the override is validated BEFORE the retry loop; config errors are NOT retried 3×).
    - Test 17 (token usage): any successful call returns TokenUsage(0,0).
  </behavior>
  <action>
    Create `DeckFlow.Core/Integration/CliCommandSpec.cs` (H1/M3): a `public sealed record CliCommandSpec` with members `string FileName`, `IReadOnlyList<string> ArgumentList`, `CliEnvelopeKind EnvelopeKind`. Add a `public enum CliEnvelopeKind { ClaudeJson, Raw }` (same file or co-located). EnvelopeKind drives output handling (ClaudeJson → unwrap `.result`; Raw → stdout is the model text). (Raw is included for the future codex provider in Phase 21.3 but is NOT exercised by any claude code path in this phase — claude is always ClaudeJson.) Do NOT add a WorkingDirectory member or any codex-isolation field; codex is Phase 21.3.

    Create `DeckFlow.Core/Integration/CliLlmDistillationService.cs` as `public sealed class CliLlmDistillationService : ILlmDistillationService` (D-01). Mirror `FfmpegAudioChunker` for process spawn structure and `LlmDistillationService` for the test-seam ctor + validators + payload records. Implementation requirements:

    CONSTANTS: `CliCommandEnvironmentKey = "DECKFLOW_LLM_CLI_COMMAND"`, `CliTimeoutEnvironmentKey = "DECKFLOW_LLM_CLI_TIMEOUT_SECONDS"`, `InstructionPlaceholder = "{instruction}"` (ROUND-3 HIGH-1), `ClaudeProvider = "claude"`, `MaxRetries = 3` (3 total attempts = retry ×2 per D-03a), `SummaryMaxWords = 200`, `MinClipCount = 3`, `MaxClipCount = 8`, `ErrorTailLength = 800`, `DefaultTimeout = TimeSpan.FromMinutes(10)` (env-overridable via CliTimeoutEnvironmentKey — D-02b). Copy `JsonOpts` (snake_case + case-insensitive) verbatim from LlmDistillationService:24-28.

    TEST SEAM (H2 requires the timeout to wrap the runner-seam call so the fake runner exercises it): the process-runner seam delegate is `Func<CliCommandSpec, string, CancellationToken, Task<string>>` — args are (spec, stdinBody, linkedToken). The delegate receives the FULL CliCommandSpec (carrying ArgumentList) plus the transcript that goes to stdin (H1: instruction is in the spec's ArgumentList, transcript is the stdin body) plus the LINKED, timeout-bearing token (H2). Public ctor `CliLlmDistillationService(string provider)` delegates to an internal ctor `CliLlmDistillationService(string provider, Func<CliCommandSpec,string,CancellationToken,Task<string>>? processRunnerOverride, TimeSpan? timeoutOverride = null)`. `ArgumentException.ThrowIfNullOrWhiteSpace(provider)`. Default runner = `RunProcessAsync`; resolved timeout = timeoutOverride ?? env value ?? DefaultTimeout, stored in a `_timeout` field.

    BUILD SPEC (`BuildCommandSpec(string instruction)`) — returns CliCommandSpec (H1/M3 + ROUND-3 HIGH-1 JSON-array override; claude-only):
      - If `DECKFLOW_LLM_CLI_COMMAND` is set (non-empty/non-whitespace): parse the value as a JSON ARRAY of strings with `JsonSerializer.Deserialize<string[]>` (ROUND-3 HIGH-1 — NOT a space split; a space split cannot reliably express the empty `--allowedTools ""` arg nor a Windows path with spaces). On parse failure (not valid JSON / not a string array) throw a clear `InvalidOperationException` naming the env var and the required JSON-array form. Reject an empty array `[]` (throw). element[0] = FileName. The remaining elements form the ArgumentList VERBATIM — empty-string elements are preserved EXACTLY as `string.Empty` (so the `""` element after `--allowedTools` stays an empty string, never a literal quote and never dropped). EXACTLY ONE element MUST equal the `{instruction}` placeholder token → substitute that one element with the instruction string. If ZERO elements equal `{instruction}`, throw `InvalidOperationException` stating the override must contain exactly one `{instruction}` placeholder so the instruction can be positioned (e.g. after `-p`). If MORE THAN ONE element equals `{instruction}`, throw `InvalidOperationException` stating the placeholder must appear exactly once. EnvelopeKind = ClaudeJson (claude).
      - Else on Linux/macOS auto-detect (OperatingSystem.IsLinux()/IsMacOS()): FileName="claude", ArgumentList=["-p", instruction, "--output-format", "json", "--allowedTools", ""], EnvelopeKind=ClaudeJson. (The empty-string element for --allowedTools is a real list element — ArgumentList passes it without shell-quoting hazards; this is exactly why ArgumentList is mandated over the Arguments string — H1/M3.)
      - Else (Windows, no override) → throw InvalidOperationException instructing the operator to set DECKFLOW_LLM_CLI_COMMAND as a JSON array with an {instruction} placeholder (e.g. `["wsl.exe","claude","-p","{instruction}","--output-format","json","--allowedTools",""]` or `["cmd.exe","/c","claude.cmd","-p","{instruction}","--output-format","json","--allowedTools",""]`). Do NOT silently fall back (claude is NOT on Windows PATH per RESEARCH).
      - The `instruction` argument is the combined system-prompt + schema-as-instructions string (built per-extraction from DistillationSchemas.*SystemPrompt + DistillationSchemas.*Schema, RESEARCH lines 722-735). The transcript is NEVER part of the instruction or ArgumentList — it goes to stdin. Document this as the command-injection mitigation (T-21.2-01).
      - If `_provider` is not "claude" (case-insensitive), throw — but note this is unreachable in practice because the factory routes only claude here; codex never constructs this service in 21.2.

    VALIDATE OVERRIDE UP FRONT (ROUND-3 MED-1 — fail-fast, do NOT retry config errors): the override parse + shape + placeholder validation must run ONCE BEFORE the retry loop, not inside it. Implement by computing the command-spec TEMPLATE once at the top of `ExtractWithRetryAsync` (before the for-loop) — i.e. call `BuildCommandSpec(instruction)` ONCE before the loop and reuse the resulting spec for every attempt (the spec is stateless per call, so building it once is correct and also makes the override error surface before any process is spawned). A missing/duplicate `{instruction}`, non-JSON, or empty-array override therefore throws IMMEDIATELY and is NOT caught by the broad retry catch. The acceptance test (Test 16) proves the fake runner is invoked 0 times on a bad override. Genuine parse/shape/vocab failures (from the model output) still retry ×2 inside the loop.

    RUN PROCESS (`RunProcessAsync(CliCommandSpec spec, string stdinBody, CancellationToken linkedToken)`) — H1 + H2 + ROUND-3 HIGH-2 + MED + L2:
      - Build ProcessStartInfo(spec.FileName); for each element of spec.ArgumentList call `startInfo.ArgumentList.Add(element)` (NEVER set startInfo.Arguments to a concatenated string — H1/M3). Set RedirectStandardInput/Output/Error=true, UseShellExecute=false, CreateNoWindow=true.
      - Start the process. ROUND-3 HIGH-2 (kill-on-cancel even if blocked): IMMEDIATELY after `Process.Start`, register the token to kill the tree — `using var killReg = linkedToken.Register(() => { try { if (!process.HasExited) process.Kill(entireProcessTree: true); } catch { } });` — so cancellation/timeout kills the child even if the parent is blocked on a stdin write the child never drains.
      - MED (deadlock-safe pipe ordering): start `process.StandardOutput.ReadToEndAsync(linkedToken)` AND `process.StandardError.ReadToEndAsync(linkedToken)` IMMEDIATELY after Process.Start (after the kill registration), BEFORE writing to stdin.
      - ROUND-3 HIGH-2 (cancellation-aware stdin write): write the transcript via `await process.StandardInput.BaseStream.WriteAsync(Encoding.UTF8.GetBytes(stdinBody).AsMemory(), linkedToken)` then `await process.StandardInput.BaseStream.FlushAsync(linkedToken)` then `process.StandardInput.Close()` — NOT a token-less `StandardInput.WriteAsync(string)` (a token-less write to a child that never reads stdin cannot observe the timeout). Use Encoding.UTF8 explicitly (the child reads UTF-8).
      - THEN `await process.WaitForExitAsync(linkedToken)`, then await the two read tasks. (Reads-first ordering — reads started before the stdin write — prevents a deadlock where a child writing enough stderr while the parent is still writing a large transcript blocks both sides; preserve this round-2 ordering. Do NOT revert to "write+close stdin, then start reads".)
      - H2 KILL: wrap the write/wait/read awaits in try/catch on OperationCanceledException: on cancel/timeout the linkedToken.Register above already calls `process.Kill(entireProcessTree: true)`; rethrow the OperationCanceledException (so a hung child is killed via the registration, not leaked, and the cancellation surfaces). The linkedToken carrying the timeout is created by the CALLER (ExtractWithRetryAsync) and passed in, so a caller's CancellationToken.None still gets the in-service timeout (H2).
      - Non-zero exit → throw InvalidOperationException with `Tail(stderr)` ONLY (L2: never include stdout, the prompt, the transcript, or the full command line in the message).
      - Return stdout (envelope handling is done by the caller via EnvelopeKind).

    EXTRACT MODEL TEXT (`ExtractModelText(CliEnvelopeKind kind, string stdout)`): if kind==ClaudeJson, JsonDocument.Parse(stdout); if `is_error==true` throw a BOUNDED, SANITIZED InvalidOperationException — L2: include only a short status (e.g. "claude returned is_error=true"), NOT the raw .result body (it can contain prompt/transcript snippets); return `.result` (throw if null). if kind==Raw, return stdout.Trim() (Raw branch exists for the future codex provider; unused by claude paths this phase).

    JSON REPAIR (M2 + ROUND-3 MED-2 — quote/escape-aware balanced scan extracting THE SINGLE object; no naive first-{/last-}; no multi-object scanner):
      - `FenceStrip(text)`: strip ```json ... ``` / ``` ... ``` fences.
      - `ExtractBalancedJsonObject(text)`: scan for the FIRST complete top-level JSON object using a depth counter that is quote/escape-aware — track whether inside a string (toggled on unescaped `"`), honor `\` escapes inside strings, only count `{`/`}` when NOT inside a string; return the substring from the first `{` to its matching `}`. Throw InvalidOperationException("No balanced JSON object found.") if none. (M2 fix; do NOT use IndexOf('{')/LastIndexOf('}').) ROUND-3 MED-2: claude returns a single `.result` envelope whose inner text is ONE JSON object, so extracting the single first balanced object is correct — do NOT build a multi-object scanner and do NOT claim multiple-object recovery; this recovers from fenced/enveloped/wrapper-prose wrapping of that one object.
      - Deserialize with JsonOpts; throw if null (M2 null check).

    RETRY LOOP (`ExtractWithRetryAsync<T>`): the method takes the per-extraction instruction string, the transcript, a validate delegate `Action<T>`, and the caller ct. STEP A (ROUND-3 MED-1 fail-fast): BEFORE the loop, `var spec = BuildCommandSpec(instruction);` — a bad override throws here, before any retry/spawn. STEP B (H2): create the linked, timeout-bearing token HERE so the runner seam sees it: `using var linkedCts = CancellationTokenSource.CreateLinkedTokenSource(ct); linkedCts.CancelAfter(_timeout);`. STEP C: Loop MaxRetries; per attempt: `var stdout = await _runProcess(spec, transcript, linkedCts.Token)` → `ExtractModelText(spec.EnvelopeKind, stdout)` → `ExtractBalancedJsonObject(FenceStrip(modelText))` → `JsonSerializer.Deserialize<T>(json, JsonOpts)` (null-check) → `validate(payload)` → return. Re-throw `OperationCanceledException` when `linkedCts.IsCancellationRequested` (do NOT retry a genuine cancellation/timeout — H2: a timeout must surface ONCE, not loop 3× and triple the hang budget). Catch broad `Exception` (parse/shape/vocab failures FROM THE MODEL OUTPUT) to retain `last` and retry. After the loop, throw `InvalidOperationException($"CLI extraction failed after {MaxRetries} attempts.", last)`. NOTE: re-create the linked CTS PER CALL (one timeout budget per extraction). NOTE: BuildCommandSpec is called ONCE before the loop (MED-1) — config errors fail fast and are not retried; the spec is reused for every attempt.

    VALIDATORS:
      - Copy `ValidateSummary`/`CountWords`/`ValidateClips` verbatim from LlmDistillationService:209-231.
      - NEW `ValidateTags(TagsPayload payload)` (H4): assert Archetype, Bracket, CardCategory arrays are each non-null; assert every value is in the matching ContentTagVocabulary allowlist (ContentTagVocabulary.Archetypes/Brackets/CardCategories.Contains, OrdinalIgnoreCase); assert no duplicates within a dimension (case-insensitive). Throw InvalidOperationException on any violation so the RETRY loop catches it (H4: vocab-invalid output MUST trigger retry — do NOT rely on the downstream FilterTags at CommandRunners.cs:1077-1079, which would silently drop and mark the video distilled with empty/weak tags).

    INTERFACE METHODS:
      - SummarizeAsync/ExtractClipsAsync/InferTagsAsync: `ArgumentException.ThrowIfNullOrWhiteSpace(transcript)`; build the instruction = system prompt + schema-as-instructions (DistillationSchemas.*SystemPrompt + DistillationSchemas.*Schema); call ExtractWithRetryAsync with the matching validator (ValidateSummary; ValidateClips; ValidateTags for tags — H4); map the private payload record to the result record with `new TokenUsage(0, 0)` (D-04/D-04a — subscription = no token cost). Mirror private `SummaryPayload`/`ClipsPayload`/`TagsPayload` records (LlmDistillationService:257-264).

    `Tail(text)` mirrors FfmpegAudioChunker:87-95 (bounded ErrorTailLength=800 — L2). Add XML doc-comments on the public type + public ctor; the 3 interface methods use `/// <inheritdoc />`.
    NO codex code (no codex provider branch beyond the factory error, no WorkingDirectory, no Environment.Clear, no temp dir — all Phase 21.3). NO new NuGet packages — `System.Diagnostics`, `System.Text.Json`, `System.Text` (Encoding) are BCL.
  </action>
  <verify>
    <automated>"/mnt/c/Program Files/dotnet/dotnet.exe" build DeckFlow.Core/DeckFlow.Core.csproj -c Debug 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c ": ILlmDistillationService" DeckFlow.Core/Integration/CliLlmDistillationService.cs` returns 1.
    - H1/M3 — ArgumentList used, NO Arguments-string assignment: `grep -c "ArgumentList.Add" DeckFlow.Core/Integration/CliLlmDistillationService.cs` >= 1 AND `grep -cE "\.Arguments\s*=" DeckFlow.Core/Integration/CliLlmDistillationService.cs` returns 0. `grep -c "record CliCommandSpec" DeckFlow.Core/Integration/CliCommandSpec.cs` returns 1 and it contains `ArgumentList`.
    - ROUND-3 HIGH-1 — JSON-array override: `grep -c "Deserialize<string\[\]>" DeckFlow.Core/Integration/CliLlmDistillationService.cs` >= 1 (override parsed as a JSON string array, NOT space-split); `grep -ci "Split(' ')\|Split(\" \")\|Split(' '" DeckFlow.Core/Integration/CliLlmDistillationService.cs` returns 0 (no space-split of the override); `grep -c "{instruction}" DeckFlow.Core/Integration/CliLlmDistillationService.cs` >= 1 (placeholder token). A clear throw exists for non-JSON / empty-array / missing-or-duplicate `{instruction}` (reviewer check; covered by Test 16). The empty-arg-preserved behavior is covered by Test 15 (arg after `--allowedTools` == string.Empty).
    - ROUND-3 MED-1 — fail-fast: BuildCommandSpec is called ONCE before the retry loop (reviewer check: in ExtractWithRetryAsync, `BuildCommandSpec(instruction)` appears before the `for`/`while` retry loop, not inside it) so a bad override throws before any `_runProcess` call; covered by Test 16 (fake runner invoked 0 times).
    - H1 — instruction is an argument, transcript is stdin: `grep -c "Func<CliCommandSpec" DeckFlow.Core/Integration/CliLlmDistillationService.cs` >= 1 (runner seam carries spec + stdin body + token).
    - H2 — in-service timeout via runner seam + kill tree: `grep -c "CreateLinkedTokenSource" DeckFlow.Core/Integration/CliLlmDistillationService.cs` >= 1 (created in ExtractWithRetryAsync, NOT only inside RunProcessAsync); `grep -c "CancelAfter" DeckFlow.Core/Integration/CliLlmDistillationService.cs` >= 1; `grep -c "Kill(entireProcessTree: true)" DeckFlow.Core/Integration/CliLlmDistillationService.cs` >= 1.
    - ROUND-3 HIGH-2 — kill-on-register + cancellation-aware stdin: `grep -c "linkedToken.Register(" DeckFlow.Core/Integration/CliLlmDistillationService.cs` >= 1 (kill registered immediately after Start); the stdin write passes `linkedToken` — `grep -cE "BaseStream.WriteAsync\(.*linkedToken" DeckFlow.Core/Integration/CliLlmDistillationService.cs` >= 1 AND `grep -c "AsMemory()" DeckFlow.Core/Integration/CliLlmDistillationService.cs` >= 1; NO token-less string write: `grep -cE "StandardInput.WriteAsync\(stdinBody\)|StandardInput.Write\(" DeckFlow.Core/Integration/CliLlmDistillationService.cs` returns 0.
    - MED — deadlock-safe ordering preserved: reviewer check on RunProcessAsync source order — both `ReadToEndAsync` tasks are started BEFORE the `BaseStream.WriteAsync`/`StandardInput.Close()` (reads-first ordering); `grep -cE "ReadToEndAsync" DeckFlow.Core/Integration/CliLlmDistillationService.cs` >= 2 AND `grep -c "StandardInput.Close" DeckFlow.Core/Integration/CliLlmDistillationService.cs` returns 1.
    - H4 — ValidateTags in retry: `grep -c "ValidateTags" DeckFlow.Core/Integration/CliLlmDistillationService.cs` >= 1; InferTagsAsync passes ValidateTags into ExtractWithRetryAsync.
    - M2 / ROUND-3 MED-2 — single balanced object scan, no naive brace match, no multi-object scanner: `grep -c "ExtractBalancedJsonObject\|BalancedJson" DeckFlow.Core/Integration/CliLlmDistillationService.cs` >= 1; `grep -cE "LastIndexOf\('\}'\)" DeckFlow.Core/Integration/CliLlmDistillationService.cs` returns 0.
    - L2 — bounded/sanitized: `grep -c "Tail(stderr)" DeckFlow.Core/Integration/CliLlmDistillationService.cs` >= 1; the is_error throw does NOT include the raw .result body (reviewer check: is_error message is a short status only, no `.result` interpolation).
    - CLAUDE-ONLY — no codex code: `grep -ci "codex\|Environment.Clear\|WorkingDirectory\|GetTempPath\|read-only" DeckFlow.Core/Integration/CliLlmDistillationService.cs` returns 0 (no codex spawn, no isolation, no temp dir, no sandbox flag) AND `grep -ci "WorkingDirectory" DeckFlow.Core/Integration/CliCommandSpec.cs` returns 0.
    - `grep -c "new TokenUsage(0, 0)" DeckFlow.Core/Integration/CliLlmDistillationService.cs` returns 3.
    - `grep -ci "dotnet add package\|PackageReference" DeckFlow.Core/Integration/CliLlmDistillationService.cs` returns 0.
    - `DeckFlow.Core` builds clean.
  </acceptance_criteria>
  <done>CliCommandSpec + CliLlmDistillationService implement ILlmDistillationService for claude with ArgumentList spawn (H1/M3), JSON-array {instruction} override with empty-arg preservation + fail-fast validation before the retry loop (ROUND-3 HIGH-1 + MED-1), in-service timeout via runner-seam + Kill(entireProcessTree) + linkedToken.Register kill-on-cancel + cancellation-aware BaseStream stdin write (H2 + ROUND-3 HIGH-2), deadlock-safe reads-first pipe ordering (MED), ValidateTags-in-retry (H4), quote/escape-aware single-object JSON repair (M2 / ROUND-3 MED-2), bounded sanitized logging (L2), and zero token usage; NO codex code; build clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add LlmDistillationProviderFactory (openai|claude; codex→21.3 NotSupportedException) + unit tests for factory and CLI service (incl. JSON-array override empty-arg + fail-fast ROUND-3 HIGH-1/MED-1, ValidateTags-retry H4, single-object balanced-scan M2, runner-seam timeout H2, None-can't-hang)</name>
  <read_first>
    - DeckFlow.Core/Integration/TranscriptProviderFactory.cs (the exact shape to copy — entire file)
    - DeckFlow.Core.Tests/LlmDistillationServiceTests.cs (xUnit structure, internal-ctor seam usage, type-assertion pattern)
    - DeckFlow.Core.Tests/TranscriptProviderFactoryTests.cs (factory test conventions to mirror, if present)
    - DeckFlow.Core/Integration/CliCommandSpec.cs + CliLlmDistillationService.cs (from Task 2 — the seam to drive with a fake runner)
    - .planning/phases/21.2-pluggable-llm-distill-cli-backends-inserted/21.2-PATTERNS.md lines 301-364 (factory pattern — adapt: codex throws, does NOT construct a service), 542-613 (test-case scaffolding to adapt)
    - .planning/phases/21.2-pluggable-llm-distill-cli-backends-inserted/21.2-REVIEWS.md ROUND-3 (HIGH-1 empty-arg + fail-fast, HIGH-2 cancel-aware stdin, MED-2 single-object) + M2/H2/H4 (the cases tests must cover)
  </read_first>
  <behavior>
    - Resolve(null) and Resolve("") → LlmDistillationService (OpenAI default).
    - Resolve("openai") → LlmDistillationService.
    - Resolve("claude") → CliLlmDistillationService.
    - Resolve("CLAUDE") → CliLlmDistillationService (case-insensitive).
    - Resolve("codex") → throws NotSupportedException whose message names Phase 21.3 (codex deferred, not yet supported).
    - Resolve("gemini") → throws NotSupportedException whose message lists the supported values (openai, claude).
    - Resolve("openai", null!) → throws ArgumentNullException.
    - CliLlmDistillationService tests cover the behavior cases from Task 2 (Test 1-17), including the M2 single-object wrapper-prose/brace-in-string case, the H4 ValidateTags vocab-invalid→retry and duplicate/null-array cases, the H2 runner-seam timeout (CancellationToken.None still times out), and the ROUND-3 HIGH-1/MED-1 JSON-array override cases — substitution at the {instruction} position, the empty-arg-after-`--allowedTools`==string.Empty assertion, and the bad-override fail-fast (fake runner invoked 0×) — (no real CLI spawned — fake runner only).
  </behavior>
  <action>
    Create `DeckFlow.Core/Integration/LlmDistillationProviderFactory.cs` as `public static class LlmDistillationProviderFactory`, copying `TranscriptProviderFactory.cs` shape verbatim and adapting: `public const string EnvironmentVariableName = "DECKFLOW_LLM_PROVIDER"`; private consts `OpenAiProvider="openai"`, `ClaudeProvider="claude"`, `CodexProvider="codex"`; two `Resolve` overloads (the env-reading one delegates to the value one); `ArgumentNullException.ThrowIfNull(httpClient)`; `IsNullOrWhiteSpace → OpenAiProvider` default branch; `OrdinalIgnoreCase` per-provider branch returning `new LlmDistillationService(httpClient)` for openai and `new CliLlmDistillationService(ClaudeProvider)` for claude. For `CodexProvider`: throw `NotSupportedException` with a message stating codex is deferred to Phase 21.3 / KB-12 and is not yet supported (do NOT construct a service). For any other value: `NotSupportedException` listing the currently-supported values "openai, claude". XML doc-comments on the type + public members mirroring TranscriptProviderFactory.

    Create `DeckFlow.Core.Tests/LlmDistillationProviderFactoryTests.cs` — `public sealed class LlmDistillationProviderFactoryTests` with [Fact] cases (use `using var httpClient = new HttpClient();` and `Assert.IsType<CliLlmDistillationService>(...)` / `Assert.IsType<LlmDistillationService>(...)`):
      - Resolve_NullOrEmpty_ReturnsOpenAiImpl, Resolve_OpenAi_ReturnsOpenAiImpl, Resolve_Claude_ReturnsCliImpl, Resolve_ClaudeUpper_ReturnsCliImpl (case-insensitive).
      - Resolve_Codex_ThrowsNotSupportedPointingAt213 — `Assert.Throws<NotSupportedException>` and assert the message contains "21.3" (or "KB-12").
      - Resolve_Unknown_ThrowsNotSupportedListingSupported — assert the message lists "openai" and "claude".
      - Resolve_NullHttpClient_ThrowsArgumentNullException.

    Create `DeckFlow.Core.Tests/CliLlmDistillationServiceTests.cs` — `public sealed class CliLlmDistillationServiceTests` with a private `CreateService(string provider, Queue<string> stdoutQueue, TimeSpan? timeout = null)` helper using the internal ctor `new(provider, (spec, stdin, ct) => Task.FromResult(stdoutQueue.Dequeue()), timeout)` (the fake runner discards spec + stdin; NO real CLI spawned). For tests that must inspect the CliCommandSpec (override-substitution + empty-arg), use a fake runner that captures the received `spec` into a local and returns a canned valid envelope, then assert on the captured spec. For the fail-fast test, use a fake runner that INCREMENTS a counter and returns a valid envelope; after the bad-override call throws, assert the counter is 0. Implement the behavior cases (Test 1-17) from Task 2 as `[Fact]`/`[Theory]` named `Method_Scenario_ExpectedResult`. Canned stdout for claude is always the claude envelope (`{"type":"result","is_error":false,"result":"<inner json or fenced/prose>"}`); use a small helper to wrap inner JSON in an envelope. MANDATORY review-driven cases:
      - M2 / ROUND-3 MED-2: `Summarize_WrapperProseAndBraceInString_ExtractsSingleBalancedObject` and `Summarize_MissingRequiredField_RetriesThenThrows`.
      - H4: `InferTags_VocabInvalidThenValid_RetriesAndSucceeds`; `InferTags_PersistentVocabInvalid_ThrowsNoSilentEmpty`; `InferTags_DuplicateOrNullArray_Retries`.
      - L2: `Summarize_ClaudeIsError_ThrowsSanitizedNoResultBody` — envelope with is_error:true whose .result contains a sentinel transcript string; assert the thrown message does NOT contain the sentinel.
      - H2: `Summarize_RunnerHangs_TimesOutWithoutHanging` — fake runner returns `Task.Delay(Timeout.Infinite, ct)` (respects the token passed in by the service); inject a tiny timeout via the ctor override; assert the call throws (OperationCanceledException/TimeoutException) within a bounded wait; AND `Summarize_RunnerHangs_CallerNoneStillTimesOut` — caller passes `CancellationToken.None` and the call STILL times out (proves the linked CTS is created in ExtractWithRetryAsync and passed into the runner).
      - ROUND-3 HIGH-1 (JSON-array override): `BuildSpec_JsonArrayOverrideWithPlaceholder_SubstitutesInstructionAtPosition` (set DECKFLOW_LLM_CLI_COMMAND to `["wsl.exe","claude","-p","{instruction}","--output-format","json","--allowedTools",""]`; drive a successful Summarize whose capturing fake runner asserts FileName=="wsl.exe" and the instruction sits at the `{instruction}` position) and `BuildSpec_JsonArrayOverride_PreservesEmptyAllowedToolsArg` (assert the captured spec's ArgumentList element immediately AFTER `--allowedTools` is exactly `string.Empty`).
      - ROUND-3 HIGH-1 + MED-1 (fail-fast): `BuildSpec_OverrideNonJson_ThrowsFastRunnerNotInvoked`, `BuildSpec_OverrideMissingPlaceholder_ThrowsFastRunnerNotInvoked`, `BuildSpec_OverrideDuplicatePlaceholder_ThrowsFastRunnerNotInvoked`, `BuildSpec_OverrideEmptyArray_ThrowsFastRunnerNotInvoked` — each sets the bad DECKFLOW_LLM_CLI_COMMAND, calls SummarizeAsync, asserts a clear exception is thrown AND the fake-runner invocation counter is 0 (config error not retried, validated before the loop). Use Environment.SetEnvironmentVariable to set/clear DECKFLOW_LLM_CLI_COMMAND within the test and restore it in a finally (or IDisposable) so tests stay isolated.
    Name tests `Method_Scenario_ExpectedResult`.
  </action>
  <verify>
    <automated>"/mnt/c/Program Files/dotnet/dotnet.exe" test DeckFlow.Core.Tests/DeckFlow.Core.Tests.csproj --filter "FullyQualifiedName~LlmDistillationProviderFactoryTests|FullyQualifiedName~CliLlmDistillationServiceTests|FullyQualifiedName~DistillationPromptRegressionTests" 2>&1 | tail -8 || echo "VSTEST UNRELIABLE IN WSL — fall back to build-clean + manual harness run; report result"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "public static class LlmDistillationProviderFactory" DeckFlow.Core/Integration/LlmDistillationProviderFactory.cs` returns 1.
    - `grep -c 'EnvironmentVariableName = "DECKFLOW_LLM_PROVIDER"' DeckFlow.Core/Integration/LlmDistillationProviderFactory.cs` returns 1.
    - codex→21.3: `grep -ci "21.3\|KB-12" DeckFlow.Core/Integration/LlmDistillationProviderFactory.cs` >= 1; the codex branch throws NotSupportedException and does NOT construct a CliLlmDistillationService (reviewer check: no `new CliLlmDistillationService(CodexProvider)`).
    - `grep -c "new CliLlmDistillationService(CodexProvider)" DeckFlow.Core/Integration/LlmDistillationProviderFactory.cs` returns 0 (codex never constructs a service in 21.2).
    - `grep -cE "NotSupportedException" DeckFlow.Core/Integration/LlmDistillationProviderFactory.cs` >= 1; the unknown-value message lists openai + claude.
    - `grep -c "\[Fact\]" DeckFlow.Core.Tests/LlmDistillationProviderFactoryTests.cs` >= 7.
    - `grep -cE "\[Fact\]|\[Theory\]" DeckFlow.Core.Tests/CliLlmDistillationServiceTests.cs` >= 15.
    - H4 test present: `grep -ci "VocabInvalid\|allowlist\|PersistentVocab" DeckFlow.Core.Tests/CliLlmDistillationServiceTests.cs` >= 1.
    - H2 test present (None-can't-hang): `grep -ci "TimesOut\|Hangs\|Infinite" DeckFlow.Core.Tests/CliLlmDistillationServiceTests.cs` >= 1 AND `grep -ci "None" DeckFlow.Core.Tests/CliLlmDistillationServiceTests.cs` >= 1.
    - M2 / ROUND-3 MED-2 test present: `grep -ci "Balanced\|BraceInString\|WrapperProse\|MissingRequiredField" DeckFlow.Core.Tests/CliLlmDistillationServiceTests.cs` >= 1.
    - ROUND-3 HIGH-1 override tests present: `grep -ci "JsonArrayOverride\|PreservesEmpty\|Placeholder" DeckFlow.Core.Tests/CliLlmDistillationServiceTests.cs` >= 1 AND `grep -c "string.Empty" DeckFlow.Core.Tests/CliLlmDistillationServiceTests.cs` >= 1 (empty-arg-preserved assertion).
    - ROUND-3 MED-1 fail-fast tests present: `grep -ci "ThrowsFast\|RunnerNotInvoked\|NonJson\|DuplicatePlaceholder\|EmptyArray" DeckFlow.Core.Tests/CliLlmDistillationServiceTests.cs` >= 1 (covers the 0-invocation assertion on bad override).
    - codex→21.3 factory test present: `grep -ci "Codex" DeckFlow.Core.Tests/LlmDistillationProviderFactoryTests.cs` >= 1.
    - `grep -ci "Process.Start\|ProcessStartInfo" DeckFlow.Core.Tests/CliLlmDistillationServiceTests.cs` returns 0 (no real CLI spawned — fake runner only).
    - `DeckFlow.Core.Tests` builds clean; the new test classes pass (or, if VSTest is unreliable in WSL, build is clean and a manual harness run reports green — record the method used in SUMMARY).
  </acceptance_criteria>
  <done>Factory (openai|claude; codex→21.3 error) + both new test files exist; provider selection + single-object JSON-repair (balanced-scan M2/MED-2) + ValidateTags-retry (H4) + runner-seam timeout (H2, None-can't-hang) + JSON-array override with empty-arg preservation + fail-fast (ROUND-3 HIGH-1/MED-1) + zero-usage covered by fake-runner tests; build clean; tests green (or build-clean + manual-harness fallback recorded).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| transcript text → claude subprocess (stdin) | Untrusted YouTube/podcast transcript content fed to the claude CLI via stdin |
| operator env (`DECKFLOW_LLM_CLI_COMMAND`) → CliCommandSpec FileName/ArgumentList | Operator-controlled JSON-array command + {instruction} placeholder used to launch a child process |
| claude stdout (JSON envelope) → JSON deserializer | Untrusted/unstructured model output parsed into typed payloads |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21.2-01 | Tampering / Injection | `BuildCommandSpec` / ProcessStartInfo.ArgumentList | mitigate | Command parts come ONLY from the per-provider const default or the operator-set `DECKFLOW_LLM_CLI_COMMAND`, which is parsed as a STRUCTURED JSON array of args (`JsonSerializer.Deserialize<string[]>`) — NOT a space-split string, so there is no space-split injection surface and empty args (`--allowedTools ""`) and Windows paths pass verbatim (ROUND-3 HIGH-1). element[0]=FileName; the rest=ArgumentList verbatim. The single `{instruction}` placeholder is substituted as one ArgumentList element — never shell-concatenated. Transcript content is NEVER added to FileName/ArgumentList (it goes to stdin). Args passed via `ArgumentList` (not a concatenated `Arguments` string); `UseShellExecute=false` (no shell interpolation). A malformed override (non-JSON / empty array / missing-or-duplicate placeholder) fails fast before any spawn (MED-1). (H1/M3/ROUND-3 HIGH-1/MED-1) |
| T-21.2-02 | Spoofing / Tampering | transcript → prompt injection into the claude CLI | mitigate | claude runs with `--allowedTools ""` (no tools — verified to truly disable filesystem/Bash access, unlike codex `--sandbox read-only` which is why codex is deferred to Phase 21.3). The empty tool-disable arg is preserved exactly as `string.Empty` via the JSON-array override (asserted by a test), so the tool-disable guarantee holds on the Windows/override path too. Output is shape-validated: required fields, `ValidateTags` against `ContentTagVocabulary`, summary ≤200 words, 3-8 clips. Transcripts are untrusted; malformed/vocab-invalid output is rejected and retried, then the video is marked failed (non-terminal). |
| T-21.2-03 | Denial of Service | hung/slow claude subprocess | mitigate | Per-extraction timeout (default 10 min, env-overridable) via a linked `CancellationTokenSource.CancelAfter` created in `ExtractWithRetryAsync` and PASSED INTO the runner seam; on timeout/cancel `linkedToken.Register` calls `process.Kill(entireProcessTree: true)` immediately after Start AND the stdin write is cancellation-aware (`BaseStream.WriteAsync(...AsMemory(), linkedToken)`), so the timeout interrupts even a child that never drains stdin (ROUND-3 HIGH-2). The timeout is independent of the caller token, so `CancellationToken.None` from the CLI distill path still cannot hang (H2), and a timeout surfaces ONCE (not retried). Reads-first pipe ordering (stdout+stderr ReadToEndAsync started before stdin write) prevents pipe-buffer deadlock (MED). |
| T-21.2-04 | Information Disclosure | logging transcript/prompt/stdout/command line or secrets | mitigate | Failure messages contain ONLY the exit code + bounded `Tail(stderr)` (800 chars). The claude `is_error:true` path throws a bounded, sanitized status WITHOUT the raw `.result` body (which can contain prompt/transcript snippets). Never log stdout, prompts, transcripts, or full command lines (L2). No API key is read or echoed — claude uses the operator's own OAuth subscription session. |
| T-21.2-05 | Tampering / Read-boundary | codex agent read isolation | transfer | The codex backend is NOT shipped in this phase — `DECKFLOW_LLM_PROVIDER=codex` throws a clear NotSupportedException. codex `exec`'s unproven read-boundary under untrusted transcript input (sandbox blocks writes, not reads) is explicitly deferred to Phase 21.3 / KB-12, which must prove a closed read boundary before shipping. No codex safety is claimed here. |
| T-21.2-SC | Tampering | npm/pip/cargo installs | mitigate | No packages installed this phase (BCL only — `System.Diagnostics.Process`, `System.Text.Json`, `System.Text`). RESEARCH Package Legitimacy Audit: satisfied by absence. No `[ASSUMED]/[SUS]/[SLOP]` packages → no install checkpoint required. |
</threat_model>

<verification>
- `DeckFlow.Core` + `DeckFlow.Core.Tests` build clean via `"/mnt/c/Program Files/dotnet/dotnet.exe" build` (0 new warnings/errors).
- New CliLlmDistillationServiceTests + LlmDistillationProviderFactoryTests + DistillationPromptRegressionTests pass (or build-clean + manual harness fallback per WSL VSTest constraint — record in SUMMARY).
- REGRESSION GUARD (L1): with no provider env set, the OpenAI path is unchanged — DistillationPromptRegressionTests asserts the extracted prompts AND the schema bytes are byte-identical; existing LlmDistillationServiceTests stay green.
- H1/M3: `grep -cE "\.Arguments\s*=" DeckFlow.Core/Integration/CliLlmDistillationService.cs` returns 0; ArgumentList used.
- ROUND-3 HIGH-1: `grep -c "Deserialize<string\[\]>" ...` >= 1 (JSON-array override); empty-arg-preserved test asserts arg after `--allowedTools` == string.Empty; bad-override fail-fast test asserts fake runner invoked 0×.
- ROUND-3 HIGH-2: `grep -c "linkedToken.Register(" ...` >= 1 and the stdin write passes `linkedToken` via `BaseStream.WriteAsync(...AsMemory(), linkedToken)`; reads-first ordering preserved.
- ROUND-3 MED-1 / MED-2 / H2 / H4 / M2 / L2 acceptance greps (Task 2) all pass; CLAUDE-ONLY grep (no codex/WorkingDirectory/Environment.Clear/temp dir) passes.
- No new NuGet packages (verify `git diff` shows no `.csproj` PackageReference changes).
- Touch-only-what-you-touch: `git diff DeckFlow.Core/Integration/LlmDistillationService.cs` shows only prompt-removal + reference repoint, no reformatting.
</verification>

<success_criteria>
- KB-10 partially satisfied: `LlmDistillationProviderFactory.Resolve` selects openai (default) / claude, throws for codex pointing at Phase 21.3, and rejects unknowns.
- KB-11 partially satisfied: the claude CLI backend launches cross-platform via ArgumentList (Linux defaults + Windows JSON-array {instruction}-placeholder env-override + error path) with best-effort-JSON repair (single-object balanced scan), in-service timeout/kill via the runner seam + cancellation-aware stdin, ValidateTags, and retry; ledger bypass is realized as zero TokenUsage (the CommandRunners cap-gate bypass lands in Plan 02).
- ROADMAP SC1 (factory selection, openai byte-identical — automated L1, codex→21.3 error), SC2 (claude CLI 3 extractions + JSON repair + ValidateTags-in-retry), SC6 (unit tests via fake-process seam, timeout None-can't-hang) addressed at the unit level. All HIGH (H1/H2/H4) + ROUND-3 HIGH-1 (JSON-array override) + ROUND-3 HIGH-2 (cancel-aware stdin/kill-register) + ROUND-3 MED-1 (fail-fast) + ROUND-3 MED-2 (single-object) + MED-deadlock + M2 + L1/L2 resolved here; M1/M4 land in Plan 02 (M3 ArgumentList resolved here).
</success_criteria>

<output>
Create `.planning/phases/21.2-pluggable-llm-distill-cli-backends-inserted/21.2-01-SUMMARY.md` when done. Record: the L1 byte-preservation verification method (prompts + schema constants asserted by DistillationPromptRegressionTests), confirmation that NO codex code was implemented (factory throws → Phase 21.3), the JSON-array override parsing approach + empty-arg test result, the fail-fast (before-loop) override validation, and whether tests ran via VSTest or a manual harness fallback.
</output>
