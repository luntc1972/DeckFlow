---
phase: 111-cut-lab-upgrade-regression-gate
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/111-cut-lab-upgrade-regression-gate/111-GATES.md
  - .planning/phases/111-cut-lab-upgrade-regression-gate/111-COVERAGE-MATRIX.md
  - DeckFlow.Web/ts-tests/cut-lab-combo-package-copy.test.ts
autonomous: true
requirements: [CLUP-10]
must_haves:
  truths:
    - "Every CLUP-10 changed surface (pool filters/search, collapse state, anchors, oracle disclosures, combo labels, package helper copy) has an explicitly-named, passing focused test."
    - "The canonical full Cut Lab gate command list (xUnit, Vitest, TypeScript compile, focused e2e) is documented as a runnable artifact and all commands pass."
  artifacts:
    - path: ".planning/phases/111-cut-lab-upgrade-regression-gate/111-COVERAGE-MATRIX.md"
      provides: "Surface -> test-file:test-name map proving each CLUP-10 surface is covered"
      contains: "CLUP-10"
    - path: ".planning/phases/111-cut-lab-upgrade-regression-gate/111-GATES.md"
      provides: "Canonical runnable gate command sequence for the Cut Lab regression suite"
      contains: "npm test"
    - path: "DeckFlow.Web/ts-tests/cut-lab-combo-package-copy.test.ts"
      provides: "Focused smoke filling any real gap in combo-label / package-helper-copy assertions"
      contains: "cut-lab"
  key_links:
    - from: "111-GATES.md"
      to: "dotnet test + npm test + dotnet build (tsc) + playwright"
      via: "documented command sequence executed end to end"
      pattern: "dotnet test"
---

<objective>
Prove CLUP-10: the changed Cut Lab surfaces are covered by focused xUnit / Vitest /
TypeScript-compile / browser smoke, and the full relevant gate suite passes. Most surfaces
already have coverage (Phases 108-110.1 shipped with tests); this plan AUDITS that coverage
into an explicit matrix, fills only genuine gaps, and captures the canonical runnable gate
list so the phase's "full-suite verification" claim is reproducible.

Purpose: CLUP-10 is a completeness/reproducibility requirement, not a feature. The risk is a
surface that changed during the upgrade but lacks a named regression test, and a gate list
that lives only in maintainers' heads. This plan makes both explicit.

Output: A coverage matrix, a runnable gate-command artifact, and (only if a true gap exists)
one focused Vitest spec.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md

<interfaces>
Existing coverage already present (verified) - map these in the matrix, do NOT duplicate:
  - Pool filters/search: DeckFlow.Web/ts-tests/cut-lab-pool-filter.test.ts
      "hides non-matching pool rows without detaching them or changing whole-pool
       serialization/counts"; e2e cut-lab-nav-themes.spec.ts asserts .cutlab-pool-filter /
       .cutlab-pool-search / .cutlab-pool-match-count visibility.
  - Collapse state: DeckFlow.Web/ts-tests/cut-lab-section-collapse.test.ts,
      cut-lab-mobile-collapse.test.ts; e2e cut-lab-nav-themes.spec.ts no-JS collapse toggles.
  - Anchors: DeckFlow.Web/ts-tests/cut-lab-jump-nav.test.ts ("opens a collapsed target before
      scrolling and moves focus to it", reduced-motion, submit-tab guard); e2e nav-themes
      anchor-nav sticky + no-JS hash nav.
  - Oracle disclosures (card text): DeckFlow.Web/ts-tests/cut-lab-structural-cardtext.test.ts;
      e2e nav-themes .cutlab-card-text summary/body toggle.
  - Combo labels: DeckFlow.Web/ts-tests/cut-lab-proposal.test.ts, cut-lab-adjust.test.ts,
      cut-lab-whatif.test.ts; xUnit CutLabViewModelWordingTests.From_MergesComboProtectedFindingsIntoOneGroup,
      From_AssignsComboBadgeMapForInitialRender.
  - Package helper copy: DeckFlow.Web/ts-tests/cut-lab-lock-interactions.test.ts
      ("computes package checkbox states for checked, unchecked, and mixed members");
      xUnit CutLab* tests.

Gate command facts (from .github/workflows/ci.yml + DeckFlow.Web/package.json + playwright.config.ts):
  - TypeScript compile is enforced by the C# build (CompileTypeScriptAssets MSBuild target,
    strict tsc). Standalone: cd DeckFlow.Web && node ./node_modules/typescript/bin/tsc -p tsconfig.json --noEmit
  - xUnit: dotnet test DeckFlow.sln -c Release --no-build   (filter: --filter "FullyQualifiedName~CutLab")
  - Vitest: cd DeckFlow.Web && npm test   (filter: npm test -- <spec-substring>)
  - e2e: cd DeckFlow.Web && npm run e2e   (WSL: launch scripts/run-web-test.sh first, then
    env -u DISPLAY -u WAYLAND_DISPLAY npx --no-install playwright test e2e/cut-lab-*.spec.ts)
  - Windows dotnet path in WSL: "/mnt/c/Program Files/dotnet/dotnet.exe". Do NOT set MTG_DATA_DIR.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the CLUP-10 coverage matrix (audit + gap flag)</name>
  <files>.planning/phases/111-cut-lab-upgrade-regression-gate/111-COVERAGE-MATRIX.md</files>
  <action>
    Grep/enumerate the actual test files to confirm each CLUP-10 surface has a named,
    currently-passing test, and write 111-COVERAGE-MATRIX.md as a table with columns:
    Surface | Layer (xUnit/Vitest/e2e) | File | Test name | Status (COVERED / GAP). Cover all
    six surfaces: pool filters/search, collapse state, anchors, oracle disclosures, combo
    labels, package helper copy. Use the interfaces list above as the starting map but VERIFY
    each by opening the file (do not trust the list blindly). For any surface with no direct
    assertion of the specific behavior, mark GAP and name exactly what assertion is missing.
    This matrix is the input to Task 2.
  </action>
  <verify>
    <automated>test -f .planning/phases/111-cut-lab-upgrade-regression-gate/111-COVERAGE-MATRIX.md && grep -q "CLUP-10" .planning/phases/111-cut-lab-upgrade-regression-gate/111-COVERAGE-MATRIX.md && grep -Eq "COVERED|GAP" .planning/phases/111-cut-lab-upgrade-regression-gate/111-COVERAGE-MATRIX.md</automated>
  </verify>
  <done>Matrix lists all six surfaces with a concrete file:test and COVERED/GAP status each.</done>
</task>

<task type="auto">
  <name>Task 2: Fill genuine gaps (combo-label + package-helper-copy focused smoke)</name>
  <files>DeckFlow.Web/ts-tests/cut-lab-combo-package-copy.test.ts</files>
  <action>
    ONLY for surfaces the Task 1 matrix marked GAP, add a focused Vitest smoke in a new file
    cut-lab-combo-package-copy.test.ts (jsdom + DeckFlowCutLab init, same pattern as existing
    cut-lab-*.test.ts). Most likely genuinely-thin spots to check and, if missing, assert
    directly: (a) a rendered proposal/finding for a combo card shows the combo label/badge text
    ("Combo piece" for CompletePiece, the context string for NeedsPartner) - reuse the fixture
    shape from cut-lab-proposal.test.ts; (b) the package helper copy string (the "lock the
    whole package" helper text / package chip label) renders for a multi-member package. If
    Task 1 finds NO real gap for a surface, do NOT invent a test - instead record in the
    SUMMARY that the surface was already covered and cite the existing test. If neither (a)
    nor (b) is a gap, this file may be a single smoke that re-asserts the combo label and
    package copy render (belt-and-suspenders) rather than being skipped, so CLUP-10 has one
    dedicated combo/package artifact. Preserve LF endings; match existing TS style.
  </action>
  <verify>
    <automated>cd DeckFlow.Web && npm test -- cut-lab-combo-package-copy</automated>
  </verify>
  <done>Every GAP from the matrix is closed by a named passing test; combo-label + package-copy each have an explicit assertion.</done>
</task>

<task type="auto">
  <name>Task 3: Author + run the canonical full-gate command list</name>
  <files>.planning/phases/111-cut-lab-upgrade-regression-gate/111-GATES.md</files>
  <action>
    Write 111-GATES.md documenting the exact, copy-pasteable command sequence that constitutes
    "full Cut Lab regression verification" for this phase, in run order:
      1. TypeScript compile (strict): cd DeckFlow.Web && node ./node_modules/typescript/bin/tsc -p tsconfig.json --noEmit
      2. Build (also compiles TS via MSBuild): "/mnt/c/Program Files/dotnet/dotnet.exe" build DeckFlow.sln -c Release
      3. xUnit Cut Lab filter: "/mnt/c/Program Files/dotnet/dotnet.exe" test DeckFlow.sln -c Release --no-build --filter "FullyQualifiedName~CutLab"
         (note: WSL VSTest can be flaky; fallback = full CI run dotnet test DeckFlow.sln -c Release)
      4. Vitest Cut Lab specs: cd DeckFlow.Web && npm test -- cut-lab
      5. Focused e2e (launch ONE headless server, then reuse it for all specs):
         if the server is not already up, bash scripts/run-web-test.sh (headless); then
         cd DeckFlow.Web && env -u DISPLAY -u WAYLAND_DISPLAY npx --no-install playwright test
         e2e/cut-lab-smoke.spec.ts e2e/cut-lab-structure.spec.ts e2e/cut-lab-pill-interactions.spec.ts
         e2e/cut-lab-scenarios.spec.ts e2e/cut-lab-tuning.spec.ts e2e/cut-lab-whatif.spec.ts
         e2e/cut-lab-export.spec.ts e2e/cut-lab-nav-themes.spec.ts
      (MED-1) SEQUENCING: run-web-test.sh runs `fuser -k 5173/tcp` on start, which kills any
      sibling plan's server. This is a Wave-1 e2e-bearing plan; the three Wave-1 e2e-bearing
      plans (111-01 Task 3, 111-02 Task 3, 111-03 Task 2) MUST be executed sequentially, NOT
      concurrently. Prefer reusing an already-running :5173 server (Playwright reuseExistingServer
      is true in WSL) over launching a second one. Document this sequencing requirement inside
      111-GATES.md as well.
      Include the hard constraints as inline notes: never open a Windows browser (run-web-test.sh
      sets DECKFLOW_DISABLE_AUTO_BROWSER=true), no gstack, no new packages, do NOT set
      MTG_DATA_DIR. Then EXECUTE the sequence once (TS compile + build + vitest cut-lab + at
      least cut-lab-smoke e2e; xUnit via CI if WSL VSTest is unstable) and record the observed
      pass/fail results at the bottom of 111-GATES.md.
  </action>
  <verify>
    <automated>cd DeckFlow.Web && node ./node_modules/typescript/bin/tsc -p tsconfig.json --noEmit && npm test -- cut-lab</automated>
  </verify>
  <done>111-GATES.md exists with the runnable sequence + recorded results; tsc clean and all cut-lab Vitest specs green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness -> app under test | Documentation + focused smoke only; no production code path altered. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-111-02 | Information disclosure | 111-GATES.md commands | accept | Commands use local placeholder admin creds already public in scripts; no secrets recorded. |
| T-111-SC | Tampering | npm/NuGet installs | mitigate | No new packages; reuse existing test tooling. Package add requires user approval (blocked). |
</threat_model>

<verification>
1. `cd DeckFlow.Web && node ./node_modules/typescript/bin/tsc -p tsconfig.json --noEmit` (strict TS compile clean).
2. `cd DeckFlow.Web && npm test -- cut-lab` (all Cut Lab Vitest specs green, including any gap-fill spec).
3. Matrix + gates artifacts exist and enumerate all six surfaces / the full command list.
4. xUnit Cut Lab filter green (via Windows dotnet.exe locally, or CI fallback if WSL VSTest is unstable).
</verification>

<success_criteria>
- All six CLUP-10 surfaces have a named, passing focused test (matrix proves it; no silent gaps).
- The canonical gate sequence is documented and reproducibly passes.
- No new packages; TS compiles strict; LF + format gate clean.
</success_criteria>

<output>
Create `.planning/phases/111-cut-lab-upgrade-regression-gate/111-02-SUMMARY.md` when done.
</output>
