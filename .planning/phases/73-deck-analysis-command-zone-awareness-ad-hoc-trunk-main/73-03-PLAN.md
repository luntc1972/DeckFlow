---
phase: 73-deck-analysis-command-zone-awareness-ad-hoc-trunk-main
plan: 03
type: execute
wave: 3
depends_on: ["73-02"]
files_modified:
  - DeckFlow.Web/Services/PromptBuilders/Analysis/ChatGptAnalysisPromptVariant.cs
  - DeckFlow.Web/Services/PromptBuilders/Analysis/GeminiAnalysisPromptVariant.cs
  - DeckFlow.Web/Services/PromptBuilders/Analysis/ClaudeAnalysisPromptVariant.cs
  - DeckFlow.Web.Tests/DeckAnalysisPacketServiceTests.cs
autonomous: true
requirements: [SPEC-72-E]

must_haves:
  truths:
    - "With the flag ON and a companion present, the ChatGPT prompt renders a `companion:` line in DECK CONTEXT"
    - "With the flag ON and a companion present, the Gemini prompt renders a `companion:` line in DECK CONTEXT"
    - "With the flag ON and a companion present, the Claude prompt renders a `<companion>` tag plus a companion note"
    - "The companion is rendered ONLY in DECK CONTEXT / side metadata — it is never injected into the mainboard deck text"
    - "When companionName is null (flag OFF), none of the three variants emit any companion text (byte-identical)"
  artifacts:
    - path: "DeckFlow.Web/Services/PromptBuilders/Analysis/ChatGptAnalysisPromptVariant.cs"
      provides: "companion: line rendered when companionName non-empty"
      contains: "companion: "
    - path: "DeckFlow.Web/Services/PromptBuilders/Analysis/ClaudeAnalysisPromptVariant.cs"
      provides: "<companion> XML tag + note rendered when companionName non-empty"
      contains: "<companion>"
    - path: "DeckFlow.Web/Services/PromptBuilders/Analysis/GeminiAnalysisPromptVariant.cs"
      provides: "companion: line rendered when companionName non-empty"
      contains: "companion: "
  key_links:
    - from: "variant.Build companionName parameter"
      to: "DECK CONTEXT / <companion> output"
      via: "guarded AppendLine when non-empty"
      pattern: "companionName"
---

<objective>
Render the command-zone awareness in the prompt text. The enriched `commanderName` (Plan 02) already
flows through the existing `commander:`/`<commander>`/title lines, so partner pairs and Background
already appear after Plan 02. This plan adds the COMPANION field to each of the three variants — three
independent hand-edits per ADR 0001 (`docs/decisions/0001-prompt-variants-decoupled.md`): NO shared
helper, each variant owns its own companion text in its platform-native format.

The companion stays side metadata: it is rendered only in the DECK CONTEXT block (ChatGPT/Gemini) or
as a `<companion>` tag (Claude). It is never added to the mainboard deck text — the companion entry
remains an inert mainboard card (research §Pitfall 4); the new field clarifies its command-zone role.

Purpose: complete the user-visible awareness — the AI now sees the companion named explicitly.
Output: companion rendering in all 3 variants + per-platform render tests + companion-not-in-deck-text
assertion. Byte-identical preserved when companionName is null.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/73-deck-analysis-command-zone-awareness-ad-hoc-trunk-main/73-RESEARCH.md
@docs/decisions/0001-prompt-variants-decoupled.md
@DeckFlow.Web/Services/PromptBuilders/Analysis/ChatGptAnalysisPromptVariant.cs
@DeckFlow.Web/Services/PromptBuilders/Analysis/ClaudeAnalysisPromptVariant.cs
@DeckFlow.Web/Services/PromptBuilders/Analysis/GeminiAnalysisPromptVariant.cs

<interfaces>
<!-- Edit anchors. Extracted from codebase 2026-06-27. companionName param added in Plan 01. -->

ChatGpt (ChatGptAnalysisPromptVariant.cs): DECK CONTEXT block lines 57-67; the `commander:` line is
  emitted at lines 60-63 (builder.AppendLine($"commander: {commanderName}")). Companion line goes
  immediately after.
Gemini (GeminiAnalysisPromptVariant.cs): DECK CONTEXT block lines 63-72; `commander:` at lines 65-68.
  Companion line goes immediately after.
Claude (ClaudeAnalysisPromptVariant.cs): `<commander>` tag at lines 58-62. Companion tag + note go
  immediately after, before the <bracket> block.

ADR 0001: each variant owns its complete text; wording may differ per platform format; do not extract
shared companion text.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Render companion in ChatGpt and Gemini DECK CONTEXT (two independent edits)</name>
  <files>DeckFlow.Web/Services/PromptBuilders/Analysis/ChatGptAnalysisPromptVariant.cs, DeckFlow.Web/Services/PromptBuilders/Analysis/GeminiAnalysisPromptVariant.cs</files>
  <read_first>
    - DeckFlow.Web/Services/PromptBuilders/Analysis/ChatGptAnalysisPromptVariant.cs lines 44-72 (DECK CONTEXT + commander line)
    - DeckFlow.Web/Services/PromptBuilders/Analysis/GeminiAnalysisPromptVariant.cs lines 46-74 (DECK CONTEXT + commander line)
    - 73-RESEARCH.md §"Pattern 5" (ChatGpt + Gemini companion line copy)
  </read_first>
  <action>
    In BOTH variants, immediately AFTER the existing `commander:` AppendLine in the DECK CONTEXT block,
    add a guarded companion line: `if (!string.IsNullOrWhiteSpace(companionName)) { builder.AppendLine($"companion: {companionName} (outside the 99; applies its companion deckbuilding restriction; costs 3 generic to bring to hand before casting)"); }`.
    Hand-edit each file separately — do NOT factor the line into a shared helper or constant (ADR 0001).
    The wording may be identical across these two; that is acceptable per ADR 0001 (independent edits
    that happen to match). When `companionName` is null/whitespace nothing is emitted, preserving the
    flag-OFF byte. Touch only the added lines.
  </action>
  <verify>
    <automated>dotnet.exe test DeckFlow.Web.Tests/DeckFlow.Web.Tests.csproj --filter "DeckAnalysisPacketServiceTests" -q</automated>
  </verify>
  <acceptance_criteria>
    - ChatGptAnalysisPromptVariant.cs emits a `companion: ` line guarded by `!string.IsNullOrWhiteSpace(companionName)` directly after the commander line
    - GeminiAnalysisPromptVariant.cs emits the equivalent `companion: ` line guarded the same way
    - Neither edit references a shared helper/constant for the companion text
    - Existing flag-OFF byte-identity test still passes (companionName null → no companion line)
  </acceptance_criteria>
  <done>ChatGPT and Gemini each render a `companion:` line in DECK CONTEXT when a companion is present, independently hand-edited, with the flag-OFF byte preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Render companion as Claude XML tag + note, and add per-platform companion render tests</name>
  <files>DeckFlow.Web/Services/PromptBuilders/Analysis/ClaudeAnalysisPromptVariant.cs, DeckFlow.Web.Tests/DeckAnalysisPacketServiceTests.cs</files>
  <read_first>
    - DeckFlow.Web/Services/PromptBuilders/Analysis/ClaudeAnalysisPromptVariant.cs lines 53-75 (<role> + <commander> + <bracket>)
    - 73-RESEARCH.md §"Pattern 5" (Claude <companion> + <companion_note> copy) and §"Code Examples" Example 3
    - DeckFlow.Web.Tests/DeckAnalysisPacketServiceTests.cs lines 850-910 (test structure), 1463-1517 (fixtures, FlattenPacketText), and the partner-pair fixture added in Plan 02
  </read_first>
  <action>
    In ClaudeAnalysisPromptVariant.cs, immediately AFTER the `<commander>` block (lines 58-62) and
    before the `<bracket>` block, add a guarded companion block: when `companionName` is non-empty,
    AppendLine `$"<companion>{companionName}</companion>"` followed by a `<companion_note>` line
    explaining the companion is outside the 99 and requires its companion restriction (costs 3 generic
    to move to hand before casting), then a blank line to match the surrounding spacing. Hand-edit
    independently — Claude uses XML tags (its native format), not the `companion:` line ChatGPT/Gemini
    use (ADR 0001). Preserve raw-string literals and LF (carve-outs).
    Then add tests to DeckAnalysisPacketServiceTests.cs (use PacketBytes/FlattenPacketText where
    comparing bytes):
    (1) `BuildAsync_CommandZoneAwareness_RendersCompanion` — flag ON + companion fixture; for the
    default/ChatGpt platform assert `AnalysisPromptText` contains `companion: ` with the companion name,
    AND assert the companion name does NOT appear under the Mainboard/decklist section (companion stays
    out of deck text). Parameterize or duplicate the assertion for Gemini (`companion: `) and Claude
    (`<companion>`) by setting `request.TargetAiPlatform` to each platform.
  </action>
  <verify>
    <automated>dotnet.exe test DeckFlow.Web.Tests/DeckFlow.Web.Tests.csproj --filter "DeckAnalysisPacketServiceTests" -q</automated>
  </verify>
  <acceptance_criteria>
    - ClaudeAnalysisPromptVariant.cs emits `<companion>{companionName}</companion>` + a `<companion_note>` line, guarded by `!string.IsNullOrWhiteSpace(companionName)`, between `<commander>` and `<bracket>`
    - No shared helper used for the Claude companion text
    - Test `BuildAsync_CommandZoneAwareness_RendersCompanion` asserts the companion appears in the prompt for ChatGpt (`companion: `), Gemini (`companion: `), and Claude (`<companion>`)
    - The same test asserts the companion name is absent from the mainboard deck-text section
    - `dotnet.exe test --filter "DeckAnalysisPacketServiceTests"` exits 0
  </acceptance_criteria>
  <done>Claude renders the companion as a native `<companion>` tag with an explanatory note, and a per-platform test proves all three variants surface the companion in DECK CONTEXT / side metadata while keeping it out of the deck text.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| companionName / commanderName → variant prompt text | Untrusted names rendered into the AI prompt artifact |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-73-01 | Tampering | Companion name rendered into prompt text | mitigate | Name is already trimmed + 200-char capped upstream by `BoundCompanionName` (Plan 02). Variants render plain text to the AI — NOT HTML — so no HTML-encoding is applied or needed (research §Security Domain). |
| T-73-04 | Tampering | Companion injected into deck text instead of side metadata | mitigate | Companion is rendered only in DECK CONTEXT / `<companion>`; the render test asserts the companion name is absent from the mainboard deck-text section (research §Pitfall 4). |
| T-73-SC | Tampering | npm/pip/cargo installs | accept | No package-manager installs in this phase. |
</threat_model>

<verification>
- `dotnet.exe test --filter "DeckAnalysisPacketServiceTests"` exits 0 (companion render across 3 platforms + companion-not-in-deck-text + Plan 02 flag-OFF byte-identity still green).
- `dotnet.exe build` clean, no new warnings; format-gate clean on changed lines.
- Manual spot-read: all three variants hand-edited independently; no shared companion helper introduced (ADR 0001 compliance).
</verification>

<success_criteria>
- All three variants render the companion in their native format when companionName is present.
- Companion never appears in the mainboard deck text.
- companionName null → no companion text in any variant (byte-identical).
- No shared helper extracted for the companion text.
</success_criteria>

<output>
Create `.planning/phases/73-deck-analysis-command-zone-awareness-ad-hoc-trunk-main/73-03-SUMMARY.md` when done.
</output>
