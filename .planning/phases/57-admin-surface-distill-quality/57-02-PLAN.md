---
phase: 57-admin-surface-distill-quality
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - DeckFlow.Core/Knowledge/DistillationSchemas.cs
  - DeckFlow.Core.Tests/DistillationPromptRegressionTests.cs
autonomous: true
requirements: [DIST-01]
user_setup: []

must_haves:
  truths:
    - "The summary prompt instructs the model to produce a paste-ready, deckbuilding-focused summary for an AI chatbot (not plot/host/sponsor recap)"
    - "The clips prompt instructs the model to select on-topic clips (named card + reason, or stated heuristic/decision) and avoid generic clips"
    - "The tags prompt instructs the model to tag only DOMINANT topics with explicit per-dimension caps (tag parsimony)"
    - "The JSON schemas and DistillationValidation rules are unchanged — reworked prompts still emit JSON parseable within the existing contract"
  artifacts:
    - path: "DeckFlow.Core/Knowledge/DistillationSchemas.cs"
      provides: "Reworked Summary / Classification / Clips / Tags system prompts (prose only)"
      contains: "SummarySystemPrompt"
    - path: "DeckFlow.Core.Tests/DistillationPromptRegressionTests.cs"
      provides: "Updated SystemPrompts fixture matching reworked prompts; schema fixtures unchanged"
      contains: "SystemPrompts_MatchShippedPhase21Fixtures"
  key_links:
    - from: "DistillationSchemas system prompts"
      to: "CliLlmDistillationService.BuildInstruction"
      via: "{SystemPrompt}\\n\\n...schema concatenation (unchanged)"
      pattern: "SummarySystemPrompt|ClipsSystemPrompt|TagsSystemPrompt"
    - from: "DistillationPromptRegressionTests"
      to: "DistillationSchemas.*Schema"
      via: "ResponseFormatSchemas fixture (must stay unchanged)"
      pattern: "ResponseFormatSchemas_MatchShippedPhase21Fixtures"
---

<objective>
Rework the four transcript->KB distill system prompts (DIST-01) to produce measurably better paste-ready KB entries: tag parsimony/relevance, on-topic clip selection, and deckbuilding-focused paste-ready summaries — using the current distill providers (no model/provider swap).

Purpose: The observed quality problems (over-tagging — all 11 card-category tags applied; generic/off-topic clips; summaries not paste-ready) degrade the KB. This is prompt-tuning only; the JSON schemas and validation contract are unchanged.
Output: Edited prose inside the four system-prompt strings in `DistillationSchemas.cs`, plus an intentional update to the prompt-regression fixture so it matches the new prompts. The schema fixtures and `DistillationValidation` rules stay byte-identical.

NOTE: Success Criterion 2 (before/after quality on real harvested content) is an operator inspection gated in Phase 58 (DOGFOOD-01) and is NOT auto-verifiable here. This plan's automated gate is: prompt text contains the specific new instructions (source assertions) AND existing Core distillation/schema tests stay green (contract preserved).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/57-admin-surface-distill-quality/57-RESEARCH.md
@.planning/phases/57-admin-surface-distill-quality/57-PATTERNS.md
@.planning/phases/57-admin-surface-distill-quality/57-VALIDATION.md
@CLAUDE.md

<interfaces>
<!-- Contract the executor must preserve. -->

DeckFlow.Core/Knowledge/DistillationSchemas.cs (the ONLY production file to edit):
  - SummarySystemPrompt        { get; } = """..."""   (raw-string literal, ~lines 55-59)  -> EDIT prose only
  - ClassificationSystemPrompt { get; } = """..."""   (raw-string literal, ~lines 62-68)  -> EDIT prose only
  - ClipsSystemPrompt          { get; } = """..."""   (raw-string literal, ~lines 71-77)  -> EDIT prose only
  - TagsSystemPrompt           { get; } = "..." + ... (string concatenation, ~lines 80-87) -> EDIT freely (NOT a raw literal)
  - SummarySchema / ClassificationSchema / ClipsSchema / TagsSchema  -> DO NOT TOUCH (JSON contract)
  - FormatAllowlist(...) private helper -> DO NOT TOUCH

DeckFlow.Core/Knowledge/DistillationValidation.cs (DO NOT EDIT — must keep passing):
  - 3-8 clips required; summary <= 200 words; tags sanitized to allowlist (SanitizeClips / SanitizeTags)

DeckFlow.Core.Tests/DistillationPromptRegressionTests.cs:
  - SystemPrompts_MatchShippedPhase21Fixtures  -> currently pins only Summary/Clips/Tags (NOT Classification).
    WILL FAIL after the rework; UPDATE the existing expectedSummaryPrompt / expectedClipsPrompt / expectedTagsPrompt
    fixtures AND ADD a new expectedClassificationPrompt fixture +
    `Assert.Equal(expectedClassificationPrompt, DistillationSchemas.ClassificationSystemPrompt)` so the reworked
    ClassificationSystemPrompt is pinned too (intentional, same commit — closes a coverage gap flagged in peer review).
  - ResponseFormatSchemas_MatchShippedPhase21Fixtures -> MUST stay green WITHOUT editing it (proves schema contract unchanged).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rework the four distill system prompts (prose only) and realign the regression fixture</name>
  <files>DeckFlow.Core/Knowledge/DistillationSchemas.cs, DeckFlow.Core.Tests/DistillationPromptRegressionTests.cs</files>
  <read_first>
    - DeckFlow.Core/Knowledge/DistillationSchemas.cs (all four prompts + all four schemas — must see what NOT to touch)
    - DeckFlow.Core/Knowledge/DistillationValidation.cs (3-8 clips, 200-word, allowlist rules — the contract the new prompts must keep satisfiable)
    - DeckFlow.Core.Tests/DistillationPromptRegressionTests.cs (both facts — the SystemPrompts fixture to update and the schema fixture that must NOT change)
    - DeckFlow.Core/Integration/CliLlmDistillationService.cs lines 397-403 (BuildInstruction concatenation — confirms prompts feed in verbatim; do not change this file)
    - 57-RESEARCH.md "Pattern 3: Distill Prompt Rework" + "Recommended prompt improvements" (the intended instruction content)
  </read_first>
  <action>
    Edit ONLY the prose text inside the four system-prompt strings in `DistillationSchemas.cs`. Apply the RESEARCH-recommended improvements:

    SummarySystemPrompt (raw-string literal): rewrite the prose so it (a) states the audience is a cEDH/Commander deckbuilder who will paste the summary into an AI chatbot for deck advice, (b) requires the summary to emphasize specific card names, deckbuilding decisions, and stated principles/heuristics, cuts and includes, (c) explicitly excludes plot/host-personality/sponsor recap, and (d) keeps the existing "200 words or fewer, plain prose, grounded only in the transcript" instruction (matches `DistillationValidation` 200-word cap).

    ClassificationSystemPrompt (raw-string literal): strengthen the KEEP gate so a transcript with at least one substantial deckbuilding lesson (named cards with reasoning, slot/cut philosophy) qualifies as KEEP; keep the DROP criteria (trivia/quiz/news/promo with no actionable deckbuilding application); keep the "when in doubt, keep" fallback.

    ClipsSystemPrompt (raw-string literal): keep "3 to 8 ... key clips" (do NOT raise the minimum above 3 — Pitfall 4). Keep the non-zero integer `timestamp_seconds` instruction. Add selectivity: prefer clips where a specific card is named WITH a reason, or a heuristic/decision is stated; prefer clips from the middle ~80% of runtime; avoid intros/housekeeping/sponsor reads/closers; penalize generic advice with no specific application. Keep "grounded only in the transcript".

    TagsSystemPrompt (string concatenation — freely editable, NOT a raw literal): keep "Choose ONLY from these allowlists — do not invent new values" and keep the three `$"...{FormatAllowlist(...)}..."` interpolated allowlist segments (Archetype/Bracket/Card category) exactly as the gate. Add: "Tag only the DOMINANT topics; if a category is merely mentioned in passing, do not tag it" and advisory caps "at most 3 archetype tags, at most 2 bracket tags, at most 5 card-category tags". Add a floor to avoid empty arrays (Pitfall 5): "if no dominant theme is clear, still output at least 1 tag per dimension." Do NOT add `maxItems` or otherwise touch `TagsSchema` — count guidance lives in the prompt only.

    HARD CONSTRAINTS / CARVE-OUTS:
    - Do NOT change `SummarySchema`, `ClassificationSchema`, `ClipsSchema`, `TagsSchema` (JSON contract) or the `FormatAllowlist` helper.
    - Do NOT change `DistillationValidation` rules (3-8 clips, <=200-word summary, allowlisted tags).
    - Do NOT change `BuildInstruction()` in `CliLlmDistillationService`.
    - Do NOT re-indent the raw-string `"""` delimiters: keep the opening `"""` on the same line as `= """` and the closing `"""` at its current indentation column. Only the text between the delimiters changes (CarveOutGuardTests + format-gate enforce this). No model/provider swap.

    Then update `DistillationPromptRegressionTests.SystemPrompts_MatchShippedPhase21Fixtures`: replace the existing `expectedSummaryPrompt` / `expectedClipsPrompt` / `expectedTagsPrompt` fixture strings so they match the reworked prompts verbatim, AND add a new `expectedClassificationPrompt` fixture plus `Assert.Equal(expectedClassificationPrompt, DistillationSchemas.ClassificationSystemPrompt)` — the test currently does NOT pin `ClassificationSystemPrompt`, so without this a bad classification rewrite would be uncaught (gap flagged in peer review). This is an intentional fixture refresh + coverage add, same commit. Do NOT modify `ResponseFormatSchemas_MatchShippedPhase21Fixtures` — it must continue to pass untouched, proving the schema contract is unchanged.

    Preserve LF line endings; changed lines must satisfy the format gate.
  </action>
  <verify>
    <automated>cd /mnt/c/users/chrislunt/source/personal/deckflow && "/mnt/c/Program Files/dotnet/dotnet.exe" build DeckFlow.Core.Tests/DeckFlow.Core.Tests.csproj 2>&1 | tail -5 && "/mnt/c/Program Files/dotnet/dotnet.exe" test DeckFlow.Core.Tests/DeckFlow.Core.Tests.csproj --no-build --filter "Distillation|CarveOutGuard" 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion (summary paste-ready): `grep -ic 'paste' DeckFlow.Core/Knowledge/DistillationSchemas.cs` returns at least 1 (summary prompt now instructs paste-ready / chatbot framing).
    - Source assertion (clip on-topic): the ClipsSystemPrompt prose contains an explicit relevance instruction — verifiable by `grep -in 'heuristic\|specific card\|named\|reason' DeckFlow.Core/Knowledge/DistillationSchemas.cs` returning at least 1 hit within the clips prompt region.
    - Source assertion (tag parsimony): `grep -ic 'dominant' DeckFlow.Core/Knowledge/DistillationSchemas.cs` returns at least 1 (tags prompt now instructs DOMINANT-topic selectivity).
    - Allowlist gate preserved: `grep -c 'FormatAllowlist(ContentTagVocabulary' DeckFlow.Core/Knowledge/DistillationSchemas.cs` returns 3 (Archetype/Bracket/CardCategory interpolations intact).
    - Schema contract untouched: `git diff DeckFlow.Core/Knowledge/DistillationSchemas.cs` shows NO changes to lines containing `SummarySchema`/`ClassificationSchema`/`ClipsSchema`/`TagsSchema` definitions, and `git diff DeckFlow.Core.Tests/DistillationPromptRegressionTests.cs` shows NO change inside `ResponseFormatSchemas_MatchShippedPhase21Fixtures`.
    - Min clip count preserved: ClipsSystemPrompt still contains "3 to 8" (`grep -c '3 to 8' DeckFlow.Core/Knowledge/DistillationSchemas.cs` returns 1).
    - Carve-out: `CarveOutGuardTests` pass (raw-string literals not re-indented).
    - `dotnet test --filter "Distillation|CarveOutGuard"` reports 0 failures (including the refreshed `SystemPrompts_MatchShippedPhase21Fixtures` and the untouched `ResponseFormatSchemas_MatchShippedPhase21Fixtures`).
    - NOTE — quality (clearer summaries / on-topic clips / accurate tags on real content) is NOT asserted here: it is an operator before/after inspection deferred to Phase 58 DOGFOOD-01.
  </acceptance_criteria>
  <done>The four system prompts carry the new paste-ready / on-topic-clip / tag-parsimony instructions; schemas and validation rules are byte-identical; the prompt-regression fixture is realigned and green; CarveOutGuard and all Core distillation tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| harvested transcript -> distill LLM prompt | Untrusted YouTube transcript content is fed (via stdin) alongside the system prompt to the Claude CLI. This boundary is unchanged by this plan. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-57-02-01 | Tampering | Prompt-injection via transcript content affecting distill output | accept | Pre-existing, unchanged risk. The Claude CLI distill path is the production path; the reworked prompts add only prose guidance and introduce no new injection vector beyond what already ships. Output is still constrained by `DistillationValidation` (allowlisted tags via `SanitizeTags`, clip-count bounds via `SanitizeClips`, 200-word summary cap). The Codex-backend read-isolation concern (ROADMAP backlog) does not apply to the Claude CLI path. |
| T-57-02-02 | Tampering | Accidental JSON-schema / validation contract drift | mitigate | Plan forbids editing the four `*Schema` constants and `DistillationValidation`; `ResponseFormatSchemas_MatchShippedPhase21Fixtures` stays untouched and green as a guard; `git diff` acceptance assertion confirms no schema-line change. |
| T-57-02-03 | Tampering | Raw-string literal re-indentation altering the shipped prompt | mitigate | Carve-out enforced by `CarveOutGuardTests` + format-gate; acceptance criteria require both to pass. |
| T-57-02-SC | Tampering | npm/NuGet installs | accept | No package installs (zero new dependencies — RESEARCH Package Legitimacy Audit: not applicable). |

**No new high-severity threats.** DIST-01 changes prose prompt text only — no new endpoints, inputs, secrets, network calls, model, or provider. The transcript->prompt boundary and its accepted prompt-injection risk are unchanged from the existing architecture. ASVS V5 unchanged (no new operator-configurable input).
</threat_model>

<verification>
- `DeckFlow.Core.Tests` builds with 0 errors.
- `dotnet test --filter "Distillation|CarveOutGuard"` reports 0 failures — refreshed `SystemPrompts_MatchShippedPhase21Fixtures` passes, `ResponseFormatSchemas_MatchShippedPhase21Fixtures` passes unchanged, `CarveOutGuardTests` pass.
- Full suite green before phase verify: `dotnet test --no-build`.
- Carve-outs intact: raw-string `"""` delimiters not re-indented; schemas and `DistillationValidation` byte-identical; LF line endings preserved.
- Quality gate (operator before/after on real content) explicitly deferred to Phase 58 DOGFOOD-01.
</verification>

<success_criteria>
- The reworked prompts instruct tag parsimony/relevance, on-topic clip selection, and paste-ready deckbuilding-focused summaries — Roadmap Phase 57 Success Criterion 2 (prompt shipped; operator before/after inspection gated in Phase 58).
- JSON schemas and `DistillationValidation` rules unchanged — prompts still emit parseable JSON within the existing contract.
- No model/provider swap; current distill providers retained.
</success_criteria>

<output>
Create `.planning/phases/57-admin-surface-distill-quality/57-02-SUMMARY.md` when done.
</output>
