a. **Failing tests**

- `DeckFlow.Web.Tests.PlanRoleClassifierTests.FromHeuristic_ProtectionPermanent_EarnsInteractionInBothModes(mode: Cedh)` failed. Asserted `HasFlag(PlanRole.Interaction) == True`; actual `False`. The failing assertion in the theory body was the synthetic Enchantment with oracle text `"When this enters, permanents you control phase out until your next turn."`
- `DeckFlow.Web.Tests.PlanRoleClassifierTests.FromHeuristic_ProtectionPermanent_EarnsInteractionInBothModes(mode: Casual)` failed. Asserted `HasFlag(PlanRole.Interaction) == True`; actual `False`. The failing assertion in the theory body was the synthetic Enchantment with oracle text `"When this enters, permanents you control phase out until your next turn."`

Cause: `DeckStatClassifier.IsProtectionCard` matches the singular-subject needle
`"phases out"`, while the test oracle used plural-subject `"phase out"`.
The artifact hexproof assertion and the creature indestructible assertion both
pass under the current production fix.

Measured from the full `DeckFlow.Web.Tests` run on 2026-07-27. No non-`PlanRoleClassifierTests` failures appeared in that run.

After correcting the test oracle to the supported singular phrasing
`"{T}: Target creature phases out."`, the suite is green.

b. **Health-band check**

Pass. `.manabase-arch-7084567-facts.json` still reads `("Solid","Solid")` for the flag-off / flag-on health bands.

Measured directly by loading the committed fixture through
`ManabaseClassifier.Classify(...)` and `ManabaseAnalyzer.Analyze(...)` with
`useHealthBandHeadlineFloor: false` and `true`. The same conclusion is
reachable from the committed test suite:
`dotnet test DeckFlow.Web.Tests/DeckFlow.Web.Tests.csproj --filter "FullyQualifiedName~ManabaseHealthBandRegressionTests"`
runs the health-band test class and reports 18/18 passed, so the band check is
covered by xUnit and not scratch-harness-only.

c. **Fixture flip list**

- `Kytheon, Hero of Akros // Gideon, Battle-Forged`: `DeckStatClassifier.IsProtectionCard(name, frontFaceOracleText) == true`; the card does reach `PlanRoleClassifier` empirically (`deck.Spells` contains it).
- `The One Ring`: `DeckStatClassifier.IsProtectionCard(name, frontFaceOracleText) == true`; the card does reach `PlanRoleClassifier` empirically (`deck.Spells` contains it).
- `Plaza of Heroes`: `DeckStatClassifier.IsProtectionCard(name, frontFaceOracleText) == true`; the card does **not** reach `PlanRoleClassifier` empirically because `deck.Spells` does not contain it. This was measured from the classified `ManabaseDeck`, not assumed from the type line.

d. **D-06 known-limitation measurement**

- `Swiftfoot Boots` (`Artifact — Equipment`, `"Equipped creature has hexproof and haste."`): `IsProtectionCard == false`; `Classify(...) == PlanRole.None`.
- `Lightning Greaves` (`Artifact — Equipment`, `"Equipped creature has haste and shroud."`): `IsProtectionCard == false`; `Classify(...) == PlanRole.None`.
- `Hexing Squelcher` (`Creature — Goblin Sorcerer`, oracle text from local `_role-floor-research/cards_full.json`): `IsProtectionCard == false`; `Classify(...) == PlanRole.None`.
- `Goblin Chirurgeon` (`Creature — Goblin Shaman`, `"Sacrifice a Goblin: Regenerate target creature."`): `IsProtectionCard == false`; `Classify(...) == PlanRole.None`.
- `Mother of Runes` (`Creature — Human Cleric`, `"{T}: Target creature you control gains protection from the color of your choice until end of turn."`): `IsProtectionCard == false`; `Classify(...) == PlanRole.None`.

All five D-06 cards above remain `PlanRole.None` after this fix. Widening `DeckStatClassifier.IsProtectionCard(...)` remains deliberately out of scope for this phase.

Measurement notes:
- `Swiftfoot Boots`, `Mother of Runes`, and the three fixture cards were measured through a scratch `dotnet` harness against the repo code.
- `Hexing Squelcher` and `Goblin Chirurgeon` used measured local corpus oracle/type data from `_role-floor-research/cards_full.json`.
- `Lightning Greaves` used the plan-provided oracle text and a reasoned `Artifact — Equipment` type line because no local facts entry for that card was present in the measured corpus files used here.

e. **Recommendation**

Delta accepted, with one authorized test-oracle correction:
`FromHeuristic_ProtectionPermanent_EarnsInteractionInBothModes` now uses the
supported singular `phases out` oracle phrasing for its third assertion. No
golden, health-band, byte-identity, or other fixture expectation value was
edited.
