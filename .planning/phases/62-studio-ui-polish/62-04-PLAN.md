---
phase: 62-studio-ui-polish
plan: 04
type: execute
wave: 3
depends_on: [01, 02]
files_modified:
  - DeckFlow.Studio/Pages/Review.razor
  - DeckFlow.Studio/Shared/NavMenu.razor
  - DeckFlow.Studio/Shared/MainLayout.razor
  - DeckFlow.Studio.Tests/ReviewPageTests.cs
  - DeckFlow.Studio.Tests/NavMenuTests.cs
  - DeckFlow.Studio.Tests/HarvestPageTests.cs
  - README.md
  # Harvest/Publish action logic untouched — A2 only re-verifies the existing visible-scoped Select-All.
autonomous: true
requirements: [SUI-02, SUI-04]
must_haves:
  truths:
    - "(Codex HIGH — concrete, testable acceptance items, ALL three required:)"
    - "A1 (SUI-02): Review shows a direct 'Go to Publish' link/button when the approved count > 0, and it is absent/disabled when approved count == 0 — verified by bUnit"
    - "A2 (SUI-02): Harvest Select-All remains scoped to the visible/filtered rows (Phase-61 invariant preserved) and the entry lists support shift-free multi-select via the existing per-row checkboxes — verified by bUnit"
    - "A3 (SUI-04): NavMenu is grouped into a Pipeline section (Home, Harvest, Creators, Review, Publish, Direct Push, Pull from Prod) and a Support section (Skipped, Blocked), with every existing destination preserved — verified by bUnit (all nav hrefs still present)"
    - "No action's behavior changes — only defaults/affordances/links; existing handlers reused; all existing page tests still pass"
  artifacts:
    - path: "DeckFlow.Studio/Shared/NavMenu.razor"
      provides: "Cleaner grouped/ordered navigation"
      contains: "NavLink"
    - path: "DeckFlow.Studio/Pages/Harvest.razor"
      provides: "Flow-tightening (defaults / multi-select ergonomics)"
      contains: "Harvest"
  key_links:
    - from: "DeckFlow.Studio/Shared/NavMenu.razor"
      to: "DeckFlow.Studio/Pages"
      via: "nav links to all Studio destinations, grouped for clarity"
      pattern: "href"
---

<objective>
SUI-02 + SUI-04: tighten the harvest→review→publish flow for fewer clicks (sensible defaults,
multi-select ergonomics, less page back-and-forth) and clean up the Studio layout/navigation for
density and clarity. Presentation-only — no behavior changes; reuse existing handlers. This is the
final polish pass over the now-settled surfaces (badges from 62-01, creator filter from 62-02).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/62-studio-ui-polish/62-CONTEXT.md
@./CLAUDE.md
@DeckFlow.Studio/Pages/Harvest.razor
@DeckFlow.Studio/Pages/Review.razor
@DeckFlow.Studio/Pages/Publish.razor
@DeckFlow.Studio/Shared/NavMenu.razor
@DeckFlow.Studio/Shared/MainLayout.razor
</context>

<tasks>
1. A3 — NavMenu.razor: group links into a Pipeline section (Home, Harvest, Creators, Review, Publish,
   Direct Push, Pull from Prod) and a Support section (Skipped, Blocked) with section headers/dividers.
   Keep EVERY existing destination/href. MainLayout.razor: tighten top-row/chrome density (no link
   removed; About already fixed in 62-01).
2. A1 — Review.razor: add a "Go to Publish" link/button shown only when the approved count > 0 (reuse
   the existing approved-count signal; navigate to /publish). No behavior change beyond navigation.
3. A2 — confirm/keep Harvest Select-All scoped to the visible/filtered rows (Phase-61 invariant) and
   verify the entry-list per-row multi-select is ergonomic; make only conservative affordance tweaks.
   Do NOT touch any action handler logic.
4. bUnit: A1 (link present when approved>0, absent/disabled when 0), A2 (select-all scoped to visible;
   covered by/extends the Phase-61 visible-projection tests), A3 (NavMenu renders both sections AND all
   existing hrefs). Confirm all existing page tests still pass (Harvest/Review/Publish/PullFromProd/
   Creators/Skipped/Blocked).
5. README: note the tightened flow (Review→Publish link) + grouped navigation.
6. Build Studio + run Studio.Tests green.
</tasks>

## Trust Boundaries
None new — presentation/navigation only.

## STRIDE Threat Register
- **Tampering (T-62-07):** a changed default must not silently widen what an action does (e.g. a
  default that selects more than intended for harvest/publish). Mitigation: defaults are conservative;
  the canonical visible/selected projection still gates harvest; existing tests guard behavior.
- **None** otherwise.
