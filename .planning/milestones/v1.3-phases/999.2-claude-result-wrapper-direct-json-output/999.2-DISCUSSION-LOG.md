# Phase 999.2: Claude `<result>` Wrapper — Direct JSON Output Option - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-05-19
**Phase:** 999.2-claude-result-wrapper-direct-json-output
**Areas discussed:** Wrapper strategy for Claude, Claude's JSON delivery shape, Code locality, Legacy artifact + regression coverage
**Mode:** interactive `discuss` (default; 4 questions per area + per-area continue/next gate)

---

## Wrapper strategy for Claude

### Q1 — Wrapper strategy: which direction for Claude prompts?

| Option | Description | Selected |
|--------|-------------|----------|
| Drop `<result>` wrapper from Claude only | Remove `ResultWrapInstruction` + "Do not put JSON outside `<result>`" lines from 5 Claude variants. Claude emits JSON in fenced ```json block (its natural behavior). Parser brace-scan fallback already handles fenced-only output. ChatGPT/Gemini unchanged. Cleanest user-visible chat. | ✓ |
| Tighten Claude prompt; keep wrapper | Add stronger anti-duplication directive (move to last instruction; ALL-CAPS mandate similar to GeminiJsonMandate). Brittle — Claude already ignores 'Do not put JSON outside <result>'. | |
| Invert parser primacy: fenced first, `<result>` fallback | Edit `ExtractJsonPayload` to prefer fenced ```json block, fall back to `<result>`. Touches parser — diverges Phase 15 D-01 minimally but rewrites Phase 10 design rationale. | |
| Hybrid: drop wrapper from Claude + add explicit fenced-block ask | Drop wrapper instruction AND explicitly tell Claude 'Return JSON in a fenced ```json code block, nothing else'. Highest determinism. | |

**User's choice:** Drop `<result>` wrapper from Claude only
**Notes:** The "hybrid" option's anti-`<result>` directive was effectively rolled into D-04 (fenced-block directive is the replacement for the wrapper, not added on top of it). Negative-instruction follow-on rejected by D-03.

### Q2 — Is the wrapper-drop uniform across all 5 Claude families, or selective?

| Option | Description | Selected |
|--------|-------------|----------|
| All 5 Claude families | Analysis, Comparison, FollowUp, MetaGap, SetUpgrade — all 5 ClaudePromptVariant files lose `ResultWrapInstruction` + "Do not put JSON outside" lines. Symmetric scope; cleanest git-blame. | ✓ |
| Only Analysis (where T4 surfaced the bug) | Edit `ClaudeAnalysisPromptVariant.cs` only. Risk: defer real fix; user re-files same bug for Comparison/MetaGap later. | |
| Analysis + Comparison + MetaGap (the three 'primary' workflows) | Edit 3 main packet-generating families; leave FollowUp + SetUpgrade as-is. | |
| You decide during planning | Plan author picks per family during execution. | |

**User's choice:** All 5 Claude families

### Q3 — Should the 'Do not put JSON outside `<result>`' follow-on line be removed entirely?

| Option | Description | Selected |
|--------|-------------|----------|
| Remove entirely | Line is dead weight without the wrapper. Each Claude variant simply stops appending both lines. Cleanest diff; 2 lines deleted per variant × 5 = 10-line removal total. | ✓ |
| Invert to 'Return JSON in a fenced ```json block; do not wrap' | Replace the line with an explicit fenced-block directive symmetric to ChatGPT line 198. Pushes Claude toward deterministic fenced output. | |
| Leave the line; let plan author rewrite contextually | Less mechanical, more judgment. | |

**User's choice:** Remove entirely
**Notes:** The "Invert" option's outcome is realized via the separate D-04 fenced-block directive insertion, not by mutating this specific line.

### Q4 — `ResultWrapInstruction` (the shared const) fate?

| Option | Description | Selected |
|--------|-------------|----------|
| Stay as-is | Const still consumed by 10 variants (ChatGPT × 5 + Gemini × 5). Name describes what it is, not who uses it. | ✓ |
| Rename to `ChatGptGeminiResultWrapInstruction` or similar | Brand-specific name documents who uses it. Couples const name to platform list — brittle when 4th platform lands. Diverges Phase 13 anti-pattern. | |
| Add an XML doc comment noting Claude-excluded | Keep name; add a one-line `<remarks>` noting Claude variants intentionally skip it. Documentation hint for the next maintainer. | |

**User's choice:** Stay as-is
**Notes:** The doc-comment option is preserved as Claude's Discretion item in D-14 — plan author may add it as a small 6th commit if helpful.

---

## Claude's JSON delivery shape

### Q1 — What format should Claude emit JSON in after dropping the `<result>` wrapper?

| Option | Description | Selected |
|--------|-------------|----------|
| Fenced ```json code block | Explicit 'Return the JSON inside a fenced ```json code block' directive symmetric to ChatGPT line 198. Claude's natural inclination already. Parser brace-scan handles. | ✓ |
| Bare JSON (no fence, no wrapper) | Ask Claude to emit raw JSON as the response tail. Parser brace-scan handles. Claude tends to wrap code blocks autonomously. | |
| Single XML tag (e.g. `<json>...</json>`) | Replace `<result>` with a different XML tag scoped to Claude. Parser would need a new branch — contradicts 'parser unchanged' lock. | |
| You decide during planning | Plan author writes the directive matching each Claude variant's surrounding instructional voice. | |

**User's choice:** Fenced ```json code block

### Q2 — Directive wording: exact-copy from ChatGPT variants vs reworded?

| Option | Description | Selected |
|--------|-------------|----------|
| Exact-copy from ChatGPT variants | Use the verbatim ChatGPT line in each Claude variant. Identical wording across platforms maximizes parser-consistency proof; trivial mechanical change. | ✓ |
| Reword for Claude's instructional voice | Each Claude variant currently uses `<task>`/`<output_schema>` XML-tagged structure. Rewrite in Claude's terser voice. | |
| Use a new shared const (e.g. `FencedJsonInstruction` in JsonTextFormatterService) | Add a 2nd shared const; ChatGPT variants migrate to it too. Bigger blast radius. | |

**User's choice:** Exact-copy from ChatGPT variants
**Notes:** Scout surfaced an indentation exception clause in CONTEXT.md `<code_context>` — ChatGPT variants prefix the AppendLine string with 3-space indent (numbered-list style); plan author strips the leading whitespace when copying into Claude variants to match Claude's flatter prose voice. Exact-copy applies to the directive content; punctuation/indent matches the destination Claude variant's surrounding style.

### Q3 — Placement of the new directive within each Claude variant?

| Option | Description | Selected |
|--------|-------------|----------|
| Same position as old wrapper lines | Plug new fenced directive into the exact spot where old lines were removed (immediately before `</task>` close). Preserves prompt flow; diff is mechanical line-replace. | ✓ |
| End of prompt (after `</task>` close) | Move to absolute-last position, mirroring GeminiJsonMandate's last-instruction-wins pattern. Breaks prompt's `<task>`-closed structure. | |
| Plan author judges per variant | Per-variant placement judgment. | |

**User's choice:** Same position as old wrapper lines

### Q4 — Existing sub-object fenced-block instructions (e.g. per-version `deck_profile` blocks)?

| Option | Description | Selected |
|--------|-------------|----------|
| Leave them alone | Those describe sub-objects (per-version deck_profile blocks) part of the deck-analysis output, not the main JSON wrapper. Tightest scope. | ✓ |
| Audit for duplication risk | Plan author greps each Claude variant for all fenced-block emit sites and adds a SUMMARY note. Light extra work. | |
| Consolidate into single JSON envelope | Major prompt rewrite — scope creep; defer to backlog. | |

**User's choice:** Leave them alone

---

## Code locality

### Q1 — Per-variant edits vs platform-level config switch?

| Option | Description | Selected |
|--------|-------------|----------|
| Per-Claude-variant edits, 5 files | Edit each `ClaudePromptVariant.cs` directly. 5 mechanical line-replace diffs; matches Phase 15 D-04 symmetry. Lowest risk; grep-able. | ✓ |
| Platform-level switch on AiPlatform record | Add `UseResultWrapper` property. Aligns with Phase 15 OCP score. BUT contradicts Phase 15 D-01 'data-only record' invariant. | |
| Platform-level switch via new `IResultWrapPolicy` interface | New strategy family + per-platform implementations injected via DI. Over-engineered for binary divergence. | |
| Hybrid: per-variant edits + helper method on `JsonTextFormatterService` | Centralizes the brand-vs-fenced choice in one place. Moderate refactor; sets pattern if a 2nd platform diverges later. | |

**User's choice:** Per-Claude-variant edits, 5 files

### Q2 — Commit decomposition?

| Option | Description | Selected |
|--------|-------------|----------|
| One commit per variant, 5 commits | Matches Phase 15 D-03 'each switch deletion is one commit (5 commits) — clean git-blame'. Per CLAUDE.md 'commit per logical change'. Each commit independently revertable. | ✓ |
| One commit covers all 5 variants | Single atomic commit. Diverges from Phase 15 wave-pattern precedent. | |
| Two commits: shared infrastructure + bulk variant edits | If a helper extraction emerges. Currently no shared infra change planned. | |

**User's choice:** One commit per variant, 5 commits

### Q3 — Plan decomposition for execute-phase?

| Option | Description | Selected |
|--------|-------------|----------|
| Single plan `999.2-01` with 5 commits | All 5 Claude variant edits are mechanical and homogeneous; one plan, 5 commits inside it. Smallest planning overhead. | ✓ |
| Two plans: `999.2-01` (3 main families) + `999.2-02` (FollowUp + SetUpgrade) | Allows verifying user-visible chat behavior on main pages before touching secondary surfaces. Adds checkpoint. | |
| Per-family plan: `999.2-01` … `999.2-05` | Maximum granularity, ridiculous overhead for mechanical edits. | |

**User's choice:** Single plan `999.2-01` with 5 commits

### Q4 — Worktree isolation strategy?

| Option | Description | Selected |
|--------|-------------|----------|
| Single sequential worktree | 5 commits in sequence inside one worktree. Parallel orchestration overhead outweighs the speedup on trivial edits. | ✓ |
| Parallel 5-worktree wave per variant | Mirror Phase 999.1 wave-1 pattern. Triggers all 4 blocking constraints from `.continue-here.md` for marginal speedup. | |
| Primary worktree, no isolation | Fastest path but breaks Phase 15/999.1 isolation precedent and CLAUDE.md GSD enforcement. Reject. | |

**User's choice:** Single sequential worktree

### Q5 — Invariant assertion strategy?

| Option | Description | Selected |
|--------|-------------|----------|
| Build-time grep gate + manual T3 (Claude-analysis) UAT | Acceptance gate: grep returns expected hits; manual T3 UAT verifies clean Claude chat output. Matches Phase 999.1 model. | ✓ |
| Full T1–T8 manual integration suite per Phase 15 SC4 model | Heavy for prompt-only Claude change. | |
| Automated xUnit test asserting Claude variants do not emit `ResultWrapInstruction` | Locks invariant in CI but VSTest WSL constraint applies. | |

**User's choice:** Build-time grep gate + manual T3 (Claude-analysis) UAT
**Notes:** UAT scope extended to T3 + T7 + Comparison ad-hoc in Area 4 Q3 to cover all 5 Claude families through their actual chat flows.

---

## Legacy artifact + regression coverage

### Q1 — Legacy artifact handling: zip artifacts saved BEFORE Phase 999.2 contain Claude responses with `<result>` wrapper. Confirm both shapes parse post-change?

| Option | Description | Selected |
|--------|-------------|----------|
| Already covered by parser | `JsonTextFormatterService.ExtractJsonPayload` already prefers `<result>` then falls back to brace-scan. Legacy zips with wrapper still parse; new wrapperless Claude responses parse via brace-scan. No parser change needed. | ✓ |
| Verify with a manual round-trip | Locate one v1.2-era or v1.3-pre-999.2 Claude zip; replay through `PacketArtifactStore` during T3 UAT. | |
| Add a parser-snapshot test | xUnit test asserting `ExtractJsonPayload` returns identical JSON for (a) wrapped, (b) fenced-only, (c) wrapped + fenced inputs. Locks invariant in CI. | |

**User's choice:** Already covered by parser
**Notes:** Documented as D-12 in CONTEXT.md.

### Q2 — Existing tests touching Claude prompt output: updates needed after 999.2?

| Option | Description | Selected |
|--------|-------------|----------|
| Audit during execution; update only if assertions reference `<result>` in Claude prompts | Plan author greps `DeckFlow.Web.Tests/` for tests asserting Claude prompt output. Updates only affected assertions. | ✓ |
| Add new tests for the post-999.2 Claude prompt shape | xUnit assertions that each Claude `BuildPrompt(...)` output contains fenced directive AND no `<result>`. Locks invariant in CI. | |
| Skip test audit entirely; rely on dotnet.exe build + manual UAT | CLAUDE.md notes VSTest unreliable in WSL. Phase 999.1 followed this. | |

**User's choice:** Audit during execution; update only if assertions reference `<result>` in Claude prompts

### Q3 — Round-trip UAT scope?

| Option | Description | Selected |
|--------|-------------|----------|
| T3 + T7 (deck-analysis + meta-gap with Claude) | T3 exercises Claude Analysis + SetUpgrade + FollowUp variants. T7 exercises ClaudeMetaGap. Add Claude deck-comparison ad-hoc. Covers all 5 Claude families with 3 manual sessions. | ✓ |
| Just T3 (Claude deck-analysis) | Only verify the page where T4 surfaced the bug. Leaves 4 other Claude families unverified. | |
| Full T1–T8 across all 3 platforms | Maximum coverage. Verifies ChatGPT/Gemini untouched + all Claude paths cleaned. Heavy time investment. | |
| T3 + screenshot of clean Claude chat in commit message | T3 only + before/after screenshots. Documents user-facing fix for milestone audit trail. | |

**User's choice:** T3 + T7 (deck-analysis + meta-gap with Claude) + Comparison ad-hoc

### Q4 — Rollback strategy?

| Option | Description | Selected |
|--------|-------------|----------|
| Parser already tolerates both shapes — no rollback infrastructure needed | Even if Claude emits both fenced block AND stray `<result>`, `ExtractJsonPayload` still succeeds. `git revert <5 commits>` restores prior state cleanly. | ✓ |
| Add a runtime flag `DECKFLOW_CLAUDE_USE_RESULT_WRAPPER` | Gate the wrap-instruction emission behind an env var. Adds runtime config surface for a one-way migration; CLAUDE.md prefers structural fixes. | |
| Document the revert command in CONTEXT.md / SUMMARY.md | Note exact `git revert <commits>` command set. No code surface added. | |

**User's choice:** Parser already tolerates both shapes — no rollback infrastructure needed
**Notes:** Document option implicit: the 5-commit-per-variant decomposition (D-09) inherently documents the revert path via `git log`.

---

## Wrap-up gate

**Question:** All 4 areas covered. Any remaining gray areas, or ready for CONTEXT.md?
**User's choice:** I'm ready for context

---

## Claude's Discretion

- Exact source line for D-05 verbatim copy per family (Comparison/FollowUp/MetaGap/SetUpgrade ChatGPT counterparts) — plan author confirms during execution.
- Order of the 5 commits within plan `999.2-01` — alphabetical vs UAT-coverage-priority — plan author picks.
- Whether to add a `<remarks>` doc comment on `ResultWrapInstruction` noting Claude-excluded — judgment call; optional 6th commit at start of plan.
- Whether the 5 commits chain through one worktree branch or directly on `v1.3` — both work; sequential per-variant commits land cleanly either way.

## Deferred Ideas

- Helper method extraction on `JsonTextFormatterService` (e.g., `EmitWrapInstructionFor(AiPlatform)`) — rejected by D-08 as over-engineered for 1-platform divergence; revisit if 2nd platform diverges.
- `AiPlatform.UseResultWrapper` record property — rejected by D-08 (would break Phase 15 D-01); v1.4+ candidate IF a future need lands a 2nd binary per-platform flag.
- `IResultWrapPolicy` strategy interface + per-platform implementations — rejected by D-08; same v1.4+ backlog as above.
- Automated xUnit prompt-snapshot tests — rejected by D-11 (VSTest WSL constraint + brittle string-comparison); revisit if WSL VSTest support lands.
- Sub-object fenced-block instruction audit / consolidation — rejected by D-07 (orthogonal scope creep); backlog candidate.
- Rename `ResultWrapInstruction` to brand-prefixed name — rejected (Phase 13 anti-pattern); stays.
- Runtime flag `DECKFLOW_CLAUDE_USE_RESULT_WRAPPER` — rejected by D-14; capture only if future Claude model behavior change forces emergency rollback.
- README brand-prose update tied to Claude `<result>` wrapper — plan execution-time grep; likely zero hits.
- Phase 999.3 (edhtop16 filter defaults) — independent; tracked separately in ROADMAP.
- Phase 13 D-08 TS/CSS-coupled identifiers — still deferred to a separate refactor with TS test surface.
