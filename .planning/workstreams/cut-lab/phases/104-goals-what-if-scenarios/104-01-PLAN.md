---
phase: 104-goals-what-if-scenarios
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - DeckFlow.Web/Models/CutLab/CutLabGoals.cs
  - DeckFlow.Web/Services/CutLab/CutLabGoalRules.cs
  - DeckFlow.Web/Models/CutLab/CutLabState.cs
  - DeckFlow.Web/Services/CutLab/CutLabStateSerializer.cs
  - DeckFlow.Web/Models/CutLab/CutLabMetrics.cs
  - DeckFlow.Web/Services/CutLab/CutLabSimulationService.cs
  - DeckFlow.Web/Services/CutLab/CutLabPageService.cs
  - DeckFlow.Web/Services/CutLab/CutLabDeltaCache.cs
  - DeckFlow.Web.Tests/CutLabStateSerializerTests.cs
  - DeckFlow.Web.Tests/CutLabSimulationServiceTests.cs
autonomous: true
requirements: [GOAL-01]

must_haves:
  truths:
    - "Editing a goal turn number changes the reported probability for that turn-indexed metric"
    - "A goal turn at or beyond the card's on-curve turn reports the true CastPercent, not a clamped early-turn value (Pitfall 1)"
    - "Goal turns persist across a page POST round-trip and are clamped server-side to 1-15"
    - "A pre-104 state JSON with no Goals field deserializes to the seeded defaults (3/2/4), not zeros"
    - "Two different goal settings on the SAME pool produce different cached metrics — goal turns are part of the snapshot AND proposal-delta cache keys (no stale reuse)"
  artifacts:
    - path: "DeckFlow.Web/Models/CutLab/CutLabGoals.cs"
      provides: "CutLabGoalSettings record (3 turn targets) + CutLabGoalDefaults seed constants"
      contains: "CutLabGoalSettings"
    - path: "DeckFlow.Web/Services/CutLab/CutLabGoalRules.cs"
      provides: "ClampGoals(1-15) helper shared by the JSON and no-JS paths"
      contains: "ClampGoals"
    - path: "DeckFlow.Web/Models/CutLab/CutLabState.cs"
      provides: "Goals property on the session envelope"
      contains: "CutLabGoalSettings Goals"
  key_links:
    - from: "DeckFlow.Web/Services/CutLab/CutLabStateSerializer.cs"
      to: "CutLabGoalRules.ClampGoals"
      via: "Deserialize post-processing next to ClampFloors"
      pattern: "CutLabGoalRules\\.ClampGoals"
    - from: "DeckFlow.Web/Services/CutLab/CutLabPageService.cs"
      to: "ICutLabSimulationService.BuildSnapshot"
      via: "passes state.Goals into both baseline and current snapshot calls"
      pattern: "state\\.Goals"
    - from: "DeckFlow.Web/Services/CutLab/CutLabDeltaCache.cs"
      to: "SnapshotCacheKey + DeltaCacheKey"
      via: "normalized goal turns folded into both cache keys so edits do not reuse stale metrics"
      pattern: "goal"
---

<objective>
Make the three fixed Phase-103 turn-indexed metrics (`CommanderByTurn`, `EngineByTurn`, `RepresentativeLineByTurn`) user-steerable turn goals (GOAL-01), threaded end-to-end through the existing simulation engine with zero new simulation math (SIM-01). This plan delivers only the backend: the goal domain model, serializer round-trip + range clamp, the engine threading that replaces the hard-coded `CutLabCategoryByTurnDefaults` reads, and the Pitfall-1 late-turn clamp fix. UI is 104-02.

Purpose: Every downstream surface (goals editor, what-if preview) recomputes metrics against the user's goal turns; that recompute must exist and be correct before any UI renders it.
Output: `CutLabGoalSettings` + `CutLabGoalRules`, a `Goals` property on `CutLabState`, and a `BuildSnapshot`/`ComputeProposalDeltas` that read goal turns.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/cut-lab/ROADMAP.md
@.planning/workstreams/cut-lab/REQUIREMENTS.md
@.planning/workstreams/cut-lab/phases/104-goals-what-if-scenarios/104-CONTEXT.md
@.planning/workstreams/cut-lab/phases/104-goals-what-if-scenarios/104-RESEARCH.md
@.planning/workstreams/cut-lab/phases/104-goals-what-if-scenarios/104-PATTERNS.md

<interfaces>
Key existing signatures the executor threads goals through (from source):

ICutLabSimulationService.BuildSnapshot(IReadOnlyList<CutLabPoolCard> workingList, string? playExperience, int? trialsOverride = InLoopTrials, string? poolKey = null, CancellationToken ct = default)
ICutLabSimulationService.ComputeProposalDeltas(IReadOnlyList<CutLabPoolCard> currentWorkingList, string candidateCardName, string? playExperience, int? trialsOverride = InLoopTrials, string? poolKey = null, CancellationToken ct = default)

Private static builder + delegate in CutLabSimulationService.cs:
  Func<IReadOnlyList<DeckCardEntry>, string?, int?, CutLabMetricSnapshot> _snapshotBuilder
  static CutLabMetricSnapshot BuildSnapshot(IReadOnlyList<DeckCardEntry> deckEntries, string? playExperience, int? trialsOverride)  // lines 98-139
  static IReadOnlyList<CutLabMetricValue> BuildMetrics(ManabaseReport report, ManabaseDeck deck, IReadOnlyList<CardFact> facts, ManabaseMode mode)  // lines 268-293
  static double PercentByTurn(CardCastability? row, int turn)  // lines 385-399 — Pitfall 1 lives here
  static double MaxPercentByTurn(IReadOnlyList<CardCastability> rows, int turn)  // lines 382-383

Seed constants (DeckFlow.Core/Manabase/CedhMulliganCalibration.cs): TurnCapExplosive=3, TurnCapEngine=2, RepresentativeLineTurnCap=4; GetRepresentativeLineTurnCap(mode)=int.MaxValue for non-Cedh.
Mode resolution: CutLabRoleAssigner.ResolveMode(playExperience) -> ManabaseMode {Cedh, Focused, Casual}.

Implementers of ICutLabSimulationService to keep in sync: CutLabSimulationService (real) and NoOpCutLabSimulationService (nested in CutLabPageService.cs ~line 760).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Goal domain model, state property, serializer round-trip + clamp</name>
  <read_first>
    - DeckFlow.Web/Models/CutLab/CutLabState.cs (analog: existing CutLabRoleFloor record lines 110-121 and Intent property line 43 — copy the sealed record + init-only + XML-doc + empty-default back-compat convention)
    - DeckFlow.Web/Services/CutLab/CutLabStateSerializer.cs (Deserialize lines 41-69 — the Packages/Decisions bounding + trailing ClampFloors call at line 63)
    - DeckFlow.Web/Services/CutLab/CutLabFloorRules.cs (ClampFloors lines 32-70 and MaxFloor line 27 — the exact "clamp untrusted persisted values, return same instance when unchanged" precedent to mirror)
    - DeckFlow.Core/Manabase/CedhMulliganCalibration.cs (seed constants + GetRepresentativeLineTurnCap)
    - DeckFlow.Web.Tests/CutLabStateSerializerTests.cs (existing test conventions)
  </read_first>
  <behavior>
    - New CutLabState().Goals equals seed defaults: CommanderByTurn=3, EngineByTurn=2, RepresentativeLineByTurn=4.
    - Deserialize of JSON that omits "goals" yields the seed defaults (3/2/4), never 0/0/0 (pre-104 back-compat).
    - Deserialize of goals {commanderByTurn:0, engineByTurn:99, representativeLineByTurn:-4} clamps to {1, 15, 1}.
    - Serialize then Deserialize round-trips a valid {commanderByTurn:5, engineByTurn:3, representativeLineByTurn:7} unchanged.
    - ClampGoals returns the same state instance when all three turns are already in [1,15] (no needless copy).
  </behavior>
  <action>
    Create `DeckFlow.Web/Models/CutLab/CutLabGoals.cs` with a `sealed record CutLabGoalSettings` holding three `int { get; init; }` turn targets `CommanderByTurn`, `EngineByTurn`, `RepresentativeLineByTurn`, each defaulted to the matching `CedhMulliganCalibration` constant (3/2/4) via a `CutLabGoalDefaults` static class (`CommanderByTurn = CedhMulliganCalibration.TurnCapExplosive`, etc., mirroring `CutLabCategoryByTurnDefaults`). Add `public const int MinGoalTurn = 1;` and `public const int MaxGoalTurn = 15;` to `CutLabGoalDefaults`. Full XML docs on every public member.
    Add `public CutLabGoalSettings Goals { get; init; } = new();` to `CutLabState` with a doc comment noting the default initializer keeps pre-104 JSON blobs deserializing cleanly (mirror the RoleFloors comment framing).
    Create `DeckFlow.Web/Services/CutLab/CutLabGoalRules.cs` with `public static CutLabState ClampGoals(CutLabState state)` that clamps each of the three turns to `[MinGoalTurn, MaxGoalTurn]` via `Math.Clamp`, returning the same instance when nothing changed and otherwise `state with { Goals = ... }` — structurally follow `CutLabFloorRules.ClampFloors`.
    In `CutLabStateSerializer.Deserialize`, wrap the final return so goals are clamped alongside floors: `return CutLabGoalRules.ClampGoals(CutLabFloorRules.ClampFloors(CutLabLockRules.EnforceCommanderLock(state)));`.
    Add serializer test cases in `CutLabStateSerializerTests.cs` covering the five behaviors above.
  </action>
  <verify>
    <automated>dotnet test DeckFlow.Web.Tests --filter "FullyQualifiedName~CutLabStateSerializerTests"</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet build DeckFlow.Web` is clean (no new warnings).
    - New serializer tests assert defaults=3/2/4, omitted-goals=defaults, clamp 0->1 / 99->15 / -4->1, and a valid round-trip; all pass.
    - `grep -n "CutLabGoalRules.ClampGoals" DeckFlow.Web/Services/CutLab/CutLabStateSerializer.cs` returns the wired call.
  </acceptance_criteria>
  <done>CutLabGoalSettings, CutLabGoalRules, and CutLabState.Goals exist; goals round-trip and clamp; pre-104 JSON degrades to seeded defaults.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Thread goal turns through the engine, fix Pitfall-1 clamp, pass state.Goals from the page service</name>
  <read_first>
    - DeckFlow.Web/Services/CutLab/CutLabSimulationService.cs (BuildMetrics lines 268-293 fixed-constant reads; private static BuildSnapshot 98-139; PercentByTurn 385-399; the _snapshotBuilder delegate type line 55 and internal ctor line 71)
    - DeckFlow.Web/Models/CutLab/CutLabMetrics.cs (CutLabCategoryByTurnDefaults lines 213-224 — the constants being replaced by goal turns)
    - DeckFlow.Web/Services/CutLab/CutLabPageService.cs (baseline BuildSnapshot lines 315-335, current BuildSnapshot lines 343-366, ComputeProposalDeltas 368-388; NoOpCutLabSimulationService nested class ~line 760 that also implements the interface)
    - DeckFlow.Core/Manabase/ManabaseModels.cs (CardCastability.EarlyCastPercents doc ~lines 265-268 and OnCurveTurn / CastPercent — the Pitfall-1 fields)
    - DeckFlow.Web/Services/CutLab/CutLabDeltaCache.cs (ComputeDeltaEntryKey/DeltaCacheKey line 142/196 and ComputeSnapshotEntryKey/SnapshotCacheKey line 145/197 — both key records currently exclude goals; TryGet/Set line 45/70 and TryGetSnapshot/SetSnapshot line 99/123)
    - DeckFlow.Web.Tests/CutLabSimulationServiceTests.cs (existing engine test conventions + the internal snapshotBuilder test seam)
  </read_first>
  <behavior>
    - PercentByTurn(row, turn) returns row.CastPercent when turn >= row.OnCurveTurn (before the EarlyCastPercents clamp), matching the existing EarlyCastPercents.Count==0 branch logic (Pitfall 1 regression).
    - For a fixed pool/mode/trials, BuildSnapshot with goals {CommanderByTurn=6} yields a CommanderByTurn metric whose Label reads "Commander by turn 6" and whose Value differs from the value produced with CommanderByTurn=3 when the commander's on-curve turn is >= 6 (proves the goal turn is read, not the constant).
    - BuildSnapshot with goals=null reproduces the pre-104 values (fixed 3/2/4) — back-compat for any caller not yet passing goals.
    - Swap/goal deltas remain deterministic for a fixed pool/turn/trials combination (no new Random path) — SIM-01 guard.
    - For the SAME pool/play-experience/trials, BuildSnapshot with goals {CommanderByTurn=3} and again with {CommanderByTurn=9} return DIFFERENT CommanderByTurn metrics (proves the goal turns are part of the cache key, not silently reusing the first cached snapshot). Same holds for ComputeProposalDeltas.
  </behavior>
  <action>
    Add an optional `CutLabGoalSettings? goals = null` parameter (last non-token parameter, before `CancellationToken`) to `ICutLabSimulationService.BuildSnapshot` and `ComputeProposalDeltas`, and to both implementers (`CutLabSimulationService` and the nested `NoOpCutLabSimulationService`). Extend the private `_snapshotBuilder` delegate and the private static `BuildSnapshot(deckEntries, playExperience, trialsOverride)` and `BuildMetrics(...)` to accept the goals; when `goals` is null, fall back to `CutLabGoalDefaults` (i.e. current fixed 3/2/4) so behavior is unchanged for null callers.
    In `BuildMetrics`, replace the three `CutLabCategoryByTurnDefaults.*` reads at lines 284-286 with `goals.CommanderByTurn` / `goals.EngineByTurn` / `goals.RepresentativeLineByTurn`, and make the three labels dynamic via interpolation (`$"Commander by turn {goals.CommanderByTurn}"`, `$"Engine by turn {goals.EngineByTurn}"`, `$"Representative line by turn {goals.RepresentativeLineByTurn}"`).
    Fix Pitfall 1 in `PercentByTurn`: add `if (turn >= row.OnCurveTurn) { return row.CastPercent; }` immediately before the `Math.Clamp` index line, so user-edited late turns read the true eventual cast percent instead of the last early-turn value.
    In `CutLabPageService`, pass `state.Goals` into the baseline `BuildSnapshot` (line 319), the current `BuildSnapshot` (line 352), and the `ComputeProposalDeltas` (line 373) calls. Do NOT thread `CutLabState` into the simulation service itself — pass only the `Goals` value object (keep the existing dependency direction).
    In `CutLabDeltaCache`, fold the goal turns into BOTH cache keys so edited goals never reuse stale 3/2/4-based results: add a `CutLabGoalSettings? goals = null` parameter to `TryGetSnapshot`/`SetSnapshot` and `TryGet`/`Set`, extend the private `SnapshotCacheKey` and `DeltaCacheKey` records with a `string GoalsKey` component, and compute that component from the three clamped turns (e.g. `$"{c}-{e}-{r}"`, empty string when `goals` is null for back-compat). Thread the `goals` argument from every `CutLabSimulationService` call into `_deltaCache.TryGet/Set/TryGetSnapshot/SetSnapshot` (GetOrBuildSnapshot line 186-200 and ComputeProposalDeltas line 154/182). Do NOT change the pool-key hashing itself.
    Leave `CutLabCategoryByTurnDefaults` in place as the null-fallback seed source (referenced by `CutLabGoalDefaults`); do not delete it.
    Add engine test cases in `CutLabSimulationServiceTests.cs` for the four behaviors above, using the existing internal snapshotBuilder/test seam so no live HTTP is needed.
  </action>
  <verify>
    <automated>dotnet test DeckFlow.Web.Tests --filter "FullyQualifiedName~CutLabSimulationServiceTests"</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet build` clean across the solution (all ICutLabSimulationService implementers updated — no CS0535/CS0111).
    - New tests prove: Pitfall-1 returns CastPercent at turn>=OnCurveTurn; a changed goal turn changes the metric Label and (for a late-on-curve card) the Value; goals=null reproduces fixed defaults; deltas deterministic across two identical runs; AND two different goal settings on the same pool/trials do NOT collide in the delta cache (return different metrics).
    - `grep -n "goals.CommanderByTurn" DeckFlow.Web/Services/CutLab/CutLabSimulationService.cs` and `grep -n "state.Goals" DeckFlow.Web/Services/CutLab/CutLabPageService.cs` both return matches.
  </acceptance_criteria>
  <done>Goal turns drive the three turn-indexed metrics; Pitfall-1 clamp is fixed; the page service recomputes baseline/current/proposal metrics against state.Goals.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client -> CutLabStateSerializer.Deserialize | Untrusted persisted/posted goal turn ints cross here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-104-01 | Tampering | Goal turn ints in CutLabStateJson | mitigate | `CutLabGoalRules.ClampGoals` bounds every turn to [1,15] inside `Deserialize` before any value reaches `PercentByTurn`; malformed/oversize JSON still fails open to `new CutLabState()` (seeded defaults) via the existing serializer guard. |
| T-104-02 | Denial of Service | Extreme goal turn value driving engine work | mitigate | Clamp caps turn at 15; `PercentByTurn` reads a fixed-length `EarlyCastPercents` array or returns `CastPercent`, so cost is O(1) regardless of turn — no unbounded loop introduced. |
| T-104-SC | Tampering | package installs | accept | No package installs in this plan (SIM-01: zero new dependencies); legitimacy gate N/A. |
</threat_model>

<verification>
- `dotnet build` clean (no new warnings; nullable satisfied).
- Both filtered test suites green.
- Manual grep gates in acceptance criteria satisfied.
</verification>

<success_criteria>
Goal turns are a first-class, clamped, persisted part of CutLabState and drive the three turn-indexed metrics through the reused engine, with the Pitfall-1 late-turn clamp corrected — all with zero new simulation math and zero new packages.
</success_criteria>

<output>
Create `.planning/workstreams/cut-lab/phases/104-goals-what-if-scenarios/104-01-SUMMARY.md` when done.
</output>
