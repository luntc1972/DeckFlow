---
phase: 77-multi-axis-deck-score
plan: 06
type: execute
wave: 4
depends_on: ["77-05"]
files_modified:
  - README.md
autonomous: false
requirements: [SCORE-04]
must_haves:
  truths:
    - "The score block renders correctly with the analysis.multi-axis-score flag ON across at least Classic, Azorius, and Nyx themes"
    - "The 4-card grid collapses 4->2->1 cleanly at desktop, tablet, and phone widths with no horizontal scroll"
    - "The cross-check note shows the correct agree/diverge state with its glyph and color"
    - "README documents the multi-axis score behavior and the flag"
  artifacts:
    - path: "README.md"
      provides: "Multi-axis score + flag documentation"
      contains: "multi-axis"
  key_links:
    - from: "operator flag ON"
      to: "/deck-analysis Step-3 render"
      via: "live browser verification across themes + viewports"
      pattern: "chatgpt-score"
---

<objective>
Visually verify the live score block across themes and viewports with the flag ON, capture
screenshots, and document the feature + flag in the README. This is the human sign-off gate for
SCORE-04's "renders in the live `/deck-analysis`" promise.

Purpose: cross-theme + mobile visual correctness cannot be proven by unit tests (UI-phase visual
verify rule).
Output: README updated; screenshots captured; operator approval recorded.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/77-multi-axis-deck-score/UI-SPEC.md
@CLAUDE.md
@README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Document the multi-axis score and flag in README; start the test server with the flag ON</name>
  <read_first>
    - README.md (existing deck-analysis / feature-flag documentation sections — match tone and the existing flag-table style used for tap-analyzer and bracket)
    - CLAUDE.md (Testing constraint: start the web app with scripts/run-web-test.sh — sets DECKFLOW_DISABLE_AUTO_BROWSER=true; never open a browser on the Windows host; flag must be flipped ON in the local store for the verify)
    - .planning/phases/77-multi-axis-deck-score/UI-SPEC.md §1-2, §10 (what the user should see on-page and in the artifact)
  </read_first>
  <action>
    Add a short README subsection describing the four-axis Power/Speed/Control/Consistency score block that
    folds into `/deck-analysis` (Step-3 results + all three paste artifacts) and the `analysis.multi-axis-score`
    flag (seeded OFF; operator flips it ON). Note the bands are coarse 0-5 heuristic estimates that the AI
    re-checks. Then start the local web app via `scripts/run-web-test.sh` (DECKFLOW_DISABLE_AUTO_BROWSER=true) and
    enable the `analysis.multi-axis-score` flag in the local store so the next task can verify live. Do NOT
    open a browser on the Windows host.
  </action>
  <verify>
    <automated>grep -ci "multi-axis\|analysis.multi-axis-score" README.md</automated>
  </verify>
  <acceptance_criteria>
    - README.md contains a multi-axis score description and names the `analysis.multi-axis-score` flag
    - The local web server is running with the flag ON (headless; no Windows-host browser launched)
  </acceptance_criteria>
  <done>README documents the feature + flag; test server is up with the flag enabled for live verify.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    A four-axis Power/Speed/Control/Consistency score block in the `/deck-analysis` Step-3 results panel
    (above the Overview) and in all three paste artifacts, behind `analysis.multi-axis-score` (now ON locally).
    Each axis shows a numeral, a 5-pip meter, a band-label pill (None..Extreme), and an inline rationale; a
    bracket cross-check note shows agree (green/✓) or diverge (gold/⚠).
  </what-built>
  <how-to-verify>
    1. Visit the local `/deck-analysis`, paste a deck, and run an analysis to reach Step-3 results.
    2. Confirm the DECK SCORE grid appears ABOVE the Overview with four cards (POWER/SPEED/CONTROL/CONSISTENCY),
       each showing numeral + 5-pip meter + band pill + rationale; verify no decimals are shown.
    3. Confirm the cross-check note renders with the correct agree/diverge glyph + color and names the bracket.
    4. Switch themes (at minimum Classic/default, Azorius, Nyx) and confirm the band pills stay legible (baked
       ink contrast) on each.
    5. Resize to tablet (~760px) and phone (~400px): confirm the grid collapses 4 -> 2 -> 1 with no horizontal
       scroll and the cards stay centered.
    6. Confirm the pasted artifact (download/copy) contains the ASCII "DECK SCORE (coarse 0-5 bands ...)" block
       with plain hyphens (no em/en dashes).
    7. Capture screenshots under .planning/ui-design/cycle13/screenshots/ (Classic + Azorius + Nyx + one mobile).
  </how-to-verify>
  <resume-signal>Type "approved" or describe the visual issues to fix.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| (none new) | Documentation + visual verification only; no code paths added |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-77-06-01 | Information Disclosure | local verify with flag ON | accept | Verification is local/headless only; flag remains seeded OFF in prod (operator flips intentionally) |
| T-77-SC | Tampering | package installs | mitigate | No new packages; README + visual verify only |
</threat_model>

<verification>
- README documents the feature + flag (grep confirms)
- Human-verify checkpoint approved with screenshots across Classic/Azorius/Nyx + mobile
</verification>

<success_criteria>
Operator confirms the live score block renders correctly across themes + viewports and the artifact carries
the ASCII score block; README updated. Flag stays seeded OFF in prod.
</success_criteria>

<output>
Create `.planning/phases/77-multi-axis-deck-score/77-06-SUMMARY.md` when done
</output>
