---
phase: 35-value-re-validation-gate
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - DeckFlow.Web.Tests/Spike001KbValueAbHarness.cs
  - .planning/spikes/001-kb-value-ab/gen-artifacts.py
autonomous: true
requirements: [KBV-01]
executor: codex   # Codex available again (2026-06-10); KBV-01 is code → Codex implements, Claude reviews. (35-02 judgment stays Claude.)

must_haves:
  truths:
    - "The harness emits a baseline prompt and a with-context prompt for all 5 bracket-spanning decks (10 files total) into the spike dir."
    - "Decks 2-5 carry real Scryfall oracle data baked into the harness (no stub/placeholder cards) and real ~99-card decklists."
    - "Each deck's with-context prompt is assembled from clips selected by the REAL fixed ContentKbRelevanceService over the rebuilt 82-row corpus — not hardcoded clips."
    - "The 5 decks span at least brackets Core(2), Upgraded(3), Optimized(4), cEDH(5) and distinct archetypes (proliferate/goodstuff, aggro/voltron, combo, control/stax, lands/ramp)."
    - "The corpus rebuild step (gen-artifacts.py) writes artifacts/spike-rows.json that the harness reads."
  artifacts:
    - path: "DeckFlow.Web.Tests/Spike001KbValueAbHarness.cs"
      provides: "5-deck real-retrieval A/B emitter Fact"
      contains: "EmitRealRetrievalPromptAllDecks"
    - path: ".planning/spikes/001-kb-value-ab/baseline-atraxa.txt"
      provides: "baseline prompt for deck 1 (one of 5 baseline files)"
    - path: ".planning/spikes/001-kb-value-ab/with-context-atraxa.txt"
      provides: "with-context prompt for deck 1 (one of 5 with-context files)"
  key_links:
    - from: "Spike001KbValueAbHarness.cs"
      to: "ContentKbRelevanceService.GetRelevantClipsAsync"
      via: "real service over FakeContentSiteIndexStore(spike-rows.json)"
      pattern: "new ContentKbRelevanceService\\("
    - from: "gen-artifacts.py"
      to: "artifacts/spike-rows.json"
      via: "rebuild from uat-content-kb.db + content-site-index.db"
      pattern: "spike-rows\\.json"
---

<objective>
Extend `Spike001KbValueAbHarness` from the single Atraxa deck to 5 bracket-spanning decks. For each
deck, run the REAL fixed `ContentKbRelevanceService` over the rebuilt 82-row prod corpus, then emit a
baseline prompt (deck only) and a with-context prompt (deck + real-selected clips) — 10 files total —
into `.planning/spikes/001-kb-value-ab/`. These 10 files are the inputs the gate (plan 35-02) judges.

Purpose: KBV-01. The frozen Spike 001 verdict tested only Atraxa; PITFALLS P12 (single-deck overfit)
requires the gate to span archetypes and brackets so a fix that happens to work for proliferate/counters
content is not mistaken for a general win.

Output: 10 emitted prompt files (5 baseline + 5 with-context), one selected-clips trace per deck, and a
harness Fact that builds clean and runs green. Deterministic, offline-corpus, zero new NuGet deps.

EXECUTOR: Claude (Codex is at the user's weekly limit this week — CLAUDE.md Codex-unavailable exception).
Do NOT dispatch Codex; do NOT route for Codex review.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/35-value-re-validation-gate/35-CONTEXT.md
@.planning/spikes/001-kb-value-ab/README.md
@.planning/spikes/001-kb-value-ab/VERDICT.md
@.planning/research/PITFALLS.md

# The harness to extend — EmitRealRetrievalPrompt is the template; EmitAbPrompts stays untouched.
@DeckFlow.Web.Tests/Spike001KbValueAbHarness.cs

# Corpus rebuild + deck-data baking scripts (run/adapt, do not rewrite their approach).
@.planning/spikes/001-kb-value-ab/gen-artifacts.py
@.planning/spikes/001-kb-value-ab/fetch-deck-cards.py

# The fixed retriever under test — READ-ONLY this phase. Do NOT modify it.
@DeckFlow.Web/Services/ContentKbRelevanceService.cs

<interfaces>
<!-- Contracts the executor needs. Use these directly — no codebase exploration required. -->

ScryfallCard positional ctor (DeckFlow.Web/Services/ScryfallDtos.cs:39) — the harness bakes cards as:
  new(Name, ManaCost, TypeLine, OracleText, Power, Toughness, Keywords[], ColorIdentity[], SetCode, SetName, CollectorNumber)
  (CardFaces/Id/Layout default to null). fetch-deck-cards.py already emits exactly this 11-arg form.

ContentKbExcerpt (DeckFlow.Web/Models/ContentKbExcerpt.cs) — required: Source, Title, VideoUrl,
  TimestampLabel, Excerpt, HarvestDate; optional: Score, ClipOrigin. All members are { get; init; }
  (System.Text.Json skips get-only — do NOT convert to get-only).

ContentSiteIndexRow (DeckFlow.Core/Knowledge/ContentArtifactSpec.cs:107) — deserialized from
  spike-rows.json. Relevant fields: Id, Source, Title, VideoUrl, ArtifactPath, ArchetypeTags[],
  BracketTags[], CardCategoryTags[], YoutubeVideoId, IsVisible, IsEvergreen.

ContentKbRelevanceService real ctor (read-only): new ContentKbRelevanceService(store,
  resolveArtifactPath: relativePath => Path.Combine(artifactsRoot, relativePath),
  flagCache: FakeFeatureFlagCache({ ["content.kb.enabled"] = true }),
  archetypeDeriver: new ContentKbArchetypeDeriver(new FakeCategoryKnowledgeStore()), logger: null).
  Call: await relevance.GetRelevantClipsAsync(commanderName, bracket, deckArchetypes) — returns
  IReadOnlyList<ContentKbExcerpt>? (null/empty allowed; zero-clip cold-start is a VALID result per P12).

Available test doubles (already exist — DO NOT re-create):
  DeckFlow.Web.Tests/TestDoubles/FakeContentSiteIndexStore.cs (.Rows.AddRange)
  DeckFlow.Web.Tests/TestDoubles/FakeFeatureFlagCache.cs
  DeckFlow.Web.Tests/TestDoubles/FakeCategoryKnowledgeStore.cs
  ContentKbArchetypeDeriver (DeckFlow.Web/Services/ContentKbArchetypeDeriver.cs)

Corpus coverage CONFIRMED from artifacts/spike-rows.json (82 rows, snail-heavy):
  archetypes present: ramp(30) voltron(24) aristocrats(21) reanimator(14) tribal(17) value-engine(55)
    aggro(19) stax(20) tokens(17) control(33) lands(15) midrange(43) blink(5) combo(31) spellslinger(6)
  brackets present: Core(28) Upgraded(28) Optimized(17) cEDH(17) Exhibition(6)
  On-point videos exist for every target slot (e.g. "Aggro Ideology: A Guide to Spee-DH" [aggro/voltron];
  "EDH's Combo Conundrum"/"How I Built My Most Powerful Deck (for $100)" [combo]; "How to Play More
  Removal"/"The Things EDH Players Get Wrong about Interaction" [control/stax]; "You Might Have Too Much
  Ramp"/"All of the Reasons You Should Put More Lands in your EDH Deck" [lands/ramp]).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rebuild corpus inputs + select and bake 4 new decks' real Scryfall data</name>
  <read_first>
    - .planning/spikes/001-kb-value-ab/gen-artifacts.py (corpus rebuild; note it currently references an
      undefined `DB` and writes /tmp/rows.json instead of artifacts/spike-rows.json — fix both)
    - .planning/spikes/001-kb-value-ab/fetch-deck-cards.py (the Scryfall→C# initializer emitter; baked
      output is the 11-arg `new(...)` form already used in the harness GetDefaultTestCards)
    - DeckFlow.Web.Tests/Spike001KbValueAbHarness.cs lines 366-459 (GetDefaultTestCards baked-card shape
      to mirror for the new decks)
    - 35-CONTEXT.md `### KBV-01 — Deck set` (the 4 bracket/archetype slots that decks 2-5 MUST satisfy)
  </read_first>
  <files>.planning/spikes/001-kb-value-ab/gen-artifacts.py, DeckFlow.Web.Tests/Spike001KbValueAbHarness.cs</files>
  <action>
    First confirm the corpus rebuild works end-to-end (per KBV-01 the gitignored corpus must be rebuilt
    before the Fact). Fix `gen-artifacts.py` so it (a) uses TWO separate sqlite connections — do NOT
    collapse to one handle: open `artifacts/content-site-index.db` for the visible `content_site_index`
    rows AND `artifacts/uat-content-kb.db` for `content_videos`/`content_summaries`/`content_clips`
    (the script currently references an undefined single `DB`; a naive `DB=` rename would run the
    summary/clip queries against the index DB, which lacks those tables, and silently emit EMPTY
    `## Summary` / `## Key Clips` in every regenerated artifact — the plan-checker Warning-1 regression).
    Join index rows to content rows by the youtube video id (`natural_key_value` ↔ `content_videos.youtube_video_id`).
    And (b) write the rows JSON to `artifacts/spike-rows.json` (the path the harness reads), not
    `/tmp/rows.json`. Touch only the db-handle/connection and output-path lines; preserve the existing
    artifact-.md reconstruction logic and LF endings. Run it
    (`python3 .planning/spikes/001-kb-value-ab/gen-artifacts.py`) and confirm it prints ~82 visible rows
    and rewrites artifacts/spike-rows.json. The two source .db files already exist under artifacts/
    (gitignored); if either is missing, STOP and report — do not fabricate corpus data.

    Then pick 4 concrete, well-known real ~99-card Commander decklists (so Scryfall lookups resolve),
    one per slot from 35-CONTEXT.md — these slots are LOCKED, only the concrete commander/list is your
    discretion:
      Deck 2 — aggro/voltron, Bracket 4 Optimized (e.g. a mono-color or two-color voltron/aggro general
        such as Rograkh/Light-Paws/Wilson; archetype profile terms: aggro, voltron).
      Deck 3 — combo, Bracket 5 cEDH (e.g. a known cEDH combo general such as Kinnan/Najeela/Thrasios+partner;
        archetype profile terms: combo, control).
      Deck 4 — control/stax, Bracket 2 Core or 3 Upgraded (e.g. a stax/control general such as
        Grand Arbiter/Winota-free pick like Talrand-control or Kenrith-stax; archetype profile terms:
        control, stax).
      Deck 5 — lands/ramp, Bracket 2 Core (e.g. a lands/ramp general such as Lord Windgrace/Omnath/Azusa;
        archetype profile terms: lands, ramp).
    Brackets across the 5 MUST span Core / Upgraded / Optimized / cEDH (deck 1 Atraxa is Upgraded).

    For each new deck, fetch real Scryfall oracle data using the fetch-deck-cards.py pattern
    (cards/collection batch endpoint with the required User-Agent + Accept headers, DFC front-face
    name join, 11-arg C# emitter with apostrophe escaping). NOTE (Codex review LOW): fetch-deck-cards.py
    is hardcoded to `/tmp/atraxa_deck.txt` and does not emit a canonical decklist string —
    parameterize its input/output paths before fetching the 4 lists, and ASSERT the fetch reports zero
    `NOT FOUND` / `UNMATCHED` cards before baking (a missing card = a typo in the decklist; fix the name,
    do not bake a stub). Bake the resulting `ScryfallCard` initializers into the harness as per-deck
    baked-card arrays (mirror the existing GetDefaultTestCards shape — one array constant or helper per
    deck). Also capture, per deck, the
    canonical decklist string (counts + commander) for the DeckAnalysisRequest.DeckSource, the
    TargetCommanderBracket label, and the explicit archetype-profile term set (HashSet<string>,
    OrdinalIgnoreCase) matching that deck's slot. Do NOT introduce placeholder/stub cards — every card
    must carry real oracle text. Preserve LF, no reformat of existing harness code.
  </action>
  <verify>
    <automated>cd /mnt/c/users/chrislunt/source/personal/deckflow && python3 -c "import glob,os;ms=sorted(glob.glob('artifacts/content-kb/salubrious-snail/*.md'));assert ms,'no snail artifacts to probe';os.remove(ms[0]);open('/tmp/regen_probe','w').write(ms[0]);print('deleted',ms[0])" && python3 .planning/spikes/001-kb-value-ab/gen-artifacts.py 2>&1 | grep -i "visible index rows" && test -f artifacts/spike-rows.json && python3 -c "import json;rows=json.load(open('artifacts/spike-rows.json'));print('rows',len(rows));assert len(rows)>=80" && python3 -c "p=open('/tmp/regen_probe').read().strip();import os;assert os.path.exists(p),f'{p} not regenerated — gen-artifacts skipped/failed';body=open(p,encoding='utf-8').read();assert '- **[' in body and '## Summary' in body and len(body.split('## Summary')[1].split('##')[0].strip())>0,f'{p} regenerated with EMPTY Summary/Key Clips — wrong-DB single-handle regression (Codex MED #3)';print('force-regenerated with non-empty content:',p)"</automated>
  </verify>
  <done>
    gen-artifacts.py runs clean and (re)writes artifacts/spike-rows.json with ~82 rows; 4 new decks'
    real baked ScryfallCard data + decklist strings + bracket labels + archetype term sets are present in
    the harness; brackets across the 5 decks span Core/Upgraded/Optimized/cEDH; no stub cards.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add the 5-deck real-retrieval Fact and emit 10 prompt files</name>
  <read_first>
    - DeckFlow.Web.Tests/Spike001KbValueAbHarness.cs lines 95-220 (EmitRealRetrievalPrompt = the
      single-deck template to generalize; CreateService + CreateAnalysisRequest helpers to parameterize)
    - .planning/spikes/001-kb-value-ab/with-context-real.txt + selected-clips-real.txt (current
      single-deck output format to mirror per deck)
  </read_first>
  <files>DeckFlow.Web.Tests/Spike001KbValueAbHarness.cs</files>
  <action>
    Add a new `[Fact]` (e.g. `EmitRealRetrievalPromptAllDecks`) that generalizes the existing
    `EmitRealRetrievalPrompt` flow across all 5 decks. Keep `EmitAbPrompts` and the original
    `EmitRealRetrievalPrompt` Facts intact (the frozen Atraxa Run-2 evidence stays reproducible).

    Build the real `ContentKbRelevanceService` ONCE over the rebuilt corpus (FakeContentSiteIndexStore
    loaded from artifacts/spike-rows.json, flag forced on, ContentKbArchetypeDeriver over a
    FakeCategoryKnowledgeStore — exactly as the existing EmitRealRetrievalPrompt does at lines 113-118).
    For each of the 5 decks: parameterize CreateAnalysisRequest (DeckSource, TargetCommanderBracket) and
    the card lookups. SEAM FIX (Codex review MED): the existing `CreateService` uses `static`
    execute*Override lambdas that always call `GetDefaultTestCards()` — a `static` lambda cannot close
    over per-deck state, so do NOT introduce global mutable state. Instead: keep the existing
    `CreateService(relevanceService)` UNCHANGED (so EmitAbPrompts + the original EmitRealRetrievalPrompt
    keep working), and ADD an overload `CreateService(IContentKbRelevanceService relevanceService,
    IReadOnlyList<ScryfallCard> cards)` whose execute*Override lambdas are NON-static and close over
    `cards`; add matching helper overloads `CreateCollectionResponse(request, cards)`,
    `CreateSearchResponse(request, cards)`, `CreateNamedResponse(request, cards)` (the existing
    GetDefaultTestCards-based helpers stay for the old Facts). Pass each deck's baked card array to the
    overload. Call
    GetRelevantClipsAsync(commanderName, bracket, deckArchetypes) with that deck's explicit archetype set,
    then BuildAsync twice: once with the real-selected clips (with-context) and once with null clips
    (baseline). Emit per deck, with deck-slug filenames:
      baseline-{slug}.txt, with-context-{slug}.txt, selected-clips-{slug}.txt
    (slug = atraxa, plus 4 slugs for decks 2-5). 10 prompt files + 5 clip traces total. The
    selected-clips trace per deck must list, per clip, score + Source + Title + TimestampLabel + Excerpt
    (mirror the existing format at lines 132-142) so the gate can audit P1 warning signs (all-one-video,
    foreign-commander noise).

    Assertions: each with-context file contains "## Expert Context"; each baseline file does NOT; all 10
    files exist and are non-empty. A deck legitimately returning zero clips (cold-start) is VALID per
    PITFALLS P12 — in that case emit the baseline file, emit a with-context file equal to baseline (no
    Expert Context block), and record "0 clips (cold-start)" in that deck's clip trace; do NOT fail the
    Fact and do NOT lower the relevance floor to manufacture clips.

    Build the test project with the Windows dotnet over WSL and run only this Fact. Zero new NuGet deps,
    LF preserved, no reformat of untouched harness code.
  </action>
  <verify>
    <automated>cd /mnt/c/users/chrislunt/source/personal/deckflow && "/mnt/c/Program Files/dotnet/dotnet.exe" test DeckFlow.Web.Tests --filter "FullyQualifiedName~Spike001KbValueAbHarness.EmitRealRetrievalPromptAllDecks" 2>&1 | grep -E "Passed!|Failed!|error" && N=$(ls .planning/spikes/001-kb-value-ab/baseline-*.txt .planning/spikes/001-kb-value-ab/with-context-*.txt 2>/dev/null | grep -Ev "with-context-real\.txt" | wc -l); echo "deck files: $N (expect 10, legacy *-real.txt excluded)"; test "$N" -eq 10</automated>
  </verify>
  <done>
    The new Fact builds clean and passes; exactly 5 baseline-*.txt + 5 with-context-*.txt files (10 total)
    plus 5 selected-clips-*.txt traces exist in the spike dir; with-context files contain "## Expert
    Context" (except documented cold-start decks), baselines do not; EmitAbPrompts + the original
    EmitRealRetrievalPrompt still pass unchanged.
  </done>
</task>

</tasks>

<threat_model>
No new attack surface. This phase runs the already-hardened (Phase 34) `ContentKbRelevanceService`
read-only over a local offline corpus and emits text files into the planning dir; it adds no endpoint,
no new input boundary, no package install, and does not flip `content.kb.enabled` in production
(that is Phase 36 / KBD-01). The Phase 34 prompt-injection mitigations (KBR-03 sanitizer + structural
fence) remain in force on the rendered prompts. No STRIDE register required for this plan.
</threat_model>

<verification>
- `python3 gen-artifacts.py` rebuilds artifacts/spike-rows.json (~82 rows) without error.
- `dotnet test --filter Spike001KbValueAbHarness` (Windows dotnet) is green for all Facts.
- 10 prompt files + 5 clip traces present; with-context files contain the Expert Context block (non-cold-start).
- No edits to ContentKbRelevanceService.cs; no new NuGet packages; LF preserved.
</verification>

<success_criteria>
KBV-01 satisfied: the Spike 001 harness runs the FIXED retriever across 5 representative,
bracket-spanning decks (distinct commanders/archetypes, not just Atraxa) and emits a baseline + a
with-context prompt per deck for the gate to judge.
</success_criteria>

<output>
Create `.planning/phases/35-value-re-validation-gate/35-01-SUMMARY.md` when done.
</output>
