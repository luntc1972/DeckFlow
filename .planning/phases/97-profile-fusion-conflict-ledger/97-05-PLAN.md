---
phase: 97-profile-fusion-conflict-ledger
plan: 05
type: tdd
wave: 3
depends_on: [97-01, 97-02, 97-04]
files_modified:
  - DeckFlow.Core/Knowledge/ProfileFusion/ProfileFusionEngine.cs
  - DeckFlow.Core.Tests/ProfileFusion/ProfileFusionEngineTests.cs
autonomous: true
requirements: [CS-16, CS-16a, CS-17, CS-18, CS-20]
must_haves:
  truths:
    - "ProfileFusionEngine.Fuse(measured, statedRules) returns FusedTarget[] where each active stated rule is joined on (mapped-metric, condition) — never metric alone (D-08) — to its measured counterpart, resolved per D-06, and carries a verdict badge."
    - "Observable metrics resolve Value to the measured value (stated band kept as ledger reference); philosophy/stated-only metrics resolve Value to the stated value and never produce a conflict."
    - "A condition-scoped stated rule (e.g. counters in condition=control) does NOT produce a false delta against an unconditional whole-deck aggregate — because P95 MeasuredMetric has no condition/archetype field, no measured segment can match, so the row resolves to verdict `insufficient-measured` with reason `no-condition-breakdown` and Conflict=null (CS-16a, honest labeling not manufactured agreement)."
    - "Superseded stated rules appear in the output as history (verdict/source noting superseded), never as active targets."
    - "The engine has zero Web/HTTP references and is fully deterministic."
  artifacts:
    - path: "DeckFlow.Core/Knowledge/ProfileFusion/ProfileFusionEngine.cs"
      provides: "Pure Fuse(measured, statedRules) -> FusedTarget[] orchestrating mapper+classification+collapser+conflict"
      contains: "public static IReadOnlyList<FusedTarget> Fuse"
  key_links:
    - from: "DeckFlow.Core/Knowledge/ProfileFusion/ProfileFusionEngine.cs"
      to: "StatedMetricKeyMapper / MetricClassification / StatedRuleRecencyCollapser / ConflictCalculator"
      via: "composes all four pure units into one join"
      pattern: "StatedMetricKeyMapper|ConflictCalculator"
---

<objective>
Compose the four pure units (Plans 02+04) into `ProfileFusionEngine.Fuse(measured, statedRules)` —
the single deterministic entry point that produces the fused profile: recency-collapse → map key →
classify → join on (metric, condition) → conflict/coverage evaluate → resolve target → emit
FusedTarget[] with verdict badges.

Purpose: This is the intellectual core of the phase (CS-16/16a/17/18/20). It is the falsifiable
rubric Phase 99 will diff decks against. Wave 3 — needs the schema (01), the mapper+classification
(02), and the collapser+conflict math (04).

Output: One pure-Core static engine + integration-style unit tests grounded on the prototype.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/97-profile-fusion-conflict-ledger/97-CONTEXT.md
@.planning/phases/97-profile-fusion-conflict-ledger/97-PATTERNS.md
@docs/research/p89-p90-prototype-snail.md

<interfaces>
Consumes (all pure-Core, already built):
  StatedRuleRecencyCollapser.Collapse(statedRules) -> { Active, Superseded }        [Plan 04]
  StatedMetricKeyMapper.TryMapToMeasuredKey / map-kind (Direct|Derived|StatedOnly)  [Plan 02]
  MetricClassification.Classify(metric) -> Observable|Philosophy                    [Plan 02]
  ConflictCalculator.Evaluate(statedRule, measuredValue, coverage) -> verdict+conflict [Plan 04]

Produces: IReadOnlyList<FusedTarget> (DeckFlow.Core/Knowledge/CreatorStyleProfile.cs, extended in Plan 01).

Inputs: IReadOnlyList<MeasuredMetric> measured, IReadOnlyList<StatedRuleCandidate> statedRules.
land_count derivation: measured value = karsten:target_lands.Value + karsten:land_delta.Value
  (approximation — document it in a // Why: comment; RESEARCH Assumption A2).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ProfileFusionEngine.Fuse — the composed deterministic join</name>
  <files>DeckFlow.Core/Knowledge/ProfileFusion/ProfileFusionEngine.cs</files>
  <read_first>
    - DeckFlow.Core/Knowledge/StatedRulesExtraction/StatedRuleReducer.cs (static single-entry-point pure-function shape)
    - DeckFlow.Core/Knowledge/ProfileFusion/StatedMetricKeyMapper.cs, MetricClassification.cs (Plan 02 outputs)
    - DeckFlow.Core/Knowledge/ProfileFusion/StatedRuleRecencyCollapser.cs, ConflictCalculator.cs (Plan 04 outputs)
    - DeckFlow.Core/Knowledge/CreatorStyleProfile.cs (FusedTarget additive fields to populate)
    - .planning/phases/97-profile-fusion-conflict-ledger/97-PATTERNS.md (ProfileFusionEngine section, lines 174-207 — signature + step outline)
  </read_first>
  <behavior>
    - Test (CS-16a conditionality): a counters rule scoped condition="archetype:control" joined against a measured category_ratio:counter that is an unconditional whole-deck aggregate (no Condition on MeasuredMetric — verified: P95 does not segment by condition, and P95 is out of this phase's blast radius) does NOT produce a false conflict. The row resolves to verdict "insufficient-measured", VerdictReason="no-condition-breakdown", Conflict=null — NOT an "agree" and NOT a delta. A companion assertion confirms the measured counter sample IS above the coverage floor, proving the rejection is condition-scope, not low-sample.
    - Test (CS-17 observable): a ramp rule (observable) resolves FusedTarget.Value = measured value; stated band retained in StatedMin/StatedMax; Source indicates measured-weighted.
    - Test (CS-17 philosophy): a power_level_philosophy rule (stated-only, no measured counterpart) resolves Value = stated value, verdict "philosophy-stated-only", Conflict == null (never conflicts).
    - Test (D-09 history): a superseded stated rule appears as a FusedTarget flagged superseded (verdict/source), not counted as an active target.
    - Test (land_count derivation): given karsten:target_lands=37 and karsten:land_delta=0.4, the land_count row's measured value ≈ 37.4 and verdict against 37-42 = agree.
    - Test (determinism): calling Fuse twice on the same inputs yields an equal FusedTarget[] (order + values).
  </behavior>
  <action>
    Create static class `ProfileFusionEngine` (namespace `DeckFlow.Core.Knowledge.ProfileFusion`),
    `public static IReadOnlyList<FusedTarget> Fuse(IReadOnlyList<MeasuredMetric> measured,
    IReadOnlyList<StatedRuleCandidate> statedRules)`. Steps: (1) `ArgumentNullException.ThrowIfNull`
    both. (2) `StatedRuleRecencyCollapser.Collapse` -> active + superseded. (3) For each ACTIVE rule:
    map metric via StatedMetricKeyMapper (Direct/Derived/StatedOnly); classify Observable/Philosophy.
    (4) Philosophy/StatedOnly -> emit FusedTarget{ Value=stated value (or band midpoint), Source
    "stated", Verdict "philosophy-stated-only", Conflict=null, StatedMin/Max, SourceClip, VideoDateUtc,
    Confidence band }. (5) Observable -> look up the measured metric by the mapped key AND matching
    Condition (D-08 join on (metric, condition)); for land_count compute the derived value (document
    approximation). CRITICAL (verified): `MeasuredMetric` has NO Condition field and P95 emits only
    unconditional whole-deck aggregates — so when the stated rule carries a non-null Condition there is
    no condition-matched measured segment to join. In that case emit verdict "insufficient-measured",
    VerdictReason="no-condition-breakdown", Conflict=null (this is the honest CS-16a "no false delta"
    outcome — NOT a manufactured "agree"). Only when the stated rule's Condition is null/unconditional
    (or matches an unconditional aggregate) do you call ConflictCalculator; resolve Value = measured
    value (D-06 observable-wins); set Verdict, VerdictReason (pass through ConflictCalculator's, e.g.
    "low-sample" on coverage-floor failure, else null), Conflict, MeasuredValue, NumDecks,
    EffectiveSampleSize, StatedMin/Max, Weight (measured-weighted for observable; a fixed convention,
    document it). (6) For
    each SUPERSEDED rule: emit a FusedTarget flagged as superseded history (e.g. Source "stated-superseded"
    or a Verdict marker) so the ledger shows it as history, never active. (7) Deterministic ordering
    (e.g. active first by metric then condition, superseded last). Map confidence double -> coarse
    band (low/med/high) for the Confidence field (D-10, informational). NO prose field. NO Web/HTTP
    references. LF endings.
  </action>
  <verify>
    <automated>/mnt/c/Program\ Files/dotnet/dotnet.exe test DeckFlow.Core.Tests --filter "FullyQualifiedName~ProfileFusionEngine"</automated>
  </verify>
  <acceptance_criteria>
    - CS-16a (explicit, blocker-fix): a condition-scoped stated rule with NO matching measured condition breakdown -> verdict "insufficient-measured", VerdictReason="no-condition-breakdown", Conflict=null (asserted). This is the ONLY sanctioned outcome for that case — it must NOT be "agree" and must NOT emit a FusedConflict. A companion assertion proves the measured sample is above the coverage floor (so the reason is condition-scope, not low-sample).
    - CS-17: observable -> Value == measured; philosophy -> Value == stated + Conflict null + verdict "philosophy-stated-only" (asserted).
    - D-09: superseded rule present as history, excluded from active targets (asserted).
    - land_count derived value ≈ target_lands + land_delta, verdict agree vs 37-42 (asserted).
    - Determinism: two Fuse calls produce Assert.Equal FusedTarget[] (asserted).
    - `grep -rn "using DeckFlow.Web\|using RestSharp\|using Microsoft.AspNetCore" DeckFlow.Core/Knowledge/ProfileFusion/ProfileFusionEngine.cs` returns NOTHING (CS-20).
  </acceptance_criteria>
  <done>Fuse composes the four units into a deterministic, condition-aware, verdict-badged FusedTarget[].</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: End-to-end prototype scenario test (the Snail say-vs-do table)</name>
  <files>DeckFlow.Core.Tests/ProfileFusion/ProfileFusionEngineTests.cs</files>
  <read_first>
    - docs/research/p89-p90-prototype-snail.md (the full P90 fusion table — land/ramp/draw/board-wipe/counters verdicts)
    - DeckFlow.Core/Knowledge/ProfileFusion/ProfileFusionEngine.cs (Task 1 output)
    - .planning/phases/97-profile-fusion-conflict-ledger/97-VALIDATION.md (Wave 0 golden-fixture requirement)
  </read_first>
  <behavior>
    - Build a synthetic-but-prototype-grounded fixture: MeasuredMetric[] with category_ratio:ramp≈12, category_ratio:draw≈11.1, category_ratio:board-wipe≈1.2, category_ratio:counter≈12 (an UNCONDITIONAL whole-deck aggregate — MeasuredMetric has no Condition field; P95 does not segment by archetype), karsten:target_lands≈37, karsten:land_delta≈0.4; StatedRuleCandidate[] with the matching prototype bands (lands 37-42, ramp 7-12, draw 13-18, board-wipe 3-5 max/lte, counters gte 8 with Condition="archetype:control") + one power_level_philosophy stated-only rule. Give the counter measured metric an EffectiveSampleSize well above MinDeckFloor so a low-sample rejection is impossible.
    - Assert the full ledger: land=agree, ramp=agree, draw=conflict, board-wipe=agree, counters=insufficient-measured (VerdictReason="no-condition-breakdown"; the control-scoped stated rule has no condition-matched measured segment — this is the CS-16a "no false delta" guarantee, matching D-02's ⚠ on counters), philosophy=philosophy-stated-only. Companion assertion: the counters row's measured EffectiveSampleSize is above MinDeckFloor, proving the rejection is condition-scope (no-condition-breakdown), NOT a coverage-floor (low-sample) rejection.
    - Assert the flagship case (ii): board-wipe verdict is "agree", NOT "conflict" (deviates-from-canon-but-matches-own-philosophy).
  </behavior>
  <action>
    Add a `[Fact] Fuse_SnailPrototypeScenario_ReproducesSayVsDoLedger()` asserting each metric's
    verdict matches the prototype table. Keep the fixture inline + readable (a small builder helper is
    fine). This is the phase's headline regression guard for the fusion rubric. LF endings.
  </action>
  <verify>
    <automated>/mnt/c/Program\ Files/dotnet/dotnet.exe test DeckFlow.Core.Tests --filter "FullyQualifiedName~ProfileFusionEngine"</automated>
  </verify>
  <acceptance_criteria>
    - The scenario test asserts all six verdicts: land=agree, ramp=agree, draw=conflict, board-wipe=agree, counters=insufficient-measured (VerdictReason="no-condition-breakdown"), philosophy=philosophy-stated-only.
    - Counters row: asserted NOT "agree" and NOT a FusedConflict; companion assertion proves measured sample > MinDeckFloor so the reason is condition-scope not low-sample (CS-16a honest labeling).
    - board-wipe asserts "agree" (case ii), proving a conflicts-only view would misrepresent it.
    - Test passes deterministically.
  </acceptance_criteria>
  <done>The engine reproduces the discriminating Snail say-vs-do ledger end-to-end.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Pure in-process composition of already-validated data; no I/O, no untrusted input crosses here. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-97-06 | Tampering | Incorrect join emits false say-vs-do deltas (CS-16a) | mitigate | A condition-scoped stated rule cannot join an unconditional measured aggregate (MeasuredMetric has no Condition field) — the engine resolves it to `insufficient-measured`/`no-condition-breakdown`, never a conflict; the counters scenario test + an above-floor companion assertion lock this; determinism test prevents nondeterministic output. |
| T-97-SC | Tampering | npm/pip/cargo installs | accept | No package-manager installs in this plan. |
</threat_model>

<verification>
- `dotnet.exe test DeckFlow.Core.Tests --filter FullyQualifiedName~ProfileFusionEngine` green.
- Full prototype scenario ledger asserted; determinism asserted.
- Zero Web/HTTP using-directives in ProfileFusionEngine.cs (CS-20).
</verification>

<success_criteria>
A single pure-Core deterministic Fuse produces the condition-aware, verdict-badged fused profile
that reproduces the prototype's discriminating say-vs-do ledger.
</success_criteria>

<output>
Create `.planning/phases/97-profile-fusion-conflict-ledger/97-05-SUMMARY.md` when done.
</output>
