---
phase: 83-packet-service-srp-split
plan: 05
type: execute
wave: 2
depends_on: ["83-01", "83-02", "83-03"]
files_modified:
  - DeckFlow.Web/Services/MetaGapService.cs
  - DeckFlow.Web.Tests/MetaGapServiceTests.cs
autonomous: true
requirements: [PKTSVC-02, PKTSVC-03, PKTSVC-04]
must_haves:
  truths:
    - "MetaGapService delegates Scryfall oracle-name resolution to ScryfallReferenceResolver (SearchFallback strategy, normalize OFF) and holds no private batch loop or private Chunk."
    - "It delegates commander reflag to DeckEntryReflagHelper."
    - "Its meta-gap paste artifact is byte-identical pre/post refactor across all 3 AI variants."
  artifacts:
    - path: "DeckFlow.Web/Services/MetaGapService.cs"
      provides: "MetaGap orchestration shell delegating resolution + reflag to shared collaborators"
      contains: "ScryfallReferenceResolver"
  key_links:
    - from: "DeckFlow.Web/Services/MetaGapService.cs"
      to: "DeckFlow.Web/Services/Packets/ScryfallReferenceResolver.cs"
      via: "instantiated from the already-injected IScryfallCardResolver"
      pattern: "new ScryfallReferenceResolver"
---

<objective>
Migrate MetaGapService (956 LOC) onto the shared resolver + reflag helper, reducing it to an
orchestration shell — gated by the 83-01 byte-identity harness.

Purpose: Remove MetaGap's copy of the Scryfall batch loop, its differently-shaped private Chunk, and
its byte-identical ReflagCommanderEntry — delegating to ScryfallReferenceResolver and
DeckEntryReflagHelper — with zero change to its paste artifact. MetaGap's two decklist builders are
DIVERGENT and stay in the service.
Output: A slimmer MetaGapService whose MetaGap byte-identity suite still passes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/83-packet-service-srp-split/83-RESEARCH.md
@CLAUDE.md

<do_not_unify>
- MetaGap's `BuildCanonicalDecklistText` (:296-336) AND `BuildCompactDecklist`/`BuildCompactRefDecklist`
  (:674-720) are DIVERGENT decklist shapes for different purposes — do NOT route either through
  PacketTextAssembler.BuildSectionedDecklistText.
- Keep `BuildComboReferenceText(label,...)` (:768-810) as-is (Cluster F).
- Keep `BuildCanonicalDeckSourceText` (:119-136) as-is (Cluster C — cache-key text).
</do_not_unify>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delegate oracle-name resolution to ScryfallReferenceResolver + reflag helper</name>
  <files>DeckFlow.Web/Services/MetaGapService.cs, DeckFlow.Web.Tests/MetaGapServiceTests.cs</files>
  <read_first>
    - DeckFlow.Web/Services/MetaGapService.cs:562-621 (ResolveOracleNameMapAsync), :759-765 (Chunk), :610 (SearchFallbackCardAsync path), :465-485 (ReflagCommanderEntry)
    - DeckFlow.Web/Services/Packets/ScryfallReferenceResolver.cs (ResolveBatchAsync + ScryfallBatchResolution)
    - DeckFlow.Web/Services/Packets/DeckEntryReflagHelper.cs (ReflagCommanderEntry)
    - DeckFlow.Web.Tests/MetaGapByteIdentityTests.cs (from 83-01 — the gate)
    - DeckFlow.Web.Tests/MetaGapServiceTests.cs (CreateService seam)
  </read_first>
  <action>
    In the MetaGapService constructor(s) (DI + internal test ctor), instantiate
    `new ScryfallReferenceResolver(cardResolver)` from the already-injected IScryfallCardResolver;
    store in a private readonly field. Replace `ResolveOracleNameMapAsync` with a call to
    `ResolveBatchAsync(names, SearchFallbackCardAsync delegate, normalizeForScryfall: false, ct)`,
    then use only the returned `OracleNameMap` (keyed by ORIGINAL request name; MetaGap retains no
    ScryfallCard list today — keep that exact shape). Delete the private `Chunk`. Replace the private
    `ReflagCommanderEntry` calls with `DeckEntryReflagHelper.ReflagCommanderEntry(...)` and delete the
    private copy (the no-commander-section reflag fixture in 83-01 gates this path). If MetaGap's
    `BuildRequestContextText` per-field lines are routed through `PacketTextAssembler.AppendKeyValueLine`,
    pass `JsonTextFormatterService.NormalizeSingleLine` as the normalizer delegate (MetaGap's CURRENT
    normalizer). Do NOT change the
    DI extension file. Do NOT touch the two divergent decklist builders or the combo/cache-key methods.

    Add a MetaGapServiceTests case asserting the oracle-name map is unchanged for a fixture with a
    collection-miss recovered via SearchFallback.
  </action>
  <verify>
    <automated>dotnet.exe test DeckFlow.Web.Tests --filter "FullyQualifiedName~MetaGap" 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - Build 0/0; full MetaGap test set + MetaGapByteIdentityTests pass (3 platforms).
    - No private Chunk / ResolveOracleNameMapAsync batch loop / ReflagCommanderEntry remain (grep confirms).
    - BuildCanonicalDecklistText, BuildCompactDecklist, BuildComboReferenceText, BuildCanonicalDeckSourceText remain untouched.
    - The DI extension file is unchanged.
  </acceptance_criteria>
  <done>MetaGapService resolves via the shared resolver and reflags via the shared helper; its byte-identity gate is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → MetaGap request model | Existing MetaGapRequest validation untouched; refactor re-routes internal call graph only. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-83-08 | Tampering | accidentally routing MetaGap's divergent decklist through the assembler, changing bytes | mitigate | do_not_unify fence + MetaGapByteIdentityTests Ordinal assertion after the task. |
| T-83-09 | Tampering | swapped fallback strategy changing which card an oracle-name maps to | mitigate | ResolveBatchAsync keeps SearchFallback delegate; byte-identity gate confirms. |
</threat_model>

<verification>
- `dotnet.exe test DeckFlow.Web.Tests --filter "FullyQualifiedName~MetaGap"` → all pass (incl. byte-identity).
- Grep: no private Chunk / batch loop / ReflagCommanderEntry in MetaGapService.
</verification>

<success_criteria>
MetaGapService delegates resolution + reflag to the shared collaborators, retains no duplicate
resolution path, keeps its divergent decklist/combo/cache-key logic, and its paste artifact is
byte-identical across all 3 AI variants (PKTSVC-02/03; gated by PKTSVC-04).
</success_criteria>

<output>
Create `.planning/phases/83-packet-service-srp-split/83-05-SUMMARY.md` when done.
</output>
