---
phase: 20.1-phase-21-live-uat-gate
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/ROADMAP.md
autonomous: false
requirements: []
user_setup:
  - service: openai
    why: "Live distill UAT makes real gpt-4o-mini calls (~$0.05 actual spend) against the 10-video UAT db. Code-gated evals E1-E4 are already unit-test covered; the live run is the only way to exercise E5 (archetype/bracket plausibility) + E6 (thesis coherence) human-judge dims."
    env_vars:
      - name: OPENAI_API_KEY
        source: "platform.openai.com -> API keys (operator's own key; NOT committed; set in the WSL shell that runs the distill command)"

must_haves:
  truths:
    - "Live distill run exits 0 against artifacts/uat-content-kb.db with OPENAI_API_KEY set (real, no --dry-run)"
    - "10 markdown artifact files exist under content-kb/{source-slug}/ after the run"
    - "Each sampled artifact has a non-empty summary section, 3-8 clips, and only allowlisted archetype/bracket/card_category tags"
    - "Operator confirms E5 (archetype/bracket plausibility) + E6 (thesis coherence) on 2-3 sampled artifacts"
    - "/gsd-verify-work 21 passes against Phase 21 acceptance"
    - "ROADMAP Phase 21 line 97 flipped from [ ] to [x]"
  artifacts:
    - path: "content-kb/"
      provides: "10 emitted .md prompt artifacts (local-only, gitignored)"
      contains: "summary + 3-8 clips + allowlisted tags per artifact"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 21 marked complete"
      contains: "[x] **Phase 21: Content KB Distillation"
  key_links:
    - from: "distill CLI verb"
      to: "artifacts/uat-content-kb.db"
      via: "--db artifacts/uat-content-kb.db --limit 10 (no --dry-run)"
      pattern: "distill --db artifacts/uat-content-kb.db"
    - from: "live distill run result (UAT pass)"
      to: "ROADMAP Phase 21 [x]"
      via: "/gsd-verify-work 21 then flip + commit"
      pattern: "\\[x\\] \\*\\*Phase 21"
---

<objective>
Run the deferred LIVE-UAT human-verify gate for Phase 21 (Content KB Distillation), the only remaining gate before Phase 21 can be marked complete. Phase 21 is code-complete and pushed (4/4 plans, full suite green, dry-run UAT passed projecting $0.045491). This gate executes the REAL distill run — which incurs ~$0.05 OpenAI spend and requires an operator-supplied OPENAI_API_KEY — then samples the emitted artifacts for the two human-judge eval dimensions (E5 archetype/bracket plausibility, E6 thesis coherence), then verifies and flips ROADMAP Phase 21 to [x].

Purpose: Close the live-UAT checkpoint that could not run unattended (needs a secret + real spend + human judgment), unblocking the Phase 21 close and the v1.4 critical path (21 -> 22).
Output: 10 local .md artifacts under content-kb/, an operator E5/E6 sign-off, a passing /gsd-verify-work 21, and ROADMAP Phase 21 flipped to [x] + committed.

THIS PLAN WRITES ZERO PRODUCTION CODE. No edits, refactors, or test authoring. The only repo file modified is .planning/ROADMAP.md (one checkbox flip). content-kb/ artifacts are gitignored and are NOT committed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/HANDOFF.json
@.planning/phases/21-content-kb-distillation-artifact-emit-local/.continue-here.md
@.planning/phases/21-content-kb-distillation-artifact-emit-local/21-AI-SPEC.md
@.planning/phases/21-content-kb-distillation-artifact-emit-local/21-04-SUMMARY.md
@CLAUDE.md

<fixed_scope>
<!-- Encode this verbatim; do NOT re-derive. Sourced from Phase 21 HANDOFF.json + .continue-here.md. -->
- UAT db: artifacts/uat-content-kb.db (confirmed present, ~404KB) — 5 enabled sources, 10 videos, all transcript_status=captions (from Phase 20 UAT). None distilled yet.
- dotnet binary: "/mnt/c/Program Files/dotnet/dotnet.exe" (Windows-dotnet-over-WSL per CLAUDE.md; do NOT use a Linux dotnet).
- Distill command (REAL run, NO --dry-run):
  "/mnt/c/Program Files/dotnet/dotnet.exe" run --project DeckFlow.CLI -- distill --db artifacts/uat-content-kb.db --limit 10
- A prior --dry-run already passed (projected $0.045491, ~20x cheaper than the AI-SPEC §4 estimate). This plan runs the REAL distill.
- Expected real-run side effects: ~10 .md artifacts under content-kb/{source-slug}/, 10 slim-index rows, per-call LlmSpendLedger entries (3 calls per video), content_distill_status=distilled rows, one content_harvest_runs row.
- content-kb/ is gitignored (line 5 of .gitignore confirmed) — artifacts are local-only; do NOT plan to commit them.
- Branch is v1.4 (confirmed). Plain default-author commits, no Co-Authored-By trailer. Do NOT push to main.
</fixed_scope>

<eval_dims_for_sampling>
<!-- From 21-AI-SPEC §5. E1-E4 are code-gated and already unit-test-covered; ONLY E5/E6 are sampled by hand here. -->
- E5 archetype/bracket/card-category accuracy: archetype tag matches the deck's actual primary game plan (not keyword-driven); bracket reflects the power tier the video situates the deck in (no inflation casual->Optimized/cEDH); card_category roles are ones the video genuinely emphasizes (no removal<->board-wipe / tutor<->draw conflation). Calibration target >= 80% plausible; NOT a hard gate.
- E6 summary thesis fidelity + fabricated-fact rate: summary captures the video's actual strategic thesis grounded entirely in the transcript; clips point to genuinely useful moments; ZERO fabricated MTG facts (combos/interactions/cards not in the transcript). Target: 0 fabricated facts on the sample; >= 90% "summary faithful = yes".
</eval_dims_for_sampling>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-flight — confirm clean baseline before any paid run</name>
  <read_first>
    - CLAUDE.md (R-7 origin staleness on resume; Do-Not-Modify list; dotnet path; no-push rule)
    - .planning/HANDOFF.json (next_action command + fixed scope)
  </read_first>
  <action>
    Establish a clean baseline so the live run is attributable and reproducible. Run (read-only, no spend):
    (1) git fetch and compare local HEAD vs origin/v1.4 (R-7) — confirm branch is v1.4 and not behind in a way that would invalidate the Phase 21 code under test.
    (2) Confirm artifacts/uat-content-kb.db exists and is the expected ~404KB UAT db.
    (3) Build clean: "/mnt/c/Program Files/dotnet/dotnet.exe" build DeckFlow.sln -c Debug — must be 0 warnings / 0 errors (VSTest is unreliable in WSL per CLAUDE.md; do NOT run a dotnet test gate here — build-clean is the gate).
    (4) Record content-kb/ baseline: capture whether content-kb/ already has any .md files (expected: none distilled yet). This is the before-count for Task 3's after-count delta.
    Do NOT set OPENAI_API_KEY or run the distill command in this task — this is the unattended-safe pre-flight only.
  </action>
  <verify>
    <automated>"/mnt/c/Program Files/dotnet/dotnet.exe" build DeckFlow.sln -c Debug 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `git branch --show-current` prints `v1.4`
    - `artifacts/uat-content-kb.db` exists (file present, non-empty)
    - Solution build reports `0 Warning(s)` and `0 Error(s)`
    - content-kb/ before-state recorded (count of pre-existing *.md files, expected 0)
  </acceptance_criteria>
  <done>Baseline confirmed: on v1.4, build clean, UAT db present, content-kb/ before-count recorded. No spend incurred.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Operator sets OPENAI_API_KEY + authorizes spend</name>
  <what-built>Phase 21 distill CLI verb is code-complete and dry-run-verified. The live paid run requires the operator's own OPENAI_API_KEY (a secret that cannot be set unattended) and authorization for ~$0.05 of real OpenAI spend.</what-built>
  <read_first>
    - .planning/phases/21-content-kb-distillation-artifact-emit-local/.continue-here.md (Next Action block + anti-patterns)
    - CLAUDE.md (secrets handling — never echo OPENAI_API_KEY into chat/logs/commits)
  </read_first>
  <how-to-verify>
    Operator action (cannot be automated — needs a human-held secret + spend approval):
    1. In the SAME WSL shell that will run the distill command, set the key WITHOUT echoing it into chat/logs:
       export OPENAI_API_KEY=sk-...    (operator types their own key)
    2. Confirm authorization for ~$0.05 (five cents) of real gpt-4o-mini spend against the 10-video UAT set. The prior dry-run projected $0.045491; the live run should land near that.
    3. Confirm the key is set for this shell only (do NOT add it to any committed file, .env, appsettings, or render.yaml; do NOT commit it — public repo).
    4. Reply "approved — key set, spend authorized" (or describe any blocker) to release Task 3.
  </how-to-verify>
  <acceptance_criteria>
    - Operator has set OPENAI_API_KEY in the live-run shell (verified by operator presence, NOT by printing the value)
    - Operator has explicitly authorized the ~$0.05 spend
    - No secret was written to any tracked file
  </acceptance_criteria>
  <resume-signal>Type "approved — key set, spend authorized" or describe the blocker.</resume-signal>
</task>

<task type="auto" autonomous="false">
  <name>Task 3: Run the live distill + auto-verify side effects</name>
  <read_first>
    - .planning/HANDOFF.json (exact distill command in next_action)
    - .planning/phases/21-content-kb-distillation-artifact-emit-local/21-04-SUMMARY.md (what the distill verb does + expected side effects)
    - 21-AI-SPEC §7 (run-record metrics: videos distilled, LLM calls, spend USD, parse-failure count, out-of-vocab WARN count, aborted_reason)
  </read_first>
  <action>
    With OPENAI_API_KEY set in the shell (from Task 2) and spend authorized, run the REAL distill (no --dry-run):
      "/mnt/c/Program Files/dotnet/dotnet.exe" run --project DeckFlow.CLI -- distill --db artifacts/uat-content-kb.db --limit 10
    Capture the full command output for review. Then auto-verify the deterministic side effects WITHOUT re-running (this is the E1-E4 production-side confirmation; the unit tests already cover the code paths):
    (1) Exit code is 0.
    (2) content-kb/ now contains >= 10 *.md files (after-count minus Task 1 before-count == 10, one per distilled video, under {source-slug}/ subdirs).
    (3) The run output reports a non-zero "videos distilled" count (expected 10), an LLM-calls count (expected 3 per distilled video), an LLM spend USD value near the $0.045491 dry-run projection, a parse-failure count of 0 (E1 production target < 0.1%), and no aborted_reason (spend stayed under the $15 DECKFLOW_LLM_MONTHLY_CAP_USD cap — SC5).
    (4) Each emitted .md artifact has a non-empty summary section, a clips section with 3-8 clips, and tags drawn only from the allowlist (no out-of-vocab tag persisted — E2). Out-of-vocab candidates, if any, appear as WARN-only in the log, NOT in the artifacts.
    Do NOT commit content-kb/ artifacts (gitignored). Do NOT re-run distill on success (idempotent skip would no-op; re-running wastes spend). If the run exits non-zero or distilled count < 10, STOP and surface the output to the operator — do NOT flip ROADMAP.
  </action>
  <verify>
    <automated>test $(find content-kb -name '*.md' 2>/dev/null | wc -l) -ge 10 && echo "OK: >=10 artifacts emitted" || echo "FAIL: fewer than 10 artifacts"</automated>
  </verify>
  <acceptance_criteria>
    - distill command exits 0
    - content-kb/ contains >= 10 *.md files after the run (delta of 10 vs Task 1 before-count)
    - Run output shows: videos distilled == 10, parse-failure count == 0, no aborted_reason, LLM spend within ~2x of $0.045491
    - Every sampled artifact's tags are allowlisted (zero out-of-vocab tags in any emitted .md)
    - No OPENAI_API_KEY value appears in any captured output, log, or planning file
  </acceptance_criteria>
  <done>Live distill ran exit 0, emitted 10 allowlisted-tag artifacts under content-kb/, run record clean (0 parse failures, no cap abort), spend near projection. E1-E4 confirmed in production; ready for human E5/E6 sampling.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Human E5/E6 sampling of emitted artifacts</name>
  <what-built>10 distilled .md prompt artifacts under content-kb/{source-slug}/, each with a summary, 3-8 timestamped clips, and archetype/bracket/card_category tags. E1-E4 (schema, allowlist, summary length, clip count/timestamp sanity) are unit-test-covered and production-confirmed in Task 3. E5/E6 are MTG-correctness dims the strict schema cannot express — they need a human (operator / experienced Commander player) to judge.</what-built>
  <read_first>
    - 21-AI-SPEC §1b (domain rubric: archetype fidelity, bracket accuracy, card-category framing, summary thesis fidelity, clip usefulness)
    - 21-AI-SPEC §5 (E5/E6 rubric Good/Bad + pass thresholds)
  </read_first>
  <how-to-verify>
    Sample 2-3 of the freshly emitted artifacts (operator's choice; prefer one clean single-archetype video + one potential failure-bait video if present — e.g. a midrange list that name-drops a combo, or a casual video that name-drops powerful cards):
    E5 (archetype/bracket plausibility):
      - Does the archetype tag match the deck's actual primary game plan, NOT just a name-dropped keyword?
      - Does the bracket tag reflect the power tier the video actually situates the deck in (no inflation casual->Optimized/cEDH, no flattening genuine cEDH->Upgraded)?
      - Are the card_category tags roles the video genuinely emphasizes (no removal<->board-wipe / tutor<->draw conflation)?
    E6 (thesis coherence + fabrication):
      - Does the <=200-word summary capture the video's actual strategic thesis, grounded in the transcript?
      - Are the clips genuinely useful moments (not intro/sponsor/filler)?
      - Are there ANY fabricated MTG facts (combos/interactions/cards not in the video)? Target: zero.
    Reply "E5/E6 pass" (or list specific issues per artifact). Per AI-SPEC these are quality signals, not hard code gates — but a fabricated-fact finding or systematic bracket inflation SHOULD block the Phase 21 flip and trigger a tags/summary-prompt revision follow-up rather than a [x].
  </how-to-verify>
  <acceptance_criteria>
    - Operator sampled >= 2 emitted artifacts for E5 + E6
    - E5: sampled artifacts' archetype/bracket/card_category tags judged plausible (>= 80% plausible target)
    - E6: 0 fabricated MTG facts found in the sample; summaries judged faithful to the transcript (>= 90% "faithful = yes" target)
    - Operator explicitly signals pass or lists blocking quality issues
  </acceptance_criteria>
  <resume-signal>Type "E5/E6 pass" to proceed to verify + ROADMAP flip, or list per-artifact issues (which block the flip and become a prompt-revision follow-up).</resume-signal>
</task>

<task type="auto">
  <name>Task 5: /gsd-verify-work 21, flip ROADMAP Phase 21 to [x], commit</name>
  <read_first>
    - .planning/ROADMAP.md line 97 (the Phase 21 list checkbox to flip)
    - CLAUDE.md (commit conventions: Conventional Commits, plain default-author, no Co-Authored-By, no push to main; Do-Not-Modify list — ROADMAP.md is NOT on it)
    - .planning/phases/21-content-kb-distillation-artifact-emit-local/.continue-here.md (post-UAT-pass step sequence)
  </read_first>
  <action>
    Only reachable after Task 3 (live run clean) AND Task 4 (E5/E6 pass). Then:
    (1) Run /gsd-verify-work 21 to verify Phase 21 against its PLAN acceptance / must-haves now that the live gate has passed. If verification surfaces gaps, STOP and surface them — do NOT flip ROADMAP.
    (2) On verify pass, flip ROADMAP Phase 21 from "[ ]" to "[x]" at line 97 ONLY (the exact line:
        "- [ ] **Phase 21: Content KB Distillation + Artifact Emit (local)** ... — *exec #5*"
        becomes "- [x] **Phase 21: Content KB Distillation + Artifact Emit (local)** ... — *exec #5*").
        Touch ONLY that one checkbox character per CLAUDE.md R-6 touch-only-what-you-touch. Do NOT reformat surrounding lines, do NOT flip any other phase, do NOT alter the Phase 21 detail block (lines 236-256) checkboxes unless verify explicitly requires it.
        SAFEGUARD (per memory feedback_planner_clobbers_roadmap): record `wc -l .planning/ROADMAP.md` before and after the edit; the line count MUST be unchanged. If it changed, the edit clobbered the file — git checkout and redo.
    (3) Commit the single-line ROADMAP flip on branch v1.4 with a plain default-author Conventional Commit, e.g.:
        docs(roadmap): mark Phase 21 complete after live distill UAT
        Do NOT commit content-kb/ artifacts (gitignored). Do NOT push to main. Do NOT add a Co-Authored-By trailer.
  </action>
  <verify>
    <automated>grep -c '^- \[x\] \*\*Phase 21: Content KB Distillation' .planning/ROADMAP.md</automated>
  </verify>
  <acceptance_criteria>
    - /gsd-verify-work 21 reports pass (no open gaps)
    - ROADMAP line 97 reads `[x] **Phase 21: Content KB Distillation` (grep count == 1)
    - `wc -l .planning/ROADMAP.md` is unchanged from before the edit (no clobber)
    - Exactly one file (.planning/ROADMAP.md) is staged in the commit; content-kb/ is NOT staged
    - Commit is plain default-author, no Co-Authored-By trailer, on branch v1.4, not pushed to main
  </acceptance_criteria>
  <done>Phase 21 verified live, ROADMAP Phase 21 flipped to [x] (line count unchanged), single-file commit landed on v1.4 with no Co-Authored-By and no artifacts staged.</done>
</task>

</tasks>

<verification>
- Build clean (0/0) at pre-flight; VSTest NOT run (unreliable in WSL — build-clean is the gate per CLAUDE.md).
- Live distill exits 0 against artifacts/uat-content-kb.db; >= 10 .md artifacts emitted under content-kb/.
- Run record: 10 videos distilled, 0 parse failures, no aborted_reason, spend near $0.045491 (< $15 cap).
- Every emitted artifact carries only allowlisted tags (E2 production confirmation); summaries <= 200 words and clips 3-8 (E3/E4, unit-covered).
- Operator E5/E6 sign-off: plausible archetype/bracket/card_category tags, faithful summaries, zero fabricated facts on the sample.
- /gsd-verify-work 21 passes; ROADMAP Phase 21 flipped to [x] with line count unchanged; single-file commit on v1.4.
</verification>

<success_criteria>
- Phase 21's deferred live-UAT human-verify gate is executed: real distill run completed with operator-supplied OPENAI_API_KEY + authorized spend.
- 10 prompt artifacts produced locally (gitignored, not committed) and sampled for the two human-judge eval dims.
- E5 (archetype/bracket plausibility) and E6 (thesis coherence / zero fabrication) signed off by the operator.
- /gsd-verify-work 21 passes and ROADMAP Phase 21 is [x].
- Zero production code changed; the only tracked-file edit is the one-character ROADMAP checkbox flip + its commit.
</success_criteria>

<output>
Create `.planning/phases/20.1-phase-21-live-uat-gate/20.1-01-SUMMARY.md` when done, recording: actual spend, videos distilled, parse-failure/abort counts, which artifacts were sampled for E5/E6 and the verdict, the /gsd-verify-work 21 result, and the ROADMAP-flip commit hash.
</output>
