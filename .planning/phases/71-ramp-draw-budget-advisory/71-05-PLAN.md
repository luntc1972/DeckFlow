---
phase: 71-ramp-draw-budget-advisory
plan: 05
type: execute
wave: 5
depends_on: ["71-04"]
files_modified: []
autonomous: false
requirements: [PLV-GLOSS, PLV-VERDICT, PLV-BUDGET]
must_haves:
  truths:
    - "The verdict + glosses + ramp/draw budget render correctly with the flag ON across desktop + mobile and 2 themes, for an issue deck and a clean deck"
    - "cEDH shows glosses but no ramp/draw budget; flag OFF is visually unchanged"
  artifacts: []
  key_links: []
---

<objective>
Human visual verification of the plain-language layer on a live server (Scryfall-backed), since the
CI Playwright suite cannot exercise a real analysis. Confirms the flag-ON UI across viewports/themes
and the flag-OFF byte-identical baseline before ship.

Purpose: project rule - UI/responsive changes need browser verification at 2+ viewports across themes.
Output: operator sign-off (or a defect list to feed gap closure).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/71-ramp-draw-budget-advisory/71-04-SUMMARY.md
@.planning/phases/71-ramp-draw-budget-advisory/71-SPEC.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Plain-language verdict, metric glosses, and Casual-only ramp/draw budget on /manabase,
    behind the `manabase.plain-language-verdict` flag (seeded OFF). CSS in site-common.css via theme
    tokens; live Playwright spec gated to DECKFLOW_LIVE_E2E.</what-built>
  <how-to-verify>
    1. Start the web app headless (no Windows browser): `scripts/run-web-test.sh` (sets
       DECKFLOW_DISABLE_AUTO_BROWSER=true). Confirm it is reachable at http://localhost:5173.
    2. Flag OFF baseline: with the flag OFF, analyze any deck on /manabase. Confirm NO glosses, NO
       "Reading your deck" block, NO ramp/draw block - i.e. the page looks exactly like before.
    3. Toggle the flag ON via /Admin/Flags (manabase.plain-language-verdict).
    4. Casual, ISSUE deck (a color-short / ramp-light list): analyze and confirm - (a) a plain gloss
       under each metric lens; (b) a "Reading your deck" block with prioritized add/remove lines
       ordered land/color -> ramp -> draw (max 3); (c) a ramp/draw budget line with the "(N do both)"
       count, the proxy disclosure, and the "community heuristic, not Karsten math" label.
    5. Casual, CLEAN deck (well-supported two-color): confirm the "Reading your deck" block shows a
       specific why-it's-fine (names the colors that cleared + the avg on-curve cast rate + Casual),
       not a generic "looks good".
    6. cEDH deck: confirm glosses render but the ramp/draw budget block is ABSENT.
    7. Repeat steps 4-6 at desktop (~1280px) AND mobile (~390px) widths, and on at least 2 guild
       themes (one light, one dark). Confirm no layout break in the two-lens band, no overflow, and
       readable contrast for the new blocks on both themes.
    8. Confirm the manabase prompt artifact ("Copy this prompt for ChatGPT / Claude") now contains the
       "Reading your deck" verdict text (Casual) and the ramp/draw budget line.
    9. (Optional) Run the live e2e: `DECKFLOW_LIVE_E2E=1 npx --no-install playwright test manabase-verdict`.
  </how-to-verify>
  <resume-signal>Type "approved" or list the defects (theme/viewport/mode + what's wrong) to feed
    `/gsd-plan-phase 71 --gaps`.</resume-signal>
</task>

</tasks>

<verification>
Operator confirms flag-OFF baseline unchanged and flag-ON renders correctly across desktop+mobile,
2 themes, issue + clean Casual decks, and cEDH (glosses, no budget); prompt artifact mirrors the verdict.
</verification>

<success_criteria>
Visual sign-off across viewports/themes/modes, or a defect list captured for gap closure.
</success_criteria>

<output>
Create `.planning/phases/71-ramp-draw-budget-advisory/71-05-SUMMARY.md` when done.
</output>
