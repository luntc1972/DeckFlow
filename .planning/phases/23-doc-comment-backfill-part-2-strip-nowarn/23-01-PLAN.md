---
phase: 23-doc-comment-backfill-part-2-strip-nowarn
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - DeckFlow.Web/Models/MetaGapResponse.cs
  - DeckFlow.Web/Models/DeckComparisonResponse.cs
  - DeckFlow.Web/Models/DeckAnalysisResponse.cs
  - DeckFlow.Web/Models/SetUpgradeResponse.cs
  - DeckFlow.Web/Models/EdhTop16Entry.cs
autonomous: true
requirements: [DOC-01]
must_haves:
  truths:
    - "Every public type AND public member in the 5 response/DTO files carries an attached XML doc-comment (<summary> or <inheritdoc/>)"
    - "A from-clean build of DeckFlow.Web with both doc suppressors OFF emits zero CS1591/CS1573/CS1587 for these 5 files"
    - "No { get; init; } was converted to { get; }; no [Attribute] inlined; no raw-string re-indented; LF preserved"
  artifacts:
    - path: "DeckFlow.Web/Models/MetaGapResponse.cs"
      provides: "Type + member <summary> on all public records/properties (58 sites)"
    - path: "DeckFlow.Web/Models/DeckComparisonResponse.cs"
      provides: "Type + member <summary> (28 sites)"
    - path: "DeckFlow.Web/Models/DeckAnalysisResponse.cs"
      provides: "Type + member <summary> (26 sites)"
    - path: "DeckFlow.Web/Models/SetUpgradeResponse.cs"
      provides: "Type + member <summary> (16 sites)"
    - path: "DeckFlow.Web/Models/EdhTop16Entry.cs"
      provides: "Type + member <summary> (16 sites)"
  key_links:
    - from: "these 5 files"
      to: "the Wave-2 suppressor-flip gate (23-05)"
      via: "0 CS1591/1573/1587 when both suppressors are removed"
      pattern: "warning CS(1591|1573|1587)"
---

<objective>
Backfill XML doc-comments on the 5 heaviest Models response/DTO files: 144 of the 475
authoritative warning sites (compiler-derived inventory, 23-RESEARCH.md). These are v1.1-era
record DTOs whose public properties trip CS1591 at the member level (not just type level) —
e.g. MetaGapResponse.cs alone has 58 sites for a handful of records.

Purpose: DOC-01 (finish the response-DTO surface) so the Wave-2 two-file suppressor flip (23-05)
can prove a clean gate. This plan owns an EXCLUSIVE, non-overlapping file set so it runs in
parallel with 23-02 / 23-03 / 23-04.
Output: 5 fully-documented Models files; zero doc-warnings on these files when built with
suppressors off.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# AUTHORITATIVE inventory + conventions — read these before touching code:
@.planning/phases/23-doc-comment-backfill-part-2-strip-nowarn/23-RESEARCH.md
@.planning/phases/17-doc-comment-backfill-part-1-controllers-services/17-CONTEXT.md
@./CLAUDE.md

<conventions>
Carry forward Phase 17 D-01..D-04 (23-RESEARCH "Convention Reuse"):
- D-01a: standalone records/classes (no interface) get a full `<summary>` written directly.
- D-04: every record gets a type-level `<summary>`; under the strip, CS1591 ALSO fires on every
  undocumented public PROPERTY of a public record — so EACH public property/positional-param here
  needs documentation too (this is why the site count is per-member, not per-type).
- D-03 voice: explain WHY/what-it-represents, not the obvious; house style of existing summaries.
  Seed from CLAUDE.md Component-Responsibilities one-liners where a type is listed; write fresh otherwise.

MEMBER-COVERAGE RULE (mandatory — CS1591 fires on EVERY public declaration, not just the type):
Document EVERY public declaration in the allowed files, including public classes, records,
interfaces, enums, enum members, constructors, methods, properties, and positional record
parameters — not just the top-level type. The per-file site counts in 23-RESEARCH.md are
compiler-derived (the compiler counts members), so each listed count already reflects the full
per-member inventory; documenting only the top-level type will leave warnings behind and fail the
23-05 gate.
</conventions>

<formatting_landmines>
CLAUDE.md R-6 — BINDING, this is a mechanical doc-only edit. Touch ONLY the `///` lines you add:
- NEVER convert `{ get; init; }` → `{ get; }` (System.Text.Json silently drops get-only props in .NET 9+; already broke EdhTop16Client once).
- NEVER inline `[Attribute]` onto the property/declaration line.
- NEVER re-indent C# raw-string literals (changes the literal shipped to the AI).
- Preserve LF line endings; preserve switch expressions; NO Format Document / Code Cleanup.
- A property with a documented `<summary>` on the line(s) immediately above it (skipping attribute lines, NOT blank lines) is what the compiler counts. A detached summary (blank line between comment and decl) does NOT count — Phase 17 hit this on ScryfallCard.
</formatting_landmines>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Document MetaGapResponse.cs + DeckComparisonResponse.cs + DeckAnalysisResponse.cs</name>
  <files>DeckFlow.Web/Models/MetaGapResponse.cs, DeckFlow.Web/Models/DeckComparisonResponse.cs, DeckFlow.Web/Models/DeckAnalysisResponse.cs</files>
  <action>
ALLOWED FILE SET (this task may edit ONLY these 3 files — ignore every other file/task):
  DeckFlow.Web/Models/MetaGapResponse.cs (58 sites), DeckComparisonResponse.cs (28), DeckAnalysisResponse.cs (26).
Add an attached `<summary>` doc-comment to EVERY public declaration in these files that lacks one —
public classes, records, interfaces, enums, enum members, constructors, methods, properties, and
positional record params (per the MEMBER-COVERAGE RULE above; this is why MetaGapResponse has 58
sites). Use `<inheritdoc/>` only where a member implements a documented interface member (rare here —
these are standalone records). Seed wording from CLAUDE.md Component-Responsibilities where the type
is listed; write fresh, WHY-focused prose otherwise. Touch ONLY the `///` lines you add (R-6
landmines above).
  </action>
  <verify>
    <automated>DOTNET="/mnt/c/Program Files/dotnet/dotnet.exe"; git stash list >/dev/null; for f in MetaGapResponse DeckComparisonResponse DeckAnalysisResponse; do echo "== $f =="; grep -c '/// <summary>\|/// <inheritdoc' "DeckFlow.Web/Models/$f.cs"; done</automated>
  </verify>
  <done>Each of the 3 files has a `///` comment attached above every public declaration (type AND member — incl. constructors, properties, positional params, any co-located enums/classes); git diff shows only added `///` lines (no get-init→get-only, no attribute inlining, no raw-string re-indent).</done>
</task>

<task type="auto">
  <name>Task 2: Document SetUpgradeResponse.cs + EdhTop16Entry.cs</name>
  <files>DeckFlow.Web/Models/SetUpgradeResponse.cs, DeckFlow.Web/Models/EdhTop16Entry.cs</files>
  <action>
ALLOWED FILE SET (this task may edit ONLY these 2 files — ignore every other file/task):
  DeckFlow.Web/Models/SetUpgradeResponse.cs (16 sites), EdhTop16Entry.cs (16).
Same rule as Task 1: attached `<summary>` on EVERY public declaration (type, record, enum, enum
member, constructor, method, property, positional record param — per the MEMBER-COVERAGE RULE).
EdhTop16Entry holds `{ get; init; }` props — DO NOT touch their accessor list, only add the `///`
line above. Touch ONLY the `///` lines you add (R-6 landmines above).
  </action>
  <verify>
    <automated>for f in SetUpgradeResponse EdhTop16Entry; do echo "== $f =="; grep -c '/// <summary>\|/// <inheritdoc' "DeckFlow.Web/Models/$f.cs"; grep -c '{ get; init; }' "DeckFlow.Web/Models/$f.cs"; done</automated>
  </verify>
  <done>Both files documented at type + every-member level (incl. constructors/positional params); `{ get; init; }` count unchanged from HEAD (no get-only mutation).</done>
</task>

</tasks>

<verification>
This plan's 5 files contribute to the Phase gate proved in 23-05. Local per-plan check is a SMOKE
CHECK ONLY (anchored grep; blind spot acknowledged — it confirms a `<summary>` exists somewhere in
each file but does NOT prove every public member carries an attached doc-comment). The AUTHORITATIVE
per-member gate is 23-05's suppressors-off, all-three-code build (CS1591/CS1573/CS1587 promoted).
This local check does NOT require the suppressors to be off:
  grep -L '<summary>' DeckFlow.Web/Models/{MetaGapResponse,DeckComparisonResponse,DeckAnalysisResponse,SetUpgradeResponse,EdhTop16Entry}.cs
must return empty.
`dotnet build -c Release` (Windows dotnet.exe) must stay 0/0 with NoWarn still in place.
</verification>

<success_criteria>
- All 5 files carry attached type-level + member-level doc-comments on EVERY public declaration (144 sites closed).
- `git diff` shows only added `///` lines (R-6 discipline; no accessor/attribute/raw-string changes).
- `dotnet build -c Release` stays 0 warnings / 0 errors (NoWarn still present this wave).
- Smoke grep is non-authoritative; the real per-member gate is 23-05's suppressors-off all-three-code build.
</success_criteria>

<output>
Create `.planning/phases/23-doc-comment-backfill-part-2-strip-nowarn/23-01-SUMMARY.md` when done.
</output>
