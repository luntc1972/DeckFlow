---
phase: 60-pull-from-prod-reconcile
plan: 03
type: execute
wave: 2
depends_on: [60-01, 60-02]
files_modified:
  - DeckFlow.Studio/Pages/PullFromProd.razor
  - DeckFlow.Studio/Shared/NavMenu.razor
  - DeckFlow.Studio/Program.cs
  - DeckFlow.Studio.Tests/TestDoubles/FakeSshArtifactDownloader.cs
  - DeckFlow.Studio.Tests/TestDoubles/FakeProdContentReader.cs
  - DeckFlow.Studio.Tests/PullFromProdPageTests.cs
  - README.md
autonomous: true
requirements: [SYNC-01, SYNC-03]
must_haves:
  truths:
    - "When prod or SCP is unconfigured, the page shows warning banners and the Pull button is disabled"
    - "Stage 1 reads prod rows via the read-only IProdContentReader (NOT ContentSiteIndexStore / no EnsureSchemaAsync) + downloads artifacts to pull-staging/ (never content-kb/), wiping staging at the start of each pull (R1)"
    - "Stage 1/2 runs the classifier and renders a per-entry diff table with the 4 kinds + a download-status flag"
    - "Stage 2 adopt-prod applies LOCAL-only: UpsertContentColumnsOnlyAsync on the local store + mirrors prod approval_status via SetApprovalStatusAsync + File.Move staged->content-kb"
    - "adopt-prod is NOT offered for LocalOnly entries (display-only)"
    - "On partial pull (ArtifactDownloaded=false) adopt-prod stays SELECTABLE — it upserts the local row + mirrors approval_status, shows a warning badge, and skips ONLY File.Move (R4)"
    - "keep-local makes no local store write"
    - "The prod reader has no write API (R1/R2 structural guarantee); Stage 2 writes hit ONLY the local store; a Stage-2 hard-guard blocks re-invoke before classify is ready"
    - "No ex.Message or secret value surfaces in any UI banner or failure reason"
  artifacts:
    - path: "DeckFlow.Studio/Pages/PullFromProd.razor"
      provides: "2-stage gated pull+reconcile page"
      min_lines: 120
      contains: "@page \"/pull-from-prod\""
    - path: "DeckFlow.Studio.Tests/TestDoubles/FakeSshArtifactDownloader.cs"
      provides: "In-memory downloader fake mirroring FakeSshArtifactUploader"
      contains: "ISshArtifactDownloader"
    - path: "DeckFlow.Studio.Tests/TestDoubles/FakeProdContentReader.cs"
      provides: "Distinct read-only prod reader fake (records read calls; no write API) (R2)"
      contains: "IProdContentReader"
    - path: "DeckFlow.Studio.Tests/PullFromProdPageTests.cs"
      provides: "bUnit gating + diff render + resolve + partial-pull-adopt + prod-write-guard + secret-leak tests"
      contains: "PullFromProd"
  key_links:
    - from: "DeckFlow.Studio/Pages/PullFromProd.razor"
      to: "ContentSyncDiffClassifier.Classify"
      via: "Stage 2 classifies prod vs local rows"
      pattern: "ContentSyncDiffClassifier"
    - from: "DeckFlow.Studio/Pages/PullFromProd.razor"
      to: "IProdContentReader.ReadAllAsync"
      via: "Stage 1 reads prod rows read-only (NEVER ContentSiteIndexStore / EnsureSchemaAsync)"
      pattern: "IProdContentReader"
    - from: "DeckFlow.Studio/Pages/PullFromProd.razor"
      to: "IndexStore.UpsertContentColumnsOnlyAsync"
      via: "adopt-prod local-only apply (NEVER prod reader, NEVER UpsertRowAsync)"
      pattern: "UpsertContentColumnsOnlyAsync"
    - from: "DeckFlow.Studio/Program.cs"
      to: "ISshArtifactDownloader"
      via: "AddSingleton<ISshArtifactDownloader, SftpArtifactDownloader>()"
      pattern: "ISshArtifactDownloader"
    - from: "DeckFlow.Studio/Program.cs"
      to: "IProdContentReader"
      via: "AddSingleton<IProdContentReader, ProdContentReader>()"
      pattern: "IProdContentReader"
---

<objective>
Build the operator-facing pull-and-reconcile page — the read mirror of DirectPush. Two gated
stages: Stage 1 reads prod rows via the dedicated read-only `IProdContentReader` (R1 — never the
DDL-running ContentSiteIndexStore) + downloads artifacts to an isolated `pull-staging/` dir and
classifies the diff; Stage 2 lets the operator pick adopt-prod / keep-local per entry, applied
LOCAL-only. Wire DI (downloader + prod reader) + nav, add the two test doubles + bUnit coverage,
and update the README.

Purpose: Delivers SYNC-01 (pull) and SYNC-03 (resolve) end-to-end through the UI, with the
absolute prod-read-only invariant enforced STRUCTURALLY (the prod reader has no write API) and
proven by tests.
Output: PullFromProd.razor, nav entry, DI registrations, FakeSshArtifactDownloader,
FakeProdContentReader, bUnit tests, README workflow section.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/60-pull-from-prod-reconcile/60-CONTEXT.md
@.planning/phases/60-pull-from-prod-reconcile/60-RESEARCH.md
@./CLAUDE.md

# The write-path page to mirror — copy its config gate, Task.Run/InvokeAsync pattern,
# sanitized catch blocks (D-07), Stage hard-guard, IDisposable CTS, and test-seam idiom.
# NOTE: DirectPush Stage 1 reads prod via ProdStoreFactory + EnsureSchemaAsync — DO NOT copy
# that read path here; use IProdContentReader.ReadAllAsync instead (R1).
@DeckFlow.Studio/Pages/DirectPush.razor

# Nav + DI to extend:
@DeckFlow.Studio/Shared/NavMenu.razor

# Test doubles to mirror (downloader fake) / reuse (index store):
@DeckFlow.Studio.Tests/TestDoubles/FakeSshArtifactUploader.cs
@DeckFlow.Studio.Tests/TestDoubles/FakeContentSiteIndexStore.cs

<interfaces>
<!-- Contracts produced by Wave 1 (Plans 01 + 02) — use directly, no exploration. -->

From DeckFlow.Core/Content (Plan 01):
```csharp
public enum SyncDiffKind { ProdNewer, MissingLocally, LocalOnly, Diverged }
public sealed record SyncDiffEntry {
    string NaturalKeyType; string NaturalKeyValue; SyncDiffKind Kind; string Title;
    ContentSiteIndexRow? ProdRow; ContentSiteIndexRow? LocalRow; string ArtifactPath;
    bool LocalIsNewer; bool ArtifactDownloaded;
}
public static class ContentSyncDiffClassifier {
    // Omits identical (in-sync) pairs (R3) — output contains only real diffs.
    static IReadOnlyList<SyncDiffEntry> Classify(IReadOnlyList<ContentSiteIndexRow> prodRows,
                                                 IReadOnlyList<ContentSiteIndexRow> localRows);
}
```

From DeckFlow.Studio.Services (Plan 02):
```csharp
public interface ISshArtifactDownloader {
    Task<IReadOnlyList<SshDownloadResult>> DownloadArtifactsAsync(
        IReadOnlyList<SshDownloadRequest> downloads, string localStagingRoot,
        IProgress<SshDownloadResult>? progress = null, CancellationToken ct = default);
}
public sealed record SshDownloadRequest(string RemoteRelativePath, string LocalRelativePath);
public sealed record SshDownloadResult(string RemoteRelativePath, string LocalPath, bool Success, string? FailureReason);

// R1 — read-only prod reader (NO write methods; plain SELECT; no EnsureSchemaAsync/DDL):
public interface IProdContentReader {
    Task<IReadOnlyList<ContentSiteIndexRow>> ReadAllAsync(string connectionString, CancellationToken ct = default);
}
```

Existing store + config (verified):
```csharp
// IContentSiteIndexStore (local, injected): GetAllRowsAsync, UpsertContentColumnsOnlyAsync(row),
//   SetApprovalStatusAsync(type,value,status) and SetApprovalStatusAsync(keys,status),
//   EnsureSchemaAsync. (UpsertRowAsync / UpsertRowPreservingVisibilityAsync exist but MUST NOT be called.)
// StudioConfig(bool IsProdConfigured, bool IsScpConfigured).
// ContentKbOrchestratorOptions.ArtifactRoot = {studioDataDir}/content-kb ; dataRoot = Path.GetDirectoryName(ArtifactRoot).
// ContentSourceType.Youtube / ContentSourceType.Podcast = natural-key type discriminators.
// Config["Studio:ProdConnectionString"] = ephemeral prod conn string (passed to ReadAllAsync; never materialized into DI state).
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: PullFromProd.razor (2-stage page) + nav + DI</name>
  <files>DeckFlow.Studio/Pages/PullFromProd.razor, DeckFlow.Studio/Shared/NavMenu.razor, DeckFlow.Studio/Program.cs</files>
  <action>
    Create `DeckFlow.Studio/Pages/PullFromProd.razor` (`@page "/pull-from-prod"`, `@implements IDisposable`), structurally mirroring DirectPush.razor but in the READ direction. Inject `IContentSiteIndexStore IndexStore` (LOCAL), `ISshArtifactDownloader SshDownloader`, `IProdContentReader ProdReader` (R1 — the read-only prod reader; do NOT inject IProdStoreFactory here), `StudioConfig Config`, `IConfiguration Configuration`, `ContentKbOrchestratorOptions Options`.

    Config gate (copy DirectPush): when `!Config.IsProdConfigured || !Config.IsScpConfigured`, show warning banner(s) and disable the Stage 1 button. Compute `_dataRoot = Path.GetDirectoryName(Options.ArtifactRoot)` and `_stagingRoot = Path.Combine(_dataRoot, "pull-staging")` and `_artifactRoot = Options.ArtifactRoot` in OnInitializedAsync (inside Task.Run, sanitized catch).

    STAGE 1 — "Pull & classify" (single combined action, per CONTEXT Q3 = 2 stages):
    - Hard-guard at top: `if (_operationInFlight || !Config.IsProdConfigured || !Config.IsScpConfigured) return;`
    - Inside `Task.Run`: wipe staging first — if `Directory.Exists(_stagingRoot)` delete recursively, then `Directory.CreateDirectory(_stagingRoot)` (staging isolates the pull; never touch `content-kb/` here).
    - Read prod rows via the READ-ONLY reader (R1): `var prodRows = await ProdReader.ReadAllAsync(Configuration["Studio:ProdConnectionString"] ?? "", ct);`. Do NOT use `IProdStoreFactory` / `ContentSiteIndexStore` / `EnsureSchemaAsync` here — that read path runs CREATE/ALTER DDL against prod (a prod write). The reader is structurally read-only (no write API). Do NOT capture the reader's results into anything a write path could reach.
    - `var requests = prodRows.Select(r => new SshDownloadRequest(r.ArtifactPath, r.ArtifactPath)).ToList();` then `var downloadResults = await SshDownloader.DownloadArtifactsAsync(requests, _stagingRoot, progress, ct);` (stream per-file results via Progress + InvokeAsync, like DirectPush). Build a set of successfully-downloaded relative paths.
    - `var localRows = await IndexStore.GetAllRowsAsync(ct);`
    - `var entries = ContentSyncDiffClassifier.Classify(prodRows, localRows);` then set `ArtifactDownloaded` on each entry by `with { ArtifactDownloaded = downloadedSet.Contains(entry.ArtifactPath) }` (record is immutable). Cache `_diffEntries`, set `_diffReady = true`.
    - Sanitized catch: prod-read failure -> "Could not read production — check the prod connection configuration and try again. Nothing was written." ; SSH failure -> "SSH download failed — check SCP configuration and Render SSH access." NEVER ex.Message (D-07). Honor OperationCanceledException.

    Diff table (rendered when `_diffReady`): columns Title | natural key (`type:value`) | Kind badge | artifact download status | Resolution control. For each entry hold a per-entry resolution selection in a `Dictionary<string,Resolution>` keyed by `type:value` (enum: None | AdoptProd | KeepLocal). Render the adopt-prod / keep-local radio ONLY for Kind != LocalOnly (Pitfall 8: LocalOnly is display-only — show an informational "local-only" badge, no radio). For Diverged entries surface `LocalIsNewer` (e.g. "local is newer" hint).
    R4 — partial pull: when an entry's `ArtifactDownloaded == false`, show an "artifact not downloaded" WARNING badge but KEEP the adopt-prod option SELECTABLE (adopt-prod still upserts the local row + mirrors approval_status; it just skips File.Move — Stage 2 handles the skip). Do NOT disable/forbid adopt-prod for a partial-pull entry.

    STAGE 2 — "Resolve" (apply button, enabled only when `_diffReady` and at least one resolution chosen):
    - Hard-guard at top (mirror DirectPush WriteRowsAsync guard / Pitfall 1): `if (!_diffReady || _operationInFlight) return;`
    - Inside Task.Run, iterate `_diffEntries`; for each with resolution == AdoptProd (skip LocalOnly entirely — it has no ProdRow): call `await IndexStore.UpsertContentColumnsOnlyAsync(entry.ProdRow!, ct);` then mirror prod approval_status: `await IndexStore.SetApprovalStatusAsync(entry.NaturalKeyType, entry.NaturalKeyValue, entry.ProdRow!.ApprovalStatus, ct);` (CONTEXT Q2: reflect prod's actual approval state, NOT a blind pending). Then promote the artifact ONLY if `entry.ArtifactDownloaded`: build `var stagedPath = Path.Combine(_stagingRoot, entry.ArtifactPath); var liveDest = Path.Combine(_dataRoot, entry.ArtifactPath); Directory.CreateDirectory(Path.GetDirectoryName(liveDest)!); File.Move(stagedPath, liveDest, overwrite: true);`. R4 — when `ArtifactDownloaded == false`, STILL do the upsert + approval mirror, SKIP only the File.Move, and record a per-entry "artifact not promoted (not downloaded)" note. For resolution == KeepLocal: no store call (optionally discard that entry's staged file). Track per-entry RowResult and stream into the UI. CRITICAL: every call in Stage 2 is on `IndexStore` (LOCAL) — there is NO prod reader/store reference in this method (the prod reader has no write API anyway, R1/R2). Use only `UpsertContentColumnsOnlyAsync`, never `UpsertRowAsync`/`UpsertRowPreservingVisibilityAsync` (Pitfall 3). Do NOT touch is_visible/is_hidden (CONTEXT: adopting never auto-publishes).
    - Add an `internal Task InvokePullApplyForTest()` seam calling the Stage 2 handler directly (mirror DirectPush `InvokeWriteRowsForTest`) so the hard-guard can be tested without a click.

    `IDisposable`: cancel + dispose the CTS on disposal (copy DirectPush).

    Nav: add a `PullFromProd` NavLink to `DeckFlow.Studio/Shared/NavMenu.razor` (href `pull-from-prod`, an `oi oi-data-transfer-download` icon, label "Pull from Prod") after the "Direct Push" item — touch only the added lines.

    DI: in `DeckFlow.Studio/Program.cs`, next to the existing `ISshArtifactUploader` registration (line ~75) add BOTH `builder.Services.AddSingleton<ISshArtifactDownloader, SftpArtifactDownloader>();` and `builder.Services.AddSingleton<IProdContentReader, ProdContentReader>();`. Add only those two lines.

    Conventions: Allman/4-space in @code, file-scoped where applicable, LF endings, changed-lines-only formatting in Program.cs and NavMenu.razor, preserve `{ get; init; }` on any new records. No new packages.
  </action>
  <verify>
    <automated>cd "$WORKTREE" && dotnet build DeckFlow.Studio/DeckFlow.Studio.csproj 2>&1 | grep -E "error|Build succeeded" | head -5 && grep -c "ProdReader\.\|ProdStoreFactory\|EnsureSchemaAsync" DeckFlow.Studio/Pages/PullFromProd.razor</automated>
  </verify>
  <done>Studio builds; page has 2 stages, gates when unconfigured, reads prod via IProdContentReader.ReadAllAsync (NOT ProdStoreFactory/ContentSiteIndexStore, NO EnsureSchemaAsync — the grep shows ProdReader. references only and zero ProdStoreFactory/EnsureSchemaAsync), classifies into the diff table, applies adopt-prod local-only (UpsertContentColumnsOnlyAsync + SetApprovalStatusAsync + File.Move on download success), partial-pull adopt still upserts + skips only File.Move (R4), no prod write path exists, never UpsertRowAsync, nav + both DI lines wired.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: FakeSshArtifactDownloader + FakeProdContentReader + bUnit tests + README</name>
  <files>DeckFlow.Studio.Tests/TestDoubles/FakeSshArtifactDownloader.cs, DeckFlow.Studio.Tests/TestDoubles/FakeProdContentReader.cs, DeckFlow.Studio.Tests/PullFromProdPageTests.cs, README.md</files>
  <behavior>
    - Page gating: prod unconfigured OR scp unconfigured -> warning banner shown and Pull button disabled.
    - Stage 1 wiring: clicking Pull invokes FakeProdContentReader.ReadAllAsync (read-only prod reader, distinct from the local store) AND FakeSshArtifactDownloader.DownloadArtifactsAsync with one request per prod row.
    - R2 structural write-free prod side: the prod reader fake is a DISTINCT instance from the local store fake, records its read calls, and exposes NO write API; the test asserts (a) the prod reader received reads only (it has no write method to call) and (b) all Stage 2 writes (UpsertContentColumnsOnlyAsync / SetApprovalStatusAsync) land on the LOCAL store fake.
    - Diff render: with a seeded prod-newer + missing-locally + local-only + diverged set, the table renders all four with correct kind badges; LocalOnly has NO adopt-prod radio.
    - adopt-prod apply (SYNC-03): selecting adopt-prod on a MissingLocally entry and clicking Resolve calls local IndexStore.UpsertContentColumnsOnlyAsync (recorded in UpsertMethodCalls) and SetApprovalStatusAsync with the PROD row's approval_status (not "pending").
    - keep-local apply: selecting keep-local results in NO UpsertContentColumnsOnlyAsync call for that entry.
    - prod-write guard: no test path ever causes FakeContentSiteIndexStore.UpsertRowAsync (which throws) to run; calling InvokePullApplyForTest() before _diffReady performs no upsert. (The prod reader cannot be written to at all — no write API — which is the R2 structural proof.)
    - R4 partial pull: an entry whose artifact failed to download (ArtifactDownloaded=false) keeps adopt-prod selectable; selecting adopt-prod STILL calls UpsertContentColumnsOnlyAsync + SetApprovalStatusAsync for it, the entry is flagged "artifact not promoted", and no File.Move-driven exception occurs (File.Move skipped).
    - secret leak (D-07): FakeSshArtifactDownloader configured to fail with a sentinel-bearing FailureReason, and FakeProdContentReader configured to throw a sentinel-bearing message, never surface the sentinel in rendered markup.
  </behavior>
  <action>
    FIRST create `DeckFlow.Studio.Tests/TestDoubles/FakeSshArtifactDownloader.cs` (internal sealed, `: ISshArtifactDownloader`), mirroring `FakeSshArtifactUploader`: record each `SshDownloadRequest` into a `DownloadedFiles` list, a `FilesToFail` HashSet keyed by RemoteRelativePath producing failed results, and an optional sentinel `FailureReason` for the secret-leak test. Reference the interface only — never SSH.NET (keeps the test project SSH-free).

    THEN create `DeckFlow.Studio.Tests/TestDoubles/FakeProdContentReader.cs` (internal sealed, `: IProdContentReader`) — R2's DISTINCT read-only prod fake, SEPARATE from the local store fake. It holds a seeded `IReadOnlyList<ContentSiteIndexRow>` to return from `ReadAllAsync`, increments a `ReadCallCount` (proves the page reads prod via THIS reader, not the store), and supports an optional sentinel-bearing exception (throw from ReadAllAsync) for the prod-read secret-leak case. It has NO write/upsert method — that absence IS the structural write-free guarantee (R1/R2); add an XML-doc comment saying so.

    THEN write `DeckFlow.Studio.Tests/PullFromProdPageTests.cs` (bUnit 2.7.2, mirror existing DirectPush page tests): register `StudioConfig`, `FakeContentSiteIndexStore` (local store — the ONLY write target), `FakeProdContentReader` (seeded prod store, read-only, distinct instance), `FakeSshArtifactDownloader`, an `IConfiguration` stub (providing `Studio:ProdConnectionString`), and `ContentKbOrchestratorOptions` in the bUnit Services; render the component and drive the stages. Cover every case in the behavior block. Reuse `FakeContentSiteIndexStore`'s existing guards: `UpsertRowAsync`/`UpsertRowPreservingVisibilityAsync` throw (proves full-upsert never happens), and use `UpsertMethodCalls`/`SingleApprovalCalls`/`BatchApprovalCalls` for assertions. Assert the prod reader (`FakeProdContentReader`) is a different object than the local store and received read-only calls while the local store received the Stage 2 writes (R2). Use `InvokePullApplyForTest()` for the hard-guard test. Run RED→GREEN.

    THEN update `README.md`: add a "Pull from Prod" entry to the Studio workflow documentation — describe the new read-only prod→local reconcile lane (read prod rows via a dedicated read-only reader, download artifacts to staging, per-entry diff classification omitting in-sync rows, adopt-prod/keep-local resolution applied locally; prod is never written — the reader has no write API). Touch only the relevant section; preserve existing line endings.
  </action>
  <verify>
    <automated>cd "$WORKTREE" && dotnet test DeckFlow.Studio.Tests/DeckFlow.Studio.Tests.csproj --filter "FullyQualifiedName~PullFromProd" 2>&1 | grep -E "Passed!|Failed!|error" | head -5</automated>
  </verify>
  <done>All PullFromProd bUnit tests pass; the prod side is a distinct read-only fake that receives only reads while Stage 2 writes hit the local store (R2); the partial-pull adopt test proves upsert+approval still run while File.Move is skipped (R4); the secret-leak tests prove no sentinel in markup; README documents the new lane.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator browser -> Studio (SignalR) | Local single-operator tool; no auth (matches DirectPush) |
| Studio -> prod Postgres | READ-ONLY: IProdContentReader SELECT only; no write API exists (R1/R2) |
| Studio -> Render /data (SFTP) | DOWNLOAD only into pull-staging/; never uploads, never writes remote |
| pull-staging/ -> content-kb/ | Staged artifacts promoted locally only on adopt-prod with a successful download |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-60-08 | Tampering | Stage 1 prod read (accidental prod write / DDL) | mitigate | Prod read goes through IProdContentReader (plain SELECT, no EnsureSchemaAsync/DDL, R1); the reader exposes no write API so the page cannot mutate prod even by mistake (R2); the build-time grep gate asserts no ProdStoreFactory/EnsureSchemaAsync in the page |
| T-60-09 | Tampering | adopt-prod local upsert | mitigate | ONLY UpsertContentColumnsOnlyAsync (preserves is_visible/is_hidden/is_evergreen); never UpsertRowAsync (Pitfall 3); approval_status mirrored from prod row, never blind-overwritten beyond that; FakeContentSiteIndexStore.UpsertRowAsync throws so any full-row upsert breaks a test |
| T-60-10 | Tampering (data integrity) | staged artifact promotion | mitigate | ArtifactDownloaded flag gates File.Move; partial-pull adopt skips ONLY the File.Move (still upserts, R4); staging wiped at start of each pull so no stale file is promoted (Pitfall 4) |
| T-60-11 | Information Disclosure | page catch blocks + banners | mitigate | All catches use sanitized literals only; never ex.Message (D-07); Npgsql/SSH exceptions can carry host/db/user/path — bUnit secret-leak tests assert no sentinel in markup |
| T-60-12 | Tampering / latent write | prod connection at DI startup | mitigate | Prod reader built with the conn string passed per-call (D-03), never a live conn-string singleton; Configuration injected, not materialized |
| T-60-SC | Tampering | NuGet installs | mitigate | NO new packages; csproj files unchanged (SSH.NET / bUnit / Npgsql / Dapper all already present) |
</threat_model>

<verification>
- `dotnet build DeckFlow.Studio/DeckFlow.Studio.csproj` clean; `dotnet build DeckFlow.Studio.Tests/DeckFlow.Studio.Tests.csproj` clean.
- `dotnet test DeckFlow.Studio.Tests --filter PullFromProd` green.
- `grep -c "UpsertRowAsync\|UpsertRowPreservingVisibilityAsync" DeckFlow.Studio/Pages/PullFromProd.razor` returns 0.
- `grep -c "ProdStoreFactory\|EnsureSchemaAsync" DeckFlow.Studio/Pages/PullFromProd.razor` returns 0 (R1: prod read is via IProdContentReader, no DDL path).
- `grep -c "ex.Message" DeckFlow.Studio/Pages/PullFromProd.razor` returns 0.
- `grep -c "ISshArtifactDownloader\|IProdContentReader" DeckFlow.Studio/Program.cs` >= 2 (both DI lines).
- `git diff --name-only` shows no change to any *.csproj (no package added).
- README contains a "Pull from Prod" section.
</verification>

<success_criteria>
- Operator can trigger a pull, see a per-entry 4-kind diff table (in-sync rows omitted), and resolve each entry (adopt-prod / keep-local) from Studio — no CLI, no manual DB edit (SYNC-01 + SYNC-03 via UI).
- Prod read goes through the dedicated read-only `IProdContentReader` (R1) — no EnsureSchemaAsync/DDL against prod.
- adopt-prod is local-only (UpsertContentColumnsOnlyAsync + mirrored approval_status + staged File.Move); LocalOnly offers no adopt; partial pull keeps adopt selectable, skipping only File.Move (R4).
- Prod is structurally never written (no write API on the reader) and proven by a distinct read-only prod fake in tests (R2); no secret/ex.Message leak.
- Nav + both DI registrations wired; README updated; zero new packages.
</success_criteria>

<output>
Create `.planning/phases/60-pull-from-prod-reconcile/60-03-SUMMARY.md` when done.
</output>
