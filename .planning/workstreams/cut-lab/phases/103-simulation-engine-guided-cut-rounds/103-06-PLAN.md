---
phase: 103-simulation-engine-guided-cut-rounds
plan: 06
type: execute
wave: 4
depends_on: ["103-02", "103-03", "103-04", "103-05"]
files_modified:
  - DeckFlow.Web/Services/CutLab/CutLabAnalysisContextBuilder.cs
  - DeckFlow.Web/Services/CutLab/CutLabPageService.cs
  - DeckFlow.Web/Extensions/CutLabServiceCollectionExtensions.cs
  - DeckFlow.Web.Tests/CutLabAnalysisContextBuilderTests.cs
  - DeckFlow.Web.Tests/CutLabPageServiceTests.cs
autonomous: true
requirements: [SIM-01, SIM-02]
must_haves:
  truths:
    - "HIGH-2: the resolve→classify→role-assign build (currently transient in CutLabPageService.BuildRoleAssignments + LoadClassificationInputsAsync) is EXTRACTED into a shared CutLabAnalysisContextBuilder that BOTH the page service (intake) and the decision endpoint (Plan 103-07) call — so the endpoint can rebuild analyzed cards / role counts / findings inputs without re-implementing the pipeline."
    - "HIGH-2: the builder's card-data source is the 103-03 resolved-card cache keyed by pool hash; on cache miss (restart/eviction) it re-resolves through the existing IScryfallCardResolver pipeline (slower, acceptable) and re-caches, fail-open per 102 semantics; classification (combo/category) uses the existing cache-backed spellbook/category services, degrading gracefully when unavailable."
    - "HIGH-4: at intake AND on every full-page render, the CURRENT proposal's deltas and the current working-list snapshot are computed server-side via CutLabSimulationService and exposed on CutLabProcessResult — so the first proposal renders with deltas (CUT-02) on the no-JS path, never a delta-less proposal."
    - "D-09 gap closed: at pool intake the resolved ScryfallCardData for the working pool is written to CutLabResolvedCardCache (via the builder) so no later decision re-fetches Scryfall."
    - "D-12/SIM-02: the original-pool baseline snapshot is computed once at intake and stored in CutLabState.BaselineSnapshot, surviving the serialized round-trip."
    - "CUT-01/HIGH-1: the initial round plan is computed at page render via CutLabCutRoundEngine over the DERIVED working list (CutLabWorkingList.Derive) so the first proposal + sticky-bar counts render on the no-JS full-page path too."
  artifacts:
    - path: "DeckFlow.Web/Services/CutLab/CutLabAnalysisContextBuilder.cs"
      provides: "shared resolve+classify+role-assign context builder (HIGH-2) used by page service + endpoint"
      contains: "CutLabAnalysisContext"
    - path: "DeckFlow.Web/Services/CutLab/CutLabPageService.cs"
      provides: "intake via shared builder + cache population + baseline + initial round plan + initial deltas/current snapshot"
      contains: "CutLabAnalysisContextBuilder"
  key_links:
    - from: "DeckFlow.Web/Services/CutLab/CutLabPageService.cs"
      to: "DeckFlow.Web/Services/CutLab/CutLabAnalysisContextBuilder.cs"
      via: "delegates resolve+classify+role-assign to the shared builder (replacing private BuildRoleAssignments transient)"
      pattern: "CutLabAnalysisContextBuilder"
    - from: "DeckFlow.Web/Services/CutLab/CutLabPageService.cs"
      to: "DeckFlow.Web/Services/CutLab/CutLabSimulationService.cs"
      via: "ComputeProposalDeltas + BuildSnapshot over the derived working list for HIGH-4 initial deltas/current snapshot"
      pattern: "ComputeProposalDeltas"
    - from: "DeckFlow.Web/Services/CutLab/CutLabPageService.cs"
      to: "DeckFlow.Web/Services/CutLab/CutLabBaselineSnapshot.cs"
      via: "compute baseline once at intake, store in CutLabState.BaselineSnapshot"
      pattern: "Baseline"
---

<objective>
Extract the resolve→classify→role-assign pipeline (currently a private transient inside `CutLabPageService`) into a shared `CutLabAnalysisContextBuilder` (HIGH-2) that both the page service and the Plan 103-07 decision endpoint call, source its card data from the 103-03 resolved-card cache (re-resolving fail-open on miss), and extend the page service so that at intake it (1) populates the resolved-card cache, (2) computes and stores the D-12 baseline snapshot, (3) computes the initial CUT-01 round plan over the DERIVED working list, and (4) computes the CURRENT proposal's deltas + current working-list snapshot server-side (HIGH-4) so the first proposal renders with deltas on the no-JS path.

Purpose: The endpoint cannot rebuild role assignments/findings without this shared builder (HIGH-2); the first proposal cannot render its deltas without server-side computation (HIGH-4); the fast loop cannot hit its budget without intake-time caching + baseline.
Output: `CutLabAnalysisContextBuilder` + DI, extended `CutLabPageService` + tests, and the CutLabProcessResult fields the view model needs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/cut-lab/phases/103-simulation-engine-guided-cut-rounds/103-PATTERNS.md
@.planning/workstreams/cut-lab/phases/103-simulation-engine-guided-cut-rounds/103-RESEARCH.md

<interfaces>
Existing (DeckFlow.Web/Services/CutLab/CutLabPageService.cs):
- interface ICutLabPageService { Task<CutLabProcessResult> ProcessAsync(CutLabRequest request, CancellationToken); }
- record CutLabProcessResult { CutLabState? State; string? SerializedStateJson; int CardCount; ...; CutLabStructuralFindingsResult Findings; IReadOnlyDictionary<string, IReadOnlyList<string>> RoleAssignmentsByCardName; IReadOnlyList<CutLabResolvedFloor> ResolvedFloors; ...; bool HasResult; }
- Private pipeline being extracted: LoadClassificationInputsAsync (uses _spellbook + _categoryKnowledge, both cache-backed) → BuildRoleAssignments(resolvedEntries, commanderNames, playExperience, classification, out analyzedCards, out commanderManaValue) using ScryfallCardFactMapper.ToCardFact + CutLabRoleAssigner.AssignRoles per entry. ResolvedCutLabEntry holds ScryfallCardData? Card. record CutLabAnalyzedCard(Name, ManaValue, IsLand, Roles, Categories){ int Quantity } lives in CutLabStructuralFindings.cs (public). CutLabStructuralFindings.Compute(analyzedCards, almostIncludedCombos, floorByRole, comboDataAvailable, categoryDataAvailable). CutLabState built via BuildState(...). Registered Scoped in Program.cs:181.

From prior plans:
- CutLabWorkingList.Derive(pool, decisions) (Plan 103-02) — the derived working list (HIGH-1).
- CutLabResolvedCardCache.Set/TryGet(poolKey, IReadOnlyList<ScryfallCardData>) (Plan 103-03).
- ICutLabSimulationService.BuildSnapshot / ComputeProposalDeltas (Plan 103-05); CutLabBaselineSnapshot.Build(...) (Plan 103-05).
- CutLabCutRoundEngine.BuildQueue(derivedWorkingList, findings, decisions, cardsToCutTarget) → CutLabRoundPlan (Plan 103-04).
- CutLabState.BaselineSnapshot / Decisions (Plan 103-02); CutLabMetricSnapshot / CutLabProposalDeltas (Plan 103-01).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extract CutLabAnalysisContextBuilder (shared resolve+classify+role-assign, HIGH-2)</name>
  <read_first>
    - DeckFlow.Web/Services/CutLab/CutLabPageService.cs (BuildRoleAssignments 452-512, LoadClassificationInputsAsync 514+, ResolveCardsAsync, ResolvedCutLabEntry, ClassificationInputs shape, the injected _cardResolver/_spellbook/_categoryKnowledge deps)
    - DeckFlow.Web/Services/CutLab/CutLabResolvedCardCache.cs (Set/TryGet + ComputePoolKey from Plan 103-03)
    - DeckFlow.Web/Services/CutLab/CutLabRoleAssigner.cs (AssignRoles, ResolveMode) + CutLabStructuralFindings.cs (CutLabAnalyzedCard public record)
    - DeckFlow.Web/Services/Scryfall/ScryfallCardResolver.cs (IScryfallCardResolver — the existing resolve pipeline)
    - DeckFlow.Web/Extensions/CutLabServiceCollectionExtensions.cs (extend to register the builder)
  </read_first>
  <behavior>
    - Test 1: BuildAsync over a small working list returns a context with one CutLabAnalyzedCard per card (name, mana value, roles, categories) and a RoleCounts dictionary summing per role.
    - Test 2 (cache hit): when CutLabResolvedCardCache already holds the pool's ScryfallCardData, BuildAsync uses it and does NOT call the resolver (call-counting fake).
    - Test 3 (cache miss, fail-open): with an empty cache, BuildAsync resolves via the IScryfallCardResolver fake, populates the cache, and still returns a context; when classification (spellbook/category) is unavailable, roles still assign from card facts and the context carries ComboDataAvailable/CategoryDataAvailable = false (no throw).
    - Test 4: commanderManaValue on the context equals the max mana value among commander cards.
  </behavior>
  <action>
    Create `DeckFlow.Web/Services/CutLab/CutLabAnalysisContextBuilder.cs` (namespace `DeckFlow.Web.Services.CutLab`, `public interface ICutLabAnalysisContextBuilder` + `public sealed class`, constructor-injected `IScryfallCardResolver`, `CutLabResolvedCardCache`, optional `ICommanderSpellbookService?`, optional `ICategoryKnowledgeStore?`, `ILogger<CutLabAnalysisContextBuilder>`). Move the resolve+classify+role-assign logic out of `CutLabPageService` into it. Expose `Task<CutLabAnalysisContext> BuildAsync(IReadOnlyList<CutLabPoolCard> workingList, string playExperience, IReadOnlyList<string> commanderNames, CancellationToken ct)` returning a `CutLabAnalysisContext` record: `IReadOnlyList<CutLabAnalyzedCard> AnalyzedCards`, `IReadOnlyDictionary<string,IReadOnlyList<string>> RolesByCardName`, `IReadOnlyDictionary<string,int> RoleCounts`, `double CommanderManaValue`, `ManabaseMode Mode`, and a `ClassificationContext` sub-record carrying `IReadOnlyList<SpellbookAlmostCombo> AlmostIncludedCombos`, `bool ComboDataAvailable`, `bool CategoryDataAvailable` (the inputs CutLabStructuralFindings.Compute needs). Card resolution: compute the pool key (Plan 103-03 helper), `CutLabResolvedCardCache.TryGet` → hit uses cached ScryfallCardData; miss resolves via `IScryfallCardResolver`, then `Set` (fail-open — log + continue if resolution partially fails). Classification uses the cache-backed spellbook/category services (fail-open, mirroring the existing LoadClassificationInputsAsync try/catch). Register `AddScoped<ICutLabAnalysisContextBuilder, CutLabAnalysisContextBuilder>()` in `AddDeckFlowCutLabServices`. Preserve LF; changed-lines-only in the extension.
  </action>
  <verify>
    <automated>"/mnt/c/Program Files/dotnet/dotnet.exe" test DeckFlow.Web.Tests --filter "FullyQualifiedName~CutLabAnalysisContextBuilder"</automated>
  </verify>
  <acceptance_criteria>
    - `CutLabAnalysisContextBuilder` returns analyzed cards + role counts + commander mana value + classification flags from a working list.
    - Cache-hit path skips the resolver; cache-miss path resolves + caches + is fail-open.
    - Registered in `AddDeckFlowCutLabServices` (grep `ICutLabAnalysisContextBuilder`).
    - Build clean, no new warnings.
  </acceptance_criteria>
  <done>The resolve+classify+role-assign pipeline is a shared service both intake and the endpoint can call.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Page service consumes the builder + computes/stores the D-12 baseline at intake</name>
  <read_first>
    - DeckFlow.Web/Services/CutLab/CutLabPageService.cs (ProcessAsync 141-294 — where BuildRoleAssignments/LoadClassificationInputsAsync are called, floor resolution, CutLabStructuralFindings.Compute, BuildState; constructor deps)
    - DeckFlow.Web/Services/CutLab/CutLabAnalysisContextBuilder.cs (Task 1)
    - DeckFlow.Web/Services/CutLab/CutLabBaselineSnapshot.cs + CutLabSimulationService.cs (Plan 103-05)
    - DeckFlow.Web/Models/CutLab/CutLabState.cs (BaselineSnapshot field) + CutLabWorkingList.cs (Derive)
    - DeckFlow.Web.Tests/CutLabPageServiceTests.cs (Fake wiring — add fakes for the builder + sim/baseline deps)
  </read_first>
  <behavior>
    - Test 1 (HIGH-2 delegation): ProcessAsync obtains analyzed cards + findings inputs via ICutLabAnalysisContextBuilder (the private BuildRoleAssignments transient is gone) and existing role-assignment/findings assertions still hold.
    - Test 2 (D-09 cache population): after ProcessAsync on a valid pool, CutLabResolvedCardCache.TryGet(poolKey) returns the resolved cards (populated by the builder).
    - Test 3 (D-12 baseline): the returned CutLabProcessResult.State.BaselineSnapshot is non-null with 7 families and the serialized state round-trips it under the cap.
    - Test 4 (fail-open): if baseline computation throws, ProcessAsync still returns a usable result (baseline null + warning), never a hard failure.
  </behavior>
  <action>
    Refactor `CutLabPageService.ProcessAsync` to call `ICutLabAnalysisContextBuilder.BuildAsync` (injected) in place of the private `BuildRoleAssignments` + `LoadClassificationInputsAsync` calls: derive the working list from the freshly-built state via `CutLabWorkingList.Derive(state.Pool, state.Decisions)` (at first intake this equals the full pool) and pass it to the builder; use `context.AnalyzedCards` + `context.Classification` for `CutLabStructuralFindings.Compute`, `context.CommanderManaValue` for floor defaults, and `context.RolesByCardName` for `RoleAssignmentsByCardName`. Remove/retire the now-duplicated private methods (or leave thin shims only if other call sites need them). After BuildState, compute the D-12 baseline via `CutLabBaselineSnapshot.Build(originalPool)` and set `state = state with { BaselineSnapshot = ... }`; fail open (log + warning + null baseline) if it throws, mirroring the existing classification fail-open. Inject the new deps through the constructor (DI binds them via the Plan 103-05/Task-1 registrations). Changed-lines-only; preserve LF; keep the banlist/commander-resolution/validation intake concerns intact.
  </action>
  <verify>
    <automated>"/mnt/c/Program Files/dotnet/dotnet.exe" test DeckFlow.Web.Tests --filter "FullyQualifiedName~CutLabPageService"</automated>
  </verify>
  <acceptance_criteria>
    - ProcessAsync delegates to `ICutLabAnalysisContextBuilder` (grep); the private BuildRoleAssignments transient no longer produces the analyzed cards inline.
    - Resolved-card cache populated once per intake; State.BaselineSnapshot non-null on success and round-trips under the cap.
    - Baseline failure is fail-open (result still HasResult); existing CutLabPageService tests stay green.
  </acceptance_criteria>
  <done>Intake runs through the shared builder, populates the cache, and stores the D-12 baseline (fail-open).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Expose initial round plan + initial proposal deltas + current snapshot (HIGH-4)</name>
  <read_first>
    - DeckFlow.Web/Services/CutLab/CutLabPageService.cs (CutLabProcessResult record; ProcessAsync tail where findings/state are set)
    - DeckFlow.Web/Services/CutLab/CutLabCutRoundEngine.cs (BuildQueue + CutLabRoundPlan from Plan 103-04)
    - DeckFlow.Web/Services/CutLab/CutLabSimulationService.cs (ComputeProposalDeltas + BuildSnapshot from Plan 103-05)
    - DeckFlow.Web/Services/CutLab/CutLabWorkingList.cs (Derive)
    - DeckFlow.Web/Models/CutLab/CutLabMetrics.cs (CutLabProposalDeltas, CutLabMetricSnapshot)
  </read_first>
  <behavior>
    - Test 1: for an oversized pool the result exposes a RoundPlan with a first proposal + cards-remaining count; for an ≤100 pool it yields an empty/nothing-to-cut plan.
    - Test 2 (HIGH-4): when a first proposal exists, CutLabProcessResult.InitialProposalDeltas is non-null (deltas for that proposal, computed server-side vs the derived working list) — the first proposal is never delta-less.
    - Test 3 (SIM-02 current snapshot): CutLabProcessResult.CurrentSnapshot is non-null (the derived working-list snapshot); at first intake it equals the baseline.
    - Test 4 (fail-open): if delta/snapshot computation throws, the result still renders (null deltas/snapshot + warning), never a hard failure.
  </behavior>
  <action>
    Add to `CutLabProcessResult`: `CutLabRoundPlan? RoundPlan { get; init; }`, `CutLabProposalDeltas? InitialProposalDeltas { get; init; }`, and `CutLabMetricSnapshot? CurrentSnapshot { get; init; }` (XML-doc each). In `ProcessAsync`, after findings + state are built: derive the working list (`CutLabWorkingList.Derive(state.Pool, state.Decisions)`), compute `RoundPlan = CutLabCutRoundEngine.BuildQueue(derivedWorkingListAnalyzed, Findings, state.Decisions, cardsToCutTarget)` (cardsToCutTarget = derived count − 100); if the round plan has a first proposal, compute `InitialProposalDeltas = _simulationService.ComputeProposalDeltas(derivedWorkingList, firstProposalCard)` (HIGH-4); compute `CurrentSnapshot = _simulationService.BuildSnapshot(derivedWorkingList, ...)`. Wrap the sim calls in the existing fail-open pattern (log + warning + null). Because CutLabController.Decide (Plan 103-07) re-renders through ProcessAsync, this single path serves both intake and post-decision full-page renders. Changed-lines-only; XML-doc new fields.
  </action>
  <verify>
    <automated>"/mnt/c/Program Files/dotnet/dotnet.exe" test DeckFlow.Web.Tests --filter "FullyQualifiedName~CutLabPageService"</automated>
  </verify>
  <acceptance_criteria>
    - `CutLabProcessResult` exposes `RoundPlan`, `InitialProposalDeltas`, and `CurrentSnapshot` (grep).
    - A first proposal always carries non-null InitialProposalDeltas (HIGH-4); an ≤100 pool yields a nothing-to-cut plan.
    - CurrentSnapshot present; equals baseline at first intake.
    - Sim failure is fail-open; build clean.
  </acceptance_criteria>
  <done>Every full-page render exposes the first proposal WITH its deltas + the current snapshot, so the no-JS path is never delta-less.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| intake request → cache/state | Client-supplied pool drives cache population + baseline + first-proposal sim; must not enable a raw fetch or oversized persisted state. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-103-06-01 | Denial of Service | Baseline + first-proposal sim at intake adding latency to the full-page POST | mitigate | Baseline + one proposal-delta run once per intake (not per action) and are fail-open; deltas cache-backed (D-10); pool bounded ≤150; existing 2MB request-size limit stands. |
| T-103-06-02 | Tampering | Oversized state after adding BaselineSnapshot | mitigate | Serializer cap (Plan 103-02) throws above 256KB; snapshot is compact numeric only. |
| T-103-06-03 | Spoofing | Builder resolution hitting an untrusted endpoint | mitigate | Cards resolved only through the existing `IScryfallCardResolver` pipeline; classification via cache-backed spellbook/category services; no new HTTP surface. |
</threat_model>

<verification>
- `"/mnt/c/Program Files/dotnet/dotnet.exe" test DeckFlow.Web.Tests --filter "FullyQualifiedName~CutLab"` green.
- Full-page intake still renders (build clean) with baseline + round plan + initial deltas + current snapshot populated; endpoint (Plan 103-07) reuses the shared builder.
</verification>

<success_criteria>
The resolve+classify+role-assign pipeline is a shared builder (HIGH-2) used by intake and the endpoint; intake populates the resolved-card cache + D-12 baseline; every full-page render exposes the first proposal with server-computed deltas + current snapshot (HIGH-4) — no delta-less proposal on the no-JS path.
</success_criteria>

<output>
Create `.planning/workstreams/cut-lab/phases/103-simulation-engine-guided-cut-rounds/103-06-SUMMARY.md` when done.
</output>
</content>
