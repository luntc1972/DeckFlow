---
phase: 18-admin-mobile-responsive-sweep
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - DeckFlow.Web/wwwroot/css/admin.css
  - DeckFlow.Web/wwwroot/css/admin-common.css
  - DeckFlow.Web/wwwroot/css/admin-mobile.css
  - DeckFlow.Web/wwwroot/css/site-common.css
  - DeckFlow.Web/Views/Shared/_AdminLayout.cshtml
autonomous: true
requirements: [AMOB-04, AMOB-03, AMOB-02, AMOB-01]
must_haves:
  truths:
    - "admin.css is a pure @import shim (no rules other than the two @import lines + header comment)"
    - "admin-common.css holds all admin layout primitives + components + admin-only global resets (no viewport @media)"
    - "admin-mobile.css holds all @media (max-width:768px) and the @media (min-width:769px) desktop-guard rules (no non-@media rules)"
    - "Dead admin-feedback-* / detail-* / type-badge rules AND the feedback-banner success rules are gone from site-common.css and live (token-corrected, .admin-shell-scoped) in admin-common.css"
    - "Every admin interactive-element in the EXHAUSTIVE Plan 01 inventory has a min-height:44px floor selector scoped under .admin-shell (sidebar links, range-selector links, feedback filter links, feedback pagination links, table action/View links, detail 'Back to list' link, summary, action-form buttons, plain detail/danger buttons, action-form inputs, the feedback type <select>, and textarea)"
    - "The CSS contract for the sidebar disclosure collapse exists (selectors .admin-sidebar__disclosure / .admin-sidebar__toggle defined) — the live collapse becomes TRUE in Plan 02 when the markup is added (AMOB-01)"
    - "Card-stack contract preserves screen-reader column-header context (thead is visually hidden via .sr-only, NOT display:none) so data-label is visual-only"
    - "Card-stack and overflow-x table CSS contracts exist for Plan 02 markup to consume"
    - "Long unbroken user data (emails, page URLs, user agents, hashes, flag keys) wraps inside card-stack td value content and .detail-grid dd/code/.detail-message via overflow-wrap:anywhere + min-width:0, so a 320px viewport has NO horizontal body overflow with realistic long data (cycle-3 NEW HIGH)"
    - "On desktop (>=769px) the no-JS nav pattern is COHERENT: the <summary> toggle is CLIPPED-but-PRESENT — its desktop rule sets an EXPLICIT non-none display (display:block) alongside the .sr-only clip, and there is NO base display:none anywhere for it to inherit, so it is NEVER display:none at any width (cycle-4 HIGH-A); it therefore stays in the a11y tree, keyboard-focusable, just visually de-emphasized, a real operable control if any AT treats the closed <details> as collapsed; the nav + brand are forced always-visible (D-DESKTOP-NAV — no self-contradiction)"
    - "EVERY admin component selector — including the existing blocks moved out of admin.css (.admin-sidebar*, .admin-table*, .admin-modal*, dialog.admin-modal::backdrop, .admin-banner*, .admin-action-form*, .admin-page-header*, .admin-analytics*, .admin-sparkline, .admin-empty, .maintenance-page*) — is rewritten scoped under .admin-shell per D-SCOPE; the only unscoped rules are the explicitly-enumerated /* admin-only global */ resets (AMOB-04 / Pitfall 10)"
    - "A deterministic selector-audit gate (inspecting comma-separated selector-prelude members before each { , including .feedback-banner) proves NO top-level (non-.admin-shell-prefixed, non-exempt) .admin-*, .detail-*, .type-badge, .feedback-banner, or dialog.admin-modal selector remains in admin-common.css or admin-mobile.css"
  artifacts:
    - path: "DeckFlow.Web/wwwroot/css/admin.css"
      provides: "Import shim entry point loaded by _AdminLayout.cshtml"
      contains: "@import url('admin-common.css')"
    - path: "DeckFlow.Web/wwwroot/css/admin-common.css"
      provides: "Admin layout primitives, tokens, components, .admin-shell-scoped touch targets"
      contains: "--danger"
      min_lines: 200
    - path: "DeckFlow.Web/wwwroot/css/admin-mobile.css"
      provides: "All admin viewport @media responsive rules"
      contains: "@media (max-width: 768px)"
      min_lines: 60
    - path: "DeckFlow.Web/wwwroot/css/site-common.css"
      provides: "Public CSS with dead admin rules removed"
    - path: "DeckFlow.Web/Views/Shared/_AdminLayout.cshtml"
      provides: "Two fingerprinted <link asp-append-version> tags (admin-common.css + admin-mobile.css)"
      contains: "admin-common.css"
  key_links:
    - from: "DeckFlow.Web/Views/Shared/_AdminLayout.cshtml"
      to: "admin-common.css + admin-mobile.css"
      via: "<link asp-append-version=true> (two tags, common first, mobile second)"
      pattern: "css/admin-(common|mobile)\\.css"
    - from: "admin.css"
      to: "admin-common.css + admin-mobile.css"
      via: "@import (retained fallback shim, not the load path)"
      pattern: "@import url\\('admin-(common|mobile)\\.css'\\)"
---

<objective>
Factor the monolithic `admin.css` into the canonical three-file split — `admin-common.css`
(layout primitives + components + admin-only globals), `admin-mobile.css` (all viewport
`@media` rules), and `admin.css` as a pure `@import` shim — mirroring the existing
`site-common.css` / `site-mobile.css` / `site.css` relationship. In the same pass, establish
every CSS contract the Phase 18 mobile sweep needs: ≥44px touch targets on ALL admin
interactive elements (full EXHAUSTIVE inventory below, every selector scoped under
`.admin-shell`), sidebar `<details>`/`<summary>` collapse below 768px, card-stack (with an
accessible visually-hidden header, NOT `thead { display:none }`) and `overflow-x` table
strategies, single-column forms, the `admin-harvest__panel` styling gap fix, migration of the
dead `admin-feedback-*` rules out of `site-common.css` into `admin-common.css` with admin-token
substitutions, AND migration of the admin feedback success banner (`feedback-banner
feedback-banner--success`, currently stranded in `site-common.css` which admin never loads) into
`admin-common.css` `.admin-shell`-scoped with admin tokens.

CRITICAL CHANGE THIS REVISION (resolves cycle-2 HIGH #2): the existing `admin.css` blocks are
NOT moved "verbatim" — their top-level `.admin-*` / `.maintenance-page*` selectors are CURRENTLY
UNSCOPED. Per D-SCOPE every one of them MUST be rewritten with a `.admin-shell ` prefix as it
moves into `admin-common.css`, EXCEPT the explicitly-enumerated admin-only global resets. A
deterministic selector-audit gate (Task 1 verify + acceptance) FAILS the task if any non-exempt
top-level `.admin-*`, `.detail-*`, `.type-badge`, `.feedback-banner`, or `dialog.admin-modal`
selector survives — and it inspects each comma-separated selector-prelude member before its `{`,
not just line-starts (resolves cycle-3 MEDIUM: grouped-list blind spot + omitted `.feedback-banner`).

CRITICAL CHANGE THIS REVISION (resolves cycle-3 NEW HIGH — long-token mobile overflow): card-stack
`.admin-shell .admin-table--card td` value content and `.admin-shell .detail-grid dd` / `code` /
`.detail-message` can hold long unbroken user strings (feedback messages, emails, flag keys, page
URLs, user agents, hashes). These selectors get `overflow-wrap: anywhere` + `min-width: 0` so a
320px viewport has NO horizontal body overflow with realistic long data (Task 2 mobile @media).

CRITICAL CHANGE THIS REVISION (resolves cycle-3 PARTIAL HIGH-3 — desktop <details> fallback
contradiction): the prior desktop guard hid the `<summary>` with `display:none`, which is
self-contradictory with the "keep summary operable" fallback (a hidden summary can't be operated).
The fix (D-DESKTOP-NAV, below) is a SINGLE coherent no-JS pattern: at ≥769px the nav + brand are
forced always-visible AND the `<summary>` is NOT `display:none` — it is visually de-emphasized but
stays in the a11y tree and focusable, so if any AT treats the closed `<details>` as collapsed, the
`<summary>` remains a real operable control. CSS, acceptance criteria, and the 769px checkpoint all
agree on this one behavior.

Purpose: Establishes the CSS foundation (AMOB-04) and all selector contracts so Plan 02 can
wire Razor markup to them without re-deciding architecture. This is the interface-first plan —
it defines every class hook (`admin-table--card`, `admin-table-scroll`,
`admin-sidebar__disclosure`, `admin-sidebar__toggle`) that Plan 02's markup activates. It also
switches `_AdminLayout.cshtml` to two `<link asp-append-version>` tags so BOTH new CSS files get
ASP.NET cache-busting fingerprints (the `@import` shim is retained inside `admin.css` as a
fallback but is no longer the load path — see Decision D-CACHE below).

Output: 3 admin CSS files (1 modified to shim, 2 new) + `site-common.css` with dead rules
removed + `_AdminLayout.cshtml` switched to two fingerprinted `<link>` tags.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-admin-mobile-responsive-sweep/18-RESEARCH.md
@.planning/phases/18-admin-mobile-responsive-sweep/18-PATTERNS.md
@./CLAUDE.md

<constraints>
- Codex is the executor. CLAUDE.md is binding: NO Format Document / Code Cleanup. Touch only
  the lines that need touching. Preserve LF line endings. Plain default-author commit, NO
  Co-Authored-By trailer. Commit per logical change.
- Layout CSS goes in `*-common.css`, NEVER in `site.css`/`admin.css`. Per the AMOB-04 spec,
  `admin.css` becomes the import shim only.
- NO new packages. NO Bootstrap/Tailwind/Fluent. Plain CSS + `<details>`/`<summary>` only.
- Build verification is `dotnet build -c Release` (CSS is not compiled, but the build copies
  wwwroot content and must stay 0/0). VSTest is unreliable in WSL — do NOT rely on test run;
  CSS has no xUnit coverage.
- Token discipline (RESEARCH Pitfall 2): admin `:root` defines ONLY `--bg --panel --text
  --muted --accent --border --focus` plus the two NEW tokens `--danger --on-accent`. NEVER use
  public-only tokens (`--line --link --accent-strong --fs-sm --ink`) in admin CSS — substitute
  per the migration map below.

- DECISION D-SCOPE (resolves cycle-1 + cycle-2 HIGH #2 / AMOB-04 / Pitfall 10): scoping approach
  is ".admin-shell descendant scoping" (NOT @layer). Every admin COMPONENT selector — both the
  ones migrated from site-common.css AND the ones moved out of the existing admin.css body — MUST
  be written as `.admin-shell <selector>`. This is the cycle-2 fix: the existing admin.css blocks
  (`.admin-sidebar`, `.admin-sidebar__brand`, `.admin-sidebar__nav`, `.admin-sidebar__link`,
  `.admin-sidebar__link--active`, `.admin-topbar`, `.admin-topbar__title`,
  `.admin-topbar__version`, `.admin-content`, `.admin-banner`, `.admin-banner--success`,
  `.admin-table`, `.admin-table th/td`, `.admin-table td:not(.route)`, `.admin-action-form`,
  `.admin-action-form button` + `:hover`, `.maintenance-page` + `h1`/`p`, `.admin-page-header` +
  `h1`, `.admin-analytics .admin-range-selector` + descendants, `.admin-analytics-table` +
  th/td/.num/.route code/.spark, `.admin-sparkline`, `.admin-empty`, `.admin-modal`,
  `.admin-modal__*`, and `dialog.admin-modal::backdrop`) were authored UNSCOPED (top-level) and
  MUST be REWRITTEN with a `.admin-shell ` prefix as they move (NOT copied verbatim). The Phase 16
  danger rules (`.admin-shell .admin-action-form button.danger`, `.admin-shell
  .admin-modal__button--danger`) are ALREADY scoped — keep them as-is. The `dialog.admin-modal`
  pseudo-element gets rewritten as `.admin-shell dialog.admin-modal::backdrop` (the `dialog`
  element type stays; the `.admin-shell` ancestor is prepended). The ONLY unscoped rules
  permitted (the EXEMPT LIST) are the admin-only global resets, each carrying a
  `/* admin-only global */` comment:
    1. `* { box-sizing: border-box; }`
    2. `html, body { ... }`
    3. bare `a { ... }` and `a:hover { ... }`
    4. the `:focus-visible` group (`a:focus-visible, button:focus-visible, ...`)
    5. `.skip-link` and `.skip-link:focus`
    6. `.sr-only`
    7. the `:root { ... }` token block (a declaration block on the root, not a component)
    8. the `@media (prefers-reduced-motion: reduce)` query (a11y preference, not a component)
  No other top-level selector may remain. Acceptance includes a deterministic selector-audit
  grep/awk gate (see Task 1 verify) that FAILS if a non-exempt top-level `.admin-*`, `.detail-*`,
  `.type-badge`, `.feedback-banner`, or `dialog.admin-modal` selector is found — and it inspects
  EACH comma-separated member of every selector prelude before its `{`, not just line-starts
  (resolves cycle-3 MEDIUM: an unscoped selector hiding later in a grouped, comma-separated list
  must still be caught). The gate is bash/grep/awk only (no CSS parser dependency) and runs in
  the Codex executor.

- DECISION D-CACHE (resolves Codex MEDIUM @import cache-busting): switch `_AdminLayout.cshtml`
  from one `<link href="~/css/admin.css">` to TWO tags —
  `<link rel="stylesheet" href="~/css/admin-common.css" asp-append-version="true" />` then
  `<link rel="stylesheet" href="~/css/admin-mobile.css" asp-append-version="true" />` (common
  first so the mobile @media overrides win the cascade). This gives BOTH files independent
  ASP.NET fingerprint query strings, eliminating the stale-import cache risk. The two-`<link>`
  pattern mirrors `_Layout.cshtml` lines 44-47 (site-common.css + site-mobile.css each
  fingerprinted). `admin.css` is STILL reduced to the `@import` shim (Task 3) and retained as a
  documented fallback entry point, but it is no longer referenced by the layout.

- DECISION D-A11Y-HEADER (resolves Codex HIGH #4 card-stack SR behavior): the card-stack
  contract MUST NOT use `thead { display: none; }` because that strips column-header semantics
  from the accessibility tree. Instead, visually hide the header row with the established
  `.sr-only` clip pattern (`position:absolute; width:1px; height:1px; overflow:hidden;
  clip:rect(0 0 0 0); white-space:nowrap;`) applied to `.admin-shell .admin-table--card thead`
  so the `<th scope="col">` cells remain in the a11y tree for screen-reader users. The CSS
  `content: attr(data-label)` per-cell label stays as a VISUAL-ONLY affordance for sighted
  users (it is decorative; SR users get the real `<th>` semantics). Document this in the
  card-stack block comment.

- DECISION D-DESKTOP-NAV (resolves cycle-3 PARTIAL HIGH-3 — desktop <details> fallback
  contradiction): the prior revision's desktop guard hid the `<summary>` with `display:none`
  while ALSO claiming the `<summary>` is the "operable fallback" if AT treats the closed
  `<details>` as collapsed. That is self-contradictory: a `display:none` summary cannot be
  operated and is removed from the a11y tree. RESOLUTION — ONE coherent no-JS desktop pattern at
  `@media (min-width: 769px)` (kept no-JS):
    (a) Force the nav + brand always-visible inside the `<details>` regardless of its open state:
        `.admin-shell .admin-sidebar__disclosure .admin-sidebar__nav,
         .admin-shell .admin-sidebar__disclosure .admin-sidebar__brand { display: block !important; }`.
    (b) DO NOT `display: none` the `<summary>`. Instead keep it in the a11y tree and focusable but
        visually de-emphasized so it does not duplicate the always-visible nav chrome on desktop:
        `.admin-shell .admin-sidebar__toggle { position: absolute; width: 1px; height: 1px;
         overflow: hidden; clip: rect(0 0 0 0); white-space: nowrap; }` — the SAME `.sr-only` clip
        pattern used for the card-stack header. A clipped element (unlike `display:none`) STAYS in
        the accessibility tree and remains keyboard-focusable, so if any AT treats the closed
        `<details>` as collapsed and skips the forced-visible nav, the `<summary>` is still a real,
        operable disclosure control reachable by Tab and announced by the screen reader. (The
        min-height:44px floor on `.admin-shell summary` from Task 1 step (g) is overridden here by
        the 1px clip on desktop; that is intentional — the floor matters on mobile where the
        summary is the visible tap target, not on desktop where it is the clipped fallback.)
  Net coherent behavior: on desktop the nav is always visually present AND keyboard/SR reachable,
  and the `<summary>` is a clipped-but-operable fallback (NOT display:none) — no contradiction.
  Plan 02's 769px checkpoint asserts exactly this single behavior (nav keyboard-reachable + SR-
  operable, summary state consistent with this decision: clipped, focusable, operable as fallback).

- DECISION D-BANNER (resolves cycle-2 MEDIUM — admin feedback success banner unstyled): the
  admin Feedback list (`AdminFeedback/Index.cshtml` line 13) renders
  `<div class="feedback-banner feedback-banner--success">`, whose CSS lives ONLY in public
  `site-common.css` (lines 772-773) which admin pages never load — so the banner is currently
  unstyled on admin. Resolve by MIGRATING those two rules (`.feedback-banner` and
  `.feedback-banner--success`) into `admin-common.css`, `.admin-shell`-scoped and token-corrected
  (the source uses no public-only token in `.feedback-banner` / `--success`, but scope both under
  `.admin-shell`). This avoids editing the Razor markup (the markup option `admin-banner
  admin-banner--success` is REJECTED to keep the view byte-stable and avoid touching more files).
  After migration, the success-banner rule lives in admin-common.css as
  `.admin-shell .feedback-banner { padding: 0.75rem 1rem; border-radius: 4px; margin-bottom:
  1rem; }` and `.admin-shell .feedback-banner--success { background: rgba(76,175,80,0.15);
  border: 1px solid rgba(76,175,80,0.4); }`. The public `.feedback-banner` rule STAYS in
  site-common.css (it is still used by the PUBLIC feedback form) — only ADD the scoped copies to
  admin-common.css; do NOT delete the public banner rules in this plan (they remain referenced by
  the public feedback page). The dead `admin-feedback-*`/`detail-*`/`type-badge` rules ARE deleted
  from site-common.css (Task 3) because those are admin-only and unreferenced publicly; the
  banner is the EXCEPTION because it is shared with the public form.

- WCAG WORDING (resolves Codex LOW): the 44px floor is DeckFlow's stricter PRODUCT floor
  aligned with WCAG 2.5.5 Target Size (Enhanced) = Level AAA (44×44 CSS px). It is NOT the AA
  bar — WCAG 2.5.8 Target Size (Minimum) = Level AA is only 24×24 px. State "44px product floor
  (WCAG 2.5.5 AAA)" in any comment referencing the target-size standard; do NOT label 44px as
  the AA minimum.
</constraints>

<interfaces>
<!-- The current admin.css :root block (admin.css lines 4-17) is the source of truth for tokens.
     Admin-defined (safe): --bg --panel --text --muted --accent --border --focus
     NEW this plan: --danger: #dc2626; --on-accent: #fff;
     The Phase 16 modal block (admin.css lines 195-307) already hardcodes #dc2626 at lines
     204+209 — the new --danger token replaces those literals so both stay consistent. -->

<!-- Token substitution map when migrating site-common.css lines 776-810 into admin-common.css: -->
<!--   --line          -> --border                                   -->
<!--   --link          -> --accent                                   -->
<!--   --accent-strong -> --accent  (was `var(--accent-strong, inherit)`) -->
<!--   --fs-sm         -> 0.85em                                     -->
<!--   --on-accent     -> --on-accent  (now defined this plan)        -->
<!--   --danger        -> --danger     (now defined this plan)        -->
<!--   --panel         -> --panel      (already in admin :root)        -->

<!-- Touch-target template (from Phase 16 admin-modal__button, admin.css lines 266-278): -->
<!--   min-height: 44px; min-width: 44px; display: inline-flex; align-items: center; -->

<!-- ADMIN INTERACTIVE-ELEMENT INVENTORY — EXHAUSTIVE (authoritative for AMOB-03 "all admin
     interactive elements"). Sourced from a grep sweep of ALL admin views
     (_AdminLayout, AdminFeedback/Index, AdminFeedback/Detail, AdminFlags/Index,
     AdminHarvest/Index, AdminAnalytics/Index). Each row MUST get a .admin-shell-scoped ≥44px
     floor selector in admin-common.css Task 1 step (g). Cycle-2 ADDED rows 5 (Back to list) and
     11 (type <select>) plus row 9 textarea split out.

     | #  | Element                                  | View(s)                        | Selector to add                                                       |
     |----|------------------------------------------|--------------------------------|------------------------------------------------------------------------|
     | 1  | Sidebar nav links                        | _AdminLayout                   | .admin-shell .admin-sidebar__link                                      |
     | 2  | Analytics range-selector links           | AdminAnalytics:14-20           | .admin-shell .admin-range-selector a                                   |
     | 3  | Feedback filter links                    | AdminFeedback/Index:25         | .admin-shell .admin-feedback-filter                                    |
     | 4  | Feedback pagination Prev/Next links      | AdminFeedback/Index:89,94      | .admin-shell .admin-feedback-pagination a                              |
     | 5  | Table action "View" link + detail "Back  | AdminFeedback Index:72 +       | .admin-shell .admin-table a, .admin-shell .admin-feedback-table a,      |
     |    |   to list" link                          |   Detail:7                     |   .admin-shell .admin-feedback-detail a                                |
     | 6  | <summary> disclosure toggle              | _AdminLayout (Plan 02 markup)  | .admin-shell summary                                                    |
     | 7  | Action-form submit buttons               | Flags:38, Harvest:43/50/77/94/100, Feedback Index:77 + Detail:29/34 | .admin-shell .admin-action-form button         |
     | 8  | Plain detail/danger buttons              | Feedback/Detail:42 (button.danger inside action-form) | .admin-shell .detail-actions button, .admin-shell button.danger |
     | 9  | Action-form text inputs                  | Harvest:76 (url), hidden inputs ignored | .admin-shell .admin-action-form input[type="url"], .admin-shell .admin-action-form input:not([type="hidden"]) |
     | 10 | Action-form textarea (future-proof)      | none today; contract for KB    | .admin-shell .admin-action-form textarea                               |
     | 11 | Feedback type <select> (+ harvest selects) | AdminFeedback/Index:33, Harvest:36,86 | .admin-shell .admin-feedback-type select, .admin-shell .admin-action-form select |
     The Phase 16 .admin-modal__button already has min-height:44px (moved + .admin-shell-scoped in
     step (c)/(d)), so it is COVERED and does not need a new floor.
     NOTE on the "Back to list" link (row 5): this is fixed by a CSS selector targeting the
     EXISTING markup (.admin-shell .admin-feedback-detail a) — NO Detail.cshtml markup change is
     required, so Detail.cshtml is NOT added to 18-02 files_modified. The fix stays CSS-only here.
     NOTE on the type <select> (row 11): the existing markup
     `<form class="admin-feedback-type"><select ...>` is targeted by
     `.admin-shell .admin-feedback-type select` — CSS-only, no markup change. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create admin-common.css with .admin-shell-scoped migrated rules, tokens, exhaustive touch targets, accessible card-stack header, banner migration, and comma-aware selector-audit gate</name>
  <files>DeckFlow.Web/wwwroot/css/admin-common.css</files>
  <read_first>
    - DeckFlow.Web/wwwroot/css/admin.css (ENTIRE current file, lines 1-307 — the source of every block being moved: :root tokens, focus-visible block, global resets, .skip-link/.sr-only, .admin-shell grid, all .admin-sidebar*/.admin-topbar*/.admin-content/.admin-banner/.admin-table/.admin-action-form/.maintenance-page/.admin-page-header/.admin-analytics*/.admin-sparkline/.admin-empty, AND the Phase 16 modal block lines 195-307). NOTE every top-level .admin-* / .maintenance-page* selector here is UNSCOPED and MUST be rewritten with a .admin-shell prefix per D-SCOPE — do NOT copy them verbatim.
    - DeckFlow.Web/wwwroot/css/site-common.css lines 772-810 (the feedback-banner success rules at 772-773 to MIGRATE per D-BANNER, AND the dead admin-feedback-*/detail-*/type-badge rules at 776-810 to migrate — all token-corrected AND .admin-shell-scoped)
    - DeckFlow.Web/wwwroot/css/site-common.css lines 1-12 (header-comment style to mirror) and the .sr-only definition there (clip pattern to reuse for the card-stack thead AND the desktop summary clip)
    - .planning/phases/18-admin-mobile-responsive-sweep/18-PATTERNS.md (admin-common.css section, lines 27-253 — exact rule inventory and token map)
  </read_first>
  <action>
    Create `admin-common.css` containing ALL non-`@media` admin rules. Per D-SCOPE, EVERY admin
    component selector below MUST carry a `.admin-shell ` prefix — including the blocks that
    currently live UNSCOPED in admin.css; rewrite them as you move them, do NOT copy verbatim.
    (a) Move the `:root` block from `admin.css` lines 4-17 VERBATIM (preserve the WDG comment
    lines 11-14, tag it `/* admin-only global */`), then add two tokens inside that `:root`:
    `--danger: #dc2626;` and `--on-accent: #fff;`. (b) Move the focus-visible block, the
    `* { box-sizing }`/`html,body`/`a`/`a:hover` global resets, and `.skip-link`/`.sr-only`
    verbatim (admin.css lines 19-68), each prefixed with a `/* admin-only global */` comment
    marking them intentional unscoped resets (per D-SCOPE exempt list these are the ONLY unscoped
    selectors permitted); add `.admin-shell .admin-table-scroll:focus-visible` (this one IS
    .admin-shell-scoped because it is a component, not a global reset) with the same
    `outline: 2px solid var(--focus); outline-offset: 2px;` so keyboard users get a visible ring
    when they tab into a scroll region. (c) Move the shell-layout block but REWRITE each selector
    `.admin-shell`-scoped: `.admin-shell` grid itself stays as `.admin-shell { ... }`; then
    `.admin-shell .admin-sidebar`, `.admin-shell .admin-sidebar__brand`,
    `.admin-shell .admin-sidebar__nav`, `.admin-shell .admin-sidebar__link` (+ `:hover`,
    `--active`), `.admin-shell .admin-topbar` (+ `__title`, `__version`),
    `.admin-shell .admin-content`, `.admin-shell .admin-banner` (+ `--success`),
    `.admin-shell .admin-table` (+ ` th`, ` td`, ` td:not(.route)`),
    `.admin-shell .admin-action-form` (+ ` button`, ` button:hover`),
    `.admin-shell .maintenance-page` (+ ` h1`, ` p`), `.admin-shell .admin-page-header` (+ ` h1`),
    `.admin-shell .admin-analytics .admin-range-selector` (+ ` a`, ` a:hover`, ` a.is-active`),
    `.admin-shell .admin-analytics-table` (+ ` th`, ` td`, ` .num`, ` .route code`, ` .spark`),
    `.admin-shell .admin-sparkline`, `.admin-shell .admin-empty` (admin.css lines 71-193) — keep
    every declaration identical, ONLY prepend `.admin-shell `. (d) Move the Phase 16 modal block
    (admin.css lines 195-307) and REWRITE the currently-unscoped selectors `.admin-shell`-scoped:
    `.admin-shell .admin-modal`, `.admin-shell .admin-modal__panel`, `.admin-shell
    .admin-modal__title`, `.admin-shell .admin-modal__message`, `.admin-shell .admin-modal__actions`,
    `.admin-shell .admin-modal__button` (+ `--cancel`, `--cancel:hover`, `--confirm`,
    `--confirm:not(--danger):hover`), and the backdrop as
    `.admin-shell dialog.admin-modal::backdrop`. The danger rules at admin.css lines 202-208 are
    ALREADY `.admin-shell`-scoped — keep them. The `@media (prefers-reduced-motion: reduce)` block
    (lines 300-305) is EXEMPT (tag `/* admin-only global */`) and is the ONLY permitted `@media`
    in this file; but rewrite its inner selectors `.admin-shell .admin-modal` and
    `.admin-shell dialog.admin-modal::backdrop`. Replace the two hardcoded `#dc2626` literals
    (admin.css lines 204, 209) with `var(--danger)`. (e) Migrate the dead rules from
    site-common.css lines 776-810 (`.admin-feedback`, `.admin-feedback-filters`,
    `.admin-feedback-filter`, `.admin-feedback-filter.active`, `.admin-feedback-table` + th/td,
    `.admin-feedback-pagination`, `.admin-feedback-empty`, `.admin-feedback-detail`, `.type-badge`,
    `.detail-grid`, `.detail-grid dt`, `.detail-message`, `.detail-actions`,
    `.detail-actions button.danger`) applying the token substitution map (`--line`->`--border`,
    `--link`->`--accent`, `var(--accent-strong, inherit)`->`var(--accent)`, `--fs-sm`->`0.85em`)
    AND prefixing EACH migrated component selector with `.admin-shell ` per D-SCOPE (e.g.
    `.admin-shell .admin-feedback`, `.admin-shell .admin-feedback-filter`,
    `.admin-shell .admin-feedback-filter.active`, `.admin-shell .type-badge`,
    `.admin-shell .detail-grid`, `.admin-shell .detail-grid dt`, `.admin-shell .detail-message`,
    `.admin-shell .detail-actions`, `.admin-shell .detail-actions button.danger`); do NOT migrate
    the duplicate `.admin-action-form { display: inline; }` from site-common.css:801 — that rule
    already exists from admin.css:136 (moved+scoped in step (c)) and must not be duplicated. (e2)
    BANNER MIGRATION (D-BANNER): add `.admin-shell .feedback-banner { padding: 0.75rem 1rem;
    border-radius: 4px; margin-bottom: 1rem; }` and `.admin-shell .feedback-banner--success {
    background: rgba(76, 175, 80, 0.15); border: 1px solid rgba(76, 175, 80, 0.4); }` (sourced
    from site-common.css:772-773; no public-only token present, just scope under .admin-shell) so
    the admin Feedback success banner is styled on admin pages. (f) Add the `admin-harvest__panel`
    styling gap fix scoped under `.admin-shell`: `.admin-shell .admin-harvest__panel` with
    `background: var(--panel); border: 1px solid var(--border); border-radius: 4px; padding:
    16px 20px; margin-bottom: 16px;` and `.admin-shell .admin-harvest__panel h2 { margin: 0 0 12px;
    font-size: 15px; font-weight: 600; }`. (g) Add the touch-target floor selectors for EVERY row
    in the EXHAUSTIVE Admin Interactive-Element Inventory above (a comment above the group should
    read `/* 44px product floor (WCAG 2.5.5 AAA) — all admin interactive elements */`):
    `.admin-shell .admin-sidebar__link`, `.admin-shell .admin-range-selector a`,
    `.admin-shell .admin-feedback-filter`, `.admin-shell .admin-feedback-pagination a`,
    `.admin-shell .admin-feedback-table a`, `.admin-shell .admin-table a`,
    `.admin-shell .admin-feedback-detail a`, `.admin-shell summary`,
    `.admin-shell .admin-action-form button`, `.admin-shell .detail-actions button`, and
    `.admin-shell button.danger` each get `min-height: 44px; display: inline-flex;
    align-items: center;` (links and buttons also need `justify-content: center` where centering
    matters per PATTERNS). The FORM CONTROLS — `.admin-shell .admin-action-form input:not([type="hidden"])`,
    `.admin-shell .admin-action-form select`, `.admin-shell .admin-feedback-type select`,
    `.admin-shell .admin-action-form textarea` — get `min-height: 44px; box-sizing: border-box;`
    only (NOT inline-flex; stretching text fields to inline flex breaks them; the type <select> in
    AdminFeedback/Index:33 and the harvest selects in AdminHarvest:36/86 are covered by these
    rules). You MAY consolidate identical declarations into grouped, comma-separated selector lists
    to keep the file readable; the verify gate (below) proves COVERAGE per inventory selector (it
    does not count literal `min-height` lines) so grouping is fully allowed. NOTE on the
    `.admin-shell summary` floor: this is the MOBILE tap-target floor; the Task 2 desktop guard
    (D-DESKTOP-NAV) clips the summary at ≥769px, intentionally overriding this floor on desktop —
    that is expected, not a conflict. (h) Add the scroll-wrapper base
    `.admin-shell .admin-table-scroll { overflow-x: auto; -webkit-overflow-scrolling: touch; }`.
    CRITICAL (D-DESKTOP-NAV — resolves cycle-4 HIGH-A): do NOT add any base
    `.admin-shell .admin-sidebar__toggle { display: none; }` rule in admin-common.css. The `<summary>`
    toggle is the MOBILE disclosure control (a visible tap target on mobile — `display: flex` is set in
    the Task 2 `@media (max-width:768px)` block) and is CLIPPED-but-present on desktop (the Task 2
    `@media (min-width:769px)` block sets a NON-NONE display ALONGSIDE the clip). It must NEVER be
    `display: none` at ANY width — a `display:none` summary is removed from the accessibility tree and
    cannot be the operable no-JS fallback (this was the cycle-4 HIGH-A bug: the prior base
    `display:none` was never reset in the ≥769px clip rule, so the summary inherited `display:none` and
    left the a11y tree on desktop). There is therefore NO base `display:none` on
    `.admin-sidebar__toggle` here; its display is governed entirely by the two Task 2 @media blocks
    (flex on mobile, a non-none display + clip on desktop).
    (i) The sparkline column floor: in the moved `.admin-shell .admin-analytics-table .spark` rule
    (step (c)), augment to `width: 130px; min-width: 80px;` so the column does not collapse on
    narrow viewports. (j) Add the ACCESSIBLE card-stack header contract base rule (D-A11Y-HEADER):
    a comment block stating "Card-stack visually hides the header row with the .sr-only clip
    pattern (NOT thead{display:none}) so <th scope=col> stays in the a11y tree; data-label is
    visual-only."; NOTE the actual `thead`-hiding rule lives in the `@media (max-width:768px)`
    block in admin-mobile.css (Task 2) — here in admin-common.css only document the contract in the
    comment.
    Open the file with a header comment mirroring site-common.css style: state that this file holds
    admin layout primitives + components, is loaded via two fingerprinted <link> tags from
    _AdminLayout.cshtml (admin-common first), contains NO viewport @media rules, and that every
    component selector is .admin-shell-scoped per D-SCOPE with the exempt-list resets the only
    exceptions. Use LF line endings, 2-space CSS indent matching existing admin.css style.
  </action>
  <verify>
    <automated>cd /mnt/c/users/chrislunt/source/personal/deckflow && f=DeckFlow.Web/wwwroot/css/admin-common.css; test -f "$f" || { echo FAIL_NOFILE; exit 1; }; \
grep -q -- '--danger: #dc2626' "$f" && grep -q -- '--on-accent' "$f" && grep -q '\.admin-shell \.admin-harvest__panel' "$f" && grep -q '\.admin-shell \.admin-table-scroll' "$f" && grep -q '\.admin-shell \.admin-table-scroll:focus-visible' "$f" && grep -q '\.admin-shell \.feedback-banner--success' "$f" && grep -q '\.admin-shell dialog\.admin-modal::backdrop' "$f" && grep -q '\.admin-shell \.type-badge' "$f" || { echo FAIL_MISSING_SELECTOR; exit 1; }; \
for sel in '.admin-shell .admin-sidebar__link' '.admin-shell .admin-range-selector a' '.admin-shell .admin-feedback-filter' '.admin-shell .admin-feedback-pagination a' '.admin-shell .admin-feedback-table a' '.admin-shell .admin-table a' '.admin-shell .admin-feedback-detail a' '.admin-shell summary' '.admin-shell .admin-action-form button' '.admin-shell .detail-actions button' '.admin-shell button.danger' '.admin-shell .admin-action-form input:not([type="hidden"])' '.admin-shell .admin-action-form select' '.admin-shell .admin-feedback-type select' '.admin-shell .admin-action-form textarea'; do grep -qF "$sel" "$f" || { echo "FAIL_TOUCHTARGET_SELECTOR_MISSING: $sel"; exit 1; }; done; \
floors=$(grep -c 'min-height: 44px' "$f"); [ "$floors" -ge 1 ] || { echo FAIL_NO_44PX_FLOOR; exit 1; }; \
# HIGH-A (cycle-4): the summary toggle must NEVER be display:none in admin-common.css (no base display:none to inherit at >=769px) \
! grep -qE '\.admin-shell \.admin-sidebar__toggle[^{]*\{[^}]*display:[[:space:]]*none' "$f" || { echo FAIL_SUMMARY_BASE_DISPLAY_NONE; exit 1; }; \
! grep -qE 'var\(--line\)|var\(--link\)|var\(--accent-strong|var\(--fs-sm\)' "$f" || { echo FAIL_PUBLIC_TOKEN; exit 1; }; \
! grep -qE 'thead[[:space:]]*\{' "$f" || { echo FAIL_THEAD_RULE; exit 1; }; \
# Comma-aware selector audit: split each prelude before { on commas, flag any non-.admin-shell, non-exempt component member \
audit=$(awk 'BEGIN{RS="{"} {p=$0; sub(/.*}/,"",p); n=split(p,parts,","); for(i=1;i<=n;i++){s=parts[i]; gsub(/^[ \t\r\n]+|[ \t\r\n]+$/,"",s); if(s ~ /^(\.admin-(sidebar|topbar|content|table|banner|action-form|page-header|analytics|sparkline|empty|modal|feedback|range-selector|harvest)|\.detail-|\.type-badge|\.feedback-banner|dialog\.admin-modal)/ && s !~ /\.admin-shell/){print s}}}' "$f" | wc -l); \
[ "$audit" -eq 0 ] || { echo "FAIL_UNSCOPED_SELECTOR audit=$audit"; awk 'BEGIN{RS="{"} {p=$0; sub(/.*}/,"",p); n=split(p,parts,","); for(i=1;i<=n;i++){s=parts[i]; gsub(/^[ \t\r\n]+|[ \t\r\n]+$/,"",s); if(s ~ /^(\.admin-(sidebar|topbar|content|table|banner|action-form|page-header|analytics|sparkline|empty|modal|feedback|range-selector|harvest)|\.detail-|\.type-badge|\.feedback-banner|dialog\.admin-modal)/ && s !~ /\.admin-shell/){print "  unscoped: "s}}}' "$f"; exit 1; }; \
echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - File `DeckFlow.Web/wwwroot/css/admin-common.css` exists.
    - `:root` contains `--danger: #dc2626;` AND `--on-accent: #fff;` plus all 7 original admin tokens.
    - SELECTOR-AUDIT GATE (D-SCOPE — comma-aware, resolves cycle-2 HIGH #2 + cycle-3 MEDIUM grouped-list blind spot): the awk audit in `<verify>` splits EACH selector prelude before its `{` on commas and inspects every member; it returns ZERO non-exempt component selectors lacking a `.admin-shell` prefix (so an unscoped selector hiding LATER in a grouped comma-separated list is caught, not just line-starts). The audit set INCLUDES `.feedback-banner` (cycle-3 fix). Every `.admin-sidebar*`, `.admin-topbar*`, `.admin-content`, `.admin-table*`, `.admin-banner*`, `.admin-action-form*`, `.maintenance-page*`, `.admin-page-header*`, `.admin-analytics*`, `.admin-sparkline`, `.admin-empty`, `.admin-modal*`, `dialog.admin-modal::backdrop`, `.admin-feedback*`, `.detail-*`, `.type-badge`, `.feedback-banner*`, `.admin-harvest__panel`, `.admin-table-scroll*` selector is prefixed `.admin-shell `. The ONLY unscoped selectors are the 8 enumerated `/* admin-only global */` exempt items.
    - TOUCH TARGETS (AMOB-03 EXHAUSTIVE inventory — resolves cycle-3 MEDIUM verify/grouping conflict): the verify proves EACH of the 15 inventory selectors EXISTS in the file (grep loop) AND that at least one `min-height: 44px` declaration block is present; it does NOT require N separate literal `min-height` lines, so grouping identical declarations into comma-separated lists is fully allowed. Coverage is per-selector-presence, not per-line-count. Covered: sidebar link, range-selector link, feedback filter, feedback pagination link, table/View action link, detail "Back to list" link (`.admin-feedback-detail a`), summary, action-form button, detail-actions/danger button, action-form input, action-form select + feedback type `<select>`, and textarea.
    - The detail "Back to list" link is covered by `.admin-shell .admin-feedback-detail a` (CSS-only; no Detail.cshtml markup change) and the feedback type `<select>` by `.admin-shell .admin-feedback-type select` (cycle-2 HIGH #1 additions).
    - BANNER (D-BANNER): `.admin-shell .feedback-banner` and `.admin-shell .feedback-banner--success` present in admin-common.css.
    - `.admin-shell .admin-table-scroll:focus-visible` is in the file (MEDIUM fix).
    - ZERO occurrences of `var(--line)`, `var(--link)`, `var(--accent-strong`, `var(--fs-sm)` (token substitution complete).
    - The Phase 16 modal `#dc2626` literals are replaced with `var(--danger)` (raw `#dc2626` only remains inside the `:root` token definition).
    - The ONLY `@media` in the file is `@media (prefers-reduced-motion: reduce)` — no `@media (max-width` or `@media (min-width` string present.
    - NO bare `thead {` rule and NO `thead { display: none` anywhere — the card-stack header is hidden via the .sr-only clip pattern in admin-mobile.css per D-A11Y-HEADER; admin-common.css only carries the contract comment.
    - `.admin-shell .admin-action-form { display: inline; }` appears exactly once (not duplicated from site-common.css:801).
    - A comment states the 44px floor aligns with WCAG 2.5.5 AAA (NOT labelled as the AA bar).
  </acceptance_criteria>
  <done>admin-common.css holds every non-viewport admin rule with admin tokens only; EVERY component selector (including the formerly-unscoped admin.css blocks) is .admin-shell-scoped and the comma-aware selector-audit gate (now including .feedback-banner) passes; all 15 touch-target inventory selectors are present and covered by a 44px floor (coverage proven per-selector, not by line-count); the feedback success banner is migrated scoped; card-stack header-accessibility contract documented (no thead{display:none}); modal block rewritten scoped with --danger substitution.</done>
</task>

<task type="auto">
  <name>Task 2: Create admin-mobile.css with all viewport @media responsive rules (accessible card-stack header, long-token overflow guards, coherent desktop nav guard, .admin-shell-scoped)</name>
  <files>DeckFlow.Web/wwwroot/css/admin-mobile.css</files>
  <read_first>
    - DeckFlow.Web/wwwroot/css/site-mobile.css lines 1-30 (file-header comment + breakpoint structure to mirror)
    - DeckFlow.Web/wwwroot/css/admin-common.css (just created in Task 1 — confirm the selector names the @media rules will override: .admin-shell, .admin-sidebar, .admin-sidebar__toggle, .admin-sidebar__disclosure, .admin-sidebar__nav, .admin-sidebar__brand, .admin-action-form, .admin-harvest__panel, .admin-table-scroll, .admin-table--card, .detail-grid, .detail-message; CONFIRM admin-common.css has NO base `display:none` on .admin-sidebar__toggle (cycle-4 HIGH-A: its display is set ONLY by these two @media blocks — flex on mobile, block+clip on desktop — so it is never display:none at any width); AND read the .sr-only clip definition there so the card-stack thead AND the desktop summary clip reuse the SAME clip values)
    - .planning/phases/18-admin-mobile-responsive-sweep/18-PATTERNS.md (admin-mobile.css section, lines 256-408 — exact @media rule inventory)
    - .planning/phases/18-admin-mobile-responsive-sweep/18-RESEARCH.md Pitfall 1 (lines 598-617 — desktop forced-open guard)
  </read_first>
  <action>
    Create `admin-mobile.css` containing ALL viewport `@media` rules (NO non-@media rules
    except the file-header comment). Open with a header comment mirroring site-mobile.css:
    state it holds admin responsive overrides, is loaded via a fingerprinted `<link>` in
    _AdminLayout.cshtml AFTER admin-common.css, and uses 768px as the primary breakpoint. Per
    D-SCOPE every selector inside the @media blocks MUST be `.admin-shell`-scoped (e.g.
    `.admin-shell .admin-sidebar__toggle`, `.admin-shell .admin-table--card`); the only exception
    is `.admin-shell` itself (the shell grid rule). Add a single `@media (max-width: 768px)`
    block containing: (a) shell grid collapse — `.admin-shell { grid-template-columns: 1fr;
    grid-template-rows: auto auto 1fr; grid-template-areas: "topbar" "sidebar" "content"; }`. (b)
    sidebar mobile chrome — `.admin-shell .admin-sidebar { border-right: none; border-bottom:
    1px solid var(--border); padding: 0; }` and the disclosure toggle
    `.admin-shell .admin-sidebar__toggle { display: flex; align-items: center; padding: 12px 20px;
    min-height: 44px; cursor: pointer; font-weight: 700; list-style: none; user-select: none;
    color: var(--text); }` plus the marker-hiding rules
    `.admin-shell .admin-sidebar__toggle::-webkit-details-marker { display: none; }` and
    `.admin-shell .admin-sidebar__toggle::marker { content: ""; }`, and the collapse rule
    `.admin-shell .admin-sidebar__disclosure:not([open]) .admin-sidebar__nav,
    .admin-shell .admin-sidebar__disclosure:not([open]) .admin-sidebar__brand { display: none; }`.
    (c) form single-column — `.admin-shell .admin-action-form { display: block; margin-bottom:
    8px; }`, `.admin-shell .admin-action-form input, .admin-shell .admin-action-form select,
    .admin-shell .admin-action-form textarea { width: 100%; box-sizing: border-box; margin-bottom:
    8px; }`, `.admin-shell .admin-action-form button { width: 100%; }`, and
    `.admin-shell .admin-harvest__panel { padding: 12px; }`. (d) overflow-x table tweak —
    `.admin-shell .admin-table-scroll { padding-bottom: 4px; }` (iOS scrollbar visibility). (e)
    card-stack pattern (D-A11Y-HEADER — DO NOT use `display: none` on `thead`): hide the header
    row with the SAME `.sr-only` clip values read from admin-common.css —
    `.admin-shell .admin-table--card thead { position: absolute; width: 1px; height: 1px;
    overflow: hidden; clip: rect(0 0 0 0); white-space: nowrap; }` (a comment must state this
    keeps `<th scope=col>` in the a11y tree while removing it visually; data-label is visual-only).
    Then `.admin-shell .admin-table--card tr { display: block; border: 1px solid var(--border);
    border-radius: 4px; margin-bottom: 8px; padding: 8px 12px; }`,
    `.admin-shell .admin-table--card td { display: flex; justify-content: space-between; gap: 8px;
    padding: 4px 0; border: none; font-size: 0.9em; min-width: 0; overflow-wrap: anywhere; }`
    (the `min-width: 0` lets the flex item shrink below its content's intrinsic width and
    `overflow-wrap: anywhere` breaks long unbroken value tokens — feedback messages, emails, flag
    keys, page URLs, user agents, hashes — so 320px gets NO horizontal body overflow with realistic
    data; a comment must reference the long-token / 320px no-overflow case, cycle-3 NEW HIGH). The
    VISUAL-ONLY label keeps `flex-shrink: 0` so the label column is not squeezed to zero while the
    value side wraps: `.admin-shell .admin-table--card td::before { content: attr(data-label);
    font-weight: 600; color: var(--muted); flex-shrink: 0; }` (a comment must mark `::before` as a
    sighted-user visual affordance only). (e1) LONG-TOKEN OVERFLOW GUARD for the detail view
    (resolves cycle-3 NEW HIGH): inside this same `@media (max-width: 768px)` block add
    `.admin-shell .detail-grid { min-width: 0; }` and
    `.admin-shell .detail-grid dd, .admin-shell .detail-grid code, .admin-shell .detail-message
    { overflow-wrap: anywhere; min-width: 0; }` (a comment must state this prevents 320px body
    overflow from long emails / page URLs / user-agents / hashes / flag keys; `.detail-grid` value
    cells are the unbounded `dd` cells plus any inline `code`). (e2) ACTION-CELL CROWDING FIX
    (resolves cycle-2 MEDIUM — the "Actions" cell on Feedback rows can carry both a "View" link AND
    an archive form/button in one flex row, which crowds at 320px): add a rule so the action cell's
    children wrap and align cleanly — `.admin-shell .admin-table--card td[data-label="Actions"],
    .admin-shell .admin-table--card td[data-label="Action"] { flex-wrap: wrap; align-items: center;
    justify-content: flex-end; gap: 8px; }` and ensure any inline action form inside it does not
    force overflow: `.admin-shell .admin-table--card td[data-label="Actions"] .admin-action-form,
    .admin-shell .admin-table--card td[data-label="Action"] .admin-action-form { display: inline-flex; }`
    (a comment must reference the 320px crowding case with both View + Archive present). Then add a
    SEPARATE `@media (min-width: 769px)` block — the COHERENT desktop nav guard (D-DESKTOP-NAV,
    resolves cycle-3 PARTIAL HIGH-3): (i) force the nav + brand always-visible inside the
    `<details>` regardless of open state —
    `.admin-shell .admin-sidebar__disclosure .admin-sidebar__nav,
    .admin-shell .admin-sidebar__disclosure .admin-sidebar__brand { display: block !important; }`
    (this `!important` is the documented exception). (ii) DO NOT `display: none` the `<summary>`.
    Instead CLIP it (same .sr-only values) AND EXPLICITLY set a NON-NONE display in this same desktop
    rule so it stays in the a11y tree AND keyboard-focusable but is visually removed so it does not
    duplicate the always-visible nav chrome —
    `.admin-shell .admin-sidebar__toggle { display: block; position: absolute; width: 1px;
    height: 1px; overflow: hidden; clip: rect(0 0 0 0); white-space: nowrap; }`. CRITICAL
    (cycle-4 HIGH-A fix): the `display: block` MUST be present ALONGSIDE the clip declarations — the
    bug was that the prior base `display:none` on `.admin-sidebar__toggle` (now removed in Task 1
    step (h)) was never reset here, so at ≥769px the summary inherited `display:none` and left the
    accessibility tree. A clipped element is in the a11y tree only if its `display` is NOT none, hence
    the explicit `display: block`. A comment MUST explain: "Desktop keeps the <summary> clipped-but-
    PRESENT (display:block + .sr-only clip, NOT display:none) so it stays in the a11y tree and remains
    a real, operable, keyboard-focusable fallback control if any AT treats the closed <details> as
    collapsed; the nav is also forced visible above. This is the single coherent no-JS desktop pattern
    (D-DESKTOP-NAV) — no contradiction with a display:none summary." Use only admin tokens
    (`--border --text --muted`). LF line endings, 2-space CSS indent.
  </action>
  <verify>
    <automated>cd /mnt/c/users/chrislunt/source/personal/deckflow && f=DeckFlow.Web/wwwroot/css/admin-mobile.css; test -f "$f" || { echo FAIL_NOFILE; exit 1; }; \
grep -q '@media (max-width: 768px)' "$f" && grep -q '@media (min-width: 769px)' "$f" && grep -q 'grid-template-areas' "$f" && grep -q '\.admin-shell \.admin-sidebar__disclosure:not(\[open\])' "$f" && grep -q 'attr(data-label)' "$f" && grep -q 'display: block !important' "$f" && grep -q 'data-label="Actions"' "$f" || { echo FAIL_CORE; exit 1; }; \
# long-token overflow guards present (cycle-3 NEW HIGH) \
grep -q 'overflow-wrap: anywhere' "$f" || { echo FAIL_OVERFLOW_WRAP; exit 1; }; \
grep -q '\.admin-shell \.admin-table--card td {' "$f" || { echo FAIL_CARD_TD; exit 1; }; \
grep -q '\.admin-shell \.detail-grid dd' "$f" || { echo FAIL_DETAIL_DD; exit 1; }; \
# at least two clip blocks: the card-stack thead AND the desktop summary (D-DESKTOP-NAV) \
[ "$(grep -c 'clip: rect(0 0 0 0)' "$f")" -ge 2 ] || { echo FAIL_CLIP_COUNT; exit 1; }; \
grep -q '\.admin-shell \.admin-table--card thead {' "$f" || { echo FAIL_THEAD_CLIP; exit 1; }; \
# desktop summary must be CLIPPED + display non-none, never display:none (HIGH-3 + cycle-4 HIGH-A fix) \
! grep -qE '\.admin-shell \.admin-sidebar__toggle[^{]*\{[^}]*display:[[:space:]]*none' "$f" || { echo FAIL_SUMMARY_DISPLAY_NONE; exit 1; }; \
# extract the >=769px .admin-sidebar__toggle declaration block (state machine) and assert it CLIPS and sets a non-none display (cycle-4 HIGH-A) \
desk=$(awk '/@media \(min-width: 769px\)/{inmedia=1} inmedia && /\.admin-shell \.admin-sidebar__toggle[^{]*\{/{intoggle=1; line=$0; sub(/.*\{/,"",line); print line; if(line ~ /\}/){intoggle=0}; next} intoggle{print; if($0 ~ /\}/){intoggle=0}}' "$f"); \
echo "$desk" | grep -q 'position: absolute' || { echo FAIL_DESKTOP_SUMMARY_NOT_CLIPPED; exit 1; }; \
echo "$desk" | grep -q 'clip: rect(0 0 0 0)' || { echo FAIL_DESKTOP_SUMMARY_NOT_CLIPPED; exit 1; }; \
echo "$desk" | grep -qE 'display:[[:space:]]*(block|inline-flex|inline-block|flex|list-item)' || { echo FAIL_DESKTOP_SUMMARY_NO_NONNONE_DISPLAY; exit 1; }; \
echo "$desk" | grep -qE 'display:[[:space:]]*none' && { echo FAIL_DESKTOP_SUMMARY_DISPLAY_NONE; exit 1; } || true; \
! grep -q 'thead {[[:space:]]*display: none' "$f" || { echo FAIL_THEAD_DISPLAY_NONE; exit 1; }; \
! grep -qE 'var\(--line\)|var\(--link\)|var\(--fs-sm\)' "$f" || { echo FAIL_PUBLIC_TOKEN; exit 1; }; \
# comma-aware selector audit (mirrors Task 1) \
audit=$(awk 'BEGIN{RS="{"} {p=$0; sub(/.*}/,"",p); gsub(/@media[^,{]*/,"",p); n=split(p,parts,","); for(i=1;i<=n;i++){s=parts[i]; gsub(/^[ \t\r\n]+|[ \t\r\n]+$/,"",s); if(s ~ /^(\.admin-(sidebar|table|action-form|harvest)|\.detail-|\.type-badge|\.feedback-banner|dialog\.admin-modal)/ && s !~ /\.admin-shell/){print s}}}' "$f" | wc -l); \
[ "$audit" -eq 0 ] || { echo "FAIL_UNSCOPED_SELECTOR audit=$audit"; exit 1; }; \
echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - File `DeckFlow.Web/wwwroot/css/admin-mobile.css` exists.
    - Contains exactly one `@media (max-width: 768px)` block AND one `@media (min-width: 769px)` block.
    - Every selector is `.admin-shell`-scoped (or `.admin-shell` itself for the grid rule) per D-SCOPE; the comma-aware selector-audit awk gate in `<verify>` returns ZERO unscoped component selectors (inspecting comma-separated prelude members).
    - Mobile block includes the shell grid collapse (`grid-template-areas` with "topbar"/"sidebar"/"content" stacking), the `.admin-shell .admin-sidebar__toggle` display:flex rule with `min-height: 44px`, the `:not([open])` collapse selector, single-column `.admin-action-form` rules (input/select/textarea full-width), `.admin-table-scroll` tweak, and the full `.admin-table--card` card-stack with `td::before { content: attr(data-label) }`.
    - LONG-TOKEN OVERFLOW (cycle-3 NEW HIGH): `.admin-shell .admin-table--card td` carries `min-width: 0` + `overflow-wrap: anywhere`; `.admin-shell .detail-grid dd`, `.admin-shell .detail-grid code`, and `.admin-shell .detail-message` carry `overflow-wrap: anywhere` (+ `min-width: 0`) so 320px has no body overflow from long emails / URLs / user-agents / hashes / flag keys.
    - ACTION-CELL FIX (cycle-2 MEDIUM): the `td[data-label="Actions"]` / `td[data-label="Action"]` flex-wrap rule is present so the View + Archive pair does not crowd at 320px.
    - CARD-STACK A11Y (D-A11Y-HEADER): the header is hidden via the `.sr-only` clip pattern (`position:absolute; width:1px; height:1px; clip: rect(0 0 0 0)`) — there is NO `thead { display: none }` rule; a comment marks `td::before` as visual-only.
    - DESKTOP NAV GUARD (D-DESKTOP-NAV — resolves cycle-3 PARTIAL HIGH-3 + cycle-4 HIGH-A): the `@media (min-width: 769px)` block forces `.admin-sidebar__nav` + `.admin-sidebar__brand` visible with `display: block !important`, AND the `<summary>` (`.admin-shell .admin-sidebar__toggle`) is CLIPPED-BUT-PRESENT — its desktop declaration block sets an EXPLICIT non-none display (`display: block`) ALONGSIDE the clip declarations (`position:absolute; width:1px; height:1px; clip: rect(0 0 0 0); overflow:hidden`) — and is NEVER `display: none` at ANY width (there is no base `display:none` in admin-common.css for it to inherit; cycle-4 HIGH-A fix). Because a clipped element is in the a11y tree only when its `display` is not none, the explicit `display: block` is what keeps the summary keyboard-focusable and screen-reader-announced as the operable fallback control. The verify FAILS if the desktop `.admin-sidebar__toggle` block uses `display: none`, OMITS a non-none display, or is not clipped. There are at least two `clip: rect(0 0 0 0)` blocks (card-stack thead + desktop summary).
    - ZERO non-@media top-level rules (every rule is inside one of the two `@media` blocks; only the header comment sits outside).
    - ZERO public-only tokens (`--line`, `--link`, `--fs-sm`).
  </acceptance_criteria>
  <done>admin-mobile.css holds all viewport responsive rules including sidebar collapse, accessible card-stack (visually-hidden header, not display:none), long-token overflow guards on card-stack td + detail-grid dd/code/detail-message (320px no-overflow), action-cell crowding fix, single-column forms, and the COHERENT desktop nav guard (nav forced visible + summary CLIPPED-but-PRESENT via explicit `display: block` alongside the clip — never display:none at any width — so it stays in the a11y tree and operable, D-DESKTOP-NAV / cycle-4 HIGH-A); all selectors .admin-shell-scoped and the comma-aware audit gate passes; no non-@media rules.</done>
</task>

<task type="auto">
  <name>Task 3: Reduce admin.css to import shim, switch _AdminLayout to two fingerprinted link tags, and remove dead rules from site-common.css</name>
  <files>DeckFlow.Web/wwwroot/css/admin.css, DeckFlow.Web/wwwroot/css/site-common.css, DeckFlow.Web/Views/Shared/_AdminLayout.cshtml</files>
  <read_first>
    - DeckFlow.Web/wwwroot/css/admin.css (ENTIRE current file — being replaced wholesale by the shim; confirm every rule it contained was moved into admin-common.css in Task 1)
    - DeckFlow.Web/wwwroot/css/admin-common.css (Task 1 output — confirm parity before deleting source rules from admin.css)
    - DeckFlow.Web/wwwroot/css/site-common.css lines 770-812 (the exact dead-rule block 776-810 to delete, bounded by the `.feedback-honeypot` rule above and the `Card Lookup readability redesign` comment below; NOTE: lines 772-773 `.feedback-banner`/`.feedback-banner--success` are PUBLIC-shared and MUST be LEFT IN PLACE per D-BANNER — only 776-810 are deleted)
    - DeckFlow.Web/Views/Shared/_AdminLayout.cshtml line 14 (the `<link href="~/css/admin.css">` tag — being replaced by two fingerprinted tags per D-CACHE) AND _Layout.cshtml lines 44-47 (the two-fingerprinted-link pattern to mirror)
    - .planning/phases/18-admin-mobile-responsive-sweep/18-RESEARCH.md Pitfall 5 (cache-busting) + Pitfall 6 (lines 666-675 — why the dead site-common.css copies MUST be removed)
  </read_first>
  <action>
    Three edits, one logical change (the CSS load wiring). (1) Replace the ENTIRE contents of
    `admin.css` with the import shim only: a header comment block stating "DeckFlow Admin Shell —
    v1.4 Phase 18. Import shim / fallback entry point only. Do not add rules here.
    _AdminLayout.cshtml loads admin-common.css + admin-mobile.css directly via two fingerprinted
    <link asp-append-version> tags (D-CACHE) — this shim is retained as a documented fallback.
    Layout primitives -> admin-common.css. Responsive overrides (@media) -> admin-mobile.css"
    followed by exactly two lines: `@import url('admin-common.css');` and
    `@import url('admin-mobile.css');` (common first so @media overrides win the cascade). (2) In
    `_AdminLayout.cshtml`, REPLACE the single `<link rel="stylesheet" href="~/css/admin.css"
    asp-append-version="true" />` on line 14 with TWO tags, in this order (mirroring _Layout.cshtml
    lines 44-47), preserving the existing indentation of line 14:
    `<link rel="stylesheet" href="~/css/admin-common.css" asp-append-version="true" />` then
    `<link rel="stylesheet" href="~/css/admin-mobile.css" asp-append-version="true" />`. This gives
    BOTH files independent ASP.NET cache-busting fingerprints (resolves the @import stale-cache
    MEDIUM). Touch ONLY line 14 (the one `<link>`); the viewport meta (line 12), `<head>`
    structure, topbar, sidebar, and `@RenderBody()` stay byte-identical. (3) In `site-common.css`,
    DELETE the dead admin block lines 776-810 (the `/* Admin feedback */` comment through the
    `.detail-actions button.danger` rule inclusive) — these rules are unreachable on admin pages
    (admin pages never load site-common.css) and now live token-corrected + `.admin-shell`-scoped
    in admin-common.css; leaving them risks the duplicate `.admin-action-form { display: inline; }`
    (site-common.css:801) styling any future public-page form that uses that class (Pitfall 6).
    CRITICAL: do NOT delete the `.feedback-banner` / `.feedback-banner--success` rules at lines
    772-773 — those are SHARED with the PUBLIC feedback form and must stay (D-BANNER migrated a
    .admin-shell-scoped COPY into admin-common.css; the public original stays here). Touch ONLY
    lines 776-810 in site-common.css — do not reformat surrounding rules (`.feedback-banner` /
    `.feedback-banner--success` / `.feedback-honeypot` above and the `Card Lookup readability
    redesign` block below stay byte-identical). Preserve LF line endings everywhere.
  </action>
  <verify>
    <automated>cd /mnt/c/users/chrislunt/source/personal/deckflow && a=DeckFlow.Web/wwwroot/css/admin.css && l=DeckFlow.Web/Views/Shared/_AdminLayout.cshtml && s=DeckFlow.Web/wwwroot/css/site-common.css && grep -q "@import url('admin-common.css')" "$a" && grep -q "@import url('admin-mobile.css')" "$a" && [ "$(grep -vcE '^\s*(/\*|\*|\*/|@import|$)' "$a")" -eq 0 ] && grep -q 'href="~/css/admin-common.css" asp-append-version="true"' "$l" && grep -q 'href="~/css/admin-mobile.css" asp-append-version="true"' "$l" && ! grep -q 'href="~/css/admin.css"' "$l" && ! grep -qE '^\s*\.admin-feedback' "$s" && ! grep -qE '^\s*\.detail-grid' "$s" && grep -q '\.feedback-banner--success' "$s" && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `admin.css` contains exactly the two `@import` lines (admin-common first, admin-mobile second) plus a header comment — no other CSS rules remain (no selectors, no `:root`, no `.admin-*` rules).
    - `_AdminLayout.cshtml` now has TWO `<link asp-append-version="true">` tags — `admin-common.css` then `admin-mobile.css` — and NO remaining `~/css/admin.css` `<link>` reference (D-CACHE).
    - `_AdminLayout.cshtml` line 12 viewport meta and all non-line-14 markup are byte-identical to before.
    - `site-common.css` no longer contains `.admin-feedback`, `.admin-feedback-table`, `.detail-grid`, `.type-badge`, `.detail-actions`, `.detail-message`, or the duplicate `.admin-action-form` block (lines 776-810 deleted).
    - `site-common.css` STILL contains `.feedback-banner` and `.feedback-banner--success` (lines 772-773 — public-shared, NOT deleted per D-BANNER).
    - `site-common.css` lines surrounding the deletion (`.feedback-honeypot` and the `Card Lookup readability redesign` comment) are byte-identical to before (no reformatting).
    - `dotnet build -c Release` (run in verification) stays 0 warnings / 0 errors — CSS/Razor changes do not affect compilation but wwwroot must copy clean.
  </acceptance_criteria>
  <done>admin.css is a pure 2-import fallback shim; _AdminLayout.cshtml loads both new CSS files via fingerprinted link tags (cache-bust fix); site-common.css dead admin rules removed while the public feedback-banner rules are preserved; no surrounding-line reformatting.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → Admin shell | All `/Admin/*` routes already gated by `BasicAuthMiddleware`; this plan adds NO new endpoints, inputs, or data flows. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-01 | Tampering | CSS file factoring | accept | Pure CSS restructure (no executable code, no user input rendered). No new XSS sink: no `data-*` value is reflected from user input in this plan; the `data-label` attributes added in Plan 02 are STATIC column headers authored in Razor, not user data. |
| T-18-02 | Information Disclosure | two `<link>` tags on BasicAuth-gated pages | accept | admin CSS files are static assets already served from the same origin; no secret or PII in CSS. Admin shell is BasicAuth-protected; CSS exposure adds no new disclosure surface. The D-CACHE switch to fingerprinted links changes only the URL query string, not the asset surface. |
| T-18-03 | Tampering | npm/pip/cargo installs | mitigate | No packages installed this phase (CSS/Razor only). RESEARCH "Package Legitimacy Audit" = not applicable. No install tasks exist, so no legitimacy checkpoint required. |

**Note:** Phase 18 is CSS + Razor markup only. No server-side logic, no new endpoints, no auth
changes, no persistence. ASVS V2-V6 inapplicable per RESEARCH Security Domain section. The
threat model is intentionally minimal and stated explicitly rather than omitted.
</threat_model>

<verification>
- `dotnet build -c Release` from clean obj/: 0 warnings / 0 errors (wwwroot static assets + Razor copy/compile clean).
- `admin.css` is the 2-import fallback shim only (grep both @import lines; no other rules).
- `_AdminLayout.cshtml` loads admin-common.css + admin-mobile.css via two fingerprinted `<link>` tags; no `~/css/admin.css` link remains (D-CACHE).
- `admin-common.css` exists with `--danger`/`--on-accent` tokens, all 15 `.admin-shell`-scoped touch-target inventory selectors present each covered by a 44px floor (incl. Back-to-list link + type select), migrated admin-feedback rules + the feedback success banner using admin tokens AND `.admin-shell`-scoped, `admin-harvest__panel`, `admin-table-scroll`, `admin-table-scroll:focus-visible`; the comma-aware selector-audit gate (including `.feedback-banner`) passes (zero unscoped component selectors); ONLY @media is prefers-reduced-motion; NO `thead { display: none }`.
- `admin-mobile.css` exists with one `max-width:768px` + one `min-width:769px` block; sidebar collapse, accessible card-stack (visually-hidden header via clip, NOT display:none), long-token overflow guards (`overflow-wrap:anywhere` + `min-width:0` on card-stack td + detail-grid dd/code/detail-message), action-cell crowding fix, single-column forms, COHERENT desktop nav guard (nav forced visible + summary CLIPPED not display:none per D-DESKTOP-NAV); all selectors `.admin-shell`-scoped (comma-aware audit gate passes).
- `site-common.css` dead admin rules (776-810) removed; the public feedback-banner rules (772-773) preserved; surrounding lines untouched.
- No public-only tokens (`--line --link --fs-sm --accent-strong`) anywhere in admin CSS.
</verification>

<success_criteria>
- AMOB-04 CSS-factoring COMPLETE: admin.css → admin-common.css + admin-mobile.css + fallback shim; ALL admin component selectors `.admin-shell`-scoped — including the formerly-unscoped existing blocks — and the deterministic comma-aware selector-audit gate (including `.feedback-banner`) proves zero non-exempt top-level selectors remain (only the 8 enumerated admin-only global resets are unscoped); zero conceptual bleed (no public tokens, dead site-common rules removed); both new files independently fingerprinted (D-CACHE).
- All CSS contracts that Plan 02 markup consumes are defined: `admin-sidebar__disclosure`/`__toggle`, `admin-table--card` (accessible header + long-token wrapping), `admin-table-scroll`, single-column forms, ≥44px touch targets on the FULL EXHAUSTIVE interactive-element inventory (sidebar/range/filter/pagination/View/Back links, summary, buttons, inputs, the feedback type select, textarea).
- 320px no-overflow CONTRACT exists: card-stack td value content + detail-grid dd/code/detail-message wrap long unbroken user data (overflow-wrap:anywhere + min-width:0) so the page never overflows horizontally at 320px with realistic data (cycle-3 NEW HIGH).
- Desktop nav no-JS pattern is COHERENT (D-DESKTOP-NAV): at ≥769px nav forced visible AND the `<summary>` clipped-but-operable (NOT display:none) so the fallback is real — CSS, acceptance, and the 769px checkpoint all agree (cycle-3 HIGH-3 contradiction resolved).
- AMOB-01 CSS contract exists (disclosure selectors defined); AMOB-01 becomes observably TRUE in Plan 02 when the `<details>`/`<summary>` markup is added.
- Pre-existing admin-feedback CSS gap CLOSED (rules now reachable on admin pages via admin-common.css); admin feedback SUCCESS BANNER gap CLOSED (D-BANNER migrated scoped copy).
- `admin-harvest__panel` styling gap CLOSED.
</success_criteria>

<output>
Create `.planning/phases/18-admin-mobile-responsive-sweep/18-01-SUMMARY.md` when done.
</output>
</content>
</invoke>
