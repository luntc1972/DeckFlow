---
phase: 42-orchestrator-extraction
plan: 04
type: execute
wave: 4
depends_on: ["42-03"]
files_modified:
  - DeckFlow.Studio/Services/ContentKbOrchestratorSmokeService.cs
  - DeckFlow.Studio/Program.cs
autonomous: true
requirements: [ORCH-02]
user_setup: []

must_haves:
  truths:
    - "DeckFlow.Studio resolves the IContentMaintenanceOrchestrator slice from DI and calls ListBlockedAsync from a Blazor service (proves D-01 slice injection + D-08)"
    - "DeckFlow.Studio references DeckFlow.Core only and has NO DeckFlow.CLI project reference"
    - "Studio composition root registers EVERY ContentKbOrchestrator ctor dependency with REAL local SQLite-backed stores (no implicit throwing fakes), registers the ContentKbOrchestratorOptions (host-resolved local artifactRoot), then calls AddContentKbOrchestrator() so the full ctor resolves"
    - "dotnet build DeckFlow.sln is 0 errors / 0 new warnings"
  artifacts:
    - path: "DeckFlow.Studio/Services/ContentKbOrchestratorSmokeService.cs"
      provides: "Minimal Studio service that resolves the maintenance slice + calls ListBlockedAsync (D-08, proves SC4 + D-01 slice injection)"
      contains: "IContentMaintenanceOrchestrator"
    - path: "DeckFlow.Studio/Program.cs"
      provides: "DI wiring: all orchestrator ctor deps (real local SQLite stores) + ContentKbOrchestratorOptions + AddContentKbOrchestrator() + smoke service"
      contains: "AddContentKbOrchestrator"
  key_links:
    - from: "DeckFlow.Studio/Services/ContentKbOrchestratorSmokeService.cs"
      to: "DeckFlow.Core.Orchestration.IContentMaintenanceOrchestrator"
      via: "constructor injection + ListBlockedAsync call"
      pattern: "IContentMaintenanceOrchestrator"
    - from: "DeckFlow.Studio/Program.cs"
      to: "AddContentKbOrchestrator"
      via: "service registration"
      pattern: "AddContentKbOrchestrator"
---

<objective>
Prove SC4 end-to-end (D-08): add a minimal `ContentKbOrchestratorSmokeService` in DeckFlow.Studio that resolves the `IContentMaintenanceOrchestrator` SLICE from DI (demonstrating D-01 slice injection resolves, not just the facade) and calls the read-only `ListBlockedAsync`, and wire the FULL orchestrator dependency set in the Studio composition root using REAL local SQLite-backed stores so the orchestrator ctor resolves cleanly — confirming Studio -> Core works with NO `DeckFlow.CLI` reference. Smoke-level only; no Studio UI feature (Phase 43+).

Purpose: Demonstrate the extraction achieved its goal — Studio consumes the orchestrator (and a focused slice of it) directly without referencing the CLI executable (ORCH-02 / SC4).
Output: One Studio service + Studio Program.cs DI wiring. Solution builds clean.

Resolution approach (closes review HIGH-2 — no implicit throwing fakes): `AddContentKbOrchestrator()` registers the FULL concrete orchestrator, whose ctor needs every store/service. The container can only build it if every ctor dependency is registered. This plan registers ALL of them with REAL local SQLite-backed implementations the host already knows how to construct (the same store types the CLI uses), pointed at a local Studio SQLite path (NOT the prod Postgres connection string). The smoke call (`ListBlockedAsync`) only reads `IBlockedVideoStore`, but every dependency is still a real, resolvable registration — nothing is left to an unnamed throwing fake.

artifactRoot binding (round-2 MEDIUM fix): the orchestrator ctor takes a `ContentKbOrchestratorOptions` record (carrying a `required string ArtifactRoot`), NOT a bare `string`. Studio registers that options object with a LOCAL Studio data-dir artifactRoot (e.g. `services.AddSingleton(new ContentKbOrchestratorOptions { ArtifactRoot = <local dir> })`) so the container resolves it without any global bare-string registration. Path resolution stays host-side (D-06).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/42-orchestrator-extraction/42-CONTEXT.md
@.planning/phases/42-orchestrator-extraction/42-03-SUMMARY.md

<interfaces>
<!-- From Wave 3 (Plan 03). Wire these in Studio. -->
DeckFlow.Core.Orchestration:
  IContentKbOrchestrator (facade) + sub-interfaces incl. IContentMaintenanceOrchestrator
    (exposes ListBlockedAsync -> BlockedVideoListResult). Studio injects the SLICE.
  AddContentKbOrchestrator(this IServiceCollection) — registers ContentKbOrchestrator scoped +
    forwards the facade AND every sub-interface to that same scoped instance (Plan 03 Task 1).
    The host MUST first register every ctor dependency (below) INCLUDING the
    ContentKbOrchestratorOptions, THEN call this.

ContentKbOrchestrator ctor dependencies to register host-side (ENUMERATED — each needs a real
registration; none may be left to an unnamed throwing fake):
  1. IContentSourceStore        -> ContentSourceStore (local SQLite path)
  2. IContentVideoStore         -> ContentVideoStore (local SQLite path)
  3. IContentSiteIndexStore     -> ContentSiteIndexStore (local SQLite path)
  4. IBlockedVideoStore         -> BlockedVideoStore (local SQLite path)   [the only one ListBlocked touches]
  5. IContentHarvestRunStore    -> ContentHarvestRunStore (local SQLite path)
  6. ILlmSpendLedger            -> LlmSpendLedger (local SQLite path)
  7. IWhisperSpendLedger        -> WhisperSpendLedger (local SQLite path)
  8. ILlmDistillationService    -> the simplest real Core impl the host can construct that needs
                                   no API key / no network at construction time (smoke never calls it).
                                   If every concrete distill service requires a provider/secret to
                                   CONSTRUCT, register a tiny Studio-local no-op/null-object impl
                                   declared in Studio (named, NOT an anonymous throwing fake) and
                                   note it in the SUMMARY. Construction must not touch the network.
  9. IYouTubeChannelVideoLister -> simplest constructable Core impl, else a named Studio null-object
 10. ITranscriptSource          -> simplest constructable Core impl, else a named Studio null-object
 11. IFfmpegAudioChunker        -> simplest constructable Core impl, else a named Studio null-object
 12. Func<DateTimeOffset>       -> () => DateTimeOffset.UtcNow
 13. ContentKbOrchestratorOptions -> `new ContentKbOrchestratorOptions { ArtifactRoot = <local
                                   Studio data-dir path> }` (NOT prod); registered as a singleton
                                   so the ctor resolves the typed options record (round-2 MEDIUM fix:
                                   the ctor no longer takes a bare `string artifactRoot`).
  (ILogger<ContentKbOrchestrator> is optional — NullLogger default; no registration required.)

  CONFIRM the actual concrete store ctor signatures (db path vs RelationalDatabaseConnection) by
  reading DeckFlow.Core/Content/*Store.cs before wiring; construct them the same way the CLI does
  for the local SQLite case (D-05/D-06: host resolves the path + builds the store).

Existing Studio shape (DeckFlow.Studio/Program.cs):
  WebApplication.CreateBuilder; UseSerilog; AddUserSecrets; StudioConfig(isProdConfigured);
  AddRazorPages + AddServerSideBlazor. Studio csproj references DeckFlow.Core only.

Prod-config invariant: the prod Postgres connection string stays PRESENCE-ONLY (StudioConfig);
the smoke store uses a LOCAL SQLite path only. Do not construct, log, or surface the prod
connection string anywhere.
-->
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Studio smoke service that resolves the maintenance slice and calls ListBlockedAsync</name>
  <read_first>
    - DeckFlow.Studio/Program.cs (current composition root)
    - DeckFlow.Studio/StudioConfig.cs (presence-only prod config pattern — do not surface the connection string)
    - DeckFlow.Core/Orchestration/IContentMaintenanceOrchestrator.cs + BlockedVideoListResult.cs (the slice + result to call)
    - DeckFlow.Core/Content/IBlockedVideoStore.cs + BlockedVideoStore.cs (the one store ListBlockedAsync needs + its ctor)
    - DeckFlow.Core/Orchestration/ServiceCollectionExtensions.cs (AddContentKbOrchestrator contract)
  </read_first>
  <action>
    Create `DeckFlow.Studio/Services/ContentKbOrchestratorSmokeService.cs`, namespace `DeckFlow.Studio.Services`, `public sealed class ContentKbOrchestratorSmokeService`. Constructor-inject `IContentMaintenanceOrchestrator` (the SLICE, not the facade — this proves D-01 slice injection resolves through the forwarding registrations from Plan 03). Add one method `public async Task<int> ProbeAsync(CancellationToken cancellationToken = default)` that calls `ListBlockedAsync` and returns `result.Items.Count` — proving Studio can resolve and invoke Core orchestration without a CLI reference. The smoke call must NOT perform writes, network calls, or touch the prod connection string. Add XML doc-comments.

    This is deliberately minimal — no Blazor page, no UI, no harvest/distill trigger (deferred to Phase 43+). The class existing, resolving the slice, and compiling against Core is the SC4 + D-01 proof.
  </action>
  <verify>
    <automated>"/mnt/c/Program Files/dotnet/dotnet.exe" build DeckFlow.Studio/DeckFlow.Studio.csproj -warnaserror 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `DeckFlow.Studio/Services/ContentKbOrchestratorSmokeService.cs` injects `IContentMaintenanceOrchestrator` as a ctor dependency (`grep -n "IContentMaintenanceOrchestrator" DeckFlow.Studio/Services/ContentKbOrchestratorSmokeService.cs` matches) and has a `ProbeAsync` that calls `ListBlockedAsync`.
    - `grep -rn "DeckFlow.CLI" DeckFlow.Studio/` returns nothing (no CLI namespace usage); `grep -n "DeckFlow.CLI" DeckFlow.Studio/DeckFlow.Studio.csproj` returns nothing (no CLI project reference — SC4).
    - The smoke service does not reference any prod connection string and writes nothing to logs about it.
    - `dotnet build DeckFlow.Studio/DeckFlow.Studio.csproj -warnaserror` = 0 errors / 0 warnings.
  </acceptance_criteria>
  <done>A minimal Studio service resolves the IContentMaintenanceOrchestrator slice and calls ListBlockedAsync; Studio has no CLI reference.</done>
</task>

<task type="auto">
  <name>Task 2: Wire all orchestrator ctor deps (real local SQLite stores) + options + AddContentKbOrchestrator() in Studio Program.cs</name>
  <read_first>
    - DeckFlow.Studio/Program.cs (insertion point: after AddSingleton(StudioConfig), before app.Build())
    - DeckFlow.Core/Orchestration/ServiceCollectionExtensions.cs (what AddContentKbOrchestrator registers + what the host must register first)
    - DeckFlow.Core/Orchestration/ContentKbOrchestratorOptions.cs (the options record to register with the local artifactRoot)
    - DeckFlow.Core/Content/*Store.cs ctors (ContentSourceStore/ContentVideoStore/ContentSiteIndexStore/BlockedVideoStore/ContentHarvestRunStore/LlmSpendLedger/WhisperSpendLedger — their path/connection params; host-side construction per D-05/D-06)
    - DeckFlow.Core/Integration/ILlmDistillationService.cs, IYouTubeChannelVideoLister.cs, ITranscriptSource.cs, IFfmpegAudioChunker.cs + their concrete impls (which can be constructed without a secret/network — informs whether a named Studio null-object is needed)
    - DeckFlow.Core/Orchestration/ContentKbOrchestrator.cs (full ctor dependency list — register EVERY one, incl. ContentKbOrchestratorOptions)
  </read_first>
  <action>
    In `DeckFlow.Studio/Program.cs`, register EVERY orchestrator ctor dependency host-side (D-05/D-06) using REAL local SQLite-backed stores, register the options record, then call `builder.Services.AddContentKbOrchestrator()`. Resolve a local Studio SQLite db path (e.g. an `MTG_DATA_DIR`-based path or a Studio-local data dir) and a local artifactRoot — NOT the prod Postgres connection string (prod stays presence-only via StudioConfig). Register, in order, the 13 enumerated dependencies from the context interfaces block:
    - The seven stores (`IContentSourceStore`/`IContentVideoStore`/`IContentSiteIndexStore`/`IBlockedVideoStore`/`IContentHarvestRunStore`/`ILlmSpendLedger`/`IWhisperSpendLedger`) -> their concrete `*Store`/`*Ledger` types constructed from the local SQLite path exactly as the CLI builds them for the SQLite case.
    - `ILlmDistillationService`/`IYouTubeChannelVideoLister`/`ITranscriptSource`/`IFfmpegAudioChunker` -> the simplest real Core impl that constructs without a secret/network; if a concrete impl requires a provider key to CONSTRUCT, declare a small NAMED Studio null-object class implementing the interface (e.g. `NoopLlmDistillationService` in `DeckFlow.Studio/Services/`) whose members throw `NotSupportedException` (the smoke ListBlocked path never calls them) and register that — this is an explicit, named registration, NOT an anonymous throwing lambda, and the SUMMARY lists exactly which ones were null-objects and why.
    - `Func<DateTimeOffset>` -> `() => DateTimeOffset.UtcNow`.
    - `ContentKbOrchestratorOptions` -> `services.AddSingleton(new ContentKbOrchestratorOptions { ArtifactRoot = <local Studio data-dir path> })` (the typed options record carrying the host-resolved local artifactRoot — round-2 MEDIUM fix; the ctor consumes this, not a bare string).
    Then `builder.Services.AddContentKbOrchestrator();` and register `ContentKbOrchestratorSmokeService` (scoped). Keep the existing Serilog/UserSecrets/StudioConfig/Blazor wiring intact — touch only the lines needed to add the registrations.

    Optional runtime assertion: resolving the smoke service at startup and logging only a benign count (never a connection string) is acceptable but not required; container-resolvable + buildable is the SC4 bar.

    Confirm no `DeckFlow.CLI` ProjectReference is added to the Studio csproj.
  </action>
  <verify>
    <automated>"/mnt/c/Program Files/dotnet/dotnet.exe" build DeckFlow.sln -warnaserror 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "AddContentKbOrchestrator" DeckFlow.Studio/Program.cs` matches.
    - Every orchestrator ctor dependency has an explicit registration in Program.cs (the seven stores + the four integration services + the `Func<DateTimeOffset>` + the `ContentKbOrchestratorOptions` with the local artifactRoot); no dependency is left unregistered (build resolves the orchestrator — a missing registration would fail container build if a startup resolve is added, and at minimum the registrations are present by grep for each interface name).
    - The options record is registered with a local artifactRoot (`grep -n "ContentKbOrchestratorOptions" DeckFlow.Studio/Program.cs` matches) and NOT a bare `string` artifactRoot registration.
    - Any null-object substitute for an integration service is a NAMED class (e.g. `Noop*`) declared in Studio, not an anonymous throwing lambda (`grep -rn "Noop\|NullObject" DeckFlow.Studio/` if used; the SUMMARY enumerates which integration deps were null-objects and why).
    - `grep -n "ContentKbOrchestratorSmokeService" DeckFlow.Studio/Program.cs` matches (registered).
    - `grep -n "DeckFlow.CLI" DeckFlow.Studio/DeckFlow.Studio.csproj` returns nothing (SC4: Core-only reference).
    - No prod connection string is constructed or logged in Program.cs (the `Studio:ProdConnectionString` value remains presence-only via StudioConfig — the smoke stores use a local SQLite path).
    - Existing Serilog/UserSecrets/StudioConfig/AddRazorPages/AddServerSideBlazor wiring is unchanged (only additive lines).
    - `dotnet build DeckFlow.sln -warnaserror` = 0 errors / 0 warnings.
  </acceptance_criteria>
  <done>Studio composition root registers all orchestrator ctor deps with real local SQLite stores (named null-objects only where construction needs a secret), registers the ContentKbOrchestratorOptions (local artifactRoot), calls AddContentKbOrchestrator(), and resolves the smoke service via the maintenance slice — no CLI reference; solution builds clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Studio process -> local SQLite Content KB DB | The smoke service reads the local KB DB. No write, no network, no prod connection. Same trust level as the existing local CLI. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42-09 | Information Disclosure | prod connection string | mitigate | The smoke stores use LOCAL SQLite paths only; the prod Postgres connection string stays presence-only (StudioConfig) and is never constructed, logged, or surfaced — honoring STU-03 / public-repo no-secrets. The ContentKbOrchestratorOptions carries only a local artifactRoot path, never a connection string. Acceptance criteria forbid constructing/logging the prod string. |
| T-42-10 | Elevation of Privilege / accidental scope | Studio gaining a CLI dependency | mitigate | Acceptance criteria assert NO DeckFlow.CLI project reference and no DeckFlow.CLI namespace usage in Studio — the architectural goal (Studio depends on Core only) is grep-enforced (SC4). |
| T-42-11 | Tampering | smoke op performing a write | mitigate | The chosen smoke operation (ListBlockedAsync) is read-only (returns rows; no DB mutation); the integration-service null-objects throw on use so no accidental harvest/distill side effect can run. |
| T-42-14 | Availability / container build | orchestrator ctor fails to resolve | mitigate | HIGH-2 fix: every ctor dependency has an explicit real (or named null-object) registration, including the ContentKbOrchestratorOptions, so the full orchestrator ctor resolves — no implicit/anonymous throwing fake and no unsatisfiable bare-string param that would silently break container build. |
| T-42-SC | Tampering | npm/pip/cargo installs | n/a | No package installs — Studio already references Core; no new dependency. |
</threat_model>

<verification>
- `dotnet build DeckFlow.sln -warnaserror` = 0E/0W.
- `grep -rn "DeckFlow.CLI" DeckFlow.Studio/` returns nothing (SC4 hard gate).
- AddContentKbOrchestrator() + every orchestrator ctor dep (real local SQLite stores + named null-objects only where construction needs a secret) + the ContentKbOrchestratorOptions (local artifactRoot) + smoke service registered in Studio Program.cs.
- Smoke service injects the IContentMaintenanceOrchestrator SLICE (D-01 proof), not just the facade.
- No prod connection string constructed/logged in Studio.
</verification>

<success_criteria>
- Studio resolves + calls the IContentMaintenanceOrchestrator slice from a Blazor service (SC4 / D-01 / ORCH-02).
- Studio references Core only, no CLI reference (SC4).
- AddContentKbOrchestrator() wired with the full ctor dependency set (real local SQLite stores) + the ContentKbOrchestratorOptions (local artifactRoot) in the Studio composition root (D-08); orchestrator ctor resolves with no implicit throwing fakes and no bare-string param (HIGH-2 + round-2 MEDIUM closed).
- Solution builds clean.
</success_criteria>

<output>
Create `.planning/phases/42-orchestrator-extraction/42-04-SUMMARY.md` when done.
</output>
</content>
