---
phase: 88-index-row-integrity-hotfix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - DeckFlow.Core/Content/ContentSiteIndexStore.cs
  - DeckFlow.Core.Tests/Content/ContentSiteIndexStoreApprovalTests.cs
  - DeckFlow.Core.Tests/Content/ContentSiteIndexStoreSchemaEnsureSwitchTests.cs
  - DeckFlow.Web/e2e/content-kb-pending-hidden.spec.ts
autonomous: true
requirements: [SYNC-04, SYNC-06]

must_haves:
  truths:
    - "D-01: A DirectPush upsert writes the local row's approval_status on BOTH insert and update (approved today, since DirectPush reads only approved local rows)"
    - "D-02: A re-push of an approved row heals a drifted 'pending' prod row to 'approved'"
    - "D-04: The public KB serving query (GetPublishedRowsAsync) returns only rows with approval_status='approved'"
    - "D-09 / D-11: A prod-mode store constructed with schema-ensure disabled issues no CREATE/ALTER/DROP SQL on read or write, locked by a recording-connection regression test"
    - "D-03: No one-time SQL backfill of drifted prod rows ships in this phase — the serve filter neutralizes them until the Phase 91 reconciler"
    - "D-13 / D-14: Fixes ship unflagged; prod rollout is verified by operator pre-audit + post-deploy re-check via the read-only Render MCP query"
    - "D-15: xUnit regression tests + Playwright e2e (pending row never renders on public KB page) cover the phase's invariants"
  artifacts:
    - path: "DeckFlow.Core/Content/ContentSiteIndexStore.cs"
      provides: "Approval-mirrored batch upsert, approval-filtered publish read, schema-ensure OFF switch"
      contains: "ensureSchemaEnabled"
    - path: "DeckFlow.Core.Tests/Content/ContentSiteIndexStoreApprovalTests.cs"
      provides: "Mirror-on-insert-and-update + serve-filter-excludes-pending regression tests"
    - path: "DeckFlow.Core.Tests/Content/ContentSiteIndexStoreSchemaEnsureSwitchTests.cs"
      provides: "No-DDL invariant test for a schema-ensure-disabled prod-mode store"
  key_links:
    - from: "DeckFlow.Web/Controllers/ContentKbController.cs"
      to: "GetPublishedRowsAsync"
      via: "public /content-kb Index action"
      pattern: "GetPublishedRowsAsync"
    - from: "ProdStoreFactory (Plan 88-03)"
      to: "ContentSiteIndexStore ctor"
      via: "ensureSchemaEnabled parameter"
      pattern: "ensureSchemaEnabled"
---

<objective>
Close two of the three live prod correctness bugs at the store layer: (1) DirectPush's prod upsert must mirror the local row's `approval_status` on insert AND update so a row is never publicly visible while `pending` (SYNC-04), plus a serve-side defense-in-depth filter so a drifted row is never rendered; (2) the prod-pointed store must never run DDL, via a schema-ensure OFF switch and a regression test that locks the invariant (SYNC-06 store layer).

Purpose: The C1 bug (visible-while-pending rows) and the C4 DDL-against-prod bug both live in `ContentSiteIndexStore`. This plan owns that file end to end.
Output: Approval-mirrored batch upsert, approval-filtered publish read, a `ensureSchemaEnabled` ctor switch that no-ops `EnsureSchemaAsync`, and three test surfaces (xUnit approval, xUnit no-DDL, Playwright serve-smoke).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/88-index-row-integrity-hotfix/88-CONTEXT.md
@.planning/phases/88-index-row-integrity-hotfix/88-PATTERNS.md
@CLAUDE.md

<interfaces>
<!-- Executor uses these directly. Confirm current state via read_first before editing. -->

ContentSiteIndexRow (DeckFlow.Core/Knowledge/ContentArtifactSpec.cs:107) — sealed record.
Relevant members:
  - string ApprovalStatus { get; init; } = "pending"
  - string? YoutubeVideoId { get; init; }
  - string? RssGuid { get; init; }
  - string? PinId => YoutubeVideoId ?? RssGuid   (computed; NO separate NaturalKeyType/Value fields)

ContentSiteIndexStore (DeckFlow.Core/Content/ContentSiteIndexStore.cs):
  - const string UpsertContentColumnsOnlySql (lines ~955-993): the DirectPush batch-upsert SQL. INSERT clause hardcodes 'pending'; ON CONFLICT (natural_key_type, natural_key_value) DO UPDATE SET omits approval_status; trailing comment claims approval_status is "intentionally absent".
  - UpsertContentColumnsOnlyBatchAsync (line ~571) — the ONLY production caller of that SQL (DirectPush).
  - BuildUpsertParameters(ContentSiteIndexRow, (string Type, string Value) naturalKey) (lines ~713-728) — Dapper DynamicParameters binder; already binds naturalKeyType/naturalKeyValue.
  - GetPublishedRowsAsync (line ~221): serve query, currently WHERE is_visible = @visible only.
  - GetApprovedRowsAsync (line ~255): sibling with WHERE approval_status = 'approved' (copy this literal exactly).
  - EnsureSchemaAsync (line ~44): runs CREATE TABLE / ALTER; called at the top of ~20 public methods.
  - ctors (lines ~13-42): ContentSiteIndexStore(string databasePath) and ContentSiteIndexStore(RelationalDatabaseConnection connectionInfo).

Vocabulary constants: ContentSourceType.Youtube = "youtube_channel", ContentSourceType.Podcast = "podcast_rss" (DeckFlow.Core/Knowledge/ContentModels.cs:180-187).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Mirror approval_status on the DirectPush batch upsert (D-01/D-02)</name>
  <files>DeckFlow.Core/Content/ContentSiteIndexStore.cs, DeckFlow.Core.Tests/Content/ContentSiteIndexStoreApprovalTests.cs</files>
  <read_first>
    - DeckFlow.Core/Content/ContentSiteIndexStore.cs — read UpsertContentColumnsOnlySql (~955-993), BuildUpsertParameters (~713-728), UpsertContentColumnsOnlyBatchAsync (~571), and GetNaturalKey (~790) to see current binding style
    - DeckFlow.Core/Knowledge/ContentArtifactSpec.cs — ContentSiteIndexRow record (ApprovalStatus default "pending")
    - DeckFlow.Core.Tests/Content/ContentSiteIndexStoreApprovalTests.cs — existing ctor/dispose fixture (temp SQLite file) and approval-assertion style to extend
    - DeckFlow.Core.Tests/Content/ContentSiteIndexStoreBatchUpsertTests.cs — the "construct store, run one write, assert on GetAllRowsAsync()" idiom
  </read_first>
  <behavior>
    - Insert case: upserting a NEW row whose ApprovalStatus is "approved" persists approval_status='approved' (not 'pending').
    - Update case: an existing prod row at approval_status='pending' + is_visible=true, re-upserted from a row with ApprovalStatus="approved", ends at approval_status='approved' (heals the drift).
    - Operator-owned columns is_visible, is_hidden, is_evergreen are NOT changed by the upsert (assert an existing is_visible value survives a content-columns-only re-upsert).
    - A row with ApprovalStatus="pending" upserted persists approval_status='pending' (the value is mirrored, not hardcoded to 'approved').
  </behavior>
  <action>
    In UpsertContentColumnsOnlySql: replace the literal `'pending'` in the INSERT VALUES list with a bound `@approvalStatus` parameter, and add `approval_status = EXCLUDED.approval_status` to the `ON CONFLICT (natural_key_type, natural_key_value) DO UPDATE SET` column list. Leave is_visible, is_hidden, is_evergreen absent from the update set (operator-owned, per the DirectPush design SC3). In BuildUpsertParameters, add one binding `parameters.Add("approvalStatus", row.ApprovalStatus);` alongside the existing naturalKeyType/naturalKeyValue adds — mirroring the local row's value, not a constant (D-01 forbids a hidden 'approved' literal). Rewrite the trailing SQL comment: it currently states approval_status is "intentionally absent"; make it state that approval_status is now mirrored from the source row on insert and update, while is_visible/is_hidden/is_evergreen remain operator-owned and excluded (D-12 comment-truth requirement). Scope is UpsertContentColumnsOnlySql only — do NOT touch UpsertSql or UpsertPreservingVisibilitySql (different callers, out of scope). Add the four behavior cases as [Fact] tests in ContentSiteIndexStoreApprovalTests following the existing temp-SQLite fixture convention.
  </action>
  <verify>
    <automated>/mnt/c/Program\ Files/dotnet/dotnet.exe test DeckFlow.Core.Tests --filter "FullyQualifiedName~ContentSiteIndexStoreApprovalTests"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "@approvalStatus" DeckFlow.Core/Content/ContentSiteIndexStore.cs` shows the parameter in the INSERT VALUES list of UpsertContentColumnsOnlySql
    - `grep -n "approval_status = EXCLUDED.approval_status" DeckFlow.Core/Content/ContentSiteIndexStore.cs` returns a hit inside the ON CONFLICT DO UPDATE SET of UpsertContentColumnsOnlySql
    - `grep -n "'pending'" DeckFlow.Core/Content/ContentSiteIndexStore.cs` no longer matches inside UpsertContentColumnsOnlySql
    - No `is_visible`, `is_hidden`, or `is_evergreen` appears in the UpsertContentColumnsOnlySql DO UPDATE SET list
    - The trailing comment no longer asserts approval_status is "intentionally absent"
    - The four behavior-case facts pass under `dotnet test`
  </acceptance_criteria>
  <done>DirectPush's batch upsert mirrors the source row's approval_status on insert and update; operator-owned visibility columns stay excluded; tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Serve-side approval filter on GetPublishedRowsAsync (D-04)</name>
  <files>DeckFlow.Core/Content/ContentSiteIndexStore.cs, DeckFlow.Core.Tests/Content/ContentSiteIndexStoreApprovalTests.cs, DeckFlow.Web/e2e/content-kb-pending-hidden.spec.ts</files>
  <read_first>
    - DeckFlow.Core/Content/ContentSiteIndexStore.cs — GetPublishedRowsAsync (~221, current `WHERE is_visible = @visible`) and GetApprovedRowsAsync (~255, `WHERE approval_status = 'approved'`) for the exact literal to copy
    - DeckFlow.Web/Controllers/ContentKbController.cs — Index action (line ~52) calling GetPublishedRowsAsync; confirms the filter belongs at the query, not the controller
    - DeckFlow.Web/e2e/smoke.spec.ts and DeckFlow.Web/playwright.config.ts — existing Playwright spec + server-launch convention
    - CLAUDE.md — UI-testing constraints (start server via scripts/run-web-test.sh; DECKFLOW_DISABLE_AUTO_BROWSER; never open a Windows-host browser; npx --no-install playwright test)
  </read_first>
  <behavior>
    - GetPublishedRowsAsync returns a row that is is_visible=true AND approval_status='approved'.
    - GetPublishedRowsAsync does NOT return a row that is is_visible=true but approval_status='pending' (the drifted-row case).
    - GetPublishedRowsAsync does NOT return an approval_status='approved' row that is is_visible=false.
  </behavior>
  <action>
    Change GetPublishedRowsAsync's WHERE clause from `WHERE is_visible = @visible` to `WHERE is_visible = @visible AND approval_status = 'approved'`, copying the `approval_status = 'approved'` literal exactly as it appears in GetApprovedRowsAsync. This is the sanctioned public-surface change per REQUIREMENTS out-of-scope note (the SYNC-04 visibility fix) and it immediately stops serving today's drifted visible-while-pending rows, covering the backfill deferred to Phase 91 (D-03/D-04). Do NOT modify ContentKbController — the filter lives in the store query. Add xUnit facts for the three behavior cases in ContentSiteIndexStoreApprovalTests (seed rows via the batch upsert or the existing seed helpers, flip visibility/approval with the store's SetVisibilityAsync/SetApprovalStatusAsync, assert on GetPublishedRowsAsync). Add a Playwright spec content-kb-pending-hidden.spec.ts that loads GET /content-kb and asserts the page renders without server error and lists approved content — a regression smoke that the filter change did not break the public browse page (the deterministic pending-exclusion proof is the xUnit test; the e2e is the house web-page-change guard per CLAUDE.md and D-15).
  </action>
  <verify>
    <automated>/mnt/c/Program\ Files/dotnet/dotnet.exe test DeckFlow.Core.Tests --filter "FullyQualifiedName~ContentSiteIndexStoreApprovalTests"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "approval_status = 'approved'" DeckFlow.Core/Content/ContentSiteIndexStore.cs` returns a hit inside GetPublishedRowsAsync's SQL (in addition to the existing one in GetApprovedRowsAsync)
    - `grep -n "GetPublishedRowsAsync" DeckFlow.Web/Controllers/ContentKbController.cs` still shows the single existing call, and ContentKbController has no new approval filtering logic (filter is in the store)
    - The three GetPublishedRowsAsync behavior facts pass, including the visible-but-pending exclusion case
    - DeckFlow.Web/e2e/content-kb-pending-hidden.spec.ts exists and asserts GET /content-kb returns 200 and renders the browse list
  </acceptance_criteria>
  <done>The public serve query returns only approved+visible rows; drifted visible-while-pending rows can no longer render; xUnit + e2e cover it.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Schema-ensure OFF switch + no-DDL regression test (D-09/D-11)</name>
  <files>DeckFlow.Core/Content/ContentSiteIndexStore.cs, DeckFlow.Core.Tests/Content/ContentSiteIndexStoreSchemaEnsureSwitchTests.cs</files>
  <read_first>
    - DeckFlow.Core/Content/ContentSiteIndexStore.cs — ctors (~13-42), the `_schemaReady`/`_schemaGate` fields, EnsureSchemaAsync (~44), and any of the ~20 `await EnsureSchemaAsync(...)` call sites (e.g. GetAllRowsAsync, UpsertContentColumnsOnlyBatchAsync) so the switch is added inside EnsureSchemaAsync, not at call sites
    - DeckFlow.Core.Tests/Content/ContentSiteIndexStoreBatchUpsertTests.cs — the temp-SQLite-file ctor + write-path idiom to reuse
    - .planning/phases/88-index-row-integrity-hotfix/88-PATTERNS.md — "No Analog Found" note on the D-11 recording test (prefer the schema-less-SQLite approach over building new recording infra)
  </read_first>
  <behavior>
    - A store built with the schema-ensure switch ON (default) auto-creates its schema exactly as today (existing behavior unchanged — all current tests still pass).
    - A store built with the switch OFF, pointed at a fresh SQLite file that has NO content_site_index table, throws a "no such table" / schema-missing error when a read (GetAllRowsAsync) or write (UpsertContentColumnsOnlyBatchAsync) is attempted — proving it issued no CREATE TABLE.
    - No existing call site of EnsureSchemaAsync needs editing (the ~20 `await EnsureSchemaAsync(...)` lines are untouched).
  </behavior>
  <action>
    Add a third ctor parameter `bool ensureSchemaEnabled = true` to the RelationalDatabaseConnection-taking constructor (the string-path ctor forwards true), stored in a new readonly field `_ensureSchemaEnabled`. Defaulting to true preserves every existing call site with zero edits. In EnsureSchemaAsync, add an early `if (!_ensureSchemaEnabled) { return; }` as the very first statement — before the `_schemaReady` fast-path — so a prod-mode store never issues CREATE TABLE / ALTER TABLE and the ~20 auto-ensure call sites stay untouched (D-09: one mechanism covers all sites). This is the store-side half of SYNC-06; Plan 88-03 flips ProdStoreFactory to pass `ensureSchemaEnabled: false`. Add ContentSiteIndexStoreSchemaEnsureSwitchTests: for the OFF case, construct the store against a brand-new temp SQLite path with ensureSchemaEnabled:false and assert that GetAllRowsAsync (read) and the batch upsert (write) each throw because the table was never created (prefer this zero-new-infra approach from PATTERNS over a DbCommand recording wrapper). Follow the temp-file ctor/dispose fixture convention.
  </action>
  <verify>
    <automated>/mnt/c/Program\ Files/dotnet/dotnet.exe test DeckFlow.Core.Tests --filter "FullyQualifiedName~ContentSiteIndexStoreSchemaEnsureSwitchTests"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "ensureSchemaEnabled" DeckFlow.Core/Content/ContentSiteIndexStore.cs` shows the ctor parameter, the `_ensureSchemaEnabled` field, and the early return in EnsureSchemaAsync
    - The early return is the first statement of EnsureSchemaAsync (before the `_schemaReady` check)
    - `git diff` shows NO edits to any `await EnsureSchemaAsync(` call site
    - OFF-case test: read and write against a schema-less SQLite file both throw (no auto-create); ON-case default-ctor tests still pass
    - Full `dotnet test DeckFlow.Core.Tests` build succeeds with no new warnings
  </acceptance_criteria>
  <done>The store has a one-switch schema-ensure OFF mode proven to issue no DDL; default behavior is byte-for-byte unchanged; test locks the invariant.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public browser → web app → prod-mirrored index | Untrusted readers see whatever GetPublishedRowsAsync returns; a mislabeled row leaks unapproved content |
| Studio (operator workstation) → prod Postgres (write) | DirectPush upsert crosses into prod; wrong column values or unintended DDL mutate the live DB |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-88-01 | Information Disclosure | GetPublishedRowsAsync + UpsertContentColumnsOnlySql | mitigate | Mirror approval_status on insert/update (Task 1) AND filter serve query on approval_status='approved' (Task 2) — defense in depth so a writer bug can never render a pending row |
| T-88-02 | Tampering | ContentSiteIndexStore.EnsureSchemaAsync against prod | mitigate | Schema-ensure OFF switch (Task 3) so a prod-pointed store issues no CREATE/ALTER/DROP; no-DDL regression test locks it (D-11) |
| T-88-SC | Tampering (supply chain) | npm/NuGet installs | accept | No new packages added in this plan; test + e2e reuse existing xUnit + Playwright deps (CLAUDE.md forbids unapproved deps) |
</threat_model>

<verification>
- `dotnet test DeckFlow.Core.Tests` passes (approval mirror, serve filter, schema-ensure switch).
- Full solution `dotnet build` clean, no new warnings.
- `bash scripts/format-check-changed.sh` passes — changed-lines format gate (CLAUDE.md; run against the staged/changed C# lines before commit).
- Playwright: `npx --no-install playwright test content-kb-pending-hidden` green against the headless server started via scripts/run-web-test.sh.
- Operator prod verification (D-14, manual, at merge/deploy time — NOT an automated gate): before merge, run the sanctioned read-only Render MCP query (`mcp__render__query_render_postgres`, id `dpg-d7oj8iugvqtc73fso0g0-a`) listing count + titles of rows where is_visible=true AND approval_status<>'approved', so the operator approves exactly what the D-04 serve filter will hide. After autodeploy, re-run the query and spot-check the public /content-kb page.
</verification>

<success_criteria>
- DirectPush's prod upsert carries the source row's approval_status on insert and update; is_visible/is_hidden/is_evergreen stay operator-owned (SYNC-04).
- The public KB serve query returns only approved+visible rows (SYNC-04 defense in depth).
- A prod-mode store issues no DDL; the invariant is test-locked (SYNC-06 store layer).
</success_criteria>

<output>
Create `.planning/phases/88-index-row-integrity-hotfix/88-01-SUMMARY.md` when done.
</output>
