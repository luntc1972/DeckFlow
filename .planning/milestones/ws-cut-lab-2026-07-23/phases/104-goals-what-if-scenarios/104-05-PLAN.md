---
phase: 104-goals-what-if-scenarios
plan: 05
type: execute
wave: 4
depends_on: [104-02, 104-03, 104-04]
files_modified:
  - DeckFlow.Web/Models/CutLabViewModel.cs
  - DeckFlow.Web/Views/Deck/CutLab.cshtml
  - DeckFlow.Web/wwwroot/ts/cut-lab.ts
  - DeckFlow.Web/Controllers/CutLabController.cs
  - DeckFlow.Web/wwwroot/css/site-common.css
  - DeckFlow.Web.Tests/CutLabControllerTests.cs
  - DeckFlow.Web/ts-tests/cut-lab-whatif.test.ts
autonomous: true
requirements: [GOAL-03]

must_haves:
  truths:
    - "User picks card A from the working list (locked cards excluded) and card B from the cut pile, previews the swap deltas, then Keep or Discard"
    - "Preview shows before/after metric deltas without changing anything; Discard clears the preview and leaves the session untouched"
    - "Keep atomically commits the swap (A cut, B restored under 'What-if swap') and the working list + metrics update"
    - "A no-JS /cut-lab/whatif form fallback previews and commits the swap via full-page re-render, with the preview issuing ZERO Scryfall resolves (reuses 104-03's resolved-card pre-seed, not a raw BuildSnapshot)"
  artifacts:
    - path: "DeckFlow.Web/Views/Deck/CutLab.cshtml"
      provides: "What-if swap section: A/B pickers, Preview/Keep/Discard controls, no-JS form"
      contains: "cut-lab/whatif"
    - path: "DeckFlow.Web/Controllers/CutLabController.cs"
      provides: "Whatif no-JS MVC action (preview + commit, full re-render)"
      exports: ["Whatif"]
  key_links:
    - from: "DeckFlow.Web/wwwroot/ts/cut-lab.ts"
      to: "/api/cut-lab/whatif and /api/cut-lab/whatif/commit"
      via: "fetch preview (render deltas) and commit (write state, patch UI)"
      pattern: "/api/cut-lab/whatif"
    - from: "DeckFlow.Web/Controllers/CutLabController.cs"
      to: "CutLabApiController swap logic (shared helpers) / CutLabDecisionApplier composition"
      via: "no-JS full-page preview + commit"
      pattern: "whatif-swap"
---

<objective>
Deliver the what-if swap UI (GOAL-03 front end): A/B pickers, an instant preview of metric deltas, and Keep/Discard — plus the no-JS `/cut-lab/whatif` form fallback that previews and commits via full-page re-render. Preview is non-destructive; Keep calls the atomic commit endpoint from 104-03.

Purpose: Make the previewable, committable swap engine usable by a builder, both with and without JavaScript.
Output: swap section in CutLab.cshtml, preview/commit fetch handlers in cut-lab.ts, the `Whatif` MVC action, and a view-model field to render no-JS deltas.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/cut-lab/phases/104-goals-what-if-scenarios/104-CONTEXT.md
@.planning/workstreams/cut-lab/phases/104-goals-what-if-scenarios/104-RESEARCH.md
@.planning/workstreams/cut-lab/phases/104-goals-what-if-scenarios/104-PATTERNS.md
@.planning/workstreams/cut-lab/phases/104-goals-what-if-scenarios/104-03-SUMMARY.md
@.planning/workstreams/cut-lab/phases/104-goals-what-if-scenarios/104-04-SUMMARY.md

<interfaces>
API (from 104-03): POST /api/cut-lab/whatif { cutLabStateJson, cardOut, cardIn } -> { deltas: CutLabDecideMetricDeltaDto[], changedFamilyCount, cardOut, cardIn, cutLabStateJson: null }. POST /api/cut-lab/whatif/commit { same } -> { ..., cutLabStateJson: <new state> }. Same-origin + RequestVerificationToken header + [RequestSizeLimit] as decide.
Swap candidate sources (server-side, for the pickers): working list = CutLabWorkingList.Derive(pool, decisions) excluding IsLocked/IsCommander for card A; cut pile = state.Pool where CutLabWorkingList.AcceptedCardNames(decisions) contains name for card B.
cut-lab.ts fetch pattern to mirror: handleDecisionSubmit 1086-1120 (AbortController + timeout cutLabDecisionTimeoutMs, RequestVerificationToken header, response.ok guard, writeDecisionStateToHiddenInputs on success). Endpoint constant precedent: cutLabDecisionApiEndpoint = '/api/cut-lab/decide' (line 130). Delta-render precedent: renderProposalCard / the proposal deltas rendering.
No-JS MVC precedent: CutLabController.Decide 64-101 (deserialize -> apply -> Serialize -> ProcessAsync -> View("CutLab", ...); three-tier catch). CutLabController already has RehydrateIntakeRequestFromState + DetermineRoundKey helpers.
View model: add a whatif-preview field so the no-JS action can render deltas server-side (owned here; CutLabViewModel already carries CompareRows for the delta-row markup shape).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: What-if swap section (Razor) + no-JS Whatif MVC action + view-model preview field</name>
  <read_first>
    - DeckFlow.Web/Controllers/CutLabController.cs (Decide 64-101; RehydrateIntakeRequestFromState 113-130; DetermineRoundKey 103-111; CutLabView helper 183-190)
    - DeckFlow.Web/Views/Deck/CutLab.cshtml (the no-JS decide form root line 24 `<form method="post" action="~/cut-lab/decide">`; the Compare section delta-row markup; the Goals section from 104-02 and Scenarios panel from 104-04 for placement)
    - DeckFlow.Web/Models/CutLabViewModel.cs (From 120-194; CutLabCompareRowView shape; the whatif no-JS deltas need a field here)
    - DeckFlow.Web/Services/CutLab/CutLabWhatifPreviewService.cs (from 104-03 — `ICutLabWhatifPreviewService.ComputeSwapPreviewAsync(CutLabState, cardOut, cardIn, ct)`: the ONE concrete, DI-registered, side-effect-free, zero-Scryfall swap-preview both controllers call. The no-JS MVC preview INJECTS and CALLS this; it does NOT reimplement the seeding/BuildSnapshot math and does NOT redefine the helper)
    - DeckFlow.Web/Controllers/Api/CutLabApiController.cs (from 104-03 — PostWhatifAsync delegates to the same service; PostWhatifCommitAsync + Restore+Accept composition to mirror for the no-JS keep path)
    - DeckFlow.Web.Tests/CutLabApiControllerTests.cs / CutLabWhatifTests.cs (104-03's resolver-stub-throws preview test — mirror it for the MVC preview path)
    - DeckFlow.Web/Models/CutLabRequest.cs (form fields; CutLabStateJson)
    - DeckFlow.Web.Tests/CutLabControllerTests.cs (existing MVC-controller test conventions incl. the sibling Decide-action test — mirror its no-JS deserialize/apply/re-render assertions)
  </read_first>
  <action>
    Add a whatif-preview field to `CutLabViewModel`: `sealed record CutLabWhatifPreviewView { string CardOut; string CardIn; IReadOnlyList<CutLabCompareRowView> DeltaRows; bool HasPreview; }` and `public CutLabWhatifPreviewView Whatif { get; init; } = new();`. In `From()`, populate it from a new optional field on `CutLabProcessResult` (or pass-through) only when the no-JS action supplied a preview; default `HasPreview=false`.
    Add a `[HttpPost("/cut-lab/whatif")]` `Whatif(CutLabRequest request, string cardOut, string cardIn, string intent)` action to `CutLabController`, flag-gated + `[ValidateAntiForgeryToken]` + `[RequestSizeLimit(2*1024*1024)]`, following the `Decide` structure and three-tier catch. `intent` is "preview" or "keep". Deserialize state; validate A(working, unlocked) + B(cut pile) via the same `CutLabWorkingList` derivations the API uses. For "preview": inject `ICutLabWhatifPreviewService` into `CutLabController` (constructor dependency) and call `_whatifPreviewService.ComputeSwapPreviewAsync(state, cardOut, cardIn, HttpContext.RequestAborted)` to get the side-effect-free deltas, then re-render `View("CutLab", ...)` with `Whatif.HasPreview=true` and the delta rows — do NOT mutate state. Do NOT hand-roll a `BuildSnapshot(poolKey: null)` path here: a raw call resolves every card via `_resolver.ResolveSingleAsync` on a cache miss (`CutLabSimulationService.cs:221-227`) and reintroduces the HIGH-5 Scryfall-on-preview bug. This plan only WIRES the MVC controller to the shared 104-03 service; it does not redefine the preview math. For "keep": re-check the pool card for `cardOut` is not null and not `IsLocked` and return the NoChange re-render if it is; then compose `CutLabDecisionApplier.Apply(state, cardIn, Restore, CutLabCutRoundEngine.WhatifSwapKey)` then `Apply(afterRestore, cardOut, Accept, CutLabCutRoundEngine.WhatifSwapKey)`, `Serialize`, `RehydrateIntakeRequestFromState`, `ProcessAsync`, and re-render. Pass `CutLabCutRoundEngine.WhatifSwapKey` (NOT `""`) to the Restore call because `Apply` throws on a blank roundKey before the Restore branch (BLOCK 1); Restore ignores the key value. Invalid A/B -> re-render with `CutLabMessages.NoChangeMessage`.
    Add the What-if swap `<section class="result-panel">` to CutLab.cshtml with a `<form method="post" action="@Url.Content("~/cut-lab/whatif")">` containing the antiforgery token, a `<select name="cardOut">` populated from the working list (excluding locked/commander cards, Pitfall 4), a `<select name="cardIn">` populated from the cut pile, and two submit buttons ("Preview swap" name=intent value=preview, "Keep swap" name=intent value=keep). When `Model.Whatif.HasPreview`, render the delta rows (reuse the Compare delta-row markup) plus the A/B names above the Keep button. This IS the no-JS fallback (D-05).
    Add xUnit cases to `DeckFlow.Web.Tests/CutLabControllerTests.cs` for the `Whatif` action: (a) intent=preview returns a model with `Whatif.HasPreview==true` and delta rows AND the persisted `CutLabStateJson` is unchanged (zero new Decisions); (b) intent=keep commits both decisions (A cut under the "What-if swap" round label, B restored to the working list); (c) a locked card A returns the NoChange re-render and leaves state unchanged.
  </action>
  <verify>
    <automated>dotnet test DeckFlow.Web.Tests --filter "FullyQualifiedName~CutLabControllerTests"</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet build` clean.
    - `grep -n "cut-lab/whatif" DeckFlow.Web/Views/Deck/CutLab.cshtml` shows the no-JS form; `grep -n "public.*Whatif(" DeckFlow.Web/Controllers/CutLabController.cs` shows the action.
    - The cardOut `<select>` excludes locked/commander cards (verify in the Razor loop filter).
    - `CutLabControllerTests` Whatif cases pass: preview leaves persisted state unchanged; keep commits both decisions under "What-if swap"; locked-A leaves state unchanged.
    - A `CutLabControllerTests` MVC-preview case runs with a resolver stub that THROWS if `ResolveSingleAsync` is called and still renders deltas (proving zero Scryfall resolves on the no-JS preview path), mirroring 104-03's preview test.
  </acceptance_criteria>
  <done>A fully server-rendered what-if swap (pick A/B, Preview shows deltas, Keep commits) works without JavaScript.</done>
</task>

<task type="auto">
  <name>Task 2: JS preview/commit + Keep/Discard enhancement</name>
  <read_first>
    - DeckFlow.Web/wwwroot/ts/cut-lab.ts (handleDecisionSubmit 1086-1120; cutLabDecisionApiEndpoint 130; getAntiForgeryToken 542+; writeDecisionStateToHiddenInputs / patchStickyBar / renderCutsMade post-success block ~1111-1120; setDecisionButtonsBusy busy-state helper; the proposal-delta render helpers)
    - DeckFlow.Web/Views/Deck/CutLab.cshtml (the whatif section from Task 1 — the A/B selects, Preview/Keep/Discard buttons, delta container)
    - DeckFlow.Web/wwwroot/css/site-common.css (delta/list layout tokens)
    - DeckFlow.Web/ts-tests/cut-lab-proposal.test.ts (existing Vitest coverage of the sibling handleDecisionSubmit flow — mirror its jsdom setup and spy conventions for the new whatif handlers)
  </read_first>
  <action>
    In cut-lab.ts, add `cutLabWhatifApiEndpoint = '/api/cut-lab/whatif'` and `cutLabWhatifCommitApiEndpoint = '/api/cut-lab/whatif/commit'`. Add `handleWhatifPreview` and `handleWhatifKeep` following the exact fetch shape of `handleDecisionSubmit` (AbortController + `cutLabDecisionTimeoutMs`, JSON `Content-Type` + `RequestVerificationToken` header, `response.ok` guard, busy-state on the clicked button). Preview reads the A/B `<select>` values + the current `CutLabStateJson` (call `writeStateToHiddenInput()` first), POSTs to the preview endpoint, and renders the returned `deltas` into the whatif delta container — WITHOUT calling `writeDecisionStateToHiddenInputs` (state must not change on preview). Show a "Keep" and "Discard" control once a preview exists; Discard clears the rendered deltas and hides the Keep control (pure client-side, no request). Keep POSTs to the commit endpoint, then on success calls `writeDecisionStateToHiddenInputs(data.cutLabStateJson)` and refreshes the cuts-made/sticky UI exactly as the decision-submit success path does. Intercept the no-JS form's submit (Task 1 form) to route through these handlers when JS is present (mirror how the decision forms are progressively enhanced), so the same markup serves both paths.
    Populate/refresh the A/B select options client-side if needed after a Keep (B moved to working list, A moved to cut pile) so a subsequent swap uses current candidates.
    Add any whatif-preview layout CSS to `site-common.css` only.
    Add `DeckFlow.Web/ts-tests/cut-lab-whatif.test.ts` (jsdom) with a spy/stub on `writeDecisionStateToHiddenInputs` (or the exported seam it uses) asserting the safety-critical invariant automatically: `handleWhatifPreview` NEVER calls `writeDecisionStateToHiddenInputs` (state must not mutate on preview), while `handleWhatifKeep` DOES call it on a successful commit response. Stub `fetch` to return a fixed preview response (deltas, cutLabStateJson:null) and a fixed commit response (cutLabStateJson set), mirroring the fetch-stub pattern in cut-lab-proposal.test.ts.
  </action>
  <verify>
    <automated>cd DeckFlow.Web && npx vitest run ts-tests/cut-lab-whatif && npx tsc -p tsconfig.json --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `tsc --noEmit` exits 0.
    - `grep -n "/api/cut-lab/whatif" DeckFlow.Web/wwwroot/ts/cut-lab.ts` returns both the preview and commit endpoints.
    - Vitest `cut-lab-whatif.test.ts` passes and asserts automatically that `handleWhatifPreview` does NOT call `writeDecisionStateToHiddenInputs` and `handleWhatifKeep` does.
    - No layout rules added to site.css.
  </acceptance_criteria>
  <done>With JS, a builder previews swap deltas instantly and Keeps or Discards without a full-page reload; the no-JS form still works.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser -> /cut-lab/whatif (form) and /api/cut-lab/whatif(+commit) | Untrusted A/B card names + state |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-104-11 | Tampering | No-JS Whatif MVC action | mitigate | `[ValidateAntiForgeryToken]` + flag gate + `[RequestSizeLimit]`; re-validate A(working, unlocked) + B(cut pile) server-side; preview never mutates state; locked-A keep -> NoChange re-render. |
| T-104-12 | Spoofing | JS fetch to the API endpoints | mitigate | Same-origin + RequestVerificationToken already enforced by the 104-03 endpoints; client sends the header exactly as the decision flow does. |
| T-104-13 | Elevation | Client offers a locked/commander card as A | mitigate | cardOut `<select>` excludes locked/commander cards; server re-validates (defense in depth, Pitfall 4). |
| T-104-SC | Tampering | package installs | accept | No package installs in this plan; legitimacy gate N/A. |
</threat_model>

<verification>
- `dotnet build` + `tsc --noEmit` clean.
- Grep gates for the no-JS form, the MVC action, and both API endpoints pass; site.css untouched.
- Full pick/preview/keep/discard + no-JS flow is exercised by the 104-06 e2e spec.
</verification>

<success_criteria>
A builder can swap a working-list card for a cut-pile card, see all goal + consistency deltas instantly, and Keep or Discard — with a working no-JS fallback and no state mutation until Keep.
</success_criteria>

<output>
Create `.planning/workstreams/cut-lab/phases/104-goals-what-if-scenarios/104-05-SUMMARY.md` when done.
</output>
