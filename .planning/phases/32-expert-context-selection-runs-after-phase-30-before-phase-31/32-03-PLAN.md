---
phase: 32-expert-context-selection
plan: 03
type: execute
wave: 3
depends_on: ["32-02"]
files_modified:
  - DeckFlow.Web/Models/ContentKbBrowseViewModel.cs
  - DeckFlow.Web/Controllers/ContentKbController.cs
  - DeckFlow.Web/Models/DeckAnalysisViewModel.cs
  - DeckFlow.Web/Controllers/DeckController.cs
  - DeckFlow.Web/Controllers/Api/ContentKbSearchApiController.cs
  - DeckFlow.Web/Views/ContentKb/Index.cshtml
  - DeckFlow.Web/Views/Deck/DeckAnalysis.cshtml
  - DeckFlow.Web/wwwroot/ts/kb-selection.ts
  - DeckFlow.Web/wwwroot/css/site-common.css
autonomous: true
requirements: [SEL-01, SEL-03]
must_haves:
  truths:
    - "Each /content-kb browse card shows a Pin and a Follow button"
    - "A selection tray below the filter bar shows current pins (N/3) and follows, hidden when empty"
    - "Pins persist in localStorage deckflow.kb.pinned (max 3); follows in deckflow.kb.followed (sticky)"
    - "Pin cap of 3 disables further Pin buttons"
    - "The /deck-analysis form shows an editable Expert Context chip area with object-aware typeahead"
    - "The entries typeahead returns {id,title} objects; picking one pins by id (YoutubeVideoId ?? RssGuid), not by title text"
    - "The analysis form submits PinnedVideoIds/FollowedCreators as hidden fields and works with JS disabled (empty selection)"
    - "Pinned localStorage clears only AFTER a successful analysis (server-rendered marker on result load), not on submit; follows remain"
    - "Replayed pins/follows render as server-side chips and re-hydrate localStorage"
    - "DeckController maps result.ResolvedPinTitles onto DeckAnalysisViewModel.ResolvedPinTitles on the upload/replay path (chips show real titles, not ids)"
  artifacts:
    - path: "DeckFlow.Web/wwwroot/ts/kb-selection.ts"
      provides: "localStorage + chips + tray + pin/follow toggles + self-contained object typeahead + post-success pin clear"
      contains: "deckflow.kb.pinned"
    - path: "DeckFlow.Web/Controllers/Api/ContentKbSearchApiController.cs"
      provides: "/api/content-kb/entries (returns {id,title}) + /api/content-kb/creators typeahead endpoints"
      contains: "content-kb"
    - path: "DeckFlow.Web/Views/ContentKb/Index.cshtml"
      provides: "Pin/Follow buttons + selection tray"
      contains: "data-kb-pin"
    - path: "DeckFlow.Web/Views/Deck/DeckAnalysis.cshtml"
      provides: "Expert Context chip area + hidden fields + typeahead + clear-pins marker when AnalysisPromptText present"
      contains: "kb-chip-area"
    - path: "DeckFlow.Web/Controllers/DeckController.cs"
      provides: "ResolvedPinTitles = result.ResolvedPinTitles on the DeckAnalysis upload/replay VM"
      contains: "ResolvedPinTitles"
    - path: "DeckFlow.Web/wwwroot/css/site-common.css"
      provides: "all new kb-* layout classes incl. origin-marker badges for Plan 04"
      contains: "kb-card-actions"
  key_links:
    - from: "DeckFlow.Web/Controllers/DeckController.cs"
      to: "DeckAnalysisViewModel.ResolvedPinTitles"
      via: "ResolvedPinTitles = result.ResolvedPinTitles on the upload/replay VM"
      pattern: "ResolvedPinTitles = result.ResolvedPinTitles"
    - from: "DeckFlow.Web/wwwroot/ts/kb-selection.ts"
      to: "/api/content-kb/entries"
      via: "self-contained fetch returning {id,title}; pick pins by id"
      pattern: "content-kb/entries"
    - from: "DeckFlow.Web/wwwroot/ts/kb-selection.ts"
      to: "hidden fields"
      via: "DOMContentLoaded injects PinnedVideoIds/FollowedCreators inputs"
      pattern: "PinnedVideoIds"
---

<objective>
Build the user-facing selection surfaces: Pin/Follow buttons + tray on /content-kb browse, the editable Expert Context chip area + object-aware typeahead on the /deck-analysis form, the localStorage-backed `kb-selection.ts` module, the typeahead JSON API (returning {id,title} objects), and all new `site-common.css` layout classes.

Purpose: SEL-01 (pin/follow from both surfaces) and SEL-03 (one-shot pins, sticky follows, localStorage). Progressive enhancement is mandatory — the form must submit (with empty selection) when JS is off. Pins clear only after a SUCCESSFUL analysis (not on submit), so a server/validation failure preserves the user's pins. This plan also authors ALL new `site-common.css` kb-* classes including the `.kb-clip-origin*` badge classes that Plan 04 consumes, so the two Wave-3 plans never both touch site-common.css.

Output: VM/controller VideoId wiring, the search API controller (object entries), the two view edits, `kb-selection.ts` (compiled to js) with its own object typeahead, and the full site-common.css class set.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-expert-context-selection-runs-after-phase-30-before-phase-31/32-CONTEXT.md
@.planning/phases/32-expert-context-selection-runs-after-phase-30-before-phase-31/32-RESEARCH.md
@.planning/phases/32-expert-context-selection-runs-after-phase-30-before-phase-31/32-PATTERNS.md
@.planning/phases/32-expert-context-selection-runs-after-phase-30-before-phase-31/32-UI-SPEC.md
@.planning/phases/32-expert-context-selection-runs-after-phase-30-before-phase-31/32-02-SUMMARY.md

<interfaces>
<!-- Confirmed against live code 2026-06-07 -->

ContentKbBrowseViewModel.Entry (DeckFlow.Web/Models/ContentKbBrowseViewModel.cs):
  - required props: Id (long, line 29), Title (32), Source (35), SourceUrl (38), DetailUrl (41), Archetype, Bracket, CardCategory, ArchetypeTags, BracketTags, CardCategoryTags
  - NEEDS new: VideoId (string) sourced from ContentSiteIndexRow.YoutubeVideoId ?? RssGuid

ContentKbController.Index (DeckFlow.Web/Controllers/ContentKbController.cs):
  - line 54 Index action; line 56 GetPublishedRowsAsync; line 58 .Select(row => new ContentKbBrowseViewModel.Entry { ... })

DeckAnalysisViewModel (DeckFlow.Web/Models/DeckAnalysisViewModel.cs):
  - line 16: DeckAnalysisRequest Request { get; init; }
  - line 71: IReadOnlyList<ContentKbExcerpt>? ExpertContextClips { get; init; }
  - AnalysisPromptText (or equivalent result text) present on the VM when an analysis succeeded — the marker for "clear pins now"
  - NEEDS new: ResolvedPinTitles (IReadOnlyDictionary<string,string>) id -> title for server-rendered chips

DeckAnalysisPacketResult (DeckFlow.Web/Services/DeckAnalysisPacketService.cs — Plan 02 added):
  - ResolvedPinTitles (IReadOnlyDictionary<string,string>?) populated by ResolvePinTitlesAsync; null when no pins

DeckController.DeckAnalysisUpload (DeckFlow.Web/Controllers/DeckController.cs):
  - lines 636-651: `return View("DeckAnalysis", new DeckAnalysisViewModel { ... ExpertContextClips = result.ExpertContextClips, ImportWarning = result.ImportWarning })` — the ONE VM site that has both a replayed selection (request.PinnedVideoIds restored by Plan 02 replay guard) and a result; add `ResolvedPinTitles = result.ResolvedPinTitles ?? new Dictionary<string, string>()` here. The other 10 `new DeckAnalysisViewModel { ... }` sites are initial-GET / error paths with empty selection and rely on the VM default (empty dictionary) — leave them unchanged.

DeckAnalysisRequest (Plan 02 added): PinnedVideoIds (List<string>), FollowedCreators (List<string>), ExpertSelectionJson (string)

IContentSiteIndexStore: GetPublishedRowsAsync(ct), GetByIdAsync(id, ct)

df-typeahead.ts (DeckFlow.Web/wwwroot/ts/df-typeahead.ts — DO NOT MODIFY; HIGH-5 Option B):
  - attachTypeahead(input, panel, minChars, onPick, options?) — CONFIRMED 2026-06-07: fetches `${endpoint}?query=${q}`, parses `string[]`, calls `onPick(name: string)` with ONLY a string. Titles alone CANNOT supply PinnedVideoIds (which must be YoutubeVideoId ?? RssGuid). This shared component is also used by card/mechanic lookup, so it is NOT extended. kb-selection.ts implements its OWN object-aware fetch/render reusing the .autocomplete-panel / .autocomplete-option CSS classes.

tsconfig.json: module "none", strict, target es2017 — NO ES imports; access namespace via (window as DeckFlowWindow).DeckFlow.
TypeScript build: MSBuild CompileTypeScriptAssets target runs tsc on build; output to wwwroot/js/. Compiled .js is git-tracked.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add VideoId to browse VM, ResolvedPinTitles to analysis VM, ContentKbSearchApiController (object entries)</name>
  <files>DeckFlow.Web/Models/ContentKbBrowseViewModel.cs, DeckFlow.Web/Controllers/ContentKbController.cs, DeckFlow.Web/Models/DeckAnalysisViewModel.cs, DeckFlow.Web/Controllers/DeckController.cs, DeckFlow.Web/Controllers/Api/ContentKbSearchApiController.cs</files>
  <read_first>
    - DeckFlow.Web/Models/ContentKbBrowseViewModel.cs (FULL — Entry record props)
    - DeckFlow.Web/Controllers/ContentKbController.cs (FULL — Index projection lines 54-74)
    - DeckFlow.Web/Models/DeckAnalysisViewModel.cs (FULL)
    - DeckFlow.Web/Controllers/DeckController.cs (DeckAnalysisUpload action lines 605-673 — the `new DeckAnalysisViewModel { ... }` at lines 636-651 that maps result.* onto the VM; this is the only site where a replayed selection + result exist together)
    - DeckFlow.Web/Controllers/Api/SuggestionsApiController.cs (FULL — controller base, route, JSON response shape, IContentSiteIndexStore injection convention, whether GET reads use SameOrigin)
    - DeckFlow.Web/Controllers/Admin/AdminContentKbController.cs (constructor ArgumentNullException guards)
    - .planning/phases/32-expert-context-selection-runs-after-phase-30-before-phase-31/32-UI-SPEC.md (IC-03 typeahead, C-04 endpoints)
    - DeckFlow.Core/Knowledge/ContentArtifactSpec.cs (ContentSiteIndexRow.YoutubeVideoId, RssGuid)
  </read_first>
  <action>
    In ContentKbBrowseViewModel.cs, add `public required string VideoId { get; init; }` to the `Entry` record with an XML summary ("Stable id used for pinning — YoutubeVideoId or RssGuid."). In ContentKbController.Index projection (line 58), set `VideoId = row.YoutubeVideoId ?? row.RssGuid ?? row.Id.ToString(CultureInfo.InvariantCulture)` so VideoId is never null/empty.

    In DeckAnalysisViewModel.cs, add `public IReadOnlyDictionary<string, string> ResolvedPinTitles { get; init; } = new Dictionary<string, string>();` with an XML summary ("Pinned video id -> title for server-rendered replay chips."). The default empty dictionary keeps all initial-GET / error VM sites valid with no edits.

    In DeckController.cs, at the `DeckAnalysisUpload` VM construction (lines 636-651 — the upload/replay path, the only `new DeckAnalysisViewModel { ... }` site that has both a result and a replayed selection), add `ResolvedPinTitles = result.ResolvedPinTitles ?? new Dictionary<string, string>(),` alongside the existing `ExpertContextClips = result.ExpertContextClips,` mapping. `result.ResolvedPinTitles` is populated by Plan 02 (`ResolvePinTitlesAsync` resolves `request.PinnedVideoIds` -> titles; the replay guard restores PinnedVideoIds before BuildAsync). Do NOT inject any new dependency into the controller and do NOT modify the other 10 `new DeckAnalysisViewModel { ... }` sites (initial GET + error paths carry an empty selection and use the VM default). Touch only the one VM initializer.

    Create DeckFlow.Web/Controllers/Api/ContentKbSearchApiController.cs: a `[Route("api/content-kb")]` ControllerBase with constructor `ArgumentNullException.ThrowIfNull` guards on injected `IContentSiteIndexStore`. Two GET actions:
      - `[HttpGet("entries")]` Search(string query, CancellationToken ct): visible (published) entries whose Title contains query (OrdinalIgnoreCase), projected to OBJECTS `{ id, title }` where `id = row.YoutubeVideoId ?? row.RssGuid ?? row.Id.ToString(CultureInfo.InvariantCulture)` and `title = row.Title`. Return a small DTO/anonymous shape `[{ "id": ..., "title": ... }]` (camelCase via the app's default JSON options). Take(10). (HIGH-5 Option B: kb-selection.ts consumes these OBJECTS directly — it does NOT use df-typeahead, which only handles string[]. The query param is named `query` to keep the same query-string convention.)
      - `[HttpGet("creators")]` Creators(string query, CancellationToken ct): distinct Source names (strings) from published rows containing query, OrdinalIgnoreCase, Take(10). Returns `string[]` (creators pin by name, no id needed).
    Gate GET reads exactly the way SuggestionsApiController gates its public GET reads (add SameOrigin only if the existing reads do). Include XML doc comments.

    LF endings; no formatter sweep; touch only needed lines.
  </action>
  <verify>
    <automated>"/mnt/c/Program Files/dotnet/dotnet.exe" build DeckFlow.Web/DeckFlow.Web.csproj 2>&1 | grep -E "Build succeeded|error" | head</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "public required string VideoId { get; init; }" DeckFlow.Web/Models/ContentKbBrowseViewModel.cs` matches one line
    - `grep -n "VideoId = row.YoutubeVideoId" DeckFlow.Web/Controllers/ContentKbController.cs` matches one line
    - `grep -n "ResolvedPinTitles" DeckFlow.Web/Models/DeckAnalysisViewModel.cs` matches the property declaration
    - `grep -n "ResolvedPinTitles = result.ResolvedPinTitles" DeckFlow.Web/Controllers/DeckController.cs` matches exactly one line (population on the upload/replay VM — proves chips get real titles, not just a VM declaration)
    - File DeckFlow.Web/Controllers/Api/ContentKbSearchApiController.cs exists with `[HttpGet("entries")]` and `[HttpGet("creators")]`
    - The entries action projects an object with both `id` and `title` (not a bare title string) — grep the controller for both `id =` and `title =` (or an `Id`/`Title` DTO) in the entries projection
    - `dotnet build DeckFlow.Web` reports Build succeeded
  </acceptance_criteria>
  <done>VMs carry VideoId + ResolvedPinTitles; DeckController populates ResolvedPinTitles from result.ResolvedPinTitles on the replay path; the entries endpoint returns {id,title} objects for id-based pinning; Web builds.</done>
</task>

<task type="auto">
  <name>Task 2: Author kb-selection.ts (self-contained object typeahead + post-success pin clear) and all new site-common.css classes</name>
  <files>DeckFlow.Web/wwwroot/ts/kb-selection.ts, DeckFlow.Web/wwwroot/css/site-common.css</files>
  <read_first>
    - DeckFlow.Web/wwwroot/ts/content-kb.ts (FULL — IIFE, data-attribute selectors, DOMContentLoaded, progressive-enhancement guards)
    - DeckFlow.Web/wwwroot/ts/df-typeahead.ts (FULL — confirm string-only contract; reuse its .autocomplete-panel/.autocomplete-option markup conventions and debounce/keyboard patterns as a REFERENCE, but do NOT call attachTypeahead for the object entries endpoint)
    - DeckFlow.Web/wwwroot/ts/site.ts (lines 94-100 localStorage try/catch guard)
    - DeckFlow.Web/tsconfig.json (module none confirmation)
    - DeckFlow.Web/wwwroot/css/site-common.css (existing .kb-filter-bar 44px min-height, .kb-tag geometry, .autocomplete-panel/.autocomplete-option)
    - .planning/phases/32-expert-context-selection-runs-after-phase-30-before-phase-31/32-UI-SPEC.md (FULL — C-01..C-06, IC-01..IC-05, CSS class list, Copywriting Contract, color/spacing/typography tokens)
    - .planning/phases/32-expert-context-selection-runs-after-phase-30-before-phase-31/32-PATTERNS.md (kb-selection.ts + site-common.css sections)
  </read_first>
  <action>
    Create DeckFlow.Web/wwwroot/ts/kb-selection.ts using the mandatory IIFE + `'use strict'` shell and the `window as DeckFlowWindow` namespace access (NO ES imports — module:none). Wrap EVERY localStorage read/write in try/catch (site.ts pattern). Implement:
      - localStorage keys: `PINNED_KEY = 'deckflow.kb.pinned'` (JSON [{id, title}], max 3), `FOLLOWED_KEY = 'deckflow.kb.followed'` (JSON [{source}]).
      - Browse page (guard: only run if [data-kb-pin]/[data-kb-entry] exist): toggle each [data-kb-pin] button (write localStorage, set aria-pressed, swap label "Pin"/"Pinned", update tray). Toggle each [data-kb-follow] keyed by data-creator (all buttons for the same creator share aria-pressed). Pin cap: when pins.length >= 3 disable un-pinned Pin buttons (disabled + aria-disabled="true" + title "Pin cap reached (3 max). Remove a pin to add another."). Render/update .kb-selection-tray (remove hidden when any pin/follow exists; re-add when both empty); each tray item has a remove button.
      - Analysis form (guard: only run if [data-kb-chips] exists): on DOMContentLoaded read server-rendered chips and re-hydrate localStorage from them (IC-04); then inject one hidden input name="PinnedVideoIds" / name="FollowedCreators" per localStorage entry so non-server selections also submit. Render visible chips with remove buttons that update localStorage AND remove the matching hidden input.
      - OBJECT TYPEAHEAD (HIGH-5 Option B — self-contained, NO attachTypeahead): implement a small typed fetch/render for the chip-area search input that hits `/api/content-kb/entries?query=<q>` and parses `Array<{ id: string; title: string }>`. Render each suggestion into a reused `.autocomplete-panel` with `.autocomplete-option` buttons (mirror df-typeahead's debounce + min-chars + keyboard/aria patterns; set option text via textContent). On pick, use the suggestion's `id` (NOT its title) to add a pinned chip + hidden `PinnedVideoIds` input + localStorage `{id, title}` entry. Provide a parallel string fetch against `/api/content-kb/creators?query=<q>` for the follow typeahead (creators pin by name). Define explicit TS interfaces for the entry object; no `any` except the documented (window as DeckFlowWindow) cast.
      - POST-SUCCESS PIN CLEAR (MED-6 — confirmed: clearing on the `submit` event wipes pins BEFORE server validation/network success, losing them on any failure). Do NOT clear on submit. Instead: the analysis view server-renders a marker element (e.g., `data-kb-clear-pins-on-load`) ONLY when the result text (AnalysisPromptText / analysis success) is present (Task 3 adds this). On DOMContentLoaded, AFTER re-hydrating chips, if `document.querySelector('[data-kb-clear-pins-on-load]')` exists, clear `deckflow.kb.pinned` only (follows remain) per IC-01 step 7. This guarantees pins survive a validation/server error and clear only after a real successful analysis render.
      - Expose win.DeckFlow.initKbSelection = initKbSelection; and document.addEventListener('DOMContentLoaded', initKbSelection);.
    Strict TS — every variable typed; no `any` except the documented (window as DeckFlowWindow) cast. Set dynamic chip/tray/suggestion text via textContent (not innerHTML) to avoid XSS.

    In site-common.css add ALL new classes (compose from existing tokens only — NO new :root vars; NO touching site.css or guild themes), with exact property values from UI-SPEC C-01..C-05:
      .kb-card-actions (+ button rule with min-height 44px), .kb-pin-btn, .kb-follow-btn (+ [aria-pressed="true"] accent + :disabled opacity 0.45), .kb-selection-tray (+ __section __label __list __item __item-label __remove __cta), .kb-chip (+ --pinned --followed), .kb-chip__remove (+ :hover danger + 44px target), .kb-chip-area (+ __heading __chips __typeahead __search __empty-hint), and the origin-marker badges .kb-clip-origin + --pinned + --followed + --evergreen (Plan 04 consumes these). Tray item truncation: max-width 16rem; overflow hidden; text-overflow ellipsis; white-space nowrap.

    LF endings; touch only the lines being added; no Format Document.
  </action>
  <verify>
    <automated>"/mnt/c/Program Files/dotnet/dotnet.exe" build DeckFlow.Web/DeckFlow.Web.csproj 2>&1 | grep -E "Build succeeded|error" | head; ls DeckFlow.Web/wwwroot/js/kb-selection.js</automated>
  </verify>
  <acceptance_criteria>
    - File DeckFlow.Web/wwwroot/ts/kb-selection.ts exists, opens with the IIFE `((): void => {` and `'use strict';`, and contains NO `import`/`export` statements (module:none)
    - `grep -c "deckflow.kb.pinned\|deckflow.kb.followed" DeckFlow.Web/wwwroot/ts/kb-selection.ts` returns >= 2
    - `grep -n "content-kb/entries" DeckFlow.Web/wwwroot/ts/kb-selection.ts` matches the self-contained fetch (object typeahead — proves HIGH-5 Option B, not a df-typeahead string call)
    - `grep -n "data-kb-clear-pins-on-load" DeckFlow.Web/wwwroot/ts/kb-selection.ts` matches the post-success clear guard (MED-6 — clear keyed off the server marker, not the submit event)
    - kb-selection.ts contains NO `addEventListener('submit'` that clears pins (the submit-time clear anti-pattern is absent)
    - Every localStorage access in kb-selection.ts is wrapped in try/catch
    - `grep -c "kb-card-actions\|kb-selection-tray\|kb-chip\|kb-clip-origin" DeckFlow.Web/wwwroot/css/site-common.css` returns >= 4 (all class families present)
    - `git diff --name-only` lists only site-common.css among css files (site.css and guild themes unchanged)
    - `dotnet build DeckFlow.Web` reports Build succeeded AND DeckFlow.Web/wwwroot/js/kb-selection.js exists (tsc emitted it)
  </acceptance_criteria>
  <done>kb-selection.ts compiles to js with its own object-aware entries typeahead (df-typeahead untouched), pins clear only after a successful analysis via the server marker, all kb-* classes live in site-common.css only, localStorage is guarded, no site.css/theme edits.</done>
</task>

<task type="auto">
  <name>Task 3: Wire Pin/Follow buttons + tray into browse view, the chip area + clear-pins marker into the analysis form</name>
  <files>DeckFlow.Web/Views/ContentKb/Index.cshtml, DeckFlow.Web/Views/Deck/DeckAnalysis.cshtml</files>
  <read_first>
    - DeckFlow.Web/Views/ContentKb/Index.cshtml (FULL — hub-card article structure lines 60-91, .kb-filter-bar, @section Scripts ~line 111)
    - DeckFlow.Web/Views/Deck/DeckAnalysis.cshtml (FULL — form tag line 73, AntiForgeryToken line 83, AI selector section, Step 1 fields, the result/AnalysisPromptText render region, @section Scripts)
    - DeckFlow.Web/Models/DeckAnalysisViewModel.cs (Request, ResolvedPinTitles from Task 1, AnalysisPromptText/result-text property used for the clear-pins marker)
    - .planning/phases/32-expert-context-selection-runs-after-phase-30-before-phase-31/32-UI-SPEC.md (C-01, C-02, C-03, C-04 exact HTML structures + Copywriting Contract)
    - .planning/phases/32-expert-context-selection-runs-after-phase-30-before-phase-31/32-PATTERNS.md (ContentKb/Index.cshtml + DeckAnalysis.cshtml sections)
  </read_first>
  <action>
    In ContentKb/Index.cshtml:
      - After the tag `<div aria-hidden="true">` inside each .hub-card article, add the `<div class="kb-card-actions">` row with the Pin button (data-kb-pin, data-video-id="@entry.VideoId", data-video-title="@entry.Title", aria-pressed="false", label "Pin") and the Follow button (data-kb-follow, data-creator="@entry.Source", aria-pressed="false", label "Follow") — exact markup from UI-SPEC C-01/C-02.
      - Between .kb-filter-bar and .hub-grid, insert the .kb-selection-tray shell from UI-SPEC C-03 (with hidden, aria-live="polite", pin-count span, pins list, follows list, CTA link to /deck-analysis).
      - In @section Scripts, add `<script src="~/js/kb-selection.js" asp-append-version="true"></script>` after the existing content-kb.js script.

    In Deck/DeckAnalysis.cshtml:
      - After the AI selector section and before the Step 1 decklist fields (inside the form, after AntiForgeryToken), insert the `<section class="kb-chip-area">` block from UI-SPEC C-04: heading + info tooltip, data-kb-chips chip container that server-renders one .kb-chip--pinned per Model.Request.PinnedVideoIds (label from Model.ResolvedPinTitles.GetValueOrDefault(id, id)) and one .kb-chip--followed per Model.Request.FollowedCreators, the hidden input name="PinnedVideoIds"/name="FollowedCreators" per replayed value, the typeahead anchor (data-kb-chip-typeahead input + .autocomplete-panel), and the empty-hint `<p>` when both lists are empty. Use the exact copy from the Copywriting Contract.
      - CLEAR-PINS MARKER (MED-6): in the result-render region, when the analysis succeeded (the result text is present — use the same condition the view already uses to render AnalysisPromptText / the analysis result), emit a hidden marker element `<div data-kb-clear-pins-on-load hidden></div>` (or add the `data-kb-clear-pins-on-load` attribute to the existing result container). kb-selection.ts clears pins on load ONLY when this marker exists, so pins survive validation/server errors and clear only after a real successful analysis. Do NOT emit the marker on initial GET or error renders.
      - In @section Scripts, add `<script src="~/js/kb-selection.js" asp-append-version="true"></script>`.
      - Do NOT move any existing form fields.

    Progressive enhancement: the hidden fields are server-rendered so the form submits the replayed selection even with JS off; the empty-hint renders when there is no selection. LF endings; touch only the lines being added; no Format Document.
  </action>
  <verify>
    <automated>"/mnt/c/Program Files/dotnet/dotnet.exe" build DeckFlow.Web/DeckFlow.Web.csproj 2>&1 | grep -E "Build succeeded|error" | head</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "data-kb-pin\|data-kb-follow\|kb-selection-tray" DeckFlow.Web/Views/ContentKb/Index.cshtml` returns >= 3
    - `grep -n "kb-selection.js" DeckFlow.Web/Views/ContentKb/Index.cshtml` and the same in DeckFlow.Web/Views/Deck/DeckAnalysis.cshtml each match one line
    - `grep -c "kb-chip-area\|name=\"PinnedVideoIds\"\|name=\"FollowedCreators\"" DeckFlow.Web/Views/Deck/DeckAnalysis.cshtml` returns >= 3
    - `grep -n "data-kb-clear-pins-on-load" DeckFlow.Web/Views/Deck/DeckAnalysis.cshtml` matches one line, and it is rendered inside the analysis-success branch (NOT unconditionally) — proves MED-6 server marker
    - `grep -n "No expert context selected" DeckFlow.Web/Views/Deck/DeckAnalysis.cshtml` matches the empty-hint copy
    - `dotnet build DeckFlow.Web` reports Build succeeded
  </acceptance_criteria>
  <done>Browse cards have Pin/Follow + tray; the analysis form has the server-rendered chip area + hidden fields + typeahead + a success-only clear-pins marker; both views load kb-selection.js; no-JS form still submits.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → /api/content-kb/* | Untrusted query string `query` reaches the search endpoints |
| localStorage → hidden form fields | Client-controlled selection enters the request (re-validated server-side in Plans 01/02) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-07 | Tampering | localStorage poisoning | mitigate | Server never trusts localStorage directly; only form-submitted values reach the DTO, and Plan 01 resolves ids against published rows / caps pins at 3 |
| T-32-08 | Information Disclosure | Search endpoints leaking hidden entries | mitigate | /api/content-kb/entries+creators read only visible (published) rows via GetPublishedRowsAsync; no hidden/admin data exposed |
| T-32-09 | Denial of Service | Unbounded typeahead result sets | mitigate | Both endpoints Take(10); the self-contained kb-selection fetch debounces and requires min chars |
| T-32-10 | Cross-Site Scripting | User-rendered chip titles / tray labels / suggestion text | mitigate | Razor HTML-encodes @entry.Title/@creator by default; TS sets textContent (not innerHTML) for dynamic chips/tray/suggestion items; entry objects rendered via textContent only |
| T-32-SC | Tampering | npm/pip/cargo installs | accept | No package installs; phase installs zero external packages (RESEARCH §Package Legitimacy Audit: not applicable) |
</threat_model>

<verification>
- `dotnet build DeckFlow.Web` clean; tsc emits wwwroot/js/kb-selection.js
- `git diff --name-only` shows only site-common.css among css files (no site.css / guild-theme edits)
- Manual: with JS disabled the /deck-analysis form still submits (selection empty); with JS enabled, pinning a card and visiting /deck-analysis shows the pin as a chip + hidden field; the entries typeahead pins by id (a picked suggestion's hidden PinnedVideoIds value is its video id, not its title)
- Manual: a server/validation FAILURE on submit preserves pinned localStorage (MED-6); only a successful analysis render clears pins
- Full visual verification (pin/follow appearance, tray, chips, typeahead, origin markers) is deferred to Plan 04's human-verify checkpoint at 2 viewports
</verification>

<success_criteria>
SEL-01 (pin/follow on browse + editable chip area on the analysis form) and SEL-03 (one-shot pins, sticky follows, localStorage; pins clear only after success) are implemented with progressive enhancement; the entries typeahead returns {id,title} objects and pins by id (HIGH-5 Option B, df-typeahead untouched); all kb-* CSS lives in site-common.css; the typeahead search API is live.
</success_criteria>

<output>
Create `.planning/phases/32-expert-context-selection-runs-after-phase-30-before-phase-31/32-03-SUMMARY.md` when done.
</output>
