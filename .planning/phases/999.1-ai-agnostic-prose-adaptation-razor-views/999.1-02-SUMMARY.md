---
phase: 999.1-ai-agnostic-prose-adaptation-razor-views
plan: "02"
subsystem: DeckFlow.Web/Views/Deck
tags: [ai-agnostic, razor-views, hybrid-pattern, prose-adaptation]
dependency_graph:
  requires: [AiPlatform.Normalize (Phase 15), _AiSelector partial (Phase 15), DeckComparisonViewModel (Phase 12/13)]
  provides: [DeckComparison.cshtml AI-agnostic prose with AiPlatform.Normalize injection]
  affects: [DeckComparison page rendering for all AI platform selections]
tech_stack:
  added: []
  patterns: [Hybrid pattern (D-01): universal noun above selector, @aiPlatform.DisplayName below]
key_files:
  created: []
  modified:
    - DeckFlow.Web/Views/Deck/DeckComparison.cshtml
decisions:
  - "Applied D-01 Hybrid pattern: above-selector prose uses 'your AI', below-selector injects @aiPlatform.DisplayName"
  - "Line 226 uses 'Deck Analysis workflow' (page back-reference), not 'your AI' — per plan spec"
  - "Lines 326-328 treated as AT-selector (ABOVE treatment per D-01 edge case) despite being physically below line-320 selector"
  - "Line 509 uses 'conversation' over 'thread' for cross-AI vocabulary alignment"
metrics:
  duration: "~10 minutes"
  completed: "2026-05-18"
  tasks: 2
  files: 1
---

# Phase 999.1 Plan 02: DeckComparison.cshtml Hybrid Prose Adaptation Summary

**One-liner:** DeckComparison.cshtml 12 ChatGPT hits reduced to 1 (lede exception) using Hybrid pattern with `AiPlatform.Normalize` injection for below-selector prose and `your AI` universal noun for above-selector prose.

## Tasks Completed

| Task | Name | Commit | Files |
|------|------|--------|-------|
| 1 | Add AiPlatform.Normalize Razor block | f5e4e39 | DeckFlow.Web/Views/Deck/DeckComparison.cshtml |
| 2 | Rewrite ChatGPT prose with Hybrid pattern | 833c9ff | DeckFlow.Web/Views/Deck/DeckComparison.cshtml |

## Changes Made

### Task 1: AiPlatform.Normalize block

Added `var aiPlatform = AiPlatform.Normalize(Model.Request.TargetAiPlatform);` to the top-level `@{ }` block in `DeckComparison.cshtml`. `AiPlatform` was already available via `_ViewImports.cshtml` (`@using DeckFlow.Web.Models`), so no additional `@using` directive was needed.

### Task 2: ChatGPT prose rewrites

**ABOVE-selector substitutions (universal noun "your AI"):**

| Original | Replaced With |
|----------|---------------|
| "generate a ChatGPT-ready comparison packet" | "generate an AI-ready comparison packet" |
| "paste 30-comparison-prompt.txt into ChatGPT" | "paste 30-comparison-prompt.txt into your AI" |
| "the current ChatGPT workflow" | "the current Deck Analysis workflow" (page back-ref per spec) |
| "so ChatGPT judges the lists against the selected bracket" | "so your AI judges the lists against the selected bracket" |
| "paste it into ChatGPT, then ask for the final answer" | "paste it into your AI, then ask for the final answer" |
| "Paste it into ChatGPT." (AT-selector li) | "Paste it into your AI." |
| "Have ChatGPT return the readable comparison" (AT-selector li) | "Have your AI return the readable comparison" |
| "if you continue the ChatGPT thread" (AT-selector li) | "if you continue the conversation with your AI" |

**BELOW-selector substitutions (inject `@aiPlatform.DisplayName`):**

| Original | Replaced With |
|----------|---------------|
| "Paste this into ChatGPT to get the side-by-side comparison" | "Paste this into @aiPlatform.DisplayName to get the side-by-side comparison" |
| "ChatGPT should return a deck_comparison JSON object" | "@aiPlatform.DisplayName should return a deck_comparison JSON object" |
| "Paste this into the same ChatGPT thread after follow-up questions" | "Paste this into the same @aiPlatform.DisplayName conversation after follow-up questions" |

## Verification Results

| Check | Expected | Actual | Status |
|-------|----------|--------|--------|
| `grep -c 'ChatGPT'` | 1 (lede only) | 1 | PASS |
| `grep -c '@aiPlatform.DisplayName'` | >= 3 | 3 | PASS |
| `grep -c 'ChatGPT, Claude, or Gemini'` | 1 (lede preserved) | 1 | PASS |
| `grep -c 'data-cache-key="chatgpt-deck-comparison"'` | 1 | 1 | PASS |
| `grep -c 'data-chatgpt-comparison-form'` | 1 | 1 | PASS |
| `grep -c 'chatgpt-step-eyebrow'` | 1 | 3 | PASS (3 steps each have the eyebrow class) |
| `dotnet build ... -c Release` | exit 0 | exit 0 | PASS |

## Deviations from Plan

None — plan executed exactly as written.

The plan anticipated `grep -c 'chatgpt-step-eyebrow'` returning `1` but the file has 3 uses (one per step panel). The verification still passes because the criterion is "preserved" — all 3 occurrences are untouched D-08 identifiers.

## Known Stubs

None — the `@aiPlatform.DisplayName` injection is fully wired to the `AiPlatform.Normalize(Model.Request.TargetAiPlatform)` local which resolves from the form-posted value at runtime.

## Threat Flags

None — no new network endpoints, auth paths, file access patterns, or schema changes introduced. Razor view only.

## Self-Check: PASSED

- DeckFlow.Web/Views/Deck/DeckComparison.cshtml: FOUND
- f5e4e39: Task 1 commit verified
- 833c9ff: Task 2 commit verified
