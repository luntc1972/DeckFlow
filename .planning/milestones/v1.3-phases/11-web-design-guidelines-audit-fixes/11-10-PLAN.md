---
id: 11-10
phase: 11
plan: 10
type: execute
wave: 7
depends_on: [11-03, 11-06, 11-07]
files_modified:
  - DeckFlow.Web/Views/AdminHarvest/Index.cshtml
requirements: [WDG-10]
autonomous: true
verification_command: "dotnet build DeckFlow.sln --configuration Release"
user_setup: []
must_haves:
  truths:
    - "The #harvest-status-live element in AdminHarvest/Index.cshtml carries role=\"status\" and aria-live=\"polite\"."
    - "Every AJAX-poll update written into that element by admin-harvest.ts:151 render() is announced to screen readers — delivering the Phase 7 SC #1/#3 intent."
    - "No JavaScript changes are required for this fix — the live-region semantics are purely HTML attribute additions."
  artifacts:
    - path: "DeckFlow.Web/Views/AdminHarvest/Index.cshtml"
      provides: "role=\"status\" + aria-live=\"polite\" on the #harvest-status-live element at line 54"
      contains: "aria-live=\"polite\""
  key_links:
    - from: "<element id=\"harvest-status-live\" role=\"status\" aria-live=\"polite\">"
      to: "Assistive tech polite-live-region announcement on each text update"
      via: "DOM mutation triggered by admin-harvest.ts:151 render()"
      pattern: "id=\"harvest-status-live\".*aria-live=\"polite\"|aria-live=\"polite\".*id=\"harvest-status-live\""
---

<objective>
Land Sweep 10 of the WDG audit: add `role="status"` and `aria-live="polite"` attributes to the `#harvest-status-live` element in AdminHarvest/Index.cshtml so screen readers hear AJAX-poll status updates.

Purpose: Phase 7 SC #1/#3 intended SR users to follow harvest progress through live region announcements, but the element currently lacks ARIA live-region semantics. Each `admin-harvest.ts:151 render()` write updates the DOM silently for SR users. WDG-10 fix is HTML-only — add two attributes to the existing element. No JavaScript changes needed.

Output: One Razor view updated — the most localized change in Phase 11.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/11-web-design-guidelines-audit-fixes/11-CONTEXT.md
@.planning/quick/260513-wdg-web-design-guidelines-audit-findings/260513-wdg-FINDINGS.md
@DeckFlow.Web/Views/AdminHarvest/Index.cshtml
@DeckFlow.Web/wwwroot/ts/admin-harvest.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add role="status" + aria-live="polite" to #harvest-status-live</name>
  <files>DeckFlow.Web/Views/AdminHarvest/Index.cshtml</files>
  <read_first>
    - DeckFlow.Web/Views/AdminHarvest/Index.cshtml lines 45-65 (the #harvest-status-live element at line 54; note prior sweep edits earlier in this file: 11-03 selected= at lines 40, 90; 11-06 captions at 140-160 and 169-179; 11-07 URL input attrs at line 76)
    - DeckFlow.Web/wwwroot/ts/admin-harvest.ts around line 151 (the render() function that updates this element — read for context only; no edits)
    - .planning/quick/260513-wdg-web-design-guidelines-audit-findings/260513-wdg-FINDINGS.md §"P1 finding N" (lines 127-132) and §"Sweep 10" (lines 306-314 list this fix under Misc P1)
  </read_first>
  <action>
    In `DeckFlow.Web/Views/AdminHarvest/Index.cshtml`, locate the element carrying `id="harvest-status-live"` (per FINDINGS.md it is at line 54 — find by `id="harvest-status-live"` rather than absolute line because prior sweeps in this file may have shifted line numbers).

    Add two attributes to that element if not already present:
    - `role="status"`
    - `aria-live="polite"`

    Attribute order is Claude's discretion. If either attribute is already on the element, do not duplicate. Do not change any other attribute (`id`, `class`, child content, etc.).

    Do NOT modify `admin-harvest.ts`. Do NOT modify any other element in the view. Do NOT regress prior sweep edits.

    Per CONTEXT.md `code_context`: the AJAX poll write side (admin-harvest.ts:151 `render()`) does not need updating — the browser fires SR announcements automatically once the live-region attributes are present on the target element.
  </action>
  <verify>
    <automated>dotnet build DeckFlow.sln --configuration Release</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'id="harvest-status-live"' DeckFlow.Web/Views/AdminHarvest/Index.cshtml` (element still present)
    - Element carries the two new attributes — locate the line containing `id="harvest-status-live"` and confirm both attributes appear on the same element:
      - `grep -E -q 'id="harvest-status-live"[^>]*aria-live="polite"|aria-live="polite"[^>]*id="harvest-status-live"' DeckFlow.Web/Views/AdminHarvest/Index.cshtml`
      - `grep -E -q 'id="harvest-status-live"[^>]*role="status"|role="status"[^>]*id="harvest-status-live"' DeckFlow.Web/Views/AdminHarvest/Index.cshtml`
    - Prior-sweep edits intact in this file:
      - 11-03 selected=: `grep -q 'selected="@(' DeckFlow.Web/Views/AdminHarvest/Index.cshtml`
      - 11-06 captions: `[ "$(grep -c '<caption' DeckFlow.Web/Views/AdminHarvest/Index.cshtml)" -ge 2 ]`
      - 11-07 URL input: `grep -q 'autocomplete="url"' DeckFlow.Web/Views/AdminHarvest/Index.cshtml` AND `grep -q 'inputmode="url"' DeckFlow.Web/Views/AdminHarvest/Index.cshtml`
    - `admin-harvest.ts` NOT changed: `git diff --name-only | grep -v 'AdminHarvest/Index.cshtml' | grep -v 'admin-harvest.ts'` — admin-harvest.ts must not appear among modified files in this plan's commit (verify by `git diff --name-only DeckFlow.Web/wwwroot/ts/admin-harvest.ts` is empty)
    - `dotnet build DeckFlow.sln --configuration Release` exits 0
  </acceptance_criteria>
  <done>
    #harvest-status-live element has role="status" + aria-live="polite"; all prior-sweep edits in this file intact; admin-harvest.ts untouched; build clean.
  </done>
</task>

</tasks>

<verification>
- `dotnet build DeckFlow.sln --configuration Release` exits 0 with zero new warnings.
- All grep assertions pass — including the regression checks for 11-03, 11-06, 11-07 edits in this same file (AdminHarvest/Index.cshtml has been touched by 4 sweeps total: 11-03, 11-06, 11-07, 11-10).
- `git diff DeckFlow.Web/Views/AdminHarvest/Index.cshtml` for this plan shows the single targeted addition only.
- No UAT in this plan per D-03 — phase-end UAT will spot-check screen reader announcement on a real harvest run cycle.
</verification>

<success_criteria>
- Sweep 10 lands: #harvest-status-live element has role="status" + aria-live="polite".
- WDG-10 requirement satisfied: SR users hear each AJAX-poll status update.
- Phase 7 SC #1/#3 intent is delivered.
- No regressions across the four-sweep AdminHarvest/Index.cshtml stack (11-03, 11-06, 11-07, 11-10).
- Phase 11 complete: all 10 WDG-* requirements have a landed plan; phase-end UAT covers full success criteria 1-5 from ROADMAP.md.
</success_criteria>

<output>
After completion, create `.planning/phases/11-web-design-guidelines-audit-fixes/11-10-SUMMARY.md`.
</output>
