---
phase: 46-review-queue-commit-publish-path
plan: 05
type: execute
wave: 3
depends_on: ["46-01", "46-02", "46-03", "46-04"]
files_modified: []
autonomous: true
requirements: [REVQ-02, REVQ-03, PUB-03]
must_haves:
  truths:
    - "The full DeckFlow.sln builds with 0 errors and 0 new warnings, Studio included"
    - "All Core.Tests added this phase pass (approval mutation + LF seed write)"
    - "The format changed-lines gate is clean for this phase's edits"
  artifacts:
    - path: ".planning/phases/46-review-queue-commit-publish-path/46-05-SUMMARY.md"
      provides: "Phase-level verification record + Studio test-coverage-gap note"
  key_links: []
---

<objective>
Final integration + verification gate for Phase 46. Confirm the whole solution builds clean with the
new Core surfaces (approval mutation, git service, LF seed write) and the two new Studio pages wired,
run the Core.Tests added this phase, and run the changed-lines format gate. Record the explicit
Studio-has-no-test-project coverage gap (the two Blazor pages are covered only by the Plan 03/04
human-verify checkpoints, not automated tests).

Purpose: Waves 1-2 build and test their own slices; this plan proves they compose into a clean
solution and documents the verification surface for the phase summary.

Output: A verification SUMMARY recording build status, test results, format-gate status, and the
coverage gap.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/46-review-queue-commit-publish-path/46-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full-solution build + phase Core.Tests + format gate</name>
  <files></files>
  <read_first>
    - .planning/phases/46-review-queue-commit-publish-path/46-01-SUMMARY.md
    - .planning/phases/46-review-queue-commit-publish-path/46-02-SUMMARY.md
    - .planning/phases/46-review-queue-commit-publish-path/46-03-SUMMARY.md
    - .planning/phases/46-review-queue-commit-publish-path/46-04-SUMMARY.md
    - CLAUDE.md (project — build via the Windows dotnet path; format changed-lines gate; LF; carve-outs)
  </read_first>
  <action>
    Run, from the repo root, in order, capturing output:
    1. Full solution build: "/mnt/c/Program Files/dotnet/dotnet.exe" build DeckFlow.sln -c Debug — confirm 0 errors and no NEW warnings (compare against the known clean baseline; pre-existing suppressed warnings are fine). The Studio project MUST be included in the solution build.
    2. Phase Core.Tests: "/mnt/c/Program Files/dotnet/dotnet.exe" test DeckFlow.Core.Tests/DeckFlow.Core.Tests.csproj --filter "FullyQualifiedName~ContentSiteIndexStoreApproval|FullyQualifiedName~ContentIndexSeedWrite" — confirm Passed!, 0 failures. (VSTest is unreliable in WSL per CLAUDE.md; if the full `dotnet test` host flakes, fall back to building DeckFlow.Core.Tests clean and report the flake explicitly in the summary rather than claiming a pass that did not run.)
    3. Format changed-lines gate: run scripts/format-check-changed.sh against this phase's staged/changed C# lines (per CLAUDE.md / Phase 50). Confirm it is clean for the phase's edits; if it reports violations, they must be fixed in the owning plan's files (touch only offending lines) before this gate closes — report which files.
    Do NOT modify production code in this plan except to fix a format-gate violation introduced by this phase (and only the offending lines). Do NOT push, do NOT commit (the orchestrator handles commits). Do NOT run `git add -A`.
  </action>
  <verify>
    <automated>cd /mnt/c/users/chrislunt/source/personal/deckflow && "/mnt/c/Program Files/dotnet/dotnet.exe" build DeckFlow.sln -c Debug 2>&1 | grep -E "error|Build succeeded|Warning\(s\)"</automated>
  </verify>
  <acceptance_criteria>
    - DeckFlow.sln build reports Build succeeded with 0 errors and 0 NEW warnings.
    - The phase Core.Tests filter reports Passed! with 0 failures (or, on a documented WSL VSTest flake, Core.Tests builds clean and the flake is recorded).
    - The changed-lines format gate is clean for this phase's files.
  </acceptance_criteria>
  <done>Solution builds clean with Studio + new Core surfaces; phase Core.Tests pass; format gate clean.</done>
</task>

<task type="auto">
  <name>Task 2: Write phase verification SUMMARY with coverage-gap note</name>
  <files>.planning/phases/46-review-queue-commit-publish-path/46-05-SUMMARY.md</files>
  <read_first>
    - @$HOME/.claude/get-shit-done/templates/summary.md
    - Task 1 captured build/test/format output
  </read_first>
  <action>
    Write 46-05-SUMMARY.md per the template. Record: full-solution build result (errors/warnings counts), phase Core.Tests result (named facts + pass/fail), format-gate result. Explicitly note the coverage gap: DeckFlow.Studio has NO test project, so Review.razor and Publish.razor are verified only by the Plan 03 and Plan 04 human-verify checkpoints (filter/badge/tint/expand/missing-artifact/batch on Review; branch/counts/raw-diff/LF/checkbox-gate/scoped-commit/no-push on Publish) — there are no automated UI tests. Note the SC5 LF assertion was verified at human-verify time via `file index-seed.json`. List the five ROADMAP SC outcomes and confirm SC4 is satisfied under the D-04 reinterpretation (Stage 1 export+diff / Stage 2 commit gated by reviewed-diff checkbox; Studio never pushes).
  </action>
  <verify>
    <automated>cd /mnt/c/users/chrislunt/source/personal/deckflow && test -f .planning/phases/46-review-queue-commit-publish-path/46-05-SUMMARY.md && grep -c "coverage gap\|no test project\|never pushes\|D-04" .planning/phases/46-review-queue-commit-publish-path/46-05-SUMMARY.md</automated>
  </verify>
  <acceptance_criteria>
    - 46-05-SUMMARY.md exists and records build, test, and format-gate results.
    - It explicitly states the Studio-has-no-test-project coverage gap and that the two pages rely on human-verify.
    - It records the SC4 D-04 reinterpretation and the SC5 LF verification path.
  </acceptance_criteria>
  <done>Phase verification summary written, including the Studio test-coverage gap and SC mapping.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Verification runner → local build/test toolchain | Read-only build/test/format invocations; no production-code mutation except a format-gate fix |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-46-05-01 | Tampering | accidental broad commit/format reflow | mitigate | This plan commits nothing (orchestrator commits); format fixes touch only offending lines; never git add -A; carve-outs preserved. |
| T-46-05-02 | Repudiation | claiming an unrun test pass | mitigate | WSL VSTest flake is explicitly handled — report the flake instead of asserting a pass; Core.Tests must at least build clean. |
</threat_model>

<verification>
- DeckFlow.sln builds 0/0 (Studio included).
- Phase Core.Tests pass (approval + seed-write facts).
- Format changed-lines gate clean.
- SUMMARY records the coverage gap + SC mapping.
</verification>

<success_criteria>
- The composed solution builds clean and the phase's automated tests pass.
- The Studio UI coverage gap and the human-verify reliance are documented.
- SC1-SC5 outcomes recorded; SC4 noted under the D-04 reinterpretation.
</success_criteria>

<output>
Create `.planning/phases/46-review-queue-commit-publish-path/46-05-SUMMARY.md` when done (this plan's own output).
</output>
