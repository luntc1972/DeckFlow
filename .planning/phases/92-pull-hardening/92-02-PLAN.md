---
phase: 92-pull-hardening
plan: 02
type: execute
wave: 2
depends_on: [92-01]
files_modified:
  - DeckFlow.Studio/ViewModels/PullFromProdCoordinator.cs
  - DeckFlow.Studio.Tests/ViewModels/PullFromProdCoordinatorTests.cs
autonomous: true
requirements: [SYNC-14, SYNC-15, SYNC-13]

must_haves:
  truths:
    - "SC2 (SYNC-14): PullAndClassifyAsync fetches origin/{branch}, computes behind-count, and WARNS-then-proceeds (never hard-refuses) when behind>0 OR when FetchAsync/GetBehindCountAsync throws GitCommandException — the two cases carry DISTINCT copy (D-guidance; Pitfall 3)"
    - "SC3 (SYNC-15): every entry with a resolved git body AND a non-null ProdRow is stamped — null prod body_sha256 => Indeterminate (D-01a, the deliberate INVERSE of P91's null-skip), hash mismatch => Confirmed (D-01), match => Clean; divergence is computed for ALL such kinds, not only Kind==Diverged (Pitfall 4)"
    - "SC1 (SYNC-13/D-02): ApplyAdoptionsAsync adopts body←git tree (File.Copy), index columns←prod (UpsertContentColumnsOnlyAsync), approval←prod-mirror (SetApprovalStatusAsync), and NEVER writes is_visible/is_hidden — ratified + regression-locked"
    - "Defense-in-depth (D-01): a Confirmed/Indeterminate entry that is NOT in the acknowledged-divergence set is skipped inside ApplyAdoptionsAsync even if it reaches the coordinator — belt to the page's suspenders"
  artifacts:
    - path: "DeckFlow.Studio/ViewModels/PullFromProdCoordinator.cs"
      provides: "Staleness pre-check stage, divergence stamping loop, freshness wrapper return type, adopt-exclusion guard"
      contains: "GetBehindCountAsync"
    - path: "DeckFlow.Studio.Tests/ViewModels/PullFromProdCoordinatorTests.cs"
      provides: "Tests for warn-then-proceed staleness, divergence stamping (Clean/Confirmed/Indeterminate), SYNC-13 field-authority regression, adopt defense-in-depth"
      contains: "BodyDivergenceStatus"
  key_links:
    - from: "PullFromProdCoordinator.PullAndClassifyAsync"
      to: "IGitRepository.FetchAsync + GetBehindCountAsync"
      via: "new first stage 'check local checkout freshness' wrapped in try/catch(GitCommandException) that warns, never throws"
      pattern: "FetchAsync|GetBehindCountAsync"
    - from: "PullFromProdCoordinator divergence stamp"
      to: "ContentSiteIndexContentSignature.ComputeBodySha256"
      via: "the ONE hash surface, compared to ProdRow.BodySha256, guarded by ArtifactPathSafety.TryBuildContainedPath"
      pattern: "ComputeBodySha256"
    - from: "PullFromProdCoordinator.ApplyAdoptionsAsync"
      to: "UpsertContentColumnsOnlyAsync + SetApprovalStatusAsync (never SetVisibility/SetHidden)"
      via: "content-only upsert + approval mirror, is_visible/is_hidden untouched (D-02)"
      pattern: "UpsertContentColumnsOnlyAsync"
---

<objective>
Harden the `PullFromProdCoordinator` — the single orchestration file for Pull — with all three guards, plus regression-lock the field-authority split D-02 ratifies. This is the behavioral heart of the phase; it consumes the 92-01 Core seam and produces the return-shape 92-03's page renders.

1. **SYNC-14 staleness pre-check** — new first stage in `PullAndClassifyAsync` that resolves the branch, fetches `origin/{branch}`, and computes behind-count. WARN-then-proceed on `behindCount > 0`; WARN (distinct copy) on `GitCommandException` (offline/auth). Never hard-refuse (Pull writes LOCAL-only, D-03).
2. **SYNC-15 divergence stamping** — a new post-classify pass stamps `BodyDivergence` per entry using `ComputeBodySha256` against the git-tree body vs `ProdRow.BodySha256`. Null prod hash ⇒ `Indeterminate` (D-01a — the INVERSE of P91's null-skip).
3. **SYNC-13 field-authority** — ratify + regression-test `ApplyAdoptionsAsync`'s existing split (body←git, index←prod, approval←prod-mirror, is_visible/is_hidden preserved) and add the D-01 defense-in-depth adopt-exclusion for un-acknowledged divergent entries.

Purpose: put every write-path decision in one testable coordinator so the page stays thin UI glue.
Output: extended coordinator (new freshness wrapper record + stamping + exclusion) and its extended unit tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/92-pull-hardening/92-CONTEXT.md
@.planning/phases/92-pull-hardening/92-RESEARCH.md
@.planning/phases/92-pull-hardening/92-PATTERNS.md
@.planning/phases/92-pull-hardening/92-01-SUMMARY.md

<interfaces>
<!-- The file being edited + its collaborators. Extracted — no exploration needed. -->

PullFromProdCoordinator.PullAndClassifyAsync (DeckFlow.Studio/ViewModels/PullFromProdCoordinator.cs:71-127):
  Task<IReadOnlyList<SyncDiffEntry>> PullAndClassifyAsync(string stagingRoot, IProgress<string> log, Action<string> onStage, CancellationToken)
  // line 92: var repoRoot = await _git.ResolveRepoRootAsync(StudioRepoLocator.ResolveStartDirectory(), ct)  — REUSE, do not re-resolve
  // lines 93-110: existing exists-check loop (calls ArtifactPathSafety.TryBuildContainedPath + File.Exists, emits per-row log) — KEEP SEPARATE (Pitfall 5)
  // lines 119-121: the .Select(e => e with { ArtifactDownloaded = ... }) stamp — the pattern to EXTEND

PullFromProdCoordinator.ApplyAdoptionsAsync (lines 137-217) — already correct field split to RATIFY:
  await _indexStore.UpsertContentColumnsOnlyAsync(prodRow, ct);                       // index cols ← prod
  await _indexStore.SetApprovalStatusAsync(keyType, keyValue, prodRow.ApprovalStatus, ct);  // approval ← prod
  // File.Copy(repoBody -> liveDest) body ← git tree; is_visible/is_hidden NEVER written
  // existing defensive skip at 150-153: if (entry.Kind == SyncDiffKind.LocalOnly || entry.ProdRow is null) continue;

New Core seam from 92-01 (available):
  Task IGitRepository.FetchAsync(string repoRoot, string remote, string branch, CancellationToken)
  Task<int> IGitRepository.GetBehindCountAsync(string repoRoot, string remote, string branch, CancellationToken)
  string GetCurrentBranchAsync(string repoRoot, CancellationToken)   // existing
  enum BodyDivergenceStatus { NotApplicable, Clean, Confirmed, Indeterminate }
  SyncDiffEntry.BodyDivergence { get; init; }

Hash + path helpers (reuse, do NOT re-hand-roll):
  string ContentSiteIndexContentSignature.ComputeBodySha256(string rawArtifactText)   // DeckFlow.Core.Content — already imported
  bool ArtifactPathSafety.TryBuildContainedPath(string root, string artifactPath, out string resolvedPath)
  ContentSiteIndexRow.BodySha256 { get; init; }  // string? — null on legacy/unbackfilled rows

FakeContentSiteIndexStore capture surface for SYNC-13 assertions (DeckFlow.Studio.Tests/TestDoubles/FakeContentSiteIndexStore.cs):
  List<string> UpsertMethodCalls           // records "UpsertContentColumnsOnlyAsync"
  List<(string Type, string Value, string Status)> SingleApprovalCalls
  // rows adopt via `with { ... }`; assert NO IsVisible/IsHidden mutation on the stored row

Test builder (DeckFlow.Studio.Tests/ViewModels/PullFromProdCoordinatorTests.cs:71-103):
  private PullFromProdCoordinator Build(FakeContentSiteIndexStore localStore, FakeProdContentReader prodReader, FakeGitRepository? git = null)
  private static SyncDiffEntry AdoptEntry(ContentSiteIndexRow prodRow, bool artifactDownloaded)
  // repo body files written under _repoRoot via Directory.CreateDirectory/File.WriteAllText
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Staleness pre-check + freshness wrapper return type (SYNC-14)</name>
  <files>DeckFlow.Studio/ViewModels/PullFromProdCoordinator.cs, DeckFlow.Studio.Tests/ViewModels/PullFromProdCoordinatorTests.cs</files>
  <read_first>
    - DeckFlow.Studio/ViewModels/PullFromProdCoordinator.cs (full — the file being edited; note PullPaths/PullApplyRowResult sibling records at 221-233 as the home for the new record)
    - DeckFlow.Studio.Tests/ViewModels/PullFromProdCoordinatorTests.cs (Build helper + FakeGitRepository injection at 71-90)
    - DeckFlow.Studio/ViewModels/DirectPushCoordinator.cs:429-452 (the no-fetch fail-CLOSED precedent this phase deliberately does NOT copy — Pull is warn-then-proceed)
    - .planning/phases/92-pull-hardening/92-RESEARCH.md (Pattern 2 + Pitfall 3 distinct-copy rule; Open Question 2 return-type)
  </read_first>
  <behavior>
    - GetBehindCountAsync returns > 0 → a WARNING progress line naming the behind-count + branch is emitted; the pull PROCEEDS and still returns entries.
    - FetchAsync (or GetBehindCountAsync) throws GitCommandException → a DISTINCT "could not verify freshness" progress line is emitted; the pull PROCEEDS and still returns entries.
    - behindCount == 0, fetch OK → no warning line; freshness reports Fresh.
    - OperationCanceledException propagates (never swallowed by the freshness catch).
  </behavior>
  <action>
    Introduce a freshness result type as sibling records in PullFromProdCoordinator.cs (next to PullPaths/PullApplyRowResult): `public enum PullFreshnessKind { Fresh, Behind, Unverified }` and `public sealed record PullFreshnessStatus(PullFreshnessKind Kind, int BehindCount, string Branch)`. Keep the tri-state distinct per Pitfall 3 (Behind vs Unverified are NOT the same).

    Widen `PullAndClassifyAsync`'s return type from `Task<IReadOnlyList<SyncDiffEntry>>` to `Task<PullClassifyResult>` where `public sealed record PullClassifyResult(IReadOnlyList<SyncDiffEntry> Entries, PullFreshnessStatus Freshness)` (also a sibling record). This keeps freshness + entries produced by the same atomic call (RESEARCH Open Question 2 — the lower-risk wrapper option).

    Add a NEW first stage before "read production content_site_index": `onStage("check local checkout freshness");` then, inside a try/catch: resolve `repoRoot` (reuse the existing resolution — move it up, or resolve once and reuse for both freshness and the body loop; do NOT resolve twice), get the branch via `_git.GetCurrentBranchAsync(repoRoot, ct)`, call `await _git.FetchAsync(repoRoot, "origin", branch, ct)`, then `var behindCount = await _git.GetBehindCountAsync(repoRoot, "origin", branch, ct)`. If `behindCount > 0`, `log.Report(...)` a WARNING naming the count + `origin/{branch}` + "consider running 'git pull' before adopting. Proceeding with the current git tree." and set the freshness to `Behind`. On `catch (OperationCanceledException) { throw; }` first, then `catch (GitCommandException)` emit the DISTINCT "Could not verify checkout freshness (fetch failed — offline, VPN, or auth). Proceeding with the local git tree as-is." and set freshness to `Unverified`. Log full `GitCommandException` detail via `_logger` (D-07 sanitized UI). Default freshness is `Fresh` when behindCount==0.

    Return `new PullClassifyResult(entries, freshness)` at the end. Follow the file's existing `onStage`/`log.Report` conventions verbatim; use the fixed remote literal `"origin"`.

    In the test file, add cases via the existing `Build(...)` helper passing a `FakeGitRepository { CannedRepoRoot = _repoRoot, CannedBehindCount = N, ThrowOnFetch = ... }`:
    - behind>0 → assert a progress line contains the behind-count and result.Freshness.Kind == Behind, and result.Entries is still populated.
    - ThrowOnFetch = new GitCommandException(...) → assert result.Freshness.Kind == Unverified, a distinct "could not verify" line, and Entries still populated (pull not blocked).
    - clean → Freshness.Kind == Fresh, no warning line.
    Any existing test that asserts on `PullAndClassifyAsync`'s old list return must be updated to read `.Entries`.

    Preserve LF; changed-lines-only formatting; keep OperationCanceledException-rethrow-first ordering (matches ApplyAdoptionsAsync:199-202).
  </action>
  <verify>
    <automated>dotnet.exe test DeckFlow.Studio.Tests --filter "FullyQualifiedName~PullFromProdCoordinator"</automated>
  </verify>
  <acceptance_criteria>
    - PullAndClassifyAsync returns PullClassifyResult(Entries, Freshness); FetchAsync + GetBehindCountAsync are called with remote "origin" and the current branch.
    - behind>0 emits a warning line and proceeds (Entries non-empty in the test); GitCommandException emits a DISTINCT line and proceeds; the two copies differ.
    - No hard-refuse path exists (grep: the freshness catch never rethrows GitCommandException; only OperationCanceledException rethrows).
    - repoRoot resolved once (grep: a single ResolveRepoRootAsync call in PullAndClassifyAsync).
    - Coordinator test filter passes.
  </acceptance_criteria>
  <done>Staleness is checked as the first stage, warns distinctly for behind vs unverified, never blocks Pull, and the return type carries freshness.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Divergence stamping + adopt defense-in-depth (SYNC-15)</name>
  <files>DeckFlow.Studio/ViewModels/PullFromProdCoordinator.cs, DeckFlow.Studio.Tests/ViewModels/PullFromProdCoordinatorTests.cs</files>
  <read_first>
    - DeckFlow.Studio/ViewModels/PullFromProdCoordinator.cs (the .Select stamp at 119-121; the exists-check loop at 93-110; ApplyAdoptionsAsync defensive skip at 150-153)
    - DeckFlow.Core/Content/ContentKbReconcileClassifier.cs:101-109 (the P91 null-skip analog this phase INVERTS — do NOT copy the `!IsNullOrEmpty` guard)
    - DeckFlow.Core/Content/ContentSiteIndexContentSignature.cs:131 (ComputeBodySha256 signature)
    - .planning/phases/92-pull-hardening/92-PATTERNS.md (PullFromProdCoordinator divergence-stamp recommended shape) + 92-RESEARCH.md Pitfall 1/4
  </read_first>
  <behavior>
    - Entry with resolved git body + non-null ProdRow + prod BodySha256 that MATCHES the git body hash → BodyDivergence == Clean.
    - Same but prod BodySha256 DIFFERS → Confirmed.
    - Same but prod BodySha256 is null → Indeterminate (NOT skipped, NOT Clean — the D-01a inverse of P91).
    - Entry with no resolved git body OR LocalOnly (no ProdRow) → NotApplicable.
    - ApplyAdoptionsAsync: a Confirmed/Indeterminate entry whose key is absent from the acknowledged set is skipped (not upserted); a Clean/NotApplicable entry adopts normally.
  </behavior>
  <action>
    Replace the existing `.Select(e => e with { ArtifactDownloaded = availableSet.Contains(e.ArtifactPath) })` stamp (lines 119-121) with a `.Select(e => { ... })` block that computes both `ArtifactDownloaded` and `BodyDivergence`. Compute `downloaded = availableSet.Contains(e.ArtifactPath)`. Initialize `divergence = BodyDivergenceStatus.NotApplicable`. Only when `downloaded && e.ProdRow is not null && ArtifactPathSafety.TryBuildContainedPath(repoRoot, e.ArtifactPath, out var repoBody)`: read `var bodyText = File.ReadAllText(repoBody)`, `var computedHash = ContentSiteIndexContentSignature.ComputeBodySha256(bodyText)`, then set `divergence = e.ProdRow.BodySha256 is null ? BodyDivergenceStatus.Indeterminate : (string.Equals(e.ProdRow.BodySha256, computedHash, StringComparison.Ordinal) ? BodyDivergenceStatus.Clean : BodyDivergenceStatus.Confirmed)`. Return `e with { ArtifactDownloaded = downloaded, BodyDivergence = divergence }`. Compute divergence for EVERY qualifying kind — do NOT gate on `Kind == Diverged` (Pitfall 4). Reuse the `repoRoot` already resolved in Task 1; keep this stamping pass SEPARATE from the exists-check loop at 93-110 (Pitfall 5 — do not merge/alter that loop's per-row logging).

    Add the defense-in-depth adopt-exclusion (D-01 belt-and-suspenders): change `ApplyAdoptionsAsync`'s signature to accept a new parameter `IReadOnlySet<string> acknowledgedDivergentKeys` (the page passes its opt-in set; keyed as `$"{NaturalKeyType}:{NaturalKeyValue}"` to match the page's EntryKey). After the existing LocalOnly/ProdRow-null skip (150-153), add: if `entry.BodyDivergence is BodyDivergenceStatus.Confirmed or BodyDivergenceStatus.Indeterminate` and the entry's key is NOT in `acknowledgedDivergentKeys`, record a skipped `PullApplyRowResult` (Success=true, Action e.g. "Skipped (divergent, not acknowledged)", sanitized note) and `continue` — never upsert it. Do not otherwise change the field-authority body of the loop.

    In the test file add cases (write repo body files under `_repoRoot` matching the file's existing convention, with content whose ComputeBodySha256 matches or mismatches the prod row's BodySha256):
    - matching hash → assert returned entry BodyDivergence == Clean.
    - mismatching hash → Confirmed.
    - prod BodySha256 null → Indeterminate (explicitly assert it is NOT Clean and NOT skipped from the diff list).
    - ApplyAdoptionsAsync with a Confirmed entry and an EMPTY acknowledged set → assert the local store's UpsertMethodCalls does NOT include that entry (skipped); with the entry's key in the set → assert it IS upserted.
    Update all existing ApplyAdoptionsAsync call sites in the tests to pass an acknowledged set (empty or populated as needed).

    Preserve LF; changed-lines-only.
  </action>
  <verify>
    <automated>dotnet.exe test DeckFlow.Studio.Tests --filter "FullyQualifiedName~PullFromProdCoordinator"</automated>
  </verify>
  <acceptance_criteria>
    - Divergence stamp uses ComputeBodySha256 (grep: the ONE hash surface, no second SHA routine) guarded by ArtifactPathSafety.TryBuildContainedPath.
    - Null prod BodySha256 yields Indeterminate (a test asserts null-hash is NOT Clean and NOT dropped) — the explicit inverse of ContentKbReconcileClassifier's null-skip.
    - Divergence is computed regardless of SyncDiffKind (a ProdNewer-kind mismatch test shows Confirmed).
    - ApplyAdoptionsAsync signature has `IReadOnlySet<string> acknowledgedDivergentKeys`; a Confirmed entry absent from it is skipped (UpsertMethodCalls excludes it); present → adopted.
    - Coordinator test filter passes.
  </acceptance_criteria>
  <done>Every comparable entry is stamped Clean/Confirmed/Indeterminate (null⇒Indeterminate), and the coordinator independently refuses to adopt un-acknowledged divergent entries.</done>
</task>

<task type="auto">
  <name>Task 3: Ratify + regression-lock the field-authority split (SYNC-13, D-02)</name>
  <files>DeckFlow.Studio.Tests/ViewModels/PullFromProdCoordinatorTests.cs</files>
  <read_first>
    - DeckFlow.Studio/ViewModels/PullFromProdCoordinator.cs:137-217 (ApplyAdoptionsAsync — the behavior being locked; note it is UNCHANGED by this task except the Task-2 signature/guard)
    - DeckFlow.Studio.Tests/TestDoubles/FakeContentSiteIndexStore.cs (UpsertMethodCalls, SingleApprovalCalls, IsVisible/IsHidden on stored rows)
    - DeckFlow.Studio.Tests/ViewModels/PullFromProdCoordinatorTests.cs (AdoptEntry helper + repo-body-write convention)
    - .planning/phases/92-pull-hardening/92-CONTEXT.md (D-02 / D-02a field authority)
  </read_first>
  <behavior>
    - Adopting a prod entry calls UpsertContentColumnsOnlyAsync (index columns ← prod) exactly once for that entry.
    - Adopting calls SetApprovalStatusAsync with the PROD row's ApprovalStatus value (approval ← prod-mirror), keyed by the entry's NaturalKeyType/NaturalKeyValue.
    - Adopting NEVER flips is_visible/is_hidden on the local row (preserved-local, D-02).
    - The git-tree body is copied into the live tree (body ← git) when present; a missing body still upserts the row (partial-repo path).
  </behavior>
  <action>
    Add regression tests (no production change — this task RATIFIES the already-correct ApplyAdoptionsAsync) using the existing `Build`/`AdoptEntry` helpers and `FakeContentSiteIndexStore`:
    - `Adopt_UpsertsContentColumnsOnly_NotFullRow`: assert `localStore.UpsertMethodCalls` contains exactly `"UpsertContentColumnsOnlyAsync"` for the adopted entry and NOT any full-row upsert method name.
    - `Adopt_MirrorsProdApprovalStatus`: seed the prod row with a specific ApprovalStatus (e.g. "approved") and assert `localStore.SingleApprovalCalls` contains `(keyType, keyValue, "approved")` matching the entry's natural key.
    - `Adopt_PreservesLocalVisibility`: pre-seed a local row with `IsVisible=true, IsHidden=false` (or false/true), adopt the prod counterpart, and assert the stored row's IsVisible/IsHidden are UNCHANGED after apply (adopt never publishes/hides).
    - `Adopt_CopiesGitBody_WhenPresent`: with a repo body file written under `_repoRoot`, adopt and assert the live-tree destination file exists / body copied; and a companion asserting a missing repo body still upserts the row (partial-repo path, note wording "not copied").
    Pass an acknowledged-divergence set (empty is fine — these fixtures use Clean/NotApplicable entries) to satisfy the Task-2 signature.

    Preserve LF; changed-lines-only. Follow the file's existing xUnit `[Fact]` naming (Method_Scenario_ExpectedResult).
  </action>
  <verify>
    <automated>dotnet.exe test DeckFlow.Studio.Tests --filter "FullyQualifiedName~PullFromProdCoordinator"</automated>
  </verify>
  <acceptance_criteria>
    - A test asserts UpsertContentColumnsOnlyAsync is the only upsert method invoked on adopt (index ← prod, content-only).
    - A test asserts SetApprovalStatusAsync is called with the prod row's ApprovalStatus (approval ← prod-mirror) under the entry's natural key.
    - A test asserts is_visible/is_hidden are unchanged after adopt (preserved-local, D-02).
    - A test asserts the git-tree body is copied on adopt when present (body ← git) and a missing body still upserts.
    - Coordinator test filter passes; full solution build 0/0.
  </acceptance_criteria>
  <done>D-02's field-authority split is regression-locked: body←git, index←prod, approval←prod-mirror, visibility preserved-local.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Studio host → `git` CLI | branch name from GetCurrentBranchAsync flows into fetch/rev-list args |
| prod `artifact_path` → local file read | untrusted path used for the divergence-check File.ReadAllText |
| Studio → prod Postgres | read-only SELECT (IProdContentReader has no write method) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-92-02 | Tampering / Information Disclosure | divergence-check File.ReadAllText(repoBody) | mitigate | Path goes through `ArtifactPathSafety.TryBuildContainedPath(repoRoot, artifactPath, ...)` — the same guard the exists-check already applies; no unguarded read |
| T-92-03 | Information Disclosure | freshness-check catch (GitCommandException) | mitigate | Log full detail via `_logger`; surface only sanitized "could not verify freshness" copy — never `ex.Message` (D-07) |
| T-92-04 | Tampering | adopting a body-vs-index divergent row | mitigate | This phase IS the mitigation: Confirmed/Indeterminate excluded from adopt unless explicitly acknowledged (D-01/D-01a) |
| T-92-06 | Elevation / clobber | ApplyAdoptionsAsync writing is_visible/is_hidden | mitigate | Content-only upsert (UpsertContentColumnsOnlyAsync) never touches visibility; regression-locked by Task 3 (D-02) |
| T-92-SC | Tampering | npm/pip/cargo installs | accept | No package installs this phase (RESEARCH §Standard Stack) |
</threat_model>

<verification>
- `dotnet.exe build DeckFlow.sln` → 0/0 (return-type + signature changes ripple to the page in 92-03, which is a later wave — build the coordinator + its test project here; the page updates in 92-03).
- `dotnet.exe test DeckFlow.Studio.Tests --filter "FullyQualifiedName~PullFromProdCoordinator"` → green.
- Note: `PullFromProd.razor.cs` will not compile against the widened `PullAndClassifyAsync` return type / new `ApplyAdoptionsAsync` param until 92-03 lands — this plan's build verification targets `DeckFlow.Studio.Tests` coordinator tests. Run the full-solution build gate at the 92-03 wave merge.
- EOL clean (LF); changed-lines format gate clean.
</verification>

<success_criteria>
- Staleness pre-check warns-then-proceeds (behind vs unverified distinct); never hard-refuses.
- Every comparable entry stamped Clean/Confirmed/Indeterminate; null prod hash ⇒ Indeterminate (inverse of P91).
- ApplyAdoptionsAsync excludes un-acknowledged divergent entries (defense-in-depth) and its field split (body←git, index←prod, approval←prod, visibility preserved) is regression-locked.
</success_criteria>

<output>
Create `.planning/phases/92-pull-hardening/92-02-SUMMARY.md` when done. Record the exact `PullClassifyResult`/`PullFreshnessStatus` shape and the `ApplyAdoptionsAsync` signature change so 92-03 wires against them without guessing.
</output>
