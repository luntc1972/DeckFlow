---
phase: 21-content-kb-distillation-artifact-emit-local
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - DeckFlow.Core/Content/ILlmSpendLedger.cs
  - DeckFlow.Core/Content/LlmSpendLedger.cs
  - DeckFlow.Core.Tests/LlmSpendLedgerTests.cs
autonomous: false
requirements: [KB-06]
user_setup:
  - service: openai
    why: "LLM distillation chat calls (gpt-4o-mini) reuse the existing OpenAI key"
    env_vars:
      - name: DECKFLOW_LLM_MONTHLY_CAP_USD
        source: "Local environment / operator config — NEW var, separate from DECKFLOW_WHISPER_MONTHLY_CAP_USD; default proposed $5.00 (A2 — confirm name + default at the decision checkpoint in this plan)"
must_haves:
  truths:
    - "A separate LLM spend ledger records per-call token cost into a net-new llm_spend_ledger table"
    - "GetMonthlyTotalAsync returns the app-side sum for a month_key"
    - "WouldExceedCapAsync compares a projected cost + monthly total against DECKFLOW_LLM_MONTHLY_CAP_USD"
    - "The ledger is independent of the Whisper ledger (no shared table, no shared cap)"
  artifacts:
    - path: "DeckFlow.Core/Content/LlmSpendLedger.cs"
      provides: "RecordCallAsync / GetMonthlyTotalAsync / WouldExceedCapAsync over llm_spend_ledger"
      contains: "llm_spend_ledger"
    - path: "DeckFlow.Core/Content/ILlmSpendLedger.cs"
      provides: "Token-based ledger interface mirroring IWhisperSpendLedger"
      contains: "RecordCallAsync"
    - path: "DeckFlow.Core.Tests/LlmSpendLedgerTests.cs"
      provides: "record/total/cap unit coverage against a temp SQLite path"
      contains: "LlmSpendLedger"
  key_links:
    - from: "DeckFlow.Core/Content/LlmSpendLedger.cs"
      to: "content_videos table"
      via: "EnsureSchemaAsync instantiates ContentVideoStore first so the FK parent exists"
      pattern: "new ContentVideoStore"
    - from: "DeckFlow.Core/Content/LlmSpendLedger.cs"
      to: "DECKFLOW_LLM_MONTHLY_CAP_USD"
      via: "configurationValueResolver fallback to Environment.GetEnvironmentVariable"
      pattern: "DECKFLOW_LLM_MONTHLY_CAP_USD"
---

<objective>
Build a SEPARATE LLM spend ledger (D-05) — a net-new `llm_spend_ledger` table + `LlmSpendLedger` class — mirroring `WhisperSpendLedger` exactly, but token-based instead of seconds-based. The Whisper ledger is the precise template; do NOT generalize the two into one (D-05: independent caps, parallel code).

Purpose: Cost guardrail (FM4 / SC5). The orchestrator (Plan 04) calls `WouldExceedCapAsync` BEFORE each paid LLM call and `RecordCallAsync` AFTER with the exact `completion.Usage` token cost. Over-cap → skip + `aborted_reason`.
Output: `ILlmSpendLedger` + `LlmSpendLedger` + unit tests (record/total/cap).

Net-new `CREATE TABLE IF NOT EXISTS` only — zero migration risk against the existing UAT db. NO new NuGet package.

This plan is `autonomous: false`: it contains a blocking decision checkpoint to confirm the gpt-4o-mini price constants (A1) and the cap env-var name + default (A2) before they are locked into code.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-content-kb-distillation-artifact-emit-local/21-RESEARCH.md
@.planning/phases/21-content-kb-distillation-artifact-emit-local/21-PATTERNS.md
@CLAUDE.md

<interfaces>
<!-- Whisper ledger is the exact template. Swap seconds_billed -> input_tokens/output_tokens, table + cap key. -->
From DeckFlow.Core/Content/IWhisperSpendLedger.cs:
  Task RecordCallAsync(long videoId, int secondsBilled, decimal costUsd, string monthKey, CancellationToken ct = default);
  Task<decimal> GetMonthlyTotalAsync(string yearMonth, CancellationToken ct = default);
  Task<bool> WouldExceedCapAsync(decimal projectedCallCostUsd, string monthKey, CancellationToken ct = default);

From DeckFlow.Core/Content/WhisperSpendLedger.cs (key seams):
  ctor(string databasePath, Func<string,string?>? configurationValueResolver = null) : this(RelationalDatabaseConnection.FromSqlitePath(databasePath), resolver)
  EnsureSchemaAsync: new ContentVideoStore(_connectionInfo).EnsureSchemaAsync()  // FK parent first (REVIEW #1)
  private const string MonthlyCapConfigurationKey = "DECKFLOW_WHISPER_MONTHLY_CAP_USD";
  private static readonly decimal DefaultMonthlyCapUsd = 15.00m;
  private const decimal WhisperUsdPerMinute = 0.006m;
  FormatDecimal invariant-culture; dual-dialect Postgres DECIMAL(10,6) / SQLite TEXT
</interfaces>
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Checkpoint: Confirm gpt-4o-mini prices + LLM cap env var (A1/A2)</name>
  <action>PAUSE for user decision: confirm gpt-4o-mini input/output $/1M price constants (A1) and the LLM cap env-var name + default (A2) before they are written into LlmSpendLedger. Do not proceed to Task 1 until confirmed.</action>
  <decision>Lock the gpt-4o-mini price constants and the LLM cap env-var name + default before they go into code.</decision>
  <context>
    A1: AI-SPEC §4 cites gpt-4o-mini ≈ $0.15 / 1M input tokens and ≈ $0.60 / 1M output tokens, but prices drift and must be verified at platform.openai.com before locking. A2: RESEARCH Q3 proposes a NEW env var `DECKFLOW_LLM_MONTHLY_CAP_USD` (mirroring the Whisper key naming) with a default of $5.00 (AI-SPEC §4 estimates ~$1.10 for a 200-video harvest, so $5.00 is generous headroom). These three values (input price, output price, env-var name+default) are operator-facing config and cost-correctness-critical — confirm before locking.
  </context>
  <options>
    <option id="accept-defaults">
      <name>Accept the researched defaults</name>
      <pros>Fastest; values are conservative and well-sourced (AI-SPEC §4)</pros>
      <cons>Prices may have drifted since the AI-SPEC was written; user may prefer a different cap/name</cons>
    </option>
    <option id="adjust">
      <name>Adjust prices / cap / env-var name</name>
      <pros>Matches current OpenAI pricing + operator preference exactly</pros>
      <cons>Requires the user to look up current rates</cons>
    </option>
  </options>
  <resume-signal>Confirm: input $/1M, output $/1M, env var name, default cap USD (e.g. "use $0.15 / $0.60, DECKFLOW_LLM_MONTHLY_CAP_USD, $5.00")</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 1: LlmSpendLedger + interface + record/total/cap tests</name>
  <files>DeckFlow.Core/Content/ILlmSpendLedger.cs, DeckFlow.Core/Content/LlmSpendLedger.cs, DeckFlow.Core.Tests/LlmSpendLedgerTests.cs</files>
  <read_first>
    - DeckFlow.Core/Content/WhisperSpendLedger.cs (FULL — §25-46 dual ctor + SQLite dir creation, §60-68 FK-parent EnsureSchemaAsync ordering with its `// Why:` comment, §155-219 dual-dialect DDL + FormatDecimal, §166-182 ReadMonthlyCapUsd resolver, §12-16 cap key/default/WhisperUsdPerMinute consts)
    - DeckFlow.Core/Content/IWhisperSpendLedger.cs (FULL — interface + doc-comment style)
    - DeckFlow.Core/Content/ContentVideoStore.cs (the `content_videos(id)` FK parent + its EnsureSchemaAsync — confirm the FK target column name)
    - DeckFlow.Core.Tests/ existing WhisperSpendLedger test (the temp-SQLite-path + `configurationValueResolver` deterministic-cap pattern to mirror)
    - .planning/phases/21-content-kb-distillation-artifact-emit-local/21-PATTERNS.md §"LlmSpendLedger" (swap-list + `[ASSUMED]` tags)
  </read_first>
  <behavior>
    - RecordCallAsync(videoId, inputTokens, outputTokens, costUsd, monthKey) inserts one ledger row; GetMonthlyTotalAsync(month) sums cost_usd for that month_key.
    - WouldExceedCapAsync(projected, month) returns true when monthly total + projected > cap, false otherwise (cap from resolver/default).
    - Cost from tokens: `inputTokens/1e6 * InputPrice + outputTokens/1e6 * OutputPrice` (decimal consts, confirmed at the checkpoint).
    - EnsureSchemaAsync is idempotent (CREATE TABLE IF NOT EXISTS) and creates content_videos FK parent first.
    - Ledger is independent of WhisperSpendLedger — different table, different cap key.
  </behavior>
  <action>
    Create `ILlmSpendLedger.cs` in `DeckFlow.Core/Content` mirroring `IWhisperSpendLedger` with token args: `Task RecordCallAsync(long videoId, int inputTokens, int outputTokens, decimal costUsd, string monthKey, CancellationToken ct = default)`, `Task<decimal> GetMonthlyTotalAsync(string yearMonth, CancellationToken ct = default)`, `Task<bool> WouldExceedCapAsync(decimal projectedCallCostUsd, string monthKey, CancellationToken ct = default)`. XML-doc each member. Create `LlmSpendLedger.cs` in `DeckFlow.Core/Content` mirroring `WhisperSpendLedger` exactly: dual ctor (`string databasePath` → `RelationalDatabaseConnection.FromSqlitePath`, plus the `RelationalDatabaseConnection` + `Func<string,string?>?` overload), SQLite directory creation, `_schemaGate`/`_schemaReady` double-checked `EnsureSchemaAsync`. In `EnsureSchemaAsync`, FIRST `await new ContentVideoStore(_connectionInfo).EnsureSchemaAsync(...)` (copy the `// Why: REVIEW #1 ... FK parent` comment) THEN `CREATE TABLE IF NOT EXISTS llm_spend_ledger`. DDL columns: `video_id` (FK → `content_videos(id) ON DELETE CASCADE`), `input_tokens INT NOT NULL`, `output_tokens INT NOT NULL`, `cost_usd` (Postgres `DECIMAL(10,6)` / SQLite `TEXT`), `month_key`, `created_utc`; index `ix_llm_spend_month` on `month_key`. Use the dual-dialect raw-string blocks + `RelationalDatabaseConnection.AddParameter` + invariant-culture `FormatDecimal` exactly like the Whisper analog (do NOT re-indent the raw-string DDL — R-6). Add `private const string MonthlyCapConfigurationKey = "DECKFLOW_LLM_MONTHLY_CAP_USD"` and `private static readonly decimal DefaultMonthlyCapUsd` using the values CONFIRMED at the decision checkpoint; copy `ReadMonthlyCapUsd` from the Whisper analog (resolver → env fallback → default). Add `private const decimal Gpt4oMiniInputUsdPerMillion` / `Gpt4oMiniOutputUsdPerMillion` set to the CONFIRMED prices, with a `// Why: gpt-4o-mini pricing confirmed <date> per platform.openai.com (A1)` comment; provide a small `static decimal ComputeCostUsd(int inputTokens, int outputTokens)` helper (mirrors `WhisperUsdPerMinute` usage) so the orchestrator records EXACT cost from `completion.Usage` (not estimate). Create `LlmSpendLedgerTests.cs` against a temp SQLite path, using `configurationValueResolver` to set a deterministic cap: assert RecordCall→GetMonthlyTotal sums correctly, WouldExceedCap true above cap / false below, and ComputeCostUsd math for a known token pair. `.ConfigureAwait(false)` throughout.
  </action>
  <verify>
    <automated>"/mnt/c/Program Files/dotnet/dotnet.exe" build DeckFlow.sln -c Debug 2>&1 | grep -iE ": error|: warning" || echo "clean build"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'llm_spend_ledger' DeckFlow.Core/Content/LlmSpendLedger.cs` returns ≥ 3 (both dialect DDLs + index).
    - `grep -c 'DECKFLOW_LLM_MONTHLY_CAP_USD' DeckFlow.Core/Content/LlmSpendLedger.cs` returns ≥ 1 and equals the value confirmed at the checkpoint.
    - `grep -c 'new ContentVideoStore' DeckFlow.Core/Content/LlmSpendLedger.cs` returns ≥ 1 (FK parent created first).
    - `grep -ciE 'whisper_spend_ledger|seconds_billed' DeckFlow.Core/Content/LlmSpendLedger.cs` returns 0 (independent ledger, not the Whisper table).
    - `"/mnt/c/Program Files/dotnet/dotnet.exe" test DeckFlow.Core.Tests --filter "FullyQualifiedName~LlmSpendLedger"` passes Failed:0 (or CI-confirmed per CLAUDE.md WSL note).
    - `git diff DeckFlow.Core/DeckFlow.Core.csproj` shows no new PackageReference.
  </acceptance_criteria>
  <done>A separate token-based `llm_spend_ledger` + `LlmSpendLedger` exist, record/total/cap pass, the FK parent is created first, and the ledger shares nothing with the Whisper ledger.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Orchestrator → ledger | Cost values written/read; cap gate decides whether a paid call proceeds |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-04 | Denial of service (cost) | Uncapped LLM spend | mitigate | `WouldExceedCapAsync` BEFORE each paid call (Plan 04); over-cap → skip + aborted_reason; cap from DECKFLOW_LLM_MONTHLY_CAP_USD |
| T-21-05 | Tampering | Ledger cost rows | accept | Local single-operator SQLite db; no remote write path; low value target |
| T-21-SC | Tampering | nuget installs | mitigate | ZERO new packages; acceptance criterion asserts no PackageReference diff |
</threat_model>

<verification>
- Clean `dotnet build DeckFlow.sln`.
- Ledger record/total/cap tests pass (or CI-confirmed).
- `llm_spend_ledger` is net-new; no overlap with `whisper_spend_ledger`.
- Price constants + cap env var match the values confirmed at the decision checkpoint.
</verification>

<success_criteria>
- Separate `LlmSpendLedger` over net-new `llm_spend_ledger` (D-05).
- Token-based exact cost from `completion.Usage`; cap via `DECKFLOW_LLM_MONTHLY_CAP_USD`.
- FK parent created first; zero migration risk; zero new packages.
</success_criteria>

<output>
Create `.planning/phases/21-content-kb-distillation-artifact-emit-local/21-02-SUMMARY.md` when done.
</output>
