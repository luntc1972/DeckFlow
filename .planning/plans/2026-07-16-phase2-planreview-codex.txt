REQUEST_CHANGES

**HIGH**
- `.planning/plans/2026-07-16-phase2-baseline-weighting.md:256-260` and test at `:129-139`: mid-sample with no global fallback returns the commander value as `Commander`. That does not match the confidence-weighted contract or the spec testing note “missing global → omit.” For `LOW <= count < HIGH`, the helper cannot compute the required blend without `globalAvg`; returning the commander value silently upgrades a weak sample to full trust. Fix: return `ManabaseBaselineMetric(null, None)` for mid-band missing global, and update the test to assert `null/None`.

**MEDIUM**
- `:49-89`, `:64-75`, `:77-89`: tests miss the exact threshold boundaries. Add cases for `commanderDeckCount == LowDeckThreshold` and `== HighDeckThreshold`. Expected: at `LOW`, `w = 0`, value equals global and source is `Blended`; at `HIGH`, source/value are commander.
- `:235` and tests: `TotalSources` only has broad coverage. Add a test where one of lands/ramp is null and the other present, to prove `TotalSources == null`. This matters because metrics are independently nullable.
- `:166-175`: type shape is mostly sensible, but downstream Component 3 asks for a result-level `Source`, while this plan only has per-metric sources. Per-metric source is useful and probably better, but the plan should explicitly say downstream derives any aggregate label from metric sources, or add a result-level aggregate source if UI/analyzer expects one.

**LOW**
- `:71-74`, `:85-88`, `:123-126`: several tests assert only lands source, not ramp/draw source. Add source assertions for all metrics in thin/blended/solid cases so a broken per-metric source cannot pass.
- `:240-264`: negative `commanderDeckCount` is implicitly treated as thin, which is fine, but the plan should state that. For negative metric averages, either reject with `ArgumentOutOfRangeException` or document that upstream aggregation guarantees non-negative values.
- `:287-297`: `/simplify` is ambiguous as an implementation step unless every worker has that command. Replace with “review for simplification” or make it optional.
- `:307`: trailing code fence appears stray; clean up the markdown.

The core blend math direction and `< LOW` / `>= HIGH` implementation are otherwise correct.