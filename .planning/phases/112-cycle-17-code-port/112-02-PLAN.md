---
phase: 112-cycle-17-code-port
plan: 02
type: execute
wave: 2
depends_on: ["112-01"]
files_modified:
  - DeckFlow.Core/Content/*Creator*.cs
  - DeckFlow.Core/Knowledge/CardGrounding/*.cs
  - DeckFlow.Core/Knowledge/CreatorStyleProfile.cs
  - DeckFlow.Core/Knowledge/CreatorStyleProfileSections.cs
  - DeckFlow.Core/Knowledge/CreatorStyleRubric/*.cs
  - DeckFlow.Core/Knowledge/GlobalCategoryBaseline.cs
  - DeckFlow.Core/Knowledge/MeasuredStyleExtraction/*.cs
  - DeckFlow.Core/Knowledge/ProfileFusion/*.cs
  - DeckFlow.Core/Knowledge/StatedRulesExtraction/*.cs
  - DeckFlow.Core.Tests/DeckFlow.Core.Tests.csproj
  - DeckFlow.Core.Tests/CreatorDeckCacheStoreTests.cs
  - DeckFlow.Core.Tests/CreatorProfileSourceStoreTests.cs
  - DeckFlow.Core.Tests/CreatorStyleProfileAdditiveRoundTripTests.cs
  - DeckFlow.Core.Tests/CreatorStyleProfileStoreTests.cs
  - DeckFlow.Core.Tests/CreatorStyleProfileTestData.cs
  - DeckFlow.Core.Tests/Knowledge/CardGrounding/CardGroundingRulesTests.cs
  - DeckFlow.Core.Tests/Knowledge/CreatorStyleRubric/CreatorStyleRubricScorerTests.cs
  - DeckFlow.Core.Tests/MeasuredStyleExtraction/*.cs
  - DeckFlow.Core.Tests/ProfileFusion/*.cs
  - DeckFlow.Core.Tests/StatedRulesExtraction/*
  - DeckFlow.Core/Integration/ILlmDistillationService.cs
  - DeckFlow.Core/Knowledge/DistillationResults.cs
  - DeckFlow.Core/Knowledge/DistillationValidation.cs
autonomous: true
requirements: [PORT-01]

must_haves:
  truths:
    - "The 64 allowlisted Cycle 17 Core files (engine + Core tests + golden fixture) exist in the working tree, brought over by path-allowlist checkout (D-05), never by diff-apply or cherry-pick."
    - "No file outside the published Commit 1 allowlist arrived — in particular none of the 37 Cycle-16 contamination files already on main, and none of D-12's never-port list (D-08, D-12/D-18)."
    - "The two dual-class Core test files carry only their plain-SQLite class; every Postgres-fixture class is gone, so no Testcontainers.PostgreSql package reference is needed (D-15 as sharpened by RESEARCH)."
    - "The three CLI-scoped Core test files that belong to Phase 115 were not ported (D-14 as sharpened by RESEARCH)."
    - "ILlmDistillationService.cs, DistillationResults.cs and DistillationValidation.cs gained only NEW members appended onto main's CURRENT content; ExtractCombinedAsync and CombinedExtractionResult still exist (D-09, RESEARCH Finding 5)."
  artifacts:
    - path: "DeckFlow.Core/Knowledge/ProfileFusion/ProfileFusionEngine.cs"
      provides: "Cycle 17 profile fusion engine (Phase 97)"
    - path: "DeckFlow.Core/Knowledge/StatedRulesExtraction/StatedRulesExtractor.cs"
      provides: "Cycle 17 stated-rules extraction (Phase 96)"
    - path: "DeckFlow.Core/Knowledge/CardGrounding/CardGroundingRules.cs"
      provides: "Cycle 17 card-grounding rules (Phase 98)"
    - path: "DeckFlow.Core.Tests/StatedRulesExtraction/Fixtures/salubrious-snail-transcript.txt"
      provides: "Golden transcript fixture for StatedRulesExtractorTests"
  key_links:
    - from: "DeckFlow.Core/Knowledge/StatedRulesExtraction/StatedRulesExtractor.cs"
      to: "DeckFlow.Core/Integration/ILlmDistillationService.cs"
      via: "four new stated-rules interface methods"
      pattern: "SelectStatedClaimsAsync|DisambiguateStatedClaimsAsync|DecomposeStatedClaimsAsync|ReduceStatedRulesAsync"
    - from: "DeckFlow.Core.Tests/DeckFlow.Core.Tests.csproj"
      to: "DeckFlow.Core.Tests/StatedRulesExtraction/Fixtures/salubrious-snail-transcript.txt"
      via: "None Include copy-to-output item"
      pattern: "salubrious-snail-transcript"
---

<objective>
Land the Cycle 17 Core engine files (Phases 94-98) and their Core test suite on `feat/personal-tools` via path-allowlist checkout, then hand-apply the three highest-risk Core M-file hunks (the LLM distillation stack).

Purpose: this is the first half of D-07's Commit 1. Core has zero Web coupling — it reaches Scryfall only through the Core-side `ICardNameGrounder` interface — so this content is genuinely additive. The distillation-stack hunks are separated into their own task because `ILlmDistillationService.cs` and `DistillationResults.cs` are two of the three files where RESEARCH empirically proved a wholesale branch-file copy DELETES members main added after the fork.

Output: Core engine + Core tests present, distillation stack extended. This plan does NOT build clean on its own and does NOT commit — plan 03 applies the remaining six Core hunks, runs the gates, and creates Commit 1.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/112-cycle-17-code-port/112-RESEARCH.md
@.planning/phases/112-cycle-17-code-port/112-CONTEXT.md
@.planning/phases/112-cycle-17-code-port/112-BASELINE.md
@CLAUDE.md
</context>

<environment>
- Branch: `feat/personal-tools`. dotnet is `"/mnt/c/Program Files/dotnet/dotnet.exe"`. Do NOT set `MTG_DATA_DIR`.
- `plan/cycle-17-creator-style` (head `6da5eb42`) is READ-ONLY. `git checkout <ref> -- <explicit paths>` is permitted ONLY for the added-file paths published below. NEVER check the branch out as a working branch, never rebase it, never merge it.
- `.gitattributes` enforces LF line endings.
- **NEVER `git checkout plan/cycle-17-creator-style -- <file>` for a file that already exists on main.** RESEARCH Finding 5 is the empirical proof: doing that silently deleted main-only members and broke the build. Every M-file hunk starts from `git show HEAD:<path>` and appends only new members.
- No new `PackageReference` may be added anywhere in this phase (CLAUDE.md). `Testcontainers.PostgreSql` is specifically forbidden.
</environment>

<port_allowlist>
## Commit 1 allowlist — 64 paths, checkout from `plan/cycle-17-creator-style` (D-05)

Core engine (40):
DeckFlow.Core/Content/CreatorDeckCacheEntry.cs
DeckFlow.Core/Content/CreatorDeckCacheStore.cs
DeckFlow.Core/Content/CreatorProfileSource.cs
DeckFlow.Core/Content/CreatorProfileSourceStore.cs
DeckFlow.Core/Content/CreatorStyleProfileReadModel.cs
DeckFlow.Core/Content/CreatorStyleProfileStore.cs
DeckFlow.Core/Content/CreatorStyleProfileSummary.cs
DeckFlow.Core/Content/ICreatorDeckCacheStore.cs
DeckFlow.Core/Content/ICreatorProfileSourceStore.cs
DeckFlow.Core/Content/ICreatorStyleProfileStore.cs
DeckFlow.Core/Knowledge/CardGrounding/CardGroundingBatchResult.cs
DeckFlow.Core/Knowledge/CardGrounding/CardGroundingDeckContext.cs
DeckFlow.Core/Knowledge/CardGrounding/CardGroundingRejectReason.cs
DeckFlow.Core/Knowledge/CardGrounding/CardGroundingRules.cs
DeckFlow.Core/Knowledge/CardGrounding/CardGroundingVerdict.cs
DeckFlow.Core/Knowledge/CardGrounding/ICardGroundingGuard.cs
DeckFlow.Core/Knowledge/CreatorStyleProfile.cs
DeckFlow.Core/Knowledge/CreatorStyleProfileSections.cs
DeckFlow.Core/Knowledge/CreatorStyleRubric/CreatorStyleRubricScorer.cs
DeckFlow.Core/Knowledge/CreatorStyleRubric/RubricScoreResult.cs
DeckFlow.Core/Knowledge/CreatorStyleRubric/SubmittedDeckStats.cs
DeckFlow.Core/Knowledge/GlobalCategoryBaseline.cs
DeckFlow.Core/Knowledge/MeasuredStyleExtraction/CategoryCounter.cs
DeckFlow.Core/Knowledge/MeasuredStyleExtraction/CreatorDeckSample.cs
DeckFlow.Core/Knowledge/MeasuredStyleExtraction/FolderWeighting.cs
DeckFlow.Core/Knowledge/MeasuredStyleExtraction/LiftCalculator.cs
DeckFlow.Core/Knowledge/MeasuredStyleExtraction/MeasuredStyleInputs.cs
DeckFlow.Core/Knowledge/MeasuredStyleExtraction/StapleStripper.cs
DeckFlow.Core/Knowledge/ProfileFusion/ConflictCalculator.cs
DeckFlow.Core/Knowledge/ProfileFusion/MetricClassification.cs
DeckFlow.Core/Knowledge/ProfileFusion/ProfileFusionEngine.cs
DeckFlow.Core/Knowledge/ProfileFusion/StatedMetricKeyMapper.cs
DeckFlow.Core/Knowledge/ProfileFusion/StatedRuleRecencyCollapser.cs
DeckFlow.Core/Knowledge/StatedRulesExtraction/ContentTypeHeuristic.cs
DeckFlow.Core/Knowledge/StatedRulesExtraction/ICardNameGrounder.cs
DeckFlow.Core/Knowledge/StatedRulesExtraction/StatedRuleCandidate.cs
DeckFlow.Core/Knowledge/StatedRulesExtraction/StatedRuleReducer.cs
DeckFlow.Core/Knowledge/StatedRulesExtraction/StatedRulesExtractor.cs
DeckFlow.Core/Knowledge/StatedRulesExtraction/StatedRulesMetricVocabulary.cs
DeckFlow.Core/Knowledge/StatedRulesExtraction/TranscriptChunker.cs

Core tests (24):
DeckFlow.Core.Tests/CreatorDeckCacheStoreTests.cs
DeckFlow.Core.Tests/CreatorProfileSourceStoreTests.cs
DeckFlow.Core.Tests/CreatorStyleProfileAdditiveRoundTripTests.cs
DeckFlow.Core.Tests/CreatorStyleProfileStoreTests.cs
DeckFlow.Core.Tests/CreatorStyleProfileTestData.cs
DeckFlow.Core.Tests/Knowledge/CardGrounding/CardGroundingRulesTests.cs
DeckFlow.Core.Tests/Knowledge/CreatorStyleRubric/CreatorStyleRubricScorerTests.cs
DeckFlow.Core.Tests/MeasuredStyleExtraction/CategoryCounterTests.cs
DeckFlow.Core.Tests/MeasuredStyleExtraction/FolderWeightingTests.cs
DeckFlow.Core.Tests/MeasuredStyleExtraction/LiftCalculatorTests.cs
DeckFlow.Core.Tests/MeasuredStyleExtraction/StapleStripperTests.cs
DeckFlow.Core.Tests/ProfileFusion/ConflictCalculatorTests.cs
DeckFlow.Core.Tests/ProfileFusion/MetricClassificationTests.cs
DeckFlow.Core.Tests/ProfileFusion/ProfileFusionEngineTests.cs
DeckFlow.Core.Tests/ProfileFusion/StatedMetricKeyMapperTests.cs
DeckFlow.Core.Tests/ProfileFusion/StatedRuleRecencyCollapserTests.cs
DeckFlow.Core.Tests/StatedRulesExtraction/CliLlmDistillationStatedRulesGoldenTests.cs
DeckFlow.Core.Tests/StatedRulesExtraction/ContentTypeHeuristicTests.cs
DeckFlow.Core.Tests/StatedRulesExtraction/Fixtures/salubrious-snail-transcript.txt
DeckFlow.Core.Tests/StatedRulesExtraction/StatedRuleCandidateVocabularyTests.cs
DeckFlow.Core.Tests/StatedRulesExtraction/StatedRuleReducerTests.cs
DeckFlow.Core.Tests/StatedRulesExtraction/StatedRulesExtractorTests.cs
DeckFlow.Core.Tests/StatedRulesExtraction/TranscriptChunkerTests.cs
DeckFlow.Core.Tests/StatedRulesExtraction/ValidateStatedRulesTests.cs
</port_allowlist>

<never_port_list>
## Files that must NOT arrive at Phase 112 — no exceptions

D-12 never-port list (Phase-100 public plumbing; the ONLY ratified exception is `PacketSessionCache.cs`, which belongs to plan 04, not this plan):
- DeckFlow.Web/Services/FeatureFlags/FeatureFlagCatalog.cs
- DeckFlow.Web/Services/FeatureFlags/FeatureFlagStore.cs
- any ToolRegistry file
- DeckFlow.Web/Models/DeckPageTab.cs
- DeckFlow.Web/Help/creator-style.md
- any SeoPaths / sitemap file

D-15 as sharpened by RESEARCH — Postgres-fixture Core tests, excluded wholesale:
- DeckFlow.Core.Tests/Integration/PostgresContainerFixture.cs
- DeckFlow.Core.Tests/Integration/PostgresFactAttribute.cs
- DeckFlow.Core.Tests/CreatorStyleProfileStorePostgresTests.cs
- DeckFlow.Core.Tests/ContentVideoStoreStatedRulesReadTests.cs

D-14 as sharpened by RESEARCH — CLI-layer tests that belong to Phase 115:
- DeckFlow.Core.Tests/Content/CreatorStyleSeedSerializationTests.cs
- DeckFlow.Core.Tests/ProfileFusion/FuseProfileRunnerTests.cs
(`ProfileFusionEngineTests.cs` tests the engine directly and DOES port — do not confuse the two.)

Phase-100 public-surface suites (never ported at any phase): the six feature-flag lockstep suites gated on `tool.creator-style.enabled`, `ToolRegistryTests` count assertions for a creator-style tile, route-gate coverage for `/creator-style`, and any sitemap/SEO assertion mentioning creator-style. None of these are in the 112 candidate set; there is nothing to strip, only something to never introduce.
</never_port_list>

<tasks>

<task type="auto">
  <name>Task 1: Path-allowlist checkout of the 64 Core files, with the two Postgres class trims and the fixture csproj item</name>
  <files>
    The 64 paths in the port_allowlist above, plus DeckFlow.Core.Tests/DeckFlow.Core.Tests.csproj
  </files>
  <read_first>
    - The `<port_allowlist>` and `<never_port_list>` sections of THIS plan — they are the authoritative file manifest.
    - `.planning/phases/112-cycle-17-code-port/112-RESEARCH.md` §"Port Allowlist — Commit 1 (Core)" and §"Test Port Inventory" (the exact classes to delete from the two dual-class test files, and the full exclusion table).
    - `.planning/phases/112-cycle-17-code-port/112-BASELINE.md` §"Drift preflight" — must read `VERDICT: GO` before starting. If it reads NO-GO, STOP.
    - Current `DeckFlow.Core.Tests/DeckFlow.Core.Tests.csproj` — read it to copy the existing `<None Include="Orchestration/Fixtures/index-seed.golden.json">` item's exact shape (ItemGroup placement, `CopyToOutputDirectory` child element) before adding the new one.
    - `git show plan/cycle-17-creator-style:DeckFlow.Core.Tests/CreatorDeckCacheStoreTests.cs` and the same for `CreatorProfileSourceStoreTests.cs`, to locate the class boundaries before trimming.
  </read_first>
  <action>
    Run a single `git checkout plan/cycle-17-creator-style -- <paths>` invocation listing exactly the 64 paths published in this plan's `<port_allowlist>`. One command, explicit paths, no globs and no directory arguments — a glob could sweep in a contamination file. Do NOT use `git cherry-pick`, `git apply`, `git merge`, or `git rebase`.

    Trim the two dual-class Core test files. Each contains a plain-SQLite test class (KEEP) followed by a Postgres-fixture class (DELETE):
    - `DeckFlow.Core.Tests/CreatorDeckCacheStoreTests.cs` — keep class `CreatorDeckCacheStoreTests`; delete the trailing class `CreatorDeckCacheStoreTestsPostgres` (declared `: IClassFixture<PostgresContainerFixture>`, starts around line 282 on the branch) in full, including its attributes, members, and closing brace.
    - `DeckFlow.Core.Tests/CreatorProfileSourceStoreTests.cs` — keep class `CreatorProfileSourceStoreTests`; delete the trailing class `CreatorProfileSourceStoreTestsPostgres` (same fixture pattern, starts around line 166).
    After trimming, remove any `using` directive that became unreferenced (e.g. a `DeckFlow.Core.Tests.Integration` import) so no CS8019/unused-using warning is introduced. Do NOT delete the `PostgresContainerFixture` type from anywhere — it was never ported, and no reference to it may remain.

    Add ONE `<None Include="StatedRulesExtraction/Fixtures/salubrious-snail-transcript.txt">` item to `DeckFlow.Core.Tests/DeckFlow.Core.Tests.csproj`, modelled byte-for-byte on the existing `<None Include="Orchestration/Fixtures/index-seed.golden.json">` item (same `<CopyToOutputDirectory>` child value, same indentation). This is VALIDATION.md Wave 0 item 2 and is a build-verified requirement of `StatedRulesExtractorTests.cs`. Do NOT add any `<PackageReference>` — specifically not `Testcontainers.PostgreSql` (D-15 excludes every test that would need it; CLAUDE.md forbids unapproved package additions).

    Do not stage or commit. Plan 03 owns Commit 1.
  </action>
  <verify>
    <automated>git status --porcelain | grep -c '^A\|^??' &amp;&amp; test $(git status --porcelain --untracked-files=all -- DeckFlow.Core DeckFlow.Core.Tests | wc -l) -ge 64 &amp;&amp; grep -c 'salubrious-snail-transcript' DeckFlow.Core.Tests/DeckFlow.Core.Tests.csproj</automated>
  </verify>
  <acceptance_criteria>
    - All 64 allowlisted paths exist in the working tree: a loop `for p in <64 paths>; do test -e "$p" || echo MISSING "$p"; done` prints nothing.
    - `grep -rc 'PostgresContainerFixture' DeckFlow.Core.Tests/` returns 0 matching lines across the whole test project (the fixture was never ported and no surviving reference may exist).
    - `grep -c 'class CreatorDeckCacheStoreTestsPostgres' DeckFlow.Core.Tests/CreatorDeckCacheStoreTests.cs` returns 0, and `grep -c 'class CreatorDeckCacheStoreTests' ` on the same file returns 1.
    - `grep -c 'class CreatorProfileSourceStoreTestsPostgres' DeckFlow.Core.Tests/CreatorProfileSourceStoreTests.cs` returns 0, and the plain `CreatorProfileSourceStoreTests` class is still present.
    - None of these exist in the working tree: `DeckFlow.Core.Tests/Integration/PostgresContainerFixture.cs`, `DeckFlow.Core.Tests/Integration/PostgresFactAttribute.cs`, `DeckFlow.Core.Tests/CreatorStyleProfileStorePostgresTests.cs`, `DeckFlow.Core.Tests/ContentVideoStoreStatedRulesReadTests.cs`, `DeckFlow.Core.Tests/Content/CreatorStyleSeedSerializationTests.cs`, `DeckFlow.Core.Tests/ProfileFusion/FuseProfileRunnerTests.cs`.
    - `DeckFlow.Core.Tests/ProfileFusion/ProfileFusionEngineTests.cs` DOES exist.
    - `DeckFlow.Core.Tests/DeckFlow.Core.Tests.csproj` contains the literal string `StatedRulesExtraction/Fixtures/salubrious-snail-transcript.txt` inside a `<None Include=...>` element.
    - `git diff HEAD -- DeckFlow.Core.Tests/DeckFlow.Core.Tests.csproj | grep -c 'PackageReference'` returns 0 — no package was added.
    - `git status --porcelain` shows zero files under `DeckFlow.Web/`, `DeckFlow.Web.Tests/`, `DeckFlow.CLI/`, or `content-kb/` — this plan touches Core only.
    - `git status --porcelain` shows zero entries for any path in this plan's `<never_port_list>`.
  </acceptance_criteria>
  <done>The 64 Core engine and Core test files are in the working tree, the two Postgres classes are gone, and the golden-fixture csproj item is present — with no package addition and no contamination.</done>
</task>

<task type="auto">
  <name>Task 2: Hand-apply the three LLM-distillation Core hunks onto main's current content</name>
  <files>
    DeckFlow.Core/Integration/ILlmDistillationService.cs,
    DeckFlow.Core/Knowledge/DistillationResults.cs,
    DeckFlow.Core/Knowledge/DistillationValidation.cs
  </files>
  <read_first>
    - `git show HEAD:DeckFlow.Core/Integration/ILlmDistillationService.cs` — main's CURRENT content. It contains `ExtractCombinedAsync`, which the Cycle 17 branch does NOT have. This is the file that broke the build during RESEARCH when copied wholesale.
    - `git show HEAD:DeckFlow.Core/Knowledge/DistillationResults.cs` — contains main-only `CombinedExtractionResult`.
    - `git show HEAD:DeckFlow.Core/Knowledge/DistillationValidation.cs` — main has diverged here since the fork with unrelated additions; anchor points must be re-verified, not assumed.
    - `git show plan/cycle-17-creator-style:<same three paths>` — the source of the NEW members only.
    - `.planning/phases/112-cycle-17-code-port/112-RESEARCH.md` §"M-File Hunk Inventory" rows 3-5, and §"Critical Findings" #5 and §"Common Pitfalls" Pitfall 1.
    - `DeckFlow.Core/Knowledge/StatedRulesExtraction/StatedRulesExtractor.cs` (just ported) — the consumer that determines the exact required signatures.
    - `CLAUDE.md` §Conventions (xmldoc on public members, file-scoped namespaces, 4-space indent, Allman braces, `Async` suffix, `CancellationToken` last).
  </read_first>
  <action>
    For each of the three files, start from main's CURRENT content in the working tree and APPEND only the new members. Never run `git checkout plan/cycle-17-creator-style -- <these paths>`; never replace whole-file content. Diff the branch version against HEAD's version to read the new members, then hand-write them into the existing file at a verified anchor, leaving every existing member byte-identical.

    `DeckFlow.Core/Integration/ILlmDistillationService.cs` — add four interface methods, each with a `NotSupportedException`-throwing default implementation so no existing implementer breaks: `SelectStatedClaimsAsync`, `DisambiguateStatedClaimsAsync`, `DecomposeStatedClaimsAsync`, `ReduceStatedRulesAsync`. Signatures and return types must match exactly what `StatedRulesExtractor.cs` calls. Preserve `ExtractCombinedAsync` untouched.

    `DeckFlow.Core/Knowledge/DistillationResults.cs` — add four new `sealed record` result types: `SelectResult`, `DisambiguateResult`, `DecomposeResult`, `ReduceResult` — the return payloads of the four methods above. Append them; do not reorder or relocate the existing `CombinedExtractionResult` record.

    `DeckFlow.Core/Knowledge/DistillationValidation.cs` — add one const `MaxStatedRulesPerVideo = 40`; two internal static methods `SanitizeStatedRules` and `ValidateStatedRules`; and four payload records `SelectPayload`, `DisambiguatePayload`, `StatedRulePayload`, `RulesPayload`. These depend only on `StatedRuleCandidate` and `StatedRulesMetricVocabulary`, both ported in task 1. Re-verify each insertion anchor against main's current content before writing — RESEARCH flags this file as having diverged since the fork.

    Honor the five `.editorconfig` carve-outs while writing: never convert `{ get; init; }` to `{ get; }` (System.Text.Json silently skips get-only properties in .NET 9+ and this has broken `EdhTop16Client` before), never inline an `[Attribute]` onto the property line, never re-indent a C# raw-string literal, preserve switch expressions, preserve xmldoc single-space indent. Write LF line endings only.

    Treat RESEARCH's code blocks as establishing the correct MEMBERS, not as copy-paste-ready text — RESEARCH explicitly states its hand-authored hunks were not run through the format gate.

    Do not stage or commit. Plan 03 owns Commit 1.
  </action>
  <verify>
    <automated>grep -q 'ExtractCombinedAsync' DeckFlow.Core/Integration/ILlmDistillationService.cs &amp;&amp; grep -q 'CombinedExtractionResult' DeckFlow.Core/Knowledge/DistillationResults.cs &amp;&amp; grep -q 'SelectStatedClaimsAsync' DeckFlow.Core/Integration/ILlmDistillationService.cs</automated>
  </verify>
  <acceptance_criteria>
    - `DeckFlow.Core/Integration/ILlmDistillationService.cs` contains all four of `SelectStatedClaimsAsync`, `DisambiguateStatedClaimsAsync`, `DecomposeStatedClaimsAsync`, `ReduceStatedRulesAsync`, AND still contains `ExtractCombinedAsync`.
    - `git diff HEAD -- DeckFlow.Core/Integration/ILlmDistillationService.cs | grep -c '^-[^-]'` returns 0 — the hunk is additive-only, zero deleted lines.
    - `git diff HEAD -- DeckFlow.Core/Knowledge/DistillationResults.cs | grep -c '^-[^-]'` returns 0, and the file contains `CombinedExtractionResult`, `SelectResult`, `DisambiguateResult`, `DecomposeResult`, `ReduceResult`.
    - `git diff HEAD -- DeckFlow.Core/Knowledge/DistillationValidation.cs | grep -c '^-[^-]'` returns 0, and the file contains `MaxStatedRulesPerVideo`, `SanitizeStatedRules`, `ValidateStatedRules`, `SelectPayload`, `DisambiguatePayload`, `StatedRulePayload`, `RulesPayload`.
    - For each of the three files, `git show HEAD:<path> | grep -c $'\r'` and the working-tree `grep -c $'\r' <path>` both return 0 — LF endings preserved.
    - No `{ get; }` appears where the branch source had `{ get; init; }`: `git diff HEAD | grep -c 'get; }'` on these three files returns 0 additions of that form.
  </acceptance_criteria>
  <done>The LLM-distillation stack carries both main's post-fork members and Cycle 17's new stated-rules members, with a strictly additive diff.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| historical git ref → shipping branch | 64 files authored 777 commits ago enter `feat/personal-tools` without line-by-line re-review |
| stale-base diff → current file content | A hunk derived against the merge-base can silently delete members main added after the fork |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-112-03 | Tampering | Wholesale branch-file copy over an M-file, silently reverting shipped Cycle 16/18/19 work | mitigate | Task 2 forbids `git checkout <branch> -- <M-file>` outright; acceptance criteria assert `git diff HEAD` has zero deleted lines for all three files and that `ExtractCombinedAsync` / `CombinedExtractionResult` survive |
| T-112-04 | Tampering | Contamination — a glob or directory checkout sweeping in one of the 37 Cycle-16 files already on main, or a D-12 never-port file | mitigate | Single explicit 64-path checkout, no globs; acceptance criteria assert zero `git status` entries outside `DeckFlow.Core*` and zero entries for any never-port path |
| T-112-05 | Elevation of Privilege | Postgres integration fixture arriving and pulling an unapproved `Testcontainers.PostgreSql` package into the build | mitigate | Both dual-class test files are trimmed; `grep -rc 'PostgresContainerFixture' DeckFlow.Core.Tests/` must return 0; csproj diff must contain zero `PackageReference` lines |
| T-112-SC | Tampering | Package-manager installs (NuGet) | mitigate | Zero packages added. Asserted by the csproj `PackageReference` grep gate above. RESEARCH verified the branch's csproj files have no package diff from the merge-base. |
</threat_model>

<verification>
- All 64 allowlisted Core paths present; zero paths outside `DeckFlow.Core/` and `DeckFlow.Core.Tests/` modified.
- `grep -rc 'PostgresContainerFixture' DeckFlow.Core.Tests/` returns 0.
- `git diff HEAD -- DeckFlow.Core/Integration/ILlmDistillationService.cs DeckFlow.Core/Knowledge/DistillationResults.cs DeckFlow.Core/Knowledge/DistillationValidation.cs | grep -c '^-[^-]'` returns 0.
- No `PackageReference` added to any csproj.
- Nothing staged, nothing committed — plan 03 owns Commit 1.
</verification>

<success_criteria>
- The Cycle 17 Core engine (Phases 94-98) and its 24-file Core test suite are in the working tree, sourced by explicit path-allowlist checkout.
- The LLM-distillation stack has been extended additively onto main's current content, preserving every main-only member.
- The golden transcript fixture is wired into `DeckFlow.Core.Tests.csproj` with no package addition.
- No never-port file, no contamination file, and no Postgres fixture arrived.
</success_criteria>

<output>
Create `.planning/phases/112-cycle-17-code-port/112-02-SUMMARY.md` when done, listing the count of files checked out, the two classes deleted, and the additive-diff line counts for the three hunked files.
</output>
