# Continue Here — DeckFlow Cycle 11

**Paused:** 2026-06-21 20:42 MDT
**Branch:** `cycle11` (worktree `/mnt/c/users/chrislunt/source/personal/deckflow-cycle11`)
**HEAD:** `0569d0c3` — pushed to `origin/cycle11` (clean tree; `DeckFlow.Web/node_modules` untracked/gitignored)

> ⚠ **Worktree gotcha:** shell cwd RESETS to the main tree (`/mnt/c/users/chrislunt/source/personal/deckflow`) between every Bash call. Prefix every command `cd /mnt/c/users/chrislunt/source/personal/deckflow-cycle11 &&`; use absolute paths.

## Milestone

**Cycle 11 — Security, Visibility Control & Creator-Lens** (15 reqs / 6 phases 64-69). On branch `cycle11`, pushed to origin, NOT merged to `main` (Render deploys from main → no prod deploy yet).

## Position

**Phase 64 (Deck-Source Host Hardening, SEC-01/02/03) — FULLY CLOSED ✅**
- planned → executed (Claude-coded, `--no-cross-ai`) → verified 3/3 → Codex code-review 0 findings → secured (0 open threats) → pushed.
- New `DeckFlow.Core/Integration/DeckSourceHost.cs` (exact-or-`.apex`-subdomain predicate) adopted at all 4 substring host-match sites (DeckEntryLoader ×2, MoxfieldApiDeckImporter canonical-forward fix, PacketArtifactStore). Grep gate clean. Core 612/612, Web 677/677 green.
- Artifacts in `.planning/phases/64-deck-source-host-hardening/`: RESEARCH, VALIDATION, 64-01/02 PLAN + SUMMARY, VERIFICATION (passed), SECURITY (secured).

## Remaining (Cycle 11)

- **Phase 65** Prod Content Artifact Reconcile (DATA-01/02) — HIGH; 86 orphaned prod `content_site_index` rows; investigation + operator-run reconcile, AI read-only on prod. **← NEXT**
- **Phase 66** Admin Tool-Visibility Toggles + Tool Registry (TOGGLE-01..07) — biggest feature; depends on 64.
- **Phase 67** Content KB Value A/B Validation (KBVAL-01/02) — gate for 68.
- **Phase 68** Creator-Philosophy Research (CREATOR-01) — CONDITIONAL on 67 lift.
- **Phase 69** Studio UI Design Pass (STUI-01/02/03).

## Resume

```
cd /mnt/c/users/chrislunt/source/personal/deckflow-cycle11
/gsd-plan-phase 65
```

## Key decisions / context

- **Delegation:** standing DeckFlow rule (expires 2026-06-24) = **Claude codes, Codex reviews** (Codex gpt-5.4, effort low, review-only). Plan-phase + execute run with Claude; Codex does plan-review + code-review only.
- **ROADMAP-parse gotcha (fixed `e688308a`):** Cycle 11 phases must use `#### Phase N:` detail headers — `gsd-sdk` won't resolve `**Phase N:**` bold-bullets. All of 64-69 now parseable.
- **Phase 64 deviation (accepted):** spoof URLs don't throw — MoxfieldParser treats untrusted URL as a literal card-name line; the security invariant is *importer never invoked for a spoof host*. Tests assert null-capture, not throw.
- **Minor debt:** `originalUrl` param in `MoxfieldApiDeckImporter.FetchViaCommanderSpellbookAsync` is now dead (error msg uses `deckId`) — optional future cleanup, zero security impact.

## Blockers

None.
