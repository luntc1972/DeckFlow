---
phase: 89-content-hash-foundation
plan: 02
type: execute
wave: 2
depends_on: ["89-01"]
files_modified:
  - DeckFlow.Core/Content/ContentSiteIndexStore.cs
  - DeckFlow.Core/Content/IContentSiteIndexStore.cs
  - DeckFlow.Core.Tests/CommandRunnerCorpusResetTests.cs
  - DeckFlow.Core.Tests/RunDistillAsyncTests.cs
  - DeckFlow.Core.Tests/Orchestration/FakeOrchestratorStores.cs
  - DeckFlow.Studio.Tests/TestDoubles/FakeContentSiteIndexStore.cs
  - DeckFlow.Web.Tests/TestDoubles/FakeContentSiteIndexStore.cs
  - DeckFlow.Core.Tests/Content/ContentSiteIndexStoreBodyHashTests.cs
autonomous: true
requirements: [SYNC-01]
must_haves:
  truths:
    - "content_site_index carries a body_sha256 column (SQLite + Postgres), added by a dialect-guarded idempotent ALTER that rides ONLY the web-app EnsureSchema path (Studio prod stores, schema-ensure OFF, never run it)."
    - "body_sha256 round-trips: it is written by the content upsert AND the seed-reseed upsert (overwrite-from-EXCLUDED), and read back by every SELECT->ContentSiteIndexRow mapper."
    - "A narrow idempotent store method (declared on IContentSiteIndexStore) sets body_sha256 only where it is currently null (safe on re-run), for the D-08 backfill to call; all 5 hand-written fakes implement it and all three test projects still build."
  artifacts:
    - path: "DeckFlow.Core/Content/ContentSiteIndexStore.cs"
      provides: "body_sha256 DDL (CREATE + ALTER), row DTO field, 6 SELECT columns, mapper, upsert plumbing (incl. reseed path), backfill-set method"
      contains: "body_sha256"
    - path: "DeckFlow.Core/Content/IContentSiteIndexStore.cs"
      provides: "SetBodySha256IfNullAsync interface member"
      contains: "SetBodySha256IfNullAsync"
  key_links:
    - from: "EnsureSchemaAsync body_sha256 ALTER"
      to: "_ensureSchemaEnabled gate (line 75)"
      via: "DDL only runs when schema-ensure is enabled (web app); Studio prod stores are constructed with ensureSchemaEnabled:false"
      pattern: "_ensureSchemaEnabled"
    - from: "UpsertContentColumnsOnlyAsync + UpsertPreservingVisibilitySql"
      to: "content_site_index.body_sha256"
      via: "INSERT column + ON CONFLICT DO UPDATE SET body_sha256 = EXCLUDED.body_sha256 (overwrite, like indexed_utc)"
      pattern: "body_sha256 = EXCLUDED.body_sha256"
---

<objective>
Add the `body_sha256` column to the `content_site_index` persistence layer end-to-end (D-09): dialect-guarded idempotent ALTER + both `CREATE TABLE` dialect strings, the internal row DTO, all 6 `SELECT` column lists, the `ToContentSiteIndexRow` mapper, upsert parameter binding, and the upsert SQL variants (including the reseed path `UpsertPreservingVisibilitySql`). Declare + implement one narrow idempotent method (`SetBodySha256IfNullAsync`) on the store interface and update the 5 hand-written fakes so all three test projects still build.

Purpose: SYNC-01 requires the column to exist in SQLite + Postgres and round-trip through the store AND the seed reseed. This plan owns `ContentSiteIndexStore.cs` in full so all DDL/SQL lands in one coherent commit; the DDL rides ONLY the web app's `EnsureSchema` (P88 D-10 keeps Studio prod stores DDL-free). Because `IContentSiteIndexStore` fakes explicitly implement every member (no default bodies), the interface change lands here in wave 2, ahead of 89-06's caller in wave 3.
Output: `body_sha256` persisted, read, upserted (content + reseed), and null-backfillable through the store interface; 5 fakes + xUnit round-trip/null-backfill tests green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/89-content-hash-foundation/89-CONTEXT.md
@.planning/phases/89-content-hash-foundation/89-PATTERNS.md
@.planning/phases/88-index-row-integrity-hotfix/88-CONTEXT.md
</context>

<interfaces>
From DeckFlow.Core/Content/ContentSiteIndexStore.cs (confirmed line refs):
- ctor stores `_ensureSchemaEnabled` (line 58); `EnsureSchemaAsync` early-returns `if (!_ensureSchemaEnabled) return;` (line 75) — the P88 D-10 gate that keeps Studio prod stores DDL-free.
- `GetTableColumnsAsync(connection, "content_site_index", ct)` — used to guard each idempotent ALTER (the is_hidden/approval_status/pushed_to_prod_utc blocks are the template, ~87-130).
- `public async Task UpsertContentColumnsOnlyAsync(ContentSiteIndexRow row, ...)` (line 194) — publish-path upsert; SQL `UpsertContentColumnsOnlySql` (~1028-1068).
- `UpsertPreservingVisibilitySql` (~982-1023) — the reseed path called by `ContentKbSeedLoader.LoadIfPresentAsync` (D-09). `is_visible` is PRESERVED here; body_sha256 must be OVERWRITTEN from EXCLUDED (like `indexed_utc`), NOT preserved.
- `public async Task<IReadOnlyList<ContentSiteIndexRow>> GetAllRowsAsync(...)` (line 318) — unfiltered read the backfill will enumerate.
- The 6 SELECT read sites (NOT 7 — the grep count of 8 `FROM content_site_index` includes 2 non-SELECT statements): `GetByNaturalKeyAsync`, `GetPublishedRowsAsync`, `GetApprovedRowsAsync`, `GetAllRowsAsync`, `GetByIdAsync`, `GetPublishedByIdAsync`.
- `private sealed class ContentSiteIndexRowData` (~1116-1135); `ToContentSiteIndexRow(ContentSiteIndexRowData)` mapper (~905-937); `BuildUpsertParameters` (~780-798).
- `SqliteCreateTableSql` / `PostgresCreateTableSql` const strings (~1071-1114).

From DeckFlow.Core/Content/IContentSiteIndexStore.cs (confirmed — all members declared explicitly, e.g. `Task UpsertContentColumnsOnlyAsync(...)` line 37, `Task<IReadOnlyList<ContentSiteIndexRow>> GetAllRowsAsync(...)` line 70). The 5 fakes hand-implement every member (no default interface bodies):
- DeckFlow.Core.Tests/CommandRunnerCorpusResetTests.cs:146
- DeckFlow.Core.Tests/RunDistillAsyncTests.cs:642
- DeckFlow.Core.Tests/Orchestration/FakeOrchestratorStores.cs:126
- DeckFlow.Studio.Tests/TestDoubles/FakeContentSiteIndexStore.cs:12
- DeckFlow.Web.Tests/TestDoubles/FakeContentSiteIndexStore.cs:15

Prod stores are built with `ensureSchemaEnabled: false` via `DeckFlow.Studio/Services/IProdStoreFactory.cs:32` (do NOT touch — just honor it).

From 89-01 (dependency): `ContentSiteIndexRow.BodySha256` (`string?`) already exists on the record.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add body_sha256 DDL, DTO, 6 SELECTs/mapper, and upsert plumbing (content + reseed)</name>
  <read_first>
    - DeckFlow.Core/Content/ContentSiteIndexStore.cs (ranges: 55-146 EnsureSchemaAsync + gate; 780-800 BuildUpsertParameters; 900-940 mapper; 980-1140 upsert SQL variants + CREATE TABLE + DTO — plus the 6 SELECT read sites named above)
    - 89-PATTERNS.md § "ContentSiteIndexStore.cs" (exact ALTER, DTO, mapper, upsert; the site count is 6 read methods, not 7)
    - .planning/phases/88-index-row-integrity-hotfix/88-CONTEXT.md (D-09/D-10 — schema-ensure OFF for prod stores)
    - DeckFlow.Core.Tests/Content/ContentSiteIndexStoreBatchUpsertTests.cs (test harness shape for a SQLite store round-trip)
  </read_first>
  <behavior>
    - Fresh SQLite DB created via CREATE TABLE has a `body_sha256` column (nullable TEXT).
    - Existing SQLite DB WITHOUT the column: after EnsureSchemaAsync (schema-ensure enabled), the column exists; running EnsureSchemaAsync twice does not error (idempotent — guarded by GetTableColumnsAsync).
    - Content round-trip: UpsertContentColumnsOnlyAsync with a row whose BodySha256 = "<64hex>" then a read (GetAllRowsAsync / GetByNaturalKeyAsync) returns a row whose BodySha256 equals that value.
    - Reseed round-trip (WARNING 1): a row upserted via UpsertPreservingVisibilitySql with a NEW BodySha256 overwrites the stored hash from EXCLUDED (matching indexed_utc semantics), while is_visible stays preserved — so a seed reload updates the hash rather than silently keeping a stale one.
    - A store constructed with ensureSchemaEnabled:false does NOT issue the ALTER (ContentSiteIndexStoreSchemaEnsureSwitchTests behavior still holds).
  </behavior>
  <action>In `ContentSiteIndexStore.cs`: (1) inside `EnsureSchemaAsync`, after the existing column-backfill blocks and before `_schemaReady = true;`, add a `if (!columns.Contains("body_sha256"))` guarded ALTER: `"ALTER TABLE content_site_index ADD COLUMN body_sha256 TEXT NULL;"` (same text both dialects — TEXT NULL is valid in both, no IsPostgres branch). (2) Add `body_sha256 TEXT NULL,` to BOTH `SqliteCreateTableSql` and `PostgresCreateTableSql`. (3) Add `public string? BodySha256 { get; init; }` to `ContentSiteIndexRowData` (nullable, not required). (4) Add `body_sha256` to each of the 6 SELECT column lists (GetByNaturalKeyAsync, GetPublishedRowsAsync, GetApprovedRowsAsync, GetAllRowsAsync, GetByIdAsync, GetPublishedByIdAsync), one-column-per-line, and read it in each DTO hydration. (5) Add `BodySha256 = row.BodySha256,` to `ToContentSiteIndexRow`. (6) Add `parameters.Add("bodySha256", row.BodySha256);` to `BuildUpsertParameters` following the `approvalStatus` idiom. (7) Add `body_sha256` to the INSERT column list AND `ON CONFLICT ... DO UPDATE SET body_sha256 = EXCLUDED.body_sha256` in `UpsertContentColumnsOnlySql`, `UpsertSql`, AND `UpsertPreservingVisibilitySql` — in the reseed path make the overwrite EXPLICIT and mirror `indexed_utc = EXCLUDED.indexed_utc` (overwrite), NOT the `is_visible` preserve idiom (WARNING 1, protects D-08's one-time intent). Touch only changed lines; preserve LF and `{ get; init; }`. Add tests to a new `DeckFlow.Core.Tests/Content/ContentSiteIndexStoreBodyHashTests.cs` covering: fresh-DB column, existing-DB idempotent ALTER, content upsert round-trip, and the reseed overwrite-from-EXCLUDED round-trip.</action>
  <verify>
    <automated>dotnet build DeckFlow.Core.Tests -c Debug; dotnet test DeckFlow.Core.Tests --filter "FullyQualifiedName~ContentSiteIndexStoreBodyHashTests|FullyQualifiedName~ContentSiteIndexStoreSchemaEnsureSwitchTests"</automated>
  </verify>
  <acceptance_criteria>
    - SOURCE: `grep -c "body_sha256" DeckFlow.Core/Content/ContentSiteIndexStore.cs` counts the ALTER, both CREATE TABLE strings, all 6 SELECTs, and 3 upsert SQL variants' INSERT + ON CONFLICT clauses (double-digit count).
    - SOURCE: `UpsertPreservingVisibilitySql` sets `body_sha256 = EXCLUDED.body_sha256` (overwrite), NOT preserved (grep confirms it is in the DO UPDATE SET list next to indexed_utc, not omitted like is_visible).
    - SOURCE: the ALTER sits above `_schemaReady = true;` and below the `_ensureSchemaEnabled` early-return (line 75).
    - BEHAVIOR: fresh-DB + existing-DB-ALTER both yield the column; double EnsureSchemaAsync idempotent; content upsert AND reseed upsert both round-trip the hash (reseed overwrites from EXCLUDED); ensureSchemaEnabled:false issues no ALTER.
  </acceptance_criteria>
  <done>body_sha256 exists in both dialects, round-trips through content upsert + reseed + SELECT + mapper, and is DDL-gated to the web EnsureSchema path.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Declare SetBodySha256IfNullAsync on the interface, implement it, and update all 5 fakes</name>
  <read_first>
    - DeckFlow.Core/Content/IContentSiteIndexStore.cs (member declaration style — Task/Task&lt;T&gt; signatures, CancellationToken last)
    - DeckFlow.Core/Content/ContentSiteIndexStore.cs (an existing single-column UPDATE method, e.g. pushed_to_prod stamping, as the shape template)
    - The 5 fakes: DeckFlow.Core.Tests/CommandRunnerCorpusResetTests.cs (~146), DeckFlow.Core.Tests/RunDistillAsyncTests.cs (~642), DeckFlow.Core.Tests/Orchestration/FakeOrchestratorStores.cs (~126), DeckFlow.Studio.Tests/TestDoubles/FakeContentSiteIndexStore.cs (~12), DeckFlow.Web.Tests/TestDoubles/FakeContentSiteIndexStore.cs (~15)
    - 89-CONTEXT.md D-08 ("smaller-surface option that stays safe on re-run")
  </read_first>
  <behavior>
    - Calling the method with (id, hash) sets body_sha256 for a row whose body_sha256 IS NULL and affects 1 row.
    - Calling it again for the same id (now non-null) is a no-op (UPDATE ... WHERE body_sha256 IS NULL affects 0 rows) — safe on re-run, never overwrites an existing hash.
    - The interface member exists; every concrete implementer (real store + 5 fakes) compiles; DeckFlow.Core.Tests, DeckFlow.Studio.Tests, DeckFlow.Web.Tests all build and pass.
  </behavior>
  <action>Add `Task<bool> SetBodySha256IfNullAsync(long id, string bodySha256, CancellationToken cancellationToken = default);` to `IContentSiteIndexStore.cs` (with `/// <summary>` xmldoc; a return-int affected-rows variant is also acceptable if it matches the store's existing single-column UPDATE method). Implement it in `ContentSiteIndexStore.cs`: SQL `UPDATE content_site_index SET body_sha256 = @bodySha256 WHERE id = @id AND body_sha256 IS NULL;` — parameterized, dialect-neutral; guard the hash arg with `ArgumentException.ThrowIfNullOrWhiteSpace`; no DDL. Add a matching member to all 5 hand-written fakes with a minimal faithful body (the two dictionary-backed `FakeContentSiteIndexStore` fakes should honor the null-only semantics against their in-memory state; the corpus-reset / distill / orchestrator fakes may return a trivial `Task.FromResult(...)` if they never exercise it — match each fake's existing stub style for unused members). Add coverage to `ContentSiteIndexStoreBodyHashTests.cs` (SQLite-backed) for the set + no-op-on-second-call semantics.</action>
  <verify>
    <automated>dotnet build DeckFlow.Core.Tests DeckFlow.Studio.Tests DeckFlow.Web.Tests -c Debug; dotnet test DeckFlow.Core.Tests --filter "FullyQualifiedName~ContentSiteIndexStoreBodyHashTests"</automated>
  </verify>
  <acceptance_criteria>
    - SOURCE: `SetBodySha256IfNullAsync` is declared in `IContentSiteIndexStore.cs`, implemented in `ContentSiteIndexStore.cs` with a `WHERE ... AND body_sha256 IS NULL` clause, and present in all 5 fake files (`grep -rl "SetBodySha256IfNullAsync"` returns 7 files: interface + store + 5 fakes).
    - BEHAVIOR: all three test projects (Core/Studio/Web) build clean (0 warnings); first call sets the hash (1 row), second call for the same id is a no-op (0 rows) and does not change the value; the SQL is parameterized.
  </acceptance_criteria>
  <done>A safe-on-re-run UPDATE-where-null setter exists on the interface + real store + 5 fakes; all test projects compile.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| store → prod Postgres / local SQLite | DDL + DML cross into the DB; DDL is gated by _ensureSchemaEnabled |
| Studio prod store → prod DB | Studio constructs prod stores with ensureSchemaEnabled:false — DDL MUST NOT reach prod from Studio |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-89-04 | Elevation/Tampering | body_sha256 ALTER | mitigate | ALTER lives strictly inside the `_ensureSchemaEnabled` gate (line 75); only the web app's EnsureSchema runs it. Studio prod stores are built ensureSchemaEnabled:false (P88 D-10) — a schema-ensure-switch test asserts no ALTER fires when disabled. |
| T-89-05 | Injection | SetBodySha256IfNull / upserts | mitigate | All body_sha256 writes are parameterized (`@bodySha256`, `@id`); no identifier is string-concatenated into SQL. |
| T-89-06 | Tampering | backfill overwrite | mitigate | The setter is `WHERE body_sha256 IS NULL`, so it can never overwrite an existing hash; the reseed path DOES overwrite (by design, from git-owned EXCLUDED) so a corrected seed hash propagates — both are deterministic and idempotent (D-08/D-09). |
</threat_model>

<verification>
- `DeckFlow.Core`, `DeckFlow.Core.Tests`, `DeckFlow.Studio.Tests`, `DeckFlow.Web.Tests` build clean (0 warnings).
- Fresh-DB CREATE and existing-DB ALTER both produce body_sha256; idempotent on re-run.
- Content upsert, reseed-overwrite, and null-only-setter tests pass; schema-ensure-switch tests still pass (no DDL when disabled).
- README intentionally untouched — this plan is internal persistence plumbing with no user-facing behavior change.
</verification>

<success_criteria>
- content_site_index has body_sha256 in SQLite + Postgres, added only via the gated EnsureSchema path.
- body_sha256 round-trips through content upsert, reseed upsert (overwrite-from-EXCLUDED), SELECT, and mapper.
- A safe UPDATE-where-null setter exists on the interface + real store + 5 fakes; all test projects compile.
</success_criteria>

<output>
Create `.planning/phases/89-content-hash-foundation/89-02-SUMMARY.md` when done.
</output>
