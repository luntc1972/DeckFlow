---
phase: 37.5-rebuild-kb-corpus
plan: 02
subsystem: cli
tags: [content-kb, classifier, distillation, sqlite, postgres, xunit]
requires:
  - phase: 37.5-rebuild-kb-corpus
    provides: corpus-reset baseline, timestamped transcripts, live all-zero clip guard
provides:
  - transcript classifier gate for distill keep/drop routing
  - filtered distill status with SQLite/Postgres CHECK migration
  - stale site-index cleanup when a video is reclassified as drop
  - fail-closed metered-provider protection before any distill run starts
affects: [37.5-03, content-kb, distill, classifier]
tech-stack:
  added: []
  patterns: [CLI-only classifier gate, idempotent distill-status constraint migration, stale index cleanup on drop]
key-files:
  created:
    - .planning/phases/37.5-rebuild-kb-corpus/37.5-02-SUMMARY.md
  modified:
    - DeckFlow.Core/Knowledge/DistillationResults.cs
    - DeckFlow.Core/Knowledge/DistillationSchemas.cs
    - DeckFlow.Core/Knowledge/DistillationValidation.cs
    - DeckFlow.Core/Integration/ILlmDistillationService.cs
    - DeckFlow.Core/Integration/CliLlmDistillationService.cs
    - DeckFlow.Core/Content/ContentVideoStore.cs
    - DeckFlow.CLI/CommandRunners.cs
    - DeckFlow.Core.Tests/CliLlmDistillationServiceTests.cs
    - DeckFlow.Core.Tests/RunDistillAsyncTests.cs
    - DeckFlow.Core.Tests/ContentVideoStoreDistillTests.cs
key-decisions:
  - "The classifier is subscription-CLI-only; RunDistillAsync now fails closed on a metered/OpenAI provider before StartRunAsync."
  - "A drop verdict marks the video filtered, clears prior distill output, and deletes any existing site-index row so stale published entries disappear."
  - "content_distill_status is upgraded in EnsureSchemaAsync with detect-before-act logic instead of relying on CREATE TABLE IF NOT EXISTS."
patterns-established:
  - "Classifier calls reuse the existing CLI extraction pipeline and keep the --allowedTools \"\" sandbox unchanged."
  - "Drop-path cleanup derives the natural key from the video and removes stale site-index rows through GetByNaturalKeyAsync/DeleteByIdAsync."
requirements-completed: [REBUILD-02]
duration: 6min
completed: 2026-06-11
---

# Phase 37.5: Rebuild KB Corpus Summary

**Distill now classifies transcripts before summarization, filters junk videos out of the corpus, and removes stale published rows when a video is reclassified as drop**

## Performance

- **Duration:** 6 min
- **Started:** 2026-06-11T19:47:26Z
- **Completed:** 2026-06-11T19:53:24Z
- **Tasks:** 5
- **Files modified:** 10

## Accomplishments
- Added Wave-0 RED coverage for classifier keep/drop parsing, filtered routing, stale index-row deletion, fail-closed provider behavior, and SQLite filtered-status acceptance.
- Added `ClassificationResult`, `ClassificationSchema`, `ClassificationSystemPrompt`, and `ClassificationPayload`, then wired `ClassifyAsync` into the CLI distillation service.
- Added `filtered` distill-status support with idempotent SQLite/Postgres CHECK migration and wired the distill runner to clear stale output and delete stale site-index rows on drop.

## Task Commits

Each task was committed atomically:

1. **Task 1: Wave 0 — classifier + filtered-status + stale-cleanup + fail-closed tests (RED)** - `e1170be` (test)
2. **Task 2: ClassificationResult + schemas + payload — GREEN** - `c5ba152` (feat)
3. **Task 3: ClassifyAsync on the LLM service (interface + CLI impl) — GREEN** - `05ab55b` (feat)
4. **Task 4: filtered distill status + CHECK migration — GREEN** - `1f7c5fe` (fix)
5. **Task 5: classifier gate + stale-row cleanup + fail-closed runner behavior — GREEN** - `f66ac58` (feat)

**Plan metadata:** summary close-out commit for `37.5-02-SUMMARY.md`

## Files Created/Modified
- `DeckFlow.Core/Knowledge/DistillationResults.cs` - Added the keep/drop `ClassificationResult` record.
- `DeckFlow.Core/Knowledge/DistillationSchemas.cs` - Added the strict classifier schema and the classifier system prompt without touching `ClipsSystemPrompt`.
- `DeckFlow.Core/Knowledge/DistillationValidation.cs` - Added `ClassificationPayload` plus classifier verdict sanitization while leaving `ValidateClips` unchanged.
- `DeckFlow.Core/Integration/ILlmDistillationService.cs` - Exposed `ClassifyAsync` with a default unsupported implementation for non-CLI providers.
- `DeckFlow.Core/Integration/CliLlmDistillationService.cs` - Implemented CLI-backed classification through the existing JSON extraction pipeline.
- `DeckFlow.Core/Content/ContentVideoStore.cs` - Added `filtered` status validation plus SQLite/Postgres CHECK-constraint upgrade logic.
- `DeckFlow.CLI/CommandRunners.cs` - Added the metered-provider fail-closed guard, classifier gate, filtered drop branch, and stale index-row deletion.
- `DeckFlow.Core.Tests/CliLlmDistillationServiceTests.cs` - Added keep/drop classifier parsing tests.
- `DeckFlow.Core.Tests/RunDistillAsyncTests.cs` - Added classifier routing/fail-closed coverage and updated runner expectations for the classifier call.
- `DeckFlow.Core.Tests/ContentVideoStoreDistillTests.cs` - Added SQLite filtered-status acceptance coverage.

## Decisions Made
- Used a default interface implementation for `ILlmDistillationService.ClassifyAsync` so unsupported OpenAI/test implementers outside this plan’s scope stay compilable while the CLI service provides the real implementation.
- Left the projected metered distillation call count at 3 because the classifier never runs on the metered/OpenAI path anymore.
- Kept drop cleanup idempotent by clearing distill output even for already-empty rows and only deleting a site-index row when one exists for the natural key.

## Deviations from Plan

None - plan executed exactly as written.

---

**Total deviations:** 0 auto-fixed (0 blocking, 0 missing critical, 0 bugs)
**Impact on plan:** None.

## Issues Encountered
- One targeted runner test still expected the pre-classifier three-call sequence; updating it to include the classifier call resolved the failure.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness
- Phase 37.5-03 can assume the distill pipeline now filters junk transcripts before artifacts/index rows are produced.
- Shared-file coordination held: Plan 01’s private `ValidateClips` all-zero guard, `RunCorpusResetAsync`, and `ClipsSystemPrompt` were not modified in this plan.

---
*Phase: 37.5-rebuild-kb-corpus*
*Completed: 2026-06-11*
