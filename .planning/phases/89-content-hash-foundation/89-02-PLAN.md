---
phase: 89-content-hash-foundation
plan: 02
type: execute
wave: 2
depends_on: ["89-01"]
files_modified:
  - DeckFlow.Core/Content/ContentSiteIndexStore.cs
  - DeckFlow.Core.Tests/Content/ContentSiteIndexStoreBodyHashTests.cs
autonomous: true
requirements: [SYNC-01]
must_haves:
  truths:
    - "content_site_index carries a body_sha256 column (SQLite + Postgres), added by a dialect-guarded idempotent ALTER that rides ONLY the web-app EnsureSchema path (Studio prod stores, schema-ensure OFF, never run it)."
    - "body_sha256 round-trips: it is written by the content upsert and read back by every SELECT->ContentSiteIndexRow mapper."
    - "A narrow idempotent store method sets body_sha256 only where it is currently null (safe on re-run), for the D-08 backfill to call."
  artifacts:
    - path: "DeckFlow.Core/Content/ContentSiteIndexStore.cs"
      provides: "body_sha256 DDL (CREATE + ALTER), row DTO field, SELECT columns, mapper, upsert plumbing, backfill-set method"
      contains: "body_sha256"
  key_links:
    - from: "EnsureSchemaAsync body_sha256 ALTER"
      to: "_ensureSchemaEnabled gate (line 75)"
      via: "DDL only runs when schema-ensure is enabled (web app); Studio prod stores are constructed with ensureSchemaEnabled:false"
      pattern: "_ensureSchemaEnabled"
    - from: "UpsertContentColumnsOnlyAsync"
      to: "content_site_index.body_sha256"
      via: "INSERT column + ON CONFLICT DO UPDATE SET"
      pattern: "body_sha256 = EXCLUDED.body_sha256"
---

<objective>
Add the `body_sha256` column to the `content_site_index` persistence layer end-to-end (D-09): dialect-guarded idempotent ALTER + both `CREATE TABLE` dialect strings, the internal row DTO, every `SELECT` column list, the `ToContentSiteIndexRow` mapper, upsert parameter binding, and the upsert SQL variants. Add one narrow idempotent method to set the hash only where null, for the D-08 backfill to consume.

Purpose: SYNC-01 requires the column to exist in SQLite + Postgres and round-trip through the store. This plan owns `ContentSiteIndexStore.cs` in full so all DDL/SQL lands in one coherent commit; the DDL rides ONLY the web app's `EnsureSchema` (P88 D-10 keeps Studio prod stores DDL-free).
Output: `body_sha256` persisted, read, upserted, and null-backfillable through the store; xUnit round-trip + null-backfill tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/89-content-hash-foundation/89-CONTEXT.md
@.planning/phases/89-content-hash-foundation/89-PATTERNS.md
@.planning/phases/88-index-row-integrity-hotfix/88-CONTEXT.md
</context>

<interfaces>
From DeckFlow.Core/Content/ContentSiteIndexStore.cs (confirmed line refs):
- ctor stores `_ensureSchemaEnabled` (line 58); `EnsureSchemaAsync` early-returns `if (!_ensureSchemaEnabled) return;` (line 75) — the P88 D-10 gate that keeps Studio prod stores DDL-free.
- `GetTableColumnsAsync(connection, "content_site_index", ct)` — used to guard each idempotent ALTER (the is_hidden/approval_status/pushed_to_prod_utc blocks are the template, ~87-130).
- `public async Task UpsertContentColumnsOnlyAsync(ContentSiteIndexRow row, ...)` (line 194) — the publish-path upsert; its SQL is `UpsertContentColumnsOnlySql` (~1028-1068).
- `public async Task<IReadOnlyList<ContentSiteIndexRow>> GetAllRowsAsync(...)` (line 318) — unfiltered read the backfill will enumerate.
- `private sealed class ContentSiteIndexRowData` (internal DTO, ~1116-1135); `ToContentSiteIndexRow(ContentSiteIndexRowData)` mapper (~905-937); `BuildUpsertParameters` (~780-798).
- `SqliteCreateTableSql` / `PostgresCreateTableSql` const strings (~1071-1114).
- Prod stores are built with `ensureSchemaEnabled: false` via `DeckFlow.Studio/Services/IProdStoreFactory.cs:32` (do NOT touch — just honor it).

From 89-01 (dependency): `ContentSiteIndexRow.BodySha256` (`string?`) already exists on the record.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add body_sha256 DDL, DTO, SELECT/mapper, and upsert plumbing</name>
  <read_first>
    - DeckFlow.Core/Content/ContentSiteIndexStore.cs (ranges: 55-146 EnsureSchemaAsync + gate; 780-800 BuildUpsertParameters; 900-940 mapper; 1020-1140 upsert SQL + CREATE TABLE + DTO — plus the 7 SELECT sites the pattern map enumerates)
    - 89-PATTERNS.md § "ContentSiteIndexStore.cs" (exact ALTER, DTO, mapper, upsert, SELECT enumeration)
    - .planning/phases/88-index-row-integrity-hotfix/88-CONTEXT.md (D-09/D-10 — schema-ensure OFF for prod stores)
    - DeckFlow.Core.Tests/Content/ContentSiteIndexStoreBatchUpsertTests.cs (test harness shape for an in-memory/SQLite store round-trip)
  </read_first>
  <behavior>
    - Fresh SQLite DB created via CREATE TABLE has a `body_sha256` column (nullable TEXT).
    - Existing SQLite DB WITHOUT the column: after EnsureSchemaAsync (schema-ensure enabled), the column exists; running EnsureSchemaAsync twice does not error (idempotent — guarded by GetTableColumnsAsync).
    - Round-trip: UpsertContentColumnsOnlyAsync with a row whose BodySha256 = "abc...64hex" then a read (GetAllRowsAsync / GetByNaturalKeyAsync) returns a row whose BodySha256 equals that value.
    - A store constructed with ensureSchemaEnabled:false does NOT issue the ALTER (existing ContentSiteIndexStoreSchemaEnsureSwitchTests behavior must still hold — extend if needed).
  </behavior>
  <action>In `ContentSiteIndexStore.cs`: (1) inside `EnsureSchemaAsync`, after the existing column-backfill blocks and before `_schemaReady = true;`, add a `if (!columns.Contains("body_sha256"))` guarded ALTER: `"ALTER TABLE content_site_index ADD COLUMN body_sha256 TEXT NULL;"` (same text both dialects — no `_connectionInfo.IsPostgres` branch needed since TEXT NULL is valid in both). (2) Add `body_sha256 TEXT NULL,` to BOTH `SqliteCreateTableSql` and `PostgresCreateTableSql` (fresh-DB path must agree with the ALTER). (3) Add `public string? BodySha256 { get; init; }` to `ContentSiteIndexRowData` (nullable, not required). (4) Add `body_sha256` to each `SELECT ... FROM content_site_index` column list (the ~7 sites the pattern map lists: GetByNaturalKeyAsync, GetPublishedRowsAsync, GetApprovedRowsAsync, GetAllRowsAsync, GetByIdAsync, GetPublishedByIdAsync + any remaining), one-column-per-line style; read it in the DTO hydration for each. (5) Add `BodySha256 = row.BodySha256,` to `ToContentSiteIndexRow`. (6) Add `parameters.Add("bodySha256", row.BodySha256);` to `BuildUpsertParameters` following the `approvalStatus` idiom. (7) Add `body_sha256` to the INSERT column list AND the `ON CONFLICT ... DO UPDATE SET body_sha256 = EXCLUDED.body_sha256` clause of `UpsertContentColumnsOnlySql`; keep `UpsertSql` / `UpsertPreservingVisibilitySql` schema-consistent (add the column so a fresh schema and these SQLs agree). Touch only changed lines; preserve LF and `{ get; init; }`. Add tests to a new `DeckFlow.Core.Tests/Content/ContentSiteIndexStoreBodyHashTests.cs`.</action>
  <verify>
    <automated>dotnet build DeckFlow.Core.Tests -c Debug; dotnet test DeckFlow.Core.Tests --filter "FullyQualifiedName~ContentSiteIndexStoreBodyHashTests|FullyQualifiedName~ContentSiteIndexStoreSchemaEnsureSwitchTests"</automated>
  </verify>
  <acceptance_criteria>
    - SOURCE: `grep -c "body_sha256" DeckFlow.Core/Content/ContentSiteIndexStore.cs` counts the ALTER, both CREATE TABLE strings, every SELECT (>=7), and the upsert INSERT + ON CONFLICT clause (expect a double-digit count).
    - SOURCE: the ALTER sits above `_schemaReady = true;` and below the `_ensureSchemaEnabled` early-return (line 75) — it is inside the gated region.
    - BEHAVIOR: fresh-DB and existing-DB-ALTER paths both yield the column; double EnsureSchemaAsync is idempotent; a body_sha256 value written by upsert reads back equal.
    - BEHAVIOR: an ensureSchemaEnabled:false store issues no ALTER (schema-ensure-switch tests still green).
  </acceptance_criteria>
  <done>body_sha256 exists in both dialects, round-trips through upsert+SELECT+mapper, and is DDL-gated to the web EnsureSchema path.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add narrow idempotent SetBodySha256IfNull store method</name>
  <read_first>
    - DeckFlow.Core/Content/ContentSiteIndexStore.cs (an existing single-column UPDATE method — e.g. the pushed_to_prod stamping method — as the shape template)
    - 89-CONTEXT.md D-08 ("smaller-surface option that stays safe on re-run")
  </read_first>
  <behavior>
    - Calling the method with (id, hash) sets body_sha256 for a row whose body_sha256 IS NULL and returns/affects 1 row.
    - Calling it again for the same id (now non-null) is a no-op (UPDATE ... WHERE body_sha256 IS NULL affects 0 rows) — safe on re-run, never overwrites an existing hash.
  </behavior>
  <action>Add `public async Task<bool> SetBodySha256IfNullAsync(long id, string bodySha256, CancellationToken cancellationToken = default)` to `ContentSiteIndexStore.cs` (or a return-int affected-rows variant, matching the store's existing single-column UPDATE method style). SQL: `UPDATE content_site_index SET body_sha256 = @bodySha256 WHERE id = @id AND body_sha256 IS NULL;` — dialect-neutral, parameterized. Guard args (`ArgumentException.ThrowIfNullOrWhiteSpace` on the hash). This method never runs DDL. Add coverage to `ContentSiteIndexStoreBodyHashTests.cs`.</action>
  <verify>
    <automated>dotnet test DeckFlow.Core.Tests --filter "FullyQualifiedName~ContentSiteIndexStoreBodyHashTests"</automated>
  </verify>
  <acceptance_criteria>
    - SOURCE: method exists with a `WHERE ... AND body_sha256 IS NULL` clause (grep confirms the IS NULL guard).
    - BEHAVIOR: first call sets the hash (1 row affected); second call for the same id is a no-op (0 rows) and does not change the stored value.
    - BEHAVIOR: the SQL is parameterized (no string-concatenated id/hash).
  </acceptance_criteria>
  <done>A safe-on-re-run UPDATE-where-null setter exists for the D-08 backfill to call.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| store → prod Postgres / local SQLite | DDL + DML cross into the DB; DDL is gated by _ensureSchemaEnabled |
| Studio prod store → prod DB | Studio constructs prod stores with ensureSchemaEnabled:false — DDL MUST NOT reach prod from Studio |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-89-04 | Elevation/Tampering | body_sha256 ALTER | mitigate | ALTER lives strictly inside the `_ensureSchemaEnabled` gate (line 75); only the web app's EnsureSchema runs it. Studio prod stores are built ensureSchemaEnabled:false (P88 D-10) — a schema-ensure-switch test asserts no ALTER fires when disabled. |
| T-89-05 | Injection | SetBodySha256IfNull / upsert | mitigate | All body_sha256 writes are parameterized (`@bodySha256`, `@id`); no identifier is string-concatenated into SQL. |
| T-89-06 | Tampering | backfill overwrite | mitigate | The setter is `WHERE body_sha256 IS NULL`, so it can never overwrite an existing hash — deterministic and idempotent (D-08). |
</threat_model>

<verification>
- `DeckFlow.Core` + `DeckFlow.Core.Tests` build clean (0 warnings).
- Fresh-DB CREATE and existing-DB ALTER both produce body_sha256; idempotent on re-run.
- Round-trip and null-only-setter tests pass; schema-ensure-switch tests still pass (no DDL when disabled).
</verification>

<success_criteria>
- content_site_index has body_sha256 in SQLite + Postgres, added only via the gated EnsureSchema path.
- body_sha256 round-trips through upsert, SELECT, and mapper.
- A safe UPDATE-where-null setter exists for backfill.
</success_criteria>

<output>
Create `.planning/phases/89-content-hash-foundation/89-02-SUMMARY.md` when done.
</output>
