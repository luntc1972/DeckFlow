---
phase: 89-content-hash-foundation
plan: 04
type: execute
wave: 3
depends_on: ["89-01", "89-02"]
files_modified:
  - DeckFlow.Web/Services/Content/ContentKbSeedLoader.cs
  - DeckFlow.Core/Orchestration/ContentIndexExportRow.cs
  - DeckFlow.Core.Tests/Orchestration/ContentIndexExportJsonGoldenTests.cs
  - DeckFlow.Core.Tests/Orchestration/ContentIndexSeedWriteTests.cs
  - DeckFlow.Core.Tests/Orchestration/Fixtures/index-seed.golden.json
autonomous: true
requirements: [SYNC-01]
must_haves:
  truths:
    - "index-seed.json carries body_sha256 (camelCase bodySha256) on export and load, so a prod reseed reconstructs the hash and a redeploy does not drop it."
    - "The single shared export factory ContentIndexExportRow.From(row) maps BodySha256, so BOTH the CLI export path and DirectPush inherit the field automatically (one factory, one home)."
    - "The seed load mapper (ContentKbSeedLoader.BuildRow) populates ContentSiteIndexRow.BodySha256 from the seed entry."
  artifacts:
    - path: "DeckFlow.Core/Orchestration/ContentIndexExportRow.cs"
      provides: "ContentIndexExportRow.BodySha256 + From() mapping (single shared seed-row factory)"
      contains: "BodySha256"
    - path: "DeckFlow.Web/Services/Content/ContentKbSeedLoader.cs"
      provides: "ContentKbSeedEntry.BodySha256 + BuildRow mapping"
      contains: "BodySha256"
    - path: "DeckFlow.Core.Tests/Orchestration/Fixtures/index-seed.golden.json"
      provides: "bodySha256 in the golden export"
      contains: "bodySha256"
  key_links:
    - from: "ContentIndexExportRow.From(ContentSiteIndexRow)"
      to: "index-seed.json bodySha256"
      via: "BodySha256 = row.BodySha256 in the single shared factory, serialized camelCase; CLI export + DirectPush both route through From()"
      pattern: "BodySha256 = row.BodySha256"
---

<objective>
Round-trip `body_sha256` through the seed JSON (D-09) at the ONE place the seed row is built and the ONE place it is loaded: add the field to the shared export factory `ContentIndexExportRow` (record property + `From()` mapping) so both the CLI export (`ContentKbOrchestrator.GetApprovedExportRowsAsync`) and DirectPush (`DirectPushCoordinator`) inherit it, and add it to the seed load record + mapper (`ContentKbSeedLoader`). Update the golden fixture + writer tests. This makes git fully reconstruct prod's hash so a reseed/redeploy cannot silently drop `body_sha256`.

Purpose: SYNC-01 requires `body_sha256` in the seed JSON alongside the DB column, because git is the source of truth and the prod row must be reconstructable from `index-seed.json`. The seed row is built by a single shared factory — extending it there (not the serializer/deserializer wrappers) is the same "one signature, one home" payoff as `AreContentEqual`. Depends on 89-01 (the row property) and 89-02 (store SELECTs now return the column, so export reads real values).
Output: `bodySha256` in the shared export factory + seed load; golden fixture updated; export/writer tests green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/89-content-hash-foundation/89-CONTEXT.md
@.planning/phases/89-content-hash-foundation/89-PATTERNS.md
</context>

<interfaces>
From DeckFlow.Core/Orchestration/ContentIndexExportRow.cs (confirmed — the SINGLE shared seed-row factory):
- `public sealed record ContentIndexExportRow` — property set intentionally mirrors the CLI JSON shape for byte-stable export. Nullable-optional convention: `public DateTimeOffset? PublishedUtc { get; init; }` (line 31); required fields use `required`.
- `public static ContentIndexExportRow From(ContentSiteIndexRow row)` (line 50) — builds the export DTO from a store row; both `ContentKbOrchestrator.GetApprovedExportRowsAsync` (~909-914, CLI export) and `DirectPushCoordinator.cs:224` route through it.
- `ContentKbCommandRunners.SerializeContentIndexExportRows` (484) only serializes already-built rows; `PublishCoordinator.cs` only deserializes/compares — NEITHER builds the DTO from a ContentSiteIndexRow, so neither needs editing (they inherit the field).

From DeckFlow.Web/Services/Content/ContentKbSeedLoader.cs (per pattern map):
- `JsonSerializerOptions` (~12-16): `PropertyNamingPolicy = JsonNamingPolicy.CamelCase`, `PropertyNameCaseInsensitive = true` — a `BodySha256` property serializes/reads as `bodySha256`; no `[JsonPropertyName]` needed.
- `private static ContentSiteIndexRow BuildRow(ContentKbSeedEntry entry)` (~68-90) — maps entry -> row.
- `private sealed record ContentKbSeedEntry { ... required ... CardCategoryTags }` (~90-113).

From DeckFlow.Core.Tests/Orchestration/ContentIndexExportJsonGoldenTests.cs:
- `CreateRows()` (~23) builds `new ContentIndexExportRow { ... }` literals asserted against `Fixtures/index-seed.golden.json` via `SerializeContentIndexExportRows`.

From 89-01 / 89-02 (dependencies): `ContentSiteIndexRow.BodySha256` exists and is returned by store SELECTs.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add bodySha256 to seed load record + mapper</name>
  <read_first>
    - DeckFlow.Web/Services/Content/ContentKbSeedLoader.cs (whole file — JsonSerializerOptions, BuildRow, ContentKbSeedEntry)
    - 89-PATTERNS.md § "Seed JSON (index-seed.json) export/load path"
  </read_first>
  <behavior>
    - A seed JSON entry containing `"bodySha256": "<64hex>"` loads into a ContentSiteIndexRow whose BodySha256 equals that value.
    - A legacy seed entry WITHOUT bodySha256 loads with BodySha256 == null (nullable, no exception) — PropertyNameCaseInsensitive/camelCase handle absence.
  </behavior>
  <action>In `ContentKbSeedLoader.cs`: add `public string? BodySha256 { get; init; }` to `ContentKbSeedEntry` (nullable — legacy seed entries predate this phase), and `BodySha256 = entry.BodySha256,` to `BuildRow`'s `new ContentSiteIndexRow { ... }` literal. No `[JsonPropertyName]` — camelCase policy maps it to `bodySha256`. Touch only added lines; preserve `{ get; init; }` (carve-out).</action>
  <verify>
    <automated>dotnet build DeckFlow.Web -c Debug</automated>
  </verify>
  <acceptance_criteria>
    - SOURCE: `ContentKbSeedEntry` has `BodySha256` and `BuildRow` maps it (grep confirms both).
    - BEHAVIOR: DeckFlow.Web builds clean; a seed entry with bodySha256 hydrates the row property; missing field -> null.
  </acceptance_criteria>
  <done>Seed load carries body_sha256 into the row.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add bodySha256 to the shared ContentIndexExportRow factory and update golden</name>
  <read_first>
    - DeckFlow.Core/Orchestration/ContentIndexExportRow.cs (whole file — the record properties + From() mapping; PublishedUtc is the nullable-optional convention template)
    - DeckFlow.Core.Tests/Orchestration/ContentIndexExportJsonGoldenTests.cs + Fixtures/index-seed.golden.json (the golden the export is asserted against; confirm camelCase field naming there)
    - DeckFlow.Core.Tests/Orchestration/ContentIndexSeedWriteTests.cs (writer coverage)
  </read_first>
  <behavior>
    - Exporting a row whose BodySha256 = "<64hex>" (via ContentIndexExportRow.From) writes `"bodySha256": "<64hex>"` into index-seed.json.
    - Exporting a row with null BodySha256 writes it per the record's existing null policy (match how PublishedUtc/null renders in the golden — value or null, whatever the serializer already does for nullable optionals).
    - The export -> golden round-trip test passes with the fixture updated; CLI export and DirectPush both emit the field with no edits to their own code (they route through From()).
  </behavior>
  <action>In `ContentIndexExportRow.cs`: (a) add `public string? BodySha256 { get; init; }` to the `ContentIndexExportRow` record, placed to preserve the byte-stable declaration order (append after `CardCategoryTags`, the last field, so existing field order — and thus JSON key order — is unchanged for the pre-existing keys) with a `/// <summary>` xmldoc matching neighbors; nullable, non-required (mirror `PublishedUtc`). (b) map `BodySha256 = row.BodySha256,` in `From()`. Do NOT edit `ContentKbCommandRunners.cs` or `PublishCoordinator.cs` — they inherit the field via the shared factory. Update `Fixtures/index-seed.golden.json` to include `bodySha256` for each entry (match the null/value convention the golden uses for other nullable optionals and the camelCase key style already there). In `ContentIndexExportJsonGoldenTests.cs`, set `BodySha256 = "<stable test hash>"` (or null) on each `new ContentIndexExportRow { ... }` in `CreateRows()` so it compiles and the golden asserts the field. Update `ContentIndexSeedWriteTests.cs` if it asserts the field set. Keep entries otherwise byte-identical (golden tests are exact-match).</action>
  <verify>
    <automated>dotnet build DeckFlow.Core DeckFlow.Studio -c Debug; dotnet test DeckFlow.Core.Tests --filter "FullyQualifiedName~ContentIndexExportJsonGoldenTests|FullyQualifiedName~ContentIndexSeedWriteTests"</automated>
  </verify>
  <acceptance_criteria>
    - SOURCE: `ContentIndexExportRow.cs` has `BodySha256` on the record AND `BodySha256 = row.BodySha256` in `From()` (grep confirms both); `ContentKbCommandRunners.cs`/`PublishCoordinator.cs` are NOT in the diff.
    - SOURCE: the record's JSON key serializes camelCase as `bodySha256` (matches the golden fixture's other camelCase keys); `grep -c "bodySha256" Fixtures/index-seed.golden.json` >= 1 per seed entry.
    - BEHAVIOR: the golden export test and the seed-write test pass with the field present; a round-trip (From()->serialize then ContentKbSeedLoader load) preserves the hash; CreateRows() compiles with BodySha256 set.
  </acceptance_criteria>
  <done>bodySha256 exports (via the single shared factory) to and loads from index-seed.json; golden + writer tests green; CLI + DirectPush inherit it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| index-seed.json (git-committed) → store upsert | operator-owned, git-tracked seed data; not attacker-controlled input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-89-09 | Tampering | seed body_sha256 drops on reseed | mitigate | Adding bodySha256 to the single shared From() factory + the load mapper ensures a reseed/redeploy reconstructs the hash instead of nulling it — the exact drift SYNC-01 guards. Golden test locks the field's presence. |
| T-89-10 | Info | seed JSON is public repo | accept | body_sha256 is a hash of already-public KB body content; it leaks nothing. No secret. No new dependency (T-89-SC: none). |
</threat_model>

<verification>
- DeckFlow.Web / DeckFlow.Core / DeckFlow.Studio build clean.
- Golden export test and seed-writer test pass with bodySha256 present in the fixture.
- From()->serialize then load round-trip preserves body_sha256; no edits to CLI/PublishCoordinator projection code.
- README intentionally untouched — internal seed-schema plumbing, no user-facing change.
</verification>

<success_criteria>
- index-seed.json round-trips body_sha256 (camelCase bodySha256) through the shared export factory and the seed load.
- Golden fixture + writer tests updated and green; CLI export + DirectPush inherit the field with no code change.
</success_criteria>

<output>
Create `.planning/phases/89-content-hash-foundation/89-04-SUMMARY.md` when done.
</output>
