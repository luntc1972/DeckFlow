---
phase: 77-multi-axis-deck-score
plan: 04
type: execute
wave: 2
depends_on: ["77-01", "77-02", "77-03"]
files_modified:
  - DeckFlow.Web/Services/FeatureFlags/FeatureFlagCatalog.cs
  - DeckFlow.Web/Services/FeatureFlags/FeatureFlagStore.cs
  - DeckFlow.Web/Models/DeckAnalysisRequest.cs
  - DeckFlow.Web/Models/DeckAnalysisViewModel.cs
  - DeckFlow.Web/Services/DeckAnalysisPacketService.cs
  - DeckFlow.Web/Controllers/DeckPacketController.cs
  - DeckFlow.Web.Tests/DeckAnalysisScoreBlockTextTests.cs
  - DeckFlow.Web.Tests/DeckAnalysisScorePersistenceTests.cs
  - DeckFlow.Web.Tests/FeatureFlagStoreSeedTests.cs
  - DeckFlow.Web.Tests/FeatureFlagCatalogTests.cs
autonomous: true
requirements: [SCORE-01, SCORE-02, SCORE-03, SCORE-04]
must_haves:
  truths:
    - "analysis.multi-axis-score is registered in FeatureFlagCatalog and seeded FALSE/0 in both Postgres and SQLite"
    - "When the flag is OFF the analysis packet + all three artifacts are byte-identical to today (score null, no scoreBlockText)"
    - "When the flag is ON, BuildAsync computes a DeckMultiAxisScore from card references + bracket classification + combos and passes a built scoreBlockText to all three variants"
    - "Combos are fetched whenever the score flag is ON even if no combo question was selected (Power/Consistency do not floor for cEDH) — via ONE widened combo gate, never double-fetched"
    - "Score survives the Step-3 early-return via a ScoreJson hidden form field (mirrors DeckProfileJson)"
    - "DeckAnalysisViewModel.Score is populated so the view can render the score block"
  artifacts:
    - path: "DeckFlow.Web/Services/FeatureFlags/FeatureFlagStore.cs"
      provides: "Seed rows for analysis.multi-axis-score (both dialects)"
      contains: "analysis.multi-axis-score"
    - path: "DeckFlow.Web/Models/DeckAnalysisRequest.cs"
      provides: "ScoreJson round-trip hidden field"
      contains: "ScoreJson"
    - path: "DeckFlow.Web/Services/DeckAnalysisPacketService.cs"
      provides: "Score gate + compute + BuildScoreBlockText + Step-3 ScoreJson round-trip"
      contains: "MultiAxisScoreFlag"
    - path: "DeckFlow.Web.Tests/DeckAnalysisScoreBlockTextTests.cs"
      provides: "BuildScoreBlockText ASCII-format + figures test"
  key_links:
    - from: "DeckAnalysisPacketService.BuildAsync"
      to: "MultiAxisScorer.Score + BuildAnalysisPrompt"
      via: "flag-gated score computation, scoreBlockText passed as trailing arg"
      pattern: "MultiAxisScorer\\.Score|scoreBlockText"
    - from: "DeckPacketController"
      to: "DeckAnalysisViewModel.Score / request.ScoreJson"
      via: "set Score from result; round-trip ScoreJson"
      pattern: "Score = result\\.Score|ScoreJson"
---

<objective>
Wire the scorer into the live analysis flow: register the `analysis.multi-axis-score` flag (seeded
OFF, both dialects), add the round-trip + view-model + result fields, compute the score inside
`DeckAnalysisPacketService.BuildAsync` (always firing combos when the flag is ON), build the ASCII
`scoreBlockText` once and pass it to all three variants, persist the score across the Step-3
early-return via a `ScoreJson` hidden field, and surface `Score` to the controller's view model.

Purpose: integrates SCORE-01..04 end-to-end behind a default-OFF flag with byte-identical OFF output.
Output: flag + models + service + controller wired; BuildScoreBlockText format test GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/77-multi-axis-deck-score/77-RESEARCH.md
@.planning/phases/77-multi-axis-deck-score/77-PATTERNS.md
@.planning/phases/77-multi-axis-deck-score/UI-SPEC.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register analysis.multi-axis-score flag (catalog + seed both dialects)</name>
  <read_first>
    - DeckFlow.Web/Services/FeatureFlags/FeatureFlagCatalog.cs (existing `analysis.command-zone-awareness` / `tool.bracket.enabled` Descriptions entries, lines 73-76)
    - DeckFlow.Web/Services/FeatureFlags/FeatureFlagStore.cs (Postgres seed rows lines 227-229; SQLite seed rows lines 261-263; idempotent `ON CONFLICT (key) DO NOTHING`)
    - DeckFlow.Web.Tests/FeatureFlagStoreSeedTests.cs (lockstep seed guard — `[InlineData("<key>", false)]` rows, e.g. `analysis.manabase.tap-analyzer`/`tool.bracket.enabled`; a new seeded flag MUST get a row or the seed contract drifts)
    - DeckFlow.Web.Tests/FeatureFlagCatalogTests.cs (lockstep catalog guard — "Kept in lockstep with FeatureFlagStoreSeedTests"; asserts every seeded key has a non-empty Describe())
    - 77-PATTERNS.md "FeatureFlagCatalog.cs" + "FeatureFlagStore.cs" sections (exact entry + comma-handling guidance)
  </read_first>
  <action>
    Add a `["analysis.multi-axis-score"] = "..."` description to FeatureFlagCatalog (the wording in
    77-RESEARCH "Catalog description": four-axis score in Step-3 results + all three artifacts, "Off =
    byte-identical"). In FeatureFlagStore, append a Postgres seed row `('analysis.multi-axis-score', FALSE)`
    and a SQLite seed row `('analysis.multi-axis-score', 0)` before each block's `ON CONFLICT` line, fixing the
    trailing comma so the previously-last row gets a comma and the new row is last without one. Seed OFF — do
    NOT use IsEnabled() semantics anywhere. THEN keep the test guards in lockstep (Codex HIGH): add
    `[InlineData("analysis.multi-axis-score", false)]` to FeatureFlagStoreSeedTests, and ensure
    FeatureFlagCatalogTests passes for the new key (it already asserts every seeded key has a non-empty
    description — the catalog entry above satisfies it; add an explicit key assertion if that test enumerates
    keys). Touch only changed lines.
  </action>
  <verify>
    <automated>grep -c "analysis.multi-axis-score" DeckFlow.Web/Services/FeatureFlags/FeatureFlagStore.cs; dotnet.exe build DeckFlow.Web 2>&1 | tail -2</automated>
  </verify>
  <acceptance_criteria>
    - FeatureFlagStore.cs contains `('analysis.multi-axis-score', FALSE)` and `('analysis.multi-axis-score', 0)` (both dialects)
    - FeatureFlagCatalog.cs contains an `["analysis.multi-axis-score"]` description entry
    - FeatureFlagStoreSeedTests.cs contains `[InlineData("analysis.multi-axis-score", false)]`
    - `dotnet.exe test DeckFlow.Web.Tests --filter "FullyQualifiedName~FeatureFlag"` exits 0 (seed + catalog lockstep guards green with the new key)
    - `dotnet.exe build DeckFlow.Web` reports 0 warnings 0 errors
  </acceptance_criteria>
  <done>Flag registered with description, seeded OFF in both dialects idempotently, and the seed/catalog lockstep tests updated.</done>
</task>

<task type="auto">
  <name>Task 2: Add ScoreJson round-trip field and Score view-model property</name>
  <read_first>
    - DeckFlow.Web/Models/DeckAnalysisRequest.cs (DeckProfileJson hidden-field pattern lines 125-132: backing field + null-guard setter)
    - DeckFlow.Web/Models/DeckAnalysisViewModel.cs (CommandZoneAwarenessEnabled init-only property lines 22-24)
    - 77-PATTERNS.md "DeckAnalysisRequest.cs" + "DeckAnalysisViewModel.cs" sections
    - CLAUDE.md carve-out (init-only, no required; preserve LF; changed-lines only)
  </read_first>
  <action>
    In DeckAnalysisRequest.cs add a `_scoreJson` backing field (init string.Empty) and a public `string
    ScoreJson { get => _scoreJson; set => _scoreJson = value ?? string.Empty; }` immediately after
    DeckProfileJson, with XML doc describing the score round-trip across workflow steps. In
    DeckAnalysisViewModel.cs add `public DeckMultiAxisScore? Score { get; init; }` after
    CommandZoneAwarenessEnabled (add `using DeckFlow.Core.Analysis;` if needed), XML-documented. Touch only
    changed lines.
  </action>
  <verify>
    <automated>dotnet.exe build DeckFlow.Web 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - DeckAnalysisRequest.cs contains `public string ScoreJson` with a null-guard setter
    - DeckAnalysisViewModel.cs contains `public DeckMultiAxisScore? Score { get; init; }`
    - `dotnet.exe build DeckFlow.Web` reports 0 warnings 0 errors
  </acceptance_criteria>
  <done>ScoreJson round-trips on the request; Score is exposed on the view model.</done>
</task>

<task type="auto">
  <name>Task 3: Compute score in BuildAsync, build scoreBlockText, round-trip via Step-3, wire controller</name>
  <read_first>
    - DeckFlow.Web/Services/DeckAnalysisPacketService.cs (DeckAnalysisPacketResult record lines 57-62; ReferenceDeckStatsFlag gate lines 647-654; comboTask/RequiresComboLookup gate; BuildAnalysisPrompt dispatcher line 1191; Step-3 early-return + SetUpgradeResponseJson deserialize lines 371-398; BuildDeckStatsText input-prep lines 1123-1147)
    - DeckFlow.Web/Controllers/DeckPacketController.cs (DeckAnalysisViewModel construction with AnalysisResponse = result.AnalysisResponse — lines 122-134, 287-299; IsCommandZoneAwarenessEnabled flag-read pattern lines 60-64)
    - DeckFlow.Web/Services/Bracket/IGameChangerCatalogService.cs (GetCatalog() — in-memory, synchronous)
    - DeckFlow.Core/Bracket/BracketClassifier.cs + DeckFlow.Web/Services/Bracket/BracketClassificationService.cs (combo->TwoCardCombo mapping lines 99-104; catalog+Classify call)
    - DeckFlow.Core/Analysis/MultiAxisScorer.cs + MultiAxisScore.cs (from 77-02 — Score signature + records)
    - 77-PATTERNS.md "DeckAnalysisPacketService.cs (BuildAsync scoring wiring)" + "Code Examples" in 77-RESEARCH (combo mapping, DeckStatAggregator.Compute call, MultiAxisScorer invocation, flag gate, BuildScoreBlockText format from UI-SPEC §10)
    - DeckFlow.Web/AssemblyInfo.cs ([InternalsVisibleTo("DeckFlow.Web.Tests")] — lets BuildScoreBlockText be internal-static and unit-tested)
  </read_first>
  <action>
    In DeckAnalysisPacketService:
    1. Inject `IGameChangerCatalogService` as a new constructor parameter (`_catalogService`) — it is already
       DI-registered from Phase 76.
    2. Add `internal const string MultiAxisScoreFlag = "analysis.multi-axis-score";` and an explicit snapshot
       gate `_flagCache is not null && _flagCache.Snapshot().TryGetValue(MultiAxisScoreFlag, out var scoreOn) &&
       scoreOn` (NEVER IsEnabled()).
    3. When the flag is ON, WIDEN the EXISTING combo gate so the single `comboTask` fires once when
       `scoreEnabled || AnalysisQuestionCatalog.RequiresComboLookup(selectedQuestions)` — reuse the one
       `comboResult` for BOTH the prompt combo-reference text and the score's combo-density signal. Do NOT add a
       second parallel `comboForScoreTask` (that would double-call Commander Spellbook whenever a combo question
       is ALSO selected — Codex HIGH). Keep the prompt-side `RequiresComboLookup` gate that decides whether combo
       REFERENCE TEXT is emitted, so the prompt output is unchanged when no combo question is selected. Track
       `comboDetectionAvailable = comboResult is not null` (Pitfall 1 — never treat null as zero combos).
    4. Call `DeckStatAggregator.Compute(...)` independently of ReferenceDeckStatsFlag (Pitfall 5), using the
       current-deck-scope non-commander card references (mirror BuildDeckStatsText input prep). Map combos to
       `IReadOnlyList<TwoCardCombo>?` (2-card filter), call `BracketClassifier.Classify(deckEntries, catalog,
       mappedCombos)`, then `MultiAxisScorer.Score(stats, bracketClassification.DetectedGameChangers.Count,
       twoCardComboCount, comboDetectionAvailable, bracketClassification.BracketNumber)`.
    5. Add `internal static string BuildScoreBlockText(DeckMultiAxisScore score)` producing the ASCII artifact
       format from UI-SPEC §10 (header "DECK SCORE (coarse 0-5 bands - magnitude, not quality)", four
       "Axis: N/5 Label (rationale)" lines via MultiAxisScorer.BandLabel + each axis rationale, the cross-check
       line, and the "heuristic estimates ... re-check and refine" disclaimer). ASCII only — plain hyphens,
       no em/en dashes.
    6. Set `scoreBlockText = scoreEnabled ? BuildScoreBlockText(score) : null` and pass it as the trailing arg
       to BuildAnalysisPrompt (which forwards to the registry/variants from 77-03).
    7. Add `DeckMultiAxisScore? Score = null` as a trailing optional param on the DeckAnalysisPacketResult
       record and set it on the result in the Step-2/score path.
    8. In the Step-3 early-return path, deserialize `request.ScoreJson` into `DeckMultiAxisScore?` (mirror the
       SetUpgradeResponseJson deserialize; wrap in try/catch -> null on malformed input) and set it on the
       returned result. Serialize the computed score to JSON into the result/request ScoreJson at Step 2 so the
       hidden field round-trips (mirror DeckProfileJson).
    In DeckPacketController: set `Score = result.Score` on the DeckAnalysisViewModel wherever AnalysisResponse
    is set from a packet result, and populate `request.ScoreJson` from the computed score so the view's hidden
    field carries it forward. Then add DeckFlow.Web.Tests/DeckAnalysisScoreBlockTextTests.cs asserting
    BuildScoreBlockText output contains "DECK SCORE", the four axis labels, "N/5" figures, the cross-check line,
    and no em/en dash characters. Touch only changed lines; do NOT change byte output when the flag is OFF.
  </action>
  <verify>
    <automated>dotnet.exe test DeckFlow.Web.Tests --filter "FullyQualifiedName~DeckAnalysisScoreBlockText" 2>&1 | tail -6; dotnet.exe build DeckFlow.Web 2>&1 | tail -2</automated>
  </verify>
  <acceptance_criteria>
    - DeckAnalysisPacketService.cs contains `MultiAxisScoreFlag` read via `_flagCache.Snapshot().TryGetValue` (NOT `IsEnabled`)
    - DeckAnalysisPacketService.cs contains `internal static string BuildScoreBlockText(`
    - DeckAnalysisPacketService.cs widens the SINGLE combo gate to `scoreEnabled || ...RequiresComboLookup(...)` and contains NO second `comboForScoreTask` (one fetch, reused — `grep -c "FindCombosAsync" DeckAnalysisPacketService.cs` is unchanged from baseline +0)
    - A test asserts Commander Spellbook is fetched exactly once when both the score flag is ON and a combo question is selected (no double-fetch)
    - DeckAnalysisPacketService.cs Step-3 early-return deserializes `request.ScoreJson` into the result Score
    - DeckPacketController.cs sets `Score = result.Score` on the view model
    - DeckPacketController.cs (Step-2 path) sets `request.ScoreJson = JsonSerializer.Serialize(result.Score)` when `result.Score is not null`, so the hidden field carries the score forward to Step-3 (guards the silent Step-3 disappearance: the score renders at Step-2 via Model.Score but is lost at Step-3 if this write is omitted)
    - DeckAnalysisScoreBlockTextTests.cs asserts the ASCII format (contains "DECK SCORE", "/5", no em/en dash) and exits 0 under the filter
    - DeckAnalysisScorePersistenceTests.cs covers the Step-3 round-trip: (a) valid ScoreJson deserializes back to the same DeckMultiAxisScore on the early-return, (b) malformed/oversized ScoreJson yields `Score == null` (try/catch fallback, no throw)
    - `dotnet.exe build DeckFlow.Web` 0/0 and full `dotnet.exe test DeckFlow.Web.Tests` exits 0 (no regression; OFF path byte-identical)
  </acceptance_criteria>
  <done>Score computed + round-tripped behind the flag; OFF path unchanged; controller surfaces Score; format test GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| browser form -> DeckPacketController/BuildAsync | The `ScoreJson` hidden field is client-supplied untrusted input deserialized at the Step-3 early-return and re-emitted into the artifact + rendered in the view |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-77-04-01 | Tampering | request.ScoreJson deserialize (Step-3) | mitigate | Deserialize into the typed `DeckMultiAxisScore` record only, inside try/catch -> null on malformed/oversized input; bands are ints clamped 0-5 by the scorer's own usage; never `eval`/reflect client strings |
| T-77-04-02 | Injection (artifact/UI) | client-supplied rationale strings re-emitted | mitigate | Artifact is plaintext (no code path); Razor view HTML-encodes `@`-rendered rationale (plan 77-05); no raw HTML emit of ScoreJson values |
| T-77-04-03 | DoS | always-on combo fetch when flag ON | accept | One extra Commander Spellbook task (~100ms) gated behind a default-OFF operator flag; existing Polly resilience pipeline applies |
| T-77-SC | Tampering | package installs | mitigate | No new NuGet packages; reuses Phase-76 IGameChangerCatalogService + existing combo/Scryfall paths |
</threat_model>

<verification>
- `dotnet.exe build DeckFlow.Web` clean (0/0)
- `dotnet.exe test DeckFlow.Web.Tests` GREEN (full Web suite; no OFF-path regression)
- Flag seeded OFF in both dialects (grep confirms)
</verification>

<success_criteria>
End-to-end score computation behind `analysis.multi-axis-score` (default OFF, byte-identical OFF output);
combos always fetched when ON; score round-trips through Step-3 via ScoreJson; controller exposes Score;
BuildScoreBlockText emits paste-safe ASCII.
</success_criteria>

<output>
Create `.planning/phases/77-multi-axis-deck-score/77-04-SUMMARY.md` when done
</output>
