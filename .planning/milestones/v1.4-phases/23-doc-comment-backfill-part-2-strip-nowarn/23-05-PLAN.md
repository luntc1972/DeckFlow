---
phase: 23-doc-comment-backfill-part-2-strip-nowarn
plan: 05
type: execute
wave: 2
depends_on: ["23-01", "23-02", "23-03", "23-04"]
files_modified:
  - DeckFlow.Web/DeckFlow.Web.csproj
  - .editorconfig
autonomous: false
requirements: [DOC-02]
user_setup: []
must_haves:
  truths:
    - "DeckFlow.Web.csproj no longer contains the <NoWarn>$(NoWarn);1591;1573;1587</NoWarn> line (deleted entirely per LD-2)"
    - ".editorconfig lines 94-96 set CS1591/CS1573/CS1587 severity = warning (NOT none, NOT deleted) and line 93 comment updated per LD-2"
    - "An injected undocumented probe class PROVES the gate fires (CS1591 surfaces) — guarding against a false-green"
    - "dotnet build -c Release -warnaserror:CS1591,CS1573,CS1587 succeeds from a clean obj/ with 0 warnings on user code (all three doc codes promoted — CS1591 alone false-greens on surviving CS1573/CS1587)"
    - "DeckFlow.Web.Tests still builds clean (editorconfig severity is solution-wide; test projects already documented)"
  artifacts:
    - path: "DeckFlow.Web/DeckFlow.Web.csproj"
      provides: "NoWarn line removed (DOC-02 literal wording)"
    - path: ".editorconfig"
      provides: "CS1591/1573/1587 severity = warning (the real solution-wide gate)"
  key_links:
    - from: ".editorconfig severity=warning + csproj NoWarn removed"
      to: "live CS1591/1573/1587 build gate"
      via: "both independent suppressors removed (research-proven both are required)"
      pattern: "dotnet_diagnostic.CS1591.severity = warning"
---

<objective>
DOC-02 — flip the doc-warning gate ON. This is the LAST plan in the phase (Wave 2): it runs ONLY
after 23-01..23-04 have documented all 475 sites. If the strip runs before backfill completes, the
build turns red mid-phase (23-RESEARCH Pitfall 4 / ROADMAP Pitfall 8).

The research PROVED the diagnostic is suppressed in TWO independent places (23-RESEARCH Pitfall 1 +
"Architecture Patterns"). Stripping only the csproj is a no-op — the `-warnaserror:CS1591` "gate"
passes trivially while still suppressed. BOTH must change:
  1. DeckFlow.Web.csproj:40 — DELETE the `<NoWarn>$(NoWarn);1591;1573;1587</NoWarn>` line (LD-2; satisfies DOC-02's literal wording).
  2. .editorconfig:93-96 — change the three `dotnet_diagnostic.CS{1591,1573,1587}.severity = none`
     to `= warning` (NOT delete — keep as a visible record per LD-2), and update the line-93 comment
     (it currently reads "already suppressed in csproj (NoWarn 1591;1573;1587)").

.editorconfig is on CLAUDE.md's Do-Not-Modify list; the USER EXPLICITLY APPROVED this single edit
for this task (LD-2). This plan is NON-autonomous: a blocking human-verify checkpoint confirms the
approved-edit boundary and the probe-validated gate before commit.

Gate proof (LD-3 — verify-once, no permanent TreatWarningsAsErrors, no CI workflow edit): run
`dotnet build -warnaserror:CS1591,CS1573,CS1587` once from clean obj/ to PROVE 0 warnings;
severity=warning is the durable state. ALL THREE codes MUST be promoted: `-warnaserror:CS1591` alone
only promotes 1591, so a surviving CS1573 or CS1587 stays a warning and the build still exits 0 (a
false-green). Since LD-1 fixes all three classes, the gate must enforce all three. Do NOT add
TreatWarningsAsErrors to the csproj. Do NOT touch .github/workflows.

Purpose: DOC-02 — the missing-doc warning becomes a real (warning-severity) gate, provably live.
Output: two-file edit (csproj line removed + editorconfig severity flipped) + a probe-validated
0-warning Release build.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/23-doc-comment-backfill-part-2-strip-nowarn/23-RESEARCH.md
@./CLAUDE.md

<current_state>
DeckFlow.Web/DeckFlow.Web.csproj:40  ->  <NoWarn>$(NoWarn);1591;1573;1587</NoWarn>
.editorconfig:93  ->  "# Suppress diagnostic warnings already suppressed in csproj (NoWarn 1591;1573;1587)."
.editorconfig:94  ->  dotnet_diagnostic.CS1591.severity = none
.editorconfig:95  ->  dotnet_diagnostic.CS1573.severity = none
.editorconfig:96  ->  dotnet_diagnostic.CS1587.severity = none
GenerateDocumentationFile=true is already set (csproj:38) — keep it.
(Line numbers re-verified at plan time against HEAD e9471fb; re-confirm at execute time.)
</current_state>

<gate_proof_methodology>
From 23-RESEARCH.md "Gate-proof methodology (REQUIRED for SC3)". The -warnaserror exit code alone is
NOT trustworthy — inject a probe to prove the warning actually fires, then remove it. ASSERT the
probe's warning in the captured build output (do not just run the build):

  printf '%s\n' 'namespace DeckFlow.Web;' 'public sealed class __TempUndocProbe { public int X { get; init; } }' > DeckFlow.Web/__TempUndocProbe.cs
  # with both suppressors removed, this MUST produce CS1591 on the type + .X. Capture the build
  # output to a log and assert it, e.g.:  grep -E '__TempUndocProbe.*warning CS1591|warning CS1591.*__TempUndocProbe' <log>
  # if it does NOT warn, a suppressor is still active and the gate is fake — STOP.
  rm DeckFlow.Web/__TempUndocProbe.cs   # ALWAYS clean up — never commit the probe

NOTE on three-code coverage: the final proof-gate build promotes ALL THREE doc codes
(`-warnaserror:CS1591,CS1573,CS1587`). `-warnaserror:CS1591` alone would let a surviving CS1573
(missing `<param>`) or CS1587 (mis-placed `///`) remain a mere warning and exit 0 — a false-green.
Belt-and-suspenders: ALSO capture the gate build output and fail if any `warning CS(1591|1573|1587)`
line appears on user-authored code.
</gate_proof_methodology>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <what-built>
About to edit `.editorconfig` — a file on CLAUDE.md's "Do Not Modify Without Explicit Permission"
list. Per LD-2 the user explicitly approved this single edit for this task. The two edits are:
  1. DeckFlow.Web.csproj:40 — delete the `<NoWarn>$(NoWarn);1591;1573;1587</NoWarn>` line.
  2. .editorconfig:94-96 — `severity = none` to `severity = warning` (x3); update line-93 comment.
Precondition: 23-01..23-04 are complete (all 475 doc sites backfilled). Confirm before the strip.
  </what-built>
  <how-to-verify>
  1. Confirm 23-01-SUMMARY.md..23-04-SUMMARY.md all exist and report PASS.
  2. Confirm you approve the `.editorconfig` edit (Do-Not-Modify list; LD-2 approval).
  3. Confirm the strip should land NOW (after backfill, not before).
  </how-to-verify>
  <resume-signal>Type "approved" to proceed with the two-file flip, or describe concerns.</resume-signal>
</task>

<task type="auto">
  <name>Task 1: Flip both suppressors (csproj delete + editorconfig severity=warning)</name>
  <files>DeckFlow.Web/DeckFlow.Web.csproj, .editorconfig</files>
  <action>
ALLOWED FILE SET (this task may edit ONLY these 2 files — ignore every other file/task):
  (a) DeckFlow.Web/DeckFlow.Web.csproj — DELETE line 40 entirely (the
      `<NoWarn>$(NoWarn);1591;1573;1587</NoWarn>` line). Leave GenerateDocumentationFile=true and
      every other PropertyGroup line byte-identical.
  (b) .editorconfig — change lines 94-96 from `severity = none` to `severity = warning` for
      CS1591, CS1573, CS1587 (do NOT delete the keys — keep them as a visible record per LD-2).
      Update the line-93 comment to reflect the gate is now ON (it currently says "already
      suppressed in csproj"); reword to note the diagnostics are surfaced as warnings post-Phase-23.
Touch ONLY those lines. Preserve LF endings (.gitattributes). Do NOT add TreatWarningsAsErrors. Do
NOT touch .github/workflows (LD-3).
  </action>
  <verify>
    <automated>echo "csproj NoWarn (expect none):"; grep -n 'NoWarn' DeckFlow.Web/DeckFlow.Web.csproj || echo "OK NoWarn removed"; echo "editorconfig severities (expect 3x warning):"; grep -n 'dotnet_diagnostic.CS15' .editorconfig</automated>
  </verify>
  <done>csproj has no `<NoWarn>` line; .editorconfig CS1591/1573/1587 all read `severity = warning`; line-93 comment updated; only those lines changed.</done>
</task>

<task type="auto">
  <name>Task 2: Probe-validated gate proof + clean Release build</name>
  <files>(no source files committed — temporary probe is created then deleted)</files>
  <action>
With both suppressors removed (Task 1), prove the gate is REAL, then prove user code is clean —
from a CLEAN obj/ (LD-3). Use Windows dotnet.exe (VSTest/builds unreliable in WSL — 23-RESEARCH P6):
  DOTNET="/mnt/c/Program Files/dotnet/dotnet.exe"
  1. Clean: rm -rf DeckFlow.Web/obj DeckFlow.Web/bin
  2. PROBE-ON: create DeckFlow.Web/__TempUndocProbe.cs (see <gate_proof_methodology>); build
     `"$DOTNET" build DeckFlow.Web/DeckFlow.Web.csproj -c Release --no-incremental` and CONFIRM it
     emits CS1591 on __TempUndocProbe (type + .X). If it does NOT warn, a suppressor is still active
     — STOP, the gate is fake. Then DELETE the probe file.
  3. PROBE-OFF clean gate: rm -rf obj/bin again; capture output to a log and run
     `"$DOTNET" build DeckFlow.Web/DeckFlow.Web.csproj -c Release --no-incremental -warnaserror:CS1591,CS1573,CS1587 2>&1 | tee /tmp/23-05-gate.log`
     — MUST succeed with 0 warnings / 0 errors on user-authored code (proves all 475 sites closed).
     ALL THREE codes are promoted because `-warnaserror:CS1591` alone false-greens on a surviving
     CS1573/CS1587. Belt-and-suspenders: assert the log is clean — `grep -E 'warning CS(1591|1573|1587)' /tmp/23-05-gate.log`
     MUST return nothing; if it matches, FAIL (a doc site escaped, even if the promoted exit code
     somehow passed).
  4. Full-solution guard: `"$DOTNET" build DeckFlow.sln -c Release` — confirm DeckFlow.Web.Tests +
     DeckFlow.Core.Tests still build clean (editorconfig severity is solution-wide; research verified
     test projects are already documented so no new warnings surface).
  5. Confirm `git status` shows NO __TempUndocProbe.cs left behind.
Do NOT add TreatWarningsAsErrors to the csproj; do NOT touch .github/workflows.
  </action>
  <verify>
    <automated>DOTNET="/mnt/c/Program Files/dotnet/dotnet.exe"; rm -rf DeckFlow.Web/obj DeckFlow.Web/bin; "$DOTNET" build DeckFlow.Web/DeckFlow.Web.csproj -c Release --no-incremental -warnaserror:CS1591,CS1573,CS1587 2>&1 | tee /tmp/23-05-gate.log | tail -5; echo "surviving doc warnings (expect none):"; grep -E 'warning CS(1591|1573|1587)' /tmp/23-05-gate.log && echo "FAIL: doc warning survived" || echo "OK no doc warnings"; echo "probe cleanup (expect none):"; ls DeckFlow.Web/__TempUndocProbe.cs 2>/dev/null || echo "OK no probe"</automated>
  </verify>
  <done>PROBE-ON build emitted CS1591 (gate is real); PROBE-OFF `-warnaserror:CS1591,CS1573,CS1587` Release build from clean obj/ = 0 warnings / 0 errors AND `grep -E 'warning CS(1591|1573|1587)'` over the captured log returns nothing; full-solution build clean; no probe file committed.</done>
</task>

</tasks>

<verification>
Maps to ROADMAP Phase 23 Success Criteria:
- SC1 (all types documented before strip): satisfied by 23-01..23-04 (precondition checkpoint) +
  proven by SC3's per-type build gate here.
- SC2 (NoWarn line removed): Task 1 csproj delete; `grep NoWarn` returns empty.
- SC3 (ROADMAP contract: `dotnet build -c Release -warnaserror:CS1591` clean from clean obj/): Task 2
  PROBE-OFF build. This plan's gate is STRICTER than the contract — it promotes ALL THREE codes
  (`-warnaserror:CS1591,CS1573,CS1587`) plus a log-grep, because CS1591 alone would false-green on a
  surviving CS1573/CS1587. A stricter local gate still satisfies (exceeds) the SC3 contract; the
  ROADMAP SC text itself is unchanged.
- SC4 (tests Failed:0 + touch-only): full-solution build clean (Task 2.4); doc edits are
  compile-stripped so no runtime regression is possible (23-RESEARCH "Test Impact"); CI push-watch
  runs the xUnit suites (VSTest unreliable in WSL). The csproj/editorconfig diff is the only
  non-`///` change in the phase.
</verification>

<success_criteria>
- DOC-02 met: csproj `<NoWarn>` line gone AND .editorconfig CS1591/1573/1587 = warning.
- Gate proven REAL via the injected probe (not a false-green exit code).
- `dotnet build -c Release -warnaserror:CS1591,CS1573,CS1587` from clean obj/ = 0 warnings / 0 errors
  on user code, AND the captured build log contains no `warning CS(1591|1573|1587)` line (all three
  doc codes enforced — CS1591 alone false-greens on a surviving CS1573/CS1587).
- Full-solution build clean (test projects unaffected).
- No TreatWarningsAsErrors added; no CI workflow file touched (LD-3); no probe file committed.
</success_criteria>

<output>
Create `.planning/phases/23-doc-comment-backfill-part-2-strip-nowarn/23-05-SUMMARY.md` when done.
</output>
