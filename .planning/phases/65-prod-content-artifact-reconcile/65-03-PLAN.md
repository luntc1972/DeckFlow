---
phase: 65-prod-content-artifact-reconcile
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - DeckFlow.Core/Content/ContentKbOrphanScanner.cs
  - DeckFlow.Core.Tests/Content/ContentKbOrphanScannerTests.cs
  - DeckFlow.CLI/ContentKbCommandRunners.cs
  - DeckFlow.CLI/Program.cs
autonomous: true
requirements: [DATA-02]
must_haves:
  truths:
    - "A pure DeckFlow.Core helper classifies each content_site_index row as OK / published-orphan / hidden-orphan given a row set and a content base, resolving each row exactly as the live serving path does (Path.GetFullPath(Path.Combine(contentBase, row.ArtifactPath)) where row.ArtifactPath already begins with content-kb/), with no console or IO glue in the classification path."
    - "The scanner rejects artifact_path values that are rooted or contain '..' segments before combining with the content base (reusing the ValidateArtifactPath guard logic)."
    - "The content-kb-check CLI normalizes --artifact-root so passing either the data-root parent OR the content-kb directory resolves to the same artifact path (no content-kb/content-kb/ double-prefix)."
    - "A new read-only `content-kb-check` CLI command reports orphans and exits 1 when published-orphan count > 0, else 0."
  artifacts:
    - path: "DeckFlow.Core/Content/ContentKbOrphanScanner.cs"
      provides: "Pure orphan-detection helper (Scan over rows + content base -> classified result with counts), mirroring ContentKbArtifactPathResolver.ResolveArtifactFullPath"
      contains: "ContentKbOrphanScanner"
      min_lines: 40
    - path: "DeckFlow.Core.Tests/Content/ContentKbOrphanScannerTests.cs"
      provides: "xUnit tests: missing-file detection, published vs hidden classification, path-traversal rejection, content-base resolution (no double-prefix), --artifact-root normalization equivalence"
      contains: "ContentKbOrphanScannerTests"
    - path: "DeckFlow.CLI/ContentKbCommandRunners.cs"
      provides: "RunContentKbCheckAsync static handler returning exit code, with --artifact-root content-base normalization"
      contains: "RunContentKbCheckAsync"
    - path: "DeckFlow.CLI/Program.cs"
      provides: "content-kb-check command registration"
      contains: "content-kb-check"
  key_links:
    - from: "RunContentKbCheckAsync (CLI)"
      to: "ContentKbOrphanScanner.Scan (Core)"
      via: "handler normalizes --artifact-root to a content base, calls pure scanner, prints, sets Environment.ExitCode"
      pattern: "ContentKbOrphanScanner"
    - from: "ContentKbOrphanScanner.Scan"
      to: "live serving resolution (ContentKbArtifactPathResolver.ResolveArtifactFullPath)"
      via: "Path.GetFullPath(Path.Combine(contentBase, row.ArtifactPath)) — contentBase is the parent that CONTAINS content-kb/, row.ArtifactPath already starts with content-kb/"
      pattern: "Path.Combine"
    - from: "ContentKbOrphanScanner.Scan"
      to: "artifact_path guard"
      via: "rooted/.. rejection before Path.Combine"
      pattern: "Path.IsPathRooted|\\.\\."
---

<objective>
Add the read-only `content-kb-check` CLI command that joins `content_site_index` rows to local
`.md` artifact files and reports orphans, exiting 1 when any PUBLISHED orphan exists. Extract the
orphan-detection as a pure, unit-testable helper `ContentKbOrphanScanner` in `DeckFlow.Core` so
the CLI handler stays thin and the classification logic is covered by xUnit (per
65-VALIDATION.md Wave 0). This is the SC3 repeatable check.

Purpose: SC3 ("a post-reconcile check confirms every remaining prod row's artifact expectation
matches reality") needs a repeatable, deterministic tool. The pure scanner makes the severity
gate (published vs hidden orphans) testable without prod or console glue.

Critical correctness invariant: `row.ArtifactPath` ALREADY begins with `content-kb/` (it is
`content-kb/{source-slug}/{video-id}.md` — see `ContentArtifactWriter.ComputeRelativeArtifactPath`).
The LIVE serving path resolves existence as `Path.GetFullPath(Path.Combine(contentBase, ArtifactPath))`
where `contentBase` is the PARENT directory that CONTAINS `content-kb/` (NOT the `content-kb` dir
itself) — see `ContentKbArtifactPathResolver.ResolveArtifactFullPath`. The scanner MUST mirror this
exactly. Combining the existing CLI helper `ContentKbCliPaths.ResolveArtifactRoot` (which returns
the `content-kb` DIRECTORY) directly with `row.ArtifactPath` would produce
`<root>/content-kb/content-kb/{slug}/{id}.md` and falsely report every row as a missing/published
orphan. The CLI command therefore normalizes `--artifact-root` to a content base before calling the
scanner (see Task 2).

Output: `ContentKbOrphanScanner.cs` (+ tests) in Core, `RunContentKbCheckAsync` in the CLI runner,
and the `content-kb-check` command in `Program.cs`.

This plan is independent of the Plan 01 probe results — it can land in Wave 1 in parallel with 65-01.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/65-prod-content-artifact-reconcile/65-RESEARCH.md
@CLAUDE.md

<interfaces>
<!-- Verified contracts the executor uses directly. No codebase exploration needed. -->

Row type — DeckFlow.Core/Knowledge/ContentArtifactSpec.cs:
  public sealed record ContentSiteIndexRow {
    public required string Source { get; init; }
    public required string Title { get; init; }
    public required string ArtifactPath { get; init; }   // relative, ALREADY content-kb/{slug}/{id}.md
    public bool IsVisible { get; init; }
    public bool IsHidden { get; init; }
    public string ApprovalStatus { get; init; } = "pending";
    ... // plus tag arrays, natural key, timestamps (not needed by the scanner)
  }
  -> ArtifactPath ALREADY begins with "content-kb/" — source: ContentArtifactWriter.ComputeRelativeArtifactPath
     (DeckFlow.Core/Knowledge/ContentArtifactWriter.cs:21) returns $"content-kb/{slug}/{file}".
     DO NOT prepend content-kb/ again.

AUTHORITATIVE artifact resolution to MIRROR — DeckFlow.Web/Services/Content/ContentKbArtifactPathResolver.cs:51-56:
  public string ResolveArtifactFullPath(string artifactPath)
    => Path.GetFullPath(Path.Combine(ContentBase, artifactPath));
  where ContentBase is "the base directory that contains the content-kb artifact tree" (the PARENT of
  content-kb/, NOT content-kb/ itself — see the property doc at line 36-39 and the existence check at
  line 29: Directory.Exists(Path.Combine(ContentBase, "content-kb"))).
  -> The scanner's existence check MUST be Path.GetFullPath(Path.Combine(contentBase, row.ArtifactPath)),
     identical to this. contentBase plays the role of ContentBase.

Path guard to REUSE (logic, not the private method) — DeckFlow.Core/Content/ContentSiteIndexStore.cs:742-766:
  private static void ValidateArtifactPath(string artifactPath):
    - throws ArgumentException("Artifact path must be relative.", nameof(...ArtifactPath)) if
      Path.IsPathRooted(artifactPath) OR IsWindowsRootedPath(artifactPath)
      (len>=3, char.IsLetter([0]), [1]==':', [2]=='\\' or '/')
    - throws ArgumentException("Artifact path must not contain '..' path segments.", ...) if any
      '/'- or '\\'-split segment (StringSplitOptions.RemoveEmptyEntries) equals ".."
  -> The scanner must apply the SAME two checks (same messages) to row.ArtifactPath BEFORE Path.Combine.
     It is private in the store; duplicate the equivalent guard inside ContentKbOrphanScanner (or extract
     a shared internal helper) — do NOT loosen it.

CLI artifact-root helper (the value passed to --artifact-root by default) — DeckFlow.CLI/ContentKbCliPaths.cs:21-31:
  public static string ResolveArtifactRoot(FileInfo? db):
    - if MTG_DATA_DIR set: returns Path.GetFullPath(Path.Combine(dataDir, "content-kb"))   // the content-kb DIR
    - else: returns Path.GetFullPath(Path.Combine(CurrentDirectory, "content-kb"))         // the content-kb DIR
  -> This returns the content-kb DIRECTORY, NOT the parent. The CLI handler must NORMALIZE it to a
     content base (see Task 2) before handing it to the scanner, otherwise the scanner double-prefixes.

Store read — DeckFlow.Core/Content/ContentSiteIndexStore.cs:
  public ContentSiteIndexStore(string databasePath)
  public async Task<IReadOnlyList<ContentSiteIndexRow>> GetAllRowsAsync(CancellationToken = default)
  -> Constructor + first call run EnsureSchemaAsync (CREATE TABLE IF NOT EXISTS). Harmless against
     a LOCAL sqlite content-kb.db. NEVER point this at prod (research anti-pattern: no EnsureSchema
     against prod). The CLI command operates on a local --db only.

CLI runner pattern — DeckFlow.CLI/ContentKbCommandRunners.cs:
  internal static class ContentKbCommandRunners
  Existing read-only analog: public static async Task<int> RunContentIndexExportAsync(FileInfo? db, FileInfo? output)
    - resolves dbPath via ContentKbCliPaths.ResolveDatabasePath(db)
    - resolves artifactRoot via ContentKbCliPaths.ResolveArtifactRoot(db)
    - try/catch (Exception ex when not OperationCanceledException) { Console.Error.WriteLine(ex.Message); return 1; }
  ContentKbCliPaths.ResolveDatabasePath(FileInfo?) and ResolveArtifactRoot(FileInfo?) exist.

CLI registration pattern — DeckFlow.CLI/Program.cs:
  var contentIndexExportCommand = new Command("content-index-export", "...");
  ...Option<FileInfo?>("--db") { Description = "..." }; AddOption(...)
  rootCommand.AddCommand(contentIndexExportCommand);
  contentIndexExportCommand.SetHandler((FileInfo? db, FileInfo? output) =>
  {
      Environment.ExitCode = ContentKbCommandRunners.RunContentIndexExportAsync(db, output).GetAwaiter().GetResult();
  }, dbOption, outOption);

Test conventions — DeckFlow.Core.Tests (e.g. ContentArtifactWriterTests.cs):
  xUnit, `public sealed class XxxTests : IDisposable`, temp dir under Path.GetTempPath()/$"...{Guid.NewGuid():N}",
  Dispose() deletes the temp dir. New C# files: CRLF, LF enforced repo-wide via .gitattributes only on
  tracked content — match the existing sibling file's EOL; changed-lines format gate applies.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 — ContentKbOrphanScanner (Core) + xUnit tests</name>
  <files>DeckFlow.Core/Content/ContentKbOrphanScanner.cs, DeckFlow.Core.Tests/Content/ContentKbOrphanScannerTests.cs</files>
  <read_first>
    - DeckFlow.Core/Content/ContentSiteIndexStore.cs lines 742-766 (ValidateArtifactPath + IsWindowsRootedPath — the guard to reuse)
    - DeckFlow.Web/Services/Content/ContentKbArtifactPathResolver.cs lines 36-56 (ResolveArtifactFullPath — the AUTHORITATIVE combine semantics: Path.GetFullPath(Path.Combine(ContentBase, artifactPath)), ContentBase = parent that contains content-kb/)
    - DeckFlow.Core/Knowledge/ContentArtifactWriter.cs lines 18-22 (ComputeRelativeArtifactPath — proof ArtifactPath already starts with content-kb/)
    - DeckFlow.Core/Knowledge/ContentArtifactSpec.cs lines 107-150 (ContentSiteIndexRow record shape)
    - DeckFlow.Core.Tests/ContentArtifactWriterTests.cs lines 1-30 (test class + temp-dir + IDisposable convention)
    - DeckFlow.Core.Tests/Content/ContentSiteIndexStoreVisibilityTests.cs (sibling content-test conventions for is_visible/is_hidden setup)
  </read_first>
  <behavior>
    - Given a row whose artifact file EXISTS at &lt;contentBase&gt;/content-kb/{slug}/{id}.md (i.e. contentBase + row.ArtifactPath, where row.ArtifactPath = "content-kb/{slug}/{id}.md") → classified OK (not an orphan). This is the regression case: it MUST fail under any logic that prepends content-kb/ a second time.
    - Given a row whose artifact file is ABSENT and IsVisible=TRUE && IsHidden=FALSE → published orphan.
    - Given a row whose artifact file is ABSENT and IsVisible=FALSE → hidden orphan (not published).
    - Given a row whose artifact file is ABSENT and IsVisible=TRUE && IsHidden=TRUE → NOT a published orphan (published requires is_hidden=FALSE per research anti-pattern note).
    - Result exposes: TotalRows, RowsWithArtifact, MissingCount, PublishedOrphanCount, HiddenOrphanCount, and a per-row classification list (path, exists, isVisible, approvalStatus).
    - Given a row with a rooted artifact_path (e.g. "/etc/passwd") or a "../" segment → Scan throws ArgumentException (path-traversal rejection) BEFORE any Path.Combine/File.Exists.
  </behavior>
  <action>
    Create `ContentKbOrphanScanner` in namespace `DeckFlow.Core.Content` (file-scoped namespace,
    Allman braces, sealed, XML doc on public surface). Expose a pure static
    `Scan(IReadOnlyList&lt;ContentSiteIndexRow&gt; rows, string contentBase)` returning a
    `ContentKbOrphanScanResult` record (also in this file or a sibling record type) carrying the
    counts in the behavior block plus a `IReadOnlyList&lt;ContentKbRowCheck&gt;` of per-row results.
    Name the parameter `contentBase` (NOT artifactRoot) — it is the directory that CONTAINS
    `content-kb/`, mirroring `ContentKbArtifactPathResolver.ContentBase`. For each row: FIRST apply
    the artifact-path guard to `row.ArtifactPath` — reject `Path.IsPathRooted(row.ArtifactPath)`, the
    Windows-rooted form (len&gt;=3, char.IsLetter([0]), ':' at [1], '/' or '\\' at [2]), and any
    '/'- or '\\'-split segment (RemoveEmptyEntries) equal to ".." — throwing `ArgumentException` with
    the SAME messages as `ContentSiteIndexStore.ValidateArtifactPath` ("Artifact path must be
    relative." / "Artifact path must not contain '..' path segments."). THEN compute existence by
    mirroring the live resolver EXACTLY:
    `File.Exists(Path.GetFullPath(Path.Combine(contentBase, row.ArtifactPath)))`. Do NOT insert a
    literal "content-kb" segment — `row.ArtifactPath` already supplies it. Classify published-orphan
    as `missing AND row.IsVisible AND !row.IsHidden`; hidden-orphan as
    `missing AND NOT (row.IsVisible && !row.IsHidden)`. Keep `Scan` free of Console/DB access — it
    takes already-loaded rows. Then create `ContentKbOrphanScannerTests` (xUnit, sealed, IDisposable
    temp-dir) covering every bullet in &lt;behavior&gt;: for the OK case write a real `.md` file at
    `Path.Combine(tempContentBase, "content-kb", slug, id + ".md")` and pass `tempContentBase` as
    contentBase, asserting the row is classified OK and NOT missing (this is the regression test that
    would FAIL under the old double-prefix logic); omit the file for the missing cases; assert
    `ArgumentException` for "/etc/passwd" and "content-kb/../secret.md". Name tests
    `Scan_Scenario_Expected` (e.g. `Scan_ExistingArtifactUnderContentBase_ClassifiedOk`,
    `Scan_VisibleRowMissingArtifact_CountsAsPublishedOrphan`, `Scan_RootedArtifactPath_Throws`). New
    files use CRLF to match sibling C# files; touch only the new files (changed-lines gate). Do NOT
    inline a full implementation in this plan — the &lt;behavior&gt; block, the cited resolver, and the
    cited guard are the contract.
  </action>
  <verify>
    <automated>dotnet test DeckFlow.Core.Tests --no-build --filter "FullyQualifiedName~ContentKbOrphan"</automated>
  </verify>
  <acceptance_criteria>
    - `ContentKbOrphanScanner.cs` defines a pure `Scan(rows, contentBase)` with no Console/DB dependency, computing existence as `Path.GetFullPath(Path.Combine(contentBase, row.ArtifactPath))` — identical to `ContentKbArtifactPathResolver.ResolveArtifactFullPath`, with no extra "content-kb" segment inserted.
    - A row whose `.md` exists at `<contentBase>/content-kb/{slug}/{id}.md` is classified OK (not missing) — covered by a regression test that would FAIL under the old double-prefix logic.
    - `dotnet test --filter "FullyQualifiedName~ContentKbOrphan"` passes; tests cover OK (content-base resolution), published-orphan, hidden-orphan, is_hidden-excludes-published, and rooted + ".." rejection.
    - Rooted or ".."-containing artifact_path throws ArgumentException before File.Exists is reached.
    - `dotnet build --no-incremental 2>&1 | tail -3` reports 0 errors / 0 new warnings.
  </acceptance_criteria>
  <done>Pure, tested orphan-scanner mirroring the live serving resolution (content base + content-kb/-prefixed ArtifactPath) with the path-traversal guard lives in DeckFlow.Core; xUnit green.</done>
</task>

<task type="auto">
  <name>Task 2: content-kb-check CLI command (handler + registration, with --artifact-root normalization)</name>
  <files>DeckFlow.CLI/ContentKbCommandRunners.cs, DeckFlow.CLI/Program.cs</files>
  <read_first>
    - DeckFlow.CLI/ContentKbCommandRunners.cs lines 319-350 (RunContentIndexExportAsync — the read-only handler pattern to mirror)
    - DeckFlow.CLI/ContentKbCliPaths.cs lines 21-31 (ResolveArtifactRoot returns the content-kb DIR, not its parent — must be normalized; ResolveDatabasePath)
    - DeckFlow.Web/Services/Content/ContentKbArtifactPathResolver.cs lines 36-56 (authoritative combine semantics the normalization must produce: contentBase is the parent that CONTAINS content-kb/)
    - DeckFlow.CLI/Program.cs lines 63-77, 207-213, 316-318 (content-source/content-index command registration + SetHandler pattern)
    - DeckFlow.Core/Content/ContentKbOrphanScanner.cs (Task 1 output — Scan(rows, contentBase) is the contract this calls)
  </read_first>
  <action>
    Add `public static async Task&lt;int&gt; RunContentKbCheckAsync(FileInfo? db, DirectoryInfo? artifactRoot)`
    to `ContentKbCommandRunners`. Resolve the db path via `ContentKbCliPaths.ResolveDatabasePath(db)`.
    Resolve the raw artifact root from `artifactRoot?.FullName ?? ContentKbCliPaths.ResolveArtifactRoot(db)`
    — NOTE this raw value is the `content-kb` DIRECTORY by default (and the operator may pass either
    convention). Then NORMALIZE it to a content base before calling the scanner: if the raw
    directory's final path segment is "content-kb" (case-insensitive, after trimming trailing
    directory separators — use `Path.GetFileName(Path.TrimEndingDirectorySeparator(rawRoot))`), use
    its PARENT (`Path.GetDirectoryName(...)` / `Directory.GetParent(...).FullName`) as `contentBase`;
    otherwise use the raw directory as `contentBase`. This makes BOTH a passed data-root parent AND a
    passed `content-kb` dir resolve to the identical artifact path, mirroring
    `ContentKbArtifactPathResolver` (where ContentBase is the parent of content-kb/). Construct
    `new ContentSiteIndexStore(dbPath)` (LOCAL sqlite only — never prod), call `GetAllRowsAsync()`,
    pass rows + `contentBase` into `ContentKbOrphanScanner.Scan(...)`, print one line per row
    (`OK     ` / `MISSING` + ArtifactPath + `(visible/not visible, approval=...)`) then a summary
    block (Total rows, Rows with artifact, Missing artifacts, Published (missing), Unpublished
    (missing)) matching the RESEARCH "Output" sample. Return `result.PublishedOrphanCount &gt; 0 ? 1 : 0`.
    Wrap the body in the same `try/catch (Exception ex when ex is not OperationCanceledException) {
    Console.Error.WriteLine(ex.Message); return 1; }` pattern — but note `ex.Message` here is a
    local-file/sqlite message, never a prod connection string (no prod access in this command).
    Then in `Program.cs` register `var contentKbCheckCommand = new Command("content-kb-check",
    "Check content_site_index rows against local artifact files and report orphans.");` with
    `--db` (`Option&lt;FileInfo?&gt;`, optional, defaults to artifacts/content-kb.db) and `--artifact-root`
    (`Option&lt;DirectoryInfo?&gt;`, optional; describe it as accepting EITHER the data-root parent of
    content-kb/ OR the content-kb directory — the handler normalizes both; defaults to the
    MTG_DATA_DIR/content-kb resolution); `AddOption` both,
    `rootCommand.AddCommand(contentKbCheckCommand)`, and a `SetHandler((FileInfo? db,
    DirectoryInfo? root) =&gt; { Environment.ExitCode =
    ContentKbCommandRunners.RunContentKbCheckAsync(db, root).GetAwaiter().GetResult(); }, dbOption,
    rootOption)` mirroring the `content-index-export` registration exactly. Touch only the changed
    lines (changed-lines format gate); new option/command declarations follow the surrounding style.
  </action>
  <verify>
    <automated>dotnet build DeckFlow.CLI --no-incremental 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `RunContentKbCheckAsync` exists in `ContentKbCommandRunners`, normalizes `--artifact-root` to a content base (final-segment "content-kb" → parent; else as-is), and calls `ContentKbOrphanScanner.Scan(rows, contentBase)`.
    - Passing `--artifact-root <data-root>` and passing `--artifact-root <data-root>/content-kb` both resolve to the same artifact paths (no `content-kb/content-kb/` double-prefix).
    - `content-kb-check` command is registered in `Program.cs` with `--db` and `--artifact-root` options and a SetHandler that sets `Environment.ExitCode` from the runner.
    - `dotnet build` reports 0 errors / 0 new warnings.
    - Running `dotnet run --project DeckFlow.CLI -- content-kb-check --db <local.db> --artifact-root <dir>` prints the per-row + summary output and exits 1 when a published orphan exists, 0 otherwise.
  </acceptance_criteria>
  <done>`content-kb-check` runs read-only against a local DB + artifact root, normalizes the root to a content base (both conventions accepted), reports orphans, and exit-codes on published-orphan presence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DB `artifact_path` column → filesystem | Untrusted relative path read from `content_site_index`, combined with a content base before File.Exists |
| CLI args (`--db`, `--artifact-root`) → local filesystem | Operator-supplied local paths; LOCAL only, never prod |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-65-01 | Tampering | `ContentKbOrphanScanner.Scan` path combine | mitigate | Reject `Path.IsPathRooted`, Windows-rooted form, and any `..` segment before `Path.Combine` — same guard as `ContentSiteIndexStore.ValidateArtifactPath:742`. Unit test `Scan_RootedArtifactPath_Throws` + `Scan_DotDotArtifactPath_Throws` lock it. HIGH if unguarded. |
| T-65-02-INFO | Information Disclosure | CLI catch block `Console.Error.WriteLine(ex.Message)` | accept | The command touches only a LOCAL sqlite db + local files (no prod connection string, no SSH host); the only exception surface is local-file/sqlite messages. No D-07-sensitive value can appear. Documented as accepted-low. |
| T-65-03-PRODWRITE | Tampering | Prod DB / prod `/data` | accept (non-threat) | This command introduces NO prod access — `ContentSiteIndexStore` is constructed against a LOCAL `--db` only; the research anti-pattern "no EnsureSchema against prod" is honored. Called out explicitly as a non-threat. |
</threat_model>

<verification>
- `dotnet build --no-incremental 2>&1 | tail -3` → 0 errors, 0 new warnings.
- `dotnet test DeckFlow.Core.Tests --no-build --filter "FullyQualifiedName~ContentKbOrphan"` → green (incl. the content-base-resolution regression test).
- `content-kb-check` against a local DB exits 1 with a published orphan, 0 without, and both `--artifact-root` conventions (data-root parent / content-kb dir) resolve identically.
</verification>

<success_criteria>
- SC3 repeatable check delivered: `content-kb-check` reports orphans with a published-orphan severity gate (exit 1/0), backed by a unit-tested pure scanner that mirrors the live serving artifact resolution (no double-prefix false positives).
</success_criteria>

<output>
Create `.planning/phases/65-prod-content-artifact-reconcile/65-03-SUMMARY.md` when done.
</output>
</content>
</invoke>
