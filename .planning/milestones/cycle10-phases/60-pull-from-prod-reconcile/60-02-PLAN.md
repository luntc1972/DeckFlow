---
phase: 60-pull-from-prod-reconcile
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - DeckFlow.Studio/Services/ISshArtifactDownloader.cs
  - DeckFlow.Studio/Services/SftpArtifactDownloader.cs
  - DeckFlow.Studio/Services/IProdContentReader.cs
  - DeckFlow.Studio/Services/ProdContentReader.cs
autonomous: true
requirements: [SYNC-01]
must_haves:
  truths:
    - "Studio can download a set of remote /data artifacts into a local staging root via SFTP"
    - "Per-file failures are returned (not thrown); a connect-level failure marks remaining files failed"
    - "A prod-DB ArtifactPath containing .. or a rooted path is rejected before any local file write (path-traversal guard, both remote and local sides)"
    - "No host/username/key/path value or ex.Message ever appears in a failure reason (sanitized literal only)"
    - "Studio can read all prod content_site_index rows via a DEDICATED read-only reader that runs ONLY a plain SELECT — no EnsureSchemaAsync, no CREATE/ALTER DDL, no mutator methods exist on the interface (R1)"
    - "The prod reader builds its prod Npgsql connection on-demand from Studio__ProdConnectionString (same convention as IProdStoreFactory) and never registers a live prod connection at DI startup"
  artifacts:
    - path: "DeckFlow.Studio/Services/ISshArtifactDownloader.cs"
      provides: "ISshArtifactDownloader + SshDownloadRequest + SshDownloadResult contracts"
      contains: "interface ISshArtifactDownloader"
    - path: "DeckFlow.Studio/Services/SftpArtifactDownloader.cs"
      provides: "SftpClient.DownloadFile-backed downloader, symmetric to SftpArtifactUploader"
      contains: "class SftpArtifactDownloader"
    - path: "DeckFlow.Studio/Services/IProdContentReader.cs"
      provides: "Read-only prod reader contract — ONE method ReadAllAsync(ct), NO write/DDL methods"
      contains: "interface IProdContentReader"
    - path: "DeckFlow.Studio/Services/ProdContentReader.cs"
      provides: "Dapper plain-SELECT prod reader; on-demand Npgsql; no EnsureSchemaAsync/DDL"
      contains: "class ProdContentReader"
  key_links:
    - from: "DeckFlow.Studio/Services/SftpArtifactDownloader.cs"
      to: "Renci.SshNet.SftpClient.DownloadFile"
      via: "one SftpClient per DownloadArtifactsAsync call, sequential downloads"
      pattern: "DownloadFile"
    - from: "DeckFlow.Studio/Services/SftpArtifactDownloader.cs"
      to: "Studio:Scp:* configuration"
      via: "reads Host/Port/Username/KeyFile/KeyPassphrase/RemoteArtifactRoot"
      pattern: "Studio:Scp:"
    - from: "DeckFlow.Studio/Services/ProdContentReader.cs"
      to: "content_site_index (prod Postgres)"
      via: "Dapper plain SELECT, on-demand RelationalDatabaseConnection (Postgres), no DDL"
      pattern: "SELECT"
---

<objective>
Add the two prod-read infrastructure pieces of the pull lane, both wave-1 Studio.Services with no
Core dependency:

1. The SCP DOWNLOAD half — a new `ISshArtifactDownloader` / `SftpArtifactDownloader` pair that is
   the exact mirror of the existing upload pair, using `SftpClient.DownloadFile` to pull artifacts
   from Render `/data` into a local staging root.
2. A DEDICATED read-only prod reader — `IProdContentReader` / `ProdContentReader` (R1) — whose ONLY
   capability is reading prod `content_site_index` rows via a plain Dapper `SELECT`. It does NOT use
   `ContentSiteIndexStore` (whose read path runs `EnsureSchemaAsync` → CREATE/ALTER DDL = a prod
   write); it has NO mutator methods at all, making the prod side STRUCTURALLY incapable of writing.

Zero new packages (SSH.NET 2025.1.0, Npgsql, Dapper all already referenced). No page, no DI wiring
(Plan 03 wires both into the page).

Purpose: Stage 1 of the pull page (Plan 03) needs to (a) fetch artifacts down without touching the
live `content-kb/` folder and (b) read all prod rows WITHOUT any risk of mutating prod. These two
services do exactly that, with the same path-traversal guard, on-demand-connection convention, and
sanitized errors as the existing write path (D-07).
Output: The downloader interface + impl AND the read-only prod reader interface + impl. No page,
no DI wiring (Plan 03 wires them).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/60-pull-from-prod-reconcile/60-CONTEXT.md
@.planning/phases/60-pull-from-prod-reconcile/60-RESEARCH.md
@./CLAUDE.md

# EXACT symmetric source for the downloader — mirror this file's structure, path guard, sanitized
# errors, and one-client-per-call idiom. The download is UploadFile flipped to DownloadFile.
@DeckFlow.Studio/Services/SftpArtifactUploader.cs
@DeckFlow.Studio/Services/ISshArtifactUploader.cs

# On-demand prod connection convention to mirror for the reader (build a Postgres
# RelationalDatabaseConnection from the raw conn string; NEVER register live at DI startup):
@DeckFlow.Studio/Services/IProdStoreFactory.cs

<interfaces>
<!-- Config keys (identical to the uploader) the downloader must read. -->
Studio:Scp:Host, Studio:Scp:Port (default 22), Studio:Scp:Username, Studio:Scp:KeyFile,
Studio:Scp:KeyPassphrase (optional), Studio:Scp:RemoteArtifactRoot.

<!-- SSH.NET 2025.1.0 download API (VERIFIED in research): -->
SftpClient.DownloadFile(string path, Stream output, Action<ulong>? downloadCallback = null)

<!-- Uploader's sanitized-error + traversal-guard pattern to copy verbatim: -->
const string SanitizedFailureReason — the ONLY string ever surfaced; never ex.Message.
TryBuildRemotePath(remoteRelativePath, out remotePath) — rejects rooted paths, '..' segments,
and any path that does not stay under RemoteArtifactRoot.

<!-- Prod-read building blocks (public in DeckFlow.Core.Storage) — use directly, no exploration: -->
PostgresConnectionStringNormalizer.Normalize(rawConnStr) — handles Render's postgresql:// URL form.
new RelationalDatabaseConnection(RelationalDatabaseProvider.Postgres, normalized) — descriptor.
RelationalDatabaseConnection.OpenConnectionAsync(ct) -> DbConnection (open Npgsql connection).
Dapper QueryAsync<T>(CommandDefinition) — used for all store reads.

<!-- Row type to materialize (DeckFlow.Core.Knowledge.ContentSiteIndexRow) and the EXACT column
     set + serialization the store uses, so the reader's SELECT matches GetAllRowsAsync 1:1: -->
SELECT id, source, title, video_url, artifact_path, published_utc, pushed_to_prod_utc,
       indexed_utc, archetype_tags, bracket_tags, card_category_tags, natural_key_type,
       natural_key_value, is_visible, is_hidden, is_evergreen, approval_status
  FROM content_site_index;
-- Tags are stored serialized; deserialize via ContentArtifactSpec.DeserializeTags(string?) (public).
-- natural_key_type is "youtube_channel" / "podcast_rss" (ContentSourceType.Youtube / .Podcast,
--   public consts); map to YoutubeVideoId vs RssGuid exactly as ContentSiteIndexStore does.
-- NO ORDER BY needed (the classifier keys by natural key); a plain SELECT suffices.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define ISshArtifactDownloader contract</name>
  <files>DeckFlow.Studio/Services/ISshArtifactDownloader.cs</files>
  <action>
    Create `DeckFlow.Studio/Services/ISshArtifactDownloader.cs` (namespace `DeckFlow.Studio.Services`), mirroring `ISshArtifactUploader.cs` exactly but in the download direction. Define:
    - `interface ISshArtifactDownloader` with `Task<IReadOnlyList<SshDownloadResult>> DownloadArtifactsAsync(IReadOnlyList<SshDownloadRequest> downloads, string localStagingRoot, IProgress<SshDownloadResult>? progress = null, CancellationToken cancellationToken = default)`.
    - `sealed record SshDownloadRequest(string RemoteRelativePath, string LocalRelativePath)` — RemoteRelativePath is the row's relative artifact path (e.g. `content-kb/{slug}/{id}.md`) resolved under RemoteArtifactRoot; LocalRelativePath is the same relative path combined with localStagingRoot to form the local destination.
    - `sealed record SshDownloadResult(string RemoteRelativePath, string LocalPath, bool Success, string? FailureReason)` — FailureReason is sanitized (never host/key/path/ex.Message), null on success.
    XML-doc every public member (note D-07: FailureReason carries no secrets; the relative-path inputs are traversal-validated by the implementation). LF endings, file-scoped namespace, `{ get; init; }` records (carve-out: do not collapse to get-only).
  </action>
  <verify>
    <automated>cd "$WORKTREE" && dotnet build DeckFlow.Studio/DeckFlow.Studio.csproj 2>&1 | grep -E "error|Build succeeded" | head -5</automated>
  </verify>
  <done>DeckFlow.Studio builds; the three download contracts exist symmetric to the upload trio.</done>
</task>

<task type="auto">
  <name>Task 2: Implement SftpArtifactDownloader (mirror of SftpArtifactUploader)</name>
  <files>DeckFlow.Studio/Services/SftpArtifactDownloader.cs</files>
  <action>
    Create `DeckFlow.Studio/Services/SftpArtifactDownloader.cs` (namespace `DeckFlow.Studio.Services`, `public sealed class SftpArtifactDownloader : ISshArtifactDownloader`) as a structural copy of `SftpArtifactUploader` with these direction changes:
    - Same ctor reading `Studio:Scp:*` keys (Host/Port/Username/KeyFile/KeyPassphrase/RemoteArtifactRoot) plus optional `ILogger<SftpArtifactDownloader>`. Never log any of these values (D-07).
    - `private const string SanitizedFailureReason = "SSH download failed — check SCP configuration and Render SSH access.";` — the ONLY failure string surfaced; never `ex.Message`.
    - `DownloadArtifactsAsync`: open ONE `SftpClient` per invocation (SftpClient is not thread-safe across concurrent calls — copy the uploader's one-client-per-call comment), connect, download sequentially, report each result via the progress sink, disconnect/dispose in finally. On a connect-level `SshException`/`IOException`, mark every not-yet-attempted request failed with the sanitized reason (copy the uploader's batch-abort loop). Re-throw `OperationCanceledException`.
    - `DownloadOne(client, request, localStagingRoot)`: (1) validate the REMOTE side with the uploader's `TryBuildRemotePath(request.RemoteRelativePath, out remotePath)` logic (reject rooted, reject `..`, confirm under RemoteArtifactRoot); (2) ALSO validate + build the LOCAL destination: reject rooted/`..` LocalRelativePath, `Path.Combine(localStagingRoot, normalized)`, then confirm the resolved full path (`Path.GetFullPath`) stays under `Path.GetFullPath(localStagingRoot)` (defense in depth — prod DB values are untrusted, Pitfall 5); (3) `Directory.CreateDirectory(Path.GetDirectoryName(localDest))`; (4) `using var fs = File.Create(localDest); client.DownloadFile(remotePath, fs);`; (5) return success result. On `SshException`/`IOException`, return a failed result with the sanitized reason only (never ex.Message). Copy the uploader's `TryBuildRemotePath`, `NormalizeRoot`, `CollapseSlashes` helpers (or factor as needed) — but DO NOT add remote directory creation (download reads the remote; it never creates remote dirs).
    Allman braces, 4-space indent, file-scoped namespace, LF endings, changed-lines-only formatting.
  </action>
  <verify>
    <automated>cd "$WORKTREE" && dotnet build DeckFlow.Studio/DeckFlow.Studio.csproj 2>&1 | grep -E "error|Build succeeded" | head -5</automated>
  </verify>
  <done>DeckFlow.Studio builds clean; downloader uses SftpClient.DownloadFile, one-client-per-call, guards remote AND local paths, surfaces only the sanitized literal, never ex.Message or secret values. No new NuGet packages added (csproj unchanged).</done>
</task>

<task type="auto">
  <name>Task 3: Define + implement IProdContentReader (read-only, no DDL) — R1</name>
  <files>DeckFlow.Studio/Services/IProdContentReader.cs, DeckFlow.Studio/Services/ProdContentReader.cs</files>
  <action>
    R1 — the prod read must NOT go through `IContentSiteIndexStore` / `ContentSiteIndexStore`, because its read methods (`GetAllRowsAsync` at ContentSiteIndexStore.cs:345 → `EnsureSchemaAsync` at :347) run `CREATE TABLE`/`ALTER TABLE` DDL = a prod write. Build a dedicated read-only reader instead.

    FIRST create `DeckFlow.Studio/Services/IProdContentReader.cs` (namespace `DeckFlow.Studio.Services`):
    - `public interface IProdContentReader` with EXACTLY ONE method: `Task<IReadOnlyList<ContentSiteIndexRow>> ReadAllAsync(string connectionString, CancellationToken cancellationToken = default)` (reference `DeckFlow.Core.Knowledge.ContentSiteIndexRow`). NO write/upsert/delete/set/ensure methods — the interface must be structurally read-only so no apply path can write prod even by mistake (this is the R2 structural guarantee). XML-doc that this performs a plain SELECT only and runs NO schema/DDL against prod.

    THEN create `DeckFlow.Studio/Services/ProdContentReader.cs` (namespace `DeckFlow.Studio.Services`, `public sealed class ProdContentReader : IProdContentReader`):
    - `ReadAllAsync`: build the prod connection on-demand exactly like `ProdStoreFactory.Create` — `var normalized = PostgresConnectionStringNormalizer.Normalize(connectionString); var conn = new RelationalDatabaseConnection(RelationalDatabaseProvider.Postgres, normalized);` then `await using var connection = await conn.OpenConnectionAsync(cancellationToken);`. Run a SINGLE plain Dapper `QueryAsync<TRowData>(new CommandDefinition("SELECT id, source, title, video_url, artifact_path, published_utc, pushed_to_prod_utc, indexed_utc, archetype_tags, bracket_tags, card_category_tags, natural_key_type, natural_key_value, is_visible, is_hidden, is_evergreen, approval_status FROM content_site_index;", cancellationToken: cancellationToken))`. NO `EnsureSchemaAsync`, NO CREATE/ALTER, NO information_schema introspection, NO write of any kind — just the SELECT.
    - Define a private nested DTO matching the SELECT columns (Dapper materialization target), mirroring the store's private `ContentSiteIndexRowData` shape (Id/Source/Title/VideoUrl/ArtifactPath/PublishedUtc?/PushedToProdUtc?/IndexedUtc/ArchetypeTags/BracketTags/CardCategoryTags/NaturalKeyType/NaturalKeyValue/IsVisible/IsHidden/IsEvergreen/ApprovalStatus). Map each DTO to `ContentSiteIndexRow`: deserialize the three tag columns via `ContentArtifactSpec.DeserializeTags(string?)` (public), and split natural_key_type into YoutubeVideoId vs RssGuid using `ContentSourceType.Youtube` / `ContentSourceType.Podcast` (public consts) — identical to `ContentSiteIndexStore.ToContentSiteIndexRow`. Return `IReadOnlyList<ContentSiteIndexRow>`.
    - F-51-PG-01 note: this is a plain SELECT with NO WHERE clause on any timestamp column, so the timestamptz-vs-text comparison bug cannot recur here (the 42883 case was a parameterized WHERE on a timestamp). Dapper materializes `indexed_utc`/`published_utc`/`pushed_to_prod_utc` into `DateTimeOffset?`/`DateTimeOffset` the same way `GetAllRowsAsync` already does on prod.
    - Sanitized errors (D-07): if the SELECT fails (e.g. table absent, connect failure), do NOT attempt to create/alter anything; let the page (Plan 03) catch and surface a sanitized literal. Do NOT log or surface the connection string / ex.Message in this class.
    Allman braces, 4-space indent, file-scoped namespace, LF endings, `{ get; init; }` on the DTO (carve-out: do not collapse), changed-lines-only formatting. No new packages.
  </action>
  <verify>
    <automated>cd "$WORKTREE" && dotnet build DeckFlow.Studio/DeckFlow.Studio.csproj 2>&1 | grep -E "error|Build succeeded" | head -5 && grep -c "EnsureSchemaAsync\|CREATE TABLE\|ALTER TABLE\|information_schema" DeckFlow.Studio/Services/ProdContentReader.cs</automated>
  </verify>
  <done>DeckFlow.Studio builds clean; IProdContentReader has exactly one read method (no mutators); ProdContentReader runs ONLY a plain SELECT (the grep for EnsureSchemaAsync/CREATE/ALTER/information_schema returns 0), builds its Npgsql connection on-demand from the conn string, materializes ContentSiteIndexRow matching the store's column set, never logs/surfaces secrets. csproj unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Render /data (remote) -> Studio local FS | SFTP transfers untrusted bytes + remote/relative paths into the local staging dir |
| prod DB ArtifactPath -> local file path | ArtifactPath read from the prod row is untrusted; used to build both the remote source and local dest |
| SSH exception -> failure reason / log | Exception text may carry host/username/remote path/connection detail |
| Studio -> prod Postgres (read) | Prod read must be structurally read-only: SELECT only, no DDL, no mutators (R1) |
| prod conn string -> reader / log | Connection string is a secret; never logged or surfaced in any error |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-60-04 | Tampering / Elevation | SftpArtifactDownloader.DownloadOne (local path) | mitigate | Reject rooted / `..` LocalRelativePath; Path.Combine then Path.GetFullPath and confirm resolved path stays strictly under Path.GetFullPath(localStagingRoot) before File.Create — prevents a malicious prod ArtifactPath escaping the staging dir |
| T-60-05 | Tampering | SftpArtifactDownloader.DownloadOne (remote path) | mitigate | Reuse uploader TryBuildRemotePath: reject rooted/`..`, confirm under RemoteArtifactRoot before DownloadFile |
| T-60-06 | Information Disclosure | catch blocks + ctor + logger | mitigate | Only SanitizedFailureReason ever surfaced; never ex.Message; Host/Username/KeyFile/connection values never logged (D-07, copied verbatim from uploader) |
| T-60-07 | Denial of Service | DownloadArtifactsAsync batch | mitigate | One client per call, sequential, OperationCanceledException honored; per-file failure isolated (not thrown); connect-level failure fails the batch cleanly with sanitized reasons |
| T-60-15 | Tampering | ProdContentReader (R1 — structural read-only) | mitigate | Reader interface exposes ONE read method and zero mutators; impl runs a single plain SELECT with NO EnsureSchemaAsync/CREATE/ALTER/information_schema — prod side is structurally incapable of writing; verified by the build-time grep==0 gate |
| T-60-16 | Information Disclosure | ProdContentReader (prod conn string + Npgsql ex) | mitigate | Connection string is never logged; SELECT failures are not caught-and-leaked here (no ex.Message), the page surfaces a sanitized literal (D-07) |
| T-60-SC | Tampering | NuGet installs | mitigate | NO new packages — SSH.NET 2025.1.0, Npgsql 10.0.0, Dapper all already referenced (RESEARCH Package Legitimacy Audit, SSH.NET 288.7M downloads VERIFIED); csproj must be unchanged |
</threat_model>

<verification>
- `dotnet build DeckFlow.Studio/DeckFlow.Studio.csproj` clean.
- `git diff --name-only` shows NO change to `DeckFlow.Studio/DeckFlow.Studio.csproj` (no package added).
- `grep -c "ex.Message" DeckFlow.Studio/Services/SftpArtifactDownloader.cs` returns 0.
- `grep -c "DownloadFile" DeckFlow.Studio/Services/SftpArtifactDownloader.cs` >= 1.
- Local-dest traversal guard present (`Path.GetFullPath` containment check on localStagingRoot).
- `grep -c "EnsureSchemaAsync\|CREATE TABLE\|ALTER TABLE\|information_schema" DeckFlow.Studio/Services/ProdContentReader.cs` returns 0 (R1: no DDL).
- `IProdContentReader` exposes only `ReadAllAsync` — `grep -cE "Upsert|Delete|Set[A-Z]|EnsureSchema|Stamp|Clear" DeckFlow.Studio/Services/IProdContentReader.cs` returns 0 (no mutators).
- `grep -c "SELECT" DeckFlow.Studio/Services/ProdContentReader.cs` >= 1.
</verification>

<success_criteria>
- A new downloader pair exists, structurally symmetric to the uploader, using `SftpClient.DownloadFile`.
- Both remote and local paths are traversal-guarded before any file write.
- Per-file and connect-level failures return sanitized results; no secret or ex.Message leak.
- A dedicated read-only `IProdContentReader`/`ProdContentReader` exists (R1): one read method, plain SELECT, NO DDL, NO mutators — the prod side is structurally write-free.
- Zero new packages; csproj untouched.
</success_criteria>

<output>
Create `.planning/phases/60-pull-from-prod-reconcile/60-02-SUMMARY.md` when done.
</output>
