---
phase: 21.2-pluggable-llm-distill-cli-backends-inserted
plan: 02
subsystem: cli
tags: [content-kb, llm, claude, spend-ledger, docs]

requires:
  - phase: 21.2-01
    provides: LLM distillation provider factory and CLI-backed claude service
provides:
  - Provider factory wired into the Content KB distill runner
  - Subscription-provider ledger cap and pricing bypass for claude
  - Cross-platform claude CLI backend operator documentation
affects: [content-kb, run-distill, llm-ledger, ops-docs]

tech-stack:
  added: []
  patterns: [environment-selected provider factory, subscription spend bypass, JSON-array CLI override docs]

key-files:
  created:
    - docs/ops/content-kb-llm-cli-backends.md
  modified:
    - DeckFlow.CLI/CommandRunners.cs
    - DeckFlow.Core.Tests/RunDistillAsyncTests.cs
    - README.md

key-decisions:
  - "Treat any non-empty provider other than openai as subscription-backed for distill ledger/pricing bypass; unsupported providers still fail in the factory."
  - "Keep RecordCallAsync for subscription runs with cost 0 so LLM call counts remain auditable."
  - "Document DECKFLOW_LLM_CLI_COMMAND as a JSON array so empty args and paths with spaces survive verbatim."

patterns-established:
  - "RunDistillAsync accepts optional isSubscriptionProvider=false to preserve the openai default test seam."
  - "Cap gates and pricing math are both guarded so subscription providers do not call OpenAI cost projectors."

requirements-completed: [KB-10, KB-11]

duration: not tracked
completed: 2026-06-01
---

# Phase 21.2 Plan 02 Summary

**Content KB distill now selects the Plan 01 provider factory and documents the claude CLI subscription backend without running the human UAT gate.**

## Accomplishments

- Wired `RunDistillAsync` to `LlmDistillationProviderFactory.Resolve`, with `openai` remaining the default path.
- Threaded `isSubscriptionProvider` through the internal distill path and guarded all LLM cap gates plus projected/exact pricing calls.
- Subscription runs still write LLM ledger rows and run records, but with spend 0.
- Added regression tests covering subscription cap bypass, dry-run `$0, subscription` output, and openai-default cap checks.
- Added the claude CLI ops guide covering WSL, Windows, Windows `dotnet.exe` from WSL, JSON-array overrides, timeout, auth, ledger behavior, and log/security hygiene.
- Added the README pointer to `DECKFLOW_LLM_PROVIDER` and the new ops doc.

## Task Commits

1. **Task 1: Wire provider factory and subscription ledger/pricing bypass** - `36e6bcf` (`feat(21.2-02): wire llm distill provider factory`)
2. **Task 2: Document claude CLI backend operation** - `4bf213e` (`docs(21.2-02): document claude llm cli backend`)

## Verification

- Red step: focused `RunDistillAsyncTests` initially failed to compile because the new `isSubscriptionProvider` seam did not exist yet.
- `"/mnt/c/Program Files/dotnet/dotnet.exe" test DeckFlow.Core.Tests/DeckFlow.Core.Tests.csproj --filter "FullyQualifiedName~RunDistillAsyncTests"`: Passed, 12/12.
- `"/mnt/c/Program Files/dotnet/dotnet.exe" build DeckFlow.sln -c Debug`: Build succeeded, 0 warnings, 0 errors.
- Acceptance greps confirmed one factory resolve, no direct `new LlmDistillationService(` in `CommandRunners`, four `!isSubscriptionProvider &&` cap guards, and required subscription/pricing ternaries.
- Documentation greps confirmed `DECKFLOW_LLM_PROVIDER`, `DECKFLOW_LLM_CLI_COMMAND`, JSON-array `["wsl.exe",...]` examples, `{instruction}`, `allowedTools`, `dotnet.exe`, README pointer, and no codex invocation examples.

## Task 3 - DEFERRED (human UAT)

Task 3 was not attempted. The live claude-backed 10-video distill UAT remains for the operator to run and judge.

The operator still needs to verify:

- claude login and dry-run `$0, subscription` output
- live artifact generation and slim-index rows
- spend 0 run record and ledger SQL checks
- E5/E6 qualitative artifact review

Do not flip Phase 21.1/21.2 roadmap or requirements traceability from this summary alone; that is blocked on the human UAT approval.

## Deviations from Plan

None for Tasks 1 and 2. Task 3 was explicitly excluded by the executor scope.

## User Setup Required

Follow `docs/ops/content-kb-llm-cli-backends.md` before running the deferred human UAT, especially for Windows `dotnet.exe` launched from WSL.

---
*Phase: 21.2-pluggable-llm-distill-cli-backends-inserted*
*Completed: 2026-06-01*
