---
phase: 103-simulation-engine-guided-cut-rounds
plan: 05
type: execute
wave: 3
depends_on: ["103-01", "103-02", "103-03"]
files_modified:
  - DeckFlow.Core/Manabase/ManabaseAnalyzer.cs
  - DeckFlow.Core.Tests/ManabaseAnalyzerTrialsOverrideTests.cs
  - DeckFlow.Web/Services/CutLab/CutLabSimulationService.cs
  - DeckFlow.Web/Services/CutLab/CutLabBaselineSnapshot.cs
  - DeckFlow.Web/Extensions/CutLabServiceCollectionExtensions.cs
  - DeckFlow.Web.Tests/CutLabSimulationServiceTests.cs
  - DeckFlow.Web.Tests/CutLabBaselineSnapshotTests.cs
autonomous: true
requirements: [SIM-01, SIM-02, CUT-02]
must_haves:
  truths:
    - "HIGH-3: ManabaseAnalyzer.Analyze gains an OPTIONAL trials override (int? trialsOverride = null → existing CastabilitySimulator.DefaultTrials) threaded to its Monte-Carlo passes. This is PURE PARAMETERIZATION of the existing constant — zero new simulation math — so SIM-01's 'no new math' holds; with no override, output is byte-identical to today."
    - "HIGH-3: the in-loop trial count consumed by the sim service is the value recorded in 103-01-SUMMARY (Wave-0 spike); if the default already meets ~1s the in-loop path uses the default and the override remains available for future tuning."
    - "SIM-01: metrics recalculate by REUSING the existing resolve→classify→analyze pipeline (ScryfallCardFactMapper.ToCardFacts → ManabaseClassifier.Classify → ManabaseAnalyzer.Analyze) — every projected number traces to a ManabaseReport field."
    - "SIM-01 (7 families): commander-on-time, keepable-hand, mana/color reliability, early-interaction, plan-presence project from named ManabaseReport fields; per-goal category-by-turn uses CedhMulliganCalibration FIXED caps (Open Q1 / D-17); flood/screw/curve is THREE separate lines (Open Q2)."
    - "D-07: proposal deltas are computed against the CURRENT working list only (baseline drift lives in the SIM-02 snapshot, not here)."
    - "D-08: below the noise floor (±1.5pp / ±1 card), a delta renders direction=None/IsMeaningful=false; identical working lists produce byte-identical deltas."
    - "D-10: a proposal's deltas are cached by (working-list hash, card) so re-presenting the same card never re-simulates."
    - "CUT-02: projection never emits verdict language; direction is carried in the CutLabMetricDirection field, copy neutrality is enforced downstream."
    - "SIM-02/D-12: CutLabBaselineSnapshot computes the compact numeric snapshot once from the same pipeline."
  artifacts:
    - path: "DeckFlow.Core/Manabase/ManabaseAnalyzer.cs"
      provides: "Analyze(..., int? trialsOverride = null) — parameterized trial count (HIGH-3)"
      contains: "trialsOverride"
    - path: "DeckFlow.Web/Services/CutLab/CutLabSimulationService.cs"
      provides: "working-list metric projection + proposal-delta computation with noise floor + delta cache"
      contains: "ManabaseAnalyzer.Analyze"
    - path: "DeckFlow.Web/Services/CutLab/CutLabBaselineSnapshot.cs"
      provides: "compact numeric baseline snapshot builder (D-12)"
      contains: "CutLabMetricSnapshot"
  key_links:
    - from: "DeckFlow.Web/Services/CutLab/CutLabSimulationService.cs"
      to: "DeckFlow.Core/Manabase/ManabaseAnalyzer.cs"
      via: "ManabaseAnalyzer.Analyze(deck, mode, ..., trialsOverride: inLoopTrials) — mandated reuse + HIGH-3 override"
      pattern: "ManabaseAnalyzer\\.Analyze"
    - from: "DeckFlow.Web/Services/CutLab/CutLabSimulationService.cs"
      to: "DeckFlow.Web/Services/CutLab/CutLabDeltaCache.cs"
      via: "TryGet/Set per (pool hash, card) before simulating (D-10)"
      pattern: "CutLabDeltaCache"
---

<objective>
Build the SIM-01 heart: a service that projects the existing `ManabaseReport` into the 7 delta-display metric families, computes per-proposal deltas against the current working list (D-07) with the noise floor (D-08), caches them (D-10), and a companion builder that produces the compact D-12 baseline snapshot. First, close the HIGH-3 gap by adding an OPTIONAL trials override to `ManabaseAnalyzer.Analyze` (pure parameterization of the existing `DefaultTrials`, zero new math) so the in-loop delta path can honor the D-11 latency budget without touching the baseline/round-summary fidelity. This is the ONLY place simulation runs — and it runs the existing engine verbatim (SIM-01).

Purpose: Every "what changes if I cut this?" number in the UI comes from here. It must be deterministic, cached, copy-neutral (CUT-02), and fast enough for the loop.
Output: parameterized `Analyze`, `CutLabSimulationService`, `CutLabBaselineSnapshot`, DI wiring, Core byte-identical + Web determinism/delegation tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/cut-lab/phases/103-simulation-engine-guided-cut-rounds/103-PATTERNS.md
@.planning/workstreams/cut-lab/phases/103-simulation-engine-guided-cut-rounds/103-RESEARCH.md
@.planning/workstreams/cut-lab/phases/103-simulation-engine-guided-cut-rounds/103-01-SUMMARY.md

<interfaces>
ManabaseAnalyzer.Analyze full overload to parameterize (DeckFlow.Core/Manabase/ManabaseAnalyzer.cs:166-310):
- public static ManabaseReport Analyze(ManabaseDeck deck, ManabaseMode mode, CommanderImportance importance = ..., IReadOnlyDictionary<string,string>? costOverrides = null, bool useManaQuantity = false, ... bool useHealthBandHeadlineFloor = false, CedhLandContext cedhContext = default)
- Monte-Carlo trial-count call sites currently hard-coded to CastabilitySimulator.DefaultTrials: SimulatePlanPresence (~263), ComputeTapAnalysis (~287), ComputeMulliganEvaluation (~293), ComputeInteractionLens (~305), and BuildCastability's internal CastabilitySimulator.Simulate (~618). SourceSearchTrials + the full-trial boundary confirms (~1141-1151) are internal correctness guards — leave them at their current values (reduced-trial noise can off-by-one the crossing).

Pipeline to reuse verbatim (DeckFlow.Web/Services/Manabase/ManabaseAnalysisService.cs:466-477, 783-796):
- IReadOnlyList<CardFact> facts = ScryfallCardFactMapper.ToCardFacts(deckEntries);
- ManabaseDeck deck = ManabaseClassifier.Classify(facts, isSingleton: true, rampCreditV2, landRampSim, payLifeUntapped, checkLandUntapped, restrictedLands);
- ManabaseReport report = ManabaseAnalyzer.Analyze(deck, mode, importance, costOverrides, useManaQuantity, colorAwareMulligan, gateRampOnCastable:true, ritualBurst, ritualLandCredit, scryCredit, colorlessSnow, interactionLens, keepShapes, useHealthBandCastability, useHealthBandHeadlineFloor, cedhContext);
- Mode resolution: CutLabRoleAssigner.ResolveMode(playExperience) → ManabaseMode.

7-family → ManabaseReport field mapping (RESEARCH Don't Hand-Roll table):
- CommanderOnTime  ← CardCastability.EarlyCastPercents (commander row) / CastPercent at OnCurveTurn
- KeepableHand     ← ManabaseReport.MulliganEvaluation.KeepableHandPercent
- ManaColorReliability ← ManabaseReport.ColorFindings (AverageCastPercent; use worst/composite ColorFindings[0])
- EarlyInteraction ← ManabaseReport.InteractionLens.QualifyingCount / OnTargetCount (cEDH-only; when null → not-applicable, not zero)
- PlanPresence     ← MulliganEvaluation.PlanPresence.PlanPresencePercent
- CategoryByTurn   ← fixed caps: CedhMulliganCalibration.TurnCapExplosive(3)/TurnCapEngine(2)/RepresentativeLineTurnCap(4) + CardCastability.EarlyCastPercents by turn (FIXED defaults, Open Q1)
- Flood / Screw / Curve (three lines, Open Q2): Flood ← ManabaseReport.LandDelta (positive/large); Screw ← MulliganEvaluation.MulliganTo5Percent (+ LandDelta negative); Curve ← Phase-102 CutLabFindingKind.CurveCongestion presence/severity for the working list.

From Plan 103-01: CutLabMetricSnapshot, CutLabMetricValue, CutLabMetricDelta, CutLabProposalDeltas, CutLabMetricDirection, CutLabNoiseFloor.
From Plan 103-03: CutLabResolvedCardCache (TryGet resolved ScryfallCardData by pool key), CutLabDeltaCache (TryGet/Set by pool key + card).
In-loop trial count: use the decision recorded in 103-01-SUMMARY (default trials unless the spike required a reduction — pass via the HIGH-3 override).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Parameterize ManabaseAnalyzer.Analyze with an optional trials override (HIGH-3)</name>
  <read_first>
    - DeckFlow.Core/Manabase/ManabaseAnalyzer.cs (the full Analyze overload 166-310 + the five DefaultTrials call sites; BuildCastability at ~600-620; the boundary-confirm guards ~1141-1151 that must stay full-trial)
    - DeckFlow.Core/Manabase/CastabilitySimulator.cs (DefaultTrials constant; the `trials` int parameter already accepted by Simulate/SimulatePlanPresence — no simulator signature change needed)
    - DeckFlow.Core.Tests/ (an existing ManabaseAnalyzer test for the fixture/deck-construction convention + xUnit style)
    - .planning/workstreams/cut-lab/phases/103-simulation-engine-guided-cut-rounds/103-RESEARCH.md (Pitfall 2: DefaultTrials/SourceSearchTrials cost; the boundary-confirm guards must not be reduced)
  </read_first>
  <behavior>
    - Test 1 (byte-identical at default): `Analyze(deck, mode)` and `Analyze(deck, mode, trialsOverride: CastabilitySimulator.DefaultTrials)` produce byte-identical ManabaseReport numeric values (KeepableHandPercent, LandDelta, PlanPresencePercent, ColorFindings[].AverageCastPercent, Castability rows) — proving pure parameterization, no behavior change with no/default override.
    - Test 2 (override threads through): `Analyze(deck, mode, trialsOverride: 2000)` completes and returns a well-formed report (the reduced count flows into the Monte-Carlo passes without exception) — this documents the override is wired, not that a specific number is "correct".
  </behavior>
  <action>
    In `DeckFlow.Core/Manabase/ManabaseAnalyzer.cs`, add an OPTIONAL trailing parameter `int? trialsOverride = null` to the full `Analyze(...)` overload (line 166). At the top, resolve `int trials = trialsOverride ?? CastabilitySimulator.DefaultTrials;` with a `// Why:` comment (HIGH-3: pure parameterization — Cut Lab's in-loop delta path may pass a reduced trial count for the D-11 latency budget; every existing caller passes null and gets byte-identical output). Replace the hard-coded `CastabilitySimulator.DefaultTrials` argument with `trials` at the four deck-level Monte-Carlo passes (SimulatePlanPresence, ComputeTapAnalysis, ComputeMulliganEvaluation, ComputeInteractionLens) and thread `trials` into `BuildCastability` (add an `int trials` parameter defaulting to `CastabilitySimulator.DefaultTrials` and pass it to its internal `CastabilitySimulator.Simulate` call). LEAVE `SourceSearchTrials` and the full-trial boundary-confirm guards (~1141-1151) unchanged — they are correctness guards, not the headline cost driver, and reduced-trial noise can off-by-one the color-source crossing (RESEARCH Pitfall 2). Do NOT add or change any probability/statistics math — only substitute the trial count. Create `DeckFlow.Core.Tests/ManabaseAnalyzerTrialsOverrideTests.cs` (xUnit, `public sealed class`, `[Fact]`s) with the two behaviors. Preserve each touched file's existing line endings exactly (per-file LF/CRLF); change only the lines whose content changes.
  </action>
  <verify>
    <automated>"/mnt/c/Program Files/dotnet/dotnet.exe" test DeckFlow.Core.Tests --filter "FullyQualifiedName~ManabaseAnalyzerTrialsOverride"</automated>
  </verify>
  <acceptance_criteria>
    - `Analyze` exposes `int? trialsOverride = null` (grep) and resolves `trials = trialsOverride ?? CastabilitySimulator.DefaultTrials`.
    - Byte-identical-at-default test passes (no behavior change with null/default override).
    - The boundary-confirm guards (~1141-1151) and `SourceSearchTrials` are unchanged (git diff shows no edit there).
    - No new probability/statistics code is introduced (parameterization only).
    - Build clean, no new warnings; DeckFlow.Core.Tests green.
  </acceptance_criteria>
  <done>The existing engine's trial count is a parameter; default callers are byte-identical, and Cut Lab can pass a reduced in-loop count.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: CutLabSimulationService — 7-family projection + proposal deltas (cached, noise-floored)</name>
  <read_first>
    - DeckFlow.Web/Services/Manabase/ManabaseAnalysisService.cs (the canonical resolve→classify→analyze pipeline + the exact Analyze flag set to mirror; TagPlanRolesAsync for plan-presence tagging)
    - DeckFlow.Core/Manabase/ManabaseModels.cs (ManabaseReport, ColorSourceFinding, ManabaseMulliganEvaluation, ManabasePlanPresence, ManabaseInteractionLens, CardCastability field shapes)
    - DeckFlow.Core/Manabase/CedhMulliganCalibration.cs (TurnCapExplosive/TurnCapEngine/RepresentativeLineTurnCap)
    - DeckFlow.Web/Services/CutLab/CutLabRoleAssigner.cs (ResolveMode)
    - DeckFlow.Web/Models/CutLab/CutLabMetrics.cs (target contract types + CutLabNoiseFloor)
    - DeckFlow.Web/Services/CutLab/CutLabDeltaCache.cs / CutLabResolvedCardCache.cs (cache APIs from Plan 103-03)
    - DeckFlow.Web/Services/CutLab/CutLabStructuralFindings.cs (CurveCongestion for the Curve line)
    - DeckFlow.Web.Tests/CutLabPageServiceTests.cs (Fake* wiring conventions)
    - .planning/workstreams/cut-lab/phases/103-simulation-engine-guided-cut-rounds/103-01-SUMMARY.md (trial-count decision → pass as trialsOverride)
  </read_first>
  <behavior>
    - Test 1 (SIM-01 delegation): the service's projected KeepableHand value equals the raw `ManabaseReport.MulliganEvaluation.KeepableHandPercent` produced by directly calling the engine on the same working list — proving projection, not reimplementation.
    - Test 2 (7 families): a projected `CutLabMetricSnapshot` for a working list contains one value per family, with Flood/Screw/Curve as three distinct `CutLabMetricKind`s.
    - Test 3 (EarlyInteraction cEDH-gate): for a Casual-mode list where `InteractionLens` is null, the EarlyInteraction line is marked not-applicable (not a fabricated 0).
    - Test 4 (D-07 + D-08 noise floor): computing a proposal delta where the after-value differs by < 1.5pp yields `Direction=None`, `IsMeaningful=false`; a > 1.5pp move yields Up/Down + IsMeaningful=true; deltas are vs the current working list, not the baseline.
    - Test 5 (D-10 cache): a second delta request for the same (pool hash, card) returns the cached `CutLabProposalDeltas` without a second Analyze call (assert via a call-counting fake or by observing no recomputation).
    - Test 6 (D-08 determinism at service level): computing the working-list snapshot twice yields byte-identical values.
    - Test 7 (CUT-02): no projected value or label contains verdict words ("worse"/"bad"/"better"); direction lives only in the enum field.
  </behavior>
  <action>
    Create `DeckFlow.Web/Services/CutLab/CutLabSimulationService.cs` (namespace `DeckFlow.Web.Services.CutLab`, `public sealed class` + `public interface ICutLabSimulationService`, constructor-injected `CutLabResolvedCardCache`, `CutLabDeltaCache`, `IScryfallCardResolver` (for cache-miss resolution — via the existing pipeline, never a raw HttpClient), and `ILogger<CutLabSimulationService>`). Provide:
    - `CutLabMetricSnapshot BuildSnapshot(workingList, intent/mode inputs, ...)`: resolve `CardFact`s (from `CutLabResolvedCardCache`, else resolve-and-cache via the existing resolver pipeline), run `ManabaseClassifier.Classify(facts, isSingleton:true, ...)` (+ plan-role tagging as ManabaseAnalysisService does), `ManabaseAnalyzer.Analyze(deck, mode, ..., trialsOverride: inLoopTrials)` where `inLoopTrials` is the value recorded in 103-01-SUMMARY (pass `null` if the spike said "no reduction"), then project the report into `CutLabMetricValue`s per the family→field mapping above.
    - `CutLabProposalDeltas ComputeProposalDeltas(currentWorkingList, candidateCardName, ...)`: check `CutLabDeltaCache.TryGet(poolKey, card)` first (D-10); on miss, BuildSnapshot for the current list and for the list-minus-candidate, diff each family (after − before), apply `CutLabNoiseFloor` (percent metrics use PercentPoints, card-count metrics use Cards) to set `Direction`/`IsMeaningful`, count changed FAMILIES, cache and return. Deltas are always vs the CURRENT working list (D-07).
    - A private `ComputePoolKey(...)` reusing the Plan 103-03 key helper.
    Keep ALL metric math as reads of `ManabaseReport` fields — never recompute castability/mulligan/color math (SIM-01 anti-pattern). Emit NO verdict copy; labels are neutral metric names. Record the exact `inLoopTrials` value used (from 103-01) in a `// Why:` comment.
  </action>
  <verify>
    <automated>"/mnt/c/Program Files/dotnet/dotnet.exe" test DeckFlow.Web.Tests --filter "FullyQualifiedName~CutLabSimulationService"</automated>
  </verify>
  <acceptance_criteria>
    - All seven behaviors pass.
    - The service calls `ManabaseAnalyzer.Analyze(...)` (grep) with the trial override wired from 103-01, and contains NO reimplemented castability/mulligan loop (grep confirms no `Random`/`Simulate` reimplementation in the file).
    - Delta-cache TryGet is consulted before any Analyze in `ComputeProposalDeltas` (source-order assertion; call-counting test proves no double-sim).
    - Flood/Screw/Curve appear as three separate `CutLabMetricDelta` entries.
    - No verdict word appears in any produced label (grep the file's label strings).
  </acceptance_criteria>
  <done>Working-list metrics + proposal deltas are computed by reusing the existing engine (with the HIGH-3 in-loop trial count), cached, noise-floored, and copy-neutral.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: CutLabBaselineSnapshot builder + service-level determinism guard + DI</name>
  <read_first>
    - DeckFlow.Web/Services/CutLab/CutLabFloorDefaults.cs (resolve-once-at-intake, compact-record precedent)
    - DeckFlow.Web/Services/CutLab/CutLabSimulationService.cs (BuildSnapshot from Task 2 — reuse it)
    - DeckFlow.Web/Models/CutLab/CutLabMetrics.cs (CutLabMetricSnapshot)
    - DeckFlow.Web/Extensions/CutLabServiceCollectionExtensions.cs (extend the Plan 103-03 extension)
    - DeckFlow.Web.Tests/CutLabFloorDefaultsTests.cs (test conventions)
  </read_first>
  <behavior>
    - Test 1 (D-12): `CutLabBaselineSnapshot.Build(originalPool, ...)` returns a `CutLabMetricSnapshot` with all 7 families, computed via the same simulation pipeline as Task 2 (baseline uses FULL-fidelity default trials — no override — since it runs once, not in-loop).
    - Test 2 (SIM-02 round-trip): the produced snapshot serializes into `CutLabState.BaselineSnapshot` and back (via CutLabStateSerializer) unchanged and under the 256KB cap.
    - Test 3 (determinism guard, D-08 service-level): building the baseline for the same original pool twice yields byte-identical snapshots.
  </behavior>
  <action>
    Create `DeckFlow.Web/Services/CutLab/CutLabBaselineSnapshot.cs` (namespace `DeckFlow.Web.Services.CutLab`). It computes the D-12 baseline once by delegating to `ICutLabSimulationService.BuildSnapshot` against the ORIGINAL locked pool at FULL default trials (pass no override) and returns a `CutLabMetricSnapshot` (compact numeric only). Prefer a small service (constructor-injected `ICutLabSimulationService`) so Plan 103-06 can call it at intake; register it in `AddDeckFlowCutLabServices` alongside `AddScoped<ICutLabSimulationService, CutLabSimulationService>()` (Scoped for the sim service since it depends on the Scoped resolver; caches stay Singleton from Plan 103-03). Extend `CutLabServiceCollectionExtensions.AddDeckFlowCutLabServices` to register `ICutLabSimulationService` and the baseline builder. Add the service-level determinism test (Test 3) fulfilling the VALIDATION.md Wave-0 determinism-guard requirement at the service tier.
  </action>
  <verify>
    <automated>"/mnt/c/Program Files/dotnet/dotnet.exe" test DeckFlow.Web.Tests --filter "FullyQualifiedName~CutLabBaselineSnapshot"</automated>
  </verify>
  <acceptance_criteria>
    - Baseline builder produces a 7-family snapshot and round-trips through CutLabStateSerializer under the cap.
    - Service-level determinism test green (same pool twice → identical snapshot).
    - `AddDeckFlowCutLabServices` now registers the simulation service + baseline builder (grep the extension).
    - Build clean, no new warnings.
  </acceptance_criteria>
  <done>The D-12 baseline is computed once via the reused engine at full fidelity, persists compactly, and is deterministic.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| working list → simulation | Card list (from client-influenced state) drives resolution + simulation; a hostile list must not trigger unbounded work or a raw external fetch. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-103-05-01 | Denial of Service | Full-trial Analyze per decision blowing the D-11 budget / CPU | mitigate | Delta cache short-circuits repeat proposals (D-10); in-loop trial count set from the 103-01 timing spike and passed via the HIGH-3 override; pool bounded ≤150 at intake. |
| T-103-05-02 | Spoofing | Cache-miss resolution hitting a raw HttpClient / untrusted endpoint | mitigate | Resolution goes only through the existing `IScryfallCardResolver` + Polly pipeline (CLAUDE.md HTTP-resilience constraint); no new HTTP path. |
| T-103-05-03 | Tampering | Verdict/judgment leaking into projected copy | mitigate | Projection emits neutral metric labels only; direction is an enum field; CUT-02 copy test asserts no verdict words. |
| T-103-05-04 | Tampering | trials override altering default-caller behavior | mitigate | Override defaults to null → DefaultTrials; a byte-identical-at-default Core test guards every existing caller against drift. |
</threat_model>

<verification>
- `"/mnt/c/Program Files/dotnet/dotnet.exe" test DeckFlow.Core.Tests --filter "FullyQualifiedName~ManabaseAnalyzerTrialsOverride"` green (byte-identical at default).
- `"/mnt/c/Program Files/dotnet/dotnet.exe" test DeckFlow.Web.Tests --filter "FullyQualifiedName~CutLab"` green.
- Determinism guard (service tier) + delegation tests prove SIM-01 reuse.
</verification>

<success_criteria>
Analyze is parameterized without behavior change at default (HIGH-3); 7-family projection + proposal deltas + baseline snapshot all computed by reusing the existing engine, cached, deterministic, noise-floored, copy-neutral.
</success_criteria>

<output>
Create `.planning/workstreams/cut-lab/phases/103-simulation-engine-guided-cut-rounds/103-05-SUMMARY.md` when done. Record the in-loop trials value used (from 103-01).
</output>
</content>
