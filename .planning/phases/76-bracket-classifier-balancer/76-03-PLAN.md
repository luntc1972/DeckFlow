---
phase: 76-bracket-classifier-balancer
plan: 03
type: execute
wave: 2
depends_on: ["76-01"]
files_modified:
  - DeckFlow.Web/Services/PromptBuilders/Bracket/IBracketPromptVariant.cs
  - DeckFlow.Web/Services/PromptBuilders/Bracket/ChatGptBracketPromptVariant.cs
  - DeckFlow.Web/Services/PromptBuilders/Bracket/ClaudeBracketPromptVariant.cs
  - DeckFlow.Web/Services/PromptBuilders/Bracket/GeminiBracketPromptVariant.cs
  - DeckFlow.Web/Services/PromptBuilders/Bracket/BracketPromptVariantRegistry.cs
  - DeckFlow.Web/Program.cs
  - DeckFlow.Web.Tests/Bracket/BracketPromptVariantParityTests.cs
autonomous: true
requirements: [BRACKET-03, BRACKET-04, BRACKET-05]
must_haves:
  truths:
    - "All three prompt variants (ChatGpt/Claude/Gemini) render a classification block containing the bracket verdict, the WHY-THIS-BRACKET reasons, and the effective-date stamp"
    - "All three variants render a balancer block (floor violations + starter cuts) when a target bracket below the classified bracket is supplied"
    - "When ComboDetectionAvailable is false, every variant discloses 'combo detection unavailable' and never claims zero combos"
    - "No shared helper or base class is used across the three variants (ADR-0001)"
    - "A parity test asserts both the classification block and the balancer block appear in all three variants"
  artifacts:
    - path: "DeckFlow.Web/Services/PromptBuilders/Bracket/BracketPromptVariantRegistry.cs"
      provides: "AiPlatform-keyed dispatch with Default fallback"
      contains: "BracketPromptVariantRegistry"
    - path: "DeckFlow.Web.Tests/Bracket/BracketPromptVariantParityTests.cs"
      provides: "3-platform parity test (classification + balancer + effective-date)"
      contains: "BracketPromptVariantParityTests"
  key_links:
    - from: "DeckFlow.Web/Program.cs"
      to: "BracketPromptVariantRegistry"
      via: "AddSingleton<IBracketPromptVariant, ...> x3 + AddSingleton<BracketPromptVariantRegistry>"
      pattern: "IBracketPromptVariant"
---

<objective>
Author the three decoupled bracket paste-artifact prompt variants (ChatGpt / Claude / Gemini) plus their registry and a 3-platform parity test. Each variant hand-codes both a classification block (always) and a balancer block (when a sub-target is chosen) with the effective-date stamp and combo-unavailable disclosure — no shared helper, per ADR-0001.

Purpose: The artifact IS the deliverable (Core Value). BRACKET-04 mandates per-variant authoring + parity test; BRACKET-03 mandates floor-violations/starter-cuts framing and combo-null disclosure; BRACKET-05 mandates the effective-date stamp + re-confirm instruction in the prose.
Output: interface + 3 variants + registry + DI + parity test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/76-bracket-classifier-balancer/76-RESEARCH.md
@.planning/phases/76-bracket-classifier-balancer/76-PATTERNS.md
@.planning/phases/76-bracket-classifier-balancer/UI-SPEC.md
@.planning/phases/76-bracket-classifier-balancer/76-01-SUMMARY.md

<interfaces>
BracketClassification, BracketTier, GameChangerCatalog, TwoCardCombo (DeckFlow.Core/Bracket/*) — from 76-01.
AiPlatform (DeckFlow.Web/Models/AiPlatform.cs) — IMPORTANT: this is a `sealed record`, NOT an enum.
  Use the static instances AiPlatform.ChatGpt / AiPlatform.Claude / AiPlatform.Gemini, AiPlatform.Default,
  and AiPlatform.Normalize(string?) for parsing. Do NOT use Enum.Parse — AiPlatform is a sealed record.
Registry analog: DeckFlow.Web/Services/PromptBuilders/Primer/PrimerPromptVariantRegistry.cs
  uses `variants.ToDictionary(v => v.Platform)` and falls back to `_variants[AiPlatform.Default]`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: IBracketPromptVariant interface + BracketPromptVariantRegistry + DI</name>
  <read_first>
    - DeckFlow.Web/Services/PromptBuilders/Primer/IPrimerPromptVariant.cs (analog: interface shape, AiPlatform Platform getter)
    - DeckFlow.Web/Services/PromptBuilders/Primer/PrimerPromptVariantRegistry.cs (analog: ToDictionary + Default fallback)
    - DeckFlow.Web/Program.cs (find AddSingleton<IPrimerPromptVariant,...> registrations to mirror for bracket)
    - .planning/phases/76-bracket-classifier-balancer/76-PATTERNS.md (IBracketPromptVariant + BracketPromptVariantRegistry sections)
  </read_first>
  <files>DeckFlow.Web/Services/PromptBuilders/Bracket/IBracketPromptVariant.cs, DeckFlow.Web/Services/PromptBuilders/Bracket/BracketPromptVariantRegistry.cs, DeckFlow.Web/Program.cs</files>
  <action>
    Create namespace `DeckFlow.Web.Services.PromptBuilders.Bracket`. Define `internal interface IBracketPromptVariant` with `AiPlatform Platform { get; }` and `string Build(BracketClassification classification, int? targetBracketNumber, string? deckName, IReadOnlyList<BracketTier> tiers, GameChangerCatalog catalog, CancellationToken cancellationToken = default);`. Define `internal sealed class BracketPromptVariantRegistry` taking `IEnumerable<IBracketPromptVariant>`, building `_variants = variants.ToDictionary(v => v.Platform)`, with a public `Build(AiPlatform platform, BracketClassification classification, int? targetBracketNumber, string? deckName, IReadOnlyList<BracketTier> tiers, GameChangerCatalog catalog, CancellationToken cancellationToken = default)` that resolves the variant or falls back to `_variants[AiPlatform.Default]`. In Program.cs register the three variant classes (created in Task 2) as `AddSingleton<IBracketPromptVariant, ChatGptBracketPromptVariant>()` etc., plus `AddSingleton<BracketPromptVariantRegistry>()`, mirroring the Primer variant registrations. (Task 2 creates the three concrete classes; if executing Task 1 before they exist, add the registration lines after Task 2 compiles — keep all bracket DI lines together.)
  </action>
  <verify>
    <automated>"/mnt/c/Program Files/dotnet/dotnet.exe" build DeckFlow.Web/DeckFlow.Web.csproj -c Debug</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: IBracketPromptVariant.Build signature contains `int? targetBracketNumber` and `IReadOnlyList<BracketTier> tiers`.
    - Source assertion: `grep -c "AddSingleton<IBracketPromptVariant" DeckFlow.Web/Program.cs` returns 3 and `grep -c "AddSingleton<BracketPromptVariantRegistry>" DeckFlow.Web/Program.cs` returns 1.
    - Registry uses `AiPlatform.Default` fallback (no Enum.Parse anywhere).
  </acceptance_criteria>
  <done>Interface + registry compile and all four bracket DI registrations are present.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Three decoupled bracket prompt variants (ChatGpt/Claude/Gemini) + 3-platform parity test</name>
  <read_first>
    - DeckFlow.Web/Services/PromptBuilders/Primer/ChatGptPrimerPromptVariant.cs, ClaudePrimerPromptVariant.cs, GeminiPrimerPromptVariant.cs (analogs: per-platform StringBuilder bodies, platform-specific framing — markdown headers for ChatGpt/Gemini, XML tags for Claude)
    - .planning/phases/76-bracket-classifier-balancer/UI-SPEC.md (§7 exact microcopy for tier names, reasons, floor-violations, starter cuts, effective-date stamp, combo-unavailable note)
    - .planning/phases/76-bracket-classifier-balancer/76-PATTERNS.md (ChatGptBracketPromptVariant section — combo-unavailable + effective-date-stamp hand-coded lines; AND BracketPromptVariantParityTests section — exact test shape)
    - DeckFlow.Web.Tests/ResultContractTests.cs (analog: 3-platform Theory + inline registry builder)
    - DeckFlow.Web/Models/AiPlatform.cs (use AiPlatform.Normalize for InlineData string -> platform; never Enum.Parse)
  </read_first>
  <files>DeckFlow.Web/Services/PromptBuilders/Bracket/ChatGptBracketPromptVariant.cs, DeckFlow.Web/Services/PromptBuilders/Bracket/ClaudeBracketPromptVariant.cs, DeckFlow.Web/Services/PromptBuilders/Bracket/GeminiBracketPromptVariant.cs, DeckFlow.Web.Tests/Bracket/BracketPromptVariantParityTests.cs</files>
  <behavior>
    - Build() returns text containing the literal section marker "WHY THIS BRACKET" (classification block, always)
    - Build() text contains the verdict (e.g. "Bracket N" + tier Name from tiers) and the detected Game Changers count
    - Build() text contains the effective-date stamp phrase "Game Changers list effective {classification.EffectiveDate}" and a re-confirm instruction
    - When ComboDetectionAvailable is false: text contains a combo-unavailable disclosure and never the phrase "0 two-card combos"/"no combos found"
    - When targetBracketNumber is non-null AND classification.BracketNumber > targetBracketNumber: text contains "FLOOR VIOLATIONS" and "STARTER CUTS" sections listing detected over-target cards/combos/MLD
    - When targetBracketNumber is null OR classification.BracketNumber <= targetBracketNumber: NO "FLOOR VIOLATIONS" section (classify-only / meets-target artifact)
    - Each of the three files is self-contained: no shared base class, no shared helper method, no cross-file static utility
    - Parity test asserts the classification block AND the balancer block AND the effective-date stamp appear in ALL three variants (ChatGpt/Claude/Gemini), and that the combo-unavailable disclosure appears with no "no combos found" claim — making the GREEN gate real for this task
  </behavior>
  <action>
    Follow the project TDD pattern (analog: 76-01 Task 3) — author the parity test and the three variants TOGETHER in this one task so the `<verify>` is a meaningful GREEN gate, not a vacuous match against a not-yet-authored test class. The parity test defines the GREEN contract (it fails to compile / fails RED until the three variants exist); the variant implementations turn it GREEN. Do NOT split the test into a later task.

    (1) Variants: Create three `internal sealed class` variants implementing `IBracketPromptVariant`, each with `Platform => AiPlatform.ChatGpt|Claude|Gemini`. Each `Build(...)` uses its own `StringBuilder`, opens with `ArgumentNullException.ThrowIfNull` on classification/tiers/catalog, and hand-writes (per platform framing — ChatGpt markdown `##` headers, Claude `<bracket_classification>`-style XML tags, Gemini markdown persona scaffold) TWO blocks: (1) CLASSIFICATION — the resolved tier verdict (look up the tier by `classification.BracketNumber` in `tiers`), a "WHY THIS BRACKET" reasons section enumerating `DetectedGameChangers` count vs the target tier's MaxGameChangers, two-card combos (names), MLD names, an "N extra-turn cards" informational line, the explicit "Not counted: tutors — removed Oct 2025" footnote, and the effective-date stamp line `"Game Changers list effective {classification.EffectiveDate}. Re-confirm Game Changers membership before suggesting swaps."`; plus the combo-unavailable disclosure when `!classification.ComboDetectionAvailable`. (2) BALANCER — only when `targetBracketNumber is int t && classification.BracketNumber > t`: a "FLOOR VIOLATIONS" list (the specific over-target Game Changers + combo halves + MLD) and a "STARTER CUTS" list framed for AI refinement into power-equivalent swaps. Use UI-SPEC §7 microcopy as the prose source. ADR-0001: do NOT factor any of this into a shared method or base class — each file repeats its own prose. Add `// Why: ADR-0001 — bracket prompt variants are intentionally decoupled; do not extract shared text.` at the top of each.

    (2) Parity test: Create `BracketPromptVariantParityTests` in namespace `DeckFlow.Web.Tests` (file `DeckFlow.Web.Tests/Bracket/BracketPromptVariantParityTests.cs`). Private `BuildRegistry()` returns `new BracketPromptVariantRegistry(new IBracketPromptVariant[] { new ChatGptBracketPromptVariant(), new ClaudeBracketPromptVariant(), new GeminiBracketPromptVariant() })` (registry from Task 1). Private builders `BuildClassification(int bracketNumber = 4, bool comboAvailable = true)`, `BuildTiers()` (5 BracketTier records), `BuildCatalog()`. `[Theory]` methods with `[InlineData("ChatGPT")] [InlineData("Claude")] [InlineData("Gemini")]`, each resolving `var platform = AiPlatform.Normalize(platformName);` (never Enum.Parse — AiPlatform is a sealed record): (a) `Build_ClassificationBlock_AppearsInAllThreeVariants` — Build with null target, assert result contains "WHY THIS BRACKET" and "Game Changers list effective" (Ordinal). (b) `Build_BalancerBlock_AppearsInAllThreeVariants_WhenTargetBelowClassified` — Build with bracketNumber 4 + targetBracketNumber 2, assert contains "FLOOR VIOLATIONS" and "STARTER CUTS". (c) `Build_EffectiveDateStamp_AppearsInAllThreeVariants` — assert contains "2026-02-09". (d) `Build_ComboUnavailable_DisclosedInAllThreeVariants` with comboAvailable=false asserting a combo-unavailable phrase is present and "no combos found" is absent. Do NOT construct a shared variant helper in the test — instantiate the three concrete variants directly (ADR-0001).
  </action>
  <verify>
    <automated>"/mnt/c/Program Files/dotnet/dotnet.exe" test DeckFlow.Web.Tests/DeckFlow.Web.Tests.csproj --filter "FullyQualifiedName~BracketPromptVariantParityTests"</automated>
  </verify>
  <acceptance_criteria>
    - Test command: all four parity theories pass across all three platform InlineData rows (12+ cases) — a real GREEN gate, since the test class is authored in THIS task alongside the variants it exercises.
    - Source assertion: each of the three variant files contains `"WHY THIS BRACKET"` and `"Game Changers list effective"` (`grep -l` matches all three).
    - Source assertion: no shared bracket helper file exists — `ls DeckFlow.Web/Services/PromptBuilders/Bracket/` contains only the interface, 3 variants, and the registry (no `*Helper*`/`*Base*`/`*Shared*`); the test file likewise instantiates the three concrete variants directly (no shared variant helper).
    - Source assertion: test file uses `AiPlatform.Normalize(` and does not contain `Enum.Parse`.
    - Behavior assertion: combo-unavailable theory proves no variant emits "no combos found" when detection is unavailable.
  </acceptance_criteria>
  <done>Three self-contained variants render classification + conditional balancer + effective-date stamp + combo disclosure, with no shared helper, and the parity test (authored in this same task) is green proving classification + balancer + effective-date + combo-unavailable disclosure across all three variants.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| classification model → prompt text | Detected card names (from untrusted deck import) interpolated into artifact prose |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-76-06 | Tampering | card names in artifact prose | accept | Output is a paste artifact the user copies into their own AI chat; card names are public strings; no markup execution context. Same surface as existing Primer/Analysis artifacts. |
| T-76-07 | Information Disclosure | combo-unavailable path | mitigate | Variants disclose unavailability (BRACKET-03) rather than asserting zero combos — prevents misleading classification. Verified by parity test. |
| T-76-SC | Tampering | npm/pip/cargo installs | accept | No package installs; in-solution only. |
</threat_model>

<verification>
- `dotnet build DeckFlow.Web/DeckFlow.Web.csproj` clean.
- `dotnet test --filter "FullyQualifiedName~BracketPromptVariantParityTests"` green.
- No shared helper/base in the Bracket prompt folder (ADR-0001).
</verification>

<success_criteria>
- BRACKET-04: classification + balancer blocks present in all three variants, guarded by a parity test; no shared helper.
- BRACKET-03 (artifact prose): floor-violations + starter-cuts framing rendered; combo-null disclosed.
- BRACKET-05 (artifact prose): effective-date stamp + re-confirm instruction in every variant.
</success_criteria>

<output>
Create `.planning/phases/76-bracket-classifier-balancer/76-03-SUMMARY.md` when done.
</output>
</content>
</invoke>
