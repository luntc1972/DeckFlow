---
phase: 20-content-kb-ingestion-transcription-local
plan: 04
type: execute
wave: 3
depends_on: ["20-01", "20-02", "20-03"]
files_modified:
  - DeckFlow.CLI/Program.cs
  - DeckFlow.CLI/CommandRunners.cs
  - DeckFlow.Core/Integration/YouTubeTranscriptSource.cs
  - DeckFlow.Core/Integration/IYouTubeChannelVideoLister.cs
  - DeckFlow.Core/Integration/YouTubeChannelVideoLister.cs
  - DeckFlow.Core.Tests/YouTubeTranscriptSourceTests.cs
autonomous: false
requirements: [KB-03, KB-04, KB-05]
user_setup:
  - service: openai
    why: "Whisper transcription fallback for caption-less videos (KB-04)"
    env_vars:
      - name: OPENAI_API_KEY
        source: "OpenAI Dashboard → API keys (local environment only; never committed)"
  - service: ffmpeg
    why: "Chunk audio >24MB before Whisper upload (D-05 local prerequisite)"
    dashboard_config:
      - task: "Install ffmpeg and put it on PATH (absent → videos >24MB marked failed, run continues)"
        location: "Local machine PATH"
must_haves:
  truths:
    - "A single CLI invocation (harvest) reads enabled content_sources, lists recent videos per source, fetches captions first, and falls back to Whisper only when captions are absent and the cap allows"
    - "YouTubeTranscriptSource implements ITranscriptSource: captions → Whisper fallback → final status, behind one abstraction"
    - "Channel video listing applies .Take(limit) so a run never drains an entire channel history (P5)"
    - "The harvester verifies ffmpeg presence at start and warns (does not abort) when absent (P7)"
    - "A local UAT over the 5 seeded MTG channels fetches captions and asserts whisper_fallback_ratio < 25% (SC2/P2), with transcript_source logged per fetch"
  artifacts:
    - path: "DeckFlow.Core/Integration/YouTubeTranscriptSource.cs"
      provides: "ITranscriptSource composing YouTubeTranscriptFetcher (captions) + WhisperTranscriptionService (fallback)"
      contains: "ITranscriptSource"
    - path: "DeckFlow.CLI/CommandRunners.cs"
      provides: "RunHarvestAsync orchestration entry (Phase-20 ingestion scope; Phase 21 layers distillation)"
      contains: "RunHarvestAsync"
    - path: "DeckFlow.Core/Integration/YouTubeChannelVideoLister.cs"
      provides: "GetUploadsAsync(.Take(limit)) recent-video lister (P5 bounded)"
      contains: "Take"
  key_links:
    - from: "DeckFlow.Core/Integration/YouTubeTranscriptSource.cs"
      to: "DeckFlow.Core/Integration/YouTubeTranscriptFetcher.cs + WhisperTranscriptionService.cs"
      via: "captions-first then Whisper fallback"
      pattern: "FetchCaptionsAsync"
    - from: "DeckFlow.CLI/CommandRunners.cs"
      to: "DeckFlow.Core/Content/ContentSourceStore.cs"
      via: "ListEnabledSourcesAsync drives the run"
      pattern: "ListEnabledSourcesAsync"
    - from: "DeckFlow.CLI/CommandRunners.cs"
      to: "ffmpeg presence warning"
      via: "IFfmpegAudioChunker.IsAvailableAsync at harvest start"
      pattern: "IsAvailableAsync"
---

<objective>
Compose the Plan 20-02/20-03 ingestion services into the `harvest` CLI verb and the `YouTubeTranscriptSource` (the first `ITranscriptSource` implementation), then prove KB-03's P1/P2 success criteria with a local UAT over the 5 seeded MTG channels. This is the phase boundary: captions/transcripts fetched + persisted, ledger rows written, cap gating Whisper. (Phase 21 layers distillation/artifact-emit on top of `RunHarvestAsync`.)

Purpose: SC1 (5 real third-party channels via YoutubeExplode, not Google.Apis), SC2 (proxy-pluggable provider + whisper_fallback_ratio < 25% + transcript_source log). The orchestration here is the ingestion-only slice: list videos → persist content_videos (pending) → captions → Whisper fallback (cap-gated) → mark status. Phase 21 owns summary/clips/tags/artifact emit/run-record.
Output: `YouTubeTranscriptSource` (ITranscriptSource); bounded channel video lister; `harvest` CLI verb + `RunHarvestAsync`; the 5-channel UAT checkpoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-content-kb-ingestion-transcription-local/20-CONTEXT.md
@.planning/phases/20-content-kb-ingestion-transcription-local/20-RESEARCH.md
@.planning/phases/20-content-kb-ingestion-transcription-local/20-01-PLAN.md
@.planning/phases/20-content-kb-ingestion-transcription-local/20-02-PLAN.md
@.planning/phases/20-content-kb-ingestion-transcription-local/20-03-PLAN.md

<interfaces>
<!-- From Plan 20-02 -->
- `ITranscriptSource { string SourceType { get; } Task<TranscriptFetchResult?> FetchTranscriptAsync(long videoId, string naturalKey, string? audioUrl, CancellationToken) }`
- `record TranscriptFetchResult { Body; Source; FinalStatus }` (init-only)
- `IYouTubeTranscriptFetcher { Task<YouTubeCaptionResult> FetchCaptionsAsync(string videoId, CancellationToken) }`; `YouTubeCaptionResult { HasCaptions; Body; IsAutoGenerated; LanguageCode }`

<!-- From Plan 20-03 -->
- `IWhisperTranscriptionService { Task<WhisperTranscriptionResult> TranscribeAsync(long videoId, int estimatedDurationSeconds, Stream audioStream, string audioFilename, CancellationToken) }`
- `IFfmpegAudioChunker { Task<bool> IsAvailableAsync(CancellationToken); Task<IReadOnlyList<string>> ChunkAsync(...) }`

<!-- From Phase 19 stores -->
- `ContentSourceStore.ListEnabledSourcesAsync(CancellationToken)` → IReadOnlyList<ContentSource> { Id, SourceSlug, DisplayName, SourceType, SourceUrl, IsEnabled }
- `ContentVideoStore.InsertVideoAsync(sourceId, youtubeVideoId, rssGuid, title, videoUrl, publishedUtc, transcriptStatus, ct)` → long; initial status TranscriptStatus.Pending
- `ContentVideoStore.InsertTranscriptAsync` / `UpdateTranscriptStatusAsync` (20-01)

<!-- YoutubeExplode 6.6.0 (RESEARCH) -->
- `youtube.Channels.GetUploadsAsync(channelId/url)` → IAsyncEnumerable<PlaylistVideo>; MUST `.Take(limit).ToListAsync()` (P5)
- `PlaylistVideo` has `.Id`, `.Url`, `.Title`; use `.Url` for downstream caption/stream calls (Open Q3 resolution)
- `youtube.Videos.Streams.GetManifestAsync(videoId)` → audio stream; `IVideoStreamInfo.Size.Bytes` known pre-download (P4)

<!-- CLI host (DeckFlow.CLI/Program.cs) -->
- flat command registration on rootCommand; handler form `Environment.ExitCode = CommandRunners.RunXAsync(...).GetAwaiter().GetResult();`
- artifacts DB path convention: artifacts/content-kb.db
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: YouTubeChannelVideoLister + YouTubeTranscriptSource (ITranscriptSource impl)</name>
  <files>DeckFlow.Core/Integration/IYouTubeChannelVideoLister.cs, DeckFlow.Core/Integration/YouTubeChannelVideoLister.cs, DeckFlow.Core/Integration/YouTubeTranscriptSource.cs, DeckFlow.Core.Tests/YouTubeTranscriptSourceTests.cs</files>
  <read_first>
    - DeckFlow.Core/Integration/YouTubeTranscriptFetcher.cs (Plan 20-02 — FetchCaptionsAsync + Func<> ctor seam to mirror)
    - DeckFlow.Core/Integration/WhisperTranscriptionService.cs (Plan 20-03 — TranscribeAsync; the fallback path)
    - DeckFlow.Core/Integration/ITranscriptSource.cs (Plan 20-02 — the contract to implement)
    - .planning/phases/20-content-kb-ingestion-transcription-local/20-RESEARCH.md Pitfall P5 (.Take bound), P4 (Size pre-download), Open Question 3 (PlaylistVideo.Url)
  </read_first>
  <action>
    Create `IYouTubeChannelVideoLister` exposing `Task<IReadOnlyList<YouTubeChannelVideo>> ListRecentAsync(string channelUrl, int limit, CancellationToken ct = default)` and `record YouTubeChannelVideo { get; init; }` (VideoId, Url, Title, PublishedUtc?). Implement `YouTubeChannelVideoLister` over `youtube.Channels.GetUploadsAsync(...)` ALWAYS chained `.Take(limit).ToListAsync(ct)` (P5 — never drain history); HttpClient ctor-injected + a Func<> override seam for tests. Create `YouTubeTranscriptSource : ITranscriptSource` with `SourceType => ContentSourceType.Youtube`, ctor deps `IYouTubeTranscriptFetcher fetcher, IWhisperTranscriptionService whisper`. `FetchTranscriptAsync(videoId, naturalKey, audioUrl, ct)`: call `fetcher.FetchCaptionsAsync(naturalKey, ct)`; if `HasCaptions`, return a TranscriptFetchResult { Body, Source = TranscriptSource.Captions, FinalStatus = TranscriptStatus.Captions } (caller persists transcript + status); else (no captions) delegate to the Whisper fallback via `whisper.TranscribeAsync(...)` — map its WhisperTranscriptionResult.FinalStatus (Whisper / Failed / SkippedOverCap) into the returned TranscriptFetchResult, returning null when there is no usable body (skipped/failed) so the caller marks status accordingly. Keep methods ≤30 lines. Add `YouTubeTranscriptSourceTests` via the Func<> seams of the injected fetcher/whisper fakes: (a) captions present → Source=captions, FinalStatus=captions, fetcher hit, whisper NOT invoked; (b) no captions + whisper success → Source=whisper, FinalStatus=whisper; (c) no captions + whisper skipped_over_cap → FinalStatus=skipped_over_cap. No live HTTP.
  </action>
  <verify>
    <automated>"/mnt/c/Program Files/dotnet/dotnet.exe" build DeckFlow.Core/DeckFlow.Core.csproj -c Debug 2>&1 | grep -E "Build succeeded|error"; "/mnt/c/Program Files/dotnet/dotnet.exe" test DeckFlow.Core.Tests/DeckFlow.Core.Tests.csproj --filter "FullyQualifiedName~YouTubeTranscriptSourceTests" 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c ": ITranscriptSource" DeckFlow.Core/Integration/YouTubeTranscriptSource.cs` ≥1
    - `grep -c "\.Take(" DeckFlow.Core/Integration/YouTubeChannelVideoLister.cs` ≥1 (P5 bound present)
    - captions-present test proves whisper.TranscribeAsync was NOT invoked (captions-first, free path)
    - Build clean; YouTubeTranscriptSourceTests pass (or build-clean fallback per WSL VSTest caveat)
  </acceptance_criteria>
  <done>YouTubeTranscriptSource composes captions-first/Whisper-fallback behind ITranscriptSource; channel listing is bounded by .Take; covered by seam tests.</done>
</task>

<task type="auto">
  <name>Task 2: harvest CLI verb + RunHarvestAsync (ingestion-only orchestration)</name>
  <files>DeckFlow.CLI/Program.cs, DeckFlow.CLI/CommandRunners.cs</files>
  <read_first>
    - DeckFlow.CLI/Program.cs (flat command registration; SetHandler GetAwaiter().GetResult())
    - DeckFlow.CLI/CommandRunners.cs lines 375-415 (RunArchidektCacheAsync — Serilog logger + artifacts/ DB path convention to mirror)
    - DeckFlow.Core/Content/ContentSourceStore.cs (ListEnabledSourcesAsync) + ContentVideoStore.cs (InsertVideoAsync initial pending + UpdateTranscriptStatusAsync from 20-01)
    - DeckFlow.Core/Integration/YouTubeTranscriptSource.cs (Task 1) + YouTubeChannelVideoLister.cs + FfmpegAudioChunker.cs (IsAvailableAsync)
    - .planning/phases/20-content-kb-ingestion-transcription-local/20-RESEARCH.md Pattern 5 (CLI wiring) + P7 (ffmpeg warn-not-abort)
  </read_first>
  <action>
    Register a flat `harvest` command on rootCommand. Options: `--db` (Option&lt;FileInfo?&gt; default artifacts/content-kb.db), `--limit` (Option&lt;int&gt; default 5 — recent videos per source, bounds P5). Add `public static async Task<int> RunHarvestAsync(FileInfo? db, int limit, Serilog.ILogger logger, CancellationToken ct)`. Compose the dependency graph by hand (CLI has no DI container): build `HttpClient` instances (one for YouTube, one with 15min Timeout for Whisper), construct `YouTubeTranscriptFetcher`, `WhisperTranscriptionService` (with WhisperSpendLedger + ContentVideoStore over the resolved DB), `FfmpegAudioChunker`, `YouTubeChannelVideoLister`, and `YouTubeTranscriptSource`. At start: call `chunker.IsAvailableAsync` and if false, `logger.Warning("ffmpeg not found on PATH — audio >24MB will be marked failed.")` and CONTINUE (P7 — do NOT abort). Flow: `ListEnabledSourcesAsync`; for each youtube_channel source, `lister.ListRecentAsync(source.SourceUrl, limit, ct)`; for each video, `InsertVideoAsync(sourceId, youtubeVideoId, null, title, url, publishedUtc, TranscriptStatus.Pending, ct)` (skip if natural-key UNIQUE collision — catch + log "already harvested"); call `transcriptSource.FetchTranscriptAsync(videoId, youtubeVideoId, audioUrl: null, ct)`; on a non-null result `InsertTranscriptAsync(videoId, result.Source, result.Body, ct)` then `UpdateTranscriptStatusAsync(videoId, result.FinalStatus, ct)`; on null result `UpdateTranscriptStatusAsync(videoId, result?.FinalStatus ?? TranscriptStatus.Failed, ct)`. Emit a structured log per fetch including `transcript_source` (captions/whisper) and `IsAutoGenerated` where applicable (SC2). Tally captions-count vs whisper-count and at end log the `whisper_fallback_ratio`. Wrap per-video work in try/catch so one bad video never aborts the run (mark failed + continue). Scope note in code comment: distillation/artifact-emit/run-record are Phase 21 — RunHarvestAsync here stops at transcript persistence. Wire SetHandler with GetAwaiter().GetResult(). No new packages.
  </action>
  <verify>
    <automated>"/mnt/c/Program Files/dotnet/dotnet.exe" build DeckFlow.CLI/DeckFlow.CLI.csproj -c Debug 2>&1 | grep -E "Build succeeded|error"; "/mnt/c/Program Files/dotnet/dotnet.exe" run --project DeckFlow.CLI -- harvest --help 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '"harvest"' DeckFlow.CLI/Program.cs` ≥1; `grep -c "RunHarvestAsync" DeckFlow.CLI/CommandRunners.cs` ≥1
    - `grep -c "IsAvailableAsync" DeckFlow.CLI/CommandRunners.cs` ≥1 (ffmpeg start-check) AND the absent path logs a warning and continues (no early return/throw on missing ffmpeg)
    - `grep -c "whisper_fallback_ratio" DeckFlow.CLI/CommandRunners.cs` ≥1 (ratio computed for SC2)
    - `harvest --help` lists --db and --limit; build clean
    - git diff touches only the Program.cs registration block + the new RunHarvestAsync method
  </acceptance_criteria>
  <done>harvest verb reads enabled sources, lists bounded recent videos, runs captions-first/Whisper-fallback per video, persists transcripts + status, warns on missing ffmpeg without aborting, and logs the fallback ratio.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: 5-channel local caption-coverage UAT (SC1 + SC2)</name>
  <files>artifacts/uat-content-kb.db (runtime UAT DB — not committed)</files>
  <action>HUMAN-VERIFY checkpoint (no code change). The operator runs the seeded harvest over the 5 real MTG channels from the local environment and confirms SC1 (third-party captions fetched via YoutubeExplode, no Google.Apis 403) and SC2 (transcript_source logged per fetch + whisper_fallback_ratio < 0.25). Exact commands are in how-to-verify below. ffmpeg is optional here (captions path needs none); a missing-ffmpeg warning must not abort the run (P7).</action>
  <verify><human-check>Operator confirms captions fetched for the majority of the 5 channels and whisper_fallback_ratio < 0.25 in the CLI log.</human-check></verify>
  <done>Operator types "approved" with the observed whisper_fallback_ratio; SC1 + SC2 confirmed from the local environment.</done>
  <what-built>
    The `harvest` CLI verb + `YouTubeTranscriptSource` now fetch captions from the local environment via YoutubeExplode 6.6.0 over the 5 seeded MTG channels. Plan 20-01's `content-source-add` seeds the sources.
  </what-built>
  <how-to-verify>
    Run from the repo root (requires internet; OPENAI_API_KEY only needed if any video lacks captions):
    1. Seed the 5 channels (idempotent) into a UAT DB:
       `dotnet run --project DeckFlow.CLI -- content-source-add --name "MTGGoldfish" --url https://www.youtube.com/@MTGGoldfish --type youtube_channel --db artifacts/uat-content-kb.db`
       repeat for: The Command Zone (https://www.youtube.com/@TheCommandZone), EDHRECast (https://www.youtube.com/@EDHRECast), Tolarian Community College (https://www.youtube.com/@TolarianCommunityCollege), Playing With Power (https://www.youtube.com/@PlayingwithPowerMTG)
    2. Run a bounded harvest: `dotnet run --project DeckFlow.CLI -- harvest --db artifacts/uat-content-kb.db --limit 1`  (1 recent video per channel = 5 videos)
    3. Confirm in the CLI log / logs/cli-*.log:
       - captions fetched for the majority of the 5 channels via YoutubeExplode (SC1 — third-party caption fetch works; no Google.Apis 403)
       - a `transcript_source` value is logged per fetch (auto-generated | manual) (SC2)
       - the run prints `whisper_fallback_ratio` and it is `< 0.25` (SC2/P2) — i.e. at most 1 of 5 fell through to Whisper
       - if ffmpeg is absent, a single warning was logged and the run did NOT abort (P7)
    4. (Optional) inspect artifacts/uat-content-kb.db content_transcripts rows have source='captions' for the captioned videos.
  </how-to-verify>
  <resume-signal>Type "approved" with the observed whisper_fallback_ratio, or describe which channels failed caption fetch / what error appeared.</resume-signal>
</task>

</tasks>

<verification>
- SC1 (P1): captions fetched for the 5 real third-party MTG channels via YoutubeExplode 6.6.0; Google.Apis.YouTube never used (grep across phase = 0).
- SC2 (P2): provider-pluggable seam (20-02) + transcript_source logged + whisper_fallback_ratio < 25% asserted in UAT.
- KB-03/KB-04/KB-05 exercised end-to-end through the harvest verb (caption path always; Whisper path when captions absent + cap allows).
- P5 bound (.Take(limit)) and P7 (ffmpeg warn-not-abort) honored.
</verification>

<success_criteria>
- One CLI invocation harvests captions/transcripts for the enabled sources and persists them locally with correct status.
- The 5-channel UAT confirms whisper_fallback_ratio < 25% with transcript_source logged per fetch.
- Nothing runs on Render; no new packages beyond the two approved.
</success_criteria>

<output>
Create `.planning/phases/20-content-kb-ingestion-transcription-local/20-04-SUMMARY.md` when done.
</output>
