---
phase: 95-measured-style-extractor
plan: 04
type: execute
wave: 2
depends_on: [95-03]
files_modified:
  - DeckFlow.Core/Knowledge/MeasuredStyleExtraction/CreatorDeckSample.cs
  - DeckFlow.Core/Knowledge/MeasuredStyleExtraction/MeasuredStyleInputs.cs
  - DeckFlow.Core/Knowledge/MeasuredStyleExtraction/StapleStripper.cs
  - DeckFlow.Core/Knowledge/MeasuredStyleExtraction/FolderWeighting.cs
  - DeckFlow.Core.Tests/MeasuredStyleExtraction/StapleStripperTests.cs
  - DeckFlow.Core.Tests/MeasuredStyleExtraction/FolderWeightingTests.cs
autonomous: true
requirements: [CS-04c, CS-04d, CS-05]
must_haves:
  truths:
    - "The pure extraction algorithm accepts plain in-memory deck+category data with zero HttpClient/AspNet dependency (D-11)"
    - "Curated staples UNION cards appearing in >60% of a creator's decks are stripped BEFORE any ratio is computed"
    - ">105-card decks are dropped and near-precon duplicates are flagged before ratios"
    - "Folder weights grade each deck (curated map, default 1.0 uncurated) and produce a fractional folder-weighted effective sample"
  artifacts:
    - path: "DeckFlow.Core/Knowledge/MeasuredStyleExtraction/CreatorDeckSample.cs"
      provides: "The narrow host-agnostic input contract (D-11)"
      min_lines: 20
    - path: "DeckFlow.Core/Knowledge/MeasuredStyleExtraction/StapleStripper.cs"
      provides: "Hybrid staple-strip + >105 filter + near-precon dedup (pure)"
      min_lines: 40
    - path: "DeckFlow.Core/Knowledge/MeasuredStyleExtraction/FolderWeighting.cs"
      provides: "Graded folder weighting + effective-sample computation (pure)"
      min_lines: 30
  key_links:
    - from: "StapleStripper"
      to: "ContentTagVocabulary.Staples"
      via: "curated staple union"
      pattern: "ContentTagVocabulary\\.Staples"
---

<objective>
Define the narrow host-agnostic extraction CONTRACT (D-11) and the first two pure, HttpClient-free algorithm pieces: `StapleStripper` (curated `ContentTagVocabulary.Staples` UNION the per-creator >60%-frequency cut, applied BEFORE any ratio — CS-05 — plus the >105-card filter and near-precon dedup from CS-04c) and `FolderWeighting` (graded per-folder weights with a 1.0 default for uncurated creators, producing the D-10 folder-weighted fractional effective sample — CS-04d).

Purpose: The Codex-flagged Core-vs-Web layering resolution — these are pure functions unit-testable with no network. They feed the Web orchestrator (Plan 07).
Output: The input contract records + two pure static classes + their tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/95-measured-style-extractor/95-CONTEXT.md
@.planning/phases/95-measured-style-extractor/95-RESEARCH.md
@DeckFlow.Core/Reporting/CategoryFilter.cs
@DeckFlow.Core/Knowledge/ContentTagVocabulary.cs

<interfaces>
<!-- Pure-static-Core-helper convention to imitate (static class, no DI, HashSet allowlist, plain in/out). -->
From DeckFlow.Core/Reporting/CategoryFilter.cs:
  public static class CategoryFilter { public static bool IsIncluded(string?); public static IReadOnlyList<string> IncludedOrFallback(IEnumerable<string>); }

<!-- Curated staple set authored in Plan 03 (dependency). -->
From DeckFlow.Core/Knowledge/ContentTagVocabulary.cs:
  public static readonly IReadOnlySet<string> Staples;   // ubiquitous lands/rocks, case-insensitive

<!-- Deck row shape the samples wrap. -->
From DeckFlow.Core/Models/DeckEntry.cs:
  public sealed record DeckEntry { string Name; int Quantity; string? SetCode; string? CollectorNumber; ... }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 0/1: Extraction input contract (CreatorDeckSample + MeasuredStyleInputs)</name>
  <read_first>
    - 95-RESEARCH.md §"Recommended Project Structure" + §"System Architecture Diagram" (the pure-layer input shape)
    - DeckFlow.Core/Models/DeckEntry.cs (the deck row type the samples carry)
    - DeckFlow.Core/Knowledge/GlobalCategoryBaseline.cs (Plan 03 — carried as the lift denominator input)
  </read_first>
  <action>Create namespace `DeckFlow.Core.Knowledge.MeasuredStyleExtraction`. Add `CreatorDeckSample.cs`: `sealed record CreatorDeckSample { required string DeckId; required IReadOnlyList<DeckEntry> Entries; required int CardCount; int? FolderId; string? FolderName; double FolderWeight = 1.0; required string ConfidenceMarker; }` — this is the narrow host-agnostic contract (D-11); it carries ONLY plain in-memory data, no HttpClient/service types. Add `MeasuredStyleInputs.cs`: `sealed record MeasuredStyleInputs { required IReadOnlyList<CreatorDeckSample> Samples; required IReadOnlyDictionary<string, IReadOnlyList<string>> CardCategories; required GlobalCategoryBaseline Baseline; bool WeightsUncurated; }` where `CardCategories` maps a card name → the (already multi-bucket) categories the Web tier resolved for it (D-06 resolution happens in the host; the pure layer just consumes the map). All `{ get; init; }`. No `using` of any `DeckFlow.Web` or `System.Net.Http` type anywhere in this namespace. Preserve LF line endings.</action>
  <verify>
    <automated>dotnet build DeckFlow.Core/DeckFlow.Core.csproj</automated>
  </verify>
  <acceptance_criteria>
    - Both records exist under `DeckFlow.Core/Knowledge/MeasuredStyleExtraction/`
    - `grep -rc "System.Net.Http\|DeckFlow.Web\|HttpClient" DeckFlow.Core/Knowledge/MeasuredStyleExtraction/` == 0 (D-11 host-agnostic contract)
    - `dotnet build DeckFlow.Core/DeckFlow.Core.csproj` succeeds
  </acceptance_criteria>
  <done>The pure extraction contract compiles with zero host/HTTP dependency.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: StapleStripper — hybrid staple-strip + >105 filter + near-precon dedup</name>
  <read_first>
    - DeckFlow.Core/Reporting/CategoryFilter.cs (pure-static-helper shape to imitate)
    - DeckFlow.Core/Knowledge/ContentTagVocabulary.cs (Staples set — Plan 03)
    - DeckFlow.Core/Knowledge/MeasuredStyleExtraction/CreatorDeckSample.cs (Task 1)
    - 95-RESEARCH.md Pitfall 5 (dedup threshold is Claude's discretion — document, don't pretend it's locked) + §specifics (>60% frequency cut, 24+/39 for Snail)
  </read_first>
  <behavior>
    - A card in ContentTagVocabulary.Staples is stripped from every deck (case-insensitive)
    - A card present in >60% of the (post->105-filter) decks is stripped even if not a curated staple; a card at exactly 60% or below is retained
    - A deck with CardCount > 105 is removed before frequency/ratio computation
    - Two decks with >70% card-overlap (Jaccard) are flagged: the later/duplicate gets a "near-precon" confidence marker (documented threshold, Claude's discretion)
    - Stripping happens before any ratio: the returned sample set has staples removed from Entries
  </behavior>
  <action>Create `StapleStripper.cs` as a `public static class` (no DI, no `this`) following `CategoryFilter`'s shape. Provide: `FilterOversized(IReadOnlyList<CreatorDeckSample>) → IReadOnlyList<CreatorDeckSample>` dropping `CardCount > 105` (CS-04c, uses the cheap size field per RESEARCH Open Question 3); `FlagNearPrecons(IReadOnlyList<CreatorDeckSample>) → IReadOnlyList<CreatorDeckSample>` computing pairwise Jaccard card-name overlap and re-marking the ConfidenceMarker of a deck exceeding a documented threshold (choose >0.70; add a `// Why:` comment stating this is Claude's-discretion per CONTEXT D-03/Pitfall 5, not a locked number); `ComputePersonalStaples(IReadOnlyList<CreatorDeckSample>, double frequencyThreshold = 0.60) → IReadOnlySet<string>` returning card names present in strictly MORE THAN 60% of decks; `StripStaples(IReadOnlyList<CreatorDeckSample>, IReadOnlySet<string> personalStaples) → IReadOnlyList<CreatorDeckSample>` removing `ContentTagVocabulary.Staples ∪ personalStaples` from each sample's Entries (case-insensitive, via a normalized comparison). Keep every method pure and total. Preserve LF line endings.</action>
  <verify>
    <automated>dotnet test DeckFlow.Core.Tests --filter "FullyQualifiedName~StapleStripperTests"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "ContentTagVocabulary.Staples" DeckFlow.Core/Knowledge/MeasuredStyleExtraction/StapleStripper.cs` >= 1
    - Tests prove: curated staple stripped; a >60% card stripped; a card at exactly 60% retained; a >105 deck removed; a >70%-overlap deck flagged near-precon
    - The >0.70 dedup threshold carries a `// Why:` comment marking it Claude's discretion
    - `dotnet test DeckFlow.Core.Tests --filter "FullyQualifiedName~StapleStripperTests"` passes
  </acceptance_criteria>
  <done>Pure staple-strip + oversize filter + near-precon dedup, applied before ratios, fully tested.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: FolderWeighting — graded weights + effective-sample computation</name>
  <read_first>
    - DeckFlow.Core/Knowledge/MeasuredStyleExtraction/CreatorDeckSample.cs (FolderId/FolderWeight fields)
    - DeckFlow.Core/Knowledge/MeasuredStyleExtraction/MeasuredStyleInputs.cs (WeightsUncurated flag)
    - 95-CONTEXT.md D-04 (Snail map Current/Secondary=1.0, Budget+In-consideration=0.25-0.5, Other=0.5; default 1.0 + weights-uncurated flag) + D-10 (effective sample = fractional double)
  </read_first>
  <behavior>
    - Given a curated folder-id→weight map, each sample's FolderWeight is set from its FolderId (missing id → default 1.0)
    - When WeightsUncurated is true, all weights default to 1.0 (nothing silently dropped)
    - The folder-weighted effective sample = sum of per-deck weights (a fractional double, e.g. 8.5), distinct from the raw integer deck count
  </behavior>
  <action>Create `FolderWeighting.cs` as a `public static class`. Provide `ApplyWeights(IReadOnlyList<CreatorDeckSample>, IReadOnlyDictionary<int,double> folderWeights, bool weightsUncurated) → IReadOnlyList<CreatorDeckSample>` that returns samples with `FolderWeight` populated from `folderWeights[FolderId]` (default 1.0 when the id is absent or `weightsUncurated` is true). Provide `EffectiveSampleSize(IReadOnlyList<CreatorDeckSample>) → double` returning the sum of FolderWeights (the D-10 fractional effective sample) and `RawDeckCount(IReadOnlyList<CreatorDeckSample>) → int` returning the count (feeds `MeasuredMetric.NumDecks`). Pure, total. Preserve LF line endings.</action>
  <verify>
    <automated>dotnet test DeckFlow.Core.Tests --filter "FullyQualifiedName~FolderWeightingTests"</automated>
  </verify>
  <acceptance_criteria>
    - Tests prove: curated map applied by folder id; missing id → 1.0; WeightsUncurated=true → all 1.0; EffectiveSampleSize returns a fractional sum (e.g. 3 decks at 1.0/0.5/1.0 → 2.5) distinct from RawDeckCount (3)
    - `dotnet test DeckFlow.Core.Tests --filter "FullyQualifiedName~FolderWeightingTests"` passes
  </acceptance_criteria>
  <done>Graded folder weighting + fractional effective-sample computation, pure and tested.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host → pure extraction | Web tier hands plain in-memory deck+category data (external-origin, pre-resolved) to pure Core functions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-95-04-01 | DoS (quality/contamination) | >105-card maybeboard-contaminated decks | mitigate | FilterOversized drops CardCount > 105 before any frequency/ratio math (CS-04c) |
| T-95-04-02 | Tampering (skewed profile) | near-precon duplicate decks | mitigate | FlagNearPrecons marks >70%-overlap decks so downstream weighting can discount them |
| T-95-04-03 | Elevation (host boundary leak) | pure extraction layer | mitigate | Namespace has zero HttpClient/Web references (D-11) — enforced by acceptance grep in Task 1 |
| T-95-04-SC | Tampering | package installs | mitigate | No new packages this plan |
</threat_model>

<verification>
- `dotnet build DeckFlow.Core/DeckFlow.Core.csproj` clean
- `dotnet test DeckFlow.Core.Tests --filter "FullyQualifiedName~StapleStripper|FullyQualifiedName~FolderWeighting"` green
- grep proof: MeasuredStyleExtraction namespace has no HttpClient/Web dependency
</verification>

<success_criteria>
- Narrow host-agnostic extraction contract exists (D-11)
- Hybrid staple-strip (curated ∪ >60%) applied before ratios; >105 filter + near-precon dedup (CS-05, CS-04c)
- Graded folder weighting + fractional effective sample (CS-04d, D-10)
</success_criteria>

<output>
Create `.planning/phases/95-measured-style-extractor/95-04-SUMMARY.md` when done.
</output>
