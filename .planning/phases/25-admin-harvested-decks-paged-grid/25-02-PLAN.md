---
phase: 25-admin-harvested-decks-paged-grid
plan: 02
type: execute
wave: 2
depends_on: ["25-01"]
files_modified:
  - DeckFlow.Web/Models/Admin/AdminHarvestViewModel.cs
  - DeckFlow.Web/Controllers/Admin/AdminHarvestController.cs
  - DeckFlow.Web/Views/AdminHarvest/Index.cshtml
  - DeckFlow.Web.Tests/AdminHarvestControllerTests.cs
autonomous: false
requirements: [AHD-01]

must_haves:
  truths:
    - "Admin /Admin/Harvest lists ALL processed decks via a server-side paged grid, not the top-10 list"
    - "Grid shows total count and Page X of Y with working Prev/Next navigation"
    - "page query param is clamped to [1, DeckTotalPages] so OFFSET is never negative and never an absurd full-table scan"
    - "Grid reuses the Phase 18 .admin-table-scroll + .admin-table overflow pattern and is usable at >=320px (zero new CSS)"
    - "The standalone Top 10 Commanders <h3>+<ul> is removed from the view"
  artifacts:
    - path: "DeckFlow.Web/Models/Admin/AdminHarvestViewModel.cs"
      provides: "HarvestedDecks + DeckPage + DeckPageSize + DeckTotalCount + computed DeckTotalPages"
      contains: "DeckTotalPages"
    - path: "DeckFlow.Web/Controllers/Admin/AdminHarvestController.cs"
      provides: "Index(int page) fetching paged decks + total count"
      contains: "GetPagedProcessedDecksAsync"
    - path: "DeckFlow.Web/Views/AdminHarvest/Index.cshtml"
      provides: "Harvested Decks panel (admin-table-scroll table + admin-feedback-pagination nav); top-10 ul removed"
      contains: "admin-table-scroll"
    - path: "DeckFlow.Web.Tests/AdminHarvestControllerTests.cs"
      provides: "Index page-clamp test (page=999999 -> DeckTotalPages)"
      contains: "DeckTotalPages"
  key_links:
    - from: "DeckFlow.Web/Controllers/Admin/AdminHarvestController.cs"
      to: "ICategoryKnowledgeStore.GetPagedProcessedDecksAsync"
      via: "controller calls _categoryStore.GetPagedProcessedDecksAsync(page, pageSize)"
      pattern: "_categoryStore\\.GetPagedProcessedDecksAsync"
    - from: "DeckFlow.Web/Views/AdminHarvest/Index.cshtml"
      to: "AdminHarvestViewModel.HarvestedDecks"
      via: "foreach over Model.HarvestedDecks rendering table rows"
      pattern: "Model\\.HarvestedDecks"
---

<objective>
Wire the Phase 25 paged harvested-decks grid into the admin UI: extend `AdminHarvestViewModel`, add the `int page` param + paged fetch to `AdminHarvestController.Index`, and replace the standalone Top 10 Commanders list in `AdminHarvest/Index.cshtml` with a server-side paged "Harvested Decks" panel. Reuses the Phase 18 responsive admin table CSS (`.admin-table-scroll` + `.admin-table`) and the existing `.admin-feedback-pagination` nav — zero new CSS.

Purpose: AHD-01 requires the admin harvested-decks view to show ALL decks with server-side paging + total count, usable at ≥320px, inheriting the Phase 18 admin shell.
Output: Updated view model, controller action, Razor view with the new grid + Prev/Next nav and the top-10 `<ul>` removed, plus a controller test asserting the upper page clamp (T-25-02 DoS mitigation).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/25-admin-harvested-decks-paged-grid/25-RESEARCH.md
@.planning/phases/25-admin-harvested-decks-paged-grid/25-01-PLAN.md

<interfaces>
<!-- Contracts. New member is created in 25-01 (wave 1). Existing shapes verified in codebase. -->

New store member available after 25-01:
```
Task<IReadOnlyList<HarvestedDeckRow>> GetPagedProcessedDecksAsync(int page, int pageSize, CancellationToken ct = default);
// HarvestedDeckRow(string DeckId, string? CommanderName, string InsertedUtc, string? LastCheckedUtc) — namespace DeckFlow.Web.Services.Harvest
```
Existing total-count member reused for pagination math:
```
Task<int> GetTotalProcessedDeckCountAsync(CancellationToken ct = default);
```

Existing controller shape (DeckFlow.Web/Controllers/Admin/AdminHarvestController.cs):
- `_categoryStore` is the injected `ICategoryKnowledgeStore` field (already present, line 29).
- AdminHarvestController has NINE ctor dependencies: IArchidektCacheJobService, IHarvestRunStore, IHarvestScheduleStore, IHarvestScheduleCache, IHarvestStatsAggregator, IArchidektDeckImporter, ICategoryKnowledgeStore, IMemoryCache, ILogger<AdminHarvestController>. The controller test (Task 1) must supply minimal inline stubs for these; only FakeCategoryKnowledgeStore exists today (TestDoubles/). See the test note in Task 1.
- Current action: `[HttpGet("")] public async Task<IActionResult> Index(CancellationToken cancellationToken)`. It builds `AdminHarvestViewModel { ActiveRun, RecentRuns, Schedule, LastBanner, Stats }` and returns View(viewModel).

Existing view model (DeckFlow.Web/Models/Admin/AdminHarvestViewModel.cs) is a `sealed record` with `{ get; init; }` props and `static readonly int[] AllowedDurationSeconds`. Add the new fields there.

Canonical paging analog (DeckFlow.Web/Controllers/Admin/AdminFeedbackController.cs):
- `page = Math.Max(page, 1); const int pageSize = 50;`
- view model computed: `public int TotalPages => (int)Math.Ceiling((double)Math.Max(TotalCount, 1) / Math.Max(PageSize, 1));`

Controller-test analog (DeckFlow.Web.Tests/AdminHarvestControllerTests.cs does NOT exist yet — create it; AdminFeedbackControllerTests.cs shows the Build()/TempData/StubTempDataProvider pattern for a single-dependency controller).

Pagination nav analog (DeckFlow.Web/Views/AdminFeedback/Index.cshtml lines 86-96): `<nav class="admin-feedback-pagination">` with asp-route-page Prev / `<span>Page X of Y</span>` / Next.

CSS already defined (DeckFlow.Web/wwwroot/css/admin-common.css — DO NOT edit):
- `.admin-shell .admin-table-scroll { overflow-x: auto; ... }` (line 319)
- `.admin-shell .admin-feedback-pagination { ... }` (line 253) and its anchors carry `min-height/min-width: 44px` (line 296) — so reusing this nav class meets the WCAG 2.5.5 touch floor with no CSS change.
- `.sr-only` clip utility (line 77); `.admin-shell .admin-harvest__panel` (line 272).

Existing AdminHarvest/Index.cshtml top-10 block to REMOVE (lines 187-203): the `@if (Model.Stats is not null) { <h3>Top 10 Commanders</h3> ... <ul> ... </ul> }` block.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: View model + controller paged fetch with page clamp + clamp test</name>
  <files>DeckFlow.Web/Models/Admin/AdminHarvestViewModel.cs, DeckFlow.Web/Controllers/Admin/AdminHarvestController.cs, DeckFlow.Web.Tests/AdminHarvestControllerTests.cs</files>
  <read_first>
    - DeckFlow.Web/Models/Admin/AdminHarvestViewModel.cs (full file: sealed record, { get; init; } props, AllowedDurationSeconds static)
    - DeckFlow.Web/Controllers/Admin/AdminHarvestController.cs (Index action lines 65-95; ctor + 9 dependency fields lines 23-60; _categoryStore field line 29)
    - DeckFlow.Web/Controllers/Admin/AdminFeedbackController.cs (Index paging analog: Math.Max clamp + const pageSize + view model assembly)
    - DeckFlow.Web/Models/Admin/AdminFeedbackListViewModel.cs (TotalPages computed property shape)
    - DeckFlow.Web.Tests/AdminFeedbackControllerTests.cs (Build()/TempData/StubTempDataProvider test scaffold to mirror)
    - DeckFlow.Web.Tests/TestDoubles/FakeCategoryKnowledgeStore.cs (already implements GetPagedProcessedDecksAsync after 25-01; has PagedDecksResult + override the count via the existing GetTotalProcessedDeckCountAsync stub)
  </read_first>
  <behavior>
    - Index(page: 999999) when the store reports 3 total decks (DeckTotalPages = 1 at pageSize 50) clamps page to DeckTotalPages and the returned view model's DeckPage == DeckTotalPages (== 1), never 999999
    - Index(page: 0) clamps DeckPage to 1 (lower clamp)
    - The controller passes the clamped page (not the raw query value) into _categoryStore.GetPagedProcessedDecksAsync
  </behavior>
  <action>
    In AdminHarvestViewModel.cs add (preserving `{ get; init; }`, no `{ get; }` mutation — R-6):
    - `public const int DefaultDeckPageSize = 50;` (mirrors the AllowedDurationSeconds static-constant pattern for testability)
    - `public IReadOnlyList<HarvestedDeckRow> HarvestedDecks { get; init; } = Array.Empty<HarvestedDeckRow>();`
    - `public int DeckPage { get; init; } = 1;`
    - `public int DeckPageSize { get; init; } = DefaultDeckPageSize;`
    - `public int DeckTotalCount { get; init; }`
    - `public int DeckTotalPages => (int)Math.Ceiling((double)Math.Max(DeckTotalCount, 1) / Math.Max(DeckPageSize, 1));`
    Add `using DeckFlow.Web.Services.Harvest;` if needed (the file already imports it for HarvestStatsPayload). Add an XML `<summary>` note to the new computed property per project doc-comment convention.

    In AdminHarvestController.cs change the Index signature to `[HttpGet("")] public async Task<IActionResult> Index(int page = 1, CancellationToken cancellationToken = default)`. Keep the existing activeRun/recentRuns/stats fetches. After the stats try/catch, add: `page = Math.Max(page, 1);` then `const int deckPageSize = AdminHarvestViewModel.DefaultDeckPageSize;` then fetch `var deckTotal = await _categoryStore.GetTotalProcessedDeckCountAsync(cancellationToken).ConfigureAwait(false);`, compute `var deckTotalPages = (int)Math.Ceiling((double)Math.Max(deckTotal, 1) / Math.Max(deckPageSize, 1)); page = Math.Min(page, deckTotalPages);` (upper clamp closes T-25-02 large-OFFSET DoS), then `var pagedDecks = await _categoryStore.GetPagedProcessedDecksAsync(page, deckPageSize, cancellationToken).ConfigureAwait(false);`. Add to the `AdminHarvestViewModel` initializer: `HarvestedDecks = pagedDecks, DeckPage = page, DeckPageSize = deckPageSize, DeckTotalCount = deckTotal`. Do not remove the existing `Stats = stats` assignment (the stats payload still feeds the Stats panel; only the view's top-10 list rendering is removed in Task 2). Touch only the Index method (R-6).

    Create DeckFlow.Web.Tests/AdminHarvestControllerTests.cs. Mirror the AdminFeedbackControllerTests `Build()` + TempData scaffold. Because AdminHarvestController takes 9 dependencies, add a small `Build(ICategoryKnowledgeStore store)` helper that constructs the controller with: the supplied `store`, a `new MemoryCache(new MemoryCacheOptions())`, `NullLogger<AdminHarvestController>.Instance`, and minimal inline private stub classes for IArchidektCacheJobService, IHarvestRunStore, IHarvestScheduleStore, IHarvestScheduleCache, IHarvestStatsAggregator, IArchidektDeckImporter that return empty/default values (GetActiveAsync -> null, GetRecentAsync -> empty list, Snapshot -> a default schedule snapshot, GetAsync -> null stats). Keep these stubs minimal — only the members Index() actually calls need real returns; the rest may `throw new NotImplementedException()`. Extend FakeCategoryKnowledgeStore usage: set `PagedDecksResult` to a 3-row list and arrange `GetTotalProcessedDeckCountAsync` to return 3 (add a settable `TotalProcessedDeckCount` field to FakeCategoryKnowledgeStore if its stub currently returns a constant — touch only that one member, R-6).

    Add three `[Fact]` tests: (1) Index(999999) -> result is a ViewResult whose model is AdminHarvestViewModel with DeckPage == model.DeckTotalPages (asserts the upper clamp); (2) Index(0) -> model.DeckPage == 1 (lower clamp); (3) capture the page passed to GetPagedProcessedDecksAsync via a recording field on the Fake and assert it equals the clamped value, not 999999.
  </action>
  <verify>
    <automated>"/mnt/c/Program Files/dotnet/dotnet.exe" build DeckFlow.sln -c Debug 2>&1 | tail -8; "/mnt/c/Program Files/dotnet/dotnet.exe" test DeckFlow.Web.Tests/DeckFlow.Web.Tests.csproj -v minimal --filter "FullyQualifiedName~AdminHarvestControllerTests" 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - AdminHarvestViewModel.cs contains `HarvestedDecks`, `DeckPage`, `DeckPageSize`, `DeckTotalCount`, and a computed `DeckTotalPages`, all using `{ get; init; }` (no `{ get; }`)
    - AdminHarvestViewModel.cs contains `DefaultDeckPageSize` constant = 50
    - AdminHarvestController.Index accepts an `int page` parameter (default 1)
    - AdminHarvestController.Index clamps page with `Math.Max(page, 1)` AND `Math.Min(page, deckTotalPages)` before fetching
    - AdminHarvestController.Index calls `_categoryStore.GetPagedProcessedDecksAsync` and `_categoryStore.GetTotalProcessedDeckCountAsync` and assigns the new view-model fields
    - AdminHarvestControllerTests.cs exists with a test asserting Index(999999) clamps DeckPage to DeckTotalPages (T-25-02 mitigation), a test for the page=0 lower clamp, and a test that the clamped page (not 999999) reaches GetPagedProcessedDecksAsync
    - `dotnet build DeckFlow.sln -c Debug` exits 0 with no new warnings; the AdminHarvestControllerTests filter run shows Failed: 0 (or build clean + queued for CI per CLAUDE.md WSL VSTest note)
  </acceptance_criteria>
  <done>View model carries paging fields + computed total pages; controller fetches the page slice with lower+upper page clamps and feeds the view model; a controller test proves the page=999999 upper clamp (T-25-02); build clean.</done>
</task>

<task type="auto">
  <name>Task 2: Replace top-10 list with paged Harvested Decks grid in the view</name>
  <files>DeckFlow.Web/Views/AdminHarvest/Index.cshtml</files>
  <read_first>
    - DeckFlow.Web/Views/AdminHarvest/Index.cshtml (full file; existing admin-table-scroll table pattern for RecentRuns ~lines 124-185; top-10 block to remove at lines 187-203; @section Scripts at the bottom)
    - DeckFlow.Web/Views/AdminFeedback/Index.cshtml (pagination nav markup lines 86-96 — asp-action Index + asp-route-page)
    - DeckFlow.Web/wwwroot/css/admin-common.css (admin-table-scroll line 319, admin-feedback-pagination line 253 + 44px anchors line 296, sr-only line 77, admin-harvest__panel line 272 — reference only, DO NOT EDIT)
  </read_first>
  <action>
    Remove the standalone Top 10 Commanders block (the `@if (Model.Stats is not null) { <h3>Top 10 Commanders</h3> ... <ul>...</ul> }` at lines 187-203). Do NOT remove or alter the Stats panel summary that consumes other `Model.Stats` fields elsewhere in the view; remove only the top-10 heading + list rendering (per AHD-01 + research Open Question 2 — `GetTopCommandersAsync` stays in BuildAsync; only the Razor `<ul>` goes).

    In its place add a new `<section class="admin-harvest__panel">` containing:
    - `<h2>Harvested Decks</h2>`
    - a total/page line: `<p>@Model.DeckTotalCount total — Page @Model.DeckPage of @Model.DeckTotalPages</p>`
    - empty-state branch: when `Model.HarvestedDecks.Count == 0` render `<p>No harvested decks yet.</p>`
    - else a scroll region matching the RecentRuns pattern: `<div class="admin-table-scroll" role="region" aria-label="Harvested decks - scroll horizontally to see all columns" tabindex="0">` containing `<table class="admin-table">` with a `<caption class="sr-only">` describing the table, a `<thead>` with `<th scope="col">` cells for Deck ID, Commander, Harvested (UTC), Last Checked (UTC), and a `<tbody>` `@foreach (var deck in Model.HarvestedDecks)` rendering `@deck.DeckId`, `@(deck.CommanderName ?? "—")`, `@deck.InsertedUtc`, `@(deck.LastCheckedUtc ?? "—")`.
    - below the scroll region, a `<nav class="admin-feedback-pagination">` mirroring AdminFeedback/Index.cshtml: Prev link `asp-action="Index" asp-route-page="@(Model.DeckPage - 1)"` shown when `Model.DeckPage > 1`, a `<span>Page @Model.DeckPage of @Model.DeckTotalPages</span>`, and a Next link `asp-route-page="@(Model.DeckPage + 1)"` shown when `Model.DeckPage < Model.DeckTotalPages`.

    Use the ASCII hyphen `-` (not an en/em dash) in the aria-label, consistent with the Phase 18 ASCII-hyphen aria-label convention. Reuse `.admin-feedback-pagination` verbatim (its anchors already meet the 44px touch floor — zero CSS change, satisfies the ≥320px usability + WCAG 2.5.5 criteria). Do not add any new CSS class or edit any .css file (R-6 + admin-common.css is the only allowed layout location and it already has what we need). Leave the `@section Scripts` block unchanged.
  </action>
  <verify>
    <automated>"/mnt/c/Program Files/dotnet/dotnet.exe" build DeckFlow.sln -c Debug 2>&1 | tail -8; grep -c 'admin-table-scroll' DeckFlow.Web/Views/AdminHarvest/Index.cshtml; grep -c 'Top 10 Commanders' DeckFlow.Web/Views/AdminHarvest/Index.cshtml</automated>
  </verify>
  <acceptance_criteria>
    - Index.cshtml contains a "Harvested Decks" `<section class="admin-harvest__panel">` with `Model.DeckTotalCount`, `Model.DeckPage`, `Model.DeckTotalPages` rendered
    - Index.cshtml contains a `<div class="admin-table-scroll" ... tabindex="0">` wrapping a `<table class="admin-table">` with `<th scope="col">` for Deck ID, Commander, Harvested, Last Checked
    - Index.cshtml iterates `@foreach (var deck in Model.HarvestedDecks)` rendering DeckId/CommanderName/InsertedUtc/LastCheckedUtc
    - Index.cshtml contains a `<nav class="admin-feedback-pagination">` with asp-route-page Prev/Next gated on DeckPage vs DeckTotalPages
    - `grep -c 'Top 10 Commanders' DeckFlow.Web/Views/AdminHarvest/Index.cshtml` returns 0 (top-10 list removed)
    - No `.css` file is modified by this task (CSS reused, not added)
    - `dotnet build DeckFlow.sln -c Debug` exits 0 with no new warnings; Razor compiles
  </acceptance_criteria>
  <done>The view renders a server-side paged Harvested Decks grid with Prev/Next nav reusing Phase 18 + existing pagination CSS; the standalone top-10 list is gone; build clean.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verify paged grid on /Admin/Harvest (paging, clamps, 320px usability, theme bleed)</name>
  <what-built>Server-side paged "Harvested Decks" grid on /Admin/Harvest replacing the top-10 list. The grid reuses the Phase 18 `.admin-table-scroll` overflow pattern and `.admin-feedback-pagination` nav, with Prev/Next paging and a total count.</what-built>
  <how-to-verify>
    1. Start the web app locally (operator launches it — do NOT auto-start per project rule), e.g. `dotnet run --project DeckFlow.Web`, and sign in to `/Admin/Harvest` via BasicAuth.
    2. Confirm the page shows a "Harvested Decks" panel with "<N> total — Page 1 of <M>" and a table of decks (Deck ID, Commander, Harvested, Last Checked). The old "Top 10 Commanders" list is gone.
    3. Click Next, confirm the URL becomes `?page=2` and a different set of rows loads; click Prev to return.
    4. Manually set `?page=0` and `?page=999999` in the URL — confirm no error: page clamps to 1 and to the last page respectively.
    5. Narrow the browser to 320px width (DevTools device toolbar) — confirm the table scrolls horizontally inside its region and Prev/Next links remain ≥44px tappable; confirm no visual bleed into non-admin pages.
    6. (If a Render/Postgres preview is available) confirm cold-load of `/Admin/Harvest` feels snappier than before and the stats counters still render.
  </how-to-verify>
  <action>HUMAN VERIFICATION — no code change. Operator launches the app and confirms the steps in <how-to-verify>. This task gates plan completion.</action>
  <files>(none — verification only)</files>
  <verify><human-check>Operator confirms paged grid, page clamps, 320px usability, and zero non-admin theme bleed.</human-check></verify>
  <done>Operator types "approved"; any reported issues are fixed in 25-02 Tasks 1-2 before completion.</done>
  <resume-signal>Type "approved" or describe issues (e.g. paging off-by-one, dash rendering, overflow not scrolling).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→/Admin/Harvest | Admin-only (BasicAuth-gated). `page` query-string param is the untrusted input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-02 | Denial of Service | `page=999999` → huge OFFSET full scan | mitigate | Controller clamps `page = Math.Min(Math.Max(page, 1), DeckTotalPages)` before the query, so OFFSET is bounded by the real row count. A controller test (Task 1) asserts page=999999 clamps to DeckTotalPages. |
| T-25-05 | Tampering | negative/zero `page` → negative OFFSET SQL error | mitigate | `Math.Max(page, 1)` floor in the controller (mirrors AdminFeedbackController); covered by the page=0 lower-clamp test (Task 1). |
| T-25-06 | Elevation of Privilege | new unauthenticated surface | accept | No new route; Index stays under the existing `/Admin/*` BasicAuth branch — no new surface added. |
| T-25-07 | Information Disclosure | deck IDs / commander names rendered | accept | Already-stored harvested public deck metadata, shown only to authenticated admins; no PII. |
| T-25-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase. |
</threat_model>

<verification>
- `dotnet build DeckFlow.sln -c Debug` exits 0, no new warnings; Razor view compiles.
- `dotnet test DeckFlow.Web.Tests --filter AdminHarvestControllerTests` passes the page-clamp tests (upper + lower).
- Grep gates: `grep -c 'admin-table-scroll' DeckFlow.Web/Views/AdminHarvest/Index.cshtml` ≥ 1; `grep -c 'Top 10 Commanders' DeckFlow.Web/Views/AdminHarvest/Index.cshtml` == 0; `grep -c 'GetPagedProcessedDecksAsync' DeckFlow.Web/Controllers/Admin/AdminHarvestController.cs` ≥ 1.
- No `.css` file in `git diff --name-only` for this plan (CSS reused, not edited).
- Human-verify checkpoint confirms paging, page clamping, ≥320px usability, and zero non-admin theme bleed.
</verification>

<success_criteria>
- ROADMAP SC1 (lists ALL decks, server-side paging, total count + nav visible) satisfied.
- ROADMAP SC3 (reuses Phase 18 responsive table/card patterns; usable at ≥320px) satisfied via `.admin-table-scroll` + existing pagination CSS, confirmed at checkpoint.
- ROADMAP SC4 (Failed: 0; R-6 touch-only) preserved: build clean, no CSS edits, only Index method + view block touched.
- AHD-01 fully delivered (data layer in 25-01, UI here).
</success_criteria>

<output>
Create `.planning/phases/25-admin-harvested-decks-paged-grid/25-02-SUMMARY.md` when done.
</output>
