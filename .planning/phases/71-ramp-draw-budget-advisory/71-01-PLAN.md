---
phase: 71-ramp-draw-budget-advisory
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - DeckFlow.Core/Manabase/ManabaseClassifier.cs
  - DeckFlow.Core/Manabase/ManabaseModels.cs
  - DeckFlow.Core/Manabase/ManabaseRampDrawBudget.cs
  - DeckFlow.Core.Tests/Manabase/ManabaseRampDrawBucketTests.cs
  - DeckFlow.Core.Tests/Manabase/ManabaseRampDrawBudgetTests.cs
autonomous: true
requirements: [PLV-BUDGET]
must_haves:
  truths:
    - "Full-deck ramp and draw slot counts are computed from existing predicates, with overlap cards counted 0.5 to each bucket"
    - "The budget draw classification catches wheels (draw N cards) and repeatable draw (Mystic Remora) per CONTEXT"
    - "A threshold-derived 24-slot ramp/draw target is produced (commander MV, else a curve proxy)"
    - "IsRampOrDraw is left untouched (byte-identical) so the land-target credit cannot drift"
  artifacts:
    - path: "DeckFlow.Core/Manabase/ManabaseRampDrawBudget.cs"
      provides: "ManabaseRampDrawBudget record + ManabaseRampDrawBudgetCalculator"
      contains: "ManabaseRampDrawBudgetCalculator"
    - path: "DeckFlow.Core.Tests/Manabase/ManabaseRampDrawBudgetTests.cs"
      provides: "Interpolation + threshold + nudge coverage"
  key_links:
    - from: "ManabaseRampDrawBudgetCalculator.Calculate"
      to: "ManabaseDeck.RampPieceCount / DrawPieceCount"
      via: "reads classifier-computed bucket counts"
      pattern: "RampPieceCount"
---

<objective>
Build the pure-logic ramp/draw slot-budget engine in DeckFlow.Core: classify every nonland
card into ramp / draw / both buckets (reusing the classifier's existing helpers via NEW budget-only
predicates), derive a threshold (commander MV, fallback curve proxy), and interpolate the community
24-slot target.

Purpose: Deliverable C foundation. This is the one net-new piece of math; isolating it in Core
keeps it deterministic and fully unit-testable, with no LLM call and no Scryfall calls.
Output: `ManabaseRampDrawBudget` record, `ManabaseRampDrawBudgetCalculator`, and additive bucket
counts on `ManabaseDeck`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/71-ramp-draw-budget-advisory/71-CONTEXT.md
@.planning/phases/71-ramp-draw-budget-advisory/71-SPEC.md
@.planning/captures/ramp-draw-unit-of-value-theory.md

<interfaces>
<!-- Existing helpers the budget predicates must reuse — do NOT invent new ones, and do NOT modify IsRampOrDraw. -->

From DeckFlow.Core/Manabase/ManabaseClassifier.cs (all private static):
- IsRampOrDraw(CardFact) at ~L620 — historic broad predicate; ramp half = ("Search your library
  for"+"land") || "Add " || "create a Treasure"; draw half = "draw a card" || "draw two cards".
  LEAVE UNTOUCHED: it is called at ManabaseClassifier.cs:172 on the ramp-credit-v2=OFF land-target path
  that the Core suite exercises; widening it would drift the land target.
- IsRockOrDork(CardFact), IsLandRampToBattlefield(CardFact), ProducesMana(CardFact),
  IsType(typeLine, type), IsLandType(typeLine).
- Classify(...) iterates `foreach (CardFact card in cards)` and already multiplies by card.Quantity.

From DeckFlow.Core/Manabase/ManabaseModels.cs:
- record ManabaseDeck { IReadOnlyList<ManaSource> Sources; IReadOnlyList<SpellRequirement> Spells;
  double AverageManaValue; int CommanderCount; ... } — add new additive fields here.
- record SpellRequirement { int ManaValue; bool IsCommander; bool IsManaSource; ... }
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Compute full-deck ramp/draw bucket counts in the classifier</name>
  <files>DeckFlow.Core/Manabase/ManabaseClassifier.cs, DeckFlow.Core/Manabase/ManabaseModels.cs, DeckFlow.Core.Tests/Manabase/ManabaseRampDrawBucketTests.cs</files>
  <behavior>
    - CRITICAL: do NOT modify `IsRampOrDraw` (~L620) and do NOT fold any broadened logic into it. It is
      called at ManabaseClassifier.cs:172 on the ramp-credit-v2=OFF land-target path, which the Core
      suite exercises; widening it would shift the ≤2-MV land target for IsRockOrDork-true /
      no-"Add"-text fixtures. Leave it byte-identical.
    - Add SEPARATE budget-only predicates used ONLY by the new bucket counts (never by the land-target
      credit), reusing the existing classifier helpers:
      • `IsRampPieceForBudget(CardFact)` = ("Search your library for"+"land") || "Add " ||
        "create a Treasure" || `IsRockOrDork(card)` || `IsLandRampToBattlefield(card)`.
      • `IsDrawPieceForBudget(CardFact)` = WIDER than the historic draw half (which only matches
        "draw a card" / "draw two cards"), because CONTEXT names overlap cards "cantrip rocks, Mystic
        Remora, wheels". Count a card as a draw piece when its (lower-cased) oracle text matches
        "draw a card" OR a wheel-style "draw{s}? <qty> card{s}?" where <qty> is a number word
        (one/two/.../seven) OR a digit OR "x" (e.g. "draw two cards", "draws seven cards",
        "each player draws seven cards", "draw X cards"). Mystic-Remora-style repeatable draw ("...you
        may draw a card") is caught by the "draw a card" clause. Implement with a single compiled regex
        (e.g. `draws?\s+(a|one|two|three|four|five|six|seven|x|\d+)\s+cards?` plus the literal
        "draw a card"). Budget-only — do NOT touch IsRampOrDraw or the L620 draw half.
    - Bucket counts over ALL nonland cards (skip `frontIsLand`), multiplied by card.Quantity:
      rampPieces += qty when IsRampPieceForBudget; drawPieces += qty when IsDrawPieceForBudget;
      bothPieces += qty when both. Expose on ManabaseDeck: RampPieceCount = rampPieces − 0.5*bothPieces
      (double), DrawPieceCount = drawPieces − 0.5*bothPieces (double), RampDrawBothCount = bothPieces (int).
    - Byte-identity: because `IsRampOrDraw` is untouched, existing ManaRampCreditTests /
      ManabaseClassifierTests / land-target tests pass unchanged. Add a regression assertion that a
      ≤2-MV IsRockOrDork-true card with NO "Add"/"Search…land"/"Treasure" text does NOT change the land
      target (proves the budget predicate did not leak into the credit path).
    - Overlap + draw-coverage fixtures: a cantrip rock (artifact, "Add {C}" + "draw a card") → 0.5 ramp
      + 0.5 draw, RampDrawBothCount == 1. A pure rock (Sol Ring) → 1.0 ramp / 0.0 draw / 0 both. A pure
      draw spell (Divination, "draw two cards") → 0.0 ramp / 1.0 draw. A WHEEL ("Each player draws seven
      cards", e.g. Windfall/Wheel of Fortune) → counted as 1.0 draw (regex wheel clause). A
      MYSTIC-REMORA-style repeatable-draw enchantment ("...you may draw a card") → counted as 1.0 draw.
  </behavior>
  <action>Add three additive properties (RampPieceCount, DrawPieceCount, RampDrawBothCount, all with
    safe defaults of 0) to the `ManabaseDeck` record in ManabaseModels.cs with xmldoc explaining the
    overlap-0.5 convention and that they are advisory-only (never feed the land target or any verdict).
    In ManabaseClassifier.Classify, accumulate the three buckets inside the existing nonland branch of
    the `foreach (CardFact card in cards)` loop using the new `IsRampPieceForBudget` /
    `IsDrawPieceForBudget` private statics (with the widened, budget-only draw regex above), and set the
    fields in the returned `ManabaseDeck`. Leave `IsRampOrDraw` and the ManabaseClassifier.cs:172
    ramp-credit call EXACTLY as they are — the budget predicates are additive, reused-helper wrappers,
    not a refactor of the historic predicate. Do NOT change land-target math, color counts, or any
    existing field.</action>
  <verify>
    <automated>cd /mnt/c/users/chrislunt/source/personal/deckflow && /mnt/c/Program\ Files/dotnet/dotnet.exe build DeckFlow.Core/DeckFlow.Core.csproj -clp:ErrorsOnly</automated>
  </verify>
  <done>ManabaseDeck carries the three bucket fields; overlap cards split 0.5/0.5; wheels and
    Mystic-Remora-style repeatable draw are counted as draw; IsRampOrDraw is unchanged and all
    pre-existing classifier/ramp-credit/land-target tests still pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ManabaseRampDrawBudget record + threshold/interpolation calculator</name>
  <files>DeckFlow.Core/Manabase/ManabaseRampDrawBudget.cs, DeckFlow.Core.Tests/Manabase/ManabaseRampDrawBudgetTests.cs</files>
  <behavior>
    - Threshold: when the deck has ≥1 commander spell (Spells where IsCommander), Threshold = the
      MAX ManaValue among commander spells, ThresholdSource = CommanderManaValue. Otherwise Threshold =
      the 75th-percentile ManaValue of nonland spells (deck.Spells, excluding IsManaSource rows),
      ThresholdSource = CurveProxy. State the proxy in the record so copy can disclose it.
    - Target interpolation (total 24, anchors MV≤2 → 8 ramp, MV4 → 12 ramp, MV≥6 → 14 ramp):
      T≤2 → 8; 2<T≤4 → 8 + 2*(T−2); 4<T≤6 → 12 + 1*(T−4); T≥6 → 14. TargetRamp = round-to-nearest int
      (MidpointRounding.AwayFromZero), TargetDraw = 24 − TargetRamp. Verify TargetRamp at T=2→8,
      T=3→10, T=4→12, T=5→13, T=6→14, and a between value T=3.5→11.
    - Nudge (deadband ±2 on ramp): rampDelta = RampPieceCount − TargetRamp. |rampDelta| ≤ 2 →
      IsBalanced true. rampDelta < −2 → IsRampLight true, RampShort = ceil(−rampDelta). rampDelta > 2 →
      IsRampHeavy true (could shift slots to draw). Mirror DrawShort for draw vs TargetDraw.
    - Fallback fixture: a commander-less deck of nonland spells with MVs {1,1,2,2,3,3,4,6} → 75th pct
      proxy resolves to ManaValue 4 (CurveProxy) → TargetRamp 12 / TargetDraw 12.
  </behavior>
  <action>Create `ManabaseRampDrawBudget` as a sealed record (RampCount double, DrawCount double,
    OverlapCount int, Threshold double, ThresholdSource enum {CommanderManaValue, CurveProxy},
    TargetRamp int, TargetDraw int, IsBalanced bool, IsRampLight bool, IsRampHeavy bool,
    int RampShort, IsDrawLight bool, int DrawShort) plus a `ManabaseRampDrawBudgetCalculator` static
    with `Calculate(ManabaseDeck deck)`. Read RampPieceCount/DrawPieceCount/RampDrawBothCount from the
    deck (Task 1). Implement the threshold, interpolation, and nudge exactly as in the behavior block.
    Add xmldoc labelling the split "community heuristic, not Karsten math" and noting the threshold is
    a single-point proxy. Pure — no I/O, no flag reads. Define the percentile helper inline (nearest-
    rank on sorted nonland MVs).</action>
  <verify>
    <automated>cd /mnt/c/users/chrislunt/source/personal/deckflow && cmd.exe /c "dotnet test DeckFlow.Core.Tests\DeckFlow.Core.Tests.csproj --filter FullyQualifiedName~ManabaseRampDraw" 2>&1 | tail -20</automated>
  </verify>
  <done>Calculator returns correct counts, threshold (commander + fallback), interpolated 24-slot
    target at every anchor and a between value, and the balanced/light/heavy nudge with the ±2
    deadband — all green in xUnit.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
Pure DeckFlow.Core CPU logic; no new trust boundary (no I/O, no network, no untrusted input — it
consumes an already-validated `ManabaseDeck`).

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-71-01 | Tampering | New Core math feeding advisory copy | accept | Advisory-only; cannot change land/color/health verdict. Determinism guarded by xUnit anchors. |
| T-71-SC | Tampering | npm/pip/cargo installs | n/a | No package installs in this plan (project rule forbids new deps). |
</threat_model>

<verification>
- `dotnet build DeckFlow.Core` clean (no new warnings).
- `dotnet test` filter `ManabaseRampDraw` green; pre-existing manabase Core tests still green
  (including the v2-OFF land-target path — `IsRampOrDraw` untouched).
- Bucket tests cover the wheel and Mystic-Remora-style repeatable-draw fixtures.
- No change to land target, color counts, castability, or health band in any existing test.
</verification>

<success_criteria>
Ramp/draw buckets (with 0.5 overlap, wheels + repeatable draw counted), threshold (commander +
curve-proxy), and the interpolated 24-slot target are computed deterministically in Core and fully
covered by tests, with `IsRampOrDraw` left byte-identical.
</success_criteria>

<output>
Create `.planning/phases/71-ramp-draw-budget-advisory/71-01-SUMMARY.md` when done.
</output>
