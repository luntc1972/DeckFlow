# Phase 07.1 — UI Review

**Audited:** 2026-05-04
**Baseline:** abstract 6-pillar standards (no UI-SPEC.md present)
**Screenshots:** captured from local `http://localhost:5173` in `.planning/ui-reviews/07.1-20260504-123103/`
**Screenshot Note:** captured against the current local working tree; `DeckFlow.Web/wwwroot/css/site-common.css` had an uncommitted theme-picker change during capture.

---

## Pillar Scores

| Pillar | Score | Key Finding |
|--------|-------|-------------|
| 1. Copywriting | 3/4 | Labels are clear, but the flag-off maintenance copy says "Categories tool" while Category Reference remains available. |
| 2. Visuals | 3/4 | The gated entry reuses existing navigation/card hierarchy, but flag-off state leaves no visible explanation in the normal UI. |
| 3. Color | 3/4 | Implementation uses existing theme tokens and classes; Classic theme screenshots look coherent, but no screenshot pass across all guild themes was completed. |
| 4. Typography | 4/4 | New UI text uses existing link, card title, description, and maintenance typography. |
| 5. Spacing | 2/4 | Existing desktop grid/dropdown spacing is preserved, but the 390px screenshot shows header/navigation overflow and the flag-off single-card state can look underfilled. |
| 6. Experience Design | 2/4 | The kill-switch prevents broken navigation, but the OFF state gives users no route from the 503 page to Category Reference or Help. |

**Overall: 17/24**

---

## Top 3 Priority Fixes

1. **Fix mobile header/nav containment** — the `home-mobile.png` capture at 390px shows the theme picker and tool navigation overflowing the viewport — allow the page header to wrap and make the nav rows fit below 390px.
2. **Clarify the flag-off maintenance copy** — users may think all category features are offline even though Category Reference remains available — change the title/message to "AI category suggestions temporarily unavailable" and mention Category Reference remains available.
3. **Add an alternate action to the maintenance page or route-specific copy** — direct visitors to `/suggest-categories` hit a dead-end 503 — include a link to `/commander-categories` or Help in the maintenance view/model path for this gate.

---

## Detailed Findings

### Pillar 1: Copywriting (3/4)

**WARNING:** The route gate copy is accurate but too broad. Both gated actions use title `Category suggestions temporarily unavailable` and message `The Categories tool is offline for maintenance. Please try again shortly.` in `DeckFlow.Web/Controllers/DeckController.cs:89` and `DeckFlow.Web/Controllers/DeckController.cs:746`. Because `Category Reference` remains intentionally visible in `DeckFlow.Web/Views/Shared/_DeckToolTabs.cshtml:51` and `DeckFlow.Web/Views/Deck/Home.cshtml:77`, "The Categories tool" can read as if the whole category area is offline. Prefer "AI category suggestions are offline for maintenance. Category Reference is still available."

**PASS:** The visible navigation/card labels are concrete and task-oriented: `AI Category Suggestions` and `Category Reference` in `DeckFlow.Web/Views/Shared/_DeckToolTabs.cshtml:49` and `:51`, mirrored on the landing page in `DeckFlow.Web/Views/Deck/Home.cshtml:73` and `:78`.

### Pillar 2: Visuals (3/4)

**WARNING:** The gated UI removes the broken entry cleanly, but the normal page offers no visible explanation when the feature is disabled. `_DeckToolTabs.cshtml:47` omits the AI suggestions link entirely and `Home.cshtml:70` omits the hub card entirely. That prevents a broken click, but users who previously used the tool get no in-context signal about why it disappeared.

**PASS:** The feature uses established hierarchy instead of inventing new visual language. Navigation entries use `.tool-nav__link` and the landing card uses `.hub-card`; these styles define clear hover/focus states and hierarchy in `DeckFlow.Web/wwwroot/css/site.css:796` and `DeckFlow.Web/wwwroot/css/site-common.css:161`.

**PASS:** The live desktop screenshots (`home-tall.png` and `suggest-desktop.png`) confirm the Classic theme home page and AI Category Suggestions form render with a clear hierarchy, readable labels, and visible primary actions when the feature is enabled.

### Pillar 3: Color (3/4)

**WARNING:** The screenshot pass covered only the Classic theme. The implementation relies on existing tokenized classes, which is good, but the audit cannot prove contrast and accent balance under each guild theme.

**PASS:** The phase introduced no new hardcoded UI colors. The gated surfaces reuse `.tool-nav__link` and `.hub-card`, which resolve color through theme variables such as `var(--ink)`, `var(--accent)`, `var(--panel)`, and `var(--line)` in `DeckFlow.Web/wwwroot/css/site.css:799`, `:810`, and `DeckFlow.Web/wwwroot/css/site-common.css:166`.

### Pillar 4: Typography (4/4)

**PASS:** New text sits inside existing typography roles. The dropdown link uses `.tool-nav__link` with `font-size: var(--fs-base)` in `DeckFlow.Web/wwwroot/css/site.css:801`; the landing card title and description use `.hub-card__title` and `.hub-card__description` with existing `var(--fs-base)` and `var(--fs-sm)` in `DeckFlow.Web/wwwroot/css/site-common.css:181` and `:187`.

**PASS:** The maintenance state uses the shared maintenance view rather than ad hoc text styling: `DeckFlow.Web/Views/Shared/_MaintenancePage.cshtml:5`.

### Pillar 5: Spacing (2/4)

**WARNING:** When `feature.categories.enabled` is OFF, `Home.cshtml:70` removes the AI suggestions card but leaves the Categories section and grid intact with only `Category Reference`. The grid definition `repeat(auto-fill, minmax(16rem, 1fr))` in `DeckFlow.Web/wwwroot/css/site-common.css:155` is good for multiple cards, but a single remaining card may look visually underfilled on wide screens.

**WARNING:** The `home-mobile.png` screenshot at 390px shows horizontal overflow in the shared header/navigation area: the theme picker is clipped at the right edge and the fourth tool-nav trigger is cut off instead of wrapping into a clean second row. The likely pressure points are the non-wrapping `.page-header` flex row in `DeckFlow.Web/wwwroot/css/site-common.css:17`, the theme picker/select minimum width in `DeckFlow.Web/wwwroot/css/site-common.css:48` and `:66`, and non-wrapping nav triggers in `DeckFlow.Web/wwwroot/css/site.css:742`. Add a small-screen layout that wraps or stacks header controls and lets nav groups fit the viewport.

**PASS:** The implementation did not introduce arbitrary spacing. Dropdown spacing stays on the established `0.55rem 1rem` link padding in `DeckFlow.Web/wwwroot/css/site.css:796`, and cards keep the established `1rem 1.1rem` padding in `DeckFlow.Web/wwwroot/css/site-common.css:161`.

### Pillar 6: Experience Design (2/4)

**WARNING:** The kill-switch protects users from a broken flow but creates a dead-end for direct traffic. A user visiting `/suggest-categories` while the flag is OFF gets a generic maintenance page via `FeatureFlagGateAttribute` and `_MaintenancePage` (`DeckFlow.Web/Infrastructure/FeatureFlagGateAttribute.cs:57`, `DeckFlow.Web/Views/Shared/_MaintenancePage.cshtml:5`) with no action to browse Category Reference, go back home, or understand that only AI suggestions are disabled.

**WARNING:** The flag-off state is operationally correct but not discoverable. The nav entry and landing card are removed (`_DeckToolTabs.cshtml:47`, `Home.cshtml:70`), but no visible disabled state or explanatory copy tells returning users that this is intentional maintenance rather than a missing feature.

**PASS:** The route gate is applied to both GET and POST actions (`DeckFlow.Web/Controllers/DeckController.cs:88` and `:745`), so stale forms cannot continue into the broken backend after the visible entry points disappear.

## Screenshots Reviewed

- `.planning/ui-reviews/07.1-20260504-123103/home-desktop.png`
- `.planning/ui-reviews/07.1-20260504-123103/home-tall.png`
- `.planning/ui-reviews/07.1-20260504-123103/home-mobile.png`
- `.planning/ui-reviews/07.1-20260504-123103/suggest-desktop.png`

## Remediation Applied

- `45a0721 fix(ui): keep theme picker readable` widened the desktop theme picker and added mobile containment rules in `site-mobile.css` so the shared header, theme picker, and tool nav fit narrow screens.

---

## Files Audited

- `DeckFlow.Web/Views/Shared/_DeckToolTabs.cshtml`
- `DeckFlow.Web/Views/Deck/Home.cshtml`
- `DeckFlow.Web/Controllers/DeckController.cs`
- `DeckFlow.Web/Infrastructure/FeatureFlagGateAttribute.cs`
- `DeckFlow.Web/Views/Shared/_MaintenancePage.cshtml`
- `DeckFlow.Web/wwwroot/css/site.css`
- `DeckFlow.Web/wwwroot/css/site-common.css`
- `.planning/phases/07.1-categories-feature-flag-sameorigin-ajax-fix/07.1-01-PLAN.md`
- `.planning/phases/07.1-categories-feature-flag-sameorigin-ajax-fix/07.1-01-SUMMARY.md`
- `.planning/phases/07.1-categories-feature-flag-sameorigin-ajax-fix/07.1-02-PLAN.md`
- `.planning/phases/07.1-categories-feature-flag-sameorigin-ajax-fix/07.1-02-SUMMARY.md`

## Registry Safety

Registry audit skipped: `components.json` is not present, so no shadcn or third-party registry blocks are installed for this phase.
