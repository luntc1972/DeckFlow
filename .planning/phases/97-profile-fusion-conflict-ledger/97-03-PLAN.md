---
phase: 97-profile-fusion-conflict-ledger
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - DeckFlow.Core/Content/IContentVideoStore.cs
  - DeckFlow.Core/Content/ContentVideoStore.cs
  - DeckFlow.Core.Tests/ContentVideoStoreStatedRulesReadTests.cs
autonomous: true
requirements: [CS-16a, CS-20]
must_haves:
  truths:
    - "A creator's persisted stated rules can be read back by source slug as StatedRuleCandidate[], carrying metric, condition, value/min/max, comparator, source_clip, confidence, and video_date."
    - "The read query joins content_stated_rules -> content_videos -> content_sources on source_slug and returns rules ordered deterministically (video_id, sort_order)."
    - "The slug is parameterized (Dapper), never string-concatenated into SQL."
  artifacts:
    - path: "DeckFlow.Core/Content/ContentVideoStore.cs"
      provides: "GetStatedRulesBySourceSlugAsync implementation (3-table Dapper join, dialect-guarded)"
      contains: "GetStatedRulesBySourceSlugAsync"
    - path: "DeckFlow.Core/Content/IContentVideoStore.cs"
      provides: "Interface method with throwing default body (mirrors InsertStatedRuleAsync idiom)"
      contains: "GetStatedRulesBySourceSlugAsync"
  key_links:
    - from: "DeckFlow.Core/Content/ContentVideoStore.cs"
      to: "content_stated_rules / content_videos / content_sources"
      via: "parameterized 3-table SELECT keyed on source_slug"
      pattern: "GetStatedRulesBySourceSlugSql"
---

<objective>
Add the missing READ path for stated rules. Today `content_stated_rules` is insert-only
(`InsertStatedRuleAsync`) with a cascade delete; nothing queries it by creator. Fusion (Plan 05)
and the CLI trigger (Plan 06) need to load a creator's stated rules as `StatedRuleCandidate[]`.

Purpose: Supplies the stated leg of the fusion join. Reading the structured DB rows (not
re-parsing the markdown `stated_rules:` YAML, which has no deserializer) keeps a single source of
truth. Independent of Plans 01/02 (touches only the Content store), so it runs in Wave 1.

Output: A dialect-guarded, slug-parameterized read method + its unit test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/97-profile-fusion-conflict-ledger/97-RESEARCH.md
@.planning/phases/97-profile-fusion-conflict-ledger/97-PATTERNS.md

<interfaces>
StatedRuleCandidate shape to hydrate (DeckFlow.Core/Knowledge/StatedRulesExtraction/StatedRuleCandidate.cs):
  Category, Metric, Value?, ValueMin?, ValueMax?, Comparator, Condition?, ClipTimestampSeconds?,
  SourceClip, Confidence, CardReference?, CardGrounded?, VideoDateUtc (required DateTimeOffset).

Existing insert SQL columns (ContentVideoStore.cs:615-649) the SELECT must match:
  content_stated_rules( video_id, category, metric, value, value_min, value_max, comparator,
    condition, clip_ts, source_clip, confidence, card_reference, card_grounded, video_date_utc, sort_order )

Interface throwing-default idiom (IContentVideoStore.cs:143-148):
  Task<long> InsertStatedRuleAsync(...) => throw new NotSupportedException("...");
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add GetStatedRulesBySourceSlugAsync to interface + store</name>
  <files>DeckFlow.Core/Content/IContentVideoStore.cs, DeckFlow.Core/Content/ContentVideoStore.cs</files>
  <read_first>
    - DeckFlow.Core/Content/IContentVideoStore.cs (InsertStatedRuleAsync throwing-default at lines 143-148 — the exact idiom to mirror)
    - DeckFlow.Core/Content/ContentVideoStore.cs (ListVideosPendingDistillSql lines 527-554 for the joined-SELECT shape; InsertStatedRuleSql lines 615-649 for exact column names/casing; imports + dialect-guard section)
    - DeckFlow.Core/Content/ContentSourceStore.cs (source_slug-keyed SELECT convention)
    - DeckFlow.Core/Knowledge/StatedRulesExtraction/StatedRuleCandidate.cs (target record fields + required VideoDateUtc)
  </read_first>
  <action>
    On `IContentVideoStore` add:
    `Task<IReadOnlyList<StatedRuleCandidate>> GetStatedRulesBySourceSlugAsync(string sourceSlug,
    CancellationToken cancellationToken = default) => throw new NotSupportedException("This content
    video store does not support stated-rule reads.");` — the throwing-default keeps other
    implementations/test doubles compiling unchanged.

    In `ContentVideoStore` implement it: a `const string GetStatedRulesBySourceSlugSql` 3-table join
    `content_stated_rules sr JOIN content_videos v ON v.id = sr.video_id JOIN content_sources s ON
    s.id = v.source_id WHERE s.source_slug = @sourceSlug ORDER BY sr.video_id, sr.sort_order`.
    Select all StatedRuleCandidate columns, aliasing where needed so Dapper maps to record property
    names (e.g. `clip_ts AS ClipTimestampSeconds`, `video_date_utc AS VideoDateUtc`,
    `source_clip AS SourceClip`, etc. — match the exact aliasing the sibling reads use). Parameterize
    with an anonymous object `new { sourceSlug }` — NEVER concatenate the slug. Follow the existing
    dialect-guard / connection-open / Dapper `QueryAsync` convention already in this file. Map DB
    integer booleans (SQLite) for `card_grounded` the same way the insert path handles them. Return
    `.ToList()` as IReadOnlyList; empty list when no rows. Guard `sourceSlug` with
    `ArgumentException.ThrowIfNullOrWhiteSpace`. Preserve LF endings.
  </action>
  <verify>
    <automated>/mnt/c/Program\ Files/dotnet/dotnet.exe build DeckFlow.Core</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "GetStatedRulesBySourceSlugAsync" DeckFlow.Core/Content/IContentVideoStore.cs DeckFlow.Core/Content/ContentVideoStore.cs` returns hits in both files.
    - The SQL uses `@sourceSlug` and Dapper `new { sourceSlug }` — `grep -n "source_slug = '" ` finds NO string-concatenated slug.
    - Core builds clean; all existing IContentVideoStore test doubles still compile (throwing default preserves them).
  </acceptance_criteria>
  <done>Stated rules readable by creator slug via a parameterized dialect-guarded join.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Round-trip test — insert stated rules, read back by slug</name>
  <files>DeckFlow.Core.Tests/ContentVideoStoreStatedRulesReadTests.cs</files>
  <read_first>
    - DeckFlow.Core.Tests/ (existing ContentVideoStore distill/insert tests — mirror their SQLite store construction + source/video seeding helpers)
    - DeckFlow.Core/Content/ContentVideoStore.cs (InsertStatedRuleAsync + source/video insert methods used to seed the fixture)
  </read_first>
  <behavior>
    - Test: seed a content_source (source_slug="salubrioussnail"), a content_video under it, insert 2-3 StatedRuleCandidates (varying metric/condition, one with a band value_min/value_max, one gte single value); GetStatedRulesBySourceSlugAsync("salubrioussnail") returns them all with fields intact (metric, condition, value/min/max, comparator, source_clip, confidence, video_date).
    - Test: an unknown slug returns an empty list (not null, no throw).
    - Test: rules from a DIFFERENT source slug are not returned (join filter correctness).
    - Postgres parity: a `[PostgresFact]`-gated variant of the field-intact read runs the SAME seed+read against the existing Testcontainers Postgres fixture (mirror the P94/P95 `PostgresFact` pattern already in DeckFlow.Core.Tests). This proves the join + `video_date_utc` read work on Postgres where the column is `TIMESTAMPTZ` (SQLite stores it as TEXT) — the F-51-PG-01 TEXT-vs-timestamptz Npgsql trap family. If the Postgres fixture is unavailable the gated fact skips (does not fail).
  </behavior>
  <action>
    Add `ContentVideoStoreStatedRulesReadTests.cs` (namespace `DeckFlow.Core.Tests`). Construct a
    fresh SQLite ContentVideoStore, EnsureSchema, seed source+video+rules via the store's own insert
    methods, then assert the read. Use xUnit `[Fact]`, descriptive names
    (`GetStatedRulesBySourceSlug_ReturnsAllRulesForCreator_WithFieldsIntact`,
    `GetStatedRulesBySourceSlug_UnknownSlug_ReturnsEmpty`,
    `GetStatedRulesBySourceSlug_OtherCreatorRules_NotReturned`). LF endings.
  </action>
  <verify>
    <automated>/mnt/c/Program\ Files/dotnet/dotnet.exe test DeckFlow.Core.Tests --filter "FullyQualifiedName~StatedRulesRead"</automated>
  </verify>
  <acceptance_criteria>
    - All three read tests pass.
    - A band rule (value_min/value_max set, comparator "range") round-trips with both bounds preserved.
    - The cross-creator isolation test proves the source_slug WHERE filter works (no leakage).
    - A `[PostgresFact]` field-intact read passes against the Postgres fixture (or skips cleanly if unavailable), proving `video_date_utc` TIMESTAMPTZ reads back correctly.
  </acceptance_criteria>
  <done>The stated-rules read path is proven correct and isolation-safe.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller → local SQLite/Postgres | The `sourceSlug` argument crosses into a SQL query. In this phase the caller is the CLI (operator-supplied slug) or the Studio page (hardcoded slug). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-97-03 | Tampering (SQL injection) | GetStatedRulesBySourceSlugAsync slug param | mitigate | Parameterized Dapper query (`new { sourceSlug }`); never string-concatenate the slug — matches every existing store method (ASVS V5). |
| T-97-04 | Information disclosure | 3-table join returns only stated-rule columns | accept | No secrets or cross-creator data returned; WHERE filters to the requested source_slug; isolation test enforces it. |
| T-97-SC | Tampering | npm/pip/cargo installs | accept | No package-manager installs in this plan. |
</threat_model>

<verification>
- `dotnet.exe build DeckFlow.Core` clean.
- `dotnet.exe test DeckFlow.Core.Tests --filter FullyQualifiedName~StatedRulesRead` green.
- Slug is parameterized (grep gate: no concatenated slug in SQL).
</verification>

<success_criteria>
Fusion can load a creator's stated rules by slug as StatedRuleCandidate[], with condition and
band values intact, via a parameterized, isolation-tested join.
</success_criteria>

<output>
Create `.planning/phases/97-profile-fusion-conflict-ledger/97-03-SUMMARY.md` when done.
</output>
