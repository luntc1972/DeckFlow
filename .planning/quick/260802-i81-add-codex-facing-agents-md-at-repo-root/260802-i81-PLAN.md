---
id: 260802-i81
description: Add Codex-facing AGENTS.md at repo root
date: 2026-08-02
branch: docs/codex-agents-md
---

# Quick Task 260802-i81: Add Codex-facing AGENTS.md at repo root

## Why

Codex reads `AGENTS.md`, not `CLAUDE.md`. DeckFlow has none, so every `codex exec` dispatch
re-derives the repo layout by exploration. Measured 2026-08-02 from the Codex session logs
(`~/.codex/sessions/2026/08/01/`): a single plan-review run emitted **622,617 chars of tool
output across 39 calls**, including whole-file `nl -ba` dumps of 31–42k chars each, overlapping
`sed` reads of the same plan, and **`CLAUDE.md` read twice in one session** (~19k tokens). Context
is re-sent on every tool call, so each dump taxes the rest of the run. That session cost 4.56M
tokens against a 20.7k fixed preamble.

Global rules already live in `~/.codex/AGENTS.md` (verified: it loads every session, and a repo
`AGENTS.md` **merges** after a `--- project-doc ---` separator rather than replacing it). This file
must therefore carry only repo-specific facts.

## Tasks

### 1. Write `AGENTS.md` at repo root

- **files:** `AGENTS.md` (new)
- **action:** Author a Codex-facing repo guide: module map (7 projects, `DeckFlow.Web/Services`
  13 subdirs), build/test commands, feature locations (Cut Lab, manabase, prompts), the
  `.editorconfig` changed-lines format gate and its five carve-outs, LF line endings, the
  TypeScript build coupling and gitignored `wwwroot/js`, and the never-open-a-browser UI rule.
- **constraint:** Under ~150 lines — it loads on every Codex invocation. Do not restate anything
  the global `~/.codex/AGENTS.md` already covers (read budget, EOL preservation, scope, output).
- **verify:** `wc -l AGENTS.md` under 150; `grep -c $'\r' AGENTS.md` returns 0 (LF per
  `.gitattributes`); every path named in the file resolves on disk.
- **done:** File exists, committed, all referenced paths verified present.

## must_haves

- **truths:** Codex reads AGENTS.md not CLAUDE.md; repo AGENTS.md merges after the global one;
  context is re-sent per tool call so file dumps compound.
- **artifacts:** `AGENTS.md` at repo root.
- **key_links:** `DeckFlow.sln`, `DeckFlow.Web/Services/CutLab/`, `DeckFlow.Core/Manabase/`,
  `scripts/format-check-changed.sh`, `scripts/run-web-test.sh`, `.editorconfig`.

## Out of scope

- Editing `.editorconfig`, `.gitattributes`, or `CLAUDE.md`.
- Any source change. Documentation only.
