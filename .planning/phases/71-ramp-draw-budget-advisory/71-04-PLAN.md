---
phase: 71-ramp-draw-budget-advisory
plan: 04
type: execute
wave: 4
depends_on: ["71-03"]
files_modified:
  - DeckFlow.Web/Views/Deck/Manabase.cshtml
  - DeckFlow.Web/wwwroot/css/site-common.css
  - DeckFlow.Web/e2e/manabase-verdict.spec.ts
  - DeckFlow.Web/Help/manabase.md
  - README.md
autonomous: true
requirements: [PLV-GLOSS, PLV-VERDICT, PLV-BUDGET]
must_haves:
  truths:
    - "Flag ON Casual: each surfaced metric shows a plain gloss, a 'Reading your deck' block renders, and the ramp/draw budget line appears"
    - "Flag ON cEDH: the Karsten-lens gloss renders (cast-rate lens + its gloss are Casual-only by construction) and the ramp/draw budget block + verdict are absent"
    - "Flag OFF: the view is byte-identical to today (no glosses, no verdict, no budget)"
    - "Layout/theme CSS lives in site-common.css with theme tokens (not site.css) and does not break the two-lens layout or mobile stacking"
  artifacts:
    - path: "DeckFlow.Web/Views/Deck/Manabase.cshtml"
      provides: "flag-gated glosses + verdict + budget rendering"
      contains: "ShowPlainLanguage"
    - path: "DeckFlow.Web/e2e/manabase-verdict.spec.ts"
      provides: "live (non-CI) Playwright coverage of the verdict surfaces"
  key_links:
    - from: "Manabase.cshtml"
      to: "Model.PlainLanguageVerdict / Model.RampDrawBudget / Model.ShowPlainLanguage"
      via: "Razor conditional rendering"
      pattern: "PlainLanguageVerdict"
---

<objective>
Render the plain-language layer on `/manabase`: inline metric glosses under each lens, the "Reading
your deck" verdict block, and the Casual-only ramp/draw budget block - all flag-gated via the view
model, with theme-token CSS in `site-common.css`. Add live Playwright coverage and update docs.

Purpose: Deliverables A/B/C on the page, satisfying the project rule that a web-page change ships
with tests + docs and verifies across themes + viewports.
Output: updated Razor view, CSS, e2e spec, README + Help/manabase.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/71-ramp-draw-budget-advisory/71-CONTEXT.md
@.planning/phases/71-ramp-draw-budget-advisory/71-SPEC.md
@.planning/phases/71-ramp-draw-budget-advisory/71-UI-SPEC.md
@.planning/phases/71-ramp-draw-budget-advisory/71-03-SUMMARY.md
@DeckFlow.Web/Views/Deck/Manabase.cshtml
@DeckFlow.Web/e2e/manabase.spec.ts

<interfaces>
<!-- View-model fields from 71-03, and the EXACT render anchors / gating in the existing view. -->

ManabaseViewModel: ManabaseVerdict? PlainLanguageVerdict; ManabaseRampDrawBudget? RampDrawBudget;
  bool ShowPlainLanguage. (Glosses: gate on ShowPlainLanguage; verdict: gate on PlainLanguageVerdict
  != null; budget: gate on RampDrawBudget != null - already Casual-only from the service.)
ManabaseVerdict { bool HasIssues; IReadOnlyList<string> Lines; string NoIssueReason; string Headline; }
ManabaseRampDrawBudget { double RampCount; double DrawCount; int OverlapCount; double Threshold;
  ThresholdSource; int TargetRamp; int TargetDraw; bool IsBalanced; ... }
ManabaseDisplay glosses: KarstenSourceGloss, CastRateGloss, WeakestColorGloss, DemandingCardsGloss.

Render anchors / lens gating in Manabase.cshtml (verified against live code):
- two-lens band `.manabase-twolens` (~L169). The LEFT (Karsten source check) lens renders when
  `showLeftLens = report.ColorFindings.Count > 0` (mode-INDEPENDENT — present in cEDH too).
- The RIGHT (Simulated cast rate) lens renders when `showRightLens = Model.ShowCastability &&
  castRows.Count > 0`, and `ShowCastability => Report is { Mode: Casual, ... }` (ManabaseViewModel.cs:67)
  — so the right lens is CASUAL-ONLY (suppressed in cEDH).
- The demanding-cards span renders only when `report.Health != Healthy && report.DemandingCards.Count
  > 0` (Manabase.cshtml:222). There is NO standalone weakest-color element.
- Existing CSS classes: `.manabase-lens`, `.manabase-lens-note`, `.manabase-help`, `.manabase-ramp`,
  `.manabase-demanding`.
- site-common.css holds layout/theme tokens; theme color tokens live in each theme file's `:root`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Render glosses, verdict, and ramp/draw budget in the view (flag-gated) + CSS</name>
  <files>DeckFlow.Web/Views/Deck/Manabase.cshtml, DeckFlow.Web/wwwroot/css/site-common.css</files>
  <action>In Manabase.cshtml, gate ALL new markup so flag OFF (ShowPlainLanguage false &&
    PlainLanguageVerdict null && RampDrawBudget null) renders byte-identically to today.
    (A) Glosses — anchor each one precisely against the live lens gating:
      • Karsten-lens gloss: inside the `@if (showLeftLens)` block, under its `.manabase-lens-note`,
        render a `.manabase-lens-gloss` paragraph with `ManabaseDisplay.KarstenSourceGloss` when
        `Model.ShowPlainLanguage`. This is the gloss GUARANTEED to render in cEDH (the left lens is
        mode-independent), so the cEDH e2e `.manabase-lens-gloss`-present assertion is satisfied by it.
      • Cast-rate gloss: inside the `@if (showRightLens)` block (Casual-only by construction), under its
        `.manabase-lens-note`, render a `.manabase-lens-gloss` with `ManabaseDisplay.CastRateGloss` when
        `Model.ShowPlainLanguage`. Do NOT place this gloss outside the right-lens block (it must not
        appear in cEDH).
      • Weakest-color gloss: there is no standalone weakest-color element, so pin
        `ManabaseDisplay.WeakestColorGloss` INSIDE the Karsten (left) lens block (e.g. appended after
        the Karsten gloss) when `Model.ShowPlainLanguage`.
      • Demanding-cards gloss: render `ManabaseDisplay.DemandingCardsGloss` ONLY alongside the existing
        conditional demanding span (the `report.Health != Healthy && report.DemandingCards.Count > 0`
        block at ~L222) and only when `Model.ShowPlainLanguage` — it must not appear when there is no
        demanding span.
    (B) "Reading your deck": when `Model.PlainLanguageVerdict is {} v`, render a `.manabase-verdict`
    block (heading v.Headline) after the Lands/Health line - if v.HasIssues, a list of v.Lines; else a
    single `.manabase-verdict-fine` paragraph with v.NoIssueReason.
    (C) Ramp/draw budget: when `Model.RampDrawBudget is {} b` (Casual-only by construction), render a
    `.manabase-rampdraw` block: "You run ~b.RampCount ramp / ~b.DrawCount draw; for a ~MV{Threshold}
    threshold a common split is b.TargetRamp / b.TargetDraw" + "(b.OverlapCount do both)" + the
    "community heuristic, not Karsten math" label and the proxy disclosure. Use F1/0.# formatting like
    the surrounding view. NO em/en dashes in copy - plain hyphens only.
    In site-common.css add `.manabase-lens-gloss`, `.manabase-verdict`, `.manabase-verdict-fine`,
    `.manabase-rampdraw` styles using EXISTING theme tokens (the same `--` custom properties the
    neighboring `.manabase-*` rules use - do not hard-code colors, do not add to site.css). Ensure the
    gloss text wraps inside the lens and the new blocks stack cleanly at the 390px mobile width (verify
    the `.manabase-twolens` grid is unaffected).</action>
  <verify>
    <automated>cd /mnt/c/users/chrislunt/source/personal/deckflow && cmd.exe /c "dotnet build DeckFlow.Web\DeckFlow.Web.csproj -clp:ErrorsOnly" 2>&1 | tail -15</automated>
  </verify>
  <done>View compiles; the Karsten-lens gloss renders in both modes, the cast-rate gloss is inside the
    Casual-only right lens, the weakest-color gloss is pinned in the Karsten lens, and the demanding
    gloss only appears with its conditional span; verdict/budget render only when their view-model
    fields are set; CSS is in site-common.css using theme tokens; two-lens layout + mobile intact.</done>
</task>

<task type="auto">
  <name>Task 2: Live (non-CI) Playwright spec for the verdict surfaces</name>
  <files>DeckFlow.Web/e2e/manabase-verdict.spec.ts</files>
  <action>Create `manabase-verdict.spec.ts`. Because rendering the verdict requires a real Scryfall
    analysis AND the flag must be ON, this spec is LIVE-only and must NOT run in CI (the CI manabase
    specs deliberately avoid submitting an analysis). Guard every test with
    `test.skip(!process.env.DECKFLOW_LIVE_E2E, 'live-only: needs Scryfall + flag on')` so a normal CI
    run skips it. Document at the top how to run it: start the server via `scripts/run-web-test.sh`
    with the flag toggled ON, then `DECKFLOW_LIVE_E2E=1 npx --no-install playwright test manabase-verdict`.
    Cover: (1) Casual issue-deck -> `.manabase-verdict` visible with at least one verdict list line,
    `.manabase-lens-gloss` visible, `.manabase-rampdraw` visible; (2) Casual clean-deck ->
    `.manabase-verdict-fine` visible (why-it's-fine), no issue list; (3) cEDH deck -> at least one
    `.manabase-lens-gloss` present (the Karsten-lens gloss) but `.manabase-rampdraw` absent and no
    `.manabase-verdict` block. Use the chromium-desktop + chromium-mobile projects (already configured)
    so both viewports are exercised. Use small, well-known fixture decklists pasted into
    `#manabase-deck-text` (one color-short deck, one clean two-color deck).</action>
  <verify>
    <automated>cd /mnt/c/users/chrislunt/source/personal/deckflow/DeckFlow.Web && npx --no-install playwright test manabase-verdict --list 2>&1 | tail -15</automated>
  </verify>
  <done>The spec parses and is discovered by Playwright, is skipped without DECKFLOW_LIVE_E2E (so CI
    stays green), and covers the issue/clean/cEDH scenarios across both viewport projects when run live.</done>
</task>

<task type="auto">
  <name>Task 3: Update README + Help/manabase.md</name>
  <files>DeckFlow.Web/Help/manabase.md, README.md</files>
  <action>In Help/manabase.md (Step 2 - Read The Report), add a short subsection documenting the
    plain-language layer: the metric glosses, the "Reading your deck" verdict (prioritized add/remove
    when there are issues, or a specific why-it's-fine when there aren't), and the Casual-only ramp/draw
    slot-budget advisory - stating explicitly that the split is a community heuristic (not Karsten math),
    the threshold is a single-point proxy (commander MV or curve 75th-percentile), and that the whole
    layer is advisory and never changes the land count, color counts, castability, or health band. Note
    it is behind an admin flag and that cEDH shows the glosses but suppresses the ramp/draw budget. In
    README.md, update the manabase feature description to mention the plain-language verdict + ramp/draw
    advisory. Keep README behavior-accurate (project rule: README updated in the same change).</action>
  <verify>
    <automated>cd /mnt/c/users/chrislunt/source/personal/deckflow && grep -l "Reading your deck" DeckFlow.Web/Help/manabase.md README.md</automated>
  </verify>
  <done>Both docs describe the verdict + ramp/draw advisory, flag-gating, the heuristic/proxy caveats,
    and that the layer is advisory-only.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| browser -> /manabase form POST | existing public endpoint; this plan adds only rendered output, no new input field or route. CSRF + rate-limit already enforced by the existing controller. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-71-06 | Information disclosure (XSS) | New verdict/gloss text in Razor | mitigate | Render via Razor's default HTML-encoding (no `Html.Raw`); the verdict strings are server-generated templated prose, not user HTML. |
| T-71-07 | Denial of service | Extra synthesis on render | accept | Synthesis reads already-computed report data (O(colors)); negligible cost, no new Scryfall calls. |
| T-71-SC | Tampering | npm/pip/cargo installs | n/a | No package installs (Playwright already present). |
</threat_model>

<verification>
- `dotnet build DeckFlow.Web` clean; CI Playwright suite still green (new spec skipped without the env).
- Flag OFF renders byte-identical (no `.manabase-verdict` / `.manabase-rampdraw` / `.manabase-lens-gloss`).
- Live run shows glosses + verdict + budget (Casual) and the Karsten-lens gloss without budget (cEDH)
  across both viewports.
- CSS additions are in site-common.css using theme tokens only.
</verification>

<success_criteria>
The plain-language layer renders correctly and only when flag-gated, with glosses anchored to the
correct lens (Karsten gloss in both modes, cast-rate gloss Casual-only), styled with theme tokens in
site-common.css, with live Playwright coverage and updated README + Help docs.
</success_criteria>

<output>
Create `.planning/phases/71-ramp-draw-budget-advisory/71-04-SUMMARY.md` when done.
</output>
