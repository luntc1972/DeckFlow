---
context: phase
phase: 82-refactor-review-sweep-ui-baseline-audit
task: 6
total_tasks: 6
status: planning-complete-not-executed
last_updated: 2026-07-04T15:15:59.325Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each was discovered through failure this session.
> Acknowledge each explicitly before proceeding.

- [ ] CONSTRAINT: **Execute only in the dedicated worktree** — All Phase 82 execution runs in `../deckflow-cycle15` (branch `plan/cycle-15-cleanup-polish`), NEVER the shared main checkout. A concurrent session live-pushed `main` (`66975ef5`) on top of this session's plan commit `852cfbe3` — the main worktree is actively contested. Structural mitigation: `cd ../deckflow-cycle15` (or point the skill at it) before any execute; verify `git branch --show-current` == `plan/cycle-15-cleanup-polish` before mutating.
- [ ] CONSTRAINT: **One execute run per bench** — Separate-bench operator directive (2026-06-30). NEVER run two `/gsd-execute-phase` passes in the same worktree; open a fresh bench per run. Prevents the Phase-78 concurrent-commit collision.
- [ ] CONSTRAINT: **cwd resets to main after every Bash call** — The tool shell returns to `/mnt/c/.../deckflow` (main) after each command; a bare `cd` does not persist. Scope every git/gsd command to the worktree path, or the skill will read the wrong `.planning`.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Editing verify blocks without checking tag balance | My MED-1 edit dropped the closing `</automated></verify>` tags in 82-02 + 82-03 Task 2, corrupting the XML — only caught by Codex re-review | blocking | After ANY edit to a `<verify>`/`<automated>` block, run `grep -c '<automated>\|</automated>\|</verify>'` on the file and confirm balance; run `gsd-sdk query verify.plan-structure` |
| Automated grep over free-form reviewer prose | A raw `chatgpt-`/`accent-strong` substring ban false-trips: deck-sync.ts has ~65 legit chatgpt-* selectors, and backlog notes say "defer chatgpt- rename to Phase 85" | advisory | Fence by candidate/finding ACTION type via manual review, not substring grep, when the target is unstructured prose |
| Phase 86 as a UI-fix owner | 82-02 originally dumped out-of-scope gap fixes on Phase 86; ROADMAP crit-5 says hand to 84/85, "not left for 86 to discover" | advisory | 86 = re-score/closeout only; out-of-84/85-scope gaps go under an explicit "needs assignment" subsection |

<current_state>
Phase 82 (first phase of Cycle 15) is FULLY PLANNED and Codex-APPROVED. 3 plans written, plan-checker blocker fixed, 3 Codex review passes (CHANGES → CHANGES → APPROVE), all findings + residuals resolved. Execution has NOT begun. Worktree `../deckflow-cycle15`, branch `plan/cycle-15-cleanup-polish`, tip `fe251a7f`, working tree clean, nothing pushed.
</current_state>

<completed_work>

Completed Tasks (planning + review loop):
- Plan Phase 82 — 3 plans / 2 waves (gsd-planner opus) — `852cfbe3`
- gsd-plan-checker — 1 blocker + 2 warnings — fixed `08e50fc6`
- Codex pass 1 (gpt-5.4) — CHANGES-REQUESTED 4 HIGH / 2 MED / 1 LOW — fixed `02bf4273`
- Codex pass 2 — CHANGES (HIGH2 partial + caught my close-tag regression) — fixed `14725281`
- Codex pass 3 — APPROVE — residuals aligned `fe251a7f`
</completed_work>

<remaining_work>

- `/gsd-execute-phase 82` (NOT started):
  - Wave 1: **82-01** sweep+triage (REVIEW-01 → `82-REVIEW.md` + `REFACTOR-TRIAGE.md`) ∥ **82-02** UI baseline audit (UIAUDIT-01 → `tasks/UI-REVIEW.md`)
  - Wave 2: **82-03** refactor execution (REVIEW-02 → refactored source + `REFACTOR-BACKLOG.md`), consumes 82-01's triage artifact
- Push branch → confirm GitHub Actions green (CI is the authoritative gate)
</remaining_work>

<decisions_made>

- Cycle 15 execution isolated to worktree `../deckflow-cycle15` (concurrent session contests main)
- TS byte-neutral gate runs Vitest (`npm --prefix DeckFlow.Web test`), not just build+dotnet+Playwright — deck-sync.ts IS covered
- THEME/AICLEAN scope-fence is a manual criterion, not an automated grep
- 82-03 target list = REFACTOR-TRIAGE.md in-scope rows (widened set), not the 2 flagship files
- 82-02 gap fixes → Phase 84/85 only; 86 = re-score; out-of-scope gaps flagged for assignment
- Codex model = gpt-5.4 medium for all Codex work this session
</decisions_made>

<blockers>
- None. Plans are execution-ready.
</blockers>

## Required Reading (in order)
1. `.planning/phases/82-refactor-review-sweep-ui-baseline-audit/82-01-PLAN.md` — sweep + triage (REVIEW-01)
2. `.planning/phases/82-refactor-review-sweep-ui-baseline-audit/82-02-PLAN.md` — UI baseline audit + gap-to-20 handoff (UIAUDIT-01)
3. `.planning/phases/82-refactor-review-sweep-ui-baseline-audit/82-03-PLAN.md` — refactor execution under byte-identical gate (REVIEW-02)
4. `.planning/ROADMAP.md` (Phase 82 section) — goal + 5 success criteria
5. `./CLAUDE.md` — byte-identical gate, .editorconfig carve-outs, WSL test constraints, delegation rule

## Infrastructure State
- Git worktree `../deckflow-cycle15` on `plan/cycle-15-cleanup-polish` (tip `fe251a7f`), clean, unpushed. Base = `origin/main` `66975ef5`.
- No background servers running from this session.
- Vitest TS runner present (`DeckFlow.Web/package.json` `test: vitest run`, `vitest.config.ts`, `ts-tests/`); dotnet build/test via Windows `dotnet.exe` (VSTest unreliable in WSL).

<context>
Delegation: Claude codes AND plans, Codex reviews. On execute, Claude drives gsd-executors; dispatch each wave's diff to Codex gpt-5.4 medium for review before moving on. The whole cycle is behavior-neutral — the byte-identical gate (build 0/0 + xUnit + CarveOutGuard + changed-lines format-gate + Vitest/Playwright for TS) IS the safety mechanism. Phase 82 is discovery+baseline: 82-01's triage artifact drives 82-03's refactor list, so run Wave 1 first and let the triage doc decide scope — don't presuppose refactor targets.
</context>

<next_action>
Start with: `cd ../deckflow-cycle15` (verify branch == plan/cycle-15-cleanup-polish), then `/gsd-execute-phase 82`. Execute in THIS worktree only, one execute pass per bench.
</next_action>
