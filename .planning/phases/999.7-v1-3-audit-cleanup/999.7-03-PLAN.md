---
phase: 999.7-v1-3-audit-cleanup
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - DeckFlow.Web/Services/PromptBuilders/Analysis/ClaudeAnalysisPromptVariant.cs
  - DeckFlow.Web/Services/PromptBuilders/Comparison/ClaudeComparisonPromptVariant.cs
  - DeckFlow.Web/Services/PromptBuilders/MetaGap/ClaudeMetaGapPromptVariant.cs
autonomous: true
requirements: [SC3, SC6, SC7]

must_haves:
  truths:
    - "All 3 Claude variant class-level `<summary>` doc-comments describe the post-Phase-999.2 reality: direct JSON fenced-block output (no `<result>` wrapper)."
    - "Phrase 'result-wrapped output' does not appear in any of the 3 files after the edit."
    - "Only the class-level `<summary>` is touched in each file. The `Build(` method body, the `Platform =>` doc, the `Build` method's `<summary>` doc above it, and the using/namespace lines are untouched."
    - "ClaudeFollowUpPromptVariant.cs and ClaudeSetUpgradePromptVariant.cs are NOT touched (per audit F-01 evidence inventory: those summaries do not carry the stale phrase)."
    - "`dotnet build DeckFlow.sln -c Release` exits 0 with warning count <= the post-Phase-999.6 baseline."
    - "`dotnet test DeckFlow.sln -c Release` reports Failed:0 Passed:497 Skipped:3 Total:500."
  artifacts:
    - path: "DeckFlow.Web/Services/PromptBuilders/Analysis/ClaudeAnalysisPromptVariant.cs"
      provides: "Updated class-level <summary> (line 13-15 region) — drops 'result-wrapped output'"
      contains: "phrase 'direct JSON fenced-block output' (or equivalent describing fenced-block emission)"
    - path: "DeckFlow.Web/Services/PromptBuilders/Comparison/ClaudeComparisonPromptVariant.cs"
      provides: "Updated class-level <summary> (line 10-12 region)"
      contains: "phrase 'direct JSON fenced-block output'"
    - path: "DeckFlow.Web/Services/PromptBuilders/MetaGap/ClaudeMetaGapPromptVariant.cs"
      provides: "Updated class-level <summary> (line 12-14 region)"
      contains: "phrase 'direct JSON fenced-block output'"
  key_links:
    - from: "DeckFlow.Web/Services/PromptBuilders/{Analysis,Comparison,MetaGap}/Claude*PromptVariant.cs class-level <summary>"
      to: "Phase 999.2 SUMMARY (`<result>` wrapper strip — direct JSON output)"
      via: "audit F-01 documentation accuracy"
      pattern: "grep -l 'result-wrapped output' in the 3 target files returns 0 matches"
---

<objective>
Update the class-level XML `/// <summary>` doc-comments in the three Claude prompt variant files to drop the stale phrase "result-wrapped output" (referenced behavior was stripped in Phase 999.2) and replace it with phrasing that matches the post-strip reality: direct JSON fenced-block output. Closes audit finding F-01 in `.planning/v1.3-MILESTONE-AUDIT.md` lines 200-204.

Purpose: documentation accuracy — runtime behavior is already correct; only the doc-comments are misleading.
Output: 3 single-line edits (the `<summary>` body of the class doc), build clean, tests pass at 999.6 baseline.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/v1.3-MILESTONE-AUDIT.md
@DeckFlow.Web/Services/PromptBuilders/Analysis/ClaudeAnalysisPromptVariant.cs
@DeckFlow.Web/Services/PromptBuilders/Comparison/ClaudeComparisonPromptVariant.cs
@DeckFlow.Web/Services/PromptBuilders/MetaGap/ClaudeMetaGapPromptVariant.cs
@CLAUDE.md

<interfaces>
Each of the 3 files has the same pattern. Example from ClaudeAnalysisPromptVariant.cs lines 13-15:

```
/// <summary>
/// Builds a deck-analysis prompt body formatted for Claude (XML-tagged prompts with result-wrapped output).
/// </summary>
internal sealed class ClaudeAnalysisPromptVariant : IAnalysisPromptVariant
```

The phrase to remove is `result-wrapped output`. The replacement phrase should describe what the variant emits today after Phase 999.2's `<result>` wrapper strip: a direct JSON fenced block (no XML wrapper around the JSON).

ClaudeComparisonPromptVariant.cs lines 10-12 and ClaudeMetaGapPromptVariant.cs lines 12-14 follow the identical 3-line summary pattern, with "deck-analysis" replaced by "deck-comparison" and "cEDH meta-gap" respectively.

Files NOT to touch (audit F-01 evidence inventory — these do not carry the stale phrase):
- DeckFlow.Web/Services/PromptBuilders/Analysis/ClaudeFollowUpPromptVariant.cs
- DeckFlow.Web/Services/PromptBuilders/Analysis/ClaudeSetUpgradePromptVariant.cs
- (and any non-Claude variant)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Replace 'result-wrapped output' with 'direct JSON fenced-block output' in all 3 Claude variant class summaries</name>
  <files>
    DeckFlow.Web/Services/PromptBuilders/Analysis/ClaudeAnalysisPromptVariant.cs,
    DeckFlow.Web/Services/PromptBuilders/Comparison/ClaudeComparisonPromptVariant.cs,
    DeckFlow.Web/Services/PromptBuilders/MetaGap/ClaudeMetaGapPromptVariant.cs
  </files>
  <read_first>
    - DeckFlow.Web/Services/PromptBuilders/Analysis/ClaudeAnalysisPromptVariant.cs (line 13-15 — class-level `<summary>` to edit)
    - DeckFlow.Web/Services/PromptBuilders/Comparison/ClaudeComparisonPromptVariant.cs (line 10-12)
    - DeckFlow.Web/Services/PromptBuilders/MetaGap/ClaudeMetaGapPromptVariant.cs (line 12-14)
    - .planning/v1.3-MILESTONE-AUDIT.md (lines 200-204 — F-01 recommendation specifies the exact replacement phrasing target)
    - CLAUDE.md (formatting paranoia rules — section "Tech stack" + "Formatting" — touch only lines that need touching)
  </read_first>
  <action>
    In each of the 3 files, locate the `/// <summary>` line that contains `result-wrapped output` (it sits ABOVE the `internal sealed class Claude*PromptVariant` line — not above the `Build(` method). Replace the parenthetical `(XML-tagged prompts with result-wrapped output)` with `(XML-tagged prompts with direct JSON fenced-block output)`. Preserve the leading `/// ` prefix, the surrounding sentence wording, and trailing period.

    Concretely, the single replaced sub-string per file is:
      from: `result-wrapped output`
      to:   `direct JSON fenced-block output`

    Constraints (NON-NEGOTIABLE per project CLAUDE.md "Formatting" paragraph):
      - Touch ONLY that single line in each file. Do not reformat any other line.
      - Do not modify the `Build` method's `<summary>` (which describes byte-for-byte parity with the pre-refactor switch arm) — those are different docs and remain accurate.
      - Do not touch using directives, namespace, attributes, class-level attributes, the `Platform =>` arrow property, the `Build(` method signature, the method body, or any raw-string literal.
      - Do not auto-convert `{ get; init; }` → `{ get; }`.
      - Do not inline `[Attribute]` onto the property line.
      - Do not re-indent C# raw-string literals.
      - Preserve LF line endings.
      - No fenced code block, no other comment, no XML tag change.

    Do NOT touch:
      - DeckFlow.Web/Services/PromptBuilders/Analysis/ClaudeFollowUpPromptVariant.cs
      - DeckFlow.Web/Services/PromptBuilders/Analysis/ClaudeSetUpgradePromptVariant.cs
      - Any non-Claude variant (GeminiAnalysisPromptVariant.cs, ChatGptAnalysisPromptVariant.cs, etc.)
      - Any other file.
  </action>
  <verify>
    <automated>grep -rEn "result-wrapped output" DeckFlow.Web/Services/PromptBuilders/ ; "/mnt/c/Program Files/dotnet/dotnet.exe" build DeckFlow.sln -c Release --nologo</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rln "result-wrapped output" DeckFlow.Web/Services/PromptBuilders/Analysis/ DeckFlow.Web/Services/PromptBuilders/Comparison/ DeckFlow.Web/Services/PromptBuilders/MetaGap/` returns 0 lines (the phrase is gone from all 3 target files and absent elsewhere in PromptBuilders/).
    - `grep -c "direct JSON fenced-block output" DeckFlow.Web/Services/PromptBuilders/Analysis/ClaudeAnalysisPromptVariant.cs` returns `1`.
    - `grep -c "direct JSON fenced-block output" DeckFlow.Web/Services/PromptBuilders/Comparison/ClaudeComparisonPromptVariant.cs` returns `1`.
    - `grep -c "direct JSON fenced-block output" DeckFlow.Web/Services/PromptBuilders/MetaGap/ClaudeMetaGapPromptVariant.cs` returns `1`.
    - `git diff --numstat DeckFlow.Web/Services/PromptBuilders/Analysis/ClaudeAnalysisPromptVariant.cs` shows `1  1  ...` (one line added, one line removed — exactly the single-line edit).
    - Same `1  1` numstat for the Comparison and MetaGap variant files.
    - `git diff DeckFlow.Web/Services/PromptBuilders/Analysis/ClaudeFollowUpPromptVariant.cs DeckFlow.Web/Services/PromptBuilders/Analysis/ClaudeSetUpgradePromptVariant.cs` is empty (these files MUST NOT be touched).
    - `"/mnt/c/Program Files/dotnet/dotnet.exe" build DeckFlow.sln -c Release --nologo` exits 0; warning count <= post-Phase-999.6 baseline (no NEW warnings introduced; doc-comment text changes do not affect IL).
    - `"/mnt/c/Program Files/dotnet/dotnet.exe" test DeckFlow.sln -c Release --nologo` reports `Failed: 0, Passed: 497, Skipped: 3, Total: 500` (Web 440 + Core 57 — Phase 999.6 PHASE EXIT GATE baseline preserved).
    - `git diff DeckFlow.Web/` (excluding the 3 target files) is empty (no formatter spillover).
  </acceptance_criteria>
  <done>3 Claude variant class summaries describe direct JSON fenced-block output; build green; tests at 999.6 baseline; no collateral edits.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Edit touches XML doc-comment text only (lines emitted into generated XML doc file, not into IL). No HTTP / auth / input surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-999.7-03 | Tampering | DeckFlow.Web/Services/PromptBuilders/{Analysis,Comparison,MetaGap}/Claude*PromptVariant.cs | accept | Doc-comment text only; IL byte-symmetric pre/post per audit F-01 fix recommendation. Build+test gates catch any accidental code-path touch. |
| T-999.7-03b | Tampering | Build pipeline (formatter spillover risk) | mitigate | `git diff` whitelist gate: exactly 3 files, 1/1 numstat each. Forbidden tools per CLAUDE.md: no Format Document / Code Cleanup / ReSharper auto-format. |

Severity: **info** — XML doc-comment edit. ASVS L1 not applicable. Build + test gates (SC6 + SC7) provide regression safety net.
</threat_model>

<verification>
1. `grep -rln "result-wrapped" DeckFlow.Web/Services/PromptBuilders/` returns 0 lines.
2. Build + test gates green per acceptance criteria.
3. `git diff --stat` shows exactly 3 files modified, no other files touched.
</verification>

<success_criteria>
SC3, SC6, SC7 from ROADMAP success criteria satisfied:
- SC3: 3 Claude variant class-level summaries updated; ClaudeFollowUp + ClaudeSetUpgrade untouched.
- SC6: Release build exits 0; warning count <= post-999.6 baseline.
- SC7: Test suite reports Failed:0 Passed:497 Skipped:3 Total:500.
</success_criteria>

<output>
Create `.planning/phases/999.7-v1-3-audit-cleanup/999.7-03-SUMMARY.md` when done.
</output>
