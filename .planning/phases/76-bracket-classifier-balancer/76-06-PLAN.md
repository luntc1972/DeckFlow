---
phase: 76-bracket-classifier-balancer
plan: 06
type: execute
wave: 5
depends_on: ["76-05"]
files_modified: ["DeckFlow.Web/e2e/bracket-smoke.spec.ts"]
autonomous: false
requirements: [BRACKET-01, BRACKET-03, BRACKET-05]
must_haves:
  truths:
    - "With tool.bracket.enabled ON locally, /bracket classifies a real high-power deck and renders the badge, reasons, floor violations, starter cuts, effective-date stamp, and a copy-ready paste artifact"
    - "The bracket page is visually correct across Classic + Azorius + Nyx themes at desktop and mobile viewports"
    - "With the flag OFF, /bracket returns 404 and the tile/tab are absent — other pages render unchanged"
  artifacts:
    - path: "DeckFlow.Web/e2e/bracket-smoke.spec.ts"
      provides: "Automated Playwright smoke spec driving the /bracket classify→balancer flow and capturing cross-theme/mobile screenshots"
      contains: "bracket"
    - path: ".planning/ui-design/cycle13/screenshots/"
      provides: "Bracket page screenshots (3 themes x desktop+mobile)"
      contains: "bracket"
  key_links:
    - from: "DeckFlow.Web/e2e/bracket-smoke.spec.ts"
      to: "/bracket"
      via: "Playwright page.goto + form POST"
      pattern: "/bracket"
---

<objective>
Operator visual + functional sign-off for the `/bracket` surface. Claude first drives the automated live checks via a Playwright smoke spec (start the headless server, exercise classify + balancer, assert the populated result, capture screenshots across themes and viewports), then pauses for the user to confirm. This is the BRACKET web-page change's mandatory theme + mobile verification gate.

Purpose: Project rule — every web-page change carries xUnit (done in 76-05) plus theme + mobile visual verification. UI testing must never open a browser on the Windows host.
Output: a runnable Playwright smoke spec, screenshots, and operator approval.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/76-bracket-classifier-balancer/UI-SPEC.md
@.planning/phases/76-bracket-classifier-balancer/76-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the Playwright bracket smoke spec + capture screenshots</name>
  <read_first>
    - scripts/run-web-test.sh (starts the web app headless with DECKFLOW_DISABLE_AUTO_BROWSER=true — never opens a Windows browser)
    - DeckFlow.Web/playwright.config.ts (testDir ./e2e; baseURL http://localhost:5173; chromium-desktop 1280px + chromium-mobile 390px projects; webServer already sets DECKFLOW_DISABLE_AUTO_BROWSER=true and reuseExistingServer under WSL)
    - DeckFlow.Web/e2e/tool-toggles.spec.ts (canonical pattern for serial mode + the /tmp/deckflow-admin-e2e.lock admin lock + CF-Connecting-IP header + toggling a tool flag via /Admin/Tools, then restoring it)
    - DeckFlow.Web/e2e/manabase.spec.ts (closest sibling tool smoke spec — POST a deck, assert the result panel renders)
    - CLAUDE.md (project) — UI testing constraints: Playwright via WSL (`npx --no-install playwright test`), no gstack daemon, headless only
    - .planning/ui-design/cycle13/screenshots/ (existing Phase 75 screenshots — match naming convention)
  </read_first>
  <files>DeckFlow.Web/e2e/bracket-smoke.spec.ts, .planning/ui-design/cycle13/screenshots/ (new bracket-*.png files)</files>
  <action>
    Create `DeckFlow.Web/e2e/bracket-smoke.spec.ts` following the tool-toggles.spec.ts conventions (`test.describe.configure({ mode: 'serial' })`, acquire the shared `/tmp/deckflow-admin-e2e.lock`, set the Basic-auth + `CF-Connecting-IP` headers, restore state in afterEach). The spec runs under BOTH config projects (chromium-desktop 1280px and chromium-mobile 390px) so each assertion + screenshot is captured at both viewports automatically. The spec must: (1) enable `tool.bracket.enabled` for this run via /Admin/Tools (do NOT commit any prod flag change; restore the toggle in afterEach); (2) `page.goto('/bracket')` → assert 200 + empty form; (3) POST a known high-power/cEDH deck (pasted list) with target B3 → assert the badge (`bracket-badge--b{N}`), the WHY-THIS-BRACKET reasons, FLOOR VIOLATIONS, STARTER CUTS, the effective-date stamp, and a non-empty copy-prompt textarea all render; (4) for each theme — Classic (`site.css`), Azorius, Nyx — switch the theme via the existing theme mechanism and call `page.screenshot({ fullPage: true })` to `.planning/ui-design/cycle13/screenshots/bracket-{theme}-{project}.png` (project name distinguishes desktop vs mobile); (5) restore `tool.bracket.enabled` to OFF and assert `page.goto('/bracket')` returns 404 and the Analyze nav/tile no longer shows Bracket. Then start the web app headless via `scripts/run-web-test.sh` (sets DECKFLOW_DISABLE_AUTO_BROWSER=true) and run the spec to produce the 6 screenshots. Do NOT modify the committed prod flag seed (stays FALSE per 76-02); the only flag change is the transient in-test toggle that afterEach reverts.
  </action>
  <verify>
    <automated>cd DeckFlow.Web && DECKFLOW_DISABLE_AUTO_BROWSER=true npx --no-install playwright test e2e/bracket-smoke.spec.ts --reporter=line</automated>
    <human-check>Screenshots exist for Classic/Azorius/Nyx at desktop + mobile, and the live classify→balancer flow produced a populated artifact; flag-OFF returns 404.</human-check>
  </verify>
  <acceptance_criteria>
    - `DeckFlow.Web/e2e/bracket-smoke.spec.ts` exists and passes under both chromium-desktop and chromium-mobile projects (no Windows browser — DECKFLOW_DISABLE_AUTO_BROWSER=true honored).
    - 6 screenshots present under .planning/ui-design/cycle13/screenshots/ (3 themes x 2 viewports), each showing the populated result panel.
    - Live POST produced a non-empty paste artifact containing "WHY THIS BRACKET".
    - With the flag toggled OFF, GET /bracket returns HTTP 404 and the Bracket tile/tab are absent.
    - The transient flag toggle is reverted in afterEach; no prod flag seed change committed.
  </acceptance_criteria>
  <done>The smoke spec passes via `npx --no-install playwright test e2e/bracket-smoke.spec.ts` and screenshots are captured across the three themes at both viewports.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Operator visual + functional sign-off</name>
  <files>none</files>
  <action>
    Pause for the operator. Present the 6 captured screenshots and the live-check results from Task 1, then wait for explicit approval before the phase is considered visually verified. Do not auto-advance: this checkpoint blocks on human input even though workflow.auto_advance is true. Built in Task 1 / plan 76-05: the /bracket Bracket Check tool — a flag-gated page that classifies a Commander deck into its official 1-5 bracket (Game Changers, two-card combos, mass-land-denial; no tutor gate), shows floor violations + starter cuts against a target, stamps the Game Changers effective-date, and emits a copy-ready ChatGpt/Claude/Gemini balancer artifact.
  </action>
  <what-built>
    The /bracket Bracket Check tool: flag-gated page that classifies a Commander deck into its official 1-5 bracket (Game Changers, two-card combos, mass-land-denial; no tutor gate), shows floor violations + starter cuts against a target, stamps the Game Changers effective-date, and emits a copy-ready ChatGpt/Claude/Gemini balancer artifact.
  </what-built>
  <how-to-verify>
    1. Review the 6 screenshots under `.planning/ui-design/cycle13/screenshots/` (Classic/Azorius/Nyx x desktop/mobile) — confirm the badge, reasons, violations, cuts, stamp, and copy-prompt all read correctly with AA contrast on each theme and that the violation tag pills wrap (don't clip) on mobile.
    2. Optionally run locally: `scripts/run-web-test.sh`, enable tool.bracket.enabled in the local flag store, visit http://localhost:5173/bracket, classify a high-power deck with target B3, and confirm the paste artifact is useful in one round-trip.
    3. Confirm with the flag OFF that /bracket is 404 and the tile/tab are gone.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix.</resume-signal>
  <verify>
    <human-check>Operator approves visual correctness across the three themes at both viewports, confirms the classify→balancer flow produces a pasteable artifact, and confirms flag-OFF byte-identity (404 + no tile/tab).</human-check>
  </verify>
  <acceptance_criteria>
    - Operator confirms visual correctness across the three themes at both viewports.
    - Operator confirms the classify→balancer flow produces a pasteable artifact.
    - Operator confirms flag-OFF byte-identity (404 + no tile/tab).
  </acceptance_criteria>
  <done>Operator has typed "approved" (or issues were fixed and re-verified).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local dev flag toggle | Temporary in-test enablement via /Admin/Tools for verification; prod seed unchanged |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-76-15 | Tampering | accidental prod flag flip in commit | mitigate | Flag toggled in the running app only (reverted in afterEach); verify no change to FeatureFlagStore seed is staged/committed; prod stays OFF (FLAG-01 / BRACKET-05). |
| T-76-16 | Information Disclosure | UI browser on Windows host | mitigate | Headless only via scripts/run-web-test.sh + playwright.config webServer (DECKFLOW_DISABLE_AUTO_BROWSER=true); Playwright under WSL; no gstack daemon. |
| T-76-SC | Tampering | npm/pip/cargo installs | accept | No installs; `npx --no-install` uses existing Playwright. |
</threat_model>

<verification>
- `DeckFlow.Web/e2e/bracket-smoke.spec.ts` passes under both viewport projects.
- 6 theme/viewport screenshots captured.
- Live classify→balancer artifact populated.
- Flag-OFF 404 confirmed; prod flag seed unchanged.
- Operator approval recorded.
</verification>

<success_criteria>
- BRACKET-01/03/05: the live tool classifies, balances, and produces a paste artifact; renders correctly across themes + mobile; remains byte-identical/absent when the flag is OFF.
</success_criteria>

<output>
Create `.planning/phases/76-bracket-classifier-balancer/76-06-SUMMARY.md` when done.
</output>
