---
phase: 79-interaction-answers-audit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - DeckFlow.Core/Analysis/StaxProtectionCatalog.cs
  - DeckFlow.Core/Analysis/DeckStatClassifier.cs
  - DeckFlow.Core/Analysis/InteractionAudit.cs
  - DeckFlow.Core/Analysis/InteractionAuditAggregator.cs
  - DeckFlow.Core.Tests/StaxProtectionCatalogTests.cs
  - DeckFlow.Core.Tests/DeckStatClassifierTests.cs
  - DeckFlow.Core.Tests/InteractionAuditAggregatorTests.cs
autonomous: true
requirements: [INTERACT-01, INTERACT-02]
must_haves:
  truths:
    - "DeckStatClassifier exposes IsTargetedRemovalCard and IsProtectionCard predicates (extends the shared classifier, not a forked Contains chain)"
    - "A curated in-repo StaxProtectionCatalog names stax/taxation and protection staples as coarse presence, golden-tested"
    - "InteractionAuditAggregator.Compute buckets a deck's cards into TargetedRemoval, BoardWipes, Counterspells, ProtectionRecursion, StaxTaxation with the actual card names behind each count"
    - "Each bucket separates Confident matches from a borderline Review tier holding weak-signal matches (pseudo-removal: bounce / tuck / temporary exile-and-return / self-target 'you control' / modal-MDFC back-face removal)"
    - "InteractionAudit carries CoverageGaps advisory strings ('0 counterspells', 'no graveyard hate') for buckets whose Confident list is empty"
  artifacts:
    - path: "DeckFlow.Core/Analysis/StaxProtectionCatalog.cs"
      provides: "Curated static stax + protection card-name sets (coarse presence)"
      contains: "StaxProtectionCatalog"
    - path: "DeckFlow.Core/Analysis/DeckStatClassifier.cs"
      provides: "IsTargetedRemovalCard + IsProtectionCard + IsPseudoRemovalCard predicates + borderline detection"
      contains: "IsTargetedRemovalCard"
    - path: "DeckFlow.Core/Analysis/InteractionAudit.cs"
      provides: "InteractionAudit / InteractionBucketResult / InteractionCard / InteractionCardInput records"
      contains: "record InteractionAudit"
    - path: "DeckFlow.Core/Analysis/InteractionAuditAggregator.cs"
      provides: "Compute() that buckets cards + emits confidence tiers + coverage gaps"
      contains: "InteractionAuditAggregator"
    - path: "DeckFlow.Core.Tests/InteractionAuditAggregatorTests.cs"
      provides: "Bucketing / confidence-tier / coverage-gap / modal-MDFC / self-target / pseudo-removal (bounce/tuck/temporary) fixtures"
  key_links:
    - from: "InteractionAuditAggregator.Compute"
      to: "DeckStatClassifier + StaxProtectionCatalog"
      via: "per-card predicate + catalog-membership calls inside the bucketing loop"
      pattern: "IsTargetedRemovalCard|IsProtectionCard|IsPseudoRemovalCard|IsSelfTargetedInteraction|IsBoardWipeCard|IsCounterspellCard|IsRecursionCard|StaxProtectionCatalog"
---

<objective>
Add the pure-Core interaction classification that Phase 79 surfaces: bucket a deck's cards into
targeted removal, board wipes, counterspells, protection/recursion, and stax/taxation — each backed
by the actual card names, with a borderline "review" confidence tier for weak-signal matches and
coverage-gap advisories for empty buckets. Classification extends the shared `DeckStatClassifier`;
stax/protection is coarse presence from a curated in-repo static catalog + golden tests.

Purpose: INTERACT-01 needs card-backed bucket counts; INTERACT-02 needs the gap advisories + a
confidence tier — both are pure CPU transforms over already-resolved Scryfall card data, so they
live in `DeckFlow.Core` where they are unit- and golden-testable without the web host.
Output: one curated catalog class, three new classifier predicates, an `InteractionAudit` model +
aggregator, and extended Core tests (all GREEN on CI).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@CLAUDE.md
@.planning/phases/77-multi-axis-deck-score/77-PATTERNS.md

<interfaces>
<!-- Existing Core surface the aggregator builds on. Extracted from the codebase — use directly. -->

From DeckFlow.Core/Analysis/DeckStatClassifier.cs (existing predicates to REUSE, do not fork):
  public static bool IsBoardWipeCard(string oracleText)
  public static bool IsCounterspellCard(string oracleText)          // "counter target spell"
  public static bool IsRecursionCard(string oracleText)             // graveyard return / reanimate
  public static bool IsInteractionCard(string typeLine, string oracleText)  // broad — NOT the bucket source

From DeckFlow.Core/Analysis/DeckStatAggregator.cs (analog to mirror — the tally-loop shape):
  public readonly record struct DeckStatCardInput(int Quantity, string TypeLine, string OracleText, string ManaCost);
  public static DeckStatSummary Compute(IEnumerable<DeckStatCardInput> cards)  // ArgumentNullException.ThrowIfNull + foreach with quantity<=0 skip + land-skip continue
  public static int EstimateManaValue(string? manaCost)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Curated StaxProtectionCatalog + golden tests</name>
  <read_first>
    - DeckFlow.Web/Data/bracket-data.json (the curated-data analog: an in-repo static list, NOT a package or heuristic — mirror the "curated coarse presence" intent as a Core static class)
    - DeckFlow.Core/Bracket/GameChangerCatalog.cs (Core catalog analog: static readonly HashSet name membership with StringComparer.OrdinalIgnoreCase; find its exact shape and public Contains-style API)
    - DeckFlow.Core/Analysis/DeckStatClassifier.cs (file-scoped namespace DeckFlow.Core.Analysis; XML-doc-on-every-public-member convention)
  </read_first>
  <behavior>
    - StaxProtectionCatalog.IsStax("Rule of Law") == true; IsStax("Winter Orb") == true
    - StaxProtectionCatalog.IsStax("Llanowar Elves") == false
    - StaxProtectionCatalog.IsProtection("Heroic Intervention") == true; IsProtection("Teferi's Protection") == true
    - StaxProtectionCatalog.IsProtection("Lightning Bolt") == false
    - Membership is case-insensitive: IsStax("rule of law") == true
  </behavior>
  <action>
    Create DeckFlow.Core/Analysis/StaxProtectionCatalog.cs — a `public static class StaxProtectionCatalog`
    in namespace `DeckFlow.Core.Analysis` holding two `private static readonly HashSet&lt;string&gt;` sets
    built with `StringComparer.OrdinalIgnoreCase`: a curated `StaxCards` set (taxation/resource-denial
    staples, e.g. Rule of Law, Winter Orb, Static Orb, Thalia Guardian of Thraben, Drannith Magistrate,
    Sphere of Resistance, Trinisphere, Blood Moon, Root Maze, Collector Ouphe, Grand Arbiter Augustin IV)
    and a curated `ProtectionCards` set (protect-your-board / fog-the-turn staples, e.g. Heroic
    Intervention, Teferi's Protection, Boros Charm, Flawless Maneuver, Deflecting Swat, Clever Concealment,
    Veil of Summer). Expose `public static bool IsStax(string name)` and `public static bool IsProtection(string name)`
    that null-guard (return false on null/blank) and test set membership. This is COARSE presence only —
    do NOT attempt oracle-text heuristics here (that is out of scope per REQUIREMENTS.md). Keep the lists
    modest and staple-only; golden tests below pin them. Then create
    DeckFlow.Core.Tests/StaxProtectionCatalogTests.cs mirroring the DeckStatClassifierTests `[Theory]/[InlineData]`
    `_TrueCases`/`_FalseCases` shape, asserting the behavior cases above plus case-insensitivity. Allman
    braces, LF endings, changed-lines only.
  </action>
  <verify>
    <automated>dotnet.exe test DeckFlow.Core.Tests --filter "FullyQualifiedName~StaxProtectionCatalog" 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "public static bool IsStax\|public static bool IsProtection" DeckFlow.Core/Analysis/StaxProtectionCatalog.cs` returns 2
    - StaxProtectionCatalog uses `StringComparer.OrdinalIgnoreCase` for both sets (grep confirms)
    - `dotnet.exe test DeckFlow.Core.Tests --filter "FullyQualifiedName~StaxProtectionCatalog"` exits 0
    - StaxProtectionCatalogTests asserts IsStax("Rule of Law")==true and IsProtection("Lightning Bolt")==false
  </acceptance_criteria>
  <done>Curated stax + protection catalog exists as a pure Core static class with case-insensitive membership and GREEN golden tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add IsTargetedRemovalCard + IsProtectionCard + IsPseudoRemovalCard predicates + borderline detection</name>
  <read_first>
    - DeckFlow.Core/Analysis/DeckStatClassifier.cs (existing single-expression `|| oracleText.Contains(..., StringComparison.OrdinalIgnoreCase)` predicate chains — IsBoardWipeCard/IsCounterspellCard/IsRecursionCard are the exact style to mirror; note the existing tutor-exclusion `.Replace(...)` trick for substring guards. IsInteractionCard at lines 42-48 already matches "destroy target"/"exile target"/"counter target"/"return target spell"/"fight target" — the coarse baseline these narrower bucket predicates refine)
    - DeckFlow.Core/Analysis/StaxProtectionCatalog.cs (Task 1 output — IsProtection name membership this predicate composes with)
    - DeckFlow.Core.Tests/DeckStatClassifierTests.cs (existing `// ----` section-header + `{MethodName}_TrueCases`/`_FalseCases` `[Theory]/[InlineData]` sections)
  </read_first>
  <behavior>
    - IsTargetedRemovalCard("Instant", "Destroy target creature.") == true
    - IsTargetedRemovalCard("Sorcery", "Exile target creature or planeswalker.") == true
    - IsTargetedRemovalCard("Instant", "Target creature gets -3/-3 until end of turn.") == true
    - IsTargetedRemovalCard("Sorcery", "Destroy all creatures.") == false (board wipe, not targeted — belongs to Wipes bucket)
    - IsTargetedRemovalCard("Instant", "Return target creature you control to its owner's hand.") == false (self-target pseudo-removal — belongs to the Review tier, not Confident)
    - IsSelfTargetedInteraction("Instant", "Target creature you control gains hexproof until end of turn.") == true (borderline / review signal)
    - IsPseudoRemovalCard("Instant", "Return target creature to its owner's hand.") == true (bounce — temporary/weak answer → Review tier)
    - IsPseudoRemovalCard("Sorcery", "Put target creature into its owner's library third from the top.") == true (tuck → Review tier)
    - IsPseudoRemovalCard("Instant", "Exile target creature. Return it to the battlefield under its owner's control at the beginning of the next end step.") == true (temporary exile-and-return → Review tier, even though "exile target" would otherwise read as hard removal)
    - IsPseudoRemovalCard("Instant", "Destroy target creature.") == false (permanent hard removal — Confident, NOT pseudo)
    - IsPseudoRemovalCard("Instant", "Return target creature you control to its owner's hand.") == false (self-bounce carries "you control" — routed by IsSelfTargetedInteraction instead, not double-counted as pseudo)
    - IsProtectionCard("Heroic Intervention", "...gain indestructible...") == true (curated-list backed)
    - IsProtectionCard("Lightning Bolt", "Deal 3 damage to any target.") == false
  </behavior>
  <action>
    Add to DeckStatClassifier four `public static bool` predicates in the existing chaining style, XML-doc each:
    (1) `IsTargetedRemovalCard(string typeLine, string oracleText)` — matches "destroy target " / "exile target "
    / (contains "target creature" AND contains "gets -") / "deal" + "damage to target creature", AND EXCLUDES
    board wipes (`!IsBoardWipeCard(oracleText)`) and self-target ("you control") so a targeted removal that only
    hits your own permanents does not count as answers. (2) `IsSelfTargetedInteraction(string typeLine, string oracleText)`
    — the weak/borderline signal: contains "you control" AND (contains "target" ) — used by the aggregator to route
    matches into the Review tier (pseudo-removal, protection-your-own-stuff bounce). (3) `IsPseudoRemovalCard(string typeLine, string oracleText)`
    — the weak/temporary-answer signal for opponents' permanents that does NOT permanently remove: matches bounce
    (`oracleText.Contains("return target", OrdinalIgnoreCase) && (oracleText.Contains("to its owner's hand") || oracleText.Contains("to their owner's hand")) && !oracleText.Contains("you control")`),
    tuck (`oracleText.Contains("target", ...) && (oracleText.Contains("into its owner's library") || oracleText.Contains("on the bottom of") || oracleText.Contains("shuffles it into"))`), and temporary
    exile-and-return (`oracleText.Contains("exile target", ...) && oracleText.Contains("return", ...) && (oracleText.Contains("end step") || oracleText.Contains("end of turn") || oracleText.Contains("next"))`).
    MUST exclude "you control" bounce (that is IsSelfTargetedInteraction's job). (4) `IsProtectionCard(string name, string oracleText)`
    — `StaxProtectionCatalog.IsProtection(name)` OR oracleText contains "gains hexproof" / "gains indestructible" /
    "gain protection from" / "phases out". Use boolean-expression / chained-if style — do NOT introduce switch
    expressions (carve-out). Then extend DeckStatClassifierTests.cs with new `// ----` sections + `IsTargetedRemovalCard_TrueCases`/`_FalseCases`,
    `IsSelfTargetedInteraction_TrueCases`/`_FalseCases`, `IsPseudoRemovalCard_TrueCases`/`_FalseCases`, `IsProtectionCard_TrueCases`/`_FalseCases`
    covering the behavior cases above (bounce/tuck/temporary each asserted true; hard-removal + self-bounce asserted false for pseudo). Changed-lines only.
  </action>
  <verify>
    <automated>dotnet.exe test DeckFlow.Core.Tests --filter "FullyQualifiedName~DeckStatClassifier" 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "public static bool IsTargetedRemovalCard\|IsSelfTargetedInteraction\|IsPseudoRemovalCard\|IsProtectionCard" DeckFlow.Core/Analysis/DeckStatClassifier.cs` returns 4
    - IsTargetedRemovalCard composes `!IsBoardWipeCard` and a "you control" exclusion (grep confirms both guards present)
    - IsPseudoRemovalCard excludes "you control" (grep confirms the guard) and matches bounce, tuck, and temporary exile-and-return signals
    - DeckStatClassifierTests contains `IsTargetedRemovalCard_FalseCases` asserting a "Destroy all creatures" board wipe is NOT targeted removal, and `IsPseudoRemovalCard_TrueCases` asserting a bounce, a tuck, and a temporary exile-and-return card each classify as pseudo-removal
    - `dotnet.exe test DeckFlow.Core.Tests --filter "FullyQualifiedName~DeckStatClassifier"` exits 0
  </acceptance_criteria>
  <done>Four predicates exist, exclude board wipes + self-target from Confident targeted removal, route self-target and bounce/tuck/temporary pseudo-removal to borderline signals; classifier test filter GREEN.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: InteractionAudit model + aggregator + bucketing/gap/tier tests</name>
  <read_first>
    - DeckFlow.Core/Analysis/DeckStatAggregator.cs (the tally-loop analog: ArgumentNullException.ThrowIfNull, `foreach` with `quantity <= 0` skip, land-skip `continue`, per-card classifier `if` blocks, object return — mirror the SHAPE, buckets replace counters)
    - DeckFlow.Core/Analysis/DeckStatClassifier.cs (Task 2 predicates + existing IsBoardWipeCard/IsCounterspellCard/IsRecursionCard to compose)
    - DeckFlow.Core/Analysis/StaxProtectionCatalog.cs (Task 1 IsStax membership)
    - DeckFlow.Core/Bracket/BracketClassification.cs (sealed-record-with-positional-params model analog for InteractionAudit)
    - DeckFlow.Core.Tests/DeckStatAggregatorTests.cs (the `Card()` factory + `Compute_Tallies*` `[Fact]` pattern)
  </read_first>
  <behavior>
    - Compute over [Swords to Plowshares(removal), Wrath of God(wipe), Counterspell(counter), Eternal Witness(recursion), Rule of Law(stax), Heroic Intervention(protection)] buckets each card into exactly its bucket with Name+Quantity in the Confident list
    - A self-target card ("Return target creature you control to its owner's hand") lands in TargetedRemoval.Review, NOT Confident
    - A bounce card ("Return target creature to its owner's hand") lands in TargetedRemoval.Review, NOT Confident (pseudo-removal, weak answer)
    - A tuck card ("Put target creature into its owner's library third from the top") lands in TargetedRemoval.Review, NOT Confident
    - A temporary exile-and-return card ("Exile target creature. Return it to the battlefield ... at the beginning of the next end step.") lands in TargetedRemoval.Review, NOT Confident (even though "exile target" reads as hard removal — the pseudo check runs first)
    - A modal-MDFC whose front face is a Land but whose oracle text carries "Destroy target creature" is classified from the supplied (already-resolved) oracle text, not skipped as a land — fixture asserts it reaches TargetedRemoval.Confident
    - Quantity is preserved: 2x a counterspell yields an InteractionCard with Quantity==2 (buckets are card-distinct; quantity is per card)
    - CoverageGaps contains "0 counterspells" when the Counterspells.Confident list is empty; contains a graveyard-hate/recursion gap advisory when ProtectionRecursion.Confident is empty
    - Empty input yields all five buckets empty and the full set of coverage-gap advisories
  </behavior>
  <action>
    Create DeckFlow.Core/Analysis/InteractionAudit.cs with: `public readonly record struct InteractionCardInput(int Quantity, string Name, string TypeLine, string OracleText, string ManaCost);`
    `public readonly record struct InteractionCard(string Name, int Quantity);`
    `public sealed record InteractionBucketResult(IReadOnlyList&lt;InteractionCard&gt; Confident, IReadOnlyList&lt;InteractionCard&gt; Review);`
    and `public sealed record InteractionAudit(InteractionBucketResult TargetedRemoval, InteractionBucketResult BoardWipes, InteractionBucketResult Counterspells, InteractionBucketResult ProtectionRecursion, InteractionBucketResult StaxTaxation, IReadOnlyList&lt;string&gt; CoverageGaps);`
    XML-doc every public member. Create DeckFlow.Core/Analysis/InteractionAuditAggregator.cs — `public static class InteractionAuditAggregator`
    with `public static InteractionAudit Compute(IEnumerable&lt;InteractionCardInput&gt; cards)`. ArgumentNullException.ThrowIfNull;
    build five `List&lt;InteractionCard&gt;` Confident + five Review lists; `foreach` skipping `Quantity &lt;= 0`; do NOT
    land-skip (an MDFC land face can still carry removal text — classify from OracleText). Per card, run the
    removal/wipe/counter chain as ORDERED `else if`s so a card lands in at most one of these mutually-exclusive
    removal-family buckets, and place the pseudo/self-target checks BEFORE IsTargetedRemovalCard so a
    temporary exile-and-return card (which also matches "exile target") is routed to Review, not Confident:
    if `DeckStatClassifier.IsBoardWipeCard(oracle)` → BoardWipes.Confident;
    else if `IsCounterspellCard(oracle)` → Counterspells.Confident;
    else if `IsSelfTargetedInteraction(typeLine, oracle)` → TargetedRemoval.Review;
    else if `IsPseudoRemovalCard(typeLine, oracle)` → TargetedRemoval.Review;
    else if `IsTargetedRemovalCard(typeLine, oracle)` → TargetedRemoval.Confident.
    Then, as INDEPENDENT (non-`else`) checks so a card can also be protection/recursion or stax:
    if `IsRecursionCard(oracle) || IsProtectionCard(name, oracle)` → ProtectionRecursion.Confident;
    if `StaxProtectionCatalog.IsStax(name)` → StaxTaxation.Confident.
    Build CoverageGaps: append a fixed advisory string for every bucket whose Confident list is empty — exact strings
    "0 counterspells", "no board wipes", "no targeted removal", "no protection or recursion (possible graveyard-hate /
    protection gap)", "no stax or taxation". Return the InteractionAudit. Then create
    DeckFlow.Core.Tests/InteractionAuditAggregatorTests.cs using a `Card()` factory mirroring DeckStatAggregatorTests,
    with `[Fact]` tests for: bucketing the six-card fixture, the self-target→Review case, the bounce→Review case,
    the tuck→Review case, the temporary exile-and-return→Review case, the modal-MDFC land-face removal→Confident
    case, quantity preservation, and the empty-input coverage-gap set. Changed-lines only.
  </action>
  <verify>
    <automated>dotnet.exe test DeckFlow.Core.Tests --filter "FullyQualifiedName~InteractionAuditAggregator" 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - InteractionAudit.cs declares a `sealed record InteractionAudit` with the five `InteractionBucketResult` members + `IReadOnlyList<string> CoverageGaps`
    - InteractionAuditAggregator.Compute composes `IsBoardWipeCard`, `IsCounterspellCard`, `IsSelfTargetedInteraction`, `IsPseudoRemovalCard`, `IsTargetedRemovalCard`, `IsRecursionCard`, `IsProtectionCard`, and `StaxProtectionCatalog.IsStax` (grep confirms all eight references)
    - The removal-family chain places `IsSelfTargetedInteraction` and `IsPseudoRemovalCard` BEFORE `IsTargetedRemovalCard` (grep/read confirms ordering so temporary exile-and-return routes to Review)
    - InteractionAuditAggregatorTests asserts a "you control" self-target card, a bounce card, a tuck card, and a temporary exile-and-return card are each in `TargetedRemoval.Review` and NOT in `TargetedRemoval.Confident`
    - InteractionAuditAggregatorTests asserts empty input yields a CoverageGaps list containing "0 counterspells"
    - `dotnet.exe test DeckFlow.Core.Tests --filter "FullyQualifiedName~InteractionAuditAggregator"` exits 0
    - `dotnet.exe build DeckFlow.Core` reports 0 warnings, 0 errors
  </acceptance_criteria>
  <done>InteractionAudit model + aggregator bucket cards with card names, separate a Review tier holding pseudo-removal (bounce/tuck/temporary) + self-target, and emit coverage-gap advisories; Core builds clean and the aggregator test filter is GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| (none new) | Pure Core CPU transform over already-resolved deck card data; no I/O, no DI, no network, no new external input |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-79-01-01 | Tampering | StaxProtectionCatalog curated static list | accept | In-repo compiled constant data; no external load, no injection vector; golden tests pin membership |
| T-79-01-02 | Information Disclosure | predicate oracle-text / name matching | accept | Reads only in-process card text/names already resolved upstream; produces card-name lists + integer counts; no PII, no external surface |
| T-79-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages introduced (REQUIREMENTS.md zero-new-deps); Core-only code |
</threat_model>

<verification>
- `dotnet.exe build DeckFlow.Core` clean (0/0)
- Core test filters for StaxProtectionCatalog, DeckStatClassifier, InteractionAuditAggregator all GREEN
- CI (GitHub Actions) green after push — the authoritative gate (WSL VSTest is unreliable)
- Changed-lines format gate clean (`scripts/format-check-changed.sh staged`): no switch expressions introduced, no raw-string re-indent, LF endings preserved
</verification>

<success_criteria>
The five interaction buckets exist as pure-Core classification backed by card names, with a borderline
Review tier for pseudo-removal (bounce / tuck / temporary exile-and-return) / self-target / modal-MDFC signals and coverage-gap advisories for empty
buckets; stax/protection is coarse curated-list presence; all Core tests GREEN on CI.
</success_criteria>

<output>
Create `.planning/phases/79-interaction-answers-audit/79-01-SUMMARY.md` when done
</output>
</output>
</content>
</invoke>
