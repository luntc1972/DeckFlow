---
phase: 37.5-rebuild-kb-corpus
plan: 03
type: execute
wave: 3
depends_on: ["37.5-01", "37.5-02"]
files_modified: []
autonomous: false
requirements: [REBUILD-01, REBUILD-02, REBUILD-03, REBUILD-04, REBUILD-05]
must_haves:
  truths:
    - "After reset, the corpus is empty; blocked_videos + content_sources preserved"
    - "The prod reset runs via `corpus-reset --connection-string <postgres>` (the Plan 01 Postgres path), not ad-hoc SQL"
    - "Distill runs on the subscription claude CLI ($0); a metered provider fails closed (no classifier)"
    - "Salubrious Snail re-harvest under the new bar drops a sampled junk video (status filtered)"
    - "Published entries show clips with real, distinct, non-zero mid-video timestamps"
    - "Only operator-published survivors appear on /content-kb"
    - "/content-kb browses cleanly and each entry's Copy artifact works"
  artifacts:
    - path: ".planning/phases/37.5-rebuild-kb-corpus/37.5-03-SUMMARY.md"
      provides: "Live-exercise record: reset counts (local + prod via --connection-string), harvest/distill counts, filtered sample, clip spot-checks, publish + browse verification"
  key_links:
    - from: "corpus-reset (local --db + prod --connection-string)"
      to: "harvest → distill (subscription classifier) → operator publish → /content-kb browse"
      via: "end-to-end pilot run for Salubrious Snail"
      pattern: "content-kb"
---

<objective>
Run the end-to-end pilot exercise on the rebuilt pipeline (REBUILD-01..05): reset the corpus
(local via `--db` AND prod via `--connection-string`), re-harvest Salubrious Snail under the new
quality bar, distill with the subscription classifier + clip-timestamp fix, have the operator
publish the survivors via the admin grid, and verify the browse site + Copy artifact. REBUILD-04
(operator publish) and REBUILD-05 (browse + copy) require ZERO code — they are operator/verification
steps confirmed already-built by RESEARCH.

Purpose: Prove the rehabilitated corpus is high-signal, correctly timestamped, and operator-gated.
Output: A verified, populated /content-kb plus a SUMMARY recording the live results.

REVIEW-FIX ALIGNMENT (Codex peer review): Plan 01 added a Postgres-capable `corpus-reset
--connection-string` path (FINDING 2), so the prod reset is now a concrete CLI command — this plan
no longer leaves "direct psql" as an alternative. Plan 02 made the classifier fail closed on metered
providers (FINDING 4), so the distill step MUST run on the subscription claude CLI; if the operator
forgets to set the subscription provider, distill exits with the "classifier requires the
subscription LLM CLI" error rather than silently billing an unmetered call — STOP and set the
provider, do not work around it.
</objective>

<scope_fence>
ALLOWED CHANGES: none to source code. This plan is operator-driven (CLI runs + admin grid +
browser verification) plus the SUMMARY write. Do NOT edit any .cs / view / CSS file here. If a
defect surfaces during the live run, STOP and report it for a gap-closure plan — do not hot-patch.
The prod reset is DESTRUCTIVE and IRREVERSIBLE: it wipes prod content_* and the operator's prior
publish (is_visible) decisions. Do NOT run the prod reset without the explicit blocking-human
approval in Task 1. CONTEXT.md accepts this loss (the old corpus is gate-condemned junk).
</scope_fence>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37.5-rebuild-kb-corpus/37.5-CONTEXT.md
@.planning/phases/37.5-rebuild-kb-corpus/37.5-RESEARCH.md
@.planning/phases/37.5-rebuild-kb-corpus/37.5-01-SUMMARY.md
@.planning/phases/37.5-rebuild-kb-corpus/37.5-02-SUMMARY.md

<runbook>
WSL → Windows dotnet (env quirks per RESEARCH + CLAUDE.md memory):
- Use `"/mnt/c/Program Files/dotnet/dotnet.exe" run --project DeckFlow.CLI -- <command>`.
- For distill on Windows, `DECKFLOW_LLM_CLI_COMMAND` must be set (the default `claude` auto-resolve
  is Linux/macOS only). Pass env to Windows dotnet via cmd/.bat `set` (a WSL export will not cross
  the boundary). Subscription claude CLI = $0; no OPENAI_API_KEY needed.
- SUBSCRIPTION PROVIDER REQUIRED FOR DISTILL (Plan 02 fail-closed): set `DECKFLOW_LLM_PROVIDER` to a
  subscription provider (e.g. the claude CLI backend) so isSubscriptionProvider is true. If it is
  unset/openai, `distill` exits non-zero with "classifier requires the subscription LLM CLI" and
  does nothing — set the provider and re-run; never bypass the guard.
- Read run logs from `logs/cli-<date>.log` by timestamp (not `tail`).
- Salubrious Snail channel: https://www.youtube.com/@salubrioussnail (README line 558).
- Local default DB: `<cwd>/artifacts/content-kb.db` (use `corpus-reset` with no `--connection-string`).
- Prod is Render Postgres: reset via `corpus-reset --connection-string "<render postgres connection
  string>"` (the Postgres path added in Plan 01). The connection string is a secret — supply it from
  the Render dashboard at the prompt; do NOT paste it into the SUMMARY, logs, or commits.
- content_sources is preserved by the reset, so the existing Salubrious Snail source row remains;
  re-register only if it is missing.
</runbook>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 1: Reset the corpus (local --db + PROD --connection-string) — destructive, operator-coordinated</name>
  <what-built>The `corpus-reset` CLI command (Plan 01) that purges content_videos (cascade) + content_site_index while preserving blocked_videos + content_sources, on both SQLite (--db) and Postgres (--connection-string).</what-built>
  <how-to-verify>
    1. LOCAL dry-run first: `"/mnt/c/Program Files/dotnet/dotnet.exe" run --project DeckFlow.CLI -- corpus-reset --dry-run` — confirm it reports the would-delete counts and deletes nothing.
    2. LOCAL reset: `... corpus-reset` — confirm the structured log reports content_videos_deleted and site_index_deleted counts.
    3. LOCAL verify-empty + preserved: query artifacts/content-kb.db — `SELECT COUNT(*) FROM content_videos;` and `FROM content_site_index;` both 0; `SELECT COUNT(*) FROM blocked_videos;` and `FROM content_sources;` UNCHANGED from before the reset.
    4. PROD (only after approval): capture the prod publish state first (`SELECT id,is_visible FROM content_site_index` against prod → save to the SUMMARY), then run `... corpus-reset --connection-string "<render postgres connection string>"` (and `--dry-run` first to preview). Confirm prod content_videos + content_site_index empty and prod blocked_videos + content_sources preserved. Do NOT record the connection string anywhere.
    EXPECTED: both DBs have empty content_* corpus; blocked_videos + content_sources intact on each; the prod reset used the `--connection-string` Postgres path (no ad-hoc SQL).
  </how-to-verify>
  <resume-signal>Type "approved" once local + prod resets are verified empty (and preserved tables intact), or describe any discrepancy (e.g., a preserved table lost rows, a FK violation on prod, or the --connection-string path errored).</resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 2: Re-harvest + distill Salubrious Snail under the new bar (subscription provider)</name>
  <what-built>The harvest pipeline (existing) + the subscription-only classifier gate + clip-timestamp fix (Plans 01-02): drop-class videos are marked `filtered`, have stale rows cleaned, and never indexed; kept videos get real mid-video clip timestamps; an all-zero distill is rejected on the live path; a metered provider fails closed.</what-built>
  <how-to-verify>
    1. Confirm the Salubrious Snail source exists (preserved by reset): `... content-source-add --url https://www.youtube.com/@salubrioussnail --name "Salubrious Snail"` only if missing.
    2. Harvest: `... harvest --limit 50` — captions fetched; confirm a sample stored transcript body contains `[mm:ss]` markers (query content_transcripts.body for one video).
    3. Confirm the subscription provider is set (DECKFLOW_LLM_PROVIDER + DECKFLOW_LLM_CLI_COMMAND). Distill: `... distill --limit 50` — watch logs/cli-<date>.log. Confirm: (a) the run did NOT fail closed (i.e. the subscription provider was honored — no "classifier requires the subscription LLM CLI" error); (b) at least one video logs `filtered ... reason=...` (a sampled junk video — e.g. a trivia/news/intro-only clip — is dropped, status `filtered`, NOT in content_site_index); (c) kept videos distill with non-`[00:00]` clip timestamps; (d) NO distill throws the all-zero-timestamp guard.
    4. Spot-check 2-3 kept entries' clips against the actual video: timestamps point at the advice moment and are distinct/non-zero.
    EXPECTED: distill runs under the subscription provider ($0); a high-signal subset is distilled with correct timestamps; obvious junk is filtered out (REBUILD-02, REBUILD-03).
  </how-to-verify>
  <resume-signal>Type "approved" with: count distilled, count filtered, the filtered sample id+reason, and the clip spot-check result. If distill failed closed (metered provider), set the subscription provider and re-run. If the all-zero guard fired or timestamps are still 00:00, describe it (likely the fetcher change didn't take — STOP for gap closure).</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Operator publish (admin grid) + browse + Copy artifact verification</name>
  <what-built>The Phase 33 admin curation grid (unchanged) — new distills default hidden (is_visible=false); the operator publishes survivors. The Phase 22 + Phase 37 browse site + detail + Copy artifact (unchanged, content.kb.enabled ON).</what-built>
  <how-to-verify>
    1. Export the refreshed site index so the web app can load it: `... content-index-export` (updates the committed index-seed.json the web app reads via ContentKbSeedLoader); deploy/refresh as the prod flow requires.
    2. In the admin grid (/Admin/ContentKb): confirm newly distilled survivors appear HIDDEN by default; publish the genuinely useful ones (REBUILD-04 hybrid gate). Confirm filtered videos are absent entirely.
    3. Browse /content-kb: only the operator-published entries appear; the dropped/junk and the unpublished do NOT (REBUILD-05).
    4. Open 2-3 published entry detail pages: summary + clips render HTML-encoded; clip timestamps are real/distinct/non-zero; the "Copy artifact" button copies clean text that pastes into ChatGPT/Claude without reformatting.
    EXPECTED: /content-kb lists only published high-signal entries with correct clip times; Copy artifact works (REBUILD-04, REBUILD-05).
  </how-to-verify>
  <resume-signal>Type "approved" once browse shows only published survivors with correct clip timestamps and Copy artifact works, or describe the issue (e.g., an unpublished entry leaked, a clip still shows 00:00, or copy text is malformed).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator CLI → PROD Postgres (`--connection-string`) | Irreversible destructive reset of production corpus + publish state via a secret connection string |
| harvested/distilled text → /content-kb detail render | Untrusted creator text rendered to the browser |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37.5-09 | Elevation of Privilege / Denial of Service | PROD corpus reset (`--connection-string`) | mitigate | Gated behind an explicit blocking-human approval (Task 1); local dry-run + local reset verified BEFORE prod; prod dry-run previewed before the real reset; operator captures prod publish state first; reset scoped to content_* (preserves blocked_videos + content_sources). CONTEXT.md explicitly accepts the one-time publish-state loss |
| T-37.5-10 | Information Disclosure | prod Postgres connection string | mitigate | The connection string is a secret supplied at the prompt; Plan 01's runner logs only provider + row counts (never the string); the operator must NOT paste it into the SUMMARY, logs, or commits (scope_fence + runbook) |
| T-37.5-11 | Tampering (stored XSS) | /content-kb detail render | accept | Already mitigated by Phase 37: Markdig `.DisableHtml()` pipeline + HtmlString encoding; artifact path validated under allowedRoot. No new exposure in this plan (verification-only) |
| T-37.5-12 | Repudiation | live-run record | mitigate | SUMMARY records reset row counts (local + prod), distilled/filtered counts, the filtered sample, clip spot-checks, and publish/browse verification — an auditable record of the destructive prod operation (without the connection string) |
| T-37.5-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs — verification-only plan, zero new dependencies |
</threat_model>

<verification>
- Local: content_videos + content_site_index empty after reset; blocked_videos + content_sources preserved. Prod: same, via `corpus-reset --connection-string`.
- Distill ran under the subscription provider (no fail-closed error); log shows ≥1 `filtered` video (not indexed) and kept videos with non-zero clip timestamps; no all-zero-guard throw.
- /content-kb lists only operator-published survivors; Copy artifact produces clean paste-ready text.
- SUMMARY records all counts + spot-checks (including the destructive prod reset) — never the connection string.
</verification>

<success_criteria>
1. After reset the corpus is empty (local via --db + prod via --connection-string); preserved tables intact.
2. Distill ran on the subscription provider; only quality-filtered Salubrious Snail deck-advice/philosophy videos are indexed; a sampled junk video is demonstrably dropped (status filtered).
3. Published entries have clips with real, distinct, non-zero mid-video timestamps (spot-checked).
4. /content-kb lists only operator-published survivors; each entry's Copy artifact works.
</success_criteria>

<output>
Create `.planning/phases/37.5-rebuild-kb-corpus/37.5-03-SUMMARY.md` when done — include reset row
counts (local + prod via --connection-string), distilled/filtered counts, the filtered sample
id+reason, clip spot-check notes, and the publish/browse/copy verification result. Do NOT include
the prod connection string.
</output>
