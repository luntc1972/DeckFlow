---
phase: 108
plan: 02
title: JSON Mutation Endpoint Patch Responses
type: execute
wave: 2
depends_on: [108-01]
files_modified:
  - DeckFlow.Web/Controllers/Api/CutLabApiController.cs
  - DeckFlow.Web/Models/Api/CutLabDecideApiResponse.cs
  - DeckFlow.Web/Models/Api/CutLabAdjustApiResponse.cs
  - DeckFlow.Web/Models/Api/CutLabWhatifApiResponse.cs
  - DeckFlow.Web.Tests/CutLabApiControllerTests.cs
autonomous: true
requirements: [CLUP-01, CLUP-03]
requirements_addressed: [CLUP-01, CLUP-03]
status: planned
must_haves:
  truths:
    - "Decide, adjust, and what-if commit JSON success responses return a server-authored Patch object."
    - "Endpoint protection and error behavior stay unchanged while success payload shaping moves to the patch builder."
    - "Legacy transition fields, if retained, exactly mirror Patch fields."
  artifacts:
    - path: "DeckFlow.Web/Controllers/Api/CutLabApiController.cs"
      provides: "Mutation endpoint patch response wiring"
      contains: "_patchBuilder"
    - path: "DeckFlow.Web/Models/Api/CutLabDecideApiResponse.cs"
      provides: "Decide response Patch property"
      contains: "CutLabUiPatchDto"
    - path: "DeckFlow.Web/Models/Api/CutLabAdjustApiResponse.cs"
      provides: "Adjust response Patch property"
      contains: "CutLabUiPatchDto"
    - path: "DeckFlow.Web/Models/Api/CutLabWhatifApiResponse.cs"
      provides: "What-if commit Patch property with preview null behavior"
      contains: "CutLabUiPatchDto"
  key_links:
    - from: "DeckFlow.Web/Controllers/Api/CutLabApiController.cs"
      to: "DeckFlow.Web/Services/CutLab/CutLabUiPatchBuilder.cs"
      via: "_patchBuilder.BuildAsync after successful mutation"
      pattern: "_patchBuilder\\.BuildAsync"
    - from: "DeckFlow.Web.Tests/CutLabApiControllerTests.cs"
      to: "DeckFlow.Web/Models/Api/CutLabUiPatchDto.cs"
      via: "response contract assertions"
      pattern: "Patch"
---

# Plan 108-02 — JSON Mutation Endpoint Patch Responses

<objective>
Wire Cut Lab JSON mutation endpoints to return the shared server-authored patch after decisions, quantity adjustments, and what-if commits.
</objective>

<threat_model>
T-108-03: Response-shape changes could accidentally drop same-origin, feature-flag, request-size, or invalid-state protections. Mitigation: keep existing endpoint attributes and validation branches; only change success payload shaping after mutation rules pass.

T-108-04: What-if commit could produce a patch for a half-applied swap. Mitigation: preserve the existing restore-then-accept atomicity check and return `BadRequest` before patch building when the replacement cut is refused.
</threat_model>

<must_haves>
- `POST /api/cut-lab/decide` returns a patch for the post-decision state.
- `POST /api/cut-lab/adjust` returns a patch for the post-adjustment state.
- `POST /api/cut-lab/whatif/commit` returns a patch for the committed swap state.
- `POST /api/cut-lab/whatif` preview stays non-mutating and does not need a full patch in Phase 108.
- Existing `FeatureFlagGate("tool.cut-lab.enabled")`, `RequestSizeLimit`, and same-origin checks remain intact.
</must_haves>

<tasks>
<task id="108-02-01" type="auto">
<name>Task 1: Return patch from decide success responses</name>
<title>Return patch from decide success responses</title>
<files>DeckFlow.Web/Controllers/Api/CutLabApiController.cs, DeckFlow.Web/Models/Api/CutLabDecideApiResponse.cs, DeckFlow.Web.Tests/CutLabApiControllerTests.cs, DeckFlow.Web.Tests/CutLabWhatifTests.cs</files>
<read_first>
- DeckFlow.Web/Controllers/Api/CutLabApiController.cs
- DeckFlow.Web/Models/Api/CutLabDecideApiResponse.cs
- DeckFlow.Web.Tests/CutLabApiControllerTests.cs
- DeckFlow.Web.Tests/CutLabWhatifTests.cs
</read_first>
<action>
Update `CutLabApiController` constructor to require `ICutLabUiPatchBuilder patchBuilder`, assign it to a private readonly `_patchBuilder` field, and throw `ArgumentNullException` for null. Update all test controller factories in `CutLabApiControllerTests.cs` and `CutLabWhatifTests.cs` to pass a fake or real builder as appropriate. Add `public CutLabUiPatchDto Patch { get; init; } = new();` to `CutLabDecideApiResponse`. In `PostDecideAsync`, after applying the decision and computing any current-card `floorWarnings`, call `_patchBuilder.BuildAsync` for the updated state and delegate the post-apply response shaping (proposal, deltas, findings, counts, export eligibility, what-if options) to it. Pass the already-resolved after-state `preResolvedCards`/`poolKey` and the current-card `floorWarnings` into `BuildAsync`, and **remove the now-redundant in-controller recomputation of the after-state analysis context, round plan, and `ComputeProposalDeltas` simulation** so a decide runs the (4000-trial) simulation once, not twice — this matters on the Render 512MB Basic tier. Return the patch in the response. If top-level legacy properties remain for transition, populate them from `patch` and add tests that prove top-level `CutLabStateJson`, `NextProposal`, `ProposalDeltas`, `FloorWarnings`, `CardsRemaining`, `CutsMade`, `StructuralFindings`, `ComboDataAvailable`, and `CategoryDataAvailable` exactly mirror `Patch`.
</action>
<acceptance_criteria>
- `CutLabApiController` has a private readonly `ICutLabUiPatchBuilder _patchBuilder` field.
- The constructor throws `ArgumentNullException` for `patchBuilder`.
- `CutLabDecideApiResponse` contains `Patch`.
- `PostDecideAsync_Accept_AppendsAcceptedDecisionAndRoundTripsState` asserts `payload.Patch.CutLabStateJson` deserializes to the updated state.
- A decide test asserts `payload.Patch.CardsRemaining == payload.CardsRemaining` if legacy top-level fields remain.
- A decide test asserts `payload.Patch.NextProposal.CardName` is the server-selected next proposal.
- A decide test asserts current-card floor warnings are present in `payload.Patch.FloorWarnings` when accepting a card would break a floor.
</acceptance_criteria>
<verify>
<automated>dotnet.exe test DeckFlow.Web.Tests --filter "CutLabApiControllerTests|CutLabWhatifTests"</automated>
</verify>
<done>Decide success responses expose `Patch`, legacy transition fields mirror patch data, and controller construction is wired for all endpoint tests.</done>
</task>

<task id="108-02-02" type="auto">
<name>Task 2: Return patch from adjust success responses</name>
<title>Return patch from adjust success responses</title>
<files>DeckFlow.Web/Controllers/Api/CutLabApiController.cs, DeckFlow.Web/Models/Api/CutLabAdjustApiResponse.cs, DeckFlow.Web.Tests/CutLabApiControllerTests.cs</files>
<read_first>
- DeckFlow.Web/Controllers/Api/CutLabApiController.cs
- DeckFlow.Web/Models/Api/CutLabAdjustApiResponse.cs
- DeckFlow.Web.Tests/CutLabApiControllerTests.cs
</read_first>
<action>
Add `public CutLabUiPatchDto Patch { get; init; } = new();` to `CutLabAdjustApiResponse`. In `PostAdjustAsync`, after `CutLabAdjustmentApplier.Apply`, call `_patchBuilder.BuildAsync` for the adjusted state and return the patch. If `CutLabStateJson` and `CardsRemaining` remain as transition fields, populate them from `patch.CutLabStateJson` and `patch.CardsRemaining`. Add tests that assert `payload.Patch.QuantityTuners` contains the adjusted card row with server-authored current quantity, legal max, and add/remove disabled state.
</action>
<acceptance_criteria>
- `CutLabAdjustApiResponse` contains `Patch`.
- `PostAdjustAsync_AddBasic_ReturnsUpdatedStateAndCardsRemaining` asserts `payload.Patch.CutLabStateJson` contains the `Island` added-basic adjustment.
- `PostAdjustAsync_AddBasic_ReturnsUpdatedStateAndCardsRemaining` asserts `payload.Patch.CurrentCount == 100` for the existing fixture after adding two Islands.
- `PostAdjustAsync_AddBasic_ReturnsUpdatedStateAndCardsRemaining` asserts `payload.Patch.CanBuildExport == true`.
- `PostAdjustAsync_AddBasic_ReturnsUpdatedStateAndCardsRemaining` asserts `payload.Patch.QuantityTuners` includes the adjusted basic row with matching current quantity and disabled button state.
- `PostAdjustAsync_IntMaxValueDelta_ReturnsBoundedCardsRemaining` asserts `payload.Patch.CardsRemaining == payload.CardsRemaining` if legacy top-level fields remain.
</acceptance_criteria>
<verify>
<automated>dotnet.exe test DeckFlow.Web.Tests --filter CutLabApiControllerTests</automated>
</verify>
<done>Adjust success responses expose a complete patch, including server-authored quantity tuner row state required for visible quantity legality parity.</done>
</task>

<task id="108-02-03" type="auto">
<name>Task 3: Return patch from what-if commit success responses</name>
<title>Return patch from what-if commit success responses</title>
<files>DeckFlow.Web/Controllers/Api/CutLabApiController.cs, DeckFlow.Web/Models/Api/CutLabWhatifApiResponse.cs, DeckFlow.Web.Tests/CutLabApiControllerTests.cs, DeckFlow.Web.Tests/CutLabWhatifTests.cs</files>
<read_first>
- DeckFlow.Web/Controllers/Api/CutLabApiController.cs
- DeckFlow.Web/Models/Api/CutLabWhatifApiResponse.cs
- DeckFlow.Web.Tests/CutLabApiControllerTests.cs
- DeckFlow.Web.Tests/CutLabWhatifTests.cs
</read_first>
<action>
Add `public CutLabUiPatchDto? Patch { get; init; }` to `CutLabWhatifApiResponse`. Keep preview responses with `Patch == null`. `PostWhatifCommitAsync` is currently synchronous (declared `Task<ActionResult<...>>` and returning `Task.FromResult(...)` at every branch); **convert it to a genuine `async Task<ActionResult<...>>`** so it can `await _patchBuilder.BuildAsync(afterSwap, ...)`. Every existing early-return guard must be preserved and must still return BEFORE any patch is built: same-origin 403, missing/invalid/empty-state `BadRequest`, no-change `BadRequest`, and — critically — the existing atomic restore-then-accept overshoot `BadRequest` (T-108-04). Rewrite each `return Task.FromResult<...>(x);` as a plain `return x;`. Only after the atomic validation succeeds, call `_patchBuilder.BuildAsync` for `afterSwap` and return it in `Patch`. If `CutLabStateJson` remains as a transition field, populate it from `patch.CutLabStateJson`.
</action>
<acceptance_criteria>
- What-if preview tests assert `payload.Patch is null`.
- `PostWhatifCommitAsync_SwapsCardsAtomicallyAndTagsAcceptedCutWithWhatifRound` asserts `payload.Patch is not null`.
- The same commit test asserts `payload.Patch.CutsMade` contains `Working Card` with round label `What-if swap`.
- The same commit test asserts `payload.Patch.WhatifCardInOptions` contains the newly cut `Working Card`.
- `PostWhatifCommitAsync_ReturnsBadRequest_WhenReplacementCutWouldOvershootRemainingBudget` still returns `BadRequestObjectResult`, not a success response with patch.
</acceptance_criteria>
<verify>
<automated>dotnet.exe test DeckFlow.Web.Tests --filter "CutLabApiControllerTests|CutLabWhatifTests"</automated>
</verify>
<done>What-if preview remains non-mutating with no patch, and what-if commit returns a complete post-swap patch only after atomic validation succeeds.</done>
</task>
</tasks>

<verification>
- dotnet.exe test DeckFlow.Web.Tests --filter "CutLabApiControllerTests|CutLabWhatifTests|CutLabUiPatchBuilderTests"
</verification>

<success_criteria>
- All JSON mutation success responses expose patch data.
- Error responses and endpoint protections remain unchanged.
- What-if preview remains non-mutating.
- What-if commit remains atomic.
</success_criteria>
