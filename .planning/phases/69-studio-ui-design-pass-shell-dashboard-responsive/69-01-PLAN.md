---
phase: 69-studio-ui-design-pass-shell-dashboard-responsive
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - DeckFlow.Studio/wwwroot/css/studio-theme.css
  - DeckFlow.Studio/Pages/_Layout.cshtml
  - DeckFlow.Studio/wwwroot/css/site.css
autonomous: true
requirements: [STUI-01, STUI-03]
must_haves:
  truths:
    - "A single shared studio-theme.css defines design tokens (color/spacing/type) aligned to the deckflow.gg brand"
    - "Dark mode follows OS preference with no per-page dark rules required"
    - "Stock template color hardcodes (#1b6ec2 / #0071c1) are replaced by tokens"
  artifacts:
    - path: "DeckFlow.Studio/wwwroot/css/studio-theme.css"
      provides: "Shared token home: spacing scale, 60/30/10 palette, light+dark token blocks, 44px floor, reduced-motion guard"
      min_lines: 60
    - path: "DeckFlow.Studio/Pages/_Layout.cshtml"
      provides: "studio-theme.css linked after bootstrap, before scoped styles"
      contains: "studio-theme.css"
  key_links:
    - from: "DeckFlow.Studio/Pages/_Layout.cshtml"
      to: "wwwroot/css/studio-theme.css"
      via: "<link> stylesheet"
      pattern: "studio-theme\\.css"
    - from: "DeckFlow.Studio/wwwroot/css/site.css"
      to: "studio-theme.css tokens"
      via: "var(--studio-accent) / var(--studio-link)"
      pattern: "var\\(--studio-"
---

<objective>
Establish the shared Studio design-token foundation: a new `studio-theme.css` holding the
spacing scale, 60/30/10 brand palette, light + OS-driven dark token blocks, the 44px touch
floor, and the reduced-motion guard — then wire it into `_Layout.cshtml` and re-point the
stock `site.css` color hardcodes at the new tokens.

Purpose: Every downstream Wave-2 plan (shell, dashboard, page responsive/dark) consumes these
tokens. This plan is the contract; it lands first so the others inherit a single source of truth.
Output: `studio-theme.css` (new), updated `_Layout.cshtml`, token-aligned `site.css`.

HARD CONSTRAINT: presentation-only. No `.razor` behavior, no service/store/markup-logic change.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/69-studio-ui-design-pass-shell-dashboard-responsive/69-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create studio-theme.css token home</name>
  <files>DeckFlow.Studio/wwwroot/css/studio-theme.css</files>
  <read_first>
    - DeckFlow.Studio/wwwroot/css/site.css (current stock tokens being superseded)
    - DeckFlow.Web/wwwroot/css/site.css :root (brand light tokens: --accent #2b6cb0, --accent-strong #1e4f82, font stack line 71)
    - DeckFlow.Web/wwwroot/css/admin-common.css :root (dark palette: --bg #0f172a, --panel #1e293b, --text #e2e8f0, --muted #94a3b8, --accent #3b82f6, --border #334155, --danger #dc2626; plus admin-hub-card, color-scheme, prefers-reduced-motion patterns to PORT)
    - .planning/phases/69-studio-ui-design-pass-shell-dashboard-responsive/69-UI-SPEC.md (Spacing Scale, Typography, Color, Responsive & Dark-Mode sections — authoritative values)
  </read_first>
  <action>
    Create the new file `DeckFlow.Studio/wwwroot/css/studio-theme.css` as the single shared token
    home. Declare a `:root` block with these EXACT values from the UI-SPEC.

    Spacing scale (8-point): `--sp-1: 4px`, `--sp-2: 8px`, `--sp-3: 16px`, `--sp-4: 24px`,
    `--sp-5: 32px`, `--sp-6: 48px`, `--sp-7: 64px`.

    Typography tokens: `--studio-font: "Segoe UI", Tahoma, Geneva, Verdana, sans-serif`;
    sizes `--fs-meta: 0.875rem` (14px), `--fs-body: 1rem` (16px), `--fs-title: 1.25rem` (20px),
    `--fs-display: 1.75rem` (28px); weights `--fw-regular: 400`, `--fw-semibold: 600`,
    `--fw-brand: 700` (the single documented 700 exception, wordmark only).

    Light palette tokens (60/30/10): `--studio-bg: #e9edf3` (dominant), `--studio-surface: #fafafa`
    (cards/sidebar/panels), `--studio-input: #ffffff`, `--studio-accent: #2b6cb0`,
    `--studio-accent-strong: #1e4f82`, `--studio-danger: #c53030`, `--studio-text: #1a202c`,
    `--studio-muted: #5a6573`, `--studio-border: #d6dae2`, plus aliases `--studio-link: var(--studio-accent)`
    and `--studio-focus: var(--studio-accent)`.

    On `:root` also set `color-scheme: light dark` so Bootstrap native controls and badges follow
    the OS theme without per-component work.

    Add an OS-driven dark override `@media (prefers-color-scheme: dark) { :root { ... } }` that
    re-points ONLY the color tokens to the dark column: `--studio-bg: #0f172a`,
    `--studio-surface: #1e293b`, `--studio-input: #1e293b`, `--studio-accent: #3b82f6`,
    `--studio-accent-strong: #2563eb`, `--studio-danger: #dc2626`, `--studio-text: #e2e8f0`,
    `--studio-muted: #94a3b8`, `--studio-border: #334155`. Spacing and type tokens are not redeclared.

    Add a `--touch-floor: 44px` token (WCAG 2.5.5 min interactive size, ported from admin-common.css)
    and a `--studio-content-max: 1200px` token (mirrors admin `.admin-content`).

    Add a global reduced-motion guard `@media (prefers-reduced-motion: reduce)` that disables
    transitions/animations on `*` (mirror the site.css / admin-common.css pattern) so downstream
    hover-lift transitions degrade gracefully.

    Do NOT add component selectors here beyond what is needed for tokens and the two global guards —
    shell, dashboard-card, and nav rules land in their own Wave-2 plans. Do NOT introduce any new
    badge colors (the StatusBadge palette is locked elsewhere). New-file line endings: LF.
  </action>
  <verify>
    <automated>test -f DeckFlow.Studio/wwwroot/css/studio-theme.css && grep -q -- '--sp-7: 64px' DeckFlow.Studio/wwwroot/css/studio-theme.css && grep -q 'prefers-color-scheme: dark' DeckFlow.Studio/wwwroot/css/studio-theme.css && grep -q 'prefers-reduced-motion' DeckFlow.Studio/wwwroot/css/studio-theme.css && grep -q '#0f172a' DeckFlow.Studio/wwwroot/css/studio-theme.css</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `DeckFlow.Studio/wwwroot/css/studio-theme.css`.
    - `:root` contains all seven `--sp-*` tokens with exact px values (4/8/16/24/32/48/64).
    - `:root` declares `color-scheme: light dark` and the light palette token values exactly as listed.
    - A `@media (prefers-color-scheme: dark)` block re-points the color tokens to the dark values (#0f172a / #1e293b / #3b82f6 / #e2e8f0 / #334155 / #94a3b8 / #dc2626).
    - Typography tokens present: `--studio-font` with the Segoe UI stack, four `--fs-*` sizes, three `--fw-*` weights.
    - `--touch-floor: 44px`, `--studio-content-max: 1200px`, and a `prefers-reduced-motion: reduce` guard all present.
    - No badge-color declarations and no `.razor` files touched.
    - `dotnet build DeckFlow.Studio/DeckFlow.Studio.csproj` clean (CSS is copied as static content; build must still succeed).
  </acceptance_criteria>
  <done>studio-theme.css exists with the full token set, light+dark blocks, 44px floor, and reduced-motion guard; build clean.</done>
</task>

<task type="auto">
  <name>Task 2: Link studio-theme.css and align site.css to tokens</name>
  <files>DeckFlow.Studio/Pages/_Layout.cshtml, DeckFlow.Studio/wwwroot/css/site.css</files>
  <read_first>
    - DeckFlow.Studio/Pages/_Layout.cshtml (current stylesheet link order: bootstrap → site.css → DeckFlow.Studio.styles.css)
    - DeckFlow.Studio/wwwroot/css/site.css (stock hardcodes: font-family Helvetica Neue line 4; `a, .btn-link { color: #0071c1 }` line 12; `.btn-primary` background #1b6ec2 / border #1861ac lines 16-18)
    - DeckFlow.Studio/wwwroot/css/studio-theme.css (the tokens created in Task 1)
    - .planning/phases/69-studio-ui-design-pass-shell-dashboard-responsive/69-UI-SPEC.md (File Targets table — link goes AFTER bootstrap, BEFORE DeckFlow.Studio.styles.css)
  </read_first>
  <action>
    In `_Layout.cshtml`, add a `<link>` for `css/studio-theme.css` positioned AFTER the
    `bootstrap.min.css` link and AFTER `css/site.css`, but BEFORE the
    `DeckFlow.Studio.styles.css` (scoped bundle) link — so tokens are defined before scoped
    component CSS but the shared theme overrides the stock site.css. Touch only the head link block.

    In `site.css`, change only the stock-hardcode lines: replace the `html, body` font-family
    `'Helvetica Neue', Helvetica, Arial, sans-serif` with `var(--studio-font)`; replace
    `a, .btn-link { color: #0071c1 }` with `color: var(--studio-link)`; replace `.btn-primary`
    `background-color: #1b6ec2` with `var(--studio-accent)` and `border-color: #1861ac` with
    `var(--studio-accent-strong)`. Leave every other rule (blazor-error-ui, validation, etc.)
    byte-stable. Preserve existing LF line endings; touch only the lines that change (changed-lines
    `.editorconfig` gate).
  </action>
  <verify>
    <automated>grep -q 'studio-theme.css' DeckFlow.Studio/Pages/_Layout.cshtml && grep -q 'var(--studio-font)' DeckFlow.Studio/wwwroot/css/site.css && ! grep -q '#1b6ec2' DeckFlow.Studio/wwwroot/css/site.css && ! grep -q '#0071c1' DeckFlow.Studio/wwwroot/css/site.css && /mnt/c/Program\ Files/dotnet/dotnet.exe build DeckFlow.Studio/DeckFlow.Studio.csproj -nologo 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `_Layout.cshtml` has a `studio-theme.css` `<link>` after bootstrap.min.css and after site.css, before DeckFlow.Studio.styles.css.
    - `site.css` font-family uses `var(--studio-font)`; link color uses `var(--studio-link)`; `.btn-primary` uses `var(--studio-accent)` + `var(--studio-accent-strong)`.
    - `#1b6ec2`, `#1861ac`, `#0071c1`, and the Helvetica Neue stack no longer appear in site.css.
    - All other site.css rules unchanged (diff shows only the four hardcode lines + nothing else).
    - `dotnet build DeckFlow.Studio` clean, no new warnings.
    - No `.razor` behavior touched; presentation-only.
  </acceptance_criteria>
  <done>studio-theme.css is loaded in the correct order and site.css references tokens with zero stock color hardcodes remaining; build clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → Studio (local Blazor Server) | DeckFlow.Studio is an operator-only local desktop tool; no public/anonymous traffic. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-69-NA | (all) | studio-theme.css / _Layout.cshtml / site.css | accept | This plan is CSS tokens + a stylesheet link + token references only. No new input, no data flow, no service, no route, no package install. No new attack surface is introduced; presentation-only on a local operator tool. |
</threat_model>

<verification>
- `dotnet build DeckFlow.Studio/DeckFlow.Studio.csproj` clean.
- Grep confirms tokens present, stock hardcodes gone, link order correct.
- Manual (operator): load Studio, confirm fonts/links render branded and OS dark mode flips the palette with no layout break (visual spot-check at one viewport — full sweep is Plan 04).
</verification>

<success_criteria>
- studio-theme.css is the single token home (spacing/type/color, light+dark, 44px floor, reduced-motion).
- _Layout.cshtml loads it in the correct cascade position.
- site.css carries zero stock color/font hardcodes; references tokens instead.
- Build clean; no behavior change.
</success_criteria>

<output>
Create `.planning/phases/69-studio-ui-design-pass-shell-dashboard-responsive/69-01-SUMMARY.md` when done.
</output>
