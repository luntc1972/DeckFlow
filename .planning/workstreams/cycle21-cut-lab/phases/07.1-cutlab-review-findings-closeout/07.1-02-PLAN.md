---
phase: 07.1-cutlab-review-findings-closeout
plan: 02
type: execute
wave: 2
depends_on: ["07.1-01"]
files_modified:
  - DeckFlow.Web/wwwroot/ts/cut-lab.ts
  - DeckFlow.Web/ts-tests/cut-lab-step-tabs.test.ts
autonomous: true
requirements: [F-5, F-5-SIDE-EFFECT, BUILD-EOL]

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "After initialization on a page where the server marked exactly one step tab aria-selected=\"true\", exactly one [role=\"tabpanel\"]:not([hidden]) exists, and it is the panel whose aria-labelledby points at that tab."
    - "Where the server marked NO tab aria-selected=\"true\", all five tabpanels stay visible — content is never stranded behind a tablist with no selection (D-4, kept deliberately)."
    - "First-load collapsed-section state is unchanged: for a user with no stored state, initialization does not force-open the step's primary <details> and does not close the others."
    - "localStorage key deckflow.cutlab.sections is not written at load time."
    - "Clicking a tab still applies the default collapse state as it does today — the side effect was removed from the initialization path only, not from activation."
  artifacts:
    - "DeckFlow.Web/wwwroot/ts/cut-lab.ts — new setVisibleStepPanel(step) primitive; activateStepTab delegates panel visibility to it; attachStepTabHandler calls it on load; applyInitialSectionCollapseState's no-storage branch no longer calls applyDefaultSectionCollapseState (its stored-state branch is unchanged)."
    - "DeckFlow.Web/ts-tests/cut-lab-step-tabs.test.ts — the toHaveLength(5) assertion at :56 is corrected to 1 plus a visible-panel-id content assertion; the fixture gains four data-cutlab-mobile-collapse <details> sections; the afterEach clears window.localStorage."
  key_links:
    - "cut-lab.ts:436-444 applyInitialSectionCollapseState runs from initializeCutLab (:4470) BEFORE attachStepTabHandler; its no-storage branch independently called applyDefaultSectionCollapseState until this plan removes that call — promoting setVisibleStepPanel inside attachStepTabHandler does not by itself close this path."
    - "cut-lab.ts:506-508 applyDefaultSectionCollapseState stays inside activateStepTab and is NOT reachable from the initialization path."
    - "cut-lab.ts:532-534 the no-selected-tab early return is the D-4 fallback and stays exactly as written."
    - "CutLab.cshtml:337,868,1128,1131,1362 render five tabpanels with no hidden attribute; no server-side hidden is added (D-1)."
---

<objective>
Close F-5 and its collapse side effect: exactly one Cut Lab tabpanel is visible after
initialization, and reaching that outcome does not move the first-load collapsed-section default.

Purpose: today all five `role="tabpanel"` sections render visible while one tab claims selection,
and the defect is locked in by an assertion. The obvious implementation — calling
`activateStepTab` on load — also force-opens the step's primary `<details>` and closes the rest
on every page load for a user with no stored state. This plan separates the two concerns.

Output: a panel-visibility primitive both entry paths use, a corrected test, and an assertion
that the collapse default did not move.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/cycle21-cut-lab/PROJECT.md
@.planning/workstreams/cycle21-cut-lab/ROADMAP.md
@.planning/workstreams/cycle21-cut-lab/phases/07.1-cutlab-review-findings-closeout/07.1-CONTEXT.md
@.planning/workstreams/cycle21-cut-lab/phases/07.1-cutlab-review-findings-closeout/07.1-RESEARCH.md
@.planning/workstreams/cycle21-cut-lab/phases/07.1-cutlab-review-findings-closeout/07.1-MUTATION-PROTOCOL.md
@.planning/workstreams/cycle21-cut-lab/phases/07.1-cutlab-review-findings-closeout/07.1-01-SUMMARY.md
</context>

<locked_decisions>
**D-1 (F-5 hiding mechanism) — locked: client-side on initialization. No server-rendered `hidden`.**
`CutLab.cshtml` is not edited. Rationale: server-rendered `hidden` fixes the first paint but
permanently strands the other four steps for a JavaScript-off user, because nothing would ever
un-hide them. Client-side hiding leaves the JS-off path exactly as it is today — all five panels
reachable, which is the honest progressive-enhancement fallback. **The accepted cost is stated,
not discovered later: with JavaScript on, the first paint briefly shows five panels before
initialization hides four.** That flash is accepted for this phase.
Reversibility: **reversible** — adding server-side `hidden` later is an additive change to the
Razor view and does not conflict with anything this plan writes.

**D-2 (collapse side effect) — locked: initialization hides panels WITHOUT the collapse default.**
See `<assumption_delta_decision>` below for the structural form of this.
Reversibility: **reversible**.

**D-4 (no-tab-selected fallback) — locked: KEEP today's behaviour, and assert it.**
`attachStepTabHandler` returns early at `cut-lab.ts:532-534` when no tab carries
`aria-selected="true"`, leaving all five panels visible. Content must never be stranded behind a
tablist that has no selection. The risk is changing this by accident while implementing F-5, so
the existing case named `leaves every panel visible when the server selected no tab` keeps its
`toHaveLength(5)` assertion and gains an explicit statement of why in its name. Match it by name,
not by line: it sits at `cut-lab-step-tabs.test.ts:83` today, but plan 01 runs first and adds three
cases above it.
Reversibility: **reversible**.
</locked_decisions>

<assumption_delta_decision>
**Signal:** `pluralization`. F-5 gives panel-hiding a **second** caller. Until now the only way any
`[role="tabpanel"]` acquired or lost its `hidden` attribute was `activateStepTab`
(`cut-lab.ts:494`), which additionally force-runs `applyDefaultSectionCollapseState`
(`:506-508`), rewrites `aria-selected` / `tabindex` / `is-active` on every tab, and optionally
moves focus. Initialization now needs the panel-visibility behaviour and none of the rest.

**Noun that is now primary:** the **panel-visibility primitive** — "make exactly one step panel
visible" — not "tab activation". Concretely `setVisibleStepPanel(step: number): boolean`.

**Decision: `promote`.** `setVisibleStepPanel` becomes the primary representation of panel
visibility and the single place the `hidden` attribute is written. `activateStepTab` is demoted to
one caller of it: the caller that additionally layers the collapse default, the ARIA/tabindex/class
sweep and optional focus on top. Initialization is the other caller, and it layers nothing.

**Rationale:** adding a second, parallel hiding path while leaving `activateStepTab` the only real
entry point would leave two places that write `hidden`, which is exactly how the current defect
(one path sets `is-active`, the other sets `hidden`, and load reaches only the first) was made.
One writer, two callers.

**Invariant test (accepted, implemented in Task 2):** `every entry path leaves exactly one panel
visible when the server selected a tab`. It parameterises over the initialization path and the
click path and asserts the visible-panel id set has size 1 and matches the selected tab's
`aria-controls`, and — because a panel can stay correctly visible while the wrong tab is treated as
selected — additionally asserts the click path moved `aria-selected` onto the clicked tab and off
the tab it replaced. It goes red the moment a future phase reintroduces a second `hidden` writer
that disagrees, or the moment tab activation stops moving selection. The documented exception — no
tab selected — is asserted separately as D-4.

**A second, independent side-effect source exists outside `attachStepTabHandler`.**
`initializeCutLab` (`cut-lab.ts:4469-4472`) calls `applyInitialSectionCollapseState` (`:436-444`)
*before* it calls `attachStepTabHandler`. That function's no-storage branch
(`readCollapsedSectionIds() === null`) independently calls `applyDefaultSectionCollapseState` on
every load for a user with no stored state — reachable regardless of what
`attachStepTabHandler`/`setVisibleStepPanel` do, because it runs first and is not called from
either of them. Promoting `setVisibleStepPanel` closes the panel-visibility side of F-5 but does
not, by itself, close this collapse-state side effect. Task 1 removes the no-storage branch's call
while leaving the stored-state branch (`restoreSectionCollapseState()`) exactly as written.
</assumption_delta_decision>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Promote the panel-visibility primitive and hide panels on load</name>
  <files>DeckFlow.Web/wwwroot/ts/cut-lab.ts, DeckFlow.Web/ts-tests/cut-lab-step-tabs.test.ts</files>
  <read_first>
    - DeckFlow.Web/wwwroot/ts/cut-lab.ts lines 294-320 (primarySectionIdsByStep, legacyAuxiliarySectionIds, getLocalStorage), 355-444 (getSectionCollapsibles, applyDefaultSectionCollapseState, readCollapsedSectionIds, restoreSectionCollapseState, applyInitialSectionCollapseState), 490-549 (getStepTabs, activateStepTab, attachStepTabHandler), 4469-4472 (initializeCutLab — confirms applyInitialSectionCollapseState runs before attachStepTabHandler on every load) — the exact current state including the 07.1-01 guard
    - DeckFlow.Web/ts-tests/cut-lab-step-tabs.test.ts (all of it, including the cases 07.1-01 added)
    - DeckFlow.Web/ts-tests/cut-lab-section-collapse.test.ts line 38 (`window.localStorage.clear()` inside `afterEach` — the isolation precedent to copy) and lines 43-56 (`restores stored collapsed sections on init` — the existing, unmodified test that already proves applyInitialSectionCollapseState's stored-state branch; read only, this file is NOT edited)
    - DeckFlow.Web/Views/Deck/CutLab.cshtml lines 337, 868, 1128, 1131, 1362 (the five tabpanel openings — read only, this file is NOT edited)
    - DeckFlow.Web/Views/Shared/_WorkflowStepTabs.cshtml lines 10-12 and 31-41 (how currentStep and aria-selected are chosen server-side — read only)
    - .planning/workstreams/cycle21-cut-lab/phases/07.1-cutlab-review-findings-closeout/07.1-MUTATION-PROTOCOL.md
  </read_first>
  <behavior>
    Write these into `ts-tests/cut-lab-step-tabs.test.ts` before editing `cut-lab.ts`, and confirm
    they fail against the current source.

    First extend `renderFixture` so the collapse behaviour is observable at all. Today the fixture
    has no `<details>`, so `getSectionCollapsibles()` returns an empty array and any collapse
    assertion is vacuous. Add four `<details data-cutlab-mobile-collapse>` elements, one inside
    each of panels 1, 2, 4 and 5, with ids exactly `cut-lab-section-lock-pool`,
    `cut-lab-section-cut-rounds`, `cut-lab-section-goals` and `cut-lab-section-export` — these are
    the four ids `primarySectionIdsByStep` maps at `cut-lab.ts:298-303`. Give the fixture a
    starting state that DIFFERS from what `applyDefaultSectionCollapseState(1)` would produce:
    render `cut-lab-section-cut-rounds` with the `open` attribute and the other three without it.
    (`applyDefaultSectionCollapseState(1)` would open `cut-lab-section-lock-pool` and close the
    other three, so any accidental invocation is visible as a flip.)

    The real defect site for the collapse side effect is NOT `attachStepTabHandler`.
    `initializeCutLab` calls `applyInitialSectionCollapseState` (`:436-444`) before it ever calls
    `attachStepTabHandler`, and that function's no-storage branch independently invokes
    `applyDefaultSectionCollapseState`. The case `does not apply the default section collapse
    state on first load` below exercises that call site: it must fail against the unedited
    source, and only Task 1's `<action>` step that edits `applyInitialSectionCollapseState`
    directly makes it pass — the setVisibleStepPanel work in `attachStepTabHandler` does not touch
    it. The stored-state branch of the same function is exercised by the existing, unmodified test
    `cut-lab-section-collapse.test.ts:43` (`restores stored collapsed sections on init`), which
    this plan does not touch and which stays green because that branch's code is unchanged — both
    branches of `applyInitialSectionCollapseState` are therefore tested, one by a new case here and
    one by the pre-existing regression test.

    Add `window.localStorage.clear();` to the `afterEach` block (`:42-46`), matching the isolation
    precedent at `cut-lab-section-collapse.test.ts:38`. Without it, the `<details>` toggle
    listeners this task's fixture wires up leak `deckflow.cutlab.sections` between test cases and
    invalidate both the no-storage assumption below and mutation M6.

    - Correct the assertion at `:56`. It currently asserts `toHaveLength(5)` and so asserts the
      bug. It becomes `toHaveLength(1)`, and gains the content assertion that the single visible
      panel's id is `cut-lab-step-panel-1` — the panel whose `aria-labelledby` is the tab the
      fixture marked `aria-selected="true"`. Do not delete the case; its later half (click Goals,
      one panel visible) still holds and stays.
    - New case `hides every panel except the server-selected one for each selected step`:
      run `renderFixture(n)` then `initialize()` for n in 1, 2, 4 and 5, and assert each time that
      the visible-panel id set is exactly the single id `cut-lab-step-panel-${n}`.
    - New case `does not apply the default section collapse state on first load`: with no stored
      state, after `initialize()` assert `cut-lab-section-cut-rounds` still has `open` and
      `cut-lab-section-lock-pool` still does not, and assert
      `window.localStorage.getItem('deckflow.cutlab.sections')` is `null`. This case exercises
      `applyInitialSectionCollapseState`'s no-storage branch, not `attachStepTabHandler`.
    - New case `still applies the default section collapse state when a tab is clicked`: after
      `initialize()`, click the step 1 tab and assert the flip DID happen —
      `cut-lab-section-lock-pool` is now open and `cut-lab-section-cut-rounds` is not. This is the
      direction control: it proves the collapse default was removed from the initialization path
      only, not deleted outright.
    - Rename the existing case at `:83` to `leaves every panel visible when the server selected no
      tab, so content is never stranded behind an unselected tablist`, keep its
      `toHaveLength(5)` assertion, and add a `// Why:` comment naming this as decision D-4.
  </behavior>
  <action>
    Edit `DeckFlow.Web/wwwroot/ts/cut-lab.ts` only. Do not edit `CutLab.cshtml`,
    `_WorkflowStepTabs.cshtml` or `site.ts`.

    (1) Add a new function `setVisibleStepPanel` immediately above `activateStepTab` (`:494`),
    with the signature `(step: number) => boolean`. It collects
    `document.querySelectorAll<HTMLElement>('[role="tabpanel"]')`, finds the one whose `id` equals
    the template `cut-lab-step-panel-${step}`, and returns `false` WITHOUT touching any panel when
    no such panel exists — this preserves today's abort-before-side-effects semantics at
    `:501-503`. Otherwise it calls `toggleAttribute('hidden', panel !== targetPanel)` on every
    panel and returns `true`. Give it a `// Why:` comment stating it is the single writer of the
    `hidden` attribute on step panels, and that it deliberately carries no collapse, ARIA or focus
    behaviour so the initialization path can use it without those.

    (2) Rewrite `activateStepTab` to delegate: after the existing step-validity check and the
    07.1-01 selection guard, call `setVisibleStepPanel(step)` and return early when it returns
    `false`. Then run the existing collapse-default block (`:506-508`) unchanged, then the
    existing tab ARIA/tabindex/class sweep, then the existing optional focus block. Remove the now
    duplicated local `panels` / `targetPanel` lookup and the `panels.forEach` hide loop, but keep
    a reference to the target panel for the focus block — obtain it from
    `document.getElementById(\`cut-lab-step-panel-${step}\`)` or have `setVisibleStepPanel` hand it
    back; either is acceptable as long as `hidden` is written in exactly one place.

    (3) In `attachStepTabHandler` (`:526`), after the existing `is-active` loop at `:537-539` and
    before the `click` listener registration, call `setVisibleStepPanel` with the selected tab's
    step, read as `Number(selectedTab.dataset.cutLabStep)`. Guard against a non-integer the same
    way `activateStepTab` does. Do NOT call `activateStepTab` here, do NOT call
    `applyDefaultSectionCollapseState` here, and do NOT call `readCollapsedSectionIds` or any
    localStorage writer here. Add a `// Why:` comment naming F-5 and stating that routing this
    through tab activation would run the collapse default on every page load for a user with no
    stored state.

    (4) Leave the early return at `:532-534` exactly as it is. That is decision D-4: with no
    selected tab, nothing is hidden.

    (5) Edit `applyInitialSectionCollapseState` (`:436-444`). Remove the no-storage branch's call
    to `applyDefaultSectionCollapseState` — the final two lines of the function today, which
    compute `selectedStep` from the currently selected tab and pass it to
    `applyDefaultSectionCollapseState`. Retain the stored-state branch exactly as written: when
    `readCollapsedSectionIds() !== null` the function still calls `restoreSectionCollapseState()`
    and returns. After this edit, the no-storage branch does nothing — a user with no stored state
    keeps whatever `<details open>` state the server rendered, and neither branch of this function
    writes `localStorage`. Add a `// Why:` comment naming F-5-SIDE-EFFECT and stating that this
    function runs unconditionally from `initializeCutLab` (`:4470`), independently of
    `attachStepTabHandler`, so step (3) above does not by itself close this path.

    Preserve `cut-lab.ts`'s existing line endings; change only the lines whose content changes.
  </action>
  <verify>
    <automated>cd DeckFlow.Web &amp;&amp; npx tsc --noEmit -p tsconfig.json &amp;&amp; npx vitest run ts-tests/cut-lab-step-tabs.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `cd DeckFlow.Web && npx vitest run ts-tests/cut-lab-step-tabs.test.ts` reports 9 passed / 0 failed. This starting count comes from `07.1-01-PLAN.md:168`, which leaves 6 tests in this file; Task 1 adds 3 (`hides every panel except...`, `does not apply the default section collapse state on first load`, `still applies the default section collapse state when a tab is clicked`) — 6 + 3 = 9.
    - `cd DeckFlow.Web && npx tsc --noEmit -p tsconfig.json` exits 0.
    - `grep -c "toggleAttribute('hidden'" DeckFlow.Web/wwwroot/ts/cut-lab.ts` returns `1` — exactly one writer of the panel `hidden` attribute.
    - `grep -c 'const setVisibleStepPanel' DeckFlow.Web/wwwroot/ts/cut-lab.ts` returns `1`.
    - `sed -n '526,548p' DeckFlow.Web/wwwroot/ts/cut-lab.ts | sed 's|//.*||' | grep -c 'applyDefaultSectionCollapseState'` returns `0` — the initialization path does not reach the collapse default; comments are stripped first so the explanatory `// Why:` line cannot invalidate the gate.
    - `sed -n '/const applyInitialSectionCollapseState/,/^  };/p' DeckFlow.Web/wwwroot/ts/cut-lab.ts | sed 's|//.*||' | grep -c 'applyDefaultSectionCollapseState'` returns `0` — `applyInitialSectionCollapseState`'s no-storage branch no longer reaches the collapse default either; comments stripped first for the same reason as above.
    - `sed -n '/const applyInitialSectionCollapseState/,/^  };/p' DeckFlow.Web/wwwroot/ts/cut-lab.ts | grep -c 'restoreSectionCollapseState()'` returns `1` — the stored-state branch still restores.
    - `grep -c 'applyDefaultSectionCollapseState' DeckFlow.Web/wwwroot/ts/cut-lab.ts` returns exactly `2` — the definition and the one remaining call inside `activateStepTab`. It is NOT "at least 3": the third occurrence a prior version of this gate counted was the defective no-storage call inside `applyInitialSectionCollapseState`, which step (5) of `<action>` deletes. A gate that still asked for 3 would lock that defect back in.
    - `git diff --stat -- DeckFlow.Web/Views/` produces no output — no Razor file changed.
    - `git status --short DeckFlow.Web/wwwroot/js/` produces no output.
  </acceptance_criteria>
  <reversibility rating="reversible">One extracted function plus two call-site changes in a single file; revertable by one commit. No wire format, storage key or server contract moves.</reversibility>
  <done>Exactly one tabpanel is visible after initialization for each of steps 1, 2, 4 and 5; neither `attachStepTabHandler` nor `applyInitialSectionCollapseState`'s no-storage branch runs the collapse default, while its stored-state branch still restores; localStorage is unwritten; clicking a tab still runs the collapse default; and the no-selection fallback still shows all five.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add the cross-entry-path invariant test</name>
  <files>DeckFlow.Web/ts-tests/cut-lab-step-tabs.test.ts</files>
  <read_first>
    - DeckFlow.Web/ts-tests/cut-lab-step-tabs.test.ts (as left by Task 1)
    - DeckFlow.Web/wwwroot/ts/cut-lab.ts (setVisibleStepPanel and its two callers, as left by Task 1)
    - DeckFlow.Web/Views/Shared/_WorkflowStepTabs.cshtml line 35 (aria-controls emission — the id the invariant reads)
  </read_first>
  <behavior>
    Add one case named `every entry path leaves exactly one panel visible when the server selected
    a tab`. It parameterises over the two entry paths and asserts the same invariant for both:

    - Path A, initialization: `renderFixture(2)` then `initialize()`.
    - Path B, activation: `renderFixture(2)`, `initialize()`, then click the step 4 tab.

    For each path assert: the ids of `[role="tabpanel"]:not([hidden])` form a set of size exactly
    1, and that single id equals the `aria-controls` value of the tab currently carrying
    `aria-selected="true"`. Read the expected id from the DOM through `aria-controls`, not from a
    literal — the point of the invariant is that the tab and the panel agree, so hardcoding the
    id would let them drift apart while the test stayed green.

    For Path B only, additionally assert that the click actually moved selection: after clicking
    the step 4 tab, `cut-lab-step-tab-4` carries `aria-selected="true"` and `cut-lab-step-tab-2`
    (the tab `renderFixture(2)` marked selected) carries `aria-selected="false"`. Without this,
    the invariant as first written is satisfiable by a no-op click: if step 2's panel simply stays
    visible because nothing happened, the panel/aria-controls comparison still matches, and a
    regression that makes clicking do nothing would pass silently.

    Note: the fixture at `:17-33` does not currently emit `aria-controls`. Add
    `aria-controls="cut-lab-step-panel-N"` to all five fixture buttons so the fixture matches what
    `_WorkflowStepTabs.cshtml:35` actually emits. That correction is itself part of the fix — a
    fixture that does not emit markup production emits is how a vacuous test is made.
  </behavior>
  <action>
    Implement the case described in `<behavior>` in
    `DeckFlow.Web/ts-tests/cut-lab-step-tabs.test.ts`, and add the missing `aria-controls`
    attributes to the five buttons in `renderFixture`. No production source changes in this task.
    Preserve the file's existing line endings.
  </action>
  <verify>
    <automated>cd DeckFlow.Web &amp;&amp; npx vitest run ts-tests/cut-lab-step-tabs.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `cd DeckFlow.Web && npx vitest run ts-tests/cut-lab-step-tabs.test.ts` reports 10 passed / 0 failed. This starting count comes from Task 1's exit count of 9 (itself sourced from `07.1-01-PLAN.md:168`'s 6 plus Task 1's 3 new cases); Task 2 adds 1 case — 9 + 1 = 10.
    - `grep -c 'aria-controls' DeckFlow.Web/ts-tests/cut-lab-step-tabs.test.ts` returns at least `6` — five fixture buttons plus at least one read in the invariant.
    - The invariant case derives the expected panel id by reading `aria-controls` from the DOM: `grep -c "getAttribute('aria-controls')" DeckFlow.Web/ts-tests/cut-lab-step-tabs.test.ts` returns at least `1`.
    - The invariant case asserts the clicked tab's selection state directly: `grep -c "cut-lab-step-tab-4.*aria-selected" DeckFlow.Web/ts-tests/cut-lab-step-tabs.test.ts` returns at least `1`, and the case also asserts `cut-lab-step-tab-2` is no longer selected after the click.
  </acceptance_criteria>
  <done>One test encodes "tab selection and panel visibility agree, on every entry path", it reads the expected id from the markup rather than from a literal, and it asserts the click actually moved `aria-selected` onto the clicked tab rather than merely leaving the prior panel visible.</done>
</task>

<task type="auto">
  <name>Task 3: Prove F-5 and its side effect with named killing mutations</name>
  <files>DeckFlow.Web/wwwroot/ts/cut-lab.ts (mutated and restored in place), DeckFlow.Web/ts-tests/cut-lab-step-tabs.test.ts (read only)</files>
  <read_first>
    - .planning/workstreams/cycle21-cut-lab/phases/07.1-cutlab-review-findings-closeout/07.1-MUTATION-PROTOCOL.md
    - DeckFlow.Web/wwwroot/ts/cut-lab.ts (as left by Task 1)
    - DeckFlow.Web/ts-tests/cut-lab-step-tabs.test.ts (as left by Task 2)
  </read_first>
  <action>
    Follow `07.1-MUTATION-PROTOCOL.md` exactly. Apply these five mutations (M4-M7 plus M18,
    below) ONE AT A TIME, fingerprinting `cut-lab.ts` with `git hash-object`
    before each and confirming byte equality after each restore. Do not restore with
    `git checkout --`.

    Mutation M4 — "skip the initialization hide": delete the `setVisibleStepPanel` call added to
    `attachStepTabHandler`. Expected: the corrected assertion (formerly `:56`) FAILS with 5
    received where 1 was expected, and the per-step case FAILS.

    Mutation M5 — "route initialization through tab activation": replace that same
    `setVisibleStepPanel` call with `activateStepTab(selectedTab, false)`. Expected: the
    panel-count assertions still PASS — that is the trap — but the case
    `does not apply the default section collapse state on first load` FAILS on the `<details>`
    open-state flip. This mutation is the whole reason the side-effect assertion exists; record
    both halves of its result, the pass and the fail, so the record shows the count assertions
    were blind to it and the content assertion was not.

    Mutation M6 — "delete the collapse default from activation too": remove the
    `applyDefaultSectionCollapseState` call from `activateStepTab`. Expected: the direction
    control case `still applies the default section collapse state when a tab is clicked` FAILS.
    Without this mutation the "does not apply on load" assertion could be satisfied by having
    deleted the behaviour entirely, and the harness would lie in that direction.

    Mutation M7 — "hide panels on the no-selection path too, without crashing": in
    `attachStepTabHandler`, change the no-selected-tab check from a bare early return to
    `if (!selectedTab) { setVisibleStepPanel(1); return; }`. Do NOT simply delete the early
    return — deleting it leaves `selectedTab` `undefined` going into step (3)'s
    `Number(selectedTab.dataset.cutLabStep)`, which throws a TypeError rather than exercising the
    D-4 assertion; that is an invalid mutation because it kills through a crash, not through
    observing hidden-panel behavior. The compiling replacement above hides every panel but panel 1
    instead. Expected: the D-4 case FAILS with 1 visible panel where 5 were expected.

    Mutation M18 — "no-op activation on click" (this mutation proves the Task 2 clicked-tab
    assertion; M ids are a phase-wide sequence, not per-plan, and M1-M17 were already allocated
    across plans 01-06, so this plan carries M4-M7 plus M18): delete `activateStepTab(button, true);` at `cut-lab.ts:549`, the
    click listener's call. Expected: the panel-visibility half of the cross-entry invariant case
    still PASSES — step 2's panel stays the sole visible one, satisfying the old comparison, which
    is the trap — but the clicked-tab-selection assertion added in Task 2 FAILS, because
    `cut-lab-step-tab-4` never becomes `aria-selected="true"` and `cut-lab-step-tab-2` never stops
    being selected. Record both halves, the pass and the fail, the same way M5's two halves are
    recorded.

    Record every mutation, its command, its observed pass/fail counts and the post-restore
    fingerprint in the plan SUMMARY under `## Mutation evidence`.
  </action>
  <verify>
    <automated>cd DeckFlow.Web &amp;&amp; npx vitest run ts-tests/cut-lab-step-tabs.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - M4 in isolation: at least 2 failed, and a failure message shows `5` received where `1` was expected.
    - M5 in isolation: the panel-count cases PASS and the case `does not apply the default section collapse state on first load` FAILS. Both halves are recorded.
    - M6 in isolation: the case `still applies the default section collapse state when a tab is clicked` FAILS.
    - M7 in isolation: `cd DeckFlow.Web && npx tsc --noEmit -p tsconfig.json` still exits 0 (the mutation compiles), and the D-4 case FAILS with `1` received where `5` was expected — not a thrown error.
    - M18 (no-op activation on click) in isolation: the panel-count/visibility half of the cross-entry invariant case still PASSES (the trap), and the clicked-tab-selection assertion added in Task 2 FAILS. Both halves are recorded.
    - After each restore, `git hash-object DeckFlow.Web/wwwroot/ts/cut-lab.ts` equals the recorded pre-mutation value.
    - After the final restore: `cd DeckFlow.Web && npx vitest run ts-tests/cut-lab-step-tabs.test.ts` reports 10 passed / 0 failed, and `cd DeckFlow.Web && npx vitest run` reports 0 failed.
    - The plan SUMMARY contains a `## Mutation evidence` section covering M4, M5, M6, M7 and M18.
  </acceptance_criteria>
  <done>Each of the five ways F-5 could be shipped wrong — no hide, hide via activation, collapse deleted outright, fallback hides panels instead of showing all five, and a click that silently does nothing — is caught by a named test, shown failing for the right reason, and restored green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server-rendered DOM → client script | The server chooses `currentStep` and emits `aria-selected` / `aria-controls`; the client now also derives panel visibility from those attributes. |
| client script → browser localStorage | `deckflow.cutlab.sections` — this plan must read-only it, never write it at load. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07.1-04 | Information Disclosure | hidden tabpanels in `CutLab.cshtml` | low | accept | `hidden` is a presentation attribute, not a security control — every panel's content remains in the DOM and readable via devtools. All five panels contain only the pool the user pasted in this same session; there is no other user's data and no server secret in any of them, so hiding is a usability change with nothing to disclose. |
| T-07.1-05 | Tampering | `deckflow.cutlab.sections` localStorage key | low | mitigate | An initialization path that began writing this key would silently persist a UI default the user never chose. Task 1 forbids any localStorage writer on the initialization path and Task 1's acceptance criteria assert the key is still `null` after load. |
| T-07.1-06 | Denial of Service | JavaScript-off users | low | mitigate | Server-rendered `hidden` would permanently strand four of five steps for a JS-off user — a self-inflicted availability defect. D-1 rejects it: `CutLab.cshtml` is not edited and the acceptance criteria assert `git diff --stat -- DeckFlow.Web/Views/` is empty. |
| T-07.1-SC | Tampering | npm/NuGet installs | low | accept | No package is added by this plan. |
</threat_model>

<verification>
- `cd DeckFlow.Web && npx tsc --noEmit -p tsconfig.json` exits 0.
- `cd DeckFlow.Web && npx vitest run` reports 33 files, 0 failed, and a test count of at least 138. 138 comes from `07.1-01-PLAN.md:313`'s whole-suite baseline of 134 after Plan 01, plus this plan's 4 added cases (3 in Task 1, 1 in Task 2) — 134 + 4 = 138.
- `git diff --stat` and `git diff --ignore-all-space --stat` report identical file lists and per-file line counts.
- `git status --short DeckFlow.Web/wwwroot/js/` is empty.
</verification>

<success_criteria>
1. Exactly one `[role="tabpanel"]:not([hidden])` exists after initialization, and it is the panel the selected tab's `aria-controls` names.
2. The `toHaveLength(5)` assertion that locked the defect in is corrected, not deleted, and its case still tests the click path it always tested.
3. First-load collapsed-section state is unchanged and `deckflow.cutlab.sections` is not written at load.
4. Clicking a tab still applies the collapse default — proved by a direction-control mutation.
5. The no-selection fallback still shows all five panels, deliberately and by assertion.
6. M4, M5, M6 and M7 each fail their named case in isolation, with M5's pass-half recorded too;
   M18 (no-op activation on click) fails the clicked-tab selection assertion
   while its panel-visibility half still passes, and both halves are recorded.
</success_criteria>

<artifacts_this_phase_produces>
- `DeckFlow.Web/wwwroot/ts/cut-lab.ts` — new function `setVisibleStepPanel(step: number): boolean`; `applyInitialSectionCollapseState`'s no-storage branch no longer calls `applyDefaultSectionCollapseState` (its stored-state branch is unchanged)
- `DeckFlow.Web/ts-tests/cut-lab-step-tabs.test.ts` — new test cases:
  - `hides every panel except the server-selected one for each selected step`
  - `does not apply the default section collapse state on first load` (exercises `applyInitialSectionCollapseState`'s no-storage branch)
  - `still applies the default section collapse state when a tab is clicked`
  - `every entry path leaves exactly one panel visible when the server selected a tab`, including the clicked-tab `aria-selected` assertion for Path B
  - renamed case `leaves every panel visible when the server selected no tab, so content is never stranded behind an unselected tablist` (was `leaves every panel visible when the server selected no tab`)
  - `renderFixture` gains four `<details data-cutlab-mobile-collapse>` sections with ids `cut-lab-section-lock-pool`, `cut-lab-section-cut-rounds`, `cut-lab-section-goals`, `cut-lab-section-export`, and `aria-controls` on all five tab buttons
  - `afterEach` gains `window.localStorage.clear()`
- Mutation identifiers `M4`, `M5`, `M6`, `M7`, `M18` (M ids are a phase-wide sequence; M1-M17 were allocated across plans 01-06, so this plan's fifth mutation takes M18)
</artifacts_this_phase_produces>

<output>
Create `.planning/workstreams/cycle21-cut-lab/phases/07.1-cutlab-review-findings-closeout/07.1-02-SUMMARY.md` when done, including the `## Mutation evidence` section.
</output>
