---
phase: 78-auto-refreshing-primer
plan: 03
type: execute
wave: 3
depends_on: ["78-02"]
files_modified:
  - DeckFlow.Web/Views/Deck/DeckPrimer.cshtml
  - DeckFlow.Web/wwwroot/css/site-common.css
  - DeckFlow.Web.Tests/DeckPrimerBannerRenderTests.cs
  - README.md
autonomous: false
requirements: [PRIMER-01, PRIMER-03]
tags: [primer, staleness, ui, css, accessibility, checkpoint]

must_haves:
  truths:
    - "When the stale flag is OFF, Step 3 renders byte-identical to today (no banner, no hidden hash field) — proven by a baseline byte-comparison render test"
    - "When the flag is ON, the primer is stale, AND a primer is present, a caution banner appears at the top of Step 3 naming the changed-card count with correct pluralization"
    - "The Regenerate button is a real submit that re-posts the existing primer form (the existing explicit generate action)"
    - "The banner uses role=status (not alert), an sr-only 'Status:' prefix, and a glyph + literal text so meaning is not color-only"
    - "The stale caution tone uses var(--gold-warning) via one net-new CSS modifier in site-common.css only"
  artifacts:
    - path: "DeckFlow.Web/Views/Deck/DeckPrimer.cshtml"
      provides: "stale banner block in Step 3 + flag-gated hidden GeneratedPrimerHash field"
      contains: "deck-restored-notice--stale"
    - path: "DeckFlow.Web/wwwroot/css/site-common.css"
      provides: ".deck-restored-notice--stale modifier (token-only)"
      contains: ".deck-restored-notice--stale"
  key_links:
    - from: "DeckPrimer.cshtml banner"
      to: "DeckPrimerViewModel.IsStale / ChangedCardCount / StaleDetectionEnabled / PrimerPromptText"
      via: "server-rendered boolean gate"
      pattern: "Model.IsStale"
    - from: ".deck-restored-notice--stale"
      to: "var(--gold-warning)"
      via: "border-left-color caution tone"
      pattern: "--gold-warning"
---

<objective>
Render the stale banner + its Regenerate action in Step 3 of the Deck Primer page, add the single net-new CSS modifier, update the README, and run a theme/mobile human-verify checkpoint.

Purpose: Surface the server-computed staleness flag (from the resume-without-rebuild path in 78-02) as a clear, accessible caution banner that names the changed-card count and offers explicit regeneration — without auto-rebuilding (PRIMER-01/03). Flag OFF stays byte-identical, proven by a baseline byte-comparison.

Output: the banner block + flag-gated hidden hash field in `DeckPrimer.cshtml`, `.deck-restored-notice--stale` in `site-common.css`, a render test (incl. a flag-OFF byte-identity assertion), a README note, and a visual checkpoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/78-auto-refreshing-primer/UI-SPEC.md
@.planning/phases/78-auto-refreshing-primer/RESEARCH.md
@.planning/phases/78-auto-refreshing-primer/78-02-SUMMARY.md

<interfaces>
<!-- From 78-02 — the view model the banner reads: -->
  bool   DeckPrimerViewModel.StaleDetectionEnabled   // true only when tool.primer.stale-flag is ON
  string? DeckPrimerViewModel.GeneratedPrimerHash     // re-arm value for the hidden field
  bool   DeckPrimerViewModel.IsStale
  int?   DeckPrimerViewModel.ChangedCardCount
  string? DeckPrimerViewModel.PrimerPromptText        // the primer shown in Step 3 (old primer on resume)

<!-- Host view (DeckPrimer.cshtml): -->
  Step 3 panel opens at :276; insert the banner immediately AFTER the .chatgpt-step-heading block (closes :284)
  and BEFORE the suggested-title section (:286). The page is ONE <form method="post" action="~/deck-primer">
  (:70-333) so a plain `type="submit"` Regenerate button posts to /deck-primer = the existing Generate action.
  AntiForgeryToken already present (:87). Hidden ScoreJson precedent for the hidden field (DeckAnalysis.cshtml:516-521).
  The Step-3 primer output region is :308-331 (the `@if (!string.IsNullOrWhiteSpace(Model.PrimerPromptText))` block + else).

<!-- Reused CSS (site-common.css:268-280): -->
  .deck-restored-notice { display:flex; align-items:center; justify-content:space-between; gap:.75rem;
    margin:0 0 .75rem; padding:.7rem .9rem; border:1px solid var(--line);
    border-left:3px solid var(--accent); border-radius:12px; background:var(--panel); color:var(--muted); }
  .run-button — primary submit CTA (reuse for Regenerate; no new button class).
</interfaces>

<microcopy>
<!-- EXACT per UI-SPEC §5. Em dash is intentional (HTML view, not email/Word — repo dash rule does not apply here). -->
  count >= 2 : Deck changed since this primer was generated — {N} cards differ. Regenerate to refresh the primer.
  count == 1 : Deck changed since this primer was generated — 1 card differs. Regenerate to refresh the primer.
  count null : Deck changed since this primer was generated. Regenerate to refresh the primer.
  sr-only prefix (leads the status text): Status:
  Regenerate button label: Regenerate primer
  FORBIDDEN phrasings (UI-SPEC §5): "Auto-updating", "Refreshing automatically", "Syncing primer",
    "Live primer", "We rebuilt your primer", "Primer updated", "Out of date", "Error:", any auto-refetch wording.
</microcopy>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Stale banner + hidden hash field + CSS modifier + render test</name>
  <files>DeckFlow.Web/Views/Deck/DeckPrimer.cshtml, DeckFlow.Web/wwwroot/css/site-common.css, DeckFlow.Web.Tests/DeckPrimerBannerRenderTests.cs</files>
  <behavior>
    - StaleDetectionEnabled false → no banner AND no hidden GeneratedPrimerHash field rendered (byte-identical to baseline).
    - StaleDetectionEnabled true + IsStale false → no banner, but the hidden GeneratedPrimerHash field IS emitted (re-arms the round-trip) when GeneratedPrimerHash is non-empty.
    - StaleDetectionEnabled true + IsStale true + a primer present → banner rendered at the top of Step 3 with role="status", an sr-only "Status:" prefix, the warning glyph, the exact pluralized microcopy for ChangedCardCount (>=2 / ==1 / null), and a Regenerate submit button (.run-button) inside the form.
  </behavior>
  <action>
    In DeckPrimer.cshtml, add the flag-gated hidden field for the round-trip near the AntiForgeryToken / top of the form: `@if (Model.StaleDetectionEnabled && !string.IsNullOrEmpty(Model.GeneratedPrimerHash)) { <input type="hidden" name="GeneratedPrimerHash" value="@Model.GeneratedPrimerHash" /> }`. Render NOTHING when StaleDetectionEnabled is false (byte-identical OFF; mirror ScoreJson "only when present", DeckAnalysis.cshtml:516-521).
    Insert the banner at the top of Step 3 — immediately after the `.chatgpt-step-heading` div (DeckPrimer.cshtml ~:284) and before the suggested-title section (~:286). W1 — the gate MUST include the primer-present condition per UI-SPEC §1: `@if (Model.StaleDetectionEnabled && Model.IsStale && !string.IsNullOrWhiteSpace(Model.PrimerPromptText))` (no banner without a visible primer to mark stale). Markup: a `<div class="deck-restored-notice deck-restored-notice--stale" role="status">` containing (a) a `<span class="sr-only">Status: </span>`, (b) a message span beginning with the `⚠` glyph + a single space + the exact microcopy chosen by ChangedCardCount (use a Razor `@{ }` block to pick the >=2 / ==1 / null variant; render `@count` via Razor so it is HTML-encoded — never interpolate card names), and (c) a `<button type="submit" class="run-button">Regenerate primer</button>` (plain submit → posts the form to ~/deck-primer = the existing Generate action; PRIMER-03 — no new endpoint, no JS rebuild). Do not add a dismiss control.
    In site-common.css, add the ONE net-new class after `.deck-restored-notice` (site-common.css:268-280): `.deck-restored-notice--stale { border-left-color: var(--gold-warning, #c8a040); color: var(--ink); }` with a `/* Why: */` comment that it reuses the notice geometry and swaps the rail to the warning gold token so "deck changed" reads as caution, token-only, inherited by all themes (UI-SPEC §3/§6). Do NOT edit any per-theme CSS file. Do NOT add `flex-wrap` yet — that is a conditional live-mobile call in Task 2.
    Create DeckFlow.Web.Tests/DeckPrimerBannerRenderTests.cs. Render the DeckPrimer view via the render harness already used in the suite (model on the nearest existing view-render test in DeckFlow.Web.Tests). Assert the three behavior states: OFF → output contains neither "deck-restored-notice--stale" nor `name="GeneratedPrimerHash"`; ON+fresh (with a primer) → contains the hidden field, not the banner; ON+stale+primer with count 3 → "3 cards differ", role="status", "Status:", "Regenerate primer"; ON+stale+primer with count 1 → "1 card differs"; ON+stale+primer with null count → the count-suppressed sentence and not the "differ." count fragment; ON+stale but NO primer present → no banner (W1 gate). 
    W4 — add a flag-OFF byte-identity test: render the Step-3 output region with StaleDetectionEnabled=false and assert it is byte-identical to a pre-feature baseline render of the same model (neutralize antiforgery tokens the way the existing suite does), proving OFF changes zero bytes — not merely that the modifier class / hidden-field strings are absent.
  </action>
  <verify>
    <automated>cd /mnt/c/users/chrislunt/source/personal/deckflow-cycle13 && grep -n "deck-restored-notice--stale\|GeneratedPrimerHash\|Regenerate primer\|role=\"status\"\|PrimerPromptText" DeckFlow.Web/Views/Deck/DeckPrimer.cshtml && grep -n "deck-restored-notice--stale" DeckFlow.Web/wwwroot/css/site-common.css && "/mnt/c/Program Files/dotnet/dotnet.exe" build DeckFlow.Web.Tests/DeckFlow.Web.Tests.csproj -c Debug 2>&1 | tail -5</automated>
  </verify>
  <done>Banner gated on StaleDetectionEnabled && IsStale && primer-present; hidden field gated on StaleDetectionEnabled; exact pluralized microcopy; role=status + sr-only prefix + glyph; .deck-restored-notice--stale in site-common.css only; render test covers the states AND the flag-OFF byte-identity baseline; solution builds.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: README + theme/mobile visual verification</name>
  <files>README.md</files>
  <what-built>
    A flag-gated ("tool.primer.stale-flag", seeded OFF) stale banner on the Deck Primer Step 3: caution-gold
    notice with "Deck changed since this primer was generated — N cards differ. Regenerate to refresh the primer."
    and a Regenerate primer submit button. It surfaces on the resume-without-rebuild path (upload a saved .zip
    while Step 1 holds a different deck). Flag OFF = byte-identical (pages and zips). README updated to document
    the flag + behavior.
  </what-built>
  <action>
    Update README.md where feature flags / Deck Primer behavior is documented: add a line for tool.primer.stale-flag (seeded OFF; surfaces the stale banner on resume when the current deck differs from the generated primer's deck; never auto-rebuilds/re-fetches). Match the README's existing flag-doc style; keep behavior-change documentation in the same change (repo rule).
  </action>
  <how-to-verify>
    1. Start the web app headless: scripts/run-web-test.sh (sets DECKFLOW_DISABLE_AUTO_BROWSER=true; UI testing must NEVER open a browser on the Windows host).
    2. Flip tool.primer.stale-flag ON in /Admin/Flags for the verify (it is seeded OFF for prod).
    3. Trigger the banner via the resume-without-rebuild flow (the only live activation path):
       a. Generate a primer for deck v1 (paste a 100-card Commander deck), then Download session (.zip).
       b. Change Step 1: paste a DIFFERENT deck v2 (add/remove a card or change a quantity vs v1).
       c. With v2 in Step 1, click "Upload & Resume" and choose the v1 .zip → the OLD (v1) primer renders in
          Step 3 and the stale banner appears naming the changed-card count.
    4. Confirm at desktop (~1280) and mobile (~390), across site.css (Classic), site-azorius.css, and site-nyx.css:
       - banner shows the gold caution rail, the ⚠ glyph + literal text, and the correct pluralized count;
       - the Regenerate primer button regenerates (rebuilds for the current v2 deck) and the banner CLEARS on the refreshed page (no client-side dismiss);
       - a clean resume (upload the v1 zip with NO new deck in Step 1) shows the old primer with NO banner (fresh);
       - reorder / printing-swap a card in v2 vs v1 does NOT show the banner (fresh);
       - on mobile, if the message crowds the button, add `.deck-restored-notice--stale { flex-wrap: wrap; }` inside the existing mobile breakpoint in site-common.css and re-verify the button wraps full-width (UI-SPEC §7) — otherwise leave it out.
    5. Flip the flag OFF and confirm Step 3 renders exactly as before (no banner, no hidden field) and the downloaded zip carries no 02-primer-deck-hash.txt.
    6. Capture screenshots at both viewports for at least Classic + one guild theme.
  </how-to-verify>
  <resume-signal>Type "approved" (note any mobile-wrap addition), or describe issues to fix.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server → rendered banner | ChangedCardCount integer is rendered into HTML |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-78-07 | Injection | banner microcopy / count render | mitigate | Render the integer ChangedCardCount via Razor (auto HTML-encoded); never interpolate deck/card names into the banner |
| T-78-08 | Bypass | banner shown while flag OFF | mitigate | Banner + hidden field both gated on Model.StaleDetectionEnabled (true only when the flag is ON) → OFF byte-identical, proven by the baseline byte-comparison render test |
| T-78-SC | Tampering | npm/NuGet installs | accept | No package installs this phase |
</threat_model>

<verification>
- `dotnet build` clean; render test covers OFF byte-identity (baseline byte-comparison) + ON fresh/stale + pluralization + the primer-present gate + a11y attributes.
- Live visual verify at 2 viewports across site/azorius/nyx via the resume-without-rebuild flow; flag ON for verify, OFF for prod.
- README documents the new flag + behavior in the same change.
</verification>

<success_criteria>
- Stale banner renders only when the flag is ON, the primer is stale, and a primer is present, with exact microcopy, role=status, sr-only prefix, glyph, and a real Regenerate submit.
- One net-new CSS class in site-common.css; no per-theme edits.
- Flag OFF Step 3 is byte-identical to baseline (proven by test); operator-approved across themes + viewports via the resume trigger.
</success_criteria>

<output>
Create `.planning/phases/78-auto-refreshing-primer/78-03-SUMMARY.md` when done.
</output>
