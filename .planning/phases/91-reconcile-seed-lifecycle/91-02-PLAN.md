---
phase: 91-reconcile-seed-lifecycle
plan: 02
type: execute
wave: 2
depends_on: [91-01]
files_modified:
  - DeckFlow.Web/Services/Content/ContentKbSeedLoader.cs
  - DeckFlow.Studio/ViewModels/DirectPushCoordinator.cs
  - DeckFlow.Core/Orchestration/ContentIndexExportRow.cs
  - DeckFlow.Studio/Services/ProdContentReader.cs
  - DeckFlow.Web.Tests/ContentKbSeedLoaderTests.cs
  - DeckFlow.Studio.Tests/ViewModels/DirectPushCoordinatorTests.cs
  - DeckFlow.Core.Tests/Orchestration/ContentIndexExportRowTests.cs
  - DeckFlow.Studio.Tests/Services/ProdContentReaderTests.cs
autonomous: true
requirements: [SYNC-17]

must_haves:
  truths:
    - "A row loaded by ContentKbSeedLoader is stamped seed_managed=true"
    - "A row written by DirectPush WriteContentAsync is stamped seed_managed=true"
    - "index-seed.json entries carry a seedManaged field (=true) via the shared export factory (D-01 JSON field)"
    - "ProdContentReader.ReadAllAsync round-trips body_sha256 and seed_managed from prod"
  artifacts:
    - path: "DeckFlow.Web/Services/Content/ContentKbSeedLoader.cs"
      provides: "BuildRow stamps SeedManaged=true"
      contains: "SeedManaged = true"
    - path: "DeckFlow.Studio/ViewModels/DirectPushCoordinator.cs"
      provides: "WriteContentAsync stamps SeedManaged=true on publish rows"
      contains: "SeedManaged"
    - path: "DeckFlow.Core/Orchestration/ContentIndexExportRow.cs"
      provides: "From() carries seedManaged=true into the seed JSON (D-01 matching field)"
      contains: "SeedManaged"
    - path: "DeckFlow.Studio/Services/ProdContentReader.cs"
      provides: "SelectAllSql + mapping include body_sha256, seed_managed"
      contains: "seed_managed"
  key_links:
    - from: "ProdContentReader.ToContentSiteIndexRow"
      to: "ContentSiteIndexRow.SeedManaged / BodySha256"
      via: "Dapper column mapping"
      pattern: "SeedManaged|BodySha256"
---

<objective>
Thread the SYNC-17 marker through the remaining I/O seams: stamp `seed_managed=true` at the two prod
write call sites that enter the seed-managed set (`ContentKbSeedLoader.BuildRow`,
`DirectPushCoordinator.WriteContentAsync`), carry a matching `seedManaged` field into the seed JSON via
the shared `ContentIndexExportRow.From()` factory (D-01's "matching seed JSON field"), and extend
`ProdContentReader` to actually SELECT and map `body_sha256` + `seed_managed` (Pitfall 2 - the
reconciler's body-hash-mismatch and seed-drift classes are unbuildable until this read gap is closed).

Purpose: Make the marker TRUE at every point a row becomes seed-owned (DB write + seed file), and make
prod's marker + body hash visible to Studio.
Output: two stamped write paths + the seed-JSON field + an extended read seam, each regression-tested.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/91-reconcile-seed-lifecycle/91-CONTEXT.md
@.planning/phases/91-reconcile-seed-lifecycle/91-RESEARCH.md
@.planning/phases/91-reconcile-seed-lifecycle/91-PATTERNS.md
@.planning/phases/91-reconcile-seed-lifecycle/91-01-SUMMARY.md

<interfaces>
<!-- Established by 91-01 (dependency). Executor USES these. -->
ContentSiteIndexRow.SeedManaged : bool?  (DeckFlow.Core/Knowledge/ContentArtifactSpec.cs)
ProdContentReader today omits body_sha256 & seed_managed from SelectAllSql (DeckFlow.Studio/Services/ProdContentReader.cs:21).
ContentKbSeedLoader.BuildRow feeds UpsertRowPreservingVisibilityAsync (ContentKbSeedLoader.cs:60-61).
DirectPushCoordinator.WriteContentAsync feeds prodStore.UpsertContentColumnsOnlyBatchAsync (DirectPushCoordinator.cs:242-250).
ContentIndexExportRow.From(row) already carries BodySha256 (added 89-04); it is the ONE seed writer.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Stamp seed_managed=true on the two prod write paths + the seed JSON (D-01 field)</name>
  <files>DeckFlow.Web/Services/Content/ContentKbSeedLoader.cs, DeckFlow.Studio/ViewModels/DirectPushCoordinator.cs, DeckFlow.Core/Orchestration/ContentIndexExportRow.cs, DeckFlow.Web.Tests/ContentKbSeedLoaderTests.cs, DeckFlow.Studio.Tests/ViewModels/DirectPushCoordinatorTests.cs, DeckFlow.Core.Tests/Orchestration/ContentIndexExportRowTests.cs</files>
  <read_first>
    - DeckFlow.Web/Services/Content/ContentKbSeedLoader.cs (BuildRow ~68-89; LoadIfPresentAsync ~43-61)
    - DeckFlow.Studio/ViewModels/DirectPushCoordinator.cs (WriteContentAsync ~242-250; ConfirmAndPublishAsync ~270 also builds publishRows)
    - DeckFlow.Core/Orchestration/ContentIndexExportRow.cs (From() ~53-90 — how BodySha256 was threaded in 89-04 is the exact template for seedManaged)
    - 91-PATTERNS.md "ContentKbSeedLoader.cs" and "DirectPushCoordinator.cs" + "Seed-export factory" sections; 91-RESEARCH.md Pattern 1 (call-site table) + Pitfall 4 (loaded value hardcoded true regardless of JSON)
    - DeckFlow.Web.Tests/ContentKbSeedLoaderTests.cs, DeckFlow.Studio.Tests/ViewModels/DirectPushCoordinatorTests.cs (test templates)
  </read_first>
  <behavior>
    - Every row ContentKbSeedLoader builds from a seed entry has SeedManaged==true (hardcoded literal, NOT read from the JSON field).
    - Every publish row DirectPush.WriteContentAsync sends to the prod batch upsert has SeedManaged==true.
    - ContentIndexExportRow.From() writes seedManaged=true, so exported index-seed.json entries carry the field (D-01) and it round-trips through the export/load golden fixture.
    - Local-distill rows (ContentKbOrchestrator path) are untouched and remain SeedManaged==null.
  </behavior>
  <action>
    In ContentKbSeedLoader.BuildRow, add `SeedManaged = true,` to the returned ContentSiteIndexRow - a
    hardcoded literal, NOT `entry.SeedManaged` (Pitfall 4: presence in the file being loaded already
    proves seed-managed regardless of JSON contents). In DirectPushCoordinator.WriteContentAsync, set
    `SeedManaged = true` on each publish row before the UpsertContentColumnsOnlyBatchAsync call (use a
    `row with { SeedManaged = true }` projection). In ContentIndexExportRow, add a `SeedManaged` field
    and set it in `From()` (every exported row is seed-managed by definition — set true; mirror exactly
    how BodySha256 was added in 89-04, including the camelCase JSON property name `seedManaged`). Do NOT
    touch the local-distill UpsertRowAsync path. Extend the named test files: ContentKbSeedLoaderTests
    (loaded row true), DirectPushCoordinatorTests (batch rows true), and ContentIndexExportRowTests /
    the seed golden fixture (seedManaged=true round-trips through export->load).
  </action>
  <acceptance_criteria>
    - ContentKbSeedLoaderTests asserts a loaded seed entry yields a stored row with SeedManaged==true.
    - DirectPushCoordinatorTests asserts the batch upsert receives rows with SeedManaged==true (fake/spy prod store).
    - ContentIndexExportRow test asserts From() sets seedManaged=true and the field round-trips through the seed export->load golden fixture.
    - No change to the local-distill write path; build clean, 0 new warnings.
  </acceptance_criteria>
  <verify>
    <automated>dotnet.exe test DeckFlow.sln --no-restore --filter "FullyQualifiedName~ContentKbSeedLoader|FullyQualifiedName~DirectPushCoordinator|FullyQualifiedName~ContentIndexExportRow"</automated>
  </verify>
  <done>Both prod write paths and the seed JSON carry seed_managed=true; local-distill stays unmarked; regression + golden-fixture tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend ProdContentReader to select + map body_sha256 and seed_managed</name>
  <files>DeckFlow.Studio/Services/ProdContentReader.cs, DeckFlow.Studio.Tests/Services/ProdContentReaderTests.cs</files>
  <read_first>
    - DeckFlow.Studio/Services/ProdContentReader.cs (SelectAllSql ~21-26; ToContentSiteIndexRow ~153-185; private ContentSiteIndexRowData ~187-207; note: this reader issues NO DDL - do not add schema-ensure)
    - DeckFlow.Core/Content/ContentSiteIndexStore.cs GetAllRowsAsync SELECT (~349-375 - the column list to mirror)
    - DeckFlow.Studio.Tests/Services/ProdContentReaderTests.cs (existing [PostgresFact] round-trip ~54-78 + always-runs failure test ~35-50)
    - 91-PATTERNS.md "ProdContentReader.cs" section; 91-RESEARCH.md Pitfall 2
  </read_first>
  <behavior>
    - ProdContentReader.ReadAllAsync returns rows whose BodySha256 and SeedManaged reflect the stored prod columns (not unconditional null).
    - The reader still runs a single plain SELECT with no DDL and no WHERE-on-timestamp.
  </behavior>
  <action>
    Add `body_sha256, seed_managed` to SelectAllSql's column list (mirroring GetAllRowsAsync). Add
    `public string? BodySha256 { get; init; }` and `public bool? SeedManaged { get; init; }` to the
    private ContentSiteIndexRowData, and map both in ToContentSiteIndexRow. Do NOT add any
    EnsureSchemaAsync/DDL call (the reader is structurally read-only, R1). Add a round-trip regression
    test in ProdContentReaderTests (gate the live path with the existing [PostgresFact] convention; the
    [PostgresFact] round-trip is the regression guard for Pitfall 2).
  </action>
  <acceptance_criteria>
    - SelectAllSql string contains `body_sha256` and `seed_managed`.
    - Test asserts a prod row with a populated body_sha256/seed_managed round-trips (not null) through ReadAllAsync.
    - No EnsureSchema/DDL statement added to ProdContentReader; build clean.
  </acceptance_criteria>
  <verify>
    <automated>dotnet.exe test DeckFlow.Studio.Tests/DeckFlow.Studio.Tests.csproj --no-restore --filter "FullyQualifiedName~ProdContentReader"</automated>
  </verify>
  <done>ProdContentReader selects and maps body_sha256 + seed_managed, stays DDL-free, and the round-trip regression guard for Pitfall 2 is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Studio -> prod DB (read) | ProdContentReader issues a single read-only SELECT; no DDL, no write |
| seed file -> seed loader (write to prod) | Loader stamps marker true; runs at web boot against prod |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-91-03 | Tampering | Stamping seed_managed from untrusted JSON field | mitigate | Hardcode `true` at the loader call site and on export, not `entry.SeedManaged` (Pitfall 4) |
| T-91-04 | Elevation of Privilege | Read seam accidentally running DDL against prod | mitigate | No EnsureSchema call added; test/comment assert read-only |
| T-91-05 | Information Disclosure | Prod connection string leaking via reader exceptions | mitigate | Preserve existing ProdContentReader catch discipline (no conn-string in logs) |
</threat_model>

<verification>
- `dotnet build DeckFlow.sln` clean, 0 new warnings.
- Named test filters green across Core + Web + Studio test projects.
- CRLF/LF endings preserved; changed-lines format gate clean.
</verification>

<success_criteria>
The seed-managed marker is stamped true at both prod write entry points AND carried into the seed JSON
via the one shared export factory (D-01), and prod's marker + body hash are readable by Studio via the
extended ProdContentReader - closing the Pitfall-2 gap that would otherwise make the reconciler unbuildable.
</success_criteria>

<output>
Create `.planning/phases/91-reconcile-seed-lifecycle/91-02-SUMMARY.md` when done.
</output>
