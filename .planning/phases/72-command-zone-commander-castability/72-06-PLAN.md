---
phase: 72-command-zone-commander-castability
plan: 06
type: execute
wave: 4
depends_on: [72-05]
files_modified:
  - DeckFlow.Web/Views/Deck/Manabase.cshtml
  - DeckFlow.Web/wwwroot/css/site-common.css
  - DeckFlow.Web/e2e/manabase-commander-callout.spec.ts
autonomous: false
requirements: [D-01, D-02, G-01, G-02, G-03, G-04, B-06, BETA-01]
must_haves:
  truths:
    - "An always-visible 'analysis is in beta - results may be inaccurate' disclaimer renders on the manabase results page regardless of the manabase.commander-castability flag (shown both flag ON and OFF)"
    - "Flag ON (Casual): a command-zone castability callout renders ABOVE the per-card castability table"
    - "Flag ON: commander rows are hidden from the per-card table (display-only) and the visible table average excludes them"
    - "Flag ON: the companion appears in the callout with its cast % and a visible +3 generic 'to hand' heuristic note"
    - "A manual companion designator input (bound to Request.CompanionName) lets the user name/override the companion; the modeled companion name pre-fills it"
    - "Flag OFF: the page is byte-identical to today (commander in the table with its star, no callout, no designator-driven change)"
  artifacts:
    - path: "DeckFlow.Web/Views/Deck/Manabase.cshtml"
      provides: "command-zone callout + castRows display filter + designator input"
      contains: "ShowCommanderCastability"
    - path: "DeckFlow.Web/wwwroot/css/site-common.css"
      provides: "callout layout styles (mirrors manabase-verdict tokens)"
      contains: "manabase-cmd-castability"
    - path: "DeckFlow.Web/e2e/manabase-commander-callout.spec.ts"
      provides: "live e2e for G-01..G-04"
  key_links:
    - from: "Manabase.cshtml castRows"
      to: "report.Castability.Where(!IsCommander)"
      via: "display-only filter when ShowCommanderCastability"
      pattern: "ShowCommanderCastability"
    - from: "Manabase.cshtml callout"
      to: "Model.CompanionCallout"
      via: "render companion row with +3 note"
      pattern: "CompanionCallout"
---

<objective>
Render the command-zone castability callout above the per-card table, filter commander rows
out of the visible table + visible average (display-only, never mutating report.Castability),
add the manual companion designator input, style the callout in site-common.css (mirroring the
Phase 71 verdict block), and prove it live with a Playwright spec + a cross-theme visual gate.

Purpose: Surface the single most important line in a Commander deck (commander cast-on-curve)
as its own callout, per SPEC §D. Flag OFF stays byte-identical.
Output: Callout + display filter + designator + CSS + e2e spec + operator visual sign-off.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/72-command-zone-commander-castability/72-RESEARCH.md
@.planning/phases/72-command-zone-commander-castability/72-SPEC.md
@.planning/phases/72-command-zone-commander-castability/72-VALIDATION.md

<interfaces>
<!-- Verified view contracts. -->
- Manabase.cshtml: `var castRows = report.Castability;` (~:164); right-lens avg uses `ManabaseDisplay.AvgOnCurve(castRows)` and `castRows.Count` (~:206-212) — so filtering castRows automatically fixes the visible average (G-03); Phase 71 verdict block `.manabase-verdict` (~:246-264) + ramp/draw block `.manabase-rampdraw` (~:266-285); castability table heading `.manabase-castability-heading` (~:415) and table loop `@foreach (var c in report.Castability)` with `manabase-row--commander` + `.manabase-cmd-glyph` star (~:430-451).
- ManabaseViewModel: `ShowCommanderCastability` + `CompanionCallout` (added 72-05); `ShowCastability` computed (Casual + Castability.Count>0); `Request.CompanionName` (added 72-05).
- CardCastability: Name, ManaValue, OnCurveTurn, CastPercent, LimitingFactor, IsCommander.
- site-common.css: `.manabase-verdict` / `.manabase-rampdraw` token-based blocks (~:2637-2692); `.manabase-cmd-glyph` (~:2417). Layout CSS MUST go here, NOT site.css (CLAUDE.md). Token additions go in each theme file's :root.
- e2e pattern: DeckFlow.Web/e2e/manabase-verdict.spec.ts (live-only header, DECKFLOW_LIVE_E2E=1, inline decklist constants, flag must be ON).
- Existing cost-overrides textarea bound to Request.CostOverridesText is the UX pattern to mirror for the designator.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Callout + display-only filter + designator input + CSS</name>
  <files>DeckFlow.Web/Views/Deck/Manabase.cshtml, DeckFlow.Web/wwwroot/css/site-common.css</files>
  <read_first>
    - DeckFlow.Web/Views/Deck/Manabase.cshtml (castRows ~:164, right-lens ~:202-214, verdict/rampdraw blocks ~:246-285, castability table ~:413-459, the deck-input/cost-overrides form region)
    - DeckFlow.Web/wwwroot/css/site-common.css (.manabase-verdict/.manabase-rampdraw ~:2637-2692, .manabase-cmd-glyph ~:2417)
  </read_first>
  <action>
    (1) Change the castRows assignment (~:164) so that when `Model.ShowCommanderCastability` is true,
    `castRows = report.Castability.Where(c => !c.IsCommander).ToList()` (typed IReadOnlyList<CardCastability>),
    else `report.Castability` unchanged. The right-lens avg + count already read castRows, so the visible
    average excludes commanders automatically (G-03). (2) Change the per-card table loop (~:430) from
    `report.Castability` to `castRows` so commander rows are hidden from the table when the flag is ON (G-02);
    keep the existing star/`manabase-row--commander` handling (it still applies when flag OFF and castRows
    includes commanders). (3) Insert a new `<section class="manabase-cmd-castability">` AFTER the ramp/draw
    block (~:285) and BEFORE the castability heading (~:415) — narrowing order: verdict → ramp/draw →
    command-zone castability → per-card table. Render it `@if (Model.ShowCommanderCastability &&
    report.Castability.Any(c => c.IsCommander))`: a heading "Command-zone castability", one line per
    `report.Castability.Where(c => c.IsCommander)` (star glyph + @c.Name + @c.CastPercent% on its on-curve
    turn), and `@if (Model.CompanionCallout is { } comp)` a companion line (distinct glyph + @comp.Name +
    @comp.CastPercent% + a visible note that the cost includes the +3 generic "to hand" tax, stated as a
    heuristic/approximation). All names rendered with `@` (Razor auto-encode); NEVER @Html.Raw. (4) Add a
    companion designator text input bound to `Request.CompanionName` near the cost-overrides control,
    mirroring its markup, with helper copy "Companion (optional) — auto-detected from Archidekt/Moxfield;
    name it here for pasted lists or override." Set its value to `Model.CompanionCallout?.Name ?? Model.Request.CompanionName`
    so a modeled companion pre-fills and survives re-analyze. (5) Add `.manabase-cmd-castability` layout CSS
    to site-common.css mirroring the `.manabase-verdict` token pattern (border, radius, panel-soft-bg, muted
    heading); use only existing CSS variables — no new theme-file tokens needed. Plain ASCII copy (no em/en
    dashes). Do NOT mutate report.Castability anywhere.
  </action>
  <verify>
    <automated>"/mnt/c/Program Files/dotnet/dotnet.exe" build DeckFlow.Web/DeckFlow.Web.csproj</automated>
  </verify>
  <acceptance_criteria>
    - View compiles; flag OFF path leaves castRows = report.Castability and table loop visually unchanged (commander star present).
    - Callout section is positioned between the ramp/draw block and the castability heading.
    - Designator input is bound to Request.CompanionName and pre-fills from CompanionCallout?.Name.
    - `grep -n "Html.Raw" DeckFlow.Web/Views/Deck/Manabase.cshtml` shows no Html.Raw on companion/commander names.
    - New CSS lives in site-common.css, not site.css.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Live e2e spec for the callout (G-01..G-04, B-06)</name>
  <files>DeckFlow.Web/e2e/manabase-commander-callout.spec.ts</files>
  <read_first>
    - DeckFlow.Web/e2e/manabase-verdict.spec.ts (live-only header, decklist constants, run instructions, flag-on requirement)
    - DeckFlow.Web/Views/Deck/Manabase.cshtml (selectors added in Task 1: .manabase-cmd-castability, table rows)
  </read_first>
  <action>
    Create a live-only Playwright spec `manabase-commander-callout.spec.ts` (DECKFLOW_LIVE_E2E gate + a header
    documenting it requires the `manabase.commander-castability` flag ON and Casual mode). Use a pasted-text
    Casual deck with a commander and a manually-designated companion (exercises B-06 paste/designator path so
    the test needs no live Moxfield/Archidekt fetch). Assert: G-01 the `.manabase-cmd-castability` callout
    renders and appears in the DOM before the `.manabase-castability-heading`; G-02 the commander's name does
    NOT appear as a row inside the per-card castability table; G-03 the visible "avg on-curve" reflects the
    non-commander rows (the callout's commander % is shown only in the callout); G-04 the companion line shows
    its % and the +3 heuristic note text. Mirror the verdict spec's locator/structure conventions.
  </action>
  <verify>
    <automated>cd DeckFlow.Web &amp;&amp; npx --no-install tsc -p tsconfig.json --noEmit 2>/dev/null; node -e "process.exit(require('fs').existsSync('e2e/manabase-commander-callout.spec.ts')?0:1)"</automated>
  </verify>
  <acceptance_criteria>
    - Spec file exists, is DECKFLOW_LIVE_E2E-gated, and encodes assertions for G-01..G-04.
    - With the app running (scripts/run-web-test.sh) + flag ON: `DECKFLOW_LIVE_E2E=1 npx --no-install playwright test manabase-commander-callout` passes (run during the visual checkpoint).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Always-on "analysis in beta" disclaimer banner (BETA-01)</name>
  <files>DeckFlow.Web/Views/Deck/Manabase.cshtml, DeckFlow.Web/wwwroot/css/site-common.css</files>
  <read_first>
    - DeckFlow.Web/Views/Deck/Manabase.cshtml (results region top — where the report renders, above the verdict block ~:246)
    - DeckFlow.Web/wwwroot/css/site-common.css (.manabase-verdict token block ~:2637-2692 for the styling pattern)
  </read_first>
  <action>
    Add a static, always-visible disclaimer banner at the TOP of the manabase results region (above the
    Phase 71 verdict block, ~:246), rendered whenever a report is present. It is NOT gated by
    `Model.ShowCommanderCastability` or any feature flag — it always shows. Copy (plain ASCII, no em/en
    dashes), e.g.: "Heads up: manabase analysis is in beta. Treat the results as a guide, not gospel -
    numbers may be inaccurate." Wrap in a `<section class="manabase-beta-notice" role="note">`. Add
    `.manabase-beta-notice` layout CSS to site-common.css mirroring the `.manabase-verdict` token pattern
    (subtle warning accent using existing CSS variables only - no new theme-file tokens). Because this banner
    is intentionally always-on, it appears in BOTH the flag-ON and flag-OFF page states; it does not affect
    the service-level analysis result/prompt bytes (F-01 is service-level, unchanged).
  </action>
  <verify>
    <automated>"/mnt/c/Program Files/dotnet/dotnet.exe" build DeckFlow.Web/DeckFlow.Web.csproj</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "manabase-beta-notice" DeckFlow.Web/Views/Deck/Manabase.cshtml` returns a match outside any flag-gated `@if`.
    - The banner renders whenever a report is present (flag ON and OFF alike).
    - `.manabase-beta-notice` CSS lives in site-common.css, not site.css; copy is plain ASCII.
    - View compiles.
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Cross-theme visual verification (Casual desktop + mobile x 2 themes)</name>
  <what-built>
    The command-zone callout above the castability table, with commander rows removed from the table, a
    display-average excluding commanders, the companion +3 note, and the designator input. Claude has started
    the app headless (scripts/run-web-test.sh, DECKFLOW_DISABLE_AUTO_BROWSER=true) with the
    manabase.commander-castability flag ON and run the live Playwright spec.
  </what-built>
  <how-to-verify>
    1. With the app running + flag ON, open /manabase and analyze a Casual commander+companion deck.
    2. Confirm at Casual desktop AND mobile widths, across 2 guild themes (light + dark):
       - the callout sits above the per-card table and mirrors the Phase 71 verdict block styling;
       - the commander star is gone from the table; the visible avg excludes the commander;
       - the companion line shows its % and a clear +3 "to hand" heuristic note;
       - the designator input pre-fills the detected/named companion.
    3. Toggle the flag OFF and confirm the page returns to today's layout (commander row + star in the table, no callout).
    4. Confirm the always-on "analysis in beta" disclaimer banner renders at the top of the results in BOTH
       flag states, across both themes (light + dark) and desktop + mobile widths, readable and on-brand.
  </how-to-verify>
  <verify>
    <automated>node -e "process.exit(require('fs').existsSync('DeckFlow.Web/e2e/manabase-commander-callout.spec.ts')?0:1)"</automated>
  </verify>
  <resume-signal>Type "approved" or describe layout/theme issues to fix.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| service → Razor | companion + commander names (from API/designator) rendered to HTML |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-72-06a | XSS / Tampering | Companion/commander name render | mitigate | Razor `@` auto-encoding for all names; NO @Html.Raw (grep-asserted) |
| T-72-06b | Tampering | Flag-OFF visual byte-identity | mitigate | castRows unchanged when flag OFF; table loop keeps commander star; designator inert without flag |
</threat_model>

<verification>
- Build clean; e2e spec present + DECKFLOW_LIVE_E2E-gated; live Playwright `manabase-commander-callout` passes.
- Operator cross-theme visual sign-off (Casual desktop + mobile x 2 themes; flag ON and OFF).
</verification>

<success_criteria>
- Command-zone callout renders above the table; commanders display-filtered from table + visible average;
  companion +3 note shown; designator wired; CSS in site-common.css; e2e + visual gate green.
</success_criteria>

<output>
Create `.planning/phases/72-command-zone-commander-castability/72-06-SUMMARY.md` when done.
</output>
