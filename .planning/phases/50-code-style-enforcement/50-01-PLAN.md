---
phase: 50-code-style-enforcement
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .editorconfig
  - .planning/phases/50-code-style-enforcement/50-RECONCILIATION.md
autonomous: false
requirements: [FMT-01]
user_setup: []

must_haves:
  truths:
    - "Operator's ReSharper code-style export exists as a transient .editorconfig-keyed input the agent can diff against the committed .editorconfig"
    - "50-RECONCILIATION.md lists every RS-vs-existing difference with a KEEP-EXISTING / ADOPT-RS / CARVE-OUT-WINS resolution and a gate-enforceable (dotnet_*/csharp_*) vs JetBrains-only (resharper_*) classification per key"
    - "The reconciled .editorconfig still contains all five carve-out rules verbatim in effect (init accessors, switch expressions, attribute placement, xmldoc indent, raw-string indent)"
    - "git diff .editorconfig shows only additive/keyed style rules — zero carve-out weakening or removal"
    - "Every ADOPT-RS key is reviewed against the four CarveOutGuard fixtures (init / raw-string / attribute / switch) so no newly-merged style key re-introduces a carve-out conflict at a different key"
    - "The throwaway RS export file is deleted from the tree after a SUCCESSFUL merge; 50-RECONCILIATION.md is the permanent audit trail"
    - "If no export is provided, the phase is in a BLOCKED state: 50-RECONCILIATION.md is marked BLOCKED, .editorconfig is unchanged, the 50-01-SUMMARY status is BLOCKED, and Plans 02-04 are FORBIDDEN to run until a real reconciliation completes"
  artifacts:
    - path: ".planning/phases/50-code-style-enforcement/50-RECONCILIATION.md"
      provides: "Permanent audit trail of every merged/rejected RS pref with reasons (D-02/D-03), OR a BLOCKED marker if no export was provided"
      contains: "CARVE-OUT-WINS"
    - path: ".editorconfig"
      provides: "Reconciled source of truth with carve-outs intact and non-conflicting RS prefs merged"
      contains: "resharper_csharp_force_indent_style_for_raw_string_literals"
  key_links:
    - from: ".editorconfig"
      to: "the five carve-outs at lines 45-82 (current file)"
      via: "verbatim preservation — no key weakened"
      pattern: "csharp_style_prefer_switch_expression|resharper_place_attribute_on_same_line|resharper_xmldoc_indent_text|force_indent_style_for_raw_string_literals|prefer_auto_properties"
    - from: "50-RECONCILIATION.md"
      to: "each exported RS key"
      via: "per-key resolution row"
      pattern: "KEEP-EXISTING|ADOPT-RS|CARVE-OUT-WINS"
---

<objective>
Reconcile the operator's exported ReSharper code-style into the committed `.editorconfig`, with the five bug-driven carve-outs overriding any conflicting ReSharper preference, and produce `50-RECONCILIATION.md` as the permanent audit trail.

Purpose: The downstream changed-lines gate (Plans 02/03) enforces the RECONCILED config. Reconciliation MUST land first (Wave 1) so the gate and the carve-out guard test evaluate the final config, not the pre-merge one. This is the foundation of the whole phase.

Output: A merged `.editorconfig` (carve-outs intact, non-conflicting RS prefs added) and `50-RECONCILIATION.md`. The transient RS export file is deleted after a successful merge (D-03). If no export is provided, the phase is BLOCKED (no `.editorconfig` change, SUMMARY status BLOCKED) and the downstream plans must not run.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/50-code-style-enforcement/50-SPEC.md
@.planning/phases/50-code-style-enforcement/50-CONTEXT.md
@.planning/phases/50-code-style-enforcement/50-RESEARCH.md
@.editorconfig

<interfaces>
<!-- The five carve-outs the agent MUST preserve verbatim. Source: .editorconfig (current). -->
<!-- Carve-out 1 — init accessor preservation (lines 45-51): keys are non-enforcing :silent. -->
<!--   dotnet_style_prefer_auto_properties = true:silent / dotnet_style_readonly_field = true:silent / csharp_style_prefer_readonly_struct_member = true:silent -->
<!-- Carve-out 2 — switch expressions (lines 53-55): csharp_style_prefer_switch_expression = true:silent -->
<!-- Carve-out 3 — attribute placement (lines 62-70): resharper_*place_*attribute_on_same_line = false  (JetBrains-only; dotnet format cannot rewrite) -->
<!-- Carve-out 4 — xmldoc indent (lines 72-76): resharper_xmldoc_indent_text = ZeroIndent / resharper_xmldoc_indent_child_elements = ZeroIndent (JetBrains-only) -->
<!-- Carve-out 5 — raw-string indent (lines 78-82): resharper_csharp_force_indent_style_for_raw_string_literals = LeftAligned (JetBrains-only) -->

<!-- RESEARCH key insight: dotnet format reads ONLY dotnet_*/csharp_* keys; it IGNORES resharper_* keys entirely. -->
<!-- So carve-outs 3/4/5 are physically un-breakable by the gate. The reconciliation report must classify each -->
<!-- exported RS key as gate-enforceable (dotnet_*/csharp_*) vs JetBrains-only (resharper_*) — see RESEARCH Open Q1. -->
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Obtain the operator's ReSharper "Export to .editorconfig" output</name>
  <read_first>
    - .planning/phases/50-code-style-enforcement/50-CONTEXT.md (D-02: operator exports via "Export to .editorconfig", tool-agnostic keyed output; D-03: input is throwaway)
    - .planning/phases/50-code-style-enforcement/50-SPEC.md (REQ-1)
  </read_first>
  <action>Operator action only — the agent cannot export ReSharper settings. The operator runs ReSharper/Rider "Export to .editorconfig" and saves it to `.planning/phases/50-code-style-enforcement/rs-export.editorconfig`. The agent waits at this blocking gate; on resume with "exported" it proceeds to Task 2 (real reconciliation). On resume with "no-export" it enters the BLOCKED path: it writes a BLOCKED `50-RECONCILIATION.md`, makes NO `.editorconfig` change, and the 50-01-SUMMARY status MUST be BLOCKED so the orchestrator does not advance Plans 02-04.</action>
  <what-built>Nothing yet — this is the one unavoidable manual step. The operator (Windows JetBrains/Rider/ReSharper) must run "Export to .editorconfig" from their local code-style profile and place the resulting file at a repo-relative path the agent can read.</what-built>
  <how-to-verify>
    1. Operator opens ReSharper/Rider settings -> Code Editing -> C# -> "Export to .editorconfig" (or Editor -> Code Style -> C# -> gear -> Export).
    2. Operator saves the export as `.planning/phases/50-code-style-enforcement/rs-export.editorconfig` (repo-relative; will be DELETED after a successful merge per D-03 — never committed as the source of truth).
    3. Confirm the file exists and contains `dotnet_*`, `csharp_*`, and/or `resharper_*` keys.
  </how-to-verify>
  <verify>Operator confirms `.planning/phases/50-code-style-enforcement/rs-export.editorconfig` exists and is non-empty (or answers "no-export" to put the phase in a BLOCKED state).</verify>
  <acceptance_criteria>
    - File `.planning/phases/50-code-style-enforcement/rs-export.editorconfig` exists and is non-empty (the SUCCESS path), OR the operator explicitly answers "no-export" (the BLOCKED path).
    - On the success path it contains at least one `[*.cs]` or `[*.{cs,...}]` section with code-style keys.
    - On the no-export path this is a BLOCKED state, NOT a successful completion: do NOT proceed to merge, do NOT change `.editorconfig`, surface the blocker, and ensure the SUMMARY status will read BLOCKED so Plans 02-04 cannot run. The task is re-runnable once the export lands.
  </acceptance_criteria>
  <done>The operator's RS export is in place at the repo-relative path (SUCCESS), or the operator chose no-export and the phase enters the explicit BLOCKED state (re-runs when the export lands).</done>
  <resume-signal>Type "exported" once `rs-export.editorconfig` is in place, or "no-export" to enter the BLOCKED state (whole plan re-runs when the export is available; Plans 02-04 do not proceed).</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Diff RS export vs committed .editorconfig, write 50-RECONCILIATION.md, merge non-conflicting prefs (carve-outs win), delete the export</name>
  <files>.editorconfig, .planning/phases/50-code-style-enforcement/50-RECONCILIATION.md, .planning/phases/50-code-style-enforcement/rs-export.editorconfig (deleted)</files>
  <read_first>
    - .planning/phases/50-code-style-enforcement/rs-export.editorconfig (the export from Task 1)
    - .editorconfig (the committed target — carve-outs at lines 45-82)
    - .planning/phases/50-code-style-enforcement/50-CONTEXT.md (D-02 reconciliation workflow, D-03 delete-after-merge)
    - .planning/phases/50-code-style-enforcement/50-SPEC.md (REQ-1, REQ-2, Constraints "Carve-outs override everything")
    - .planning/phases/50-code-style-enforcement/50-RESEARCH.md (Open Q1: classify each key dotnet_*/csharp_* enforceable vs resharper_* JetBrains-only)
  </read_first>
  <action>
    BLOCKED-PATH SHORT-CIRCUIT (FIX MEDIUM 3): if Task 1 was resolved "no-export" (no `rs-export.editorconfig` in the tree), do NOT write a "successful" stub. Instead write `50-RECONCILIATION.md` whose FIRST line/heading is an explicit `# RECONCILIATION — BLOCKED (no ReSharper export provided)` marker stating the export is pending, that NO `.editorconfig` change was made, and that Plans 02-04 are FORBIDDEN to run until a real reconciliation completes. Make NO change to `.editorconfig`. Do NOT invent RS preferences. The 50-01-SUMMARY (see <output>) MUST carry status `BLOCKED`. Then stop — the plan re-runs when the export lands. (A BLOCKED reconciliation is NOT a successful summary; the two must agree.)
    SUCCESS PATH (export present): Compare every key in `rs-export.editorconfig` against the committed `.editorconfig`. For EACH RS-exported key, decide a resolution and record it as a row in `50-RECONCILIATION.md`:
    - CARVE-OUT-WINS — the RS key would re-indent raw strings, strip or weaken `init` (e.g. flip `dotnet_style_prefer_auto_properties` to a non-silent severity that rewrites get-only props), inline `[Attribute]` (`resharper_*place_attribute_on_same_line = true`), 5-space-indent xmldoc, or rewrite switch expressions. REJECT it; the committed carve-out value stands. Cite which carve-out (1-5) and its bug rationale.
    - KEEP-EXISTING — the committed `.editorconfig` already covers this concept with a deliberate value and the RS value is a redundant or weaker restatement. Do not change `.editorconfig`.
    - ADOPT-RS — a genuinely new, non-conflicting preference. Merge it into the correct `[*.cs]` (or appropriately scoped) section of `.editorconfig`.
    For every row also classify enforceability: `dotnet_*`/`csharp_*` = gate-enforceable; `resharper_*` = JetBrains-only (invisible to `dotnet format`, per RESEARCH key insight). This tells Plan 02 whether the gate needs `style` mode at all (RESEARCH Open Q1).
    CONFLICT-AT-A-DIFFERENT-KEY GUARD: the CARVE-OUT-WINS check above catches RS keys that match a carve-out's OWN key. It does NOT, by itself, catch a NEW `dotnet_*`/`csharp_*` ADOPT-RS key that re-introduces a carve-out conflict at a DIFFERENT key (e.g. a style/severity rule that makes the gate-enforcing formatter want to rewrite `init`, re-indent a raw string, inline an attribute, or convert a switch expression). Before finalizing the ADOPT-RS set, REVIEW every gate-enforceable (dotnet_*/csharp_*) ADOPT-RS key against the four CarveOutGuard fixtures — init / raw-string / attribute / switch (Plan 03). If any ADOPT-RS key could cause `dotnet format` to alter one of those four fixtures, reclassify it CARVE-OUT-WINS (reject it) and note the at-risk fixture in its row. The FMT-02 CarveOutGuard test (Plan 03) is the CI backstop, but it only runs at the Plan 04 gate — this review catches a conflicting style key BEFORE the phase gate so a bad merge is never shipped to the scratch PRs.
    Merge ONLY the ADOPT-RS keys into `.editorconfig` — append them into the existing `[*.cs]` block (or a correctly-scoped section), preserving the file's existing comment structure and the carve-out comment rationales verbatim. Do NOT touch the carve-out lines (45-82) except to leave them exactly as-is. Preserve LF line endings (.gitattributes pins `.editorconfig` to `eol=lf`); add no whole-file reflow.
    `50-RECONCILIATION.md` structure (success path): a header explaining the source-of-truth model and D-02/D-03; a table with columns `RS Key | RS Value | Committed Value | Resolution | Enforceable? | Reason`; a "Carve-out conflict review" subsection listing, for the four CarveOutGuard fixtures, that no ADOPT-RS key endangers them (or which were reclassified); a closing "Carve-out integrity" section confirming all five carve-outs survived; and a note that `rs-export.editorconfig` was deleted (D-03).
    After writing the report and merging (SUCCESS path only), DELETE `rs-export.editorconfig` from the tree (D-03 — it is throwaway; the report is the durable record).
  </action>
  <verify>
    <automated>test -f .planning/phases/50-code-style-enforcement/50-RECONCILIATION.md &amp;&amp; ! test -f .planning/phases/50-code-style-enforcement/rs-export.editorconfig &amp;&amp; grep -Eq 'CARVE-OUT-WINS|KEEP-EXISTING|ADOPT-RS|BLOCKED' .planning/phases/50-code-style-enforcement/50-RECONCILIATION.md &amp;&amp; grep -q 'resharper_csharp_force_indent_style_for_raw_string_literals = LeftAligned' .editorconfig &amp;&amp; grep -q 'csharp_style_prefer_switch_expression = true:silent' .editorconfig &amp;&amp; grep -q 'resharper_xmldoc_indent_text = ZeroIndent' .editorconfig &amp;&amp; grep -q 'resharper_place_attribute_on_same_line = false' .editorconfig &amp;&amp; grep -q 'dotnet_style_prefer_auto_properties = true:silent' .editorconfig &amp;&amp; echo OK</automated>
  </verify>
  <acceptance_criteria>
    - SUCCESS PATH: `50-RECONCILIATION.md` exists with a per-key resolution table; every RS-exported key has exactly one of KEEP-EXISTING / ADOPT-RS / CARVE-OUT-WINS and an Enforceable? (dotnet_*/csharp_* vs resharper_*) classification.  (SPEC AC-1)
    - BLOCKED PATH (no-export): `50-RECONCILIATION.md` is explicitly marked `BLOCKED` (not a successful stub), `.editorconfig` is UNCHANGED (`git diff .editorconfig` empty), and the 50-01-SUMMARY status is `BLOCKED` so Plans 02-04 do not run. The reconciliation marker and the SUMMARY status AGREE (both BLOCKED).  (FIX MEDIUM 3)
    - SUCCESS PATH: The report contains a "Carve-out conflict review" subsection confirming each gate-enforceable ADOPT-RS key was checked against the four CarveOutGuard fixtures (init / raw-string / attribute / switch) and none endangers them — any that did were reclassified CARVE-OUT-WINS.
    - All five carve-out rules are still present verbatim in `.editorconfig`: `dotnet_style_prefer_auto_properties = true:silent`, `csharp_style_prefer_switch_expression = true:silent`, `resharper_place_attribute_on_same_line = false`, `resharper_xmldoc_indent_text = ZeroIndent`, `resharper_csharp_force_indent_style_for_raw_string_literals = LeftAligned`.  (SPEC AC-2 input)
    - `git diff .editorconfig` shows only additive style keys and/or new comments — NO deletion or severity-weakening of any carve-out line (and empty on the BLOCKED path).  (SPEC AC-1, AC-5)
    - `rs-export.editorconfig` no longer exists in the tree (deleted on success per D-03; absent on the no-export path).
    - `.editorconfig` line endings remain LF (no CRLF introduced); no unrelated source file changed.  (SPEC AC-5)
    - Any RS key rejected as CARVE-OUT-WINS names its carve-out (1-5) and the bug it protects against.  (SPEC AC-1)
  </acceptance_criteria>
  <done>On success: 50-RECONCILIATION.md merged-and-rejected audit is complete (including the four-fixture conflict review), .editorconfig carries the ADOPT-RS keys with all five carve-outs intact, and the throwaway RS export is deleted. On no-export: 50-RECONCILIATION.md and the SUMMARY both read BLOCKED, .editorconfig is untouched, and the downstream plans are gated off.</done>
</task>

</tasks>

<verification>
- `grep -c` of each carve-out key in `.editorconfig` returns >= 1 (all five survive) — success path.
- `50-RECONCILIATION.md` table covers every key present in the now-deleted export (cross-checked against the SUMMARY's key list) and includes the four-fixture conflict review — OR is explicitly marked BLOCKED (no-export path).
- `git diff .editorconfig` reviewed: additive-only on success, empty on BLOCKED, never carve-out weakening.
- Working tree has no `rs-export.editorconfig`.
- BLOCKED path: the reconciliation marker and the 50-01-SUMMARY status both say BLOCKED (they must agree).
</verification>

<success_criteria>
- A reconciliation report records every RS-vs-existing difference with KEEP-EXISTING / ADOPT-RS / CARVE-OUT-WINS plus enforceability — OR is an explicit BLOCKED marker matching a BLOCKED SUMMARY when no export was provided.
- No ADOPT-RS key re-introduces a carve-out conflict at a different key (verified against the four CarveOutGuard fixtures before the phase gate).
- The reconciled `.editorconfig` is the single source of truth, carve-outs overriding, with zero mass reflow.
- The transient export is gone; the report is the permanent trail.
</success_criteria>

<output>
Create `.planning/phases/50-code-style-enforcement/50-01-SUMMARY.md` when done. The SUMMARY MUST: (1) carry an explicit status — `COMPLETE` on the success path, or `BLOCKED` on the no-export path (in which case it states `.editorconfig` is unchanged and Plans 02-04 must NOT run until reconciliation completes — the SUMMARY status and the 50-RECONCILIATION.md marker MUST agree, FIX MEDIUM 3); (2) on success, list which keys were ADOPT-RS (so Plan 02 knows whether the gate needs `style` mode vs whitespace-only — noting the gate default is FULL); (3) on success, record that the ADOPT-RS list was reviewed against the four CarveOutGuard fixtures (init / raw-string / attribute / switch) with the outcome (none conflicting, or which were reclassified) BEFORE Plan 04's scratch PRs; and (4) on success, confirm all five carve-outs survived.
</output>
