---
phase: 83-packet-service-srp-split
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - DeckFlow.Web.Tests/PacketByteIdentityFixtures.cs
  - DeckFlow.Web.Tests/DeckAnalysisByteIdentityTests.cs
  - DeckFlow.Web.Tests/DeckComparisonByteIdentityTests.cs
  - DeckFlow.Web.Tests/MetaGapByteIdentityTests.cs
  - DeckFlow.Web.Tests/DeckPrimerByteIdentityTests.cs
autonomous: true
requirements: [PKTSVC-04]
must_haves:
  truths:
    - "An automated test suite captures each of the 4 packet services' paste-artifact text and asserts it exactly (Ordinal) against a checked-in golden captured from real output, for all 3 AI variants (ChatGPT/Claude/Gemini)."
    - "The Analysis suite sweeps every PromptMutatingAnalysisFlags flag plus reference.full-oracle-text and reference.deck-stats both ON and OFF, AND a single ALL-flags-ON combination case."
    - "The harness exercises every code path the Wave-2 migrations touch: includeVersions=true decklist with a Possible-Includes section and a renamed/DFC card; a real collection-miss/fallback path per resolver consumer; and a commander-inference/reflag path per Comparison/MetaGap."
    - "The suite passes green against the UNREFACTORED services, establishing the baseline every Wave-2 migration is gated against."
  artifacts:
    - path: "DeckFlow.Web.Tests/PacketByteIdentityFixtures.cs"
      provides: "Shared deterministic fixture decks (incl. miss/DFC/no-commander/whitespace cases) + CreateService seam re-use for all 4 byte-identity suites"
      min_lines: 60
    - path: "DeckFlow.Web.Tests/DeckAnalysisByteIdentityTests.cs"
      provides: "Analysis artifact byte-identity across 3 platforms x flag matrix (single-flag + all-on), includeVersions decklist, and single-slash collection-miss fallback"
      min_lines: 40
    - path: "DeckFlow.Web.Tests/DeckComparisonByteIdentityTests.cs"
      provides: "Comparison artifact byte-identity across 3 platforms, incl. a printed-name SearchFallback case and a no-commander-section reflag case"
      min_lines: 30
    - path: "DeckFlow.Web.Tests/MetaGapByteIdentityTests.cs"
      provides: "MetaGap artifact byte-identity across 3 platforms, incl. a forced collection-miss and a no-commander-section reflag case"
      min_lines: 30
    - path: "DeckFlow.Web.Tests/DeckPrimerByteIdentityTests.cs"
      provides: "Primer artifact byte-identity across 3 platforms, incl. whitespace-bearing request fields"
      min_lines: 30
  key_links:
    - from: "DeckFlow.Web.Tests/PacketByteIdentityFixtures.cs"
      to: "DeckAnalysisPacketServiceTests.CreateService / DeckComparisonServiceTests / MetaGapServiceTests / DeckPrimerPacketServiceTests seams"
      via: "reuse of existing executeCollectionAsyncOverride / override-delegate constructors (no live HTTP)"
      pattern: "executeCollectionAsyncOverride|loadDeckEntriesAsyncOverride"
---

<objective>
Build the byte-identical regression harness (PKTSVC-04) FIRST, on the four UNREFACTORED packet
services. This is the ENTIRE safety net for the phase: every Wave-2 migration is proven
behavior-neutral by re-running this suite and asserting it still passes unchanged. A harness with a
coverage hole is worse than none — a regression that slips through it is shipped silently. Therefore
this plan explicitly requires fixtures that drive every path the migrations extract.

Purpose: Capture each service's exact paste-artifact bytes deterministically across the paths that
actually change under refactor (versioned decklist, collection-miss/fallback, commander reflag, flag
combinations, whitespace normalization) so a dropped blank line, a swapped fallback strategy, a
mis-keyed resolver match, or a whitespace-collapse change fails a test immediately.
Output: One shared fixtures file + four per-service `*ByteIdentityTests.cs` suites, all green
against today's code, with goldens captured from REAL output (not hand-approximated).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/83-packet-service-srp-split/83-RESEARCH.md
@CLAUDE.md

<interfaces>
<!-- Captured result-record fields the harness must snapshot (paste artifacts only). -->
DeckAnalysisPacketResult: ReferenceText, AnalysisPromptText, SetUpgradePromptText, RequestContextText, DecklistText, SetUpgradeCardText
DeckComparisonResult: DeckAListText, DeckBListText, DeckAComboText, DeckBComboText, ComparisonContextText, ComparisonPromptText, FollowUpPromptText, RequestContextText
MetaGapResult: PromptText, RequestContextText, DecklistText
DeckPrimerPacketResult: PromptTextsByPlatform (already keyed per platform), RequestContextText, DecklistText

AiPlatforms = ["ChatGPT", "Claude", "Gemini"]  (per ResultContractTests.cs:25)
Analysis prompt-mutating flags (PromptMutatingAnalysisFlags, DeckAnalysisPacketService.cs:158-164):
  analysis.command-zone-awareness, analysis.multi-axis-score, analysis.interaction-audit, analysis.wincon-map
Additional reference-block flags to sweep ON/OFF: analysis.reference.full-oracle-text, analysis.reference.deck-stats
</interfaces>

<path_coverage_requirements>
<!-- These are NON-NEGOTIABLE fixtures. The migrations extract exactly these paths; without a
     fixture forcing each, a regression passes the gate. Grounded in file:line reads. -->

1. Analysis versioned decklist (guards H1 — DeckAnalysisPacketService.cs:1053-1139):
   - One Analysis fixture built with IncludeCardVersions=true, a full-decklist analysis question,
     commander + mainboard entries WITH SetCode/CollectorNumber, at least one Possible-Includes
     (sideboard/maybeboard) entry, and one renamed/DFC card (a `A // B` name and a card whose
     printed name differs from oracle name so the oracleNameMap "[printed as: X]" annotation fires).
   - The golden MUST capture the current asymmetry: commander/mainboard lines get the
     ` (SET) COLLECTOR` suffix and DFC-slash truncation, but Possible-Includes lines stay PLAIN
     (no suffix, no slash truncation) even with IncludeCardVersions=true.

2. Collection-miss / fallback per resolver consumer (guards H2 —
   DeckAnalysisPacketService.cs:1950-2043 keys matches by ORIGINAL request name vs returned card.Name):
   - Analysis fixture with a single-slash `" / "` (Archidekt-style) name whose normalized submission
     is `" // "`: the collection response returns the `//` card.Name, the original `" / "` name does
     NOT match it, so the entry falls through to SearchPrintingFallbackCardAsync — the golden locks
     that fallthrough (incl. the `submitted_name: ... | resolved_card: ...` displayName branch).
   - Comparison fixture with a printed-name that resolves via SearchFallbackCardAsync (collection miss).
   - MetaGap fixture with a forced collection miss recovered via SearchFallbackCardAsync.

3. Commander inference / reflag per Comparison + MetaGap (guards M2 —
   DeckComparisonService.cs:363 / MetaGapService.cs:465):
   - One Comparison fixture and one MetaGap fixture whose deck source has NO explicit Commander
     section, so ReflagCommanderEntry actually runs (reflagging the first Quantity==1 name match) —
     affecting decklist text, combo inputs, and cache-key inputs.

4. Analysis flag COMBINATION (guards M1):
   - In addition to all-off + each-single-flag-on, one "ALL four PromptMutatingAnalysisFlags ON"
     case using the companion/partner fixture, to exercise shared combo-fetch/enrichment ordering
     under interacting flags.

5. Whitespace normalization fields (guards H3 — locks NormalizeSingleLine behavior BEFORE 83-06/07):
   - Every service's fixture request populates DeckName / StrategyNotes / MetaNotes (whichever apply)
     with values containing a tab, an embedded newline, a run of multiple spaces, and a bare `\r`,
     so RequestContextText and any prompt "deck_name:/strategy_notes:/meta_notes:" lines lock the
     EXACT current whitespace-collapse output per service before any normalizer is touched.
</path_coverage_requirements>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Shared fixtures + Analysis & Comparison byte-identity suites</name>
  <files>DeckFlow.Web.Tests/PacketByteIdentityFixtures.cs, DeckFlow.Web.Tests/DeckAnalysisByteIdentityTests.cs, DeckFlow.Web.Tests/DeckComparisonByteIdentityTests.cs</files>
  <read_first>
    - DeckFlow.Web.Tests/DeckAnalysisPacketServiceTests.cs (the CreateService(...) seam ~1846-1911, incl. CreateCompanionFixtureEntries; deterministic executeCollectionAsyncOverride/executeSearchAsyncOverride/executeNamedAsyncOverride fixtures)
    - DeckFlow.Web.Tests/DeckComparisonServiceTests.cs (its analogous CreateService seam)
    - DeckFlow.Web.Tests/AnalysisScorePromptParityTests.cs (the exact-byte Ordinal excision pattern to mirror, :76-92)
    - DeckFlow.Web.Tests/ResultContractTests.cs (DI-free registry construction + AiPlatforms array :25-35)
    - DeckFlow.Web/Services/DeckAnalysisPacketService.cs:1053-1139 (BuildDecklistText/FormatDecklistLine — the H1 asymmetry) and :1950-2043 (LookupCardReferencesAsync — the H2 match-by-original-name + single-slash fallthrough) and :158-164 (PromptMutatingAnalysisFlags)
    - DeckFlow.Web/Services/DeckComparisonService.cs:46-60 (result fields), :363-383 (ReflagCommanderEntry), :385-445 (LookupCardDetailsAsync / SearchFallback)
  </read_first>
  <action>
    Create `PacketByteIdentityFixtures.cs` in `namespace DeckFlow.Web.Tests` exposing the deterministic
    fixture decks and Scryfall override responses required by <path_coverage_requirements> items 1-5
    above (reuse `CreateCompanionFixtureEntries`-style data where possible; add the single-slash DFC
    card, the printed-name≠oracle card, the no-commander-section deck, and the whitespace-bearing
    request fields as new deterministic fixtures — no live HTTP). Provide helpers that build each
    service via its EXISTING test seam (real ScryfallCardResolver with the override delegates so the
    real batch/fallback logic runs, real prompt-variant registries with all 3 concrete variants, fresh
    PacketSessionCache, a fake/stub IFeatureFlagCache whose flag states are parameterizable).

    Create `DeckAnalysisByteIdentityTests.cs`: Theory over the 3 AiPlatforms crossed with the flag
    matrix — each of the 4 PromptMutatingAnalysisFlags keys plus analysis.reference.full-oracle-text
    and analysis.reference.deck-stats, exercised BOTH ON and OFF (all-OFF baseline plus each-flag-ON),
    PLUS one ALL-four-mutating-flags-ON case (M1) on the companion/partner fixture. Include the
    IncludeCardVersions=true decklist fixture (item 1) asserting the Possible-Includes-stay-plain
    asymmetry, and the single-slash collection-miss fixture (item 2) asserting the SearchPrintingFallback
    path output. For every combination assert the captured artifact fields (AnalysisPromptText,
    SetUpgradePromptText, ReferenceText, RequestContextText, DecklistText, SetUpgradeCardText values)
    equal a checked-in golden using `StringComparison.Ordinal`.

    Create `DeckComparisonByteIdentityTests.cs`: Theory over the 3 AiPlatforms (Comparison has no
    prompt-mutating flags — confirm by grep and record as a comment). Include the printed-name
    SearchFallback fixture (item 2) and the no-commander-section reflag fixture (item 3). Capture and
    Ordinal-assert DeckAListText, DeckBListText, DeckAComboText, DeckBComboText, ComparisonContextText,
    ComparisonPromptText, FollowUpPromptText, RequestContextText against goldens.

    GOLDEN-CAPTURE INTEGRITY (W3): Do NOT hand-type or approximate any golden. For each new assertion,
    FIRST run the test with the real output dumped (temporary `Assert.Fail(actual)` or console/attach
    of `actual`), copy the EXACT captured bytes into the golden constant/`.txt` fixture, THEN flip to
    the Ordinal equality assertion and confirm green. A golden that was not captured from a real run is
    a defect. Persist goldens as verbatim string constants OR checked-in `.txt` fixtures loaded via
    File.ReadAllText — mirror the repo's existing prompt-template fixture handling; pick ONE mechanism.

    Do NOT modify any production service file in this plan. Capture the CURRENT (unrefactored) output.
  </action>
  <verify>
    <automated>dotnet.exe test DeckFlow.Web.Tests --filter "FullyQualifiedName~ByteIdentity" 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet.exe build DeckFlow.Web.Tests` is 0 warnings / 0 errors.
    - Both new suites run and PASS against the unrefactored services (green baseline).
    - Analysis suite includes: all 6 flags ON and OFF, an ALL-4-mutating-flags-ON case, the
      IncludeCardVersions=true decklist fixture asserting Possible-Includes-stay-plain, and the
      single-slash collection-miss fallback fixture (grep the test source to confirm each).
    - Comparison suite includes the printed-name SearchFallback fixture and the no-commander-section reflag fixture.
    - Every assertion uses StringComparison.Ordinal; every golden was captured from a real run (no hand-typed goldens).
  </acceptance_criteria>
  <done>Analysis + Comparison artifacts are golden-locked across the full path matrix, green on today's code, goldens captured from real output.</done>
</task>

<task type="auto">
  <name>Task 2: MetaGap & Primer byte-identity suites</name>
  <files>DeckFlow.Web.Tests/MetaGapByteIdentityTests.cs, DeckFlow.Web.Tests/DeckPrimerByteIdentityTests.cs</files>
  <read_first>
    - DeckFlow.Web.Tests/MetaGapServiceTests.cs (CreateService seam; EDH Top16 + Scryfall override fixtures)
    - DeckFlow.Web.Tests/DeckPrimerPacketServiceTests.cs (uses the SECOND internal ctor with loadDeckEntriesAsyncOverride/findCombosAsyncOverride — Primer never calls Scryfall)
    - DeckFlow.Web/Services/MetaGapService.cs:42-50 (result fields), :465-485 (ReflagCommanderEntry), :562-621 (ResolveOracleNameMapAsync / SearchFallback)
    - DeckFlow.Web/Services/DeckPrimerPacketService.cs:83-92 (result fields, PromptTextsByPlatform), :551-576 (NormalizeSingleLine + char-by-char CollapseWhitespace), :112-118 (StaleFlag gates a UI banner, NOT prompt text)
    - DeckFlow.Web.Tests/PacketByteIdentityFixtures.cs (from Task 1)
  </read_first>
  <action>
    Create `MetaGapByteIdentityTests.cs`: Theory over the 3 AiPlatforms. Include the forced
    collection-miss fixture (item 2) and the no-commander-section reflag fixture (item 3). Capture and
    Ordinal-assert PromptText, RequestContextText, DecklistText against goldens. Confirm (and comment)
    MetaGap has no prompt-mutating feature-flag registry.

    Create `DeckPrimerByteIdentityTests.cs`: a single `BuildAsync` per fixture yields
    `PromptTextsByPlatform` keyed for all 3 platforms — iterate the 3 keys and Ordinal-assert each
    platform's prompt text against a golden, plus RequestContextText and DecklistText. Populate the
    fixture request's DeckName/StrategyNotes/MetaNotes with tab/newline/multi-space/bare-`\r` values
    (item 5) so Primer's char-by-char CollapseWhitespace output is locked exactly. Sweep the Primer
    `tool.primer.stale-flag` both ON and OFF and assert prompt bytes are byte-identical across the
    toggle (it gates only a UI banner).

    GOLDEN-CAPTURE INTEGRITY (W3): capture every golden from a real run before flipping to Ordinal
    equality (same discipline as Task 1). Do NOT modify any production service file.
  </action>
  <verify>
    <automated>dotnet.exe test DeckFlow.Web.Tests --filter "FullyQualifiedName~ByteIdentity" 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - Build 0/0; both new suites PASS against unrefactored services.
    - MetaGap suite includes the forced collection-miss fixture and the no-commander-section reflag fixture.
    - Primer suite locks whitespace-bearing fields and proves prompt-text byte-identity is unaffected by tool.primer.stale-flag ON vs OFF.
    - All 4 services now have a green golden-locked byte-identity suite covering every path the Wave-2 migrations touch; goldens captured from real output.
  </acceptance_criteria>
  <done>The full PKTSVC-04 harness exists, green on today's code, with no coverage hole through which a migration regression can pass — the gate is armed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → production services | Read-only: the harness invokes BuildAsync via existing test seams; it introduces no new external input surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-83-01 | Tampering | golden fixtures capturing wrong (already-broken) output | mitigate | Goldens are captured from the current shipped code, which is verified in production; the suite proves NEUTRALITY of the refactor, not correctness of the baseline. |
| T-83-02 | Repudiation | non-deterministic capture (live HTTP / timing) causing flaky goldens | mitigate | Reuse only the existing deterministic override-delegate seams (no live HTTP); exclude timing/TimingSummary fields from the golden assertions. |
| T-83-15 | Tampering | a coverage hole (missing versioned-decklist / miss-fallback / reflag / flag-combo fixture) letting a migration regression pass the gate | mitigate | <path_coverage_requirements> mandates a fixture per extracted path; acceptance criteria grep for each. |
| T-83-16 | Repudiation | hand-approximated golden that always passes | mitigate | W3 golden-capture-integrity instruction: capture from a real run before asserting equality. |
</threat_model>

<verification>
- `dotnet.exe build DeckFlow.Web.Tests` → 0/0.
- `dotnet.exe test DeckFlow.Web.Tests --filter "FullyQualifiedName~ByteIdentity"` → all pass.
- Grep confirms all 6 Analysis flag keys + an all-flags-on case + the IncludeCardVersions + single-slash-miss fixtures in the Analysis suite; the fallback + no-commander fixtures in Comparison/MetaGap; whitespace fields in Primer.
</verification>

<success_criteria>
PKTSVC-04's guard EXISTS and is green on the unrefactored services with NO coverage hole: 4 services
x 3 AI variants, Analysis flags ON/OFF + all-on, versioned decklist, per-consumer collection-miss
fallback, per-service commander reflag, whitespace normalization — Ordinal byte-equality, goldens
captured from real output. No production file changed.
</success_criteria>

<output>
Create `.planning/phases/83-packet-service-srp-split/83-01-SUMMARY.md` when done.

Scope note to record in the summary (W2): PacketArtifactStore.cs zip-serialization duplication
(REFACTOR-BACKLOG.md row 6 / STATE.md:86) is EXPLICITLY DEFERRED — out of PKTSVC-01..04 scope
(the criteria cover prompt-assembly and Scryfall-resolution, not zip serialization). Nothing
silently dropped; it remains recorded in REFACTOR-BACKLOG.md.
</output>
