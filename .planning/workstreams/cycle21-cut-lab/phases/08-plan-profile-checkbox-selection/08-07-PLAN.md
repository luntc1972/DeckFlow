---
phase: 08-plan-profile-checkbox-selection
plan: 07
type: execute
wave: 5
# ⛔ GATED ON PHASE 7. depends_on lists only in-phase plans because GSD resolves those IDs;
# the cross-phase gate is stated here and in <phase_gate> below and MUST be honored by the executor.
# Phase 7 (Cut Lab Workflow UX) rewrites DeckFlow.Web/Views/Deck/CutLab.cshtml and
# DeckFlow.Web/wwwroot/ts/cut-lab.ts and reserves the wizard step slot this panel fills.
# Do not execute this plan until Phase 7 is complete and its SUMMARYs are on the branch.
depends_on: ["08-04", "08-06"]
files_modified:
  - DeckFlow.Web/Models/CutLabRequest.cs
  - DeckFlow.Web/Controllers/CutLabController.cs
  - DeckFlow.Web/Services/CutLab/CutLabPageService.cs
  - DeckFlow.Web/Models/CutLabViewModel.cs
  - DeckFlow.Web/Views/Deck/CutLab.cshtml
  - DeckFlow.Web/wwwroot/ts/cut-lab.ts
  - DeckFlow.Web/wwwroot/css/site-common.css
  - DeckFlow.Web/Controllers/Api/CutLabApiController.cs
  - DeckFlow.Web/Models/Api/CutLabPlanApplyApiRequest.cs
  - DeckFlow.Web/Models/Api/CutLabPlanApplyApiResponse.cs
  - DeckFlow.Web.Tests/CutLabPageServiceTests.cs
  - DeckFlow.Web.Tests/CutLabControllerTests.cs
  - DeckFlow.Web.Tests/CutLabApiControllerTests.cs
  - DeckFlow.Web/ts-tests/cut-lab-proposal.test.ts
  - DeckFlow.Web/e2e/cut-lab-smoke.spec.ts
  - DeckFlow.Web/e2e/cut-lab-structure.spec.ts
  - DeckFlow.Web/e2e/cut-lab-scenarios.spec.ts
  - DeckFlow.Web/e2e/cut-lab-tuning.spec.ts
  - DeckFlow.Web/e2e/cut-lab-export.spec.ts
  - DeckFlow.Web/e2e/cut-lab-pill-interactions.spec.ts
  - DeckFlow.Web/e2e/cut-lab-nav-themes.spec.ts
  - DeckFlow.Web/e2e/cut-lab-theme-readability.spec.ts
  - DeckFlow.Web/e2e/cut-lab-whatif.spec.ts
autonomous: true
requirements: [PLPR-01, PLPR-02, PLPR-04]

must_haves:
  truths:
    - "The intake form no longer contains a free-text primary or secondary plan field."
    - "After a deck is processed and before the cut rounds, a plan panel presents the twelve generic strategy checkboxes, each with a one-line definition and a mechanical consequence line."
    - "The commander theme section lists EDHREC themes ordered by deck count, with the top three at or above a five percent share pre-checked."
    - "When EDHREC is unavailable the commander section says so plainly and the generic strategy checkboxes still work."
    - "With every box unchecked the panel states that the engine behaves exactly as it does today."
    - "A session restored from saved state comes back with the same boxes checked."
  artifacts:
    - path: "DeckFlow.Web/Views/Deck/CutLab.cshtml"
      provides: "The plan panel inside Phase 7's reserved wizard step slot, and the removal of the two free-text plan fields"
      contains: "cut-lab-plan-panel"
    - path: "DeckFlow.Web/wwwroot/ts/cut-lab.ts"
      provides: "Checkbox state capture into the round-tripped session field"
      contains: "cut-lab-plan-panel"
    - path: "DeckFlow.Web/wwwroot/css/site-common.css"
      provides: "Theme-agnostic layout for the panel at both viewport classes"
      contains: "cut-lab-plan-panel"
  key_links:
    - from: "DeckFlow.Web/Views/Deck/CutLab.cshtml"
      to: "CutLabRequest.PlanStrategies / PlanThemes"
      via: "checkbox inputs bound by name to the request model"
      pattern: "name=\"PlanStrategies\""
    - from: "DeckFlow.Web/Services/CutLab/CutLabPageService.cs"
      to: "CutLabIntent.PlanProfile"
      via: "request-to-intent projection alongside the existing bracket and play-experience mapping"
      pattern: "PlanProfile = "
---

<objective>
Give the plan profile a user interface: replace the two deterministically-inert free-text plan
fields with a single checkbox panel in Phase 7's reserved wizard step, and make the panel explain
in plain language what each box actually does to the engine.

Purpose: everything built in 08-01 through 08-06 is unreachable until a user can check a box.
Output: the request contract, the panel markup, a state-carrying plan-apply AJAX round trip, its
layout CSS, and the migration of all nine existing e2e specs off the removed field.
</objective>

<phase_gate>
⛔ **This plan is gated on Phase 7 and must not start before it completes.**

Phase 7 rewrites `DeckFlow.Web/Views/Deck/CutLab.cshtml` and `DeckFlow.Web/wwwroot/ts/cut-lab.ts`
into workflow order and, per the ROADMAP Phase 7 entry, "must reserve an empty wizard step slot for
Phase 8's plan panel so Phase 8's UI inserts without restructuring the wizard".

Before writing any markup, the executor MUST:
1. Confirm Phase 7's SUMMARY files exist in `.planning/workstreams/cycle21-cut-lab/phases/07-*/`.
2. Consume the named Phase 7 slot exactly: empty `role="tabpanel"` element
   `#cut-lab-step-panel-3`, wizard index 3. Do NOT invent a slot or restructure the wizard.
   D-1 is resolved as Option 3, wizard + pinned proposal; Phase 7's panel-hiding remains in force.
3. Read Phase 7's TypeScript step-navigation contract before adding a change handler, so the panel
   participates in step validation rather than bypassing it.

If the reserved slot is absent, STOP and report rather than improvising a location.
</phase_gate>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/cycle21-cut-lab/phases/08-plan-profile-checkbox-selection/08-CONTEXT.md
@.planning/specs/2026-08-02-cutlab-plan-profile-design.md
@.planning/workstreams/cycle21-cut-lab/ROADMAP.md
@DeckFlow.Web/Models/CutLabRequest.cs

<consumer_census>
<!-- Every reader of the two fields being removed from the form, and of the id being deleted.
     Verified by grep across C#, Razor, TypeScript, the TS unit tests and the e2e suite. -->

| Consumer | file:line | Reads | Status after removal |
|---|---|---|---|
| Intake textarea | `DeckFlow.Web/Views/Deck/CutLab.cshtml:159-162` | `Model.Request.PrimaryPlan`, id `cut-lab-primary-plan` | DELETED by this plan |
| Request-to-intent projection | `DeckFlow.Web/Services/CutLab/CutLabPageService.cs:923-924` | `request.PrimaryPlan` / `SecondaryPlan` | Keeps compiling; stops being populated from the form. Left in place so an old round-tripped state still maps. |
| Intake rehydration | `DeckFlow.Web/Controllers/CutLabController.cs:419-420` | `state.Intent.PrimaryPlan` | Keeps compiling; must also rehydrate the new plan-profile selections |
| `CutLabIntent` legacy properties | `DeckFlow.Web/Models/CutLab/CutLabState.cs:192,195` | — | UNCHANGED. In-flight sessions must still deserialize (PLPR-01). Do not delete them. |
| e2e smoke | `DeckFlow.Web/e2e/cut-lab-smoke.spec.ts:56,88,118` | `#cut-lab-primary-plan` | BREAKS — migrated by Task 3 |
| e2e structure | `DeckFlow.Web/e2e/cut-lab-structure.spec.ts:55,427` | same | BREAKS — migrated by Task 3 |
| e2e scenarios | `DeckFlow.Web/e2e/cut-lab-scenarios.spec.ts:40,47,50,122-170` | same, and asserts session restore THROUGH this field | BREAKS — migrated by Task 3, restore assertion re-pointed |
| e2e tuning | `DeckFlow.Web/e2e/cut-lab-tuning.spec.ts:272,301,306` | same | BREAKS — migrated by Task 3 |
| e2e export | `DeckFlow.Web/e2e/cut-lab-export.spec.ts:39,46,49` | same | BREAKS — migrated by Task 3 |
| e2e pill interactions | `DeckFlow.Web/e2e/cut-lab-pill-interactions.spec.ts:64,121,142` | same | BREAKS — migrated by Task 3 |
| e2e nav themes | `DeckFlow.Web/e2e/cut-lab-nav-themes.spec.ts` | same | BREAKS — migrated by Task 3 |
| e2e theme readability | `DeckFlow.Web/e2e/cut-lab-theme-readability.spec.ts` | same | BREAKS — migrated by Task 3 |
| e2e what-if | `DeckFlow.Web/e2e/cut-lab-whatif.spec.ts` | same | BREAKS — migrated by Task 3 |
| TS unit-test DOM fixtures | `DeckFlow.Web/ts-tests/*.test.ts` (8 files, `<textarea name="PrimaryPlan">`) | their own synthetic DOM | UNAFFECTED except `cut-lab-proposal.test.ts`, which Task 3 edits to prove a restored legacy session retains its persisted values when those controls are absent. |

This census is the reason Task 3 exists inside this plan rather than as a follow-up: removing the field
without migrating all nine specs would land the branch with a red e2e suite.
</consumer_census>

<interfaces>
<!-- Consumed -->
CutLabPlanProfile { IReadOnlyList<string> GenericStrategies; IReadOnlyList<CutLabCommanderTheme> CommanderThemes; bool CommanderThemesUnavailable; }   // 08-01
DeckPlanStrategyCatalog.Entries   // 08-01 — Slug, DisplayName, Definition, Consequence per entry
IEdhrecCommanderThemeService.GetCommanderThemesAsync(string, CancellationToken) -> EdhrecThemeResult   // 08-03
EdhrecCommanderThemeService.SelectDefaultThemes(IReadOnlyList<CutLabCommanderTheme>)                   // 08-03
CutLabResolvedFloor.PlanDelta                                                                          // 08-04

<!-- Contract this plan CREATES -->
CutLabRequest:
  IReadOnlyList<string> PlanStrategies { get; set; }   // checked strategy slugs, form-bound
  IReadOnlyList<string> PlanThemes { get; set; }       // checked theme slugs, form-bound
CutLabViewModel: a plan-panel view model carrying, per strategy, Slug/DisplayName/Definition/
  Consequence/IsChecked; per theme, Slug/DisplayName/DeckCount/SharePercent/IsChecked; plus
  CommanderThemesUnavailable and a ZeroSelectionNotice flag.
CutLabPlanApplyApiRequest:
  string CutLabStateJson { get; init; }   // required serialized state; init-only so System.Text.Json binds it
CutLabPlanApplyApiResponse:
  CutLabUiPatchDto Patch { get; init; }   // the server-authored replacement state/UI patch
</interfaces>

<constraints>
- Layout CSS goes in `site-common.css`, NEVER `site.css` — guild themes are standalone forks of `site.css`.
- Use `--panel` for panel surfaces, not `--theme-surface`, which renders light inside dark themes.
- `wwwroot/js/*.js` is gitignored and MSBuild recompiles the TypeScript. Never stage compiled JS.
- Checkbox copy is a curated finite list — never freeform. Every checkbox carries a definition line AND a
  mechanical consequence line; an undocumented label is the prior-art failure this design explicitly avoids.
- `PrimaryPlan` / `SecondaryPlan` stay on `CutLabIntent` and on `CutLabRequest`. Only the form fields go.
- Preserve each touched file's line endings.
</constraints>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bind checked strategies and themes through the request and into the intent</name>
  <files>DeckFlow.Web/Models/CutLabRequest.cs, DeckFlow.Web/Services/CutLab/CutLabPageService.cs, DeckFlow.Web/Controllers/CutLabController.cs, DeckFlow.Web/Models/CutLabViewModel.cs, DeckFlow.Web.Tests/CutLabPageServiceTests.cs, DeckFlow.Web.Tests/CutLabControllerTests.cs</files>
  <read_first>
    - DeckFlow.Web/Models/CutLabRequest.cs (whole file — the existing form-bound property conventions)
    - DeckFlow.Web/Services/CutLab/CutLabPageService.cs around line 923 (the request-to-`CutLabIntent`
      projection that gains the `PlanProfile` assignment)
    - DeckFlow.Web/Controllers/CutLabController.cs lines 405-435 (`RehydrateIntakeRequestFromState`, which
      must repopulate the new selections so a restored session shows the same boxes checked)
    - DeckFlow.Web/Models/CutLabViewModel.cs (how existing sections build their view rows, and the
      floor-row record that 08-04 extended with `PlanDelta`)
    - DeckFlow.Core/Analysis/DeckPlanStrategyCatalog.cs and DeckFlow.Web/Services/CutLab/EdhrecCommanderThemeService.cs
      (the catalog copy and the preselector this projection consumes)
  </read_first>
  <action>
    Add `PlanStrategies` and `PlanThemes` to `CutLabRequest` as form-bound string collections, following the
    file's existing property style.

    In the `CutLabPageService` request-to-intent projection, build `PlanProfile` from those two collections:
    filter strategy slugs through `DeckPlanStrategyCatalog.TryGetBySlug` so an unknown slug is discarded, and
    resolve theme slugs against the commander's fetched theme list so `CommanderThemes` carries real display
    names and deck counts rather than client-supplied strings. Set `CommanderThemesUnavailable` from the
    `EdhrecThemeResult`. Leave the existing `PrimaryPlan` / `SecondaryPlan` assignments untouched.

    Apply the pre-check rule on first presentation only — when the incoming request carries no
    `PlanThemes` at all and the session has no prior `PlanProfile` — using
    `EdhrecCommanderThemeService.SelectDefaultThemes`. A user who deliberately unchecks every theme must not
    have the top three re-checked on the next round trip. Use `PlanProfile == null` for never presented and
    a non-null `CutLabPlanProfile` with empty collections for presented and cleared; thread that distinction
    through the request projection, session state, Razor and TypeScript. Do not add a marker property. If a
    later implementation proves one indispensable, it must be a persisted `{ get; init; }` property.

    Extend `RehydrateIntakeRequestFromState` to copy the state's `PlanProfile` selections back onto the
    request, alongside the bracket and play-experience lines it already restores.

    Build the plan-panel view model in `CutLabViewModel` as described in `<interfaces>`: one row per
    `DeckPlanStrategyCatalog.Entries` item carrying its slug, display name, definition, consequence and
    checked state; one row per known EDHREC theme carrying its slug, display name, deck count, its share of
    the commander's total theme deck count, and its checked state, ordered by deck count descending; plus
    the unavailable flag and a zero-selection flag that is true when nothing at all is checked.
  </action>
  <verify>
    <automated>dotnet build DeckFlow.Web/DeckFlow.Web.csproj 2>&amp;1 | tail -5 &amp;&amp; dotnet test DeckFlow.Web.Tests/DeckFlow.Web.Tests.csproj --filter "FullyQualifiedName~CutLabPageServiceTests|FullyQualifiedName~CutLabControllerTests"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "PlanStrategies\|PlanThemes" DeckFlow.Web/Models/CutLabRequest.cs` returns both properties.
    - `grep -n "PlanProfile = " DeckFlow.Web/Services/CutLab/CutLabPageService.cs` returns the assignment.
    - `grep -n "PrimaryPlan" DeckFlow.Web/Models/CutLab/CutLabState.cs` still returns the legacy property —
      it was not deleted.
    - A test asserts that submitting an unknown strategy slug produces a `PlanProfile` whose
      `GenericStrategies` does not contain it.
    - A `CutLabPageServiceTests` case asserts that a non-null empty profile does NOT get the top three
      re-checked, while a null profile does; a `CutLabControllerTests` case proves that distinction survives
      restore.
    - `dotnet build DeckFlow.Web/DeckFlow.Web.csproj` reports 0 errors and no more than 9 warnings.
  </acceptance_criteria>
  <done>Checked boxes survive the round trip into `CutLabIntent.PlanProfile`, unknown slugs are discarded, pre-check applies only on first presentation, and a restored session shows the same selections.</done>
</task>

<task type="auto">
  <name>Task 2: Render the plan panel in Phase 7's reserved slot and wire its TypeScript state</name>
  <files>DeckFlow.Web/Views/Deck/CutLab.cshtml, DeckFlow.Web/wwwroot/ts/cut-lab.ts, DeckFlow.Web/Controllers/Api/CutLabApiController.cs, DeckFlow.Web/Models/Api/CutLabPlanApplyApiRequest.cs, DeckFlow.Web/Models/Api/CutLabPlanApplyApiResponse.cs, DeckFlow.Web.Tests/CutLabApiControllerTests.cs</files>
  <read_first>
    - `.planning/workstreams/cycle21-cut-lab/phases/07-*/07-*-SUMMARY.md` (ALL of them — the reserved wizard
      slot's actual element, id and step index, and the D-1 step-model decision that shaped the surrounding
      markup)
    - DeckFlow.Web/Views/Deck/CutLab.cshtml (current state after Phase 7 — the reserved slot, the existing
      intake block at lines 155-170 whose two textareas are removed, and the established section markup
      conventions)
    - DeckFlow.Web/wwwroot/ts/cut-lab.ts (current state after Phase 7 — the step-navigation contract and the
      existing pattern for capturing form state into the round-tripped session field)
    - DeckFlow.Web/Models/CutLabViewModel.cs (the plan-panel view model added in Task 1)
  </read_first>
  <action>
    Delete the primary-plan and secondary-plan textareas and their labels from the intake block, leaving
    intake at deck input plus bracket plus play experience.

    Render the plan panel into Phase 7's reserved wizard slot under a root element carrying the class
    `cut-lab-plan-panel`. It has three parts:

    A generic-strategies fieldset — one checkbox per view-model strategy row, each with a visible label
    (display name), the one-line definition, and the mechanical consequence line, all rendered through Razor
    expression syntax so they are HTML-encoded. Inputs are named `PlanStrategies` with the slug as value, so
    model binding collects the checked set.

    A commander-themes fieldset — one checkbox per theme row named `PlanThemes`, labelled with the display
    name and its deck count, ordered by deck count descending. When the view model reports commander themes
    unavailable, render a single plain sentence in that fieldset's place saying the commander themes could
    not be loaded and that the generic strategies still apply, and render no theme checkboxes.

    A zero-selection notice — shown when nothing is checked — stating plainly that with no boxes checked the
    engine behaves exactly as it does today.

    Do not render a marker input: the canonical distinction is null profile (never presented) versus a
    non-null profile with empty collections (presented and cleared).

    In `cut-lab.ts`, attach a change handler on the panel root that builds the non-null profile in the
    serialized state and immediately performs the **plan-apply** round trip. Reuse the decide AJAX transport
    shape at `cut-lab.ts:3325-3345`: JSON POST, `RequestVerificationToken`, same-origin endpoint resolution,
    busy/error handling, and `applyServerPatch`. Post a typed `CutLabPlanApplyApiRequest` containing the current
    `CutLabStateJson`; `/api/cut-lab/plan-apply` validates/rebuilds the profile server-side and returns the
    existing UI patch DTO. The client replaces its hidden state from that patch before rendering it. This is
    an explicit apply operation, not a form-association assumption: the slot is outside the intake form.
    Register the handler through Phase 7's five-slot contract so the panel participates in navigation.

    In `CutLabApiController`, add the plan-apply endpoint beside decide. It must call
    `SameOriginRequestValidator.IsValid(Request)` and return its standard 403 result before
    processing the body, then use the same cancellation, state deserialization and patch-building
    path. Return a `CutLabPlanApplyApiResponse` carrying the `CutLabUiPatchDto`; add controller
    tests for empty versus non-empty profile, patch/state return, and rejected cross-origin input.
    Never stage anything under `wwwroot/js/`.

    Never stage anything under `wwwroot/js/`.
  </action>
  <verify>
    <automated>dotnet build DeckFlow.Web/DeckFlow.Web.csproj 2>&amp;1 | tail -5 &amp;&amp; npx --no-install vitest run --dir DeckFlow.Web/ts-tests</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "cut-lab-primary-plan" DeckFlow.Web/Views/Deck/CutLab.cshtml` returns 0.
    - `grep -n "cut-lab-plan-panel" DeckFlow.Web/Views/Deck/CutLab.cshtml DeckFlow.Web/wwwroot/ts/cut-lab.ts`
      matches in both files.
    - `grep -c "name=\"PlanStrategies\"" DeckFlow.Web/Views/Deck/CutLab.cshtml` returns 1 (a single templated
      input inside the loop).
    - No theme or strategy string is emitted through `Html.Raw` — `grep -n "Html.Raw" DeckFlow.Web/Views/Deck/CutLab.cshtml`
      shows no new occurrence inside the panel block.
    - `git status --porcelain DeckFlow.Web/wwwroot/js/` is empty.
    - The TypeScript compiles as part of the build with no new errors.
  </acceptance_criteria>
  <done>Intake is down to URL, bracket and experience; the panel renders in Phase 7's reserved slot with documented checkboxes, an unavailable state and a zero-selection notice, and its state round-trips.</done>
</task>

<task type="auto">
  <name>Task 3: Style the panel, migrate all selector consumers, and protect legacy state</name>
  <files>DeckFlow.Web/wwwroot/css/site-common.css, DeckFlow.Web/e2e/cut-lab-smoke.spec.ts, DeckFlow.Web/e2e/cut-lab-structure.spec.ts, DeckFlow.Web/e2e/cut-lab-scenarios.spec.ts, DeckFlow.Web/e2e/cut-lab-tuning.spec.ts, DeckFlow.Web/e2e/cut-lab-export.spec.ts, DeckFlow.Web/e2e/cut-lab-pill-interactions.spec.ts, DeckFlow.Web/e2e/cut-lab-nav-themes.spec.ts, DeckFlow.Web/e2e/cut-lab-theme-readability.spec.ts, DeckFlow.Web/e2e/cut-lab-whatif.spec.ts, DeckFlow.Web/ts-tests/cut-lab-proposal.test.ts</files>
  <read_first>
    - DeckFlow.Web/wwwroot/css/site-common.css (the existing Cut Lab layout blocks and the responsive
      breakpoint convention this panel must follow)
    - DeckFlow.Web/wwwroot/css/site-mobile.css (how the mobile viewport class is applied, so the panel is
      styled through the same mechanism rather than a new media query)
    - DeckFlow.Web/e2e/cut-lab-scenarios.spec.ts (lines 40-60 and 115-175 — the only spec that asserts
      SESSION RESTORE through the removed field; the other five merely fill it)
    - DeckFlow.Web/e2e/cut-lab-smoke.spec.ts (the simplest of the five fill-only specs, as the pattern for
      the other four)
  </read_first>
  <action>
    Add the panel's layout to `site-common.css` — never `site.css`. Scope every rule under
    `.cut-lab-plan-panel`. Use the existing panel surface token `--panel`, not `--theme-surface`. The
    checkbox rows read as a single-column stack on the mobile viewport class and may use a multi-column grid
    on desktop; the definition and consequence lines must remain readable at the mobile width without
    horizontal scrolling. Do not introduce new colour tokens; consume the ones already declared.

    Migrate all nine e2e specs. In the fill-only specs — smoke, structure, tuning, export, pill
    interactions — delete each `#cut-lab-primary-plan` fill or visibility assertion and any now-unused
    parameter threaded solely to supply that text. These are the lines enumerated in the consumer census;
    nav-themes, theme-readability and what-if — delete each `#cut-lab-primary-plan` interaction and adapt
    the flow to the panel; work from the census so none is missed.

    `cut-lab-scenarios.spec.ts` needs more than deletion: its saved-versus-fresh session test asserts restore
    THROUGH the primary-plan field. Re-point that assertion at a field that still exists and is still
    restored by `RehydrateIntakeRequestFromState` — the bracket or the play experience — so the test keeps
    proving session restore rather than being weakened to a smoke check. Preserve the distinct saved/fresh
    values so the assertion can still tell the two sessions apart.

    In `cut-lab.ts`, when `PrimaryPlan` / `SecondaryPlan` controls are absent, preserve
    `persistedState.intent.primaryPlan` and `persistedState.intent.secondaryPlan` in `buildSnapshotFromDom`.
    Add a `cut-lab-proposal.test.ts` legacy-session fixture with removed controls and non-empty serialized
    legacy values; assert the resulting snapshot retains both values.
  </action>
  <verify>
    <automated>grep -rc "cut-lab-primary-plan" DeckFlow.Web/e2e/ | grep -v ":0" || echo "no references remain"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "cut-lab-primary-plan" DeckFlow.Web/e2e/ DeckFlow.Web/Views/` returns nothing.
    - `grep -n "cut-lab-plan-panel" DeckFlow.Web/wwwroot/css/site-common.css` returns matches; the same grep
      against `DeckFlow.Web/wwwroot/css/site.css` returns nothing.
    - `grep -n "theme-surface" DeckFlow.Web/wwwroot/css/site-common.css` shows no new occurrence inside the
      panel block.
    - `git diff --name-only DeckFlow.Web/ts-tests/` includes `cut-lab-proposal.test.ts` for the required
      legacy-session regression and no unrelated TS-test file.
    - `cut-lab-scenarios.spec.ts` still contains two distinct session values and an equality assertion on a
      restored field — the restore coverage was re-pointed, not deleted.
    - The legacy-session TypeScript regression proves missing legacy controls preserve both persisted values.
    - The Cut Lab e2e suite passes headless against a server started with `scripts/run-web-test.sh`. Ask the
      user before opening any browser; run Playwright with `env -u DISPLAY npx --no-install playwright test`.
  </acceptance_criteria>
  <done>The panel is styled in `site-common.css` for both viewport classes, all nine e2e specs no longer reference the removed field, the explicit plan-apply patch updates the UI, and `cut-lab-scenarios` still proves session restore.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser form post → `CutLabRequest.PlanStrategies` / `PlanThemes` | Untrusted user input crosses into the model binder |
| EDHREC theme display names → Razor output | Third-party strings are rendered into the page |
| session state field → page | The round-tripped Cut Lab session JSON is client-controlled |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-07-01 | Tampering | Injected strategy or theme slug in the form post | mitigate | Strategy slugs are filtered through `DeckPlanStrategyCatalog.TryGetBySlug` and theme slugs are resolved against the commander's fetched theme list before either reaches `CutLabIntent.PlanProfile`; unmatched values are discarded, not stored. A test asserts an unknown slug is dropped. |
| T-08-07-02 | Information Disclosure | Cross-site scripting via an EDHREC theme display name or a catalog copy string | mitigate | All panel text is emitted through Razor expression syntax, which HTML-encodes by default. No `Html.Raw`, no `@Html.Encode`-bypassing helper and no `innerHTML` assignment is introduced in the panel block; the acceptance criteria grep for `Html.Raw` inside the panel. In TypeScript, use `textContent` rather than `innerHTML` for any dynamic label. |
| T-08-07-03 | Tampering | Cross-site request forgery on `POST /api/cut-lab/plan-apply` | mitigate | The new JSON endpoint must reject a request when `SameOriginRequestValidator.IsValid(Request)` is false, returning its standard 403 before deserializing state; skipping that validator on an API endpoint is prohibited. The client uses same-origin endpoint resolution and sends `RequestVerificationToken`, matching the existing Cut Lab API transport. |
| T-08-07-04 | Denial of Service | An enormous `PlanStrategies` or `PlanThemes` collection in a crafted post | mitigate | Both collections are reduced to the fixed catalog and the fetched theme list before storage, bounding the persisted set at twelve strategies and the commander's real theme count. The model binder's default collection limit applies to the raw post. |
| T-08-07-05 | Repudiation | A user unable to tell why a proposal changed | mitigate | Every checkbox carries a mechanical consequence line, and the zero-selection notice states the no-op explicitly, so the panel documents its own effect. |
| T-08-07-SC | Tampering | npm / NuGet installs | mitigate | No packages added; Playwright and vitest are already present. Any new package stops the plan pending explicit user approval. |
</threat_model>

<verification>
1. Phase 7 is complete and its SUMMARYs are present — the `<phase_gate>` check passed before any markup was written.
2. `dotnet build DeckFlow.sln` — 0 errors, at most 9 warnings; the TypeScript compiles as part of the build.
3. `dotnet test DeckFlow.Web.Tests/DeckFlow.Web.Tests.csproj` full suite passes.
4. `npx --no-install vitest run --dir DeckFlow.Web/ts-tests` passes, including the required
   `cut-lab-proposal.test.ts` legacy-session regression.
5. Cut Lab e2e suite green headless, server started via `scripts/run-web-test.sh` so no browser opens on the
   Windows host. Ask the user before launching any browser.
6. `grep -rn "cut-lab-primary-plan" DeckFlow.Web/` returns nothing.
7. `git status --porcelain DeckFlow.Web/wwwroot/js/` is empty.
8. `git diff --stat` vs `git diff --ignore-all-space --stat` agree — no EOL churn.
</verification>

<success_criteria>
- Intake carries no free-text plan field, and in-flight sessions still deserialize.
- The plan panel sits in Phase 7's reserved wizard slot with twelve documented strategy checkboxes.
- Commander themes render deck-count ordered with the top three at or above five percent pre-checked, and an
  explicit unavailable state when EDHREC could not be reached.
- The zero-selection notice states the no-op plainly.
- Panel layout lives in `site-common.css` and reads correctly at both viewport classes.
- All nine migrated e2e specs pass, with session-restore coverage preserved rather than weakened.
</success_criteria>

<output>
Create `.planning/workstreams/cycle21-cut-lab/phases/08-plan-profile-checkbox-selection/08-07-SUMMARY.md` when done.
</output>
