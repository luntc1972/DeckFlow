---
phase: 51-verify-v1-7-on-main-non-prod-uat
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/51-verify-v1-7-on-main-non-prod-uat/51-OPS-RESULTS.md
autonomous: false
requirements: [OPS-01]
must_haves:
  truths:
    - "The Render `deckflow` web service deploys from the `main` branch (the deploy branch is flipped from the old deploy branch to main in the Render dashboard), and a deploy succeeds from main"
    - "Cycle 8 work is confirmed to branch off the v1.7-inclusive `main` (HEAD of main contains squash 39e74d55 + tag 2026.06.3) so future work builds on the verified base"
    - "The OPS-01 result (PASS / FAIL / WAIVED-with-reason) is recorded in 51-OPS-RESULTS.md"
  artifacts:
    - path: ".planning/phases/51-verify-v1-7-on-main-non-prod-uat/51-OPS-RESULTS.md"
      provides: "Recorded Render deploy-branch flip + branch-base confirmation"
      contains: "main"
  key_links:
    - from: "51-OPS-RESULTS.md"
      to: "Render service deckflow deploy branch"
      via: "operator flips deploy branch to main and confirms a successful deploy"
      pattern: "deploy branch"
---

<objective>
Finish OPS-01: v1.7 is already merged to `main` and tagged (squash 39e74d55, tag 2026.06.3). The remainder is the operator action to flip the live Render `deckflow` web service's deploy branch to `main` and confirm a deploy succeeds from it, plus confirming Cycle 8 work branches off the v1.7-inclusive `main`. Record the result.

Purpose: Until the live Render service deploys from `main`, prod is not tracking the v1.7-inclusive base; OPS-01 is the gate that makes future-cycle work deploy from the verified branch.
Output: A recorded OPS-01 result in `51-OPS-RESULTS.md`. No product code changes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@render.yaml

<interfaces>
<!-- Ops facts the operator needs; render.yaml is IaC-only and does NOT sync to the live service. -->

From render.yaml (header comment):
- The running DeckFlow service was created standalone in the dashboard, NOT via blueprint — changes to render.yaml do NOT sync to it. The deploy-branch change is a dashboard (or Render MCP) action on the live `deckflow` web service.
- Service name: `deckflow`; autoDeploy: true; secrets (DECKFLOW_DATABASE_CONNECTION_STRING, FEEDBACK_ADMIN_USER, FEEDBACK_ADMIN_PASSWORD) use `sync: false`.

Local git facts (for the branch-base confirmation):
- `main` HEAD = 1b73498 (cycle-8 start) → c2ab3cc → 39e74d5 (v1.7 squash); tag 2026.06.3 + tag v1.7 point at the v1.7 commit.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Flip Render deploy branch to main + confirm a successful deploy (OPS-01)</name>
  <read_first>
    - render.yaml (the header note: live service is dashboard-managed, render.yaml does NOT sync; service name `deckflow`)
    - .planning/REQUIREMENTS.md OPS-01 traceability line (remainder = flip Render deploy branch → main)
    - CLAUDE.md "Do Not Modify Without Explicit Permission": render.yaml / deployment config is operator-owned; the AI does not edit it or push.
  </read_first>
  <what-needs-doing>
    This is a genuine human-only action: it changes the live Render dashboard deploy configuration for the `deckflow` web service. The AI cannot perform the dashboard flip.

    **Ordering gate (verify-then-deploy):** Do NOT perform the deploy-branch flip until the Phase 51 UAT smokes are recorded — 51-UAT-RESULTS.md (51-01) and 51-STUDIO-UAT-RESULTS.md (51-02) must each exist with a PASS or explicitly WAIVED-with-reason result. Flipping prod to `main` before the smokes are recorded reverses the intended order. If a smoke is FAIL, hold the flip and capture the defect as a follow-up finding first.
  </what-needs-doing>
  <how-to-verify>
    1. In the Render dashboard, open the `deckflow` web service → Settings → Build & Deploy → Branch. Confirm the current deploy branch, then change it to `main` (if it is not already `main`). Save.
    2. Trigger (or allow autoDeploy to run) a deploy from `main` and confirm it reaches "Live" / succeeds. Note the deployed commit SHA matches `main` HEAD.
    3. Confirm the deployed site at https://www.deckflow.gg responds (health check path `/` returns 200) after the deploy from main.
  </how-to-verify>
  <action>Operator performs the dashboard deploy-branch flip to `main` and confirms a deploy from main succeeds and the live site responds. Record PASS / FAIL / WAIVED-with-reason with the date, the prior deploy branch, the new deploy branch (`main`), and the deployed commit SHA under a "## OPS-01 — Render deploy branch flip" heading in 51-OPS-RESULTS.md. Do NOT paste any secret/connection-string value. The AI must not edit render.yaml or push; this is a dashboard-only operator action.</action>
  <acceptance_criteria>
    - 51-UAT-RESULTS.md and 51-STUDIO-UAT-RESULTS.md both exist with a PASS or WAIVED-with-reason result BEFORE the flip (verify-then-deploy gate satisfied).
    - Render `deckflow` service deploy branch == `main` (recorded prior + new branch).
    - A deploy from `main` succeeded and https://www.deckflow.gg returns 200 on `/` post-deploy; deployed SHA matches main HEAD.
    - 51-OPS-RESULTS.md "## OPS-01" section records the result, branches, SHA, and date; no secrets in the file.
  </acceptance_criteria>
  <resume-signal>Type "approved" with the deploy-branch result (or "fail/waived: <reason>").</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Confirm Cycle 8 branches off v1.7-inclusive main</name>
  <read_first>
    - .planning/STATE.md (last_activity: v1.7 merged to main squash 39e74d55 + tagged 2026.06.3; Cycle 8 started on main 1b73498)
    - .planning/ROADMAP.md Cycle 8 "Depends on" note (OPS-01 merge complete; remainder = Render flip)
  </read_first>
  <what-built>v1.7 was squash-merged to main (39e74d55) and tagged 2026.06.3; Cycle 8 planning commits sit on top of main. This task confirms the verified base is the branch point for Cycle 8 work.</what-built>
  <how-to-verify>
    1. Confirm `git merge-base --is-ancestor 39e74d5 main` succeeds (the v1.7 squash is an ancestor of main HEAD).
    2. Confirm `git tag --points-at 39e74d5` includes 2026.06.3 (and v1.7), i.e. the CalVer tag is on the v1.7 commit reachable from main.
    3. Confirm the active working branch for Cycle 8 is `main` (or a branch created from current main HEAD), so Phase 51+ work builds on the v1.7-inclusive base.
  </how-to-verify>
  <action>Run the three git confirmations above (read-only; no push, no branch creation unless the operator chooses to branch Cycle 8 work). Record PASS / FAIL with the date, the `main` HEAD SHA, and the tag-points-at output under a "## OPS-01 — Cycle 8 branch base" heading in 51-OPS-RESULTS.md. If the v1.7 squash is NOT an ancestor of main, mark FAIL and capture it as a follow-up finding.</action>
  <acceptance_criteria>
    - `git merge-base --is-ancestor 39e74d5 main` returns success (exit 0).
    - 2026.06.3 (and v1.7) tag points at the v1.7 squash commit reachable from main.
    - 51-OPS-RESULTS.md "## OPS-01 — Cycle 8 branch base" section records PASS/FAIL with main HEAD SHA + tag output + date.
  </acceptance_criteria>
  <resume-signal>Type "approved" with the branch-base result (or "fail: <detail>").</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → Render dashboard | Live deploy-config change to the `deckflow` web service |
| repo → prod | Flipping deploy branch to main makes future main commits auto-deploy to prod |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-51-04-01 | Information disclosure | Render env secrets (DB conn string, admin creds) | mitigate | render.yaml uses `sync: false` for all secrets; the flip is a branch-only dashboard change that does not touch secret values; no secret is written into 51-OPS-RESULTS.md |
| T-51-04-02 | Tampering | Unintended prod deploy from an unverified commit | mitigate | The flip targets `main`, whose HEAD is confirmed (Task 2) to descend from the v1.7-inclusive squash before any deploy is allowed to go live |
| T-51-04-03 | Elevation of privilege | AI editing deployment config / pushing main | accept→prevented | render.yaml + deploy config are on the do-not-modify list; the AI performs no edit/push — Task 1 is a human-action checkpoint |
| T-51-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs — ops + verification only |
</threat_model>

<verification>
- 51-OPS-RESULTS.md exists with "## OPS-01 — Render deploy branch flip" and "## OPS-01 — Cycle 8 branch base" sections.
- No render.yaml / deployment config edits and no git push performed by the AI.
</verification>

<success_criteria>
The Render `deckflow` service deploy branch is flipped to `main` with a successful deploy confirmed, Cycle 8's branch base is confirmed to include the v1.7 squash + CalVer tag, and both results are recorded in 51-OPS-RESULTS.md.
</success_criteria>

<output>
Create `.planning/phases/51-verify-v1-7-on-main-non-prod-uat/51-04-SUMMARY.md` when done.
</output>
