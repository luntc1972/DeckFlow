---
phase: 98-card-grounding-guard
plan: 04
type: execute
wave: 3
depends_on: ["98-02"]
files_modified:
  - DeckFlow.Web.Tests/Services/Scryfall/CardGroundingHallucinationFixtureTests.cs
autonomous: true
requirements: [CS-25]

must_haves:
  truths:
    - "A plausible-but-fake card name is rejected as NotFound"
    - "A real card that is banned in Commander (Dockside Extortionist, banned 2024-09-23) is rejected as NotLegal — a fake or illegal card that previously would have shipped now cannot"
    - "An off-color-identity real card is rejected as IdentityViolation"
    - "A card already in the submitted deck is rejected as SingletonDuplicate"
    - "An ambiguous fuzzy name is rejected as Ambiguous, distinct from NotFound"
    - "A typo of a real card (Dockside Extortonist) heals to the canonical name and is accepted only if it also passes legality/identity"
  artifacts:
    - path: "DeckFlow.Web.Tests/Services/Scryfall/CardGroundingHallucinationFixtureTests.cs"
      provides: "Known-hallucination fixture table asserting exact reject reasons (CS-25)"
      contains: "CardGroundingHallucination"
  key_links:
    - from: "DeckFlow.Web.Tests/Services/Scryfall/CardGroundingHallucinationFixtureTests.cs"
      to: "CardGroundingGuard"
      via: "guard under test with a FakeResolver"
      pattern: "CardGroundingGuard"
---

<objective>
Add the CS-25 known-hallucination fixture tests: a fixture table exercising the strict guard against the classic LLM hallucination shapes — plausible-but-fake names, real-but-now-banned staples, off-identity staples, cards already in the submitted deck, and ambiguous names — asserting the exact `CardGroundingRejectReason` for each. These are the regression fixtures that prove a fake or illegal card that previously would have shipped now cannot (the phase's #1 pitfall).

Purpose: Success criterion 4 of the phase requires "tests including known-hallucination fixtures that previously would have shipped a fake or illegal card." This plan is the falsifiable proof the guard works end-to-end on realistic bad inputs, driven through the same `FakeResolver` seam so no live network is touched.
Output: 1 new test file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/98-card-grounding-guard/98-CONTEXT.md
@.planning/phases/98-card-grounding-guard/98-RESEARCH.md
@.planning/phases/98-card-grounding-guard/98-PATTERNS.md
@.planning/phases/98-card-grounding-guard/98-02-SUMMARY.md

<interfaces>
<!-- Guard + verdict shapes from 98-02. -->

  interface ICardGroundingGuard { Task<CardGroundingVerdict> TryValidateAsync(string, CardGroundingDeckContext, CancellationToken); Task<CardGroundingBatchResult> ValidateAllAsync(IReadOnlyList<string>, CardGroundingDeckContext, CancellationToken); }
  record CardGroundingVerdict { bool Accepted; string CanonicalName; CardGroundingRejectReason RejectReason; }
  enum CardGroundingRejectReason { None, NotFound, Ambiguous, NotLegal, IdentityViolation, SingletonDuplicate, Uncastable, UpstreamUnavailable }
  record CardGroundingDeckContext { IReadOnlySet<string> CommanderColorIdentity; IReadOnlySet<char> DeckProducedColors; IReadOnlySet<string> DeckCardNames; }

Fixture seams already established in 98-02's CardGroundingGuardTests.cs:
  a FakeResolver (implements IScryfallCardResolver; drives ExecuteCollectionAsync + ExecuteNamedFuzzyAsync with canned RestResponse<ScryfallCard> incl. StatusCode + Content), and a Card() factory (now able to set ColorIdentity + Legalities + ManaCost + TypeLine).
  Reuse the same seam style here. The real ScryfallCard/legalities/404-body shapes were verified live 2026-07-18 (RESEARCH Pitfall 1 + Code Examples).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the known-hallucination fixture tests (CS-25)</name>
  <files>DeckFlow.Web.Tests/Services/Scryfall/CardGroundingHallucinationFixtureTests.cs</files>
  <read_first>
    - DeckFlow.Web.Tests/Services/Scryfall/CardGroundingGuardTests.cs (the FakeResolver + Card() factory built in 98-02 — same seam shape; replicate the fake locally, it is private nested there)
    - DeckFlow.Web.Tests/Services/Scryfall/ScryfallCardNameGrounderTests.cs (the proven "Dockside Extortonist" typo-heals-to-real-card fixture and Card() factory)
    - DeckFlow.Web/Services/Scryfall/CardGroundingGuard.cs (reject precedence order to assert exact reasons)
    - .planning/phases/98-card-grounding-guard/98-RESEARCH.md (Pitfall 1 ambiguous 404 body; Validation Architecture CS-25 fixture list; live-verified legalities shape)
    - .planning/phases/98-card-grounding-guard/98-CONTEXT.md (Specific Ideas: the fixture shapes; D-09 Dockside banned legality reject)
  </read_first>
  <action>
    Create `CardGroundingHallucinationFixtureTests.cs` in namespace `DeckFlow.Web.Tests`, driving the real `CardGroundingGuard` through a `FakeResolver` (98-02 builds `FakeResolver` as a private nested class inside `CardGroundingGuardTests`, which is inaccessible from this file — REPLICATE the fake here with the same seam shape (implements `IScryfallCardResolver`, canned `RestResponse<ScryfallCard>` incl. StatusCode + Content); do not add a new interface and do not modify 98-02's file). Each fixture constructs a canned resolver response modelling the named scenario, invokes `TryValidateAsync` with a chosen `CardGroundingDeckContext`, and asserts the exact `CardGroundingRejectReason` (and `Accepted`).

    Cover at minimum these named fixtures (the classic hallucination shapes from CONTEXT Specific Ideas + RESEARCH):
    - Plausible-but-fake name (e.g. "Prismatic Lotus Vault" or similar invented name): fuzzy resolver returns 404 with a plain not-found body -> assert Accepted=false, RejectReason=NotFound.
    - Ambiguous name (fuzzy 404 body containing `"type":"ambiguous"`, e.g. the "aust com" case from RESEARCH): assert RejectReason=Ambiguous (distinct from NotFound).
    - Real-but-banned staple: "Dockside Extortionist" resolves (200) with `legalities["commander"] = "banned"` (banned 2024-09-23 per reference_dockside_banned.md): assert RejectReason=NotLegal. This is the headline fixture — a real card the guard must now REJECT that a naive validator would ship.
    - Typo-of-real-card: "Dockside Extortonist" fuzzy-heals to a resolved card; if that card's legalities are "legal" and it is in-identity, assert Accepted=true with CanonicalName="Dockside Extortionist"; (pair it with the banned fixture to show typo-healing does not bypass legality).
    - Off-identity staple: a resolved, legal card whose ColorIdentity is not a subset of the deck context's CommanderColorIdentity (e.g. a blue card into a mono-red commander context): assert RejectReason=IdentityViolation.
    - Already-in-deck card: a resolved, legal, in-identity card whose canonical name is in `deckContext.DeckCardNames` (non-basic): assert RejectReason=SingletonDuplicate; plus a basic-land counter-fixture (a Forest already in the deck) asserting it is NOT a SingletonDuplicate.

    Prefer a `[Theory]`/`[InlineData]` or `[MemberData]` fixture table where the canned response + expected reason can be tabulated, with standalone `[Fact]`s for the cases needing distinct resolver Content bodies (ambiguous vs not-found). Add a top-of-file doc comment listing the fixture provenance (LLM-typical fakes + the Dockside banned reference note).
  </action>
  <verify>
    <automated>cd /mnt/c/users/chrislunt/source/personal/deckflow-cycle17 && dotnet build DeckFlow.Web.Tests/DeckFlow.Web.Tests.csproj -warnaserror 2>&1 | tail -5 && dotnet test DeckFlow.Web.Tests/DeckFlow.Web.Tests.csproj --filter "FullyQualifiedName~Hallucination" 2>&1 | tail -6</automated>
  </verify>
  <acceptance_criteria>
    - The file contains a fixture asserting `RejectReason.NotLegal` for a real card whose commander legality is "banned" (the Dockside fixture).
    - The file contains distinct fixtures asserting `RejectReason.Ambiguous` and `RejectReason.NotFound` from two different fuzzy-404 bodies.
    - The file contains fixtures asserting `RejectReason.IdentityViolation` and `RejectReason.SingletonDuplicate`, plus a basic-land case that is NOT a singleton violation.
    - At least one fixture asserts an Accepted verdict with the canonical name for a healed typo of a legal, in-identity card.
    - No live network: all Scryfall responses are canned through the FakeResolver (`grep -c "HttpClient\|api.scryfall.com" <file>` returns 0).
    - `dotnet test ... --filter "FullyQualifiedName~Hallucination"` all passed, 0 failed; `dotnet build -warnaserror` 0 warnings.
  </acceptance_criteria>
  <done>The CS-25 hallucination fixture suite is green, proving fake/banned/off-identity/duplicate/ambiguous inputs are rejected with the exact reasons and only safe cards are accepted.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| hallucinated input → guard | The fixtures ARE the adversarial inputs (fake/banned/off-identity/duplicate names) crossing the guard boundary; the test asserts the boundary holds |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-98-30 | Spoofing (regression: a bad card starts shipping) | Guard behavior over time | mitigate | Fixture regression suite pins exact reject reasons for the canonical hallucination shapes; a future guard change that fails-open breaks these tests (this plan IS the mitigation for the phase's #1 pitfall) |
| T-98-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs (test-only, RESEARCH Package Legitimacy Audit: zero new dependencies) |
</threat_model>

<verification>
- `dotnet build DeckFlow.Web.Tests/DeckFlow.Web.Tests.csproj -warnaserror` clean.
- `dotnet test DeckFlow.Web.Tests --filter "FullyQualifiedName~Hallucination"` all green.
- Fixtures use only the FakeResolver seam — no live Scryfall.
</verification>

<success_criteria>
- CS-25: known-hallucination fixtures cover fake, banned (real-but-illegal), off-identity, already-in-deck, and ambiguous inputs, each asserting the exact reject reason, plus an accepted safe card — proving the guard rejects everything that previously would have shipped a fake or illegal card.
</success_criteria>

<output>
Create `.planning/phases/98-card-grounding-guard/98-04-SUMMARY.md` when done.
</output>
