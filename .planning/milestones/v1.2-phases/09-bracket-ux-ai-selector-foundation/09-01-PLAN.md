---
phase: 09-bracket-ux-ai-selector-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - DeckFlow.Web/wwwroot/css/site-common.css
  - DeckFlow.Web/Views/Shared/_AiSelector.cshtml
  - DeckFlow.Web/Views/Shared/_BracketCallout.cshtml
autonomous: true
requirements:
  - BRKT-01
  - AISEL-01

must_haves:
  truths:
    - "site-common.css contains all .bracket-callout and .ai-selector CSS blocks using only existing theme tokens"
    - "_AiSelector.cshtml exists in Views/Shared with @model string and three radio inputs for ChatGPT, Claude, Gemini"
    - "_BracketCallout.cshtml exists in Views/Shared with the .bracket-callout wrapper and eyebrow label"
  artifacts:
    - path: "DeckFlow.Web/Views/Shared/_AiSelector.cshtml"
      provides: "Razor partial — AI target radio pill group"
      contains: "ai-selector"
    - path: "DeckFlow.Web/Views/Shared/_BracketCallout.cshtml"
      provides: "Razor partial — bracket callout wrapper"
      contains: "bracket-callout"
    - path: "DeckFlow.Web/wwwroot/css/site-common.css"
      provides: "CSS rules for both new components"
      contains: ".ai-selector__option:checked"
  key_links:
    - from: "Views/Shared/_AiSelector.cshtml"
      to: "site-common.css .ai-selector"
      via: "class='ai-selector'"
      pattern: "ai-selector"
    - from: "Views/Shared/_BracketCallout.cshtml"
      to: "site-common.css .bracket-callout"
      via: "class='bracket-callout'"
      pattern: "bracket-callout"
---

<objective>
Add the CSS rules and shared Razor partials that BRKT-01 and AISEL-01 require. No C# model changes
in this plan — pure CSS and markup.

Purpose: Establish the visual primitives before the view-insertion plan wires them in. Plans 01 and
02 run in parallel; Plan 03 depends on both.

Output: _AiSelector.cshtml, _BracketCallout.cshtml, appended CSS in site-common.css.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-bracket-ux-ai-selector-foundation/09-UI-SPEC.md
@.planning/phases/09-bracket-ux-ai-selector-foundation/09-RESEARCH.md

<interfaces>
<!-- site-common.css ends at line 1164 with .chatgpt-sticky-download__button -->
<!-- Append new blocks after that line — do NOT edit site.css or any site-*.css -->
<!-- Existing token references confirmed available: --panel, --accent-strong, --accent,
     --line, --info, --line-cool, --panel-soft-bg, --muted, --on-accent, --ink,
     --fs-sm, --fs-base, --fs-lg, --focus -->
<!-- .sr-only already defined at site.css line 76 — do NOT redefine it -->

<!-- Partial model pattern — verified from _MoxfieldBulkEditHint.cshtml: -->
<!--   @model string   (at top of partial) -->
<!--   Called via: @await Html.PartialAsync("_AiSelector", someStringValue) -->

<!-- Radio checked binding pattern (Razor): -->
<!--   checked="@(Model == "ChatGPT")"  where @model string means Model is the string -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Append bracket-callout and ai-selector CSS to site-common.css</name>
  <files>DeckFlow.Web/wwwroot/css/site-common.css</files>
  <read_first>
    - DeckFlow.Web/wwwroot/css/site-common.css (read the last 20 lines to find the append point; confirm no bracket-callout or ai-selector selectors already present)
    - .planning/phases/09-bracket-ux-ai-selector-foundation/09-UI-SPEC.md (CSS spec section — exact property values)
  </read_first>
  <action>
Append the following two CSS blocks to the END of site-common.css (after the last existing rule).
Use exactly these selector names and token references — no hardcoded hex values, no new :root tokens.

Block 1 — Bracket callout:
```css
/* ── Bracket Required-Field Callout (BRKT-01) ─────────────────────────── */
.bracket-callout {
    border: 1px solid var(--line);
    border-left: 4px solid var(--accent-strong);
    border-radius: 8px;
    background: var(--panel-soft-bg);
    padding: 16px;
    margin-bottom: 16px;
}

.bracket-callout__label {
    font-size: var(--fs-sm);
    font-weight: 600;
    color: var(--accent-strong);
    text-transform: uppercase;
    letter-spacing: 0.05em;
    margin-bottom: 8px;
}
```

Block 2 — AI selector:
```css
/* ── AI Target Selector (AISEL-01) ────────────────────────────────────── */
.ai-selector {
    border: 1px solid var(--line-cool);
    border-radius: 10px;
    background: var(--info);
    padding: 16px;
    margin-bottom: 16px;
}

.ai-selector__heading {
    font-size: var(--fs-lg);
    font-weight: 600;
    color: var(--ink);
    margin: 0 0 8px 0;
}

.ai-selector__options {
    display: flex;
    flex-wrap: wrap;
    gap: 8px;
}

.ai-selector__option-label {
    display: inline-flex;
    align-items: center;
    gap: 4px;
    padding: 8px 16px;
    border: 2px solid var(--line);
    border-radius: 6px;
    background: var(--panel);
    color: var(--ink);
    font-size: var(--fs-base);
    font-weight: 400;
    cursor: pointer;
    min-height: 44px;
    user-select: none;
}

.ai-selector__option:checked + .ai-selector__option-label {
    border-color: var(--accent-strong);
    background: var(--panel-soft-bg);
    font-weight: 600;
    color: var(--accent-strong);
}

.ai-selector__option-label:hover {
    border-color: var(--accent);
    color: var(--accent);
}

.ai-selector__hint {
    font-size: var(--fs-sm);
    color: var(--muted);
    margin: 8px 0 0 0;
}
```

Do NOT add a `.ai-selector__option` rule — the hidden radio uses the existing `.sr-only` class
from site.css. Do NOT touch site.css or any site-*.css file.
  </action>
  <verify>
    <automated>grep -c "bracket-callout" /mnt/c/users/chrislunt/source/personal/decksyncworkbench/DeckFlow.Web/wwwroot/css/site-common.css && grep -c "ai-selector__option:checked" /mnt/c/users/chrislunt/source/personal/decksyncworkbench/DeckFlow.Web/wwwroot/css/site-common.css && grep -c "var(--accent-strong)" /mnt/c/users/chrislunt/source/personal/decksyncworkbench/DeckFlow.Web/wwwroot/css/site-common.css</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "bracket-callout" site-common.css returns >= 2 (both .bracket-callout and .bracket-callout__label present)
    - grep -c "ai-selector__option:checked" site-common.css returns 1
    - grep "var(--accent-strong)" site-common.css shows at least the bracket-callout__label and the :checked state rule
    - grep "var(--info)" site-common.css shows the .ai-selector background rule
    - site.css is unchanged: git diff DeckFlow.Web/wwwroot/css/site.css shows no changes
    - No guild theme file (site-selesnya.css, site-dimir.css, etc.) has been modified
    - No hardcoded hex color values in the new blocks
  </acceptance_criteria>
  <done>site-common.css has both CSS blocks appended; all selectors use only existing theme tokens; site.css and guild theme files untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Create _AiSelector.cshtml and _BracketCallout.cshtml shared partials</name>
  <files>
    DeckFlow.Web/Views/Shared/_AiSelector.cshtml
    DeckFlow.Web/Views/Shared/_BracketCallout.cshtml
  </files>
  <read_first>
    - DeckFlow.Web/Views/Shared/ (list contents to confirm no _AiSelector or _BracketCallout already exist)
    - DeckFlow.Web/Views/Shared/_MoxfieldBulkEditHint.cshtml (reference for @model string partial pattern)
    - .planning/phases/09-bracket-ux-ai-selector-foundation/09-UI-SPEC.md (HTML structure spec)
    - .planning/phases/09-bracket-ux-ai-selector-foundation/09-RESEARCH.md (code examples section 6 and 7)
  </read_first>
  <action>
Create two new files in DeckFlow.Web/Views/Shared/.

--- FILE 1: _AiSelector.cshtml ---

@model string
@* AI target selector — radio pill group. Model is the currently-selected platform string.
   Accepted values: "ChatGPT", "Claude", "Gemini". Defaults to "ChatGPT" if model is null/empty.
   Usage: @await Html.PartialAsync("_AiSelector", Model.Request.TargetAiPlatform)
*@
@{
    var selected = string.IsNullOrEmpty(Model) ? "ChatGPT" : Model;
}
<div class="ai-selector">
    <p class="ai-selector__heading">Analyze with</p>
    <div class="ai-selector__options" role="radiogroup" aria-label="AI analysis target">
        <input type="radio" name="TargetAiPlatform" id="ai-chatgpt" value="ChatGPT"
               class="sr-only ai-selector__option" checked="@(selected == "ChatGPT")" />
        <label for="ai-chatgpt" class="ai-selector__option-label">ChatGPT</label>

        <input type="radio" name="TargetAiPlatform" id="ai-claude" value="Claude"
               class="sr-only ai-selector__option" checked="@(selected == "Claude")" />
        <label for="ai-claude" class="ai-selector__option-label">Claude</label>

        <input type="radio" name="TargetAiPlatform" id="ai-gemini" value="Gemini"
               class="sr-only ai-selector__option" checked="@(selected == "Gemini")" />
        <label for="ai-gemini" class="ai-selector__option-label">Gemini</label>
    </div>
    <p class="ai-selector__hint">ChatGPT format used for all targets in this phase. Claude and Gemini optimizations arrive in Phase 10.</p>
</div>


--- FILE 2: _BracketCallout.cshtml ---

@* Bracket required-field callout wrapper (BRKT-01).
   This partial renders only the outer .bracket-callout div and eyebrow label.
   The existing TargetCommanderBracket <label class="field"> block in ChatGptPackets.cshtml
   is placed inside this partial's wrapper via the calling view.
   Usage: wrap the bracket <label class="field"> inside @await Html.PartialAsync("_BracketCallout")
   
   NOTE: Because this partial wraps inline markup from the parent view, it cannot fully
   encapsulate the df-select content. Instead, the calling view (ChatGptPackets.cshtml)
   places the <div class="bracket-callout"> wrapper and <p class="bracket-callout__label">
   directly inline — this partial exists for documentation/discoverability only and is NOT
   called via PartialAsync. The actual bracket callout markup lives inline in ChatGptPackets.cshtml.
   See Plan 09-03 Task 1 for the inline wrapper implementation.
*@
<div class="bracket-callout">
    <p class="bracket-callout__label">Required before generating</p>
    @* Caller places the existing <label class="field"> for TargetCommanderBracket here *@
</div>

KEY RULES for _AiSelector.cshtml:
- @model string MUST be the first line (before any blank lines)
- The `name` attribute on all three radios MUST be exactly `TargetAiPlatform` (case-sensitive — must match the C# property name added in Plan 09-02)
- Use checked="@(selected == "ChatGPT")" not checked="checked" — Razor conditional attribute
- Do NOT add any data-df-select attributes — the AI selector uses native radio, not the df-select combobox
- The sr-only class on the inputs references site.css:76 — do not add a new .sr-only definition
  </action>
  <verify>
    <automated>ls /mnt/c/users/chrislunt/source/personal/decksyncworkbench/DeckFlow.Web/Views/Shared/_AiSelector.cshtml /mnt/c/users/chrislunt/source/personal/decksyncworkbench/DeckFlow.Web/Views/Shared/_BracketCallout.cshtml && grep -c "name=\"TargetAiPlatform\"" /mnt/c/users/chrislunt/source/personal/decksyncworkbench/DeckFlow.Web/Views/Shared/_AiSelector.cshtml && grep "model string" /mnt/c/users/chrislunt/source/personal/decksyncworkbench/DeckFlow.Web/Views/Shared/_AiSelector.cshtml</automated>
  </verify>
  <acceptance_criteria>
    - Both files exist under DeckFlow.Web/Views/Shared/
    - _AiSelector.cshtml first non-comment line is exactly: @model string
    - grep -c 'name="TargetAiPlatform"' _AiSelector.cshtml returns 3 (one per radio input)
    - grep -c 'class="sr-only ai-selector__option"' _AiSelector.cshtml returns 3
    - grep 'checked=' _AiSelector.cshtml shows @(selected == "ChatGPT"), @(selected == "Claude"), @(selected == "Gemini") — conditional Razor expressions, not static checked
    - grep 'ai-selector__hint' _AiSelector.cshtml returns the hint paragraph with "Phase 10" text
    - _BracketCallout.cshtml contains class="bracket-callout" and class="bracket-callout__label"
    - dotnet build DeckFlow.Web passes (no Razor compilation errors)
  </acceptance_criteria>
  <done>Both shared partials created; _AiSelector.cshtml has correct @model string declaration, three TargetAiPlatform radios with conditional checked binding, and the Phase 9 hint text.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → POST form | TargetAiPlatform radio value is user-supplied; server must not trust it for security-sensitive decisions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | Tampering | TargetAiPlatform form field | accept | Value is a UI hint only in Phase 9 — no authorization or data-access decision depends on it; accepted values "ChatGPT", "Claude", "Gemini" are validated server-side in Phase 10 when they drive artifact format branching |
| T-09-02 | Information Disclosure | CSS / Razor partials | accept | Static markup; no PII or secrets; no new attack surface |
</threat_model>

<verification>
dotnet build DeckFlow.Web — must produce 0 errors, 0 warnings about missing partials.

grep -c "bracket-callout" DeckFlow.Web/wwwroot/css/site-common.css
grep -c "ai-selector__option:checked" DeckFlow.Web/wwwroot/css/site-common.css
ls DeckFlow.Web/Views/Shared/_AiSelector.cshtml DeckFlow.Web/Views/Shared/_BracketCallout.cshtml
</verification>

<success_criteria>
- site-common.css has .bracket-callout and .ai-selector CSS blocks appended with zero hardcoded hex values
- _AiSelector.cshtml exists in Views/Shared with @model string, three TargetAiPlatform radios, conditional checked binding
- _BracketCallout.cshtml exists in Views/Shared with .bracket-callout wrapper
- dotnet build DeckFlow.Web clean (0 errors)
- site.css and all guild theme CSS files are untouched
</success_criteria>

<output>
After completion, create .planning/phases/09-bracket-ux-ai-selector-foundation/09-01-SUMMARY.md
</output>
