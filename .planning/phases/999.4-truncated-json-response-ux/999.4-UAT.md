---
phase: 999.4-truncated-json-response-ux
uat_date: 2026-05-21
pass: true
pass_attestation: "user-attested 2026-05-21; signal: 'uat done' + 'approved'"
extracted_from: 999.4-01-PLAN.md Task 6 <how-to-verify>
---

## Pre-Flight

User starts dev server (do NOT auto-launch per CLAUDE.md):

```bash
dotnet run --project DeckFlow.Web
```

Five commits already landed (T1-T5):

```text
153b4bc feat(999.4): catch truncated JSON in ResponseParsers (analysis + set-upgrade)
b6841c4 feat(999.4): catch truncated JSON in DeckComparisonService.ParseComparisonResponse
d5c8ecb feat(999.4): catch truncated JSON in MetaGapService.ParseResponse
22dc7b8 feat(999.4): map truncated-paste message to corrupted-zip on upload paths
b556724 test(999.4): assert truncated response throws locked message across 4 parse sites
```

## Locked Messages (verify byte-identical to UTF-8 source)

**Paste-path (em-dash U+2014, NOT hyphen):**

> The pasted response appears truncated — wait for the AI to finish generating before copying, then re-submit.

**Upload-path (ASCII-only, no em-dash):**

> The uploaded zip contains an incomplete response payload. Re-export from the originating session or paste a fresh response.

## Grep Gate (run before live UAT)

```text
$ grep -c "catch (JsonException)" DeckFlow.Web/Services/ResponseParsers.cs
2

$ grep -c "catch (JsonException)" DeckFlow.Web/Services/DeckComparisonService.cs
1

$ grep -c "catch (JsonException)" DeckFlow.Web/Services/MetaGapService.cs
2

$ grep -cF "internal const string TruncatedResponseMessage" DeckFlow.Web/Services/ResponseParsers.cs
1

$ grep -c "ResponseParsers.TruncatedResponseMessage" DeckFlow.Web/Controllers/DeckController.cs
3

$ grep -c "CorruptedZipMessage" DeckFlow.Web/Controllers/DeckController.cs
4

$ grep -c "exception.Message == ResponseParsers.TruncatedResponseMessage" DeckFlow.Web/Controllers/DeckController.cs
3

$ grep -rh "Assert.Equal(ResponseParsers.TruncatedResponseMessage" DeckFlow.Web.Tests/ResponseParsersTests.cs DeckFlow.Web.Tests/DeckComparisonServiceTests.cs DeckFlow.Web.Tests/MetaGapServiceTests.cs | wc -l
4
```

All 9 gates pass as of 153b4bc..b556724.

## Paste-Path Workflows (4 pages)

### W1 — `/deck-analysis` Step 3 (DeckProfileJson)

- **AI selection:** ChatGPT
- **Step 1:** any deck-source URL or paste (real deck).
- **Step 2:** select ChatGPT.
- **Step 3:** paste truncated fixture into `DeckProfileJson` textarea, Submit:

  ```text
  {"deck_profile":{"format":"commander","commander":"Plagon
  ```

- **Expect:** locked paste-path message inline. NO `JsonReaderException` stack trace. `DeckProfileJson` textarea still contains truncated paste (`Request = request` preservation).
- **Pass/Fail:** ___

### W2 — `/deck-analysis` Step 5 (SetUpgradeResponseJson)

- **Pre-req:** complete real Step 3 first (paste a valid deck_profile JSON), then proceed to Step 5.
- **Step 5:** paste truncated fixture, Submit:

  ```text
  {"set_upgrade_report":{"sets":[{"set_code":"duskmourn","set_name":"Dusk
  ```

- **Expect:** same locked message; no stack trace; `SetUpgradeResponseJson` textarea survives.
- **Pass/Fail:** ___

### W3 — `/deck-comparison` Step 3 (ComparisonResponseJson)

- **AI selection:** Claude (switch platforms for D-02 universal-noun spot-check).
- **Step 1:** deck A + deck B sources.
- **Step 2:** Claude.
- **Step 3:** paste truncated fixture, Submit:

  ```text
  {"deck_comparison":{"deck_a_name":"Atraxa Blink","deck_b_name":"Partn
  ```

- **Expect:** locked message identical to W1/W2 (proves message text is `TargetAiPlatform`-invariant); textarea survives.
- **Pass/Fail:** ___

### W4 — `/cedh-meta-gap` Step 3 (MetaGapResponseJson)

- **Step 1:** commander.
- **Step 2:** ChatGPT or Claude.
- **Step 3:** paste truncated fixture, Submit:

  ```text
  {"meta_gap":{"commander":"Tymna / Kraum","meta_summary":"Play more stack interac
  ```

- **Expect:** locked message; no stack trace; `MetaGapResponseJson` textarea survives.
- **Pass/Fail:** ___

### W5 — AI-platform coverage

At least 2 of 3 AI selections (ChatGPT / Claude / Gemini) exercised across W1-W4. Confirm rendered message bytes match across selections.

- ChatGPT: Y / N
- Claude: Y / N
- Gemini: Y / N (requires `DECKFLOW_GEMINI_ENABLED=true`; skip if env-var not set)

Record env-var status in SUMMARY.md.

## Upload-Path Workflows (D-06 — 3 pages)

For each: download a real session zip, open archive, truncate the named JSON entry mid-object, re-zip, upload via the page's "Upload" / "Import" affordance.

### W6a — `/deck-analysis/upload` corrupted-zip

- **Zip entry to truncate:** `40-deck-profile.json` (Step 3 packet) or `51-set-upgrade-response.json` (Step 5 packet).
- **Expect:** corrupted-zip message (NOT paste-oriented copy); no stack trace; form fields reset to empty (by design — `Request = new DeckAnalysisRequest()`).
- **Pass/Fail/Walkthrough-only:** ___

### W6b — `/deck-comparison/upload` corrupted-zip

- **Zip entry to truncate:** `40-deck-comparison-response.json`.
- **Expect:** corrupted-zip message; no stack trace; form fields reset.
- **Pass/Fail/Walkthrough-only:** ___

### W6c — `/cedh-meta-gap/upload` corrupted-zip

- **Zip entry to truncate:** `40-meta-gap-response.json`.
- **Expect:** corrupted-zip message; no stack trace; form fields reset.
- **Pass/Fail/Walkthrough-only:** ___

**Walkthrough-only fallback (PLAN.md step 6d):** if zip surgery is impractical in WSL, open the three catch blocks in `DeckController.cs` (~lines 631 / 912 / 1163), confirm by inspection that `exception.Message == ResponseParsers.TruncatedResponseMessage ? CorruptedZipMessage : exception.Message` substitution is wired. Record walkthrough disposition in SUMMARY.md.

## D-03 PacketArtifactStore Spot-Check

Open `DeckFlow.Web/Services/PacketArtifactStore.cs:518-540`. Inspect existing `catch (JsonException) { return Array.Empty<EdhTop16Entry>(); }`.

- Acceptable as-is (closed, no follow-up): ___
- Follow-up needed (capture in SUMMARY.md `Deferred audit disposition` section, do NOT expand 999.4 scope): ___

## README Sweep

```bash
grep -i "truncated\|parse fail" README.md
```

Pre-execution baseline: exit 1 (no hits). Expect same. If hits exist, add one-line note describing inline error UX.

- Edit needed: Y / N
- If Y, location: ___

## Closure (Commit 6 of 6)

1. Author `.planning/phases/999.4-truncated-json-response-ux/999.4-01-SUMMARY.md` matching `999.3-04-SUMMARY.md` shape.
2. Update `.planning/ROADMAP.md` Phase 999.4 row: `1/1` plans, `Complete`, completion date `2026-05-21`.
3. Commit:

   ```text
   docs(999.4): UAT closure + SUMMARY + ROADMAP update
   ```

   Plain default-author, no `Co-Authored-By` trailer. Body records UAT findings (W1-W6 dispositions, AI-platform coverage, D-03 disposition, D-07 deferral confirmation, WSL flake observation, README disposition).

## Sign-Off

User attests pass by replying `approved` to orchestrator after commit 6 lands.

- UAT pass attestation: approved (2026-05-21)
- Commit 6 SHA: filled post-commit (see ROADMAP Phase 999.4 Progress row + SUMMARY.md commit hash block)
