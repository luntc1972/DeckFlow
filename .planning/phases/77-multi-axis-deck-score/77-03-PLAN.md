---
phase: 77-multi-axis-deck-score
plan: 03
type: execute
wave: 0
depends_on: []
files_modified:
  - DeckFlow.Web/Services/PromptBuilders/Analysis/IAnalysisPromptVariant.cs
  - DeckFlow.Web/Services/PromptBuilders/Analysis/AnalysisPromptVariantRegistry.cs
  - DeckFlow.Web/Services/PromptBuilders/Analysis/ChatGptAnalysisPromptVariant.cs
  - DeckFlow.Web/Services/PromptBuilders/Analysis/ClaudeAnalysisPromptVariant.cs
  - DeckFlow.Web/Services/PromptBuilders/Analysis/GeminiAnalysisPromptVariant.cs
  - DeckFlow.Web.Tests/AnalysisScorePromptParityTests.cs
autonomous: true
requirements: [SCORE-04]
must_haves:
  truths:
    - "IAnalysisPromptVariant.Build and the registry accept a trailing optional string? scoreBlockText = null"
    - "All three variants (ChatGpt/Claude/Gemini) emit the scoreBlockText block when it is non-empty"
    - "All three variants produce byte-identical output to today when scoreBlockText is null/empty"
    - "A parity test asserts the score block appears in all three variants and the same band figures match across all three"
  artifacts:
    - path: "DeckFlow.Web/Services/PromptBuilders/Analysis/IAnalysisPromptVariant.cs"
      provides: "scoreBlockText param on the Build contract"
      contains: "scoreBlockText"
    - path: "DeckFlow.Web.Tests/AnalysisScorePromptParityTests.cs"
      provides: "3-platform present/absent/figures-match parity tests (ADR-0001)"
      contains: "DECK SCORE"
  key_links:
    - from: "AnalysisPromptVariantRegistry.Build"
      to: "each variant.Build"
      via: "scoreBlockText passthrough as trailing arg"
      pattern: "scoreBlockText"
---

<objective>
Thread a pre-built `string? scoreBlockText` argument through the analysis prompt contract and
have each of the three platform variants independently insert it (ADR-0001: no shared helper).
Guard completeness + flag-OFF byte-identity with a parity test modelled on
`BracketPromptVariantParityTests`.

Purpose: SCORE-04 — the score block must appear in all three paste artifacts, hand-edited per
variant, with a parity test; null path stays byte-identical.
Output: contract + registry + three variants edited, plus a new parity test (GREEN).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/77-multi-axis-deck-score/77-PATTERNS.md
@.planning/phases/77-multi-axis-deck-score/UI-SPEC.md
@docs/decisions/0001-prompt-variants-decoupled.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add scoreBlockText param to contract, registry, and all three variants</name>
  <read_first>
    - DeckFlow.Web/Services/PromptBuilders/Analysis/IAnalysisPromptVariant.cs (existing `string? companionName = null` trailing optional param at line 37 — the exact analog)
    - DeckFlow.Web/Services/PromptBuilders/Analysis/AnalysisPromptVariantRegistry.cs (Build signature lines 30-41 + passthrough line 46 — add scoreBlockText as the trailing arg)
    - DeckFlow.Web/Services/PromptBuilders/Analysis/ChatGptAnalysisPromptVariant.cs (companionName guard block lines 65-69; insertion region after `## DECK CONTEXT`, before `## EVIDENCE RULES`)
    - DeckFlow.Web/Services/PromptBuilders/Analysis/ClaudeAnalysisPromptVariant.cs (companionName param + `<task>`/`<commander>` block — insertion after the commander/context metadata)
    - DeckFlow.Web/Services/PromptBuilders/Analysis/GeminiAnalysisPromptVariant.cs (same markdown structure as ChatGPT)
    - 77-PATTERNS.md variant sections (companionName insertion guard pattern to copy) + docs/decisions/0001 (ADR-0001: each variant hand-edited independently, NO shared helper)
  </read_first>
  <action>
    Add `string? scoreBlockText = null` as the new LAST optional param to IAnalysisPromptVariant.Build (with a
    `<param name="scoreBlockText">` XML doc mirroring the companionName doc), to AnalysisPromptVariantRegistry.Build,
    and pass it through on the variant dispatch line. In EACH of the three variant Build() methods, add the same
    trailing `string? scoreBlockText = null` param and a guard block that appends the block when present:
    `if (!string.IsNullOrWhiteSpace(scoreBlockText)) { builder.AppendLine(); builder.AppendLine(scoreBlockText); }`.
    Place the insertion at each variant's own chosen position per 77-PATTERNS (ChatGPT/Gemini: after `## DECK
    CONTEXT`, before `## EVIDENCE RULES`; Claude: after the commander/context metadata, in its prose style).
    Per ADR-0001 hand-edit each variant separately — do NOT extract a shared helper and do NOT build the block text
    here (the caller supplies the pre-built string). Touch only changed lines.
  </action>
  <verify>
    <automated>dotnet.exe build DeckFlow.Web 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "scoreBlockText" DeckFlow.Web/Services/PromptBuilders/Analysis/IAnalysisPromptVariant.cs` >= 1
    - All three variant files contain `if (!string.IsNullOrWhiteSpace(scoreBlockText))`
    - No shared score-text helper added across variants (ADR-0001) — each variant owns its insertion
    - `dotnet.exe build DeckFlow.Web` reports 0 warnings 0 errors
  </acceptance_criteria>
  <done>Contract + registry + three variants accept and emit scoreBlockText; Web builds clean.</done>
</task>

<task type="auto">
  <name>Task 2: AnalysisScorePromptParityTests — present / absent / figures-match across three platforms</name>
  <read_first>
    - DeckFlow.Web.Tests/Bracket/BracketPromptVariantParityTests.cs (EXACT template: BuildRegistry() inline-instantiating each concrete variant — no shared helper; AiPlatform.Normalize; [Theory]/[InlineData] three-platform pattern; minimal request/decklist/reference/schema fixture builders)
    - 77-PATTERNS.md "AnalysisScorePromptParityTests.cs (NEW)" section (score parity test bodies)
    - DeckFlow.Web/Services/PromptBuilders/Analysis/AnalysisPromptVariantRegistry.cs (Build arg order to call with scoreBlockText)
  </read_first>
  <behavior>
    - Score_Block_AppearsInAllThreeVariants("ChatGPT"|"Claude"|"Gemini"): Build with a non-null scoreBlockText
      containing "DECK SCORE" => Assert.Contains("DECK SCORE", result, Ordinal)
    - Score_NullPath_ByteIdenticalToExcisedScorePath(platform): build twice — once with the fixed scoreBlockText
      `x`, once with `scoreBlockText: null`. Assert the null-path output EQUALS the with-score output after the
      contiguous `x` block (and only that block) is excised — i.e. the variant adds nothing else and leaks no
      stray blank lines/whitespace. This proves OFF-path BYTE identity, not mere marker absence (Codex HIGH).
    - Score_AllFourAxisFigures_MatchAcrossAllThreeVariants: a scoreBlockText carrying all four axes
      (e.g. `Power: 4/5`, `Speed: 3/5`, `Control: 2/5`, `Consistency: 5/5`) => assert EVERY one of the four
      `Axis: N/5` pairs is present (Ordinal) in each of the three variants' output — not just a single token.
  </behavior>
  <action>
    Create DeckFlow.Web.Tests/AnalysisScorePromptParityTests.cs in namespace `DeckFlow.Web.Tests` with the
    ADR-0001 "Why:" header comment. Replicate BracketPromptVariantParityTests' BuildRegistry() (inline `new
    ChatGptAnalysisPromptVariant()/ClaudeAnalysisPromptVariant()/GeminiAnalysisPromptVariant()`), the minimal
    request/decklist/reference/schema fixture builders, and the AiPlatform.Normalize call. Add the three
    `[Theory]/[InlineData("ChatGPT")/("Claude")/("Gemini")]` tests from the behavior list, passing a fixed
    `scoreBlockText` string (e.g. "DECK SCORE (coarse 0-5 bands - magnitude, not quality)\n  Power: 4/5 High")
    as the trailing arg. Use `StringComparison.Ordinal` on all assertions.
  </action>
  <verify>
    <automated>dotnet.exe test DeckFlow.Web.Tests --filter "FullyQualifiedName~AnalysisScorePromptParity" 2>&1 | tail -6</automated>
  </verify>
  <acceptance_criteria>
    - AnalysisScorePromptParityTests.cs asserts all three variants contain the score block (present test)
    - AnalysisScorePromptParityTests.cs contains an OFF-path excision-equality test proving the null-path output equals the with-score output minus the contiguous score block (byte identity, per variant)
    - AnalysisScorePromptParityTests.cs contains a figures-match test asserting all four `Axis: N/5` pairs appear in every variant
    - `dotnet.exe test DeckFlow.Web.Tests --filter "FullyQualifiedName~AnalysisScorePromptParity"` exits 0
  </acceptance_criteria>
  <done>Parity test GREEN: score block present in all three variants when supplied, absent (byte-identical) when null.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| (none new) | Variants append a caller-supplied plaintext string into a prompt artifact; no new external input crosses here (the score text is built server-side in plan 77-04) |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-77-03-01 | Tampering | scoreBlockText emission | accept | Variants emit only the pre-built server-side string; null/empty path is byte-identical (parity test guards this) |
| T-77-SC | Tampering | package installs | mitigate | No new NuGet packages; in-solution code only |
</threat_model>

<verification>
- `dotnet.exe build DeckFlow.Web` clean (0/0)
- `dotnet.exe test DeckFlow.Web.Tests --filter "FullyQualifiedName~AnalysisScorePromptParity"` GREEN
</verification>

<success_criteria>
All three analysis prompt variants accept and independently insert scoreBlockText (ADR-0001, no shared
helper); parity test proves presence + figure consistency + null-path byte-identity.
</success_criteria>

<output>
Create `.planning/phases/77-multi-axis-deck-score/77-03-SUMMARY.md` when done
</output>
