---
phase: 91-reconcile-seed-lifecycle
plan: 06
type: execute
wave: 4
depends_on: [91-02, 91-04, 91-05]
files_modified:
  - DeckFlow.Studio/Services/IContentKbReconcileOrchestrator.cs
  - DeckFlow.Studio/Services/ContentKbReconcileOrchestrator.cs
  - DeckFlow.Studio/Program.cs
  - DeckFlow.Studio.Tests/Services/ContentKbReconcileOrchestratorTests.cs
autonomous: true
requirements: [SYNC-11]

must_haves:
  truths:
    - "A dry-run reads prod once, enumerates the git content-kb tree, parses the seed, runs the pure classifier, and persists to the local store"
    - "The file-orphan class walks repoRoot/content-kb (the git tree), not the Studio local artifact root"
    - "Every artifact path (row->file and file->row) is validated via ArtifactPathSafety before File access"
    - "A human-readable D-06 report file is emitted alongside the store persistence"
  artifacts:
    - path: "DeckFlow.Studio/Services/ContentKbReconcileOrchestrator.cs"
      provides: "I/O orchestrator (mirrors GitBodyCoverageAudit) feeding the pure classifier + store + report"
      contains: "ContentKbReconcileOrchestrator"
      min_lines: 80
  key_links:
    - from: "ContentKbReconcileOrchestrator"
      to: "ContentKbReconcileClassifier.Classify"
      via: "collections built by orchestrator I/O"
      pattern: "ContentKbReconcileClassifier"
    - from: "ContentKbReconcileOrchestrator"
      to: "IContentKbReconcileStore.PersistRunAsync"
      via: "scope-tagged persistence"
      pattern: "PersistRunAsync"
---

<objective>
Ship the Studio I/O orchestrator that composes the reconcile dry-run: read prod ONCE via the extended
`ProdContentReader`, enumerate the git `content-kb/**/*.md` tree (the genuinely-new file->row capability,
Pitfall 6), parse `index-seed.json` via `SeedIndexFileReader`, feed all three collections to the pure
`ContentKbReconcileClassifier`, persist scope-tagged results to the local store, and write the D-06
human-readable report. Mirrors `GitBodyCoverageAudit`'s shape and reuses every existing I/O seam.

Purpose: Turn the pure classifier into a runnable dry-run that reads all three inputs safely and
records results - the always-available (flag-independent) detection surface.
Output: `IContentKbReconcileOrchestrator` + `ContentKbReconcileOrchestrator` + report writer + registration.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/91-reconcile-seed-lifecycle/91-CONTEXT.md
@.planning/phases/91-reconcile-seed-lifecycle/91-RESEARCH.md
@.planning/phases/91-reconcile-seed-lifecycle/91-PATTERNS.md
@.planning/phases/91-reconcile-seed-lifecycle/91-02-SUMMARY.md
@.planning/phases/91-reconcile-seed-lifecycle/91-04-SUMMARY.md
@.planning/phases/91-reconcile-seed-lifecycle/91-05-SUMMARY.md

<interfaces>
<!-- From 91-02/91-04/91-05 (dependencies). Executor USES these. -->
IProdContentReader.ReadAllAsync(connStr, ct) : IReadOnlyList<ContentSiteIndexRow>   // now includes BodySha256/SeedManaged
ContentKbReconcileClassifier.Classify(prodRows, existingGitBodyRelPaths, seedNaturalKeys, gitBodyByRelPath, logger)
SeedIndexFileReader.ReadNaturalKeys(seedFilePath) : IReadOnlySet<string>
IContentKbReconcileStore.PersistRunAsync(scopeTag, seen, now, ct)
ArtifactPathSafety.TryBuildContainedPath(root, artifactPath, out resolvedPath) : bool   // + IsSafeArtifactPath prefix guard
IGitRepository.ResolveRepoRootAsync(...) / StudioRepoLocator.ResolveStartDirectory()   // repoRoot resolution (see DirectPushCoordinator.CommitAndPushBodiesAsync)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ContentKbReconcileOrchestrator - prod read + git enum + seed parse -> classifier -> store</name>
  <files>DeckFlow.Studio/Services/IContentKbReconcileOrchestrator.cs, DeckFlow.Studio/Services/ContentKbReconcileOrchestrator.cs, DeckFlow.Studio/Program.cs, DeckFlow.Studio.Tests/Services/ContentKbReconcileOrchestratorTests.cs</files>
  <read_first>
    - DeckFlow.Studio/Services/GitBodyCoverageAudit.cs (full file - constructor takes IProdContentReader; read-prod-once loop ~25-65; ArtifactPathSafety-guarded File.Exists)
    - DeckFlow.Studio/Services/ArtifactPathSafety.cs (TryBuildContainedPath ~22-42, IsSafeArtifactPath prefix/`..` guard ~64)
    - DeckFlow.Studio/ViewModels/DirectPushCoordinator.cs (repoRoot resolution via IGitRepository.ResolveRepoRootAsync + StudioRepoLocator ~410)
    - DeckFlow.Studio.Tests/Services/GitBodyCoverageAuditTests.cs (orchestrator test template with a fake IProdContentReader + temp git tree)
    - 91-RESEARCH.md Pitfall 6 (walk repoRoot/content-kb, NOT the local artifact root; expected ~328 order of magnitude), Open Question 2 (build ONE existing-paths HashSet, reuse for published-orphan AND file-orphan); 91-PATTERNS.md "ContentKbReconcileOrchestrator.cs"
  </read_first>
  <behavior>
    - RunDryRunAsync reads prod exactly once, enumerates repoRoot/content-kb for *.md files, and derives their content-kb/-relative paths (excludes seed/index-seed.json - not a .md).
    - It builds one existingGitBodyRelPaths set (reused for both published-orphan and file-orphan) and a gitBodyByRelPath map (bodies read for hash-mismatch), each path passed through ArtifactPathSafety.
    - It calls the pure classifier with (prodRows, existingGitBodyRelPaths, seedNaturalKeys, gitBodyByRelPath), persists results via PersistRunAsync with the run's scope tag, and returns the discrepancy list.
    - The orchestrator itself does NO destructive write and NO prod DDL.
    - Given a fixture prod-row set + temp git tree + seed, the returned classes match the pure classifier's output.
  </behavior>
  <action>
    Create `ContentKbReconcileOrchestrator` mirroring GitBodyCoverageAudit: constructor takes
    IProdContentReader, IContentKbReconcileStore, IGitRepository, ILogger. `RunDryRunAsync(scopeTag,
    ct)` resolves repoRoot (IGitRepository.ResolveRepoRootAsync + StudioRepoLocator.ResolveStartDirectory,
    same as DirectPushCoordinator), reads prod once, enumerates
    `Directory.EnumerateFiles(Path.Combine(repoRoot,"content-kb"),"*.md",SearchOption.AllDirectories)`,
    converts each to a content-kb/-relative path validated by ArtifactPathSafety.IsSafeArtifactPath,
    builds the existing-paths HashSet + the body-text map (reading each body once), parses seed keys via
    SeedIndexFileReader.ReadNaturalKeys(seedPath), calls ContentKbReconcileClassifier.Classify, then
    PersistRunAsync(scopeTag, discrepancies, DateTimeOffset.UtcNow). Read prod ONCE per run (Security
    Domain), never per-row. Register in Program.cs mirroring the sibling service registrations. Add
    ContentKbReconcileOrchestratorTests using a fake IProdContentReader + a temp content-kb tree fixture,
    asserting each class is detected and that prod is read exactly once (spy).
  </action>
  <acceptance_criteria>
    - Test: a temp git tree + fake prod rows produce the expected published-orphan / file-orphan / seed-drift / body-hash-mismatch discrepancies persisted to the store.
    - Test asserts ReadAllAsync is invoked exactly once per RunDryRunAsync.
    - Test asserts seed/index-seed.json is excluded from the file-orphan enumeration.
    - Orchestrator issues no DDL and no visibility write; build clean.
  </acceptance_criteria>
  <verify>
    <automated>dotnet.exe test DeckFlow.Studio.Tests/DeckFlow.Studio.Tests.csproj --no-restore --filter "FullyQualifiedName~ReconcileOrchestrator"</automated>
  </verify>
  <done>The dry-run orchestrator reads prod once, walks the git content-kb tree safely, parses the seed, drives the pure classifier, and persists scope-tagged results with no destructive action; tests green.</done>
</task>

<task type="auto">
  <name>Task 2: D-06 human-readable report writer</name>
  <files>DeckFlow.Studio/Services/ContentKbReconcileOrchestrator.cs, DeckFlow.Studio.Tests/Services/ContentKbReconcileOrchestratorTests.cs</files>
  <read_first>
    - DeckFlow.Core/Reporting/ReconciliationReporter.cs (mine ONLY the StringBuilder sectioned-report shape - this is deck-comparison domain, NOT reusable types, per RESEARCH Anti-Patterns)
    - 91-RESEARCH.md Anti-Patterns (ReconciliationReporter is not directly reusable); 91-CONTEXT.md D-06
  </read_first>
  <behavior>
    - After a dry-run, a git-tracked human-readable report file is written under the repo (e.g. content-kb/reconcile-report.md) with one section per discrepancy class and counts.
    - The report is a layered VIEW; the local store (91-05) remains the source of truth (D-06).
  </behavior>
  <action>
    Add a report writer to the orchestrator that renders the persisted discrepancies to a sectioned
    Markdown/text report (StringBuilder + AppendSection helpers, mining ReconciliationReporter's SHAPE
    only - no ReconciliationReporter/DeckDiff types). Write it to a stable, git-tracked path under the
    repo checkout (document the chosen path in the summary). The store persistence remains authoritative;
    the report never replaces it. Test asserts the report contains a section + count for each class from
    a fixture run.
  </action>
  <acceptance_criteria>
    - Test asserts the report text contains a labeled section and count for each of the 4 classes.
    - The report path is under the git checkout (git-tracked), documented in the SUMMARY.
    - Build clean.
  </acceptance_criteria>
  <verify>
    <automated>dotnet.exe test DeckFlow.Studio.Tests/DeckFlow.Studio.Tests.csproj --no-restore --filter "FullyQualifiedName~ReconcileOrchestrator"</automated>
  </verify>
  <done>A git-tracked human-readable D-06 report is emitted per dry-run alongside the authoritative local store; test green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| prod DB -> orchestrator (read) | Single read-only SELECT via ProdContentReader; no DDL, no write |
| git filesystem -> orchestrator | Enumerated/opened .md files; path traversal risk if paths trusted blindly |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-91-14 | Tampering / Elevation of Privilege | Path traversal via a crafted ArtifactPath (row->file) or enumerated file (file->row) | mitigate | ArtifactPathSafety.TryBuildContainedPath / IsSafeArtifactPath on BOTH directions before any File access (V5) |
| T-91-15 | Denial of Service | Unbounded/per-row prod queries on dry-run | mitigate | Read prod ONCE per run (spy test asserts single read), mirroring GitBodyCoverageAudit |
| T-91-16 | Information Disclosure | Prod conn-string leaking via orchestrator logs | mitigate | Preserve ProdContentReader catch discipline; no new logging path surfaces the conn string |
</threat_model>

<verification>
- `dotnet build DeckFlow.sln` clean.
- ReconcileOrchestrator tests green (four classes + single-prod-read + seed-exclusion + report sections).
</verification>

<success_criteria>
The dry-run is fully runnable: it safely reads all three inputs (prod once, git tree walk, seed parse),
classifies via pure Core, persists scope-tagged to the local store, and emits a human-readable report -
all read-only, flag-independent, with path-traversal guards on both file directions.
</success_criteria>

<output>
Create `.planning/phases/91-reconcile-seed-lifecycle/91-06-SUMMARY.md` when done.
</output>
