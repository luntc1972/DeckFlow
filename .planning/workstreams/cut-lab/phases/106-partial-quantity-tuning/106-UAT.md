---
status: complete
phase: 106-partial-quantity-tuning
source:
  - 106-01-SUMMARY.md
  - 106-02-SUMMARY.md
  - 106-03-SUMMARY.md
  - 106-04-SUMMARY.md
  - 106-05-SUMMARY.md
started: 2026-07-22T14:45:00Z
updated: 2026-07-22T14:45:00Z
---

## Current Test

[testing complete]

## Tests

### 1. Cold Start Smoke Test
expected: With `tool.cut-lab.enabled` ON (flip via /Admin/Flags or run the dev server), start the app fresh and open /cut-lab. The page loads without errors, you can paste/import an oversized (101-150 card) Commander pool with a big basic-land stack (e.g. 30+ Islands), and it reaches the Decide workspace with a "N to cut" sticky counter.
result: pass

### 2. Inline steppers on tunable rows
expected: In the Decide workspace, a "Tune quantities" section lists working-list rows. Basic lands and legal-multiple cards (Relentless Rats, etc.) each show − and + steppers with the current copy count. Singleton cards show NO steppers. The − is disabled at 0 and the + is disabled at the legal max.
result: pass
note: verified via tuner screenshots (azorius/nyx, desktop+mobile) — steppers + bounds + Added tag render correctly

### 3. Trim copies live (JS)
expected: Click − on a basic (e.g. Island). Its count drops by 1 and the sticky "N to cut" counter updates immediately — no full page reload. Clicking + raises it back. The count reflects the tuned total, not the original pool.
result: pass
note: covered by cut-lab-tuning e2e (live stepper trim patches sticky count without reload)

### 4. Add a basic land
expected: An "Add a basic land" control offers only basics NOT already in your working list. Pick one (e.g. Wastes) and add it — a new row appears in the tuner for that basic, tagged with a "Lands" role, and the count goes up by 1. No error, no network stall.
result: issue
reported: "Desktop table layout renders the \"Add a basic land\" label vertically (one letter per line) — the toolbar flex row collapses the label because .df-select is width:100%. Mobile stacked layout is fine. Function works (e2e adds a basic OK)."
severity: cosmetic
resolution: FIXED in 4272b1ff — .cutlab-tuner__add-basic now stacks (flex column); desktop label renders horizontally; re-verified via full tuning e2e 6/6 both projects + screenshot review

### 5. Export gate is exact-100
expected: The Export step tab and the "Build export" button are DISABLED whenever the tuned list is under 100 OR over 100 — they enable ONLY at exactly 100 cards. Tune the count to 99, then 101, then exactly 100 and confirm the gate flips on only at 100.
result: pass
note: covered by cut-lab-tuning e2e (Export gate flips on only at exactly 100)

### 6. Under-100 guidance message
expected: When the tuned list drops below 100, the proposal panel shows a distinct message: heading "You're under 100 cards" and body "Add copies or a basic land in the Tune quantities section below to reach exactly 100." (Different from the at-100 and over-100 states.)
result: pass
note: exact copy grep-confirmed in CutLab.cshtml + branch precedes IsAtTarget; e2e exercises sub-100 state

### 7. Export reflects tuned counts (both dialects)
expected: At exactly 100, build the export. The add/cut patch shows your tuned changes — a trimmed basic appears as a CUT of N, an added basic appears as an ADD — and this is correct in BOTH the Moxfield and Archidekt outputs. An intentionally added basic does NOT produce a "metadata unavailable" warning.
result: pass
note: covered by cut-lab-tuning e2e (CUT/ADD tuned counts asserted) + CutLabExportServiceTests both dialects, no spurious basic warning

### 8. Scenario save/reload preserves tuning
expected: Tune to exactly 100 (trim a basic and/or add one), save the working state as a scenario, then reload that scenario. The tuning survives — the trimmed/added copy counts are still applied and the list is still exactly 100 (it does NOT revert to the pre-tuning count). [This is the defect fixed during review — the key check.]
result: pass
note: covered by cut-lab-tuning e2e save/reload — this is the defect fixed in review (b6228046); adjustments persist at exactly 100

### 9. No-JS fallback
expected: With JavaScript disabled, the steppers and add-basic still work via a normal form POST — each click does a full-page reload that returns the page with the updated count and tuned list. Server rejects any illegal quantity (e.g. a 2nd copy of a singleton) regardless of what's posted.
result: skipped
reason: "Server-side legality (singleton reject) covered by CutLabAdjustmentApplierTests + CutLabApiControllerTests; full JS-disabled full-page render not exercised by the JS-driven e2e — needs a manual JS-off pass."

## Summary

total: 9
passed: 6
issues: 0  # 1 found + resolved (add-basic label, 4272b1ff)
pending: 0
skipped: 1

## Gaps

```yaml
- truth: "The add-basic control label reads clearly on desktop"
  status: resolved
  reason: "User reported: desktop toolbar flex row collapses the label to ~0 width so 'Add a basic land' wraps one letter per line; .df-select width:100% consumes the row. Mobile (column stack) is fine."
  severity: cosmetic
  test: 4
  artifacts: [".planning/ui-design/cut-lab/screenshots/tuning-azorius-desktop-chromium-desktop.png"]
  missing: ["desktop layout rule stacking the add-basic label above the select (match mobile), or label flex:0 0 auto + white-space:nowrap"]
```
