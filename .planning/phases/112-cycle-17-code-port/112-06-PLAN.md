---
phase: 112-cycle-17-code-port
plan: 06
type: execute
wave: 6
depends_on: ["112-05"]
files_modified:
  - DeckFlow.Web.Tests/Services/CreatorStyle/CreatorStyleDiRegistrationTests.cs
autonomous: true
requirements: [PORT-02]

must_haves:
  truths:
    - "A DI-resolution xUnit test builds the real service provider and resolves every creator-style interface, so a missing registration is caught permanently in CI rather than once by hand (D-13)."
    - "At least one assertion resolves the REAL ArchidektOwnerClient rather than FakeArchidektOwnerClient, so the missing-archidekt-pipeline failure mode cannot pass on paper while the real app throws (D-20)."
    - "Coverage is the extension's full registration set derived from the ServiceCollection descriptor delta, with the five scoped services resolved from inside an IServiceScope — the headless boot cannot substitute for this, because Phase 112 wires no creator-style route and startup touches only the seed-loader path (D-13)."
    - "The application starts locally with all creator-style services resolving through DI and no missing-registration exception — ROADMAP success criterion 3 (PORT-02)."
    - "scripts/format-check-changed.sh staged passes with the full Commit 2 file set staged, with any fix applied inside the same commit (D-04)."
    - "git diff --name-status main lists only Commit 1 and Commit 2 allowlist paths plus the declared M-files, and none of D-12's never-port strings arrived (D-08, D-12/D-18)."
    - "Commit 2 of D-07's two-commit structure exists on feat/personal-tools."
    - "dotnet build DeckFlow.sln has 0 errors and no new warnings versus the recorded baseline, and both full test suites introduce no new failure."
  artifacts:
    - path: "DeckFlow.Web.Tests/Services/CreatorStyle/CreatorStyleDiRegistrationTests.cs"
      provides: "Permanent CI guard that the real creator-style DI graph resolves"
      contains: "ArchidektOwnerClient"
  key_links:
    - from: "DeckFlow.Web.Tests/Services/CreatorStyle/CreatorStyleDiRegistrationTests.cs"
      to: "DeckFlow.Web/Extensions/CreatorStyleServiceCollectionExtensions.cs"
      via: "AddDeckFlowCreatorStyle on a real ServiceCollection"
      pattern: "AddDeckFlowCreatorStyle"
    - from: "DeckFlow.Web.Tests/Services/CreatorStyle/CreatorStyleDiRegistrationTests.cs"
      to: "DeckFlow.Web/Services/Http/ResiliencePipelineFactory.cs"
      via: "real ArchidektOwnerClient resolution exercising the archidekt pipeline key"
      pattern: "AddDeckFlowResiliencePipelines"
---

<objective>
Strengthen the DI-resolution test to resolve the whole creator-style graph — including the real `ArchidektOwnerClient` — prove the host actually boots, then gate and create Commit 2.

Purpose: RESEARCH Pitfall 3 is the sharpest risk left in this phase. The Cycle 17 branch's own `CreatorStyleDiRegistrationTests.cs` injects `FakeArchidektOwnerClient`, so it passes green whether or not the `archidekt` Polly pipeline is registered — proving the wiring topology but not that any real constructor succeeds. D-20 requires at least one assertion through the real implementation, because `ArchidektOwnerClient`'s constructor calls `GetPipeline` (not `TryGetPipeline`) and throws `KeyNotFoundException` at object-construction time on an unregistered key.

Output: Commit 2 on `feat/personal-tools`, format-gated, path-audited, with both full test suites green against the recorded baseline and a real headless host boot confirmed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/112-cycle-17-code-port/112-RESEARCH.md
@.planning/phases/112-cycle-17-code-port/112-CONTEXT.md
@.planning/phases/112-cycle-17-code-port/112-VALIDATION.md
@.planning/phases/112-cycle-17-code-port/112-BASELINE.md
@.planning/phases/112-cycle-17-code-port/112-02-PLAN.md
@.planning/phases/112-cycle-17-code-port/112-04-PLAN.md
@CLAUDE.md
</context>

<environment>
- Branch: `feat/personal-tools`, Commit 1 landed, all Commit 2 content staged-ready in the working tree. dotnet is `"/mnt/c/Program Files/dotnet/dotnet.exe"`. Do NOT set `MTG_DATA_DIR`.
- VSTest is unreliable under WSL — run `dotnet test` through the Windows dotnet above.
- UI/host testing must NEVER open a browser on the Windows host. Use `scripts/run-web-test.sh`, which exports `DECKFLOW_DISABLE_AUTO_BROWSER=true`.
- A stale Windows-side server may already hold port 5173 and WSL's `ss` cannot see it. Probe with `curl` (and Windows `netstat.exe` if needed) before starting, and use `FORCE_RESTART=1` if a stale process must be replaced.
- `.gitattributes` enforces LF. Commits use the plain default author, Conventional Commits format, NO `Co-Authored-By` trailer.
- No new `PackageReference` anywhere in this phase.
</environment>

<never_port_list>
## Must not appear anywhere in the Phase 112 branch diff (D-12, D-18)
- DeckFlow.Web/Services/FeatureFlags/FeatureFlagCatalog.cs
- DeckFlow.Web/Services/FeatureFlags/FeatureFlagStore.cs
- any ToolRegistry file
- DeckFlow.Web/Models/DeckPageTab.cs
- DeckFlow.Web/Help/creator-style.md
- any SeoPaths or sitemap file
- the string `tool.creator-style.enabled` anywhere in added content
`DeckFlow.Web/Services/PacketSessionCache.cs` IS expected in the diff — it is D-18's single ratified additive exception. Its presence is legal; a non-additive diff on it is not.
</never_port_list>

<tasks>

<task type="auto">
  <name>Task 1: Prove the whole creator-style DI graph resolves, including the real ArchidektOwnerClient (D-13, D-20)</name>
  <files>DeckFlow.Web.Tests/Services/CreatorStyle/CreatorStyleDiRegistrationTests.cs</files>
  <read_first>
    - `DeckFlow.Web.Tests/Services/CreatorStyle/CreatorStyleDiRegistrationTests.cs` as ported in plan 04 — read how it builds its `ServiceCollection`, what it stubs, and where `FakeArchidektOwnerClient` is injected. The existing tests stay; this task ADDS to them.
    - `.planning/phases/112-cycle-17-code-port/112-RESEARCH.md` §"Critical Findings" #2 (the Polly 8.6.6 `KeyNotFoundException` repro, and that the throw fires in `ArchidektOwnerClient`'s CONSTRUCTOR), §"Common Pitfalls" Pitfall 3, and §"Validation Architecture" → the PORT-02 DI row marked as a Wave 0 gap.
    - `.planning/phases/112-cycle-17-code-port/112-VALIDATION.md` §"Wave 0 Requirements" item 1.
    - `.planning/phases/112-cycle-17-code-port/112-CONTEXT.md` D-13, D-17, D-20.
    - `DeckFlow.Web/Extensions/CreatorStyleServiceCollectionExtensions.cs` and `DeckFlow.Web/Services/Http/ResiliencePipelineFactory.cs` — the two registration sites the test must exercise together.
    - `DeckFlow.Web/Services/Persistence/DeckFlowDatabaseConnectionFactory.cs:124-132` — `ResolveArtifactsPath`, so the temp-directory layout below is written against how the factory actually resolves paths rather than an assumption.
    - `DeckFlow.Web/Services/CreatorStyle/ArchidektOwnerClient.cs` — its constructor's exact dependencies (`ResiliencePipelineProvider<string>`, a `RestClient` built from the named `archidekt-owner` HttpClient, an optional logger), so the test composes the same graph the app composes.
    - An existing DI or integration test in `DeckFlow.Web.Tests` for the house pattern of building a provider in a test (naming, `IWebHostEnvironment` fake, disposal).
  </read_first>
  <action>
    Keep every existing test in the file. Add a companion test whose entire purpose is to prove the REAL object graph constructs.

    The new test must: build a fresh `ServiceCollection`; call `AddDeckFlowResiliencePipelines()` and `AddDeckFlowCreatorStyle(environment)` with a real (or minimally faked `IWebHostEnvironment`) environment plus whatever logging and HttpClientFactory registrations the real app supplies; build the provider; and resolve `IArchidektOwnerClient` — receiving the REAL `ArchidektOwnerClient`, NOT `FakeArchidektOwnerClient`. Assert the resolved instance is of type `ArchidektOwnerClient`. Then resolve `CreatorProfileDeckCrawler` from a scope, since it takes `IArchidektOwnerClient` directly and is the transitive path the running app takes.

    The test must NOT substitute a fake for `IArchidektOwnerClient`, must NOT pre-register the `archidekt` pipeline inline inside the test, and must NOT wrap the resolution in a try/catch that swallows `KeyNotFoundException`. Any of those would restore exactly the blind spot D-20 exists to close. It must make NO network call — resolution is construction-time only, and `ArchidektOwnerClient`'s constructor does not perform I/O.

    Name the test so its intent is unmistakable, for example `Resolve_RealArchidektOwnerClient_DoesNotThrow_WhenArchidektPipelineRegistered`, and add an inline `// Why:` comment recording that a faked variant cannot detect the missing-pipeline failure mode because the throw happens in the constructor.

    Add a second, negative-control assertion only if it can be done without registering a competing pipeline set: construct a provider that calls `AddDeckFlowCreatorStyle` WITHOUT `AddDeckFlowResiliencePipelines`, and assert that resolving `IArchidektOwnerClient` throws. This pins the failure mode so a future removal of the `archidekt` registration turns the test red instead of silently passing. If the surrounding test infrastructure makes this negative control impossible without contorting the design, skip it and say so in the plan summary — do not fake it.

    **Whole-graph coverage (D-13's actual bar).** D-13 requires resolving EVERY creator-style interface, not only the archidekt path. The archidekt assertions above close the one failure mode RESEARCH proved; they do not prove the other thirteen registrations construct. Neither does task 2's headless boot — Phase 112 wires no creator-style route, and startup touches only the seed-loader path, so a missing or mis-lifetimed registration for any scoped service would boot green and ship broken.

    Therefore add a third test that resolves the extension's ENTIRE registration set from one provider. Derive the set programmatically so the test cannot drift as the extension changes: build a `ServiceCollection`, snapshot its `Count`, call `AddDeckFlowCreatorStyle(environment)`, then iterate the descriptors added after the snapshot and resolve each one's `ServiceType` from a scope — skipping only open generics and `IHttpClientFactory` plumbing that `AddHttpClient` contributes. Assert every resolution returns non-null and throws nothing.

    Back the programmatic sweep with an explicit floor list, asserted by name, so a future refactor that accidentally empties the descriptor delta still fails loudly. The floor is the fourteen service types the extension registers: `ICardNameGrounder`, `ICardGroundingGuard`, `ICreatorDeckCacheStore`, `ICreatorProfileSourceStore`, `CategoryKnowledgeRepository`, `ICreatorStyleProfileStore`, `CreatorWhitelistPoolBuilder`, `ICreatorStyleSeedLoader`, `IArchidektOwnerClient`, `CreatorProfileDeckCrawler`, `CreatorDeckCategoryResolver`, `MeasuredStyleProfileBuilder`, `ISubmittedDeckStatsBuilder`, `ICreatorStylePacketService`. Resolve the five scoped ones (`CreatorProfileDeckCrawler`, `CreatorDeckCategoryResolver`, `MeasuredStyleProfileBuilder`, `ISubmittedDeckStatsBuilder`, `ICreatorStylePacketService`) from inside an `IServiceScope` — resolving a scoped service from the root provider either throws or silently gives it singleton lifetime, and both outcomes make the test lie.

    **Temp database isolation — read the factory before writing this.** The four store registrations open real SQLite connections through `DeckFlowDatabaseConnectionFactory`, and its path resolution is NOT `ContentRootPath`. `ResolveArtifactsPath` (`DeckFlow.Web/Services/Persistence/DeckFlowDatabaseConnectionFactory.cs:124-132`) returns `MTG_DATA_DIR` when that variable is set, and otherwise `Path.GetFullPath(Path.Combine(environment.ContentRootPath, "..", "artifacts"))` — the **parent's** `artifacts` sibling, not the content root itself. Pointing `ContentRootPath` at a bare temp directory would therefore scatter `content-kb.db` and friends into that directory's parent, leaking files and failing to isolate concurrent tests.

    Structure it as: create ONE per-test parent temp directory; set `ContentRootPath` to a child of it (e.g. `<parent>/app`), so the factory resolves artifacts to `<parent>/artifacts`; on dispose call `Microsoft.Data.Sqlite.SqliteConnection.ClearAllPools()` before deleting, because pooled handles keep the files locked on Windows and `Directory.Delete` will throw; then delete the PARENT directory recursively. Leave `MTG_DATA_DIR` unset — setting it is forbidden by project convention and would defeat the per-test isolation by pointing every test at one shared directory.

    Dispose any built `ServiceProvider` per the file's existing pattern so the test does not leak SQLite handles.

    Do not stage or commit; task 3 owns Commit 2.
  </action>
  <verify>
    <automated>"/mnt/c/Program Files/dotnet/dotnet.exe" test DeckFlow.Web.Tests/DeckFlow.Web.Tests.csproj --filter "FullyQualifiedName~CreatorStyleDiRegistration"</automated>
  </verify>
  <acceptance_criteria>
    - `"/mnt/c/Program Files/dotnet/dotnet.exe" test DeckFlow.Web.Tests/DeckFlow.Web.Tests.csproj --filter "FullyQualifiedName~CreatorStyleDiRegistration"` exits 0 with 0 failed tests and a total count strictly greater than the count before this task.
    - `CreatorStyleDiRegistrationTests.cs` contains at least one test that resolves `IArchidektOwnerClient` and asserts the concrete type is `ArchidektOwnerClient`.
    - In that test's body, `FakeArchidektOwnerClient` does not appear — verified by reading the method, not just the file.
    - The new test does not contain `catch (KeyNotFoundException` or any catch that swallows a resolution failure.
    - The new test resolves `CreatorProfileDeckCrawler` from a scope and does not throw.
    - Temporarily commenting out the `archidekt` registration line in `ResiliencePipelineFactory.cs` makes this test FAIL — confirm this once locally, then restore the line and re-run to green. Record both outcomes in the plan summary. This is the proof that the test actually guards the failure mode rather than merely asserting a type.
    - No network call is made by the test: `grep -Ec 'http://|https://' ` on the new test method returns 0 apart from any registration constant already required.
    - A whole-graph test resolves every service type `AddDeckFlowCreatorStyle()` registers, derived from the `ServiceCollection` descriptor delta rather than a hand-copied list, and asserts each resolution is non-null.
    - That test also names all fourteen floor types explicitly — `ICardNameGrounder`, `ICardGroundingGuard`, `ICreatorDeckCacheStore`, `ICreatorProfileSourceStore`, `CategoryKnowledgeRepository`, `ICreatorStyleProfileStore`, `CreatorWhitelistPoolBuilder`, `ICreatorStyleSeedLoader`, `IArchidektOwnerClient`, `CreatorProfileDeckCrawler`, `CreatorDeckCategoryResolver`, `MeasuredStyleProfileBuilder`, `ISubmittedDeckStatsBuilder`, `ICreatorStylePacketService` — so an empty or truncated descriptor delta cannot pass vacuously.
    - The five scoped services are resolved from inside an `IServiceScope`, not the root provider.
    - Temporarily commenting out any ONE `services.Add*` line in `CreatorStyleServiceCollectionExtensions.cs` makes the whole-graph test FAIL. Confirm this once with a single arbitrary registration (not the archidekt one, which the other test already covers), then restore and re-run to green. Record which line was removed in the plan summary.
    - The test creates a per-test PARENT temp directory, sets `ContentRootPath` to a child of it, and deletes the parent recursively on dispose — so the `<parent>/artifacts` directory the factory actually writes to is cleaned up too. `MTG_DATA_DIR` is not set anywhere in the test.
    - `SqliteConnection.ClearAllPools()` is called before the directory delete, so pooled handles do not lock the files and fail the cleanup on Windows.
    - After running the test twice in a row, no `content-kb.db`, `category-knowledge.db`, or `creator-deck-cache.db` file exists anywhere under the repo working tree or the system temp root — verify with `find` once and record the result.
  </acceptance_criteria>
  <done>The DI-resolution test exercises the real `ArchidektOwnerClient` AND resolves every service `AddDeckFlowCreatorStyle()` registers, with guard value demonstrated twice: removing the `archidekt` pipeline reddens the archidekt test, and removing any single registration reddens the whole-graph test.</done>
</task>

<task type="auto">
  <name>Task 2: Headless host boot smoke — prove the app starts with all creator-style services resolving</name>
  <files>no source files; produces evidence only</files>
  <read_first>
    - `scripts/run-web-test.sh` — read the whole script. It exports `DECKFLOW_DISABLE_AUTO_BROWSER=true` (the entire point), defaults `PORT` to 5173, reuses an existing server unless `FORCE_RESTART=1`, and runs `dotnet run --project DeckFlow.Web --urls http://localhost:5173`.
    - `.planning/phases/112-cycle-17-code-port/112-VALIDATION.md` §"Manual-Only Verifications" — this task automates that row.
    - `DeckFlow.Web/Program.cs` (as edited in plan 05) — the exact startup log line to assert on, "Content site-index schema ensured and seed load completed during startup."
    - `DeckFlow.Web/Program.cs` Serilog configuration — logs roll to `logs/web-.log` under the content root; that file is where the assertion evidence lives.
    - `CLAUDE.md` §Constraints — UI testing must NEVER open a browser on the Windows host.
  </read_first>
  <action>
    Before starting anything, check whether a stale server already holds port 5173. WSL's `ss` cannot see Windows-side listeners, so probe with `curl --silent --output /dev/null --write-out '%{http_code}' http://localhost:5173/`. If something responds, it is a stale process from an earlier session and must be replaced — run the launcher with `FORCE_RESTART=1`.

    Start the host in the background with `scripts/run-web-test.sh` (never `run-web.sh` or `run-web-uat.sh`, which may launch a Windows browser). Do NOT set `MTG_DATA_DIR`.

    Poll `curl` against `http://localhost:5173/` until it returns a 2xx or 3xx status, with a bounded timeout of about 120 seconds. If it never becomes healthy, capture the console output and the tail of `logs/web-*.log` and STOP with that evidence — a boot failure here is exactly the ROADMAP criterion-3 failure this task exists to detect.

    Once healthy, assert against the current `logs/web-*.log`: the startup line "Content site-index schema ensured and seed load completed during startup." is present, and there is no `KeyNotFoundException`, no "Unable to resolve service for type", and no `Fatal` level entry.

    Stop the server cleanly (kill the launcher process, and free port 5173 so no stale listener is left behind for the next session). Re-probe with `curl` to confirm the port is free.

    Record in the plan summary: the healthy HTTP status observed, the matched startup log line, and confirmation that the port was released. Do not commit any log file — `logs/` is build output.
  </action>
  <verify>
    <automated>curl --silent --output /dev/null --write-out '%{http_code}' http://localhost:5173/ | grep -Eq '^(000|[45][0-9][0-9])$'</automated>
  </verify>
  <acceptance_criteria>
    - During the run, `curl --silent --output /dev/null --write-out '%{http_code}' http://localhost:5173/` returned a 2xx or 3xx status at least once.
    - The current `logs/web-*.log` contains the literal line fragment `Content site-index schema ensured and seed load completed during startup.`
    - `grep -Ec 'KeyNotFoundException|Unable to resolve service for type|\[FTL\]|Fatal' logs/web-*.log` for the boot in question returns 0.
    - The server was launched via `scripts/run-web-test.sh`; `grep -c 'run-web.sh\|run-web-uat.sh'` in the executed command history returns 0 — no browser-launching script was used.
    - After teardown, `curl --silent --output /dev/null --write-out '%{http_code}' http://localhost:5173/` returns `000` or a connection failure — port 5173 is released.
    - `git status --porcelain` shows no `logs/` file staged or added.
  </acceptance_criteria>
  <done>The real host boots headlessly with every creator-style service resolving, the seed fork-join completes, and no port is left held.</done>
</task>

<task type="auto">
  <name>Task 3: Gate and create Commit 2 (format gate, path audit, solution build, full test suites)</name>
  <files>git index and one new commit on feat/personal-tools</files>
  <read_first>
    - `.planning/phases/112-cycle-17-code-port/112-BASELINE.md` — the recorded pre-port error count, warning count, warning-ID table, and pre-existing test failures. Everything in this task is compared against it.
    - `.planning/phases/112-cycle-17-code-port/112-04-PLAN.md` §port_allowlist — the 38 Commit 2 paths.
    - `.planning/phases/112-cycle-17-code-port/112-02-PLAN.md` §port_allowlist — the 64 Commit 1 paths, needed for the cumulative branch-wide audit.
    - This plan's `<never_port_list>`.
    - `scripts/format-check-changed.sh` — the invocation is `scripts/format-check-changed.sh staged`, so `git add` must come first.
    - `.planning/phases/112-cycle-17-code-port/112-CONTEXT.md` D-04, D-07, D-08, D-09, D-18.
    - `CLAUDE.md` §Commit Conventions and §"Do Not Modify Without Explicit Permission".
  </read_first>
  <commit_rule_note>
    Same waiver as plan 112-03 Task 2. `CLAUDE.md` §Authoring rules asks for developer testing before committing functional changes; this task commits without an interactive gate under the established GSD planned-phase pattern — AI commits to the feature branch, `/gsd:verify-work` runs UAT afterward, user reviews before pushing. Nothing reaches `main`, no push happens. Note that Task 2's headless boot smoke already provides real-host evidence before this commit. Convert to `type="checkpoint"` if the user wants the rule enforced literally.
  </commit_rule_note>
  <action>
    Stage exactly the Commit 2 file set: the 38 allowlisted paths from plan 04 (36 code plus 2 seed placeholders), the six Web M-files (`ScryfallDtos.cs`, `ScryfallCardResolver.cs`, `PacketSessionCache.cs`, `DeckFlowDatabaseConnectionFactory.cs`, `ResiliencePipelineFactory.cs`, `Program.cs`), and the one new file `DeckFlow.Web/Extensions/CreatorStyleServiceCollectionExtensions.cs`. Stage nothing under `DeckFlow.Core/`, `DeckFlow.Core.Tests/`, or `DeckFlow.CLI/` — Core is already committed and the CLI is Phase 115.

    Run the D-04 format gate on the staged set: `scripts/format-check-changed.sh staged`. Fix any violation and re-stage INSIDE this same commit — never as a follow-up commit. When fixing, refuse formatter overreach that would violate the five carve-outs: do not convert `{ get; init; }` to `{ get; }` (System.Text.Json silently skips get-only properties in .NET 9+ and this has broken `EdhTop16Client` before), do not inline an `[Attribute]` onto the property line, do not re-indent a C# raw-string literal, preserve switch expressions, preserve xmldoc single-space indent.

    Run the full solution build: `"/mnt/c/Program Files/dotnet/dotnet.exe" build DeckFlow.sln`. Errors must be 0 and the warning count must be at or below the baseline with no warning ID absent from the baseline table. A regression here is a ROADMAP criterion-2 failure — fix before committing.

    Run the targeted Web suite:
    `"/mnt/c/Program Files/dotnet/dotnet.exe" test DeckFlow.Web.Tests/DeckFlow.Web.Tests.csproj --filter "FullyQualifiedName~CreatorStyle|FullyQualifiedName~CardGrounding|FullyQualifiedName~ScryfallCardNameGrounder|FullyQualifiedName~ProgramStartup"`
    Then run BOTH full suites (`DeckFlow.Core.Tests` and `DeckFlow.Web.Tests`) and diff the failure list against the baseline's pre-existing-failures list. Zero NEW failures is the bar.

    Run the D-08 path audit, cumulative across both commits:
    (a) `git diff --name-only main..HEAD -- DeckFlow.Core DeckFlow.Core.Tests DeckFlow.Web DeckFlow.Web.Tests DeckFlow.CLI content-kb scripts` must list exactly 119 paths — 74 from Commit 1 plus 45 from Commit 2 (38 allowlisted, 6 Web M-files, 1 new extension) — and nothing else. Enumerate any path that is not on either published allowlist and STOP if one exists.
    (b) `git diff --name-only main..HEAD` piped to `grep -Ei 'FeatureFlagCatalog|FeatureFlagStore|ToolRegistry|DeckPageTab|SeoPaths|sitemap|Help/creator-style|CreatorStyleController|CreatorStyleViewModel|CreatorStyle\.cshtml|CreatorStyleCommandRunners'` must return zero lines.
    (c) `git diff main..HEAD` piped to `grep -c 'tool.creator-style.enabled'` must return 0.
    (d) `git diff main..HEAD -- '*.csproj'` piped to `grep -c 'PackageReference'` must return 0.
    (e) `git diff main..HEAD -- DeckFlow.Web/Services/PacketSessionCache.cs` piped to `grep -c '^-[^-]'` must return 0 — D-18's exception is additive-only.
    (f) `git diff --name-only main..HEAD -- DeckFlow.Web/Services/CardLookupService.cs DeckFlow.Web/Services/Scryfall/ScryfallReferenceResolver.cs DeckFlow.Web/Services/ManabaseAnalysisService.cs DeckFlow.Web/Extensions/HttpClientServiceCollectionExtensions.cs DeckFlow.Web/Extensions/PacketServiceCollectionExtensions.cs DeckFlow.Web/Extensions/ScryfallServiceCollectionExtensions.cs` must return zero lines — the D-02/D-16 and D-10 hands-off files are untouched.
    Build and tests passing is NOT sufficient on its own: an older copy of a file main has since improved would compile green while silently reverting Cycle 18/19 work. The audit is the gate that catches that.

    Commit with a Conventional Commits message, plain default author, no `Co-Authored-By` trailer. Subject: `feat(112): port Cycle 17 creator-style Web services and DI`. Body: note this is Commit 2 of 2 per D-07; list the six Web M-file hunks with a one-line justification each (D-09); call out `PacketSessionCache.cs` explicitly as D-18's single ratified exception to D-12's never-port list; call out the `archidekt` pipeline registration as D-17's ratified flip of D-03; and state that no package reference was added.

    README is not updated at 112: no user-visible behavior changes, because no creator-style route is wired until Phase 114. State this decision explicitly in the plan summary rather than silently skipping it.

    Do not push. The user pushes.
  </action>
  <verify>
    <automated>"/mnt/c/Program Files/dotnet/dotnet.exe" build DeckFlow.sln &amp;&amp; test $(git diff --name-only main..HEAD -- DeckFlow.Core DeckFlow.Core.Tests DeckFlow.Web DeckFlow.Web.Tests DeckFlow.CLI content-kb scripts | wc -l) -eq 119</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/format-check-changed.sh staged` exits 0 with the full Commit 2 set staged, and any fix it required is inside this commit — `git rev-list --count main..HEAD` shows exactly 2 more production commits than before Phase 112 began, not 3.
    - `"/mnt/c/Program Files/dotnet/dotnet.exe" build DeckFlow.sln` exits 0 with 0 errors and a warning count at or below the `112-BASELINE.md` figure, with no warning ID absent from the baseline table.
    - The targeted Web test filter exits 0 with 0 failed tests.
    - Full `DeckFlow.Core.Tests` and full `DeckFlow.Web.Tests` runs introduce zero failures beyond those listed under the baseline's pre-existing-failures heading.
    - `git diff --name-only main..HEAD -- DeckFlow.Core DeckFlow.Core.Tests DeckFlow.Web DeckFlow.Web.Tests DeckFlow.CLI content-kb scripts | wc -l` returns 119, and every listed path appears on either the plan-02 or plan-04 published allowlist or in the declared M-file set.
    - The never-port name grep (audit step b) returns 0 lines.
    - `git diff main..HEAD` piped to `grep -c 'tool.creator-style.enabled'` returns 0.
    - `git diff main..HEAD -- '*.csproj'` piped to `grep -c 'PackageReference'` returns 0.
    - `git diff main..HEAD -- DeckFlow.Web/Services/PacketSessionCache.cs` piped to `grep -c '^-[^-]'` returns 0.
    - The six hands-off files (audit step f) show zero diff against main.
    - `git log -1 --format=%B` starts with `feat(112): port Cycle 17 creator-style Web services and DI`, mentions D-18 and D-17 by name, and contains no `Co-Authored-By` line.
    - `git log -1 --format=%an` is the repo's configured default author (`luntc1972`).
    - `git status --porcelain` shows no leftover unstaged production changes from Phase 112.
    - No push was performed: `git rev-list --count origin/feat/personal-tools..HEAD` is greater than 0 (or the remote branch does not exist yet).
  </acceptance_criteria>
  <done>Commit 2 exists, both commits pass the cumulative D-08 path audit, the solution builds clean against the baseline, and both full test suites are green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DI container → running host | A missing registration or an unregistered Polly key surfaces only at runtime construction, never at compile time |
| working tree → git history | Once committed, a silent revert of shipped Cycle 16/18/19 work becomes part of the branch record |
| local host process → developer machine | A headless test server left running holds a port and can mislead the next session's tests |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-112-01 | Denial of Service | Unregistered `archidekt` Polly key causing `ArchidektOwnerClient`'s constructor to throw at DI resolution, taking down any request path that touches creator-style | mitigate | Task 1's real-implementation resolution test, whose guard value is proven by temporarily removing the registration and observing a red test; task 2's real host boot asserts no `KeyNotFoundException` in the startup log |
| T-112-15 | Repudiation | A DI test that fakes the risky dependency, passing on paper while the real app throws | mitigate | D-20: the test must resolve the concrete `ArchidektOwnerClient`, must not substitute a fake, must not pre-register the pipeline inline, and must not swallow `KeyNotFoundException` — each asserted explicitly |
| T-112-06 | Tampering | Silent revert of shipped work by a stale file copy that still compiles and still passes tests | mitigate | Cumulative D-08 path audit: exact 119-path count, never-port name grep, `tool.creator-style.enabled` content grep, csproj `PackageReference` grep, additive-only assertion on `PacketSessionCache.cs`, zero-diff assertion on the six hands-off files |
| T-112-07 | Tampering | Formatter overreach converting `{ get; init; }` to `{ get; }`, silently breaking System.Text.Json deserialization | mitigate | Task 3 instructs explicit refusal of the five carve-out violations; the `CarveOutGuard` test in the suite backstops it and runs in the full-suite gate |
| T-112-13 | Elevation of Privilege | An accidental public route or nav entry exposing the admin-only engine before Phase 114 adds its BasicAuth gate | mitigate | Audit step b greps the whole branch diff for controller, view, help, SEO, and ToolRegistry paths; audit step c greps for the feature-flag key |
| T-112-SC | Tampering | Package-manager installs (NuGet) | mitigate | Audit step d asserts zero `PackageReference` lines across the entire branch diff; `Testcontainers.PostgreSql` specifically forbidden per D-15 and CLAUDE.md |

## Not applicable at 112 (per RESEARCH §Security Domain)
- ASVS V2 Authentication and V4 Access Control: no creator-style route is wired at 112; the admin controller and its BasicAuth gate land at Phase 114.
- ASVS V6 Cryptography: nothing in this phase handles secrets or crypto.
- ASVS V5 Input Validation applies only partially and is satisfied by ported, already-reviewed logic (`CardGroundingGuard`, `ScryfallCardNameGrounder`); no new validation surface is designed here.
</threat_model>

<verification>
- `"/mnt/c/Program Files/dotnet/dotnet.exe" build DeckFlow.sln` — 0 errors, warnings at or below baseline, no new warning ID.
- `dotnet test` on both projects, full runs — zero new failures versus the baseline's pre-existing list.
- Targeted filter `FullyQualifiedName~CreatorStyleDiRegistration` — passes, and fails when the `archidekt` registration is temporarily removed.
- Headless boot via `scripts/run-web-test.sh` — healthy HTTP response, seed-load log line present, no `KeyNotFoundException`, port released afterwards.
- `git diff --name-only main..HEAD -- <production dirs> | wc -l` returns 119; never-port grep, flag-key grep, `PackageReference` grep all return 0.
- `scripts/format-check-changed.sh staged` — exit 0.
</verification>

<success_criteria>
- Creator-style Web services, seed loader, and DI registrations are ported and resolve at application startup (PORT-02, ROADMAP criterion 3).
- A permanent CI test resolves the real `ArchidektOwnerClient` and turns red if the `archidekt` pipeline registration disappears (D-13, D-20).
- The solution builds with 0 errors and no new warnings versus the recorded baseline (ROADMAP criterion 2).
- Both full test suites are green with no new failures (ROADMAP criterion 4).
- Commit 2 of D-07's two commits exists, format-gated and cumulatively path-audited.
</success_criteria>

<output>
Create `.planning/phases/112-cycle-17-code-port/112-06-SUMMARY.md` when done, quoting: the strengthened test's name and the red/green result of the temporary-removal proof, the headless boot evidence, the final warning count against the baseline, both full-suite summary lines, the 119-path audit result, and the Commit 2 SHA.
</output>
