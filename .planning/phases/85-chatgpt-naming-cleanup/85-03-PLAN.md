---
phase: 85-chatgpt-naming-cleanup
plan: 03
type: execute
wave: 2
depends_on: [85-01]
files_modified:
  - DeckFlow.Web/wwwroot/ts/deck-sync.ts
  - DeckFlow.Web/wwwroot/ts/moxfield-extension-bridge.ts
  - DeckFlow.Web/wwwroot/ts/busy-indicator.ts
  - DeckFlow.Web/Views/Deck/DeckAnalysis.cshtml
  - DeckFlow.Web/Views/Deck/DeckComparison.cshtml
  - DeckFlow.Web/Views/Deck/CedhMetaGap.cshtml
  - DeckFlow.Web/Views/Shared/_WorkflowStepTabs.cshtml
  - DeckFlow.Web/e2e/deck-analysis-render.spec.ts
  - DeckFlow.Web/e2e/print-analysis-results.spec.ts
  - DeckFlow.Web/e2e/print-button-appearance.spec.ts
  - DeckFlow.Web/e2e/ui-responsive.spec.ts
  - DeckFlow.Web.Tests/DeckAnalysisScoreViewTests.cs
  - DeckFlow.Web.Tests/DeckAnalysisPrintButtonViewTests.cs
  - DeckFlow.Web.Tests/DeckComparisonPrintButtonViewTests.cs
  - DeckFlow.Web.Tests/MetaGapViewRenderTests.cs
autonomous: true
requirements: [AICLEAN-02, AICLEAN-03]
must_haves:
  truths:
    - "All 48 data-chatgpt-* attribute names + 38 camelCase ChatGpt* symbols in deck-sync.ts are renamed to data-prompt-* / Prompt* (mechanical D1), and the moxfield-extension-bridge.ts 3 cache-key if-checks + the 2 self-referential Phase-82 comments (busy-indicator.ts, moxfield-extension-bridge.ts) are updated so no chatgpt- substring remains in these 3 TS files"
    - "The D5 contract VALUES are renamed in ONE task and ONE commit on BOTH the TS consuming side (deck-sync.ts, moxfield-extension-bridge.ts) AND the Razor emitting side (DeckAnalysis/DeckComparison/CedhMetaGap): chatgpt-packets->prompt-packets, chatgpt-deck-comparison->prompt-deck-comparison, chatgpt-cedh-meta-gap->prompt-cedh-meta-gap, chatgpt-deck-url->prompt-deck-url, chatgpt-deck-text->prompt-deck-text, decksync-chatgpt-ui-mode->decksync-prompt-ui-mode — with the six OLD literals absent everywhere"
    - "DeckAnalysis, DeckComparison, CedhMetaGap, and the shared _WorkflowStepTabs partial emit only prompt-* classes and data-prompt-* attributes"
    - "The 4 e2e specs and 4 xUnit view-render tests assert the renamed prompt-* selectors and pass (automated)"
    - "No genuine ChatGPT-model reference is renamed (none exist in these files — deck-sync.ts holds only generic UI symbols; the model trio lives in Services/PromptBuilders, untouched)"
  artifacts:
    - path: "DeckFlow.Web/wwwroot/ts/deck-sync.ts"
      provides: "Renamed Prompt* symbols + data-prompt-* selectors + renamed D5 cache-key/sync-panel/storage-key literals"
      contains: "prompt-packets"
    - path: "DeckFlow.Web/wwwroot/ts/moxfield-extension-bridge.ts"
      provides: "Renamed 3 cache-key if-checks + reworded Phase-82 comment"
      contains: "prompt-cedh-meta-gap"
    - path: "DeckFlow.Web/Views/Deck/DeckAnalysis.cshtml"
      provides: "Renamed data-cache-key/data-sync-panel contract values + prompt-* classes/attrs"
      contains: "data-cache-key=\"prompt-packets\""
  key_links:
    - from: "DeckFlow.Web/Views/Deck/DeckAnalysis.cshtml data-cache-key=\"prompt-packets\""
      to: "deck-sync.ts + moxfield-extension-bridge.ts cache-key branch selection"
      via: "identical string-literal renamed on both sides in ONE task/ONE commit (D5 lockstep)"
      pattern: "prompt-packets"
    - from: "_WorkflowStepTabs.cshtml .prompt-step-* + data-prompt-*step"
      to: "site CSS .prompt-step-* (85-02) + deck-sync.ts show/validate step selectors"
      via: "same-wave D1 rename convergence"
      pattern: "prompt-step-|data-prompt-.*step"
---

<objective>
Rename the behavior-critical `chatgpt-*` surface — all TypeScript symbols/selectors in deck-sync.ts +
moxfield-extension-bridge.ts + busy-indicator.ts, the three D5-contract workflow views
(DeckAnalysis/DeckComparison/CedhMetaGap), the shared _WorkflowStepTabs partial, and the e2e + xUnit
view-render tests that assert those selectors — to `prompt-*` / `Prompt*`, in ONE plan so the D5
client<->server contract values move in lockstep (RESEARCH Hazard 1 / D5). Satisfies AICLEAN-02/03.

Purpose: this is the only part of the phase where a one-sided rename is a real BEHAVIOR regression,
not a cosmetic one. The 3 sessionStorage cache-key literals (`chatgpt-packets`,
`chatgpt-deck-comparison`, `chatgpt-cedh-meta-gap`) are read by deck-sync.ts's clear-and-restart
branch AND by moxfield-extension-bridge.ts's form-routing if-checks AND emitted by the Razor
`data-cache-key` attribute — all three MUST rename together or the Clear button silently falls through
to the generic clear and the Moxfield extension silently mis-routes imported deck text (no build
error, no existing test — RESEARCH Pitfall 3). This plan owns 100% of the D5 contract, and per
plan-review Task 1 renames BOTH the TS consuming side AND the Razor emitting side in a SINGLE commit —
committing is forbidden until both sides are changed, so no intermediate state strands a contract value.
Output: 3 TS files + 3 workflow views + 1 shared partial + 4 e2e + 4 xUnit tests renamed; scoped e2e
+ xUnit green (automated); zero chatgpt- in these files.

D3 keep-list: NONE of these files contain a genuine ChatGPT-model reference. deck-sync.ts's
`ChatGpt*` symbols are all generic prompt-tool UI machinery (RESEARCH TS Inventory — all 38 RENAME).
The model trio (ChatGpt*PromptVariant, AiPlatform.ChatGpt) lives in Services/PromptBuilders / Models,
which this plan does not touch.

WAVE note: runs in wave 2 alongside 85-02 (CSS) and 85-04 (remaining views) — disjoint file sets, so
parallel. The .prompt-* classes emitted here resolve against the .prompt-* CSS rules 85-02 renames in
the same wave; render is byte-identical at the wave boundary (proven by 85-05). Build + xUnit stay
green per commit because the view-render tests in THIS plan move in lockstep with the views.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/85-chatgpt-naming-cleanup/85-CONTEXT.md
@.planning/phases/85-chatgpt-naming-cleanup/85-RESEARCH.md
@.planning/phases/85-chatgpt-naming-cleanup/85-VALIDATION.md
@CLAUDE.md

<interfaces>
<!-- D5 CONTRACT VALUE TABLE (shared source of truth; rename identically on every listed side, one commit): -->
<!--   chatgpt-packets            -> prompt-packets            (DeckAnalysis.cshtml:91 data-cache-key; deck-sync.ts:919,1159 + clearChatGptPacketsState branch; moxfield-extension-bridge.ts:240 if) -->
<!--   chatgpt-deck-comparison    -> prompt-deck-comparison    (DeckComparison.cshtml:173 data-cache-key; deck-sync.ts generic path; moxfield-extension-bridge.ts:249 if) -->
<!--   chatgpt-cedh-meta-gap      -> prompt-cedh-meta-gap      (CedhMetaGap.cshtml:42 data-cache-key; moxfield-extension-bridge.ts:256 if) -->
<!--   chatgpt-deck-url           -> prompt-deck-url           (DeckAnalysis.cshtml:163 data-sync-panel; deck-sync.ts:72) -->
<!--   chatgpt-deck-text          -> prompt-deck-text          (DeckAnalysis.cshtml:169 data-sync-panel; deck-sync.ts:73) -->
<!--   decksync-chatgpt-ui-mode   -> decksync-prompt-ui-mode   (deck-sync.ts:1190 chatGptUiModeStorageKey value; single file) -->

<!-- 38 camelCase/PascalCase symbols in deck-sync.ts, all RENAME ChatGpt->Prompt (RESEARCH TS Inventory): -->
<!-- ChatGptUiMode(type) applyChatGptCedhSort applyChatGptUiMode attachChatGptCedhWorkflow -->
<!-- attachChatGptComparisonWorkflow attachChatGptPacketsWorkflow chatGptUiModeStorageKey -->
<!-- clearChatGptPacketsState getDefaultChatGptUiMode maxChatGptCedhReferences mobileChatGptUiModeQuery -->
<!-- parseChatGptCedhPage parseChatGptCedhSortValue parseChatGptCedhStep parseChatGptComparisonStep -->
<!-- parseChatGptDownloadFilename parseChatGptStep parseChatGptUiMode registerChatGptDownloadHandler -->
<!-- registerChatGptPrintHandler scrollChatGptCedhResults scrollChatGptComparisonResults scrollChatGptResults -->
<!-- setChatGptCedhValidationMessage setChatGptComparisonValidationMessage setChatGptValidationMessage -->
<!-- showChatGptCedhReferencePage showChatGptCedhStep showChatGptComparisonStep showChatGptStep -->
<!-- sortChatGptCedhFromHeader sortChatGptCedhFromMobileControl syncChatGptCedhCheckboxState -->
<!-- triggerChatGptBlobDownload validateChatGptCedhStep validateChatGptComparisonStep validateChatGptPacketsStep -->
<!-- wireChatGptZipUpload + CHATGPT_DOWNLOAD_FALLBACK_FILENAME (const; value 'session.zip' has no chatgpt text) -->

<!-- 48 data-chatgpt-* attribute names -> data-prompt-* (RESEARCH TS Inventory; mechanical prefix swap). -->

<!-- SELF-REFERENTIAL COMMENTS to reword (RESEARCH Hazard 4 / Pitfall 2 — they quote chatgpt- literals -->
<!-- and would trip the strict grep gate even after every real identifier is renamed): -->
<!--   busy-indicator.ts:3        "// fully chatgpt-*-free ..."  -> reword to prompt-*/past tense -->
<!--   moxfield-extension-bridge.ts:6-11  the chatgpt-packets/... "moved VERBATIM" block -> reword to past tense (Phase 85 renamed these) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename TS symbols/selectors AND the 3 workflow views + shared partial — D5 both-sides in ONE commit</name>
  <files>DeckFlow.Web/wwwroot/ts/deck-sync.ts, DeckFlow.Web/wwwroot/ts/moxfield-extension-bridge.ts, DeckFlow.Web/wwwroot/ts/busy-indicator.ts, DeckFlow.Web/Views/Deck/DeckAnalysis.cshtml, DeckFlow.Web/Views/Deck/DeckComparison.cshtml, DeckFlow.Web/Views/Deck/CedhMetaGap.cshtml, DeckFlow.Web/Views/Shared/_WorkflowStepTabs.cshtml</files>
  <read_first>
    - DeckFlow.Web/wwwroot/ts/deck-sync.ts (63 kebab + ~146 camelCase refs — the bulk; contract literals at :72-73, :919, :1159, :1190)
    - DeckFlow.Web/wwwroot/ts/moxfield-extension-bridge.ts (:240/:249/:256 cache-key if-checks; :6-11 comment)
    - DeckFlow.Web/wwwroot/ts/busy-indicator.ts (:3 comment)
    - DeckFlow.Web/Views/Deck/DeckAnalysis.cshtml (135 kebab refs; data-cache-key="chatgpt-packets":91, data-sync-panel chatgpt-deck-url/text:163,169)
    - DeckFlow.Web/Views/Deck/DeckComparison.cshtml (56 refs; data-cache-key="chatgpt-deck-comparison":173)
    - DeckFlow.Web/Views/Deck/CedhMetaGap.cshtml (75 refs; data-cache-key="chatgpt-cedh-meta-gap":42; :has attr on the cedh-reference-table)
    - DeckFlow.Web/Views/Shared/_WorkflowStepTabs.cshtml (5 refs; shared step-tab chrome for all 4 workflow pages)
    - .planning/phases/85-chatgpt-naming-cleanup/85-RESEARCH.md (TS Inventory; Views Inventory; Hazard 1; Hazard 4; Pitfall 3)
  </read_first>
  <action>
    Rename BOTH sides of the D5 contract and all supporting identifiers in a SINGLE commit (do NOT
    commit until every listed file below is edited — a task-level commit between the TS and Razor edits
    would strand the 6 contract values, RESEARCH Pitfall 3). Steps:
    (1) TS: in deck-sync.ts / moxfield-extension-bridge.ts / busy-indicator.ts rename all 38
    camelCase/PascalCase `ChatGpt*` symbols -> `Prompt*`, all `data-chatgpt-*` string selectors ->
    `data-prompt-*`, CHATGPT_DOWNLOAD_FALLBACK_FILENAME -> PROMPT_DOWNLOAD_FALLBACK_FILENAME (value
    'session.zip' unchanged), and the D5 literals per the <interfaces> table on the consuming side
    (incl the 3 moxfield-extension-bridge if-checks at :240/:249/:256 and the decksync-prompt-ui-mode
    storage key). Reword the 2 self-referential Phase-82 comments (busy-indicator.ts:3,
    moxfield-extension-bridge.ts:6-11) to past-tense prompt-* wording so no `chatgpt-` substring survives.
    (2) Razor: in DeckAnalysis/DeckComparison/CedhMetaGap rename every `chatgpt-*` class -> `prompt-*`
    and every `data-chatgpt-*` attribute NAME -> `data-prompt-*`, and rename the D5 emitting-side VALUES
    to EXACTLY match step (1): DeckAnalysis data-cache-key "chatgpt-packets"->"prompt-packets" +
    data-sync-panel "chatgpt-deck-url"/"chatgpt-deck-text"->"prompt-deck-url"/"prompt-deck-text";
    DeckComparison data-cache-key ->"prompt-deck-comparison"; CedhMetaGap data-cache-key
    ->"prompt-cedh-meta-gap". Rename the cedh-reference-table/checkbox attribute NAMES to match the CSS
    attr-selector rename in 85-02.
    (3) Shared partial: in _WorkflowStepTabs rename its `.chatgpt-step-*`/`data-chatgpt-*step` to
    prompt-* (it renders for DeckAnalysis/DeckComparison/CedhMetaGap AND DeckPrimer).
    Change only identifier tokens; KEEP any user-visible prose verbatim (none needs changing in these
    workflow-form views). Do NOT touch content-kb.ts (its 'Copy for ChatGPT' copy is D3 keep-list and
    out of scope). Do NOT edit DeckFlow.Web/wwwroot/js (gitignored compiled output). Changed-lines-only,
    LF endings, no reflow. Then commit once.
  </action>
  <acceptance_criteria>
    - Zero `chatgpt` (any case) in the 3 TS files; zero `chatgpt-` (strict kebab) in the 3 views + shared partial.
    - Six NEW literals present on their expected side: prompt-packets (DeckAnalysis + deck-sync.ts + moxfield-extension-bridge.ts), prompt-deck-comparison (DeckComparison + moxfield-extension-bridge.ts), prompt-cedh-meta-gap (CedhMetaGap + moxfield-extension-bridge.ts), prompt-deck-url + prompt-deck-text (DeckAnalysis + deck-sync.ts), decksync-prompt-ui-mode (deck-sync.ts).
    - Six OLD literals (chatgpt-packets / chatgpt-deck-comparison / chatgpt-cedh-meta-gap / chatgpt-deck-url / chatgpt-deck-text / decksync-chatgpt-ui-mode) absent from ALL 7 files.
    - Renamed symbols present: PromptUiMode, clearPromptPacketsState, registerPromptPrintHandler; no ChatGpt symbol remains.
    - dotnet build DeckFlow.sln reports 0 Warning(s) and 0 Error(s) (TS compiles via the MSBuild target).
  </acceptance_criteria>
  <verify>
    <automated>cd /mnt/c/users/chrislunt/source/personal/deckflow-cycle15 && TS="DeckFlow.Web/wwwroot/ts/deck-sync.ts DeckFlow.Web/wwwroot/ts/moxfield-extension-bridge.ts DeckFlow.Web/wwwroot/ts/busy-indicator.ts" && V="DeckFlow.Web/Views/Deck/DeckAnalysis.cshtml DeckFlow.Web/Views/Deck/DeckComparison.cshtml DeckFlow.Web/Views/Deck/CedhMetaGap.cshtml DeckFlow.Web/Views/Shared/_WorkflowStepTabs.cshtml" && test -z "$(grep -rIni 'chatgpt' $TS)" && test -z "$(grep -rIn 'chatgpt-' $V)" && test -z "$(grep -rIn 'chatgpt-packets\|chatgpt-deck-comparison\|chatgpt-cedh-meta-gap\|chatgpt-deck-url\|chatgpt-deck-text\|decksync-chatgpt-ui-mode' $TS $V)" && grep -q 'data-cache-key="prompt-packets"' DeckFlow.Web/Views/Deck/DeckAnalysis.cshtml && grep -q 'prompt-packets' DeckFlow.Web/wwwroot/ts/moxfield-extension-bridge.ts && grep -q 'prompt-cedh-meta-gap' DeckFlow.Web/wwwroot/ts/moxfield-extension-bridge.ts && grep -q 'decksync-prompt-ui-mode' DeckFlow.Web/wwwroot/ts/deck-sync.ts && b=$("/mnt/c/Program Files/dotnet/dotnet.exe" build DeckFlow.sln 2>&1) && echo "$b" | grep -q '0 Warning(s)' && echo "$b" | grep -q '0 Error(s)'</automated>
  </verify>
  <done>TS + 3 workflow views + shared partial renamed in one commit; all 6 D5 literals renamed both-sides, old literals gone everywhere; build 0/0.</done>
</task>

<task type="auto">
  <name>Task 2: Update the 4 e2e specs + 4 xUnit view-render tests to the renamed selectors (lockstep, automated e2e)</name>
  <files>DeckFlow.Web/e2e/deck-analysis-render.spec.ts, DeckFlow.Web/e2e/print-analysis-results.spec.ts, DeckFlow.Web/e2e/print-button-appearance.spec.ts, DeckFlow.Web/e2e/ui-responsive.spec.ts, DeckFlow.Web.Tests/DeckAnalysisScoreViewTests.cs, DeckFlow.Web.Tests/DeckAnalysisPrintButtonViewTests.cs, DeckFlow.Web.Tests/DeckComparisonPrintButtonViewTests.cs, DeckFlow.Web.Tests/MetaGapViewRenderTests.cs</files>
  <read_first>
    - DeckFlow.Web/e2e/print-button-appearance.spec.ts + DeckFlow.Web/e2e/ui-responsive.spec.ts (assert chatgpt-* selectors; 12 + 4 refs)
    - DeckFlow.Web.Tests/DeckAnalysisScoreViewTests.cs ("chatgpt-score", "chatgpt-score-grid", "chatgpt-score-crosscheck")
    - DeckFlow.Web.Tests/DeckAnalysisPrintButtonViewTests.cs + DeckComparisonPrintButtonViewTests.cs (PrintButtonHook = "data-chatgpt-print")
    - DeckFlow.Web.Tests/MetaGapViewRenderTests.cs (chatgpt-cedh-* asserts)
    - CLAUDE.md (headless run-web-test.sh + DECKFLOW_DISABLE_AUTO_BROWSER=true; never a Windows-host browser; npx --no-install)
  </read_first>
  <action>
    Update every asserted `chatgpt-*` class/attribute string in these 4 e2e specs and 4 xUnit view-render
    tests to the renamed `prompt-*`/`data-prompt-*` value so they assert the renamed selectors emitted by
    Task 1's views (lockstep test updates, NOT behavior changes): DeckAnalysisScoreViewTests
    "chatgpt-score"->"prompt-score" etc.; *PrintButtonViewTests PrintButtonHook
    "data-chatgpt-print"->"data-prompt-print"; MetaGapViewRenderTests chatgpt-cedh-*->prompt-cedh-*.
    Do NOT touch bracket-smoke.spec.ts / interactions.spec.ts — their only chatgpt hits are all-caps
    "ChatGPT" prose/regex asserting genuine ChatGPT-variant behavior (D3 keep-list, not identifiers).
    Changed-lines-only, LF. Then run the scoped xUnit tests AND the 4 scoped Playwright e2e specs against
    the headless scripts/run-web-test.sh server (DECKFLOW_DISABLE_AUTO_BROWSER=true; npx --no-install;
    NEVER a Windows-host browser; no new deps).
  </action>
  <acceptance_criteria>
    - Zero `chatgpt-` (strict kebab) remains in the 4 e2e specs and the 4 xUnit test files.
    - The scoped xUnit tests (Score / PrintButton / MetaGapViewRender) pass against the renamed views.
    - The 4 scoped Playwright e2e specs pass against the renamed pages (headless server; no Windows browser).
  </acceptance_criteria>
  <verify>
    <automated>cd /mnt/c/users/chrislunt/source/personal/deckflow-cycle15 && test -z "$(grep -rIn 'chatgpt-' DeckFlow.Web/e2e/deck-analysis-render.spec.ts DeckFlow.Web/e2e/print-analysis-results.spec.ts DeckFlow.Web/e2e/print-button-appearance.spec.ts DeckFlow.Web/e2e/ui-responsive.spec.ts DeckFlow.Web.Tests/DeckAnalysisScoreViewTests.cs DeckFlow.Web.Tests/DeckAnalysisPrintButtonViewTests.cs DeckFlow.Web.Tests/DeckComparisonPrintButtonViewTests.cs DeckFlow.Web.Tests/MetaGapViewRenderTests.cs)" && "/mnt/c/Program Files/dotnet/dotnet.exe" test DeckFlow.Web.Tests --filter "FullyQualifiedName~Score|FullyQualifiedName~PrintButton|FullyQualifiedName~MetaGapViewRender" && ( cd DeckFlow.Web && DECKFLOW_DISABLE_AUTO_BROWSER=true npx --no-install playwright test deck-analysis-render print-analysis-results print-button-appearance ui-responsive )</automated>
  </verify>
  <done>4 e2e specs + 4 xUnit view tests assert the renamed prompt-* selectors and pass (automated); zero chatgpt- remains in them.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Client (browser TS + rendered markup) <-> sessionStorage. The D5 cache-key values are the client-side
contract between the Razor-emitted `data-cache-key` attribute, deck-sync.ts's persistence/clear branch,
and moxfield-extension-bridge.ts's form-routing. No server/DB/auth boundary is introduced or altered;
sessionStorage is per-tab, client-only, no cross-session/cross-tool persistence (RESEARCH Runtime State).

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-85-03-01 | (all) | TS symbol / data-attr / cache-key identifier rename | accept | ASVS L1: pure identifier rename of client-side machinery; no new attacker-controllable input, no trust boundary crossed, no new persisted/server state. No package installs. |
| T-85-03-02 | Tampering (correctness, not security) | D5 cache-key client<->server contract | mitigate | The 3 cache-key literals are renamed on ALL sides (Razor emitter + deck-sync.ts + moxfield-extension-bridge.ts) in ONE task/ONE commit; Task 1's verify asserts the six NEW literals present + six OLD literals absent across all 7 files, closing the silent-desync regression RESEARCH Pitfall 3 flagged (re-asserted in 85-05). |
</threat_model>

<verification>
- Zero `chatgpt` (any case) in the 3 TS files; zero `chatgpt-` (strict kebab) in the 3 workflow views + shared partial + 4 e2e + 4 xUnit tests.
- The six D5 literals renamed both-sides in one commit; six OLD literals absent from all 7 D5 files (Task 1 gate; re-checked in 85-05).
- Scoped xUnit (Score/PrintButton/MetaGapViewRender) + the 4 scoped Playwright e2e specs pass (automated).
- dotnet build DeckFlow.sln reports 0 Warning(s) and 0 Error(s).
</verification>

<success_criteria>
- AICLEAN-02/03: TS + the 3 D5-contract views + shared partial + their e2e/xUnit tests renamed to
  prompt-* in lockstep; the D5 contract moves atomically in one commit; no dead selector; scoped suites green.
</success_criteria>

<output>
Create `.planning/phases/85-chatgpt-naming-cleanup/85-03-SUMMARY.md` when done. Record: the D5 contract
table as applied, confirmation the 6 literals were renamed both-sides in one commit (old literals gone),
the 38 symbols + 48 data-attrs renamed, the 2 reworded comments, and the e2e/xUnit lockstep updates.
</output>
