---
phase: 107-cut-lab-tech-debt-cleanup
plan: 04
type: execute
wave: 3
depends_on: ["107-03"]
files_modified:
  - DeckFlow.Web/Models/Api/CutLabDecideApiResponse.cs
  - DeckFlow.Web/Controllers/Api/CutLabApiController.cs
  - DeckFlow.Web/Models/CutLabViewModel.cs
  - DeckFlow.Web/Views/Deck/CutLab.cshtml
  - DeckFlow.Web/wwwroot/ts/cut-lab.ts
  - DeckFlow.Web.Tests/CutLabApiControllerTests.cs
  - DeckFlow.Web/ts-tests/cut-lab-proposal.test.ts
  - DeckFlow.Web/e2e/cut-lab-structure.spec.ts
autonomous: true
requirements: [CLEANUP-6]
must_haves:
  truths:
    - "On a JS decide/accept, the Structural findings table updates client-side without a full page round-trip"
    - "The no-JS / server-render path for Model.Findings/FindingGroups is completely unchanged (additive only)"
    - "The findings grouping (WeakFloorCase merge) has a single source of truth — computed server-side, not re-implemented in TypeScript"
    - "The client renderer replaces ONLY the findings body children and writes values via textContent/typed DOM (no innerHTML with raw data)"
    - "End of phase: full dotnet + Vitest + Playwright suites green, no EOL churn, no compiled wwwroot/js staged"
  artifacts:
    - path: "DeckFlow.Web/Models/Api/CutLabDecideApiResponse.cs"
      provides: "CutLabDecideFindingDto + CutLabDecideFindingGroupDto + StructuralFindings/availability flags on the response"
      contains: "StructuralFindings"
    - path: "DeckFlow.Web/wwwroot/ts/cut-lab.ts"
      provides: "renderStructuralFindings client renderer (body-scoped) wired into the decide success path"
      contains: "renderStructuralFindings"
    - path: "DeckFlow.Web/e2e/cut-lab-structure.spec.ts"
      provides: "e2e asserting the findings table updates on decide WITHOUT a navigation reload"
  key_links:
    - from: "CutLabApiController.PostDecideAsync (afterFindings already computed)"
      to: "CutLabDecideApiResponse.StructuralFindings"
      via: "server-side grouping (reused BuildFindingGroups) mapped into the new DTO"
      pattern: "StructuralFindings"
    - from: "cut-lab.ts handleDecisionSubmit success path"
      to: "[data-cut-lab-structural-findings-body] node"
      via: "renderStructuralFindings replaces only body children"
      pattern: "data-cut-lab-structural-findings-body"
---

<objective>
Item 6 (D-02, the only behavior-adjacent item — ISOLATED per D-04). Live-patch the Structural
findings table on JS decide/accept, mirroring the existing count-chip / working-list live-patch
pattern in cut-lab.ts. The server ALREADY computes the findings on every decide call
(`afterFindings`) and currently discards them — this is a plumbing task (serialize + render), not
new business logic. The live-patch MUST AUGMENT, not replace, the server-render refresh: the no-JS
form fallback stays intact. Also runs the end-of-phase full-suite gate.

Purpose: Close the last Cycle-18 Cut Lab tech-debt item — findings no longer go stale after a JS decide.
Output: New decide-response DTO fields, a server mapping, a body-scoped client renderer, e2e/Vitest/unit
coverage, and a phase-wide green gate.

Scope decisions (RESEARCH open questions 2 + Item 6):
- DECIDE endpoint only (ROADMAP names "JS decide"). `/api/cut-lab/adjust` computes-and-discards the
  same findings; extending it is OUT OF SCOPE — document the adjust-path gap explicitly as accepted.
- Grouping (`BuildFindingGroups` WeakFloorCase-merge) is done SERVER-SIDE and returned pre-grouped;
  do NOT port the merge logic to TypeScript (avoids a second source of truth).

NOTE (gate): No UI-SPEC.md required — this live-patches an ALREADY-SHIPPED table with server-computed
values; no new visual/interaction design.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/cut-lab/phases/107-cut-lab-tech-debt-cleanup/107-CONTEXT.md
@.planning/workstreams/cut-lab/phases/107-cut-lab-tech-debt-cleanup/107-RESEARCH.md

<interfaces>
<!-- Shapes the executor renders near-verbatim (from RESEARCH Item 6). Do not explore the codebase for these. -->

View-model shapes to mirror in the new DTO (CutLabViewModel.cs:848-874):
  CutLabFindingView      { Kind, Heading, Lead, Evidence: IReadOnlyList<string> }
  CutLabFindingGroupView { Kind, Heading, Items: IReadOnlyList<CutLabFindingView> }

Existing sibling DTO pattern to copy (CutLabDecideApiResponse.cs — 6 CutLabDecide*Dto records
already live in this file; e.g. CutLabDecideFloorWarningDto with pre-formatted strings the client
renders verbatim).

Server already computes findings and discards them (CutLabApiController.cs):
  PostDecideAsync (:44-152) → (CutLabStructuralFindingsResult afterFindings, CutLabRoundPlan roundPlan)
    = CutLabCutRoundEngine.BuildFindingsAndRoundPlan(...) at :112; used only by
    BuildNextProposal(roundPlan, afterFindings) at :135 for FindingCount/FindingChips.
  afterFindings also carries ComboDataAvailable / CategoryDataAvailable
    (→ Model.ComboDataUnavailable / CategoryDataUnavailable degradation notes).

Grouping logic (single source of truth to reuse, NOT reimplement in TS):
  CutLabViewModel.BuildFindingGroups (:386-426, private static) collapses multiple WeakFloorCase
  findings into one merged group at a specific queue position.

Existing client live-patch pattern to extend (cut-lab.ts):
  handleDecisionSubmit (:2072-2130) POSTs, gets CutLabDecisionResponse, then live-patches:
  writeDecisionStateToHiddenInputs, rebuildWhatifSelectOptionsFromState, patchStickyBar,
  renderRoundBanner, renderProposalCard, renderCutsMade (~:2120). Item 6 adds one more call here.

Target DOM section (CutLab.cshtml:466-518, confirmed markup):
  <section class="result-panel"> containing, as SIBLINGS:
    (a) a <div class="panel-heading"> with the count badge (.cutlab-findings-count, gated on
        totalStructuralFindings > 0),
    (b) the findings loop `@foreach (var findingGroup in Model.FindingGroups)` OR an else empty-state
        <p> (gated on totalStructuralFindings > 0),
    (c) two degradation-note <p class="cutlab-degradation-note"> blocks gated on
        Model.ComboDataUnavailable / Model.CategoryDataUnavailable.
  CRITICAL: the loop/empty-state and the degradation notes and the heading are FLAT SIBLINGS with no
  wrapper — a naive "clear the whole section" would nuke the heading + notes. The plan adds explicit
  markers so the renderer touches only what it should.
</interfaces>

Security (gate_notes): the renderer must build DOM via textContent / typed element creation — NEVER
`innerHTML` with server data. Values are already-validated server output, but keep injection surface
at zero. The decide endpoint is already same-origin-guarded (CutLabApiController.cs:47-50); no new
request fields are added.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Serialize the already-computed findings into the decide response (server)</name>
  <read_first>
    - DeckFlow.Web/Models/Api/CutLabDecideApiResponse.cs (full — the 6 sibling CutLabDecide*Dto
      records + CutLabDecideApiResponse record; CutLabDecideFloorWarningDto is the pattern).
    - DeckFlow.Web/Controllers/Api/CutLabApiController.cs (PostDecideAsync :44-152, esp. afterFindings
      at :112 and BuildNextProposal at :135).
    - DeckFlow.Web/Models/CutLabViewModel.cs (BuildFindingGroups :386-426; CutLabFindingView /
      CutLabFindingGroupView :848-874) — the grouping + shapes to reuse.
    - DeckFlow.Web.Tests/CutLabApiControllerTests.cs (existing decide-response assertions to extend).
  </read_first>
  <behavior>
    - PostDecideAsync's JSON response includes `StructuralFindings` (a list of grouped findings
      mirroring the server-rendered FindingGroups) plus `ComboDataAvailable` / `CategoryDataAvailable`.
    - For a decide that changes the findings, the response's StructuralFindings reflect the AFTER state
      (computed from afterFindings), not the before state.
    - WeakFloorCase findings are merged into a single group in the DTO (same shape BuildFindingGroups
      produces server-side) — the client receives pre-grouped data.
  </behavior>
  <action>
    In CutLabDecideApiResponse.cs, add two `sealed record` DTOs mirroring the view shapes:
    `CutLabDecideFindingDto { Kind, Heading, Lead, Evidence: IReadOnlyList<string> }` and
    `CutLabDecideFindingGroupDto { Kind, Heading, Items: IReadOnlyList<CutLabDecideFindingDto> }`
    (use `{ get; init; }` — carve-out: never convert to get-only). Add to `CutLabDecideApiResponse`:
    `StructuralFindings: IReadOnlyList<CutLabDecideFindingGroupDto>` plus `bool ComboDataAvailable` /
    `bool CategoryDataAvailable`.
    Make `BuildFindingGroups` reusable as the SINGLE grouping source: change its accessibility so the
    controller can call it (e.g. `internal static`, backed by the existing
    `[InternalsVisibleTo("DeckFlow.Web.Tests")]`) or extract a small shared helper both the ViewModel
    and controller call — do NOT duplicate the merge logic. In PostDecideAsync, map the ALREADY-computed
    `afterFindings` through that grouping into `StructuralFindings`, and set the two availability bools
    from `afterFindings.ComboDataAvailable` / `.CategoryDataAvailable`. Do not recompute findings.
    Extend CutLabApiControllerTests to assert the decide response now carries StructuralFindings with
    the expected grouped content + availability flags for a representative decide.
    Preserve LF endings, switch expressions, xmldoc indent; changed-lines-only.
  </action>
  <acceptance_criteria>
    - `grep -c "StructuralFindings" DeckFlow.Web/Models/Api/CutLabDecideApiResponse.cs` >= 1; both new
      DTO records present with `{ get; init; }` members.
    - `grep -n "BuildFindingGroups" DeckFlow.Web/Controllers/Api/CutLabApiController.cs` shows the
      controller reuses the ViewModel's grouping (no new WeakFloorCase-merge code in the controller).
    - PostDecideAsync populates StructuralFindings from `afterFindings` (not a second engine call).
    - `dotnet test DeckFlow.sln --filter "FullyQualifiedName~CutLabApiControllerTests"` passes,
      including a new assertion on StructuralFindings content + ComboDataAvailable/CategoryDataAvailable.
    - The server-rendered `Model.Findings`/`Model.FindingGroups` path is unchanged (`git diff` shows no
      change to the CutLab.cshtml findings loop in this task).
  </acceptance_criteria>
  <verify>
    <automated>dotnet build DeckFlow.sln && dotnet test DeckFlow.sln --filter "FullyQualifiedName~CutLabApiControllerTests"</automated>
  </verify>
  <done>Decide response serializes the pre-grouped findings + availability flags from the already-computed afterFindings; grouping is single-sourced; controller tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Body-scoped client renderer that live-patches the findings section (additive)</name>
  <read_first>
    - DeckFlow.Web/wwwroot/ts/cut-lab.ts: the `CutLabDecisionResponse` type declaration; the decide
      success path in `handleDecisionSubmit` (:2072-2130, esp. the render calls ending ~:2120); an
      existing typed DOM renderer to mirror (e.g. `renderProposalCard` / `renderFloorWarnings` /
      `appendDeltaLine`) for the textContent/createElement style.
    - DeckFlow.Web/Views/Deck/CutLab.cshtml:466-518 (the Structural findings section — heading + count
      badge, the findings loop / empty-state, and the two degradation notes are FLAT SIBLINGS; classes
      .cutlab-findings-count, .cutlab-finding, .cutlab-finding__heading, .cutlab-finding__item,
      .cutlab-finding__lead, .kb-chip-area__chips, .cutlab-degradation-note).
    - DeckFlow.Web/ts-tests/cut-lab-proposal.test.ts (existing client-renderer unit tests to extend).
  </read_first>
  <behavior>
    - Given a decide response with StructuralFindings, renderStructuralFindings rebuilds ONLY the
      findings body (group list / empty-state) inside a dedicated body node, updates/creates the count
      badge, and toggles the two degradation notes — using textContent / createElement only (no
      innerHTML with response data). The heading text and other sibling markup are NOT destroyed.
    - Availability degradation notes toggle to match ComboDataAvailable / CategoryDataAvailable.
    - Called only in the JS decide success path; the server-rendered initial section and the no-JS
      form-post fallback are untouched.
  </behavior>
  <action>
    Markup (CutLab.cshtml:466-518): wrap the findings loop + empty-state `<p>` in a dedicated body
    container `<div data-cut-lab-structural-findings-body>` (the renderer's ONLY replacement target),
    add `data-cut-lab-structural-findings` on the `<section>` as the locator, and add a stable marker
    on EACH degradation note (e.g. `data-cut-lab-degradation="combo"` /
    `data-cut-lab-degradation="category"`) so the renderer can toggle them by attribute without
    touching the heading.
    CRITICAL (count slot for the 0->N case): today `.panel-heading__actions` + the
    `.cutlab-findings-count` badge span are rendered ONLY inside the `@if (totalStructuralFindings > 0)`
    branch — so an initially-zero pool has NO insertion slot for the badge when findings later appear.
    FIX: render the `.panel-heading__actions` wrapper UNCONDITIONALLY (outside the `> 0` gate) and mark
    it `data-cut-lab-findings-count-slot`; keep the `.cutlab-findings-count` badge inside it but
    hidden/empty at zero (e.g. omit/blank the badge text when `totalStructuralFindings == 0`, or hide
    the slot via an attribute/class). This gives the renderer a STABLE selector to populate on 0->N and
    clear on N->0. Keep the findings loop/empty-state and degradation-note Razor gating otherwise intact
    (additive markup only — server render + no-JS fallback unchanged).
    Client: extend the `CutLabDecisionResponse` TS type with `structuralFindings` (array of groups →
    findings with kind/heading/lead/evidence) and the two availability booleans (camelCase). Write
    `renderStructuralFindings(response)` that: locates the section by `data-cut-lab-structural-findings`;
    updates the count badge inside the stable `data-cut-lab-findings-count-slot` — populating/showing
    the `.cutlab-findings-count` text when findings go 0→N and clearing/hiding it on N→0 (the slot
    always exists, so no create-parent race);
    CLEARS AND REBUILDS ONLY the children of `data-cut-lab-structural-findings-body` via
    createElement / textContent (group headings, leads, evidence chips using the same class names as
    the Razor markup, or an empty-state `<p>` when zero); and shows/hides each
    `data-cut-lab-degradation` note per the availability flags. NEVER assign response strings via
    innerHTML. Wire ONE call to it into `handleDecisionSubmit`'s success path immediately after
    `renderCutsMade` (~:2120). Do NOT remove or alter any server-render path.
    Add a Vitest unit test in cut-lab-proposal.test.ts asserting the renderer: (1) updates
    .cutlab-findings-count, (2) rebuilds body group headings/leads from a sample response WITHOUT
    disturbing the section heading, (3) toggles a degradation note, (4) the count badge APPEARS in the
    `data-cut-lab-findings-count-slot` on a 0→N transition and CLEARS/hides on an N→0 transition.
    Never stage compiled wwwroot/js. LF endings; changed-lines-only.
  </action>
  <acceptance_criteria>
    - `grep -c "data-cut-lab-structural-findings\b" DeckFlow.Web/Views/Deck/CutLab.cshtml` >= 1 and
      `grep -c "data-cut-lab-structural-findings-body" DeckFlow.Web/Views/Deck/CutLab.cshtml` == 1;
      `grep -c "data-cut-lab-degradation" DeckFlow.Web/Views/Deck/CutLab.cshtml` == 2. The server-render
      findings loop/heading/notes are otherwise unchanged (additive markers only).
    - `grep -c "data-cut-lab-findings-count-slot" DeckFlow.Web/Views/Deck/CutLab.cshtml` == 1 AND the
      `.panel-heading__actions` slot is rendered UNCONDITIONALLY (outside the `totalStructuralFindings > 0`
      gate) so a zero-findings pool still has the slot; the badge text is hidden/empty at zero.
    - The new Vitest test asserts the badge appears in the slot on 0→N and clears on N→0.
    - `grep -n "renderStructuralFindings" DeckFlow.Web/wwwroot/ts/cut-lab.ts` shows the function def
      AND one call in the decide success path; the renderer replaces children of the BODY node only
      (not the section) and creates/removes DOM via createElement/textContent.
    - No `innerHTML` assignment is fed by `structuralFindings`/response data in cut-lab.ts.
    - `npx --no-install tsc -p DeckFlow.Web/tsconfig.json --noEmit` clean; `npx --no-install vitest run`
      (in DeckFlow.Web/) passes including the new findings-renderer test (which asserts the section
      heading survives a rebuild).
  </acceptance_criteria>
  <verify>
    <automated>cd DeckFlow.Web && npx --no-install tsc -p tsconfig.json --noEmit && npx --no-install vitest run</automated>
  </verify>
  <done>Findings section has section/body/degradation markers + an unconditional data-cut-lab-findings-count-slot; typed body-scoped renderer rebuilds only the body, toggles notes, and populates/clears the badge in the stable slot (0→N/N→0), wired into the success path; heading + server/no-JS path intact; TS + Vitest green.</done>
</task>

<task type="auto">
  <name>Task 3: e2e — assert the findings table updates on decide without a reload</name>
  <read_first>
    - DeckFlow.Web/e2e/cut-lab-structure.spec.ts (~:128-136 — existing full-page-load assertions on
      .cutlab-finding__heading, .cutlab-findings-count, and finding-group text like "Weak floor cases").
    - DeckFlow.Web/e2e/cut-lab-smoke.spec.ts (the existing import → lock → decide flow + admin-lock /
      flag-ON harness patterns to reuse for setup).
    - CLAUDE.md testing constraint: start the server via `scripts/run-web-test.sh`
      (DECKFLOW_DISABLE_AUTO_BROWSER=true); drive Playwright headless in WSL; never open a Windows browser.
  </read_first>
  <action>
    Add a test to cut-lab-structure.spec.ts that: imports a pool, reaches a proposal, and accepts a
    cut that changes the structural findings via the JS decide path (fetch POST, NOT a form
    navigation). Assert the Structural findings section content (.cutlab-findings-count and/or a
    .cutlab-finding__heading / lead) changes as a result of the decide RESPONSE without any
    `page.goto()` / full reload — e.g. capture the count-badge text before, trigger decide, and assert
    it updated in place. Guard against a navigation (assert the same page context / no reload marker).
    Follow the existing spec's admin-lock/flag-ON setup and serialization conventions.
  </action>
  <acceptance_criteria>
    - New test in cut-lab-structure.spec.ts asserts a findings-section text change AFTER a decide with
      NO `page.goto()`/reload between the before/after read (live-patch proven).
    - `scripts/run-web-test.sh` + `npx --no-install playwright test cut-lab-structure.spec.ts` passes
      (headless, no Windows-host browser); existing cut-lab e2e specs remain green.
  </acceptance_criteria>
  <verify>
    <automated>scripts/run-web-test.sh & sleep 8; cd DeckFlow.Web && npx --no-install playwright test e2e/cut-lab-structure.spec.ts</automated>
  </verify>
  <done>e2e proves the structural-findings section updates on a JS decide without a round-trip; full cut-lab e2e suite green.</done>
</task>

<task type="auto">
  <name>Task 4: End-of-phase full-suite green gate (all six items landed)</name>
  <read_first>
    - .planning/workstreams/cut-lab/phases/107-cut-lab-tech-debt-cleanup/107-VALIDATION.md (the phase
      validation contract — full-suite commands + sign-off).
    - CLAUDE.md constraints: LF endings enforced; compiled `wwwroot/js/*.js` is gitignored (never
      staged); UI testing must use `scripts/run-web-test.sh` (no Windows-host browser).
  </read_first>
  <action>
    Run the phase-wide gate proving the ROADMAP success criterion ("each item fixed or closed-with-reason;
    full suite + e2e green; no behavior regression") after all four plans' work is on the branch:
    (1) `dotnet test DeckFlow.sln` (full) — green.
    (2) TypeScript compile + full Vitest: `cd DeckFlow.Web && npx --no-install tsc -p tsconfig.json
        --noEmit && npx --no-install vitest run` — green.
    (3) Full Playwright e2e: `scripts/run-web-test.sh` then `npx --no-install playwright test` — all
        cut-lab specs green.
    (4) Hygiene: `git diff --check` (no whitespace errors/conflict markers); EOL-churn check — compare
        `git diff --stat` vs `git diff --ignore-all-space --stat` (gap must be small; re-normalize any
        file whose committed EOL was flipped); and confirm NO compiled `wwwroot/js/*.js` is staged
        (`git diff --cached --name-only | grep -E 'wwwroot/js/.*\.js$'` returns nothing).
    Record the results (and the closed-with-reason items: 4a xmldoc, 4b Manabase copy, adjust-path
    findings gap, adjust-path chip staleness) in the SUMMARY. If any suite is red or hygiene fails,
    STOP and surface — do not mark the phase done.
  </action>
  <acceptance_criteria>
    - `dotnet test DeckFlow.sln` full run passes (0 failed).
    - `npx --no-install tsc -p DeckFlow.Web/tsconfig.json --noEmit` clean; `npx --no-install vitest run` passes.
    - `npx --no-install playwright test` cut-lab specs all pass (headless).
    - `git diff --check` clean; `git diff --cached --name-only | grep -E 'wwwroot/js/.*\.js$'` returns nothing.
    - EOL check: `git diff --stat` and `git diff --ignore-all-space --stat` line counts do not diverge
      by more than trivial (no whole-file EOL churn).
  </acceptance_criteria>
  <verify>
    <automated>dotnet test DeckFlow.sln && cd DeckFlow.Web && npx --no-install tsc -p tsconfig.json --noEmit && npx --no-install vitest run</automated>
  </verify>
  <done>Full dotnet + TS/Vitest + Playwright suites green; no EOL churn; no compiled js staged; all six items recorded fixed-or-closed; phase gate satisfied.</done>
</task>

</tasks>

<threat_model>
No new external attack surface. Item 6 adds server-computed OUTPUT fields to an existing,
same-origin-guarded decide endpoint (CutLabApiController.cs:47-50) — no new request fields, no new
endpoint, no new deserialization. The one live-DOM concern: the client renderer must write response
values via textContent / typed element creation, NEVER innerHTML with raw data — even though the
values are already server-validated, this keeps the injection surface at zero (enforced by Task 2
acceptance). Adjust endpoint is intentionally unchanged (documented gap).
</threat_model>

<verification>
- `dotnet build DeckFlow.sln` clean; CutLabApiControllerTests green (StructuralFindings serialized).
- TS compiles; Vitest findings-renderer test green (section heading survives a rebuild).
- Playwright cut-lab-structure.spec.ts proves live-patch without reload; full cut-lab e2e green.
- Server-render `Model.Findings`/`Model.FindingGroups` no-JS path unchanged (additive-only diff).
- Grouping is single-sourced (no WeakFloorCase merge logic in cut-lab.ts).
- Task 4 phase gate: full dotnet + Vitest + Playwright green; no EOL churn; no compiled wwwroot/js staged.
</verification>

<success_criteria>
Structural findings live-patch on JS decide (no round-trip, body-scoped renderer), server/no-JS
fallback preserved, grouping single-sourced server-side, renderer injection-safe, e2e+unit+controller
coverage green, and the end-of-phase full-suite gate passes.
</success_criteria>

<output>
Create `.planning/workstreams/cut-lab/phases/107-cut-lab-tech-debt-cleanup/107-04-SUMMARY.md` when done
(record the accepted decide-only scope, the unchanged adjust-path findings gap, and the Task 4 gate results).
</output>
