---
phase: 29-core-xml-doc-backfill-gate-widen
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - DeckFlow.Core/Storage/IRelationalDialect.cs
  - DeckFlow.Core/Storage/PostgresRelationalDialect.cs
  - DeckFlow.Core/Storage/SqliteRelationalDialect.cs
  - DeckFlow.Core/Storage/RelationalDatabaseConnection.cs
autonomous: true
requirements: [HSK-01]

must_haves:
  truths:
    - "Probe build over DeckFlow.Core/Storage/*.cs reports 0 CS1591/CS1573/CS1587 warnings"
    - "Every public type and member in the 4 Storage files has an XML doc comment"
    - "No raw-string literal content or indentation changed in PostgresRelationalDialect.cs or SqliteRelationalDialect.cs"
  artifacts:
    - path: "DeckFlow.Core/Storage/IRelationalDialect.cs"
      provides: "Documented relational-dialect interface (3 missing property docs added)"
      contains: "/// <summary>"
    - path: "DeckFlow.Core/Storage/PostgresRelationalDialect.cs"
      provides: "Documented Postgres dialect impl (5 sites)"
      contains: "/// <summary>"
    - path: "DeckFlow.Core/Storage/SqliteRelationalDialect.cs"
      provides: "Documented SQLite dialect impl (5 sites)"
      contains: "/// <summary>"
    - path: "DeckFlow.Core/Storage/RelationalDatabaseConnection.cs"
      provides: "Documented connection handle (8 sites)"
      contains: "/// <summary>"
  key_links:
    - from: "DeckFlow.Core/Storage/PostgresRelationalDialect.cs"
      to: "DeckFlow.Core/Storage/IRelationalDialect.cs"
      via: "inheritdoc on interface-implementing members"
      pattern: "inheritdoc"
---

<objective>
Add XML doc comments to every undocumented public type and member in the `DeckFlow.Core/Storage/` folder (4 files, ~21 compiler-reported sites: IRelationalDialect 3, PostgresRelationalDialect 5, SqliteRelationalDialect 5, RelationalDatabaseConnection 8).

Purpose: Storage is the highest-density folder of the Core doc backfill (HSK-01) and contains two LOW-risk raw-string files. Documenting it cleanly is a prerequisite for the Wave-2 `.editorconfig` gate widen, which must build green.
Output: 4 fully-documented Storage source files; probe build over Storage reports 0 doc warnings.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<scope_fence>
ALLOWED FILE SET (Codex MUST NOT touch any source file outside this list):
- DeckFlow.Core/Storage/IRelationalDialect.cs
- DeckFlow.Core/Storage/PostgresRelationalDialect.cs
- DeckFlow.Core/Storage/SqliteRelationalDialect.cs
- DeckFlow.Core/Storage/RelationalDatabaseConnection.cs

ALLOWED ARTIFACT (workflow output, exempt from the source-edit fence):
- .planning/phases/29-core-xml-doc-backfill-gate-widen/29-01-SUMMARY.md

PARALLEL-EXECUTION REALITY: GSD execute-phase runs all four Wave-1 plans (29-01..29-04) as parallel subagents in ONE shared worktree. Your three sibling plans will be editing their own Core folders concurrently. Design for that:
- RELAXED CLEAN-TREE PRECONDITION: before starting, run `git status --short`. Modifications confined to the Wave-1 sibling allowed source sets (`DeckFlow.Core/Reporting/`, `DeckFlow.Core/Filtering/`, `DeckFlow.Core/Knowledge/`, `DeckFlow.Core/Integration/`, `DeckFlow.Core/Exporting/`, `DeckFlow.Core/Parsing/`, `DeckFlow.Core/Models/`, `DeckFlow.Core/Normalization/`, `DeckFlow.Core/Diffing/`) and `.planning/phases/29-core-xml-doc-backfill-gate-widen/29-0*-SUMMARY.md` are EXPECTED — ignore them. STOP and report ONLY if a modified/untracked file appears outside BOTH this plan's allowed set AND every Wave-1 sibling allowed set (e.g., `DeckFlow.Web/`, `DeckFlow.CLI/`, repo-root files). Do NOT touch any file owned by a sibling plan.

PROBE-FILE LOCK (`DeckFlow.Core/.editorconfig` is an EXCLUSIVE lock SHARED by all four Wave-1 plans, guarded by an ATOMIC sidecar lock dir):
- The probe file MUST be named exactly `DeckFlow.Core/.editorconfig` — only that name overrides Roslyn severity at the `DeckFlow.Core/` directory, so per-plan unique filenames do NOT work; collision is handled by mutual exclusion, not renaming.
- ATOMIC ACQUIRE (a `[ -e ]`-then-`printf` test is NOT atomic — two parallel agents can both observe "absent" and both clobber the probe). Use `mkdir DeckFlow.Core/.editorconfig.probe.lock` as the mutex: `mkdir` is atomic and succeeds for exactly one contender. Poll every 3s, give up after 120s (40 attempts). On timeout, treat the lock as stale ONLY if `DeckFlow.Core/.editorconfig` is absent OR older than ~5 minutes (`find DeckFlow.Core/.editorconfig -mmin +5`); in that case remove the stale lock dir + stale probe file and retry the mkdir ONCE; otherwise STOP with a clear error.
- WRITE/REMOVE the `DeckFlow.Core/.editorconfig` probe file ONLY while holding the lock dir.
- TRAP CLEANUP: wrap the whole locked probe section with `trap 'rm -f DeckFlow.Core/.editorconfig; rmdir DeckFlow.Core/.editorconfig.probe.lock 2>/dev/null || true' EXIT INT TERM` so an interrupted/crashed build never leaves the probe file or lock dir behind to falsify a sibling's probe.
- HOLD MINIMALLY: acquire lock → write probe → `rm -rf DeckFlow.Core/obj DeckFlow.Core/bin` → build (capture exit code) → capture/dedup warnings → IMMEDIATELY remove probe file → `rmdir` the lock dir → clear the trap (`trap - EXIT INT TERM`). Never hold the lock across a doc-editing step.
- RELEASE: confirm `DeckFlow.Core/.editorconfig` AND `DeckFlow.Core/.editorconfig.probe.lock` are gone; neither may be committed nor appear in `git status` at plan end.
Do NOT touch `.editorconfig` at repo root. Do NOT touch any other Core folder's source. Ignore tasks belonging to plans 29-02, 29-03, 29-04, 29-05.
</scope_fence>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-core-xml-doc-backfill-gate-widen/29-RESEARCH.md
@.planning/phases/29-core-xml-doc-backfill-gate-widen/29-PATTERNS.md

# Gold-standard doc-style exemplars (READ - match this style exactly):
@DeckFlow.Core/Content/IContentVideoStore.cs
@DeckFlow.Core/Content/ContentVideoStore.cs
</context>

<constraints>
From CLAUDE.md + 29-RESEARCH.md (full authority - every constraint is blocking):
- Touch ONLY the `///` doc lines you add. Do NOT run Format Document / Code Cleanup / ReSharper reformat.
- NEVER convert `{ get; init; }` to `{ get; }` (System.Text.Json drops get-only props in .NET 9+). None of these files contain init accessors, but the rule stands.
- NEVER re-indent C# raw-string literals (triple-quote). PostgresRelationalDialect.cs and SqliteRelationalDialect.cs each contain 2 SQL column-type raw strings - doc `///` lines go ABOVE member declarations only, never inside a raw-string block.
- NEVER inline an attribute onto a declaration line.
- Preserve LF line endings (.gitattributes enforces).
- Build env: `"/mnt/c/Program Files/dotnet/dotnet.exe"`. VSTest unreliable in WSL - clean build is the gate.
- Every authoritative probe build MUST first remove `DeckFlow.Core/obj` and `DeckFlow.Core/bin` so the inventory is trustworthy (stale obj/ hides warnings).
- Commits: plain default-author (no Co-Authored-By trailer); one logical change per commit.
</constraints>

<tasks>

<task type="auto">
  <name>Task 1: Document IRelationalDialect plus the two dialect implementations</name>
  <files>DeckFlow.Core/Storage/IRelationalDialect.cs, DeckFlow.Core/Storage/PostgresRelationalDialect.cs, DeckFlow.Core/Storage/SqliteRelationalDialect.cs</files>
  <read_first>
    - DeckFlow.Core/Storage/IRelationalDialect.cs (current state - one property already documented, 3 bare)
    - DeckFlow.Core/Storage/PostgresRelationalDialect.cs (current state - note the 2 SQL raw-string column-type values)
    - DeckFlow.Core/Storage/SqliteRelationalDialect.cs (current state - note the 2 SQL raw-string column-type values)
    - .planning/phases/29-core-xml-doc-backfill-gate-widen/29-PATTERNS.md (Pattern 8 IRelationalDialect property docs; Pattern 2 inheritdoc; one-line form)
    - DeckFlow.Core/Content/IContentVideoStore.cs (interface property/method doc style)
    - .planning/phases/29-core-xml-doc-backfill-gate-widen/29-RESEARCH.md (Per-file counts; Risk Files - the RESEARCH counts IRelationalDialect 3 / Postgres 5 / Sqlite 5 are the expected starting point but the final probe in Task 2 is authoritative)
  </read_first>
  <action>
    For `IRelationalDialect.cs`: add a single-line `/// <summary>` to each of the 3 undocumented interface properties - `FeedbackCreatedUtcColumnType`, `FeedbackOrderByClause`, `FeedbackInsertReturningIdSql` (concrete summaries per 29-PATTERNS.md Pattern 8). Do NOT rewrite the already-documented `SurrogateIdColumnType` doc. For `PostgresRelationalDialect.cs` and `SqliteRelationalDialect.cs`: each has 5 undocumented sites (the class type plus its members implementing IRelationalDialect). Add a multi-line `<summary>` on each class type describing it as the Postgres/SQLite implementation of `IRelationalDialect`, and add `/// <inheritdoc />` (with the space before the close) above each interface-implementing member rather than repeating the interface summary. The `///` lines go ABOVE the property/member declaration line; never place a `///` line inside a raw-string block, and do not re-indent those literals.
  </action>
  <verify>
    <automated>grep -c "inheritdoc" DeckFlow.Core/Storage/PostgresRelationalDialect.cs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "/// <summary>" DeckFlow.Core/Storage/IRelationalDialect.cs` is at least 4 (1 prior plus 3 new).
    - Both dialect files contain `inheritdoc` on members and a class-level `<summary>`.
    - `git diff` for both dialect files shows only added `///` lines - no changes to any line containing a triple-quote delimiter or to lines between delimiters.
    - No `{ get; }` appears where `{ get; init; }` previously existed (none expected).
  </acceptance_criteria>
  <done>IRelationalDialect and both dialect implementations are fully documented; raw strings untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Document RelationalDatabaseConnection and probe-verify the Storage folder is clean</name>
  <files>DeckFlow.Core/Storage/RelationalDatabaseConnection.cs</files>
  <read_first>
    - DeckFlow.Core/Storage/RelationalDatabaseConnection.cs (current state - 8 sites; some members may already be partially documented)
    - .planning/phases/29-core-xml-doc-backfill-gate-widen/29-PATTERNS.md (Pattern 3 constructor docs; Pattern 1 method docs; shared patterns for paramref / see cref / see langword null)
    - DeckFlow.Core/Content/ContentVideoStore.cs (constructor + factory + method doc style, including paramref usage)
    - .planning/phases/29-core-xml-doc-backfill-gate-widen/29-RESEARCH.md (Probe build methodology - temp DeckFlow.Core/.editorconfig override + clean obj/ + sort -u dedup)
  </read_first>
  <action>
    Add XML doc comments to the 8 undocumented public sites in `RelationalDatabaseConnection.cs` (RESEARCH per-file count is the expected starting point; the probe below is authoritative if it drifted). Use: multi-line `<summary>` on the type if undocumented; constructor summaries starting with "Creates" or "Initializes"; factory-method summaries (e.g., `FromSqlitePath`) using `<paramref name="..."/>` for inline param references and `<param>` for each parameter; `<returns>` only on members that return a value, with `<see langword="null"/>` phrasing for nullable returns. Match the exemplar style in ContentVideoStore.cs exactly (imperative mood, no trailing period on `<param>` noun phrases, no blank line between the `///` block and the declaration). Then run the AUTHORITATIVE Storage probe via the verify snippet: it ATOMICALLY acquires the `DeckFlow.Core/.editorconfig` lock (mkdir sidecar lock dir, 120s cap, stale recovery), installs a `trap` so an interrupt cannot leave the probe/lock behind, drops obj/bin, builds, EXPLICITLY checks the dotnet build exit code (fail before interpreting warnings if the build itself failed), filters the deduped warnings to `Storage/` only (a sibling's incomplete folder must never fail this plan), then releases the lock and clears the trap. Confirm ZERO CS1591/CS1573/CS1587 warnings remain for any file under `DeckFlow.Core/Storage/`.
  </action>
  <verify>
    <automated>DOTNET="/mnt/c/Program Files/dotnet/dotnet.exe"; LOCK=DeckFlow.Core/.editorconfig.probe.lock; PROBE=DeckFlow.Core/.editorconfig; trap 'rm -f "$PROBE"; rmdir "$LOCK" 2>/dev/null || true' EXIT INT TERM; n=0; until mkdir "$LOCK" 2>/dev/null; do n=$((n+1)); if [ "$n" -ge 40 ]; then if [ ! -e "$PROBE" ] || [ -n "$(find "$PROBE" -mmin +5 2>/dev/null)" ]; then echo "stale lock - recovering"; rm -f "$PROBE"; rmdir "$LOCK" 2>/dev/null || true; mkdir "$LOCK" 2>/dev/null && break; fi; echo "FAIL could not acquire probe lock after 120s"; exit 1; fi; sleep 3; done; printf '%s\n' 'root = false' '' '[*.cs]' 'dotnet_diagnostic.CS1591.severity = warning' 'dotnet_diagnostic.CS1573.severity = warning' 'dotnet_diagnostic.CS1587.severity = warning' > "$PROBE"; rm -rf DeckFlow.Core/obj DeckFlow.Core/bin; "$DOTNET" build DeckFlow.Core/DeckFlow.Core.csproj --no-incremental 2>&1 | tee /tmp/29-01-build.log >/dev/null; BUILD_RC=${PIPESTATUS[0]}; rm -f "$PROBE"; rmdir "$LOCK" 2>/dev/null || true; trap - EXIT INT TERM; if [ "$BUILD_RC" -ne 0 ]; then echo "FAIL Core probe build errored (rc=$BUILD_RC) before warning analysis:"; grep -E "error CS" /tmp/29-01-build.log | head; exit 1; fi; grep -E "warning CS(1591|1573|1587)" /tmp/29-01-build.log | grep "Storage/" | sort -u > /tmp/29-01-storage.log; if [ -s /tmp/29-01-storage.log ]; then echo FAIL; cat /tmp/29-01-storage.log; else echo "OK 0 Storage doc warnings"; fi</automated>
  </verify>
  <acceptance_criteria>
    - Probe build (atomic-locked temp editorconfig override, clean obj/, build-exit-code checked) yields 0 CS1591/CS1573/CS1587 warnings for any `DeckFlow.Core/Storage/` file (`/tmp/29-01-storage.log` empty) and a passing dotnet build exit code.
    - `DeckFlow.Core/.editorconfig` and `DeckFlow.Core/.editorconfig.probe.lock` removed; `git status --short` does not list either.
    - `git diff --stat DeckFlow.Core/Storage/` lists exactly and only the 4 Storage files (run scoped to the Storage path so concurrent sibling edits do not appear).
    - `"/mnt/c/Program Files/dotnet/dotnet.exe" build DeckFlow.Core/DeckFlow.Core.csproj -c Release` reports 0 errors (suppressor still active - sanity build).
  </acceptance_criteria>
  <done>All 4 Storage files documented; Storage probe shows 0 doc warnings; temp probe file and lock dir removed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer edit -> source tree | Mass `///` insertions across SQL-bearing files risk accidental code/literal mutation |
| temp probe file -> repo | A lingering `DeckFlow.Core/.editorconfig` would alter solution-wide warning severity for everyone |
| concurrent Wave-1 subagents -> shared worktree | Two plans creating/deleting `DeckFlow.Core/.editorconfig` simultaneously corrupts the other's probe |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-01-01 | Tampering | Raw-string SQL in Postgres/Sqlite dialect files | mitigate | `///` lines added above declarations only; git diff acceptance asserts no triple-quote line changed |
| T-29-01-02 | Tampering | Temp probe `DeckFlow.Core/.editorconfig` | mitigate | Atomic mkdir-lock acquire/release + EXIT/INT/TERM trap; probe file removed and absence asserted; never committed |
| T-29-01-03 | Tampering | Concurrent sibling probe collision | mitigate | Atomic mkdir sidecar lock guarantees single writer; 120s cap with stale recovery; folder-scoped warning filter ignores sibling progress |
| T-29-01-04 | Denial of Service | Build pipeline | accept | Doc comments are compile-stripped; zero runtime/behavior change possible |
| T-29-01-SC | Tampering | npm/pip/cargo installs | n/a | No package installs in this phase |
</threat_model>

<verification>
- Probe build (atomic-locked temp editorconfig, clean obj/, build-exit-code checked) reports 0 CS1591/CS1573/CS1587 for `DeckFlow.Core/Storage/`.
- `git status --short` shows no `DeckFlow.Core/.editorconfig` nor `.editorconfig.probe.lock`.
- `git diff --stat DeckFlow.Core/Storage/` lists only the 4 allowed Storage files.
- Full Core build clean (0 errors) under the still-active global suppressor.
</verification>

<success_criteria>
All 21 undocumented public sites in the 4 Storage files carry XML doc comments matching the Content KB exemplar style; Storage-scoped probe build is warning-free; no raw-string literals or init accessors mutated; only allowed files changed.
</success_criteria>

<output>
Create `.planning/phases/29-core-xml-doc-backfill-gate-widen/29-01-SUMMARY.md` when done.
</output>
