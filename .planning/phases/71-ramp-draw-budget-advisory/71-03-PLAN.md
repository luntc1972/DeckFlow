---
phase: 71-ramp-draw-budget-advisory
plan: 03
type: execute
wave: 3
depends_on: ["71-01", "71-02"]
files_modified:
  - DeckFlow.Web/Services/FeatureFlags/FeatureFlagStore.cs
  - DeckFlow.Web/Services/FeatureFlags/FeatureFlagCatalog.cs
  - DeckFlow.Web/Services/Manabase/ManabaseAnalysisService.cs
  - DeckFlow.Web/Controllers/ManabaseController.cs
  - DeckFlow.Web/Models/ManabaseViewModel.cs
  - DeckFlow.Web/Models/ManabaseDisplay.cs
  - DeckFlow.Web.Tests/FeatureFlagStoreSeedTests.cs
  - DeckFlow.Web.Tests/FeatureFlagCatalogTests.cs
  - DeckFlow.Web.Tests/Manabase/ManabaseAnalysisServiceTests.cs
  - DeckFlow.Web.Tests/Manabase/ManabaseControllerModeTests.cs
  - DeckFlow.Web.Tests/Manabase/ManabaseControllerDownloadTests.cs
  - DeckFlow.Web.Tests/Manabase/ManabaseDisplayTests.cs
  - DeckFlow.Web.Tests/Manabase/ManabaseViewModelTests.cs
autonomous: true
requirements: [PLV-FLAG, PLV-GLOSS, PLV-VERDICT, PLV-BUDGET]
must_haves:
  truths:
    - "manabase.plain-language-verdict is seeded OFF in both SQLite and Postgres and read fail-safe via Snapshot().TryGetValue"
    - "Flag ON computes the verdict (Casual) + ramp/draw budget (Casual only) and threads them to the view model (via the controller) and prompt; flag OFF leaves them null and the prompt byte-identical"
    - "The analyze AND download controller actions copy Verdict/Budget/ShowPlainLanguage onto the view model / into the text builder, each guarded by a controller test"
    - "Metric glosses are available as testable display strings"
  artifacts:
    - path: "DeckFlow.Web/Controllers/ManabaseController.cs"
      provides: "analyze-action VM carries the verdict/budget; download passes them into the text builder"
      contains: "ShowPlainLanguage"
    - path: "DeckFlow.Web/Models/ManabaseViewModel.cs"
      provides: "PlainLanguageVerdict + RampDrawBudget + ShowPlainLanguage"
      contains: "ShowPlainLanguage"
    - path: "DeckFlow.Web/Services/FeatureFlags/FeatureFlagCatalog.cs"
      provides: "operator description for the new flag"
      contains: "manabase.plain-language-verdict"
  key_links:
    - from: "ManabaseAnalysisService.AnalyzeAsync"
      to: "ManabaseVerdictSynthesizer / ManabaseRampDrawBudgetCalculator"
      via: "computed only when the flag is on (budget Casual-only)"
      pattern: "plain-language-verdict"
    - from: "ManabaseController.Manabase(POST)"
      to: "ManabaseViewModel.PlainLanguageVerdict / RampDrawBudget / ShowPlainLanguage"
      via: "explicit field copy in the VM construction (~L88-97)"
      pattern: "PlainLanguageVerdict = result.Verdict"
    - from: "ManabaseController.Download(POST)"
      to: "ManabaseReportTextBuilder.Build(..., result.Verdict, result.Budget)"
      via: "verdict/budget passed into the .txt build (~L122)"
      pattern: "result.Verdict, result.Budget"
---

<objective>
Wire the `manabase.plain-language-verdict` flag end-to-end (server side): seed it OFF in both
dialects with an operator description, read it fail-safe, and - when ON - compute the verdict and
(Casual-only) ramp/draw budget, threading them through the service result, the CONTROLLER's view-model
construction, the augmented prompt, AND the .txt download. Add the metric glosses as testable strings.

Purpose: Deliverable D plumbing + Deliverable A copy (glosses are UI-ONLY by SPEC decision - they are
NOT mirrored into the prompt builder; only the verdict + budget are). The controller copies result
fields onto the VM explicitly (it does not auto-map), so the new fields MUST be set there or the
on-page UI renders nothing, and the Download text site (which drifted before) MUST be both wired and
test-guarded.
Output: seeded flag, catalog entry, service wiring, controller VM/download wiring, view-model fields,
gloss strings, Web tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/71-ramp-draw-budget-advisory/71-CONTEXT.md
@.planning/phases/71-ramp-draw-budget-advisory/71-01-SUMMARY.md
@.planning/phases/71-ramp-draw-budget-advisory/71-02-SUMMARY.md

<interfaces>
<!-- The fail-safe flag pattern this MUST copy (NOT IFeatureFlagCache.IsEnabled). -->

From ManabaseAnalysisService.cs:
- public const string RampCreditV2FlagKey = "manabase.ramp-credit-v2"; (declare a sibling const for
  the new key alongside these).
- private bool IsFlagOn(string key) => _featureFlags is {} flags &&
  flags.Snapshot().TryGetValue(key, out bool enabled) && enabled;  // fail-safe OFF - reuse this.
- AnalyzeAsync builds: report = ManabaseAnalyzer.Analyze(...); swapPrompt =
  ManabaseSwapPromptBuilder.Build(report, deckName, resolved.DecklistText, options.Mode);
  returns new ManabaseAnalysisResult(report, InputSummary, Unresolved, FallbackNotice, swapPrompt, Suggestions).
  resolved.Deck is the ManabaseDeck (has the 71-01 bucket fields).

From ManabaseController.cs (the controller copies fields EXPLICITLY — no auto-map):
- Manabase(POST) action ~L88-97 constructs `new ManabaseViewModel { Request, Report = result.Report,
  InputSummary, Unresolved, ImportWarning, ChatGptSwapPrompt = result.ChatGptSwapPrompt, Suggestions }`.
- Download(POST) action ~L122 calls `ManabaseReportTextBuilder.Build(result.Report, request.DeckName,
  decklistText: null, request.Mode)` and returns a File(...) with the UTF-8 bytes.
- RunAnalysisAsync(request, token) returns the ManabaseAnalysisResult both actions share.
- Existing test ManabaseControllerDownloadTests already exercises the Download action — extend it.

From FeatureFlagStore.cs: PostgresSeedSql + SqliteSeedSql (add the new key, default FALSE / 0, BEFORE
the ON CONFLICT line); seeds use `('key', FALSE)` / `('key', 0)`.
From FeatureFlagCatalog.cs: Descriptions dictionary (add a one-line entry).
From 71-01: ManabaseRampDrawBudget, ManabaseRampDrawBudgetCalculator.Calculate(deck).
From 71-02: ManabaseVerdict, ManabaseVerdictSynthesizer.Synthesize(report, mode, budget?),
  ManabaseSwapPromptBuilder.Build(..., verdict, budget), ManabaseReportTextBuilder.Build(..., verdict, budget).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Seed the flag OFF (both dialects) + catalog description + seed/catalog tests</name>
  <files>DeckFlow.Web/Services/FeatureFlags/FeatureFlagStore.cs, DeckFlow.Web/Services/FeatureFlags/FeatureFlagCatalog.cs, DeckFlow.Web.Tests/FeatureFlagStoreSeedTests.cs, DeckFlow.Web.Tests/FeatureFlagCatalogTests.cs</files>
  <action>Add `('manabase.plain-language-verdict', FALSE)` to PostgresSeedSql and
    `('manabase.plain-language-verdict', 0)` to SqliteSeedSql (before the ON CONFLICT clause, matching
    the existing formatting). Add a FeatureFlagCatalog.Descriptions entry, e.g. "Show a plain-language
    'Reading your deck' verdict, friendly one-line explanations for each manabase metric, and (Casual
    only) a ramp vs. draw slot-budget advisory. Off by default; recommendations are heuristic and never
    change the land count, color counts, castability, or health rating." Add an InlineData row
    ("manabase.plain-language-verdict", false) to FeatureFlagStoreSeedTests and an InlineData row
    to FeatureFlagCatalogTests so both guards cover the new key.</action>
  <verify>
    <automated>cd /mnt/c/users/chrislunt/source/personal/deckflow && cmd.exe /c "dotnet test DeckFlow.Web.Tests\DeckFlow.Web.Tests.csproj --filter FullyQualifiedName~FeatureFlag" 2>&1 | tail -20</automated>
  </verify>
  <done>The flag seeds OFF in both dialects (ON CONFLICT preserves operator values), has a non-empty
    catalog description, and both flag guard tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Service + result/view-model fields + controller VM &amp; download wiring + tests</name>
  <files>DeckFlow.Web/Services/Manabase/ManabaseAnalysisService.cs, DeckFlow.Web/Controllers/ManabaseController.cs, DeckFlow.Web/Models/ManabaseViewModel.cs, DeckFlow.Web.Tests/Manabase/ManabaseAnalysisServiceTests.cs, DeckFlow.Web.Tests/Manabase/ManabaseControllerModeTests.cs, DeckFlow.Web.Tests/Manabase/ManabaseControllerDownloadTests.cs, DeckFlow.Web.Tests/Manabase/ManabaseViewModelTests.cs</files>
  <action>In ManabaseAnalysisService: declare `public const string PlainLanguageVerdictFlagKey =
    "manabase.plain-language-verdict";`. In AnalyzeAsync, after the report is built, read
    `bool plainLanguage = IsFlagOn(PlainLanguageVerdictFlagKey);`. When ON: compute
    `ManabaseRampDrawBudget? budget = options.Mode == ManabaseMode.Casual ?
    ManabaseRampDrawBudgetCalculator.Calculate(resolved.Deck) : null;` and
    `ManabaseVerdict? verdict = options.Mode == ManabaseMode.Casual ?
    ManabaseVerdictSynthesizer.Synthesize(report, options.Mode, budget) : null;` (planner decision:
    the "Reading your deck" verdict and the budget are Casual-only; cEDH gets glosses only - matches
    the SPEC done-when). Pass `verdict` and `budget` into the augmented ManabaseSwapPromptBuilder.Build
    call. When OFF: budget=null, verdict=null, and call Build with the existing 4-arg form so the prompt
    is byte-identical. Extend the `ManabaseAnalysisResult` record with `ManabaseVerdict? Verdict`,
    `ManabaseRampDrawBudget? Budget`, and `bool ShowPlainLanguage` (the flag value, so the view can gate
    glosses in BOTH modes). Add matching `PlainLanguageVerdict`, `RampDrawBudget`, and `ShowPlainLanguage`
    init properties to ManabaseViewModel (defaults null/false).
    In ManabaseController: the controller copies result fields onto the VM EXPLICITLY, so update BOTH
    call sites. (a) Analyze action (~L88-97): in the `new ManabaseViewModel { ... }` set
    `PlainLanguageVerdict = result.Verdict, RampDrawBudget = result.Budget,
    ShowPlainLanguage = result.ShowPlainLanguage`. (b) Download action (~L122): pass the verdict + budget
    into `ManabaseReportTextBuilder.Build(result.Report, request.DeckName, decklistText: null,
    request.Mode, result.Verdict, result.Budget)` so the .txt mirrors the page. (The Load action is
    unchanged — no report there.)
    Tests: ManabaseAnalysisServiceTests - flag OFF -> result.Verdict null, result.Budget null,
    ShowPlainLanguage false, prompt byte-identical to the no-verdict build; flag ON + Casual ->
    Verdict non-null, Budget non-null, prompt contains "Reading your deck"; flag ON + cEDH ->
    ShowPlainLanguage true but Budget null and Verdict null (use FakeFeatureFlagCache to toggle the key).
    ManabaseControllerModeTests (extend or add) - invoke the analyze action with a stub
    IManabaseAnalysisService returning a result whose Verdict/Budget/ShowPlainLanguage are set, and
    assert the returned ViewResult's ManabaseViewModel has PlainLanguageVerdict / RampDrawBudget /
    ShowPlainLanguage populated (guards the controller copy the BLOCKER flagged).
    ManabaseControllerDownloadTests (extend) - invoke the Download action with a stub
    IManabaseAnalysisService returning a result whose Verdict has a known issue Line and a non-null
    Budget; read the FileContentResult bytes back to a string and assert the .txt contains the verdict
    text ("Reading your deck" / the issue Line) AND the ramp/draw budget line (guards the Download
    drift the MED finding flagged). ManabaseViewModelTests - the new properties round-trip and default
    to null/false.</action>
  <verify>
    <automated>cd /mnt/c/users/chrislunt/source/personal/deckflow && cmd.exe /c "dotnet test DeckFlow.Web.Tests\DeckFlow.Web.Tests.csproj --filter FullyQualifiedName~ManabaseAnalysisService|FullyQualifiedName~ManabaseControllerMode|FullyQualifiedName~ManabaseControllerDownload|FullyQualifiedName~ManabaseViewModel" 2>&1 | tail -25</automated>
  </verify>
  <done>Flag ON (Casual) threads verdict + budget into the result, the controller-built view model,
    the prompt, and the .txt download (the latter proven by a Download controller test); flag ON (cEDH)
    suppresses budget/verdict but keeps ShowPlainLanguage; flag OFF is byte-identical - all green.</done>
</task>

<task type="auto">
  <name>Task 3: Metric gloss strings + display tests</name>
  <files>DeckFlow.Web/Models/ManabaseDisplay.cs, DeckFlow.Web.Tests/Manabase/ManabaseDisplayTests.cs</files>
  <action>Add static gloss helpers/constants to ManabaseDisplay for each surfaced metric (one-line,
    plain): KarstenSourceGloss = "Enough lands/rocks of each color to reliably have that color when
    you need it. 'need -3' means about 3 short."; CastRateGloss = "Across simulated games, how often
    your spells are castable on or before their ideal turn. Higher = smoother."; WeakestColorGloss
    (short); DemandingCardsGloss (short). These are UI-ONLY (per SPEC: glosses are not mirrored into the
    prompt builder). Keep them as `public const string` (or static readonly) so ManabaseDisplayTests can
    assert non-empty, contain the key phrasing, and avoid em/en dashes (use plain hyphens). Tests:
    assert each gloss is non-empty and contains its anchor phrase.</action>
  <verify>
    <automated>cd /mnt/c/users/chrislunt/source/personal/deckflow && cmd.exe /c "dotnet test DeckFlow.Web.Tests\DeckFlow.Web.Tests.csproj --filter FullyQualifiedName~ManabaseDisplay" 2>&1 | tail -20</automated>
  </verify>
  <done>Gloss strings exist as testable display constants (UI-only) and the display tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| operator -> /Admin/Flags -> feature_flags table | the new flag is toggled by an authenticated operator (existing BasicAuth gate); no new endpoint added. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-71-04 | Tampering | New flag default | mitigate | Seeded OFF both dialects + read fail-safe via Snapshot().TryGetValue (NOT IsEnabled), so a missing/unseeded key never enables the feature. Guarded by FeatureFlagStoreSeedTests. |
| T-71-05 | Elevation of privilege | Flag toggling | accept | Reuses the existing /Admin BasicAuth gate; no new surface. |
| T-71-SC | Tampering | npm/pip/cargo installs | n/a | No package installs (no new deps). |
</threat_model>

<verification>
- `dotnet build DeckFlow.Web` + `DeckFlow.Web.Tests` clean.
- Flag OFF -> prompt + result byte-identical (asserted); flag ON Casual -> verdict+budget present on
  the controller-built VM and in the .txt download; cEDH -> glosses gate on (ShowPlainLanguage) but
  budget/verdict suppressed.
- Controller tests prove BOTH sites: the analyze-action VM carries PlainLanguageVerdict/RampDrawBudget/
  ShowPlainLanguage, AND the Download .txt contains the verdict + ramp/draw budget block.
</verification>

<success_criteria>
The flag is seeded OFF in both dialects with a description, read fail-safe, and gates server-side
computation of the verdict (Casual), budget (Casual), the augmented prompt, AND the .txt download;
both controller copy sites are test-guarded; glosses exist as testable UI-only strings; flag-OFF
stays byte-identical.
</success_criteria>

<output>
Create `.planning/phases/71-ramp-draw-budget-advisory/71-03-SUMMARY.md` when done.
</output>
