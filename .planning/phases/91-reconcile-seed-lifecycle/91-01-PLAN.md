---
phase: 91-reconcile-seed-lifecycle
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - DeckFlow.Core/Knowledge/ContentArtifactSpec.cs
  - DeckFlow.Core/Content/ContentSiteIndexStore.cs
  - DeckFlow.Core/Content/SeedIndexFileReader.cs
  - DeckFlow.Core.Tests/Content/SeedManagedSchemaTests.cs
  - DeckFlow.Core.Tests/Content/SeedManagedWritePathTests.cs
  - DeckFlow.Core.Tests/Content/SeedIndexFileReaderTests.cs
autonomous: true
requirements: [SYNC-17]

must_haves:
  truths:
    - "content_site_index carries a seed_managed marker column on both SQLite and Postgres, added idempotently"
    - "A row already classified (seed_managed IS NOT NULL) is never overwritten by the null-only backfill setter"
    - "The seed_managed value is a per-row bound parameter, never a hardcoded SQL literal"
    - "index-seed.json natural-key membership is readable via one shared Core reader"
  artifacts:
    - path: "DeckFlow.Core/Knowledge/ContentArtifactSpec.cs"
      provides: "ContentSiteIndexRow.SeedManaged nullable property"
      contains: "SeedManaged"
    - path: "DeckFlow.Core/Content/ContentSiteIndexStore.cs"
      provides: "seed_managed DDL, create-table, 5 SELECTs, mapping, upsert threading, SetSeedManagedIfNullAsync"
      contains: "seed_managed"
    - path: "DeckFlow.Core/Content/SeedIndexFileReader.cs"
      provides: "Shared parser: index-seed.json -> natural-key set (U+0000-joined)"
      contains: "SeedIndexFileReader"
  key_links:
    - from: "ContentSiteIndexStore.BuildUpsertParameters"
      to: "@seedManaged"
      via: "DynamicParameters.Add bound per-row (mirrors bodySha256)"
      pattern: "seedManaged"
---

<objective>
Ship the SYNC-17 foundation contract: the `seed_managed` marker column on `content_site_index`
(nullable, dialect-guarded, additive), a null-only idempotent backfill write method, and a single
shared Core reader for `index-seed.json` natural-key membership that later plans (D-02 backfill,
reconcile orchestrator) reuse.

Purpose: Establish the row-level seed-ownership marker that gates all seed-driven removal (the
Codex-HIGH hard prereq). Nullable (`NULL` = unclassified, distinct from `false` = classified
prod-owned) so the D-02 backfill can be re-run safely.
Output: `ContentSiteIndexRow.SeedManaged`, the column across all store SQL, `SetSeedManagedIfNullAsync`,
and `SeedIndexFileReader`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/91-reconcile-seed-lifecycle/91-CONTEXT.md
@.planning/phases/91-reconcile-seed-lifecycle/91-RESEARCH.md
@.planning/phases/91-reconcile-seed-lifecycle/91-PATTERNS.md

<interfaces>
<!-- Contracts this plan establishes for downstream plans. Executor CREATES these. -->
New on ContentSiteIndexRow (DeckFlow.Core/Knowledge/ContentArtifactSpec.cs, alongside BodySha256/AwaitingConfirmUtc):
  public bool? SeedManaged { get; init; }

New on IContentSiteIndexStore / ContentSiteIndexStore:
  Task<int> SetSeedManagedIfNullAsync(long id, bool seedManaged, CancellationToken cancellationToken = default);

New Core reader (DeckFlow.Core/Content/SeedIndexFileReader.cs):
  // Parses index-seed.json using the ContentKbSeedLoader JSON shape (camelCase, case-insensitive).
  // Returns the set of U+0000-joined natural keys ("{Type}U+0000{Value}") present in the seed.
  public static IReadOnlySet<string> ReadNaturalKeys(string seedFilePath);
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Thread seed_managed through the store (model, DDL, create-table, SELECTs, mapping, upsert)</name>
  <files>DeckFlow.Core/Knowledge/ContentArtifactSpec.cs, DeckFlow.Core/Content/ContentSiteIndexStore.cs, DeckFlow.Core.Tests/Content/SeedManagedSchemaTests.cs, DeckFlow.Core.Tests/Content/SeedManagedWritePathTests.cs</files>
  <read_first>
    - DeckFlow.Core/Content/ContentSiteIndexStore.cs (self - the body_sha256/awaiting_confirm_utc rollout is the exact template: DDL ~132-150, create-table ~1210-1258, 5 SELECTs ~242-448, mapping ~1031-1065/1260-1281, BuildUpsertParameters ~896-925, UpsertContentColumnsOnlySql/UpsertPreservingVisibilitySql ~1113-1208)
    - DeckFlow.Core/Knowledge/ContentArtifactSpec.cs (BodySha256/AwaitingConfirmUtc props ~148-164)
    - DeckFlow.Core.Tests/Content/ContentSiteIndexStoreBodyHashTests.cs (test template) and ContentSiteIndexStoreSchemaEnsureSwitchTests.cs
    - 91-PATTERNS.md "ContentSiteIndexStore.cs - seed_managed column" section
    - 91-RESEARCH.md Pattern 1, Pattern 2, Pitfall 1, Pitfall 4
  </read_first>
  <behavior>
    - Fresh SQLite DB: EnsureSchemaAsync creates content_site_index with `seed_managed INTEGER NULL`.
    - Existing DB missing the column: EnsureSchemaAsync ALTERs it in (idempotent - second run is a no-op, no error).
    - A row written via UpsertContentColumnsOnlyAsync with SeedManaged=true reads back SeedManaged==true.
    - A row written via the plain local-distill UpsertRowAsync path reads back SeedManaged==null (never forced true).
    - GetAllRowsAsync / GetByNaturalKeyAsync / GetPublishedRowsAsync / GetApprovedRowsAsync / GetByIdAsync all round-trip SeedManaged.
  </behavior>
  <action>
    Add `public bool? SeedManaged { get; init; }` to `ContentSiteIndexRow` (nullable per Pitfall 1 -
    NULL means "unclassified", distinct from false="classified prod-owned"; do NOT use a non-null
    bool with a DEFAULT).
    In `EnsureSchemaAsync`, add a dialect-guarded additive ALTER mirroring the `awaiting_confirm_utc`
    branch: Postgres `ALTER TABLE content_site_index ADD COLUMN seed_managed BOOLEAN NULL;`, SQLite
    `... ADD COLUMN seed_managed INTEGER NULL;`, guarded by `if (!columns.Contains("seed_managed"))`.
    Add `seed_managed BOOLEAN NULL,` / `seed_managed INTEGER NULL,` to PostgresCreateTableSql /
    SqliteCreateTableSql. Add `seed_managed` to all 5 SELECT column lists and to the private
    ContentSiteIndexRowData materialization + ToContentSiteIndexRow mapping (map INTEGER/BOOLEAN to
    bool?). In BuildUpsertParameters add `parameters.Add("seedManaged", row.SeedManaged);` - a bound
    per-row parameter (Pattern 1). Add `seed_managed` to the column/VALUES/ON-CONFLICT lists of
    `UpsertContentColumnsOnlySql` and `UpsertPreservingVisibilitySql` only, with
    `seed_managed = EXCLUDED.seed_managed`. Do NOT add it to the plain `UpsertSql` (local-distill
    path stays unmarked). On `UpsertPreservingVisibilitySql`, add an explicit `// Why:` comment (per
    Pitfall 4) stating seed_managed is overwritten-from-EXCLUDED on this variant because every row
    passing through the seed-load path is by definition (re)entering the seed-managed set.
    Write SeedManagedSchemaTests (dialect ALTER idempotency, both dialects via RelationalDatabaseConnection)
    and SeedManagedWritePathTests (bound-param invariant: columns-only/preserving-visibility upserts
    carry the value; plain UpsertRowAsync leaves it null).
  </action>
  <acceptance_criteria>
    - `dotnet build DeckFlow.sln` clean, 0 new warnings.
    - Test asserts second EnsureSchemaAsync on an already-migrated DB performs no ALTER and does not throw.
    - Test asserts a plain UpsertRowAsync row reads back `SeedManaged == null` (local-distill stays unmarked).
    - Test asserts a UpsertContentColumnsOnlyAsync row with SeedManaged=true reads back true through GetAllRowsAsync.
    - No `seed_managed` literal appears in any UPSERT VALUES clause (value flows only via @seedManaged).
  </acceptance_criteria>
  <verify>
    <automated>dotnet.exe test DeckFlow.Core.Tests/DeckFlow.Core.Tests.csproj --no-restore --filter "FullyQualifiedName~SeedManagedSchema|FullyQualifiedName~SeedManagedWritePath"</automated>
  </verify>
  <done>seed_managed threads through model, DDL, create-table, all 5 SELECTs, mapping, and the two seed-managed upsert variants as a bound parameter; local-distill path leaves it null; both tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: SetSeedManagedIfNullAsync (null-only idempotent backfill write)</name>
  <files>DeckFlow.Core/Content/ContentSiteIndexStore.cs, DeckFlow.Core.Tests/Content/SeedManagedWritePathTests.cs</files>
  <read_first>
    - DeckFlow.Core/Content/ContentSiteIndexStore.cs `SetBodySha256IfNullAsync` (~486-501 - exact template)
    - DeckFlow.Core/Content/IContentSiteIndexStore.cs (interface + throwing-default-method convention used for SetBodySha256IfNullAsync / SetAwaitingConfirmAsync)
    - 91-PATTERNS.md "Backfill write template" section; 91-RESEARCH.md Code Examples (SetBodySha256IfNullAsync)
  </read_first>
  <behavior>
    - Calling SetSeedManagedIfNullAsync(id, true) on a row with seed_managed IS NULL sets it true and returns 1.
    - Calling it again on the now-classified row returns 0 (no overwrite).
    - Calling it on a row already classified false leaves it false and returns 0.
  </behavior>
  <action>
    Add `SetSeedManagedIfNullAsync(long id, bool seedManaged, CancellationToken = default)` to
    IContentSiteIndexStore (as a throwing default interface method, mirroring SetBodySha256IfNullAsync
    so the ~12 existing test doubles compile unchanged) and implement it in ContentSiteIndexStore with
    `UPDATE content_site_index SET seed_managed = @seedManaged WHERE id = @id AND seed_managed IS NULL;`.
    Call EnsureSchemaAsync first, mirror the SetBodySha256IfNullAsync method body exactly.
  </action>
  <acceptance_criteria>
    - Test seeds an unclassified row, calls the setter twice, asserts (1, then 0) return values and final value unchanged after the second call.
    - Test asserts a row pre-set to false is not overwritten (returns 0).
    - Full solution build clean (existing IContentSiteIndexStore doubles unchanged).
  </acceptance_criteria>
  <verify>
    <automated>dotnet.exe test DeckFlow.Core.Tests/DeckFlow.Core.Tests.csproj --no-restore --filter "FullyQualifiedName~SeedManagedWritePath"</automated>
  </verify>
  <done>SetSeedManagedIfNullAsync exists as a throwing default interface method + implementation, is idempotent on re-run, and never overwrites a classified row; tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: SeedIndexFileReader - shared index-seed.json natural-key reader</name>
  <files>DeckFlow.Core/Content/SeedIndexFileReader.cs, DeckFlow.Core.Tests/Content/SeedIndexFileReaderTests.cs</files>
  <read_first>
    - DeckFlow.Web/Services/Content/ContentKbSeedLoader.cs (private ContentKbSeedEntry record + JsonSerializerOptions shape ~1-89 - the byte-shape to match)
    - DeckFlow.Core/Content/ContentNaturalKey.cs (U+0000-joined key convention ~35-53)
    - DeckFlow.Core/Orchestration/ContentIndexExportRow.cs (seed row shape written by From())
    - 91-RESEARCH.md "Don't Hand-Roll" (seed JSON parsing shape) + Pitfall 6
  </read_first>
  <behavior>
    - Given an index-seed.json with two entries (one YouTube, one podcast), ReadNaturalKeys returns a 2-element set of "{Type}U+0000{Value}" keys.
    - A missing/absent seed file returns an empty set (not an exception) - mirrors ContentKbSeedLoader's LoadIfPresent tolerance.
    - Keys use the U+0000 separator, matching ContentSyncDiffClassifier / DirectPushCoordinator key format (SYNC-05).
  </behavior>
  <action>
    Create static `SeedIndexFileReader` in DeckFlow.Core/Content. Reuse the ContentKbSeedLoader seed
    entry shape (natural_key_type / natural_key_value / bodySha256 fields) and its JsonSerializerOptions
    (camelCase, case-insensitive). Expose `ReadNaturalKeys(string seedFilePath)` returning
    `IReadOnlySet<string>` of `$"{type}U+0000{value}"` keys. Return an empty set when the file does not
    exist. Keep it pure/static (no DI) so both the D-02 backfill (91-03) and the reconcile orchestrator
    (91-06) reuse it. Do NOT create a second seed serializer.
  </action>
  <acceptance_criteria>
    - Test parses a fixture index-seed.json and asserts the exact U+0000-joined key set.
    - Test asserts a non-existent path yields an empty set with no throw.
    - Key separator is verified as U+0000 (not a printable separator).
  </acceptance_criteria>
  <verify>
    <automated>dotnet.exe test DeckFlow.Core.Tests/DeckFlow.Core.Tests.csproj --no-restore --filter "FullyQualifiedName~SeedIndexFileReader"</automated>
  </verify>
  <done>SeedIndexFileReader parses the real seed shape into U+0000-joined natural keys, tolerates a missing file, and is reused (not re-implemented) downstream; tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| seed file (git checkout) -> parser | Operator-authored JSON parsed into a key set; no destructive action taken in this plan |
| store schema-ensure -> DB | Additive DDL only; runs against local SQLite here (prod ALTER exercised via the web host in 91-03) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-91-01 | Tampering | seed_managed hardcoded in shared upsert SQL would misclassify local-distill rows | mitigate | Bound @seedManaged per row (Pattern 1); test asserts plain UpsertRowAsync leaves it null |
| T-91-02 | Denial of Service | Non-idempotent DDL crashing host startup on re-run | mitigate | `if (!columns.Contains(...))` guard + idempotency test; nullable column avoids default-collapse |
| T-91-SC | Tampering | npm/pip/cargo installs | accept | No packages added this phase (RESEARCH: pure in-house extension); supply-chain N/A |
</threat_model>

<verification>
- `dotnet build DeckFlow.sln` clean, 0 new warnings.
- `dotnet.exe test DeckFlow.Core.Tests/DeckFlow.Core.Tests.csproj --no-restore --filter "FullyQualifiedName~SeedManaged|FullyQualifiedName~SeedIndexFileReader"` green.
- LF line endings preserved (.gitattributes); format gate (changed lines) clean.
</verification>

<success_criteria>
content_site_index carries the nullable seed_managed marker on both dialects (additive, idempotent),
threaded through every store read/write as a bound parameter (never a literal), with a null-only
idempotent backfill setter and a shared seed-key reader - the SYNC-17 foundation all later plans build on.
</success_criteria>

<output>
Create `.planning/phases/91-reconcile-seed-lifecycle/91-01-SUMMARY.md` when done.
</output>
