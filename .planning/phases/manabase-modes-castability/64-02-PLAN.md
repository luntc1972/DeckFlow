---
phase: manabase-modes-castability
plan: 02
type: execute
wave: 2
depends_on: [64-01]
files_modified:
  - DeckFlow.Web/Models/ManabaseRequest.cs
  - DeckFlow.Web/Models/ManabaseViewModel.cs
  - DeckFlow.Web/Controllers/ManabaseController.cs
  - DeckFlow.Web/Services/Manabase/ManabaseAnalysisService.cs
  - DeckFlow.Web/Views/Deck/Manabase.cshtml
  - DeckFlow.Web/wwwroot/css/site-common.css
  - DeckFlow.Web.Tests/Manabase/ManabaseAnalysisServiceTests.cs
  - DeckFlow.Web.Tests/Manabase/ManabaseControllerModeTests.cs
  - DeckFlow.Web/e2e/manabase-castability.spec.ts
autonomous: true
requirements: [MODE-03, CAST-03, COMMANDER-02, FORMULA-01]
user_setup: []

must_haves:
  truths:
    - "The form shows a Casual/cEDH selector; Casual is the default and re-renders selected on postback"
    - "Selecting cEDH lowers the land target into the ~28-32 band and the page states the mode used"
    - "Casual mode renders a Castability table sorted worst-cast% first, listing only real spells (no Sol Ring/Birds/lands as rows), with a per-row limiting factor and an 'estimate, on the play, on curve' caveat"
    - "The page has no horizontal overflow on mobile or desktop across themes"
  artifacts:
    - path: "DeckFlow.Web/Models/ManabaseRequest.cs"
      provides: "ManabaseMode Mode (default Casual)"
      contains: "Mode"
    - path: "DeckFlow.Web/Controllers/ManabaseController.cs"
      provides: "passes request.Mode into AnalyzeAsync"
      contains: "request.Mode"
    - path: "DeckFlow.Web/Services/Manabase/ManabaseAnalysisService.cs"
      provides: "AnalyzeAsync overload taking ManabaseMode; calls Analyze(deck, mode)"
      contains: "ManabaseMode"
    - path: "DeckFlow.Web/Views/Deck/Manabase.cshtml"
      provides: "mode radio + castability table (casual)"
      contains: "Castability"
    - path: "DeckFlow.Web/e2e/manabase-castability.spec.ts"
      provides: "playwright smoke: mode toggle + castability table, desktop+mobile"
      contains: "Castability"
  key_links:
    - from: "ManabaseController.Manabase(POST)"
      to: "IManabaseAnalysisService.AnalyzeAsync"
      via: "request.Mode argument"
      pattern: "AnalyzeAsync\\([^)]*Mode"
    - from: "Manabase.cshtml"
      to: "Model.Report.Castability"
      via: "view model"
      pattern: "Report\\.Castability"
---

<objective>
Surface the modes + castability from Wave 1 in the Web UI (MODE-03/04, CAST-03): a Casual/cEDH radio on the form, mode echoed in the report, and a Casual-only Castability table (worst cast% first) that excludes mana rocks/dorks from its rows. Includes xUnit + Playwright per the project's web-page-change rule, verified desktop + mobile across themes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/manabase-modes-castability/64-RESEARCH.md
@.planning/phases/manabase-modes-castability/64-01-PLAN.md
@.planning/phases/manabase-modes-castability/64-UI-SPEC.md
@.planning/manabase-modes-castability-SPEC.md
@CLAUDE.md

> **The view, selectors, table, states, responsive + a11y rules are specified in `64-UI-SPEC.md` — build the view to that contract.**

<interfaces>
From Wave 1 (Core):
  enum ManabaseMode { Casual, Cedh }
  record CardCastability { string Name; int ManaValue; int OnCurveTurn; int CastPercent; string LimitingFactor; }
  ManabaseReport.Mode ; ManabaseReport.Castability (ascending by CastPercent)
  ManabaseAnalyzer.Analyze(ManabaseDeck deck, ManabaseMode mode)
Existing:
  ManabaseRequest { DeckInputSource, DeckUrl, DeckText, DeckName, DeckSource }
  IManabaseAnalysisService.AnalyzeAsync(string deckSource, string? deckName, CancellationToken)
  ManabaseClassifier.Classify(facts, isSingleton: true)  // service currently hardcodes isSingleton:true
</interfaces>
</context>

<tasks>

1. **Request** (ManabaseRequest.cs): add `public ManabaseMode Mode { get; set; } = ManabaseMode.Casual;` and `public CommanderImportance CommanderImportance { get; set; } = CommanderImportance.Standard;` (needs `using DeckFlow.Core.Manabase;`).

1b. **Commander-importance selector** (Manabase.cshtml + service + controller): add a "How important is casting your commander?" selector (3 options — Central: "must cast ASAP every game", Standard: "matters, cast when convenient", Low: "optional / late value"; default Standard). Thread `request.CommanderImportance` through the controller into `AnalyzeAsync(... , mode, importance, ...)` and on to `ManabaseAnalyzer.Analyze(deck, mode, importance)`. Persist the choice on postback. Help text: explains Brago-type commanders want Central.

2. **Service** (ManabaseAnalysisService.cs) — bundle into an options object to stop the param list telescoping (Codex LOW): introduce `ManabaseAnalysisOptions { ManabaseMode Mode = Casual; CommanderImportance CommanderImportance = Standard; }`. `AnalyzeAsync(string deckSource, string? deckName, ManabaseAnalysisOptions options, CancellationToken)` (default `new()` to keep existing call sites compiling). Pass `options.Mode`/`options.CommanderImportance` to `ManabaseAnalyzer.Analyze(deck, options.Mode, options.CommanderImportance)` and `options.Mode` to `ManabaseSwapPromptBuilder.Build(...)`. Keep `Classify(facts, isSingleton: true)` as-is.

3. **Controller** (ManabaseController.cs): build `new ManabaseAnalysisOptions { Mode = request.Mode, CommanderImportance = request.CommanderImportance }` and pass into `AnalyzeAsync(...)`.

4. **View model** (ManabaseViewModel.cs): no new field needed (castability + mode live on `Report`); add a convenience `bool ShowCastability => Report is { Mode: ManabaseMode.Casual, Castability.Count: > 0 };` (casual-only surface).

5. **View** (Manabase.cshtml):
   - Add a Casual/cEDH radio group bound to `Request.Mode` (two radios; Casual checked by default; persists on postback). Place near the existing input controls. Label cEDH: "lower land count, fast-mana heavy".
   - In the results block, echo the mode: e.g. "Mode: Casual" / "Mode: cEDH" next to the land summary.
   - When `Model.ShowCastability`, render a **Castability** table: columns `Card | MV | Cast on curve | Limiting`. Rows from `Report.Castability` (already sorted worst-first). Show `CastPercent%`; map `LimitingFactor` to friendly text ("mana", "color: U", "mana + color"). One-line caveat under the heading: "Estimate — chance to cast on its mana-value turn, on the play. Counts ramp; rocks/dorks aren't listed."
   - Reuse existing table styling; if new layout CSS is needed it goes in `site-common.css` (NOT `site.css`) per the theme-system constraint.

5b. **Formula panels (FORMULA-01)** — two native `<details>` below the results per 64-UI-SPEC §4b:
   - **"How the analysis works"** — static methodology copy (Karsten regression + Monte-Carlo castability model + aggregation + commander weighting); render even on the empty form.
   - **"This deck's numbers"** — only when `HasResult`: show the land-target terms evaluated for this deck, the per-color source tally, and the sim parameters. If `ManabaseReport` doesn't already expose the regression-term breakdown + sim params, add a small ADDITIVE breakdown to the report in Wave 1 (e.g. `ManabaseReport.Explanation`) OR compute the displayable terms in the view from existing fields — pick the lighter; do not change the numbers. Both panels collapsed by default, keyboard-accessible, no JS.

6. **CSS** (site-common.css, only if needed): a responsive table wrapper (e.g. `.castability-table` with `overflow-x:auto` on narrow viewports) so 4 columns don't overflow on mobile, plus `<details>` panel styling. No per-theme tokens here.

7. **Tests:**
   - `ManabaseAnalysisServiceTests`: existing tests still pass (default Casual); add a cEDH-mode test asserting the returned `Report.Mode == Cedh` and `TargetLands` lower than the Casual run for the same fixture deck. (Uses the existing test seam / mocked Scryfall resolver — mirror current service tests.)
   - `ManabaseControllerModeTests`: POST with `Mode = Cedh` flows through to the service (mode argument received) and the view model's report mode is cEDH; POST default is Casual. **Add: a cEDH report is present but `ShowCastability == false` (castability table hidden in cEDH for v1)** (Codex LOW). Mirror existing controller test patterns (NullLogger, fake service).
   - **View/render test (Codex LOW):** assert the `LimitingFactor` → friendly-text mapping renders ("mana", "color: U", "mana + color") and the casual-only gate hides the table when `ShowCastability` is false. Use a view-model/render unit test or a Playwright assertion.
   - `DeckFlow.Web/e2e/manabase-castability.spec.ts` (Playwright): with `feature.manabase.enabled` on, submit a known deck in Casual → assert Castability table present, rows sorted ascending by %, and no Sol Ring/Birds/land row; toggle cEDH → assert land target text drops + "cEDH" shown + castability table absent. Run at desktop + mobile viewport; assert no horizontal scroll (document scrollWidth ≈ clientWidth). Follow the existing e2e harness (server started first, admin/send patterns per the e2e memory).
</tasks>

<verification>
- `dotnet build` clean.
- xUnit Web tests pass (targeted; VSTest flaky in WSL — build + push CI as backstop).
- Playwright smoke green at desktop + mobile; manual spot-check across at least the default + one alt theme for overflow (web-page-change DoD rule).
- README: add a line noting the Mana Base analyzer's Casual/cEDH modes + castability readout (per "always update README" rule).
- Changed-lines format-gate passes.
</verification>

<threat_model>
- New `Mode` is form-bound (enum). Model binding of an out-of-range enum value defaults/round-trips safely; invalid input falls back to Casual (no exception path that bypasses the existing try/catch boundary in the controller).
- No new endpoint, no new auth surface, no new external call. Same Scryfall path, same anti-forgery token on POST.
- Castability is render-only; no persistence, no PII, no secrets.
- Mobile/theme overflow is the main UX risk — covered by the e2e no-horizontal-scroll assertion + manual theme spot-check.
</threat_model>
