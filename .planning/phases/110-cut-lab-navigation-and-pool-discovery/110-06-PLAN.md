---
phase: 110-cut-lab-navigation-and-pool-discovery
plan: 06
type: execute
wave: 5
depends_on: ["110-02", "110-03", "110-04", "110-05"]
files_modified:
  - DeckFlow.Web/e2e/cut-lab-nav-themes.spec.ts
autonomous: false
requirements: [CLUP-07, CLUP-08]
must_haves:
  truths:
    - "Classic, Nyx, and Commander Table mobile screenshots show the jump nav, filter/search, collapse, and disclosures with no text overlap or unreadable control states"
    - "The sticky jump nav and the round-counter sticky bar do not overlap when both are stuck"
    - "Anchor-nav pills are readable and not obscured by the back-to-top button at mobile width (asserted, not eyeballed)"
    - "With JavaScript disabled (CLUP-07): anchor-nav #hash links still navigate, native <details> still open/close, pool filter/search controls stay hidden with the full table visible, card-text disclosures still open, and submit step-buttons still submit"
  artifacts:
    - path: "DeckFlow.Web/e2e/cut-lab-nav-themes.spec.ts"
      provides: "Cross-theme mobile Playwright coverage + screenshots for the Phase 110 chrome"
      contains: "commander-table"
  key_links:
    - from: "DeckFlow.Web/e2e/cut-lab-nav-themes.spec.ts"
      to: ".planning/ui-design/cut-lab/screenshots/"
      via: "page.screenshot per theme+viewport"
      pattern: "screenshot"
---

<objective>
Verify the Phase 110 chrome (jump nav, pool filter/search, section collapse, card-text disclosures)
renders cleanly on mobile across the Classic, Nyx, and Commander Table themes (CLUP-08, success
criterion 7): no text overlap, no unreadable pill/button states, and the two stickies do not collide.

Purpose: three of these themes are the required readability gates and Commander Table is a standalone
fork that historically skipped `site.css` imports (focus-ring / token traps). This plan produces the
screenshot evidence and a blocking human review; the deeper regression sweep is Phase 111.
Output: a cross-theme mobile e2e spec + screenshots + human sign-off.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/110-cut-lab-navigation-and-pool-discovery/110-CONTEXT.md
@.planning/phases/110-cut-lab-navigation-and-pool-discovery/110-UI-SPEC.md
@.planning/phases/110-cut-lab-navigation-and-pool-discovery/110-02-SUMMARY.md
@.planning/phases/110-cut-lab-navigation-and-pool-discovery/110-03-SUMMARY.md
@.planning/phases/110-cut-lab-navigation-and-pool-discovery/110-04-SUMMARY.md
@.planning/phases/110-cut-lab-navigation-and-pool-discovery/110-05-SUMMARY.md

<interfaces>
<!-- Reuse the existing Cut Lab e2e harness — do not reinvent server/theme/admin plumbing. -->

Existing spec pattern (DeckFlow.Web/e2e/cut-lab-smoke.spec.ts):
  baseUrl = 'http://localhost:5173'; screenshotDir = '../../.planning/ui-design/cut-lab/screenshots'.
  Theme via cookie: classic -> 'site.css', nyx -> 'site-nyx.css',
    commander-table -> 'site-commander-table.css'.
  Mobile viewport: { width: 430, height: 2200 }.
  Support helpers: e2e/support/admin-lock (acquireAdminLockForTest/releaseAdminLockForTest),
    e2e/support/admin-tools (setToolEnabled — the tool.cut-lab.enabled flag must be ON for the route),
    e2e/support/cut-lab-mobile-collapse (expandMobileCollapsibles).
  oversizedPool fixture in the smoke spec drives a full analysis result.

New selectors to assert (from plans 110-02..05): .cutlab-anchor-nav, .cutlab-anchor-nav-list a,
  .cutlab-sticky-bar, .cutlab-pool-filter, .cutlab-pool-search, .cutlab-pool-match-count,
  .cutlab-card-text, details.cutlab-collapsible.

Server startup: scripts/run-web-test.sh (sets DECKFLOW_DISABLE_AUTO_BROWSER=true). UI testing must
  NEVER open a browser on the Windows host; run Playwright headless in WSL
  (env -u DISPLAY -u WAYLAND_DISPLAY npx --no-install playwright test). Probe port 5173 (netstat)
  before starting to avoid a stale Windows server poisoning results.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Cross-theme mobile e2e spec capturing the Phase 110 chrome</name>
  <files>DeckFlow.Web/e2e/cut-lab-nav-themes.spec.ts</files>
  <read_first>
    - DeckFlow.Web/e2e/cut-lab-smoke.spec.ts (theme loop, viewport, oversizedPool fixture, screenshotDir, admin-lock + setToolEnabled usage)
    - DeckFlow.Web/e2e/support/cut-lab-mobile-collapse.ts (expandMobileCollapsibles)
    - .planning/phases/110-cut-lab-navigation-and-pool-discovery/110-UI-SPEC.md (Layout & Responsive Contract; Component Inventory collision + sticky-nav rows)
    - .planning/phases/110-cut-lab-navigation-and-pool-discovery/110-CONTEXT.md (D-02, D-06, D-07)
  </read_first>
  <action>
    Add a Playwright spec iterating the three required themes (Classic 'site.css', Nyx 'site-nyx.css',
    Commander Table 'site-commander-table.css') at the mobile viewport (430x2200), reusing the smoke
    spec's admin-lock + setToolEnabled('cut-lab', true) + oversizedPool import flow to reach a full Cut
    Lab result. For each theme assert: (1) `.cutlab-anchor-nav` is visible and, after scrolling past
    its origin, is sticky at the top; (2) its pills do not overflow-clip their text and are not
    obscured by the back-to-top button; (3) scrolling the "Cut rounds" section under the nav does NOT
    produce an overlapping-rectangle collision between `.cutlab-anchor-nav` and `.cutlab-sticky-bar`
    (compare boundingBox — the sticky bar's top must be >= the nav's bottom when both are on-screen);
    (4) `.cutlab-pool-filter` controls are visible and their bounding boxes do not overlap the table
    header; (5) a `.cutlab-card-text` `<summary>` toggles open and its body is visible. Capture a
    full-page screenshot per theme to the existing screenshotDir with a descriptive name
    (e.g. `cut-lab-nav-{theme}-mobile.png`). Run headless; never open a Windows-host browser. LF endings.
  </action>
  <acceptance_criteria>
    - `grep -c "commander-table\|site-commander-table.css" DeckFlow.Web/e2e/cut-lab-nav-themes.spec.ts`
      >= 1 and Classic + Nyx are also iterated.
    - The spec asserts a non-overlap boundingBox relationship between `.cutlab-anchor-nav` and
      `.cutlab-sticky-bar` (collision guard) and asserts anchor-nav stickiness at top.
    - The spec asserts, at mobile width with the nav stuck, that the `.cutlab-anchor-nav` bounding
      box does not intersect the `#back-to-top-button` bounding box (back-to-top collision guard,
      UI-SPEC Layout & Responsive Contract) — a programmatic check, not prose-only.
    - The spec captures at least one screenshot per theme into
      `.planning/ui-design/cut-lab/screenshots/`.
    - The spec reuses the existing support helpers (admin-lock, setToolEnabled) — no new admin/flag
      plumbing invented.
    - Server started via scripts/run-web-test.sh; run:
      `cd DeckFlow.Web && env -u DISPLAY -u WAYLAND_DISPLAY npx --no-install playwright test e2e/cut-lab-nav-themes.spec.ts`
      passes for all three themes (after confirming port 5173 is the WSL test server, not a stale
      Windows host).
  </acceptance_criteria>
  <verify>
    <automated>cd DeckFlow.Web && env -u DISPLAY -u WAYLAND_DISPLAY npx --no-install playwright test e2e/cut-lab-nav-themes.spec.ts</automated>
  </verify>
  <done>Spec passes across Classic/Nyx/Commander Table on mobile, asserts the sticky collision guard, and writes per-theme screenshots.</done>
</task>

<task type="auto">
  <name>Task 2: No-JS fallback e2e (CLUP-07 progressive-enhancement guarantee)</name>
  <files>DeckFlow.Web/e2e/cut-lab-nav-themes.spec.ts</files>
  <read_first>
    - DeckFlow.Web/e2e/cut-lab-whatif.spec.ts (~103-115 buildNoJsPage: browser.newContext({ javaScriptEnabled: false }))
    - DeckFlow.Web/e2e/cut-lab-structure.spec.ts (~322-332 no-JS submit-fallback context with httpCredentials send:'always')
    - DeckFlow.Web/Views/Deck/CutLab.cshtml (anchor-nav #hash links, <details data-cutlab-mobile-collapse>, .cutlab-pool-filter hidden default, .cutlab-card-text <details>, submit step-buttons)
    - .planning/phases/110-cut-lab-navigation-and-pool-discovery/110-CONTEXT.md (D-04, D-14, D-30)
  </read_first>
  <action>
    Add a `test` in the same spec file that builds a Playwright context with
    `javaScriptEnabled: false` (reuse the buildNoJsPage / no-JS-context pattern from
    cut-lab-whatif.spec.ts and cut-lab-structure.spec.ts, including the admin httpCredentials
    send:'always' so the flagged route loads), imports the oversizedPool to reach a full Cut Lab
    result, and asserts the CLUP-07 no-JS fallback: (a) an anchor-nav link is a plain in-page
    `#hash` anchor — clicking it changes the URL hash / the target section is scrolled into the
    viewport by the browser (no script); (b) a collapsible `<details data-cutlab-mobile-collapse>`
    toggles open then closed via native `<summary>` clicks; (c) the `.cutlab-pool-filter` container
    is still `hidden` (D-14) while the full unfiltered pool table (all `tr[data-cut-lab-card]` rows)
    is visible; (d) a `.cutlab-card-text` disclosure opens via native `<summary>` click (D-30);
    (e) a submit step-button (type="submit" tab) still submits and the page re-renders. Run headless;
    never open a Windows-host browser. LF endings.
  </action>
  <acceptance_criteria>
    - `grep -c "javaScriptEnabled: false" DeckFlow.Web/e2e/cut-lab-nav-themes.spec.ts` returns >= 1.
    - The no-JS test asserts all five fallbacks (a)-(e): anchor `#hash` navigation, native
      `<details>` toggle, `.cutlab-pool-filter` remains `hidden` with the full table visible, native
      card-text `<details>` toggle, and a submit step-button still submits.
    - `grep -c "cutlab-pool-filter" DeckFlow.Web/e2e/cut-lab-nav-themes.spec.ts` >= 1 and the
      assertion checks the container is hidden (not merely present).
    - `cd DeckFlow.Web && env -u DISPLAY -u WAYLAND_DISPLAY npx --no-install playwright test e2e/cut-lab-nav-themes.spec.ts`
      passes including the no-JS test (server via scripts/run-web-test.sh; confirm port 5173 is the
      WSL test server, not a stale Windows host).
  </acceptance_criteria>
  <verify>
    <automated>cd DeckFlow.Web && env -u DISPLAY -u WAYLAND_DISPLAY npx --no-install playwright test e2e/cut-lab-nav-themes.spec.ts -g "no-JS"</automated>
  </verify>
  <done>A javaScriptEnabled:false test proves the CLUP-07 fallback: hash nav, native details, hidden filter controls with full table, native disclosures, and submit tabs all work with no script.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human review of cross-theme mobile screenshots</name>
  <what-built>
    A cross-theme mobile e2e spec that renders the Phase 110 Cut Lab chrome (jump nav, pool
    filter/search, section collapse, card-text disclosures) on Classic, Nyx, and Commander Table and
    writes full-page screenshots to `.planning/ui-design/cut-lab/screenshots/`.
  </what-built>
  <how-to-verify>
    1. Open `.planning/ui-design/cut-lab/screenshots/cut-lab-nav-classic-mobile.png`,
       `...-nyx-mobile.png`, and `...-commander-table-mobile.png`.
    2. Confirm the sticky jump-nav pills are readable (no clipped/truncated labels, no transparent
       background letting content bleed through) in all three themes — especially the dark Commander
       Table fork.
    3. Confirm the jump nav and the round-counter sticky bar are not overlapping when both are visible.
    4. Confirm the stuck jump nav is not sitting under or behind the back-to-top button at mobile
       width (pills fully visible, back-to-top not covering any pill).
    5. Confirm the pool filter/search controls, collapse summaries, and card-text disclosures show no
       text overlap and no unreadable control states.
  </how-to-verify>
  <resume-signal>Type "approved" if all three themes read cleanly, or describe the overlap/readability issues to fix.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → local dev server | e2e-only; no production surface, no new endpoints |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-110-11 | Denial of Service | Stale Windows server on :5173 poisoning results | mitigate | Probe port 5173 before run; start the WSL test server via scripts/run-web-test.sh; run headless |
</threat_model>

<verification>
- Spec passes on Classic/Nyx/Commander Table at mobile width.
- Sticky-bar AND back-to-top collision guards asserted programmatically; screenshots captured.
- No-JS fallback test (javaScriptEnabled:false) proves the CLUP-07 progressive-enhancement guarantee.
- Human sign-off confirms no text overlap / unreadable pills across the three themes.
</verification>

<success_criteria>
Classic, Nyx, and Commander Table mobile screenshots show the Phase 110 navigation and pool-discovery
chrome with no text overlap or unreadable control states; the sticky nav collides with neither the
round-counter bar nor the back-to-top button; and a javaScriptEnabled:false test proves the CLUP-07
no-JS fallback (hash nav, native details, hidden filter controls, native disclosures, submit tabs).
</success_criteria>

<output>
Create `.planning/phases/110-cut-lab-navigation-and-pool-discovery/110-06-SUMMARY.md` when done.
</output>
