---
phase: 20-content-kb-ingestion-transcription-local
plan: 03
type: execute
wave: 2
depends_on: ["20-02"]
files_modified:
  - DeckFlow.Core/Integration/IFfmpegAudioChunker.cs
  - DeckFlow.Core/Integration/FfmpegAudioChunker.cs
  - DeckFlow.Core/Integration/IWhisperTranscriptionService.cs
  - DeckFlow.Core/Integration/WhisperTranscriptionService.cs
  - DeckFlow.Core/Integration/WhisperResiliencePipeline.cs
  - DeckFlow.Core/DeckFlow.Core.csproj
  - DeckFlow.Core.Tests/WhisperTranscriptionServiceTests.cs
  - DeckFlow.Core.Tests/FfmpegAudioChunkerTests.cs
autonomous: true
requirements: [KB-04, KB-05]
notes:
  - "No packages.lock.json is generated (GeneratePackageLockFile unset repo-wide) — the approved OpenAI 2.10.0 add touches only DeckFlow.Core.csproj. Do NOT add packages.lock.json to files_modified."
  - "WhisperTranscriptionService is PURE (D-11): it persists NOTHING — no ContentVideoStore, no WhisperSpendLedger write. It RETURNS billed seconds + cost + status; the harvest verb (20-04) writes the ledger row on success and the transcript/status."
  - "depends_on 20-02 only (not 20-01) — it consumes AudioDownloadResult from 20-02; persistence wiring is the verb's job in 20-04. Wave 2, acyclic."
must_haves:
  truths:
    - "Before any Whisper API call, the cap check (monthly ledger total + duration*$0.006/min) is consulted via an injected IWhisperSpendLedger READ; over-cap returns an Outcome=SkippedOverCap result and makes NO API call and writes NO ledger row"
    - "A successful Whisper transcription returns Outcome=Whisper with the concatenated body, billed seconds, and cost — the service WRITES NOTHING (the harvest verb persists transcript + ledger row)"
    - "The OpenAI transcribe delegate is invoked THROUGH a concrete Polly pipeline: timeout = 12min (HttpClient timeout 15min set by the composition root); a test asserts the delegate runs through the Polly wrapper"
    - "Audio >24MB is chunked via shelled-out ffmpeg; each chunk is transcribed and the texts concatenated in order; if ffmpeg is absent or a chunk op fails, the service returns Outcome=Failed (the run continues — no crash)"
    - "OPENAI_API_KEY is read from the environment only, never committed"
  artifacts:
    - path: "DeckFlow.Core/Integration/WhisperTranscriptionService.cs"
      provides: "OpenAI 2.10.0 AudioClient via HttpClientPipelineTransport + cap-check READ gate + chunk loop + Polly wrapper + Func<> seam; persists nothing"
      contains: "HttpClientPipelineTransport"
    - path: "DeckFlow.Core/Integration/WhisperResiliencePipeline.cs"
      provides: "Concrete Polly pipeline (12min timeout + retry) wrapping the OpenAI transcribe delegate (Codex HIGH-4)"
      contains: "TimeoutStrategy"
    - path: "DeckFlow.Core/Integration/FfmpegAudioChunker.cs"
      provides: "Process.Start ffmpeg segment chunker + presence verification"
      contains: "segment_time"
    - path: "DeckFlow.Core.Tests/WhisperTranscriptionServiceTests.cs"
      provides: "cap-over-cap-skip + success-returns-cost + delegate-runs-through-Polly + chunk-loop-concat via Func<> seam"
      contains: "WhisperTranscriptionServiceTests"
  key_links:
    - from: "DeckFlow.Core/Integration/WhisperTranscriptionService.cs"
      to: "DeckFlow.Core/Content/WhisperSpendLedger.cs"
      via: "WouldExceedCapAsync READ before call (no write — the verb records the row)"
      pattern: "WouldExceedCapAsync"
    - from: "DeckFlow.Core/Integration/WhisperTranscriptionService.cs"
      to: "DeckFlow.Core/Integration/WhisperResiliencePipeline.cs"
      via: "transcribe delegate executed through the 12min Polly pipeline"
      pattern: "ExecuteAsync"
    - from: "DeckFlow.Core/Integration/WhisperTranscriptionService.cs"
      to: "DeckFlow.Core/Integration/AudioDownloadResult + IFfmpegAudioChunker"
      via: "size > 24MB → ChunkAsync → per-chunk transcribe → concat"
      pattern: "ChunkAsync"
---

<objective>
Build the Whisper transcription runtime (KB-04) as a PURE service (D-11) and wire the plain local spend-cap CHECK (KB-05) in `DeckFlow.Core/Integration`, plus the ffmpeg shell-out chunker for audio >24MB. Depends on Plan 20-02 (`AudioDownloadResult` audio contract + the Func<> precedent established in Integration).

Purpose: Whisper is the captions-absent fallback. The cap check (D-06) gates every call: projected monthly total = `WhisperSpendLedger.GetMonthlyTotalAsync(monthKey)` + duration*$0.006/min vs `DECKFLOW_WHISPER_MONTHLY_CAP_USD` (default $15). Over-cap → return Outcome=SkippedOverCap, make NO API call, write NO ledger row. The OpenAI SDK hands resilience to a CONCRETE Polly pipeline (Codex HIGH-4 + Pitfall P3): `ClientRetryPolicy(0)` + `NetworkTimeout = InfiniteTimeSpan` on the SDK, and a 12-min Polly timeout pipeline wrapping the transcribe delegate. Audio >24MB is downloaded (20-02 `IYouTubeAudioSource`, wired by the verb) then chunked here via ffmpeg, each chunk transcribed, texts concatenated. ffmpeg is a documented local prerequisite (D-05, currently NOT installed) — absent/chunk-fail → Outcome=Failed + continue, never crash. PER D-11 the service PERSISTS NOTHING: it returns billed seconds + cost + status; the harvest verb (20-04) writes the ledger row on success and the transcript/status.
Output: `IWhisperTranscriptionService` + pure impl with cap-check READ + chunk loop + Polly wrapper + Func<> seam; `WhisperResiliencePipeline`; `IFfmpegAudioChunker` + impl; OpenAI 2.10.0 added to DeckFlow.Core.csproj; xUnit tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-content-kb-ingestion-transcription-local/20-CONTEXT.md
@.planning/phases/20-content-kb-ingestion-transcription-local/20-RESEARCH.md
@.planning/phases/20-content-kb-ingestion-transcription-local/20-02-PLAN.md

<interfaces>
<!-- From DeckFlow.Core/Content/WhisperSpendLedger.cs (Phase 19 — READ-wire only; the verb writes) -->
- `Task<decimal> GetMonthlyTotalAsync(string yearMonth, CancellationToken)` — app-side sum over month_key
- `Task<bool> WouldExceedCapAsync(decimal projectedCallCostUsd, string monthKey, CancellationToken)` — total + projected > cap (READ — call this before the API call)
- `Task RecordCallAsync(long videoId, int secondsBilled, decimal costUsd, string monthKey, CancellationToken)` — ONE row per real call; the HARVEST VERB calls this on success, NOT this service (D-11)
- cap env key: `DECKFLOW_WHISPER_MONTHLY_CAP_USD` (default 15.00m); month key format `yyyy-MM`

<!-- From Plan 20-02 (DeckFlow.Core/Integration/IYouTubeAudioSource.cs) -->
- `record AudioDownloadResult : IDisposable { string TempFilePath; string FileName; long SizeBytes; double DurationSeconds; }` — the audio handle the verb downloads and passes in; Dispose() deletes the temp file
- `ITranscriptSource` non-null `TranscriptFetchResult` + `TranscriptOutcome` enum (Captions|Whisper|Failed|SkippedOverCap) — Whisper maps its own status to these via the verb/source in 20-04

<!-- From DeckFlow.Core/Knowledge/ContentModels.cs -->
- `TranscriptSource.Whisper = "whisper"`; `TranscriptStatus.Whisper/.Failed/.SkippedOverCap`

<!-- OpenAI 2.10.0 verified facts (RESEARCH HIGH confidence, Pattern 2) -->
- `new OpenAIClientOptions { Transport = new HttpClientPipelineTransport(httpClient), RetryPolicy = new ClientRetryPolicy(maxRetries: 0), NetworkTimeout = Timeout.InfiniteTimeSpan }`
- `new AudioClient("whisper-1", new ApiKeyCredential(apiKey), options)`
- `audioClient.TranscribeAudioAsync(Stream, audioFilename, new AudioTranscriptionOptions { ResponseFormat = AudioTranscriptionFormat.Verbose }, ct)`
- billed seconds: prefer `result.Value.Usage.Duration` (TimeSpan, added in 2.10.0); fall back to `result.Value.Duration` (TimeSpan?, Verbose). cost = seconds/60 * 0.006m
- `HttpClientPipelineTransport` + `ApiKeyCredential` + `ClientRetryPolicy` live in System.ClientModel.Primitives (transitive from OpenAI 2.10.0)

<!-- Polly v8 (already a Core dependency) -->
- `new ResiliencePipelineBuilder().AddTimeout(TimeSpan.FromMinutes(12)).AddRetry(new RetryStrategyOptions { ... }).Build()` then `pipeline.ExecuteAsync(async ct => ...)` — the 12min Polly timeout (distinct from the 15min HttpClient socket timeout set by the composition root)

<!-- ffmpeg (RESEARCH Pattern 4 / P4 / P7) -->
- 25MB Whisper limit; threshold 24MB margin. Pre-download size = AudioDownloadResult.SizeBytes (from 20-02)
- chunk: `ffmpeg -i "input" -f segment -segment_time 300 -c copy -reset_timestamps 1 "chunk_%04d.ext"` (300s safer than 600s per A3)
- ffmpeg presence: `Process.Start("ffmpeg", "-version")`, check ExitCode; ffmpeg NOT currently on PATH
- temp files in Path.GetTempPath(); delete in finally (no raw audio retained — D-15)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: FfmpegAudioChunker (shell-out + presence check)</name>
  <files>DeckFlow.Core/Integration/IFfmpegAudioChunker.cs, DeckFlow.Core/Integration/FfmpegAudioChunker.cs, DeckFlow.Core.Tests/FfmpegAudioChunkerTests.cs</files>
  <read_first>
    - .planning/phases/20-content-kb-ingestion-transcription-local/20-RESEARCH.md Pattern 4 (Process.Start segment command) + Pitfall P7 (absent ffmpeg → warn, mark failed, continue)
    - DeckFlow.Core/Integration/ArchidektApiDeckImporter.cs (namespace + doc-comment + sealed-class conventions)
  </read_first>
  <action>
    Create `IFfmpegAudioChunker` exposing `Task<bool> IsAvailableAsync(CancellationToken ct = default)` (runs `ffmpeg -version` via System.Diagnostics.Process, returns false on non-zero exit or process-start failure — never throws) and `Task<IReadOnlyList<string>> ChunkAsync(string inputPath, string outputDirectory, int segmentSeconds = 300, CancellationToken ct = default)` (returns the ordered chunk file paths). Implement `FfmpegAudioChunker` using `ProcessStartInfo("ffmpeg")` with `RedirectStandardError = true, UseShellExecute = false, CreateNoWindow = true`, arguments `-i "{inputPath}" -f segment -segment_time {segmentSeconds} -c copy -reset_timestamps 1 "{outputPattern}"` where outputPattern = `chunk_%04d{ext}` (ext = Path.GetExtension(inputPath)); `await process.WaitForExitAsync(ct)`; on non-zero exit throw `InvalidOperationException` with the captured stderr tail; return `Directory.GetFiles(outputDirectory, "chunk_*{ext}").OrderBy(f => f, StringComparer.Ordinal)`. Keep `IsAvailableAsync` exception-safe (try/catch around Process.Start). No NuGet ffmpeg wrapper (D-05). No raw audio retained by the chunker itself — callers delete temp dirs in finally. Add `FfmpegAudioChunkerTests`: a test asserting `IsAvailableAsync` returns false gracefully when ffmpeg is absent (run the real method — in CI/local without ffmpeg it returns false; mark `[Trait]` so it is environment-tolerant) and a test asserting the arguments string is constructed with `-segment_time 300 -c copy` (extract the arg-builder to an `internal static string BuildSegmentArguments(...)` pure helper and unit-test that directly — avoids needing ffmpeg installed).
  </action>
  <verify>
    <automated>"/mnt/c/Program Files/dotnet/dotnet.exe" build DeckFlow.Core/DeckFlow.Core.csproj -c Debug 2>&1 | grep -E "Build succeeded|error"; "/mnt/c/Program Files/dotnet/dotnet.exe" test DeckFlow.Core.Tests/DeckFlow.Core.Tests.csproj --filter "FullyQualifiedName~FfmpegAudioChunkerTests" 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "IsAvailableAsync" DeckFlow.Core/Integration/FfmpegAudioChunker.cs` ≥1; `grep -c "segment_time" DeckFlow.Core/Integration/FfmpegAudioChunker.cs` ≥1
    - `grep -c "BuildSegmentArguments" DeckFlow.Core/Integration/FfmpegAudioChunker.cs` ≥1 (pure, testable arg builder)
    - No ffmpeg NuGet package added to DeckFlow.Core.csproj
    - Build clean; arg-builder test passes without ffmpeg installed
  </acceptance_criteria>
  <done>FfmpegAudioChunker shells out for segmenting, reports availability without throwing, and its arg-builder is unit-tested without ffmpeg present.</done>
</task>

<task type="auto">
  <name>Task 2: WhisperResiliencePipeline + pure WhisperTranscriptionService (cap-READ gate + AudioClient through Polly + chunk loop)</name>
  <files>DeckFlow.Core/DeckFlow.Core.csproj, DeckFlow.Core/Integration/WhisperResiliencePipeline.cs, DeckFlow.Core/Integration/IWhisperTranscriptionService.cs, DeckFlow.Core/Integration/WhisperTranscriptionService.cs, DeckFlow.Core.Tests/WhisperTranscriptionServiceTests.cs</files>
  <read_first>
    - .planning/phases/20-content-kb-ingestion-transcription-local/20-RESEARCH.md Pattern 2 (AudioClient + HttpClientPipelineTransport), "Cap check before Whisper call" example, Pitfall P3 (SDK timeout) + P4 (Duration vs Usage.Duration)
    - DeckFlow.Core/Content/WhisperSpendLedger.cs (WouldExceedCapAsync / GetMonthlyTotalAsync signatures — READ-wire only; do NOT call RecordCallAsync here)
    - DeckFlow.Core/Integration/IYouTubeAudioSource.cs (Plan 20-02 — AudioDownloadResult shape consumed by the chunk decision)
    - DeckFlow.Core/Integration/IFfmpegAudioChunker.cs (Task 1 — ChunkAsync for >24MB)
    - DeckFlow.Web/Services/CardLookupService.cs lines 50-95 (Func<> seam to mirror for the transcribe call)
    - DeckFlow.Core/DeckFlow.Core.csproj (add OpenAI 2.10.0 PackageReference only)
  </read_first>
  <action>
    Add `<PackageReference Include="OpenAI" Version="2.10.0" />` to DeckFlow.Core.csproj (D-02 approved). No packages.lock.json is generated (GeneratePackageLockFile unset repo-wide; no lockfile tracked or on disk) — the approved add touches only the .csproj; do NOT create packages.lock.json.

    CONCRETE POLLY WRAPPER (Codex HIGH-4): Create `WhisperResiliencePipeline` exposing a factory `public static ResiliencePipeline Build()` that returns `new ResiliencePipelineBuilder().AddTimeout(TimeSpan.FromMinutes(12)).AddRetry(new RetryStrategyOptions { MaxRetryAttempts = 2, BackoffType = DelayBackoffType.Exponential, ShouldHandle = args => ValueTask.FromResult(args.Outcome.Exception is HttpRequestException or TimeoutRejectedException) }).Build()`. (12-min Polly timeout is distinct from the 15-min HttpClient socket timeout set by the composition root in 20-04.) The WhisperTranscriptionService runs EVERY OpenAI transcribe call through `pipeline.ExecuteAsync(...)` — never calls the transcribe delegate directly.

    Create `IWhisperTranscriptionService` exposing `Task<WhisperTranscriptionResult> TranscribeAsync(AudioDownloadResult audio, CancellationToken ct = default)` and a `public sealed record WhisperTranscriptionResult { get; init; }` with `TranscriptOutcome Outcome` (from 20-02 — Whisper|Failed|SkippedOverCap), `int SecondsBilled`, `decimal CostUsd`, `string? Body`, `string? FailureReason`. NOTE: the service takes an `AudioDownloadResult` (download already done by the verb via 20-02 `IYouTubeAudioSource`); cap projection uses `audio.DurationSeconds` as the duration source (Codex MEDIUM-c — the explicit cap-projection duration source is the AudioDownloadResult duration, supplied by the lister/audio source).

    Implement `WhisperTranscriptionService` (PURE — D-11) taking ctor deps: `IWhisperSpendLedger ledger` (READ only — for WouldExceedCapAsync), `IFfmpegAudioChunker chunker`, `HttpClient httpClient`, and an optional `Func<Stream, string, CancellationToken, Task<(string Body, int BilledSeconds)>>` transcribe-override for the test seam (mirror CardLookupService). It takes NO ContentVideoStore and does NOT call ledger.RecordCallAsync. The production transcribe delegate builds the `AudioClient` per RESEARCH Pattern 2 (HttpClientPipelineTransport + ClientRetryPolicy(0) + NetworkTimeout InfiniteTimeSpan; reads `OPENAI_API_KEY` from Environment in the production path only) and is ALWAYS invoked through `WhisperResiliencePipeline.Build().ExecuteAsync(...)`. The service ctor must NOT set or mutate `httpClient.Timeout` (the CLI composition root in 20-04 sets it to 15min before injection).

    month_key (Codex MEDIUM-c): capture `monthKey = DateTime.UtcNow.ToString("yyyy-MM")` at TranscribeAsync START and use the same value for both the cap-check projection and the returned result (so the verb records the ledger row under the same month). Document the known-tiny risk in a `// Why:` comment: a call that crosses a UTC month boundary attributes spend to the start month — accepted as negligible for a single-user local harvester.

    Flow in TranscribeAsync: projectedCost = `(decimal)audio.DurationSeconds / 60m * 0.006m`; if `await ledger.WouldExceedCapAsync(projectedCost, monthKey, ct)` → return `WhisperTranscriptionResult { Outcome = SkippedOverCap }` with NO API call and NO ledger write. Else decide chunking: if `audio.SizeBytes <= 24_000_000` transcribe the single file; else `if (!await chunker.IsAvailableAsync(ct)) return Failed("ffmpeg not available")`; `var chunks = await chunker.ChunkAsync(audio.TempFilePath, <temp out dir>, 300, ct)` (wrap in try/catch → on chunk failure return `Failed(...)`), then for EACH chunk in order open the stream and run the transcribe delegate through the Polly pipeline, accumulating body parts (`string.Join(" ", parts)` — ordered concatenation) and summing billed seconds. On overall success compute billedSeconds (prefer `Usage.Duration`, fall back to `Duration`; for multi-chunk, sum), cost = billedSeconds/60m*0.006m; return `WhisperTranscriptionResult { Outcome = Whisper, Body = concatenated, SecondsBilled = sum, CostUsd = cost }` — DO NOT persist (the verb writes the ledger + transcript). Clean up any chunk temp dir in a finally (the input AudioDownloadResult is disposed by the verb). On any transcription exception (incl. missing OPENAI_API_KEY surfaced as a clear message) → return `Failed(reason)` — no ledger row, no crash. Keep methods ≤30 lines (extract AudioClient build, single-file transcribe, and chunk-loop to private helpers).

    Tests (`WhisperTranscriptionServiceTests`) via the Func<> seam + a fake IWhisperSpendLedger (in-memory; assert WouldExceedCapAsync called, RecordCallAsync NEVER called) + a fake IFfmpegAudioChunker:
    - (a) over-cap → Outcome==SkippedOverCap AND the transcribe delegate was never invoked AND ledger.RecordCallAsync never called AND service touched no store
    - (b) under-cap single-file success (SizeBytes < 24MB) → Outcome==Whisper, Body == delegate body, SecondsBilled/CostUsd from the delegate's billed seconds; service wrote NOTHING
    - (c) the transcribe delegate is invoked THROUGH the Polly wrapper — assert via a delegate seam that counts invocations and that a simulated transient HttpRequestException is retried (proves the call runs through WhisperResiliencePipeline, Codex HIGH-4); OR assert a 12min-timeout pipeline is the one executing by injecting a pipeline-instrumented seam
    - (d) chunk path: SizeBytes > 24MB + fake chunker returning 2 chunks → delegate invoked twice, Body is the ordered concatenation of both chunk texts, SecondsBilled is the sum
    - (e) chunk path with chunker.IsAvailableAsync==false → Outcome==Failed, no delegate invocation
    No live OpenAI calls; no DB writes asserted.
  </action>
  <verify>
    <automated>"/mnt/c/Program Files/dotnet/dotnet.exe" build DeckFlow.Core/DeckFlow.Core.csproj -c Debug 2>&1 | grep -E "Build succeeded|error"; "/mnt/c/Program Files/dotnet/dotnet.exe" test DeckFlow.Core.Tests/DeckFlow.Core.Tests.csproj --filter "FullyQualifiedName~WhisperTranscriptionServiceTests" 2>&1 | tail -6</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'OpenAI" Version="2.10.0"' DeckFlow.Core/DeckFlow.Core.csproj` == 1; no package beyond OpenAI + YoutubeExplode (from 20-02) added
    - `grep -c "ClientRetryPolicy" DeckFlow.Core/Integration/WhisperTranscriptionService.cs` ≥1 AND `grep -c "InfiniteTimeSpan" ...` ≥1 (SDK resilience disabled — P3)
    - `grep -c "FromMinutes(12)" DeckFlow.Core/Integration/WhisperResiliencePipeline.cs` ≥1 AND `grep -c "ExecuteAsync" DeckFlow.Core/Integration/WhisperTranscriptionService.cs` ≥1 (transcribe runs through the concrete 12min Polly pipeline — Codex HIGH-4); test (c) asserts invocation-through-wrapper
    - `grep -c "WouldExceedCapAsync" DeckFlow.Core/Integration/WhisperTranscriptionService.cs` ≥1 (cap READ gate before call)
    - `grep -ci "RecordCallAsync\|InsertTranscript\|UpdateTranscriptStatus\|ContentVideoStore" DeckFlow.Core/Integration/WhisperTranscriptionService.cs` == 0 (D-11 — service persists nothing; the verb owns the ledger write + transcript)
    - `grep -c "new HttpClient()" DeckFlow.Core/Integration/WhisperTranscriptionService.cs` == 0 (ctor-injected; ban scoped to Core/Integration)
    - `grep -c "\.Timeout" DeckFlow.Core/Integration/WhisperTranscriptionService.cs` == 0 (service does NOT set httpClient.Timeout — the composition root owns it; 20-04 Task 2)
    - `grep -ci "OPENAI_API_KEY" DeckFlow.Core/Integration/WhisperTranscriptionService.cs` ≥1 read from Environment; no key literal committed
    - Over-cap test proves NO transcribe invocation; chunk test proves ordered concatenation across 2 chunks; build clean
    - DeckFlow.Core gains NO Microsoft.AspNetCore.* / Microsoft.Extensions.Http reference
  </acceptance_criteria>
  <done>Whisper service gates on the cap (READ), runs every OpenAI call through a concrete 12min Polly pipeline, chunks >24MB audio and concatenates in order, returns billed seconds/cost/status, persists NOTHING, and degrades to Failed without crashing; covered by seam tests including a Polly-invocation assertion.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local harvester → OpenAI API | OPENAI_API_KEY (secret) + audio bytes cross to a third party |
| local harvester → YouTube | untrusted caption/stream content crosses in (handled in 20-02) |
| ffmpeg subprocess | input path string interpolated into process arguments |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-01 | Information Disclosure | OPENAI_API_KEY | mitigate | Read from Environment only; never logged, never committed (CLAUDE.md secrets rule); grep gate confirms no literal key |
| T-20-02 | Denial of Service (cost) | Whisper spend | mitigate | WouldExceedCapAsync READ before every call; over-cap returns SkippedOverCap + no API call + no ledger write (KB-05/D-06); the verb records spend only on success |
| T-20-03 | Tampering | ffmpeg arguments | mitigate | inputPath is a local temp file path the harvester itself wrote (AudioDownloadResult.TempFilePath, not user-supplied); quoted in args; single-user local run — accept residual |
| T-20-SC | Tampering | YoutubeExplode 6.6.0 + OpenAI 2.10.0 installs | accept | Both verified against official GitHub repos + NuGet flat-container in RESEARCH Package Legitimacy Audit; canonical Tyrrrz/openai repos; RESEARCH authorizes treating as approved without a checkpoint (registry+source == slopcheck OK path); D-02 user-approved |
</threat_model>

<verification>
- KB-04: Whisper runtime RETURNS transcripts (source=whisper) + billed seconds/cost; AudioClient wired via HttpClientPipelineTransport with SDK resilience disabled and the transcribe call run through a concrete 12min Polly pipeline. Persistence (transcript row + ledger row) is the harvest verb's job (D-11).
- KB-05: plain local cap CHECK gates calls; over-cap returns SkippedOverCap; NO advisory-lock/SERIALIZABLE/kill-switch.
- ffmpeg absent / chunk-fail → Outcome=Failed + continue (D-05); current machine has no ffmpeg, so the degrade path is the live default.
- SC3 timeouts (15min HttpClient set by composition root / 12min Polly here) and SC6 (env-only key) honored.
</verification>

<success_criteria>
- The cap check provably prevents API calls when over cap, with zero spend rows in that case (the service never writes; the verb skips the record on a SkippedOverCap result).
- A successful call returns billed seconds + cost + concatenated body and the transcribe delegate ran through the Polly wrapper.
- No new packages beyond the two approved; Core stays free of Web/host dependencies; the service persists nothing.
</success_criteria>

<output>
Create `.planning/phases/20-content-kb-ingestion-transcription-local/20-03-SUMMARY.md` when done.
</output>
