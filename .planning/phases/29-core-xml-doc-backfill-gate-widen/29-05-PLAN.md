---
phase: 29-core-xml-doc-backfill-gate-widen
plan: 05
type: execute
wave: 2
depends_on: [29-01, 29-02, 29-03, 29-04]
files_modified:
  - .editorconfig
autonomous: false
requirements: [HSK-01]

must_haves:
  truths:
    - "The injected __TempUndocProbe class fires CS1591 with the new [DeckFlow.Core/**.cs] gate active (proves the gate is real, not a no-op)"
    - "After removing the probe, dotnet build DeckFlow.Core with -warnaserror:CS1591,CS1573,CS1587 passes with 0 errors from a clean obj/"
    - "The existing [DeckFlow.Web/**.cs] gate section is unchanged"
    - "Full solution build (DeckFlow.sln -c Release) reports 0 errors and 0 warnings; DeckFlow.Core.Tests produces no gated warnings"
    - "This .editorconfig edit is the FINAL commit of Phase 29"
  artifacts:
    - path: ".editorconfig"
      provides: "Additive [DeckFlow.Core/**.cs] CS1591/1573/1587 = warning gate section"
      contains: "[DeckFlow.Core/**.cs]"
  key_links:
    - from: ".editorconfig [DeckFlow.Core/**.cs] section"
      to: "DeckFlow.Core/**/*.cs"
      via: "path-scoped diagnostic severity = warning"
      pattern: "DeckFlow.Core/\\*\\*.cs"
---

<objective>
Widen the XML doc-comment warning gate to DeckFlow.Core by appending a new `[DeckFlow.Core/**.cs]` section to `.editorconfig` (CS1591/CS1573/CS1587 = warning), leaving the existing `[DeckFlow.Web/**.cs]` Phase 23 gate untouched. Prove the gate is real with the inject-probe technique, then verify the full solution builds clean.

Purpose: This is the closing requirement of HSK-01 and the ROADMAP guard rule (gate widen is the FINAL commit of Phase 29). After this, undocumented Core public sites become build warnings across the solution.
Output: One additive `.editorconfig` block; probe-proven gate; clean full-solution build.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<scope_fence>
ALLOWED FILE SET (the ONLY file to be committed by this plan):
- .editorconfig (one additive block only)

You may create and delete a TEMPORARY `DeckFlow.Core/__TempUndocProbe.cs` file ONLY to prove the gate fires; it MUST be removed before commit and MUST NOT appear in `git status` or any commit.
Do NOT touch the existing `[DeckFlow.Web/**.cs]` section or the global `[*.cs]` suppressor lines. Do NOT touch any source file (all backfill is done by Wave-1 plans 29-01..04). Ignore tasks belonging to plans 29-01, 29-02, 29-03, 29-04.
Before starting, confirm all four Wave-1 SUMMARYs exist (`29-01-SUMMARY.md` .. `29-04-SUMMARY.md`); if any is missing, STOP - this plan must not run until backfill is complete.
</scope_fence>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-core-xml-doc-backfill-gate-widen/29-RESEARCH.md
@.planning/phases/29-core-xml-doc-backfill-gate-widen/29-PATTERNS.md
@.editorconfig
</context>

<constraints>
From CLAUDE.md + 29-RESEARCH.md (full authority - every constraint is blocking):
- `.editorconfig` is on the CLAUDE.md Do-Not-Modify list. This edit requires explicit user approval at execution time (the human-verify checkpoint below). Do not commit before approval.
- The edit is ADDITIVE: append a new `[DeckFlow.Core/**.cs]` section AFTER the existing Web section (currently lines 111-115). Do NOT modify the Web section or the global `[*.cs]` suppressor (lines 96-98).
- Preserve LF line endings.
- `[DeckFlow.Core/**.cs]` must NOT match `DeckFlow.Core.Tests/` (different prefix - confirmed by RESEARCH). The test project's internal members must remain ungated.
- Probe `__TempUndocProbe.cs` is NEVER committed - always removed and absence verified.
- Use `-warnaserror:CS1591,CS1573,CS1587` (all three) - CS1591 alone false-greens on a surviving CS1573. Do NOT pass `-warnaserror-` (invalid switch).
- Build env: `"/mnt/c/Program Files/dotnet/dotnet.exe"`. Clean `obj/` before the gate build.
- Commits: plain default-author; one logical change per commit. This is the FINAL commit of the phase.
</constraints>

<tasks>

<task type="auto">
  <name>Task 1: Append the [DeckFlow.Core/**.cs] gate section and prove it fires with an injected probe</name>
  <files>.editorconfig</files>
  <read_first>
    - .editorconfig (read the existing [DeckFlow.Web/**.cs] section at lines ~111-115 and the global [*.cs] suppressor at ~96-98 - both must remain untouched)
    - .planning/phases/29-core-xml-doc-backfill-gate-widen/29-PATTERNS.md (editorconfig gate-widen pattern - the exact additive block)
    - .planning/phases/29-core-xml-doc-backfill-gate-widen/29-RESEARCH.md (Gate-widen edit exact shape; Gate-widen probe; Common Pitfalls 1 and 4)
  </read_first>
  <action>
    Append (do NOT insert before the Web section) a new section after the existing `[DeckFlow.Web/**.cs]` block: a `[DeckFlow.Core/**.cs]` header, a comment line `# Phase 29 HSK-01: XML doc-comment gate widened to DeckFlow.Core`, then the three lines setting `dotnet_diagnostic.CS1591.severity = warning`, `dotnet_diagnostic.CS1573.severity = warning`, `dotnet_diagnostic.CS1587.severity = warning` (exact shape per 29-PATTERNS.md gate-widen pattern). Then prove the gate is live: create `DeckFlow.Core/__TempUndocProbe.cs` containing a public sealed class with one public `int X { get; init; }` auto-property (no doc comments), run a clean non-incremental Core build, and confirm CS1591 fires on `__TempUndocProbe` AND on `__TempUndocProbe.X`. If it does NOT fire, the gate is not taking effect - STOP and report (do not commit). Remove `__TempUndocProbe.cs` and confirm it is gone from `git status`.
  </action>
  <verify>
    <automated>DOTNET="/mnt/c/Program Files/dotnet/dotnet.exe"; printf '%s\n' 'namespace DeckFlow.Core;' 'public sealed class __TempUndocProbe { public int X { get; init; } }' > DeckFlow.Core/__TempUndocProbe.cs; "$DOTNET" build DeckFlow.Core/DeckFlow.Core.csproj --no-incremental 2>&1 | grep "__TempUndocProbe" | grep -c "warning CS1591" > /tmp/29-05-probe.log; rm -f DeckFlow.Core/__TempUndocProbe.cs; N=$(cat /tmp/29-05-probe.log); if [ "$N" -ge 1 ]; then echo "OK gate fires ($N CS1591 on probe)"; else echo "FAIL gate did not fire - do not commit"; fi</automated>
  </verify>
  <acceptance_criteria>
    - `.editorconfig` contains a literal `[DeckFlow.Core/**.cs]` section with the three `severity = warning` lines.
    - `grep -c "\[DeckFlow.Web/\*\*.cs\]" .editorconfig` is still exactly 1 and the Web section's three lines are unchanged (`git diff .editorconfig` shows only an appended block, no edits to existing lines).
    - The probe build fired at least one CS1591 on `__TempUndocProbe`.
    - `DeckFlow.Core/__TempUndocProbe.cs` does not exist and is absent from `git status`.
  </acceptance_criteria>
  <done>The additive Core gate section is present, proven live by the probe, and no probe artifact remains.</done>
</task>

<task type="auto">
  <name>Task 2: Promote all three doc codes to errors from a clean obj/ and guard the full solution</name>
  <files>.editorconfig</files>
  <read_first>
    - .planning/phases/29-core-xml-doc-backfill-gate-widen/29-RESEARCH.md (Gate-widen build all-three-code promotion; Common Pitfall 6 build-is-the-gate)
  </read_first>
  <action>
    Remove `DeckFlow.Core/obj` and `DeckFlow.Core/bin`, then build `DeckFlow.Core/DeckFlow.Core.csproj -c Release --no-incremental -warnaserror:CS1591,CS1573,CS1587` via the Windows dotnet.exe and confirm 0 errors (this is the real gate - all four Wave-1 backfills must have closed every site). As a belt-and-suspenders check, grep the build log for any surviving `warning CS(1591|1573|1587)`. Then build the full solution `DeckFlow.sln -c Release` and confirm `0 Warning(s) 0 Error(s)` - this proves the Web gate still holds and that `DeckFlow.Core.Tests/` (574+ internal undocumented members) is NOT gated by the new `[DeckFlow.Core/**.cs]` path pattern. Do not commit yet - the human-verify checkpoint (Task 3) gates the commit.
  </action>
  <verify>
    <automated>DOTNET="/mnt/c/Program Files/dotnet/dotnet.exe"; rm -rf DeckFlow.Core/obj DeckFlow.Core/bin; "$DOTNET" build DeckFlow.Core/DeckFlow.Core.csproj -c Release --no-incremental -warnaserror:CS1591,CS1573,CS1587 2>&1 | tee /tmp/29-05-gate.log | tail -3; if grep -qE "warning CS(1591|1573|1587)" /tmp/29-05-gate.log; then echo "FAIL doc warning survived"; else echo "OK no doc warnings"; fi; "$DOTNET" build DeckFlow.sln -c Release 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - Core build with `-warnaserror:CS1591,CS1573,CS1587` from a clean obj/ reports 0 errors.
    - `/tmp/29-05-gate.log` contains no `warning CS1591`, `CS1573`, or `CS1587` lines.
    - Full-solution `DeckFlow.sln -c Release` build reports `0 Warning(s)` and `0 Error(s)`.
    - No `DeckFlow.Core.Tests` CS1591 warnings appear in either build (path prefix excludes the test project).
  </acceptance_criteria>
  <done>Core builds clean as errors with all three doc codes promoted; full solution builds with 0 warnings/0 errors.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <what-built>
    Appended an additive `[DeckFlow.Core/**.cs]` section to `.editorconfig` setting CS1591/CS1573/CS1587 to `warning`. The gate was proven live (injected `__TempUndocProbe` fired CS1591, then removed) and the full solution builds at 0 warnings / 0 errors with all four Wave-1 backfill plans complete.
  </what-built>
  <how-to-verify>
    1. Review `git diff .editorconfig` - confirm it is ONLY an appended block; the existing `[DeckFlow.Web/**.cs]` section and the global `[*.cs]` suppressor are untouched. (`.editorconfig` is on the Do-Not-Modify list, so this edit needs your explicit approval.)
    2. Confirm the new section reads exactly: header `[DeckFlow.Core/**.cs]`, the Phase 29 comment, and the three `dotnet_diagnostic.CS{1591,1573,1587}.severity = warning` lines.
    3. Optionally re-run `"/mnt/c/Program Files/dotnet/dotnet.exe" build DeckFlow.sln -c Release` and confirm `0 Warning(s) 0 Error(s)`.
    4. Confirm `git status` shows no `DeckFlow.Core/__TempUndocProbe.cs` and no `DeckFlow.Core/.editorconfig`.
  </how-to-verify>
  <resume-signal>Type "approved" to commit this as the FINAL commit of Phase 29, or describe required changes.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer edit -> build config | `.editorconfig` change alters warning severity solution-wide; an over-broad path glob could mis-gate the test project |
| temp probe file -> repo | A lingering `__TempUndocProbe.cs` would ship a junk public type into DeckFlow.Core |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-05-01 | Tampering | `.editorconfig` Do-Not-Modify file | mitigate | Blocking human-verify checkpoint; git diff acceptance asserts additive-only edit; Web section + global suppressor untouched |
| T-29-05-02 | Tampering | `[DeckFlow.Core/**.cs]` path glob mis-matching DeckFlow.Core.Tests | mitigate | Full-solution build acceptance asserts 0 warnings and no DeckFlow.Core.Tests CS1591 (path prefix excludes tests, RESEARCH-confirmed) |
| T-29-05-03 | Tampering | Temp `__TempUndocProbe.cs` | mitigate | Probe removed and absence asserted before commit; never committed |
| T-29-05-04 | Spoofing | False-green gate (suppressor still active) | mitigate | Inject-probe proves CS1591 actually fires before trusting the gate; -warnaserror covers all three codes |
| T-29-05-05 | Denial of Service | Build pipeline | accept | Doc/editorconfig change is compile-stripped/severity-only; zero runtime/behavior change possible |
| T-29-05-SC | Tampering | npm/pip/cargo installs | n/a | No package installs in this phase |
</threat_model>

<verification>
- Inject-probe fires CS1591 (gate is real).
- Clean-obj Core build with -warnaserror:CS1591,CS1573,CS1587 = 0 errors.
- Full-solution `DeckFlow.sln -c Release` = 0 warnings / 0 errors; no DeckFlow.Core.Tests gated warnings.
- `git diff .editorconfig` is additive-only; Web section + global suppressor unchanged.
- No probe artifacts in `git status`.
- Human-verify checkpoint approved before the final commit.
</verification>

<success_criteria>
The `[DeckFlow.Core/**.cs]` gate section exists as an additive `.editorconfig` block, proven live by the injected probe; DeckFlow.Core builds clean with all three doc codes promoted to errors from a clean obj/; the full solution builds at 0 warnings / 0 errors with the Web gate intact and the test project ungated; the edit is approved at the human checkpoint and committed as the FINAL commit of Phase 29.
</success_criteria>

<output>
Create `.planning/phases/29-core-xml-doc-backfill-gate-widen/29-05-SUMMARY.md` when done.
</output>
