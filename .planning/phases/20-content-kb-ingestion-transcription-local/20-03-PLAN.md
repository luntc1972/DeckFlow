---
phase: 20-content-kb-ingestion-transcription-local
plan: 03
type: execute
wave: 2
depends_on: ["20-01", "20-02"]
files_modified:
  - DeckFlow.Core/Integration/IFfmpegAudioChunker.cs
  - DeckFlow.Core/Integration/FfmpegAudioChunker.cs
  - DeckFlow.Core/Integration/IWhisperTranscriptionService.cs
  - DeckFlow.Core/Integration/WhisperTranscriptionService.cs
  - DeckFlow.Core/DeckFlow.Core.csproj
  - DeckFlow.Core.Tests/WhisperTranscriptionServiceTests.cs
  - DeckFlow.Core.Tests/FfmpegAudioChunkerTests.cs
autonomous: true
requirements: [KB-04, KB-05]
must_haves:
  truths:
    - "Before any Whisper API call, the cap check (monthly ledger total + duration*$0.006/min) is consulted; over-cap returns a skipped_over_cap outcome and makes NO API call"
    - "A successful Whisper transcription records exactly one spend ledger row (seconds_billed + cost_usd) and persists transcript text with source=whisper"
    - "The OpenAI AudioClient is wired so Polly owns all resilience: SDK retry disabled (ClientRetryPolicy(0)) and SDK NetworkTimeout = InfiniteTimeSpan; HttpClient timeout 15min, Polly timeout 12min"
    - "Audio >24MB is chunked via shelled-out ffmpeg; if ffmpeg is absent or a chunk op fails, the video is marked failed and the run continues (no crash)"
    - "OPENAI_API_KEY is read from the environment only, never committed"
  artifacts:
    - path: "DeckFlow.Core/Integration/WhisperTranscriptionService.cs"
      provides: "OpenAI 2.10.0 AudioClient via HttpClientPipelineTransport + cap-check gate + ledger write + Func<> seam"
      contains: "HttpClientPipelineTransport"
    - path: "DeckFlow.Core/Integration/FfmpegAudioChunker.cs"
      provides: "Process.Start ffmpeg segment chunker + presence verification"
      contains: "ffmpeg"
    - path: "DeckFlow.Core.Tests/WhisperTranscriptionServiceTests.cs"
      provides: "cap-over-cap-skip + successful-transcribe-records-ledger via Func<> seam"
      contains: "WhisperTranscriptionServiceTests"
  key_links:
    - from: "DeckFlow.Core/Integration/WhisperTranscriptionService.cs"
      to: "DeckFlow.Core/Content/WhisperSpendLedger.cs"
      via: "WouldExceedCapAsync before call; RecordCallAsync after success"
      pattern: "WouldExceedCapAsync"
    - from: "DeckFlow.Core/Integration/WhisperTranscriptionService.cs"
      to: "DeckFlow.Core/Content/ContentVideoStore.cs"
      via: "UpdateTranscriptStatusAsync(skipped_over_cap | whisper | failed) + InsertTranscriptAsync(whisper)"
      pattern: "UpdateTranscriptStatusAsync"
    - from: "DeckFlow.Core/Integration/WhisperTranscriptionService.cs"
      to: "OpenAI AudioClient"
      via: "ClientRetryPolicy(0) + NetworkTimeout InfiniteTimeSpan (Polly owns resilience)"
      pattern: "ClientRetryPolicy"
---

<objective>
Build the Whisper transcription runtime (KB-04) and wire the plain local spend-cap check (KB-05) in `DeckFlow.Core/Integration`, plus the ffmpeg shell-out chunker for audio >24MB. Depends on Plan 20-01 (`UpdateTranscriptStatusAsync`) and Plan 20-02 (`ITranscriptSource`/result shapes + the Func<> precedent now established in Integration).

Purpose: Whisper is the captions-absent fallback. The cap check (D-06) gates every call: projected monthly total = `WhisperSpendLedger.GetMonthlyTotalAsync(monthKey)` + duration*$0.006/min vs `DECKFLOW_WHISPER_MONTHLY_CAP_USD` (default $15). Over-cap → mark `skipped_over_cap`, make NO API call. The OpenAI SDK must hand resilience to Polly (Pitfall P3): `ClientRetryPolicy(0)` + `NetworkTimeout = InfiniteTimeSpan`. ffmpeg is a documented local prerequisite (D-05, currently NOT installed) — absent → mark failed + continue, never crash.
Output: `IWhisperTranscriptionService` + impl with cap-check + ledger write + transcript persist + Func<> seam; `IFfmpegAudioChunker` + impl; OpenAI 2.10.0 added to DeckFlow.Core.csproj; xUnit tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-content-kb-ingestion-transcription-local/20-CONTEXT.md
@.planning/phases/20-content-kb-ingestion-transcription-local/20-RESEARCH.md
@.planning/phases/20-content-kb-ingestion-transcription-local/20-01-PLAN.md

<interfaces>
<!-- From DeckFlow.Core/Content/WhisperSpendLedger.cs (Phase 19 — wire these, do not modify) -->
- `Task<decimal> GetMonthlyTotalAsync(string yearMonth, CancellationToken)` — app-side sum over month_key
- `Task<bool> WouldExceedCapAsync(decimal projectedCallCostUsd, string monthKey, CancellationToken)` — total + projected > cap
- `Task RecordCallAsync(long videoId, int secondsBilled, decimal costUsd, string monthKey, CancellationToken)` — one row per real call; videoId must be ≥1
- cap env key: `DECKFLOW_WHISPER_MONTHLY_CAP_USD` (default 15.00m); month key format `yyyy-MM`

<!-- From DeckFlow.Core/Content/ContentVideoStore.cs (Plan 20-01 adds UpdateTranscriptStatusAsync) -->
- `Task<long> InsertTranscriptAsync(long videoId, string source, string body, CancellationToken)` — source ∈ {captions, whisper}
- `Task UpdateTranscriptStatusAsync(long videoId, string status, CancellationToken)` — NEW in 20-01; status ∈ TranscriptStatus allowlist

<!-- From DeckFlow.Core/Knowledge/ContentModels.cs -->
- `TranscriptSource.Whisper = "whisper"`; `TranscriptStatus.Whisper/.Failed/.SkippedOverCap`

<!-- OpenAI 2.10.0 verified facts (RESEARCH HIGH confidence, Pattern 2) -->
- `new OpenAIClientOptions { Transport = new HttpClientPipelineTransport(httpClient), RetryPolicy = new ClientRetryPolicy(maxRetries: 0), NetworkTimeout = Timeout.InfiniteTimeSpan }`
- `new AudioClient("whisper-1", new ApiKeyCredential(apiKey), options)`
- `audioClient.TranscribeAudioAsync(Stream, audioFilename, new AudioTranscriptionOptions { ResponseFormat = AudioTranscriptionFormat.Verbose }, ct)`
- billed seconds: prefer `result.Value.Usage.Duration` (TimeSpan, added in 2.10.0); fall back to `result.Value.Duration` (TimeSpan?, Verbose). cost = seconds/60 * 0.006m
- `HttpClientPipelineTransport` + `ApiKeyCredential` + `ClientRetryPolicy` live in System.ClientModel.Primitives (transitive from OpenAI 2.10.0)

<!-- ffmpeg (RESEARCH Pattern 4 / P4 / P7) -->
- 25MB Whisper limit; threshold 24MB margin. Pre-download size: YoutubeExplode stream manifest `IVideoStreamInfo.Size.Bytes`
- chunk: `ffmpeg -i "input" -f segment -segment_time 300 -c copy -reset_timestamps 1 "chunk_%04d.ext"` (300s safer than 600s per A3)
- ffmpeg presence: `Process.Start("ffmpeg", "-version")`, check ExitCode; ffmpeg NOT currently on PATH
- temp files in Path.GetTempPath(); delete in finally (no raw audio retained — D-15)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: FfmpegAudioChunker (shell-out + presence check)</name>
  <files>DeckFlow.Core/Integration/IFfmpegAudioChunker.cs, DeckFlow.Core/Integration/FfmpegAudioChunker.cs, DeckFlow.Core.Tests/FfmpegAudioChunkerTests.cs</files>
  <read_first>
    - .planning/phases/20-content-kb-ingestion-transcription-local/20-RESEARCH.md Pattern 4 (Process.Start segment command) + Pitfall P7 (absent ffmpeg → warn, mark failed, continue)
    - DeckFlow.Core/Integration/ArchidektApiDeckImporter.cs (namespace + doc-comment + sealed-class conventions)
  </read_first>
  <action>
    Create `IFfmpegAudioChunker` exposing `Task<bool> IsAvailableAsync(CancellationToken ct = default)` (runs `ffmpeg -version` via System.Diagnostics.Process, returns false on non-zero exit or process-start failure — never throws) and `Task<IReadOnlyList<string>> ChunkAsync(string inputPath, string outputDirectory, int segmentSeconds = 300, CancellationToken ct = default)` (returns the ordered chunk file paths). Implement `FfmpegAudioChunker` using `ProcessStartInfo("ffmpeg")` with `RedirectStandardError = true, UseShellExecute = false, CreateNoWindow = true`, arguments `-i "{inputPath}" -f segment -segment_time {segmentSeconds} -c copy -reset_timestamps 1 "{outputPattern}"` where outputPattern = `chunk_%04d{ext}`; `await process.WaitForExitAsync(ct)`; on non-zero exit throw `InvalidOperationException` with the captured stderr tail; return `Directory.GetFiles(outputDirectory, "chunk_*{ext}").OrderBy(f => f, StringComparer.Ordinal)`. Keep `IsAvailableAsync` exception-safe (try/catch around Process.Start). No NuGet ffmpeg wrapper (D-05). No raw audio retained by the chunker itself — callers delete temp dirs in finally. Add `FfmpegAudioChunkerTests`: a test asserting `IsAvailableAsync` returns false gracefully when ffmpeg is absent (run the real method — in CI/local without ffmpeg it returns false; mark `[Trait]` so it is environment-tolerant) and a test asserting the arguments string is constructed with `-segment_time 300 -c copy` (extract the arg-builder to a `internal static string BuildSegmentArguments(...)` pure helper and unit-test that directly — avoids needing ffmpeg installed).
  </action>
  <verify>
    <automated>"/mnt/c/Program Files/dotnet/dotnet.exe" build DeckFlow.Core/DeckFlow.Core.csproj -c Debug 2>&1 | grep -E "Build succeeded|error"; "/mnt/c/Program Files/dotnet/dotnet.exe" test DeckFlow.Core.Tests/DeckFlow.Core.Tests.csproj --filter "FullyQualifiedName~FfmpegAudioChunkerTests" 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "IsAvailableAsync" DeckFlow.Core/Integration/FfmpegAudioChunker.cs` ≥1; `grep -c "segment_time" DeckFlow.Core/Integration/FfmpegAudioChunker.cs` ≥1
    - `grep -c "BuildSegmentArguments" DeckFlow.Core/Integration/FfmpegAudioChunker.cs` ≥1 (pure, testable arg builder)
    - No ffmpeg NuGet package added to DeckFlow.Core.csproj
    - Build clean; arg-builder test passes without ffmpeg installed
  </acceptance_criteria>
  <done>FfmpegAudioChunker shells out for segmenting, reports availability without throwing, and its arg-builder is unit-tested without ffmpeg present.</done>
</task>

<task type="auto">
  <name>Task 2: WhisperTranscriptionService — cap-check gate + AudioClient + ledger write</name>
  <files>DeckFlow.Core/DeckFlow.Core.csproj, DeckFlow.Core/Integration/IWhisperTranscriptionService.cs, DeckFlow.Core/Integration/WhisperTranscriptionService.cs, DeckFlow.Core.Tests/WhisperTranscriptionServiceTests.cs</files>
  <read_first>
    - .planning/phases/20-content-kb-ingestion-transcription-local/20-RESEARCH.md Pattern 2 (AudioClient + HttpClientPipelineTransport), "Cap check before Whisper call" example, Pitfall P3 (SDK timeout) + P4 (Duration vs Usage.Duration)
    - DeckFlow.Core/Content/WhisperSpendLedger.cs (WouldExceedCapAsync / GetMonthlyTotalAsync / RecordCallAsync signatures — wire, do not modify)
    - DeckFlow.Core/Content/IContentVideoStore.cs + the 20-01 UpdateTranscriptStatusAsync addition (status persistence target)
    - DeckFlow.Web/Services/CardLookupService.cs lines 50-95 (Func<> seam to mirror for the transcribe call)
    - DeckFlow.Core/DeckFlow.Core.csproj (add OpenAI 2.10.0 PackageReference only)
  </read_first>
  <action>
    Add `<PackageReference Include="OpenAI" Version="2.10.0" />` to DeckFlow.Core.csproj (D-02 approved). Create `IWhisperTranscriptionService` exposing `Task<WhisperTranscriptionResult> TranscribeAsync(long videoId, int estimatedDurationSeconds, Stream audioStream, string audioFilename, CancellationToken ct = default)` and a `public sealed record WhisperTranscriptionResult { get; init; }` with `string FinalStatus` (TranscriptStatus const), `int SecondsBilled`, `decimal CostUsd`, `string? Body`. Implement `WhisperTranscriptionService` taking ctor deps: `IWhisperSpendLedger ledger`, `IContentVideoStore videoStore`, `HttpClient httpClient`, and an optional `Func<...>` transcribe-override for the test seam (mirror CardLookupService); the production transcribe delegate builds the `AudioClient` per RESEARCH Pattern 2 (HttpClientPipelineTransport + ClientRetryPolicy(0) + NetworkTimeout InfiniteTimeSpan; httpClient.Timeout = 15min set by the caller/ctor; reads `OPENAI_API_KEY` from Environment in the production path only). Flow in TranscribeAsync: compute monthKey = `DateTime.UtcNow.ToString("yyyy-MM")`; projectedCost = `estimatedDurationSeconds / 60m * 0.006m`; if `await ledger.WouldExceedCapAsync(projectedCost, monthKey, ct)` → `await videoStore.UpdateTranscriptStatusAsync(videoId, TranscriptStatus.SkippedOverCap, ct)` and return a SkippedOverCap result with NO API call; else invoke the transcribe delegate; on success compute billedSeconds (prefer `Usage.Duration`, fall back to `Duration`), cost = billedSeconds/60m*0.006m; `await ledger.RecordCallAsync(videoId, billedSeconds, cost, monthKey, ct)`; `await videoStore.InsertTranscriptAsync(videoId, TranscriptSource.Whisper, body, ct)`; `await videoStore.UpdateTranscriptStatusAsync(videoId, TranscriptStatus.Whisper, ct)`; return Whisper result. On any transcription exception (including a missing OPENAI_API_KEY surfaced as a clear message) → `UpdateTranscriptStatusAsync(videoId, TranscriptStatus.Failed, ct)` and return a Failed result (no ledger row — RecordCallAsync only on success per D-10). Keep methods ≤30 lines (extract the AudioClient build + the success-accounting to private helpers). Add `WhisperTranscriptionServiceTests` via the Func<> seam + a fake/real ledger over a temp SQLite file: (a) over-cap → result.FinalStatus == SkippedOverCap AND the transcribe delegate was never invoked AND no ledger row written, (b) under-cap success → exactly one ledger row with the expected seconds/cost AND a whisper transcript row AND status whisper, (c) transcribe delegate throws → status failed AND no ledger row. No live OpenAI calls.
  </action>
  <verify>
    <automated>"/mnt/c/Program Files/dotnet/dotnet.exe" build DeckFlow.Core/DeckFlow.Core.csproj -c Debug 2>&1 | grep -E "Build succeeded|error"; "/mnt/c/Program Files/dotnet/dotnet.exe" test DeckFlow.Core.Tests/DeckFlow.Core.Tests.csproj --filter "FullyQualifiedName~WhisperTranscriptionServiceTests" 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'OpenAI" Version="2.10.0"' DeckFlow.Core/DeckFlow.Core.csproj` == 1; no package beyond OpenAI + YoutubeExplode (from 20-02) added
    - `grep -c "ClientRetryPolicy" DeckFlow.Core/Integration/WhisperTranscriptionService.cs` ≥1 AND `grep -c "InfiniteTimeSpan" ...` ≥1 (P3 mitigation present)
    - `grep -c "WouldExceedCapAsync" DeckFlow.Core/Integration/WhisperTranscriptionService.cs` ≥1 (cap gate before call)
    - `grep -c "new HttpClient()" DeckFlow.Core/Integration/WhisperTranscriptionService.cs` == 0 (HttpClient ctor-injected)
    - `grep -ci "OPENAI_API_KEY" DeckFlow.Core/Integration/WhisperTranscriptionService.cs` ≥1 read from Environment; no key literal committed
    - Over-cap test proves NO transcribe-delegate invocation and NO ledger row; success test proves exactly one ledger row + whisper status
    - Build clean; DeckFlow.Core gains NO Microsoft.AspNetCore.* / Microsoft.Extensions.Http reference
  </acceptance_criteria>
  <done>Whisper service gates on the cap, hands resilience to Polly, records spend only on success, persists transcript + status, and degrades to failed without crashing; covered by seam tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local harvester → OpenAI API | OPENAI_API_KEY (secret) + audio bytes cross to a third party |
| local harvester → YouTube | untrusted caption/stream content crosses in (handled in 20-02) |
| ffmpeg subprocess | input path string interpolated into process arguments |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-01 | Information Disclosure | OPENAI_API_KEY | mitigate | Read from Environment only; never logged, never committed (CLAUDE.md secrets rule); grep gate confirms no literal key |
| T-20-02 | Denial of Service (cost) | Whisper spend | mitigate | WouldExceedCapAsync before every call; over-cap skips + marks skipped_over_cap (KB-05/D-06) |
| T-20-03 | Tampering | ffmpeg arguments | mitigate | inputPath is a local temp file path the harvester itself wrote (not user-supplied); quoted in args; single-user local run — accept residual |
| T-20-SC | Tampering | YoutubeExplode 6.6.0 + OpenAI 2.10.0 installs | accept | Both verified against official GitHub repos + NuGet flat-container in RESEARCH Package Legitimacy Audit; canonical Tyrrrz/openai repos; RESEARCH authorizes treating as approved without a checkpoint (registry+source == slopcheck OK path); D-02 user-approved |
</threat_model>

<verification>
- KB-04: Whisper runtime persists transcripts (source=whisper) + per-call ledger rows; AudioClient wired via HttpClientPipelineTransport with SDK resilience disabled.
- KB-05: plain local cap check gates calls; over-cap marks skipped_over_cap; NO advisory-lock/SERIALIZABLE/kill-switch.
- ffmpeg absent → mark failed + continue (D-05); current machine has no ffmpeg, so degrade path is the live default.
- SC3 timeouts (15min HttpClient / 12min Polly) and SC6 (env-only key) honored.
</verification>

<success_criteria>
- The cap check provably prevents API calls when over cap, with zero spend rows in that case.
- A successful call writes exactly one ledger row and one whisper transcript, and flips status to whisper.
- No new packages beyond the two approved; Core stays free of Web/host dependencies.
</success_criteria>

<output>
Create `.planning/phases/20-content-kb-ingestion-transcription-local/20-03-SUMMARY.md` when done.
</output>
