---
phase: 37-retire-clip-injection
plan: 02
type: execute
wave: 2
depends_on: ["37-01"]
autonomous: true
requirements: [RET-03, RET-04, RET-06]
files_modified:
  # Un-dark
  - DeckFlow.Web/Services/FeatureFlags/FeatureFlagStore.cs
  # Browse-page selection-UI strip (KEPT page, partial edit)
  - DeckFlow.Web/Views/ContentKb/Index.cshtml
  # Admin score-preview removal
  - DeckFlow.Web/Controllers/Admin/AdminContentKbController.cs
  - DeckFlow.Web/Views/AdminContentKb/Index.cshtml
  - DeckFlow.Web/Models/AdminContentKbViewModel.cs
  - DeckFlow.Web/wwwroot/ts/content-kb-admin.ts                       # DELETE (entirely score-preview)
  - DeckFlow.Web.Tests/AdminContentKbControllerTests.cs               # EDIT
  # RET-06 pointer note + nav copy rewrite
  - DeckFlow.Web/Views/Deck/DeckAnalysis.cshtml
  - DeckFlow.Web/Views/Deck/Home.cshtml
  - DeckFlow.Web/Views/Shared/_DeckToolTabs.cshtml
  # RET-04 XSS regression test (NEW)
  - DeckFlow.Web.Tests/ContentKbMarkdigXssTests.cs                    # NEW

must_haves:
  truths:
    - "/content-kb is reachable in a fresh DB (content.kb.enabled default ON) and renders distilled entries"
    - "The /content-kb browse page no longer shows the expert pin/follow tray, pin/follow buttons, 'Run analysis' CTA, or kb-selection script"
    - "Harvested clip/summary/title text on KB views is HTML-encoded (no Html.Raw); the Markdig pipeline keeps .DisableHtml() so <script> renders inert"
    - "The DeckAnalysis page carries a note + link to /content-kb for copyable expert prompts"
    - "The admin curation grid (publish/unpublish, evergreen toggle, reload-seed, instant filter) is intact; the relevance-score preview is gone"
  artifacts:
    - path: "DeckFlow.Web.Tests/ContentKbMarkdigXssTests.cs"
      provides: "RET-04 regression: Markdown.ToHtml of a <script> payload through the .DisableHtml() pipeline contains no <script>"
  key_links:
    - from: "DeckFlow.Web/Views/Deck/DeckAnalysis.cshtml"
      to: "/content-kb"
      via: "RET-06 pointer note + link where the removed accordion was"
      pattern: "/content-kb"
    - from: "ContentKbController Markdig pipeline"
      to: "Detail.cshtml @Model.RenderedHtml"
      via: ".DisableHtml() preserved (XSS-safe render of harvested text)"
      pattern: "DisableHtml"
---

<objective>
Un-dark the `/content-kb` browse-site (flip `content.kb.enabled` seed default ON) while guaranteeing the now-public, harvested (untrusted) text stays XSS-safe (RET-04); strip the dead expert-selection UI off the KEPT browse page and the admin relevance-score preview; and replace the removed auto-injection with a manual pointer note linking to the KB's existing copyable prompts (RET-06). Confirm the KB reference path (harvest/distill + browse + curation) remains intact (RET-03).

Purpose: After Plan 37-01 removed the injection path, the browse-site is the surviving KB value. Un-darking it exposes harvested text to the public, so the XSS posture (already correct via Razor `@` + Markdig `.DisableHtml()`) must be asserted and preserved, and the page must not keep buttons that POST to the deleted typeahead API.
Output: `/content-kb` live on fresh DBs, XSS-regression test, slimmed browse + admin pages, accurate nav copy, and a deck-analysis → KB pointer.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md

## Executor: Codex (cross-AI dispatch)
This plan is implemented by Codex (gpt-5.4, medium). Claude reviews.

## ALLOWED FILE SET — HARD FENCE (public repo; do NOT exceed)
Codex MUST edit/create/delete ONLY the files in this plan's `files_modified` frontmatter.
- Do NOT re-touch anything owned by Plan 37-01 (prompt variants, DeckAnalysisPacketService, request/viewmodel, packet store/cache, DeckController — EXCEPT `DeckAnalysis.cshtml`, which BOTH plans edit: 01 removed the accordion/panel/widget; 02 adds the RET-06 pointer note + may rewrite the instructions block. Plan 02 runs in a LATER wave so 01's edits are already committed — only ADD the note; do not reintroduce removed markup).
- Do NOT touch `ContentKbController.cs` logic except to CONFIRM (read-only) the Markdig pipeline keeps `.DisableHtml()`. If a change is needed there, STOP and surface it.
- Do NOT touch Core content stores, `ContentKbClipParser`, `ContentSiteIndexStore`, the CLI, `kb-entry-filter.ts`, or `content-kb.ts` — all KEPT.
- Do NOT introduce ANY `Html.Raw` / `MarkupString` / `IHtmlContent` / `WriteLiteral` on harvested text. This is a blocking acceptance criterion (RET-04).
- No "Format Document" / reflow. Edit only target lines. Preserve LF. Do NOT collapse `{ get; init; }` → `{ get; }` (KbEntryRow / AdminContentKbViewModel use init). Do NOT stage `wwwroot/js/*.js`. No new deps.
- Before starting: `git status` clean; ignore tasks outside this plan.

## Build gate (run after each task)
`"/mnt/c/Program Files/dotnet/dotnet.exe" build DeckFlow.sln -c Debug` MUST report 0/0.
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/37-retire-clip-injection/37-CONTEXT.md
@.planning/phases/37-retire-clip-injection/37-RESEARCH.md
@.planning/phases/37-retire-clip-injection/37-VALIDATION.md
@.planning/phases/37-retire-clip-injection/37-01-PLAN.md

<interfaces>
<!-- Current contracts/locations. From 37-RESEARCH.md (grep-verified). -->

Flag seed (FeatureFlagStore.cs): line 180 `('content.kb.enabled', FALSE)` (Postgres), line 190 `('content.kb.enabled', 0)` (SQLite). Un-dark = flip to TRUE / 1. (Prod row flipped separately via /Admin/Flags — manual, RET-03 smoke.)

Browse page (ContentKb/Index.cshtml): selection tray :62-71, per-entry pin/follow buttons :104-118, "Run analysis with this selection →" CTA :71, `kb-selection.js` script tag :145. REMOVE all; KEEP search/filter/listing (`data-kb-search`, kb-entry-filter.ts wiring, content-kb.ts).

Admin controller (AdminContentKbController.cs): `_relevanceService` field :26, ctor param :39 + guard :45 + assign :50; preview params `previewCommander`/`previewBracket`/`sortBy` + `previewActive`/`previewScores` :78-89; `RelevanceScore` projection :100; score-sort block :110-116. REMOVE all. KEEP GetAllRowsAsync, visibility filter, sources, status, SetEvergreen, SetVisibility, ReloadSeed.

Admin view (AdminContentKb/Index.cshtml): preview form (PreviewCommander/Bracket inputs) + score column/sort REMOVE; content-kb-admin.js script tag REMOVE. KEEP grid, publish/unpublish, evergreen toggle :233-243, per-source bulk, reload-seed, `data-kb-search` filter row :196.

Admin VM (AdminContentKbViewModel / KbEntryRow): remove `RelevanceScore` + any `PreviewCommander`/`PreviewBracket`/`SortBy`. KEEP `IsEvergreen`, `IsVisible`, etc. (init properties — do not collapse).

content-kb-admin.ts: 227 lines, ENTIRELY the preview-commander typeahead — DELETE the whole file (CONTEXT said "edit"; RESEARCH confirms nothing to keep).

XSS (RET-04): Markdig pipeline `ContentKbController.cs:20` = `new MarkdownPipelineBuilder().UseAdvancedExtensions().DisableHtml().Build()` → produces `RenderedHtml` (HtmlString) rendered at `Detail.cshtml:44`. PRESERVE `.DisableHtml()`. All other harvested fields use Razor `@` (encoded). Zero `Html.Raw` in `Views/ContentKb/*` today — keep it that way.

RET-06: copy affordance ALREADY EXISTS on Detail.cshtml:35-43 (`data-copy-target="kb-artifact-text"` button) wired by content-kb.ts:84-114. Do NOT rebuild — add a NOTE + link to `/content-kb` on the deck-analysis page where the removed accordion was (around DeckAnalysis.cshtml:182, or in the "What you do" block :234-245).

Nav copy: Home.cshtml:67-69 says "...pin videos or follow creators to inject their advice straight into your deck analysis prompt." — now FALSE; rewrite to browse + copyable prompts only. `_DeckToolTabs.cshtml:43` link label "Knowledge Base" — keep label, ensure no injection promise in surrounding copy.
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public → /content-kb (un-darked) | Harvested (scraped, untrusted) clip/summary/title text rendered to anonymous users |
| harvested Markdown → Markdig render | Untrusted transcript text passed to `Markdown.ToHtml` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37-04 | Tampering / Elevation (Stored XSS) | `/content-kb` Detail render of harvested text | mitigate | **HIGH-relevance.** Preserve Markdig `.DisableHtml()` (ContentKbController.cs:20) so embedded `<script>` renders inert; keep all other harvested fields on Razor `@` encoding; introduce ZERO `Html.Raw`/`MarkupString`. Asserted by RET-04 regression test (Task 4). This mitigation is an explicit acceptance criterion, not implied. |
| T-37-05 | Tampering | `</textarea>` breakout in copyable-prompt block (`Detail.cshtml:43`) | accept | `@Model.CleanBodyText` is `@`-encoded inside the textarea; `<`/`>` encoded → no breakout. No code change. |
| T-37-06 | Spoofing / dead-UI | Browse page pin/follow buttons POSTing to deleted `/api/content-kb` | mitigate | Task 2 removes the tray/buttons/CTA/script so no client calls a deleted endpoint. |
| T-37-SC | Tampering | npm/NuGet installs | mitigate | Zero installs — removal/config only; slopcheck N/A. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Un-dark — flip content.kb.enabled seed default ON (both providers)</name>
  <files>DeckFlow.Web/Services/FeatureFlags/FeatureFlagStore.cs</files>
  <read_first>
    - DeckFlow.Web/Services/FeatureFlags/FeatureFlagStore.cs (line 180 Postgres `('content.kb.enabled', FALSE)`, line 190 SQLite `('content.kb.enabled', 0)`)
    - 37-RESEARCH.md § Un-Dark + XSS (RET-04) — seed default flips for FRESH DBs; existing prod row flipped via /Admin/Flags (manual, out of code scope)
    - 37-CONTEXT.md decision "Un-dark = YES"
  </read_first>
  <action>
    Change the seed default for `content.kb.enabled` from FALSE→TRUE (Postgres, line 180) and 0→1 (SQLite, line 190). Touch ONLY those two values; do not reorder or reformat the seed list. This only affects fresh DBs; the live prod flag row is flipped manually via /Admin/Flags (noted in SUMMARY for the operator).
  </action>
  <verify>
    <automated>"/mnt/c/Program Files/dotnet/dotnet.exe" build DeckFlow.sln -c Debug</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "content.kb.enabled" DeckFlow.Web/Services/FeatureFlags/FeatureFlagStore.cs` shows `TRUE` (Postgres) and `1` (SQLite) — no `FALSE`/`0` left for this key.
    - Build 0/0.
    - Diff touches only the two seed lines.
    - SUMMARY records the manual prod-flip TODO (operator flips via /Admin/Flags).
  </acceptance_criteria>
  <done>Fresh DBs seed `content.kb.enabled` ON; `/content-kb` reachable without a manual toggle on new installs.</done>
</task>

<task type="auto">
  <name>Task 2: Strip expert-selection UI from the KEPT /content-kb browse page</name>
  <files>DeckFlow.Web/Views/ContentKb/Index.cshtml</files>
  <read_first>
    - DeckFlow.Web/Views/ContentKb/Index.cshtml (tray :62-71, CTA :71, pin/follow buttons :104-118, kb-selection.js script :145, search/filter rows to KEEP `data-kb-search`)
    - 37-RESEARCH.md § kb-selection.ts (DISCREPANCY: browse Index ALSO loads kb-selection + renders tray/buttons/CTA) AND § Common Pitfalls #1
  </read_first>
  <action>
    Remove from `ContentKb/Index.cshtml`: the `kb-selection-tray` block (:62-71), every per-entry pin/follow button (:104-118 — the `data-video-id`/`data-creator` controls), the "Run analysis with this selection →" CTA, and the `kb-selection.js` `<script>` tag (:145). KEEP the page heading, the `data-kb-search` filter row, the entry listing markup, and the `content-kb.ts`/`kb-entry-filter.ts` script wiring. Edit only those regions; no reflow.
  </action>
  <verify>
    <automated>"/mnt/c/Program Files/dotnet/dotnet.exe" build DeckFlow.sln -c Debug</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "kb-selection\|kb-selection-tray\|data-video-id\|data-creator\|Run analysis" DeckFlow.Web/Views/ContentKb/Index.cshtml` returns NOTHING.
    - `grep -n "data-kb-search\|kb-entry-filter\|content-kb.js" DeckFlow.Web/Views/ContentKb/Index.cshtml` STILL returns matches (filter/listing kept).
    - Build 0/0.
  </acceptance_criteria>
  <done>The browse page lists/filters distilled entries with no expert pin/follow tray, no CTA, and no call to the deleted typeahead API.</done>
</task>

<task type="auto">
  <name>Task 3: Remove admin relevance-score preview (controller + view + VM + TS + tests)</name>
  <files>DeckFlow.Web/Controllers/Admin/AdminContentKbController.cs, DeckFlow.Web/Views/AdminContentKb/Index.cshtml, DeckFlow.Web/Models/AdminContentKbViewModel.cs, DeckFlow.Web/wwwroot/ts/content-kb-admin.ts, DeckFlow.Web.Tests/AdminContentKbControllerTests.cs</files>
  <read_first>
    - DeckFlow.Web/Controllers/Admin/AdminContentKbController.cs (`_relevanceService` :26, ctor :39/:45/:50, preview logic :78-89, RelevanceScore projection :100, score-sort :110-116; KEEP GetAllRowsAsync/visibility/sources/status/SetEvergreen/SetVisibility/ReloadSeed)
    - DeckFlow.Web/Views/AdminContentKb/Index.cshtml (preview form + score column/sort to remove; KEEP grid :233-243 evergreen toggle, `data-kb-search` :196, reload-seed, per-source bulk; content-kb-admin.js script tag to remove)
    - DeckFlow.Web/Models/AdminContentKbViewModel.cs (RelevanceScore + Preview*/SortBy to remove; KEEP IsEvergreen/IsVisible init props)
    - DeckFlow.Web/wwwroot/ts/content-kb-admin.ts (227 lines, ENTIRELY preview typeahead → DELETE)
    - DeckFlow.Web.Tests/AdminContentKbControllerTests.cs (FakeContentKbRelevanceService double :369-398, score-preview cases :127,156,172,195,254, ctor call :331)
    - 37-RESEARCH.md § AdminContentKb DISCREPANCY (content-kb-admin.ts delete-whole) AND § Test Impact (AdminContentKbControllerTests EDIT)
  </read_first>
  <action>
    Controller: remove `_relevanceService` field, its ctor param/guard/assign, the `previewCommander`/`previewBracket`/`sortBy` action params, the `previewActive`/`previewScores` logic, the `RelevanceScore` projection, and the score-sort block. KEEP all curation actions. The interface `IContentKbRelevanceService` no longer exists (deleted in 37-01), so the ctor signature must drop that dependency.
    View: remove the preview form (PreviewCommander/Bracket inputs), the score column + score-sort affordance, and the `content-kb-admin.js` `<script>` tag. KEEP grid, publish/unpublish, evergreen toggle, reload-seed, per-source bulk, `data-kb-search` filter.
    VM: remove `RelevanceScore` and any `PreviewCommander`/`PreviewBracket`/`SortBy`. KEEP curation fields (init).
    TS: DELETE `content-kb-admin.ts` entirely.
    Tests: remove the `FakeContentKbRelevanceService` double + the score-preview test cases; drop the relevance arg from the ctor calls. KEEP grid/visibility/seed/evergreen tests. Remove any orphaned `using`/helpers.
    Edit only target lines; no reflow; preserve LF; do not collapse init properties.
  </action>
  <verify>
    <automated>"/mnt/c/Program Files/dotnet/dotnet.exe" build DeckFlow.sln -c Debug</automated>
  </verify>
  <acceptance_criteria>
    - `test -f DeckFlow.Web/wwwroot/ts/content-kb-admin.ts` is FALSE (deleted).
    - `grep -rn "RelevanceScore\|previewCommander\|PreviewBracket\|ScoreAllAsync\|_relevanceService\|content-kb-admin" DeckFlow.Web/Controllers/Admin DeckFlow.Web/Views/AdminContentKb DeckFlow.Web/Models/AdminContentKbViewModel.cs` returns NOTHING.
    - `grep -rn "FakeContentKbRelevanceService\|IContentKbRelevanceService" DeckFlow.Web.Tests/AdminContentKbControllerTests.cs` returns NOTHING.
    - `grep -n "SetEvergreen\|ReloadSeed\|data-kb-search" DeckFlow.Web/Views/AdminContentKb/Index.cshtml` STILL returns matches (curation grid kept).
    - Build 0/0; `git diff --stat` shows no out-of-fence files.
  </acceptance_criteria>
  <done>The admin score-preview is gone (controller/view/VM/TS/tests); the curation grid + evergreen toggle + reload-seed + instant filter remain and build clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: RET-04 XSS-regression test — Markdig .DisableHtml() strips raw HTML</name>
  <files>DeckFlow.Web.Tests/ContentKbMarkdigXssTests.cs</files>
  <read_first>
    - DeckFlow.Web/Controllers/ContentKbController.cs (pipeline :20 `UseAdvancedExtensions().DisableHtml()`, render :123/:141 RenderedHtml)
    - DeckFlow.Web/Views/ContentKb/Detail.cshtml (:44 `@Model.RenderedHtml`)
    - 37-RESEARCH.md § Un-Dark + XSS (RET-04) — verification task is "assert pipeline strips raw HTML; do not regress"
    - 37-VALIDATION.md § Wave 0 (RET-04 XSS-regression test)
  </read_first>
  <behavior>
    - Rendering Markdown containing `<script>alert(1)</script>` through the same pipeline the controller uses (`UseAdvancedExtensions().DisableHtml()`) produces HTML that contains NO `<script>` tag (the raw HTML is escaped/stripped).
    - Rendering a benign Markdown sample (e.g., `**bold**`) still produces the expected `<strong>` (pipeline still functional, not over-stripped).
  </behavior>
  <action>
    Create `ContentKbMarkdigXssTests.cs`: build the pipeline exactly as `ContentKbController` does (`new MarkdownPipelineBuilder().UseAdvancedExtensions().DisableHtml().Build()`) — or invoke the controller's pipeline if it is exposed — and assert `Markdown.ToHtml("<script>alert(1)</script>", pipeline)` does NOT contain `<script>`. Add a positive-control assert that `**bold**` → contains `<strong>`. If the controller's pipeline is private and not reachable, replicate the identical builder in the test and add a comment that the test pins the controller's XSS posture (and the controller must keep `.DisableHtml()`). No production code change.
  </action>
  <verify>
    <automated>"/mnt/c/Program Files/dotnet/dotnet.exe" build DeckFlow.sln -c Debug</automated>
  </verify>
  <acceptance_criteria>
    - `ContentKbMarkdigXssTests.cs` exists and asserts no `<script>` in the rendered output of a `<script>` payload via a `.DisableHtml()` pipeline.
    - `grep -n "DisableHtml" DeckFlow.Web/Controllers/ContentKbController.cs` STILL returns the pipeline line (mitigation preserved — blocking criterion).
    - `grep -rn "Html.Raw\|MarkupString\|WriteLiteral\|IHtmlContent" DeckFlow.Web/Views/ContentKb` returns NOTHING (no raw sink on harvested text).
    - Build 0/0.
  </acceptance_criteria>
  <done>RET-04 is pinned by an automated regression: the harvested-text render pipeline strips raw `<script>`, and no `Html.Raw` exists on KB views.</done>
</task>

<task type="auto">
  <name>Task 5: RET-06 deck-analysis KB pointer note + fix injection-advertising nav copy</name>
  <files>DeckFlow.Web/Views/Deck/DeckAnalysis.cshtml, DeckFlow.Web/Views/Deck/Home.cshtml, DeckFlow.Web/Views/Shared/_DeckToolTabs.cshtml</files>
  <read_first>
    - DeckFlow.Web/Views/Deck/DeckAnalysis.cshtml (where accordion was, ~:182; the "What you do" instructions block ~:234-245) — NOTE 37-01 already removed the accordion/panel/widget in a prior wave; only ADD the pointer note here
    - DeckFlow.Web/Views/Deck/Home.cshtml (:67-69 injection-advertising copy)
    - DeckFlow.Web/Views/Shared/_DeckToolTabs.cshtml (:43 "Knowledge Base" link + surrounding copy)
    - 37-RESEARCH.md § RET-06 Copyable-Prompt Affordance (copy button ALREADY on Detail page; surface/link, don't rebuild) AND the "Planner-must-handle nav-copy fix" note
  </read_first>
  <action>
    DeckAnalysis.cshtml: where the removed Expert-Context accordion was (or inside the existing "What you do" instructions block), add a short NOTE: tell users they can browse distilled creator advice at the Knowledge Base and copy a ready-to-paste prompt from any entry's detail page — with a link to `/content-kb`. Do NOT rebuild a copy widget (it already exists on Detail). Do NOT reintroduce any removed accordion/panel/widget markup.
    Home.cshtml: rewrite the false injection copy (:67-69) — replace "...pin videos or follow creators to inject their advice straight into your deck analysis prompt" with accurate copy: a browsable Knowledge Base of distilled creator advice with copyable prompts. Keep the link target `/content-kb`.
    _DeckToolTabs.cshtml: ensure the "Knowledge Base" link copy makes no injection promise; adjust surrounding text if it implies auto-injection. Keep the link working.
    Plain encoded Razor `@`/static text only — no `Html.Raw`. Edit only target lines; no reflow.
  </action>
  <verify>
    <automated>"/mnt/c/Program Files/dotnet/dotnet.exe" build DeckFlow.sln -c Debug</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "/content-kb" DeckFlow.Web/Views/Deck/DeckAnalysis.cshtml` returns a match (pointer note + link present).
    - `grep -in "inject their advice\|inject.*deck analysis prompt" DeckFlow.Web/Views/Deck/Home.cshtml` returns NOTHING (false injection copy gone).
    - `grep -rn "Html.Raw\|MarkupString" DeckFlow.Web/Views/Deck/DeckAnalysis.cshtml DeckFlow.Web/Views/Deck/Home.cshtml DeckFlow.Web/Views/Shared/_DeckToolTabs.cshtml` returns NOTHING.
    - Build 0/0; `git diff --stat` shows no out-of-fence files.
  </acceptance_criteria>
  <done>The deck-analysis page points users to the KB's copyable prompts via a link to `/content-kb`; Home/nav copy no longer advertises the removed auto-injection.</done>
</task>

</tasks>

<verification>
- `dotnet build DeckFlow.sln -c Debug` → 0/0.
- Fresh-DB seed flips `content.kb.enabled` ON (RET-04 un-dark / SC3). [Manual: operator flips prod row via /Admin/Flags.]
- `ContentKbMarkdigXssTests` proves `.DisableHtml()` strips `<script>`; zero `Html.Raw` on KB views (RET-04 / SC3 XSS).
- Browse page has no pin/follow tray/CTA/script; admin score-preview removed, curation grid intact (RET-03).
- DeckAnalysis links `/content-kb`; Home copy no longer promises injection (RET-06 / SC1).
- Manual smoke (RET-03): CLI harvest/distill still populates corpus; `/content-kb` renders distilled entries with harvested text encoded.
- `git diff --stat` confirms no out-of-fence edits.
</verification>

<success_criteria>
- RET-03: KB reference intact — harvest/distill + browse + admin curation list/render entries.
- RET-04: `/content-kb` un-darked (seed ON) + harvested text HTML-encoded (Markdig `.DisableHtml()` preserved, no `Html.Raw`), asserted by regression test.
- RET-06: DeckAnalysis carries a note + link to `/content-kb` copyable prompts; nav copy accurate.
</success_criteria>

<output>
Create `.planning/phases/37-retire-clip-injection/37-02-SUMMARY.md` when done.
</output>
