---
phase: 20-content-kb-ingestion-transcription-local
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - DeckFlow.Core/Integration/ITranscriptSource.cs
  - DeckFlow.Core/Integration/IYouTubeTranscriptFetcher.cs
  - DeckFlow.Core/Integration/YouTubeTranscriptFetcher.cs
  - DeckFlow.Core/DeckFlow.Core.csproj
  - DeckFlow.Core.Tests/YouTubeTranscriptFetcherTests.cs
autonomous: true
requirements: [KB-03]
notes:
  - "No packages.lock.json is generated (GeneratePackageLockFile unset repo-wide) — the approved YoutubeExplode 6.6.0 add touches only DeckFlow.Core.csproj. Do NOT add packages.lock.json to files_modified."
must_haves:
  truths:
    - "YouTubeTranscriptFetcher returns caption text for a video that has caption tracks"
    - "When a video has zero caption tracks, the fetcher reports a no-captions outcome (Whisper-fallback signal), never throws"
    - "Auto-generated caption tracks are accepted as valid captions (NOT treated as missing) — keeps whisper_fallback_ratio low (P2)"
    - "Each fetch surfaces transcript_source (auto-generated | manual) for structured logging"
    - "The fetcher is constructed in production with an injected HttpClient (no new HttpClient() inside the method) and unit-tested via a Func<> delegate seam (no live HTTP)"
    - "A DECKFLOW_YOUTUBE_TRANSCRIPT_PROVIDER hook selects the transcript provider (proxy-pluggable, P2)"
  artifacts:
    - path: "DeckFlow.Core/Integration/ITranscriptSource.cs"
      provides: "ITranscriptSource abstraction (YouTube now, podcast stub later) + result record"
      contains: "interface ITranscriptSource"
    - path: "DeckFlow.Core/Integration/YouTubeTranscriptFetcher.cs"
      provides: "YoutubeExplode 6.6.0 caption fetch with auto-gen acceptance + Func<> test seam"
      contains: "TryGetByLanguage"
    - path: "DeckFlow.Core.Tests/YouTubeTranscriptFetcherTests.cs"
      provides: "xUnit coverage of captions / no-captions / auto-gen via delegate seam"
      contains: "YouTubeTranscriptFetcherTests"
  key_links:
    - from: "DeckFlow.Core/Integration/YouTubeTranscriptFetcher.cs"
      to: "YoutubeExplode ClosedCaptions manifest"
      via: "GetManifestAsync + TryGetByLanguage + Tracks.Count == 0 gate"
      pattern: "TryGetByLanguage"
    - from: "DeckFlow.Core/Integration/YouTubeTranscriptFetcher.cs"
      to: "DECKFLOW_YOUTUBE_TRANSCRIPT_PROVIDER"
      via: "provider-toggle env read"
      pattern: "DECKFLOW_YOUTUBE_TRANSCRIPT_PROVIDER"
---

<objective>
Build the YouTube caption fetcher and the `ITranscriptSource` abstraction in `DeckFlow.Core/Integration`, following the `ArchidektApiDeckImporter` resilience precedent and the `CardLookupService` Func<> test seam. This is KB-03 and SC1/SC2 of the (amended) phase.

Purpose: Captions-first ingestion. Accepting auto-generated captions (not just manual) keeps `whisper_fallback_ratio` under 25% (Pitfall P2). The proxy-pluggable provider toggle satisfies SC2 (P2). `TryGetByLanguage` (never the throwing `GetByLanguage`) avoids exceptions on the common MTG case.
Output: `ITranscriptSource` + result record; `IYouTubeTranscriptFetcher` + `YouTubeTranscriptFetcher` with the Func<> seam; YoutubeExplode 6.6.0 added to DeckFlow.Core.csproj; xUnit tests via the delegate seam.

SC5 AMENDMENT (D-03/D-04): ROADMAP SC5 mandated `IHttpClientFactory` + `ResiliencePipelineProvider<string>` named clients. That is a DeckFlow.Web-host concept the Core/CLI home cannot use. This phase amends SC5 to the Core/Integration shape: RestSharp/HttpClient injected via constructor + Polly built DIRECTLY (ArchidektApiDeckImporter precedent) + `internal` Func<> test ctor. ZERO `new HttpClient()` in method bodies; NO Microsoft.Extensions.Http added to Core. The SPIRIT of SC5 (named resilience, no raw clients, test seam) is preserved.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-content-kb-ingestion-transcription-local/20-CONTEXT.md
@.planning/phases/20-content-kb-ingestion-transcription-local/20-RESEARCH.md

<interfaces>
<!-- From DeckFlow.Core/Integration/ArchidektApiDeckImporter.cs — resilience + DI ctor pattern to copy: -->
- `private static readonly AsyncRetryPolicy<RestResponse> RetryPolicy = Policy<RestResponse>.HandleResult(...).WaitAndRetryAsync(retryCount, sleepDurationProvider, onRetry);`
- production ctor takes an optional injected dependency (`RestClient? = null`); falls back to a real one only in the ctor, never in a method.

<!-- From DeckFlow.Web/Services/CardLookupService.cs:50-89 — Func<> delegate test-seam pattern to copy: -->
- private readonly `Func<...>` field assigned from an optional `...Override` ctor parameter (`_executeAsync = executeAsyncOverride ?? (real impl)`)
- internal ctor exposes the override; production ctor delegates to it. [assembly: InternalsVisibleTo("DeckFlow.Core.Tests")] already present in DeckFlow.Core/AssemblyInfo.cs.

<!-- From DeckFlow.Core/Knowledge/ContentModels.cs -->
- `TranscriptSource.Captions = "captions"`, `TranscriptSource.Whisper = "whisper"`
- `TranscriptStatus.Captions/.Whisper/.Failed/.SkippedOverCap`
- `ContentSourceType.Youtube = "youtube_channel"`

<!-- YoutubeExplode 6.6.0 verified API facts (RESEARCH HIGH confidence) -->
- `youtube.Videos.ClosedCaptions.GetManifestAsync(videoId, ct)` → `ClosedCaptionManifest`
- `manifest.Tracks` is `IReadOnlyList<ClosedCaptionTrackInfo>`; `Tracks.Count == 0` is the only true no-captions gate
- `manifest.TryGetByLanguage(string)` returns null on miss (safe); `GetByLanguage` THROWS — never use it
- `ClosedCaptionTrackInfo.IsAutoGenerated` is a bool; accept auto-generated as valid captions
- `youtube.Videos.ClosedCaptions.GetAsync(trackInfo, ct)` → track; `track.Captions.Select(c => c.Text)`
- `YoutubeClient` accepts an `HttpClient` ctor argument
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define ITranscriptSource abstraction + result record</name>
  <files>DeckFlow.Core/Integration/ITranscriptSource.cs</files>
  <read_first>
    - DeckFlow.Core/Knowledge/ContentModels.cs lines 140-199 (TranscriptSource / TranscriptStatus / ContentSourceType const classes the record fields reference)
    - DeckFlow.Core/Integration/ArchidektApiDeckImporter.cs (file-scoped namespace DeckFlow.Core.Integration; XML doc-comment style)
    - .planning/phases/20-content-kb-ingestion-transcription-local/20-RESEARCH.md Pattern 3 (the minimal interface shape — do NOT over-engineer a plugin system)
  </read_first>
  <action>
    Create `ITranscriptSource` in `DeckFlow.Core/Integration` (interface-first contract Plan 20-03 + Phase 21 implement against). Expose: a `string SourceType { get; }` property (matches a `ContentSourceType` const) and `Task<TranscriptFetchResult?> FetchTranscriptAsync(long videoId, string naturalKey, string? audioUrl, CancellationToken ct = default)` — null return means captions absent AND Whisper skipped (cap/ffmpeg), caller persists status. Add a `public sealed record TranscriptFetchResult` with `{ get; init; }` properties: `Body` (string), `Source` (string — a `TranscriptSource` const), `FinalStatus` (string — a `TranscriptStatus` const). XML `<summary>` on the interface, every member, and the record. Keep it minimal — the podcast path is deferred (D-09), so no RSS-specific members. File-scoped namespace, Allman braces, LF endings.
  </action>
  <verify>
    <automated>"/mnt/c/Program Files/dotnet/dotnet.exe" build DeckFlow.Core/DeckFlow.Core.csproj -c Debug 2>&1 | grep -E "Build succeeded|error"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "interface ITranscriptSource" DeckFlow.Core/Integration/ITranscriptSource.cs` == 1
    - `grep -c "record TranscriptFetchResult" DeckFlow.Core/Integration/ITranscriptSource.cs` == 1
    - All record properties use `{ get; init; }` (grep `init;` present, no `{ get; }`-only)
    - Build clean 0 errors / 0 warnings (GenerateDocumentationFile is on — every public member documented)
  </acceptance_criteria>
  <done>ITranscriptSource + TranscriptFetchResult exist with full doc-comments and init-only properties; build clean.</done>
</task>

<task type="auto">
  <name>Task 2: Add YoutubeExplode 6.6.0 + implement YouTubeTranscriptFetcher with Func<> seam</name>
  <files>DeckFlow.Core/DeckFlow.Core.csproj, DeckFlow.Core/Integration/IYouTubeTranscriptFetcher.cs, DeckFlow.Core/Integration/YouTubeTranscriptFetcher.cs, DeckFlow.Core.Tests/YouTubeTranscriptFetcherTests.cs</files>
  <read_first>
    - DeckFlow.Core/Integration/ArchidektApiDeckImporter.cs (production ctor + injected-client pattern; copy the shape, swap RestClient for HttpClient/YoutubeClient)
    - DeckFlow.Web/Services/CardLookupService.cs lines 50-95 (Func<> override field + internal ctor seam — the canonical seam to mirror)
    - DeckFlow.Core/DeckFlow.Core.csproj (PackageReference block — add YoutubeExplode 6.6.0 only; OpenAI is added in Plan 20-03)
    - DeckFlow.Core.Tests/ContentSourceStoreTests.cs (xUnit fixture style)
    - .planning/phases/20-content-kb-ingestion-transcription-local/20-RESEARCH.md "Caption detection with auto-gen acceptance" code example (lines ~472-499) + Pitfall P2/P6
  </read_first>
  <action>
    Add `<PackageReference Include="YoutubeExplode" Version="6.6.0" />` to DeckFlow.Core.csproj (D-02 approved). No packages.lock.json is generated (GeneratePackageLockFile is unset repo-wide and no lockfile is tracked or on disk) — the approved package add touches only the .csproj; do NOT create or add packages.lock.json to files_modified (restore is allowed for an approved package add). Create `IYouTubeTranscriptFetcher` exposing `Task<YouTubeCaptionResult> FetchCaptionsAsync(string videoId, CancellationToken ct = default)` and a `public sealed record YouTubeCaptionResult { get; init; }` carrying: `bool HasCaptions`, `string? Body`, `bool IsAutoGenerated`, `string? LanguageCode` (with a static `NoCaptions()` factory). Implement `YouTubeTranscriptFetcher` in DeckFlow.Core/Integration following BOTH precedents: a production ctor `YouTubeTranscriptFetcher(HttpClient httpClient)` that delegates to an `internal` ctor taking `Func<string, CancellationToken, Task<YouTubeCaptionResult>> executeAsync` (the test seam); the production delegate wraps a `new YoutubeClient(httpClient)` plus a `private static readonly AsyncRetryPolicy` (HandleResult on transient YoutubeExplode failures, WaitAndRetry exponential backoff — copy ArchidektApiDeckImporter's policy shape). Caption logic per RESEARCH: `GetManifestAsync`; if `manifest.Tracks.Count == 0` return `YouTubeCaptionResult.NoCaptions()`; else select track via `TryGetByLanguage("en-US") ?? TryGetByLanguage("en") ?? Tracks.FirstOrDefault(t => t.Language.Code.StartsWith("en", OrdinalIgnoreCase)) ?? Tracks[0]` (NEVER GetByLanguage); fetch via `GetAsync(trackInfo)`; body = `string.Join(" ", track.Captions.Select(c => c.Text))`; set `IsAutoGenerated = trackInfo.IsAutoGenerated` (accept auto-gen as captions — do NOT downgrade to no-captions). Add a `DECKFLOW_YOUTUBE_TRANSCRIPT_PROVIDER` provider-selection hook (read env in the production ctor; default provider = direct YoutubeExplode; the env value selects an alternate provider delegate — for this phase only the default is wired, but the seam exists per SC2/P2). Keep methods ≤30 lines (extract caption-track selection to a private static helper). The caller (Plan 20-03/Phase 21) emits the `transcript_source` structured log from `IsAutoGenerated`; expose it on the result. Add `YouTubeTranscriptFetcherTests` exercising via the internal Func<> ctor: (a) HasCaptions true path returns joined body, (b) NoCaptions path returns HasCaptions=false and never throws, (c) auto-generated track yields HasCaptions=true with IsAutoGenerated=true. No live HTTP in tests.
  </action>
  <verify>
    <automated>"/mnt/c/Program Files/dotnet/dotnet.exe" build DeckFlow.Core/DeckFlow.Core.csproj -c Debug 2>&1 | grep -E "Build succeeded|error"; "/mnt/c/Program Files/dotnet/dotnet.exe" test DeckFlow.Core.Tests/DeckFlow.Core.Tests.csproj --filter "FullyQualifiedName~YouTubeTranscriptFetcherTests" 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'YoutubeExplode" Version="6.6.0"' DeckFlow.Core/DeckFlow.Core.csproj` == 1; no other new package added
    - `grep -c "GetByLanguage\b" DeckFlow.Core/Integration/YouTubeTranscriptFetcher.cs | grep -v Try` is 0 — only TryGetByLanguage is used (anti-pattern gate)
    - `grep -c "new HttpClient()" DeckFlow.Core/Integration/YouTubeTranscriptFetcher.cs` == 0 (HttpClient is ctor-injected; this Core/Integration grep scope is where the anti-pattern ban applies — not the CLI composition root)
    - `grep -c "DECKFLOW_YOUTUBE_TRANSCRIPT_PROVIDER" DeckFlow.Core/Integration/YouTubeTranscriptFetcher.cs` ≥1
    - `grep -c "internal YouTubeTranscriptFetcher" DeckFlow.Core/Integration/YouTubeTranscriptFetcher.cs` ≥1 (Func<> test ctor present)
    - Build clean; YouTubeTranscriptFetcherTests compile + pass (or build-clean fallback per WSL VSTest caveat noted in SUMMARY)
    - DeckFlow.Core.csproj gains NO Microsoft.AspNetCore.* / Microsoft.Extensions.Http reference
  </acceptance_criteria>
  <done>YouTubeTranscriptFetcher fetches captions (auto-gen accepted), reports no-captions without throwing, uses TryGetByLanguage only, is ctor-injected + Func<>-tested, and the provider toggle seam exists.</done>
</task>

</tasks>

<verification>
- KB-03: caption fetch via YoutubeExplode 6.6.0; Google.Apis.YouTube NOT referenced anywhere in this phase.
- SC1/SC2 (amended): fetcher built; provider toggle present; auto-gen accepted to hold fallback ratio low. (The live 5-channel proof runs in Plan 20-04 UAT.)
- SC5 AMENDMENT honored: Core/Integration RestSharp/HttpClient + direct Polly + Func<> seam; no IHttpClientFactory, no new HttpClient() in methods, no Microsoft.Extensions.Http in Core.
- ITranscriptSource contract exists for Plan 20-03 + Phase 21 to implement against (interface-first ordering).
</verification>

<success_criteria>
- A different executor can wire the orchestrator against ITranscriptSource + YouTubeCaptionResult without exploring the codebase.
- Caption fetch is unit-tested without live HTTP; build clean; no new dependency beyond YoutubeExplode 6.6.0.
</success_criteria>

<output>
Create `.planning/phases/20-content-kb-ingestion-transcription-local/20-02-SUMMARY.md` when done.
</output>
</content>
</invoke>
