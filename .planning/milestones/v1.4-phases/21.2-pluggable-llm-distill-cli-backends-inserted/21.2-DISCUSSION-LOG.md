# Phase 21.2 — Discussion Log

**Date:** 2026-06-01
**Mode:** discuss (default)

Human-reference only; downstream agents read CONTEXT.md.

## Trigger
Phase 21.1 live-UAT distill gate blocked on OpenAI billing (`HTTP 429 insufficient_quota`). User asked to drive distillation through Codex/Claude CLI (subscription) instead of the OpenAI API — both backends, runnable from WSL or Windows.

## Areas discussed (user selected all 4)

| Area | Options presented | Selected |
|------|-------------------|----------|
| Seam placement | New impl + shared prompts / Refactor to shared transport | **New impl + shared prompts** (D-01) — minimal churn to shipped Phase 21 |
| CLI I/O + cross-platform | stdin/stdout + env-override + auto-detect / temp-file + plain-text / explicit-env-only | **stdin in / stdout JSON, per-provider default + env override, auto-detect WSL vs Windows** (D-02) |
| JSON-repair / retry | repair+retry×2 mark-failed / retry×1 / retry×3 abort | **repair + retry ×2, mark video failed (re-attemptable), continue run** (D-03) |
| Ledger / cost | bypass+spend0+count / bypass no-record-change | **bypass cap-gate, spend=0, reuse whisper_calls overload for call-count** (D-04) |

## Locked carry-forwards
- Provider switch via `DECKFLOW_LLM_PROVIDER` (openai default|claude|codex), factory mirrors `TranscriptProviderFactory`.
- OpenAI default = byte-identical to current (regression guard).
- Acceptance run (CLI distill over 10-video UAT db + E5/E6) doubles as the Phase 21.1 gate.

## Deferred
- Non-CLI Anthropic/OpenAI HTTP "claude-api" provider; parallel CLI calls; Gemini CLI (v1.5).

## Open research items (for plan-phase researcher)
- Exact `codex exec` flag for clean machine-readable JSON output (codex emits agent chatter).
- Claude Code `-p --output-format json` envelope shape (`.result` field) confirmation across versions.
