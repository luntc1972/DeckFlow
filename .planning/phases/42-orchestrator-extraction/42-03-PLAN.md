---
phase: 42-orchestrator-extraction
plan: 03
type: execute
wave: 3
depends_on: ["42-02"]
files_modified:
  - DeckFlow.CLI/ContentKbCommandRunners.cs
  - DeckFlow.Core/Orchestration/ServiceCollectionExtensions.cs
  - DeckFlow.Core.Tests/CommandRunnerValidateClipsTests.cs
  - DeckFlow.Core.Tests/Orchestration/ThrowingOrchestratorDependencies.cs
  - DeckFlow.Core.Tests/Orchestration/FakeOrchestratorStores.cs
autonomous: true
requirements: [ORCH-02]
user_setup: []

must_haves:
  truths:
    - "Every public ContentKbCommandRunners Run*Async method is a thin adapter: resolve provider/paths, construct stores, build the orchestrator, call it, map result to exit code (D-05 provider selection stays host-side; D-06 path resolution stays host-side)"
    - "No harvest/distill/maintenance/source/export domain logic remains in DeckFlow.CLI"
    - "AddContentKbOrchestrator() registers the concrete ContentKbOrchestrator once and forwards EVERY slice interface (IContentKbOrchestrator + all five sub-interfaces) to that same scoped instance (D-01/D-08 — Studio slice injection resolves)"
    - "The orchestrator ctor consumes a ContentKbOrchestratorOptions record (host-resolved ArtifactRoot), not a bare string, so DI registration needs no global bare-string registration"
    - "CommandRunnerValidateClipsTests is re-pointed to the new ctor-injected orchestrator seam with identical assertions; no static back-compat shim remains; the Fake* stores it used are lifted into a shared internal test-doubles file reusable by Plan 42-05"
    - "dotnet build DeckFlow.sln is 0 errors / 0 new warnings; Core.Tests green"
  artifacts:
    - path: "DeckFlow.Core/Orchestration/ServiceCollectionExtensions.cs"
      provides: "AddContentKbOrchestrator() DI registration forwarding all slice interfaces to one scoped instance, resolving ContentKbOrchestratorOptions from DI (D-01/D-08 prerequisite)"
      contains: "AddContentKbOrchestrator"
    - path: "DeckFlow.CLI/ContentKbCommandRunners.cs"
      provides: "Thin adapters over IContentKbOrchestrator (ORCH-02)"
    - path: "DeckFlow.Core.Tests/CommandRunnerValidateClipsTests.cs"
      provides: "Behavior-parity anchor re-pointed to the new seam (D-09)"
      contains: "ContentKbOrchestrator"
    - path: "DeckFlow.Core.Tests/Orchestration/ThrowingOrchestratorDependencies.cs"
      provides: "Shared Throwing* test doubles for unused orchestrator ctor deps (consumed by the anchor test + Plan 42-05 parity tests)"
      contains: "Throwing"
    - path: "DeckFlow.Core.Tests/Orchestration/FakeOrchestratorStores.cs"
      provides: "Shared internal Fake* stores lifted out of CommandRunnerValidateClipsTests (consumed by the anchor test + Plan 42-05 parity tests)"
      contains: "FakeContentVideoStore"
  key_links:
    - from: "DeckFlow.CLI/ContentKbCommandRunners.cs"
      to: "DeckFlow.Core.Orchestration.ContentKbOrchestrator"
      via: "construct orchestrator, delegate, map result to exit code"
      pattern: "ContentKbOrchestrator|IContentKbOrchestrator"
    - from: "DeckFlow.Core.Tests/CommandRunnerValidateClipsTests.cs"
      to: "DeckFlow.Core.Orchestration.ContentKbOrchestrator"
      via: "ctor-injected orchestrator + DistillAsync call"
      pattern: "new ContentKbOrchestrator|DistillAsync"
---

<objective>
Turn `ContentKbCommandRunners` into thin adapters over `IContentKbOrchestrator` (ORCH-02): each public `Run*Async` resolves the provider/connection (D-05) and paths (D-06), constructs the stores + orchestrator, calls the operation, and maps the structured result to the existing exit code + console output. Add the `AddContentKbOrchestrator()` DI extension in Core that forwards every slice interface to one scoped `ContentKbOrchestrator` (D-01/D-08 prerequisite — makes Studio slice injection resolvable) and resolves the orchestrator's `ContentKbOrchestratorOptions` from DI (no global bare-string registration). Re-point the `CommandRunnerValidateClipsTests` anchor to the new ctor-injected seam with identical assertions, remove the static back-compat path (D-09), and lift the test's Fake* stores into a shared internal test-doubles file that Plan 42-05 reuses.

Purpose: Establish route/behavior parity — identical CLI behavior, no domain logic in CLI (ORCH-02).
Output: Slim CLI adapters, the Core DI extension, the re-pointed test anchor, the shared `Throwing*` doubles, and the shared `Fake*` stores. Solution builds clean and Core.Tests stays green. (Broader CLI exit-code/output parity + JSON-seed byte-identity tests live in Plan 42-05, Wave 4, reusing those doubles.)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/42-orchestrator-extraction/42-CONTEXT.md
@.planning/phases/42-orchestrator-extraction/42-02-SUMMARY.md

<interfaces>
<!-- From Wave 2 (Plan 02). The orchestrator is implemented; wire it here. -->
DeckFlow.Core.Orchestration.ContentKbOrchestrator : IContentKbOrchestrator
  ctor injects: IContentSourceStore, IContentVideoStore, IContentSiteIndexStore,
    IBlockedVideoStore, IContentHarvestRunStore, ILlmSpendLedger, IWhisperSpendLedger,
    ILlmDistillationService, IYouTubeChannelVideoLister, ITranscriptSource,
    IFfmpegAudioChunker, Func<DateTimeOffset> utcNow, ContentKbOrchestratorOptions options,
    ILogger<ContentKbOrchestrator>? logger = null
    (ContentKbOrchestratorOptions { required string ArtifactRoot } — round-2 MEDIUM fix:
     the ctor takes this options record instead of a bare `string artifactRoot`, so the DI
     container resolves a typed options object rather than a global bare string. Path
     resolution stays host-side per D-06; the options record only transports the resolved value.)
  facade inherits: IHarvestOrchestrator, IDistillOrchestrator,
    IContentMaintenanceOrchestrator, IContentSourceManager, IContentIndexExporter
  operations return: HarvestResult, DistillResult, ContentSourceResult,
    ContentMaintenanceResult, BlockedVideoListResult, ContentIndexExportResult.
  progress param: IOrchestratorProgress? (synchronous void Report(string)) — NOT IProgress<string>.

<!-- CLI host wiring that STAYS in CLI (do not move to Core):
  ContentKbCliPaths.ResolveDatabasePath/ResolveArtifactRoot (D-06) — the resolved artifactRoot
    is wrapped in `new ContentKbOrchestratorOptions { ArtifactRoot = <resolved> }` and passed to the ctor
  Postgres-vs-Sqlite branch + PostgresConnectionStringNormalizer in RunCorpusResetAsync (D-05)
  LlmDistillationProviderFactory.Resolve + isSubscriptionProvider derivation (was lines 87-90)
  TranscriptProviderFactory.Resolve + WhisperTranscriptionService + YouTubeTranscriptSource wiring
  HttpClient lifetimes (using var ...) for llm/youtube/whisper
  ParseVideoIds (CLI option parser) — stays in CLI; pass IReadOnlyList<string>? to orchestrator
  exit-code policy: map result -> 0/1/2/3 exactly as today (D-03)
Current public adapter exit-code contract to preserve (from ContentKbCommandRunners.cs):
  AddSource: valid=0, bad-type=2, same-url-exists=0, slug-conflict=3, error=1
  Distill: metered-provider refusal (!dryRun && !isSubscriptionProvider, line 410) -> exit 1; else success=0
  others: success=0, OperationCanceledException rethrows, generic Exception logs + 1

CLI uses Serilog.ILogger (Log.Logger). The orchestrator takes ILogger<T> (M.E.Logging);
the CLI adapter does NOT need to pass a logger (NullLogger default) — it surfaces live
lines via an IOrchestratorProgress sink that writes synchronously to Console
(Console.WriteLine), preserving today's real-time per-video output interleaving.
Build that synchronous console progress sink in the CLI (a small sealed class implementing
IOrchestratorProgress whose Report calls Console.WriteLine directly) — do NOT use
new Progress<string>(...) (async post reorders the lines). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add AddContentKbOrchestrator() DI extension in Core forwarding all slice interfaces</name>
  <read_first>
    - DeckFlow.Core/Orchestration/ContentKbOrchestrator.cs (ctor dependency list to register, incl. the ContentKbOrchestratorOptions param)
    - DeckFlow.Core/Orchestration/ContentKbOrchestratorOptions.cs (the options record the extension resolves from DI)
    - DeckFlow.Core/Orchestration/IContentKbOrchestrator.cs + the five sub-interface files (the slice interfaces that MUST each be forwarded)
    - DeckFlow.Core/Content/*Store.cs and DeckFlow.Core/Integration/*.cs (concrete types available for registration)
    - DeckFlow.Core/DeckFlow.Core.csproj (confirm Microsoft.Extensions.DependencyInjection.Abstractions availability)
  </read_first>
  <action>
    BUILD-CHECK FIRST (review MEDIUM): before writing the extension, confirm Core can see `IServiceCollection`/`ServiceCollectionServiceExtensions` (these come from `Microsoft.Extensions.DependencyInjection.Abstractions`, commonly transitive via `Microsoft.Extensions.Logging.Abstractions` which Core already references). If a trial reference does NOT resolve, STOP and surface in the SUMMARY that a `Microsoft.Extensions.DependencyInjection.Abstractions` PackageReference would be required and that it needs user approval — do NOT add the package yourself (project rule: no new packages without approval). Only proceed once `IServiceCollection` resolves with the existing references.

    Create `DeckFlow.Core/Orchestration/ServiceCollectionExtensions.cs`, namespace `DeckFlow.Core.Orchestration`, `public static class ServiceCollectionExtensions` with `public static IServiceCollection AddContentKbOrchestrator(this IServiceCollection services)` mirroring the `AddDeckFlowResiliencePipelines()` precedent.

    Register the concrete ONCE, then forward EVERY slice interface to that same scoped instance so a consumer can inject the facade OR any sub-interface and get the same object (HIGH-1 — without this, Studio injecting `IContentMaintenanceOrchestrator` fails to resolve):
    - `services.AddScoped<ContentKbOrchestrator>();`
    - `services.AddScoped<IContentKbOrchestrator>(sp => sp.GetRequiredService<ContentKbOrchestrator>());`
    - `services.AddScoped<IHarvestOrchestrator>(sp => sp.GetRequiredService<ContentKbOrchestrator>());`
    - `services.AddScoped<IDistillOrchestrator>(sp => sp.GetRequiredService<ContentKbOrchestrator>());`
    - `services.AddScoped<IContentMaintenanceOrchestrator>(sp => sp.GetRequiredService<ContentKbOrchestrator>());`
    - `services.AddScoped<IContentSourceManager>(sp => sp.GetRequiredService<ContentKbOrchestrator>());`
    - `services.AddScoped<IContentIndexExporter>(sp => sp.GetRequiredService<ContentKbOrchestrator>());`
    (Use the ACTUAL sub-interface names defined in Plan 01 — if any name differs, forward that one instead; the rule is: one concrete registration + a forwarding registration for the facade and for every sub-interface, all `GetRequiredService<ContentKbOrchestrator>()` so a single scoped instance backs them all.)

    artifactRoot DI binding (round-2 MEDIUM fix): the plain `AddScoped<ContentKbOrchestrator>()` registration lets the container construct the orchestrator by resolving each ctor dependency — including `ContentKbOrchestratorOptions`. Because the ctor now takes the typed options record (not a bare `string`), the host registers the options object (e.g. `services.AddSingleton(new ContentKbOrchestratorOptions { ArtifactRoot = <host-resolved path> })`) and the container resolves it normally. Document in the method's XML summary that the host MUST first register: the store interfaces (`IContentVideoStore` etc.), the integration services, `Func<DateTimeOffset>`, AND the `ContentKbOrchestratorOptions` (carrying the host-resolved ArtifactRoot — D-06), THEN call `AddContentKbOrchestrator()`. The extension itself does NOT register stores, paths, connection strings, or the options — only the orchestrator concrete + the six slice forwards (D-05/D-06: those stay host-side).
  </action>
  <verify>
    <automated>"/mnt/c/Program Files/dotnet/dotnet.exe" build DeckFlow.sln -warnaserror 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `DeckFlow.Core/Orchestration/ServiceCollectionExtensions.cs` contains `public static IServiceCollection AddContentKbOrchestrator(this IServiceCollection services)`.
    - The extension registers the concrete once (`grep -n "AddScoped<ContentKbOrchestrator>()" DeckFlow.Core/Orchestration/ServiceCollectionExtensions.cs` matches) AND forwards every slice interface to it: a `GetRequiredService<ContentKbOrchestrator>()` forwarding registration exists for `IContentKbOrchestrator` AND each of the five sub-interfaces (`grep -c "GetRequiredService<ContentKbOrchestrator>()" DeckFlow.Core/Orchestration/ServiceCollectionExtensions.cs` returns 6).
    - The extension does NOT register a bare `string` or a connection string or the options itself (`grep -n "AddSingleton<string>\|AddScoped<string>\|ContentKbOrchestratorOptions" DeckFlow.Core/Orchestration/ServiceCollectionExtensions.cs` returns nothing inside the registration body — options registration is the HOST's job, documented in the XML summary); the ctor consuming the options record is what makes this DI-clean.
    - No new PackageReference was added to `DeckFlow.Core.csproj` (`git diff DeckFlow.Core/DeckFlow.Core.csproj` shows no PackageReference additions); if a DI.Abstractions ref is genuinely required, the SUMMARY flags it as a blocker for user approval instead of adding it.
    - `dotnet build DeckFlow.sln -warnaserror` = 0 errors / 0 warnings.
  </acceptance_criteria>
  <done>AddContentKbOrchestrator() registers one scoped concrete and forwards the facade + all five sub-interfaces to it, with no new package dependency and no bare-string/options registration (host supplies ContentKbOrchestratorOptions); injecting any slice resolves the same instance.</done>
</task>

<task type="auto">
  <name>Task 2: Convert ContentKbCommandRunners to thin adapters over the orchestrator</name>
  <read_first>
    - DeckFlow.CLI/ContentKbCommandRunners.cs (ALL public Run*Async + the now-redundant internal-static/private domain methods to delete)
    - DeckFlow.CLI/ContentKbCliPaths.cs (path resolution stays here — D-06)
    - DeckFlow.CLI/Program.cs lines 257-307 (the delegation call sites — their signatures must stay compatible)
    - DeckFlow.Core/Orchestration/ContentKbOrchestrator.cs + ContentKbOrchestratorOptions.cs + result records + IOrchestratorProgress (the surface to call + map)
  </read_first>
  <action>
    Rewrite each public `Run*Async` in `DeckFlow.CLI/ContentKbCommandRunners.cs` as a thin adapter (ORCH-02). For each: resolve paths via `ContentKbCliPaths` (D-06), resolve provider/connection + construct concrete stores (the Postgres-vs-Sqlite branch in corpus-reset stays here — D-05), resolve the provider factories + HttpClients (LlmDistillationProviderFactory, TranscriptProviderFactory, WhisperTranscriptionService, YouTubeTranscriptSource, YouTubeChannelVideoLister, FfmpegAudioChunker) exactly as today, derive `isSubscriptionProvider`, wrap the resolved artifactRoot in `new ContentKbOrchestratorOptions { ArtifactRoot = <resolved> }`, build a `new ContentKbOrchestrator(...)` injecting those stores/services + `() => DateTimeOffset.UtcNow` + the options record, build a SYNCHRONOUS Console-writing `IOrchestratorProgress` sink (a small sealed class implementing `IOrchestratorProgress` whose `Report(line)` calls `Console.WriteLine(line)` directly — do NOT use `new Progress<string>(...)`, which posts asynchronously and would reorder today's interleaved per-video output), call the matching orchestrator operation, then MAP the returned result record to the existing console output + exit code (D-03). Keep the existing `try/catch (Exception ... when not OperationCanceledException) { logger.Error(...); Console.Error.WriteLine(...); return 1; }` envelope.

    Map results to the preserved exit-code contract:
    - AddSource: ContentSourceResult.Outcome -> Added/AlreadyExistsSameUrl = 0, InvalidType = 2, SlugConflict = 3, Error = 1. The orchestrator already short-circuits InvalidType before store init (Plan 02); the adapter just maps the outcome to exit 2 and writes the exact unsupported-type message the result carries. Emit the "Computed slug"/"Added content source"/"source already exists" lines from the result (or via the progress sink).
    - SetSourceEnabled / Block / Unblock / CorpusReset / Harvest: success -> 0; carry the orchestrator's Message to Console where today's code wrote a line.
    - ListBlocked: format the `BlockedVideoListResult.Items` into the `id\t{BlockedUtc:O}\t{reason}` lines on Console.Out (the formatting that Core no longer does — D-02).
    - Distill: DistillResult.Success=false (metered-provider refusal, the `!dryRun && !isSubscriptionProvider` case) -> write the AbortedReason to Console.Error + return 1; success -> 0. Emit the dry-run / per-video lines via the progress sink (already reported during the call).
    - ContentIndexExport: take ContentIndexExportResult.Rows, do the JSON serialize (camelCase, indented) + `File.WriteAllTextAsync(path, json + "\n")` + Directory.CreateDirectory + the default `content-kb/seed/index-seed.json` path here (D-06); print "Exported {n} rows to {path}". (Plan 42-05 pins this serialized byte shape with a golden fixture — keep the camelCase options, indentation, property order, null handling, and trailing `+ "\n"` exactly as today. Factor the serialize step into a small named helper the test can call, OR keep it in a `static`/`internal` method exposed to the test assembly via the existing `[InternalsVisibleTo]` so Plan 42-05 can serialize through the SAME code path.)

    DELETE the now-unused internal-static overloads and private domain helpers that were lifted to Core in Wave 2 (RunDistillAsync internal overload, RunHarvestAsync internal overload, HarvestExplicitVideoIdsAsync, HarvestSourceAsync, HarvestVideoAsync, ResolveHarvestVideoIdAsync, PersistTranscriptResultAsync, DistillVideoAsync, MarkSkippedOverCapAsync, FilterTags, the counts classes DistillCounts/HarvestCounts/DistillVideoOutcome/HarvestVideoResolution, the validators ValidateClips/ValidateSummary/ValidateTranscriptLength/CountWords, the cost helpers, the constants block, the ContentIndexExportRow record — now in Core). Keep `ParseVideoIds` (it parses the CLI `--video-ids` option string; not domain logic). Keep `HandleContentSourceUniqueViolationAsync`/`ExceptionContains` ONLY if the orchestrator did not absorb the unique-violation classification; if the orchestrator returns the slug-conflict-vs-same-url outcome, delete the CLI copy.

    Do NOT add a static back-compat shim of the old `RunDistillAsync(stores...)` signature (D-09) — the test calls the orchestrator directly now.
  </action>
  <verify>
    <automated>"/mnt/c/Program Files/dotnet/dotnet.exe" build DeckFlow.sln -warnaserror 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "HarvestVideoAsync|DistillVideoAsync|HarvestSourceAsync|MarkSkippedOverCapAsync|class DistillCounts|class HarvestCounts|record ContentIndexExportRow" DeckFlow.CLI/ContentKbCommandRunners.cs` returns nothing (domain logic + lifted records removed — ORCH-02 / SC2; anchored to declarations, not comments).
    - `grep -n "ContentKbOrchestrator\|IContentKbOrchestrator" DeckFlow.CLI/ContentKbCommandRunners.cs` shows each public Run*Async constructs/uses the orchestrator; the resolved artifactRoot is passed via `new ContentKbOrchestratorOptions` (`grep -n "ContentKbOrchestratorOptions" DeckFlow.CLI/ContentKbCommandRunners.cs` matches).
    - The CLI progress sink implements `IOrchestratorProgress` and writes via `Console.WriteLine`; no `new Progress<` appears in `DeckFlow.CLI/ContentKbCommandRunners.cs` (`grep -n "new Progress<" DeckFlow.CLI/ContentKbCommandRunners.cs` returns nothing).
    - `grep -nE "internal static .*Task<int> RunDistillAsync\(" DeckFlow.CLI/ContentKbCommandRunners.cs` returns nothing (no static store-injecting shim — D-09).
    - The Postgres-vs-Sqlite branch + `PostgresConnectionStringNormalizer` still appears in the corpus-reset adapter in CLI (D-05 host-side); `ContentKbCliPaths.Resolve*` still called in CLI (D-06).
    - `DeckFlow.CLI/Program.cs` lines 257-307 still compile against the public Run*Async signatures (no Program.cs edit required; if a signature changed, Program.cs updated to match and still maps to Environment.ExitCode).
    - `dotnet build DeckFlow.sln -warnaserror` = 0 errors / 0 warnings.
  </acceptance_criteria>
  <done>All public Run*Async are thin adapters; no domain logic remains in CLI; provider/path resolution stays host-side; artifactRoot passed via ContentKbOrchestratorOptions; synchronous IOrchestratorProgress console sink; the index-export JSON serialize is reachable by the test assembly; build clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Lift Fake* stores to a shared file, add shared Throwing* doubles, and re-point CommandRunnerValidateClipsTests to the orchestrator seam (D-09)</name>
  <behavior>
    - Constructing ContentKbOrchestrator with the fakes and calling DistillAsync (limit:10, dryRun:false, isSubscriptionProvider:true) with all-zero-timestamp clips:
      * returns a DistillResult with Success=true (the parity of the old exit-code 0). (isSubscriptionProvider:true here is correct — this is the SUBSCRIPTION/validation-fail-short-circuit case; it passes the metered-refusal gate then fails on all-zero clips. The metered-refusal exit-1 case, isSubscriptionProvider:false, is pinned separately in Plan 42-05.)
      * records exactly one distill status update of "failed" for video id 10.
      * writes NO summary rows, NO clip rows, NO upserted index rows.
    - Each Throwing* double throws InvalidOperationException from every interface member, so any unexpected call by the distill path fails loudly (proving the path does not touch harvest-only deps).
  </behavior>
  <read_first>
    - DeckFlow.Core.Tests/CommandRunnerValidateClipsTests.cs (current test + the PRIVATE nested fake stores at ~lines 78+: FakeContentSourceStore, FakeContentVideoStore, FakeContentSiteIndexStore, FakeContentHarvestRunStore, FakeLlmSpendLedger, FakeLlmDistillationService — these get LIFTED to the shared file; only the call site at lines 56-76 + the using at line 1 also change)
    - DeckFlow.Core/Orchestration/ContentKbOrchestrator.cs (ctor parameter order + DistillAsync signature) + ContentKbOrchestratorOptions.cs (the options record to construct)
    - DeckFlow.Core/Orchestration/DistillResult.cs (Success/status fields to assert against)
    - DeckFlow.Core/Content/IBlockedVideoStore.cs, IWhisperSpendLedger.cs; DeckFlow.Core/Integration/IYouTubeChannelVideoLister.cs, ITranscriptSource.cs, IFfmpegAudioChunker.cs (the interfaces to stub as throwing doubles)
  </read_first>
  <action>
    First, LIFT the shared fakes (round-2 MEDIUM test-double fix): move the Fake* store classes that are currently PRIVATE nested types inside `CommandRunnerValidateClipsTests` out into a new shared file `DeckFlow.Core.Tests/Orchestration/FakeOrchestratorStores.cs`, namespace `DeckFlow.Core.Tests` (the project's single test namespace per CLAUDE.md). Change them from `private sealed class` to `internal sealed class` so both this anchor test and Plan 42-05's parity tests can construct them. Move FakeContentSourceStore, FakeContentVideoStore, FakeContentSiteIndexStore, FakeContentHarvestRunStore, FakeLlmSpendLedger, FakeLlmDistillationService verbatim (preserve their recording surfaces: StatusUpdates/Summaries/Clips/inserts) — do not change behavior, only visibility + location. Keep the supporting record types they reference (StatusUpdate, SummaryWrite, ClipWrite, etc.) reachable (move alongside if they were nested too).

    Create `DeckFlow.Core.Tests/Orchestration/ThrowingOrchestratorDependencies.cs`, namespace `DeckFlow.Core.Tests`, with one `internal sealed` `Throwing*` test double per harvest-only orchestrator dependency the distill path must not invoke: `ThrowingBlockedVideoStore : IBlockedVideoStore`, `ThrowingWhisperSpendLedger : IWhisperSpendLedger`, `ThrowingYouTubeChannelVideoLister : IYouTubeChannelVideoLister`, `ThrowingTranscriptSource : ITranscriptSource`, `ThrowingFfmpegAudioChunker : IFfmpegAudioChunker`. Each member throws `new InvalidOperationException("<TypeName>.<Member> must not be called by the distill path")`. These are shared doubles reused by Plan 42-05's parity tests.

    Then rewrite the call site in `DeckFlow.Core.Tests/CommandRunnerValidateClipsTests.cs` to use the new seam (D-09): replace `using DeckFlow.CLI;` with the Orchestration namespace; construct `new ContentKbOrchestrator(...)` passing the now-shared fake stores (FakeContentSourceStore, FakeContentVideoStore, FakeContentSiteIndexStore, FakeContentHarvestRunStore, FakeLlmSpendLedger, FakeLlmDistillationService) plus the `Throwing*` doubles above for the harvest-only deps. Pass `() => new DateTimeOffset(2026,5,27,12,34,56,TimeSpan.Zero)`, a `new ContentKbOrchestratorOptions { ArtifactRoot = <temp dir> }`, and no logger (NullLogger default).

    Call `await orchestrator.DistillAsync(limit:10, dryRun:false, isSubscriptionProvider:true, videoIds:null, progress:null, cancellationToken:CancellationToken.None)`. Keep the assertions semantically identical: assert the result represents the old exit-0 success (`result.Success` true), `videoStore.StatusUpdates` equals `[new StatusUpdate(10, "failed")]`, `videoStore.Summaries` empty, `videoStore.Clips` empty, `indexStore.UpsertedRows` empty. Rename the test method only if needed for clarity; the assertions stay as-is.

    Do NOT reintroduce a static `ContentKbCommandRunners.RunDistillAsync(stores...)` to keep the old call working — that is the forbidden back-compat shim (D-09).
  </action>
  <verify>
    <automated>"/mnt/c/Program Files/dotnet/dotnet.exe" test DeckFlow.Core.Tests/DeckFlow.Core.Tests.csproj --filter "FullyQualifiedName~CommandRunnerValidateClips" 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `DeckFlow.Core.Tests/Orchestration/FakeOrchestratorStores.cs` declares the lifted Fake* stores as `internal sealed` (`grep -n "internal sealed class FakeContentVideoStore" DeckFlow.Core.Tests/Orchestration/FakeOrchestratorStores.cs` matches); the originals are no longer declared inside CommandRunnerValidateClipsTests (`grep -n "private sealed class FakeContentVideoStore" DeckFlow.Core.Tests/CommandRunnerValidateClipsTests.cs` returns nothing).
    - `DeckFlow.Core.Tests/Orchestration/ThrowingOrchestratorDependencies.cs` declares the five `Throwing*` doubles, each implementing its interface with every member throwing (`grep -c "throw new InvalidOperationException" DeckFlow.Core.Tests/Orchestration/ThrowingOrchestratorDependencies.cs` >= 5).
    - `grep -n "using DeckFlow.CLI;" DeckFlow.Core.Tests/CommandRunnerValidateClipsTests.cs` returns nothing (CLI dependency removed from the test).
    - `grep -n "new ContentKbOrchestrator\|DistillAsync" DeckFlow.Core.Tests/CommandRunnerValidateClipsTests.cs` matches (calls the new seam); the artifactRoot is passed via `new ContentKbOrchestratorOptions` (`grep -n "ContentKbOrchestratorOptions" DeckFlow.Core.Tests/CommandRunnerValidateClipsTests.cs` matches).
    - The four original assertions are present and unchanged in intent: StatusUpdates == [(10,"failed")], Summaries empty, Clips empty, UpsertedRows empty.
    - `dotnet test --filter CommandRunnerValidateClips` passes (1 passed); if VSTest is unreliable in WSL, fall back to `dotnet build DeckFlow.Core.Tests -warnaserror` clean + push-and-watch CI, and the SUMMARY records which path verified it.
    - `dotnet build DeckFlow.sln -warnaserror` = 0 errors / 0 warnings.
  </acceptance_criteria>
  <done>Shared Fake* stores lifted to FakeOrchestratorStores.cs (internal) and shared Throwing* doubles exist; the anchor test exercises the orchestrator via ctor injection (options-record artifactRoot) with identical assertions; no static shim; Core.Tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | CLI command-line entry remains the same trust boundary it was; behavior parity is the goal. No new external surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42-06 | Tampering / Data-integrity | result -> exit-code mapping | mitigate | The adapter must map DistillResult.Success=false (metered refusal, !dryRun && !isSubscriptionProvider) to exit 1 and a successful validation-fail run to exit 0 exactly as today; the re-pointed anchor test pins the validation-fail-before-DB-write invariant and Plan 42-05 pins the full exit-code matrix incl. the metered refusal. Acceptance criteria enumerate the per-operation exit-code contract. |
| T-42-07 | Repudiation | live Console progress ordering | mitigate | The CLI progress sink implements the synchronous IOrchestratorProgress (Console.WriteLine directly), preserving today's interleaving of per-video lines; acceptance criteria forbid `new Progress<` in the adapter. |
| T-42-08 | Information Disclosure | corpus-reset Postgres connection string | mitigate | The Postgres connection string stays host-side (CLI), never enters Core (D-05); it is read from the CLI arg and not logged — unchanged from today. The orchestrator options record carries only the artifactRoot path, no connection string. |
| T-42-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs intended. Task 1 build-checks `IServiceCollection` resolves via existing transitive references and explicitly forbids adding a DI.Abstractions package without user approval; the SUMMARY must flag it as a blocker if genuinely needed. |
</threat_model>

<verification>
- `dotnet build DeckFlow.sln -warnaserror` = 0E/0W.
- CLI grep gates: no lifted domain methods/records remain (declaration-anchored); orchestrator is constructed in each adapter; artifactRoot passed via ContentKbOrchestratorOptions; synchronous IOrchestratorProgress sink (no `new Progress<`); no static back-compat shim.
- AddContentKbOrchestrator() forwards facade + all five sub-interfaces to one scoped concrete (6 forwarding registrations); no bare-string/options registration inside the extension (host supplies the options).
- Provider/path resolution stays host-side (D-05/D-06 grep checks).
- Shared Fake* stores lifted to FakeOrchestratorStores.cs (internal) + shared Throwing* doubles created; CommandRunnerValidateClipsTests passes via the new seam (or build-clean + CI fallback per WSL VSTest constraint).
- No new NuGet package added to any csproj.
</verification>

<success_criteria>
- ContentKbCommandRunners are thin adapters; zero domain logic in CLI (SC2 / ORCH-02).
- AddContentKbOrchestrator() exists in Core and forwards every slice interface to one scoped instance, ctor consumes the options record so DI needs no bare-string registration (SC4 prerequisite, D-01/D-08).
- Anchor test re-pointed to the ctor seam, assertions identical, no static shim; Fake* stores lifted to a shared file for 42-05 reuse (D-09 / SC3).
- Solution builds clean; Core.Tests green (SC3).
</success_criteria>

<output>
Create `.planning/phases/42-orchestrator-extraction/42-03-SUMMARY.md` when done.
</output>
</content>
