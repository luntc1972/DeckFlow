# Phase 999.4: Truncated-JSON Response UX - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-05-21
**Phase:** 999.4-truncated-json-response-ux
**Areas discussed:** Catch-site layer, Message wording + per-AI copy, Scope inventory across parse sites, Test gate + verification

---

## Pre-discuss: ROADMAP scaffold

| Option | Description | Selected |
|--------|-------------|----------|
| Run discuss-phase first | Stop /gsd-plan-phase 999.4 to capture goals/decisions; discuss-phase writes the detail section | ✓ |
| I'll add detail manually | User edits ROADMAP.md to add `### Phase 999.4:` section, then re-run | |
| Auto-draft detail from summary | Append minimal section inferred from summary line + Phase 999.2 UAT context | |

| Option | Description | Selected |
|--------|-------------|----------|
| Yes — insert + commit | Append `### Phase 999.4:` detail block + commit `docs(roadmap): scaffold Phase 999.4 detail section` | ✓ |
| Yes — insert only, no commit | Write to ROADMAP.md without committing | |
| Let me edit it first | Stop; user edits manually | |

**Outcome:** ROADMAP.md scaffold committed as `39c6dff` before discuss-phase resumed.

---

## Catch-site layer

| Option | Description | Selected |
|--------|-------------|----------|
| ResponseParsers / services | Wrap `JsonDocument.Parse(json)` in try/catch inside ResponseParsers.ParseAnalysisResponse, ParseSetUpgradeResponse, DeckComparisonService.ParseComparisonResponse, MetaGapService.ParseMetaGapResponse. On catch, re-throw `InvalidOperationException` with the truncation message. | ✓ |
| Controller filter additions | Add `JsonException` to each controller action's existing `catch (Exception) when (...)` filter (DeckController.cs:912, 975, 1067) | |
| JsonTextFormatterService chokepoint | Validate JSON inside `ExtractJsonPayload` before returning | |
| New TruncatedResponseException type | Introduce `sealed class TruncatedResponseException : Exception`, propagate explicitly | |

**User's choice:** ResponseParsers / services (Recommended)
**Notes:** Leverages existing controller `catch (InvalidOperationException)` filter pattern (Phase 10 / 999.1 idiom). Diff scope ~4 parse sites × 5 lines each. Smallest change consistent with established surface; no new exception type per CLAUDE.md "do not introduce abstractions beyond what task requires."

---

## Message wording + per-AI copy

| Option | Description | Selected |
|--------|-------------|----------|
| AI-agnostic universal noun | Single string: "The pasted response appears truncated — wait for the AI to finish generating before copying, then re-submit." | ✓ |
| Per-AI DisplayName injection | Inject `AiPlatform.DisplayName` per Phase 999.1 Hybrid pattern: "wait for Claude to finish" | |
| Agnostic + actionable hint to Step 2 selector | Longer copy adding "copy the entire JSON block from `{` to closing `}`" | |
| Per-AI + actionable hint | Combine DisplayName injection + actionable hint | |

**User's choice:** AI-agnostic universal noun (Recommended)
**Notes:** Phase 999.1 D-05 universal-noun convention applies; no Razor `@aiPlatform.DisplayName` plumbing into service-layer catch path. Single `const string` reused across 4 parse sites = one place to edit if wording is ever refined.

---

## Scope inventory across parse sites

| Option | Description | Selected |
|--------|-------------|----------|
| All 4 AI-response parse sites | ResponseParsers.ParseAnalysisResponse + ParseSetUpgradeResponse + DeckComparisonService.ParseComparisonResponse + MetaGapService.ParseMetaGapResponse. Carve out: MetaGapService.cs:407 EDH Top16 override + PacketArtifactStore.cs:529 zip-load | ✓ |
| Just Step 3 (deck-analysis main response) | Only ResponseParsers.ParseAnalysisResponse | |
| All 4 + zip-upload embedded responses | Add `/deck-analysis/upload` PacketArtifactStore path | |
| All 4 + classification nuance | Sniff exception message: truncation vs malformed | |

**User's choice:** All 4 AI-response parse sites (Recommended)
**Notes:** Symmetric scope matches Phase 999.3 D-08 ("all 3 services symmetric"). Carve-outs: EDH Top16 override (admin/dev use, already has its own catch), PacketArtifactStore zip-load (different failure mode, already has its own catch), ScryfallTaggerParsers (upstream API surface). Plan author audits PacketArtifactStore message during scout for follow-up note if needed.

---

## Test gate + verification

| Option | Description | Selected |
|--------|-------------|----------|
| xUnit on ResponseParsers + manual UAT | Pure-helper static parse path → no HTTP, no WSL VSTest flake risk. Assert `InvalidOperationException` thrown with locked message. Plus manual UAT round-trip on 3 pages. | ✓ |
| Manual UAT only | Skip xUnit; manual UAT only (matches Phase 999.1/999.2 model) | |
| Manual UAT + grep gate | Manual UAT plus structural grep assertion of catch-block count | |
| xUnit + manual UAT + grep gate | All three combined | |

**User's choice:** xUnit on ResponseParsers + manual UAT (Recommended)
**Notes:** First backlog phase to land xUnit; pure JSON-parse tests are NOT subject to the dynamic-service WSL VSTest flake that caused 999.1 / 999.2 to opt out. Manual UAT remains the binding gate if WSL VSTest still flakes despite the pure-helper shape. xUnit + manual UAT structurally prevents future catch-block removal regression.

---

## Wrap-up gate

| Option | Description | Selected |
|--------|-------------|----------|
| Looks good — write CONTEXT.md | Lock the 4 decisions, capture canonical refs, commit, route to /gsd-plan-phase 999.4 | ✓ |
| Discuss textarea preservation explicitly | Lock D-05 requiring controller actions preserve `*ResponseJson` property on re-render | |
| Discuss logging level | Lock D-05 on Warning-level `_logger.LogWarning` on truncation catch | |
| Discuss README update scope | Lock D-05 README troubleshooting sweep | |

**User's choice:** Looks good — write CONTEXT.md
**Notes:** Textarea preservation noted as plan-author scout spot-check under D-04 manual UAT criteria rather than a separate D-05. Logging and README captured under `<deferred>`.

---

## Claude's Discretion

- Host file for `TruncatedResponseMessage` const (ResponseParsers default vs new shared class).
- Test method naming style within DeckFlow.Web.Tests conventions.
- Whether `ParseComparisonResponse` / `ParseMetaGapResponse` need `internal` exposure (vs public-surface tests with mocked HTTP) for the new xUnit facts.
- Commit ordering within the plan (sequential by file vs by UAT criticality).
- Exact local-variable / refactor pattern around `JsonDocument.Parse` to preserve dispose semantics (try/local-assign vs private static helper).

## Deferred Ideas

- `PacketArtifactStore.cs:529` zip-load user-facing message audit (separate phase if message is unfriendly)
- Per-AI message variants (`@aiPlatform.DisplayName` injection) — rejected by D-02
- Classification nuance (truncation vs malformed JSON) — rejected by D-03
- Telemetry logging on truncation catch (`_logger.LogWarning`)
- Actionable-hint expansion ("copy from `{` to `}`")
- Per-Step-3-vs-Step-5 message differentiation on deck-analysis
- README troubleshooting section (plan-author grep at execution; defer if zero hits)
