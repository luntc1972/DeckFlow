---
phase: 28-housekeeping-bundle
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/28-housekeeping-bundle/28-DISCOVERY.md
autonomous: false
requirements: [HSK-02]

must_haves:
  truths:
    - "A provable codex CLI read-isolation mechanism is identified and documented, OR the lack of one is documented with a re-demote recommendation"
    - "The exact codex command-spec (executable + isolation flags + Raw envelope) the implementation will use is recorded"
    - "The codex model env var name + mini-tier default are decided (D-07)"
    - "A live sentinel-exfil probe protocol against the real codex CLI is specified for VERIFICATION"
  artifacts:
    - path: ".planning/phases/28-housekeeping-bundle/28-DISCOVERY.md"
      provides: "Codex isolation decision + command-spec + model config + probe protocol, with evidence"
      contains: "isolation"
  key_links:
    - from: "28-DISCOVERY.md isolation decision"
      to: "28-04-PLAN implementation"
      via: "command-spec + envelope kind + model env var"
      pattern: "CliEnvelopeKind.Raw"
---

<objective>
Investigate codex CLI tool/read-isolation options and decide a PROVABLE mechanism for running untrusted transcripts through codex without arbitrary local-file read access. Produce 28-DISCOVERY.md with the chosen mechanism, the exact command-spec the implementation will use, the model-config decision, and the live-probe protocol — OR a documented re-demote recommendation if no provable boundary exists.

Purpose: D-01 makes proven untrusted-input read isolation a NON-NEGOTIABLE acceptance gate for HSK-02; D-02 makes the mechanism researcher-decided and forbids pre-committing to one in planning before evidence. This phase's research was skipped, so this discovery unit supplies the decision that 28-04 (implementation) consumes. D-03 governs the fallback.
Output: 28-DISCOVERY.md with an isolation decision backed by evidence, and a blocking decision checkpoint for the user to ratify ship-vs-redemote.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-housekeeping-bundle/28-CONTEXT.md
@.planning/phases/28-housekeeping-bundle/28-PATTERNS.md

# The seam codex will plug into:
@DeckFlow.Core/Integration/CliLlmDistillationService.cs
@DeckFlow.Core/Integration/CliCommandSpec.cs
@DeckFlow.Core/Integration/LlmDistillationProviderFactory.cs
</context>

<scope_fence>
ALLOWED FILE SET (touch ONLY this):
- CREATE: .planning/phases/28-housekeeping-bundle/28-DISCOVERY.md
Do NOT modify any source code, tests, or other planning docs in this plan. This is research/decision output only. No package installs.
</scope_fence>

<tasks>

<task type="auto">
  <name>Task 1: Investigate codex isolation options and write 28-DISCOVERY.md</name>
  <files>
    .planning/phases/28-housekeeping-bundle/28-DISCOVERY.md
  </files>
  <read_first>
    - .planning/ROADMAP.md Backlog "Codex Distill Backend" section (lines 167-174) — the acceptance bar: codex exec is an agent; --sandbox read-only blocks writes but not reads; ship only with a verified no-tools mode OR a sandbox/container exposing only stdin
    - .planning/phases/28-housekeeping-bundle/28-CONTEXT.md D-01 through D-07 (isolation bar, researcher-decided mechanism, fallback, sentinel test, UAT depth, spend=0, model config)
    - DeckFlow.Core/Integration/CliLlmDistillationService.cs lines 103-129 (BuildCommandSpec claude branch + override path) and 271-275 (Raw envelope extraction)
    - DeckFlow.Core/Integration/CliCommandSpec.cs (CliEnvelopeKind.Raw)
  </read_first>
  <action>
    Investigate the installed codex CLI's isolation surface. Determine via codex --help / codex exec --help / official docs which of these provably prevents arbitrary local-file reads when processing an untrusted (prompt-injected) transcript on stdin: (a) a no-tools / disabled-tools mode, (b) a sandbox mode that exposes only stdin (no filesystem read), (c) a container/namespace wrapper. For each candidate, record what it does and does NOT block (writes vs reads), and cite the evidence (help text excerpt, doc URL, or observed behavior). Pick the ONE mechanism that is demonstrably read-isolating. Write 28-DISCOVERY.md containing: (1) Decision — the chosen mechanism (or "no provable boundary found"); (2) Command-spec — the exact executable name and argument list, including the isolation flags and the model flag, that the codex branch of BuildCommandSpec will emit, with CliEnvelopeKind.Raw (model text arrives on raw stdout); note that the Windows path requires the DECKFLOW_LLM_CLI_COMMAND JSON-array override the same way the claude branch does; (3) Model config (D-07) — the env var name (e.g. DECKFLOW_LLM_CODEX_MODEL) and the mini-tier default model id, following the ReadTimeout env-read pattern; (4) Sentinel-exfil test design (D-04) — the seam-level xUnit test shape (temp sentinel file + prompt-injected transcript that tries to read it + assert sentinel content never appears in any output) AND the one-time live-probe protocol against the real codex CLI for the VERIFICATION; (5) Parity checklist — confirm the existing JSON-repair / ValidateTags / timeout / zero-token-ledger guarantees flow through unchanged via the shared ExtractWithRetryAsync path. If NO provable read boundary exists, instead write the Decision section as a re-demote recommendation per D-03 (document findings, recommend returning HSK-02 to backlog; do NOT recommend shipping behind a warning) and explicitly state that 28-04 implementation must NOT proceed. Preserve LF line endings. Do not invent a mechanism that the codex CLI does not actually provide — every claim must cite help text, docs, or observed CLI behavior.
  </action>
  <verify>
    <automated>test -f .planning/phases/28-housekeeping-bundle/28-DISCOVERY.md</automated>
  </verify>
  <acceptance_criteria>
    - 28-DISCOVERY.md exists with a clear Decision section: either a named provable isolation mechanism OR a re-demote recommendation.
    - If ship: an exact command-spec (executable + argument list + isolation flags + model flag) is recorded, paired with CliEnvelopeKind.Raw.
    - If ship: the model env var name and mini-tier default model id are stated (D-07).
    - A sentinel-exfil seam-test design and a live-probe protocol are both specified (D-04).
    - Every isolation claim cites concrete evidence (help text excerpt, doc URL, or observed behavior) — no unsupported assertions.
    - The file states explicitly whether 28-04 implementation should proceed or halt.
  </acceptance_criteria>
  <done>28-DISCOVERY.md records an evidence-backed isolation decision (mechanism + command-spec + model config + probe protocol), or a documented re-demote recommendation, and states whether 28-04 proceeds.</done>
</task>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Task 2: Decision gate — ship codex backend or re-demote (D-01/D-03)</name>
  <files>.planning/phases/28-housekeeping-bundle/28-DISCOVERY.md</files>
  <action>
    Present the 28-DISCOVERY.md finding to the user and obtain an explicit ship-vs-redemote decision before any code is written. This is a security gate (untrusted-input read isolation) and is NEVER auto-approvable — workflow.auto_advance must be ignored here. If the discovery names a provable read-isolation mechanism with cited evidence, the user may select "ship" (proceed to 28-04). If the discovery found no provable boundary, the only valid path is "redemote" per D-03 — do NOT ship behind a warning.
  </action>
  <decision>Ship the codex distill backend (HSK-02) or re-demote it to backlog?</decision>
  <context>
    D-01 makes a PROVEN untrusted-input read-isolation boundary a non-negotiable acceptance gate; D-03 says if no provable boundary exists, re-demote to backlog with findings documented and do NOT ship behind a warning. This checkpoint ratifies the 28-DISCOVERY.md finding before any implementation.
  </context>
  <options>
    <option id="ship">
      <name>Proceed to 28-04 implementation</name>
      <pros>HSK-02 closes; codex becomes a second subscription-backed distill provider</pros>
      <cons>Only valid if 28-DISCOVERY.md names a provable read-isolation mechanism with cited evidence</cons>
    </option>
    <option id="redemote">
      <name>Re-demote HSK-02 to backlog (D-03 fallback)</name>
      <pros>No unsafe codex spawn ships; claude backend already covers the use case</pros>
      <cons>HSK-02 stays open; ROADMAP + REQUIREMENTS need a re-demote note</cons>
    </option>
  </options>
  <verify>
    <human-check>User has reviewed 28-DISCOVERY.md and selected ship or redemote.</human-check>
  </verify>
  <resume-signal>Select: ship or redemote (based on the 28-DISCOVERY.md evidence)</resume-signal>
  <done>User has ratified the ship-vs-redemote decision; 28-04 proceeds only on "ship".</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| untrusted transcript (prompt-injected) → codex exec agent | the core threat: a malicious transcript could instruct codex to read+echo local files |
| codex CLI → local filesystem | codex exec is an agent; --sandbox read-only blocks writes but NOT reads |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-05 | Information Disclosure | codex reads arbitrary local files under prompt injection | mitigate | This discovery exists to find a PROVABLE read boundary (no-tools mode / stdin-only sandbox / container); D-03 fallback re-demotes if none exists — ship is blocked until proven |
| T-28-06 | Elevation of Privilege | codex agent tool use beyond distillation | mitigate | Chosen mechanism must disable agent tools entirely; documented + later proven by sentinel-exfil test |
| T-28-SC | Tampering | npm/pip/cargo installs | accept | No installs in discovery; codex CLI assumed pre-installed (operator-provided), legitimacy is operator's host concern |
</threat_model>

<verification>
- 28-DISCOVERY.md exists with an evidence-backed Decision and (if ship) a concrete command-spec + model config + probe protocol.
- The decision checkpoint is presented to the user and resolved before 28-04 runs.
</verification>

<success_criteria>
Supplies the D-02 researcher-decided isolation mechanism (or D-03 re-demote) that gates HSK-02 / Phase 28 SC #1. 28-04 implementation is unblocked only on a "ship" decision.
</success_criteria>

<output>
Create `.planning/phases/28-housekeeping-bundle/28-03-SUMMARY.md` when done.
</output>
