---
phase: 29-core-xml-doc-backfill-gate-widen
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - DeckFlow.Core/Reporting/ReconciliationReporter.cs
  - DeckFlow.Core/Reporting/CategoryCardReporter.cs
  - DeckFlow.Core/Reporting/CategoryCountReporter.cs
  - DeckFlow.Core/Reporting/CategorySuggestionReporter.cs
  - DeckFlow.Core/Reporting/CategoryFilter.cs
  - DeckFlow.Core/Filtering/DeckEntryFilter.cs
autonomous: true
requirements: [HSK-01]

must_haves:
  truths:
    - "Probe build over DeckFlow.Core/Reporting/*.cs and DeckFlow.Core/Filtering/*.cs reports 0 CS1591/CS1573/CS1587 warnings"
    - "Every public type and member in the 6 Reporting+Filtering files has an XML doc comment"
    - "No raw-string instruction-text content or indentation changed in ReconciliationReporter.cs"
  artifacts:
    - path: "DeckFlow.Core/Reporting/ReconciliationReporter.cs"
      provides: "Documented reconciliation reporter (8 sites incl. raw-string constants)"
      contains: "/// <summary>"
    - path: "DeckFlow.Core/Reporting/CategoryCardReporter.cs"
      provides: "Documented category-card reporter (2 sites)"
      contains: "/// <summary>"
    - path: "DeckFlow.Core/Filtering/DeckEntryFilter.cs"
      provides: "Documented deck-entry filter (1 site)"
      contains: "/// <summary>"
  key_links:
    - from: "DeckFlow.Core/Reporting/ReconciliationReporter.cs"
      to: "DeckFlow.Core/Models/DeckDiff (existing)"
      via: "documented method params referencing DeckDiff"
      pattern: "paramref"
---

<objective>
Add XML doc comments to every undocumented public type and member in the `DeckFlow.Core/Reporting/` (5 files, ~15 sites) and `DeckFlow.Core/Filtering/` (1 file, 1 site) folders. ReconciliationReporter alone holds 8 sites including two raw-string instruction constants (MEDIUM risk).

Purpose: Reporting+Filtering is a contiguous, non-overlapping slice of the HSK-01 Core backfill. Documenting it cleanly is a prerequisite for the Wave-2 gate widen.
Output: 6 fully-documented source files; probe build over Reporting and Filtering reports 0 doc warnings.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<scope_fence>
ALLOWED FILE SET (Codex MUST NOT touch any file outside this list):
- DeckFlow.Core/Reporting/ReconciliationReporter.cs
- DeckFlow.Core/Reporting/CategoryCardReporter.cs
- DeckFlow.Core/Reporting/CategoryCountReporter.cs
- DeckFlow.Core/Reporting/CategorySuggestionReporter.cs
- DeckFlow.Core/Reporting/CategoryFilter.cs
- DeckFlow.Core/Filtering/DeckEntryFilter.cs

You may create and delete a TEMPORARY `DeckFlow.Core/.editorconfig` probe file ONLY for verification; it MUST be removed before commit and MUST NOT appear in `git status`.
Do NOT touch `.editorconfig` at repo root. Do NOT touch any other Core folder. Ignore tasks belonging to plans 29-01, 29-03, 29-04, 29-05.
Before starting, run `git status --short` and confirm a clean tree; if other modified files appear, STOP and report.
</scope_fence>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-core-xml-doc-backfill-gate-widen/29-RESEARCH.md
@.planning/phases/29-core-xml-doc-backfill-gate-widen/29-PATTERNS.md

# Gold-standard doc-style exemplars (READ - match this style exactly):
@DeckFlow.Core/Content/IContentVideoStore.cs
@DeckFlow.Core/Content/ContentVideoStore.cs
</context>

<constraints>
From CLAUDE.md + 29-RESEARCH.md (full authority - every constraint is blocking):
- Touch ONLY the `///` doc lines you add. Do NOT run Format Document / Code Cleanup.
- NEVER convert `{ get; init; }` to `{ get; }`. None of these files contain init accessors, but the rule stands.
- NEVER re-indent C# raw-string literals (triple-quote). ReconciliationReporter.cs has 4 raw-string instruction blocks - doc `///` lines go ABOVE the `public const string` declaration, never inside a raw-string block.
- NEVER inline an attribute onto a declaration line.
- Preserve LF line endings.
- Build env: `"/mnt/c/Program Files/dotnet/dotnet.exe"`. Clean build is the gate.
- Commits: plain default-author; one logical change per commit.
</constraints>

<tasks>

<task type="auto">
  <name>Task 1: Probe the work list and document ReconciliationReporter (raw-string danger zone)</name>
  <files>DeckFlow.Core/Reporting/ReconciliationReporter.cs</files>
  <read_first>
    - DeckFlow.Core/Reporting/ReconciliationReporter.cs (current state - 8 sites; note raw-string constants CategoryFixInstructions, MoxfieldImportInstructions)
    - .planning/phases/29-core-xml-doc-backfill-gate-widen/29-PATTERNS.md (Pattern 9 ReconciliationReporter raw-string-safe doc adds)
    - .planning/phases/29-core-xml-doc-backfill-gate-widen/29-RESEARCH.md (Probe build methodology; Risk Files table)
    - DeckFlow.Core/Content/ContentVideoStore.cs (method doc style)
  </read_first>
  <action>
    First run the probe: create temp `DeckFlow.Core/.editorconfig` (root=false, `[*.cs]` with CS1591/1573/1587 = warning), clean non-incremental build via Windows dotnet.exe, `grep -E "warning CS(1591|1573|1587)" | sort -u`, filter to Reporting and Filtering files; remove the temp file. That deduped list is the work list. Then document `ReconciliationReporter.cs`: add a single-line `<summary>` ABOVE each `public const string` instruction constant (per 29-PATTERNS.md Pattern 9 exact summaries) and `<summary>`/`<param>`/`<returns>` on `WriteReport` and `ToText` using `<paramref name="diff"/>` and `<paramref name="outputPath"/>`. Critical: every `///` line goes above the declaration keyword line; do NOT add a `///` line inside or between the triple-quote delimiters, and do not re-indent the raw-string instruction content.
  </action>
  <verify>
    <automated>grep -c "/// <summary>" DeckFlow.Core/Reporting/ReconciliationReporter.cs</automated>
  </verify>
  <acceptance_criteria>
    - `DeckFlow.Core/.editorconfig` removed after probe; absent from `git status`.
    - `grep -c "/// <summary>" DeckFlow.Core/Reporting/ReconciliationReporter.cs` is at least 4 (2 constants + 2 methods, plus any type-level doc).
    - `git diff DeckFlow.Core/Reporting/ReconciliationReporter.cs` shows no change to any line containing a triple-quote delimiter or to lines between delimiters.
  </acceptance_criteria>
  <done>ReconciliationReporter fully documented with raw-string instruction blocks untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Document the four remaining Reporting/Filtering files and probe-verify clean</name>
  <files>DeckFlow.Core/Reporting/CategoryCardReporter.cs, DeckFlow.Core/Reporting/CategoryCountReporter.cs, DeckFlow.Core/Reporting/CategorySuggestionReporter.cs, DeckFlow.Core/Reporting/CategoryFilter.cs, DeckFlow.Core/Filtering/DeckEntryFilter.cs</files>
  <read_first>
    - DeckFlow.Core/Reporting/CategoryCardReporter.cs (current state - 2 sites)
    - DeckFlow.Core/Reporting/CategoryCountReporter.cs (current state - 2 sites)
    - DeckFlow.Core/Reporting/CategorySuggestionReporter.cs (current state - 2 sites)
    - DeckFlow.Core/Reporting/CategoryFilter.cs (current state - 1 site)
    - DeckFlow.Core/Filtering/DeckEntryFilter.cs (current state - 1 site)
    - .planning/phases/29-core-xml-doc-backfill-gate-widen/29-PATTERNS.md (Pattern 1 method docs; one-line form; shared patterns)
    - DeckFlow.Core/Content/IContentSourceStore.cs (utility doc style analog)
  </read_first>
  <action>
    Document each of the 5 files per the Task-1 probe list. For each undocumented type add a `<summary>`; for each undocumented public method add `<summary>` plus `<param>` for every parameter (in declaration order) and `<returns>` only when a value is returned, with `<see langword="null"/>` phrasing for nullable returns and `<see cref="..."/>` for cross-type references. Use the single-line `/// <summary>text.</summary>` form for concise members and the multi-line form for longer descriptions, matching the IContentSourceStore.cs analog. Then re-run the probe and confirm ZERO CS1591/CS1573/CS1587 warnings for every file under `DeckFlow.Core/Reporting/` and `DeckFlow.Core/Filtering/`.
  </action>
  <verify>
    <automated>DOTNET="/mnt/c/Program Files/dotnet/dotnet.exe"; printf '%s\n' 'root = false' '' '[*.cs]' 'dotnet_diagnostic.CS1591.severity = warning' 'dotnet_diagnostic.CS1573.severity = warning' 'dotnet_diagnostic.CS1587.severity = warning' > DeckFlow.Core/.editorconfig; "$DOTNET" build DeckFlow.Core/DeckFlow.Core.csproj --no-incremental 2>&1 | grep -E "warning CS(1591|1573|1587)" | grep -E "Reporting/|Filtering/" | sort -u > /tmp/29-02.log; rm -f DeckFlow.Core/.editorconfig; if [ -s /tmp/29-02.log ]; then echo FAIL; cat /tmp/29-02.log; else echo "OK 0 Reporting/Filtering doc warnings"; fi</automated>
  </verify>
  <acceptance_criteria>
    - Probe yields 0 CS1591/CS1573/CS1587 warnings for any `DeckFlow.Core/Reporting/` or `DeckFlow.Core/Filtering/` file (`/tmp/29-02.log` empty).
    - `DeckFlow.Core/.editorconfig` removed; absent from `git status`.
    - `git diff --stat` lists only the 6 allowed files.
    - `"/mnt/c/Program Files/dotnet/dotnet.exe" build DeckFlow.Core/DeckFlow.Core.csproj -c Release` reports 0 errors.
  </acceptance_criteria>
  <done>All 6 Reporting/Filtering files documented; folder-scoped probe shows 0 doc warnings; temp probe file removed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer edit -> source tree | `///` insertions near raw-string instruction blocks risk literal mutation |
| temp probe file -> repo | A lingering `DeckFlow.Core/.editorconfig` would alter solution-wide warning severity |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-02-01 | Tampering | Raw-string instruction constants in ReconciliationReporter.cs | mitigate | `///` lines added above the const declaration only; git diff acceptance asserts no triple-quote line changed |
| T-29-02-02 | Tampering | Temp probe `DeckFlow.Core/.editorconfig` | mitigate | Probe file removed and absence asserted; never committed |
| T-29-02-03 | Denial of Service | Build pipeline | accept | Doc comments are compile-stripped; zero runtime/behavior change possible |
| T-29-02-SC | Tampering | npm/pip/cargo installs | n/a | No package installs in this phase |
</threat_model>

<verification>
- Probe build reports 0 doc warnings for `DeckFlow.Core/Reporting/` and `DeckFlow.Core/Filtering/`.
- `git status --short` shows no `DeckFlow.Core/.editorconfig`.
- `git diff --stat` lists only the 6 allowed files.
- Full Core build clean (0 errors) under the still-active global suppressor.
</verification>

<success_criteria>
All ~16 undocumented public sites across the 6 Reporting+Filtering files carry XML doc comments matching the Content KB exemplar style; folder-scoped probe build is warning-free; ReconciliationReporter raw-string instruction blocks unchanged; only allowed files changed.
</success_criteria>

<output>
Create `.planning/phases/29-core-xml-doc-backfill-gate-widen/29-02-SUMMARY.md` when done.
</output>
