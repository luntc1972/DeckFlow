---
phase: 72-command-zone-commander-castability
plan: 07
type: execute
wave: 4
depends_on: [72-05]
files_modified:
  - DeckFlow.Web.Tests/DeckAnalysisPacketServiceTests.cs
  - README.md
  - DeckFlow.Web/Help/manabase.md
autonomous: true
requirements: [F-02, F-03]
must_haves:
  truths:
    - "Loading a Moxfield companion deck does NOT inject the companion as deck content into the deck-analysis packet"
    - "The deck-analysis packet for a companion/Background deck is byte-identical whether the manabase.commander-castability flag is OFF or ON (companion side-metadata is inert to DeckAnalysisPacketService)"
    - "README and Help/manabase.md document the command-zone callout, the companion +3 'to hand' heuristic, and the new flag"
  artifacts:
    - path: "DeckFlow.Web.Tests/DeckAnalysisPacketServiceTests.cs"
      provides: "deck-analysis companion-inert byte-identity tests (flag OFF + ON)"
      contains: "companion"
    - path: "DeckFlow.Web/Help/manabase.md"
      provides: "companion + command-zone castability help section"
      contains: "ompanion"
    - path: "README.md"
      provides: "Phase 72 what's-new entry"
  key_links:
    - from: "DeckAnalysisPacketService board filter"
      to: "companion entries"
      via: "exclusion-based filter never includes a companion (Moxfield not injected; Archidekt stays mainboard either way)"
      pattern: "maybeboard"
---

<objective>
Lock the negative guard: prove the companion side-metadata is INERT to deck-analysis (the only
deck-analysis work in Phase 72; §E rendering is deferred to Phase 73), and document the new
feature for users. Deck-analysis output must be byte-identical with the manabase flag OFF AND
ON for the same imported deck, and the Moxfield companion must never leak into deck content.

Purpose: DeckAnalysisPacketService filters by EXCLUSION (RESEARCH Pitfall 1) — a regression
test is the only thing standing between "inert side metadata" and a silent prod deck-analysis change.
Output: deck-analysis byte-identity tests (F-02/F-03) + README + Help/manabase.md updates.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/72-command-zone-commander-castability/72-RESEARCH.md
@.planning/phases/72-command-zone-commander-castability/72-SPEC.md

<interfaces>
<!-- Verified contracts. -->
- DeckAnalysisPacketService.BuildAsync(DeckAnalysisRequest request, CancellationToken) → DeckAnalysisPacketResult (~:333); board filter is EXCLUSION-based: `!= "maybeboard" && != "sideboard"` (ResolvePreScryfallCommanderState ~:165-169), so any companion entry with Board != maybeboard/sideboard would be treated as deck content.
- Phase 72 design (72-03): Moxfield companion is NOT injected as an entry (name-only side metadata); Archidekt companion stays Board="mainboard" unchanged. DeckAnalysisPacketService does NOT read the manabase.commander-castability flag or DetectedCompanionName.
- DeckFlow.Web.Tests/DeckAnalysisPacketServiceTests.cs — existing test seams (fake loader / fixtures) to mirror.
- Help/manabase.md sections: Step 1 Load (~:12), Deck type/importance (~:21), Reduced costs (~:26), Step 2 Read The Report / Castability (~:42-58), Notes (~:83).
- README.md manabase "What's new" entries (~:164-213) — append a Phase 72 entry in the same style; plain ASCII, no em/en dashes.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Deck-analysis companion-inert byte-identity tests (F-02, F-03)</name>
  <files>DeckFlow.Web.Tests/DeckAnalysisPacketServiceTests.cs</files>
  <read_first>
    - DeckFlow.Web/Services/DeckAnalysisPacketService.cs (ResolvePreScryfallCommanderState exclusion filter ~:160-175, BuildAsync ~:333)
    - DeckFlow.Web.Tests/DeckAnalysisPacketServiceTests.cs (existing fixture/loader-fake patterns)
    - DeckFlow.Core.Tests/Fixtures/moxfield-companion-direct.json (companion deck for the no-leak assertion)
  </read_first>
  <behavior>
    - Building the deck-analysis packet for a Moxfield companion deck yields deck content that does NOT contain the companion card name (companion not injected as an entry).
    - The deck-analysis packet bytes for a companion/Background deck are identical with the manabase.commander-castability flag OFF and ON (the flag never reaches DeckAnalysisPacketService).
  </behavior>
  <action>
    Add two tests to DeckAnalysisPacketServiceTests. (F-02/no-leak) Drive the analysis packet build for a deck
    whose import carries a companion (use the loader/importer path or a fake returning entries equivalent to
    the Moxfield companion fixture: mainboard + commander, NO companion entry) and assert the produced packet's
    deck content does not include the companion card name and the entry set equals the non-companion baseline.
    (F-03/flag-invariance) Build the packet twice for the same companion/Background-bearing request with the
    manabase.commander-castability flag toggled OFF then ON (via the existing fake feature-flag cache) and
    assert the two packet outputs are byte-for-byte equal. Mirror the existing test seams; no production change
    expected (if a test reveals the companion DOES leak, that is a real defect to fix in 72-03 — surface it).
  </action>
  <verify>
    <automated>"/mnt/c/Program Files/dotnet/dotnet.exe" test DeckFlow.Web.Tests --filter "FullyQualifiedName~DeckAnalysisPacketServiceTests"</automated>
  </verify>
  <acceptance_criteria>
    - Companion-deck packet excludes the companion card name (no injection).
    - Flag OFF vs ON packet bytes are identical for the same imported deck.
    - All existing DeckAnalysisPacketServiceTests still green.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: README + Help/manabase.md documentation</name>
  <files>README.md, DeckFlow.Web/Help/manabase.md</files>
  <read_first>
    - README.md (manabase "What's new" entries ~:164-213)
    - DeckFlow.Web/Help/manabase.md (Step 2 Castability ~:42-58, Notes ~:83)
  </read_first>
  <action>
    Add a Phase 72 "What's new" entry to README.md (style-matched to the existing manabase entries) describing:
    the flagged `manabase.commander-castability` feature — a command-zone castability callout above the
    per-card table showing each commander/partner/background's cast-on-curve chance, the commander row moved
    out of the per-card table (display-only; health/verdict unchanged), companion auto-detection
    (Archidekt category / Moxfield direct API) with a manual designator fallback for pasted lists and the
    Moxfield Spellbook-fallback path, and the companion's +3 generic "to hand" cost modeled as a stated
    heuristic; note flag default OFF = unchanged output. In Help/manabase.md add a section explaining the
    command-zone callout, partners/Background, and the companion +3 heuristic (clear about it being an
    approximation, not a rules-exact sequence). Plain ASCII only (no em/en dashes). Keep both consistent with
    the shipped behavior.
  </action>
  <verify>
    <automated>grep -qi "companion" DeckFlow.Web/Help/manabase.md &amp;&amp; grep -qi "command-zone\|command zone\|commander-castability" README.md &amp;&amp; echo OK</automated>
  </verify>
  <acceptance_criteria>
    - README has a Phase 72 manabase entry mentioning the command-zone callout, companion +3 heuristic, and the flag.
    - Help/manabase.md documents the callout + companion heuristic; no em/en dashes introduced.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| importer → deck-analysis | companion side-metadata must not cross into deck-analysis content |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-72-07 | Tampering | DeckAnalysisPacketService byte-identity | mitigate | Regression tests assert no companion leak + flag OFF/ON packet equality (Pitfall 1 guard) |
| T-72-07b | Tampering | npm/nuget installs | n/a | No package installs in this plan |
</threat_model>

<verification>
- `"/mnt/c/Program Files/dotnet/dotnet.exe" test DeckFlow.Web.Tests --filter "FullyQualifiedName~DeckAnalysisPacketServiceTests"` green.
- README + Help/manabase.md updated; doc grep checks pass.
</verification>

<success_criteria>
- Deck-analysis proven byte-identical (flag OFF + ON) with companion side-metadata inert and no companion
  leak; user-facing docs updated for the command-zone callout + companion heuristic.
</success_criteria>

<output>
Create `.planning/phases/72-command-zone-commander-castability/72-07-SUMMARY.md` when done.
</output>
