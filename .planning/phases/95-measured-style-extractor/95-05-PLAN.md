---
phase: 95-measured-style-extractor
plan: 05
type: execute
wave: 3
depends_on: [95-03, 95-04]
files_modified:
  - DeckFlow.Core/Knowledge/MeasuredStyleExtraction/CategoryCounter.cs
  - DeckFlow.Core/Knowledge/MeasuredStyleExtraction/LiftCalculator.cs
  - DeckFlow.Core.Tests/MeasuredStyleExtraction/CategoryCounterTests.cs
  - DeckFlow.Core.Tests/MeasuredStyleExtraction/LiftCalculatorTests.cs
autonomous: true
requirements: [CS-06, CS-07]
must_haves:
  truths:
    - "A multi-category card is counted in EVERY bucket it qualifies for, never just its first match (D-06, Command Zone New Era rule)"
    - "Synergy is a lift ratio Pr(A∩B)/(Pr(A)·Pr(B)) using the creator numerator over the global baseline denominator — not raw co-occurrence (D-07)"
    - "The lift metric demotes ubiquitous staples relative to raw co-occurrence"
  artifacts:
    - path: "DeckFlow.Core/Knowledge/MeasuredStyleExtraction/CategoryCounter.cs"
      provides: "Multi-bucket category counting over staple-stripped decks (pure)"
      min_lines: 30
    - path: "DeckFlow.Core/Knowledge/MeasuredStyleExtraction/LiftCalculator.cs"
      provides: "Creator-numerator / global-baseline lift math (pure)"
      min_lines: 30
  key_links:
    - from: "LiftCalculator"
      to: "GlobalCategoryBaseline"
      via: "Pr(A)·Pr(B) denominator + catA|catB pair-key lookup"
      pattern: "DecksWithCategoryPair|DecksWithCategory"
---

<objective>
The two remaining pure algorithm pieces: `CategoryCounter` (counts each card in every category bucket it qualifies for — CS-06 / Command Zone New Era rule — consuming the already-resolved multi-bucket category map, never re-filtering to first match) and `LiftCalculator` (the D-07 lift metric: creator `Pr(A∩B)` over global `Pr(A)·Pr(B)` from the `GlobalCategoryBaseline`, NOT raw co-occurrence — the metric that demotes staples).

Purpose: CS-06 and CS-07, the discriminating synergy signal.
Output: Two pure static classes + tests, including a test that proves lift demotes staples versus raw co-occurrence.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/95-measured-style-extractor/95-CONTEXT.md
@.planning/phases/95-measured-style-extractor/95-RESEARCH.md
@DeckFlow.Core/Reporting/CategoryFilter.cs

<interfaces>
<!-- Inputs (Plans 03 + 04). -->
From DeckFlow.Core/Knowledge/MeasuredStyleExtraction/MeasuredStyleInputs.cs (Plan 04):
  IReadOnlyList<CreatorDeckSample> Samples; IReadOnlyDictionary<string, IReadOnlyList<string>> CardCategories; GlobalCategoryBaseline Baseline;
From DeckFlow.Core/Knowledge/GlobalCategoryBaseline.cs (Plan 03):
  int TotalDecks; IReadOnlyDictionary<string,int> DecksWithCategory; IReadOnlyDictionary<string,int> DecksWithCategoryPair;  // pair key = "catA|catB" sorted lexicographically
From DeckFlow.Core/Reporting/CategoryFilter.cs:
  IReadOnlyList<string> IncludedOrFallback(IEnumerable<string>);  // already multi-bucket — do NOT add .First()/.Take(1)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: CategoryCounter — multi-bucket counting (CS-06)</name>
  <read_first>
    - DeckFlow.Core/Reporting/CategoryFilter.cs (IncludedOrFallback already returns ALL qualifying categories)
    - DeckFlow.Core/Knowledge/MeasuredStyleExtraction/MeasuredStyleInputs.cs (CardCategories map)
    - 95-PATTERNS.md §"Multi-category ... never first-match (D-06)" (explicit anti-pattern: no .First()/.Take(1))
  </read_first>
  <behavior>
    - A card mapped to {ramp, draw} increments BOTH the ramp and the draw bucket for its deck
    - Per-deck category presence is boolean-per-category (a deck with 3 ramp cards counts once toward "decks-with-ramp" for lift, but total ramp-card counts are also available for ratio metrics)
    - Excluded card-type labels (Creature/Artifact) are dropped via CategoryFilter, not counted as buckets
  </behavior>
  <action>Create `CategoryCounter.cs` as a `public static class`. Provide `CountPerDeck(CreatorDeckSample, IReadOnlyDictionary<string, IReadOnlyList<string>> cardCategories) → IReadOnlyDictionary<string,int>` returning, for one deck, the count of cards in each category bucket — for each card, look up its categories, pass them through `CategoryFilter.IncludedOrFallback`, and increment EVERY returned bucket (never take only the first). Provide `DeckCategoryPresence(CreatorDeckSample, cardCategories) → IReadOnlySet<string>` returning the distinct set of categories present in the deck (for the lift numerator). Provide `AggregateCounts(IReadOnlyList<CreatorDeckSample>, cardCategories) → IReadOnlyDictionary<string,double>` returning the mean per-deck count per category across the creator's decks (a category-ratio metric). Never introduce `.First()`/`.Take(1)` on the category collection. Pure, total. Preserve LF line endings.</action>
  <verify>
    <automated>dotnet test DeckFlow.Core.Tests --filter "FullyQualifiedName~CategoryCounterTests"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "\.First()\|\.Take(1)" DeckFlow.Core/Knowledge/MeasuredStyleExtraction/CategoryCounter.cs` == 0
    - Tests prove a {ramp,draw} card increments both buckets; a Creature-only label is excluded; presence-set is deduped per deck
    - `dotnet test DeckFlow.Core.Tests --filter "FullyQualifiedName~CategoryCounterTests"` passes
  </acceptance_criteria>
  <done>Multi-bucket category counting + per-deck presence + creator-mean ratios, no first-match shortcut.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: LiftCalculator — creator numerator / global baseline (CS-07)</name>
  <read_first>
    - DeckFlow.Core/Knowledge/GlobalCategoryBaseline.cs (TotalDecks + the two dictionaries + the "catA|catB" sorted-key convention)
    - DeckFlow.Core/Knowledge/MeasuredStyleExtraction/CategoryCounter.cs (DeckCategoryPresence — the numerator source)
    - 95-CONTEXT.md D-07 (creator Pr(A∩B) / global Pr(A)·Pr(B), NOT raw co-occurrence)
    - 95-RESEARCH.md §"Code Examples" (the lift-baseline read shape)
  </read_first>
  <behavior>
    - Lift(A,B) = creatorPr(A∩B) / (globalPr(A) · globalPr(B)) where creatorPr(A∩B) = (creator decks with both A and B) / (creator deck count), globalPr(X) = Baseline.DecksWithCategory[X] / Baseline.TotalDecks
    - The pair key is looked up as the lexicographically-sorted "catA|catB" (matching GlobalCategoryBaseline)
    - A staple-like category pair with high raw co-occurrence but high global marginals yields LOWER lift than a rare-but-creator-favored pair (demotes staples)
    - Division-by-zero guards: a category absent from the global baseline (Pr = 0) yields a defined result (skip/omit that pair rather than NaN/Infinity)
  </behavior>
  <action>Create `LiftCalculator.cs` as a `public static class`, following `KarstenManabase`'s pure-math-helper shape (static, plain numeric/collection params, doc comment stating the formula provenance = D-07). Provide `ComputeLift(IReadOnlyList<CreatorDeckSample> creatorDecks, IReadOnlyDictionary<string, IReadOnlyList<string>> cardCategories, GlobalCategoryBaseline baseline) → IReadOnlyList<CategoryLift>` where `CategoryLift` is a small record `{ string CategoryA; string CategoryB; double Lift; int CreatorDecksWithBoth; }`. Compute creator per-deck presence via `CategoryCounter.DeckCategoryPresence`, count creator decks containing each category and each sorted category pair, compute `creatorPr(A∩B)/(globalPr(A)·globalPr(B))`, and guard against zero denominators (omit or return a documented sentinel — choose omission with a `// Why:` comment). Do NOT emit raw co-occurrence counts as the metric — lift only. Pure, total. Preserve LF line endings.</action>
  <verify>
    <automated>dotnet test DeckFlow.Core.Tests --filter "FullyQualifiedName~LiftCalculatorTests"</automated>
  </verify>
  <acceptance_criteria>
    - Tests include a "demotes staples" case: two pairs with equal creator co-occurrence but different global marginals produce different lift, with the higher-global-marginal pair scoring lower
    - Tests prove the zero-baseline guard (a category absent from the baseline does not yield NaN/Infinity)
    - The pair key is read as sorted "catA|catB" (order-independent)
    - `dotnet test DeckFlow.Core.Tests --filter "FullyQualifiedName~LiftCalculatorTests"` passes
  </acceptance_criteria>
  <done>Lift = creator numerator / global baseline, staple-demoting, zero-guarded, pure and tested.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host → pure lift math | Pre-resolved creator + global-baseline numbers cross into pure Core functions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-95-05-01 | Tampering (misleading metric) | raw co-occurrence masquerading as synergy | mitigate | LiftCalculator emits lift only (Pr(A∩B)/(Pr(A)·Pr(B))); no raw-count metric path — enforced by the demotes-staples test |
| T-95-05-02 | DoS (NaN/Infinity poisoning downstream) | zero-denominator lift | mitigate | Explicit zero-baseline guard with documented omission; tested |
| T-95-05-03 | Elevation (host boundary leak) | pure math namespace | mitigate | No HttpClient/Web references (D-11) — same namespace guard as Plan 04 |
| T-95-05-SC | Tampering | package installs | mitigate | No new packages this plan |
</threat_model>

<verification>
- `dotnet build DeckFlow.Core/DeckFlow.Core.csproj` clean
- `dotnet test DeckFlow.Core.Tests --filter "FullyQualifiedName~CategoryCounter|FullyQualifiedName~LiftCalculator"` green
</verification>

<success_criteria>
- Multi-bucket category counting, no first-match (CS-06)
- Lift = creator Pr(A∩B) / global Pr(A)·Pr(B), staple-demoting, zero-guarded (CS-07)
</success_criteria>

<output>
Create `.planning/phases/95-measured-style-extractor/95-05-SUMMARY.md` when done.
</output>
