---
phase: 90-directpush-correctness-seed-sync
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - DeckFlow.Studio/Services/GitBodyCoverageAudit.cs
  - DeckFlow.Studio/Services/IGitBodyCoverageAudit.cs
  - DeckFlow.Studio.Tests/Services/GitBodyCoverageAuditTests.cs
autonomous: true
requirements: [SYNC-07]
must_haves:
  truths:
    - "A read-only pre-flip audit enumerates approved+visible prod rows whose body .md is MISSING from the local git content-kb tree and reports the count and list, WITHOUT deleting, reconciling, or writing anything (that is P91). (D-11)"
    - "The audit reads prod via the existing structurally-read-only IProdContentReader path (single SELECT, no DDL) and checks body presence via a safe-path containment check, so it can never write to prod or escape the content-kb root. (D-11)"
  artifacts:
    - path: "DeckFlow.Studio/Services/GitBodyCoverageAudit.cs"
      provides: "read-only cross-reference of visible+approved prod rows vs git-tree body presence"
      contains: "ReadAllAsync"
    - path: "DeckFlow.Studio/Services/IGitBodyCoverageAudit.cs"
      provides: "audit contract returning missing-body rows (report-only)"
      contains: "interface IGitBodyCoverageAudit"
  key_links:
    - from: "GitBodyCoverageAudit"
      to: "IProdContentReader.ReadAllAsync"
      via: "read-only prod row source (no write path)"
      pattern: "ReadAllAsync"
---

<objective>
Add a read-only pre-flip git-coverage audit (D-11): for every approved + visible prod `content_site_index` row, check whether its body `.md` exists in the local git `content-kb` tree (what will become `/app` after deploy), and report the rows whose body is MISSING. This makes the SYNC-07 flip precondition — "every visible row's body is actually in git" — explicit and observable BEFORE the operator flips `sync.directpush-gitbody` ON.

Purpose: STATE.md records ~70/106 prod rows may be body-only on the `/data` overlay and not in the approved git seed. The moment the flag flips ON, such a row's detail page goes 404. This audit surfaces the count/list so the operator can git-commit missing bodies first. It is READ-ONLY reporting this phase — it does NOT delete or reconcile (that is Phase 91's reconciler).
Output: a Studio read-only audit service + interface + unit tests; no prod writes, no deletes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/90-directpush-correctness-seed-sync/90-CONTEXT.md
@.planning/phases/90-directpush-correctness-seed-sync/90-PATTERNS.md
@.planning/phases/90-directpush-correctness-seed-sync/90-RESEARCH.md
</context>

<interfaces>
<!-- Extracted from 90-PATTERNS.md / 90-RESEARCH.md. -->

Prod row source (read-only, reuse — do NOT add a write path) — ProdContentReader.cs:16-62 / IProdContentReader.cs:1-20:
- `Task<IReadOnlyList<ContentSiteIndexRow>> ReadAllAsync(string connectionString, CancellationToken ct = default)` — single SELECT, SslMode.Require, never calls EnsureSchema. This interface is "deliberately read-only" per its own doc comment.
- Prod connection string comes from `Studio:ProdConnectionString` (used at DirectPushCoordinator.cs:393, PullFromProdCoordinator.cs:84).

ContentSiteIndexRow fields available (per 89 work): approval_status, is_visible/is_hidden, plus the per-row artifact/body path used to build the on-disk `.md` location. Determine each row's body relative path the same way the serving resolver does (a `content-kb/...` relative path); reuse the resolver's safe-path/containment concept — do NOT invent a second path-validation routine (90-RESEARCH Security Domain; ContentKbArtifactPathResolver.cs:175-193 `IsSafeArtifactPath`).

No existing analog for the cross-reference utility itself (90-PATTERNS § "No Analog Found"): compose `IProdContentReader.ReadAllAsync` (prod rows) + `File.Exists` against the local repo content-kb root. The local repo root is the same checkout Studio commits into via `IGitRepository` (DirectPushCoordinator's repo-root resolution).
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Read-only git-coverage audit service (report missing bodies)</name>
  <read_first>
    - DeckFlow.Studio/Services/ProdContentReader.cs (16-62) + IProdContentReader.cs (1-20) — the read-only prod-read contract to consume
    - DeckFlow.Studio/ViewModels/DirectPushCoordinator.cs (how the repo root / content-kb path + Studio:ProdConnectionString are resolved — grep `repoRoot`, `ProdConnectionString`)
    - DeckFlow.Web/Services/Content/ContentKbArtifactPathResolver.cs (175-193 IsSafeArtifactPath — the containment concept to reuse, not re-implement)
    - 90-PATTERNS.md § "No Analog Found" (compose ReadAllAsync + File.Exists) and § "Read-only prod accessor"
  </read_first>
  <behavior>
    - Given prod rows where only some approved+visible rows have a body file present in the local content-kb tree, the audit returns EXACTLY the approved+visible rows whose body file is MISSING (not the ones present, not hidden/pending rows).
    - The audit performs NO writes: it calls only the read-only prod read + File.Exists; it never calls upsert/set/delete/EnsureSchema and never constructs a prod store factory.
    - A row with an unsafe/uncontained artifact path is reported as missing/invalid (never resolved outside the content-kb root).
  </behavior>
  <action>Create `IGitBodyCoverageAudit` with a single method returning a report of missing-body rows (e.g. `Task<GitBodyCoverageReport> RunAsync(string prodConnectionString, string repoRoot, CancellationToken ct)`), and `GitBodyCoverageAudit` implementing it. Implementation: call `IProdContentReader.ReadAllAsync(prodConnectionString, ct)`; filter to rows that are approved AND visible; for each, compute the expected body `.md` path under `Path.Combine(repoRoot, "content-kb", ...)` using the row's artifact-relative path, guarded by the same safe-path/containment check the resolver uses (reuse the static guard or the resolver; do NOT write a second validator); use `File.Exists` to test presence; collect rows whose file is absent (or path invalid) into the report with enough identity to act on (natural key + title + expected path). Return a report object exposing the missing count and the list. The service MUST take `IProdContentReader` by constructor injection (read-only) and MUST NOT reference `IProdStoreFactory` or any write API (fail-safe: structurally incapable of writing prod — 90-RESEARCH Security T-90 feature_flags/read-only pattern). Add xUnit tests in DeckFlow.Studio.Tests using a fake IProdContentReader returning a mix of present/missing/hidden/pending rows against a temp directory tree: assert only approved+visible+missing rows are reported, present rows are excluded, hidden/pending rows are excluded, and an unsafe path is reported (not silently resolved).</action>
  <verify>
    <automated>dotnet build DeckFlow.Studio.Tests -c Debug; dotnet test DeckFlow.Studio.Tests --filter "FullyQualifiedName~GitBodyCoverageAudit"</automated>
  </verify>
  <acceptance_criteria>
    - SOURCE: `GitBodyCoverageAudit` depends only on `IProdContentReader` (+ path helpers); grep shows NO `IProdStoreFactory`, `Upsert`, `SetVisibility`, `Delete`, or `EnsureSchema` reference in the file.
    - SOURCE: body-path resolution uses the existing safe-path/containment guard (no new path-validation routine).
    - BEHAVIOR: tests prove only approved+visible+missing rows are reported; present rows excluded; hidden/pending excluded; unsafe path flagged not resolved.
    - BEHAVIOR: the audit returns a count + actionable list (natural key + expected path) and writes nothing.
  </acceptance_criteria>
  <done>Read-only audit reports approved+visible prod rows whose body is missing from the git tree; tests green; no write path exists.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Studio -> prod Postgres | audit reads prod rows; must be structurally read-only |
| prod row artifact path -> local filesystem check | row-supplied path used to build a File.Exists probe |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-90-04 | Tampering (accidental prod write) | audit touching prod | mitigate | Depend ONLY on the read-only `IProdContentReader` (single SELECT, no DDL); no `IProdStoreFactory`/write API referenced. Enforced by a source grep in acceptance criteria. |
| T-90-05 | Tampering (path traversal) | row artifact path -> File.Exists | mitigate | Reuse the existing `IsSafeArtifactPath`/containment guard; invalid/uncontained paths are reported, never probed outside the content-kb root. |
| T-90-SC | Supply chain | package installs | mitigate | No new packages — composes existing types + `System.IO.File`. No install step. |
</threat_model>

<verification>
- DeckFlow.Studio.Tests builds and the audit tests pass.
- Audit is read-only (source grep confirms no write/DDL/store-factory reference).
- MANUAL (flag-flip time, non-unit): before flipping `sync.directpush-gitbody` ON, run the audit against `Studio:ProdConnectionString` + the local repo and confirm the missing-body count is zero (or git-commit the listed bodies first). This is the rollout precondition for Plan 90-01's flip.
</verification>

<success_criteria>
- A read-only audit reports approved+visible prod rows whose body is missing from the git `/app` tree, with an actionable list; it never writes to prod or deletes anything.
</success_criteria>

<output>
Create `.planning/phases/90-directpush-correctness-seed-sync/90-02-SUMMARY.md` when done.
</output>
