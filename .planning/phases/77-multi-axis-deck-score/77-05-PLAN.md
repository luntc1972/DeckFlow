---
phase: 77-multi-axis-deck-score
plan: 05
type: execute
wave: 3
depends_on: ["77-04"]
files_modified:
  - DeckFlow.Web/Views/Deck/DeckAnalysis.cshtml
  - DeckFlow.Web/wwwroot/css/site-common.css
  - DeckFlow.Web.Tests/DeckAnalysisScoreViewTests.cs
autonomous: true
requirements: [SCORE-01, SCORE-04]
must_haves:
  truths:
    - "When Model.Score is non-null, the Step-3 summary panel renders a four-card score grid above the Overview"
    - "Each axis card shows the numeral, a 5-pip meter, a band-label pill, and the inline rationale (four redundant level encodings; not color-only)"
    - "The bracket cross-check note renders with --agree (glyph + green) or --diverge (glyph + gold) state"
    - "When Model.Score is null the page is byte-identical to today (no eyebrow, no grid)"
    - "A hidden ScoreJson form field round-trips the serialized score"
    - "Score CSS lives in site-common.css with baked-hex band modifiers; no per-theme fork edit needed"
  artifacts:
    - path: "DeckFlow.Web/Views/Deck/DeckAnalysis.cshtml"
      provides: "Score block markup + hidden ScoreJson field"
      contains: "chatgpt-score"
    - path: "DeckFlow.Web/wwwroot/css/site-common.css"
      provides: "Score classes, band-color modifiers/tokens, 4->2->1 responsive grid"
      contains: "chatgpt-score-grid"
    - path: "DeckFlow.Web.Tests/DeckAnalysisScoreViewTests.cs"
      provides: "Flag-OFF byte-identity + score-present render test"
  key_links:
    - from: "DeckAnalysis.cshtml score block"
      to: "Model.Score + MultiAxisScorer.BandLabel"
      via: "@if (Model.Score is not null) render with per-axis band/rationale"
      pattern: "Model\\.Score|BandLabel"
---

<objective>
Render the score block in the Step-3 results panel of `/deck-analysis` and add the hidden
`ScoreJson` round-trip field, plus the supporting layout CSS in `site-common.css` (baked-hex band
pills, 4->2->1 responsive grid, cross-check callout). Lock flag-OFF byte-identity and score-present
rendering with a Razor render test.

Purpose: SCORE-01 (coarse bands on screen, no decimals), SCORE-04 (the `/deck-analysis` surface, no
new tile). OFF path stays byte-identical.
Output: view + CSS + render test (GREEN). Theme/mobile visual sign-off is plan 77-06.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/77-multi-axis-deck-score/UI-SPEC.md
@.planning/phases/77-multi-axis-deck-score/77-PATTERNS.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Render the score block and hidden ScoreJson field in DeckAnalysis.cshtml</name>
  <read_first>
    - DeckFlow.Web/Views/Deck/DeckAnalysis.cshtml (the `@if (Model.AnalysisResponse is not null)` summary panel lines 519-564; `<h3>Analysis Summary</h3>` insertion point ~line 530; existing `<textarea name="DeckProfileJson">@Model.Request.DeckProfileJson</textarea>` hidden-field pattern line 511; `@if (Model.CommandZoneAwarenessEnabled)` byte-identity guard style)
    - .planning/phases/77-multi-axis-deck-score/UI-SPEC.md §1-2 (placement order), §6 (microcopy), §9 (a11y: role=group aria-label, aria-hidden pip meter, glyph + class + text on cross-check)
    - 77-PATTERNS.md "DeckAnalysis.cshtml (MODIFIED)" section (the exact `@if (Model.Score is not null)` markup with the foreach axis grid, pip loop, band pill, rationale, and cross-check note)
    - DeckFlow.Core/Analysis/MultiAxisScorer.cs (BandLabel for the pill text + aria-label)
  </read_first>
  <action>
    Insert the score block per 77-PATTERNS between `<h3>Analysis Summary</h3>` and the existing Overview/`stack`
    markup, guarded by `@if (Model.Score is not null)` so the OFF/absent path is byte-identical. Render
    `.chatgpt-score` wrapper: a `.chatgpt-score__eyebrow` heading, a `.chatgpt-score-grid` foreach over the four
    axes (POWER/SPEED/CONTROL/CONSISTENCY) emitting `.chatgpt-score-card .chatgpt-score-band--@band` with
    `.chatgpt-score-label`, `.chatgpt-score-value` numeral, a 5-pip `.chatgpt-score-meter`
    (`.chatgpt-score-pip--filled` for pip<=band, `aria-hidden="true"`), a `.chatgpt-score-band` pill with
    `@MultiAxisScorer.BandLabel(band)`, and the `.chatgpt-score-rationale` line; each card `role="group"` with a
    full `aria-label` ("POWER score: N of 5, Label"). Render `.chatgpt-score-crosscheck
    .chatgpt-score-crosscheck--@(Model.Score.ScoreAlignsBracket ? "agree" : "diverge")` with a leading glyph and
    `Model.Score.BracketCrossCheckText`, `role="note"`. Add a hidden `<textarea name="ScoreJson"
    ...>@Model.Request.ScoreJson</textarea>` next to the DeckProfileJson field so the score round-trips. Use the
    `·` separator in page UI only (artifact text stays ASCII from 77-04). Add `@using DeckFlow.Core.Analysis` if
    needed. Touch only changed lines.
  </action>
  <verify>
    <automated>grep -c "chatgpt-score" DeckFlow.Web/Views/Deck/DeckAnalysis.cshtml; dotnet.exe build DeckFlow.Web 2>&1 | tail -2</automated>
  </verify>
  <acceptance_criteria>
    - DeckAnalysis.cshtml contains `@if (Model.Score is not null)` wrapping a `.chatgpt-score-grid` with four axis cards
    - DeckAnalysis.cshtml contains `<textarea name="ScoreJson"` rendering `@Model.Request.ScoreJson`
    - Each score card carries `role="group"` and an `aria-label` containing the band word (a11y, not color-only)
    - `dotnet.exe build DeckFlow.Web` reports 0 warnings 0 errors
  </acceptance_criteria>
  <done>Score grid + cross-check + hidden ScoreJson render under the Model.Score guard; OFF path unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Add score CSS to site-common.css (band pills, responsive grid, cross-check)</name>
  <read_first>
    - DeckFlow.Web/wwwroot/css/site-common.css (`.manabase-health--*` baked-hex pill pattern lines 2564-2586; `.bracket-callout` left-border callout lines 1852-1868; `.manabase-lens`/`-label`/`-big` soft-card chrome lines 2602-2676)
    - .planning/phases/77-multi-axis-deck-score/UI-SPEC.md §4 (net-new classes + band hex values), §7 (theme tokens — baked ink, no per-theme override needed), §8 (4->2->1 responsive grid)
    - 77-PATTERNS.md "site-common.css (MODIFIED)" section (which existing classes to mirror) + Pitfall 7 (CSS in site-common.css, NOT site.css)
  </read_first>
  <action>
    Add all score classes to site-common.css ONLY (never site.css): `.chatgpt-score`, `.chatgpt-score__eyebrow`
    (mirror `.manabase-lens-label` uppercase/letter-spaced/--muted), `.chatgpt-score-grid` with the §8 4->2->1
    media queries (max-width 860px -> 2 cols, 520px -> 1 col), `.chatgpt-score-card` (mirror `.manabase-lens` soft
    card), `.chatgpt-score-label`, `.chatgpt-score-value` (mirror `.manabase-lens-big` big numeral),
    `.chatgpt-score-meter`, `.chatgpt-score-pip`/`--filled`, `.chatgpt-score-band`, `.chatgpt-score-band--0..--5`
    baked-hex bg+ink per UI-SPEC §4 (the `.manabase-health--*` pattern; do NOT use semantic per-theme vars for the
    band fill), `.chatgpt-score-rationale` (--muted, --fs-xs), and `.chatgpt-score-crosscheck` (mirror
    `.bracket-callout`) with `--agree` (var(--success) left-border) and `--diverge` (var(--warning)/--gold-warning
    left-border). Optionally add the `--score-band-{0..5}-bg/ink` tokens to the `:root` of site-common.css for
    documentation; per UI-SPEC §7 the baked-hex modifiers carry their own contrast so NO per-theme fork edit is
    required (matches the Phase-75 tap-analyzer precedent). Touch only changed lines; preserve LF.
  </action>
  <verify>
    <automated>grep -c "chatgpt-score-grid\|chatgpt-score-band--5\|chatgpt-score-crosscheck--diverge" DeckFlow.Web/wwwroot/css/site-common.css; grep -c "chatgpt-score" DeckFlow.Web/wwwroot/css/site.css</automated>
  </verify>
  <acceptance_criteria>
    - site-common.css contains `.chatgpt-score-grid`, `.chatgpt-score-band--5`, and `.chatgpt-score-crosscheck--diverge`
    - site-common.css contains both `@media (max-width: 860px)` and `@media (max-width: 520px)` rules for `.chatgpt-score-grid`
    - `grep -c "chatgpt-score" DeckFlow.Web/wwwroot/css/site.css` returns 0 (no score CSS leaked into site.css — Pitfall 7)
  </acceptance_criteria>
  <done>Score layout + baked-hex band pills + responsive grid + cross-check callout live in site-common.css only.</done>
</task>

<task type="auto">
  <name>Task 3: Render test — flag-OFF byte-identity and score-present rendering</name>
  <read_first>
    - DeckFlow.Web.Tests/Bracket/BracketViewRenderTests.cs (Phase-76 sibling render harness — closest analog; exercises a view via IRazorViewEngine/render host with flag OFF vs present states. Copy its host/render harness for the score OFF/ON tests)
    - DeckFlow.Web.Tests/Manabase/ManabaseViewRenderTests.cs (secondary render-test analog if the bracket harness needs a fallback shape)
    - DeckFlow.Web/Views/Deck/DeckAnalysis.cshtml (the score block from Task 1)
    - DeckFlow.Web/Models/DeckAnalysisViewModel.cs (Score property to populate the ON case; AnalysisResponse must be non-null for the panel to render)
    - DeckFlow.Core/Analysis/MultiAxisScore.cs (construct a DeckMultiAxisScore fixture for the ON case)
  </read_first>
  <behavior>
    - Rendering DeckAnalysis with Model.Score == null produces markup with NO occurrence of "chatgpt-score"
    - OFF-path byte-identity (Codex MED, mirror ManabaseViewRenderTests): render once with Model.Score == null and once with a non-null Score, then assert the null-path markup EQUALS the scored markup after excising the single contiguous score-grid block (the OFF view leaks no surrounding-markup drift), not merely the absence of the class string
    - Rendering DeckAnalysis with a non-null Model.Score (and a non-null AnalysisResponse) produces markup containing "chatgpt-score-grid", all four axis labels, and the cross-check note
  </behavior>
  <action>
    Create DeckFlow.Web.Tests/DeckAnalysisScoreViewTests.cs reusing the BracketViewRenderTests.cs host/render
    harness (Phase-76 sibling — a source-text scan cannot discriminate the flag-gated block, so render the view).
    Add two tests: (1) Score null -> rendered HTML DoesNotContain "chatgpt-score"; (2) Score non-null +
    AnalysisResponse present -> rendered HTML Contains "chatgpt-score-grid" and each axis label. Use Ordinal
    comparisons.
  </action>
  <verify>
    <automated>dotnet.exe test DeckFlow.Web.Tests --filter "FullyQualifiedName~DeckAnalysisScoreView" 2>&1 | tail -6</automated>
  </verify>
  <acceptance_criteria>
    - DeckAnalysisScoreViewTests.cs has a null-Score test asserting the rendered output DoesNotContain "chatgpt-score"
    - DeckAnalysisScoreViewTests.cs has an OFF-path excision-equality test (null-path markup == scored markup minus the contiguous score-grid block)
    - DeckAnalysisScoreViewTests.cs has a non-null-Score test asserting the rendered output Contains "chatgpt-score-grid"
    - `dotnet.exe test DeckFlow.Web.Tests --filter "FullyQualifiedName~DeckAnalysisScoreView"` exits 0
    - Full `dotnet.exe test DeckFlow.Web.Tests` exits 0 (no regression)
  </acceptance_criteria>
  <done>Render test proves OFF byte-identity and ON score-grid presence; full Web suite GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Model.Score / Model.Request.ScoreJson -> rendered HTML | Score values (originating from the round-tripped ScoreJson) are rendered into the page |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-77-05-01 | Injection (XSS) | rationale / cross-check text + ScoreJson in view | mitigate | Use Razor `@`-expressions (auto HTML-encode) for all score values and the hidden textarea content; never `@Html.Raw` on score-derived strings |
| T-77-05-02 | Tampering | flag-OFF byte-identity | mitigate | Entire block inside `@if (Model.Score is not null)`; render test asserts OFF output omits "chatgpt-score" |
| T-77-SC | Tampering | package installs | mitigate | No new NuGet/npm packages; CSS + Razor only |
</threat_model>

<verification>
- `dotnet.exe build DeckFlow.Web` clean (0/0)
- `dotnet.exe test DeckFlow.Web.Tests --filter "FullyQualifiedName~DeckAnalysisScoreView"` GREEN
- `grep -c "chatgpt-score" DeckFlow.Web/wwwroot/css/site.css` == 0 (Pitfall 7)
</verification>

<success_criteria>
The Step-3 results panel renders the four-axis score grid + cross-check above the Overview when scored,
is byte-identical when not; CSS confined to site-common.css; ScoreJson round-trips; render test GREEN.
</success_criteria>

<output>
Create `.planning/phases/77-multi-axis-deck-score/77-05-SUMMARY.md` when done
</output>
