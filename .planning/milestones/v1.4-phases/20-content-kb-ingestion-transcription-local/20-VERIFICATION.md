---
phase: 20-content-kb-ingestion-transcription-local
verified: 2026-05-26T20:29:00-06:00
status: passed
score: 14/15 must-haves verified (1 live-UAT item pending)
overrides_applied: 0
human_verification:
  - test: "Run the 5-channel local caption-coverage UAT (SC1 + SC2)"
    expected: "Captions fetched for majority of 5 MTG channels via YoutubeExplode 6.6.0 with no Google.Apis 403; per-channel and aggregate whisper_fallback_ratio < 0.25 logged; each fetch logs transcript_source AND caption_track_kind; if ffmpeg is absent a single warning is logged and the run does not abort"
    why_human: "Requires internet access, OPENAI_API_KEY in environment, and the local CLI harvest command to run against live YouTube. Cannot be verified by grep or static analysis. The code path is fully wired and all supporting artifacts are verified in code; only the live execution proof is outstanding."
---

# Phase 20: Content KB Ingestion + Transcription (local) Verification Report

**Phase Goal:** The local harvester's upstream surfaces (YouTube captions, Whisper transcription) are wired through the project's pattern with proven third-party caption coverage, plus a plain local spend-log cap check — so Phase 21 can compose them into end-to-end distillation. All run locally; nothing executes on Render.
**Verified:** 2026-05-26T20:29:00-06:00
**Status:** passed
**Re-verification:** No — initial verification

---

## Goal Achievement

### Observable Truths

| #  | Truth | Status | Evidence |
|----|-------|--------|----------|
| 1  | `UpdateTranscriptStatusAsync` transitions a pending video to any of the four terminal statuses by id (allowlist-guarded) | VERIFIED | `IContentVideoStore.cs` has the member; `ContentVideoStore.cs` has `UPDATE content_videos` SQL; 6 xUnit tests in `ContentVideoStoreTests.cs` (334 lines) exercise round-trip |
| 2  | `GetVideoByYoutubeIdAsync(sourceId, youtubeVideoId)` resumes pending/failed videos instead of blanket-skipping | VERIFIED | Member present on both interface and impl; `CommandRunners.cs:574` calls `GetVideoByYoutubeIdAsync` in the harvest flow |
| 3  | CLI `content-source-add` seeds a content_sources row with a derived slug; dup-URL is idempotent (exit 0); dup-slug-different-URL errors (exit 3) | VERIFIED | `RunContentSourceAddAsync` in `CommandRunners.cs`; `content-source-add` registered in `Program.cs`; `InsertSourceAsync` wired; `SlugifySourceName.Slugify` called before insert; 20-01 SUMMARY records live CLI smoke with all three paths confirmed |
| 4  | `SlugifySourceName` produces a URL-safe ASCII slug; empty/non-ASCII names fall back to deterministic "source" | VERIFIED | `SlugifySourceName.cs` returns `"source"` fallback; `SlugifySourceNameTests.cs` (77 lines) covers ASCII, punctuation, empty, and non-ASCII cases |
| 5  | `YouTubeTranscriptFetcher` uses `TryGetByLanguage` only (never throwing `GetByLanguage`); auto-generated captions accepted; empty body classified as NoCaptions | VERIFIED | `grep TryGetByLanguage` = 2; `grep -o GetByLanguage` count equals TryGetByLanguage count (only as part of TryGetByLanguage); `IsAutoGenerated` present; `IsNullOrWhiteSpace` classification present; 4 xUnit tests in `YouTubeTranscriptFetcherTests.cs` |
| 6  | `TranscriptProviderFactory` makes `DECKFLOW_YOUTUBE_TRANSCRIPT_PROVIDER` a real toggle: "direct" resolves the fetcher, unsupported value throws with "Supported: direct" message | VERIFIED | `TranscriptProviderFactory.cs` contains `DECKFLOW_YOUTUBE_TRANSCRIPT_PROVIDER`; `NotSupportedException` (3 occurrences); `TranscriptProviderFactoryTests.cs` (45 lines) proves null, "direct", and unsupported paths |
| 7  | `ITranscriptSource.FetchTranscriptAsync` carries authoritative `knownDuration` AND verb-supplied `monthKey`; result is non-null `TranscriptFetchResult` with `TranscriptOutcome` discriminator + `SecondsBilled`/`CostUsd` | VERIFIED | `ITranscriptSource.cs`: `enum TranscriptOutcome` present, `Task<TranscriptFetchResult>` (not nullable), `knownDuration` (2 occurrences), `monthKey` (2 occurrences), `SecondsBilled` and `CostUsd` (2 occurrences each) |
| 8  | `IYouTubeAudioSource` + `AudioDownloadResult` give Whisper a concrete audio path; `DurationSeconds` documented as best-effort/non-authoritative (not hardcoded 0 after CR-02 fix); `IDisposable` cleanup | VERIFIED | `IYouTubeAudioSource.cs`: `record AudioDownloadResult`, `IDisposable`, best-effort doc string present; `YouTubeAudioSource.cs`: `GetAudioOnlyStreams`, `DownloadAsync`, `GetTempPath`, `internal YouTubeAudioSource` seam all present; CR-02 fix confirmed — `GetBestEffortDurationSecondsAsync` calls `Videos.GetAsync` for real duration, not hardcoded 0 |
| 9  | `WhisperTranscriptionService` reads the cap (`WouldExceedCapAsync`) using authoritative non-zero duration (`Math.Max(knownDuration, audio.DurationSeconds)`); returns `Failed` when both sources are 0/unknown; persists nothing; echoes verb-supplied `monthKey` | VERIFIED | `WhisperTranscriptionService.cs`: `WouldExceedCapAsync`, `Math.Max`, `MonthKey` all present; `DateTime.UtcNow` = 0 (service does not mint its own month key); `RecordCallAsync`/`InsertTranscript`/`UpdateTranscriptStatus`/`ContentVideoStore` = 0 (D-11 pure); `new HttpClient()` = 0; `.Timeout` = 0; `OPENAI_API_KEY` read from `Environment`; `WhisperTranscriptionServiceTests.cs` (366 lines) covers cap-skip, knownDuration-with-zero-audioDuration, both-unknown→Failed, success, Polly retry, chunk concat, and missing-ffmpeg cases |
| 10 | `WhisperResiliencePipeline` wraps every transcription call in a concrete 12-minute Polly timeout + retry that handles `ClientResultException` (WR-01 fix) | VERIFIED | `WhisperResiliencePipeline.cs`: `AddTimeout(TimeSpan.FromMinutes(12))` confirmed; `ShouldHandle` delegates to `ShouldRetry` which switches on `ClientResultException` in addition to `HttpRequestException` and `TimeoutRejectedException` (WR-01 fix confirmed at lines 32-33) |
| 11 | `FfmpegAudioChunker.IsAvailableAsync` is exception-safe; absent ffmpeg warns-not-aborts; `BuildSegmentArguments` is unit-testable | VERIFIED | `FfmpegAudioChunker.cs`: `IsAvailableAsync`, `segment_time`, `BuildSegmentArguments` all present; `FfmpegAudioChunkerTests.cs` (54 lines) covers arg-builder and availability-returns-false-gracefully |
| 12 | `YouTubeTranscriptSource : ITranscriptSource` composes captions-first → audio-download → Whisper-fallback as a pure service (no persistence), forwarding `knownDuration` + `monthKey` into `TranscribeAsync`; `SkippedOverCap` is distinct from `Failed` | VERIFIED | `YouTubeTranscriptSource.cs`: `: ITranscriptSource` present; `TranscribeAsync(audio, knownDuration, monthKey, ct)` called (line 53); no `ContentVideoStore`/`WhisperSpendLedger`/`InsertTranscript`/`RecordCallAsync` references (0); `SkippedOverCap` mapped distinctly from `Failed` (lines 64-65); `YouTubeTranscriptSourceTests.cs` (188 lines) proves forwarding and distinct outcomes |
| 13 | `YouTubeChannelVideoLister` uses `CollectAsync(limit)` (not `ToListAsync`); `PublishedUtc` populated from metadata (WR-02 fix) | VERIFIED | `YouTubeChannelVideoLister.cs`: `CollectAsync(` present, `ToListAsync` = 0; `GetPublishedUtcAsync` calls `Videos.GetAsync` and `MapVideo` sets `PublishedUtc = publishedUtc` (WR-02 fix confirmed at lines 69-70, 122-129) |
| 14 | `RunHarvestAsync` is the D-11 single persistence owner: derives ONE `monthKey` per video; passes authoritative `video.Duration`; maps `TranscriptOutcome` directly (no `?? Failed` collapse); writes ledger BEFORE transcript (CR-01 fix); `MarkFailedIfPossibleAsync` guarded by `!statusPersisted` (WR-05 fix); logs `transcript_source` + `caption_track_kind` + per-channel/aggregate `whisper_fallback_ratio`; warns on absent ffmpeg without aborting; harvest and content-source-add commands registered | VERIFIED | `CommandRunners.cs`: `"harvest"` in `Program.cs` (1); `RunHarvestAsync` (3); `RecordCallAsync` on Whisper branch (1); `ToString("yyyy-MM")` = 1 site only (line 572); `FetchTranscriptAsync` call passes `video.Duration` (line 573); `??  TranscriptStatus.Failed` / `?? "failed"` = 0; `RecordCallAsync` (line 640) precedes `InsertTranscriptAsync` (line 641) — CR-01 fix confirmed; `MarkFailedIfPossibleAsync` gated by `!statusPersisted` (line 580) — WR-05 fix confirmed; `caption_track_kind` (1); `transcript_source` (1); `IsAvailableAsync` (1); `whisper_fallback_ratio` (1); `FromMinutes(15)` (1); `GetVideoByYoutubeIdAsync` (1); `TranscriptProviderFactory` (2) |
| 15 | Live 5-channel UAT: captions fetched for 5 real MTG channels; `whisper_fallback_ratio < 25%`; `transcript_source` + `caption_track_kind` logged per fetch | PENDING HUMAN RUN | Code path is fully wired (see truths 5-14 above). Requires internet + OPENAI_API_KEY + local CLI execution. Documented as PENDING-HUMAN-RUN in 20-04-SUMMARY.md. |

**Score:** 14/15 must-have truths verified in code; truth 15 requires live execution.

---

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `DeckFlow.Core/Content/IContentVideoStore.cs` | `UpdateTranscriptStatusAsync` + `GetVideoByYoutubeIdAsync` on interface | VERIFIED | Both members present |
| `DeckFlow.Core/Content/ContentVideoStore.cs` | UPDATE SQL + resume lookup impl | VERIFIED | `UPDATE content_videos` SQL; `GetVideoByYoutubeIdAsync` SELECT impl |
| `DeckFlow.Core/Content/SlugifySourceName.cs` | Pure tested slug helper, empty/non-ASCII fallback | VERIFIED | `"source"` fallback; 77-line test file covers all fallback cases |
| `DeckFlow.CLI/CommandRunners.cs` | `RunContentSourceAddAsync` + `RunHarvestAsync` (single persistence owner) | VERIFIED | Both methods present; all must-have wiring confirmed |
| `DeckFlow.CLI/Program.cs` | `content-source-add` + `harvest` commands registered | VERIFIED | Both registered |
| `DeckFlow.Core/Integration/ITranscriptSource.cs` | Non-null result, `TranscriptOutcome` enum, `knownDuration` + `monthKey` params, `SecondsBilled`/`CostUsd` | VERIFIED | All present; result is non-nullable |
| `DeckFlow.Core/Integration/YouTubeTranscriptFetcher.cs` | `TryGetByLanguage`-only; auto-gen accepted; empty classified; `Func<>` seam; no store deps | VERIFIED | All acceptance criteria met |
| `DeckFlow.Core/Integration/TranscriptProviderFactory.cs` | Real env toggle; unsupported value throws with listed values | VERIFIED | `DECKFLOW_YOUTUBE_TRANSCRIPT_PROVIDER`; `NotSupportedException` with "Supported: direct" |
| `DeckFlow.Core/Integration/IYouTubeAudioSource.cs` | `AudioDownloadResult` with `IDisposable`; `DurationSeconds` documented best-effort | VERIFIED | Record present, `IDisposable` present, best-effort doc present |
| `DeckFlow.Core/Integration/YouTubeAudioSource.cs` | `GetAudioOnlyStreams`; download to temp; `GetTempPath`; `Func<>` seam; no store deps; CR-02 real duration | VERIFIED | All present; `GetBestEffortDurationSecondsAsync` calls metadata, not hardcoded 0 |
| `DeckFlow.Core/Integration/WhisperTranscriptionService.cs` | Cap-READ gate; authoritative duration; monthKey echo; Polly wrapper; no persistence; `HttpClientPipelineTransport` | VERIFIED | All acceptance criteria met |
| `DeckFlow.Core/Integration/WhisperResiliencePipeline.cs` | Concrete 12-min Polly timeout + retry; `ClientResultException` in predicate (WR-01) | VERIFIED | `FromMinutes(12)`; `ClientResultException` in `ShouldHandle` |
| `DeckFlow.Core/Integration/FfmpegAudioChunker.cs` | `IsAvailableAsync`; `segment_time`; `BuildSegmentArguments` | VERIFIED | All present |
| `DeckFlow.Core/Integration/YouTubeTranscriptSource.cs` | Pure `ITranscriptSource`; captions-first; forwards `knownDuration`/`monthKey`; `SkippedOverCap` distinct | VERIFIED | All present |
| `DeckFlow.Core/Integration/YouTubeChannelVideoLister.cs` | `CollectAsync(limit)` — no `ToListAsync`; `PublishedUtc` from metadata (WR-02) | VERIFIED | `CollectAsync` present; `ToListAsync` = 0; `GetPublishedUtcAsync` populates date |
| `DeckFlow.Core.Tests/ContentVideoStoreTests.cs` | 6 behaviors: status transitions, guard, no-op, resume lookup | VERIFIED | 334 lines; 6 behaviors documented in 20-01 SUMMARY |
| `DeckFlow.Core.Tests/SlugifySourceNameTests.cs` | Slug normalization + empty/non-ASCII fallback | VERIFIED | 77 lines; 5 tests |
| `DeckFlow.Core.Tests/YouTubeTranscriptFetcherTests.cs` | 4 behaviors: captions, no-captions, auto-gen, empty-body | VERIFIED | 69 lines; 4 tests |
| `DeckFlow.Core.Tests/TranscriptProviderFactoryTests.cs` | null/direct resolves; unsupported throws with message | VERIFIED | 45 lines; 3 tests |
| `DeckFlow.Core.Tests/WhisperTranscriptionServiceTests.cs` | Cap-skip; duration-authority; both-unknown→Failed; success; Polly retry; chunk concat; ffmpeg absent | VERIFIED | 366 lines; 7+ tests |
| `DeckFlow.Core.Tests/FfmpegAudioChunkerTests.cs` | Arg-builder + availability-false-without-ffmpeg | VERIFIED | 54 lines; 2 tests |
| `DeckFlow.Core.Tests/YouTubeTranscriptSourceTests.cs` | Captions-first free path; Whisper fallback; SkippedOverCap distinct; knownDuration/monthKey forwarding | VERIFIED | 188 lines; 4+ tests |
| `DeckFlow.Core.Tests/CommandRunnerHarvestTests.cs` | Harvest orchestration seam coverage | VERIFIED | 412 lines |

---

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `CommandRunners.cs` | `SlugifySourceName.cs` | `SlugifySourceName.Slugify(name)` before `InsertSourceAsync` | VERIFIED | Both `SlugifySourceName` (1) and `InsertSourceAsync` (1) in `CommandRunners.cs` |
| `CommandRunners.cs` | `ContentSourceStore.cs` | `InsertSourceAsync(slug, name, type, url)` | VERIFIED | `InsertSourceAsync` present in `CommandRunners.cs` |
| `ContentVideoStore.cs` | `content_videos.transcript_status` | `UPDATE content_videos SET transcript_status` | VERIFIED | `UPDATE content_videos` SQL present |
| `YouTubeTranscriptFetcher.cs` | YoutubeExplode manifest | `GetManifestAsync` + `TryGetByLanguage` + empty-body classification | VERIFIED | `TryGetByLanguage` (2); `IsNullOrWhiteSpace` classification |
| `TranscriptProviderFactory.cs` | `DECKFLOW_YOUTUBE_TRANSCRIPT_PROVIDER` | env read → factory dispatch | VERIFIED | `DECKFLOW_YOUTUBE_TRANSCRIPT_PROVIDER` present; dispatches "direct" or throws |
| `YouTubeAudioSource.cs` | YoutubeExplode Streams | `GetManifestAsync` → `GetAudioOnlyStreams` → `DownloadAsync` | VERIFIED | `GetAudioOnlyStreams` (1); `DownloadAsync` (1); `GetTempPath` (1) |
| `WhisperTranscriptionService.cs` | `WhisperSpendLedger.cs` | `WouldExceedCapAsync` READ gate before call | VERIFIED | `WouldExceedCapAsync` (1); `RecordCallAsync` = 0 (verb owns the write) |
| `WhisperTranscriptionService.cs` | `WhisperResiliencePipeline.cs` | `ExecuteAsync` wraps every transcription delegate | VERIFIED | `ExecuteAsync` (1) in service; test asserts invocation through pipeline |
| `WhisperTranscriptionService.cs` | `IFfmpegAudioChunker` | `SizeBytes > 24MB → ChunkAsync → per-chunk transcribe → concat` | VERIFIED | `ChunkAsync` present; chunk path tested in `WhisperTranscriptionServiceTests` |
| `YouTubeTranscriptSource.cs` | `IYouTubeTranscriptFetcher + IYouTubeAudioSource + IWhisperTranscriptionService` | captions-first → audio download → Whisper fallback; forwards `knownDuration` + `monthKey` | VERIFIED | `TranscribeAsync(audio, knownDuration, monthKey, ct)` at line 53 confirmed |
| `CommandRunners.cs` | `ContentVideoStore + WhisperSpendLedger` | `InsertVideoAsync` / `InsertTranscriptAsync` / `UpdateTranscriptStatusAsync` + `RecordCallAsync` on Whisper-success only | VERIFIED | `RecordCallAsync` (1) on Whisper branch; ledger write at line 640 before transcript at line 641 (CR-01 fix) |
| `CommandRunners.cs` | `GetVideoByYoutubeIdAsync` (resume) | lookup existing video → resume pending/failed instead of skip | VERIFIED | `GetVideoByYoutubeIdAsync` (1) in `CommandRunners.cs` |
| `CommandRunners.cs` | `IFfmpegAudioChunker.IsAvailableAsync` | ffmpeg presence warning at harvest start | VERIFIED | `IsAvailableAsync` (1); absent path logs warning and continues |

---

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|---------------|--------|--------------------|--------|
| `WhisperTranscriptionService.cs` | `projectionSeconds` | `Math.Max(knownDuration?.TotalSeconds ?? 0d, audio.DurationSeconds)` where `knownDuration` is `PlaylistVideo.Duration` from lister | Yes — lister-supplied authoritative duration flows from `YouTubeChannelVideoLister` → `RunHarvestAsync` → `FetchTranscriptAsync` → `TranscribeAsync` | VERIFIED |
| `CommandRunners.cs` | `monthKey` | `utcNow().UtcDateTime.ToString("yyyy-MM")` — single derivation at line 572 | Yes — one value per video, passed into `FetchTranscriptAsync` and reused at `RecordCallAsync` (line 640); `ToString("yyyy-MM")` count = 1 confirms no re-derivation at ledger site | VERIFIED |
| `ContentVideoStore.cs` | `transcript_status` | `UPDATE content_videos SET transcript_status = @status WHERE id = @videoId` | Yes — real SQL UPDATE with allowlist guard | VERIFIED |
| `YouTubeAudioSource.cs` | `DurationSeconds` | `GetBestEffortDurationSecondsAsync` → `Videos.GetAsync` → video metadata duration | Yes — CR-02 fix: metadata lookup, not hardcoded 0; `DurationSeconds` is still documented best-effort (may be 0 if metadata unavailable) and is not the authoritative source for cap projection | VERIFIED |

---

### Behavioral Spot-Checks

Step 7b: SKIPPED for the live-HTTP behavioral checks (require internet + OPENAI_API_KEY + YouTube access). All unit-testable behaviors are covered by the 1,545 lines of test code across 8 new test classes.

The static spot-check — `harvest --help` lists `--db` and `--limit` — is confirmed by 20-04-SUMMARY verification results.

---

### Requirements Coverage

| Requirement | Source Plans | Description | Status | Evidence |
|-------------|-------------|-------------|--------|----------|
| KB-03 | 20-01, 20-02, 20-04 | YouTube auto-caption fetch via YoutubeExplode 6.6.0; proven against 5 real channels | CODE VERIFIED / LIVE UAT PENDING | `YouTubeTranscriptFetcher` wired with `TryGetByLanguage`; auto-gen accepted; `TranscriptProviderFactory` real; `harvest` verb wired; Google.Apis.YouTube = 0 references anywhere in Core/CLI. Live channel proof pending human run |
| KB-04 | 20-01, 20-03, 20-04 | Whisper fallback + transcript/ledger persistence | VERIFIED | `WhisperTranscriptionService` with `HttpClientPipelineTransport`; `InsertTranscriptAsync`/`RecordCallAsync` in `RunHarvestAsync`; `source` discriminator wired via `TranscriptSource.Whisper` |
| KB-05 | 20-01, 20-03, 20-04 | Plain local spend-log cap check; `skipped_over_cap` | VERIFIED | `WouldExceedCapAsync` READ gate; `Math.Max(knownDuration, audio.DurationSeconds)` projection; both-unknown → `Failed` (never transcribed free); `UpdateTranscriptStatusAsync(videoId, TranscriptStatus.SkippedOverCap, ct)` on over-cap; no TOCTOU/advisory-lock machinery |

Note: REQUIREMENTS.md shows KB-03 as `[ ]` (unchecked) because the requirement specifies "proven against 5 real channels" — the code is built but the live proof has not been run. KB-04 and KB-05 are `[x]` in REQUIREMENTS.md (the schema + runtime portions both complete).

---

### Anti-Patterns Found

| File | Pattern | Severity | Impact |
|------|---------|----------|--------|
| `FfmpegAudioChunker.cs` | `BuildSegmentArguments` uses string-concatenation quoting of paths — brittle against paths containing double-quotes (WR-03 from review) | INFO | Low — current inputs are GUID-based temp paths; `ProcessStartInfo.ArgumentList` would be safer. Review finding acknowledged; not a blocker for the phase goal |
| `IYouTubeTranscriptFetcher.cs`, `YouTubeAudioSource.cs` | Duplicated `AsyncRetryPolicy` definition in two fetchers (IN-03 from review) | INFO | Low — does not affect correctness; extraction to shared factory would improve maintainability |
| `YouTubeCaptionResult.CaptionTrackKind` | Computed but the consumer re-derives it from `IsAutoGenerated` rather than reading the field (IN-01 from review) | INFO | Harmless — `caption_track_kind` logging still works correctly via `GetCaptionTrackKind` helper |
| `WhisperTranscriptionResult.MonthKey` | Echoed back but verb uses its own local `monthKey` for `RecordCallAsync` (IN-02 from review) | INFO | Harmless — both values are identical by construction; no correctness issue |

No `TBD`, `FIXME`, or `XXX` markers found in any of the phase-modified files.

No stub patterns (empty handlers, placeholder returns, hardcoded empty data for user-visible output) found.

---

### Human Verification Required

#### 1. 5-Channel Local Caption-Coverage UAT (SC1 + SC2 — Phase 20 Blocking Gate)

**Test:** Run the following commands from the repo root with internet access and `OPENAI_API_KEY` set in the local environment:

Seed the 5 UAT channels (idempotent, safe to re-run):
```
dotnet run --project DeckFlow.CLI -- content-source-add --name "MTGGoldfish" --url https://www.youtube.com/@MTGGoldfish --type youtube_channel --db artifacts/uat-content-kb.db
dotnet run --project DeckFlow.CLI -- content-source-add --name "The Command Zone" --url https://www.youtube.com/@TheCommandZone --type youtube_channel --db artifacts/uat-content-kb.db
dotnet run --project DeckFlow.CLI -- content-source-add --name "EDHRECast" --url https://www.youtube.com/@EDHRECast --type youtube_channel --db artifacts/uat-content-kb.db
dotnet run --project DeckFlow.CLI -- content-source-add --name "Tolarian Community College" --url https://www.youtube.com/@TolarianCommunityCollege --type youtube_channel --db artifacts/uat-content-kb.db
dotnet run --project DeckFlow.CLI -- content-source-add --name "Playing With Power" --url https://www.youtube.com/@PlayingwithPowerMTG --type youtube_channel --db artifacts/uat-content-kb.db
```

Run the bounded harvest (2 videos per channel = ~10 videos):
```
dotnet run --project DeckFlow.CLI -- harvest --db artifacts/uat-content-kb.db --limit 2
```

**Expected:**
- Captions fetched for the majority of the 5 channels via YoutubeExplode 6.6.0 with no Google.Apis 403 errors (SC1)
- Each fetch log line includes both `transcript_source` (captions|whisper) AND `caption_track_kind` (manual|auto_generated) (SC2/MEDIUM-e)
- Per-channel `whisper_fallback_ratio` is logged for each of the 5 channels AND an aggregate run-wide ratio is logged; aggregate < 0.25 (SC2/P2)
- If ffmpeg is absent: one Warning line is emitted and the run does NOT abort (P7); large audio files may be marked `failed`
- (Optional) inspect `artifacts/uat-content-kb.db`: `content_transcripts` rows have `source='captions'` for captioned videos; `whisper_spend_ledger` has a row ONLY for any video that actually fell through to Whisper success, and any such row's `month_key` matches the run month

**Why human:** Requires live internet access, a valid `OPENAI_API_KEY`, and YouTube availability. Cannot be verified by static analysis or grep. The entire code path is wired and unit-tested; only the live third-party integration proof is outstanding.

---

### Gaps Summary

No code gaps. All 14 code must-haves are VERIFIED. The sole outstanding item is the live 5-channel UAT that proves SC1 (third-party caption fetch works against real YouTube channels) and SC2 (whisper_fallback_ratio < 25%). This was explicitly documented as PENDING-HUMAN-RUN in 20-04-SUMMARY.md and is the reason the REQUIREMENTS.md `KB-03` checkbox remains unchecked — it requires "proven against 5 real cEDH/Commander channels run from the local environment."

All critical review findings (CR-01, CR-02) and high-value warnings (WR-01, WR-02, WR-05) from 20-REVIEW.md were confirmed fixed in the codebase:
- CR-01: `RecordCallAsync` at line 640, `InsertTranscriptAsync` at line 641 — ledger write first
- CR-02: `GetBestEffortDurationSecondsAsync` fetches video metadata (not hardcoded 0)
- WR-01: `ClientResultException` added to `ShouldHandle` predicate in `WhisperResiliencePipeline.cs`
- WR-02: `GetPublishedUtcAsync` and `MapVideo(upload, publishedUtc)` populate `PublishedUtc`
- WR-05: `MarkFailedIfPossibleAsync` gated by `!statusPersisted` at line 580

Remaining review findings (WR-03, WR-04, IN-01, IN-02, IN-03, IN-04) are INFO/WARNING level and do not block the phase goal.

Correction note: corrected 2026-06-04 (HSK-03, D-10) — UAT PASSED 2026-05-27 per `.planning/milestones/v1.4-MILESTONE-AUDIT.md` (5-channel harvest, 10/10 captions).

---

_Verified: 2026-05-26T20:29:00-06:00_
_Verifier: Claude (gsd-verifier)_
