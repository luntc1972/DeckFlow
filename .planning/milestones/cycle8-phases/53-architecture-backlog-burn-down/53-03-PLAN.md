---
phase: 53-architecture-backlog-burn-down
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - DeckFlow.Core/Analysis/DeckStatClassifier.cs
  - DeckFlow.Web/Services/DeckComparisonService.cs
  - DeckFlow.Core.Tests/DeckStatClassifierTests.cs
autonomous: true
requirements: [ARCH-02]

must_haves:
  truths:
    - "The deck-comparison stat tallies (ramp/draw/interaction/wipes/recursion/closing-power counts) are computed identically before and after the move"
    - "The pure deck-stat classifiers live in DeckFlow.Core and are unit-tested there (previously untestable except through the Web assembly)"
    - "DeckComparisonService delegates to the Core classifiers instead of owning private static copies"
  artifacts:
    - path: "DeckFlow.Core/Analysis/DeckStatClassifier.cs"
      provides: "Pure static deck-stat classifiers (IsRampCard, IsDrawCard, IsInteractionCard, IsBoardWipeCard, IsRecursionCard, IsClosingPowerCard) + ParseManaToken"
      contains: "public static"
      min_lines: 50
    - path: "DeckFlow.Core.Tests/DeckStatClassifierTests.cs"
      provides: "xUnit coverage for each classifier — true and false cases"
      contains: "DeckStatClassifier"
  key_links:
    - from: "DeckFlow.Web/Services/DeckComparisonService.cs"
      to: "DeckFlow.Core.Analysis.DeckStatClassifier"
      via: "static calls in the curve/stat tally loop"
      pattern: "DeckStatClassifier\\.(IsRampCard|IsDrawCard|IsInteractionCard|IsBoardWipeCard|IsRecursionCard|IsClosingPowerCard|ParseManaToken)"
---

<objective>
Relocate the pure deck-stat classifiers (`IsRampCard`, `IsDrawCard`, `IsInteractionCard`, `IsBoardWipeCard`, `IsRecursionCard`, `IsClosingPowerCard`) and `ParseManaToken` out of `DeckComparisonService` into `DeckFlow.Core`, and add Core unit tests. These are pure CPU domain logic that CLAUDE.md says belongs in Core, and they are currently untestable except through the Web assembly. Closes Phase 39 audit Finding E (classifiers only — distill helpers are already in Core, out of scope).

Purpose: Pure domain logic in the right layer, now directly testable; the prompt-text packet behavior is unchanged because the classifier expressions are moved verbatim.
Output: One Core static class, a Core test file, and a `DeckComparisonService` that delegates to it.

Behavior-preservation proof: the classifier boolean expressions are moved character-for-character; the new Core unit tests lock the exact true/false matching behavior; the solution build + existing Web comparison tests confirm the tally loop is unchanged.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/53-architecture-backlog-burn-down/53-CONTEXT.md
@./CLAUDE.md

<interfaces>
<!-- The six classifiers + ParseManaToken to relocate, with their exact current signatures (private static in DeckComparisonService today). They become public static on the new Core class so the Web service and Core tests can both call them. -->
From DeckFlow.Web/Services/DeckComparisonService.cs:
  private static bool IsRampCard(string typeLine, string oracleText)
  private static bool IsDrawCard(string oracleText)
  private static bool IsInteractionCard(string typeLine, string oracleText)
  private static bool IsBoardWipeCard(string oracleText)
  private static bool IsRecursionCard(string oracleText)
  private static bool IsClosingPowerCard(string typeLine, string oracleText)
  private static int ParseManaToken(string token)
Call sites are in the curve/stat tally loop (around the `if (IsRampCard(...))` / `if (IsDrawCard(...))` block) — those call sites repoint to DeckStatClassifier.Xxx.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create DeckFlow.Core.Analysis.DeckStatClassifier + Core tests (RED then GREEN)</name>
  <read_first>
    DeckFlow.Web/Services/DeckComparisonService.cs (the six classifier method bodies + ParseManaToken — the source expressions to move verbatim),
    DeckFlow.Core.Tests/ReportingTests.cs (existing Core test style + assertion conventions),
    DeckFlow.Core.Tests/DeckFlow.Core.Tests.csproj (xUnit, Using Include Xunit)
  </read_first>
  <files>DeckFlow.Core/Analysis/DeckStatClassifier.cs, DeckFlow.Core.Tests/DeckStatClassifierTests.cs</files>
  <behavior>
    - IsRampCard: true for type line containing "Land"; true for oracle "add one mana" / "add two mana" / "search your library for a basic land" / "Treasure token" / "create a Treasure"; true when oracle has "search your library for up to" AND "land"; false for a vanilla creature with no ramp text.
    - IsDrawCard: true for "draw a card" / "draw two cards" / "draw X cards" / "investigate" / "connive"; false for a removal spell.
    - IsInteractionCard: true for type "Instant", or oracle "destroy target" / "exile target" / "counter target" / "return target spell" / "fight target"; false for a plain ramp rock.
    - IsBoardWipeCard: true for "destroy all creatures" / "destroy all artifacts" / "destroy all enchantments" / "exile all"; true when "each creature" AND "gets -"; false for single-target removal.
    - IsRecursionCard: true for the graveyard-return phrases and "reanimate" / "from your graveyard to your hand"; false otherwise.
    - IsClosingPowerCard: true for "you win the game" / "extra turn" / "double strike" / "each opponent loses" / "Craterhoof"; true when "combat damage to a player" AND "draw"; false otherwise.
    - ParseManaToken: numeric token -> its int; "X" -> 0; hybrid (contains '/') -> 1; anything else -> 1.
  </behavior>
  <action>
    Create `DeckStatClassifier` (public static class, namespace DeckFlow.Core.Analysis) and move the six classifier methods plus ParseManaToken from DeckComparisonService into it as `public static` members, copying each boolean expression / switch verbatim (do not re-author the matching logic — these are the exact phrase lists the packet depends on; CLAUDE.md no-reflow applies to the literals). Write `DeckStatClassifierTests` covering the true and false cases enumerated in <behavior> for every classifier and ParseManaToken (use [Theory]/[InlineData] where it reduces duplication). Run the new tests — they must pass GREEN against the moved-verbatim logic.
  </action>
  <verify>Core builds; DeckStatClassifierTests pass</verify>
  <acceptance_criteria>
    - source: DeckStatClassifier.cs exists in DeckFlow.Core/Analysis with the six classifiers + ParseManaToken as public static, expressions identical to the originals
    - source: DeckStatClassifierTests.cs has at least one true-case and one false-case assertion per classifier plus ParseManaToken cases
    - test-command: `"/mnt/c/Program Files/dotnet/dotnet.exe" test DeckFlow.Core.Tests/DeckFlow.Core.Tests.csproj --filter "FullyQualifiedName~DeckStatClassifier"` — all pass, 0 failed
  </acceptance_criteria>
  <done>DeckStatClassifier lives in Core with passing unit tests covering each classifier's true/false behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Repoint DeckComparisonService to the Core classifiers; remove the private copies</name>
  <read_first>
    DeckFlow.Web/Services/DeckComparisonService.cs (the tally loop call sites + the private classifier definitions to delete),
    DeckFlow.Core/Analysis/DeckStatClassifier.cs (the new home, created in Task 1)
  </read_first>
  <files>DeckFlow.Web/Services/DeckComparisonService.cs</files>
  <action>
    Delete the six private static classifier methods and the private static ParseManaToken from DeckComparisonService. Repoint every call site in the curve/stat tally loop to `DeckStatClassifier.IsRampCard(...)`, `DeckStatClassifier.IsDrawCard(...)`, etc., and `DeckStatClassifier.ParseManaToken(...)`. Add `using DeckFlow.Core.Analysis;` (sorted per CLAUDE.md import order: System.* first, then third-party, then DeckFlow.* — place it in the DeckFlow.* group). Touch ONLY the lines that change (the call sites, the deletions, the one using) — do not reflow the rest of the file (changed-lines format gate). Confirm no other method in DeckComparisonService referenced these privately beyond the tally loop (grep before deleting). Leave all packet/prompt-building logic untouched.
  </action>
  <verify>Solution builds clean; Web comparison tests pass</verify>
  <acceptance_criteria>
    - source: `grep -c "private static bool IsRampCard\|private static int ParseManaToken" DeckFlow.Web/Services/DeckComparisonService.cs` returns 0 (private copies removed)
    - source: `grep -c "DeckStatClassifier\." DeckFlow.Web/Services/DeckComparisonService.cs` is >= 7 (all call sites repointed)
    - source: only the call-site lines, the deletions, and one using directive changed (no mass reflow)
    - test-command: `"/mnt/c/Program Files/dotnet/dotnet.exe" build DeckFlow.sln` exits 0, 0 new warnings
    - test-command: `"/mnt/c/Program Files/dotnet/dotnet.exe" test DeckFlow.Web.Tests/DeckFlow.Web.Tests.csproj --filter "FullyQualifiedName~Comparison"` — 0 failed
  </acceptance_criteria>
  <done>DeckComparisonService delegates to DeckStatClassifier; no private classifier copies remain; build + comparison tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Pure-function relocation between assemblies. No new input, no I/O, no dependency, no SQL. The classifier inputs (typeLine/oracleText) already flow through DeckComparisonService unchanged. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-53-03 | Tampering | Classifier phrase lists preserved across the move (they drive packet stat tallies) | mitigate | Expressions moved verbatim; new Core unit tests lock true/false behavior; Web comparison tests confirm the tally loop is unchanged. |

No new threats. No package installs.
</threat_model>

<verification>
- `dotnet build DeckFlow.sln` clean, 0 new warnings.
- `DeckStatClassifierTests` pass (each classifier true/false + ParseManaToken).
- Web comparison tests pass (tally loop behavior unchanged).
</verification>

<success_criteria>
The deck-stat classifiers live in DeckFlow.Core.Analysis with dedicated Core unit tests, DeckComparisonService delegates to them with no private copies, and behavior is provably unchanged (build + tests green).
</success_criteria>

<output>
Create `.planning/phases/53-architecture-backlog-burn-down/53-03-SUMMARY.md` when done.
</output>
