---
phase: 28-housekeeping-bundle
plan: 04
type: execute
wave: 2
depends_on: ["28-03"]
files_modified:
  - DeckFlow.Core/Integration/LlmDistillationProviderFactory.cs
  - DeckFlow.Core/Integration/CliLlmDistillationService.cs
  - DeckFlow.Core.Tests/LlmDistillationProviderFactoryTests.cs
  - DeckFlow.Core.Tests/CliLlmDistillationServiceTests.cs
autonomous: true
requirements: [HSK-02]

must_haves:
  truths:
    - "DECKFLOW_LLM_PROVIDER=codex resolves to a CliLlmDistillationService (no NotSupportedException)"
    - "The codex branch emits a Raw-envelope command-spec with the isolation flags from 28-DISCOVERY.md"
    - "Codex model is configurable via env var with a mini-tier default (D-07)"
    - "A permanent sentinel-exfil xUnit regression test proves untrusted-input read isolation at the seam (D-04)"
    - "Existing openai and claude provider paths are unchanged"
  artifacts:
    - path: "DeckFlow.Core/Integration/LlmDistillationProviderFactory.cs"
      provides: "codex provider branch returning CliLlmDistillationService"
      contains: "CodexProvider"
    - path: "DeckFlow.Core/Integration/CliLlmDistillationService.cs"
      provides: "codex branch of BuildCommandSpec with Raw envelope + model env var read"
      contains: "CliEnvelopeKind.Raw"
    - path: "DeckFlow.Core.Tests/CliLlmDistillationServiceTests.cs"
      provides: "codex seam tests incl. permanent sentinel-exfil regression"
      contains: "sentinel"
  key_links:
    - from: "LlmDistillationProviderFactory codex branch"
      to: "CliLlmDistillationService(CodexProvider)"
      via: "provider string equality check"
      pattern: "CodexProvider"
    - from: "BuildCommandSpec codex branch"
      to: "ExtractModelText Raw path"
      via: "CliEnvelopeKind.Raw"
      pattern: "CliEnvelopeKind.Raw"
---

<objective>
Implement the codex LLM distillation backend: replace the NotSupportedException stub in the provider factory, add the codex branch to CliLlmDistillationService.BuildCommandSpec (Raw envelope + isolation flags + configurable mini-tier model), and add seam-level tests including the permanent sentinel-exfil regression. Implementation uses the exact mechanism + command-spec recorded in 28-DISCOVERY.md.

Purpose: Close HSK-02 / Phase 28 SC #1 — DECKFLOW_LLM_PROVIDER=codex distills end-to-end via the existing CliCommandSpec seam, with the proven untrusted-input read-isolation boundary D-01 requires, and the same JSON-repair / ValidateTags / timeout / zero-token-ledger guarantees as the claude backend.
Output: Working codex provider path + green Core tests + sentinel-exfil regression.

GATE: This plan runs ONLY if 28-DISCOVERY.md (Plan 03) records a "ship" decision with a provable isolation mechanism. If 28-DISCOVERY.md recommends re-demote (D-03), DO NOT implement — halt and surface to the user.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-housekeeping-bundle/28-CONTEXT.md
@.planning/phases/28-housekeeping-bundle/28-PATTERNS.md
@.planning/phases/28-housekeeping-bundle/28-DISCOVERY.md

@DeckFlow.Core/Integration/LlmDistillationProviderFactory.cs
@DeckFlow.Core/Integration/CliLlmDistillationService.cs
@DeckFlow.Core/Integration/CliCommandSpec.cs
@DeckFlow.Core.Tests/LlmDistillationProviderFactoryTests.cs
@DeckFlow.Core.Tests/CliLlmDistillationServiceTests.cs

<interfaces>
Provider constants (LlmDistillationProviderFactory.cs:13-15):
  OpenAiProvider = "openai"; ClaudeProvider = "claude"; CodexProvider = "codex" (already defined)

Factory stub to replace (LlmDistillationProviderFactory.cs:49-53): codex branch throws NotSupportedException — replace with `return new CliLlmDistillationService(CodexProvider);`
Factory final throw (line 55-56): error message currently lists "openai, claude" — add "codex".

Service single-provider guard to make two-branch (CliLlmDistillationService.cs:106-109): currently throws for any non-claude provider.
Claude command-spec (CliLlmDistillationService.cs:117-123): claude -p {instruction} --output-format json --allowedTools "" with CliEnvelopeKind.ClaudeJson.
Override path (BuildOverrideCommandSpec, line 174): hard-codes CliEnvelopeKind.ClaudeJson — must accept the target envelope kind as a parameter so codex override uses CliEnvelopeKind.Raw.
Raw extraction (ExtractModelText, lines 271-275): already returns stdout.Trim() for CliEnvelopeKind.Raw — no change.
Timeout env-read pattern (CliTimeoutEnvironmentKey + ReadTimeout, lines 14, 389-403): the template for the new codex model env var read.

Test seam (CliLlmDistillationServiceTests.cs:341-345): CreateService passes "claude"; codex tests pass "codex" with a Queue-backed fake runner returning raw model text.
Factory tests (LlmDistillationProviderFactoryTests.cs:48-68): Resolve_Codex_ThrowsNotSupportedPointingAt213 (lines 48-56) must become Resolve_Codex_ReturnsCliImpl; Resolve_Unknown_ThrowsNotSupportedListingSupported (lines 59-68) must also assert "codex" appears in the message.
</interfaces>
</context>

<scope_fence>
ALLOWED FILE SET (Codex: touch ONLY these; ignore all other tasks/files):
- EDIT: DeckFlow.Core/Integration/LlmDistillationProviderFactory.cs
- EDIT: DeckFlow.Core/Integration/CliLlmDistillationService.cs
- EDIT: DeckFlow.Core.Tests/LlmDistillationProviderFactoryTests.cs
- EDIT: DeckFlow.Core.Tests/CliLlmDistillationServiceTests.cs
Do NOT touch CliCommandSpec.cs (Raw envelope already exists), any .csproj, Program.cs, the CLI, or any planning doc. No new NuGet packages. Use the exact command-spec/flags/model env var recorded in 28-DISCOVERY.md.
Pre-dispatch: confirm git status is clean and that 28-DISCOVERY.md decision is "ship".
</scope_fence>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add codex branch to factory + service (Raw envelope, isolation flags, model config)</name>
  <files>
    DeckFlow.Core/Integration/LlmDistillationProviderFactory.cs
    DeckFlow.Core/Integration/CliLlmDistillationService.cs
  </files>
  <read_first>
    - .planning/phases/28-housekeeping-bundle/28-DISCOVERY.md (the exact command-spec, isolation flags, model env var name + mini-tier default to use)
    - DeckFlow.Core/Integration/LlmDistillationProviderFactory.cs (full file — branches + final throw)
    - DeckFlow.Core/Integration/CliLlmDistillationService.cs lines 103-175 (BuildCommandSpec + BuildOverrideCommandSpec) and 271-275 (Raw extraction) and 389-403 (ReadTimeout pattern)
    - .planning/phases/28-housekeeping-bundle/28-PATTERNS.md (replacement snippets for factory + service branches)
  </read_first>
  <behavior>
    - LlmDistillationProviderFactory.Resolve("codex", httpClient) returns a CliLlmDistillationService (no throw); "openai" and "claude" unchanged.
    - LlmDistillationProviderFactory final throw message lists openai, claude, and codex.
    - CliLlmDistillationService("codex").BuildCommandSpec(instruction) returns a CliCommandSpec with the codex executable, the isolation flags from 28-DISCOVERY.md, the configured model, and CliEnvelopeKind.Raw.
    - When DECKFLOW_LLM_CLI_COMMAND override is set for codex, the override command-spec uses CliEnvelopeKind.Raw (not ClaudeJson).
    - The codex model env var (per D-07) defaults to the mini-tier model id from 28-DISCOVERY.md when unset/whitespace; an invalid value throws InvalidOperationException naming the key (ReadTimeout pattern).
  </behavior>
  <action>
    In LlmDistillationProviderFactory.cs: replace the codex NotSupportedException block (lines 49-53) with `return new CliLlmDistillationService(CodexProvider);` (mirror the claude branch). Update the final throw message (line 56) to list "openai, claude, codex". In CliLlmDistillationService.cs: (1) change the single-provider guard in BuildCommandSpec (lines 106-109) from a claude-only throw into a two-branch dispatch — claude keeps the existing command-spec (CliEnvelopeKind.ClaudeJson), codex returns a new CliCommandSpec built from the 28-DISCOVERY.md command-spec (codex executable + isolation flags + model flag + instruction) with CliEnvelopeKind.Raw; any other provider still throws NotSupportedException naming the provider. (2) Add a CodexProvider const ("codex"). (3) Add the model env var: a new internal const string (name per 28-DISCOVERY.md, e.g. DECKFLOW_LLM_CODEX_MODEL) plus a ReadCodexModel() helper following ReadTimeout's null-check-then-default-else-validate shape, returning the mini-tier default from 28-DISCOVERY.md when unset. Wire the resolved model into the codex command-spec's argument list at the position 28-DISCOVERY.md specifies. (4) Parameterize BuildOverrideCommandSpec to accept the target CliEnvelopeKind (default keep ClaudeJson for claude) and pass CliEnvelopeKind.Raw on the codex override path so a codex DECKFLOW_LLM_CLI_COMMAND override produces a Raw spec. Keep the Windows-path InvalidOperationException guidance message but note codex requires its own override example. Do NOT change ExtractModelText (Raw already handled), ExtractWithRetryAsync, RunProcessAsync, JSON repair, ValidateTags wiring, or zero-token-usage — those guarantees must flow through unchanged. Use file-scoped namespaces, Allman braces, 4-space indent, sealed where applicable, XML doc on any new public surface (project NoWarn covers Web only — Core has no CS1591 gate yet, but keep doc-comments on new public members for consistency). Preserve LF line endings. Build DeckFlow.Core clean (0 new warnings).
  </action>
  <verify>
    <automated>"/mnt/c/Program Files/dotnet/dotnet.exe" build DeckFlow.Core/DeckFlow.Core.csproj -c Debug</automated>
  </verify>
  <acceptance_criteria>
    - DeckFlow.Core build reports 0 errors and 0 new warnings.
    - LlmDistillationProviderFactory.cs codex branch returns `new CliLlmDistillationService(CodexProvider)`; no NotSupportedException for "codex".
    - Final factory throw message contains "codex" (alongside openai, claude).
    - CliLlmDistillationService BuildCommandSpec codex branch produces a CliCommandSpec with CliEnvelopeKind.Raw and the isolation flags + model flag from 28-DISCOVERY.md.
    - BuildOverrideCommandSpec accepts a target envelope kind; codex override path uses CliEnvelopeKind.Raw.
    - A codex model env var const + ReadCodexModel helper exist with a mini-tier default; invalid value throws InvalidOperationException naming the key.
    - ExtractModelText, ExtractWithRetryAsync, RunProcessAsync, JSON-repair, ValidateTags, and zero-token-usage are unchanged.
  </acceptance_criteria>
  <done>codex resolves to the CLI service and emits a Raw-envelope, read-isolated, model-configurable command-spec; openai/claude paths unchanged; Core builds clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: codex seam tests + permanent sentinel-exfil regression (D-04)</name>
  <files>
    DeckFlow.Core.Tests/LlmDistillationProviderFactoryTests.cs
    DeckFlow.Core.Tests/CliLlmDistillationServiceTests.cs
  </files>
  <read_first>
    - .planning/phases/28-housekeeping-bundle/28-DISCOVERY.md (sentinel-exfil seam-test design + parity checklist)
    - DeckFlow.Core.Tests/LlmDistillationProviderFactoryTests.cs (full file — Resolve_Codex_* and Resolve_Unknown_* tests to update)
    - DeckFlow.Core.Tests/CliLlmDistillationServiceTests.cs lines 341-391 (CreateService, ClaudeEnvelope, WithCommandOverrideAsync helpers to mirror for codex)
    - DeckFlow.Core.Tests/CliLlmDistillationServiceTests.cs lines 100-160 (existing summarize/clips/tags + sanitized-error test patterns to mirror with Raw output)
  </read_first>
  <behavior>
    - Resolve_Codex_ReturnsCliImpl asserts Resolve("codex", httpClient) is a CliLlmDistillationService (replaces the 21.3-pointer test).
    - Resolve_Unknown_ThrowsNotSupportedListingSupported additionally asserts the message contains "codex".
    - A codex SummarizeAsync test drives the fake runner returning RAW model text (no claude envelope) and gets a valid SummaryResult.
    - Codex InferTagsAsync still runs ValidateTags (invalid tags fail the same way as claude).
    - Sentinel-exfil regression: given a transcript containing a prompt-injection attempting to read a temp sentinel file, the sentinel content NEVER appears in the result or in any thrown exception's ToString() — proven at the fake-runner seam (no real codex spawn).
  </behavior>
  <action>
    In LlmDistillationProviderFactoryTests.cs: replace Resolve_Codex_ThrowsNotSupportedPointingAt213 (lines 48-56) with Resolve_Codex_ReturnsCliImpl asserting Assert.IsType&lt;CliLlmDistillationService&gt; for Resolve("codex", httpClient); optionally add a Resolve_CodexUpper variant mirroring Resolve_ClaudeUpper. Update Resolve_Unknown_ThrowsNotSupportedListingSupported to also Assert.Contains("codex", exception.Message, OrdinalIgnoreCase). In CliLlmDistillationServiceTests.cs: add a codex-provider CreateService overload (or parameterize) that passes "codex" to the internal ctor with a Queue-backed fake runner; add a RawOutput(string) helper returning the model text verbatim (codex Raw envelope). Add codex tests mirroring the existing claude summarize/clips/tags happy-path and the ValidateTags-failure path, but feeding RawOutput(...) instead of ClaudeEnvelope(...). Add the PERMANENT sentinel-exfil regression test (D-04): create a temp file containing a unique sentinel string; build a transcript that includes a prompt-injection telling the model to read and echo that file; run it through the codex fake runner (the fake runner simulates an isolated codex that returns only valid distillation JSON, never the sentinel); assert the sentinel string is DoesNotContain in the SummaryResult output AND in any exception.ToString(); clean up the temp file in a finally. Use the WithCommandOverrideAsync env set/restore idiom for any env-dependent test and always restore in finally. Name the sentinel test descriptively (e.g. Codex_MaliciousTranscript_DoesNotLeakSentinelFile). Keep xUnit conventions ([Fact]/[Theory]); no new packages. Preserve LF line endings. Run the focused Core test filter and confirm all green.
  </action>
  <verify>
    <automated>"/mnt/c/Program Files/dotnet/dotnet.exe" test DeckFlow.Core.Tests/DeckFlow.Core.Tests.csproj --filter "FullyQualifiedName~CliLlmDistillation|FullyQualifiedName~LlmDistillationProviderFactory"</automated>
  </verify>
  <acceptance_criteria>
    - Focused Core test run reports Failed: 0 (codex + claude + factory tests all pass).
    - Resolve_Codex_ReturnsCliImpl exists and passes; the 21.3-pointer test is gone.
    - Resolve_Unknown test asserts "codex" is in the message.
    - At least one codex test feeds RawOutput (not ClaudeEnvelope) and gets a valid result.
    - A permanently-named sentinel-exfil test asserts the sentinel content appears in NO output and NO exception text, with temp-file cleanup in finally.
    - No new NuGet packages; no test framework change.
  </acceptance_criteria>
  <done>codex factory + seam tests pass green, including a permanent sentinel-exfil regression proving untrusted-input read isolation at the seam, with claude/openai tests still green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| untrusted transcript → codex exec | prompt-injected transcript could attempt arbitrary file reads |
| operator env (DECKFLOW_LLM_PROVIDER, model, CLI override) → process spawn | env selects provider, model, and override command |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-07 | Information Disclosure | codex reads/echoes local files under prompt injection | mitigate | codex branch emits the read-isolation flags from 28-DISCOVERY.md; permanent sentinel-exfil xUnit regression asserts no sentinel leakage; one-time live probe in VERIFICATION (D-04) |
| T-28-08 | Tampering | codex output bypasses JSON-repair/ValidateTags | mitigate | codex flows through the shared ExtractWithRetryAsync path (JSON-repair + validate + retry + timeout) unchanged; parity asserted by mirrored tags-validation test |
| T-28-09 | Spoofing | malformed override command-spec | mitigate | BuildOverrideCommandSpec keeps fail-fast JSON-array validation (single {instruction} placeholder); codex override only switches envelope kind to Raw |
| T-28-SC | Tampering | npm/pip/cargo installs | accept | No installs — BCL + existing xUnit only; codex CLI is operator-provided host tooling |
</threat_model>

<verification>
- DeckFlow.Core builds clean (0 new warnings) after factory + service changes.
- Focused Core test filter green (Failed: 0), including the sentinel-exfil regression.
- openai + claude provider tests still pass (no regression).
- The live sentinel-exfil probe + 10-video UAT (D-04/D-05) are executed during phase VERIFICATION, not in this plan.
</verification>

<success_criteria>
Satisfies HSK-02 / Phase 28 SC #1: DECKFLOW_LLM_PROVIDER=codex distills via the CliCommandSpec seam with no NotSupportedException, proven read isolation at the seam, configurable mini-tier model, and unchanged openai/claude paths.
</success_criteria>

<output>
Create `.planning/phases/28-housekeeping-bundle/28-04-SUMMARY.md` when done.
