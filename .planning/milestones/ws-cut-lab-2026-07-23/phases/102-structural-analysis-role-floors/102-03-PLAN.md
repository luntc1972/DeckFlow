---
phase: 102-structural-analysis-role-floors
plan: 03
type: execute
wave: 2
depends_on: [102-01, 102-02]
files_modified:
  - DeckFlow.Web/Services/CutLab/CutLabPageService.cs
  - DeckFlow.Web/Models/CutLabViewModel.cs
  - DeckFlow.Web.Tests/CutLabPageServiceTests.cs
autonomous: true
requirements: [SLOT-01, SLOT-02, FLOOR-01, FLOOR-02]
must_haves:
  truths:
    - "A successful pool POST computes role groups, findings, and resolved floors server-side and exposes them on the view model — derived data is never serialized into CutLabStateJson"
    - "Spellbook is fetched once and categories once (ONE batched query) per POST, both fail-open with availability flags carried into the findings"
    - "A Spellbook or category-store outage still renders the page (findings degrade, roles fall back to heuristics) — no hard failure"
    - "Resolving ICutLabPageService from a container that mirrors Program.cs registrations supplies all four new optional analysis dependencies — a dropped registration cannot silently degrade Phase 102 analysis (DI-guard test)"
    - "User-adjusted floors submitted in CutLabStateJson survive the round-trip: they win the merge over recomputed defaults and re-serialize into the response state"
    - "CutLabViewModel.PoolStatusText is gone and the count-chip copy exists in fewer places (101-VERIFICATION open item 1)"
  artifacts:
    - path: "DeckFlow.Web/Services/CutLab/CutLabPageService.cs"
      provides: "Stages A-F wired after commander resolution: CardFact projection, classification I/O, role assignment, floor defaults+merge, findings, floor evaluation inputs; internal DI probe HasStructuralAnalysisDependencies"
      contains: "GetCategoriesForNamesAsync"
    - path: "DeckFlow.Web/Models/CutLabViewModel.cs"
      provides: "Role-group views, finding views, floor rows, per-card role memberships for the pool table"
      contains: "FloorRows"
  key_links:
    - from: "CutLabPageService"
      to: "ScryfallCardFactMapper.ToCardFact"
      via: "ResolvedCutLabEntry extended to carry ScryfallCardData (per-POST only)"
      pattern: "ToCardFact"
    - from: "CutLabPageService"
      to: "CutLabRoleAssigner.AssignRoles"
      via: "per-card role assignment over the resolved pool"
      pattern: "AssignRoles"
    - from: "CutLabPageService"
      to: "CutLabFloorDefaults.ResolveDefaults"
      via: "bracket/play-experience/commander-MV floor derivation + prior-floor merge"
      pattern: "ResolveDefaults"
    - from: "CutLabPageService"
      to: "CutLabStructuralFindings.Compute"
      via: "findings with availability flags"
      pattern: "CutLabStructuralFindings"
    - from: "CutLabPageServiceTests (DI guard)"
      to: "Program.cs AddScoped<ICutLabPageService, CutLabPageService>()"
      via: "ServiceCollection mirroring Program.cs registrations + HasStructuralAnalysisDependencies probe"
      pattern: "HasStructuralAnalysisDependencies"
---

<objective>
Wire the Phase 102 analysis pipeline into CutLabPageService.ProcessAsync — CardFact projection,
Manabase-mirrored classification I/O (Spellbook once + ONE batched category query, both fail-open),
eight-role assignment, floor defaults with user-override merge, structural findings — and extend
CutLabViewModel to carry role groups, findings, and floor rows to the view, deleting the dead
PoolStatusText on the way.

Purpose: Plans 01/02 shipped the pure rules; this plan is the orchestration that turns a POST into
the structural read the UI (plan 04) renders. It is the only plan touching the page service, so all
I/O, merge, and view-model shaping lands here.
Output: extended CutLabPageService (+ResolvedCutLabEntry carrying ScryfallCardData), extended
CutLabViewModel, extended CutLabPageServiceTests with fakes for the two new optional deps plus a
DI-guard construction test.

DECISIONS locked for this plan:
- Derived data (roles, findings, defaults) is computed per POST and NEVER serialized into
  CutLabState (RESEARCH Pitfall 3; 256 KB cap headroom).
- New deps are OPTIONAL nullable ctor params defaulting null (ManabaseAnalysisService precedent)
  so existing test call sites compile unchanged; DI supplies the registered implementations.
  Program.cs registration stays the plain AddScoped at line 181 — unchanged.
- BECAUSE the deps are optional, missing container registrations would silently null them and
  degrade all Phase 102 analysis (codex review LOW). A DI-guard test therefore builds a
  ServiceCollection mirroring Program.cs registrations and asserts the resolved service received all
  four deps via an internal probe property (mechanism specified in Task 1).
- PlayExperience → ManabaseMode via CutLabRoleAssigner.ResolveMode; bracket → floors via
  CutLabFloorDefaults.ResolveBracket (Open Q2 resolution).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/cut-lab/phases/102-structural-analysis-role-floors/102-RESEARCH.md
@.planning/workstreams/cut-lab/phases/102-structural-analysis-role-floors/102-PATTERNS.md
@.planning/workstreams/cut-lab/phases/102-structural-analysis-role-floors/102-UI-SPEC.md

<interfaces>
<!-- CutLabPageService.ProcessAsync pipeline (Services/CutLab/CutLabPageService.cs):
  ResolveCommanderSelection at line 158; BuildState at line 171; Error(message, warnings) helper at
  lines 417-422; private sealed record ResolvedCutLabEntry(Name, Quantity, TypeLine, IsCommander)
  at line 424 — currently DISCARDS the resolved ScryfallCardData (used transiently ~lines 205-221).
  New stages slot between commander resolution and BuildState/serialize.
  CutLabPageService is internal sealed; InternalsVisibleTo("DeckFlow.Web.Tests") already grants the
  test project access (AssemblyInfo.cs) — the DI probe below relies on this. -->

<!-- Classification I/O to mirror (Services/Manabase/ManabaseAnalysisService.cs:835-877 Spellbook
  once + comboNames HashSet OrdinalIgnoreCase; 905-929 GetCategoriesFailOpenAsync with static
  EmptyCategories; 870-873 the batching rationale comment — ONE GetCategoriesForNamesAsync call). -->

<!-- Optional-dep ctor precedent (ManabaseAnalysisService.cs:313-337):
  ICategoryKnowledgeStore? categoryKnowledge = null, ICommanderSpellbookService? spellbook = null.
  CutLabPageService ctor guard style at lines 77-91 (ThrowIfNull required deps, NullLogger fallback). -->

<!-- DI (Program.cs): ICategoryKnowledgeStore singleton :172; ICommanderSpellbookService registered;
  IManabaseBaselineProvider singleton :95,310; ICedhLandBaselineProvider singleton :94,307;
  CutLabPageService AddScoped :181 — registration unchanged, optional params resolve automatically
  (MS DI supplies registered services to optional ctor parameters; unregistered → default null,
  which is exactly the silent-degradation risk the DI-guard test pins). -->

<!-- Plan 01 outputs: CutLabFloorRules.RoleKeys / .Evaluate / .ClampFloors;
  CutLabFloorDefaults.ResolveBracket / .ResolveDefaults / CutLabResolvedFloor
  (Role, Floor, IsUserSet, DefaultValue, ResolvedBracket, BracketWasFallback);
  CutLabState.RoleFloors : IReadOnlyList<CutLabRoleFloor>. -->

<!-- Plan 02 outputs: CutLabRoleAssigner.ResolveMode / .AssignRoles(fact, categories, isComboPiece,
  mode) -> IReadOnlyList<string> (ramp land-gated: lands and ramp are disjoint);
  CutLabStructuralFindings.Compute(pool, nearCombos, floors, comboDataAvailable,
  categoryDataAvailable) -> CutLabStructuralFindingsResult;
  CutLabAnalyzedCard(Name, ManaValue, IsLand, Roles, Categories). -->

<!-- Test seam (DeckFlow.Web.Tests/CutLabPageServiceTests.cs:17-53): private FakeLoader/FakeResolver/
  FakeBanListService records passed to the real service; FakeCategoryKnowledgeStore already exists at
  DeckFlow.Web.Tests/TestDoubles/FakeCategoryKnowledgeStore.cs:12. PoolStatusText asserted at :52 —
  assertion must change with the view-model cleanup. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Carry ScryfallCardData through resolution + fail-open classification I/O stage + DI guard</name>
  <files>DeckFlow.Web/Services/CutLab/CutLabPageService.cs, DeckFlow.Web.Tests/CutLabPageServiceTests.cs</files>
  <behavior>
    - ProcessAsync with a stubbed spellbook returning one included combo and a category store returning tags → the analysis stage receives comboNames containing the combo pieces and categoriesByName from ONE batched call (fake records call count == 1)
    - ProcessAsync with a spellbook stub that throws → page still succeeds; comboDataAvailable=false flows to findings; a LogWarning fired (no rethrow except OperationCanceledException)
    - ProcessAsync with a category store stub that throws → page still succeeds; categoryDataAvailable=false; heuristic-only classification proceeds
    - ProcessAsync with both new ctor params omitted (null) → compiles and behaves as both-unavailable (existing test call sites unchanged)
    - OperationCanceledException from either dep propagates (never swallowed)
    - DI guard: a ServiceCollection mirroring Program.cs registrations (fakes registered under ICategoryKnowledgeStore, ICommanderSpellbookService, IManabaseBaselineProvider, ICedhLandBaselineProvider plus the three required deps and logging) with the SAME plain `AddScoped<ICutLabPageService, CutLabPageService>()` resolves a service whose HasStructuralAnalysisDependencies probe is true; removing any one of the four registrations from that collection flips the probe false (proving the test actually detects a dropped registration)
  </behavior>
  <read_first>
    - DeckFlow.Web/Services/CutLab/CutLabPageService.cs (FULL read — pipeline order, ResolvedCutLabEntry at line 424, transient ScryfallCardData use ~205-221, Error helper, ctor guards)
    - DeckFlow.Web/Services/Manabase/ManabaseAnalysisService.cs lines 313-337 and 828-929 (optional-dep ctor + the exact Spellbook/category fail-open stage to mirror, incl. the batching rationale comment)
    - DeckFlow.Web/Services/CommanderSpellbookService.cs lines 16-48 (ICommanderSpellbookService contract, FindCombosAsync null-on-failure, IncludedCombos/AlmostIncludedCombos)
    - DeckFlow.Web/Program.cs lines 170-190 (the exact CutLabPageService registration at :181 and the four dependency registrations the DI-guard test mirrors)
    - DeckFlow.Web.Tests/CutLabPageServiceTests.cs (existing fakes and call sites being extended)
    - DeckFlow.Web.Tests/TestDoubles/FakeCategoryKnowledgeStore.cs (existing test double to reuse)
    - DeckFlow.Core/Manabase/ScryfallCardFactMapper.cs (ToCardFact signature) and DeckFlow.Core/Manabase/ScryfallCardData.cs
  </read_first>
  <action>
    Extend the private `ResolvedCutLabEntry` record with a `ScryfallCardData? Card` member populated at
    the resolution site (~lines 205-221) — per-POST only; nothing new is serialized. Commander included.

    Extend the ctor with four optional nullable deps defaulting null, stored in readonly fields:
    `ICategoryKnowledgeStore? categoryKnowledge = null`, `ICommanderSpellbookService? spellbook = null`,
    `IManabaseBaselineProvider? manabaseBaseline = null`, `ICedhLandBaselineProvider? cedhBaseline = null`.
    Do not add ThrowIfNull for these (they are optional by design); keep existing guards untouched.
    Program.cs registration is unchanged — the container supplies the registered implementations.

    DI-GUARD PROBE (codex review LOW — optional deps + plain AddScoped means a dropped registration
    silently nulls them): add an internal probe property on CutLabPageService:
    `internal bool HasStructuralAnalysisDependencies =>
        _categoryKnowledge is not null && _spellbook is not null
        && _manabaseBaseline is not null && _cedhBaseline is not null;`
    with xmldoc stating it exists solely for the DI-wiring guard test (InternalsVisibleTo already
    grants DeckFlow.Web.Tests access). Then add a construction test in CutLabPageServiceTests:
    build a `ServiceCollection` that mirrors Program.cs — register fakes under the SAME interfaces and
    lifetimes Program.cs uses (`AddSingleton<ICategoryKnowledgeStore>(fake)`,
    `AddSingleton<ICommanderSpellbookService>(fake)`, `AddSingleton<IManabaseBaselineProvider>(fake)`,
    `AddSingleton<ICedhLandBaselineProvider>(fake)`, plus the three required deps
    (IDeckEntryLoader/IScryfallCardResolver/ICommanderBanListService fakes) and `AddLogging()`),
    then the EXACT registration shape from Program.cs:181:
    `services.AddScoped<ICutLabPageService, CutLabPageService>();` (plain — no factory lambda).
    BuildServiceProvider, CreateScope, resolve ICutLabPageService, cast to CutLabPageService, and
    `Assert.True(service.HasStructuralAnalysisDependencies)`. Add the negative control in the same or
    a second Fact: the same collection minus one of the four registrations resolves with the probe
    FALSE — proving the guard detects a dropped registration rather than passing vacuously. Comment
    the test with why it exists: optional ctor params mean MS DI passes null for unregistered
    services, so only this test catches a Program.cs registration regression.

    Add the classification I/O stage after ResolveCommanderSelection (line 158), mirroring
    ManabaseAnalysisService.cs:835-877 exactly in shape:
      - Spellbook once: try/catch around `_spellbook.FindCombosAsync(analyzedEntries, ct)` building a
        `HashSet<string>(StringComparer.OrdinalIgnoreCase)` of IncludedCombos card names AND capturing
        `IReadOnlyList<SpellbookAlmostCombo>` from AlmostIncludedCombos; `comboDataAvailable` = the call
        ran and returned non-null. Catch OperationCanceledException → throw; catch Exception → LogWarning
        with a structured template ("Cut Lab: Commander Spellbook fetch failed; continuing without combo
        roles.") and degrade. Null `_spellbook` field → comboDataAvailable=false.
      - Categories once: a private `GetCategoriesFailOpenAsync(poolNames, ct)` mirroring
        ManabaseAnalysisService.cs:905-929 (static EmptyCategories sentinel, ONE batched
        GetCategoriesForNamesAsync call for the whole pool — NEVER per-card; keep the rationale as a
        comment citing the ~65-query/20s incident). `categoryDataAvailable` = the call ran successfully.
    Pass entries to FindCombosAsync in the same DeckEntry shape ManabaseAnalysisService uses (read its
    call site for the exact projection from resolved entries).

    Extend CutLabPageServiceTests: an inline `FakeSpellbookService : ICommanderSpellbookService` (queue
    of results or configured exception) following the existing private-fake record style; reuse
    FakeCategoryKnowledgeStore; add a throwing category-store fake; inline fakes for
    IManabaseBaselineProvider/ICedhLandBaselineProvider (needed by the DI-guard test; plan 01's
    CutLabFloorDefaultsTests has the same-interface fakes to pattern-match). Cover every behavior row.
    Existing ctor call sites stay valid (optional params).
  </action>
  <verify>
    <automated>dotnet test DeckFlow.Web.Tests --filter "FullyQualifiedName~CutLabPageServiceTests" --nologo</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'ScryfallCardData' DeckFlow.Web/Services/CutLab/CutLabPageService.cs` returns ≥2 (record member + population site)
    - `grep -c 'GetCategoriesForNamesAsync' DeckFlow.Web/Services/CutLab/CutLabPageService.cs` returns 1 (exactly one batched call site)
    - `grep -c 'OperationCanceledException' DeckFlow.Web/Services/CutLab/CutLabPageService.cs` returns ≥2 (both fail-open wrappers rethrow cancellation)
    - `grep -c 'HasStructuralAnalysisDependencies' DeckFlow.Web/Services/CutLab/CutLabPageService.cs` returns ≥1 (internal DI probe present) and `grep -c 'HasStructuralAnalysisDependencies' DeckFlow.Web.Tests/CutLabPageServiceTests.cs` returns ≥2 (positive + negative-control assertions)
    - The DI-guard test registers the four analysis interfaces and uses the plain `AddScoped<ICutLabPageService, CutLabPageService>()` registration shape from Program.cs:181; its negative control (one registration removed → probe false) passes
    - A test asserts the page succeeds when the spellbook fake throws and when the category fake throws
    - A test asserts the category fake was invoked exactly once for a multi-card pool
    - All CutLabPageServiceTests pass, including every pre-existing test
  </acceptance_criteria>
  <done>The page service carries resolved ScryfallCardData per POST and runs the canonical fail-open, batched classification I/O with availability flags — outages degrade, cancellation propagates, existing tests compile untouched, and a DI-guard test with a negative control proves the container supplies all four optional analysis deps so a dropped Program.cs registration can never silently degrade Phase 102.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire roles, floors, findings into the result + view model; delete PoolStatusText</name>
  <files>DeckFlow.Web/Services/CutLab/CutLabPageService.cs, DeckFlow.Web/Models/CutLabViewModel.cs, DeckFlow.Web.Tests/CutLabPageServiceTests.cs</files>
  <behavior>
    - Happy-path POST (bracket 4, play experience "Focused", commander MV 3) → view model exposes: 8 role groups in fixed order with member cards and locked counts; 8 floor rows each carrying InPoolCount, Floor, DefaultValue, IsUserSet, provenance label inputs (ResolvedBracket, BracketWasFallback), and AtFloor flag (count ≤ floor + 1); findings list from CutLabStructuralFindings
    - A card with roles [draw, engines] appears in BOTH groups and its pool-table role list reads "Card draw · Engines"
    - Prior CutLabStateJson carrying {role:"interaction", floor:15, isUserSet:true} → the interaction floor row shows Floor 15, IsUserSet true, DefaultValue = derived default; the response state's RoleFloors round-trips the user entry
    - Floors with IsUserSet=false are NOT written into the response CutLabState.RoleFloors (only user decisions persist)
    - PoolStatusText no longer exists on CutLabViewModel; the test formerly asserting it now asserts the replacing surface (CardCount + locked count fields)
    - A wiring guard test asserts CutLabFloorRules.RoleKeys sequence-equals the role keys emitted by the role groups (plan 01/plan 02 key lists cannot drift)
  </behavior>
  <read_first>
    - DeckFlow.Web/Models/CutLabViewModel.cs (FULL read — record shape, static From factory, PoolStatusText at :48-49, 75, 79-88)
    - DeckFlow.Web/Services/CutLab/CutLabPageService.cs (BuildState at :353, result record shape returned to the controller)
    - DeckFlow.Web/Services/CutLab/CutLabFloorDefaults.cs and CutLabFloorRules.cs (plan 01 APIs)
    - DeckFlow.Web/Services/CutLab/CutLabRoleAssigner.cs and CutLabStructuralFindings.cs (plan 02 APIs)
    - DeckFlow.Web.Tests/CutLabPageServiceTests.cs line 52 (the PoolStatusText assertion to replace)
  </read_first>
  <action>
    In CutLabPageService, after the Task 1 I/O stage, add the pure stages:
      - Stage A/C: project each ResolvedCutLabEntry with a non-null Card through
        ScryfallCardFactMapper.ToCardFact(card, quantity, isCommander), then
        CutLabRoleAssigner.AssignRoles(fact, categoriesFor(name), comboNames.Contains(name),
        CutLabRoleAssigner.ResolveMode(intent.PlayExperience)). Entries whose Card is null (resolution
        miss) get empty roles — never a throw.
      - Stage D: `CutLabFloorDefaults.ResolveDefaults(intent.Bracket, intent.PlayExperience,
        commanderManaValue, commanderNames, _manabaseBaseline, _cedhBaseline, priorState.RoleFloors)`
        where commanderManaValue comes from the commander's CardFact (0 when unresolved).
      - Stage E: build `CutLabAnalyzedCard` list (Name, ManaValue from fact, IsLand from roles, Roles,
        Categories) and call CutLabStructuralFindings.Compute with the resolved floor dictionary
        (role → Floor) and both availability flags.
      - Stage F/persistence: BuildState writes ONLY user-set floors into CutLabState.RoleFloors
        (IsUserSet=true entries from the resolved list); derived defaults/roles/findings never enter the
        serialized state. Extend the service result record with the derived data: role assignments per
        card, resolved floors (CutLabResolvedFloor list), findings result.

    Extend CutLabViewModel (namespace DeckFlow.Web.Models) with init-only members + From(...) mapping:
      - `IReadOnlyList<CutLabRoleGroupView> RoleGroups` — record with RoleKey, DisplayLabel (fixed map:
        lands→"Lands", ramp→"Ramp", draw→"Card draw", interaction→"Interaction", protection→"Protection",
        engines→"Engines", payoffs→"Payoffs", wincons→"Win conditions"), Members
        (IReadOnlyList of record: Name, IsLocked, IsCommander), LockedCount.
      - `IReadOnlyList<CutLabFindingView> Findings` — Heading, Lead, Evidence (name + optional "· MV n"
        suffix precomputed), plus `bool ComboDataUnavailable` / `bool CategoryDataUnavailable` on the
        view model root for the two UI-SPEC degradation lines.
      - `IReadOnlyList<CutLabFloorRowView> FloorRows` — RoleKey, DisplayLabel, InPoolCount, Floor,
        DefaultValue, IsUserSet, AtFloor (InPoolCount ≤ Floor + 1), SourceLabel prebuilt per UI-SPEC:
        "Default for B{bracket}: {value}" or "Default: {value} — based on {play experience}" when
        BracketWasFallback.
      - Per-card role display for the pool table: expose a lookup (e.g. IReadOnlyDictionary<string,string>
        RoleListByCardName with " · "-joined display labels, and the raw space-separated role-key string
        for the data-cut-lab-role attribute, e.g. RoleKeysByCardName).
      - DELETE PoolStatusText (member, its From assignment, and the private helper at :79-88). Update
        CutLabPageServiceTests.cs:52 to assert CardCount and the locked-count data instead (the view and
        TS own the display string — 101-VERIFICATION open item 1; plan 04 finishes the copy consolidation).
    Every new member: xmldoc, { get; init; }, IReadOnlyList surfaces.

    Extend CutLabPageServiceTests to cover every behavior row, including the RoleKeys drift-guard test
    (Assert.Equal(CutLabFloorRules.RoleKeys, model/result role-group key sequence)).
  </action>
  <verify>
    <automated>dotnet test DeckFlow.Web.Tests --filter "FullyQualifiedName~CutLabPageServiceTests" --nologo &amp;&amp; dotnet build DeckFlow.Web/DeckFlow.Web.csproj -c Debug --nologo -clp:ErrorsOnly</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'PoolStatusText' DeckFlow.Web/Models/CutLabViewModel.cs` returns 0 and `grep -rc 'PoolStatusText' DeckFlow.Web.Tests/CutLabPageServiceTests.cs` returns 0
    - `grep -c 'FloorRows' DeckFlow.Web/Models/CutLabViewModel.cs` returns ≥1, `grep -c 'RoleGroups' DeckFlow.Web/Models/CutLabViewModel.cs` returns ≥1, `grep -c 'Findings' DeckFlow.Web/Models/CutLabViewModel.cs` returns ≥1
    - `grep -c 'ToCardFact' DeckFlow.Web/Services/CutLab/CutLabPageService.cs` returns ≥1 (mapper used, no hand-mapping)
    - A test asserts only IsUserSet floors persist into the response state's RoleFloors
    - A test asserts a two-role card appears in both role groups
    - The RoleKeys drift-guard test exists and passes
    - All CutLabPageServiceTests pass; Web builds clean
  </acceptance_criteria>
  <done>ProcessAsync produces the full structural read (groups, findings, floors with provenance and merge) on the view model while the serialized envelope carries only user decisions, and the dead PoolStatusText triplication is gone from model and tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client POST (CutLabStateJson + intent fields) → page service | Untrusted; floors arrive pre-clamped by the serializer (plan 01) but roles/findings must never be client-influenced |
| page service → Spellbook / category DB | Outbound; repeated POSTs could hammer upstreams |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-102-03-01 | Tampering | Client-supplied role/finding data | mitigate | Roles and findings recomputed server-side every POST from Scryfall-resolved facts; no client channel exists for them (only RoleFloors round-trips, already clamped at deserialize) |
| T-102-03-02 | Denial of Service | Upstream abuse via repeated POSTs | mitigate | Spellbook responses memory-cached per deck list (existing service); ONE batched category query per POST; Scryfall behind ScryfallThrottle; all fail-open — an outage degrades, never blocks |
| T-102-03-03 | Denial of Service | Per-card category queries regression | mitigate | Acceptance criterion pins exactly one GetCategoriesForNamesAsync call site; rationale comment carried from the documented 20 s incident |
| T-102-03-04 | Denial of Service (silent degradation) | Optional analysis deps nulled by a missing DI registration | mitigate | DI-guard test mirrors Program.cs registrations + plain AddScoped, asserts HasStructuralAnalysisDependencies with a negative control — a dropped registration fails the suite instead of silently degrading |
| T-102-SC | Tampering | npm/pip/cargo installs | accept | Zero new dependencies this phase |
</threat_model>

<verification>
- `dotnet build` clean, no new warnings
- `dotnet test DeckFlow.Web.Tests --filter CutLab` fully green (all plans' suites, incl. regression guards CutLabLockStateTests / CutLabStateSerializerTests / CutLabControllerTests)
- LF endings; changed-lines format gate; { get; init; } carve-out intact
</verification>

<success_criteria>
- ProcessAsync runs stages A-F per POST with fail-open batched I/O and availability flags (SLOT-01/02 wiring)
- DI-guard test (with negative control) proves the plain Program.cs AddScoped supplies all four optional analysis deps — no silent degradation path
- Floor defaults derive and merge server-side; only user-set floors persist (FLOOR-01/02 wiring)
- View model exposes role groups, findings, floor rows, and per-card role lists ready for plan 04's Razor sections
- PoolStatusText dead code removed with tests updated
</success_criteria>

<output>
Create `.planning/workstreams/cut-lab/phases/102-structural-analysis-role-floors/102-03-SUMMARY.md` when done
</output>
