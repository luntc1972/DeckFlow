---
phase: 01-wdg-04-focus-trapped-modal
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - DeckFlow.Web/wwwroot/ts/admin-modal.ts
  - DeckFlow.Web/Views/Shared/_AdminConfirmModal.cshtml
  - DeckFlow.Web/wwwroot/css/admin.css
  - DeckFlow.Web/wwwroot/ts/admin-feedback.ts
  - DeckFlow.Web/Views/AdminFeedback/Detail.cshtml
autonomous: true
requirements:
  - MODAL-01
must_haves:
  truths:
    - "Admin opens delete-confirm on /Admin/Feedback/Detail/{id} and the native <dialog> element opens via showModal() — no hand-rolled focus-trap library, no npm dependency added"
    - "Tab/Shift+Tab cycle stays inside the modal across the two native button form controls (Cancel + Confirm), verified by HUMAN-UAT on /Admin/Feedback/Detail/{id}. Nested custom-element cycling (df-select/df-typeahead) deferred to Phase 7 reuse per CONTEXT D-09 + RESEARCH UAT-9 (Phase 7 ContentSources delete-confirm modal will exercise nested custom elements when admin source-CRUD UI lands)."
    - "Escape closes the modal AND restores focus to the trigger button; cancel button + click-outside also close it"
    - "New modal CSS lands in wwwroot/css/admin.css (or new _AdminConfirmModal.cshtml partial), uses existing :root tokens, and does not leak any unscoped element selectors into the 22 guild themes (Pitfall 10 mitigation)"
  artifacts:
    - path: "DeckFlow.Web/wwwroot/ts/admin-modal.ts"
      provides: "Reusable IIFE module exposing window.DeckFlowAdminModal.showConfirm(opts): Promise<boolean>"
      contains: "window.DeckFlowAdminModal"
    - path: "DeckFlow.Web/Views/Shared/_AdminConfirmModal.cshtml"
      provides: "Structural singleton <dialog> partial with id='admin-confirm-modal'"
      contains: "id=\"admin-confirm-modal\""
    - path: "DeckFlow.Web/wwwroot/css/admin.css"
      provides: "Modal CSS block appended at end-of-file, scoped to .admin-modal* selectors"
      contains: "admin-modal__panel"
    - path: "DeckFlow.Web/wwwroot/ts/admin-feedback.ts"
      provides: "Submit interceptor calling showConfirm on [data-admin-confirm-delete] forms"
      contains: "data-admin-confirm-delete"
    - path: "DeckFlow.Web/Views/AdminFeedback/Detail.cshtml"
      provides: "Delete form with onsubmit removed; partial included; Scripts section loads admin-modal.js before admin-feedback.js"
      contains: "_AdminConfirmModal"
  key_links:
    - from: "DeckFlow.Web/Views/AdminFeedback/Detail.cshtml"
      to: "DeckFlow.Web/wwwroot/js/admin-modal.js"
      via: "<script src=\"~/js/admin-modal.js\"> in @section Scripts (loaded BEFORE admin-feedback.js)"
      pattern: "admin-modal\\.js"
    - from: "DeckFlow.Web/wwwroot/ts/admin-feedback.ts"
      to: "window.DeckFlowAdminModal.showConfirm"
      via: "defensive read of window.DeckFlowAdminModal?.showConfirm inside submit handler"
      pattern: "window\\.DeckFlowAdminModal"
    - from: "DeckFlow.Web/Views/Shared/_AdminConfirmModal.cshtml"
      to: "DeckFlow.Web/wwwroot/ts/admin-modal.ts"
      via: "DOM ids admin-confirm-modal / admin-modal-title / admin-modal-message + data-admin-modal-confirm/cancel hooks"
      pattern: "data-admin-modal-(confirm|cancel)"
    - from: "DeckFlow.Web/Views/AdminFeedback/Detail.cshtml"
      to: "AdminFeedbackController.Apply"
      via: "<form method=\"post\" asp-action=\"Apply\"> with preserved @Html.AntiForgeryToken()"
      pattern: "@Html\\.AntiForgeryToken\\(\\)"
---

<objective>
Phase 1 ships ONE plan delivering the WDG-04 focus-trapped modal primitive that replaces the deferred inline `onsubmit="return confirm(...)"` at `DeckFlow.Web/Views/AdminFeedback/Detail.cshtml:41`. The plan creates:

- A reusable browser-side TypeScript module (`admin-modal.ts`) exposing `window.DeckFlowAdminModal.showConfirm(opts): Promise<boolean>`, built on the native HTML `<dialog>` element + `showModal()` (no npm focus-trap dep per CONTEXT D-06 + RESEARCH).
- A structural-only Razor partial (`_AdminConfirmModal.cshtml`) that renders the singleton `<dialog>` DOM template with ARIA labelledby/describedby + autofocus on Cancel for destructive-safety.
- An appended Phase 1 CSS block in `admin.css` using UI-SPEC.md token+pixel values verbatim, scoped via `.admin-modal*` prefixes (research invariant #11) including a local `prefers-reduced-motion` gate (admin doesn't inherit `site-common.css`).
- An additive edit to `admin-feedback.ts` that intercepts the Delete form submit, awaits the modal, and calls `form.submit()` on confirmation — preserving the existing AntiForgeryToken POST body.
- A targeted edit to `Detail.cshtml` removing `onsubmit`, adding the partial include, and wiring two `<script>` tags in the correct order (`admin-modal.js` BEFORE `admin-feedback.js` per RESEARCH Pitfall 4).

Purpose: Close v1.3 WDG-04 carry-over and ship the FIRST reusable admin-modal primitive that Phase 7 (Content KB Admin UI) will consume for ContentSources delete without rewrite.

Output: 5 changed/created files, ZERO new npm/NuGet dependencies, manual UAT-1..7 PASS, `dotnet build DeckFlow.Web -c Release` clean.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-wdg-04-focus-trapped-modal/01-CONTEXT.md
@.planning/phases/01-wdg-04-focus-trapped-modal/01-RESEARCH.md
@.planning/phases/01-wdg-04-focus-trapped-modal/01-UI-SPEC.md

# Existing files the executor must read before editing
@DeckFlow.Web/Views/AdminFeedback/Detail.cshtml
@DeckFlow.Web/wwwroot/ts/admin-feedback.ts
@DeckFlow.Web/wwwroot/css/admin.css
@DeckFlow.Web/Views/Shared/_AdminLayout.cshtml
@DeckFlow.Web/Views/AdminFeedback/Index.cshtml
@DeckFlow.Web/tsconfig.json

<interfaces>
<!-- Key contracts the executor needs. Extracted from CONTEXT.md D-07 + RESEARCH.md verified IIFE pattern. -->
<!-- Executor should implement these directly — no codebase exploration needed for the API surface. -->

admin-modal.ts public surface (IIFE-attached, NOT ES exports because tsconfig module:"none"):

```typescript
interface Window {
  DeckFlowAdminModal?: {
    showConfirm?: (opts: ConfirmOptions) => Promise<boolean>;
  };
}

interface ConfirmOptions {
  title: string;                // required, plain string (textContent-safe)
  message: string;              // required, plain string (textContent-safe)
  confirmLabel?: string;        // default: "Confirm"
  cancelLabel?: string;         // default: "Cancel"
  danger?: boolean;             // default: false; when true, toggles .admin-modal__button--danger on confirm button
}

// Resolves true when user clicks Confirm.
// Resolves false on ESC, Cancel click, backdrop click, or any error.
window.DeckFlowAdminModal.showConfirm(opts): Promise<boolean>
```

DOM contract (between admin-modal.ts and _AdminConfirmModal.cshtml):
- `<dialog id="admin-confirm-modal" class="admin-modal">` — singleton per page
- Inside: `#admin-modal-title` (referenced by aria-labelledby) — title <p>
- Inside: `#admin-modal-message` (referenced by aria-describedby) — message <p>
- Inside: `[data-admin-modal-cancel]` — cancel button (has autofocus per UI-SPEC safety extension)
- Inside: `[data-admin-modal-confirm]` — confirm button

DOM contract (between admin-feedback.ts and Detail.cshtml):
- `<form data-admin-confirm-delete data-admin-feedback-id="@Model.Id">` — the form to intercept
- admin-feedback.ts attaches a submit listener that reads `form.dataset.adminFeedbackId`

Script include order (REQUIRED by RESEARCH Pitfall 4):
1. `~/js/admin-modal.js` (declares window.DeckFlowAdminModal.showConfirm)
2. `~/js/admin-feedback.js` (reads window.DeckFlowAdminModal?.showConfirm defensively)
</interfaces>

<corrections_to_context>
<!-- Three CONTEXT.md decisions need mechanical corrections per RESEARCH.md §"Correction to CONTEXT" subsections. The user's intent is preserved; only implementation details shift. -->

1. D-01 pseudo-code shows ES `import/export`. The codebase is `tsconfig module:"none"` — ES module syntax compile-fails. Use the IIFE + `window.DeckFlowAdminModal` pattern (mirrors `df-select.ts:837-839`).

2. D-03 says "Click-outside cancels (standard native <dialog> UX)". Native `<dialog>` opened via `showModal()` defaults to `closedby="closerequest"` and does NOT close on backdrop click. Hand-roll `dialog.addEventListener('click', e => { if (e.target === dialog) dialog.close('cancel'); })` (3 lines). Do NOT use the declarative `closedby="any"` attribute (insufficient baseline support; RESEARCH Pitfall 6).

3. D-06 implies hand-rolling focus restoration is required. Native `<dialog>` already restores focus to the invoking element on close. Keep the `previouslyFocused?.focus()` line as defense-in-depth (~2 lines) for future Phase 7 reuse where the trigger element might be re-rendered during the await — but it is belt-and-suspenders, not a gap-filler.
</corrections_to_context>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create admin-modal.ts (IIFE + window.DeckFlowAdminModal.showConfirm)</name>
  <files>DeckFlow.Web/wwwroot/ts/admin-modal.ts</files>
  <read_first>
    - DeckFlow.Web/wwwroot/ts/admin-feedback.ts (existing admin IIFE shape, 'use strict', DOMContentLoaded wrapping)
    - DeckFlow.Web/wwwroot/ts/df-select.ts lines 1-6 and 837-839 (canonical `interface Window` declaration-merge + `window.DeckFlow.attachX = …` pattern — grep first to locate exact lines)
    - DeckFlow.Web/tsconfig.json (confirm strict:true and module:"none" — drives IIFE pattern, forbids ES export keyword)
    - .planning/phases/01-wdg-04-focus-trapped-modal/01-RESEARCH.md "Code Examples" section (lines ~563-680) — verbatim implementation reference
    - .planning/phases/01-wdg-04-focus-trapped-modal/01-CONTEXT.md D-06 + D-07 (ARIA + API shape)
    - .planning/phases/01-wdg-04-focus-trapped-modal/01-UI-SPEC.md "Interaction Spec" + "A11y Spec" sections
  </read_first>
  <action>
Create a new file `DeckFlow.Web/wwwroot/ts/admin-modal.ts` (LF line endings, 2-space indent per `.editorconfig`).

File top: declaration-merge an `interface Window { DeckFlowAdminModal?: { showConfirm?: (opts: ConfirmOptions) => Promise<boolean>; }; }` plus an `interface ConfirmOptions { title: string; message: string; confirmLabel?: string; cancelLabel?: string; danger?: boolean; }`.

Body: an IIFE `(((): void => { 'use strict'; ... })();`. Inside, define `const showConfirm = (opts: ConfirmOptions): Promise<boolean> => new Promise((resolve) => { ... })` with this exact behavior (per D-06, D-07, RESEARCH §"Code Examples", and the corrections_to_context block above):

1. `document.querySelector<HTMLDialogElement>('#admin-confirm-modal')` — if missing, resolve(false) and return (fail-closed — partial not rendered on this page). Also fail-closed if `typeof dialog.showModal !== 'function'` (older browser without native `<dialog>` support per RESEARCH §"Browser baseline" — resolve(false) and return) and if `dialog.open === true` (another modal call still in progress — resolve(false) and return; prevents `InvalidStateError` from showModal-on-open-dialog).
2. Query the four inner elements by id/data-attribute: `#admin-modal-title`, `#admin-modal-message`, `[data-admin-modal-confirm]`, `[data-admin-modal-cancel]`. If any missing, resolve(false) and return.
3. Populate via `textContent` (NEVER `innerHTML` — XSS guard per RESEARCH Security Domain §V5): title, message, confirm-button label (fallback "Confirm"), cancel-button label (fallback "Cancel").
4. Toggle the `admin-modal__button--danger` class on the confirm button using `confirmBtn.classList.toggle('admin-modal__button--danger', opts.danger === true)`.
5. Capture `const previouslyFocused = document.activeElement as HTMLElement | null;` (defense-in-depth restore-focus per RESEARCH correction).
6. Define handlers:
   - `onConfirm`: sets `dialog.returnValue = 'confirm'` then `dialog.close()`
   - `onCancel`: sets `dialog.returnValue = 'cancel'` then `dialog.close()`
   - `onBackdropClick(event: MouseEvent)`: if `event.target === dialog` then `onCancel()` (hand-rolled backdrop close per D-03 correction; native `<dialog>` does NOT do this by default)
   - `onClose`: removes all four listeners, calls `previouslyFocused?.focus()`, resolves `dialog.returnValue === 'confirm'`
7. Register listeners: `click` on confirmBtn → onConfirm; `click` on cancelBtn → onCancel; `click` on dialog → onBackdropClick; `close` on dialog → onClose.
8. **Danger-variant safety extension (per RESEARCH Open Decisions §1 + UI-SPEC A11y Spec):** when `opts.danger === true`, after opening, ensure focus lands on the Cancel button (the partial already has `autofocus` on Cancel, so default suffices; this is reinforcement only — do NOT call `confirmBtn.focus()` ever).
9. Reset `dialog.returnValue = ''`. Then wrap the `dialog.showModal()` call in `try { dialog.showModal(); } catch { /* detach the four listeners registered in step 7 (extract a `cleanup()` helper so step 6 onClose + this catch both call it), then */ resolve(false); return; }` — covers `InvalidStateError` or any other showModal failure (per RESEARCH §"Code Examples" + the showConfirm API contract "any error resolves false"). The cleanup helper MUST detach the click listeners on confirmBtn / cancelBtn / dialog AND the close listener on dialog to avoid leaking handlers when showModal fails.

After the showConfirm definition, attach to global: `window.DeckFlowAdminModal = window.DeckFlowAdminModal ?? {}; window.DeckFlowAdminModal.showConfirm = showConfirm;`.

Formatting constraints (CLAUDE.md R-6): 2-space indent throughout; LF line endings; no trailing whitespace; final newline. Do NOT use any `any` types — all queries use generic type parameters (`querySelector<HTMLDialogElement>`, `querySelector<HTMLButtonElement>`, etc.). Do NOT add JSDoc to every line — one top-of-file comment block explaining purpose + consumer list (per `admin-feedback.ts:1-11` style) is sufficient.

XML doc-comments / `<summary>` are NOT required on TypeScript files (those are a C# convention per DOC-01 Phase 2).
  </action>
  <verify>
    <automated>grep -c "window.DeckFlowAdminModal" DeckFlow.Web/wwwroot/ts/admin-modal.ts (expect &gt;=2: declaration-merge + assignment) AND grep -cE "^[[:space:]]*'use strict'" DeckFlow.Web/wwwroot/ts/admin-modal.ts (expect 1) AND grep -c "showModal()" DeckFlow.Web/wwwroot/ts/admin-modal.ts (expect 1) AND grep -c "textContent" DeckFlow.Web/wwwroot/ts/admin-modal.ts (expect &gt;=4: title, message, confirm label, cancel label) AND grep -c "innerHTML" DeckFlow.Web/wwwroot/ts/admin-modal.ts (expect 0 — XSS guard) AND grep -c "import " DeckFlow.Web/wwwroot/ts/admin-modal.ts (expect 0 — module:"none") AND grep -c "^export " DeckFlow.Web/wwwroot/ts/admin-modal.ts (expect 0). Then run `/mnt/c/Program\ Files/dotnet/dotnet.exe build DeckFlow.Web/DeckFlow.Web.csproj -c Release` from clean obj/ — MUST produce `wwwroot/js/admin-modal.js` with zero TS errors and zero new warnings.</automated>
  </verify>
  <acceptance_criteria>
    - File `DeckFlow.Web/wwwroot/ts/admin-modal.ts` exists with LF line endings (`file DeckFlow.Web/wwwroot/ts/admin-modal.ts` reports `ASCII text` not `CRLF`).
    - Contains exact substring `window.DeckFlowAdminModal = window.DeckFlowAdminModal ?? {};`.
    - Contains exact substring `window.DeckFlowAdminModal.showConfirm = showConfirm;`.
    - Contains exact substring `dialog.showModal()`.
    - Contains exact substring `dialog.addEventListener('click', onBackdropClick)` (D-03 hand-rolled backdrop close).
    - Contains exact substring `dialog.addEventListener('close', onClose)`.
    - Contains exact substring `event.target === dialog` (backdrop discrimination).
    - Contains exact substring `previouslyFocused?.focus()` (D-06 defense-in-depth restore).
    - Contains exact substring `classList.toggle('admin-modal__button--danger'` (danger variant toggle).
    - No `innerHTML` assignments anywhere in the file.
    - No `import ` or top-level `export ` statements.
    - TypeScript compiles via MSBuild `CompileTypeScriptAssets` target with zero errors; emitted `DeckFlow.Web/wwwroot/js/admin-modal.js` exists after build.
    - Contains exact substring `typeof dialog.showModal` (fail-closed guard for older browsers per MEDIUM #1 revision).
    - Contains exact substring `dialog.open` (already-open guard per MEDIUM #1 revision — prevents `InvalidStateError`).
    - `tr '\n' ' ' < DeckFlow.Web/wwwroot/ts/admin-modal.ts | grep -cE 'try[[:space:]]*\{[^{}]*dialog\.showModal\(\)[^{}]*\}[[:space:]]*catch'` ≥ 1 (multiline-tolerant — try-block wraps the showModal call AND a catch handler follows; works whether executor writes single-line `try { dialog.showModal(); } catch {}` or multi-line formatted block. Per Codex re-review NEW MEDIUM — original line-local grep failed on natural multi-line formatting).
    - A `cleanup` helper (or equivalent inline listener-detach block) exists and is reachable from BOTH the success path (`onClose`) AND the showModal-failure catch path (no leaked event listeners on failure).
  </acceptance_criteria>
  <done>
    `admin-modal.ts` exists; `dotnet build DeckFlow.Web -c Release` from clean obj/ succeeds; emitted `admin-modal.js` artifact is present in `wwwroot/js/`; all grep assertions above pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create _AdminConfirmModal.cshtml structural-only partial</name>
  <files>DeckFlow.Web/Views/Shared/_AdminConfirmModal.cshtml</files>
  <read_first>
    - DeckFlow.Web/Views/Shared/_WorkflowStepTabs.cshtml (sibling shared partial structure — header comment style, no model)
    - DeckFlow.Web/Views/Shared/_AdminLayout.cshtml lines 14, 18, 36 (confirm .admin-shell exists; confirm admin.css link; confirm RenderSection("Scripts") exists)
    - .planning/phases/01-wdg-04-focus-trapped-modal/01-UI-SPEC.md "Razor Partial Structure" section (lines 303-323) + "A11y Spec" + "CSS Class Naming (BEM)" (lines 277-300)
    - .planning/phases/01-wdg-04-focus-trapped-modal/01-CONTEXT.md D-04, D-08
    - .editorconfig (confirm 4-space indent on .cshtml)
  </read_first>
  <action>
Create a new file `DeckFlow.Web/Views/Shared/_AdminConfirmModal.cshtml` (LF line endings, 4-space indent per `.editorconfig` `[*.{cshtml,razor,cs}]`).

NO `@model` directive (per D-08 structural-only). NO `@inject`. NO `@using`. NO server-side data binding.

Top of file: a single Razor comment block (`@* ... *@`) explaining (a) singleton DOM template, (b) populated at runtime by `admin-modal.ts` via `textContent`, (c) consumers (AdminFeedback Detail v1.4 Phase 1; future Phase 7 ContentSources delete reuse).

Body: a single `<dialog>` element with these EXACT attributes (per UI-SPEC §"CSS Class Naming" + §"A11y Spec" + §"Razor Partial Structure"):

```
<dialog id="admin-confirm-modal" class="admin-modal" aria-labelledby="admin-modal-title" aria-describedby="admin-modal-message">
    <div class="admin-modal__panel">
        <p id="admin-modal-title" class="admin-modal__title"></p>
        <p id="admin-modal-message" class="admin-modal__message"></p>
        <div class="admin-modal__actions">
            <button type="button" class="admin-modal__button admin-modal__button--cancel" value="cancel" data-admin-modal-cancel autofocus>Cancel</button>
            <button type="button" class="admin-modal__button admin-modal__button--confirm" value="confirm" data-admin-modal-confirm>Confirm</button>
        </div>
    </div>
</dialog>
```

Critical attribute rules:
- TITLE element MUST be `<p class="admin-modal__title">`, NOT `<h2>` (per UI-SPEC §"Color" WIG note line 96 — avoids heading-hierarchy violation when modal renders on pages without an `<h1>`).
- `aria-labelledby` value MUST be `admin-modal-title` (matches title id; NOT `admin-confirm-modal__title`).
- `aria-describedby` value MUST be `admin-modal-message` (matches message id).
- `autofocus` MUST be on the CANCEL button (per UI-SPEC §"A11y Spec" line 240 — safety: ENTER doesn't auto-confirm destructive ops).
- Both buttons MUST be `type="button"` (NOT `type="submit"` — modal lives outside any form per D-08).
- Both buttons MUST carry data attributes (`data-admin-modal-cancel`, `data-admin-modal-confirm`) — TS query hooks per RESEARCH §"D-08 structural-only signature".
- Both buttons carry `value="cancel"` / `value="confirm"` (semantic, even though TS uses `dialog.returnValue` setter).
- NO `@Html.AntiForgeryToken()` inside the dialog — modal is a UI gate, NOT a form; the OUTER form in Detail.cshtml retains its token (RESEARCH §"D-08 structural-only signature" + Pitfall 5).
- NO `role="dialog"`, NO `aria-modal="true"` — both are implicit on native `<dialog>` opened via `showModal()` (UI-SPEC §"A11y Spec" lines 234-235 — DO NOT manually add).
- Default text content is `Cancel` and `Confirm` (admin-modal.ts overwrites at call time via `textContent`; defaults provide graceful degradation if JS doesn't load).

End with final newline (LF).
  </action>
  <verify>
    <automated>test -f DeckFlow.Web/Views/Shared/_AdminConfirmModal.cshtml AND grep -cE '^<dialog id="admin-confirm-modal" class="admin-modal"' DeckFlow.Web/Views/Shared/_AdminConfirmModal.cshtml (expect 1, anchored) AND grep -cE 'aria-labelledby="admin-modal-title"' DeckFlow.Web/Views/Shared/_AdminConfirmModal.cshtml (expect 1) AND grep -cE 'aria-describedby="admin-modal-message"' DeckFlow.Web/Views/Shared/_AdminConfirmModal.cshtml (expect 1) AND grep -cE 'autofocus' DeckFlow.Web/Views/Shared/_AdminConfirmModal.cshtml (expect 1 — Cancel only) AND grep -cE '^<h[0-9]' DeckFlow.Web/Views/Shared/_AdminConfirmModal.cshtml (expect 0 — no heading elements) AND grep -c 'data-admin-modal-cancel' DeckFlow.Web/Views/Shared/_AdminConfirmModal.cshtml (expect 1) AND grep -c 'data-admin-modal-confirm' DeckFlow.Web/Views/Shared/_AdminConfirmModal.cshtml (expect 1) AND grep -c '@model ' DeckFlow.Web/Views/Shared/_AdminConfirmModal.cshtml (expect 0) AND grep -c 'AntiForgeryToken' DeckFlow.Web/Views/Shared/_AdminConfirmModal.cshtml (expect 0). Then `/mnt/c/Program\ Files/dotnet/dotnet.exe build DeckFlow.Web/DeckFlow.Web.csproj -c Release` clean — Razor compile MUST succeed (partial parses without view-model binding).</automated>
  </verify>
  <acceptance_criteria>
    - File exists with LF line endings.
    - Contains exactly one `<dialog>` element with `id="admin-confirm-modal"` and `class="admin-modal"`.
    - Title element is `<p id="admin-modal-title" class="admin-modal__title"></p>` (NOT `<h2>`).
    - Message element is `<p id="admin-modal-message" class="admin-modal__message"></p>`.
    - Cancel button precedes Confirm button in DOM order (so initial focus per native first-focusable algorithm + explicit autofocus both land on Cancel).
    - Cancel button has `autofocus` attribute; Confirm button does NOT.
    - Both buttons are `type="button"` (not `submit`).
    - No `@model`, `@inject`, `@using`, or `@Html.AntiForgeryToken()` directives.
    - No `role="dialog"` or `aria-modal=` explicit attributes (implicit on native).
    - Razor compiles via MSBuild without errors (no view-model binding issues).
  </acceptance_criteria>
  <done>
    Partial exists and parses; all grep assertions pass; `dotnet build` clean.
  </done>
</task>

<task type="auto">
  <name>Task 3: Append Phase 1 modal CSS block to admin.css (end-of-file, scoped to .admin-modal*)</name>
  <files>DeckFlow.Web/wwwroot/css/admin.css</files>
  <read_first>
    - DeckFlow.Web/wwwroot/css/admin.css (full file, currently 193 lines — confirm token names at lines 4-17; confirm `.admin-action-form button` shape at line 137; confirm no `.danger` rule currently exists)
    - .planning/phases/01-wdg-04-focus-trapped-modal/01-UI-SPEC.md "Visual Spec" + "Spacing values" + "Animation + timing" + "Acceptance" sections (the Acceptance grep table at lines 366-401 is the AUTHORITATIVE pixel/token contract — values land in CSS verbatim)
    - .planning/phases/01-wdg-04-focus-trapped-modal/01-RESEARCH.md §"admin.css Insertion-Point Safety" (lines 395-428) + §"Open Decisions for Planner" item 2 (lines 803-804) — unified `.danger` rule for in-page Delete button + modal danger variant
    - .editorconfig (confirm 2-space indent for .css)
  </read_first>
  <action>
Append a new CSS block to the END of `DeckFlow.Web/wwwroot/css/admin.css` (after the existing `.admin-empty` rule at line ~193). Touch ZERO existing lines. Use 2-space indent per `.editorconfig`. LF line endings preserved.

Start the new block with this header (mirrors existing chronological section-header style at lines 11-14, 19-22, 55-57, 130-133, 147):

```
/* === Phase 1 (v1.4) — WDG-04 Focus-Trapped Modal ===
   Reusable admin confirm-dialog primitive on native HTML <dialog>.
   Consumers: AdminFeedback Detail v1.4 Phase 1; reusable by Phase 7 ContentSources delete.
   Scope discipline: every selector below is prefixed `.admin-modal*` or `dialog.admin-modal::backdrop`
   so admin standalone load (no site-common inheritance) cannot bleed into 22 guild themes (PITFALLS.md P10). */
```

End the new block with a matching END marker on its own line (REQUIRED — the scope-discipline `awk` gate in `<verify>` extracts the section between these two markers; without the end marker, the gate cannot bound the new section and degrades to whole-file grep, which trips on pre-existing :root + grid-template lines):

```
/* === END Phase 1 === */
```

Then add the following rules (values from UI-SPEC §"Acceptance" table — copy verbatim, do NOT re-derive):

1. **Unified danger class + filter-cascade neutralizer** (RESEARCH Open Decisions §2 + §3 — closes the latent dead `.danger` on the in-page Delete button AND covers the modal danger variant; MEDIUM #2 revision per REVIEWS.md closes the existing `.admin-action-form button:hover { filter: brightness(1.1); }` cascade that would otherwise render danger hover as `#dc2626 + brightness(1.1)` instead of the spec'd `#b91c1c`):
   ```
   /* Background color — base + hover, applies to both in-page .danger and modal --danger. */
   .admin-shell .admin-action-form button.danger,
   .admin-shell .admin-modal__button--danger {
     background: #dc2626;
   }
   .admin-shell .admin-action-form button.danger:hover,
   .admin-shell .admin-modal__button--danger:hover {
     background: #b91c1c;
     /* Neutralize the brightness filter inherited from .admin-action-form button:hover (admin.css:138)
        and from .admin-modal__button--confirm:hover (added below). Without `filter: none`, the danger
        hover would render as #dc2626 + brightness(1.1), NOT the UI-SPEC #b91c1c. MEDIUM #2 fix. */
     filter: none;
   }
   ```
   NOTE: this is the ONLY part of the new block that uses the `.admin-shell` parent selector (because it touches an existing in-page button). All subsequent modal-only rules use `.admin-modal*` prefix per UI-SPEC §"CSS Class Naming" scoping discipline.

2. **Dialog element + backdrop:**
   - `.admin-modal { padding: 0; border: 1px solid var(--border); border-radius: 4px; background: var(--panel); color: var(--text); width: min(480px, calc(100vw - 32px)); max-width: 480px; min-width: 280px; max-height: min(80vh, 600px); box-shadow: 0 12px 32px rgba(0, 0, 0, 0.5); opacity: 1; transition: opacity 120ms ease-out, transform 120ms ease-out; }`
   - `dialog.admin-modal::backdrop { background: rgba(15, 23, 42, 0.72); transition: opacity 120ms ease-out; }`

3. **Panel (inner wrapper):**
   - `.admin-modal__panel { padding: 24px; max-height: min(80vh, 600px); overflow-y: auto; }`

4. **Title:**
   - `.admin-modal__title { margin: 0 0 16px 0; font-size: 18px; font-weight: 600; line-height: 1.3; text-wrap: balance; color: var(--text); }`

5. **Message:**
   - `.admin-modal__message { margin: 0 0 24px 0; font-size: 15px; line-height: 1.5; color: var(--text); word-wrap: break-word; overflow-wrap: anywhere; }`

6. **Actions container:**
   - `.admin-modal__actions { display: flex; gap: 8px; justify-content: flex-end; }`

7. **Base button:**
   - `.admin-modal__button { min-height: 44px; min-width: 44px; padding: 0 16px; border-radius: 3px; border: 1px solid transparent; font-size: 13px; font-weight: 600; line-height: 1.2; white-space: normal; cursor: pointer; transition: background-color 120ms ease, border-color 120ms ease; }`

8. **Cancel button (ghost / outlined):**
   - `.admin-modal__button--cancel { background: transparent; color: var(--text); border-color: var(--border); }`
   - `.admin-modal__button--cancel:hover { background: rgba(255, 255, 255, 0.04); }`

9. **Confirm button (accent / non-destructive default):**
   - `.admin-modal__button--confirm { background: var(--accent); color: #ffffff; border-color: transparent; }`
   - `.admin-modal__button--confirm:not(.admin-modal__button--danger):hover { filter: brightness(1.1); }`

   The `:not(.admin-modal__button--danger)` selector exclusion is MEDIUM #2 belt-and-suspenders: even though step 1's `.admin-modal__button--danger:hover { filter: none }` would unset it via source-order, scoping the brightness selector here AS WELL makes the cascade intent explicit and prevents a future Phase 7 author from accidentally re-applying brightness to the danger variant by reordering rules. The `.admin-modal__button--danger` BACKGROUND override from step 1 still wins via source-order when both `--confirm` and `--danger` classes are present on the confirm button.

10. **Reduced-motion gate** (admin standalone — does NOT inherit site-common.css reduced-motion; must re-declare locally per UI-SPEC §"Animation + timing" line 200-211 + the existing admin.css precedent of re-declaring `:focus-visible` because admin standalone):
   ```
   @media (prefers-reduced-motion: reduce) {
     .admin-modal,
     dialog.admin-modal::backdrop {
       transition: none;
     }
   }
   ```

Constraints (CLAUDE.md R-6 formatting paranoia):
- 2-space indent throughout the new block.
- LF line endings; final newline preserved.
- DO NOT run Format Document on admin.css.
- DO NOT touch any line before the new block.
- DO NOT add `:focus-visible` overrides — the existing rule at admin.css:23-32 already covers all buttons via the inherited `button:focus-visible` selector.
- DO NOT add `--admin-danger` token to `:root` — Phase 3 owns admin-common.css factoring per D-05; Phase 1 hard-codes `#dc2626` / `#b91c1c` (RESEARCH §"Color" line 92 rationale).
- DO NOT use bare element selectors at top level (`dialog { … }`, `button { … }`, `h2 { … }`) — only `dialog.admin-modal::backdrop` is permitted because it is class-anchored.
  </action>
  <verify>
    <automated>Run ALL 28 anchored grep assertions from UI-SPEC §"Acceptance" (lines 372-401) against the updated admin.css. Specifically:

# Panel surface
grep -cE 'admin-modal' DeckFlow.Web/wwwroot/css/admin.css (expect &gt;=10 — multiple selector lines)
grep -cE 'var\(--panel\)' DeckFlow.Web/wwwroot/css/admin.css (expect &gt;=2 — pre-existing + new)
grep -cE 'border-radius:\s*4px' DeckFlow.Web/wwwroot/css/admin.css (expect &gt;=2 — admin-banner pre-existing + admin-modal new)
grep -cE 'padding:\s*24px' DeckFlow.Web/wwwroot/css/admin.css (expect &gt;=1)
grep -cE 'max-width:\s*480px' DeckFlow.Web/wwwroot/css/admin.css (expect 1)
grep -cE 'max-height:\s*min\(80vh,\s*600px\)' DeckFlow.Web/wwwroot/css/admin.css (expect &gt;=1)
grep -cE '^[[:space:]]+background:[[:space:]]*#dc2626' DeckFlow.Web/wwwroot/css/admin.css (expect 1 — danger bg declaration only; excludes comment-text mentions per Codex re-review NEW HIGH)
grep -cE '^[[:space:]]+background:[[:space:]]*#b91c1c' DeckFlow.Web/wwwroot/css/admin.css (expect 1 — danger hover bg declaration only)
# Filter-cascade fix (MEDIUM #2 per REVIEWS.md)
grep -cE '\.admin-action-form button\.danger:hover' DeckFlow.Web/wwwroot/css/admin.css (expect &gt;=1 — in-page danger hover selector)
grep -cE '\.admin-modal__button--danger:hover' DeckFlow.Web/wwwroot/css/admin.css (expect &gt;=1 — modal danger hover selector)
grep -cE 'filter:[[:space:]]*none' DeckFlow.Web/wwwroot/css/admin.css (expect &gt;=1 — neutralizes inherited brightness on danger)
grep -cE ':not\(\.admin-modal__button--danger\):hover' DeckFlow.Web/wwwroot/css/admin.css (expect &gt;=1 — confirm-hover excludes danger variant)
grep -cE 'admin-modal__button--confirm' DeckFlow.Web/wwwroot/css/admin.css (expect &gt;=1)
grep -cE 'rgba\(15,\s*23,\s*42,\s*0\.72\)' DeckFlow.Web/wwwroot/css/admin.css (expect 1)

# Typography
grep -cE 'font-size:\s*18px' DeckFlow.Web/wwwroot/css/admin.css (expect &gt;=1 — title)
grep -cE 'font-weight:\s*600' DeckFlow.Web/wwwroot/css/admin.css (expect &gt;=3 — pre-existing + title + button)
grep -cE 'line-height:\s*1\.3' DeckFlow.Web/wwwroot/css/admin.css (expect 1)
grep -cE 'text-wrap:\s*balance' DeckFlow.Web/wwwroot/css/admin.css (expect 1)
grep -cE 'font-size:\s*15px' DeckFlow.Web/wwwroot/css/admin.css (expect &gt;=2 — body baseline + message)
grep -cE 'line-height:\s*1\.5' DeckFlow.Web/wwwroot/css/admin.css (expect &gt;=2 — body baseline + message)
grep -cE 'font-size:\s*13px' DeckFlow.Web/wwwroot/css/admin.css (expect &gt;=2 — pre-existing .admin-action-form button + new modal button)
grep -cE 'min-height:\s*44px' DeckFlow.Web/wwwroot/css/admin.css (expect 1)
grep -cE 'min-width:\s*44px' DeckFlow.Web/wwwroot/css/admin.css (expect 1)
grep -cE 'border-radius:\s*3px' DeckFlow.Web/wwwroot/css/admin.css (expect &gt;=2 — pre-existing button + new modal button)

# Layout
grep -cE 'gap:\s*8px' DeckFlow.Web/wwwroot/css/admin.css (expect &gt;=1)
grep -cE 'justify-content:\s*flex-end' DeckFlow.Web/wwwroot/css/admin.css (expect 1)
grep -cE '0 12px 32px rgba\(0,\s*0,\s*0,\s*0\.5\)' DeckFlow.Web/wwwroot/css/admin.css (expect 1)

# Animation
grep -cE '120ms ease-out' DeckFlow.Web/wwwroot/css/admin.css (expect &gt;=2 — dialog transition + backdrop transition)
grep -cE 'prefers-reduced-motion' DeckFlow.Web/wwwroot/css/admin.css (expect 1)

# Scope discipline (anti-bleed) — HIGH #1 revision per REVIEWS.md
# Extract ONLY the new Phase 1 section (bounded by the two markers added to Task 3 action), then
# inspect that subset for top-level bare-element selectors. The previous whole-file grep was
# guaranteed-fail because pre-existing :root, [role="tab"]:focus-visible, grid-template values,
# and multiline comment bodies all matched.
awk '/^\/\* === Phase 1 \(v1\.4\) — WDG-04 Focus-Trapped Modal ===/,/^\/\* === END Phase 1 === \*\//' DeckFlow.Web/wwwroot/css/admin.css > /tmp/phase-1-section.css
test -s /tmp/phase-1-section.css || (echo "FAIL: section markers missing or empty" &amp;&amp; exit 1)
# Top-level bare-element selectors (line begins with a letter — NOT . @ } space tab) must be ZERO.
# Allowed: lines starting with `.admin-` (class selectors), `@media` (at-rules), `dialog.admin-modal` (class-anchored pseudo), whitespace, `/* */` comments, declarations indented inside blocks.
grep -cE '^[a-z][a-z0-9_-]*[[:space:]]*\{' /tmp/phase-1-section.css   # expect 0 — no bare element top-level selectors like `button {`, `dialog {`, `h2 {`
grep -cE '^(button|h[1-6]|div|p|input|select|textarea|a|ul|ol|li|table|tr|td|th)[[:space:],{]' /tmp/phase-1-section.css   # expect 0 — explicit element-name allowlist check
# Class-anchored `dialog.admin-modal::backdrop` IS allowed because the element name is qualified by a class. Verify it appears:
grep -cE '^dialog\.admin-modal::backdrop' /tmp/phase-1-section.css   # expect &gt;=1 (the backdrop rule is present)

# Build gate
/mnt/c/Program\ Files/dotnet/dotnet.exe build DeckFlow.Web/DeckFlow.Web.csproj -c Release — MUST be clean (0 errors, 0 new warnings).</automated>
  </verify>
  <acceptance_criteria>
    - File `DeckFlow.Web/wwwroot/css/admin.css` line count increases by exactly the new block (no edits to lines 1-193).
    - All 25+ anchored grep assertions above return the expected counts.
    - Section header comment `/* === Phase 1 (v1.4) — WDG-04 Focus-Trapped Modal ===` is present at the start of the new block.
    - Matching END marker `/* === END Phase 1 === */` is present on its own line at the bottom of the new block (REQUIRED by the HIGH #1 scope-discipline awk gate).
    - Exactly one unified danger rule covering both `.admin-shell .admin-action-form button.danger` AND `.admin-shell .admin-modal__button--danger` selectors.
    - The unified danger HOVER rule contains `filter: none;` to neutralize the inherited `.admin-action-form button:hover { filter: brightness(1.1) }` cascade from `admin.css:138` (MEDIUM #2 per REVIEWS.md — without it, danger hover renders `#dc2626 + brightness(1.1)` instead of `#b91c1c`).
    - The confirm-hover brightness selector is `.admin-modal__button--confirm:not(.admin-modal__button--danger):hover` (NOT bare `.admin-modal__button--confirm:hover`) — belt-and-suspenders against the danger-variant cascade.
    - `grep -cE "\.admin-action-form button\.danger:hover" DeckFlow.Web/wwwroot/css/admin.css` ≥ 1.
    - `grep -cE "\.admin-modal__button--danger:hover" DeckFlow.Web/wwwroot/css/admin.css` ≥ 1.
    - `grep -cE "filter:[[:space:]]*none" DeckFlow.Web/wwwroot/css/admin.css` ≥ 1 AND the matching context is the unified danger hover rule (manual review confirms).
    - `grep -cE ":not\(\.admin-modal__button--danger\):hover" DeckFlow.Web/wwwroot/css/admin.css` ≥ 1 (confirm-hover exclusion is present).
    - Reduced-motion `@media` block is present, scoped to `.admin-modal` + `dialog.admin-modal::backdrop` (not global).
    - No bare element selectors added at top level INSIDE THE NEW Phase 1 SECTION ONLY (verified via `awk` section extraction in `<verify>` — pre-existing top-level constructs like `:root`, `html, body`, `a`, `* { box-sizing }` are out of scope for this gate because they are not in the new block).
    - LF line endings preserved (file does not contain CRLF).
    - `dotnet build DeckFlow.Web -c Release` from clean obj/ produces 0 errors and 0 new warnings.
  </acceptance_criteria>
  <done>
    admin.css contains the new Phase 1 modal block at end-of-file; all grep assertions pass; build clean.
  </done>
</task>

<task type="auto">
  <name>Task 4: Wire admin-feedback.ts to intercept delete form submit (additive — preserve existing IIFE)</name>
  <files>DeckFlow.Web/wwwroot/ts/admin-feedback.ts</files>
  <read_first>
    - DeckFlow.Web/wwwroot/ts/admin-feedback.ts (full 27 lines — existing IIFE shape, DOMContentLoaded wrapping, data-admin-feedback-submit-on-change handler that MUST be preserved unchanged)
    - .planning/phases/01-wdg-04-focus-trapped-modal/01-RESEARCH.md §"Consumer wire-up" (lines 654-680) + §"Consumer pattern in admin-feedback.ts" (lines 207-225) — reference implementation
    - .planning/phases/01-wdg-04-focus-trapped-modal/01-CONTEXT.md D-01, D-07
    - DeckFlow.Web/wwwroot/ts/admin-modal.ts (created in Task 1 — confirm the ConfirmOptions shape and `window.DeckFlowAdminModal.showConfirm` global)
  </read_first>
  <action>
ADDITIVELY edit `DeckFlow.Web/wwwroot/ts/admin-feedback.ts`. Do NOT rewrite or reformat any existing line. The existing `data-admin-feedback-submit-on-change` handler at lines 12-24 MUST stay byte-identical.

Inside the existing IIFE (after the existing `document.addEventListener('DOMContentLoaded', ...)` block — append a SECOND `document.addEventListener('DOMContentLoaded', ...)` block OR add a second `querySelectorAll(...).forEach(...)` block inside the existing DOMContentLoaded handler — pick whichever produces the smaller diff. Recommended: append a SECOND DOMContentLoaded handler so the existing 12-line block is untouched):

```typescript
  document.addEventListener('DOMContentLoaded', () => {
    const deleteForms = document.querySelectorAll<HTMLFormElement>('[data-admin-confirm-delete]');
    deleteForms.forEach((form) => {
      form.addEventListener('submit', async (event: SubmitEvent) => {
        event.preventDefault();
        const showConfirm = window.DeckFlowAdminModal?.showConfirm;
        if (showConfirm === undefined) {
          // admin-modal.js not loaded — fail closed (do NOT submit silently).
          return;
        }
        const id = form.dataset.adminFeedbackId ?? '?';
        const confirmed = await showConfirm({
          title: 'Delete Feedback',
          message: `Delete feedback #${id} permanently?`,
          confirmLabel: 'Delete',
          danger: true,
        });
        if (confirmed) {
          form.submit();
        }
      });
    });
  });
```

Key requirements:
- The `window.DeckFlowAdminModal` global type is available because admin-modal.ts (Task 1) declaration-merges `interface Window` at file-top — TypeScript's strict mode honors this across all `.ts` files compiled in the same project.
- `event.preventDefault()` MUST run BEFORE the await — otherwise the default form submission races the confirm dialog.
- `showConfirm === undefined` check is a defensive guard (RESEARCH §"Consumer pattern" line 212) — if admin-modal.js failed to load OR loaded out of order, the delete is blocked (fail-closed) rather than silently submitting.
- `form.submit()` (the native HTMLFormElement.submit, NOT `event.target.submit()`) bypasses the submit-event listener — won't re-trigger our handler, won't re-show the modal.
- The `id` interpolation in the message string is safe because admin-modal.ts uses `textContent` (Task 1 step 3) — no HTML injection risk.
- Copy values per CONTEXT D-07 example: `title: 'Delete Feedback'`, `confirmLabel: 'Delete'`, `danger: true`. Message preserves the verbatim copy from the existing `onsubmit="return confirm('Delete feedback #@Model.Id permanently?')` per UI-SPEC §"Copywriting Contract" line 105 (operator muscle-memory preserved).

Formatting (CLAUDE.md R-6):
- 2-space indent (matches existing file).
- LF line endings.
- Do NOT modify any line in 1-26 of the existing file.
- The new block appends BEFORE the closing `})();` of the existing IIFE wrapper.
  </action>
  <verify>
    <automated>Pre-edit baseline: capture existing line count with `wc -l DeckFlow.Web/wwwroot/ts/admin-feedback.ts`. Post-edit: line count should be approximately +25 (no existing lines deleted).

grep -c "data-admin-confirm-delete" DeckFlow.Web/wwwroot/ts/admin-feedback.ts (expect 1)
grep -c "data-admin-feedback-submit-on-change" DeckFlow.Web/wwwroot/ts/admin-feedback.ts (expect 1 — PRESERVED, not removed)
grep -c "window.DeckFlowAdminModal" DeckFlow.Web/wwwroot/ts/admin-feedback.ts (expect 1)
grep -c "event.preventDefault()" DeckFlow.Web/wwwroot/ts/admin-feedback.ts (expect 1)
grep -c "Delete Feedback" DeckFlow.Web/wwwroot/ts/admin-feedback.ts (expect 1)
grep -c "confirmLabel: 'Delete'" DeckFlow.Web/wwwroot/ts/admin-feedback.ts (expect 1)
grep -c "danger: true" DeckFlow.Web/wwwroot/ts/admin-feedback.ts (expect 1)
grep -c "form.submit()" DeckFlow.Web/wwwroot/ts/admin-feedback.ts (expect 1)
grep -c "import " DeckFlow.Web/wwwroot/ts/admin-feedback.ts (expect 0 — module:"none")
git diff --stat DeckFlow.Web/wwwroot/ts/admin-feedback.ts — should show only insertions, zero deletions in the original lines.

Build: /mnt/c/Program\ Files/dotnet/dotnet.exe build DeckFlow.Web/DeckFlow.Web.csproj -c Release MUST be clean (0 errors, 0 new TS warnings). Emitted `wwwroot/js/admin-feedback.js` must be regenerated with the new handler.</automated>
  </verify>
  <acceptance_criteria>
    - Existing `data-admin-feedback-submit-on-change` handler (lines 12-24 in original) is byte-identical post-edit.
    - New handler block exists; intercepts submit on `[data-admin-confirm-delete]` forms.
    - Calls `event.preventDefault()` BEFORE reading `window.DeckFlowAdminModal?.showConfirm`.
    - Defensive `if (showConfirm === undefined) return;` guard present.
    - Confirm copy matches `{ title: 'Delete Feedback', message: \`Delete feedback #${id} permanently?\`, confirmLabel: 'Delete', danger: true }` per D-07 + UI-SPEC.
    - On confirmation, calls `form.submit()` (native, bypasses submit-event listener — no re-entry).
    - File ends inside the existing IIFE closing `})();`.
    - No `import`/`export` keywords added.
    - TypeScript compiles via MSBuild clean; `wwwroot/js/admin-feedback.js` regenerated.
    - `git diff` shows only insertions in the new block; zero modifications to lines 1-26.
  </acceptance_criteria>
  <done>
    admin-feedback.ts compiles with the new handler; existing handler preserved verbatim; build clean; emitted JS regenerated.
  </done>
</task>

<task type="auto">
  <name>Task 5: Wire Detail.cshtml — remove onsubmit, add partial include, add Scripts section</name>
  <files>DeckFlow.Web/Views/AdminFeedback/Detail.cshtml</files>
  <read_first>
    - DeckFlow.Web/Views/AdminFeedback/Detail.cshtml (full 46 lines — current state has deferred onsubmit at line 41)
    - DeckFlow.Web/Views/AdminFeedback/Index.cshtml lines 100-103 (canonical `@section Scripts { <script src="~/js/...js" asp-append-version="true"></script> }` pattern)
    - .planning/phases/01-wdg-04-focus-trapped-modal/01-RESEARCH.md §"Razor PartialAsync call site in Detail.cshtml" (lines 284-314) + §"Script include order" (lines 236-248)
    - .planning/phases/01-wdg-04-focus-trapped-modal/01-CONTEXT.md D-04, D-08, D-09 (NO scope creep)
    - .editorconfig (4-space indent for .cshtml)
  </read_first>
  <action>
Surgical edits to `DeckFlow.Web/Views/AdminFeedback/Detail.cshtml`. Touch only the lines below; preserve all other lines byte-identically (CLAUDE.md "touch only lines that need touching").

EDIT 1 — Delete the comment at line 39 (the deferred-confirm Phase 11 D-05 comment):
```
@* Deferred: inline onsubmit confirm() retained per Phase 11 D-05; v1.4 will replace with a styled focus-trapped modal. See .planning/quick/260513-wdg-web-design-guidelines-audit-findings/260513-wdg-FINDINGS.md finding D + BB. *@
```
Remove the entire single-line comment. The deferred-state rationale is no longer applicable — Phase 1 IS the replacement.

EDIT 2 — Replace lines 40-44 (the delete form). The CURRENT block is:
```
<form method="post" asp-action="Apply" asp-route-id="@Model.Id" asp-route-op="delete" class="admin-action-form"
      onsubmit="return confirm('Delete feedback #@Model.Id permanently?');">
    @Html.AntiForgeryToken()
    <button type="submit" class="danger">Delete</button>
</form>
```

Replace with:
```
<form method="post" asp-action="Apply" asp-route-id="@Model.Id" asp-route-op="delete"
      class="admin-action-form" data-admin-confirm-delete data-admin-feedback-id="@Model.Id">
    @Html.AntiForgeryToken()
    <button type="submit" class="danger">Delete</button>
</form>
```

Changes:
- REMOVED: `onsubmit="return confirm('Delete feedback #@Model.Id permanently?');"` attribute (CSP-clean — no inline JS).
- ADDED: `data-admin-confirm-delete` attribute — admin-feedback.ts hook.
- ADDED: `data-admin-feedback-id="@Model.Id"` attribute — passes Id to TS without inline `@`-interpolation in a `<script>` block.
- PRESERVED: `@Html.AntiForgeryToken()` (CSRF discipline per RESEARCH Pitfall 5 — the existing token MUST remain in the POST body for `[ValidateAntiForgeryToken]` on `AdminFeedbackController.Apply`).
- PRESERVED: `class="danger"` on the button (now the unified danger CSS rule from Task 3 actually styles it).
- PRESERVED: `type="submit"` on the button (admin-feedback.ts intercepts the submit event).

EDIT 3 — Add partial include immediately before the closing `</section>` tag (currently line 46). Insert on a new line at 4-space indent:
```
    @await Html.PartialAsync("_AdminConfirmModal")
```

This renders the singleton dialog DOM template once per page so admin-modal.ts can populate + showModal() on demand.

EDIT 4 — Append a `@section Scripts` block at the very end of the file (after the closing `</section>` tag, on new lines):
```
@section Scripts
{
    <script src="~/js/admin-modal.js" asp-append-version="true"></script>
    <script src="~/js/admin-feedback.js" asp-append-version="true"></script>
}
```

CRITICAL ORDER: `admin-modal.js` MUST precede `admin-feedback.js` (RESEARCH Pitfall 4). The first script declares `window.DeckFlowAdminModal.showConfirm`; the second reads it. Reverse order causes the defensive `if (showConfirm === undefined) return;` guard to fire and silently break the Delete button.

DO NOT TOUCH:
- Lines 1-38 (model declaration, ViewData, section open, Back link, h1, dl, h2, pre, detail-actions open, Mark Read form, Archive form).
- Line 45 (the closing `</section>` tag stays where it is — partial include goes BEFORE it).
- The `Mark Read` form (lines 27-30) and `Archive` form (lines 34-37) — they have no `onsubmit` (no confirm needed; they are non-destructive).
- The page-level chrome: `_AdminLayout.cshtml` is OUT OF SCOPE (CONTEXT D-09 "Files NOT to touch" + RESEARCH Pitfall 7).

LF line endings throughout. 4-space indent per `.editorconfig`. Do NOT run Format Document.
  </action>
  <verify>
    <automated>grep -c 'onsubmit' DeckFlow.Web/Views/AdminFeedback/Detail.cshtml (expect 0 — REMOVED)
grep -c 'data-admin-confirm-delete' DeckFlow.Web/Views/AdminFeedback/Detail.cshtml (expect 1)
grep -c 'data-admin-feedback-id="@Model.Id"' DeckFlow.Web/Views/AdminFeedback/Detail.cshtml (expect 1)
grep -c '@Html.AntiForgeryToken()' DeckFlow.Web/Views/AdminFeedback/Detail.cshtml (expect 3 — Mark Read + Archive + Delete forms, all preserved)
grep -c '_AdminConfirmModal' DeckFlow.Web/Views/AdminFeedback/Detail.cshtml (expect 1 — partial include)
grep -c 'admin-modal.js' DeckFlow.Web/Views/AdminFeedback/Detail.cshtml (expect 1)
grep -c 'admin-feedback.js' DeckFlow.Web/Views/AdminFeedback/Detail.cshtml (expect 1)
grep -c '@section Scripts' DeckFlow.Web/Views/AdminFeedback/Detail.cshtml (expect 1)

# Script load order check — admin-modal.js line number MUST be less than admin-feedback.js line number
MODAL_LINE=$(grep -n 'admin-modal.js' DeckFlow.Web/Views/AdminFeedback/Detail.cshtml | head -1 | cut -d: -f1)
FEEDBACK_LINE=$(grep -n 'admin-feedback.js' DeckFlow.Web/Views/AdminFeedback/Detail.cshtml | head -1 | cut -d: -f1)
[ "$MODAL_LINE" -lt "$FEEDBACK_LINE" ] || (echo "FAIL: script order wrong" &amp;&amp; exit 1)

# Build gate
/mnt/c/Program\ Files/dotnet/dotnet.exe build DeckFlow.Web/DeckFlow.Web.csproj -c Release MUST be clean.</automated>
  </verify>
  <acceptance_criteria>
    - Zero occurrences of `onsubmit` in `Detail.cshtml`.
    - Exactly one `data-admin-confirm-delete` attribute on the delete form.
    - Exactly one `data-admin-feedback-id="@Model.Id"` attribute on the delete form.
    - All three `@Html.AntiForgeryToken()` invocations preserved (Mark Read, Archive, Delete forms).
    - Exactly one `@await Html.PartialAsync("_AdminConfirmModal")` invocation before `</section>`.
    - `@section Scripts` block exists at file-end with both script tags using `asp-append-version="true"`.
    - `admin-modal.js` `<script>` precedes `admin-feedback.js` `<script>` in DOM order.
    - The deferred-confirm comment at original line 39 is removed.
    - The non-destructive Mark Read + Archive forms at original lines 27-37 are byte-identical (NOT touched).
    - `_AdminLayout.cshtml` shows zero changes in `git diff` for this task.
    - Razor compiles via MSBuild; build clean.
  </acceptance_criteria>
  <done>
    Detail.cshtml has new wire-up; build clean; all grep + ordering assertions pass; git diff scope confirmed (Detail.cshtml only, not _AdminLayout.cshtml).
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 6: Manual UAT — verify modal lifecycle end-to-end</name>
  <what-built>
    Phase 1 has shipped the reusable admin confirm modal: a native `<dialog>` opened via `showModal()` from `admin-modal.ts`, populated via `textContent`, with hand-rolled backdrop close + defense-in-depth focus restore. AdminFeedback Detail's Delete button now routes through the modal before the POST. CSS is scoped to `.admin-modal*` and `dialog.admin-modal::backdrop`; admin shell standalone (no guild-theme bleed risk). Build is clean; all greps + ordering assertions in Tasks 1-5 pass.
  </what-built>
  <how-to-verify>
    User starts the dev server (per MEMORY `feedback_user_starts_server`; never auto-launch): from a Windows shell run `& "C:\Program Files\dotnet\dotnet.exe" run --project DeckFlow.Web`, or from WSL run `scripts/run-web.sh`. Authenticate via BasicAuth at `/Admin/Feedback`, click any feedback row to reach `/Admin/Feedback/Detail/{id}`.

    **UAT-1 — Modal opens via showModal() (SC #1):** Click the red Delete button. EXPECT: styled dark modal appears centered on screen with title "Delete Feedback", message "Delete feedback #{id} permanently?", a `[Cancel]` (outlined) button on the left, a red `[Delete]` button on the right. Initial focus on Cancel. DevTools Elements panel shows `<dialog id="admin-confirm-modal" open>` elevated in the top layer. DevTools Network panel shows NO request for any focus-trap npm package — only `admin.css`, `admin-modal.js`, `admin-feedback.js`.

    **UAT-2 — ESC closes (SC #3):** With modal open, press Escape. EXPECT: modal closes, focus returns to the Delete button on the page, NO POST request fires in Network panel.

    **UAT-3 — Backdrop click closes (SC #3 + D-03):** Click Delete → modal opens. Click anywhere in the dimmed area OUTSIDE the modal panel. EXPECT: modal closes, focus returns to Delete button, NO POST fires. Then click Delete again → modal opens → click WITHIN the panel (on title or message text) → EXPECT: modal stays open (backdrop discrimination working).

    **UAT-4 — Tab cycle stays inside modal (SC #2):** Click Delete → modal opens. Press Tab repeatedly. EXPECT: focus cycles Cancel → Delete → Cancel → Delete (or includes dialog itself per browser quirk); focus NEVER lands on the underlying Back link, Mark Read, Archive, or other page buttons. Press Shift+Tab — EXPECT: reverse cycle stays inside modal. DevTools console: `document.activeElement` always returns a descendant of `<dialog id="admin-confirm-modal">`.

    **UAT-5 — Cancel button closes (SC #3):** Click Delete → modal opens. Click Cancel. EXPECT: modal closes, focus returns to Delete button, NO POST.

    **UAT-6 — Confirm actually deletes (functional happy path + CSRF preservation):** Click Delete → modal opens → click the red Delete (modal confirm). EXPECT: modal closes; Network panel shows `POST /Admin/Feedback/Apply/{id}?op=delete` with form body containing `__RequestVerificationToken` (CSRF token preserved per RESEARCH Pitfall 5). Page redirects to `/Admin/Feedback`; the deleted feedback row no longer appears in the list.

    **UAT-7 — Zero CSS bleed into guild themes (SC #4):** Navigate to homepage `/` rendered in Rakdos theme (or Azorius/Boros/Gruul — any guild theme). DevTools → Elements → search for any element matching `.admin-modal` — EXPECT: zero matches (no `.admin-shell` on public pages, so the partial is absent). Computed panel on `<body>` and `<button>` — EXPECT: no inherited `border-radius: 4px` or `box-shadow: 0 12px 32px ...` properties from the new admin.css rules.

    **UAT-8 — Screen-reader smoke (optional, SC #2 a11y):** If NVDA (Windows) or VoiceOver (Mac) available, navigate to Delete button via Tab, press Enter. EXPECT: SR announces "Delete Feedback, dialog" (confirms `aria-labelledby` firing) and "Delete feedback #{id} permanently?" (confirms `aria-describedby`). SR ignores Mark Read / Archive buttons while modal open (page behind is inert per native `<dialog>` semantics).

    If ALL of UAT-1..7 pass, type "approved". If any fail, describe the failure (which UAT step, what was observed vs expected, browser + version) and execute-phase will revise.
  </how-to-verify>
  <resume-signal>Type "approved" if UAT-1..7 all pass, or describe failures.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → admin Razor view | Untrusted user input (Tab/Enter/click events) crosses into modal TS — handled via native `<dialog>` semantics + `textContent` (no `innerHTML`) |
| browser → AdminFeedbackController.Apply | Existing CSRF-protected boundary; Phase 1 PRESERVES the `@Html.AntiForgeryToken()` POST body — modal is a UX gate, not a security control |
| admin.css load → guild-theme pages | Existing isolation: admin.css is loaded ONLY by `_AdminLayout.cshtml`; Phase 1 maintains `.admin-modal*` scope discipline so even if admin.css were loaded elsewhere, no element-level selectors could bleed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering / Information Disclosure | `admin-modal.ts` title/message rendering | mitigate | Use `textContent` (NEVER `innerHTML`) for all four mutations (title, message, confirm label, cancel label) — XSS-safe by construction regardless of caller input. Verified by Task 1 acceptance: `grep -c innerHTML admin-modal.ts == 0` |
| T-01-02 | Tampering / Spoofing | Delete form CSRF token | mitigate | `@Html.AntiForgeryToken()` PRESERVED in `Detail.cshtml` Delete form post-refactor (RESEARCH Pitfall 5). UAT-6 explicitly verifies `__RequestVerificationToken` in POST body. Server-side `[ValidateAntiForgeryToken]` on `AdminFeedbackController.Apply` is the actual security boundary; modal is UX-only |
| T-01-03 | Tampering | Direct POST bypass of modal | accept | The modal is a UX guard, NOT a security control. Server-side anti-forgery token + BasicAuth on `/Admin/*` are the actual boundary; an attacker bypassing the modal via direct `curl` still must present a valid token + valid admin credentials. Risk = pre-existing baseline; modal does NOT widen it |
| T-01-04 | Information Disclosure / Tampering | CSP `script-src 'self'` posture | mitigate | Phase 1 REMOVES the inline `onsubmit="return confirm(...)"` handler (continues v1.3 WDG-04 CSP-cleanup lineage). All JS becomes external (`~/js/admin-modal.js`, `~/js/admin-feedback.js`). NO new inline `<script>` blocks. Improves CSP posture; does not regress it |
| T-01-05 | Denial of Service | Modal fail-closed when admin-modal.js missing | mitigate | Defensive `if (showConfirm === undefined) return;` in admin-feedback.ts — if admin-modal.js fails to load or loads out of order, the Delete button blocks entirely (no silent submission). UAT manual coverage; RESEARCH Pitfall 4 |
| T-01-06 | Tampering | CSS bleed into 22 guild themes | mitigate | All new selectors in admin.css are prefixed `.admin-modal*` (descendants) or `dialog.admin-modal::backdrop` (class-anchored pseudo) — zero bare-element selectors at top level. The unified danger rule uses `.admin-shell` parent. UAT-7 verifies zero modal-CSS bleed on guild-theme public pages. PITFALLS.md Pitfall 10 mitigation |
| T-01-07 | Tampering | `event.target.submit()` re-entry loop | mitigate | admin-feedback.ts calls `form.submit()` (native HTMLFormElement.submit), NOT `event.target.submit()` and NOT `form.dispatchEvent(new SubmitEvent(...))` — native submit bypasses the submit-event listener, so the handler cannot re-fire and the modal cannot loop |
| T-01-SC | Tampering | npm/NuGet install supply-chain | accept | Phase 1 installs ZERO new packages (RESEARCH §"Package Legitimacy Audit"). No npm focus-trap dep, no NuGet additions. Supply-chain surface unchanged from baseline; no slopcheck or `[ASSUMED]`/`[SUS]` package gates required |

**Severity:** All threats are low/info. Phase 1 is a UX-only refactor; the actual security boundary (CSRF token + BasicAuth + server-side authorization on `/Admin/*`) is unchanged. Modal failure modes (missing script, malformed partial, browser without `<dialog>` support) all fail closed — Delete is blocked rather than silently submitted.
</threat_model>

<verification>
## Automated Verification Gates (run after Task 5 completes)

**Gate 1 — File presence (all 5 artifacts):**
```
test -f DeckFlow.Web/wwwroot/ts/admin-modal.ts
test -f DeckFlow.Web/Views/Shared/_AdminConfirmModal.cshtml
test -f DeckFlow.Web/wwwroot/css/admin.css
test -f DeckFlow.Web/wwwroot/ts/admin-feedback.ts
test -f DeckFlow.Web/Views/AdminFeedback/Detail.cshtml
```

**Gate 2 — All 28 anchored greps from UI-SPEC §"Acceptance" (lines 372-401):**
See Task 3 `<verify>` block — comprehensive list. Each grep value comes from UI-SPEC verbatim.

**Gate 3 — Cross-file integration greps:**
```
# admin-modal.ts ↔ window contract
grep -c "window.DeckFlowAdminModal" DeckFlow.Web/wwwroot/ts/admin-modal.ts   # expect &gt;=2
grep -c "window.DeckFlowAdminModal" DeckFlow.Web/wwwroot/ts/admin-feedback.ts # expect 1

# Partial ↔ TS DOM contract
grep -c 'id="admin-confirm-modal"' DeckFlow.Web/Views/Shared/_AdminConfirmModal.cshtml  # expect 1
grep -c "'#admin-confirm-modal'" DeckFlow.Web/wwwroot/ts/admin-modal.ts                 # expect 1
grep -c 'id="admin-modal-title"' DeckFlow.Web/Views/Shared/_AdminConfirmModal.cshtml    # expect 1
grep -c 'id="admin-modal-message"' DeckFlow.Web/Views/Shared/_AdminConfirmModal.cshtml  # expect 1
grep -c 'data-admin-modal-cancel' DeckFlow.Web/Views/Shared/_AdminConfirmModal.cshtml   # expect 1
grep -c 'data-admin-modal-confirm' DeckFlow.Web/Views/Shared/_AdminConfirmModal.cshtml  # expect 1

# Detail.cshtml ↔ admin-feedback.ts DOM contract
grep -c 'data-admin-confirm-delete' DeckFlow.Web/Views/AdminFeedback/Detail.cshtml   # expect 1
grep -c 'data-admin-confirm-delete' DeckFlow.Web/wwwroot/ts/admin-feedback.ts        # expect 1
grep -c 'data-admin-feedback-id' DeckFlow.Web/Views/AdminFeedback/Detail.cshtml      # expect 1
grep -c 'adminFeedbackId' DeckFlow.Web/wwwroot/ts/admin-feedback.ts                  # expect 1

# Script load order in Detail.cshtml
MODAL_LINE=$(grep -n 'admin-modal.js' DeckFlow.Web/Views/AdminFeedback/Detail.cshtml | head -1 | cut -d: -f1)
FEEDBACK_LINE=$(grep -n 'admin-feedback.js' DeckFlow.Web/Views/AdminFeedback/Detail.cshtml | head -1 | cut -d: -f1)
[ "$MODAL_LINE" -lt "$FEEDBACK_LINE" ] || exit 1

# CSRF preservation
grep -c '@Html.AntiForgeryToken()' DeckFlow.Web/Views/AdminFeedback/Detail.cshtml   # expect 3 (Mark Read + Archive + Delete)

# onsubmit fully removed
grep -c 'onsubmit' DeckFlow.Web/Views/AdminFeedback/Detail.cshtml   # expect 0
```

**Gate 4 — Scope guard (out-of-bounds files MUST NOT change):**
```
git diff --name-only DeckFlow.Web/Views/Shared/_AdminLayout.cshtml   # expect empty (Phase 3 owns)
git diff --name-only DeckFlow.Web/Controllers/                       # expect empty
git diff --name-only DeckFlow.Web/Views/AdminFeedback/Index.cshtml   # expect empty
git diff --name-only DeckFlow.Web/package.json                       # expect empty (zero new deps)
git diff --name-only DeckFlow.Web/package-lock.json                  # expect empty
git diff --name-only DeckFlow.Web/wwwroot/css/site*.css              # expect empty (admin standalone)
git diff --name-only DeckFlow.Web/wwwroot/ts/deck-sync.ts            # expect empty (public-facing — out per D-09)
```

**Gate 5 — Build & test:**
```
# Clean rebuild from scratch
rm -rf DeckFlow.Web/obj DeckFlow.Web/bin
/mnt/c/Program\ Files/dotnet/dotnet.exe build DeckFlow.Web/DeckFlow.Web.csproj -c Release   # expect 0 errors, 0 new warnings
# Confirm both TS artifacts emitted
test -f DeckFlow.Web/wwwroot/js/admin-modal.js
test -f DeckFlow.Web/wwwroot/js/admin-feedback.js
# Full test suite (push-and-watch CI preferred per CLAUDE.md; if WSL VSTest is attempted, treat known WSL flakiness)
/mnt/c/Program\ Files/dotnet/dotnet.exe test DeckFlow.sln   # expect Failed: 0 (preserves 497-pass baseline)
```

## Manual UAT Gates (Task 6 — blocking checkpoint)

UAT-1 through UAT-7 (mandatory) + UAT-8 (optional SR smoke) per Task 6 `<how-to-verify>`. User runs in evergreen Chrome/Firefox/Safari per RESEARCH §"Browser baseline".

## Phase Gate

Phase 1 is COMPLETE only when ALL of the following are true:
- Gates 1-5 above PASS
- UAT-1..7 PASS (UAT-8 optional)
- `dotnet test` in push-and-watch CI shows Failed: 0 (preserves 497/3/500 baseline per STATE.md)
- `git diff` scope confirmed: only the 5 files listed in `files_modified` frontmatter + the 2 emitted `wwwroot/js/admin-*.js` artifacts have changed
</verification>

<success_criteria>
Phase 1 ships successfully when all four ROADMAP Phase 1 Success Criteria are observably TRUE:

1. **SC #1 (native dialog, no library):** Admin opens delete-confirm on `/Admin/Feedback/Detail/{id}` and the native `<dialog>` element opens via `showModal()`. No hand-rolled focus-trap library. No npm dependency added (verified by zero diff in `DeckFlow.Web/package.json` + `package-lock.json`). UAT-1 confirms.

2. **SC #2 (focus trap):** Tab/Shift+Tab cycle stays inside the modal across the two native button form controls (Cancel + Confirm) on `/Admin/Feedback/Detail/{id}`. Nested custom-element cycling (`df-select`/`df-typeahead`) is explicitly deferred to Phase 7 ContentSources delete-confirm reuse per CONTEXT D-09 + RESEARCH UAT-9 — Phase 7 will exercise nested cycling when the admin source-CRUD UI lands. Phase 1 verifies native button cycling only. UAT-4 confirms.

3. **SC #3 (close behaviors):** Escape closes the modal AND restores focus to the trigger button (UAT-2). Cancel button closes (UAT-5). Backdrop click closes (UAT-3 — note: hand-rolled per RESEARCH D-03 correction).

4. **SC #4 (CSS scope discipline):** New modal CSS lands in `wwwroot/css/admin.css` (per D-05; Phase 3 will extract during admin-common factoring). Uses existing `:root` tokens (`--bg`, `--panel`, `--text`, `--accent`, `--border`, `--focus`). Hard-codes `#dc2626`/`#b91c1c` for danger because admin-scope `--danger` is undefined per UI-SPEC line 92. Zero unscoped element selectors leaked to the 22 guild themes — verified by UAT-7 + scope-discipline grep.

All four SCs are gated on UAT-1..7 PASS + Gate 1-5 automated checks + push-CI `dotnet test` Failed: 0.
</success_criteria>

<output>
On phase completion:
1. Create `.planning/phases/01-wdg-04-focus-trapped-modal/01-01-SUMMARY.md` using `@$HOME/.claude/get-shit-done/templates/summary.md` with frontmatter:
   - `phase: 01-wdg-04-focus-trapped-modal`
   - `plan: 01`
   - `requirements-completed: [MODAL-01]` (R-2: roadmap checkbox auto-flips from this field)
   - `tasks-completed: 6` (5 implementation + 1 UAT checkpoint)
   - `files-changed: [...]` (the 5 from frontmatter + 2 emitted .js artifacts)
   - `commits: [...]` (one or more — recommend ONE atomic commit per CLAUDE.md "plain default-author commits, no Co-Authored-By" + R-6 formatting paranoia)
   - `uat-status: pass` (set after Task 6 approval)
2. After SUMMARY exists, the orchestrator will:
   - Auto-flip MODAL-01 to `[x]` in REQUIREMENTS.md (R-2 enforcement)
   - Update ROADMAP.md Phase 1 progress to `1/1 plans` + `[x]` checkbox
   - Update STATE.md current position + progress counters (R-1 enforcement via gsd-sdk verify-state)
3. Commit message format (plain default-author, no Co-Authored-By trailer):
   ```
   feat(01): WDG-04 focus-trapped admin confirm modal (MODAL-01)

   Replaces deferred inline onsubmit confirm in AdminFeedback Detail with
   a reusable native <dialog> + showModal() primitive scoped to .admin-shell.

   - admin-modal.ts: window.DeckFlowAdminModal.showConfirm IIFE
   - _AdminConfirmModal.cshtml: structural-only singleton partial
   - admin.css: Phase 1 modal block + unified .danger rule (closes latent dead class)
   - admin-feedback.ts: delete-form submit interceptor (additive)
   - Detail.cshtml: removes onsubmit, adds partial include + Scripts section

   Manual UAT 1-7 passed. Closes v1.3 WDG-04 carry-over.
   ```
</output>
