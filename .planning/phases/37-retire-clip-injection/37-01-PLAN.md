---
phase: 37-retire-clip-injection
plan: 01
type: execute
wave: 1
depends_on: []
autonomous: true
requirements: [RET-01, RET-02, RET-05]
files_modified:
  # Wave 0 — new test assertions (created/edited first)
  - DeckFlow.Web.Tests/AnalysisPromptVariantNoExpertContextTests.cs   # NEW (RET-01)
  - DeckFlow.Web.Tests/PacketLegacyZipBackCompatTests.cs              # NEW (RET-05)
  # Production removals (DELETE)
  - DeckFlow.Web/Services/ContentKbRelevanceService.cs                # DELETE (incl. IContentKbRelevanceService)
  - DeckFlow.Web/Services/ContentKbArchetypeDeriver.cs                # DELETE
  - DeckFlow.Web/Services/ContentKbClipSanitizer.cs                   # DELETE
  - DeckFlow.Web/Models/ContentKbExcerpt.cs                           # DELETE
  - DeckFlow.Web/Controllers/Api/ContentKbSearchApiController.cs      # DELETE
  - DeckFlow.Web/Views/Deck/_ContentKbPanel.cshtml                    # DELETE
  - DeckFlow.Web/wwwroot/ts/kb-selection.ts                           # DELETE
  - DeckFlow.Web/wwwroot/ts/content-kb-admin.ts                       # DELETE (entirely score-preview typeahead)
  # Production de-wire (EDIT)
  - DeckFlow.Web/Program.cs
  - DeckFlow.Web/Services/DeckAnalysisPacketService.cs
  - DeckFlow.Web/Services/PromptBuilders/Analysis/IAnalysisPromptVariant.cs
  - DeckFlow.Web/Services/PromptBuilders/Analysis/AnalysisPromptVariantRegistry.cs
  - DeckFlow.Web/Services/PromptBuilders/Analysis/ChatGptAnalysisPromptVariant.cs
  - DeckFlow.Web/Services/PromptBuilders/Analysis/ClaudeAnalysisPromptVariant.cs
  - DeckFlow.Web/Services/PromptBuilders/Analysis/GeminiAnalysisPromptVariant.cs
  - DeckFlow.Web/Models/DeckAnalysisRequest.cs
  - DeckFlow.Web/Models/DeckAnalysisViewModel.cs
  - DeckFlow.Web/Services/PacketArtifactStore.cs
  - DeckFlow.Web/Services/PacketSessionCache.cs
  - DeckFlow.Web/Controllers/DeckController.cs
  - DeckFlow.Web/Views/Deck/DeckAnalysis.cshtml
  # Admin score-preview removal — PULLED FORWARD (consumes IContentKbRelevanceService; must build-green WITH the interface deletion)
  - DeckFlow.Web/Controllers/Admin/AdminContentKbController.cs
  - DeckFlow.Web/Views/AdminContentKb/Index.cshtml
  - DeckFlow.Web/Models/AdminContentKbViewModel.cs
  # Browse-page selection-UI strip — PULLED FORWARD (references kb-selection.ts + the deleted typeahead API; must build-green WITH those deletes)
  - DeckFlow.Web/Views/ContentKb/Index.cshtml
  # Dead expert-selection CSS (full code removal per CONTEXT)
  - DeckFlow.Web/wwwroot/css/site-common.css
  # Test deletions/edits tied to this removal (DELETE/EDIT)
  - DeckFlow.Web.Tests/ContentKbRelevanceServiceTests.cs             # DELETE
  - DeckFlow.Web.Tests/ContentKbClipSanitizerTests.cs                # DELETE
  - DeckFlow.Web.Tests/ContentKbMergedClipsTests.cs                  # DELETE
  - DeckFlow.Web.Tests/ContentKbArchetypeDeriverTests.cs            # DELETE
  - DeckFlow.Web.Tests/ContentKbExcerptTests.cs                      # DELETE
  - DeckFlow.Web.Tests/Spike001KbValueAbHarness.cs                   # DELETE
  - DeckFlow.Web.Tests/AnalysisPromptVariantExpertContextTests.cs    # DELETE
  - DeckFlow.Web.Tests/DeckAnalysisPacketServiceExpertContextTests.cs # DELETE
  - DeckFlow.Web.Tests/DeckControllerTests.cs                        # EDIT
  - DeckFlow.Web.Tests/DeckAnalysisPacketServiceTests.cs            # EDIT
  - DeckFlow.Web.Tests/DeckAnalysisRequestTests.cs                  # EDIT
  - DeckFlow.Web.Tests/PacketArtifactStoreTests.cs                  # EDIT
  - DeckFlow.Web.Tests/AiPlatformExtensionTests.cs                  # EDIT
  - DeckFlow.Web.Tests/AdminContentKbControllerTests.cs             # EDIT (PULLED FORWARD — removes FakeContentKbRelevanceService + score-preview asserts)

must_haves:
  truths:
    - "A generated deck-analysis prompt for ChatGPT, Claude, and Gemini contains NO '## Expert Context' block"
    - "The DeckAnalysis page renders no expert-selection accordion and no 'What Experts Say' panel"
    - "The /content-kb browse page no longer renders the pin/follow tray, per-entry pin/follow buttons, the 'Run analysis' CTA, or the kb-selection script"
    - "The admin curation grid is intact but the relevance-score preview (controller/view/VM/TS) is gone, and IContentKbRelevanceService no longer exists"
    - "The solution builds at 0 warnings / 0 errors with no dead references to the removed types — at every task boundary"
    - "A pre-retire packet zip carrying ExpertSelectionJson / ExpertContextJson loads without throwing"
  artifacts:
    - path: "DeckFlow.Web.Tests/AnalysisPromptVariantNoExpertContextTests.cs"
      provides: "RET-01 regression: each of 3 variants' Build output omits '## Expert Context'"
    - path: "DeckFlow.Web.Tests/PacketLegacyZipBackCompatTests.cs"
      provides: "RET-05 regression: legacy zip with 32-/33- entries loads without exception"
  key_links:
    - from: "DeckAnalysisPacketService.BuildAnalysisPrompt"
      to: "IAnalysisPromptVariant.Build"
      via: "prompt-variant call with kbExcerpts param removed"
      pattern: "Build\\("
    - from: "PacketArtifactStore read path"
      to: "legacy zip entries 32-/33-"
      via: "allow-list filtered ReadEntries (ignores removed names gracefully)"
      pattern: "ReadEntries"
    - from: "AdminContentKbController ctor"
      to: "(removed IContentKbRelevanceService)"
      via: "ctor signature drops the relevance dependency in the SAME unit the interface is deleted"
      pattern: "AdminContentKbController\\("
---

<objective>
Fully remove the gate-condemned Content-KB clip-injection / expert-selection path from deck analysis AND every consumer of the removed types — including the admin relevance-score preview and the `/content-kb` browse-page selection UI — in ONE build-green wave (RET-01, RET-02), preserving pre-retire packet back-compat (RET-05). Because `IContentKbRelevanceService` is consumed by `AdminContentKbController` and the browse page references `kb-selection.ts` + the deleted typeahead API, those consumers are removed in the SAME wave as the interface/asset deletions so `dotnet build` is 0/0 at every task boundary.

Purpose: The Phase-35 5-deck blind value gate proved injection adds ~zero analysis value (0/5 decks changed a recommendation). Removing it fully (not disabling) clears dead expert-selection threading before the Phase 38 controller SRP split. A previous wave split (admin consumers in a later wave) would have broken the build at the Wave-1 commit (CS0246/CS0535) — this revision consolidates the interface deletion with ALL its consumers.
Output: A slimmer DeckController / DeckAnalysisPacketService / prompt-variant surface, an admin grid with no score-preview, a browse page with no selection UI, dead CSS removed; build remains 0 warn / 0 err at every commit boundary; new RET-01 and RET-05 regression assertions land.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md

## Executor: Codex (cross-AI dispatch)
This plan is implemented by Codex (gpt-5.4, medium). Claude reviews.

## ALLOWED FILE SET — HARD FENCE (public repo; do NOT exceed)
Codex MUST edit/create/delete ONLY the files listed in this plan's `files_modified` frontmatter.
- Do NOT touch `ContentKbController.cs` (browse controller logic + Markdig pipeline — owned by Plan 37-02; XSS posture preserved there), `Views/ContentKb/Detail.cshtml`, `FeatureFlagStore.cs`, `Home.cshtml`, or `_DeckToolTabs.cshtml` — those belong to Plan 37-02.
- This plan DOES own `Views/ContentKb/Index.cshtml` (browse-page selection strip only — KEEP search/filter/listing), `AdminContentKbController.cs` + `AdminContentKb/Index.cshtml` + `AdminContentKbViewModel.cs` (score-preview removal only — KEEP the curation grid), `content-kb-admin.ts`, and `AdminContentKbControllerTests.cs`. These were pulled forward from the old 37-02 to keep the interface deletion build-green.
- Do NOT touch `ICategoryKnowledgeStore` / its registration, `ContentKbClipParser`, `ContentSiteIndexStore`, any Core content store, `kb-entry-filter.ts`, `content-kb.ts`, or the CLI — all KEPT.
- For the dead CSS removal: edit ONLY the `.kb-pin-btn` / `.kb-follow-btn` / `.kb-selection-tray*` rule block in `site-common.css` (lines 340-434, between `.kb-card-actions button {}` above and `.kb-chip-area {}` below). Do NOT reflow surrounding CSS. Layout CSS correctly lives in `site-common.css` (per project constraint) — do not move it.
- Before starting: run `git status` and confirm a clean tree. Ignore any task outside this plan.
- Do NOT run "Format Document" / Code Cleanup / ReSharper reflow. Edit ONLY the target lines. Preserve LF endings. Do NOT collapse `{ get; init; }` → `{ get; }`. Do NOT re-indent raw-string literals (changes the AI prompt text shipped).
- Do NOT stage compiled `wwwroot/js/*.js` (gitignored; regenerated by the MSBuild tsc target).
- No new NuGet/npm dependencies.

## Build gate (run after each task — every task boundary MUST be 0/0)
`"/mnt/c/Program Files/dotnet/dotnet.exe" build DeckFlow.sln -c Debug` MUST report 0 Warning(s) / 0 Error(s). Baseline is 0/0 (37-RESEARCH.md § Environment Availability).
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/37-retire-clip-injection/37-CONTEXT.md
@.planning/phases/37-retire-clip-injection/37-RESEARCH.md
@.planning/phases/37-retire-clip-injection/37-VALIDATION.md

<interfaces>
<!-- Current contracts the executor edits. From 37-RESEARCH.md (grep-verified file:line). -->

IAnalysisPromptVariant.Build (IAnalysisPromptVariant.cs:26,37): trailing param `IReadOnlyList<ContentKbExcerpt>? kbExcerpts` is REMOVED. The 3 concrete variants + AnalysisPromptVariantRegistry pass-through (registry :41,:47) drop it too.

DeckAnalysisPacketResult record (DeckAnalysisPacketService.cs:57-58): drop params `ExpertContextClips` and `ResolvedPinTitles`.

DeckAnalysisPacketService ctor (:144): drop `IContentKbRelevanceService? contentKbRelevanceService`; field `:79` removed. Body removals: replay `:548-555`, fresh `GetMergedClipsAsync` `:699-711`, `ExpertSelectionJson` serialize `:730-738`, `ResolvePinTitlesAsync` `:741-743`, result-construction args `:770-771`, `kbExcerpts` local, `BuildAnalysisPrompt` trailing param `:1161` + its call `:717`. Cache-key populate sites `:295,301`.

DeckAnalysisRequest (DeckAnalysisRequest.cs): remove `ExpertContextJson` (:129), `PinnedVideoIds` (:138), `FollowedCreators` (:147), `ExpertSelectionJson` (:156) + backing fields. (FOUR fields — `ExpertContextJson` AND `ExpertSelectionJson` are both packet JSON fields; all four must go atomically.)

DeckAnalysisViewModel: remove `ExpertContextClips` (:71), `ResolvedPinTitles` (:76).

PacketArtifactStore.cs: remove `ExpertSelectionJsonOptions` (:31-35), `ExpertSelectionState` record (:911-), load-restore block (:319-337), read TryGetValue/assigns (:287-288,:298-299), and (recommended, per Open Q1) the `BuildZip` params `expertContextJson`/`selectionJson` (:128-129) + entry tuples (:141-142) + the `32-expert-context.json`/`33-expert-selection.json` names in PacketAllowedNames (:37-53). ReadEntries' allow-list filter makes leftover legacy entries a no-op → RET-05 holds without any JsonSerializerOptions change.

PacketSessionCache.cs:187-188: remove `NormalizedPinnedVideoIds` / `NormalizedFollowedCreators` from the cache-key record.

DeckController.cs: remove `ExpertContextClips`/`ResolvedPinTitles` viewmodel maps (:503-504, :904) and the `expertContextJson`/`selectionJson` BuildZip args (:793-800, :822-829).

AdminContentKbController.cs (PULLED FORWARD): `_relevanceService` field (:26), ctor param (:39) + guard (:45) + assign (:50), the `previewCommander`/`previewBracket`/`sortBy` action params (:67-70) + `previewActive`/`previewScores` logic (:74-89), `RelevanceScore` projection (:100), score-sort block (:110-116), and the `NormalizePreviewCommander`/`NormalizePreviewBracket`/`NormalizeSortBy` private helpers (:152-194). KEEP `GetAllRowsAsync`, visibility filter, sources, status, `SetEvergreen`, `SetVisibility`, `ReloadSeed`. The ctor signature drops the relevance dependency in the SAME unit the interface is deleted → build stays 0/0.

AdminContentKb/Index.cshtml (PULLED FORWARD): preview form (`previewCommander`/`previewBracket` inputs, :4-28 viewdata + :126 input), score column (:217 `RelevanceScore`), score-sort affordance, and the `content-kb-admin.js` script tag (:265). KEEP grid, publish/unpublish, evergreen toggle, per-source bulk, reload-seed, `data-kb-search` filter.

AdminContentKbViewModel.cs (PULLED FORWARD): remove `PreviewCommander` (:20), `RelevanceScore` (:91), and any `PreviewBracket`/`SortBy`. KEEP `IsEvergreen`, `IsVisible`, etc. (init properties — do not collapse).

ContentKb/Index.cshtml (PULLED FORWARD): selection tray (:62-71), per-entry pin/follow buttons (:104-118, incl. `data-video-id` :109 / `data-creator` :118), "Run analysis with this selection →" CTA (:71), `kb-selection.js` script tag (:145). REMOVE all; KEEP heading, `data-kb-search` filter row, entry listing, and `content-kb.ts` / `kb-entry-filter.ts` script wiring.

site-common.css (dead CSS): `.kb-pin-btn` / `.kb-follow-btn` rules (:340-362), `.kb-selection-tray*` rules (:364-433) — the whole block lines 340-434 (rule block + trailing blank line). Bounded by `.kb-card-actions button {}` (ends :338) above and `.kb-chip-area {}` (starts :435) below. Remove the block only; no reflow.

Markdig pipeline (XSS, RET-04) — DO NOT TOUCH here; owned by Plan 02. Leave `ContentKbController.cs` untouched.
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → /api/content-kb (typeahead) | Untrusted query params; endpoint NOT flag-gated (live even while KB dark) |
| stored packet zip → PacketArtifactStore read | Untrusted/legacy on-disk JSON entries crossing into deserialize |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37-01 | Info disclosure / Elevation | `ContentKbSearchApiController` (`/api/content-kb/entries|creators`) | mitigate | DELETE the controller — closes a non-flag-gated unauthenticated public endpoint. Net attack-surface REDUCTION (37-RESEARCH § Security Domain V4). |
| T-37-02 | Tampering | Legacy packet zip load (`PacketArtifactStore.ReadEntries`) | accept | Allow-list-filtered read + `try/catch JsonException` already ignores removed `32-`/`33-` entries; no `UnmappedMemberHandling.Disallow` / `[JsonRequired]`. RET-05 test asserts no throw. No code change needed beyond removing read sites. |
| T-37-03 | Tampering (prompt injection) | Clip text → LLM prompt | mitigate | ELIMINATED by this phase — removing the injection path removes the only route untrusted clip text reached an LLM prompt. Sanitizer + structural fence become unnecessary and are deleted. |
| T-37-06 | Spoofing / dead-UI | Browse page pin/follow buttons POSTing to deleted `/api/content-kb` | mitigate | This plan strips the browse-page tray/buttons/CTA/script in the SAME wave it deletes `ContentKbSearchApiController` + `kb-selection.ts` → no client references a deleted endpoint at any commit boundary. |
| T-37-SC | Tampering | npm/NuGet installs | mitigate | Zero installs — removal-only. No `[ASSUMED]`/`[SUS]` packages; slopcheck N/A. |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (Wave 0): Write RET-01 + RET-05 regression assertions — lands ATOMICALLY with Task 2</name>
  <files>DeckFlow.Web.Tests/AnalysisPromptVariantNoExpertContextTests.cs, DeckFlow.Web.Tests/PacketLegacyZipBackCompatTests.cs</files>
  <read_first>
    - DeckFlow.Web/Services/PromptBuilders/Analysis/ChatGptAnalysisPromptVariant.cs (current Build signature + Expert-Context block :251-265)
    - DeckFlow.Web/Services/PromptBuilders/Analysis/ClaudeAnalysisPromptVariant.cs (block :243-257)
    - DeckFlow.Web/Services/PromptBuilders/Analysis/GeminiAnalysisPromptVariant.cs (block :254-279 + helper EstimateExpertContextLength :283-)
    - DeckFlow.Web.Tests/AnalysisPromptVariantExpertContextTests.cs (existing pattern to mirror; being deleted)
    - DeckFlow.Web/Services/PacketArtifactStore.cs (ReadEntries :281, PacketAllowedNames :37-53, allow-list-filtered read)
    - 37-RESEARCH.md § Back-Compat (RET-05) — forgiving allow-list + try/catch JsonException; TWO fields 32-/33-
    - 37-VALIDATION.md § Wave 0 Requirements
  </read_first>
  <behavior>
    AnalysisPromptVariantNoExpertContextTests:
    - For each of the 3 variants (ChatGpt, Claude, Gemini): building an analysis prompt produces output whose text does NOT contain the literal "## Expert Context".
    - Uses the post-removal Build signature (no kbExcerpts param). Written test-first so RED is "## Expert Context still present"; GREEN after Task 2 strips the blocks.
    PacketLegacyZipBackCompatTests:
    - Constructing a packet zip that INCLUDES `32-expert-context.json` and `33-expert-selection.json` entries (plus the kept entries the reader needs) and loading it through PacketArtifactStore's read path does NOT throw and returns a populated result.
    - Asserts the load is a no-op for the legacy entries (they are ignored, not deserialized into removed properties).
  </behavior>
  <action>
    Create `AnalysisPromptVariantNoExpertContextTests.cs`: one xUnit `[Fact]` per variant asserting `Build(...)` output lacks `## Expert Context`. Mirror the fixture-setup style of the soon-deleted `AnalysisPromptVariantExpertContextTests.cs` but invert the assertion (DoesNotContain). Use the FINAL (post-removal) Build signature (no `kbExcerpts` param). Create `PacketLegacyZipBackCompatTests.cs`: build a zip stream containing the two legacy entries plus the minimum kept entries, run it through the store's load method, assert no exception and a non-null result. Reuse zip-construction helpers from `PacketArtifactStoreTests.cs`. No production code in this task.

    ATOMICITY (RET-02 / addresses review finding #5): These tests reference the POST-removal `Build` signature, so they will NOT compile against the current (pre-removal) signature. Task 1 and Task 2 are therefore ONE atomic commit/build boundary — do NOT run the build gate or commit between Task 1 and Task 2; author both, then build once. The build gate is satisfied at the Task 2 boundary, not between 1 and 2. (If you prefer, author Task 1's files and Task 2's edits in a single working pass before the first build.)
  </action>
  <verify>
    <automated>echo "Build gate deferred to Task 2 (atomic with Task 1) — see acceptance_criteria"</automated>
  </verify>
  <acceptance_criteria>
    - Both new test files exist under DeckFlow.Web.Tests/.
    - `grep -c "## Expert Context" DeckFlow.Web.Tests/AnalysisPromptVariantNoExpertContextTests.cs` shows the literal is referenced as a NEGATIVE assertion (DoesNotContain), across 3 variant tests.
    - PacketLegacyZipBackCompatTests references `32-expert-context.json` and `33-expert-selection.json` as legacy entries that must load without throwing.
    - Task 1 + Task 2 land as ONE atomic commit; the build is verified 0/0 at the Task 2 boundary (NOT between 1 and 2). The tests use the post-removal signature and will not compile alone — by design.
  </acceptance_criteria>
  <done>RET-01 and RET-05 regression assertions exist and express the post-removal contract; they compile and pass once Task 2 completes (the same atomic commit).</done>
</task>

<task type="auto">
  <name>Task 2: Atomic injection removal — de-wire ALL consumers (incl. admin + browse strip), delete orphans + dead CSS (build-green)</name>
  <files>DeckFlow.Web/Program.cs, DeckFlow.Web/Services/DeckAnalysisPacketService.cs, DeckFlow.Web/Services/PromptBuilders/Analysis/IAnalysisPromptVariant.cs, DeckFlow.Web/Services/PromptBuilders/Analysis/AnalysisPromptVariantRegistry.cs, DeckFlow.Web/Services/PromptBuilders/Analysis/ChatGptAnalysisPromptVariant.cs, DeckFlow.Web/Services/PromptBuilders/Analysis/ClaudeAnalysisPromptVariant.cs, DeckFlow.Web/Services/PromptBuilders/Analysis/GeminiAnalysisPromptVariant.cs, DeckFlow.Web/Models/DeckAnalysisRequest.cs, DeckFlow.Web/Models/DeckAnalysisViewModel.cs, DeckFlow.Web/Services/PacketArtifactStore.cs, DeckFlow.Web/Services/PacketSessionCache.cs, DeckFlow.Web/Controllers/DeckController.cs, DeckFlow.Web/Views/Deck/DeckAnalysis.cshtml, DeckFlow.Web/Controllers/Admin/AdminContentKbController.cs, DeckFlow.Web/Views/AdminContentKb/Index.cshtml, DeckFlow.Web/Models/AdminContentKbViewModel.cs, DeckFlow.Web/Views/ContentKb/Index.cshtml, DeckFlow.Web/wwwroot/css/site-common.css, DeckFlow.Web/Services/ContentKbRelevanceService.cs, DeckFlow.Web/Services/ContentKbArchetypeDeriver.cs, DeckFlow.Web/Services/ContentKbClipSanitizer.cs, DeckFlow.Web/Models/ContentKbExcerpt.cs, DeckFlow.Web/Controllers/Api/ContentKbSearchApiController.cs, DeckFlow.Web/Views/Deck/_ContentKbPanel.cshtml, DeckFlow.Web/wwwroot/ts/kb-selection.ts, DeckFlow.Web/wwwroot/ts/content-kb-admin.ts</files>
  <read_first>
    - 37-RESEARCH.md § Removal-Graph Verification (every file:line below) AND § Partial-Edit Blast Radius AND § Common Pitfalls (1-5)
    - DeckFlow.Web/Program.cs (DI :182-190 deriver+relevance; ctor arg :331)
    - DeckFlow.Web/Services/DeckAnalysisPacketService.cs (field :79, ctor :144, replay :548-555, fresh :699-711, serialize :730-738, resolve :741-743, result :770-771, BuildAnalysisPrompt :1161 + call :717, cache populate :295,301)
    - DeckFlow.Web/Services/PromptBuilders/Analysis/IAnalysisPromptVariant.cs (:26,:37), AnalysisPromptVariantRegistry.cs (:41,:47)
    - DeckFlow.Web/Services/PromptBuilders/Analysis/ChatGptAnalysisPromptVariant.cs (param :34, block :251-265), ClaudeAnalysisPromptVariant.cs (param :35, block :243-257), GeminiAnalysisPromptVariant.cs (param :36, block :254-279, helper EstimateExpertContextLength :283-)
    - DeckFlow.Web/Models/DeckAnalysisRequest.cs (:129 ExpertContextJson, :138 PinnedVideoIds, :147 FollowedCreators, :156 ExpertSelectionJson)
    - DeckFlow.Web/Models/DeckAnalysisViewModel.cs (:71,:76)
    - DeckFlow.Web/Services/PacketArtifactStore.cs (options :31-35, BuildZip params :128-129 + tuples :141-142, AllowedNames :37-53, read :287-299, restore :319-337, ExpertSelectionState :911-)
    - DeckFlow.Web/Services/PacketSessionCache.cs (:187-188)
    - DeckFlow.Web/Controllers/DeckController.cs (:503-504, :793-800, :822-829, :904)
    - DeckFlow.Web/Views/Deck/DeckAnalysis.cshtml (accordion :182-232, _ContentKbPanel render :605, kb-selection.js script :981)
    - DeckFlow.Web/Controllers/Admin/AdminContentKbController.cs (_relevanceService :26, ctor :39/:45/:50, preview params :67-70, preview logic :74-89, RelevanceScore projection :100, score-sort :110-116, Normalize* helpers :152-194)
    - DeckFlow.Web/Views/AdminContentKb/Index.cshtml (preview viewdata :4-28, preview input :126, score column :217, content-kb-admin.js script :265; KEEP grid/evergreen/reload-seed/data-kb-search)
    - DeckFlow.Web/Models/AdminContentKbViewModel.cs (PreviewCommander :20, RelevanceScore :91)
    - DeckFlow.Web/Views/ContentKb/Index.cshtml (tray :62-71, CTA :71, pin/follow buttons :104-118 incl. data-video-id :109 / data-creator :118, kb-selection.js script :145; KEEP data-kb-search + listing)
    - DeckFlow.Web/wwwroot/css/site-common.css (:340-434 dead block; bounded by .kb-card-actions button {} :336-338 and .kb-chip-area {} :435)
  </read_first>
  <action>
    Perform the removal bottom-up so the build stays 0/0 at the END of this task (Pitfall 5: also grep views/TS, not just C#). All consumers of removed types are in THIS task, so the single build at the task boundary is 0/0.
    1. PROMPT VARIANTS: from `IAnalysisPromptVariant.Build` remove the trailing `kbExcerpts` param + its doc line; from `AnalysisPromptVariantRegistry` remove the param + pass-through arg; from each of ChatGpt/Claude/Gemini variants remove the `kbExcerpts` param and the entire `## Expert Context` block (and its `ContentKbClipSanitizer.Sanitize(...)` calls). In Gemini, ALSO delete the private helper `EstimateExpertContextLength` (Pitfall 3 — dangling-unused warning).
    2. PACKET SERVICE: remove field `_contentKbRelevanceService`, the ctor param, the replay block, the fresh `GetMergedClipsAsync` call + `kbExcerpts` local + `ExpertSelection` build, the `ExpertSelectionJson` serialize, the `ResolvePinTitlesAsync` call, drop `ExpertContextClips`/`ResolvedPinTitles` from `DeckAnalysisPacketResult`, drop the `BuildAnalysisPrompt` trailing `kbExcerpts` param and update its single call, and remove the cache-key populate sites at :295/:301.
    3. REQUEST/VIEWMODEL: remove `ExpertContextJson`, `PinnedVideoIds`, `FollowedCreators`, `ExpertSelectionJson` (+ backing fields) from `DeckAnalysisRequest` — ALL FOUR atomically (Pitfall 2); remove `ExpertContextClips`, `ResolvedPinTitles` from `DeckAnalysisViewModel`.
    4. PACKET STORE/CACHE: remove `ExpertSelectionJsonOptions`, `ExpertSelectionState`, the load-restore block and the read TryGetValue/assigns; drop the `BuildZip` `expertContextJson`/`selectionJson` params + their entry tuples; remove `32-expert-context.json`/`33-expert-selection.json` from `PacketAllowedNames` (Open Q1 recommendation — clean surface for Phase 38). Do NOT change any other JsonSerializerOptions. From `PacketSessionCache` remove the two `Normalized*` cache-key fields.
    5. DECK CONTROLLER + VIEW: remove the two viewmodel maps and the two BuildZip arg sites in `DeckController`; from `DeckAnalysis.cshtml` delete the accordion (:182-232 — verify the closing `</details>` live), the `_ContentKbPanel` partial render (:605), and the `kb-selection.js` script tag (:981). (RET-06's pointer NOTE replacing the accordion is added in Plan 02, a later wave.)
    6. ADMIN SCORE-PREVIEW (pulled forward — REQUIRED for build-green with the interface deletion): in `AdminContentKbController.cs` remove `_relevanceService` field/ctor-param/guard/assign, the `previewCommander`/`previewBracket`/`sortBy` action params, the `previewActive`/`previewScores` logic, the `RelevanceScore` projection, the score-sort block, and the now-orphaned `NormalizePreviewCommander`/`NormalizePreviewBracket`/`NormalizeSortBy` helpers — KEEP all curation actions. In `AdminContentKb/Index.cshtml` remove the preview form (preview viewdata + `previewCommander`/`previewBracket` input), the score column (`RelevanceScore` cell), the score-sort affordance, and the `content-kb-admin.js` script tag — KEEP grid, publish/unpublish, evergreen toggle, reload-seed, per-source bulk, `data-kb-search`. In `AdminContentKbViewModel.cs` remove `PreviewCommander`, `RelevanceScore`, and any `PreviewBracket`/`SortBy` — KEEP curation init properties.
    7. BROWSE-PAGE STRIP (pulled forward — REQUIRED so no caller outlives `kb-selection.ts` / the deleted API): in `ContentKb/Index.cshtml` remove the `kb-selection-tray` block (:62-71), every per-entry pin/follow button (:104-118 — the `data-video-id`/`data-creator` controls), the "Run analysis with this selection →" CTA (:71), and the `kb-selection.js` `<script>` tag (:145). KEEP the heading, `data-kb-search` filter row, entry listing, and `content-kb.ts`/`kb-entry-filter.ts` wiring.
    8. DEAD CSS: in `site-common.css` remove the `.kb-pin-btn` / `.kb-follow-btn` / `.kb-selection-tray*` rule block (lines 340-434, including the trailing blank line at 434). Do NOT touch `.kb-card-actions button` above or `.kb-chip-area` below. No reflow; preserve LF.
    9. DELETE orphans: `ContentKbRelevanceService.cs` (incl. `IContentKbRelevanceService`), `ContentKbArchetypeDeriver.cs`, `ContentKbClipSanitizer.cs`, `ContentKbExcerpt.cs`, `ContentKbSearchApiController.cs`, `_ContentKbPanel.cshtml`, `kb-selection.ts`, `content-kb-admin.ts`. From `Program.cs` remove the deriver+relevance DI registrations and the relevance ctor arg to `DeckAnalysisPacketService`. Do NOT touch `ICategoryKnowledgeStore` (deriver depends on it but it is KEPT — 37-RESEARCH § ContentKbArchetypeDeriver).
    Edit only target lines; no reflow; preserve LF; do not collapse `{ get; init; }`. (Admin test edits land in Task 3, in the SAME atomic wave commit — see Task 3 note.)
  </action>
  <verify>
    <automated>"/mnt/c/Program Files/dotnet/dotnet.exe" build DeckFlow.sln -c Debug</automated>
  </verify>
  <acceptance_criteria>
    - Build reports 0 Warning(s) / 0 Error(s).
    - `grep -rn "ContentKbRelevanceService\|IContentKbRelevanceService\|ContentKbExcerpt\|ContentKbArchetypeDeriver\|ContentKbClipSanitizer\|ExpertSelection\b" DeckFlow.Web --include=*.cs --include=*.cshtml | grep -v -E "(ContentKbRelevanceService|ContentKbArchetypeDeriver|ContentKbClipSanitizer|ContentKbExcerpt|ContentKbSearchApiController)\.cs"` returns NOTHING (the deleted files are gone, so their refs are gone too).
    - `grep -rn "kb-selection" DeckFlow.Web/Views/Deck/DeckAnalysis.cshtml DeckFlow.Web/Views/ContentKb/Index.cshtml` returns nothing; `test -f DeckFlow.Web/wwwroot/ts/kb-selection.ts` is FALSE; `test -f DeckFlow.Web/wwwroot/ts/content-kb-admin.ts` is FALSE.
    - `grep -n "## Expert Context" DeckFlow.Web/Services/PromptBuilders/Analysis/*.cs` returns nothing.
    - `grep -rn "ExpertContextJson\|ExpertSelectionJson\|PinnedVideoIds\|FollowedCreators" DeckFlow.Web/Models/DeckAnalysisRequest.cs` returns nothing (all four fields removed atomically).
    - `grep -n "EstimateExpertContextLength" DeckFlow.Web/Services/PromptBuilders/Analysis/GeminiAnalysisPromptVariant.cs` returns nothing.
    - `grep -rn "RelevanceScore\|previewCommander\|PreviewCommander\|PreviewBracket\|ScoreAllAsync\|_relevanceService\|content-kb-admin\|NormalizePreviewCommander" DeckFlow.Web/Controllers/Admin/AdminContentKbController.cs DeckFlow.Web/Views/AdminContentKb/Index.cshtml DeckFlow.Web/Models/AdminContentKbViewModel.cs` returns NOTHING.
    - `grep -n "SetEvergreen\|ReloadSeed\|data-kb-search" DeckFlow.Web/Views/AdminContentKb/Index.cshtml` STILL returns matches (curation grid kept).
    - `grep -n "kb-selection-tray\|data-video-id\|data-creator\|Run analysis" DeckFlow.Web/Views/ContentKb/Index.cshtml` returns NOTHING; `grep -n "data-kb-search\|kb-entry-filter\|content-kb.js" DeckFlow.Web/Views/ContentKb/Index.cshtml` STILL returns matches.
    - `grep -n "kb-pin-btn\|kb-follow-btn\|kb-selection-tray" DeckFlow.Web/wwwroot/css/site-common.css` returns NOTHING; `grep -n "kb-card-actions\|kb-chip-area" DeckFlow.Web/wwwroot/css/site-common.css` STILL returns matches (neighbors preserved).
    - `git diff --stat` shows NO files outside this plan's `files_modified` were touched.
    - The Task-1 RET-01 tests now compile and the prompt output omits `## Expert Context`.
  </acceptance_criteria>
  <done>All injection production code + admin score-preview + browse-page selection UI + dead CSS are removed; the 3 variants emit no `## Expert Context`; the DeckAnalysis page has no accordion/panel/widget; the admin grid keeps curation; the browse page keeps search/listing; build is 0/0 with no dead references.</done>
</task>

<task type="auto">
  <name>Task 3: Prune the injection test suite (delete 8 dedicated, edit 6 incidental incl. admin)</name>
  <files>DeckFlow.Web.Tests/ContentKbRelevanceServiceTests.cs, DeckFlow.Web.Tests/ContentKbClipSanitizerTests.cs, DeckFlow.Web.Tests/ContentKbMergedClipsTests.cs, DeckFlow.Web.Tests/ContentKbArchetypeDeriverTests.cs, DeckFlow.Web.Tests/ContentKbExcerptTests.cs, DeckFlow.Web.Tests/Spike001KbValueAbHarness.cs, DeckFlow.Web.Tests/AnalysisPromptVariantExpertContextTests.cs, DeckFlow.Web.Tests/DeckAnalysisPacketServiceExpertContextTests.cs, DeckFlow.Web.Tests/DeckControllerTests.cs, DeckFlow.Web.Tests/DeckAnalysisPacketServiceTests.cs, DeckFlow.Web.Tests/DeckAnalysisRequestTests.cs, DeckFlow.Web.Tests/PacketArtifactStoreTests.cs, DeckFlow.Web.Tests/AiPlatformExtensionTests.cs, DeckFlow.Web.Tests/AdminContentKbControllerTests.cs</files>
  <read_first>
    - 37-RESEARCH.md § Test Impact (the full disposition table with file:line)
    - DeckFlow.Web.Tests/DeckControllerTests.cs (:715 DeckAnalysis_MapsExpertContextClips_, :780 _LeavesExpertContextClipsNull_, fixture :717)
    - DeckFlow.Web.Tests/DeckAnalysisPacketServiceTests.cs (:1374-1430 pin/follow normalization asserts)
    - DeckFlow.Web.Tests/DeckAnalysisRequestTests.cs (:13-18 PinnedVideoIds/FollowedCreators coalesce asserts)
    - DeckFlow.Web.Tests/PacketArtifactStoreTests.cs (:82,124,161,186,193-220,257-296 expert round-trip asserts; KEEP deck-profile/set-upgrade)
    - DeckFlow.Web.Tests/AiPlatformExtensionTests.cs (:42 stub variant Build signature)
    - DeckFlow.Web.Tests/AdminContentKbControllerTests.cs (FakeContentKbRelevanceService double :369-401, score-preview cases :120,152,168,195,254, ctor call :331, Build overloads :312,323)
  </read_first>
  <action>
    DELETE wholesale: `ContentKbRelevanceServiceTests.cs`, `ContentKbClipSanitizerTests.cs`, `ContentKbMergedClipsTests.cs`, `ContentKbArchetypeDeriverTests.cs`, `ContentKbExcerptTests.cs`, `Spike001KbValueAbHarness.cs`, `AnalysisPromptVariantExpertContextTests.cs`, `DeckAnalysisPacketServiceExpertContextTests.cs`.
    EDIT:
    - `DeckControllerTests.cs`: delete the two methods `DeckAnalysis_MapsExpertContextClips_*` and `*_LeavesExpertContextClipsNull_*` + the `clips` fixture; update any `DeckAnalysisPacketResult` construction to drop `ExpertContextClips`/`ResolvedPinTitles`.
    - `DeckAnalysisPacketServiceTests.cs`: remove the `PinnedVideoIds`/`FollowedCreators` normalization assignments/asserts (:1374-1430).
    - `DeckAnalysisRequestTests.cs`: remove the `PinnedVideoIds`/`FollowedCreators` null-coalesce asserts (:13-18).
    - `PacketArtifactStoreTests.cs`: remove the expert-context/selection round-trip asserts (the ExpertContextJson `== ""`/`"[]"` asserts and the selection round-trip cases); KEEP deck-profile/set-upgrade round-trip.
    - `AiPlatformExtensionTests.cs`: drop the `kbExcerpts` param from the stub variant's `Build(...)` signature so it matches the edited `IAnalysisPromptVariant`.
    - `AdminContentKbControllerTests.cs` (PULLED FORWARD — was Plan 02): remove the `FakeContentKbRelevanceService` double (:369-401) + the score-preview test cases (`Index_WithPreviewParams_*`, `Index_WithNoPreviewParams_*`, `Index_WithInvalidPreviewBracket_*`, and any preview-normalization case ~:120,152,168,195,254); drop the `relevanceService` arg from the two `Build(...)` test-helper overloads (:312,323) and the ctor call (:331). KEEP the grid/visibility/seed/evergreen tests.
    After edits, remove any now-orphaned `using` directives / helper methods (Pitfall 4 / Wave 0 note) so no unused-symbol warnings appear.

    ATOMICITY: Task 3 lands in the SAME atomic wave commit as Task 2 (Task 2's interface deletion makes `AdminContentKbControllerTests.cs` un-buildable until this task removes `FakeContentKbRelevanceService`/the ctor arg). Do NOT commit Task 2 separately from Task 3 — the build is 0/0 only after BOTH complete. Run the build gate once, after Task 3.
  </action>
  <verify>
    <automated>"/mnt/c/Program Files/dotnet/dotnet.exe" build DeckFlow.sln -c Debug</automated>
  </verify>
  <acceptance_criteria>
    - The 8 listed test files no longer exist (`test -f` FALSE for each).
    - `grep -rn "ContentKbExcerpt\|FakeContentKbRelevanceService\|IContentKbRelevanceService\|GetMergedClipsAsync\|ExpertSelection\|PinnedVideoIds\|FollowedCreators\|RelevanceScore\|ScoreAllAsync" DeckFlow.Web.Tests --include=*.cs` returns NOTHING (admin test double + preview asserts gone too — no Plan-02 carve-out anymore).
    - Build is 0 Warning(s) / 0 Error(s) — no orphaned-using / unused-helper warnings.
    - The two new Task-1 tests (`AnalysisPromptVariantNoExpertContextTests`, `PacketLegacyZipBackCompatTests`) remain present and compile.
    - `git diff --stat` shows no file outside this plan's `files_modified`.
  </acceptance_criteria>
  <done>The injection-specific test suite (incl. the admin score-preview tests) is removed/trimmed; the surviving suite compiles at 0/0 and retains the new RET-01/RET-05 assertions.</done>
</task>

</tasks>

<verification>
- `dotnet build DeckFlow.sln -c Debug` → 0 Warning(s) / 0 Error(s) at the wave commit boundary (Task 2+Task 3 atomic; Task 1 folded into Task 2's build).
- Grep proves zero references to `ContentKbRelevanceService` / `IContentKbRelevanceService` / `ContentKbExcerpt` / `ExpertSelection` / admin `RelevanceScore`/`ScoreAllAsync` outside the deleted files (RET-02 / SC2).
- The 3 prompt variants emit no `## Expert Context`; `DeckAnalysis.cshtml` has no accordion/panel/widget; `ContentKb/Index.cshtml` has no selection tray/buttons/CTA/script; admin grid has no score column/preview; dead CSS removed (RET-01 / SC1).
- `PacketLegacyZipBackCompatTests` proves a legacy zip loads without throwing (RET-05 / SC4).
- Final solution-wide sweep (closing acceptance, addresses review finding #3): `grep -rn "ContentKbRelevanceService\|IContentKbRelevanceService\|ExpertSelection\b\|ContentKbArchetypeDeriver\|ContentKbClipSanitizer\|ContentKbExcerpt\|ContentKbSearchApiController\|kb-selection\|ExpertContextJson\|ExpertSelectionJson\|PinnedVideoIds\|FollowedCreators" DeckFlow.Web DeckFlow.Web.Tests --include=*.cs --include=*.cshtml --include=*.ts | grep -v -E "/(bin|obj)/"` returns NOTHING.
- `git diff --stat` confirms no out-of-fence edits.
</verification>

<success_criteria>
- RET-01: No `## Expert Context` in any of 3 variants; no accordion/panel/widget on DeckAnalysis; no selection UI on the browse page; admin score-preview gone.
- RET-02: Build 0/0 at the wave commit boundary; no dead references to removed types (solution-wide sweep clean).
- RET-05: Pre-retire packet zip loads without error.
</success_criteria>

<output>
Create `.planning/phases/37-retire-clip-injection/37-01-SUMMARY.md` when done.
</output>
