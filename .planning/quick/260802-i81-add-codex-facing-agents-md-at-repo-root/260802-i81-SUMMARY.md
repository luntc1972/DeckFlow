---
id: 260802-i81
status: complete
date: 2026-08-02
branch: docs/codex-agents-md
---

# Quick Task 260802-i81 — Summary

## Where the file lives — read this before editing it

`AGENTS.md` at the repo root is a **symlink**, not a file:

```
<repo>/AGENTS.md -> /home/clunt/.claude/codex/repos/deckflow-AGENTS.md
```

DeckFlow's own `.gitignore:12` ignores `AGENTS.md`, immediately above `/.codex` — a deliberate
policy, since this repo is public. That ignore was **not** overridden. Instead the file is versioned
in the dev-clunt config repo (`~/.claude`), the same pattern already used for `config.toml`, the
global `AGENTS.md`, the skills quarantine list, and the sync hook. This gets version control and
cross-machine survival without publishing agent instructions to a public repo, and the repo's own
ignore rule guarantees the symlink is never staged here.

**Edit it at `~/.claude/codex/repos/deckflow-AGENTS.md`.** On a fresh clone the symlink must be
re-created — the file itself comes from dev-clunt.

## What changed

Created the DeckFlow Codex guide (111 lines, 6,264 bytes, LF), tracked at
`~/.claude/codex/repos/deckflow-AGENTS.md` and symlinked to `<repo>/AGENTS.md`. Documentation only,
no source touched. No change to DeckFlow's `.gitignore`.

Contents, all repo-specific: the 7-project layout with `.cs` counts, the 13 `DeckFlow.Web/Services`
subfolders (so Codex opens a folder instead of grepping ~170 files), where Cut Lab / manabase /
prompt builders / the browser extension live, build + test commands, the TypeScript build coupling
and gitignored `wwwroot/js`, the never-open-a-browser UI-testing rule, the `.editorconfig`
changed-lines gate with all five carve-outs, naming/async/logging conventions, the hard constraints
(RestSharp+Polly, `ScryfallThrottle`, `site-common.css`, forwarded-headers ordering, the
`TaggerSessionCache` TTL invariant, LF, 512 MB tier), test conventions, and commit rules.

## Verification

- `wc -l` = 111 (under the 150 budget); `grep -c $'\r'` = 0, matching `.gitattributes` LF.
- **All 16 referenced paths resolve on disk** — `DeckFlow.sln`, `DeckFlow.Web/Services/CutLab`,
  `DeckFlow.Core/Manabase`, `scripts/format-check-changed.sh`, `scripts/run-web-test.{sh,ps1}`,
  `.editorconfig`, `wwwroot/css/site-common.css`, and the rest.
- All 4 named symbols exist: `ScryfallThrottle` (12 files), `CarveOutGuard` (1), `TaggerSessionCache`
  (7), `EdhTop16Client` (10).
- `DeckFlow.Web/wwwroot/js/*.js` confirmed gitignored.
- Corrected one figure before finalizing: CutLab test files are **34**, not the ~25 first written.
- **Loads correctly in Codex**: probed `codex exec` rooted at this repo and read
  `world_state.agents_md` from the session log — 8,893 chars (~2,223 tok), `directory` resolves to
  the repo root, `--- project-doc ---` separator present, repo section present. Global rules from
  `~/.codex/AGENTS.md` merge ahead of it as expected.
- **Re-verified after the move to a symlink** — byte-identical 8,893-char load through
  `<repo>/AGENTS.md -> ~/.claude/codex/repos/deckflow-AGENTS.md`, and `git check-ignore` confirms
  the symlink stays unstaged in DeckFlow.

## Why it matters

Codex reads `AGENTS.md`, not `CLAUDE.md`. Before this the repo had none, so `world_state.agents_md`
was empty and each dispatch rebuilt its bearings by exploring. Measured from
`~/.codex/sessions/2026/08/01/`: one plan-review run produced 622,617 chars of tool output over 39
calls — whole-file `nl -ba` dumps of 31–42k chars, overlapping `sed` reads of one plan, and
`CLAUDE.md` read **twice** (~19k tokens). Because context is re-sent on every tool call, that
growth compounded: 20.7k on call 1 to 192.3k on call 40, 4.56M for the run.

## Not verified

The token saving itself. The Codex workspace is **out of credits**, so no real review has run
against this file yet. Expected effect is a drop in tool-output volume on the next review; confirm
by re-measuring `custom_tool_call_output` size once credits are restored.

## Follow-ups

- On a fresh clone, re-create the symlink (the file ships via dev-clunt, not this repo).
- Re-measure a review dispatch after credits are refilled; compare against the 622,617-char baseline.
- Consider a `DeckFlow.Studio` note if Codex is ever pointed at that project specifically.
