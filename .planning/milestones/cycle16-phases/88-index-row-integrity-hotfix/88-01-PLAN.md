---
phase: 88-index-row-integrity-hotfix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - DeckFlow.Core/Content/ContentSiteIndexStore.cs
  - DeckFlow.Core/Content/IContentSiteIndexStore.cs
  - DeckFlow.Web/Controllers/ContentKbController.cs
  - DeckFlow.Core.Tests/Content/ContentSiteIndexStoreApprovalTests.cs
  - DeckFlow.Core.Tests/Content/ContentSiteIndexStoreVisibilityTests.cs
  - DeckFlow.Core.Tests/Content/ContentSiteIndexStoreSchemaEnsureSwitchTests.cs
  - DeckFlow.Web.Tests/ContentKbControllerTests.cs
  - DeckFlow.Web/e2e/content-kb-pending-hidden.spec.ts
autonomous: true
requirements: [SYNC-04, SYNC-06]

must_haves:
  truths:
    - "D-01: A DirectPush upsert writes the local row's approval_status on BOTH insert and update (approved today, since DirectPush reads only approved local rows)"
    - "D-02: A re-push of an approved row heals a drifted 'pending' prod row to 'approved'"
    - "D-04: The public KB browse query (GetPublishedRowsAsync) returns only rows with approval_status='approved'"
    - "D-04 / Codex HIGH: Direct navigation to /content-kb/{id} for a visible-but-pending row returns 404 — the detail route can never render a pending row"
    - "D-09 / D-11: A prod-mode store constructed with schema-ensure disabled issues zero CREATE/ALTER/DROP SQL on the DirectPush read+write paths, locked by a REQUIRED recording-connection regression test"
    - "D-03: No one-time SQL backfill of drifted prod rows ships in this phase — the serve filter neutralizes them until the Phase 91 reconciler"
    - "D-13 / D-14: Fixes ship unflagged; prod rollout is verified by operator pre-audit + post-deploy re-check via the read-only Render MCP query"
    - "D-15: xUnit regression tests + Playwright e2e (pending row never renders on public KB page — browse or detail) cover the phase's invariants"
  artifacts:
    - path: "DeckFlow.Core/Content/ContentSiteIndexStore.cs"
      provides: "Approval-mirrored batch upsert, approval-filtered publish reads (list + by-id), schema-ensure OFF switch"
      contains: "GetPublishedByIdAsync"
    - path: "DeckFlow.Core/Content/IContentSiteIndexStore.cs"
      provides: "GetPublishedByIdAsync interface member for the public detail read"
      contains: "GetPublishedByIdAsync"
    - path: "DeckFlow.Core.Tests/Content/ContentSiteIndexStoreApprovalTests.cs"
      provides: "Mirror-on-insert-and-update + serve-filter-excludes-pending regression tests (old-behavior facts updated)"
    - path: "DeckFlow.Core.Tests/Content/ContentSiteIndexStoreSchemaEnsureSwitchTests.cs"
      provides: "REQUIRED recording-connection no-DDL invariant test + supplemental schema-missing test"
  key_links:
    - from: "DeckFlow.Web/Controllers/ContentKbController.cs"
      to: "GetPublishedRowsAsync"
      via: "public /content-kb Index action"
      pattern: "GetPublishedRowsAsync"
    - from: "DeckFlow.Web/Controllers/ContentKbController.cs"
      to: "GetPublishedByIdAsync"
      via: "public /content-kb/{id} Detail action"
      pattern: "GetPublishedByIdAsync"
    - from: "ProdStoreFactory (Plan 88-03)"
      to: "ContentSiteIndexStore ctor"
      via: "ensureSchemaEnabled parameter"
      pattern: "ensureSchemaEnabled"
---

<objective>
Close two of the three live prod correctness bugs at the store layer: (1) DirectPush's prod upsert must mirror the local row's `approval_status` on insert AND update so a row is never publicly visible while `pending` (SYNC-04), plus serve-side defense-in-depth on BOTH public read paths — the browse list AND the by-id detail route — so a drifted row can never render anywhere; (2) the prod-pointed store must never run DDL, via a schema-ensure OFF switch locked by a REQUIRED recording-connection test asserting zero CREATE/ALTER/DROP SQL on the DirectPush read+write paths (SYNC-06 store layer, D-11).

Purpose: The C1 bug (visible-while-pending rows) and the C4 DDL-against-prod bug both live in `ContentSiteIndexStore`. This plan owns that file end to end, plus the two public read call sites.
Output: Approval-mirrored batch upsert, approval-filtered publish reads (list + new `GetPublishedByIdAsync`), a `ensureSchemaEnabled` ctor switch that no-ops `EnsureSchemaAsync`, updated old-behavior tests, and four test surfaces (xUnit approval, xUnit web detail-404, xUnit recording-connection no-DDL, Playwright pending-hidden e2e).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/88-index-row-integrity-hotfix/88-CONTEXT.md
@.planning/phases/88-index-row-integrity-hotfix/88-PATTERNS.md
@CLAUDE.md

<interfaces>
<!-- Executor uses these directly. Confirm current state via read_first before editing. -->

ContentSiteIndexRow (DeckFlow.Core/Knowledge/ContentArtifactSpec.cs:107) — sealed record.
Relevant members:
  - string ApprovalStatus { get; init; } = "pending"
  - string? YoutubeVideoId { get; init; }
  - string? RssGuid { get; init; }
  - string? PinId => YoutubeVideoId ?? RssGuid   (computed; NO separate NaturalKeyType/Value fields)

ContentSiteIndexStore (DeckFlow.Core/Content/ContentSiteIndexStore.cs):
  - const string UpsertContentColumnsOnlySql (lines ~955-993): the DirectPush upsert SQL. INSERT clause hardcodes 'pending'; ON CONFLICT (natural_key_type, natural_key_value) DO UPDATE SET omits approval_status; trailing comment claims approval_status is "intentionally absent". Used by BOTH UpsertContentColumnsOnlyAsync (single, ~165) and UpsertContentColumnsOnlyBatchAsync (~571) — the mirror change affects both entry points via the shared const.
  - BuildUpsertParameters(ContentSiteIndexRow, (string Type, string Value) naturalKey) (lines ~713-728) — Dapper DynamicParameters binder; already binds naturalKeyType/naturalKeyValue.
  - GetPublishedRowsAsync (line ~221): browse serve query, currently WHERE is_visible = @visible only.
  - GetApprovedRowsAsync (line ~255): sibling with WHERE approval_status = 'approved' (copy this literal exactly).
  - GetByIdAsync (line ~320): unfiltered by-id read — used by ContentKbController.Detail today; must remain unfiltered for admin/Studio use.
  - EnsureSchemaAsync (line ~44): runs CREATE TABLE / ALTER; called at the top of ~20 public methods.
  - OpenConnectionAsync (line ~742): one-line delegate to _connectionInfo.OpenConnectionAsync — the seam point for the recording-connection test.
  - ctors (lines ~13-42): ContentSiteIndexStore(string databasePath) and ContentSiteIndexStore(RelationalDatabaseConnection connectionInfo).
  - [assembly: InternalsVisibleTo("DeckFlow.Core.Tests")] already present (DeckFlow.Core/AssemblyInfo.cs:3) — internal test seams are the house pattern.

ContentKbController (DeckFlow.Web/Controllers/ContentKbController.cs):
  - Index (~52): calls GetPublishedRowsAsync — browse list.
  - Detail (~89-98): calls GetByIdAsync then checks ONLY `row is null || !row.IsVisible` → a visible-but-pending row still renders at /content-kb/{id}. THIS IS THE HIGH-severity leak this plan closes.

Existing tests asserting OLD behavior (MUST be updated, they fail after this plan):
  - DeckFlow.Core.Tests/Content/ContentSiteIndexStoreVisibilityTests.cs (~:77) `GetPublishedRowsAsync_FiltersHiddenRows_AndGetByIdReturnsVisibility` — seeds rows without setting approval; expects them in GetPublishedRowsAsync.
  - DeckFlow.Core.Tests/Content/ContentSiteIndexStoreApprovalTests.cs (~:84) `UpsertContentColumnsOnlyAsync_NewRow_LandsAsPending` and `UpsertContentColumnsOnlyAsync_ExistingRow_PreservesApprovalStatus` — assert the hardcoded-'pending' insert and approval-absent update.

Vocabulary constants: ContentSourceType.Youtube = "youtube_channel", ContentSourceType.Podcast = "podcast_rss" (DeckFlow.Core/Knowledge/ContentModels.cs:180-187).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Mirror approval_status on the DirectPush upsert + update old-behavior tests (D-01/D-02, Codex MED-1)</name>
  <files>DeckFlow.Core/Content/ContentSiteIndexStore.cs, DeckFlow.Core.Tests/Content/ContentSiteIndexStoreApprovalTests.cs</files>
  <read_first>
    - DeckFlow.Core/Content/ContentSiteIndexStore.cs — read UpsertContentColumnsOnlySql (~955-993), BuildUpsertParameters (~713-728), UpsertContentColumnsOnlyAsync (~165), UpsertContentColumnsOnlyBatchAsync (~571), and GetNaturalKey (~790) to see current binding style
    - DeckFlow.Core/Knowledge/ContentArtifactSpec.cs — ContentSiteIndexRow record (ApprovalStatus default "pending")
    - DeckFlow.Core.Tests/Content/ContentSiteIndexStoreApprovalTests.cs — existing ctor/dispose fixture (temp SQLite file), the CreateYoutubeRow helper, AND the two old-behavior facts that must change: UpsertContentColumnsOnlyAsync_NewRow_LandsAsPending, UpsertContentColumnsOnlyAsync_ExistingRow_PreservesApprovalStatus
    - DeckFlow.Core.Tests/Content/ContentSiteIndexStoreBatchUpsertTests.cs — the "construct store, run one write, assert on GetAllRowsAsync()" idiom
  </read_first>
  <behavior>
    - Insert case: upserting a NEW row whose ApprovalStatus is "approved" persists approval_status='approved' (not 'pending').
    - Update case: an existing prod row at approval_status='pending' + is_visible=true, re-upserted from a row with ApprovalStatus="approved", ends at approval_status='approved' (heals the drift).
    - Operator-owned columns is_visible, is_hidden, is_evergreen are NOT changed by the upsert (assert an existing is_visible value survives a content-columns-only re-upsert).
    - A row with ApprovalStatus="pending" upserted persists approval_status='pending' (the value is mirrored, not hardcoded to 'approved').
  </behavior>
  <action>
    In UpsertContentColumnsOnlySql: replace the literal `'pending'` in the INSERT VALUES list with a bound `@approvalStatus` parameter, and add `approval_status = EXCLUDED.approval_status` to the `ON CONFLICT (natural_key_type, natural_key_value) DO UPDATE SET` column list. This SQL const serves BOTH UpsertContentColumnsOnlyAsync (single) and UpsertContentColumnsOnlyBatchAsync — both get mirror semantics; no other SQL const changes. Leave is_visible, is_hidden, is_evergreen absent from the update set (operator-owned, per the DirectPush design SC3). In BuildUpsertParameters, add one binding `parameters.Add("approvalStatus", row.ApprovalStatus);` alongside the existing naturalKeyType/naturalKeyValue adds — mirroring the local row's value, not a constant (D-01 forbids a hidden 'approved' literal). Rewrite the trailing SQL comment: it currently states approval_status is "intentionally absent"; make it state that approval_status is now mirrored from the source row on insert and update, while is_visible/is_hidden/is_evergreen remain operator-owned and excluded (D-12 comment-truth requirement). Do NOT touch UpsertSql or UpsertPreservingVisibilitySql (different callers, out of scope). UPDATE THE TWO OLD-BEHAVIOR FACTS (Codex MED-1, they fail otherwise): retarget UpsertContentColumnsOnlyAsync_NewRow_LandsAsPending to assert the MIRROR semantics (a row created with the default ApprovalStatus "pending" still lands pending — rename to reflect mirroring, e.g. UpsertContentColumnsOnlyAsync_NewRow_MirrorsSourceApproval); rewrite UpsertContentColumnsOnlyAsync_ExistingRow_PreservesApprovalStatus — preservation is no longer the contract; convert it into the heal-the-drift regression (pending prod row + approved source ends approved, and the mirrored reverse case). Then add the four behavior cases above as [Fact] tests following the existing temp-SQLite fixture convention (reuse/extend the renamed facts where they overlap).
  </action>
  <verify>
    <automated>/mnt/c/Program\ Files/dotnet/dotnet.exe test DeckFlow.Core.Tests --filter "FullyQualifiedName~ContentSiteIndexStoreApprovalTests"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "@approvalStatus" DeckFlow.Core/Content/ContentSiteIndexStore.cs` shows the parameter in the INSERT VALUES list of UpsertContentColumnsOnlySql
    - `grep -n "approval_status = EXCLUDED.approval_status" DeckFlow.Core/Content/ContentSiteIndexStore.cs` returns a hit inside the ON CONFLICT DO UPDATE SET of UpsertContentColumnsOnlySql
    - `grep -n "'pending'" DeckFlow.Core/Content/ContentSiteIndexStore.cs` no longer matches inside UpsertContentColumnsOnlySql
    - No `is_visible`, `is_hidden`, or `is_evergreen` appears in the UpsertContentColumnsOnlySql DO UPDATE SET list
    - The trailing comment no longer asserts approval_status is "intentionally absent"
    - `grep -n "UpsertContentColumnsOnlyAsync_ExistingRow_PreservesApprovalStatus" DeckFlow.Core.Tests/Content/ContentSiteIndexStoreApprovalTests.cs` returns NOTHING (old preservation contract removed/renamed)
    - The full ContentSiteIndexStoreApprovalTests suite passes under `dotnet test` (no old-behavior fact left failing)
  </acceptance_criteria>
  <done>DirectPush's upsert (single + batch) mirrors the source row's approval_status on insert and update; operator-owned visibility columns stay excluded; old-behavior facts retargeted; tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Serve-side approval filter on BOTH public reads — browse list + detail-by-id (D-04, Codex HIGH + MED-1 + LOW)</name>
  <files>DeckFlow.Core/Content/ContentSiteIndexStore.cs, DeckFlow.Core/Content/IContentSiteIndexStore.cs, DeckFlow.Web/Controllers/ContentKbController.cs, DeckFlow.Core.Tests/Content/ContentSiteIndexStoreApprovalTests.cs, DeckFlow.Core.Tests/Content/ContentSiteIndexStoreVisibilityTests.cs, DeckFlow.Web.Tests/ContentKbControllerTests.cs, DeckFlow.Web/e2e/content-kb-pending-hidden.spec.ts</files>
  <read_first>
    - DeckFlow.Core/Content/ContentSiteIndexStore.cs — GetPublishedRowsAsync (~221, current `WHERE is_visible = @visible`), GetApprovedRowsAsync (~255, `WHERE approval_status = 'approved'` literal to copy), GetByIdAsync (~320, the unfiltered by-id read that stays unfiltered)
    - DeckFlow.Core/Content/IContentSiteIndexStore.cs — interface surface + xmldoc convention for the new member
    - DeckFlow.Web/Controllers/ContentKbController.cs — Index (~52) and Detail (~89-98, the `row is null || !row.IsVisible` check that leaks pending rows)
    - DeckFlow.Web.Tests/ContentKbControllerTests.cs — existing Detail_ReturnsNotFound_* facts and the store fake/double the controller tests use
    - DeckFlow.Core.Tests/Content/ContentSiteIndexStoreVisibilityTests.cs — GetPublishedRowsAsync_FiltersHiddenRows_AndGetByIdReturnsVisibility (~:77, seeds rows without approval — breaks after the filter)
    - DeckFlow.Web/e2e/smoke.spec.ts and DeckFlow.Web/playwright.config.ts — existing Playwright spec + server-launch convention
    - CLAUDE.md — UI-testing constraints (start server via scripts/run-web-test.sh; DECKFLOW_DISABLE_AUTO_BROWSER; never open a Windows-host browser; npx --no-install playwright test)
  </read_first>
  <behavior>
    - GetPublishedRowsAsync returns a row that is is_visible=true AND approval_status='approved'; excludes visible-but-pending and approved-but-hidden rows.
    - NEW GetPublishedByIdAsync(long id) returns the row only when is_visible=true AND approval_status='approved'; returns null for a visible-but-pending row, a hidden row, or a missing id.
    - ContentKbController.Detail returns 404 for a visible-but-pending row (direct navigation to /content-kb/{id} is blocked).
    - Existing GetByIdAsync stays unfiltered (admin/Studio still see pending rows).
  </behavior>
  <action>
    Browse list: change GetPublishedRowsAsync's WHERE clause from `WHERE is_visible = @visible` to `WHERE is_visible = @visible AND approval_status = 'approved'`, copying the literal exactly from GetApprovedRowsAsync. Detail route (Codex HIGH — the by-id path leaks pending rows): add a new store method `Task<ContentSiteIndexRow?> GetPublishedByIdAsync(long id, CancellationToken cancellationToken = default)` to IContentSiteIndexStore and ContentSiteIndexStore — same SELECT shape as GetByIdAsync but with `WHERE id = @id AND is_visible = @visible AND approval_status = 'approved'` (fix at the store/query layer per review guidance; GetByIdAsync itself stays unfiltered for admin/Studio callers). Switch ContentKbController.Detail (~:93) to call GetPublishedByIdAsync and drop the now-redundant `!row.IsVisible` check (`row is null` alone returns NotFound); Index stays on GetPublishedRowsAsync. The earlier "do NOT modify ContentKbController" guidance is relaxed exactly this far: Detail's read call changes, nothing else in the controller. INTERFACE RIPPLE: adding the member to IContentSiteIndexStore breaks every implementer — run `grep -rln "IContentSiteIndexStore" --include=*.cs` across the solution and add the member to each implementing test double/fake found (Web.Tests, Studio.Tests, Core.Tests); build ALL test projects (house rule: verify-builds-test-project). Tests: (a) store facts in ContentSiteIndexStoreApprovalTests — GetPublishedRowsAsync three-case matrix (approved+visible in; visible+pending out; approved+hidden out) and GetPublishedByIdAsync three-case matrix (approved+visible returns row; visible+pending returns null; hidden returns null); (b) web fact in ContentKbControllerTests — Detail_ReturnsNotFound_WhenRowVisibleButPending, following the existing Detail_ReturnsNotFound_* pattern; (c) OLD-BEHAVIOR FALLOUT (Codex MED-1): update GetPublishedRowsAsync_FiltersHiddenRows_AndGetByIdReturnsVisibility in ContentSiteIndexStoreVisibilityTests to set approval_status='approved' explicitly on rows it expects in the published set, and sweep BOTH store test files for any other fact that expects a row in GetPublishedRowsAsync without setting approval — every test intending a row to be public must set approval explicitly. E2E (Codex LOW — must assert exclusion, not just smoke): in content-kb-pending-hidden.spec.ts, seed a visible-but-pending row into the e2e server's SQLite content DB (direct SQLite insert into the harness DB before page load, with a unique title and a valid content-kb/ artifact path; follow the admin-e2e DB-sharing conventions), then assert (1) GET /content-kb renders 200 AND does NOT contain the seeded title, (2) direct GET /content-kb/{seededId} returns 404, (3) an approved+visible control row DOES render in the browse list.
  </action>
  <verify>
    <automated>/mnt/c/Program\ Files/dotnet/dotnet.exe test DeckFlow.Core.Tests --filter "FullyQualifiedName~ContentSiteIndexStoreApprovalTests|FullyQualifiedName~ContentSiteIndexStoreVisibilityTests"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "approval_status = 'approved'" DeckFlow.Core/Content/ContentSiteIndexStore.cs` returns hits inside GetPublishedRowsAsync AND GetPublishedByIdAsync (in addition to GetApprovedRowsAsync)
    - `grep -n "GetPublishedByIdAsync" DeckFlow.Core/Content/IContentSiteIndexStore.cs DeckFlow.Web/Controllers/ContentKbController.cs` returns a hit in each (interface member exists; Detail uses it)
    - `grep -n "GetByIdAsync" DeckFlow.Web/Controllers/ContentKbController.cs` returns NOTHING (detail no longer reads unfiltered)
    - ContentKbControllerTests contains a passing Detail_ReturnsNotFound_WhenRowVisibleButPending fact
    - The GetPublishedRowsAsync + GetPublishedByIdAsync case-matrix facts pass, and the updated VisibilityTests fact sets approval explicitly and passes
    - All test projects build (`dotnet build` on the solution) — every IContentSiteIndexStore fake implements the new member
    - content-kb-pending-hidden.spec.ts seeds a visible-but-pending row and asserts: browse page omits its title, /content-kb/{id} returns 404, and an approved control row renders
  </acceptance_criteria>
  <done>Both public read paths (browse + detail) exclude pending rows at the store/query layer; a drifted visible-while-pending row can render nowhere; xUnit + web + e2e prove it, and no old-behavior test is left failing.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Schema-ensure OFF switch + REQUIRED recording-connection no-DDL test (D-09/D-11, Codex MED-2)</name>
  <files>DeckFlow.Core/Content/ContentSiteIndexStore.cs, DeckFlow.Core.Tests/Content/ContentSiteIndexStoreSchemaEnsureSwitchTests.cs</files>
  <read_first>
    - DeckFlow.Core/Content/ContentSiteIndexStore.cs — ctors (~13-42), the `_schemaReady`/`_schemaGate` fields, EnsureSchemaAsync (~44), OpenConnectionAsync (~742, the one-line `_connectionInfo.OpenConnectionAsync` delegate — the injection seam), and any of the ~20 `await EnsureSchemaAsync(...)` call sites
    - DeckFlow.Core/Storage/RelationalDatabaseConnection.cs — sealed record with CreateConnection()/OpenConnectionAsync() (cannot be subclassed; the seam must live on the store)
    - DeckFlow.Core/AssemblyInfo.cs — InternalsVisibleTo("DeckFlow.Core.Tests") already present
    - DeckFlow.Core.Tests/Orchestration/RecordingOrchestratorDoubles.cs — the house Recording* double convention (records invoked operations, exposes IReadOnlyList for assertion)
    - DeckFlow.Core.Tests/Content/ContentSiteIndexStoreBatchUpsertTests.cs — the temp-SQLite-file ctor + write-path idiom to reuse
  </read_first>
  <behavior>
    - A store built with the schema-ensure switch ON (default) auto-creates its schema exactly as today (existing behavior unchanged — all current tests still pass).
    - REQUIRED (D-11): a prod-mode store (ensureSchemaEnabled:false) driven through the DirectPush read path (GetAllRowsAsync) and write path (UpsertContentColumnsOnlyBatchAsync) against a recording connection issues ZERO SQL commands matching CREATE/ALTER/DROP — attempted-but-failed DDL is caught, not just successful auto-create.
    - Supplemental: a switch-OFF store pointed at a schema-less SQLite file throws a "no such table" error on read and write (proves no auto-create as a cheap second signal).
    - No existing call site of EnsureSchemaAsync needs editing.
  </behavior>
  <action>
    Switch: add a ctor parameter `bool ensureSchemaEnabled = true` to the RelationalDatabaseConnection-taking constructor (the string-path ctor forwards true), stored in a readonly `_ensureSchemaEnabled` field; in EnsureSchemaAsync add an early `if (!_ensureSchemaEnabled) { return; }` as the very first statement — before the `_schemaReady` fast-path — so the ~20 auto-ensure call sites stay untouched (D-09). Plan 88-03 flips ProdStoreFactory to pass `ensureSchemaEnabled: false`. REQUIRED recording-connection test (Codex MED-2 — D-11 demands "zero CREATE/ALTER/DROP issued", which a schema-missing throw alone cannot prove against an existing-but-older schema): RelationalDatabaseConnection is a sealed record, so the seam lives on the store — add an `internal` constructor (house test-seam pattern, InternalsVisibleTo already grants access) accepting a connection-factory override, e.g. `Func<CancellationToken, Task<DbConnection>>`, which OpenConnectionAsync uses when supplied; the public ctors pass null and behave exactly as today. In DeckFlow.Core.Tests add Recording doubles following the RecordingOrchestratorDoubles naming convention: a RecordingDbConnection/RecordingDbCommand pair that wraps a real SqliteConnection, records every executed CommandText into an exposed IReadOnlyList<string>, and delegates execution. The REQUIRED fact: pre-create a full schema in a temp SQLite file (via a normal switch-ON store), then construct a prod-mode store (ensureSchemaEnabled:false) with the recording connection override, drive GetAllRowsAsync (DirectPush diff read) and UpsertContentColumnsOnlyBatchAsync (DirectPush publish write), and assert no recorded CommandText matches CREATE/ALTER/DROP (case-insensitive word-boundary match). Keep the supplemental schema-less fact: switch-OFF store on a fresh empty SQLite path throws on read and write (no auto-create). Both facts live in ContentSiteIndexStoreSchemaEnsureSwitchTests with the temp-file ctor/dispose fixture convention.
  </action>
  <verify>
    <automated>/mnt/c/Program\ Files/dotnet/dotnet.exe test DeckFlow.Core.Tests --filter "FullyQualifiedName~ContentSiteIndexStoreSchemaEnsureSwitchTests"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "ensureSchemaEnabled" DeckFlow.Core/Content/ContentSiteIndexStore.cs` shows the ctor parameter, the `_ensureSchemaEnabled` field, and the early return in EnsureSchemaAsync (first statement, before the `_schemaReady` check)
    - `grep -n "internal ContentSiteIndexStore" DeckFlow.Core/Content/ContentSiteIndexStore.cs` shows the connection-factory test seam ctor
    - `git diff` shows NO edits to any `await EnsureSchemaAsync(` call site
    - The REQUIRED recording fact exists and passes: prod-mode store + recording connection, GetAllRowsAsync + UpsertContentColumnsOnlyBatchAsync driven, zero recorded CommandText matching CREATE/ALTER/DROP
    - The supplemental schema-less fact passes: read and write both throw against an empty SQLite file (no auto-create); ON-case default-ctor behavior unchanged (existing suites pass)
    - Full `dotnet test DeckFlow.Core.Tests` build succeeds with no new warnings
  </acceptance_criteria>
  <done>The store has a one-switch schema-ensure OFF mode; the D-11 invariant (zero CREATE/ALTER/DROP on DirectPush read+write) is locked by a recording-connection test, with the schema-missing test as a supplemental signal.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public browser → web app → prod-mirrored index | Untrusted readers see whatever the public read paths return; a mislabeled row leaks unapproved content via the browse list OR direct /content-kb/{id} navigation |
| Studio (operator workstation) → prod Postgres (write) | DirectPush upsert crosses into prod; wrong column values or unintended DDL mutate the live DB |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-88-01 | Information Disclosure | GetPublishedRowsAsync + GetPublishedByIdAsync + UpsertContentColumnsOnlySql | mitigate | Mirror approval_status on insert/update (Task 1) AND filter BOTH public read paths on approval_status='approved' (Task 2) — browse list and detail-by-id, so a writer bug can never render a pending row anywhere |
| T-88-06 | Information Disclosure | ContentKbController.Detail direct-id navigation | mitigate | Detail reads via GetPublishedByIdAsync (store-layer filter); visible-but-pending row 404s; web test + e2e assert it (Codex HIGH) |
| T-88-02 | Tampering | ContentSiteIndexStore.EnsureSchemaAsync against prod | mitigate | Schema-ensure OFF switch (Task 3); REQUIRED recording-connection test asserts zero CREATE/ALTER/DROP on DirectPush read+write (D-11, Codex MED-2) |
| T-88-SC | Tampering (supply chain) | npm/NuGet installs | accept | No new packages added in this plan; test + e2e reuse existing xUnit + Playwright deps (CLAUDE.md forbids unapproved deps) |
</threat_model>

<verification>
- `dotnet test DeckFlow.Core.Tests` and `dotnet test DeckFlow.Web.Tests` pass (approval mirror, both serve filters, detail 404, recording-connection no-DDL, updated old-behavior facts).
- Full solution `dotnet build` clean, no new warnings — including ALL test projects (interface member ripple to fakes).
- `bash scripts/format-check-changed.sh` passes — changed-lines format gate (CLAUDE.md; run against the staged/changed C# lines before commit).
- Playwright: `npx --no-install playwright test content-kb-pending-hidden` green against the headless server started via scripts/run-web-test.sh — asserts seeded pending row hidden from browse AND 404 on direct detail navigation.
- Operator prod verification (D-14, manual, at merge/deploy time — NOT an automated gate): before merge, run the sanctioned read-only Render MCP query (`mcp__render__query_render_postgres`, id `dpg-d7oj8iugvqtc73fso0g0-a`) listing count + titles of rows where is_visible=true AND approval_status<>'approved', so the operator approves exactly what the serve filters will hide. After autodeploy, re-run the query and spot-check the public /content-kb page and one drifted row's /content-kb/{id} URL (expect 404).
</verification>

<success_criteria>
- DirectPush's prod upsert carries the source row's approval_status on insert and update; is_visible/is_hidden/is_evergreen stay operator-owned (SYNC-04).
- BOTH public KB read paths (browse list + detail-by-id) return only approved+visible rows — a pending row can render nowhere (SYNC-04 defense in depth, Codex HIGH closed).
- A prod-mode store issues zero CREATE/ALTER/DROP on the DirectPush read+write paths, proven by a recording-connection test (SYNC-06 store layer, D-11).
</success_criteria>

<output>
Create `.planning/phases/88-index-row-integrity-hotfix/88-01-SUMMARY.md` when done.
</output>
