---
phase: 66-admin-tool-visibility-toggles-tool-registry
plan: 05
type: execute
wave: 2
depends_on: [66-01]
files_modified:
  - DeckFlow.Web/Controllers/Admin/AdminToolsController.cs
  - DeckFlow.Web/Views/AdminTools/_ViewStart.cshtml
  - DeckFlow.Web/Views/AdminTools/Index.cshtml
  - DeckFlow.Web/Views/AdminLanding/Index.cshtml
  - DeckFlow.Web.Tests/AdminToolsControllerTests.cs
autonomous: true
requirements: [TOGGLE-02, TOGGLE-07]
must_haves:
  truths:
    - "Admin can enable/disable any of the 13 public tools from /Admin/Tools, grouped by nav section with friendly labels."
    - "The toggle persists via the flag store and synchronously reloads the cache, so the new state is visible on the redirect-target GET (same round-trip)."
    - "Disabling a core tool surfaces an inline warning banner but the toggle still succeeds (warn-not-block)."
    - "The page only toggles registry tool flags — infra flags are not exposed here; the raw /Admin/Flags page stays intact."
  artifacts:
    - path: "DeckFlow.Web/Controllers/Admin/AdminToolsController.cs"
      provides: "GET list + POST toggle (BasicAuth via /Admin branch, anti-forgery + SameOrigin)"
      contains: "ValidateAntiForgeryToken"
    - path: "DeckFlow.Web/Views/AdminTools/Index.cshtml"
      provides: "Section-grouped tool toggle UI + core warning banner"
    - path: "DeckFlow.Web/Views/AdminLanding/Index.cshtml"
      provides: "Tools admin-hub card"
      contains: "Admin/Tools"
  key_links:
    - from: "AdminToolsController.Toggle"
      to: "IFeatureFlagStore + IFeatureFlagCache.ReloadAsync"
      via: "SetEnabledAsync then ReloadAsync (mirror AdminFlagsController)"
      pattern: "ReloadAsync"
    - from: "AdminToolsController"
      to: "IToolRegistry"
      via: "restricts toggles to registry tool flag keys"
      pattern: "IToolRegistry"
---

<objective>
Build the dedicated `/Admin/Tools` page: a registry-driven, friendly-named, section-grouped on/off panel
for the 13 public tools, with an inline warning when a core workflow is disabled. Mirrors the proven
AdminFlagsController toggle (BasicAuth + anti-forgery + SameOrigin + same-round-trip ReloadAsync). The raw
/Admin/Flags page is left untouched for infra flags.

Purpose: TOGGLE-02 (admin toggles any tool) and TOGGLE-07 (core-tool warning, warn-not-block). Decision 2
(dedicated page, mirror AdminFlags pattern) and Decision 4 (3 core tools, inline warn-not-block, no JS confirm).
Output: AdminToolsController, AdminTools/Index view, AdminLanding card, controller tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/66-admin-tool-visibility-toggles-tool-registry/66-CONTEXT.md
@DeckFlow.Web/Controllers/Admin/AdminFlagsController.cs
@DeckFlow.Web/Views/AdminLanding/Index.cshtml

<interfaces>
<!-- Mirror AdminFlagsController exactly. Reuse its proven guards. -->
  IFeatureFlagStore.SetEnabledAsync(string key, bool enabled, CancellationToken)
  IFeatureFlagCache.Snapshot() : IReadOnlyDictionary<string,bool>
  IFeatureFlagCache.ReloadAsync(CancellationToken)              // D-10 same-round-trip
  SameOriginRequestValidator.IsValid(Request) / GetForbiddenMessage()   // HIGH-4 guard
  [ValidateAntiForgeryToken] on the POST
  From Plan 01: IToolRegistry.All (ToolDefinition: Key, Label, Route, Section, FlagKey, Core, Tab, ...)
  ToolVisibility is for public-surface filtering — the ADMIN page lists ALL tools regardless of flag state
  (you toggle disabled tools back on here), grouped by Section in enum order.

AdminFlags toggle contract to copy (AdminFlagsController.Toggle):
  - SameOrigin check → 403 with GetForbiddenMessage()
  - blank key → BadRequest
  - key not in cache snapshot → BadRequest("Unknown flag key.")
  - SetEnabledAsync + ReloadAsync + TempData success + RedirectToAction(Index)
  Route attribute: [Route("Admin/Tools")], [HttpGet("")], [HttpPost("{flagKey}/toggle")].
  /Admin is already behind BasicAuthMiddleware (Program.cs) — no extra auth wiring needed.

AdminLanding card markup pattern (existing): <a class="admin-hub-card" href="@Url.Content("~/Admin/Tools")"> ...
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: AdminToolsController (list + guarded toggle)</name>
  <files>DeckFlow.Web/Controllers/Admin/AdminToolsController.cs, DeckFlow.Web.Tests/AdminToolsControllerTests.cs</files>
  <behavior>
    - GET Index: returns a view model listing all 13 tools grouped by Section (enum order), each row carrying
      Label, FlagKey, Core, and current Enabled (from cache snapshot; default true if key absent), plus a
      collection of currently-disabled core tools for the warning banner.
    - POST Toggle cross-origin → 403, store NOT written.
    - POST Toggle with a flag key that is NOT a registry tool flag key → BadRequest, store NOT written
      (prevents toggling infra flags through this page).
    - POST Toggle with a valid registry tool flag key → SetEnabledAsync called once with that key + posted value,
      then ReloadAsync, then RedirectToAction(Index).
    - POST Toggle disabling a CORE tool → still writes + redirects (not blocked), and sets a TempData warning
      message naming the tool.
  </behavior>
  <action>
    Create `AdminToolsController : Controller` with `[Route("Admin/Tools")]`, injecting IFeatureFlagStore,
    IFeatureFlagCache, IToolRegistry. GET `[HttpGet("")]` Index: build `AdminToolsListViewModel` (define it in
    this file like AdminFlagsListViewModel) grouped by Section using registry.All + cache.Snapshot(); a tool row
    record holds Key, Label, FlagKey, Core, Enabled; include `DisabledCoreToolLabels` for the banner. POST
    `[HttpPost("{flagKey}/toggle")] [ValidateAntiForgeryToken]` Toggle(string flagKey, bool enabled, CT): copy
    AdminFlagsController.Toggle's SameOrigin 403 guard and blank-key BadRequest; THEN validate flagKey is a
    registry tool flag key (`registry.All.Any(t => t.FlagKey == flagKey)`) → else BadRequest("Unknown tool.");
    SetEnabledAsync + ReloadAsync; set TempData success; if the toggled tool is Core and `enabled == false`, set
    a TempData warning naming the tool's Label; RedirectToAction(Index). Honor get;init; carve-out on the view
    model records.
    Add `AdminToolsControllerTests` mirroring AdminFlagsControllerToggleTests (FakeFeatureFlagStore +
    FakeFeatureFlagCache + DefaultHttpContext with Origin header + StubTempDataProvider): cover the five
    &lt;behavior&gt; POST cases plus a GET test asserting 13 rows across 4 sections and the disabled-core list.
  </action>
  <verify>
    <automated>dotnet build DeckFlow.Web/DeckFlow.Web.csproj -c Debug 2>&1 | grep -E "error|Build succeeded" ; dotnet test DeckFlow.Web.Tests/DeckFlow.Web.Tests.csproj --filter "FullyQualifiedName~AdminToolsControllerTests"</automated>
  </verify>
  <done>Controller lists 13 tools grouped by section; toggle is CSRF+SameOrigin guarded, tool-flag-only, reloads cache; core-disable warns not blocks; tests green.</done>
</task>

<task type="auto">
  <name>Task 2: AdminTools view + AdminLanding card</name>
  <files>DeckFlow.Web/Views/AdminTools/_ViewStart.cshtml, DeckFlow.Web/Views/AdminTools/Index.cshtml, DeckFlow.Web/Views/AdminLanding/Index.cshtml</files>
  <action>
    MED-2 FIRST: create `Views/AdminTools/_ViewStart.cshtml` containing exactly `@{ Layout = "_AdminLayout"; }`
    (every existing admin view folder — AdminFlags, AdminLanding, etc. — has this local _ViewStart; without it
    the new page renders under the public site layout instead of the admin shell).
    Then create `Views/AdminTools/Index.cshtml` modeled on `Views/AdminFlags/Index.cshtml`: render the success
    TempData banner; render an inline WARNING banner (distinct styling, reuse an existing admin alert/warn class
    if present, else a `.admin-alert--warn` style consistent with the admin CSS) whenever
    Model.DisabledCoreToolLabels is non-empty, naming those tools and explaining they are core workflows that are
    currently hidden. For each Section render a heading and a table/list of its tools: friendly Label, current
    state, and a toggle form POSTing to `~/Admin/Tools/@row.FlagKey/toggle` with `@Html.AntiForgeryToken()` and a
    hidden `enabled` = the flipped value (mirror AdminFlags toggle form). Mark core tools with a small "core"
    badge. No JS confirm dialog (Decision 4). Add a `Tools` card to `Views/AdminLanding/Index.cshtml` linking
    `~/Admin/Tools` with title "Tools" and description "Turn public tools on or off; cascades to home tile, nav,
    help, and route." — touch only the added card markup (changed-lines format gate).
  </action>
  <verify>
    <automated>dotnet build DeckFlow.Web/DeckFlow.Web.csproj -c Debug 2>&1 | grep -E "error|Build succeeded"</automated>
  </verify>
  <done>AdminTools/_ViewStart sets _AdminLayout; /Admin/Tools renders section-grouped toggles + core warning banner under the admin shell; AdminLanding has a Tools card; build clean. (Live render verified in 66-06.)</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin browser → /Admin/Tools POST | authenticated mutating endpoint behind BasicAuth |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-66-08 | Spoofing/CSRF | POST /Admin/Tools/{flagKey}/toggle | mitigate | [ValidateAntiForgeryToken] + SameOriginRequestValidator (copied from AdminFlags), BasicAuth on /Admin |
| T-66-09 | Tampering | toggling an arbitrary/infra flag via this page | mitigate | flagKey validated against registry tool flag keys; non-tool keys → BadRequest |
| T-66-10 | Elevation | disabling a core tool by accident | accept | TOGGLE-07 = warn not block; inline banner names disabled core tools |
| T-66-SC | Tampering | npm/nuget installs | mitigate | no new packages |
</threat_model>

<success_criteria>
- /Admin/Tools lists all 13 tools grouped by section with working on/off toggles (same-round-trip).
- Toggle is anti-forgery + SameOrigin guarded and refuses non-tool flag keys.
- Disabling a core tool warns inline but proceeds.
- /Admin/Flags untouched; AdminLanding links the new page.
- dotnet build clean; controller tests green.
</success_criteria>

<output>
Create `.planning/phases/66-admin-tool-visibility-toggles-tool-registry/66-05-SUMMARY.md` when done.
</output>
