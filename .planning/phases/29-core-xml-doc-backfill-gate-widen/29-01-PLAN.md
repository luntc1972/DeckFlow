---
phase: 29-core-xml-doc-backfill-gate-widen
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - DeckFlow.Core/Storage/IRelationalDialect.cs
  - DeckFlow.Core/Storage/PostgresRelationalDialect.cs
  - DeckFlow.Core/Storage/SqliteRelationalDialect.cs
  - DeckFlow.Core/Storage/RelationalDatabaseConnection.cs
autonomous: true
requirements: [HSK-01]

must_haves:
  truths:
    - "Probe build over DeckFlow.Core/Storage/*.cs reports 0 CS1591/CS1573/CS1587 warnings"
    - "Every public type and member in the 4 Storage files has an XML doc comment"
    - "No raw-string literal content or indentation changed in PostgresRelationalDialect.cs or SqliteRelationalDialect.cs"
  artifacts:
    - path: "DeckFlow.Core/Storage/IRelationalDialect.cs"
      provides: "Documented relational-dialect interface (3 missing property docs added)"
      contains: "/// <summary>"
    - path: "DeckFlow.Core/Storage/PostgresRelationalDialect.cs"
      provides: "Documented Postgres dialect impl (5 sites)"
      contains: "/// <summary>"
    - path: "DeckFlow.Core/Storage/SqliteRelationalDialect.cs"
      provides: "Documented SQLite dialect impl (5 sites)"
      contains: "/// <summary>"
    - path: "DeckFlow.Core/Storage/RelationalDatabaseConnection.cs"
      provides: "Documented connection handle (8 sites)"
      contains: "/// <summary>"
  key_links:
    - from: "DeckFlow.Core/Storage/PostgresRelationalDialect.cs"
      to: "DeckFlow.Core/Storage/IRelationalDialect.cs"
      via: "inheritdoc on interface-implementing members"
      pattern: "inheritdoc"
---

<objective>
Add XML doc comments to every undocumented public type and member in the `DeckFlow.Core/Storage/` folder (4 files, ~21 compiler-reported sites: IRelationalDialect 3, PostgresRelationalDialect 5, SqliteRelationalDialect 5, RelationalDatabaseConnection 8).

Purpose: Storage is the highest-density folder of the Core doc backfill (HSK-01) and contains two LOW-risk raw-string files. Documenting it cleanly is a prerequisite for the Wave-2 `.editorconfig` gate widen, which must build green.
Output: 4 fully-documented Storage source files; probe build over Storage reports 0 doc warnings.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<scope_fence>
ALLOWED FILE SET (Codex MUST NOT touch any file outside this list):
- DeckFlow.Core/Storage/IRelationalDialect.cs
- DeckFlow.Core/Storage/PostgresRelationalDialect.cs
- DeckFlow.Core/Storage/SqliteRelationalDialect.cs
- DeckFlow.Core/Storage/RelationalDatabaseConnection.cs

You may create and delete a TEMPORARY `DeckFlow.Core/.editorconfig` probe file ONLY for verification; it MUST be removed before commit and MUST NOT appear in `git status`.
Do NOT touch `.editorconfig` at repo root. Do NOT touch any other Core folder. Ignore tasks belonging to plans 29-02, 29-03, 29-04, 29-05.
Before starting, run `git status --short` and confirm a clean tree scoped to these files; if other modified files appear, STOP and report.
</scope_fence>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-core-xml-doc-backfill-gate-widen/29-RESEARCH.md
@.planning/phases/29-core-xml-doc-backfill-gate-widen/29-PATTERNS.md

# Gold-standard doc-style exemplars (READ - match this style exactly):
@DeckFlow.Core/Content/IContentVideoStore.cs
@DeckFlow.Core/Content/ContentVideoStore.cs
</context>

<constraints>
From CLAUDE.md + 29-RESEARCH.md (full authority - every constraint is blocking):
- Touch ONLY the `///` doc lines you add. Do NOT run Format Document / Code Cleanup / ReSharper reformat.
- NEVER convert `{ get; init; }` to `{ get; }` (System.Text.Json drops get-only props in .NET 9+). None of these files contain init accessors, but the rule stands.
- NEVER re-indent C# raw-string literals (triple-quote). PostgresRelationalDialect.cs and SqliteRelationalDialect.cs each contain 2 SQL column-type raw strings - doc `///` lines go ABOVE member declarations only, never inside a raw-string block.
- NEVER inline an attribute onto a declaration line.
- Preserve LF line endings (.gitattributes enforces).
- Build env: `"/mnt/c/Program Files/dotnet/dotnet.exe"`. VSTest unreliable in WSL - clean build is the gate.
- Commits: plain default-author (no Co-Authored-By trailer); one logical change per commit.
</constraints>

<tasks>

<task type="auto">
  <name>Task 1: Re-run the doc-warning probe to confirm the current Storage work list</name>
  <files>DeckFlow.Core/Storage/IRelationalDialect.cs</files>
  <read_first>
    - .planning/phases/29-core-xml-doc-backfill-gate-widen/29-RESEARCH.md (Code Examples, "Probe build methodology"; "Per-file counts")
  </read_first>
  <action>
    Create a temporary `DeckFlow.Core/.editorconfig` with `root = false` and a `[*.cs]` section setting CS1591, CS1573, CS1587 severity to `warning` (exact content per RESEARCH.md probe methodology). Run a clean non-incremental build of `DeckFlow.Core/DeckFlow.Core.csproj` via the Windows dotnet.exe path, pipe through `grep -E "warning CS(1591|1573|1587)"` then `sort -u` (non-incremental build duplicates each warning - dedup is required). Filter the deduped list to the four Storage files. This deduped list is the authoritative per-file work list for this plan; RESEARCH counts (IRelationalDialect 3, Postgres 5, Sqlite 5, RelationalDatabaseConnection 8) are the expected starting point but may have drifted. Remove the temp `DeckFlow.Core/.editorconfig` and confirm it is gone from the working tree.
  </action>
  <verify>
    <automated>if [ -f DeckFlow.Core/.editorconfig ]; then echo "FAIL probe file lingering"; else echo "OK probe removed"; fi</automated>
  </verify>
  <acceptance_criteria>
    - The probe produced a deduped list of CS1591/CS1573/CS1587 sites scoped to the 4 Storage files.
    - `DeckFlow.Core/.editorconfig` does not exist after this task and does not appear in `git status`.
    - The work list for subsequent tasks is the probe output, not a memorized count.
  </acceptance_criteria>
  <done>A deduped Storage-only doc-warning site list exists and the temp probe editorconfig is removed.</done>
</task>

<task type="auto">
  <name>Task 2: Document IRelationalDialect plus the two dialect implementations</name>
  <files>DeckFlow.Core/Storage/IRelationalDialect.cs, DeckFlow.Core/Storage/PostgresRelationalDialect.cs, DeckFlow.Core/Storage/SqliteRelationalDialect.cs</files>
  <read_first>
    - DeckFlow.Core/Storage/IRelationalDialect.cs (current state - one property already documented, 3 bare)
    - DeckFlow.Core/Storage/PostgresRelationalDialect.cs (current state - note the 2 SQL raw-string column-type values)
    - DeckFlow.Core/Storage/SqliteRelationalDialect.cs (current state - note the 2 SQL raw-string column-type values)
    - .planning/phases/29-core-xml-doc-backfill-gate-widen/29-PATTERNS.md (Pattern 8 IRelationalDialect property docs; Pattern 2 inheritdoc; one-line form)
    - DeckFlow.Core/Content/IContentVideoStore.cs (interface property/method doc style)
  </read_first>
  <action>
    For `IRelationalDialect.cs`: add a single-line `/// <summary>` to each of the 3 undocumented interface properties - `FeedbackCreatedUtcColumnType`, `FeedbackOrderByClause`, `FeedbackInsertReturningIdSql` (concrete summaries per 29-PATTERNS.md Pattern 8). Do NOT rewrite the already-documented `SurrogateIdColumnType` doc. For `PostgresRelationalDialect.cs` and `SqliteRelationalDialect.cs`: each has 5 undocumented sites (the class type plus its members implementing IRelationalDialect). Add a multi-line `<summary>` on each class type describing it as the Postgres/SQLite implementation of `IRelationalDialect`, and add `/// <inheritdoc />` (with the space before the close) above each interface-implementing member rather than repeating the interface summary. The `///` lines go ABOVE the property/member declaration line; never place a `///` line inside a raw-string block, and do not re-indent those literals.
  </action>
  <verify>
    <automated>grep -c "inheritdoc" DeckFlow.Core/Storage/PostgresRelationalDialect.cs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "/// <summary>" DeckFlow.Core/Storage/IRelationalDialect.cs` is at least 4 (1 prior plus 3 new).
    - Both dialect files contain `inheritdoc` on members and a class-level `<summary>`.
    - `git diff` for both dialect files shows only added `///` lines - no changes to any line containing a triple-quote delimiter or to lines between delimiters.
    - No `{ get; }` appears where `{ get; init; }` previously existed (none expected).
  </acceptance_criteria>
  <done>IRelationalDialect and both dialect implementations are fully documented; raw strings untouched.</done>
</task>

<task type="auto">
  <name>Task 3: Document RelationalDatabaseConnection and probe-verify the Storage folder is clean</name>
  <files>DeckFlow.Core/Storage/RelationalDatabaseConnection.cs</files>
  <read_first>
    - DeckFlow.Core/Storage/RelationalDatabaseConnection.cs (current state - 8 sites; some members may already be partially documented)
    - .planning/phases/29-core-xml-doc-backfill-gate-widen/29-PATTERNS.md (Pattern 3 constructor docs; Pattern 1 method docs; shared patterns for paramref / see cref / see langword null)
    - DeckFlow.Core/Content/ContentVideoStore.cs (constructor + factory + method doc style, including paramref usage)
  </read_first>
  <action>
    Add XML doc comments to the 8 undocumented public sites in `RelationalDatabaseConnection.cs` per the probe list from Task 1. Use: multi-line `<summary>` on the type if undocumented; constructor summaries starting with "Creates" or "Initializes"; factory-method summaries (e.g., `FromSqlitePath`) using `<paramref name="..."/>` for inline param references and `<param>` for each parameter; `<returns>` only on members that return a value, with `<see langword="null"/>` phrasing for nullable returns. Match the exemplar style in ContentVideoStore.cs exactly (imperative mood, no trailing period on `<param>` noun phrases, no blank line between the `///` block and the declaration). Then re-run the Task-1 probe and confirm ZERO CS1591/CS1573/CS1587 warnings remain for any file under `DeckFlow.Core/Storage/`.
  </action>
  <verify>
    <automated>DOTNET="/mnt/c/Program Files/dotnet/dotnet.exe"; printf '%s\n' 'root = false' '' '[*.cs]' 'dotnet_diagnostic.CS1591.severity = warning' 'dotnet_diagnostic.CS1573.severity = warning' 'dotnet_diagnostic.CS1587.severity = warning' > DeckFlow.Core/.editorconfig; "$DOTNET" build DeckFlow.Core/DeckFlow.Core.csproj --no-incremental 2>&1 | grep -E "warning CS(1591|1573|1587)" | grep "Storage/" | sort -u > /tmp/29-01-storage.log; rm -f DeckFlow.Core/.editorconfig; if [ -s /tmp/29-01-storage.log ]; then echo FAIL; cat /tmp/29-01-storage.log; else echo "OK 0 Storage doc warnings"; fi</automated>
  </verify>
  <acceptance_criteria>
    - Probe build (temp editorconfig override) yields 0 CS1591/CS1573/CS1587 warnings for any `DeckFlow.Core/Storage/` file (`/tmp/29-01-storage.log` empty).
    - `DeckFlow.Core/.editorconfig` removed; `git status --short` does not list it.
    - `git diff --stat` lists only the 4 Storage files.
    - `"/mnt/c/Program Files/dotnet/dotnet.exe" build DeckFlow.Core/DeckFlow.Core.csproj -c Release` reports 0 errors (suppressor still active - sanity build).
  </acceptance_criteria>
  <done>All 4 Storage files documented; Storage probe shows 0 doc warnings; temp probe file removed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer edit -> source tree | Mass `///` insertions across SQL-bearing files risk accidental code/literal mutation |
| temp probe file -> repo | A lingering `DeckFlow.Core/.editorconfig` would alter solution-wide warning severity for everyone |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-01-01 | Tampering | Raw-string SQL in Postgres/Sqlite dialect files | mitigate | `///` lines added above declarations only; git diff acceptance asserts no triple-quote line changed |
| T-29-01-02 | Tampering | Temp probe `DeckFlow.Core/.editorconfig` | mitigate | Probe file removed and absence asserted in every probe task; never committed |
| T-29-01-03 | Denial of Service | Build pipeline | accept | Doc comments are compile-stripped; zero runtime/behavior change possible |
| T-29-01-SC | Tampering | npm/pip/cargo installs | n/a | No package installs in this phase |
</threat_model>

<verification>
- Probe build (temp editorconfig) reports 0 CS1591/CS1573/CS1587 for `DeckFlow.Core/Storage/`.
- `git status --short` shows no `DeckFlow.Core/.editorconfig`.
- `git diff --stat` lists only the 4 allowed Storage files.
- Full Core build clean (0 errors) under the still-active global suppressor.
</verification>

<success_criteria>
All 21 undocumented public sites in the 4 Storage files carry XML doc comments matching the Content KB exemplar style; Storage-scoped probe build is warning-free; no raw-string literals or init accessors mutated; only allowed files changed.
</success_criteria>

<output>
Create `.planning/phases/29-core-xml-doc-backfill-gate-widen/29-01-SUMMARY.md` when done.
</output>
