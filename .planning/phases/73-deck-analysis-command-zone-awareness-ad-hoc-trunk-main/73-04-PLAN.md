---
phase: 73-deck-analysis-command-zone-awareness-ad-hoc-trunk-main
plan: 04
type: execute
wave: 4
depends_on: ["73-03"]
files_modified:
  - DeckFlow.Web/Models/DeckAnalysisViewModel.cs
  - DeckFlow.Web/Controllers/DeckPacketController.cs
  - DeckFlow.Web/Views/Deck/DeckAnalysis.cshtml
  - DeckFlow.Web/Help/deck-analysis.md
  - README.md
  - DeckFlow.Web/e2e/deck-analysis-command-zone.spec.ts
autonomous: false
requirements: [SPEC-72-E]

must_haves:
  truths:
    - "When analysis.command-zone-awareness is ON, the deck-analysis Step 1 form shows an optional companion designator input (collapsible, parity with the manabase companion input)"
    - "The typed companion designator round-trips through DeckAnalysisRequest.CompanionName and is honored by the service (designator wins over auto-detect), so Archidekt and pasted-text decks can name a companion"
    - "When the flag is OFF, the companion input is not rendered and the page is unchanged"
    - "Every DeckAnalysisViewModel render path (including error/upload paths) sets CommandZoneAwarenessEnabled consistently via one factory, so the flag-gated UI cannot appear on some paths and not others"
    - "README and in-app Help describe the new flag and the companion designator"
  artifacts:
    - path: "DeckFlow.Web/Views/Deck/DeckAnalysis.cshtml"
      provides: "Flag-gated single companion designator input bound to CompanionName (no hidden mirror)"
      contains: "CompanionName"
    - path: "DeckFlow.Web/e2e/deck-analysis-command-zone.spec.ts"
      provides: "Playwright smoke: input renders when flag ON, absent when OFF"
      contains: "command-zone"
    - path: "README.md"
      provides: "Documents analysis.command-zone-awareness flag + companion designator"
      contains: "command-zone-awareness"
  key_links:
    - from: "DeckAnalysis.cshtml companion input (name=CompanionName)"
      to: "DeckAnalysisRequest.CompanionName"
      via: "form post round-trip"
      pattern: "CompanionName"
    - from: "DeckPacketController flag factory (all 11 view-model sites)"
      to: "DeckAnalysisViewModel.CommandZoneAwarenessEnabled"
      via: "single helper stamps Snapshot().TryGetValue default-OFF"
      pattern: "command-zone-awareness"
---

<objective>
Surface the companion designator in the deck-analysis UI so the awareness feature works for Archidekt
and pasted-text decks (which never emit `DetectedCompanionName` — research §RQ3/§Pitfall 5), at parity
with the manabase companion input. The field is gated on `analysis.command-zone-awareness`; when the
flag is OFF the page is unchanged.

Scope note (recorded decision): the designator is a MANUAL input — its primary value is decks where
auto-detection cannot fire (Archidekt, pasted text, Moxfield-Spellbook fallback). Auto-detect pre-fill
into the visible input is NOT required here because the detected companion is only known after the
Step-2 build, and Moxfield-direct decks already get the companion via auto-detect in the service
(Plans 02/03). The typed value round-trips via the form-bound `Request.CompanionName` (added in Plan 01)
and the service already prefers the designator over the detected value (`ResolveCompanionName`, Plan 02).

**Blast-radius control (Codex MED-1):** `DeckPacketController` constructs `new DeckAnalysisViewModel`
at ELEVEN sites (lines 57, 106, 126, 136, 209, 219, 240, 250, 264, 285, 294), including error and upload
paths. The flag must be stamped through a SINGLE factory/helper so no path can render the wrong UI.

**Single form, single input (Codex MED-1):** `DeckAnalysis.cshtml` is ONE long `<form>` spanning all
steps (line 86), unlike `Manabase.cshtml` whose hidden `CompanionName` mirror (line 524) exists only
because its companion input lives in a SEPARATE download form (line 516). So this plan adds exactly ONE
visible `name="CompanionName"` input and NO hidden mirror — a second field would duplicate-bind on POST.

Purpose: complete companion coverage across all deck sources + document the feature.
Output: flag-gated Step-1 companion input (single field), centralized controller flag plumbing,
README + Help, Playwright smoke, and an operator cross-theme/mobile visual sign-off.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/73-deck-analysis-command-zone-awareness-ad-hoc-trunk-main/73-RESEARCH.md
@.planning/phases/73-deck-analysis-command-zone-awareness-ad-hoc-trunk-main/73-VALIDATION.md
@DeckFlow.Web/Views/Deck/Manabase.cshtml
@DeckFlow.Web/Views/Deck/DeckAnalysis.cshtml

<interfaces>
<!-- Parity anchors. Extracted from codebase 2026-06-27. -->

Manabase companion input (Manabase.cshtml lines 89-93) — the VISIBLE-input parity pattern:
  inside <details class="manabase-overrides">, a <label for="manabase-companion-name"> +
  <input name="CompanionName" ... value="@(Model.CompanionCallout?.Name ?? Model.Request.CompanionName)">
  The hidden mirror at line 524 is in a SEPARATE download form (line 516) — it exists ONLY because
  Manabase has two forms. DeckAnalysis has ONE form, so DO NOT copy the hidden mirror (MED-1):
  a second name="CompanionName" would duplicate-bind. One visible input is enough.

DeckAnalysis.cshtml: @model DeckFlow.Web.Models.DeckAnalysisViewModel; the single Step-1..N deck form
  begins at <form method="post" action="~/deck-analysis"> (line 86); currentStep var at line 5.

DeckAnalysisViewModel (DeckFlow.Web/Models/DeckAnalysisViewModel.cs) — add `bool CommandZoneAwarenessEnabled { get; init; }`.
DeckPacketController (DeckFlow.Web/Controllers/DeckPacketController.cs) — does NOT yet inject a flag
  cache; constructs `new DeckAnalysisViewModel { ... }` at 11 sites (lines 57, 106, 126, 136, 209,
  219, 240, 250, 264, 285, 294). Default-OFF flag read pattern (mirror the service):
  _flagCache.Snapshot().TryGetValue("analysis.command-zone-awareness", out var on) && on.

Existing flagged-feature e2e analog: DeckFlow.Web/e2e/manabase-commander-callout.spec.ts (how a flag is
  enabled for the test + how the companion input is asserted).

Run server for UI tests: scripts/run-web-test.sh (sets DECKFLOW_DISABLE_AUTO_BROWSER=true). Drive with
  `npx --no-install playwright test`. NEVER open a browser on the Windows host.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Centralized flag plumbing + single companion designator input + docs</name>
  <files>DeckFlow.Web/Models/DeckAnalysisViewModel.cs, DeckFlow.Web/Controllers/DeckPacketController.cs, DeckFlow.Web/Views/Deck/DeckAnalysis.cshtml, DeckFlow.Web/Help/deck-analysis.md, README.md</files>
  <read_first>
    - DeckFlow.Web/Views/Deck/Manabase.cshtml lines 85-95 (visible companion input — the parity pattern) and 516-526 (the hidden mirror lives in a SEPARATE download form — do NOT copy it here)
    - DeckFlow.Web/Views/Deck/DeckAnalysis.cshtml lines 1-100 (model, currentStep, the single Step-1 deck form start at line 86)
    - DeckFlow.Web/Models/DeckAnalysisViewModel.cs (init-property style)
    - DeckFlow.Web/Controllers/DeckPacketController.cs ctor (lines 16-48) and ALL 11 `new DeckAnalysisViewModel { ... }` sites (lines 57, 106, 126, 136, 209, 219, 240, 250, 264, 285, 294)
    - README.md lines 214-224 (manabase flag/companion doc style); DeckFlow.Web/Help/deck-analysis.md (structure)
  </read_first>
  <action>
    Add `bool CommandZoneAwarenessEnabled { get; init; }` (default false) to DeckAnalysisViewModel.
    Inject `IFeatureFlagCache? flagCache` into DeckPacketController (constructor param + null-safe field,
    like the service). Add a SINGLE private helper that reads the flag once with the default-OFF snapshot
    pattern — e.g. `private bool CommandZoneAwarenessEnabled() => _flagCache is not null && _flagCache.Snapshot().TryGetValue("analysis.command-zone-awareness", out var on) && on;`
    — and set `CommandZoneAwarenessEnabled = CommandZoneAwarenessEnabled()` on EVERY one of the 11
    `new DeckAnalysisViewModel` initializers (including the error and upload paths) so the flag-gated UI
    is consistent on every render path (MED-1). (If preferred, wrap construction in one private factory
    method that stamps the flag and is called by all 11 sites — either approach must cover all 11.) This
    is controller-only UI plumbing — do NOT add any shared prompt helper (ADR 0001 is about prompt
    variants, not controllers).
    In DeckAnalysis.cshtml, inside the single Step-1 deck form, when `Model.CommandZoneAwarenessEnabled`
    is true, render a collapsible companion designator: a `<details>` block (mirror `manabase-overrides`)
    containing ONE labeled `<input name="CompanionName">` pre-filled with `@Model.Request.CompanionName`.
    Render EXACTLY ONE element named `CompanionName` — do NOT add a hidden mirror (single form →
    duplicate model binding, MED-1). Use existing site-common / form classes — put any new layout CSS in
    `site-common.css`, never `site.css` (theme constraint); do not fork per-theme files.
    Add a README bullet near the manabase flag docs describing `analysis.command-zone-awareness`
    (default OFF, byte-identical when off, names the full command zone + companion as side metadata in
    /deck-analysis prompts) and the companion designator. Add a short Help section to
    `DeckFlow.Web/Help/deck-analysis.md` describing the companion designator and when to use it
    (Archidekt / pasted decks). New file lines use CRLF unless the file is already LF; do not reflow
    existing lines.
  </action>
  <verify>
    <automated>dotnet.exe build DeckFlow.Web/DeckFlow.Web.csproj 2>&1 | tail -5; grep -c 'name="CompanionName"' DeckFlow.Web/Views/Deck/DeckAnalysis.cshtml</automated>
  </verify>
  <acceptance_criteria>
    - DeckAnalysisViewModel has `CommandZoneAwarenessEnabled` init bool
    - DeckPacketController injects a flag cache and sets the bool via `Snapshot().TryGetValue("analysis.command-zone-awareness", ...)` (default-OFF, not IsEnabled())
    - ALL 11 `new DeckAnalysisViewModel` sites (lines 57, 106, 126, 136, 209, 219, 240, 250, 264, 285, 294) set `CommandZoneAwarenessEnabled` through the single shared helper/factory — error and upload paths included (verify: every initializer references the helper, none is left unstamped)
    - `grep -c 'name="CompanionName"' DeckAnalysis.cshtml` returns exactly 1 (one visible input, NO hidden mirror)
    - DeckAnalysis.cshtml companion input is gated on `Model.CommandZoneAwarenessEnabled`
    - README.md contains `analysis.command-zone-awareness`; Help/deck-analysis.md mentions the companion designator
    - `dotnet.exe build DeckFlow.Web` is clean (no new warnings); any new layout CSS lives in site-common.css
  </acceptance_criteria>
  <done>The Step-1 form shows ONE optional companion designator only when the flag is ON; every controller render path stamps the flag via a single helper; the typed value round-trips via CompanionName and is honored by the service; README + Help document the flag and the field.</done>
</task>

<task type="auto">
  <name>Task 2: Playwright smoke — companion input renders when flag ON, absent when OFF</name>
  <files>DeckFlow.Web/e2e/deck-analysis-command-zone.spec.ts</files>
  <read_first>
    - DeckFlow.Web/e2e/manabase-commander-callout.spec.ts (full — how the flag is enabled for the test and how the companion input is located/asserted)
    - DeckFlow.Web/e2e/deck-analysis-render.spec.ts (deck-analysis page navigation pattern)
    - scripts/run-web-test.sh (server startup with DECKFLOW_DISABLE_AUTO_BROWSER)
  </read_first>
  <action>
    Add a Playwright spec `deck-analysis-command-zone.spec.ts` mirroring the flag-enabling + assertion
    approach in `manabase-commander-callout.spec.ts`. Two cases: (1) with `analysis.command-zone-awareness`
    enabled, load `/deck-analysis` and assert the single companion designator input (`input[name="CompanionName"]`)
    is present/visible; (2) with the flag disabled (or default), assert the companion input is absent.
    Reuse the spec's existing flag-toggle helper / admin path rather than hand-rolling a new one. Keep it a
    thin smoke — no deck build / Scryfall round-trip.
  </action>
  <verify>
    <automated>cd DeckFlow.Web && npx --no-install playwright test e2e/deck-analysis-command-zone.spec.ts --reporter=line</automated>
  </verify>
  <acceptance_criteria>
    - `deck-analysis-command-zone.spec.ts` exists with a flag-ON case asserting the `input[name="CompanionName"]` is shown and a flag-OFF case asserting it is absent
    - The spec reuses the flag-enabling pattern from manabase-commander-callout.spec.ts (no new bespoke admin flow)
    - `npx --no-install playwright test e2e/deck-analysis-command-zone.spec.ts` passes (server started via scripts/run-web-test.sh; no Windows-host browser opened)
  </acceptance_criteria>
  <done>A Playwright smoke proves the companion input appears only when the flag is ON, run headless without opening a Windows browser.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Operator cross-theme + mobile visual sign-off of the companion designator</name>
  <what-built>A flag-gated single companion designator input on the deck-analysis Step-1 form (collapsible, parity with the manabase companion input), plus README/Help docs and a Playwright smoke.</what-built>
  <how-to-verify>
    1. Start the server: `scripts/run-web-test.sh` (DECKFLOW_DISABLE_AUTO_BROWSER set; do NOT open a Windows-host browser — use the headless server + a manually-opened browser or Playwright screenshots).
    2. With `analysis.command-zone-awareness` enabled via /Admin/Flags, open `/deck-analysis` Step 1 and confirm the companion designator input renders, is labeled, and is collapsible.
    3. Verify at desktop (≥1200px) and mobile (≤480px) viewports, on the default theme plus at least one guild theme (e.g. azorius/nyx), in both light and dark mode — the input is legible, aligned, and not clipped.
    4. Disable the flag and confirm the input disappears and the page layout is unchanged.
    5. Enter a companion (e.g. "Jegantha, the Wellspring") on an Archidekt deck, build Step 2, and confirm the companion is named in the generated prompt artifact (DECK CONTEXT / `<companion>`), and that submitting does NOT produce a duplicate/double CompanionName binding error.
  </how-to-verify>
  <resume-signal>Type "approved" or describe the visual/layout issues to fix.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser form (CompanionName input) → DeckPacketController → DeckAnalysisRequest → service | User-typed companion designator crosses here; untrusted |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-73-01 | Tampering | Companion designator input → prompt | mitigate | The service single-line-bounds + 200-char caps the value (`BoundCompanionName`, Plan 02) and Claude XML-escapes it (Plan 03) before it reaches any prompt. The Razor input echoes `Request.CompanionName` as text in a value attribute — Razor HTML-encodes attribute output by default, so reflected XSS on the page is mitigated; the prompt artifact itself is plain text (not HTML). Exactly one CompanionName field exists, so there is no second uncontrolled bind site. |
| T-73-05 | Spoofing/Tampering | Crafted POST sets CommandZoneAwarenessEnabled | accept | `CommandZoneAwarenessEnabled` is server-computed from the flag cache and init-only on the view model; it is not form-bound, so a crafted POST cannot force the input or enable the feature. |
| T-73-SC | Tampering | npm/pip/cargo installs | accept | No package-manager installs in this phase (Playwright already present per package.json). |
</threat_model>

<verification>
- `dotnet.exe build DeckFlow.Web` clean, no new warnings; full Web test suite green (`dotnet.exe test DeckFlow.Web.Tests/DeckFlow.Web.Tests.csproj -q`).
- `grep -c 'name="CompanionName"' DeckFlow.Web/Views/Deck/DeckAnalysis.cshtml` returns 1 (single input, no hidden mirror).
- `npx --no-install playwright test e2e/deck-analysis-command-zone.spec.ts` passes (flag-ON input shown, flag-OFF absent).
- Operator visual sign-off PASS across desktop + mobile, default + one guild theme, light + dark.
- format-gate clean on changed lines; new layout CSS in site-common.css only.
</verification>

<success_criteria>
- A single companion designator input appears on Step 1 only when the flag is ON; absent (and page unchanged) when OFF.
- All 11 controller render paths stamp the flag via one helper/factory (no path renders the wrong UI).
- Typed designator round-trips via CompanionName and is honored by the service (designator wins); no duplicate binding.
- README + Help document the flag and the field.
- Playwright smoke green; operator cross-theme/mobile sign-off approved.
</success_criteria>

<output>
Create `.planning/phases/73-deck-analysis-command-zone-awareness-ad-hoc-trunk-main/73-04-SUMMARY.md` when done.
</output>
</output>
