---
phase: 12-ai-agnostic-url-page-rename
plan: 03
type: execute
wave: 3
depends_on: [12-02]
files_modified:
  - DeckFlow.Web/Views/Deck/DeckAnalysis.cshtml
  - DeckFlow.Web/Views/Deck/DeckComparison.cshtml
  - DeckFlow.Web/Views/Deck/CedhMetaGap.cshtml
  - DeckFlow.Web/Views/Shared/_DeckToolTabs.cshtml
  - DeckFlow.Web/Views/Deck/Home.cshtml
  - DeckFlow.Web/wwwroot/css/site-common.css
autonomous: true
requirements: [RENAME-02]
must_haves:
  truths:
    - "Page 1 (/deck-analysis) renders <h1> text `Deck Analysis` and a <p class=\"page-lede\"> explainer line with the exact Mock A copy — per D-06 only the Page-1 H1 changes (Pages 2 and 3 H1 unchanged, already AI-agnostic)"
    - "Page 2 (/deck-comparison) renders <h1> text `Deck Comparison` (unchanged per D-06) and a <p class=\"page-lede\"> explainer line with the exact Mock A copy per D-07"
    - "Page 3 (/cedh-meta-gap) renders <h1> text `cEDH Meta Gap` (unchanged per D-06) and a <p class=\"page-lede\"> explainer line with the exact Mock A copy per D-07"
    - "Page 1 <title> element value is `Deck Analysis - DeckFlow` (via ViewData[\"Title\"]) — per D-09 Page-1 title string changes"
    - "Nav strip in _DeckToolTabs.cshtml shows `Deck Analysis` as the first label with href ~/deck-analysis — per D-09 nav label scope (only Page-1 label changes)"
    - "Home hub-hero and the three hub-cards point at the new slugs; Page-1 hub card title is `Deck Analysis` and hub-hero title is `Analyze Your Deck` — per D-09 hub-label scope"
    - "site-common.css contains a `.page-lede` rule using var(--muted) and var(--fs-base) — per D-08 cross-cutting CSS lives in site-common.css ONLY, NOT in any per-theme guild fork"
  artifacts:
    - path: "DeckFlow.Web/Views/Deck/DeckAnalysis.cshtml"
      provides: "Page 1 with new H1, ViewData[Title], and lede paragraph (D-06/D-07/D-09)"
      contains: "page-lede"
    - path: "DeckFlow.Web/Views/Deck/DeckComparison.cshtml"
      provides: "Page 2 with lede paragraph (H1 unchanged per D-06)"
      contains: "page-lede"
    - path: "DeckFlow.Web/Views/Deck/CedhMetaGap.cshtml"
      provides: "Page 3 with lede paragraph (H1 unchanged per D-06)"
      contains: "page-lede"
    - path: "DeckFlow.Web/wwwroot/css/site-common.css"
      provides: "Cross-cutting .page-lede CSS rule per D-08 (CLAUDE.md D-07 cross-cutting invariant)"
      contains: ".page-lede"
  key_links:
    - from: "DeckFlow.Web/Views/Deck/*.cshtml <p class=\"page-lede\">"
      to: "DeckFlow.Web/wwwroot/css/site-common.css .page-lede rule"
      via: "CSS class selector (D-08 cross-cutting placement)"
      pattern: "\\.page-lede"
    - from: "_DeckToolTabs.cshtml href attributes"
      to: "DeckController route attributes (landed in Plan 01)"
      via: "Url.Content tilde-prefix resolution"
      pattern: "Url\\.Content\\(\"~/(deck-analysis|deck-comparison|cedh-meta-gap)\""
---

<objective>
Apply the user-visible label and explainer-line changes locked in `12-CONTEXT.md` decisions D-06 through D-09: change Page 1's H1 + `<title>` + nav label + hub-card title from "ChatGPT Analysis" to "Deck Analysis"; add the three exact Mock A explainer paragraphs under each H1; update hrefs in `_DeckToolTabs.cshtml` and `Home.cshtml` to the new slugs; and add a `.page-lede` CSS rule to `site-common.css` (cross-cutting per CLAUDE.md D-07).

Purpose: Closes the user-visible label half of RENAME-02. Per D-06 (only Page-1 H1 changes — Pages 2 and 3 H1s are already AI-agnostic), D-07 (exact explainer copy), D-08 (.page-lede goes in site-common.css, NOT a per-theme fork), D-09 (nav + hub + title aligned).
Output: All three pages render the Mock A explainer line; Page-1 label text and `<title>` are AI-agnostic; nav + hub URLs match the Plan 01 slugs; `.page-lede` styling lands cross-theme.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/STATE.md
@.planning/phases/12-ai-agnostic-url-page-rename/12-CONTEXT.md
@.planning/phases/12-ai-agnostic-url-page-rename/12-PATTERNS.md
@.planning/AI-AGNOSTIC-RENAME-BRAINSTORM.md
@.planning/phases/12-ai-agnostic-url-page-rename/12-02-SUMMARY.md

<interfaces>
<!-- Exact Mock A explainer copy (D-07). DO NOT paraphrase. -->
- Page 1 lede text: `Generate a prompt to paste into ChatGPT, Claude, or Gemini.`
- Page 2 lede text: `Generate a prompt comparing two decks. Paste into ChatGPT, Claude, or Gemini.`
- Page 3 lede text: `Generate a prompt analyzing your deck against current cEDH meta. Paste into ChatGPT, Claude, or Gemini.`

<!-- Page 1 label changes (D-06, D-09). Only Page 1 H1/title/nav/hub-card label changes; Pages 2 and 3 keep their existing AI-agnostic text. -->
- Page 1 H1: `ChatGPT Analysis` → `Deck Analysis`
- Page 1 ViewData["Title"]: `ChatGPT Analysis` → `Deck Analysis`  (renders as `Deck Analysis - DeckFlow` via _Layout.cshtml:43)
- Nav label in _DeckToolTabs.cshtml: `ChatGPT Analysis` → `Deck Analysis`
- Home.cshtml hub-hero title: `Analyze Your Deck with ChatGPT` → `Analyze Your Deck`
- Home.cshtml hub-card title (Page 1 card): `ChatGPT Analysis` → `Deck Analysis`

<!-- href changes (apply to ALL six href values across _DeckToolTabs.cshtml and Home.cshtml). -->
- ~/chatgpt-packets         → ~/deck-analysis
- ~/chatgpt-deck-comparison → ~/deck-comparison
- ~/chatgpt-cedh-meta-gap   → ~/cedh-meta-gap

<!-- New CSS rule in site-common.css (D-08). Place at END of file (after line 1388 per PATTERNS.md), following the .hub-lede analog at site-common.css:209-213. -->

```css
/* === Phase 12 (RENAME-02) — page-lede explainer line ===
   Muted one-line explainer under an H1 on the three AI workflow pages.
   Per CLAUDE.md D-07, cross-cutting rules live in site-common.css. */
.page-lede {
  margin: 0.25rem 0 1rem;
  color: var(--muted);
  font-size: var(--fs-base);
}
```

<!-- Existing .hub-lede analog at site-common.css:209-213 (verified by grep 2026-05-16): -->
```
.hub-lede {
  margin: 1rem 0 0.5rem;
  color: var(--muted);
  font-size: var(--fs-base);
}
```

<!-- The `<p class="page-lede">…</p>` element MUST be inserted IMMEDIATELY AFTER the `<h1>` element in each view file. -->
<!-- The H1 element in each view is currently inside the page content — find it via grep `<h1` in each renamed .cshtml. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add .page-lede rule to site-common.css and add lede paragraphs to all three view files</name>
  <files>
    DeckFlow.Web/wwwroot/css/site-common.css,
    DeckFlow.Web/Views/Deck/DeckAnalysis.cshtml,
    DeckFlow.Web/Views/Deck/DeckComparison.cshtml,
    DeckFlow.Web/Views/Deck/CedhMetaGap.cshtml
  </files>
  <read_first>
    - DeckFlow.Web/wwwroot/css/site-common.css (lines 1-30 for the cross-cutting section header comment, lines 200-220 for the .hub-lede analog, AND the final ~20 lines of the file to find the insertion point)
    - DeckFlow.Web/Views/Deck/DeckAnalysis.cshtml (entire file — locate the `<h1>` element)
    - DeckFlow.Web/Views/Deck/DeckComparison.cshtml (entire file — locate the `<h1>` element)
    - DeckFlow.Web/Views/Deck/CedhMetaGap.cshtml (entire file — locate the `<h1>` element)
    - .planning/phases/12-ai-agnostic-url-page-rename/12-CONTEXT.md (D-07, D-08)
    - .planning/phases/12-ai-agnostic-url-page-rename/12-PATTERNS.md (site-common.css section)
  </read_first>
  <action>
Step 1: Append the `.page-lede` CSS rule to `DeckFlow.Web/wwwroot/css/site-common.css` at the END of the file (per D-08). Use the exact block specified in the `<interfaces>` above — keep the 3-line header comment, the 4 declarations (margin, color, font-size), and the trailing closing brace. Do NOT add a width constraint or font-weight override (those were brainstorm ideas; D-08 keeps the rule minimal). Do NOT add this rule to any per-theme guild file (`site-azorius.css`, etc.) — CLAUDE.md D-07 invariant + D-08 say cross-cutting CSS goes in site-common.css ONLY.

Step 2: For each of the three renamed view files, find the page's `<h1>` element via grep (`grep -n "<h1" {file}`) and insert IMMEDIATELY AFTER the closing `</h1>` tag a new line with the `<p class="page-lede">` paragraph carrying the exact Mock A copy from `<interfaces>` above (per D-07). Maintain the existing indentation level of the H1 line (whatever Razor indent that file uses). Preserve a blank line if one already separates the H1 from the next block.

Exact copy (DO NOT paraphrase) per D-07:
- `DeckAnalysis.cshtml`: `<p class="page-lede">Generate a prompt to paste into ChatGPT, Claude, or Gemini.</p>`
- `DeckComparison.cshtml`: `<p class="page-lede">Generate a prompt comparing two decks. Paste into ChatGPT, Claude, or Gemini.</p>`
- `CedhMetaGap.cshtml`: `<p class="page-lede">Generate a prompt analyzing your deck against current cEDH meta. Paste into ChatGPT, Claude, or Gemini.</p>`

This task is a single logical change ("add page-lede explainer to all three AI-workflow pages plus the supporting CSS rule") and ships as ONE commit per CLAUDE.md "one logical change per commit". Even though it touches 4 files, the change is cohesive — the CSS rule has no purpose without the markup, and the markup has no styling without the rule.
  </action>
  <verify>
    <automated>
      cd /mnt/c/users/chrislunt/source/personal/deckflow &amp;&amp; \
      dotnet build DeckFlow.Web/DeckFlow.Web.csproj --nologo --verbosity quiet &amp;&amp; \
      grep -c "^\.page-lede" DeckFlow.Web/wwwroot/css/site-common.css | grep -qE '^1$' &amp;&amp; \
      grep -c 'page-lede">Generate a prompt to paste into ChatGPT, Claude, or Gemini\.' DeckFlow.Web/Views/Deck/DeckAnalysis.cshtml | grep -qE '^1$' &amp;&amp; \
      grep -c 'page-lede">Generate a prompt comparing two decks\. Paste into ChatGPT, Claude, or Gemini\.' DeckFlow.Web/Views/Deck/DeckComparison.cshtml | grep -qE '^1$' &amp;&amp; \
      grep -c 'page-lede">Generate a prompt analyzing your deck against current cEDH meta\. Paste into ChatGPT, Claude, or Gemini\.' DeckFlow.Web/Views/Deck/CedhMetaGap.cshtml | grep -qE '^1$' &amp;&amp; \
      grep -lr "\.page-lede" DeckFlow.Web/wwwroot/css/ | grep -v "site-common.css" | grep -v "^$" | wc -l | grep -qE '^0$'
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^\.page-lede" DeckFlow.Web/wwwroot/css/site-common.css` returns exactly `1` (one CSS rule selector at column 0) — D-08
    - The .page-lede block contains `color: var(--muted);` (verify with `grep -A4 "^\.page-lede" DeckFlow.Web/wwwroot/css/site-common.css | grep -c "var(--muted)"` == `1`)
    - The .page-lede block contains `font-size: var(--fs-base);` (verify with `grep -A4 "^\.page-lede" DeckFlow.Web/wwwroot/css/site-common.css | grep -c "var(--fs-base)"` == `1`)
    - The .page-lede rule does NOT appear in ANY other CSS file under `wwwroot/css/` (verify with `grep -lr "\.page-lede" DeckFlow.Web/wwwroot/css/ | grep -v "site-common.css" | wc -l` == `0`) — D-08 cross-cutting invariant
    - `DeckAnalysis.cshtml` contains exactly the literal `<p class="page-lede">Generate a prompt to paste into ChatGPT, Claude, or Gemini.</p>` once (D-07)
    - `DeckComparison.cshtml` contains exactly the literal `<p class="page-lede">Generate a prompt comparing two decks. Paste into ChatGPT, Claude, or Gemini.</p>` once (D-07)
    - `CedhMetaGap.cshtml` contains exactly the literal `<p class="page-lede">Generate a prompt analyzing your deck against current cEDH meta. Paste into ChatGPT, Claude, or Gemini.</p>` once (D-07)
    - In each .cshtml file, the page-lede `<p>` element appears AFTER the corresponding `<h1>` element (verify by inspecting line numbers — `grep -n "<h1\|page-lede" {file}` shows `<h1>` line < `page-lede` line)
    - `dotnet build DeckFlow.Web/DeckFlow.Web.csproj` exits 0 with no new warnings
  </acceptance_criteria>
  <done>Cross-cutting .page-lede rule lives in site-common.css only (D-08); all three pages have the exact Mock A explainer paragraph under their H1 (D-07).</done>
</task>

<task type="auto">
  <name>Task 2: Update Page-1 H1, ViewData[Title], nav label, and hub-card labels + all six hrefs</name>
  <files>
    DeckFlow.Web/Views/Deck/DeckAnalysis.cshtml,
    DeckFlow.Web/Views/Shared/_DeckToolTabs.cshtml,
    DeckFlow.Web/Views/Deck/Home.cshtml
  </files>
  <read_first>
    - DeckFlow.Web/Views/Deck/DeckAnalysis.cshtml (entire file — locate `ViewData["Title"]` assignment and the H1 element)
    - DeckFlow.Web/Views/Shared/_DeckToolTabs.cshtml (entire file — locate the three ChatGPT-page nav links around line 18)
    - DeckFlow.Web/Views/Deck/Home.cshtml (lines 11-31 per PATTERNS.md — hub-hero and three hub-cards)
    - DeckFlow.Web/Views/Shared/_Layout.cshtml (line 43 — confirm `<title>@ViewData["Title"] - DeckFlow</title>` pattern)
    - .planning/phases/12-ai-agnostic-url-page-rename/12-CONTEXT.md (D-06, D-09)
    - .planning/phases/12-ai-agnostic-url-page-rename/12-PATTERNS.md (_DeckToolTabs.cshtml and Home.cshtml sections)
  </read_first>
  <action>
**DeckAnalysis.cshtml (Page-1 H1 + title) — D-06 / D-09:**
- Replace literal `ViewData["Title"] = "ChatGPT Analysis";` with `ViewData["Title"] = "Deck Analysis";`
- Replace the H1 text content `ChatGPT Analysis` with `Deck Analysis` (the H1 element opening + closing tags + any attribute classes stay UNCHANGED — only the visible text inside changes)

**DeckComparison.cshtml + CedhMetaGap.cshtml:** Do NOT touch H1 text or ViewData["Title"]. Per D-06 those page labels are already AI-agnostic. The lede paragraphs already landed in Task 1.

**_DeckToolTabs.cshtml (all three nav links) — D-09:**
- Page-1 nav link `href`: `~/chatgpt-packets` → `~/deck-analysis`
- Page-1 nav link visible text: `ChatGPT Analysis` → `Deck Analysis`
- Page-2 nav link `href`: `~/chatgpt-deck-comparison` → `~/deck-comparison` (label `Deck Comparison` unchanged)
- Page-3 nav link `href`: `~/chatgpt-cedh-meta-gap` → `~/cedh-meta-gap` (label `cEDH Meta Gap` unchanged)
- Do NOT change the `@(Model == DeckPageTab.ChatGptPackets ? "is-active" : string.Empty)` conditional — the `DeckPageTab` enum value rename is Phase 13.

**Home.cshtml (hub-hero + three hub-cards) — D-09 per PATTERNS.md mapping:**
- Hub-hero `href`: `~/chatgpt-packets` → `~/deck-analysis`
- Hub-hero `<span class="hub-hero__title">` text: `Analyze Your Deck with ChatGPT` → `Analyze Your Deck`
- (Hub-hero description text may also reference ChatGPT — read the file and update any literal `ChatGPT` in the visible hub-hero copy to AI-agnostic equivalents IF the wording becomes nonsensical otherwise. Specifically the line `"Five-step workflow: load your deck, pick your questions, copy the prompt, paste into ChatGPT, review the structured response."` keeps `paste into ChatGPT` AS-IS — that string is an example of where users paste, parallel to the lede pattern, so it stays vendor-positive. Alternative: change to `paste into ChatGPT, Claude, or Gemini`. **Choose the second option (`paste into ChatGPT, Claude, or Gemini`) to stay consistent with the new lede copy across the rest of the page family.**)
- First hub-card `href` (Page 1): `~/chatgpt-packets` → `~/deck-analysis`
- First hub-card `<h3 class="hub-card__title">` text: `ChatGPT Analysis` → `Deck Analysis`
- (First hub-card body copy: read the file; if it contains the literal phrase `ChatGPT packet` or similar, update to `analysis prompt` per Mock A Q2 Option-1 artifact terminology. If unsure, leave body copy as-is — Plan 05 docs sweep will catch hardcoded URL references in copy.)
- Second hub-card `href` (Page 2): `~/chatgpt-deck-comparison` → `~/deck-comparison` (title `Deck Comparison` unchanged)
- Third hub-card `href` (Page 3): `~/chatgpt-cedh-meta-gap` → `~/cedh-meta-gap` (title `cEDH Meta Gap` unchanged)

Per CLAUDE.md "one logical change per commit": this task is a single logical change ("rebrand Page-1 labels + sync all nav/hub hrefs with new slugs") and ships as ONE commit. The three-file edit is cohesive — partial application would leave the user-facing surface inconsistent.
  </action>
  <verify>
    <automated>
      cd /mnt/c/users/chrislunt/source/personal/deckflow &amp;&amp; \
      dotnet build DeckFlow.Web/DeckFlow.Web.csproj --nologo --verbosity quiet &amp;&amp; \
      grep -c 'ViewData\["Title"\] = "Deck Analysis"' DeckFlow.Web/Views/Deck/DeckAnalysis.cshtml | grep -qE '^1$' &amp;&amp; \
      grep -c "ChatGPT Analysis" DeckFlow.Web/Views/Deck/DeckAnalysis.cshtml | grep -qE '^0$' &amp;&amp; \
      grep -c "Url\.Content(\"~/chatgpt-" DeckFlow.Web/Views/Shared/_DeckToolTabs.cshtml | grep -qE '^0$' &amp;&amp; \
      grep -c "Url\.Content(\"~/chatgpt-" DeckFlow.Web/Views/Deck/Home.cshtml | grep -qE '^0$' &amp;&amp; \
      grep -cE 'Url\.Content\("~/(deck-analysis|deck-comparison|cedh-meta-gap)"\)' DeckFlow.Web/Views/Shared/_DeckToolTabs.cshtml | grep -qE '^3$' &amp;&amp; \
      grep -cE 'Url\.Content\("~/(deck-analysis|deck-comparison|cedh-meta-gap)"\)' DeckFlow.Web/Views/Deck/Home.cshtml | grep -qE '^[3-9]$|^[1-9][0-9]+$' &amp;&amp; \
      grep -c "ChatGPT Analysis" DeckFlow.Web/Views/Shared/_DeckToolTabs.cshtml | grep -qE '^0$' &amp;&amp; \
      grep -c "Deck Analysis" DeckFlow.Web/Views/Shared/_DeckToolTabs.cshtml | grep -qE '^1$'
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'ViewData\["Title"\] = "Deck Analysis"' DeckFlow.Web/Views/Deck/DeckAnalysis.cshtml` returns `1` — per D-09 Page-1 title string
    - `grep -c "ChatGPT Analysis" DeckFlow.Web/Views/Deck/DeckAnalysis.cshtml` returns `0` — per D-06 Page-1 H1 changes (the literal label `ChatGPT Analysis` is gone from this view — but verify the lede `ChatGPT, Claude, or Gemini` IS still present)
    - `grep -c "Deck Analysis" DeckFlow.Web/Views/Deck/DeckAnalysis.cshtml` returns at least `2` (H1 + Title) — per D-06 + D-09
    - `grep -c "ChatGPT, Claude, or Gemini" DeckFlow.Web/Views/Deck/DeckAnalysis.cshtml` returns `1` (lede preserved from Task 1 — D-07)
    - `grep -c "ChatGPT Analysis" DeckFlow.Web/Views/Shared/_DeckToolTabs.cshtml` returns `0` — per D-09 nav label
    - `grep -c ">Deck Analysis<" DeckFlow.Web/Views/Shared/_DeckToolTabs.cshtml` returns `1` (the nav link text per D-09)
    - `grep -c "Url.Content(\"~/chatgpt-" DeckFlow.Web/Views/Shared/_DeckToolTabs.cshtml` returns `0`
    - `grep -cE 'Url\.Content\("~/(deck-analysis|deck-comparison|cedh-meta-gap)"\)' DeckFlow.Web/Views/Shared/_DeckToolTabs.cshtml` returns `3`
    - `grep -c "Url.Content(\"~/chatgpt-" DeckFlow.Web/Views/Deck/Home.cshtml` returns `0`
    - `grep -cE 'Url\.Content\("~/(deck-analysis|deck-comparison|cedh-meta-gap)"\)' DeckFlow.Web/Views/Deck/Home.cshtml` returns at least `3` (hub-hero + 3 hub-cards, but hub-hero shares the Page-1 href so total may be 4 — accept ≥3)
    - `grep -c "Analyze Your Deck with ChatGPT" DeckFlow.Web/Views/Deck/Home.cshtml` returns `0` — per D-09 hub-hero title scope
    - `grep -c "Analyze Your Deck" DeckFlow.Web/Views/Deck/Home.cshtml` returns at least `1` (the hub-hero title after rewrite per D-09)
    - `grep -c "DeckPageTab.ChatGptPackets" DeckFlow.Web/Views/Shared/_DeckToolTabs.cshtml` returns `1` (enum reference UNCHANGED per Phase 13 invariant)
    - `dotnet build DeckFlow.Web/DeckFlow.Web.csproj` exits 0 with no new warnings
  </acceptance_criteria>
  <done>Page-1 visible label is AI-agnostic everywhere (H1, title, nav, hub) per D-06 + D-09; all 6 hrefs across nav + home point at the new slugs; Pages 2 and 3 labels and explainer lines all line up with Mock A; build clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Razor view → rendered HTML | Server-rendered template; no user input in this plan's surface area. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-06 | Information disclosure | New `<p class="page-lede">` copy | accept | Copy text is hardcoded literal HTML in Razor views. No reflection, no user data, no PII. Cannot leak information. |
| T-12-07 | Cross-cutting CSS drift across 22 guild themes | site-common.css `.page-lede` rule | mitigate | Rule lives ONLY in `site-common.css` per CLAUDE.md D-07 and per D-08. Acceptance gate (`grep -lr "\.page-lede" DeckFlow.Web/wwwroot/css/ | grep -v site-common.css | wc -l == 0`) blocks accidental placement in a guild theme fork. This prevents the 22× theme-fork drift the project explicitly warns against. |
| T-12-08 | Tampering / Open-redirect via Url.Content | new ~/deck-* hrefs | mitigate | `Url.Content("~/{slug}")` resolves tilde-prefix server-side against `PathBase`. All slug strings are hardcoded literals (`deck-analysis`, `deck-comparison`, `cedh-meta-gap`) — no user input crosses the boundary, so open-redirect is structurally impossible. |
</threat_model>

<verification>
Manual smoke check (user-launched dev server only — do NOT auto-launch):
- Visit `/deck-analysis`: page renders `<h1>Deck Analysis</h1>` followed by the lede paragraph; browser tab title reads `Deck Analysis - DeckFlow`.
- Visit `/deck-comparison`: page renders `<h1>Deck Comparison</h1>` (unchanged) followed by the lede paragraph.
- Visit `/cedh-meta-gap`: page renders `<h1>cEDH Meta Gap</h1>` (unchanged) followed by the lede paragraph.
- Top nav strip on every Deck-tool page shows `Deck Analysis` | `Deck Comparison` | `cEDH Meta Gap` (all linking to new slugs).
- Home page (`/` route): hub-hero says `Analyze Your Deck` and points at `/deck-analysis`; first hub-card title says `Deck Analysis`.
- DevTools → Computed styles on the `.page-lede` element: confirm `color` resolves to the theme's `--muted` token value and `font-size` to `--fs-base` (varies per theme; spot-check 2-3 themes by visiting `?theme=…`).
- Mobile / 375px viewport check: lede wraps cleanly; nav strip does not overflow horizontally.
</verification>

<success_criteria>
- All 3 view files contain the exact Mock A lede paragraph
- Page 1 H1 reads `Deck Analysis` everywhere (view, nav, hub-card, browser title)
- Pages 2 and 3 H1s unchanged (`Deck Comparison`, `cEDH Meta Gap`)
- `_DeckToolTabs.cshtml` and `Home.cshtml` contain 0 `Url.Content("~/chatgpt-` occurrences
- `.page-lede` rule lives in `site-common.css` only (0 occurrences in per-theme files)
- `dotnet build DeckFlow.sln --configuration Release` exits 0 with no new warnings
</success_criteria>

<output>
Create `.planning/phases/12-ai-agnostic-url-page-rename/12-03-SUMMARY.md` documenting:
- .page-lede CSS rule added (line number in site-common.css)
- 3 lede paragraphs added to view files
- Page-1 H1/title/nav/hub labels rewritten
- 6 hrefs across nav + home updated to new slugs
- Build clean
- Manual smoke-test results
</output>
</output>
