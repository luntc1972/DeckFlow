---
phase: 90-directpush-correctness-seed-sync
plan: 05
type: execute
wave: 3
depends_on: [90-03, 90-04, 90-07]
files_modified:
  - DeckFlow.Studio/ViewModels/DirectPushCoordinator.cs
  - DeckFlow.Studio/Services/IDeployedBodyConfirmer.cs
  - DeckFlow.Studio/Services/DeployedBodyConfirmer.cs
  - DeckFlow.Studio/Program.cs
  - DeckFlow.Studio.Tests/ViewModels/DirectPushCoordinatorTests.cs
  - DeckFlow.Studio.Tests/Services/DeployedBodyConfirmerTests.cs
autonomous: true
requirements: [SYNC-09, SYNC-10]
must_haves:
  truths:
    - "WritePublishAsync is split so the content upsert (UpsertContentColumnsOnlyBatchAsync) no longer stamps pushed_to_prod_utc or flips is_visible; those move to a NEW post-confirm method, so a content upsert never contracts before the body is confirmed. (D-06, D-07)"
    - "is_visible flips to true ONLY after the Studio confirmer polls the authenticated deployed-body-hash endpoint (Plan 90-07) BY NATURAL KEY and gets 200 AND bodySha256 == the expected stored hash (defeating the un-deployed-update / new-row-404 / missing-body / stale-deploy races); the prod-first-then-local write ordering and the ContentIndexExportRow.From key derivation are preserved through the split. (D-06, D-09 REVISED)"
    - "pushed_to_prod_utc is stamped ONLY in the post-confirm path (never in the content-only upsert) and preserves F-51-PG-01 timestamptz handling on Postgres; the durable awaiting-confirm marker is set at content-upsert time and cleared on confirm. (D-07, D-10)"
    - "The content-only write still mirrors approval_status into prod via UpsertContentColumnsOnlyBatchAsync (the P88 approval mirror at ContentSiteIndexStore.cs:826-829 is NOT dropped by the WriteContentAsync/ConfirmAndPublish split), so a DirectPush'd row is never visible-while-pending. (D-03)"
  artifacts:
    - path: "DeckFlow.Studio/ViewModels/DirectPushCoordinator.cs"
      provides: "content-only write (+marker) + hash-match-gated post-confirm stamp/visibility + marker clear"
      contains: "SetVisibilityAsync"
    - path: "DeckFlow.Studio/Services/DeployedBodyConfirmer.cs"
      provides: "polls the deployed-body-hash endpoint by natural key until 200 && hash==expected (bounded retries+backoff)"
      contains: "bodySha256"
  key_links:
    - from: "DeployedBodyConfirmer"
      to: "GET /Admin/api/contentkb/deployed-body-hash (Plan 90-07)"
      via: "natural-key poll until 200 && bodySha256==expected, admin BasicAuth"
      pattern: "deployed-body-hash"
    - from: "DirectPushCoordinator post-confirm method"
      to: "prodStore.StampPushedToProdAsync + SetVisibilityAsync"
      via: "prod-first-then-local, only after the hash-match confirm succeeds"
      pattern: "StampPushedToProdAsync"
---

<objective>
Re-plumb `DirectPushCoordinator` to the expand→verify→contract ordering (SYNC-09 / D-06) and confirmed-stamp timing (SYNC-10 / D-07). Split `WritePublishAsync` so the content upsert no longer stamps `pushed_to_prod_utc` or flips `is_visible`; add a confirmer (its own files) that polls the Plan 90-07 authenticated deployed-body-hash endpoint BY NATURAL KEY, with bounded retries + backoff, until it returns `200 AND bodySha256 == the expected stored hash`; only then run the stamp + visibility flip (prod-first-then-local). Set the Plan 90-03 durable awaiting-confirm marker at content-upsert time and clear it on confirm so a mid-flight push is recoverable (D-10).

Purpose: Today Stage 3 stamps + flips-visible before git even runs — the contract-before-expand bug (M3/M6a). Codex plan-review killed the original "GET public detail page → 200" confirm (a not-yet-visible row 404s forever; a missing artifact renders a 200 shell → false confirm; an updated visible row returns the OLD body's 200 before Render deploys → stale confirm; local-vs-prod id mismatch). D-09 (REVISED) replaces it with a hash-match poll of the dedicated /app-only endpoint — a hash match is proof the intended body is live.
Output: the split coordinator methods, a dedicated `IDeployedBodyConfirmer`/`DeployedBodyConfirmer` (separate files), marker set/clear wiring, HttpClient/auth DI, and ordering tests using a stub confirmer.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/90-directpush-correctness-seed-sync/90-CONTEXT.md
@.planning/phases/90-directpush-correctness-seed-sync/90-PATTERNS.md
@.planning/phases/90-directpush-correctness-seed-sync/90-RESEARCH.md
@.planning/phases/90-directpush-correctness-seed-sync/90-07-PLAN.md
</context>

<interfaces>
<!-- Extracted from 90-PATTERNS.md / 90-RESEARCH.md / 90-CONTEXT.md D-09 REVISED / Plan 90-07. -->

Method to split — DirectPushCoordinator.cs:213-233 (WritePublishAsync) + 204-211 (prod-first-then-local ordering doc comment, PUB-01/HIGH-3):
- Current body: `UpsertContentColumnsOnlyBatchAsync` -> derive keys via `ContentIndexExportRow.From(row)` (Type/Value) -> `pushedUtc = DateTimeOffset.UtcNow` -> prod `StampPushedToProdAsync` -> prod `SetVisibilityAsync(true)` -> local `StampPushedToProdAsync` -> local `SetVisibilityAsync(true)`.
- Split into: (a) `WriteContentAsync` = ONLY `UpsertContentColumnsOnlyBatchAsync` + set the awaiting-confirm marker (from 90-03 `SetAwaitingConfirmAsync`) for the keys; (b) `ConfirmAndPublishAsync` = the four Stamp/SetVisibility calls (SAME prod-first-then-local order) + clear the marker (`ClearAwaitingConfirmAsync`), run ONLY after the hash-match confirm succeeds.
- Reuse `ContentIndexExportRow.From()` for key derivation in BOTH methods — do not hand-roll natural-key extraction (Pitfall 5: ordering invariant must survive the split; covered by DirectPushCoordinatorTests).

Confirm endpoint (Plan 90-07, a hard dependency) — `GET /Admin/api/contentkb/deployed-body-hash?naturalKeyType={t}&naturalKeyValue={v}`:
- Resolves the /app artifact ONLY, is_visible-independent; returns 404 if the /app body is missing, else `{ bodySha256 }` (recomputed via `ComputeBodySha256`). Behind admin BasicAuth; NOT behind SameOriginRequestValidator.
- The confirmer sends admin BasicAuth (Studio already holds admin creds) — read the base URL + admin user/password from Studio config (e.g. `Studio:PublicSiteBaseUrl`, `Studio:AdminUser`, `Studio:AdminPassword`); do NOT hardcode the domain or creds.

Confirm transport — DeckFlow.Studio/Program.cs:121-124 (singleton `HttpClient(new ResilientHttpHandler()){ Timeout=... }`) + consumers at 131-140 (`sp.GetRequiredService<HttpClient>()`). ResilientHttpHandler (ResilientHttpHandler.cs:18-120) gives Polly-v8 retry on transient HTTP; the confirmer ALSO applies its own bounded application-level poll loop (retries + backoff) for the deploy-lag case (a 404/hash-mismatch is a valid "not deployed yet", not a transport error). Follow the Studio-local HttpClient+ResilientHttpHandler convention (NOT the Web RestSharp convention).

Expected hash source — each pushed `ContentSiteIndexRow.BodySha256` (computed at publish time, 89-05) is the expected value; the confirmer compares the endpoint's returned `bodySha256` to this ordinally. Reuse of the SAME `ComputeBodySha256` on both sides (publish + endpoint) makes the comparison valid — do NOT build a second hash path.

Test seam — DirectPushCoordinatorTests.cs:58-72 Build(...). Add a `FakeDeployedBodyConfirmer` returning confirmed/not-confirmed deterministically so coordinator ORDERING is testable WITHOUT live HTTP. The DeployedBodyConfirmer's own HTTP/poll logic is tested separately (DeployedBodyConfirmerTests) with a stubbed HttpMessageHandler (RichardSzalay.MockHttp is already in the solution).
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Split WritePublishAsync into content-only write (+marker) and post-confirm stamp/visibility</name>
  <read_first>
    - DeckFlow.Studio/ViewModels/DirectPushCoordinator.cs (204-233 — WritePublishAsync + the prod-first-then-local ordering comment)
    - DeckFlow.Core/Content/ContentSiteIndexStore.cs (StampPushedToProdAsync 649-654, SetVisibilityAsync 767-773; the 90-03 SetAwaitingConfirmAsync/ClearAwaitingConfirmAsync; the P88 approval mirror binding `approvalStatus` in the content-columns-only upsert at 826-829)
    - 90-PATTERNS.md § "SYNC-09/SYNC-10 ordering re-plumb" and Pitfall 5 (ordering invariant must survive the split)
  </read_first>
  <behavior>
    - After the content-only write, prod has the upserted content (incl. body_sha256) but pushed_to_prod_utc is still null and is_visible is still false; the awaiting-confirm marker is SET for those keys locally.
    - The post-confirm method stamps pushed_to_prod_utc and flips is_visible=true in prod-FIRST-then-local order, and clears the awaiting-confirm marker; it runs only when invoked after a successful hash-match confirm (Task 2 gates the call).
    - Key derivation uses ContentIndexExportRow.From() in both methods; F-51-PG-01 timestamptz handling preserved when writing the stamp on Postgres.
    - The content-only write STILL carries approval_status into prod (UpsertContentColumnsOnlyBatchAsync mirrors it, ContentSiteIndexStore.cs:826-829) — the split does not drop the P88 approval mirror (D-03).
  </behavior>
  <action>Split `WritePublishAsync` into: (a) `WriteContentAsync(publishRows, ct)` containing ONLY `prodStore.UpsertContentColumnsOnlyBatchAsync(publishRows, ct)` PLUS a `_localStore.SetAwaitingConfirmAsync(keys, DateTimeOffset.UtcNow, ct)` call (marker set locally for the pushed keys, from 90-03); and (b) `ConfirmAndPublishAsync(publishRows, ct)` containing the four Stamp/SetVisibility calls in the EXACT existing prod-first-then-local order (prod Stamp -> prod SetVisibility(true) -> local Stamp -> local SetVisibility(true)) PLUS `_localStore.ClearAwaitingConfirmAsync(keys, ct)` after visibility. Derive `keys` via `ContentIndexExportRow.From(row)` in both. Preserve the 204-211 ordering doc comment (move/duplicate it onto ConfirmAndPublishAsync and note the invariant explicitly). Do NOT stamp or flip visibility in WriteContentAsync. Preserve the `ContentSiteIndexBatchUpsertException` handling/semantics already present. Do NOT alter the approval_status binding — WriteContentAsync must keep calling UpsertContentColumnsOnlyBatchAsync (which mirrors approval_status per P88/D-03). Extend DirectPushCoordinatorTests: after WriteContentAsync, assert prod store recorded the content upsert (with the row's approval_status='approved' mirrored, D-03) but NO stamp/visibility call and the marker is set; after ConfirmAndPublishAsync, assert stamp+visibility happened prod-before-local and the marker is cleared.</action>
  <verify>
    <automated>dotnet build DeckFlow.Studio.Tests -c Debug; dotnet test DeckFlow.Studio.Tests --filter "FullyQualifiedName~DirectPushCoordinator"</automated>
  </verify>
  <acceptance_criteria>
    - SOURCE: `WriteContentAsync` contains no `StampPushedToProdAsync`/`SetVisibilityAsync` call; `ConfirmAndPublishAsync` contains all four in prod-first-then-local order (grep confirms).
    - SOURCE: both methods derive keys via `ContentIndexExportRow.From`; the marker is set in WriteContentAsync and cleared in ConfirmAndPublishAsync.
    - BEHAVIOR: tests prove content-only write leaves pushed_utc null + is_visible false + marker set AND mirrors approval_status='approved' into prod (D-03 approval mirror preserved); post-confirm stamps + flips visible prod-before-local + clears marker.
  </acceptance_criteria>
  <done>WritePublishAsync is split; stamp/visibility only happen post-confirm; marker set/cleared; ordering invariant preserved; tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: DeployedBodyConfirmer (endpoint hash-match poll by natural key) gating ConfirmAndPublishAsync</name>
  <read_first>
    - 90-07-PLAN.md (the endpoint contract: route under /Admin, natural-key query, 404-on-missing, { bodySha256 }, BasicAuth, no SameOrigin)
    - DeckFlow.Studio/Program.cs (121-124 singleton HttpClient + ResilientHttpHandler; 131-140 GetRequiredService<HttpClient>; where DirectPushCoordinator is registered)
    - DeckFlow.Studio/Services/ResilientHttpHandler.cs (18-120 — Polly-v8 transient retry)
    - DeckFlow.Studio/ViewModels/DirectPushCoordinator.cs (constructor; the pushed rows carry ContentSiteIndexRow.BodySha256 = expected hash + natural key via ContentIndexExportRow.From)
    - 90-CONTEXT.md D-09 (REVISED) — the hash-match rationale (defeats all four races)
    - DeckFlow.Studio.Tests (existing MockHttp usage, if any; RichardSzalay.MockHttp is in-solution)
  </read_first>
  <behavior>
    - The confirmer, for a (naturalKeyType, naturalKeyValue, expectedBodySha256) tuple, GETs the deployed-body-hash endpoint with admin BasicAuth and returns CONFIRMED only when the response is 200 AND the returned bodySha256 == expectedBodySha256 (ordinal).
    - A 404 (body not yet at /app), a hash mismatch (old body still deployed), or a transient error is "not yet confirmed" -> the confirmer retries with backoff up to a bounded cap, then returns NOT-CONFIRMED (never blocks forever, never a false positive).
    - ConfirmAndPublishAsync (Task 1) runs ONLY for rows the confirmer reports CONFIRMED; not-confirmed rows stay content-upserted + awaiting-confirm (marker set, is_visible false, pushed_utc null).
    - The confirmer is a separate seam so coordinator ordering tests use a fake; the confirmer's own HTTP/poll logic is unit-tested with a stubbed handler.
  </behavior>
  <action>Create `DeckFlow.Studio/Services/IDeployedBodyConfirmer.cs` (`Task<bool> IsDeployedBodyConfirmedAsync(string naturalKeyType, string naturalKeyValue, string expectedBodySha256, CancellationToken ct)`) and `DeckFlow.Studio/Services/DeployedBodyConfirmer.cs` implementing it — SEPARATE files, NOT inlined into the coordinator (Codex MED #5). The impl: build the URL from `Studio:PublicSiteBaseUrl` + `/Admin/api/contentkb/deployed-body-hash` + the natural-key query (URL-encode), attach admin BasicAuth (`Studio:AdminUser`/`Studio:AdminPassword` — do NOT hardcode); inject the singleton `HttpClient` (Program.cs:121-124); poll in a bounded loop (e.g. N attempts with capped exponential/linear backoff — a small fixed cap, config-overridable): on 200 parse `{ bodySha256 }` and return true if it equals `expectedBodySha256` (ordinal); on 404 / mismatch / transient -> wait + retry; after the cap return false. Do NOT hardcode the domain/creds; do NOT build a second hash routine (compare strings only). Register the confirmer in Program.cs (singleton/scoped consistent with the coordinator + HttpClient). Inject `IDeployedBodyConfirmer` into DirectPushCoordinator and add `VerifyAndPublishAsync(publishRows, ct)` that, per pushed row, derives (naturalKey via ContentIndexExportRow.From, expected hash via row.BodySha256), calls the confirmer, and invokes `ConfirmAndPublishAsync` ONLY for confirmed rows; leaves not-confirmed rows awaiting-confirm. Add `FakeDeployedBodyConfirmer` to DirectPushCoordinatorTests: confirmer-false -> no stamp/visibility, marker retained; confirmer-true -> ConfirmAndPublishAsync runs (stamp+visibility+marker clear). Add `DeckFlow.Studio.Tests/Services/DeployedBodyConfirmerTests.cs` using a stubbed HttpMessageHandler (MockHttp): 200+matching hash -> confirmed; 200+mismatched hash -> not confirmed (bounded retries then false); 404 -> not confirmed after retries; verifies the BasicAuth header + natural-key query are sent.</action>
  <verify>
    <automated>dotnet build DeckFlow.Studio.Tests -c Debug; dotnet test DeckFlow.Studio.Tests --filter "FullyQualifiedName~DirectPushCoordinator|FullyQualifiedName~DeployedBodyConfirmer"</automated>
  </verify>
  <acceptance_criteria>
    - SOURCE: `IDeployedBodyConfirmer` + `DeployedBodyConfirmer` are their OWN files (not inside the coordinator); the impl GETs the deployed-body-hash endpoint by natural key with admin BasicAuth and returns true only on 200 && returned bodySha256 == expected (ordinal); URL/creds come from config (grep shows no hardcoded domain/creds).
    - SOURCE: the poll is BOUNDED (a cap on retries) and treats 404/mismatch/transient as "retry then give up false" — never an infinite loop, never a false positive; string compare only (no second hash routine).
    - SOURCE: `ConfirmAndPublishAsync` is invoked only for confirmed rows; not-confirmed rows are left awaiting-confirm (read confirms the gate); Program.cs registers the confirmer.
    - BEHAVIOR: coordinator tests (fake confirmer) prove false->no stamp/visibility+marker retained, true->publish; confirmer tests prove match->confirmed, mismatch/404->not-confirmed-after-bounded-retries, and BasicAuth+natural-key are sent.
  </acceptance_criteria>
  <done>A bounded hash-match poll of the /app endpoint (by natural key) gates the stamp/visibility flip; confirmer lives in its own files; ordering + HTTP logic unit-tested; no false positives.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Studio -> web deployed-body-hash endpoint (authenticated GET) | server-to-server hash read with admin BasicAuth |
| Studio -> prod store (stamp/visibility) | the contract step; must only run after a hash-match confirm |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-90-11 | Spoofing / false-positive confirm | confirming a stale or missing body | mitigate | D-09 REVISED: confirm requires 200 AND bodySha256 == expected (same ComputeBodySha256 both sides). An un-deployed update returns the OLD hash (mismatch), a new row / missing body 404s, so a stale/missing body can NEVER produce a false confirm. |
| T-90-12 | Availability (infinite poll / deadlock) | poll never terminates | mitigate | The confirmer poll is BOUNDED (retry cap + backoff); after the cap it returns NOT-CONFIRMED and the row stays durably awaiting-confirm (Plan 90-03 marker), resumable via Plan 90-06 — never a silent infinite loop. |
| T-90-13 | Availability (stuck-hidden rows) | not-confirmed rows never go visible | accept (mitigated) | Not-confirmed rows stay durably in awaiting-confirm and are resumable (Plan 90-06); [skip render] was dropped (Plan 90-04) so a real deploy DOES happen; operator can re-run verify. |
| T-90-19 | Info disclosure (admin creds in Studio) | BasicAuth creds for the endpoint | mitigate | Creds/URL read from Studio config (not hardcoded, not logged); reuse the admin creds Studio already holds; the endpoint is read-only (Plan 90-07). |
| T-90-SC | Supply chain | package installs | mitigate | No new packages — existing HttpClient/ResilientHttpHandler + in-solution MockHttp for tests. No install step. |
</threat_model>

<verification>
- DeckFlow.Studio + DeckFlow.Studio.Tests build clean.
- Content-only write leaves prod unstamped/hidden + marker set; post-confirm stamps+flips visible prod-before-local + clears marker.
- Confirmer gates the contract on 200 && hash==expected, bounded retries; false -> no stamp/visibility (marker retained); true -> full publish. Coordinator ordering tested with a fake confirmer; confirmer HTTP/poll tested with a stubbed handler (no live HTTP).
- MANUAL (async, live Render — NOT unit-coverable): the real poll against the live endpoint after a Render autodeploy + actual deploy timing are verified by the operator at flag-flip time; Phase 93 locks the full loop. Flagged explicitly — not faked as unit-covered.
</verification>

<success_criteria>
- `is_visible`/`pushed_to_prod_utc` flip only after the confirmer polls the Plan 90-07 endpoint by natural key and gets 200 && bodySha256==expected (bounded retries, no false positives, no deadlock); content-only upsert never contracts; prod-first-then-local ordering + F-51-PG-01 handling + the D-03 approval mirror preserved; awaiting-confirm marker set on push and cleared on confirm.
</success_criteria>

<output>
Create `.planning/phases/90-directpush-correctness-seed-sync/90-05-SUMMARY.md` when done.
</output>
