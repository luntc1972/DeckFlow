## Phase 999.5-01 Summary

- Files modified: `DeckFlow.Web.Tests/FeedbackStoreTests.cs`, `DeckFlow.Web.Tests/Security/AdminBruteForceTrackerStoreTests.cs`, `DeckFlow.Web.Tests/ScryfallThrottleTests.cs`, `DeckFlow.Web.Tests/DeckAnalysisPacketServiceTests.cs`, `DeckFlow.Web.Tests/DeckComparisonServiceTests.cs`, `.planning/phases/999.5-v1-3-backlog-catch-up-test-hardening/999.5-01-SUMMARY.md`.
- Scope: P01 — Test-suite hardening. Close 4 pre-existing test-failure clusters surfaced by `dotnet test DeckFlow.sln -c Release` on 2026-05-21 (baseline `441 / 414 / 24 / 3`). All 14 P01-targeted failures (~10 FeedbackStore SQLite race + 2 ScryfallThrottle 429 timing + 1 DeckAnalysisPacket prose rot + 1 DeckComparison CRLF) returned to passing.
- Decisions delivered: D-01, D-02, D-03 (with executor-scoped extension after Codex pass-1 MED-1 + post-Task-4 sweep miss), D-04, D-05, D-06, D-07, D-08. Cross-cutting D-18..D-23 honored throughout (wave 1, commit-per-task, plain author, Windows-side dotnet gate, test-triplet reporting in every commit body).
- Cluster 1 — FeedbackStore SQLite pool race (D-01/D-03): `SqliteConnection.ClearAllPools(); GC.Collect(); GC.WaitForPendingFinalizers();` 3-line preamble inserted before every `File.Delete` site that disposes a `FeedbackStore` / `AdminBruteForceTrackerStore`-backed temp DB. Raw `File.Delete` (no try/catch rescue) in `FeedbackStoreTests` per D-01 corrected rationale — pool drain eliminates the race; if File.Delete still throws, that's a real bug we must see, not silently swallow. `AdminBruteForceTrackerStoreTests` legacy try/catch preserved per D-03 corrected scope. 3 File.Delete sites covered total — `FeedbackStoreTests.cs:31` (Dispose), `FeedbackStoreTests.cs:186` (in-fact `otherDb` finally), `AdminBruteForceTrackerStoreTests.cs:34` (Dispose with legacy try/catch).
- Cluster 2 — ScryfallThrottle 429-no-Retry-After timing (D-04/D-05/D-06): test was stale, not production behavior. `ScryfallThrottle.cs:117-122` deliberately retries 429-without-Retry-After using `FallbackRetryDelay` for Cloudflare BIC/burst recovery (intentional + inline-documented in the SUT). Renamed both facts to `ExecuteAsync_{NonGeneric|Generic}_RetriesFor429WithFallbackDelay_WhenRetryAfterMissing`; flipped assertion `Assert.Equal(1, calls)` → `Assert.Equal(3, calls)` with inline comment cross-referencing `ScryfallThrottle.cs:30` (`MaxRetryAttempts` is `private const`, not visible to test assembly via `[InternalsVisibleTo]`, so hardcoded `3` per D-06 fallback). Other 429 facts (`AboveRetryAfterCap`, `WithZeroRetryAfter`, `Returns200ResponseAsIs`) stay byte-identical.
- Cluster 3 — DeckAnalysisPacket prose rot (D-07): mechanical single-token swap `ChatGPT` → `AI` in the expected validation message at `DeckAnalysisPacketServiceTests.cs:360`. Catches the test up to Phase 999.1 shipped AI-agnostic prose. SUT untouched.
- Cluster 4 — DeckComparison CRLF assertion (D-08): normalized actual via `.Replace("\r\n", "\n")` in `BuildAsync_EmitsCommanderSection_WhenDeckHasNoExplicitCommanderHeader` (lines ~283-290) before `Assert.StartsWith` / `Assert.Contains`. Kept expected literals readable. SUT untouched — platform-line-ending emission via `StringBuilder.AppendLine` is documented intent.
- D-03 sweep extension discovered mid-execution: Task 4's full-suite gate revealed `HashIp_DifferentSalts_ProduceDifferentHashes` still failing because D-03's original sweep pattern (`File.Delete(_dbPath)` grep) missed in-method `File.Delete(otherDb)` calls in `finally` blocks. Task 5 extended the preamble to line 186; grep sweep confirmed all 3 sites in `DeckFlow.Web.Tests/` are now covered.
- Build gate (D-21): every task green — `"/mnt/c/Program Files/dotnet/dotnet.exe" build DeckFlow.sln -c Release -nologo` returns `0 Warning(s) 0 Error(s)`. Well within post-Phase-999.4 `4af987c` baseline.
- Test gate (D-22): per-task focused gates all green; final full-suite gate (post-Task 5): `Failed 9, Passed 429, Skipped 3, Total 441`. Delta vs pre-P01 baseline (`441 / 414 / 24 / 3`): **+15 passed, -15 failed**. P01-targeted failures: **14/14 resolved** (all 4 clusters). Residual 9 failures are pre-existing, outside P01 scope (`PacketArtifactStoreTests`, `ArchidektCacheJobServiceTests`, `AiPlatformPhase10RoundTripTests`, `BasicAuthMiddlewareTests`) — deferred per D-22 ("Pre-existing failures NOT in scope stay deferred").
- D-23 binding (test-triplet reporting): every commit body includes the focused-test triplet (`Passed X, Failed Y, Skipped Z, filter: <expression>`). Grep counts alone insufficient — full xUnit run via Windows-side dotnet path is the only acknowledged truth source.
- Plan execution strategy deviation: original plan assumed Codex would run the D-21/D-22 build+test gate via its own sandbox. Codex sandbox failed to reach Windows-side `dotnet.exe` via WSL vsock interop (`UtilBindVsockAnyPort:309: socket failed 1`) on first dispatch. Orchestrator (Claude) took over the gate + commit ceremony; Codex retained authorship of every code edit. Hybrid model: Codex writes code in WSL filesystem, orchestrator runs Windows dotnet, orchestrator commits with D-23-compliant body. No deviation in CONTEXT.md decisions — only in *who* runs which workflow step. Pattern preserved for future Codex-cross-AI runs that hit the same sandbox barrier.
- Codex sessions: single Codex thread spanned Tasks 1-5 (threadId `019e4d83-9123-7093-b7cd-f0de03a40705`). Each task dispatch via `mcp__codex__codex-reply` after the previous task's commit landed. Codex never committed directly (sandbox vsock block); every commit by orchestrator under the project's local `luntc1972 / luntc1972@yahoo.com` git config per D-20.
- 5-commit hash sequence (P01 atomic per D-19, plus the in-flight Task 5 sweep extension):
  1. `71f9171` `fix(999.5): drain Sqlite pool in test Dispose to release file handle (D-01/D-03)`
  2. `5853b05` `fix(999.5): correct ScryfallThrottle 429-no-Retry-After test assertions (D-04/D-05/D-06)`
  3. `fd062b9` `fix(999.5): update DeckAnalysisPacketServiceTests:360 to AI-agnostic prose (D-07)`
  4. `3363206` `fix(999.5): normalize CRLF in DeckComparisonServiceTests:286 assertion (D-08)`
  5. `a995166` `fix(999.5): extend SQLite pool drain to in-method File.Delete (D-03 sweep extension)`
  6. (this commit) `docs(999.5): summary for plan 01 test-suite hardening`
- Closure commit hash: pending — fill SHA after commit lands.
- must_haves status (per 999.5-01-PLAN.md frontmatter):
  - "All 4 P01 test-failure clusters return passing": **MET** — FeedbackStore 0 failed, ScryfallThrottle 0 failed, DeckAnalysisPacket 0 failed, DeckComparison 0 failed.
  - "Build warning count <= post-Phase-999.4 4af987c baseline": **MET** — 0 warnings.
  - "Full-suite delta: P01-targeted failures resolved; pre-existing out-of-scope failures may remain": **MET** — 14/14 P01 fails resolved; 9 pre-existing remain (deferred per D-22).
  - "Plain-author commits with D-23-compliant bodies": **MET** — all 6 commits by `luntc1972 <luntc1972@yahoo.com>`, no `Co-Authored-By` trailer, every fix commit body carries the test triplet.
- Next step: Wave 2 dispatch — 999.5-02-PLAN (D-09..D-13 semantic guards), 999.5-03-PLAN (D-14..D-17 harvest archival), 999.5-04-PLAN (D-24..D-28 ChatGPT `<result>` strip). All three depend on P01 (now landed). Orchestrator-led hybrid execution pattern continues — Codex writes code, orchestrator runs gates + commits.
