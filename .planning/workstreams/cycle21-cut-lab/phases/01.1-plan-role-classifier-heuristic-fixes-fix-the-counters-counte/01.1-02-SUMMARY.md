Delta disposition: accepted, with one authorized test-oracle correction in
`PlanRoleClassifierTests.FromHeuristic_ProtectionPermanent_EarnsInteractionInBothModes`.
The plan's own synthetic enchantment oracle used plural-subject wording
(`"permanents you control phase out"`) that falls inside the D-06 known
limitation gap, while the production fix in `PlanRoleClassifier` was correct.
The third assertion now tests the supported singular phrasing
`"{T}: Target creature phases out."`

No golden, byte-identity, health-band, or fixture expectation was edited at
this checkpoint. `ManabaseHealthBandRegressionTests` held at `("Solid",
"Solid")` with 18/18 passing via
`dotnet test DeckFlow.Web.Tests/DeckFlow.Web.Tests.csproj --filter "FullyQualifiedName~ManabaseHealthBandRegressionTests"`.

D-06 decision: follow-up phase requested to widen
`DeckStatClassifier.IsProtectionCard`. The cards still scoring `PlanRole.None`
after this ticket are Swiftfoot Boots, Lightning Greaves, Hexing Squelcher,
Goblin Chirurgeon, and Mother of Runes. This matters for Phase 2 because
interaction floors per commander are measured through this same classifier, so
an under-detecting protection predicate biases the research corpus.

Requirement traceability gap: Phase 01.1 still has no ratified requirement ID
in `REQUIREMENTS.md`. The proposed `CLSF-01` / `CLSF-02` pair is documented in
`01.1-01-PLAN.md` and remains pending ratification.

Observation for the follow-up phase scope: `IsProtectionCard`'s verb agreement
is inconsistent across its own four oracle needles. It matches singular
`"gains hexproof"` and `"gains indestructible"`, plural
`"gain protection from"`, and singular `"phases out"`. That means it
under-detects in both directions depending on a card's phrasing, which argues
for a corpus-backed vocabulary pass instead of adding needles one at a time.
