---
phase: 29-core-xml-doc-backfill-gate-widen
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - DeckFlow.Core/Integration/DeckImporterInterfaces.cs
  - DeckFlow.Core/Integration/ArchidektRecentDecksImporter.cs
  - DeckFlow.Core/Integration/MoxfieldApiUrl.cs
  - DeckFlow.Core/Integration/ArchidektApiUrl.cs
  - DeckFlow.Core/Integration/MoxfieldApiDeckImporter.cs
  - DeckFlow.Core/Exporting/DeltaExporter.cs
  - DeckFlow.Core/Exporting/FullImportExporter.cs
  - DeckFlow.Core/Exporting/MoxfieldTextExporter.cs
  - DeckFlow.Core/Exporting/CategoryNormalization.cs
  - DeckFlow.Core/Parsing/IParser.cs
  - DeckFlow.Core/Parsing/ArchidektParser.cs
  - DeckFlow.Core/Parsing/MoxfieldParser.cs
  - DeckFlow.Core/Parsing/DeckParseException.cs
  - DeckFlow.Core/Models/PrintingChoice.cs
  - DeckFlow.Core/Models/MatchMode.cs
  - DeckFlow.Core/Normalization/CardNormalizer.cs
  - DeckFlow.Core/Diffing/DiffEngine.cs
autonomous: true
requirements: [HSK-01]

must_haves:
  truths:
    - "Probe build over DeckFlow.Core/Integration|Exporting|Parsing|Models|Normalization|Diffing reports 0 CS1591/CS1573/CS1587 warnings"
    - "Every public type and member in the 17 files has an XML doc comment"
    - "The MoxfieldApiDeckImporter constructor CS1573 site has its executeAsync <param> tag completed"
    - "Each enum (PrintingChoice, MatchMode) has a type <summary> AND a per-member <summary>"
  artifacts:
    - path: "DeckFlow.Core/Models/PrintingChoice.cs"
      provides: "Documented enum type + 3 members"
      contains: "/// <summary>"
    - path: "DeckFlow.Core/Models/MatchMode.cs"
      provides: "Documented enum type + members"
      contains: "/// <summary>"
    - path: "DeckFlow.Core/Integration/MoxfieldApiDeckImporter.cs"
      provides: "Documented importer (2 sites incl. 1 CS1573 executeAsync param)"
      contains: "param name=\"executeAsync\""
    - path: "DeckFlow.Core/Parsing/IParser.cs"
      provides: "Documented parser interface methods (2 sites)"
      contains: "/// <summary>"
  key_links:
    - from: "DeckFlow.Core/Integration/MoxfieldApiDeckImporter.cs"
      to: "executeAsync constructor parameter"
      via: "completed CS1573 <param> tag"
      pattern: "param name=\"executeAsync\""
---

<objective>
Add XML doc comments to every undocumented public type and member in the remaining six Core folders: `Integration/` (5 files, ~13 sites incl. 1 CS1573), `Exporting/` (4 files, ~10 sites), `Parsing/` (4 files, ~7 sites), `Models/` (2 enum files, ~7 sites), `Normalization/` (1 file, 2 sites), `Diffing/` (1 file, 2 sites). 17 files, ~41 compiler-reported sites total.

Purpose: This plan closes the long tail of the HSK-01 Core backfill - all files not covered by 29-01/02/03. None of these files contain raw-string literals or init accessors. Completing them is the last prerequisite for the Wave-2 gate widen.
Output: 17 fully-documented source files; probe build over these six folders reports 0 doc warnings.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<scope_fence>
ALLOWED FILE SET (Codex MUST NOT touch any source file outside this list) - exactly the 17 files in `files_modified` above, spanning `DeckFlow.Core/Integration/`, `Exporting/`, `Parsing/`, `Models/`, `Normalization/`, `Diffing/`.

ALLOWED ARTIFACT (workflow output, exempt from the source-edit fence):
- .planning/phases/29-core-xml-doc-backfill-gate-widen/29-04-SUMMARY.md

PARALLEL-EXECUTION REALITY: GSD execute-phase runs all four Wave-1 plans (29-01..29-04) as parallel subagents in ONE shared worktree. Your three sibling plans will be editing their own Core folders concurrently. Design for that:
- RELAXED CLEAN-TREE PRECONDITION: before starting, run `git status --short`. Modifications confined to the Wave-1 sibling allowed source sets (`DeckFlow.Core/Storage/`, `DeckFlow.Core/Reporting/`, `DeckFlow.Core/Filtering/`, `DeckFlow.Core/Knowledge/`) and `.planning/phases/29-core-xml-doc-backfill-gate-widen/29-0*-SUMMARY.md` are EXPECTED — ignore them. STOP and report ONLY if a modified/untracked file appears outside BOTH this plan's allowed set AND every Wave-1 sibling allowed set (e.g., `DeckFlow.Web/`, `DeckFlow.CLI/`, repo-root files, or any Core file not under one of the Wave-1 folders). Do NOT touch any file owned by a sibling plan.

PROBE-FILE LOCK (`DeckFlow.Core/.editorconfig` is an EXCLUSIVE lock SHARED by all four Wave-1 plans, guarded by an ATOMIC sidecar lock dir):
- The probe file MUST be named exactly `DeckFlow.Core/.editorconfig` — only that name overrides Roslyn severity at the `DeckFlow.Core/` directory, so per-plan unique filenames do NOT work; collision is handled by mutual exclusion, not renaming.
- ATOMIC ACQUIRE (a `[ -e ]`-then-`printf` test is NOT atomic — two parallel agents can both observe "absent" and both clobber the probe). Use `mkdir DeckFlow.Core/.editorconfig.probe.lock` as the mutex: `mkdir` is atomic and succeeds for exactly one contender. Poll every 3s, give up after 120s (40 attempts). On timeout, treat the lock as stale ONLY if `DeckFlow.Core/.editorconfig` is absent OR older than ~5 minutes (`find DeckFlow.Core/.editorconfig -mmin +5`); in that case remove the stale lock dir + stale probe file and retry the mkdir ONCE; otherwise STOP with a clear error.
- WRITE/REMOVE the `DeckFlow.Core/.editorconfig` probe file ONLY while holding the lock dir.
- TRAP CLEANUP: wrap the whole locked probe section with `trap 'rm -f DeckFlow.Core/.editorconfig; rmdir DeckFlow.Core/.editorconfig.probe.lock 2>/dev/null || true' EXIT INT TERM` so an interrupted/crashed build never leaves the probe file or lock dir behind to falsify a sibling's probe.
- HOLD MINIMALLY: acquire lock → write probe → `rm -rf DeckFlow.Core/obj DeckFlow.Core/bin` → build (capture exit code) → capture/dedup warnings → IMMEDIATELY remove probe file → `rmdir` the lock dir → clear the trap (`trap - EXIT INT TERM`). Never hold the lock across a doc-editing step.
- RELEASE: confirm `DeckFlow.Core/.editorconfig` AND `DeckFlow.Core/.editorconfig.probe.lock` are gone; neither may be committed nor appear in `git status` at plan end.
Do NOT touch `.editorconfig` at repo root. Do NOT touch Storage/, Reporting/, Filtering/, Knowledge/, Content/, or Loading/ (owned by other plans or already documented). Ignore tasks belonging to plans 29-01, 29-02, 29-03, 29-05.
Note: `Exporting/CategoryNormalization.cs` may already be fully documented (RESEARCH Open Question 2 - probe showed 0 sites). It is in the allowed set so you may add the single flagged site if the probe reports one; if the probe shows 0 for it, leave it untouched.
</scope_fence>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-core-xml-doc-backfill-gate-widen/29-RESEARCH.md
@.planning/phases/29-core-xml-doc-backfill-gate-widen/29-PATTERNS.md

# Gold-standard doc-style exemplars (READ - match this style exactly):
@DeckFlow.Core/Content/IContentVideoStore.cs
@DeckFlow.Core/Content/IContentSourceStore.cs
@DeckFlow.Core/Integration/DeckImporterInterfaces.cs
</context>

<constraints>
From CLAUDE.md + 29-RESEARCH.md (full authority - every constraint is blocking):
- Touch ONLY the `///` doc lines you add. Do NOT run Format Document / Code Cleanup.
- NEVER convert `{ get; init; }` to `{ get; }`. None of these files contain init accessors, but the rule stands.
- NEVER re-indent C# raw-string literals. None of these 17 files contain raw strings (per RESEARCH); if you encounter one, do not touch it.
- NEVER inline an attribute onto a declaration line.
- Preserve LF line endings.
- CS1573 is all-or-nothing: MoxfieldApiDeckImporter constructor needs its `executeAsync` `<param>` tag added in positional order without removing existing tags.
- Enums: CS1591 fires on the enum TYPE and on EACH enum member - document both (per 29-PATTERNS.md Pattern 5).
- Build env: `"/mnt/c/Program Files/dotnet/dotnet.exe"`. Clean build is the gate.
- Every authoritative probe build MUST first remove `DeckFlow.Core/obj` and `DeckFlow.Core/bin` so the inventory is trustworthy (stale obj/ hides warnings).
- Commits: plain default-author; one logical change per commit.
</constraints>

<tasks>

<task type="auto">
  <name>Task 1: Document Integration folder (incl. MoxfieldApiDeckImporter CS1573)</name>
  <files>DeckFlow.Core/Integration/DeckImporterInterfaces.cs, DeckFlow.Core/Integration/ArchidektRecentDecksImporter.cs, DeckFlow.Core/Integration/MoxfieldApiUrl.cs, DeckFlow.Core/Integration/ArchidektApiUrl.cs, DeckFlow.Core/Integration/MoxfieldApiDeckImporter.cs</files>
  <read_first>
    - DeckFlow.Core/Integration/DeckImporterInterfaces.cs (current state - partial docs already present; enum + interfaces)
    - DeckFlow.Core/Integration/ArchidektRecentDecksImporter.cs (current state - 3 sites)
    - DeckFlow.Core/Integration/MoxfieldApiUrl.cs (current state - 2 sites)
    - DeckFlow.Core/Integration/ArchidektApiUrl.cs (current state - 2 sites)
    - DeckFlow.Core/Integration/MoxfieldApiDeckImporter.cs (current state - 2 sites incl. constructor missing executeAsync param)
    - .planning/phases/29-core-xml-doc-backfill-gate-widen/29-RESEARCH.md (CS1573 sites table; Probe methodology - RESEARCH per-file counts are expected starting points; the Task-3 probe is authoritative)
    - .planning/phases/29-core-xml-doc-backfill-gate-widen/29-PATTERNS.md (Pattern 5 enum docs; Pattern 10 CS1573 fix; Pattern 1 method docs)
  </read_first>
  <action>
    Document each Integration site. For `MoxfieldApiDeckImporter.cs` complete the constructor CS1573 by adding `<param name="executeAsync">` (describing the test-seam delegate) in its positional slot - do NOT remove existing `<param>` tags or reorder them. For `DeckImporterInterfaces.cs` add `<summary>` only to the undocumented members; do NOT rewrite the partial docs already present. For the two ApiUrl helper classes and ArchidektRecentDecksImporter add `<summary>` to the type and each undocumented public member, with `<param>`/`<returns>` per Pattern 1. Match the DeckImporterInterfaces.cs and Content KB exemplar style. (Defer the authoritative probe to Task 3 to minimize probe-lock contention with sibling plans.)
  </action>
  <verify>
    <automated>grep -c 'param name="executeAsync"' DeckFlow.Core/Integration/MoxfieldApiDeckImporter.cs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'param name="executeAsync"' DeckFlow.Core/Integration/MoxfieldApiDeckImporter.cs` is at least 1.
    - Each of the 5 Integration files contains at least one added `/// <summary>` (or the executeAsync param tag) where a site was bare.
    - `git diff --stat DeckFlow.Core/Integration/` lists only Integration files at this point (path-scoped so concurrent sibling edits do not appear).
  </acceptance_criteria>
  <done>All Integration sites documented incl. the MoxfieldApiDeckImporter CS1573 param completion.</done>
</task>

<task type="auto">
  <name>Task 2: Document Models enums, Parsing, Diffing, Normalization</name>
  <files>DeckFlow.Core/Models/PrintingChoice.cs, DeckFlow.Core/Models/MatchMode.cs, DeckFlow.Core/Parsing/IParser.cs, DeckFlow.Core/Parsing/ArchidektParser.cs, DeckFlow.Core/Parsing/MoxfieldParser.cs, DeckFlow.Core/Parsing/DeckParseException.cs, DeckFlow.Core/Diffing/DiffEngine.cs, DeckFlow.Core/Normalization/CardNormalizer.cs</files>
  <read_first>
    - DeckFlow.Core/Models/PrintingChoice.cs (current state - enum type + 3 members all bare)
    - DeckFlow.Core/Models/MatchMode.cs (current state - enum type + members)
    - DeckFlow.Core/Parsing/IParser.cs (current state - type doc present, 2 methods bare)
    - DeckFlow.Core/Parsing/ArchidektParser.cs (current state - 2 sites)
    - DeckFlow.Core/Parsing/MoxfieldParser.cs (current state - 2 sites)
    - DeckFlow.Core/Parsing/DeckParseException.cs (current state - 1 site)
    - DeckFlow.Core/Diffing/DiffEngine.cs (current state - 2 sites)
    - DeckFlow.Core/Normalization/CardNormalizer.cs (current state - 2 sites)
    - .planning/phases/29-core-xml-doc-backfill-gate-widen/29-PATTERNS.md (Pattern 5 enum exact output for PrintingChoice; Pattern 7 IParser exact output; Pattern 1 method docs)
  </read_first>
  <action>
    Document each site. For the two enums (`PrintingChoice.cs`, `MatchMode.cs`): add a multi-line `<summary>` on the enum type AND a single-line `<summary>` on each enum member - use the exact PrintingChoice output shape in 29-PATTERNS.md Pattern 5 (Unresolved/KeepArchidekt/UseMoxfield) and analogous concise summaries for MatchMode members. For `IParser.cs` add member docs to `ParseFile` and `ParseText` exactly as 29-PATTERNS.md Pattern 7 shows; do NOT rewrite the existing type doc. For `ArchidektParser.cs`, `MoxfieldParser.cs`, `DeckParseException.cs`, `DiffEngine.cs`, `CardNormalizer.cs` add `<summary>` to undocumented types and `<summary>`/`<param>`/`<returns>` to undocumented public members per Pattern 1. For the exception type, document the type and any public constructor. (Defer the authoritative probe to Task 3.)
  </action>
  <verify>
    <automated>grep -c "/// <summary>" DeckFlow.Core/Models/PrintingChoice.cs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "/// <summary>" DeckFlow.Core/Models/PrintingChoice.cs` is at least 4 (type + 3 members).
    - `grep -c "/// <summary>" DeckFlow.Core/Models/MatchMode.cs` is at least 4 (type + 3 members).
    - `grep -c "/// <summary>" DeckFlow.Core/Parsing/IParser.cs` is at least 3 (existing type + 2 methods).
  </acceptance_criteria>
  <done>Both enums (type + every member), IParser methods, and the Parsing/Diffing/Normalization files documented.</done>
</task>

<task type="auto">
  <name>Task 3: Document Exporting folder and probe-verify all six folders clean</name>
  <files>DeckFlow.Core/Exporting/DeltaExporter.cs, DeckFlow.Core/Exporting/FullImportExporter.cs, DeckFlow.Core/Exporting/MoxfieldTextExporter.cs, DeckFlow.Core/Exporting/CategoryNormalization.cs</files>
  <read_first>
    - DeckFlow.Core/Exporting/DeltaExporter.cs (current state - 4 sites)
    - DeckFlow.Core/Exporting/FullImportExporter.cs (current state - 4 sites)
    - DeckFlow.Core/Exporting/MoxfieldTextExporter.cs (current state - 2 sites)
    - DeckFlow.Core/Exporting/CategoryNormalization.cs (current state - probe may show 0 sites; see scope_fence note)
    - .planning/phases/29-core-xml-doc-backfill-gate-widen/29-RESEARCH.md (CS1591 fix shape for FullImportExporter; Open Question 2 re CategoryNormalization; Probe build methodology - temp DeckFlow.Core/.editorconfig override + clean obj/ + sort -u dedup)
    - .planning/phases/29-core-xml-doc-backfill-gate-widen/29-PATTERNS.md (Pattern 1 method docs; paramref usage)
  </read_first>
  <action>
    Document the Exporting files. For each undocumented type add a `<summary>`; for each public method (e.g., FullImportExporter `WriteFile`/`ToText`, DeltaExporter, MoxfieldTextExporter) add `<summary>` plus `<param>` for every parameter using `<paramref name="path"/>` style and `<returns>` when a value is returned, matching the RESEARCH "CS1591 fix shape" example and the Content KB exemplars. If the probe reports 0 sites for `CategoryNormalization.cs`, leave it untouched. Then run the AUTHORITATIVE plan-04 probe via the verify snippet: it ATOMICALLY acquires the `DeckFlow.Core/.editorconfig` lock (mkdir sidecar lock dir, 120s cap, stale recovery), installs a `trap` so an interrupt cannot leave the probe/lock behind, drops obj/bin, builds, EXPLICITLY checks the dotnet build exit code (fail before interpreting warnings if the build itself failed), filters deduped warnings to `Integration/|Exporting/|Parsing/|Models/|Normalization/|Diffing/` only (validates all three tasks of this plan; a sibling's incomplete folder must never fail this plan), then releases the lock and clears the trap. Confirm ZERO CS1591/CS1573/CS1587 warnings remain for any file under those six folders.
  </action>
  <verify>
    <automated>DOTNET="/mnt/c/Program Files/dotnet/dotnet.exe"; LOCK=DeckFlow.Core/.editorconfig.probe.lock; PROBE=DeckFlow.Core/.editorconfig; trap 'rm -f "$PROBE"; rmdir "$LOCK" 2>/dev/null || true' EXIT INT TERM; n=0; until mkdir "$LOCK" 2>/dev/null; do n=$((n+1)); if [ "$n" -ge 40 ]; then if [ ! -e "$PROBE" ] || [ -n "$(find "$PROBE" -mmin +5 2>/dev/null)" ]; then echo "stale lock - recovering"; rm -f "$PROBE"; rmdir "$LOCK" 2>/dev/null || true; mkdir "$LOCK" 2>/dev/null && break; fi; echo "FAIL could not acquire probe lock after 120s"; exit 1; fi; sleep 3; done; printf '%s\n' 'root = false' '' '[*.cs]' 'dotnet_diagnostic.CS1591.severity = warning' 'dotnet_diagnostic.CS1573.severity = warning' 'dotnet_diagnostic.CS1587.severity = warning' > "$PROBE"; rm -rf DeckFlow.Core/obj DeckFlow.Core/bin; "$DOTNET" build DeckFlow.Core/DeckFlow.Core.csproj --no-incremental 2>&1 | tee /tmp/29-04-build.log >/dev/null; BUILD_RC=${PIPESTATUS[0]}; rm -f "$PROBE"; rmdir "$LOCK" 2>/dev/null || true; trap - EXIT INT TERM; if [ "$BUILD_RC" -ne 0 ]; then echo "FAIL Core probe build errored (rc=$BUILD_RC) before warning analysis:"; grep -E "error CS" /tmp/29-04-build.log | head; exit 1; fi; grep -E "warning CS(1591|1573|1587)" /tmp/29-04-build.log | grep -E "Integration/|Exporting/|Parsing/|Models/|Normalization/|Diffing/" | sort -u > /tmp/29-04.log; if [ -s /tmp/29-04.log ]; then echo FAIL; cat /tmp/29-04.log; else echo "OK 0 doc warnings across the six folders"; fi</automated>
  </verify>
  <acceptance_criteria>
    - Probe (atomic-locked temp editorconfig, clean obj/, build-exit-code checked) yields 0 CS1591/CS1573/CS1587 warnings for any file under the six folders (`/tmp/29-04.log` empty) and a passing dotnet build exit code.
    - `DeckFlow.Core/.editorconfig` and `DeckFlow.Core/.editorconfig.probe.lock` removed; absent from `git status`.
    - `git diff --stat DeckFlow.Core/Integration/ DeckFlow.Core/Exporting/ DeckFlow.Core/Parsing/ DeckFlow.Core/Models/ DeckFlow.Core/Normalization/ DeckFlow.Core/Diffing/` lists only files from the 17-file allowed set (path-scoped so concurrent sibling edits do not appear).
    - `"/mnt/c/Program Files/dotnet/dotnet.exe" build DeckFlow.Core/DeckFlow.Core.csproj -c Release` reports 0 errors.
  </acceptance_criteria>
  <done>All Exporting files documented; probe across all six plan-04 folders shows 0 doc warnings; temp probe file and lock dir removed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer edit -> source tree | Large 17-file `///` sweep risks accidental code mutation or scope creep into adjacent files |
| temp probe file -> repo | A lingering `DeckFlow.Core/.editorconfig` would alter solution-wide warning severity |
| concurrent Wave-1 subagents -> shared worktree | Two plans creating/deleting `DeckFlow.Core/.editorconfig` simultaneously corrupts the other's probe |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-04-01 | Tampering | 17-file mass doc sweep | mitigate | path-scoped git diff --stat acceptance asserts only the 17 allowed files changed; per-task scope fence |
| T-29-04-02 | Tampering | MoxfieldApiDeckImporter CS1573 + enum member docs | mitigate | grep acceptance asserts executeAsync param + enum type/member summaries present; existing tags not removed |
| T-29-04-03 | Tampering | Temp probe `DeckFlow.Core/.editorconfig` | mitigate | Atomic mkdir-lock acquire/release + EXIT/INT/TERM trap; probe file removed and absence asserted; never committed |
| T-29-04-04 | Tampering | Concurrent sibling probe collision | mitigate | Atomic mkdir sidecar lock guarantees single writer; 120s cap with stale recovery; folder-scoped warning filter ignores sibling progress |
| T-29-04-05 | Denial of Service | Build pipeline | accept | Doc comments are compile-stripped; zero runtime/behavior change possible |
| T-29-04-SC | Tampering | npm/pip/cargo installs | n/a | No package installs in this phase |
</threat_model>

<verification>
- Probe build (atomic-locked temp editorconfig, clean obj/, build-exit-code checked) reports 0 CS1591/CS1573/CS1587 across Integration/Exporting/Parsing/Models/Normalization/Diffing.
- MoxfieldApiDeckImporter executeAsync param present; both enums fully documented (type + members).
- `git status --short` shows no `DeckFlow.Core/.editorconfig` nor `.editorconfig.probe.lock`.
- `git diff --stat` over the six folders lists only files in the 17-file allowed set.
- Full Core build clean (0 errors) under the still-active global suppressor.
</verification>

<success_criteria>
All ~41 undocumented public sites across the 17 files carry XML doc comments matching the Content KB exemplar style; the 1 CS1573 (executeAsync) is completed; both enums document type and every member; folder-scoped probe build is warning-free; only allowed files changed.
</success_criteria>

<output>
Create `.planning/phases/29-core-xml-doc-backfill-gate-widen/29-04-SUMMARY.md` when done.
</output>
