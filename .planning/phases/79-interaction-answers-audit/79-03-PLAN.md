---
phase: 79-interaction-answers-audit
plan: 03
type: execute
wave: 3
depends_on: [79-02]
files_modified:
  - DeckFlow.Web/Models/DeckAnalysisRequest.cs
  - DeckFlow.Web/Models/DeckAnalysisViewModel.cs
  - DeckFlow.Web/Services/DeckAnalysisPacketService.cs
  - DeckFlow.Web/Services/Persistence/PacketArtifactStore.cs
  - DeckFlow.Web/Controllers/DeckPacketController.cs
  - DeckFlow.Web/Views/Deck/DeckAnalysis.cshtml
  - DeckFlow.Web/wwwroot/css/site-common.css
  - DeckFlow.Web.Tests/InteractionAuditSurfaceContractTests.cs
  - DeckFlow.Web.Tests/DeckAnalysisInteractionAuditViewTests.cs
  - DeckFlow.Web/wwwroot/ts/e2e-placeholder-none.ts
autonomous: true
requirements: [INTERACT-01, INTERACT-03]
must_haves:
  truths:
    - "With the flag ON, the /deck-analysis Step-3 on-page readout shows the same five interaction buckets, card lists, and coverage-gap advisories as the paste artifact, hedged as an AI-rechecked first pass"
    - "The computed InteractionAudit round-trips through a hidden InteractionAuditJson field so Step-3 re-renders the readout without recomputation, AND is persisted as a 60-interaction-audit.json zip entry that LoadFromZip restores on re-upload"
    - "The round-tripped hidden field is treated as untrusted input: deserialization is size-capped, structurally validated (every bucket, every inner list, every card Name, every Quantity, the CoverageGaps list), and never throws"
    - "With the flag OFF, the DeckAnalysisPacketResult.InteractionAudit is null, request.InteractionAuditJson is empty, the hidden field + readout region are absent, and the RENDERED page + 3 artifacts + zip are byte-identical to the pre-feature baseline (page proven by a Razor IRazorViewEngine render excision-equality test; zip proven by an entry-map + per-platform content comparison)"
    - "New layout CSS lives in site-common.css using existing theme tokens + baked hex; NO new :root tokens are introduced (so no per-theme-file fork is required)"
  artifacts:
    - path: "DeckFlow.Web/Models/DeckAnalysisViewModel.cs"
      provides: "InteractionAudit view-model property"
      contains: "InteractionAudit"
    - path: "DeckFlow.Web/Services/Persistence/PacketArtifactStore.cs"
      provides: "60-interaction-audit.json zip writer (conditional) + loader restoring request.InteractionAuditJson"
      contains: "60-interaction-audit.json"
    - path: "DeckFlow.Web/Views/Deck/DeckAnalysis.cshtml"
      provides: "flag-guarded on-page readout block + CONDITIONAL hidden InteractionAuditJson field"
      contains: "InteractionAudit"
    - path: "DeckFlow.Web/wwwroot/css/site-common.css"
      provides: "interaction-audit readout layout classes"
      contains: "interaction-audit"
    - path: "DeckFlow.Web.Tests/InteractionAuditSurfaceContractTests.cs"
      provides: "flag-OFF artifact/zip byte-identity (entry-map + per-platform content) + download->upload zip round-trip + deserialize hardening"
    - path: "DeckFlow.Web.Tests/DeckAnalysisInteractionAuditViewTests.cs"
      provides: "page-level flag-OFF byte-identity via IRazorViewEngine render excision-equality (Phase-77 DeckAnalysisScoreViewTests pattern)"
      contains: "SplitAroundInteractionBlock"
  key_links:
    - from: "DeckPacketController Step-3 action"
      to: "DeckAnalysisViewModel.InteractionAudit + request.InteractionAuditJson"
      via: "InteractionAudit = result.InteractionAudit mapping + request.InteractionAuditJson = JsonSerializer.Serialize(result.InteractionAudit) (mirrors ScoreJson at 158-161/330)"
      pattern: "InteractionAudit = result.InteractionAudit"
    - from: "DeckAnalysis.cshtml @if (Model.InteractionAudit is not null)"
      to: "conditional hidden InteractionAuditJson textarea"
      via: "round-trip of the serialized audit between Step-2 and Step-3 + into the 60-interaction-audit.json zip entry"
      pattern: "InteractionAuditJson"
    - from: "PacketArtifactStore.BuildZip / LoadFromZip"
      to: "60-interaction-audit.json entry <-> request.InteractionAuditJson"
      via: "conditional write (dropped when empty -> flag-OFF zip byte-identical) + restore on re-upload"
      pattern: "60-interaction-audit.json"
    - from: "DeckAnalysisInteractionAuditViewTests"
      to: "DeckAnalysis.cshtml rendered output"
      via: "IRazorViewEngine render + prefix/suffix excision-equality between null and populated InteractionAudit"
      pattern: "SplitAroundInteractionBlock"
---

<objective>
Surface the Phase 79 interaction audit onto the /deck-analysis Step-3 ON-PAGE readout (mirroring the
paste-artifact content from Plan 79-02), round-trip the computed audit through a hidden
`InteractionAuditJson` field AND a `60-interaction-audit.json` zip entry so Step-3 re-renders and the
download/re-upload restores it, and lock the flag-OFF byte-identity contract for the page + three
artifacts + zip. Page byte-identity is proven at the render layer by a dedicated `IRazorViewEngine`
excision-equality test mirroring Phase 77's `DeckAnalysisScoreViewTests`; artifact/zip byte-identity is
proven by an entry-map + per-platform content contract test.

Purpose: INTERACT-01 requires the on-page readout (not just the artifact); INTERACT-03 (ROADMAP success
criterion #4 = page + 3 artifacts + zip byte-identical when OFF + round-trip restores) requires the
RENDERED page AND the zip to be byte-identical when the flag is OFF and to actually restore the audit
when ON. This mirrors the Phase-77 multi-axis-score hidden-field round-trip (Request.ScoreJson →
ViewModel.Score → result record → view guard + hidden field, with TryDeserializeScore hardening) AND its
render-level guard (DeckAnalysisScoreViewTests contiguous-suppressible-block excision-equality). NOTE:
ScoreJson round-trips ONLY through the hidden form field and is NOT persisted in the zip (verified: no
ScoreJson entry in PacketArtifactStore) — the score is recomputed on re-analysis. The interaction audit
must ADDITIONALLY survive the download zip (must_haves truth #2 + ROADMAP criterion #4 "zip ... round-trip
restores"), so it needs its OWN zip artifact modeled on the DeckProfileJson / 20-edh-top16-references.json
conditional-section precedent (written only when non-empty; restored in LoadFromZip).
Output: the hidden-field + zip round-trip plumbing, a flag-guarded readout block + CSS, the deep
untrusted-input deserialize guard, an entry-map/per-platform artifact/zip byte-identity + zip round-trip
contract test, and a page-level Razor render byte-identity test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md
@.planning/phases/77-multi-axis-deck-score/77-PATTERNS.md
@.planning/phases/79-interaction-answers-audit/79-02-PLAN.md

<interfaces>
<!-- Existing Phase-77 round-trip chain to mirror (Score → InteractionAudit). Extracted from the codebase. -->
From DeckFlow.Web/Models/DeckAnalysisRequest.cs:
  public string ScoreJson { get => _scoreJson; set => _scoreJson = value ?? string.Empty; }   // line 140, hidden-field round-trip
From DeckFlow.Web/Models/DeckAnalysisViewModel.cs:
  public DeckMultiAxisScore? Score { get; init; }                                             // line 34
From DeckFlow.Web/Controllers/DeckPacketController.cs:
  if (result.Score is not null) { request.ScoreJson = JsonSerializer.Serialize(result.Score); }  // lines 158-161 (and ~330) — hidden-field SOURCE write; mirror for InteractionAudit
  Score = result.Score,                                                                        // line 175 (result → view-model mapping in the Step-3 View(...))
From DeckFlow.Web/Services/DeckAnalysisPacketService.cs:
  DeckMultiAxisScore? Score = null                                                            // result record, line 65
  var savedScore = scoreFlagEnabled ? TryDeserializeScore(request.ScoreJson) : null;          // step-3 saved path, line 404
  ... Score: savedScore);                                                                      // line 416 (step-3) / Score: computedScore line 821 (computed)
  private static DeckMultiAxisScore? TryDeserializeScore(string? scoreJson)                    // line 1311: size-cap + IsStructurallyValidScore + catch(JsonException) → null
  private static bool IsStructurallyValidScore(DeckMultiAxisScore? score)                      // line 1336: null-guards every rationale.SignalText + band range 0..5 (threat T-77-04-01) — MIRROR THIS DEPTH
  private const int MaxScoreJsonLength = 8192;                                                 // line 1353

<!-- Conditional hidden-field render precedent (byte-identity guard). DeckAnalysis.cshtml lines 516-521. -->
  @if (!string.IsNullOrEmpty(Model.Request.ScoreJson))
  {
      @* Round-trips the multi-axis score across the Step-3 re-post; rendered only when present
         so the flag-OFF page stays byte-identical to baseline (no extra field). *@
      <textarea name="ScoreJson" hidden aria-hidden="true" tabindex="-1">@Model.Request.ScoreJson</textarea>
  }
  @if (Model.Score is not null) { <div class="chatgpt-score" role="region" ...> ... }          // line 539, flag-guarded readout block

<!-- Zip round-trip precedent (the REAL zip-persist analog — NOT ScoreJson, which is not zipped). PacketArtifactStore.cs. -->
  private static readonly HashSet<string> PacketAllowedNames = { ... "40-deck-profile.json", ... };  // line 28 — add "60-interaction-audit.json"
  public static byte[] BuildZip(DeckAnalysisRequest request, ..., string? canonicalDeckListText = null, string? originalDeckText = null)  // line 108 — add string? interactionAuditJson = null trailing optional
  // responseSections (line 134): ("40-deck-profile.json", "DECK PROFILE JSON", string.IsNullOrWhiteSpace(request.DeckProfileJson) ? null : ExtractJsonObject(request.DeckProfileJson))
  //   → NormalizeSections drops null/blank sections, so a conditional section is absent (byte-identical) when empty. Mirror this for 60-interaction-audit.json.
  public static void LoadFromZip(Stream zipStream, DeckAnalysisRequest request)               // line 266 — entries.TryGetValue("60-interaction-audit.json", ...) → request.InteractionAuditJson = value ?? string.Empty
  // BuildZip call sites in controller: DeckPacketController.cs lines 230 (cached) and 251 (fresh) — pass interactionAuditJson: request.InteractionAuditJson at both.

<!-- Phase-77 render-level byte-identity guard to mirror EXACTLY for the page test (last task). -->
From DeckFlow.Web.Tests/DeckAnalysisScoreViewTests.cs:
  RenderAsync(DeckMultiAxisScore? score, string scoreJson)   // full IRazorViewEngine render scaffold (ServiceCollection + FakeFeatureFlagCache + FindView("DeckAnalysis") + StringWriter)
  NeutralizeAntiforgery(string html)                          // stabilizes the per-render __RequestVerificationToken so only the block differs
  SplitAroundScoreBlock(html) => (Prefix, Suffix, Middle)     // const head = "<h3>Analysis Summary</h3>"; const stack = "<div class=\"stack\">"
  ScoreNull_RendersNoScoreMarkup / ScoreNull_MarkupEqualsScoredMinusScoreBlock  // the two byte-identity assertions to mirror

<!-- Core model from Plan 79-01 (already merged in wave 1). -->
From DeckFlow.Core/Analysis/InteractionAudit.cs:
  public sealed record InteractionAudit(InteractionBucketResult TargetedRemoval, InteractionBucketResult BoardWipes,
      InteractionBucketResult Counterspells, InteractionBucketResult ProtectionRecursion,
      InteractionBucketResult StaxTaxation, IReadOnlyList<string> CoverageGaps);
  public sealed record InteractionBucketResult(IReadOnlyList<InteractionCard> Confident, IReadOnlyList<InteractionCard> Review);
  public readonly record struct InteractionCard(string Name, int Quantity);

<!-- From Plan 79-02 (wave 2), the local already computed in BuildAsync when the flag is on. -->
  InteractionAudit interactionAudit = InteractionAuditAggregator.Compute(...);  // reuse this local; add it to the result record
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Round-trip plumbing (Request field, ViewModel field, result record, deep deserialize hardening, controller mapping + hidden-field source write)</name>
  <read_first>
    - DeckFlow.Web/Models/DeckAnalysisRequest.cs lines 125-145 (the `_scoreJson` backing field + null-guard `ScoreJson` property — the EXACT hidden-field pattern to mirror for InteractionAuditJson)
    - DeckFlow.Web/Models/DeckAnalysisViewModel.cs lines 30-40 (the `Score { get; init; }` property — add `InteractionAudit { get; init; }` beside it)
    - DeckFlow.Web/Services/DeckAnalysisPacketService.cs lines 55-66 (DeckAnalysisPacketResult record trailing optional params — add `InteractionAudit? InteractionAudit = null`); lines 397-416 (step-3 saved-path deserialize + return); lines 810-821 (computed-path result construction — add `InteractionAudit: interactionAudit`); lines 1305-1353 (TryDeserializeScore + IsStructurallyValidScore + MaxScoreJsonLength — mirror the FULL VALIDATION DEPTH as TryDeserializeInteractionAudit + IsStructurallyValidInteractionAudit + MaxInteractionAuditJsonLength)
    - DeckFlow.Web/Controllers/DeckPacketController.cs lines 155-179 (the `if (result.Score is not null) { request.ScoreJson = JsonSerializer.Serialize(result.Score); }` hidden-field SOURCE write at 158-161 AND the `Score = result.Score,` view-model mapping at 175) + the SECOND site near line 330 — add BOTH the serialize-source write and the view-model mapping for InteractionAudit at every site that handles Score
    - CLAUDE.md carve-out: new record members are `{ get; init; }`, never `{ get; }`, never `required`
  </read_first>
  <action>
    In DeckAnalysisRequest.cs add a `_interactionAuditJson` backing field (init `string.Empty`) + a public
    `InteractionAuditJson` property with the same null-guard setter as ScoreJson, placed immediately after ScoreJson.
    In DeckAnalysisViewModel.cs add `public InteractionAudit? InteractionAudit { get; init; }` after Score (XML-doc:
    the interaction audit computed at Step 2; null when the analysis.interaction-audit flag is off). In
    DeckAnalysisPacketService.cs: add `InteractionAudit? InteractionAudit = null` as a new trailing optional param on
    the DeckAnalysisPacketResult record; in the computed path (line ~821) add `InteractionAudit: interactionAudit`
    (the local Plan 79-02 already computes — promote it, no recompute); in the step-3 saved path (line ~404-416) add
    `var savedInteractionAudit = interactionAuditFlagEnabled ? TryDeserializeInteractionAudit(request.InteractionAuditJson) : null;`
    gated on the SAME explicit-snapshot flag read used for the artifact, and set `InteractionAudit: savedInteractionAudit`.
    Add `private static InteractionAudit? TryDeserializeInteractionAudit(string? json)` and
    `private const int MaxInteractionAuditJsonLength = 16384;` mirroring TryDeserializeScore: return null on
    null/blank/oversize, `JsonSerializer.Deserialize`, then delegate to a NEW
    `private static bool IsStructurallyValidInteractionAudit(InteractionAudit? audit)`, `catch (JsonException) → null` —
    NEVER throw, never reflect/eval the client string. The validator MUST mirror IsStructurallyValidScore's DEPTH and
    reject untrusted-input degeneracies the Razor readout would otherwise render: return false unless audit is non-null
    AND all five buckets (TargetedRemoval, BoardWipes, Counterspells, ProtectionRecursion, StaxTaxation) are non-null
    AND each bucket's `Confident` AND `Review` lists are non-null AND every `InteractionCard` in every one of those ten
    lists has a non-null/non-blank `Name` and a `Quantity` in a sane range (>= 1 and <= 99) AND `CoverageGaps` is
    non-null with no null/blank entry. (Well-formed JSON can still deserialize a bucket to null, a null inner list, a
    null card Name, or a wild Quantity — each is rejected here so the round-trip yields null, threat T-79-03-01.) In
    DeckPacketController.cs at BOTH Score sites: add the hidden-field source write
    `if (result.InteractionAudit is not null) { request.InteractionAuditJson = JsonSerializer.Serialize(result.InteractionAudit); }`
    mirroring the `request.ScoreJson = ...` guard at 158-161/330, and add `InteractionAudit = result.InteractionAudit,`
    beside each `Score = result.Score,` view-model mapping. Changed-lines only; do not re-indent existing raw-string literals.
  </action>
  <verify>
    <automated>dotnet.exe build DeckFlow.Web 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - DeckAnalysisRequest.cs declares `InteractionAuditJson` with a null-guarding setter (mirrors ScoreJson)
    - DeckAnalysisViewModel.cs declares `public InteractionAudit? InteractionAudit { get; init; }` (init, not get-only, not required)
    - DeckAnalysisPacketResult record has an `InteractionAudit? InteractionAudit = null` param; both computed and step-3 paths set it
    - `TryDeserializeInteractionAudit` size-caps at `MaxInteractionAuditJsonLength`, calls `IsStructurallyValidInteractionAudit`, and catches `JsonException` returning null (grep confirms the cap + validator call + catch)
    - `IsStructurallyValidInteractionAudit` rejects: any null bucket, any null Confident/Review inner list, any null/blank card Name, any Quantity outside 1..99, and a null CoverageGaps (read confirms all five bucket checks + inner-list + card Name + Quantity-range + CoverageGaps guards)
    - DeckPacketController.cs writes `request.InteractionAuditJson = JsonSerializer.Serialize(result.InteractionAudit)` guarded by `if (result.InteractionAudit is not null)` at BOTH Score sites, and `grep -c "InteractionAudit = result.InteractionAudit" DeckFlow.Web/Controllers/DeckPacketController.cs` matches the number of `Score = result.Score` sites
    - `dotnet.exe build DeckFlow.Web` reports 0 errors, 0 warnings
  </acceptance_criteria>
  <done>The audit round-trips Request→result→ViewModel, the controller writes the hidden-field source, and untrusted-input deserialization is deeply structurally validated; Web builds clean.</done>
</task>

<task type="auto">
  <name>Task 2: On-page readout block + CONDITIONAL hidden round-trip field + site-common.css</name>
  <read_first>
    - DeckFlow.Web/Views/Deck/DeckAnalysis.cshtml lines 516-573 (the CONDITIONAL `@if (!string.IsNullOrEmpty(Model.Request.ScoreJson)) { <textarea name="ScoreJson" hidden ...> }` field at 516-521 — rendered ONLY when non-empty precisely to keep the flag-OFF page byte-identical, with the explanatory comment — and the `@if (Model.Score is not null) { <div class="chatgpt-score" ...> ... }` readout block at 539. MIRROR BOTH: a CONDITIONAL hidden InteractionAuditJson field guarded on `!string.IsNullOrEmpty(Model.Request.InteractionAuditJson)` and a flag-guarded `@if (Model.InteractionAudit is not null)` readout region under "Analysis Summary")
    - DeckFlow.Web/wwwroot/css/site-common.css (search `.chatgpt-score`, `.manabase-lens`, `.bracket-callout` — the soft-card + eyebrow + left-border-callout patterns using existing tokens `var(--line)`, `var(--panel-soft-bg)`, `var(--muted)`, `var(--accent-strong)`, `var(--warning)`; note the score used baked hex only for band pills and added NO new :root tokens)
    - CLAUDE.md theme rule: layout CSS goes in site-common.css only; token ADDITIONS would require editing every site-*.css :root — so add NO new tokens (reuse existing) to keep this to one CSS file
  </read_first>
  <action>
    In DeckAnalysis.cshtml add a CONDITIONAL hidden field mirroring the ScoreJson guard EXACTLY — wrap it in
    `@if (!string.IsNullOrEmpty(Model.Request.InteractionAuditJson)) { <textarea name="InteractionAuditJson" hidden aria-hidden="true" tabindex="-1">@Model.Request.InteractionAuditJson</textarea> }`
    placed beside the ScoreJson field, with a matching comment ("rendered only when present so the flag-OFF page stays
    byte-identical to baseline"). Do NOT emit the textarea unconditionally — an always-present field adds flag-OFF page
    bytes and breaks page byte-identity (the Task-5 excision test asserts `name="InteractionAuditJson"` is ABSENT on the
    OFF page). Then add a flag-guarded readout region `@if (Model.InteractionAudit is not null) { ... }` inside the
    Analysis Summary panel (near the Score block). Wrap the region in a single container with a STABLE outer class
    `interaction-audit` and surround it with stable, contiguous markup on BOTH sides so a render test can split cleanly
    around it (Task 5 relies on this — the guarded region must be one contiguous block whose only surrounding whitespace
    when OFF is inert). Render a titled region (role="region", aria-label "Interaction and Answers Audit") with a hedge
    eyebrow stating this is DeckFlow's automated first-pass read the AI re-verifies; then the five buckets in fixed order
    (Targeted removal, Board wipes, Counterspells, Protection or recursion, Stax or taxation), each listing its Confident
    `InteractionCard` names (Razor `@` auto-encodes card names — no manual encoding) with a quantity prefix when >1 and a
    muted "(review: ...)" sub-list for the Review tier; and a coverage-gaps callout listing
    `Model.InteractionAudit.CoverageGaps`. Use prose "approximately / verify against your list" — NEVER "the deck has N".
    Add layout classes to site-common.css only (e.g. `.interaction-audit`, `.interaction-audit__eyebrow`,
    `.interaction-audit-bucket`, `.interaction-audit-gaps`) built from EXISTING tokens (var(--line), var(--panel-soft-bg),
    var(--muted), var(--accent-strong), var(--warning)) + baked hex where a theme-invariant value is needed; add NO new
    `:root` tokens (so no per-theme site-*.css edits). Include the responsive grid breakpoints matching the score block's
    `@media` pattern. Changed-lines only.
  </action>
  <verify>
    <automated>dotnet.exe build DeckFlow.Web 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - DeckAnalysis.cshtml guards the hidden field with `@if (!string.IsNullOrEmpty(Model.Request.InteractionAuditJson))` (CONDITIONAL, mirroring ScoreJson at 516-521) — the `name="InteractionAuditJson"` textarea is NOT emitted unconditionally
    - DeckAnalysis.cshtml contains `@if (Model.InteractionAudit is not null)` guarding the readout
    - The readout lists all five bucket labels (Targeted removal, Board wipes, Counterspells, Protection or recursion, Stax or taxation) and renders `CoverageGaps`
    - The guarded readout is a single contiguous block with a stable `interaction-audit` outer class and stable contiguous markup immediately before/after it (so Task 5's excision split is deterministic)
    - The view uses Razor `@` interpolation for card names (auto-encoded); no `Html.Raw` on card names (grep confirms no Html.Raw in the block)
    - site-common.css gains `.interaction-audit` layout classes and `grep -c ":root" DeckFlow.Web/wwwroot/css/site-common.css` is unchanged from baseline (no new :root tokens); no site-*.css theme file is modified
    - `dotnet.exe build DeckFlow.Web` reports 0 errors
  </acceptance_criteria>
  <done>The Step-3 readout renders the five card-backed buckets + gaps behind the flag guard as one contiguous suppressible block, round-trips via a CONDITIONAL hidden field that is absent when OFF, and styles live only in site-common.css using existing tokens.</done>
</task>

<task type="auto">
  <name>Task 3: Persist InteractionAuditJson through the zip (60-interaction-audit.json writer + loader + controller wiring)</name>
  <read_first>
    - DeckFlow.Web/Services/Persistence/PacketArtifactStore.cs lines 28-44 (PacketAllowedNames set — add `"60-interaction-audit.json"`); lines 108-141 (BuildZip signature + promptSections/responseSections + the CONDITIONAL `40-deck-profile.json` responseSection using `string.IsNullOrWhiteSpace(...) ? null : ...` — the EXACT dropped-when-empty precedent; NormalizeSections at 805-810 filters blanks so an empty section produces NO entry); lines 266-291 (LoadFromZip entries.TryGetValue + request field assignment — mirror `request.DeckProfileJson = deckProfile ?? string.Empty` for InteractionAuditJson)
    - DeckFlow.Web/Controllers/DeckPacketController.cs lines 230-241 (cached BuildZip call) and 251-262 (fresh BuildZip call) — both pass named optional args; add `interactionAuditJson: request.InteractionAuditJson` to each
  </read_first>
  <action>
    Add `"60-interaction-audit.json"` to `PacketAllowedNames`. Add `string? interactionAuditJson = null` as a new
    trailing optional param on `BuildZip` (after `originalDeckText`). Append a CONDITIONAL responseSection
    `("60-interaction-audit.json", "INTERACTION AUDIT JSON", string.IsNullOrWhiteSpace(interactionAuditJson) ? null : ExtractJsonObject(interactionAuditJson))`
    to the `responseSections` list — because NormalizeSections drops null/blank sections, when the flag is OFF (empty
    InteractionAuditJson) NO entry is written and the zip (including all-responses.txt) is byte-identical to baseline;
    when ON, the entry is written and also folded into all-responses.txt (acceptable session state). In LoadFromZip add
    `entries.TryGetValue("60-interaction-audit.json", out var interactionAuditJson);` and
    `request.InteractionAuditJson = interactionAuditJson ?? string.Empty;` (do NOT add it to the
    "recognized-session" required-entry guard — it is optional supplemental state, like the deck-list entries). In
    DeckPacketController.cs pass `interactionAuditJson: request.InteractionAuditJson` at BOTH BuildZip call sites
    (cached ~230 and fresh ~251). Changed-lines only.
  </action>
  <verify>
    <automated>dotnet.exe build DeckFlow.Web 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "60-interaction-audit.json" DeckFlow.Web/Services/Persistence/PacketArtifactStore.cs` returns >= 3 (allowed-name entry, BuildZip section, LoadFromZip TryGetValue)
    - BuildZip gains a trailing `string? interactionAuditJson = null` param and writes the section CONDITIONALLY (`string.IsNullOrWhiteSpace(...) ? null : ...`) so an empty value produces no entry
    - LoadFromZip restores `request.InteractionAuditJson` from the `60-interaction-audit.json` entry and does NOT add it to the required-entry InvalidOperationException guard
    - Both BuildZip call sites in DeckPacketController.cs pass `interactionAuditJson: request.InteractionAuditJson`
    - `dotnet.exe build DeckFlow.Web` reports 0 errors
  </acceptance_criteria>
  <done>The audit is persisted as a conditional 60-interaction-audit.json zip entry (dropped when OFF → byte-identical) and restored to request.InteractionAuditJson on re-upload; Web builds clean.</done>
</task>

<task type="auto">
  <name>Task 4: Per-surface flag-OFF artifact/zip byte-identity (entry-map + per-platform content) + zip round-trip contract test + live UI verify</name>
  <read_first>
    - DeckFlow.Web.Tests/ResultContractTests.cs (the BuildAnalysisPrompt-per-platform contract-test harness — how a request is built + the packet prompt is produced for assertions across ChatGpt/Claude/Gemini)
    - DeckFlow.Web.Tests/AnalysisScorePromptParityTests.cs (Score_NullPath_ByteIdenticalToExcisedScorePath — the flag-OFF byte-identity proxy pattern to mirror)
    - DeckFlow.Web/Services/Persistence/PacketArtifactStore.cs (BuildZip → ReadEntries round-trip: use `PacketArtifactStore.BuildZip(...)` to produce bytes, then open the zip with `System.IO.Compression.ZipArchive` to read the entry-name set + entry text for the byte-level comparison; LoadFromZip for the round-trip restore)
    - DeckFlow.Web/Services/DeckAnalysisPacketService.cs (BuildAsync flag-OFF path — with the flag off, interactionAudit/InteractionAudit is null, request.InteractionAuditJson stays empty, and the artifact block is null; the test asserts this)
    - scripts/run-web-test.sh + CLAUDE.md testing note (start the headless web app with DECKFLOW_DISABLE_AUTO_BROWSER=true; drive live checks with `npx --no-install playwright test`; never open a Windows-host browser; CI is the authoritative gate)
  </read_first>
  <action>
    Create DeckFlow.Web.Tests/InteractionAuditSurfaceContractTests.cs proving the flag-OFF ARTIFACT/ZIP byte-identity
    contract (actually building the zip and comparing, per HIGH-2 — not merely asserting a null result) and the zip
    round-trip:
    (1) PROMPT byte-identity, ALL THREE platforms: a `[Theory]` over TargetAiPlatform ChatGpt/Claude/Gemini asserting
    that with the interaction-audit flag OFF, `BuildAsync` yields `result.InteractionAudit is null` AND the analysis
    prompt text `Assert.DoesNotContain` the interaction block header/sentinel — i.e. the per-platform artifact equals the
    pre-feature (null-block) path.
    (2) ZIP entry-map byte-identity: with the flag OFF (empty `request.InteractionAuditJson`), call
    `PacketArtifactStore.BuildZip(...)`, open the resulting bytes as a ZipArchive, and assert the entry-name set does
    NOT contain `"60-interaction-audit.json"` and that the `31-analysis-prompt.txt` and `all-responses.txt` entry
    contents contain no interaction sentinel (the OFF zip == pre-feature zip entry map + content). Then with a non-empty
    valid `request.InteractionAuditJson` (flag-ON proxy) assert the built zip DOES contain a `"60-interaction-audit.json"`
    entry whose text round-trips (see (3)).
    (3) ZIP download→upload round-trip fidelity: build a zip with a valid serialized `InteractionAuditJson`, then
    `PacketArtifactStore.LoadFromZip(zipStream, freshRequest)` and assert `freshRequest.InteractionAuditJson` equals the
    original serialized JSON AND `TryDeserializeInteractionAudit` (exercised via the Step-3 saved path or a direct
    contract seam) re-materializes a non-null `InteractionAudit` — proving the audit survives the zip round-trip
    (ROADMAP criterion #4 "round-trip restores").
    (4) hardening: oversize (`> MaxInteractionAuditJsonLength`), malformed JSON, a bucket deserialized to null, a null
    inner Confident/Review list, a null card Name, and an out-of-range Quantity each deserialize to null and never throw.
    Use the existing test seams (flag-cache fake / snapshot) the score/deck-stats tests use — mirror their setup. Add a
    Playwright spec (or extend the existing e2e deck-analysis smoke) that, with the flag ON, asserts the Step-3 readout
    region is visible at desktop (1280) and mobile (390) widths and, with the flag OFF, that the region is absent — run
    it against the headless server started via scripts/run-web-test.sh. NOTE: this Playwright selector-visibility check
    is a live-UX smoke, NOT the page byte-identity proof — the page byte-identity gate (ROADMAP criterion #4) is
    satisfied by Task 5's Razor render excision-equality test, not by a selector being visible/absent (the ts
    placeholder file is only a stub anchor if no e2e harness edit is needed; prefer extending the existing Playwright
    deck-analysis spec and drop the stub). Changed-lines only.
  </action>
  <verify>
    <automated>dotnet.exe test DeckFlow.Web.Tests --filter "FullyQualifiedName~InteractionAuditSurfaceContract" 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - A `[Theory]` over ChatGpt/Claude/Gemini asserts flag-OFF `BuildAsync` result `InteractionAudit is null` and the per-platform artifact prompt has no interaction-block header (per-platform content byte-identity)
    - A `[Fact]` BUILDS the zip with the flag OFF, opens it, and asserts the entry-name set excludes `60-interaction-audit.json` and that `31-analysis-prompt.txt` + `all-responses.txt` contents carry no interaction sentinel (zip entry-map + content byte-identity); a companion assertion builds the ON zip and finds the `60-interaction-audit.json` entry
    - A `[Fact]` builds a zip with valid `InteractionAuditJson`, calls `LoadFromZip`, and asserts `request.InteractionAuditJson` is restored and re-materializes a non-null `InteractionAudit` (download→upload round-trip fidelity)
    - A `[Fact]`/`[Theory]` asserts oversize, malformed, null-bucket, null-inner-list, null-card-Name, and out-of-range-Quantity `InteractionAuditJson` each yield null and do not throw
    - The Playwright deck-analysis spec asserts the readout region visible (flag ON) at 1280 + 390 widths and absent (flag OFF) — a live-UX smoke, explicitly NOT the byte-identity proof
    - `dotnet.exe test DeckFlow.Web.Tests --filter "FullyQualifiedName~InteractionAuditSurfaceContract"` exits 0
    - CI (GitHub Actions) green after push — authoritative gate
  </acceptance_criteria>
  <done>The flag-OFF artifact + zip byte-identity is proven by an actual zip build + entry-map/per-platform content comparison, the download→upload zip round-trip restores the audit, and the deep untrusted-input hardening is proven; live UI smoke verified at desktop + mobile.</done>
</task>

<task type="auto">
  <name>Task 5: Page-level flag-OFF byte-identity Razor render test (Phase-77 excision-equality)</name>
  <read_first>
    - DeckFlow.Web.Tests/DeckAnalysisScoreViewTests.cs (READ IN FULL — the EXACT precedent to mirror: an IRazorViewEngine render harness with `RenderAsync`, `BuildModel`, `NeutralizeAntiforgery`, `SplitAroundScoreBlock`, `CreateHostingEnvironment`, `TestWebHostEnvironment`, `StubTempDataProvider`, `FakeFeatureFlagCache`, and the two byte-identity `[Fact]`s `ScoreNull_RendersNoScoreMarkup` + `ScoreNull_MarkupEqualsScoredMinusScoreBlock`. Copy the whole ServiceCollection → FindView("DeckAnalysis") → StringWriter → RenderAsync scaffold and the helper types VERBATIM; only swap the model-shaping bits Score→InteractionAudit and the split anchors)
    - DeckFlow.Web/Views/Deck/DeckAnalysis.cshtml (the flag-guarded `@if (Model.InteractionAudit is not null)` readout region + CONDITIONAL InteractionAuditJson field added in Task 2 — identify the STABLE literal markup immediately BEFORE and immediately AFTER that region to use as the two split anchors, mirroring how SplitAroundScoreBlock uses `"<h3>Analysis Summary</h3>"` and `"<div class=\"stack\">"`; the outer `interaction-audit` class is a reliable middle sentinel)
    - DeckFlow.Web/Models/DeckAnalysisViewModel.cs + DeckAnalysisRequest.cs (the InteractionAudit + InteractionAuditJson members added in Task 1 — set both on the test's model/request so the hidden field lives in the shared prefix)
  </read_first>
  <action>
    Create DeckFlow.Web.Tests/DeckAnalysisInteractionAuditViewTests.cs as a dedicated `IRazorViewEngine` render guard
    mirroring DeckAnalysisScoreViewTests.cs EXACTLY. Copy the `RenderAsync` ServiceCollection/FindView/StringWriter
    scaffold, the `CreateHostingEnvironment`/`TestWebHostEnvironment`/`StubTempDataProvider` helpers, and the
    `NeutralizeAntiforgery` helper verbatim (only rename the model-shaping bits so `BuildModel` sets `InteractionAudit`
    and `Request.InteractionAuditJson` instead of Score/ScoreJson). Provide:
    (1) `InteractionAuditNull_RendersNoInteractionMarkup`: render DeckAnalysis.cshtml with `InteractionAudit: null` and
    `InteractionAuditJson: string.Empty` (the true flag-OFF baseline) and BOTH
    `Assert.DoesNotContain("interaction-audit", html, StringComparison.Ordinal)` AND
    `Assert.DoesNotContain("name=\"InteractionAuditJson\"", html, StringComparison.Ordinal)` (the CONDITIONAL hidden
    field is absent on the OFF page — HIGH-3).
    (2) `InteractionAuditNull_MarkupEqualsPopulatedMinusInteractionBlock`: render TWICE with an IDENTICAL Request (same
    NON-EMPTY `InteractionAuditJson` hidden field so the field sits in the shared prefix for both, same AnalysisResponse) —
    once `InteractionAudit: null`, once `InteractionAudit: BuildAudit()`; `NeutralizeAntiforgery` both; then
    `SplitAroundInteractionBlock` each around the two stable anchors bracketing the readout region;
    `Assert.Equal(nullPrefix, populatedPrefix)` (no upstream drift — incl. the hidden InteractionAuditJson field),
    `Assert.Equal(nullSuffix, populatedSuffix)` (no downstream drift), `Assert.True(string.IsNullOrWhiteSpace(nullMiddle))`
    (OFF excision is whitespace-only), and `Assert.Contains("interaction-audit-bucket", populatedMiddle, StringComparison.Ordinal)`
    (the populated middle carries the block). Add a `SplitAroundInteractionBlock` mirroring `SplitAroundScoreBlock` using
    the two stable anchors, a `BuildAudit()` returning a populated `InteractionAudit` (five `InteractionBucketResult`
    buckets each with >=1 Confident `InteractionCard` + a non-empty `CoverageGaps` entry), and a
    `FixedInteractionAuditJson` round-trip constant serialized from that audit. Optionally add
    `InteractionAuditPresent_RendersAllFiveBuckets` asserting each bucket label + a card name appears. This Razor render
    test — NOT Task 4's Playwright selector-visibility check — is what satisfies ROADMAP success criterion #4's page
    byte-identity gate. Deterministic, CI-safe, no browser. Changed-lines only; LF endings preserved.
  </action>
  <verify>
    <automated>dotnet.exe test DeckFlow.Web.Tests --filter "FullyQualifiedName~DeckAnalysisInteractionAuditView" 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - DeckFlow.Web.Tests/DeckAnalysisInteractionAuditViewTests.cs exists and renders DeckAnalysis.cshtml through `IRazorViewEngine` (mirrors DeckAnalysisScoreViewTests.cs; no browser, no Playwright)
    - `InteractionAuditNull_RendersNoInteractionMarkup` asserts `Assert.DoesNotContain("interaction-audit", html, StringComparison.Ordinal)` AND `Assert.DoesNotContain("name=\"InteractionAuditJson\"", html, ...)` on the flag-OFF render (readout region AND conditional hidden field both absent)
    - `InteractionAuditNull_MarkupEqualsPopulatedMinusInteractionBlock` performs the excision-equality assertion: `Assert.Equal` on prefix AND on suffix between the null and populated renders, `Assert.True(string.IsNullOrWhiteSpace(nullMiddle))`, and a populated-middle sentinel `Assert.Contains` — proving the rendered page is byte-identical outside the contiguous suppressible block
    - A `SplitAroundInteractionBlock` helper mirrors `SplitAroundScoreBlock` (two stable view anchors); `NeutralizeAntiforgery` reused so the CSRF nonce is not a false diff
    - `dotnet.exe test DeckFlow.Web.Tests --filter "FullyQualifiedName~DeckAnalysisInteractionAuditView"` exits 0
    - CI (GitHub Actions) green after push — authoritative gate
  </acceptance_criteria>
  <done>The rendered /deck-analysis page is proven byte-identical to the pre-feature baseline when the flag is OFF (readout region AND conditional hidden field absent) via a deterministic Razor IRazorViewEngine excision-equality test mirroring Phase 77's DeckAnalysisScoreViewTests — the page-layer half of the flag-OFF byte-identity gate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| client → InteractionAuditJson hidden field | Round-tripped serialized audit is UNTRUSTED client input re-POSTed on Step-3 and into the download endpoint |
| client → 60-interaction-audit.json zip entry | Re-uploaded zip entry is UNTRUSTED; restored into request.InteractionAuditJson then re-validated by TryDeserializeInteractionAudit |
| feature-flag store → controller/view | The flag gates whether the readout region + conditional hidden field + zip entry render/emit |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-79-03-01 | Tampering | InteractionAuditJson deserialization (hidden field + zip entry) | mitigate | `TryDeserializeInteractionAudit` size-caps at MaxInteractionAuditJsonLength, then `IsStructurallyValidInteractionAudit` validates every bucket, every Confident/Review inner list, every card Name (non-blank), every Quantity (1..99), and the CoverageGaps list; catches JsonException → null; never throws, never reflects/evals (mirrors + deepens T-77-04-01) |
| T-79-03-02 | Injection (XSS) | card names + gap strings rendered into HTML readout | mitigate | Razor `@` auto-encodes all interpolated card names / gap advisories; no `Html.Raw` on audit data (acceptance criterion asserts absence) |
| T-79-03-03 | Denial of Service | oversize round-tripped payload (field or zip entry) | mitigate | 16 KB cap rejects oversize InteractionAuditJson before parse; the zip loader inherits ReadEntries' 2 MB/entry + 10 MB total caps; audit is not recomputed on the round-trip path |
| T-79-03-04 | Tampering | flag-OFF byte-identity regression (page + artifact + zip) | mitigate | Three-layer proof: Task 4 InteractionAuditSurfaceContractTests BUILDS the zip and asserts the entry-map excludes 60-interaction-audit.json + per-platform prompt content carries no block when OFF; Task 5 DeckAnalysisInteractionAuditViewTests renders the real view and asserts prefix/suffix excision-equality (readout AND conditional hidden field absent when OFF); the conditional hidden-field guard keeps the OFF page free of extra bytes |
| T-79-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages (REQUIREMENTS.md zero-new-deps) |
</threat_model>

<verification>
- `dotnet.exe build DeckFlow.Web` clean (0/0)
- InteractionAuditSurfaceContract test filter GREEN (artifact/zip entry-map + per-platform byte-identity + zip download→upload round-trip + deep hardening)
- DeckAnalysisInteractionAuditView test filter GREEN (page render excision-equality — the page byte-identity gate; conditional hidden field absent when OFF)
- Playwright deck-analysis spec GREEN at 1280 + 390 (live-UX smoke)
- CI (GitHub Actions) green after push — authoritative gate (WSL VSTest unreliable)
- Changed-lines format gate clean; no new :root tokens; no theme site-*.css modified; LF endings preserved
- Manual confirmation: flag OFF ⇒ readout region + hidden field absent + zip has no 60-interaction-audit.json + prompt files byte-identical to baseline; flag ON ⇒ readout matches the paste artifact content and the zip round-trips
</verification>

<success_criteria>
The Step-3 on-page readout mirrors the paste-artifact interaction buckets + gaps (hedged), round-trips via a
hardened untrusted `InteractionAuditJson` hidden field (CONDITIONAL — absent when OFF) AND a conditional
`60-interaction-audit.json` zip entry that LoadFromZip restores, and with the flag OFF the page + three
artifacts + zip are byte-identical to baseline — proven at the artifact/zip layer by an entry-map + per-platform
content contract test (with a download→upload round-trip fidelity assertion) AND at the page layer by a Razor
IRazorViewEngine render excision-equality test (Phase-77 pattern), with a desktop+mobile Playwright live-UX smoke;
new CSS lives only in site-common.css with no new theme tokens.
</success_criteria>

<output>
Create `.planning/phases/79-interaction-answers-audit/79-03-SUMMARY.md` when done
</output>
</output>
</content>
</invoke>
