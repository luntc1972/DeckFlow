---
phase: 103-simulation-engine-guided-cut-rounds
plan: 09
type: execute
wave: 6
depends_on: ["103-07", "103-08"]
files_modified:
  - DeckFlow.Web/wwwroot/ts/cut-lab.ts
  - DeckFlow.Web/wwwroot/css/site-common.css
  - DeckFlow.Web/ts-tests/cut-lab-proposal.test.ts
autonomous: true
requirements: [CUT-03, SIM-01]
must_haves:
  truths:
    - "HIGH-5: the TS intercepts the decision/restore FORM submit (preventDefault) — it does NOT replace a plain click. When JS is disabled the same form posts to /cut-lab/decide (CutLabController.Decide, Plan 103-07); when JS is present the submit is intercepted and enhanced with fetch. Progressive enhancement over a working no-JS form."
    - "D-09: intercepting a decision form submit POSTs JSON to /api/cut-lab/decide and patches the proposal card + sticky bar + delta values + cuts-made list in place — no page reload; the hidden CutLabStateJson field(s) are updated so the no-JS form fallback stays in sync."
    - "D-11: an inline 16px spinner (not the full-page overlay) shows during the request; buttons disable + aria-busy; at the 3s hard cap buttons re-enable with the 'taking longer' copy."
    - "D-03: when the endpoint returns the next round's banner, the sticky bar's round field and the banner swap in the same tick — no dead-end gap."
    - "D-14: the sticky bar text nodes patch in place after every successful decision (round · N to cut · N cut so far)."
    - "D-06: directional colors come only from the token classes on the numeric span (styled in site-common.css); layout CSS lives in site-common.css, zero :root token additions."
    - "Compiled wwwroot/js/cut-lab.js is never staged (gitignored)."
  artifacts:
    - path: "DeckFlow.Web/wwwroot/ts/cut-lab.ts"
      provides: "decision-form submit interception + fetch + in-place DOM patch + sticky-bar update + inline spinner"
      contains: "/api/cut-lab/decide"
    - path: "DeckFlow.Web/wwwroot/css/site-common.css"
      provides: "sticky bar, proposal card, delta tokens, cuts-made, compare panel layout"
      contains: "cutlab-sticky-bar"
  key_links:
    - from: "DeckFlow.Web/wwwroot/ts/cut-lab.ts"
      to: "POST /api/cut-lab/decide"
      via: "form submit preventDefault → fetch JSON + renderDecisionResponse patch (deck-sync.ts submitDeckSyncApi pattern)"
      pattern: "preventDefault"
---

<objective>
Hydrate the Cut rounds workspace as PROGRESSIVE ENHANCEMENT over the no-JS forms built in Plan 103-08: intercept the decision/restore form `submit` (preventDefault, HIGH-5), POST JSON to `/api/cut-lab/decide`, patch the proposal card + sticky bar + deltas + cuts-made list in place (D-09), show the inline busy state (D-11), and add all new layout CSS to `site-common.css` with the directional-token classes (D-06). When JS is off, the same forms post to `/cut-lab/decide` and the page re-renders server-side — this layer only enhances that.

Purpose: Delivers the interactive CUT-03 loop feel within the ~1s/3s budget without breaking the no-JS fallback.
Output: Extended `cut-lab.ts`, new `site-common.css` rules, Vitest coverage of the interception + patch logic.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/cut-lab/phases/103-simulation-engine-guided-cut-rounds/103-UI-SPEC.md
@.planning/workstreams/cut-lab/phases/103-simulation-engine-guided-cut-rounds/103-PATTERNS.md

<interfaces>
Fetch/patch precedent — DeckFlow.Web/wwwroot/ts/deck-sync.ts submitDeckSyncApi (~lines 1128-1179): fetch POST JSON, response.ok guard reading { message } / { Message }, then a renderXxxResponse(...) DOM patch; window.hideBusyIndicator?.(). Busy indicator: DeckFlow.Web/wwwroot/ts/busy-indicator.ts.
Existing cut-lab.ts (~850 lines): current lock/package DOM-patch handlers + hidden CutLabStateJson re-serialization — the new handler MUST keep that hidden field in sync.
No-JS markup (Plan 103-08): each decision/restore button is a submit inside `<form method="post" action="/cut-lab/decide">` with `@Html.AntiForgeryToken()` + hidden CutLabStateJson + CardName + Decision, plus data-cut-lab-decision / data-cut-lab-restore / data-cut-lab-card hooks. The TS listens for the FORM submit, not a button click.
JSON endpoint contract (Plan 103-07): POST /api/cut-lab/decide with { cutLabStateJson, cardName, decision } returns { cutLabStateJson, nextProposal, deltas, floorWarnings, cardsRemaining, cutsMade }.
DOM hooks (Plan 103-08): data-cut-lab-sticky-round/-remaining/-accepted, .cutlab-proposal, .cutlab-round-banner, .cutlab-delta, .cutlab-cuts-made, .cutlab-decision-btn--accept/--reject/--defer.
CSS precedent — site-common.css .cutlab-* block (~line 4146+); .prompt-sticky-download sticky precedent (~1920-1948); @keyframes busy-spin in site.css:1121. Tokens --danger/--success confirmed present in every theme (UI-SPEC Color section).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Intercept decision-form submit + fetch + in-place DOM patch + inline busy state (+ Vitest)</name>
  <read_first>
    - DeckFlow.Web/wwwroot/ts/cut-lab.ts (existing lock/package handlers + hidden CutLabStateJson re-serialization pattern)
    - DeckFlow.Web/wwwroot/ts/deck-sync.ts (submitDeckSyncApi fetch+JSON+error-payload+patch shape to copy)
    - DeckFlow.Web/wwwroot/ts/busy-indicator.ts (show/hide API + why the full-page overlay is reserved for import)
    - DeckFlow.Web/ts-tests/cut-lab-lock-interactions.test.ts (Vitest DOM-fixture + dispatch-event + hidden-field-assertion conventions)
    - .planning/workstreams/cut-lab/phases/103-simulation-engine-guided-cut-rounds/103-UI-SPEC.md (Component Contract 2 + 5: inline spinner, 1.5s soft / 3s hard cap, error copy, success patch behavior)
  </read_first>
  <behavior>
    - Test 1 (HIGH-5 interception): dispatching a `submit` event on a decision form (with a mocked OK fetch) calls preventDefault, posts to /api/cut-lab/decide with the card + decision + current state, and writes the returned cutLabStateJson into the hidden field(s) — the browser does NOT navigate.
    - Test 2: on OK response, the sticky-bar remaining/accepted/round text nodes and the round banner are updated from the response.
    - Test 3: on a non-OK response, buttons re-enable and the neutral error line renders (no state mutation); the no-JS form remains intact for retry.
    - Test 4: submitting a restore form posts decision=restore and removes the row / updates counts on OK.
  </behavior>
  <action>
    In `cut-lab.ts`, add a delegated `submit` handler (on the document or the Cut rounds container) for the decision forms (`form:has([data-cut-lab-decision])` / `[data-cut-lab-restore]`, or match by the form's hidden Decision input) that: (1) calls `event.preventDefault()` (HIGH-5 — the no-JS form still works when this handler is absent); (2) reads card + decision + current hidden `CutLabStateJson` from the submitted form; (3) disables the decision buttons, sets `aria-busy="true"` on `.cutlab-proposal`, swaps the submitted button label for a 16px `.cutlab-busy-spinner` + "Recalculating…"; (4) fetches POST `/api/cut-lab/decide` with JSON body `{ cutLabStateJson, cardName, decision }` and `Content-Type: application/json`; (5) on `response.ok`: parse, write the returned `cutLabStateJson` back into EVERY hidden CutLabStateJson field (all decision/restore forms share the state — keeps the no-JS fallback synced), patch the proposal card (heading, evidence chips, changed-only delta lines + full-metric expander, floor warning), swap the round banner + the three `data-cut-lab-sticky-*` text nodes (D-03/D-14), rebuild the `.cutlab-cuts-made` list (append on accept, remove-row on restore), and render the at-100/empty terminal state when `nextProposal` is terminal; (6) on `!response.ok`: read `{ message }/{ Message }`, re-enable buttons, render the neutral error line; (7) enforce a 3s hard-cap timeout via `AbortController` → re-enable + "This is taking longer than expected. Try again in a moment." copy. Mirror `submitDeckSyncApi`'s error-payload handling. Keep each numeric delta value in its own span carrying `--up`/`--down`/`--none` (D-06). Add `DeckFlow.Web/ts-tests/cut-lab-proposal.test.ts` covering the four behaviors (mock `fetch`, DOM fixture with real `<form>` elements per the lock-interactions test). Never stage compiled `wwwroot/js/cut-lab.js`.
  </action>
  <verify>
    <automated>cd DeckFlow.Web && npx tsc --noEmit -p tsconfig.json && npm test -- --run cut-lab-proposal</automated>
  </verify>
  <acceptance_criteria>
    - `cut-lab.ts` intercepts the form `submit` with `preventDefault` (grep `preventDefault`) and posts to `/api/cut-lab/decide` (grep), updating every hidden `CutLabStateJson` from the response.
    - Sticky-bar `data-cut-lab-sticky-*` nodes + round banner are patched in place after a successful decision.
    - An `AbortController` enforces the 3s cap (grep `AbortController`).
    - `npx tsc --noEmit` passes (strict); the new Vitest spec is green.
    - No `wwwroot/js/*.js` staged in git.
  </acceptance_criteria>
  <done>The decision loop runs async by ENHANCING the no-JS form (submit interception), with in-place patching, inline busy state, and no-JS-field sync, with Vitest coverage.</done>
</task>

<task type="auto">
  <name>Task 2: Layout CSS for sticky bar, proposal card, delta tokens, cuts-made, compare panel</name>
  <read_first>
    - DeckFlow.Web/wwwroot/css/site-common.css (existing .cutlab-* block ~4146+; .prompt-sticky-download sticky precedent ~1920-1948)
    - DeckFlow.Web/wwwroot/css/site.css (@keyframes busy-spin ~1121 — reuse at 16px; do NOT add layout here)
    - .planning/workstreams/cut-lab/phases/103-simulation-engine-guided-cut-rounds/103-UI-SPEC.md (Component Contracts 1-5: exact class specs, 44px, sticky 44px max-height, directional token colors, spacing scale)
  </read_first>
  <action>
    Append to `site-common.css` (immediately after the existing `.cutlab-*` block) layout rules for: `.cutlab-sticky-bar` (position sticky, top 0, z-index 20, min-height and max-height 44px, flex row, gap 0.75rem, padding 0 1rem, background var(--panel), 1px var(--line) border, radius 8px, box-shadow 0 2px 6px rgba(0,0,0,0.18)) plus its three field spans; `.cutlab-round-banner`; `.cutlab-proposal` (var(--panel)/var(--line)/radius 10px/padding 1.5rem) plus `__heading`/`__evidence`/`__floor-warning`; `.cutlab-delta` and `.cutlab-delta__line`; the directional token classes `.cutlab-delta__value--up` = color var(--success), `--down` = color var(--danger), `--none` = color var(--muted) (D-06 — numeric token only); the decision-form/button rules `.cutlab-proposal__actions` (flex row of forms), `.cutlab-decision-btn--accept` (accent fill), `--reject` and `--defer` (neutral bordered), all min-height 44px, and ensure the wrapping `<form>` does not add layout gaps (display contents or inline-flex); `.cutlab-busy-spinner` (16px, reuse @keyframes busy-spin); `.cutlab-cuts-made` + `__row` + `.cutlab-restore-btn` (44px); `.cutlab-compare` reusing the responsive `table[data-prompt-cedh-reference-table]` idiom. Use ONLY existing theme tokens — add ZERO `:root` tokens. Layout stays in site-common.css, never site.css. LF endings; append only, do not reflow the existing block.
  </action>
  <verify>
    <automated>cd DeckFlow.Web && grep -q "cutlab-sticky-bar" wwwroot/css/site-common.css && grep -q "cutlab-delta__value--down" wwwroot/css/site-common.css && grep -q "cutlab-compare" wwwroot/css/site-common.css && echo CSS_OK</automated>
  </verify>
  <acceptance_criteria>
    - `site-common.css` contains `.cutlab-sticky-bar`, `.cutlab-proposal`, `.cutlab-delta__value--down/--up/--none`, `.cutlab-cuts-made`, `.cutlab-compare`, `.cutlab-busy-spinner` rules (grep).
    - Sticky bar has `max-height: 44px` (grep). The decision-form wrappers do not break the 44px button row layout.
    - Directional colors reference `var(--success)`/`var(--danger)`/`var(--muted)` only on the value token classes.
    - No new `:root { }` block added anywhere in site-common.css for this phase; site.css untouched (git diff).
  </acceptance_criteria>
  <done>All new Cut-rounds/Compare/Cuts-made layout is themed and token-driven in site-common.css with 44px targets, decision-form wrappers, and directional delta tokens.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| endpoint JSON → DOM | Response fields (card names, banners) are injected into the DOM; must use textContent, not innerHTML, to avoid DOM-based XSS. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-103-09-01 | Tampering (DOM XSS) | Patching response card names/labels into the proposal card | mitigate | Patch via `textContent`/`createElement`, never `innerHTML` with response strings; class names are code-controlled constants. |
| T-103-09-02 | Denial of Service | Hung request blocking the UI | mitigate | `AbortController` 3s hard cap re-enables the buttons; inline spinner never blocks the whole page. |
| T-103-09-03 | Spoofing | Direct POST bypassing the UI | mitigate | Server-side SameOriginRequestValidator + feature gate (JSON, Plan 103-07) and antiforgery + feature gate (form, Plan 103-07) are the authoritative guards; the TS layer is convenience only. |
</threat_model>

<verification>
- `cd DeckFlow.Web && npx tsc --noEmit -p tsconfig.json` clean.
- `npm test` (Vitest) green including the new cut-lab-proposal spec.
- CSS greps pass; site.css untouched.
</verification>

<success_criteria>
Async decision/restore loop via form-submit interception (progressive enhancement over the no-JS fallback) with in-place patching, inline busy state, and no-JS-field sync; all new layout themed in site-common.css with directional delta tokens and 44px targets.
</success_criteria>

<output>
Create `.planning/workstreams/cut-lab/phases/103-simulation-engine-guided-cut-rounds/103-09-SUMMARY.md` when done.
</output>
</content>
