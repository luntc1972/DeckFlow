---
phase: 999.3-packet-download-session-cache
plan: 04
type: execute
wave: 4
depends_on:
  - 999.3-01
  - 999.3-02
  - 999.3-03
files_modified:
  - README.md
  - .planning/STATE.md
  - .planning/ROADMAP.md
autonomous: false
requirements:
  - D-14
tags:
  - documentation
  - uat
  - verification

must_haves:
  truths:
    - "D-14: manual UAT + `dotnet build` clean + grep gate is the binding test gate (matches Phase 999.1 / 999.2 family pattern; no automated xUnit per CLAUDE.md WSL/VSTest constraint). This plan IS the D-14 execution + closure artifact."
    - "Manual UAT executed against a large Commander deck previously used in Phase 999.2 UAT (obs 237 + 240 on 2026-05-20). Preview → download click measured at <2s on cache hit vs >2 minutes on cache miss / pre-phase baseline."
    - "Cache hit-ratio observability confirmed: at least one `Packet cache hit` log line appears in `/data/logs/web-*.log` (local: `logs/web-*.log` under content root) after a preview → download flow with identical inputs."
    - "Cache miss fall-through observability confirmed: a cold-cache download emits a `Packet cache miss` log line followed by successful BuildAsync execution and a valid zip response."
    - "**M3 fix — D-09 upload-write isolation:** UAT proves `/deck-analysis/upload` is the WRITER of a cache entry (not preview) via this sequence: (a) kill dev server process, (b) restart, (c) upload zip FIRST (cold cache → miss → write), (d) IMMEDIATELY click Download (warm cache → hit). Serilog log must show `Packet cache write` line BEFORE the `Packet cache hit` line with matching 8-char key prefix. This isolates upload as the writer; preview is not exercised in this sequence."
    - "**M4 fix — D-10 precedence (response-json-only fast-path) UAT:** Replace iter-1's brittle UI field surgery with a reproducible POST recipe. UAT artifacts include `999.3-uat-d10-precedence-comparison.curl` and `999.3-uat-d10-precedence-metagap.curl` — verbatim `curl --data-binary @...` recipes captured from real browser devtools 'copy as cURL' on a deck-comparison or cedh-meta-gap download POST, then edited to clear DeckSource fields. UAT step replays via `curl`; Serilog log must show NO `Packet cache lookup` line for that request (cache layered BENEATH the short-circuit per D-10)."
    - "Grep gate on all 3 services + cache wrapper + controller: exact-count assertions pass (see verification block)."
    - "`dotnet.exe build DeckFlow.sln -c Release` exits 0 with `CURRENT -le BASELINE` (per `_baseline-warnings.txt`)."
    - "README.md updated with a brief note that packet download endpoints reuse the preview's BuildAsync result via an in-memory session cache (5-min TTL, 10MB cap)."
    - "STATE.md updated to reflect Phase 999.3 closure: `last_activity` mentions the closure; counter fields (`completed_phases`, `completed_plans`) are INCREMENTED by reading current values and adding phase deltas (NOT by writing absolute values); `Current Position` block points to the next phase."
  artifacts:
    - path: ".planning/phases/999.3-packet-download-session-cache/999.3-UAT.md"
      provides: "UAT execution log with timings, log-line excerpts, and pass/fail attestation"
      min_lines: 40
    - path: ".planning/phases/999.3-packet-download-session-cache/999.3-uat-d10-precedence-comparison.curl"
      provides: "Reproducible POST recipe for deck-comparison response-json-only fast path (M4 fix)"
      min_lines: 5
    - path: ".planning/phases/999.3-packet-download-session-cache/999.3-uat-d10-precedence-metagap.curl"
      provides: "Reproducible POST recipe for cedh-meta-gap response-json-only fast path (M4 fix)"
      min_lines: 5
    - path: "README.md"
      provides: "User-visible note that packet downloads are now cached"
      contains: "session cache"
    - path: ".planning/STATE.md"
      provides: "Phase 999.3 closure markers"
      contains: "999.3"
  key_links:
    - from: ".planning/STATE.md"
      to: ".planning/ROADMAP.md (Phase 999.3 entry)"
      via: "status flip to Complete + completion date"
      pattern: "999.3.*Complete"
---

<objective>
Close Phase 999.3 via manual UAT, grep gate, build gate, README update, and STATE update. This plan has ONE human-verify checkpoint (the UAT) and three autonomous tasks bracketing it.

Purpose: D-14 mandates "manual UAT + dotnet build clean + grep gate". This plan executes that gate, documents the result, and updates the project artifacts.

**Codex review fixes addressed in Plan 04:**
- **M3:** D-09 upload UAT is no longer brittle. Sequence forces the upload to be the WRITER (process restart → upload zip → immediate download) so the cache write/hit sequence cannot be confused with a residual preview entry.
- **M4:** D-10 precedence UAT replaces UI field surgery with reproducible POST recipes captured via `curl`. UAT artifacts (`*.curl` files) live next to the UAT document.

Output: One UAT document, two `.curl` recipe files (M4), three modified files (`README.md`, `STATE.md`, `ROADMAP.md`), one commit closing the phase.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/999.3-packet-download-session-cache/999.3-CONTEXT.md
@.planning/phases/999.3-packet-download-session-cache/999.3-01-SUMMARY.md
@.planning/phases/999.3-packet-download-session-cache/999.3-02-SUMMARY.md
@.planning/phases/999.3-packet-download-session-cache/999.3-03-SUMMARY.md
@.planning/phases/999.3-packet-download-session-cache/999.3-REVIEWS.md
@CLAUDE.md

<interfaces>
<!-- UAT environment expectations and grep-gate command set. -->

UAT environment:
  - User starts dev server manually (global memory: "User starts dev server — never auto-launch DeckFlow web; ask user to start"). Executor MUST NOT auto-launch.
  - Browser at `http://localhost:5173` (per launchSettings.json).
  - Log location: `<ContentRoot>/logs/web-{date}.log`.
  - UAT deck: same large Commander deck from Phase 999.2 UAT on 2026-05-20 (observations 237 + 240). Executor asks user for the deck URL or source at UAT time.

Grep-gate baseline (D-14):
  - `grep -c "_packetCache.Set" DeckFlow.Web/Services/DeckAnalysisPacketService.cs` → 1
  - `grep -c "_packetCache.Set" DeckFlow.Web/Services/DeckComparisonService.cs` → 1
  - `grep -c "_packetCache.Set" DeckFlow.Web/Services/MetaGapService.cs` → 1
  - `grep -c "_packetCache.TryGet" DeckFlow.Web/Controllers/DeckController.cs` → 3
  - `grep -cE "public async Task<string\?> TryComputeCacheKeyAsync" DeckFlow.Web/Services/DeckAnalysisPacketService.cs` → 1
  - `grep -cE "public async Task<string\?> TryComputeCacheKeyAsync" DeckFlow.Web/Services/DeckComparisonService.cs` → 1
  - `grep -cE "public async Task<string\?> TryComputeCacheKeyAsync" DeckFlow.Web/Services/MetaGapService.cs` → 1
  - `test -f DeckFlow.Web/Services/PacketSessionCache.cs` → exit 0
  - `grep -c "AddSingleton<PacketSessionCache>" DeckFlow.Web/Program.cs` → 1
  - `grep -c "sp.GetRequiredService<PacketSessionCache>" DeckFlow.Web/Program.cs` → 3
  - Negative gates (Codex H1+H2+H4 regression prevention):
    - `! grep -q "public static string ComputeCacheKey" DeckFlow.Web/Services/*.cs` (iter-1 static signature deleted)
    - `! grep -q "ResolveCanonicalTextAsync" DeckFlow.Web/Controllers/DeckController.cs` (method does not exist)
    - `! grep -q "request\.CommanderName" DeckFlow.Web/Controllers/DeckController.cs`
    - `! grep -q "IDeckEntryLoader" DeckFlow.Web/Controllers/DeckController.cs`
    - `! grep -q "new DeckAnalysisCacheInputs\|new DeckComparisonCacheInputs\|new MetaGapCacheInputs" DeckFlow.Web/Controllers/DeckController.cs`
    - `! grep -q "reason={Reason}" DeckFlow.Web/Services/PacketSessionCache.cs` (M1 eviction template fix)

Build gate (D-14):
  - `dotnet.exe build DeckFlow.sln -c Release > /tmp/999.3-04-build.log 2>&1`
  - Confirm `grep -q "Build succeeded" /tmp/999.3-04-build.log`.
  - `CURRENT=$(grep -oP '\d+(?= Warning\(s\))' /tmp/999.3-04-build.log | tail -1)`
  - `BASELINE=$(cat .planning/phases/999.3-packet-download-session-cache/_baseline-warnings.txt)`
  - Assert `[ "$CURRENT" -le "$BASELINE" ]`.

STATE.md update strategy (W6 fix — field references, not line numbers; counters incremented from current values):
  - `last_updated` → bump to current ISO timestamp.
  - `last_activity` → `"2026-05-21 -- Phase 999.3 closed (packet download session cache shipped)"` (use UAT date from UAT doc).
  - `completed_phases` → read current value, INCREMENT BY 1. Do NOT prescribe absolute number.
  - `completed_plans` → read current value, INCREMENT BY 4.
  - `total_phases` / `total_plans` → read current values; raise if necessary.
  - `## Current Position` block → update `Phase:` to next per ROADMAP.md.
  - `## Session Continuity` block → update `Stopped at:`.
  - `## Operator Next Steps` → update to `/gsd-plan-phase {next-phase}`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Capture pre-UAT grep + build baseline + scaffold UAT doc + M4 curl recipes</name>
  <files>.planning/phases/999.3-packet-download-session-cache/999.3-UAT.md, .planning/phases/999.3-packet-download-session-cache/999.3-uat-d10-precedence-comparison.curl, .planning/phases/999.3-packet-download-session-cache/999.3-uat-d10-precedence-metagap.curl</files>
  <read_first>
    - .planning/phases/999.3-packet-download-session-cache/999.3-01-SUMMARY.md (Plan 01 closure).
    - .planning/phases/999.3-packet-download-session-cache/999.3-02-SUMMARY.md (Plan 02 closure).
    - .planning/phases/999.3-packet-download-session-cache/999.3-03-SUMMARY.md (Plan 03 closure).
    - .planning/phases/999.3-packet-download-session-cache/999.3-CONTEXT.md (D-14 gate).
    - .planning/phases/999.3-packet-download-session-cache/999.3-REVIEWS.md (M3 + M4 UAT-tightening rationale).
    - .planning/phases/999.3-packet-download-session-cache/_baseline-warnings.txt.
  </read_first>
  <action>
    Create three files.

    **FILE 1 — `999.3-UAT.md`:**
    Frontmatter:
    ```
    ---
    phase: 999.3-packet-download-session-cache
    uat_date: 2026-05-21
    deck_under_test: "<populated at UAT checkpoint>"
    pass: <populated at UAT checkpoint>
    ---
    ```

    H2 sections:

    1. `## Grep Gate (D-14)` — Run the grep commands listed in the verify block; record each command's exact output. Expected counts per `<interfaces>` block above. **Include negative gates** verifying Codex H1+H2+H4 regression prevention.

    2. `## Build Gate (D-14)` — MSBuild summary-line extraction. Record `CURRENT=N, BASELINE=M, delta=N-M`. Pass criteria: `CURRENT -le BASELINE`.

    3. `## UAT Procedure (deferred to Task 2 checkpoint)` — Document the UAT steps the user will execute. Leave "Result:" placeholders.

       a. User starts dev server manually.
       b. Browser to `http://localhost:5173`.

       **Workflow A: Deck Analysis cache-hit UAT.**
       c. User navigates to `/deck-analysis`.
       d. User pastes the Phase 999.2 reference deck.
       e. User selects same TargetAiPlatform + analysis questions.
       f. User clicks Submit (preview). Note time.
       g. After preview renders, click Download. Note time.
       h. Expected: Download click → zip arrives in <2 seconds (cache hit).
       i. Executor tails `logs/web-{today}.log` and runs `grep "Packet cache hit" logs/web-*.log` — must find at least one entry with 8-char key prefix.

       **Workflow A: Deck Analysis cache-miss UAT (TTL expiry OR process restart).**
       j. User waits 5+ minutes (or restarts the dev server).
       k. User clicks Download again.
       l. Expected: cache miss → BuildAsync runs → download arrives after ~2+ minutes.
       m. Executor greps `Packet cache miss` then `Packet cache write` (write happens after BuildAsync completes).

       **Workflow B: Deck Comparison cache-hit UAT.**
       n. Same shape as A. Capture log lines.

       **Workflow C: cEDH Meta-Gap cache-hit UAT.**
       o. Same shape. Capture log lines.

       **Workflow D (M3 fix): D-09 upload-write isolation UAT.**
       p. User kills the dev server process (Ctrl+C or terminate).
       q. User restarts the dev server (cold cache: no entries from preview).
       r. User navigates to `/deck-analysis`.
       s. User uses the `/deck-analysis/upload` form to upload a previously-generated zip containing the same deck state as the Phase 999.2 reference deck. (If no such zip exists, the user must FIRST generate one in a prior session and save it locally; document this prerequisite.)
       t. Upload completes; view renders with deck-analysis state.
       u. Executor greps the log for the upload request — expected: `Packet cache miss` (during upload's BuildAsync), then `Packet cache write` (after BuildAsync completes). Capture both lines verbatim with their key prefixes.
       v. User IMMEDIATELY clicks Download (no preview button — go straight from the just-rendered upload view).
       w. Expected: cache HIT (the upload was the writer). Download arrives in <2s.
       x. Executor greps the log for the download request — expected: `Packet cache hit` with the SAME 8-char key prefix recorded in step (u). **This is the M3 proof: upload was the writer, download was the reader.**

       **Workflow E (M4 fix): D-10 precedence preservation UAT — reproducible POST recipes.**
       y. User opens browser DevTools, Network tab.
       z. User submits a deck-comparison preview against any two decks, lets it complete.
       aa. User submits the deck-comparison download (with any inputs that include DeckASource/DeckBSource so it goes through the FULL pipeline first time). The download POST appears in the Network tab.
       ab. User right-clicks the download POST → "Copy → Copy as cURL (POSIX)" → pastes into `999.3-uat-d10-precedence-comparison.curl` (FILE 2 below). Manually edits the body of the `--data-raw` / `--data-binary` payload to:
           - Clear `DeckASource` and `DeckBSource` values (set to empty strings).
           - LEAVE `ComparisonResponseJson` populated with any plausible JSON (real or fabricated).
           - Keep all other form fields verbatim.
       ac. User runs the edited curl recipe from a fresh shell against the dev server: `bash 999.3-uat-d10-precedence-comparison.curl` (after `chmod +x` if needed). The endpoint MUST return a zip (HTTP 200, Content-Type application/zip).
       ad. Executor greps the log for the request lifecycle of THAT curl invocation — expected: NO `Packet cache lookup` line. The cache is layered BENEATH the existing short-circuit at lines 705-725, so the response-json-only fast path fires WITHOUT touching the cache.
       ae. Repeat steps z-ad for cedh-meta-gap via `999.3-uat-d10-precedence-metagap.curl` (FILE 3 below) — clear `DeckSource`, leave `MetaGapResponseJson` populated.

    4. `## Decisions Verified (D-01..D-14)` — Checklist mapping each decision to its observable verification:
       - D-01 (field-bag composition) — verified via cache hit on preview→download with identical inputs.
       - D-02 (URL normalization) — verified via cache hit when same deck submitted once as URL and once as canonical paste (defer to inspection if not testable in UAT timeframe).
       - D-03 (SHA-256 hex key) — verified via log inspection: `KeyPrefix` field is 8 lowercase hex chars.
       - D-04 (global key scope) — verified by inspection: no per-IP/per-session salt code path.
       - D-05 (5-min TTL) — verified via Workflow A step l (cache miss after >5 min wait).
       - D-06 (dedicated MemoryCache w/ SizeLimit) — verified via grep gate.
       - D-07 (size = char-count sum) — verified via log: `SizeBytes` > 0 on write, reasonably-sized for large deck.
       - D-08 (all 3 services) — verified via grep gate (Set count of 1 per service).
       - D-09 (/upload writes) — **verified via Workflow D (M3 isolated sequence — upload was the writer)**.
       - D-10 (precedence: short-circuit → cache → BuildAsync) — **verified via Workflow E (M4 reproducible curl recipes — short-circuit fires WITHOUT a cache log line)**.
       - D-11 (silent miss) — verified via Workflow A step l (no user-facing error; download still succeeds).
       - D-12 (stampede accepted) — verified by inspection: no SemaphoreSlim in PacketSessionCache.cs.
       - D-13 (structured Serilog) — verified via log inspection: template literal matches `"Packet cache {Outcome} for {KeyPrefix} ({SizeBytes} bytes)"`.
       - D-14 (test gate) — this document IS the verification artifact.

    5. `## Results` — Populate at the UAT checkpoint. Sections:
       - Preview→download timing (cache hit) for each workflow A/B/C: _____ seconds.
       - Preview→download timing (cache miss / pre-phase baseline) for workflow A: _____ seconds.
       - Cache hit/miss/write log line excerpts (verbatim).
       - **Workflow D (M3 upload isolation):** log excerpts showing `write` then `hit` with matching 8-char key prefix.
       - **Workflow E (M4 D-10 precedence):** log excerpts showing the curl POST request lifecycle with NO `Packet cache` line.
       - Pass/fail attestation for each workflow A-E.

    **FILE 2 — `999.3-uat-d10-precedence-comparison.curl`:**
    A scaffold file the user fills in via DevTools "copy as cURL" during the UAT checkpoint. Initial scaffold contents:
    ```bash
    #!/usr/bin/env bash
    # 999.3 UAT D-10 precedence — deck-comparison response-json-only fast path.
    #
    # M4 fix (Codex review): replaces brittle UI field surgery with a reproducible POST recipe.
    #
    # HOW TO USE:
    # 1. Open browser DevTools → Network tab.
    # 2. Submit a deck-comparison preview, then submit the download.
    # 3. Right-click the download POST → Copy → Copy as cURL (POSIX).
    # 4. Paste the resulting `curl` command BELOW (replacing this placeholder).
    # 5. Edit the request body:
    #    - Clear DeckASource and DeckBSource values (empty strings).
    #    - Leave ComparisonResponseJson populated with any plausible JSON.
    #    - Keep other form fields verbatim.
    # 6. Run from a fresh shell: `bash 999.3-uat-d10-precedence-comparison.curl`.
    # 7. Expected: HTTP 200 + Content-Type application/zip. Serilog log shows NO `Packet cache` line.

    # ---PASTE EDITED CURL COMMAND BELOW---
    curl --fail --output deck-comparison-download.zip \
      'http://localhost:5173/deck-comparison/download' \
      -X POST \
      -H 'Content-Type: application/x-www-form-urlencoded' \
      --data-urlencode 'DeckASource=' \
      --data-urlencode 'DeckBSource=' \
      --data-urlencode 'ComparisonResponseJson={"deckABlurb":"placeholder","deckBBlurb":"placeholder"}' \
      --data-urlencode 'WorkflowStep=3' \
      --data-urlencode 'TargetAiPlatform=ChatGPT'
    # ---END---
    ```
    The placeholder curl is a best-effort initial guess; the user replaces it during the UAT checkpoint with a real DevTools-captured recipe.

    **FILE 3 — `999.3-uat-d10-precedence-metagap.curl`:**
    Same scaffold shape for cedh-meta-gap:
    ```bash
    #!/usr/bin/env bash
    # 999.3 UAT D-10 precedence — cedh-meta-gap response-json-only fast path.
    #
    # M4 fix (Codex review): see 999.3-uat-d10-precedence-comparison.curl header.
    #
    # HOW TO USE: same as the comparison recipe. Clear DeckSource (not DeckASource/DeckBSource).
    # Leave MetaGapResponseJson populated.

    # ---PASTE EDITED CURL COMMAND BELOW---
    curl --fail --output cedh-meta-gap-download.zip \
      'http://localhost:5173/cedh-meta-gap/download' \
      -X POST \
      -H 'Content-Type: application/x-www-form-urlencoded' \
      --data-urlencode 'DeckSource=' \
      --data-urlencode 'MetaGapResponseJson={"placeholder":true}' \
      --data-urlencode 'WorkflowStep=3' \
      --data-urlencode 'TargetAiPlatform=ChatGPT'
    # ---END---
    ```

    Save all three files. `999.3-UAT.md` is the binding D-14 test-gate artifact.
  </action>
  <verify>
    <automated>
      cd /mnt/c/users/chrislunt/source/personal/deckflow &amp;&amp;
      test -f .planning/phases/999.3-packet-download-session-cache/999.3-UAT.md &amp;&amp;
      test -f .planning/phases/999.3-packet-download-session-cache/999.3-uat-d10-precedence-comparison.curl &amp;&amp;
      test -f .planning/phases/999.3-packet-download-session-cache/999.3-uat-d10-precedence-metagap.curl &amp;&amp;
      grep -q "## Grep Gate" .planning/phases/999.3-packet-download-session-cache/999.3-UAT.md &amp;&amp;
      grep -q "## Build Gate" .planning/phases/999.3-packet-download-session-cache/999.3-UAT.md &amp;&amp;
      grep -q "## UAT Procedure" .planning/phases/999.3-packet-download-session-cache/999.3-UAT.md &amp;&amp;
      grep -q "## Decisions Verified" .planning/phases/999.3-packet-download-session-cache/999.3-UAT.md &amp;&amp;
      grep -q "Workflow D" .planning/phases/999.3-packet-download-session-cache/999.3-UAT.md &amp;&amp;
      grep -q "Workflow E" .planning/phases/999.3-packet-download-session-cache/999.3-UAT.md &amp;&amp;
      grep -q "D-01" .planning/phases/999.3-packet-download-session-cache/999.3-UAT.md &amp;&amp;
      grep -q "D-14" .planning/phases/999.3-packet-download-session-cache/999.3-UAT.md &amp;&amp;
      grep -c "_packetCache.Set" DeckFlow.Web/Services/DeckAnalysisPacketService.cs | grep -qx 1 &amp;&amp;
      grep -c "_packetCache.Set" DeckFlow.Web/Services/DeckComparisonService.cs | grep -qx 1 &amp;&amp;
      grep -c "_packetCache.Set" DeckFlow.Web/Services/MetaGapService.cs | grep -qx 1 &amp;&amp;
      grep -c "_packetCache.TryGet" DeckFlow.Web/Controllers/DeckController.cs | grep -qx 3 &amp;&amp;
      grep -cE "public async Task<string\?> TryComputeCacheKeyAsync" DeckFlow.Web/Services/DeckAnalysisPacketService.cs | grep -qx 1 &amp;&amp;
      grep -cE "public async Task<string\?> TryComputeCacheKeyAsync" DeckFlow.Web/Services/DeckComparisonService.cs | grep -qx 1 &amp;&amp;
      grep -cE "public async Task<string\?> TryComputeCacheKeyAsync" DeckFlow.Web/Services/MetaGapService.cs | grep -qx 1 &amp;&amp;
      grep -c "AddSingleton<PacketSessionCache>" DeckFlow.Web/Program.cs | grep -qx 1 &amp;&amp;
      grep -c "sp.GetRequiredService<PacketSessionCache>" DeckFlow.Web/Program.cs | grep -qx 3 &amp;&amp;
      test -f DeckFlow.Web/Services/PacketSessionCache.cs &amp;&amp;
      ! grep -q "public static string ComputeCacheKey" DeckFlow.Web/Services/*.cs &amp;&amp;
      ! grep -q "ResolveCanonicalTextAsync" DeckFlow.Web/Controllers/DeckController.cs &amp;&amp;
      ! grep -q "request\.CommanderName" DeckFlow.Web/Controllers/DeckController.cs &amp;&amp;
      ! grep -q "IDeckEntryLoader" DeckFlow.Web/Controllers/DeckController.cs &amp;&amp;
      ! grep -q "reason={Reason}" DeckFlow.Web/Services/PacketSessionCache.cs &amp;&amp;
      dotnet.exe build DeckFlow.sln -c Release > /tmp/999.3-04-build.log 2>&amp;1 &amp;&amp;
      grep -q "Build succeeded" /tmp/999.3-04-build.log &amp;&amp;
      CURRENT=$(grep -oP '\d+(?= Warning\(s\))' /tmp/999.3-04-build.log | tail -1) &amp;&amp;
      BASELINE=$(cat .planning/phases/999.3-packet-download-session-cache/_baseline-warnings.txt) &amp;&amp;
      [ "$CURRENT" -le "$BASELINE" ]
    </automated>
  </verify>
  <acceptance_criteria>
    - `999.3-UAT.md` exists with all five H2 sections (Grep Gate, Build Gate, UAT Procedure, Decisions Verified, Results) PLUS Workflow D (M3) and Workflow E (M4) steps.
    - `999.3-uat-d10-precedence-comparison.curl` and `999.3-uat-d10-precedence-metagap.curl` exist as scaffolds (M4 fix).
    - All grep-gate commands return expected counts.
    - **Negative grep gates pass** (Codex H1+H2+H4 + M1 regression prevention).
    - `dotnet.exe build DeckFlow.sln -c Release` exits 0 with `CURRENT -le BASELINE`.
    - `## Results` section exists as placeholder.
  </acceptance_criteria>
  <done>UAT document + curl recipes scaffolded; grep + build gates pass. Ready for the human-verify checkpoint.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human UAT — measure preview→download timing + M3 upload isolation + M4 D-10 precedence</name>
  <what-built>
    Phase 999.3 has shipped Plans 01-03:
    - Plan 01: `PacketSessionCache.cs` + DI registration + warning baseline + `PacketSizeEstimator` + cache-input records. M1 + M2 fixes locked in (eviction log uses exact D-13 template; PacketSizeEstimator is the only helper shape).
    - Plan 02: All 3 workflow services write to the cache at line 467 / 198 / 196 of `BuildAsync`. Each service exposes `public async Task<string?> TryComputeCacheKeyAsync(request, ct)` (Codex H1+H3+H4 architectural pivot — async helper, not static helper). `/deck-analysis/upload` participates automatically (D-09).
    - Plan 03: All 3 download handlers consult the cache between the existing response-json-only short-circuit and BuildAsync, calling Plan 02's async helpers. NO `IDeckEntryLoader` was added; NO `request.CommanderName` referenced; NO controller-side normalization.

    Task 1 of Plan 04 has scaffolded `999.3-UAT.md` + two `*.curl` recipe files. This checkpoint is the manual UAT.
  </what-built>
  <how-to-verify>
    **DO NOT auto-launch the dev server.** Ask the user to start it.

    Steps (record results in `999.3-UAT.md` `## Results` section as the user reports them):

    1. **User starts the dev server** via `scripts/run-web.sh` (WSL/Linux) or `scripts/run-web.ps1` (Windows).

    2. **Workflow A: Deck Analysis cache-hit + cache-miss UAT.** See UAT doc steps c-m.

    3. **Workflow B: Deck Comparison cache-hit UAT.** See UAT doc step n.

    4. **Workflow C: cEDH Meta-Gap cache-hit UAT.** See UAT doc step o.

    5. **Workflow D (M3 fix): D-09 upload-write isolation UAT.** See UAT doc steps p-x.
       - **Critical:** confirm the upload's `Packet cache write` line precedes the download's `Packet cache hit` line, with MATCHING 8-char key prefix.
       - If the user has no pre-existing zip: skip Workflow D and note "Workflow D deferred — no upload zip available" in the UAT doc. The remaining workflows (A/B/C/E) are sufficient for phase closure; D-09 is verified by inspection of the service code path instead.

    6. **Workflow E (M4 fix): D-10 precedence preservation UAT.** See UAT doc steps y-ae.
       - User captures real cURL recipes via DevTools "Copy as cURL", pastes into the two scaffold files, edits the bodies, runs from a fresh shell.
       - Executor confirms HTTP 200 + zip download AND no `Packet cache` log line for that request.

    7. **Sign-off.** Executor populates `## Results` with captured data. User confirms "UAT passed" or describes failures.

    **Resume signal:** Type "UAT approved — close phase" to proceed, OR describe failures.
  </how-to-verify>
  <resume-signal>Type "UAT approved — close phase" if all workflows pass, or describe failures (which workflow, which step, what was observed).</resume-signal>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Update README + STATE + ROADMAP to reflect phase closure</name>
  <files>README.md, .planning/STATE.md, .planning/ROADMAP.md</files>
  <read_first>
    - README.md (entire file).
    - .planning/STATE.md (entire file — locate fields by NAME).
    - .planning/ROADMAP.md (entire file — locate Phase 999.3 entry by header).
    - .planning/phases/999.3-packet-download-session-cache/999.3-UAT.md (just-approved with `## Results` populated).
    - CLAUDE.md "Commits".
  </read_first>
  <action>
    Apply THREE additive edits. **Reference STATE.md / ROADMAP.md sections by FIELD NAME / HEADER TEXT, not by line number. Counter fields are updated by READING current value and INCREMENTING.**

    **EDIT 1 — README.md:**
    Add 1-3 sentences user-visible (Performance / Features section if one exists; otherwise a new `## Performance` H2). Suggested copy:
    > **Packet download caching (v1.3 / Phase 999.3):** Deck Analysis, Deck Comparison, and cEDH Meta-Gap download endpoints reuse the Scryfall pipeline result built during preview, so a large Commander deck's download click completes in under 2 seconds instead of 2+ minutes. Cache is in-memory only (process-local, 5-minute TTL, 10MB cap); cache miss falls through silently to the full pipeline.

    Match README's existing voice. DO NOT reformat the rest of the README.

    **EDIT 2 — .planning/STATE.md:**
    Locate fields by name (grep), not by line number.
    1. `last_updated` → bump to current ISO timestamp.
    2. `last_activity` → `"2026-05-21 -- Phase 999.3 closed (packet download session cache shipped)"` (use UAT date from UAT doc frontmatter).
    3. `completed_phases` → read current value, INCREMENT BY 1.
    4. `completed_plans` → read current value, INCREMENT BY 4.
    5. `total_phases` → read current; raise if needed.
    6. `total_plans` → read current; raise by 4 if needed.
    7. `## Current Position` block → update `Phase:` to next per ROADMAP.md (likely "999.4 truncated-json-response-ux — PENDING" — confirm via file).
    8. `## Session Continuity` → update `Stopped at:` to `Phase 999.3 closed — packet download session cache shipped`.
    9. `## Operator Next Steps` → update bullet to `Run /gsd-plan-phase 999.4 to decompose the truncated-JSON response UX phase.`

    DO NOT touch `## Accumulated Context`.

    **EDIT 3 — .planning/ROADMAP.md:**
    Locate Phase 999.3 entry by header `### Phase 999.3:`. Update:
    - `**Plans:**` line → `**Plans:** 4/4 plans complete`.
    - Plan list → Wave-grouped list:
      ```
      Plans:
      **Wave 1**

      - [x] 999.3-01-PLAN.md — Create PacketSessionCache wrapper + size-estimation helpers + Program.cs DI registration + warning baseline

      **Wave 2**

      - [x] 999.3-02-PLAN.md — Cache write at end of BuildAsync in 3 services + Program.cs factory blocks + per-service async TryComputeCacheKeyAsync helpers

      **Wave 3**

      - [x] 999.3-03-PLAN.md — Layer cache-read precedence onto 3 download handlers in DeckController.cs (D-10), calling Plan 02's async helpers

      **Wave 4**

      - [x] 999.3-04-PLAN.md — Manual UAT (incl. M3 upload isolation + M4 D-10 curl recipes) + grep gate + build gate + README/STATE/ROADMAP closure
      ```
    - Progress table row for 999.3 → Plans column `4/4`, Status `Complete`, completion date from UAT.

    DO NOT touch other phase entries or backlog.
  </action>
  <verify>
    <automated>
      cd /mnt/c/users/chrislunt/source/personal/deckflow &amp;&amp;
      grep -qi "session cache\|packet.*cache\|999.3" README.md &amp;&amp;
      grep -q "999.3 closed\|999.3.*shipped\|packet download session cache" .planning/STATE.md &amp;&amp;
      grep -q "999.3-01-PLAN.md" .planning/ROADMAP.md &amp;&amp;
      grep -q "999.3-02-PLAN.md" .planning/ROADMAP.md &amp;&amp;
      grep -q "999.3-03-PLAN.md" .planning/ROADMAP.md &amp;&amp;
      grep -q "999.3-04-PLAN.md" .planning/ROADMAP.md &amp;&amp;
      grep -q "999.3.*4/4.*Complete\|999.3 Packet Download Session Cache.*4/4.*Complete" .planning/ROADMAP.md
    </automated>
  </verify>
  <acceptance_criteria>
    - README.md contains a user-visible note mentioning the session cache.
    - .planning/STATE.md `last_activity` mentions "999.3 closed" or "session cache shipped".
    - .planning/STATE.md `completed_phases` and `completed_plans` are INCREMENTED.
    - .planning/ROADMAP.md Phase 999.3 entry lists all four PLAN files in their wave structure.
    - .planning/ROADMAP.md Progress table row for 999.3 shows `4/4` plans and `Complete` status.
    - No other phase entries or backlog items modified.
  </acceptance_criteria>
  <done>Phase 999.3 documentation closure complete. One commit `docs(999.3): close phase — packet session cache shipped, UAT approved (M3+M4 verified)` covering Plan 04 Task 1 (UAT scaffold + curl recipes), Task 2 result (populated `999.3-UAT.md`), and Task 3 (README + STATE + ROADMAP). Plain default-author commit. DO NOT push autonomously — `git push` requires user instruction.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| n/a | Plan 04 is a verification + documentation plan — no new code paths, no new trust boundaries. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-999.3-18 | Tampering | UAT log excerpts pasted into `999.3-UAT.md` could contain user-supplied deck contents | accept | Deck contents are public. Log lines are SHA-256 prefixes + size counts only — no deck text appears in the structured log template per D-13. |
| T-999.3-19 | Information Disclosure | README mentions the cache structure | accept | Cache is in-memory only with no security boundary. Public repo regardless. |
| T-999.3-20 | Information Disclosure | M4 curl recipe files may contain real session cookies or auth tokens | mitigate | The recipes target DeckFlow which has no auth on these endpoints (anonymous public app). Executor reviews each `.curl` file before commit to confirm no `Cookie:` header carrying sensitive values. If found, strip before committing. |
| T-999.3-SC | Tampering | npm/pip/cargo installs | accept | No package installs in Plan 04. |
</threat_model>

<verification>
- All grep-gate commands listed in `999.3-UAT.md ## Grep Gate` return expected counts.
- All negative grep gates pass (Codex H1+H2+H4 + M1 regression prevention).
- `dotnet.exe build DeckFlow.sln -c Release` exits 0 with `CURRENT -le BASELINE`.
- Manual UAT timings recorded for Workflows A/B/C with <2s cache-hit download click.
- At least one `Packet cache hit`, `Packet cache miss`, `Packet cache write` log line excerpt captured verbatim.
- **Workflow D (M3 fix):** upload-write isolation log sequence captured (write before hit with matching key prefix).
- **Workflow E (M4 fix):** D-10 precedence proven via reproducible curl recipes — short-circuit fires WITHOUT a cache log line.
- README.md updated.
- STATE.md `last_activity` reflects closure; counters incremented from prior values.
- ROADMAP.md Phase 999.3 + Progress table show `4/4` complete.
</verification>

<success_criteria>
- D-14 test gate satisfied: manual UAT + dotnet build clean + grep gate.
- Codex M3 + M4 fixes verified empirically (upload-write isolation; reproducible D-10 curl recipes).
- Phase 999.3 closed with one closure commit (Plan 04). Total Phase 999.3 commits: ~7-9.
- Plain default-author commits, no `Co-Authored-By` trailer.
- README updated.
- Phase 999.2 reference deck preview→download <2s on cache hit (vs >120s pre-phase).
</success_criteria>

<output>
Create `.planning/phases/999.3-packet-download-session-cache/999.3-04-SUMMARY.md` summarizing:
- UAT timings (cache hit + cache miss for Workflows A/B/C).
- M3 Workflow D result (upload-write isolation log excerpts).
- M4 Workflow E result (D-10 precedence curl recipes + log evidence).
- Git commit SHA + message for the closure commit.
- `dotnet build` exit 0 with `CURRENT -le BASELINE`.
- README + STATE + ROADMAP updates (STATE counters described by field name + increment, not absolute values).
- Pointer to next phase: `/gsd-plan-phase 999.4` (truncated-json-response-ux).
</output>
</content>
