---
status: complete
quick_id: 260507-m8k
date: 2026-05-07
commit: 9698551
---

## 260507-m8k Summary

**Final commit on `main`:** `9698551` — `fix(admin-harvest): live decks counter + auto-refresh on run state change`. Pre-dispatch plan commit at `240deac`.

**Files modified (11):**
- `DeckFlow.Web/Services/Harvest/IHarvestRunStore.cs` — added `UpdateProgressAsync` (counters-only) + `GetRecentRevisionAsync`.
- `DeckFlow.Web/Services/Harvest/HarvestRunStore.cs` — implemented both new methods; dialect-safe SQL using existing `@name` + Postgres-vs-SQLite Guid binding pattern.
- `DeckFlow.Web/Services/ICategoryKnowledgeStore.cs` — added optional `IProgress<int>?` to `RunCacheSweepAsync`.
- `DeckFlow.Web/Services/CategoryKnowledgeStore.cs` — forwarded reporter into sweep session.
- `DeckFlow.Core/Knowledge/ArchidektDeckCacheSession.cs` — `progress?.Report(added + updated)` after each processed deck (success path only; cancellation/exception paths do not report).
- `DeckFlow.Web/Services/ArchidektCacheJobService.cs` — private sealed `IProgress<int>` with throttle (≥ 10 decks OR ≥ 2 s); errors swallowed with WARN `Harvest.Run.ProgressWriteFailed`; per-job `jobCts.Token` used so operator cancel still aborts in-flight progress writes.
- `DeckFlow.Web/Controllers/Admin/AdminHarvestController.cs` — `recentRunsRevision` added to `HarvestStatusPayload` inside the existing 1 s memory cache wrapper; `SameOriginRequestValidator` gate untouched.
- `DeckFlow.Web/wwwroot/ts/admin-harvest.ts` — removed idle early-return; idle 10 s / active 3 s cadence; `lastRevision` baseline + `window.location.reload()` on change; non-OK response reschedules at idle cadence; terminal-reload fallback preserved.
- `DeckFlow.Web.Tests/{TestDoubles/FakeCategoryKnowledgeStore.cs, CategorySuggestionServiceTests.cs, ArchidektCacheJobServiceTests.cs}` — test fakes mirror new interface members. Not in original plan; required by build.

**Files NOT modified (intentionally):**
- `DeckFlow.Web/Views/AdminHarvest/Index.cshtml` — `<noscript><meta http-equiv="refresh" content="5" /></noscript>` block deliberately untouched (no-JS path preserved).

**SQL verification:**
- `UpdateProgressAsync`: `UPDATE harvest_runs SET decks_processed = @decksProcessed, additional_decks_found = @additionalDecksFound WHERE id = @id;`
- `GetRecentRevisionAsync`: `SELECT MAX(started_utc), MAX(completed_utc), COUNT(1) FROM harvest_runs;`
- Both statements use only `MAX`, `COUNT(1)`, and standard parameterized UPDATE — universal across SQLite and Postgres. No `RETURNING`, no `ON CONFLICT`, no dialect-specific function. Existing `@name` parameter convention + Postgres-Guid-direct vs SQLite-id.ToString() ID binding preserved.

**`recentRunsRevision` token format:** `{startedTicks}|{completedTicks}|{count}` (UTC ticks as `long.ToString(InvariantCulture)`; empty segments when either timestamp is NULL). Future debugging can grep for it in controller + TS.

**Verification mode:** local static + build only. Live B1/B2 manual gates require post-deploy verification on `deckflow.gg` per project push-and-watch pattern (matches recent task 260507-l7x).
- **Pass 1 — `dotnet build DeckFlow.sln`:** CLEAN. 0 errors, 10 warnings (all NU1900 NuGet vulnerability-data fetch failures — network/firewall, baseline noise unrelated to code).
- **Pass 2 — static contract checks:** `UpdateStateAsync` 13 call sites preserved (original 8-arg shape). `RunCacheSweepAsync` 7 call sites preserved (new optional param defaulted). New wires `RecentRunsRevision`/`GetRecentRevisionAsync`/`UpdateProgressAsync` visible end-to-end (13 occurrences in source). `wwwroot/js/admin-harvest.js` regenerated by MSBuild's `tsc` step (9 new symbol matches).

**Deviations from plan (justified):**
- `IProgress<int>?` placed AFTER `CancellationToken` on `RunCacheSweepAsync` and `RunAsync` (project convention says CT last; here append-after preserves all positional callers — alternative would have broken 7 existing call sites). Codex flagged; orchestrator accepted.
- `ArchidektCacheJobService` uses a private sealed `IProgress<int>` impl rather than `Progress<int>` because `BackgroundService` has no `SynchronizationContext`. Plan called this out as expected.
- Test seam updates in `DeckFlow.Web.Tests` not in original plan but required by build.

**Risks for production verification (post-deploy on deckflow.gg):**
- B1 live gate: start 5-min harvest, hit `GET /Admin/Harvest/status` mid-run, confirm `decksProcessed > 0` within ~30 s. Reload `/Admin/Harvest`, confirm banner counter increments mid-run via 3 s active poll.
- B2 live gate: load `/Admin/Harvest` while idle, trigger Run Now in another tab/session, confirm original tab auto-reloads within ~10 s and Recent Runs grid shows the new run.
- Operator-cancel during in-flight progress write: `UpdateProgressAsync` deliberately does NOT touch `state`, `started_utc`, `completed_utc`, or `error_message`, so a racing terminal-state write should not be clobbered. Verify by clicking Cancel mid-run and confirming the row's final state matches the Cancel write, not a stale progress write.
- noscript path: disable JS, reload `/Admin/Harvest`, confirm 5 s meta-refresh still fires.
