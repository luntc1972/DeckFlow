---
phase: 999.7-v1-3-audit-cleanup
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - DeckFlow.Web/Controllers/DeckController.cs
  - .planning/phases/999.5-v1-3-backlog-catch-up-test-hardening/999.5-UAT.md
autonomous: true
requirements: [SC4, SC5, SC6, SC7]

must_haves:
  truths:
    - "DeckController.cs DeckAnalysisDownload handler (around line 514, inside the `try` block before TryComputeCacheKeyAsync) carries a single-line comment documenting the intentional omission of the response-JSON short-circuit tier."
    - "The new comment cites Phase 999.3 D-10 and audit F-02 as the rationale references."
    - "The new comment lives BETWEEN the `DeckAnalysisDownload` handler signature and the existing `Phase 999.3 D-10: service-owned cache-key parity` comment line (anchored verification via awk-bounded grep — not fuzzy line-number range)."
    - "The existing `// Phase 999.3 D-10: service-owned cache-key parity before BuildAsync; misses fall through silently.` comment is preserved."
    - "999.5-UAT.md frontmatter `status:` field reads `superseded-by-999.6` (replacing `testing`)."
    - "999.5-UAT.md body (everything after the closing `---` of the frontmatter) is byte-identical."
    - "`dotnet build DeckFlow.sln -c Release` exits 0; warning count <= post-999.6 baseline."
    - "`dotnet test DeckFlow.sln -c Release` reports Failed:0 Passed:497 Skipped:3 Total:500."
  artifacts:
    - path: "DeckFlow.Web/Controllers/DeckController.cs"
      provides: "One-line explanatory comment near line 514 documenting deck-analysis asymmetry vs comparison/meta-gap"
      contains: "phrase 'audit F-02' AND phrase 'D-10' AND phrase 'response-json' (or 'response-JSON')"
    - path: ".planning/phases/999.5-v1-3-backlog-catch-up-test-hardening/999.5-UAT.md"
      provides: "Frontmatter status field finalized to terminal label"
      contains: "literal text `status: superseded-by-999.6` on its own line within the YAML frontmatter"
  key_links:
    - from: "DeckController.cs DeckAnalysisDownload (lines 508-521)"
      to: "DeckController.cs Comparison + CedhMetaGap download handlers (3-tier order per Phase 999.3 D-10)"
      via: "documented intentional asymmetry comment"
      pattern: "audit F-02 recommendation closed"
    - from: ".planning/phases/999.5-.../999.5-UAT.md frontmatter"
      to: ".planning/phases/999.6-.../999.6-UAT.md (the supersedence target — full-suite gate)"
      via: "`status: superseded-by-999.6` terminal label"
      pattern: "no `status: testing` left in archived UAT files"
---

<objective>
Close two adjacent paper-trail cleanups together (both are single-line edits, both are documentation-only, neither carries cross-file dependency):

1. **DeckController.cs:~514 asymmetry comment** — add a one-line comment documenting why DeckAnalysisDownload has NO response-JSON short-circuit tier before the cache lookup, while Comparison + CedhMetaGap correctly implement the 3-tier order (response-JSON → cache → BuildAsync) per Phase 999.3 D-10. Closes audit finding F-02 in `.planning/v1.3-MILESTONE-AUDIT.md` lines 206-210.

2. **999.5-UAT.md frontmatter status finalization** — change `status: testing` to `status: superseded-by-999.6` (terminal label) since the 999.5 UAT was never formally closed and was superseded by the Phase 999.6 full-suite gate per the audit verification artifact inventory (line 107).

Both edits land in a single plan to avoid micro-plan churn (per planner sizing guidance). Both touch a single line in a single file each, no cross-file dependency.

Purpose: documentation accuracy + archived UAT clarity before milestone archive.
Output: 2 single-line edits; build + tests green at 999.6 baseline.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/v1.3-MILESTONE-AUDIT.md
@DeckFlow.Web/Controllers/DeckController.cs
@.planning/phases/999.5-v1-3-backlog-catch-up-test-hardening/999.5-UAT.md
@CLAUDE.md

<interfaces>
DeckController.cs DeckAnalysisDownload handler entry (lines 508-521 — verified at planning time):

```
[HttpPost("/deck-analysis/download")]
[ValidateAntiForgeryToken]
public async Task<IActionResult> DeckAnalysisDownload(DeckAnalysisRequest request)
{
    request ??= new DeckAnalysisRequest();

    try
    {
        // Phase 999.3 D-10: service-owned cache-key parity before BuildAsync; misses fall through silently.
        // Misses intentionally pay one extra deck load here before BuildAsync (D-11 accepted trade-off).
        var cacheKey = await _deckAnalysisPacketService.TryComputeCacheKeyAsync(request, HttpContext.RequestAborted);
        ...
```

The new asymmetry comment should slot ABOVE the existing `// Phase 999.3 D-10:` comment line (or between it and the `var cacheKey =` call) — i.e. it documents the OMISSION of an earlier tier that comparison/meta-gap have. The comment should NOT alter the existing two-line D-10/D-11 comment.

999.5-UAT.md frontmatter (lines 1-7 — verified at planning time):

```
---
status: testing
phase: 999.5-v1-3-backlog-catch-up-test-hardening
source: [999.5-01-SUMMARY.md, 999.5-02-SUMMARY.md, 999.5-03-SUMMARY.md, 999.5-04-SUMMARY.md]
started: 2026-05-22T14:02:49Z
updated: 2026-05-22T14:02:49Z
---
```

Only the `status:` line on line 2 changes.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add F-02 asymmetry comment to DeckController.cs DeckAnalysisDownload handler</name>
  <files>DeckFlow.Web/Controllers/DeckController.cs</files>
  <read_first>
    - DeckFlow.Web/Controllers/DeckController.cs (lines 500-525 — DeckAnalysisDownload entry, existing D-10 / D-11 comments)
    - .planning/v1.3-MILESTONE-AUDIT.md (lines 206-210 — F-02 statement of intent: "deck-analysis has no paste-response-only re-download path")
    - .planning/phases/999.3-packet-download-session-cache/ (any SUMMARY referencing D-10 for the comparison/meta-gap 3-tier order — for citation accuracy)
    - CLAUDE.md (formatting paranoia rules)
  </read_first>
  <action>
    Insert a single-line `// `-prefixed comment inside the DeckAnalysisDownload handler's `try` block, immediately ABOVE the existing comment `// Phase 999.3 D-10: service-owned cache-key parity before BuildAsync; misses fall through silently.`. The new line must:
      - Be a `//` line comment (NOT `/// <summary>` — this is in-method code, not a doc-comment).
      - Document the intentional asymmetry: deck-analysis has no response-JSON short-circuit tier (unlike comparison + cedh-meta-gap, which implement the 3-tier order: response-JSON → cache → BuildAsync per Phase 999.3 D-10).
      - Cite both Phase 999.3 D-10 AND audit F-02 as references.
      - Be a single line (one line of source; the comment may wrap visually but inserts only one new line in the file).
      - Use indentation matching the surrounding `// Phase 999.3 D-10:` line (same leading whitespace).

    Suggested wording (executor MAY choose equivalent phrasing as long as it conveys the rationale and cites both references; do NOT include fenced code blocks):

      `// Audit F-02: intentional asymmetry — DeckAnalysis has no response-JSON short-circuit tier (deck-analysis lacks a paste-response-only re-download path; Comparison + CedhMetaGap implement the full 3-tier order per Phase 999.3 D-10).`

    Constraints (NON-NEGOTIABLE per project CLAUDE.md "Formatting" paragraph):
      - Touch ONLY the lines being added. Do not reformat any other line.
      - Do not modify the existing two-line D-10 / D-11 comment block.
      - Do not modify the `var cacheKey = ...` call or anything below it.
      - Do not modify any other method, attribute, or class in DeckController.cs.
      - Preserve LF line endings.
      - No fenced code blocks anywhere in the source.
  </action>
  <verify>
    <automated>grep -c "Audit F-02" DeckFlow.Web/Controllers/DeckController.cs ; grep -n "Phase 999.3 D-10: service-owned cache-key parity" DeckFlow.Web/Controllers/DeckController.cs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "F-02" DeckFlow.Web/Controllers/DeckController.cs` returns `1` (exactly one citation of F-02 introduced).
    - `grep -c "Phase 999.3 D-10: service-owned cache-key parity before BuildAsync" DeckFlow.Web/Controllers/DeckController.cs` returns `1` (the existing D-10 comment is preserved verbatim).
    - `grep -c "D-11 accepted trade-off" DeckFlow.Web/Controllers/DeckController.cs` returns `1` (the existing D-11 comment is preserved verbatim).
    - `grep -nE "response-(json|JSON)|response-json" DeckFlow.Web/Controllers/DeckController.cs` shows the new comment with the response-json terminology (or an explicit synonym such as "paste-response-only").
    - **HANDLER-ANCHORED PLACEMENT GATE (replaces fuzzy line-range check):** The command `awk '/public async Task<IActionResult> DeckAnalysisDownload/,/Phase 999.3 D-10: service-owned cache-key parity/' DeckFlow.Web/Controllers/DeckController.cs | grep -c "F-02"` MUST return `1`. This confirms the F-02 comment lives strictly inside the awk-bounded block — between the `DeckAnalysisDownload` handler signature and the existing D-10 comment — proving correct method placement regardless of file-line drift.
    - `git diff --numstat DeckFlow.Web/Controllers/DeckController.cs` shows `1  0  ...` (one line added, zero removed — single-line insertion, no replacement).
    - `git diff DeckFlow.Web/Controllers/DeckController.cs` shows additions only inside DeckAnalysisDownload; no other method changed.
  </acceptance_criteria>
  <done>F-02 asymmetry comment present inside the DeckAnalysisDownload handler `try` block above the existing D-10 comment (verified by awk-bounded grep gate); existing D-10 / D-11 comments preserved verbatim; no other lines touched.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Finalize 999.5-UAT.md frontmatter status to superseded-by-999.6</name>
  <files>.planning/phases/999.5-v1-3-backlog-catch-up-test-hardening/999.5-UAT.md</files>
  <read_first>
    - .planning/phases/999.5-v1-3-backlog-catch-up-test-hardening/999.5-UAT.md (lines 1-15 — frontmatter)
    - .planning/v1.3-MILESTONE-AUDIT.md (line 107 + line 178 — confirms "superseded by 999.6" terminal label is the audit-recommended target)
  </read_first>
  <action>
    On line 2 of `.planning/phases/999.5-v1-3-backlog-catch-up-test-hardening/999.5-UAT.md`, replace `status: testing` with `status: superseded-by-999.6`. Touch only that line.
    Do NOT modify:
      - the opening `---` on line 1 or the closing `---` of the frontmatter,
      - the `phase:` / `source:` / `started:` / `updated:` lines,
      - any line in the body of the file (the entire content after the closing frontmatter `---`).
    Preserve LF line endings.
  </action>
  <verify>
    <automated>grep -E "^status: " .planning/phases/999.5-v1-3-backlog-catch-up-test-hardening/999.5-UAT.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^status: superseded-by-999.6$" .planning/phases/999.5-v1-3-backlog-catch-up-test-hardening/999.5-UAT.md` returns `1`.
    - `grep -c "^status: testing$" .planning/phases/999.5-v1-3-backlog-catch-up-test-hardening/999.5-UAT.md` returns `0`.
    - `git diff --numstat .planning/phases/999.5-v1-3-backlog-catch-up-test-hardening/999.5-UAT.md` shows `1  1  ...` (one line added, one removed — exactly the single-line replacement).
    - `git diff .planning/phases/999.5-v1-3-backlog-catch-up-test-hardening/999.5-UAT.md` shows only the `status:` line modified.
    - Body of the file (lines past the closing frontmatter `---`) is byte-identical: `diff <(sed -n '/^---$/,/^---$/!p' before) <(sed -n '/^---$/,/^---$/!p' after)` returns empty (executor may use git show HEAD:path for "before").
  </acceptance_criteria>
  <done>999.5-UAT.md frontmatter status finalized; body untouched.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Build + test gate (regression catch for Task 1 — code-touching change)</name>
  <files>(none — verification gate only)</files>
  <read_first>
    - .planning/v1.3-MILESTONE-AUDIT.md (line 181 — Phase 999.6 PHASE EXIT GATE baseline: `Failed: 0, Passed: 497 (Web 440 + Core 57), Skipped: 3, Total: 500`)
    - CLAUDE.md (testing guidance — VSTest reliable via `/mnt/c/Program Files/dotnet/dotnet.exe`)
  </read_first>
  <action>
    Run the Windows-dotnet test gate. The Task 1 edit is a `//` comment inside a `try` block (no IL impact) but the build+test gate is the regression catch per CLAUDE.md's "Run + update tests after changes" instruction and per project pinned baseline at 999.6 PHASE EXIT GATE.

    Commands (executor runs in repo root):
      1. `"/mnt/c/Program Files/dotnet/dotnet.exe" build DeckFlow.sln -c Release --nologo`
      2. `"/mnt/c/Program Files/dotnet/dotnet.exe" test DeckFlow.sln -c Release --nologo`

    If build exits non-zero OR test reports any non-baseline result (Failed != 0, or Passed != 497, or Total != 500, or Skipped != 3), STOP and surface the diff to user — do NOT commit. Task 1 should be byte-symmetric in IL (it's a `//` comment), so any regression here means something unintended was changed; investigate before proceeding.
  </action>
  <verify>
    <automated>"/mnt/c/Program Files/dotnet/dotnet.exe" build DeckFlow.sln -c Release --nologo ; "/mnt/c/Program Files/dotnet/dotnet.exe" test DeckFlow.sln -c Release --nologo</automated>
  </verify>
  <acceptance_criteria>
    - `"/mnt/c/Program Files/dotnet/dotnet.exe" build DeckFlow.sln -c Release --nologo` exits 0; warning count <= post-Phase-999.6 baseline (no NEW warnings).
    - `"/mnt/c/Program Files/dotnet/dotnet.exe" test DeckFlow.sln -c Release --nologo` final report contains `Failed: 0` AND `Passed: 497` AND `Skipped: 3` AND `Total: 500` (or per-project: Web `Failed:0,Passed:440,Skipped:3,Total:443` + Core `Failed:0,Passed:57,Total:57`).
    - Test output contains no NEW failure not already in the 999.6 baseline (per `.planning/phases/999.6-v1-3-ship-gate-test-residual-cleanup/999.6-UAT.md`).
  </acceptance_criteria>
  <done>Build + test gate green at 999.6 PHASE EXIT GATE baseline; no regressions from Task 1 comment insertion or Task 2 docs edit.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none for Task 2) | `.planning/phases/.../999.5-UAT.md` — planning artifact, no runtime path. |
| code path (Task 1) | DeckController.cs DeckAnalysisDownload — HTTP POST endpoint exists, but the edit is a `// ` comment inside a `try` block with zero IL impact. The endpoint already enforces `[ValidateAntiForgeryToken]` (CSRF guard) and goes through `SameOriginRequestValidator` per project CLAUDE.md architecture. No new surface added. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-999.7-04a | Tampering | DeckController.cs DeckAnalysisDownload | accept | `//` comment line; IL byte-symmetric pre/post. Build + test gate (Task 3) catches any accidental code touch. Handler-anchored grep gate (awk-bounded) confirms placement inside the correct method. |
| T-999.7-04b | Tampering | .planning/phases/999.5-UAT.md | accept | Planning artifact; no runtime read. |
| T-999.7-04c | Tampering | Build pipeline (formatter spillover risk on DeckController.cs — large file ~600+ lines) | mitigate | `git diff --numstat` gate: `1 0` for DeckController.cs (one line added, zero removed). Anti-format rule from CLAUDE.md (no Format Document, no inline-attribute conversion, no `{ get; init; }` → `{ get; }` collapse) enforced via diff review. |

Severity: **info** — comment insertion + planning-doc-status flip. ASVS L1 not applicable. Build + test gate provides regression safety net.
</threat_model>

<verification>
1. Task 1 acceptance: F-02 comment present; D-10 / D-11 existing comments preserved; numstat `1  0`; awk-bounded grep gate returns `1`.
2. Task 2 acceptance: `status: superseded-by-999.6` exactly once; body untouched.
3. Task 3 acceptance: Build + test gate at 999.6 baseline.
4. `git diff --stat` shows exactly 2 files modified across this plan.
</verification>

<success_criteria>
SC4, SC5, SC6, SC7 from ROADMAP success criteria satisfied:
- SC4: DeckController.cs:~514 carries F-02 asymmetry comment citing Phase 999.3 D-10 (placement confirmed by awk-bounded grep gate anchored to the DeckAnalysisDownload handler).
- SC5: 999.5-UAT.md frontmatter `status: testing` → `status: superseded-by-999.6`; body untouched.
- SC6: Release build exits 0; warning count <= post-999.6 baseline.
- SC7: Test suite reports Failed:0 Passed:497 Skipped:3 Total:500.
</success_criteria>

<output>
Create `.planning/phases/999.7-v1-3-audit-cleanup/999.7-04-SUMMARY.md` when done.
</output>
