## Phase 999.5-03 Summary

- Files modified: `.planning/debug/archive/harvest-killed-by-suggestion.md` (renamed from `.planning/debug/`), `.planning/debug/archive/harvest-killed-by-suggestion-rootcause.md` (renamed), `.planning/STATE.md` (Deferred Items row removed), `DeckFlow.Web.Tests/CategorySuggestionServiceTests.cs` (regression fact added), `DeckFlow.Web.Tests/CommanderCategoryServiceTests.cs` (NEW FILE, 105 lines), `.planning/phases/999.5-v1-3-backlog-catch-up-test-hardening/999.5-03-SUMMARY.md`.
- Scope: P03 — Archive the parked `harvest-killed-by-suggestion` H1 hypothesis logs (gate-starvation fix already shipped 2026-05-03 in commits to `CategorySuggestionService.cs` + `CommanderCategoryService.cs`), prune the STATE Deferred Items row, lock the contract with one named regression fact per service.
- Decisions delivered: D-14 (read-only fix verification), D-15 (history-preserving archive move via `git mv`), D-16 (STATE row removal), D-17 (2 regression facts — 1 in existing CategorySuggestionServiceTests, 1 in new CommanderCategoryServiceTests). Cross-cutting D-18/D-19/D-20/D-21/D-22/D-23 honored.
- D-14 verification grep (verbatim, 2026-05-21):
  ```
  CategorySuggestionService.cs:67    private readonly IArchidektCacheJobService _harvestJobService;
  CategorySuggestionService.cs:78        IArchidektCacheJobService harvestJobService,
  CategorySuggestionService.cs:131       var harvestActive = runCachedPath && _harvestJobService.GetActiveJob() is not null;
  CommanderCategoryService.cs:43     private readonly IArchidektCacheJobService _harvestJobService;
  CommanderCategoryService.cs:51         IArchidektCacheJobService harvestJobService,
  CommanderCategoryService.cs:69         var sweepPerformed = _harvestJobService.GetActiveJob() is null;
  ```
  Contract present at the exact CONTEXT-canonical line numbers (no drift). NO source-code change required by P03 — the fix shipped 2026-05-03.
- D-15 archive move: `git mv` (NOT cp + rm) used to preserve history. `.planning/debug/archive/` created via `mkdir -p`. Git status reports both moves as `R` (rename, 100% similarity).
- D-16 STATE row removal: surgically deleted the single line `| debug | harvest-killed-by-suggestion (H1 hypothesis parked in .planning/debug/) | deferred from v1.3 — root-cause work is gating, not parallel-shippable |` from `.planning/STATE.md` §Deferred Items table. Sibling rows (Gemini paste-limit, v1.1 phase-dir archive) preserved byte-identical.
- D-17 regression facts:
  - `CategorySuggestionServiceTests.SuggestAsync_SkipsCacheSweep_WhenHarvestActive` — wires the existing happy-path request fixture verbatim, substitutes the sibling-local `FakeActiveJobService` for the `IArchidektCacheJobService` slot, asserts `Assert.Equal(0, store.RunCacheSweepCalls)` (vs the happy-path's `Equal(1, ...)`). Same code path, opposite assertion — locks the "harvest active → skip sweep" contract.
  - `CommanderCategoryServiceTests.LookupAsync_SkipsCacheSweep_WhenHarvestActive` — NEW FILE (flat under `DeckFlow.Web.Tests/`, file-per-type per TESTING.md). 105 lines including sibling-local `FakeCategoryKnowledgeStore` + `FakeActiveJobService`. Asserts `Assert.Equal(0, store.RunCacheSweepCalls)` after `LookupAsync("Atraxa, Praetors' Voice", ...)`.
- Notable discovery — CONTEXT vs reality drift: `CommanderCategoryService` ctor is SIMPLER than CONTEXT P03 §canonical_refs speculated. Actual deps: `ICategoryKnowledgeStore`, `IArchidektCacheJobService`, `ILogger<CommanderCategoryService>` (3 total). No `IScryfallCommanderSearchService`, no `IArchidektDeckImporter`, no `IMemoryCache`, no `IParser`. The CONTEXT note about "hand-rolled Fake* doubles for HTTP-facing collaborators" became a no-op — none needed. Plan executed correctly because Codex was instructed to "read CommanderCategoryService.cs ctor first" — the actual ctor shape drove the harness wiring.
- `FakeActiveJobService` shape (shared across both files, sibling-local per TESTING.md Pattern 3 — NOT promoted to a shared TestDoubles directory):
  - `GetActiveJob()` → non-null `ArchidektCacheJobStatus` with `ArchidektCacheJobState.Running`
  - `EnqueueAsync(...)` → throws `NotSupportedException("Not used in these tests.")`
  - `GetJob(Guid)` → `null`
  - `CancelActiveAsync(...)` → `Task.FromResult(false)`
- `FakeCategoryKnowledgeStore` in `CommanderCategoryServiceTests.cs`: minimally implements only the members the LookupAsync skip-sweep branch actually reaches (`GetProcessedDeckCountAsync`, `GetCategoryRowsForCommanderAsync`, `GetCommanderDeckCountAsync`, `RunCacheSweepAsync` counter). Unreached `ICategoryKnowledgeStore` members throw `NotSupportedException("Not reached via skip-sweep branch.")` per CONTEXT D-17 short-circuit acknowledgment.
- Build gate (D-21): green throughout — `0 Warning(s) 0 Error(s)`.
- Test gate (D-22): focused gate green per task:
  - T1 sanity (doc-only, no source change): full suite `441/431/9/3` → `443/431/9/3` (delta +2 facts from P02 close, 9 pre-existing residual outside 999.5 scope)
  - T2 focused: `Passed 7, Failed 0, Skipped 0` filter: `FullyQualifiedName~CategorySuggestionServiceTests|FullyQualifiedName~CommanderCategoryServiceTests`
- D-23 binding: every commit body includes the focused-test triplet.
- Execution deviation note: Codex sandbox was read-only on the git index (could not run `git mv`). Orchestrator performed the archive move and STATE.md edit directly; Codex retained D-14 verification + D-17 test-fact authorship. Same hybrid pattern as P01 wave 1.
- 2-commit hash sequence (P03 per D-19):
  1. `c330ea7` `docs(999.5): archive harvest-killed-by-suggestion debug + prune STATE row (D-14/D-15/D-16)`
  2. `6c95c50` `test(999.5): lock harvest-active skip-sweep contract in suggestion services (D-17)`
  3. (this commit) `docs(999.5): summary for plan 03 harvest archival`
- must_haves status (per 999.5-03-PLAN.md frontmatter):
  - "Debug docs archived under .planning/debug/archive/ with git history preserved": **MET** — both files `R 100%` in git status.
  - "STATE Deferred Items 'harvest-killed-by-suggestion' row removed": **MET**.
  - "Regression facts assert RunCacheSweepCalls == 0 when GetActiveJob() returns non-null": **MET** — both facts pass focused gate.
- Closure commit hash: pending — fill SHA after commit lands.
- Next step: P04 dispatch (D-24..D-28 ChatGPT `<result>` strip) → phase verification.
