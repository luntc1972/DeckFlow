---
phase: 32-expert-context-selection
plan: 02
type: execute
wave: 2
depends_on: ["32-01"]
files_modified:
  - DeckFlow.Web/Models/DeckAnalysisRequest.cs
  - DeckFlow.Web/Services/PacketArtifactStore.cs
  - DeckFlow.Web/Services/DeckAnalysisPacketService.cs
  - DeckFlow.Web/Services/PacketSessionCache.cs
  - DeckFlow.Web/Services/ContentKbRelevanceService.cs
  - DeckFlow.Web/Controllers/DeckController.cs
  - DeckFlow.Web.Tests/PacketArtifactStoreTests.cs
  - DeckFlow.Web.Tests/DeckAnalysisRequestTests.cs
  - DeckFlow.Web.Tests/ContentKbRelevanceServiceTests.cs
  - DeckFlow.Web.Tests/DeckAnalysisPacketServiceTests.cs
autonomous: true
requirements: [SEL-04, SEL-02]
must_haves:
  truths:
    - "DeckAnalysisRequest carries PinnedVideoIds, FollowedCreators, and ExpertSelectionJson with null-safe setters"
    - "BuildZip writes 33-expert-selection.json only when selectionJson is provided"
    - "LoadFromZip restores PinnedVideoIds + FollowedCreators from 33-expert-selection.json using camelCase options"
    - "A corrupt 33-expert-selection.json degrades to empty selection without throwing"
    - "33-expert-selection.json is in PacketAllowedNames in the same commit BuildZip can emit it"
    - "DeckAnalysisPacketService calls GetMergedClipsAsync with the request selection when not replaying"
    - "Both DeckController.DeckAnalysisDownload BuildZip call sites (cache-hit + cache-miss) pass selectionJson"
    - "The DeckAnalysis packet cache key includes the normalized selection (pins + follows): the same deck with different pins produces a different cache key and never reuses stale expert clips. Pin ids are normalized case-SENSITIVELY (ordinal); followed creators are normalized case-INSENSITIVELY (ordinal-ignore-case) — so two case-only-different pins ('abc' vs 'ABC') produce DIFFERENT keys, matching the case-sensitive pin selection/merge/title-resolution path, while case-only-different follows produce the SAME key"
    - "Replay-first guard: a replayed clip set short-circuits selection re-merge"
    - "DeckAnalysisPacketResult.ResolvedPinTitles maps each pinned video id to its title; empty when no pins"
    - "Every serialize/deserialize of ExpertSelectionState uses one shared camelCase, case-insensitive JsonSerializerOptions"
  artifacts:
    - path: "DeckFlow.Web/Models/DeckAnalysisRequest.cs"
      provides: "PinnedVideoIds, FollowedCreators, ExpertSelectionJson"
      contains: "PinnedVideoIds"
    - path: "DeckFlow.Web/Services/PacketArtifactStore.cs"
      provides: "33-expert-selection.json allowlist + writer + reader + ExpertSelectionState record + ExpertSelectionJsonOptions"
      contains: "33-expert-selection.json"
    - path: "DeckFlow.Web/Services/DeckAnalysisPacketService.cs"
      provides: "GetMergedClipsAsync wiring + selection round-trip into zip + ResolvedPinTitles on the result + selection folded into BuildDeckAnalysisCacheInputs (pins ordinal, follows ordinal-ignore-case)"
      contains: "GetMergedClipsAsync"
    - path: "DeckFlow.Web/Services/PacketSessionCache.cs"
      provides: "DeckAnalysisCacheInputs record extended with NormalizedPinnedVideoIds + NormalizedFollowedCreators"
      contains: "NormalizedPinnedVideoIds"
    - path: "DeckFlow.Web/Controllers/DeckController.cs"
      provides: "selectionJson threaded into BOTH DeckAnalysisDownload BuildZip call sites"
      contains: "selectionJson"
    - path: "DeckFlow.Web/Services/ContentKbRelevanceService.cs"
      provides: "ResolvePinTitlesAsync helper (id -> title) reusing the injected store"
      contains: "ResolvePinTitlesAsync"
    - path: "DeckFlow.Web.Tests/PacketArtifactStoreTests.cs"
      provides: "selection round-trip (camelCase raw JSON) + corrupt-json degradation tests"
      exports: ["BuildZip_with_selection_json_round_trips_into_request", "LoadFromZip_with_corrupt_selection_json_degrades_to_empty_selection", "LoadFromZip_with_camelCase_selection_json_binds_properties"]
  key_links:
    - from: "DeckFlow.Web/Services/DeckAnalysisPacketService.cs"
      to: "ContentKbRelevanceService.GetMergedClipsAsync"
      via: "selection extracted from request"
      pattern: "GetMergedClipsAsync"
    - from: "DeckFlow.Web/Services/DeckAnalysisPacketService.cs"
      to: "DeckFlow.Web/Services/PacketSessionCache.cs"
      via: "BuildDeckAnalysisCacheInputs folds normalized pins+follows into the cache key (pins ordinal, follows ignore-case)"
      pattern: "NormalizedPinnedVideoIds"
    - from: "DeckFlow.Web/Controllers/DeckController.cs"
      to: "PacketArtifactStore.BuildZip"
      via: "selectionJson passed on both cache-hit and cache-miss branches"
      pattern: "selectionJson:"
    - from: "DeckFlow.Web/Services/DeckAnalysisPacketService.cs"
      to: "ContentKbRelevanceService.ResolvePinTitlesAsync"
      via: "pinned ids resolved to titles, stored on DeckAnalysisPacketResult.ResolvedPinTitles"
      pattern: "ResolvedPinTitles"
    - from: "DeckFlow.Web/Services/PacketArtifactStore.cs"
      to: "33-expert-selection.json"
      via: "PacketAllowedNames + BuildZip + LoadFromZip"
      pattern: "33-expert-selection\\.json"
---

<objective>
Round-trip the expert selection through the request DTO, the packet zip, and the packet service. Add `PinnedVideoIds`/`FollowedCreators`/`ExpertSelectionJson` to `DeckAnalysisRequest`; add `33-expert-selection.json` to the zip allowlist + writer + reader (with a dedicated camelCase `JsonSerializerOptions`); thread the selection through BOTH `DeckController.DeckAnalysisDownload` BuildZip call sites; and switch the non-replay clip source to `GetMergedClipsAsync` (from Plan 01).

Purpose: SEL-04 (selection persisted in zip, re-upload restores) and the server side of SEL-02 (the merged set is what reaches prompt + zip + panel — preserving the Phase 30 prompt==zip==panel invariant). The replay-first guard must keep replayed clips byte-stable.

Output: three additive request fields, the zip same-commit allowlist + writer + reader + `ExpertSelectionState` record + shared `ExpertSelectionJsonOptions`, the BuildZip call-site threading in DeckController (both branches), the `GetMergedClipsAsync` call-site swap with selection threading, `ResolvePinTitlesAsync` + `DeckAnalysisPacketResult.ResolvedPinTitles` (so Plan 03 server-renders replay chips with real titles), and zip round-trip + corrupt-json tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-expert-context-selection-runs-after-phase-30-before-phase-31/32-CONTEXT.md
@.planning/phases/32-expert-context-selection-runs-after-phase-30-before-phase-31/32-RESEARCH.md
@.planning/phases/32-expert-context-selection-runs-after-phase-30-before-phase-31/32-PATTERNS.md
@.planning/phases/32-expert-context-selection-runs-after-phase-30-before-phase-31/32-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 (already shipped in this phase) + live code confirmed 2026-06-07 -->

ContentKbRelevanceService (Plan 01 added):
  - GetMergedClipsAsync(ExpertSelection selection, string? commanderName, string? bracket, IReadOnlySet<string>? deckArchetypes = null, int maxRenderedChars = DefaultMaxRenderedChars, CancellationToken ct = default)
  - internal sealed record ExpertSelection(IReadOnlyList<string> PinnedVideoIds, IReadOnlySet<string> FollowedCreators)
  - ContentKbExcerpt now has ClipOrigin { get; init; } = "auto"
  - CASE SEMANTICS (END-TO-END — DO NOT VIOLATE): pin ids match against `row.YoutubeVideoId ?? row.RssGuid` ORDINALLY (case-sensitive — Plan 01 uses `pinIds.Contains(...)` with no ignore-case comparer; YouTube video ids are genuinely case-sensitive). Followed creators match against `row.Source` via an OrdinalIgnoreCase set. Plan 02's cache-key normalization (HIGH-D) MUST match this: pins normalized ordinally (NO lowercasing), follows normalized ignore-case (lowercased).

PacketArtifactStore (DeckFlow.Web/Services/PacketArtifactStore.cs):
  - PacketAllowedNames HashSet (lines 27-42) — same-commit gate; ReadEntries throws on unlisted names
  - BuildZip signature (line 94) — last existing optional param is expertContextJson; "32-expert-context.json" already wired
  - LoadFromZip (lines 205-314) — entries.TryGetValue("32-expert-context.json"...) at ~line 216; request.ExpertContextJson assignment ~line 226
  - TryDeserializeFetchedEntries (lines 518-533) — canonical corrupt-json-degrades pattern

DeckController.DeckAnalysisDownload (DeckFlow.Web/Controllers/DeckController.cs):
  - [HttpPost("/deck-analysis/download")] action lines 513-577 — THIS is where BuildZip is called, NOT the packet service
  - CACHE-HIT branch: `PacketArtifactStore.BuildZip(...)` at lines 533-548 (cachedResult / cachedBytes)
  - CACHE-MISS branch: `PacketArtifactStore.BuildZip(...)` at lines 559-574 (result / bytes)
  - BOTH call sites currently pass `expertContextJson:` as the final named arg — the selectionJson arg must be added to BOTH
  - request.ExpertContextJson is read into expertContextJson at the call sites (~lines 544/570); mirror that for selection

DeckAnalysisPacketService (DeckFlow.Web/Services/DeckAnalysisPacketService.cs):
  - IndentedJsonSerializerOptions static field: lines 66-69 (WriteIndented only — NOT camelCase; do not reuse for selection)
  - replayedExpertContextJson guard: lines 534-547 (deserialize at line 541 uses NO options — fine for ContentKbExcerpt which is PascalCase JSON)
  - call site that builds kbExcerpts: search for `GetRelevantClipsAsync(` in BuildAsync — swap to GetMergedClipsAsync
  - DeckAnalysisPacketResult record: lines 43-57 (sealed record, positional params; add a final optional `IReadOnlyDictionary<string, string>? ResolvedPinTitles = null`)
  - new DeckAnalysisPacketResult(...) construction site (search for `new DeckAnalysisPacketResult(`)
  - _contentKbRelevanceService field is nullable (line 78); ResolvePinTitlesAsync only called when it is non-null
  - NOTE: BuildAsync does NOT call BuildZip — the zip is built by DeckController.DeckAnalysisDownload. The service threads request.ExpertSelectionJson so the controller can pass it.

DeckAnalysisCacheInputs + BuildDeckAnalysisCacheInputs (HIGH-B — cache key currently EXCLUDES selection):
  - DeckAnalysisCacheInputs record: DeckFlow.Web/Services/PacketSessionCache.cs:180 — positional record `(string Commander, string NormalizedDeckSource, bool IncludeCardVersions, bool IncludeCandidateReferencesInAnalysis, string TargetAiPlatformKey, IReadOnlyList<string> SelectedQuestionIds)`; selection is NOT a field, so a cache hit returns prior clips regardless of pins/follows
  - BuildDeckAnalysisCacheInputs: DeckFlow.Web/Services/DeckAnalysisPacketService.cs:280-294 — the SOLE constructor of DeckAnalysisCacheInputs (used by BOTH the write site ~line 369 and TryComputeCacheKeyAsync read site ~line 723, guaranteeing write↔read key parity). SelectedQuestionIds is already normalized via `.OrderBy(id, StringComparer.Ordinal).ToArray()` — copy that normalization shape for the new selection fields
  - WHY this matters: GetMergedClipsAsync (Plan 01) regenerates ExpertContextClips from the selection, but if the cache key ignores selection, BuildAsync short-circuits to a cached result built under a different (or empty) selection — stale clips reach prompt+zip+panel, bypassing GetMergedClipsAsync entirely

DeckAnalysisRequest (DeckFlow.Web/Models/DeckAnalysisRequest.cs):
  - List-field backing pattern (SelectedAnalysisQuestions lines 156-161): `set => _field = value ?? [];`
  - ExpertContextJson property (~line 131) — insertion anchor for new fields
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add PinnedVideoIds, FollowedCreators, ExpertSelectionJson to DeckAnalysisRequest</name>
  <files>DeckFlow.Web/Models/DeckAnalysisRequest.cs</files>
  <read_first>
    - DeckFlow.Web/Models/DeckAnalysisRequest.cs (FULL — backing-field + null-safe setter pattern, SelectedAnalysisQuestions lines 156-161, ExpertContextJson ~line 131)
    - .planning/phases/32-expert-context-selection-runs-after-phase-30-before-phase-31/32-PATTERNS.md (DeckAnalysisRequest.cs section)
  </read_first>
  <action>
    After the `ExpertContextJson` property (~line 131), add three backing fields and three properties, following the exact null-safe setter convention:
      - `private List<string> _pinnedVideoIds = [];` + `public List<string> PinnedVideoIds { get => _pinnedVideoIds; set => _pinnedVideoIds = value ?? []; }`
      - `private List<string> _followedCreators = [];` + `public List<string> FollowedCreators { get => _followedCreators; set => _followedCreators = value ?? []; }`
      - `private string _expertSelectionJson = string.Empty;` + `public string ExpertSelectionJson { get => _expertSelectionJson; set => _expertSelectionJson = value ?? string.Empty; }`
    Each property gets an XML `<summary>` (PinnedVideoIds: one-shot, cleared after use; FollowedCreators: sticky; ExpertSelectionJson: serialized 33-expert-selection.json round-tripped through the zip).
    `List<string>` is the binding type so ASP.NET Core MVC binds repeated same-name hidden fields natively (same as SelectedAnalysisQuestions). Touch only the lines that need touching; LF endings.
  </action>
  <verify>
    <automated>"/mnt/c/Program Files/dotnet/dotnet.exe" build DeckFlow.Web/DeckFlow.Web.csproj 2>&1 | grep -E "Build succeeded|error" | head</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "public List<string> PinnedVideoIds" DeckFlow.Web/Models/DeckAnalysisRequest.cs` matches one line
    - `grep -n "public List<string> FollowedCreators" DeckFlow.Web/Models/DeckAnalysisRequest.cs` matches one line
    - `grep -n "public string ExpertSelectionJson" DeckFlow.Web/Models/DeckAnalysisRequest.cs` matches one line
    - All three setters use `value ?? []` / `value ?? string.Empty` (null-safe, matching existing convention)
    - `dotnet build DeckFlow.Web` reports Build succeeded
  </acceptance_criteria>
  <done>Request DTO carries all three selection fields with null-safe setters; Web builds.</done>
</task>

<task type="auto">
  <name>Task 2: Wire 33-expert-selection.json into PacketArtifactStore (allowlist + camelCase writer + reader), thread BOTH DeckController BuildZip sites, and swap to GetMergedClipsAsync</name>
  <files>DeckFlow.Web/Services/PacketArtifactStore.cs, DeckFlow.Web/Services/DeckAnalysisPacketService.cs, DeckFlow.Web/Services/PacketSessionCache.cs, DeckFlow.Web/Controllers/DeckController.cs, DeckFlow.Web/Services/ContentKbRelevanceService.cs</files>
  <read_first>
    - DeckFlow.Web/Services/PacketArtifactStore.cs (FULL — PacketAllowedNames 27-42, BuildZip 94-129, LoadFromZip 205-314, TryDeserializeFetchedEntries 518-533, ReadEntries throw behavior 601-605)
    - DeckFlow.Web/Services/DeckAnalysisPacketService.cs (FULL — IndentedJsonSerializerOptions 66-69, BuildDeckAnalysisCacheInputs 280-294 [HIGH-B: the cache-input builder], replay guard 534-547, GetRelevantClipsAsync call site in BuildAsync, DeckAnalysisPacketResult record 43-57, the `new DeckAnalysisPacketResult(...)` site)
    - DeckFlow.Web/Services/PacketSessionCache.cs (DeckAnalysisCacheInputs positional record at line 180 — HIGH-B: extend with the two normalized selection fields)
    - DeckFlow.Web/Controllers/DeckController.cs (FULL — DeckAnalysisDownload action 513-577 incl. BOTH BuildZip call sites: cache-hit 533-548 and cache-miss 559-574)
    - DeckFlow.Web/Services/ContentKbRelevanceService.cs (the injected IContentSiteIndexStore field + GetPublishedRowsAsync usage — reuse for ResolvePinTitlesAsync)
    - .planning/phases/32-expert-context-selection-runs-after-phase-30-before-phase-31/32-RESEARCH.md (Pattern 2, Pitfall 3, Code Examples: replay-first guard extension, PacketAllowedNames addition)
    - .planning/phases/32-expert-context-selection-runs-after-phase-30-before-phase-31/32-PATTERNS.md (PacketArtifactStore.cs section)
  </read_first>
  <action>
    SAME-COMMIT RULE (critical): all of the following land in one commit so `ReadEntries` never rejects a name it just emitted.

    DEDICATED JSON OPTIONS (HIGH-4 — confirmed: the existing replay deserialize at DeckAnalysisPacketService.cs:541 uses NO options, and IndentedJsonSerializerOptions sets only WriteIndented, NOT camelCase. A camelCase selection payload like `{"pinnedVideoIds":["abc"],"followedCreators":["X"]}` will NOT bind to PascalCase `PinnedVideoIds`/`FollowedCreators` under default options — it silently restores EMPTY selections). Therefore:
      - In PacketArtifactStore.cs add a single `internal static readonly JsonSerializerOptions ExpertSelectionJsonOptions = new() { PropertyNamingPolicy = JsonNamingPolicy.CamelCase, PropertyNameCaseInsensitive = true };` with an XML `<summary>` and a `// Why:` comment ("camelCase + case-insensitive so the zip JSON `pinnedVideoIds` binds to PinnedVideoIds on round-trip — default options would silently drop them").
      - EVERY `JsonSerializer.Serialize`/`Deserialize` of `ExpertSelectionState` (in LoadFromZip here, in BuildZip's writer if it serializes, AND in DeckAnalysisPacketService's replay-guard restore + the controller's pre-BuildZip serialize) MUST pass `PacketArtifactStore.ExpertSelectionJsonOptions`. Do not call Serialize/Deserialize<ExpertSelectionState> with default options anywhere.

    In PacketArtifactStore.cs:
      - Add `"33-expert-selection.json",` to the `PacketAllowedNames` HashSet (next to `"32-expert-context.json"`).
      - Extend `BuildZip` with a NEW last optional parameter `string? selectionJson = null` (after the existing `expertContextJson` param). Add `("33-expert-selection.json", "EXPERT SELECTION JSON", selectionJson)` to the same `NormalizeSections`/`promptSections` list that emits 32-expert-context.json — so the entry is written only when `selectionJson` is non-null/non-empty (match the existing 32-expert-context.json conditional behavior exactly). The caller serializes ExpertSelectionState with ExpertSelectionJsonOptions before passing it.
      - In `LoadFromZip`, after the `entries.TryGetValue("32-expert-context.json"...)` block, add `entries.TryGetValue("33-expert-selection.json", out var selectionJson);`. After `request.ExpertContextJson = expertContextJson ?? string.Empty;`, add `request.ExpertSelectionJson = selectionJson ?? string.Empty;` then deserialize with graceful degradation: if `selectionJson` is non-blank, `try { var sel = JsonSerializer.Deserialize<ExpertSelectionState>(selectionJson, ExpertSelectionJsonOptions); if (sel?.PinnedVideoIds?.Count > 0) request.PinnedVideoIds = [..sel.PinnedVideoIds]; if (sel?.FollowedCreators?.Count > 0) request.FollowedCreators = [..sel.FollowedCreators]; } catch (JsonException) { /* degrade to empty — no throw */ }`.
      - Add `internal sealed record ExpertSelectionState { public IReadOnlyList<string> PinnedVideoIds { get; init; } = []; public IReadOnlyList<string> FollowedCreators { get; init; } = []; }` as a TOP-LEVEL record at the bottom of PacketArtifactStore.cs (HIGH-C: the `PacketArtifactStore` static class closes around line 674; place this record AFTER that closing brace, alongside the existing top-level records `RestoredComparisonArtifacts`/`RestoredCedhMetaGapArtifacts` — it is NOT a nested type). Because it is top-level in namespace `DeckFlow.Web.Services`, ALL references use the bare type name `ExpertSelectionState` (e.g. `new ExpertSelectionState { ... }`, `JsonSerializer.Deserialize<ExpertSelectionState>(...)`) — never `PacketArtifactStore.ExpertSelectionState` (that member-access syntax would NOT compile against a top-level record). Use `{ get; init; }` per the round-tripped-record constraint; include an XML `<summary>` and a `// Why:` comment noting System.Text.Json drops get-only props. Serialization always uses ExpertSelectionJsonOptions (camelCase).

    In DeckAnalysisPacketService.cs:
      - At the `kbExcerpts` build site in BuildAsync, replace the `GetRelevantClipsAsync(...)` call with `GetMergedClipsAsync(...)`, constructing the `ExpertSelection` from `request.PinnedVideoIds` (as IReadOnlyList<string>) and `request.FollowedCreators` (as `new HashSet<string>(request.FollowedCreators, StringComparer.OrdinalIgnoreCase)`), passing the same commanderName/bracket/archetypes/maxRenderedChars args the existing call used. The replay-first guard (`replayedExpertContextJson is null`) is preserved — replayed clips short-circuit re-merge.
      - Extend the replay guard block (lines 534-547) to also restore selection state: mirror the ExpertContextJson pattern for `request.ExpertSelectionJson` — if non-blank, deserialize `ExpertSelectionState` (with `PacketArtifactStore.ExpertSelectionJsonOptions`) in a try/catch and populate `request.PinnedVideoIds`/`request.FollowedCreators` so the chip area re-renders on replay (Pitfall 3). Degrade to empty on JsonException.
      - In BuildAsync, serialize the request's selection into `request.ExpertSelectionJson` so the controller can pass it to BuildZip and prompt==zip==panel stays coherent: when `request.PinnedVideoIds.Count > 0 || request.FollowedCreators.Count > 0`, set `request.ExpertSelectionJson = JsonSerializer.Serialize(new ExpertSelectionState { PinnedVideoIds = [..request.PinnedVideoIds], FollowedCreators = [..request.FollowedCreators] }, PacketArtifactStore.ExpertSelectionJsonOptions);` (bare `ExpertSelectionState` — it is a top-level type; only `ExpertSelectionJsonOptions` is a static member of `PacketArtifactStore`) (otherwise leave it as whatever replay restored / empty). This is the single source of the selectionJson string the controller forwards.

      - CACHE-KEY PARITY (HIGH-B — REQUIRED, option (a): fold selection INTO the cache key): the DeckAnalysis packet cache key is derived from `DeckAnalysisCacheInputs` via `BuildDeckAnalysisCacheInputs` (lines 280-294), which today excludes the selection. Without this fix, BuildAsync hits a cached result built under a DIFFERENT (or empty) selection and returns its stale clips, bypassing GetMergedClipsAsync. Fix in TWO coordinated edits:
        1. In PacketSessionCache.cs, extend the `DeckAnalysisCacheInputs` positional record (line 180) with TWO new trailing parameters AFTER `IReadOnlyList<string> SelectedQuestionIds`: `IReadOnlyList<string> NormalizedPinnedVideoIds` and `IReadOnlyList<string> NormalizedFollowedCreators`. (Records compare by value, so adding fields changes the derived key automatically — confirm how the key string is built from this record and that the new fields participate; if the key is composed field-by-field, append both new field sequences to the key composition in the same deterministic order.)
        2. In BuildDeckAnalysisCacheInputs (lines 280-294), populate the two new args with NORMALIZED selection, mirroring the existing `SelectedQuestionIds` normalization shape (Trim → drop empties → Distinct → OrderBy Ordinal → ToArray). CRITICAL CASE SEMANTICS (HIGH-D — pins and follows differ; do NOT lowercase pins):
            - pins (case-SENSITIVE / ordinal, matching the case-sensitive pin selection in Plan 01 and ResolvePinTitlesAsync below): `(request.PinnedVideoIds ?? new List<string>()).Select(v => v.Trim()).Where(v => v.Length > 0).Distinct(StringComparer.Ordinal).OrderBy(v => v, StringComparer.Ordinal).ToArray()` — NO `.ToLowerInvariant()`. YouTube video ids are genuinely case-sensitive, and the merge path (`pinIds.Contains(row.YoutubeVideoId ?? row.RssGuid)`) is ordinal, so two case-only-different pins ('abc' vs 'ABC') MUST fork the key (they resolve to different clips/titles).
            - follows (case-INSENSITIVE / ignore-case, matching the OrdinalIgnoreCase follow set in Plan 01): `(request.FollowedCreators ?? new List<string>()).Select(v => v.Trim().ToLowerInvariant()).Where(v => v.Length > 0).Distinct(StringComparer.Ordinal).OrderBy(v => v, StringComparer.Ordinal).ToArray()` — lowercased so case-only-different follows ('EDHRECast' vs 'edhrecast') collapse to the SAME key (they resolve to the same clips).
            Both are sort+distinct so order/dup differences in the form post do not fork the key. Because BuildDeckAnalysisCacheInputs is the SINGLE shared builder used by BOTH the write site (~line 369) and TryComputeCacheKeyAsync read site (~line 723), write↔read key parity is preserved automatically — do NOT compute the normalization inline anywhere else.
        Net effect: same deck + same selection (modulo pin order/dup and follow order/dup/case) → cache hit (clips byte-stable); same deck + different pins (including case-only-different pins) OR different follows → distinct key → BuildAsync re-runs and GetMergedClipsAsync regenerates the clips.

    In DeckController.cs — BOTH BuildZip call sites (HIGH-3; confirmed: BuildZip is invoked TWICE in DeckAnalysisDownload — cache-hit at lines 533-548 and cache-miss at lines 559-574 — NOT in the packet service):
      - CACHE-MISS branch (lines 559-574): after `var result = await _deckAnalysisPacketService.BuildAsync(...)`, the result/request now carries `request.ExpertSelectionJson` (set by BuildAsync). Add a new named argument `selectionJson: !string.IsNullOrWhiteSpace(request.ExpertSelectionJson) ? request.ExpertSelectionJson : null` to this `PacketArtifactStore.BuildZip(...)` call (after the existing `expertContextJson:` arg).
      - CACHE-HIT branch (lines 533-548): this branch does NOT call BuildAsync (it serves `cachedResult`), so `request.ExpertSelectionJson` is whatever the inbound form/replay populated. Add the same `selectionJson:` named argument computed from `request.ExpertSelectionJson` (fallback null when blank) so a re-download of a cached packet still embeds the selection the user submitted.
      - Add `DeckFlow.Web/Controllers/DeckController.cs` is already in files_modified. Do NOT modify any other action; touch only the two BuildZip calls in DeckAnalysisDownload.

    RESOLVED PIN TITLES (required so Plan 03 server-renders replay chips with real titles):
    In ContentKbRelevanceService.cs add `Task<IReadOnlyDictionary<string, string>> ResolvePinTitlesAsync(IReadOnlyList<string> videoIds, CancellationToken ct = default)` to BOTH the interface and the implementation. Implementation: if `videoIds` is empty return an empty dictionary; else `var rows = await _store.GetPublishedRowsAsync(ct);` and build a case-sensitive (ordinal) `Dictionary<string, string>` mapping each requested id (matched ORDINALLY against `row.YoutubeVideoId ?? row.RssGuid`) to `row.Title`, skipping ids with no matching row (the caller falls back to the id string). This ordinal match is intentional and consistent with the case-sensitive pin merge in Plan 01 and the ordinal pin normalization in the cache key (HIGH-D). Reuse the already-injected store field — do NOT add a new dependency. Include an XML `<summary>`. Do not touch GetMergedClipsAsync or the auto path.
    In DeckAnalysisPacketService.cs: (1) extend the `DeckAnalysisPacketResult` record (lines 43-57) with a final optional parameter `IReadOnlyDictionary<string, string>? ResolvedPinTitles = null`. (2) In BuildAsync, after the merged-clip build, when `_contentKbRelevanceService is not null` AND `request.PinnedVideoIds.Count > 0`, call `resolvedPinTitles = await _contentKbRelevanceService.ResolvePinTitlesAsync(request.PinnedVideoIds, cancellationToken)` into a local declared near `kbExcerpts`; default it to an empty dictionary otherwise. (3) Pass `ResolvedPinTitles: resolvedPinTitles` into the `new DeckAnalysisPacketResult(...)`. This populates the result on BOTH the fresh-analysis and upload/replay paths (replay restores PinnedVideoIds in the replay guard, so titles resolve there too).

    Touch only the lines that need touching; no formatter sweep; LF endings.
  </action>
  <verify>
    <automated>"/mnt/c/Program Files/dotnet/dotnet.exe" build DeckFlow.Web/DeckFlow.Web.csproj 2>&1 | grep -E "Build succeeded|error" | head</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "33-expert-selection.json" DeckFlow.Web/Services/PacketArtifactStore.cs` returns >= 3 (allowlist + BuildZip section + LoadFromZip TryGetValue)
    - `grep -n "ExpertSelectionJsonOptions" DeckFlow.Web/Services/PacketArtifactStore.cs` matches the field definition; `grep -c "ExpertSelectionJsonOptions" DeckFlow.Web` (Serialize/Deserialize sites) shows it used at every ExpertSelectionState serialize/deserialize (LoadFromZip + BuildAsync serialize + replay-guard restore)
    - `grep -n "PropertyNamingPolicy = JsonNamingPolicy.CamelCase" DeckFlow.Web/Services/PacketArtifactStore.cs` matches one line
    - NO `JsonSerializer.Deserialize<ExpertSelectionState>` or `Serialize(...ExpertSelectionState...)` call passes default options (every call includes ExpertSelectionJsonOptions)
    - `grep -n "internal sealed record ExpertSelectionState" DeckFlow.Web/Services/PacketArtifactStore.cs` matches one line
    - `grep -n "string? selectionJson = null" DeckFlow.Web/Services/PacketArtifactStore.cs` matches one line (BuildZip last optional param)
    - `grep -c "selectionJson:" DeckFlow.Web/Controllers/DeckController.cs` returns >= 2 (BOTH DeckAnalysisDownload BuildZip call sites pass the selection arg — HIGH-3)
    - `grep -n "GetMergedClipsAsync" DeckFlow.Web/Services/DeckAnalysisPacketService.cs` matches the call site (was GetRelevantClipsAsync)
    - `grep -n "catch (JsonException)" DeckFlow.Web/Services/PacketArtifactStore.cs` shows a catch around the selection deserialization (graceful degradation)
    - `grep -c "ResolvePinTitlesAsync" DeckFlow.Web/Services/ContentKbRelevanceService.cs` returns >= 2 (interface + impl)
    - `grep -n "ResolvedPinTitles" DeckFlow.Web/Services/DeckAnalysisPacketService.cs` matches BOTH the DeckAnalysisPacketResult record param and the `new DeckAnalysisPacketResult(...)` argument
    - `grep -n "ResolvePinTitlesAsync(request.PinnedVideoIds" DeckFlow.Web/Services/DeckAnalysisPacketService.cs` matches one call site
    - `grep -c "NormalizedPinnedVideoIds" DeckFlow.Web/Services/PacketSessionCache.cs` returns >= 1 (new field on DeckAnalysisCacheInputs)
    - `grep -c "NormalizedFollowedCreators" DeckFlow.Web/Services/PacketSessionCache.cs` returns >= 1
    - `grep -n "NormalizedPinnedVideoIds" DeckFlow.Web/Services/DeckAnalysisPacketService.cs` shows BuildDeckAnalysisCacheInputs populates the field (HIGH-B: selection is folded into the cache key, not ignored)
    - The pins normalization in BuildDeckAnalysisCacheInputs has NO `.ToLowerInvariant()` and uses ordinal Distinct/OrderBy (HIGH-D: pins are case-sensitive); the follows normalization DOES use `.ToLowerInvariant()` (follows are ignore-case) — both use the deterministic `.OrderBy(..., StringComparer.Ordinal)` + `.Distinct(...)` shape so the same selection re-hits
    - `dotnet build DeckFlow.Web` reports Build succeeded
  </acceptance_criteria>
  <done>33-expert-selection.json is allowlisted, written, and read in one commit with camelCase options; replay restores selection; both DeckController BuildZip sites embed the selection; non-replay path merges via GetMergedClipsAsync; the DeckAnalysis cache key folds the normalized selection (HIGH-B) with pins normalized case-sensitively and follows case-insensitively (HIGH-D) so a different selection — including a case-only-different pin — never reuses stale clips.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Zip selection round-trip (camelCase raw JSON) + corrupt-json degradation tests</name>
  <files>DeckFlow.Web.Tests/PacketArtifactStoreTests.cs, DeckFlow.Web.Tests/DeckAnalysisRequestTests.cs, DeckFlow.Web.Tests/ContentKbRelevanceServiceTests.cs, DeckFlow.Web.Tests/DeckAnalysisPacketServiceTests.cs</files>
  <read_first>
    - DeckFlow.Web.Tests/PacketArtifactStoreTests.cs (FULL — BuildZip_with_expert_context_round_trips_into_request lines 79-132 is the template)
    - DeckFlow.Web/Services/PacketArtifactStore.cs (Task 2 BuildZip/LoadFromZip/ExpertSelectionJsonOptions)
    - DeckFlow.Web.Tests/ContentKbRelevanceServiceTests.cs (FULL — TrackingContentSiteIndexStore fake + CreateRow helper; extend with a ResolvePinTitlesAsync fact)
    - DeckFlow.Web/Services/ContentKbRelevanceService.cs (Task 2 ResolvePinTitlesAsync impl)
    - DeckFlow.Web.Tests/DeckAnalysisPacketServiceTests.cs (FULL — `CreateService(...)` helper builds a real DeckAnalysisPacketService; existing facts call `service.TryComputeCacheKeyAsync(...)` patterns or build requests — extend with the HIGH-B cache-key parity fact)
    - DeckFlow.Web/Services/DeckAnalysisPacketService.cs (TryComputeCacheKeyAsync ~line 334 [public] + BuildDeckAnalysisCacheInputs 280-294 from Task 2)
    - .planning/phases/32-expert-context-selection-runs-after-phase-30-before-phase-31/32-PATTERNS.md (PacketArtifactStoreTests.cs section)
  </read_first>
  <behavior>
    - BuildZip_with_selection_json_round_trips_into_request: build a zip with selectionJson serialized via PacketArtifactStore.ExpertSelectionJsonOptions from pinnedVideoIds=["abc123"], followedCreators=["EDHRECast"]; LoadFromZip into a fresh request; assert PinnedVideoIds == ["abc123"], FollowedCreators == ["EDHRECast"], and ExpertSelectionJson contains "abc123".
    - LoadFromZip_with_camelCase_selection_json_binds_properties (HIGH-4 regression guard): build a zip whose 33-expert-selection.json entry is the EXACT raw string `{"pinnedVideoIds":["abc123"],"followedCreators":["EDHRECast"]}` (camelCase, hand-written — NOT serialized from the record); LoadFromZip; assert PinnedVideoIds == ["abc123"] and FollowedCreators == ["EDHRECast"]. This fails if the deserialize uses default (PascalCase) options.
    - LoadFromZip_with_corrupt_selection_json_degrades_to_empty_selection: a zip whose 33-expert-selection.json is malformed JSON loads with PinnedVideoIds.Count == 0 and FollowedCreators.Count == 0 and does NOT throw.
    - BuildZip_without_selection_omits_entry: when selectionJson is null, the zip contains no 33-expert-selection.json entry (and ReadEntries does not throw).
    - DeckAnalysisRequest binding (DeckAnalysisRequestTests): setting PinnedVideoIds/FollowedCreators to null yields empty lists (null-safe setter).
    - ResolvePinTitlesAsync_MapsKnownIdsToTitles_SkipsUnknown: given two published rows and a request for [knownId, missingId], the returned dictionary has knownId -> its Title and does NOT contain missingId; an empty id list returns an empty dictionary.
    - CacheKey_SameDeckDifferentPins_ProducesDifferentKey (HIGH-B + HIGH-D regression guard): two DeckAnalysisRequests identical in deck source + options + AI platform + question ids but differing ONLY in PinnedVideoIds (e.g. [] vs ["vid-a"], or ["vid-a"] vs ["vid-b"]) yield DIFFERENT cache keys from TryComputeCacheKeyAsync — proving a different selection cannot reuse stale expert clips. HIGH-D pin case-sensitivity guard: a request with `["abc"]` and one with `["ABC"]` (case-only difference) MUST yield DIFFERENT keys (pins are ordinal). Companion assertion: the SAME deck + SAME pins reordered/duplicated (e.g. ["vid-a","vid-b"] vs ["vid-b","vid-a","vid-a"]) yields the SAME key (order/dup-insensitive but case-SENSITIVE for pins). For FollowedCreators: differing follows fork the key, but case-only-different follows ("EDHRECast" vs "edhrecast") yield the SAME key (follows are ignore-case).
  </behavior>
  <action>
    In PacketArtifactStoreTests.cs, add the four `[Fact]` tests above (round-trip, raw-camelCase-binds, corrupt, omit), modeled byte-for-byte on the existing `BuildZip_with_expert_context_round_trips_into_request` test (same BuildZip arg shape, plus the new `selectionJson:` argument). For the camelCase test, inject the literal `{"pinnedVideoIds":["abc123"],"followedCreators":["EDHRECast"]}` as the zip entry (write a valid zip then overwrite/insert that exact 33-expert-selection.json content) so the test proves the options bind real-world camelCase JSON. For the corrupt test, inject a malformed `33-expert-selection.json` and assert empty + no throw. For the omit test, build with `selectionJson: null` and assert the entry is absent.

    Create DeckFlow.Web.Tests/DeckAnalysisRequestTests.cs (or extend if it exists — check first) with a `[Fact]` asserting `new DeckAnalysisRequest { PinnedVideoIds = null! }.PinnedVideoIds` is an empty list and the same for FollowedCreators (null-safe setter coverage).

    In ContentKbRelevanceServiceTests.cs add a `[Fact] ResolvePinTitlesAsync_MapsKnownIdsToTitles_SkipsUnknown` using the existing `TrackingContentSiteIndexStore` fake + `CreateRow` helper: seed two published rows (distinct YoutubeVideoId + Title), call `ResolvePinTitlesAsync(["known-id", "missing-id"])`, assert the dictionary maps `known-id` to its title and has no `missing-id` key; also assert `ResolvePinTitlesAsync([])` returns an empty dictionary. This proves ResolvedPinTitles is populated (not just declared).

    In DeckAnalysisPacketServiceTests.cs add `[Fact] CacheKey_SameDeckDifferentPins_ProducesDifferentKey` (HIGH-B + HIGH-D): build the service via the existing `CreateService(...)` helper, construct two `DeckAnalysisRequest` instances identical in DeckSource/options/TargetAiPlatform/SelectedAnalysisQuestions but with different `PinnedVideoIds` (one empty, one `["vid-a"]`); await `service.TryComputeCacheKeyAsync(reqA, default)` and `...(reqB, default)` and assert the two non-null keys are NOT equal. Add companion asserts: (a) a third request with the SAME deck and a reordered+duplicated pin list (`["vid-b","vid-a","vid-a"]` vs `["vid-a","vid-b"]`) produces the SAME key as its normalized twin; (b) HIGH-D — a request with pins `["abc"]` and one with pins `["ABC"]` produce DIFFERENT keys (pins are case-sensitive/ordinal); (c) differing FollowedCreators fork the key, but `["EDHRECast"]` vs `["edhrecast"]` produce the SAME key (follows are ignore-case). Use whatever minimal deck source the existing TryComputeCacheKeyAsync-exercising facts use so the key resolves non-null (do not introduce live Scryfall calls — mirror an existing cache-key/offline fact's request shape). This is the regression guard that selection participates in the cache key with the correct per-field case semantics.

    LF endings; `{ get; init; }` on any test DTOs; no formatter sweep.
  </action>
  <verify>
    <automated>"/mnt/c/Program Files/dotnet/dotnet.exe" test DeckFlow.Web.Tests/DeckFlow.Web.Tests.csproj --filter "PacketArtifactStoreTests|DeckAnalysisRequestTests|ResolvePinTitlesAsync|CacheKey_SameDeckDifferentPins" 2>&1 | grep -E "Passed!|Failed!|error" | head</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "BuildZip_with_selection_json_round_trips_into_request" DeckFlow.Web.Tests/PacketArtifactStoreTests.cs` matches one line
    - `grep -n "LoadFromZip_with_camelCase_selection_json_binds_properties" DeckFlow.Web.Tests/PacketArtifactStoreTests.cs` matches one line, and the test body contains the literal `pinnedVideoIds` (camelCase raw JSON — proves HIGH-4 binding)
    - `grep -n "LoadFromZip_with_corrupt_selection_json_degrades_to_empty_selection" DeckFlow.Web.Tests/PacketArtifactStoreTests.cs` matches one line
    - `dotnet test --filter "PacketArtifactStoreTests|DeckAnalysisRequestTests"` reports Passed with 0 failures
    - The corrupt-json test asserts no exception is thrown (e.g., the call completes and counts are 0)
    - `grep -n "ResolvePinTitlesAsync_MapsKnownIdsToTitles_SkipsUnknown" DeckFlow.Web.Tests/ContentKbRelevanceServiceTests.cs` matches one line and the test asserts known-id -> title mapping (proves ResolvedPinTitles population)
    - `grep -n "CacheKey_SameDeckDifferentPins_ProducesDifferentKey" DeckFlow.Web.Tests/DeckAnalysisPacketServiceTests.cs` matches one line; the test asserts keys DIFFER on different pins AND on case-only-different pins (HIGH-D), MATCH on normalized-equal (reordered/dup) pins, and MATCH on case-only-different follows (HIGH-D)
    - `dotnet test --filter "CacheKey_SameDeckDifferentPins"` reports Passed with 0 failures
  </acceptance_criteria>
  <done>Selection zip round-trip (incl. raw camelCase JSON), corrupt-json degradation, no-selection omission, null-safe binding, AND the HIGH-B/HIGH-D cache-key parity (different pins -> different key, case-only-different pins -> different key, reordered/dup pins -> same key, case-only-different follows -> same key) are all covered by passing tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| re-uploaded zip → LoadFromZip | Attacker-controlled 33-expert-selection.json crosses into the request DTO |
| request DTO → relevance service | PinnedVideoIds/FollowedCreators are untrusted form/zip input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-04 | Tampering | Selection JSON injection via re-upload | mitigate | PacketAllowedNames gate (ReadEntries throws on unlisted entries) + try/catch JSON deserialize (ExpertSelectionJsonOptions) + empty-list fallback (LoadFromZip_with_corrupt test proves no throw) |
| T-32-05 | Tampering | Spoofed video ids in PinnedVideoIds | mitigate | GetMergedClipsAsync (Plan 01) resolves ids against published rows only; unknown ids produce no clips |
| T-32-06 | Denial of Service | Oversized selection lists from a crafted zip | mitigate | Pin cap (3) + K=5 + budget enforced server-side in GetMergedClipsAsync regardless of list size |
| T-32-15 | Information Disclosure | Stale expert clips served from packet cache when selection changes | mitigate | DeckAnalysisCacheInputs includes normalized PinnedVideoIds (ordinal/case-sensitive) + FollowedCreators (ignore-case) (HIGH-B + HIGH-D); a different selection — including a case-only-different pin — forks the cache key so GetMergedClipsAsync re-runs (CacheKey_SameDeckDifferentPins test proves it) |
| T-32-SC | Tampering | npm/pip/cargo installs | accept | No package installs; phase installs zero external packages (RESEARCH §Package Legitimacy Audit: not applicable) |
</threat_model>

<verification>
- `dotnet build DeckFlow.Web` clean
- `dotnet test --filter "PacketArtifactStoreTests|DeckAnalysisRequestTests|ResolvePinTitlesAsync"` green
- Existing packet round-trip tests still pass (regression guard for 32-expert-context.json + prompt sections)
- Prompt==zip==panel invariant preserved: the merged set serialized into the zip is the same set rendered into the prompt and panel
- Selection participates in the DeckAnalysis packet cache key (HIGH-B + HIGH-D): `dotnet test --filter "CacheKey_SameDeckDifferentPins"` green — same deck + different pins (or a case-only-different pin) never reuses stale expert clips; case-only-different follows still hit
- Both DeckController.DeckAnalysisDownload BuildZip call sites embed the selection (HIGH-3): a cache-hit re-download and a fresh download both contain 33-expert-selection.json when a selection is present
</verification>

<success_criteria>
SEL-04 (selection persisted in zip, re-upload restores pins+follows AND clips) is implemented and tested with camelCase JSON options; the server side of SEL-02 (merged set reaches prompt+zip+panel) is wired via GetMergedClipsAsync with the replay-first guard intact; both DeckController BuildZip sites thread selectionJson; the DeckAnalysis packet cache key folds the normalized selection — pins case-sensitively (ordinal), follows case-insensitively (HIGH-D) — so a changed selection (including a case-only pin difference) forces a re-merge (no stale clips); `DeckAnalysisPacketResult.ResolvedPinTitles` resolves pinned ids to titles for Plan 03's server-rendered replay chips.
</success_criteria>

<output>
Create `.planning/phases/32-expert-context-selection-runs-after-phase-30-before-phase-31/32-02-SUMMARY.md` when done.
</output>
