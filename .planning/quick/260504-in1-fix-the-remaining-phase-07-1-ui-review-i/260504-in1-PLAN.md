---
quick_id: 260504-in1
slug: fix-the-remaining-phase-07-1-ui-review-i
status: planned
---

# Quick Task 260504-in1 Plan

fix the remaining Phase 07.1 UI review issues: when AI Category Suggestions is feature-flagged off, update maintenance copy to say only AI category suggestions are unavailable, add a link/action to Category Reference or Home from the maintenance page, and add a small in-context explanation on the home/categories area or nav so returning users understand the feature is temporarily unavailable

## Tasks

1. Update the feature-flag maintenance path
   - Files: `DeckFlow.Web/Models/Admin/MaintenanceViewModel.cs`, `DeckFlow.Web/Infrastructure/FeatureFlagGateAttribute.cs`, `DeckFlow.Web/Controllers/DeckController.cs`, `DeckFlow.Web/Views/Shared/_MaintenancePage.cshtml`
   - Action: Add optional maintenance-page action metadata and configure the AI Category Suggestions gate with precise copy plus a Category Reference link.
   - Verify: Direct `/suggest-categories` requests with the flag off render a 503 page that says only AI suggestions are unavailable and offers Category Reference.

2. Add in-context flag-off messaging
   - Files: `DeckFlow.Web/Views/Deck/Home.cshtml`, `DeckFlow.Web/Views/Shared/_DeckToolTabs.cshtml`, CSS as needed
   - Action: When `feature.categories.enabled` is off, show concise status text in the Categories hub/dropdown while keeping Category Reference available.
   - Verify: Feature-on state remains unchanged; feature-off state is discoverable without a dead link.

3. Validate and summarize
   - Files: quick task summary and `.planning/STATE.md`
   - Action: Build the web project, commit code atomically, write summary, and update the quick task state table.
   - Verify: Release build passes and quick task artifacts are committed.
