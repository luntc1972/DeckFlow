---
phase: 103-simulation-engine-guided-cut-rounds
plan: 08
type: execute
wave: 5
depends_on: ["103-06"]
files_modified:
  - DeckFlow.Web/Models/CutLabViewModel.cs
  - DeckFlow.Web/Views/Deck/CutLab.cshtml
autonomous: true
requirements: [CUT-01, CUT-02, CUT-03, SIM-02]
must_haves:
  truths:
    - "D-02/CUT-02: exactly one proposal card renders at a time, with the 'Proposed cut: {card}' heading, 'Flagged by {n} findings' neutral .kb-chip evidence, changed-only compact delta lines (D-05), and a 'Show full metric breakdown' expander revealing all 7 families."
    - "D-14/CUT-03: a sticky round+count bar (round name · N to cut · N cut so far) renders and pins within the Cut rounds section, 44px, theme-tokened, with data-cut-lab-sticky-* hooks."
    - "D-06/CUT-02: delta values carry direction via colored ▲/▼ token (--success/--danger) only on the numeric token, with neutral sentence copy — never a verdict, never a colored card name/heading/row."
    - "D-16/CUT-03: a 'Cuts made' restorable list renders (omitted until first cut; collapsed >5) with per-row Restore buttons."
    - "SIM-02/D-13: a collapsed 'Compare to baseline' panel renders a Metric·Baseline·Current·Delta table (all 7 families) available at any scroll position."
    - "CUT-01/D-03: round banners (Round 1 · Obvious cuts / Round 2 · Structural choices / Round 3 · Preference calls / Second pass ...) render from the round plan; empty/at-100 states render the specified copy."
    - "Full-page (no-JS) render works from the server round plan — the JS hydration in Plan 103-09 only enhances it."
  artifacts:
    - path: "DeckFlow.Web/Models/CutLabViewModel.cs"
      provides: "round/proposal/deltas/cuts-made/compare view fields + From() mapping"
      contains: "Proposal"
    - path: "DeckFlow.Web/Views/Deck/CutLab.cshtml"
      provides: "Cut rounds + Cuts made + Compare to baseline sections"
      contains: "cutlab-proposal"
  key_links:
    - from: "DeckFlow.Web/Views/Deck/CutLab.cshtml"
      to: "CutLabViewModel proposal/round/compare fields"
      via: "Razor renders sticky bar, proposal card, cuts-made, compare table"
      pattern: "cutlab-sticky-bar|cutlab-proposal|cutlab-compare"
---

<objective>
Render the three new Cut Lab sections the UI spec defines — the Cut rounds workspace (sticky bar + round banner + one-at-a-time proposal card + tradeoff deltas + accept/reject/defer), the Cuts made restore list, and the Compare to baseline panel — appended after the Phase 102 Role floors section. Server-rendered from the round plan + baseline so the no-JS path works; Plan 103-09 adds fetch hydration.

Purpose: This is the CUT-01/02/03 + SIM-02 surface the user actually touches. Copy and color are prescribed verbatim by the approved UI spec.
Output: Extended `CutLabViewModel` (+ From mapping) and `CutLab.cshtml`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/cut-lab/phases/103-simulation-engine-guided-cut-rounds/103-UI-SPEC.md
@.planning/workstreams/cut-lab/phases/103-simulation-engine-guided-cut-rounds/103-PATTERNS.md

<interfaces>
Existing view model (DeckFlow.Web/Models/CutLabViewModel.cs): sealed record with `From(CutLabRequest, CutLabProcessResult)`; nested `*View` records (CutLabRoleGroupView, CutLabFindingView, CutLabFloorRowView) as the shape precedent; DisplayLabelFor(roleKey) helper.
Existing view (DeckFlow.Web/Views/Deck/CutLab.cshtml): @model CutLabViewModel; `<section class="result-panel">` blocks; `<details class="cutlab-role-group">` accordions; hidden `CutLabStateJson`; `data-cut-lab-*` attribute convention.

From Plan 103-06: CutLabProcessResult.RoundPlan (CutLabRoundPlan: ordered items with card, round key/label, finding count + chips, CardsRemainingToTarget).
From Plan 103-01/05: CutLabMetricSnapshot (baseline), CutLabProposalDeltas / CutLabMetricDelta (proposal deltas — computed server-side for the first proposal by Plan 103-06/07).
From Plan 103-04: round label constants.
UI copy: 103-UI-SPEC Copywriting Contract (every heading, sub-line, banner, empty state, delta line, sticky-bar fields) + Component Contracts 1-4 (class names, data-attrs, 44px, directional token classes).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend CutLabViewModel with round/proposal/deltas/cuts-made/compare fields + From mapping</name>
  <read_first>
    - DeckFlow.Web/Models/CutLabViewModel.cs (record + From + nested *View record conventions + DisplayLabelFor)
    - DeckFlow.Web/Services/CutLab/CutLabPageService.cs (CutLabProcessResult.RoundPlan + BaselineSnapshot access via State)
    - DeckFlow.Web/Models/CutLab/CutLabMetrics.cs (snapshot/delta types)
    - .planning/workstreams/cut-lab/phases/103-simulation-engine-guided-cut-rounds/103-UI-SPEC.md (which fields each surface needs + exact labels)
  </read_first>
  <action>
    Add nested display records + fields to `CutLabViewModel`: (a) `CutLabProposalView?` (card name, round key, round banner heading + sub-line, "Flagged by {n} findings" + chip labels or the Round-3 "preference call" line, the changed-only `CutLabDeltaLineView[]` + a full-7-family `CutLabDeltaLineView[]` for the expander, floor-warning message(s), a `HasProposal`/terminal flag); (b) `CutLabDeltaLineView` (metric label, direction enum, formatted value token e.g. "2.1%"/"1 card", IsMeaningful, the neutral sentence "cutting {card} {lowers|raises} {metric} by …"); (c) sticky-bar fields (round label, cards-remaining, cut-so-far count); (d) `CutLabCutMadeRowView[]` (card, "cut in {round label}", restore hooks) sourced from `State.Decisions` (Accepted only, most-recent-first by Ordinal); (e) `CutLabCompareRowView[]` (metric label, baseline value, current value, delta token+direction) built from `State.BaselineSnapshot` vs the current working-list snapshot, plus baselineCount→currentCount context. Map all of these in `From(...)` from `CutLabProcessResult.RoundPlan` + `State`. Use `DisplayLabelFor`/the metric labels; do not invent copy — pull verbatim from the UI spec. Changed-lines only; XML-doc new members.
  </action>
  <verify>
    <automated>"/mnt/c/Program Files/dotnet/dotnet.exe" build DeckFlow.Web/DeckFlow.Web.csproj -clp:ErrorsOnly</automated>
  </verify>
  <acceptance_criteria>
    - View model exposes proposal, sticky-bar, cuts-made, and compare fields (grep `Proposal`, `Sticky`, `CutMade`/`CutsMade`, `Compare`).
    - `From` populates them from RoundPlan + State (Decisions, BaselineSnapshot) with no null-ref for an empty/at-100 pool.
    - Delta line carries direction as an enum + a separate formatted value token (not baked into one colored string).
    - Build clean, no new warnings.
  </acceptance_criteria>
  <done>The view model carries every datum the three new sections render.</done>
</task>

<task type="auto">
  <name>Task 2: Render Cut rounds + Cuts made + Compare to baseline sections in CutLab.cshtml</name>
  <read_first>
    - DeckFlow.Web/Views/Deck/CutLab.cshtml (existing result-panel + cutlab-role-group accordion sections + hidden CutLabStateJson + data-cut-lab-* attributes; the Role floors section is the insertion point — append after it)
    - .planning/workstreams/cut-lab/phases/103-simulation-engine-guided-cut-rounds/103-UI-SPEC.md (Component Contracts 1-4 + Copywriting Contract — class names, data-attrs, 44px, directional token classes, all copy strings)
  </read_first>
  <action>
    Append three `<section class="result-panel">` blocks after the Role floors section, only when `Model.HasResult`:
    (1) Cut rounds: `<h2>Cut rounds</h2>` + sub-line; the `.cutlab-sticky-bar` (three spans `data-cut-lab-sticky-round`/`-remaining`/`-accepted`, 44px); the round banner (`.cutlab-round-banner`, text from the proposal view); the `.cutlab-proposal` card — heading "Proposed cut: {card}", `.cutlab-proposal__evidence` with neutral `.kb-chip` finding tags, `.cutlab-delta` with one `.cutlab-delta__line` per changed family (value token span `.cutlab-delta__value--up|--down|--none` with `▲`/`▼` glyph `aria-hidden="true"`, neutral sentence text), the "{n} of 7 metric families changed" summary, a `<details data-cut-lab-delta-expander>` "Show full metric breakdown" listing all 7 families, an optional `.cutlab-proposal__floor-warning` (reusing `.cutlab-finding` styling) when a floor warning exists, and `.cutlab-proposal__actions` with three 44px buttons (`.cutlab-decision-btn--accept` accent-fill / `--reject` / `--defer` neutral-bordered, each `data-cut-lab-decision` + `data-cut-lab-card`). Render the empty ("Nothing to cut") and at-100 ("You're at 100 cards") states per the Copywriting Contract when there is no proposal.
    (2) Cuts made: `<details class="cutlab-cuts-made">` (omitted entirely when no accepted cuts; open when ≤5 rows) with summary "Cuts made · {n} cards" and `.cutlab-cuts-made__row`s (card · "cut in {round}" · `.cutlab-restore-btn` 44px with `data-cut-lab-restore` + `data-cut-lab-card`).
    (3) Compare to baseline: `<details class="cutlab-compare">` collapsed, summary "Compare to baseline", the count context sub-line, and a `table[data-prompt-cedh-reference-table]` with columns Metric·Baseline·Current·Delta (all 7 family rows; Delta cell uses the same directional token classes). Keep the hidden `CutLabStateJson` field as the single source of truth for the no-JS form path. All copy verbatim from the UI spec; all layout classes will be styled in Plan 103-09 (site-common.css). LF endings; do not reflow existing markup.
  </action>
  <verify>
    <automated>"/mnt/c/Program Files/dotnet/dotnet.exe" build DeckFlow.Web/DeckFlow.Web.csproj -clp:ErrorsOnly</automated>
  </verify>
  <acceptance_criteria>
    - The view contains `.cutlab-sticky-bar`, `.cutlab-proposal`, `.cutlab-cuts-made`, `.cutlab-compare` blocks with the specified `data-cut-lab-*` attributes (grep each).
    - Exactly one proposal card renders (no list/carousel) — D-02.
    - Directional token classes `--up`/`--down`/`--none` present; glyphs marked `aria-hidden`.
    - The 7-family expander and the compare table both enumerate all 7 families.
    - Empty/at-100/Nothing-to-cut copy matches the UI spec strings verbatim.
    - Build clean; Razor compiles.
  </acceptance_criteria>
  <done>The three new sections render server-side with prescribed copy, classes, and data hooks — no-JS usable.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| server → rendered HTML | Card names / metric labels rendered into the page; must be output-encoded (Razor default) to avoid stored/reflected XSS from a crafted pool. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-103-08-01 | Tampering (XSS) | Card names / round labels in Razor output | mitigate | Use Razor auto-encoded `@` output for all card/metric text (never `Html.Raw`); data-attrs carry encoded values. |
| T-103-08-02 | Information Disclosure | Verdict copy implying a card is "worse" | mitigate | Copy is pulled verbatim from the approved neutral UI-spec strings; the CUT-02 e2e/copy check (Plan 103-10) guards against drift. |
</threat_model>

<verification>
- `"/mnt/c/Program Files/dotnet/dotnet.exe" build DeckFlow.sln -clp:ErrorsOnly` clean.
- Existing CutLab controller/view-model tests stay green: `... test DeckFlow.Web.Tests --filter "FullyQualifiedName~CutLab"`.
- Visual verification of the three sections is covered by Plan 103-10 (theme×viewport screenshots).
</verification>

<success_criteria>
Cut rounds, Cuts made, and Compare-to-baseline sections render server-side with the exact UI-spec copy, classes, 44px targets, directional tokens, and data hooks; no-JS path functional.
</success_criteria>

<output>
Create `.planning/workstreams/cut-lab/phases/103-simulation-engine-guided-cut-rounds/103-08-SUMMARY.md` when done.
</output>
