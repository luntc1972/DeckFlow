---
phase: 999.1-ai-agnostic-prose-adaptation-razor-views
plan: 03
subsystem: ui
tags: [razor, cshtml, ai-agnostic, AiPlatform, hybrid-prose]

# Dependency graph
requires:
  - phase: 15-aiplatform-value-object-refactor
    provides: AiPlatform.Normalize() method and DisplayName property used for below-selector injection
provides:
  - CedhMetaGap.cshtml with hybrid prose pattern (AiPlatform.Normalize block + universal above-selector + DisplayName below-selector)
  - Home.cshtml DeckComparison hub-card generalized to brand-neutral "AI-authored breakdown"
affects:
  - 999.1-ai-agnostic-prose-adaptation-razor-views (plans 01, 02, 04, 05, 06, 07 — same phase, parallel wave)

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Hybrid prose pattern: AiPlatform.Normalize() in @{} block, universal noun above selector, @aiPlatform.DisplayName below selector"
    - "D-05 lede exception: enumerated 'ChatGPT, Claude, or Gemini' form preserved in page-lede elements"
    - "D-03 carve-out: JudgeQuestions-related hub card copy left intentionally ChatGPT-named"

key-files:
  created: []
  modified:
    - DeckFlow.Web/Views/Deck/CedhMetaGap.cshtml
    - DeckFlow.Web/Views/Deck/Home.cshtml

key-decisions:
  - "CedhMetaGap line-22 page-lede kept with enumerated 'ChatGPT, Claude, or Gemini' per D-05 lede exception"
  - "Home.cshtml line-14 hub-hero lede kept with enumerated form per D-05 hub-level lede exception"
  - "Home.cshtml line-62 JudgeQuestions hub-card kept ChatGPT-named per D-03 carve-out"
  - "Below-selector prose at lines 297 and 312 in CedhMetaGap uses @aiPlatform.DisplayName injection"
  - "Phase 13 D-08 identifiers (data-cache-key, data-chatgpt-cedh-form, chatgpt-step-eyebrow) left untouched"

patterns-established:
  - "Hybrid prose: above-selector uses universal noun 'your AI', below-selector injects @aiPlatform.DisplayName"

requirements-completed: [D-01, D-02, D-03, D-05]

# Metrics
duration: 5min
completed: 2026-05-18
---

# Phase 999.1 Plan 03: cEDH Meta Gap + Home Hub Card AI-Agnostic Prose Summary

**Hybrid prose pattern applied to CedhMetaGap.cshtml (6 ChatGPT hits rewritten) and DeckComparison hub-card in Home.cshtml generalized to "AI-authored breakdown"**

## Performance

- **Duration:** 5 min
- **Started:** 2026-05-18T18:42:03Z
- **Completed:** 2026-05-18T18:46:33Z
- **Tasks:** 2
- **Files modified:** 2

## Accomplishments

- Added `AiPlatform.Normalize(Model.Request.TargetAiPlatform)` to `CedhMetaGap.cshtml` top `@{}` block enabling `@aiPlatform.DisplayName` injection in below-selector prose
- Rewrote 5 above-selector ChatGPT hits in CedhMetaGap to universal "your AI" / "AI prompt" noun form (lines 23, 35, 180, 280, 281)
- Rewrote 2 below-selector ChatGPT hits in CedhMetaGap to `@aiPlatform.DisplayName` injection (lines 297, 312)
- Preserved line-22 page-lede enumerated form "ChatGPT, Claude, or Gemini" per D-05
- Preserved all Phase 13 D-08 identifiers: `data-cache-key="chatgpt-cedh-meta-gap"`, `data-chatgpt-cedh-form`, `chatgpt-step-eyebrow` CSS class
- Generalized Home.cshtml line-26 DeckComparison hub-card from "ChatGPT-authored breakdown" to "AI-authored breakdown"
- Preserved Home.cshtml line-14 hub-hero enumerated lede per D-05 and line-62 JudgeQuestions card per D-03 carve-out

## Task Commits

Each task was committed atomically:

1. **Task 1: Add AiPlatform.Normalize Razor block + rewrite ChatGPT prose in CedhMetaGap.cshtml** - `61f2ee1` (feat)
2. **Task 2: Generalize DeckComparison hub-card description in Home.cshtml** - `c99faed` (feat)

## Files Created/Modified

- `DeckFlow.Web/Views/Deck/CedhMetaGap.cshtml` - Added AiPlatform.Normalize block; rewrote 5 above-selector + 2 below-selector ChatGPT prose hits; D-05 lede and D-08 identifiers preserved
- `DeckFlow.Web/Views/Deck/Home.cshtml` - Line 26 DeckComparison hub-card: "ChatGPT-authored" → "AI-authored"; D-05 hub-hero and D-03 JudgeQuestions preserved

## Decisions Made

- "AI prompt" chosen over "prompt for your AI" for line-23 (shorter, reads naturally in sentence context)
- "Step 2 generates the prompt from..." chosen for line-35 (drops redundant noun entirely, reads cleanest)
- "AI-authored breakdown" chosen for Home.cshtml hub card (hyphenated adjective reads naturally; "your AI's breakdown" is awkward without selector context)

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Created Windows junction for node_modules to enable dotnet build in worktree**
- **Found during:** Task 1 (build verification)
- **Issue:** Worktree had no `node_modules/typescript` so `dotnet build` failed at the MSBuild `CompileTypeScriptAssets` target. A Linux symlink was created first but Windows dotnet.exe cannot follow WSL symlinks.
- **Fix:** Used PowerShell `New-Item -ItemType Junction` to create a Windows directory junction pointing to the main repo's `node_modules`. This is an in-memory worktree scaffolding fix, not a code change.
- **Files modified:** `DeckFlow.Web/node_modules` (junction, gitignored, not committed)
- **Verification:** `dotnet build DeckFlow.Web/DeckFlow.Web.csproj -c Debug` exits 0
- **Committed in:** Not committed (gitignored junction)

---

**Total deviations:** 1 auto-fixed (1 blocking — worktree build environment setup)
**Impact on plan:** Required fix for build verification. No scope creep.

### Plan Criterion Note

The plan acceptance criterion for `chatgpt-step-eyebrow` states `grep -c 'chatgpt-step-eyebrow' ... returns 1`. The baseline file has 3 instances (one per step panel: Step 1, Step 2, Step 3). All 3 were preserved untouched as required. The criterion count appears to be incorrect in the plan — the invariant (don't touch this class) is satisfied.

## Issues Encountered

None beyond the worktree node_modules blocking issue documented above.

## Next Phase Readiness

- CedhMetaGap.cshtml hybrid prose is complete; pages 01 and 02 (DeckAnalysis + DeckComparison) are handled by sibling wave-1 agents
- Home.cshtml hub-card prose for the 3 multi-AI tools is now brand-neutral (DeckAnalysis card was already neutral; DeckComparison updated here; cEDH Meta Gap card was already neutral)
- Phase 999.1 plans 04–07 (C# exception messages, Help markdown, log prefix rename, data-busy-title) can proceed independently

---
*Phase: 999.1-ai-agnostic-prose-adaptation-razor-views*
*Completed: 2026-05-18*
