---
context: phase
phase: 999.7-v1-3-audit-cleanup
task: null
total_tasks: 4
status: paused
last_updated: 2026-05-22T20:39:59Z
branch: v1.3
head_commit: 664f1da
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

- [ ] CONSTRAINT: Prod /Admin/Harvest broken — production v1.3 deploy has stuck Queued harvest jobs. Restart Render web service before any further deploy or operator action; reaper SQL at HarvestRunStore.cs:475-489 will flip stale rows to Failed on first request.
- [ ] CONSTRAINT: Phase 999.7 Plan 03 cannot pass its own grep gate — audit F-01 evidence inventory undercounted by 2. Repo grep returns 5 Claude variants containing `result-wrapped output`, not 3. Plan 03 must be re-scoped (Option A all 5, or Option B keep 3 with grep restricted to those paths) before /gsd-execute-phase 999.7.
- [ ] CONSTRAINT: Codex executes, Claude reviews — per project CLAUDE.md. Do NOT spawn gsd-executor; route 999.7 execution through cross-AI dispatch. Plan reviews go to Codex (authoritative HIGH gate).

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Audit evidence inventory undercounted | Phase 999.2 audit F-01 listed 3 files with `result-wrapped output` but repo actually had 5 (ClaudeFollowUp + ClaudeSetUpgrade also matched). Plan 03 trusted the inventory verbatim and inherited the undercount. | blocking | When planning sweep edits, re-run the grep yourself before authoring acceptance gates. `grep -rln <phrase> <root-dir>` takes 2s and surfaces actual scope. Never trust an audit's evidence inventory without independent verification. |
| Inner UpdateStateAsync without try/catch in worker catches | ArchidektCacheJobService.cs:343-377 calls `_runStore.UpdateStateAsync(...Failed...)` inside catch handlers with no wrapping try/catch. If PG transient throws, the exception escapes the catch + escapes `await foreach`, killing the worker until process restart. Phase 999.6 fixed only the host-shutdown branch (line 304-323) and not the other two. | blocking | Mirror the host-shutdown defense in both remaining catches: wrap inner UpdateStateAsync in try { } catch (Exception writeException) { _logger.LogWarning(writeException, "Harvest.Run.TerminalWriteFailed jobId={JobId}", signal.JobId); }. Belt-and-braces: outer `await foreach` itself should be wrapped so worker auto-restarts on unexpected throws. |

<current_state>
Three open threads when paused (mid /gsd-review for 999.7):

1. **HARVESTER (urgent, prod)** — User clicked Run Now on /Admin/Harvest prod, AJAX returned `state:Queued, jobId:987e7b71-..., startedUtc:null`. Job never transitions Running. Hypothesis: ExecuteAsync worker died on a prior unwrapped UpdateStateAsync throw; channel still accepts writes but no one reads.

2. **999.7 plans committed but blocked** — 4 PLAN.md files in `.planning/phases/999.7-v1-3-audit-cleanup/` (commit 254df9e). Internal Claude plan-checker passed iter 2/3. Codex peer review (commit 664f1da, REVIEWS.md) flagged 2 HIGH on Plan 03 + 4 MED/LOW spread across plans. All HIGH cluster on Plan 03's incomplete file inventory.

3. **Redirect removal request** — User said 11 chatgpt-* 301 redirects from Phase 12 can be retired. Not yet captured anywhere. Decide: Phase 999.8 in v1.3, or v1.4 scope.
</current_state>

<completed_work>

- /gsd:plan-phase 999.7 — 4 plans created (01 STATE.md counters, 02 REQUIREMENTS.md WDG flips, 03 Claude variant summaries, 04 DeckController F-02 comment + 999.5-UAT status). Wave 1, autonomous, all SC1..SC7 covered per checker.
- Plan-checker revision loop iter 1→2 — 2 BLOCKERs on Plan 01 (counting scope + self-referential timing) fixed via Option A (literal SC1 target values, no depends_on chain). 2 warnings (stale line refs, F-02 grep anchor) also fixed.
- ROADMAP.md annotations applied (wave + cross-cutting constraint headers).
- STATE.md updated via state.planned-phase verb (Status=Ready to execute, plan_count=4).
- Commit 254df9e: 4 plans + STATE.md + ROADMAP.md.
- /gsd-review --phase 999.7 — Codex invoked (Claude self-skipped per SELF_CLI rule, no other CLIs installed). REVIEWS.md written with 10 findings.
- Commit 664f1da: REVIEWS.md with 2 HIGH + 4 MED + 4 LOW.
</completed_work>

<remaining_work>

| ID | Task | Blocker |
|----|------|---------|
| harvester-bug-prod | Diagnose AdminHarvest worker death; Codex applies fix to ArchidektCacheJobService.cs catches at lines 343-377 | URGENT — prod broken; user restart Render to unstick first |
| 999.7-replan | /gsd:plan-phase 999.7 --reviews — Codex HIGH on Plan 03 scope (5 files not 3) | Awaits user decision Option A vs B |
| 999.7-execute | /gsd-execute-phase 999.7 (Codex executor) | Blocked on replan |
| redirects-removal | Capture as Phase 999.8 or v1.4 backlog item | Awaits user scope decision |
| audit-md-tracking | Commit OR .gitignore `.planning/v1.3-MILESTONE-AUDIT.md` (currently untracked; all 999.7 plans depend on it) | Codex MED finding |
</remaining_work>

<decisions_made>

- Skip discuss-phase + research for 999.7 — ROADMAP scope hyper-concrete (file paths + line numbers + 7 verifiable SCs); audit doc is binding contract.
- 4 plans in Wave 1, zero `files_modified` overlap, all `autonomous:true` — Codex executes per cross-AI rule; parallel-safe.
- Plan 01 writes SC1 literal target values (11/11/46/46/100) verbatim (Option A) — execution-order-independent; 999.7 treated as meta-audit phase excluded from v1.3 totals by convention.
- Pause 999.7 cycle to address prod harvester first — production > planning artifacts.
</decisions_made>

<blockers>

- Prod /Admin/Harvest stuck — worker exited on prior unwrapped throw. **Workaround: restart Render web service.**
- 999.7 Plan 03 evidence-inventory drift (3 files listed vs 5 actual). **Workaround: /gsd:plan-phase 999.7 --reviews after user picks Option A or B.**
</blockers>

## Required Reading (in order)

1. `.planning/phases/999.7-v1-3-audit-cleanup/999.7-REVIEWS.md` — Codex review with 2 HIGH + 4 MED + 4 LOW; resolution path Option A vs B for Plan 03.
2. `.planning/v1.3-MILESTONE-AUDIT.md` (untracked) — audit binding all 999.7 scope. F-01 (3 Claude doc-comments) is the undercounted finding; F-02 is the DeckController asymmetry comment.
3. `DeckFlow.Web/Services/ArchidektCacheJobService.cs:240-385` — ExecuteAsync worker loop. Lines 343-377 are the unwrapped-UpdateStateAsync catches that likely killed prod worker; line 304-323 is the correctly-wrapped reference impl from Phase 999.6 fix.
4. `DeckFlow.Web/Services/Harvest/HarvestRunStore.cs:69-96, 211-232, 475-489` — EnsureSchemaAsync gate + reaper SQL + GetActiveAsync (includes 'Queued' state so stuck rows block new enqueues until reaper clears them).
5. `CLAUDE.md` (user global + project) — Codex codes, Claude reviews/investigates. Plan reviews routed to Codex. Plan 999.7 plans must keep `autonomous:true` so /gsd-execute-phase dispatches to Codex.
6. `.planning/REQUIREMENTS.md` lines 14-23 (WDG-01..10 checkboxes) — target for Plan 02.

## Critical Anti-Patterns (do NOT repeat these)

- ANTI-PATTERN: trusting audit evidence inventory verbatim → grep yourself first.
- ANTI-PATTERN: unwrapped inner UpdateStateAsync in BackgroundService catch handlers → wrap in try/catch + LogWarning; mirror line 304-323 host-shutdown pattern.
- ANTI-PATTERN: assuming /gsd-review with no flags invokes all reviewers → in Claude Code runtime, SELF_CLI=claude is auto-skipped; if only codex+claude are installed, you get a single-reviewer run. Pass --all or specific flags if needed.

## Infrastructure State

- Branch: `v1.3` (HEAD: 664f1da). Do NOT switch branches.
- Prod: Render Starter web + Basic-256mb Postgres. v1.3 deployed for testing 2026-05-22 (user-flagged).
- Untracked: `.planning/v1.3-MILESTONE-AUDIT.md`, `.planning/phases/999.7-v1-3-audit-cleanup/.gitkeep`.
- /Admin/Harvest worker: presumed dead in prod runtime. Restart Render web service to recover.
- Phase 999.7 dir has 4 PLAN.md + 1 REVIEWS.md, no SUMMARY.md yet (execute-phase generates).
- Test baseline (Phase 999.6 PHASE EXIT GATE): `Failed: 0, Passed: 497 (Web 440 + Core 57), Skipped: 3, Total: 500`.

<context>
Was 2/3 through /gsd-plan-phase 999.7 → /gsd-review 999.7 chain when user reported prod harvester broken mid-review. /gsd-review completed (Codex flagged 2 HIGH). User then noted past-URL redirects can be removed (separate scope).

Mental state: phase 999.7 is a mechanical doc-cleanup that should have shipped trivially, but audit F-01 inventory undercount blocks Plan 03's grep gate. Resolution is simple once user picks SC3 scope (A: all 5 Claude variants; B: only 3 + scoped grep). Prod harvester is the actual urgent surface — a previously-shipped phase (999.6) added defense to one catch but missed the other two, and prod stress (PG transient on terminal-write?) exposed the gap.

Sequencing: prod fix first, then 999.7 replan, then 999.7 execute, then redirect-removal capture.
</context>

<next_action>

1. Ask user to restart Render web service (unsticks prod harvester immediately via D-02 reaper).
2. After restart, run `/gsd-debug "AdminHarvest worker dies on terminal-write exception in ArchidektCacheJobService.cs:343-377"` — investigation only; Codex applies fix per CLAUDE.md.
3. Ask user: SC3 scope Option A (5 files) or B (3 files + scoped grep)?
4. Run `/gsd:plan-phase 999.7 --reviews` with chosen option.
5. After replan clears HIGH, `/clear` then `/gsd-execute-phase 999.7`.
6. Capture redirect-removal: `/gsd-capture "remove 11 chatgpt-* 301 redirects added in Phase 12 — past links can be retired"`.
</next_action>
