---
phase: 57-admin-surface-distill-quality
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - DeckFlow.Web/Models/AdminContentKbViewModel.cs
  - DeckFlow.Web/Controllers/Admin/AdminContentKbController.cs
  - DeckFlow.Web/Program.cs
  - DeckFlow.Web/Views/AdminContentKb/Index.cshtml
  - DeckFlow.Web/wwwroot/css/admin-common.css
  - DeckFlow.Web.Tests/AdminContentKbControllerTests.cs
autonomous: true
requirements: [SITE-01]
user_setup: []

must_haves:
  truths:
    - "Admin sees a Publish State column on /Admin/ContentKb showing the same four states as Studio (Never published / Pushed-hidden / Published / Local-newer)"
    - "Existing admin columns (Title, Source, Tags, Status, Action) are unchanged and unshifted"
    - "PublishState is derived once via PublishStateDeriver — no duplicate status logic"
    - "DeckFlow.Web starts without InvalidOperationException (PublishStateDeriver is registered)"
  artifacts:
    - path: "DeckFlow.Web/Models/AdminContentKbViewModel.cs"
      provides: "KbEntryRow.PushedToProdUtc, KbEntryRow.IndexedUtc, KbEntryRow.PublishState"
      contains: "public PublishState PublishState"
    - path: "DeckFlow.Web/Program.cs"
      provides: "PublishStateDeriver singleton registration"
      contains: "AddSingleton<DeckFlow.Core.Content.PublishStateDeriver>"
    - path: "DeckFlow.Web/Controllers/Admin/AdminContentKbController.cs"
      provides: "deriver injection + mapping in Index()"
      contains: "_deriver.Derive(r.PushedToProdUtc, r.IsVisible, r.IndexedUtc)"
    - path: "DeckFlow.Web/Views/AdminContentKb/Index.cshtml"
      provides: "Publish State column (th + td) + colspan 6"
      contains: "Publish State"
    - path: "DeckFlow.Web/wwwroot/css/admin-common.css"
      provides: "kb-status--local-newer badge class"
      contains: "kb-status--local-newer"
  key_links:
    - from: "AdminContentKbController.Index()"
      to: "PublishStateDeriver.Derive"
      via: "injected _deriver field"
      pattern: "_deriver\\.Derive"
    - from: "Index.cshtml row"
      to: "entry.PublishState.ToDisplayString()"
      via: "view model KbEntryRow"
      pattern: "PublishState.*ToDisplayString"
---

<objective>
Add a derived Publish State column to the `/Admin/ContentKb` curation grid (SITE-01), showing the same four states as Studio: Never published / Pushed-hidden / Published / Local-newer.

Purpose: The site admin gets the production-push signal for every KB entry without opening Studio, mirroring the publish-state surfacing PUB-03 added to Studio's Review/Publish pages.
Output: Extended `KbEntryRow`, controller mapping + deriver injection, Program.cs DI registration, one new view column, one new CSS badge class, and four new controller unit tests (one per derived publish state).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/57-admin-surface-distill-quality/57-RESEARCH.md
@.planning/phases/57-admin-surface-distill-quality/57-PATTERNS.md
@.planning/phases/57-admin-surface-distill-quality/57-VALIDATION.md
@CLAUDE.md

<interfaces>
<!-- Contracts the executor needs. Extracted from codebase — no exploration required. -->

From DeckFlow.Core/Content/PublishStateDeriver.cs (pure, parameterless ctor):
  public sealed class PublishStateDeriver
  public PublishState Derive(DateTimeOffset? pushedToProdUtc, bool isVisible, DateTimeOffset localIndexedUtc)
    // null pushedToProdUtc          -> NeverPublished
    // pushed but !isVisible         -> PushedHidden
    // pushed + visible + local>push -> LocalNewer
    // pushed + visible + local<=push-> Published

From DeckFlow.Core/Content/PublishState.cs:
  public enum PublishState { NeverPublished, PushedHidden, Published, LocalNewer }
  public static string ToDisplayString(this PublishState state)
    // NeverPublished => "Never published", PushedHidden => "Pushed-hidden",
    // Published => "Published", LocalNewer => "Local-newer"

From DeckFlow.Core/Knowledge/ContentArtifactSpec.cs — ContentSiteIndexRow (already populated by GetAllRowsAsync):
  public DateTimeOffset? PushedToProdUtc { get; init; }    // line 133
  public required DateTimeOffset IndexedUtc { get; init; } // line 136
  public bool IsVisible { get; init; }                     // line 139

From DeckFlow.Web/wwwroot/css/admin-common.css — existing badge classes:
  .admin-shell .kb-status--published   (line 597)
  .admin-shell .kb-status--hidden      (line 603)
  .admin-shell .kb-status--unpublished (line 608)
  // kb-status--local-newer does NOT exist yet — must be added.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend KbEntryRow, register deriver, map PublishState in controller</name>
  <files>DeckFlow.Web/Models/AdminContentKbViewModel.cs, DeckFlow.Web/Program.cs, DeckFlow.Web/Controllers/Admin/AdminContentKbController.cs, DeckFlow.Web.Tests/AdminContentKbControllerTests.cs</files>
  <read_first>
    - DeckFlow.Web/Models/AdminContentKbViewModel.cs (the KbEntryRow sealed record being extended — lines 64-86)
    - DeckFlow.Web/Controllers/Admin/AdminContentKbController.cs (constructor lines 32-46 + .Select() mapping lines 64-74)
    - DeckFlow.Web/Program.cs (registration block around lines 92-97)
    - DeckFlow.Web.Tests/AdminContentKbControllerTests.cs (Build() helper lines 282-304, Row() helper lines 306-321)
    - DeckFlow.Core/Content/PublishStateDeriver.cs (source-of-truth Derive signature + precedence)
    - DeckFlow.Studio/Program.cs lines 108-110 (the AddSingleton<PublishStateDeriver>() analog to mirror)
    - DeckFlow.Studio/Pages/Review.razor lines 128, 244 (deriver injection + call analog)
  </read_first>
  <behavior>
    - Test 1: Index() maps a row's PushedToProdUtc and IndexedUtc onto KbEntryRow (round-trips the values).
    - Test 2: Index() derives PublishState.NeverPublished when PushedToProdUtc is null.
    - Test 3: Index() derives PublishState.Published when visible=true and PushedToProdUtc >= IndexedUtc.
    - Test 4: Index() derives PublishState.LocalNewer when IndexedUtc > PushedToProdUtc (visible=true).
  </behavior>
  <action>
    In AdminContentKbViewModel.cs: add `using DeckFlow.Core.Content;` to the using block. Insert three properties into the `KbEntryRow` sealed record after the `IsEvergreen` property (before the record's closing brace): nullable `DateTimeOffset? PushedToProdUtc { get; init; }`; `required DateTimeOffset IndexedUtc { get; init; }`; and `PublishState PublishState { get; init; } = PublishState.NeverPublished` (defaulted so test Row() callers do not break on a required-property ripple). Each property keeps the `{ get; init; }` accessor — CARVE-OUT: never simplify to `{ get; }`.

    In Program.cs: insert `builder.Services.AddSingleton<DeckFlow.Core.Content.PublishStateDeriver>();` immediately after the existing `AddSingleton<IContentKbSeedLoader, ContentKbSeedLoader>();` line (~line 96). Use the fully-qualified type name to match the adjacent `DeckFlow.Core.Content.IContentSiteIndexStore` registration style. This closes the easy-miss gap — Studio registers the deriver but Web does not (Pitfall 1: unregistered -> InvalidOperationException at startup/first request).

    In AdminContentKbController.cs: add a 5th constructor parameter `PublishStateDeriver deriver`, with `ArgumentNullException.ThrowIfNull(deriver);` and assignment to a new `private readonly PublishStateDeriver _deriver;` field, following the existing one-guard-per-parameter pattern. In the `.Select(r => new KbEntryRow { ... })` block in Index(), add three initializers after `IsEvergreen = r.IsEvergreen,`: `PushedToProdUtc = r.PushedToProdUtc,`, `IndexedUtc = r.IndexedUtc,`, and `PublishState = _deriver.Derive(r.PushedToProdUtc, r.IsVisible, r.IndexedUtc),`. Do not add any if/else publish-state logic — the deriver is the single source of truth (Don't-Hand-Roll).

    In AdminContentKbControllerTests.cs: pass `new DeckFlow.Core.Content.PublishStateDeriver()` as the 5th argument to the `new AdminContentKbController(...)` call in the `Build()` helper (no fake — it is pure and stateless). Add an optional `DateTimeOffset? pushedToProdUtc = null` parameter to the `Row()` helper and wire `PushedToProdUtc = pushedToProdUtc` into its initializer (IndexedUtc is already populated from the existing `indexed` parameter). Add the four `[Fact]` tests from the behavior block, asserting on `Assert.IsType<ViewResult>(result).Model as AdminContentKbViewModel` then `vm.Entries[0].PublishState` (and `.PushedToProdUtc`/`.IndexedUtc` for Test 1), following the existing `Method_Scenario_ExpectedResult` naming.

    Respect carve-outs: changed C# lines must satisfy the format gate; preserve LF line endings; do not mass-reflow untouched lines.
  </action>
  <verify>
    <automated>cd /mnt/c/users/chrislunt/source/personal/deckflow && "/mnt/c/Program Files/dotnet/dotnet.exe" build DeckFlow.Web.Tests/DeckFlow.Web.Tests.csproj 2>&1 | tail -5 && "/mnt/c/Program Files/dotnet/dotnet.exe" test DeckFlow.Web.Tests/DeckFlow.Web.Tests.csproj --no-build --filter "AdminContentKbController" 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'PublishState PublishState { get; init; }' DeckFlow.Web/Models/AdminContentKbViewModel.cs` returns 1.
    - `grep -c 'PushedToProdUtc { get; init; }' DeckFlow.Web/Models/AdminContentKbViewModel.cs` returns 1.
    - `grep -c 'required DateTimeOffset IndexedUtc { get; init; }' DeckFlow.Web/Models/AdminContentKbViewModel.cs` returns 1.
    - `grep -c 'AddSingleton<DeckFlow.Core.Content.PublishStateDeriver>' DeckFlow.Web/Program.cs` returns 1.
    - `grep -c '_deriver.Derive(r.PushedToProdUtc, r.IsVisible, r.IndexedUtc)' DeckFlow.Web/Controllers/Admin/AdminContentKbController.cs` returns 1.
    - `grep -c 'new DeckFlow.Core.Content.PublishStateDeriver()' DeckFlow.Web.Tests/AdminContentKbControllerTests.cs` returns at least 1.
    - DeckFlow.Web.Tests builds with 0 errors.
    - `dotnet test --filter "AdminContentKbController"` reports the four new publish-state facts passing and 0 failures.
    - No `if (r.IsVisible)`-style publish-state derivation added in the controller — derivation only via `_deriver.Derive`.
  </acceptance_criteria>
  <done>KbEntryRow exposes PushedToProdUtc/IndexedUtc/PublishState; the deriver is registered in Web DI and injected into the controller; Index() maps all three; the four new controller tests pass; build is clean.</done>
</task>

<task type="auto">
  <name>Task 2: Add Publish State view column and local-newer badge CSS</name>
  <files>DeckFlow.Web/Views/AdminContentKb/Index.cshtml, DeckFlow.Web/wwwroot/css/admin-common.css</files>
  <read_first>
    - DeckFlow.Web/Views/AdminContentKb/Index.cshtml (thead lines 132-138, Status td block lines 153-166, empty-filter colspan line 241)
    - DeckFlow.Web/wwwroot/css/admin-common.css (existing kb-status classes lines 597-612)
    - DeckFlow.Studio/Pages/Review.razor lines 621-626 (the locked four-state badge vocabulary analog)
    - DeckFlow.Core/Content/PublishState.cs (ToDisplayString extension — the display-text source)
  </read_first>
  <action>
    In Index.cshtml `<thead>`: insert `<th scope="col">Publish State</th>` after the existing `Status` `<th>` and before the `Action` `<th>`, making the table six columns.

    In Index.cshtml `<tbody>` per-row markup: insert a new `<td data-label="Publish State">` immediately after the closing `</td>` of the existing Status cell and before the Action `<td>`. The cell renders `<span class="kb-status @(...)">@entry.PublishState.ToDisplayString()</span>`, where the switch maps `DeckFlow.Core.Content.PublishState.Published => "kb-status--published"`, `PushedHidden => "kb-status--hidden"`, `LocalNewer => "kb-status--local-newer"`, and the discard `_ => "kb-status--unpublished"`. Use the fully-qualified `DeckFlow.Core.Content.PublishState` member form inside the Razor switch unless a `@using DeckFlow.Core.Content` already exists in the view. Display text comes only from `ToDisplayString()` — do not hardcode the four strings (Don't-Hand-Roll: locked vocabulary shared with Studio).

    In Index.cshtml: change the empty-filter row `<td colspan="5">No entries match the current filter.</td>` to `colspan="6"` (Pitfall 3).

    In admin-common.css: add a new `.admin-shell .kb-status--local-newer` rule after the existing `.admin-shell .kb-status--unpublished` block (~line 612), mirroring Studio's `bg-info text-dark` teal — `color: #000; border-color: #0dcaf0; background: rgba(13, 202, 240, 0.15);` — with a `/* Why: mirrors Studio's bg-info badge for Local-newer */` comment. CARVE-OUT: this CSS goes in `admin-common.css` (where all `kb-status` classes live), NOT `site.css` or `site-common.css`. The other three states reuse the existing `kb-status--published/hidden/unpublished` classes — only `local-newer` is new.

    Preserve LF line endings; do not reflow untouched lines.
  </action>
  <verify>
    <automated>cd /mnt/c/users/chrislunt/source/personal/deckflow && grep -c 'Publish State' DeckFlow.Web/Views/AdminContentKb/Index.cshtml && grep -c 'colspan="6"' DeckFlow.Web/Views/AdminContentKb/Index.cshtml && grep -c 'kb-status--local-newer' DeckFlow.Web/wwwroot/css/admin-common.css && "/mnt/c/Program Files/dotnet/dotnet.exe" build DeckFlow.Web/DeckFlow.Web.csproj 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'Publish State' DeckFlow.Web/Views/AdminContentKb/Index.cshtml` returns at least 2 (one `<th>`, one `data-label`).
    - `grep -c 'kb-status--local-newer' DeckFlow.Web/Views/AdminContentKb/Index.cshtml` returns 1.
    - `grep -c 'kb-status--local-newer' DeckFlow.Web/wwwroot/css/admin-common.css` returns 1.
    - `grep -c 'colspan="6"' DeckFlow.Web/Views/AdminContentKb/Index.cshtml` returns 1 and `grep -c 'colspan="5"' DeckFlow.Web/Views/AdminContentKb/Index.cshtml` returns 0.
    - `grep -c 'entry.PublishState.ToDisplayString()' DeckFlow.Web/Views/AdminContentKb/Index.cshtml` returns 1 (display text from extension, not hardcoded).
    - `grep -c 'kb-status--local-newer' DeckFlow.Web/wwwroot/css/site.css DeckFlow.Web/wwwroot/css/site-common.css 2>/dev/null` totals 0 (carve-out: not in site/site-common css).
    - DeckFlow.Web builds with 0 errors (Razor compiles).
    - Manual (Phase 58 / operator): load `/Admin/ContentKb`; the six-column grid shows the new Publish State column with no existing column shifted or removed.
  </acceptance_criteria>
  <done>The grid renders a sixth Publish State column using the locked four-state vocabulary; the empty-filter colspan is 6; the local-newer badge class exists in admin-common.css; Web builds clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser (admin) -> /Admin/ContentKb | Admin request crosses into the app; already gated by `BasicAuthMiddleware` on the `/Admin` branch. No change in this plan. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-57-01-01 | Information Disclosure | New Publish State column on `/Admin/ContentKb` | accept | Column is derived from existing DB row values (`PushedToProdUtc`, `IsVisible`, `IndexedUtc`) — operator-created data, not user PII. The page stays behind the existing `/Admin` BasicAuth gate; no new route, no new public surface. No data leaves the admin boundary. |
| T-57-01-02 | Tampering | `KbEntryRow` deserialization | accept | New properties use `{ get; init; }` (carve-out preserved) so System.Text.Json does not silently skip them; no user input flows into these properties — they are populated server-side from the trusted store row and the pure `PublishStateDeriver`. No injection surface. |
| T-57-01-03 | Elevation of Privilege | DI registration of `PublishStateDeriver` | mitigate | Registered as a stateless singleton (pure logic, no I/O, no mutable state) mirroring Studio; cannot be abused for state leakage across requests. |
| T-57-01-SC | Tampering | npm/NuGet installs | accept | No package installs in this plan (zero new dependencies — RESEARCH Package Legitimacy Audit: not applicable). |

**No new high-severity threats.** SITE-01 adds a read-only derived display column behind the existing admin auth gate; it introduces no new endpoints, no new inputs, no new secrets, and no new network calls. ASVS V4/V5 unchanged.
</threat_model>

<verification>
- `DeckFlow.Web.Tests` builds with 0 errors and the four new `AdminContentKbController` publish-state facts pass.
- `DeckFlow.Web` builds with 0 errors (Razor view compiles).
- Full suite green before phase verify: `dotnet test --no-build`.
- Carve-outs intact: `{ get; init; }` preserved on new properties; raw-string literals untouched (none in this plan); LF line endings preserved; `kb-status--local-newer` only in `admin-common.css`.
</verification>

<success_criteria>
- `/Admin/ContentKb` shows a Publish State column rendering the same four states as Studio (Never published / Pushed-hidden / Published / Local-newer) — Roadmap Phase 57 Success Criterion 1.
- Existing admin columns unchanged; empty-filter colspan updated to 6.
- PublishState derived solely through `PublishStateDeriver` (single source of truth, PUB-02 reuse).
- Web app starts without `InvalidOperationException` (deriver registered).
</success_criteria>

<output>
Create `.planning/phases/57-admin-surface-distill-quality/57-01-SUMMARY.md` when done.
</output>
