---
phase: 50-code-style-enforcement
plan: 04
type: execute
wave: 3
depends_on: ["50-01", "50-02", "50-03"]
files_modified:
  - CLAUDE.md
  - README.md
autonomous: false
requirements: [FMT-05, FMT-03, FMT-04]
user_setup: []

must_haves:
  truths:
    - "Project CLAUDE.md no longer contains a blanket 'never reformat' / 'DO NOT run Format Document' prohibition"
    - "CLAUDE.md states .editorconfig is the enforced source of truth, the five carve-outs remain protected and override conflicting prefs, new/changed code must pass the gate, and existing files are not mass-reflowed"
    - "All five carve-out specifics (init accessors, raw-string indent, attribute placement, switch expressions, xmldoc indent) are still present in CLAUDE.md — not deleted"
    - "The hook install step (git config core.hooksPath .githooks) is documented"
    - "FMT-03 is proven both directions via two scratch test PRs (mis-formatted added line FAILS CI; legacy one-line edit PASSES); FMT-04 is proven by the hook blocking a bad staged commit and allowing a clean one"
    - "The FMT-02 CarveOutGuard fixtures are proven to have actually RUN (not skipped) and passed in CI — confirmed from the CI log (50-03-SUMMARY), not merely compiled"
  artifacts:
    - path: "CLAUDE.md"
      provides: "Source-of-truth formatting model replacing the never-reformat prohibition (FMT-05)"
      contains: "source of truth"
  key_links:
    - from: "CLAUDE.md"
      to: ".editorconfig + the gate + the five carve-outs"
      via: "rewritten Formatting constraint"
      pattern: "editorconfig|carve-out|format-gate|core.hooksPath"
---

<objective>
Rewrite the project `CLAUDE.md` Formatting rule from the blanket "never reformat" prohibition to the `.editorconfig`-source-of-truth model (FMT-05), document the pre-commit hook install, and PROVE the gate behaviorally: two scratch test PRs for FMT-03 (mis-formatted added line fails; legacy one-line edit passes), a hook block/allow check for FMT-04, and confirmation that the FMT-02 CarveOutGuard fixtures (Plan 03) actually RAN (not skipped) and passed in CI.

Purpose: Close the documentation requirement and turn the wave-2 mechanism into proven behavior. The five carve-out specifics must be RETAINED in CLAUDE.md, not deleted — only the "forbidden to reformat" framing changes. This plan is also the phase gate where the CarveOutGuard test (Plan 03) is proven to have executed (not just compiled) in CI — which is why this plan depends on 50-03.

Output: Rewritten `CLAUDE.md` Formatting section, hook-install docs (CLAUDE.md + README), and recorded behavioral proof of FMT-03/FMT-04 plus the FMT-02 CarveOutGuard CI run.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/50-code-style-enforcement/50-SPEC.md
@.planning/phases/50-code-style-enforcement/50-CONTEXT.md
@.planning/phases/50-code-style-enforcement/50-02-SUMMARY.md
@.planning/phases/50-code-style-enforcement/50-03-SUMMARY.md
@CLAUDE.md

<interfaces>
<!-- CLAUDE.md target lines (current). The Write/Edit MUST touch ONLY these, no whole-file reflow. -->
<!-- Line 19 (Constraints > Formatting): the blanket "DO NOT run Format Document / Code Cleanup / ReSharper-style -->
<!--   reformatting across existing files ... any deviation is the formatter being wrong" prohibition. -->
<!--   This is the string to REPLACE. The five carve-out specifics embedded in it (init/attribute/raw-string/switch/LF) -->
<!--   MUST be carried into the new wording, not dropped. -->
<!-- Line 106 (Code Style): "No `.editorconfig` or `.prettierrc` checked in — formatting is by convention/IDE defaults." -->
<!--   This is now STALE (a .editorconfig exists and is enforced). Correct it in the same edit. -->

<!-- New model to state (SPEC REQ-5 / D-04 / D-05): -->
<!--   * .editorconfig is the ENFORCED, tool-agnostic source of truth (read by dotnet format, VS, Rider). -->
<!--   * The five carve-outs remain protected and OVERRIDE any conflicting preference (list all five specifics). -->
<!--   * New/changed code must satisfy the changed-lines gate (pre-commit hook + CI format-gate job). -->
<!--   * Existing files are NOT mass-reflowed (the gate is changed-lines-only). -->
<!--   * Hook install: one-time `git config core.hooksPath .githooks`; CI format-gate is the authoritative enforcer. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite the CLAUDE.md Formatting constraint to the source-of-truth model + fix the stale no-editorconfig line + document hook install</name>
  <files>CLAUDE.md, README.md</files>
  <read_first>
    - CLAUDE.md (lines 10-19 Constraints/Formatting; line 106 stale no-editorconfig claim)
    - .planning/phases/50-code-style-enforcement/50-SPEC.md (REQ-5 acceptance: blanket prohibition gone, carve-out specifics retained)
    - .planning/phases/50-code-style-enforcement/50-02-SUMMARY.md (hook install model, format-gate job name, gate behavior + mode)
    - .editorconfig (confirm the five carve-outs the new wording references)
    - README.md (locate where a "Development" / contributing section belongs for the hook-install note)
  </read_first>
  <action>
    Edit ONLY the relevant lines (no whole-file reflow; preserve LF).
    1. Replace the line-19 Formatting constraint: remove the blanket "DO NOT run Format Document / Code Cleanup / ReSharper-style reformatting across existing files ... the formatter being wrong" prohibition. Write the source-of-truth model: `.editorconfig` is the enforced, tool-agnostic source of truth (read by `dotnet format`, VS, Rider); new/changed code must pass the changed-lines gate (pre-commit hook + CI `format-gate` job); existing files are NOT mass-reflowed; the five bug-driven carve-outs remain protected and OVERRIDE any conflicting preference. RETAIN all five carve-out specifics verbatim in meaning: (1) never auto-convert `{ get; init; }` to `{ get; }` (JsonSerializer skips get-only props in .NET 9+ — broke EdhTop16Client), (2) never inline `[Attribute]` onto the property line, (3) never re-indent C# raw-string literals (changes the literal shipped to the AI), (4) preserve switch expressions, (5) preserve xmldoc single-space indent. Keep the LF line-endings note.
    2. Fix the stale line-106 claim "No `.editorconfig` ... checked in — formatting is by convention/IDE defaults" -> state that `.editorconfig` IS checked in and is the enforced source of truth (reference the gate).
    3. Document the hook install: add a short "Code formatting gate" note to CLAUDE.md (or the appropriate Constraints/Workflow area) AND to README (Development/Contributing area) covering the one-time `git config core.hooksPath .githooks` opt-in, that the CI `format-gate` job is the authoritative enforcer, and that the gate is changed-lines-only. Update README only where behavior is now described (CLAUDE.md Constraints note: "README updated when behavior changes").
    Do NOT edit the global `~/.claude/CLAUDE.md` (out of scope). Do NOT weaken or remove any carve-out.
  </action>
  <verify>
    <automated>! grep -q 'DO NOT run "Format Document"' CLAUDE.md &amp;&amp; ! grep -qi 'never reformat' CLAUDE.md &amp;&amp; grep -qi 'source of truth' CLAUDE.md &amp;&amp; grep -q 'core.hooksPath' CLAUDE.md &amp;&amp; grep -q 'get; init;' CLAUDE.md &amp;&amp; grep -qi 'raw-string' CLAUDE.md &amp;&amp; grep -qi 'switch expression' CLAUDE.md &amp;&amp; grep -qi 'attribute' CLAUDE.md &amp;&amp; ! grep -q 'No `.editorconfig`' CLAUDE.md &amp;&amp; echo OK</automated>
  </verify>
  <acceptance_criteria>
    - CLAUDE.md no longer contains the blanket `DO NOT run "Format Document"` / "never reformat" prohibition.  (SPEC AC-6)
    - CLAUDE.md states `.editorconfig` is the enforced source of truth, the gate is changed-lines-only, existing files are not mass-reflowed, and the five carve-outs override conflicting prefs.  (SPEC REQ-5)
    - All five carve-out specifics still present: `{ get; init; }`, `[Attribute]` own-line, raw-string indent, switch expressions, xmldoc indent.  (SPEC AC-6 "specifics still present")
    - The stale "No `.editorconfig` ... checked in" line is corrected.
    - `git config core.hooksPath .githooks` install step documented in CLAUDE.md and README.  (SPEC REQ-4 documented)
    - `git diff` for this task shows ONLY CLAUDE.md and README changing — no source reflow, LF preserved.  (SPEC AC-5)
  </acceptance_criteria>
  <done>CLAUDE.md replaces the never-reformat rule with the source-of-truth model (carve-out specifics retained), the stale no-editorconfig line is fixed, and the hook install is documented in CLAUDE.md + README.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Prove FMT-03 (two scratch test PRs), FMT-04 (hook block/allow), and the FMT-02 CarveOutGuard CI run</name>
  <read_first>
    - scripts/format-check-changed.sh (the gate engine being proven)
    - .githooks/pre-commit (the hook being proven)
    - .github/workflows/ci.yml (the format-gate job)
    - DeckFlow.Core.Tests/CarveOutGuardTests.cs (the FMT-02 guard — must be observed RUNNING in CI, not skipped)
    - .planning/phases/50-code-style-enforcement/50-03-SUMMARY.md (notes the STATIC trait-based design: [Trait("Category","CarveOutGuard")], CI runs them via the existing unfiltered `dotnet test` while local convention excludes them with `--filter Category!=CarveOutGuard`; NO runtime Assert.Skip since xUnit 2.9.3 lacks it; the dotnet format mode it mirrors)
    - .planning/phases/50-code-style-enforcement/50-SPEC.md (REQ-3 / REQ-4 acceptance; phase Acceptance Criteria 3 + 4)
  </read_first>
  <action>Behavioral proof, operator-driven (cannot be a unit test — FMT-03 is live-CI, FMT-04 is a scripted local commit). The operator runs the hook block/allow/legacy-pass sequence locally and opens two throwaway scratch PRs (one mis-formatted added line, one legacy one-line edit) to observe the CI format-gate verdict, then deletes the scratch branches. The operator ALSO confirms from the CI test logs that the four FMT-02 CarveOutGuard fixtures actually EXECUTED (four fixtures ran, exit 0 — not reported as Skipped). Because the fixtures use STATIC trait exclusion (no runtime Assert.Skip — xUnit 2.9.3 has none), the existing CI `dotnet test DeckFlow.sln` run (which applies no `Category!=CarveOutGuard` filter) executes them; "Skipped" would therefore indicate an unexpected filter or a [Trait]/discovery problem, NOT an env-signal miss. The agent records all outcomes in the SUMMARY.</action>
  <what-built>The shared gate script, the pre-commit hook, the CI format-gate job (Plans 01/02), and the FMT-02 CarveOutGuard test (Plan 03). This checkpoint proves they behave correctly end-to-end — these are live-CI/scripted proofs, not unit tests — including that the CarveOutGuard fixtures truly ran in CI rather than being skipped.</what-built>
  <how-to-verify>
    FMT-04 (local hook):
    1. Run the one-time opt-in: `git config core.hooksPath .githooks`.
    2. Stage a `.cs` change that ADDS a deliberately mis-formatted line (e.g. wrong indentation / bad brace whitespace). Attempt `git commit` -> EXPECT the hook to BLOCK with a message naming the offending `file:line`.
    3. Fix the line (or stage a cleanly-formatted change), `git commit` -> EXPECT success.
    4. Edit ONE line in a legacy `.cs` file that has pre-existing non-carve-out quirks elsewhere; stage + commit -> EXPECT success (the legacy quirks are not on the changed line — proves the formatter-status-captured / off-hunk-ignored behavior from FIX HIGH 1).
    FMT-03 (CI):
    5. Open a scratch test PR (or push a scratch branch) whose diff ADDS a mis-formatted line -> EXPECT the `format-gate` job to FAIL; CI logs show the gate ran, printed the chosen base ref, and named the line (proves FIX HIGH 2 Windows->WSL-mount path-normalization caught the added line, and FIX HIGH 3 base logging).
    6. Open a scratch test PR that ONLY touches one line in a legacy file (no new violation on the changed line) -> EXPECT `format-gate` to PASS while build-and-test also runs (proves legacy off-hunk violations don't false-fail).
    7. Confirm overall CI is green with `format-gate` present on a clean PR, and that the CI log shows the SELECTED BASE REF line (FIX HIGH 3 acceptance — including, where applicable, the empty-tree sentinel note when BASE would equal HEAD).
    FMT-02 (CarveOutGuard RAN in CI — runtime proof, not just compile):
    8. In the same clean CI run, open the `build-and-test` test-job logs and confirm the four CarveOutGuard fixtures (init / raw-string / attribute / switch) executed and PASSED — i.e. they show as Passed, NOT Skipped. The fixtures are STATICALLY trait-tagged (no runtime Assert.Skip — xUnit 2.9.3 has none) and the CI `dotnet test DeckFlow.sln` run applies no exclusion filter, so they MUST appear as run. A "Skipped" or "0 run" result means a stray `Category!=CarveOutGuard` filter, a missing `[Trait]`, or a discovery problem — FMT-02 would NOT actually be enforced. Record the observed run count (4) + exit 0.
    9. Delete the scratch branches/PRs afterward (they are throwaway proofs).
  </how-to-verify>
  <verify>Operator reports: hook blocked the bad commit + allowed the clean + allowed the legacy edit; CI format-gate FAILED the mis-format PR and PASSED the legacy PR; the CI log printed the chosen base ref; overall CI green; AND the four CarveOutGuard fixtures show as Passed (ran via the unfiltered CI `dotnet test`, not skipped) in the CI test log.</verify>
  <acceptance_criteria>
    - Hook BLOCKS a mis-formatted staged commit with a file:line message; ALLOWS a clean commit.  (SPEC AC-4 / FMT-04)
    - Hook ALLOWS a one-line edit in a quirky legacy file (proves FIX HIGH 1 — formatter status captured, off-hunk legacy violations ignored).  (SPEC REQ-3 legacy-passes case, local mirror)
    - CI `format-gate` FAILS the mis-formatted-added-line PR (logs show the gate ran and named the line — proves FIX HIGH 2 Windows->WSL-mount path normalization).  (SPEC AC-3 / FMT-03)
    - CI `format-gate` PASSES the legacy-one-line PR.  (SPEC AC-3 / FMT-03)
    - The CI log prints the chosen base ref (FIX HIGH 3 acceptance; empty-tree sentinel noted if BASE would equal HEAD).
    - Overall CI green with `format-gate` present on a clean PR.  (SPEC AC-7)
    - The four FMT-02 CarveOutGuard fixtures show as PASSED (executed via the unfiltered CI `dotnet test`, NOT Skipped) in the CI test-job log; operator records the run count (4) and exit 0.  (FMT-02 runtime proof, not just compile-clean)
    - Scratch branches/PRs deleted after proof.
    - If GitHub branch protection requires named checks, operator confirms `format-gate` was added to the required set (RESEARCH A2) — or notes none is configured.
  </acceptance_criteria>
  <done>FMT-03 proven both directions in CI (with chosen base ref logged), FMT-04 proven via hook block/allow/legacy-pass, and the four FMT-02 CarveOutGuard fixtures confirmed RUN (not skipped) and passed in CI; scratch artifacts deleted; outcomes recorded in the SUMMARY.</done>
  <resume-signal>Type "proven" with the two PR outcomes (fail/pass), the hook block/allow/legacy-pass result, the logged base ref, and the CarveOutGuard CI run result (4 passed, not skipped), or describe any deviation.</resume-signal>
</task>

</tasks>

<verification>
- CLAUDE.md greps: blanket prohibition gone, source-of-truth + hooksPath present, all five carve-out specifics retained, stale line fixed.
- `git diff` for Task 1 shows only CLAUDE.md + README.
- Task 2 records: hook block + allow + legacy-pass; CI fail-PR + pass-PR + clean-green + logged base ref; and the four CarveOutGuard fixtures Passed (ran via the unfiltered CI `dotnet test`, not skipped) in CI.
</verification>

<success_criteria>
- The blanket "never reformat" rule is replaced with the enforced-source-of-truth model, carve-out specifics retained, hook install documented.
- FMT-03 proven both directions in CI; FMT-04 proven via hook block/allow/legacy-pass; FMT-02 CarveOutGuard proven to have RUN (not skipped) in CI.
- Phase git diff shows no whole-file reflow — only config/CI/hook/test/doc files changed.
</success_criteria>

<output>
Create `.planning/phases/50-code-style-enforcement/50-04-SUMMARY.md` when done. Record the two test-PR outcomes, the hook block/allow/legacy-pass result, the logged CI base ref (and empty-tree-sentinel note if applicable), the CarveOutGuard CI run result (4 fixtures passed, not skipped), and the branch-protection status (A2).
</output>
