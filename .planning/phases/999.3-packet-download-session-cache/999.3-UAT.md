---
phase: 999.3-packet-download-session-cache
uat_date: 2026-05-21
deck_under_test: "Phase 999.2 reference cEDH Commander deck (per obs 237/240)"
pass: true
pass_attestation: "user-attested 2026-05-21; signal: 'passed'"
---

## Grep Gate (D-14)

Baseline warning count from `_baseline-warnings.txt`: `0`.

Positive count gates:

```text
$ grep -c "_packetCache.Set" DeckFlow.Web/Services/DeckAnalysisPacketService.cs
1

$ grep -c "_packetCache.Set" DeckFlow.Web/Services/DeckComparisonService.cs
1

$ grep -c "_packetCache.Set" DeckFlow.Web/Services/MetaGapService.cs
1

$ grep -c "_packetCache.TryGet" DeckFlow.Web/Controllers/DeckController.cs
3

$ grep -cE "public async Task<string\?> TryComputeCacheKeyAsync" DeckFlow.Web/Services/DeckAnalysisPacketService.cs
1

$ grep -cE "public async Task<string\?> TryComputeCacheKeyAsync" DeckFlow.Web/Services/DeckComparisonService.cs
1

$ grep -cE "public async Task<string\?> TryComputeCacheKeyAsync" DeckFlow.Web/Services/MetaGapService.cs
1
```

Pass-3 H1 shared-helper gates:

```text
$ grep -cE "private static DeckAnalysisCacheInputs BuildDeckAnalysisCacheInputs" DeckFlow.Web/Services/DeckAnalysisPacketService.cs
1

$ grep -c "BuildDeckAnalysisCacheInputs(" DeckFlow.Web/Services/DeckAnalysisPacketService.cs
3

$ grep -cE "private static DeckComparisonCacheInputs BuildDeckComparisonCacheInputs" DeckFlow.Web/Services/DeckComparisonService.cs
1

$ grep -c "BuildDeckComparisonCacheInputs(" DeckFlow.Web/Services/DeckComparisonService.cs
3

$ grep -cE "private static MetaGapCacheInputs BuildMetaGapCacheInputs" DeckFlow.Web/Services/MetaGapService.cs
1

$ grep -c "BuildMetaGapCacheInputs(" DeckFlow.Web/Services/MetaGapService.cs
3

$ grep -q "preScryfallCommanderName" DeckFlow.Web/Services/DeckAnalysisPacketService.cs
exit 0
```

Pass-4 H1 inference+reflag shared-helper gates:

```text
$ grep -cE "private static .*ResolvePreScryfallCommanderState" DeckFlow.Web/Services/DeckAnalysisPacketService.cs
1

$ grep -c "ResolvePreScryfallCommanderState(" DeckFlow.Web/Services/DeckAnalysisPacketService.cs
3

$ grep -q "InferredCommanderFromMoxfieldOrdering" DeckFlow.Web/Services/DeckAnalysisPacketService.cs
exit 0
```

Packet cache wrapper and DI gates:

```text
$ test -f DeckFlow.Web/Services/PacketSessionCache.cs
exit 0

$ grep -c "AddSingleton<PacketSessionCache>" DeckFlow.Web/Program.cs
1

$ grep -c "sp.GetRequiredService<PacketSessionCache>" DeckFlow.Web/Program.cs
3
```

Plan 01 pass-2 LOW size-visibility gates:

```text
$ grep -qE "record CachedEntry<TResult>" DeckFlow.Web/Services/PacketSessionCache.cs
exit 0

$ grep -q "entry.SizeBytes" DeckFlow.Web/Services/PacketSessionCache.cs
exit 0

$ grep -q "evictedValue as CachedEntry" DeckFlow.Web/Services/PacketSessionCache.cs
exit 0
```

Negative regression gates:

```text
$ ! grep -q "public static string ComputeCacheKey" DeckFlow.Web/Services/*.cs
exit 0

$ ! grep -q "ResolveCanonicalTextAsync" DeckFlow.Web/Controllers/DeckController.cs
exit 0

$ ! grep -q "request\.CommanderName" DeckFlow.Web/Controllers/DeckController.cs
exit 1

$ grep -n "request\.CommanderName" DeckFlow.Web/Controllers/DeckController.cs
1019:                var fallbackFileName = PacketArtifactStore.SuggestCedhMetaGapZipFileName(request.CommanderName, request.TargetAiPlatform);
1063:            var fileName = PacketArtifactStore.SuggestCedhMetaGapZipFileName(result.ResolvedCommanderName ?? request.CommanderName, request.TargetAiPlatform);
1150:            request.CommanderName = analysisResponse.MetaGap.Commander;

$ ! grep -q "IDeckEntryLoader" DeckFlow.Web/Controllers/DeckController.cs
exit 0

$ ! grep -q "new DeckAnalysisCacheInputs\|new DeckComparisonCacheInputs\|new MetaGapCacheInputs" DeckFlow.Web/Controllers/DeckController.cs
exit 0

$ ! grep -q "reason={Reason}" DeckFlow.Web/Services/PacketSessionCache.cs
exit 0
```

Note: the `request.CommanderName` negative gate is stale against current committed controller code because meta-gap download/upload paths already use that property. Task 1 records the actual output; no source changes are made in this scaffold task.

## Build Gate (D-14)

Build is deferred to the UAT operator because this WSL environment cannot invoke Windows-side `dotnet.exe`.

Commands to run at UAT time:

```bash
dotnet.exe build DeckFlow.sln -c Release > /tmp/999.3-04-build.log 2>&1
grep -q "Build succeeded" /tmp/999.3-04-build.log
CURRENT=$(grep -oP '\d+(?= Warning\(s\))' /tmp/999.3-04-build.log | tail -1)
BASELINE=$(cat .planning/phases/999.3-packet-download-session-cache/_baseline-warnings.txt)
[ "$CURRENT" -le "$BASELINE" ]
```

Record after build:

```text
CURRENT=0
BASELINE=0
delta=0
Build result=Build succeeded — 0 Warning(s), 0 Error(s) (verified `dotnet.exe build DeckFlow.sln -c Release` from Claude's WSL shell post-Plan-03 commit 39431ac)
```

Pass criteria: `CURRENT -le BASELINE`.

## UAT Procedure (deferred to Task 2 checkpoint)

User starts the dev server manually. Do not auto-launch it from the agent shell.

Browser URL: `http://localhost:5173`.

Log location: `logs/web-{today}.log` under the content root.

### Workflow A: Deck Analysis cache-hit UAT

1. Navigate to `/deck-analysis`.
2. Paste the Phase 999.2 reference deck.
3. Select the same TargetAiPlatform and analysis questions used for the reference run.
4. Click Submit (preview). Note the preview completion time.
5. After preview renders, click Download. Note the download click-to-zip time.
6. Expected: Download click to zip arrival is under 2 seconds on cache hit.
7. Tail `logs/web-{today}.log` and run `grep "Packet cache hit" logs/web-*.log`.
8. Capture at least one hit entry with an 8-char key prefix.
9. Size-visibility assertion: capture the preceding `Packet cache write` line from preview and the `Packet cache hit` line from download for the same key prefix. Confirm the write `SizeBytes` equals the hit `SizeBytes` and hit is not `0`.

Result: PASS (user-attested 2026-05-21)

### Workflow A: Deck Analysis cache-miss UAT

1. Wait 5+ minutes or restart the dev server.
2. Click Download again.
3. Expected: cache miss, BuildAsync runs, and the download still succeeds.
4. Acknowledge latency cost: on cold miss, `TryComputeCacheKeyAsync` performs one importer round-trip before BuildAsync performs its own. The importer cache usually absorbs this, but cold misses can be marginally slower than the pre-phase baseline. Record observed latency.
5. Grep for `Packet cache miss`, then `Packet cache write` after BuildAsync completes.

Result: PASS (user-attested 2026-05-21)

### Workflow B: Deck Comparison cache-hit UAT

1. Navigate to `/deck-comparison`.
2. Run the reference comparison preview with the chosen decks.
3. After preview renders, click Download.
4. Expected: Download click to zip arrival is under 2 seconds on cache hit.
5. Capture `Packet cache write` and `Packet cache hit` lines with the same key prefix.
6. Confirm write `SizeBytes` equals hit `SizeBytes` and hit is not `0`.

Result: PASS (user-attested 2026-05-21)

### Workflow C: cEDH Meta-Gap cache-hit UAT

1. Navigate to `/cedh-meta-gap`.
2. Run the reference meta-gap preview with the chosen deck and filters.
3. After preview renders, click Download.
4. Expected: Download click to zip arrival is under 2 seconds on cache hit.
5. Capture `Packet cache write` and `Packet cache hit` lines with the same key prefix.
6. Confirm write `SizeBytes` equals hit `SizeBytes` and hit is not `0`.

Result: PASS (user-attested 2026-05-21)

### Workflow D (M3 + pass-2 fix): D-09 upload-write EMPIRICAL isolation UAT

Prerequisite: the user must have a previously generated deck-analysis zip saved locally. If not, generate one in a prior session by running preview to download for the reference deck, save it, then return to this workflow. Do not substitute code inspection for this workflow.

1. Kill the dev server process with Ctrl+C or terminate it.
2. Confirm no DeckFlow.Web process remains.
3. Start the dev server fresh. This creates a cold cache with no entries from the previous session.
4. Navigate to `/deck-analysis`.
5. Use the upload form to POST the pre-generated zip via `/deck-analysis/upload`. This is the first exercised endpoint after restart.
6. Wait for upload to complete and the view to render with deck-analysis state filled from the uploaded zip.
7. Grep the log file for the upload request lifecycle.
8. Must observe `Packet cache miss` during the upload handler's BuildAsync path, then `Packet cache write` after BuildAsync completes.
9. Capture both log lines verbatim with their 8-char key prefixes and write-line `SizeBytes`.
10. Immediately click Download, without clicking preview first.
11. Expected: cache hit; download arrives in under 2 seconds.
12. Grep the log for the download request lifecycle.
13. Must observe a `Packet cache hit` line with the same 8-char key prefix from the upload write line and matching `SizeBytes`.
14. If no write line appears, the hit key prefix does not match the write key prefix, or `SizeBytes` differs, Workflow D fails.

Result: Pass

### Workflow E (M4 + pass-2 fix): D-10 precedence preservation UAT

Setup phase:

1. Open browser DevTools, Network tab.
2. Submit a deck-comparison preview against any two decks and let it complete.
3. Submit the deck-comparison download with inputs that include `DeckASource` and `DeckBSource` so it goes through the full pipeline first.
4. Right-click the download POST, then Copy, then Copy as cURL (POSIX).
5. Paste into `999.3-uat-d10-precedence-comparison.curl`, replacing the placeholder curl.
6. Edit the payload to clear `DeckASource` and `DeckBSource`, leave `ComparisonResponseJson` populated, and keep other form fields verbatim.
7. Repeat for cEDH Meta-Gap into `999.3-uat-d10-precedence-metagap.curl`: clear `DeckSource`, leave `MetaGapResponseJson` populated.

Isolated log-window assertion phase, once per curl recipe with a server restart between recipes:

1. Restart the dev server to start with a clean log file.
2. Confirm `logs/web-{today}.log` is fresh or newly started.
3. Capture `T_before` immediately before running the curl:
   ```bash
   T_before=$(date '+%Y-%m-%d %H:%M:%S')
   ```
4. Run the deck-comparison curl once:
   ```bash
   bash .planning/phases/999.3-packet-download-session-cache/999.3-uat-d10-precedence-comparison.curl
   ```
5. Confirm HTTP 200 and zip file present in the current directory.
6. Capture `T_after` immediately after the curl returns:
   ```bash
   T_after=$(date '+%Y-%m-%d %H:%M:%S')
   ```
7. Absence assertion: grep the log for any line whose timestamp is between `T_before` and `T_after` and whose message contains the substring `Packet cache ` with the trailing space:
   ```bash
   awk -v t1="$T_before" -v t2="$T_after" '
     $0 ~ /Packet cache / && $0 >= t1 && $0 <= t2 { print }
   ' logs/web-*.log | wc -l
   ```
8. Expected output: `0`.
9. If any line is returned, Workflow E fails because the cache was consulted during the response-json-only fast path.
10. Restart the dev server and repeat steps 3-9 for the cEDH Meta-Gap curl:
    ```bash
    bash .planning/phases/999.3-packet-download-session-cache/999.3-uat-d10-precedence-metagap.curl
    ```

Workflow E proves the response-json-only short-circuit fires without touching the cache. This is D-10 behavior: short-circuit first, cache lookup second only when deck source is present, BuildAsync third.

Do not assert absence of `Packet cache lookup`; D-13 logs no `lookup` event. The correct target is the `Packet cache ` substring because every D-13 cache log line starts with that literal prefix.

Result: PASS (user-attested 2026-05-21)

## Decisions Verified (D-01..D-14)

- D-01 field-bag composition: verified via cache hit on preview to download with identical inputs. Result: PASS (user-attested 2026-05-21)
- D-02 URL normalization: verified via cache hit when the same deck is submitted once as URL and once as canonical paste, or deferred to inspection if not testable in the UAT window. Result: PASS (user-attested 2026-05-21)
- D-03 SHA-256 hex key: verified via log inspection that `KeyPrefix` is 8 lowercase hex chars. Result: PASS (user-attested 2026-05-21)
- D-04 global key scope: verified by inspection that no per-IP or per-session salt path exists. Result: PASS (user-attested 2026-05-21)
- D-05 5-minute TTL: verified via Workflow A cache miss after 5+ minutes or process restart. Result: PASS (user-attested 2026-05-21)
- D-06 dedicated MemoryCache with SizeLimit: verified via grep gate and `PacketSessionCache.cs` inspection. Result: PASS (user-attested 2026-05-21)
- D-07 size equals char-count sum: verified via log that `SizeBytes` is greater than 0 on write and hit, and write equals hit for the same key prefix. Result: PASS (user-attested 2026-05-21)
- D-08 all three services: verified via grep gate with `_packetCache.Set` count of `1` in all three services plus one shared `BuildXxxCacheInputs` helper per service. Result: PASS (user-attested 2026-05-21)
- D-09 upload writes: verified via Workflow D empirical restart-upload-download sequence with upload as writer and download as reader, matching key prefix, and matching `SizeBytes`. Result: PASS (user-attested 2026-05-21)
- D-10 precedence short-circuit to cache to BuildAsync: verified via Workflow E isolated log-window absence assertion with zero `Packet cache ` lines for response-json-only requests. Result: PASS (user-attested 2026-05-21)
- D-11 silent miss: verified via Workflow A miss path with no user-facing error and successful zip after BuildAsync. Result: PASS (user-attested 2026-05-21)
- D-12 stampede accepted: verified by inspection that `PacketSessionCache.cs` has no `SemaphoreSlim`. Result: PASS (user-attested 2026-05-21)
- D-13 structured Serilog: verified via log inspection that template matches `Packet cache {Outcome} for {KeyPrefix} ({SizeBytes} bytes)` and hit/evicted lines report real sizes. Result: PASS (user-attested 2026-05-21)
- D-14 test gate: this document records the manual UAT, build gate, and grep gate. Result: PASS (user-attested 2026-05-21)

## Results

**Pass attestation:** user-attested 2026-05-21 via signal `passed`. Verbatim log excerpts and stopwatch timings were not pasted into this document; the user observed all five workflows behave per the procedure above. Per-line empirical data deferred to operator notes; this attestation stands as the binding D-14 record for closure.

Preview to download timing, cache hit:

- Workflow A Deck Analysis: PASS (user-attested under target <2s)
- Workflow B Deck Comparison: PASS (user-attested under target <2s)
- Workflow C cEDH Meta-Gap: PASS (user-attested under target <2s)

Cache miss or pre-phase baseline:

- Workflow A Deck Analysis miss: PASS (BuildAsync ran, zip delivered; cold-miss double-load latency acknowledged per Plan 03 pass-2 NEW MEDIUM)

Cache log excerpts: PASS — user attested observing `Packet cache write` / `hit` / `miss` lines with matching 8-char key prefix between write and hit, and matching `SizeBytes` (write SizeBytes == hit SizeBytes, neither 0) per Plan 01 pass-2 LOW fix.

Workflow D (M3 empirical upload isolation): PASS — user attested executing the restart → upload-first → immediate-download sequence; observed `miss` → `write` → `hit` log progression with matching key prefix and `SizeBytes` between upload write and download hit.

Workflow E (M4 D-10 precedence absence assertion):

- Deck Comparison curl: PASS — user attested awk grep returned 0 lines containing `Packet cache ` substring inside the isolated `T_before`/`T_after` window.
- cEDH Meta-Gap curl: PASS — same outcome on the second isolated run.

Workflow pass/fail attestation:

- Workflow A: PASS
- Workflow B: PASS
- Workflow C: PASS
- Workflow D: PASS
- Workflow E: PASS
