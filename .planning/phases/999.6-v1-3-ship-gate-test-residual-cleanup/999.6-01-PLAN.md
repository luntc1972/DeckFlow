---
phase: 999.6-v1-3-ship-gate-test-residual-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - DeckFlow.Web.Tests/PacketArtifactStoreTests.cs
  - DeckFlow.Web.Tests/AiPlatformPhase10RoundTripTests.cs
  - DeckFlow.Web.Tests/PacketArtifactStoreRoundTripTests.cs
  - DeckFlow.Web.Tests/DeckFlow.Web.Tests.csproj
  - DeckFlow.Web.Tests/Fixtures/arna-test/00-input-summary.txt
  - DeckFlow.Web.Tests/Fixtures/arna-test/01-request-context.txt
  - DeckFlow.Web.Tests/Fixtures/arna-test/40-deck-profile.json
  - DeckFlow.Web.Tests/Fixtures/arna-test/51-set-upgrade-response.json
autonomous: true
requirements:
  - D-01
  - D-02
  - D-03
  - D-04
  - D-05
  - D-06
  - D-07
  - D-08
  - D-17
  - D-18
  - D-19
  - D-20
  - D-21
  - D-22

must_haves:
  truths:
    - "D-01: 3-plan split (P01 mechanical / P02 BasicAuth debug+fix / P03 Archidekt debug+fix) — this plan is P01."
    - "D-02: Strict sequential P01 → P02 → P03; no parallel dispatch. Each plan's full-suite gate must show incremental progress toward Failed 0."
    - "D-17: Strict P01 → P02 → P03 sequential — this is P01, no upstream waves."
    - "D-08: P01 atomicity — 4 commits per cluster, NOT one mega-commit. Sequence: D-04 msg update → D-05 Theory prefix → D-07 deletion → D-06 fixture conversion. Each commit body carries the focused-test triplet per [[feedback-run-and-update-tests]]."
    - "D-18: Atomic commits per cluster — 4 commits land in this plan (D-04, D-05, D-07, D-06)."
    - "D-19: Plain default-author commits as luntc1972 <luntc1972@yahoo.com>; no Co-Authored-By trailer."
    - "D-20: Build gate `/mnt/c/Program Files/dotnet/dotnet.exe build DeckFlow.sln -c Release -nologo` exits 0 warnings / 0 errors before every commit."
    - "D-21: Each fix task runs the focused `--filter` gate; final task runs the full-suite gate and reports Passed/Failed/Skipped triplet in commit body."
    - "D-22: Codex codes all test edits (autonomous: true, dispatched via cross_ai_command). Orchestrator runs the Windows-side dotnet gate and commits when Codex sandbox can't reach the WSL vsock — hybrid pattern inherited from 999.5 P01 SUMMARY."
    - "D-03: No new `[Skip]` directives. Only authorized deletion is the D-07 dead-premise test."
    - "P01 fix outcome: 6 of 9 residual failures resolved (1 PacketArtifactStore msg + 3 AiPlatform Theory rows + 1 RoundTrip fixture + 1 dead-premise delete). Full-suite delta target: `Failed 3` post-P01."
  deliverables:
    - decision: D-04
      where: "DeckFlow.Web.Tests/PacketArtifactStoreTests.cs:57"
      change: "Replace expected message string with the verbatim production string from PacketArtifactStore.cs:218."
    - decision: D-05
      where: "DeckFlow.Web.Tests/AiPlatformPhase10RoundTripTests.cs:704"
      change: "`Assert.StartsWith(\"atraxa-compare2-\", fileName)` → `Assert.StartsWith(\"atraxa-comparison-\", fileName)` (covers all 3 InlineData rows in the Theory)."
    - decision: D-06
      where: "DeckFlow.Web.Tests/PacketArtifactStoreRoundTripTests.cs:15 + DeckFlow.Web.Tests.csproj + new Fixtures/arna-test/ directory"
      change: "Replace `/tmp/arna-test` hard-coded path with embedded fixture under DeckFlow.Web.Tests/Fixtures/arna-test/, copy-to-output. If fixture source files cannot be recovered, delete the test (authorized fallback per D-06)."
    - decision: D-07
      where: "DeckFlow.Web.Tests/AiPlatformPhase10RoundTripTests.cs:747-754"
      change: "Delete the `SuggestComparisonZipFileName_includes_compare2_page_segment` Fact block in full (no orphaned helpers)."
  key_links:
    - from: "DeckFlow.Web.Tests/PacketArtifactStoreTests.cs"
      to: "DeckFlow.Web/Services/PacketArtifactStore.cs"
      via: "Production message string copied verbatim — read SUT first, never paraphrase."
    - from: "DeckFlow.Web.Tests/AiPlatformPhase10RoundTripTests.cs Theory rows"
      to: "DeckFlow.Web/Services/PacketArtifactStore.cs:533-534 (SuggestComparisonZipFileName emitter)"
      via: "Production emits `{commander}-comparison-{platform}-{timestamp}.zip` — prefix must match verbatim."
    - from: "DeckFlow.Web.Tests/Fixtures/arna-test/**/*"
      to: "DeckFlow.Web.Tests.csproj `<Content>` block"
      via: "`<Content CopyToOutputDirectory=\"PreserveNewest\">` mirrors DeckFlow.Web/Help/**/*.md production pattern; consumed via `Path.Combine(AppContext.BaseDirectory, \"Fixtures\", \"arna-test\", <file>)`."
---

<objective>
Close D-04, D-05, D-06, D-07 — six of the nine residual test failures blocking v1.3 ship. All four edits are mechanical: catch stale tests up to shipped production renames (PacketArtifactStore msg generalization + Phase 12 page-rename `compare2` → `comparison`), convert the dev-machine-only Arna fixture to an embedded fixture (or delete it if the source files are unrecoverable), and delete the dead-premise `compare2_page_segment` Fact whose contract no longer exists in production.

Purpose: After this plan lands, the full-suite gate moves from `Failed 9` → `Failed 3`, leaving only the flaky BasicAuth fact (P02) and the two Archidekt cache-job ambiguous failures (P03) for sequential triage.

Output: 4 atomic commits per D-08 (D-04 msg update → D-05 Theory prefix update → D-07 deletion → D-06 embedded-fixture conversion or test deletion). Final full-suite gate result recorded in the closing-task commit body per D-21.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/999.6-v1-3-ship-gate-test-residual-cleanup/999.6-CONTEXT.md
@.planning/phases/999.5-v1-3-backlog-catch-up-test-hardening/999.5-01-SUMMARY.md
@.planning/codebase/TESTING.md
@CLAUDE.md

<interfaces>
<!-- Production SUT message strings + filename emitters consumed by these tests. -->
<!-- Codex should NOT re-derive these; read the SUT file directly to confirm verbatim. -->

From DeckFlow.Web/Services/PacketArtifactStore.cs:218 (D-04 source of truth):
```
throw new InvalidOperationException("Imported zip did not contain a recognized DeckFlow session — expected 01-request-context.txt, 40-deck-profile.json, or 51-set-upgrade-response.json.");
```

From DeckFlow.Web/Services/PacketArtifactStore.cs:530-534 (D-05 source of truth):
```csharp
public static string SuggestPacketZipFileName(string? commanderName, string? targetAiPlatform = null)
    => $"{CreateSafePathSegment(commanderName, "deck-analysis")}-analysis-{CreateSafePathSegment(targetAiPlatform, "chatgpt")}-{DateTime.UtcNow:yyyyMMdd-HHmmss}.zip";

public static string SuggestComparisonZipFileName(string? commanderName, string? targetAiPlatform = null)
    => $"{CreateSafePathSegment(commanderName, "deck-comparison")}-comparison-{CreateSafePathSegment(targetAiPlatform, "chatgpt")}-{DateTime.UtcNow:yyyyMMdd-HHmmss}.zip";
```

Embedded fixture analog from DeckFlow.Web.csproj (D-06 cited pattern):
```xml
<ItemGroup>
  <Content Include="Help\**\*.md">
    <CopyToOutputDirectory>PreserveNewest</CopyToOutputDirectory>
  </Content>
</ItemGroup>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update PacketArtifactStoreTests msg-string assertion (D-04)</name>
  <files>DeckFlow.Web.Tests/PacketArtifactStoreTests.cs</files>
  <read_first>
    - DeckFlow.Web.Tests/PacketArtifactStoreTests.cs (full file — Fact `LoadFromZip_throws_when_no_response_json_present` at line 43, assertion at line 57)
    - DeckFlow.Web/Services/PacketArtifactStore.cs (lines 200-225, around `throw new InvalidOperationException` at line 218 — the verbatim source-of-truth string)
    - .planning/phases/999.6-v1-3-ship-gate-test-residual-cleanup/999.6-CONTEXT.md §D-04
  </read_first>
  <action>
    Update the expected message string on `PacketArtifactStoreTests.cs:57`. The current `Assert.Equal` argument is the old string `"Imported zip did not contain 40-deck-profile.json or 51-set-upgrade-response.json."`. The production SUT at `PacketArtifactStore.cs:218` now emits a generalized message — read that file first, then copy the production string into the assertion VERBATIM (project rule: no paraphrasing per CLAUDE.md "raw-string literal preservation"). No SUT change. No other line touched. Preserve LF line endings and `.editorconfig` formatting; do NOT auto-format the file.
  </action>
  <verify>
    <automated>"/mnt/c/Program Files/dotnet/dotnet.exe" test DeckFlow.sln -c Release --nologo --filter "FullyQualifiedName~PacketArtifactStoreTests"</automated>
  </verify>
  <acceptance_criteria>
    - Focused-test gate `--filter "FullyQualifiedName~PacketArtifactStoreTests"` reports `Failed: 0` (full PacketArtifactStoreTests class green).
    - Specifically, `LoadFromZip_throws_when_no_response_json_present` transitions from FAIL → PASS.
    - Build gate `/mnt/c/Program Files/dotnet/dotnet.exe build DeckFlow.sln -c Release -nologo` exits 0 warnings 0 errors.
    - Commit message: `fix(999.6): update PacketArtifactStoreTests msg-string for AI-agnostic rename (D-04)`
    - Commit body contains the focused-test triplet line `Passed X, Failed 0, Skipped Z, filter: FullyQualifiedName~PacketArtifactStoreTests` (with actual counts, no placeholders).
    - Commit author is `luntc1972 <luntc1972@yahoo.com>`, no `Co-Authored-By` trailer.
    - Diff touches ONLY line ~57 of `PacketArtifactStoreTests.cs` (no formatter sweep, no other-line edits).
  </acceptance_criteria>
  <done>Stale msg-string drift closed; 1 of 6 stale failures resolved.</done>
</task>

<task type="auto">
  <name>Task 2: Update AiPlatform Theory prefix from compare2 → comparison (D-05)</name>
  <files>DeckFlow.Web.Tests/AiPlatformPhase10RoundTripTests.cs</files>
  <read_first>
    - DeckFlow.Web.Tests/AiPlatformPhase10RoundTripTests.cs (lines 697-705 — `SuggestComparisonZipFileName_includes_lowercased_ai_name` Theory; one `Assert.StartsWith` covers all 3 InlineData rows since the Theory uses `[MemberData(nameof(AllPlatforms))]`)
    - DeckFlow.Web/Services/PacketArtifactStore.cs (lines 533-534 — `SuggestComparisonZipFileName` emitter is the source of truth for the prefix)
    - .planning/phases/999.6-v1-3-ship-gate-test-residual-cleanup/999.6-CONTEXT.md §D-05
  </read_first>
  <action>
    On `AiPlatformPhase10RoundTripTests.cs:704`, change `Assert.StartsWith("atraxa-compare2-", fileName);` to `Assert.StartsWith("atraxa-comparison-", fileName);`. Confirm by reading `PacketArtifactStore.cs:533-534` that the production emitter produces `{commander}-comparison-{platform}-{timestamp}.zip` (so `atraxa-comparison-` is the literal prefix for commanderName `"Atraxa"`). Single-line edit covers all 3 platform rows (Claude / ChatGPT / Gemini) because the Theory dispatches them through one assertion. Do NOT touch the sibling `Assert.Contains($"-{expectedSegment}-", fileName);` on line 703. Do NOT touch any other Fact / Theory in this file in this task — the D-07 deletion is a separate commit. Preserve LF line endings; do not auto-format.
  </action>
  <verify>
    <automated>"/mnt/c/Program Files/dotnet/dotnet.exe" test DeckFlow.sln -c Release --nologo --filter "FullyQualifiedName~AiPlatformPhase10RoundTripTests.SuggestComparisonZipFileName_includes_lowercased_ai_name"</automated>
  </verify>
  <acceptance_criteria>
    - Focused gate `--filter "FullyQualifiedName~AiPlatformPhase10RoundTripTests.SuggestComparisonZipFileName_includes_lowercased_ai_name"` reports `Passed: 3, Failed: 0, Skipped: 0`.
    - Build gate exits 0 warnings 0 errors.
    - Commit message: `fix(999.6): update AiPlatform Theory prefix to comparison after Phase 12 page rename (D-05)`
    - Commit body contains the focused-test triplet line.
    - Commit author plain default per D-19.
    - Diff touches ONLY line ~704 of `AiPlatformPhase10RoundTripTests.cs`.
  </acceptance_criteria>
  <done>3 of 6 stale failures resolved (one assertion line covers Claude + ChatGPT + Gemini Theory rows).</done>
</task>

<task type="auto">
  <name>Task 3: Delete dead-premise compare2_page_segment Fact (D-07)</name>
  <files>DeckFlow.Web.Tests/AiPlatformPhase10RoundTripTests.cs</files>
  <read_first>
    - DeckFlow.Web.Tests/AiPlatformPhase10RoundTripTests.cs (lines 738-774 — verify the Fact at lines 747-754 has no helper methods reaching outside its own block; verify the sibling Facts `SuggestPacketZipFileName_includes_analysis_page_segment` (738-745) and `SuggestCedhMetaGapZipFileName_includes_cedh_page_segment` (756-763) STAY)
    - .planning/phases/999.6-v1-3-ship-gate-test-residual-cleanup/999.6-CONTEXT.md §D-07
  </read_first>
  <action>
    Delete the `[Fact]` block at `AiPlatformPhase10RoundTripTests.cs` lines 747-754 (`public void SuggestComparisonZipFileName_includes_compare2_page_segment()`). Premise is dead — no production path emits `-compare2-` after Phase 12 URL rename. The 3 Theory rows updated in Task 2 already cover the `-comparison-` prefix contract via `Assert.StartsWith`. The neighboring `_includes_analysis_page_segment` (lines 738-745) and `_includes_cedh_page_segment` (lines 756-763) Facts stay byte-identical — DO NOT touch them. Delete only the 8 lines (`[Fact]` attribute through closing `}`). Leave one blank line separator above and below to keep file formatting consistent. No other deletion or edit. Preserve LF line endings; do not auto-format.
  </action>
  <verify>
    <automated>"/mnt/c/Program Files/dotnet/dotnet.exe" test DeckFlow.sln -c Release --nologo --filter "FullyQualifiedName~AiPlatformPhase10RoundTripTests"</automated>
  </verify>
  <acceptance_criteria>
    - Focused gate `--filter "FullyQualifiedName~AiPlatformPhase10RoundTripTests"` reports `Failed: 0`.
    - Build gate exits 0 warnings 0 errors.
    - `grep -c "compare2_page_segment\|SuggestComparisonZipFileName_includes_compare2_page_segment" DeckFlow.Web.Tests/AiPlatformPhase10RoundTripTests.cs` returns `0`.
    - `grep -c "SuggestPacketZipFileName_includes_analysis_page_segment" DeckFlow.Web.Tests/AiPlatformPhase10RoundTripTests.cs` still returns `1` (sibling unchanged).
    - `grep -c "SuggestCedhMetaGapZipFileName_includes_cedh_page_segment" DeckFlow.Web.Tests/AiPlatformPhase10RoundTripTests.cs` still returns `1` (sibling unchanged).
    - Commit message: `test(999.6): delete dead-premise compare2_page_segment Fact (D-07)`
    - Commit body contains focused-test triplet + 1-sentence rationale referencing "no production path emits -compare2-" + D-07.
    - Commit author plain default per D-19.
  </acceptance_criteria>
  <done>4 of 6 stale failures resolved; dead-premise test removed cleanly.</done>
</task>

<task type="auto">
  <name>Task 4: Convert Arna fixture to embedded resource — or delete test if fixture unrecoverable (D-06)</name>
  <files>DeckFlow.Web.Tests/PacketArtifactStoreRoundTripTests.cs, DeckFlow.Web.Tests/DeckFlow.Web.Tests.csproj, DeckFlow.Web.Tests/Fixtures/arna-test/00-input-summary.txt, DeckFlow.Web.Tests/Fixtures/arna-test/01-request-context.txt, DeckFlow.Web.Tests/Fixtures/arna-test/40-deck-profile.json, DeckFlow.Web.Tests/Fixtures/arna-test/51-set-upgrade-response.json</files>
  <read_first>
    - DeckFlow.Web.Tests/PacketArtifactStoreRoundTripTests.cs (full file — 67 lines; private static `FixtureDirectory = "/tmp/arna-test"` at line 15; `BuildFixtureZip()` at lines 45-58 reads 4 file names from that directory via `WriteEntry`)
    - DeckFlow.Web.Tests/DeckFlow.Web.Tests.csproj (full file — verify the existing ItemGroup pattern before adding the `<Content>` block)
    - DeckFlow.Web/DeckFlow.Web.csproj (search for the `<Content Include="Help\**\*.md">` block — analog pattern for `CopyToOutputDirectory="PreserveNewest"`)
    - .planning/phases/999.6-v1-3-ship-gate-test-residual-cleanup/999.6-CONTEXT.md §D-06 (especially the fallback authorization: "If files aren't recoverable from dev machine, delete the test instead with explicit DISCUSSION-LOG entry — fixture-less integration assertion has no value.")
  </read_first>
  <action>
    Two-branch task — DECIDE which branch applies before editing:

    **Branch A — Fixture is recoverable from `/tmp/arna-test/` on the executor host:**
    1. Verify `/tmp/arna-test/00-input-summary.txt`, `/tmp/arna-test/01-request-context.txt`, `/tmp/arna-test/40-deck-profile.json`, `/tmp/arna-test/51-set-upgrade-response.json` all exist (`ls /tmp/arna-test/`).
    2. Verify NO secrets / API keys / PII in the four files (threat-model gate — these get committed to a public repo). Spot-grep each file for `password`, `api_key`, `token`, `secret`, `Bearer`, `@gmail`, `@yahoo`. If any hit, STOP and escalate to orchestrator before committing.
    3. Create directory `DeckFlow.Web.Tests/Fixtures/arna-test/` (relative to repo root). Copy the 4 fixture files into it. Use LF line endings on text files (`.editorconfig` enforces).
    4. Edit `DeckFlow.Web.Tests/DeckFlow.Web.Tests.csproj` — add a new `<ItemGroup>` block (placement: just before the closing `</Project>` or appended after the existing ProjectReference group). Use this exact shape (analog of the `Help\**\*.md` pattern in `DeckFlow.Web.csproj`):
       - `<Content Include="Fixtures\arna-test\**\*">` with `<CopyToOutputDirectory>PreserveNewest</CopyToOutputDirectory>` child element.
    5. Edit `PacketArtifactStoreRoundTripTests.cs:15` — change the `FixtureDirectory` initializer to `Path.Combine(AppContext.BaseDirectory, "Fixtures", "arna-test")`. Keep the rest of the file (Fact body, `BuildFixtureZip`, `WriteEntry`) byte-identical.

    **Branch B — Fixture files do NOT exist on executor host:**
    1. Confirm `/tmp/arna-test/` is absent (or contains 0 of the 4 expected files).
    2. DELETE the entire file `DeckFlow.Web.Tests/PacketArtifactStoreRoundTripTests.cs`. Authorized by D-06 fallback clause ("fixture-less integration assertion has no value"). No orphaned references — the class is `public sealed` with no consumers outside its own file.
    3. Verify by `grep -rn "PacketArtifactStoreRoundTripTests" DeckFlow.Web.Tests/` returns 0 hits after deletion.
    4. No csproj edit needed in Branch B (no fixture directory to register).

    Preserve LF line endings throughout; do not auto-format any file. Do NOT touch `PacketArtifactStoreTests.cs` (Task 1 already handled it).
  </action>
  <verify>
    <automated>"/mnt/c/Program Files/dotnet/dotnet.exe" build DeckFlow.sln -c Release -nologo &amp;&amp; "/mnt/c/Program Files/dotnet/dotnet.exe" test DeckFlow.sln -c Release --nologo --filter "FullyQualifiedName~PacketArtifactStoreRoundTripTests"</automated>
  </verify>
  <acceptance_criteria>
    - Build gate `dotnet build DeckFlow.sln -c Release -nologo` exits 0 warnings 0 errors.
    - **Branch A acceptance:** focused gate `--filter "FullyQualifiedName~PacketArtifactStoreRoundTripTests"` reports `Passed: 1, Failed: 0`; `DeckFlow.Web.Tests/Fixtures/arna-test/` directory exists with 4 files; `DeckFlow.Web.Tests.csproj` contains one new `<Content Include="Fixtures\arna-test\**\*">` block with `<CopyToOutputDirectory>PreserveNewest</CopyToOutputDirectory>`; PII / secret grep returned 0 hits across the 4 fixture files.
    - **Branch B acceptance:** focused gate `--filter "FullyQualifiedName~PacketArtifactStoreRoundTripTests"` reports `Passed: 0, Failed: 0, Total: 0` (class no longer exists); `find DeckFlow.Web.Tests -name "PacketArtifactStoreRoundTripTests.cs"` returns nothing; csproj unchanged.
    - Commit message Branch A: `test(999.6): embed Arna fixture as test asset to remove dev-path dependency (D-06)`
    - Commit message Branch B: `test(999.6): delete PacketArtifactStoreRoundTripTests — Arna fixture unrecoverable, integration assertion has no value (D-06 fallback)`
    - Commit body in either branch carries the focused-test triplet line + rationale + branch identifier (Branch A / Branch B).
    - Commit author plain default per D-19.
  </acceptance_criteria>
  <done>6 of 6 P01 stale failures resolved; PacketArtifactStoreRoundTripTests no longer dev-machine-only.</done>
</task>

<task type="auto">
  <name>Task 5: Closing full-suite gate + plan SUMMARY</name>
  <files>.planning/phases/999.6-v1-3-ship-gate-test-residual-cleanup/999.6-01-SUMMARY.md</files>
  <read_first>
    - .planning/phases/999.6-v1-3-ship-gate-test-residual-cleanup/999.6-01-PLAN.md (this file — to extract must_haves status)
    - .planning/phases/999.5-v1-3-backlog-catch-up-test-hardening/999.5-01-SUMMARY.md (reference template for SUMMARY format)
  </read_first>
  <action>
    Run the full-suite gate `"/mnt/c/Program Files/dotnet/dotnet.exe" test DeckFlow.sln -c Release --nologo` (no `--filter`). Capture the closing triplet `Passed X, Failed Y, Skipped Z, Total N` from the dotnet test summary line. Target post-P01: `Failed 3` (the residual 3 are P02 BasicAuth × 1 + P03 ArchidektCacheJob × 2). If `Failed` is greater than 3 OR `Skipped` is greater than 3 (the pre-phase baseline), STOP and surface the regression — do not write the SUMMARY until investigated.

    Then create `.planning/phases/999.6-v1-3-ship-gate-test-residual-cleanup/999.6-01-SUMMARY.md` using the 999.5-01-SUMMARY.md format as a template. Include: files modified list, scope statement, decisions delivered (D-04, D-05, D-06, D-07, D-08, D-17..D-22 honored, D-03 honored — no new Skip), per-cluster outcome (Branch A or Branch B for D-06 noted explicitly), build gate result, full-suite gate result with exact triplet, residual 3 failures explicitly enumerated (`BasicAuthMiddlewareTests.CorrectCredentials_InvokesNext` + `ArchidektCacheJobServiceTests.BackgroundService_SucceedsAndUpdatesProcessedCounts` + `ArchidektCacheJobServiceTests.GetActiveJob_ReturnsNullAfterCompletedJob` — all carried forward to P02 + P03), 4-commit hash sequence from Tasks 1-4, closure commit hash placeholder.

    Commit the SUMMARY as the closing artifact of this plan.
  </action>
  <verify>
    <automated>test -f .planning/phases/999.6-v1-3-ship-gate-test-residual-cleanup/999.6-01-SUMMARY.md &amp;&amp; grep -c "Failed 3\|Failed: 3" .planning/phases/999.6-v1-3-ship-gate-test-residual-cleanup/999.6-01-SUMMARY.md</automated>
  </verify>
  <acceptance_criteria>
    - Full-suite gate exits with `Failed: 3, Skipped: 3` (or Skipped 3 = unchanged baseline; Failed at most 3 — must be exactly 3 unless D-06 Branch B reduced total by 1, in which case `Total: 444 Failed: 3` is also acceptable).
    - File `.planning/phases/999.6-v1-3-ship-gate-test-residual-cleanup/999.6-01-SUMMARY.md` exists.
    - SUMMARY contains an explicit "Residual failures carried forward" section enumerating the 3 remaining failures by full test name + which downstream plan owns them (P02 / P03).
    - SUMMARY contains the full-suite triplet line `Passed X, Failed 3, Skipped 3, Total <444|445>` from the closing run.
    - SUMMARY contains the 4 commit hashes from Tasks 1-4.
    - Commit message: `docs(999.6): summary for plan 01 mechanical stale-test cleanup`
    - Commit author plain default per D-19.
  </acceptance_criteria>
  <done>P01 closed; full-suite at `Failed 3`; P02 unblocked.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo → public GitHub | `luntc1972/DeckFlow` is a public repo. Any file committed in Task 4 Branch A (Arna fixture text/JSON) is world-visible. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-999.6-P01-01 | Information Disclosure | DeckFlow.Web.Tests/Fixtures/arna-test/* (Task 4 Branch A) | mitigate | Pre-commit grep sweep over the 4 fixture files for `password`, `api_key`, `token`, `secret`, `Bearer`, `@gmail`, `@yahoo`. Any hit blocks commit and escalates to orchestrator. Documented in Task 4 action step 2. |
| T-999.6-P01-02 | Tampering | None applicable | accept | No production code modified; no new dependencies; no package manager installs. Threat surface limited to test-asset content. |

No new threat surface introduced by D-04/D-05/D-07 (test assertion string updates + dead-Fact deletion only). No npm/pip/cargo installs in this plan — Package Legitimacy Gate not applicable.
</threat_model>

<verification>
- Each task's focused `--filter` gate exits with `Failed: 0` on the affected class(es).
- Task 5 closing full-suite gate exits with `Failed: 3, Skipped: 3` (the 3 residuals are P02 + P03 scope, explicitly enumerated in SUMMARY).
- Build gate `dotnet build DeckFlow.sln -c Release -nologo` exits 0 warnings 0 errors after every commit.
- All 4 fix commits + 1 SUMMARY commit authored by `luntc1972 <luntc1972@yahoo.com>` with no `Co-Authored-By` trailer (per D-19).
- Every commit body carries the test-triplet `Passed X, Failed Y, Skipped Z, filter: <expression>` per D-21 / `[[feedback-run-and-update-tests]]`.
- Skipped count unchanged from pre-phase baseline (3) per D-03 — no `[Skip]` directive added in any commit.
</verification>

<success_criteria>
1. Pre-phase failure count `9` reduced to `3` after Task 5 closing gate.
2. Skipped count stays at `3` (no new `[Skip]` added; one fact possibly deleted in D-06 Branch B + one fact deleted in D-07 — Total may drop from 445 to 444 or 443).
3. 4 atomic commits per D-08 sequence (Task 1 → Task 2 → Task 3 → Task 4) + 1 closing SUMMARY commit.
4. No production SUT touched (`DeckFlow.Web/Services/PacketArtifactStore.cs` etc. unchanged byte-for-byte).
5. P02 (BasicAuth flaky) and P03 (Archidekt cache-job) failures enumerated in SUMMARY as the carry-forward residuals.
</success_criteria>

<output>
Create `.planning/phases/999.6-v1-3-ship-gate-test-residual-cleanup/999.6-01-SUMMARY.md` when done (Task 5).
</output>
