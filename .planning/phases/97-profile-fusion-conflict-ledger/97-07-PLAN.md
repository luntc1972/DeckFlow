---
phase: 97-profile-fusion-conflict-ledger
plan: 07
type: execute
wave: 4
depends_on: [97-01, 97-05]
files_modified:
  - DeckFlow.Studio/Program.cs
  - DeckFlow.Studio/Pages/CreatorStyleLedger.razor
  - DeckFlow.Studio/Shared/NavMenu.razor
autonomous: false
requirements: [CS-19, CS-18]
user_setup: []
must_haves:
  truths:
    - "A new read-only Studio Blazor page at /creator-style-ledger loads the fused profile by creator slug and renders one say-vs-do row per (metric, condition)."
    - "Each row shows: stated band, measured value + numDecks/coverage, resolved target, verdict badge (agree / conflict / insufficient-measured / philosophy-stated-only) with the VerdictReason (low-sample | no-condition-breakdown) surfaced as subtext/tooltip when insufficient-measured, and source-clip link + video_date (D-12)."
    - "The page is strictly read-only — no recompute/write action; ICreatorStyleProfileStore is registered in Studio DI so the page resolves."
    - "The flagship board-wipes 'agree' (deviates-from-canon-but-matches-own-philosophy) case is visible, not hidden as a conflicts-only view would."
  artifacts:
    - path: "DeckFlow.Studio/Pages/CreatorStyleLedger.razor"
      provides: "Read-only say-vs-do ledger page (D-11/D-12)"
      contains: "creator-style-ledger"
    - path: "DeckFlow.Studio/Program.cs"
      provides: "ICreatorStyleProfileStore singleton registration"
      contains: "AddSingleton<ICreatorStyleProfileStore>"
  key_links:
    - from: "DeckFlow.Studio/Pages/CreatorStyleLedger.razor"
      to: "ICreatorStyleProfileStore.GetBySlugAsync"
      via: "OnInitializedAsync loads FusedTargets and renders D-12 rows"
      pattern: "GetBySlugAsync"
---

<objective>
Add the CS-19 say-vs-do conflict ledger as a new READ-ONLY Studio Blazor page, neighbor to
`CreatorSources.razor`, rendering the persisted `FusedTarget[]` as D-12 rows. Register
`ICreatorStyleProfileStore` in Studio DI (currently absent) and add a nav link.

Purpose: This is the only admin-visible surface of the phase. D-11 explicitly waives the
public/theme/mobile/Playwright web-page rule — Studio is loopback-only local tooling. Wave 4 —
needs the extended schema (01) and works against data produced by the CLI trigger (05/06).

Output: One read-only Blazor page + DI registration + nav entry. Verification = build-green + a
manual smoke row (VALIDATION.md).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/97-profile-fusion-conflict-ledger/97-CONTEXT.md
@.planning/phases/97-profile-fusion-conflict-ledger/97-PATTERNS.md

<interfaces>
Studio DI registration pattern (DeckFlow.Studio/Program.cs:90-91, contentKbDatabasePath at line 75):
  builder.Services.AddSingleton<IContentSourceStore>(_ => new ContentSourceStore(contentKbDatabasePath));
  builder.Services.AddSingleton<IContentVideoStore>(_ => new ContentVideoStore(contentKbDatabasePath));
  // ADD: AddSingleton<ICreatorStyleProfileStore>(_ => new CreatorStyleProfileStore(contentKbDatabasePath));

Page analog (DeckFlow.Studio/Pages/CreatorSources.razor): @page, @inherits StudioCancellableComponentBase,
  [Inject] store, OnInitializedAsync -> Task.Run(() => store.GetBySlugAsync(...), Cts.Token),
  loading/error/empty-state markup (lines 37-62). COPY the read path + states; do NOT copy its
  write actions (_operationInFlight, add/remove — lines 13-35, 180-259).

Nav (DeckFlow.Studio/Shared/NavMenu.razor): existing <NavLink class="nav-link" href="creators">…</NavLink> list.

FusedTarget render fields (from Plan 01): Metric, Condition, Value (resolved target), Weight, Source,
  StatedMin/StatedMax, MeasuredValue, NumDecks, EffectiveSampleSize, Verdict, SourceClip, VideoDateUtc, Confidence, Conflict.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register ICreatorStyleProfileStore in Studio DI</name>
  <files>DeckFlow.Studio/Program.cs</files>
  <read_first>
    - DeckFlow.Studio/Program.cs (lines 75, 90-91 — contentKbDatabasePath + the sibling singleton registrations; confirm no existing ICreatorStyleProfileStore registration)
    - DeckFlow.Core/Content/CreatorStyleProfileStore.cs (constructor signature — takes the db path)
  </read_first>
  <action>
    Add `builder.Services.AddSingleton<ICreatorStyleProfileStore>(_ => new
    CreatorStyleProfileStore(contentKbDatabasePath));` alongside the existing IContentSourceStore /
    IContentVideoStore registrations at lines 90-91. Add the `using DeckFlow.Core.Content;` import if
    not already present. Preserve LF endings and existing ordering conventions.
  </action>
  <verify>
    <automated>/mnt/c/Program\ Files/dotnet/dotnet.exe build DeckFlow.Studio</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "AddSingleton<ICreatorStyleProfileStore>" DeckFlow.Studio/Program.cs` returns a hit.
    - DeckFlow.Studio builds clean.
  </acceptance_criteria>
  <done>The ledger page's store dependency resolves in Studio DI.</done>
</task>

<task type="auto">
  <name>Task 2: CreatorStyleLedger.razor read-only page + nav link</name>
  <files>DeckFlow.Studio/Pages/CreatorStyleLedger.razor, DeckFlow.Studio/Shared/NavMenu.razor</files>
  <read_first>
    - DeckFlow.Studio/Pages/CreatorSources.razor (full file — page skeleton lines 1-6, OnInitializedAsync + Task.Run + Cts.Token, loading/error/empty states 37-62; do NOT copy write actions)
    - DeckFlow.Studio/StudioCancellableComponentBase.cs (Cts, SafeStateHasChangedAsync)
    - DeckFlow.Studio/Shared/NavMenu.razor (the <NavLink> list convention)
    - .planning/phases/97-profile-fusion-conflict-ledger/97-CONTEXT.md (D-12 row contract)
    - .planning/phases/97-profile-fusion-conflict-ledger/97-PATTERNS.md (CreatorStyleLedger.razor section lines 336-422 — exact skeleton + table pattern)
  </read_first>
  <action>
    Create `DeckFlow.Studio/Pages/CreatorStyleLedger.razor` with BOTH `@page "/creator-style-ledger"`
    and `@page "/creator-style-ledger/{Slug}"` (route-param overload so the page is general, not baked to
    one creator), `@inherits StudioCancellableComponentBase`, `@using DeckFlow.Core.Content`,
    `@using DeckFlow.Core.Knowledge`. `[Inject] private ICreatorStyleProfileStore ProfileStore`.
    Add `[Parameter] public string? Slug { get; set; }`; resolve the effective slug as
    `Slug ?? "salubrioussnail"` (salubrioussnail is only the default when no route slug is supplied —
    do NOT hardcode it as the sole target). `OnInitializedAsync` loads `_profile = await Task.Run(() =>
    ProfileStore.GetBySlugAsync(Slug ?? "salubrioussnail", Cts.Token), Cts.Token)` inside try/catch(OperationCanceledException)/catch(Exception
    -> generic operator-safe `_error`, NEVER echo exception.Message)/finally(SafeStateHasChangedAsync).

    Render loading/error/empty states per the analog. When loaded, render a `<table class="table
    table-sm table-hover align-middle">` with one row per FusedTarget (D-12 columns): Metric,
    Condition, stated band (StatedMin–StatedMax), measured value + NumDecks/EffectiveSampleSize,
    resolved target (Value), a Verdict BADGE (map agree->success, conflict->danger, insufficient-measured
    ->secondary, philosophy-stated-only->info using Bootstrap `badge` classes already used in Studio).
    When Verdict=="insufficient-measured" AND VerdictReason is non-null, surface the reason as static
    subtext/tooltip on the badge so the operator sees WHY (D-12): `low-sample` (measured sample below
    MinDeckFloor) vs `no-condition-breakdown` (a condition-scoped stated rule with no measured segment
    to compare). Use a plain `title="@target.VerdictReason"` attribute or a muted `<small>` beneath the
    badge — static markup only, NO new interactivity (still read-only). Also render Weight, Confidence,
    and a source-clip link (SourceClip) + VideoDateUtc. Show superseded rows
    visually de-emphasized (e.g. muted) so history is legible but distinct from active targets. This is
    strictly READ-ONLY — no buttons, no _operationInFlight, no add/remove. Blazor auto-encodes all
    interpolated content; render SourceClip text as an encoded string (do NOT use MarkupString on
    content-KB data). Preserve LF endings.

    In NavMenu.razor add `<NavLink class="nav-link" href="creator-style-ledger">Style Ledger</NavLink>`
    following the existing NavLink list pattern.
  </action>
  <verify>
    <automated>/mnt/c/Program\ Files/dotnet/dotnet.exe build DeckFlow.Studio</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "creator-style-ledger" DeckFlow.Studio/Pages/CreatorStyleLedger.razor DeckFlow.Studio/Shared/NavMenu.razor` returns hits in both.
    - Page has NO write action: `grep -n "_operationInFlight\|@onclick" CreatorStyleLedger.razor` returns nothing (read-only).
    - Verdict badge mapping present for all four verdicts (agree/conflict/insufficient-measured/philosophy-stated-only).
    - When Verdict=="insufficient-measured", VerdictReason (low-sample | no-condition-breakdown) is rendered as static subtext/`title` tooltip on the badge (D-12 "show why") — no new interactive controls added (read-only preserved).
    - No `MarkupString` used on content-KB-sourced fields (XSS-safe render).
    - DeckFlow.Studio builds clean; solution build stays green.
  </acceptance_criteria>
  <done>Read-only ledger page renders D-12 rows with verdict badges + nav entry.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Studio ledger read-only smoke verification</name>
  <what-built>A read-only Studio Blazor page at /creator-style-ledger that renders the fused Salubrious Snail say-vs-do ledger (one row per metric/condition: stated band · measured + coverage · resolved target · verdict badge · source-clip + date). ICreatorStyleProfileStore registered in Studio DI; nav link added. Loopback-only — no theme/mobile/Playwright per D-11.</what-built>
  <how-to-verify>
    1. Ensure a fused profile exists: run `fuse-profile --slug salubrioussnail` (Plan 06) against the Studio content-kb.db (or seed one) so FusedTargets are persisted.
    2. Launch Studio: `dotnet run --project DeckFlow.Studio` (loopback).
    3. Open http://localhost:<port>/creator-style-ledger (default slug) AND http://localhost:<port>/creator-style-ledger/salubrioussnail (explicit route slug) — confirm both resolve the same profile (proves the page is slug-parameterized, not hardcoded).
    4. Confirm each (metric, condition) row shows: stated band, measured value + numDecks/coverage, resolved target, a verdict badge, and a source-clip link + video_date.
    5. Confirm the board-wipes row reads a positive/agree verdict (not a red conflict) — the flagship deviates-from-canon-but-matches-own-philosophy case must be legible.
    6. Confirm the page is read-only (no recompute/edit controls).
  </how-to-verify>
  <resume-signal>Type "approved" or describe rendering/verdict issues.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| content-KB data → Blazor render | Fused-profile fields (metric, source-clip text) sourced from the content KB are rendered in the ledger page. Studio is loopback-only, single operator, no auth surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-97-09 | Injection (XSS) | Rendering SourceClip / metric strings in Blazor | mitigate | Blazor auto-encodes interpolated content; do NOT use MarkupString on content-KB fields (acceptance-criteria grep gate). |
| T-97-10 | Information disclosure | Error path leaking DB path via exception.Message | mitigate | catch(Exception) sets a generic operator-safe `_error`; never echo exception.Message (mirrors CreatorSources.razor convention). |
| T-97-11 | Elevation / tampering | Accidental write from an intended read-only page | mitigate | Page has no store write call and no interactive controls (read-only grep gate); fusion writes happen only via the CLI (Plan 06). |
| T-97-SC | Tampering | npm/pip/cargo installs | accept | No package-manager installs in this plan. |
</threat_model>

<verification>
- `dotnet.exe build DeckFlow.Studio` and solution build clean.
- Read-only grep gate passes (no write controls, no MarkupString on KB data).
- Manual smoke: ledger renders D-12 rows with verdict badges; board-wipes shows agree.
</verification>

<success_criteria>
The operator can view a read-only say-vs-do ledger in Studio that reproduces the discriminating
verdicts (including the board-wipes agree case), with fusion triggered only via the CLI.
</success_criteria>

<output>
Create `.planning/phases/97-profile-fusion-conflict-ledger/97-07-SUMMARY.md` when done.
</output>
