---
phase: 20-content-kb-ingestion-transcription-local
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - DeckFlow.Core/Integration/ITranscriptSource.cs
  - DeckFlow.Core/Integration/IYouTubeTranscriptFetcher.cs
  - DeckFlow.Core/Integration/YouTubeTranscriptFetcher.cs
  - DeckFlow.Core/Integration/IYouTubeAudioSource.cs
  - DeckFlow.Core/Integration/YouTubeAudioSource.cs
  - DeckFlow.Core/Integration/TranscriptProviderFactory.cs
  - DeckFlow.Core/DeckFlow.Core.csproj
  - DeckFlow.Core.Tests/YouTubeTranscriptFetcherTests.cs
  - DeckFlow.Core.Tests/TranscriptProviderFactoryTests.cs
autonomous: true
requirements: [KB-03]
notes:
  - "No packages.lock.json is generated (GeneratePackageLockFile unset repo-wide) — the approved YoutubeExplode 6.6.0 add touches only DeckFlow.Core.csproj. Do NOT add packages.lock.json to files_modified."
  - "ALL services here are PURE (D-11): they persist NOTHING and take no store/ledger dependency. The harvest verb (20-04) is the single persistence owner."
  - "DURATION AUTHORITY (Codex iter2 NEW-HIGH): AudioDownloadResult.DurationSeconds is BEST-EFFORT and may be 0. The AUTHORITATIVE cap-projection duration is the lister-supplied knownDuration (PlaylistVideo.Duration), plumbed through ITranscriptSource.FetchTranscriptAsync → IWhisperTranscriptionService.TranscribeAsync. The cap check must NEVER see 0/unknown for a real video."
  - "MONTH-KEY CONSISTENCY (Codex iter2 MEDIUM): the harvest verb (20-04) derives ONE monthKey per video and passes it into FetchTranscriptAsync → TranscribeAsync for the cap check, then reuses the SAME monthKey local for the ledger RecordCallAsync. ITranscriptSource.FetchTranscriptAsync therefore carries a monthKey param."
must_haves:
  truths:
    - "YouTubeTranscriptFetcher returns a status-carrying result with caption text for a video that has caption tracks — persists nothing"
    - "When a video has zero caption tracks, the fetcher reports a NoCaptions outcome (Whisper-fallback signal), never throws"
    - "An empty/whitespace caption track is classified as NoCaptions/FailedEmptyCaptions — an empty body is NEVER returned as captions and NEVER persisted (MEDIUM-b)"
    - "Auto-generated caption tracks are accepted as valid captions (NOT treated as missing) — keeps whisper_fallback_ratio low (P2)"
    - "Each fetch surfaces caption_track_kind (auto_generated | manual) for structured logging"
    - "TranscriptFetchResult is non-null + status-carrying and also conveys Whisper billed seconds/cost so the harvest verb can write the ledger (HIGH-1/HIGH-3)"
    - "DECKFLOW_YOUTUBE_TRANSCRIPT_PROVIDER selects a transcript provider via a real factory: 'direct' resolves the YoutubeExplode fetcher; an unsupported value throws with a listed-supported-values message (SC2/P2 proxy-pluggable day-1)"
    - "IYouTubeAudioSource downloads a video's audio-only stream to a temp file and returns an AudioDownloadResult (temp path, filename, size bytes, duration seconds, IDisposable cleanup) — no audio retained beyond the cleanup handle (D-15)"
    - "AudioDownloadResult.DurationSeconds is documented as BEST-EFFORT (may be 0); the AUTHORITATIVE cap-projection duration is the lister-supplied knownDuration carried through TranscribeAsync — the cap check NEVER sees 0 for a real video (Codex iter2 NEW-HIGH — KB-05 cap bypass)"
    - "ITranscriptSource.FetchTranscriptAsync carries the authoritative knownDuration AND a verb-supplied monthKey, so the implementation can pass both into the Whisper cap projection and the ledger month matches the cap-check month (Codex iter2 NEW-HIGH + MEDIUM)"
  artifacts:
    - path: "DeckFlow.Core/Integration/ITranscriptSource.cs"
      provides: "ITranscriptSource abstraction (FetchTranscriptAsync carries knownDuration + monthKey) + non-null status-carrying TranscriptFetchResult (carries Whisper cost)"
      contains: "enum TranscriptOutcome"
    - path: "DeckFlow.Core/Integration/YouTubeTranscriptFetcher.cs"
      provides: "YoutubeExplode 6.6.0 caption fetch (auto-gen accepted, empty classified) + Func<> seam; persists nothing"
      contains: "TryGetByLanguage"
    - path: "DeckFlow.Core/Integration/TranscriptProviderFactory.cs"
      provides: "DECKFLOW_YOUTUBE_TRANSCRIPT_PROVIDER selector — direct wired, unsupported value fails clearly"
      contains: "DECKFLOW_YOUTUBE_TRANSCRIPT_PROVIDER"
    - path: "DeckFlow.Core/Integration/YouTubeAudioSource.cs"
      provides: "Audio-only stream download to temp file via YoutubeExplode + AudioDownloadResult with IDisposable cleanup; DurationSeconds documented best-effort/non-authoritative"
      contains: "GetAudioOnlyStreams"
    - path: "DeckFlow.Core.Tests/TranscriptProviderFactoryTests.cs"
      provides: "xUnit: direct resolves, unsupported value throws with supported-values message"
      contains: "TranscriptProviderFactory"
  key_links:
    - from: "DeckFlow.Core/Integration/YouTubeTranscriptFetcher.cs"
      to: "YoutubeExplode ClosedCaptions manifest"
      via: "GetManifestAsync + TryGetByLanguage + Tracks.Count == 0 gate + empty-body classification"
      pattern: "TryGetByLanguage"
    - from: "DeckFlow.Core/Integration/TranscriptProviderFactory.cs"
      to: "DECKFLOW_YOUTUBE_TRANSCRIPT_PROVIDER"
      via: "provider-toggle env read → factory dispatch"
      pattern: "DECKFLOW_YOUTUBE_TRANSCRIPT_PROVIDER"
    - from: "DeckFlow.Core/Integration/YouTubeAudioSource.cs"
      to: "YoutubeExplode Streams"
      via: "GetManifestAsync → GetAudioOnlyStreams().GetWithHighestBitrate() → DownloadAsync(streamInfo, tempPath)"
      pattern: "GetAudioOnlyStreams"
---

<objective>
Build the YouTube caption fetcher, the `ITranscriptSource` abstraction, the real provider-selection factory, and the YouTube audio-download contract in `DeckFlow.Core/Integration`, following the `ArchidektApiDeckImporter` resilience precedent and the `CardLookupService` Func<> test seam. This is KB-03 and SC1/SC2 of the (amended) phase. ALL services here are PURE per D-11 — they return status-carrying results and persist NOTHING; the harvest verb (20-04) is the single persistence owner.

Purpose: Captions-first ingestion. Accepting auto-generated captions (not just manual) keeps `whisper_fallback_ratio` under 25% (Pitfall P2). The provider factory satisfies SC2 (proxy-pluggable day-1): `direct` is wired, an unsupported value fails clearly. `TryGetByLanguage` (never the throwing `GetByLanguage`) avoids exceptions on the common MTG case. The `IYouTubeAudioSource` contract (Codex HIGH-2) gives the Whisper fallback (20-03) and harvest verb (20-04) a concrete audio path: temp file + size + duration + cleanup handle. The non-null `TranscriptFetchResult` (Codex HIGH-3) carries the Whisper billed seconds/cost so the verb (single persistence owner, D-11) can write the ledger row from it. DURATION AUTHORITY (Codex iter2 NEW-HIGH): `AudioDownloadResult.DurationSeconds` is best-effort and can be 0 when the YoutubeExplode stream manifest lacks a duration; the authoritative duration for the cap projection (KB-05) is the lister-supplied `knownDuration` that `FetchTranscriptAsync` accepts — so the cap check (20-03) never projects a $0 cost for a caption-less video and silently transcribes it for free. MONTH-KEY (Codex iter2 MEDIUM): `FetchTranscriptAsync` also carries a verb-supplied `monthKey`, so the cap-check month and the verb's ledger-write month are identical.
Output: `ITranscriptSource` (FetchTranscriptAsync carries knownDuration + monthKey) + non-null status-carrying `TranscriptFetchResult`; `IYouTubeTranscriptFetcher` + `YouTubeTranscriptFetcher` with Func<> seam (empty-track classified); `TranscriptProviderFactory` (real toggle); `IYouTubeAudioSource` + `YouTubeAudioSource` + `AudioDownloadResult` (DurationSeconds documented best-effort/non-authoritative); YoutubeExplode 6.6.0 added to DeckFlow.Core.csproj; xUnit tests via delegate seams.

SC5 AMENDMENT (D-03/D-04): ROADMAP SC5 mandated `IHttpClientFactory` + `ResiliencePipelineProvider<string>` named clients. That is a DeckFlow.Web-host concept the Core/CLI home cannot use. This phase amends SC5 to the Core/Integration shape: HttpClient injected via constructor + Polly built DIRECTLY (ArchidektApiDeckImporter precedent) + `internal` Func<> test ctor. ZERO `new HttpClient()` in Core/Integration method bodies; NO Microsoft.Extensions.Http added to Core. The SPIRIT of SC5 (named resilience, no raw clients, test seam) is preserved.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-content-kb-ingestion-transcription-local/20-CONTEXT.md
@.planning/phases/20-content-kb-ingestion-transcription-local/20-RESEARCH.md

NOTE: PLANS SUPERSEDE 20-RESEARCH.md where they differ. Specifically: the result type is the non-null `TranscriptFetchResult` (NOT the nullable `TranscriptFetchResult?` shown in RESEARCH Pattern 3), channel listing uses `CollectAsync(limit)` (NOT `.Take().ToListAsync()`), and missing ffmpeg warns+marks-failed (never throws). RESEARCH carries a top supersede note to the same effect.

<interfaces>
<!-- From DeckFlow.Core/Integration/ArchidektApiDeckImporter.cs — resilience + DI ctor pattern to copy: -->
- `private static readonly AsyncRetryPolicy<RestResponse> RetryPolicy = Policy<RestResponse>.HandleResult(...).WaitAndRetryAsync(retryCount, sleepDurationProvider, onRetry);`
- production ctor takes an optional injected dependency (`RestClient? = null`); falls back to a real one only in the ctor, never in a method.

<!-- From DeckFlow.Web/Services/CardLookupService.cs:50-89 — Func<> delegate test-seam pattern to copy: -->
- private readonly `Func<...>` field assigned from an optional `...Override` ctor parameter (`_executeAsync = executeAsyncOverride ?? (real impl)`)
- internal ctor exposes the override; production ctor delegates to it. [assembly: InternalsVisibleTo("DeckFlow.Core.Tests")] already present in DeckFlow.Core/AssemblyInfo.cs.

<!-- From DeckFlow.Core/Knowledge/ContentModels.cs -->
- `TranscriptSource.Captions = "captions"`, `TranscriptSource.Whisper = "whisper"`
- `TranscriptStatus.Captions/.Whisper/.Failed/.SkippedOverCap`
- `ContentSourceType.Youtube = "youtube_channel"`

<!-- YoutubeExplode 6.6.0 verified API facts (RESEARCH HIGH confidence) -->
- `youtube.Videos.ClosedCaptions.GetManifestAsync(videoId, ct)` → `ClosedCaptionManifest`
- `manifest.Tracks` is `IReadOnlyList<ClosedCaptionTrackInfo>`; `Tracks.Count == 0` is the only true no-captions gate
- `manifest.TryGetByLanguage(string)` returns null on miss (safe); `GetByLanguage` THROWS — never use it
- `ClosedCaptionTrackInfo.IsAutoGenerated` is a bool; accept auto-generated as valid captions
- `youtube.Videos.ClosedCaptions.GetAsync(trackInfo, ct)` → track; `track.Captions.Select(c => c.Text)`
- `YoutubeClient` accepts an `HttpClient` ctor argument
- AUDIO: `youtube.Videos.Streams.GetManifestAsync(videoUrlOrId, ct)` → `StreamManifest`; `manifest.GetAudioOnlyStreams().GetWithHighestBitrate()` → `IStreamInfo` (audio-only); `streamInfo.Size.Bytes` (long) known PRE-download (P4); `streamInfo.Container.Name` gives the extension; `youtube.Videos.Streams.DownloadAsync(streamInfo, tempFilePath, ct)` writes the file (RESEARCH confirmed via Tyrrrz README)
- VIDEO DURATION: `PlaylistVideo.Duration` is `TimeSpan?`; the lister (20-04) supplies it as the AUTHORITATIVE cap-projection duration via `knownDuration`. AudioDownloadResult.DurationSeconds is best-effort (stream manifest often omits stream duration → can be 0) and is NOT the cap source; AudioDownloadResult also carries the stream-derived size for the chunk decision
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ITranscriptSource + non-null status-carrying TranscriptFetchResult (carries Whisper cost; FetchTranscriptAsync carries knownDuration + monthKey)</name>
  <files>DeckFlow.Core/Integration/ITranscriptSource.cs</files>
  <read_first>
    - DeckFlow.Core/Knowledge/ContentModels.cs lines 140-199 (TranscriptSource / TranscriptStatus / ContentSourceType const classes the record fields reference)
    - DeckFlow.Core/Integration/ArchidektApiDeckImporter.cs (file-scoped namespace DeckFlow.Core.Integration; XML doc-comment style)
    - .planning/phases/20-content-kb-ingestion-transcription-local/20-RESEARCH.md Pattern 3 (the minimal interface shape — do NOT over-engineer a plugin system; NOTE the plan SUPERSEDES the nullable result shown there — use the non-null result below)
  </read_first>
  <action>
    Create `ITranscriptSource` in `DeckFlow.Core/Integration` (interface-first contract Plan 20-04 + Phase 21 implement against). Replace the nullable result design (Codex HIGH-3) with a NON-NULL discriminated result. Define `public enum TranscriptOutcome { Captions, Whisper, Failed, SkippedOverCap }`. Define `public sealed record TranscriptFetchResult` with `{ get; init; }` properties: `TranscriptOutcome Outcome` (required), `string? Body` (present only for Captions/Whisper success), `string? Source` (a `TranscriptSource` const — captions|whisper — null for failed/skipped), `string? FailureReason` (set for Failed), `bool? IsAutoGenerated` (set when Source==captions), and — so the verb can write the ledger from a Whisper success (D-11/HIGH-1) — `int? SecondsBilled` and `decimal? CostUsd` (set ONLY when Outcome==Whisper; null otherwise). Add static factory helpers: `TranscriptFetchResult.FromCaptions(string body, bool isAutoGenerated)`, `.FromWhisper(string body, int secondsBilled, decimal costUsd)`, `.Failed(string reason)`, `.SkippedOverCap()`. CRITICAL: never collapse SkippedOverCap/Failed via `?? Failed` — the Outcome enum is the single source of truth and the harvest verb maps it directly to a TranscriptStatus const (Captions→captions, Whisper→whisper, Failed→failed, SkippedOverCap→skipped_over_cap). Expose on the interface: `string SourceType { get; }` (matches a `ContentSourceType` const) and `Task<TranscriptFetchResult> FetchTranscriptAsync(string naturalKey, TimeSpan? knownDuration, string monthKey, CancellationToken ct = default)` — NOTE the result is non-null and the method takes NO videoId/store reference (services are pure per D-11; the caller owns persistence and the videoId).

    DURATION CONTRACT (Codex iter2 NEW-HIGH): `knownDuration` is the AUTHORITATIVE cap-projection duration (the lister-supplied `PlaylistVideo.Duration`); the implementation (20-04 `YouTubeTranscriptSource`) MUST pass it through to `IWhisperTranscriptionService.TranscribeAsync` so the cap projection (20-03) never sees 0/unknown for a real video. Document on the `knownDuration` XML `<param>`: "Authoritative video duration from the channel lister; passed to the Whisper cap projection so a caption-less video is never projected at $0 and transcribed free (KB-05)."

    MONTH-KEY CONTRACT (Codex iter2 MEDIUM): `monthKey` (format `yyyy-MM`) is derived ONCE by the harvest verb (20-04) per video and passed in; the implementation passes it through to `IWhisperTranscriptionService.TranscribeAsync` for the cap check, and the verb reuses the SAME monthKey local for the ledger `RecordCallAsync` — so the cap-check month and the ledger month are identical even across a UTC month boundary. Document on the `monthKey` XML `<param>`: "Verb-supplied yyyy-MM month key used for BOTH the Whisper cap check and the verb's ledger write — must be the same value for both (Codex iter2 MEDIUM)."

    XML `<summary>` on the enum, interface, every member, the record, and each factory. Keep it minimal — podcast path deferred (D-09), no RSS-specific members. File-scoped namespace, Allman braces, LF endings, init-only.
  </action>
  <verify>
    <automated>"/mnt/c/Program Files/dotnet/dotnet.exe" build DeckFlow.Core/DeckFlow.Core.csproj -c Debug 2>&1 | grep -E "Build succeeded|error"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "interface ITranscriptSource" DeckFlow.Core/Integration/ITranscriptSource.cs` == 1
    - `grep -c "enum TranscriptOutcome" DeckFlow.Core/Integration/ITranscriptSource.cs` == 1 (non-null discriminated result — no nullable TranscriptFetchResult? on the interface)
    - `grep -c "Task<TranscriptFetchResult>" DeckFlow.Core/Integration/ITranscriptSource.cs` ≥1 AND `grep -c "Task<TranscriptFetchResult?>" ...` == 0 (result is non-null)
    - `grep -c "knownDuration" DeckFlow.Core/Integration/ITranscriptSource.cs` ≥1 (authoritative cap-projection duration carried on the contract — Codex iter2 NEW-HIGH)
    - `grep -c "monthKey" DeckFlow.Core/Integration/ITranscriptSource.cs` ≥1 (verb-supplied month key carried on the contract — Codex iter2 MEDIUM)
    - `grep -c "SecondsBilled" DeckFlow.Core/Integration/ITranscriptSource.cs` ≥1 AND `grep -c "CostUsd" ...` ≥1 (Whisper cost conveyed for the ledger write — HIGH-1)
    - All record properties use `{ get; init; }`; build clean 0 errors / 0 warnings (GenerateDocumentationFile on — every public member documented)
  </acceptance_criteria>
  <done>ITranscriptSource + a non-null status-carrying TranscriptFetchResult (TranscriptOutcome discriminator + factories + Whisper SecondsBilled/CostUsd) exist with full doc-comments; FetchTranscriptAsync carries the authoritative knownDuration AND the verb-supplied monthKey; build clean. SkippedOverCap and Failed are distinct outcomes.</done>
</task>

<task type="auto">
  <name>Task 2: YoutubeExplode 6.6.0 + YouTubeTranscriptFetcher (pure, empty-track classified) + TranscriptProviderFactory</name>
  <files>DeckFlow.Core/DeckFlow.Core.csproj, DeckFlow.Core/Integration/IYouTubeTranscriptFetcher.cs, DeckFlow.Core/Integration/YouTubeTranscriptFetcher.cs, DeckFlow.Core/Integration/TranscriptProviderFactory.cs, DeckFlow.Core.Tests/YouTubeTranscriptFetcherTests.cs, DeckFlow.Core.Tests/TranscriptProviderFactoryTests.cs</files>
  <read_first>
    - DeckFlow.Core/Integration/ArchidektApiDeckImporter.cs (production ctor + injected-client pattern; copy the shape, swap RestClient for HttpClient/YoutubeClient)
    - DeckFlow.Web/Services/CardLookupService.cs lines 50-95 (Func<> override field + internal ctor seam — the canonical seam to mirror)
    - DeckFlow.Core/DeckFlow.Core.csproj (PackageReference block — add YoutubeExplode 6.6.0 only; OpenAI is added in Plan 20-03)
    - DeckFlow.Core.Tests/ContentSourceStoreTests.cs (xUnit fixture style)
    - .planning/phases/20-content-kb-ingestion-transcription-local/20-RESEARCH.md "Caption detection with auto-gen acceptance" code example (lines ~472-499) + Pitfall P2/P6
  </read_first>
  <action>
    Add `<PackageReference Include="YoutubeExplode" Version="6.6.0" />` to DeckFlow.Core.csproj (D-02 approved). No packages.lock.json is generated (GeneratePackageLockFile unset repo-wide; no lockfile tracked or on disk) — the approved add touches only the .csproj; do NOT create packages.lock.json.

    Create `IYouTubeTranscriptFetcher` exposing `Task<YouTubeCaptionResult> FetchCaptionsAsync(string videoId, CancellationToken ct = default)` and a `public sealed record YouTubeCaptionResult { get; init; }` carrying: `bool HasCaptions`, `string? Body`, `bool IsAutoGenerated`, `string? LanguageCode` (static `NoCaptions()` factory). The fetcher PERSISTS NOTHING (D-11). Implement `YouTubeTranscriptFetcher` following BOTH precedents: production ctor `YouTubeTranscriptFetcher(HttpClient httpClient)` delegating to an `internal` ctor taking `Func<string, CancellationToken, Task<YouTubeCaptionResult>> executeAsync` (test seam); the production delegate wraps `new YoutubeClient(httpClient)` plus a `private static readonly AsyncRetryPolicy` (HandleResult on transient YoutubeExplode failures, WaitAndRetry exponential backoff — copy ArchidektApiDeckImporter's policy shape; name retryable exceptions: `HttpRequestException`, `TaskCanceledException` on transient timeout, and any YoutubeExplode transient exception type if exposed). Caption logic per RESEARCH: `GetManifestAsync`; if `manifest.Tracks.Count == 0` return `YouTubeCaptionResult.NoCaptions()`; else select track via `TryGetByLanguage("en-US") ?? TryGetByLanguage("en") ?? Tracks.FirstOrDefault(t => t.Language.Code.StartsWith("en", OrdinalIgnoreCase)) ?? Tracks[0]` (NEVER GetByLanguage); fetch via `GetAsync(trackInfo)`; body = `string.Join(" ", track.Captions.Select(c => c.Text))`. EMPTY-TRACK CLASSIFICATION (Codex MEDIUM-b): if the joined body is null/empty/whitespace, return `NoCaptions()` (treat as captions-absent so the Whisper fallback can run) — NEVER return HasCaptions=true with an empty body. On success set `IsAutoGenerated = trackInfo.IsAutoGenerated` (accept auto-gen as captions). Keep methods ≤30 lines (extract caption-track selection to a private static helper).

    Create `TranscriptProviderFactory` (Codex HIGH-5 — real toggle, SC2/P2). A small static or instance factory: `IYouTubeTranscriptFetcher Resolve(string? providerEnvValue, HttpClient httpClient)`. Read `DECKFLOW_YOUTUBE_TRANSCRIPT_PROVIDER`; treat null/empty/"direct" (case-insensitive) as the wired default → return `new YouTubeTranscriptFetcher(httpClient)`. For ANY other value, throw `NotSupportedException($"Unsupported DECKFLOW_YOUTUBE_TRANSCRIPT_PROVIDER '{value}'. Supported: direct.")` — a clear, listed-supported-values message. This is the proxy-pluggable seam: a future proxy provider is added as a new case; today only `direct` exists but the dispatch is real, not symbolic. The harvest verb (20-04) calls `TranscriptProviderFactory.Resolve(...)` rather than newing the fetcher directly. XML doc-comments throughout.

    Tests:
    - `YouTubeTranscriptFetcherTests` via the internal Func<> ctor: (a) HasCaptions true path returns joined body, (b) NoCaptions path returns HasCaptions=false and never throws, (c) auto-generated track yields HasCaptions=true + IsAutoGenerated=true, (d) a track whose joined caption text is empty/whitespace returns HasCaptions=false (NoCaptions — empty body never surfaced as captions). No live HTTP.
    - `TranscriptProviderFactoryTests`: (a) Resolve(null) and Resolve("direct") return a non-null YouTubeTranscriptFetcher; (b) Resolve("magic-proxy") throws NotSupportedException whose message contains "Supported: direct".
  </action>
  <verify>
    <automated>"/mnt/c/Program Files/dotnet/dotnet.exe" build DeckFlow.Core/DeckFlow.Core.csproj -c Debug 2>&1 | grep -E "Build succeeded|error"; "/mnt/c/Program Files/dotnet/dotnet.exe" test DeckFlow.Core.Tests/DeckFlow.Core.Tests.csproj --filter "FullyQualifiedName~YouTubeTranscriptFetcherTests|FullyQualifiedName~TranscriptProviderFactoryTests" 2>&1 | tail -6</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'YoutubeExplode" Version="6.6.0"' DeckFlow.Core/DeckFlow.Core.csproj` == 1; no other new package added in this plan
    - In DeckFlow.Core/Integration/YouTubeTranscriptFetcher.cs, `GetByLanguage` appears 0 times except as part of `TryGetByLanguage` (anti-pattern gate: `grep -o 'GetByLanguage' file | wc -l` equals the `TryGetByLanguage` count)
    - `grep -c "new HttpClient()" DeckFlow.Core/Integration/YouTubeTranscriptFetcher.cs` == 0 AND in TranscriptProviderFactory.cs == 0 (HttpClient ctor-injected; ban scoped to Core/Integration, not the CLI composition root)
    - `grep -c "DECKFLOW_YOUTUBE_TRANSCRIPT_PROVIDER" DeckFlow.Core/Integration/TranscriptProviderFactory.cs` ≥1 AND the factory throws NotSupportedException on an unknown value (TranscriptProviderFactoryTests proves the listed-supported-values message)
    - `grep -c "internal YouTubeTranscriptFetcher" DeckFlow.Core/Integration/YouTubeTranscriptFetcher.cs` ≥1 (Func<> test ctor present)
    - The fetcher takes NO ContentVideoStore/WhisperSpendLedger/DB dependency (D-11 pure): `grep -ci "ContentVideoStore\|WhisperSpendLedger\|InsertTranscript\|UpdateTranscriptStatus" DeckFlow.Core/Integration/YouTubeTranscriptFetcher.cs` == 0
    - empty-track test proves an empty caption body yields HasCaptions=false (no empty captions surfaced)
    - Build clean; both test classes compile + pass (or build-clean fallback per WSL VSTest caveat noted in SUMMARY)
    - DeckFlow.Core.csproj gains NO Microsoft.AspNetCore.* / Microsoft.Extensions.Http reference
  </acceptance_criteria>
  <done>YouTubeTranscriptFetcher fetches captions (auto-gen accepted, empty classified as NoCaptions), persists nothing, uses TryGetByLanguage only, is ctor-injected + Func<>-tested; TranscriptProviderFactory makes the env toggle real (direct wired, unsupported value fails clearly).</done>
</task>

<task type="auto">
  <name>Task 3: IYouTubeAudioSource + AudioDownloadResult (audio download contract for Whisper — Codex HIGH-2)</name>
  <files>DeckFlow.Core/Integration/IYouTubeAudioSource.cs, DeckFlow.Core/Integration/YouTubeAudioSource.cs</files>
  <read_first>
    - DeckFlow.Core/Integration/ArchidektApiDeckImporter.cs (namespace + doc-comment + sealed conventions; production ctor injected client)
    - DeckFlow.Web/Services/CardLookupService.cs lines 50-95 (Func<> seam to mirror)
    - .planning/phases/20-content-kb-ingestion-transcription-local/20-RESEARCH.md Pitfall P4 (Size.Bytes known pre-download) + P6 (no audio retained — D-15) + the YoutubeExplode Streams API facts in <interfaces> above
  </read_first>
  <action>
    Define the explicit audio path contract (Codex HIGH-2) so the Whisper fallback is composable. Create `public sealed record AudioDownloadResult : IDisposable` with `{ get; init; }` properties: `string TempFilePath`, `string FileName` (basename incl. container extension — Whisper validates format by extension), `long SizeBytes`, `double DurationSeconds`. CRITICAL (Codex iter2 NEW-HIGH — KB-05 cap bypass): `DurationSeconds` is BEST-EFFORT — the YoutubeExplode audio-stream manifest entry frequently does NOT carry a stream duration, so this field can legitimately be `0`. It is therefore NOT authoritative for the cap projection. The AUTHORITATIVE cap-projection duration is the lister-supplied `knownDuration` (`PlaylistVideo.Duration`) the verb/source passes through to `IWhisperTranscriptionService.TranscribeAsync` (ITranscriptSource Task 1 of this plan + 20-03 + 20-04). Document this on the `DurationSeconds` XML `<summary>`: "Best-effort stream duration; may be 0 when the audio manifest lacks it — NOT authoritative for the cap projection; the cap uses the lister-supplied knownDuration passed through TranscribeAsync." Implement `IDisposable.Dispose()` to delete `TempFilePath` (and its parent temp dir if the source created one) inside a try/catch (best-effort cleanup — no throw on missing file); this is the cleanup handle the harvest verb / YouTubeTranscriptSource disposes in a `finally`/`using`. No raw audio retained beyond this handle (D-15).

    Create `IYouTubeAudioSource` exposing `Task<AudioDownloadResult> DownloadAudioAsync(string videoUrlOrId, CancellationToken ct = default)`. Implement `YouTubeAudioSource` with a production ctor `YouTubeAudioSource(HttpClient httpClient)` delegating to an `internal` ctor with a `Func<string, CancellationToken, Task<AudioDownloadResult>>` override seam (mirror CardLookupService). The production delegate: `var youtube = new YoutubeClient(httpClient)`; `var manifest = await youtube.Videos.Streams.GetManifestAsync(videoUrlOrId, ct)`; `var streamInfo = manifest.GetAudioOnlyStreams().GetWithHighestBitrate()`; compute `ext = "." + streamInfo.Container.Name`; build a unique temp path under `Path.Combine(Path.GetTempPath(), "deckflow-audio", Guid.NewGuid().ToString("N") + ext)` (create the dir); `await youtube.Videos.Streams.DownloadAsync(streamInfo, tempPath, ct)`; set `SizeBytes = streamInfo.Size.Bytes` (the chunk decision in 20-03 uses this); set `DurationSeconds` from the stream/video duration available on the manifest entry if present, else 0 (BEST-EFFORT — the cap projection in 20-03 ignores a 0 here and uses the lister-supplied knownDuration the verb passes through; never block download on a missing stream duration). Wrap the download in the same `private static readonly AsyncRetryPolicy` shape (transient HttpRequestException/timeout). This service PERSISTS NOTHING. Keep methods ≤30 lines (extract temp-path building to a private static helper). XML doc-comments throughout.

    NOTE: this service is unit-tested at the contract level via the Func<> seam in 20-04's composition tests (a fake AudioDownloadResult over a small temp file); a live YouTube download is exercised only in the 20-04 UAT. No live-HTTP unit test here (download requires network); rely on build-clean + the Func<> seam being present.
  </action>
  <verify>
    <automated>"/mnt/c/Program Files/dotnet/dotnet.exe" build DeckFlow.Core/DeckFlow.Core.csproj -c Debug 2>&1 | grep -E "Build succeeded|error"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "record AudioDownloadResult" DeckFlow.Core/Integration/IYouTubeAudioSource.cs` ≥1 AND it implements `IDisposable` (cleanup handle present) carrying TempFilePath, FileName, SizeBytes, DurationSeconds
    - The `DurationSeconds` XML summary documents it as best-effort / NOT authoritative for the cap (Codex iter2 NEW-HIGH): `grep -ci "best-effort\|not authoritative\|knownDuration" DeckFlow.Core/Integration/IYouTubeAudioSource.cs` ≥1
    - `grep -c "GetAudioOnlyStreams" DeckFlow.Core/Integration/YouTubeAudioSource.cs` ≥1 AND `grep -c "DownloadAsync" ...` ≥1 (download to temp file)
    - `grep -c "GetTempPath" DeckFlow.Core/Integration/YouTubeAudioSource.cs` ≥1 (temp file, not retained beyond cleanup handle — D-15)
    - `grep -c "new HttpClient()" DeckFlow.Core/Integration/YouTubeAudioSource.cs` == 0 (ctor-injected); `grep -c "internal YouTubeAudioSource" ...` ≥1 (Func<> seam present)
    - The service takes NO ContentVideoStore/WhisperSpendLedger/DB dependency (D-11 pure)
    - Build clean; DeckFlow.Core gains NO Microsoft.AspNetCore.* / Microsoft.Extensions.Http reference
  </acceptance_criteria>
  <done>IYouTubeAudioSource + AudioDownloadResult give Whisper a concrete audio path: audio-only stream → temp file with size/duration + IDisposable cleanup; DurationSeconds is documented best-effort/non-authoritative (cap uses knownDuration); persists nothing; ctor-injected with a Func<> seam.</done>
</task>

</tasks>

<verification>
- KB-03: caption fetch via YoutubeExplode 6.6.0; Google.Apis.YouTube NOT referenced anywhere in this phase.
- SC1/SC2 (amended): fetcher built (pure); provider factory real (direct wired, unsupported fails); auto-gen accepted + empty classified to hold fallback ratio low. (The live 5-channel proof runs in Plan 20-04 UAT.)
- SC5 AMENDMENT honored: Core/Integration HttpClient ctor-injected + direct Polly + Func<> seam; no IHttpClientFactory, no new HttpClient() in Core/Integration methods, no Microsoft.Extensions.Http in Core.
- D-11: all four services persist nothing — no store/ledger dependency. The harvest verb (20-04) owns persistence; the result carries Whisper cost for the verb's ledger write.
- Duration authority (Codex iter2 NEW-HIGH): the contract carries knownDuration as the authoritative cap-projection duration; AudioDownloadResult.DurationSeconds is documented best-effort. The cap check (20-03) never sees 0 for a real video.
- Month-key (Codex iter2 MEDIUM): FetchTranscriptAsync carries a verb-supplied monthKey, so the cap-check month and the verb's ledger-write month match.
- ITranscriptSource (non-null result) + IYouTubeAudioSource contracts exist for Plan 20-03/20-04 + Phase 21 (interface-first ordering).
</verification>

<success_criteria>
- A different executor can wire the harvest verb against ITranscriptSource + YouTubeCaptionResult + IYouTubeAudioSource without exploring the codebase.
- Caption fetch + provider factory are unit-tested without live HTTP; build clean; no new dependency beyond YoutubeExplode 6.6.0.
- skipped_over_cap and failed are distinct outcomes on the result; empty caption tracks never surface as captions; the result conveys Whisper cost for the ledger; knownDuration + monthKey are carried on the contract.
</success_criteria>

<output>
Create `.planning/phases/20-content-kb-ingestion-transcription-local/20-02-SUMMARY.md` when done.
</output>
