---
phase: 01-interaction-taxonomy-split
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - DeckFlow.Web/Services/CutLab/CutLabRoleAssigner.cs
  - DeckFlow.Web/Services/CutLab/CutLabFloorRules.cs
  - DeckFlow.Web/Services/CutLab/CutLabFloorDefaults.cs
  - DeckFlow.Web/Services/CutLab/CutLabStructuralFindings.cs
  - DeckFlow.Web/Services/CutLab/CutLabCutRoundEngine.cs
  - DeckFlow.Web/Views/Deck/CutLab.cshtml
  - DeckFlow.Web.Tests/CutLabRoleAssignerTests.cs
  - DeckFlow.Web.Tests/CutLabFloorRulesTests.cs
  - DeckFlow.Web.Tests/CutLabFloorDefaultsTests.cs
  - DeckFlow.Web.Tests/CutLabStructuralFindingsTests.cs
  - DeckFlow.Web.Tests/CutLabCutRoundEngineTests.cs
  - DeckFlow.Web.Tests/CutLabStateSerializerTests.cs
  - DeckFlow.Web/e2e/cut-lab-structure.spec.ts
autonomous: true
requirements: [ISPL-01, ISPL-02, ISPL-03]
user_setup: []
must_haves:
  truths:
    - "CutLabRoleAssigner.AssignRoles returns the role keys 'interaction-targeted' and/or 'interaction-mass'; the key 'interaction' is never emitted by Cut Lab role assignment"
    - "Targeted membership is driven by DeckStatClassifier.IsTargetedRemovalCard plus the existing interactionMeritPreGate signal; mass membership is driven by DeckStatClassifier.IsBoardWipeCard -- the two predicates were already called separately at CutLabRoleAssigner.cs:141-143 and this plan only stops merging their results"
    - "A card that satisfies both predicates (a spell that is both targeted removal and a wipe) appears in BOTH roles, consistent with Cut Lab's existing multi-role membership model where cutting a card decrements every role it fills"
    - "Bracket default floors exist for both new roles at every bracket, and at every bracket targetedFloor + massFloor is greater than or equal to the merged interaction floor shipped today (B2 6, B3 8, B4 10, B5 12)"
    - "A CutLabState persisted with a user-set legacy 'interaction' role floor restores without exception and without losing the user's intent: the legacy value is split across the two new roles in proportion to their default ratio, and the split sums exactly to the legacy value"
    - "Cut Lab's PlanRole/Manabase interaction taxonomy is untouched -- PlanRole.Interaction, ManabaseLabels, PlanRoleClassifier, MetaGapResponse, DeckPrimerPacketService and every Manabase/DeckAnalysis/CedhMetaGap view keep the single 'interaction' concept"
    - "The role-group help text that today renders only for the merged interaction group renders correctly for both new groups, with copy that distinguishes targeted removal from mass removal"
    - "Full build is clean (0 errors, 0 new warnings), the whole xUnit suite passes, vitest passes, and the cut-lab e2e specs pass"
  artifacts:
    - path: "DeckFlow.Web/Services/CutLab/CutLabRoleAssigner.cs"
      provides: "Nine-role taxonomy with targeted and mass interaction split from the already-separate classifier predicates"
      contains: "interaction-targeted"
    - path: "DeckFlow.Web/Services/CutLab/CutLabFloorDefaults.cs"
      provides: "Bracket bands for both new roles whose per-bracket sum preserves the shipped interaction guardrail"
      contains: "interaction-mass"
    - path: "DeckFlow.Web/Services/CutLab/CutLabFloorRules.cs"
      provides: "Canonical nine-key role list plus legacy-interaction floor migration during clamp"
      contains: "MigrateLegacyInteractionFloor"
  key_links:
    - from: "CutLabRoleAssigner.AssignRoles"
      to: "DeckStatClassifier.IsBoardWipeCard"
      via: "sole predicate for interaction-mass membership"
      pattern: "IsBoardWipeCard"
    - from: "CutLabRoleAssigner.AssignRoles"
      to: "DeckStatClassifier.IsTargetedRemovalCard"
      via: "predicate for interaction-targeted membership, together with interactionMeritPreGate"
      pattern: "IsTargetedRemovalCard"
    - from: "CutLabFloorRules.ClampFloors"
      to: "MigrateLegacyInteractionFloor"
      via: "legacy 'interaction' persisted key is proportionally split rather than silently dropped"
      pattern: "MigrateLegacyInteractionFloor"
---

# Phase 1 Plan 01 — Interaction Taxonomy Split

## Why This Phase Exists

`CutLabRoleAssigner.cs:141-143` already calls two distinct classifier predicates and then merges
both results into a single `interaction` role key:

```csharp
if (interactionMeritPreGate
    || DeckStatClassifier.IsBoardWipeCard(oracle)
    || DeckStatClassifier.IsTargetedRemovalCard(typeLine, oracle))
{
    assigned.Add(InteractionRole);
}
```

The merge is the only thing this plan undoes. Two reasons it matters:

1. **Template alignment.** The 2025 Command Zone template splits targeted disruption (~12) from
   mass disruption (~6). Cut Lab's single interaction floor tops out at 12, which under-counts the
   combined guardrail and cannot express the very different ratios different decks want.
2. **It unblocks Phase 2.** Targeted removal and board wipes have materially different
   per-commander distributions. Averaging them into one bucket inflates within-role variance,
   which deflates the z-scores and Cohen's d that Phase 2's statistical bar reads. Measuring a
   role that is about to be redefined also wastes the run outright.

## Scope Fence — READ BEFORE EDITING

The string `"interaction"` appears across two unrelated taxonomies. Only the Cut Lab one changes.

**IN SCOPE — Cut Lab role keys (6 production sites):**

| File | Site |
|---|---|
| `CutLabRoleAssigner.cs` | `:19` const, `:26-36` `RoleKeys`, `:52-64` `RoleDisplayLabels`, `:141-143` the merge |
| `CutLabFloorRules.cs` | `:13-23` `RoleKeys` |
| `CutLabFloorDefaults.cs` | `:114` `GetBracketBand` |
| `CutLabStructuralFindings.cs` | `:115` const, `:121-131` `WeakFloorRoleOrder` |
| `CutLabCutRoundEngine.cs` | `:121-132` `LockedOvershootRoleOrder` |
| `Views/Deck/CutLab.cshtml` | `:592-594` role-group help conditional |

**OUT OF SCOPE — do NOT touch. Different taxonomy, same word:**

- `DeckFlow.Core/Manabase/ManabaseLabels.cs:34` — `PlanRole.Interaction => "interaction"`
- `DeckFlow.Web/Services/Manabase/PlanRoleClassifier.cs:243` — category keyword matching
- `DeckFlow.Web/Models/MetaGapResponse.cs:62` — JSON contract property
- `DeckFlow.Web/Services/DeckPrimerPacketService.cs:642`
- `Views/Deck/Manabase.cshtml`, `DeckAnalysis.cshtml`, `CedhMetaGap.cshtml`, `Bracket.cshtml`
- `e2e/manabase-*.spec.ts`, `e2e/deck-analysis-render.spec.ts`
- `site-common.css` `.interaction-audit*` rules (Deck Analysis, unrelated)

`PlanRole.Interaction` remains a single unsplit concept. `CutLabRoleAssigner` consumes it via
`interactionMeritPreGate` and routes it to the **targeted** role only.

## Design Decisions

### D-01 — Both-predicate cards join both roles

A card satisfying both `IsTargetedRemovalCard` and `IsBoardWipeCard` is assigned both keys. This
follows Cut Lab's existing multi-role model (`CutLabRoleAssigner` xmldoc: "Multi-role membership is
allowed; cutting a card reduces every role count it currently fills"). Do not invent a
precedence rule.

### D-02 — `interactionMeritPreGate` routes to targeted

`PlanRoleClassifier.Classify`'s interaction merit signal (which is what makes counterspells count
as interaction in cEDH mode only) is a targeted-answer signal, not a sweeper signal. It joins
`interaction-targeted`.

### D-03 — Floor bands preserve the shipped sum at a 2:1 targeted:mass ratio

Today: `"interaction" => normalizedBracket switch { 2 => 6, 3 => 8, 4 => 10, _ => 12 }`.

| Bracket | targeted | mass | sum | shipped merged floor |
|---|--:|--:|--:|--:|
| B2 | 4 | 2 | 6 | 6 |
| B3 | 5 | 3 | 8 | 8 |
| B4 | 7 | 3 | 10 | 10 |
| B5 | 9 | 3 | 12 | 12 |

Mass flattens at 3 from B3 up because higher-bracket decks answer specific threats rather than
sweeping; targeted absorbs the growth. This mirrors the Command Zone 2025 revision, where targeted
removal nearly doubled while board wipes dropped. Sums are exactly preserved, satisfying ISPL-03.

### D-04 — Legacy floor migration splits proportionally

`CutLabFloorRules.ClampFloors` currently drops any role key that fails `TryGetCanonicalRole`
(`:44-48`: `changed = true; continue`). A persisted user-set `interaction` floor would therefore be
**silently discarded** — the user's override lost with no warning. That is the ISPL-02 failure this
plan must prevent.

Migration rule, applied only when the legacy key is present AND `IsUserSet`:

```
targeted = round(legacyFloor * targetedDefault / (targetedDefault + massDefault))
mass     = legacyFloor - targeted
```

Subtracting for `mass` guarantees the split sums exactly to the legacy value regardless of
rounding. Both migrated entries carry `IsUserSet = true`. A legacy entry that is NOT user-set is
dropped (it was a stale default; fresh defaults supersede it).

## Tasks

### Task 1 — Split the role assignment

**File:** `DeckFlow.Web/Services/CutLab/CutLabRoleAssigner.cs`

1. Replace `private const string InteractionRole = "interaction";` with two consts:
   `InteractionTargetedRole = "interaction-targeted"` and `InteractionMassRole = "interaction-mass"`.
2. In `RoleKeys`, replace the single `InteractionRole` entry with the two new keys, targeted first,
   preserving the surrounding order (lands, ramp, draw, **interaction-targeted, interaction-mass**,
   protection, engines, payoffs, wincons). Array length becomes 9.
3. In `RoleDisplayLabels`, replace `["interaction"] = "Interaction"` with
   `["interaction-targeted"] = "Targeted removal"` and `["interaction-mass"] = "Mass removal"`.
4. Replace the merged assignment at `:141-143` with two independent checks:
   - targeted when `interactionMeritPreGate || DeckStatClassifier.IsTargetedRemovalCard(typeLine, oracle)`
   - mass when `DeckStatClassifier.IsBoardWipeCard(oracle)`
   Add a `// Why:` comment recording D-01 and D-02.
5. Update the class xmldoc: "eight structural role keys" becomes nine, and note that the
   interaction split is Cut Lab's own taxonomy, deliberately finer than `PlanRole.Interaction`.

**Verification:** `dotnet build` clean. A card that is only a wipe gets `interaction-mass` and not
`interaction-targeted`; a card that is only targeted removal gets the inverse; a card that is both
gets both.

### Task 2 — Floor keys, bands, and legacy migration

**Files:** `CutLabFloorRules.cs`, `CutLabFloorDefaults.cs`

1. `CutLabFloorRules.RoleKeys` — replace `"interaction"` with the two new keys in the same
   position. This list is the canonical persistence order; keep it identical to
   `CutLabRoleAssigner.RoleKeys`.
2. `CutLabFloorDefaults.GetBracketBand` — replace the `"interaction"` arm with two arms carrying
   the D-03 values. Keep the existing `// Why:` comment convention on each.
3. Add `MigrateLegacyInteractionFloor` to `CutLabFloorRules` implementing D-04, and call it from
   `ClampFloors` **before** the canonical-role filter so the legacy key is converted rather than
   dropped. The migration must be idempotent: running it on already-migrated state is a no-op.
4. Update the "eight" xmldoc references in `CutLabFloorDefaults.cs:7` and `:43`.

**Verification:** a state with `{role: "interaction", floor: 14, isUserSet: true}` at bracket 4
(defaults targeted 7, mass 3) migrates to `interaction-targeted: 10` + `interaction-mass: 4`:

```
targeted = round(14 * 7 / 10) = round(9.8) = 10
mass     = 14 - 10            = 4
```

The test must assert `targeted + mass == 14` exactly rather than hardcoding both arms, so the
invariant survives any later tuning of the D-03 ratio. Assert the individual arms only in the one
bracket-4 case above, to pin the rounding direction.

### Task 3 — Findings order and overshoot order

**Files:** `CutLabStructuralFindings.cs`, `CutLabCutRoundEngine.cs`

1. `CutLabStructuralFindings` — replace the `InteractionRole` const with the two keys, and expand
   `WeakFloorRoleOrder` to place targeted then mass where `interaction` sat. `RoleDisplayName` gains
   both arms with the Task-1 labels.
2. `CutLabCutRoundEngine.LockedOvershootRoleOrder` — replace `"interaction"` with the two keys.
   Order within the advisory is least-structural first, so **mass before targeted**: a sweeper is
   more replaceable than a cheap targeted answer in an overshoot situation. Record the reasoning in
   the existing `// Why:` block above the array.

**Verification:** weak-floor findings fire independently per new role; a pool with 2 wipes against a
mass floor of 3 raises a mass weak-floor finding without implicating targeted removal.

### Task 4 — Role-group help text

**File:** `DeckFlow.Web/Views/Deck/CutLab.cshtml:592-594`

The current conditional renders help copy only for `group.RoleKey == "interaction"`:

> "Interaction counts one-shot removal and wipes. Counterspells count as interaction only for a
> cEDH play experience."

That sentence is now wrong in two ways (it describes a merged role, and it attributes wipes to the
targeted bucket). Replace the single conditional with two, one per new role:

- **interaction-targeted:** one-shot removal aimed at a single permanent or spell. Counterspells
  count here only for a cEDH play experience.
- **interaction-mass:** board wipes and other sweepers that hit multiple permanents at once.

Keep the existing `.cutlab-role-group__help` class and the surrounding markup. Razor auto-encodes;
do not use `Html.Raw`.

**Verification:** render the page against a real imported pool and confirm both help paragraphs
appear under their own role group. Prove the render — do not eyeball the Razor.

### Task 5 — Tests and e2e

**Unit** — update every assertion that assumes the merged key or an 8-role list:
`CutLabRoleAssignerTests`, `CutLabFloorRulesTests`, `CutLabFloorDefaultsTests`,
`CutLabStructuralFindingsTests`, `CutLabCutRoundEngineTests`, `CutLabStateSerializerTests`. Other
Cut Lab test files reference `"interaction"` incidentally and may compile unchanged — fix only what
actually breaks; do not mass-rewrite.

**New tests required:**
1. Wipe-only card → `interaction-mass` only.
2. Targeted-only card → `interaction-targeted` only.
3. Both-predicate card → both roles (D-01).
4. cEDH-mode counterspell → `interaction-targeted`, not mass (D-02).
5. Per bracket, `targeted + mass >= ` the shipped merged floor (D-03) — table-driven over all four
   brackets.
6. Legacy user-set `interaction` floor migrates, and `targeted + mass` equals the legacy value
   exactly (D-04).
7. Legacy non-user-set `interaction` floor is dropped, not migrated.
8. Migration is idempotent.

**e2e** — `DeckFlow.Web/e2e/cut-lab-structure.spec.ts` has four tests keyed on
`data-cut-lab-floor="interaction"` (`:206-273`) plus a scroll target at `:467`. Retarget them to
`interaction-targeted`. Do not delete coverage; the floor-edit, persist-across-recalculate,
at-floor-marker, and reset-to-default behaviors must all still be exercised.

**Verification:** full xUnit suite green, vitest green, `cut-lab-structure.spec.ts` green.

### Task 6 — Full gates

1. `dotnet build` from repo root — 0 errors, 0 new warnings.
2. Full xUnit suite (Core + Web).
3. `npx --no-install playwright test` for the cut-lab specs, headless, no browser on the Windows
   host — start the app via `scripts/run-web-test.sh` so `DECKFLOW_DISABLE_AUTO_BROWSER=true` is set.
4. Confirm no file in the Scope Fence "out of scope" list is in the diff.
5. Line-ending check per touched file: compare `git diff --stat` against
   `git diff --ignore-all-space --stat`, and each file's endings against `git show HEAD:<path>`.
   These are LF files per `.gitattributes`; restore any flipped file before commit.
6. Changed-lines format gate: `scripts/format-check-changed.sh staged`.

## Regression Risks

| Risk | Why it could bite | Mitigation |
|---|---|---|
| Persisted floors silently lost | `ClampFloors` drops unknown keys by design | D-04 migration + tests 6-8 |
| Guardrail weakened without notice | Split sums could drift below the merged floor | D-03 sums preserved + test 5 asserts per bracket |
| Manabase taxonomy collaterally split | Same string, different concept, 10+ call sites | Scope Fence; verification step 4 |
| Both-predicate cards double-counted in a way that inflates totals | A card in both roles increments both counts | Intended per D-01 and consistent with existing multi-role behavior; call it out in the summary so it is a decision, not a surprise |
| e2e coverage quietly reduced | Easiest fix is deleting the broken tests | Task 5 requires retarget, not deletion |

## Out of Scope

- Commander-aware floor values (Phase 3).
- Any change to `PlanRole` or the Manabase interaction lens.
- Recursion as a separate role — the Command Zone template implies it, but it has no existing
  classifier predicate and would be new detection logic, not a de-merge.
- Renaming the display label of any other role.

---
*Plan written: 2026-07-26*
