---
phase: 102-structural-analysis-role-floors
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - DeckFlow.Web/Services/CutLab/CutLabRoleAssigner.cs
  - DeckFlow.Web/Services/CutLab/CutLabStructuralFindings.cs
  - DeckFlow.Web/Services/Manabase/PlanRoleClassifier.cs
  - DeckFlow.Web.Tests/CutLabRoleAssignerTests.cs
  - DeckFlow.Web.Tests/CutLabStructuralFindingsTests.cs
  - DeckFlow.Web.Tests/Manabase/PlanRoleClassifierTests.cs
autonomous: true
requirements: [SLOT-01, SLOT-02]
must_haves:
  truths:
    - "Every pool card maps to zero or more of the eight role keys using ONLY existing signals — no new classification model, no new keyword lists"
    - "A land NEVER counts as ramp — the lands and ramp roles are disjoint, because IsRampCard's first arm returns true for every type line containing 'Land' and must be land-gated"
    - "One-shot removal (Swords to Plowshares) and, in cEDH mode, pure counterspells land in the interaction role despite the PlanRole permanent gate"
    - "Ramp/draw/lands roles populate from DeckStatClassifier/IsLand even though PlanRole deliberately excludes them"
    - "A card may hold multiple roles simultaneously (e.g. draw + engines; payoffs + wincons overlap is allowed and honest)"
    - "Each of the five finding detectors returns a finding with per-card evidence when its signal triggers, and nothing when it does not"
    - "The stranded-subtheme exclusion set is PlanRoleClassifier's OWN vocabulary, consumed via a shared helper — never a duplicated substring list that can drift"
    - "When Spellbook or category data failed open, the dependent detectors report data-unavailable instead of a confident false negative"
  artifacts:
    - path: "DeckFlow.Web/Services/CutLab/CutLabRoleAssigner.cs"
      provides: "Pure eight-role assignment composed from PlanRoleClassifier + DeckStatClassifier + CutLabLockRules.IsLand + combo membership, with the ramp rule land-gated"
      contains: "AssignRoles"
    - path: "DeckFlow.Web/Services/CutLab/CutLabStructuralFindings.cs"
      provides: "Five deterministic finding detectors with evidence and two source-availability flags"
      contains: "categoryDataAvailable"
    - path: "DeckFlow.Web/Services/Manabase/PlanRoleClassifier.cs"
      provides: "Shared category-vocabulary helper so the stranded-subtheme exclusion cannot drift from FromCategories"
      contains: "CategoryMapsToPlanRole"
  key_links:
    - from: "CutLabRoleAssigner.AssignRoles"
      to: "PlanRoleClassifier.Classify"
      via: "pre-gate interaction out-param overload"
      pattern: "interactionMeritPreGate"
    - from: "CutLabRoleAssigner.AssignRoles"
      to: "DeckStatClassifier"
      via: "IsRampCard/IsDrawCard/IsProtectionCard/IsClosingPowerCard predicates (ramp gated by !IsLand)"
      pattern: "DeckStatClassifier\\."
    - from: "CutLabStructuralFindings"
      to: "PlanRoleClassifier.CategoryMapsToPlanRole"
      via: "stranded-subtheme role-vocabulary exclusion"
      pattern: "CategoryMapsToPlanRole"
    - from: "CutLabStructuralFindings"
      to: "SpellbookAlmostCombo"
      via: "enabler-starved near-combo evidence"
      pattern: "SpellbookAlmostCombo"
---

<objective>
Build the two pure analysis rule sets of Phase 102: CutLabRoleAssigner (SLOT-01 — the eight-role
functional-slot assignment composed entirely from existing classifiers) and CutLabStructuralFindings
(SLOT-02 — the five evidence-backed structural detectors with fail-open degradation).

Purpose: These are the only genuinely new "classification-adjacent" code in the phase, and the
success criteria pin the constraint: existing role/category inference only. Shipping them as pure
statics with exhaustive unit tests proves the taxonomy before the page service (plan 03) wires I/O
into them.
Output: CutLabRoleAssigner, CutLabStructuralFindings, a small shared vocabulary helper on
PlanRoleClassifier (behavior-preserving refactor), and their xUnit test files (new CutLab tests flat
in DeckFlow.Web.Tests/, namespace DeckFlow.Web.Tests; PlanRoleClassifierTests extended in place).

DECISIONS locked for this plan (from 102-RESEARCH Open Questions 1-2 + UI-SPEC + codex plan review):
- Payoffs vs win conditions OVERLAP by design: wincons = IsClosingPowerCard OR included-combo member
  (ungated); payoffs = PlanRole.Payoff (permanent-gated). A card may be both; no precedence rule.
- PlayExperience string → ManabaseMode drives classification ("cEDH"→Cedh, "Focused"→Focused,
  else Casual); bracket drives numeric floors (plan 01). When they disagree, PlayExperience wins
  for classification.
- Finding thresholds are named product constants with // Why: comments, [ASSUMED]-flagged, exact
  values specified below (RESEARCH A3 — listed for user adjustment at review).
- RAMP IS LAND-GATED (codex review HIGH): DeckStatClassifier.IsRampCard returns TRUE for every type
  line containing "Land" (DeckStatClassifier.cs:17), so the ramp role rule is
  `!CutLabLockRules.IsLand(typeLine) && DeckStatClassifier.IsRampCard(typeLine, oracle)` — a Forest
  is exactly [lands], never ramp; role counts and every downstream floor/finding stay honest.
- STRANDED-SUBTHEME EXCLUSION VIA SHARED HELPER (codex review MEDIUM): the role-vocabulary exclusion
  consults a new `PlanRoleClassifier.CategoryMapsToPlanRole` helper that FromCategories itself is
  refactored to share — never a reproduced substring list in CutLabStructuralFindings.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/cut-lab/phases/102-structural-analysis-role-floors/102-RESEARCH.md
@.planning/workstreams/cut-lab/phases/102-structural-analysis-role-floors/102-PATTERNS.md
@.planning/workstreams/cut-lab/phases/102-structural-analysis-role-floors/102-UI-SPEC.md
@.planning/workstreams/cut-lab/REQUIREMENTS.md

<interfaces>
<!-- PlanRoleClassifier (DeckFlow.Web/Services/Manabase/PlanRoleClassifier.cs:43-48) — the ONLY
  entry point for interaction (pre-gate overload; the permanent gate at lines 81-85 strips one-shot
  removal from returned flags, line 70 captures interaction merit first):
  public static PlanRole Classify(CardFact fact, IReadOnlyList<string> categories,
      bool isComboPiece, ManabaseMode mode, out bool interactionMeritPreGate) -->

<!-- PlanRoleClassifier.FromCategories (same file, lines 102-143): the category substring vocabulary
  lives as private Has(...) keyword groups inside this method (payoff/finisher/wincon...; tutor/combo;
  removal/interaction/protect/wipe/answer; counter [cEDH-gated]; engine/advantage/card draw/value/
  draw-not-ramp). Task 2 extracts this vocabulary into a shared helper — behavior-preserving. -->

<!-- DeckStatClassifier (DeckFlow.Core/Analysis/DeckStatClassifier.cs) — verified signatures:
  IsRampCard(string typeLine, string oracleText)   :16  ⚠ first arm: typeLine contains "Land" ⇒ TRUE
  IsDrawCard(string oracleText)                    :29
  IsBoardWipeCard(oracle)                          :54-59
  IsClosingPowerCard(typeLine, oracle)             :78-85
  IsTargetedRemovalCard(typeLine, oracle)          :138-144
  IsProtectionCard(string name, string oracleText) :180-185 -->

<!-- CutLabLockRules.IsLand(string? typeLine) — CutLabLockRules.cs:123-124, MDFC front-face aware -->

<!-- ScryfallCardFactMapper.ToCardFact(ScryfallCardData card, int quantity, bool isCommander = false)
  — DeckFlow.Core/Manabase/ScryfallCardFactMapper.cs:16; the bridge plan 03 uses. This plan's tests
  hand-build CardFact instances directly (DeckFlow.Core/Manabase/CardFact.cs). -->

<!-- SpellbookAlmostCombo (DeckFlow.Web/Services/CommanderSpellbookService.cs:26-30):
  public sealed record SpellbookAlmostCombo(string MissingCard, IReadOnlyList<string> CardsInDeck,
      IReadOnlyList<string> Results, string Instructions); -->

<!-- Degradation-flag pattern (DeckFlow.Core/Analysis/WinConMapAggregator.cs:22-27, 49-61):
  bool comboDataAvailable parameter — false when the fail-open source was down, and the result
  reflects unavailability rather than a confident empty finding. -->

<!-- Curve-bucket convention (DeckFlow.Core/Analysis/DeckStatAggregator.cs): nonland MV buckets
  "0-1", "2", "3", "4", "5+". -->

<!-- Role keys (fixed order, plan 01 also defines CutLabFloorRules.RoleKeys — this plan must use
  identical strings but does NOT reference plan 01 code, keeping wave-1 write-sets disjoint):
  lands, ramp, draw, interaction, protection, engines, payoffs, wincons -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: CutLabRoleAssigner — eight-role assignment from existing signals</name>
  <files>DeckFlow.Web/Services/CutLab/CutLabRoleAssigner.cs, DeckFlow.Web.Tests/CutLabRoleAssignerTests.cs</files>
  <behavior>
    - A basic Forest (typeLine "Basic Land — Forest") → roles contain EXACTLY "lands" — never ramp,
      despite IsRampCard's Land-typeline arm returning true for it (the land gate wins)
    - Cultivate (sorcery, "search your library for ... land") → contains "ramp" (nonland land-search
      passes the gate), never via PlanRole (Pitfall 1)
    - A modal DFC land front face (e.g. typeLine "Land" front) → contains "lands", NOT "ramp"
    - Swords to Plowshares (instant, exile creature) in ANY mode → contains "interaction" via the pre-gate out-param despite the permanent gate stripping the flag
    - Counterspell (pure counterspell) → contains "interaction" when mode is Cedh; does NOT when mode is Casual (mode gate respected)
    - Rhystic Study (repeatable permanent draw) → contains "draw" and "engines" (multi-membership)
    - A combo piece name present in the included-combo set → contains "wincons" even when IsClosingPowerCard is false
    - Torment of Hailfire (X sorcery closer) → contains "wincons" via IsClosingPowerCard (ungated) even though PlanRole.Payoff is stripped by the permanent gate
    - ResolveMode("cEDH") → ManabaseMode.Cedh; "Focused" → Focused; "Casual"/""/unknown → Casual (OrdinalIgnoreCase)
  </behavior>
  <read_first>
    - DeckFlow.Web/Services/Manabase/PlanRoleClassifier.cs (FULL read — precedence, permanent gate lines 81-85, pre-gate capture line 70, mode-gated counterspell lines 196-208, xmldoc style to copy)
    - DeckFlow.Core/Analysis/DeckStatClassifier.cs (predicate signatures and semantics — note IsRampCard's first arm is a "Land" type-line check, which is WHY the ramp role must be land-gated)
    - DeckFlow.Core/Manabase/CardFact.cs (the input record's members — name, type line, oracle text, mana value)
    - DeckFlow.Web/Services/CutLab/CutLabLockRules.cs (IsLand at line 123)
    - DeckFlow.Web.Tests/CutLabLockStateTests.cs (flat-root test conventions)
  </read_first>
  <action>
    Create CutLabRoleAssigner.cs (namespace DeckFlow.Web.Services.CutLab) as a `public static class`.
    Class xmldoc documents the composition contract: the eight-role taxonomy is WIDER than PlanRole
    (which deliberately excludes ramp/lands/filler draw — cite PlanRoleClassifier), every signal is an
    existing classifier, and multi-membership is allowed (a cut decrements every role the card fills).
    Expose:
      - `public static ManabaseMode ResolveMode(string? playExperience)` — "cEDH" → ManabaseMode.Cedh,
        "Focused" → ManabaseMode.Focused, else ManabaseMode.Casual (OrdinalIgnoreCase, null-safe).
      - `public static IReadOnlyList<string> AssignRoles(CardFact fact, IReadOnlyList<string> categories,
        bool isComboPiece, ManabaseMode mode)` — returns the card's role keys (subset of the eight, in
        the canonical fixed order lands, ramp, draw, interaction, protection, engines, payoffs, wincons):
        * call PlanRoleClassifier.Classify(fact, categories, isComboPiece, mode, out bool interactionMeritPreGate) ONCE
        * lands: CutLabLockRules.IsLand(fact type line)
        * ramp: `!CutLabLockRules.IsLand(typeLine) && DeckStatClassifier.IsRampCard(typeLine, oracle)`
          — with a `// Why:` comment: IsRampCard's first arm (DeckStatClassifier.cs:17) returns true
          for EVERY type line containing "Land", so without the gate every land would double-count as
          ramp, inflating ramp counts across floors and findings (codex review HIGH). Lands and ramp
          are disjoint by construction.
        * draw: DeckStatClassifier.IsDrawCard(oracle)
        * interaction: interactionMeritPreGate OR DeckStatClassifier.IsBoardWipeCard(oracle) OR DeckStatClassifier.IsTargetedRemovalCard(typeLine, oracle)
        * protection: DeckStatClassifier.IsProtectionCard(name, oracle)
        * engines: roles.HasFlag(PlanRole.Engine)
        * payoffs: roles.HasFlag(PlanRole.Payoff)
        * wincons: DeckStatClassifier.IsClosingPowerCard(typeLine, oracle) OR isComboPiece
        Use the same CardFact member the existing classifiers consume for type line / oracle text (read
        CardFact.cs and PlanRoleClassifier call sites; do not invent new projections).
        ArgumentNullException.ThrowIfNull(fact) and categories. No I/O, no logger, no async.
      - Role-key string constants (or a static readonly array) local to this class — identical strings
        to plan 01's CutLabFloorRules.RoleKeys but defined independently so wave-1 write-sets stay
        disjoint (plan 03 asserts the two lists match with a wiring test).

    Write CutLabRoleAssignerTests.cs covering every behavior row: hand-built CardFact instances with
    realistic type lines and oracle text, category lists where the signal is category-driven, and both
    Casual and Cedh mode rows for the counterspell gate. Assert exact role-set contents (order-sensitive
    where the API returns the canonical order). The land-gate rows are mandatory: Forest → exactly
    ["lands"]; Cultivate → contains "ramp"; a modal DFC land front face → "lands" and NOT "ramp".
  </action>
  <verify>
    <automated>dotnet test DeckFlow.Web.Tests --filter "FullyQualifiedName~CutLabRoleAssignerTests" --nologo</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'interactionMeritPreGate' DeckFlow.Web/Services/CutLab/CutLabRoleAssigner.cs` returns ≥1 (pre-gate overload used — Pitfall 2)
    - `grep -c 'DeckStatClassifier\.' DeckFlow.Web/Services/CutLab/CutLabRoleAssigner.cs` returns ≥4 (ramp/draw/protection/wincon predicates reused, not duplicated)
    - `grep -c 'IsLand' DeckFlow.Web/Services/CutLab/CutLabRoleAssigner.cs` returns ≥2 (lands role AND the ramp land-gate both go through CutLabLockRules.IsLand)
    - The ramp rule is land-gated: IsRampCard is never called without the `!...IsLand` guard (code review of the single call site; a test asserts Forest → exactly ["lands"])
    - No new keyword/regex classification lists exist in the file (no string-literal oracle-text matching beyond delegating to existing classifiers)
    - A test asserts Forest maps to exactly ["lands"] (never ramp); another asserts Cultivate contains "ramp"; another asserts a modal DFC land front face maps to "lands" and not "ramp"
    - A test asserts Swords to Plowshares lands in interaction in Casual mode; another asserts Counterspell is interaction ONLY in Cedh mode
    - A test asserts a card can hold ≥2 roles simultaneously
    - All CutLabRoleAssignerTests pass
  </acceptance_criteria>
  <done>Eight-role assignment is a pure, tested composition of existing signals with the land-gated ramp rule (lands and ramp disjoint), the pre-gate interaction fix, mode gating, and honest multi-membership — zero new classification vocabulary introduced.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: CutLabStructuralFindings — five detectors with evidence and degradation flags; shared vocabulary helper on PlanRoleClassifier</name>
  <files>DeckFlow.Web/Services/CutLab/CutLabStructuralFindings.cs, DeckFlow.Web/Services/Manabase/PlanRoleClassifier.cs, DeckFlow.Web.Tests/CutLabStructuralFindingsTests.cs, DeckFlow.Web.Tests/Manabase/PlanRoleClassifierTests.cs</files>
  <behavior>
    - Curve congestion: 40-card nonland pool with 15 cards at MV 3 (37.5% ≥ 30% AND ≥ 12 cards) → one finding, evidence lists the 15 cards with their MV, lead follows "{n} nonland cards sit at mana value {bucket} — {pct}% of your nonland pool."
    - Curve congestion NOT triggered when a bucket holds 30%+ share but fewer than 12 cards
    - Stranded subtheme: category "landfall" on exactly 3 pool cards (within 2..4) and CategoryMapsToPlanRole("landfall") == false → finding "'{theme}' appears on only {n} cards — likely too few to function as a theme." with the 3 member cards as evidence
    - Stranded subtheme NOT triggered for a category on 5+ cards, on 1 card, or for a category where PlanRoleClassifier.CategoryMapsToPlanRole returns true (e.g. "Removal", "Card Draw", "Win Condition")
    - CategoryMapsToPlanRole drift-guard: for representative category names ("Win Condition", "Card Draw", "Removal", "counterspells", "tutor", "Ramp", "landfall", "tokens"), CategoryMapsToPlanRole(name) == (PlanRoleClassifier.FromCategories([name], ManabaseMode.Cedh) != PlanRole.None) — the helper and FromCategories can never disagree
    - All pre-existing PlanRoleClassifierTests still pass unchanged (the FromCategories refactor is behavior-preserving)
    - Redundant finishers: wincons count 6 against floor 3 (count ≥ floor + 3) → finding with the finisher list and count-vs-floor lead
    - Weak floor case: interaction count 8 against floor 7 (count ≤ floor + 1) → finding naming the role, count, floor, and member cards
    - Enabler-starved: a SpellbookAlmostCombo with 2+ CardsInDeck → finding per starved combo, lead "{card} is missing its combo partner{s}: {missing}."; an almost-combo with only 1 card in deck is ignored (noise)
    - comboDataAvailable=false → redundant-finishers still computes from closing-power counts, but enabler-starved returns the degraded marker (no confident empty); categoryDataAvailable=false → stranded-subthemes returns the degraded marker
    - All five detectors silent (healthy pool, both flags true) → empty findings list (the all-clear empty state is a view concern)
  </behavior>
  <read_first>
    - DeckFlow.Web/Services/Manabase/PlanRoleClassifier.cs lines 94-143 (FromCategories — the substring vocabulary being extracted into the shared helper; the mode-gated "counter" arm at 127-132 stays mode-gated in FromCategories)
    - DeckFlow.Web.Tests/Manabase/PlanRoleClassifierTests.cs (existing tests — they must stay green; the drift-guard test is added here)
    - DeckFlow.Core/Analysis/WinConMapAggregator.cs (the comboDataAvailable degradation pattern at lines 22-27, 49-61 — copy the xmldoc param wording)
    - DeckFlow.Core/Analysis/DeckStatAggregator.cs (curve-bucket convention "0-1","2","3","4","5+")
    - DeckFlow.Web/Services/CommanderSpellbookService.cs lines 16-30 (SpellbookCombo/SpellbookAlmostCombo records)
    - DeckFlow.Core/Manabase/ManabaseRampDrawBudget.cs lines 84-93 (named-constant + // Why: comment style for thresholds)
    - .planning/workstreams/cut-lab/phases/102-structural-analysis-role-floors/102-UI-SPEC.md (Copywriting Contract — the five lead patterns and the two degradation strings are prescriptive)
  </read_first>
  <action>
    FIRST, the shared vocabulary helper (codex review MEDIUM — the exclusion set must be
    PlanRoleClassifier's own vocabulary, not a reproduced copy):
    In PlanRoleClassifier.cs add
      `internal static bool CategoryMapsToPlanRole(string categoryName)`
    — returns true when the lowercased category name matches ANY of the keyword groups FromCategories
    consumes (payoff group: win/finisher/payoff/wincon/win con/win-con/closer/beater; tutor/combo;
    interaction group: removal/interaction/protect/wipe/answer; counter — mode-AGNOSTIC here, a
    "counter" tag is role vocabulary regardless of mode; engine group: engine/advantage/card draw/value,
    plus draw-and-not-ramp/mana). Refactor FromCategories to share the same keyword predicates (e.g.
    extract each Has(...) group into a small private static predicate consumed by BOTH FromCategories
    and CategoryMapsToPlanRole) so the vocabulary exists ONCE. Behavior-preserving: FromCategories'
    outputs are byte-identical, including the cEDH-only counter gate — only the helper is mode-agnostic.
    xmldoc on the helper: it exists so CutLabStructuralFindings' stranded-subtheme exclusion can consult
    the classifier's own vocabulary; drift between the two is structurally impossible.
    In DeckFlow.Web.Tests/Manabase/PlanRoleClassifierTests.cs add the drift-guard test: for the
    representative names in the behavior row, assert
    `PlanRoleClassifier.CategoryMapsToPlanRole(name) == (PlanRoleClassifier.FromCategories(new[]{name}, ManabaseMode.Cedh) != PlanRole.None)`
    (Cedh so the counter arm participates on both sides). All existing tests stay untouched and green.

    THEN create CutLabStructuralFindings.cs (namespace DeckFlow.Web.Services.CutLab) as a `public static class`
    with pure `Compute(...)`. Define co-located result types:
      - `public enum CutLabFindingKind { CurveCongestion, StrandedSubtheme, RedundantFinishers, WeakFloorCase, EnablerStarved }`
      - `public sealed record CutLabFindingEvidence` (`string CardName`, `double? ManaValue` — MV populated
        only for curve-congestion chips per UI-SPEC "Card Name · MV 3")
      - `public sealed record CutLabFinding` (`CutLabFindingKind Kind`, `string Heading`, `string Lead`,
        `IReadOnlyList<CutLabFindingEvidence> Evidence`) — Heading strings fixed by UI-SPEC: "Curve congestion",
        "Stranded subthemes", "Redundant finishers", "Weak floor cases", "Enabler-starved cards"
      - `public sealed record CutLabStructuralFindingsResult` (`IReadOnlyList<CutLabFinding> Findings`,
        `bool ComboDataAvailable`, `bool CategoryDataAvailable`) — the view renders the UI-SPEC degradation
        copy off the two flags; this class does not embed the degradation strings
      - input record `public sealed record CutLabAnalyzedCard` (`string Name`, `double ManaValue`,
        `bool IsLand`, `IReadOnlyList<string> Roles`, `IReadOnlyList<string> Categories`)
    Signature:
      `public static CutLabStructuralFindingsResult Compute(IReadOnlyList<CutLabAnalyzedCard> pool,
       IReadOnlyList<SpellbookAlmostCombo> nearCombos, IReadOnlyDictionary<string, int> floors,
       bool comboDataAvailable, bool categoryDataAvailable)`
    Detectors (role counts derived internally from pool Roles; floors keyed by the eight role-key strings):
      1. Curve congestion — bucket nonland cards by the DeckStatAggregator convention ("0-1","2","3","4","5+");
         flag any bucket where share ≥ `CongestionShareThreshold = 0.30` AND count ≥ `CongestionMinimumCards = 12`.
      2. Stranded subthemes — requires categoryDataAvailable; group pool cards by category; candidate themes
         are categories on `StrandedThemeMinCards = 2` .. `StrandedThemeMaxCards = 4` cards, excluding any
         category where `PlanRoleClassifier.CategoryMapsToPlanRole(category)` returns true (the shared
         helper added above — do NOT reproduce the substring vocabulary here; a `// Why:` comment cites
         the drift risk).
      3. Redundant finishers — wincons role count ≥ floors["wincons"] + `RedundantFinisherMargin = 3`;
         evidence = the wincons cards.
      4. Weak floor cases — one finding per role where count ≤ floor + `WeakFloorMargin = 1` (matches the
         UI-SPEC at-floor marker band); evidence = the role's member cards.
      5. Enabler-starved — requires comboDataAvailable; one finding per SpellbookAlmostCombo with
         CardsInDeck.Count ≥ `NearComboMinPiecesInDeck = 2`; lead lists MissingCard.
    All seven threshold constants: `private const`, one `// Why:` comment each, plus a single [ASSUMED]
    xmldoc block on the class stating they are unsigned product constants awaiting sign-off (RESEARCH A3).
    Lead sentences follow the UI-SPEC patterns verbatim (percentages rounded to whole numbers). Banned
    words in any copy this class emits: "bad", "worse", "should cut", "weak card" (non-judgmental rule).
    ArgumentNullException.ThrowIfNull on inputs; no detector may throw on empty/degraded inputs.

    Write CutLabStructuralFindingsTests.cs covering every behavior row above, including both degradation
    flags, the helper-driven exclusion rows, and the healthy-pool empty result.
  </action>
  <verify>
    <automated>dotnet test DeckFlow.Web.Tests --filter "FullyQualifiedName~CutLabStructuralFindingsTests|FullyQualifiedName~PlanRoleClassifierTests" --nologo</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'comboDataAvailable' DeckFlow.Web/Services/CutLab/CutLabStructuralFindings.cs` returns ≥1 and `grep -c 'categoryDataAvailable' DeckFlow.Web/Services/CutLab/CutLabStructuralFindings.cs` returns ≥1
    - `grep -c 'CategoryMapsToPlanRole' DeckFlow.Web/Services/Manabase/PlanRoleClassifier.cs` returns ≥2 (declaration + FromCategories sharing) and `grep -c 'CategoryMapsToPlanRole' DeckFlow.Web/Services/CutLab/CutLabStructuralFindings.cs` returns ≥1 (exclusion consumes the helper; no local substring vocabulary)
    - The drift-guard test exists in DeckFlow.Web.Tests/Manabase/PlanRoleClassifierTests.cs and passes; every pre-existing PlanRoleClassifierTests case passes unchanged (behavior-preserving refactor)
    - The seven threshold constants exist with the exact values 0.30, 12, 2, 4, 3, 1, 2 as specified (`grep -c 'CongestionShareThreshold' ...` ≥1 etc. for each named constant)
    - `grep -c '// Why:' DeckFlow.Web/Services/CutLab/CutLabStructuralFindings.cs` returns ≥7 (one per threshold)
    - `grep -ci 'should cut' DeckFlow.Web/Services/CutLab/CutLabStructuralFindings.cs` returns 0 (banned-word check; likewise no "worse"/"bad card" copy)
    - A test asserts enabler-starved is NOT reported as an empty confident result when comboDataAvailable=false (degraded flag surfaces instead)
    - A test asserts the curve-congestion lead contains the bucket label and rounded percentage
    - All CutLabStructuralFindingsTests pass
  </acceptance_criteria>
  <done>The five structural detectors compute deterministic, evidence-backed, non-judgmental findings with named product-constant thresholds and honest degradation when a fail-open source was down; the stranded-subtheme exclusion consults PlanRoleClassifier's own vocabulary through a shared, drift-guarded helper.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pool card names/oracle text → pure analyzers | Card data originates from Scryfall resolution server-side; user influences which cards, not the card facts |
| Spellbook / category-store responses → findings | External/fail-open sources; unavailability must not fabricate confident findings |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-102-02-01 | Information Disclosure / Integrity | Findings fabricated from absent upstream data | mitigate | Dual availability flags (comboDataAvailable/categoryDataAvailable) threaded through Compute; dependent detectors return degraded markers, never confident empties (Pitfall 6) |
| T-102-02-02 | Denial of Service | Pathological pool inputs (150 cards × many categories) into pure analyzers | mitigate | Pure O(n·categories) passes over ≤151 cards; no recursion, no I/O; pool size already capped upstream by CutLabPoolValidator |
| T-102-02-03 | Tampering | Client-supplied role data influencing findings | mitigate | Roles/categories are computed server-side per POST (plan 03); this API takes no client-originated role input by construction |
| T-102-SC | Tampering | npm/pip/cargo installs | accept | Zero new dependencies this phase |
</threat_model>

<verification>
- `dotnet build` clean, no new warnings
- `dotnet test DeckFlow.Web.Tests --filter "CutLabRoleAssignerTests|CutLabStructuralFindingsTests|PlanRoleClassifierTests"` green (incl. every pre-existing PlanRoleClassifier test — the refactor is behavior-preserving)
- No modification to any file outside this plan's write-set (wave-1 disjointness with plan 102-01 — 102-01 does NOT touch PlanRoleClassifier.cs; this plan owns it exclusively this wave)
- LF endings; changed-lines format gate
</verification>

<success_criteria>
- CutLabRoleAssigner assigns all eight roles from existing signals only, with the land-gated ramp rule (Forest → exactly [lands]), pre-gate interaction, mode-gated counterspells, and multi-membership proven by tests (SLOT-01)
- CutLabStructuralFindings implements the five detectors with UI-SPEC lead copy, named [ASSUMED] thresholds, evidence lists, and degradation flags (SLOT-02)
- Stranded-subtheme exclusion consumes PlanRoleClassifier.CategoryMapsToPlanRole (shared, drift-guarded) — zero duplicated vocabulary; zero new classification vocabulary; zero I/O in either CutLab class
</success_criteria>

<output>
Create `.planning/workstreams/cut-lab/phases/102-structural-analysis-role-floors/102-02-SUMMARY.md` when done
</output>
