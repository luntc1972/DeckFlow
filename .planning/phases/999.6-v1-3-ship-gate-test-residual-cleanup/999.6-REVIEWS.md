---
phase: 999.6
reviewers: [codex]
reviewed_at: 2026-05-22T16:23:33Z
plans_reviewed:
  - 999.6-01-PLAN.md
  - 999.6-02-PLAN.md
  - 999.6-03-PLAN.md
---

# Cross-AI Plan Review — Phase 999.6

## Codex Review

Using `gstack-openclaw-ceo-review` and `plan-eng-review` as the review frame.

**Summary**
Plan 01 is mostly sound for the mechanical stale-test work, but it has a fixture-source/path mismatch and incorrect total-count expectations that should be fixed before execution.

Plan 02 has the right root-cause-first shape, but as written it likely misses the real cross-class env-var race and its verification command cannot pass while two known P03 failures remain.

Plan 03 should not execute as written. Its diagnosis appears wrong against the current code: the failing assertions are `Assert.NotNull(service.GetJob(job.JobId))`, not `Assert.Null(service.GetActiveJob())`, and `GetActiveJob()` is store-derived, not backed by an `_activeJob` status field.

Overall: the phase goal is achievable, but P02 and P03 need plan amendments before implementation. I would block execution on the HIGH issues below.

**Strengths**
- Good sequential decomposition: P01 removes stale noise, P02 isolates the flaky auth case, P03 handles the ambiguous background-service failures.
- Strong no-skip discipline, with explicit skipped-count preservation.
- Good commit atomicity and triplet-in-commit-body requirements.
- Good use of debug artifacts before touching ambiguous areas.
- P01 D-04/D-05/D-07 are precise and low-risk.

**Concerns**
- **HIGH — P03 root cause is pointed at the wrong assertion.** Current code shows failures should be at `Assert.NotNull(service.GetJob(job.JobId))` on [ArchidektCacheJobServiceTests.cs](/mnt/c/users/chrislunt/source/personal/deckflow/DeckFlow.Web.Tests/ArchidektCacheJobServiceTests.cs:118) and line 170, matching CONTEXT.md’s original triage. The plan reframes them as `GetActiveJob()` null failures, which sends the investigation toward the wrong fix.
- **HIGH — P03 assumes an `_activeJob` status lifecycle that does not exist.** [ArchidektCacheJobService.GetActiveJob](/mnt/c/users/chrislunt/source/personal/deckflow/DeckFlow.Web/Services/ArchidektCacheJobService.cs:225) reads `IHarvestRunStore.GetActiveAsync`; `GetJob()` only returns the active row. The likely issue is completed-job lookup contract, not active-job clearing.
- **HIGH — P03 file scope is too narrow for the likely real fix.** If `GET /api/archidekt-cache-jobs/{jobId}` should work after completion, the fix likely needs `IHarvestRunStore.GetByIdAsync`, `HarvestRunStore`, fake store, and `ArchidektCacheJobService.GetJob`, not just the test helper.
- **HIGH — P02 likely root cause requires a second test class.** `AdminBruteForceTrackerStoreTests` also mutates `FEEDBACK_ADMIN_USER/PASSWORD`. A shared xUnit collection would need to cover both [BasicAuthMiddlewareTests.cs](/mnt/c/users/chrislunt/source/personal/deckflow/DeckFlow.Web.Tests/BasicAuthMiddlewareTests.cs:13) and [AdminBruteForceTrackerStoreTests.cs](/mnt/c/users/chrislunt/source/personal/deckflow/DeckFlow.Web.Tests/Security/AdminBruteForceTrackerStoreTests.cs:15), but P02’s modified-file list does not include the latter.
- **HIGH — P02 verification command cannot pass as written.** It runs full suite with `|| exit 1` while the expected post-P02 state is still `Failed: 2`. `dotnet test` exits nonzero on expected residual failures, so the loop fails even when P02 succeeded.
- **MEDIUM — P01 fixture source path can trigger an unnecessary deletion.** D-06 says recover from `C:\tmp\arna-test`; in WSL that is likely `/mnt/c/tmp/arna-test`, while the plan only checks `/tmp/arna-test`.
- **MEDIUM — P01 total-count acceptance is off by one.** Starting from 445, D-07 deletion means Branch A should end at 444; Branch B should end at 443. The plan sometimes says Branch B 444 and phase acceptance says 444/445.
- **LOW — Nonzero full-suite gates need parser wording.** P01/P02 should say “run and parse expected residuals” rather than implying a successful process exit before phase exit.

**Suggestions**
- Amend P03 before execution: reframe debug around `GetJob(jobId)` after terminal completion and the API contract implied by `AcceptedAtAction` / `GetByIdAsync`.
- Add conditional P03 files: `DeckFlow.Web/Services/Harvest/IHarvestRunStore.cs`, `HarvestRunStore.cs`, fake run store, and affected tests.
- Replace P03’s likely fix branches with: stale test update if completed lookup is not a contract, or production fix adding by-id run lookup if it is.
- Amend P02 to explicitly test cross-class env-var racing with `AdminBruteForceTrackerStoreTests`; if confirmed, use one shared named collection for all `FEEDBACK_ADMIN_*` mutators.
- Change P02 full-suite 5x verification to capture output and assert `Failed: 2, Skipped: 3`, not require exit code 0.
- Amend P01 Task 4 to check both `/tmp/arna-test` and `/mnt/c/tmp/arna-test` before taking the deletion fallback.
- Correct test-count expectations to Branch A `Total 444`, Branch B `Total 443`.

**Risk Assessment**
Overall risk: **HIGH until amended**.

P01 is low-to-medium risk after fixture/count corrections. P02 is medium risk but currently has a high-risk verification and file-scope gap. P03 is high risk because the plan’s diagnosis conflicts with the current code and could produce a test-only wait fix that does not address the actual `GetJob`/completed-run contract.
tokens used

---

## Consensus Summary

Single reviewer (Codex) — no consensus to synthesize. Self-review by Claude was skipped per cross-AI independence rule. CLI availability: codex ✓, gemini ✗, opencode ✗, qwen ✗, cursor ✗, coderabbit ✗.

### Blocking Concerns (HIGH — execution blocked)

1. **P03 root cause diagnosis wrong** — failing assertions are `Assert.NotNull(service.GetJob(job.JobId))` at lines 118/170, NOT `Assert.Null(service.GetActiveJob())`. Plan must reframe debug around `GetJob(jobId)` after terminal completion + the API contract implied by `AcceptedAtAction` / `GetByIdAsync`.
2. **P03 `_activeJob` lifecycle does not exist** — `ArchidektCacheJobService.GetActiveJob` reads `IHarvestRunStore.GetActiveAsync`, not an in-memory `_activeJob` field. Plan's fix-shape branches assume a contract that isn't there.
3. **P03 file scope too narrow** — likely real fix touches `IHarvestRunStore.GetByIdAsync`, `HarvestRunStore`, fake store, and `ArchidektCacheJobService.GetJob`. Plan only lists test file + service. Add conditional `files_modified` entries.
4. **P02 env-var race needs second test class** — `AdminBruteForceTrackerStoreTests` also mutates `FEEDBACK_ADMIN_USER/PASSWORD`. Shared xUnit collection must cover both `BasicAuthMiddlewareTests` AND `AdminBruteForceTrackerStoreTests`. P02 `files_modified` missing the second class.
5. **P02 verification command exit-code mismatch** — `dotnet test ... || exit 1` fails by definition because expected post-P02 state is `Failed: 2` (P03 still pending). Replace with parse-output assertion (`Failed: 2, Skipped: 3` grep match) rather than exit-code check.

### Medium

6. **P01 fixture source path** — D-06 says `C:\tmp\arna-test`; in WSL that is `/mnt/c/tmp/arna-test`, not `/tmp/arna-test`. Plan should probe both before falling back to deletion.
7. **P01 total-count off-by-one** — D-07 deletion means Branch A ends at Total 444, Branch B at Total 443. Plan acceptance criteria say 444/445 in places.

### Low

8. **Full-suite gate parser wording** — P01 and P02 closing gates should parse expected residual counts, not imply a zero exit code.

### Recommendation

**BLOCK execution.** Re-run `/gsd-plan-phase 999.6 --reviews` to replan with this feedback. P03 most urgent — the debug session can't proceed against a wrong-target assertion. P02 verification fix is also mandatory before any P02 execution.
