---
phase: 111-cut-lab-upgrade-regression-gate
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - DeckFlow.Web.Tests/CutLabViewModelWordingTests.cs
  - DeckFlow.Web/ts-tests/cut-lab-structural-evidence-lock.test.ts
  - DeckFlow.Web/e2e/cut-lab-pill-interactions.spec.ts
autonomous: true
requirements: [CLUP-09]
must_haves:
  truths:
    - "A Structural evidence pill that matches a canonical pool card locks/unlocks that card's pool checkbox (server render AND live JS patch)."
    - "A Structural evidence pill whose text matches NO lockable pool card renders inert (a plain span, no aria-pressed, click is a no-op)."
    - "Role-group card pills continue to lock/unlock the canonical pool checkbox (regression preserved)."
  artifacts:
    - path: "DeckFlow.Web/ts-tests/cut-lab-structural-evidence-lock.test.ts"
      provides: "Vitest coverage for createStructuralEvidenceChip matched-vs-inert render + click lock behavior"
      contains: "data-cut-lab-chip-card"
    - path: "DeckFlow.Web/e2e/cut-lab-pill-interactions.spec.ts"
      provides: "e2e proof a Structural evidence pill locks a canonical pool card end-to-end; inert invariant smoke-checked (deterministic inert proof lives in Vitest+xUnit)"
      contains: "data-cut-lab-structural-findings"
    - path: "DeckFlow.Web.Tests/CutLabViewModelWordingTests.cs"
      provides: "Server-side FindLockableEvidenceCard matched/inert coverage (extended)"
      contains: "FindLockableEvidenceCard"
  key_links:
    - from: "createStructuralEvidenceChip (cut-lab.ts:1344)"
      to: "findLockablePoolCardForEvidence (cut-lab.ts:1275)"
      via: "matched -> button[data-cut-lab-chip-card]; unmatched -> span.kb-chip"
      pattern: "data-cut-lab-chip-card"
    - from: "delegated click handler (cut-lab.ts:3253)"
      to: "tr[data-cut-lab-card] input[data-cut-lab-lock-card]"
      via: "button[data-cut-lab-chip-card] toggles the canonical pool checkbox"
      pattern: "cutLabChipCard"
---

<objective>
Prove CLUP-09: role-group AND Structural-evidence card pills lock/unlock the canonical
pool card, while a Structural evidence chip that matches no lockable pool card stays
inert. Regression coverage for shipped locking behavior (Phases 108-110.1), not new
product behavior.

Purpose: Cut Lab locking has two chip surfaces (role-group chips and Structural "finding
evidence" chips) that both drive the SAME canonical input[data-cut-lab-lock-card] pool
checkbox through one delegated click handler. Existing tests cover role-group chips and
individual pool pills, but NO test asserts (a) a Structural evidence chip locks the
canonical card, or (b) unmatched Structural evidence renders inert. That is the CLUP-09 gap.

Output: Extended xUnit wording tests, one new focused Vitest spec, and an extended
Playwright spec, all passing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md

<interfaces>
Client render + lock wiring - DeckFlow.Web/wwwroot/ts/cut-lab.ts:

- findLockablePoolCardForEvidence(evidence): { cardName, checkbox } | null   (line 1275)
    Case-insensitive (ASCII) match of evidence text against every enabled pool row
    (tr[data-cut-lab-card] with a non-disabled input[data-cut-lab-lock-card]). Also matches
    the mana-value evidence form. Returns null when nothing matches.

- createStructuralEvidenceChip(evidence, comboBadgeByCardName): HTMLElement   (line 1344)
    match = findLockablePoolCardForEvidence(evidence)
      match == null -> createTextElement('span', 'kb-chip', evidence)          (INERT)
      match != null -> <button type="button" class="kb-chip cutlab-role-chip"
                          data-cut-lab-chip-card="<match.cardName>"
                          aria-pressed="<checkbox.checked>">   (+ locked class + combo badge)

- Delegated click handler (line 3253): a click on button[data-cut-lab-chip-card] finds
    tr[data-cut-lab-card="<name>"] input[data-cut-lab-lock-card], flips .checked, calls
    refreshAndSerialize(). A plain span has no data attr, so the handler ignores it (inert).

- Structural findings DOM anchors:
    section = [data-cut-lab-structural-findings]
    body    = [data-cut-lab-structural-findings-body]
    rebuilt chip = [data-cut-lab-structural-findings-body] button[data-cut-lab-chip-card="<name>"]

Server render - DeckFlow.Web/Models/CutLabViewModel.cs:180:
    CutLabPoolCard? FindLockableEvidenceCard(string evidence)   (server-side mirror used by the
    Razor structural-findings partial to emit a lockable button vs an inert span.)

Scaffolds to COPY patterns from (do NOT duplicate their assertions):
  - DeckFlow.Web/ts-tests/cut-lab-lock-interactions.test.ts (lines 416, 471)
  - DeckFlow.Web/ts-tests/cut-lab-structural-cardtext.test.ts (CutLabUiPatch fixture shape)
  - DeckFlow.Web.Tests/CutLabViewModelWordingTests.cs (FindLockableEvidenceCard_* lines 16, 41)
  - DeckFlow.Web/e2e/cut-lab-pill-interactions.spec.ts (extend; admin-lock + setToolEnabled + oversizedPool)
  - DeckFlow.Web/e2e/cut-lab-structure.spec.ts (JS decide drive + structural-findings locators)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Vitest - Structural evidence chip matched-lock + unmatched-inert</name>
  <files>DeckFlow.Web/ts-tests/cut-lab-structural-evidence-lock.test.ts</files>
  <behavior>
    - Render matched: after a JS decide/patch whose structuralFindings evidence names a pool
      card ("Counterspell"), the rebuilt chip is button[data-cut-lab-chip-card="Counterspell"]
      with aria-pressed reflecting the canonical checkbox state.
    - Render inert: evidence with NO matching pool card ("Curve congestion at MV 2") renders
      as span.kb-chip, NOT a button, no data-cut-lab-chip-card, no aria-pressed.
    - Click-lock: clicking the matched button flips the canonical
      tr[data-cut-lab-card="Counterspell"] input[data-cut-lab-lock-card] .checked and updates
      aria-pressed; second click unlocks.
    - Click-inert: dispatching a click on the inert span changes no pool checkbox.
  </behavior>
  <action>
    Create DeckFlow.Web/ts-tests/cut-lab-structural-evidence-lock.test.ts (Vitest + jsdom).
    Reuse the jsdom bootstrap + DeckFlowCutLab init pattern from
    cut-lab-structural-cardtext.test.ts and cut-lab-lock-interactions.test.ts (copy their
    document HTML fixture: a pool table with tr[data-cut-lab-card] rows each holding
    input[data-cut-lab-lock-card], plus the [data-cut-lab-structural-findings] /
    [data-cut-lab-structural-findings-body] section). Build a CutLabUiPatch whose
    structuralFindings contains one group with two evidence items: one matching a pool card
    ("Counterspell") and one non-lockable ("Curve congestion at MV 2"). Drive the patch the
    same way cut-lab-structural-cardtext.test.ts does, then assert the four behaviors above by
    querying the structural-findings body. Do NOT assert combo-badge specifics - only chip
    element type, data attr, aria-pressed, and the canonical checkbox side-effect. Match
    existing TS style; preserve LF endings.
  </action>
  <verify>
    <automated>cd DeckFlow.Web && npm test -- cut-lab-structural-evidence-lock</automated>
  </verify>
  <done>New spec passes; matched (button, locks) and inert (span, no-op) cases both asserted.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: xUnit - extend server FindLockableEvidenceCard matched/inert coverage</name>
  <files>DeckFlow.Web.Tests/CutLabViewModelWordingTests.cs</files>
  <behavior>
    - A pool with a lockable non-commander card returns that CutLabPoolCard from
      FindLockableEvidenceCard for exact-name evidence AND the mana-value form.
    - Evidence matching only the commander, a card not in the pool, or a non-card phrase
      returns null (inert).
  </behavior>
  <action>
    Extend DeckFlow.Web.Tests/CutLabViewModelWordingTests.cs. Do NOT rewrite existing tests.
    Add [Theory]/[Fact] cases that (1) build a CutLabViewModel pool containing at least one
    lockable non-commander card plus a commander, (2) assert FindLockableEvidenceCard returns
    the expected CutLabPoolCard (identity by card name) for the exact-name and mana-value
    forms, and (3) assert null for a card NOT in the pool and for a commander-only match.
    Reuse the pool/view-model builder the existing FindLockableEvidenceCard_* tests already
    use (copy that construction). Keep xUnit conventions (Assert.Equal / Assert.Null), Allman
    braces, LF endings, changed-lines format-gate clean.
  </action>
  <verify>
    <automated>"/mnt/c/Program Files/dotnet/dotnet.exe" test DeckFlow.Web.Tests/DeckFlow.Web.Tests.csproj --filter "FullyQualifiedName~CutLabViewModelWordingTests"</automated>
  </verify>
  <done>New assertions pass alongside existing ones; matched identity and inert-null both proven.</done>
</task>

<task type="auto">
  <name>Task 3: e2e - Structural evidence pill locks canonical pool card; unmatched inert</name>
  <files>DeckFlow.Web/e2e/cut-lab-pill-interactions.spec.ts</files>
  <action>
    Add ONE new test to cut-lab-pill-interactions.spec.ts (keep the existing Commander-Table
    Lock-All test untouched). Reuse acquireAdminLockForTest, setToolEnabled(page, 'Cut Lab',
    true), and oversizedPool (already includes 'Counterspell' and 'Command Tower'). Flow:
      1. Import the oversized pool (same selectors as the existing test up to the "Lock your
         pool" heading: #cut-lab-input-source -> PasteText, #cut-lab-deck-text,
         #cut-lab-primary-plan, Import pool button).
      2. Locate [data-cut-lab-structural-findings]. If it only populates after a decide, drive
         one JS decide first using the cut-lab-structure.spec.ts pattern (waitForResponse on
         /api/cut-lab/decide). Then assert the lockable-evidence button set is non-empty:
         expect(await page.locator('[data-cut-lab-structural-findings] button[data-cut-lab-chip-card]').count(),
         'expected >= 1 lockable Structural evidence chip from the oversized pool decide').toBeGreaterThan(0).
         (MED-3) If a future engine change legitimately yields zero lockable structural-evidence
         chips for this pool, do NOT let the test pass silently: either adjust the fixture pool so
         at least one evidence card is in-pool, or test.skip() with an explicit annotation that
         CLUP-09 lock behavior is still proven by Task 1 Vitest + Task 2 xUnit. Pick the first
         lockable evidence button and read its data-cut-lab-chip-card into cardName.
      3. Assert tr[data-cut-lab-card="<cardName>"] input[data-cut-lab-lock-card] is NOT
         checked, click the evidence button, assert checked + aria-pressed 'true'; click again
         -> unchecked, aria-pressed 'false'.
      4. Inert smoke (H4): the AUTHORITATIVE deterministic proof that unmatched Structural
         evidence is inert comes from Task 1 Vitest (a patch with a known non-lockable evidence
         item -> span.kb-chip, no aria-pressed, click no-op) and Task 2 xUnit
         (FindLockableEvidenceCard returns null). This e2e step is a corroborating smoke only,
         because a live decide cannot deterministically guarantee an unmatched evidence item is
         present. Assert: (a) the invariant that NO
         [data-cut-lab-structural-findings] span.kb-chip carries data-cut-lab-chip-card (count
         == 0, always true); and (b) IF any [data-cut-lab-structural-findings] span.kb-chip is
         present, assert it is not a <button>, has no aria-pressed attribute, capture the count
         of checked input[data-cut-lab-lock-card] before/after clicking it, and assert the
         count is UNCHANGED (clicking an inert span is a no-op on pool lock state). If no inert
         span is present in this run, skip (b) with an inline comment that the deterministic
         inert proof lives in Task 1 + Task 2.
    Preserve LF endings; match the file's existing TS style. Server MUST be launched headless
    via scripts/run-web-test.sh (never open a Windows browser); run Playwright headless in WSL.
    (MED-1) SEQUENCING: this is a Wave-1 e2e plan. run-web-test.sh runs `fuser -k 5173/tcp` on
    start, which would kill a sibling plan's server. The three Wave-1 e2e-bearing plans (111-01
    Task 3, 111-02 Task 3, 111-03 Task 2) MUST be executed sequentially, NOT concurrently. The
    verify command below enforces this defensively: it REUSES an already-running :5173 server
    instead of launching (and killing) a second one.
  </action>
  <verify>
    <automated>if curl -s -o /dev/null http://localhost:5173/; then SRV=""; else bash scripts/run-web-test.sh > scratchpad-web.log 2>&1 & SRV=$!; until curl -s -o /dev/null http://localhost:5173/; do sleep 2; done; fi; (cd DeckFlow.Web && env -u DISPLAY -u WAYLAND_DISPLAY npx --no-install playwright test e2e/cut-lab-pill-interactions.spec.ts); RC=$?; [ -n "$SRV" ] && kill $SRV 2>/dev/null || true; exit $RC</automated>
  </verify>
  <done>New e2e test passes: a Structural evidence pill locks/unlocks the canonical card (deterministic); the inert invariant holds (no span carries the lockable attr) and any present inert span is a no-op on click. Deterministic unmatched-inert proof is owned by Task 1 Vitest + Task 2 xUnit.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness -> app under test | Playwright drives the local headless app; admin routes gated by BasicAuth + admin-lock helper. No production surface touched. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-111-01 | Tampering | e2e admin-lock serialization | accept | Reuse existing acquireAdminLockForTest + setToolEnabled serialization; no new admin write path introduced. |
| T-111-SC | Tampering | npm/NuGet installs | mitigate | No new packages. Reuse @playwright/test, xUnit, vitest already present; any package add requires user approval (blocked here). |
</threat_model>

<verification>
Run the plan's gates in order and confirm each passes:
1. `cd DeckFlow.Web && npm test -- cut-lab-structural-evidence-lock` (new Vitest spec green).
2. `"/mnt/c/Program Files/dotnet/dotnet.exe" test DeckFlow.Web.Tests/DeckFlow.Web.Tests.csproj --filter "FullyQualifiedName~CutLabViewModelWordingTests"` (server matched/inert green). If WSL VSTest is unstable, fall back to push-and-watch CI (`dotnet test DeckFlow.sln -c Release`).
3. Launch app via `scripts/run-web-test.sh`, then `env -u DISPLAY -u WAYLAND_DISPLAY npx --no-install playwright test e2e/cut-lab-pill-interactions.spec.ts` (structural evidence lock + inert green).
No production code changed. If a gate reveals a real defect in shipped code, record it as a deviation in the SUMMARY and hand the fix to Codex.
</verification>

<success_criteria>
- CLUP-09 proven at three layers: server (FindLockableEvidenceCard), client render/click
  (Vitest), and end-to-end (Playwright).
- Matched Structural evidence locks/unlocks the canonical pool checkbox; unmatched Structural
  evidence is inert (span, no data attr, click no-op).
- All three gates pass; no new packages; LF endings + changed-lines format gate clean.
</success_criteria>

<output>
Create `.planning/phases/111-cut-lab-upgrade-regression-gate/111-01-SUMMARY.md` when done.
</output>
