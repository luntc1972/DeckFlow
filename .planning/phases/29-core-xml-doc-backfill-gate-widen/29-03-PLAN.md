---
phase: 29-core-xml-doc-backfill-gate-widen
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - DeckFlow.Core/Knowledge/CategoryKnowledgeRepository.cs
  - DeckFlow.Core/Knowledge/BoardCategoryComparer.cs
  - DeckFlow.Core/Knowledge/ArchidektDeckCacheSession.cs
autonomous: true
requirements: [HSK-01]

must_haves:
  truths:
    - "Probe build over DeckFlow.Core/Knowledge/*.cs reports 0 CS1591/CS1573/CS1587 warnings"
    - "All 5 Knowledge-folder CS1573 sites have their complete <param> set (board, deckCount, boardFilter, deckCountIncrement) in positional order"
    - "No SQL raw-string content or indentation changed in CategoryKnowledgeRepository.cs"
  artifacts:
    - path: "DeckFlow.Core/Knowledge/CategoryKnowledgeRepository.cs"
      provides: "Documented repository (7 sites incl. 5 CS1573 param completions)"
      contains: "/// <summary>"
    - path: "DeckFlow.Core/Knowledge/BoardCategoryComparer.cs"
      provides: "Documented comparer (3 sites)"
      contains: "/// <summary>"
    - path: "DeckFlow.Core/Knowledge/ArchidektDeckCacheSession.cs"
      provides: "Documented cache-session model (2 sites)"
      contains: "/// <summary>"
  key_links:
    - from: "DeckFlow.Core/Knowledge/CategoryKnowledgeRepository.cs"
      to: "method parameters board / deckCount / boardFilter / deckCountIncrement"
      via: "completed <param> tags in positional order"
      pattern: "param name=\"board\""
---

<objective>
Add XML doc comments to every undocumented public type and member in `DeckFlow.Core/Knowledge/` (3 files, ~12 sites). CategoryKnowledgeRepository carries 7 sites including 5 of the phase's 6 CS1573 (incomplete `<param>` set) cases and 50 SQL raw-string literals (HIGH risk).

Purpose: Knowledge is the highest-risk folder of the HSK-01 Core backfill. Completing it cleanly - especially the CS1573 param-set completions without disturbing SQL raw strings - is a prerequisite for the Wave-2 gate widen.
Output: 3 fully-documented source files; probe build over Knowledge reports 0 doc warnings (CS1591 AND CS1573).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<scope_fence>
ALLOWED FILE SET (Codex MUST NOT touch any file outside this list):
- DeckFlow.Core/Knowledge/CategoryKnowledgeRepository.cs
- DeckFlow.Core/Knowledge/BoardCategoryComparer.cs
- DeckFlow.Core/Knowledge/ArchidektDeckCacheSession.cs

You may create and delete a TEMPORARY `DeckFlow.Core/.editorconfig` probe file ONLY for verification; it MUST be removed before commit and MUST NOT appear in `git status`.
Do NOT touch `.editorconfig` at repo root. Do NOT touch any other Core folder. Ignore tasks belonging to plans 29-01, 29-02, 29-04, 29-05.
Before starting, run `git status --short` and confirm a clean tree; if other modified files appear, STOP and report.
</scope_fence>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-core-xml-doc-backfill-gate-widen/29-RESEARCH.md
@.planning/phases/29-core-xml-doc-backfill-gate-widen/29-PATTERNS.md

# Gold-standard doc-style exemplars (READ - match this style exactly):
@DeckFlow.Core/Content/IContentVideoStore.cs
@DeckFlow.Core/Content/ContentVideoStore.cs
</context>

<constraints>
From CLAUDE.md + 29-RESEARCH.md (full authority - every constraint is blocking):
- Touch ONLY the `///` doc lines you add. Do NOT run Format Document / Code Cleanup.
- NEVER convert `{ get; init; }` to `{ get; }`. None of these files contain init accessors, but the rule stands.
- NEVER re-indent C# raw-string literals (triple-quote). CategoryKnowledgeRepository.cs has 50 SQL raw strings, all inside method bodies (lines ~64-299 per 29-PATTERNS.md). Doc `///` lines go ABOVE member declaration lines (~33, ~45, ~235, ~409, ~508) only - never near or inside a raw-string block.
- NEVER inline an attribute onto a declaration line.
- Preserve LF line endings.
- CS1573 is all-or-nothing: a method with ANY `<param>` tags must have ALL. Insert ONLY the missing tags in their correct positional slot; do NOT remove existing `<param>` tags.
- Build env: `"/mnt/c/Program Files/dotnet/dotnet.exe"`. Clean build is the gate.
- Commits: plain default-author; one logical change per commit.
</constraints>

<tasks>

<task type="auto">
  <name>Task 1: Probe the work list and complete the 5 CS1573 param sets in CategoryKnowledgeRepository</name>
  <files>DeckFlow.Core/Knowledge/CategoryKnowledgeRepository.cs</files>
  <read_first>
    - DeckFlow.Core/Knowledge/CategoryKnowledgeRepository.cs (current state - read lines ~225-260, ~400-420, ~500-520 to locate the 3 CS1573 methods; do NOT read or alter raw-string bodies)
    - .planning/phases/29-core-xml-doc-backfill-gate-widen/29-RESEARCH.md (CS1573 sites table; Probe build methodology)
    - .planning/phases/29-core-xml-doc-backfill-gate-widen/29-PATTERNS.md (Pattern 10 CS1573 fix; Raw-String Literal Safety Zone)
  </read_first>
  <action>
    Run the probe first (temp `DeckFlow.Core/.editorconfig` root=false with CS1591/1573/1587 = warning, clean non-incremental build, `grep | sort -u`, filter to Knowledge files, remove temp file) to get the authoritative Knowledge work list. Then complete the CS1573 param sets per the RESEARCH CS1573 table: `GetCategoryRowsForCardAsync` (add `<param name="boardFilter">`), `ReplaceSourceRowsAsync` (add `<param name="board">` and `<param name="deckCount">`), `PersistObservedCategoriesAsync` (add `<param name="board">` and `<param name="deckCountIncrement">`). Each new `<param>` tag must sit in the SAME positional slot as the parameter in the signature (e.g., `board`/`deckCount` between `rows`/`normalizedCardName` and `cancellationToken` per 29-PATTERNS.md Pattern 10). Do NOT remove or reorder existing `<param>` tags. Also add `<summary>` to any remaining undocumented public site flagged by the probe in this file (type and other public members). All `///` lines go ABOVE the declaration; never touch SQL raw-string lines.
  </action>
  <verify>
    <automated>grep -c 'param name="board"' DeckFlow.Core/Knowledge/CategoryKnowledgeRepository.cs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'param name="boardFilter"' DeckFlow.Core/Knowledge/CategoryKnowledgeRepository.cs` is at least 1.
    - `grep -c 'param name="board"' ...` is at least 2 (ReplaceSourceRowsAsync + PersistObservedCategoriesAsync).
    - `grep -c 'param name="deckCount"' ...` is at least 1 and `grep -c 'param name="deckCountIncrement"' ...` is at least 1.
    - `git diff DeckFlow.Core/Knowledge/CategoryKnowledgeRepository.cs` shows no change to any line containing a triple-quote delimiter or to lines between delimiters.
    - `DeckFlow.Core/.editorconfig` removed; absent from `git status`.
  </acceptance_criteria>
  <done>All 5 CS1573 param sets completed in positional order; SQL raw strings untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Document BoardCategoryComparer + ArchidektDeckCacheSession and probe-verify Knowledge clean</name>
  <files>DeckFlow.Core/Knowledge/BoardCategoryComparer.cs, DeckFlow.Core/Knowledge/ArchidektDeckCacheSession.cs</files>
  <read_first>
    - DeckFlow.Core/Knowledge/BoardCategoryComparer.cs (current state - 3 sites)
    - DeckFlow.Core/Knowledge/ArchidektDeckCacheSession.cs (current state - 2 sites)
    - .planning/phases/29-core-xml-doc-backfill-gate-widen/29-PATTERNS.md (Pattern 1 method docs; Pattern 4 record/property one-liners; one-line form)
    - DeckFlow.Core/Content/IContentVideoStore.cs (model/comparer doc style)
  </read_first>
  <action>
    Document the 3 sites in `BoardCategoryComparer.cs` and the 2 sites in `ArchidektDeckCacheSession.cs` per the Task-1 probe list. Add a `<summary>` to each undocumented type; for methods add `<summary>` plus `<param>` for every parameter and `<returns>` when a value is returned; for record/model properties use the single-line `/// <summary>text.</summary>` form. Match the Content KB exemplar style exactly. Then re-run the probe and confirm ZERO CS1591/CS1573/CS1587 warnings remain for any file under `DeckFlow.Core/Knowledge/` (this validates both this task's files AND Task 1's CS1573 completions).
  </action>
  <verify>
    <automated>DOTNET="/mnt/c/Program Files/dotnet/dotnet.exe"; printf '%s\n' 'root = false' '' '[*.cs]' 'dotnet_diagnostic.CS1591.severity = warning' 'dotnet_diagnostic.CS1573.severity = warning' 'dotnet_diagnostic.CS1587.severity = warning' > DeckFlow.Core/.editorconfig; "$DOTNET" build DeckFlow.Core/DeckFlow.Core.csproj --no-incremental 2>&1 | grep -E "warning CS(1591|1573|1587)" | grep "Knowledge/" | sort -u > /tmp/29-03.log; rm -f DeckFlow.Core/.editorconfig; if [ -s /tmp/29-03.log ]; then echo FAIL; cat /tmp/29-03.log; else echo "OK 0 Knowledge doc warnings"; fi</automated>
  </verify>
  <acceptance_criteria>
    - Probe yields 0 CS1591/CS1573/CS1587 warnings for any `DeckFlow.Core/Knowledge/` file (`/tmp/29-03.log` empty) - confirms no surviving CS1573.
    - `DeckFlow.Core/.editorconfig` removed; absent from `git status`.
    - `git diff --stat` lists only the 3 allowed Knowledge files.
    - `"/mnt/c/Program Files/dotnet/dotnet.exe" build DeckFlow.Core/DeckFlow.Core.csproj -c Release` reports 0 errors.
  </acceptance_criteria>
  <done>All 3 Knowledge files documented; folder-scoped probe shows 0 doc warnings incl. CS1573; temp probe file removed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer edit -> source tree | `///` insertions in a 50-raw-string SQL file risk silent SQL-literal mutation |
| temp probe file -> repo | A lingering `DeckFlow.Core/.editorconfig` would alter solution-wide warning severity |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-03-01 | Tampering | 50 SQL raw strings in CategoryKnowledgeRepository.cs | mitigate | `///` lines added only above member declarations (lines ~33/45/235/409/508); git diff acceptance asserts no triple-quote line changed |
| T-29-03-02 | Tampering | CS1573 param-set completions | mitigate | Insert only missing tags in positional order; grep acceptance asserts each new tag present; existing tags not removed |
| T-29-03-03 | Tampering | Temp probe `DeckFlow.Core/.editorconfig` | mitigate | Probe file removed and absence asserted; never committed |
| T-29-03-04 | Denial of Service | Build pipeline | accept | Doc comments are compile-stripped; zero runtime/behavior change possible |
| T-29-03-SC | Tampering | npm/pip/cargo installs | n/a | No package installs in this phase |
</threat_model>

<verification>
- Probe build reports 0 CS1591/CS1573/CS1587 for `DeckFlow.Core/Knowledge/`.
- All 5 CS1573 param tags present (grep assertions).
- `git status --short` shows no `DeckFlow.Core/.editorconfig`.
- `git diff --stat` lists only the 3 allowed files.
- Full Core build clean (0 errors) under the still-active global suppressor.
</verification>

<success_criteria>
All ~12 undocumented public sites across the 3 Knowledge files carry XML doc comments; the 5 CS1573 param sets are complete and positionally correct; folder-scoped probe build is warning-free for CS1591 AND CS1573; SQL raw strings unchanged; only allowed files changed.
</success_criteria>

<output>
Create `.planning/phases/29-core-xml-doc-backfill-gate-widen/29-03-SUMMARY.md` when done.
</output>
