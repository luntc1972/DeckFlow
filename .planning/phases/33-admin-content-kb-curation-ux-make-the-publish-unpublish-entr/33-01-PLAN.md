---
phase: 33-admin-content-kb-curation-ux
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - DeckFlow.Web/Views/AdminContentKb/Index.cshtml
  - DeckFlow.Web/wwwroot/ts/content-kb-admin.ts
  - DeckFlow.Web/wwwroot/css/admin-common.css
autonomous: true
requirements: [KBUX-01]

must_haves:
  truths:
    - "An admin can type into a single filter box and the entries table instantly narrows to rows whose title, source, or any tag contains the typed text (case-insensitive substring), with no full page reload."
    - "Clearing the filter box restores all rows that the current server-side visibility toggle (All/Published/Hidden) allows."
    - "A live result count shows how many entries currently match (e.g. '12 of 240 entries shown')."
    - "Each entry row carries machine-readable searchable text (title + source + tags) so filtering does not depend on parsing rendered chip markup."
    - "When the filter matches zero rows, a friendly inline 'no matches' message appears in place of the table rows — rendered as a single full-width `<tr>` inside `<tbody>` that the filter logic shows only when no entry row matches, so it visually replaces the rows rather than appearing below the table."
  artifacts:
    - path: "DeckFlow.Web/Views/AdminContentKb/Index.cshtml"
      provides: "Filter input markup, per-row data-kb-search attribute, live-count element, no-matches tbody row"
      contains: "kb-filter-search"
    - path: "DeckFlow.Web/wwwroot/ts/content-kb-admin.ts"
      provides: "wireEntryFilter() that filters rows client-side off data-kb-search and updates the count + empty-state row"
      contains: "kb-filter-search"
    - path: "DeckFlow.Web/wwwroot/css/admin-common.css"
      provides: ".admin-shell-scoped styling for the filter bar, count, and no-match row"
      contains: "kb-filter"
  key_links:
    - from: "Index.cshtml #kb-filter-search input"
      to: "content-kb-admin.ts wireEntryFilter()"
      via: "getElementById + input event listener"
      pattern: "kb-filter-search"
    - from: "content-kb-admin.ts wireEntryFilter()"
      to: "entry <tr> rows"
      via: "querySelectorAll on [data-kb-search] reading the attribute value"
      pattern: "data-kb-search"
---

<objective>
Add an instant client-side filter/search box above the Content KB entries table so an admin
can locate a specific entry to publish/unpublish in a list that has grown long, by typing any
fragment of the entry's title, source/creator, or tags.

Purpose: The curation list (all index rows, published + hidden) is rendered in full and has
grown too long to scan by eye. The Phase 32/30 work already surfaces every row; this phase makes
that long list navigable.

Output: A filter input in `Index.cshtml`, a `wireEntryFilter()` behavior in the existing
`content-kb-admin.ts` module, and `.admin-shell`-scoped CSS in `admin-common.css`.

Design choice — CLIENT-SIDE filtering (rationale, MUST follow):
The controller `AdminContentKbController.Index` already calls `_store.GetAllRowsAsync(...)` and
materializes EVERY row into `model.Entries`; the view renders all of them in one `@foreach`. The
list is a single-operator, bounded admin grid (low hundreds of rows). Therefore filtering is done
in the browser against the already-rendered DOM — NO new controller parameter, NO new endpoint,
NO new query string, NO server round-trip. This also means the filter text never reaches the
server, so there is zero injection/parameter surface introduced (see threat model). The existing
server-side `visibilityFilter` toggle (All / Published / Hidden) is UNCHANGED and continues to be
the server-side narrowing; the client filter narrows further within whatever the toggle returned.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<allowed_file_set>
This plan may modify ONLY these three files:
  1. DeckFlow.Web/Views/AdminContentKb/Index.cshtml
  2. DeckFlow.Web/wwwroot/ts/content-kb-admin.ts
  3. DeckFlow.Web/wwwroot/css/admin-common.css
Do NOT touch the controller, the view model, any other view, any other .ts, any other .css,
the guild-theme site*.css files, or compiled wwwroot/js output (gitignored — never stage .js).
Ignore any task or idea not listed in THIS plan. If a change appears to require editing a file
outside this set, STOP and report instead of widening scope.
</allowed_file_set>

<interfaces>
<!-- Extracted from the codebase. The executor must use these as-is; no exploration needed. -->

From DeckFlow.Web/Models/AdminContentKbViewModel.cs — KbEntryRow (each row in Model.Entries):
  Id (long), Title (string), Source (string), Tags (IReadOnlyList<string>),
  IsVisible (bool), IsEvergreen (bool), RelevanceScore (double?)

Current entries table structure in Index.cshtml (the @foreach over Model.Entries, ~lines 164-238):
  <table class="admin-table admin-table--card"> inside <div class="admin-table-scroll" tabindex="0">
  <thead> has a VARIABLE number of <th> columns: Title, Source, Tags, Status,
    (optional) Score (artifact) — only when previewActive — and Action.
    So the visible column count is 5 normally, 6 when previewActive is true.
  Each row is a <tr> with cells:
    <td data-label="Title" class="admin-kb-title">@entry.Title</td>
    <td data-label="Source" class="admin-kb-source">@entry.Source</td>
    <td data-label="Tags"> ... <span class="kb-tag">@tag</span> ... </td>
    <td data-label="Status"> ... </td>
    (optional) <td data-label="Score (artifact)"> ... </td>   // only when previewActive
    <td data-label="Action"> publish/unpublish + evergreen forms </td>

Existing client behaviors in content-kb-admin.ts (an IIFE that wires on DOMContentLoaded):
  wireReloadConfirm(), wireTwoClickConfirm(), wireScrollRestore(), wireToast(),
  wireCommanderPreviewTypeahead()
  — all invoked inside the single document.addEventListener('DOMContentLoaded', ...) at the bottom.
  CSP is `script-src 'self'` (NO inline scripts) — all logic stays in this compiled module.

Existing admin CSS conventions (admin-common.css): every selector is prefixed `.admin-shell `.
CSS custom properties available: var(--text), var(--muted), var(--border), var(--panel),
var(--accent), var(--focus), var(--danger).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add filter input, per-row searchable attribute, live count, and empty-state tbody row to Index.cshtml</name>
  <read_first>
    - DeckFlow.Web/Views/AdminContentKb/Index.cshtml (whole file — note the `kb-entries-heading` section, the `admin-section-header` div, the `admin-table-scroll` wrapper, the variable column count driven by `previewActive`, and the entries `@foreach`)
    - DeckFlow.Web/Models/AdminContentKbViewModel.cs (KbEntryRow shape)
  </read_first>
  <action>
    In Index.cshtml, inside the entries `<section aria-labelledby="kb-entries-heading">`, between the
    existing `admin-section-header` div (the All/Published/Hidden toggle) and the
    `<div class="admin-table-scroll">`, add a filter bar wrapped in `<div class="kb-filter">`:
      - A `<label>` whose text is "Filter entries" containing an `<input>` with id `kb-filter-search`,
        type `search`, name omitted (NOT submitted — purely client-side), `autocomplete="off"`,
        placeholder "Filter by title, source, or tag", and `aria-controls="kb-entries-table"`.
      - A live-count element: `<p class="kb-filter__count" id="kb-filter-count" role="status" aria-live="polite"></p>`
        (left empty in Razor; the TS fills it on load and on each keystroke).
    Give the entries `<table>` the id `kb-entries-table` (add `id="kb-entries-table"` to the existing
    `<table class="admin-table admin-table--card">` — do not change its existing classes).
    On EACH entry `<tr>`, add a `data-kb-search` attribute whose value is the lowercase concatenation of
    title, source, and all tags joined by spaces, HTML-attribute-encoded by Razor's default `@` output.
    Build it in the `@foreach` as a local string before the `<tr>`, e.g. derive
    `searchText = (entry.Title + " " + entry.Source + " " + string.Join(" ", entry.Tags)).ToLowerInvariant()`
    and emit it as `data-kb-search="@searchText"`. Use the default `@` Razor expression (which HTML-encodes);
    do NOT use Html.Raw.
    Render the zero-match message as a single full-width row INSIDE the `<tbody>`, AFTER the entry
    `@foreach` but still within the `<tbody>` (so it lives among the data rows and visually replaces them
    when shown). It MUST be initially hidden and span every visible column. The column count is variable:
    compute it in Razor from `previewActive` (e.g. `var columnCount = previewActive ? 6 : 5;`) and emit:
    `<tr class="kb-filter__empty-row hidden" id="kb-filter-empty"><td colspan="@columnCount">No entries match the current filter.</td></tr>`.
    The TS toggles this row's visibility (the `hidden` class) based on match count — it is NOT placed after
    `</table>`.
    Touch only the lines needed; preserve LF line endings and existing indentation/markup style.
  </action>
  <verify>
    <automated>grep -c 'id="kb-filter-search"' DeckFlow.Web/Views/AdminContentKb/Index.cshtml | grep -qx 1 && grep -q 'id="kb-entries-table"' DeckFlow.Web/Views/AdminContentKb/Index.cshtml && grep -q 'data-kb-search=' DeckFlow.Web/Views/AdminContentKb/Index.cshtml && grep -q 'id="kb-filter-count"' DeckFlow.Web/Views/AdminContentKb/Index.cshtml && grep -q 'id="kb-filter-empty"' DeckFlow.Web/Views/AdminContentKb/Index.cshtml && grep -q 'colspan=' DeckFlow.Web/Views/AdminContentKb/Index.cshtml && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Index.cshtml contains exactly one input with id="kb-filter-search".
    - The entries `<table>` has id="kb-entries-table" and retains its existing `admin-table admin-table--card` classes.
    - Each entry `<tr>` carries a `data-kb-search` attribute built from title + source + tags, emitted via the default `@` Razor expression (no Html.Raw).
    - A live-count element `#kb-filter-count` (role="status", aria-live="polite") exists in the filter bar.
    - The zero-match element `#kb-filter-empty` is a `<tr>` INSIDE the `<tbody>` (after the entry `@foreach`, not after `</table>`) containing a single `<td colspan="N">` where N matches the visible column count (6 when previewActive, else 5); it is initially hidden via the `hidden` class.
    - The existing All/Published/Hidden toggle, preview form, per-row publish/evergreen forms, and antiforgery tokens are unchanged.
  </acceptance_criteria>
  <done>The view renders a filter input + count, every entry row exposes data-kb-search, and a full-width hidden empty-state row sits inside the tbody.</done>
</task>

<task type="auto">
  <name>Task 2: Wire client-side instant filtering in content-kb-admin.ts</name>
  <read_first>
    - DeckFlow.Web/wwwroot/ts/content-kb-admin.ts (whole file — note the IIFE structure, the existing wire* helpers, and the single DOMContentLoaded registration at the bottom)
    - DeckFlow.Web/Views/AdminContentKb/Index.cshtml (the markup added in Task 1, for the exact element ids)
  </read_first>
  <action>
    In content-kb-admin.ts, add a new helper `wireEntryFilter()` (same IIFE scope as the other wire* helpers).
    It must:
      - `getElementById('kb-filter-search')` as HTMLInputElement; if null, return (page may have no entries).
      - Read all DATA rows via `document.querySelectorAll<HTMLTableRowElement>('#kb-entries-table tbody tr')`
        but EXCLUDE the empty-state row (`#kb-filter-empty`) from the iterated set — e.g. resolve the empty-state
        row first and skip it in the loop, or query rows that are not the empty-state row. The empty-state row
        must never be treated as a data row (it carries no `data-kb-search`).
      - Resolve `#kb-filter-count` and the `#kb-filter-empty` tbody row (may be null — guard).
      - Define `applyFilter()`: take `input.value.trim().toLowerCase()`; for each DATA row read its
        `dataset.kbSearch ?? ''`; a row matches when the query is empty OR the row's data-kb-search
        `.includes(query)`. Toggle each non-matching row hidden via the `hidden` attribute
        (set `row.hidden = !matches`). Count matches against the total DATA-row count (excluding the
        empty-state row). Update `#kb-filter-count` textContent to `"{matched} of {total} entries shown"`
        (when query empty, still show the count, e.g. "240 of 240 entries shown"). Toggle the
        `#kb-filter-empty` tbody row's visibility (remove/add the `hidden` class on the `<tr>`) so it shows
        ONLY when matched === 0 AND total > 0 — so the empty-state row visually replaces the (now-hidden) data rows.
      - Add an `input` event listener on the search input calling `applyFilter`, and call `applyFilter()`
        once immediately so the initial count renders.
    Register `wireEntryFilter()` inside the existing `document.addEventListener('DOMContentLoaded', ...)`
    block alongside the other wire* calls. Do NOT introduce new dependencies, new globals, or inline scripts.
    Keep TypeScript `strict` happy (the project's tsconfig has strict:true): use explicit null guards and typed queries.
    Match the existing 2-space indentation and code style in the file.
  </action>
  <verify>
    <automated>grep -q "wireEntryFilter" DeckFlow.Web/wwwroot/ts/content-kb-admin.ts && grep -q "kb-filter-search" DeckFlow.Web/wwwroot/ts/content-kb-admin.ts && grep -q "kb-entries-table" DeckFlow.Web/wwwroot/ts/content-kb-admin.ts && grep -q "kb-filter-empty" DeckFlow.Web/wwwroot/ts/content-kb-admin.ts && grep -q "dataset.kbSearch" DeckFlow.Web/wwwroot/ts/content-kb-admin.ts && echo OK</automated>
    <automated>"/mnt/c/Program Files/dotnet/dotnet.exe" build DeckFlow.Web/DeckFlow.Web.csproj -clp:ErrorsOnly</automated>
  </verify>
  <acceptance_criteria>
    - content-kb-admin.ts defines `wireEntryFilter()` and calls it inside the existing DOMContentLoaded handler.
    - The filter reads `dataset.kbSearch` off `#kb-entries-table tbody tr` (excluding the `#kb-filter-empty` row) and toggles each data row's `hidden` property by case-insensitive substring match.
    - The live count `#kb-filter-count` updates on every `input` event and on initial load; total excludes the empty-state row.
    - The `#kb-filter-empty` tbody row is shown (its `hidden` class removed) only when zero data rows match and data rows exist, so it replaces the hidden rows in place.
    - `dotnet build DeckFlow.Web.csproj` exits 0 (TypeScript compiles via the MSBuild CompileTypeScriptAssets target with strict:true; behavior: no new compiler/TS errors).
    - No new package, no inline `<script>`, no global leakage (logic stays inside the existing IIFE).
  </acceptance_criteria>
  <done>Typing in the filter box instantly hides non-matching rows and updates the count without a page reload; at zero matches the in-tbody empty-state row appears in place of the rows; build is clean.</done>
</task>

<task type="auto">
  <name>Task 3: Style the filter bar, count, and empty state in admin-common.css</name>
  <read_first>
    - DeckFlow.Web/wwwroot/css/admin-common.css (lines ~160-250 — note `.admin-shell .admin-section-header`, `.admin-shell .admin-kb-toggle`, `.admin-shell .admin-table`, var(--*) tokens, and the `.hidden` utility convention used elsewhere)
    - DeckFlow.Web/Views/AdminContentKb/Index.cshtml (the kb-filter markup added in Task 1, for exact class names)
  </read_first>
  <action>
    Append (do NOT reformat existing rules) `.admin-shell`-scoped rules to admin-common.css for the filter bar:
      - `.admin-shell .kb-filter`: block layout with sensible top/bottom margin separating it from the
        section header and the table; flex column or row with gap so the input and count read clearly.
      - `.admin-shell .kb-filter label`: stack the label text above its input (display:flex; flex-direction:column),
        muted label color via var(--muted).
      - `.admin-shell #kb-filter-search`: full-width-ish search input matching existing admin input styling
        (border via var(--border), padding consistent with the preview form's text input, border-radius ~4px,
        background/text using the panel/text tokens).
      - `.admin-shell .kb-filter__count`: small, muted (var(--muted)), tabular-nums so the count digits do not jitter.
      - `.admin-shell .kb-filter__empty-row td`: a clearly-visible inline message (padding, var(--muted) text,
        centered if desired) on the full-width empty-state row. The row itself is hidden/shown by the existing
        `.hidden` utility — if `.hidden { display:none }` is not already in scope for the admin shell, confirm it
        is and rely on it rather than redefining it (note: `display:none` on a `<tr>` correctly removes the row).
    Use only existing CSS custom properties; do not add new color literals where a token exists.
    This is the admin-shell stylesheet (the admin page uses _AdminLayout.cshtml + admin-common.css, NOT the
    guild-theme site*.css system), so all new rules go here — do NOT add anything to site.css or site-common.css.
    Preserve LF line endings.
  </action>
  <verify>
    <automated>grep -q '\.admin-shell .kb-filter' DeckFlow.Web/wwwroot/css/admin-common.css && grep -q 'kb-filter__count' DeckFlow.Web/wwwroot/css/admin-common.css && grep -q 'kb-filter__empty-row' DeckFlow.Web/wwwroot/css/admin-common.css && echo OK</automated>
    <automated>! grep -q 'kb-filter' DeckFlow.Web/wwwroot/css/site.css && ! grep -q 'kb-filter' DeckFlow.Web/wwwroot/css/site-common.css && echo "NO-LAYOUT-CSS-IN-SITE-FILES"</automated>
  </verify>
  <acceptance_criteria>
    - admin-common.css contains `.admin-shell .kb-filter`, `.admin-shell .kb-filter__count`, and `.admin-shell .kb-filter__empty-row td` rules.
    - Every new rule is prefixed with `.admin-shell ` (matches existing convention).
    - New rules reference only existing var(--*) tokens for colors/borders (no raw hex where a token exists).
    - No `kb-filter` rule was added to site.css or site-common.css.
    - Existing admin-common.css rules are untouched (only appended-to).
  </acceptance_criteria>
  <done>The filter bar, count, and empty-state row render with admin-shell-consistent styling at desktop width.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    A client-side filter box above the Content KB entries table: typing any fragment of an entry's
    title, source, or tags instantly narrows the rendered list (no page reload), shows a live
    "{n} of {total} entries shown" count, and shows a "No entries match" row in place of the rows at
    zero matches. The existing All/Published/Hidden server toggle and per-row publish/evergreen actions
    are unchanged.
  </what-built>
  <how-to-verify>
    1. Start the dev server yourself (the project rule is: the user starts the server — ask the user to
       run it, or run your local build and have the user open it). Navigate to /Admin/ContentKb
       (BasicAuth gate applies — use the FEEDBACK_ADMIN_USER / FEEDBACK_ADMIN_PASSWORD creds).
    2. Confirm the filter box appears above the entries table with the initial count "{N} of {N} entries shown".
    3. Type part of a known entry title — confirm the table narrows instantly with no full-page reload and the count updates.
    4. Type part of a source/creator name, then part of a tag — confirm both match.
    5. Type gibberish — confirm the "No entries match the current filter." message appears IN PLACE OF the rows
       (as a full-width row inside the table body, not below the table) and the count shows "0 of {N}".
    6. Clear the box — confirm all rows (within the current All/Published/Hidden toggle) return and the empty-state row hides.
    7. Switch the All/Published/Hidden toggle and confirm the filter still works against the toggled set.
    8. Verify at a narrow/mobile viewport that the filter bar and table remain usable.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → /Admin/ContentKb | Admin-authenticated surface behind BasicAuthMiddleware (`/Admin/*` branch). Operator-only. |
| filter input → DOM | Filter text is consumed only in the browser; it never crosses to the server. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-01 | Tampering / Injection | Filter query string used in SQL | accept (not applicable) | Filtering is CLIENT-SIDE only; no new controller parameter, no new query string, no DB query touches the filter text. There is no injection surface because the value never reaches the server. |
| T-33-02 | Information Disclosure (XSS) | `data-kb-search` and entry title/source/tags rendered into the attribute/cells | mitigate | Values are emitted with the default Razor `@` expression which HTML-encodes; no `Html.Raw` is used on entry data. The TS reads `dataset.kbSearch` (already-decoded text) and uses `.includes()` + sets the `hidden` property — it never writes user/data text into the DOM via innerHTML, so no reflected/stored XSS is introduced. |
| T-33-03 | Elevation of Privilege | Admin authz on the page | mitigate | No new endpoint is added; the page remains behind the existing `/Admin/*` BasicAuth branch. The client filter cannot be reached unauthenticated because the page itself is gated. |
| T-33-04 | Denial of Service | Large row count filtered on every keystroke | accept | Single-operator admin grid (low hundreds of rows); `.includes()` over precomputed `data-kb-search` strings on `input` is O(rows) and trivially fast at this scale. No debounce required; revisit only if the corpus grows past a few thousand rows. |
| T-33-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan — pure view/TS/CSS edits using existing tooling. If any install appears necessary, STOP (see allowed_file_set). |
</threat_model>

<verification>
- `dotnet build DeckFlow.Web.csproj` exits 0 (TS compiles clean under strict mode).
- Index.cshtml exposes #kb-filter-search, #kb-entries-table, per-row data-kb-search, #kb-filter-count, and the #kb-filter-empty tbody row with a colspan td.
- content-kb-admin.ts defines and registers wireEntryFilter(), excluding the empty-state row from the data-row set.
- admin-common.css contains the .admin-shell .kb-filter* rules; site.css/site-common.css contain none.
- No new server-side filter parameter; no new endpoint; BasicAuth gate unchanged.
- Test note: this plan adds NO server-side logic (filtering is client-side DOM), so no new xUnit test is
  required. The existing AdminContentKbControllerTests remain green (build verifies). This skip is intentional
  and recorded here per project testing rules.
</verification>

<success_criteria>
- An admin can type a fragment of title, source, or tag and the entries list narrows instantly with no reload.
- A live "{matched} of {total} entries shown" count is visible and accurate.
- A friendly empty-state row shows in place of the rows at zero matches; clearing the box restores the toggle-scoped rows.
- The filter cooperates with the existing All/Published/Hidden server toggle.
- Build clean; no scope creep beyond the three allowed files.
</success_criteria>

<output>
Create `.planning/phases/33-admin-content-kb-curation-ux-make-the-publish-unpublish-entr/33-01-SUMMARY.md` when done.
</output>
</content>
</invoke>
