---
phase: 05-archidekt-bracket-capture
phase_number: "05"
phase_name: Archidekt Bracket Capture
plan: 02
type: execute
status: planned
generated: 2026-07-29
wave: 2
depends_on: [05-01]
dependencies: [05-01]
files_modified:
  - DeckFlow.Core/Knowledge/CategoryCacheSchema.cs
  - DeckFlow.Core/Knowledge/DeckQueueRepository.cs
  - DeckFlow.Core/Knowledge/CategoryKnowledgeRepository.cs
  - DeckFlow.Core/Knowledge/ArchidektDeckMetadataParameters.cs
  - DeckFlow.Web/Services/Persistence/CategoryKnowledgeStore.cs
  - DeckFlow.Core.Tests/CategoryKnowledgeRepositoryTests.cs
  - DeckFlow.Core.Tests/CategoryCacheSchemaParityTests.cs
  - DeckFlow.Core.Tests/ArchidektDeckMetadataParametersTests.cs
  - DeckFlow.Web.Tests/Integration/PostgresStorageTests.cs
autonomous: true
requirements: [BRKT-02, BRKT-03]
requirement_ids: [BRKT-02, BRKT-03]

must_haves:
  truths:
    - "Fresh and legacy `deck_queue` schemas contain nullable curated Archidekt metadata columns without backfill (BRKT-02, D-04)."
    - "`archidekt_metadata_captured_utc` distinguishes pre-change rows from post-change captured-absent rows (BRKT-03, D-03)."
    - "Skip/failure updates do not fabricate metadata or captured timestamps (D-06)."
    - "Repository metadata writes are shared by later bulk and URL paths through one `ArchidektDeckMetadata?` parameter (D-05)."
    - "A null-metadata update never wipes previously captured metadata: an already-captured row keeps all six original column values after a later `skip: true, metadata: null` mark (BRKT-03, D-06)."
    - "The URL upsert gates preservation **per record, not per column** (D-10): a non-null metadata record writes all six columns verbatim — including writing NULL over a previously-captured value that Archidekt no longer declares — while a null record preserves all six. `captured, absent` is therefore reachable on a re-import, and both write paths produce identical column state for identical metadata (D-05)."
    - "Every metadata SQL parameter is dialect-neutral: the mapping is a pure `ArchidektDeckMetadataParameters.From(metadata)` that emits only `int?` and `string?`, never `bool` or `DateTimeOffset` (BRKT-02)."
    - "Widening the two repository methods leaves no positional caller mis-bound: the one unprotected forward outside `DeckFlow.Core` (`CategoryKnowledgeStore.cs:110`) becomes a named-argument call in this wave, and `dotnet.exe build DeckFlow.sln --no-restore` proves `DeckFlow.Web` still compiles at the end of wave 2 rather than at the end of wave 3 (D-05)."
  artifacts:
    - path: "DeckFlow.Core/Knowledge/CategoryCacheSchema.cs"
      provides: "SQLite/Postgres nullable metadata schema"
      contains: "archidekt_metadata_captured_utc"
    - path: "DeckFlow.Core/Knowledge/DeckQueueRepository.cs"
      provides: "processed-row metadata update semantics"
      contains: "ArchidektDeckMetadata?"
    - path: "DeckFlow.Core/Knowledge/CategoryKnowledgeRepository.cs"
      provides: "public facade propagation"
      contains: "ArchidektDeckMetadata?"
    - path: "DeckFlow.Core/Knowledge/ArchidektDeckMetadataParameters.cs"
      provides: "pure dialect-neutral metadata-to-SQL-parameter mapping"
      contains: "ArchidektDeckMetadataParameters"
    - path: "DeckFlow.Web/Services/Persistence/CategoryKnowledgeStore.cs"
      provides: "positional-binding-proof forward to the widened repository method"
      contains: "MarkUrlDeckProcessedAsync(deckId, commanderName, cancellationToken: cancellationToken)"
    - path: "DeckFlow.Core.Tests/ArchidektDeckMetadataParametersTests.cs"
      provides: "dialect-independent parameter-type gate that runs without Docker"
      contains: "ArchidektDeckMetadataParameters"
  key_links:
    - from: "DeckFlow.Core/Knowledge/DeckQueueRepository.cs"
      to: "deck_queue"
      via: "MarkDeckProcessedAsync and MarkUrlDeckProcessedAsync SQL"
      pattern: "archidekt_metadata_captured_utc"
---

<objective>
Add the additive `deck_queue` schema and repository write semantics for curated Archidekt metadata, including three-state captured/absent behavior.

Purpose: BRKT-02 and BRKT-03 require nullable persistence that preserves existing rows and distinguishes "not captured yet" from "captured and absent".
Output: schema migration, repository/facade signature updates, and SQLite/Postgres parity tests.
</objective>

<execution_context>
@/home/clunt/.codex/get-shit-done/workflows/execute-plan.md
@/home/clunt/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/cycle21-cut-lab/ROADMAP.md
@.planning/workstreams/cycle21-cut-lab/REQUIREMENTS.md
@.planning/workstreams/cycle21-cut-lab/phases/05-archidekt-bracket-capture/05-CONTEXT.md
@.planning/workstreams/cycle21-cut-lab/phases/05-archidekt-bracket-capture/05-RESEARCH.md
@.planning/workstreams/cycle21-cut-lab/phases/05-archidekt-bracket-capture/05-PATTERNS.md
@.planning/workstreams/cycle21-cut-lab/phases/05-archidekt-bracket-capture/05-VALIDATION.md
@.planning/workstreams/cycle21-cut-lab/phases/05-archidekt-bracket-capture/05-01-SUMMARY.md

<interfaces>
From plan 05-01:
- `ArchidektDeckMetadata` has nullable `EdhBracket`, `DeckFormat`, `Theorycrafted`, `CreatedUtc`, `UpdatedUtc`, and non-null `CapturedUtc`. `ArchidektDeckImportResult.Metadata` is itself nullable — an unrecognizable payload records no capture at all.

From `DeckFlow.Core/Knowledge/CategoryCacheSchema.cs`:
- `EnsureSchemaAsync` creates `deck_queue` with nullable optional columns and then performs idempotent `ALTER TABLE ... ADD COLUMN ... NULL` after `GetTableColumnsAsync`. Pre-Phase-5 column list is `CategoryCacheSchema.cs:48-57`; the migration block to extend is `CategoryCacheSchema.cs:61-67`.
- Existing dialect hazard to mirror, not repeat: `last_checked_utc` is declared `TEXT` on both dialects (`CategoryCacheSchema.cs:54`) but is bound as a raw `DateTime` (`DeckQueueRepository.cs:308`), so `DateTimeTypeHandler` renders `"O"` text for SQLite and hands Postgres a native `timestamptz` — the F-51-PG-01 class of defect, rationale comment at `DeckQueueRepository.cs:125-133`. Existing code also never binds a `bool`: it writes `skipped = skip ? 1 : 0` (`DeckQueueRepository.cs:309`, `:456`).

From `DeckFlow.Core/Knowledge/DeckQueueRepository.cs`:
- `MarkDeckProcessedAsync(string deckId, string? commanderName, bool skip = false, CancellationToken cancellationToken = default)` updates queued decks.
- `MarkUrlDeckProcessedAsync(string deckId, string? commanderName, CancellationToken cancellationToken = default)` upserts URL-imported decks.

Full caller audit for the two widened methods, done at HEAD `7fe8987b` — inserting `ArchidektDeckMetadata? metadata = null` before `CancellationToken` re-binds argument 3 (URL) / argument 4 (processed) for every **positional** caller, which is `CS1503`, not a silent behavior change:
- `CategoryKnowledgeRepository.cs:257` — `MarkDeckProcessedAsync(deckId, commanderName, skip, cancellationToken)`, **positional**, breaks — but the file is in this plan's write set and is widened in the same task.
- `CategoryKnowledgeRepository.cs:300` — `MarkUrlDeckProcessedAsync(deckId, commanderName, cancellationToken)`, **positional**, breaks — same, in this plan's write set.
- `DeckFlow.Web/Services/Persistence/CategoryKnowledgeStore.cs:110` — `_repository.MarkUrlDeckProcessedAsync(deckId, commanderName, cancellationToken)`, **positional**, breaks, and it is the only unprotected site **outside** this plan's original write set. It is therefore pulled into Task 2 (see below).
- `ArchidektDeckCacheSession.cs:125` and `:137` — already use `skip: false` / `commanderName: null, skip: true` plus `cancellationToken: cancellationToken`, so they are **named** and safe; plan 05-03 edits them anyway.
- Every remaining call site passes two arguments only (`CategoryCacheSchemaParityTests.cs` ×12, `CategoryKnowledgeRepositoryTests.cs:388`, `ContentHashDedupTests.cs:306`, `PostgresStorageTests.cs:137`/`:161`, `CategoryKnowledgeStoreTests.cs:161-163`) and is unaffected.
- `AdminHarvestController.cs:273` calls the **web store's** 3-arg member, which plan 05-03 leaves byte-for-byte untouched, so it is unaffected by this plan.
</interfaces>

<source_audit>
GOAL: Phase 5 metadata becomes queryable future corpus provenance on `deck_queue`. Covered here.
REQ: BRKT-02 and BRKT-03 covered here.
RESEARCH: nullable additive schema, no backfill, captured timestamp, shared repository record, SQLite/Postgres parity. Covered here.
CONTEXT: D-01 curated columns, D-03 capture marker, D-04 nullable compatibility, D-05 shared repository surface, D-06 no fabricated metadata, and D-10 per-record URL-upsert gating covered here. D-08 hash boundary is verified in plan 05-03. Deferred raw JSON/backfill/UI are not planned.
</source_audit>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing schema and repository metadata tests</name>
  <read_first>
    - `DeckFlow.Core.Tests/CategoryKnowledgeRepositoryTests.cs` — direct SQLite repository assertions.
    - `DeckFlow.Core.Tests/CategoryCacheSchemaParityTests.cs` — **fresh-schema** parity assertions only. All 16 `[Fact]`s are fresh-schema (first `:22`, last `:477`; 0 `[Theory]`s; the file is 557 lines); `EnsureSchemaAsync` appears only at `:27` and `:70`; there is **no** legacy-schema test and **no** double-`EnsureSchemaAsync` idempotence test anywhere in the repo. Test 2 below must be built from scratch, not adapted.
    - `DeckFlow.Web.Tests/Integration/PostgresStorageTests.cs` — gated Postgres integration style; `DeckFlow.Web.Tests/Integration/PostgresFactAttribute.cs:14-18` is the gate.
    - `DeckFlow.Core/Knowledge/DeckQueueRepository.cs:125-133` — the F-51-PG-01 rationale comment; the same dialect trap this plan must not re-enter.
    - `.planning/workstreams/cycle21-cut-lab/phases/05-archidekt-bracket-capture/05-CONTEXT.md` D-03, D-04, D-06.
  </read_first>
  <files>DeckFlow.Core.Tests/CategoryKnowledgeRepositoryTests.cs, DeckFlow.Core.Tests/CategoryCacheSchemaParityTests.cs, DeckFlow.Core.Tests/ArchidektDeckMetadataParametersTests.cs, DeckFlow.Web.Tests/Integration/PostgresStorageTests.cs</files>
  <behavior>
    - Test 1: fresh SQLite `EnsureSchemaAsync` creates `archidekt_edh_bracket`, `archidekt_deck_format`, `archidekt_theorycrafted`, `archidekt_created_utc`, `archidekt_updated_utc`, and `archidekt_metadata_captured_utc`.
    - Test 2: legacy SQLite `deck_queue` missing those columns gains them idempotently and preserves existing rows with `archidekt_metadata_captured_utc IS NULL`.
    - Test 3: `MarkDeckProcessedAsync(..., metadata)` writes all provided metadata fields; `metadata.EdhBracket == null` still writes non-null `archidekt_metadata_captured_utc`.
    - Test 4 (**two-step, the BRKT-03 anti-wipe gate**): first `MarkDeckProcessedAsync(deckId, commanderName, skip: false, metadata: <non-null with all six values set>)`, **then** `MarkDeckProcessedAsync(deckId, commanderName: null, skip: true, metadata: null)`, then assert all six columns still hold the **original** values and `archidekt_metadata_captured_utc` is still non-null. This is reachable in production: `AddDeckIdsAsync` requeues any deck whose `last_checked_utc` predates the 5-day `DeckRefreshCooldown` (`DeckQueueRepository.cs:14`, predicate `:146`, `:151`), and a transient Archidekt `HttpRequestException` on re-harvest routes to `MarkDeckProcessedAsync(skip: true, metadata: null)` (`ArchidektDeckCacheSession.cs:137`). An implementation doing `SET archidekt_edh_bracket = NULL, ...` must fail this test.
    - Test 4b (separate, additional): on a **fresh** never-captured row, `MarkDeckProcessedAsync(..., skip: true, metadata: null)` leaves every Archidekt metadata column null. Keep this distinct from Test 4 — on a fresh row "left unchanged" and "overwritten with NULL" are indistinguishable, which is exactly why Test 4 exists.
    - Tests 5, 5b, and 5c are one set and they **partition the URL-upsert update branch** under D-10; write all three and do not let any two overlap. The partition is on the *record*, not the column: (5) record non-null, all six values non-null; (5c) record non-null, some values null; (5b) record itself null.
    - Test 5 (**all-non-null overwrite**): `MarkUrlDeckProcessedAsync(..., metadata)` inserts, then updates with a **second record whose six values are all non-null and each differs from the first**, asserting the update branch rewrites all six columns to the second record's values. Make the record "different" by changing values, never by nulling a field — the nulled-field case is Test 5c and conflating them would let one implementation satisfy both readings.
    - Test 5c (**non-null record containing a null field — the D-10 gate**): after a first capture with `EdhBracket = 3` and the other five values non-null, a second `MarkUrlDeckProcessedAsync` with a non-null record whose `EdhBracket` is **null** and whose other fields are non-null must leave `archidekt_edh_bracket` **NULL** and advance `archidekt_metadata_captured_utc` to the second record's value. This is the "Archidekt owner cleared the bracket" case: the row must end up saying *captured at T2, bracket absent*, not *captured at T2, bracket 3*. A per-column `COALESCE` implementation keeps the stale `3` and fails this test, which is exactly what it is for.
    - Test 5b (**null record — mirror of Test 4 for the URL upsert**): after a non-null `MarkUrlDeckProcessedAsync` capture, a later `MarkUrlDeckProcessedAsync(deckId, commanderName, metadata: null)` preserves all six previously captured values and leaves `archidekt_metadata_captured_utc` non-null and **unchanged**. `metadata = null` is the parameter default, so it is the easy accidental call.
    - Test 6: gated Postgres storage test verifies the metadata columns exist and one metadata write succeeds when `DECKFLOW_POSTGRES_TESTS=1` is enabled.
    - Test 7 (**dialect-independent parameter-type gate; runs with or without Docker**): `ArchidektDeckMetadataParameters.From(metadata)` emits, for every one of the six values and for both a fully-populated and a partly-null metadata record, only `int?` or `string?` — and **never** `bool`, `bool?`, `DateTime`, or `DateTimeOffset`. Include a case where `Theorycrafted` is `true`, one where it is `false`, and one where it is null.
      **Assert values, not only runtime types (Round 4, F4-2).** A type-only assertion is satisfied by an *inverted* mapping — `Theorycrafted ? 0 : 1` emits an `int` either way — and by any wrong timestamp rendering, because any string is a string. So the earlier "assert on the runtime type, not on its rendered text" instruction is **necessary but not sufficient**, and is hereby superseded on the value question: assert both. Specifically: `Theorycrafted = true` → `1`, `false` → `0`, `null` → `Assert.Null` (a null has **no** runtime type, so `Assert.IsType<int>` on the null case throws instead of failing meaningfully — do not write it that way); and each emitted timestamp string equals `value.ToUniversalTime().ToString("O", CultureInfo.InvariantCulture)` computed on the **`DateTimeOffset`**. Keep the type assertions for every non-null value.
  </behavior>
  <action>
Write tests before production changes. Prefer direct SQL in tests to inspect `deck_queue`; do not add production read APIs just for verification.

Timestamp assertions are **exact string equality against the `"O"` round-trip format** — not "by value or ISO string", and not an OR-assertion, which would pin nothing. Task 2 formats timestamps to `"O"` in C# before binding precisely so both dialects store byte-identical ISO text; the tests must depend on that and would fail if a raw `DateTime`/`DateTimeOffset` were bound instead.

**Pin which `"O"` (Round 4, F4-1) — there are two, and only one of them detects a raw bind.** Compute every expected timestamp string as `value.ToUniversalTime().ToString("O", CultureInfo.InvariantCulture)` on the **`DateTimeOffset`**, which renders the `+00:00` form (`2026-07-30T21:00:00.0000000+00:00`). Do **not** compute it on a `DateTime`. This is what makes the assertion discriminating: if the repository binds a raw `DateTimeOffset` instead of routing through `ArchidektDeckMetadataParameters.From`, `DateTimeOffsetTypeHandler.SetValue` (`DapperTypeHandlers.cs:148-154`) stores `normalized.UtcDateTime.ToString("O")` — the **`DateTime`** `"O"`, which renders the `Z` form (`...T21:00:00.0000000Z`). The two strings differ, so the test goes red. Compute the expected value the other way and both a correct implementation and a raw bind produce the same string, and the test pins nothing.

For legacy migration coverage there is no existing pattern to copy, so build it explicitly. Every repository method calls `EnsureSchemaAsync` first, so the legacy table must exist **before** the repository is touched: open a raw `SqliteConnection`, `CREATE TABLE deck_queue` with the pre-Phase-5 column list (`CategoryCacheSchema.cs:48-57` minus the six new columns), insert one row, then run `EnsureSchemaAsync` **twice** through the repository, then assert the inserted row still exists, all six new columns are present, and `archidekt_metadata_captured_utc IS NULL` on that row. Migration source under test: `CategoryCacheSchema.cs:61-67`. This proves both D-04 no-backfill compatibility and migration idempotence.

For skip/failure coverage write both halves described in Test 4 and Test 4b: the two-step already-captured case is the one that can actually fail, so do not collapse them into one test. The URL upsert gets the three-way partition instead of a pair — Tests 5, 5c, and 5b — because D-10 gates that path per record rather than per column, and 5c is the only test that can tell the two gating styles apart. These are the repository-level guards for D-06, D-10, and BRKT-03; propagation tests in plan 05-03 cover callers.

Write Test 7 against `ArchidektDeckMetadataParameters.From(...)` even though Task 2 creates that type — this is the TDD red step for the gate that replaces the unrunnable Postgres check.
  </action>
  <verify>
    <automated>dotnet.exe test DeckFlow.Core.Tests/DeckFlow.Core.Tests.csproj --filter "FullyQualifiedName~CategoryKnowledgeRepositoryTests|FullyQualifiedName~CategoryCacheSchemaParityTests|FullyQualifiedName~ArchidektDeckMetadataParametersTests" --no-restore</automated>
  </verify>
  <done>Schema and repository tests fail red against the current code and fully specify nullable metadata persistence, captured/absent semantics, the two-step anti-wipe guarantee for both write paths, and the dialect-neutral parameter-type contract.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add nullable metadata columns and repository write semantics</name>
  <read_first>
    - `DeckFlow.Core/Knowledge/CategoryCacheSchema.cs` — initial DDL and additive migration blocks.
    - `DeckFlow.Core/Knowledge/DeckQueueRepository.cs` — processed-row update and URL upsert SQL.
    - `DeckFlow.Core/Knowledge/CategoryKnowledgeRepository.cs` — facade forwarding pattern.
    - `DeckFlow.Core/Integration/DeckImporterInterfaces.cs` — `ArchidektDeckMetadata` contract from plan 05-01.
    - `DeckFlow.Web/Services/Persistence/CategoryKnowledgeStore.cs:110` — the one positional forward outside `DeckFlow.Core`; see the caller audit in this plan's interfaces section.
  </read_first>
  <files>DeckFlow.Core/Knowledge/CategoryCacheSchema.cs, DeckFlow.Core/Knowledge/DeckQueueRepository.cs, DeckFlow.Core/Knowledge/CategoryKnowledgeRepository.cs, DeckFlow.Core/Knowledge/ArchidektDeckMetadataParameters.cs, DeckFlow.Web/Services/Persistence/CategoryKnowledgeStore.cs</files>
  <action>
Add nullable curated columns to the `deck_queue` create-table DDL and additive migration path: `archidekt_edh_bracket INTEGER NULL`, `archidekt_deck_format INTEGER NULL`, `archidekt_theorycrafted INTEGER NULL`, `archidekt_created_utc TEXT NULL`, `archidekt_updated_utc TEXT NULL`, and `archidekt_metadata_captured_utc TEXT NULL`. Keep the migration idempotent by checking `GetTableColumnsAsync` once and adding each missing column separately.

**Step A — extract the mapping first, as its own type.** Create `DeckFlow.Core/Knowledge/ArchidektDeckMetadataParameters.cs` holding a pure, dependency-free `ArchidektDeckMetadataParameters.From(ArchidektDeckMetadata metadata)` that returns the six SQL parameter values. Conversions are **mandatory and explicit in C#, never in SQL and never dialect-branched**: bind `metadata.Theorycrafted is null ? (int?)null : (metadata.Theorycrafted.Value ? 1 : 0)`, and bind every timestamp as a pre-formatted `"O"` string (`metadata.CreatedUtc?.ToUniversalTime().ToString("O")` and likewise for `UpdatedUtc` and the non-null `CapturedUtc`). Every emitted value must therefore be `int?` or `string?`. Two concrete Postgres failures this prevents, both invisible under SQLite: binding a `bool?` to `archidekt_theorycrafted INTEGER` makes `BoolTypeHandler.SetValue` bind a native `boolean`, and Postgres has no assignment cast boolean→integer (`42804`); and `COALESCE(timestamptz, text)` cannot be unified, because `DateTimeTypeHandler.SetValue` passes a raw `DateTime` for non-SQLite. `DeckQueueRepository` must build its metadata parameters **only** through this type — no ad-hoc inline conversion at the call site, or the Task 1 Test 7 gate stops proving anything about what the repository actually binds.

**The From-routing hop is NOT test-enforced, and one field's failure is invisible everywhere it can be run (Round 4, F4-1).** Test 7 proves `From` is correct; **nothing proves `DeckQueueRepository` calls it.** That gap is not uniform across the six values — it was traced per field against `DapperTypeHandlers.cs`:

- **Timestamps — detectable.** A raw `DateTimeOffset` bind renders the `Z` form while `From` renders `+00:00`, so Task 1's exact-string assertions catch it (see the pinning note in Task 1's action block). This holds *only* if the expected strings are computed on the `DateTimeOffset`.
- **`archidekt_theorycrafted` — UNDETECTABLE in every runnable environment.** A repository that binds `metadata.Theorycrafted` (`bool?`) directly hits `BoolTypeHandler.SetValue`, which on SQLite writes `value ? 1 : 0` (`DapperTypeHandlers.cs:111-116`) — a **byte-identical read-back** to `From`'s `int?`. Every SQLite test stays green. On Postgres that same bind is a native `boolean` into an `INTEGER` column → `42804`, and Postgres is provable in **zero** environments (`PostgresFactAttribute.cs:14-18`; CI out of scope per Task 3). So an inlined theorycrafted conversion compiles, passes locally, and 500s on the first metadata write on Render — the exact `F-51-PG-01` class this phase claims to close.

Therefore: **prefer passing the `ArchidektDeckMetadataParameters` instance itself as the Dapper parameter fragment**, so there is no second conversion site that *can* be got wrong, rather than destructuring its six values at the call site and re-binding them. If you destructure instead, the From-routing invariant rests on this instruction plus code review — not on any failing test — and that must be stated in `05-02-SUMMARY.md`.

Extend `DeckQueueRepository.MarkDeckProcessedAsync` and `MarkUrlDeckProcessedAsync` with `ArchidektDeckMetadata? metadata = null` while keeping `CancellationToken` last. Add `using DeckFlow.Core.Integration;`. When metadata is non-null, set all six columns from `ArchidektDeckMetadataParameters.From(metadata)`. When metadata is null, **leave all six Archidekt metadata columns unchanged** — on the processed-row `UPDATE` simply omit them from the `SET` list, so an already-captured row that later gets re-harvested and skipped cannot revert to "not captured". Skip/failure rows must never gain a capture timestamp per D-06; a fresh never-captured row naturally stays null because the columns were never written.

**Step B — the URL upsert gates per record, not per column (ratified as D-10).** Do **not** mirror the single-field `commander_name` idiom (`DeckQueueRepository.cs:413`) across the six metadata columns. Per-column `COALESCE(excluded.archidekt_x, deck_queue.archidekt_x)` looks like the house pattern and is wrong here: a *captured* record whose `EdhBracket` is null — the legitimate captured-absent state under D-03 — binds NULL, so `COALESCE` keeps the stale bracket and the row then asserts "Archidekt declared bracket 3 at T2" when it declared nothing. That corruption is permanent and undetectable, it makes "captured, absent" unreachable on any re-import, and it makes the two write paths disagree for the same deck, against D-05. Mirroring a one-field idiom across six fields is what breaks it; the one-field case has no such state.

Gate on the record instead. `ArchidektDeckMetadata.CapturedUtc` is non-null by contract (plan 05-01), so `excluded.archidekt_metadata_captured_utc IS NULL` holds **exactly** when the caller passed `metadata: null` — that is the per-record discriminator, available inside the SQL, needing no dialect branch:

```sql
ON CONFLICT(deck_id) DO UPDATE
SET processed = 1,
    skipped = 0,
    last_checked_utc = excluded.last_checked_utc,
    commander_name = COALESCE(excluded.commander_name, deck_queue.commander_name),
    archidekt_edh_bracket = CASE WHEN excluded.archidekt_metadata_captured_utc IS NULL
        THEN deck_queue.archidekt_edh_bracket ELSE excluded.archidekt_edh_bracket END,
    -- ... the identical CASE for archidekt_deck_format, archidekt_theorycrafted,
    --     archidekt_created_utc, archidekt_updated_utc, and
    --     archidekt_metadata_captured_utc itself
```

**Extend the INSERT column list and VALUES with all six metadata columns — the discriminator only works because the INSERT always carries them (Round 4, F4-4).** The statement's current INSERT list is `(deck_id, inserted_utc, processed, skipped, last_checked_utc, commander_name)` (`DeckQueueRepository.cs:407-408`). In both SQLite and Postgres, `excluded.<col>` for a column **absent** from the INSERT list evaluates to the proposed row's default — NULL — so without widening the list and VALUES, `excluded.archidekt_metadata_captured_utc IS NULL` reads "caller passed null" on *every* conflict and the insert branch writes no metadata either. Bind all six parameters **always**: the `From` values when `metadata` is non-null, NULL when it is null. This is the one load-bearing structural edit to the statement; every other detail of the branch is spelled out above, so it is called out explicitly here rather than left implicit.

Semantics, stated so they can be checked against Tests 5 / 5c / 5b: a non-null record writes **all six** columns verbatim, which is byte-identical to what the bulk `UPDATE` path does for the same record; a null record leaves all six untouched. `commander_name` keeps its existing per-column `COALESCE` — it is not metadata and D-10 does not touch it.

This is safe on Postgres **only because Step A binds timestamps as `"O"` strings**: both `CASE` arms are then `text`, and `text`/`text` unifies trivially where `timestamptz`/`text` would not. Do not apply this instruction without Step A.

Extend `CategoryKnowledgeRepository.MarkDeckProcessedAsync` and `MarkUrlDeckProcessedAsync` with the same optional metadata parameter and forward it. Both existing forwards inside that file (`:257`, `:300`) pass their arguments **positionally**, so update them in the same edit or the file will not compile. Do not add backfill logic, sentinel bracket values, raw JSON columns, or a metadata read API. Keep every touched file LF-terminated and do not reflow lines you are not changing.

**Fix the one production forward outside `DeckFlow.Core` in this wave, not in wave 3.** `CategoryKnowledgeStore.cs:110` currently reads:

```csharp
public Task MarkUrlDeckProcessedAsync(string deckId, string? commanderName, CancellationToken cancellationToken = default) => _repository.MarkUrlDeckProcessedAsync(deckId, commanderName, cancellationToken);
```

Once the facade gains `ArchidektDeckMetadata? metadata = null` as parameter 3, that positional `cancellationToken` binds to `ArchidektDeckMetadata?` and the file fails to compile with `CS1503` — `DeckFlow.Web` is broken by wave 2 even though every `DeckFlow.Core.Tests` command is green. Convert the forward to a **named** argument, `_repository.MarkUrlDeckProcessedAsync(deckId, commanderName, cancellationToken: cancellationToken)`, which is immune to any further parameter insertion. Change nothing else in that file — do **not** add the metadata overload here; that is plan 05-03 Task 3's job. Plan 05-03 edits this same file in a later wave, and the overlap is harmless: the waves are sequential and the two edits touch different members (05-02 fixes the existing 3-arg forward; 05-03 adds a new 4-arg overload beside it).
  </action>
  <verify>
    <automated>dotnet.exe test DeckFlow.Core.Tests/DeckFlow.Core.Tests.csproj --filter "FullyQualifiedName~CategoryKnowledgeRepositoryTests|FullyQualifiedName~CategoryCacheSchemaParityTests|FullyQualifiedName~ArchidektDeckMetadataParametersTests" --no-restore</automated>
    <automated>dotnet.exe build DeckFlow.sln --no-restore</automated>
  </verify>
  <done>Fresh and legacy schemas expose nullable metadata columns; repository writes support metadata records; a null-metadata mark leaves previously captured values untouched on both the processed `UPDATE` and the URL upsert; the URL upsert's INSERT column list and VALUES carry all six metadata columns so the `excluded.*` discriminator functions; every metadata parameter flows through `ArchidektDeckMetadataParameters.From` and is `int?`/`string?` — enforced for timestamps by Task 1's exact-string assertions, and for `archidekt_theorycrafted` by instruction and code review only, since a raw `bool?` bind is indistinguishable on SQLite and Postgres is unprovable here (Round 4, F4-1; record this in `05-02-SUMMARY.md`); SQLite tests are green; and `dotnet.exe build DeckFlow.sln --no-restore` is clean — `DeckFlow.Core.Tests` references only `DeckFlow.Core` and `DeckFlow.CLI` (`DeckFlow.Core.Tests.csproj`), so a green Core test run cannot see a `DeckFlow.Web` break caused by widening these repository signatures.</done>
</task>

<task type="auto">
  <name>Task 3: Validate dialect parity including gated Postgres coverage</name>
  <read_first>
    - `DeckFlow.Web.Tests/Integration/PostgresStorageTests.cs` — `[PostgresFact]` and environment-gated patterns.
    - `DeckFlow.Web.Tests/Integration/PostgresFactAttribute.cs:14-18` — the gate reads `DECKFLOW_POSTGRES_TESTS` from the **Windows** process environment and sets `Skip` otherwise.
    - `DeckFlow.Core.Tests/CategoryCacheSchemaParityTests.cs` — SQLite parity assertions.
    - `.planning/workstreams/cycle21-cut-lab/phases/05-archidekt-bracket-capture/05-VALIDATION.md` — Postgres fallback note.
  </read_first>
  <files>DeckFlow.Web.Tests/Integration/PostgresStorageTests.cs, DeckFlow.Core.Tests/CategoryCacheSchemaParityTests.cs</files>
  <action>
The authoritative dialect gate for this phase is the **dialect-independent** `ArchidektDeckMetadataParametersTests` from Tasks 1-2, not the Postgres integration test. It runs everywhere, with or without Docker, and it is what actually proves the `bool`/`DateTimeOffset` binding hazards cannot reach Render. Treat it as required, not supplementary.

Keep the `[PostgresFact]` metadata test committed behind its existing gate. `.github/workflows/ci.yml` is **out of scope for this phase** — it is on the do-not-modify-without-permission list and this plan does not touch it — so CI will not set `DECKFLOW_POSTGRES_TESTS` and the Postgres path remains unproven in CI as well as locally. That is an accepted, recorded risk, not a silent one.

A WSL-side `DECKFLOW_POSTGRES_TESTS=1` does **not** reach Windows `dotnet.exe` without `WSLENV`, so the naive command exits 0 with everything skipped — indistinguishable from a real pass. The command must export the variable across the boundary, and the entry needs the **`/w` flag** (`DECKFLOW_POSTGRES_TESTS/w`) — a bare `WSLENV` entry with no flag does not translate WSL→Win32, so it fails the same silent way it was meant to fix. Not `/u`, which is the Win32→WSL direction. See `reference_wsl_dotnet_env_secret` and `reference_wslenv_w_flag_windows_dotnet`.

Do not introduce dialect-specific SQL branches beyond what `CategoryCacheSchema` / `DeckQueueRepository` already require. The same logical metadata columns must be used for SQLite and Postgres.
  </action>
  <verify>
    <automated>dotnet.exe test DeckFlow.Core.Tests/DeckFlow.Core.Tests.csproj --filter "FullyQualifiedName~CategoryKnowledgeRepositoryTests|FullyQualifiedName~CategoryCacheSchemaParityTests|FullyQualifiedName~ArchidektDeckMetadataParametersTests" --no-restore</automated>
    <automated>WSLENV="${WSLENV:+$WSLENV:}DECKFLOW_POSTGRES_TESTS/w" DECKFLOW_POSTGRES_TESTS=1 dotnet.exe test DeckFlow.Web.Tests/DeckFlow.Web.Tests.csproj --filter FullyQualifiedName~PostgresStorageTests --no-restore</automated>
    <automated>scripts/format-check-changed.sh staged</automated>
  </verify>
  <done>SQLite verification is green **and** `ArchidektDeckMetadataParametersTests` is green — that pair is the gate. For the Postgres command, only test output showing the `[PostgresFact]` metadata tests **passed** counts as Postgres verification. A skipped or zero-test run is recorded verbatim in `05-02-SUMMARY.md` as "**NOT VERIFIED on Postgres**" — never as "skipped as expected" — and is carried into the phase summary as an **open production risk**, because Render runs Postgres and this phase's write path has no executed Postgres proof.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Importer metadata -> SQL parameters | Parsed external values become persisted corpus metadata. |
| Legacy schema -> migrated schema | Existing harvested rows cross into a new schema without backfill. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04 | Tampering | `deck_queue` metadata columns | mitigate | Use parameterized Dapper commands; tests assert malformed importer values are null before persistence. |
| T-05-05 | Repudiation | `archidekt_metadata_captured_utc` | mitigate | Schema and repository tests prove null means not captured, while non-null plus null bracket means captured absent. Test 4 and Test 5b additionally prove a later null-metadata mark cannot revert a captured row to "not captured", and Test 5c proves the opposite corruption — a re-capture that no longer declares a bracket must not leave the stale one behind, falsely attributing it to the later capture timestamp (D-10). |
| T-05-13 | Tampering | Postgres parameter type coercion | mitigate (**partial — one residual hole, Round 4 F4-1**) | `ArchidektDeckMetadataParameters.From` emits only `int?`/`string?`, and Test 7 asserts both the runtime types and the values. That protects `From` itself. It does **not** prove `DeckQueueRepository` routes through `From`: timestamps are detectable via the `+00:00`-vs-`Z` rendering split, but a raw `bool?` bind for `archidekt_theorycrafted` is byte-identical to `From`'s `int?` on SQLite (`DapperTypeHandlers.cs:111-116`) and fails **only** on Postgres (`42804`), which is provable in zero environments. So the `42804` failure **can** ship undetected for that one column; the From-routing invariant is enforced by instruction plus code review, not by a failing test. **NOT VERIFIED on Postgres** — carry into `05-02-SUMMARY.md` alongside the existing Postgres risk. |
| T-05-06 | Denial of Service | schema migration startup | mitigate | Use additive nullable columns and existing idempotent column discovery; no table rewrite or backfill. |
| T-05-07 | Information Disclosure | `deck_queue` metadata | mitigate | Store curated provenance/filter fields only; raw payload and arbitrary top-level metadata remain out of scope. |
| T-05-SC | Tampering | package installs | accept | No package-manager install is planned. |
</threat_model>

<verification>
Run the focused Core schema/repository tests plus `ArchidektDeckMetadataParametersTests` after each task; that trio is the phase's real dialect gate. Run `dotnet.exe build DeckFlow.sln --no-restore` at the end of Task 2 — and only there, because Task 1 is the TDD **red** step, where a clean solution build is not a meaningful gate. Run the `WSLENV`-prefixed Postgres command too, and treat anything other than passing `[PostgresFact]` tests as "NOT VERIFIED on Postgres". Finish with `scripts/format-check-changed.sh staged` (CI `format-gate` is authoritative). Files stay LF per `.gitattributes`; do not inline `[Attribute]` onto property lines in any new type.
</verification>

<success_criteria>
- BRKT-02 is satisfied by nullable additive `deck_queue` columns with no backfill, proven on both a fresh schema and a from-scratch legacy-table migration run twice.
- BRKT-03 is satisfied by `archidekt_metadata_captured_utc` semantics **and** by the two-step proof that a null-metadata mark cannot wipe an already-captured row on either write path.
- D-10 holds on the URL upsert: preservation is gated per record, Tests 5 / 5c / 5b partition the update branch, and Test 5c proves a re-import that no longer declares a bracket clears `archidekt_edh_bracket` to NULL instead of retaining the stale value.
- `ArchidektDeckMetadataParameters.From` is the single mapping used by `DeckQueueRepository`, and its test asserts every emitted value is `int?`/`string?` and never `bool` or `DateTimeOffset`.
- Timestamps are stored as `"O"` text on both dialects and asserted by exact string equality.
- Wave 2 ends with `dotnet.exe build DeckFlow.sln --no-restore` clean, so widening the two repository signatures cannot leave `DeckFlow.Web` uncompilable until wave 3.
- D-01, D-03, D-04, D-05, and D-06 are covered at the repository boundary.
</success_criteria>

<output>
Create `.planning/workstreams/cycle21-cut-lab/phases/05-archidekt-bracket-capture/05-02-SUMMARY.md` when done.
</output>
