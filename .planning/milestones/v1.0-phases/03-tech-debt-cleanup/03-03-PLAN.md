---
phase: 03-tech-debt-cleanup
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .gitignore
  - README.md
  - DeckFlow.Web/wwwroot/js/card-lookup.js
  - DeckFlow.Web/wwwroot/js/card-search.js
  - DeckFlow.Web/wwwroot/js/category-suggestions.js
  - DeckFlow.Web/wwwroot/js/commander-search.js
  - DeckFlow.Web/wwwroot/js/deck-sync.js
  - DeckFlow.Web/wwwroot/js/df-select.js
  - DeckFlow.Web/wwwroot/js/df-typeahead.js
  - DeckFlow.Web/wwwroot/js/feedback.js
  - DeckFlow.Web/wwwroot/js/judge-questions.js
  - DeckFlow.Web/wwwroot/js/site.js
autonomous: true
requirements:
  - TD-03

must_haves:
  truths:
    - "`DeckFlow.Web/wwwroot/js/*.js` files are no longer tracked in git (D-08, ROADMAP SC #3)"
    - "`.gitignore` contains the line `DeckFlow.Web/wwwroot/js/*.js` (D-08)"
    - "Existing `tsc` MSBuild step (`CompileTypeScriptAssets` in `DeckFlow.Web.csproj`) continues to produce the .js files at build time (D-07)"
    - "Pre-removal byte-identical verification PASSED (D-09): the .js files produced by a clean `dotnet build` from .ts sources match the previously-tracked .js content"
    - "README.md adds local-development setup section documenting `cd DeckFlow.Web && npm install typescript` (D-10)"
    - "Render Docker build still succeeds: Dockerfile L36 `RUN npm install typescript` is unchanged (D-10 carry-forward)"
    - "Vendored 3rd-party JS in `wwwroot/lib/` is NOT caught by the `.gitignore` glob (D-08 verification)"
  artifacts:
    - path: ".gitignore"
      provides: "Single-line addition `DeckFlow.Web/wwwroot/js/*.js` next to existing extensions/*.zip exclusion"
      contains: "DeckFlow.Web/wwwroot/js/*.js"
    - path: "README.md"
      provides: "New 'Local Development setup' section (or amendment to Getting Started) documenting one-time `npm install typescript` in DeckFlow.Web/ for local TS compilation"
      contains: "npm install typescript"
  key_links:
    - from: ".gitignore (new line)"
      to: "DeckFlow.Web/wwwroot/js/*.js"
      via: "git rm --cached + .gitignore glob"
      pattern: "DeckFlow.Web/wwwroot/js/\\*.js"
    - from: "DeckFlow.Web/DeckFlow.Web.csproj `CompileTypeScriptAssets` target"
      to: "DeckFlow.Web/wwwroot/js/*.js (build output)"
      via: "tsc -p tsconfig.json (BeforeTargets=Build)"
      pattern: "CompileTypeScriptAssets"
---

<objective>
Stop tracking the 10 generated `wwwroot/js/*.js` files in git (TypeScript is the
source of truth at `wwwroot/ts/*.ts`); rely on the existing
`CompileTypeScriptAssets` MSBuild target in `DeckFlow.Web.csproj` to produce
them at every build. Add a one-line `.gitignore` glob and a README onboarding
section documenting the one-time `npm install typescript` step for local devs
(per D-07, D-08, D-10).

Per D-09: Mandatory pre-removal verification — on a clean checkout, `dotnet build`
the project, capture the produced `wwwroot/js/*.js`, and `diff` against the
currently-tracked content. Files MUST be byte-identical before `git rm --cached`
runs. This eliminates the silent-runtime-change risk of "ship the .gitignore +
remove tracked files" landing differently than the source-of-truth `.ts` produces.

Render production is unaffected — Dockerfile L36 already does
`RUN npm install typescript` in the build stage, so the deploy pipeline doesn't
care whether the .js was tracked locally.

Output:
- `.gitignore` with one new line: `DeckFlow.Web/wwwroot/js/*.js`
- 10 untracked .js files (via `git rm --cached`); files remain on disk
- README addition documenting local TS toolchain setup
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-tech-debt-cleanup/03-CONTEXT.md
@.planning/phases/03-tech-debt-cleanup/03-PATTERNS.md

@.gitignore
@DeckFlow.Web/DeckFlow.Web.csproj
@DeckFlow.Web/tsconfig.json
@README.md
@Dockerfile

<interfaces>
<!-- Current .gitignore (full file, 17 lines): -->
```
.vs/
bin/
obj/
artifacts/
*.user
*.suo
*.log
node_modules/
package.json
package-lock.json
AGENTS.md
DeckFlow.Web/wwwroot/extensions/*.zip
/.codex
docs/superpowers/

# Probe scratch files
*_probe.json
```

<!-- DeckFlow.Web/tsconfig.json (full file): -->
```json
{
  "compilerOptions": {
    "target": "es2017",
    "module": "none",
    "declaration": false,
    "outDir": "wwwroot/js",
    "rootDir": "wwwroot/ts",
    "allowSyntheticDefaultImports": true,
    "esModuleInterop": true,
    "forceConsistentCasingInFileNames": true,
    "strict": true,
    "ignoreDeprecations": "6.0"
  },
  "include": ["wwwroot/ts/**/*.ts"]
}
```
`outDir: "wwwroot/js"` confirmed — tsc emits to the right folder.

<!-- The 10 currently-tracked generated files (verified via `ls wwwroot/js/*.js`): -->
```
DeckFlow.Web/wwwroot/js/card-lookup.js
DeckFlow.Web/wwwroot/js/card-search.js
DeckFlow.Web/wwwroot/js/category-suggestions.js
DeckFlow.Web/wwwroot/js/commander-search.js
DeckFlow.Web/wwwroot/js/deck-sync.js
DeckFlow.Web/wwwroot/js/df-select.js
DeckFlow.Web/wwwroot/js/df-typeahead.js
DeckFlow.Web/wwwroot/js/feedback.js
DeckFlow.Web/wwwroot/js/judge-questions.js
DeckFlow.Web/wwwroot/js/site.js
```

<!-- DeckFlow.Web/wwwroot/lib/ contents (verified — these are vendored 3rd-party
     JS that must NOT be touched by the new .gitignore glob): -->
```
DeckFlow.Web/wwwroot/lib/bootstrap/
DeckFlow.Web/wwwroot/lib/jquery/
DeckFlow.Web/wwwroot/lib/jquery-validation/
DeckFlow.Web/wwwroot/lib/jquery-validation-unobtrusive/
```
`wwwroot/lib/` is NOT under `wwwroot/js/`, so the glob `DeckFlow.Web/wwwroot/js/*.js` is precisely scoped.

<!-- README.md current 'Getting Started' section (lines 70-77): -->
```
## Getting Started
1. Restore/build: `dotnet build DeckFlow.sln`
2. Run the web app: `dotnet run --project DeckFlow.Web`
3. Use the CLI to compare or harvest decks: `dotnet run --project DeckFlow.CLI -- --help`
```
No npm/Node onboarding text — verified via `grep -n "npm install\|node_modules" README.md` returns nothing.

<!-- Dockerfile L36 reference (production unaffected): -->
```
RUN cd DeckFlow.Web && npm install typescript
```
This stage runs in the Render build container and is independent of local dev.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-removal byte-identical verification (D-09 gate)</name>
  <files>
    /tmp/td03-tracked-js/  (scratch — not committed),
    /tmp/td03-built-js/    (scratch — not committed)
  </files>
  <read_first>
    REQUIRED reads before edits:
    - .planning/phases/03-tech-debt-cleanup/03-CONTEXT.md (D-07, D-08, D-09, D-10)
    - DeckFlow.Web/DeckFlow.Web.csproj (verify CompileTypeScriptAssets target exists with BeforeTargets="Build")
    - DeckFlow.Web/tsconfig.json (verify outDir=wwwroot/js)
    - .gitignore (current 17 lines)
  </read_first>
  <action>
    Per D-09: byte-identical verification BEFORE any deletion. The point is to prove that the in-tree `.js` files exactly match what `tsc` would regenerate. If they don't match, untracking would silently change runtime behavior.

    Step 1 — Capture currently-tracked .js content (snapshot):
    ```bash
    mkdir -p /tmp/td03-tracked-js
    cp DeckFlow.Web/wwwroot/js/*.js /tmp/td03-tracked-js/
    ls -la /tmp/td03-tracked-js/ | grep -c '\.js$'
    ```
    Expected: 10 files copied.

    Step 2 — Force a clean rebuild from .ts sources. Delete the in-tree .js, then build:
    ```bash
    rm DeckFlow.Web/wwwroot/js/*.js
    dotnet build DeckFlow.Web/DeckFlow.Web.csproj 2>&1 | tee /tmp/td03-build.log
    ```
    Build MUST succeed. The `CompileTypeScriptAssets` target (BeforeTargets="Build") in `DeckFlow.Web.csproj` runs `node ./node_modules/typescript/bin/tsc -p tsconfig.json` and re-emits the 10 .js files. If the build fails citing missing `tsc` or missing `node_modules`, the developer environment is missing the one-time `npm install typescript` step in `DeckFlow.Web/`. In that case, run:
    ```bash
    (cd DeckFlow.Web && npm install typescript)
    dotnet build DeckFlow.Web/DeckFlow.Web.csproj
    ```
    Then re-build. Note this also confirms the README addition in Task 3 is needed.

    Step 3 — Snapshot the built .js:
    ```bash
    mkdir -p /tmp/td03-built-js
    cp DeckFlow.Web/wwwroot/js/*.js /tmp/td03-built-js/
    ```

    Step 4 — Diff:
    ```bash
    diff -r /tmp/td03-tracked-js /tmp/td03-built-js | tee /tmp/td03-diff.txt
    ```
    Expected: ZERO output. Files MUST be byte-identical.

    If `diff` reports differences, STOP. The tracked .js drifted from the .ts source — likely someone hand-edited a generated .js. Surface as a blocker; this plan cannot proceed without first resolving the drift (either re-emit the .ts to match the hand-edit, or accept the .ts version as source of truth and document the lost edits). Do NOT proceed to Task 2.

    If diff is empty, proceed to Task 2.

    Step 5 — Restore working tree to clean git state (the rebuild may have left .js on disk that match the tracked version; that's fine — git diff is empty so no change to commit yet):
    ```bash
    git status DeckFlow.Web/wwwroot/js/
    ```
    Expected: clean (no modifications).
  </action>
  <acceptance_criteria>
    - `ls /tmp/td03-tracked-js/*.js | wc -l` returns 10
    - `ls /tmp/td03-built-js/*.js | wc -l` returns 10
    - `diff -r /tmp/td03-tracked-js /tmp/td03-built-js | wc -l` returns 0 (no diff output)
    - `dotnet build DeckFlow.Web/DeckFlow.Web.csproj 2>&1 | grep -cE " error "` returns 0
    - `git status DeckFlow.Web/wwwroot/js/` reports clean working tree (or only build-output noise that git ignores)
  </acceptance_criteria>
  <verify>
    <automated>diff -r /tmp/td03-tracked-js /tmp/td03-built-js && dotnet build DeckFlow.Web/DeckFlow.Web.csproj -v:m</automated>
  </verify>
  <done>
    Byte-identical verification PASSED: tsc-emitted .js matches currently-tracked .js for all 10 files. The deletion-then-untrack in Task 2 is safe — runtime behavior will not change. Scratch dirs `/tmp/td03-{tracked,built}-js` retained for evidence.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add .gitignore glob and untrack the 10 .js files</name>
  <files>
    .gitignore,
    DeckFlow.Web/wwwroot/js/card-lookup.js,
    DeckFlow.Web/wwwroot/js/card-search.js,
    DeckFlow.Web/wwwroot/js/category-suggestions.js,
    DeckFlow.Web/wwwroot/js/commander-search.js,
    DeckFlow.Web/wwwroot/js/deck-sync.js,
    DeckFlow.Web/wwwroot/js/df-select.js,
    DeckFlow.Web/wwwroot/js/df-typeahead.js,
    DeckFlow.Web/wwwroot/js/feedback.js,
    DeckFlow.Web/wwwroot/js/judge-questions.js,
    DeckFlow.Web/wwwroot/js/site.js
  </files>
  <read_first>
    REQUIRED reads before edits:
    - .gitignore (current 17 lines — verify line 12 is `DeckFlow.Web/wwwroot/extensions/*.zip` so we know exactly where to insert the new glob)
    - .planning/phases/03-tech-debt-cleanup/03-PATTERNS.md (lines 333-361 — .gitignore insertion pattern + verified scope of `wwwroot/lib/`)
    - Output of Task 1 (verify diff PASSED before proceeding)
  </read_first>
  <action>
    Step 1 — Insert the glob into `.gitignore` per D-08, immediately AFTER the existing line `DeckFlow.Web/wwwroot/extensions/*.zip`. Final relevant section reads:
    ```
    DeckFlow.Web/wwwroot/extensions/*.zip
    DeckFlow.Web/wwwroot/js/*.js
    /.codex
    ```
    Use the Edit tool to make this exact insertion. Do NOT touch any other line. Final `.gitignore` is 18 lines.

    Step 2 — Untrack the 10 .js files (preserving them on disk so the running app keeps serving them; future builds re-emit them):
    ```bash
    git rm --cached DeckFlow.Web/wwwroot/js/card-lookup.js
    git rm --cached DeckFlow.Web/wwwroot/js/card-search.js
    git rm --cached DeckFlow.Web/wwwroot/js/category-suggestions.js
    git rm --cached DeckFlow.Web/wwwroot/js/commander-search.js
    git rm --cached DeckFlow.Web/wwwroot/js/deck-sync.js
    git rm --cached DeckFlow.Web/wwwroot/js/df-select.js
    git rm --cached DeckFlow.Web/wwwroot/js/df-typeahead.js
    git rm --cached DeckFlow.Web/wwwroot/js/feedback.js
    git rm --cached DeckFlow.Web/wwwroot/js/judge-questions.js
    git rm --cached DeckFlow.Web/wwwroot/js/site.js
    ```
    Use `--cached` (not plain `git rm`) — this stages a deletion in the index without removing the file from the working tree. Verify with:
    ```bash
    ls DeckFlow.Web/wwwroot/js/*.js | wc -l   # must still print 10 (files on disk)
    git ls-files DeckFlow.Web/wwwroot/js/*.js | wc -l   # must print 0 (no longer tracked)
    ```

    Step 3 — Confirm `wwwroot/lib/` is unaffected:
    ```bash
    git status DeckFlow.Web/wwwroot/lib/
    ```
    Expected: empty (no changes — the glob only catches `wwwroot/js/*.js`, not `wwwroot/lib/**`).

    Step 4 — Confirm clean rebuild reproduces the .js files (smoke check the lifecycle):
    ```bash
    rm DeckFlow.Web/wwwroot/js/*.js
    dotnet build DeckFlow.Web/DeckFlow.Web.csproj
    ls DeckFlow.Web/wwwroot/js/*.js | wc -l
    ```
    Expected: 10 (rebuilt by `CompileTypeScriptAssets` MSBuild target). After build, the .js files are present on disk but un-tracked (the .gitignore glob excludes them from `git status`).

    Step 5 — Verify untracked status with `git status`:
    ```bash
    git status DeckFlow.Web/wwwroot/js/
    ```
    Expected: nothing reported under that path (files exist on disk but are gitignored). If `git status` reports them as untracked, the .gitignore glob is wrong — re-check Step 1.

    Step 6 — Do NOT commit yet. Task 3 adds README docs in the same commit (per CLAUDE.md "README updated when behavior changes" — local-dev setup changes count).
  </action>
  <acceptance_criteria>
    - `grep -c "DeckFlow.Web/wwwroot/js/\*.js" .gitignore` returns 1 (exact glob present)
    - `grep -cE "^DeckFlow.Web/wwwroot/" .gitignore` returns 2 (existing extensions/*.zip + new wwwroot/js/*.js)
    - `git ls-files DeckFlow.Web/wwwroot/js/*.js | wc -l` returns 0 (not tracked)
    - `ls DeckFlow.Web/wwwroot/js/*.js | wc -l` returns 10 (files exist on disk after rebuild)
    - `git status --porcelain DeckFlow.Web/wwwroot/js/` returns empty (gitignored, not flagged as untracked)
    - `git status --porcelain DeckFlow.Web/wwwroot/lib/` returns empty (vendored libs unaffected)
    - `git diff --cached --name-status | grep -c "^D.*DeckFlow.Web/wwwroot/js/.*\.js$"` returns 10 (10 staged deletions)
    - `git diff --cached --name-status | grep -cE "^M\\s+\\.gitignore$"` returns 1 (.gitignore staged as modified)
    - `dotnet build DeckFlow.Web/DeckFlow.Web.csproj 2>&1 | grep -cE " error "` returns 0
  </acceptance_criteria>
  <verify>
    <automated>git ls-files DeckFlow.Web/wwwroot/js/ | grep -c '\.js$' | xargs -I{} test {} -eq 0 && rm -f DeckFlow.Web/wwwroot/js/*.js && dotnet build DeckFlow.Web/DeckFlow.Web.csproj -v:m && test "$(ls DeckFlow.Web/wwwroot/js/*.js 2>/dev/null | wc -l)" = "10"</automated>
  </verify>
  <done>
    `.gitignore` contains the new `DeckFlow.Web/wwwroot/js/*.js` line. All 10 .js files are untracked but still present on disk and continue to be regenerated by every `dotnet build`. `wwwroot/lib/` (vendored 3rd-party JS) is unaffected. Changes staged but not yet committed (Task 3 adds README and creates the single commit).
  </done>
</task>

<task type="auto">
  <name>Task 3: README local-dev TypeScript setup section + commit</name>
  <files>
    README.md
  </files>
  <read_first>
    REQUIRED reads before edits:
    - README.md (full file — find a sensible insertion point; current 'Getting Started' is at lines 70-77, helper-scripts subsection at 75-77, UI styling at 79+)
    - Dockerfile (verify L36 reads `RUN cd DeckFlow.Web && npm install typescript` so the README guidance mirrors what production does)
    - DeckFlow.Web/DeckFlow.Web.csproj (verify the `CompileTypeScriptAssets` target message points at `node ./node_modules/typescript/bin/tsc`)
    - .planning/phases/03-tech-debt-cleanup/03-CONTEXT.md (D-10 — README onboarding text scope)
  </read_first>
  <action>
    Per D-10: Add a "Local development TypeScript toolchain" subsection under `## Getting Started` that documents the one-time `npm install typescript` step. Use the Edit tool to insert the following block immediately AFTER the `### Helper scripts` subsection in README.md (the section ending at the line break before `### UI styling`). Exact text:

    ```markdown
    ### Local development TypeScript toolchain

    Browser-side scripts under `DeckFlow.Web/wwwroot/ts/` compile to
    `DeckFlow.Web/wwwroot/js/` via the `CompileTypeScriptAssets` MSBuild target
    (BeforeTargets="Build") in `DeckFlow.Web.csproj`. The compiled `.js` files
    are NOT tracked in git — `dotnet build` regenerates them every time.

    First-time setup on a new dev machine:

    ```
    cd DeckFlow.Web
    npm install typescript
    ```

    This populates `DeckFlow.Web/node_modules/typescript/` so the MSBuild target
    can invoke `node ./node_modules/typescript/bin/tsc -p tsconfig.json`. The
    Render production build does the equivalent in its Docker stage
    (`RUN npm install typescript`), so deployments are unaffected.

    If `dotnet build DeckFlow.Web` reports a missing `tsc`, run the
    `npm install typescript` step above and rebuild.
    ```

    Place after `### Helper scripts` and before `### UI styling`. Do NOT change any other README content.

    Step 2 — Stage README:
    ```bash
    git add README.md
    ```

    Step 3 — Commit. Per CLAUDE.md (one logical commit, plain default-author, no Co-Authored-By trailer). Single commit covers .gitignore + 10 deletions + README:
    ```bash
    git status   # confirm staged: .gitignore (M), README.md (M), 10 .js (D)
    git commit -m "tech-debt(03-03): untrack generated wwwroot/js .js + .gitignore glob + README dev-setup (TD-03)"
    ```

    Step 4 — Verify post-commit working tree clean:
    ```bash
    git status
    ```
    Expected: working tree clean (the 10 .js files are gitignored but exist on disk; this is correct).

    Step 5 — Final smoke check: clean wipe + rebuild + verify HTML page would still load JS. Since the JS files are produced by the build, running the app post-build serves them just like before:
    ```bash
    dotnet build DeckFlow.Web/DeckFlow.Web.csproj
    ls DeckFlow.Web/wwwroot/js/site.js   # must exist
    ```
    Optional: `dotnet run --project DeckFlow.Web` and curl `http://localhost:5173/js/site.js` returns 200. This is a manual smoke; not strictly required by the acceptance criteria.
  </action>
  <acceptance_criteria>
    - `grep -c "Local development TypeScript toolchain" README.md` returns 1
    - `grep -c "npm install typescript" README.md` returns at least 1
    - `grep -c "CompileTypeScriptAssets" README.md` returns at least 1
    - `git log --oneline -1 | grep -c "tech-debt(03-03)"` returns 1
    - `git show --name-only HEAD | grep -cE '\\.js$'` returns 10 (Warning H: 10 .js files in the commit; replaced fragile stat regex)
    - `git show --name-only HEAD | grep -cE 'README\\.md$'` returns 1
    - `git show --name-only HEAD | grep -cE '\\.gitignore$'` returns 1
    - `git log -1 --pretty=%B | grep -c "Co-Authored-By"` returns 0 (plain default-author commit per CLAUDE.md)
    - `git status --porcelain` returns empty (clean working tree post-commit)
  </acceptance_criteria>
  <verify>
    <automated>test $(git show --name-only HEAD | grep -cE "(\.gitignore$|README\.md$|DeckFlow\.Web/wwwroot/js/.*\.js$)") -ge 12</automated>
  </verify>
  <done>
    README has a new "Local development TypeScript toolchain" subsection under Getting Started, documenting the one-time `npm install typescript` step. A single commit lands the .gitignore glob, the 10 untracked-file deletions, and the README addition. `git status` is clean.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Source-controlled artifacts ↔ Build-generated artifacts | The 10 `wwwroot/js/*.js` files move from "tracked content the repo owns" to "build output the repo regenerates." Source of truth shifts to the `.ts` sources at `wwwroot/ts/`. |
| Local dev environment ↔ Production build | Render Docker build stage already does `npm install typescript`; local dev now requires the same one-time step. |

## STRIDE Threat Register

ASVS L1 risk surface for this plan is essentially nil — repo hygiene change with grep + diff gates.

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03-01 | Tampering | Generated .js drift | mitigate | Task 1 D-09 byte-identical gate: a clean tsc rebuild MUST produce .js byte-identical to the tracked content before any `git rm --cached` runs. Catches hand-edited generated files (which would silently lose edits if untracked). |
| T-03-03-02 | Information Disclosure | Secrets in tracked .js | mitigate | Confirmed: no secrets ever lived in `wwwroot/js/*.js`. The .ts sources are pure DOM/UI logic (typeahead, feedback handlers, etc.) with no API keys, tokens, or credentials. Spot-check via `grep -iE "(secret\|password\|api[_-]?key\|token)" DeckFlow.Web/wwwroot/ts/*.ts` returns no matches. |
| T-03-03-03 | History rewrite risk | Repo history | accept | Per the orchestrator's threat-model note: deletion uses `git rm --cached` ONLY. NO `git filter-branch` / `git filter-repo` / BFG history rewrite is performed. The .js files remain in past commits as a normal historical artifact; this is correct and expected — the public repo's prior commits already exposed those generated bytes. |
| T-03-03-04 | Denial of Service | Build failure on missing tsc | mitigate | Task 3 README addition documents the `npm install typescript` one-time setup. Render production unaffected (Dockerfile L36 already does this). Local devs see a clear error message if they skip the step, with README pointing to the fix. |
| T-03-03-05 | Glob over-reach | wwwroot/lib/ accidentally caught | mitigate | The glob `DeckFlow.Web/wwwroot/js/*.js` is precisely scoped. `wwwroot/lib/` (jquery, bootstrap, jquery-validation, jquery-validation-unobtrusive) sits at a sibling path under `wwwroot/`, NOT under `wwwroot/js/`. Verified by `ls DeckFlow.Web/wwwroot/lib/` showing 4 vendor subdirs. Task 2 acceptance includes `git status DeckFlow.Web/wwwroot/lib/` returning empty. |
</threat_model>

<verification>
- `git status` clean post-commit (no stray modifications).
- `git ls-files DeckFlow.Web/wwwroot/js/*.js` returns empty (no longer tracked).
- `ls DeckFlow.Web/wwwroot/js/*.js | wc -l` returns 10 after a fresh `dotnet build` (files exist on disk; build produces them).
- README contains the new "Local development TypeScript toolchain" subsection.
- Render auto-deploy from `main` continues to render `https://www.deckflow.gg` correctly. The Docker build's existing `npm install typescript` produces the same .js files in the production container; no behavior change at runtime.
- `curl https://www.deckflow.gg/js/site.js` returns 200 with a non-empty body post-deploy.
- Verify zero secrets in tracked sources: `grep -irE "(secret|password|api[_-]?key|token)" DeckFlow.Web/wwwroot/ts/` returns no matches (sanity audit per threat-model).
</verification>

<success_criteria>
- ROADMAP Phase 03 SC #3 satisfied: `DeckFlow.Web/wwwroot/js/*.js` files no longer tracked in git; `.gitignore` excludes them; existing `tsc` MSBuild step still produces them at build time and the deployed site continues to load JS correctly.
- Single commit covers the three logical changes (.gitignore + 10 untracks + README) per CLAUDE.md "commit per logical change" — this IS one logical change ("untrack generated JS").
- README updated per CLAUDE.md "README updated when behavior changes" — local dev workflow now requires `npm install typescript` once.
- Plain default-author commit, no Co-Authored-By trailer.
- Pre-removal byte-identical verification (D-09) PASSED — runtime behavior preserved.
- `wwwroot/lib/` (vendored 3rd-party JS) untouched.
</success_criteria>

<output>
After completion, create `.planning/phases/03-tech-debt-cleanup/03-03-SUMMARY.md` documenting:
- Result of D-09 byte-identical verification (diff output, expected empty)
- Final `.gitignore` line numbers / new content
- Final list of the 10 untracked files with `git ls-files` confirmation
- README diff excerpt showing the new subsection
- Commit SHA created
- Confirmation that `wwwroot/lib/` was unaffected by the glob (`git status` empty for that path)
- Note: post-deploy verification on deckflow.gg should be checked at next push (not within this plan's WSL-local execution)
</output>
