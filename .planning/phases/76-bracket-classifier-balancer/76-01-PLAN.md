---
phase: 76-bracket-classifier-balancer
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - DeckFlow.Core/Bracket/GameChangerCatalog.cs
  - DeckFlow.Core/Bracket/TwoCardCombo.cs
  - DeckFlow.Core/Bracket/BracketClassification.cs
  - DeckFlow.Core/Bracket/BracketRubricThresholds.cs
  - DeckFlow.Core/Bracket/BracketClassifier.cs
  - DeckFlow.Web/Data/bracket-data.json
  - DeckFlow.Web/DeckFlow.Web.csproj
  - DeckFlow.Core.Tests/Bracket/BracketClassifierTests.cs
autonomous: true
requirements: [BRACKET-01, BRACKET-02]
must_haves:
  truths:
    - "BracketClassifier.Classify returns B4 when GC count >= 4, when a two-card combo is present, or when mass-land-denial is present"
    - "BracketClassifier.Classify returns B3 when 1-3 Game Changers are present with no combo and no MLD"
    - "Extra-turn spells never change the bracket number (informational only)"
    - "Mass extra-card-draw is informational-only by design (RESEARCH A2): no model field, no curated list, no detection — covered by Game Changers; the view shows a static line + an --extracards tag kind only"
    - "Auto-B5 at GC>=10 is a named PRODUCT HEURISTIC, not the official WotC rubric; user-facing copy never presents 10 as authoritative and the downstream artifact advises meta-level cEDH confirmation"
    - "Tutor count is never an input to bracket gating"
    - "A null combo input sets ComboDetectionAvailable=false and never asserts zero combos"
    - "Game Changers list and bracket tiers live in DeckFlow.Web/Data/bracket-data.json (versioned, effective-date stamped), not a .cs literal"
  artifacts:
    - path: "DeckFlow.Core/Bracket/BracketClassifier.cs"
      provides: "Pure static Classify entry point"
      contains: "public static class BracketClassifier"
    - path: "DeckFlow.Core/Bracket/GameChangerCatalog.cs"
      provides: "GameChangerCatalog + BracketTier records"
      contains: "public sealed record GameChangerCatalog"
    - path: "DeckFlow.Web/Data/bracket-data.json"
      provides: "Versioned Game Changers + MLD + extra-turn + 5 bracket tiers, effective-date 2026-02-09"
      contains: "effectiveDate"
    - path: "DeckFlow.Core.Tests/Bracket/BracketClassifierTests.cs"
      provides: "Gating unit tests (GC threshold, combo, MLD, extra-turn informational, null-combo disclosure)"
      contains: "BracketClassifierTests"
  key_links:
    - from: "DeckFlow.Core/Bracket/BracketClassifier.cs"
      to: "DeckFlow.Core/Bracket/GameChangerCatalog.cs"
      via: "catalog.GameChangers / catalog.MassLandDenialCards intersect deck names"
      pattern: "catalog\\.GameChangers"
---

<objective>
Build the pure-Core bracket foundation: the immutable catalog/classification record models, a versioned `bracket-data.json` seed file (Game Changers + MLD + extra-turn lists + the 5 bracket tiers, migrated out of the `.cs` literal), and the pure static `BracketClassifier` with a full RED→GREEN unit suite encoding the official Oct-2025/Feb-2026 rubric.

Purpose: This is the dependency root for the entire phase — Phase 77's Power axis also consumes `GameChangerCatalog` and `BracketClassifier`. Pure Core logic, no DI, no HTTP.
Output: 5 Core source files, 1 JSON seed, 1 csproj content item, 1 Core test file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/76-bracket-classifier-balancer/76-RESEARCH.md
@.planning/phases/76-bracket-classifier-balancer/76-PATTERNS.md

<interfaces>
<!-- Executor uses these directly; no codebase exploration needed. -->

DeckEntry (DeckFlow.Core/Models/DeckEntry.cs) — relevant fields:
  required string Name; required string Board;  // Board is "mainboard" | "commander" | "sideboard" | ...

DESIGN DECISION (corrects 76-RESEARCH §2.4 / §Example 1):
  DeckFlow.Core MUST NOT reference DeckFlow.Web. The research's `Classify(..., CommanderSpellbookResult?)`
  signature inverts the dependency (CommanderSpellbookResult lives in DeckFlow.Web). Instead define a
  Core-local record `TwoCardCombo` and have the Web orchestrator (plan 76-04) map SpellbookCombo -> TwoCardCombo.
  The Classify input is `IReadOnlyList<TwoCardCombo>? twoCardCombos` where:
    null  = combo detection UNAVAILABLE  (ComboDetectionAvailable=false)
    empty = available, zero two-card combos found

DESIGN DECISION — extra-card-draw is informational-only (RESEARCH §4.2 / decision A2):
  Mass extra-card-draw is DELIBERATELY treated as informational-only and is covered by the Game
  Changers list. There is intentionally NO `ExtraCardDraw` field on any record, NO curated extra-card-draw
  list in bracket-data.json, and NO detection logic. The view (76-05) renders a static "0 mass extra-card
  draw" line and an `--extracards` tag kind only. Do NOT invent a detection field/list for it — its
  apparent absence from the model is by design, not a coverage gap.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Core bracket record models + rubric thresholds</name>
  <read_first>
    - DeckFlow.Core/Manabase/ManabaseReport.cs (analog: sealed-record result shape, get; init; convention)
    - DeckFlow.Core/Models/DeckEntry.cs (input record being consumed)
    - .planning/phases/76-bracket-classifier-balancer/76-PATTERNS.md (Pattern Assignments: GameChangerCatalog.cs + BracketClassification.cs section)
  </read_first>
  <files>DeckFlow.Core/Bracket/GameChangerCatalog.cs, DeckFlow.Core/Bracket/TwoCardCombo.cs, DeckFlow.Core/Bracket/BracketClassification.cs, DeckFlow.Core/Bracket/BracketRubricThresholds.cs</files>
  <action>
    Create namespace `DeckFlow.Core.Bracket`. Define four files, all `public sealed record` with `{ get; init; }`-style positional records (positional records satisfy the editorconfig carve-out — never `{ get; }` only):
    - `GameChangerCatalog(DateOnly EffectiveDate, IReadOnlyList<string> GameChangers, IReadOnlyList<string> MassLandDenialCards, IReadOnlyList<string> ExtraTurnCards, IReadOnlyList<BracketTier> Tiers)` plus `BracketTier(int Number, string Name, string Label, string Summary, string TurnsExpectation, int MaxGameChangers)` (MaxGameChangers = -1 means unlimited). Both in GameChangerCatalog.cs. NOTE: there is intentionally NO extra-card-draw field — mass extra-card-draw is informational-only and covered by Game Changers per RESEARCH A2 (see DESIGN DECISION above); do not add one.
    - `TwoCardCombo(IReadOnlyList<string> CardNames, IReadOnlyList<string> Results)` in TwoCardCombo.cs.
    - `BracketClassification(int BracketNumber, IReadOnlyList<string> DetectedGameChangers, IReadOnlyList<string> DetectedMassLandDenial, IReadOnlyList<string> DetectedExtraTurnCards, IReadOnlyList<TwoCardCombo>? TwoCardCombos, bool ComboDetectionAvailable, string EffectiveDate)` in BracketClassification.cs.
    - `BracketRubricThresholds` static class in BracketRubricThresholds.cs with public const ints: `HardFloorGameChangerCount = 4`, `MinGameChangersForB3 = 1`, `CedhGameChangerCount = 10`, `ZeroSignalBracket = 2`. `HardFloorGameChangerCount`, `MinGameChangersForB3`, and `ZeroSignalBracket` encode the official WotC rubric. `CedhGameChangerCount = 10` is a PRODUCT HEURISTIC for auto-B5, NOT part of the official WotC rubric — RESEARCH itself flags B5-by-GC-count as a judgment call. Its `/// <summary>` MUST state, in words, that it is a product heuristic and that a B5/cEDH classification should be confirmed at the meta level (not asserted authoritatively). Add a `/// <summary>` doc comment per public type/member (GenerateDocumentationFile is on in Web but Core has no doc-gen requirement; still add summaries for the public surface per project convention).
    Use `IReadOnlyList<T>` on every collection surface. No `List<T>` on public members.
  </action>
  <verify>
    <automated>"/mnt/c/Program Files/dotnet/dotnet.exe" build DeckFlow.Core/DeckFlow.Core.csproj -c Debug</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -c "public sealed record" DeckFlow.Core/Bracket/GameChangerCatalog.cs` returns 2 (GameChangerCatalog + BracketTier).
    - Source assertion: `BracketClassification.cs` contains `IReadOnlyList<TwoCardCombo>? TwoCardCombos` and `bool ComboDetectionAvailable`.
    - Source assertion: `BracketRubricThresholds.cs` `CedhGameChangerCount` summary contains the word "heuristic" (proves it is documented as a product heuristic, not the official rubric).
    - Source assertion: no `ExtraCardDraw` member exists anywhere under DeckFlow.Core/Bracket/ (`grep -ci "ExtraCardDraw" DeckFlow.Core/Bracket/*.cs` returns 0) — informational-only by design.
    - Build: `DeckFlow.Core.csproj` compiles with 0 errors, 0 new warnings.
    - No `using DeckFlow.Web` anywhere under DeckFlow.Core/Bracket/.
  </acceptance_criteria>
  <done>Four record/threshold files compile in DeckFlow.Core with no Web reference; the cEDH GC-count threshold is documented as a product heuristic; no extra-card-draw field exists.</done>
</task>

<task type="auto">
  <name>Task 2: Versioned bracket-data.json seed + csproj content item</name>
  <read_first>
    - DeckFlow.Web/Models/CommanderBracketCatalog.cs (the .cs literal being migrated — copy the 5 tiers' Value/Label/Summary/TurnsExpectation VERBATIM into the JSON so plan 76-02's shim refactor stays byte-identical)
    - .planning/phases/76-bracket-classifier-balancer/76-RESEARCH.md (§1.2 the 53 Game Changers; §1.3 MLD + extra-turn lists; §2.2 schema)
    - DeckFlow.Web/DeckFlow.Web.csproj (existing `<Content Include="Help\**\*.md">` pattern to mirror)
  </read_first>
  <files>DeckFlow.Web/Data/bracket-data.json, DeckFlow.Web/DeckFlow.Web.csproj</files>
  <action>
    Create `DeckFlow.Web/Data/bracket-data.json`. The JSON keys are camelCase and bind to the Core `GameChangerCatalog` record's PascalCase properties via CASE-INSENSITIVE matching in 76-02 (System.Text.Json `JsonSerializerDefaults.Web`). CRITICAL: case-insensitive matching maps `effectiveDate`→EffectiveDate, `gameChangers`→GameChangers, `massLandDenialCards`→MassLandDenialCards, `extraTurnCards`→ExtraTurnCards, and `tiers`→Tiers — but it does NOT bridge a different name, so the bracket-tier array key MUST be `tiers` (NOT `bracketTiers`) to match the record property `Tiers`. Top-level keys: `effectiveDate` = `"2026-02-09"`; `gameChangers` = the 53-card list from 76-RESEARCH §1.2 (White/Blue/Black/Red/Green/Multicolor sections flattened to a single JSON string array, sorted ascending OrdinalIgnoreCase); `massLandDenialCards` = the §1.3 MLD list; `extraTurnCards` = the §1.3 extra-turn list; `tiers` = 5 objects each `{ "number", "name", "label", "summary", "turnsExpectation", "maxGameChangers" }`. For each tier, `label`/`summary`/`turnsExpectation` MUST be copied verbatim from the corresponding `CommanderBracketOption` literal in CommanderBracketCatalog.cs (Exhibition/Core/Upgraded/Optimized/cEDH) so the 76-02 shim produces byte-identical output for existing analysis/primer prompts; `name` = Exhibition/Core/Upgraded/Optimized/cEDH; `number` = 1..5; `maxGameChangers` = 0,0,3,-1,-1. Add to DeckFlow.Web.csproj a `<Content Include="Data\bracket-data.json"><CopyToOutputDirectory>Always</CopyToOutputDirectory></Content>` item (mirror the existing Help content item; preserve CRLF and the existing ItemGroup style). The JSON file itself uses LF per .gitattributes — touch only new lines.
  </action>
  <verify>
    <automated>"/mnt/c/Program Files/dotnet/dotnet.exe" build DeckFlow.Web/DeckFlow.Web.csproj -c Debug && test -f DeckFlow.Web/bin/Debug/net10.0/Data/bracket-data.json && echo COPIED</automated>
  </verify>
  <acceptance_criteria>
    - JSON validity: `python3 -c "import json;d=json.load(open('DeckFlow.Web/Data/bracket-data.json'));assert d['effectiveDate']=='2026-02-09';assert len(d['gameChangers'])==53;assert len(d['tiers'])==5;print('OK')"` prints OK.
    - Source assertion: the bracket-tier array key is `tiers` (NOT `bracketTiers`) — `python3 -c "import json;d=json.load(open('DeckFlow.Web/Data/bracket-data.json'));assert 'tiers' in d and 'bracketTiers' not in d;print('KEY_OK')"` prints KEY_OK (guards the FIX-1 binding contract).
    - Each `tiers[].summary` string equals the matching `CommanderBracketOption.Summary` literal in CommanderBracketCatalog.cs character-for-character.
    - Build copies the file to output: the `COPIED` marker prints.
    - csproj edit adds exactly one `<Content Include="Data\bracket-data.json">` element.
  </acceptance_criteria>
  <done>bracket-data.json exists, is valid JSON with 53 GCs + 5 tiers (array key `tiers`) stamped 2026-02-09, copies to build output, and tier text matches the existing literal verbatim.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: BracketClassifier (pure static) + classifier unit suite</name>
  <read_first>
    - DeckFlow.Core/Manabase/ManabaseClassifier.cs (analog: pure static class shape, single public Classify entry, private helpers, InvariantCulture date formatting)
    - DeckFlow.Core.Tests/DeckStatClassifierTests.cs (analog: xUnit Theory/Fact on a static classifier)
    - .planning/phases/76-bracket-classifier-balancer/76-PATTERNS.md (BracketClassifier.cs section — exact gating logic) and 76-RESEARCH.md §1.1, §3.2, Pitfall 1/2/5
  </read_first>
  <files>DeckFlow.Core/Bracket/BracketClassifier.cs, DeckFlow.Core.Tests/Bracket/BracketClassifierTests.cs</files>
  <behavior>
    - 4+ Game Changers -> BracketNumber 4
    - exactly 3 / 2 / 1 Game Changers, no combo, no MLD -> BracketNumber 3
    - any two-card combo present (twoCardCombos non-null and Count>0) -> BracketNumber 4
    - any MLD card present -> BracketNumber 4
    - zero signals (0 GC, no combo, no MLD) -> BracketNumber 2 (B1 requires self-declaration, never auto)
    - 10+ Game Changers -> BracketNumber 5 (CedhGameChangerCount threshold — a PRODUCT HEURISTIC, not the official rubric; downstream artifact must advise meta-level cEDH confirmation)
    - extra-turn cards present but no other signal -> BracketNumber unchanged (informational only); DetectedExtraTurnCards is populated
    - twoCardCombos == null -> ComboDetectionAvailable=false AND classifier does NOT use combo absence to gate down (no claim of "zero combos")
    - DetectedGameChangers/MassLandDenial/ExtraTurnCards are deck-name ∩ catalog-list, OrdinalIgnoreCase
    - EffectiveDate string formatted "yyyy-MM-dd" InvariantCulture from catalog.EffectiveDate
  </behavior>
  <action>
    Implement `public static class BracketClassifier` with `public static BracketClassification Classify(IReadOnlyList<DeckEntry> entries, GameChangerCatalog catalog, IReadOnlyList<TwoCardCombo>? twoCardCombos)`. Build `deckNames` = entries where Board is "mainboard" or "commander", select Name, into a `HashSet<string>(StringComparer.OrdinalIgnoreCase)`. Intersect catalog.GameChangers / MassLandDenialCards / ExtraTurnCards against deckNames for the three detected lists. `comboAvailable = twoCardCombos is not null`; `combos = twoCardCombos ?? []`. Gating using `BracketRubricThresholds`: if `detectedGCs.Count >= CedhGameChangerCount` -> 5; else if `detectedMld.Count > 0 || combos.Count > 0 || detectedGCs.Count >= HardFloorGameChangerCount` -> 4; else if `detectedGCs.Count >= MinGameChangersForB3` -> 3; else `ZeroSignalBracket` (2). Add a `// Why:` comment at the cEDH branch noting `CedhGameChangerCount` auto-B5 is a product heuristic (not official WotC) and that the downstream balancer/classification artifact (76-04) instructs the AI to re-confirm B5/cEDH at the meta level. Tutors are never referenced. Extra-turn detection is recorded but never feeds gating. Return BracketClassification with `TwoCardCombos: comboAvailable ? combos : null`, `ComboDetectionAvailable: comboAvailable`, `EffectiveDate: catalog.EffectiveDate.ToString("yyyy-MM-dd", CultureInfo.InvariantCulture)`. Then write `BracketClassifierTests` in namespace `DeckFlow.Core.Tests` covering every bullet in <behavior>, with private `BuildCatalog(int gcCount, params string[] mldCards)`, `BuildEntries(...)`, and `BuildCombos(int twoCardCount)` helpers (build a GameChangerCatalog in-memory; do NOT read the JSON file here). Include the critical Pitfall-1 test `Classify_NullCombo_SetsComboDetectionAvailableFalse_AndDoesNotClaimZeroCombos`.
  </action>
  <verify>
    <automated>"/mnt/c/Program Files/dotnet/dotnet.exe" test DeckFlow.Core.Tests/DeckFlow.Core.Tests.csproj --filter "FullyQualifiedName~BracketClassifierTests"</automated>
  </verify>
  <acceptance_criteria>
    - Test command: all `BracketClassifierTests` pass (>= 7 cases: GC-threshold theory, combo gate, MLD gate, zero-signal=B2, cedh=B5, extra-turn-informational, null-combo disclosure).
    - Source assertion: `grep -c "tutor" DeckFlow.Core/Bracket/BracketClassifier.cs` (case-insensitive `grep -ci`) returns 0 — no tutor gating.
    - Source assertion: BracketClassifier.cs contains a `// Why:` comment noting the cEDH/B5 GC-count gate is a product heuristic.
    - Behavior assertion: a deck with only extra-turn cards (no GC/combo/MLD) classifies to BracketNumber 2, and DetectedExtraTurnCards.Count > 0.
  </acceptance_criteria>
  <done>BracketClassifier compiles and the full classifier unit suite is green, encoding the rubric with no tutor gate, the cEDH auto-B5 documented as a product heuristic, and correct null-combo disclosure.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| JSON seed file → catalog model | `bracket-data.json` deserialized into Core records (consumed at startup in 76-02) |
| deck entries → classifier | Card names originate from untrusted deck import (validated upstream by IDeckEntryLoader / DeckSourceHost in Web layer) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-76-01 | Tampering | bracket-data.json malformed/partial | accept | Pure data file, public card names only, no secrets; deserialization hardening handled in 76-02 (try/catch + fail-closed). Classifier consumes already-parsed records. |
| T-76-02 | Information Disclosure | Core classifier output | accept | Output is public deck classification; no PII, no secrets in this layer. |
| T-76-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan (in-solution only); nothing to verify. |
</threat_model>

<verification>
- `dotnet build DeckFlow.Core/DeckFlow.Core.csproj` clean.
- `dotnet build DeckFlow.Web/DeckFlow.Web.csproj` clean; bracket-data.json copied to output.
- `dotnet test DeckFlow.Core.Tests --filter "FullyQualifiedName~BracketClassifierTests"` green.
- Core has no reference to DeckFlow.Web.
</verification>

<success_criteria>
- BRACKET-01: classifier encodes B4 hard floors (GC>=4, two-card combo, MLD), B3 for 1-3 GCs, B2 zero-signal, B5 at GC>=10 (documented as a product heuristic, not official WotC); extra-turns informational; mass extra-card-draw informational-only with no detection field (RESEARCH A2); no tutor gate — all proven by unit tests.
- BRACKET-02: Game Changers + bracket tiers live in versioned `bracket-data.json` (effective 2026-02-09, array key `tiers`), Core models created; tier text copied verbatim to enable byte-identical migration in 76-02.
</success_criteria>

<output>
Create `.planning/phases/76-bracket-classifier-balancer/76-01-SUMMARY.md` when done.
</output>
</content>
</invoke>
