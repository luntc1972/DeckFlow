---
phase: 01-visual-system-tokens
plan: 03
type: execute
wave: 3
depends_on:
  - 02
files_modified:
  - DeckFlow.Web/wwwroot/css/site-abzan.css
  - DeckFlow.Web/wwwroot/css/site-bant.css
  - DeckFlow.Web/wwwroot/css/site-esper.css
  - DeckFlow.Web/wwwroot/css/site-grixis.css
  - DeckFlow.Web/wwwroot/css/site-jeskai.css
  - DeckFlow.Web/wwwroot/css/site-jund.css
  - DeckFlow.Web/wwwroot/css/site-mardu.css
  - DeckFlow.Web/wwwroot/css/site-naya.css
  - DeckFlow.Web/wwwroot/css/site-nyx.css
  - DeckFlow.Web/wwwroot/css/site-planeswalker-dark.css
  - DeckFlow.Web/wwwroot/css/site-sultai.css
  - DeckFlow.Web/wwwroot/css/site-rakdos.css
  - DeckFlow.Web/wwwroot/css/site-selesnya.css
  - DeckFlow.Web/wwwroot/css/site-dimir.css
autonomous: false
requirements:
  - UI-VS-04
must_haves:
  truths:
    - "All 25 :root-declaring CSS files (site.css + 11 non-importer forks + 11 importer forks + site-common.css + site-commander-table.css) reach the new tokens — either via @import inheritance or via explicit declaration"
    - "Spot-checking three contrasting themes (Rakdos red, Selesnya green-white, Dimir blue-black) shows tokens resolve cleanly with no fallback bleed in browser DevTools"
    - "On Rakdos theme, --link is overridden to a peach/light color so a body link is visually distinct from a --danger error message"
    - "Classic theme (and any importer theme that doesn't override) still renders identical to pre-migration"
    - "User confirms visual smoke check on /, /feedback, /help, /about, /sync passes on classic + 3 spot themes"
  artifacts:
    - path: "DeckFlow.Web/wwwroot/css/site-abzan.css"
      provides: "Type-scale tokens explicitly declared (non-importer fork)"
      contains: "--fs-base"
    - path: "DeckFlow.Web/wwwroot/css/site-rakdos.css"
      provides: "--link override for error-vs-link disambiguation (importer; only override needed)"
      contains: "--link:"
    - path: "DeckFlow.Web/wwwroot/css/site-selesnya.css"
      provides: "Importer — token inheritance verified, optional --link override if classic blue clashes with green palette"
    - path: "DeckFlow.Web/wwwroot/css/site-dimir.css"
      provides: "Importer — token inheritance verified"
  key_links:
    - from: "site.css :root"
      to: "all 11 importer themes (rakdos, selesnya, dimir, azorius, boros, golgari, gruul, izzet, orzhov, simic, temur)"
      via: "@import url('site.css') at top of file"
      pattern: "@import url\\('site\\.css'\\)"
    - from: "site.css :root"
      to: "site-common.css and the 11 non-importer forks (abzan, bant, esper, grixis, jeskai, jund, mardu, naya, nyx, planeswalker-dark, sultai)"
      via: "explicit token declaration in each fork's :root (because they don't @import)"
      pattern: "--fs-base"
    - from: "site-rakdos.css :root override of --link"
      to: ".feedback-error vs body link disambiguation on red theme"
      via: "Rakdos --link defaults to peach (#ff9ea4 or similar light color) while --danger inherits classic #c53030"
      pattern: "manual visual check on /feedback in rakdos theme"
---

<objective>
Propagate the type-scale and semantic-color tokens defined in Plans 01 and 02 to every theme file so that all 25 `:root`-declaring stylesheets either inherit the new tokens (via `@import url('site.css')`) or declare them explicitly. Add the Rakdos `--link` override that operationalizes the UI-VS-02 success criterion ("on Rakdos red theme, the feedback error message is visually distinct from a body link").

Purpose: Without this plan, the 11 non-importer guild themes (abzan, bant, esper, grixis, jeskai, jund, mardu, naya, nyx, planeswalker-dark, sultai) ship with stale `:root` blocks missing `--fs-*`, `--link`, `--danger`, etc., so cascade falls through to the user-agent default. This plan closes that gap and validates that tokens resolve correctly on three contrasting themes.

Output: Every non-importer theme file declares the 6 type-scale tokens + 4 semantic color tokens in its `:root`. Importer themes are verified to inherit cleanly. site-common.css and site-commander-table.css are checked. Rakdos gets a `--link` override. User signs off on a visual smoke check via a manual checkpoint at the end of the plan.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@CLAUDE.md
@DeckFlow.Web/wwwroot/css/site.css
@DeckFlow.Web/wwwroot/css/site-rakdos.css
@DeckFlow.Web/wwwroot/css/site-selesnya.css
@DeckFlow.Web/wwwroot/css/site-dimir.css
@DeckFlow.Web/wwwroot/css/site-abzan.css
@.planning/phases/01-visual-system-tokens/01-01-SUMMARY.md
@.planning/phases/01-visual-system-tokens/01-02-SUMMARY.md

<interfaces>
<!-- Theme file inventory (verified by grep on the working tree): -->

IMPORTER themes (11) — start with `@import url('site.css');`, INHERIT all tokens automatically:
  site-azorius.css, site-boros.css, site-dimir.css, site-golgari.css, site-gruul.css,
  site-izzet.css, site-orzhov.css, site-rakdos.css, site-selesnya.css, site-simic.css, site-temur.css

NON-IMPORTER full forks (11) — full standalone CSS, NO @import; each must explicitly declare new tokens in :root:
  site-abzan.css, site-bant.css, site-esper.css, site-grixis.css, site-jeskai.css,
  site-jund.css, site-mardu.css, site-naya.css, site-nyx.css, site-planeswalker-dark.css, site-sultai.css

OTHER :root-declaring files:
  site.css            — already has all tokens (Plans 01 + 02)
  site-common.css     — does NOT @import site.css; cascade order means site.css loads first via _Layout link, so site-common.css inherits via document-level cascade. The :root in site-common.css declares its own dark/contrast scheme tokens; verify it does NOT override --fs-* or --link with stale values.
  site-commander-table.css — has its own :root with --bg/--panel/etc.; this is a special-purpose stylesheet for the commander table; verify no conflict but DO NOT add type-scale or color tokens (out of scope; the commander table already uses fixed sizing)

Token block to insert into each non-importer theme :root (place at the END of the existing :root block, before the closing `}`):
```css
  /* type scale (UI-VS-01) — inherited shape, themes can override individual steps */
  --fs-xs:   0.75rem;
  --fs-sm:   0.85rem;
  --fs-base: 0.95rem;
  --fs-lg:   1.05rem;
  --fs-xl:   1.5rem;
  --fs-2xl:  1.9rem;

  /* semantic color tokens (UI-VS-02) — defaults track each theme's --accent */
  --link:        var(--accent);
  --danger:      #c53030;
  --cta-border:  var(--accent);
  --focus:       var(--accent);

  /* hoisted hex tokens (UI-VS-03) */
  /* themes alias --info-default / --bg-default to their own --info / --bg; site.css :root uses literal classic defaults */
  --on-accent:       #fff;
  --accent-default:  #3a82f7;
  --bg-default:      var(--bg);
  --info-default:    var(--info);
  --success:         #2f855a;
  --error-strong:    #c53030;
  --gold-warning:    #c8a040;
  --line-cool:       #8aaac8;
  --line-cool-soft:  #b0b8c8;
  --line-warm-soft:  #a8c8b8;
```

Per-theme overrides (judgment calls; defer to executor with explicit guidance):
  - site-rakdos.css: REQUIRED override `--link: #ff9ea4;` (matches the existing `a { color: #ff9ea4; }` rule at line ~48 — promote the literal to a token so it survives token-rewires in Plan 02). Optionally `--link: var(--rakdos-neon-soft, #f0a0a7);`. Either is acceptable; the value MUST visually contrast with the red --danger #c53030 so error messages do NOT look like body links. Executor reads `site-rakdos.css` lines 47-50 to confirm the existing link color, then inserts the matching `--link:` token override.
  - site-selesnya.css: optional `--link:` override only if the classic blue-on-green looks bad. Executor visually checks; if classic blue (--accent #3a7a52 = green) reads fine on cream panel, no override needed. Default behavior: inherit, no override.
  - site-dimir.css: inherit, no override (blue palette already harmonizes with classic --accent blue).
  - Dark themes (esper, grixis, jund, sultai, nyx, planeswalker-dark, abzan): each has its own --accent suited to the palette; --link defaults to that --accent which is correct. NO override needed unless executor spots a contrast issue at smoke-check.

Verification approach for importer themes:
  Importers automatically inherit via `@import url('site.css')` because the cascade resolves the import block before the theme's own :root, and the theme's :root only redeclares the tokens it overrides. So importer themes need NO file edits except site-rakdos.css (--link override). However, this plan REQUIRES executor to grep each importer file to confirm no stale token redeclaration would shadow the new tokens; if a stale redeclaration is found (e.g. an importer redeclares --fs-base inadvertently), executor flags it and removes/updates the redeclaration.

Out of scope:
  - Replacing font-size literals INSIDE non-importer themes (Phase 2 candidate; ROADMAP criterion #1 only requires site.css + site-common.css migration)
  - Hoisting hex literals inside theme files (Phase 2 candidate; ROADMAP criterion #3 only mentions site.css + site-common.css)
  - site-common.css :root token additions (Plan 02 already migrated its consumers; site-common.css inherits :root tokens from the document-level cascade)
  - site-commander-table.css token additions (special-purpose; out of scope)
  - site-mobile.css (no :root; layout-only; out of scope)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Inject token block into 11 non-importer theme files</name>
  <files>
    DeckFlow.Web/wwwroot/css/site-abzan.css,
    DeckFlow.Web/wwwroot/css/site-bant.css,
    DeckFlow.Web/wwwroot/css/site-esper.css,
    DeckFlow.Web/wwwroot/css/site-grixis.css,
    DeckFlow.Web/wwwroot/css/site-jeskai.css,
    DeckFlow.Web/wwwroot/css/site-jund.css,
    DeckFlow.Web/wwwroot/css/site-mardu.css,
    DeckFlow.Web/wwwroot/css/site-naya.css,
    DeckFlow.Web/wwwroot/css/site-nyx.css,
    DeckFlow.Web/wwwroot/css/site-planeswalker-dark.css,
    DeckFlow.Web/wwwroot/css/site-sultai.css
  </files>
  <read_first>
    - DeckFlow.Web/wwwroot/css/site.css (confirm exact token block names + values added by Plans 01 + 02)
    - DeckFlow.Web/wwwroot/css/site-abzan.css (lines 1-40 — see existing :root structure for non-importer pattern)
    - .planning/phases/01-visual-system-tokens/01-02-SUMMARY.md (Plan 02 final token list)
    - .planning/phases/01-visual-system-tokens/01-03-PLAN.md (this plan, &lt;interfaces&gt; for the canonical token block)
  </read_first>
  <action>
    For EACH of the 11 non-importer theme files listed in `<files>`:

    1. Open the file. Locate the existing `:root { ... }` block at the top (typically lines 1-35).
    2. Find the line just before the closing `}` of that :root block.
    3. Insert the canonical token block from `<interfaces>` (the 16-line `--fs-*` + semantic color + hoist block) immediately before the closing `}`.
    4. DO NOT modify or remove any existing tokens in the theme's :root — they are intentional theme-specific overrides.
    5. DO NOT add layout rules or new selectors.
    6. DO NOT touch the body / non-:root sections of the file.

    Per-theme adjustment for the `--accent`-aliased tokens: each theme's `--accent` is already declared earlier in :root (e.g. site-abzan.css has `--accent: #86a35a;`). The `--link: var(--accent);` line in the new block resolves correctly to each theme's accent without further work — that's the inheritance design.

    Executor processes files sequentially; for each file, the executor reads the existing :root, inserts the block, saves, and verifies via grep before moving to the next. If any file's :root block is structurally different (e.g. uses a non-standard formatting, has a missing closing brace), executor stops and flags.
  </action>
  <verify>
    <automated>
      cd /mnt/c/users/chrislunt/source/personal/decksyncworkbench &amp;&amp; \
      bash -c '
        files="site-abzan.css site-bant.css site-esper.css site-grixis.css site-jeskai.css site-jund.css site-mardu.css site-naya.css site-nyx.css site-planeswalker-dark.css site-sultai.css"
        all_ok=1
        for f in $files; do
          path="DeckFlow.Web/wwwroot/css/$f"
          fs=$(grep -cE "^\s*--fs-base:" "$path")
          lk=$(grep -cE "^\s*--link:" "$path")
          dn=$(grep -cE "^\s*--danger:" "$path")
          oa=$(grep -cE "^\s*--on-accent:" "$path")
          if [ "$fs" -ne 1 ] || [ "$lk" -ne 1 ] || [ "$dn" -ne 1 ] || [ "$oa" -ne 1 ]; then
            echo "FAIL: $f (fs=$fs link=$lk danger=$dn on-accent=$oa)"
            all_ok=0
          fi
        done
        [ "$all_ok" -eq 1 ] &amp;&amp; echo "ALL 11 NON-IMPORTERS OK"
      '
      cd /mnt/c/users/chrislunt/source/personal/decksyncworkbench &amp;&amp; dotnet build DeckFlow.Web/DeckFlow.Web.csproj --no-restore
    </automated>
  </verify>
  <acceptance_criteria>
    - For each of the 11 non-importer files, all four greps return exactly 1: `--fs-base:`, `--link:`, `--danger:`, `--on-accent:`
    - `grep -lE "^\s*--fs-base:" DeckFlow.Web/wwwroot/css/site-*.css | wc -l` returns >= 12 (site.css + 11 non-importers)
    - No file has duplicate `:root` blocks: `for f in DeckFlow.Web/wwwroot/css/site-*.css; do c=$(grep -c "^:root" "$f"); [ "$c" -le 1 ] || echo "DUP: $f"; done` produces no output
    - `dotnet build DeckFlow.Web/DeckFlow.Web.csproj --no-restore` exits 0
  </acceptance_criteria>
  <done>All 11 non-importer themes carry the canonical token block in their :root; build is clean.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add --link override to site-rakdos.css and audit importer themes for stale token shadows</name>
  <files>
    DeckFlow.Web/wwwroot/css/site-rakdos.css,
    DeckFlow.Web/wwwroot/css/site-selesnya.css,
    DeckFlow.Web/wwwroot/css/site-dimir.css
  </files>
  <read_first>
    - DeckFlow.Web/wwwroot/css/site-rakdos.css (full file — 227 lines)
    - DeckFlow.Web/wwwroot/css/site-selesnya.css (full file — 115 lines)
    - DeckFlow.Web/wwwroot/css/site-dimir.css (full file — 100 lines)
    - .planning/phases/01-visual-system-tokens/01-02-SUMMARY.md
  </read_first>
  <action>
    THREE-PART task:

    PART A — site-rakdos.css `--link` override (REQUIRED, fixes the ROADMAP success criterion #2 disambiguation):
      Read site-rakdos.css around line 47-50 to confirm the existing `a { color: #ff9ea4; }` rule.
      Add to the existing :root block (before closing `}`) AFTER the line `--accent-strong: #a92434;`:
        ```css
        /* UI-VS-02: override --link so error-on-red is visually distinct from body link */
        --link: #ff9ea4;
        ```
      DO NOT remove or modify the body-rule `a { color: #ff9ea4; }` — that's the consumer; the new `--link` token mirrors its value so future link selectors that switch to `var(--link)` (e.g. inside any newly-themed page) automatically pick up Rakdos's peach.
      DO NOT add a `--danger:` override; Rakdos inherits `--danger: #c53030;` from site.css's classic-default, which IS the goal — so error red stays red while link is peach.

    PART B — site-selesnya.css audit:
      Open site-selesnya.css. Read existing :root and body rules.
      Determine: does the existing `--accent: #3a7a52;` (green) make sense as `--link` color on a cream panel? Visually probable yes.
      DECISION: leave inherited (no override). Document the call in SUMMARY. If the executor visually doubts (mocks up a cream panel + green link in a quick test page), executor MAY add `--link: #285938;` (== existing --accent-strong) for darker contrast — note the choice in SUMMARY.

    PART C — site-dimir.css audit:
      Open site-dimir.css. Read existing :root and body rules.
      Determine: does the dimir blue --accent serve as link color? Yes, conventional.
      DECISION: leave inherited. Document.

    PART D — Stale-shadow audit on ALL 11 IMPORTERS (read-only, no edits unless something is broken):
      For each importer file (azorius, boros, dimir, golgari, gruul, izzet, orzhov, rakdos, selesnya, simic, temur), grep for accidental redeclarations of the new tokens that would shadow site.css:
        ```
        grep -lE "^\s*--(fs-(xs|sm|base|lg|xl|2xl)|link|danger|cta-border|focus|on-accent|success|error-strong)" DeckFlow.Web/wwwroot/css/site-{azorius,boros,golgari,gruul,izzet,orzhov,simic,temur}.css
        ```
      Expected: zero hits (importers should NOT redeclare these tokens; they inherit from @import). EXCEPT site-rakdos.css after PART A (1 hit on `--link`) and POSSIBLY site-selesnya.css if PART B chose to override.
      If any other importer redeclares one of these tokens, executor reads the surrounding context, judges whether the redeclaration is intentional (theme override) or stale (leftover from prior refactors), and either keeps + documents or removes + documents. SUMMARY records each finding.
  </action>
  <verify>
    <automated>
      cd /mnt/c/users/chrislunt/source/personal/decksyncworkbench &amp;&amp; \
      bash -c '
        rakdos_link=$(grep -cE "^\s*--link:\s*#ff9ea4" DeckFlow.Web/wwwroot/css/site-rakdos.css)
        # Importers that should have NO redeclaration of new tokens (excluding rakdos and selesnya):
        clean=$(grep -lE "^\s*--(fs-xs|fs-sm|fs-base|fs-lg|fs-xl|fs-2xl|danger|cta-border|focus|on-accent):" DeckFlow.Web/wwwroot/css/site-azorius.css DeckFlow.Web/wwwroot/css/site-boros.css DeckFlow.Web/wwwroot/css/site-dimir.css DeckFlow.Web/wwwroot/css/site-golgari.css DeckFlow.Web/wwwroot/css/site-gruul.css DeckFlow.Web/wwwroot/css/site-izzet.css DeckFlow.Web/wwwroot/css/site-orzhov.css DeckFlow.Web/wwwroot/css/site-simic.css DeckFlow.Web/wwwroot/css/site-temur.css 2>/dev/null | wc -l)
        echo "rakdos_link=$rakdos_link clean_importers_with_shadows=$clean"
        [ "$rakdos_link" -ge 1 ] &amp;&amp; [ "$clean" -eq 0 ]
      '
      cd /mnt/c/users/chrislunt/source/personal/decksyncworkbench &amp;&amp; dotnet build DeckFlow.Web/DeckFlow.Web.csproj --no-restore
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE "^\s*--link:\s*#ff9ea4" DeckFlow.Web/wwwroot/css/site-rakdos.css` returns >= `1` (Rakdos --link override present)
    - No "clean" importer (azorius, boros, dimir, golgari, gruul, izzet, orzhov, simic, temur) redeclares any new token (`--fs-*`, `--danger`, `--cta-border`, `--focus`, `--on-accent`) — those should inherit from site.css
    - `dotnet build DeckFlow.Web/DeckFlow.Web.csproj --no-restore` exits 0
    - SUMMARY documents Selesnya and Dimir audit decisions (override or inherit; rationale)
    - SUMMARY lists any unexpected token redeclarations found in other importers
  </acceptance_criteria>
  <done>Rakdos has its --link override; importer themes audited for stale shadows; build clean.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Manual smoke check on classic + 3 spot themes</name>
  <what-built>
    Plans 01 + 02 + 03 together migrated the typography and color systems to a token layer:
      - 6-step type-scale tokens in site.css :root (--fs-xs through --fs-2xl)
      - 4 semantic color tokens (--link, --danger, --cta-border, --focus) + 10 hoist tokens (--on-accent, --success, etc.)
      - All font-size rem literals in site.css and site-common.css migrated to var(--fs-*)
      - All standalone hex literals outside :root in site.css and site-common.css hoisted into :root tokens
      - .feedback-error -> var(--danger); .admin-feedback-filter.active -> var(--link) + var(--on-accent)
      - All 11 non-importer guild themes carry the canonical token block in :root
      - site-rakdos.css overrides --link to peach (#ff9ea4) so error red is visually distinct from body link
  </what-built>
  <how-to-verify>
    Run the dev server locally (or check the deployed dev/preview URL) and verify visual parity + the Rakdos disambiguation:

    1. **Classic theme parity** (pre-migration vs post-migration; classic theme = no theme cookie set):
       - `pwsh ./scripts/run-web.ps1` (or `bash ./scripts/run-web.sh`)
       - Open http://localhost:5173 in a browser with classic theme active
       - Walk these pages, comparing to memory of pre-migration look:
         - `/` (home hub)
         - `/feedback` (feedback form)
         - `/help` (help index)
         - `/about` (about page)
         - `/sync` (DeckSync entry)
       - Each page must look IDENTICAL to pre-migration. No visible drift in font sizes, link colors, error colors, focus rings, CTA borders.
       - Watch for visual deltas at lines that were rounded in Plan 01 (canonical list, inlined here so this step does not depend on 01-01-SUMMARY existing):
           - .feedback-error font-size 0.875rem -> 0.85rem (~3% smaller)
           - body html base 1rem (where rounded) -> 0.95rem (~5% smaller)
           - any 1.4rem heading -> 1.5rem (~7% larger)
           - 0.68rem -> 0.75rem (~10% larger; site-common.css small labels)
           - 0.78rem -> 0.85rem (~9% larger)
           - 0.65rem -> 0.75rem (~15% larger; site.css single use)
           - 0.9rem -> 0.95rem (~5% larger)
       - If any text feels off-size on the smoke pages, log in SUMMARY for triage.

    2. **Rakdos disambiguation (the headline UI-VS-02 fix)**:
       - Set theme cookie to rakdos (use the theme picker in the UI)
       - Reload `/feedback`
       - Submit an empty form to trigger a `.feedback-error` validation message
       - Confirm: the error message is RED (`--danger` #c53030) and a body link (`a` tag, peach `--link` #ff9ea4) is CLEARLY a different hue. The error must NOT look like a clickable link.

    3. **Token resolution on Selesnya**:
       - Switch to selesnya theme; reload `/feedback`
       - Open browser DevTools, inspect any element with `font-size: var(--fs-base)` and any element with `color: var(--link)`
       - Computed Style panel: confirm `font-size: 0.95rem` (or 0.95rem * 15px = ~14.25px) and link color resolves to a green hue (not unset, not classic blue overriding green)
       - If any token shows as "not defined" / falls back to user-agent default, the executor's :root insert in Plan 03 Task 1 missed; flag and revisit.

    4. **Token resolution on Dimir**:
       - Switch to dimir theme; reload `/feedback`
       - Same DevTools spot-check: `font-size: var(--fs-base)` and `color: var(--link)` resolve to defined values; no fallback bleed.

    5. **Live-site parity check** (post-deploy, NOT now):
       - After this phase merges to main, Render auto-deploys. Walk `/`, `/feedback`, `/help`, `/about`, `/sync` on https://www.deckflow.gg with classic theme; confirm visual parity. This is criterion #5 of the phase goal — final sign-off.

    Approve this checkpoint only after steps 1-4 pass on local. Step 5 is the post-merge sign-off (record observation in MEMORY after deploy).
  </how-to-verify>
  <resume-signal>Type "approved" if smoke check passes, or describe specific visual regressions to fix.</resume-signal>
</task>

</tasks>

<verification>
After all 3 tasks:

1. `dotnet build DeckFlow.Web/DeckFlow.Web.csproj --no-restore` exits 0.
2. All 11 non-importer themes have the new token block (12 :root files contain `--fs-base`: site.css + 11 forks).
3. Rakdos has `--link: #ff9ea4` override.
4. No "clean" importer accidentally shadows new tokens.
5. Manual smoke check confirms classic + Rakdos + Selesnya + Dimir render correctly.
</verification>

<success_criteria>
- All 25 :root-declaring CSS files reach the new tokens (via @import inheritance OR explicit declaration)
- Rakdos --link override gives error-vs-link disambiguation (UI-VS-02 success criterion #2 lands)
- No importer accidentally shadows new tokens with stale redeclarations
- Classic theme renders identically pre/post migration (criterion #5)
- Three contrasting themes (Rakdos / Selesnya / Dimir) resolve all tokens cleanly
- ROADMAP success criterion #4 satisfied (UI-VS-04 complete)
- Phase goal achieved: single semantic-token layer drives typography + color across classic + all 25 themes
</success_criteria>

<output>
After completion, create `.planning/phases/01-visual-system-tokens/01-03-SUMMARY.md` covering:
  - List of 11 non-importer files updated with line counts
  - Rakdos --link override placement
  - Selesnya / Dimir audit decisions (override or inherit; rationale)
  - Any unexpected importer-side token shadows found and resolution
  - Smoke-check results (which pages tested, which themes, any visual regressions found)
  - Live-site parity check status (deferred to post-deploy if smoke check is local-only)
  - Phase status: all 4 ROADMAP success criteria covered (criterion #5 = post-deploy sign-off)

  - REQUIRED final step before writing SUMMARY: enumerate residual literals in non-importer themes so the deferred work is visible to the next milestone planner. Run:
    ```bash
    for f in DeckFlow.Web/wwwroot/css/site-{abzan,bant,esper,grixis,jeskai,jund,mardu,naya,nyx,planeswalker-dark,sultai}.css; do
      rem=$(grep -cE "font-size:\\s*[0-9.]+rem" "$f")
      hex=$(grep -cE "#[0-9a-fA-F]{3,6}" "$f")
      echo "$f: $rem rem-literals, $hex hex-literals"
    done >> .planning/phases/01-visual-system-tokens/01-03-SUMMARY.md
    ```
    Label that section in the SUMMARY as `## Deferred — non-importer theme literals (Phase 2 candidate)`. ROADMAP criterion #1 only requires site.css + site-common.css; the per-theme residuals are bounded scope reduction and must be logged for future planning.
</output>
</content>
</invoke>