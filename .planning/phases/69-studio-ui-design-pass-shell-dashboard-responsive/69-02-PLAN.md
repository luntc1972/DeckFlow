---
phase: 69-studio-ui-design-pass-shell-dashboard-responsive
plan: 02
type: execute
wave: 2
depends_on: ["69-01"]
files_modified:
  - DeckFlow.Studio/Shared/MainLayout.razor
  - DeckFlow.Studio/Shared/MainLayout.razor.css
  - DeckFlow.Studio/Shared/NavMenu.razor
  - DeckFlow.Studio/Shared/NavMenu.razor.css
autonomous: true
requirements: [STUI-01]
must_haves:
  truths:
    - "The stock blue→purple gradient sidebar is replaced by a branded panel-surface sidebar with an accent left-border active state"
    - "The shell shows the 'DeckFlow Studio' brand wordmark and a consistent content container"
    - "Nav links meet the 44px minimum touch target"
  artifacts:
    - path: "DeckFlow.Studio/Shared/NavMenu.razor.css"
      provides: "Branded sidebar: panel surface via tokens, accent left-border + tint active link, 44px link floor"
      contains: "var(--studio-surface)"
    - path: "DeckFlow.Studio/Shared/MainLayout.razor.css"
      provides: "Branded shell: token surfaces, .studio-content container with max-width cap + min-width:0"
      contains: "var(--studio"
  key_links:
    - from: "DeckFlow.Studio/Shared/NavMenu.razor.css"
      to: "studio-theme.css tokens"
      via: "var(--studio-surface) / var(--studio-accent)"
      pattern: "var\\(--studio-accent\\)"
    - from: "DeckFlow.Studio/Shared/MainLayout.razor.css"
      to: "studio-theme.css tokens"
      via: "var(--studio-content-max)"
      pattern: "var\\(--studio-content-max\\)"
---

<objective>
Replace the stock Blazor template chrome with a branded Studio shell: re-skin the sidebar
(`NavMenu`) from the navy→purple gradient to the admin-console panel-surface pattern with an
accent left-border active state and a 44px link floor, restyle the top row / content container
(`MainLayout`) onto the shared tokens, add the `DeckFlow Studio` brand wordmark, and apply a
consistent `.studio-content` container.

Purpose: STUI-01 — make Studio look like a real branded tool, not the stock template.
Output: restyled MainLayout + NavMenu (markup + scoped CSS).

HARD CONSTRAINT: presentation-only. Nav structure (Pipeline / Support sections, link targets,
toggle behavior) is UNCHANGED — restyle only. No route, no behavior, no @code logic change.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/69-studio-ui-design-pass-shell-dashboard-responsive/69-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-skin NavMenu sidebar onto branded tokens</name>
  <files>DeckFlow.Studio/Shared/NavMenu.razor, DeckFlow.Studio/Shared/NavMenu.razor.css</files>
  <read_first>
    - DeckFlow.Studio/Shared/NavMenu.razor (current markup: Pipeline + Support sections, navbar-brand "DeckFlow.Studio", toggle)
    - DeckFlow.Studio/Shared/NavMenu.razor.css (stock link color #d7d7d7, active rgba white, 3rem link height)
    - DeckFlow.Studio/Shared/MainLayout.razor.css (the `.sidebar` gradient at line 12 — gradient REMOVAL coordinates with this task; the gradient lives in MainLayout.razor.css, recolor it there in Task 2 and ensure NavMenu surface tokens win)
    - DeckFlow.Web/wwwroot/css/admin-common.css (.admin-sidebar__link--active border-left:4px solid var(--accent) line ~136; panel surface pattern)
    - .planning/phases/69-studio-ui-design-pass-shell-dashboard-responsive/69-UI-SPEC.md (Shell Contract table: sidebar target = secondary surface, brand wordmark at top, accent left-border + tint active, 44px-min link height)
  </read_first>
  <action>
    In `NavMenu.razor.css`: replace the stock link palette and active/hover rules with token-driven
    branded ones. The `.nav-item ::deep a` rule: set `color: var(--studio-text)` (was #d7d7d7),
    keep `border-radius: 4px`, and raise `min-height: var(--touch-floor)` (44px) while keeping the
    flex/align-center layout (the existing 3rem height becomes a min-height floor — links must be at
    least 44px tall). The active rule `.nav-item ::deep a.active`: use the admin pattern —
    `border-left: 4px solid var(--studio-accent)` plus a subtle tint background
    `color-mix(in srgb, var(--studio-accent) 14%, transparent)` and `color: var(--studio-text)`
    (replace the rgba-white fill). The hover rule: a lighter accent tint
    `color-mix(in srgb, var(--studio-accent) 8%, transparent)`. Set the section-header `.text-muted`
    color via `var(--studio-muted)` if a ::deep override is needed for dark legibility. Keep the
    `@media (min-width: 641px)` toggle-hide block byte-stable. Set `.navbar-toggler` background to a
    token-friendly translucent tint that reads on the new surface.

    In `NavMenu.razor` markup: change the brand text from `DeckFlow.Studio` to `DeckFlow Studio`
    (wordmark copy per the UI-SPEC Copywriting Contract) and add a class hook
    `class="navbar-brand studio-wordmark"` so the wordmark can pick up the `--fw-brand` (700) weight.
    Do NOT change any `NavLink href`, the section structure, the toggle `@onclick`, or the `@code`
    block — link targets and toggle behavior stay identical.
  </action>
  <verify>
    <automated>grep -q 'var(--studio-accent)' DeckFlow.Studio/Shared/NavMenu.razor.css && grep -q 'var(--touch-floor)' DeckFlow.Studio/Shared/NavMenu.razor.css && grep -q 'DeckFlow Studio' DeckFlow.Studio/Shared/NavMenu.razor && ! grep -q '#d7d7d7' DeckFlow.Studio/Shared/NavMenu.razor.css && grep -c 'href=' DeckFlow.Studio/Shared/NavMenu.razor</automated>
  </verify>
  <acceptance_criteria>
    - Sidebar link color, active, and hover states reference `var(--studio-*)` tokens; `#d7d7d7` and rgba-white fills removed.
    - Active link uses `border-left: 4px solid var(--studio-accent)` + accent tint (admin pattern).
    - Nav links have `min-height: var(--touch-floor)` (44px).
    - Brand wordmark text is exactly `DeckFlow Studio` with a `studio-wordmark` class hook.
    - `grep -c 'href='` on NavMenu.razor returns the SAME value AFTER as BEFORE the change (currently 10 = 9 NavLink hrefs + the `navbar-brand href=""`; expect 10->10). The check is before==after equality, NOT a literal "9" — no nav target is added or removed; the 9 NavLink targets (Home/Harvest/Creators/Review/Publish/DirectPush/PullFromProd/Skipped/Blocked) stay unchanged.
    - `@code` block (collapseNavMenu, ToggleNavMenu) byte-stable.
    - `dotnet build DeckFlow.Studio` clean.
  </acceptance_criteria>
  <done>Sidebar is branded via tokens with accent-active state, 44px floor, and the "DeckFlow Studio" wordmark; nav targets + toggle behavior unchanged; build clean.</done>
</task>

<task type="auto">
  <name>Task 2: Restyle MainLayout shell + content container</name>
  <files>DeckFlow.Studio/Shared/MainLayout.razor, DeckFlow.Studio/Shared/MainLayout.razor.css</files>
  <read_first>
    - DeckFlow.Studio/Shared/MainLayout.razor (top-row "About" link → https://www.deckflow.gg; article.content)
    - DeckFlow.Studio/Shared/MainLayout.razor.css (.sidebar gradient line 12; .top-row bg #f7f7f7 / border #d6d5d5; 250px sidebar; 3.5rem top row; existing 2rem/1.5rem gutters)
    - DeckFlow.Web/wwwroot/css/admin-common.css (.admin-content padding:24px max-width:1200px min-width:0 line ~155; .admin-topbar panel + border pattern)
    - .planning/phases/69-studio-ui-design-pass-shell-dashboard-responsive/69-UI-SPEC.md (Shell Contract: top row stays slim, About link UNCHANGED target, .studio-content container max-width cap 1200px + min-width:0 + 24px padding; sidebar 250px / 3.5rem constants KEPT)
  </read_first>
  <action>
    In `MainLayout.razor.css`: replace the `.sidebar` gradient (`linear-gradient(navy→purple)`)
    with the secondary surface `background: var(--studio-surface)` and add
    `border-right: 1px solid var(--studio-border)` (admin panel pattern). Recolor `.top-row`:
    `background: var(--studio-surface)`, `border-bottom: 1px solid var(--studio-border)` (replace
    #f7f7f7 / #d6d5d5). Keep the 250px sidebar width, the 3.5rem top-row height, the sticky
    positioning, and the `@media (max-width: 640.98px)` / `@media (min-width: 641px)` blocks
    structurally intact (only swap the two color values inside them if any hardcodes remain).

    Add a `.studio-content` rule mapping the content container to the admin pattern:
    `max-width: var(--studio-content-max)` (1200px), `min-width: 0` (so child tables can shrink
    below content size and not blow out the layout), `padding: var(--sp-4)` (24px). Keep the
    existing `padding-left: 2rem / padding-right: 1.5rem` gutter behavior in the `min-width:641px`
    block (the UI-SPEC says keep the existing gutter) — `.studio-content` adds the max-width cap and
    min-width:0 without removing the gutter.

    In `MainLayout.razor`: add `studio-content` to the content article so it becomes
    `class="content studio-content px-4"`. Keep the `About` anchor exactly as-is (it already points
    at `https://www.deckflow.gg` — a real target; the UI-SPEC says no change needed). Optionally wrap
    a page-title slot only if trivial; do NOT add `@code` or change `@Body`.
  </action>
  <verify>
    <automated>grep -q 'var(--studio-surface)' DeckFlow.Studio/Shared/MainLayout.razor.css && grep -q 'var(--studio-content-max)' DeckFlow.Studio/Shared/MainLayout.razor.css && grep -q 'min-width: 0' DeckFlow.Studio/Shared/MainLayout.razor.css && ! grep -q 'linear-gradient' DeckFlow.Studio/Shared/MainLayout.razor.css && grep -q 'studio-content' DeckFlow.Studio/Shared/MainLayout.razor && grep -q 'https://www.deckflow.gg' DeckFlow.Studio/Shared/MainLayout.razor</automated>
  </verify>
  <acceptance_criteria>
    - `.sidebar` gradient removed; sidebar + top-row use `var(--studio-surface)` and `var(--studio-border)`.
    - `.studio-content` rule present with `max-width: var(--studio-content-max)`, `min-width: 0`, and `var(--sp-4)` padding.
    - 250px sidebar width and 3.5rem top-row height constants preserved; responsive media blocks structurally intact.
    - Content article markup carries the `studio-content` class; `@Body` and the About link unchanged (About still → https://www.deckflow.gg).
    - No `@code` added to MainLayout; presentation-only.
    - `dotnet build DeckFlow.Studio` clean.
  </acceptance_criteria>
  <done>Shell is branded onto tokens with a capped, shrinkable `.studio-content` container; sidebar/top-row gradient and stock colors gone; About link and layout behavior intact; build clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → Studio (local Blazor Server) | Operator-only local tool; the shell renders no untrusted input. The About link targets a fixed first-party URL (deckflow.gg) and is unchanged. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-69-NA | (all) | MainLayout / NavMenu restyle | accept | Scoped-CSS recolor + a wordmark text change + a container class. No new input surface, no route change, no data flow, no package install. Nav targets and toggle behavior asserted unchanged. No new attack surface on this local operator tool. |
</threat_model>

<verification>
- `dotnet build DeckFlow.Studio` clean.
- Grep gates confirm tokens applied, gradient/stock colors removed, nav href count unchanged, About link intact.
- Manual (operator, folds into Plan 04 sweep): sidebar reads branded with accent-active state in light + dark; links ≥44px; no layout blowout.
</verification>

<success_criteria>
- Stock template chrome replaced by a branded, token-driven shell (STUI-01).
- Sidebar uses the admin panel + accent-left-border pattern; 44px link floor.
- `.studio-content` container caps width and allows table shrink (min-width:0).
- Nav structure, link targets, toggle, and About link behavior unchanged; build clean.
</success_criteria>

<output>
Create `.planning/phases/69-studio-ui-design-pass-shell-dashboard-responsive/69-02-SUMMARY.md` when done.
</output>
