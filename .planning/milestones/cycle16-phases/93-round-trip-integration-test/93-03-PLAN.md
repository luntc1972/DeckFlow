---
phase: 93-round-trip-integration-test
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/93-round-trip-integration-test/93-PREFLIP-CHECKLIST.md
autonomous: true
requirements: [SYNC-16]

must_haves:
  truths:
    - "93-PREFLIP-CHECKLIST.md consolidates FU-1 (stale-visible-on-update), FU-2 (indeterminate-flag strand), and FU-3 (live Studio-UI + prod-Postgres reconcile walk) from 90-FOLLOWUPS.md into ONE operator artifact, each with an explicit pre-flip DECISION prompt (D-08)"
    - "The checklist states that FU-1/FU-2 CODE stays DEFERRED — this phase documents them as pre-flip operator decisions only; no code fix ships this phase"
    - "The checklist gives concrete live pre-flip steps for flipping BOTH prod flags ON: sync.directpush-gitbody (git-body serving / seed re-export end-state) and sync.reconcile (destructive Apply gate)"
    - "The checklist states the SYNC-16 test is a LOCAL/manual Docker gate (D-07) — it auto-skips in CI and is run locally/push-and-watch before a flip; it is NOT per-PR coverage"
  artifacts:
    - path: ".planning/phases/93-round-trip-integration-test/93-PREFLIP-CHECKLIST.md"
      provides: "Operator pre-flip checklist: FU-1/FU-2/FU-3 decisions + live flip steps for sync.directpush-gitbody and sync.reconcile + D-07 gate note"
      contains: "sync.directpush-gitbody"
  key_links:
    - from: ".planning/phases/93-round-trip-integration-test/93-PREFLIP-CHECKLIST.md"
      to: ".planning/phases/90-directpush-correctness-seed-sync/90-FOLLOWUPS.md"
      via: "FU-1/FU-2/FU-3 items lifted verbatim in intent, each turned into an operator decision + action"
      pattern: "FU-1|FU-2|FU-3"
---

<objective>
Deliver the D-08 operator artifact: a single pre-flip checklist that gates flipping the two Cycle-16 flags that shipped OFF (`sync.directpush-gitbody`, `sync.reconcile`). This is DOC-ONLY — no code, no flag flip (both are operator-owned).

The checklist consolidates the three deferred follow-ups from `90-FOLLOWUPS.md` (FU-1 stale-visible-on-update, FU-2 indeterminate-flag strand, FU-3 live reconcile walk), converts each into an explicit pre-flip DECISION + ACTION for the operator, adds the concrete live-flip steps for each flag, and records that the SYNC-16 test is a local Docker gate (D-07), not CI coverage. FU-1/FU-2 CODE remains deferred by design — FU-1 hinges on an unmade accept-by-design-vs-hide-then-reconfirm decision, and the flips are operator-owned.

Purpose: keep the final phase's blast radius = tests + docs, while giving the operator a single reviewable gate before enabling the destructive/serving-path flags.
Output: `.planning/phases/93-round-trip-integration-test/93-PREFLIP-CHECKLIST.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/93-round-trip-integration-test/93-CONTEXT.md
@.planning/phases/90-directpush-correctness-seed-sync/90-FOLLOWUPS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the operator pre-flip checklist (D-08)</name>
  <files>.planning/phases/93-round-trip-integration-test/93-PREFLIP-CHECKLIST.md</files>
  <read_first>
    - .planning/phases/90-directpush-correctness-seed-sync/90-FOLLOWUPS.md (FU-1/FU-2/FU-3 — the source items and their exact "Pre-flip action (P93)" lines to encode)
    - .planning/phases/93-round-trip-integration-test/93-CONTEXT.md (D-06, D-07, D-08 — the local-gate framing + document-only scope)
    - .planning/ROADMAP.md §Phase 93 (goal + success criteria the test proves)
  </read_first>
  <behavior>
    - A reader (the operator) can, from this one file, decide and execute each pre-flip step for both flags without re-reading 90-FOLLOWUPS or the phase context.
  </behavior>
  <action>
    Create `.planning/phases/93-round-trip-integration-test/93-PREFLIP-CHECKLIST.md` — a Markdown operator checklist. Structure it as:

    1. **Header / purpose** — one paragraph: this is the pre-flip gate for `sync.directpush-gitbody` and `sync.reconcile`, both shipped OFF this cycle; flipping is operator-owned; the SYNC-16 round-trip test (`RoundTripSyncLoopTests`) is the automated proof behind this gate.

    2. **D-07 gate note** — a callout stating the SYNC-16 test is a LOCAL/manual Docker gate: it is `[PostgresFact]` and auto-skips wherever `DECKFLOW_POSTGRES_TESTS=1` + Docker are absent (including CI). Run it locally with Docker (`DECKFLOW_POSTGRES_TESTS=1 dotnet.exe test DeckFlow.Web.Tests --filter FullyQualifiedName~RoundTrip`) or push-and-watch before a flip. The CI skip is NOT coverage and must not be mistaken for it. `.github/workflows/` is intentionally untouched.

    3. **FU-1 — Updated-visible row shows stale-but-visible content during the deploy window (MED, code DEFERRED).** Summarize the mechanism (content-only upsert changes title/tags/artifact/body_sha256 while is_visible stays true; the row serves updated metadata over the old deployed body until Render redeploys, or stays visible with stale content if the deploy fails). Then an explicit **DECISION** checkbox: accept-by-design (and correct the Stage-4 UI copy to be accurate for the update-to-visible case) **vs** hide-then-reconfirm on update. State the code fix is deferred — record the decision here before flipping `sync.directpush-gitbody`.

    4. **FU-2 — ON row can strand after a Stage-4 indeterminate flag read (MED, code DEFERRED).** Summarize: if the Stage-4 flag read is indeterminate while prod is genuinely ON, the fail-closed read pushes `[skip render]` and triggers no redeploy; the resume path only re-polls (does not drop `[skip render]`), so resume alone cannot un-strand. State it is SAFE (no false publish; row is operator-visible awaiting-confirm) and RECOVERABLE (full re-push or any later normal deploy). **DECISION/ACTION** checkbox: consider letting resume/awaiting-confirm re-trigger the git redeploy stage (drop `[skip render]` on resume) — deferred code; decide before/soon after flip.

    5. **FU-3 — Live Studio-UI + prod-Postgres reconcile walk before flipping `sync.reconcile` (deferred, gate).** Summarize that the automated `ReconcileFixtureDriveTests` (and now the SYNC-16 reconcile assertion) prove the safety story on a stand-in, but NOT (a) real Render Postgres, (b) the actual Studio `/reconcile` Blazor page, (c) a real `sync.reconcile` flip in the live web DB. **ACTION** steps (checkboxes): run `/reconcile` live once → dry-run against real prod (expect file-orphans in the hundreds per the 2026-07-05 audit) → review the readable D-06 report → confirm no prod write → then a scoped dry-run→Apply with the flag ON confirming only seed-owned rows soft-hide and a known prod-owned row stays visible.

    6. **Live flip steps — `sync.directpush-gitbody` ON.** Ordered checklist: SYNC-16 test green locally/CI-watch; FU-1 decision recorded; FU-2 decision recorded; then flip the flag in the prod web flag store; post-flip smoke (a DirectPush of one row serves from /app git, seed re-export committed, no revert on next deploy).

    7. **Live flip steps — `sync.reconcile` ON.** Ordered checklist: FU-3 live walk completed and reviewed; then flip the flag; confirm the destructive Apply is now enabled and scoped (seed-owned soft-hide only, prod-owned stays visible).

    Keep it operator-actionable with `- [ ]` checkboxes for each decision/action. New file: prefer CRLF only if the repo default; this is a `.planning` doc so match the surrounding `.planning` docs' line endings (LF is fine here — do not introduce mixed endings). Doc-only; no code.
  </action>
  <verify>
    <automated>test -f .planning/phases/93-round-trip-integration-test/93-PREFLIP-CHECKLIST.md && grep -q "sync.directpush-gitbody" .planning/phases/93-round-trip-integration-test/93-PREFLIP-CHECKLIST.md && grep -q "sync.reconcile" .planning/phases/93-round-trip-integration-test/93-PREFLIP-CHECKLIST.md && grep -qi "auto-skip" .planning/phases/93-round-trip-integration-test/93-PREFLIP-CHECKLIST.md</automated>
  </verify>
  <acceptance_criteria>
    - The file exists and covers FU-1, FU-2, FU-3, each with an explicit operator decision/action; grep finds "FU-1", "FU-2", "FU-3".
    - It contains live flip steps for BOTH sync.directpush-gitbody and sync.reconcile (grep finds both flag names).
    - It states FU-1/FU-2 code is deferred and that the SYNC-16 test is a local Docker gate that auto-skips in CI (grep finds an auto-skip / local-gate statement).
    - No code file modified; no `.github/workflows/` change.
  </acceptance_criteria>
  <done>The operator has a single pre-flip checklist consolidating FU-1/FU-2/FU-3 decisions plus concrete live flip steps for both flags, with the D-07 local-gate caveat stated.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Documentation-only artifact — no code, no runtime surface, no external boundary crossed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-93-SC | Tampering | npm/pip/cargo installs | accept | No package installs, no code — a single `.planning` Markdown doc. Nothing to verify |
</threat_model>

<verification>
- `93-PREFLIP-CHECKLIST.md` exists under the phase dir and references FU-1/FU-2/FU-3, both flag names, and the D-07 auto-skip gate note.
- No code file or `.github/workflows/` change.
</verification>

<success_criteria>
- One operator pre-flip checklist consolidates FU-1/FU-2/FU-3 as explicit decisions + actions (D-08).
- FU-1/FU-2 code is documented as deferred; the checklist gives live flip steps for both sync.directpush-gitbody and sync.reconcile.
- The SYNC-16 test's D-07 local-gate/auto-skip semantics are stated so the CI skip is not mistaken for coverage.
</success_criteria>

<output>
Create `.planning/phases/93-round-trip-integration-test/93-03-SUMMARY.md` when done.
</output>
