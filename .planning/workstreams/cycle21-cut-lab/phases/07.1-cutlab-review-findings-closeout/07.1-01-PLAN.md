---
phase: 07.1-cutlab-review-findings-closeout
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - DeckFlow.Web/wwwroot/ts/cut-lab.ts
  - DeckFlow.Web/ts-tests/cut-lab-step-tabs.test.ts
  - .planning/workstreams/cycle21-cut-lab/phases/07.1-cutlab-review-findings-closeout/07.1-MUTATION-PROTOCOL.md
autonomous: true
requirements: [R2-1, BUILD-EOL]

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "ArrowRight and ArrowLeft move DOM focus onto the reserved Plan tab (step 3) without ever selecting it; Home and End move focus to the first and last tabs (step 1 and step 5) — the Plan tab is never removed from the roving traversal ring, for any of the four keys."
    - "After that keypress the Plan tab's aria-selected is still the string 'false', and cut-lab-step-panel-3 has not been revealed."
    - "After that keypress the panel that was visible before the keypress is still the visible one."
    - "Every plan in phase 07.1 has one canonical, written mutation-proof procedure to follow, including the restore verification and the MSB3027 false-kill check."
  artifacts:
    - "DeckFlow.Web/wwwroot/ts/cut-lab.ts — activateStepTab refuses to select a tab carrying aria-disabled=\"true\"; the keydown roving list no longer filters on the native disabled DOM property."
    - "DeckFlow.Web/ts-tests/cut-lab-step-tabs.test.ts — the existing focus assertion survives and gains aria-selected + panel-visibility assertions."
    - ".planning/workstreams/cycle21-cut-lab/phases/07.1-cutlab-review-findings-closeout/07.1-MUTATION-PROTOCOL.md — the six-step mutation-proof procedure every later plan cites."
  key_links:
    - "site.ts:294-310 capture-phase guard covers the CLICK path only; the keyboard path is closed inside activateStepTab, so both entry points now refuse an aria-disabled tab."
    - "_WorkflowStepTabs.cshtml:35 emits aria-controls at cut-lab-step-panel-3; panel 3 stays in the DOM so the reference never dangles."
---

<objective>
Close R2-1 (the phase's only HIGH) and, on the same single path, prove the mutation-proof
verification architecture that all six later plans depend on.

Purpose: R2-1 is the only item whose naive fix breaks an existing passing test, so it is the
right thinnest end-to-end slice. Fixing it exercises every layer this phase touches —
production TypeScript, a vitest test, the killing-mutation protocol, the clean-build/EOL gate,
and a persisted evidence artifact — before any expansion work is committed.

Output: the R2-1 fix, its hardened test, and `07.1-MUTATION-PROTOCOL.md`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/cycle21-cut-lab/PROJECT.md
@.planning/workstreams/cycle21-cut-lab/ROADMAP.md
@.planning/workstreams/cycle21-cut-lab/STATE.md
@.planning/workstreams/cycle21-cut-lab/phases/07.1-cutlab-review-findings-closeout/07.1-CONTEXT.md
@.planning/workstreams/cycle21-cut-lab/phases/07.1-cutlab-review-findings-closeout/07.1-RESEARCH.md
@.planning/workstreams/cycle21-cut-lab/todos/pending/2026-08-22-close-six-open-cut-lab-code-review-findings.md
</context>

<locked_decisions>
Recorded here so no later plan re-opens them by accident.

**D-A (R2-1 guard placement) — locked: inside `activateStepTab`, not at the keydown call site.**
`activateStepTab` has exactly three callers — `cut-lab.ts:549` (click), `:571` (keydown),
`:598` (anchor nav). A guard at the keydown call site alone leaves the anchor-nav path able to
select a reserved tab. No caller ever needs to activate a tab that is `aria-disabled="true"` at
the moment of the call: `setExportEnabled` (`:2917-2921`) flips `aria-disabled` on the Export
tab but never calls `activateStepTab`. Placing the guard in the primitive closes all three
paths with one predicate.
Reversibility: **reversible** — a three-line guard in one function, revertable by one commit.

**D-B (roving-focus membership) — locked: every step tab stays in the traversal ring.**
The filter at `:556` is deleted rather than re-pointed at `aria-disabled`. Filtering the ring on
`aria-disabled` is the naive fix the gate explicitly names as wrong — it is the recorded killing
mutation of the existing test at `cut-lab-step-tabs.test.ts:70`. Focus and selection decouple:
the ring is all tabs, the selection predicate is `aria-disabled`.
Reversibility: **reversible**.
</locked_decisions>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: End-to-end "keyboard reaches the reserved tab without opening it" — one path only</name>
  <files>DeckFlow.Web/wwwroot/ts/cut-lab.ts, DeckFlow.Web/ts-tests/cut-lab-step-tabs.test.ts</files>
  <read_first>
    - DeckFlow.Web/wwwroot/ts/cut-lab.ts (lines 490-575 — getStepTabs, activateStepTab, attachStepTabHandler and the keydown handler in their current state)
    - DeckFlow.Web/ts-tests/cut-lab-step-tabs.test.ts (all 89 lines — the fixture at :17-33 and the three existing cases)
    - DeckFlow.Web/wwwroot/ts/site.ts lines 288-312 (the capture-phase click guard — read it to confirm it cannot see a direct activateStepTab call, and do NOT modify it: it is shared by five tools)
    - DeckFlow.Web/Views/Shared/_WorkflowStepTabs.cshtml lines 21-41 (the aria-disabled rationale comment and the aria-controls emission — read-only, do not edit)
    - .planning/workstreams/cycle21-cut-lab/phases/07.1-cutlab-review-findings-closeout/07.1-CONTEXT.md
  </read_first>
  <behavior>
    Write these assertions into `ts-tests/cut-lab-step-tabs.test.ts` BEFORE changing `cut-lab.ts`,
    and confirm they fail against the current source for the stated reason.

    Extend the existing case at `:70` (`keeps aria-disabled tabs focusable and reachable by arrow
    navigation`) — keep its name, keep `expect(plan.disabled).toBe(false)`, keep
    `expect(document.activeElement).toBe(plan)`. Change its fixture call from `renderFixture()` to
    `renderFixture(2)` — the fixture's `selectedStep` parameter (default `1`) marks that step's
    tab `aria-selected="true"`, and the new assertion below (that Decide's selection survives the
    keypress) is only meaningful if Decide was actually selected beforehand. Add, after the
    ArrowRight dispatch:
    - `plan.getAttribute('aria-selected')` is the string `'false'`.
    - `document.getElementById('cut-lab-step-panel-3')` has the `hidden` attribute, OR — if no
      panel has been hidden yet on this entry path — panel 3 is no more visible than it was
      before the keypress. Express this as: the set of ids of `[role="tabpanel"]:not([hidden])`
      is unchanged across the keypress. Capture the before-set, dispatch, compare to the
      after-set. This is a content assertion, not a count assertion, so it survives a later
      change to how many panels are visible on load (plan 07.1-05 changes exactly that).
    - `document.getElementById('cut-lab-step-tab-2')` still carries `aria-selected="true"`
      (selection did not move off Decide).

    Add a second case, `moves focus to the first and last tabs via Home and End`: focus the
    Export tab (step 5) and dispatch `Home`, asserting focus lands on step 1 (the first tab in
    the ring); then focus step 1 and dispatch `End`, asserting focus lands on step 5 (Export, the
    last tab in the ring). These are boundary-navigation checks only — Home reaches step 1 and
    End reaches step 5, and neither key can ever land on the Plan tab (step 3) in this five-tab
    ring, so this case makes no claim about reaching or selecting Plan. After each dispatch, also
    assert that no tab carrying `aria-disabled="true"` reports `aria-selected="true"`.

    Add a third case, `skips the reserved tab across two ArrowRight presses without ever selecting
    it`: focus the Decide tab (step 2) and dispatch `ArrowRight` twice. Assert that after the
    first dispatch focus is on the Plan tab (step 3) while its `aria-selected` stays the string
    `'false'`, and that after the second dispatch focus has moved on to step 4 with Plan's
    `aria-selected` still `'false'`. This is the case that actually exercises the R2-1 guard
    across a full traversal through the reserved tab: mutation M1 (removing the `aria-disabled`
    early return from `activateStepTab`) makes the wizard select and reveal the Plan tab (step 3)
    on the first `ArrowRight`, so this case fails at that intermediate assertion instead of ever
    reaching step 4.

    Add a fourth case, `refuses to select an aria-disabled tab on a direct click`: click the Plan
    tab (step 3) directly and assert `aria-selected` stays `'false'` and the visible-panel id set
    is unchanged. (In jsdom the shared `site.ts` capture guard is not installed by this spec, so
    this case proves the guard inside `activateStepTab` specifically, not the site.ts guard.)
  </behavior>
  <action>
    Fix R2-1 in `DeckFlow.Web/wwwroot/ts/cut-lab.ts` in two edits, both scoped to this file.

    (1) In `activateStepTab` (`:494`), immediately after the existing `step` validity check at
    `:495-498` and BEFORE the `panels` lookup at `:500`, return early when the button carries
    `aria-disabled` equal to the string `true`. Read it with `button.getAttribute('aria-disabled')`
    — do not read the native `disabled` DOM property, which F-2 deliberately made permanently
    false, and do not add a native `disabled` attribute anywhere. Add a `// Why:` comment on the
    guard naming R2-1 and stating that focus and selection are separate concerns in a roving
    tablist: this function owns selection only.

    (2) In the `keydown` handler (`:551-572`), remove the roving-list filter at `:556` that
    currently narrows `getStepTabs()` by the native `disabled` DOM property, so the traversal ring
    is every step tab. Keep the local name in use downstream or rename it to `rovingTabs` and
    update `:557`, `:558`, `:562`, `:564`, `:566`, `:568` accordingly. Do NOT reintroduce any
    filter here — filtering the ring on `aria-disabled` is the exact mutation the existing test
    at `:70` is designed to kill. `nextTab.focus()` at `:570` stays unconditional;
    `activateStepTab(nextTab, false)` at `:571` stays as written and is now a no-op for a
    reserved tab because of edit (1).

    Leave `cut-lab-step-panel-3` in the DOM and leave `_WorkflowStepTabs.cshtml` untouched — the
    partial emits `aria-controls` at that id and a dangling `aria-controls` is its own a11y defect.
    Do not touch `wwwroot/ts/site.ts`.

    Preserve `cut-lab.ts`'s existing line endings exactly; detect them for this file before
    editing and change only the lines whose content actually changes.
  </action>
  <verify>
    <automated>cd DeckFlow.Web &amp;&amp; npx tsc --noEmit -p tsconfig.json &amp;&amp; npx vitest run ts-tests/cut-lab-step-tabs.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `cd DeckFlow.Web && npx vitest run ts-tests/cut-lab-step-tabs.test.ts` reports 6 passed / 0 failed.
    - `cd DeckFlow.Web && npx tsc --noEmit -p tsconfig.json` exits 0 with no output.
    - Comment-stripped region check — `sed -n '551,575p' DeckFlow.Web/wwwroot/ts/cut-lab.ts | sed 's|//.*||' | grep -c 'filter('` returns `0`. The traversal ring is unfiltered by any predicate; comments are stripped first so an explanatory `// Why:` line cannot invalidate the gate.
    - `sed -n '494,512p' DeckFlow.Web/wwwroot/ts/cut-lab.ts | sed 's|//.*||' | grep -c "getAttribute('aria-disabled')"` returns `1` — exactly one selection guard, and it is in `activateStepTab`.
    - `grep -c 'cut-lab-step-panel-3' DeckFlow.Web/Views/Deck/CutLab.cshtml` returns `1` — panel 3 still exists.
    - `git diff --stat -- DeckFlow.Web/wwwroot/ts/site.ts DeckFlow.Web/Views/Shared/_WorkflowStepTabs.cshtml` produces no output — neither shared file was touched.
    - `git status --short DeckFlow.Web/wwwroot/js/` produces no output — no compiled JS is staged or modified.
  </acceptance_criteria>
  <reversibility rating="reversible">A guard clause in one function plus deletion of one filter expression; one revert commit restores prior behaviour.</reversibility>
  <done>The reserved Plan tab receives keyboard focus from ArrowRight and ArrowLeft — including across two consecutive ArrowRight presses that traverse through it — and in every one of those cases its aria-selected stays "false" and the visible-panel id set is unchanged; Home and End move focus to the first and last tabs without ever reaching Plan; a direct click on Plan is equally refused.</done>
</task>

<task type="auto">
  <name>Task 2: Prove the R2-1 fix with its named killing mutation</name>
  <files>DeckFlow.Web/wwwroot/ts/cut-lab.ts (mutated and restored in place), DeckFlow.Web/ts-tests/cut-lab-step-tabs.test.ts (read only)</files>
  <read_first>
    - DeckFlow.Web/wwwroot/ts/cut-lab.ts (the two regions edited in Task 1)
    - DeckFlow.Web/ts-tests/cut-lab-step-tabs.test.ts (the assertions added in Task 1)
    - .planning/workstreams/cycle21-cut-lab/phases/07.1-cutlab-review-findings-closeout/07.1-RESEARCH.md (section "Test and mutation map")
  </read_first>
  <action>
    Run three mutations against the Task 1 work, ONE AT A TIME. Batched mutations over-attribute
    protection, so each mutation is applied, observed, and fully restored before the next one is
    applied. Before starting, take a byte-level fingerprint of `cut-lab.ts`
    (`git hash-object DeckFlow.Web/wwwroot/ts/cut-lab.ts`) and record it; after each restore,
    recompute it and confirm it equals the recorded value. Do not restore with
    `git checkout --` — it restores from the index and will silently discard the Task 1 fix
    along with the mutation. Restore by re-applying the inverse edit, then verify by fingerprint.

    Mutation M1 — "remove the activation guard": delete the early-return added to
    `activateStepTab` in Task 1. Expected: the extended case at `cut-lab-step-tabs.test.ts:70`
    FAILS on the aria-selected assertion and on the visible-panel-set assertion.

    Mutation M2 — "skip aria-disabled tabs during traversal": in the keydown handler, narrow the
    roving ring back to tabs whose `aria-disabled` attribute is not the string `true`. Expected:
    the focus assertion `expect(document.activeElement).toBe(plan)` FAILS. This is the mutation
    the gate names as the naive R2-1 fix, and it must be shown to be caught.

    Mutation M3 — "restore the native-disabled filter": narrow the roving ring back to tabs whose
    native `disabled` DOM property is false. Expected: every case still PASSES — this mutation is
    a no-op today because F-2 removed the native attribute, and the point of running it is to
    record that this predicate carries zero protection, which is exactly why R2-1 existed.
    M3's expected result is a PASS; record it as a documented non-kill, not as a failure of the
    protocol.

    Record for each mutation: the exact edit, the command run, the observed pass/fail counts, and
    the post-restore fingerprint. Put this evidence in the plan SUMMARY under a
    `## Mutation evidence` heading. A chat statement is not a record.
  </action>
  <verify>
    <automated>cd DeckFlow.Web &amp;&amp; npx vitest run ts-tests/cut-lab-step-tabs.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - M1 applied in isolation: `cd DeckFlow.Web && npx vitest run ts-tests/cut-lab-step-tabs.test.ts` reports at least 1 failed, and the failure message names an aria-selected or visible-panel assertion.
    - M2 applied in isolation: the same command reports at least 1 failed, and the failure names `document.activeElement`.
    - M3 applied in isolation: the same command reports 6 passed / 0 failed, and the SUMMARY records it as a deliberate non-kill with the reason.
    - After each restore, `git hash-object DeckFlow.Web/wwwroot/ts/cut-lab.ts` equals the pre-mutation value recorded at the start of the task.
    - After the final restore, `cd DeckFlow.Web && npx vitest run ts-tests/cut-lab-step-tabs.test.ts` reports 6 passed / 0 failed.
    - The plan SUMMARY contains a `## Mutation evidence` section with one row per mutation carrying edit, command, observed counts and post-restore fingerprint.
  </acceptance_criteria>
  <done>M1 and M2 each fail the suite in isolation and M3 is recorded as a documented non-kill; the source is byte-identical to its pre-mutation state and the suite is green.</done>
</task>

<task type="auto">
  <name>Task 3: Write 07.1-MUTATION-PROTOCOL.md — the procedure every later plan cites</name>
  <files>.planning/workstreams/cycle21-cut-lab/phases/07.1-cutlab-review-findings-closeout/07.1-MUTATION-PROTOCOL.md</files>
  <read_first>
    - .planning/workstreams/cycle21-cut-lab/phases/07.1-cutlab-review-findings-closeout/07.1-CONTEXT.md (locked constraint 5)
    - .planning/workstreams/cycle21-cut-lab/phases/07.1-cutlab-review-findings-closeout/07.1-RESEARCH.md (section "Test and mutation map")
    - The Task 2 evidence just produced (it is the worked example the document must contain)
  </read_first>
  <action>
    Create `.planning/workstreams/cycle21-cut-lab/phases/07.1-cutlab-review-findings-closeout/07.1-MUTATION-PROTOCOL.md`.

    It states the six-step procedure exactly once, so no later plan restates it:
    1. Run the target test and record the pass/fail counts. A test that is not green before the
       mutation proves nothing after it.
    2. Fingerprint every file about to be mutated with `git hash-object <path>`.
    3. Apply exactly ONE mutation. Never batch.
    4. Re-run the same command. Record counts and the failure message text.
    5. Restore by applying the inverse edit — never `git checkout --`, which restores from the
       index and eats unstaged fixes along with the mutation. Re-fingerprint and confirm equality.
    6. Re-run and confirm the original counts.

    It also states four traps, each with the check that catches it:
    - **Direction.** A guard that means "stay quiet" is disproved only by ADDING noise, not by
      removing it. Any such mutation needs a control run and an explicit pass count, or the
      harness lies in both directions.
    - **MSB3027 false kill.** A running `DeckFlow.Web` locks the exe; `dotnet test` then exits 1
      having run zero tests, which looks exactly like a successful kill. Before believing any C#
      kill, grep the output for `error MSB` and confirm a nonzero executed-test count.
    - **Count assertions are blindable; content assertions are not.** Where both are possible,
      assert content (an id set, a string) rather than a count.
    - **Documented non-kill.** A mutation that is expected NOT to fail the suite is recorded as
      such, with the reason, and is not silently dropped.

    Include the Task 2 M1/M2/M3 run verbatim as the worked example. Include the exact commands
    for this repo: vitest is run from `DeckFlow.Web` and never from the repo root (the root sweep
    pulls in the Playwright e2e specs and produces false failures); the C# suite is
    `dotnet test DeckFlow.Web.Tests`; Playwright is `npx --no-install playwright test` against a
    server started by `scripts/run-web-test.sh`, never a browser on the Windows host.

    Write the file with LF endings, matching the other `.planning/` markdown in this phase directory.
  </action>
  <verify>
    <automated>test -f .planning/workstreams/cycle21-cut-lab/phases/07.1-cutlab-review-findings-closeout/07.1-MUTATION-PROTOCOL.md &amp;&amp; grep -c 'git hash-object' .planning/workstreams/cycle21-cut-lab/phases/07.1-cutlab-review-findings-closeout/07.1-MUTATION-PROTOCOL.md</automated>
  </verify>
  <acceptance_criteria>
    - The file exists at the stated path.
    - `grep -c 'error MSB' <file>` returns at least `1` — the MSB3027 false-kill trap is stated.
    - `grep -c 'git hash-object' <file>` returns at least `2` — fingerprint before and after.
    - `grep -c 'DeckFlow.Web' <file>` returns at least `1` and the document states that vitest runs from `DeckFlow.Web`, not the repo root.
    - `grep -ci 'M1' <file>` returns at least `1` — the worked example from Task 2 is present.
    - `file <file>` reports no CRLF terminators.
    - BUILD-EOL: `git diff --stat` and `git diff --ignore-all-space --stat`, run against this
      plan's full working-tree diff (all files touched across Tasks 1-3), report the same file
      list and the same per-file line counts. A gap between the two means EOL churn on a touched
      file that must be re-normalized before this plan's commit.
  </acceptance_criteria>
  <done>Every later plan in phase 07.1 can cite one canonical procedure instead of restating it, and that procedure carries a worked example that actually ran.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server-rendered DOM → client script | `_WorkflowStepTabs.cshtml` emits `aria-disabled` / `aria-selected`; `cut-lab.ts` reads them as the authority for whether a step may be selected. |
| user keyboard/pointer input → `activateStepTab` | The only inputs this plan changes the handling of. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07.1-01 | Elevation of Privilege | `activateStepTab` (`cut-lab.ts:494`) | low | mitigate | A keyboard user could reach a step the pointer path refuses. The guard added in Task 1 makes `aria-disabled` authoritative on every activation path, so no entry point can open a step the server marked unreachable. This is a UI-affordance concern only: panel 3 is reserved and empty, and no server data or endpoint is gated by tab selection. |
| T-07.1-02 | Information Disclosure | `cut-lab-step-panel-3` | low | accept | The reserved panel stays in the DOM (its id is the target of `aria-controls`) and is therefore readable via devtools regardless of the `hidden` attribute. It contains no data — it is an empty reserved slot — so there is nothing to disclose. |
| T-07.1-03 | Tampering | `wwwroot/js/*.js` build output | low | accept | Compiled JS is gitignored and recompiled in the Docker build; the acceptance criteria assert nothing under `wwwroot/js/` is modified, so no stale artifact can be shipped from this plan. |
| T-07.1-SC | Tampering | npm/NuGet installs | low | accept | This plan adds no package. RESEARCH.md declares no new dependency and no `## Package Legitimacy Audit` is required because there is no package-manager install task in this phase. |
</threat_model>

<verification>
- `cd DeckFlow.Web && npx tsc --noEmit -p tsconfig.json` exits 0.
- `cd DeckFlow.Web && npx vitest run ts-tests/cut-lab-step-tabs.test.ts` reports 6 passed / 0 failed.
- `cd DeckFlow.Web && npx vitest run` reports 33 files and at least 134 tests, 0 failed (the phase baseline is 33 files / 131 tests; this plan adds 3 cases).
- `git diff --stat` and `git diff --ignore-all-space --stat` report the same file list and the same per-file line counts.
- `git status --short DeckFlow.Web/wwwroot/js/` is empty.
</verification>

<success_criteria>
1. R2-1 is closed: ArrowRight and ArrowLeft each move focus onto the reserved Plan tab — including across two consecutive ArrowRight presses that traverse through it — without ever selecting it, and the previously visible panel stays visible; Home and End move focus to the first and last tabs without ever reaching Plan.
2. The existing focus assertion at `cut-lab-step-tabs.test.ts:70` still passes — the naive fix was not taken.
3. M1 and M2 each fail the suite in isolation and are recorded with their observed output; M3 is recorded as a documented non-kill.
4. `07.1-MUTATION-PROTOCOL.md` exists and carries the six steps, the four traps and the worked example.
5. No line-ending churn and no compiled JS staged.
</success_criteria>

<artifacts_this_phase_produces>
Symbols and files this plan creates or changes — for the source-grounding pass, these are NEW and
must not be flagged as drift against the pre-phase tree:

- `.planning/workstreams/cycle21-cut-lab/phases/07.1-cutlab-review-findings-closeout/07.1-MUTATION-PROTOCOL.md` (new file)
- `DeckFlow.Web/wwwroot/ts/cut-lab.ts` — new early-return guard inside `activateStepTab`; the roving-ring local in the keydown handler may be renamed to `rovingTabs`
- `DeckFlow.Web/ts-tests/cut-lab-step-tabs.test.ts` — new test cases:
  - `moves focus to the first and last tabs via Home and End`
  - `skips the reserved tab across two ArrowRight presses without ever selecting it`
  - `refuses to select an aria-disabled tab on a direct click`
  and extended assertions (plus a fixture-selection fix to `renderFixture(2)`) inside the existing
  case `keeps aria-disabled tabs focusable and reachable by arrow navigation`
- Mutation identifiers `M1`, `M2`, `M3` (evidence labels, recorded in the SUMMARY and the protocol document)
</artifacts_this_phase_produces>

<output>
Create `.planning/workstreams/cycle21-cut-lab/phases/07.1-cutlab-review-findings-closeout/07.1-01-SUMMARY.md` when done, including the `## Mutation evidence` section.
</output>
