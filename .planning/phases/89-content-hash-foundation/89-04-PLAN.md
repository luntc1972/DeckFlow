---
phase: 89-content-hash-foundation
plan: 04
type: execute
wave: 3
depends_on: ["89-01", "89-02"]
files_modified:
  - DeckFlow.Web/Services/Content/ContentKbSeedLoader.cs
  - DeckFlow.CLI/ContentKbCommandRunners.cs
  - DeckFlow.Studio/ViewModels/PublishCoordinator.cs
  - DeckFlow.Core.Tests/Orchestration/ContentIndexExportJsonGoldenTests.cs
  - DeckFlow.Core.Tests/Orchestration/ContentIndexSeedWriteTests.cs
  - DeckFlow.Core.Tests/Orchestration/Fixtures/index-seed.golden.json
autonomous: true
requirements: [SYNC-01]
must_haves:
  truths:
    - "index-seed.json carries body_sha256 (camelCase bodySha256) on export and load, so a prod reseed reconstructs the hash and a redeploy does not drop it."
    - "The seed load mapper (ContentKbSeedLoader.BuildRow) populates ContentSiteIndexRow.BodySha256 from the seed entry."
    - "The golden seed fixture includes bodySha256 and the golden test still passes."
  artifacts:
    - path: "DeckFlow.Web/Services/Content/ContentKbSeedLoader.cs"
      provides: "ContentKbSeedEntry.BodySha256 + BuildRow mapping"
      contains: "BodySha256"
    - path: "DeckFlow.Core.Tests/Orchestration/Fixtures/index-seed.golden.json"
      provides: "bodySha256 in the golden export"
      contains: "bodySha256"
  key_links:
    - from: "seed export projection"
      to: "index-seed.json bodySha256"
      via: "ContentSiteIndexRow.BodySha256 serialized camelCase"
      pattern: "bodySha256"
---

<objective>
Round-trip `body_sha256` through the seed JSON (D-09): add the field to the seed load record + mapper (`ContentKbSeedLoader`) and to both export projections (CLI `ContentKbCommandRunners` and Studio `PublishCoordinator`), and update the golden fixture + writer tests. This makes git fully reconstruct prod's hash so a reseed/redeploy cannot silently drop `body_sha256`.

Purpose: SYNC-01 requires `body_sha256` in the seed JSON alongside the DB column, because git is the source of truth and the prod row must be reconstructable from `index-seed.json`. Depends on 89-01 (the row property) and 89-02 (store SELECTs now return the column, so export reads real values).
Output: `bodySha256` in seed export + load; golden fixture updated; export/writer tests green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/89-content-hash-foundation/89-CONTEXT.md
@.planning/phases/89-content-hash-foundation/89-PATTERNS.md
</context>

<interfaces>
From DeckFlow.Web/Services/Content/ContentKbSeedLoader.cs (per pattern map):
- `JsonSerializerOptions` (~12-16): `PropertyNamingPolicy = JsonNamingPolicy.CamelCase`, `PropertyNameCaseInsensitive = true` — so a `BodySha256` property auto-serializes/reads as `bodySha256`; no `[JsonPropertyName]` needed.
- `private static ContentSiteIndexRow BuildRow(ContentKbSeedEntry entry)` (~68-90) — maps entry -> row.
- `private sealed record ContentKbSeedEntry { ... required ... CardCategoryTags }` (~90-113).

From the export side:
- DeckFlow.CLI/ContentKbCommandRunners.cs — seed-export runner (writes rows -> index-seed.json; `--output` default `content-kb/seed/index-seed.json`, DeckFlow.CLI/Program.cs:100).
- DeckFlow.Studio/ViewModels/PublishCoordinator.cs — `SeedRelative = "content-kb/seed/index-seed.json"` (~line 26); re-exports seed on publish.
- Locate the exact projection via DeckFlow.Core.Tests/Orchestration/ContentIndexExportJsonGoldenTests.cs (asserts Fixtures/index-seed.golden.json) and ContentIndexSeedWriteTests.cs (writer).

From 89-01 / 89-02 (dependencies): `ContentSiteIndexRow.BodySha256` exists and is returned by store SELECTs.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add bodySha256 to seed load record + mapper</name>
  <read_first>
    - DeckFlow.Web/Services/Content/ContentKbSeedLoader.cs (whole file — JsonSerializerOptions, BuildRow, ContentKbSeedEntry)
    - 89-PATTERNS.md § "Seed JSON (index-seed.json) export/load path"
  </read_first>
  <behavior>
    - A seed JSON entry containing `"bodySha256": "<64hex>"` loads into a ContentSiteIndexRow whose BodySha256 equals that value.
    - A legacy seed entry WITHOUT bodySha256 loads with BodySha256 == null (nullable, no exception) — PropertyNameCaseInsensitive/camelCase already handle absence.
  </behavior>
  <action>In `ContentKbSeedLoader.cs`: add `public string? BodySha256 { get; init; }` to `ContentKbSeedEntry` (nullable — legacy seed entries predate this phase), and `BodySha256 = entry.BodySha256,` to `BuildRow`'s `new ContentSiteIndexRow { ... }` literal. No `[JsonPropertyName]` — camelCase policy maps it to `bodySha256`. Touch only added lines; preserve `{ get; init; }` (carve-out).</action>
  <verify>
    <automated>dotnet build DeckFlow.Web -c Debug</automated>
  </verify>
  <acceptance_criteria>
    - SOURCE: `ContentKbSeedEntry` has `BodySha256` and `BuildRow` maps it (grep confirms both).
    - BEHAVIOR: DeckFlow.Web builds clean; a seed entry with bodySha256 hydrates the row property; missing field -> null.
  </acceptance_criteria>
  <done>Seed load carries body_sha256 into the row.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add bodySha256 to export projections and update golden</name>
  <read_first>
    - DeckFlow.CLI/ContentKbCommandRunners.cs (the seed-export runner projection)
    - DeckFlow.Studio/ViewModels/PublishCoordinator.cs (seed re-export projection)
    - DeckFlow.Core.Tests/Orchestration/ContentIndexExportJsonGoldenTests.cs + Fixtures/index-seed.golden.json (the golden the export is asserted against)
    - DeckFlow.Core.Tests/Orchestration/ContentIndexSeedWriteTests.cs (writer coverage)
  </read_first>
  <behavior>
    - Exporting a row whose BodySha256 = "<64hex>" writes `"bodySha256": "<64hex>"` into index-seed.json.
    - Exporting a row with null BodySha256 writes `"bodySha256": null` (or omits per the writer's existing null policy — match whatever the golden convention is for other nullable fields like publishedUtc).
    - The export -> golden round-trip test passes with the fixture updated.
  </behavior>
  <action>Add `body_sha256`/`bodySha256` to the export projection in BOTH `ContentKbCommandRunners.cs` (CLI seed export) and `PublishCoordinator.cs` (Studio re-export) — wherever each builds the seed entry DTO/anonymous object from a `ContentSiteIndexRow`, add the `BodySha256 = row.BodySha256` field following the same style as the adjacent nullable fields (mirror how PublishedUtc is projected). Update `Fixtures/index-seed.golden.json` to include `bodySha256` for each entry (match the null/value convention the golden uses for other nullable optional columns). Update `ContentIndexSeedWriteTests.cs` if it asserts the field set. Keep entries otherwise byte-identical (golden tests are exact-match).</action>
  <verify>
    <automated>dotnet build DeckFlow.CLI DeckFlow.Studio -c Debug; dotnet test DeckFlow.Core.Tests --filter "FullyQualifiedName~ContentIndexExportJsonGoldenTests|FullyQualifiedName~ContentIndexSeedWriteTests"</automated>
  </verify>
  <acceptance_criteria>
    - SOURCE: `grep -rn "BodySha256\|bodySha256" DeckFlow.CLI/ContentKbCommandRunners.cs DeckFlow.Studio/ViewModels/PublishCoordinator.cs` each match the export projection.
    - SOURCE: `grep -c "bodySha256" DeckFlow.Core.Tests/Orchestration/Fixtures/index-seed.golden.json` >= 1 for every seed entry in the fixture.
    - BEHAVIOR: the golden export test and the seed-write test both pass with the field present; a round-trip (export then ContentKbSeedLoader load) preserves the hash.
  </acceptance_criteria>
  <done>bodySha256 exports to and loads from index-seed.json; golden + writer tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| index-seed.json (git-committed) → store upsert | operator-owned, git-tracked seed data; not attacker-controlled input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-89-09 | Tampering | seed body_sha256 drops on reseed | mitigate | Adding bodySha256 to both export projections + load ensures a reseed/redeploy reconstructs the hash instead of nulling it — the exact drift SYNC-01 guards. Golden test locks the field's presence. |
| T-89-10 | Info | seed JSON is public repo | accept | body_sha256 is a hash of already-public KB body content; it leaks nothing. No secret. No new dependency (T-89-SC: none). |
</threat_model>

<verification>
- DeckFlow.Web / DeckFlow.CLI / DeckFlow.Studio build clean.
- Golden export test and seed-writer test pass with bodySha256 present in the fixture.
- Export->load round-trip preserves body_sha256.
</verification>

<success_criteria>
- index-seed.json round-trips body_sha256 (camelCase bodySha256) through export and load.
- Golden fixture + writer tests updated and green.
</success_criteria>

<output>
Create `.planning/phases/89-content-hash-foundation/89-04-SUMMARY.md` when done.
</output>
