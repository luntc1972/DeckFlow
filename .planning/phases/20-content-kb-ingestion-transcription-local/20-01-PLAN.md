---
phase: 20-content-kb-ingestion-transcription-local
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - DeckFlow.Core/Content/IContentVideoStore.cs
  - DeckFlow.Core/Content/ContentVideoStore.cs
  - DeckFlow.Core/Content/SlugifySourceName.cs
  - DeckFlow.CLI/Program.cs
  - DeckFlow.CLI/CommandRunners.cs
  - DeckFlow.Core.Tests/ContentVideoStoreTests.cs
  - DeckFlow.Core.Tests/SlugifySourceNameTests.cs
autonomous: true
requirements: [KB-03, KB-04, KB-05]
must_haves:
  truths:
    - "A pending content_videos row can be transitioned to captions | whisper | failed | skipped_over_cap by id (UpdateTranscriptStatusAsync)"
    - "A re-run can look up an existing video by (sourceId, youtube_video_id) so pending/failed videos resume instead of being blanket-skipped (GetVideoByYoutubeIdAsync)"
    - "A user can seed a content source from the CLI: content-source-add --url --type --name writes a row to local content_sources"
    - "SlugifySourceName produces a URL-safe ASCII slug from --name; empty/non-ASCII names fall back to a deterministic non-empty slug"
    - "content-source-add distinguishes duplicate-URL (idempotent OK, exit 0) from duplicate-slug-different-URL (error, non-zero exit)"
    - "A re-listed source is returned by ContentSourceStore.ListEnabledSourcesAsync after add"
  artifacts:
    - path: "DeckFlow.Core/Content/ContentVideoStore.cs"
      provides: "UpdateTranscriptStatusAsync(long,string) status UPDATE + GetVideoByYoutubeIdAsync(long,string) resume lookup"
      contains: "UpdateTranscriptStatusAsync"
    - path: "DeckFlow.Core/Content/SlugifySourceName.cs"
      provides: "Pure tested static SlugifySourceName helper (ASCII slug, empty/non-ASCII fallback)"
      contains: "SlugifySourceName"
    - path: "DeckFlow.CLI/CommandRunners.cs"
      provides: "RunContentSourceAddAsync seeding wrapper with dup-url vs dup-slug handling"
      contains: "RunContentSourceAddAsync"
    - path: "DeckFlow.Core.Tests/ContentVideoStoreTests.cs"
      provides: "xUnit coverage of UpdateTranscriptStatusAsync + GetVideoByYoutubeIdAsync + invalid guard"
      contains: "GetVideoByYoutubeIdAsync"
    - path: "DeckFlow.Core.Tests/SlugifySourceNameTests.cs"
      provides: "xUnit coverage of slug normalization + empty/non-ASCII fallback"
      contains: "SlugifySourceName"
  key_links:
    - from: "DeckFlow.CLI/CommandRunners.cs"
      to: "DeckFlow.Core/Content/SlugifySourceName.cs"
      via: "SlugifySourceName(name) → slug before InsertSourceAsync"
      pattern: "SlugifySourceName"
    - from: "DeckFlow.CLI/CommandRunners.cs"
      to: "DeckFlow.Core/Content/ContentSourceStore.cs"
      via: "InsertSourceAsync(slug, name, type, url)"
      pattern: "InsertSourceAsync"
    - from: "DeckFlow.Core/Content/ContentVideoStore.cs"
      to: "content_videos.transcript_status"
      via: "UPDATE ... SET transcript_status"
      pattern: "UPDATE content_videos"
---

<objective>
Close the Wave-0 store gap and seed the harvester's source list so the ingestion services (Plans 20-02, 20-03) and the harvest verb (Plan 20-04) have a place to write status and a populated `content_sources` table. Add the re-run resume lookup (`GetVideoByYoutubeIdAsync`) so reruns resume pending/failed videos rather than blanket-skipping by youtube_video_id (Codex MEDIUM-d).

Purpose: Research Assumption A6 (HIGH risk) — `ContentVideoStore` is insert-only; Phase 20 cannot mark a video's final `transcript_status` without `UpdateTranscriptStatusAsync`, and cannot resume a partial run without a lookup. D-08 pulls a minimal `content-source-add` CLI verb forward to seed the 5 UAT channels (real + reusable, not a throwaway fixture); the slug derivation is extracted to a tested `SlugifySourceName` helper (Codex MEDIUM-a) and the verb distinguishes duplicate-URL (idempotent) from duplicate-slug-different-URL (error).
Output: `IContentVideoStore.UpdateTranscriptStatusAsync` + `GetVideoByYoutubeIdAsync` + impl + tests; a tested `SlugifySourceName` helper; a `content-source-add` CLI verb with dup-url-vs-dup-slug handling.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-content-kb-ingestion-transcription-local/20-CONTEXT.md
@.planning/phases/20-content-kb-ingestion-transcription-local/20-RESEARCH.md

<interfaces>
<!-- Extracted from codebase — executor uses these directly, no exploration needed. -->

From DeckFlow.Core/Content/ContentVideoStore.cs (existing insert pattern to mirror):
- `EnsureSchemaAsync(CancellationToken)` — idempotent CREATE TABLE; call before any query
- Parameter helper: `RelationalDatabaseConnection.AddParameter(command, "@name", value)`
- Connection: `await using var connection = await OpenConnectionAsync(ct)` (private helper already present)
- transcript_status CHECK constraint values (both dialects, lines 330/379): `'pending','captions','whisper','failed','skipped_over_cap'`
- `InsertVideoAsync(sourceId, youtubeVideoId, rssGuid, title, videoUrl, publishedUtc, transcriptStatus, ct)` → long id; UNIQUE(source_id, youtube_video_id)
- `RETURNING id` is used on inserts; UPDATE does not need RETURNING

From DeckFlow.Core/Knowledge/ContentModels.cs:
- `public static class TranscriptStatus` — `Pending`, `Captions`, `Whisper`, `Failed`, `SkippedOverCap` (string consts; values match the CHECK list)
- `public static class ContentSourceType` — `Youtube = "youtube_channel"`, `Podcast = "podcast_rss"`
- existing video read model (e.g. `ContentVideo` record) to return from GetVideoByYoutubeIdAsync — mirror its existing shape; if no read model exists, return a minimal `(long Id, string TranscriptStatus)?` tuple or add a small record

From DeckFlow.Core/Content/ContentSourceStore.cs:
- `Task<long> InsertSourceAsync(string sourceSlug, string displayName, string sourceType, string sourceUrl, CancellationToken)` — RETURNING id; UNIQUE(source_url), UNIQUE(source_slug)
- `Task<IReadOnlyList<ContentSource>> ListEnabledSourcesAsync(CancellationToken)`
- `Task<ContentSource?> GetSourceByUrlAsync(...)` / `GetSourceBySlugAsync(...)` IF present — otherwise the runner inspects the thrown UNIQUE-violation message to classify dup-url vs dup-slug (see Task 2)

From DeckFlow.CLI/Program.cs (flat command-tree convention):
- All commands registered on `rootCommand` directly; handler form `Environment.ExitCode = CommandRunners.RunXAsync(...).GetAwaiter().GetResult();`
- artifacts DB path convention: `Path.Combine(Directory.GetCurrentDirectory(), "artifacts", "content-kb.db")` (matches RunArchidektCacheAsync `artifacts/` convention)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ContentVideoStore — UpdateTranscriptStatusAsync + GetVideoByYoutubeIdAsync (Wave-0 store + resume gaps)</name>
  <files>DeckFlow.Core/Content/IContentVideoStore.cs, DeckFlow.Core/Content/ContentVideoStore.cs, DeckFlow.Core.Tests/ContentVideoStoreTests.cs</files>
  <read_first>
    - DeckFlow.Core/Content/ContentVideoStore.cs (mirror InsertVideoAsync/InsertTranscriptAsync shape: EnsureSchemaAsync → OpenConnectionAsync → CreateCommand → AddParameter → ExecuteNonQueryAsync / single-row read; reuse the existing private const SQL string pattern; note the UNIQUE(source_id, youtube_video_id) index name)
    - DeckFlow.Core/Content/IContentVideoStore.cs (add the two interface members with XML doc-comments matching existing members)
    - DeckFlow.Core/Knowledge/ContentModels.cs lines 156-172 (TranscriptStatus consts — the only legal status values; existing ContentVideo read model if present)
    - DeckFlow.Core.Tests/ContentSourceStoreTests.cs (xUnit fixture convention: temp SQLite path per test, IDisposable cleanup with SqliteConnection.ClearAllPools)
  </read_first>
  <behavior>
    - Test: InsertVideoAsync (pending) then UpdateTranscriptStatusAsync(id, TranscriptStatus.Captions) → row's transcript_status reads back "captions"
    - Test: UpdateTranscriptStatusAsync(id, TranscriptStatus.SkippedOverCap) succeeds and reads back "skipped_over_cap"
    - Test: UpdateTranscriptStatusAsync with a status not in the TranscriptStatus allowlist throws ArgumentException BEFORE issuing SQL (guard, not DB CHECK failure)
    - Test: UpdateTranscriptStatusAsync against a non-existent videoId completes without throwing and affects 0 rows
    - Test: GetVideoByYoutubeIdAsync(sourceId, "abc123") after InsertVideoAsync returns the row with its id + current transcript_status
    - Test: GetVideoByYoutubeIdAsync for a (sourceId, youtubeVideoId) that was never inserted returns null
  </behavior>
  <action>
    Add two members to `IContentVideoStore` with XML `<summary>`/`<param>` doc-comments matching the existing member style:
    (1) `Task UpdateTranscriptStatusAsync(long videoId, string status, CancellationToken cancellationToken = default)`.
    (2) `Task<ContentVideo?> GetVideoByYoutubeIdAsync(long sourceId, string youtubeVideoId, CancellationToken cancellationToken = default)` (return the existing `ContentVideo` read model if one exists; if none exists, add a minimal `public sealed record ContentVideoRef { long Id; string TranscriptStatus; }` in the same file the other content records live in, with `{ get; init; }`, and return that — keep the choice consistent and documented).
    Implement both in `ContentVideoStore` following the existing query shape exactly. For UpdateTranscriptStatusAsync: validate `status` against the `TranscriptStatus` allowlist (Pending, Captions, Whisper, Failed, SkippedOverCap) and throw `ArgumentException` for any other value BEFORE touching the DB (keeps the guard in C#, clear message); `EnsureSchemaAsync`; `OpenConnectionAsync`; run `UPDATE content_videos SET transcript_status = @status WHERE id = @videoId;` as a new `private const string` (Allman, raw-string `"""` literal, byte-preserved); `AddParameter` both params; `ExecuteNonQueryAsync`; no RETURNING. For GetVideoByYoutubeIdAsync: `SELECT id, transcript_status FROM content_videos WHERE source_id = @sourceId AND youtube_video_id = @youtubeVideoId;` as a new private const; read a single row via a reader; return null when no row. Add the six behavior tests to a new `ContentVideoStoreTests` xUnit fixture mirroring `ContentSourceStoreTests` (temp SQLite file per fixture, IDisposable cleanup). Preserve LF endings, file-scoped namespace, `{ get; init; }`.
  </action>
  <verify>
    <automated>"/mnt/c/Program Files/dotnet/dotnet.exe" build DeckFlow.Core/DeckFlow.Core.csproj -c Debug 2>&1 | grep -E "Build succeeded|error" ; "/mnt/c/Program Files/dotnet/dotnet.exe" test DeckFlow.Core.Tests/DeckFlow.Core.Tests.csproj --filter "FullyQualifiedName~ContentVideoStoreTests" 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "UpdateTranscriptStatusAsync" DeckFlow.Core/Content/IContentVideoStore.cs` ≥1 and same in ContentVideoStore.cs
    - `grep -c "GetVideoByYoutubeIdAsync" DeckFlow.Core/Content/IContentVideoStore.cs` ≥1 and same in ContentVideoStore.cs
    - `grep "UPDATE content_videos" DeckFlow.Core/Content/ContentVideoStore.cs` matches (new UPDATE SQL present)
    - Build succeeds 0 errors; new ContentVideoStoreTests pass Failed:0 (or, if VSTest flaky in WSL per CLAUDE.md, build is clean and the test file compiles — note the push-and-watch fallback in SUMMARY)
    - No `{ get; }` mutations, no Format-Document reflow of unrelated lines (git diff touches only the new members + new SQL consts + new test file)
  </acceptance_criteria>
  <done>UpdateTranscriptStatusAsync (allowlist-guarded) and GetVideoByYoutubeIdAsync (resume lookup) exist on interface + impl and are covered by xUnit tests; build clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: SlugifySourceName helper + content-source-add CLI verb (D-08 seeding, dup-url vs dup-slug)</name>
  <files>DeckFlow.Core/Content/SlugifySourceName.cs, DeckFlow.Core.Tests/SlugifySourceNameTests.cs, DeckFlow.CLI/Program.cs, DeckFlow.CLI/CommandRunners.cs</files>
  <read_first>
    - DeckFlow.CLI/Program.cs (flat command registration on rootCommand; Option<T> + AddOption + SetHandler with Environment.ExitCode = ...GetAwaiter().GetResult())
    - DeckFlow.CLI/CommandRunners.cs lines 375-415 (RunArchidektCacheAsync — the artifacts/ DB-path + Serilog logger convention to mirror)
    - DeckFlow.Core/Content/ContentSourceStore.cs (InsertSourceAsync signature + UNIQUE(source_slug)/UNIQUE(source_url) constraints; any GetSourceByUrlAsync/GetSourceBySlugAsync read helpers)
    - DeckFlow.Core/Knowledge/ContentModels.cs lines 177-184 (ContentSourceType.Youtube / .Podcast)
  </read_first>
  <behavior>
    - Test: SlugifySourceName("MTGGoldfish") == "mtggoldfish"
    - Test: SlugifySourceName("The Command Zone!!") == "the-command-zone" (collapsed runs, trimmed dashes)
    - Test: SlugifySourceName("") falls back to a deterministic non-empty slug (e.g. "source") — never returns empty/whitespace
    - Test: SlugifySourceName("日本語") (non-ASCII only) falls back to a deterministic non-empty slug — never empty, never non-ASCII
    - Test (store-level): after content-source add then ListEnabledSourcesAsync, the seeded source is returned
  </behavior>
  <action>
    Create a pure `public static class SlugifySourceName` (or a static method on a small helper class) in `DeckFlow.Core/Content/SlugifySourceName.cs` with `public static string Slugify(string name)`: lowercase-invariant, `Regex.Replace(name.ToLowerInvariant(), "[^a-z0-9]+", "-").Trim('-')`; if the result is empty or whitespace (punctuation-only / non-ASCII input) return a deterministic non-empty fallback `"source"` (Codex MEDIUM-a — never emit an empty slug). XML doc-comment; file-scoped namespace; LF. Add `SlugifySourceNameTests` covering the four behavior cases above.

    Register a flat `content-source-add` command on `rootCommand`. Options: `--url` (Option&lt;string&gt;, IsRequired), `--name` (Option&lt;string&gt;, IsRequired), `--type` (Option&lt;string&gt; default `ContentSourceType.Youtube` = "youtube_channel", Description "youtube_channel | podcast_rss"), `--db` (Option&lt;FileInfo?&gt; defaulting to `artifacts/content-kb.db` when null). Add `public static async Task<int> RunContentSourceAddAsync(string url, string name, string type, FileInfo? db)` to CommandRunners. In the runner: resolve the DB path (default `Path.Combine(Directory.GetCurrentDirectory(), "artifacts", "content-kb.db")`); validate `type` is one of the two `ContentSourceType` consts and return exit code 2 with a clear message otherwise; derive the slug via `SlugifySourceName.Slugify(name)`; print the computed slug to the console before insert; construct `new ContentSourceStore(dbPath)` and call `InsertSourceAsync(slug, name, type, url)`. Dup handling (Codex MEDIUM-a): catch the UNIQUE-violation case and CLASSIFY it — if a source with the same `source_url` already exists (use GetSourceByUrlAsync if available, else match the violated-constraint/column name in the exception message), print "source already exists (same url)" and return 0 (idempotent re-seed); if instead the SAME slug already maps to a DIFFERENT url (duplicate-slug, different-url), print a clear error "slug '{slug}' already used by a different url — pass a distinct --name" and return a non-zero exit (e.g. 3) — do NOT silently treat it as success. Other exceptions return 1. Wire SetHandler exactly like the other commands (`GetAwaiter().GetResult()`). No new packages — `System.Text.RegularExpressions` is BCL.
  </action>
  <verify>
    <automated>"/mnt/c/Program Files/dotnet/dotnet.exe" build DeckFlow.CLI/DeckFlow.CLI.csproj -c Debug 2>&1 | grep -E "Build succeeded|error"; "/mnt/c/Program Files/dotnet/dotnet.exe" test DeckFlow.Core.Tests/DeckFlow.Core.Tests.csproj --filter "FullyQualifiedName~SlugifySourceNameTests" 2>&1 | tail -5; "/mnt/c/Program Files/dotnet/dotnet.exe" run --project DeckFlow.CLI -- content-source-add --url https://www.youtube.com/@MTGGoldfish --name "MTGGoldfish" --type youtube_channel --db artifacts/uat-content-kb.db 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "SlugifySourceName" DeckFlow.Core/Content/SlugifySourceName.cs` ≥1; SlugifySourceNameTests cover empty + non-ASCII fallback (both assert non-empty)
    - `grep -c "content-source-add" DeckFlow.CLI/Program.cs` ≥1; `grep -c "RunContentSourceAddAsync" DeckFlow.CLI/CommandRunners.cs` ≥1
    - CLI run prints the computed slug (e.g. "mtggoldfish") and reports success; re-running the SAME url is idempotent (exit 0, "already exists (same url)")
    - A second add with a name that slugs to the same slug but a DIFFERENT url returns a non-zero exit with a distinct-slug error (no silent success)
    - `--type bogus` returns exit 2 with a clear error and writes no row
    - git diff touches only the new helper + test + Program.cs registration block + the new CommandRunners method (no reflow)
  </acceptance_criteria>
  <done>SlugifySourceName is a tested pure helper with empty/non-ASCII fallback; content-source-add seeds a content_sources row from --url/--name/--type with a derived slug, idempotent on dup-url, errors on dup-slug-different-url; build clean.</done>
</task>

</tasks>

<verification>
- `UpdateTranscriptStatusAsync` + `GetVideoByYoutubeIdAsync` present on interface + impl, allowlist-guarded, tested (closes Research A6 HIGH gap + Codex MEDIUM-d resume).
- `SlugifySourceName` is pure + tested (Codex MEDIUM-a); `content-source-add` usable to seed the 5 UAT channels (D-08) with dup-url-vs-dup-slug handling.
- DeckFlow.Core gains NO new package and NO Microsoft.AspNetCore.* reference.
- Build clean; touch-only-what-you-touch preserved (CLAUDE.md R-6).
</verification>

<success_criteria>
- A pending video can be transitioned to any of the four terminal statuses by id; a rerun can find an existing video by (sourceId, youtube_video_id) to resume.
- The CLI can seed all five UAT channels into local content_sources with stable slugs; dup-url is idempotent, dup-slug-different-url errors.
- No formatting drift, no new dependencies, LF endings preserved.
</success_criteria>

<output>
Create `.planning/phases/20-content-kb-ingestion-transcription-local/20-01-SUMMARY.md` when done.
</output>
