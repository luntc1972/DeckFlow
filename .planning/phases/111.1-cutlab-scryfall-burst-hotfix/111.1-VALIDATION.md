---
phase: 111.1
slug: cutlab-scryfall-burst-hotfix
status: draft
nyquist_compliant: true
wave_0_complete: true
created: 2026-07-31
revision: 1
revision_notes: >
  Created in the round-1 review revision pass to close blocker B-4: 19 sibling phases carry a
  `<N>-VALIDATION.md`, including the decimal-phase precedent `110.1-VALIDATION.md`, and
  `111.1-RESEARCH.md` has a `## Validation Architecture` section, which triggers the convention.
---

# Phase 111.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.
> Observable behaviors to sample are defined in `111.1-RESEARCH.md` §"Validation Architecture".
> Round-1 review findings that added or changed a validation obligation are cited by ID
> (`111.1-REVIEWS.md`).

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | xUnit 2.9.3 (DeckFlow.Web.Tests / DeckFlow.Core.Tests) |
| **Config file** | DeckFlow.sln — standard SDK-style test projects, no per-project runner config |
| **Quick run command** | `"/mnt/c/Program Files/dotnet/dotnet.exe" build DeckFlow.sln -c Release` (clean, 0 new warnings) |
| **Targeted run command** | `"/mnt/c/Program Files/dotnet/dotnet.exe" test DeckFlow.sln -c Release --no-build --filter "FullyQualifiedName~CutLabAnalysisContextBuilderTests\|FullyQualifiedName~CutLabPageServiceTests\|FullyQualifiedName~ScryfallReferenceResolverTests\|FullyQualifiedName~ScryfallCardResolverTests\|FullyQualifiedName~ScryfallThrottleTests\|FullyQualifiedName~DeckAnalysisPacketServiceTests"` |
| **Full suite command** | `"/mnt/c/Program Files/dotnet/dotnet.exe" test DeckFlow.Web.Tests -c Release` + `... test DeckFlow.Core.Tests -c Release` |
| **Estimated runtime** | ~60-120 seconds (build) + suite; expect the Web suite to slow after 111.1-05 raises the global throttle floor — that delta is a recorded deliverable, not a surprise |

*Notes: VSTest is unreliable in WSL (project CLAUDE.md). Rely on `dotnet build` clean + targeted
xUnit assertions, and fall back to push-and-watch CI as the authoritative run when a full-suite run
hangs. Do NOT set `MTG_DATA_DIR`. No TypeScript or Razor is changed by this phase, so no Vitest/tsc
step applies.*

---

## Sampling Rate

- **After every task commit:** the task's own `<automated>` filter run
- **After every plan:** targeted phase filter (above)
- **After every wave:** full `DeckFlow.Web.Tests`
- **Before `/gsd:verify-work`:** full Web + Core suites against the FINAL branch state, recorded in
  `111.1-GATES.md` (SC-6 is not satisfied by an intermediate-state green run)
- **Max feedback latency:** ~120 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 01-T1 RED: routing, 429-vs-other, and second-import locks | 111.1-01 | 1 | SC-1, SC-2, B-1 | T-111.1-01/02/15 | Test-only; no egress. Real-resolver seam bypasses `ScryfallThrottle` via the internal ctor, so no live pacing or live calls | unit | `dotnet test DeckFlow.sln --filter "FullyQualifiedName~CutLabAnalysisContextBuilderTests"` | ✅ CutLabAnalysisContextBuilderTests.cs | ⬜ pending |
| 01-T2 GREEN builder: delegate swap + 429 fail-open + unattempted tracking | 111.1-01 | 1 | SC-1, SC-2, B-1 | T-111.1-01/02/03/15 | Fail-open narrowed to 429 ONLY; non-429 still fails closed; unattempted names never recorded as known-missing | unit | `dotnet test DeckFlow.sln --filter "FullyQualifiedName~CutLabAnalysisContextBuilderTests\|FullyQualifiedName~ScryfallReferenceResolverTests\|FullyQualifiedName~ScryfallCardResolverTests"` | ✅ (same) | ⬜ pending |
| 01-T3 GREEN page service: drop the blind cache short-circuit + degraded-import warning | 111.1-01 | 1 | B-1, W-1 | T-111.1-15/16 | Warning text is plain user copy — no status codes, no upstream internals; Razor auto-encodes | unit | `dotnet test DeckFlow.sln --filter "FullyQualifiedName~CutLabPageServiceTests\|FullyQualifiedName~CutLabWhatifTests\|FullyQualifiedName~CutLabExportServiceTests"` | ✅ CutLabPageServiceTests.cs | ⬜ pending |
| 02-T1 SC-3 end-to-end banner locks (+ B-1, W-1 end to end) | 111.1-02 | 2 | SC-3, B-1, W-1 | T-111.1-04/17 | Fixtures pinned to `CutLabPoolValidator`'s 101-150 window so validation cannot manufacture a false failure; 503 assertions use the exact string each route produces | unit | `dotnet test DeckFlow.sln --filter "FullyQualifiedName~CutLabPageServiceTests"` | ✅ CutLabPageServiceTests.cs | ⬜ pending |
| 02-T2 SC-6 gate sequence recorded and run | 111.1-02 | 2 | SC-6 | T-111.1-05 | Every git gate path-scoped (`-- <paths>`) so a sibling plan's artifact cannot mask or trip it | doc + suite | `dotnet test DeckFlow.Web.Tests -c Release` and `... DeckFlow.Core.Tests -c Release` | ❌ Wave 0 — `111.1-GATES.md` created by this task | ⬜ pending |
| 03-T1 ADR 0004 recorded | 111.1-03 | 2 | SC-4 | T-111.1-07 | Document-only; every empirical claim traced to `111.1-REVIEWS.md` §0 measured probes | doc | `test -f docs/decisions/0004-scryfall-batch-match-key-asymmetry.md && grep -q '## Consequences' ...` | ❌ Wave 0 — created by this task | ⬜ pending |
| 03-T2 RED: punctuation second-pass hit + slash non-collision | 111.1-03 | 2 | SC-4 | T-111.1-18/19 | Slash-collision lock is a permanent guard against a future match-key "simplification" | unit | `dotnet test DeckFlow.sln --filter "FullyQualifiedName~ScryfallReferenceResolverTests"` | ✅ ScryfallReferenceResolverTests.cs | ⬜ pending |
| 03-T3 GREEN: additive second pass + SC-5 xmldoc | 111.1-03 | 2 | SC-4, SC-5 | T-111.1-18/19/20 | Ambiguous keys match NOTHING and fall through to fallback — a wrong card is worse than an extra call | unit | `dotnet test DeckFlow.sln --filter "FullyQualifiedName~ScryfallReferenceResolverTests\|FullyQualifiedName~DeckAnalysisPacketServiceTests\|FullyQualifiedName~DeckComparisonServiceTests\|FullyQualifiedName~MetaGapServiceTests\|FullyQualifiedName~DeckHistoryPageServiceTests\|FullyQualifiedName~CutLabAnalysisContextBuilderTests"` | ✅ (six existing suites) | ⬜ pending |
| 04-T1 pacing census + latency derivation | 111.1-04 | 3 | SC-7 (measurement) | T-111.1-09/10/11/21 | Zero live Scryfall calls; counts come from test seams, not timing; `NOT MEASURED` is a permitted visible outcome | doc | `test -f .planning/phases/111.1-cutlab-scryfall-burst-hotfix/111.1-PACING-MEASUREMENT.md && grep -qi 'concurren' ...` | ❌ Wave 0 — created by this task | ⬜ pending |
| 04-T2 SC-7 acceptance checkpoint (BLOCKING) | 111.1-04 | 3 | SC-7 (acceptance) | T-111.1-09 | Human gate; never auto-approved, `workflow.auto_advance` does not apply | manual | — (see Manual-Only Verifications) | ⬜ pending |
| 05-T1 RED: pacing-floor lower-bound lock | 111.1-05 | 4 | SC-7 | T-111.1-14 | Lower bound ONLY — the gate is process-wide static state, so an upper bound would be flaky | unit | `dotnet test DeckFlow.sln --filter "FullyQualifiedName~ScryfallThrottleTests"` | ✅ ScryfallThrottleTests.cs | ⬜ pending |
| 05-T2 GREEN: MinInterval 500ms + both stale comments corrected | 111.1-05 | 4 | SC-7 | T-111.1-12/13 | No new configuration surface; adjacent retry constants untouched | unit | `dotnet test DeckFlow.sln --filter "FullyQualifiedName~ScryfallThrottleTests\|FullyQualifiedName~ScryfallCardResolverTests\|FullyQualifiedName~CardLookupServiceTests"` | ✅ (same) | ⬜ pending |
| 05-T3 final-state gates + complete criterion map | 111.1-05 | 4 | SC-6, SC-7 | T-111.1-22 | Every row must read COVERED or DECLINED; a remaining gap escalates to a top-of-file callout | doc + suite | `dotnet test DeckFlow.Web.Tests -c Release` and `... DeckFlow.Core.Tests -c Release` | ✅ 111.1-GATES.md (from 02-T2) | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Quarantined Assertions — must pass UNMODIFIED

These three are the assertions this phase most risks breaking. Any of them going red is a
STOP-and-report, never a "fix the test."

| Assertion | Location | Why it is quarantined |
|-----------|----------|-----------------------|
| H2 lock: `ResolveBatchAsync_SingleSlashNameWithNormalizeOn_FallsThroughToFallbackKeyedByOriginalName` | `DeckFlow.Web.Tests/ScryfallReferenceResolverTests.cs:27-52` | Deliberate regression lock on the DFC fallback path. Plan 03's second pass is designed slash-preserving specifically so this survives. Its fixture returns the card in `data` with `NotFound: []`, so it is NOT protected by the "lands in not_found at the API" argument. |
| Deck Analysis golden `submitted_name: Blex, Vexing Pest / ...` | `DeckFlow.Web.Tests/AnalysisGoldens.cs:1798` | A DFC single-slash annotation. Plan 03 flips `FromFallback` for the punctuation class only; this row is a slash case and must not move. |
| Deck Analysis assertion `submitted_name: Ya viene el coco \| resolved_card: Perfect Defense // Denting Blows` | `DeckFlow.Web.Tests/DeckAnalysisPacketServiceTests.cs:1460` | Unrelated printed/oracle names; must not be captured by any normalized key. |

Regenerating a golden here is a prompt-artifact change, and prompt artifacts are the project's stated
core value. Report and let the user decide.

---

## Wave 0 Requirements

No new test project, framework, or fixture scaffolding is needed — every test file this phase
extends already exists and already exercises the exact seams being changed. Four ARTIFACTS do not yet
exist and are created by the tasks that own them:

| Artifact | Created by | Why it is Wave 0 |
|----------|-----------|------------------|
| `111.1-GATES.md` | 02-T2 | It IS the SC-6 deliverable; 05-T3 appends to it |
| `docs/decisions/0004-scryfall-batch-match-key-asymmetry.md` | 03-T1 | It IS the SC-4 deliverable; 03-T3 and 05-T3 cross-reference it |
| `111.1-PACING-MEASUREMENT.md` | 04-T1 | It IS the SC-7 measurement precondition; 04-T2's checkpoint reads it |
| `111.1-VALIDATION.md` (this file) | round-1 review revision | Closes blocker B-4 |

Extended test files (all exist today):
- `CutLabAnalysisContextBuilderTests.cs` — `CountingResolver` gains fallback/collection knobs; a real
  `ScryfallCardResolver` seam is added for the end-to-end call count (round-1 review W-3)
- `CutLabPageServiceTests.cs` — `FakeResolver` gains the same knobs plus a mutable card list
- `ScryfallReferenceResolverTests.cs` — two additive tests; the H2 lock is untouched
- `ScryfallThrottleTests.cs` — one additive lower-bound pacing test

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| SC-7 pacing acceptance | SC-7 | The decision is the user's, and the criterion explicitly requires acceptance to FOLLOW the measurement | Read `111.1-PACING-MEASUREMENT.md`, then reply `accept` / `accept with mitigation: <what>` / `reject` at 111.1-04 Task 2. User confirmed 2026-07-31 to leave this gate in place; it is never auto-approved. |
| Production banner gone | SC-3 | Prod-only, intermittent, and reproducing it means hammering the live rate limit the phase exists to stop | After 111.1-01 deploys, import a real oversized pool on `https://www.deckflow.gg/cut-lab` once and confirm no Scryfall banner. Do NOT retry in a loop. |
| Degraded-import warning reads well | W-1 | Copy quality and theme rendering are not unit-assertable | Eyeball the warning on one theme after 111.1-01; the string itself is asserted by 02-T1 via a distinctive substring. |

---

## Validation Sign-Off

- [x] All tasks have an `<automated>` verify or a named Wave 0 artifact dependency
- [x] Sampling continuity: no 3 consecutive tasks without an automated verify
- [x] Wave 0 covers all MISSING references (four artifacts, each owned by a named task)
- [x] No watch-mode flags
- [x] Feedback latency < 120s
- [x] Quarantined assertions enumerated with STOP-and-report handling
- [x] `nyquist_compliant: true` set in frontmatter

**Approval:** planner-approved 2026-07-31 (round-1 review revision; Codex cross-AI gate still OWED —
unavailable until 2026-08-04 per `111.1-REVIEWS.md`, deferred not discharged)
</content>
