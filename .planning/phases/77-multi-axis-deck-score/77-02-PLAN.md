---
phase: 77-multi-axis-deck-score
plan: 02
type: execute
wave: 1
depends_on: ["77-01"]
files_modified:
  - DeckFlow.Core/Analysis/MultiAxisScore.cs
  - DeckFlow.Core/Analysis/MultiAxisScorer.cs
  - DeckFlow.Core.Tests/MultiAxisScorerTests.cs
autonomous: true
requirements: [SCORE-01, SCORE-02, SCORE-03]
must_haves:
  truths:
    - "MultiAxisScorer.Score returns Power/Speed/Control/Consistency bands each in 0-5"
    - "BandLabel maps 0=None,1=Low,2=Modest,3=Moderate,4=High,5=Extreme (and >5 clamps to Extreme)"
    - "Each axis carries a DeckScoreRationale with the signal values that produced its band"
    - "Score cross-checks against the bracket number and sets ScoreAlignsBracket + BracketCrossCheckText"
    - "A cEDH-proxy DeckStatSummary scores Power>=4 and Speed>=4; a battlecruiser-proxy scores Power<=2 and Speed<=2"
  artifacts:
    - path: "DeckFlow.Core/Analysis/MultiAxisScore.cs"
      provides: "DeckMultiAxisScore + DeckScoreRationale records"
      contains: "public sealed record DeckMultiAxisScore"
    - path: "DeckFlow.Core/Analysis/MultiAxisScorer.cs"
      provides: "Static Score() + BandLabel() band derivation"
      contains: "public static DeckMultiAxisScore Score("
    - path: "DeckFlow.Core.Tests/MultiAxisScorerTests.cs"
      provides: "Band-label theory + golden cEDH-vs-casual + per-axis derivation tests"
  key_links:
    - from: "MultiAxisScorer.Score"
      to: "DeckStatSummary new fields"
      via: "reads stats.Tutors/FastMana/RampDrawUnderThreeMv/Counters/Interaction/Wipes/AverageManaValue"
      pattern: "stats\\.(Tutors|FastMana|Counters|RampDrawUnderThreeMv)"
---

<objective>
Create the pure-Core multi-axis scorer: a `DeckMultiAxisScore` record and a static
`MultiAxisScorer` that maps existing + new deck signals into four coarse 0-5 bands with
inline rationale, plus a bracket cross-check. Calibrate band cutpoints so the golden
cEDH-vs-battlecruiser test passes (SCORE-03).

Purpose: SCORE-01 (coarse bands, no decimals), SCORE-02 (band derivation from signals),
SCORE-03 (rationale + bracket cross-check) — the deterministic heart of the feature.
Output: two new Core files + a new test file, all GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/77-multi-axis-deck-score/77-RESEARCH.md
@.planning/phases/77-multi-axis-deck-score/77-PATTERNS.md
@.planning/phases/77-multi-axis-deck-score/UI-SPEC.md
@CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create MultiAxisScore records and MultiAxisScorer</name>
  <read_first>
    - DeckFlow.Core/Bracket/BracketClassification.cs (analog: sealed record with positional params + XML doc)
    - DeckFlow.Core/Bracket/BracketClassifier.cs (analog: static class, ArgumentNullException.ThrowIfNull, chained-if threshold gates lines 79-99, null-vs-empty combo semantics lines 63-68)
    - DeckFlow.Core/Analysis/DeckStatAggregator.cs (DeckStatSummary fields consumed: AverageManaValue, Interaction, Wipes, Ramp, Draw, plus the new Tutors/FastMana/RampDrawUnderThreeMv/Counters)
    - 77-RESEARCH.md "New Core Types: MultiAxisScore" + "Band derivation logic" tables + "Bracket cross-check (SCORE-03)"
    - 77-PATTERNS.md "MultiAxisScore.cs" + "MultiAxisScorer.cs" sections
    - UI-SPEC.md §3 (band-label vocabulary), §6 (rationale phrasing), §10 (cross-check copy)
  </read_first>
  <behavior>
    - BandLabel(0..5) => None/Low/Modest/Moderate/High/Extreme; BandLabel(6) => Extreme (clamp)
    - Score(stats, gameChangerCount, twoCardComboCount, comboDetectionAvailable, bracketNumber) returns
      all four bands within [0,5] and four non-empty rationale strings
    - comboDetectionAvailable == false => Power/Consistency rationale text discloses "combo data unavailable"
      (never asserts "0 combos")
    - ScoreAlignsBracket == false and BracketCrossCheckText names the contradiction when an axis diverges
      from the bracket (e.g. Power band 5 with bracketNumber 2)
  </behavior>
  <action>
    Create DeckFlow.Core/Analysis/MultiAxisScore.cs with file-scoped namespace `DeckFlow.Core.Analysis;`:
    `public sealed record DeckMultiAxisScore(int PowerBand, int SpeedBand, int ControlBand,
    int ConsistencyBand, DeckScoreRationale PowerRationale, DeckScoreRationale SpeedRationale,
    DeckScoreRationale ControlRationale, DeckScoreRationale ConsistencyRationale, int BracketNumber,
    string BracketCrossCheckText, bool ScoreAlignsBracket)` and
    `public sealed record DeckScoreRationale(string SignalText)`, XML doc on both.
    Create DeckFlow.Core/Analysis/MultiAxisScorer.cs: `public static class MultiAxisScorer` with
    `public static DeckMultiAxisScore Score(DeckStatSummary stats, int gameChangerCount,
    int twoCardComboCount, bool comboDetectionAvailable, int bracketNumber)` guarded by
    `ArgumentNullException.ThrowIfNull(stats)`, and `public static string BandLabel(int band)` as the
    documented switch expression (band switch over 0..5, `_ => "Extreme"`).
    Derive each band with chained-if threshold gates (NOT switch expressions — re-indent carve-out) using
    the 77-RESEARCH band tables as starting cutpoints:
    - Power: GC-dominant (gameChangerCount) with twoCardComboCount + stats.FastMana as modifiers.
    - Speed: stats.AverageManaValue + stats.FastMana + stats.RampDrawUnderThreeMv.
    - Control: stats.Interaction + stats.Wipes + stats.Counters.
    - Consistency: stats.Tutors + twoCardComboCount + stats.AverageManaValue smoothness.
    Build each DeckScoreRationale signal string from the actual signal values per UI-SPEC §6 (ASCII, plain
    hyphens, no em/en dashes). Compute ScoreAlignsBracket + BracketCrossCheckText from the bracket cross-check
    rule in 77-RESEARCH (misalign if Power>=4 and bracket<=2, or Power<=1 and bracket>=4; agree text vs
    divergence text per UI-SPEC §6/§10). Clamp every band into [0,5].
  </action>
  <verify>
    <automated>dotnet.exe build DeckFlow.Core 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - MultiAxisScore.cs contains `public sealed record DeckMultiAxisScore(` and `public sealed record DeckScoreRationale(`
    - MultiAxisScorer.cs contains `public static DeckMultiAxisScore Score(` and `public static string BandLabel(int band) => band switch`
    - `grep -c "switch" DeckFlow.Core/Analysis/MultiAxisScorer.cs | grep -v '^0$'` shows switch used ONLY for BandLabel (band derivations use chained-if)
    - `dotnet.exe build DeckFlow.Core` reports 0 warnings 0 errors
  </acceptance_criteria>
  <done>Records + scorer compile clean; BandLabel is the only switch expression; bands clamped 0-5.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: MultiAxisScorerTests — band labels, axis derivations, golden test, then calibrate</name>
  <read_first>
    - DeckFlow.Core.Tests/Bracket/BracketClassifierTests.cs (analog: sealed test class, helper fixture factories, [Theory]/[InlineData] + [Fact] golden patterns)
    - 77-PATTERNS.md "MultiAxisScorerTests.cs (NEW)" section (CedhStats()/CasualStats() DeckStatSummary fixture construction with `{ init }` field syntax; BandLabel theory; golden [Fact]s)
    - 77-RESEARCH.md "Test Fixtures for Golden Test (SCORE-03)" (exact cEDH vs battlecruiser signal values)
    - DeckFlow.Core/Analysis/MultiAxisScorer.cs (the implementation from Task 1 — calibrate its cutpoints here)
  </read_first>
  <behavior>
    - BandLabel_MapsCorrectly theory: (0,None)(1,Low)(2,Modest)(3,Moderate)(4,High)(5,Extreme)(6,Extreme)
    - Score_CedhDeck_ScoresPowerAndSpeedHigh: CedhStats(), GC 8, combos 3, available, bracket 5 => PowerBand>=4 AND SpeedBand>=4
    - Score_CasualDeck_ScoresPowerAndSpeedLow: CasualStats(), GC 0, combos 0, available, bracket 2 => PowerBand<=2 AND SpeedBand<=2
    - Score_DenseInteraction_ScoresControlHigh: high Interaction/Wipes/Counters => ControlBand>=4
    - Score_ManyTutorsAndCombos_ScoresConsistencyHigh => ConsistencyBand>=4
    - Score_ComboUnavailable_DisclosesInRationale: comboDetectionAvailable false => Power rationale contains "combo data unavailable"
  </behavior>
  <action>
    Create DeckFlow.Core.Tests/MultiAxisScorerTests.cs (`public sealed class MultiAxisScorerTests`, namespace
    `DeckFlow.Core.Tests`, `using DeckFlow.Core.Analysis;`). Add `CedhStats()` and `CasualStats()` private
    static DeckStatSummary factories using the 77-RESEARCH signal values (init-field syntax for the four new
    fields). Add the `[Theory]/[InlineData]` BandLabel test and the `[Fact]` golden + per-axis derivation +
    combo-disclosure tests from the behavior list. Run the filter; if the golden or axis assertions fail,
    calibrate the chained-if cutpoints in MultiAxisScorer.cs (Task-1 file) until GREEN — keep the bands coarse
    (no decimals) and monotonic. Use ASCII assertion messages.
  </action>
  <verify>
    <automated>dotnet.exe test DeckFlow.Core.Tests --filter "FullyQualifiedName~MultiAxisScorer" 2>&1 | tail -6</automated>
  </verify>
  <acceptance_criteria>
    - MultiAxisScorerTests.cs contains `Score_CedhDeck_ScoresPowerAndSpeedHigh` and `Score_CasualDeck_ScoresPowerAndSpeedLow`
    - MultiAxisScorerTests.cs contains `BandLabel_MapsCorrectly` `[Theory]` with the 7 InlineData rows
    - `dotnet.exe test DeckFlow.Core.Tests --filter "FullyQualifiedName~MultiAxisScorer"` exits 0
    - `dotnet.exe test DeckFlow.Core.Tests` exits 0 (no regression in the broader Core suite)
  </acceptance_criteria>
  <done>Golden cEDH>casual band separation holds; BandLabel theory + axis tests GREEN; full Core suite GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| (none new) | Pure Core deterministic mapping over integer signals + a bracket number; no I/O, no DI, no network |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-77-02-01 | Tampering | band derivation cutpoints | accept | Heuristic estimate disclosed to the user/AI as "re-check and refine" (UI-SPEC §10); golden test guards gross miscalibration |
| T-77-SC | Tampering | package installs | mitigate | No new NuGet packages; in-solution code only |
</threat_model>

<verification>
- `dotnet.exe build DeckFlow.Core` clean (0/0)
- `dotnet.exe test DeckFlow.Core.Tests --filter "FullyQualifiedName~MultiAxisScorer"` GREEN
- Full `dotnet.exe test DeckFlow.Core.Tests` GREEN
</verification>

<success_criteria>
Deterministic four-axis scorer with rationale + bracket cross-check; cEDH proxy scores Power/Speed
>= 4 and battlecruiser proxy <= 2; band labels map exactly; no decimals exposed.
</success_criteria>

<output>
Create `.planning/phases/77-multi-axis-deck-score/77-02-SUMMARY.md` when done
</output>
