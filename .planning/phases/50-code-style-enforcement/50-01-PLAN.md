---
phase: 50-code-style-enforcement
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .editorconfig
  - .planning/rs-export.editorconfig
  - .planning/phases/50-code-style-enforcement/50-RECONCILIATION.md
autonomous: true
requirements: [FMT-01]
user_setup: []

must_haves:
  truths:
    - "The operator's ReSharper code-style export already exists in D-02 .editorconfig-keyed form at .planning/rs-export.editorconfig (27 lines, header 'Generated from rs.DotSettings, best-effort'); the agent reads it directly and diffs it against the committed .editorconfig"
    - "50-RECONCILIATION.md lists every one of the export's 8 source keys with a KEEP-EXISTING / ADOPT-RS / REJECT(constraint-wins) / IGNORE(out-of-scope) resolution and a gate-enforceable (dotnet_*/csharp_*) vs JetBrains-only (resharper_*) classification per key"
    - "The reconciled .editorconfig still contains all five carve-out rules verbatim in effect (init accessors, switch expressions, attribute placement, xmldoc indent, raw-string indent)"
    - "The committed [*] end_of_line stays lf — the export's [*] end_of_line = crlf is REJECTED on the LF hard constraint (.editorconfig line 9 + .gitattributes '* text=auto eol=lf'); the merge introduces NO crlf under [*]"
    - "git diff .editorconfig shows at most 3 additive resharper_* lines in [*.cs] and NOTHING removed/weakened — zero gate-enforceable (dotnet_*/csharp_*) keys added, so the changed-lines gate enforces the SAME style as the current committed config"
    - "Every ADOPT-RS key is reviewed against the four CarveOutGuard fixtures (init / raw-string / attribute / switch); the 3 resharper_* keys are JetBrains-only and invisible to dotnet format, so none can threaten a carve-out"
    - "The throwaway RS export file (.planning/rs-export.editorconfig, and any leftover .planning/rs.DotSettings) is deleted from the tree after a SUCCESSFUL merge; 50-RECONCILIATION.md is the permanent audit trail (D-03)"
    - "BLOCKED fallback only: if .planning/rs-export.editorconfig is missing or empty at execution time, 50-RECONCILIATION.md is marked BLOCKED, .editorconfig is unchanged, the 50-01-SUMMARY status is BLOCKED, and Plans 02-04 are FORBIDDEN to run"
  artifacts:
    - path: ".planning/phases/50-code-style-enforcement/50-RECONCILIATION.md"
      provides: "Permanent audit trail listing all 8 source keys + resolutions (D-02/D-03), OR a BLOCKED marker if the export is missing/empty"
      contains: "REJECT"
    - path: ".editorconfig"
      provides: "Reconciled source of truth with carve-outs intact, LF preserved, and up to 3 non-conflicting resharper_* prefs merged"
      contains: "resharper_csharp_force_indent_style_for_raw_string_literals"
  key_links:
    - from: ".editorconfig"
      to: "the five carve-outs at lines 45-82 (current file)"
      via: "verbatim preservation — no key weakened"
      pattern: "csharp_style_prefer_switch_expression|resharper_place_attribute_on_same_line|resharper_xmldoc_indent_text|force_indent_style_for_raw_string_literals|prefer_auto_properties"
    - from: ".editorconfig"
      to: "[*] end_of_line = lf (line 9)"
      via: "LF constraint wins over export crlf — never overwritten"
      pattern: "end_of_line = lf"
    - from: "50-RECONCILIATION.md"
      to: "each of the 8 exported RS source keys"
      via: "per-key resolution row"
      pattern: "KEEP-EXISTING|ADOPT-RS|REJECT|IGNORE"
---

<objective>
Reconcile the operator's exported ReSharper code-style (already present at `.planning/rs-export.editorconfig`) into the committed `.editorconfig`, with the five bug-driven carve-outs and the LF line-ending constraint overriding any conflicting ReSharper preference, and produce `50-RECONCILIATION.md` as the permanent audit trail.

Purpose: The downstream changed-lines gate (Plans 02/03) enforces the RECONCILED config. Reconciliation MUST land first (Wave 1) so the gate and the carve-out guard test evaluate the final config, not the pre-merge one. This is the foundation of the whole phase.

Output: A merged `.editorconfig` (carve-outs intact, LF preserved, up to 3 non-conflicting `resharper_*` prefs added) and `50-RECONCILIATION.md`. The transient RS export file is deleted after a successful merge (D-03). The export is already committed into `.planning/` so the operator-export checkpoint is SATISFIED; a BLOCKED state is only a fallback if the file is missing/empty at execution time.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/50-code-style-enforcement/50-SPEC.md
@.planning/phases/50-code-style-enforcement/50-CONTEXT.md
@.planning/phases/50-code-style-enforcement/50-RESEARCH.md
@.editorconfig

<interfaces>
<!-- The concrete export input. Source: .planning/rs-export.editorconfig (27 lines, "Generated from rs.DotSettings, best-effort"). -->
<!-- The export contains exactly 8 source keys, pre-classified below; the reconciliation must record all 8. -->
<!--   [*]    end_of_line = crlf          -> REJECT (constraint-wins): collides with committed [*] end_of_line = lf + .gitattributes eol=lf. -->
<!--   [*]    insert_final_newline = true  -> KEEP-EXISTING (already matches committed line 10). -->
<!--   (root) root = true                  -> KEEP-EXISTING (already matches committed line 1). -->
<!--   [*.cs] resharper_align_multiline_binary_expressions_chain = false -> ADOPT-RS (JetBrains-only, additive). -->
<!--   [*.cs] resharper_indent_braces_inside_statement_conditions   = false -> ADOPT-RS (JetBrains-only, additive). -->
<!--   [*.cs] resharper_stick_comment                               = false -> ADOPT-RS (JetBrains-only, additive). -->
<!--   The export self-documents 4 rs.DotSettings settings with NO EditorConfig equivalent -> IGNORE (out of scope): -->
<!--     GenerateMemberBody/PlaceBackingFieldAboveProperty, CodeInspection/Highlighting/IdentifierHighlightingEnabled, -->
<!--     CodeStyle/CodeCleanup/RecentlyUsedProfile, Naming/CSharpAutoNaming/IsNotificationDisabled. -->

<!-- The five carve-outs the agent MUST preserve verbatim. Source: .editorconfig (current). -->
<!-- Carve-out 1 — init accessor preservation (lines 45-51): keys are non-enforcing :silent. -->
<!--   dotnet_style_prefer_auto_properties = true:silent / dotnet_style_readonly_field = true:silent / csharp_style_prefer_readonly_struct_member = true:silent -->
<!-- Carve-out 2 — switch expressions (lines 53-55): csharp_style_prefer_switch_expression = true:silent -->
<!-- Carve-out 3 — attribute placement (lines 62-70): resharper_*place_*attribute_on_same_line = false  (JetBrains-only; dotnet format cannot rewrite) -->
<!-- Carve-out 4 — xmldoc indent (lines 72-76): resharper_xmldoc_indent_text = ZeroIndent / resharper_xmldoc_indent_child_elements = ZeroIndent (JetBrains-only) -->
<!-- Carve-out 5 — raw-string indent (lines 78-82): resharper_csharp_force_indent_style_for_raw_string_literals = LeftAligned (JetBrains-only) -->

<!-- The HARD LF constraint (separate from the 5 named carve-outs). Source: .editorconfig line 9 ([*] end_of_line = lf), -->
<!-- line 24-25 ([*.{ps1,bat,cmd}] end_of_line = crlf — untouched), and .gitattributes line 6 (* text=auto eol=lf). -->
<!-- The export's [*] end_of_line = crlf MUST be REJECTED; the committed LF stays. -->

<!-- RESEARCH key insight: dotnet format reads ONLY dotnet_*/csharp_* keys; it IGNORES resharper_* keys entirely. -->
<!-- So the 3 ADOPT-RS resharper_* keys are physically un-enforceable by the gate (informational/Rider-only) and -->
<!-- CANNOT threaten any carve-out. The reconciliation report classifies each exported key gate-enforceable vs JetBrains-only. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Diff .planning/rs-export.editorconfig vs committed .editorconfig, write 50-RECONCILIATION.md with the determined 8-key classification, merge the 3 ADOPT-RS resharper_* keys (carve-outs + LF win), delete the export</name>
  <files>.editorconfig, .planning/phases/50-code-style-enforcement/50-RECONCILIATION.md, .planning/rs-export.editorconfig (deleted on success)</files>
  <read_first>
    - .planning/rs-export.editorconfig (the concrete export — 27 lines, 8 source keys; the input to reconcile)
    - .editorconfig (the committed target — carve-outs at lines 45-82; [*] end_of_line = lf at line 9; [*.{ps1,bat,cmd}] crlf at lines 24-25)
    - .gitattributes (the LF hard constraint — line 6 '* text=auto eol=lf')
    - .planning/phases/50-code-style-enforcement/50-CONTEXT.md (D-02 reconciliation workflow, D-03 delete-after-merge)
    - .planning/phases/50-code-style-enforcement/50-SPEC.md (REQ-1, REQ-2, Constraints "Carve-outs override everything")
    - .planning/phases/50-code-style-enforcement/50-RESEARCH.md (Open Q1: classify each key dotnet_*/csharp_* enforceable vs resharper_* JetBrains-only; resharper_* keys INVISIBLE to dotnet format)
  </read_first>
  <action>
    BLOCKED-PATH FALLBACK: if `.planning/rs-export.editorconfig` is missing or empty at execution time, do NOT write a "successful" stub. Instead write `50-RECONCILIATION.md` whose FIRST heading is an explicit `# RECONCILIATION — BLOCKED (no ReSharper export present)` marker stating the export is missing, that NO `.editorconfig` change was made, and that Plans 02-04 are FORBIDDEN to run until a real reconciliation completes. Make NO change to `.editorconfig`. The 50-01-SUMMARY (see <output>) MUST carry status `BLOCKED`. Then stop — the plan re-runs when the export lands. (The export is already committed into `.planning/`, so this path should not trigger; it exists only as a safety net.)

    SUCCESS PATH (export present — expected): The export at `.planning/rs-export.editorconfig` contains exactly 8 source keys. Record EACH as a row in `50-RECONCILIATION.md` using the determined classification below (all 8 rows MUST appear). Do NOT silently omit, re-derive away, or collapse rows.

    ADOPT-RS (3 keys — `resharper_*` C# keys; JetBrains-only, INVISIBLE to `dotnet format`, therefore informational/Rider-only, NOT gate-enforceable, and they CANNOT threaten a carve-out). Add each additively into the existing `[*.cs]` block of `.editorconfig` only if not already present (grep first; if already present, leave the file unchanged for that key and mark the row "ADOPT-RS (already present)"):
    - `resharper_align_multiline_binary_expressions_chain = false`
    - `resharper_indent_braces_inside_statement_conditions = false`
    - `resharper_stick_comment = false`

    REJECT — constraint wins (1 key; this is NOT one of the 5 named carve-outs — it is a separate HARD constraint):
    - export `[*] end_of_line = crlf` COLLIDES with the committed `.editorconfig` `[*] end_of_line = lf` (line 9) and `.gitattributes` `* text=auto eol=lf`. The committed LF STAYS; the CRLF preference is REJECTED. The `[*.{ps1,bat,cmd}] end_of_line = crlf` exception (committed lines 24-25) is untouched. Record the resolution as `REJECT (constraint-wins / KEEP-EXISTING-LF)`. NEVER write `end_of_line = crlf` under `[*]`.

    KEEP-EXISTING (2 keys — already match the committed file, no change):
    - export `root = true` (matches committed line 1).
    - export `insert_final_newline = true` (matches committed line 10).

    IGNORE — out of scope (4 keys the export self-documents as having NO EditorConfig equivalent; record them so the audit covers all 8 source keys):
    - `CodeEditing/GenerateMemberBody/PlaceBackingFieldAboveProperty`
    - `CodeInspection/Highlighting/IdentifierHighlightingEnabled`
    - `CodeStyle/CodeCleanup/RecentlyUsedProfile`
    - `CodeStyle/Naming/CSharpAutoNaming/IsNotificationDisabled`

    For every row also classify enforceability: `dotnet_*`/`csharp_*` = gate-enforceable; `resharper_*` = JetBrains-only (invisible to `dotnet format`, per RESEARCH key insight); line-ending / IGNORE keys = not a gate-enforceable C# style key. NOTE the net result for Plan 02: ZERO gate-enforceable (dotnet_*/csharp_*) keys are added, so the changed-lines gate enforces the SAME style as the current committed config.

    CARVE-OUT CONFLICT REVIEW (kept from the prior revision; now concrete): before finalizing, REVIEW the 3 ADOPT-RS keys against the four CarveOutGuard fixtures — init / raw-string / attribute / switch (Plan 03). All 3 are `resharper_*` and invisible to `dotnet format`, so none can cause the gate-enforcing formatter to rewrite `init`, re-indent a raw string, inline an attribute, or convert a switch expression. Record the explicit finding: "no RS key collides with init / raw-string / attribute / switch / xmldoc — no carve-out conflicts; the only conflict is the EOL one, rejected on the LF constraint."

    Merge ONLY the (up to) 3 ADOPT-RS resharper_* keys into `.editorconfig` — append them into the existing `[*.cs]` block, preserving the file's existing comment structure and the carve-out comment rationales verbatim. Do NOT touch the carve-out lines (45-82) except to leave them exactly as-is. Do NOT touch `[*] end_of_line = lf` (line 9). Preserve LF line endings (.gitattributes pins `.editorconfig` to `eol=lf`); add no whole-file reflow.

    `50-RECONCILIATION.md` structure (success path): a header explaining the source-of-truth model, D-02/D-03, and that the input was `.planning/rs-export.editorconfig`; a table with columns `RS Key | RS Value | Committed Value | Resolution | Enforceable? | Reason` covering ALL 8 source keys with the resolutions above; a "Carve-out conflict review" subsection recording the four-fixture finding above (no conflicts; only the EOL conflict, rejected on LF); a "Line-ending constraint" subsection stating the export crlf was rejected and committed LF preserved; a closing "Carve-out integrity" section confirming all five carve-outs survived; and a note that `.planning/rs-export.editorconfig` was deleted (D-03).

    After writing the report and merging (SUCCESS path only), DELETE `.planning/rs-export.editorconfig` from the tree, AND delete any leftover `.planning/rs.DotSettings` if present (D-03 — these are throwaway inputs; the report is the durable record).
  </action>
  <verify>
    <automated>test -f .planning/phases/50-code-style-enforcement/50-RECONCILIATION.md &amp;&amp; ! test -f .planning/rs-export.editorconfig &amp;&amp; grep -Eq 'REJECT|KEEP-EXISTING|ADOPT-RS|IGNORE|BLOCKED' .planning/phases/50-code-style-enforcement/50-RECONCILIATION.md &amp;&amp; grep -q 'resharper_csharp_force_indent_style_for_raw_string_literals = LeftAligned' .editorconfig &amp;&amp; grep -q 'csharp_style_prefer_switch_expression = true:silent' .editorconfig &amp;&amp; grep -q 'resharper_xmldoc_indent_text = ZeroIndent' .editorconfig &amp;&amp; grep -q 'resharper_place_attribute_on_same_line = false' .editorconfig &amp;&amp; grep -q 'dotnet_style_prefer_auto_properties = true:silent' .editorconfig &amp;&amp; grep -A2 '^\[\*\]$' .editorconfig | grep -q 'end_of_line = lf' &amp;&amp; ! (git diff .editorconfig | grep -E '^\+' | grep -q 'end_of_line = crlf') &amp;&amp; echo OK</automated>
  </verify>
  <acceptance_criteria>
    - SUCCESS PATH: `50-RECONCILIATION.md` exists with a per-key resolution table covering ALL 8 source keys from `.planning/rs-export.editorconfig`; the determined classification is recorded exactly — 3x ADOPT-RS (resharper_* C# keys), 1x REJECT (export `[*] end_of_line = crlf`, constraint-wins/KEEP-EXISTING-LF), 2x KEEP-EXISTING (`root = true`, `insert_final_newline = true`), 4x IGNORE (out-of-scope rs.DotSettings keys). Each row has an Enforceable? (dotnet_*/csharp_* vs resharper_* vs n/a) classification.  (SPEC AC-1)
    - CRLF-REJECT (HARD): the committed `.editorconfig` `[*] end_of_line` stays `lf` after the merge — `grep -A2 '^\[\*\]$' .editorconfig` shows `end_of_line = lf` — and `git diff .editorconfig` introduces NO `end_of_line = crlf` under `[*]`. The `[*.{ps1,bat,cmd}] end_of_line = crlf` exception is untouched.
    - SUCCESS PATH: `.editorconfig` diff = AT MOST 3 additive `resharper_*` lines in the `[*.cs]` section (fewer if any were already present) + NOTHING removed or weakened + NO `[*] end_of_line` change. ZERO gate-enforceable (`dotnet_*`/`csharp_*`) keys added — so the changed-lines gate enforces the SAME style as the current committed `.editorconfig`.
    - SUCCESS PATH: the report's "Carve-out conflict review" subsection records that no RS key collides with init / raw-string / attribute / switch / xmldoc ("no carve-out conflicts"), and that the only conflict was the EOL one, rejected on the LF constraint.
    - All five carve-out rules are still present verbatim in `.editorconfig`: `dotnet_style_prefer_auto_properties = true:silent`, `csharp_style_prefer_switch_expression = true:silent`, `resharper_place_attribute_on_same_line = false`, `resharper_xmldoc_indent_text = ZeroIndent`, `resharper_csharp_force_indent_style_for_raw_string_literals = LeftAligned`.  (SPEC AC-2 input)
    - D-03: after a successful merge, `.planning/rs-export.editorconfig` no longer exists in the tree (and any leftover `.planning/rs.DotSettings` is deleted); `50-RECONCILIATION.md` is the permanent audit trail listing all 8 source keys + resolutions.
    - `.editorconfig` line endings remain LF (no CRLF introduced anywhere); no unrelated source file changed.  (SPEC AC-5)
    - BLOCKED PATH (fallback only — export missing/empty): `50-RECONCILIATION.md` is explicitly marked `BLOCKED` (not a successful stub), `.editorconfig` is UNCHANGED (`git diff .editorconfig` empty), and the 50-01-SUMMARY status is `BLOCKED` so Plans 02-04 do not run. The reconciliation marker and the SUMMARY status AGREE (both BLOCKED).
  </acceptance_criteria>
  <done>On success: 50-RECONCILIATION.md records all 8 source keys with the determined resolutions (3 ADOPT-RS, 1 REJECT/EOL, 2 KEEP-EXISTING, 4 IGNORE) plus the four-fixture conflict review and the LF-constraint note; `.editorconfig` carries up to 3 additive `resharper_*` keys with all five carve-outs intact and `[*] end_of_line = lf` preserved; and the throwaway export is deleted. On the (unexpected) missing-export fallback: 50-RECONCILIATION.md and the SUMMARY both read BLOCKED, `.editorconfig` is untouched, and the downstream plans are gated off.</done>
</task>

</tasks>

<verification>
- `grep -c` of each carve-out key in `.editorconfig` returns >= 1 (all five survive) — success path.
- `grep -A2 '^\[\*\]$' .editorconfig` shows `end_of_line = lf` — the export crlf was rejected (LF constraint held).
- `git diff .editorconfig` reviewed: at most 3 additive `resharper_*` lines, zero gate-enforceable (`dotnet_*`/`csharp_*`) keys, zero `end_of_line = crlf` under `[*]`, no carve-out weakening; empty on the BLOCKED fallback.
- `50-RECONCILIATION.md` table covers all 8 source keys from the (now-deleted) `.planning/rs-export.editorconfig` and includes the four-fixture conflict review + the line-ending constraint note — OR is explicitly marked BLOCKED (fallback only).
- Working tree has no `.planning/rs-export.editorconfig` (and no leftover `.planning/rs.DotSettings`).
- BLOCKED fallback: the reconciliation marker and the 50-01-SUMMARY status both say BLOCKED (they must agree).
</verification>

<success_criteria>
- A reconciliation report records all 8 exported source keys with KEEP-EXISTING / ADOPT-RS / REJECT(constraint-wins) / IGNORE plus enforceability — OR is an explicit BLOCKED marker matching a BLOCKED SUMMARY only if the export is missing.
- The export's `[*] end_of_line = crlf` is rejected; the committed `[*] end_of_line = lf` is preserved; no `crlf` is introduced under `[*]`.
- ZERO gate-enforceable keys merged — the reconciled `.editorconfig` enforces the SAME style as before; the only additions are up to 3 informational `resharper_*` C# keys.
- The reconciled `.editorconfig` is the single source of truth, carve-outs overriding, with zero mass reflow.
- The transient export is gone; the report is the permanent trail (D-03).
</success_criteria>

<output>
Create `.planning/phases/50-code-style-enforcement/50-01-SUMMARY.md` when done. The SUMMARY MUST: (1) carry an explicit status — `COMPLETE` on the success path, or `BLOCKED` only if the export was missing (in which case it states `.editorconfig` is unchanged and Plans 02-04 must NOT run until reconciliation completes — the SUMMARY status and the 50-RECONCILIATION.md marker MUST agree); (2) on success, list which keys were ADOPT-RS (the up to 3 `resharper_*` keys) and state explicitly that ZERO gate-enforceable (`dotnet_*`/`csharp_*`) keys were added, so Plan 02's gate enforces the same style as the committed config (gate default is FULL); (3) on success, record that the export's `[*] end_of_line = crlf` was REJECTED on the LF constraint and the committed `[*] end_of_line = lf` was preserved; (4) on success, record that the ADOPT-RS list was reviewed against the four CarveOutGuard fixtures (init / raw-string / attribute / switch) with the outcome (none conflicting, since all 3 are JetBrains-only) BEFORE Plan 04's scratch PRs; and (5) on success, confirm all five carve-outs survived and that `.planning/rs-export.editorconfig` was deleted (D-03).
</output>
