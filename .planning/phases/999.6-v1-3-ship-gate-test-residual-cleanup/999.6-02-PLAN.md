---
phase: 999.6-v1-3-ship-gate-test-residual-cleanup
plan: 02
type: execute
wave: 2
depends_on:
  - "999.6-01"
files_modified:
  - .planning/debug/999.6-basicauth-flaky.md
  - DeckFlow.Web.Tests/BasicAuthMiddlewareTests.cs
  # DeckFlow.Web/Infrastructure/BasicAuthMiddleware.cs ONLY if D-15 escalation confirms a real production state-management bug — debug verdict decides.
autonomous: false
requirements:
  - D-09
  - D-10
  - D-11
  - D-15
  - D-17
  - D-18
  - D-19
  - D-20
  - D-21
  - D-22
  - D-03
  - D-14

must_haves:
  truths:
    - "D-17: Strict P01 → P02 → P03 sequential — this plan starts AFTER 999.6-01-SUMMARY.md confirms full-suite Failed 3."
    - "D-18: Atomic commit per cluster — debug-doc artifact commits alongside its fix per project debug convention."
    - "D-19: Plain default-author commits as luntc1972 <luntc1972@yahoo.com>; no Co-Authored-By trailer."
    - "D-20: Build gate exits 0 warnings 0 errors before commit."
    - "D-21: Focused gate `--filter \"FullyQualifiedName~BasicAuthMiddlewareTests\"` AND full-suite gate both report Failed 0; triplet in commit body."
    - "D-22: Claude orchestrates the /gsd-debug investigation (autonomous: false — requires hypothesis triage with human-in-loop sign-off on the verdict before Task 2 dispatches). Codex applies the fix (autonomous: true, cross_ai_command dispatch)."
    - "D-03: No new `[Skip]` directives. No `[Collection]` workaround papering over root cause (explicitly forbidden by D-10)."
    - "D-14 / D-10: Acceptable fix shapes — explicit per-fact env-var reset in Arrange, test fixture redesign, OR middleware change if a real production state-management bug surfaces. NOT acceptable: `[Collection(\"BasicAuthSerial\")]` or `[Fact(Skip=...)]`."
    - "D-11: After fix, run BOTH `--filter \"FullyQualifiedName~BasicAuthMiddlewareTests\"` AND full-suite, 5 consecutive runs each. All 5/5 must show Failed 0 to declare stable."
    - "D-15: If debug surfaces a real production bug in BasicAuthMiddleware, the SUT fix lands inside this plan (no carve-out to 999.7)."
  deliverables:
    - decision: D-09
      where: ".planning/debug/999.6-basicauth-flaky.md"
      change: "/gsd-debug session output testing 4 hypotheses in order: env-var caching in BasicAuthMiddleware, shared static field, IMemoryCache auth-state retention, intra-class fact-order state bleed. Produces a `## Verdict` section with one of: STALE_TEST, FLAKY_DUE_TO_<root_cause>, REAL_PRODUCTION_BUG."
    - decision: D-10
      where: "DeckFlow.Web.Tests/BasicAuthMiddlewareTests.cs (and/or BasicAuthMiddleware.cs if D-15 fires)"
      change: "Fix shape determined by debug verdict — per-fact env reset, fixture redesign, OR middleware change."
    - decision: D-11
      where: "Verification gate"
      change: "5/5 consecutive full-suite runs AND 5/5 consecutive focused runs all report Failed 0 for `CorrectCredentials_InvokesNext`."
  key_links:
    - from: "DeckFlow.Web.Tests/BasicAuthMiddlewareTests.cs `EnvScope.Set/Clear`"
      to: "Process-wide `Environment.SetEnvironmentVariable` for FEEDBACK_ADMIN_USER + FEEDBACK_ADMIN_PASSWORD"
      via: "EnvScope is per-fact disposable; full-suite interleaving with any OTHER test that touches the same env vars (without using EnvScope) would leak state across fact boundaries. Hypothesis priority 1."
    - from: "DeckFlow.Web/Infrastructure/BasicAuthMiddleware.cs"
      to: "Program.cs:120-128 registration"
      via: "If middleware reads env vars once at construction and caches them, the cached-value path is what diverges between isolated vs full-suite runs. Hypothesis priority 2."
---

<objective>
Stabilize the flaky `BasicAuthMiddlewareTests.CorrectCredentials_InvokesNext` Fact. The test passes 5/5 with `--filter "FullyQualifiedName~BasicAuthMiddlewareTests"` but fails in full-suite ordering. State leak between facts — shared mutable static, env-var contamination, IMemoryCache retention, or fact-order coupling.

Investigation MUST precede fix (per D-09 hypothesis-priority order). Claude runs `/gsd-debug 999.6-basicauth-flaky` with hypothesis triage; the verdict commits to `.planning/debug/999.6-basicauth-flaky.md`. Codex then applies the fix per the verdict, in a second task.

Purpose: After this plan lands, full-suite gate moves from `Failed 3` → `Failed 2` (only the 2 Archidekt cache-job failures remain for P03). 5/5 consecutive runs confirm stability per D-11 — no `[Collection]` papering, no `[Skip]` escape hatch.

Output: 1 debug-doc commit + 1 fix commit (atomic per D-18, possibly bundled if the fix is trivially small and the debug verdict + fix can co-commit cleanly).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/999.6-v1-3-ship-gate-test-residual-cleanup/999.6-CONTEXT.md
@.planning/phases/999.6-v1-3-ship-gate-test-residual-cleanup/999.6-01-SUMMARY.md
@.planning/codebase/TESTING.md
@CLAUDE.md

<interfaces>
<!-- Test file's existing EnvScope helper + the 5 facts that share it. -->
<!-- Claude reads the SUT + Program.cs registration block to test hypotheses. -->

From DeckFlow.Web.Tests/BasicAuthMiddlewareTests.cs (existing EnvScope helper, lines 101-135):
```csharp
private sealed class EnvScope : IDisposable
{
    private readonly Dictionary<string, string?> _previous = new();
    private EnvScope(params string[] keys) { /* captures previous values */ }
    public static EnvScope Clear(params string[] keys) { /* sets nulls */ }
    public static EnvScope Set(string k1, string v1, string k2, string v2) { /* sets values */ }
    public void Dispose() { /* restores _previous */ }
}
```

From DeckFlow.Web/Infrastructure/BasicAuthMiddleware.cs (ctor signature read at debug time):
```
DeckFlow.Web/Infrastructure/BasicAuthMiddleware.cs — TO BE READ in Task 1 (do not re-derive from memory).
```

From DeckFlow.Web/Program.cs:120-128 (BasicAuth wiring):
```
DeckFlow.Web/Program.cs — TO BE READ in Task 1 — env-var registration block FEEDBACK_ADMIN_USER + FEEDBACK_ADMIN_PASSWORD.
```
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: /gsd-debug investigation — root-cause BasicAuth state leak (D-09)</name>
  <files>.planning/debug/999.6-basicauth-flaky.md</files>
  <read_first>
    - DeckFlow.Web.Tests/BasicAuthMiddlewareTests.cs (full file — 5 Facts share `EnvScope`; lines 15-99 cover all facts; lines 101-135 are the EnvScope helper)
    - DeckFlow.Web/Infrastructure/BasicAuthMiddleware.cs (entire file — verify whether the middleware reads env vars on every InvokeAsync or caches at construction)
    - DeckFlow.Web/Program.cs (lines 100-150 — BasicAuth registration block; check whether the middleware is registered as a singleton or per-request)
    - DeckFlow.Web.Tests/Security/AdminBruteForceTrackerStoreTests.cs (the sibling test exists per 999.5-01-SUMMARY; may also touch FEEDBACK_ADMIN_* env vars or share state)
    - .planning/phases/999.6-v1-3-ship-gate-test-residual-cleanup/999.6-CONTEXT.md §D-09 (hypothesis priority order)
    - .planning/codebase/TESTING.md §"Process-wide environment isolation via xUnit collections" (existing project convention for env-var coupling)
  </read_first>
  <what-built>
    Claude runs `/gsd-debug 999.6-basicauth-flaky` as the orchestration of this task. The debug session tests four hypotheses in strict priority order (per D-09):

    1. **H1 — Env-var cache in middleware:** Does `BasicAuthMiddleware` read `FEEDBACK_ADMIN_USER` / `FEEDBACK_ADMIN_PASSWORD` on every `InvokeAsync` (fresh-read), or at construction (cached)? If cached, a different test that mutates those env vars BEFORE this test's `EnvScope.Set` runs would corrupt the cache. Verify by reading `BasicAuthMiddleware.cs` line by line.
    2. **H2 — Shared static field:** Does the middleware or test class hold a `static` field that retains state? Particularly look for static caches, throttle counters, or `AdminBruteForceTrackerStore` instance reuse across facts.
    3. **H3 — IMemoryCache / store retention:** Does the `AdminBruteForceTrackerStore` (created per-fact via `CreateStore(out var dbPath)` at test lines 18-22) leak state through the temp-DB path? Note: each `CreateStore` call uses `Guid.NewGuid():N` so the path SHOULD be unique — but is `Dispose` ever called? grep the test file for the unique paths and `File.Delete` cleanup.
    4. **H4 — Fact-order state bleed:** xUnit runs facts in undefined order within a class. Does `WrongCredentials_Returns401` or `MalformedHeader_Returns401` leave behind state (lockout counters in `AdminBruteForceTrackerStore`) that propagates to `CorrectCredentials_InvokesNext`? Recall `BasicAuthMiddleware` integrates brute-force throttling — a previous fact's "wrong creds" attempts may have triggered a lockout that survives into the "correct creds" fact.

    Output the verdict to `.planning/debug/999.6-basicauth-flaky.md` with these sections:
    - `## Hypotheses Tested` — each H1-H4 with PASS/FAIL/INCONCLUSIVE marker.
    - `## Root Cause` — narrative explanation of the leak.
    - `## Verdict` — one of: `STALE_TEST`, `FLAKY_DUE_TO_<root_cause>`, or `REAL_PRODUCTION_BUG`.
    - `## Recommended Fix Shape` — concrete fix proposal (test-side reset, fixture redesign, or middleware change per D-15).
    - `## Forbidden Fix Shapes` — explicit note that `[Collection("BasicAuthSerial")]` and `[Fact(Skip=...)]` are NOT acceptable per D-10.

    THIS IS A BLOCKING CHECKPOINT — human reviews the verdict + fix shape BEFORE Task 2 dispatches. The reviewer (user) signs off on the chosen fix shape, particularly if D-15 fires (real production bug → BasicAuthMiddleware.cs touched).
  </what-built>
  <how-to-verify>
    1. Read `.planning/debug/999.6-basicauth-flaky.md`. Confirm it contains all 5 sections above.
    2. Confirm the `## Verdict` line names exactly one of `STALE_TEST`, `FLAKY_DUE_TO_<root_cause>`, or `REAL_PRODUCTION_BUG`.
    3. Confirm `## Recommended Fix Shape` proposes a concrete change (file path + brief diff sketch) — not "add some env-var reset somewhere".
    4. If verdict is `REAL_PRODUCTION_BUG`, confirm the fix shape edits `DeckFlow.Web/Infrastructure/BasicAuthMiddleware.cs` — and review the proposed SUT change for SOLID compliance (especially SRP — does the middleware do one thing? — and DIP — does it depend on env-var abstraction or `Environment.GetEnvironmentVariable` directly?).
    5. If verdict is `STALE_TEST` or `FLAKY_DUE_TO_*`, confirm fix shape touches ONLY `BasicAuthMiddlewareTests.cs` (test-side fix).
    6. Sign off with "approved" or describe concerns.
  </how-to-verify>
  <resume-signal>Type "approved" or describe required revisions to the debug verdict.</resume-signal>
  <acceptance_criteria>
    - File `.planning/debug/999.6-basicauth-flaky.md` exists.
    - File contains the 5 required sections: Hypotheses Tested, Root Cause, Verdict, Recommended Fix Shape, Forbidden Fix Shapes.
    - Verdict line matches one of the 3 authorized values.
    - User signs off via "approved" before Task 2 may dispatch.
    - Debug doc committed: `debug(999.6): root-cause investigation for BasicAuth flaky fact (D-09)`. Commit body cites the verdict.
    - Commit author plain default per D-19.
  </acceptance_criteria>
  <done>Verdict locked; Task 2 fix shape constrained by it.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Apply fix per debug verdict — Codex dispatch (D-10, D-15 if applicable)</name>
  <files>DeckFlow.Web.Tests/BasicAuthMiddlewareTests.cs (always); DeckFlow.Web/Infrastructure/BasicAuthMiddleware.cs (only if Task 1 verdict = REAL_PRODUCTION_BUG)</files>
  <read_first>
    - .planning/debug/999.6-basicauth-flaky.md (Task 1 output — the `## Recommended Fix Shape` section IS the action specification for this task)
    - DeckFlow.Web.Tests/BasicAuthMiddlewareTests.cs (file being modified — focus on `CorrectCredentials_InvokesNext` at line 84 and the `EnvScope` helper at line 101)
    - If verdict = REAL_PRODUCTION_BUG: DeckFlow.Web/Infrastructure/BasicAuthMiddleware.cs (full file; the SUT change must be minimal and SOLID-compliant)
    - .planning/phases/999.6-v1-3-ship-gate-test-residual-cleanup/999.6-CONTEXT.md §D-10, §D-15
  </read_first>
  <action>
    Apply the fix EXACTLY as specified in the `## Recommended Fix Shape` section of `.planning/debug/999.6-basicauth-flaky.md`. Do NOT deviate from the approved fix shape — if the executor finds a "better" approach mid-flight, STOP and re-loop through Task 1 with a new verdict.

    Three legitimate fix-shape branches (per D-10):
    - **Branch T (Test-side reset):** Add an explicit Arrange step inside `CorrectCredentials_InvokesNext` (and any other affected fact named in the verdict) that resets the leaking state — e.g., explicit `Environment.SetEnvironmentVariable(EnvUser, null); Environment.SetEnvironmentVariable(EnvPass, null);` BEFORE the `EnvScope.Set` call. Or migrate the class to follow the existing project convention `[CollectionDefinition("BasicAuthMiddlewareTests", DisableParallelization = true)]` PAIRED WITH `[Collection(...)]` decoration — but ONLY if the verdict explicitly identifies cross-class env-var parallelism (this IS NOT a "paper over" if the root cause IS process-wide env-var racing across distinct test classes, per TESTING.md §"Process-wide environment isolation via xUnit collections" precedent set by CategoryKnowledgeStoreTests).
    - **Branch F (Fixture redesign):** Rewrite `CreateStore` / `EnvScope` helpers if the leak traces to their shared structure. Preserve all 5 facts' existing semantics.
    - **Branch P (Real production bug):** Edit `DeckFlow.Web/Infrastructure/BasicAuthMiddleware.cs` per D-15. Apply the minimal change that closes the bug — do NOT refactor surrounding code. Must pass SOLID review (especially SRP + DIP). Update test alongside in same commit (cannot land SUT-only or test-only).

    Forbidden across all branches: `[Fact(Skip = ...)]` on any BasicAuth fact (violates D-03); `[Collection("BasicAuthSerial")]` JUST to serialize WITHOUT root-cause naming (violates D-10 "papers over root cause" clause — distinct from the legitimate Branch T env-var-collection precedent above).

    Preserve LF line endings; do not auto-format. Touch only the lines the verdict identifies.
  </action>
  <verify>
    <automated>"/mnt/c/Program Files/dotnet/dotnet.exe" build DeckFlow.sln -c Release -nologo &amp;&amp; for i in 1 2 3 4 5; do "/mnt/c/Program Files/dotnet/dotnet.exe" test DeckFlow.sln -c Release --no-build --nologo --filter "FullyQualifiedName~BasicAuthMiddlewareTests" || exit 1; done &amp;&amp; for i in 1 2 3 4 5; do "/mnt/c/Program Files/dotnet/dotnet.exe" test DeckFlow.sln -c Release --no-build --nologo || exit 1; done</automated>
  </verify>
  <acceptance_criteria>
    - Build gate exits 0 warnings 0 errors.
    - Focused gate `--filter "FullyQualifiedName~BasicAuthMiddlewareTests"` reports `Failed: 0` on EACH of 5 consecutive runs (per D-11).
    - Full-suite gate reports `Failed: 2, Skipped: 3` on EACH of 5 consecutive runs (the 2 remaining failures are P03 ArchidektCacheJob scope; the BasicAuth fact is stable).
    - `grep -c "Skip\s*=" DeckFlow.Web.Tests/BasicAuthMiddlewareTests.cs` returns `0` (no `[Skip]` added — D-03).
    - If Branch P applied: `dotnet build` warning count unchanged from post-Plan-01 baseline.
    - Commit message Branch T: `fix(999.6): reset env-var state per-fact in BasicAuthMiddlewareTests (D-09/D-10)`
    - Commit message Branch F: `refactor(999.6): isolate BasicAuth test fixture from process-wide env-var state (D-09/D-10)`
    - Commit message Branch P: `fix(999.6): correct BasicAuthMiddleware state-management bug surfaced by flaky test (D-09/D-15)`
    - Commit body in every branch carries BOTH triplets: focused-test triplet AND full-suite triplet (per D-11 + D-21).
    - Commit author plain default per D-19.
  </acceptance_criteria>
  <done>BasicAuth flaky fact is stable; 1 of 3 P02+P03 residuals closed; full-suite at `Failed 2`.</done>
</task>

<task type="auto">
  <name>Task 3: Closing plan SUMMARY + carry-forward to P03</name>
  <files>.planning/phases/999.6-v1-3-ship-gate-test-residual-cleanup/999.6-02-SUMMARY.md</files>
  <read_first>
    - .planning/phases/999.6-v1-3-ship-gate-test-residual-cleanup/999.6-02-PLAN.md (must_haves section)
    - .planning/debug/999.6-basicauth-flaky.md (verdict — must be cited in SUMMARY)
    - .planning/phases/999.6-v1-3-ship-gate-test-residual-cleanup/999.6-01-SUMMARY.md (format reference)
  </read_first>
  <action>
    Create `.planning/phases/999.6-v1-3-ship-gate-test-residual-cleanup/999.6-02-SUMMARY.md` documenting: files modified, scope (P02 BasicAuth flaky-fix), decisions delivered (D-09, D-10, D-11 verified via 5/5 runs, D-15 application status — fired or not), branch applied (T/F/P), debug verdict citation, build + 5×focused + 5×full-suite gate results with triplets, residual 2 failures explicitly enumerated (Archidekt cache-job — carried forward to P03), commit hash sequence (debug-doc commit + fix commit).

    Commit the SUMMARY as closing artifact.
  </action>
  <verify>
    <automated>test -f .planning/phases/999.6-v1-3-ship-gate-test-residual-cleanup/999.6-02-SUMMARY.md &amp;&amp; grep -c "Failed 2\|Failed: 2" .planning/phases/999.6-v1-3-ship-gate-test-residual-cleanup/999.6-02-SUMMARY.md</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/999.6-v1-3-ship-gate-test-residual-cleanup/999.6-02-SUMMARY.md` exists.
    - SUMMARY cites debug verdict line verbatim.
    - SUMMARY documents 5×focused + 5×full-suite run results (all 10 reporting Failed 0 focused / Failed 2 full-suite).
    - SUMMARY enumerates the 2 carry-forward residuals: `ArchidektCacheJobServiceTests.BackgroundService_SucceedsAndUpdatesProcessedCounts` and `ArchidektCacheJobServiceTests.GetActiveJob_ReturnsNullAfterCompletedJob`.
    - Commit message: `docs(999.6): summary for plan 02 BasicAuth flaky stabilization`
    - Commit author plain default per D-19.
  </acceptance_criteria>
  <done>P02 closed; full-suite at `Failed 2`; P03 unblocked.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test process → BasicAuthMiddleware production code | Task 2 Branch P potentially modifies the production middleware. State-management bugs in auth code are HIGH severity (information disclosure + authentication bypass risk). |
| repo → public GitHub | `.planning/debug/999.6-basicauth-flaky.md` becomes world-visible; must NOT contain real admin credentials. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-999.6-P02-01 | Spoofing / Elevation of Privilege | BasicAuthMiddleware.cs (Task 2 Branch P only) | mitigate | Real-bug branch only fires under D-15. Fix is MINIMAL — no surrounding refactor. SOLID review (SRP, DIP) required before commit per Task 1 verification step 4. Test gate after fix runs 5×full-suite to confirm no regression in `EnvVarsMissing_Returns503`, `NoAuthHeader_Returns401_WithChallenge`, `MalformedHeader_Returns401`, `WrongCredentials_Returns401`. |
| T-999.6-P02-02 | Information Disclosure | .planning/debug/999.6-basicauth-flaky.md | mitigate | Debug doc may reference env var NAMES (`FEEDBACK_ADMIN_USER`) but MUST NOT contain real env var VALUES from any local `.env` or Render dashboard. Pre-commit grep gate on the debug file for any string longer than 8 chars matching `admin|secret|password|@gmail|@yahoo|Bearer\s+\S+` — manual review escalation if any hit. |
| T-999.6-P02-03 | Denial of Service | xUnit test runner | accept | No new test-parallelism collection added under D-10 ban — flaky test stabilizes without throttling concurrent test execution. |

No npm/pip/cargo installs — Package Legitimacy Gate not applicable. Branch P severity = HIGH (auth code); Branches T/F severity = LOW (test-only changes).
</threat_model>

<verification>
- `.planning/debug/999.6-basicauth-flaky.md` exists with all 5 required sections + verdict (Task 1).
- Task 2 verify command runs 5×focused-filter + 5×full-suite; ALL 10 must exit `Failed: 0` (focused) / `Failed: 2` (full-suite).
- Build gate exits 0 warnings 0 errors after every commit.
- No `[Skip]` directive added to any test in this plan (D-03).
- No `[Collection("BasicAuthSerial")]` decoration UNLESS the verdict explicitly identifies process-wide env-var racing across distinct test classes (D-10 + TESTING.md precedent).
- Branch P only: `dotnet build` warning count unchanged from post-Plan-01 baseline; no new SOLID violations.
- All commits authored by `luntc1972 <luntc1972@yahoo.com>` (D-19).
- Commit bodies carry the focused-test triplet AND (for Task 2) the full-suite triplet (D-11 + D-21).
</verification>

<success_criteria>
1. `CorrectCredentials_InvokesNext` passes 5/5 in focused isolation AND 5/5 in full-suite ordering.
2. Pre-plan failure count `3` reduced to `2` after Task 3 closing gate.
3. Skipped count stays at `3` (D-03 — no new `[Skip]`).
4. If D-15 fired (Branch P), the production fix is in `DeckFlow.Web/Infrastructure/BasicAuthMiddleware.cs` ONLY (no scope creep into adjacent files).
5. Debug-doc artifact (`.planning/debug/999.6-basicauth-flaky.md`) committed alongside fix per D-18.
6. Carry-forward residuals (2 Archidekt cache-job failures) explicitly named in SUMMARY for P03.
</success_criteria>

<output>
Create `.planning/phases/999.6-v1-3-ship-gate-test-residual-cleanup/999.6-02-SUMMARY.md` when done (Task 3).
</output>
