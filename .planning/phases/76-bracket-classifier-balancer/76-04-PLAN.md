---
phase: 76-bracket-classifier-balancer
plan: 04
type: execute
wave: 3
depends_on: ["76-01", "76-02", "76-03"]
files_modified:
  - DeckFlow.Web/Services/Bracket/IBracketClassificationService.cs
  - DeckFlow.Web/Services/Bracket/BracketClassificationService.cs
  - DeckFlow.Web/Program.cs
  - DeckFlow.Web.Tests/Bracket/BracketClassificationServiceTests.cs
autonomous: true
requirements: [BRACKET-01, BRACKET-03]
must_haves:
  truths:
    - "BracketClassificationService loads a deck from a URL or pasted text, fetches combos via ICommanderSpellbookService, classifies via BracketClassifier, and builds the paste artifact via BracketPromptVariantRegistry"
    - "A null ICommanderSpellbookService result yields ComboDetectionAvailable=false (combo input passed to the classifier is null, never an empty list misread as zero combos)"
    - "SpellbookCombo with exactly two card names is mapped to a Core TwoCardCombo; combos with 3+ cards are excluded from the two-card gate"
    - "The artifact is built for the requested AI platform and included in the result only when classification succeeds"
  artifacts:
    - path: "DeckFlow.Web/Services/Bracket/BracketClassificationService.cs"
      provides: "Orchestration: load -> combos -> classify -> artifact"
      contains: "BracketClassifier.Classify"
    - path: "DeckFlow.Web.Tests/Bracket/BracketClassificationServiceTests.cs"
      provides: "Service tests incl. null-spellbook disclosure + 2-card mapping"
      contains: "BracketClassificationServiceTests"
  key_links:
    - from: "DeckFlow.Web/Services/Bracket/BracketClassificationService.cs"
      to: "ICommanderSpellbookService.FindCombosAsync"
      via: "null result -> null TwoCardCombo input (unavailable)"
      pattern: "FindCombosAsync"
    - from: "DeckFlow.Web/Services/Bracket/BracketClassificationService.cs"
      to: "BracketPromptVariantRegistry.Build"
      via: "platform-routed artifact build"
      pattern: "BracketPromptVariantRegistry"
---

<objective>
Author the orchestrating `BracketClassificationService` that turns a deck source into a full bracket result: load the deck (existing `IDeckEntryLoader`), detect two-card combos (existing `ICommanderSpellbookService`, null = unavailable), classify (`BracketClassifier` from 76-01), and build the platform paste artifact (`BracketPromptVariantRegistry` from 76-03). This is the seam the controller (76-05) calls.

Purpose: BRACKET-01 end-to-end classification over a real deck; BRACKET-03 combo-null disclosure must survive the mapping from Web's SpellbookCombo to the Core TwoCardCombo input.
Output: interface + service + DI + service tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/76-bracket-classifier-balancer/76-RESEARCH.md
@.planning/phases/76-bracket-classifier-balancer/76-PATTERNS.md
@.planning/phases/76-bracket-classifier-balancer/76-01-SUMMARY.md
@.planning/phases/76-bracket-classifier-balancer/76-02-SUMMARY.md
@.planning/phases/76-bracket-classifier-balancer/76-03-SUMMARY.md

<interfaces>
IDeckEntryLoader (DeckFlow.Core/Loading/DeckEntryLoader.cs):
  Task<DeckSourceLoadResult> LoadFromSourceAsync(string deckSource, ..., CancellationToken ct = default);
  DeckSourceLoadResult(List<DeckEntry> Entries, string? FallbackNotice, string? DetectedCompanionName = null);
  // URL/host validation (SSRF) already enforced inside the loader via DeckFlow.Core/Integration/DeckSourceHost.cs.

ICommanderSpellbookService (DeckFlow.Web/Services/CommanderSpellbookService.cs):
  Task<CommanderSpellbookResult?> FindCombosAsync(IReadOnlyList<DeckEntry> entries, CancellationToken ct = default);
  CommanderSpellbookResult(IReadOnlyList<SpellbookCombo> IncludedCombos, IReadOnlyList<SpellbookAlmostCombo> AlmostIncludedCombos);
  SpellbookCombo(IReadOnlyList<string> CardNames, IReadOnlyList<string> Results, string Instructions, int? Popularity=null, int? ManaValueNeeded=null);
  // null result == UNAVAILABLE (HTTP/parse failure or no main combos), NEVER "zero combos".

From 76-01: BracketClassifier.Classify(IReadOnlyList<DeckEntry>, GameChangerCatalog, IReadOnlyList<TwoCardCombo>?); TwoCardCombo(IReadOnlyList<string> CardNames, IReadOnlyList<string> Results).
From 76-02: IGameChangerCatalogService.GetCatalog() -> GameChangerCatalog.
From 76-03: BracketPromptVariantRegistry.Build(AiPlatform, BracketClassification, int? target, string? deckName, IReadOnlyList<BracketTier> tiers, GameChangerCatalog, CancellationToken).
AiPlatform.Normalize(string?) — AiPlatform is a record (not enum).
MaxDeckSourceChars guard: ManabaseAnalysisService uses 100_000 — mirror it.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: IBracketClassificationService + BracketClassificationService + result record + DI</name>
  <read_first>
    - DeckFlow.Web/Services/Manabase/ManabaseAnalysisService.cs (analog: orchestrating service, IDeckEntryLoader.LoadFromSourceAsync usage, MaxDeckSourceChars guard, InvalidOperationException on empty source, internal test-seam ctor)
    - DeckFlow.Web/Services/CommanderSpellbookService.cs (FindCombosAsync null semantics; SpellbookCombo shape)
    - DeckFlow.Web/Program.cs (AddScoped service registration block)
    - .planning/phases/76-bracket-classifier-balancer/76-PATTERNS.md (BracketClassificationService section — combo null handling, ArgumentNull guards, NullLogger fallback)
  </read_first>
  <files>DeckFlow.Web/Services/Bracket/IBracketClassificationService.cs, DeckFlow.Web/Services/Bracket/BracketClassificationService.cs, DeckFlow.Web/Program.cs</files>
  <action>
    Create namespace `DeckFlow.Web.Services.Bracket`. Define result record `public sealed record BracketClassificationResult(BracketClassification Classification, IReadOnlyList<BracketTier> Tiers, string? PromptArtifact, int? TargetBracketNumber, string? ImportWarning);`. Define `IBracketClassificationService` with `Task<BracketClassificationResult> ClassifyAsync(string deckSource, int? targetBracketNumber, string platform, string? deckName, CancellationToken cancellationToken = default);`. Implement `BracketClassificationService` (`sealed`, `AddScoped`): inject `IDeckEntryLoader`, `ICommanderSpellbookService`, `IGameChangerCatalogService`, `BracketPromptVariantRegistry`, and optional `ILogger<BracketClassificationService>` (NullLogger fallback). `ArgumentNullException.ThrowIfNull` each. In ClassifyAsync: guard `string.IsNullOrWhiteSpace(deckSource)` -> throw `InvalidOperationException` with a user-facing "Enter a deck URL or paste a deck list." message; guard length > 100_000 -> InvalidOperationException. Call `LoadFromSourceAsync(deckSource, ct)`; if Entries empty -> InvalidOperationException "That deck looks empty." Call `FindCombosAsync(entries, ct)`; map: `IReadOnlyList<TwoCardCombo>? twoCardCombos = comboResult is null ? null : comboResult.IncludedCombos.Where(c => c.CardNames.Count == 2).Select(c => new TwoCardCombo(c.CardNames, c.Results)).ToList();` (null preserved = unavailable). Get catalog via `_catalogService.GetCatalog()`. `var classification = BracketClassifier.Classify(entries, catalog, twoCardCombos);`. Build artifact: `var artifact = _registry.Build(AiPlatform.Normalize(platform), classification, targetBracketNumber, deckName, catalog.Tiers, catalog, ct);`. Return BracketClassificationResult(classification, catalog.Tiers, artifact, targetBracketNumber, load.FallbackNotice). Register `builder.Services.AddScoped<IBracketClassificationService, BracketClassificationService>();` in Program.cs. Use structured logging (no interpolation). Let HttpRequestException/OperationCanceledException propagate to the controller (76-05) for its guarded catch.
  </action>
  <verify>
    <automated>"/mnt/c/Program Files/dotnet/dotnet.exe" build DeckFlow.Web/DeckFlow.Web.csproj -c Debug</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: BracketClassificationService maps only `CardNames.Count == 2` combos and uses `comboResult is null ? null : ...` (combo-null preserved). `grep -c "Count == 2" DeckFlow.Web/Services/Bracket/BracketClassificationService.cs` returns 1.
    - Source assertion: `grep -c "AddScoped<IBracketClassificationService" DeckFlow.Web/Program.cs` returns 1.
    - Build: DeckFlow.Web compiles 0 errors / 0 new warnings.
  </acceptance_criteria>
  <done>Orchestration service compiles, is DI-registered, maps 2-card combos and preserves combo-null as unavailable.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: BracketClassificationService tests (combo-null disclosure, 2-card mapping, artifact build)</name>
  <read_first>
    - DeckFlow.Web.Tests/Manabase/ (any ManabaseAnalysisService test using fake loader/spellbook seams) for the test-double wiring pattern
    - DeckFlow.Web/Services/Bracket/BracketClassificationService.cs (the service under test, from Task 1)
    - DeckFlow.Web.Tests/TestDoubles/ (existing fakes — reuse FakeHttpClientFactory-style seams; check for an existing spellbook fake/stub)
  </read_first>
  <files>DeckFlow.Web.Tests/Bracket/BracketClassificationServiceTests.cs</files>
  <action>
    Create `BracketClassificationServiceTests`. Build the service with test doubles: a fake `IDeckEntryLoader` returning a fixed `DeckSourceLoadResult` (a deck containing e.g. "Rhystic Study","Cyclonic Rift","Mana Vault","Smothering Tithe","Armageddon"), a fake `ICommanderSpellbookService` whose `FindCombosAsync` is parameterized (returns null, or a CommanderSpellbookResult with a 2-card IncludedCombo, or one with a 3-card combo), a real `GameChangerCatalogService` pointed at the repo `DeckFlow.Web/Data/bracket-data.json` (or an in-memory IGameChangerCatalogService fake returning a hand-built catalog), and a real `BracketPromptVariantRegistry` with the three variants. Tests: (1) `ClassifyAsync_NullSpellbook_SetsComboDetectionAvailableFalse` — combo service returns null -> result.Classification.ComboDetectionAvailable is false and artifact contains the combo-unavailable disclosure. (2) `ClassifyAsync_TwoCardCombo_GatesB4` — 2-card combo -> BracketNumber >= 4. (3) `ClassifyAsync_ThreeCardCombo_NotCountedAsTwoCardGate` — only a 3-card combo + 0 GC + no MLD -> NOT gated to B4 by the combo (combo excluded). (4) `ClassifyAsync_EmptySource_Throws` — whitespace deckSource -> InvalidOperationException. (5) `ClassifyAsync_BuildsArtifactForPlatform` — platform "Claude" -> artifact non-empty and contains "WHY THIS BRACKET".
  </action>
  <verify>
    <automated>"/mnt/c/Program Files/dotnet/dotnet.exe" test DeckFlow.Web.Tests/DeckFlow.Web.Tests.csproj --filter "FullyQualifiedName~BracketClassificationServiceTests"</automated>
  </verify>
  <acceptance_criteria>
    - Test command: all five service tests pass.
    - Behavior assertion: null-spellbook test proves ComboDetectionAvailable=false AND the artifact discloses unavailability (BRACKET-03 / Pitfall 1).
    - Behavior assertion: 3-card-combo test proves a 3-card combo does not trigger the two-card B4 gate.
  </acceptance_criteria>
  <done>Service tests green, proving combo-null disclosure, 2-card-only gating, empty-source guard, and platform artifact build.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client deckSource (URL or pasted text) → loader | Untrusted input; URL imports cross to Archidekt/Moxfield |
| Commander Spellbook API response → service | Untrusted upstream JSON |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-76-08 | Tampering (SSRF) | deck URL import in ClassifyAsync | mitigate | Reuse IDeckEntryLoader / DeckSourceHost host allowlist (Phase 64) — no new HTTP client, no raw URL fetch in this service. Same surface as ManabaseAnalysisService. |
| T-76-09 | Denial of Service | oversized pasted deck | mitigate | MaxDeckSourceChars 100_000 guard before load (mirrors ManabaseAnalysisService). |
| T-76-10 | Tampering | malicious Spellbook JSON | mitigate | Parsing/defensiveness already in CommanderSpellbookService (try/catch, null-return); service treats null as unavailable, not zero. |
| T-76-SC | Tampering | npm/pip/cargo installs | accept | No package installs; in-solution only. |
</threat_model>

<verification>
- `dotnet build DeckFlow.Web/DeckFlow.Web.csproj` clean.
- `dotnet test --filter "FullyQualifiedName~BracketClassificationServiceTests"` green.
- No `new HttpClient()` introduced (egress only via existing services).
</verification>

<success_criteria>
- BRACKET-01: full deck -> bracket classification path works end-to-end over a loaded deck.
- BRACKET-03: combo-null surfaces as "unavailable" (disclosed in artifact), 2-card-only gating, oversized/empty source guarded.
</success_criteria>

<output>
Create `.planning/phases/76-bracket-classifier-balancer/76-04-SUMMARY.md` when done.
</output>
