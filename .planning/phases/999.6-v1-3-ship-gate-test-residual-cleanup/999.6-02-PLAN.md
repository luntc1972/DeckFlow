---
phase: 999.6-v1-3-ship-gate-test-residual-cleanup
plan: 02
type: execute
wave: 2
depends_on:
  - "999.6-01"
files_modified:
  - .planning/debug/999.6-basicauth-flaky.md
  - DeckFlow.Web.Tests/BasicAuthMiddlewareTests.cs
  - DeckFlow.Web.Tests/Security/AdminBruteForceTrackerStoreTests.cs
  - DeckFlow.Web.Tests/Infrastructure/EnvScope.cs
  - DeckFlow.Web.Tests/Infrastructure/AdminEnvCollection.cs
  # DeckFlow.Web/Infrastructure/BasicAuthMiddleware.cs ONLY if D-15 escalation confirms a real production state-management bug — debug verdict decides.
autonomous: false
requirements:
  - D-03
  - D-09
  - D-10
  - D-11
  - D-14
  - D-15
  - D-17
  - D-18
  - D-19
  - D-20
  - D-21
  - D-22

must_haves:
  truths:
    - "D-17: Strict P01 → P02 → P03 sequential — this plan starts AFTER 999.6-01-SUMMARY.md confirms full-suite `Failed: 3` parsed from stdout."
    - "D-18: Atomic commits per cluster — debug-doc artifact commits alongside its fix per project debug convention."
    - "D-19: Plain default-author commits as luntc1972 <luntc1972@yahoo.com>; no Co-Authored-By trailer."
    - "D-20: Build gate exits 0 warnings 0 errors before commit."
    - "D-21: Focused gate `--filter \"FullyQualifiedName~BasicAuthMiddlewareTests\"` AND full-suite gate. Full-suite gate parses `Failed: 2, .* Skipped: 3` from captured stdout (NOT exit-code gated, per Codex HIGH #5). Triplet in commit body."
    - "D-22: Claude orchestrates the /gsd-debug investigation (autonomous: false — requires hypothesis triage with human-in-loop sign-off on the verdict before Task 2 dispatches). Codex applies the fix (autonomous: true, cross_ai_command dispatch)."
    - "D-03: No new `[Skip]` directives. No `[Collection]` workaround papering over root cause UNLESS the debug verdict explicitly identifies process-wide env-var racing across distinct test classes (legitimate xUnit serialization pattern, NOT a paper-over)."
    - "D-14 / D-10: Acceptable fix shapes — extract shared EnvScope helper + `[CollectionDefinition(DisableParallelization=true)]` covering both env-mutating classes (F-ENV-COLLECTION), explicit per-fact env-var clear in Arrange (F-ENV-CLEAR-EAGER), OR middleware change if a real production state-management bug surfaces (F-MIDDLEWARE-IDEMPOTENT). NOT acceptable: `[Collection]` decoration WITHOUT root-cause naming (paper-over) or `[Fact(Skip=...)]`."
    - "D-11: After fix, run BOTH `--filter \"FullyQualifiedName~BasicAuthMiddlewareTests\"` AND full-suite, 5 consecutive runs each. All 10 parse expected residuals from captured stdout (focused `Failed: 0`, full-suite `Failed: 2, .* Skipped: 3`) to declare stable."
    - "D-15: If debug surfaces a real production bug in BasicAuthMiddleware, the SUT fix lands inside this plan (no carve-out to 999.7)."
    - "Codex HIGH #4: P02 files_modified now includes `AdminBruteForceTrackerStoreTests.cs` because it ALSO mutates `FEEDBACK_ADMIN_USER` / `FEEDBACK_ADMIN_PASSWORD` via its own private EnvScope. Orchestrator pre-confirmed via grep (lines 96-97, 125-126 of that file)."
    - "Codex HIGH #5: Full-suite verification parses `Failed: 2, .* Skipped: 3` from captured stdout — `dotnet test` exits non-zero by design while P03 residuals remain; exit-code gating would fail every time."
  deliverables:
    - decision: D-09
      where: ".planning/debug/999.6-basicauth-flaky.md"
      change: "/gsd-debug session output. Pre-verified hypothesis: cross-class env-var race between BasicAuthMiddlewareTests (5 facts using inner EnvScope helper, lines 86,42,57,71,86) and AdminBruteForceTrackerStoreTests (2 facts using DIFFERENT inner EnvScope helper, lines 96-97 + 125-126) — both mutate the SAME process-wide env vars FEEDBACK_ADMIN_USER / FEEDBACK_ADMIN_PASSWORD. xUnit runs test classes in parallel collections by default → race. Debug session re-confirms this hypothesis with H1 PASS evidence, tests H2-H4 as fallbacks, produces ## Verdict + ## Recommended Fix Shape sections."
    - decision: D-10
      where: "Files determined by branch (F-ENV-COLLECTION most likely)"
      change: "Branch F-ENV-COLLECTION (likely): extract shared `DeckFlow.Web.Tests/Infrastructure/EnvScope.cs` helper + add `DeckFlow.Web.Tests/Infrastructure/AdminEnvCollection.cs` with `[CollectionDefinition(\"AdminEnvSerial\", DisableParallelization=true)]` + apply `[Collection(\"AdminEnvSerial\")]` to BOTH `BasicAuthMiddlewareTests` and `AdminBruteForceTrackerStoreTests`. Delete the two private inner `EnvScope` classes. Branch F-ENV-CLEAR-EAGER: each fact explicitly clears env vars at Arrange start (defensive, less robust). Branch F-MIDDLEWARE-IDEMPOTENT: only if a real BasicAuthMiddleware state-caching bug surfaces (escalate per D-15)."
    - decision: D-11
      where: "Verification gate"
      change: "5/5 consecutive focused-filter runs + 5/5 consecutive full-suite runs. All 10 captured logs parse expected residuals — focused `Failed: 0`, full-suite `Failed: 2, .* Skipped: 3`. NOT exit-code gated."
  key_links:
    - from: "DeckFlow.Web.Tests/BasicAuthMiddlewareTests.cs inner EnvScope.Set/Clear (lines 101-135)"
      to: "Process-wide `Environment.SetEnvironmentVariable` for FEEDBACK_ADMIN_USER + FEEDBACK_ADMIN_PASSWORD"
      via: "EnvScope is per-fact disposable, restores _previous on Dispose. Safe in isolation."
    - from: "DeckFlow.Web.Tests/Security/AdminBruteForceTrackerStoreTests.cs DIFFERENT inner EnvScope.Set (lines 158-170)"
      to: "Process-wide `Environment.SetEnvironmentVariable` for FEEDBACK_ADMIN_USER + FEEDBACK_ADMIN_PASSWORD (lines 96-97, 125-126)"
      via: "Distinct private EnvScope class, same env vars. xUnit's default parallel-by-collection test runner can run these two classes' facts simultaneously → cross-class race on the same process-global env vars → `CorrectCredentials_InvokesNext` sees a stale value at the moment of BasicAuthMiddleware.InvokeAsync."
    - from: "DeckFlow.Web/Infrastructure/BasicAuthMiddleware.cs"
      to: "Program.cs:120-128 registration"
      via: "Hypothesis priority 2: if middleware caches env vars at construction, the cache becomes corrupt under cross-class racing. Verified at debug time."
---

<objective>
Stabilize the flaky `BasicAuthMiddlewareTests.CorrectCredentials_InvokesNext` Fact. The test passes 5/5 with `--filter "FullyQualifiedName~BasicAuthMiddlewareTests"` but fails in full-suite ordering.

Orchestrator pre-verified the root cause: `AdminBruteForceTrackerStoreTests` (in `DeckFlow.Web.Tests/Security/`) also mutates `FEEDBACK_ADMIN_USER` + `FEEDBACK_ADMIN_PASSWORD` via its OWN private `EnvScope` helper (lines 96-97 + 125-126 of that file). xUnit runs the two test classes in parallel collections by default, so the env-var mutations race process-wide. Codex HIGH #4 made this the canonical hypothesis; debug session confirms with empirical evidence (timestamped fail/pass cross-correlation) before fix dispatch.

Investigation MUST precede fix (per D-09). Claude runs `/gsd-debug 999.6-basicauth-flaky` with hypothesis triage; the verdict commits to `.planning/debug/999.6-basicauth-flaky.md`. Codex then applies the fix per the verdict, in a second task.

Purpose: After this plan lands, full-suite stdout parses as `Failed: 2, .* Skipped: 3` (only the 2 Archidekt cache-job failures remain for P03). 5/5 consecutive runs confirm stability per D-11 — neither a `[Collection]`-paper-over (forbidden by D-10) nor a `[Skip]` escape hatch.

**Verification correction (Codex HIGH #5):** All gates parse expected residuals from captured stdout. The full-suite `dotnet test` command exits non-zero while P03 residuals remain (Failed: 2), so exit-code gating would fail every run. Replace `|| exit 1` patterns with `tee /tmp/...log; grep -q "Failed: 2, .* Skipped: 3" /tmp/...log`.

Output: 1 debug-doc commit + 1 fix commit (atomic per D-18, possibly bundled if the debug verdict + fix can co-commit cleanly).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/999.6-v1-3-ship-gate-test-residual-cleanup/999.6-CONTEXT.md
@.planning/phases/999.6-v1-3-ship-gate-test-residual-cleanup/999.6-01-SUMMARY.md
@.planning/phases/999.6-v1-3-ship-gate-test-residual-cleanup/999.6-REVIEWS.md
@.planning/codebase/TESTING.md
@CLAUDE.md

<interfaces>
<!-- The two distinct private EnvScope classes that share global env-var state. -->
<!-- Pre-grep confirmed: both touch the SAME FEEDBACK_ADMIN_USER + FEEDBACK_ADMIN_PASSWORD process vars. -->

From DeckFlow.Web.Tests/BasicAuthMiddlewareTests.cs (lines 101-135 — inner EnvScope #1):
  private sealed class EnvScope : IDisposable
  {
      private readonly Dictionary<string, string?> _previous = new();
      private EnvScope(params string[] keys) { /* captures previous values */ }
      public static EnvScope Clear(params string[] keys) { /* sets nulls */ }
      public static EnvScope Set(string k1, string v1, string k2, string v2) { /* sets values */ }
      public void Dispose() { /* restores _previous */ }
  }

From DeckFlow.Web.Tests/Security/AdminBruteForceTrackerStoreTests.cs (lines 158-170 — inner EnvScope #2, structurally distinct):
  private sealed class EnvScope : IDisposable
  {
      private readonly string _name;
      private readonly string? _previous;
      private EnvScope(string name) { _name = name; _previous = Environment.GetEnvironmentVariable(name); }
      public static EnvScope Set(string name, string value) { /* one var at a time */ }
      public void Dispose() => Environment.SetEnvironmentVariable(_name, _previous);
  }

Env-var mutation call sites in AdminBruteForceTrackerStoreTests.cs (pre-grep confirmed):
  Line 96:  using var _envUser = EnvScope.Set("FEEDBACK_ADMIN_USER", "admin");
  Line 97:  using var _envPass = EnvScope.Set("FEEDBACK_ADMIN_PASSWORD", "secret");
  Line 125: using var _envUser = EnvScope.Set("FEEDBACK_ADMIN_USER", "admin");
  Line 126: using var _envPass = EnvScope.Set("FEEDBACK_ADMIN_PASSWORD", "secret");

From DeckFlow.Web/Infrastructure/BasicAuthMiddleware.cs (read at debug time, not pre-loaded):
  TO BE READ in Task 1 — verify whether the middleware reads env vars on every InvokeAsync (fresh-read, race-safe) or caches at construction (race-vulnerable).

From DeckFlow.Web/Program.cs:120-128 (BasicAuth wiring — read at debug time):
  Middleware registration block; relevant to whether construction-time env-var snapshot is shared across requests.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: /gsd-debug investigation — confirm cross-class env-var race + decide branch (D-09)</name>
  <files>.planning/debug/999.6-basicauth-flaky.md</files>
  <read_first>
    - DeckFlow.Web.Tests/BasicAuthMiddlewareTests.cs (full file — 5 Facts share inner EnvScope; lines 15-99 cover all facts; lines 101-135 are the EnvScope helper)
    - DeckFlow.Web.Tests/Security/AdminBruteForceTrackerStoreTests.cs (full file — 2 Facts using DISTINCT inner EnvScope at lines 158-170 mutate the SAME env vars at lines 96-97, 125-126)
    - DeckFlow.Web/Infrastructure/BasicAuthMiddleware.cs (entire file — verify whether the middleware reads env vars on every InvokeAsync or caches at construction)
    - DeckFlow.Web/Program.cs (lines 100-150 — BasicAuth registration block; check whether the middleware is registered as a singleton or per-request)
    - .planning/phases/999.6-v1-3-ship-gate-test-residual-cleanup/999.6-CONTEXT.md §D-09 (hypothesis priority order)
    - .planning/phases/999.6-v1-3-ship-gate-test-residual-cleanup/999.6-REVIEWS.md (Codex HIGH #4 + #5 — the pre-confirmed cross-class hypothesis)
    - .planning/codebase/TESTING.md §"Process-wide environment isolation via xUnit collections" (existing project convention for env-var coupling — legitimate Collection pattern, NOT a paper-over)
  </read_first>
  <what-built>
    Claude runs `/gsd-debug 999.6-basicauth-flaky` as the orchestration of this task. Hypothesis ordering is re-prioritized per orchestrator's pre-confirmed grep evidence (Codex HIGH #4) — H1 is now the primary candidate, with H2-H4 as fallbacks if H1 is ruled out.

    1. **H1 — Cross-class env-var race (PRE-CONFIRMED by orchestrator grep; debug session re-validates):**
       - `BasicAuthMiddlewareTests` uses inner `EnvScope` (lines 101-135) to set/clear `FEEDBACK_ADMIN_USER` + `FEEDBACK_ADMIN_PASSWORD`.
       - `AdminBruteForceTrackerStoreTests` uses a DIFFERENT inner `EnvScope` (lines 158-170) to set the SAME env vars (lines 96-97 + 125-126).
       - xUnit default test-class parallelism runs the two classes simultaneously, racing on the same process-wide env vars.
       - Empirical validation: confirm by running the two test classes' facts in interleaved order via xUnit `[Collection]` ordering experiments, OR by timestamped logging of env-var reads inside `BasicAuthMiddleware.InvokeAsync` matched against `EnvScope.Set/Dispose` calls in both classes.
       - Expected outcome: H1 PASS.

    2. **H2 — Env-var cache in middleware (fallback):** Does `BasicAuthMiddleware` read env vars on every `InvokeAsync` (fresh-read) or at construction (cached)? If cached AND middleware is registered as singleton, the cache becomes corrupt under cross-class racing — but the H1 fix (`[Collection]` serialization) likely also closes H2's symptom. Document the middleware's actual read pattern in the debug doc regardless.

    3. **H3 — IMemoryCache / store retention:** Does `AdminBruteForceTrackerStore` (created per-fact via `CreateStore(out var dbPath)` at test lines 18-22 of BasicAuthMiddlewareTests) leak state through the temp-DB path? Note: each `CreateStore` uses `Guid.NewGuid():N` so paths SHOULD be unique. Verify Dispose is invoked and the file is cleaned. (Memory observation 770 already noted 1 inline finally uncovered in 999.5 — re-check.)

    4. **H4 — Intra-class fact-order state bleed:** xUnit runs facts within a class in undefined order. Does `WrongCredentials_Returns401` leave behind brute-force lockout state in `AdminBruteForceTrackerStore` that propagates to `CorrectCredentials_InvokesNext`? Recall the middleware integrates brute-force throttling.

    Output the verdict to `.planning/debug/999.6-basicauth-flaky.md` with these sections:
    - `## Hypotheses Tested` — H1-H4 each with PASS/FAIL/INCONCLUSIVE marker + supporting evidence.
    - `## Root Cause` — narrative explanation of the leak.
    - `## Verdict` — one of: `STALE_TEST`, `FLAKY_DUE_TO_CROSS_CLASS_ENV_VAR_RACE` (most likely per Codex HIGH #4), `FLAKY_DUE_TO_<other_root_cause>`, or `REAL_PRODUCTION_BUG`.
    - `## Recommended Fix Shape` — name the branch (`F-ENV-COLLECTION` / `F-ENV-CLEAR-EAGER` / `F-MIDDLEWARE-IDEMPOTENT`) + concrete diff sketch (file paths + 5-15 line preview).
    - `## Cross-Class Files Touched` — explicit list. If `F-ENV-COLLECTION`: `DeckFlow.Web.Tests/Infrastructure/EnvScope.cs` (new shared helper), `DeckFlow.Web.Tests/Infrastructure/AdminEnvCollection.cs` (new `[CollectionDefinition(\"AdminEnvSerial\", DisableParallelization=true)]`), `BasicAuthMiddlewareTests.cs` (add `[Collection(\"AdminEnvSerial\")]`, delete inner EnvScope, import shared), `Security/AdminBruteForceTrackerStoreTests.cs` (same).
    - `## Forbidden Fix Shapes` — explicit note that `[Collection]` WITHOUT a CollectionDefinition disabling parallelization (i.e., grouping without serializing) AND `[Fact(Skip=...)]` are NOT acceptable per D-10. **Note exemption (D-14 nuance):** A `[CollectionDefinition(DisableParallelization=true)]` covering ONLY the env-mutating test classes IS legitimate when root cause IS process-wide env-var racing across distinct test classes. The project already uses this pattern per `.planning/codebase/TESTING.md §"Process-wide environment isolation via xUnit collections"`. Distinguish from a paper-over: legitimate Collection serialization names the root cause (env-var race) and only covers env-mutating classes; paper-over Collection silences a race without identifying it.

    THIS IS A BLOCKING CHECKPOINT — human reviews the verdict + fix shape BEFORE Task 2 dispatches. Reviewer (user) signs off on the chosen fix shape, particularly if `F-MIDDLEWARE-IDEMPOTENT` fires (real production bug → `BasicAuthMiddleware.cs` touched; SOLID review required).
  </what-built>
  <how-to-verify>
    1. Read `.planning/debug/999.6-basicauth-flaky.md`. Confirm all 6 required sections present (added `## Cross-Class Files Touched` per Codex HIGH #4).
    2. Confirm the `## Verdict` line names exactly one of the authorized values.
    3. Confirm `## Recommended Fix Shape` names exactly one branch (`F-ENV-COLLECTION` / `F-ENV-CLEAR-EAGER` / `F-MIDDLEWARE-IDEMPOTENT`) and includes a concrete diff sketch (file path + 5-15 line change preview).
    4. Confirm `## Cross-Class Files Touched` lists ALL files affected — particularly that `AdminBruteForceTrackerStoreTests.cs` appears in the list if `F-ENV-COLLECTION` is chosen (Codex HIGH #4 required).
    5. If verdict is `REAL_PRODUCTION_BUG` (`F-MIDDLEWARE-IDEMPOTENT`), confirm the fix shape edits `DeckFlow.Web/Infrastructure/BasicAuthMiddleware.cs` — and review the proposed SUT change for SOLID compliance (especially SRP — does the middleware do one thing? — and DIP — does it depend on env-var abstraction or `Environment.GetEnvironmentVariable` directly?).
    6. Verify `## Forbidden Fix Shapes` correctly distinguishes legitimate `[CollectionDefinition(DisableParallelization=true)]` (allowed when root cause is named) from paper-over `[Collection]` decoration (forbidden).
    7. Sign off with "approved" or describe concerns.
  </how-to-verify>
  <resume-signal>Type "approved" or describe required revisions to the debug verdict.</resume-signal>
  <acceptance_criteria>
    - File `.planning/debug/999.6-basicauth-flaky.md` exists.
    - File contains 6 required sections: Hypotheses Tested, Root Cause, Verdict, Recommended Fix Shape, Cross-Class Files Touched, Forbidden Fix Shapes.
    - Verdict line matches one of the authorized values.
    - User signs off via "approved" before Task 2 may dispatch.
    - Debug doc committed: `debug(999.6): root-cause investigation for BasicAuth flaky fact (D-09)`. Commit body cites the verdict + branch.
    - Commit author plain default per D-19.
  </acceptance_criteria>
  <done>Verdict locked; Task 2 fix shape constrained by it (Cross-Class Files Touched section is the authoritative file list).</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Apply fix per debug verdict — Codex dispatch (D-10, D-15 if applicable)</name>
  <files>Files determined by Task 1 ## Cross-Class Files Touched section. Default for branch F-ENV-COLLECTION: DeckFlow.Web.Tests/Infrastructure/EnvScope.cs (new), DeckFlow.Web.Tests/Infrastructure/AdminEnvCollection.cs (new), DeckFlow.Web.Tests/BasicAuthMiddlewareTests.cs (modified), DeckFlow.Web.Tests/Security/AdminBruteForceTrackerStoreTests.cs (modified). For branch F-MIDDLEWARE-IDEMPOTENT: add DeckFlow.Web/Infrastructure/BasicAuthMiddleware.cs.</files>
  <read_first>
    - .planning/debug/999.6-basicauth-flaky.md (Task 1 output — the `## Recommended Fix Shape` + `## Cross-Class Files Touched` sections ARE the action specification for this task)
    - DeckFlow.Web.Tests/BasicAuthMiddlewareTests.cs (file being modified — focus on `CorrectCredentials_InvokesNext` at line 84 and the inner EnvScope helper at line 101)
    - DeckFlow.Web.Tests/Security/AdminBruteForceTrackerStoreTests.cs (file being modified per Codex HIGH #4 — focus on call sites at lines 96-97, 125-126 and the inner EnvScope helper at line 158)
    - If verdict = `F-MIDDLEWARE-IDEMPOTENT`: DeckFlow.Web/Infrastructure/BasicAuthMiddleware.cs (full file; the SUT change must be minimal and SOLID-compliant)
    - .planning/phases/999.6-v1-3-ship-gate-test-residual-cleanup/999.6-CONTEXT.md §D-10, §D-15
  </read_first>
  <action>
    Apply the fix EXACTLY as specified in the `## Recommended Fix Shape` + `## Cross-Class Files Touched` sections of `.planning/debug/999.6-basicauth-flaky.md`. Do NOT deviate from the approved fix shape — if mid-flight the executor finds a "better" approach, STOP and re-loop through Task 1 with a new verdict.

    Three legitimate fix-shape branches (per D-10 + D-14 + Codex HIGH #4):

    **Branch F-ENV-COLLECTION (most likely per orchestrator's pre-confirmed grep):**
    1. Create `DeckFlow.Web.Tests/Infrastructure/EnvScope.cs` — shared `internal sealed class EnvScope : IDisposable` with `Set(string name, string value)`, `Set(string k1, string v1, string k2, string v2)`, `Clear(params string[] keys)`, and `Dispose()` that restores previous values. Subsumes both existing private `EnvScope` shapes. Namespace `DeckFlow.Web.Tests.Infrastructure`.
    2. Create `DeckFlow.Web.Tests/Infrastructure/AdminEnvCollection.cs` — `[CollectionDefinition("AdminEnvSerial", DisableParallelization = true)] public sealed class AdminEnvCollection { }`. Empty class body; the attribute IS the contract. Namespace `DeckFlow.Web.Tests.Infrastructure`.
    3. Edit `DeckFlow.Web.Tests/BasicAuthMiddlewareTests.cs`:
       - Add `using DeckFlow.Web.Tests.Infrastructure;`.
       - Add `[Collection("AdminEnvSerial")]` attribute on the class.
       - DELETE the inner `EnvScope` class (lines 101-135) — now provided by the shared helper.
       - All existing `EnvScope.Set / EnvScope.Clear` call sites continue to work unchanged (shared helper preserves call signatures).
    4. Edit `DeckFlow.Web.Tests/Security/AdminBruteForceTrackerStoreTests.cs`:
       - Add `using DeckFlow.Web.Tests.Infrastructure;`.
       - Add `[Collection("AdminEnvSerial")]` attribute on the class.
       - DELETE the inner `EnvScope` class (lines 158-170) — now provided by the shared helper.
       - All existing `EnvScope.Set` call sites continue to work unchanged. (Adjust if shared signature requires — debug doc specifies.)
    5. The `[CollectionDefinition(DisableParallelization=true)]` serializes facts from BOTH classes across the same xUnit collection → no cross-class env-var race possible.

    **Branch F-ENV-CLEAR-EAGER (defensive, less robust):**
    1. In `BasicAuthMiddlewareTests.CorrectCredentials_InvokesNext` (and any other facts named in the verdict), prepend an explicit Arrange step: `Environment.SetEnvironmentVariable(EnvUser, null); Environment.SetEnvironmentVariable(EnvPass, null);` BEFORE the `EnvScope.Set` call.
    2. Mirror in `AdminBruteForceTrackerStoreTests` for its 2 env-mutating facts.
    3. Preserves existing class shapes (no shared helper, no `[Collection]`); ONLY adds defensive clears. Less robust because xUnit may still interleave; relies on the clear-then-set happening atomically before the test's middleware call.

    **Branch F-MIDDLEWARE-IDEMPOTENT (real production bug):**
    1. Edit `DeckFlow.Web/Infrastructure/BasicAuthMiddleware.cs` per D-15. The fix must make the middleware idempotent under env-var mutation (e.g., read env vars on every `InvokeAsync`, never cache at construction).
    2. Apply the MINIMAL change that closes the bug — do NOT refactor surrounding code.
    3. Must pass SOLID review (SRP: middleware still does one thing; DIP: prefer env-var abstraction over direct `Environment.GetEnvironmentVariable` if the verdict says so).
    4. Update test alongside in same commit (cannot land SUT-only or test-only). The test fix in this branch is whichever test-side change the verdict still requires (typically Branch F-ENV-COLLECTION in addition to the SUT fix, to prevent the same race from breaking new tests later).

    Forbidden across all branches:
    - `[Fact(Skip = ...)]` on any BasicAuth or AdminBruteForce fact (violates D-03).
    - `[Collection("AnyName")]` WITHOUT a corresponding `[CollectionDefinition(DisableParallelization = true)]` (a paper-over per D-10).

    Preserve LF line endings; do not auto-format. Touch only the lines/files the `## Cross-Class Files Touched` section identifies.
  </action>
  <verify>
    <automated>"/mnt/c/Program Files/dotnet/dotnet.exe" build DeckFlow.sln -c Release -nologo &amp;&amp; for i in 1 2 3 4 5; do "/mnt/c/Program Files/dotnet/dotnet.exe" test DeckFlow.sln -c Release --no-build --nologo --filter "FullyQualifiedName~BasicAuthMiddlewareTests" 2>&amp;1 | tee /tmp/p02-focused-r$i.log; grep -q "Failed: 0," /tmp/p02-focused-r$i.log || { echo "FAIL focused run $i"; exit 1; }; done &amp;&amp; for i in 1 2 3 4 5; do "/mnt/c/Program Files/dotnet/dotnet.exe" test DeckFlow.sln -c Release --no-build --nologo 2>&amp;1 | tee /tmp/p02-full-r$i.log; grep -q "Failed: 2, .* Skipped: 3" /tmp/p02-full-r$i.log || { echo "FAIL full run $i"; exit 1; }; done</automated>
  </verify>
  <acceptance_criteria>
    - Build gate exits 0 warnings 0 errors.
    - All 5 captured focused-filter logs (`/tmp/p02-focused-r1.log` through `r5.log`) contain `Failed: 0,` (per D-11 — 5/5 stability).
    - All 5 captured full-suite logs (`/tmp/p02-full-r1.log` through `r5.log`) contain `Failed: 2, .* Skipped: 3` (per D-11 + Codex HIGH #5 — 5/5 stability, parsed from stdout NOT exit-code).
    - `grep -v '^#' DeckFlow.Web.Tests/BasicAuthMiddlewareTests.cs | grep -c "Skip\s*="` returns `0`.
    - `grep -v '^#' DeckFlow.Web.Tests/Security/AdminBruteForceTrackerStoreTests.cs | grep -c "Skip\s*="` returns `0`.
    - Branch F-ENV-COLLECTION acceptance: files `DeckFlow.Web.Tests/Infrastructure/EnvScope.cs` and `DeckFlow.Web.Tests/Infrastructure/AdminEnvCollection.cs` exist; both `BasicAuthMiddlewareTests` and `AdminBruteForceTrackerStoreTests` carry `[Collection("AdminEnvSerial")]`; the two private inner `EnvScope` classes are deleted.
    - Branch F-MIDDLEWARE-IDEMPOTENT acceptance: `dotnet build` warning count unchanged from post-Plan-01 baseline; SOLID review documented in commit body.
    - Commit message Branch F-ENV-COLLECTION: `fix(999.6): serialize FEEDBACK_ADMIN_* env-mutating tests via shared xUnit collection (D-09/D-10)`
    - Commit message Branch F-ENV-CLEAR-EAGER: `fix(999.6): eager env-var clear before EnvScope.Set across BasicAuth + AdminBruteForce tests (D-09/D-10)`
    - Commit message Branch F-MIDDLEWARE-IDEMPOTENT: `fix(999.6): make BasicAuthMiddleware idempotent under env-var mutation (D-09/D-15)`
    - Commit body in every branch carries BOTH triplets: focused-test triplet (parsed from any of the 5 focused logs) AND full-suite triplet (parsed from any of the 5 full logs).
    - Commit author plain default per D-19.
  </acceptance_criteria>
  <done>BasicAuth flaky fact stable across 5/5 focused + 5/5 full-suite runs; 1 of 3 P02+P03 residuals closed; full-suite stdout parses as `Failed: 2`.</done>
</task>

<task type="auto">
  <name>Task 3: Closing plan SUMMARY + carry-forward to P03</name>
  <files>.planning/phases/999.6-v1-3-ship-gate-test-residual-cleanup/999.6-02-SUMMARY.md</files>
  <read_first>
    - .planning/phases/999.6-v1-3-ship-gate-test-residual-cleanup/999.6-02-PLAN.md (must_haves section)
    - .planning/debug/999.6-basicauth-flaky.md (verdict — must be cited in SUMMARY)
    - .planning/phases/999.6-v1-3-ship-gate-test-residual-cleanup/999.6-01-SUMMARY.md (format reference)
  </read_first>
  <action>
    Create `.planning/phases/999.6-v1-3-ship-gate-test-residual-cleanup/999.6-02-SUMMARY.md` documenting: files modified (including `AdminBruteForceTrackerStoreTests.cs` per Codex HIGH #4 if Branch F-ENV-COLLECTION or F-ENV-CLEAR-EAGER applied), scope (P02 BasicAuth flaky-fix), decisions delivered (D-09, D-10, D-11 verified via 5/5 runs parsed from stdout, D-15 application status — fired or not), branch applied (F-ENV-COLLECTION / F-ENV-CLEAR-EAGER / F-MIDDLEWARE-IDEMPOTENT), debug verdict citation verbatim, build + 5×focused + 5×full-suite gate results with triplets parsed from `/tmp/p02-*.log` files, residual 2 failures explicitly enumerated (Archidekt cache-job — carried forward to P03), commit hash sequence (debug-doc commit + fix commit).

    Commit the SUMMARY as closing artifact.
  </action>
  <verify>
    <automated>test -f .planning/phases/999.6-v1-3-ship-gate-test-residual-cleanup/999.6-02-SUMMARY.md &amp;&amp; grep -q "Failed: 2" .planning/phases/999.6-v1-3-ship-gate-test-residual-cleanup/999.6-02-SUMMARY.md</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/999.6-v1-3-ship-gate-test-residual-cleanup/999.6-02-SUMMARY.md` exists.
    - SUMMARY cites debug verdict line verbatim.
    - SUMMARY documents 5×focused + 5×full-suite run results (all 10 parsed from captured stdout per Codex HIGH #5 — focused `Failed: 0`, full `Failed: 2, .* Skipped: 3`). NOT exit-code gated.
    - SUMMARY enumerates the 2 carry-forward residuals: `ArchidektCacheJobServiceTests.BackgroundService_SucceedsAndUpdatesProcessedCounts` and `ArchidektCacheJobServiceTests.GetActiveJob_ReturnsNullAfterCompletedJob`.
    - SUMMARY records which branch was taken AND lists all files modified (must include `Security/AdminBruteForceTrackerStoreTests.cs` if F-ENV-COLLECTION or F-ENV-CLEAR-EAGER per Codex HIGH #4).
    - Commit message: `docs(999.6): summary for plan 02 BasicAuth flaky stabilization`
    - Commit author plain default per D-19.
  </acceptance_criteria>
  <done>P02 closed; full-suite parses as `Failed: 2`; P03 unblocked.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test process → BasicAuthMiddleware production code | Task 2 Branch F-MIDDLEWARE-IDEMPOTENT potentially modifies the production middleware. State-management bugs in auth code are HIGH severity (information disclosure + authentication bypass risk). |
| repo → public GitHub | `.planning/debug/999.6-basicauth-flaky.md` becomes world-visible; must NOT contain real admin credentials. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-999.6-P02-01 | Spoofing / Elevation of Privilege | BasicAuthMiddleware.cs (Task 2 Branch F-MIDDLEWARE-IDEMPOTENT only) | mitigate | Real-bug branch only fires under D-15. Fix is MINIMAL — no surrounding refactor. SOLID review (SRP, DIP) required before commit per Task 1 verification step 5. Test gate after fix parses 5×full-suite from stdout to confirm no regression in `EnvVarsMissing_Returns503`, `NoAuthHeader_Returns401_WithChallenge`, `MalformedHeader_Returns401`, `WrongCredentials_Returns401`. |
| T-999.6-P02-02 | Information Disclosure | .planning/debug/999.6-basicauth-flaky.md | mitigate | Debug doc may reference env var NAMES (`FEEDBACK_ADMIN_USER`) but MUST NOT contain real env var VALUES from any local `.env` or Render dashboard. Pre-commit grep gate on the debug file for any string longer than 8 chars matching `admin|secret|password|@gmail|@yahoo|Bearer\s+\S+` — manual review escalation if any hit. |
| T-999.6-P02-03 | Denial of Service | xUnit test runner | accept | Branch F-ENV-COLLECTION adds ONE collection-scoped serialization (`AdminEnvSerial`) covering exactly the env-mutating classes. Does NOT serialize the broader test suite — parallel test execution remains the default outside this collection. Negligible runtime impact. |

No npm/pip/cargo installs — Package Legitimacy Gate not applicable. Branch F-MIDDLEWARE-IDEMPOTENT severity = HIGH (auth code); Branch F-ENV-COLLECTION severity = LOW (test infrastructure addition); Branch F-ENV-CLEAR-EAGER severity = LOW (test-only Arrange).
</threat_model>

<verification>
- `.planning/debug/999.6-basicauth-flaky.md` exists with all 6 required sections + verdict (Task 1).
- Task 2 verify command runs 5×focused-filter + 5×full-suite; ALL 10 captured logs parse expected residuals (focused `Failed: 0`, full `Failed: 2, .* Skipped: 3`) per Codex HIGH #5. NOT exit-code gated.
- Build gate exits 0 warnings 0 errors after every commit.
- No `[Skip]` directive added to any test in this plan (D-03).
- `[CollectionDefinition(DisableParallelization=true)]` legitimacy — covers ONLY env-mutating classes (`BasicAuthMiddlewareTests` + `AdminBruteForceTrackerStoreTests`); root cause is explicitly named (cross-class env-var race) in the debug doc. Distinct from a paper-over per D-10/D-14 nuance recorded in Task 1's Forbidden Fix Shapes section.
- Branch F-MIDDLEWARE-IDEMPOTENT only: `dotnet build` warning count unchanged from post-Plan-01 baseline; no new SOLID violations.
- All commits authored by `luntc1972 <luntc1972@yahoo.com>` (D-19).
- Commit bodies carry the focused-test triplet AND (for Task 2) the full-suite triplet (D-11 + D-21).
</verification>

<success_criteria>
1. `CorrectCredentials_InvokesNext` passes 5/5 in focused isolation AND 5/5 in full-suite ordering.
2. Pre-plan failure count `3` reduced to `2` after Task 3 closing gate (parsed from stdout per Codex HIGH #5).
3. Skipped count stays at `3` (D-03 — no new `[Skip]`).
4. If D-15 fired (Branch F-MIDDLEWARE-IDEMPOTENT), the production fix is in `DeckFlow.Web/Infrastructure/BasicAuthMiddleware.cs` ONLY (no scope creep into adjacent files).
5. `AdminBruteForceTrackerStoreTests.cs` modified per Codex HIGH #4 (Branch F-ENV-COLLECTION or F-ENV-CLEAR-EAGER paths).
6. Debug-doc artifact (`.planning/debug/999.6-basicauth-flaky.md`) committed alongside fix per D-18.
7. Carry-forward residuals (2 Archidekt cache-job failures) explicitly named in SUMMARY for P03.
</success_criteria>

<output>
Create `.planning/phases/999.6-v1-3-ship-gate-test-residual-cleanup/999.6-02-SUMMARY.md` when done (Task 3).
</output>
