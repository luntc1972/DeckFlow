---
phase: 61-creator-sources-and-selection
plan: 04
type: execute
wave: 3
depends_on: [01, 03]
files_modified:
  - DeckFlow.Studio/Pages/Skipped.razor
  - DeckFlow.Studio/Shared/NavMenu.razor
  - DeckFlow.Studio.Tests/SkippedPageTests.cs
  - README.md
autonomous: true
requirements: [HSEL-03]
must_haves:
  truths:
    - "A Skipped page lists every skipped video (id + reason + skipped time) from SkippedVideoStore"
    - "Each row has an Un-skip action that removes it from the skip list, so it reappears in Harvest selection (parity with Block/Unblock)"
    - "(Codex LOW) Verified end-to-end: a skipped video is hidden from the Harvest browse list, and after un-skip it reappears on re-browse — not just removed from the /skipped table"
    - "The page shows an empty-state when nothing is skipped"
    - "Nav exposes the Skipped page; the Blocked page and its behavior are unchanged"
  artifacts:
    - path: "DeckFlow.Studio/Pages/Skipped.razor"
      provides: "List + un-skip of skipped videos (HSEL-03), mirroring Blocked.razor"
      contains: "@page \"/skipped\""
    - path: "DeckFlow.Studio.Tests/SkippedPageTests.cs"
      provides: "bUnit: lists skipped rows; un-skip removes a row + calls RemoveSkipAsync; empty-state"
      contains: "Skipped"
  key_links:
    - from: "DeckFlow.Studio/Pages/Skipped.razor"
      to: "DeckFlow.Core.Content.ISkippedVideoStore"
      via: "ListSkippedAsync to render; RemoveSkipAsync to un-skip"
      pattern: "ISkippedVideoStore"
---

<objective>
Deliver HSEL-03: a `Skipped.razor` page (mirroring `Blocked.razor`) that lists skipped videos and
lets the operator un-skip an entry to bring it back into Harvest selection — the parity partner to
Block/Unblock. Wire nav, update README, cover with bUnit.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/61-creator-sources-and-selection/61-CONTEXT.md
@./CLAUDE.md
# Mirror this page's structure/conventions:
@DeckFlow.Studio/Pages/Blocked.razor
@DeckFlow.Studio/Shared/NavMenu.razor
</context>

<tasks>
1. `Skipped.razor` (`/skipped`): load `ListSkippedAsync()` into a table (video id, reason, skipped
   time), each row with an Un-skip button calling `RemoveSkipAsync(id)` then refreshing the list.
   Empty-state when none. Sanitized error/success feedback. Mirror `Blocked.razor` layout/loading.
2. Nav: add a `NavLink` to `/skipped` in `NavMenu.razor` (near the Blocked link).
3. README: document the Skipped list + un-skip in the Studio section.
4. bUnit `SkippedPageTests`: renders skipped rows; un-skip removes a row and calls `RemoveSkipAsync`;
   empty-state renders. Use a fake store. Confirm Blocked page tests are unaffected.
   (Codex LOW) Add a reappearance check (in SkippedPageTests or HarvestPageTests): seed a skipped
   video, confirm Harvest browse hides it, un-skip it, then confirm a re-browse shows it again — the
   user-visible HSEL-03 outcome, not just the table row removal.
5. Build Studio + run Studio.Tests green (note the BlockedPageTests isolation flake).
</tasks>

## Trust Boundaries
Read/remove over the operator's own skipped_videos list (Plan 01, parameterized). No external calls.

## STRIDE Threat Register
- **Integrity (T-61-08):** un-skip must only delete the skipped_videos row — never touch artifacts,
  blocked_videos, or content_site_index. Mitigation: page calls only `RemoveSkipAsync`.
- **Spoofing/EoP (T-61-09):** none new — same local single-operator surface as the Blocked page;
  inherits the existing Studio access model.
