---
phase: 999.6
slug: v1-3-ship-gate-test-residual-cleanup
status: verified
threats_open: 0
asvs_level: 1
created: 2026-05-22
---

# Phase 999.6 — Security

> Per-phase security contract: threat register, accepted risks, and audit trail.

Phase 999.6 ("v1.3 ship-gate test residual cleanup") closed 9 failing tests → 0 across three plans. P01 was mechanical test-only cleanup (no production code touched). P02 was test-infrastructure stabilization (xUnit `[Collection]` serialization + shared `EnvScope` helper; H2 falsified — production `BasicAuthMiddleware` was already idempotent and not modified). P03 fired D-15 (real production bug) and shipped an additive `IHarvestRunStore.GetByIdAsync` method plus a rewired `ArchidektCacheJobService.GetJob` so terminal-state harvest jobs are retrievable by id.

---

## Trust Boundaries

| Boundary | Description | Data Crossing |
|----------|-------------|---------------|
| repo → public GitHub (`luntc1972/DeckFlow`) | Any committed file (debug docs, test fixtures, code) is world-visible. Phase committed two new debug docs under `.planning/debug/` that must contain no live credentials or PII. | Plain-text Markdown (debug investigation notes); test source code (xUnit serialization helpers) |
| test process → BasicAuthMiddleware production code | Hypothetical Branch F-MIDDLEWARE-IDEMPOTENT would have modified the production middleware. Branch did NOT fire (H2 falsified). | N/A — branch not taken |
| test process → ArchidektCacheJobService production code | Branch F-PROD-CONTRACT (D-15 fired) modified the service + persistence interface to add a by-id lookup against `harvest_runs`. | `Guid` lookup key → admin-internal `harvest_runs` row (no customer PII) |

---

## Threat Register

| Threat ID | Category | Component | Disposition | Mitigation | Status |
|-----------|----------|-----------|-------------|------------|--------|
| T-999.6-P01-01 | Information Disclosure | `DeckFlow.Web.Tests/Fixtures/arna-test/*` (P01 Task 4 Branch A) | mitigate (branch-not-fired) | Branch B taken instead — `PacketArtifactStoreRoundTripTests.cs` DELETED at commit `01534e3`. `git log --all -- 'DeckFlow.Web.Tests/Fixtures/arna-test/*'` returns nothing; directory does not exist on disk. No fixture content committed to public repo. | closed |
| T-999.6-P01-02 | Tampering | None applicable | accept | P01 was test-source-only — no production code, dependencies, or package installs touched. Threat surface limited to test-asset content (assertion strings, deleted Facts). | closed |
| T-999.6-P02-01 | Spoofing / Elevation of Privilege | `DeckFlow.Web/Infrastructure/BasicAuthMiddleware.cs` (Branch F-MIDDLEWARE-IDEMPOTENT) | mitigate (branch-not-fired) | H2 falsified per `.planning/debug/999.6-basicauth-flaky.md` — `BasicAuthMiddleware.InvokeAsync` reads `Environment.GetEnvironmentVariable` fresh on every invocation; no construction cache. `git diff f07e120..360dee0 -- DeckFlow.Web/Infrastructure/BasicAuthMiddleware.cs` is empty. Production auth code untouched. | closed |
| T-999.6-P02-02 | Information Disclosure | `.planning/debug/999.6-basicauth-flaky.md` (public-repo committed file) | mitigate | Grep against `password\|secret\|@gmail\|@yahoo\|Bearer\s+\S+` returned only env-var NAMES (`FEEDBACK_ADMIN_PASSWORD`), method names, and the literal placeholder fixtures already in the test source (`EnvScope.Set(EnvUser, "admin", EnvPass, "secret")`). No real admin credentials, no Bearer tokens, no real email addresses. | closed |
| T-999.6-P02-03 | Denial of Service | xUnit test runner (`[CollectionDefinition("AdminEnvSerial", DisableParallelization = true)]`) | accept | Scope strictly limited to env-mutating classes — `[Collection("AdminEnvSerial")]` declared at exactly 2 sites (`BasicAuthMiddlewareTests.cs:14`, `Security/AdminBruteForceTrackerStoreTests.cs:17`). Broader test-suite parallelism unaffected. | closed |
| T-999.6-P03-01 | Tampering | `IHarvestRunStore.GetByIdAsync` / `HarvestRunStore.GetByIdAsync` (Branch F-PROD-CONTRACT, D-15 fired) | mitigate | Verified at `HarvestRunStore.cs:235-262`: (a) parameterized SQL — `WHERE id = @id LIMIT 1`, no string concat; (b) provider-specific Guid binding (`_connectionInfo.IsPostgres ? (object)id : id.ToString()`) present at 4 sites in `HarvestRunStore.cs` (3 pre-existing + new `GetByIdAsync`) — SQLite/Postgres parity preserved; (c) method scoped to `harvest_runs` table only (no cross-table escalation); (d) additive interface change — OCP/DIP preserved. | closed |
| T-999.6-P03-02 | Denial of Service / Availability | `ArchidektCacheJobService.GetJob` callsite (sync wrapper pattern) | accept | `GetJob` at `ArchidektCacheJobService.cs:208-212` retains the `.GetAwaiter().GetResult()` sync wrapper; only callers are admin endpoints (`ArchidektCacheJobsController`) at documented sub-1RPS load. No new concurrency surface. | closed |
| T-999.6-P03-03 | Information Disclosure | `.planning/debug/999.6-archidekt-cache-job.md` (public-repo committed file) | mitigate | Grep against `MTG_DATA_DIR\|api_key\|API_KEY\|Bearer \|password=\|@gmail\|@yahoo\|/home/\|/Users/\|C:\\` returned no matches. Debug doc references cache-job state lifecycle, source-line citations, and SQL DDL only — no dev paths, no SQLite file contents, no harvest URLs with keys. | closed |

*Status: open · closed*
*Disposition: mitigate (implementation required) · accept (documented risk) · transfer (third-party)*

---

## Accepted Risks Log

| Risk ID | Threat Ref | Rationale | Accepted By | Date |
|---------|------------|-----------|-------------|------|
| AR-999.6-01 | T-999.6-P01-02 | P01 was mechanical test-source-only cleanup. No production code, no new dependencies, no package installs. Threat surface limited to test assertion strings and a deleted dead-premise Fact. | luntc1972 (orchestrator) | 2026-05-22 |
| AR-999.6-02 | T-999.6-P02-03 | xUnit `[CollectionDefinition(DisableParallelization=true)]` scope is strictly the env-mutating pair (`BasicAuthMiddlewareTests` + `AdminBruteForceTrackerStoreTests`). Parallel test execution remains the default outside this collection. Negligible runtime impact (≈400ms added to suite). Matches existing project convention in `CategoryKnowledgeStoreTests.cs` and `DeckFlowDatabaseConnectionFactoryTests.cs`. | luntc1972 (orchestrator) | 2026-05-22 |
| AR-999.6-03 | T-999.6-P03-02 | `ArchidektCacheJobService.GetJob` retains the `.GetAwaiter().GetResult()` sync wrapper. Callsites are admin-only (`/api/archidekt-cache-jobs/{jobId}` + admin harvest dashboard) at documented sub-1RPS load. Comment block at SUT site notes the rationale. T-07-10 acceptance bound carried forward unchanged from prior phases. | luntc1972 (orchestrator) | 2026-05-22 |

---

## Security Audit Trail

| Audit Date | Threats Total | Closed | Open | Run By |
|------------|---------------|--------|------|--------|
| 2026-05-22 | 8 | 8 | 0 | gsd-security-auditor (sonnet) |

### Audit 2026-05-22 — Initial verification

- Register origin: PLAN-authored (`register_authored_at_plan_time: true`) across all 3 plans (999.6-01-PLAN.md, 999.6-02-PLAN.md, 999.6-03-PLAN.md).
- No `## Threat Flags` sections in any SUMMARY.md (no unregistered flags surfaced during implementation).
- Auditor mode: verify mitigations exist; do NOT scan for new threats (per workflow rule for plan-authored registers).
- 2 of 5 "mitigate" threats closed by branch-not-fired evidence (T-999.6-P01-01 = Branch B taken; T-999.6-P02-01 = H2 falsified). Verified via `git log`, `git diff`, and filesystem absence — not by trusting documentation.
- 3 of 5 "mitigate" threats closed by direct source/file verification (T-999.6-P02-02 grep of debug doc; T-999.6-P03-01 parameterized SQL + 4-site provider binding; T-999.6-P03-03 grep of debug doc).
- 3 "accept" threats closed by acceptance-bound preservation (T-999.6-P01-02; T-999.6-P02-03 scope = 2 collection sites; T-999.6-P03-02 sync wrapper unchanged).

---

## Sign-Off

- [x] All threats have a disposition (mitigate / accept / transfer)
- [x] Accepted risks documented in Accepted Risks Log
- [x] `threats_open: 0` confirmed
- [x] `status: verified` set in frontmatter

**Approval:** verified 2026-05-22 (auditor: gsd-security-auditor; orchestrator: luntc1972)
