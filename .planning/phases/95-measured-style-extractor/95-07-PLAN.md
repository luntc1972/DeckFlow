---
phase: 95-measured-style-extractor
plan: 07
type: execute
wave: 4
depends_on: [95-01, 95-03, 95-04, 95-05, 95-06]
files_modified:
  - DeckFlow.Web/Services/CreatorStyle/MeasuredStyleProfileBuilder.cs
  - DeckFlow.Web/Services/CreatorStyle/CreatorDeckCategoryResolver.cs
  - DeckFlow.Web/Program.cs
  - DeckFlow.Web.Tests/Services/CreatorStyle/MeasuredStyleProfileBuilderTests.cs
  - DeckFlow.Web.Tests/Services/CreatorStyle/SnailSeedCorpusFixture.cs
  - README.md
autonomous: true
requirements: [CS-06, CS-08, CS-09, CS-10]
must_haves:
  truths:
    - "A creator's crawled decks produce a MeasuredMetric[] carrying raw NumDecks + the nested folder-weighted EffectiveSampleSize on every stat"
    - "Emitted metrics include lift-based synergy, Commander-Spellbook combo density (null-graceful), and Karsten land/curve consistency computed with an explicit fixed ManabaseMode.Casual + experimental flags defaulted OFF (D-08, D-09)"
    - "Category tags come from CardCategoryRepository with a Scryfall Tagger tail fill, multi-bucket"
    - "A profile built from fewer than MinDeckFloor decks is marked insufficient_sample; the profile persists via CreatorStyleProfileStore.UpsertAsync"
    - "The extractor is validated against a checked-in deterministic Salubrious Snail representative fixture (automated invariants), with the full live 39-deck crawl kept as a Manual-Only check (D-12)"
  artifacts:
    - path: "DeckFlow.Web/Services/CreatorStyle/MeasuredStyleProfileBuilder.cs"
      provides: "Web-host orchestrator composing pure extraction + Karsten + combo + category resolution -> MeasuredMetric[]"
      min_lines: 120
    - path: "DeckFlow.Web/Services/CreatorStyle/CreatorDeckCategoryResolver.cs"
      provides: "3-layer category resolution (harvested repo -> Tagger tail), multi-bucket"
      min_lines: 40
    - path: "DeckFlow.Web.Tests/Services/CreatorStyle/SnailSeedCorpusFixture.cs"
      provides: "Checked-in deterministic reduced-but-representative Salubrious Snail sample corpus (D-12 automated validation input)"
      min_lines: 40
  key_links:
    - from: "MeasuredStyleProfileBuilder"
      to: "CreatorStyleProfileStore.UpsertAsync"
      via: "MeasuredMetric[] persistence into CreatorStyleProfile"
      pattern: "UpsertAsync"
    - from: "MeasuredStyleProfileBuilder"
      to: "ICommanderSpellbookService.FindCombosAsync"
      via: "combo density (null-graceful)"
      pattern: "FindCombosAsync"
    - from: "MeasuredStyleProfileBuilder"
      to: "ManabaseAnalyzer.Analyze"
      via: "Scryfall->CardFact->Classify->Analyze per deck (ManabaseMode.Casual, flags OFF)"
      pattern: "ManabaseAnalyzer.Analyze"
---

<objective>
The capstone orchestrator: `MeasuredStyleProfileBuilder` composes the crawler (Plan 06), the pure extraction (Plans 04/05), the global lift baseline (Plan 03), plus two Web-host services — `CommanderSpellbookService.FindCombosAsync` (combo density, CS-08, null-graceful) and the `ManabaseAnalysisService` Scryfall→CardFact→Karsten chain (land/curve consistency, CS-09) — and category resolution (`CardCategoryRepository` + Scryfall Tagger tail, D-06/CS-06, multi-bucket). It emits `MeasuredMetric[]` with raw `NumDecks` + the nested folder-weighted `EffectiveSampleSize` on every stat (CS-10) and persists the profile via `CreatorStyleProfileStore.UpsertAsync`, marking `insufficient_sample` below `MinDeckFloor`.

Purpose: CS-06/08/09/10 — turn crawled decks into a persisted measured profile. This is the D-11 "Web resolves external data → hands off to pure Core math" seam, top of the dependency chain.
Output: Orchestrator + category resolver + DI wiring + tests (incl. a deterministic Snail fixture validation) + README.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/95-measured-style-extractor/95-CONTEXT.md
@.planning/phases/95-measured-style-extractor/95-PATTERNS.md
@DeckFlow.Web/Services/Manabase/ManabaseAnalysisService.cs
@DeckFlow.Web/Services/CommanderSpellbookService.cs

<interfaces>
<!-- The Web->CardFact->pure-Core-Karsten chain to REPLICATE (ManabaseAnalysisService.cs:408-463). -->
  ScryfallCardNameIndex index = await ResolveCardsAsync(deckCards, ct);
  IReadOnlyList<CardFact> facts = ScryfallCardFactMapper.ToCardFacts(deckEntries);
  ManabaseDeck deck = ManabaseClassifier.Classify(facts, isSingleton: true, ...);
  // MED-3: pass an EXPLICIT fixed mode; leave every experimental flag at its false default (do NOT thread IFeatureFlagCache).
  ManabaseReport report = ManabaseAnalyzer.Analyze(deck, ManabaseMode.Casual);  // report.ActualLands, report.TargetLands, report.LandDelta, report.Health
  // ManabaseAnalyzer.Analyze (ManabaseAnalyzer.cs:124-133) REQUIRES `mode`; all bool flags (useManaQuantity,
  // colorAwareMulligan, gateRampOnCastable, useHealthBandCastability, useHealthBandHeadlineFloor) default false.
  // ManabaseMode enum (ManabaseMode.cs): Casual (Karsten singleton land target — the historic default) | Cedh.

<!-- Combo density (null-graceful contract: returns null on failure, treat as "no combo data"). -->
From DeckFlow.Web/Services/CommanderSpellbookService.cs:
  interface ICommanderSpellbookService { Task<CommanderSpellbookResult?> FindCombosAsync(IReadOnlyList<DeckEntry>, CancellationToken); }
  record CommanderSpellbookResult(IReadOnlyList<SpellbookCombo> IncludedCombos, IReadOnlyList<SpellbookAlmostCombo> AlmostIncludedCombos);

<!-- Category resolution (D-06 3-layer). -->
From DeckFlow.Core/Knowledge/CategoryKnowledgeRepository.cs:
  Task<IReadOnlyList<string>> GetCategoriesAsync(string cardName, CancellationToken);  // multi-bucket (CategoryFilter.IncludedOrFallback)
  Task<GlobalCategoryBaseline> GetGlobalCategoryBaselineAsync(CancellationToken);       // Plan 03
From DeckFlow.Web/Services/Scryfall/ScryfallTaggerLookupService.cs:
  interface IScryfallTaggerLookupService { Task<IReadOnlyList<string>> LookupOracleTagsAsync(string cardName, CancellationToken); }

<!-- Pure extraction (Plans 04/05) + persistence (Plan 01). -->
  StapleStripper.FilterOversized/FlagNearPrecons/ComputePersonalStaples/StripStaples
  FolderWeighting.ApplyWeights/EffectiveSampleSize/RawDeckCount
  CategoryCounter.AggregateCounts/CountPerDeck/DeckCategoryPresence
  LiftCalculator.ComputeLift
  CreatorStyleProfile.MinDeckFloor (= 5); MeasuredMetric{Metric,Value,NumDecks,Distribution}; MetricDistribution{Mean,Min,Max,StdDev,EffectiveSampleSize?}
  ICreatorStyleProfileStore.UpsertAsync(CreatorStyleProfile)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: CreatorDeckCategoryResolver (3-layer, multi-bucket) + per-deck Karsten (fixed mode) & combo resolution</name>
  <read_first>
    - DeckFlow.Web/Services/Manabase/ManabaseAnalysisService.cs (lines ~400-463 — the Scryfall→CardFact→Classify→Analyze chain to replicate; how ResolveCardsAsync / ScryfallCardFactMapper are used; note lines 231-268 thread live feature flags — the substrate does NOT)
    - DeckFlow.Core/Manabase/ManabaseAnalyzer.cs (lines 124-133 — Analyze REQUIRES `mode`; bool flags default false)
    - DeckFlow.Core/Manabase/ManabaseMode.cs (Casual | Cedh)
    - DeckFlow.Web/Services/CommanderSpellbookService.cs (FindCombosAsync null-graceful contract, lines 117-141)
    - DeckFlow.Web/Services/Scryfall/ScryfallTaggerLookupService.cs (LookupOracleTagsAsync — the Tagger tail)
    - DeckFlow.Core/Knowledge/CategoryKnowledgeRepository.cs (GetCategoriesAsync — harvested layer; GetGlobalCategoryBaselineAsync — Plan 03)
    - DeckFlow.Core/Manabase/ManabaseModels.cs (ManabaseReport: ActualLands/TargetLands/LandDelta/Health)
  </read_first>
  <action>Create `CreatorDeckCategoryResolver.cs` (in `DeckFlow.Web.Services.CreatorStyle`) taking `CategoryKnowledgeRepository` and `IScryfallTaggerLookupService`. Provide `ResolveAsync(IReadOnlyList<CreatorDeckSample>, CancellationToken) → IReadOnlyDictionary<string, IReadOnlyList<string>>` mapping each distinct card name across all samples to its multi-bucket category list using the D-06 3-layer priority: (1) `CategoryKnowledgeRepository.GetCategoriesAsync` (harvested, ~97% coverage) → if it returns categories, use them; (2) for the tail (cards the repo has no categories for), `IScryfallTaggerLookupService.LookupOracleTagsAsync` and map/keep the resulting oracle tags. Preserve multi-bucket (never `.First()`); dedupe per card. In `MeasuredStyleProfileBuilder.cs`, add a private per-deck helper that replicates the ManabaseAnalysisService chain (resolve Scryfall cards → `ScryfallCardFactMapper.ToCardFacts` → `ManabaseClassifier.Classify(facts, isSingleton:true)` → `ManabaseAnalyzer.Analyze(deck, ManabaseMode.Casual)`) returning the per-deck `ManabaseReport` (for `LandDelta`/`Health`/`TargetLands`). MED-3 DECISION — the substrate uses a FIXED `ManabaseMode.Casual` (Karsten's historic singleton land target, matching `ManabaseAnalysisOptions.Mode`'s default) for EVERY creator so the land/curve signal is comparable across creators and deterministic; and it does NOT thread the live `IFeatureFlagCache` manabase flags (useManaQuantity / colorAwareMulligan / gateRampOnCastable / health-band flags) — they stay at their `Analyze` false defaults so the substrate signal never shifts when an operator flips a display flag. Add a `// Why:` comment stating this rationale (Cedh-mode differentiation, if ever wanted, is a P97 fusion concern, not this substrate). Also add a private helper calling `ICommanderSpellbookService.FindCombosAsync(entries, ct)` per deck, treating a `null` result as "no combo data, contribute 0/skip" (CS-08 graceful degradation). Reuse the exact Scryfall-resolution dependency ManabaseAnalysisService uses; do NOT modify the three Core Manabase classes. Preserve LF line endings.</action>
  <verify>
    <automated>dotnet build DeckFlow.Web/DeckFlow.Web.csproj</automated>
  </verify>
  <acceptance_criteria>
    - `CreatorDeckCategoryResolver` consults `GetCategoriesAsync` first and `LookupOracleTagsAsync` only for the tail; `grep -c "\.First()\|\.Take(1)" CreatorDeckCategoryResolver.cs` == 0
    - The Karsten helper calls `ManabaseAnalyzer.Analyze(deck, ManabaseMode.Casual)` with a literal `ManabaseMode.Casual` and NO threaded feature flags (`grep -c "ManabaseMode.Casual" MeasuredStyleProfileBuilder.cs` >= 1; no `IFeatureFlagCache`/`IsFlagOn` used in the Karsten helper) and reads `ManabaseReport.LandDelta`/`Health` (MED-3)
    - The combo helper treats a null `FindCombosAsync` result as no-combo-data (no NullReferenceException path)
    - `dotnet build DeckFlow.Web/DeckFlow.Web.csproj` succeeds, 0 new warnings
  </acceptance_criteria>
  <done>Category (3-layer multi-bucket), Karsten (reused chain, fixed ManabaseMode.Casual, flags OFF), and combo (null-graceful) resolution helpers compile.</done>
</task>

<task type="auto">
  <name>Task 2: MeasuredStyleProfileBuilder — assemble MeasuredMetric[] and persist</name>
  <read_first>
    - DeckFlow.Core/Knowledge/CreatorStyleProfile.cs (MeasuredMetric/MetricDistribution incl. the Plan-01 EffectiveSampleSize; MinDeckFloor)
    - DeckFlow.Core/Content/CreatorStyleProfileStore.cs (UpsertAsync signature + InsufficientSample semantics)
    - DeckFlow.Core/Knowledge/MeasuredStyleExtraction/*.cs (StapleStripper/FolderWeighting/CategoryCounter/LiftCalculator — Plans 04/05)
    - 95-CONTEXT.md D-10 (raw NumDecks int + nested fractional EffectiveSampleSize) + D-07 (lift)
  </read_first>
  <action>In `MeasuredStyleProfileBuilder.cs`, add `BuildAsync(string creatorSlug, string platform, CancellationToken) → CreatorStyleProfile`: (1) `CreatorProfileDeckCrawler.CrawlAsync(slug)` → samples; (2) `StapleStripper.FilterOversized` → `FlagNearPrecons` → resolve categories (Task 1 resolver) → `ComputePersonalStaples` → `StripStaples`; (3) `FolderWeighting.ApplyWeights(samples, source.FolderWeights, source.WeightsUncurated)`; compute `rawDeckCount = FolderWeighting.RawDeckCount` and `effectiveSample = FolderWeighting.EffectiveSampleSize`; (4) fetch `GetGlobalCategoryBaselineAsync`; assemble `MeasuredMetric[]`: category-ratio metrics from `CategoryCounter.AggregateCounts`, top lift pairs from `LiftCalculator.ComputeLift`, combo-density metric (mean IncludedCombos per deck from the Task-1 combo helper, null-graceful), and Karsten metrics (mean `LandDelta`, land-consistency/`Health` distribution from the Task-1 Karsten helper). EVERY `MeasuredMetric` sets `NumDecks = rawDeckCount` and a `MetricDistribution` whose `EffectiveSampleSize = effectiveSample` (plus Mean/Min/Max/StdDev where a distribution is meaningful; use the metric's own value for degenerate cases) — CS-10/D-10. Build the `CreatorStyleProfile { Slug = slug, Platform = platform, MinDecks = rawDeckCount, InsufficientSample = rawDeckCount < CreatorStyleProfile.MinDeckFloor, MeasuredMetrics = metrics, StatedRules = empty, FusedTargets = empty, UpdatedUtc = now }` and `ICreatorStyleProfileStore.UpsertAsync` it. Return the profile. Preserve LF line endings.</action>
  <verify>
    <automated>dotnet build DeckFlow.Web/DeckFlow.Web.csproj</automated>
  </verify>
  <acceptance_criteria>
    - Every emitted `MeasuredMetric` sets `NumDecks` (int raw count) AND a `MetricDistribution.EffectiveSampleSize` (fractional) — `grep -c "EffectiveSampleSize" MeasuredStyleProfileBuilder.cs` >= 1
    - Metrics include lift-based synergy (from LiftCalculator), combo density (from FindCombosAsync), and Karsten land/curve (from ManabaseReport)
    - `InsufficientSample` set from `rawDeckCount < CreatorStyleProfile.MinDeckFloor`
    - Persists via `ICreatorStyleProfileStore.UpsertAsync`
    - `dotnet build DeckFlow.Web/DeckFlow.Web.csproj` succeeds, 0 new warnings
  </acceptance_criteria>
  <done>Crawled decks → staple-stripped, folder-weighted, lift/combo/Karsten MeasuredMetric[] with NumDecks + EffectiveSampleSize, persisted; insufficient-sample flagged.</done>
</task>

<task type="auto">
  <name>Task 3: Orchestrator tests + deterministic Snail fixture validation (D-12) + DI + README</name>
  <read_first>
    - DeckFlow.Web.Tests/Services/CommanderSpellbookServiceTests.cs (fake service + FakeHttpClientFactory conventions)
    - DeckFlow.Web/Program.cs (service registration site)
    - DeckFlow.Web.Tests/Services/CreatorStyle/CreatorProfileDeckCrawlerTests.cs (Plan 06 test harness — reuse the SQLite-store + fake-importer setup)
    - docs/research/p88-measured-style-prototype-snail.md (the Snail corpus characteristics the fixture must be representative of)
  </read_first>
  <action>Two deliverables. FIRST, the general end-to-end test: create `DeckFlow.Web.Tests/Services/CreatorStyle/MeasuredStyleProfileBuilderTests.cs` driving `BuildAsync` end-to-end against fakes — a fake crawler/importer returning a small deterministic multi-folder creator corpus, a fake `ICommanderSpellbookService` returning combos for one deck and `null` for another (prove null-graceful), a stub category source, and a real SQLite `CreatorStyleProfileStore`. Assert: (a) the persisted profile round-trips with MeasuredMetrics carrying NumDecks == raw deck count and a non-null EffectiveSampleSize distinct from NumDecks when folders are weighted; (b) a corpus below MinDeckFloor sets InsufficientSample = true; (c) a lift metric is present and a staple category is demoted; (d) the null-combo deck does not throw and contributes no combo count.
  SECOND, the D-12 Snail validation (HIGH-3): create `DeckFlow.Web.Tests/Services/CreatorStyle/SnailSeedCorpusFixture.cs` — a CHECKED-IN, deterministic, reduced-but-representative Salubrious Snail sample set (a `static IReadOnlyList<CreatorDeckSample>` of ≥ MinDeckFloor decks across ≥3 folders, seeded from the p88 prototype's observed shape: shared staples like Sol Ring / Command Tower present in most decks, at least one known staple category pair, folder tags Current/Secondary/Budget). Add a `[Fact]` `MeasuredStyleProfileBuilder_SnailSeedCorpus_ExtractorInvariants` that runs the fixture through the full `MeasuredStyleProfileBuilder` (with fakes for the external Scryfall/Spellbook/Tagger calls) and asserts extractor INVARIANTS: (i) curated + personal staples are stripped (Sol Ring / Command Tower absent from any emitted ratio); (ii) EVERY MeasuredMetric carries NumDecks (int) AND a nested EffectiveSampleSize; (iii) lift DEMOTES a known staple pair relative to a discriminating pair; (iv) folder weighting is applied (EffectiveSampleSize != raw NumDecks when Budget folder weight < 1.0); (v) a below-`MinDeckFloor` subset yields InsufficientSample = true. This is AUTOMATED and deterministic; the full live 39-deck crawl stays a Manual-Only check (95-VALIDATION.md). State in a test-class comment which is automated (this fixture) vs manual (live crawl).
  Register `CreatorDeckCategoryResolver` and `MeasuredStyleProfileBuilder` in `Program.cs` DI. Update `README.md` with a short "Creator measured-style extractor (substrate)" note describing the Archidekt crawl → staple-strip → lift/combo/Karsten → persisted MeasuredMetric[] pipeline and that it is substrate (no user page this phase). Preserve LF line endings. No new NuGet packages.</action>
  <verify>
    <automated>dotnet test DeckFlow.Web.Tests --filter "FullyQualifiedName~MeasuredStyleProfileBuilderTests"</automated>
  </verify>
  <acceptance_criteria>
    - General test proves: NumDecks vs EffectiveSampleSize distinction, InsufficientSample below floor, lift present + staple demoted, null-combo graceful
    - `SnailSeedCorpusFixture.cs` exists as a checked-in deterministic ≥MinDeckFloor multi-folder Snail-representative corpus; the `..._SnailSeedCorpus_ExtractorInvariants` `[Fact]` asserts invariants (i)-(v) above (D-12 automated validation)
    - A comment marks the automated fixture vs the Manual-Only live 39-deck crawl
    - `CreatorDeckCategoryResolver` + `MeasuredStyleProfileBuilder` registered in Program.cs
    - README updated with the extractor pipeline note
    - `dotnet test DeckFlow.Web.Tests --filter "FullyQualifiedName~MeasuredStyleProfileBuilderTests"` passes
  </acceptance_criteria>
  <done>End-to-end builder proven against fakes + real SQLite store, plus a deterministic Snail fixture proving extractor invariants (D-12 automated; live crawl manual); DI wired; README documents the substrate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Web orchestrator → external services | Combines Archidekt-crawled decks + Scryfall + Commander-Spellbook + Tagger (all untrusted external) |
| orchestrator → creator_style_profile | Computed profile persisted for downstream fusion (P97) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-95-07-01 | Denial of Service (external service failure cascade) | FindCombosAsync / Tagger / Scryfall | mitigate | Combo lookup is null-graceful (skip on null); Tagger is a tail-only fill; each external call already flows through existing Polly pipelines |
| T-95-07-02 | Tampering (self-polluted baseline) | GetGlobalCategoryBaselineAsync read vs creator crawl | mitigate | Builder only READS the global baseline; the creator crawl is cached in creator_deck_cache (Plan 06), never written to the corpus (Pitfall 3) |
| T-95-07-03 | Tampering (misleading trust on thin sample) | InsufficientSample flag | mitigate | Profiles below MinDeckFloor (5) are persisted with insufficient_sample=true, never silently trusted (CS-03 guardrail) |
| T-95-07-04 | Tampering (non-deterministic substrate signal) | Karsten mode/flag stance | mitigate | Fixed `ManabaseMode.Casual` + experimental flags left OFF (not threaded from IFeatureFlagCache) so the land/curve signal is reproducible and does not drift when an operator flips a display flag (MED-3) |
| T-95-07-05 | Information disclosure | persisted profile | accept | Aggregate public-deck statistics keyed by creator slug; no secrets/PII |
| T-95-07-SC | Tampering | package installs | mitigate | No new NuGet packages this plan |
</threat_model>

<verification>
- `dotnet build DeckFlow.Web/DeckFlow.Web.csproj` clean
- `dotnet test DeckFlow.Web.Tests --filter "FullyQualifiedName~MeasuredStyleProfileBuilderTests"` green (incl. the Snail fixture invariants)
- Full solution: `dotnet test DeckFlow.sln` green before phase verify
</verification>

<success_criteria>
- MeasuredMetric[] with NumDecks + nested EffectiveSampleSize on every stat (CS-10/D-10)
- Lift synergy + combo density (null-graceful, CS-08) + Karsten land/curve at fixed ManabaseMode.Casual (CS-09) + multi-bucket categories (CS-06)
- Profile persisted via CreatorStyleProfileStore.UpsertAsync; insufficient_sample below MinDeckFloor
- Extractor invariants proven against a deterministic Snail fixture (D-12 automated); live 39-deck crawl kept Manual-Only
</success_criteria>

<output>
Create `.planning/phases/95-measured-style-extractor/95-07-SUMMARY.md` when done.
</output>
</content>
</invoke>
