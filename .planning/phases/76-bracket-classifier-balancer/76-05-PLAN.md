---
phase: 76-bracket-classifier-balancer
plan: 05
type: execute
wave: 4
depends_on: ["76-02", "76-04"]
files_modified:
  - DeckFlow.Web/Models/BracketViewModel.cs
  - DeckFlow.Web/Models/BracketRequest.cs
  - DeckFlow.Web/Controllers/BracketController.cs
  - DeckFlow.Web/Views/Deck/Bracket.cshtml
  - DeckFlow.Web/wwwroot/css/site-common.css
  - DeckFlow.Web/Services/Tools/ToolRegistry.cs
  - DeckFlow.Web/Models/DeckPageTab.cs
  - DeckFlow.Web/Views/Shared/_ToolTileIcon.cshtml
  - DeckFlow.Web/Help/bracket.md
  - DeckFlow.Web.Tests/Bracket/BracketViewRenderTests.cs
  - DeckFlow.Web.Tests/Tools/ToolRegistryTests.cs
  - DeckFlow.Web.Tests/Tools/ToolVisibilityTests.cs
  - README.md
autonomous: true
requirements: [BRACKET-01, BRACKET-03, BRACKET-05]
must_haves:
  truths:
    - "GET /bracket renders the empty form; POST /bracket classifies and (when a target below the classified bracket is chosen) renders floor violations + starter cuts + the copy-prompt artifact"
    - "Both /bracket actions are gated by [FeatureFlagGate(\"tool.bracket.enabled\")] — the route 404s when the flag is OFF"
    - "With no classification (flag-OFF / empty state) the Bracket view renders no bracket-badge markup; with a classification it renders bracket-badge and bracket-badge--bN"
    - "The bracket tool is registered in ToolRegistry (Analyze section) with a tile, /bracket nav entry, icon (IconKey 'bracket', auto-derived from the tool key), DeckPageTab.Bracket=15, and a help topic reference (HelpSlug 'bracket' → DeckFlow.Web/Help/bracket.md, which declares requires_flag: tool.bracket.enabled); new CSS lives in site-common.css only (no theme fork)"
    - "Mass extra-card-draw renders as a static informational line (\"0 mass extra-card draw\") with an --extracards tag kind only — no detection field/list by design (RESEARCH A2)"
    - "Target bracket is validated to 1-5; out-of-range input is rejected before classification"
  artifacts:
    - path: "DeckFlow.Web/Controllers/BracketController.cs"
      provides: "GET+POST /bracket, flag-gated, guarded"
      contains: "FeatureFlagGate(\"tool.bracket.enabled\")"
    - path: "DeckFlow.Web/Views/Deck/Bracket.cshtml"
      provides: "Bracket page: form + badge + reasons + violations + stamp + copy-prompt"
      contains: "bracket-badge"
    - path: "DeckFlow.Web/Help/bracket.md"
      provides: "Bracket Check help topic; satisfies HelpFlagHeaderConsistencyTests for HelpSlug 'bracket'"
      contains: "requires_flag: tool.bracket.enabled"
    - path: "DeckFlow.Web/wwwroot/css/site-common.css"
      provides: ".bracket-badge, .bracket-violation, .bracket-stamp"
      contains: "Phase 76 — bracket tool"
    - path: "DeckFlow.Web.Tests/Bracket/BracketViewRenderTests.cs"
      provides: "Flag OFF/ON view-markup invariant"
      contains: "BracketViewRenderTests"
  key_links:
    - from: "DeckFlow.Web/Controllers/BracketController.cs"
      to: "IBracketClassificationService.ClassifyAsync"
      via: "POST action under RunGuardedAsync"
      pattern: "ClassifyAsync"
    - from: "DeckFlow.Web/Services/Tools/ToolRegistry.cs"
      to: "/bracket"
      via: "Create(\"bracket\", ... DeckPageTab.Bracket ...)"
      pattern: "\"/bracket\""
    - from: "DeckFlow.Web/Services/Tools/ToolRegistry.cs"
      to: "DeckFlow.Web/Help/bracket.md"
      via: "Create(\"bracket\", ... helpSlug \"bracket\" ...)"
      pattern: "requires_flag: tool.bracket.enabled"
---

<objective>
Build the `/bracket` surface: view models, the flag-gated `BracketController` (mirroring `ManabaseController`), the `Bracket.cshtml` page, three tokenized net-new CSS classes in `site-common.css`, the tool-registry tile/nav/icon wiring, the `Help/bracket.md` help topic, and the registry/visibility/render test updates. README updated. Live theme/mobile sign-off is the separate checkpoint plan 76-06.

Purpose: BRACKET-05 (flag-gated tool tile, nav, icon, help topic, byte-identical when OFF) and the user-facing surface for BRACKET-01/03.
Output: 2 models, 1 controller, 1 view, CSS, 3 registry-wiring edits, 1 help topic, 3 test files (render + registry + visibility), README.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/76-bracket-classifier-balancer/76-PATTERNS.md
@.planning/phases/76-bracket-classifier-balancer/UI-SPEC.md
@.planning/phases/76-bracket-classifier-balancer/76-04-SUMMARY.md

<interfaces>
IBracketClassificationService.ClassifyAsync(string deckSource, int? targetBracketNumber, string platform, string? deckName, CancellationToken) -> BracketClassificationResult (from 76-04).
BracketClassificationResult(BracketClassification Classification, IReadOnlyList<BracketTier> Tiers, string? PromptArtifact, int? TargetBracketNumber, string? ImportWarning).
DeckToolControllerBase: protected CreateTimeoutScope(LookupTimeout); LookupTimeout=20s.
FeatureFlagGateAttribute: [FeatureFlagGate("tool.bracket.enabled")] returns 404 when flag OFF.
ManabaseController.cs: RunGuardedAsync structure to copy (timeout/InvalidOperation/HttpRequest/Exception -> re-render with ErrorMessage).
ManabaseRequest.cs: DeckInputSource enum + DeckUrl/DeckText/DeckSource pattern to mirror in BracketRequest.

ToolRegistry.Create REAL signature (DeckFlow.Web/Services/Tools/ToolRegistry.cs:30-60) — verified against source:
  Create(string key, string label, string route, ToolNavSection section, string flagKey, bool core,
         string tileTitle, string tileDescription, string? helpSlug, DeckPageTab tab, bool isPrimaryTile,
         params string[] additionalRoutes)
  The 9th positional is `helpSlug` (string?), NOT an iconKey. `IconKey` is ALWAYS auto-set to `key`
  inside Create (`IconKey = key;`). So the bracket tile's icon resolves via IconKey == "bracket" with NO
  extra arg. Pass `"bracket"` for helpSlug (like `manabase`, `deck-analysis`, etc.) — ROADMAP Phase 76
  success-criterion 5 REQUIRES the bracket tool be registered with a help-topic reference. A non-null
  HelpSlug makes HelpFlagHeaderConsistencyTests require a DeckFlow.Web/Help/bracket.md file whose
  front-matter declares `requires_flag: tool.bracket.enabled` — Task 3 step (G) creates exactly that file.
DeckPageTab: existing max is Manabase=14; add Bracket=15.
_ToolTileIcon.cshtml: switch on IconKey; insert `case "bracket":` before `default:` (line ~44) — resolves because IconKey == key == "bracket".
site-common.css: .manabase-pill block ~line 2403; mobile @media (max-width:480px) ~line 2819; append Phase 76 block.
Help/*.md front-matter idiom (verified against DeckFlow.Web/Help/manabase.md) — YAML between `---` fences with
  keys `title`, `summary`, `order`, `requires_flag`. Example header verbatim:
    ---
    title: Mana Base
    summary: ...
    order: 35
    requires_flag: tool.manabase.enabled
    ---
  Bracket help file mirrors this with `requires_flag: tool.bracket.enabled`. Help content ships via the
  wildcard `<Content Include="Help\**\*.md">` in DeckFlow.Web.csproj (verified line 31) — a new file under
  Help/ is picked up automatically; NO csproj edit required.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: BracketViewModel + BracketRequest + BracketController</name>
  <read_first>
    - DeckFlow.Web/Models/ManabaseViewModel.cs (analog: view model with init props + computed helpers)
    - DeckFlow.Web/Models/ManabaseRequest.cs (analog: DeckInputSource + DeckUrl/DeckText/DeckSource)
    - DeckFlow.Web/Controllers/ManabaseController.cs (analog: GET/POST, FeatureFlagGate, ValidateAntiForgeryToken, RunGuardedAsync)
    - .planning/phases/76-bracket-classifier-balancer/76-PATTERNS.md (BracketController + BracketViewModel + BracketRequest sections)
  </read_first>
  <files>DeckFlow.Web/Models/BracketViewModel.cs, DeckFlow.Web/Models/BracketRequest.cs, DeckFlow.Web/Controllers/BracketController.cs</files>
  <action>
    Create `BracketRequest` (mirror ManabaseRequest): `DeckInputSource DeckInputSource` (reuse the existing enum), `DeckUrl`, `DeckText`, computed `DeckSource`, `string? DeckName`, `int? TargetBracketNumber`, `string TargetAiPlatform = "ChatGPT"`. Create `BracketViewModel` (`sealed`, `{ get; init; }` props): `DeckPageTab ActiveTab = DeckPageTab.Bracket`, `BracketRequest Request = new()`, `string? ErrorMessage`, `BracketClassification? Classification`, `IReadOnlyList<BracketTier>? Tiers`, `int? TargetBracketNumber`, `string? PromptArtifact`, `string? ImportWarning`; computed `HasResult => Classification is not null`, `HasTarget => TargetBracketNumber.HasValue`, `IsOverTarget => HasResult && HasTarget && Classification!.BracketNumber > TargetBracketNumber`. Create `BracketController : DeckToolControllerBase` injecting `IBracketClassificationService` + `ILogger<BracketController>` with ArgumentNull guards. GET `[HttpGet("/bracket")] [FeatureFlagGate("tool.bracket.enabled")] public IActionResult Bracket() => View("Bracket", new BracketViewModel());`. POST `[HttpPost("/bracket")] [ValidateAntiForgeryToken] [FeatureFlagGate("tool.bracket.enabled")] public async Task<IActionResult> Bracket(BracketRequest request)`: `request ??= new()`; validate `request.TargetBracketNumber` is null or in 1..5 (else set ErrorMessage and re-render without classifying); call a private `RunGuardedAsync(request, "classify", "Something went wrong classifying that deck. Please try again.", async token => { var result = await _bracketService.ClassifyAsync(request.DeckSource, request.TargetBracketNumber, request.TargetAiPlatform, request.DeckName, token); return View("Bracket", new BracketViewModel { Request = request, Classification = result.Classification, Tiers = result.Tiers, TargetBracketNumber = result.TargetBracketNumber, PromptArtifact = result.PromptArtifact, ImportWarning = result.ImportWarning }); })`. Copy the RunGuardedAsync catch structure verbatim from ManabaseController (timeout/InvalidOperation/HttpRequest via UpstreamErrorMessageBuilder/Exception), substituting "Bracket" and re-rendering with Request preserved. Structured logging only.
  </action>
  <verify>
    <automated>"/mnt/c/Program Files/dotnet/dotnet.exe" build DeckFlow.Web/DeckFlow.Web.csproj -c Debug</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -c "FeatureFlagGate(\"tool.bracket.enabled\")" DeckFlow.Web/Controllers/BracketController.cs` returns 2 (GET + POST).
    - Source assertion: controller validates target range 1..5 (contains a check rejecting out-of-range before ClassifyAsync).
    - Build: DeckFlow.Web compiles 0 errors / 0 new warnings.
  </acceptance_criteria>
  <done>Models + flag-gated, guarded controller compile; target range validated; POST calls ClassifyAsync and re-renders the view.</done>
</task>

<task type="auto">
  <name>Task 2: Bracket.cshtml view + 3 net-new site-common.css classes + README</name>
  <read_first>
    - DeckFlow.Web/Views/Deck/Manabase.cshtml (analog: hero + _BusyIndicator + _DeckToolTabs + error-banner + form.result-panel + manabase-segmented pills + result-panel + nested copy-prompt panel + _DeckFlowBridgeHint + data-sync-panel)
    - .planning/phases/76-bracket-classifier-balancer/UI-SPEC.md (§3 wireframe, §4 slot order, §5 reused classes + net-new classes, §7 exact microcopy, §8 tokens, §9 responsive, §10 a11y)
    - DeckFlow.Web/wwwroot/css/site-common.css (.manabase-chip / .manabase-health--* construction for color-mix tokenization; .manabase-pill mobile @media)
  </read_first>
  <files>DeckFlow.Web/Views/Deck/Bracket.cshtml, DeckFlow.Web/wwwroot/css/site-common.css, README.md</files>
  <action>
    Create `Views/Deck/Bracket.cshtml` `@model DeckFlow.Web.Models.BracketViewModel`. Build the slot order from UI-SPEC §4: `.hero` (h1 "Bracket Check" + `.lede` + `details.hero-detail` "How it works" using §7 body) → `_BusyIndicator` → `_DeckToolTabs` passing `Model.ActiveTab` → `.error-banner` (hidden unless ErrorMessage) → `form.result-panel` POST /bracket with antiforgery: input-method select + URL field (`_DeckFlowBridgeHint` + `data-sync-panel` show/hide copied verbatim from Manabase) + paste textarea + deck name + `fieldset.manabase-segmented` target pills iterating `Model.Tiers ?? <the 5 tiers>` as B1-B5 radios bound to `TargetBracketNumber` + AI platform select (reuse the existing platform partial/pattern) + actions `Classify deck` (`.run-button`) / `Start over` (`.clear-cache-button`). Then `@if (Model.HasResult)` a `section.result-panel data-scroll-on-load`: `.bracket-badge.bracket-badge--b@(Model.Classification!.BracketNumber)` with `__eyebrow`/`__tier`/`__name`/`__meta` (aria-label per §10), a target-comparison line (over/meets per §7), "WHY THIS BRACKET" `.manabase-verdict.manabase-verdict--issues` reasons list (GC count vs target cap, two-card combos by name, MLD names, "N extra-turn cards", a STATIC informational "0 mass extra-card draw" line with an `--extracards` tag kind — mass extra-card-draw is informational-only and covered by Game Changers per RESEARCH A2, so there is NO detected count/list backing it, it is a fixed line, NOT a computed value; and the tutors-not-counted footnote), the combo-unavailable `role="note"` disclosure when `!Classification.ComboDetectionAvailable`, then when `Model.IsOverTarget` the "FLOOR VIOLATIONS — cards that exceed B@Model.TargetBracketNumber" list of `.bracket-violation` rows (name + `.bracket-violation__tag--{gamechanger,combo,mld,extracards}`) and "STARTER CUTS" `.manabase-verdict-list`, then `.bracket-stamp` (effective-date line per §7 using `Classification.EffectiveDate`), then the `details.result-panel.nested-panel` copy-prompt block (`.copy-button[data-copy-target="#bracket-prompt"]` + `textarea#bracket-prompt readonly` = `Model.PromptArtifact`). Always-on `details.result-panel.nested-panel` "How brackets are determined". Use ONLY reused classes from §5 plus the three net-new. In `site-common.css` append a `/* Phase 76 — bracket tool */` block defining `.bracket-badge` (+ `__eyebrow/__tier/__name/__meta` + `--b1..--b5` accents via color-mix over `--panel-soft-bg` reusing the .manabase-health hex values + `--accent-strong` for b3), `.bracket-violation` (+ `__name/__tag` + kind modifiers incl `--extracards`) inside `.bracket-violation-list`, and `.bracket-stamp` — every color references an existing `var(--…)` token or the four baked health hex values (no new raw colors); include the §9 responsive rules (`.bracket-violation` flex-wrap below 480px). Do NOT edit any theme `site-*.css` fork. Update README.md: add the Bracket Check tool to the tools list/section describing classify + balancer + flag `tool.bracket.enabled` (OFF by default).
  </action>
  <verify>
    <automated>"/mnt/c/Program Files/dotnet/dotnet.exe" build DeckFlow.Web/DeckFlow.Web.csproj -c Debug && grep -c "Phase 76 — bracket tool" DeckFlow.Web/wwwroot/css/site-common.css</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `Bracket.cshtml` contains `bracket-badge`, `bracket-violation`, `bracket-stamp`, and `data-copy-target="#bracket-prompt"`.
    - Source assertion: `Bracket.cshtml` renders a STATIC extra-card-draw line (contains "extra-card draw" / `--extracards`) with no model-bound count — informational-only by design (RESEARCH A2).
    - Source assertion: net-new CSS lives only in site-common.css — `grep -rl "bracket-badge" DeckFlow.Web/wwwroot/css/ | grep -v site-common.css` returns nothing.
    - Source assertion: site-common.css bracket block introduces no raw hex outside the existing health palette (review: every `#` hex equals one already used by `.manabase-health--*`).
    - Build clean; the `Phase 76 — bracket tool` marker count is 1.
  </acceptance_criteria>
  <done>Bracket page renders the full result surface (incl. the static informational extra-card-draw line), 3 tokenized classes live in site-common.css only, README documents the tool.</done>
</task>

<task type="auto">
  <name>Task 3: Tool-registry tile/nav/icon wiring + help topic + registry/visibility/render test updates</name>
  <read_first>
    - DeckFlow.Web/Services/Tools/ToolRegistry.cs (the manabase Create row to mirror; Analyze section; note Create 9th positional is helpSlug, IconKey = key auto)
    - DeckFlow.Web/Models/DeckPageTab.cs (existing values; append Bracket=15, do not renumber)
    - DeckFlow.Web/Views/Shared/_ToolTileIcon.cshtml (switch; insert case "bracket" before default — SVG from UI-SPEC §2 / 76-PATTERNS)
    - DeckFlow.Web/Help/manabase.md (analog: a flagged tool's help topic — copy the `---`-fenced front-matter idiom incl. `requires_flag: tool.manabase.enabled`; the bracket version uses `tool.bracket.enabled`)
    - DeckFlow.Web.Tests/Tools/ToolRegistryTests.cs (All_ReturnsExpectedToolDefinitions Collection + AssertTool signature; All_HasUniqueKeys...AndExactlyThreeCoreTools counts)
    - DeckFlow.Web.Tests/Tools/ToolVisibilityTests.cs (VisibleBySection_AllEnabled Analyze order + Sum; VisibleBySection_OmitsSectionsWithNoVisibleTools disabled-cache)
    - DeckFlow.Web.Tests/Tools/HelpFlagHeaderConsistencyTests.cs (iterates tools where HelpSlug is not null — bracket now has HelpSlug "bracket", so Help/bracket.md MUST exist and declare requires_flag: tool.bracket.enabled)
    - DeckFlow.Web.Tests/Manabase/ManabaseViewRenderTests.cs (analog: IRazorViewEngine render scaffold + FakeFeatureFlagCache + TestWebHostEnvironment + StubTempDataProvider — copy verbatim, substitute Bracket)
  </read_first>
  <files>DeckFlow.Web/Services/Tools/ToolRegistry.cs, DeckFlow.Web/Models/DeckPageTab.cs, DeckFlow.Web/Views/Shared/_ToolTileIcon.cshtml, DeckFlow.Web/Help/bracket.md, DeckFlow.Web.Tests/Bracket/BracketViewRenderTests.cs, DeckFlow.Web.Tests/Tools/ToolRegistryTests.cs, DeckFlow.Web.Tests/Tools/ToolVisibilityTests.cs</files>
  <action>
    (A) Add to ToolRegistry `Definitions`, immediately after the `manabase` row:
    `Create("bracket", "Bracket", "/bracket", ToolNavSection.Analyze, "tool.bracket.enabled", false, "Bracket Check", "Classify a Commander deck into its official 1-5 bracket from Game Changers, two-card combos, and mass-land-denial — then generate a balancer prompt to hit a target bracket. No tutor-counting.", "bracket", DeckPageTab.Bracket, false),`.
    NOTE: the 9th positional is `"bracket"` (helpSlug) — ROADMAP Phase 76 success-criterion 5 REQUIRES the bracket tool be registered with a help-topic reference, mirroring `manabase`/`deck-analysis`. This makes HelpFlagHeaderConsistencyTests require `DeckFlow.Web/Help/bracket.md` with a `requires_flag: tool.bracket.enabled` header — step (G) creates that file. The tile icon resolves via `IconKey`, which Create auto-sets to the key ("bracket"); no iconKey argument exists.
    (B) Add `Bracket = 15,` to DeckPageTab.cs after `Manabase = 14` with an XML summary (do not renumber existing).
    (C) Add `case "bracket":` with the 5-step-staircase SVG (UI-SPEC §2 / 76-PATTERNS _ToolTileIcon section) before `default:` in _ToolTileIcon.cshtml.
    (D) FIX 4 — update ToolRegistryTests.cs: in `All_ReturnsExpectedToolDefinitions` add a `tool => AssertTool(...)` lambda for bracket immediately after the manabase lambda, with helpSlug `"bracket"` and `false` core: `AssertTool(tool, "bracket", "Bracket", "/bracket", ToolNavSection.Analyze, "tool.bracket.enabled", false, "Bracket Check", "Classify a Commander deck into its official 1-5 bracket from Game Changers, two-card combos, and mass-land-denial — then generate a balancer prompt to hit a target bracket. No tutor-counting.", "bracket", DeckPageTab.Bracket, false)`. In `All_HasUniqueKeysRoutesAndTabs_AndExactlyThreeCoreTools` bump the four `Assert.Equal(13, ...)` to `14` (Count, distinct Keys, distinct Routes, distinct Tabs) and the `Assert.Equal(18, ...)` distinct routes+additionalRoutes to `19` (bracket adds one new route, no additional routes); leave the coreKeys assertion unchanged (bracket core=false).
    (E) FIX 4 — update ToolVisibilityTests.cs: in `VisibleBySection_AllEnabled_ReturnsAllSectionsInOrder` add `"bracket"` to the Analyze `AssertSection(...)` expected keys after `"manabase"`, and bump `Assert.Equal(13, sections.Sum(...))` to `14`. In `VisibleBySection_OmitsSectionsWithNoVisibleTools` add `["tool.bracket.enabled"] = false` to the disabled-cache dictionary (alongside the other four Analyze flags) so the Analyze section is still omitted.
    (F) Create `BracketViewRenderTests` (namespace DeckFlow.Web.Tests) by copying the ManabaseViewRenderTests render scaffold (RenderBracketViewAsync + StubTempDataProvider + TestWebHostEnvironment + CreateHostingEnvironment, registering ToolRegistry + FakeFeatureFlagCache), with two `[Fact]`s: `OffState_NoClassification_RendersNoBracketBadge` (model = `new BracketViewModel()` → assert `Assert.DoesNotContain("bracket-badge", html, Ordinal)`), and `OnState_WithClassification_RendersBracketBadge` (model with a B4 BracketClassification + Tiers → assert contains `bracket-badge` and `bracket-badge--b4`). The TestWebHostEnvironment ContentRootPath must point at the DeckFlow.Web project dir so `_DeckToolTabs`/partials resolve (mirror Manabase test).
    (G) Create `DeckFlow.Web/Help/bracket.md` — a help topic for the Bracket Check tool. It MUST begin with `---`-fenced YAML front-matter copying the manabase.md idiom EXACTLY, with these keys: `title: Bracket Check`, a one-line `summary`, an `order` value that fits the Help ordering (use a value after manabase's 35, e.g. `order: 36`), and the REQUIRED `requires_flag: tool.bracket.enabled` (this is the key HelpFlagHeaderConsistencyTests asserts equals the tool FlagKey). Body (concise, matching other Help/*.md tone/structure): a `# Bracket Check` heading; what it does — classifies a Commander deck into the official 1-5 bracket from Game Changers, two-card combos, mass land denial, and extra turns; tutors are NOT counted; pick a target bracket to get floor violations + starter cuts rendered as a copy-ready AI prompt; the verdict itself is computed in DeckFlow (the AI prompt is only for the balancer cuts). Close with the standard "this tool can be turned off by an administrator; when it is, this help topic is hidden too." note used by other topics. NO csproj edit is needed — the `<Content Include="Help\**\*.md">` wildcard (DeckFlow.Web.csproj line 31) picks up the new file automatically.
  </action>
  <verify>
    <automated>"/mnt/c/Program Files/dotnet/dotnet.exe" test DeckFlow.Web.Tests/DeckFlow.Web.Tests.csproj --filter "FullyQualifiedName~BracketViewRenderTests|FullyQualifiedName~ToolRegistryTests|FullyQualifiedName~ToolVisibilityTests|FullyQualifiedName~HelpFlagHeaderConsistencyTests"</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: ToolRegistry.cs contains `Create("bracket"` with `"/bracket"`, `ToolNavSection.Analyze`, `"tool.bracket.enabled"`, `"bracket"` in the helpSlug slot, and `DeckPageTab.Bracket`.
    - Source assertion: DeckPageTab.cs contains `Bracket = 15,` and Manabase=14 is unchanged.
    - Source assertion: `DeckFlow.Web/Help/bracket.md` exists and its front-matter contains `requires_flag: tool.bracket.enabled` (e.g. `grep -c "requires_flag: tool.bracket.enabled" DeckFlow.Web/Help/bracket.md` returns 1).
    - Test command: ToolRegistryTests (count 14 + bracket AssertTool row, helpSlug "bracket") and ToolVisibilityTests (Analyze includes bracket; Sum 14; omit-section still omits Analyze) pass.
    - Test command: HelpFlagHeaderConsistencyTests passes WITH the Help/bracket.md file present — proves bracket HelpSlug "bracket" resolves to a real help file declaring requires_flag: tool.bracket.enabled.
    - Test command: both BracketViewRenderTests facts pass (OFF → no badge markup; ON → bracket-badge + bracket-badge--b4).
  </acceptance_criteria>
  <done>Tool tile/nav/icon registered (HelpSlug "bracket", IconKey auto "bracket"), enum extended, Help/bracket.md created with the requires_flag header, registry+visibility contract tests updated to 14 tools, and the flag-OFF/ON view-markup invariant is proven by a passing render test.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → POST /bracket | Untrusted form post (deck source, target bracket, platform) |
| flag state → route exposure | tool.bracket.enabled controls whether /bracket exists |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-76-11 | Tampering (CSRF) | POST /bracket | mitigate | [ValidateAntiForgeryToken] on POST (same as ManabaseController); form emits antiforgery token. |
| T-76-12 | Tampering | out-of-range TargetBracketNumber | mitigate | Controller validates 1..5 before ClassifyAsync; out-of-range re-renders with ErrorMessage, no classification. |
| T-76-13 | Information Disclosure / EoP | tool reachable when flag OFF | mitigate | [FeatureFlagGate("tool.bracket.enabled")] 404s both actions; ToolRegistry filters disabled tools from nav/tiles; the help topic is hidden when the flag is OFF (requires_flag: tool.bracket.enabled); BracketViewRenderTests proves no badge markup without a classification. Byte-identity of other pages preserved (no theme fork; CSS additive but only referenced by /bracket markup). |
| T-76-14 | Tampering | XSS via deck name / card names in view | mitigate | Razor auto-encodes model output; no `Html.Raw` on user/card strings; artifact rendered inside readonly textarea. |
| T-76-SC | Tampering | npm/pip/cargo installs | accept | No package installs; in-solution only. |
</threat_model>

<verification>
- `dotnet build DeckFlow.Web/DeckFlow.Web.csproj` clean.
- `dotnet test --filter "FullyQualifiedName~BracketViewRenderTests|FullyQualifiedName~ToolRegistryTests|FullyQualifiedName~ToolVisibilityTests|FullyQualifiedName~HelpFlagHeaderConsistencyTests"` green.
- Full suite `dotnet test DeckFlow.sln` green before phase gate.
- Net-new CSS only in site-common.css; no theme fork edited.
- Help/bracket.md present with `requires_flag: tool.bracket.enabled` header (HelpFlagHeaderConsistencyTests).
</verification>

<success_criteria>
- BRACKET-05: flag-gated tile + nav + DeckPageTab + icon (IconKey auto "bracket") + help topic (HelpSlug "bracket" → Help/bracket.md with requires_flag: tool.bracket.enabled) registered; /bracket 404s when OFF; HelpFlagHeaderConsistencyTests passes with the help file present; view renders no badge markup without a classification (byte-identity guard); CSS additive in site-common.css; registry/visibility contract tests updated to 14 tools.
- BRACKET-01/03 (surface): the page renders badge + reasons + violations + cuts + stamp + copy-prompt artifact, with mass extra-card-draw as a static informational line (RESEARCH A2).
</success_criteria>

<output>
Create `.planning/phases/76-bracket-classifier-balancer/76-05-SUMMARY.md` when done.
</output>
</content>
</invoke>
