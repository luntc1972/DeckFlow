---
phase: 102-structural-analysis-role-floors
plan: 04
type: execute
wave: 3
depends_on: [102-03]
files_modified:
  - DeckFlow.Web/Views/Deck/CutLab.cshtml
  - DeckFlow.Web/wwwroot/css/site-common.css
  - DeckFlow.Web/wwwroot/ts/cut-lab.ts
  - DeckFlow.Web/ts-tests/cut-lab-lock-interactions.test.ts
  - DeckFlow.Web/e2e/cut-lab-smoke.spec.ts
  - DeckFlow.Web/Services/CutLab/CutLabLockRules.cs
  - DeckFlow.Web.Tests/CutLabRoleGroupLockTests.cs
autonomous: true
requirements: [SLOT-01, SLOT-02, FLOOR-01, FLOOR-02]
must_haves:
  truths:
    - "The Cut Lab results view renders three new sections in order: 'How your pool competes' (8 display-only role accordions), 'Structural findings', and 'Role floors' (8-row editor table + Recalculate analysis button)"
    - "The pool table stays the single canonical lock surface; role-group chips are display-only and reflect lock state from it"
    - "The standalone 'Lock all lands' toolbar pill is gone; each role group's body carries one 'Lock all {role}' pill driving the pool-table checkboxes"
    - "data-cut-lab-role carries a space-separated multi-role token list and the TS matches by token, keeping bulk land-lock behavior working"
    - "Editing a floor number input marks the row Adjusted, updates the at-floor marker live, and serializes roleFloors into CutLabStateJson in the pinned camelCase contract"
    - "Findings render with the gold-warning advisory idiom, never --danger; degraded sources render the muted UI-SPEC copy lines"
    - "The misleading Manabase castability help line is replaced with the UI-SPEC play-experience copy; cut-lab.ts no longer hard-codes form[action=\"/cut-lab\"]"
    - "The test-only CutLabLockRules.BulkLockRoleGroup server path is removed (PATTERNS extend-or-replace directive) — cut-lab.ts lockAllRole is the single bulk-lock driver and IsLand coverage is retained"
  artifacts:
    - path: "DeckFlow.Web/Views/Deck/CutLab.cshtml"
      provides: "Three new result sections + pool-table role-list column + copy amendment"
      contains: "How your pool competes"
    - path: "DeckFlow.Web/wwwroot/ts/cut-lab.ts"
      provides: "Floor editor serialization, token-based role matching, per-group bulk lock, data-hook form lookup"
      contains: "roleFloors"
    - path: "DeckFlow.Web/wwwroot/css/site-common.css"
      provides: ".cutlab-role-group / .cutlab-role-chip--locked / .cutlab-finding / .cutlab-floor-state--at classes"
      contains: "cutlab-finding"
  key_links:
    - from: "CutLab.cshtml floor inputs"
      to: "cut-lab.ts buildCutLabStateJson"
      via: "data-cut-lab-floor attribute → roleFloors camelCase serialization"
      pattern: "data-cut-lab-floor"
    - from: "CutLab.cshtml pool rows"
      to: "cut-lab.ts token role matching"
      via: "space-separated data-cut-lab-role list"
      pattern: "data-cut-lab-role"
    - from: "role-group pills"
      to: "pool-table checkboxes"
      via: "single lock source (Pitfall 8)"
      pattern: "data-cut-lab-lock-role"
---

<objective>
Render Phase 102's structural read: the three new Razor sections per the approved 102-UI-SPEC
(slot-competition accordions, structural findings, role-floor editor), the pool-table multi-role
upgrade, the cut-lab.ts extensions (floor serialization, token role matching, per-group bulk lock,
recalculate CTA), the site-common.css classes — and update the Vitest + smoke-e2e fixtures that pin
the changed surfaces, in this same plan (they assert data-cut-lab-lock-all-lands and
data-cut-lab-role="land", which this plan changes).

Purpose: This is the user-visible half of SLOT-01/02 and FLOOR-01/02, behind the still-OFF
tool.cut-lab.enabled flag. 101-VERIFICATION open items 2 (hard-coded form selector) and 3
(castability copy) are folded in because this plan owns those exact surfaces.
Output: extended CutLab.cshtml, cut-lab.ts, site-common.css, updated cut-lab-lock-interactions
Vitest, updated cut-lab-smoke e2e.

BINDING UI decisions (102-UI-SPEC, approved 6/6): role groups display-only + collapsed by default
(A1/A2); native number inputs, no custom stepper (A5); Recalculate via form.requestSubmit() (A6);
standalone lands pill removed (A7); findings summary chip is the results focal point (A8);
multi-role token attribute (A9); gold-warning advisory chain, zero --danger surfaces (A4); zero
new :root tokens; layout CSS in site-common.css ONLY. Compiled wwwroot/js/*.js is gitignored —
never staged.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/cut-lab/phases/102-structural-analysis-role-floors/102-UI-SPEC.md
@.planning/workstreams/cut-lab/phases/102-structural-analysis-role-floors/102-PATTERNS.md
@.planning/workstreams/cut-lab/phases/102-structural-analysis-role-floors/102-RESEARCH.md

<interfaces>
<!-- View model (plan 03): RoleGroups (RoleKey, DisplayLabel, Members(Name,IsLocked,IsCommander),
  LockedCount), Findings (Heading, Lead, Evidence), ComboDataUnavailable, CategoryDataUnavailable,
  FloorRows (RoleKey, DisplayLabel, InPoolCount, Floor, DefaultValue, IsUserSet, AtFloor,
  SourceLabel), RoleListByCardName (display " · " joined), RoleKeysByCardName (space-separated
  keys for the attribute). Read Models/CutLabViewModel.cs for exact names before templating. -->

<!-- CutLab.cshtml anchors (current): form data-cache-key="cut-lab" + data-busy-* at :22-28;
  standalone Lock-all-lands pill at :171-175 (REMOVE); pool row data-cut-lab-role single "land"
  value at :190-194 (UPGRADE); Type/role cell at :214-219 (REPLACE with role list); castability
  help line at :100 (REPLACE); result-panel/panel-heading shell at :124-131; package member chips
  at :272-277. -->

<!-- cut-lab.ts anchors: api.isLandRole exact-equality at :65-67 (→ token matching);
  buildCutLabStateJson at :69-94 (add roleFloors); getForm hard-coded selector at :102-103
  (→ data-cache-key hook); lockAllLands at :351-367 (→ generalize per-role); refreshAndSerialize
  funnel at :544-548; delegated handlers at :568-624 (lock-all-lands click at :591). -->

<!-- CSS recipes (102-PATTERNS, verbatim sources): advisory block idiom site-common.css:2849-2852
  (border-left 3px var(--gold-warning, var(--warning, #c8a040)); background color-mix 10% over
  var(--panel-soft-bg, var(--panel))); accordion summary :495-510; chips :546-580; pill :~2495-2515;
  existing cutlab block :4141-4147 (add new classes beside it); responsive table data-label pattern
  :1154-1263 via table[data-prompt-cedh-reference-table]. -->

<!-- Serialized floor contract (plans 01/03): CutLabState.RoleFloors → JSON member "roleFloors":
  [{"role":"interaction","floor":15,"isUserSet":true}] (JsonSerializerDefaults.Web camelCase).
  Only user-adjusted floors are serialized; the server re-clamps on POST. -->

<!-- Vitest fixtures to update (ts-tests/cut-lab-lock-interactions.test.ts): isLandRole expectations
  at :56-61; lock-all-lands button at :107, :158; data-cut-lab-role="land" rows at :110-130;
  byte-exact camelCase contract test at :63-96. E2e assertions to update
  (e2e/cut-lab-smoke.spec.ts): [data-cut-lab-lock-all-lands] at :62 and :99. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Razor — three new sections, pool-table role upgrade, copy fixes, CSS</name>
  <files>DeckFlow.Web/Views/Deck/CutLab.cshtml, DeckFlow.Web/wwwroot/css/site-common.css</files>
  <read_first>
    - DeckFlow.Web/Views/Deck/CutLab.cshtml (FULL read — current sections, the anchors listed in interfaces)
    - DeckFlow.Web/Models/CutLabViewModel.cs (exact member names from plan 03)
    - DeckFlow.Web/wwwroot/css/site-common.css lines 495-580, 1154-1263, 2495-2515, 2849-2852, 2977-2980, 4141-4147 (recipes to copy; add new classes beside the existing cutlab block)
    - .planning/workstreams/cut-lab/phases/102-structural-analysis-role-floors/102-UI-SPEC.md (Copywriting Contract + three Component Contracts — copy strings are prescriptive, use them verbatim)
    - DeckFlow.Web/Views/Deck/Manabase.cshtml line 125 (sitewide details/summary precedent)
  </read_first>
  <action>
    In CutLab.cshtml, inside the existing results branch after the lock panel, add three
    `<section class="result-panel">` blocks per the UI-SPEC Component Contracts, rendered only when the
    pool imported successfully:
      1. "How your pool competes" (h2) + sub-line "Cards grouped by the role they're fighting for. A card
         can fill more than one role — it appears in every group it competes in." Then eight
         `<details class="cutlab-role-group">` in fixed order (Model.RoleGroups), collapsed (no `open`).
         Summary: "{DisplayLabel} · {count} cards · {lockedCount} locked". Body: one
         `.manabase-pill` button per group — text "Lock all {display label, lowercase}" — with
         `data-cut-lab-lock-role="{RoleKey}"` and `type="button"`, then a `.kb-chip-area__chips` flow of
         `.kb-chip` per member (class `cutlab-role-chip--locked` added when IsLocked; commander chip keeps
         its existing badge treatment). Empty group body: "No {role} cards detected in this pool."
         Under the Interaction group add the help line: "Interaction counts one-shot removal and wipes.
         Counterspells count as interaction only for a cEDH play experience."
      2. "Structural findings" (h2) + sub-line "Measured observations about your pool's shape. Nothing
         here says a card is bad — it says what the numbers show." Panel-heading chip: "{n} structural
         findings" (`.prompt-size-note`). Each finding: `<div class="cutlab-finding">` with Heading
         (Label size), Lead, evidence chips (curve chips already carry "· MV n" from the view model).
         Zero findings: "No structural issues found. Your pool's curve, themes, finishers, and role
         coverage all look self-supporting at the current floors." Degraded sources: when
         Model.ComboDataUnavailable render the muted line "Combo data unavailable right now —
         win-condition and enabler-starved reads are incomplete. Re-run the import to try again."; when
         Model.CategoryDataUnavailable render "Community category data unavailable — subtheme detection
         fell back to card-text heuristics only." (Body size, --muted, inside the affected area — never
         an error banner).
      3. "Role floors" (h2) + sub-line "Minimum counts the finished 100 should keep, derived from your
         bracket and play experience. Adjust any floor — later cut suggestions will warn you before
         breaking one, never silently." A responsive `<table data-prompt-cedh-reference-table>` with
         columns Role · In pool · Floor · Source and eight rows (Model.FloorRows): In-pool cell
         "{count} in pool" plus, when AtFloor, "· at floor" in `<span class="cutlab-floor-state--at">`;
         Floor cell `<input type="number" min="0" max="@Model.CardCount" step="1"
         data-cut-lab-floor="@row.RoleKey" value="@row.Floor">`; Source cell: SourceLabel when
         !IsUserSet, else a `.kb-chip cutlab-floor-badge--adjusted` reading "Adjusted" plus a text
         button "Reset to default" with `data-cut-lab-floor-reset="@row.RoleKey"`
         `data-cut-lab-floor-default="@row.DefaultValue"` (no confirmation). Below the table, a
         `.toolbar` with `<button type="button" data-cut-lab-recalculate>Recalculate analysis</button>`.
    Pool-table amendments: change the row attribute to
    `data-cut-lab-role="@Model.RoleKeysByCardName[name]"` (space-separated keys, empty when none);
    replace the Type/role cell contents at :214-219 with the " · "-joined display list from
    Model.RoleListByCardName (Body size, --muted); DELETE the standalone Lock-all-lands pill block at
    :171-175. Replace the help line at :100 with: "Your play experience sets how roles are read
    (counterspells count as interaction only in cEDH) and, with your bracket, sets the default role
    floors below." Consolidate the lock-count chip copy so the "(protected from any future cut)" string
    exists once in the view (TS updates only the numbers — finishing open item 1).

    In site-common.css, beside the existing cutlab block (~:4141), add: `.cutlab-role-group` (accordion
    idiom: --panel background, --line border, summary cursor/padding/weight per :495-510, [open] summary
    border-bottom, 44px min summary hit area); `.cutlab-role-chip--locked { border-left: 3px solid
    var(--accent); }`; `.cutlab-finding` (the :2849-2852 advisory recipe verbatim: gold-warning left
    border + 10% color-mix background + var(--ink, inherit)); `.cutlab-floor-state--at { color:
    var(--gold-warning, var(--warning, #c8a040)); font-weight: 600; }`;
    `.cutlab-floor-badge--adjusted { border-left: 3px solid var(--accent); }`. Floor inputs and pills
    reuse existing .field input / .manabase-pill rules — no new :root tokens, no --danger, no
    --theme-surface, nothing in site.css.
  </action>
  <verify>
    <automated>dotnet build DeckFlow.Web/DeckFlow.Web.csproj -c Debug --nologo -clp:ErrorsOnly</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'How your pool competes' DeckFlow.Web/Views/Deck/CutLab.cshtml` returns 1; `grep -c 'Structural findings' DeckFlow.Web/Views/Deck/CutLab.cshtml` returns ≥1; `grep -c 'Role floors' DeckFlow.Web/Views/Deck/CutLab.cshtml` returns ≥1
    - `grep -c 'data-cut-lab-lock-all-lands' DeckFlow.Web/Views/Deck/CutLab.cshtml` returns 0 (standalone pill removed) and `grep -c 'data-cut-lab-lock-role' DeckFlow.Web/Views/Deck/CutLab.cshtml` returns ≥1
    - `grep -c 'data-cut-lab-floor=' DeckFlow.Web/Views/Deck/CutLab.cshtml` returns ≥1 and `grep -c 'data-cut-lab-recalculate' DeckFlow.Web/Views/Deck/CutLab.cshtml` returns 1
    - `grep -c 'castability table' DeckFlow.Web/Views/Deck/CutLab.cshtml` returns 0 (open item 3 fixed)
    - `grep -c 'cutlab-finding' DeckFlow.Web/wwwroot/css/site-common.css` returns ≥1 and `grep -c 'cutlab-role-group' DeckFlow.Web/wwwroot/css/site-common.css` returns ≥1
    - `grep -c 'var(--danger' DeckFlow.Web/wwwroot/css/site-common.css` count unchanged from HEAD (zero new danger surfaces) and no edits to DeckFlow.Web/wwwroot/css/site.css
    - Web project builds clean (Razor compiles against the plan-03 view model)
  </acceptance_criteria>
  <done>The dark Cut Lab page renders the full Phase 102 structural read with UI-SPEC-verbatim copy, advisory-gold findings, an eight-row floor editor, a multi-role pool table, and the folded-in Phase 101 copy fixes — styled entirely by site-common.css additions using existing tokens.</done>
</task>

<task type="auto">
  <name>Task 2: cut-lab.ts — floor serialization, token roles, per-group bulk lock, form hook</name>
  <files>DeckFlow.Web/wwwroot/ts/cut-lab.ts</files>
  <read_first>
    - DeckFlow.Web/wwwroot/ts/cut-lab.ts (FULL read — api object, snapshot interfaces, buildCutLabStateJson, getForm, lockAllLands, refreshAndSerialize, delegated handlers)
    - DeckFlow.Web/ts-tests/cut-lab-lock-interactions.test.ts (the camelCase contract test pinning buildCutLabStateJson output — the extension must slot into that exact string shape)
    - DeckFlow.Web/Views/Deck/CutLab.cshtml (Task 1 output — attribute names to bind: data-cut-lab-floor, data-cut-lab-floor-reset, data-cut-lab-floor-default, data-cut-lab-lock-role, data-cut-lab-recalculate, data-cache-key)
  </read_first>
  <action>
    Extend cut-lab.ts (strict TS, no `any`, module "none" — same file conventions):
      - Snapshot types: add `roleFloors: Array<{ role: string; floor: number; isUserSet: boolean }>` to
        CutLabStateSnapshot and a floor row shape to the DOM-read state.
      - `buildCutLabStateJson`: serialize `roleFloors` after the existing members with explicit
        field-by-field normalization (role, floor as integer, isUserSet boolean) so the JSON member names
        match the C# JsonSerializerDefaults.Web camelCase contract exactly. Only rows the user has
        adjusted (isUserSet) are included — mirroring the server's persistence rule.
      - Replace `isLandRole` with token matching: rename semantics to a role-token check (keep an exported
        api function; e.g. `hasRoleToken(roleList: string | null | undefined, role: string): boolean`)
        that splits the space-separated data-cut-lab-role value and compares tokens case-insensitively.
        Keep isLandRole as a thin wrapper over hasRoleToken(list, 'lands') if removing it would churn the
        api surface — decide from the Vitest updates in Task 3, but the exported api MUST expose the
        token matcher.
      - Generalize `lockAllLands` to `lockAllRole(roleKey: string)`: iterate pool-table rows, match via
        the token check, set checkboxes, funnel through refreshAndSerialize(). Delegate clicks from
        `[data-cut-lab-lock-role]` buttons (replacing the removed lock-all-lands handler at :591). Pill
        pressed/active state reflects "every non-disabled member locked" as before.
      - Floor editor bindings: on `change`/`input` of `[data-cut-lab-floor]` inputs — clamp the value
        client-side to [0, max attr] for UX (server re-clamps authoritatively), mark the row user-set
        (e.g. `data-cut-lab-floor-user-set="true"` and toggle the Adjusted badge/reset button visibility),
        recompute the at-floor marker for that row (count ≤ floor + 1 → show `.cutlab-floor-state--at`
        text "· at floor", else plain), and refreshAndSerialize(). On `[data-cut-lab-floor-reset]` click —
        restore the input to data-cut-lab-floor-default, clear the user-set flag, update marker, and
        refreshAndSerialize().
      - Recalculate CTA: click on `[data-cut-lab-recalculate]` → getForm()?.requestSubmit() (the existing
        submit listener re-serializes state; busy indicator flows from the existing data-busy-* config).
      - `getForm` (open item 2): replace the `'form[action="/cut-lab"]'` selector with
        `'form[data-cache-key="cut-lab"]'` (attribute already present at CutLab.cshtml:23).
    No DOM logic renders findings or role groups — they are server-rendered; TS only handles locks,
    floors, and the recalculate submit (RESEARCH anti-pattern: no client-side classification).
  </action>
  <verify>
    <automated>cd DeckFlow.Web &amp;&amp; npx --no-install tsc -p tsconfig.json --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'roleFloors' DeckFlow.Web/wwwroot/ts/cut-lab.ts` returns ≥2 (type + serialization)
    - `grep -c 'form\[action' DeckFlow.Web/wwwroot/ts/cut-lab.ts` returns 0 and `grep -c 'data-cache-key' DeckFlow.Web/wwwroot/ts/cut-lab.ts` returns ≥1 (open item 2 fixed)
    - `grep -c 'requestSubmit' DeckFlow.Web/wwwroot/ts/cut-lab.ts` returns ≥1
    - `grep -c ': any' DeckFlow.Web/wwwroot/ts/cut-lab.ts` returns 0
    - Token matcher exported on the api object (`grep -c 'hasRoleToken' DeckFlow.Web/wwwroot/ts/cut-lab.ts` returns ≥2 — declaration + api wiring)
    - `tsc --noEmit` passes strict; compiled wwwroot/js/cut-lab.js NOT staged (`git status --porcelain | grep -c 'wwwroot/js/'` returns 0)
  </acceptance_criteria>
  <done>Floor edits, per-group bulk locks, and the recalculate CTA all funnel through the single refreshAndSerialize path with roleFloors riding the pinned camelCase contract, token-based role matching, and no hard-coded form path.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Update Vitest + smoke-e2e fixtures pinning the changed surfaces</name>
  <files>DeckFlow.Web/ts-tests/cut-lab-lock-interactions.test.ts, DeckFlow.Web/e2e/cut-lab-smoke.spec.ts</files>
  <behavior>
    - Vitest: token matcher truth table — hasRoleToken('lands ramp','lands')=true, ('lands ramp','ramp')=true, ('land','lands')=false (old singular value no longer matches), (null,'lands')=false, ('LANDS','lands')=true
    - Vitest: byte-exact camelCase contract test extended — a snapshot with one user-set floor serializes with `"roleFloors":[{"role":"interaction","floor":15,"isUserSet":true}]` in the full-string equality assertion; a snapshot with no user-set floors serializes `"roleFloors":[]`
    - Vitest DOM harness: fixtures use data-cut-lab-role="lands" (and a multi-token row like "draw engines"), a per-group `[data-cut-lab-lock-role="lands"]` button instead of the removed lock-all-lands button; clicking it locks exactly the lands rows; a floor input change marks user-set and lands in the parsed hidden-field JSON
    - e2e smoke: the two [data-cut-lab-lock-all-lands] assertions (:62, :99) target `[data-cut-lab-lock-role="lands"]` inside the Lands role group (open the details first), and the bulk-land-lock persistence flow still passes end-to-end
  </behavior>
  <read_first>
    - DeckFlow.Web/ts-tests/cut-lab-lock-interactions.test.ts (FULL read — the fixtures and full-string contract assertions being updated)
    - DeckFlow.Web/e2e/cut-lab-smoke.spec.ts (FULL read — the flag/admin-lock harness and the two pill assertions)
    - DeckFlow.Web/wwwroot/ts/cut-lab.ts (Task 2 output — exact api surface and attribute bindings)
    - DeckFlow.Web/Views/Deck/CutLab.cshtml (Task 1 output — real attribute names for realistic fixtures)
  </read_first>
  <action>
    Update cut-lab-lock-interactions.test.ts: rewrite the isLandRole expectations (:56-61) to the token
    matcher truth table above; extend the byte-exact buildCutLabStateJson test (:63-96) with the
    roleFloors member (full-string equality, not shape matching — compute the exact expected JSON from
    the Task 2 serialization order); update the DOM harness (:98-205) fixtures to multi-token
    data-cut-lab-role values and the per-group `[data-cut-lab-lock-role]` button; add a floor-input
    interaction case (dispatch change on `[data-cut-lab-floor="interaction"]`, assert the Adjusted state
    hook and the parsed hidden-field JSON contains the floor entry). Keep the side-effect import and
    globalThis.DeckFlowCutLab api handle pattern.

    Update cut-lab-smoke.spec.ts: replace the `[data-cut-lab-lock-all-lands]` visibility assertion (:62)
    and click (:99) with opening the Lands `details.cutlab-role-group` (click its summary) and using
    `[data-cut-lab-lock-role="lands"]`; keep every other assertion (import, commander disabled+checked,
    legality line, persistence regex, flag-OFF 404) untouched. Do not add new spec files here (plan 05
    owns the new structure spec).

    Run Vitest for this file and the full smoke spec via the headless harness (scripts/run-web-test.sh
    starts the server with DECKFLOW_DISABLE_AUTO_BROWSER=true; probe for a stale Windows server on 5173
    first per project memory).
  </action>
  <verify>
    <automated>cd DeckFlow.Web &amp;&amp; npx --no-install vitest run ts-tests/cut-lab-lock-interactions.test.ts &amp;&amp; env -u DISPLAY -u WAYLAND_DISPLAY npx --no-install playwright test e2e/cut-lab-smoke.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'data-cut-lab-lock-all-lands' DeckFlow.Web/ts-tests/cut-lab-lock-interactions.test.ts` returns 0 and `grep -c 'data-cut-lab-lock-all-lands' DeckFlow.Web/e2e/cut-lab-smoke.spec.ts` returns 0
    - `grep -c 'roleFloors' DeckFlow.Web/ts-tests/cut-lab-lock-interactions.test.ts` returns ≥2 (contract + DOM cases)
    - The full-string contract assertion contains "isUserSet" (camelCase pinned byte-exact)
    - Vitest file passes; cut-lab-smoke.spec.ts passes end-to-end via the headless harness (no Windows browser)
    - `dotnet test DeckFlow.Web.Tests --filter CutLab` still fully green (no server-side regressions from view changes)
  </acceptance_criteria>
  <done>Every existing contract pin (camelCase JSON, land bulk-lock, persistence, flag gating) is updated in lockstep with the changed surfaces and green, so plan 05 inherits a passing suite.</done>
</task>

<task type="auto">
  <name>Task 4: Remove the test-only BulkLockRoleGroup server path (pattern-compliance cleanup)</name>
  <files>DeckFlow.Web/Services/CutLab/CutLabLockRules.cs, DeckFlow.Web.Tests/CutLabRoleGroupLockTests.cs</files>
  <read_first>
    - DeckFlow.Web/Services/CutLab/CutLabLockRules.cs (BulkLockRoleGroup at :99-118; do NOT touch EnforceCommanderLock, the package lock methods, or IsLand)
    - DeckFlow.Web.Tests/CutLabRoleGroupLockTests.cs (two BulkLockRoleGroup facts + the IsLand theory to KEEP)
  </read_first>
  <action>
    Delete CutLabLockRules.BulkLockRoleGroup (the xmldoc + method at CutLabLockRules.cs:99-118) and its
    two [Fact] tests (BulkLockRoleGroup_Lands_LocksExactlyLandFrontFaceCards,
    BulkLockRoleGroup_UnknownRoleGroup_IsNoOp) from CutLabRoleGroupLockTests.cs. KEEP the
    IsLand_TypeLineRow theory — IsLand stays load-bearing (CutLabRoleAssigner lands role, the pool-table
    role attribute) — and update the test class xmldoc summary to "Coverage for the shared land-type
    predicate." Rationale (record in the commit body): 102-PATTERNS directed extend-or-replace alongside
    the pill change (UI-SPEC A7); after Task 2, cut-lab.ts lockAllRole is the single bulk-lock driver
    operating on the pool-table checkboxes, the C# method has zero production callers (grep confirms only
    its own tests reference it), and generalizing an unused method to the eight role keys would create a
    second lock source (RESEARCH Pitfall 8) with no consumer — so remove, don't generalize.
  </action>
  <verify>
    <automated>dotnet test DeckFlow.Web.Tests --filter "FullyQualifiedName~CutLab" --nologo</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn 'BulkLockRoleGroup' DeckFlow.Web/Services DeckFlow.Web.Tests | wc -l` returns 0
    - `grep -c 'IsLand_TypeLineRow' DeckFlow.Web.Tests/CutLabRoleGroupLockTests.cs` returns ≥1 (IsLand coverage retained)
    - All CutLab-filtered tests pass; `dotnet build` clean
  </acceptance_criteria>
  <done>The hard-coded "lands" server-side bulk-lock stopgap is gone with the rationale recorded, IsLand coverage retained, and TS remains the single bulk-lock surface.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser DOM (floor inputs, hidden state field) → server | User-adjustable numbers and DOM-editable JSON; client-side clamping is UX only |
| Razor output → browser | Card names/categories from external sources rendered into HTML |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-102-04-01 | Tampering | Floor inputs / roleFloors in the hidden field | mitigate | Client clamp is convenience only; CutLabStateSerializer.Deserialize re-clamps every POST (plan 01) — values in the DOM are never trusted |
| T-102-04-02 | Cross-site scripting | Card names, category/theme names, finding copy in Razor | mitigate | All output through Razor's default HTML encoding (`@` expressions); no `Html.Raw` on any Phase 102 surface (acceptance greps stay Raw-free for new sections) |
| T-102-04-03 | Tampering | CSRF on the re-submitted form | mitigate | Recalculate reuses the existing single POST form with @Html.AntiForgeryToken() and [ValidateAntiForgeryToken] — no new endpoint, no new form |
| T-102-SC | Tampering | npm/pip/cargo installs | accept | Zero new dependencies; Vitest/Playwright already installed |
</threat_model>

<verification>
- `dotnet build` clean; `tsc --noEmit` strict clean; no compiled js staged
- `dotnet test DeckFlow.Web.Tests --filter CutLab` + Vitest file + cut-lab-smoke.spec.ts all green via scripts/run-web-test.sh
- No edits outside the seven listed files; site.css untouched; zero new :root tokens
- LF endings; changed-lines format gate
</verification>

<success_criteria>
- Three UI-SPEC sections render with prescriptive copy behind the OFF flag (SLOT-01/02, FLOOR-01 visible)
- Floor edits round-trip through roleFloors with Adjusted/at-floor states and the Recalculate CTA (FLOOR-02 UX)
- Single lock source preserved: display-only groups, per-group pills driving the pool table, old pill removed with its test pins updated in the same plan, and the unused server-side BulkLockRoleGroup removed (pattern-compliance cleanup)
- 101-VERIFICATION open items 1 (copy consolidation finished), 2, and 3 closed
</success_criteria>

<output>
Create `.planning/workstreams/cut-lab/phases/102-structural-analysis-role-floors/102-04-SUMMARY.md` when done
</output>
