---
phase: 96-stated-rules-distiller
plan: 05
type: execute
wave: 2
depends_on: ["96-01"]
files_modified:
  - DeckFlow.Core/Content/IContentVideoStore.cs
  - DeckFlow.Core/Content/ContentVideoStore.cs
  - DeckFlow.Core/Knowledge/ContentArtifactSpec.cs
  - DeckFlow.Core/Knowledge/ContentArtifactWriter.cs
  - DeckFlow.Core.Tests/ContentVideoStoreDistillTests.cs
  - DeckFlow.Core.Tests/ContentArtifactWriterTests.cs
  - DeckFlow.Core.Tests/ContentArtifactSpecTests.cs
autonomous: true
requirements: [CS-11a, CS-11b, CS-11c]
must_haves:
  truths:
    - "A new content_stated_rules child table persists per-video rules on both SQLite and Postgres, cleared on re-distill (no orphan rows)"
    - "The artifact frontmatter gains content_type: and stated_rules: as strictly additive lines; every pre-existing artifact renders byte-identically"
    - "Each persisted/rendered rule carries video_date provenance sourced from the video's publish date"
    - "D-04: each rule records the source video's publish date (video_date) only; newer-supersedes-older reconciliation is deferred to Phase 97 (substrate-only boundary)"
  artifacts:
    - path: "DeckFlow.Core/Content/ContentVideoStore.cs"
      provides: "content_stated_rules DDL (both dialects, incl. video_date_utc provenance column) + default-method InsertStatedRuleAsync + 4th ClearDistillOutput DELETE"
      contains: "content_stated_rules"
    - path: "DeckFlow.Core/Knowledge/ContentArtifactWriter.cs"
      provides: "additive content_type: + stated_rules: frontmatter emission"
      contains: "content_type"
  key_links:
    - from: "ClearDistillOutputSql"
      to: "content_stated_rules"
      via: "4th DELETE line added in same commit as InsertStatedRuleAsync (Pitfall 3)"
      pattern: "DELETE FROM content_stated_rules"
---

<objective>
Give Phase 97 a queryable per-video stated-rules source and make the distill artifact self-describing. Add a dialect-guarded `content_stated_rules` child table (mirroring `content_clips`/`content_tags` exactly, with the mandatory 4th clear-on-redistill DELETE — RESEARCH Pitfall 3), and extend `ContentArtifactWriter.ToText`/`ContentArtifactSpec` with a strictly-additive `content_type:` field and `stated_rules:` frontmatter block (CS-11a/CS-11b) under the byte-stable artifact gate.

Purpose: persistence + rendering substrate so a re-distill emits both new sections AND backs them with structured rows (D-05 mechanism), while pre-existing artifacts stay byte-identical.
Output: extended store + writer/spec + round-trip, clear-on-redistill, and byte-stability tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/96-stated-rules-distiller/96-CONTEXT.md
@.planning/phases/96-stated-rules-distiller/96-RESEARCH.md
@.planning/phases/96-stated-rules-distiller/96-PATTERNS.md
@.planning/phases/96-stated-rules-distiller/96-01-SUMMARY.md

<interfaces>
<!-- Locked shapes. -->

IContentVideoStore new method — HIGH-2: MUST be a DEFAULT interface method with a NotSupportedException body (mirror the existing optional default methods already on this interface — ListVideosPendingDistillAsync/GetLatestTranscriptAsync/ClearDistillOutputAsync all use `=> throw new NotSupportedException(...)`). Only the concrete `ContentVideoStore` overrides it; every existing fake (SpyContentVideoStore in BlockedVideoStoreTests, the fakes in RunDistillAsyncTests/VideoStatusResolverTests/CommandRunnerCorpusResetTests/ThrowingOrchestratorDependencies, etc.) inherits the default and keeps compiling untouched:
  Task<long> InsertStatedRuleAsync(long videoId, StatedRuleCandidate rule, int sortOrder, CancellationToken cancellationToken = default)
      => throw new NotSupportedException("This content video store does not support stated-rule inserts.");

content_stated_rules columns (Postgres form; SQLite substitutes INTEGER/REAL/INTEGER + AUTOINCREMENT per the file's existing pattern):
  id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  video_id BIGINT NOT NULL REFERENCES content_videos(id) ON DELETE CASCADE,
  category TEXT NOT NULL, metric TEXT NOT NULL,
  value DOUBLE PRECISION NULL, value_min DOUBLE PRECISION NULL, value_max DOUBLE PRECISION NULL,
  comparator TEXT NOT NULL, condition TEXT NULL, clip_ts INT NULL,
  source_clip TEXT NOT NULL, confidence DOUBLE PRECISION NOT NULL,
  card_reference TEXT NULL, card_grounded BOOLEAN NULL,
  video_date_utc TIMESTAMPTZ NULL, sort_order INT NOT NULL DEFAULT 0
  + index ix_content_stated_rules_video_id ON content_stated_rules(video_id)
  Note: a single-value comparator (gte/lte/eq) persists its Value into the dedicated `value` column (NOT value_min) so the band trio (value / value_min / value_max) round-trips losslessly to StatedRuleCandidate. The nullable `card_reference` TEXT column persists the rule's optional card name (null for metric-based rules) so CardReference round-trips too.
  HIGH-1 (D-04/CS-11c provenance): `video_date_utc` stores each rule's source-video publish date (StatedRuleCandidate.VideoDateUtc) so provenance is QUERYABLE, not markdown-only. Mirror the existing `published_utc` date-column convention EXACTLY — Postgres `TIMESTAMPTZ NULL`, SQLite `TEXT NULL` (verified against content_videos.published_utc in ContentVideoStore.cs ~650 / ~704). Bind it in InsertStatedRuleSql/InsertStatedRuleAsync and assert it survives the insert→read round-trip.

ContentArtifactMetadata new properties:
  public required string ContentType { get; init; }
  public IReadOnlyList<StatedRuleCandidate> StatedRules { get; init; } = Array.Empty<StatedRuleCandidate>();

ContentArtifactWriter.ToText additive frontmatter (inside the existing ---...--- block, AFTER generated_utc, BEFORE closing ---):
  content_type: "<value>"            (via existing Quote())
  stated_rules: <json>               (via new ContentArtifactSpec.SerializeStatedRules(...))
  Existing source/title/url/video_id/tags/generated_utc lines + ## Summary/## Key Clips/## Tags body: BYTE-FOR-BYTE UNCHANGED.

SerializeStatedRules snake_case contract — HIGH-3 (CS-11a lock, P97-facing): the emitted `stated_rules:` JSON is the SINGLE contract Phase 97 parses, and CS-11a locks its keys as snake_case EXACTLY:
  category, metric, value, value_min, value_max, comparator, condition, clip_ts, source_clip, confidence, card_reference, card_grounded, video_date
A plain `JsonSerializer.Serialize(rules)` over StatedRuleCandidate emits PascalCase (`Category`, `ValueMin`, `SourceClip`, `ClipTimestampSeconds`, `VideoDateUtc`) — WRONG. Because two keys differ from a naive snake_case of the property name (`clip_ts` ≠ clip_timestamp_seconds, `video_date` ≠ video_date_utc), a bare `JsonNamingPolicy.SnakeCaseLower` policy is INSUFFICIENT. Serialize via a dedicated artifact-projection record (e.g. internal `StatedRuleArtifactEntry`) whose members carry explicit `[JsonPropertyName("...")]` attributes for the 13 locked keys above (or, equivalently, SnakeCaseLower + [JsonPropertyName] overrides on ClipTimestampSeconds→clip_ts and VideoDateUtc→video_date). SerializeStatedRules maps each StatedRuleCandidate → the projection, then JsonSerializer.Serialize(projection[]). These key names MUST match 96-03's snake_case field names for the shared shape (category/metric/value/value_min/value_max/comparator/condition/source_clip/confidence/card_reference); clip_ts and video_date/card_grounded are the artifact/DB-facing names (the LLM input schema in 96-03 uses clip_timestamp_seconds and omits video_date/card_grounded, which are stamped post-LLM — intentional input-vs-output naming, cross-checked so P97 parses one artifact contract).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: content_stated_rules dialect-guarded table + insert + clear-on-redistill</name>
  <read_first>
    - DeckFlow.Core/Content/ContentVideoStore.cs (content_clips analog: InsertClipAsync ~204-219, InsertClipSql ~568-572, Postgres DDL ~675-694, SQLite DDL ~729-748, ClearDistillOutputSql ~604-611 — mirror ALL exactly; ALSO the published_utc date-column convention — Postgres TIMESTAMPTZ NULL ~650, SQLite TEXT NULL ~704 — mirror for video_date_utc per HIGH-1)
    - DeckFlow.Core/Content/IContentVideoStore.cs (InsertClipAsync XML-doc + signature idiom ~104-118; AND the existing DEFAULT interface methods with NotSupportedException bodies — ListVideosPendingDistillAsync ~56-59, GetLatestTranscriptAsync ~92-93, ClearDistillOutputAsync ~162-163 — the exact precedent InsertStatedRuleAsync must follow per HIGH-2)
    - DeckFlow.Core.Tests/BlockedVideoStoreTests.cs (SpyContentVideoStore ~353 implements only a SUBSET of IContentVideoStore, relying on default interface methods — proof that a required InsertStatedRuleAsync would break compilation and why it must be a default method)
    - DeckFlow.Core/Knowledge/StatedRulesExtraction/StatedRuleCandidate.cs (96-01 — the record being persisted/read back)
    - DeckFlow.Core.Tests/ContentVideoStoreDistillTests.cs (ClearDistillOutputAsync_RemovesPriorSummaryClipAndTagRowsOnly ~141-151 — extend + add a dedicated round-trip test)
    - .planning/phases/96-stated-rules-distiller/96-PATTERNS.md (section "content_stated_rules new store table" — DDL/insert/clear idioms; RESEARCH Pitfall 3)
  </read_first>
  <behavior>
    - Insert-then-read round-trips every StatedRuleCandidate field including the band trio (value / value_min / value_max), condition, clip_ts, source_clip, confidence, card_reference, card_grounded, and video_date_utc (VideoDateUtc provenance, HIGH-1) — on both dialects.
    - ClearDistillOutputAsync removes content_stated_rules rows for the video (no orphans on re-distill).
    - Re-distill (insert → clear → insert) shows a clean replace, not row-count growth.
  </behavior>
  <action>
    Extend `IContentVideoStore` with `InsertStatedRuleAsync(long videoId, StatedRuleCandidate rule, int sortOrder, CancellationToken = default)` per <interfaces> (pass the whole record — justified since it has 13 fields, unlike the flat 3-field InsertTagAsync). HIGH-2: declare it as a DEFAULT interface method with a `=> throw new NotSupportedException("This content video store does not support stated-rule inserts.");` body (exactly like the existing ListVideosPendingDistillAsync/GetLatestTranscriptAsync/ClearDistillOutputAsync defaults) so ONLY the concrete `ContentVideoStore` overrides it and every existing fake keeps compiling — do NOT declare it as a plain required member. In `ContentVideoStore.cs`: add the `content_stated_rules` Postgres + SQLite DDL (with the extra `value` column so the band trio round-trips, a nullable `card_reference` TEXT column, and a `video_date_utc` date column — Postgres `TIMESTAMPTZ NULL`, SQLite `TEXT NULL`, mirroring the existing `published_utc` convention — per HIGH-1) into the same EnsureSchema DDL sequence as content_clips, plus `ix_content_stated_rules_video_id`; add `InsertStatedRuleSql` (`INSERT ... RETURNING id;`, including the `value`, `video_date_utc`, and `card_reference` columns) and `InsertStatedRuleAsync` mirroring InsertClipAsync (EnsureSchemaAsync → OpenConnectionAsync → ExecuteScalarAsync<long> with Dapper anonymous-object params bound from the record, binding rule.VideoDateUtc → @videoDateUtc); add the 4th `DELETE FROM content_stated_rules WHERE video_id = @videoId;` line to `ClearDistillOutputSql` IN THIS SAME COMMIT (Pitfall 3). Apply the exact BIGINT→INTEGER / DOUBLE PRECISION→REAL / BOOLEAN→INTEGER / TIMESTAMPTZ→TEXT / IDENTITY→AUTOINCREMENT dialect substitution used by every other table in the file. Add a read-back helper if the round-trip test needs one (mirror how clips/tags are read in the existing test, or query directly in-test).
    Extend `ContentVideoStoreDistillTests.cs`: add InsertStatedRuleAsync to the existing clear test's setup+assert (proving the row is cleared) AND a new `InsertStatedRuleAsync_RoundTrips_AllFields` covering both the SQLite (unconditional) and gated Postgres paths using the existing [PostgresFact]/env-gated convention already in that file.
  </action>
  <verify>
    <automated>dotnet.exe test DeckFlow.Core.Tests --filter FullyQualifiedName~ContentVideoStoreDistillTests</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "DELETE FROM content_stated_rules" DeckFlow.Core/Content/ContentVideoStore.cs` >= 1 (4th clear line present).
    - Round-trip test asserts value/value_min/value_max/condition/clip_ts/source_clip/confidence/card_reference/card_grounded AND video_date_utc (VideoDateUtc, HIGH-1) all survive insert→read on SQLite; Postgres path runs under the existing env gate.
    - HIGH-2 source-compatibility: `InsertStatedRuleAsync` is a default interface method; the whole solution (`dotnet build DeckFlow.sln`) compiles with EVERY existing IContentVideoStore fake untouched (no fake edited in this plan).
    - Clear test proves stated-rule rows are removed and re-insert shows no row-count growth.
    - `dotnet build DeckFlow.Core/DeckFlow.Core.csproj` clean, 0 new warnings.
  </acceptance_criteria>
  <done>content_stated_rules persists losslessly on both dialects and is cleared on re-distill; tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Additive content_type: + stated_rules: frontmatter (byte-stable gate)</name>
  <read_first>
    - DeckFlow.Core/Knowledge/ContentArtifactWriter.cs (ToText ~43-102 — the frontmatter AppendLine sequence + Quote() helper ~191-195; the exact lines that must stay untouched)
    - DeckFlow.Core/Knowledge/ContentArtifactSpec.cs (SerializeTags ~48-53, ContentArtifactMetadata ~91-98, ArtifactFileFormat doc fixture ~13-41 — extend all three)
    - DeckFlow.Core/Knowledge/StatedRulesExtraction/StatedRuleCandidate.cs (96-01 — projected into the snake_case stated_rules: entry)
    - (LOW-1) This plan does NOT reference `ContentTypeHeuristic` (96-02) at all. `content_type` reaches ToText as an OPAQUE pre-computed string on `metadata.ContentType`; the orchestrator (96-08) computes it via ContentTypeHeuristic and passes it in. So 96-05 needs NO dependency on 96-02 (depends_on stays ["96-01"]); the writer just Quote()s whatever string it receives. Do not import or reference the heuristic here.
    - DeckFlow.Core.Tests/ContentArtifactWriterTests.cs + ContentArtifactSpecTests.cs (extend; these + the orchestrator are the ONLY references to ToText/ContentArtifactMetadata — small enumerable blast radius, Pitfall 4)
    - .planning/phases/96-stated-rules-distiller/96-PATTERNS.md (section "ContentArtifactWriter.ToText" — additive-only rules; RESEARCH Pitfall 4 byte-stable gate)
  </read_first>
  <behavior>
    - An artifact rendered with a ContentType + non-empty StatedRules emits `content_type: "..."` and `stated_rules: [...]` inside the frontmatter, after generated_utc, before the closing ---.
    - The emitted `stated_rules:` JSON uses EXACTLY the CS-11a snake_case keys (category, metric, value, value_min, value_max, comparator, condition, clip_ts, source_clip, confidence, card_reference, card_grounded, video_date) — never PascalCase, never `clip_timestamp_seconds`/`video_date_utc` (HIGH-3).
    - An artifact rendered with the SAME pre-existing inputs as before this change (empty StatedRules path via the prior call shape) produces byte-identical source/title/url/video_id/tags/generated_utc lines and identical ## Summary/## Key Clips/## Tags bodies.
    - stated_rules serializes via JsonSerializer (valid YAML flow mapping) — no new YAML dependency.
  </behavior>
  <action>
    Add `public required string ContentType { get; init; }` and `public IReadOnlyList<StatedRuleCandidate> StatedRules { get; init; } = Array.Empty<StatedRuleCandidate>();` to `ContentArtifactMetadata`. Add `public static string SerializeStatedRules(IReadOnlyList<StatedRuleCandidate> rules)` to `ContentArtifactSpec` — HIGH-3: do NOT emit a plain `JsonSerializer.Serialize(rules)` (that renders PascalCase). Project each `StatedRuleCandidate` onto a dedicated internal artifact record (e.g. `StatedRuleArtifactEntry`) whose members carry explicit `[JsonPropertyName("category"|"metric"|"value"|"value_min"|"value_max"|"comparator"|"condition"|"clip_ts"|"source_clip"|"confidence"|"card_reference"|"card_grounded"|"video_date")]` attributes matching the CS-11a snake_case contract EXACTLY (note `clip_ts` maps from ClipTimestampSeconds and `video_date` maps from VideoDateUtc — a bare SnakeCaseLower policy would wrongly emit `clip_timestamp_seconds`/`video_date_utc`), guard with `ArgumentNullException.ThrowIfNull`, then `JsonSerializer.Serialize(projected)`. In `ContentArtifactWriter.ToText`, insert TWO new AppendLine calls (`content_type:` via `Quote(metadata.ContentType)` — an OPAQUE passed-in string, LOW-1, not computed here; then `stated_rules:` via `SerializeStatedRules(metadata.StatedRules)`) strictly between the existing `generated_utc:` line and the closing `---` — do NOT touch any existing line or the body sections. Update the `ArtifactFileFormat` doc fixture with matching `content_type:` + `stated_rules:` example lines (using the snake_case keys) in the same commit. Never hand-concatenate an LLM string; use Quote()/JsonSerializer only (T-96-05).
    Extend `ContentArtifactWriterTests.cs` with (a) a test asserting the two new lines appear in the right position with a populated StatedRules list, (b) a byte-stability test proving the pre-existing frontmatter+body lines are unchanged, and (c) a snake_case-key assertion (HIGH-3) on the emitted `stated_rules:` JSON. Extend `ContentArtifactSpecTests.cs` for SerializeStatedRules + the updated ArtifactFileFormat fixture.
  </action>
  <verify>
    <automated>dotnet.exe test DeckFlow.Core.Tests --filter FullyQualifiedName~"ContentArtifactWriterTests|ContentArtifactSpecTests"</automated>
  </verify>
  <acceptance_criteria>
    - Writer test confirms `content_type:` and `stated_rules:` render after `generated_utc:` and before closing `---`.
    - HIGH-3: a test asserts the emitted `stated_rules:` JSON contains the EXACT snake_case keys — including `video_date`, `card_reference`, `card_grounded`, `clip_ts`, `value_min`, `source_clip` — and does NOT contain any PascalCase key (e.g. no `VideoDateUtc`, `ClipTimestampSeconds`, `SourceClip`).
    - A byte-stability assertion proves the existing source/title/url/video_id/tags/generated_utc + ## Summary/## Key Clips/## Tags output is unchanged (Pitfall 4).
    - `stated_rules:` value parses as JSON (valid YAML flow mapping); no YamlDotNet or other new package added.
    - `dotnet build DeckFlow.Core/DeckFlow.Core.csproj` clean, 0 new warnings.
  </acceptance_criteria>
  <done>Frontmatter gains both fields additively; pre-existing artifacts byte-identical; doc fixture + tests updated in the same commit.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| rule content → SQL | LLM-derived rule fields inserted into content_stated_rules |
| rule content → artifact text | LLM-derived strings rendered into the .md frontmatter |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-96-02 | Tampering | SQL injection via rule fields | mitigate | Dapper anonymous-object parameter binding only (mirrors InsertClipAsync); no string-concatenated SQL |
| T-96-05 | Tampering | control chars / path traversal in source_clip/condition/card name | mitigate | Rendered exclusively via Quote()/JsonSerializer; never used as a path segment (reuse existing SanitizePathSegment path, add no new path-construction code) |
| T-96-01 | Tampering | orphaned/stale rows surviving re-distill | mitigate | 4th ClearDistillOutput DELETE in the same commit as the insert; regression test guards it (Pitfall 3) |
| T-96-SC | Tampering | package installs (e.g. a YAML lib) | mitigate | Explicitly NO new package — stated_rules uses JsonSerializer (JSON ⊂ YAML); any new dep needs user approval per CLAUDE.md |
</threat_model>

<verification>
- `dotnet build DeckFlow.Core/DeckFlow.Core.csproj` clean.
- `dotnet.exe test DeckFlow.Core.Tests --filter FullyQualifiedName~"ContentVideoStoreDistillTests|ContentArtifactWriterTests|ContentArtifactSpecTests"` green.
- Byte-stable gate: no pre-existing artifact output changes.
</verification>

<success_criteria>
content_stated_rules backs every rendered rule on both dialects with clean re-distill semantics, and the artifact frontmatter gains content_type:/stated_rules: additively without churning any existing artifact.
</success_criteria>

<output>
Create `.planning/phases/96-stated-rules-distiller/96-05-SUMMARY.md` when done.
</output>
</output>
