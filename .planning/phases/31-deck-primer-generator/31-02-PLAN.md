---
phase: 31-deck-primer-generator
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - DeckFlow.Web/Services/PacketArtifactStore.cs
  - DeckFlow.Web/Models/PrimerSectionCatalog.cs
  - DeckFlow.Web/Models/DeckPrimerRequest.cs
  - DeckFlow.Web/Models/DeckPrimerViewModel.cs
  - DeckFlow.Web/Models/DeckPageTab.cs
  - DeckFlow.Web.Tests/PrimerSectionCatalogTests.cs
  - DeckFlow.Web.Tests/DeckPrimerRequestTests.cs
requirements: [PRM-03, PRM-04, PRM-09, PRM-10, PRM-11, PRM-12]
autonomous: true
must_haves:
  truths:
    - "PrimerAllowedNames exists in PacketArtifactStore.cs as the FIRST artifact-store change, before any BuildPrimerZip/LoadPrimerFromZip method"
    - "PrimerSectionCatalog exposes 31 sections grouped into exactly 5 groups (Identity / Combos / Gameplay / Matchups / Maintenance), each section carrying Id, Number, Title, HelpText (PRM-12), Group, and an optional bracket gate"
    - "Exactly two sections are cEDH-only (#24/#25) and exactly one is casual-only (#26); NormalizeSelections strips IDs gated out by the active bracket"
    - "GetPresetForBracket returns a cEDH preset for bracket 5 and a Casual/Upgraded preset for brackets 1-4 (PRM-03)"
    - "DeckPrimerRequest is a class with MUTABLE null-guard setters; TargetAiPlatform setter calls AiPlatform.Normalize(value).Key; SelectedSectionIds setter null-guards to []"
    - "DeckPrimerViewModel is a sealed class with { get; init; } properties; ActiveTab defaults to DeckPageTab.DeckPrimer"
    - "DeckPageTab.DeckPrimer = 12 is added without disturbing existing values or breaking switch consumers"
  artifacts:
    - path: "DeckFlow.Web/Services/PacketArtifactStore.cs"
      provides: "PrimerAllowedNames HashSet allowlist (added first, before zip methods)"
      contains: "PrimerAllowedNames"
    - path: "DeckFlow.Web/Models/PrimerSectionCatalog.cs"
      provides: "PrimerSectionEntry + PrimerSectionGroup records + 31-section static catalog with preset + gate + NormalizeSelections helpers"
      contains: "PrimerSectionCatalog"
    - path: "DeckFlow.Web/Models/DeckPrimerRequest.cs"
      provides: "Form-bound request DTO with mutable null-guard setters and AiPlatform.Normalize in TargetAiPlatform"
      contains: "AiPlatform.Normalize(value).Key"
    - path: "DeckFlow.Web/Models/DeckPrimerViewModel.cs"
      provides: "Razor view model (sealed class, { get; init; })"
      contains: "DeckPageTab.DeckPrimer"
    - path: "DeckFlow.Web/Models/DeckPageTab.cs"
      provides: "DeckPrimer enum member = 12"
      contains: "DeckPrimer = 12"
    - path: "DeckFlow.Web.Tests/PrimerSectionCatalogTests.cs"
      provides: "31-count / 5-group / preset / gate / NormalizeSelections tests"
      exports: ["Catalog_Has31Sections", "Catalog_Has5Groups", "Normalize_StripsCedhOnly_ForCasualBracket", "GetPreset_Cedh_IncludesCedhOnly_ExcludesCasualOnly"]
    - path: "DeckFlow.Web.Tests/DeckPrimerRequestTests.cs"
      provides: "AiPlatform.Normalize setter + null-guard setter tests"
      exports: ["TargetAiPlatform_Setter_NormalizesUnknownToDefault", "SelectedSectionIds_NullSet_BecomesEmptyList"]
  key_links:
    - from: "DeckPrimerRequest.TargetAiPlatform setter"
      to: "AiPlatform.Normalize"
      via: "Phase 10 hardening contract"
      pattern: "AiPlatform\\.Normalize"
    - from: "PrimerSectionCatalog.NormalizeSelections"
      to: "CedhOnlySectionIds / CasualOnlySectionIds gate sets"
      via: "server-side bracket gating"
      pattern: "NormalizeSelections"
    - from: "PacketArtifactStore.PrimerAllowedNames"
      to: "31-05 BuildPrimerZip/LoadPrimerFromZip"
      via: "allowlist enforced by ReadEntries"
      pattern: "PrimerAllowedNames"
---

<objective>
Foundation models for the deck primer: the section catalog (31 sections / 5 groups / per-section help text + bracket gates + presets), the form-bound request DTO, the view model, the new `DeckPageTab.DeckPrimer` enum value, and — FIRST, per the carried-forward invariant — the `PrimerAllowedNames` allowlist in `PacketArtifactStore` (the zip Build/Load methods themselves land in 31-05). Includes round-trip and catalog unit tests.

Purpose: Everything downstream (service in 31-03, variants in 31-04, zip in 31-05, controller/view/TS in 31-06) consumes these contracts. Interface-first ordering: define the contracts before implementing against them. `PrimerAllowedNames` must exist before any zip method references it (Pitfall 2 — `ReadEntries` throws on unlisted names).

Output: `PrimerAllowedNames` (allowlist only), `PrimerSectionCatalog` (+ `PrimerSectionEntry` / `PrimerSectionGroup`), `DeckPrimerRequest`, `DeckPrimerViewModel`, `DeckPageTab.DeckPrimer = 12`, and two unit-test files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-deck-primer-generator/31-CONTEXT.md
@.planning/phases/31-deck-primer-generator/31-RESEARCH.md
@.planning/phases/31-deck-primer-generator/31-PATTERNS.md

<interfaces>
<!-- Confirmed against live code 2026-06-08. Executor uses these directly. -->

PacketArtifactStore (DeckFlow.Web/Services/PacketArtifactStore.cs) — internal static:
  - Existing allowlists: PacketAllowedNames (line 37), ComparisonAllowedNames (line 55), CedhAllowedNames (line 72) — all `new(StringComparer.OrdinalIgnoreCase)`
  - ReadEntries(stream, allowedNames) (line 625) THROWS InvalidOperationException on any entry name not in the passed set; MaxEntryUncompressedBytes=2MB (line 14), MaxTotalUncompressedBytes=10MB (line 15)
  - ADD PrimerAllowedNames immediately AFTER CedhAllowedNames (after line ~83), BEFORE the first public static method. Do NOT add Build/Load methods in this plan — that is 31-05. This plan adds the allowlist constant ONLY.

AnalysisQuestionCatalog (DeckFlow.Web/Models/AnalysisQuestionCatalog.cs) — the catalog analog:
  - record AnalysisQuestionOption(string Id, string Text); record AnalysisQuestionBucket(string Id, string Label, IReadOnlyList<...>); static class with Buckets, AllQuestions, NormalizeSelections (dedup/validate/order)
  - Mirror its public static class shape; the primer leaf is richer (Number, HelpText, Group, BracketGate)

DeckAnalysisRequest (DeckFlow.Web/Models/DeckAnalysisRequest.cs) — the request DTO analog:
  - MUTABLE setters with null-guard backing fields (NOT init) — form-bound, mutated across workflow steps. This is intentional per PATTERNS.md.
  - TargetAiPlatform setter calls AiPlatform.Normalize(value).Key (Phase 10 hardening, ~line 181)
  - DeckInputSource / DeckUrl / DeckText / DeckSource routing (lines 59-74); WorkflowStep; Format; DeckName

DeckAnalysisViewModel (DeckFlow.Web/Models/DeckAnalysisViewModel.cs) — sealed CLASS (not record), every prop { get; init; }; ActiveTab default

CommanderBracketCatalog (DeckFlow.Web/Models/CommanderBracketCatalog.cs):
  - Values (exact strings for TargetCommanderBracket + preset routing): "Exhibition"(B1), "Core"(B2), "Upgraded"(B3), "Optimized"(B4), "cEDH"(B5)
  - Find(value) returns null on unknown — guard before use

AiPlatform (DeckFlow.Web/Models/AiPlatform.cs): Normalize(string?) → AiPlatform (line 60); Default => ChatGpt (line 42); Key property

DeckPageTab (DeckFlow.Web/Models/DeckPageTab.cs): existing values 0-5,7-11 (6 is a gap). ADD `DeckPrimer = 12` (next free, out of all existing ranges).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add PrimerAllowedNames (first), PrimerSectionCatalog, and DeckPageTab.DeckPrimer</name>
  <files>DeckFlow.Web/Services/PacketArtifactStore.cs, DeckFlow.Web/Models/PrimerSectionCatalog.cs, DeckFlow.Web/Models/DeckPageTab.cs</files>
  <read_first>
    - DeckFlow.Web/Services/PacketArtifactStore.cs (lines 1-90 — the three existing allowlist HashSets; ReadEntries 625-650)
    - DeckFlow.Web/Models/AnalysisQuestionCatalog.cs (FULL — record shapes, static class, NormalizeSelections dedup/validate/order pattern)
    - DeckFlow.Web/Models/CommanderBracketCatalog.cs (FULL — bracket value strings for preset routing)
    - DeckFlow.Web/Models/DeckPageTab.cs (FULL — enum values, the gap at 6, consumers with `_ =>` default arms)
    - .planning/phases/31-deck-primer-generator/31-RESEARCH.md (Section 2 catalog model, Section 5 PrimerAllowedNames, Pitfall 2)
    - .planning/phases/31-deck-primer-generator/31-PATTERNS.md (PrimerSectionCatalog.cs, PacketArtifactStore.cs, DeckPageTab.cs sections)
  </read_first>
  <action>
    FIRST (carried-forward invariant): In `PacketArtifactStore.cs`, immediately after the `CedhAllowedNames` block (after line ~83) and BEFORE the first public static method, add `PrimerAllowedNames` as `new(StringComparer.OrdinalIgnoreCase)` containing exactly these entry names: `00-primer-input-summary.txt`, `01-primer-request-context.txt`, `10-primer-deck-list.txt`, `10b-primer-deck-original.txt`, `30-primer-chatgpt-prompt.txt`, `30-primer-claude-prompt.txt`, `30-primer-gemini-prompt.txt`, `all-primer-prompts.txt`. Do NOT add BuildPrimerZip/LoadPrimerFromZip here — those are 31-05. This is the allowlist constant only. The set must match the entry names 31-05 will write/read. Touch only the inserted lines.

    Create `DeckFlow.Web/Models/PrimerSectionCatalog.cs` (namespace `DeckFlow.Web.Models;`, file-scoped):
      - `public sealed record PrimerSectionEntry(string Id, int Number, string Title, string HelpText, string Group, string? BracketGate = null);` with XML doc on the record + each param (BracketGate: null = all brackets, "cedh-only" = bracket 5 only, "casual-only" = brackets 1-4 only). Positional record (init accessors implicit) per the { get; init; } invariant.
      - `public sealed record PrimerSectionGroup(string Id, string Label, IReadOnlyList<PrimerSectionEntry> Sections);` with XML doc.
      - `public static class PrimerSectionCatalog` with:
        - `public static IReadOnlyList<PrimerSectionGroup> Groups { get; }` — EXACTLY 5 groups: Identity, Combos, Gameplay, Matchups, Maintenance — totaling EXACTLY 31 PrimerSectionEntry across them, numbered 1-31. Each entry has a meaningful Title and a HelpText sentence describing what good AI output for that section looks like (PRM-12). Sections #24 and #25 carry BracketGate "cedh-only"; section #26 carries BracketGate "casual-only". Choose section content faithful to the domain (commander identity, color pie, archetype, card rationale -> Identity; verified combos, near-combos, speculative-synergy ask -> Combos; game plan, engine, mulligan, role-count grounding -> Gameplay; matchups, threat assessment, sideboard thinking -> Matchups; budget cuts, upgrade paths, version history, meta shifts -> Maintenance). Keep IDs stable kebab-case.
        - `public static IReadOnlyList<PrimerSectionEntry> AllSections { get; } = Groups.SelectMany(g => g.Sections).ToList();`
        - `public static IReadOnlySet<string> CedhOnlySectionIds { get; }` and `public static IReadOnlySet<string> CasualOnlySectionIds { get; }` — `new HashSet<string>(StringComparer.OrdinalIgnoreCase)` populated from the gated entries.
        - `public static IReadOnlyList<string> GetPresetForBracket(string bracketValue)` — returns default-selected section IDs. cEDH ("cEDH") preset = all sections EXCEPT casual-only #26 (includes cEDH-only #24/#25). Casual/Upgraded preset (Exhibition/Core/Upgraded/Optimized) = a curated subset EXCLUDING cEDH-only #24/#25 (Optimized may include more Combos/Matchups per RESEARCH Section 2 table). Unknown bracket -> empty list.
        - `public static IReadOnlyList<string> NormalizeSelections(IEnumerable<string>? selections, string bracketValue)` — validate against AllSections IDs, dedupe (OrdinalIgnoreCase), preserve catalog order, AND strip IDs gated out by the active bracket (cEDH-only stripped when bracket != "cEDH"; casual-only stripped when bracket == "cEDH"). Mirror AnalysisQuestionCatalog.NormalizeSelections for the validate/dedup/order spine, adding the gate filter.
      - File-scoped namespace; XML docs on all public members (CS1591 gate); Allman braces; 4-space; LF.

    In `DeckPageTab.cs`, add `DeckPrimer = 12,` (with XML `<summary>` "Deck-primer artifact generator page.") after the last existing member. Do not renumber existing members. Touch only the added lines.
  </action>
  <verify>
    <automated>"/mnt/c/Program Files/dotnet/dotnet.exe" build DeckFlow.Web/DeckFlow.Web.csproj -warnaserror:CS1591 2>&1 | grep -E "Build succeeded|error" | head</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "PrimerAllowedNames" DeckFlow.Web/Services/PacketArtifactStore.cs` matches the new HashSet (added before any Build/Load primer method — none exist yet in this plan)
    - `grep -cE "PrimerSectionEntry\(" DeckFlow.Web/Models/PrimerSectionCatalog.cs` is consistent with 31 entries (manual: AllSections.Count == 31, asserted by the Task 3 test)
    - The 5 group labels are present: `grep -cE "\"Identity\"|\"Combos\"|\"Gameplay\"|\"Matchups\"|\"Maintenance\"" DeckFlow.Web/Models/PrimerSectionCatalog.cs` >= 5
    - `grep -c "cedh-only" DeckFlow.Web/Models/PrimerSectionCatalog.cs` shows exactly 2 cedh-only entries; `grep -c "casual-only"` shows exactly 1
    - `grep -n "GetPresetForBracket\|NormalizeSelections" DeckFlow.Web/Models/PrimerSectionCatalog.cs` both present
    - `grep -n "DeckPrimer = 12" DeckFlow.Web/Models/DeckPageTab.cs` matches one line
    - `dotnet build DeckFlow.Web -warnaserror:CS1591` reports Build succeeded, 0 errors
  </acceptance_criteria>
  <done>PrimerAllowedNames exists (allowlist only); PrimerSectionCatalog has 31 sections in 5 groups with help text, presets, gate sets, and bracket-aware NormalizeSelections; DeckPageTab.DeckPrimer = 12; Web builds CS1591-clean.</done>
</task>

<task type="auto">
  <name>Task 2: DeckPrimerRequest DTO + DeckPrimerViewModel</name>
  <files>DeckFlow.Web/Models/DeckPrimerRequest.cs, DeckFlow.Web/Models/DeckPrimerViewModel.cs</files>
  <read_first>
    - DeckFlow.Web/Models/DeckAnalysisRequest.cs (FULL — mutable null-guard setters, DeckInputSource/DeckSource routing, TargetAiPlatform setter at ~line 181, WorkflowStep/Format/DeckName)
    - DeckFlow.Web/Models/DeckAnalysisViewModel.cs (FULL — sealed class, { get; init; } props, ActiveTab default)
    - DeckFlow.Web/Models/AiPlatform.cs (Normalize, Key, Default)
    - .planning/phases/31-deck-primer-generator/31-PATTERNS.md (DeckPrimerRequest.cs + DeckPrimerViewModel.cs sections — DTO nuance)
  </read_first>
  <action>
    Create `DeckFlow.Web/Models/DeckPrimerRequest.cs` as `public sealed class DeckPrimerRequest` (NOT a record — form-bound, mutated across workflow steps per the DTO nuance). Mirror DeckAnalysisRequest's mutable-setter + null-guard-backing-field pattern:
      - private backing fields: `_deckText`, `_deckUrl`, `_format = "Commander"`, `_deckName`, `_targetCommanderBracket`, `_targetAiPlatform = "ChatGPT"`, `_selectedSectionIds = []` (List of string).
      - `DeckInputSource DeckInputSource { get; set; } = DeckInputSource.PasteText;`
      - `DeckUrl`/`DeckText` with `value ?? string.Empty` setters; `DeckSource` get/set routing to URL or Text per DeckInputSource (copy DeckAnalysisRequest lines 59-74 exactly).
      - `int WorkflowStep { get; set; } = 1;`
      - `Format` (`value ?? "Commander"`), `DeckName` (`value ?? string.Empty`).
      - `TargetCommanderBracket` with `value ?? string.Empty` setter (values: Exhibition/Core/Upgraded/Optimized/cEDH).
      - `TargetAiPlatform` setter MUST be `set => _targetAiPlatform = AiPlatform.Normalize(value).Key;` (Phase 10 hardening — prevents crafted unknown platform strings). Getter returns the backing field.
      - `SelectedSectionIds` of type `List<string>` with `get => _selectedSectionIds; set => _selectedSectionIds = value ?? [];`.
      - XML docs on the class + each public property (CS1591 gate). File-scoped namespace `DeckFlow.Web.Models;`. Allman; 4-space; LF.

    Create `DeckFlow.Web/Models/DeckPrimerViewModel.cs` as `public sealed class DeckPrimerViewModel` with `{ get; init; }` props (constructed once by the controller, never mutated): `DeckPageTab ActiveTab { get; init; } = DeckPageTab.DeckPrimer;`, `DeckPrimerRequest Request { get; init; } = new();`, `string? ErrorMessage`, `string? InputSummary`, `string? SuggestedChatTitle`, `string? PrimerPromptText`, `string? TimingSummary`, `string? ImportWarning`. XML docs; file-scoped namespace; LF.

    Touch only new files; no formatter sweep.
  </action>
  <verify>
    <automated>"/mnt/c/Program Files/dotnet/dotnet.exe" build DeckFlow.Web/DeckFlow.Web.csproj -warnaserror:CS1591 2>&1 | grep -E "Build succeeded|error" | head</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "AiPlatform.Normalize(value).Key" DeckFlow.Web/Models/DeckPrimerRequest.cs` matches the TargetAiPlatform setter exactly
    - `grep -n "_selectedSectionIds = value ?? \[\]" DeckFlow.Web/Models/DeckPrimerRequest.cs` matches the null-guard list setter
    - `grep -n "public sealed class DeckPrimerRequest" DeckFlow.Web/Models/DeckPrimerRequest.cs` matches (class, NOT record)
    - `grep -n "DeckPageTab ActiveTab { get; init; } = DeckPageTab.DeckPrimer" DeckFlow.Web/Models/DeckPrimerViewModel.cs` matches one line
    - No `{ get; init; }` was converted to `{ get; }`; the request DTO has no get-only serialized property
    - `dotnet build DeckFlow.Web -warnaserror:CS1591` reports Build succeeded, 0 errors
  </acceptance_criteria>
  <done>DeckPrimerRequest uses mutable null-guard setters with the AiPlatform.Normalize TargetAiPlatform contract; DeckPrimerViewModel is a sealed { get; init; } class defaulting ActiveTab to DeckPrimer; Web builds clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Catalog + request DTO unit tests</name>
  <files>DeckFlow.Web.Tests/PrimerSectionCatalogTests.cs, DeckFlow.Web.Tests/DeckPrimerRequestTests.cs</files>
  <read_first>
    - DeckFlow.Web.Tests/PacketArtifactStoreTests.cs (xUnit style, [Fact], naming, namespace DeckFlow.Web.Tests)
    - DeckFlow.Web/Models/PrimerSectionCatalog.cs (from Task 1)
    - DeckFlow.Web/Models/DeckPrimerRequest.cs (from Task 2)
    - .planning/phases/31-deck-primer-generator/31-PATTERNS.md (test conventions section)
  </read_first>
  <behavior>
    - Catalog_Has31Sections: PrimerSectionCatalog.AllSections.Count == 31.
    - Catalog_Has5Groups: Groups.Count == 5 and labels are exactly {Identity, Combos, Gameplay, Matchups, Maintenance}.
    - Catalog_EverySection_HasHelpText: no entry has null/whitespace HelpText (PRM-12).
    - Gates_ExactlyTwoCedhOnly_OneCasualOnly: CedhOnlySectionIds.Count == 2, CasualOnlySectionIds.Count == 1.
    - GetPreset_Cedh_IncludesCedhOnly_ExcludesCasualOnly: GetPresetForBracket("cEDH") contains the two cEDH-only IDs and not the casual-only ID.
    - GetPreset_Casual_ExcludesCedhOnly: GetPresetForBracket("Core") contains neither cEDH-only ID.
    - Normalize_StripsCedhOnly_ForCasualBracket: NormalizeSelections([cedh-only id, valid common id], "Core") drops the cEDH-only id, keeps the common one.
    - Normalize_StripsCasualOnly_ForCedhBracket: NormalizeSelections([casual-only id, valid common id], "cEDH") drops the casual-only id.
    - Normalize_DropsUnknownIds_AndDedupes: unknown id removed; duplicate collapsed; catalog order preserved.
    - TargetAiPlatform_Setter_NormalizesUnknownToDefault: request.TargetAiPlatform = "bogus" equals AiPlatform.Default.Key.
    - SelectedSectionIds_NullSet_BecomesEmptyList: request.SelectedSectionIds = null becomes an empty (non-null) list.
  </behavior>
  <action>
    Create `DeckFlow.Web.Tests/PrimerSectionCatalogTests.cs` (`public sealed class PrimerSectionCatalogTests`, namespace `DeckFlow.Web.Tests`, xUnit `[Fact]`) implementing the catalog behaviors above. Read the gated section IDs from CedhOnlySectionIds/CasualOnlySectionIds rather than hardcoding strings, so the test does not couple to label choices.

    Create `DeckFlow.Web.Tests/DeckPrimerRequestTests.cs` (`public sealed class DeckPrimerRequestTests`) implementing the request-DTO behaviors. Assert `new DeckPrimerRequest { TargetAiPlatform = "bogus" }.TargetAiPlatform == AiPlatform.Default.Key` and the null-list guard.

    xUnit `[Fact]`, Method_Scenario_ExpectedResult naming, single namespace. LF; no formatter sweep.
  </action>
  <verify>
    <automated>"/mnt/c/Program Files/dotnet/dotnet.exe" test DeckFlow.Web.Tests/DeckFlow.Web.Tests.csproj --filter "PrimerSectionCatalogTests|DeckPrimerRequestTests" 2>&1 | grep -E "Passed!|Failed!|error" | head</automated>
  </verify>
  <acceptance_criteria>
    - File DeckFlow.Web.Tests/PrimerSectionCatalogTests.cs exists with the 9 catalog `[Fact]` methods above
    - File DeckFlow.Web.Tests/DeckPrimerRequestTests.cs exists with the 2 request-DTO `[Fact]` methods
    - `Catalog_Has31Sections` and `Catalog_Has5Groups` assert the counts (proves PRM-04 catalog shape)
    - `dotnet test --filter "PrimerSectionCatalogTests|DeckPrimerRequestTests"` reports Passed with 0 failures
  </acceptance_criteria>
  <done>Catalog (31/5/gates/presets/normalize) and request-DTO (Normalize setter + null guard) behaviors are covered by passing tests; Web.Tests builds and the filtered run is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| form post -> DeckPrimerRequest | Untrusted SelectedSectionIds + TargetAiPlatform + TargetCommanderBracket bound here; normalized/gated downstream |
| uploaded zip -> PrimerAllowedNames | Allowlist that 31-05's ReadEntries enforces against re-uploaded primer zips |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-03 | Tampering | Crafted TargetAiPlatform string posted to the form | mitigate | TargetAiPlatform setter routes through AiPlatform.Normalize(value).Key — unknown values collapse to Default; unit-tested |
| T-31-04 | Tampering | Crafted/unknown SelectedSectionIds or cross-bracket gated IDs posted | mitigate | NormalizeSelections validates against AllSections, strips unknown IDs AND bracket-gated IDs server-side regardless of client toggles; unit-tested |
| T-31-05 | Tampering | Future primer zip upload with an unlisted entry name | mitigate | PrimerAllowedNames added here so 31-05's ReadEntries rejects unlisted entries (defense established before the zip path exists — Pitfall 2) |
| T-31-06 | Denial of Service | List/string null assignment producing NRE downstream | mitigate | Every setter null-guards (value ?? string.Empty / value ?? []) |
| T-31-SC | Tampering | npm/pip/cargo installs | accept | Zero package installs in this plan (RESEARCH §Package Legitimacy Audit: not applicable) |
</threat_model>

<verification>
- `dotnet build DeckFlow.Web -warnaserror:CS1591` clean
- `dotnet build DeckFlow.Web.Tests` clean
- `dotnet test --filter "PrimerSectionCatalogTests|DeckPrimerRequestTests"` green
- `grep -n "PrimerAllowedNames" DeckFlow.Web/Services/PacketArtifactStore.cs` confirms the allowlist landed first (no zip methods added in this plan)
</verification>

<success_criteria>
PRM-04 (31 sections / 5 collapsible groups model), PRM-12 (per-section help text), and the model-side of PRM-03 (bracket presets + gates), PRM-10 (selection persistence contract — IDs are normalizable/gateable), PRM-11 (group membership for badge counts), and PRM-09 (PrimerAllowedNames allowlist) are established as typed contracts with tests. No service/variant/zip/UI behavior in this plan — those consume these contracts in later waves.
</success_criteria>

<output>
Create `.planning/phases/31-deck-primer-generator/31-02-SUMMARY.md` when done.
</output>
