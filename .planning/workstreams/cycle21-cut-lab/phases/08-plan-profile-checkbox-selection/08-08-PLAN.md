---
phase: 08-plan-profile-checkbox-selection
plan: 08
type: execute
wave: 6
# ⛔ GATED ON PHASE 7, transitively via 08-07. Do not execute before 08-07 is complete.
depends_on: ["08-07"]
files_modified:
  - DeckFlow.Web/e2e/cut-lab-plan-panel.spec.ts
autonomous: false
requirements: [PLPR-01, PLPR-02, PLPR-03, PLPR-04, PLPR-05, PLPR-06]

must_haves:
  truths:
    - "A headless end-to-end pass proves the plan panel renders, pre-checks the top themes, and reorders the proposal when a box is toggled."
    - "The panel reads correctly at both the desktop and the mobile viewport with no horizontal page scroll."
    - "A human has confirmed the panel's appearance and behavior at both viewports before the phase is called done."
  artifacts:
    - path: "DeckFlow.Web/e2e/cut-lab-plan-panel.spec.ts"
      provides: "Two-viewport Playwright coverage of the plan panel and its engine effect"
      min_lines: 120
  key_links:
    - from: "DeckFlow.Web/e2e/cut-lab-plan-panel.spec.ts"
      to: "the Cut Lab feature flag"
      via: "the shared admin-tools flag helper used by the other Cut Lab specs"
      pattern: "setToolEnabled"
---

<objective>
Prove the whole phase end to end in a browser and put a human in front of it: one Playwright pass
over the plan panel at two viewports, then a blocking visual check.

Purpose: every prior plan proved its own unit. This is the only artifact that proves the panel, the
engine and the EDHREC layer work together on a real page.
Output: one new e2e spec and a signed-off human verification.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/cycle21-cut-lab/phases/08-plan-profile-checkbox-selection/08-CONTEXT.md
@.planning/workstreams/cycle21-cut-lab/ROADMAP.md

<constraints>
- **UI testing must NEVER open a browser on the Windows host.** Start the app with
  `scripts/run-web-test.sh`, which sets `DECKFLOW_DISABLE_AUTO_BROWSER=true`, and run Playwright headless
  as `env -u DISPLAY npx --no-install playwright test`. Do not use the WSL `gstack` headless daemon — it is
  observed unstable and will not follow a form POST navigation.
- **Ask the user for permission before opening any browser**, including for the checkpoint.
- Cut Lab is flag-gated. Toggle `tool.cut-lab.enabled` through the same admin helper the existing Cut Lab
  specs use, and restore the prior flag state afterwards.
- Cut Lab admin-flag specs are serialized against each other — follow the existing serialization and
  admin-lock convention rather than racing them.
- EDHREC is a live third party. The spec must not fail when EDHREC is unreachable: assert either the theme
  list OR the unavailable message, and assert the generic strategy section unconditionally.
</constraints>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Two-viewport Playwright coverage of the plan panel</name>
  <files>DeckFlow.Web/e2e/cut-lab-plan-panel.spec.ts</files>
  <read_first>
    - DeckFlow.Web/e2e/bracket-smoke.spec.ts (the multi-viewport screenshot and flag-toggle pattern this
      spec copies)
    - DeckFlow.Web/e2e/cut-lab-smoke.spec.ts (as migrated by 08-07 — the current intake flow, now without
      the primary-plan field, and the helper it uses to reach a processed pool)
    - DeckFlow.Web/e2e/support/admin-tools.ts and support/admin-lock.ts (the flag helper and the
      serialization lock)
    - DeckFlow.Web/Views/Deck/CutLab.cshtml (as written by 08-07 — the panel's actual selectors)
  </read_first>
  <behavior>
    - Panel presence: after a deck is processed, the `cut-lab-plan-panel` root is visible and the intake no
      longer exposes a free-text plan field.
    - Documentation rule: every rendered strategy checkbox has a visible definition line and a visible
      consequence line — assert per checkbox, not just that some text exists somewhere in the panel.
    - Commander themes: either the theme checkboxes render in non-increasing deck-count order with at most
      three pre-checked, or the unavailable message renders. Both outcomes are a pass; a page error is not.
    - Zero-selection notice: with every box cleared, the notice is visible.
    - Engine effect: record the proposed card name with nothing checked, check a generic strategy that the
      pool clearly matches, and assert the proposed card name changes or the panel-driven finding appears —
      this is the end-to-end proof that a checkbox reaches the engine.
    - Round trip: reload the page and confirm the checked boxes come back checked.
    - Viewports: run the presence, documentation, notice and no-horizontal-scroll assertions at both 1280px
      desktop and 390x844 mobile, capturing a screenshot at each for the checkpoint.
  </behavior>
  <action>
    Create `DeckFlow.Web/e2e/cut-lab-plan-panel.spec.ts` following `bracket-smoke.spec.ts`'s structure:
    acquire the admin lock, enable `tool.cut-lab.enabled` in a setup hook, restore the previous flag value in
    teardown, and run the tests serially.

    Cover every behavior listed above. Write the commander-theme assertion as an explicit branch on which of
    the two states rendered, and record in the SUMMARY which branch actually ran during the verification pass —
    a run that only ever exercised the unavailable branch has not tested the theme list.

    Assert absence of horizontal page scroll at the mobile viewport by comparing the document scroll width
    against the viewport width, the way the repo's other mobile specs do; a screenshot alone does not prove it.

    Save both screenshots under the phase's screenshot directory so the checkpoint has artifacts to review.
  </action>
  <verify>
    <automated>scripts/run-web-test.sh &amp; sleep 20 &amp;&amp; env -u DISPLAY npx --no-install playwright test DeckFlow.Web/e2e/cut-lab-plan-panel.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - The spec passes headless with no browser window appearing on the Windows host.
    - `grep -n "setToolEnabled" DeckFlow.Web/e2e/cut-lab-plan-panel.spec.ts` shows the flag enabled in setup
      and restored in teardown.
    - Both a 1280px and a 390x844 screenshot exist after the run.
    - The mobile test asserts document scroll width is not greater than viewport width.
    - The commander-theme test handles both the populated and the unavailable branch, and the SUMMARY records
      which one ran.
    - The engine-effect test asserts an observable change after toggling a checkbox, not merely that the
      checkbox is checked.
  </acceptance_criteria>
  <done>A headless two-viewport pass proves the panel renders, documents itself, round-trips, and measurably changes engine output.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human visual and behavioral verification at both viewports</name>
  <action>
    Before pausing, make sure everything automatable is already done: Task 1's spec passes headless, both
    screenshots exist under the phase's screenshot directory, the full e2e suite is green, and both test
    projects pass. Then present the what-built summary and the numbered steps below and wait for the
    resume signal. Do not open a browser on the user's behalf — ask first.
  </action>
  <what-built>
    The full Phase 8 plan profile: the twelve generic strategy checkboxes with definitions and consequence
    lines, the EDHREC commander theme section with top-three pre-checking, the zero-selection no-op notice,
    and the four engine effects — on-plan protection, off-plan-first proposal ordering, plan-driven floor
    deltas and the stranded-off-plan-package finding. Automated coverage: unit tests across seven plans plus
    the two-viewport Playwright pass from Task 1.
  </what-built>
  <how-to-verify>
    1. Ask permission before opening a browser. Then start the app with `scripts/run-web-test.sh` — it
       suppresses the Windows-host auto-launch — and open `http://localhost:5173` yourself.
    2. Confirm `tool.cut-lab.enabled` is on for this local instance, then import a real 110+ card deck.
    3. On the intake step, confirm there is no free-text "Primary plan" or "Secondary plan" box.
    4. Reach the plan panel. At roughly 1440px wide, check that every strategy checkbox shows both a
       definition and a consequence line, and that nothing is truncated or overlapping.
    5. With nothing checked, read the no-op notice and confirm the next proposed card. Note it down.
    6. Check two generic strategies that genuinely describe the deck. Confirm the proposed card changes and
       that the reason shown cites your selection rather than generic copy.
    7. Look at the role-floors table. Confirm the Bracket and Commander columns still show the numbers they
       showed before, and that any raise is attributable to the plan rather than silently folded into the
       bracket column.
    8. If the commander theme section loaded, check a theme and confirm cards from it stop being proposed
       first. If it shows "unavailable", confirm the generic strategies still work — both are valid.
    9. Resize to 390x844 and repeat steps 4 and 5. Confirm no horizontal page scrolling.
    10. Reload the page and confirm your checked boxes came back.
    11. Compare the two screenshots from Task 1 against what you see.
  </how-to-verify>
  <resume-signal>Type "approved", or describe what looked wrong at which viewport and which step.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → local app instance | The e2e run toggles a feature flag through the admin surface |
| local app → live EDHREC | The verification pass makes real outbound requests to a third party |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-08-01 | Tampering | The spec leaving `tool.cut-lab.enabled` on after the run | mitigate | The flag's prior value is captured in setup and restored in teardown, following the existing Cut Lab spec convention; the admin lock prevents a concurrent spec from observing the toggled state. |
| T-08-08-02 | Denial of Service | The verification pass hammering EDHREC | mitigate | The service memoizes in memory and on disk, only checked themes are fetched, and a single local verification run is a handful of requests. The 10s timeout bounds a hang. |
| T-08-08-03 | Information Disclosure | Screenshots capturing local session or deck data | accept | Screenshots show a synthetic test deck on a localhost instance with no account data; they are committed as phase artifacts exactly as the Phase 7 mockups were. |
| T-08-08-SC | Tampering | npm / NuGet installs | mitigate | No packages added; Playwright is already present and is invoked with `--no-install`. |
</threat_model>

<verification>
1. Phase 7 and plan 08-07 are complete before this plan runs.
2. `dotnet test DeckFlow.Web.Tests/DeckFlow.Web.Tests.csproj` and `dotnet test DeckFlow.Core.Tests/DeckFlow.Core.Tests.csproj`
   both pass at or above the recorded baselines.
3. `env -u DISPLAY npx --no-install playwright test DeckFlow.Web/e2e/` — the whole e2e suite, not only the new
   spec, passes against a server started by `scripts/run-web-test.sh`.
4. The human checkpoint returned "approved".
5. `git status --porcelain DeckFlow.Web/wwwroot/js/` is empty.

**Closeout note, not a task for this plan:** PLPR-01 through PLPR-06 are proposed IDs and do not yet exist in
`.planning/workstreams/cycle21-cut-lab/REQUIREMENTS.md`. They must be ratified there before milestone
closeout, alongside the same outstanding gap already recorded for SCRY-01..04, CLUX-01..08 and the Phase
01.1 / 01.2 classifier IDs.
</verification>

<success_criteria>
- The plan panel is proven end to end at two viewports, headless, with no browser opened on the Windows host.
- A checkbox toggle produces an observable change in the proposed card.
- A human has reviewed both viewports and approved.
</success_criteria>

<output>
Create `.planning/workstreams/cycle21-cut-lab/phases/08-plan-profile-checkbox-selection/08-08-SUMMARY.md` when done.
</output>
