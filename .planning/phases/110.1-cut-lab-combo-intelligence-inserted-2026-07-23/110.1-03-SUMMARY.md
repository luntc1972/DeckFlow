# Phase 110.1 Plan 03 Summary

## What changed

- Updated `DeckFlow.Web/Views/Deck/CutLab.cshtml` so structural evidence chips render a nested `cutlab-combo-badge` span inside the existing `button[data-cut-lab-chip-card]` when `Model.ComboBadgeByCardName` contains the card.
- Kept `data-cut-lab-chip-card` and `aria-pressed` on the outer button, with badge copy `Combo piece` for complete combos and the server-authored `Needs ...` text for near combos.
- Reworked `RenderCardTextDisclosure(...)` so combo-only cards still render the existing `cutlab-card-text` disclosure when there is no `CardTextByCardName` entry, and added the combo context as `<p class="cutlab-card-text__combo">` inside the existing disclosure body.
- Added combo badge styling to `DeckFlow.Web/wwwroot/css/site-common.css` only, using existing Cut Lab/theme tokens already used by chip surfaces.
- Extended `DeckFlow.Web/wwwroot/ts/cut-lab.ts` with `comboBadgeByCardName` on `CutLabUiPatch`, re-applied combo badges during structural chip rebuilds, and refreshed/removes the cloned disclosure `.cutlab-card-text__combo` paragraph from the patch DTO so what-if/re-decide context does not go stale.
- Extended `DeckFlow.Web/ts-tests/cut-lab-lock-interactions.test.ts` with a nested-badge button fixture proving lock/unlock still toggles by clicking the child badge span without any production lock-logic change.
- Extended `DeckFlow.Web/ts-tests/cut-lab-structural-cardtext.test.ts` with combo badge patch data and assertions for both badge re-application and disclosure combo-context refresh.

## Verification

- `cd DeckFlow.Web && npx tsc -p tsconfig.json --noEmit`
  - Result: PASS
  - Errors: 0
- `cd DeckFlow.Web && npx vitest run cut-lab-lock-interactions cut-lab-structural-cardtext`
  - Result: PASS
  - Test files: 2 passed
  - Tests: 10 passed, 0 failed
- `'/mnt/c/Program Files/dotnet/dotnet.exe' build DeckFlow.sln`
  - Result: PASS
  - Warnings: 0
  - Errors: 0

## Confirmations

- `git diff DeckFlow.Web/wwwroot/css/site.css` is empty.
- The combo badge `<span>` is a descendant of the existing structural evidence `<button data-cut-lab-chip-card="...">` element in both Razor render and TypeScript what-if re-render paths.

## Deviations

- None.
