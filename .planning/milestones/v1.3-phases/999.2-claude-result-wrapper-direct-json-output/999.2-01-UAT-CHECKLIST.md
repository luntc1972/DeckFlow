# Phase 999.2 — Manual UAT Checklist

**Phase:** Claude `<result>` Wrapper — Direct JSON Output Option
**Plan:** 999.2-01-PLAN.md, Task 7
**Dev server:** `scripts/run-web.sh` → http://localhost:5173

**Signal back:**
- `approved` — all checks pass
- `failed: <step #> <description>` — halt + offer per-commit revert (D-09/D-14)
- `partial: <steps passed>` — note coverage gap (legacy zip step 30 or Gemini step 32)

---

## Automated gate (pre-UAT, already PASSED 2026-05-20)

- [x] **1.** `cd /mnt/c/users/chrislunt/source/personal/deckflow`
- [x] **2.** `grep -rn 'ResultWrapInstruction' DeckFlow.Web/Services/PromptBuilders/*/Claude*.cs` → 0 hits
- [x] **3.** `grep -rn '<result>' DeckFlow.Web/Services/PromptBuilders/*/Claude*.cs` → 0 hits
- [x] **4.** `grep -rn 'ResultWrapInstruction' DeckFlow.Web/Services/PromptBuilders/` → 10 hits (5 ChatGPT + 5 Gemini)
- [x] **5.** `grep -rn 'ResultWrapInstruction' DeckFlow.Web/Services/PromptBuilders/*/Gemini*.cs` → 5 hits
- [x] **6.** `grep -rn 'GeminiJsonMandate' DeckFlow.Web/Services/PromptBuilders/*/Gemini*.cs` → 5 hits
- [x] **7.** `grep -rn 'fenced .*json code block' DeckFlow.Web/Services/PromptBuilders/*/Claude*.cs` → 5 hits
- [x] **8.** `dotnet.exe build DeckFlow.sln -c Release --nologo /v:m` → exit 0, 0 warnings, 0 errors
- [x] **9.** Commit chain (6 in order, plain default-author, no Co-Authored-By):
  - `c070b61` ClaudeAnalysisPromptVariant
  - `0286895` ClaudeComparisonPromptVariant
  - `577daee` ClaudeFollowUpPromptVariant
  - `7fbdf8a` ClaudeMetaGapPromptVariant
  - `0cf870d` ClaudeSetUpgradePromptVariant
  - `fcb00f7` ResultContractTests update
- [x] **10.** `grep -ni 'result>\|wrapper' README.md` → 2 hits both unrelated (bash wrapper line 146, HTTP 429 wrapper line 606); zero Claude-tied refs

---

## T3 — Claude / deck-analysis — PASSED 2026-05-20

Covers: `ClaudeAnalysisPromptVariant` + `ClaudeSetUpgradePromptVariant` (via `requiresFullDecklists` toggle) + `ClaudeFollowUpPromptVariant` (follow-up panel).

- [x] **11.** Navigate http://localhost:5173/deck-analysis (Step 1 — paste deck)
- [x] **12.** Step 2: select **Claude** in `_AiSelector`
- [x] **13.** Step 3: paste real deck (Atraxa or bookmarked T3 fixture)
- [x] **14.** Step 4: click "Generate Packet", download zip
- [x] **15.** Open generated `*.txt` packet. Confirm:
  - [x] Contains `fenced \`\`\`json code block`
  - [x] Does NOT contain `<result>`
  - [x] Does NOT contain `Wrap your final structured output in`
- [x] **16.** Paste prompt body into claude.ai
- [x] **17.** Observe Claude response: ONE fenced ` ```json ` block, NO `<result>` tags
- [x] **18.** Copy Claude full response → paste into DeckFlow "Paste response" textarea → submit
- [x] **19.** Confirm parser extracts JSON cleanly; UI renders `deck_profile` fields (game_plan, speed, strengths, weaknesses, deck_needs, weak_slots)
- [x] **20.** **SetUpgrade sub-coverage:** if T3 deck has `requiresFullDecklists` toggled, prompt body includes set-upgrade variant — re-run Claude paste, verify same clean-output on set-upgrade JSON
- [x] **21.** **FollowUp sub-coverage:** click "Ask Claude a follow-up question" if exposed; verify follow-up prompt body fenced-only / no-`<result>`

**Notes:** Initial set-upgrade-submit failure was user error (analysis JSON pasted into set-upgrade textarea); re-pasted correct response, parsed cleanly.

---

## T7 — Claude / cEDH meta-gap — PASSED 2026-05-20

Covers: `ClaudeMetaGapPromptVariant`.

- [x] **22.** Navigate http://localhost:5173/cedh-meta-gap
- [x] **23.** Step 2: select **Claude**
- [x] **24.** Fill commander + filters (cached T7 fixture; "Atraxa" + defaults usually returns hits)
- [x] **25.** Generate packet → paste into Claude → verify:
  - [x] Prompt has `fenced \`\`\`json code block`
  - [x] No `<result>` in prompt or response
  - [x] Response pastes back, parses cleanly into `meta_gap` JSON

---

## Ad-hoc — Claude / deck-comparison — PASSED 2026-05-20

Covers: `ClaudeComparisonPromptVariant`.

- [x] **26.** Navigate http://localhost:5173/deck-comparison
- [x] **27.** Step 2: select **Claude**
- [x] **28.** Paste two real decks (cached pair)
- [x] **29.** Generate packet → paste into Claude → verify:
  - [x] Prompt has `fenced \`\`\`json code block` + `deck_comparison`
  - [x] No `<result>` in prompt or response
  - [x] Response pastes back, parses cleanly into `deck_comparison` JSON

---

## Legacy zip regression (D-12, optional) — PASSED 2026-05-20

- [x] **30.** If pre-999.2 zip artifacts on disk (Phase 13 UAT T4 or earlier), drag into "Upload previous packet" flow on any page. Confirm legacy artifact loads + round-trips (exercises `ExtractJsonPayload` `<result>`-regex branch).

---

## ChatGPT + Gemini non-regression (D-02 invariant) — PASSED 2026-05-20

- [x] **31.** On any page, re-run packet generation with **ChatGPT** selected. Confirm prompt body STILL contains `ResultWrapInstruction` output (substring `Wrap the entire JSON response in <result>...</result>`). Phase 999.2 must NOT have touched ChatGPT.
- [x] **32.** If `DECKFLOW_GEMINI_ENABLED` set, repeat with **Gemini**. Confirm prompt body STILL ends with `GeminiJsonMandate` block (`MANDATORY — DO NOT SKIP` + ending `Nothing else after </result>.`). Phase 999.2 must NOT have touched Gemini.

---

## UAT VERDICT: APPROVED — 2026-05-20

All 32 steps PASS. Phase 999.2 closes clean.

**Issues surfaced during UAT (pre-existing, NOT Phase 999.2 regressions):**
- `DeckAnalysisDownload` re-runs full Scryfall pipeline (no session cache between preview-generate and zip-download). Backlog candidate.
- `MetaGapService` (and likely Analysis/Comparison siblings) bubbles raw `JsonReaderException` to error page when user pastes truncated JSON. Backlog candidate: graceful "response truncated, wait for completion before copying" message.

---

## Coverage map

| Variant | Covered by |
|---|---|
| ClaudeAnalysisPromptVariant | T3 (steps 11-19) |
| ClaudeSetUpgradePromptVariant | T3 sub-coverage (step 20) |
| ClaudeFollowUpPromptVariant | T3 sub-coverage (step 21) |
| ClaudeMetaGapPromptVariant | T7 (steps 22-25) |
| ClaudeComparisonPromptVariant | Ad-hoc comparison (steps 26-29) |
| Parser legacy branch (D-12) | Step 30 (optional) |
| ChatGPT/Gemini invariant (D-02) | Steps 31-32 |
