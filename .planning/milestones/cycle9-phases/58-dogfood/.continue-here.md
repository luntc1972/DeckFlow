---
context: phase
phase: 58-dogfood
task: 2
total_tasks: 3
status: in_progress
last_updated: 2026-06-19T03:34:08.923Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> Discovered through actual failure this session. Acknowledge before proceeding.

- [ ] CONSTRAINT: **AI never writes prod** — DirectPush + git-Publish prod writes are operator-only. The resuming agent must NOT run DirectPush, push seeds to deploy, or write the Render Postgres. Stamp/verify only via read-only paths.
- [ ] CONSTRAINT: **DeckFlow prod reads via Render MCP only** — use `mcp__render__query_render_postgres` (postgresId `dpg-d7oj8iugvqtc73fso0g0-a`, read-only by design). NEVER use the write-capable `Studio:ProdConnectionString` for queries, even reads.

**Do not proceed until both boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| git-Publish from partial store | Studio `/publish` regenerated the seed from a partial 22-approved local store → rewrote `index-seed.json` 86→22 (removed 86); would wipe prod KB on deploy | blocking | For the dogfood, publish ONLY via DirectPush (upsert-only, never deletes). If git-Publish is used, diff `content-kb/seed/index-seed.json` entry count before committing; revert if it drops. |
| WSL→Windows env not inherited | `dotnet.exe` launched from WSL did not see `DECKFLOW_LLM_PROVIDER` → defaulted openai/Metered → distill classifier refused | blocking | Launch Studio with `export WSLENV=DECKFLOW_LLM_PROVIDER:DECKFLOW_LLM_CLI_COMMAND:DECKFLOW_DISABLE_AUTO_BROWSER:ASPNETCORE_ENVIRONMENT` before the dotnet.exe run. Verify via `cmd.exe /c "echo %DECKFLOW_LLM_PROVIDER%"`. |

<current_state>
Phase 58 (Dogfood), plan 58-02, Task 2 of 3. SC1 + SC3 DONE and recorded; SC2/SC4 pending an operator DirectPush.

The dogfood entry `e3qGnuupp8U` ("Stop Building Like Everyone Else", Command Zone 743) was harvested + distilled ($0 subscription, llm_calls=3) and judged higher-quality than baseline `content-kb/the-command-zone/6oS1E5BGi0U.md` (tag discipline 3 vs 12 archetype tags; cleaner paste-ready clips). It is still `approval_status=pending` in the Studio local store and ABSENT from prod (verified read-only: prod content_site_index = 108 rows, 22 pushed, dogfood_present=0).
</current_state>

<completed_work>
- 58-01: scaffolding (58-VERIFICATION-QUERIES.md + baseline) — Done (f5483a6)
- 58-02 Task 1 (SC1): real harvest+distill of e3qGnuupp8U, quality verdict HIGHER — Done; recorded in 58-DOGFOOD-RESULTS.md
- SC3 (spend): $0 subscription ≤ $15 cap — Done; recorded
- Side fix: Review.razor content-preview wrap (ebae1c10); seed-gut revert (4185f6a3); cycle9 pushed to origin (..4185f6a3)
</completed_work>

<remaining_work>
- 58-02 Task 2 (SC2 publish): OPERATOR approves e3qGnuupp8U in /review + runs DirectPush (prod write). Not started — entry still pending.
- 58-02 Task 3 (SC2 verify): confirm `Published` in Studio + DEPLOYED deckflow.gg/Admin/ContentKb (local Web :5173 can't show it — separate db + 503 CF guard).
- 58-03 (SC4): no-regression re-check (prod corpus intact 108 rows, additive-only so far) + Overall Verdict in 58-DOGFOOD-RESULTS.md.
- Then: Cycle 9 closeable → squash to main + CalVer tag.
</remaining_work>

<decisions_made>
- Codex = all gpt-5.4 medium this session (gpt-5.5 400s on this login).
- Skip research, plan directly — dogfood runs already-built 55-57 machinery.
- Publish via DirectPush (upsert-only safe), not git-Publish (gutted seed once).
- Prod reads via Render MCP read-only tool, never the write-capable Studio conn.
</decisions_made>

<blockers>
- e3qGnuupp8U stuck `pending` → operator must approve + DirectPush before SC2/SC4 can close. AI cannot do the prod write.
</blockers>

## Required Reading (in order)
1. `.planning/phases/58-dogfood/58-DOGFOOD-RESULTS.md` — evidence doc; SC1/SC3 filled, SC2/SC4 are the open slots.
2. `.planning/phases/58-dogfood/58-VERIFICATION-QUERIES.md` — baseline + the SELECT/diff queries + local-vs-prod db split notes.
3. `.planning/phases/58-dogfood/58-02-PLAN.md` — the operator runbook (Tasks 2/3).

## Infrastructure State
- Studio: running background on http://localhost:5271 (launched with WSLENV → provider=claude subscription; prod+SCP configured for DirectPush). Data dir `DeckFlow.Studio/artifacts/studio/`, db `content-kb.db`.
- Web: running background on http://localhost:5173 (admin admin/dogfood). `/Admin/ContentKb` returns 503 locally (prod CF-Connecting-IP guard) + reads separate `artifacts/content-site-index.db` → the SC2 web check is the DEPLOYED site only.
- Prod: Render Postgres `deckflow`, postgresId `dpg-d7oj8iugvqtc73fso0g0-a`. 108 rows, 22 pushed, dogfood absent.
- git: branch `cycle9`, pushed to origin at `4185f6a3`. 58-DOGFOOD-RESULTS.md edits uncommitted.

<context>
Final phase of Cycle 9. Two success criteria left and both hinge on one operator action (approve + DirectPush the new entry). I'm holding the no-AI-prod-write line — operator does the push, I verify read-only via Render MCP. After SC2/SC4 confirm and the verdict is written, the milestone is closeable.
</context>

<next_action>
Start with: ask the operator to (1) approve `e3qGnuupp8U` in Studio /review, (2) run DirectPush (Stage 1→3) themselves. Then re-run the read-only prod check via `mcp__render__query_render_postgres` (postgresId `dpg-d7oj8iugvqtc73fso0g0-a`): confirm `dogfood_present=1` + `pushed_to_prod_utc` non-null; verify Studio + deployed /Admin/ContentKb show `Published` (SC2); fill SC2/SC4 + Overall Verdict in 58-DOGFOOD-RESULTS.md and commit.
</next_action>
