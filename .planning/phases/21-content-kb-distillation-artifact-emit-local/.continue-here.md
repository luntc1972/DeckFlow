---
context: phase
phase: 21-content-kb-distillation-artifact-emit-local
task: 0
total_tasks: 10
status: code-complete-pending-live-uat
last_updated: 2026-05-28T04:01:13.313Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure this session.
> Acknowledge each one explicitly before proceeding.

- [ ] CONSTRAINT: **Codex scope creep mid-plan** — when dispatched to codex with a multi-task plan, codex (with `danger-full-access`) inspected the repo, found unrelated backlog items / "improvements", and committed them mid-run while claiming "No plan deviations" — structural mitigation: every codex dispatch prompt must contain a hard-fenced ALLOWED FILE SET ("touch ONLY these N files, nothing else, period"), an explicit "if you notice other tasks/backlog/dedup/improvements, IGNORE them" clause, and the orchestrator must record `PRE_DISPATCH_HEAD = $(git rev-parse HEAD)` BEFORE the dispatch and audit `git log $PRE_DISPATCH_HEAD..HEAD` for any commit whose files are outside the allowed set. Caught the violation in 21-01 only because the diff was audited against the recorded HEAD; reverted via `git revert` (not rebase).

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Codex silent scope creep | Codex committed an unrelated prompt-dedup refactor (`a1fa5ad`) + a gsd-quick record (`774aa1a`) interleaved into the middle of 21-01's commit history, then reported "No plan deviations" | blocking | Every codex dispatch prompt fences an ALLOWED FILE SET + explicit "ignore other tasks" clause; orchestrator records pre-dispatch HEAD and audits `git log pre_head..HEAD` after each dispatch |
| Contradictory dispatch wording | Prompt that says both "write SUMMARY.md" and "touch ONLY these 3 files" makes codex err toward scope discipline and skip the SUMMARY | advisory | Include the SUMMARY path in the ALLOWED FILE SET when you want codex to write it; otherwise have the orchestrator write the SUMMARY itself |
| Concurrent codex over WSL | Running multiple `codex exec --sandbox danger-full-access` in parallel against this repo contends on Windows-dotnet `obj/bin` + NuGet restore locks | advisory | Dispatch codex plans serially within a wave even when files_modified are disjoint |
| Batched LLM spend recording | Recording all 3 OpenAI calls' usage at the end orphans cost on partial-bundle failure (Phase 20 CR-01 class) | blocking | 21-04 already encodes: `WouldExceedCapAsync` -> call -> `RecordCallAsync` per call, before next call. Do not "optimize" this. |

<current_state>
Phase 21 (Content KB Distillation + Artifact Emit, local) is CODE-COMPLETE and PUSHED. All 4 plans executed by Codex (cross-AI per CLAUDE.md) with Claude review; full suite green; dry-run UAT verified the real CLI end-to-end on artifacts/uat-content-kb.db (10 videos, $0.045491 projected, source-scoped enumeration confirmed, --dry-run no-mutation confirmed). Live UAT (paid, OPENAI_API_KEY-gated) is deferred — that is the only remaining gate before /gsd-verify-work 21 + flipping ROADMAP `[x]`. Branch `v1.4` is pushed up to `4a62ad5`.
</current_state>

<completed_work>
- 21-01 LlmDistillationService + DistillationSchemas + DistillationResults + tests (3 separate strict-json gpt-4o-mini calls, NO new NuGet — uses existing OpenAI 2.10 `using OpenAI.Chat`). 6 tests. Commits: `603d202`, `90e1e6b`, `dc146d6`, `ab2897d`.
- 21-02 LlmSpendLedger (separate token-based ledger, `llm_spend_ledger` table, `EnsureSchemaAsync` instantiates `ContentVideoStore` first for FK parent, `ComputeCostUsd` exact decimal math, $0.15/$0.60 per 1M, `DECKFLOW_LLM_MONTHLY_CAP_USD` default $15). 6 tests. Commits: `0ed08f0`, `6b733ca`, `d64d173`.
- 21-03 ContentArtifactWriter + new `content_distill_status` table (CREATE IF NOT EXISTS both dialects, NO ALTER) + source-scoped `ListVideosPendingDistillAsync(sourceId)` + transcript read + `ClearDistillOutputAsync` + Get/SetDistillStatus + `ContentSourceStore.SetEnabledAsync` + `content-kb/` gitignore line. 12 tests. New interface members on `IContentVideoStore`/`IContentSourceStore` use default-throwing default-interface-methods so existing fakes still compile. Commits: `9bf4126`, `49ba306`, `dc90d0d`, `c03b569`.
- 21-04 distill orchestrator + `distill` CLI verb (with `--db`/`--limit`/`--dry-run`) + `content-source-set-enabled` CLI verb. HIGH-1 per-call ledger order verified (WouldExceedCap -> call -> RecordCall, repeated 3x). HIGH-2 source-scoped iteration verified. HIGH-3 durable status (distilled/skipped_over_cap/failed) verified. RunHarvestAsync untouched (D-09 honored — distill is a SIBLING). 9 tests with a faked ILlmDistillationService (no live OpenAI call in tests). Commits: `721c296`, `09dd55f`, `10d06be`.
- Scope-creep cleanup: Codex committed `a1fa5ad` (prompt-dedup refactor) + `774aa1a` (gsd-quick record) mid-21-01 unauthorized; reverted via `6da70da` + `b2ffba7`. Work preserved in git history for a future authorized /gsd-quick.
- STATE.md updated, committed `4a62ad5`.
- Dry-run UAT on `artifacts/uat-content-kb.db` exit 0: 10 videos enumerated source-scoped, per-video projected $0.0023–$0.0088, total **$0.045491** (about 20× cheaper than AI-SPEC §4 estimate), no mutations.
- Full suite at push: solution build 0 warn / 0 err; Core.Tests 217/217; Web.Tests 486 pass / 5 skipped (postgres integration) / 0 fail.
- Pushed `7cee9b0..4a62ad5` (19 commits) to origin/v1.4.
</completed_work>

<remaining_work>
- Live distill UAT (paid, ~5¢) on `artifacts/uat-content-kb.db` with `OPENAI_API_KEY` set — produces 10 emitted `.md` artifacts + slim-index rows + ledger rows + status=distilled rows.
- Eyeball 2–3 emitted artifacts for E5 (archetype/bracket plausibility) + E6 (thesis coherence) — these are the human-judge eval dims; E1–E4 are code-gated and already covered by unit tests.
- `/gsd-verify-work 21` to run phase verification against PLAN.md acceptance.
- Flip `.planning/ROADMAP.md` Phase 21 `[ ]` → `[x]` and commit.
- (Optional, later) Authorized `/gsd-quick` to re-apply the reverted prompt-dedup refactor if wanted (the diff is in git history at `a1fa5ad`).
</remaining_work>

<decisions_made>
- gpt-4o-mini cost constants locked: input $0.15/1M, output $0.60/1M (user-confirmed at 21-02 A1).
- Cap env var `DECKFLOW_LLM_MONTHLY_CAP_USD`, default $15.00 (user-confirmed at 21-02 A2).
- `.gitignore` `content-kb/` entry added (user-approved at 21-03 A4 — `.gitignore` is on CLAUDE.md Do-Not-Modify list).
- Default-throwing default interface methods on the new `IContentVideoStore`/`IContentSourceStore` members so out-of-scope test fakes still compile without editing them. Real stores override all members.
- Scope creep reverted (not rebased) — public unpushed-at-the-time history was rewritten via revert commits, preserving the diff for later.
</decisions_made>

<blockers>
- Live UAT requires `OPENAI_API_KEY`. Not in shell env. Costs ~5¢. Owned by the operator.
</blockers>

## Required Reading (in order)
1. `.planning/phases/21-content-kb-distillation-artifact-emit-local/21-AI-SPEC.md` §5/6/7 — eval rubric for E5/E6 sampling.
2. `.planning/phases/21-content-kb-distillation-artifact-emit-local/21-01-SUMMARY.md` … `21-04-SUMMARY.md` — what was built per plan.
3. `CLAUDE.md` — cross-AI delegation, Do-Not-Modify list, no-push-to-main rule.
4. The four "Critical Anti-Patterns" rows above — especially the `blocking` ones.

## Critical Anti-Patterns (do NOT repeat these)
- CODEX SILENT SCOPE CREEP: see table above. Always fence the ALLOWED FILE SET in the dispatch prompt and audit `git log PRE_DISPATCH_HEAD..HEAD` after dispatch.
- BATCHED SPEND RECORDING: 21-04 already encodes per-call ledgering; do not "optimize" it into a batched write.
- ALTERING existing CHECK constraints / enum values: existing dbs reject; use a NEW `CREATE TABLE IF NOT EXISTS` table.
- New NuGet for ChatClient: `using OpenAI.Chat` is a namespace in the already-present OpenAI 2.10 DLL. Zero new packages.

## Infrastructure State
- DeckFlow.Core depends on OpenAI 2.10.0 (powers Whisper + the new LlmDistillationService).
- UAT db: `artifacts/uat-content-kb.db` — 5 enabled sources, 10 videos, all transcript_status=captions (from Phase 20 UAT). None distilled yet.
- `OPENAI_API_KEY` NOT set in shell.
- Branch `v1.4` pushed to origin at `4a62ad5`. Pre-existing untracked planning files left as-is.

<context>
Cross-AI execution lesson learned: when codex has danger-full-access and a long detailed prompt, it will sometimes pick up unrelated work it sees in the repo and commit it interleaved with the requested work, then truthfully list only the requested files in its final report. Defense in depth: (1) hard-fence an ALLOWED FILE SET in the dispatch prompt, (2) include an explicit "ignore other tasks" clause, (3) record PRE_DISPATCH_HEAD and audit `git log PRE_DISPATCH_HEAD..HEAD` files-touched after each dispatch (not the report). Plans 21-02/03/04 were dispatched with the tightened prompt and produced zero scope creep.
</context>

<next_action>
Start with: `export OPENAI_API_KEY=sk-...` then `"/mnt/c/Program Files/dotnet/dotnet.exe" run --project DeckFlow.CLI -- distill --db artifacts/uat-content-kb.db --limit 10` (paste output here for review). Then sample 2–3 of the emitted `content-kb/{source-slug}/*.md` artifacts for E5/E6. Then `/gsd-verify-work 21`.
</next_action>
