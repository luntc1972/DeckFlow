---
phase: 93-round-trip-integration-test
plan: 02
type: execute
wave: 2
depends_on: [93-01]
files_modified:
  - DeckFlow.Web.Tests/Integration/RoundTrip/RoundTripSyncLoopTests.cs
autonomous: true
requirements: [SYNC-16]

must_haves:
  truths:
    - "SC1: one [PostgresFact] walks the ENTIRE loop — distill -> Publish/DirectPush -> prod PG store -> web body resolution -> deploy/reseed -> PullFromProd -> reconcile dry-run — against the real Testcontainers Postgres + a real `git init` tree via the 93-01 harness (real coordinators, only LLM/SFTP/deploy-confirm transports faked)"
    - "STORE FLOW is faithful (CH3/W3): distill writes row A to the LOCAL store + artifact root; the test APPROVES row A (SetApprovalStatusAsync) so Publish can export it; Publish exports the approved LOCAL row into the git seed + body; the reseed (ContentKbSeedLoader over /app) is the FIRST path that creates row A in the prod PG store. The test ASSERTS prod row A is ABSENT before the reseed and PRESENT after — so the 'reseed reconstructs prod' proof is non-vacuous, not a store-target artifact"
    - "SC2: served body == published body AND body_sha256 matches at EVERY hop — local-distill-computed == index-seed.json == prod-PG-row (read AFTER reseed) == served-/app-body-recompute — all measured with the single ContentSiteIndexContentSignature.ComputeBodySha256 surface (never a second scheme). The seed assertion is explicit: index-seed.json CONTAINS row A's (and B's) natural key + bodySha256 before the second reseed (W-2/M1)"
    - "SC3 (load-bearing): a PUBLISHED row A AND a DirectPush'd row B are NOT reverted after a SECOND reseed (redeploy) — the no-revert-after-reseed assertion that is the M2/C3 bug the whole flag exists to fix; is_visible + content survive the reseed (content-only upsert never writes visibility). Non-vacuous because both rows are approved + present in the re-exported seed (W-2/M1)"
    - "GIT PREREQ (CH1/CH2): the 93-01 harness has already bootstrapped `git init` + identity + bare `origin` + tracking, so DirectPush's CommitAndPushBodiesAsync (GetSubjectsAheadOfRemoteAsync(origin) -> PushAsync) succeeds instead of throwing DirectPushPushBlockedException"
    - "PullFromProd applies FIELD AUTHORITY: body <- git, approval <- prod, is_visible preserved, and the happy-path divergence stamp is BodyDivergenceStatus.Clean (git-body hash matches prod body_sha256)"
    - "Reconcile dry-run over the coherent loop surfaces ZERO unexpected discrepancies, and a second dry-run is idempotent (zero duplicate discrepancies / no ghost rows)"
    - "The test class comment states D-07 explicitly: this is a LOCAL/manual Docker gate that auto-skips in CI (DECKFLOW_POSTGRES_TESTS + Docker absent) — it is the pre-flip proof harness, NOT a per-PR lock"
  artifacts:
    - path: "DeckFlow.Web.Tests/Integration/RoundTrip/RoundTripSyncLoopTests.cs"
      provides: "The SYNC-16 round-trip [PostgresFact](s): full-loop walk + hash-at-every-hop + no-revert-after-reseed + Pull field-authority + reconcile-idempotent, on the 93-01 harness"
      contains: "PostgresFact"
  key_links:
    - from: "DeckFlow.Web.Tests/Integration/RoundTrip/RoundTripSyncLoopTests.cs"
      to: "DeckFlow.Web.Tests/Integration/RoundTrip/RoundTripHarness.cs (93-01)"
      via: "IClassFixture<PostgresContainerFixture> + harness for real PG store, real git repo, /app deploy-copy, and the four deterministic seams"
      pattern: "RoundTripHarness"
    - from: "the served-body-recompute hop"
      to: "the distill-computed / seed-json / prod-row body_sha256"
      via: "ContentSiteIndexContentSignature.ComputeBodySha256 asserted equal at each hop (SYNC-03 web guard is the same surface)"
      pattern: "ComputeBodySha256"
    - from: "the DirectPush'd + published rows"
      to: "post-second-reseed is_visible/content state"
      via: "ContentKbSeedLoader.LoadIfPresentAsync reseed then GetByNaturalKeyAsync assert-unchanged (no-revert)"
      pattern: "LoadIfPresentAsync"
---

<objective>
Deliver SYNC-16: the automated end-to-end test that locks the whole Content-KB sync loop, on the 93-01 harness. Zero production-code change — this plan adds test assertions only.

The test walks the loop the CONTEXT specifies (`93-CONTEXT.md` §Specific Ideas):
`git init` -> distill(canned LLM) -> body+row+hash -> Publish export seed + commit(real git) -> copy tree -> /app + reseed into **PG** -> web resolve + hash-assert(served==published, hash match) -> DirectPush a 2nd row into PG (`sync.directpush-gitbody` ON, faked confirm) + re-export seed + commit -> **reseed again (redeploy)** + assert **no-revert** -> PullFromProd(field authority, `BodyDivergence.Clean`) -> Reconcile dry-run(zero unexpected discrepancies, idempotent re-run zero dupes).

It asserts the ROADMAP's three success criteria — (SC1) full chain on containerized PG + real git; (SC2) served==published and `body_sha256` matches at every hop; (SC3) published/DirectPush'd rows are not reverted after reseed — plus the Pull field-authority and reconcile-idempotent invariants.

Purpose: no future change can silently reintroduce any Cycle-16 drift class (unreachable body, /app-shadows-/data, revert-after-reseed, Pull clobber, ghost rows, body-hash mismatch). The no-revert assertion is the load-bearing one.
Output: `DeckFlow.Web.Tests/Integration/RoundTrip/RoundTripSyncLoopTests.cs`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/93-round-trip-integration-test/93-CONTEXT.md
@.planning/phases/93-round-trip-integration-test/93-01-PLAN.md

<interfaces>
<!-- Coordinator/store contracts this test drives. Extracted from the codebase — no exploration needed. -->
<!-- The 93-01 harness supplies: real PG prod store + local store, real GitRepository temp repo, /app deploy-copy,
     ContentKb:ContentBase config, FixtureProdReader/FixtureProdStoreFactory, CannedLlmDistillationService,
     RecordingSshArtifactUploader, AppTreeDeployedBodyConfirmer. This plan wires the coordinators to them. -->

Publish (DeckFlow.Studio/ViewModels/PublishCoordinator.cs) ctor + flow:
  public PublishCoordinator(IGitRepository git, IContentKbOrchestrator orchestrator, IContentSiteIndexStore indexStore,
      ContentKbOrchestratorOptions options, PublishStateDeriver deriver);
  // LoadInitDataAsync:63, ExportAndDiffAsync:85 (seed export :97, body copy :110), CommitAsync:203 (StampPushedToProdAsync:217).
  // Shared seed writer: ContentKbOrchestrator.ExportIndexToFileAsync:748.

DirectPush (DeckFlow.Studio/ViewModels/DirectPushCoordinator.cs) ctor + flow:
  public DirectPushCoordinator(IContentSiteIndexStore localStore, ISshArtifactUploader uploader,
      IProdStoreFactory prodStoreFactory, IConfiguration configuration, ContentKbOrchestratorOptions options,
      IGitRepository git, IContentKbOrchestrator orchestrator, IProdContentReader prodReader,
      IDeployedBodyConfirmer confirmer, ILogger<DirectPushCoordinator>? logger = null);
  // flag const :48 (sync.directpush-gitbody); Stage-4 flag read via prodReader.ReadFlagAsync/TryReadFlagAsync
  // (:589/:600) -> set FixtureProdReader.Flag = true to drive ON; ComputeDiffAsync/ClassifyDiff :125/147,
  // UploadArtifactsAsync :212, WriteContentAsync :242 (SeedManaged=true :252, awaiting-confirm :261),
  // VerifyAndPublishAsync :299 (tri-state :321), ConfirmAndPublishAsync :273 (stamp pushed_to_prod_utc + flip is_visible :283-287),
  // CommitAndPushBodiesAsync :405 (seed re-export :486, [skip render] gate :526).

PullFromProd (DeckFlow.Studio/ViewModels/PullFromProdCoordinator.cs) ctor + flow:
  public PullFromProdCoordinator(IContentSiteIndexStore indexStore, IGitRepository git, IProdContentReader prodReader,
      IConfiguration configuration, ContentKbOrchestratorOptions options, ILogger<PullFromProdCoordinator> logger);
  // PullAndClassifyAsync:76 (prod read :92, git-tree resolve :100, classify :125), ApplyAdoptionsAsync:146
  // (body<-git :187, approval<-prod :188), ComputeBodyDivergence:285 -> BodyDivergenceStatus.Clean on match.

Reconcile (DeckFlow.Studio/Services/ContentKbReconcileOrchestrator.cs + DeckFlow.Studio/ViewModels/ReconcileCoordinator.cs)
  — construct exactly as ReconcileFixtureDriveTests.cs:92-95 does (orchestrator: reader, localStore, git, config, logger;
  coordinator: orchestrator, localStore, storeFactory, reader, config, logger). RunDryRunAsync returns Discrepancies;
  a coherent loop -> zero unexpected; a second RunDryRunAsync must be idempotent (no dupes / ghost rows).

Reseed (DeckFlow.Web/Services/Content/ContentKbSeedLoader.cs):
  public ContentKbSeedLoader(ContentKbArtifactPathResolver resolver, IContentSiteIndexStore store, ILogger<...> logger);
  public async Task<int> LoadIfPresentAsync(CancellationToken ct = default);   // :43 reads resolver.SeedFilePath under /app,
  // upserts content-only PRESERVING visibility (UpsertRowPreservingVisibilityAsync, seed_managed=true :90) — this is the
  // no-revert mechanism: reseed must NOT flip is_visible or overwrite a DirectPush'd/published row's content back.

Web body resolution + SYNC-03 guard (drive in-process — no web host needed):
  DeckFlow.Web/Services/Content/ContentKbArtifactPathResolver.cs — TryResolveExistingArtifact(artifactPath, out full) (:102),
    SeedFilePath property (both keyed off ContentKb:ContentBase = /app).
  DeckFlow.Web/Services/Content/ContentKbArtifactBodyResolver.cs:28 — TryReadArtifactTextAsync(artifactPath, ct).
  DeckFlow.Web/Controllers/ContentKbController.cs:136-152 — reads body, computes ComputeBodySha256(raw) :144, warns fail-open
    on mismatch :145. The served-body-recompute hop = TryReadArtifactTextAsync(/app) + ComputeBodySha256, asserted == prod-row hash.

Prod store row reads (DeckFlow.Core/Content/ContentSiteIndexStore.cs):
  GetByNaturalKeyAsync(:243), GetPublishedRowsAsync(:285); row.body_sha256 (:132), seed_managed (:152), IsVisible.

Body-hash surface (THE one surface): ContentSiteIndexContentSignature.ComputeBodySha256(string):131.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: SYNC-16 round-trip [PostgresFact] — full loop + hash-at-every-hop + no-revert (SC1/SC2/SC3)</name>
  <files>DeckFlow.Web.Tests/Integration/RoundTrip/RoundTripSyncLoopTests.cs</files>
  <read_first>
    - DeckFlow.Web.Tests/Integration/RoundTrip/RoundTripHarness.cs + RoundTripSeams.cs (93-01 — the harness + seams this test drives)
    - DeckFlow.Studio.Tests/ViewModels/ReconcileFixtureDriveTests.cs (the driver precedent: dry-run drive, ITestOutputHelper logging, discrepancy grouping)
    - DeckFlow.Studio/ViewModels/PublishCoordinator.cs (LoadInitDataAsync:63, ExportAndDiffAsync:85, CommitAsync:203)
    - DeckFlow.Studio/ViewModels/DirectPushCoordinator.cs (the full stage sequence :125-526; flag const :48; confirmer/uploader use)
    - DeckFlow.Web/Services/Content/ContentKbSeedLoader.cs (LoadIfPresentAsync:43 — the reseed / no-revert mechanism)
    - DeckFlow.Web/Services/Content/ContentKbArtifactPathResolver.cs + ContentKbArtifactBodyResolver.cs:28 (in-process body resolution)
    - DeckFlow.Web/Controllers/ContentKbController.cs:136-152 (the served-body ComputeBodySha256 guard the test mirrors)
    - DeckFlow.Core/Content/ContentSiteIndexContentSignature.cs:131 (ComputeBodySha256)
    - DeckFlow.Core/Orchestration/ContentKbOrchestrator.cs:1181-1290 (DistillVideoAsync inputs: GetLatestTranscriptAsync + the source/video store reads + the unconditional ClassifyAsync ~:1186 + Usage cost math) — seed IContentSourceStore + IContentVideoStore + a transcript
    - DeckFlow.Web/Controllers/Admin/ContentKbDeployedBodyController.cs:71-77 (authoritative natural-key -> row.ArtifactPath -> /app resolution the AppTreeDeployedBodyConfirmer mirrors)
  </read_first>
  <behavior>
    - SC1: the fact runs the whole loop on the real PG container + real git tree; with the PG gate absent it auto-skips (harness fixture behavior), never fails.
    - SC2: at each hop the body_sha256 is EQUAL: the distill-computed hash == the value written into index-seed.json == the prod PG row's body_sha256 == ComputeBodySha256 recomputed over the /app served body; and the served body TEXT equals the published body TEXT.
    - SC3: after Publish (row A) and DirectPush (row B, flag ON, faked confirm), a SECOND reseed (redeploy) is run; BOTH rows retain their is_visible and content (body_sha256 unchanged, not reverted to a prior/hidden state). The content-only reseed upsert never writes visibility.
    - The DirectPush stage runs with sync.directpush-gitbody ON (FixtureProdReader.Flag = true); the AppTreeDeployedBodyConfirmer confirms only after DeployToAppAsync carries row B's body, so ConfirmAndPublishAsync stamps pushed_to_prod_utc + flips is_visible for real.
  </behavior>
  <action>
    Create `DeckFlow.Web.Tests/Integration/RoundTrip/RoundTripSyncLoopTests.cs` — `public sealed class RoundTripSyncLoopTests : IClassFixture<PostgresContainerFixture>`, taking the fixture + `ITestOutputHelper` (mirror ReconcileFixtureDriveTests). Add a class-level XML doc that STATES D-07: this is a local/manual Docker gate (`[PostgresFact]`), auto-skips in CI where DECKFLOW_POSTGRES_TESTS + Docker are absent, and is the pre-flip proof harness for `sync.directpush-gitbody`/`sync.reconcile` — NOT a per-PR lock.

    Write one comprehensive `[PostgresFact]` `RoundTrip_DistillToReconcile_HashMatchesEveryHop_NoRevertAfterReseed` that drives the harness stage by stage, logging each checkpoint via `_output.WriteLine` and asserting with SPECIFIC failure messages:
    1. Boot: `EnsureProdSchemaAsync(conn)`, `InitRepoAsync()` (already bootstraps identity + bare `origin` + tracking — CH1/CH2), wire the REAL coordinators (Publish, DirectPush, PullFromProd, Reconcile) against the harness LOCAL store (distill/Publish source) + PROD PG store (reseed/DirectPush/serve target) + git/seams + FixtureProdReader/Factory (flag OFF initially).
    2. Distill (canned LLM, over a seeded IContentSourceStore source + IContentVideoStore video + transcript; canned ClassifyAsync returns non-drop) row A into the **LOCAL** store -> capture `hashDistill = ComputeBodySha256(writtenBodyA)`; assert the **LOCAL**-store row A.body_sha256 == hashDistill (distill-computed hop). Assert the PROD PG store has NO row A yet (CH3/W3: distill never writes prod).
    3. **Approve row A** (`localStore.SetApprovalStatusAsync(... approved)`) so it is exportable, then Publish row A: ExportAndDiff (seed export + body copy) + Commit (real git). Read the exported `content-kb/seed/index-seed.json` and assert it CONTAINS row A's natural key with `bodySha256 == hashDistill` (approved-only export — W-2/M1; distill-computed == seed-json hop).
    4. `DeployToAppAsync()` (copy committed tree -> /app) then reseed via `ContentKbSeedLoader.LoadIfPresentAsync` into the PROD PG store. Assert prod row A now EXISTS (created by the reseed — the reconstructs-prod proof). Resolve the /app body with `ContentKbArtifactBodyResolver.TryReadArtifactTextAsync` and assert: served TEXT == published TEXT, and `ComputeBodySha256(served)` == prod-row-A.body_sha256 == hashDistill (served-body-recompute hop == SC2 served==published + hash match).
    5. DirectPush row B with `FixtureProdReader.Flag = true` (sync.directpush-gitbody ON): distill+approve row B into the local store, then run the coordinator stages (diff -> upload(recording) -> write content(awaiting-confirm) -> re-export seed + commit(real git, origin exists so push succeeds) -> DeployToAppAsync so /app carries B -> confirm via AppTreeDeployedBodyConfirmer -> ConfirmAndPublish stamps pushed_to_prod_utc + flips is_visible). Assert prod-row-B.body_sha256 == ComputeBodySha256 over B's /app body, and row B is now visible. Assert the re-exported index-seed.json now contains BOTH A and B keys + bodySha256 (W-2/M1) — so the next reseed is non-vacuous for both.
    6. **No-revert (SC3):** run a SECOND reseed (`LoadIfPresentAsync`) simulating a redeploy. Assert BOTH row A (published) and row B (DirectPush'd) still exist, remain visible, and keep their body_sha256 unchanged — i.e. the reseed did NOT revert content or flip visibility. Give this the most specific failure message (this is the M2/C3 load-bearing check).

    Wire coordinators using the CLI/coordinator-test precedents; do NOT stub the artifact-write/hash/upsert/seed-export/commit hops — those are exactly what SYNC-16 proves. If a coordinator dependency cannot be injected without a PRODUCTION code change, STOP and record it as an open question in the SUMMARY (hard constraint: zero production-code change).

    Preserve LF; changed-lines-only formatting.
  </action>
  <verify>
    <automated>dotnet.exe build DeckFlow.sln (0 warnings / 0 errors). Operator (Docker up): `DECKFLOW_POSTGRES_TESTS=1 dotnet.exe test DeckFlow.Web.Tests --filter FullyQualifiedName~RoundTripSyncLoop` — VSTest is unreliable in WSL so this may be operator-run/push-and-watch; without the flag the fact auto-skips.</automated>
  </verify>
  <acceptance_criteria>
    - One `[PostgresFact]` walks distill(local) -> approve -> Publish -> reseed -> resolve -> DirectPush(flag ON) -> second reseed on the real PG store + real git tree (SC1).
    - Store flow asserted: distill writes LOCAL only (prod row A ABSENT before reseed); reseed creates prod row A (PRESENT after); index-seed.json contains A (and B) natural key + bodySha256 before the second reseed (CH3/W3, W-2/M1).
    - body_sha256 equality asserted at every hop: local-distill-computed == seed-json == prod-row(post-reseed) == served-body-recompute, and served body TEXT == published body TEXT, all via ComputeBodySha256 (SC2; grep: no second hashing scheme in the file).
    - After the second reseed, the published row A AND the DirectPush'd row B retain is_visible + body_sha256 (no-revert-after-reseed asserted with a specific message) — non-vacuous because both are approved + in the seed (SC3).
    - Class doc states the D-07 local-gate/auto-skip/not-a-lock semantics; no production .cs modified.
    - `dotnet.exe build DeckFlow.sln` is 0/0.
  </acceptance_criteria>
  <done>The SYNC-16 round-trip fact proves the full loop, hash-at-every-hop, and no-revert-after-reseed on containerized PG + real git; auto-skips without Docker.</done>
</task>

<task type="auto">
  <name>Task 2: Pull field-authority + reconcile dry-run idempotent-zero-dupes assertions</name>
  <files>DeckFlow.Web.Tests/Integration/RoundTrip/RoundTripSyncLoopTests.cs</files>
  <read_first>
    - DeckFlow.Studio/ViewModels/PullFromProdCoordinator.cs (PullAndClassifyAsync:76, ApplyAdoptionsAsync:146 body<-git:187 / approval<-prod:188, ComputeBodyDivergence:285)
    - DeckFlow.Core/Content/SyncDiffEntry.cs (BodyDivergenceStatus enum — Clean on hash match; the 92-01 stamp)
    - DeckFlow.Studio.Tests/ViewModels/ReconcileFixtureDriveTests.cs:92-127 (RunDryRunAsync drive + discrepancy grouping/report assertions to mirror)
    - DeckFlow.Studio/Services/ContentKbReconcileOrchestrator.cs + ViewModels/ReconcileCoordinator.cs (construction + RunDryRunAsync)
    - RoundTripSyncLoopTests.cs (Task 1 — extend the same class; reuse the harness/loop state)
  </read_first>
  <behavior>
    - `PullAndClassifyAsync` returns ONLY entries that DIFFER between the local index and prod (PullFromProdCoordinator.cs:123) — a byte-identical row does NOT appear. To observe a Clean-body entry, the test first creates a DELIBERATE non-body difference (e.g. prod row A has a different `approval_status` — or a differing metadata column — than the local index row) while keeping the git-tree body hash EQUAL to prod `body_sha256`. That surfaces an entry whose `BodyDivergenceStatus` is Clean (body matches; only the metadata/approval differs).
    - Adoption then applies body from git, approval from prod, and PRESERVES is_visible (no clobber).
    - Reconcile dry-run over the coherent loop surfaces zero UNEXPECTED discrepancies (no ghost rows, no false orphans for the published/DirectPush'd rows); a second dry-run returns the same result with zero duplicate discrepancies.
  </behavior>
  <action>
    Extend `RoundTripSyncLoopTests.cs` with the Pull + Reconcile tail — either as continuation checkpoints appended to the Task 1 fact, or as one or two additional `[PostgresFact]` methods that rebuild the coherent-loop state via a shared private helper (Claude's discretion per 93-CONTEXT; keep each assertion's failure message specific and keep the loop-build logic DRY):
    - **Pull field-authority:** FIRST introduce a deliberate non-body difference so an entry is actually returned (PullAndClassifyAsync returns only DIFFERING entries — cs:123): e.g. set prod row A's `approval_status` (or a metadata column) to differ from the local index row, keeping the git-tree body hash EQUAL to prod `body_sha256`. Drive `PullFromProdCoordinator.PullAndClassifyAsync` over the /app git tree + real PG prod read (via FixtureProdReader) and assert the returned entry for A carries `BodyDivergenceStatus.Clean` (body matches; only metadata/approval differs). Run `ApplyAdoptionsAsync` and assert: local body == git body, approval == prod approval, and is_visible is PRESERVED (not overwritten) — the Pull-clobber guard. Keep failure messages specific.
    - **Reconcile idempotent:** construct the reconcile orchestrator + coordinator exactly as ReconcileFixtureDriveTests:92-95 (real orchestrator, real local reconcile store, FixtureProdReader/Factory, real git). Run `RunDryRunAsync` and assert ZERO unexpected discrepancies for the published/DirectPush'd rows (the loop is coherent — no published-orphan, no file-orphan, no body-hash-mismatch, no seed-drift for those rows). Run `RunDryRunAsync` a SECOND time and assert the discrepancy set is idempotent — no duplicate discrepancy ids, no ghost rows introduced by the re-run.

    Do NOT touch any production .cs. Preserve LF; changed-lines-only formatting.
  </action>
  <verify>
    <automated>dotnet.exe build DeckFlow.sln (0 warnings / 0 errors). Operator (Docker up): `DECKFLOW_POSTGRES_TESTS=1 dotnet.exe test DeckFlow.Web.Tests --filter FullyQualifiedName~RoundTripSyncLoop` — may be operator-run per WSL VSTest caveat; auto-skips without the flag.</automated>
  </verify>
  <acceptance_criteria>
    - Pull assertion: a deliberate non-body diff (approval/metadata) makes A's entry appear (PullAndClassifyAsync returns only differing entries); its divergence stamp == BodyDivergenceStatus.Clean; adoption gives body<-git, approval<-prod, is_visible preserved.
    - Reconcile assertion: first dry-run has zero unexpected discrepancies for the coherent rows; second dry-run is idempotent (no duplicate ids / no ghost rows).
    - Loop-build logic is not copy-pasted wholesale — a shared helper (or continuation of the Task 1 fact) rebuilds state.
    - No production .cs modified; `dotnet.exe build DeckFlow.sln` is 0/0.
  </acceptance_criteria>
  <done>Pull field-authority (Clean divergence, no is_visible clobber) and reconcile idempotent-zero-dupes are asserted on the same real-PG + real-git harness.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test host → `git` CLI | temp-repo paths become subprocess arguments (via the real GitRepository, inherited from the 93-01 harness) |
| Test host → Postgres container | row round-trip over the throwaway container (inherited from the 93-01 harness) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-93-03 | Tampering | RoundTripSyncLoopTests git/PG drive | mitigate | Reuses the 93-01 harness (GitRepository/BuildStartInfo ArgumentList shell-out, ephemeral Testcontainers PG); adds no new subprocess or connection surface — assertion-only |
| T-93-SC | Tampering | npm/pip/cargo installs | accept | No package installs — this plan adds one test `.cs` file only; Testcontainers.PostgreSql + the Studio ProjectReference are already in place from 93-01. Nothing to verify |
</threat_model>

<verification>
- `dotnet.exe build DeckFlow.sln` → 0 warnings / 0 errors.
- Operator (Docker up): `DECKFLOW_POSTGRES_TESTS=1 dotnet.exe test DeckFlow.Web.Tests --filter FullyQualifiedName~RoundTripSyncLoop` → green; without the flag every fact auto-skips (D-07).
- No production .cs file modified; EOL clean (LF); changed-lines format gate clean.
</verification>

<success_criteria>
- SC1: one [PostgresFact] walks distill -> Publish/DirectPush -> prod PG store -> web body resolution -> deploy/reseed -> PullFromProd -> reconcile on containerized Postgres + a real git tree.
- SC2: served body == published body and body_sha256 matches at every hop (distill-computed == seed-json == prod-row == served-body-recompute), all via the single ComputeBodySha256 surface.
- SC3: a published AND a DirectPush'd row are not reverted after a second reseed (no-revert-after-reseed).
- Pull applies field authority (body<-git, approval<-prod, is_visible preserved, BodyDivergence.Clean); reconcile dry-run is zero-unexpected and idempotent.
- Class comment states the D-07 local-gate semantics; zero production-code change.
</success_criteria>

<output>
Create `.planning/phases/93-round-trip-integration-test/93-02-SUMMARY.md` when done.
</output>
