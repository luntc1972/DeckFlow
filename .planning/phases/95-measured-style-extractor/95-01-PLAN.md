---
phase: 95-measured-style-extractor
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - DeckFlow.Core/Knowledge/CreatorStyleProfile.cs
  - DeckFlow.Core.Tests/CreatorStyleProfileTestData.cs
  - DeckFlow.Core.Tests/CreatorStyleProfileStoreTests.cs
  - DeckFlow.Core/Content/ICreatorProfileSourceStore.cs
  - DeckFlow.Core/Content/CreatorProfileSourceStore.cs
  - DeckFlow.Core/Content/CreatorProfileSource.cs
  - DeckFlow.Core.Tests/CreatorProfileSourceStoreTests.cs
autonomous: true
requirements: [CS-04a, CS-10]
must_haves:
  truths:
    - "A MeasuredMetric can carry a folder-weighted effective sample size without adding a new top-level MeasuredMetric field (D-10)"
    - "A creator's slug -> platform -> Archidekt username/profile-URL + curated per-folder weight map persists and round-trips on both dialects"
    - "An uncurated creator's weights default to 1.0 with a weights_uncurated flag set true"
    - "A creator-level last_crawled_utc freshness marker persists (nullable) and can be stamped by a single-column update without rewriting the curated weights (D-02 short-circuit substrate)"
  artifacts:
    - path: "DeckFlow.Core/Knowledge/CreatorStyleProfile.cs"
      provides: "MetricDistribution.EffectiveSampleSize nested field (D-10)"
      contains: "EffectiveSampleSize"
    - path: "DeckFlow.Core/Content/CreatorProfileSourceStore.cs"
      provides: "Dialect-guarded creator_profile_source store (mirror CreatorStyleProfileStore) with a last_crawled_utc freshness column + SetLastCrawledAsync single-column setter"
      min_lines: 120
    - path: "DeckFlow.Core/Content/CreatorProfileSource.cs"
      provides: "CreatorProfileSource record (slug, platform, username, folder weights, weights_uncurated, last_crawled_utc)"
      min_lines: 20
  key_links:
    - from: "CreatorProfileSourceStore"
      to: "RelationalDatabaseConnection"
      via: "dialect-guarded EnsureSchemaAsync + Dapper UPSERT"
      pattern: "PostgresCreateTableSql|SqliteCreateTableSql"
---

<objective>
Lay the persistence substrate the measured-style extractor writes into: (1) extend the P94 `MetricDistribution` nested record with the D-10 folder-weighted effective-sample field WITHOUT touching any top-level `MeasuredMetric` name, and (2) add the new dialect-guarded `creator_profile_source` store that maps a creator slug to their Archidekt username/profile URL and a manually-curated per-folder weight map, plus a nullable `last_crawled_utc` crawl-freshness marker (the D-02 warm-cache short-circuit substrate Plan 06 reads/stamps).

Purpose: CS-10 requires every emitted metric to carry both raw `NumDecks` and the folder-weighted effective sample; CS-04a requires a NEW creator-profile-source table (NOT `CreatorSourceStore`) to drive the crawler and to hold the D-02 last-crawled marker.
Output: An additive nested schema field + one new dialect-guarded store, both round-trip-tested on SQLite and gated Postgres.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/95-measured-style-extractor/95-CONTEXT.md
@.planning/phases/94-style-profile-foundation/94-CONTEXT.md
@DeckFlow.Core/Content/CreatorStyleProfileStore.cs
@DeckFlow.Core/Knowledge/CreatorStyleProfile.cs

<interfaces>
<!-- P94 schema this plan extends (top-level names LOCKED; nested record extensible per 94-CONTEXT D-08). -->
From DeckFlow.Core/Knowledge/CreatorStyleProfile.cs:
  public sealed record MeasuredMetric { string Metric; double Value; int NumDecks; MetricDistribution? Distribution; }  // top-level names LOCKED — DO NOT add a property here
  public sealed record MetricDistribution { double Mean; double Min; double Max; double StdDev; }  // <- extend THIS with the effective-sample field

From DeckFlow.Core/Content/CreatorStyleProfileStore.cs (the exact store skeleton to mirror):
  - public ctor(string databasePath); public ctor(RelationalDatabaseConnection, bool ensureSchemaEnabled=true); internal ctor(..., Func<CancellationToken,Task<DbConnection>>? connectionFactoryOverride)
  - SemaphoreSlim _schemaGate; volatile bool _schemaReady; EnsureSchemaAsync no-ops when !ensureSchemaEnabled
  - paired const string PostgresCreateTableSql / SqliteCreateTableSql (Postgres BOOLEAN/TIMESTAMPTZ, SQLite INTEGER/TEXT)
  - Dapper DynamicParameters + CommandDefinition UPSERT via ON CONFLICT (slug) DO UPDATE

From DeckFlow.Core/Knowledge/CreatorStyleProfileSections.cs:
  static string? SerializeSection<T>(IReadOnlyList<T>)  // JSON-or-null for empty
  static IReadOnlyList<T> DeserializeSection<T>(string?)  // empty-not-null
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add D-10 EffectiveSampleSize nested field + extend P94 round-trip fixtures</name>
  <read_first>
    - DeckFlow.Core/Knowledge/CreatorStyleProfile.cs (the MetricDistribution record + MeasuredMetric top-level-lock comment)
    - .planning/phases/94-style-profile-foundation/94-CONTEXT.md (D-08: "P95/P97 may extend the nested record but MUST keep the CS-01 top-level field names")
    - DeckFlow.Core.Tests/CreatorStyleProfileTestData.cs (CreateFullProfile + AssertProfilesEqual — the harness to extend, not duplicate)
    - DeckFlow.Core.Tests/CreatorStyleProfileStoreTests.cs (UpsertAsync_ThenGetBySlug_RoundTripsFullShape)
  </read_first>
  <behavior>
    - A MeasuredMetric whose Distribution has EffectiveSampleSize = 8.5 round-trips through UpsertAsync/GetBySlugAsync with 8.5 preserved
    - A MeasuredMetric whose Distribution.EffectiveSampleSize is null round-trips as null (backward compatible with P94 fixtures)
    - Top-level MeasuredMetric shape is unchanged: Metric/Value/NumDecks/Distribution only
  </behavior>
  <action>Add `public double? EffectiveSampleSize { get; init; }` to the `MetricDistribution` record in CreatorStyleProfile.cs (nullable, `{ get; init; }` per the System.Text.Json carve-out — never `{ get; }`), with an XML doc noting it is the D-10 folder-weighted effective sample (fractional) distinct from the raw integer `MeasuredMetric.NumDecks`. Do NOT add any property to `MeasuredMetric`, `CreatorStyleProfile`, or any other top-level record (P94 top-level-names lock). Extend `CreatorStyleProfileTestData.CreateFullProfile` to populate `EffectiveSampleSize` on its measured-metric fixture, and extend `AssertProfilesEqual` to assert the field. Keep the existing null case covered so P94's partial-profile tests still pass.</action>
  <verify>
    <automated>dotnet test DeckFlow.Core.Tests --filter "FullyQualifiedName~CreatorStyleProfileStoreTests"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "EffectiveSampleSize" DeckFlow.Core/Knowledge/CreatorStyleProfile.cs` returns the new `double?` field on MetricDistribution
    - No new property exists on MeasuredMetric: `grep -c "get; init;" ` on the MeasuredMetric block still shows exactly 4 top-level members (Metric, Value, NumDecks, Distribution)
    - `dotnet test DeckFlow.Core.Tests --filter "FullyQualifiedName~CreatorStyleProfileStoreTests"` passes (round-trip preserves 8.5 and null)
  </acceptance_criteria>
  <done>MetricDistribution carries EffectiveSampleSize (nullable), top-level names untouched, P94 round-trip harness extended and green.</done>
</task>

<task type="auto">
  <name>Task 2: CreatorProfileSource record + dialect-guarded CreatorProfileSourceStore (incl. last_crawled_utc marker)</name>
  <read_first>
    - DeckFlow.Core/Content/CreatorStyleProfileStore.cs (verbatim skeleton: ctors, schema gate, paired DDL consts, Dapper UPSERT)
    - DeckFlow.Core/Content/ICreatorStyleProfileStore.cs (interface shape to mirror)
    - DeckFlow.Core/Content/SlugifySourceName.cs (slug key convention — reuse, do not reinvent)
    - DeckFlow.Core/Knowledge/CreatorStyleProfileSections.cs (JSON serialize-or-null helper for the folder-weight map)
    - 95-PATTERNS.md §"CreatorProfileSourceStore.cs" (explicit "do NOT use CreatorSourceStore — wrong shape")
  </read_first>
  <action>Create `DeckFlow.Core/Content/CreatorProfileSource.cs`: a `sealed record CreatorProfileSource { required string Slug; required string Platform; required string ProfileUsername; string? ProfileUrl; IReadOnlyDictionary<int,double> FolderWeights = ...; bool WeightsUncurated; DateTimeOffset? LastCrawledUtc; DateTimeOffset UpdatedUtc; }` — `FolderWeights` keys on Archidekt `parentFolderId` (int) → weight (double), per D-04; `LastCrawledUtc` (nullable) is the D-02 creator-level crawl-freshness marker Plan 06 reads to decide warm-cache-vs-recrawl and stamps after a successful crawl. Use `{ get; init; }` throughout (System.Text.Json carve-out). Create `ICreatorProfileSourceStore` (EnsureSchemaAsync / UpsertAsync(CreatorProfileSource) / GetBySlugAsync(string) / `SetLastCrawledAsync(string slug, DateTimeOffset whenUtc, CancellationToken cancellationToken = default)` — a single-column UPDATE of `last_crawled_utc` so the crawler stamps freshness WITHOUT re-writing the operator-curated weights) and `CreatorProfileSourceStore` mirroring `CreatorStyleProfileStore` EXACTLY: three ctors (path, connection+ensureSchemaEnabled, internal connectionFactoryOverride test seam), `SemaphoreSlim` schema gate, paired `PostgresCreateTableSql`/`SqliteCreateTableSql` for table `creator_profile_source` (columns: `slug TEXT PK`, `platform TEXT NOT NULL`, `profile_username TEXT NOT NULL`, `profile_url TEXT NULL`, `folder_weights_json TEXT NULL`, `weights_uncurated` Postgres `BOOLEAN NOT NULL DEFAULT FALSE` / SQLite `INTEGER NOT NULL DEFAULT 0`, `last_crawled_utc` Postgres `TIMESTAMPTZ NULL` / SQLite `TEXT NULL` (nullable — an uncrawled creator has no marker), `updated_utc` Postgres `TIMESTAMPTZ NOT NULL DEFAULT now()` / SQLite `TEXT NOT NULL DEFAULT (datetime('now'))`). Serialize `FolderWeights` via `CreatorStyleProfileSections.SerializeSection`-style JSON (or `JsonSerializer` directly for the dictionary). Dapper `ON CONFLICT (slug) DO UPDATE` UPSERT writes all columns incl. `last_crawled_utc`; `SetLastCrawledAsync` issues a parameterized `UPDATE creator_profile_source SET last_crawled_utc = @when WHERE slug = @slug` only (no weight rewrite). Honor the F-51-PG-01 lesson: keep the dialect-guarded timestamp column shape for BOTH timestamp columns identical to CreatorStyleProfileStore's `updated_utc`. Register nothing in DI this plan (substrate; Plan 06 wires it). Preserve LF line endings.</action>
  <verify>
    <automated>dotnet build DeckFlow.Core/DeckFlow.Core.csproj</automated>
  </verify>
  <acceptance_criteria>
    - `DeckFlow.Core/Content/CreatorProfileSourceStore.cs` contains both `PostgresCreateTableSql` and `SqliteCreateTableSql` const strings referencing `creator_profile_source`, and both DDLs declare a nullable `last_crawled_utc` column
    - `ICreatorProfileSourceStore` exposes `SetLastCrawledAsync(string, DateTimeOffset, CancellationToken)`; the impl issues a single-column `UPDATE ... SET last_crawled_utc` (does NOT rewrite folder_weights_json)
    - The store has an `internal` ctor accepting `Func<CancellationToken, Task<DbConnection>>? connectionFactoryOverride` (test seam parity with CreatorStyleProfileStore)
    - No reference to `CreatorSourceStore` anywhere in the new files: `grep -rc "CreatorSourceStore" DeckFlow.Core/Content/CreatorProfileSource*.cs` == 0
    - `dotnet build DeckFlow.Core/DeckFlow.Core.csproj` succeeds with 0 new warnings
  </acceptance_criteria>
  <done>New dialect-guarded creator_profile_source store + record compile clean, mirroring the P94 store skeleton, keyed by slug, storing the curated folder-weight map + weights_uncurated flag + a nullable last_crawled_utc marker with a single-column setter.</done>
</task>

<task type="auto">
  <name>Task 3: Round-trip tests for CreatorProfileSourceStore (SQLite + gated Postgres)</name>
  <read_first>
    - DeckFlow.Core.Tests/CreatorStyleProfileStoreTests.cs (SQLite-unconditional + `[PostgresFact]`/`DECKFLOW_POSTGRES_TESTS=1` gated Testcontainers pattern)
    - DeckFlow.Core/Content/CreatorProfileSourceStore.cs (the store under test — from Task 2)
  </read_first>
  <action>Create `DeckFlow.Core.Tests/CreatorProfileSourceStoreTests.cs` mirroring `CreatorStyleProfileStoreTests`'s dual-dialect convention: an unconditional SQLite `[Fact]` proving Upsert→GetBySlug round-trips the full shape (including a curated multi-folder `FolderWeights` map, `WeightsUncurated = false`, and a non-null `LastCrawledUtc`); a `[Fact]` proving the uncurated default case (empty/absent weights → `WeightsUncurated = true` AND `LastCrawledUtc = null` survives write→read); an UPSERT-overwrite `[Fact]` (second upsert on same slug replaces prior weights); a `SetLastCrawledAsync` `[Fact]` proving it stamps `last_crawled_utc` on an existing row WITHOUT altering the folder-weight map (upsert a curated row, call `SetLastCrawledAsync`, re-read: `LastCrawledUtc` updated, `FolderWeights` unchanged key-for-key); and the gated-Postgres equivalent using the same `DECKFLOW_POSTGRES_TESTS=1` Testcontainers gate the P94 tests use. Assert the folder-weight dictionary round-trips key-for-key. Preserve LF line endings.</action>
  <verify>
    <automated>dotnet test DeckFlow.Core.Tests --filter "FullyQualifiedName~CreatorProfileSourceStoreTests"</automated>
  </verify>
  <acceptance_criteria>
    - Test file contains at least: a full-shape SQLite round-trip (incl. LastCrawledUtc), an uncurated-default case (LastCrawledUtc null), an UPSERT-overwrite case, and a SetLastCrawledAsync case proving weights are untouched
    - Postgres test is gated on the same env var/Testcontainers seam as CreatorStyleProfileStoreTests (no unconditional Postgres dependency)
    - `dotnet test DeckFlow.Core.Tests --filter "FullyQualifiedName~CreatorProfileSourceStoreTests"` passes (SQLite path green without Docker)
  </acceptance_criteria>
  <done>SQLite round-trip green unconditionally; Postgres round-trip runs only when DECKFLOW_POSTGRES_TESTS=1; folder-weight map + weights_uncurated + last_crawled_utc fidelity and the single-column setter proven.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → creator_profile_source row | Operator-curated username/profile-URL + folder-weight map is stored (untrusted-ish config; consumed later by the crawler) |
| store → relational DB | Dapper-parameterized writes cross into SQLite/Postgres |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-95-01-01 | Tampering | folder_weights_json column | mitigate | Store a typed `IReadOnlyDictionary<int,double>` via System.Text.Json; deserialize back to the typed shape (no dynamic eval); malformed JSON surfaces as a deserialize exception, not silent bad weights |
| T-95-01-02 | Injection | Dapper UPSERT/SELECT/SetLastCrawled UPDATE | mitigate | Parameterized `DynamicParameters`/`CommandDefinition` only (mirror CreatorStyleProfileStore); never string-interpolate slug/username/timestamp into SQL |
| T-95-01-03 | Information disclosure | profile_url / username | accept | Public Archidekt profile identifiers, no secret content; low value |
| T-95-01-SC | Tampering | package installs | mitigate | No new NuGet/npm/pip packages this plan (reuse Dapper/System.Text.Json only) |
</threat_model>

<verification>
- `dotnet build DeckFlow.Core/DeckFlow.Core.csproj` clean (0 new warnings)
- `dotnet test DeckFlow.Core.Tests --filter "FullyQualifiedName~CreatorStyleProfileStoreTests"` green (P94 harness still passes with the new nested field)
- `dotnet test DeckFlow.Core.Tests --filter "FullyQualifiedName~CreatorProfileSourceStoreTests"` green (incl. LastCrawledUtc round-trip + SetLastCrawledAsync)
- `CarveOutGuard` test still green (records use `{ get; init; }`)
</verification>

<success_criteria>
- MetricDistribution carries `double? EffectiveSampleSize`; no top-level MeasuredMetric/profile field added (D-10 + P94 lock)
- `creator_profile_source` store persists slug→platform→username/URL + curated folder-weight map + weights_uncurated + a nullable last_crawled_utc marker (with a single-column setter) on both dialects (CS-04a + D-02 substrate)
- All new + existing Core tests green; LF preserved
</success_criteria>

<output>
Create `.planning/phases/95-measured-style-extractor/95-01-SUMMARY.md` when done.
</output>
