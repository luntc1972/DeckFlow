---
phase: 999.1-ai-agnostic-prose-adaptation-razor-views
plan: 07
type: execute
wave: 2
depends_on:
  - 999.1-01
  - 999.1-02
  - 999.1-03
  - 999.1-04
  - 999.1-05
  - 999.1-06
files_modified:
  - README.md
autonomous: false
requirements: [D-02, D-03, D-05]
must_haves:
  truths:
    - "README.md workflow descriptions use 'AI' / 'your AI' phrasing instead of hardcoded 'ChatGPT' where the description applies generically to any AI workflow (per CONTEXT.md <deferred> + CLAUDE.md 'README updated when behavior changes')."
    - "All Phase 10 / Phase 13 D-08 / Phase 15 invariants survive the phase: data-cache-key='chatgpt-*' attrs, data-chatgpt-* attrs, chatgpt-packets-form / chatgpt-step-eyebrow CSS classes, parseChatGptDownloadFilename TS const, 'chatgpt' zip filename fallback in PacketArtifactStore.cs, AiPlatform.Key literal 'ChatGPT' value, JsonTextFormatterService.ResultWrapInstruction enumerated form."
    - "Phase 999.1 D-03 carve-outs (JudgeQuestions.cshtml, Home.cshtml line-62 hub card, Help/ask-a-judge.md) are unchanged."
    - "dotnet build DeckFlow.sln -c Release exits 0."
    - "Manual UAT (T1 / T4 / T5 paste-back + filename verify) confirms the three multi-AI pages render brand-neutral prose above the selector and inject the selected platform's DisplayName below."
  artifacts:
    - path: "README.md"
      provides: "Brand-neutral workflow descriptions where AI-agnostic; legitimate ChatGPT-specific references (JudgeQuestions context, historical decision records) preserved."
    - path: ".planning/phases/999.1-ai-agnostic-prose-adaptation-razor-views/999.1-HUMAN-UAT.md"
      provides: "Manual UAT checklist for human verification before phase commit"
      contains: "T1 / T4 / T5"
  key_links:
    - from: "README workflow description sections"
      to: "DeckFlow multi-AI pages (DeckAnalysis / DeckComparison / CedhMetaGap)"
      via: "Narrative description of paste-into-AI workflow"
      pattern: "ChatGPT|your AI"
---

<objective>
Final verification + brand audit wave. This plan:

1. Audits README.md for ChatGPT mentions and generalizes the ones that apply broadly to "any AI workflow" (per CLAUDE.md "README updated when behavior changes" and CONTEXT.md `<deferred>`). Legitimate ChatGPT-specific references (JudgeQuestions-related content, historical decision records, doc-comment style narrative) are preserved.
2. Runs the cross-file invariant grep gate: Phase 10 / Phase 13 D-08 / Phase 15 invariants must remain byte-identical across the codebase.
3. Runs `dotnet build DeckFlow.sln -c Release` clean.
4. Emits `999.1-HUMAN-UAT.md` and waits for the user to manually verify the three pages.

Plan 07 is `autonomous: false` because of the UAT checkpoint at the end. The user must run the dev server (do NOT auto-start per user MEMORY.md feedback) and confirm visually that prose adapts correctly across ChatGPT / Claude / Gemini selections.

Purpose: Lock the phase before commit — invariants intact, build green, human UAT signed off.
Output: Edited `README.md`; `999.1-HUMAN-UAT.md` file with checklist + sign-off line.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/999.1-ai-agnostic-prose-adaptation-razor-views/999.1-CONTEXT.md
@.planning/phases/999.1-ai-agnostic-prose-adaptation-razor-views/999.1-01-SUMMARY.md
@.planning/phases/999.1-ai-agnostic-prose-adaptation-razor-views/999.1-02-SUMMARY.md
@.planning/phases/999.1-ai-agnostic-prose-adaptation-razor-views/999.1-03-SUMMARY.md
@.planning/phases/999.1-ai-agnostic-prose-adaptation-razor-views/999.1-04-SUMMARY.md
@.planning/phases/999.1-ai-agnostic-prose-adaptation-razor-views/999.1-05-SUMMARY.md
@.planning/phases/999.1-ai-agnostic-prose-adaptation-razor-views/999.1-06-SUMMARY.md
@README.md
@CLAUDE.md

<interfaces>
Phase 999.1 invariants grep gate (must all PASS at the end):

Phase 10 invariants (CONTEXT.md `<code_context>` → Phase 10 invariants to grep-verify):
- `request.TargetAiPlatform` (property name on request DTOs) — count unchanged from baseline.
- `name="TargetAiPlatform"` (form field name) — count unchanged.
- `"ChatGPT"`, `"Claude"`, `"Gemini"` as `AiPlatform.Key` string literal values — count unchanged in `AiPlatform.cs`.
- `"chatgpt"` zip filename fallback in `PacketArtifactStore.cs:536-542` — present.
- `<result>...</result>` wrapper convention in `JsonTextFormatterService.cs` — present.

Phase 13 D-08 invariants (TS/CSS-coupled identifiers still deferred):
- `data-cache-key="chatgpt-packets"` (DeckAnalysis.cshtml line 72) — 1 hit.
- `data-cache-key="chatgpt-deck-comparison"` (DeckComparison.cshtml line 171) — 1 hit.
- `data-cache-key="chatgpt-cedh-meta-gap"` (CedhMetaGap.cshtml line 40) — 1 hit.
- `chatgpt-packets-form` CSS class (DeckAnalysis.cshtml line 72) — 1 hit.
- `chatgpt-step-eyebrow` CSS class — 3 hits (one per multi-AI view).
- `data-chatgpt-*` attrs — multiple, unchanged from baseline.
- `parseChatGptDownloadFilename` TypeScript const (DeckFlow.Web/wwwroot/ts/) — present, unchanged.

Phase 15 invariants:
- `AiPlatform.Normalize` used by Plans 01-03 — present in the 3 multi-AI views.
- `AiPlatform.All` order — unchanged in AiPlatform.cs.

Phase 999.1 D-03 carve-outs (must REMAIN ChatGPT-named):
- `JudgeQuestions.cshtml` — exactly 6 ChatGPT hits (CONTEXT.md inventory).
- `Home.cshtml` line 62 JudgeQuestions card description — 1 hit.
- `Help/ask-a-judge.md` — 2 hits.

Phase 999.1 D-04 boundary (PromptVariants raw-string literals):
- `grep -nE 'ChatGPT|ChatGpt' DeckFlow.Web/Services/PromptBuilders/` — hits in ChatGPT-variant files (e.g., `ChatGptAnalysisPromptVariant.cs`) are expected and per-platform-correct. Hits in NON-ChatGPT-variant files (`ClaudeAnalysisPromptVariant.cs`, `GeminiAnalysisPromptVariant.cs`, etc.) → Phase 15 follow-up bug; document in SUMMARY, DO NOT FIX in this phase.

User MEMORY.md feedback:
- `feedback_user_starts_server.md` — never auto-launch DeckFlow web; ask user to start.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Brand audit of README.md</name>
  <files>README.md</files>
  <read_first>
    - .planning/phases/999.1-ai-agnostic-prose-adaptation-razor-views/999.1-CONTEXT.md (`<deferred>` README brand update note; D-03 JudgeQuestions carve-out; D-05 universal noun)
    - README.md (full file — scan all ~35 ChatGPT mentions and classify each)
    - CLAUDE.md ("README updated when behavior changes" + commit hygiene — no Co-Authored-By trailer)
  </read_first>
  <action>
    Read README.md end-to-end. For EACH ChatGPT mention, classify per these rules:

    GENERALIZE (substitute universal `your AI` / `the AI` / `an AI prompt` / `AI-ready` etc. per D-05 Claude's Discretion) when the sentence describes a generic AI workflow that applies equally to ChatGPT, Claude, and Gemini. Examples:
    - Line 3 product blurb "...ChatGPT prompt-building workflows for single-deck analysis, cEDH meta-gap analysis, and head-to-head deck comparison..." → "...AI prompt-building workflows for...". (Note: line 3 also lists "Ask-a-Judge handoff flow" — that's distinct; treat the prompt-building list as generic-AI.)
    - Line 8 repo description: same generalization.
    - Line 132 component description (DeckFlow.Web provides...): generalize.
    - Line 135 cEDH Meta Gap page narrative: generalize.
    - Line 230 / 235 / 236 / 245 (DeckAnalysis page table rows + suggested-title note): generalize.
    - Line 250 / 260 / 263 (paste-back instructions): generalize.
    - Line 270 (prompts return JSON instruction): "All AI prompts generated by this app..." (drop "ChatGPT").
    - Line 280 / 302 / 313 / 314 / 315 / 322 (analysis prompt narrative): generalize.
    - Line 330 / 341 / 347 / 355 / 357 (DeckComparison narrative): generalize.
    - Line 367 (paste-back note): generalize.
    - Line 411 (cEDH Meta Gap instructed-to): generalize.
    - Line 439 (paste-back note): generalize.
    - Line 600 (Scryfall integration note): "The AI workflow uses the same batch endpoint..."
    - Line 637 (DeckComparisonService narrative): generalize.
    - Lines 195 / 280 / 367 / 439 "future ChatGPT context" — generalize to "future AI context".

    KEEP (D-03 JudgeQuestions carve-out — ChatGPT-the-product-specific):
    - Line 493 Ask-a-Judge section "...A clearly labeled **secondary** ChatGPT prompt generator is provided below..." through the same paragraph — this describes the JudgeQuestions page intent and is carved out per D-03. Preserve verbatim.

    KEEP (legitimate plumbing references):
    - "ChatGPT artifact folders are still filesystem-backed" (line 195 — wait, also generalize this; CLAUDE.md says behavior change → update). Actually: re-read carefully; this is about session zips. Per Plan 12 already renamed terminology, generalize: "AI artifact folders are still filesystem-backed..." → "if saved AI sessions need to survive deploys/restarts." Claude's Discretion — but the file path `MTG_DATA_DIR` is unchanged, the storage behavior is unchanged, so generalize the prose.

    The exact line-by-line classification is the executor's call (Claude's Discretion). What matters:
    1. After editing, README.md ChatGPT mention count drops substantially (target: from ~35 to ≤5, with the remaining ~3-5 being legitimate D-03 Ask-a-Judge references).
    2. ZERO Razor / TS / CSS-coupled identifiers in README are edited (those are still Phase 13 D-08 deferred; if README contains `data-cache-key="chatgpt-packets"` examples or `chatgpt-packets-form` CSS class references, KEEP them — they document current code).
    3. Historical decision records / changelogs / commit log excerpts (if any) that quote prior names KEEP their literal "ChatGPT" — those are historical fact, not behavior description.

    Touch only the prose. Preserve LF line endings, table structure, markdown anchors, fenced code blocks. Do NOT reformat headings, table cells, or list bullets.
  </action>
  <verify>
    <automated>bash -c "before=$(git show HEAD:README.md | grep -c 'ChatGPT'); after=$(grep -c 'ChatGPT' README.md); echo \"before=$before after=$after\"; test $after -lt $before"</automated>
    <automated>grep -c 'secondary \*\*ChatGPT' README.md</automated>
    <automated>grep -cE 'your AI|the AI|an AI|AI prompt|AI workflow|AI-ready|AI-authored|AI session|AI context' README.md</automated>
    <automated>bash -c "test $(grep -c 'ChatGPT' README.md) -le 6 || { echo 'FAIL: too many ChatGPT mentions remain in README'; exit 1; }"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'ChatGPT' README.md` is strictly LESS than the pre-phase baseline (which was ~35).
    - `grep -c 'ChatGPT' README.md` is ≤ `6` (allowing the Ask-a-Judge carve-out paragraph + any historical references the executor deemed legitimate).
    - `grep -c 'secondary \*\*ChatGPT' README.md` returns `1` (the D-03 Ask-a-Judge carve-out paragraph at line 493 area preserved with its bolded "secondary" qualifier).
    - `grep -cE 'your AI|the AI|an AI|AI prompt|AI workflow|AI-ready|AI-authored|AI session|AI context' README.md` returns at least `8` (multiple universal-noun substitutions land).
  </acceptance_criteria>
  <done>README.md describes the multi-AI workflow generically; D-03 carve-out preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Full-phase invariants grep gate + dotnet build clean</name>
  <files>(no source modifications — gate-only task)</files>
  <read_first>
    - .planning/phases/999.1-ai-agnostic-prose-adaptation-razor-views/999.1-CONTEXT.md (`<code_context>` Phase 10 invariants list, `<domain>` "What this phase does NOT do" list, D-03 carve-out list, D-04 PromptVariants boundary)
    - All 6 plan SUMMARYs in this phase (confirm each prior plan's verify gates passed before invariant gate runs)
  </read_first>
  <action>
    Run the full-phase invariant grep gate. Each command below must produce the expected output; any deviation is a blocker that the executor must surface in the SUMMARY.

    1. **Phase 10 invariants (must all remain present):**
       - `grep -n 'request.TargetAiPlatform' DeckFlow.Web/Services/*.cs` — count unchanged from baseline.
       - `grep -rn 'name="TargetAiPlatform"' DeckFlow.Web/Views/` — count unchanged from baseline.
       - `grep -n 'Key: "ChatGPT"' DeckFlow.Web/Models/AiPlatform.cs` — returns 1 hit.
       - `grep -n 'Key: "Claude"' DeckFlow.Web/Models/AiPlatform.cs` — returns 1 hit.
       - `grep -n 'Key: "Gemini"' DeckFlow.Web/Models/AiPlatform.cs` — returns 1 hit.
       - `grep -n '"chatgpt"' DeckFlow.Web/Services/PacketArtifactStore.cs` — returns at least 1 hit (Phase 10 fallback line 536-542).
       - `grep -n '<result>' DeckFlow.Web/Services/JsonTextFormatterService.cs` — returns at least 1 hit (ResultWrapInstruction).

    2. **Phase 13 D-08 invariants (TS/CSS-coupled, still deferred — must remain present):**
       - `grep -c 'data-cache-key="chatgpt-packets"' DeckFlow.Web/Views/Deck/DeckAnalysis.cshtml` returns `1`.
       - `grep -c 'data-cache-key="chatgpt-deck-comparison"' DeckFlow.Web/Views/Deck/DeckComparison.cshtml` returns `1`.
       - `grep -c 'data-cache-key="chatgpt-cedh-meta-gap"' DeckFlow.Web/Views/Deck/CedhMetaGap.cshtml` returns `1`.
       - `grep -c 'chatgpt-packets-form' DeckFlow.Web/Views/Deck/DeckAnalysis.cshtml` returns `1`.
       - `grep -c 'chatgpt-step-eyebrow' DeckFlow.Web/Views/Deck/DeckAnalysis.cshtml` returns `1`.
       - `grep -c 'chatgpt-step-eyebrow' DeckFlow.Web/Views/Deck/DeckComparison.cshtml` returns `1`.
       - `grep -c 'chatgpt-step-eyebrow' DeckFlow.Web/Views/Deck/CedhMetaGap.cshtml` returns `1`.
       - `grep -rn 'data-chatgpt-' DeckFlow.Web/Views/Deck/` — line count unchanged from baseline.
       - `grep -rn 'parseChatGptDownloadFilename' DeckFlow.Web/wwwroot/ts/` — returns at least 1 hit (TS const unchanged).

    3. **D-03 carve-outs (must REMAIN ChatGPT-named):**
       - `grep -c 'ChatGPT' DeckFlow.Web/Views/Deck/JudgeQuestions.cshtml` returns `6`.
       - `grep -c 'ChatGPT prompt generator as backup' DeckFlow.Web/Views/Deck/Home.cshtml` returns `1` (line 62 carve-out).
       - `grep -c 'ChatGPT' DeckFlow.Web/Help/ask-a-judge.md` returns `2`.

    4. **D-04 PromptVariants boundary (drift surveillance, NOT fixed in this phase):**
       - `grep -rnE 'ChatGPT|ChatGpt' DeckFlow.Web/Services/PromptBuilders/` — capture full output. For each hit, classify:
         - File path contains `ChatGpt` (e.g., `ChatGptAnalysisPromptVariant.cs`) → expected, per-platform-correct.
         - File path is `Claude*PromptVariant.cs` or `Gemini*PromptVariant.cs` AND match is inside a string literal or comment that should say something else → DRIFT; record line in SUMMARY as Phase 15 follow-up bug. DO NOT FIX in this phase.

    5. **Phase 999.1 substantive scope (must all PASS):**
       - `grep -c 'ChatGPT' DeckFlow.Web/Views/Deck/DeckAnalysis.cshtml` returns `1` (line-30 lede).
       - `grep -c 'ChatGPT' DeckFlow.Web/Views/Deck/DeckComparison.cshtml` returns `1` (line-145 lede).
       - `grep -c 'ChatGPT' DeckFlow.Web/Views/Deck/CedhMetaGap.cshtml` returns `1` (line-22 lede).
       - `grep -c 'ChatGPT' DeckFlow.Web/Views/Deck/Home.cshtml` returns `2` (line-14 hub-hero + line-62 D-03 carve-out).
       - `grep -c 'ChatGPT' DeckFlow.Web/Services/ResponseParsers.cs` returns `0`.
       - `grep -c 'ChatGPT' DeckFlow.Web/Services/MetaGapService.cs` returns `1` (line 266 Phase 10 fallback).
       - `grep -c 'ChatGPT' DeckFlow.Web/Services/DeckComparisonService.cs` returns `1` (line 229 Phase 10 fallback).
       - `grep -c 'ChatGPT' DeckFlow.Web/Services/DeckAnalysisPacketService.cs` returns `3` (lines 534 + 936 doc comments + line 1309 Phase 10 fallback).
       - `grep -c 'ChatGPT' DeckFlow.Web/Help/deck-analysis.md` returns `0`.
       - `grep -c 'ChatGPT' DeckFlow.Web/Help/deck-comparison.md` returns `0`.
       - `grep -c 'ChatGPT' DeckFlow.Web/Help/cedh-meta-gap.md` returns `0`.

    6. **Build gate:**
       - `dotnet build DeckFlow.sln --configuration Release` exits 0 with zero new warnings vs baseline.

    Record any drift / failure in `999.1-07-SUMMARY.md` under `## Drift Findings`. Do NOT fix Phase 15 PromptVariants drift in this plan (per D-04).
  </action>
  <verify>
    <automated>bash -c "set -e; grep -c 'Key: \"ChatGPT\"' DeckFlow.Web/Models/AiPlatform.cs | grep -q '^1$'; grep -c 'Key: \"Claude\"' DeckFlow.Web/Models/AiPlatform.cs | grep -q '^1$'; grep -c 'Key: \"Gemini\"' DeckFlow.Web/Models/AiPlatform.cs | grep -q '^1$'; echo 'PASS: AiPlatform.Key string literals intact'"</automated>
    <automated>bash -c "set -e; for f in DeckFlow.Web/Views/Deck/DeckAnalysis.cshtml DeckFlow.Web/Views/Deck/DeckComparison.cshtml DeckFlow.Web/Views/Deck/CedhMetaGap.cshtml; do test $(grep -c 'chatgpt-step-eyebrow' \"$f\") -eq 1 || { echo \"FAIL: chatgpt-step-eyebrow missing in $f\"; exit 1; }; done; echo 'PASS: Phase 13 D-08 CSS classes intact'"</automated>
    <automated>bash -c "set -e; test $(grep -c 'data-cache-key=\"chatgpt-packets\"' DeckFlow.Web/Views/Deck/DeckAnalysis.cshtml) -eq 1; test $(grep -c 'data-cache-key=\"chatgpt-deck-comparison\"' DeckFlow.Web/Views/Deck/DeckComparison.cshtml) -eq 1; test $(grep -c 'data-cache-key=\"chatgpt-cedh-meta-gap\"' DeckFlow.Web/Views/Deck/CedhMetaGap.cshtml) -eq 1; echo 'PASS: Phase 13 D-08 data-cache-key attrs intact'"</automated>
    <automated>bash -c "set -e; test $(grep -c 'ChatGPT' DeckFlow.Web/Views/Deck/JudgeQuestions.cshtml) -eq 6; test $(grep -c 'ChatGPT prompt generator as backup' DeckFlow.Web/Views/Deck/Home.cshtml) -eq 1; test $(grep -c 'ChatGPT' DeckFlow.Web/Help/ask-a-judge.md) -eq 2; echo 'PASS: D-03 carve-outs intact'"</automated>
    <automated>bash -c "set -e; test $(grep -c 'ChatGPT' DeckFlow.Web/Views/Deck/DeckAnalysis.cshtml) -eq 1; test $(grep -c 'ChatGPT' DeckFlow.Web/Views/Deck/DeckComparison.cshtml) -eq 1; test $(grep -c 'ChatGPT' DeckFlow.Web/Views/Deck/CedhMetaGap.cshtml) -eq 1; test $(grep -c 'ChatGPT' DeckFlow.Web/Views/Deck/Home.cshtml) -eq 2; echo 'PASS: Razor view ChatGPT counts match expected'"</automated>
    <automated>bash -c "set -e; test $(grep -c 'ChatGPT' DeckFlow.Web/Services/ResponseParsers.cs) -eq 0; test $(grep -c 'ChatGPT' DeckFlow.Web/Services/MetaGapService.cs) -eq 1; test $(grep -c 'ChatGPT' DeckFlow.Web/Services/DeckComparisonService.cs) -eq 1; test $(grep -c 'ChatGPT' DeckFlow.Web/Services/DeckAnalysisPacketService.cs) -eq 3; echo 'PASS: Service ChatGPT counts match expected'"</automated>
    <automated>bash -c "set -e; test $(grep -c 'ChatGPT' DeckFlow.Web/Help/deck-analysis.md) -eq 0; test $(grep -c 'ChatGPT' DeckFlow.Web/Help/deck-comparison.md) -eq 0; test $(grep -c 'ChatGPT' DeckFlow.Web/Help/cedh-meta-gap.md) -eq 0; echo 'PASS: Help md ChatGPT counts at 0'"</automated>
    <automated>dotnet build DeckFlow.sln -c Release --nologo</automated>
  </verify>
  <acceptance_criteria>
    - All 7 invariant gate bash blocks above echo `PASS: ...` and exit 0.
    - `dotnet build DeckFlow.sln -c Release` exits 0 with no new warnings vs baseline.
    - Any PromptVariants drift findings are recorded in the SUMMARY's `## Drift Findings` section (NOT fixed in this phase).
  </acceptance_criteria>
  <done>Phase 999.1 invariants intact, build clean, drift surveyed.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human UAT — render the three multi-AI pages across ChatGPT / Claude / Gemini selections</name>
  <files>.planning/phases/999.1-ai-agnostic-prose-adaptation-razor-views/999.1-HUMAN-UAT.md</files>
  <read_first>
    - .planning/phases/999.1-ai-agnostic-prose-adaptation-razor-views/999.1-CONTEXT.md (D-01 hybrid pattern + D-03 carve-out + D-05 universal noun — drives the UAT expected text)
    - CLAUDE.md ("README updated when behavior changes"; commit hygiene)
    - User MEMORY.md feedback (`feedback_user_starts_server.md` — never auto-launch DeckFlow web; ask user to start)
  </read_first>
  <what-built>
    Plans 01–06 rewrote the visible ChatGPT prose across three Razor views (DeckAnalysis, DeckComparison, CedhMetaGap), the Home hub-card description for DeckComparison, exception messages in three services, three Help markdown files, and the DeckAnalysisPacketService log prefix. Above the `_AiSelector` in each multi-AI view, prose uses `your AI`. Below the selector, prose injects `@aiPlatform.DisplayName`. Plan 07 Task 1 audited README and Task 2 ran the cross-file invariants grep gate.
  </what-built>
  <how-to-verify>
    Write `.planning/phases/999.1-ai-agnostic-prose-adaptation-razor-views/999.1-HUMAN-UAT.md` first with the checklist below, then prompt the user to perform the UAT.

    **The executor MUST NOT auto-start the dev server** (per user MEMORY.md `feedback_user_starts_server.md`). Ask the user to launch DeckFlow web themselves.

    Checklist body for `999.1-HUMAN-UAT.md`:

    ```
    # 999.1 Manual UAT

    ## Pre-flight

    - [ ] User starts DeckFlow web locally (e.g., `dotnet run --project DeckFlow.Web` from a separate terminal). DO NOT auto-launch.
    - [ ] Browser opens to `http://localhost:5173/` (or whichever port the user's launchSettings specifies).

    ## T1 — Deck Analysis page (`/deck-analysis`)

    For each AiPlatform (ChatGPT, Claude, Gemini if DECKFLOW_GEMINI_ENABLED=true), perform a full round-trip:
    - [ ] Load a deck in Step 1.
    - [ ] Step 2: pick ChatGPT. Verify the prose around "Suggested ChatGPT conversation title" / "Paste this into ChatGPT for the main deck analysis." / "ChatGPT should return a deck_profile JSON object" reads naturally — these are below-selector and should say "ChatGPT" (because ChatGPT is the selected platform).
    - [ ] Re-pick Claude in Step 2 and re-render the page. Verify the SAME below-selector prose now reads "Suggested Claude conversation title", "Paste this into Claude...", "Claude should return a deck_profile JSON object". Above-selector prose ("Ask your AI to return the deck_profile JSON inside a fenced ```json code block.") stays the same regardless of selection.
    - [ ] Re-pick Gemini (if enabled). Verify same dynamic injection. Below-selector prose says "Gemini".
    - [ ] Form busy overlay (visible briefly when generating the packet): title says "Building Deck Analysis Packet" (not "Building ChatGPT Packets").
    - [ ] Paste an intentionally-bad JSON into Step 3 to trigger the error banner. Banner text should be "The submitted AI response did not contain a valid deck_profile payload." or "Paste the deck_profile JSON returned from your AI into Step 3." — NOT "ChatGPT".

    ## T4 — Deck Comparison page (`/deck-comparison`)

    - [ ] Pick ChatGPT → page lede ("Generate a prompt comparing two decks. Paste into ChatGPT, Claude, or Gemini.") stays enumerated above selector. Below selector ("Paste this into ChatGPT to get the side-by-side comparison...") shows ChatGPT.
    - [ ] Switch to Claude → below-selector copy changes to "Paste this into Claude...".
    - [ ] Switch to Gemini (if enabled) → same dynamic injection.
    - [ ] Paste bad JSON to trigger the error banner. Banner says "...AI response..." not "...ChatGPT response...".

    ## T5 — cEDH Meta-Gap page (`/cedh-meta-gap`)

    - [ ] Pick each AI in turn, verify above-selector copy uses "your AI" and below-selector copy injects the selected DisplayName.
    - [ ] Trigger error path (bad JSON), verify banner text.

    ## Hub + Help pages

    - [ ] `/` Home page: DeckComparison hub card description reads "AI-authored breakdown" (not "ChatGPT-authored breakdown"). JudgeQuestions card still says "ChatGPT prompt generator as backup" (D-03 carve-out preserved).
    - [ ] `/help/deck-analysis`, `/help/deck-comparison`, `/help/cedh-meta-gap` — scan for "ChatGPT" — should not appear. `/help/ask-a-judge` — still mentions ChatGPT (D-03 carve-out).

    ## JudgeQuestions carve-out (regression check)

    - [ ] `/judge-questions` page renders unchanged: H2 "ChatGPT prompt generator", warning "ChatGPT can be confidently wrong about MTG rules", etc. all intact.

    ## Sign-off

    - [ ] All steps above passed → write "approved" in the chat to resume the executor.
    - [ ] Any failure → describe what's wrong (which page, which AI, which prose); executor will record in 07-SUMMARY.md.
    ```
  </how-to-verify>
  <action>
    Execute the manual verification described in `<how-to-verify>`. This is a `checkpoint:human-verify` task — the executor's job is to write `999.1-HUMAN-UAT.md` with the checklist body above, then ask the user to start the dev server and walk through the checklist. Do NOT auto-launch the dev server (CLAUDE.md feedback memory). Wait for the user's "approved" signal or recorded issues before exiting the checkpoint.
  </action>
  <verify>
    <human-check>User confirms all UAT steps passed in `999.1-HUMAN-UAT.md` (or recorded specific failures for follow-up).</human-check>
  </verify>
  <done>User typed "approved"; `999.1-HUMAN-UAT.md` exists with the checklist populated and a sign-off line.</done>
  <acceptance_criteria>
    - `.planning/phases/999.1-ai-agnostic-prose-adaptation-razor-views/999.1-HUMAN-UAT.md` exists with the sections: Pre-flight, T1, T4, T5, Hub + Help pages, JudgeQuestions carve-out, Sign-off.
    - User signed off "approved" OR explicit issues are recorded in the file.
  </acceptance_criteria>
  <resume-signal>Type "approved" if all UAT steps pass. Type "issues: ..." with a brief description if any prose is wrong; the executor will record the issues in `999.1-07-SUMMARY.md` for follow-up. The executor will NOT auto-start the dev server.</resume-signal>
</task>

</tasks>

<verification>
- `999.1-HUMAN-UAT.md` exists in the phase directory with the checklist body above.
- User signed off with "approved" or recorded UAT issues for follow-up.
- All invariant grep gates in Task 2 passed.
- `dotnet build DeckFlow.sln -c Release` exits 0.
</verification>

<success_criteria>
- README brand audit complete.
- All Phase 10 / Phase 13 D-08 / Phase 15 invariants preserved.
- All D-03 carve-outs preserved.
- Build clean.
- Human UAT signed off.
</success_criteria>

<output>
Create `.planning/phases/999.1-ai-agnostic-prose-adaptation-razor-views/999.1-07-SUMMARY.md` when done; include the grep-gate output, build result, and UAT sign-off line. Capture any PromptVariants drift findings as a Phase 15 follow-up backlog note.
</output>
