---
phase: 51-verify-v1-7-on-main-non-prod-uat
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/51-verify-v1-7-on-main-non-prod-uat/51-UAT-RESULTS.md
autonomous: false
requirements: [HARD-01]
must_haves:
  truths:
    - "Operator confirms /Admin/Harvest renders the page skeleton immediately with no initial scroll-jump, the commander grid lazy-loads via AJAX, and numbered pagination swaps only the grid section"
    - "The P44 smoke result (PASS / FAIL / WAIVED-with-reason) is recorded in 51-UAT-RESULTS.md with the date and observed behavior"
  artifacts:
    - path: ".planning/phases/51-verify-v1-7-on-main-non-prod-uat/51-UAT-RESULTS.md"
      provides: "Recorded P44 /Admin/Harvest lazy-grid smoke result"
      contains: "P44"
  key_links:
    - from: "51-UAT-RESULTS.md"
      to: "44-VERIFICATION.md human_verification"
      via: "smoke recorded against the deferred P44 browser-UX gate"
      pattern: "P44"
---

<objective>
Close the deferred Phase 44 (Admin Grid Lazy Paging) non-prod operator-UAT smoke by running the `/Admin/Harvest` lazy-grid browser check on the v1.7-on-main build and recording the result. This is the Web-side slice of HARD-01.

Purpose: HARD-01 requires the deferred P44 smoke to pass and be recorded; the automated controller tests already pass (8/8) but the browser UX (innerHTML swap, no-first-load-scroll-jump, error/retry) was never operator-verified.
Output: A recorded P44 result line in `51-UAT-RESULTS.md`. No product code changes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/44-admin-grid-lazy-paging/44-VERIFICATION.md
@.planning/phases/44-admin-grid-lazy-paging/44-03-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Operator smoke — /Admin/Harvest lazy-grid (P44)</name>
  <read_first>
    - .planning/phases/44-admin-grid-lazy-paging/44-VERIFICATION.md (the exact human_verification steps + expected results)
    - .planning/phases/44-admin-grid-lazy-paging/44-03-SUMMARY.md (what the client wiring does: admin-harvest.ts auto-load/swap/pagination)
  </read_first>
  <what-built>Phase 44 made /Admin/Harvest skip the synchronous count+aggregate on first render and lazy-load the commander grid via an AJAX PartialView at GET /Admin/Harvest/commanders, with numbered pagination that swaps only the grid section. Automated controller tests pass; the browser UX was deferred.</what-built>
  <how-to-verify>
    1. From the repo root on `main`, start the web app with the auto-browser suppressed: set DECKFLOW_DISABLE_AUTO_BROWSER=true, run DeckFlow.Web, and note the localhost URL from launchSettings.json.
    2. Sign in through admin Basic Auth (FEEDBACK_ADMIN_USER / FEEDBACK_ADMIN_PASSWORD) and navigate to /Admin/Harvest.
    3. Confirm the page skeleton (stats, recent runs, schedule) renders immediately; "Loading commanders…" shows briefly, then the grid populates via AJAX with NO initial page-scroll jump to the grid section.
    4. Click a numbered page link (or Prev/Next): confirm only the #commanders-grid-container contents swap (no full-page reload), the "Page X of Y" line updates, the current page renders as a non-link <strong aria-current="page">, and the grid scrolls into view after the click.
    5. Optional negative checks: throttle/offline in DevTools and click a page link — confirm the inline "Could not load commanders. Retry" appears and Retry works; issue a cross-origin fetch to /Admin/Harvest/commanders?page=1 with a foreign Origin header and confirm 403.
  </how-to-verify>
  <action>Run the smoke above against the v1.7-on-main build. Record the outcome (PASS / FAIL / WAIVED-with-reason), the date, and a one-line note of observed behavior under a "## P44 — /Admin/Harvest lazy grid" heading in 51-UAT-RESULTS.md. If any step fails, capture it as a follow-up finding (do NOT fix product code in this phase) and mark the result FAIL with the defect description.</action>
  <acceptance_criteria>
    - Operator observed: initial render has no scroll jump; grid lazy-loads via AJAX; pagination swaps only the grid section; current page is <strong aria-current="page">.
    - 51-UAT-RESULTS.md contains a "## P44" section with a result line (PASS/FAIL/WAIVED), date 2026-06-17 or later, and observed behavior.
    - If FAIL: a follow-up finding is captured; no DeckFlow.Web product files are modified by this plan.
  </acceptance_criteria>
  <resume-signal>Type "approved" with the P44 result (or "fail: <defect>"), then the result is written to 51-UAT-RESULTS.md.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → /Admin/* | Admin Basic Auth gate; same-origin guard on the AJAX partial |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-51-01-01 | Information disclosure | Admin BasicAuth creds in shell env | accept | Operator-run smoke uses FEEDBACK_ADMIN_USER/PASSWORD from local env; never echoed into 51-UAT-RESULTS.md or commits (results record behavior only, not credentials) |
| T-51-01-02 | Elevation of privilege | Cross-origin POST to /Admin/Harvest/commanders | mitigate | Negative check confirms SameOriginRequestValidator returns 403 (already shipped in P44; this smoke re-confirms) |
| T-51-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan — verification only |
</threat_model>

<verification>
- 51-UAT-RESULTS.md exists and contains a "## P44" result section.
- No files under DeckFlow.Web/ are modified by this plan (verification-only).
</verification>

<success_criteria>
The deferred P44 /Admin/Harvest lazy-grid smoke is run on the v1.7-on-main build and its result (PASS/FAIL/WAIVED-with-reason) is recorded in 51-UAT-RESULTS.md.
</success_criteria>

<output>
Create `.planning/phases/51-verify-v1-7-on-main-non-prod-uat/51-01-SUMMARY.md` when done.
</output>
