# Phase 110.1 Plan 02 Summary

## What changed

- Added `CutLabFindingKind.ComboProtected`, `ComboBadgeState`, nullable `CutLabFindingEvidence.BadgeState`, and `ComputeComboProtected(...)` in `CutLabStructuralFindings.cs`.
- Extended `CutLabStructuralFindings.Compute(...)` with trailing nullable `completeCombos` and coalesced it internally to `Array.Empty<SpellbookCombo>()`.
- Implemented complete-combo evidence (`CompletePiece`), near-combo evidence (`NeedsPartner`), near-combo variant grouping by order-insensitive `CardsInDeck`, and the round-1 advisory copy in the ComboProtected lead.
- Documented the weak-floor precedence inline: weak-floor findings stay unchanged, while ComboProtected carries the combo explanation for overlapping cards.
- Added `ComboProtected` to `CutLabCutRoundEngine.ExcludedFindingKindsFromTally`.
- Threaded per-card combo badge state through the initial render path:
  `CutLabPageService.ProcessAsync -> CutLabProcessResult.ComboBadgeByCardName -> CutLabViewModel.ComboBadgeByCardName`.
- Added ComboProtected merging in `CutLabFindingPresenter.BuildFindingGroups`.
- Added `ComboBadgeByCardName` plus required combo-context DTO payload to `CutLabUiPatchDto`, populated it on `CutLabUiPatchBuilder.BuildAsync`, and kept it empty on `BuildAdjustPatch`.
- Added TDD coverage for structural findings, presenter/view-model grouping/threading, and patch DTO round-trip. New tests use real `CardComboMembership` with populated `SpellbookCombo` / `SpellbookAlmostCombo`, not the Wave 1 compatibility overload.

## Verification

- `dotnet build DeckFlow.sln`
  - Result: PASS
  - Errors: 0
  - Warnings: 9 existing warnings in `DeckFlow.Core.Tests/Manabase/ManabaseBaselineWeightingTests.cs`
  - New warnings from this plan: 0
- `dotnet test DeckFlow.Web.Tests --filter "FullyQualifiedName~CutLabStructuralFindingsTests|FullyQualifiedName~CutLabUiPatchBuilderTests|FullyQualifiedName~CutLabViewModelWordingTests"`
  - Result: PASS
  - Failed: 0
  - Passed: 59
  - Skipped: 0

## TDD evidence

- Task 1 red pass failed on the missing `completeCombos` parameter, missing `ComboProtected`, and missing badge-state members before production code was added.
- Task 2 red pass failed on the missing presenter merge branch and missing `ComboBadgeByCardName` result/view-model contracts before production code was added.
- Task 3 red pass failed on the missing patch DTO combo badge map before production code was added.

## CutLabCutRoundEngine diff confirmation

- `git diff DeckFlow.Web/Services/CutLab/CutLabCutRoundEngine.cs` shows only:
  - the `ExcludedFindingKindsFromTally` addition of `CutLabFindingKind.ComboProtected`
  - the `completeCombos:` argument at the `CutLabStructuralFindings.Compute(...)` call site
- No new branch, sort key, proposal-ordering rule, or other ranking logic was added.

## Deviations

- `dotnet build DeckFlow.sln` was not warning-clean before this plan. The build still succeeds, and this work introduced no new warnings.
