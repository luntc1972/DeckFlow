---
phase: 999.3-packet-download-session-cache
plan: 04
type: execute
wave: 4
depends_on:
  - 999.3-01
  - 999.3-02
  - 999.3-03
files_modified:
  - README.md
  - .planning/STATE.md
  - .planning/ROADMAP.md
autonomous: false
requirements:
  - D-14
tags:
  - documentation
  - uat
  - verification

must_haves:
  truths:
    - "Manual UAT executed against a large Commander deck previously used in Phase 999.2 UAT (obs 237 + 240 on 2026-05-20 — the same deck that exposed the 2+ minute double-replay). Preview → download click measured at <2s on cache hit vs >2 minutes on cache miss / pre-phase baseline."
    - "Cache hit-ratio observability confirmed: at least one `Packet cache hit` log line appears in `/data/logs/web-*.log` (local: `logs/web-*.log` under content root) after a preview → download flow with identical inputs."
    - "Cache miss fall-through observability confirmed: a cold-cache download (process restarted between preview and download, OR a 5-minute wait between preview and download) emits a `Packet cache miss` log line followed by a successful BuildAsync execution and a valid zip response."
    - "Grep gate on all 3 services + cache wrapper + controller: exact-count assertions pass (see verification block)."
    - "`dotnet.exe build DeckFlow.sln -c Release` exits 0 with current warning count ≤ `_baseline-warnings.txt` (B8 + W5: MSBuild summary line, not per-line regex)."
    - "README.md updated with a brief note that packet download endpoints now reuse the preview's BuildAsync result via an in-memory session cache (5-min TTL, 10MB cap)."
    - "STATE.md updated to reflect Phase 999.3 closure: `last_activity` mentions the closure; counter fields (`completed_phases`, `completed_plans`) are INCREMENTED by reading current values and adding the phase deltas (NOT by writing absolute line-number values; W6 fix); `Current Position` block points to the next phase."
  artifacts:
    - path: ".planning/phases/999.3-packet-download-session-cache/999.3-UAT.md"
      provides: "UAT execution log with timings, log-line excerpts, and pass/fail attestation"
      min_lines: 30
    - path: "README.md"
      provides: "User-facing note that packet downloads are now cached"
      contains: "session cache"
    - path: ".planning/STATE.md"
      provides: "Phase 999.3 closure markers"
      contains: "999.3"
  key_links:
    - from: ".planning/STATE.md"
      to: ".planning/ROADMAP.md (Phase 999.3 entry)"
      via: "status flip to Complete + completion date"
      pattern: "999.3.*Complete"
---

<objective>
Close Phase 999.3 via manual UAT, grep gate, build gate, README update, and STATE update. This plan has ONE human-verify checkpoint (the UAT) and three autonomous tasks bracketing it.

Purpose: D-14 mandates "manual UAT + dotnet build clean + grep gate" as the test gate (xUnit is unreliable in WSL per CLAUDE.md, and CONTEXT D-14 default position is "skip xUnit, manual UAT only, same as 999.1 and 999.2"). This plan executes that gate, documents the result, and updates the project artifacts that reflect phase completion.

Output: One new file (`999.3-UAT.md`), three modified files (`README.md`, `STATE.md`, `ROADMAP.md`), one commit closing the phase.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/999.3-packet-download-session-cache/999.3-CONTEXT.md
@.planning/phases/999.3-packet-download-session-cache/999.3-01-SUMMARY.md
@.planning/phases/999.3-packet-download-session-cache/999.3-02-SUMMARY.md
@.planning/phases/999.3-packet-download-session-cache/999.3-03-SUMMARY.md
@CLAUDE.md

<interfaces>
<!-- UAT environment expectations and the grep-gate command set. -->

UAT environment:
  - User starts the dev server manually per global memory note "User starts dev server — never auto-launch DeckFlow web; ask user to start" — executor MUST NOT run `dotnet run` or `scripts/run-web.{ps1,sh}` to auto-launch the app.
  - Browser at `http://localhost:5173` (per launchSettings.json).
  - Log location: `<ContentRoot>/logs/web-{date}.log` (Serilog file sink, daily rolling per Program.cs:34-47).
  - UAT deck: the same large Commander deck the user used in Phase 999.2 UAT on 2026-05-20 (observations 237 at 3:24p and 240 at 3:31p exposed the 2+ minute double-replay). Executor asks the user for the deck URL or deck source at UAT time.

Grep-gate baseline (D-14):
  - `grep -c "_packetCache.Set" DeckFlow.Web/Services/DeckAnalysisPacketService.cs` → 1
  - `grep -c "_packetCache.Set" DeckFlow.Web/Services/DeckComparisonService.cs` → 1
  - `grep -c "_packetCache.Set" DeckFlow.Web/Services/MetaGapService.cs` → 1
  - `grep -c "_packetCache.TryGet" DeckFlow.Web/Controllers/DeckController.cs` → 3
  - `grep -c "public static string ComputeCacheKey" DeckFlow.Web/Services/DeckAnalysisPacketService.cs` → 1
  - `grep -c "public static string ComputeCacheKey" DeckFlow.Web/Services/DeckComparisonService.cs` → 1
  - `grep -c "public static string ComputeCacheKey" DeckFlow.Web/Services/MetaGapService.cs` → 1
  - `test -f DeckFlow.Web/Services/PacketSessionCache.cs` → exit 0
  - `grep -c "AddSingleton<PacketSessionCache>" DeckFlow.Web/Program.cs` → 1
  - `grep -c "sp.GetRequiredService<PacketSessionCache>" DeckFlow.Web/Program.cs` → 3

Build gate (D-14, B8 + W5 — MSBuild summary, NOT per-line regex):
  - `dotnet.exe build DeckFlow.sln -c Release > /tmp/999.3-04-build.log 2>&1`
  - Confirm `grep -q "Build succeeded" /tmp/999.3-04-build.log`.
  - `CURRENT=$(grep -oP '\d+(?= Warning\(s\))' /tmp/999.3-04-build.log | tail -1)`
  - `BASELINE=$(cat .planning/phases/999.3-packet-download-session-cache/_baseline-warnings.txt)`
  - Assert `[ "$CURRENT" -le "$BASELINE" ]`.

STATE.md update strategy (W6 fix — reference FIELDS not line numbers; counters are incremented from current values, NOT written as absolute numbers):
  - `last_updated` field: bump to current ISO timestamp.
  - `last_activity` field: set to "2026-05-21 -- Phase 999.3 closed (packet download session cache shipped)" (or current date).
  - `completed_phases` counter: read current value, increment by 1.
  - `completed_plans` counter: read current value, increment by 4 (Plan 01, 02, 03, 04 from this phase).
  - `total_phases` and `total_plans` counters: read current value, increment if necessary so they remain ≥ completed values (Phase 999.3 ships as 4 plans).
  - `Current Position` block: update `Phase:` to point to the next planned phase per ROADMAP.md (likely Phase 999.4 truncated-json-response-ux); update `Status:` to a brief description.
  - `Session Continuity` block: update `Stopped at:` to reflect the closure.
  - `Operator Next Steps`: update to point at `/gsd-plan-phase 999.4` (or whichever phase ROADMAP.md identifies as next).
  - DO NOT prescribe LINE NUMBERS for STATE.md edits — read the file, find the field by name, update the value.
  - DO NOT write ABSOLUTE counter values (e.g., "set completed_phases to 8"). Instead, "read current `completed_phases`, increment by 1, write the result."
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Capture pre-UAT grep + build baseline</name>
  <files>.planning/phases/999.3-packet-download-session-cache/999.3-UAT.md</files>
  <read_first>
    - .planning/phases/999.3-packet-download-session-cache/999.3-01-SUMMARY.md (Plan 01 closure).
    - .planning/phases/999.3-packet-download-session-cache/999.3-02-SUMMARY.md (Plan 02 closure).
    - .planning/phases/999.3-packet-download-session-cache/999.3-03-SUMMARY.md (Plan 03 closure).
    - .planning/phases/999.3-packet-download-session-cache/999.3-CONTEXT.md (D-14 gate definition).
    - .planning/phases/999.3-packet-download-session-cache/_baseline-warnings.txt (warning baseline integer captured by Plan 01 Task 0).
  </read_first>
  <action>
    Create the file `.planning/phases/999.3-packet-download-session-cache/999.3-UAT.md`.

    Begin the file with a frontmatter block:
    ```
    ---
    phase: 999.3-packet-download-session-cache
    uat_date: 2026-05-21
    deck_under_test: "<populated at UAT checkpoint>"
    pass: <populated at UAT checkpoint>
    ---
    ```

    Add the following H2 sections (populate content as part of this task before the UAT checkpoint runs):

    1. `## Grep Gate (D-14)` — Run the grep commands listed in the verify block of this task. Record each command's exact output. Expected counts:
       - `grep -c "_packetCache.Set" DeckFlow.Web/Services/DeckAnalysisPacketService.cs` → 1
       - `grep -c "_packetCache.Set" DeckFlow.Web/Services/DeckComparisonService.cs` → 1
       - `grep -c "_packetCache.Set" DeckFlow.Web/Services/MetaGapService.cs` → 1
       - `grep -c "_packetCache.TryGet" DeckFlow.Web/Controllers/DeckController.cs` → 3
       - `grep -c "public static string ComputeCacheKey" DeckFlow.Web/Services/DeckAnalysisPacketService.cs` → 1
       - `grep -c "public static string ComputeCacheKey" DeckFlow.Web/Services/DeckComparisonService.cs` → 1
       - `grep -c "public static string ComputeCacheKey" DeckFlow.Web/Services/MetaGapService.cs` → 1
       - `test -f DeckFlow.Web/Services/PacketSessionCache.cs && echo present` → "present"
       - `grep -c "AddSingleton<PacketSessionCache>" DeckFlow.Web/Program.cs` → 1
       - `grep -c "sp.GetRequiredService<PacketSessionCache>" DeckFlow.Web/Program.cs` → 3
       - `grep -c "SizeLimit = 10_000_000" DeckFlow.Web/Services/PacketSessionCache.cs` → 1
       - `grep -c "TimeSpan.FromMinutes(5)" DeckFlow.Web/Services/PacketSessionCache.cs` → 1
       - `grep -c "Packet cache {Outcome} for {KeyPrefix} ({SizeBytes} bytes)" DeckFlow.Web/Services/PacketSessionCache.cs` → 1
       - `! grep -q "SemaphoreSlim" DeckFlow.Web/Services/PacketSessionCache.cs` → exit 0
       - `! grep -q "Lazy<Task" DeckFlow.Web/Services/PacketSessionCache.cs` → exit 0

    2. `## Build Gate (D-14, B8 + W5)` — Use MSBuild summary line extraction, NOT per-line warning regex:
       - Run `dotnet.exe build DeckFlow.sln -c Release > /tmp/999.3-04-build.log 2>&1`
       - Confirm `grep -q "Build succeeded" /tmp/999.3-04-build.log`.
       - Extract `CURRENT=$(grep -oP '\d+(?= Warning\(s\))' /tmp/999.3-04-build.log | tail -1)`.
       - Read `BASELINE=$(cat .planning/phases/999.3-packet-download-session-cache/_baseline-warnings.txt)`.
       - Record both numbers verbatim in this section: "CURRENT=N, BASELINE=M, delta=N-M".
       - Pass criteria: `CURRENT -le BASELINE` (delta ≤ 0).

    3. `## UAT Procedure (deferred to Task 2 checkpoint)` — Document the UAT steps the user will execute. Leave a "Result:" placeholder for each step:
       a. User starts dev server manually (per global memory note — executor never auto-launches).
       b. Browser to `http://localhost:5173`.
       c. Navigate to Deck Analysis (or whichever workflow exposed the bug in Phase 999.2 UAT — confirm with user).
       d. Paste the same large Commander deck used in Phase 999.2 UAT 2026-05-20 (the deck the user can re-supply).
       e. Submit preview. Note time.
       f. After preview renders, click Download. Note time.
       g. Expected: Download click → zip arrives in under 2 seconds (cache hit).
       h. Confirm by tailing `logs/web-{today}.log` and grepping for `Packet cache hit` — must find at least one entry with an 8-char key prefix and a positive size.
       i. Wait > 5 minutes (or restart the dev server). Click Download again with the same form state. Expected: cache miss, BuildAsync re-runs, log line shows `Packet cache miss`, then `Packet cache write` after BuildAsync completes.
       j. Repeat steps c-h on Deck Comparison workflow (compare two large Commander decks).
       k. Repeat steps c-h on cEDH Meta-Gap workflow.

    4. `## Decisions Verified (D-01..D-14)` — A checklist mapping each decision to its observable verification:
       - D-01 (field-bag composition) — verified via cache hit on preview→download with identical inputs and cache miss when changing IncludeCardVersions/etc.
       - D-02 (URL normalization) — verified via cache hit when same deck submitted once as URL and once as canonical paste (if testable in UAT timeframe; defer to inspection if too time-consuming).
       - D-03 (SHA-256 hex key) — verified via inspection of log line `KeyPrefix` field — must be 8 lowercase hex chars.
       - D-04 (global key scope) — verified by inspection: no per-IP / per-session salt code path in PacketSessionCache.cs.
       - D-05 (5-min TTL) — verified via timed cache miss after >5 min wait (UAT step i).
       - D-06 (dedicated MemoryCache w/ SizeLimit) — verified via grep gate.
       - D-07 (size = char-count sum) — verified via inspection of `SizeBytes` log field — must be > 0 on write, reasonably sized for a large deck.
       - D-08 (all 3 services) — verified via grep gate count of 1 per service.
       - D-09 (/upload writes) — verified via UAT step: upload a zip with previously-cached deck-analysis state, then download — should hit cache.
       - D-10 (precedence: short-circuit → cache → BuildAsync) — verified via inspection of DeckController.cs download handlers + UAT: a request with ResponseJson set AND DeckSource empty (deck-comparison) goes through the existing fast path WITHOUT a cache log line.
       - D-11 (silent miss) — verified via UAT step i: no user-facing error on cache miss; download still succeeds.
       - D-12 (stampede accepted) — verified via inspection: no SemaphoreSlim in PacketSessionCache.cs.
       - D-13 (structured Serilog) — verified via UAT log inspection: log line matches template exactly.
       - D-14 (test gate) — this entire document is the verification artifact.

    5. `## Results` — Populate at the UAT checkpoint (Task 2). Sections to fill:
       - Preview→download timing (cache hit): _____ seconds (expected: <2s).
       - Preview→download timing (cache miss / pre-phase baseline): _____ seconds (expected: >120s on large deck per Phase 999.2 observation).
       - Cache hit log line excerpt (paste verbatim).
       - Cache miss log line excerpt (paste verbatim).
       - Cache write log line excerpt (paste verbatim).
       - Cache evicted log line excerpt (if observed during UAT; optional — eviction may not naturally occur within UAT window).
       - Pass/fail attestation for each of the three workflows.

    Save the file. This document is the binding test-gate artifact for D-14 — Plan 04 closure depends on it being populated and approved at the Task 2 checkpoint.
  </action>
  <verify>
    <automated>
      cd /mnt/c/users/chrislunt/source/personal/deckflow &amp;&amp;
      test -f .planning/phases/999.3-packet-download-session-cache/999.3-UAT.md &amp;&amp;
      grep -q "## Grep Gate" .planning/phases/999.3-packet-download-session-cache/999.3-UAT.md &amp;&amp;
      grep -q "## Build Gate" .planning/phases/999.3-packet-download-session-cache/999.3-UAT.md &amp;&amp;
      grep -q "## UAT Procedure" .planning/phases/999.3-packet-download-session-cache/999.3-UAT.md &amp;&amp;
      grep -q "## Decisions Verified" .planning/phases/999.3-packet-download-session-cache/999.3-UAT.md &amp;&amp;
      grep -q "D-01" .planning/phases/999.3-packet-download-session-cache/999.3-UAT.md &amp;&amp;
      grep -q "D-14" .planning/phases/999.3-packet-download-session-cache/999.3-UAT.md &amp;&amp;
      grep -c "_packetCache.Set" DeckFlow.Web/Services/DeckAnalysisPacketService.cs | grep -qx 1 &amp;&amp;
      grep -c "_packetCache.Set" DeckFlow.Web/Services/DeckComparisonService.cs | grep -qx 1 &amp;&amp;
      grep -c "_packetCache.Set" DeckFlow.Web/Services/MetaGapService.cs | grep -qx 1 &amp;&amp;
      grep -c "_packetCache.TryGet" DeckFlow.Web/Controllers/DeckController.cs | grep -qx 3 &amp;&amp;
      grep -c "public static string ComputeCacheKey" DeckFlow.Web/Services/DeckAnalysisPacketService.cs | grep -qx 1 &amp;&amp;
      grep -c "public static string ComputeCacheKey" DeckFlow.Web/Services/DeckComparisonService.cs | grep -qx 1 &amp;&amp;
      grep -c "public static string ComputeCacheKey" DeckFlow.Web/Services/MetaGapService.cs | grep -qx 1 &amp;&amp;
      grep -c "AddSingleton<PacketSessionCache>" DeckFlow.Web/Program.cs | grep -qx 1 &amp;&amp;
      grep -c "sp.GetRequiredService<PacketSessionCache>" DeckFlow.Web/Program.cs | grep -qx 3 &amp;&amp;
      test -f DeckFlow.Web/Services/PacketSessionCache.cs &amp;&amp;
      dotnet.exe build DeckFlow.sln -c Release > /tmp/999.3-04-build.log 2>&amp;1 &amp;&amp;
      grep -q "Build succeeded" /tmp/999.3-04-build.log &amp;&amp;
      CURRENT=$(grep -oP '\d+(?= Warning\(s\))' /tmp/999.3-04-build.log | tail -1) &amp;&amp;
      BASELINE=$(cat .planning/phases/999.3-packet-download-session-cache/_baseline-warnings.txt) &amp;&amp;
      [ "$CURRENT" -le "$BASELINE" ]
    </automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/999.3-packet-download-session-cache/999.3-UAT.md` exists.
    - File contains the four required H2 sections: Grep Gate, Build Gate, UAT Procedure, Decisions Verified.
    - All grep-gate commands listed in the UAT file return the expected counts (verified by the automated block).
    - All D-01 through D-14 entries are listed in the Decisions Verified checklist (even if their `Result:` field is still empty pending the UAT checkpoint).
    - `dotnet.exe build DeckFlow.sln -c Release` exits 0 with "Build succeeded." and current warning count ≤ `_baseline-warnings.txt` (B8 + W5).
    - `## Results` section exists but is empty/placeholder-filled — will be populated at the Task 2 checkpoint.
  </acceptance_criteria>
  <done>UAT document scaffolded with grep + build gates passing. Ready for the human-verify checkpoint to populate the timings + log excerpts.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human UAT — measure preview→download click timing on large Commander deck</name>
  <what-built>
    Phase 999.3 has shipped Plans 01-03:
    - Plan 01: `PacketSessionCache.cs` wrapper + DI registration in `Program.cs:242` area + warning baseline captured.
    - Plan 02: All 3 workflow services (`DeckAnalysisPacketService`, `DeckComparisonService`, `MetaGapService`) write to the cache at line 467 / 198 / 196 of their `BuildAsync` methods (full-pipeline returns only — short-circuit branches at 156, 188, 122 do NOT cache). Each service exposes a `public static string ComputeCacheKey(...)` helper for cross-plan parity. `/deck-analysis/upload` participates automatically (D-09).
    - Plan 03: All 3 download handlers in `DeckController.cs` consult the cache between the existing response-json-only short-circuit and the `BuildAsync` fallthrough (D-10), calling Plan 02's static helpers for cache-key composition.

    Task 1 of Plan 04 has captured the grep-gate baseline + build attestation in `.planning/phases/999.3-packet-download-session-cache/999.3-UAT.md`. This checkpoint is the manual UAT — the user runs DeckFlow, exercises the three workflows, and confirms the cache speeds up the preview→download click as expected.
  </what-built>
  <how-to-verify>
    **DO NOT auto-launch the dev server. Ask the user to start it.** (Per global memory note: "User starts dev server — never auto-launch DeckFlow web; ask user to start".)

    Steps (ask the user to perform; record results in `999.3-UAT.md` `## Results` section as the user reports them):

    1. **User starts the dev server** via `scripts/run-web.sh` (WSL/Linux) or `scripts/run-web.ps1` (Windows). Browser opens to `http://localhost:5173`.

    2. **Deck Analysis cache-hit UAT.**
       - User navigates to `/deck-analysis`.
       - User pastes the same large Commander deck used in Phase 999.2 UAT on 2026-05-20 (the deck that exposed the 2+ minute double-replay; reference observations 237 + 240 in the session log).
       - User selects the same TargetAiPlatform + analysis questions used at preview time.
       - User clicks Submit (preview).
       - User notes the wall-clock time from click to preview render. (Expected: matches pre-phase baseline, ~2+ minutes on large deck.)
       - User clicks Download.
       - User notes the wall-clock time from click to zip download. (Expected: <2 seconds — cache hit.)
       - Executor tails `logs/web-{today}.log` and runs `grep "Packet cache hit" logs/web-*.log` — must find at least one entry. Capture the line verbatim in `999.3-UAT.md` `## Results`.

    3. **Deck Analysis cache-miss UAT (TTL expiry OR process restart).**
       - User waits 5+ minutes (or restarts the dev server — fastest option).
       - User clicks Download again (without re-running preview).
       - Expected: cache miss → BuildAsync runs from scratch → download arrives after ~2+ minutes (matching pre-phase baseline).
       - Executor greps `Packet cache miss` in the log — must find one entry, followed by `Packet cache write` after BuildAsync completes. Capture both lines verbatim.

    4. **Deck Comparison cache-hit UAT.**
       - User navigates to `/deck-comparison`.
       - User pastes two large Commander decks (use the same Phase 999.2 deck for one slot + any second large deck).
       - User clicks Submit (preview).
       - User clicks Download. Expected: <2s on the cache-hit second click.
       - Executor confirms `Packet cache hit` log line for the comparison workflow. Capture verbatim.

    5. **cEDH Meta-Gap cache-hit UAT.**
       - User navigates to `/cedh-meta-gap`.
       - User selects a commander with edhtop16 results (e.g., a popular cEDH commander).
       - User pastes a deck source.
       - User clicks Submit (preview).
       - User clicks Download. Expected: <2s.
       - Executor confirms `Packet cache hit` log line for the meta-gap workflow.

    6. **D-10 precedence preservation UAT (deck-comparison response-json-only short-circuit).**
       - User opens browser DevTools.
       - User submits a deck-comparison preview, lets it complete.
       - User pastes a fabricated `ComparisonResponseJson` into the Step 3 textarea (simulating a round-trip-edit case).
       - User clears DeckASource and DeckBSource on the form (simulating the "AI response edit only" flow that triggers the fast-path short-circuit at lines 705-725).
       - User clicks Download.
       - Expected: fast-path short-circuit fires (no cache log line; no BuildAsync; instant download). Confirm by grepping the log for the ABSENCE of a `Packet cache` line during this specific click.

    7. **Sign-off.**
       - User reports timings + log excerpts.
       - Executor populates the `## Results` section of `999.3-UAT.md` with the captured data.
       - User confirms "UAT passed" or describes failures.

    **Resume signal:** Type "UAT approved — close phase" to proceed, OR describe failures and the executor will route back to the relevant plan for remediation.
  </how-to-verify>
  <resume-signal>Type "UAT approved — close phase" if all five UATs pass, or describe failures (which workflow, which step, what was observed).</resume-signal>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Update README + STATE + ROADMAP to reflect phase closure (W6: field references, not line numbers)</name>
  <files>README.md, .planning/STATE.md, .planning/ROADMAP.md</files>
  <read_first>
    - README.md (entire file — look for an existing "Features" or "Performance" section to nest the new bullet under; if none, add the note adjacent to the existing change log or under the relevant workflow section).
    - .planning/STATE.md (entire file — find the `## Current Position` block, `## Session Continuity` block, `## Operator Next Steps`, and the YAML-frontmatter-style counter fields at the top. W6: locate by FIELD NAME, not by line number — line numbers will drift as STATE.md evolves).
    - .planning/ROADMAP.md (entire file — find the Phase 999.3 entry by header, NOT by line number; update the `**Plans:**` line, the plan list, and the Progress table row for 999.3).
    - .planning/phases/999.3-packet-download-session-cache/999.3-UAT.md (the just-approved UAT document with `## Results` populated).
    - CLAUDE.md "Commits" — README updated when behavior changes (mandatory).
  </read_first>
  <action>
    Apply THREE additive edits. **CRITICAL (W6 fix):** reference STATE.md and ROADMAP.md sections by FIELD NAME / HEADER TEXT, not by line number. Counter fields are updated by READING the current value and INCREMENTING — do NOT write absolute integers based on what the plan guesses.

    **EDIT 1 — README.md:**
    Add a concise note (1-3 sentences max) somewhere user-visible (the "Performance" or "Features" section if one exists; otherwise near the top of the file under a new `## Performance` H2). Suggested text:

    > **Packet download caching (v1.3 / Phase 999.3):** Deck Analysis, Deck Comparison, and cEDH Meta-Gap download endpoints reuse the Scryfall pipeline result built during preview, so a large Commander deck's download click completes in under 2 seconds instead of 2+ minutes. Cache is in-memory only (process-local, 5-minute TTL, 10MB cap); cache miss falls through silently to the full pipeline.

    Adjust the phrasing to match the README's existing voice (read the existing copy first; the README likely uses second-person voice or terse changelog-style entries — match it).

    DO NOT reformat or restructure the rest of the README. Touch ONLY the new lines being added.

    **EDIT 2 — .planning/STATE.md (W6 fix — reference fields, not line numbers; increment counters, do not set absolutes):**

    Update the following fields in place. Locate each by field name / header text using grep, NOT by line number:

    1. **`last_updated` field** (typically near the top, YAML-style frontmatter): bump to current ISO timestamp (`date -u +%Y-%m-%dT%H:%M:%SZ` or equivalent).

    2. **`last_activity` field**: set to a string like `2026-05-21 -- Phase 999.3 closed (packet download session cache shipped)` (use the actual UAT date from the UAT document's frontmatter).

    3. **`completed_phases` counter**: read current value, INCREMENT BY 1 (Phase 999.3 just closed). Write the result. Do NOT write a hard-coded absolute number — the plan does not know what the current count is without reading the file.

    4. **`completed_plans` counter**: read current value, INCREMENT BY 4 (this phase shipped 4 plans: 01, 02, 03, 04). Write the result.

    5. **`total_phases` counter**: read current value. If `total_phases < completed_phases` after step 3's increment, raise `total_phases` to match. Otherwise leave unchanged.

    6. **`total_plans` counter**: read current value. If `total_plans < completed_plans` after step 4's increment, raise `total_plans` by 4 (since Phase 999.3 added 4 plans to the project total). Otherwise leave unchanged.

    7. **`## Current Position` block** (locate by H2 header `## Current Position`, not by line number): update the `Phase:` line to point to the next phase per ROADMAP.md. Read ROADMAP.md to identify the next phase by inspection — likely "999.4 (truncated-json-response-ux) — PENDING" but confirm via the file. Update the `Status:` line to a brief one-line description.

    8. **`## Session Continuity` block** (locate by H2 header): update the `Stopped at:` field to `Phase 999.3 closed — packet download session cache shipped`.

    9. **`## Operator Next Steps`** (locate by H2 header): update the bullet to `Run /gsd-plan-phase 999.4 to decompose the truncated-JSON response UX phase.` (or substitute the actual next phase if ROADMAP.md identifies a different one).

    DO NOT touch the `## Accumulated Context` section's historical v1.1/v1.2 content. ONLY update the position/progress/continuity fields described above.

    **EDIT 3 — .planning/ROADMAP.md (W6 — reference Phase 999.3 entry by header, not line number):**

    Locate the Phase 999.3 entry by grepping for `### Phase 999.3:` or analogous header. Update in place:

    - The `**Plans:**` line currently says `0/0 plans complete` or similar placeholder. Change to `**Plans:** 4/4 plans complete`.
    - The Plans block currently has a placeholder `_TBD — generated by `/gsd:plan-phase 999.3`._`. Replace with a Wave-grouped list of the four plans (waves match the final dependency graph after the B5 revision):
      ```
      Plans:
      **Wave 1**

      - [x] 999.3-01-PLAN.md — Create PacketSessionCache wrapper + size-estimation helpers + Program.cs DI registration + warning baseline

      **Wave 2**

      - [x] 999.3-02-PLAN.md — Wire cache write at end of BuildAsync in DeckAnalysisPacketService + DeckComparisonService + MetaGapService + Program.cs factory blocks + public static ComputeCacheKey helpers

      **Wave 3**

      - [x] 999.3-03-PLAN.md — Layer cache-read precedence onto 3 download handlers in DeckController.cs (D-10), calling Plan 02's ComputeCacheKey helpers

      **Wave 4**

      - [x] 999.3-04-PLAN.md — Manual UAT + grep gate + build gate + README/STATE/ROADMAP closure
      ```

    Update the Progress table row for 999.3 (locate by grepping the row containing "999.3" in the table): change the Plans column to `4/4`, the Status to `Complete`, and the completion date to the UAT date from the UAT document.

    DO NOT touch any other phase entries or the backlog section.
  </action>
  <verify>
    <automated>
      cd /mnt/c/users/chrislunt/source/personal/deckflow &amp;&amp;
      grep -qi "session cache\|packet.*cache\|999.3" README.md &amp;&amp;
      grep -q "999.3 closed\|999.3.*shipped\|packet download session cache" .planning/STATE.md &amp;&amp;
      grep -q "999.3-01-PLAN.md" .planning/ROADMAP.md &amp;&amp;
      grep -q "999.3-02-PLAN.md" .planning/ROADMAP.md &amp;&amp;
      grep -q "999.3-03-PLAN.md" .planning/ROADMAP.md &amp;&amp;
      grep -q "999.3-04-PLAN.md" .planning/ROADMAP.md &amp;&amp;
      grep -q "999.3.*4/4.*Complete\|999.3 Packet Download Session Cache.*4/4.*Complete" .planning/ROADMAP.md
    </automated>
  </verify>
  <acceptance_criteria>
    - README.md contains a user-visible note mentioning the session cache (case-insensitive grep for "session cache" or "packet cache" or "999.3" passes).
    - .planning/STATE.md `last_activity` mentions "999.3 closed" or "session cache shipped" or similar phrase.
    - .planning/STATE.md `completed_phases` and `completed_plans` are INCREMENTED from their previous values (do not check absolute values — only verify the field exists and was bumped).
    - .planning/ROADMAP.md Phase 999.3 entry lists all four PLAN files (01-04) in their wave structure (Wave 1, Wave 2, Wave 3, Wave 4).
    - .planning/ROADMAP.md Progress table row for 999.3 shows `4/4` plans and `Complete` status.
    - No other phase entries or backlog items modified.
  </acceptance_criteria>
  <done>Phase 999.3 documentation closure complete. One commit `docs(999.3): close phase — packet session cache shipped, UAT approved` covering Plan 04 Task 1 (UAT scaffold), Task 2 result (the populated `999.3-UAT.md`), and Task 3 (README + STATE + ROADMAP). Plain default-author commit. Push to origin v1.3 when user explicitly approves push (do NOT push autonomously per CLAUDE.md "Commits" — `git push` requires user instruction).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| n/a | Plan 04 is a verification + documentation plan — no new code paths, no new trust boundaries. The UAT exercises the boundaries verified by Plans 01-03's threat models. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-999.3-18 | Tampering | UAT log excerpts pasted into `999.3-UAT.md` could contain user-supplied deck contents | accept | Deck contents are public (Archidekt/Moxfield URLs). Log lines are SHA-256 prefixes + size counts only — no deck text appears in the structured log template per D-13. Pasting the log excerpt verbatim into the UAT doc cannot leak PII or secrets. |
| T-999.3-19 | Information Disclosure | README mentions the cache structure | accept | The cache is in-memory only with no security boundary. Documenting its existence and TTL/SizeLimit does not enable any attack — the implementation is in a public repo regardless. |
| T-999.3-SC | Tampering | npm/pip/cargo installs | accept | No package installs in Plan 04. Documentation-only edits. |
</threat_model>

<verification>
- All grep-gate commands listed in `999.3-UAT.md ## Grep Gate` return expected counts.
- `dotnet.exe build DeckFlow.sln -c Release` exits 0 with current warning count ≤ `_baseline-warnings.txt` (B8 + W5).
- Manual UAT timings recorded for all three workflows (deck-analysis, deck-comparison, cedh-meta-gap) with <2s cache-hit download click on the Phase 999.2 reference deck.
- At least one `Packet cache hit`, one `Packet cache miss`, and one `Packet cache write` log line excerpt captured verbatim in `999.3-UAT.md ## Results`.
- D-10 precedence preservation UAT confirms the existing response-json-only short-circuit STILL fires without a cache log line (verifying it precedes the cache lookup).
- README.md updated.
- .planning/STATE.md `last_activity` reflects Phase 999.3 closure; counter fields incremented from prior values (W6 fix — no absolute-number prescription).
- .planning/ROADMAP.md Phase 999.3 entry + Progress table reflect 4/4 plans complete and the UAT date.
</verification>

<success_criteria>
- D-14 test gate satisfied: manual UAT + dotnet build clean + grep gate.
- Phase 999.3 closed with one closure commit (Plan 04) bringing total Phase 999.3 commits to ~7-9 (1 from Plan 01 + 4 from Plan 02 + 1 from Plan 03 + 1 from Plan 04).
- Plain default-author commits, no `Co-Authored-By` trailer (CLAUDE.md "Commits").
- README updated when behavior changes (CLAUDE.md "Commits").
- The user's Phase 999.2 reference deck preview→download click is now <2s on cache hit (vs >120s pre-phase) — the binding acceptance signal for D-14.
</success_criteria>

<output>
Create `.planning/phases/999.3-packet-download-session-cache/999.3-04-SUMMARY.md` when done summarizing:
- The UAT timings captured (cache hit + cache miss for all three workflows).
- The cache-hit / cache-miss / cache-write log line excerpts.
- The git commit SHA + message for the closure commit.
- Confirmation that `dotnet build` exits 0 with current warning count ≤ baseline.
- Confirmation that README + STATE + ROADMAP were updated (STATE counter increments described, not absolute values).
- Pointer to the next phase: `/gsd-plan-phase 999.4` (truncated-json-response-ux).
</output>
</content>