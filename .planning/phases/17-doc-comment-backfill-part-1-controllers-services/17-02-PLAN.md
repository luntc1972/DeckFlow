---
phase: 17-doc-comment-backfill-part-1-controllers-services
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - DeckFlow.Web/Services/EdhTop16Client.cs
  - DeckFlow.Web/Services/ICategoryKnowledgeStore.cs
  - DeckFlow.Web/Services/CategoryKnowledgeStore.cs
  - DeckFlow.Web/Services/IFeedbackStore.cs
  - DeckFlow.Web/Services/FeedbackStore.cs
  - DeckFlow.Web/Services/ScryfallSetService.cs
  - DeckFlow.Web/Services/ScryfallDtos.cs
  - DeckFlow.Web/Services/DeckFlowDatabaseConnectionFactory.cs
autonomous: true
requirements: [DOC-01]

must_haves:
  truths:
    - "Every undocumented public type in the 8 Services files in this plan (12 types: 4 interfaces + 4 impls + 3 records + 1 static class) carries a summary (interface/standalone/record) or inheritdoc (impl) ATTACHED to the type declaration — on the line(s) immediately above it with NO intervening blank line — not merely somewhere in the file"
    - "ScryfallCard (ScryfallDtos.cs) is documented: its existing summary at lines 36-38 is currently DETACHED from the line-40 declaration by the blank line at line 39; deleting that single blank line re-attaches it (the one allowed whitespace-only deviation from 'only added /// lines')"
    - "Locked decisions applied verbatim and traceable: D-01 (interface owns prose, impl uses inheritdoc), D-01a (standalone types incl. DeckFlowDatabaseConnectionFactory get a full summary), D-02 (param/returns only when >=2 real params — trailing CancellationToken excluded — OR non-obvious return; IFeedbackStore.AddAsync/CountAsync/UpdateStatusAsync/HashIp + IScryfallSetService.BuildSetPacketAsync trigger it, GetSetsAsync/GetAsync/ListAsync/DeleteAsync/CountsByStatusAsync do not), D-03 (hybrid sourcing: seed from CLAUDE.md Component-Responsibilities table, verify against code, write fresh otherwise; comments explain WHY not what), D-04 (records get a type-level summary; param on positional fields only where non-obvious — ScryfallCardFace + ScryfallCard fields self-evident, no param)"
    - "DeckFlowDatabaseConnectionFactory (a co-located static class whose file already has method-level summaries) carries a type-level summary per D-01a — closing the file-level-grep blind spot that hid it"
    - "NoWarn 1591;1573;1587 still present in DeckFlow.Web.csproj (csproj untouched)"
    - "dotnet build -c Release stays at 0 warnings / 0 errors via the WSL dotnet path"
    - "Test suite stays Failed:0 (no runtime change)"
    - "git diff shows only added doc-comment lines for these files, PLUS the single blank-line deletion at ScryfallDtos.cs:39 (whitespace-only, re-attaches an existing summary — no code/text change)"
    - "Authoritative count: 12 declaration-attached gaps across 8 files in THIS plan (17-CONTEXT.md's '45 undocumented types' figure is STALE — see objective)"
  artifacts:
    - path: "DeckFlow.Web/Services/EdhTop16Client.cs"
      provides: "summary on IEdhTop16Client interface + inheritdoc on EdhTop16Client impl (D-01)"
      contains: "summary"
    - path: "DeckFlow.Web/Services/ICategoryKnowledgeStore.cs"
      provides: "type-level summary on ICategoryKnowledgeStore interface"
      contains: "summary"
    - path: "DeckFlow.Web/Services/CategoryKnowledgeStore.cs"
      provides: "type-level inheritdoc on CategoryKnowledgeStore impl (D-01)"
      contains: "inheritdoc"
    - path: "DeckFlow.Web/Services/IFeedbackStore.cs"
      provides: "summary on IFeedbackStore interface + FeedbackRequestContext record (D-04)"
      contains: "summary"
    - path: "DeckFlow.Web/Services/FeedbackStore.cs"
      provides: "type-level inheritdoc on FeedbackStore impl (D-01)"
      contains: "inheritdoc"
    - path: "DeckFlow.Web/Services/ScryfallSetService.cs"
      provides: "summary on IScryfallSetService interface + inheritdoc on ScryfallSetService impl (D-01)"
      contains: "summary"
    - path: "DeckFlow.Web/Services/ScryfallDtos.cs"
      provides: "type-level summary on ScryfallCardFace record (D-04) + re-attached summary on ScryfallCard record (blank-line deletion at line 39)"
      contains: "summary"
    - path: "DeckFlow.Web/Services/DeckFlowDatabaseConnectionFactory.cs"
      provides: "type-level summary on DeckFlowDatabaseConnectionFactory static class (D-01a); methods already documented, no member churn"
      contains: "summary"
  key_links:
    - from: "this plan's 8 files"
      to: "ROADMAP SC1 anchored grep (per-type, not file-level)"
      via: "per-type ATTACHED summary/inheritdoc presence on the declaration line"
      pattern: "summary|inheritdoc"
---

<objective>
Backfill XML doc-comments on the 12 undocumented public types across the 8 Services files in
scope. Four are interface/impl pairs governed by D-01 (prose on the interface, inheritdoc on the
impl); three are positional DTO records governed by D-04 (ScryfallCardFace, FeedbackRequestContext,
and ScryfallCard — see below); one is a standalone static class governed by D-01a
(`DeckFlowDatabaseConnectionFactory` — already has method-level summaries, only the type-level
summary is missing). Type tally: 4 interfaces + 4 impls + 3 records + 1 static class = 12 types
across 8 files.

Purpose: Reduce documentation debt on the Services surface (DOC-01 partial coverage) without
flipping the NoWarn gate (Phase 23 owns that). Output: 8 source files with doc-comment additions
only — zero runtime/behavior change, zero code-line churn (the single ScryfallDtos.cs:39 blank-line
deletion is whitespace-only and changes no code or comment text; see below).

Authoritative count note: 17-CONTEXT.md:11 reports "45 undocumented types" — that figure is STALE
(it came from a scan bug). The authoritative inventory is the per-type declaration-attached scan in
these two plans: ~19 declaration-attached gaps total (7 in 17-01 across 5 files + 12 in THIS plan
across 8 files). These plans SUPERSEDE the 45 figure. Do NOT edit the CONTEXT prose; this note
governs.

ScryfallCard (the one whitespace deviation): ScryfallDtos.cs already has a `<summary>` for
ScryfallCard at lines 36-38, but a BLANK line at line 39 separates it from the
`public sealed record ScryfallCard(` declaration at line 40. C# does NOT associate a `///` comment
across a blank line, so ScryfallCard is effectively undocumented and will emit CS1591 when the
NoWarn suppression is stripped in Phase 23. A full scan of all Controllers+Services confirms
ScryfallCard is the ONLY declaration-detached case. FIX: DELETE the single blank line at line 39 so
the existing summary re-attaches. This is the ONE allowed deviation from "only added `///` lines":
it is a single whitespace-only blank-line deletion — preserve the existing summary text VERBATIM,
change no code, no comment text, nothing else. It is called out explicitly in Task 2's action and
acceptance criteria.

Why DeckFlowDatabaseConnectionFactory was added late: the ROADMAP SC1 verification uses a
file-level anchored grep (`grep -L '<summary>'`), which passes any file that has at least one
`<summary>` anywhere. This file has three method-level summaries (on CreateAdminThrottleConnection,
CreateFeatureFlagConnection, CreateHarvestStateConnection) but NO summary on the
`public static class DeckFlowDatabaseConnectionFactory` declaration itself (line 7). The file-level
grep therefore reported it as "documented" while the TYPE was undocumented — exactly the gap
DOC-01 / D-01a forbid, and exactly the detachment class that the ScryfallCard blank line creates.
This revision adds the type, re-attaches ScryfallCard, and replaces the file-level grep with an
automated per-declaration checker so this class of miss cannot recur.

Execution note (CLAUDE.md): Codex writes the doc-comments; Claude reviews. The D-01..D-04 rules
are inline below so the executor does not need to re-open CONTEXT.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-doc-comment-backfill-part-1-controllers-services/17-CONTEXT.md
@CLAUDE.md
</context>

<doc_comment_rules>
Locked decisions from 17-CONTEXT.md. Apply verbatim; do NOT re-derive.

- Per **D-01**: Interface/impl pairs — the INTERFACE type carries the full prose summary
  (+ param/returns per D-02); the implementing class AND its public members use inheritdoc.
  THIS PLAN has four such pairs:
    IEdhTop16Client / EdhTop16Client (EdhTop16Client.cs),
    ICategoryKnowledgeStore / CategoryKnowledgeStore (split across two files),
    IFeedbackStore / FeedbackStore (split across two files),
    IScryfallSetService / ScryfallSetService (ScryfallSetService.cs).
  Write the prose on the interface; put a type-level inheritdoc on the impl class.
  Several impl classes already have per-method summary/inheritdoc on individual members — leave
  those byte-identical; this plan only adds the missing TYPE-level comment.
- Per **D-01a**: Standalone classes/records with no interface get a full summary written directly.
  THIS PLAN has one such standalone type: DeckFlowDatabaseConnectionFactory (static class). Its
  methods already carry summaries; only the type-level summary is missing — add ONLY that.
- Per **D-02**: Add param/returns on an interface method ONLY when it has >=2 real params (a trailing
  CancellationToken cancellationToken = default does NOT count) OR when the return's meaning is
  non-obvious from the method name. Single-arg self-evident methods get summary only. The exact
  triggers in this plan are enumerated inline in Task 1 (IFeedbackStore + IScryfallSetService).
- Per **D-03**: Hybrid summary sourcing — seed each prose summary from CLAUDE.md's
  Component-Responsibilities table one-liner where the type is listed (EdhTop16Client, FeedbackStore,
  etc.), VERIFY each against the actual current code, and write fresh for types not in the table.
  Inline comments explain WHY not what.
- Per **D-04**: Records get a type-level summary; add param on positional fields ONLY where a field's
  meaning is not self-evident. ScryfallCardFace fields (Name, ManaCost, TypeLine, OracleText,
  Power, Toughness) ARE self-evident -> type-level summary only, NO param. ScryfallCard fields are
  likewise self-evident Scryfall card attributes -> its EXISTING summary stays as-is (just
  re-attach it); NO param. FeedbackRequestContext: type-level summary; add param only on positional
  fields whose meaning is not obvious from the name (judge per the field names present in the file).

ATTACHMENT RULE (critical, this is the ScryfallCard root cause): a `///` comment only documents a
declaration if it sits on the line(s) IMMEDIATELY above the declaration with NO intervening blank
line. Attribute lines (`[property: JsonPropertyName(...)]`, etc.) between the comment and the
declaration are fine — a blank line is NOT. When adding a summary, ensure no blank line separates it
from the declaration; when re-attaching ScryfallCard, delete the blank line.

FORMATTING (CLAUDE.md R-6 — binding): touch ONLY the doc-comment lines you add (plus the single
ScryfallDtos.cs:39 blank-line deletion explicitly authorized below). NO Format Document. NEVER
convert get-init to get-only (System.Text.Json silently skips get-only props — this exact bug
already broke EdhTop16Client deserialization once; see CLAUDE.md). NEVER inline a JsonPropertyName
attribute or any attribute onto the property/type line. NEVER re-indent C# raw-string literals.
Preserve LF line endings. Each file's git diff MUST contain only added doc-comment lines, EXCEPT
ScryfallDtos.cs which additionally has one deleted blank line (line 39, whitespace-only).

DO NOT touch DeckFlow.Web/DeckFlow.Web.csproj — NoWarn strip is Phase 23's job (Pitfall 8).
</doc_comment_rules>

<tasks>

<task type="auto">
  <name>Task 1: Document the 4 interface/impl pairs (D-01 inheritdoc split)</name>
  <files>DeckFlow.Web/Services/EdhTop16Client.cs, DeckFlow.Web/Services/ICategoryKnowledgeStore.cs, DeckFlow.Web/Services/CategoryKnowledgeStore.cs, DeckFlow.Web/Services/IFeedbackStore.cs, DeckFlow.Web/Services/FeedbackStore.cs, DeckFlow.Web/Services/ScryfallSetService.cs</files>
  <read_first>
    - DeckFlow.Web/Services/EdhTop16Client.cs (target — IEdhTop16Client line 7, EdhTop16Client line 19; both type-level undocumented)
    - DeckFlow.Web/Services/ICategoryKnowledgeStore.cs (target — interface line 7 type-level undocumented; members already documented)
    - DeckFlow.Web/Services/CategoryKnowledgeStore.cs (target — impl line 15 type-level undocumented; members already mix summary/inheritdoc)
    - DeckFlow.Web/Services/IFeedbackStore.cs (target — IFeedbackStore line 5; FeedbackRequestContext record handled in Task 2)
    - DeckFlow.Web/Services/FeedbackStore.cs (target — impl line 8 type-level undocumented)
    - DeckFlow.Web/Services/ScryfallSetService.cs (target — IScryfallSetService line 12, ScryfallSetService impl line 22)
    - DeckFlow.Web/Services/CommanderSpellbookService.cs (exemplar — already-documented service; match house summary/inheritdoc voice)
    - .planning/phases/17-doc-comment-backfill-part-1-controllers-services/17-CONTEXT.md (D-01..D-04)
    - CLAUDE.md (Comments + R-6 + Component-Responsibilities table for D-03 seeding; note the EdhTop16Client get-init deserialization warning)
  </read_first>
  <action>
    Apply the D-01 split to each of the 4 pairs. For each pair: write the full prose summary on
    the INTERFACE type; add a single type-level inheritdoc immediately above the IMPLEMENTING
    class declaration. Ensure every `///` block is ATTACHED (no blank line between it and the
    declaration). Do NOT touch any already-present per-member comments.
    - IEdhTop16Client (EdhTop16Client.cs:7) — full summary (per D-03 seed "EDH metagame data", verify);
      EdhTop16Client (EdhTop16Client.cs:19) — type-level inheritdoc.
    - ICategoryKnowledgeStore (ICategoryKnowledgeStore.cs:7) — add the missing TYPE-level summary
      (members already documented); per D-03 seed "Persistence of card-category observations over
      RelationalDatabaseConnection", verify.
    - CategoryKnowledgeStore (CategoryKnowledgeStore.cs:15) — add the missing TYPE-level inheritdoc
      above the class (existing per-method summaries/inheritdoc stay untouched).
    - IFeedbackStore (IFeedbackStore.cs:5) — full TYPE-level summary (per D-03 seed "Persistence over
      RelationalDatabaseConnection", verify). Per D-02, add `<param>`/`<returns>` on these specific
      interface methods (enumerated so you do NOT re-derive — verify against the current signatures):
        * AddAsync(FeedbackSubmission submission, FeedbackRequestContext context, CancellationToken) —
          2 real params (submission + context; trailing CancellationToken excluded) -> add `<param>`
          for submission AND context; the returned long is the new row id (non-obvious) -> add `<returns>`.
        * CountAsync(FeedbackStatus? status, FeedbackType? type, CancellationToken) — 2 real params
          (status + type) -> add `<param>` for both. Return (matching count) is self-evident from the
          name; `<returns>` optional.
        * UpdateStatusAsync(long id, FeedbackStatus status, CancellationToken) — 2 real params
          (id + status) -> add `<param>` for both. Void-returning Task -> no `<returns>`.
        * HashIp(string? ip) — single param, but the RETURN (a salted/hashed IP string, not the raw IP)
          is non-obvious -> `<summary>` + `<returns>` explaining it returns the salted hash (D-02
          non-obvious-return trigger). No `<param>` needed (single self-evident arg).
      These methods get `<summary>` ONLY (single real param or zero, self-evident return), no param/returns:
      GetAsync, ListAsync, DeleteAsync, CountsByStatusAsync.
    - FeedbackStore (FeedbackStore.cs:8) — type-level inheritdoc.
    - IScryfallSetService (ScryfallSetService.cs:12) — full summary; BuildSetPacketAsync has 2 real
      params (setCodes + commanderColorIdentity, trailing CancellationToken excluded) -> add param
      for both + returns per D-02. GetSetsAsync is single-arg (only CancellationToken) -> summary only.
      ScryfallSetService (ScryfallSetService.cs:22, sealed partial class) — type-level inheritdoc.
    Touch only added doc-comment lines.
  </action>
  <verify>
    <automated>set -e; for f in DeckFlow.Web/Services/EdhTop16Client.cs DeckFlow.Web/Services/ICategoryKnowledgeStore.cs DeckFlow.Web/Services/CategoryKnowledgeStore.cs DeckFlow.Web/Services/IFeedbackStore.cs DeckFlow.Web/Services/FeedbackStore.cs DeckFlow.Web/Services/ScryfallSetService.cs; do awk 'BEGIN{bad=0} /^[[:space:]]*(public|internal)[[:space:]]+(sealed[[:space:]]+)?(abstract[[:space:]]+)?(partial[[:space:]]+)?(static[[:space:]]+)?(class|interface|record|enum)[[:space:]]/{ ok=0; for(i=NR-1;i>=1;i--){ l=lines[i]; if(l ~ /^[[:space:]]*$/){break} if(l ~ /^[[:space:]]*\[/){continue} if(l ~ /^[[:space:]]*\/\/\//){ok=1} break } if(!ok){printf "MISSING attached doc: %s:%d %s\n", FILENAME, NR, $0; bad=1} } {lines[NR]=$0} END{exit bad}' "$f"; done; echo "PAIRS PER-TYPE GATE PASS"</automated>
  </verify>
  <acceptance_criteria>
    - AUTOMATED PER-TYPE GATE: the awk checker exits 0 and prints "PAIRS PER-TYPE GATE PASS". It
      fails (non-zero, prints "MISSING attached doc: file:line") on ANY public type declaration in
      these 6 files whose immediately-preceding non-attribute line is not an attached `///`
      `<summary>`/`<inheritdoc/>` — a blank-line detachment or a summary elsewhere in the file
      counts as MISSING (exactly how DeckFlowDatabaseConnectionFactory was missed and ScryfallCard
      detached).
    - Interface types (IEdhTop16Client, ICategoryKnowledgeStore, IFeedbackStore, IScryfallSetService)
      each carry a full type-level summary; impl classes (EdhTop16Client, CategoryKnowledgeStore,
      FeedbackStore, ScryfallSetService) each carry a type-level inheritdoc (D-01).
    - IFeedbackStore: AddAsync has `<param>` for submission + context and a `<returns>` for the row id;
      CountAsync + UpdateStatusAsync have `<param>` for both real params; HashIp has a `<returns>`
      describing the salted hash; GetAsync/ListAsync/DeleteAsync/CountsByStatusAsync are summary-only.
    - IScryfallSetService.BuildSetPacketAsync has param tags for both real params + a returns tag (D-02);
      GetSetsAsync has summary only.
    - git diff for each of the 6 files shows ONLY added doc-comment lines — no code-line churn, no
      get-init to get-only mutation, no attribute inlining, no raw-string re-indent.
    - "/mnt/c/Program Files/dotnet/dotnet.exe" build -c Release stays 0 warn / 0 err.
  </acceptance_criteria>
  <done>All 4 pairs split per D-01; the enumerated IFeedbackStore + IScryfallSetService param/returns tags present; awk gate green for these 6 files; build 0/0; diffs comment-only; member-level comments untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Document the DTO records (D-04) + re-attach ScryfallCard (ScryfallCardFace, FeedbackRequestContext, ScryfallCard)</name>
  <files>DeckFlow.Web/Services/IFeedbackStore.cs, DeckFlow.Web/Services/ScryfallDtos.cs</files>
  <read_first>
    - DeckFlow.Web/Services/IFeedbackStore.cs (target — FeedbackRequestContext positional record line 17; interface itself handled in Task 1, do not re-touch it here)
    - DeckFlow.Web/Services/ScryfallDtos.cs (target — ScryfallCard summary lines 36-38 DETACHED by blank line 39 from declaration line 40; ScryfallCardFace positional record further down; many sibling records ScryfallRuling/ScryfallPrintingIdentifier/ScryfallSearchResponse already documented — match that exact house style)
    - .planning/phases/17-doc-comment-backfill-part-1-controllers-services/17-CONTEXT.md (D-04 rule)
    - CLAUDE.md (Comments + R-6: never mutate get-init, never inline JsonPropertyName attribute, never re-indent)
  </read_first>
  <action>
    Three sub-edits, all in these 2 files:

    1. RE-ATTACH ScryfallCard (ScryfallDtos.cs) — THE ONE AUTHORIZED WHITESPACE DEVIATION.
       ScryfallCard already has a `<summary>` at lines 36-38 ("Represents a Scryfall card payload.")
       but line 39 is BLANK, which detaches it from the `public sealed record ScryfallCard(`
       declaration at line 40 — so the record is effectively undocumented (will emit CS1591 at the
       Phase 23 NoWarn strip). FIX: delete ONLY the blank line at line 39 so the existing summary
       sits immediately above the declaration. Preserve the summary text VERBATIM. Change NO code,
       NO comment text — this is a single whitespace-only blank-line deletion and the only non-`///`
       edit permitted in either plan. Do NOT add a new summary or param tags (the fields — Name,
       ManaCost, TypeLine, etc. — are self-evident per D-04).

    2. ScryfallCardFace (ScryfallDtos.cs) — write a fresh type-level summary (D-04); describe it as
       one face of a multi-faced Scryfall card (DFC/split/adventure). Positional fields (Name,
       ManaCost, TypeLine, OracleText, Power, Toughness) are self-evident -> NO param tags. Match the
       summary voice of the already-documented sibling records in the same file. Ensure ATTACHED (no
       blank line). Do NOT inline or reflow the [property: JsonPropertyName(...)] attributes.

    3. FeedbackRequestContext (IFeedbackStore.cs) — write a fresh type-level summary; describe it as
       the request-side context captured for a feedback submission (Ip, UserAgent, PageUrl,
       AppVersion). Per D-04, add a `<param>` ONLY on a positional field whose meaning is not obvious
       from the name — the Ip field is later salted/hashed by IFeedbackStore.HashIp, so if that is
       not obvious from "Ip" alone add a short `<param name="Ip">` noting it is the raw client IP
       (hashed at store time); UserAgent/PageUrl/AppVersion are self-evident -> no param. Ensure
       ATTACHED. Do not re-touch the IFeedbackStore interface comment from Task 1.

    Touch only added doc-comment lines, plus the single ScryfallDtos.cs:39 blank-line deletion.
  </action>
  <verify>
    <automated>set -e; for f in DeckFlow.Web/Services/ScryfallDtos.cs DeckFlow.Web/Services/IFeedbackStore.cs; do awk 'BEGIN{bad=0} /^[[:space:]]*(public|internal)[[:space:]]+(sealed[[:space:]]+)?record[[:space:]]/{ ok=0; for(i=NR-1;i>=1;i--){ l=lines[i]; if(l ~ /^[[:space:]]*$/){break} if(l ~ /^[[:space:]]*\[/){continue} if(l ~ /^[[:space:]]*\/\/\//){ok=1} break } if(!ok){printf "MISSING attached summary: %s:%d %s\n", FILENAME, NR, $0; bad=1} } {lines[NR]=$0} END{exit bad}' "$f"; done; echo "RECORDS PER-TYPE GATE PASS (incl re-attached ScryfallCard)"</automated>
  </verify>
  <acceptance_criteria>
    - AUTOMATED PER-TYPE GATE: the awk checker exits 0 and prints "RECORDS PER-TYPE GATE PASS". It
      asserts EVERY public record in these 2 files (ScryfallCard, ScryfallCardFace, every sibling
      record in ScryfallDtos.cs, FeedbackRequestContext) has an ATTACHED `///` `<summary>` on the
      line(s) immediately above the declaration. ScryfallCard MUST pass — i.e. the line-39 blank line
      is gone and the existing summary now sits directly above line 40 (this is the regression the
      checker is specifically designed to catch).
    - ScryfallCard re-attachment: `git diff -- DeckFlow.Web/Services/ScryfallDtos.cs` shows exactly
      ONE deleted blank line (line 39) plus any added `///` lines for ScryfallCardFace — the
      ScryfallCard summary text at 36-38 is byte-identical, no new ScryfallCard summary added.
    - ScryfallCardFace has NO param tags (fields self-evident); FeedbackRequestContext has a param tag
      only on the non-obvious Ip field (if warranted), none on self-evident fields.
    - git diff for both files shows ONLY added doc-comment lines + the one ScryfallDtos.cs:39 blank
      deletion; all get-init setters and JsonPropertyName attributes are byte-identical.
    - "/mnt/c/Program Files/dotnet/dotnet.exe" build -c Release stays 0 warn / 0 err.
  </acceptance_criteria>
  <done>ScryfallCard re-attached (single blank-line deletion, summary verbatim); ScryfallCardFace + FeedbackRequestContext have attached type-level summaries; records per-type gate green; diffs comment-only except the one authorized blank-line deletion; attributes + init setters untouched.</done>
</task>

<task type="auto">
  <name>Task 3: Document DeckFlowDatabaseConnectionFactory type (D-01a — closes the file-level-grep blind spot)</name>
  <files>DeckFlow.Web/Services/DeckFlowDatabaseConnectionFactory.cs</files>
  <read_first>
    - DeckFlow.Web/Services/DeckFlowDatabaseConnectionFactory.cs (target — `public static class DeckFlowDatabaseConnectionFactory` at line 7 has NO type-level summary; three methods at lines 15/24/33 already carry summaries — leave them byte-identical; two methods CreateFeedbackConnection (line 12) and CreateCategoryKnowledgeConnection (line 42) take a single self-evident IWebHostEnvironment param)
    - DeckFlow.Web/Services/CommanderSpellbookService.cs (exemplar — house static/service summary voice)
    - .planning/phases/17-doc-comment-backfill-part-1-controllers-services/17-CONTEXT.md (D-01a + D-02; the undocumented-type inventory at line 132 lists this type)
    - CLAUDE.md (Comments + R-6 touch-only discipline; Component-Responsibilities note that this factory selects SQLite vs Postgres via DECKFLOW_DATABASE_PROVIDER)
  </read_first>
  <action>
    Add a single type-level `<summary>` (D-01a — standalone static class, no interface) immediately
    above the `public static class DeckFlowDatabaseConnectionFactory` declaration at line 7, ATTACHED
    (no intervening blank line). Describe it as the factory that builds RelationalDatabaseConnection
    handles for the app's logical databases, selecting SQLite (default, file under MTG_DATA_DIR /
    artifacts) vs Postgres based on the DECKFLOW_DATABASE_PROVIDER + DECKFLOW_DATABASE_CONNECTION_STRING
    env vars; note in WHY-style prose that the admin-throttle / feature-flag / harvest connections
    deliberately share the feedback connection (single logical DB, per D-07).
    Per D-02: the three documented methods already have summaries — DO NOT touch them. The two
    currently-undocumented methods (CreateFeedbackConnection, CreateCategoryKnowledgeConnection) each
    take only a single self-evident `IWebHostEnvironment environment` param and return a
    RelationalDatabaseConnection whose meaning is obvious from the method name -> they fall under the
    D-02 single-arg / self-evident-return rule, so type-level summary is sufficient and NO
    method-level summary/param/returns is required on them. (If a future executor judges the return
    non-obvious, a one-line summary is acceptable, but it is NOT required by D-02 and is not the
    deliverable of this task.) Do NOT add summaries to private helpers (CreateConnection,
    ResolveArtifactsPath) or to the internal NormalizePostgresConnectionString.
    Touch ONLY the added `///` line(s) above the class declaration (R-6).
  </action>
  <verify>
    <automated>awk 'BEGIN{bad=0} /^[[:space:]]*public[[:space:]]+static[[:space:]]+class[[:space:]]+DeckFlowDatabaseConnectionFactory/{ ok=0; for(i=NR-1;i>=1;i--){ l=lines[i]; if(l ~ /^[[:space:]]*$/){break} if(l ~ /^[[:space:]]*\[/){continue} if(l ~ /^[[:space:]]*\/\/\//){ok=1} break } if(!ok){printf "MISSING attached type-level summary at %s:%d\n", FILENAME, NR; bad=1} } {lines[NR]=$0} END{exit bad}' DeckFlow.Web/Services/DeckFlowDatabaseConnectionFactory.cs && echo "TYPE-LEVEL SUMMARY ATTACHED"</automated>
  </verify>
  <acceptance_criteria>
    - AUTOMATED PER-TYPE GATE: the awk checker exits 0 and prints "TYPE-LEVEL SUMMARY ATTACHED" —
      the `public static class DeckFlowDatabaseConnectionFactory` declaration has an ATTACHED `///`
      `<summary>` on the line(s) immediately above it (no blank-line detachment). The file-level
      `grep -L '<summary>'` already passed BEFORE this change (because of the three method summaries)
      — that is precisely the blind spot; this gate verifies against the TYPE declaration line, not
      the file.
    - The three pre-existing method summaries (CreateAdminThrottleConnection,
      CreateFeatureFlagConnection, CreateHarvestStateConnection) are byte-identical (untouched).
    - No new member-level doc-comments added (D-02 not triggered for the single-arg factory methods);
      private/internal helpers remain undocumented.
    - git diff shows ONLY the added type-level `///` line(s) — no code-line churn, no member churn,
      no attribute/get-init mutation.
    - "/mnt/c/Program Files/dotnet/dotnet.exe" build -c Release stays 0 warn / 0 err.
  </acceptance_criteria>
  <done>DeckFlowDatabaseConnectionFactory carries an attached type-level summary; the three existing method summaries untouched; awk gate green; diff is a single comment hunk; build 0/0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none affected) | This plan adds XML doc-comments only (plus one whitespace-only blank-line deletion). Comments are stripped by the C# compiler and never reach any runtime path, HTTP egress, persistence query, auth gate, or data flow. No trust boundary is created, moved, or weakened. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-02 | Tampering | Source files (doc-comment additions + 1 blank-line deletion) | accept | Doc-comments introduce ZERO new runtime attack surface — compile-time-stripped. The ScryfallDtos.cs:39 deletion is a whitespace-only line removal that re-attaches an existing comment; it changes no token, so deserialization of ScryfallCard is byte-identical (the build-0/0 + comment-only-diff gates confirm). The get-init preservation rule (R-6) guards the System.Text.Json get-only-skip bug that previously broke EdhTop16Client deserialization. DeckFlowDatabaseConnectionFactory parses connection strings / env vars at startup — but Task 3 touches ONLY the type-level comment above the class, no method body, so the connection-string parsing path is byte-identical. Block-on severity high: none expected. |

No npm/pip/cargo package installs in this plan — Package Legitimacy Gate not applicable.
</threat_model>

<verification>
After all three tasks:
1. AUTOMATED PER-TYPE SC1 GATE (authoritative): these files have co-located interface+impl+records
   +a static class, AND one detached-summary case (ScryfallCard), so verification is per-declaration,
   not file-level — file-level grep is INSUFFICIENT and is the exact blind spot that hid
   DeckFlowDatabaseConnectionFactory and detached ScryfallCard. Run a single awk gate across all 8
   files; it must exit 0 and print "PER-TYPE GATE PASS". It fails on ANY public type whose
   immediately-preceding non-attribute line is not a `///` `<summary>`/`<inheritdoc/>` (blank-line
   detachment = MISSING):
   `set -e; for f in DeckFlow.Web/Services/EdhTop16Client.cs DeckFlow.Web/Services/ICategoryKnowledgeStore.cs DeckFlow.Web/Services/CategoryKnowledgeStore.cs DeckFlow.Web/Services/IFeedbackStore.cs DeckFlow.Web/Services/FeedbackStore.cs DeckFlow.Web/Services/ScryfallSetService.cs DeckFlow.Web/Services/ScryfallDtos.cs DeckFlow.Web/Services/DeckFlowDatabaseConnectionFactory.cs; do awk 'BEGIN{bad=0} /^[[:space:]]*(public|internal)[[:space:]]+(sealed[[:space:]]+)?(abstract[[:space:]]+)?(partial[[:space:]]+)?(static[[:space:]]+)?(class|interface|record|enum)[[:space:]]/{ ok=0; for(i=NR-1;i>=1;i--){ l=lines[i]; if(l ~ /^[[:space:]]*$/){break} if(l ~ /^[[:space:]]*\[/){continue} if(l ~ /^[[:space:]]*\/\/\//){ok=1} break } if(!ok){printf "MISSING attached doc: %s:%d %s\n", FILENAME, NR, $0; bad=1} } {lines[NR]=$0} END{exit bad}' "$f"; done; echo "PER-TYPE GATE PASS"`
   This is the authoritative gate; non-zero exit blocks the plan. It specifically catches the
   ScryfallCard detachment regression.
2. SECONDARY SMOKE CHECK (ROADMAP-anchored, weaker — cannot detect detachment or co-located gaps):
   the file-level grep must also return empty. Smoke check only, NOT authoritative:
   `grep -L '<summary>' DeckFlow.Web/Services/EdhTop16Client.cs DeckFlow.Web/Services/ICategoryKnowledgeStore.cs DeckFlow.Web/Services/IFeedbackStore.cs DeckFlow.Web/Services/ScryfallSetService.cs DeckFlow.Web/Services/ScryfallDtos.cs DeckFlow.Web/Services/DeckFlowDatabaseConnectionFactory.cs` returns empty.
3. BUILD GATE (local, authoritative): VSTest is unreliable in WSL (CLAUDE.md Testing constraint),
   so the LOCAL gate is a clean Release build with 0/0 via the WSL dotnet path:
   `"/mnt/c/Program Files/dotnet/dotnet.exe" build -c Release` => 0 Warning(s) / 0 Error(s).
   The FULL test suite runs via CI / push-and-watch (not locally in WSL). Doc-comment edits +
   the whitespace-only blank-line deletion are compile-stripped / token-preserving and cannot
   regress runtime, but the gate stands: Failed:0 must hold — never ship on any failing test.
4. `git diff --stat` for the 8 files shows only insertions EXCEPT ScryfallDtos.cs which also shows
   one deleted line (the line-39 blank); manual `git diff` scan confirms every other changed hunk is
   an added doc-comment line (no deletions of code, no get-init mutation, no attribute inlining, no
   raw-string re-indent), and the ScryfallCard summary text is byte-identical.
5. `grep -c NoWarn DeckFlow.Web/DeckFlow.Web.csproj` unchanged (csproj NOT touched by this plan).
</verification>

<success_criteria>
- 12 public Services types documented PER TYPE: 4 interfaces (IEdhTop16Client, ICategoryKnowledgeStore,
  IFeedbackStore, IScryfallSetService) with full summaries, 4 impls (EdhTop16Client,
  CategoryKnowledgeStore, FeedbackStore, ScryfallSetService) with type-level inheritdoc, 3 records
  (ScryfallCardFace + FeedbackRequestContext with new type-level summaries, ScryfallCard with its
  existing summary RE-ATTACHED via the line-39 blank deletion), and 1 standalone static class
  (DeckFlowDatabaseConnectionFactory) with a type-level summary (D-01a).
- AUTOMATED PER-TYPE GATE (awk script in <verification>) exits 0: every public type declaration in
  all 8 files has an ATTACHED `///` `<summary>`/`<inheritdoc/>` immediately above it, no blank-line
  detachment (ScryfallCard included); a summary located elsewhere in a file does NOT count (the
  DeckFlowDatabaseConnectionFactory + ScryfallCard misses are the canonical counterexamples). The
  legacy file-level `grep -L '<summary>'` smoke check also passes.
- Build 0/0 Release via WSL dotnet path; test suite Failed:0 preserved (no runtime change; full
  suite via CI/push-watch per WSL/VSTest constraint).
- DeckFlow.Web.csproj NoWarn untouched.
- All diffs comment-only (R-6 touch-only discipline; get-init + JsonPropertyName attributes
  preserved) EXCEPT the single authorized whitespace-only blank-line deletion at ScryfallDtos.cs:39.
</success_criteria>

<output>
Create `.planning/phases/17-doc-comment-backfill-part-1-controllers-services/17-02-SUMMARY.md` when done.
</output>
