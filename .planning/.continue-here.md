---
context: plan
phase: SEO ladder P0-P3 (.planning/plans/2026-08-04-seo-ladder.md)
task: 10
total_tasks: 10
status: complete-pending-push
branch: feat/seo-ladder
worktree: /mnt/c/users/chrislunt/source/personal/deckflow-seo-ladder
last_updated: 2026-08-05T19:20:00.000Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

- [ ] CONSTRAINT: One agent owns one branch — `feat/seo-ladder` is this thread's branch. Do not touch `gsd/cycle21-cut-lab` / the `deckflow-role-floors` worktree from here.
- [ ] CONSTRAINT: Review findings are written to a file the moment they exist. Findings 2-4 of the earlier review were held in conversation only and were destroyed by a context clear. This branch now writes them to `.planning/reviews/`.

<current_state>
**The ladder is complete: 10/10.** Rebased onto `main` (15 commits, zero conflicts), then three more commits landed.

Local state: build **0 errors / 0 new warnings** (baseline is 9 pre-existing CS8629 in `DeckFlow.Core.Tests`), `DeckFlow.Web.Tests` **2308 passed / 0 failed / 16 skipped**, changed-lines format gate exit 0.

**The branch has NOT been pushed.** It is 20 commits ahead of `origin/feat/seo-ladder` and 15 behind, because the rebase rewrote history — the push needs `--force-with-lease`, which is the user's call.
</current_state>

<completed_work>
Waves P0-P2 (T-1..T-9) were done in the prior session — see git log. Added this session:

- Rebased onto `main` (picks up `5ac0e741`, the stale `is-disabled` fix that had CI red).
- **Finding 1 fixed** — `4171f61a`. `[FeatureFlagGate("tool.help.enabled")]` added to `HelpController.Topic`; with the flag off, `/help` 404'd but `/help/{slug}` stayed live for any topic with no per-topic `RequiresFlag`. TDD: test red before the fix, green after.
- **T-10 shipped** — `d6d00b16`. `/set-upgrade-analysis` landing page (~750 words), controller + view + tests, registered in `SeoPaths` and in deck-analysis's `AdditionalRoutes`. README's three stale `/extension-install.html` references now point at `/deckflow-bridge`.
- **Simplify pass** — `e30e5048`. Sibling-gate rule and orphan-gate-key rule in `ToolRouteGateCoverageTests`; removed tests that could not fail.
</completed_work>

<verification>
- **UI review, two viewports — DONE.** Headless Playwright at 1280x900 and 390x844 over `/`, `/set-upgrade-analysis`, `/deckflow-bridge`, `/deck-analysis`, `/manabase`. 10/10 pass: exactly one `<h1>`, no horizontal overflow, valid JSON-LD, unique meta descriptions, correct canonicals. Test server stopped, port 5173 confirmed clear.
- **Guards proven by mutation, not assumption.** Removing the gate from `SetUpgradeAnalysisController.Index` fails `ToolRouteGateCoverageTests`; removing it from `HelpController.Topic` fails the new sibling rule. Both restored after testing.
- **Branch review — clean, and persisted** to `.planning/reviews/2026-08-05-seo-ladder-review-cavecrew.md`. Verified the four `SeoPaths` consumers cannot drift, all indexable paths are flag-joined, cross-tool links check the right flag keys, and the `LowercaseUrls` change has no other unsafe comparators.
</verification>

<blockers>
- **OWED: the independent Codex review gate.** Codex is out of credits until **2026-08-10 09:48**. The clean local review above is advisory and does not discharge it. Findings 2-4 were not recovered — the review failed to reproduce them, which is not the same as clearing them. Per the earlier analysis at most one finding is genuinely unaccounted for, on the P2 commits.
- **OWED: push.** Needs `--force-with-lease` after the rebase. AI commits, user pushes.
</blockers>

<deviations_from_plan>
The plan's Tests section said to add `/bracket`, `/cut-lab`, `/deck-history` and `/content-kb` to the e2e smoke route list. **Three of those seed flag-OFF** in `FeatureFlagStore` (`tool.bracket.enabled`, `tool.cut-lab.enabled`, `tool.deck-history.enabled`), so an unconditional `GET` in `smoke.spec.ts` would 404 — following the plan literally would have added three permanently-red tests. Each already has its own spec that toggles its flag through the admin console. Only the two ungated routes were added (`/deckflow-bridge`, `/set-upgrade-analysis`), with a comment recording why the others are excluded.
</deviations_from_plan>

<followups>
- `PageMetadataViewTests` could gain a `[Theory]` asserting exactly one `<h1>` across all 21 indexable views; today that guard exists only on the newest page. Not done — it risks surfacing failures on existing views, which is a separate scope.
- The "get action methods with `HttpMethodAttribute`" reflection walk is hand-rolled in three test files. A shared helper is warranted; it is pre-existing debt, not introduced here.
- `ToolDefinition` conflates capability (`Route`, `AdditionalRoutes`, `FlagKey`) with presentation (`Tab`, `TileTitle`, `IsPrimaryTile`), which is why Help cannot cheaply join `ToolRegistry`. Worth extracting a `GatedRoute` list when a **second** non-tool gated route appears — one special case is not a pattern.
</followups>

<next_action>
Push with `--force-with-lease`, then watch CI. After 2026-08-10, run the owed Codex review over `main..HEAD` — `codex review --base main -c model="gpt-5.6-sol"` first, and redirect its output to a file, since `codex review` does not take `-o`.
</next_action>
