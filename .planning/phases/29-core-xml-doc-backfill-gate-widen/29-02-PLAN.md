---
phase: 29-core-xml-doc-backfill-gate-widen
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - DeckFlow.Core/Reporting/ReconciliationReporter.cs
  - DeckFlow.Core/Reporting/CategoryCardReporter.cs
  - DeckFlow.Core/Reporting/CategoryCountReporter.cs
  - DeckFlow.Core/Reporting/CategorySuggestionReporter.cs
  - DeckFlow.Core/Reporting/CategoryFilter.cs
  - DeckFlow.Core/Filtering/DeckEntryFilter.cs
autonomous: true
requirements: [HSK-01]

must_haves:
  truths:
    - "Probe build over DeckFlow.Core/Reporting/*.cs and DeckFlow.Core/Filtering/*.cs reports 0 CS1591/CS1573/CS1587 warnings"
    - "Every public type and member in the 6 Reporting+Filtering files has an XML doc comment"
    - "No raw-string instruction-text content or indentation changed in ReconciliationReporter.cs"
  artifacts:
    - path: "DeckFlow.Core/Reporting/ReconciliationReporter.cs"
      provides: "Documented reconciliation reporter (8 sites incl. raw-string constants)"
      contains: "/// <summary>"
    - path: "DeckFlow.Core/Reporting/CategoryCardReporter.cs"
      provides: "Documented category-card reporter (2 sites)"
      contains: "/// <summary>"
    - path: "DeckFlow.Core/Filtering/DeckEntryFilter.cs"
      provides: "Documented deck-entry filter (1 site)"
      contains: "/// <summary>"
  key_links:
    - from: "DeckFlow.Core/Reporting/ReconciliationReporter.cs"
      to: "DeckFlow.Core/Models/DeckDiff (existing)"
      via: "documented method params referencing DeckDiff"
      pattern: "paramref"
---

<objective>
Add XML doc comments to every undocumented public type and member in the `DeckFlow.Core/Reporting/` (5 files, ~15 sites) and `DeckFlow.Core/Filtering/` (1 file, 1 site) folders. ReconciliationReporter alone holds 8 sites including two raw-string instruction constants (MEDIUM risk).

Purpose: Reporting+Filtering is a contiguous, non-overlapping slice of the HSK-01 Core backfill. Documenting it cleanly is a prerequisite for the Wave-2 gate widen.
Output: 6 fully-documented source files; probe build over Reporting and Filtering reports 0 doc warnings.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<scope_fence>
ALLOWED FILE SET (Codex MUST NOT touch any source file outside this list):
- DeckFlow.Core/Reporting/ReconciliationReporter.cs
- DeckFlow.Core/Reporting/CategoryCardReporter.cs
- DeckFlow.Core/Reporting/CategoryCountReporter.cs
- DeckFlow.Core/Reporting/CategorySuggestionReporter.cs
- DeckFlow.Core/Reporting/CategoryFilter.cs
- DeckFlow.Core/Filtering/DeckEntryFilter.cs

ALLOWED ARTIFACT (workflow output, exempt from the source-edit fence):
- .planning/phases/29-core-xml-doc-backfill-gate-widen/29-02-SUMMARY.md

PARALLEL-EXECUTION REALITY: GSD execute-phase runs all four Wave-1 plans (29-01..29-04) as parallel subagents in ONE shared worktree. Your three sibling plans will be editing their own Core folders concurrently. Design for that:
- RELAXED CLEAN-TREE PRECONDITION: before starting, run `git status --short`. Modifications confined to the Wave-1 sibling allowed source sets (`DeckFlow.Core/Storage/`, `DeckFlow.Core/Knowledge/`, `DeckFlow.Core/Integration/`, `DeckFlow.Core/Exporting/`, `DeckFlow.Core/Parsing/`, `DeckFlow.Core/Models/`, `DeckFlow.Core/Normalization/`, `DeckFlow.Core/Diffing/`) and `.planning/phases/29-core-xml-doc-backfill-gate-widen/29-0*-SUMMARY.md` are EXPECTED — ignore them. STOP and report ONLY if a modified/untracked file appears outside BOTH this plan's allowed set AND every Wave-1 sibling allowed set (e.g., `DeckFlow.Web/`, `DeckFlow.CLI/`, repo-root files). Do NOT touch any file owned by a sibling plan.

PROBE-FILE LOCK (`DeckFlow.Core/.editorconfig` is an EXCLUSIVE lock SHARED by all four Wave-1 plans, guarded by an ATOMIC sidecar lock dir):
- The probe file MUST be named exactly `DeckFlow.Core/.editorconfig` — only that name overrides Roslyn severity at the `DeckFlow.Core/` directory, so per-plan unique filenames do NOT work; collision is handled by mutual exclusion, not renaming.
- ATOMIC ACQUIRE (a `[ -e ]`-then-`printf` test is NOT atomic — two parallel agents can both observe "absent" and both clobber the probe). Use `mkdir DeckFlow.Core/.editorconfig.probe.lock` as the mutex: `mkdir` is atomic and succeeds for exactly one contender. Poll every 3s, give up after 120s (40 attempts). On timeout, treat the lock as stale ONLY if `DeckFlow.Core/.editorconfig` is absent OR older than ~5 minutes (`find DeckFlow.Core/.editorconfig -mmin +5`); in that case remove the stale lock dir + stale probe file and retry the mkdir ONCE; otherwise STOP with a clear error.
- WRITE/REMOVE the `DeckFlow.Core/.editorconfig` probe file ONLY while holding the lock dir.
- TRAP CLEANUP: wrap the whole locked probe section with `trap 'rm -f DeckFlow.Core/.editorconfig; rmdir DeckFlow.Core/.editorconfig.probe.lock 2>/dev/null || true' EXIT INT TERM` so an interrupted/crashed build never leaves the probe file or lock dir behind to falsify a sibling's probe.
- HOLD MINIMALLY: acquire lock → write probe → `rm -rf DeckFlow.Core/obj DeckFlow.Core/bin` → build (capture exit code) → capture/dedup warnings → IMMEDIATELY remove probe file → `rmdir` the lock dir → clear the trap (`trap - EXIT INT TERM`). Never hold the lock across a doc-editing step.
- RELEASE: confirm `DeckFlow.Core/.editorconfig` AND `DeckFlow.Core/.editorconfig.probe.lock` are gone; neither may be committed nor appear in `git status` at plan end.
Do NOT touch `.editorconfig` at repo root. Do NOT touch any other Core folder's source. Ignore tasks belonging to plans 29-01, 29-03, 29-04, 29-05.
</scope_fence>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-core-xml-doc-backfill-gate-widen/29-RESEARCH.md
@.planning/phases/29-core-xml-doc-backfill-gate-widen/29-PATTERNS.md

# Gold-standard doc-style exemplars (READ - match this style exactly):
@DeckFlow.Core/Content/IContentVideoStore.cs
@DeckFlow.Core/Content/ContentVideoStore.cs
</context>

<constraints>
From CLAUDE.md + 29-RESEARCH.md (full authority - every constraint is blocking):
- Touch ONLY the `///` doc lines you add. Do NOT run Format Document / Code Cleanup.
- NEVER convert `{ get; init; }` to `{ get; }`. None of these files contain init accessors, but the rule stands.
- NEVER re-indent C# raw-string literals (triple-quote). ReconciliationReporter.cs has 4 raw-string instruction blocks - doc `///` lines go ABOVE the `public const string` declaration, never inside a raw-string block.
- NEVER inline an attribute onto a declaration line.
- Preserve LF line endings.
- Build env: `"/mnt/c/Program Files/dotnet/dotnet.exe"`. Clean build is the gate.
- Every authoritative probe build MUST first remove `DeckFlow.Core/obj` and `DeckFlow.Core/bin` so the inventory is trustworthy (stale obj/ hides warnings).
- Commits: plain default-author; one logical change per commit.
</constraints>

<tasks>

<task type="auto">
  <name>Task 1: Document ReconciliationReporter (raw-string danger zone)</name>
  <files>DeckFlow.Core/Reporting/ReconciliationReporter.cs</files>
  <read_first>
    - DeckFlow.Core/Reporting/ReconciliationReporter.cs (current state - 8 sites; note raw-string constants CategoryFixInstructions, MoxfieldImportInstructions)
    - .planning/phases/29-core-xml-doc-backfill-gate-widen/29-PATTERNS.md (Pattern 9 ReconciliationReporter raw-string-safe doc adds)
    - .planning/phases/29-core-xml-doc-backfill-gate-widen/29-RESEARCH.md (Probe build methodology; Risk Files table - RESEARCH per-file counts are the expected starting point; the Task-2 probe is authoritative)
    - DeckFlow.Core/Content/ContentVideoStore.cs (method doc style)
  </read_first>
  <action>
    Document `ReconciliationReporter.cs`: add a single-line `<summary>` ABOVE each `public const string` instruction constant (per 29-PATTERNS.md Pattern 9 exact summaries) and `<summary>`/`<param>`/`<returns>` on `WriteReport` and `ToText` using `<paramref name="diff"/>` and `<paramref name="outputPath"/>`. Critical: every `///` line goes above the declaration keyword line; do NOT add a `///` line inside or between the triple-quote delimiters, and do not re-indent the raw-string instruction content. (Defer the authoritative probe to Task 2 to minimize probe-lock contention with sibling plans.)
  </action>
  <verify>
    <automated>grep -c "/// <summary>" DeckFlow.Core/Reporting/ReconciliationReporter.cs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "/// <summary>" DeckFlow.Core/Reporting/ReconciliationReporter.cs` is at least 4 (2 constants + 2 methods, plus any type-level doc).
    - `git diff DeckFlow.Core/Reporting/ReconciliationReporter.cs` shows no change to any line containing a triple-quote delimiter or to lines between delimiters.
  </acceptance_criteria>
  <done>ReconciliationReporter fully documented with raw-string instruction blocks untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Document the four remaining Reporting/Filtering files and probe-verify clean</name>
  <files>DeckFlow.Core/Reporting/CategoryCardReporter.cs, DeckFlow.Core/Reporting/CategoryCountReporter.cs, DeckFlow.Core/Reporting/CategorySuggestionReporter.cs, DeckFlow.Core/Reporting/CategoryFilter.cs, DeckFlow.Core/Filtering/DeckEntryFilter.cs</files>
  <read_first>
    - DeckFlow.Core/Reporting/CategoryCardReporter.cs (current state - 2 sites)
    - DeckFlow.Core/Reporting/CategoryCountReporter.cs (current state - 2 sites)
    - DeckFlow.Core/Reporting/CategorySuggestionReporter.cs (current state - 2 sites)
    - DeckFlow.Core/Reporting/CategoryFilter.cs (current state - 1 site)
    - DeckFlow.Core/Filtering/DeckEntryFilter.cs (current state - 1 site)
    - .planning/phases/29-core-xml-doc-backfill-gate-widen/29-PATTERNS.md (Pattern 1 method docs; one-line form; shared patterns)
    - DeckFlow.Core/Content/IContentSourceStore.cs (utility doc style analog)
    - .planning/phases/29-core-xml-doc-backfill-gate-widen/29-RESEARCH.md (Probe build methodology - temp DeckFlow.Core/.editorconfig override + clean obj/ + sort -u dedup)
  </read_first>
  <action>
    Document each of the 5 files. For each undocumented type add a `<summary>`; for each undocumented public method add `<summary>` plus `<param>` for every parameter (in declaration order) and `<returns>` only when a value is returned, with `<see langword="null"/>` phrasing for nullable returns and `<see cref="..."/>` for cross-type references. Use the single-line `/// <summary>text.</summary>` form for concise members and the multi-line form for longer descriptions, matching the IContentSourceStore.cs analog. Then run the AUTHORITATIVE Reporting+Filtering probe via the verify snippet: it ATOMICALLY acquires the `DeckFlow.Core/.editorconfig` lock (mkdir sidecar lock dir, 120s cap, stale recovery), installs a `trap` so an interrupt cannot leave the probe/lock behind, drops obj/bin, builds, EXPLICITLY checks the dotnet build exit code (fail before interpreting warnings if the build itself failed), filters deduped warnings to `Reporting/` and `Filtering/` only (a sibling's incomplete folder must never fail this plan), then releases the lock and clears the trap. Confirm ZERO CS1591/CS1573/CS1587 warnings for every file under those two folders.
  </action>
  <verify>
    <automated>DOTNET="/mnt/c/Program Files/dotnet/dotnet.exe"; LOCK=DeckFlow.Core/.editorconfig.probe.lock; PROBE=DeckFlow.Core/.editorconfig; trap 'rm -f "$PROBE"; rmdir "$LOCK" 2>/dev/null || true' EXIT INT TERM; n=0; until mkdir "$LOCK" 2>/dev/null; do n=$((n+1)); if [ "$n" -ge 40 ]; then if [ ! -e "$PROBE" ] || [ -n "$(find "$PROBE" -mmin +5 2>/dev/null)" ]; then echo "stale lock - recovering"; rm -f "$PROBE"; rmdir "$LOCK" 2>/dev/null || true; mkdir "$LOCK" 2>/dev/null && break; fi; echo "FAIL could not acquire probe lock after 120s"; exit 1; fi; sleep 3; done; printf '%s\n' 'root = false' '' '[*.cs]' 'dotnet_diagnostic.CS1591.severity = warning' 'dotnet_diagnostic.CS1573.severity = warning' 'dotnet_diagnostic.CS1587.severity = warning' > "$PROBE"; rm -rf DeckFlow.Core/obj DeckFlow.Core/bin; "$DOTNET" build DeckFlow.Core/DeckFlow.Core.csproj --no-incremental 2>&1 | tee /tmp/29-02-build.log >/dev/null; BUILD_RC=${PIPESTATUS[0]}; rm -f "$PROBE"; rmdir "$LOCK" 2>/dev/null || true; trap - EXIT INT TERM; if [ "$BUILD_RC" -ne 0 ]; then echo "FAIL Core probe build errored (rc=$BUILD_RC) before warning analysis:"; grep -E "error CS" /tmp/29-02-build.log | head; exit 1; fi; grep -E "warning CS(1591|1573|1587)" /tmp/29-02-build.log | grep -E "Reporting/|Filtering/" | sort -u > /tmp/29-02.log; if [ -s /tmp/29-02.log ]; then echo FAIL; cat /tmp/29-02.log; else echo "OK 0 Reporting/Filtering doc warnings"; fi</automated>
  </verify>
  <acceptance_criteria>
    - Probe (atomic-locked temp editorconfig, clean obj/, build-exit-code checked) yields 0 CS1591/CS1573/CS1587 warnings for any `DeckFlow.Core/Reporting/` or `DeckFlow.Core/Filtering/` file (`/tmp/29-02.log` empty) and a passing dotnet build exit code.
    - `DeckFlow.Core/.editorconfig` and `DeckFlow.Core/.editorconfig.probe.lock` removed; absent from `git status`.
    - `git diff --stat DeckFlow.Core/Reporting/ DeckFlow.Core/Filtering/` lists exactly and only the 6 allowed files (path-scoped so concurrent sibling edits do not appear).
    - `"/mnt/c/Program Files/dotnet/dotnet.exe" build DeckFlow.Core/DeckFlow.Core.csproj -c Release` reports 0 errors.
  </acceptance_criteria>
  <done>All 6 Reporting/Filtering files documented; folder-scoped probe shows 0 doc warnings; temp probe file and lock dir removed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer edit -> source tree | `///` insertions near raw-string instruction blocks risk literal mutation |
| temp probe file -> repo | A lingering `DeckFlow.Core/.editorconfig` would alter solution-wide warning severity |
| concurrent Wave-1 subagents -> shared worktree | Two plans creating/deleting `DeckFlow.Core/.editorconfig` simultaneously corrupts the other's probe |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-02-01 | Tampering | Raw-string instruction constants in ReconciliationReporter.cs | mitigate | `///` lines added above the const declaration only; git diff acceptance asserts no triple-quote line changed |
| T-29-02-02 | Tampering | Temp probe `DeckFlow.Core/.editorconfig` | mitigate | Atomic mkdir-lock acquire/release + EXIT/INT/TERM trap; probe file removed and absence asserted; never committed |
| T-29-02-03 | Tampering | Concurrent sibling probe collision | mitigate | Atomic mkdir sidecar lock guarantees single writer; 120s cap with stale recovery; folder-scoped warning filter ignores sibling progress |
| T-29-02-04 | Denial of Service | Build pipeline | accept | Doc comments are compile-stripped; zero runtime/behavior change possible |
| T-29-02-SC | Tampering | npm/pip/cargo installs | n/a | No package installs in this phase |
</threat_model>

<verification>
- Probe build (atomic-locked temp editorconfig, clean obj/, build-exit-code checked) reports 0 doc warnings for `DeckFlow.Core/Reporting/` and `DeckFlow.Core/Filtering/`.
- `git status --short` shows no `DeckFlow.Core/.editorconfig` nor `.editorconfig.probe.lock`.
- `git diff --stat DeckFlow.Core/Reporting/ DeckFlow.Core/Filtering/` lists only the 6 allowed files.
- Full Core build clean (0 errors) under the still-active global suppressor.
</verification>

<success_criteria>
All ~16 undocumented public sites across the 6 Reporting+Filtering files carry XML doc comments matching the Content KB exemplar style; folder-scoped probe build is warning-free; ReconciliationReporter raw-string instruction blocks unchanged; only allowed files changed.
</success_criteria>

<output>
Create `.planning/phases/29-core-xml-doc-backfill-gate-widen/29-02-SUMMARY.md` when done.
</output>
