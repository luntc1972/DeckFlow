---
phase: 58-dogfood
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/58-dogfood/58-DOGFOOD-RESULTS.md
  - .planning/phases/58-dogfood/58-VERIFICATION-QUERIES.md
autonomous: true
requirements: [DOGFOOD-01]

must_haves:
  truths:
    - "An evidence artifact scaffold exists for the operator to fill in during the live run"
    - "A reusable set of read-only verification queries exists for confirming publish-state surfacing and no-regression"
    - "A named pre-Cycle-9 baseline KB entry is selected for the quality comparison"
  artifacts:
    - path: ".planning/phases/58-dogfood/58-DOGFOOD-RESULTS.md"
      provides: "Evidence document scaffold with all 4 success-criteria sections"
      contains: "Before/After Quality Comparison"
    - path: ".planning/phases/58-dogfood/58-VERIFICATION-QUERIES.md"
      provides: "Read-only SQL/checklist for publish-state + no-regression confirmation"
      contains: "pushed_to_prod_utc"
  key_links:
    - from: "58-DOGFOOD-RESULTS.md"
      to: "PublishState display strings"
      via: "exact status strings Published / Local-newer / Never published / Pushed-hidden"
      pattern: "Published|Local-newer|Never published|Pushed-hidden"
---

<objective>
Build the autonomous scaffolding the operator needs BEFORE the live dogfood run: the evidence document
template, the read-only verification queries, and a selected pre-Cycle-9 baseline entry for the
before/after quality comparison.

Purpose: The live run (Plan 02) is operator-driven, paid, and one-shot. Pre-staging the evidence
structure and the exact confirmation queries means the operator captures complete, checkable proof in
one pass instead of improvising mid-run.

Output: `58-DOGFOOD-RESULTS.md` (empty-but-structured) and `58-VERIFICATION-QUERIES.md` (ready-to-run
read-only queries + a named baseline entry).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@DeckFlow.Core/Content/PublishState.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Select the pre-Cycle-9 baseline entry + author the verification-queries artifact</name>
  <files>.planning/phases/58-dogfood/58-VERIFICATION-QUERIES.md</files>
  <read_first>
    - `content-kb/` directory listing and one full pre-Cycle-9 entry markdown file (e.g. one under
      `content-kb/the-command-zone/` or `content-kb/edhrecast/`) to pick a concrete named baseline whose
      summary/clips/tags were produced by the OLD distill prompt (pre-commit `00c3bc7`).
    - `content-kb/seed/index-seed.json` to confirm the seed row shape (`publishedUtc`, `artifactPath`,
      `archetypeTags`, etc.) and identify a baseline entry that is currently `Published` in prod.
    - `DeckFlow.Core/Content/PublishState.cs` for the exact four display strings.
    - `DeckFlow.Core/Content/PublishStateDeriver.cs` for the inputs the derivation reads
      (`pushed_to_prod_utc`, `is_visible`, local indexed/distill time).
  </read_first>
  <action>
    Create `58-VERIFICATION-QUERIES.md`. It must contain, as clearly labeled sections:
    (1) The chosen BASELINE entry — its exact `content-kb/<creator>/<id>.md` path plus a one-line note on
    why it is a fair comparison target (same creator/format family as the planned new video where
    possible, distilled under the pre-`00c3bc7` prompt). Name a primary and one alternate.
    (2) Read-only SQL against the local Studio Content-KB SQLite DB (under `MTG_DATA_DIR`) to read, for the
    new entry's row in `content_site_index`, identified by `natural_key_value` (the YouTube video id;
    `natural_key_type` is `youtube_channel`) and/or `video_url`: read `pushed_to_prod_utc`, `is_visible`,
    and `indexed_utc` (the local indexed/distill timestamp) — the three inputs `PublishStateDeriver`
    consumes. (There is NO `youtube_id` column — identity lives in `natural_key_value`.) State that these
    are SELECT-only and never run against prod-write connections.
    (3) The no-regression check: a query that lists every row currently derivable as `Published` BEFORE the
    run (capture as a baseline snapshot) so it can be re-run AFTER publish and diffed — the success
    condition is "no previously-`Published` row flipped to `Local-newer` or `Never published`".
    (4) The two-surface confirmation checklist: the Studio Review/Publish route (`/review`, `/publish`)
    and the web `/Admin/ContentKb` page both showing the exact string `Published` for the new entry —
    note that `/Admin/ContentKb` is gated by the `content.kb.enabled` feature flag and may need it ON to
    view. Reference the locked display strings from `PublishState.cs` verbatim.
    Do NOT invent column names — use the names verified by reading the deriver and seed contract. If a
    column name cannot be confirmed from the read files, mark it `[CONFIRM AT RUN]` rather than guessing.
  </action>
  <verify>
    <automated>test -f .planning/phases/58-dogfood/58-VERIFICATION-QUERIES.md &amp;&amp; grep -q "pushed_to_prod_utc" .planning/phases/58-dogfood/58-VERIFICATION-QUERIES.md &amp;&amp; grep -q "content.kb.enabled" .planning/phases/58-dogfood/58-VERIFICATION-QUERIES.md</automated>
  </verify>
  <acceptance_criteria>
    - File exists with the four labeled sections (baseline selection, publish-state inputs query,
      no-regression snapshot query, two-surface confirmation checklist).
    - A concrete `content-kb/<creator>/<id>.md` baseline path is named (primary + alternate).
    - All four publish-state strings appear verbatim: `Never published`, `Pushed-hidden`, `Published`,
      `Local-newer`.
    - Every query is explicitly SELECT-only; no write/DDL statements present.
  </acceptance_criteria>
  <done>Operator has a ready-to-run, read-only confirmation kit and a named baseline entry.</done>
</task>

<task type="auto">
  <name>Task 2: Author the DOGFOOD-RESULTS evidence-document scaffold</name>
  <files>.planning/phases/58-dogfood/58-DOGFOOD-RESULTS.md</files>
  <read_first>
    - The phase's four Success Criteria in `.planning/ROADMAP.md` (Phase 58 section).
    - `DeckFlow.Core/Content/LlmSpendLedger.cs` and `DeckFlow.Core/Content/SpendLedgerBase.cs` for the
      spend-cap env var name (`DECKFLOW_LLM_MONTHLY_CAP_USD`) and the $15 default, so the spend section
      records cap-vs-actual correctly.
    - `58-VERIFICATION-QUERIES.md` (produced in Task 1) so the results doc references the same baseline
      entry and the same queries.
  </read_first>
  <action>
    Create `58-DOGFOOD-RESULTS.md` as an evidence scaffold with one section per success criterion, each
    with empty `[FILL AT RUN]` slots and explicit checkable acceptance fields:
    (SC1) Chosen new video: URL/ID, creator, why chosen; the distilled new entry path; a Before/After
    Quality Comparison table (baseline path vs new path) with rows for Summary paste-readiness, Clip
    on-topic-ness, Tag accuracy, and the operator's higher-quality verdict (Yes/No + one-line rationale).
    (SC2) Review→Publish path used (git Publish via `/publish` Stage1/Stage2 commit, OR DirectPush via
    `/direct-push` Stage1/2/3) + post-publish proof that BOTH Studio and `/Admin/ContentKb` show the exact
    string `Published` for the new entry (screenshot paths or pasted status strings).
    (SC3) Spend: cap source (`DECKFLOW_LLM_MONTHLY_CAP_USD` or $15 default), month-to-date total before
    and after the run, this run's delta, and the within-cap assertion.
    (SC4) No-regression: the before/after `Published`-set diff result from the Task-1 snapshot query, with
    an explicit "0 unexpected flips to Local-newer / Never published" line.
    Note at the top that this is a DOGFOOD VALIDATION artifact (no product code shipped) and that the
    operator fills the `[FILL AT RUN]` slots during Plan 02. Reference the locked display strings.
  </action>
  <verify>
    <automated>test -f .planning/phases/58-dogfood/58-DOGFOOD-RESULTS.md &amp;&amp; grep -q "Before/After Quality Comparison" .planning/phases/58-dogfood/58-DOGFOOD-RESULTS.md &amp;&amp; grep -q "DECKFLOW_LLM_MONTHLY_CAP_USD" .planning/phases/58-dogfood/58-DOGFOOD-RESULTS.md</automated>
  </verify>
  <acceptance_criteria>
    - File exists with exactly four success-criteria sections, each containing `[FILL AT RUN]` slots.
    - The Before/After Quality Comparison table has rows for summary, clips, tags, and a verdict.
    - The spend section names `DECKFLOW_LLM_MONTHLY_CAP_USD` and records cap-vs-actual.
    - The no-regression section references the Task-1 snapshot query and an explicit zero-unexpected-flips
      assertion.
  </acceptance_criteria>
  <done>A structured, fill-in-the-blanks evidence document is staged for the live run.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| AI agent → DB | This plan is documentation-only; queries authored here must be read-only by construction |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-58-01 | Tampering | verification queries authored here | mitigate | All authored SQL is SELECT-only; the plan forbids any INSERT/UPDATE/DELETE/DDL and forbids running anything against a prod-write connection (AI never writes prod, per project rule). |
| T-58-02 | Information disclosure | secrets in evidence doc | mitigate | Scaffold instructs the operator to record cap/spend numbers and status strings only — no connection strings, prod credentials, or `Studio:ProdConnectionString` values are to be pasted into the committed artifact. |
</threat_model>

<verification>
- `dotnet build` is NOT required (no code touched). Both artifacts are markdown.
- No unit tests added — this is a documentation/validation plan with no test framework surface.
</verification>

<success_criteria>
- `58-VERIFICATION-QUERIES.md` and `58-DOGFOOD-RESULTS.md` both exist and are committed.
- Baseline entry named; all four publish-state strings present; spend env var present.
- All queries are read-only.
</success_criteria>

<output>
Create `.planning/phases/58-dogfood/58-01-SUMMARY.md` when done.
</output>
