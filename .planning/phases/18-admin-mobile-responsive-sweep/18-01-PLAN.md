---
phase: 18-admin-mobile-responsive-sweep
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - DeckFlow.Web/wwwroot/css/admin.css
  - DeckFlow.Web/wwwroot/css/admin-common.css
  - DeckFlow.Web/wwwroot/css/admin-mobile.css
  - DeckFlow.Web/wwwroot/css/site-common.css
autonomous: true
requirements: [AMOB-04, AMOB-03, AMOB-02, AMOB-01]
must_haves:
  truths:
    - "admin.css is a pure @import shim (no rules other than the two @import lines + header comment)"
    - "admin-common.css holds all admin layout primitives + components + admin-only global resets (no viewport @media)"
    - "admin-mobile.css holds all @media (max-width:768px) and the @media (min-width:769px) desktop-guard rules (no non-@media rules)"
    - "Dead admin-feedback-* / detail-* / type-badge rules are gone from site-common.css and live (token-corrected) in admin-common.css"
    - "All admin interactive elements have a min-height:44px touch-target floor selector defined"
    - "Sidebar collapses to <details>/<summary> below 768px and is forced-open above 768px via CSS only"
    - "Card-stack and overflow-x table CSS contracts exist for Plan 02 markup to consume"
  artifacts:
    - path: "DeckFlow.Web/wwwroot/css/admin.css"
      provides: "Import shim entry point loaded by _AdminLayout.cshtml"
      contains: "@import url('admin-common.css')"
    - path: "DeckFlow.Web/wwwroot/css/admin-common.css"
      provides: "Admin layout primitives, tokens, components, touch targets"
      contains: "--danger"
      min_lines: 200
    - path: "DeckFlow.Web/wwwroot/css/admin-mobile.css"
      provides: "All admin viewport @media responsive rules"
      contains: "@media (max-width: 768px)"
      min_lines: 60
    - path: "DeckFlow.Web/wwwroot/css/site-common.css"
      provides: "Public CSS with dead admin rules removed"
  key_links:
    - from: "DeckFlow.Web/Views/Shared/_AdminLayout.cshtml"
      to: "admin.css"
      via: "<link href=~/css/admin.css>"
      pattern: "css/admin\\.css"
    - from: "admin.css"
      to: "admin-common.css + admin-mobile.css"
      via: "@import"
      pattern: "@import url\\('admin-(common|mobile)\\.css'\\)"
---

<objective>
Factor the monolithic `admin.css` into the canonical three-file split — `admin-common.css`
(layout primitives + components + admin-only globals), `admin-mobile.css` (all viewport
`@media` rules), and `admin.css` as a pure `@import` shim — mirroring the existing
`site-common.css` / `site-mobile.css` / `site.css` relationship. In the same pass, establish
every CSS contract the Phase 18 mobile sweep needs: ≥44px touch targets on all admin
interactive elements, sidebar `<details>`/`<summary>` collapse below 768px, card-stack and
`overflow-x` table strategies, single-column forms, the `admin-harvest__panel` styling gap fix,
and migration of the dead `admin-feedback-*` rules out of `site-common.css` into
`admin-common.css` with admin-token substitutions.

Purpose: Establishes the CSS foundation (AMOB-04) and all selector contracts so Plan 02 can
wire Razor markup to them without re-deciding architecture. This is the interface-first plan —
it defines every class hook (`admin-table--card`, `admin-table-scroll`,
`admin-sidebar__disclosure`, `admin-sidebar__toggle`) that Plan 02's markup activates.

Output: 3 admin CSS files (1 modified to shim, 2 new) + `site-common.css` with dead rules removed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-admin-mobile-responsive-sweep/18-RESEARCH.md
@.planning/phases/18-admin-mobile-responsive-sweep/18-PATTERNS.md
@./CLAUDE.md

<constraints>
- Codex is the executor. CLAUDE.md is binding: NO Format Document / Code Cleanup. Touch only
  the lines that need touching. Preserve LF line endings. Plain default-author commit, NO
  Co-Authored-By trailer. Commit per logical change.
- Layout CSS goes in `*-common.css`, NEVER in `site.css`/`admin.css`. Per the AMOB-04 spec,
  `admin.css` becomes the import shim only.
- NO new packages. NO Bootstrap/Tailwind/Fluent. Plain CSS + `<details>`/`<summary>` only.
- Build verification is `dotnet build -c Release` (CSS is not compiled, but the build copies
  wwwroot content and must stay 0/0). VSTest is unreliable in WSL — do NOT rely on test run;
  CSS has no xUnit coverage.
- @import shim chosen over two `<link>` tags: AMOB-04 spec says "import shim"; admin pages are
  BasicAuth-gated (no public SLA so FOUC risk is negligible per RESEARCH Pitfall 5); and the
  shim requires ZERO change to `_AdminLayout.cshtml` line 14. The two-`<link>` variant is the
  documented fallback but is NOT used here.
- Token discipline (RESEARCH Pitfall 2): admin `:root` defines ONLY `--bg --panel --text
  --muted --accent --border --focus` plus the two NEW tokens `--danger --on-accent`. NEVER use
  public-only tokens (`--line --link --accent-strong --fs-sm --ink`) in admin CSS — substitute
  per the migration map below.
</constraints>

<interfaces>
<!-- The current admin.css :root block (admin.css lines 4-17) is the source of truth for tokens.
     Admin-defined (safe): --bg --panel --text --muted --accent --border --focus
     NEW this plan: --danger: #dc2626; --on-accent: #fff;
     The Phase 16 modal block (admin.css lines 195-307) already hardcodes #dc2626 at lines
     204+209 — the new --danger token replaces those literals so both stay consistent. -->

<!-- Token substitution map when migrating site-common.css lines 776-810 into admin-common.css: -->
<!--   --line          -> --border                                   -->
<!--   --link          -> --accent                                   -->
<!--   --accent-strong -> --accent  (was `var(--accent-strong, inherit)`) -->
<!--   --fs-sm         -> 0.85em                                     -->
<!--   --on-accent     -> --on-accent  (now defined this plan)        -->
<!--   --danger        -> --danger     (now defined this plan)        -->
<!--   --panel         -> --panel      (already in admin :root)        -->

<!-- Touch-target template (from Phase 16 admin-modal__button, admin.css lines 266-278): -->
<!--   min-height: 44px; min-width: 44px; display: inline-flex; align-items: center; -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create admin-common.css with migrated rules, tokens, touch targets, and gap fixes</name>
  <files>DeckFlow.Web/wwwroot/css/admin-common.css</files>
  <read_first>
    - DeckFlow.Web/wwwroot/css/admin.css (ENTIRE current file, lines 1-307 — the source of every block being moved: :root tokens, focus-visible block, global resets, .skip-link/.sr-only, .admin-shell grid, all .admin-sidebar*/.admin-topbar*/.admin-content/.admin-banner/.admin-table/.admin-action-form/.maintenance-page/.admin-page-header/.admin-analytics*/.admin-sparkline/.admin-empty, AND the Phase 16 modal block lines 195-307)
    - DeckFlow.Web/wwwroot/css/site-common.css lines 776-810 (dead admin-feedback-*/detail-*/type-badge rules to migrate — token-corrected)
    - DeckFlow.Web/wwwroot/css/site-common.css lines 1-12 (header-comment style to mirror)
    - .planning/phases/18-admin-mobile-responsive-sweep/18-PATTERNS.md (admin-common.css section, lines 27-253 — exact rule inventory and token map)
  </read_first>
  <action>
    Create `admin-common.css` containing ALL non-`@media` admin rules. (a) Move the `:root`
    block from `admin.css` lines 4-17 VERBATIM (preserve the WDG comment lines 11-14), then add
    two tokens inside that `:root`: `--danger: #dc2626;` and `--on-accent: #fff;`. (b) Move the
    focus-visible block, the `* { box-sizing }`/`html,body`/`a`/`a:hover` global resets, and
    `.skip-link`/`.sr-only` verbatim (admin.css lines 19-68), each prefixed with a
    `/* admin-only global */` comment marking them intentional unscoped resets. (c) Move the
    shell-layout block verbatim: `.admin-shell` grid, `.admin-sidebar*`, `.admin-topbar*`,
    `.admin-content`, `.admin-banner*`, `.admin-table*`, `.admin-action-form*`,
    `.maintenance-page*`, `.admin-page-header*`, `.admin-analytics*`, `.admin-analytics-table*`,
    `.admin-sparkline`, `.admin-empty` (admin.css lines 71-193). (d) Move the entire Phase 16
    modal block (admin.css lines 195-307) VERBATIM including its `.admin-shell`-scoped selectors
    and the `@media (prefers-reduced-motion: reduce)` block — that motion query is the ONLY
    permitted `@media` in admin-common.css because it is an a11y preference query, not a
    viewport breakpoint; replace the two hardcoded `#dc2626` literals (admin.css lines 204, 209)
    with `var(--danger)`. (e) Migrate the dead rules from site-common.css lines 776-810
    (`.admin-feedback`, `.admin-feedback-filters`, `.admin-feedback-filter`,
    `.admin-feedback-filter.active`, `.admin-feedback-table` + th/td, `.admin-feedback-pagination`,
    `.admin-feedback-empty`, `.admin-feedback-detail`, `.type-badge`, `.detail-grid`,
    `.detail-grid dt`, `.detail-message`, `.detail-actions`, `.detail-actions button.danger`)
    applying the token substitution map (`--line`→`--border`, `--link`→`--accent`,
    `var(--accent-strong, inherit)`→`var(--accent)`, `--fs-sm`→`0.85em`); do NOT migrate the
    duplicate `.admin-action-form { display: inline; }` from site-common.css:801 — that rule
    already exists from admin.css:136 and must not be duplicated. (f) Add the
    `admin-harvest__panel` styling gap fix (no current CSS): `.admin-harvest__panel` with
    `background: var(--panel); border: 1px solid var(--border); border-radius: 4px; padding:
    16px 20px; margin-bottom: 16px;` and `.admin-harvest__panel h2 { margin: 0 0 12px;
    font-size: 15px; font-weight: 600; }`. (g) Add the touch-target floor selectors (per Phase
    16 template): `.admin-shell .admin-action-form button`, `.admin-shell .admin-sidebar__link`,
    and `.admin-analytics .admin-range-selector a` each get `min-height: 44px; display:
    inline-flex; align-items: center;` (the action-form button and range-selector also need
    `justify-content: center` per PATTERNS). (h) Add the desktop sidebar-disclosure default
    `.admin-sidebar__toggle { display: none; }` and the scroll-wrapper base
    `.admin-table-scroll { overflow-x: auto; -webkit-overflow-scrolling: touch; }`. (i) Add a
    `.admin-analytics-table .spark { width: 130px; min-width: 80px; }` rule so the sparkline
    column does not collapse on narrow viewports (the existing `.spark { width: 130px }` from
    admin.css:182 already moved in step (c) — replace/augment it to include `min-width: 80px`).
    Open with a header comment mirroring site-common.css style: state that this file holds admin
    layout primitives + components, is loaded via @import from admin.css, and contains NO
    viewport @media rules (all responsive overrides live in admin-mobile.css). Use LF line
    endings, 2-space CSS indent matching existing admin.css style.
  </action>
  <verify>
    <automated>cd /mnt/c/users/chrislunt/source/personal/deckflow && f=DeckFlow.Web/wwwroot/css/admin-common.css; test -f "$f" && grep -q -- '--danger: #dc2626' "$f" && grep -q -- '--on-accent' "$f" && grep -q '\.admin-harvest__panel' "$f" && grep -q '\.admin-table-scroll' "$f" && grep -cE 'min-height: 44px' "$f" | awk '$1>=3{exit 0}{exit 1}' && grep -q '\.admin-feedback-table' "$f" && ! grep -qE 'var\(--line\)|var\(--link\)|var\(--accent-strong|var\(--fs-sm\)' "$f" && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - File `DeckFlow.Web/wwwroot/css/admin-common.css` exists.
    - `:root` contains `--danger: #dc2626;` AND `--on-accent: #fff;` plus all 7 original admin tokens.
    - Selectors `.admin-feedback-table`, `.type-badge`, `.detail-grid`, `.detail-actions`,
      `.detail-message`, `.admin-harvest__panel`, `.admin-table-scroll` all present.
    - At least 3 selectors carry `min-height: 44px` (action-form button, sidebar link, range-selector link).
    - ZERO occurrences of `var(--line)`, `var(--link)`, `var(--accent-strong`, `var(--fs-sm)` (token substitution complete).
    - The Phase 16 modal `#dc2626` literals are replaced with `var(--danger)` (grep `var(--danger)` returns the danger-button rules; raw `#dc2626` only remains inside the `:root` token definition).
    - The ONLY `@media` in the file is `@media (prefers-reduced-motion: reduce)` — no `max-width`/`min-width` queries (`grep -c 'max-width\|min-width' admin-common.css` accounts only for the spark `min-width: 80px` declaration, NOT a media query — verify no `@media (max-width` or `@media (min-width` string present).
    - `.admin-action-form { display: inline; }` appears exactly once (from the admin.css:136 move, not duplicated from site-common.css:801).
  </acceptance_criteria>
  <done>admin-common.css holds every non-viewport admin rule with admin tokens only; touch-target floors and gap fixes present; modal block moved verbatim with --danger substitution.</done>
</task>

<task type="auto">
  <name>Task 2: Create admin-mobile.css with all viewport @media responsive rules</name>
  <files>DeckFlow.Web/wwwroot/css/admin-mobile.css</files>
  <read_first>
    - DeckFlow.Web/wwwroot/css/site-mobile.css lines 1-30 (file-header comment + breakpoint structure to mirror)
    - DeckFlow.Web/wwwroot/css/admin-common.css (just created in Task 1 — confirm the selector names the @media rules will override: .admin-shell, .admin-sidebar, .admin-sidebar__toggle, .admin-sidebar__disclosure, .admin-sidebar__nav, .admin-sidebar__brand, .admin-action-form, .admin-harvest__panel, .admin-table-scroll, .admin-table--card)
    - .planning/phases/18-admin-mobile-responsive-sweep/18-PATTERNS.md (admin-mobile.css section, lines 256-408 — exact @media rule inventory)
    - .planning/phases/18-admin-mobile-responsive-sweep/18-RESEARCH.md Pitfall 1 (lines 598-617 — desktop forced-open guard)
  </read_first>
  <action>
    Create `admin-mobile.css` containing ALL viewport `@media` rules (NO non-@media rules
    except the file-header comment). Open with a header comment mirroring site-mobile.css:
    state it holds admin responsive overrides, is loaded via @import in admin.css AFTER
    admin-common.css, and uses 768px as the primary breakpoint. Add a single
    `@media (max-width: 768px)` block containing: (a) shell grid collapse —
    `.admin-shell { grid-template-columns: 1fr; grid-template-rows: auto auto 1fr;
    grid-template-areas: "topbar" "sidebar" "content"; }`. (b) sidebar mobile chrome —
    `.admin-sidebar { border-right: none; border-bottom: 1px solid var(--border); padding: 0; }`
    and the disclosure toggle `.admin-sidebar__toggle { display: flex; align-items: center;
    padding: 12px 20px; min-height: 44px; cursor: pointer; font-weight: 700; list-style: none;
    user-select: none; color: var(--text); }` plus the marker-hiding rules
    `.admin-sidebar__toggle::-webkit-details-marker { display: none; }` and
    `.admin-sidebar__toggle::marker { content: ""; }`, and the collapse rule
    `.admin-sidebar__disclosure:not([open]) .admin-sidebar__nav, .admin-sidebar__disclosure:not([open]) .admin-sidebar__brand { display: none; }`. (c) form single-column —
    `.admin-shell .admin-action-form { display: block; margin-bottom: 8px; }`,
    `.admin-shell .admin-action-form input, .admin-shell .admin-action-form select { width:
    100%; box-sizing: border-box; margin-bottom: 8px; }`, `.admin-shell .admin-action-form
    button { width: 100%; }`, and `.admin-shell .admin-harvest__panel { padding: 12px; }`.
    (d) overflow-x table tweak — `.admin-table-scroll { padding-bottom: 4px; }` (iOS scrollbar
    visibility). (e) card-stack pattern — `.admin-table--card thead { display: none; }`,
    `.admin-table--card tr { display: block; border: 1px solid var(--border); border-radius:
    4px; margin-bottom: 8px; padding: 8px 12px; }`, `.admin-table--card td { display: flex;
    justify-content: space-between; gap: 8px; padding: 4px 0; border: none; font-size: 0.9em; }`,
    `.admin-table--card td::before { content: attr(data-label); font-weight: 600; color:
    var(--muted); flex-shrink: 0; }`. Then add a SEPARATE `@media (min-width: 769px)` block (the
    Pitfall 1 desktop forced-open guard): `.admin-sidebar__toggle { display: none; }` and
    `.admin-sidebar__disclosure .admin-sidebar__nav, .admin-sidebar__disclosure
    .admin-sidebar__brand { display: block !important; }` — this `!important` is the documented
    exception that prevents a collapsed-on-mobile state from persisting after resize to desktop.
    Use only admin tokens (`--border --text --muted`). LF line endings, 2-space CSS indent.
  </action>
  <verify>
    <automated>cd /mnt/c/users/chrislunt/source/personal/deckflow && f=DeckFlow.Web/wwwroot/css/admin-mobile.css; test -f "$f" && grep -q '@media (max-width: 768px)' "$f" && grep -q '@media (min-width: 769px)' "$f" && grep -q 'grid-template-areas' "$f" && grep -q '\.admin-sidebar__disclosure:not(\[open\])' "$f" && grep -q 'attr(data-label)' "$f" && grep -q 'display: block !important' "$f" && ! grep -qE 'var\(--line\)|var\(--link\)|var\(--fs-sm\)' "$f" && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - File `DeckFlow.Web/wwwroot/css/admin-mobile.css` exists.
    - Contains exactly one `@media (max-width: 768px)` block AND one `@media (min-width: 769px)` block.
    - Mobile block includes the shell grid collapse (`grid-template-areas` with "topbar"/"sidebar"/"content" stacking), the `.admin-sidebar__toggle` display:flex rule with `min-height: 44px`, the `:not([open])` collapse selector, single-column `.admin-action-form` rules, `.admin-table-scroll` tweak, and the full `.admin-table--card` card-stack with `td::before { content: attr(data-label) }`.
    - Desktop guard block contains `.admin-sidebar__toggle { display: none; }` and the `display: block !important` forced-open for nav + brand.
    - ZERO non-@media top-level rules (every rule is inside one of the two `@media` blocks; only the header comment sits outside).
    - ZERO public-only tokens (`--line`, `--link`, `--fs-sm`).
  </acceptance_criteria>
  <done>admin-mobile.css holds all viewport responsive rules including sidebar collapse, card-stack, single-column forms, and the desktop forced-open guard; no non-@media rules.</done>
</task>

<task type="auto">
  <name>Task 3: Reduce admin.css to import shim and remove dead rules from site-common.css</name>
  <files>DeckFlow.Web/wwwroot/css/admin.css, DeckFlow.Web/wwwroot/css/site-common.css</files>
  <read_first>
    - DeckFlow.Web/wwwroot/css/admin.css (ENTIRE current file — being replaced wholesale by the 4-line shim; confirm every rule it contained was moved into admin-common.css in Task 1)
    - DeckFlow.Web/wwwroot/css/admin-common.css (Task 1 output — confirm parity before deleting source rules from admin.css)
    - DeckFlow.Web/wwwroot/css/site-common.css lines 770-812 (the exact dead-rule block 776-810 to delete, bounded by the `.feedback-honeypot` rule above and the `Card Lookup readability redesign` comment below)
    - DeckFlow.Web/Views/Shared/_AdminLayout.cshtml line 14 (the `<link href="~/css/admin.css">` tag — confirm it stays UNCHANGED; the shim is loaded transparently)
    - .planning/phases/18-admin-mobile-responsive-sweep/18-RESEARCH.md Pitfall 6 (lines 666-675 — why the dead site-common.css copies MUST be removed)
  </read_first>
  <action>
    Replace the ENTIRE contents of `admin.css` with the import shim only: a header comment
    block stating "DeckFlow Admin Shell — v1.4 Phase 18. Import shim only. Do not add rules
    here. Layout primitives and components -> admin-common.css. Responsive overrides (@media)
    -> admin-mobile.css" followed by exactly two lines: `@import url('admin-common.css');` and
    `@import url('admin-mobile.css');`. The order matters — admin-common first, admin-mobile
    second, so the `@media` overrides win the cascade. Then, in `site-common.css`, DELETE the
    dead admin block lines 776-810 (the `/* Admin feedback */` comment through the
    `.detail-actions button.danger` rule inclusive) — these rules are unreachable on admin
    pages (admin pages never load site-common.css) and now live token-corrected in
    admin-common.css; leaving them risks the duplicate `.admin-action-form { display: inline; }`
    (site-common.css:801) styling any future public-page form that uses that class (Pitfall 6).
    Touch ONLY those lines in site-common.css — do not reformat surrounding rules
    (`.feedback-honeypot` above and the `Card Lookup readability redesign` block below stay
    byte-identical). Do NOT touch `_AdminLayout.cshtml` line 14 — the `<link href="~/css/admin.css"
    asp-append-version="true">` tag is unchanged; it transparently pulls both files via @import.
    Preserve LF line endings everywhere.
  </action>
  <verify>
    <automated>cd /mnt/c/users/chrislunt/source/personal/deckflow && a=DeckFlow.Web/wwwroot/css/admin.css && grep -q "@import url('admin-common.css')" "$a" && grep -q "@import url('admin-mobile.css')" "$a" && [ "$(grep -vcE '^\s*(/\*|\*|\*/|@import|$)' "$a")" -eq 0 ] && ! grep -qE '^\s*\.admin-feedback' DeckFlow.Web/wwwroot/css/site-common.css && ! grep -qE '^\s*\.detail-grid' DeckFlow.Web/wwwroot/css/site-common.css && grep -q 'css/admin.css' DeckFlow.Web/Views/Shared/_AdminLayout.cshtml && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `admin.css` contains exactly the two `@import` lines (admin-common first, admin-mobile second) plus a header comment — no other CSS rules remain (no selectors, no `:root`, no `.admin-*` rules).
    - `site-common.css` no longer contains `.admin-feedback`, `.admin-feedback-table`, `.detail-grid`, `.type-badge`, `.detail-actions`, `.detail-message`, or the duplicate `.admin-action-form` block (lines 776-810 deleted).
    - `site-common.css` lines surrounding the deletion (`.feedback-honeypot` and the `Card Lookup readability redesign` comment) are byte-identical to before (no reformatting).
    - `_AdminLayout.cshtml` line 14 `<link href="~/css/admin.css">` is UNCHANGED.
    - `dotnet build -c Release` (run in Task 4 / verification) stays 0 warnings / 0 errors — CSS changes do not affect compilation but wwwroot must copy clean.
  </acceptance_criteria>
  <done>admin.css is a pure 2-import shim; site-common.css dead admin rules removed; _AdminLayout.cshtml untouched; no surrounding-line reformatting.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → Admin shell | All `/Admin/*` routes already gated by `BasicAuthMiddleware`; this plan adds NO new endpoints, inputs, or data flows. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-01 | Tampering | CSS file factoring | accept | Pure CSS restructure (no executable code, no user input rendered). No new XSS sink: no `data-*` value is reflected from user input in this plan; the `data-label` attributes added in Plan 02 are STATIC column headers authored in Razor, not user data. |
| T-18-02 | Information Disclosure | `@import` shim on BasicAuth-gated pages | accept | admin CSS files are static assets already served from the same origin; no secret or PII in CSS. Admin shell is BasicAuth-protected; CSS exposure adds no new disclosure surface. |
| T-18-03 | Tampering | npm/pip/cargo installs | mitigate | No packages installed this phase (CSS/Razor only). RESEARCH "Package Legitimacy Audit" = not applicable. No install tasks exist, so no legitimacy checkpoint required. |

**Note:** Phase 18 is CSS + Razor markup only. No server-side logic, no new endpoints, no auth
changes, no persistence. ASVS V2-V6 inapplicable per RESEARCH Security Domain section. The
threat model is intentionally minimal and stated explicitly rather than omitted.
</threat_model>

<verification>
- `dotnet build -c Release` from clean obj/: 0 warnings / 0 errors (wwwroot static assets copy clean).
- `admin.css` is the 2-import shim only (grep both @import lines; no other rules).
- `admin-common.css` exists with `--danger`/`--on-accent` tokens, ≥3 `min-height: 44px` selectors, migrated admin-feedback rules using admin tokens, `admin-harvest__panel`, `admin-table-scroll`; ONLY @media is prefers-reduced-motion.
- `admin-mobile.css` exists with one `max-width:768px` + one `min-width:769px` block; sidebar collapse, card-stack, single-column forms, desktop forced-open guard.
- `site-common.css` dead admin rules (776-810) removed; surrounding lines untouched.
- No public-only tokens (`--line --link --fs-sm --accent-strong`) anywhere in admin CSS.
</verification>

<success_criteria>
- AMOB-04 CSS-factoring half COMPLETE: admin.css → admin-common.css + admin-mobile.css + shim; all selectors `.admin-shell`-scoped or marked admin-only global; zero conceptual bleed (no public tokens, dead site-common rules removed).
- All CSS contracts that Plan 02 markup consumes are defined: `admin-sidebar__disclosure`/`__toggle`, `admin-table--card`, `admin-table-scroll`, single-column forms, ≥44px touch targets.
- Pre-existing admin-feedback CSS gap CLOSED (rules now reachable on admin pages via admin-common.css).
- `admin-harvest__panel` styling gap CLOSED.
</success_criteria>

<output>
Create `.planning/phases/18-admin-mobile-responsive-sweep/18-01-SUMMARY.md` when done.
</output>
