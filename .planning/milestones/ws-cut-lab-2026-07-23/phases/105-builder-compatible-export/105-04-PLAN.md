---
phase: 105-builder-compatible-export
plan: 04
type: execute
wave: 3
depends_on: [105-03]
files_modified:
  - DeckFlow.Web/Services/CutLab/CutLabExportService.cs
  - DeckFlow.Web/Controllers/CutLabController.cs
  - DeckFlow.Web/Program.cs
  - DeckFlow.Web/Models/CutLabViewModel.cs
  - DeckFlow.Web.Tests/CutLabExportServiceTests.cs
  - DeckFlow.Web.Tests/CutLabControllerTests.cs
  - DeckFlow.Web/Models/WorkflowStepTabsModel.cs
  - DeckFlow.Web/Views/Shared/_WorkflowStepTabs.cshtml
  - DeckFlow.Web/Views/Deck/CutLab.cshtml
  - DeckFlow.Web/wwwroot/ts/cut-lab.ts
  - DeckFlow.Web/wwwroot/css/site-common.css
  - README.md
autonomous: true
requirements: [EXPORT-01, EXPORT-02, EXPORT-03]
must_haves:
  truths:
    - "At exactly 100 cards the Export surface unlocks and shows Moxfield + Archidekt full-list text and a both-dialect CUT/ADD patch."
    - "Below 100 cards the Export step is disabled/greyed with a 'reach 100 to export' hint and the final-list copy is hard-blocked."
    - "Activating the Export step tab submits the Export form (server POST /cut-lab/export, not a client-side toggle) and re-renders the page with the Export panel + validation summary populated; below 100 the tab is a disabled submit control."
    - "A kept sideboard or maybeboard card appears in the finished 100-card main deck (as mainboard) in both dialects — none dropped."
    - "The always-visible validation summary shows count / color-identity / banlist; color and banlist failures name offending cards but do not disable export; illegal color-identity and merely-unverified cards are shown as distinct statuses."
  artifacts:
    - path: "DeckFlow.Web/Services/CutLab/CutLabExportService.cs"
      provides: "Thin Web orchestrator: reconstruct final entries from working list + OriginalEntries (retaining original board), gather color-identity + banlist inputs, call Core composer"
      exports: ["ICutLabExportService"]
    - path: "DeckFlow.Web.Tests/CutLabExportServiceTests.cs"
      provides: "Unit coverage: final-entry reconstruction + fallback warning, kept sideboard+maybeboard survive to mainboard 100 in both dialects, banlist re-check surfaced as non-blocking warning, banlist fail-open, no redundant Scryfall resolution"
    - path: "DeckFlow.Web.Tests/CutLabControllerTests.cs"
      provides: "Updated CreateController helper supplies a fake export service (existing tests compile) + a POST /cut-lab/export action test (state rehydration, export view-model attach, off-count POST returns panel not 4xx)"
    - path: "DeckFlow.Web/Controllers/CutLabController.cs"
      provides: "POST /cut-lab/export action mirroring Goals/Whatif"
      contains: "Export"
    - path: "DeckFlow.Web/Views/Deck/CutLab.cshtml"
      provides: "Export panel (4 copy blocks + validation summary) + Export step tab"
  key_links:
    - from: "CutLabController.Export"
      to: "ICutLabExportService"
      via: "injected service mirroring ICutLabWhatifPreviewService"
      pattern: "_exportService"
    - from: "CutLabExportService"
      to: "CutLabExportComposer.Compose"
      via: "reconstructed final entries + identity/banlist inputs"
      pattern: "CutLabExportComposer"
    - from: "CutLab Export step tab (_WorkflowStepTabs)"
      to: "POST /cut-lab/export"
      via: "submit button bound to the Export form (type=submit form=cut-lab-export-form)"
      pattern: "cut-lab-export-form"
    - from: "CutLab.cshtml export panel"
      to: "cut-lab.ts copy handler"
      via: "data-copy-target buttons"
      pattern: "data-copy-target"
---

<objective>
Wire the Core composer (105-03) into a user-facing Export surface. Add a thin `ICutLabExportService` (mirroring `ICutLabWhatifPreviewService`) that reconstructs the full-fidelity final 100-card `DeckEntry` list from the derived working list + the captured `OriginalEntries`, gathers color-identity + banlist inputs from already-resolved cards, and calls `CutLabExportComposer`. Add the `POST /cut-lab/export` controller action, view-model export fields, the Razor Export panel (four copy blocks + the always-on 3-check validation summary), the Export step tab (D1: Process→Decide→Goals→Export, gated to unlock at exactly 100 cards), copy-to-clipboard wiring, and layout CSS.

Purpose: Deliver EXPORT-01/02/03 to the user through Cut Lab's existing single-view, action-per-step pattern — the whole Cut Lab loop ends here (paste straight back into the builder, zero reformatting).
Output: Working, gated Export surface producing both-dialect final list + CUT/ADD patch + validation summary.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/cut-lab/phases/105-builder-compatible-export/105-CONTEXT.md
@.planning/workstreams/cut-lab/phases/105-builder-compatible-export/105-RESEARCH.md
@DeckFlow.Web/Controllers/CutLabController.cs
@DeckFlow.Web/Services/CutLab/CutLabWhatifPreviewService.cs
@DeckFlow.Web/Models/WorkflowStepTabsModel.cs
@DeckFlow.Web/Views/Shared/_WorkflowStepTabs.cshtml

<interfaces>
<!-- Mirror for the new export service (dedicated injected service): -->
<!--   ICutLabWhatifPreviewService, injected at CutLabController.cs:14, ctor :18-28, used :198 -->
<!--   CutLabWhatifPreviewService deps: ICutLabSimulationService, ICutLabAnalysisContextBuilder, CutLabResolvedCardCache -->

<!-- Existing controller test helper that MUST be updated for the new ctor dep (HIGH 2): -->
<!--   CutLabControllerTests.CreateController(service, whatifPreviewService?) at CutLabControllerTests.cs:593-600 -->
<!--   builds: new CutLabController(service, whatifPreviewService ?? new FakeWhatifPreviewService(), new FakeLogger<...>) -->
<!--   adding ICutLabExportService to the ctor breaks EVERY controller test unless this helper supplies a fake. -->

<!-- Resolve color identities for the working list (post-105-02 ResolvedCards carry ColorIdentity): -->
<!--   CutLabAnalysisContext(... double CommanderManaValue, ... IReadOnlyList<ScryfallCardData> ResolvedCards)  CutLabAnalysisContextBuilder.cs:49-56 -->
<!--   await _contextBuilder.BuildAsync(workingList, playExperience, commanderNames, preResolvedCards:null, poolKey:null, ct) -->

<!-- Derive the working (kept) list + cut pile from immutable Pool: -->
<!--   CutLabWorkingList.Derive(state.Pool, state.Decisions)  (used at CutLabController.cs:288) -->

<!-- Banlist re-check against the FINAL working list (new call site, same method): -->
<!--   ICommanderBanListService.GetBannedCardsAsync(ct)  CommanderBanListService.cs:18-19 -->
<!--   consumer pattern CutLabPageService.cs:717-736; fail-open on HttpRequestException CutLabPageService.cs:255-260 -->

<!-- Count gate for the enable-at-100 UI (commander + 99 = 100): -->
<!--   CutLabRoundPlan.CardsRemainingToTarget == 0  (CutLabCutRoundEngine.cs:49); surfaced on view model StickyBar CutLabViewModel.cs:426 -->

<!-- Controller action to mirror (attributes + shape): CutLabController.Goals CutLabController.cs:120-165 -->
<!--   [HttpPost("/cut-lab/export")] [FeatureFlagGate("tool.cut-lab.enabled")] [ValidateAntiForgeryToken] [RequestSizeLimit(2*1024*1024)] -->

<!-- View model entry point: CutLabViewModel.From(...) CutLabViewModel.cs:134 -->

<!-- Step-tab strip construction pattern: DeckComparison.cshtml:214-227 + Html.PartialAsync("_WorkflowStepTabs", model) -->
<!-- NOTE: Cut Lab currently renders _DeckToolTabs (CutLab.cshtml:76), NOT _WorkflowStepTabs. D1 requires introducing the -->
<!--       Process→Decide→Goals→Export step strip on the Cut Lab page and gating the Export step. -->
<!-- Step-tab model today (WorkflowStepTabsModel.cs:7): record WorkflowStepTab(int Step, string Label, bool IsComplete) -->
<!--   partial (_WorkflowStepTabs.cshtml:9-24) picks currentStep = first !IsComplete step; renders is-complete class only -->

<!-- Copy-to-clipboard handler to mirror: content-kb.ts:125-136 (button[data-copy-target] → navigator.clipboard.writeText(target.value)) -->
<!-- Copy button markup to mirror: DeckPrimer.cshtml:353 (<button class="copy-button" data-copy-target="...">Copy</button>) -->
<!-- Reconstruct final DeckEntry: NormalizedName via CardNormalizer.Normalize (CardNormalizer.cs:15) -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: CutLabExportService + Export controller action + view-model fields (+ unit tests)</name>
  <files>DeckFlow.Web/Services/CutLab/CutLabExportService.cs, DeckFlow.Web/Controllers/CutLabController.cs, DeckFlow.Web/Program.cs, DeckFlow.Web/Models/CutLabViewModel.cs, DeckFlow.Web.Tests/CutLabExportServiceTests.cs, DeckFlow.Web.Tests/CutLabControllerTests.cs</files>
  <behavior>
    - Final-entry reconstruction: each kept card is rebuilt from `state.OriginalEntries` by normalized name, RETAINING the original Board/SetCode/CollectorNumber/Category (including `sideboard`/`maybeboard` for included-board cards) so the composer's CUT-side diff baseline reads correctly; a kept card with no OriginalEntries match falls back to Board="mainboard" AND is surfaced as a reconstruction warning (not silently dropped).
    - Board normalization for the full list: a kept card whose original board was `sideboard` or `maybeboard` appears in the finished 100-card main deck as mainboard in BOTH the Moxfield and Archidekt full-list output (none dropped). Reconstruction retains the ORIGINAL board here; the Core composer (105-03) normalizes boards for the full-list export (BLOCK).
    - Banlist re-check: a banlist service returning a banned name that is present in the final list surfaces that name in the export summary's BanlistOffenders; HardBlock stays false (count is the only hard block).
    - Banlist fail-open: when `GetBannedCardsAsync` throws `HttpRequestException`, the service does not throw — it returns the export view with a "legality not verified" warning and HardBlock still driven only by count.
    - No redundant Scryfall resolution: color identities are read from the cards the analysis context already resolved; the service does not trigger a second/independent Scryfall fetch for the same cards.
    - Existing controller tests still compile: the new `ICutLabExportService` ctor dependency is supplied by the shared `CreateController` helper via a fake export service; `POST /cut-lab/export` rehydrates state, attaches the export view-model, and an off-count (≠100) POST returns the rendered panel (count-block message), not a 4xx (HIGH 2).
  </behavior>
  <action>
    Create `ICutLabExportService` + `CutLabExportService` in `DeckFlow.Web/Services/CutLab/`, mirroring `ICutLabWhatifPreviewService`/`CutLabWhatifPreviewService` (same DI shape — inject `ICutLabAnalysisContextBuilder`, `CutLabResolvedCardCache`, `ICommanderBanListService`, `ILogger`). Method `Task<CutLabExportView> BuildExportAsync(CutLabState state, string playExperience, IReadOnlyList<string> commanderNames, CancellationToken ct)`:
    1. Derive the kept working list: `CutLabWorkingList.Derive(state.Pool, state.Decisions)`.
    2. Reconstruct the final `List<DeckEntry>`: for each kept card, look up metadata (Board/SetCode/CollectorNumber/Category) from `state.OriginalEntries` by normalized name (`CardNormalizer.Normalize`), using the working-list Quantity and NormalizedName; commander cards get Board="commander". RETAIN each matched card's ORIGINAL board (including `sideboard`/`maybeboard` for included-board cards) so the composer's CUT-side diff baseline reads correctly against the builder's real list — the board-normalization to `mainboard` for the FINISHED FULL-LIST export happens inside the Core composer (105-03), NOT here (BLOCK). Kept cards not found in OriginalEntries fall back to Board="mainboard" and are collected into a reconstruction-warning list surfaced on the view.
    3. Resolve color identities: `var ctx = await _contextBuilder.BuildAsync(keptWorkingList, playExperience, commanderNames, null, null, ct);` then from `ctx.ResolvedCards` build `cardIdentitiesByName` (name → ScryfallCardData.ColorIdentity) and `unverifiedCardNames` (kept cards with no resolved card). `commanderIdentity` = normalized union of the commander cards' ColorIdentity. Do NOT open a second Scryfall path — reuse the cards the context builder already resolved.
    4. Banlist: `bannedCardNamesPresent` = intersection of the FINAL kept list with `await _banListService.GetBannedCardsAsync(ct)` (new call site vs. intake pool check — RESEARCH Reuse Map). Fail-open on HttpRequestException with a "legality not verified" warning, matching CutLabPageService.cs:255-260.
    5. Call `CutLabExportComposer.Compose(finalEntries, originalEntriesAsDeckEntries, commanderIdentity, cardIdentitiesByName, unverifiedCardNames, bannedCardNamesPresent)` and project the result into a `CutLabExportView` view record — carrying the four text blobs, `HardBlock`/`OffCount`, `BanlistOffenders`, and the TWO separate color-identity buckets from the composer (`IllegalColorIdentity` and `UnverifiedColorIdentity`) plus the reconstruction warnings, so the view can label illegal vs unverified distinctly (WARNING 7).
    Add `[HttpPost("/cut-lab/export")]` `Export` action to `CutLabController`, mirroring `Goals` (CutLabController.cs:120-165): same `[FeatureFlagGate("tool.cut-lab.enabled")]`, `[ValidateAntiForgeryToken]`, `[RequestSizeLimit(2*1024*1024)]`, `try/catch` shape. Inject `ICutLabExportService` as a new ctor dependency alongside the page + whatif services. Deserialize `request.CutLabStateJson`, rehydrate + re-run `_pageService.ProcessAsync` for the standard page render, then call `_exportService.BuildExportAsync` and attach the export view to the model. The action ALWAYS returns the rendered Cut Lab view: an off-count (≠100) POST is not an error — the composer sets `HardBlock=true`/`OffCount`, and the view renders the count-block message with the final-list copy blocks suppressed (server returns the panel, not a 4xx). Add export fields to `CutLabViewModel` and populate them in a `From(...)` overload (mirror how the what-if preview is threaded via `CutLabViewModel.From(request, result, whatifView)` at CutLabController.cs:262). The export view-model property MUST default non-null (`public CutLabExportView Export { get; init; } = new();`, mirroring `Whatif` at CutLabViewModel.cs:108) and carry a `HasExport` flag (mirroring `CutLabWhatifPreviewView.HasPreview` at CutLabViewModel.cs:1011) that is true ONLY when this round-trip actually produced an export — so the step tab can read `Model.Export.HasExport` safely on every render (Process/Decide/Goals renders leave it the default empty view with `HasExport==false`). Register the new service in `Program.cs` next to the existing `ICutLabWhatifPreviewService` registration (scoped).
    Add `DeckFlow.Web.Tests/CutLabExportServiceTests.cs` (flat, alongside the existing `CutLab*Tests.cs`) covering the behaviors above, using fakes for `ICutLabAnalysisContextBuilder`, `CutLabResolvedCardCache`, and `ICommanderBanListService` in the style of `CutLabWhatifTests.cs`/`CutLabPageServiceTests.cs`: (a) a kept card absent from OriginalEntries reconstructs to Board="mainboard" AND appears in the reconstruction-warning list; (b) a fake banlist returning a name present in the final list surfaces it in `BanlistOffenders` with `HardBlock==false`; (c) a banlist fake that throws `HttpRequestException` yields a view with the "legality not verified" warning and no throw; (d) with a fake context builder that records its invocations, assert color identities come from the single `BuildAsync` result and no second resolution is triggered; (e) a pool with an INCLUDED sideboard card AND an included maybeboard card that both survive into the final 100 → the Moxfield AND Archidekt full-list output each contain exactly the 100-card finished main deck with those two cards present as mainboard and none dropped (BLOCK).
    Update `DeckFlow.Web.Tests/CutLabControllerTests.cs` (HIGH 2): extend the shared `CreateController` helper (CutLabControllerTests.cs:593-600) to supply a fake/no-op `ICutLabExportService` for the new constructor parameter (add a small `FakeExportService` test double if none exists), so every existing controller test still compiles and passes unchanged. Add at least one `POST /cut-lab/export` action test asserting state rehydration + export view-model attachment on the returned `CutLabViewModel`, and that an off-count (≠100) POST returns the rendered Cut Lab view with `HardBlock`/count-block populated (not a 4xx).
  </action>
  <verify>
    <automated>dotnet test DeckFlow.Web.Tests --filter "FullyQualifiedName~CutLabExportServiceTests|FullyQualifiedName~CutLabControllerTests"</automated>
  </verify>
  <done>POST /cut-lab/export returns the Cut Lab view populated with four export text blobs + a validation summary; count≠100 sets HardBlock and returns the panel with the count-block message; reconstruction retains each kept card's original board for the diff baseline while the Core composer board-normalizes the finished full list so kept sideboard/maybeboard cards land in the 100-card main deck (none dropped); the service reuses already-resolved cards (no redundant Scryfall fetch), re-checks the banlist against the final list, fails open on banlist HttpRequestException, and warns on unmatched-reconstruction cards; the shared CreateController helper supplies a fake export service so every existing CutLabControllerTests compiles and a /cut-lab/export action test passes — all asserted by CutLabExportServiceTests and CutLabControllerTests.</done>
</task>

<task type="auto">
  <name>Task 2: Export panel, Export step tab (gated at 100 with disabled affordance), copy wiring, CSS</name>
  <files>DeckFlow.Web/Models/WorkflowStepTabsModel.cs, DeckFlow.Web/Views/Shared/_WorkflowStepTabs.cshtml, DeckFlow.Web/Views/Deck/CutLab.cshtml, DeckFlow.Web/wwwroot/ts/cut-lab.ts, DeckFlow.Web/wwwroot/css/site-common.css, README.md</files>
  <action>
    **Step-tab disabled affordance (WARNING 6 — concrete, no deferral).** Extend the shared step-tab model and partial so a step can be rendered disabled, then use it to gate Export:
    - In `WorkflowStepTabsModel.cs`, add TWO trailing defaulted positional parameters to the record: `public sealed record WorkflowStepTab(int Step, string Label, bool IsComplete, bool IsEnabled = true, string? SubmitFormId = null);` (both defaulted → every existing `new WorkflowStepTab(step, label, complete)` call site, e.g. DeckComparison, stays source-compatible: enabled + no form binding — non-breaking, keeping the deck-sync client-toggle consumers on their existing `type="button"` path). `IsEnabled` gates the disabled affordance; `SubmitFormId` (when non-null) turns that step into a server-POST activator (see the Export activation note below). Add `<param>` xmldoc lines for `IsEnabled` and `SubmitFormId` mirroring the others.
    - In `_WorkflowStepTabs.cshtml` (button render at :17-24): when `!step.IsEnabled`, add an `is-disabled` class, emit `aria-disabled="true"` and the `disabled` attribute on the `<button>`, and EXCLUDE it from being the selected tab. When `step.SubmitFormId` is non-null, render THAT step's `<button>` as `type="submit" form="@step.SubmitFormId"` (a real server-POST activator) instead of the default `type="button"`; steps with a null `SubmitFormId` keep `type="button"` unchanged (deck-sync client-toggle path untouched). Change the `currentStep` computation to prefer the first step that is `!IsComplete && IsEnabled`; if none, fall back to the first `IsEnabled` step; else step 1 — so a disabled Export tab is never the focusable/aria-selected entry point.
    - Style `.prompt-step-tab.is-disabled` in `site-common.css` (greyed foreground, `cursor: not-allowed`, no hover affordance) using existing tokens so all guild themes inherit; keep layout CSS out of `site.css` (theme constraint).

    **Export panel + step strip in CutLab.cshtml.** Per D1, introduce the Cut Lab step-tab strip using `WorkflowStepTabsModel` + `Html.PartialAsync("_WorkflowStepTabs", model)` exactly as DeckComparison.cshtml:214-227 does, with steps `1 Process`, `2 Decide`, `3 Goals`, `4 Export`. Build the Export tab as `new WorkflowStepTab(4, "Export", IsComplete: Model.Export.HasExport, IsEnabled: CardsRemainingToTarget == 0, SubmitFormId: "cut-lab-export-form")` — enabled (clickable) only at exactly 100, and marked `IsComplete` (the `is-complete` styling) ONLY after an export has actually been produced this round-trip (`Model.Export.HasExport`, mirroring `Model.Whatif.HasPreview` at CutLab.cshtml:660 — NOT merely reaching 100), so "Export" is never shown finished before the user has exported (LOW). Below 100 it renders greyed/`aria-disabled` via the affordance above and cannot submit. Selection note: with `IsComplete` now keyed off `HasExport`, at 100-not-yet-exported the Export step becomes the first `!IsComplete && IsEnabled` step and is the aria-selected entry point (correct — it is the user's next action); this preserves the rev2 default-selected invariant (exactly one aria-selected, valid focusable tab).

    **Export step activation — server POST, mirroring Goals/Whatif (HIGH — no inert tab).** Cut Lab has NO client-side step toggle: its Decide/Goals/Whatif steps are each a `<form method="post" action="~/cut-lab/...">` submit that re-renders the whole page server-side (Goals form CutLab.cshtml:549 + submit :594-595; Whatif form :631 + submits :713-714; controller actions CutLabController.cs:120/172), and Cut Lab renders `_DeckToolTabs` today (CutLab.cshtml:76), not a step strip. `_WorkflowStepTabs` alone only emits buttons and switches nothing. So the Export step tab MUST activate the panel by that SAME server-POST mechanism — do NOT add a client-side `showStep`/`data-*-show-step` toggle to cut-lab.ts (that pattern lives only in deck-sync.ts and, applied here, would reveal an EMPTY panel since the four export blobs + the validation summary only exist after `BuildExportAsync` runs on the POST). Concretely: give the Export `<form method="post" action="~/cut-lab/export">` (added in (c) below) `id="cut-lab-export-form"`, and the Export step tab (via `SubmitFormId: "cut-lab-export-form"`) renders as `<button type="submit" form="cut-lab-export-form">` — clicking it submits the Export form → `POST /cut-lab/export` → the page re-renders with the Export panel populated and scrolled into view (`data-scroll-on-load`, as used at CutLab.cshtml:187/215). Works with JS off; the below-100 "disabled until 100" state is the `disabled` submit control from the affordance above. cut-lab.ts is touched ONLY for the copy-to-clipboard handler below, NOT for any step switching. Add an Export `<section class="result-panel">` mirroring the What-if panel (CutLab.cshtml:624-718) and Goals panel (:542-598). It contains:
    - (a) the always-visible validation summary — ✅/❌ **Card count = 100**; **Color-identity** shown as TWO distinct statuses per WARNING 7: an "illegal" line (✅ clean / ⚠ Color-identity illegal — naming the `IllegalColorIdentity` cards, i.e. a known rule-break) AND a separate "unverified" line (⚠ Could not verify — naming the `UnverifiedColorIdentity` cards, i.e. unresolved cards) so a merely-unresolved card is never presented as illegal; ✅/⚠ **Banlist clean** (naming `BanlistOffenders`). Also surface any reconstruction warnings. Per D2 none of these ⚠ warnings disable anything.
    - (b) four copy blocks — Moxfield full list, Archidekt full list, Moxfield CUT/ADD patch, Archidekt CUT/ADD patch — each a read-only `<textarea>`/`<pre>` + a `<button class="copy-button" data-copy-target="...">Copy` mirroring DeckPrimer.cshtml:353.
    - (c) the count gate: when `CardsRemainingToTarget != 0` (below 100), hard-block the final-list copy blocks (disable/hide the copy buttons) and show a "reach 100 to export" hint — color-identity/banlist warnings must NOT disable anything (D2). Add the `POST /cut-lab/export` form `<form method="post" action="~/cut-lab/export" id="cut-lab-export-form">` with `@Html.AntiForgeryToken()` + hidden `CutLabStateJson` (mirror the Goals form at CutLab.cshtml:549) — the `id` is the submit target the Export step tab binds to via `SubmitFormId` (see the activation note above). (An off-count POST still returns this panel with the count-block message — see Task 1.)

    In cut-lab.ts add a copy handler mirroring content-kb.ts:125-136 (`button[data-copy-target]` → `navigator.clipboard.writeText(target.value)` with a transient "Copied" state); wire it in the existing init path. Reuse existing `.result-panel`, `.copy-button`, and cutlab-* token classes so all guild themes inherit correctly. Update the cut-lab TS/Vitest snapshot if the DOM structure it asserts changes. Update `README.md` for the new Export capability (project behavior-change rule).
  </action>
  <verify>
    <automated>cd DeckFlow.Web && npx --no-install tsc -p tsconfig.json --noEmit && npx --no-install vitest run cut-lab</automated>
  </verify>
  <done>Export panel renders the validation summary (with illegal vs unverified color-identity shown separately) + four copy blocks; the Export step tab is a `type="submit" form="cut-lab-export-form"` server-POST activator that re-renders the populated panel (no client-side step toggle added to cut-lab.ts), is greyed/aria-disabled and non-submittable below 100, unlocks at 100, and shows `is-complete` only after an export has been produced (`Model.Export.HasExport`, not merely reaching 100); final-list copy is blocked with a hint below 100; color/banlist warnings never disable export; existing step-tab callers still compile (defaulted `IsEnabled`/`SubmitFormId`); README documents the Export capability; TS compiles and the cut-lab unit/snapshot test passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| browser → POST /cut-lab/export | Untrusted form incl. CutLabStateJson crosses into the new endpoint |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-105-06 | Tampering/Spoofing (CSRF) | POST /cut-lab/export | mitigate | `[ValidateAntiForgeryToken]` on the action + `@Html.AntiForgeryToken()` in the form, matching every other /cut-lab/* POST (CutLabController.cs:39,81,122,174) |
| T-105-07 | Denial of Service | Oversized export POST body / state | mitigate | `[RequestSizeLimit(2*1024*1024)]` on the action + `CutLabStateSerializer.MaxUploadBytes` cap on deserialize |
| T-105-08 | Information Disclosure | Export text (client-side copy) | accept | Output is the user's own deck; `navigator.clipboard` is same-origin, no server round-trip for the copy |
| T-105-SC | Tampering | package installs | mitigate | No npm/NuGet packages added (uses in-tree Playwright/Vitest/TS already present); nothing to audit |
</threat_model>

<verification>
- `dotnet build` clean (no new warnings; LF preserved; changed-lines format gate satisfied on touched C# lines).
- `dotnet test DeckFlow.Web.Tests --filter FullyQualifiedName~CutLab` green (incl. CutLabExportServiceTests AND the updated CutLabControllerTests — helper compiles + /cut-lab/export action test).
- `cd DeckFlow.Web && npx --no-install tsc -p tsconfig.json --noEmit` clean; `npx --no-install vitest run cut-lab` green.
- README updated for the new Export capability (project behavior-change rule; `README.md` in files_modified).
</verification>

<success_criteria>
- EXPORT-01: Moxfield + Archidekt full-list copy blocks present and populated at 100 cards; kept sideboard/maybeboard cards land in the mainboard 100 in both dialects (none dropped).
- EXPORT-02: both-dialect CUT/ADD patch copy blocks present, computed vs. OriginalEntries.
- EXPORT-03: always-visible 3-check validation summary; count≠100 hard-blocks final-list copy; illegal vs unverified color-identity shown as distinct statuses; color/banlist name offenders without blocking.
- D1: Export step tab in the Process→Decide→Goals→Export strip, greyed/aria-disabled below 100 and unlocking only at exactly 100 cards; the tab is a server-POST submit control (`form=cut-lab-export-form`) that activates + populates the Export panel on click, mirroring the Goals/Whatif step forms (no client-side step toggle).
</success_criteria>

<output>
Create `.planning/workstreams/cut-lab/phases/105-builder-compatible-export/105-04-SUMMARY.md` when done.
</output>
