---
phase: 58-dogfood
plan: 03
type: execute
wave: 3
depends_on: ["58-02"]
files_modified:
  - .planning/phases/58-dogfood/58-DOGFOOD-RESULTS.md
autonomous: true
requirements: [DOGFOOD-01]

must_haves:
  truths:
    - "The no-regression check confirms no previously-Published entry flipped to Local-newer or Never published"
    - "The DOGFOOD-RESULTS evidence document is complete across all four success criteria"
    - "DOGFOOD-01 is demonstrably satisfied by checkable evidence, not subjective assertion"
  artifacts:
    - path: ".planning/phases/58-dogfood/58-DOGFOOD-RESULTS.md"
      provides: "Completed evidence doc with SC4 no-regression result and overall verdict"
      contains: "No-regression"
  key_links:
    - from: "58-VERIFICATION-QUERIES.md no-regression snapshot"
      to: "58-DOGFOOD-RESULTS.md SC4 result"
      via: "before/after Published-set diff"
      pattern: "Published"
---

<objective>
Close the dogfood: run the read-only no-regression check (SC4), finalize the `58-DOGFOOD-RESULTS.md`
evidence document, and record the overall DOGFOOD-01 verdict against the four success criteria.

Purpose: Convert the operator's live-run notes (Plan 02) plus a SELECT-only corpus re-check into a single
committed evidence artifact that demonstrates the cycle's pipeline works end-to-end.

Output: Completed `58-DOGFOOD-RESULTS.md` (SC4 filled + overall verdict).

This plan is autonomous: it runs ONLY read-only local queries (no spend, no prod write, no quality
judgment — those were captured in Plan 02) and assembles the document.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/58-dogfood/58-DOGFOOD-RESULTS.md
@.planning/phases/58-dogfood/58-VERIFICATION-QUERIES.md
@DeckFlow.Core/Content/PublishStateDeriver.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run the no-regression check and record SC4</name>
  <files>.planning/phases/58-dogfood/58-DOGFOOD-RESULTS.md</files>
  <read_first>
    - `58-VERIFICATION-QUERIES.md` — the no-regression snapshot query (the set of rows that derived as
      `Published` BEFORE the run, captured in Plan 01/Plan 02).
    - `DeckFlow.Core/Content/PublishStateDeriver.cs` — to re-derive each row's state from
      `pushed_to_prod_utc`, `is_visible`, and the local indexed/distill time using the same logic.
    - `58-DOGFOOD-RESULTS.md` SC1/SC2/SC3 slots already filled by Plan 02.
  </read_first>
  <action>
    Re-run the SELECT-only no-regression query from `58-VERIFICATION-QUERIES.md` against the LOCAL Studio
    Content-KB DB (never a prod-write connection). Diff the AFTER `Published`-set against the BEFORE snapshot.
    Record into SC4 of `58-DOGFOOD-RESULTS.md`: the count of previously-`Published` rows, the count still
    `Published` after, and an explicit list (ideally empty) of any rows that flipped to `Local-newer` or
    `Never published`. The success condition is zero unexpected flips. If any flip is found, record it as a
    `## ⚠` finding with the row id and the three derivation inputs, rather than asserting success.
    If the DB is not accessible to the executor (e.g. the operator's local DB only), record the SC4 result
    from the operator's Plan-02 capture instead and mark the source.
  </action>
  <verify>
    <automated>grep -q "No-regression" .planning/phases/58-dogfood/58-DOGFOOD-RESULTS.md &amp;&amp; grep -qiE "0 (unexpected )?flip|zero .*flip|no .*flip" .planning/phases/58-dogfood/58-DOGFOOD-RESULTS.md</automated>
  </verify>
  <acceptance_criteria>
    - SC4 records before/after `Published`-set counts and an explicit flip list (empty = pass).
    - The query used was SELECT-only against the local DB; no prod write occurred.
    - Any detected flip is surfaced as a `## ⚠` finding with row id + derivation inputs.
  </acceptance_criteria>
  <done>SC4 no-regression result is recorded with checkable counts.</done>
</task>

<task type="auto">
  <name>Task 2: Finalize the evidence document with the overall DOGFOOD-01 verdict</name>
  <files>.planning/phases/58-dogfood/58-DOGFOOD-RESULTS.md</files>
  <read_first>
    - `58-DOGFOOD-RESULTS.md` with SC1–SC4 now filled.
    - The Phase 58 Success Criteria in `.planning/ROADMAP.md`.
  </read_first>
  <action>
    Verify each of the four success criteria has concrete, checkable evidence filled in (no remaining
    `[FILL AT RUN]` placeholders). Add an "Overall Verdict" section at the top: PASS only if all four are
    satisfied — (1) new video distilled + judged higher-quality, (2) Published in both surfaces with proof,
    (3) spend within cap, (4) zero unexpected regression flips. If any criterion is unmet or any placeholder
    remains, mark the verdict accordingly and list the gap as a `## ⚠` finding rather than declaring PASS.
    State explicitly in the doc that no product code or schema changed this phase (validation only) and that
    no unit tests were added (no test surface for a dogfood run; the existing suite is unchanged).
  </action>
  <verify>
    <automated>grep -q "Overall Verdict" .planning/phases/58-dogfood/58-DOGFOOD-RESULTS.md &amp;&amp; ! grep -q "FILL AT RUN" .planning/phases/58-dogfood/58-DOGFOOD-RESULTS.md</automated>
  </verify>
  <acceptance_criteria>
    - No `[FILL AT RUN]` placeholders remain in `58-DOGFOOD-RESULTS.md`.
    - An "Overall Verdict" section exists and reflects the true state of all four criteria.
    - The doc states no product code/schema changed and no tests were added (validation-only phase).
  </acceptance_criteria>
  <done>The committed evidence artifact fully demonstrates (or honestly fails) DOGFOOD-01.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| AI agent → DB | Read-only re-check; SELECT-only against the local DB, never prod-write |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-58-07 | Tampering | no-regression re-check query | mitigate | Re-uses the SELECT-only query from `58-VERIFICATION-QUERIES.md`; no writes/DDL; never a prod-write connection. |
| T-58-08 | Repudiation | false PASS verdict | mitigate | Verdict is gated on concrete checkable evidence (counts, status strings, proof paths) and forbidden from declaring PASS while any `[FILL AT RUN]` placeholder remains or any flip is detected. |
</threat_model>

<verification>
- No code built; no tests added (validation-only phase — stated explicitly in the evidence doc).
- `58-DOGFOOD-RESULTS.md` is complete: SC1–SC4 filled, Overall Verdict present, no placeholders.
</verification>

<success_criteria>
- SC4 no-regression result recorded (zero unexpected flips = pass).
- Evidence document complete with an honest Overall Verdict and no remaining placeholders.
</success_criteria>

<output>
Create `.planning/phases/58-dogfood/58-03-SUMMARY.md` when done.
</output>
