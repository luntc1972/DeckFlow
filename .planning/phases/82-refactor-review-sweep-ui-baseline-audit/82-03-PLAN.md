---
phase: 82-refactor-review-sweep-ui-baseline-audit
plan: 03
type: execute
wave: 2
depends_on: ["82-01"]
files_modified:
  - .planning/phases/82-refactor-review-sweep-ui-baseline-audit/REFACTOR-BACKLOG.md
  # Source files below are a NON-EXHAUSTIVE candidate set. The AUTHORITATIVE target list is
  # every row REFACTOR-TRIAGE.md marks `in-scope` (82-01 widens the sweep beyond the two named
  # flagships — e.g. df-select.ts, card-lookup.ts, DirectPush.razor.cs). The executor edits
  # EVERY in-scope file, not just the two below; a `backlog` row's file is NOT touched here —
  # it is recorded in REFACTOR-BACKLOG.md instead.
  - DeckFlow.Web/wwwroot/ts/deck-sync.ts
  - DeckFlow.Studio/Pages/Harvest.razor.cs
  - DeckFlow.Web/wwwroot/ts/df-select.ts
  - DeckFlow.Web/wwwroot/ts/card-lookup.ts
  - DeckFlow.Studio/Pages/DirectPush.razor.cs
  # ...and any additional file named by an `in-scope` REFACTOR-TRIAGE.md row.
autonomous: true
requirements: [REVIEW-02]
must_haves:
  truths:
    - "Every triage target marked in-scope has been refactored under the byte-identical/behavior-neutral gate"
    - "Build stays clean (0 warnings / 0 errors) after every executed refactor"
    - "The full test suite (incl. CarveOutGuard) passes after every executed refactor"
    - "The changed-lines format-gate exits 0 on the refactored lines"
    - "Every in-scope refactor is behavior-neutral — no observable behavior change (paste artifacts identical; TS covered by Vitest + Playwright functional parity, no pixel-diff claim)"
    - "Every triage target marked backlog / over-risk-budget is explicitly recorded with a written reason — not silently dropped"
  artifacts:
    - path: ".planning/phases/82-refactor-review-sweep-ui-baseline-audit/REFACTOR-BACKLOG.md"
      provides: "Written deferral record for every over-budget target: file, reason, and what would unblock it"
      contains: "reason"
  key_links:
    - from: ".planning/phases/82-refactor-review-sweep-ui-baseline-audit/REFACTOR-TRIAGE.md"
      to: "the executed refactors + REFACTOR-BACKLOG.md"
      via: "in-scope rows -> refactored; backlog rows -> deferral record"
      pattern: "in-scope|backlog"
---

<objective>
Execute the refactors that plan 82-01's REFACTOR-TRIAGE.md marked `in-scope`, each under
the cycle's byte-identical/behavior-neutral gate, and explicitly record every `backlog` /
over-risk-budget target with a written deferral reason.

Purpose: This is the "resolve" half of the sweep (REVIEW-02). The triage artifact is the
authoritative task list — this plan does NOT re-decide scope, it executes it. Every executed
refactor must prove no observable behavior change; every deferral must be written down, not
dropped. If the triage legitimately marked ALL candidates `backlog` (each with a valid,
evidence-based reason), this plan is satisfied by recording those deferrals — that is a valid
REVIEW-02 completion. Note: "no TS test harness" is NOT a valid deferral reason for deck-sync.ts
— the repo has a vitest runner and deck-sync.ts is already imported by existing ts-tests, so a
blanket all-backlog outcome that defers the named flagship target on that basis should be
treated as a triage error, not accepted here.

Output: refactored in-scope source files (byte-neutral) + REFACTOR-BACKLOG.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@./CLAUDE.md

# THE task list for this plan — read it first; it is the contract:
@.planning/phases/82-refactor-review-sweep-ui-baseline-audit/REFACTOR-TRIAGE.md
@.planning/phases/82-refactor-review-sweep-ui-baseline-audit/82-REVIEW.md

# Byte-identical / behavior-neutral gate (Cycle 15 cross-cutting):
#  - dotnet build clean (0 warnings / 0 errors)
#  - full test suite passes, including CarveOutGuardTests (DeckFlow.Core.Tests)
#  - changed-lines format-gate: scripts/format-check-changed.sh staged  -> exit 0
#  - 5 .editorconfig carve-outs never violated (get;init; / attribute lines / raw-string
#    indent / switch expressions / xmldoc indent / LF line endings)
#  - if TS touched: recompile + run the Vitest TS unit suite (npm --prefix DeckFlow.Web test)
#    AND Playwright e2e — covered TS flows pass (functional parity; note there is no
#    visual-snapshot infra, so e2e proves behavior/functional parity, not pixel-identical render)
</context>

<interfaces>
<!-- WSL build/test invocation (VSTest unreliable in WSL — rely on Windows dotnet + CI): -->
<!--   "/mnt/c/Program Files/dotnet/dotnet.exe" build DeckFlow.sln   (expect 0 warn / 0 err) -->
<!--   "/mnt/c/Program Files/dotnet/dotnet.exe" test DeckFlow.Core.Tests/DeckFlow.Core.Tests.csproj -->
<!--   "/mnt/c/Program Files/dotnet/dotnet.exe" test DeckFlow.Web.Tests/DeckFlow.Web.Tests.csproj -->
<!--   scripts/format-check-changed.sh staged   (changed-lines format gate, exit 0) -->
<!-- TS refactor path: recompile via build, run Vitest (npm --prefix DeckFlow.Web test), then -->
<!--   start scripts/run-web-test.sh and run npx --no-install playwright test -->
<!--   (covered TS flows pass — functional parity; no pixel-diff/visual-snapshot gate exists). -->
<!-- CI (format-gate + build + tests) is the authoritative enforcer per CLAUDE.md. -->
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Execute each in-scope refactor under the byte-identical gate</name>
  <files>every file named by an `in-scope` row in REFACTOR-TRIAGE.md (candidate set incl. DeckFlow.Web/wwwroot/ts/deck-sync.ts, DeckFlow.Studio/Pages/Harvest.razor.cs, DeckFlow.Web/wwwroot/ts/df-select.ts, DeckFlow.Web/wwwroot/ts/card-lookup.ts, DeckFlow.Studio/Pages/DirectPush.razor.cs — non-exhaustive)</files>
  <read_first>
    - .planning/phases/82-refactor-review-sweep-ui-baseline-audit/REFACTOR-TRIAGE.md (the in-scope rows = the exact refactor task list + approach + files per row)
    - .planning/phases/82-refactor-review-sweep-ui-baseline-audit/82-REVIEW.md (the finding detail behind each in-scope row)
    - ./CLAUDE.md (byte-identical gate, the 5 .editorconfig carve-outs, SOLID review checklist, changed-lines format discipline)
    - DeckFlow.Core.Tests/CarveOutGuardTests.cs (the carve-out guard that must stay green)
    - For each in-scope target: the actual source file named in its triage row (e.g. DeckFlow.Web/wwwroot/ts/deck-sync.ts and/or DeckFlow.Studio/Pages/Harvest.razor.cs) before editing it
  </read_first>
  <behavior>
    - Refactor is behavior-neutral: for each in-scope target, the existing test suite that
      covers it stays green with NO test assertions changed to accommodate new behavior.
    - New/extracted collaborators get their own unit tests in the test project matching the
      collaborator's LANGUAGE: C# collaborators -> xUnit (DeckFlow.Core.Tests / DeckFlow.Web.Tests);
      TypeScript collaborators/helpers -> Vitest under DeckFlow.Web/ts-tests.
    - No observable output change: C# paste-artifact-producing paths and Razor/TS behavior are
      identical pre/post — proven by the unchanged xUnit suite AND, where TS is touched, the
      unchanged Vitest TS suite plus unchanged Playwright e2e (functional parity; no pixel-diff infra).
  </behavior>
  <action>
    For EACH row in REFACTOR-TRIAGE.md marked `in-scope`, apply exactly the refactor approach that
    row specifies (extract-method / extract-collaborator / dedupe) and touch ONLY the files that
    row names. Do not expand scope beyond the triage rows and do not touch any `backlog` row's file.
    Follow the changed-lines-only discipline: edit only the lines the refactor requires; never
    mass-reflow a file; never violate the 5 .editorconfig carve-outs.
    After EACH target: run `"/mnt/c/Program Files/dotnet/dotnet.exe" build DeckFlow.sln` (require
    0 warnings / 0 errors), run the relevant test project(s) via dotnet.exe test (all pass,
    including CarveOutGuardTests), and run `scripts/format-check-changed.sh staged` (exit 0).
    If a target touches TypeScript (deck-sync.ts, df-select.ts, card-lookup.ts, or any TS file),
    rebuild so tsc recompiles, run the Vitest TS unit suite `npm --prefix DeckFlow.Web test`
    (all pass), then start scripts/run-web-test.sh and run `npx --no-install playwright test` —
    the covered TS flows must pass (functional parity; there is no visual-snapshot gate, so this
    proves behavior parity, not pixel-identical render). If any gate fails, the refactor is not
    byte-neutral: revert that target and re-file it as `backlog` in Task 2 with the failure as the reason.
    NOTE: if REFACTOR-TRIAGE.md marks ZERO rows `in-scope`, this task is a no-op — proceed to Task 2.
  </action>
  <acceptance_criteria>
    - `"/mnt/c/Program Files/dotnet/dotnet.exe" build DeckFlow.sln` completes with 0 warnings and 0 errors.
    - `"/mnt/c/Program Files/dotnet/dotnet.exe" test DeckFlow.Core.Tests/DeckFlow.Core.Tests.csproj` passes, including CarveOutGuardTests.
    - `"/mnt/c/Program Files/dotnet/dotnet.exe" test DeckFlow.Web.Tests/DeckFlow.Web.Tests.csproj` passes (PG-skip tolerated as in Cycle 14 baseline).
    - `scripts/format-check-changed.sh staged` exits 0 (changed-lines format gate clean).
    - No test assertion was weakened/changed to accommodate new behavior (behavior-neutral proof): the diff on `*Tests.cs` files adds tests for extracted collaborators only, it does not edit existing expected-output assertions.
    - If any TS file (deck-sync.ts / df-select.ts / card-lookup.ts / …) was refactored: `npm --prefix DeckFlow.Web test` (Vitest) passes AND `npx --no-install playwright test` passes with covered flows functionally unchanged.
    - Only files named `in-scope` by REFACTOR-TRIAGE.md were modified; no `backlog`-marked file was touched.
    - Every `in-scope` row in REFACTOR-TRIAGE.md has its named file(s) present in this task's diff — no in-scope row left unexecuted (accounting keyed to triage rows, not to the two flagship files).
  </acceptance_criteria>
  <verify>
    <automated>"/mnt/c/Program Files/dotnet/dotnet.exe" build DeckFlow.sln 2>&1 | grep -qE '0 Warning\(s\)' && "/mnt/c/Program Files/dotnet/dotnet.exe" build DeckFlow.sln 2>&1 | grep -qE '0 Error\(s\)' && scripts/format-check-changed.sh staged</automated>
  </verify>
  <done>Every in-scope triage target is refactored, build is 0/0, full test suite (incl. CarveOutGuard) passes, changed-lines format-gate is clean, and no existing behavior assertion changed — behavior-neutral proven. (Or a no-op if triage marked nothing in-scope.) REVIEW-02 (execution half) satisfied.</done>
</task>

<task type="auto">
  <name>Task 2: Record every over-budget / backlog target with a written deferral reason</name>
  <files>.planning/phases/82-refactor-review-sweep-ui-baseline-audit/REFACTOR-BACKLOG.md</files>
  <read_first>
    - .planning/phases/82-refactor-review-sweep-ui-baseline-audit/REFACTOR-TRIAGE.md (the `backlog` rows to carry forward + any in-scope target that failed its gate in Task 1 and was reverted)
    - .planning/ROADMAP.md ("Carry-forward backlog (not in Cycle 15)" section — where cycle-level deferrals are tracked)
  </read_first>
  <action>
    Write `REFACTOR-BACKLOG.md`. For EVERY target marked `backlog` in REFACTOR-TRIAGE.md — plus
    any target that was reverted in Task 1 because it failed the byte-identical gate — record a row:
    (a) file + finding, (b) written reason it exceeds this cycle's risk budget (e.g. "refactor
    would require net-new test scaffolding for an otherwise-untested collaborator; behavior-neutrality
    proof not achievable within this cycle's gates"), and (c) what would unblock it in a future cycle
    (e.g. "add unit coverage for the extracted collaborator first"). Base the reason on the target's
    ACTUAL coverage — do not cite "no test harness" for files already exercised by existing tests
    (e.g. deck-sync.ts is imported by vitest ts-tests). No target may be dropped without a reason string. Confirm each REVIEW.md
    finding is accounted for by exactly one of: refactored (Task 1) OR recorded here — nothing falls
    through. Do NOT edit REFACTOR-TRIAGE.md (owned by plan 82-01); this is the separate deferral record.
  </action>
  <acceptance_criteria>
    - `.planning/phases/82-refactor-review-sweep-ui-baseline-audit/REFACTOR-BACKLOG.md` exists.
    - Every backlog row has a non-empty reason string: no row lacks a "reason" (`grep -ci 'reason' REFACTOR-BACKLOG.md` >= number of deferral rows; zero bare deferrals).
    - Every candidate from REFACTOR-TRIAGE.md is accounted for: the count of (in-scope-refactored + backlog-recorded) equals the "candidates surfaced" count in REFACTOR-TRIAGE.md — nothing silently dropped.
    - REFACTOR-TRIAGE.md was NOT modified by THIS plan (file ownership preserved): it is not staged and has no uncommitted content change attributable to 82-03 — `git diff --name-only HEAD -- .planning/phases/82-refactor-review-sweep-ui-baseline-audit/REFACTOR-TRIAGE.md` prints nothing once 82-01's TRIAGE commit is HEAD. (Compare against the plan-owned path only; do not gate on whole-worktree cleanliness, which is sensitive to unrelated local state.)
  </acceptance_criteria>
  <verify>
    <automated>test -f .planning/phases/82-refactor-review-sweep-ui-baseline-audit/REFACTOR-BACKLOG.md && grep -qi 'reason' .planning/phases/82-refactor-review-sweep-ui-baseline-audit/REFACTOR-BACKLOG.md && { git diff --name-only HEAD -- .planning/phases/82-refactor-review-sweep-ui-baseline-audit/REFACTOR-TRIAGE.md | grep -q . && exit 1 || exit 0; }</automated>
  </verify>
  <done>Every backlog / over-budget / gate-failing target is recorded in REFACTOR-BACKLOG.md with a written reason and an unblock note; every surfaced candidate is accounted for (refactored or deferred); nothing silently dropped. REVIEW-02 (deferral half) satisfied.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none introduced) | Behavior-neutral refactor: extracts/dedupes existing code paths + writes one planning doc. No new inputs, endpoints, auth surfaces, or data flows are introduced. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-82-03 | Tampering | Accidental behavior/security drift during refactor (the real risk) | mitigate | Byte-identical gate: 0/0 build + full xUnit suite + CarveOutGuardTests + changed-lines format-gate + (for TS) Vitest suite + Playwright e2e prove no observable behavior change. Any gate failure reverts the target to backlog. |
| T-82-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs — refactor uses existing deps only; `npx --no-install` for e2e. No supply-chain surface. |

Behavior-neutral refactor + deferral record: no new inputs, endpoints, auth, or data flows.
The only meaningful risk is accidental behavior drift, and the mitigation of record IS the
byte-identical gate (build 0/0 + full test suite + CarveOutGuard + changed-lines format-gate +
e2e). No security-relevant surface is added.
</threat_model>

<verification>
- Build 0 warnings / 0 errors after every executed refactor.
- Full test suite (Core + Web xUnit, incl. CarveOutGuardTests) passes; no existing behavior assertion changed.
- If TS was refactored: Vitest suite (`npm --prefix DeckFlow.Web test`) passes AND Playwright e2e covered flows pass (functional parity; no pixel-diff infra).
- scripts/format-check-changed.sh staged exits 0.
- REFACTOR-BACKLOG.md records every deferral with a written reason; every surfaced candidate accounted for.
</verification>

<success_criteria>
- REVIEW-02 satisfied: every in-scope triage target refactored under the byte-identical/
  behavior-neutral gate with tests, and every over-budget target explicitly deferred to backlog
  with written reasoning — nothing silently dropped.
</success_criteria>

<output>
Create `.planning/phases/82-refactor-review-sweep-ui-baseline-audit/82-03-SUMMARY.md` when done.
</output>
