---
phase: 85-chatgpt-naming-cleanup
plan: 05
type: execute
wave: 3
depends_on: [85-02, 85-03, 85-04]
files_modified:
  - .planning/phases/85-chatgpt-naming-cleanup/render-snapshot-post85.json
autonomous: false
requirements: [AICLEAN-01, AICLEAN-02, AICLEAN-03]
must_haves:
  truths:
    - "A post-rename render/computed-style snapshot is captured with the same harness and the SAME key/route/theme ordering as 85-01, and after token-normalizing prompt/Prompt to the same placeholder used for chatgpt/ChatGpt, diffs byte-identical against render-baseline-pre85.json — proving only the intended identifier tokens changed"
    - "The grep-clean gates pass: zero strict chatgpt- across DeckFlow.Web/wwwroot/css + wwwroot/ts + Views; each of the 12 edited .cs files holds zero identifier-form ChatGpt|chatgpt; git diff shows no keep-list .cs file was modified by the phase"
    - "The full xUnit suite (dotnet.exe test DeckFlow.sln) passes and the full Playwright e2e suite (npx --no-install playwright test against the headless run-web-test.sh server) passes green — AICLEAN-03's explicit BLOCKING e2e gate"
    - "The 3 D5 cache-key literals are byte-identical across deck-sync.ts + moxfield-extension-bridge.ts + the 3 Razor emitters, with no old chatgpt- cache-key literal surviving anywhere"
    - "A human has signed off that the D3 keep-list is intact (no genuine ChatGPT-model ref or user-visible copy renamed) and the D5 contract moved in lockstep"
  artifacts:
    - path: ".planning/phases/85-chatgpt-naming-cleanup/render-snapshot-post85.json"
      provides: "Post-rename normalized snapshot proving byte-identical render vs the 85-01 baseline"
      contains: "prompt-building-routes"
  key_links:
    - from: ".planning/phases/85-chatgpt-naming-cleanup/render-snapshot-post85.json"
      to: ".planning/phases/85-chatgpt-naming-cleanup/render-baseline-pre85.json"
      via: "token-normalized structural diff (byte-identical render proof)"
      pattern: "render-(baseline-pre85|snapshot-post85)"
---

<objective>
Prove the Phase 85 rename is byte-identical and complete: re-capture the rendered-output snapshot,
diff it token-normalized against the 85-01 baseline, run the full grep-clean + build + xUnit + FULL
Playwright e2e gates, and take a human sign-off on the two semantic judgments grep cannot make (D3
keep-list intact, D5 contract lockstep). This is the phase acceptance gate for AICLEAN-01/02/03.

Purpose: a mechanical rename across 25 CSS forks + 3 TS files + 15 views + a handful of C# files is only
"done" when (a) the render is provably unchanged except the intended tokens, (b) no chatgpt- identifier
survives, (c) the keep-list (genuine ChatGPT-model refs + user-visible copy) is untouched, and (d) the
D5 client<->server contract is consistent. Grep proves (b); the snapshot diff proves (a); build + FULL
xUnit + FULL e2e prove nothing broke; a human confirms (c) and (d) because they are semantic, not textual.
Output: post-rename snapshot artifact + a green full-suite run + a recorded keep-list/D5 sign-off.

WAVE note: wave 3, depends on all three rename plans (85-02/03/04) completing. Runs after the wave-2
rename lands so the byte-identical proof reflects the final state.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/85-chatgpt-naming-cleanup/85-CONTEXT.md
@.planning/phases/85-chatgpt-naming-cleanup/85-RESEARCH.md
@.planning/phases/85-chatgpt-naming-cleanup/85-VALIDATION.md
@.planning/phases/85-chatgpt-naming-cleanup/render-baseline-pre85.json
@CLAUDE.md

<interfaces>
<!-- The 12 .cs files the phase is ALLOWED to change (85-03 view-tests + 85-04 C#/help/Manabase/doc-comments), -->
<!-- and which hold ZERO identifier-form ChatGpt|chatgpt (case-sensitive) AFTER the phase: -->
<!--   DeckFlow.Web.Tests/DeckAnalysisScoreViewTests.cs -->
<!--   DeckFlow.Web.Tests/DeckAnalysisPrintButtonViewTests.cs -->
<!--   DeckFlow.Web.Tests/DeckComparisonPrintButtonViewTests.cs -->
<!--   DeckFlow.Web.Tests/MetaGapViewRenderTests.cs -->
<!--   DeckFlow.Web.Tests/HelpContentServiceTests.cs -->
<!--   DeckFlow.Web.Tests/HelpControllerTests.cs -->
<!--   DeckFlow.Web/Services/Manabase/ManabaseAnalysisService.cs -->
<!--   DeckFlow.Web/Models/ManabaseViewModel.cs -->
<!--   DeckFlow.Web/Controllers/ManabaseController.cs -->
<!--   DeckFlow.Web.Tests/Manabase/ManabaseAnalysisServiceTests.cs -->
<!--   DeckFlow.Web/Controllers/DeckPacketController.cs -->
<!--   DeckFlow.Web/Services/DeckAnalysisPacketService.cs -->
<!-- (DeckAnalysisPacketService.cs:2131 keeps the all-caps "ChatGPT" model-key default, which the -->
<!-- case-sensitive ChatGpt|chatgpt grep does NOT match — so an exact-path zero-check on it still passes.) -->

<!-- D3 keep-list: the .cs occurrences of identifier-form ChatGpt|chatgpt that legitimately REMAIN and must -->
<!-- NOT be modified by the phase (ChatGptSwapPrompt is NO LONGER here — renamed to PromptSwapPrompt in 85-04): -->
<!--   7x Services/PromptBuilders/*/ChatGpt*PromptVariant.cs (+ Claude*/Gemini* siblings) -->
<!--   Extensions/PromptVariantServiceCollectionExtensions.cs (DI registrations) -->
<!--   Models/AiPlatform.cs (AiPlatform.ChatGpt) -->
<!--   Services/Persistence/PacketArtifactStore.cs ("*-chatgpt-prompt.txt" zip entries + CreateSafePathSegment "chatgpt") -->
<!--   trio xUnit tests referencing PascalCase ChatGpt / lowercase chatgpt (AiPlatform*, *ByteIdentity*, *Goldens.cs, -->
<!--   PacketByteIdentityFixtures, Bracket*, Primer*, Gemini*, DeckAnalysisPacketServiceTests, ...) -->
<!-- NOTE: gate (b) checks the 12 EXACT edited paths == 0 (NOT a substring match), because keep-list files -->
<!-- like DeckAnalysisPacketServiceTests.cs share a name-prefix with an edited file (DeckAnalysisPacketService.cs) -->
<!-- and a substring gate would false-fail on them. Gate (c) (git diff subset) guarantees no keep-list .cs changed. -->
<!-- (all-caps "ChatGPT" user-visible COPY in Views + the *Request.cs/_AiSelector/FeatureFlagCatalog/ToolRegistry -->
<!-- "ChatGPT" strings are NOT matched by the case-sensitive grep, so they trip no gate and need no allowlist entry.) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Post-rename snapshot + byte-identical diff + full grep/build/xUnit/e2e gates</name>
  <files>.planning/phases/85-chatgpt-naming-cleanup/render-snapshot-post85.json</files>
  <read_first>
    - .planning/phases/85-chatgpt-naming-cleanup/render-baseline-pre85.json (the pre-rename baseline to diff against — MIRROR its exact key/route/theme ordering before diffing, since the snapshot script is an uncommitted one-off)
    - .planning/phases/85-chatgpt-naming-cleanup/85-VALIDATION.md (Core Acceptance Gates)
    - .planning/phases/85-chatgpt-naming-cleanup/85-RESEARCH.md (Validation Architecture; Byte-Identical Proof Strategy)
  </read_first>
  <action>
    Using the SAME headless harness as 85-01 (Node + playwright-core scratch script, run-web-test.sh,
    DECKFLOW_DISABLE_AUTO_BROWSER=true, no Windows-host browser), re-capture the render/computed-style
    snapshot for the same 6 routes + selector set from the POST-rename tree, emitting keys/routes/themes
    in the SAME ORDER as render-baseline-pre85.json (the diff is order-sensitive; the script is a one-off,
    so mirror the baseline's structure), normalizing prompt/Prompt substrings to the SAME placeholder token
    85-01 used for chatgpt/ChatGpt. Write to render-snapshot-post85.json (LF). Diff it against the baseline:
    it MUST be identical (token-symmetric) — any non-token difference is a byte-identical violation to fix.
    Then run the full gate battery:
      (a) grep-clean: zero strict `chatgpt-` across DeckFlow.Web/wwwroot/css + wwwroot/ts + Views;
      (b) C# edited-files clean: each of the 12 EXACT edited .cs paths (<interfaces>) holds zero
          case-sensitive identifier-form `ChatGpt\|chatgpt` (exact-path check, NOT substring — keep-list
          files like DeckAnalysisPacketServiceTests.cs share a name-prefix and must not be caught);
      (c) keep-list untouched: `git diff --name-only <BASE>..HEAD -- '*.cs'` contains NO .cs file outside
          the 12 allowed ones (no PromptVariant / AiPlatform / PacketArtifactStore / *Request / _AiSelector /
          trio-test file was modified), where BASE = parent of the commit that added render-baseline-pre85.json;
      (d) D5 lockstep: NO old chatgpt- cache-key literal (chatgpt-packets / chatgpt-deck-comparison /
          chatgpt-cedh-meta-gap / chatgpt-deck-url / chatgpt-deck-text / decksync-chatgpt-ui-mode) survives
          in deck-sync.ts / moxfield-extension-bridge.ts / the 3 Razor emitters;
      (e) build 0/0 (assert `0 Warning(s)` AND `0 Error(s)` from full output); full xUnit
          (dotnet.exe test DeckFlow.sln); FULL Playwright e2e (npx --no-install playwright test against the
          headless run-web-test.sh server) — all green (AICLEAN-03 BLOCKING e2e gate).
    Fix any residual before the checkpoint. Do NOT touch wwwroot/js (gitignored).
  </action>
  <acceptance_criteria>
    - render-snapshot-post85.json exists and its token-normalized content is byte-identical to render-baseline-pre85.json.
    - Zero strict `chatgpt-` across css/ ts/ Views/; each of the 12 exact edited .cs files has zero identifier-form ChatGpt|chatgpt.
    - `git diff --name-only <BASE>..HEAD -- '*.cs'` contains no .cs file outside the 12 allowed ones (no keep-list .cs modified).
    - No old chatgpt- cache-key literal survives in the 5 D5 files.
    - Build 0 Warning(s)/0 Error(s); full xUnit green; FULL Playwright e2e green.
  </acceptance_criteria>
  <verify>
    <automated>cd /mnt/c/users/chrislunt/source/personal/deckflow-cycle15 && test -f .planning/phases/85-chatgpt-naming-cleanup/render-snapshot-post85.json && python3 -c "import json,re,sys; norm=lambda s: re.sub('prompt','X',re.sub('chatgpt','X',json.dumps(json.load(open(s)),sort_keys=True),flags=re.I),flags=re.I); sys.exit(0 if norm('.planning/phases/85-chatgpt-naming-cleanup/render-baseline-pre85.json')==norm('.planning/phases/85-chatgpt-naming-cleanup/render-snapshot-post85.json') else 1)" && test -z "$(grep -rIn 'chatgpt-' DeckFlow.Web/wwwroot/css DeckFlow.Web/wwwroot/ts DeckFlow.Web/Views)" && EXP="DeckFlow.Web.Tests/DeckAnalysisScoreViewTests.cs DeckFlow.Web.Tests/DeckAnalysisPrintButtonViewTests.cs DeckFlow.Web.Tests/DeckComparisonPrintButtonViewTests.cs DeckFlow.Web.Tests/MetaGapViewRenderTests.cs DeckFlow.Web.Tests/HelpContentServiceTests.cs DeckFlow.Web.Tests/HelpControllerTests.cs DeckFlow.Web/Services/Manabase/ManabaseAnalysisService.cs DeckFlow.Web/Models/ManabaseViewModel.cs DeckFlow.Web/Controllers/ManabaseController.cs DeckFlow.Web.Tests/Manabase/ManabaseAnalysisServiceTests.cs DeckFlow.Web/Controllers/DeckPacketController.cs DeckFlow.Web/Services/DeckAnalysisPacketService.cs" && for f in $EXP; do test $(grep -c 'ChatGpt\|chatgpt' "$f") -eq 0 || { echo "STRAY_IDENTIFIER $f"; exit 1; }; done && BASE=$(git log --diff-filter=A --format=%H -1 -- .planning/phases/85-chatgpt-naming-cleanup/render-baseline-pre85.json)^ && UNEXP=$(comm -23 <(git diff --name-only "$BASE"..HEAD -- '*.cs' | sort) <(printf '%s\n' $EXP | sort)) && test -z "$UNEXP" && test -z "$(grep -rIn 'chatgpt-packets\|chatgpt-deck-comparison\|chatgpt-cedh-meta-gap\|chatgpt-deck-url\|chatgpt-deck-text\|decksync-chatgpt-ui-mode' DeckFlow.Web/wwwroot/ts DeckFlow.Web/Views)" && b=$("/mnt/c/Program Files/dotnet/dotnet.exe" build DeckFlow.sln 2>&1) && echo "$b" | grep -q '0 Warning(s)' && echo "$b" | grep -q '0 Error(s)' && "/mnt/c/Program Files/dotnet/dotnet.exe" test DeckFlow.sln && ( cd DeckFlow.Web && DECKFLOW_DISABLE_AUTO_BROWSER=true npx --no-install playwright test )</automated>
  </verify>
  <done>Post-rename snapshot proves byte-identical render; grep-clean + exact-path C# keep-list + D5-lockstep gates pass; build 0/0; full xUnit + full Playwright e2e green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human sign-off — D3 keep-list intact + D5 contract lockstep</name>
  <what-built>
    The Phase 85 rename is complete and automatically verified: byte-identical render proof (snapshot
    diff vs baseline), zero chatgpt- across css/ts/Views, C# keep-list gate (each edited .cs is clean;
    git diff shows no keep-list .cs modified), full xUnit + FULL Playwright e2e green. Two judgments remain
    that grep/tests cannot make — they need a human eye.
  </what-built>
  <how-to-verify>
    1. Review the final phase diff (`git diff` for the 85-* range). Confirm the D3 KEEP-LIST is INTACT —
       NONE of these were renamed: the 7 Services/PromptBuilders/*/ChatGpt*PromptVariant.cs classes (+ their
       Claude*/Gemini* siblings), their DI registrations, Models/AiPlatform.cs (AiPlatform.ChatGpt),
       PacketArtifactStore.cs "*-chatgpt-prompt.txt" zip entries + the CreateSafePathSegment "chatgpt" fallback,
       the "ChatGPT" TargetAiPlatform defaults (*Request.cs / _AiSelector.cshtml), DeckAnalysisPacketService.cs:2131
       "ChatGPT" default, the ManabaseCommandRunner.cs CLI header, and every all-caps "ChatGPT" user-visible copy
       string in the views (Home/About/Help/Bracket/JudgeQuestions/DeckPrimer/Manabase/ContentKb/_Layout).
       (Note: ChatGptSwapPrompt WAS intentionally renamed to PromptSwapPrompt — confirm its emitted swap-prompt
       string value is unchanged, i.e. the rendered textarea copy reads the same.)
    2. Confirm the D5 CONTRACT moved in LOCKSTEP: the 3 sessionStorage cache-key values
       (prompt-packets / prompt-deck-comparison / prompt-cedh-meta-gap) and the 2 sync-panel values
       (prompt-deck-url / prompt-deck-text) changed on BOTH the Razor emitting side and the
       deck-sync.ts + moxfield-extension-bridge.ts consuming side — no side left on the old literal.
    3. Optional spot-check (manual browser via headless run-web-test.sh + Playwright): on /deck-analysis
       click "Clear and restart" (still clears the form), confirm the Moxfield-extension import still routes
       deck text into the right form, and confirm the /manabase swap-prompt textarea still shows the same
       copy — the behaviors RESEARCH Pitfall 3 flagged as having no automated coverage.
  </how-to-verify>
  <resume-signal>Type "approved" to mark Phase 85 complete, or describe any keep-list/contract issue to fix.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

N/A for the automated gate (read-only capture + test runs). The human checkpoint reviews a completed
static rename; no code is authored here. No attacker-controllable input, no trust boundary introduced.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-85-05-01 | (all) | verification snapshot + full-suite gate + human sign-off | accept | ASVS L1: read-only proof/verification pass; no source authored, no trust boundary crossed. No package installs. The human checkpoint is the residual control for the two semantic (non-textual) risks: D3 keep-list integrity and D5 contract lockstep. |
</threat_model>

<verification>
- render-snapshot-post85.json is token-normalized byte-identical to render-baseline-pre85.json (same key/route/theme ordering mirrored before diffing).
- Zero strict chatgpt- across css/ts/Views; each of the 12 exact edited .cs files is clean; `git diff --name-only` touches no keep-list .cs.
- D5 cache-key literals consistent across all 5 files; no old literal survives.
- Full dotnet.exe build 0/0 + full xUnit + FULL Playwright e2e green.
- Human sign-off recorded for D3 keep-list + D5 lockstep.
</verification>

<success_criteria>
- AICLEAN-01/02/03 satisfied: byte-identical render proven; grep-clean; full suites green (incl the
  BLOCKING full-e2e gate); keep-list intact and D5 contract lockstep confirmed by a human.
</success_criteria>

<output>
Create `.planning/phases/85-chatgpt-naming-cleanup/85-05-SUMMARY.md` when done. Record the snapshot-diff
result, all gate outcomes (grep/build/xUnit/full-e2e/keep-list), the keep-list/D5 sign-off, and mark
AICLEAN-01/02/03 complete.
</output>
