---
phase: 107-cut-lab-tech-debt-cleanup
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - DeckFlow.Web/wwwroot/css/site-abzan.css
  - DeckFlow.Web/wwwroot/css/site-dimir.css
  - DeckFlow.Web/wwwroot/css/site-esper.css
  - DeckFlow.Web/wwwroot/css/site-golgari.css
  - DeckFlow.Web/wwwroot/css/site-grixis.css
  - DeckFlow.Web/wwwroot/css/site-jund.css
  - DeckFlow.Web/wwwroot/css/site-planeswalker-dark.css
  - DeckFlow.Web/wwwroot/css/site-rakdos.css
  - DeckFlow.Web/wwwroot/css/site-sultai.css
  - DeckFlow.Web/wwwroot/css/site-common.css
  - DeckFlow.Web/Views/Deck/CutLab.cshtml
autonomous: false
requirements: [CLEANUP-3, CLEANUP-4]
must_haves:
  truths:
    - "All 9 remaining dark themes render Cut Lab delta up/down colors at AA (>=4.5:1) contrast against --panel"
    - "The Lock-all-<role> BUTTON pill visually distinguishes its locked (is-selected) state in every theme, WITHOUT regressing the radio-pill :has() selection UX"
    - "Mobile pool-row 'Package assignment' label no longer truncates"
    - "Nyx-mobile commander-badge overlap ends either fixed (CSS applied + re-verified) or closed-with-reason (screenshot-backed) — never contingent"
    - "Item-4 xmldoc garble and Manabase-copy leak are confirmed already-fixed (closed with reason)"
  artifacts:
    - path: "DeckFlow.Web/wwwroot/css/site-sultai.css"
      provides: "--cutlab-delta-up/down :root overrides (representative of all 9 dark themes)"
      contains: "--cutlab-delta-down"
    - path: "DeckFlow.Web/wwwroot/css/site-common.css"
      provides: "button.manabase-pill.is-selected button-variant-only selected-state rule"
      contains: "button.manabase-pill.is-selected"
  key_links:
    - from: "site-*.css :root (9 dark themes)"
      to: "site-common.css .cutlab-delta__value--up/--down"
      via: "var(--cutlab-delta-up/down) token seam"
      pattern: "--cutlab-delta-(up|down):"
    - from: "CutLab.cshtml Lock-all BUTTON (:435, class manabase-pill is-selected)"
      to: "site-common.css button.manabase-pill.is-selected"
      via: "button-scoped class selector (radio labels keep :has(> input:checked) untouched)"
      pattern: "button\\.manabase-pill\\.is-selected"
---

<objective>
Item 3 + Item 4 (CSS/visual cleanup). Add AA-passing `--cutlab-delta-up/down` `:root` overrides to
the 9 remaining dark themes (Nyx already has them). Add a BUTTON-SCOPED `button.manabase-pill.is-selected`
rule so the Lock-all-<role> button (which has no nested radio) shows a selected state — WITHOUT
touching the radio-pill `:has(> input:checked)` mechanism. Shorten the one over-long mobile pool-row
label. Resolve the Nyx-mobile badge overlap to a definite fixed-or-closed outcome. Confirm-and-close
the two item-4 sub-items already fixed in a prior phase (xmldoc garble, Manabase castability copy).

Purpose: Retire the dark-theme contrast + cosmetic tech-debt. Visual-only; no server/client logic.
Output: 9 theme token overrides, one button-only selected-pill rule, one shortened mobile label, and
a screenshot-verified cosmetic pass with a decisive Nyx-badge outcome.

NOTE (gate): No UI-SPEC.md required — this only adjusts token values and a missing selected-state
rule for ALREADY-SHIPPED UI; no new visual/interaction design is being introduced.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/cut-lab/phases/107-cut-lab-tech-debt-cleanup/107-CONTEXT.md
@.planning/workstreams/cut-lab/phases/107-cut-lab-tech-debt-cleanup/107-RESEARCH.md

Project CSS constraint (CLAUDE.md): layout/structural CSS stays in site-common.css; ONLY token
values go in each theme file's `:root`. The token seam already exists in site-common.css:4557-4563
(`color: var(--cutlab-delta-up, var(--success))` / `var(--cutlab-delta-down, var(--danger))`) — do
NOT modify those rules. `--panel` is the correct background to contrast-check against.

Precomputed WCAG contrast (from RESEARCH Item 3, closed-form — no tooling needed):
- `--cutlab-delta-up: #4ade80` (green): worst-case 6.99:1 across all 9 panels → PASS everywhere.
- `--cutlab-delta-down: #fc8181` (red): worst-case 4.99:1 → PASS everywhere.
- Do NOT reuse Nyx's `#f87171` red — it fails AA (4.41:1) on sultai + planeswalker-dark.
The 9 dark themes needing overrides: abzan, dimir, esper, golgari, grixis, jund, planeswalker-dark,
rakdos, sultai. Nyx already has overrides — leave it unchanged.

CRITICAL — radio-pill selection is deliberately `:has()`-only (site-common.css:2538-2544 + its
comment): the static server-rendered `.is-selected` class is INTENTIONALLY NOT styled for radio
pills, because it cannot clear on click (would leave two pills lit per group). A GLOBAL
`.manabase-pill.is-selected` rule would REGRESS that UX — the Bracket/PlayExperience radio labels
(CutLab.cshtml:171,:190) also carry `is-selected`. Therefore the new rule MUST be scoped to the
button variant only: `button.manabase-pill.is-selected` (the Lock-all control at CutLab.cshtml:435
is a `<button>`, has NO nested radio). Radio pills stay `:has(> input:checked)`-driven, untouched.

Already-fixed (RESEARCH Item 4 sub-items 1-2, confirmed by grep this session — CLOSE, no work):
- CutLabPoolValidator.cs xmldoc now reads the corrected wording ("...excluding the commander — the
  commander is the plus one").
- No "castability table" / "All modes show" copy remains in CutLab.cshtml (zero hits).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add --cutlab-delta-up/down overrides to the 9 remaining dark themes</name>
  <read_first>
    - DeckFlow.Web/wwwroot/css/site-nyx.css (:47 `--cutlab-delta-down`, :57 `--cutlab-delta-up`) —
      the reference pattern for where/how the token override sits inside `:root`.
    - DeckFlow.Web/wwwroot/css/site-common.css:4557-4563 — the token seam (DO NOT MODIFY).
    - The `:root { ... }` block near the top of each of the 9 target theme files (grep `:root`).
  </read_first>
  <action>
    In each of the 9 dark theme files (site-abzan, site-dimir, site-esper, site-golgari, site-grixis,
    site-jund, site-planeswalker-dark, site-rakdos, site-sultai), add two declarations inside the
    existing `:root` block: `--cutlab-delta-up: #4ade80;` and `--cutlab-delta-down: #fc8181;`.
    Place them near other Cut-Lab / delta tokens if present, else at the end of `:root`. Do NOT edit
    site-common.css in this task, do NOT touch light themes, do NOT change Nyx. LF endings; add only
    the two new lines per file (changed-lines-only gate).
  </action>
  <acceptance_criteria>
    - For each of the 9 files: `grep -c -- "--cutlab-delta-up:\|--cutlab-delta-down:" <file>` returns 2.
    - Every value is `#4ade80` (up) / `#fc8181` (down); `grep -rn "#f87171" DeckFlow.Web/wwwroot/css/site-{abzan,dimir,esper,golgari,grixis,jund,planeswalker-dark,rakdos,sultai}.css` returns zero (Nyx's red not copied).
    - site-nyx.css and all 12 light themes are unchanged (`git diff --stat` lists only the 9 dark files).
    - Each new declaration sits inside a `:root { }` block (not a `.class` rule).
  </acceptance_criteria>
  <verify>
    <automated>for f in abzan dimir esper golgari grixis jund planeswalker-dark rakdos sultai; do echo -n "$f "; grep -c -- "--cutlab-delta-up:\|--cutlab-delta-down:" DeckFlow.Web/wwwroot/css/site-$f.css; done</automated>
  </verify>
  <done>All 9 dark themes carry the AA-passing delta token pair in :root; Nyx + light themes + the token seam untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Add BUTTON-scoped .is-selected pill rule + shorten the mobile pool-row label</name>
  <read_first>
    - DeckFlow.Web/wwwroot/css/site-common.css:2538-2550 — the `:has(> input:checked)` rule AND its
      comment explaining why the static `.is-selected` class is deliberately NOT styled for radio
      pills. The new rule must mirror this rule's selected treatment (background var(--accent...),
      border-color var(--accent...), color var(--on-accent...), font-weight 600) but be BUTTON-scoped.
    - DeckFlow.Web/Views/Deck/CutLab.cshtml:435-438 (Lock-all `<button class="manabase-pill
      @(group.AllLockableMembersLocked ? "is-selected" : null)">`, NO nested `<input type=radio>`),
      and :171 / :190 (Bracket / PlayExperience `<label>` pills that carry is-selected AND nested radios
      — these MUST keep relying solely on :has(> input:checked)).
    - DeckFlow.Web/Views/Deck/CutLab.cshtml:334 (`<td data-label="Package assignment">`) and the
      mobile responsive-table rule site-common.css:1230-1252 (fixed `grid-template-columns: 6.5rem 1fr`
      + `content: attr(data-label)` + `white-space: nowrap`) that truncates the 19-char label.
    - DeckFlow.Web/Views/Deck/CutLab.cshtml:293 (desktop `<th>Package assignment` — a SEPARATE string,
      must stay unchanged).
  </read_first>
  <action>
    (a) In site-common.css, add a `button.manabase-pill.is-selected` rule (button variant ONLY —
    NOT a bare `.manabase-pill.is-selected`) applying the same selected token treatment as the
    `:has(> input:checked)` rule (accent background/border, on-accent color, font-weight 600). Add a
    short `/* Why: */` comment noting this is button-only because radio pills stay :has()-driven to
    avoid the stale-double-highlight bug the neighboring comment describes. Do NOT modify the existing
    `:has(> input:checked)` rule. The `<label>` radio pills at :171/:190 must NOT gain any new selected
    styling path (they are not `<button>`, so they will not match).
    (b) In CutLab.cshtml:334, change ONLY the mobile `data-label` value from `"Package assignment"`
    to `"Package"`. Leave the desktop `<th>` at :293 unchanged. This is the lowest-risk fix (does not
    touch the shared 6.5rem column). LF endings; changed-lines-only.
  </action>
  <acceptance_criteria>
    - `grep -c "button\.manabase-pill\.is-selected" DeckFlow.Web/wwwroot/css/site-common.css` >= 1;
      the rule sets the same accent/on-accent tokens as the `:has(> input:checked)` rule.
    - NO bare (unscoped) `.manabase-pill.is-selected` selector is introduced:
      `grep -n "manabase-pill\.is-selected" DeckFlow.Web/wwwroot/css/site-common.css` shows ONLY the
      `button.`-prefixed form.
    - The existing `.manabase-pill:has(> input:checked)` rule is byte-for-byte unchanged; radio pills
      (Bracket/PlayExperience) still rely solely on `:has(> input:checked)` — verified in the
      screenshot checkpoint (clicking one radio moves the highlight, no two-pills-lit double-highlight).
    - `grep -n 'data-label="Package assignment"' DeckFlow.Web/Views/Deck/CutLab.cshtml` returns zero;
      `grep -n 'data-label="Package"' DeckFlow.Web/Views/Deck/CutLab.cshtml` returns 1.
    - Desktop header string unchanged: `grep -n "Package assignment" DeckFlow.Web/Views/Deck/CutLab.cshtml`
      still finds the `<th>` occurrence (~:293).
    - `dotnet build DeckFlow.sln` clean (Razor compiles).
  </acceptance_criteria>
  <verify>
    <automated>dotnet build DeckFlow.sln && grep -c "button\.manabase-pill\.is-selected" DeckFlow.Web/wwwroot/css/site-common.css</automated>
  </verify>
  <done>Button-variant pills have a themed selected-state rule (radio pills untouched); the mobile package label is shortened; desktop header + layout columns unchanged; build clean.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Item 3: 9 dark themes now override --cutlab-delta-up/down (AA-passing green/red). Item 4: added
    button.manabase-pill.is-selected so the Lock-all-<role> button shows a selected state (radio pills
    untouched); shortened the mobile pool-row "Package" label. Nyx-mobile commander-badge overlap
    needs a decisive live check (RESEARCH: the site-common.css:1254-1257 grid override MAY already
    resolve it).
  </what-built>
  <how-to-verify>
    1. Start the test server: `scripts/run-web-test.sh` (sets DECKFLOW_DISABLE_AUTO_BROWSER=true —
       never opens a Windows-host browser).
    2. Drive Playwright headless (WSL): `npx --no-install playwright test` for the cut-lab specs, and
       capture theme×viewport screenshots per the existing cut-lab screenshot pattern
       (.planning/ui-design/cut-lab/screenshots/). Cover at least: a dark theme (e.g. sultai or
       planeswalker-dark) desktop showing the Compare delta up/down colors; Nyx MOBILE viewport
       showing the commander row badge; a theme showing the Lock-all-<role> BUTTON pill in its locked
       (is-selected) vs unlocked state; the Bracket/PlayExperience RADIO pills (click between options
       to confirm NO two-pills-lit double-highlight — the :has() UX must be intact); a MOBILE viewport
       showing the pool-row "Package" label not truncated.
    3. DECISIVE Nyx-badge outcome (no contingent ending):
       - If the Nyx-mobile commander badge does NOT overlap adjacent cell content → close-with-reason:
         record the screenshot as evidence that the existing :1254-1257 grid override already resolves
         it; note this closure in the SUMMARY.
       - If it DOES still overlap → apply a targeted CSS fix around site-common.css:1254-1257 (extend
         the commander-cell grid-column/containment override) IN THIS PLAN, rebuild, re-run this
         screenshot step, and only then proceed. Do not defer.
    4. Confirm all of: delta colors legible on dark panels; Lock-all button locked-state visibly
       distinct; radio-pill selection has no double-highlight; mobile "Package" label fully visible;
       Nyx badge resolved per step 3.
  </how-to-verify>
  <resume-signal>Type "approved" once every check passes and the Nyx-badge outcome is decided (fixed-and-reverified OR closed-with-screenshot); otherwise describe the failing theme/viewport (attach screenshot).</resume-signal>
</task>

</tasks>

<threat_model>
No new threats. CSS token values + one BUTTON-scoped CSS selector rule + one Razor `data-label`
attribute string. No new inputs, endpoints, scripts, or data flow; purely presentational. No
injection surface (no user data rendered differently).
</threat_model>

<verification>
- `dotnet build DeckFlow.sln` clean.
- 9 dark themes each show 2 delta tokens; `button.manabase-pill.is-selected` rule present (no bare
  variant); the `:has(> input:checked)` rule unchanged; mobile label shortened.
- Screenshot checkpoint approved (items 3 + 4 cosmetics visually confirmed across theme×viewport,
  radio-pill :has() UX intact, Nyx-badge decisively fixed-or-closed).
- Item-4 already-fixed sub-items (xmldoc, Manabase copy) recorded as closed-with-reason in SUMMARY.
</verification>

<success_criteria>
Dark-theme Cut Lab deltas pass AA; Lock-all BUTTON pill shows a selected state in all themes without
regressing radio-pill selection; mobile package label fits; Nyx-mobile badge fixed-or-closed; the two
already-fixed item-4 sub-items closed with reason.
</success_criteria>

<output>
Create `.planning/workstreams/cut-lab/phases/107-cut-lab-tech-debt-cleanup/107-02-SUMMARY.md` when done
(record the two already-fixed item-4 sub-items as closed-with-reason, and the DECISIVE Nyx-badge outcome —
fixed-and-reverified or closed-with-screenshot).
</output>
