---
phase: 104-goals-what-if-scenarios
plan: 06
type: execute
wave: 5
depends_on: [104-04, 104-05]
files_modified:
  - DeckFlow.Web/e2e/cut-lab-scenarios.spec.ts
  - DeckFlow.Web/e2e/cut-lab-whatif.spec.ts
autonomous: false
requirements: [GOAL-01, GOAL-02, GOAL-03]

must_haves:
  truths:
    - "An e2e spec proves save -> reload page -> load restores the whole session (pool/locks/intent/goals) from localStorage"
    - "An e2e spec proves pick A/B -> preview shows deltas -> Keep commits the swap (working list changes) and Discard leaves it unchanged"
    - "The what-if no-JS form fallback previews and commits via full-page re-render"
    - "The full test gate (dotnet build clean + xUnit CutLab + Vitest cut-lab* + Playwright cut-lab*) is green before phase close"
    - "Goals editor + scenarios panel + swap preview render correctly across themes (Classic/Nyx) and desktop+mobile"
  artifacts:
    - path: "DeckFlow.Web/e2e/cut-lab-scenarios.spec.ts"
      provides: "GOAL-02 save/reload/load round-trip e2e"
      contains: "scenario"
    - path: "DeckFlow.Web/e2e/cut-lab-whatif.spec.ts"
      provides: "GOAL-03 preview/keep/discard + no-JS e2e"
      contains: "whatif"
  key_links:
    - from: "e2e specs"
      to: "the Cut Lab feature flag"
      via: "setToolEnabled('tool.cut-lab.enabled', true) before driving the UI"
      pattern: "setToolEnabled"
---

<objective>
Prove the phase end-to-end and gate it: new Playwright specs for scenario save/reload/load (GOAL-02) and swap preview/keep/discard including the no-JS fallback (GOAL-03), the full-suite gate, theme/viewport screenshots, and a human-verify checkpoint. This closes the Nyquist Wave-0 e2e gaps from 104-VALIDATION.md.

Purpose: The goals/scenarios/swap surfaces only count as done when a browser can drive them and the whole suite is green across themes and viewports.
Output: two e2e specs, a green full-suite run, screenshots, and human sign-off.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/cut-lab/phases/104-goals-what-if-scenarios/104-VALIDATION.md
@.planning/workstreams/cut-lab/phases/104-goals-what-if-scenarios/104-02-SUMMARY.md
@.planning/workstreams/cut-lab/phases/104-goals-what-if-scenarios/104-04-SUMMARY.md
@.planning/workstreams/cut-lab/phases/104-goals-what-if-scenarios/104-05-SUMMARY.md

<interfaces>
E2e conventions (from DeckFlow.Web/e2e/cut-lab-structure.spec.ts): baseUrl 'http://localhost:5173'; themes[{classic:'site.css'}, {azorius:'site-azorius.css'}, {nyx:'site-nyx.css'}] via a theme cookie; viewports[{desktop:1440x1000},{mobile:430x932}]; screenshotDir '../../.planning/ui-design/cut-lab/screenshots'; setToolEnabled('tool.cut-lab.enabled', true) + acquireAdminLockForTest/releaseAdminLockForTest to enable the flag; a shared oversizedPool paste fixture.
Server must be started headless via scripts/run-web-test.sh (sets DECKFLOW_DISABLE_AUTO_BROWSER=true). Drive with `npx --no-install playwright test`. NEVER open a browser on the Windows host.
Selectors to target (from 104-02/04/05): goal inputs `[data-cut-lab-goal]`; scenario controls `[data-cut-lab-scenario-name]`, `[data-cut-lab-scenario-save]`, `[data-cut-lab-scenario-load]`, `[data-cut-lab-scenario-delete]`; whatif form `form[action$="/cut-lab/whatif"]` with `select[name="cardOut"]` / `select[name="cardIn"]` / intent buttons.
No-JS test: use a Playwright context with `javaScriptEnabled: false` for the whatif form fallback case.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scenario save/reload/load e2e (GOAL-02)</name>
  <read_first>
    - DeckFlow.Web/e2e/cut-lab-structure.spec.ts (flag enable via setToolEnabled + admin lock; oversizedPool fixture; import a pool and reach the results page; theme/viewport helpers)
    - DeckFlow.Web/e2e/support/admin-tools.ts and support/admin-lock.ts (helper signatures)
    - DeckFlow.Web/wwwroot/ts/cut-lab.ts (scenario save/list/load/delete are folded in here per 104-04 — there is no standalone cut-lab-scenarios.ts) + the Scenarios panel markup (104-04 SUMMARY) for selectors
  </read_first>
  <action>
    Create `DeckFlow.Web/e2e/cut-lab-scenarios.spec.ts`. Enable the flag, import the oversized pool, and reach the Cut Lab results page. Edit at least one goal turn and lock at least one card so the saved snapshot is distinguishable. Type a scenario name, click Save, and assert the scenario appears in the list. Reload the page (fresh import or empty page), click Load on the saved scenario, and assert the session is restored: the working-list/pool state, the locked card, the declared intent, and the edited goal turn all match what was saved. Add an assertion that a 21st save surfaces the cap message (or that Save is blocked at the cap) — a lighter-weight check is acceptable if 20 saves is too slow (e.g. seed 20 index entries via `page.evaluate` on localStorage, then attempt one more). Keep the spec deterministic and self-cleaning (clear the scenario localStorage keys in afterEach).
  </action>
  <verify>
    <automated>DECKFLOW_DISABLE_AUTO_BROWSER=true nohup scripts/run-web-test.sh >/tmp/cutlab-web-scenarios.log 2>&1 & until curl -sf http://localhost:5173/ >/dev/null 2>&1; do sleep 1; done; (cd DeckFlow.Web && npx --no-install playwright test e2e/cut-lab-scenarios.spec.ts); RC=$?; fuser -k 5173/tcp 2>/dev/null || true; exit $RC</automated>
  </verify>
  <acceptance_criteria>
    - The scenarios spec passes: save shows in list; load after reload restores pool + lock + intent + edited goal turn; cap message asserted.
    - Spec cleans up its localStorage keys (no cross-test bleed).
  </acceptance_criteria>
  <done>A browser proves named scenarios round-trip the full session across a page reload.</done>
</task>

<task type="auto">
  <name>Task 2: What-if swap preview/keep/discard + no-JS e2e (GOAL-03)</name>
  <read_first>
    - DeckFlow.Web/e2e/cut-lab-structure.spec.ts (same setup helpers)
    - The what-if section markup + endpoints (104-05 SUMMARY) for selectors
    - DeckFlow.Web/Controllers/CutLabController.cs Whatif action (no-JS behavior)
  </read_first>
  <action>
    Create `DeckFlow.Web/e2e/cut-lab-whatif.spec.ts`. Enable the flag, import the oversized pool, and accept at least one cut so the cut pile is non-empty. JS path: select card A (a working-list card) and card B (a cut-pile card), click Preview, assert delta rows render and the working list has NOT changed yet; click Discard, assert the preview clears and state is unchanged; Preview again, click Keep, assert A now appears in the cuts-made list under "What-if swap" and B is back in the working list. Add a guard case: assert locked/commander cards are absent from the card-A `<select>`. No-JS path: open a `javaScriptEnabled: false` context, submit the whatif form with intent=preview and assert deltas render server-side, then intent=keep and assert the full-page re-render reflects the committed swap.
  </action>
  <verify>
    <automated>DECKFLOW_DISABLE_AUTO_BROWSER=true nohup scripts/run-web-test.sh >/tmp/cutlab-web-whatif.log 2>&1 & until curl -sf http://localhost:5173/ >/dev/null 2>&1; do sleep 1; done; (cd DeckFlow.Web && npx --no-install playwright test e2e/cut-lab-whatif.spec.ts); RC=$?; fuser -k 5173/tcp 2>/dev/null || true; exit $RC</automated>
  </verify>
  <acceptance_criteria>
    - The whatif spec passes: preview is non-destructive; Discard reverts; Keep commits (A cut under "What-if swap", B restored); locked cards excluded from A; no-JS form previews and commits.
  </acceptance_criteria>
  <done>A browser proves the full swap flow (JS and no-JS), including non-destructive preview and atomic Keep.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Full-suite gate + theme/viewport screenshots + human verify</name>
  <what-built>
    Goals editor (GOAL-01), named scenarios (GOAL-02), and what-if swap (GOAL-03) on the Cut Lab tool, all behind `tool.cut-lab.enabled`. Full automated gate: `dotnet build` clean, xUnit CutLab suite, Vitest `cut-lab*`, and Playwright `cut-lab*` specs. Screenshots captured at Classic + Nyx themes x desktop + mobile for the goals, scenarios, and swap-preview surfaces (screenshotDir `.planning/ui-design/cut-lab/screenshots`).
  </what-built>
  <how-to-verify>
    1. Run the full gate: `dotnet build` (clean); `dotnet test DeckFlow.Web.Tests --filter "FullyQualifiedName~CutLab"`; `cd DeckFlow.Web && npx vitest run ts-tests/cut-lab*`; then the e2e specs against a BACKGROUND server (never the blocking `run-web-test.sh &&` form): `DECKFLOW_DISABLE_AUTO_BROWSER=true nohup scripts/run-web-test.sh >/tmp/cutlab-web-gate.log 2>&1 & until curl -sf http://localhost:5173/ >/dev/null 2>&1; do sleep 1; done; (cd DeckFlow.Web && npx --no-install playwright test e2e/cut-lab*.spec.ts); RC=$?; fuser -k 5173/tcp 2>/dev/null || true; exit $RC` (or use Playwright's `webServer` config to own server lifecycle). All green.
    2. Start the app headless (`scripts/run-web-test.sh`), enable `tool.cut-lab.enabled`, import an oversized pool, and manually confirm: editing a goal turn changes the goal row's probability; casual play-experience annotates the representative-line goal as uncapped; saving/loading a named scenario restores the session; previewing a swap shows deltas and Keep/Discard behave (Discard non-destructive, Keep commits).
    3. Review the theme x viewport screenshots for layout correctness (no overflow/contrast issues) on Classic and Nyx, desktop and mobile.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness -> app | e2e drives the real flag-gated endpoints; no new production surface added here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-104-14 | Tampering | e2e flag toggling / admin lock | mitigate | Reuse setToolEnabled + acquireAdminLockForTest/releaseAdminLockForTest exactly as cut-lab-structure.spec.ts; serialize admin-touching specs via the shared lock to avoid flake. |
| T-104-SC | Tampering | package installs | accept | No package installs in this plan; all frameworks already present. |
</threat_model>

<verification>
- Both new specs green via `npx --no-install playwright test` against the headless server (no Windows-host browser).
- Full gate green: dotnet build clean + xUnit CutLab + Vitest cut-lab* + Playwright cut-lab*.
- Human confirms goals/scenarios/swap behavior and screenshot layout across Classic/Nyx x desktop/mobile.
</verification>

<success_criteria>
All three GOAL requirements are proven end-to-end in a real browser (JS and no-JS where applicable), the full suite is green, and a human has signed off on behavior and cross-theme/viewport layout.
</success_criteria>

<output>
Create `.planning/workstreams/cut-lab/phases/104-goals-what-if-scenarios/104-06-SUMMARY.md` when done.
</output>
