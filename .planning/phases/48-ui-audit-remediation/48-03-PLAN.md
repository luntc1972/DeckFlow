---
phase: 48-ui-audit-remediation
plan: 03
type: execute
wave: 2
depends_on: ["48-01", "48-02"]
files_modified:
  - .planning/phases/48-ui-audit-remediation/48-AUDIT.md
autonomous: false
requirements: [UIR-02, UIR-03]

must_haves:
  truths:
    - "Every remediated finding (F1-F7) is verified with browser screenshots at mobile (<= 768px) AND desktop (>= 1024px)"
    - "The re-scored 6-pillar total reaches >= 20/24"
    - "Token changes are spot-checked in a sample of guild themes (light + dark), including site-commander-table, not just Classic"
    - "Final score is re-verified against the deployed deckflow.gg site (per UIR-03 / SC3 caveat)"
  artifacts:
    - path: ".planning/phases/48-ui-audit-remediation/48-AUDIT.md"
      provides: "Post-remediation re-score section appended (>= 20/24) with per-finding 2-viewport screenshot evidence and deployed-site confirmation"
      contains: "Post-Remediation Re-Score"
  key_links:
    - from: "screenshots in logs/audit-shots/"
      to: "48-AUDIT.md re-score section"
      via: "per-finding evidence references"
      pattern: "Post-Remediation"
---

<objective>
Verify the Plan 01 + Plan 02 remediations in the browser at two viewports, re-score the 6-pillar audit to confirm >= 20/24, and confirm the result against the deployed deckflow.gg site (UIR-03).

Purpose: UIR-03 mandates browser-screenshot verification at >= 2 viewports for every CSS/layout change — grep is insufficient. This plan is the goal-backward gate: it proves the phase goal (score >= 20/24, 2-viewport verified) is actually achieved, not just coded.
Output: A "Post-Remediation Re-Score" section appended to `48-AUDIT.md` with per-finding evidence, plus a checkpoint where the operator confirms the deployed-site result before close.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/48-ui-audit-remediation/48-AUDIT.md
@.planning/phases/48-ui-audit-remediation/48-CONTEXT.md
@.planning/phases/48-ui-audit-remediation/48-01-SUMMARY.md
@.planning/phases/48-ui-audit-remediation/48-02-SUMMARY.md

<verification_notes>
- The original audit used gstack headless Chromium, 8 pages x 2 viewports (desktop 1280x720, mobile 375x812), shots in logs/audit-shots/. Reuse that harness/dimensions for an apples-to-apples re-score.
- Start the local web app for screenshotting WITHOUT popping a Windows browser: set DECKFLOW_DISABLE_AUTO_BROWSER=true (per project memory — this is the live env var name; do NOT use the stale MTGDECKSTUDIO_ name). Local Classic ~= deployed default for static CSS, but the FINAL UIR-03 re-score must screenshot the DEPLOYED deckflow.gg before close (audit scope caveat 1).
- Theme spot-check: there are 24 user-selectable themes (13 full-forks incl. site-commander-table + 11 @import). Capture at least TWO LIGHT themes — one @import/site.css-family (e.g. Classic or Jeskai) AND site-commander-table (a separate full-fork with its own warm parchment --bg/--panel/--muted/--line that Plan 01 edited independently) — plus one DARK theme (site-planeswalker-dark), to confirm token changes propagated across BOTH theme architectures and dark-mode panel/bg inversion (Plan 01 Task 2) reads correctly.
- Pages to recapture (closing-path coverage): Home (F1/F2/F4), Card Lookup + Ask a Judge (F6/F7), one tool page with panels (Deck Comparison or Primer for F2/F4). Mobile + desktop each.
</verification_notes>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Capture post-remediation screenshots at 2 viewports (local) + per-finding verification</name>
  <files>.planning/phases/48-ui-audit-remediation/48-AUDIT.md</files>
  <read_first>
    - .planning/phases/48-ui-audit-remediation/48-AUDIT.md (Findings F1-F7 + original screenshot method/dimensions)
    - .planning/phases/48-ui-audit-remediation/48-01-SUMMARY.md (token propagation table)
    - .planning/phases/48-ui-audit-remediation/48-02-SUMMARY.md (layout/markup changes)
  </read_first>
  <action>
    Build + run the local web app with `DECKFLOW_DISABLE_AUTO_BROWSER=true` (background) on its dev URL. Using the same gstack/headless-Chromium harness and dimensions as the original audit (desktop 1280x720, mobile 375x812), capture post-remediation screenshots into `logs/audit-shots/` (post-remediation subfolder) for: Home, Card Lookup, Ask a Judge, and one panel-heavy tool page (Deck Comparison or Deck Primer) — each at desktop AND mobile. Repeat Home + one tool page under a DARK theme (site-planeswalker-dark) AND under TWO light themes: one site.css-family theme (Classic or a guild @import theme) AND site-commander-table (the separate warm-parchment full-fork edited independently in Plan 01) — to spot-check token propagation across BOTH theme architectures (full-fork + @import) and the commander-table fork specifically.

    For each remediated finding F1-F7, record a one-line verdict + the screenshot path proving it: F1 icons+elevation visible (light+dark); F2 cards separate from bg (token delta + resting shadow) light+dark incl. commander-table; F3 smallest text >= 12.75px; F4 distinct heading/label tiers; F5 muted text legible/AA (confirm in commander-table's warm palette too); F6 no dead band on short pages; F7 example/next-step panel present. No finding may be marked done on grep alone.

    Append a "Post-Remediation Re-Score" section to 48-AUDIT.md with the 6-pillar table re-scored (target Visuals 3, Color 4, Typography 4 -> total >= 20/24), the per-finding evidence table, and the 2-viewport + light/dark + commander-table theme spot-check note.
  </action>
  <verify>
    <automated>ls logs/audit-shots/ 2>/dev/null | grep -i "post\|remed\|desktop\|mobile" | head</automated>
    <automated>ls logs/audit-shots/ 2>/dev/null | grep -i "commander-table\|commander_table\|commandertable" | head || echo "ensure a site-commander-table light-theme shot exists"</automated>
    <automated>grep -q "Post-Remediation Re-Score" .planning/phases/48-ui-audit-remediation/48-AUDIT.md && echo "re-score section appended"</automated>
  </verify>
  <acceptance_criteria>
    - SCREENSHOT: Post-remediation shots exist in logs/audit-shots/ for Home + Card Lookup + Ask a Judge + one panel page, at BOTH desktop (>= 1024px) and mobile (<= 768px).
    - SCREENSHOT: At least one DARK theme (planeswalker-dark) AND two LIGHT themes captured — one site.css-family theme AND site-commander-table (the independent warm-parchment full-fork) — to spot-check token propagation across both theme architectures.
    - SOURCE: 48-AUDIT.md has a "Post-Remediation Re-Score" section: re-scored 6-pillar table totaling >= 20/24 + a per-finding (F1-F7) evidence table referencing screenshot paths; commander-table noted in the F2/F5 token spot-check.
    - No finding closed on grep/static analysis alone (UIR-03).
  </acceptance_criteria>
  <done>All closing-path findings are browser-verified at 2 viewports across light (incl. commander-table) + dark themes; local re-score >= 20/24 recorded.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Operator confirms deployed deckflow.gg final re-score (UIR-03 / SC3)</name>
  <what-built>
    Plans 01+02 implemented the token + layout/markup remediations (F1-F7). Task 1 of this plan re-scored the LOCAL build to >= 20/24 with 2-viewport, light+dark screenshot evidence (including the site-commander-table fork) appended to 48-AUDIT.md. The audit's scope caveat 1 and SC3 require the FINAL score to be confirmed against the DEPLOYED deckflow.gg site, which only the operator can deploy/confirm (push to main -> Render auto-deploy is an explicit operator action; AI does not push main).
  </what-built>
  <how-to-verify>
    1. Operator pushes the v1.7 branch / merges so the CSS+markup changes deploy to deckflow.gg (operator's explicit push — AI does not push main).
    2. After Render deploy completes, visit https://www.deckflow.gg at desktop (>= 1024px) and mobile (<= 768px):
       - Home: hub cards show icons + elevation; cards clearly separate from the page background; section labels read as a distinct tier.
       - Switch to a dark theme (e.g. planeswalker-dark) and to site-commander-table (warm parchment) and one other guild theme: confirm panels still lift (dark inversion correct, commander-table panel/bg delta now visible) and icons inherit theme color.
       - Card Lookup / Ask a Judge: content is capped/centered and ends with an example/next-step panel (no large dead band).
       - Smallest helper/footer text is comfortably legible (>= ~12.75px) and muted text is readable (incl. commander-table's warm muted).
    3. Confirm the deployed result matches the local re-score (>= 20/24) and no finding regressed.
  </how-to-verify>
  <acceptance_criteria>
    - Operator confirms the deployed deckflow.gg site reflects the remediations at both viewports across light (incl. commander-table) + dark themes.
    - Operator confirms the deployed score is >= 20/24 (SC2) — or reports specific regressions for a gap-closure pass.
  </acceptance_criteria>
  <resume-signal>Type "approved" once deckflow.gg is verified at >= 20/24 at both viewports, or describe the regressions to fix.</resume-signal>
</task>

</tasks>

<verification>
- Post-remediation screenshots exist at 2 viewports for the closing-path pages, including a dark theme + two light themes (one site.css-family + site-commander-table) for the fork spot-check.
- 48-AUDIT.md "Post-Remediation Re-Score" section shows >= 20/24.
- Operator has confirmed the DEPLOYED deckflow.gg result (UIR-03 / SC3) at both viewports.
</verification>

<success_criteria>
- UIR-02 closing-path verified achieved: total audit score >= 20/24.
- UIR-03 satisfied: every remediated finding screenshot-verified at mobile + desktop; final score confirmed on the deployed site.
</success_criteria>

<output>
Create `.planning/phases/48-ui-audit-remediation/48-03-SUMMARY.md` when done.
</output>
</content>
