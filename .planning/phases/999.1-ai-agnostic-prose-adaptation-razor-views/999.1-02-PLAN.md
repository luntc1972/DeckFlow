---
phase: 999.1-ai-agnostic-prose-adaptation-razor-views
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - DeckFlow.Web/Views/Deck/DeckComparison.cshtml
autonomous: true
requirements: [D-01, D-02, D-05]
must_haves:
  truths:
    - "DeckComparison.cshtml above-selector prose uses universal noun 'your AI' except the line-145 Phase-12 enumerated lede."
    - "DeckComparison.cshtml below-selector prose injects @aiPlatform.DisplayName via AiPlatform.Normalize(Model.Request.TargetAiPlatform)."
    - "Phase 13 D-08 / Phase 10 invariants on DeckComparison.cshtml are not touched: data-cache-key='chatgpt-deck-comparison', data-chatgpt-comparison-form, data-chatgpt-comparison-current-step, chatgpt-step-eyebrow CSS class all survive byte-identical."
  artifacts:
    - path: "DeckFlow.Web/Views/Deck/DeckComparison.cshtml"
      provides: "Hybrid-prose Razor view with AiPlatform.Normalize block"
      contains: "AiPlatform.Normalize(Model.Request.TargetAiPlatform)"
  key_links:
    - from: "DeckFlow.Web/Views/Deck/DeckComparison.cshtml"
      to: "DeckFlow.Web.Models.AiPlatform.Normalize"
      via: "Razor @{ var aiPlatform = AiPlatform.Normalize(...); } + @aiPlatform.DisplayName"
      pattern: "AiPlatform\\.Normalize"
---

<objective>
Rewrite the 12 ChatGPT hits in `DeckComparison.cshtml` using the Hybrid pattern (D-01) anchored on the Step 2 `_AiSelector` boundary at line 320. Line-145 enumerated page-lede is preserved per D-05. Phase 13 D-08 TS/CSS-coupled identifiers (`data-cache-key="chatgpt-deck-comparison"`, `data-chatgpt-comparison-form`, `chatgpt-step-eyebrow` class) survive untouched.

Purpose: Strip the ChatGPT brand from the Deck Comparison page so it reads correctly for any AiPlatform selection.
Output: Edited `DeckComparison.cshtml` with brand-neutral prose above the selector and `@aiPlatform.DisplayName` injection below.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/999.1-ai-agnostic-prose-adaptation-razor-views/999.1-CONTEXT.md
@DeckFlow.Web/Models/AiPlatform.cs
@DeckFlow.Web/Views/Deck/DeckComparison.cshtml
@DeckFlow.Web/Views/Shared/_AiSelector.cshtml
@CLAUDE.md

<interfaces>
DeckComparison.cshtml selector boundary:
- Line 320 contains `@await Html.PartialAsync("_AiSelector", Model.Request.TargetAiPlatform)`.
- Lines physically ABOVE line 320: ABOVE-selector → universal noun `your AI`.
- Lines physically BELOW line 320: BELOW-selector → `@aiPlatform.DisplayName` injection.
- EDGE CASE: Lines 326-328 are physically BELOW line 320 (immediately following the selector partial) but are AT-selector step-2 instructional prose. Per D-01 edge case clause in CONTEXT.md ("AT-selector prose is treated as ABOVE"), these AT-selector lines receive the universal-noun treatment, NOT `@aiPlatform.DisplayName` injection. They are listed in the ABOVE-SELECTOR substitution table below for that reason — the table groups by *treatment*, not by physical line position.

DeckComparison.cshtml ChatGPT hits (12 total):
- Line 145 page-lede `"ChatGPT, Claude, or Gemini"` → KEEP (D-05 lede exception).
- Line 146 `<p class="lede">...ChatGPT-ready comparison packet...` → ABOVE-selector (universal).
- Line 158 step-instruction "paste <code>30-comparison-prompt.txt</code> into ChatGPT" → ABOVE-selector.
- Line 226 "the current ChatGPT workflow" → ABOVE-selector.
- Line 234 "the comparison prompt includes these definitions so ChatGPT judges the lists..." → ABOVE-selector.
- Line 316 "paste it into ChatGPT, then ask for the final answer..." → ABOVE-selector (still physically before line-320 selector).
- Line 326 "<li>Paste it into ChatGPT.</li>" → AT-selector (physically below 320), treated as ABOVE per D-01 edge case.
- Line 327 "Have ChatGPT return the readable comparison..." → AT-selector (physically below 320), treated as ABOVE per D-01 edge case.
- Line 328 "Use <code>32-comparison-follow-up-prompt.txt</code> later if you continue the ChatGPT thread..." → AT-selector (physically below 320), treated as ABOVE per D-01 edge case.
- Line 480 "Paste this into ChatGPT to get the side-by-side comparison..." → BELOW-selector → `@aiPlatform.DisplayName`.
- Line 495 "ChatGPT should return a <code>deck_comparison</code> JSON object matching this schema." → BELOW-selector.
- Line 509 "Paste this into the same ChatGPT thread after follow-up questions..." → BELOW-selector.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add AiPlatform.Normalize Razor block to top of DeckComparison.cshtml</name>
  <files>DeckFlow.Web/Views/Deck/DeckComparison.cshtml</files>
  <read_first>
    - .planning/phases/999.1-ai-agnostic-prose-adaptation-razor-views/999.1-CONTEXT.md (D-01 hybrid pattern, code_context plumbing snippet)
    - DeckFlow.Web/Views/Deck/DeckComparison.cshtml (top — confirm existing `@{ }` block location, `@model` directive, and `@using` directives)
    - DeckFlow.Web/Models/AiPlatform.cs (Normalize signature)
    - DeckFlow.Web/Views/Shared/_AiSelector.cshtml (reference pattern)
  </read_first>
  <action>
    1. Locate the existing top-of-file `@{ ... }` block in `DeckComparison.cshtml`.
    2. Add inside: `var aiPlatform = AiPlatform.Normalize(Model.Request.TargetAiPlatform);` (confirm the model path matches the `_AiSelector` invocation on line 320 — use the same expression).
    3. If `DeckFlow.Web.Models` is not already imported (via `@using DeckFlow.Web.Models` at top or implicit via `_ViewImports.cshtml`), either add `@using DeckFlow.Web.Models` after `@model` OR fully-qualify the call (`DeckFlow.Web.Models.AiPlatform.Normalize(...)`); match the file's existing convention.
    4. Do not reformat existing locals. Preserve LF line endings.
  </action>
  <verify>
    <automated>grep -c 'AiPlatform.Normalize(Model.Request.TargetAiPlatform)' DeckFlow.Web/Views/Deck/DeckComparison.cshtml</automated>
    <automated>dotnet build DeckFlow.Web/DeckFlow.Web.csproj -c Debug --nologo -v quiet</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'AiPlatform.Normalize(Model.Request.TargetAiPlatform)' DeckFlow.Web/Views/Deck/DeckComparison.cshtml` returns `1`.
    - `dotnet build DeckFlow.Web/DeckFlow.Web.csproj -c Debug` exits 0.
  </acceptance_criteria>
  <done>The `aiPlatform` local is declared; the project builds.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite ChatGPT prose in DeckComparison.cshtml using Hybrid pattern</name>
  <files>DeckFlow.Web/Views/Deck/DeckComparison.cshtml</files>
  <read_first>
    - .planning/phases/999.1-ai-agnostic-prose-adaptation-razor-views/999.1-CONTEXT.md (D-01 hybrid pattern + edge case "AT-selector prose is treated as ABOVE", D-05 universal noun, D-08 carve-out)
    - DeckFlow.Web/Views/Deck/DeckComparison.cshtml (full file)
  </read_first>
  <action>
    Apply Hybrid substitutions. KEEP line 145 enumerated lede unchanged per D-05.

    ABOVE-SELECTOR treatment (universal noun `your AI` — Claude's Discretion picks natural variant). The lines below are grouped by *treatment* not by physical position. Lines 146–316 are physically above the line-320 `_AiSelector` partial; lines 326–328 are physically immediately AT/just-below the partial but are treated as ABOVE per the D-01 edge case clause in CONTEXT.md ("AT-selector prose is treated as ABOVE"):
    - Line 146 "generate a ChatGPT-ready comparison packet" → "generate a comparison packet ready for your AI" (rephrase to keep flow natural; OR shorter: "generate an AI-ready comparison packet" — pick the variant that reads best; do NOT keep the literal "ChatGPT").
    - Line 158 "paste <code>30-comparison-prompt.txt</code> into ChatGPT" → "paste <code>30-comparison-prompt.txt</code> into your AI"
    - Line 226 "Each side accepts the same sources as the current ChatGPT workflow" → "Each side accepts the same sources as the current Deck Analysis workflow" (page-named — referring to the sibling DeckAnalysis page workflow, NOT to ChatGPT-the-product; this is a back-reference to another DeckFlow page, so use the page name not `your AI`). Claude's Discretion.
    - Line 234 "the comparison prompt includes these definitions so ChatGPT judges the lists..." → "...so your AI judges the lists..."
    - Line 316 "paste it into ChatGPT, then ask for the final answer..." → "paste it into your AI, then ask..."
    - Line 326 (AT-selector → treated as ABOVE per D-01 edge case) "<li>Paste it into ChatGPT.</li>" → "<li>Paste it into your AI.</li>"
    - Line 327 (AT-selector → treated as ABOVE per D-01 edge case) "Have ChatGPT return the readable comparison plus a fenced..." → "Have your AI return the readable comparison plus a fenced..."
    - Line 328 (AT-selector → treated as ABOVE per D-01 edge case) "later if you continue the ChatGPT thread" → "later if you continue the conversation with your AI" (natural-reading variant; Claude's Discretion).

    BELOW-SELECTOR (lines 480, 495, 509 — inject `@aiPlatform.DisplayName`):
    - Line 480 "Paste this into ChatGPT to get the side-by-side comparison..." → "Paste this into @aiPlatform.DisplayName to get the side-by-side comparison..."
    - Line 495 "ChatGPT should return a <code>deck_comparison</code> JSON object matching this schema." → "@aiPlatform.DisplayName should return a <code>deck_comparison</code> JSON object matching this schema."
    - Line 509 "Paste this into the same ChatGPT thread after follow-up questions..." → "Paste this into the same @aiPlatform.DisplayName conversation after follow-up questions..." (use "conversation" instead of "thread" to align with the cross-AI vocabulary, or keep "thread"; Claude's Discretion).

    Do NOT touch:
    - Line 145 lede `ChatGPT, Claude, or Gemini` (D-05 exception preserved).
    - Phase 13 D-08 identifiers on line 171 (`data-cache-key="chatgpt-deck-comparison"`, `data-chatgpt-comparison-form`, `data-chatgpt-comparison-current-step`) — Phase 13 D-08 preserved.
    - `chatgpt-step-eyebrow` CSS class on line 314 — Phase 13 D-08 preserved.
    - Touch only the prose lines. Preserve LF line endings.
  </action>
  <verify>
    <automated>bash -c "count=$(grep -c 'ChatGPT' DeckFlow.Web/Views/Deck/DeckComparison.cshtml); test $count -le 1 || { echo \"FAIL: $count ChatGPT hits remaining (expect <=1 for line-145 lede)\"; exit 1; }"</automated>
    <automated>grep -c '@aiPlatform.DisplayName' DeckFlow.Web/Views/Deck/DeckComparison.cshtml</automated>
    <automated>grep -c 'ChatGPT, Claude, or Gemini' DeckFlow.Web/Views/Deck/DeckComparison.cshtml</automated>
    <automated>grep -c 'data-cache-key="chatgpt-deck-comparison"' DeckFlow.Web/Views/Deck/DeckComparison.cshtml</automated>
    <automated>grep -c 'data-chatgpt-comparison-form' DeckFlow.Web/Views/Deck/DeckComparison.cshtml</automated>
    <automated>grep -c 'chatgpt-step-eyebrow' DeckFlow.Web/Views/Deck/DeckComparison.cshtml</automated>
    <automated>dotnet build DeckFlow.Web/DeckFlow.Web.csproj -c Debug --nologo -v quiet</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'ChatGPT' DeckFlow.Web/Views/Deck/DeckComparison.cshtml` returns exactly `1` (only the line-145 enumerated lede).
    - `grep -c '@aiPlatform.DisplayName' DeckFlow.Web/Views/Deck/DeckComparison.cshtml` returns at least `3` (the three below-selector injection sites).
    - `grep -c 'ChatGPT, Claude, or Gemini' DeckFlow.Web/Views/Deck/DeckComparison.cshtml` returns `1` (lede preserved).
    - `grep -c 'data-cache-key="chatgpt-deck-comparison"' DeckFlow.Web/Views/Deck/DeckComparison.cshtml` returns `1` (Phase 13 D-08 preserved).
    - `grep -c 'data-chatgpt-comparison-form' DeckFlow.Web/Views/Deck/DeckComparison.cshtml` returns `1` (Phase 13 D-08 preserved).
    - `grep -c 'chatgpt-step-eyebrow' DeckFlow.Web/Views/Deck/DeckComparison.cshtml` returns `1` (Phase 13 D-08 preserved).
    - `dotnet build DeckFlow.Web/DeckFlow.Web.csproj -c Debug` exits 0.
  </acceptance_criteria>
  <done>All 12 above/below-selector ChatGPT hits in DeckComparison.cshtml read correctly across AI selections; Phase 10/13 D-08 invariants intact; build clean.</done>
</task>

</tasks>

<verification>
- `grep -c 'ChatGPT' DeckFlow.Web/Views/Deck/DeckComparison.cshtml` returns `1` (lede only).
- `grep -nE 'data-cache-key|data-chatgpt-|chatgpt-step-eyebrow' DeckFlow.Web/Views/Deck/DeckComparison.cshtml` line set unchanged from baseline.
- `dotnet build DeckFlow.Web/DeckFlow.Web.csproj -c Release` exits 0.
</verification>

<success_criteria>
- DeckComparison.cshtml reads correctly for any AiPlatform selection.
- Phase 12 lede preserved; Phase 13 D-08 identifiers preserved; Phase 10 invariants preserved.
- Build clean.
</success_criteria>

<output>
Create `.planning/phases/999.1-ai-agnostic-prose-adaptation-razor-views/999.1-02-SUMMARY.md` when done.
</output>
