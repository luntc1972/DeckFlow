---
phase: 102-structural-analysis-role-floors
plan: 05
type: execute
wave: 4
depends_on: [102-04]
files_modified:
  - DeckFlow.Web/e2e/cut-lab-structure.spec.ts
autonomous: true
requirements: [SLOT-01, SLOT-02, FLOOR-01, FLOOR-02]
must_haves:
  truths:
    - "A live import renders all three Phase 102 sections: eight role-group accordions, the structural-findings panel, and the eight-row floor editor"
    - "Adjusting a floor writes roleFloors into the hidden CutLabStateJson, and after Recalculate analysis the Adjusted badge and floor value persist"
    - "The at-floor marker appears when a floor is raised to within 1 of the pool count for that role"
    - "Theme x viewport screenshots (3 themes x 2 viewports) of the structural view exist for the UI review gate"
    - "The full Cut Lab test surface is green: Web + Core xUnit, Vitest, and both e2e specs"
  artifacts:
    - path: "DeckFlow.Web/e2e/cut-lab-structure.spec.ts"
      provides: "End-to-end proof of SLOT-01/02 rendering and FLOOR-01/02 persistence + screenshots"
      contains: "roleFloors"
  key_links:
    - from: "cut-lab-structure.spec.ts"
      to: "/cut-lab page"
      via: "flag-gated admin harness + paste import"
      pattern: "setToolEnabled"
    - from: "floor input interaction"
      to: "hidden CutLabStateJson field"
      via: "toHaveValue regex on roleFloors"
      pattern: "roleFloors"
---

<objective>
Prove Phase 102 end-to-end in a live browser: a new cut-lab-structure e2e spec covering role-group
rendering, findings, floor adjustment persistence across a Recalculate round-trip, and the
theme x viewport screenshot matrix — then sweep the full test surface green as the phase gate.

Purpose: Unit tests (plans 01-03) prove the rules and Vitest (plan 04) pins the contracts; this
plan proves the assembled feature in the real pipeline (server + serializer + TS + Razor) and
produces the screenshot evidence the project's UI-review convention requires.
Output: DeckFlow.Web/e2e/cut-lab-structure.spec.ts + screenshots under
.planning/ui-design/cut-lab/screenshots/ (gitignored planning assets; spec is the committed artifact).

Constraints: run ONLY via scripts/run-web-test.sh (DECKFLOW_DISABLE_AUTO_BROWSER=true) +
`env -u DISPLAY -u WAYLAND_DISPLAY npx --no-install playwright test` — never a Windows-host
browser; probe for a stale Windows server on port 5173 before starting (project memory
reference_stale_windows_server_5173). Known out-of-scope flake source: deck-input-store.ts
restore desync (memory followup_deck_input_store_restore_desync) — do not misattribute.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/cut-lab/phases/102-structural-analysis-role-floors/102-UI-SPEC.md
@.planning/workstreams/cut-lab/phases/102-structural-analysis-role-floors/102-PATTERNS.md

<interfaces>
<!-- e2e harness to copy (DeckFlow.Web/e2e/cut-lab-smoke.spec.ts):
  test.describe.configure({ mode: 'serial' }); acquireAdminLockForTest/releaseAdminLockForTest from
  './support/admin-lock'; setToolEnabled(page, 'Cut Lab', true) in beforeEach; OFF + release in
  afterEach try/finally; importPool(page) paste-import helper waiting on the "Lock your pool"
  heading (30 s timeout); persistence regex assertions on the hidden field, e.g.
  await expect(hiddenState).toHaveValue(/"name":"Plains".*"isLocked":true/);
  theme x viewport matrix at :10-19, 129-147 — 3 themes (site.css / site-azorius.css / site-nyx.css
  via deckflow-theme cookie) x 2 viewports (1440 / 430 wide), fullPage screenshots into
  .planning/ui-design/cut-lab/screenshots/. -->

<!-- Phase 102 DOM hooks (plan 04): details.cutlab-role-group (8, collapsed); .cutlab-finding;
  input[data-cut-lab-floor="{roleKey}"]; [data-cut-lab-floor-reset]; [data-cut-lab-recalculate];
  [data-cut-lab-lock-role="{roleKey}"]; .cutlab-floor-state--at; the Adjusted .kb-chip badge;
  hidden input[name=CutLabStateJson] carrying "roleFloors". Section headings: "How your pool
  competes" / "Structural findings" / "Role floors". -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: cut-lab-structure.spec.ts — structural view + floor persistence e2e</name>
  <files>DeckFlow.Web/e2e/cut-lab-structure.spec.ts</files>
  <behavior>
    - After importPool: the three section headings render; exactly 8 details.cutlab-role-group exist, all collapsed; the floor table has 8 rows each with a numeric input carrying data-cut-lab-floor
    - Opening the Lands group shows member chips and the "Lock all lands" pill; clicking the pill checks the pool-table land checkboxes (single lock source observable from the table)
    - Setting input[data-cut-lab-floor="interaction"] to 99 (clamped client-side to pool count) then to a valid high value: the row shows the Adjusted badge and the hidden CutLabStateJson matches /"roleFloors":\[.*"role":"interaction".*"isUserSet":true/
    - Clicking [data-cut-lab-recalculate] re-submits; after the round-trip the interaction floor input still holds the adjusted value and the Adjusted badge persists (server merge honored)
    - Raising a floor to within 1 of that role's in-pool count shows the at-floor marker text "at floor"
    - "Reset to default" restores the default value and removes the Adjusted badge from the row
  </behavior>
  <read_first>
    - DeckFlow.Web/e2e/cut-lab-smoke.spec.ts (FULL read — the harness, importPool helper, persistence assertion style, screenshot matrix to copy)
    - DeckFlow.Web/e2e/support/admin-lock.ts (lock helpers imported by the smoke spec)
    - DeckFlow.Web/Views/Deck/CutLab.cshtml (plan 04 output — exact headings, attributes, badge markup to assert)
    - DeckFlow.Web/playwright.config.ts (base URL, projects, timeouts)
  </read_first>
  <action>
    Create cut-lab-structure.spec.ts mirroring the smoke spec's harness exactly: serial mode, admin
    lock acquire/release, setToolEnabled('Cut Lab', true/false) in beforeEach/afterEach try/finally,
    the same paste-import helper (reuse or re-declare importPool with the same pool fixture). Implement
    one test per behavior row above, using role-scoped locators (e.g.
    page.locator('details.cutlab-role-group', { hasText: 'Interaction' })) and the hidden-field regex
    style from the smoke spec. Use the smoke spec's 30 s import timeout; interactions after render use
    default timeouts.

    Add the screenshot matrix test copying smoke :10-19/129-147: 3 themes (deckflow-theme cookie:
    site.css, site-azorius.css, site-nyx.css) x 2 viewports (1440x1000, 430x932), fullPage screenshots
    of the imported structural view (open one role group and scroll the floors table into view first)
    into .planning/ui-design/cut-lab/screenshots/ with names like
    structure-{theme}-{viewport}.png. While reviewing screenshots, note (do not fix here) the
    101-VERIFICATION cosmetic item: Nyx mobile commander-badge overlap — record in the SUMMARY if still
    visible on the new sections.

    Run via scripts/run-web-test.sh + headless Playwright per the objective constraints. Then run the
    full phase gate: `dotnet test DeckFlow.Web.Tests`, `dotnet test DeckFlow.Core.Tests`,
    `npx --no-install vitest run`, and both cut-lab e2e specs — all green before the SUMMARY.
  </action>
  <verify>
    <automated>env -u DISPLAY -u WAYLAND_DISPLAY npx --no-install playwright test e2e/cut-lab-structure.spec.ts e2e/cut-lab-smoke.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'roleFloors' DeckFlow.Web/e2e/cut-lab-structure.spec.ts` returns ≥1 (persistence regex present)
    - `grep -c 'setToolEnabled' DeckFlow.Web/e2e/cut-lab-structure.spec.ts` returns ≥2 (ON in beforeEach, OFF in afterEach)
    - `grep -c "mode: 'serial'" DeckFlow.Web/e2e/cut-lab-structure.spec.ts` returns 1
    - A test asserts exactly 8 role-group accordions and 8 floor inputs
    - A test asserts the Adjusted badge survives the Recalculate round-trip
    - ≥6 screenshots exist in .planning/ui-design/cut-lab/screenshots/ matching structure-* (3 themes x 2 viewports)
    - Full sweep green: DeckFlow.Web.Tests and DeckFlow.Core.Tests 0 failures, `vitest run` passes, both e2e specs pass headless
  </acceptance_criteria>
  <done>The assembled Phase 102 feature is proven live — rendering, single lock source, floor persistence through a full server round-trip, at-floor states, and theme/viewport visual evidence — with the entire Cut Lab test surface green as the phase gate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| e2e admin harness → /Admin/Flags | Test-only BasicAuth flag toggling against the local dev server |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-102-05-01 | Elevation of Privilege | Flag left ON after a failed run | mitigate | afterEach try/finally sets the flag OFF and releases the admin lock (smoke-spec pattern copied verbatim); local dev server only, flag remains seeded OFF in both dialects |
| T-102-05-02 | Information Disclosure | Secrets in spec/screenshots | mitigate | Spec uses the existing local admin-lock helpers; screenshots capture the local page only; public-repo rule — no credentials in committed spec code |
| T-102-SC | Tampering | npm/pip/cargo installs | accept | Zero new dependencies; Playwright already installed (npx --no-install) |
</threat_model>

<verification>
- Both e2e specs green via scripts/run-web-test.sh harness (never a Windows browser; stale-5173 probe first)
- `dotnet test DeckFlow.Web.Tests` and `dotnet test DeckFlow.Core.Tests` 0 failures; `npx --no-install vitest run` passes
- Screenshot matrix present for the UI review gate; compiled js and screenshot temp assets not staged outside .planning/
- LF endings on the new spec
</verification>

<success_criteria>
- SLOT-01/02 and FLOOR-01/02 proven end-to-end in a live browser behind the flag
- Floor adjustment persistence + explicit at-floor signaling verified through a real POST round-trip
- 3-theme x 2-viewport screenshot evidence produced
- Entire phase test surface green — ready for /gsd-verify-work
</success_criteria>

<output>
Create `.planning/workstreams/cut-lab/phases/102-structural-analysis-role-floors/102-05-SUMMARY.md` when done
</output>
