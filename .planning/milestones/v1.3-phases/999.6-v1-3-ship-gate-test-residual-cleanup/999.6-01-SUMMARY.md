---
phase: 999.6-v1-3-ship-gate-test-residual-cleanup
plan: 01
subsystem: test-suite
tags: [test-hardening, stale-tests, ship-gate, mechanical-cleanup]
dependency_graph:
  requires: [phase-999.5]
  provides: [unblocks-P02-basicauth-debug, unblocks-P03-archidekt-debug]
  affects: [test-suite-baseline]
tech_stack:
  added: []
  patterns: [dual-path-fixture-probe-before-deletion-fallback, codex-codes-claude-orchestrates-gate]
key_files:
  created: []
  modified:
    - DeckFlow.Web.Tests/PacketArtifactStoreTests.cs
    - DeckFlow.Web.Tests/AiPlatformPhase10RoundTripTests.cs
    - .planning/phases/999.6-v1-3-ship-gate-test-residual-cleanup/999.6-DISCUSSION-LOG.md
  deleted:
    - DeckFlow.Web.Tests/PacketArtifactStoreRoundTripTests.cs
decisions: [D-04, D-05, D-06-BranchB, D-07, D-08, D-17, D-18, D-19, D-20, D-21, D-22, D-03-honored]
metrics:
  duration: ~10 minutes (mechanical cleanup; 4 Codex dispatches + 4 build/test gates + 4 commits + 1 SUMMARY)
  completed_date: 2026-05-22
---

# Phase 999.6 Plan 01: Mechanical Stale-Test Cleanup Summary

**One-liner:** Closed 6 of 9 residual test failures by catching stale tests up to shipped production renames (PacketArtifactStore msg generalization + Phase 12 page-rename `compare2` → `comparison`), deleting one dead-premise Fact, and deleting the dev-machine-only Arna fixture test under D-06 authorized Branch B fallback.

## Scope

P01 — Mechanical stale-test cleanup. Close the 6 stale tests in the 9-failure pre-phase baseline (445 / 433 / 9 / 3). All 4 mechanical edits dispatched via Codex per D-22 cross-AI rule; orchestrator (Claude) ran the Windows-side `dotnet` gate and committed under `luntc1972 <luntc1972@yahoo.com>` per D-19. Hybrid Codex-codes / Claude-orchestrates pattern carried forward from 999.5 P01.

## Files Modified

- `DeckFlow.Web.Tests/PacketArtifactStoreTests.cs` — line 57 msg-string updated VERBATIM from production SUT (D-04).
- `DeckFlow.Web.Tests/AiPlatformPhase10RoundTripTests.cs` — line 704 prefix `atraxa-compare2-` → `atraxa-comparison-` (D-05, covers 3 Theory rows); lines 747-754 dead-premise Fact deleted (D-07); class size 64 → 63.
- `DeckFlow.Web.Tests/PacketArtifactStoreRoundTripTests.cs` — DELETED (D-06 Branch B; 67 lines, 1 Fact).
- `.planning/phases/999.6-v1-3-ship-gate-test-residual-cleanup/999.6-DISCUSSION-LOG.md` — Execution Addendum appended (D-06 Branch B probe-path rationale).
- `.planning/phases/999.6-v1-3-ship-gate-test-residual-cleanup/999.6-01-SUMMARY.md` — this file.

## Decisions Delivered

- **D-04 (PacketArtifactStoreTests msg-string drift):** Stale `Assert.Equal` updated VERBATIM from the generalized production string at `PacketArtifactStore.cs:218` (`"Imported zip did not contain a recognized DeckFlow session — expected 01-request-context.txt, 40-deck-profile.json, or 51-set-upgrade-response.json."`). Production SUT byte-identical untouched. Em-dash U+2014 preserved per CLAUDE.md raw-string-literal preservation rule.
- **D-05 (AiPlatform Theory prefix `compare2` → `comparison`):** Single-line update at `AiPlatformPhase10RoundTripTests.cs:704`; covers all 3 `[MemberData(nameof(AllPlatforms))]` rows (Claude / ChatGPT / Gemini). Production emitter at `PacketArtifactStore.cs:533-534` produces `{commander}-comparison-{platform}-{timestamp}.zip` since Phase 12 URL rename. Sibling `Assert.Contains` on line 703 untouched.
- **D-06 — Branch B taken (Arna fixture unrecoverable):** Per probe protocol, both authorized paths (`/tmp/arna-test/` and `/mnt/c/tmp/arna-test/`) returned `No such file or directory`. Per D-06 authorized fallback clause, deleted `PacketArtifactStoreRoundTripTests.cs` (67 lines). No `Fixtures/arna-test/` directory created, no csproj edit. T-999.6-P01-01 (Information Disclosure threat) is N/A under Branch B — no fixture content committed to repo. DISCUSSION-LOG.md updated with Execution Addendum recording the dual-path probe result.
- **D-07 (dead-premise `compare2_page_segment` Fact deletion):** 8-line `[Fact]` block at `AiPlatformPhase10RoundTripTests.cs:747-754` removed. No production path emits `-compare2-` after Phase 12 URL rename; the 3 D-05 Theory rows already cover `-comparison-` prefix contract via `Assert.StartsWith`. Sibling Facts at lines 738-745 + 756-763 stay byte-identical (`grep -c` returns 1 each). Total drops 445 → 444.
- **D-08 (P01 atomicity):** 4 atomic commits in canonical order — D-04 msg update → D-05 Theory prefix update → D-07 deletion → D-06 Branch B deletion. NOT a mega-commit. Each fix-commit body carries the focused-test triplet per `[[feedback-run-and-update-tests]]`.
- **D-17 (strict P01 sequential — no upstream waves):** Honored. No parallel dispatch.
- **D-18 (atomic commits per cluster):** Honored. 4 fix commits + 1 SUMMARY commit (pending).
- **D-19 (plain default-author commits):** All 4 fix commits authored by `luntc1972 <luntc1972@yahoo.com>`; no `Co-Authored-By` trailer. Verified via `git log -1 --format="%an <%ae>"`.
- **D-20 (build gate):** `"/mnt/c/Program Files/dotnet/dotnet.exe" build DeckFlow.sln -c Release -nologo` returns `0 Warning(s) 0 Error(s)` after every commit.
- **D-21 (focused + full-suite test gate, stdout-parsed not exit-code gated):** Each fix-commit ran the focused `--filter` gate; closing full-suite gate parsed `Failed: 3, Passed: 437, Skipped: 3, Total: 443` from captured `/tmp/p01-fullsuite.log`. NOT exit-code gated — `dotnet test` exits non-zero by design with 3 residuals; acceptance is stdout-parsed triplet match.
- **D-22 (cross-AI dispatch via Codex):** Codex authored all 4 test edits via `codex exec --full-auto --color never --skip-git-repo-check -` piped stdin. Codex never committed (sandbox vsock barrier inherited from 999.5 P01). Orchestrator (Claude) ran Windows-side `dotnet.exe` build + test gates and committed under the project's local git config per D-19/D-20.
- **D-03 (no new `[Skip]` directives):** Honored — Skipped count unchanged at 3 (pre-phase baseline). The two authorized deletions (D-07 dead-premise + D-06 Branch B fixture-unrecoverable) each recorded explicit rationale in commit body + this SUMMARY + DISCUSSION-LOG addendum.

## Per-Cluster Outcome

| Decision | Branch | Outcome | Focused triplet |
|----------|--------|---------|-----------------|
| D-04 | n/a | `LoadFromZip_throws_when_no_response_json_present` PASS | `Passed: 3, Failed: 0, Skipped: 0, Total: 3, filter: FullyQualifiedName~PacketArtifactStoreTests` |
| D-05 | n/a | 3 Theory rows (Claude / ChatGPT / Gemini) PASS | `Passed: 3, Failed: 0, Skipped: 0, Total: 3, filter: FullyQualifiedName~AiPlatformPhase10RoundTripTests.SuggestComparisonZipFileName_includes_lowercased_ai_name` |
| D-07 | n/a | dead-premise Fact removed; class size 64 → 63 | `Passed: 63, Failed: 0, Skipped: 0, Total: 63, filter: FullyQualifiedName~AiPlatformPhase10RoundTripTests` |
| D-06 | **Branch B** | Test file deleted (fixture unrecoverable on both probe paths) | `"No test matches the given testcase filter"`, filter: `FullyQualifiedName~PacketArtifactStoreRoundTripTests` |

D-06 probe-path verdict (Branch B): `/tmp/arna-test/` MISSING + `/mnt/c/tmp/arna-test/` MISSING (both reported `No such file or directory`). No fixture source available to recover; integration assertion fixture-less has no value per D-06 authorized fallback clause.

## Build Gate Result

`"/mnt/c/Program Files/dotnet/dotnet.exe" build DeckFlow.sln -c Release -nologo` → `Build succeeded. 0 Warning(s) 0 Error(s)`. Identical to pre-phase 999.5 close baseline.

## Full-Suite Gate Result (closing)

Captured to `/tmp/p01-fullsuite.log` via `tee`:

```
Passed!  - Failed:     0, Passed:    57, Skipped:     0, Total:    57, Duration: 601 ms - DeckFlow.Core.Tests.dll (net10.0)
Failed!  - Failed:     3, Passed:   437, Skipped:     3, Total:   443, Duration: 29 s   - DeckFlow.Web.Tests.dll (net10.0)
```

**Plan triplet (DeckFlow.Web.Tests, the suite under change):**
`Passed: 437, Failed: 3, Skipped: 3, Total: 443`

**Delta vs pre-phase baseline (445 / 433 / 9 / 3):**
- Failed: 9 → **3** (−6, matches success-criterion 1)
- Passed: 433 → **437** (+4 net — 6 stale tests resolved, but 1 of those (D-06) is a deletion not a fix, so net +4 passing)
- Skipped: 3 → **3** (unchanged, D-03 honored)
- Total: 445 → **443** (−2 from the two authorized deletions — D-07 + D-06 Branch B; matches Codex MEDIUM #7 Branch B forecast)

NOT exit-code gated per Codex HIGH #5 / LOW #8 — `dotnet test` exits non-zero by design with 3 residuals.

## Residual Failures Carried Forward

The 3 residual failures parsed from `/tmp/p01-fullsuite.log` are EXACTLY the expected P02 + P03 carry-forwards per CONTEXT D-02 incremental progress schedule:

| Test | Owner | Failure shape |
|------|-------|---------------|
| `DeckFlow.Web.Tests.BasicAuthMiddlewareTests.CorrectCredentials_InvokesNext` | **P02** (BasicAuth debug+fix) | `Assert.True() Failure: Expected True / Actual False` at `BasicAuthMiddlewareTests.cs:96` — flaky in full-suite ordering only (passes 5/5 filter-isolated per CONTEXT §domain bullet 2). State leak between facts. |
| `DeckFlow.Web.Tests.ArchidektCacheJobServiceTests.BackgroundService_SucceedsAndUpdatesProcessedCounts` | **P03** (Archidekt debug+fix) | `Assert.NotNull() Failure: Value is null` at `ArchidektCacheJobServiceTests.cs:118` — poll-before-scheduler-runs race smell per CONTEXT D-13. |
| `DeckFlow.Web.Tests.ArchidektCacheJobServiceTests.GetActiveJob_ReturnsNullAfterCompletedJob` | **P03** (Archidekt debug+fix) | `Assert.NotNull() Failure: Value is null` at `ArchidektCacheJobServiceTests.cs:170` — name-vs-assertion mismatch ("ReturnsNullAfterCompletedJob" yet asserts NotNull) flagged for `/gsd-debug` investigation. |

P01 closes the 6 stale tests AND clears noise so P02 / P03 can triage their failures against a clean baseline.

## Total-count Note

Total dropped from 445 → 443 (Branch B taken). Breakdown:
- −1 from D-07 (dead-premise Fact deletion) → 444
- −1 from D-06 Branch B (PacketArtifactStoreRoundTripTests file deletion) → 443

If D-06 Branch A had been viable, Total would have stayed at 444 with one passing Arna fixture round-trip test. Branch A was not viable (both probe paths missing the fixture files).

## Commit Hash Sequence

4 atomic fix commits per D-08, plus this closing SUMMARY commit:

1. `d9aeb65` — `fix(999.6): update PacketArtifactStoreTests msg-string for AI-agnostic rename (D-04)`
2. `9379356` — `fix(999.6): update AiPlatform Theory prefix to comparison after Phase 12 page rename (D-05)`
3. `420fbfb` — `test(999.6): delete dead-premise compare2_page_segment Fact (D-07)`
4. `01534e3` — `test(999.6): delete PacketArtifactStoreRoundTripTests — Arna fixture unrecoverable on dev machine, integration assertion has no value (D-06 fallback)`
5. (this commit, pending) — `docs(999.6): summary for plan 01 mechanical stale-test cleanup`

All 5 commits authored by `luntc1972 <luntc1972@yahoo.com>`, no `Co-Authored-By` trailer.

## must_haves Status (per 999.6-01-PLAN.md frontmatter)

- "D-01 3-plan split": **HONORED** (this is P01).
- "D-02 strict sequential P01 → P02 → P03": **HONORED**, P01 closes here; P02 unblocked next.
- "D-17 strict sequential — no upstream waves": **HONORED**.
- "D-08 P01 atomicity, 4 commits not mega-commit": **MET** — 4 fix commits in canonical order.
- "D-18 atomic commits per cluster, 4 commits land": **MET**.
- "D-19 plain default-author commits as `luntc1972 <luntc1972@yahoo.com>`, no Co-Authored-By": **MET** — verified via `git log -1 --format="%an <%ae>"` on every commit.
- "D-20 build gate 0/0": **MET** — every commit preceded by clean build.
- "D-21 focused gate per task, full-suite gate at close, stdout-parsed not exit-code gated": **MET** — each fix gated focused; closing gate parsed `Failed: 3, .* Skipped: 3` from stdout.
- "D-22 Codex codes, orchestrator runs Windows-dotnet gate + commits": **MET** — 4 Codex dispatches via `codex exec --full-auto --color never --skip-git-repo-check -` piped stdin; orchestrator ran gate and committed.
- "D-03 no new `[Skip]` directives; the two authorized deletions are D-07 + D-06 Branch B": **MET** — Skipped unchanged at 3; D-07 + D-06-Branch-B deletions each recorded with explicit rationale.
- "P01 fix outcome: 6 of 9 residual failures resolved; Full-suite delta target `Failed: 3`": **MET** — full-suite triplet parses `Failed: 3, Passed: 437, Skipped: 3, Total: 443`.
- "Total: 443 if D-06 Branch B taken": **MET** — Total: 443.

## Self-Check: PASSED

- Modified files exist and reflect intended edits: `DeckFlow.Web.Tests/PacketArtifactStoreTests.cs:57` updated, `DeckFlow.Web.Tests/AiPlatformPhase10RoundTripTests.cs:704` updated + 8-line block at former lines 747-754 removed, `DeckFlow.Web.Tests/PacketArtifactStoreRoundTripTests.cs` deleted, DISCUSSION-LOG.md appended.
- All 4 commit hashes resolve in `git log`: `d9aeb65`, `9379356`, `420fbfb`, `01534e3` (verified via `git log --oneline` lookback).
- Full-suite log `/tmp/p01-fullsuite.log` contains `Failed:     3, .* Skipped:     3` line, residuals enumerated match P02 + P03 expected carry-forwards.

## Codex Sessions

4 independent Codex dispatches via shell pipe (`codex exec --full-auto --color never --skip-git-repo-check -`), one per code-touching task. MCP `mcp__codex__codex` tool not used; shell-pipe form chosen for deterministic stdin payload. Codex never committed (sandbox vsock barrier inherited from 999.5 P01 SUMMARY). Each dispatch returned a unified diff confirming exactly the requested single-line / single-block edit was applied — orchestrator verified diff before running gate, no Codex deviation observed.

## Next Step

P02 dispatch unblocked: `999.6-02-PLAN.md` (BasicAuth flaky-test root-cause + fix, per CONTEXT D-09/D-10/D-11). Orchestrator-led hybrid execution pattern continues — Codex codes, orchestrator runs Windows-side dotnet gate + commits.
