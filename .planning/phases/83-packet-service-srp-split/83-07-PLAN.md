---
phase: 83-packet-service-srp-split
plan: 07
type: execute
wave: 2
depends_on: ["83-01", "83-02"]
files_modified:
  - DeckFlow.Web/Services/DeckPrimerPacketService.cs
  - DeckFlow.Web.Tests/DeckPrimerPacketServiceTests.cs
autonomous: true
requirements: [PKTSVC-01, PKTSVC-02, PKTSVC-03, PKTSVC-04]
must_haves:
  truths:
    - "DeckPrimerPacketService delegates its sectioned decklist text (no versions, no oracleNameMap) to PacketTextAssembler, passing its OWN NormalizeSingleLine as the AppendKeyValueLine normalizer delegate (Primer NormalizeSingleLine is KEPT — the Primer prompt-variant files call it and its char-by-char CollapseWhitespace is not byte-equivalent to JsonTextFormatterService's)."
    - "PKTSVC-02 is satisfied for Primer by VERIFYING it retains zero duplicate Scryfall-resolution code — NOT by adding a resolver (that would be a net-new feature and is prohibited)."
    - "Its per-platform primer paste artifacts are byte-identical pre/post refactor across all 3 AI variants."
  artifacts:
    - path: "DeckFlow.Web/Services/DeckPrimerPacketService.cs"
      provides: "Primer orchestration shell delegating decklist assembly to PacketTextAssembler"
      contains: "PacketTextAssembler"
  key_links:
    - from: "DeckFlow.Web/Services/DeckPrimerPacketService.cs"
      to: "DeckFlow.Web/Services/Packets/PacketTextAssembler.cs"
      via: "BuildSectionedDecklistText (plain shape) + AppendKeyValueLine"
      pattern: "PacketTextAssembler"
---

<objective>
Migrate DeckPrimerPacketService (904 LOC) onto the shared PacketTextAssembler, reducing it to an
orchestration shell — gated by the 83-01 byte-identity harness. Primer is the lowest-risk migration:
it never calls Scryfall, so it consumes only the text assembler (no resolver).

Purpose: Remove Primer's copy of the sectioned-decklist builder (keeping its own NormalizeSingleLine),
delegating to PacketTextAssembler — with zero change to its per-platform primer artifacts. Explicitly
record that PKTSVC-02 is satisfied by Primer having ZERO duplicate Scryfall path, without adding one.
Output: A slimmer DeckPrimerPacketService whose Primer byte-identity suite still passes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/83-packet-service-srp-split/83-RESEARCH.md
@CLAUDE.md

<do_not_unify>
- Do NOT add IScryfallCardResolver / ScryfallReferenceResolver to Primer — it has no card-resolution
  need today; adding one is a net-new feature and violates the milestone's no-new-feature gate
  (research Pitfall 3). PKTSVC-02 is met by verification, not by wiring.
- Keep Primer's `CollapseWhitespace` (:554-576, char-by-char, collapses ANY whitespace run) — it is a
  DIFFERENT behavior from Analysis/Comparison's newline-only collapse (Cluster G). Do NOT consolidate.
- Keep `BuildCanonicalDeckSourceText` (:801-818, Board|Qty|Name shape tied to the staleness hash) and
  `EvaluateStaleness` untouched (Cluster C — its doc comment says order/format MUST NOT change).
- Keep `BuildComboReferenceText(..., spikeVerdict)` (:481-549, Markdown, popularity/mana ranking) as-is (Cluster F).
</do_not_unify>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delegate sectioned decklist (keep NormalizeSingleLine); verify zero Scryfall path</name>
  <files>DeckFlow.Web/Services/DeckPrimerPacketService.cs, DeckFlow.Web.Tests/DeckPrimerPacketServiceTests.cs</files>
  <read_first>
    - DeckFlow.Web/Services/DeckPrimerPacketService.cs:682-719 (BuildDecklistText — plain three-section shape), :551-552 (private NormalizeSingleLine), :732-751 (BuildRequestContextText), :554-576 (CollapseWhitespace — leave), :801-818 (BuildCanonicalDeckSourceText — leave)
    - DeckFlow.Web/Services/Packets/PacketTextAssembler.cs (BuildSectionedDecklistText + AppendKeyValueLine)
    - DeckFlow.Web/Services/JsonTextFormatterService.cs:34-41 (the DIFFERENT `\n`/`\r`-only normalizer — for contrast; NOT to be swapped in)
    - DeckFlow.Web/Services/PromptBuilders/Primer/*.cs (call DeckPrimerPacketService.NormalizeSingleLine directly — do not break them)
    - DeckFlow.Web.Tests/DeckPrimerByteIdentityTests.cs (from 83-01 — the gate; PromptTextsByPlatform per platform)
    - DeckFlow.Web.Tests/DeckPrimerPacketServiceTests.cs (second internal ctor seam — no Scryfall dependency)
  </read_first>
  <action>
    Replace Primer's `BuildDecklistText` call with `PacketTextAssembler.BuildSectionedDecklistText(
    entries, possibleIncludes, includeVersions:false, oracleNameMap:null)` and delete the now-unused
    private builder (the assembler reproduces Primer's plain shape).

    H3 — KEEP `DeckPrimerPacketService.NormalizeSingleLine` (:551-552) EXACTLY AS-IS. Do NOT delete it
    and do NOT replace it with `JsonTextFormatterService.NormalizeSingleLine` — Primer's delegates to
    its char-by-char CollapseWhitespace (collapses ANY whitespace run) and is NOT byte-equivalent, and
    the Primer prompt-variant files call `DeckPrimerPacketService.NormalizeSingleLine(...)` directly
    (deleting it breaks their compile and changes bytes). When routing `BuildRequestContextText`'s
    per-field lines through `PacketTextAssembler.AppendKeyValueLine`, pass Primer's OWN
    `NormalizeSingleLine` as the `Func<string?, string, string>` normalizer delegate. Keep Primer's own
    field list/order. Do NOT add any Scryfall dependency. Leave CollapseWhitespace,
    BuildCanonicalDeckSourceText/EvaluateStaleness, and the combo formatter untouched.

    Add/keep a test note (assertion or comment) recording that DeckPrimerPacketService references no
    IScryfallCardResolver / ScryfallReferenceResolver — satisfying PKTSVC-02 by absence of a duplicate
    resolution path.
  </action>
  <verify>
    <automated>dotnet.exe test DeckFlow.Web.Tests --filter "FullyQualifiedName~DeckPrimer" 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - Build 0/0; full DeckPrimer test set + DeckPrimerByteIdentityTests pass (all 3 platforms in PromptTextsByPlatform).
    - Private BuildDecklistText is gone (grep confirms).
    - `DeckPrimerPacketService.NormalizeSingleLine` REMAINS (still called by the Primer prompt-variant files); it was NOT deleted or swapped for JsonTextFormatterService's version.
    - `grep -n "IScryfallCardResolver\|ScryfallReferenceResolver" DeckFlow.Web/Services/DeckPrimerPacketService.cs` returns ZERO hits (PKTSVC-02 by absence).
    - CollapseWhitespace, BuildCanonicalDeckSourceText, EvaluateStaleness, BuildComboReferenceText remain (Cluster G/C/F untouched).
    - DeckPrimerPacketService is materially smaller and delegates decklist assembly to the collaborator.
  </acceptance_criteria>
  <done>DeckPrimerPacketService is an orchestration shell over PacketTextAssembler; artifacts byte-identical across 3 platforms; zero Scryfall path added.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → Primer request model | DeckPrimerRequest validation untouched; refactor re-routes internal text assembly only, adds no external surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-83-13 | Tampering | consolidating Primer's char-by-char CollapseWhitespace with the newline-only version, changing bytes for irregular-whitespace card text | mitigate | do_not_unify fence keeps it inline; DeckPrimerByteIdentityTests confirms. |
| T-83-14 | Elevation of scope | adding a Scryfall dependency to satisfy PKTSVC-02's letter (net-new feature) | mitigate | Acceptance requires ZERO IScryfallCardResolver/ScryfallReferenceResolver hits in the file. |
</threat_model>

<verification>
- `dotnet.exe test DeckFlow.Web.Tests --filter "FullyQualifiedName~DeckPrimer"` → all pass (incl. byte-identity, 3 platforms).
- Grep: no private BuildDecklistText and no Scryfall type references in DeckPrimerPacketService; NormalizeSingleLine still present.
</verification>

<success_criteria>
DeckPrimerPacketService delegates decklist assembly to PacketTextAssembler, retains zero duplicate
Scryfall-resolution path (PKTSVC-02 by verified absence), keeps its divergent whitespace/cache-key/
combo logic, and its per-platform primer artifacts are byte-identical across all 3 AI variants
(PKTSVC-01/02/03; gated by PKTSVC-04).
</success_criteria>

<output>
Create `.planning/phases/83-packet-service-srp-split/83-07-SUMMARY.md` when done.
</output>
