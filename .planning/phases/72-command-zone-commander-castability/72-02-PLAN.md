---
phase: 72-command-zone-commander-castability
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - DeckFlow.Core.Tests/Fixtures/moxfield-companion-direct.json
  - DeckFlow.Core.Tests/Fixtures/archidekt-background-companion.json
  - .planning/phases/72-command-zone-commander-castability/72-FIXTURE-NOTES.md
autonomous: false
requirements: [B-04, B-05]
must_haves:
  truths:
    - "A real Moxfield direct-API payload proves the exact 'companions' board key + entry shape"
    - "A real Archidekt deck payload proves the exact category strings for Background and Companion cards"
    - "The captured fixtures are confirmed to come from genuine companion/background decks (data assumptions A1/A2/A3 verified, not assumed)"
  artifacts:
    - path: "DeckFlow.Core.Tests/Fixtures/moxfield-companion-direct.json"
      provides: "Moxfield v2 direct deck JSON containing a populated companions board"
      contains: "companions"
    - path: "DeckFlow.Core.Tests/Fixtures/archidekt-background-companion.json"
      provides: "Archidekt deck JSON containing a Background card and/or a Companion card with their category strings"
      contains: "categories"
    - path: ".planning/phases/72-command-zone-commander-castability/72-FIXTURE-NOTES.md"
      provides: "Recorded exact board key + category strings + sample deck URLs the fixtures came from"
  key_links:
    - from: "moxfield-companion-direct.json"
      to: "72-03 Moxfield importer companion-name extraction"
      via: "fixture feeds the importer xUnit test"
      pattern: "companions"
---

<objective>
[BLOCKING] Capture two real third-party API payloads to ground the [ASSUMED] data shapes
(RESEARCH Assumptions A1/A2/A3, Open Questions 1-2) BEFORE any importer code relies on them:
the Moxfield direct-API "companions" board, and the Archidekt "Background"/"Companion" category
strings. These fixtures back the importer xUnit tests in plan 72-03.

Purpose: RESEARCH confirms the companions board only from an unofficial wrapper (MEDIUM
confidence). Do NOT rely on auto-detect until the shape is captured.
Output: Two committed fixture JSON files + a notes doc recording the exact key/category strings.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/72-command-zone-commander-castability/72-RESEARCH.md
@.planning/phases/72-command-zone-commander-castability/72-SPEC.md

<interfaces>
<!-- How existing importer tests consume JSON — the fixture format target. -->
- DeckFlow.Core.Tests/MoxfieldApiDeckImporterTests.cs feeds the direct-API path via an `executeAsync`
  test-seam delegate returning a RestResponse whose Content is a raw-string JSON body (see the inline
  direct-API JSON at ~:122-153 with `"commanders"`, `"mainboard"` object boards). The fixture captured
  here mirrors that shape but ADDS a populated `"companions"` board.
- Moxfield direct deck API (public): `https://api2.moxfield.com/v3/decks/all/{deckId}` (or the v2 path the
  importer already calls — confirm against MoxfieldApiDeckImporter.FetchDirectAsync). A Yorion / Lurrus /
  Jegantha companion deck is a good source.
- Archidekt deck API (public): `https://archidekt.com/api/decks/{deckId}/` — `cards[].categories[]` holds
  the category strings; find a deck that uses a Background (e.g. a "Choose a Background" partner) and/or a
  companion.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Capture Moxfield direct-API companions fixture</name>
  <files>DeckFlow.Core.Tests/Fixtures/moxfield-companion-direct.json, .planning/phases/72-command-zone-commander-castability/72-FIXTURE-NOTES.md</files>
  <read_first>
    - DeckFlow.Core/Integration/MoxfieldApiDeckImporter.cs (FetchDirectAsync URL + board reads, ~:72-98)
    - DeckFlow.Core.Tests/MoxfieldApiDeckImporterTests.cs (direct-API inline JSON ~:114-153)
  </read_first>
  <what-built>
    Claude attempts a live fetch of a known Moxfield companion deck's direct-API JSON (curl/WebFetch the
    public deck endpoint the importer uses) and saves the response (trimmed to the boards + a few cards)
    to DeckFlow.Core.Tests/Fixtures/moxfield-companion-direct.json. It records in 72-FIXTURE-NOTES.md:
    the exact top-level board key (`companions` vs other), the per-entry shape (`quantity`, `card.name`,
    `card.set`, `card.cn`), whether a no-companion deck omits the key or sends an empty object, and the
    source deck URL.
  </what-built>
  <how-to-verify>
    Moxfield's Cloudflare edge can block cloud/WSL IPs (the importer has a Spellbook fallback precisely
    for this). If the live fetch is blocked:
    1. Open a known companion deck on moxfield.com in your browser (e.g. a Yorion or Lurrus deck).
    2. Use the browser devtools Network tab to copy the deck API JSON response, OR paste a companion deck
       URL so Claude can retry.
    Then confirm: the saved fixture contains a populated companion board with at least one card, and the
    board key + entry shape recorded in 72-FIXTURE-NOTES.md match what you see.
  </how-to-verify>
  <verify>
    <automated>test -s DeckFlow.Core.Tests/Fixtures/moxfield-companion-direct.json &amp;&amp; grep -qi "companion" DeckFlow.Core.Tests/Fixtures/moxfield-companion-direct.json &amp;&amp; echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Fixture file exists, non-empty, and contains a companion board with >= 1 card entry.
    - 72-FIXTURE-NOTES.md records: exact board key, entry field names, absent-vs-empty behavior, source URL.
  </acceptance_criteria>
  <resume-signal>Type "approved" once the companion-board shape is confirmed, or paste a companion deck URL to retry.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: Capture Archidekt Background + Companion category fixture</name>
  <files>DeckFlow.Core.Tests/Fixtures/archidekt-background-companion.json, .planning/phases/72-command-zone-commander-castability/72-FIXTURE-NOTES.md</files>
  <read_first>
    - DeckFlow.Core/Integration/ArchidektApiDeckImporter.cs (categories read ~:73-102, DetermineBoard/IsBoardCategory ~:126-154)
  </read_first>
  <what-built>
    Claude fetches a known Archidekt deck JSON that uses a Background card and (ideally) a Companion card,
    saves it (trimmed) to DeckFlow.Core.Tests/Fixtures/archidekt-background-companion.json, and records in
    72-FIXTURE-NOTES.md the EXACT category strings Archidekt emits for the Background card and the Companion
    card (e.g. is it "Background"? "Companion"? something else), since the service-layer detection in
    72-05 matches these strings OrdinalIgnoreCase.
  </what-built>
  <how-to-verify>
    Confirm the recorded category strings are the literal values from a real deck (open the same deck on
    archidekt.com and check the card's category label). If the live API is unreachable, paste a Background/
    Companion deck URL so Claude can retry, or paste the category JSON from the browser.
  </how-to-verify>
  <verify>
    <automated>test -s DeckFlow.Core.Tests/Fixtures/archidekt-background-companion.json &amp;&amp; grep -qi "categor" DeckFlow.Core.Tests/Fixtures/archidekt-background-companion.json &amp;&amp; echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Fixture exists and contains a card with a Background category and/or a Companion category.
    - 72-FIXTURE-NOTES.md records the EXACT Background and Companion category strings (verbatim, with casing).
  </acceptance_criteria>
  <resume-signal>Type "approved" once the category strings are confirmed verbatim, or paste a Background/Companion deck URL to retry.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| third-party API → fixture file | Captured JSON is checked into the test project; treated as untrusted sample data |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-72-02 | Information disclosure | Captured fixture | mitigate | Trim to boards + a few cards; no auth tokens/cookies/PII copied into the fixture |
| T-72-DATA | Tampering | [ASSUMED] data shapes A1/A2/A3 | mitigate | Blocking human-verify confirms shapes come from genuine companion/background decks before code relies on them |
</threat_model>

<verification>
- Both fixtures present and non-empty; 72-FIXTURE-NOTES.md records exact keys/strings + source URLs.
- Human-verify checkpoints approved (data assumptions confirmed, not assumed).
</verification>

<success_criteria>
- Moxfield companions board shape + Archidekt Background/Companion category strings are captured and
  verified, unblocking the importer + service detection in 72-03 / 72-05.
</success_criteria>

<output>
Create `.planning/phases/72-command-zone-commander-castability/72-02-SUMMARY.md` when done.
</output>
