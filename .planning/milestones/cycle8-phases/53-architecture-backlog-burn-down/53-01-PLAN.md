---
phase: 53-architecture-backlog-burn-down
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - DeckFlow.Core/Knowledge/CategoryKnowledgeRepository.cs
  - DeckFlow.Core/Knowledge/CategoryCacheSchema.cs
  - DeckFlow.Core/Knowledge/DeckQueueRepository.cs
  - DeckFlow.Core/Knowledge/CardCategoryRepository.cs
autonomous: true
requirements: [ARCH-02]

must_haves:
  truths:
    - "Card and commander category lookups return identical rows before and after the split (live request path unchanged)"
    - "The harvest deck-queue add/mark/dedup behavior is unchanged"
    - "CategoryKnowledgeRepository public surface (24 public/internal methods + DatabasePath) is unchanged so Web, CLI, and the hosted job do not repoint"
    - "Schema/DDL ownership lives in one collaborator; EnsureSchemaAsync DDL is no longer duplicated reasoning across query methods"
  artifacts:
    - path: "DeckFlow.Core/Knowledge/CategoryCacheSchema.cs"
      provides: "Schema/DDL + migration + index creation collaborator (owns EnsureSchemaAsync table/index DDL)"
      min_lines: 80
    - path: "DeckFlow.Core/Knowledge/DeckQueueRepository.cs"
      provides: "deck_queue + crawl_state harvest-queue collaborator (Add/Next/Mark/ContentHash/CrawlPage/commander counts)"
      min_lines: 80
    - path: "DeckFlow.Core/Knowledge/CardCategoryRepository.cs"
      provides: "card_category_observations + card_deck_totals + sources/cards read/query/upsert + filtering/normalization collaborator"
      min_lines: 80
    - path: "DeckFlow.Core/Knowledge/CategoryKnowledgeRepository.cs"
      provides: "Thin facade preserving the existing public surface, delegating to the three collaborators"
      contains: "public sealed class CategoryKnowledgeRepository"
  key_links:
    - from: "DeckFlow.Web/Services/CategoryKnowledgeStore.cs"
      to: "CategoryKnowledgeRepository"
      via: "unchanged public method calls (no repoint)"
      pattern: "CategoryKnowledgeRepository"
    - from: "DeckFlow.Core/Knowledge/CategoryKnowledgeRepository.cs"
      to: "CategoryCacheSchema / DeckQueueRepository / CardCategoryRepository"
      via: "facade delegation"
      pattern: "_(schema|deckQueue|cardCategory)\\."
---

<objective>
Split the 1272-LOC `CategoryKnowledgeRepository` god-file (24 methods, 5 reasons-to-change) into three focused collaborators using **facade-then-extract**, so the live card/commander lookup path and the harvest job keep working with zero caller changes. Closes Phase 39 audit Finding B.

Purpose: Biggest non-content god-file, on the live request path. Splitting by responsibility (Schema / deck Queue / CardCategory) consolidates `EnsureSchemaAsync` DDL ownership and unblocks future change without re-reasoning the whole file.
Output: Four files — a thin `CategoryKnowledgeRepository` facade plus `CategoryCacheSchema`, `DeckQueueRepository`, `CardCategoryRepository` collaborators.

Behavior-preservation proof: existing safety net — 17 round-trip facts in `CategoryKnowledgeRepositoryTests`, plus `CategoryCacheSchemaParityTests`, `ContentHashDedupTests`, `DeckCategoryCacheWriterTests`, `ArchidektDeckCacheSessionTests`. No new harness.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/53-architecture-backlog-burn-down/53-CONTEXT.md
@./CLAUDE.md

<interfaces>
<!-- The facade MUST preserve these public/internal members verbatim. No caller repoints. -->
From DeckFlow.Core/Knowledge/CategoryKnowledgeRepository.cs — public surface to preserve:
- Two ctors: (string databasePath, ILogger? logger = null) and (RelationalDatabaseConnection connectionInfo, ILogger? logger = null)
- string? DatabasePath { get; }
- EnsureSchemaAsync; GetCategoriesAsync; GetCategoryRowsForCardAsync; GetCategoryRowsForCommanderAsync
- GetCommanderDeckCountAsync; GetPagedProcessedCommanderRowsAsync; GetDistinctProcessedCommanderCountAsync
- ReplaceSourceRowsAsync; DeleteSourceDataAsync; PersistObservedCategoriesAsync; PersistCardDeckTotalsAsync
- internal PersistDeckCategoryBatchAsync; GetCardDeckTotalsAsync; HasSourceDataAsync
- AddDeckIdsAsync; GetNextUnprocessedDeckIdsAsync; GetUnprocessedCountAsync; GetProcessedDeckCountAsync
- GetRecentDeckCrawlPageAsync; SetRecentDeckCrawlPageAsync; MarkDeckProcessedAsync
- GetContentHashAsync; SetContentHashAsync; MarkUrlDeckProcessedAsync; MarkDecksProcessedAsync
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract CategoryCacheSchema (DDL/migration/index ownership)</name>
  <read_first>
    DeckFlow.Core/Knowledge/CategoryKnowledgeRepository.cs (entire file — the source of all DDL),
    DeckFlow.Core/Storage/RelationalDatabaseConnection.cs (Dialect, CreateConnection, IsSqlite, ExtractSqlitePath),
    DeckFlow.Core.Tests/CategoryCacheSchemaParityTests.cs,
    DeckFlow.Core.Tests/CategoryKnowledgeRepositoryTests.cs
  </read_first>
  <files>DeckFlow.Core/Knowledge/CategoryCacheSchema.cs, DeckFlow.Core/Knowledge/CategoryKnowledgeRepository.cs</files>
  <action>
    Create `CategoryCacheSchema` (internal sealed class, namespace DeckFlow.Core.Knowledge) owning ALL schema/DDL: move EnsureSchemaAsync and the private static table-creation helpers (CreateCardsTableAsync, CreateSourcesTableAsync, CreateCardCategoryObservationsTableAsync, CreateCardDeckTotalsTableAsync), the deck_queue + crawl_state DDL, the content_hash ALTER migration, the index batch with its try/catch + logger warning, and GetTableColumnsAsync (used by the migration). Its ctor takes the RelationalDatabaseConnection, the directory-create path logic, and the optional ILogger. Preserve every SQL string and the load-bearing index-ordering comment (the "create the replacement first" comment) verbatim — it runs inside a swallowing try/catch and any reorder changes behavior. Keep the 15-second CommandTimeout. The facade EnsureSchemaAsync delegates to CategoryCacheSchema.EnsureSchemaAsync; query/queue methods that call EnsureSchemaAsync at entry call the shared schema instance. For THIS task the facade may still hold the query/queue method bodies inline (collaborators arrive in Tasks 2-3); only schema moves out. Do NOT change DDL text, index names, or CommandTimeout.
  </action>
  <verify>dotnet build DeckFlow.Core + DeckFlow.Core.Tests both clean, 0 new warnings</verify>
  <acceptance_criteria>
    - source: CategoryCacheSchema.cs exists and contains all CREATE TABLE / CREATE INDEX / ALTER TABLE DDL; `grep -c "CREATE TABLE" DeckFlow.Core/Knowledge/CategoryKnowledgeRepository.cs` returns 0
    - source: the index-ordering comment ("create the replacement first") moved verbatim into CategoryCacheSchema.cs
    - test-command: `"/mnt/c/Program Files/dotnet/dotnet.exe" build DeckFlow.Core/DeckFlow.Core.csproj && "/mnt/c/Program Files/dotnet/dotnet.exe" build DeckFlow.Core.Tests/DeckFlow.Core.Tests.csproj` exits 0, 0 new warnings
    - behavior: CategoryCacheSchemaParityTests still compiles against the unchanged public EnsureSchemaAsync
  </acceptance_criteria>
  <done>All DDL lives in CategoryCacheSchema; the facade EnsureSchemaAsync delegates to it; both projects build clean.</done>
</task>

<task type="auto">
  <name>Task 2: Extract DeckQueueRepository (harvest queue + crawl state)</name>
  <read_first>
    DeckFlow.Core/Knowledge/CategoryKnowledgeRepository.cs (deck_queue + crawl_state methods),
    DeckFlow.Core.Tests/ContentHashDedupTests.cs,
    DeckFlow.Core.Tests/CategoryKnowledgeRepositoryTests.cs
  </read_first>
  <files>DeckFlow.Core/Knowledge/DeckQueueRepository.cs, DeckFlow.Core/Knowledge/CategoryKnowledgeRepository.cs</files>
  <action>
    Create `DeckQueueRepository` (internal sealed class, namespace DeckFlow.Core.Knowledge) owning the deck_queue + crawl_state concern. Move the bodies of GetCommanderDeckCountAsync, GetPagedProcessedCommanderRowsAsync, GetDistinctProcessedCommanderCountAsync, AddDeckIdsAsync, GetNextUnprocessedDeckIdsAsync, GetUnprocessedCountAsync, GetProcessedDeckCountAsync, GetRecentDeckCrawlPageAsync, SetRecentDeckCrawlPageAsync, MarkDeckProcessedAsync, GetContentHashAsync, SetContentHashAsync, MarkUrlDeckProcessedAsync, MarkDecksProcessedAsync, plus the private ProcessedCommanderAggregateRow row type. Ctor takes the shared RelationalDatabaseConnection and the CategoryCacheSchema instance (to call EnsureSchema at entry). Preserve the AddDeckIdsAsync F-51-PG-01 `::timestamptz` dialect-guard comment and SQL verbatim (hard-won Postgres parity fix), and the D-17 / B2 commander-capture comments. Replace facade methods with one-line delegations. Do NOT alter DateTime binding, the DeckRefreshCooldown constant (5 days), or query text.
  </action>
  <verify>dotnet build DeckFlow.Core + DeckFlow.Core.Tests both clean</verify>
  <acceptance_criteria>
    - source: DeckQueueRepository.cs exists; the `::timestamptz` F-51-PG-01 comment + SQL present verbatim
    - source: facade deck-queue methods are now one-line delegations forwarding to the collaborator
    - source: DeckRefreshCooldown and the ON CONFLICT idioms unchanged (text identical in the new file)
    - test-command: `"/mnt/c/Program Files/dotnet/dotnet.exe" build DeckFlow.Core/DeckFlow.Core.csproj && "/mnt/c/Program Files/dotnet/dotnet.exe" build DeckFlow.Core.Tests/DeckFlow.Core.Tests.csproj` exits 0, 0 new warnings
  </acceptance_criteria>
  <done>deck_queue/crawl_state logic lives in DeckQueueRepository; facade delegates; builds clean.</done>
</task>

<task type="auto">
  <name>Task 3: Extract CardCategoryRepository, reduce facade to a thin delegator, run the safety net</name>
  <read_first>
    DeckFlow.Core/Knowledge/CategoryKnowledgeRepository.cs (remaining card/category methods + private resolvers),
    DeckFlow.Core/Normalization/CardNormalizer.cs,
    DeckFlow.Core/Reporting (CategoryFilter),
    DeckFlow.Core.Tests/CategoryKnowledgeRepositoryTests.cs,
    DeckFlow.Core.Tests/DeckCategoryCacheWriterTests.cs
  </read_first>
  <files>DeckFlow.Core/Knowledge/CardCategoryRepository.cs, DeckFlow.Core/Knowledge/CategoryKnowledgeRepository.cs</files>
  <action>
    Create `CardCategoryRepository` (internal sealed class, namespace DeckFlow.Core.Knowledge) owning card_category_observations + card_deck_totals + sources + cards read/query/upsert plus filtering/normalization. Move the bodies of GetCategoriesAsync, GetCategoryRowsForCardAsync, GetCategoryRowsForCommanderAsync, ReplaceSourceRowsAsync, DeleteSourceDataAsync, PersistObservedCategoriesAsync, PersistCardDeckTotalsAsync, PersistDeckCategoryBatchAsync, GetCardDeckTotalsAsync, HasSourceDataAsync, and the private static helpers ResolveCardIdAsync, ResolveSourceIdForReadAsync, ResolveSourceIdAsync, ResolveDeckQueueIdForSourceAsync, UpsertCategoryObservationAsync, UpsertCardDeckTotalAsync, NormalizeBoard, FilterGenericCategoryRowsWithFallback, plus the private row types CategoryKnowledgeAggregateRow and BoardDeckTotalRow. Preserve the ArchidektLiveSourcePrefix constant and the sources→deck_queue join in GetCategoryRowsForCommanderAsync. After this task CategoryKnowledgeRepository is a thin facade: ctors build the three collaborators, expose DatabasePath, and every public/internal method is a one-line delegation. Then run the full safety net to prove behavior preservation.
  </action>
  <verify>Build the solution, then run the Knowledge safety-net test filter — all pass</verify>
  <acceptance_criteria>
    - source: CardCategoryRepository.cs exists; `grep -c "ON CONFLICT" DeckFlow.Core/Knowledge/CategoryKnowledgeRepository.cs` returns 0 (all upserts moved)
    - source: CategoryKnowledgeRepository.cs is now a thin facade — every public/internal method body forwards to a collaborator (no inline Dapper queries remain)
    - test-command: `"/mnt/c/Program Files/dotnet/dotnet.exe" build DeckFlow.sln` exits 0, 0 new warnings
    - test-command: `"/mnt/c/Program Files/dotnet/dotnet.exe" test DeckFlow.Core.Tests/DeckFlow.Core.Tests.csproj --filter "FullyQualifiedName~CategoryKnowledgeRepository|FullyQualifiedName~CategoryCacheSchemaParity|FullyQualifiedName~ContentHashDedup|FullyQualifiedName~DeckCategoryCacheWriter|FullyQualifiedName~ArchidektDeckCacheSession"` — 0 failed
  </acceptance_criteria>
  <done>Facade is thin and delegates to all three collaborators; the 17 round-trip facts + parity + dedup + cache-writer + cache-session tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This is a behavior-preserving internal refactor of an existing Core class. No new input surface, no new external call, no new SQL, no dependency added. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-53-01 | Tampering | CategoryKnowledge SQL (DDL/UPSERT) preserved across the move | mitigate | Every SQL string moved verbatim; the 17 round-trip facts + parity/dedup tests are the regression guard (must pass before done). |

No new threats introduced. No package installs (Dapper is already in solution and unchanged).
</threat_model>

<verification>
- `dotnet build DeckFlow.sln` clean, 0 new warnings.
- The Knowledge safety-net test filter passes (17 facts + parity + dedup + cache writer + cache session).
- No caller (`CategoryKnowledgeStore.cs`, `DeckCommandRunners.cs`, `ArchidektDeckCacheSession.cs`, `DeckCategoryCacheWriter.cs`) was modified — the facade preserves the surface.
</verification>

<success_criteria>
CategoryKnowledgeRepository is a thin facade delegating to CategoryCacheSchema, DeckQueueRepository, and CardCategoryRepository; behavior is provably unchanged (existing tests green); the solution builds clean with no caller repoints.
</success_criteria>

<output>
Create `.planning/phases/53-architecture-backlog-burn-down/53-01-SUMMARY.md` when done.
</output>
