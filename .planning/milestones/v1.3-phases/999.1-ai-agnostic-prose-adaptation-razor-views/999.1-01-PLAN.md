---
phase: 999.1-ai-agnostic-prose-adaptation-razor-views
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - DeckFlow.Web/Views/Deck/DeckAnalysis.cshtml
autonomous: true
requirements: [D-01, D-02, D-05]
must_haves:
  truths:
    - "DeckAnalysis.cshtml above-selector prose uses universal noun 'your AI' (no hardcoded 'ChatGPT' brand) except in the Phase-12 enumerated lede form."
    - "DeckAnalysis.cshtml below-selector prose injects @aiPlatform.DisplayName via AiPlatform.Normalize(Model.TargetAiPlatform) for the user's chosen platform."
    - "The data-busy-title attribute on line 72 reads 'Building Deck Analysis Packet' (matches Phase 13 class naming and sibling pages' busy titles)."
    - "Phase 10 / Phase 13 D-08 invariants on the same file are NOT touched: class='deck-form chatgpt-packets-form', data-cache-key='chatgpt-packets', data-chatgpt-packets-form, data-chatgpt-current-step, data-chatgpt-ui-mode, chatgpt-step-eyebrow CSS class all survive byte-identical."
  artifacts:
    - path: "DeckFlow.Web/Views/Deck/DeckAnalysis.cshtml"
      provides: "Hybrid-prose Razor view with AiPlatform.Normalize block and data-busy-title rename"
      contains: "@{ var aiPlatform = AiPlatform.Normalize(Model.Request.TargetAiPlatform); }"
  key_links:
    - from: "DeckFlow.Web/Views/Deck/DeckAnalysis.cshtml"
      to: "DeckFlow.Web.Models.AiPlatform.Normalize"
      via: "Razor @{ var aiPlatform = AiPlatform.Normalize(...); } block + @aiPlatform.DisplayName injection sites"
      pattern: "AiPlatform\\.Normalize"
---

<objective>
Rewrite the 28 ChatGPT hits in `DeckAnalysis.cshtml` using the Hybrid pattern (D-01): universal noun `your AI` above the Step 2 `_AiSelector` partial (line 170), and `@aiPlatform.DisplayName` injection below. Rename `data-busy-title="Building ChatGPT Packets"` on line 72 to `"Building Deck Analysis Packet"` to match Phase 13 class naming and the two sibling pages' existing neutral busy titles. The line-30 page-lede enumerated form `"ChatGPT, Claude, or Gemini"` is preserved per D-05.

Purpose: Strip the ChatGPT brand from the primary multi-AI workflow page so it reads correctly for ChatGPT, Claude, and Gemini users while honoring the Phase 10 / Phase 13 D-08 / Phase 15 TS/CSS-coupled identifier carve-outs.
Output: Edited `DeckAnalysis.cshtml` with brand-neutral prose above the selector, platform-name injection below, and the busy-title rename.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/999.1-ai-agnostic-prose-adaptation-razor-views/999.1-CONTEXT.md
@DeckFlow.Web/Models/AiPlatform.cs
@DeckFlow.Web/Views/Deck/DeckAnalysis.cshtml
@DeckFlow.Web/Views/Shared/_AiSelector.cshtml
@CLAUDE.md

<interfaces>
<!-- Key contracts the executor consumes — extracted from the codebase. -->

From DeckFlow.Web/Models/AiPlatform.cs:
- `public sealed record AiPlatform(string Key, string DisplayName, string Description)`
- `public static AiPlatform Normalize(string? key)` — returns the matching AiPlatform or `Default` (ChatGpt) when null/empty/unrecognised.
- `Normalize(Model.Request.TargetAiPlatform).DisplayName` yields "ChatGPT" | "Claude" | "Gemini".

From DeckFlow.Web/Views/Shared/_AiSelector.cshtml (reference pattern — DO NOT MODIFY):
- `@model string` (currently-selected platform Key)
- Already uses an `@{ }` block for normalization at the top of the file.

DeckAnalysis.cshtml selector boundary:
- Line 170 contains `@await Html.PartialAsync("_AiSelector", Model.Request.TargetAiPlatform)`.
- Above line 170: ABOVE-selector prose (use `your AI`).
- Below line 170: BELOW-selector prose (use `@aiPlatform.DisplayName`).
- Line 164 `<p class="chatgpt-step-eyebrow">Step 2</p>` is structurally Step 2 setup but renders AT-selector — per D-01 edge case, AT-selector prose is treated as ABOVE (universal noun). The CSS class name `chatgpt-step-eyebrow` stays per Phase 13 D-08.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add AiPlatform.Normalize Razor block to top of DeckAnalysis.cshtml</name>
  <files>DeckFlow.Web/Views/Deck/DeckAnalysis.cshtml</files>
  <read_first>
    - .planning/phases/999.1-ai-agnostic-prose-adaptation-razor-views/999.1-CONTEXT.md (D-01 hybrid pattern, D-05 universal noun convention, code_context Phase 15 plumbing)
    - DeckFlow.Web/Views/Deck/DeckAnalysis.cshtml (whole file — confirm existing `@{ }` block location and `@model` directive)
    - DeckFlow.Web/Models/AiPlatform.cs (Normalize signature)
    - DeckFlow.Web/Views/Shared/_AiSelector.cshtml (reference pattern; DO NOT EDIT)
  </read_first>
  <action>
    1. Locate the existing top-of-file `@{ ... }` block in `DeckAnalysis.cshtml` (the block that declares per-view locals; typically right after the `@model` and any `@using` directives).
    2. Inside that block, add a single line: `var aiPlatform = AiPlatform.Normalize(Model.Request.TargetAiPlatform);` (use `Model.Request.TargetAiPlatform` — same shape as the existing `_AiSelector` invocation on line 170; confirm exact property path when you read the file). The `AiPlatform` type lives in `DeckFlow.Web.Models`. If the existing `@{ }` block does not already `using DeckFlow.Web.Models`, either add `@using DeckFlow.Web.Models` at the top of the file (after `@model`) OR fully-qualify the call (`DeckFlow.Web.Models.AiPlatform.Normalize(...)`). Pick whichever matches existing style — `_DeckToolTabs.cshtml` and `Home.cshtml` already use unqualified `DeckFlow.Web.Models.*` style; match the file's existing convention.
    3. Do NOT add any other locals, do NOT reformat existing locals, do NOT touch the form attributes on line 72 in this task (Task 3).
    4. Preserve LF line endings. Touch only the lines that need touching.
  </action>
  <verify>
    <automated>grep -c 'AiPlatform.Normalize(Model.Request.TargetAiPlatform)' DeckFlow.Web/Views/Deck/DeckAnalysis.cshtml</automated>
    <automated>dotnet build DeckFlow.Web/DeckFlow.Web.csproj -c Debug --nologo -v quiet</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'AiPlatform.Normalize(Model.Request.TargetAiPlatform)' DeckFlow.Web/Views/Deck/DeckAnalysis.cshtml` returns `1`.
    - `dotnet build DeckFlow.Web/DeckFlow.Web.csproj -c Debug` exits 0.
    - `grep -c '@model' DeckFlow.Web/Views/Deck/DeckAnalysis.cshtml` unchanged from baseline (the `@model` directive count is identical).
    - No new `using` lines beyond `DeckFlow.Web.Models` (if added).
  </acceptance_criteria>
  <done>The Razor view declares `aiPlatform` and the project builds clean.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite ChatGPT prose in DeckAnalysis.cshtml using Hybrid pattern</name>
  <files>DeckFlow.Web/Views/Deck/DeckAnalysis.cshtml</files>
  <read_first>
    - .planning/phases/999.1-ai-agnostic-prose-adaptation-razor-views/999.1-CONTEXT.md (D-01 hybrid pattern, D-05 universal noun, deferred Phase 13 D-08 carve-outs)
    - DeckFlow.Web/Views/Deck/DeckAnalysis.cshtml (full file)
    - .planning/phases/13-chatgpt-class-rename-summary-doc-comments/13-CONTEXT.md (D-08 deferred identifier list)
  </read_first>
  <action>
    Edit each ChatGPT hit in `DeckAnalysis.cshtml` per the Hybrid pattern. Use the selector boundary at line 170 (`@await Html.PartialAsync("_AiSelector", ...)`) to decide universal vs injection.

    NOTE ON THE LINE-72 ATTRIBUTE: This task does NOT touch the `data-busy-title="Building ChatGPT Packets"` attribute on line 72 — that rename is handled by Task 3. Therefore, immediately after Task 2 completes, `grep -c 'ChatGPT' DeckFlow.Web/Views/Deck/DeckAnalysis.cshtml` will return `2` (the line-30 enumerated lede AND the still-present line-72 `data-busy-title`). Task 3 brings the final count down to `1`. The verify gate and acceptance criterion below are intentionally `-le 2` to reflect this Task 2 → Task 3 ordering.

    KEEP AS-IS (per D-05 lede preservation + carve-outs):
    - Line 30 `<p class="page-lede">Generate a prompt to paste into ChatGPT, Claude, or Gemini.</p>` — KEEP (Phase 12 enumerated lede form per D-05 exception clause).
    - Line 72 `data-busy-title="Building ChatGPT Packets"` — KEEP IN THIS TASK (renamed by Task 3).
    - Class names like `class="chatgpt-packets-form"`, attribute names `data-cache-key="chatgpt-packets"`, `data-chatgpt-*`, `chatgpt-step-eyebrow` — KEEP (Phase 13 D-08 deferred).

    ABOVE-SELECTOR substitutions (universal noun `your AI` — pick the natural-reading variant per D-05 / Claude's Discretion):
    - Line 43 "what to paste into ChatGPT" → "what to paste into your AI". "and what to paste back into this page" stays.
    - Line 109 AriaLabel `"ChatGPT packet workflow steps"` → `"Deck Analysis packet workflow steps"` (page-named form; matches Phase 13 class rename for the workflow tablist, not `your AI` because it's an aria-label for the workflow tab strip — page-named reads better).
    - Line 178 "<li>Copy <code>analysis.txt</code> and paste it into ChatGPT.</li>" → "...paste it into your AI.</li>"
    - Line 179 "Ask ChatGPT to return the <code>deck_profile</code> JSON..." → "Ask your AI to return the <code>deck_profile</code> JSON..."
    - Line 185 "<strong>How to push back on ChatGPT's conclusions</strong>" → "<strong>How to push back on your AI's conclusions</strong>"
    - Line 186 "If ChatGPT makes a bad assumption..." → "If your AI makes a bad assumption..."
    - Line 188 "keep the conversation in ChatGPT going" → "keep the conversation with your AI going" (read naturally — "in ChatGPT" → "with your AI" is the variant that reads best; Claude's Discretion).
    - Line 195 "if you asked additional questions in ChatGPT" → "if you asked additional questions of your AI" (or "in the same AI conversation"; pick natural; Claude's Discretion).
    - Line 223 "tell ChatGPT what the deck is trying to do" → "tell your AI what the deck is trying to do"
    - Line 224 "tell ChatGPT what kinds of tables or threats..." → "tell your AI what kinds of tables or threats..."
    - Line 226 "ChatGPT will infer more from the decklist alone." → "your AI will infer more from the decklist alone."

    BELOW-SELECTOR substitutions (inject `@aiPlatform.DisplayName`):
    - Line 359 `<small class="sync-column__hint">...ChatGPT should return the decklist...` → `...@aiPlatform.DisplayName should return the decklist...`
    - Line 365 "the original deck's set code and collector number are provided to ChatGPT" → "...provided to @aiPlatform.DisplayName"
    - Line 371 "ChatGPT will use these names when assigning categories..." → "@aiPlatform.DisplayName will use these names when assigning categories..."
    - Line 398 "<h3>Suggested ChatGPT conversation title</h3>" → "<h3>Suggested @aiPlatform.DisplayName conversation title</h3>"
    - Line 399 "Rename your ChatGPT conversation to this..." → "Rename your @aiPlatform.DisplayName conversation to this..."
    - Line 421 "Paste this into ChatGPT for the main deck analysis." → "Paste this into @aiPlatform.DisplayName for the main deck analysis."
    - Line 435 "you usually do not need to paste it into ChatGPT separately" → "...into @aiPlatform.DisplayName separately"
    - Line 450 "ChatGPT should return a <code>deck_profile</code> JSON object matching this schema." → "@aiPlatform.DisplayName should return a <code>deck_profile</code> JSON object matching this schema."
    - Line 469 "the fenced <code>deck_profile</code> JSON block or raw JSON payload returned from ChatGPT" → "...returned from @aiPlatform.DisplayName"
    - Line 653 "Paste the returned <code>set-upgrade-analysis.txt</code> into ChatGPT." → "...into @aiPlatform.DisplayName."
    - Line 654 "Have ChatGPT return both a fenced..." → "Have @aiPlatform.DisplayName return both a fenced..."
    - Line 661 "to keep the ChatGPT prompt sendable" → "to keep the @aiPlatform.DisplayName prompt sendable"
    - Line 672 "Controls what ChatGPT looks for." → "Controls what @aiPlatform.DisplayName looks for."
    - Line 692 "Paste this into ChatGPT after you have a deck profile..." → "Paste this into @aiPlatform.DisplayName after you have a deck profile..." (and the second "It tells ChatGPT to return both..." → "It tells @aiPlatform.DisplayName to return both..." — same sentence, two hits).
    - Line 710 "the fenced <code>set_upgrade_report</code> JSON block or raw JSON payload returned from ChatGPT" → "...returned from @aiPlatform.DisplayName"

    Touch only the prose; do not reformat other lines, do not reflow JS/CSS class names, do not change the `chatgpt-step-eyebrow` CSS class on line 164, do not change Phase 10 invariants, do not change the line-72 `data-busy-title` (Task 3). Preserve LF line endings.
  </action>
  <verify>
    <automated>bash -c "count=$(grep -c 'ChatGPT' DeckFlow.Web/Views/Deck/DeckAnalysis.cshtml); test $count -le 2 || { echo \"FAIL: $count ChatGPT hits remaining (expect <=2: line-30 lede + line-72 data-busy-title still pending Task 3)\"; exit 1; }"</automated>
    <automated>grep -c '@aiPlatform.DisplayName' DeckFlow.Web/Views/Deck/DeckAnalysis.cshtml</automated>
    <automated>grep -c 'your AI' DeckFlow.Web/Views/Deck/DeckAnalysis.cshtml</automated>
    <automated>grep -c 'chatgpt-packets-form' DeckFlow.Web/Views/Deck/DeckAnalysis.cshtml</automated>
    <automated>grep -c 'data-cache-key="chatgpt-packets"' DeckFlow.Web/Views/Deck/DeckAnalysis.cshtml</automated>
    <automated>grep -c 'chatgpt-step-eyebrow' DeckFlow.Web/Views/Deck/DeckAnalysis.cshtml</automated>
    <automated>grep -c 'ChatGPT, Claude, or Gemini' DeckFlow.Web/Views/Deck/DeckAnalysis.cshtml</automated>
    <automated>dotnet build DeckFlow.Web/DeckFlow.Web.csproj -c Debug --nologo -v quiet</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'ChatGPT' DeckFlow.Web/Views/Deck/DeckAnalysis.cshtml` returns `2` or fewer (the line-30 enumerated lede `"ChatGPT, Claude, or Gemini"` AND the line-72 `data-busy-title="Building ChatGPT Packets"` which Task 3 will rename; the lede is preserved per D-05 exception, Task 3 brings final count to `1`).
    - `grep -c '@aiPlatform.DisplayName' DeckFlow.Web/Views/Deck/DeckAnalysis.cshtml` returns at least `13` (the below-selector injection sites listed above; the 2-hit line 692 contributes 2).
    - `grep -c 'your AI' DeckFlow.Web/Views/Deck/DeckAnalysis.cshtml` returns at least `10` (above-selector universal noun count; one occurrence is the "your AI's conclusions" possessive variant which still matches the pattern).
    - `grep -c 'chatgpt-packets-form' DeckFlow.Web/Views/Deck/DeckAnalysis.cshtml` returns `1` (Phase 13 D-08 invariant — CSS class preserved).
    - `grep -c 'data-cache-key="chatgpt-packets"' DeckFlow.Web/Views/Deck/DeckAnalysis.cshtml` returns `1` (Phase 13 D-08 invariant preserved).
    - `grep -c 'chatgpt-step-eyebrow' DeckFlow.Web/Views/Deck/DeckAnalysis.cshtml` returns `1` (Phase 13 D-08 invariant preserved).
    - `grep -c 'ChatGPT, Claude, or Gemini' DeckFlow.Web/Views/Deck/DeckAnalysis.cshtml` returns `1` (line-30 lede preserved per D-05 exception).
    - `dotnet build DeckFlow.Web/DeckFlow.Web.csproj -c Debug` exits 0.
  </acceptance_criteria>
  <done>All 28 above-selector/below-selector ChatGPT prose hits in DeckAnalysis.cshtml read correctly for any AiPlatform selection, build is clean, Phase 10/13 D-08 invariants preserved. The line-72 `data-busy-title` remains for Task 3 to rename.</done>
</task>

<task type="auto">
  <name>Task 3: Rename data-busy-title on DeckAnalysis.cshtml line 72</name>
  <files>DeckFlow.Web/Views/Deck/DeckAnalysis.cshtml</files>
  <read_first>
    - .planning/phases/999.1-ai-agnostic-prose-adaptation-razor-views/999.1-CONTEXT.md (D-02 #3 — data-busy-title rename target value; Claude's Discretion #5 — also audit data-busy-message)
    - DeckFlow.Web/Views/Deck/DeckAnalysis.cshtml line 72 (the dense form-tag line with many other attributes that must survive byte-identical)
    - DeckFlow.Web/Views/Deck/DeckComparison.cshtml line 171 (reference — existing neutral busy title)
    - DeckFlow.Web/Views/Deck/CedhMetaGap.cshtml line 40 (reference — existing neutral busy title)
  </read_first>
  <action>
    On line 72 of `DeckAnalysis.cshtml` (the `<form>` tag), surgically change ONLY the `data-busy-title` attribute value:
    - From: `data-busy-title="Building ChatGPT Packets"`
    - To: `data-busy-title="Building Deck Analysis Packet"`

    Audit the sibling `data-busy-message="Resolving the deck and generating prompt files."` attribute — per CONTEXT.md Claude's Discretion #5, this attribute is already brand-neutral and needs NO change. Confirm by inspection; do not edit.

    Do NOT touch any other attribute on line 72:
    - `class="deck-form chatgpt-packets-form"` — Phase 13 D-08 (KEEP)
    - `data-cache-key="chatgpt-packets"` — Phase 13 D-08 (KEEP)
    - `data-chatgpt-packets-form` — Phase 13 D-08 (KEEP)
    - `data-chatgpt-current-step="@currentStep"` — Phase 13 D-08 (KEEP)
    - `data-chatgpt-ui-mode="guided"` — Phase 13 D-08 (KEEP)
    - `data-set-options-url=...` — irrelevant (KEEP)
    - `action="@Url.Content("~/deck-analysis")"` — Phase 12 (KEEP)

    Touch only the lines that need touching. Preserve LF line endings.
  </action>
  <verify>
    <automated>grep -c 'data-busy-title="Building Deck Analysis Packet"' DeckFlow.Web/Views/Deck/DeckAnalysis.cshtml</automated>
    <automated>grep -c 'data-busy-title="Building ChatGPT Packets"' DeckFlow.Web/Views/Deck/DeckAnalysis.cshtml</automated>
    <automated>grep -c 'data-cache-key="chatgpt-packets"' DeckFlow.Web/Views/Deck/DeckAnalysis.cshtml</automated>
    <automated>grep -c 'chatgpt-packets-form' DeckFlow.Web/Views/Deck/DeckAnalysis.cshtml</automated>
    <automated>grep -c 'data-busy-message="Resolving the deck and generating prompt files."' DeckFlow.Web/Views/Deck/DeckAnalysis.cshtml</automated>
    <automated>dotnet build DeckFlow.Web/DeckFlow.Web.csproj -c Debug --nologo -v quiet</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'data-busy-title="Building Deck Analysis Packet"' DeckFlow.Web/Views/Deck/DeckAnalysis.cshtml` returns `1`.
    - `grep -c 'data-busy-title="Building ChatGPT Packets"' DeckFlow.Web/Views/Deck/DeckAnalysis.cshtml` returns `0`.
    - `grep -c 'data-cache-key="chatgpt-packets"' DeckFlow.Web/Views/Deck/DeckAnalysis.cshtml` returns `1` (Phase 13 D-08 preserved).
    - `grep -c 'chatgpt-packets-form' DeckFlow.Web/Views/Deck/DeckAnalysis.cshtml` returns `1` (Phase 13 D-08 preserved).
    - `grep -c 'data-busy-message="Resolving the deck and generating prompt files."' DeckFlow.Web/Views/Deck/DeckAnalysis.cshtml` returns `1` (unchanged, already neutral).
    - `dotnet build DeckFlow.Web/DeckFlow.Web.csproj -c Debug` exits 0.
  </acceptance_criteria>
  <done>The loading-overlay title is brand-neutral and matches Phase 13 class naming; Phase 13 D-08 attributes survive byte-identical.</done>
</task>

</tasks>

<verification>
After all three tasks:
- `grep -c 'ChatGPT' DeckFlow.Web/Views/Deck/DeckAnalysis.cshtml` returns exactly `1` (only the line-30 enumerated lede).
- `grep -nE 'data-cache-key|data-chatgpt-|chatgpt-packets-form|chatgpt-step-eyebrow' DeckFlow.Web/Views/Deck/DeckAnalysis.cshtml` returns identical line set to pre-phase baseline (Phase 13 D-08 invariants intact).
- `dotnet build DeckFlow.Web/DeckFlow.Web.csproj -c Release` exits 0.
</verification>

<success_criteria>
- DeckAnalysis.cshtml reads correctly for ChatGPT, Claude, and Gemini selections.
- Page-12 lede and all Phase 13 D-08 / Phase 10 identifier invariants preserved.
- `data-busy-title` matches the post-Phase-13 class naming and the sibling pages' neutral form.
- Build is clean.
</success_criteria>

<output>
Create `.planning/phases/999.1-ai-agnostic-prose-adaptation-razor-views/999.1-01-SUMMARY.md` when done.
</output>
