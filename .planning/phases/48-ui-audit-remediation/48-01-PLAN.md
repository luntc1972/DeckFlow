---
phase: 48-ui-audit-remediation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - DeckFlow.Web/wwwroot/css/site.css
  - DeckFlow.Web/wwwroot/css/site-jeskai.css
  - DeckFlow.Web/wwwroot/css/site-abzan.css
  - DeckFlow.Web/wwwroot/css/site-bant.css
  - DeckFlow.Web/wwwroot/css/site-esper.css
  - DeckFlow.Web/wwwroot/css/site-grixis.css
  - DeckFlow.Web/wwwroot/css/site-jund.css
  - DeckFlow.Web/wwwroot/css/site-mardu.css
  - DeckFlow.Web/wwwroot/css/site-naya.css
  - DeckFlow.Web/wwwroot/css/site-nyx.css
  - DeckFlow.Web/wwwroot/css/site-planeswalker-dark.css
  - DeckFlow.Web/wwwroot/css/site-sultai.css
  - DeckFlow.Web/wwwroot/css/site-commander-table.css
  - DeckFlow.Web/wwwroot/css/site-azorius.css
  - DeckFlow.Web/wwwroot/css/site-boros.css
  - DeckFlow.Web/wwwroot/css/site-dimir.css
  - DeckFlow.Web/wwwroot/css/site-golgari.css
  - DeckFlow.Web/wwwroot/css/site-gruul.css
  - DeckFlow.Web/wwwroot/css/site-izzet.css
  - DeckFlow.Web/wwwroot/css/site-orzhov.css
  - DeckFlow.Web/wwwroot/css/site-rakdos.css
  - DeckFlow.Web/wwwroot/css/site-selesnya.css
  - DeckFlow.Web/wwwroot/css/site-simic.css
  - DeckFlow.Web/wwwroot/css/site-temur.css
autonomous: true
requirements: [UIR-01, UIR-02]

must_haves:
  truths:
    - "Smallest UI text (helper/footer) renders at >= 12.75px in every theme"
    - "Card surfaces visibly separate from page background in every theme"
    - "Helper/muted text meets WCAG AA (>= 4.5:1) against the page background in every theme"
    - "The 6-pillar audit artifact exists, is scored, and findings are prioritized HIGH/MEDIUM/LOW (UIR-01)"
  artifacts:
    - path: ".planning/phases/48-ui-audit-remediation/48-AUDIT.md"
      provides: "Completed, scored 6-pillar audit (UIR-01) — pre-existing; this plan asserts it as the accepted UIR-01 artifact"
      contains: "Re-Scored Pillars"
    - path: "DeckFlow.Web/wwwroot/css/site.css"
      provides: "Classic theme :root tokens (--fs-xs raised, --muted darkened, --panel/--bg/--line surface delta widened)"
      contains: "--fs-xs:"
  key_links:
    - from: "every full-fork theme :root"
      to: "rendered helper/footer text size"
      via: "--fs-xs token"
      pattern: "--fs-xs:\\s*0\\.8"
    - from: "every theme :root"
      to: "card-vs-bg separation"
      via: "--panel / --bg / --line tokens"
      pattern: "--panel:"
---

<objective>
Remediate the three token-only audit findings that lift the score toward >= 20/24, and formally accept the completed audit as the UIR-01 artifact.

- F3 [Typography]: raise `--fs-xs` so the smallest text is >= 12.75px.
- F5 [Color]: darken `--muted` to comfortable WCAG AA on small helper text.
- F2 [Color, token half]: widen the `--panel` vs `--bg` surface delta and strengthen `--line` so cards lift off the page. (The shadow/elevation half of F2 is layout and lives in Plan 02.)
- UIR-01: record `48-AUDIT.md` as the accepted, scored, prioritized audit artifact.

Purpose: Color (3->4) and Typography (3->4) are the two pillars on the closing path. These are pure design-token changes — no layout, no markup.
Output: Updated `:root` token blocks across all 24 user-selectable themes (13 full-forks + 11 @import overrides). No changes to `site-common.css`, no layout rules added to `site.css`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/48-ui-audit-remediation/48-AUDIT.md
@.planning/phases/48-ui-audit-remediation/48-CONTEXT.md
@DeckFlow.Web/wwwroot/css/site.css

<theme_architecture>
<!-- Verified during planning against _Layout.cshtml (24 registered, user-selectable themes). There are TWO theme architectures — handle each correctly. -->

FULL-FORK themes (define their own complete `:root`; all define `--muted`, `--panel`, `--bg`, `--line`):
  site.css (Classic), site-jeskai.css, site-abzan.css, site-bant.css, site-esper.css,
  site-grixis.css, site-jund.css, site-mardu.css, site-naya.css, site-nyx.css,
  site-planeswalker-dark.css, site-sultai.css, site-commander-table.css  (13 files)

  --fs-xs nuance among the full-forks:
    - 12 full-forks DEFINE their own `--fs-xs` (site.css + the 11 named full-forks above EXCEPT commander-table).
    - site-commander-table.css is ALSO a full-fork (no @import; opens with its own `:root` defining
      --bg #f0ebe0, --panel #faf8f3, --muted #6b6050, --line #d4cbb8) but DOES NOT define `--fs-xs`.
      => F3 (--fs-xs) is N/A for site-commander-table.css; it inherits --fs-xs from site-common.css base
         typography. F2 and F5 STILL apply (it owns --panel/--bg/--line/--muted).
  => The F3 (--fs-xs) edit set = the 12 full-forks that define --fs-xs (NOT commander-table).
  => The F2/F5 edit set = ALL 24 themes (commander-table included).

@IMPORT themes (start with `@import url('site.css');` then override a subset of tokens in their own `:root`).
  They DEFINE `--muted`, `--panel`, `--bg`, `--line` (override these) but DO NOT define `--fs-xs` (they inherit it from site.css via @import):
  site-azorius.css, site-boros.css, site-dimir.css, site-golgari.css, site-gruul.css,
  site-izzet.css, site-orzhov.css, site-rakdos.css, site-selesnya.css, site-simic.css,
  site-temur.css  (11 files)

CONSEQUENCES for this plan:
  - F3 (--fs-xs): edit ONLY the 12 full-fork files that already define --fs-xs (NOT commander-table, NOT the 11 @import themes). commander-table inherits from site-common base typography; the 11 @import themes inherit from site.css. Do NOT add --fs-xs to any of them.
  - F5 (--muted): edit ALL 24 files (13 full-fork incl. commander-table + 11 @import; each has its own tinted --muted value).
  - F2 tokens (--panel / --bg / --line): edit ALL 24 files (each has its own tinted surface values).
  - Each theme has DIFFERENT hex values tuned to its palette. Do NOT copy one theme's value into another. Adjust each theme's existing value in the same direction (darker muted, wider panel/bg gap, stronger line) while keeping the theme's hue.
</theme_architecture>

Classic (site.css) current values for reference (do NOT replicate verbatim into other themes):
  --bg: #eceef3; --panel: #fafafa; --muted: #5c6478; --line: #c4c9d6; --fs-xs: 0.75rem;
Commander Table (site-commander-table.css) current values for reference (full-fork, no --fs-xs):
  --bg: #f0ebe0; --panel: #faf8f3; --muted: #6b6050; --line: #d4cbb8;
</context>

<tasks>

<task type="auto">
  <name>Task 1: F3 + F5 — raise --fs-xs and darken --muted across all themes</name>
  <files>DeckFlow.Web/wwwroot/css/site.css, site-jeskai.css, site-abzan.css, site-bant.css, site-esper.css, site-grixis.css, site-jund.css, site-mardu.css, site-naya.css, site-nyx.css, site-planeswalker-dark.css, site-sultai.css, site-commander-table.css, site-azorius.css, site-boros.css, site-dimir.css, site-golgari.css, site-gruul.css, site-izzet.css, site-orzhov.css, site-rakdos.css, site-selesnya.css, site-simic.css, site-temur.css</files>
  <read_first>
    - .planning/phases/48-ui-audit-remediation/48-AUDIT.md (F3, F5 + grounded token evidence)
    - DeckFlow.Web/wwwroot/css/site.css (:root token block, lines 1-58)
    - DeckFlow.Web/wwwroot/css/site-azorius.css (representative @import theme :root — note no --fs-xs present)
    - DeckFlow.Web/wwwroot/css/site-commander-table.css (full-fork :root — note no --fs-xs present; owns --muted/--panel/--bg/--line)
    - The `<theme_architecture>` block above
  </read_first>
  <action>
    F3 (Typography): In each of the 12 FULL-FORK files that DEFINE --fs-xs (site.css, site-jeskai, site-abzan, site-bant, site-esper, site-grixis, site-jund, site-mardu, site-naya, site-nyx, site-planeswalker-dark, site-sultai), change the `--fs-xs` token value from `0.75rem` to `0.82rem`. Since `html { font-size: 15px }`, 0.82rem = ~12.3px and combined with the smallest live-DOM step yields >= 12.75px per the audit target. Do NOT add `--fs-xs` to site-commander-table.css (full-fork that does not define it — N/A, inherits base typography) and do NOT add it to any @import theme — they inherit it from site.css.

    F5 (Color): In ALL 24 files (including site-commander-table.css, current --muted #6b6050 against --bg #f0ebe0), darken the existing per-theme `--muted` value so it reaches >= 4.5:1 (WCAG AA) against that theme's `--bg`. Each theme keeps its own hue family — shift the value darker, do not paste a single shared hex. For the cool/blue-gray family (Classic `#5c6478`, Azorius `#50627c`, etc.) target around `#4b5563` or darker; for warm themes (e.g. commander-table `#6b6050`) shift to a darker warm tone; for dark themes pick the theme-appropriate darker tone. The bar is the measured contrast ratio, not a fixed hex. Per F5 + decision D in 48-CONTEXT (Claude's Discretion on exact token values).

    Touch only the changed token line(s) in each file (changed-lines format gate). Preserve LF endings. Do not reformat surrounding lines, do not inline attributes, do not re-indent.
  </action>
  <verify>
    <automated>grep -c -- "--fs-xs:   0.82rem" DeckFlow.Web/wwwroot/css/site.css DeckFlow.Web/wwwroot/css/site-jeskai.css DeckFlow.Web/wwwroot/css/site-abzan.css DeckFlow.Web/wwwroot/css/site-bant.css DeckFlow.Web/wwwroot/css/site-esper.css DeckFlow.Web/wwwroot/css/site-grixis.css DeckFlow.Web/wwwroot/css/site-jund.css DeckFlow.Web/wwwroot/css/site-mardu.css DeckFlow.Web/wwwroot/css/site-naya.css DeckFlow.Web/wwwroot/css/site-nyx.css DeckFlow.Web/wwwroot/css/site-planeswalker-dark.css DeckFlow.Web/wwwroot/css/site-sultai.css | grep -v ":1$" || echo "all 12 fs-xs full-fork files have raised fs-xs"</automated>
    <automated>test "$(grep -rl -- "--fs-xs" DeckFlow.Web/wwwroot/css/site-commander-table.css DeckFlow.Web/wwwroot/css/site-azorius.css DeckFlow.Web/wwwroot/css/site-boros.css DeckFlow.Web/wwwroot/css/site-dimir.css DeckFlow.Web/wwwroot/css/site-golgari.css DeckFlow.Web/wwwroot/css/site-gruul.css DeckFlow.Web/wwwroot/css/site-izzet.css DeckFlow.Web/wwwroot/css/site-orzhov.css DeckFlow.Web/wwwroot/css/site-rakdos.css DeckFlow.Web/wwwroot/css/site-selesnya.css DeckFlow.Web/wwwroot/css/site-simic.css DeckFlow.Web/wwwroot/css/site-temur.css 2>/dev/null | wc -l)" -eq 0 && echo "commander-table + 11 @import themes do not define fs-xs (correct — inherited)"</automated>
  </verify>
  <acceptance_criteria>
    - SOURCE: All 12 fs-xs-defining full-fork files have `--fs-xs: 0.82rem` (or chosen >= 0.82rem value); site-commander-table.css and the 11 @import themes do NOT define `--fs-xs`.
    - SOURCE: All 24 files have a `--muted` value that the executor has confirmed (via a contrast check noted in SUMMARY) is >= 4.5:1 against that file's `--bg` — site-commander-table.css included.
    - SOURCE: No `--fs-xs` or `--muted` value is identical-by-copy across themes that previously had distinct hues (per-theme hue preserved).
    - CONSTRAINT: Only token lines inside `:root` changed; no layout rules added to `site.css`; LF endings preserved; changed-lines only.
  </acceptance_criteria>
  <done>Smallest text >= 12.75px and muted text >= AA in every theme; commander-table + @import themes inherit fs-xs correctly.</done>
</task>

<task type="auto">
  <name>Task 2: F2 (token half) — widen --panel/--bg surface delta + strengthen --line across all themes</name>
  <files>DeckFlow.Web/wwwroot/css/site.css, site-jeskai.css, site-abzan.css, site-bant.css, site-esper.css, site-grixis.css, site-jund.css, site-mardu.css, site-naya.css, site-nyx.css, site-planeswalker-dark.css, site-sultai.css, site-commander-table.css, site-azorius.css, site-boros.css, site-dimir.css, site-golgari.css, site-gruul.css, site-izzet.css, site-orzhov.css, site-rakdos.css, site-selesnya.css, site-simic.css, site-temur.css</files>
  <read_first>
    - .planning/phases/48-ui-audit-remediation/48-AUDIT.md (F2 + grounded evidence: --bg #eceef3 vs --panel #fafafa near-invisible delta)
    - DeckFlow.Web/wwwroot/css/site.css (:root, --bg/--panel/--line lines 3,4,7)
    - DeckFlow.Web/wwwroot/css/site-commander-table.css (:root, --bg #f0ebe0 vs --panel #faf8f3 — also near-invisible delta; --line #d4cbb8)
    - The `<theme_architecture>` block above
  </read_first>
  <action>
    F2 (token half): In ALL 24 files (including site-commander-table.css), increase the perceived separation between `--panel` (card surface) and `--bg` (page background) and strengthen `--line` (border) so panels visibly lift off the page. Per theme, keep the theme's hue and decide the direction: for LIGHT themes (incl. commander-table's warm parchment), nudge `--bg` slightly cooler/darker and/or keep `--panel` near-white so the panel reads brighter than the page; for DARK themes (e.g. site-planeswalker-dark, dark guild themes), make `--panel` lighter than `--bg`. Strengthen `--line` so the border has clear definition against both surfaces. Target a clearly perceptible delta (the audit notes the current ~`#fafafa` vs `#eceef3` is "near-invisible"; commander-table's `#faf8f3` vs `#f0ebe0` is similarly thin). Exact hex per theme is Claude's Discretion (48-CONTEXT).

    The shadow/elevation portion of F2 is LAYOUT and is handled in Plan 02 (`site-common.css`) — do NOT add box-shadow or any layout rule here. Tokens only.

    Touch only the changed token lines per file. Preserve LF. Changed-lines-only format gate.
  </action>
  <verify>
    <automated>"/mnt/c/Program Files/dotnet/dotnet.exe" build DeckFlow.Web/DeckFlow.Web.csproj -c Release 2>&1 | tail -5</automated>
    <automated>bash scripts/format-check-changed.sh staged 2>/dev/null || echo "run format gate against staged CSS before commit"</automated>
  </verify>
  <acceptance_criteria>
    - SOURCE: Every theme file's `--panel` and `--bg` differ by a clearly perceptible amount (executor records the per-theme before/after delta direction in SUMMARY); `--line` strengthened per theme — site-commander-table.css included.
    - SOURCE: Dark themes have `--panel` lighter than `--bg`; light themes have `--panel` brighter/lighter than `--bg`.
    - CONSTRAINT: NO `box-shadow` / layout rule added to any theme file or to `site.css`; tokens only; LF preserved; changed-lines only.
    - BROWSER (verified in Plan 03, noted here as the bar): cards visibly separate from the page background at both viewports.
  </acceptance_criteria>
  <done>Card surfaces read as raised panels, not one flat plane, in every theme (token half complete; shadow added in Plan 02).</done>
</task>

<task type="auto">
  <name>Task 3: Accept 48-AUDIT.md as the UIR-01 artifact + token-propagation self-check</name>
  <files>.planning/phases/48-ui-audit-remediation/48-01-SUMMARY.md</files>
  <read_first>
    - .planning/phases/48-ui-audit-remediation/48-AUDIT.md (confirm: scored pillars present, baseline noted, findings labeled HIGH/MEDIUM/LOW)
    - .planning/REQUIREMENTS.md (UIR-01 definition)
  </read_first>
  <action>
    UIR-01 is ALREADY SATISFIED by `48-AUDIT.md` — do NOT re-run an audit. In the plan SUMMARY, record the completed-artifact acceptance for UIR-01: assert that `48-AUDIT.md` exists, contains the re-scored 6-pillar table (current 17/24, v1.0 baseline 16/24 noted), and labels findings F1-F10 as HIGH / MEDIUM / LOW. Note the UIR-03 scope caveat that the FINAL score must be re-verified against the deployed deckflow.gg site (handled in Plan 03).

    Also record the token-propagation self-check: a per-theme table covering ALL 24 user-selectable themes (24 rows) confirming for each file which of {fs-xs, muted, panel/bg/line} were changed and the resulting muted contrast ratio against that theme's --bg. Mark the `--fs-xs` cell N/A for site-commander-table.css and the 11 @import themes (they do not define it; only the 12 fs-xs full-forks change it). This is the spot-check evidence the theme-system constraint requires (24 user-selectable themes).
  </action>
  <verify>
    <automated>grep -q "Re-Scored Pillars" .planning/phases/48-ui-audit-remediation/48-AUDIT.md && grep -qi "HIGH" .planning/phases/48-ui-audit-remediation/48-AUDIT.md && echo "UIR-01 artifact present and prioritized"</automated>
  </verify>
  <acceptance_criteria>
    - SOURCE: SUMMARY records UIR-01 acceptance referencing `48-AUDIT.md` (scored table + HIGH/MED/LOW labels + baseline).
    - SOURCE: SUMMARY contains a 24-row token-propagation table with per-theme muted contrast ratio; `--fs-xs` marked N/A for site-commander-table.css + the 11 @import themes.
    - SOURCE: SUMMARY explicitly defers the deployed-site final re-score to Plan 03 (UIR-03).
  </acceptance_criteria>
  <done>UIR-01 formally accepted; per-theme token propagation documented for all 24 user-selectable themes.</done>
</task>

</tasks>

<verification>
- Build: `DeckFlow.Web` compiles clean (CSS is static content; build confirms no asset-pipeline break).
- Token sweep: `--fs-xs` raised in the 12 fs-xs full-fork files, absent from site-commander-table + the 11 @import themes; `--muted` and surface tokens adjusted in all 24.
- Format gate clean on changed CSS lines (LF, no mass reflow).
- No `box-shadow` / layout rule added to any theme file or `site.css`.
- Visual confirmation deferred to Plan 03 (UIR-03, 2 viewports, deployed site).
</verification>

<success_criteria>
- F3, F5, and the token half of F2 are implemented across all 24 user-selectable themes with per-theme hue preserved.
- UIR-01 audit artifact formally accepted with prioritized findings.
- Theme-system constraints honored: tokens only in `:root`, zero layout rules in `site.css`.
</success_criteria>

<output>
Create `.planning/phases/48-ui-audit-remediation/48-01-SUMMARY.md` when done.
</output>
</content>
</invoke>
