---
context: phase
phase: 71-ramp-draw-budget-advisory
task: 0
total_tasks: 12
status: planned_not_executed
last_updated: 2026-06-26T20:42:00.501Z
---

# Continue Here — Phase 71 (+ Phase 72 scoped)

<current_state>
Phase 71 is fully PLANNED and REVIEWED but NOT executed. No production code written
this session — everything committed is planning artifacts. Phase 72 is SPEC'd and
Codex-reviewed, ready to plan after 71.

Two ready next moves, your pick:
1. Execute Phase 71 (Codex implements wave-by-wave; Claude reviews).
2. Plan Phase 72 (/gsd-plan-phase 72).
</current_state>

<completed_work>
- Phase 70 closed out: MQ-02 already shipped + flag ON in prod (verified via Render MCP);
  OBS-1 SSRF host-check already fixed+tested in DeckSourceHost; 70-06 two-lens live-render
  verified (Playwright, real Brago Scryfall, 3 themes x 2 viewports). Memory corrected.
- Filed `.planning/captures/ramp-draw-unit-of-value-theory.md` (creator theory evaluation).
- Phase 71 PLANNED: 71-SPEC, 71-CONTEXT, 71-01..05 PLAN, 71-UI-SPEC — all committed.
  - Claude gsd-plan-checker: REVISE (controller VM-copy blocker + 2 warnings) -> fixed.
  - Codex gsd review (gpt-5.4 medium): BLOCK (2 HIGH + 1 MED) -> HIGH-1 dissolved by
    scope (glosses UI-only), HIGH-2 (budget draw predicate widened: wheels/Mystic Remora),
    MED (Download .txt test) -> fixed.
  - gsd-ui-checker: APPROVED (zero new theme tokens; issue/ok via border+color-mix, no fixed hex).
- Phase 72 SPEC'd + Codex SPEC-review (revised 3 HIGH + 3 MED + 1 LOW).
</completed_work>

<remaining_work>
- Phase 71 EXECUTION (not started): wave1=71-01 Core budget engine, wave2=71-02 Core verdict
  + builder, wave3=71-03 Web flag/controller/glosses, wave4=71-04 Razor+CSS+Playwright+docs,
  wave5=71-05 human visual verify.
- Phase 72 PLAN (not started): run /gsd-plan-phase 72 after 71 lands.
</remaining_work>

<decisions_made>
- 71: glosses UI-only; verdict + ramp/draw budget on both UI + prompt; one flag
  manabase.plain-language-verdict (OFF); Casual full / cEDH glosses-only.
- 71: IsRampOrDraw kept byte-identical; budget uses separate IsRampPieceForBudget /
  IsDrawPieceForBudget (latter widened for wheels + Mystic Remora).
- 72: split out (not folded into 71). Commander table move-out = DISPLAY-ONLY (keep
  report.Castability so AvgOnCurve/Health unchanged). Companion = inert side metadata,
  NOT a remapped Board (else deck-analysis drifts flag-OFF). Companion auto-detect =
  Archidekt category + Moxfield DIRECT API only; manual designator fallback otherwise.
  +3 companion tax is a heuristic. Multi-commander headline = worst-of. No +2 recast tax.
</decisions_made>

<blockers>
- Parallel session is co-committing + pushing on main (admin/e2e/bridge: commits 0d8dbbb1,
  aa04954e, 65638dcb). Phase 71 touches manabase files — mostly disjoint, but executing
  on main risks working-tree collisions with their churn. Coordinate or worktree-isolate.
</blockers>

## Required Reading (in order)
1. `.planning/phases/71-ramp-draw-budget-advisory/71-CONTEXT.md` — 71 locked decisions
2. `.planning/phases/71-ramp-draw-budget-advisory/71-UI-SPEC.md` — 71 visual contract (approved)
3. `.planning/phases/71-ramp-draw-budget-advisory/71-01..05-PLAN.md` — 71 executable plans
4. `.planning/phases/72-command-zone-commander-castability/72-SPEC.md` — 72 scope (Codex-revised)
5. `.planning/captures/ramp-draw-unit-of-value-theory.md` — origin of the advisory

## Critical Anti-Patterns (do NOT repeat these)
- [BYTE-IDENTITY]: flag OFF must be byte-identical. 71 + 72 both flag-gated; never let a
  new flag's plumbing change prod output when OFF. 72 specifically: do NOT mutate
  report.Castability and do NOT remap DeckEntry.Board globally.
- [SCOPE STAGING]: when committing 71/72 planning, stage ONLY phase files — the parallel
  session's e2e/playwright/admin edits live in the same tree; never `git add -A`.

## Infrastructure State
- No background servers left running (test web server on :5173 was stopped after 70-06).
- Codex this session: gpt-5.4 @ reasoning_effort=medium, sandbox danger-full-access.
- Local main ahead of origin by ~5 (mix of doc commits + parallel-session code). User pushes.

<context>
All work this session was planning/review, no implementation. The delegation model holds:
Codex codes, Claude plans/reviews. Phase 71 is the next thing to build; Phase 72 builds on it.
</context>

<next_action>
Start with: confirm whether to EXECUTE Phase 71 (Codex wave 1 = 71-01 Core ramp/draw budget
engine) or PLAN Phase 72. If executing on main, first check whether the parallel session has
settled (git log --oneline -5) to avoid working-tree collisions.
</next_action>
