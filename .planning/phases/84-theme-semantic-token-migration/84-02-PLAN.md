---
phase: 84-theme-semantic-token-migration
plan: 02
type: execute
wave: 2
depends_on: [84-01]
files_modified:
  - DeckFlow.Web/e2e/theming.spec.ts
autonomous: false
requirements: [THEME-02, THEME-03]
must_haves:
  truths:
    - "theming.spec.ts asserts computed --danger != computed --link for every theme in themeFiles"
    - "theming.spec.ts asserts --link/--focus/--cta-border resolve to a non-empty color in every theme (no orphaned raw --accent-strong regression)"
    - "In red guild themes (rakdos/boros/gruul/mardu/jund/naya/grixis/jeskai), error/danger text is visibly NOT the link color, confirmed at desktop (1280) and mobile (390) viewports"
    - "The only visible color delta vs pre-migration baseline is the ~8 already-migrated D1 sites; the ~30 decorative sites are unchanged"
    - "site-rakdos.css --link:#ff9ea4 override is retained (audit confirms still needed)"
    - "Full theming e2e suite is green; dotnet build clean"
  artifacts:
    - path: "DeckFlow.Web/e2e/theming.spec.ts"
      provides: "THEME-02 danger!=link regression guard + semantic-token resolution assertion across all themes"
      contains: "getPropertyValue('--danger')"
  key_links:
    - from: "DeckFlow.Web/e2e/theming.spec.ts"
      to: "getComputedStyle(document.documentElement) --danger vs --link"
      via: "string inequality per theme (Tier-2 pattern)"
      pattern: "getPropertyValue\\('--(danger|link)'\\)"
---

<objective>
Prove the migration (THEME-02 / THEME-03): add a permanent computed-style regression guard that
--danger can never equal --link in any theme, run it across all themes at desktop + mobile, and
capture visual spot-check evidence that the only color delta is the ~8 documented D1 shifts. Audit
the UI-VS-* per-fork workarounds now that --danger is decoupled.

Purpose: THEME-02 is a structural guarantee (error color must never coincide with a guild's brand
color); the new assertion makes any future re-introduction of a raw --accent-strong link/danger
collision a red test, not a visual-review miss.
Output: extended theming.spec.ts; green e2e run; before/after screenshot evidence; UI-VS audit note.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/84-theme-semantic-token-migration/84-CONTEXT.md
@.planning/phases/84-theme-semantic-token-migration/84-RESEARCH.md
@.planning/phases/84-theme-semantic-token-migration/84-VALIDATION.md
@.planning/phases/84-theme-semantic-token-migration/84-01-SUMMARY.md
@CLAUDE.md

<interfaces>
<!-- Existing e2e harness to EXTEND (do NOT reinvent). Verified 2026-07-04. -->
DeckFlow.Web/e2e/theming.spec.ts:
  - const themeFiles: string[]  // all theme css names incl site.css, site-commander-table.css
  - async function readThemeSnapshot(page, themeFile): sets deckflow-theme cookie, goto /deck-analysis,
    returns getComputedStyle reads from document.documentElement (rootAccent etc.)
  - async function setThemedViewport(page): 390x844 if project name includes 'mobile', else 1280x900
  - helpers normalizeColor(value), isRealColor(value)
  - Tier-2 pattern precedent: expect(new Set(...).size).toBeGreaterThanOrEqual(2)

Run: start server via scripts/run-web-test.sh (sets DECKFLOW_DISABLE_AUTO_BROWSER=true), then
  cd DeckFlow.Web && npx --no-install playwright test theming
NEVER open a Windows-host browser (CLAUDE.md constraint).

Red guild themes (accent-strong in the red family, verified by hex):
  rakdos #a92434, boros #8d2a15, gruul #812817, mardu #8f3729, jund #a6613f, naya #91482e,
  grixis #d16670, jeskai #922b23.

The ~8 already-migrated D1 sites that intentionally shift from --accent to --accent-strong shade
(from 84-01-SUMMARY / RESEARCH Pitfall 2): site.css:104,116 global :focus-visible outline;
site.css:1031 .cache-pill__reset; site.css:1255,1300,1348 the three >summary help-hint links;
site.css:1065 .run-button/.copy-button border; site-common.css:116,1957 already-migrated focus outlines.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend theming.spec.ts with the THEME-02 danger!=link regression guard</name>
  <files>DeckFlow.Web/e2e/theming.spec.ts</files>
  <read_first>
    - DeckFlow.Web/e2e/theming.spec.ts (full — reuse readThemeSnapshot / setThemedViewport / Tier-2 pattern)
    - .planning/phases/84-theme-semantic-token-migration/84-RESEARCH.md ("Validation Architecture" + "Wave 0 Gaps")
    - .planning/phases/84-theme-semantic-token-migration/84-VALIDATION.md (Wave 0 Requirements)
    - .planning/phases/84-theme-semantic-token-migration/84-01-SUMMARY.md (what actually shipped)
  </read_first>
  <action>
    Extend the ThemeSnapshot type and readThemeSnapshot to also read
    getComputedStyle(document.documentElement).getPropertyValue for '--danger', '--link', '--focus',
    and '--cta-border' (trimmed). Add a new Tier-1-style test that iterates every entry in themeFiles
    and asserts: (1) normalized --danger != normalized --link for that theme (the core THEME-02
    structural guard, with an assertion message naming the theme); (2) --link, --focus, and
    --cta-border each resolve to a non-empty real color (isRealColor) so a future regression that
    reintroduces an orphaned raw --accent-strong call site under a no-token fork is caught. Reuse the
    existing normalizeColor/isRealColor helpers and the deckflow-theme cookie mechanism; do NOT add
    new packages, new config, or a new spec file — extend this one. Do NOT add watch-mode flags.
    Note in a comment that rakdos is the strongest case (its --link:#ff9ea4 is deliberately distinct
    from both its red --accent-strong and the fixed --danger).
  </action>
  <acceptance_criteria>
    - theming.spec.ts contains a test that reads getPropertyValue('--danger') and getPropertyValue('--link') and asserts inequality per theme.
    - The assertion runs over the full themeFiles array, not a representative subset.
    - `cd DeckFlow.Web && npx --no-install playwright test theming` (against a headless run-web-test.sh server) passes, including the new danger!=link case for every theme — critically the 8 red guilds.
    - No new dependency added to DeckFlow.Web/package.json; no new spec file created.
    - `git diff DeckFlow.Web/e2e/theming.spec.ts` shows only additive extension (existing tiers intact).
  </acceptance_criteria>
  <verify>
    <automated>cd /mnt/c/users/chrislunt/source/personal/deckflow-cycle15/DeckFlow.Web && grep -q "getPropertyValue('--danger')" e2e/theming.spec.ts && grep -q "getPropertyValue('--link')" e2e/theming.spec.ts && git -C .. diff --quiet DeckFlow.Web/package.json && echo NO_NEW_DEPS</automated>
    <human-check>Start server via scripts/run-web-test.sh; then cd DeckFlow.Web && npx --no-install playwright test theming — full theming suite green including the new danger!=link case per theme.</human-check>
  </verify>
  <done>New danger!=link + token-resolution assertions cover all themes; theming e2e green; no new deps/specs.</done>
</task>

<task type="auto">
  <name>Task 2: Audit UI-VS-* workarounds and capture THEME-03 visual spot-check evidence</name>
  <files>DeckFlow.Web/wwwroot/css/site-rakdos.css (audited, expected unchanged); .planning/phases/84-theme-semantic-token-migration/84-02-SUMMARY.md (evidence)</files>
  <read_first>
    - .planning/phases/84-theme-semantic-token-migration/theme-baseline-pre84.json (the pre-migration computed-style baseline captured by 84-01 Task 0 — this is the fixed prior-state artifact to diff against)
    - DeckFlow.Web/wwwroot/css/site-rakdos.css (the UI-VS-02 --link:#ff9ea4 override + comment)
    - .planning/phases/84-theme-semantic-token-migration/84-01-SUMMARY.md (the ~8 shifting sites + decorative list)
    - .planning/phases/84-theme-semantic-token-migration/84-RESEARCH.md (Pitfall 2; "Validation Architecture" screenshot methodology; Phase 82 scratch-script precedent)
    - .planning/phases/84-theme-semantic-token-migration/84-VALIDATION.md (Manual-Only Verifications)
  </read_first>
  <action>
    (a) UI-VS-* audit: grep the wwwroot/css/ tree for every UI-VS-* tagged workaround. For each,
    determine whether the now-decoupled --danger makes it redundant. Per RESEARCH, the expected
    outcome is that site-rakdos.css's UI-VS-02 --link:#ff9ea4 override STAYS (it keeps link distinct
    from BOTH the red accent-strong AND the fixed danger — reverting it is a regression). Record the
    finding in the SUMMARY; only if an audited workaround is provably redundant AND its removal is
    byte-safe, note it as a follow-up candidate — do NOT remove rakdos's override in this phase.
    (b) THEME-03 no-drift diff (required across ALL 27 themes, per CONTEXT <verification>): using the
    SAME scratch-script + representative selector set that 84-01 Task 0 used, re-capture the
    post-migration computed-style snapshot from the migrated tree (headless run-web-test.sh server —
    NOT a new tracked dependency, NOT a Windows browser) for EVERY theme in themeFiles at light AND
    dark, and diff it against the committed pre-migration baseline at
    .planning/phases/84-theme-semantic-token-migration/theme-baseline-pre84.json (reference it BY PATH;
    do NOT re-derive a baseline from the already-mutated tree — the baseline only exists because
    84-01 Task 0 captured it first). The diff MUST show, for every theme x {light,dark}: exactly the
    ~8 D1-shift selectors changed value, all 19 swapped affordance selectors resolve to the SAME color
    as baseline (byte-identical — the swap is color-neutral), and every decorative selector unchanged.
    Write the per-theme pass/fail result table into the SUMMARY and save the post-migration snapshot
    alongside the baseline for reproducibility.
    (c) Red-guild error-surface proof (kept as-is): additionally capture desktop (1280x900) AND mobile
    (390x844) screenshots of the error/feedback surface in each of the 8 red guild themes
    (rakdos/boros/gruul/mardu/jund/naya/grixis/jeskai) showing error/danger text visibly distinct from
    link text. Attach all evidence paths/observations to the SUMMARY.
  </action>
  <acceptance_criteria>
    - A UI-VS-* audit table appears in the SUMMARY listing each tagged workaround and its keep/redundant disposition; rakdos UI-VS-02 dispositioned KEEP with rationale.
    - `git diff DeckFlow.Web/wwwroot/css/site-rakdos.css` is EMPTY (override retained).
    - Screenshot evidence captured for all 8 red guilds (desktop + mobile) showing error/danger text visibly distinct from link text.
    - The no-drift diff runs against the committed baseline file `.planning/phases/84-theme-semantic-token-migration/theme-baseline-pre84.json` (referenced by path, produced by 84-01 Task 0 before any CSS edit) across ALL themes x {light,dark} — NOT a 2-theme sample, NOT a baseline re-derived from the mutated tree.
    - The diff proves, for EVERY theme: exactly the ~8 D1-shift selectors changed value; all 19 swapped affordance selectors resolve identically to baseline (color-neutral swap); every decorative selector unchanged (THEME-03 no-drift backed by a concrete artifact, not executor discretion).
    - Evidence gathered via headless run-web-test.sh server only — no Windows-host browser opened.
  </acceptance_criteria>
  <verify>
    <automated>cd /mnt/c/users/chrislunt/source/personal/deckflow-cycle15 && git diff --quiet DeckFlow.Web/wwwroot/css/site-rakdos.css && echo RAKDOS_OVERRIDE_RETAINED && grep -rl "UI-VS-" DeckFlow.Web/wwwroot/css/ | sort</automated>
    <human-check>Review the captured screenshots: in each red guild theme, error/danger text is clearly a different color from link text at both desktop and mobile; no non-error, non-D1 surface changed color across the theme spot-check.</human-check>
  </verify>
  <done>UI-VS audit recorded (rakdos override kept); an all-27-themes x light/dark computed-style diff vs the committed theme-baseline-pre84.json shows only the ~8 D1 sites changed and the 19 swaps color-neutral; red-guild error-surface danger!=link captured desktop+mobile via headless server.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human sign-off on the color-correctness delta</name>
  <action>Present the theming e2e result, the red-guild screenshots, and the ~8 D1-shift before/after captures to the developer for visual sign-off on the intended color delta before the phase is considered complete.</action>
  <what-built>
    Semantic-token migration (84-01) plus the danger!=link regression guard and visual evidence
    (84-02 Tasks 1-2). The only intended color change is the ~8 D1 sites shifting from --accent to
    --accent-strong shade; error/danger is now decoupled from link in red guilds.
  </what-built>
  <how-to-verify>
    1. Confirm the theming e2e suite ran green (Task 1), including danger!=link for every theme.
    2. Review the red-guild screenshots (rakdos/boros/gruul/mardu/jund/naya/grixis/jeskai), desktop
       and mobile: error/danger text must read as danger red, clearly NOT the link color.
    3. Review the ~8 D1-shift before/after captures: confirm the shade change is acceptable and is the
       ONLY visible delta; decorative surfaces (badges, score pips, active-tab, headings) unchanged.
  </how-to-verify>
  <resume-signal>Type "approved" to accept the documented ~8-site delta, or describe any surface that changed unexpectedly.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

N/A — this plan adds a Playwright computed-style assertion and captures screenshots; it introduces
no runtime code path, no attacker-controllable input, and no server/DB/auth surface. All CSS was
already edited in 84-01.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-84-02 | (all) | e2e test + screenshot evidence | accept | ASVS: no new attacker-controllable input, no trust boundary crossed; test-only change, no package installs (reuses existing Playwright). |
</threat_model>

<verification>
- Extended theming e2e green across all themes (danger!=link + token resolution) — THEME-02 structural guard.
- Red-guild screenshots desktop + mobile confirm danger visually distinct from link — THEME-02 live proof.
- Before/after evidence confirms only the ~8 D1 sites shifted; decorative surfaces unchanged — THEME-03.
- rakdos UI-VS-02 override retained (empty diff); no new deps/specs.
</verification>

<success_criteria>
- THEME-02 satisfied: --danger never equals --link in any theme (permanent test guard) and is
  visibly distinct in red guilds at desktop + mobile.
- THEME-03 satisfied: theme render diff limited to the ~8 intended D1 shifts; no other surface changed.
- UI-VS-* workarounds audited; rakdos override correctly retained.
</success_criteria>

<output>
Create `.planning/phases/84-theme-semantic-token-migration/84-02-SUMMARY.md` when done. Include: the
UI-VS-* audit table, the danger!=link test coverage summary, screenshot evidence paths/observations
for the 8 red guilds and the ~8 D1-shift selectors, and confirmation that Typography/font-size work
remains handed to Phase 86 (D3).
</output>
