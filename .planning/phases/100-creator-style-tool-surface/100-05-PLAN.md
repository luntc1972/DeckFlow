---
phase: 100-creator-style-tool-surface
plan: 05
type: execute
wave: 3
depends_on: [100-01, 100-02, 100-03]
files_modified:
  - DeckFlow.Web/Models/CreatorStyleViewModel.cs
  - DeckFlow.Web/Controllers/CreatorStyleController.cs
  - DeckFlow.Web/Views/Deck/CreatorStyle.cshtml
  - DeckFlow.Web/wwwroot/css/site-common.css
  - DeckFlow.Web.Tests/CreatorStyleControllerTests.cs
  - DeckFlow.Web.Tests/CreatorStyleViewRenderTests.cs
  - DeckFlow.Web/e2e/creator-style.spec.ts
  - README.md
autonomous: true
requirements: [CS-31]
must_haves:
  truths:
    - "With the flag ON, a user picks a creator, submits a deck (URL or paste), and receives a ChatGPT-ready critique packet in one round-trip on a new tool page (D-100-11, D-100-13)."
    - "The creator picker is a native server-populated `<select>` listing only creators in the store, labeled with evidence depth 'N decks · M videos' — no free-text creator entry (D-100-09)."
    - "Deck input reuses the standardized site-dropdown + URL/paste toggle (data-sync-panel/data-df-select), single form -> result below, no multi-step wizard (D-100-10, D-100-11)."
    - "Result is a copy-ready packet block + a small summary strip (rubric verdict chips + exemplar deck names); grounding-degraded shows a warning banner with IN-03-branched wording; empty store shows a distinct 'no creator profiles loaded' info state, not a degradation banner (D-100-13, D-100-15, D-100-16)."
    - "Only one prompt arm ships — ChatGPT — no Claude/Gemini AI selector (D-100-14); copy is craft-first with no creator-crawl/KB-sourcing claims (D-100-12)."
    - "A reachable creator whose profile is InsufficientSample (present in the store, listed by the picker) returns a distinct `.info-banner` showing the Notice — never a silent bare-form re-render (D-100-16, checker BLOCKER 2)."
    - "The POST is CSRF-protected and the route 404s while the flag is OFF (T-100-01, T-100-04)."
  artifacts:
    - path: "DeckFlow.Web/Controllers/CreatorStyleController.cs"
      provides: "GET empty form + POST build, flag-gated, CSRF-protected, guarded error ladder"
      contains: "FeatureFlagGate(\"tool.creator-style.enabled\")"
    - path: "DeckFlow.Web/Views/Deck/CreatorStyle.cshtml"
      provides: "single-form tool page with picker + deck toggle + result panel"
      contains: "creator-style-input-source"
    - path: "DeckFlow.Web/e2e/creator-style.spec.ts"
      provides: "desktop+mobile e2e: flag-off 404, flag-on 200 empty-store info-state (committed seed is []); populated-form covered by xUnit"
      contains: "/creator-style"
  key_links:
    - from: "CreatorStyleController.CreatorStyle (POST)"
      to: "ICreatorStylePacketService.TryComputeCacheKeyAsync + BuildAsync"
      via: "read-side cache-key check then build (mirrors DeckPacketController)"
      pattern: "TryComputeCacheKeyAsync"
    - from: "CreatorStyleController.CreatorStyle (GET)"
      to: "ICreatorStyleProfileStore.GetAllAsync + IContentSiteIndexStore.GetPublishedRowsAsync"
      via: "server-side picker population with evidence-depth labels"
      pattern: "GetAllAsync"
---

<objective>
Ship the user-visible Creator-Style tool page: a controller (GET empty form, POST build), a view model, a Razor view cloned from the Manabase single-form shape, plus the CS-31 web bundle (controller tests, desktop+mobile e2e, README). This is the only user-facing surface in the milestone.

Purpose: Everything upstream (engine, flag, cache-bypass, store listing, seed) exists after Plans 01-04. This plan wires them into a page a user can actually use, honoring every page-UX decision (D-100-09 through D-100-16) and the approved UI-SPEC.
Output: A working, flag-gated, CSRF-protected tool page with correct picker/degraded/empty-store states, tested desktop+mobile, documented in README.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/100-creator-style-tool-surface/100-CONTEXT.md
@.planning/phases/100-creator-style-tool-surface/100-UI-SPEC.md
@.planning/phases/100-creator-style-tool-surface/100-PATTERNS.md
@.planning/phases/100-creator-style-tool-surface/100-VALIDATION.md

Template sources (clone shape, swap ids/types):
@DeckFlow.Web/Controllers/ManabaseController.cs
@DeckFlow.Web/Models/ManabaseViewModel.cs
@DeckFlow.Web/Views/Deck/Manabase.cshtml

Scope fences (binding):
- Deck-input toggle template is `Manabase.cshtml` (single-deck data-sync-panel/data-df-select), NOT DeckComparison.cshtml (which has no toggle) — RESEARCH.md Pitfall 1.
- Do NOT touch SitemapController.cs or reference SeoPaths (deferred post-merge; `/creator-style` already absent from IndexablePaths so D-100-07's sitemap bar is met by inaction).
- Do NOT include `_WorkflowStepTabs`; DO include `_DeckToolTabs` (D-100-11).
- Do NOT add an AI/model selector (`_AiSelector`) — ChatGPT-only arm (D-100-14).
- Reuse existing CSS classes (`.warning-banner`, `.error-banner`, `.info-banner`, `.copy-button`, `.run-button`, `.sync-columns`/`.sync-column`, existing chip classes like `.manabase-chip`). Introduce NO new CSS custom property (avoids the 15-theme-file fan-out); any minor layout rule goes ONLY in `site-common.css`, never `site.css`, restricted to on-grid multiples-of-4 rem values per UI-SPEC.
- Copy is craft-first (D-100-12): use the UI-SPEC Copywriting Contract strings verbatim; never mention crawling/KB/scraping/transcripts.
- LF endings; compiled `.js` is gitignored — do NOT stage wwwroot/js output; TS not needed (reuse deck-sync.js).
- e2e state note (checker HIGH-2, mechanism chosen): the committed prod seed files are `[]` (Plan 04), so the running e2e test server has EMPTY creator stores → GET /creator-style with the flag ON renders the D-100-16 empty-store `.info-banner`, NOT the form. There is no clean existing e2e mechanism to seed creator profiles into the test server (flag toggles have admin endpoints; creator profiles do not). CHOSEN: the e2e ON-path asserts the empty-store info-state at HTTP 200 (the genuine rendering under the committed seed), and the POPULATED-FORM path (picker + toggle visible) is covered by an xUnit controller/view test using fake stores that return a creator. This keeps prod seeds at `[]` and avoids brittle live-DB seeding. The URL/paste toggle JS itself is already covered generically by `manabase.spec.ts` (same deck-sync.js + data-sync-panel markup).
</context>

<tasks>

<task type="auto">
  <name>Task 1: CreatorStyleViewModel + CreatorStyleController (GET picker/empty-store, POST build with cache read-side + guarded error ladder)</name>
  <read_first>
    - DeckFlow.Web/Controllers/ManabaseController.cs (full — class shape, ctor null-guards, `[HttpGet]`/`[HttpPost]` + `[FeatureFlagGate]` + `[ValidateAntiForgeryToken]`, RunGuardedAsync error ladder ~174-224, LookupTimeout usage)
    - DeckFlow.Web/Controllers/DeckToolControllerBase.cs (CreateTimeoutScope, LookupTimeout=20s)
    - DeckFlow.Web/Models/ManabaseViewModel.cs (init-only view-model shape, HasResult pattern)
    - DeckFlow.Web/Controllers/DeckPacketController.cs (lines ~235-237 read-side: TryComputeCacheKeyAsync then _packetCache.TryGet then BuildAsync fallback)
    - DeckFlow.Web/Services/CreatorStyle/CreatorStylePacketService.cs (BuildAsync result shape incl. the new ProfileUnavailable flag from Plan 02, CreatorStylePacketResult fields, TryComputeCacheKeyAsync)
    - DeckFlow.Core/Content/ICreatorStyleProfileStore.cs (GetAllAsync + CreatorStyleProfileSummary from Plan 03)
    - DeckFlow.Core/Content/IContentSiteIndexStore.cs (GetPublishedRowsAsync — for the video-count-by-source map) and the shared `SlugifySourceName.Slugify` helper (DeckFlow.Core/Content/CreatorSourceStore.cs:111 uses it)
    - DeckFlow.Web/Models/CreatorStyleRequest.cs (DeckInputSource/DeckUrl/DeckText/CreatorSlug/Format shape)
  </read_first>
  <action>
    Create `CreatorStyleViewModel` (sealed, init-only) mirroring `ManabaseViewModel`: `ActiveTab = DeckPageTab.CreatorStyle`, `Request` (CreatorStyleRequest, default new), `ErrorMessage` (string?), `Result` (CreatorStylePacketResult?), `AvailableCreators` (IReadOnlyList of a small `CreatorPickerOption` record with Slug + DisplayLabel, default Array.Empty), `NoProfilesLoaded` (bool), and a `HasResult => Result is not null && !Result.ProfileUnavailable` computed property. Define `CreatorPickerOption` (Slug, DisplayLabel) as a nested or sibling record with `{ get; init; }`.

    Create `CreatorStyleController : DeckToolControllerBase` with ctor-injected `ICreatorStylePacketService`, `ICreatorStyleProfileStore`, `IContentSiteIndexStore`, `PacketSessionCache`, and `ILogger<CreatorStyleController>` (ArgumentNullException.ThrowIfNull each). Add a private `BuildPickerOptionsAsync(ct)` that: calls `_profileStore.GetAllAsync(ct)`; calls `_siteIndexStore.GetPublishedRowsAsync(ct)` ONCE and groups by `SlugifySourceName.Slugify(row.Source)` to a slug->videoCount map; produces one `CreatorPickerOption` per summary with DisplayLabel formatted per UI-SPEC: `{Slug-derived display name} — {MinDecks} decks · {videoCount} videos` (use the profile's display name if available; else the slug). Returns the option list.

    GET `[HttpGet("/creator-style")]` + `[FeatureFlagGate("tool.creator-style.enabled")]`: build picker options; if the list is empty, return the view with `NoProfilesLoaded = true` (D-100-16 distinct empty-store state — NOT an error banner); else return the view with `AvailableCreators` populated and an empty Request.

    POST `[HttpPost("/creator-style")]` + `[ValidateAntiForgeryToken]` + `[FeatureFlagGate("tool.creator-style.enabled")]`, `Task<IActionResult> CreatorStyle(CreatorStyleRequest request)`: null-coalesce request; wrap in a guarded execution helper cloned verbatim from `ManabaseController.RunGuardedAsync` (rename types to CreatorStyleRequest/CreatorStyleViewModel/"CreatorStyle") that catches OperationCanceledException (timeout copy), InvalidOperationException (validation), HttpRequestException (UpstreamErrorMessageBuilder.BuildScryfallMessage), and Exception (generic). Inside the body: read-side cache check — `var cacheKey = await _packetService.TryComputeCacheKeyAsync(request, token);` then `if (cacheKey is not null && _packetCache.TryGet<CreatorStylePacketResult>(cacheKey, out var cached)) result = cached; else result = await _packetService.BuildAsync(request, token);`. Re-populate `AvailableCreators` (POST must keep the picker rendered). Return the view with Request + Result. The view distinguishes ProfileUnavailable/GroundingDegraded/ErrorMessage from the result shape (no exception for unavailable — Plan 02 made it a typed flag).
  </action>
  <verify>
    <automated>dotnet build DeckFlow.Web/DeckFlow.Web.csproj -c Debug 2>&1 | grep -iE " error| warning" | grep -v "0 Warning" || echo BUILD CLEAN</automated>
  </verify>
  <acceptance_criteria>
    - Both actions carry `[FeatureFlagGate("tool.creator-style.enabled")]`; POST additionally carries `[ValidateAntiForgeryToken]`.
    - GET returns `NoProfilesLoaded = true` when GetAllAsync yields no creators; otherwise populates `AvailableCreators` with evidence-depth labels built from GetAllAsync + a single GetPublishedRowsAsync group-by.
    - POST computes a read-side cache key via `TryComputeCacheKeyAsync` and falls back to `BuildAsync`, wrapped in the four-catch guarded ladder.
    - No `_AiSelector` / model-arm selection anywhere; no SitemapController/SeoPaths reference.
    - `dotnet build DeckFlow.Web` clean.
  </acceptance_criteria>
  <done>The controller + view model exist, are flag-gated and CSRF-protected, populate the picker server-side, and route POST through the cache read-side then BuildAsync.</done>
</task>

<task type="auto">
  <name>Task 2: CreatorStyle.cshtml — single-form page (picker + URL/paste toggle + result panel + banners)</name>
  <read_first>
    - DeckFlow.Web/Views/Deck/Manabase.cshtml (page chrome/hero ~1-28, `_BusyIndicator` + `_DeckToolTabs` includes, error-banner ~26-28, form open + AntiForgeryToken ~168-171, URL/paste toggle block ~38-57, Scripts section ~721-725)
    - DeckFlow.Web/Views/Deck/DeckComparison.cshtml (copy-button + `data-copy-target` + readonly textarea idiom ~344-346/484-486; `.info-banner` markup idiom ~159)
    - DeckFlow.Web/Views/Deck/CedhMetaGap.cshtml (native `<select data-df-select>` Mode/SortBy idiom — the picker pattern; do NOT use its commander-name `<datalist>` free-text pattern)
    - DeckFlow.Web/Views/Shared/_DeckToolTabs.cshtml (the persistent nav strip — REQUIRED; distinct from _WorkflowStepTabs which is FORBIDDEN)
    - .planning/phases/100-creator-style-tool-surface/100-UI-SPEC.md (full Copywriting Contract + Color/Spacing/Typography constraints + Interaction contract)
    - DeckFlow.Web/wwwroot/css/site-common.css (existing chip/banner/result-panel classes to reuse)
  </read_first>
  <action>
    Create `Views/Deck/CreatorStyle.cshtml` (`@model DeckFlow.Web.Models.CreatorStyleViewModel`) resolved via the existing DeckViewLocationExpander (no new Views folder). Structure, cloning Manabase.cshtml:

    - Hero: H1 "Creator-Style Deck Critique"; lede + secondary lede from the UI-SPEC Copywriting Contract verbatim.
    - `@await Html.PartialAsync("_BusyIndicator")` and `@await Html.PartialAsync("_DeckToolTabs", Model.ActiveTab)`. Do NOT include `_WorkflowStepTabs` or `_AiSelector`.
    - Empty-store branch: when `Model.NoProfilesLoaded`, render ONLY the `.info-banner` "No creator profiles loaded yet." heading + body from the contract, and render NO form/picker/deck-input. Otherwise render the form.
    - Form: `<form method="post" action="@Url.Content("~/creator-style")" class="result-panel" ...busy attrs...>` + `@Html.AntiForgeryToken()`.
    - Creator picker: a native `<select id="creator-style-creator" name="CreatorSlug" data-df-select>` with label "Creator", one `<option value="@opt.Slug">@opt.DisplayLabel</option>` per `Model.AvailableCreators`, preselecting `Model.Request.CreatorSlug`. No free-text/datalist.
    - Deck input: clone the Manabase URL/paste toggle block verbatim, swapping ids `manabase-*` -> `creator-style-*` (`creator-style-input-source`, `data-sync-panel="creator-style-deck-url"`, `creator-style-deck-url`, `data-sync-panel="creator-style-deck-text"`, `creator-style-deck-text`), bound to DeckInputSource/DeckUrl/DeckText. Include `_DeckFlowBridgeHint` in the URL panel as Manabase does.
    - Primary CTA: `.run-button` labeled "Build Critique Packet".
    - Error banner: `.error-banner` bound to `Model.ErrorMessage` (hidden when empty), as Manabase does.
    - Profile-unavailable branch (D-100-16, checker BLOCKER 2): when `Model.Result?.ProfileUnavailable == true`, render a `.info-banner` displaying `Model.Result.Notice` (the service returns ProfileUnavailable=true for BOTH a missing profile AND an InsufficientSample creator — and InsufficientSample creators ARE listed by the picker, so this is a normal reachable POST outcome). This branch is SEPARATE from and OUTSIDE the `HasResult`-gated packet section, and is styled `.info-banner` (expected state), NOT the `.warning-banner` used for GroundingDegraded. Do NOT filter InsufficientSample creators out of the picker (D-100-09).
    - Result (when `Model.HasResult`): a `.warning-banner` shown when `Result.GroundingDegraded` displaying `Result.Notice` (IN-03-branched text already produced by the service); a small summary strip above the packet — rubric verdict chip(s) reusing an existing chip class (e.g. `.manabase-chip`) printing `Result.RubricScores` verdict strings VERBATIM, plus an "Exemplars: {names}" line from `Result.Exemplars`; then the copy-ready block: a `.copy-button data-copy-target="creator-style-packet-output"` labeled "Copy" and a single `<textarea id="creator-style-packet-output" readonly spellcheck="false">@Result.ArtifactText</textarea>`. No full rendered report, no multi-file decomposition (D-100-13).
    - Scripts section: include `busy-indicator.js`, `moxfield-extension-bridge.js`, `deck-sync.js` exactly as Manabase does (deck-sync.js drives the toggle; no new script).

    Any layout gap not covered by existing classes: add a minimal rule to `site-common.css` only, using on-grid rem values, and introduce NO new CSS custom property. Prefer reusing existing classes over new CSS.
  </action>
  <verify>
    <automated>dotnet build DeckFlow.Web/DeckFlow.Web.csproj -c Debug 2>&1 | grep -iE " error| warning" | grep -v "0 Warning" || echo BUILD CLEAN</automated>
  </verify>
  <acceptance_criteria>
    - The view includes `_DeckToolTabs` and does NOT include `_WorkflowStepTabs` or `_AiSelector`.
    - `grep -c "creator-style-input-source" DeckFlow.Web/Views/Deck/CreatorStyle.cshtml` >= 1; the toggle uses `data-sync-panel="creator-style-deck-url"` and `data-sync-panel="creator-style-deck-text"`.
    - The picker is a native `<select ... data-df-select>` with no `<datalist>` / free-text creator input.
    - A single readonly `<textarea id="creator-style-packet-output">` + `.copy-button data-copy-target="creator-style-packet-output"` renders the packet; warning/error/info banners use the existing `.warning-banner`/`.error-banner`/`.info-banner` classes.
    - Copy strings match the UI-SPEC Copywriting Contract; no crawl/KB/scrape/transcript wording; no new CSS custom property added (grep the diff of theme files shows no change).
    - When `Result.ProfileUnavailable` is true the view renders an `.info-banner` with `Result.Notice` OUTSIDE the `HasResult` packet section (no packet block, no warning-banner) — distinct from the GroundingDegraded `.warning-banner`.
    - `dotnet build DeckFlow.Web` clean.
  </acceptance_criteria>
  <done>The tool page renders the picker, URL/paste toggle, CTA, result copy-block, the ProfileUnavailable info-banner, and all three banner states, reusing existing classes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Controller xUnit tests + desktop+mobile e2e + README</name>
  <read_first>
    - DeckFlow.Web.Tests/... existing tool-controller test idioms (RunGuarded error-ladder assertions; how flag-gate/CSRF attributes are asserted; how ICreatorStylePacketService is faked via its internal test-seam ctor)
    - DeckFlow.Web/e2e/manabase.spec.ts (form render + toggle assertions ~9-31)
    - DeckFlow.Web/e2e/tool-toggles.spec.ts (flag-off 404 idiom + admin flip helpers `restoreAllTogglesOn`/toggle-off)
    - DeckFlow.Web/playwright.config.ts (chromium-desktop 1280x900 + chromium-mobile 390x844 — both run automatically)
    - README.md (## Highlights section + dated changelog convention — where sibling tools are documented)
  </read_first>
  <behavior>
    - Test: GET returns the view with picker options when the (faked) store lists creators; returns NoProfilesLoaded when the store is empty.
    - Test: POST success renders a result; POST that triggers HttpRequestException renders the upstream error copy; POST timeout renders the timeout copy (error-ladder coverage mirroring ManabaseController tests).
    - Test: both actions expose `[FeatureFlagGate("tool.creator-style.enabled")]` and POST exposes `[ValidateAntiForgeryToken]` (attribute-presence reflection assertion).
    - Test (checker BLOCKER 2): a POST for a creator whose BuildAsync returns `ProfileUnavailable = true` (InsufficientSample path) renders the view with the Notice surfaced (info-banner state) and NO packet block — asserted via the returned view model (`Result.ProfileUnavailable` true, `HasResult` false, `Result.Notice` non-empty).
    - e2e (flag OFF, seeded default): `/creator-style` returns 404. Runs on desktop + mobile projects.
    - e2e (flag ON, empty committed `[]` seed): `/creator-style` returns 200 and renders the empty-store `.info-banner` ("No creator profiles loaded yet.") with NO form — the honest rendering under the committed seed (checker HIGH-2). Zero console errors on load. Runs on desktop + mobile projects.
    - xUnit (populated-form path, RENDERED — Codex r2 MED): a render-level test in the `DeckPrimerBannerRenderTests.cs` style renders the real `Views/Deck/CreatorStyle.cshtml` with a populated view model (one creator) and asserts the actual HTML contains the creator `<select` markup, the `creator-style-input-source` toggle select, the URL panel visible / paste panel hidden (`data-sync-panel` attributes), and `form action="/creator-style"` — proving the populated one-round-trip form actually renders, not just that the view model is populated.
  </behavior>
  <action>
    Create `DeckFlow.Web.Tests/CreatorStyleControllerTests.cs` (namespace `DeckFlow.Web.Tests`) covering the GET picker/empty-store behaviors, the POST error ladder (fake `ICreatorStylePacketService` via its internal test-seam ctor overrides + a fake `ICreatorStyleProfileStore`/`IContentSiteIndexStore`), the flag-gate + CSRF attribute-presence assertions, AND the ProfileUnavailable/InsufficientSample POST path: stub the packet service to return a result with `ProfileUnavailable = true` + a non-empty `Notice`, POST, and assert the returned `CreatorStyleViewModel` has `Result.ProfileUnavailable == true`, `HasResult == false`, and `Result.Notice` populated (the D-100-16 distinct-state contract).

    Create `DeckFlow.Web/e2e/creator-style.spec.ts` with two tests: (1) flag-OFF (seeded default) → `/creator-style` returns 404 (mirror `tool-toggles.spec.ts`); (2) flag-ON via the existing admin-flip helper (`restoreAllTogglesOn`/toggle idiom in `tool-toggles.spec.ts`) → `/creator-style` returns 200 and the empty-store `.info-banner` heading text is visible while `form[action="/creator-style"]` is absent (checker HIGH-2 — the committed `[]` seed makes this the genuine ON rendering). Assert zero console errors on load as `manabase.spec.ts` does. No new playwright config. Do NOT seed creator profiles into the live test DB and do NOT change the committed seed files away from `[]`.

The populated-form assertions (picker present, `creator-style-input-source` toggle present, deck-text panel hidden) go into a RENDERED Razor view test, NOT the e2e and NOT view-model-only assertions (Codex r2 MED): add `CreatorStyleViewRenderTests.cs` (or a render-level case inside the controller test file) following the existing `DeckFlow.Web.Tests/DeckPrimerBannerRenderTests.cs` pattern — it renders the real `Views/Deck/CreatorStyle.cshtml` in-memory and asserts the HTML contains the creator `<select` element, the `creator-style-input-source` toggle, the `data-sync-panel` URL/paste panels, and `form action="/creator-style"` for a populated model, plus the empty-store `.info-banner` (form absent) for an empty model — see the behavior block.

    Update README.md: add a Highlights bullet + a short workflow note documenting the Creator-Style Critique tool (pick a creator, submit a deck, copy the ChatGPT-ready packet) and that it is gated by `tool.creator-style.enabled` (seeded OFF). Craft-first tone; no crawl/KB claims.
  </action>
  <verify>
    <automated>dotnet test DeckFlow.Web.Tests --filter "FullyQualifiedName~CreatorStyleControllerTests" 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - CreatorStyleControllerTests pass: GET picker + empty-store, POST error ladder (upstream/timeout/generic), attribute-presence (flag gate on both actions, CSRF on POST).
    - `DeckFlow.Web/e2e/creator-style.spec.ts` exists with a flag-off-404 test and a flag-on-200 test that asserts the empty-store `.info-banner` (NOT form selectors) under the committed `[]` seed; runs on desktop + mobile.
    - The populated-form contract (picker + toggle present, NoProfilesLoaded false) is asserted by an xUnit controller test with a fake store returning one creator.
    - README documents the tool + the seeded-OFF flag with no crawl/KB wording.
    - A controller test asserts the InsufficientSample/ProfileUnavailable POST path surfaces the Notice (info-banner state) with no packet block (checker BLOCKER 2).
    - Existing byte-identity suites still pass (no shared prompt path touched by this plan).
  </acceptance_criteria>
  <done>Controller tests and desktop+mobile e2e pass and README documents the workflow.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → POST /creator-style | Untrusted form input (deck URL/text, creator slug) crosses here; must be CSRF-protected and creator input constrained to the picker. |
| flag state → route reachability | The route must 404 while the flag is OFF; nav/tile must be hidden. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-100-01 | Elevation of Privilege | Route reachable while flag OFF | mitigate | `[FeatureFlagGate("tool.creator-style.enabled")]` on both actions → 404 when OFF; nav/tile hidden via existing ToolVisibility. e2e asserts the 404. |
| T-100-04 | Tampering (CSRF) | POST /creator-style | mitigate | `[ValidateAntiForgeryToken]` + `@Html.AntiForgeryToken()` in the form; attribute-presence test asserts it. |
| T-100-03 | Tampering (prompt injection) | Creator input | mitigate | Native picker populated only from `GetAllAsync`; no free-text creator field (D-100-09). Deck URL/text routes through the existing hardened IDeckEntryLoader importers (no new fetch surface). |
| T-100-SC | Tampering | npm/pip/cargo installs | accept | No package installs; reuses existing deck-sync.js/copy-button wiring, no new TS/npm deps. |
</threat_model>

<verification>
- `dotnet build DeckFlow.Web` clean (0 new warnings).
- `dotnet test DeckFlow.Web.Tests --filter FullyQualifiedName~CreatorStyleControllerTests` green.
- Full `dotnet test` (both projects) green; existing byte-identity suites unchanged.
- `npx --no-install playwright test creator-style` green on desktop + mobile (server via scripts/run-web-test.sh; flag ON → asserts empty-store info-state at 200 under the committed `[]` seed; flag OFF → 404). Populated-form rendering is verified by xUnit, not e2e.
- Manual UAT (flag ON locally): empty-store info state, degraded warning-banner wording branches, copy-button works, renders across guild themes at desktop + mobile breakpoints.
</verification>

<success_criteria>
A user with the flag ON can pick a creator, submit a deck, and copy a ChatGPT-ready critique packet on a new single-form page; the route 404s while OFF; picker/degraded/empty-store states are correct; desktop+mobile e2e + controller tests pass; README documents the workflow.
</success_criteria>

<output>
Create `.planning/phases/100-creator-style-tool-surface/100-05-SUMMARY.md` when done.
</output>
