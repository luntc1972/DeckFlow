---
phase: 89-content-hash-foundation
plan: 03
type: execute
wave: 2
depends_on: ["89-01"]
files_modified:
  - DeckFlow.Core/Content/ContentSyncDiffClassifier.cs
  - DeckFlow.Core.Tests/Content/ContentSyncDiffClassifierTests.cs
  - DeckFlow.Core.Tests/Content/OneSignatureSurfaceGuardTests.cs
autonomous: true
requirements: [SYNC-02]
must_haves:
  truths:
    - "ContentSyncDiffClassifier.Fingerprint is deleted; the equal-timestamp branch compares via the one unified signature (ContentSiteIndexContentSignature.AreContentEqual), so DirectPush, Pull, and reconcile all share a single body-inclusive signature. (D-03)"
    - "The indexed_utc timestamp-direction logic (ProdNewer/Diverged) is preserved unchanged, so the F-51-PG-01 timestamptz-direction guard still holds. (D-04 — body hash is the equal-timestamp tie-breaker)"
    - "An invariant test asserts exactly one signature-building surface remains in DeckFlow.Core.Content (no second Fingerprint-style scheme)."
  artifacts:
    - path: "DeckFlow.Core/Content/ContentSyncDiffClassifier.cs"
      provides: "Classifier on the unified signature; Fingerprint removed"
      contains: "AreContentEqual"
  key_links:
    - from: "ContentSyncDiffClassifier.Classify (equal-timestamp branch)"
      to: "ContentSiteIndexContentSignature.AreContentEqual"
      via: "unified body-inclusive comparator (the exact call DirectPushCoordinator.ClassifyDiff already makes)"
      pattern: "ContentSiteIndexContentSignature.AreContentEqual"
---

<objective>
Collapse the two divergent row-signature schemes into one (SYNC-02, D-03/D-04): delete `ContentSyncDiffClassifier.Fingerprint` and switch its equal-timestamp tie-breaker onto `ContentSiteIndexContentSignature.AreContentEqual` — the same comparator `DirectPushCoordinator.ClassifyDiff` already calls. Preserve the `indexed_utc` direction branches (ProdNewer / LocalNewer / Diverged) that guard the F-51-PG-01 timestamptz class. Add an invariant test asserting only one signature surface remains.

Purpose: This is the "one signature, one home" half of SYNC-02. Once 89-01 extended `BuildSignature` with body_sha256, both classifiers become body-hash-aware the moment the classifier drops its private subset fingerprint. DirectPush and Pull inherit this for free (no code change in those coordinators).
Output: Fingerprint removed; classifier on the unified signature; classifier tests updated; a one-signature-surface guard test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/89-content-hash-foundation/89-CONTEXT.md
@.planning/phases/89-content-hash-foundation/89-PATTERNS.md
</context>

<interfaces>
From DeckFlow.Core/Content/ContentSyncDiffClassifier.cs (per pattern map):
- `Classify(prodRows, localRows, logger)` — UTC-direction branches at ~55-61 (`prodUtc > localUtc` -> ProdNewer, `localUtc > prodUtc` -> local newer) MUST stay untouched (D-04).
- Equal-timestamp branch at ~63: currently `else if (!string.Equals(Fingerprint(prod), Fingerprint(local), StringComparison.Ordinal))` -> emits `SyncDiffKind.Diverged`.
- `private static string Fingerprint(ContentSiteIndexRow row)` at ~136-143 — the divergent subset (title/artifact_path/tags) to DELETE.

From 89-01 (dependency):
- `ContentSiteIndexContentSignature.AreContentEqual(a, b)` now body-hash-inclusive (compares body_sha256).

Reference (NO edit — verify they compile/benefit):
- DeckFlow.Studio/ViewModels/DirectPushCoordinator.cs line 166 already calls `ContentSiteIndexContentSignature.AreContentEqual`.
- DeckFlow.Studio/ViewModels/PullFromProdCoordinator.cs line 119 calls `ContentSyncDiffClassifier.Classify`.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Delete Fingerprint and switch classifier onto the unified signature</name>
  <read_first>
    - DeckFlow.Core/Content/ContentSyncDiffClassifier.cs (whole file — the Classify branches ~50-70 and the Fingerprint method ~136-143)
    - DeckFlow.Core.Tests/Content/ContentSyncDiffClassifierTests.cs (existing coverage to update — equal-timestamp-different-content cases)
    - 89-PATTERNS.md § "ContentSyncDiffClassifier.cs" (exact call-site swap; D-04 keep UTC-direction branches)
  </read_first>
  <behavior>
    - Equal timestamps + differing body_sha256 (all other columns equal) -> classifier emits Diverged (the body hash is now the tie-breaker — previously Fingerprint ignored the body entirely).
    - Equal timestamps + fully equal rows (including body_sha256) -> no diff entry.
    - prodUtc > localUtc -> ProdNewer branch unchanged; localUtc > prodUtc -> local-newer branch unchanged (D-04 direction logic intact, guards F-51-PG-01).
  </behavior>
  <action>In `ContentSyncDiffClassifier.cs`: delete the `Fingerprint` method (~136-143). In the equal-timestamp branch (~63) replace `!string.Equals(Fingerprint(prod), Fingerprint(local), StringComparison.Ordinal)` with `!ContentSiteIndexContentSignature.AreContentEqual(prod, local)`. Do NOT touch the `prodUtc > localUtc` / `localUtc > prodUtc` direction branches (D-04). Add `using` for the signature type if not already file-scoped. Update `ContentSyncDiffClassifierTests.cs`: add a case where two rows are identical except body_sha256 and assert Diverged; add a case where they are fully equal and assert no diff; keep/verify the existing direction-branch cases. Touch only changed lines; preserve LF.</action>
  <verify>
    <automated>dotnet build DeckFlow.Core -c Debug; dotnet test DeckFlow.Core.Tests --filter "FullyQualifiedName~ContentSyncDiffClassifierTests"</automated>
  </verify>
  <acceptance_criteria>
    - SOURCE: `grep -c "Fingerprint" DeckFlow.Core/Content/ContentSyncDiffClassifier.cs` returns 0 (method gone, no callers remain).
    - SOURCE: the equal-timestamp branch calls `ContentSiteIndexContentSignature.AreContentEqual` (grep confirms).
    - SOURCE: the two UTC-direction branches (`prodUtc`/`localUtc` compares) are still present and unchanged.
    - BEHAVIOR: body_sha256-only divergence now yields Diverged; fully-equal rows yield no diff; direction cases unchanged.
  </acceptance_criteria>
  <done>Fingerprint deleted; classifier compares via the one unified signature; direction logic preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Add one-signature-surface invariant test</name>
  <read_first>
    - 89-PATTERNS.md § "One signature, one home (SYNC-02 invariant)" (CarveOutGuard-style source-scan test)
    - An existing invariant/guard test in the repo (e.g. CarveOutGuard) for the source-scan idiom
  </read_first>
  <behavior>
    - The test scans `DeckFlow.Core/Content/*.cs` source and asserts no `Fingerprint`-style private signature-builder exists outside `ContentSiteIndexContentSignature` — i.e. `ContentSiteIndexContentSignature.BuildSignature`/`AreContentEqual` is the sole signature surface.
    - The test fails if a future edit reintroduces a second signature/fingerprint method in DeckFlow.Core.Content.
  </behavior>
  <action>Create `DeckFlow.Core.Tests/Content/OneSignatureSurfaceGuardTests.cs` (xUnit, `public sealed class`). Mirror the project's existing source-scan guard style (like CarveOutGuard): read the `DeckFlow.Core/Content` source files from the test working directory (resolve the repo path via `AppContext.BaseDirectory` walk-up, matching how existing guard tests locate source), assert that `"Fingerprint"` does not appear as a method definition in `ContentSyncDiffClassifier.cs`, and assert `BuildSignature` is defined in exactly one file (`ContentSiteIndexContentSignature.cs`). Keep the assertion message explicit about the SYNC-02 invariant.</action>
  <verify>
    <automated>dotnet test DeckFlow.Core.Tests --filter "FullyQualifiedName~OneSignatureSurfaceGuardTests"</automated>
  </verify>
  <acceptance_criteria>
    - SOURCE: `OneSignatureSurfaceGuardTests.cs` exists and locates DeckFlow.Core.Content source deterministically (no hardcoded absolute path).
    - BEHAVIOR: test passes now; a hypothetical reintroduced `Fingerprint` method would fail it (assertion targets exactly that).
  </acceptance_criteria>
  <done>An automated guard locks the "one signature surface" invariant so it cannot silently regress.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| prod row vs local row | classifier compares two in-memory rows; pure transform, no I/O, no untrusted execution |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-89-07 | Tampering | divergent-signature drift | mitigate | Deleting Fingerprint + the OneSignatureSurfaceGuard test removes the exact second-scheme drift SYNC-02 targets; the guard blocks regression. |
| T-89-08 | Repudiation | timestamptz direction bug (F-51-PG-01) | accept/mitigate | D-04 preserves the indexed_utc direction branches verbatim; classifier tests assert ProdNewer/local-newer behavior is unchanged. No new dependency (T-89-SC: none). |
</threat_model>

<verification>
- `DeckFlow.Core` + `DeckFlow.Core.Tests` build clean.
- Classifier tests (including new body_sha256-divergence case) and the one-signature-surface guard pass.
- `grep -c "Fingerprint" DeckFlow.Core/Content/ContentSyncDiffClassifier.cs` == 0.
</verification>

<success_criteria>
- Only one signature surface remains (ContentSiteIndexContentSignature); Fingerprint is gone.
- The classifier's equal-timestamp branch uses the unified, body-hash-aware comparator.
- The indexed_utc direction logic is preserved.
</success_criteria>

<output>
Create `.planning/phases/89-content-hash-foundation/89-03-SUMMARY.md` when done.
</output>
