# 999.1 Manual UAT

## Pre-flight

- [ ] User starts DeckFlow web locally (e.g., `dotnet run --project DeckFlow.Web` from a separate terminal). DO NOT auto-launch.
- [ ] Browser opens to `http://localhost:5173/` (or whichever port the user's launchSettings specifies).

## T1 — Deck Analysis page (`/deck-analysis`)

For each AiPlatform (ChatGPT, Claude, Gemini if DECKFLOW_GEMINI_ENABLED=true), perform a full round-trip:
- [ ] Load a deck in Step 1.
- [ ] Step 2: pick ChatGPT. Verify the prose around "Suggested ChatGPT conversation title" / "Paste this into ChatGPT for the main deck analysis." / "ChatGPT should return a deck_profile JSON object" reads naturally — these are below-selector and should say "ChatGPT" (because ChatGPT is the selected platform).
- [ ] Re-pick Claude in Step 2 and re-render the page. Verify the SAME below-selector prose now reads "Suggested Claude conversation title", "Paste this into Claude...", "Claude should return a deck_profile JSON object". Above-selector prose ("Ask your AI to return the deck_profile JSON inside a fenced ```json code block.") stays the same regardless of selection.
- [ ] Re-pick Gemini (if enabled). Verify same dynamic injection. Below-selector prose says "Gemini".
- [ ] Form busy overlay (visible briefly when generating the packet): title says "Building Deck Analysis Packet" (not "Building ChatGPT Packets").
- [ ] Paste an intentionally-bad JSON into Step 3 to trigger the error banner. Banner text should be "The submitted AI response did not contain a valid deck_profile payload." or "Paste the deck_profile JSON returned from your AI into Step 3." — NOT "ChatGPT".

## T4 — Deck Comparison page (`/deck-comparison`)

- [ ] Pick ChatGPT → page lede ("Generate a prompt comparing two decks. Paste into ChatGPT, Claude, or Gemini.") stays enumerated above selector. Below selector ("Paste this into ChatGPT to get the side-by-side comparison...") shows ChatGPT.
- [ ] Switch to Claude → below-selector copy changes to "Paste this into Claude...".
- [ ] Switch to Gemini (if enabled) → same dynamic injection.
- [ ] Paste bad JSON to trigger the error banner. Banner says "...AI response..." not "...ChatGPT response...".

## T5 — cEDH Meta-Gap page (`/cedh-meta-gap`)

- [ ] Pick each AI in turn, verify above-selector copy uses "your AI" and below-selector copy injects the selected DisplayName.
- [ ] Trigger error path (bad JSON), verify banner text.

## Hub + Help pages

- [ ] `/` Home page: DeckComparison hub card description reads "AI-authored breakdown" (not "ChatGPT-authored breakdown"). JudgeQuestions card still says "ChatGPT prompt generator as backup" (D-03 carve-out preserved).
- [ ] `/help/deck-analysis`, `/help/deck-comparison`, `/help/cedh-meta-gap` — scan for "ChatGPT" — should not appear. `/help/ask-a-judge` — still mentions ChatGPT (D-03 carve-out).

## JudgeQuestions carve-out (regression check)

- [ ] `/judge-questions` page renders unchanged: H2 "ChatGPT prompt generator", warning "ChatGPT can be confidently wrong about MTG rules", etc. all intact.

## Sign-off

- [x] All steps above passed → write "approved" in the chat to resume the executor.
- [ ] Any failure → describe what's wrong (which page, which AI, which prose); executor will record in 07-SUMMARY.md.

**Result:** approved — 2026-05-19 (user UAT walkthrough confirmed T1 / T4 / T5 / Hub+Help / JudgeQuestions regression all pass).
