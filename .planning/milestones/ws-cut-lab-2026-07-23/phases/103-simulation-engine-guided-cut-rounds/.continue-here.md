---
context: phase
phase: 103-simulation-engine-guided-cut-rounds
workstream: cut-lab
task: 3
total_tasks: 3
status: in_progress
last_updated: 2026-07-20T05:05:41.091Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> Discovered through failure this session. Acknowledge before proceeding.

- [ ] CONSTRAINT: **Echo-fake tests hide integration bugs** — Waves 4 and 5 passed their unit suites but FAILED blind verify. Fakes that echo exact card names never exercised DFC/split name mismatch, triple-resolve, or decide-path re-resolve. Mitigation: for any Cut Lab resolve/cache/decide change, add a counting-fake resolver test AND run the real-DI e2e (`cut-lab-structure`) before claiming done — do not trust filtered xUnit alone.
- [ ] CONSTRAINT: **Latency is an acceptance criterion, not a warning** — decide path was 5143ms (client aborts at 3000ms) and shipped "green" on unit tests because e2e was the only gate that exercised the real POST. Mitigation: keep the e2e latency-visible; any change touching the simulation/analysis path must re-run e2e and read the server-log `POST /*/decide` duration.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Echo-fake blindness | Unit fakes returned input names verbatim, masking DFC name-join loss (W4) and decide re-resolve (W5); both FAILED blind verify after "green" units | blocking | Counting-fake resolver test + real-DI `cut-lab-structure` e2e required before done-claim on any resolve/cache/decide change |
| Latency shipped as unit-green | 5143ms decide passed units; only e2e (real POST vs 3s client abort) caught it | blocking | e2e must run on any sim/analysis-path change; read server-log decide ms |
| Plan literal "7-row compare" | Plan asserted 7 compare rows; actual = 10 (families expand per-kind) | advisory | Assert against actual per-kind expansion, not family count |

## Critical Anti-Patterns (do NOT repeat these)
- ANTI-PATTERN: Trusting filtered xUnit as the completion gate for integration-shaped Cut Lab work → always add the counting-fake test + run e2e.
- ANTI-PATTERN: Codex orchestrator-path typos silently no-op a dispatch (att-1 of 103-02 wrote to a bad `-o` path, exit 1, zero repo mutation) → after every codex exec, audit `git log`/`git status` before treating it as run.

<current_state>
Phase 103 (Cut Lab simulation-engine-guided cut rounds), workstream cut-lab, branch `gsd/cycle18-cut-lab`.
9 of 10 plans complete + committed; plan 103-10 Tasks 1-2 (e2e + full gate) done; /simplify cleanup batch applied. ALL automated gates green. Paused AT the plan 103-10 Task 3 **human-verify checkpoint** (blocking) — awaiting user UAT sign-off. Working tree clean. HEAD = simplify commit `5e05de3a`.
</current_state>

<completed_work>

Plans (each Codex gpt-5.4 implemented, Claude blind-verified):
- 103-01 metric contract + determinism/timing — `396569be` (PASS_WITH_NOTES)
- 103-02 state envelope + working list — `23b9cdc0`
- 103-03 caches (+ fix batch) — `8c63c0fa`
- 103-04 cut round engine — `74b82ec3` (PASS)
- 103-05 simulation service + trials override (+ F1-F5 fixes + latency fixes `7f568f28`/`431b0440`) — `89534f55`
- 103-06 analysis context builder + intake (R1 FAIL DFC/triple-resolve → fixes `04e0f942`/`6c9ccf74` → R2 PASS_WITH_NOTES)
- 103-07 decide endpoints (R1 FAIL re-resolve/roundKey → fixes `c78b4c6a`/`8904761c`/`5f61445e`/`d37b8e83` + no-JS rehydration `3ad761e3`)
- 103-08 UI sections — `39161e60`
- 103-09 TS hydration + CSS (+ fixes `3aff8bf0`/`f488eefa`) — `37734744`
- 103-10 T1 e2e spec + T2 full gate — `fa13f6f9`/`e2150630`/`0191ba47`/`afa6f94d`
- /simplify batch (16 findings) — `1d239402` + `5e05de3a`

Latest gates (after simplify): build 0 warnings; CutLab xUnit 210/210; Manabase Core 394/394; vitest 56/56; tsc clean; Playwright `cut-lab-structure` 20/20. Decide latency 1634ms first / ~4ms cached (was 5143ms).
</completed_work>

<remaining_work>

- 103-10 Task 3: **human-verify checkpoint** (blocking) — user walkthrough of delta readability, neutral framing (CUT-02), perceived latency (~1s / 3s cap), compare panel, lossless restore, 2 themes × 2 viewports.
- After "approved": phase-close chain per execute-phase.md — `code_review_gate` (Skill gsd-code-review 103), `regression_gate`, `schema_drift_gate` (n/a, no schema), `gsd-verifier` (VERIFICATION.md), `update_roadmap` (`gsd-sdk query phase.complete 103`), close phase todos, evolve PROJECT.md.
- Milestone note: Cycle 18 lands on `main` per memory; Cycle 17 is ON-HOLD until this does. Branch stays local; user pushes.
</remaining_work>

<decisions_made>

- In-loop 4000 trials, baseline/summaries 20000; source-search trials scale proportionally (min 500) — killed the 5143ms decide.
- Non-finite metrics OMITTED (not NaN) — Web JSON throws on NaN; Casual/Focused lack InteractionLens.
- All name joins via `CutLabCardNames.Normalize` (front-face) — DFC resolver names broke silent joins.
- No-JS decide rehydrates intake from `CutLabState` (deck text from pool) — mini-forms post hidden fields only.
- Structural-analysis table NOT live-patched on JS decide (accepted residual); refreshes on server render.
- Compare table = 10 per-kind rows (Flood/Screw/Curve + category-by-turn ×3, EarlyInteraction omitted non-cEDH); plan's "7" was wrong.
- /simplify: engine owns round label/banner/known-key mappings; shared `CutLabMetricDelta.Between`; Unit single-sourced through DTOs; pool key computed once per decide; resolve/classification parallelized; `TrySeedFromSuperset` returns false on partial coverage.
</decisions_made>

<blockers>
- Human-verify checkpoint: blocking by design, awaits user. No technical blocker.
</blockers>

## Required Reading (in order)
1. `.planning/HANDOFF.json` — machine-readable state mirror of this file
2. `.planning/workstreams/cut-lab/phases/103-simulation-engine-guided-cut-rounds/103-10-PLAN.md` — Task 3 checkpoint spec + acceptance criteria
3. `.planning/workstreams/cut-lab/phases/103-simulation-engine-guided-cut-rounds/103-10-SUMMARY.md` — final e2e/gate results (human-verify marked PENDING)
4. `$HOME/.claude/get-shit-done/workflows/execute-phase.md` — the phase-close chain to run after approval (code_review_gate → regression_gate → verify_phase_goal → update_roadmap)
5. `.foreman/ledger.md` (untracked) — per-wave attempt/verify history

## Infrastructure State
- Branch `gsd/cycle18-cut-lab`, working tree clean, HEAD `5e05de3a`. Not pushed (user pushes).
- Server port 5173 free; no background processes running.
- Flag `tool.cut-lab.enabled` OFF (prod + default); enable locally for UAT only.
- Screenshots captured at `.planning/ui-design/cut-lab/screenshots/` (structure + cut-lab theme×viewport matrix).

<context>
Ran the full phase via foreman cross-AI pattern: Codex gpt-5.4 medium wrote every plan via `codex exec` (danger-full-access), Claude orchestrated waves and ran a fresh-context blind verifier after each. Two waves (4, 6→e2e) surfaced real bugs that unit suites missed; all fixed and re-verified. Everything automated is green. The only thing standing between here and phase-complete is the user's UAT sign-off on readability/latency/framing.
</context>

<next_action>
Re-present the 103-10 Task 3 human-verify checkpoint (steps in 103-10-PLAN.md Task 3 / prior turn). On "approved": run the execute-phase close chain starting with `Skill(gsd-code-review, "103")`, then regression gate, then `gsd-verifier`, then `gsd-sdk query phase.complete 103`. If the user reports issues, open a gap-closure loop instead.
</next_action>
