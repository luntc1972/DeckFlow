---
phase: 13-chatgpt-class-rename-summary-doc-comments
plan: 04
type: execute
wave: 4
depends_on:
  - 13-01
  - 13-02
  - 13-03
files_modified:
  - DeckFlow.Web.Tests/ChatGptCedhMetaGapServiceTests.cs
  - DeckFlow.Web.Tests/ChatGptDeckComparisonServiceTests.cs
  - DeckFlow.Web.Tests/ChatGptDeckPacketServiceTests.cs
  - DeckFlow.Web.Tests/ChatGptJsonTextFormatterServiceTests.cs
  - DeckFlow.Web.Tests/ChatGptPacketArtifactStoreRoundTripTests.cs
  - DeckFlow.Web.Tests/ChatGptPacketArtifactStoreTests.cs
  - DeckFlow.Web.Tests/ChatGptPhase10RoundTripTests.cs
  - DeckFlow.Web.Tests/ChatGptResponseParsersTests.cs
  - DeckFlow.Web.Tests/ChatGptResultContractTests.cs
  - DeckFlow.Web.Tests/MetaGapServiceTests.cs
  - DeckFlow.Web.Tests/DeckComparisonServiceTests.cs
  - DeckFlow.Web.Tests/DeckAnalysisPacketServiceTests.cs
  - DeckFlow.Web.Tests/JsonTextFormatterServiceTests.cs
  - DeckFlow.Web.Tests/PacketArtifactStoreRoundTripTests.cs
  - DeckFlow.Web.Tests/PacketArtifactStoreTests.cs
  - DeckFlow.Web.Tests/AiPlatformPhase10RoundTripTests.cs
  - DeckFlow.Web.Tests/ResponseParsersTests.cs
  - DeckFlow.Web.Tests/ResultContractTests.cs
  - DeckFlow.Web.Tests/DeckControllerTests.cs
  - DeckFlow.Web.Tests/TestDoubles/TestServiceFactory.cs
  - .planning/phases/13-chatgpt-class-rename-summary-doc-comments/13-HUMAN-UAT.md
autonomous: false
requirements:
  - CLASSRENAME-01
  - CLASSRENAME-02
  - CLASSRENAME-03

must_haves:
  truths:
    - "All 9 ChatGpt-prefixed test files in DeckFlow.Web.Tests/ are renamed via git mv to AI-agnostic filenames per Wave 4 enumeration in 13-RESEARCH.md L514-524"
    - "DeckControllerTests.cs (126 ChatGpt hits) has all 6 inline private sealed class test doubles renamed in lockstep (FakeChatGptDeckPacketService to FakeDeckAnalysisPacketService, FakeChatGptDeckComparisonService to FakeDeckComparisonService, FakeChatGptCedhMetaGapService to FakeMetaGapService, ConfigurableChatGptCedhMetaGapService to ConfigurableMetaGapService, ThrowingChatGptCedhMetaGapService to ThrowingMetaGapService, ThrowingChatGptDeckPacketService to ThrowingDeckAnalysisPacketService); test method names with ChatGpt prefix renamed per Open Question 2; all controller-ctor sites pass renamed Fake*/Throwing*/Configurable* doubles; Equal(\"ChatGpt*\", view.ViewName) literal assertions updated to renamed view names matching Phase 12 + Wave 3 action method names"
    - "TestServiceFactory.cs has 3 factory methods + 2 ILogger<T> generic args renamed (CreateChatGptDeckPacketService to CreateDeckAnalysisPacketService at L99, CreateChatGptDeckComparisonService to CreateDeckComparisonService at L129, CreateChatGptCedhMetaGapService to CreateMetaGapService at L151)"
    - "HelpContentServiceTests.cs fixture string `chatgpt-analysis` at L44/L48/L51 is NOT TOUCHED (permitted exception per Pitfall 3 + D-09 allowlist row 9)"
    - "Final build gate `dotnet build DeckFlow.sln --configuration Release` returns 0 errors, 0 new warnings"
    - "Final verification grep gate (allowlisted): `grep -rEn ChatGpt[A-Z] --include=*.cs DeckFlow.Web/ DeckFlow.Core/ DeckFlow.Web.Tests/ DeckFlow.Core.Tests/ | grep -vE \"ChatGPT\" | grep -vE \"ChatGPT/Claude/Gemini\"` returns ZERO hits"
    - "DeckFlow.CLI builds + runs all 5 commands clean (no broken DI registrations) — soft smoke check"
    - "HUMAN-UAT.md emitted at .planning/phases/13-chatgpt-class-rename-summary-doc-comments/13-HUMAN-UAT.md with the T1-T8 manual integration suite checklist (per D-09 SC4 + D-10 — VSTest unreliable in WSL, manual is the gate for behavioral parity)"
    - "Test method names containing ChatGpt prefix in DeckControllerTests.cs (e.g., ChatGptCedhMetaGap_Get_ReturnsExpectedViewModel) renamed in lockstep with action method renames per Open Question 2 — they are CLR identifiers that hit the grep gate"
  artifacts:
    - path: "DeckFlow.Web.Tests/DeckAnalysisPacketServiceTests.cs"
      provides: "Renamed from ChatGptDeckPacketServiceTests.cs; test class name + ~43 internal type refs updated to Wave-1/Wave-2 names"
      contains: "public sealed class DeckAnalysisPacketServiceTests"
    - path: "DeckFlow.Web.Tests/DeckComparisonServiceTests.cs"
      provides: "Renamed from ChatGptDeckComparisonServiceTests.cs"
      contains: "public sealed class DeckComparisonServiceTests"
    - path: "DeckFlow.Web.Tests/MetaGapServiceTests.cs"
      provides: "Renamed from ChatGptCedhMetaGapServiceTests.cs"
      contains: "public sealed class MetaGapServiceTests"
    - path: "DeckFlow.Web.Tests/JsonTextFormatterServiceTests.cs"
      provides: "Renamed from ChatGptJsonTextFormatterServiceTests.cs"
      contains: "public sealed class JsonTextFormatterServiceTests"
    - path: "DeckFlow.Web.Tests/PacketArtifactStoreRoundTripTests.cs"
      provides: "Renamed from ChatGptPacketArtifactStoreRoundTripTests.cs"
      contains: "public sealed class PacketArtifactStoreRoundTripTests"
    - path: "DeckFlow.Web.Tests/PacketArtifactStoreTests.cs"
      provides: "Renamed from ChatGptPacketArtifactStoreTests.cs"
      contains: "public sealed class PacketArtifactStoreTests"
    - path: "DeckFlow.Web.Tests/AiPlatformPhase10RoundTripTests.cs"
      provides: "Renamed from ChatGptPhase10RoundTripTests.cs — 121 hits resolved"
      contains: "public sealed class AiPlatformPhase10RoundTripTests"
    - path: "DeckFlow.Web.Tests/ResponseParsersTests.cs"
      provides: "Renamed from ChatGptResponseParsersTests.cs"
      contains: "public sealed class ResponseParsersTests"
    - path: "DeckFlow.Web.Tests/ResultContractTests.cs"
      provides: "Renamed from ChatGptResultContractTests.cs — `\"ChatGPT\"` string literal inside [InlineData] preserved as test input"
      contains: "public sealed class ResultContractTests"
    - path: ".planning/phases/13-chatgpt-class-rename-summary-doc-comments/13-HUMAN-UAT.md"
      provides: "T1-T8 manual integration suite checklist for the user to verify zero user-visible behavior change per D-09 SC4 + D-10"
      contains: "## T1"
  key_links:
    - from: "DeckFlow.Web.Tests/DeckControllerTests.cs"
      to: "DeckFlow.Web/Controllers/DeckController.cs"
      via: "ctor-passing test doubles + view-name string assertions matching renamed action methods"
      pattern: "(Fake|Throwing|Configurable)(DeckAnalysisPacketService|DeckComparisonService|MetaGapService)"
    - from: "DeckFlow.Web.Tests/TestDoubles/TestServiceFactory.cs"
      to: "DeckFlow.Web/Services/{DeckAnalysisPacketService,DeckComparisonService,MetaGapService}.cs"
      via: "static factory methods CreateDeckAnalysisPacketService / CreateDeckComparisonService / CreateMetaGapService"
      pattern: "Create(DeckAnalysisPacketService|DeckComparisonService|MetaGapService)"
---

<objective>
Close the rename loop in the test project: rename all 9 `ChatGpt*` test files via `git mv`, edit `DeckControllerTests.cs` (126 hits) to update controller-ctor calls + inline test doubles + test method names + view-name string assertions, edit `TestServiceFactory.cs` to update 3 factory methods + 2 `ILogger<T>` generic args. Run the final build gate `dotnet build DeckFlow.sln --configuration Release` and confirm 0 errors / 0 new warnings. Run the final verification grep gate with allowlist. Confirm DeckFlow.CLI still builds and runs 5 commands clean. Emit HUMAN-UAT.md with T1-T8 manual integration suite, then pause for user to confirm behavioral parity (zero user-visible change verified by zips round-tripping byte-identical).

Purpose: With Waves 1-3 done, the test project is the final code-side surface still referencing old type names. After this wave the entire solution is rename-complete and the build gate locks the result. Per CLAUDE.md "VSTest unreliable in WSL", `dotnet test` is NOT the gate — manual T1-T8 round-trip is.

Output: 9 renamed test files, 2 edited test files (DeckControllerTests.cs + TestServiceFactory.cs), 1 emitted HUMAN-UAT.md with T1-T8 checklist, build-clean confirmation, grep-gate-clean confirmation, CLI smoke confirmation, checkpoint for user to run T1-T8 manually.

Per CLAUDE.md memory: do NOT auto-launch the DeckFlow web app for manual UAT — the user starts the dev server themselves.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/13-chatgpt-class-rename-summary-doc-comments/13-CONTEXT.md
@.planning/phases/13-chatgpt-class-rename-summary-doc-comments/13-RESEARCH.md
@.planning/phases/13-chatgpt-class-rename-summary-doc-comments/13-PATTERNS.md
@.planning/phases/13-chatgpt-class-rename-summary-doc-comments/13-01-SUMMARY.md
@.planning/phases/13-chatgpt-class-rename-summary-doc-comments/13-02-SUMMARY.md
@.planning/phases/13-chatgpt-class-rename-summary-doc-comments/13-03-SUMMARY.md
@.planning/milestones/v1.2-MILESTONE-AUDIT.md
@.planning/codebase/CONVENTIONS.md
@CLAUDE.md

<test_file_rename_map>
Source: 13-RESEARCH.md L514-524

| Old path | New path | Hit count |
|---|---|---|
| DeckFlow.Web.Tests/ChatGptCedhMetaGapServiceTests.cs | DeckFlow.Web.Tests/MetaGapServiceTests.cs | 17 |
| DeckFlow.Web.Tests/ChatGptDeckComparisonServiceTests.cs | DeckFlow.Web.Tests/DeckComparisonServiceTests.cs | 13 |
| DeckFlow.Web.Tests/ChatGptDeckPacketServiceTests.cs | DeckFlow.Web.Tests/DeckAnalysisPacketServiceTests.cs | 43 |
| DeckFlow.Web.Tests/ChatGptJsonTextFormatterServiceTests.cs | DeckFlow.Web.Tests/JsonTextFormatterServiceTests.cs | 16 |
| DeckFlow.Web.Tests/ChatGptPacketArtifactStoreRoundTripTests.cs | DeckFlow.Web.Tests/PacketArtifactStoreRoundTripTests.cs | 3 |
| DeckFlow.Web.Tests/ChatGptPacketArtifactStoreTests.cs | DeckFlow.Web.Tests/PacketArtifactStoreTests.cs | 7 |
| DeckFlow.Web.Tests/ChatGptPhase10RoundTripTests.cs | DeckFlow.Web.Tests/AiPlatformPhase10RoundTripTests.cs | 121 |
| DeckFlow.Web.Tests/ChatGptResponseParsersTests.cs | DeckFlow.Web.Tests/ResponseParsersTests.cs | 16 |
| DeckFlow.Web.Tests/ChatGptResultContractTests.cs | DeckFlow.Web.Tests/ResultContractTests.cs | 20 |
</test_file_rename_map>

<inline_test_double_rename_map>
Source: 13-RESEARCH.md L529 + 13-PATTERNS.md "Test-double naming" section

Inside DeckControllerTests.cs lines ~775/781/810/831/844/857:
| Old test-double class (private sealed) | New name |
|---|---|
| FakeChatGptDeckPacketService | FakeDeckAnalysisPacketService |
| FakeChatGptDeckComparisonService | FakeDeckComparisonService |
| FakeChatGptCedhMetaGapService | FakeMetaGapService |
| ConfigurableChatGptCedhMetaGapService | ConfigurableMetaGapService |
| ThrowingChatGptCedhMetaGapService | ThrowingMetaGapService |
| ThrowingChatGptDeckPacketService | ThrowingDeckAnalysisPacketService |

All six STAY inline as `private sealed class` declarations — do NOT promote to separate fixture files (13-RESEARCH.md Anti-Pattern).
</inline_test_double_rename_map>

<allowlist_for_final_grep_gate>
Source: 13-RESEARCH.md L562-575

After Wave 4 completion, `grep -rEn "ChatGpt[A-Z]" --include="*.cs" DeckFlow.Web/ DeckFlow.Core/ DeckFlow.Web.Tests/ DeckFlow.Core.Tests/` may return hits ONLY at these allowlist sites:
- DeckFlow.Web/Models/DeckAnalysisRequest.cs — `_targetAiPlatform = "ChatGPT"` default and switch arms `case "ChatGPT":` (D-07 #1 + D-07 #5)
- DeckFlow.Web/Models/DeckComparisonRequest.cs — same pattern at L12/L60/L62/L71
- DeckFlow.Web/Models/MetaGapRequest.cs — same pattern at L8/L43/L45/L54
- DeckFlow.Web/Configuration/AiPlatformOptions.cs — doc-comment narrative "ChatGPT/Claude/Gemini selector" at L4 (D-07 #5)
- DeckFlow.Web/Services/PacketArtifactStore.cs — 3 `"chatgpt"` (lowercase) AI-segment fallbacks at L537/540/543 (D-07 #4) — these don't match `ChatGpt[A-Z]` anyway (lowercase c)
- DeckFlow.Web.Tests/HelpContentServiceTests.cs L44/L48/L51 — `"chatgpt-analysis"` fixture filename (per Pitfall 3) — also lowercase, doesn't match `ChatGpt[A-Z]`
- (NOT permitted post-Wave 2:) ChatGptResultWrapInstruction — renamed to ResultWrapInstruction per Open Question 1

The refined grep filter command for the final gate:
```bash
grep -rEn "ChatGpt[A-Z]" --include="*.cs" \
  DeckFlow.Web/ DeckFlow.Core/ DeckFlow.Web.Tests/ DeckFlow.Core.Tests/ \
  | grep -vE '"ChatGPT"' \
  | grep -vE 'ChatGPT/Claude/Gemini'
```
Expected: ZERO output lines.

Separate lowercase-chatgpt gate:
```bash
grep -rEn "[Cc]hatgpt" --include="*.cs" DeckFlow.Web/ DeckFlow.Web.Tests/ \
  | grep -vE 'chatgpt-analysis'
```
Expected: 3 hits — all 3 in `DeckFlow.Web/Services/PacketArtifactStore.cs` at L537/540/543 (the `"chatgpt"` AI-segment fallback). Any other hits indicate an over-eager rename.
</allowlist_for_final_grep_gate>

<preservation_list_wave4>
- HelpContentServiceTests.cs L44/L48/L51 `"chatgpt-analysis"` fixture filename — DO NOT TOUCH (Pitfall 3 — test creates a temp file with this literal name to verify slug-from-filename derivation).
- ResultContractTests.cs `[InlineData("ChatGPT", ...)]` — test inputs `"ChatGPT"`, `"Claude"`, `"Gemini"` STAY as literal string fixtures (these test the AI Key dispatch).
- All `[InternalsVisibleTo("DeckFlow.Web.Tests")]` in `DeckFlow.Web/AssemblyInfo.cs` is unchanged (assembly name, not a renamed type).
- Test method names that don't contain `ChatGpt[A-Z]` stay as-is (only test methods with the ChatGpt prefix in their identifier get renamed per Open Question 2).
- AI platform test input strings inside test method bodies (e.g., `[Theory] [InlineData("ChatGPT", "Claude", "Gemini")]`) — preserved byte-identical.
</preservation_list_wave4>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename the 9 ChatGpt-prefixed test files + sweep internal type refs + XML summaries</name>
  <files>
    DeckFlow.Web.Tests/ChatGptCedhMetaGapServiceTests.cs → DeckFlow.Web.Tests/MetaGapServiceTests.cs,
    DeckFlow.Web.Tests/ChatGptDeckComparisonServiceTests.cs → DeckFlow.Web.Tests/DeckComparisonServiceTests.cs,
    DeckFlow.Web.Tests/ChatGptDeckPacketServiceTests.cs → DeckFlow.Web.Tests/DeckAnalysisPacketServiceTests.cs,
    DeckFlow.Web.Tests/ChatGptJsonTextFormatterServiceTests.cs → DeckFlow.Web.Tests/JsonTextFormatterServiceTests.cs,
    DeckFlow.Web.Tests/ChatGptPacketArtifactStoreRoundTripTests.cs → DeckFlow.Web.Tests/PacketArtifactStoreRoundTripTests.cs,
    DeckFlow.Web.Tests/ChatGptPacketArtifactStoreTests.cs → DeckFlow.Web.Tests/PacketArtifactStoreTests.cs,
    DeckFlow.Web.Tests/ChatGptPhase10RoundTripTests.cs → DeckFlow.Web.Tests/AiPlatformPhase10RoundTripTests.cs,
    DeckFlow.Web.Tests/ChatGptResponseParsersTests.cs → DeckFlow.Web.Tests/ResponseParsersTests.cs,
    DeckFlow.Web.Tests/ChatGptResultContractTests.cs → DeckFlow.Web.Tests/ResultContractTests.cs
  </files>
  <read_first>
    DeckFlow.Web.Tests/ChatGptCedhMetaGapServiceTests.cs (current state — 17 hits per 13-RESEARCH.md L515; mostly `new ChatGptCedhMetaGapRequest { ... }` constructions),
    DeckFlow.Web.Tests/ChatGptDeckComparisonServiceTests.cs (13 hits),
    DeckFlow.Web.Tests/ChatGptDeckPacketServiceTests.cs (43 hits — large file; mostly `new ChatGptDeckRequest { ... }`),
    DeckFlow.Web.Tests/ChatGptJsonTextFormatterServiceTests.cs (16 hits — uses renamed const ResultWrapInstruction),
    DeckFlow.Web.Tests/ChatGptPacketArtifactStoreRoundTripTests.cs (3 hits — round-trip tests of the static PacketArtifactStore methods),
    DeckFlow.Web.Tests/ChatGptPacketArtifactStoreTests.cs (7 hits),
    DeckFlow.Web.Tests/ChatGptPhase10RoundTripTests.cs (121 hits — LARGEST file; round-trip tests for all 3 request models across ChatGPT/Claude/Gemini),
    DeckFlow.Web.Tests/ChatGptResponseParsersTests.cs (16 hits — calls into renamed ResponseParsers static class),
    DeckFlow.Web.Tests/ChatGptResultContractTests.cs (20 hits — `[Theory] [InlineData("ChatGPT", "Claude", "Gemini")]` strings preserved),
    DeckFlow.Web.Tests/DeckSyncServiceTests.cs (canonical analog for `public sealed class *Tests` xUnit pattern),
    DeckFlow.Web.Tests/CardLookupServiceTests.cs (canonical analog),
    DeckFlow.Web/Models/DeckAnalysisRequest.cs (Wave 1 — confirm new request type name),
    DeckFlow.Web/Models/DeckComparisonRequest.cs (Wave 1),
    DeckFlow.Web/Models/MetaGapRequest.cs (Wave 1),
    DeckFlow.Web/Services/DeckAnalysisPacketService.cs (Wave 2 — confirm new service interface),
    DeckFlow.Web/Services/DeckComparisonService.cs (Wave 2),
    DeckFlow.Web/Services/MetaGapService.cs (Wave 2),
    DeckFlow.Web/Services/PacketArtifactStore.cs (Wave 2),
    DeckFlow.Web/Services/ResponseParsers.cs (Wave 2),
    DeckFlow.Web/Services/JsonTextFormatterService.cs (Wave 2 — confirm const ResultWrapInstruction),
    .planning/phases/13-chatgpt-class-rename-summary-doc-comments/13-CONTEXT.md (D-03 doc-comment scope incl. test classes),
    .planning/phases/13-chatgpt-class-rename-summary-doc-comments/13-RESEARCH.md (Wave 4 file enumeration L514-524; Open Question 2 test-method-name lockstep L739-742; Pitfall 3 HelpContentServiceTests preserved L317-325),
    .planning/phases/13-chatgpt-class-rename-summary-doc-comments/13-PATTERNS.md (Pattern: test-double sealed-class naming)
  </read_first>
  <action>
    Nine commits — one per test file. Each: `git mv` to rename file, then in-file edits to update test class name, test method names that contain `ChatGpt*`, and all internal type references to Wave-1/Wave-2 renamed names. Backfill class-level `<summary>` doc comment on each renamed test class for consistency with D-03 (terse one-sentence anchored to what the test fixture covers).

    For each file:
    1. `git mv {old path} {new path}` per the test_file_rename_map.
    2. Rename the test class declaration `public sealed class ChatGpt*Tests` to `public sealed class *Tests` per the map.
    3. Inside method bodies, replace every ChatGpt-prefixed type identifier with its Wave-1/Wave-2 renamed counterpart. This includes: request constructions (`new ChatGptDeckRequest`), response refs (`ChatGptDeckAnalysisResponse`, `ChatGptCedhMetaGapResponse` plus all nested shapes), service interface/class refs (`IChatGptDeckPacketService`, `ChatGptDeckPacketService`), result record refs, static helper class calls (`ChatGptPacketArtifactStore.X` to `PacketArtifactStore.X`, etc.), DeckPageTab enum value refs.
    4. Rename test methods that have `ChatGpt*` in their name per Open Question 2. Examples: `ChatGptCedhMetaGap_Get_ReturnsExpectedViewModel` to `CedhMetaGap_Get_ReturnsExpectedViewModel`; `RoundTrip_ChatGptDeckRequest_PreservesAllFields` to `RoundTrip_DeckAnalysisRequest_PreservesAllFields` — match the renamed Wave-1 type names.
    5. PRESERVE: `[InlineData("ChatGPT", "Claude", "Gemini")]` test inputs (these are the AI key string literals — D-07 #1); `chatgpt-analysis` fixture strings IF the file is HelpContentServiceTests.cs (not in this wave's rename list, but if any of the 9 renamed files contains a `"chatgpt-..."` string fixture, leave it byte-identical per allowlist).
    6. Add a class-level `<summary>` doc comment on each renamed test class — terse, one sentence, anchored to what the fixture verifies. Examples: "Verifies that the DeckAnalysisPacketService correctly hydrates decks via Scryfall and produces the expected prompt-packet artifacts."; "Round-trip tests for the deck-analysis, deck-comparison, and cEDH meta-gap request DTOs across all three AI platforms (ChatGPT, Claude, Gemini)."

    Special handling per file:
    - DeckAnalysisPacketServiceTests.cs (was ChatGptDeckPacketServiceTests.cs — 43 hits): largest service-test file. Read in chunks if needed.
    - AiPlatformPhase10RoundTripTests.cs (was ChatGptPhase10RoundTripTests.cs — 121 hits): file rename is "ChatGpt" → "AiPlatform" (renames the FILE concept to reflect its actual subject — round-trip for ALL 3 AIs, not just ChatGPT). Inside, update class name `ChatGptPhase10RoundTripTests` → `AiPlatformPhase10RoundTripTests`. Per Open Question 2, also rename test method names containing `ChatGpt` (e.g., method names referencing ChatGptDeckRequest may have `_ChatGpt_` in their identifier — rename to use the new request type names). All `[InlineData("ChatGPT", "Claude", "Gemini")]` test inputs STAY as literal strings (these ARE the AI keys being round-tripped — D-07 #1).
    - ResultContractTests.cs (was ChatGptResultContractTests.cs — 20 hits): test class name `ChatGptResultContractTests` → `ResultContractTests`. `[Theory] [InlineData("ChatGPT", "Claude", "Gemini")]` test inputs PRESERVED. Method names with `ChatGpt` prefix renamed per Open Question 2.
    - JsonTextFormatterServiceTests.cs (was ChatGptJsonTextFormatterServiceTests.cs — 16 hits): references to renamed `JsonTextFormatterService` static class; if any test references the renamed const `ResultWrapInstruction` (Wave 2), use the new name.
    - PacketArtifactStoreTests.cs + PacketArtifactStoreRoundTripTests.cs: references to renamed static class `PacketArtifactStore` and its methods.

    One commit per file. Commit message pattern: `refactor(13): rename {OldTestClass} test file to {NewTestClass} with XML summary`. Plain author, no Co-Authored-By trailer.
  </action>
  <verify>
    <automated>test ! -f DeckFlow.Web.Tests/ChatGptCedhMetaGapServiceTests.cs &amp;&amp; test ! -f DeckFlow.Web.Tests/ChatGptDeckComparisonServiceTests.cs &amp;&amp; test ! -f DeckFlow.Web.Tests/ChatGptDeckPacketServiceTests.cs &amp;&amp; test ! -f DeckFlow.Web.Tests/ChatGptJsonTextFormatterServiceTests.cs &amp;&amp; test ! -f DeckFlow.Web.Tests/ChatGptPacketArtifactStoreRoundTripTests.cs &amp;&amp; test ! -f DeckFlow.Web.Tests/ChatGptPacketArtifactStoreTests.cs &amp;&amp; test ! -f DeckFlow.Web.Tests/ChatGptPhase10RoundTripTests.cs &amp;&amp; test ! -f DeckFlow.Web.Tests/ChatGptResponseParsersTests.cs &amp;&amp; test ! -f DeckFlow.Web.Tests/ChatGptResultContractTests.cs &amp;&amp; test -f DeckFlow.Web.Tests/MetaGapServiceTests.cs &amp;&amp; test -f DeckFlow.Web.Tests/DeckComparisonServiceTests.cs &amp;&amp; test -f DeckFlow.Web.Tests/DeckAnalysisPacketServiceTests.cs &amp;&amp; test -f DeckFlow.Web.Tests/JsonTextFormatterServiceTests.cs &amp;&amp; test -f DeckFlow.Web.Tests/PacketArtifactStoreRoundTripTests.cs &amp;&amp; test -f DeckFlow.Web.Tests/PacketArtifactStoreTests.cs &amp;&amp; test -f DeckFlow.Web.Tests/AiPlatformPhase10RoundTripTests.cs &amp;&amp; test -f DeckFlow.Web.Tests/ResponseParsersTests.cs &amp;&amp; test -f DeckFlow.Web.Tests/ResultContractTests.cs</automated>
  </verify>
  <acceptance_criteria>
    - All 9 old test file paths gone; all 9 new test file paths exist.
    - `grep -c "public sealed class DeckAnalysisPacketServiceTests" DeckFlow.Web.Tests/DeckAnalysisPacketServiceTests.cs` returns 1.
    - `grep -c "public sealed class DeckComparisonServiceTests" DeckFlow.Web.Tests/DeckComparisonServiceTests.cs` returns 1.
    - `grep -c "public sealed class MetaGapServiceTests" DeckFlow.Web.Tests/MetaGapServiceTests.cs` returns 1.
    - `grep -c "public sealed class AiPlatformPhase10RoundTripTests" DeckFlow.Web.Tests/AiPlatformPhase10RoundTripTests.cs` returns 1.
    - `grep -c "public sealed class JsonTextFormatterServiceTests" DeckFlow.Web.Tests/JsonTextFormatterServiceTests.cs` returns 1.
    - `grep -c "public sealed class PacketArtifactStoreTests" DeckFlow.Web.Tests/PacketArtifactStoreTests.cs` returns 1.
    - `grep -c "public sealed class PacketArtifactStoreRoundTripTests" DeckFlow.Web.Tests/PacketArtifactStoreRoundTripTests.cs` returns 1.
    - `grep -c "public sealed class ResponseParsersTests" DeckFlow.Web.Tests/ResponseParsersTests.cs` returns 1.
    - `grep -c "public sealed class ResultContractTests" DeckFlow.Web.Tests/ResultContractTests.cs` returns 1.
    - `grep -rEc "ChatGpt[A-Z]" DeckFlow.Web.Tests/DeckAnalysisPacketServiceTests.cs DeckFlow.Web.Tests/DeckComparisonServiceTests.cs DeckFlow.Web.Tests/MetaGapServiceTests.cs DeckFlow.Web.Tests/JsonTextFormatterServiceTests.cs DeckFlow.Web.Tests/PacketArtifactStoreTests.cs DeckFlow.Web.Tests/PacketArtifactStoreRoundTripTests.cs DeckFlow.Web.Tests/AiPlatformPhase10RoundTripTests.cs DeckFlow.Web.Tests/ResponseParsersTests.cs DeckFlow.Web.Tests/ResultContractTests.cs | grep -v ":0$" | wc -l` returns 0.
    - `grep -c '"ChatGPT"' DeckFlow.Web.Tests/ResultContractTests.cs DeckFlow.Web.Tests/AiPlatformPhase10RoundTripTests.cs` returns at least 2 (preserved test inputs — D-07 #1).
    - `grep -c '"chatgpt-analysis"' DeckFlow.Web.Tests/HelpContentServiceTests.cs` returns at least 3 (untouched per Pitfall 3 — file not in this wave's rename list).
    - `grep -cE "^\s*///\s*<summary>" DeckFlow.Web.Tests/DeckAnalysisPacketServiceTests.cs` returns at least 1 (class-level summary added).
    - `git log --oneline -9` shows 9 commits matching `refactor(13): rename ChatGpt.* test file to .* with XML summary`, plain-author (no Co-Authored-By).
  </acceptance_criteria>
  <done>9 test files renamed via git mv, internal type refs updated to Wave-1/Wave-2 names, test method names with ChatGpt prefix renamed per Open Question 2, `[InlineData("ChatGPT", "Claude", "Gemini")]` test inputs preserved, HelpContentServiceTests.cs `"chatgpt-analysis"` fixtures NOT touched (file not in rename list), each renamed test class carries a one-sentence `<summary>`, 9 separate plain-author commits.</done>
</task>

<task type="auto">
  <name>Task 2: Edit DeckControllerTests.cs + TestServiceFactory.cs (final test-project sweep)</name>
  <files>
    DeckFlow.Web.Tests/DeckControllerTests.cs,
    DeckFlow.Web.Tests/TestDoubles/TestServiceFactory.cs
  </files>
  <read_first>
    DeckFlow.Web.Tests/DeckControllerTests.cs (current state — 126 ChatGpt hits per 13-RESEARCH.md L529; verify exact line numbers of 6 inline private sealed class test doubles around L775/781/810/831/844/857; verify `Equal("ChatGptCedhMetaGap", view.ViewName)` assertions [A3 — may already be `"CedhMetaGap"` post-Phase-12; verify current state and adjust]; verify 39 controller-ctor sites where Fake*/Throwing*/Configurable* doubles are passed; verify 4 test method names like `ChatGptCedhMetaGap_Get_ReturnsExpectedViewModel`),
    DeckFlow.Web.Tests/TestDoubles/TestServiceFactory.cs (current state — 5 ChatGpt hits per 13-RESEARCH.md L530: factory methods CreateChatGptDeckPacketService at L99, CreateChatGptDeckComparisonService at L129, CreateChatGptCedhMetaGapService at L151; plus 2 `ILogger<ChatGptX>` generic-arg refs),
    DeckFlow.Web/Controllers/DeckController.cs (Wave 3 — verify renamed action methods and ctor parameter types that the test fixtures will call),
    DeckFlow.Web/Services/DeckAnalysisPacketService.cs (Wave 2),
    DeckFlow.Web/Services/DeckComparisonService.cs (Wave 2),
    DeckFlow.Web/Services/MetaGapService.cs (Wave 2),
    .planning/phases/13-chatgpt-class-rename-summary-doc-comments/13-CONTEXT.md (D-01 Test-double naming preservation rules — Fake*/Throwing*/Configurable* prefixes kept),
    .planning/phases/13-chatgpt-class-rename-summary-doc-comments/13-RESEARCH.md (Wave 4 DeckControllerTests/TestServiceFactory enumeration L527-531; Open Question 2 test method names L739-742; Open Question 3 inline test doubles confirmed `private` L744-747; Assumption A3 view-name assertion may already be `"CedhMetaGap"` L727),
    .planning/phases/13-chatgpt-class-rename-summary-doc-comments/13-PATTERNS.md (Test-double naming section — Fake*/Throwing*/Configurable* preservation rule)
  </read_first>
  <action>
    Two commits, one per file.

    Edit 1 — DeckControllerTests.cs:
    1. Rename the 6 inline private sealed class test doubles per the inline_test_double_rename_map above (FakeChatGptDeckPacketService to FakeDeckAnalysisPacketService, etc.). The classes STAY inline as `private sealed class` declarations (NOT promoted to separate files). At each declaration site (~L775/781/810/831/844/857), update:
       - The class name itself
       - The interface it implements (e.g., `: IChatGptDeckPacketService` → `: IDeckAnalysisPacketService`)
       - The method signatures inside that implement renamed Wave-1/Wave-2 model + request types (e.g., parameters typed as `ChatGptDeckRequest` → `DeckAnalysisRequest`)
       - Any internal field types
    2. At every controller-ctor site where these doubles are instantiated (e.g., `new DeckController(... new FakeChatGptDeckPacketService() ...)`), rename the `new FakeChatGptX()` call to the new test-double name.
    3. Update any test method names that contain `ChatGpt[A-Z]` in their identifier per Open Question 2 — e.g., `ChatGptCedhMetaGap_Get_ReturnsExpectedViewModel` → `CedhMetaGap_Get_ReturnsExpectedViewModel`; `ChatGptPackets_Post_BadModelState_ReturnsView` → `DeckAnalysis_Post_BadModelState_ReturnsView`. The `[Fact]` / `[Theory]` attributes are unchanged.
    4. Update view-name string assertions: per Assumption A3, the assertions at the top of the file (~L39 region for `view.ViewName`) may currently assert `"ChatGptCedhMetaGap"` OR `"CedhMetaGap"`. READ the current state first. If they assert old view-name strings like `"ChatGptCedhMetaGap"`/`"ChatGptPackets"`/`"ChatGptDeckComparison"`, update to the new names matching Phase 12 view filenames AND Wave 3 action method names (`"CedhMetaGap"`, `"DeckAnalysis"`, `"DeckComparison"`). If they already match Phase 12 view names (`"CedhMetaGap"`, `"DeckAnalysis"`, `"DeckComparison"` — set by Phase 12's view rename), preserve byte-identical.
    5. Replace every other ChatGpt-prefixed type identifier in method bodies (request constructions, response refs, service result refs, DeckPageTab enum value refs, static helper calls) with the renamed Wave-1/Wave-2 names per the full naming map in 13-CONTEXT.md.
    Commit: `refactor(13): sweep DeckControllerTests.cs — rename 6 inline test doubles, test methods, type refs, view-name assertions`.

    Edit 2 — TestServiceFactory.cs:
    Per 13-RESEARCH.md L530:
    - L99: `public static IChatGptDeckPacketService CreateChatGptDeckPacketService(...)` → `public static IDeckAnalysisPacketService CreateDeckAnalysisPacketService(...)`. Inside the method body, rename the `new ChatGptDeckPacketService(...)` to `new DeckAnalysisPacketService(...)` and the `ILogger<ChatGptDeckPacketService>` generic arg if present to `ILogger<DeckAnalysisPacketService>`.
    - L129: `public static IChatGptDeckComparisonService CreateChatGptDeckComparisonService(...)` → `public static IDeckComparisonService CreateDeckComparisonService(...)`. Same pattern for the body.
    - L151: `public static IChatGptCedhMetaGapService CreateChatGptCedhMetaGapService(...)` → `public static IMetaGapService CreateMetaGapService(...)`. Same pattern.
    - Update any internal field types or local variable declarations of the renamed service types.
    - Preserve all other helper-method bodies byte-identical (the factory uses real services like `MoxfieldParser`, `ArchidektParser`, etc. — those are NOT renamed in this phase).
    Commit: `refactor(13): update TestServiceFactory.cs factory methods + ILogger generic args for renamed services`.
  </action>
  <verify>
    <automated>! grep -qE "ChatGpt[A-Z]" DeckFlow.Web.Tests/DeckControllerTests.cs &amp;&amp; ! grep -qE "ChatGpt[A-Z]" DeckFlow.Web.Tests/TestDoubles/TestServiceFactory.cs &amp;&amp; grep -q "private sealed class FakeDeckAnalysisPacketService" DeckFlow.Web.Tests/DeckControllerTests.cs &amp;&amp; grep -q "private sealed class FakeDeckComparisonService" DeckFlow.Web.Tests/DeckControllerTests.cs &amp;&amp; grep -q "private sealed class FakeMetaGapService" DeckFlow.Web.Tests/DeckControllerTests.cs &amp;&amp; grep -q "private sealed class ConfigurableMetaGapService" DeckFlow.Web.Tests/DeckControllerTests.cs &amp;&amp; grep -q "private sealed class ThrowingMetaGapService" DeckFlow.Web.Tests/DeckControllerTests.cs &amp;&amp; grep -q "private sealed class ThrowingDeckAnalysisPacketService" DeckFlow.Web.Tests/DeckControllerTests.cs &amp;&amp; grep -q "CreateDeckAnalysisPacketService" DeckFlow.Web.Tests/TestDoubles/TestServiceFactory.cs &amp;&amp; grep -q "CreateDeckComparisonService" DeckFlow.Web.Tests/TestDoubles/TestServiceFactory.cs &amp;&amp; grep -q "CreateMetaGapService" DeckFlow.Web.Tests/TestDoubles/TestServiceFactory.cs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE "ChatGpt[A-Z]" DeckFlow.Web.Tests/DeckControllerTests.cs` returns 0.
    - `grep -cE "ChatGpt[A-Z]" DeckFlow.Web.Tests/TestDoubles/TestServiceFactory.cs` returns 0.
    - `grep -c "private sealed class FakeDeckAnalysisPacketService" DeckFlow.Web.Tests/DeckControllerTests.cs` returns 1.
    - `grep -c "private sealed class FakeDeckComparisonService" DeckFlow.Web.Tests/DeckControllerTests.cs` returns 1.
    - `grep -c "private sealed class FakeMetaGapService" DeckFlow.Web.Tests/DeckControllerTests.cs` returns 1.
    - `grep -c "private sealed class ConfigurableMetaGapService" DeckFlow.Web.Tests/DeckControllerTests.cs` returns 1.
    - `grep -c "private sealed class ThrowingMetaGapService" DeckFlow.Web.Tests/DeckControllerTests.cs` returns 1.
    - `grep -c "private sealed class ThrowingDeckAnalysisPacketService" DeckFlow.Web.Tests/DeckControllerTests.cs` returns 1.
    - `grep -c "CreateDeckAnalysisPacketService" DeckFlow.Web.Tests/TestDoubles/TestServiceFactory.cs` returns 1.
    - `grep -c "CreateDeckComparisonService" DeckFlow.Web.Tests/TestDoubles/TestServiceFactory.cs` returns 1.
    - `grep -c "CreateMetaGapService" DeckFlow.Web.Tests/TestDoubles/TestServiceFactory.cs` returns 1.
    - `grep -cE "ILogger<(DeckAnalysisPacketService|DeckComparisonService|MetaGapService)>" DeckFlow.Web.Tests/TestDoubles/TestServiceFactory.cs` returns at least 2.
    - View-name assertions in DeckControllerTests.cs (~L39 region) match either Phase 12 names (`"CedhMetaGap"`, `"DeckAnalysis"`, `"DeckComparison"`) or have been updated to match — `grep -cE 'Equal\\("(CedhMetaGap|DeckAnalysis|DeckComparison)"' DeckFlow.Web.Tests/DeckControllerTests.cs` returns at least 3 (one per page).
    - `git log --oneline -2` shows 2 commits: `refactor(13): sweep DeckControllerTests.cs ...` and `refactor(13): update TestServiceFactory.cs ...`, plain-author.
  </acceptance_criteria>
  <done>DeckControllerTests.cs swept clean of ChatGpt-prefix identifiers (126 hits resolved), 6 inline private sealed test doubles renamed (Fake*/Throwing*/Configurable* prefixes preserved, classes stay inline), test method names with ChatGpt prefix renamed, view-name assertions match Phase 12 names. TestServiceFactory.cs has 3 factory methods + 2 ILogger generic args renamed. 2 plain-author commits.</done>
</task>

<task type="auto">
  <name>Task 3: Final build gate + grep verification + CLI smoke + emit 13-HUMAN-UAT.md</name>
  <files>
    .planning/phases/13-chatgpt-class-rename-summary-doc-comments/13-HUMAN-UAT.md
  </files>
  <read_first>
    .planning/milestones/v1.2-MILESTONE-AUDIT.md (T1-T8 manual integration suite source — emit a Phase-13-specific HUMAN-UAT.md that references the same T1-T8 tests, adapted for the renamed pages),
    .planning/phases/13-chatgpt-class-rename-summary-doc-comments/13-CONTEXT.md (D-09 verification gate; D-10 manual T1-T8 is HUMAN-UAT per CLAUDE.md VSTest WSL constraint),
    .planning/phases/13-chatgpt-class-rename-summary-doc-comments/13-RESEARCH.md (Allowlist L562-575 + L580-585 refined grep gate; DeckFlow.CLI 5-command smoke per D-09 SC3 L80),
    CLAUDE.md (Testing constraint: VSTest unreliable in WSL → `dotnet build` clean is the gate, manual T1-T8 is HUMAN-UAT)
  </read_first>
  <action>
    Three steps: build gate, grep gate, then emit HUMAN-UAT.md.

    Step A — Run the final solution build-clean gate:
    ```
    dotnet build DeckFlow.sln --configuration Release 2>&1 | tee /tmp/build-13.log
    ```
    Confirm output contains `Build succeeded.` AND the warning/error counts are zero or zero NEW vs baseline. Per CLAUDE.md "VSTest unreliable in WSL" — DO NOT run `dotnet test`; build-clean is the gate.

    If build is RED, the executor MUST diagnose and fix in a separate commit per logical change:
    - Read the error message
    - Identify the file + line
    - Verify against the naming map in 13-CONTEXT.md D-01
    - Edit the file
    - Re-run build
    - Commit: `fix(13): {error description} ({file})`
    Iterate until clean.

    Step B — Run the final solution-wide grep verification gate with allowlist:
    ```
    grep -rEn "ChatGpt[A-Z]" --include="*.cs" \
      DeckFlow.Web/ DeckFlow.Core/ DeckFlow.Web.Tests/ DeckFlow.Core.Tests/ \
      | grep -vE '"ChatGPT"' \
      | grep -vE 'ChatGPT/Claude/Gemini' \
      | tee /tmp/grep-gate-13.log
    ```
    Expected: ZERO output lines. If any hits, fix per the same iteration pattern as Step A.

    Also run the lowercase-chatgpt audit:
    ```
    grep -rEn "[Cc]hatgpt" --include="*.cs" DeckFlow.Web/ DeckFlow.Web.Tests/ \
      | grep -vE 'chatgpt-analysis' \
      | tee /tmp/lowercase-gate-13.log
    ```
    Expected: 3 hits — all 3 in `DeckFlow.Web/Services/PacketArtifactStore.cs` at L537/540/543 (the `"chatgpt"` AI-segment fallback per D-07 #4). Any other hits indicate an over-eager rename.

    Step C — DeckFlow.CLI smoke check:
    ```
    dotnet build DeckFlow.CLI/DeckFlow.CLI.csproj --configuration Release 2>&1 | grep -E "Error|error CS"
    ```
    Expected: empty output (no errors). DeckFlow.CLI uses DeckFlow.Core only, so it should be unaffected by Wave 1-4 renames — but confirm the build doesn't fail.

    Step D — Emit HUMAN-UAT.md at `.planning/phases/13-chatgpt-class-rename-summary-doc-comments/13-HUMAN-UAT.md` using this exact content:

    ```markdown
    # Phase 13: ChatGpt* Class Rename — Human UAT Checklist

    **Per CLAUDE.md "VSTest unreliable in WSL":** automated `dotnet test` is NOT part of the verifier gate. Build-clean is the gate; behavioral parity is verified by the manual T1-T8 round-trip below (per D-09 SC4 + D-10).

    **Per CLAUDE.md memory:** the user starts the dev server. Do NOT auto-launch DeckFlow web — ask the user to start it manually.

    ## Setup
    - [ ] User starts `dotnet run --project DeckFlow.Web` locally (or `scripts/run-web.ps1` / `scripts/run-web.sh`)
    - [ ] User confirms dev server is listening on http://localhost:5173

    ## T1: Deck-Analysis page renders + form submits
    Source: `.planning/milestones/v1.2-MILESTONE-AUDIT.md` T1
    - [ ] Visit http://localhost:5173/deck-analysis — page renders with AI-agnostic H1 + page-lede (Phase 12 layout intact)
    - [ ] Paste a small valid deck export (10-30 cards + a commander) → click "Generate analysis prompt" → page advances to Step 2 with no errors
    - [ ] Step 5 follow-up form submits, page returns to Step-5 view with no errors

    ## T2: Deck-Analysis zip download produces valid artifact
    - [ ] On the deck-analysis page after Step 5, click "Download session zip"
    - [ ] Open the downloaded `.zip` (e.g., `deck-analysis-<commander>-<timestamp>-chatgpt.zip`)
    - [ ] Confirm `01-request-context.txt`, `40-deck-analysis-response.json` (or equivalent), prompt files, and `00-readme.md` (if present) are inside
    - [ ] Confirm `target_ai_platform: ChatGPT` line is present in `01-request-context.txt` (D-07 #1 preservation)

    ## T3: Deck-Analysis zip upload round-trips byte-identical
    - [ ] On the deck-analysis page, click "Upload session zip" and select the file from T2
    - [ ] Page restores to Step 5 state with all form fields and prompt artifacts re-hydrated
    - [ ] Re-download the zip and `diff` (or compare sizes / inner JSON byte-identical with the T2 download)

    ## T4: Deck-Comparison page renders + form submits + zip round-trip
    - [ ] Repeat T1-T3 against http://localhost:5173/deck-comparison
    - [ ] Confirm zip filename is `deck-comparison-*.zip` (Phase 12 filename)
    - [ ] Confirm `40-deck-comparison-response.json` (or equivalent) inside zip

    ## T5: cEDH Meta-Gap page renders + form submits + zip round-trip
    - [ ] Repeat T1-T3 against http://localhost:5173/cedh-meta-gap
    - [ ] Confirm zip filename uses the cedh-meta-gap term (Phase 12)
    - [ ] Confirm `40-meta-gap-response.json` (or equivalent) inside zip

    ## T6: AI selector dispatches across ChatGPT/Claude/Gemini
    - [ ] On any of the 3 pages, change the AI radio to "Claude" → re-generate prompt → confirm output mentions Claude-specific instructions
    - [ ] Repeat with "Gemini" if `DECKFLOW_GEMINI_ENABLED` is set
    - [ ] Confirm `_AiSelector.cshtml` still renders `id="ai-chatgpt"`, `value="ChatGPT"` (Phase 13 preservation — Pitfall 4)

    ## T7: Permanent 301 redirects from legacy ChatGPT URLs (Phase 12 invariant)
    - [ ] Visit http://localhost:5173/chatgpt-packets → confirm 301 → /deck-analysis
    - [ ] Visit http://localhost:5173/chatgpt-deck-comparison → confirm 301 → /deck-comparison
    - [ ] Visit http://localhost:5173/chatgpt-cedh-meta-gap → confirm 301 → /cedh-meta-gap

    ## T8: Pre-Phase-13 saved zips load successfully
    - [ ] If the user has any zips saved BEFORE Phase 13 work (with the old `target_ai_platform` field name conventions but Phase-12 filenames), upload one of each (deck-analysis / deck-comparison / cedh-meta-gap)
    - [ ] Confirm all three load and restore state without errors (PacketArtifactStore deserialization unchanged — D-07 #4 preserved the AI-segment fallback)

    ## Sign-off
    - [ ] All T1-T8 pass — zero user-visible behavior change verified
    - [ ] User types "approved" or pastes any specific failure description for the planner to triage as a gap

    **Resume signal:** When user types "approved", proceed to plan 13-04 SUMMARY emission. If user reports any failure, surface it as a gap closure candidate (`/gsd:plan-phase 13 --gaps`).
    ```

    Commit: `docs(13): emit HUMAN-UAT.md with T1-T8 manual integration suite for Phase 13 sign-off`.
  </action>
  <verify>
    <automated>dotnet build DeckFlow.sln --configuration Release 2>&amp;1 | grep -qE "Build succeeded\.|0 Error\(s\)" &amp;&amp; [ "$(grep -rEn 'ChatGpt[A-Z]' --include='*.cs' DeckFlow.Web/ DeckFlow.Core/ DeckFlow.Web.Tests/ DeckFlow.Core.Tests/ 2>/dev/null | grep -vE '"ChatGPT"' | grep -vE 'ChatGPT/Claude/Gemini' | wc -l)" = "0" ] &amp;&amp; test -f .planning/phases/13-chatgpt-class-rename-summary-doc-comments/13-HUMAN-UAT.md &amp;&amp; grep -q "## T1" .planning/phases/13-chatgpt-class-rename-summary-doc-comments/13-HUMAN-UAT.md &amp;&amp; grep -q "## T8" .planning/phases/13-chatgpt-class-rename-summary-doc-comments/13-HUMAN-UAT.md</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet build DeckFlow.sln --configuration Release` exits with code 0 and output contains `Build succeeded` and `0 Error(s)`.
    - Solution build warnings count is 0 NEW vs baseline (NoWarn 1591/1573/1587 still suppresses missing-summary on UNTOUCHED types per D-04; renamed types do not need NoWarn since they got summaries in Waves 1/2/4).
    - Final grep gate: `grep -rEn "ChatGpt[A-Z]" --include="*.cs" DeckFlow.Web/ DeckFlow.Core/ DeckFlow.Web.Tests/ DeckFlow.Core.Tests/ | grep -vE '"ChatGPT"' | grep -vE 'ChatGPT/Claude/Gemini'` returns 0 lines.
    - Lowercase-chatgpt gate: `grep -rEn "[Cc]hatgpt" --include="*.cs" DeckFlow.Web/ DeckFlow.Web.Tests/ | grep -vE 'chatgpt-analysis'` returns 3 lines (all in PacketArtifactStore.cs at L537/540/543).
    - `dotnet build DeckFlow.CLI/DeckFlow.CLI.csproj --configuration Release 2>&1 | grep -cE "error CS"` returns 0.
    - `.planning/phases/13-chatgpt-class-rename-summary-doc-comments/13-HUMAN-UAT.md` exists.
    - `grep -cE "^## T[1-8]" .planning/phases/13-chatgpt-class-rename-summary-doc-comments/13-HUMAN-UAT.md` returns 8 (T1 through T8 sections present).
    - `git log --oneline -1` shows `docs(13): emit HUMAN-UAT.md with T1-T8 manual integration suite for Phase 13 sign-off`, plain-author.
  </acceptance_criteria>
  <done>Final solution build returns 0 errors / 0 new warnings, final allowlisted grep gate returns ZERO hits, lowercase-chatgpt audit returns exactly 3 hits (all in PacketArtifactStore.cs preservation sites), DeckFlow.CLI builds clean, HUMAN-UAT.md emitted with T1-T8 checklist, 1 plain-author commit for the doc emission (any build-fix commits in this task also plain-author).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Human UAT — T1-T8 manual integration suite</name>
  <what-built>
    Phase 13 complete: 26 ChatGpt-prefixed C# types renamed to AI-agnostic names across 4 waves (Models, Services, Controller+Views, Tests). 736 ChatGpt-prefix sites resolved across 32 files. XML `<summary>` doc-comments backfilled on every renamed type per CLASSRENAME-02. DI registrations updated. README updated. Solution build is clean. Final allowlisted grep gate returns zero hits. CLI smoke clean.

    All preservation invariants honored per D-07:
    - `"ChatGPT"` AI Key string literals preserved in request DTOs (4 sites)
    - `"chatgpt"` lowercase fallbacks in PacketArtifactStore.cs preserved (3 sites)
    - AiPlatformOptions.cs doc-comment narrative preserved
    - HelpContentServiceTests.cs `"chatgpt-analysis"` fixture preserved (3 sites)
    - `_AiSelector.cshtml` fully UNTOUCHED (5 ChatGPT/chatgpt literals at L13-L26 preserved per Pitfall 4)
    - All chatgpt-prefixed CSS classes + data-attributes in 3 main views preserved (D-08 deferred to Phase 16)
    - All form-field `name="..."` attributes preserved (property names not renamed in Phase 13)
    - All `[JsonPropertyName("snake_case")]` attrs preserved byte-identical
    - DeckPageTab enum integer values 5/8/9 preserved byte-stable
    - Phase 12 invariants preserved: `View("DeckAnalysis|DeckComparison|CedhMetaGap", ...)` literal strings, route attribute strings, 301-redirect block in Program.cs

    Per CLAUDE.md feedback memory: the user starts the dev server themselves.
  </what-built>
  <how-to-verify>
    1. Start the DeckFlow dev server locally: `dotnet run --project DeckFlow.Web` (or `scripts/run-web.ps1` on Windows / `scripts/run-web.sh` on WSL/Linux).
    2. Confirm the server is listening on http://localhost:5173.
    3. Open `.planning/phases/13-chatgpt-class-rename-summary-doc-comments/13-HUMAN-UAT.md` and walk through T1 through T8 sequentially.
    4. For each Tx section, check off each checkbox after performing the action and confirming the expected outcome.
    5. If any test fails, capture the failure mode (HTTP status, console error, missing field, mismatched zip content) and paste it back to the planner — it will be triaged via `/gsd:plan-phase 13 --gaps` as a gap closure plan.
    6. Pay special attention to:
       - T7 (301 redirects from /chatgpt-packets, etc.) — verifies Phase 12 redirects still work
       - T8 (pre-Phase-13 saved zips load) — verifies the PacketArtifactStore round-trip is intact
       - T6 (AI selector dispatch) — verifies _AiSelector.cshtml is still functional with literal "ChatGPT"/"Claude"/"Gemini" radios

    Expected total UAT time: 15-25 minutes for a thorough run.
  </how-to-verify>
  <resume-signal>
    Type "approved" when all T1-T8 pass.
    Type a specific failure description (e.g., "T2 download fails with 500", "T8 upload restores Step 1 instead of Step 5") if anything broke. The planner will replan via `/gsd:plan-phase 13 --gaps` if needed.
  </resume-signal>
  <files>(none — verification-only checkpoint that the user performs manually against the running dev server)</files>
  <action>Wait for the user to start the dev server, walk through T1-T8 in 13-HUMAN-UAT.md, and type "approved" (all passed) or paste a specific failure description (triage as gap closure candidate). DO NOT auto-launch the dev server — per CLAUDE.md feedback memory, the user starts it themselves.</action>
  <verify>
    <human-check>User has typed "approved" in response to the checkpoint, indicating all 8 T1-T8 manual tests passed and zero user-visible behavior change is confirmed.</human-check>
  </verify>
  <done>User typed "approved". Phase 13 manual integration suite complete. Behavioral parity confirmed. Plan 13-04 SUMMARY may now be emitted.</done>
</task>

</tasks>

<verification>
**Wave 4 (final phase) verification (run AT END of plan, before declaring complete):**

```bash
# 1. All 9 old test files gone; 9 new test files exist
ls DeckFlow.Web.Tests/ChatGpt*.cs 2>&1
# Expected: "No such file"

# 2. Final grep gate (allowlisted)
grep -rEn "ChatGpt[A-Z]" --include="*.cs" \
  DeckFlow.Web/ DeckFlow.Core/ DeckFlow.Web.Tests/ DeckFlow.Core.Tests/ \
  | grep -vE '"ChatGPT"' \
  | grep -vE 'ChatGPT/Claude/Gemini'
# Expected: ZERO output

# 3. Lowercase-chatgpt audit
grep -rEn "[Cc]hatgpt" --include="*.cs" DeckFlow.Web/ DeckFlow.Web.Tests/ \
  | grep -vE 'chatgpt-analysis'
# Expected: 3 lines (all PacketArtifactStore.cs at L537/540/543)

# 4. Solution build-clean gate
dotnet build DeckFlow.sln --configuration Release
# Expected: Build succeeded. 0 Warning(s) [or 0-new vs baseline]. 0 Error(s).

# 5. DeckFlow.CLI smoke
dotnet build DeckFlow.CLI/DeckFlow.CLI.csproj --configuration Release
# Expected: 0 errors

# 6. HUMAN-UAT.md emitted
test -f .planning/phases/13-chatgpt-class-rename-summary-doc-comments/13-HUMAN-UAT.md && grep -cE "^## T[1-8]" .planning/phases/13-chatgpt-class-rename-summary-doc-comments/13-HUMAN-UAT.md
# Expected: 8

# 7. _AiSelector.cshtml UNTOUCHED (5 expected ChatGPT/chatgpt literals)
grep -cE "ChatGPT|ai-chatgpt|chatgpt" DeckFlow.Web/Views/Shared/_AiSelector.cshtml
# Expected: >= 5

# 8. _AiSelector.cshtml diff vs HEAD~N should be empty
git diff main -- DeckFlow.Web/Views/Shared/_AiSelector.cshtml
# Expected: empty (no changes across Phase 13)

# 9. Phase 13 commit count + plain-author verification
git log --oneline main..HEAD | grep -cE '^[a-f0-9]+ (refactor|docs|fix)\(13\):'
# Expected: 22-28 (11 + 9 + 3 + 9 + 2 + 1 = 35 max, fewer if some were combined). Plain author only.
git log --format='%ae' main..HEAD | sort -u
# Expected: only the user's default git email (NO noreply@anthropic.com / Co-Authored-By trailer)
```

After all 9 automated checks pass, hand control to the user via Task 4's checkpoint for manual T1-T8 UAT.
</verification>

<success_criteria>
- All 9 ChatGpt-prefixed test files renamed via `git mv`.
- DeckControllerTests.cs (126 hits) fully swept: 6 inline test doubles renamed, test methods renamed, ctor sites updated, view-name assertions updated to match Phase 12 view names.
- TestServiceFactory.cs 3 factory methods + 2 ILogger generic args renamed.
- Test method names containing `ChatGpt[A-Z]` renamed per Open Question 2.
- HelpContentServiceTests.cs `"chatgpt-analysis"` fixture preserved (Pitfall 3).
- ResultContractTests.cs + AiPlatformPhase10RoundTripTests.cs `[InlineData("ChatGPT", "Claude", "Gemini")]` test inputs preserved.
- Final solution build `dotnet build DeckFlow.sln --configuration Release` returns 0 errors / 0 new warnings.
- Final allowlisted grep gate `grep -rEn "ChatGpt[A-Z]" --include="*.cs" ... | grep -vE '"ChatGPT"' | grep -vE 'ChatGPT/Claude/Gemini'` returns ZERO hits.
- Lowercase-chatgpt audit returns exactly 3 hits (PacketArtifactStore.cs L537/L540/L543).
- DeckFlow.CLI builds clean.
- HUMAN-UAT.md emitted with T1-T8 checklist.
- _AiSelector.cshtml verified UNTOUCHED across Phase 13.
- All Phase 13 commits are plain-author (no Co-Authored-By trailer).
- Human user runs T1-T8 manually and approves (or surfaces gaps for `--gaps` follow-up).
</success_criteria>

<output>
Create `.planning/phases/13-chatgpt-class-rename-summary-doc-comments/13-04-SUMMARY.md` when done (AFTER user types "approved" to Task 4 checkpoint). Capture:
- All 9 test file renames + 2 test file edits
- Final solution build result (0 errors / 0 new warnings)
- Final allowlisted grep gate result (ZERO hits)
- Lowercase-chatgpt audit result (3 hits — PacketArtifactStore.cs preservation)
- DeckFlow.CLI build result (clean)
- HUMAN-UAT.md path
- T1-T8 manual UAT outcome (recorded after user runs through them)
- Total Phase 13 commit count + confirmation all plain-author
- Forward-looking note: Phase 13 complete; next phase per ROADMAP is Phase 14 (Broader Codebase Name-vs-Behavior Audit) — uses Phase 13's rename pattern as template, removes `NoWarn 1591` from csproj as candidate per D-04 deferred scope.
</output>
