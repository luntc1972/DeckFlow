---
phase: 72-command-zone-commander-castability
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - DeckFlow.Core/Manabase/ManabaseAnalyzer.cs
  - DeckFlow.Core/Manabase/ManabaseSwapPromptBuilder.cs
  - DeckFlow.Core.Tests/Manabase/ManabaseAnalyzerTests.cs
  - DeckFlow.Core.Tests/Manabase/CastabilitySimulatorTests.cs
  - DeckFlow.Core.Tests/Manabase/ManabaseClassifierTests.cs
  - DeckFlow.Core.Tests/Manabase/ManabaseSwapPromptBuilderTests.cs
autonomous: true
requirements: [D-01, C-01, C-02, A/B-01, A/B-02, B-03, F-pin]
must_haves:
  truths:
    - "With two command-zone commanders, the headline spell is the WORST-of (lowest CastPercent) commander deterministically"
    - "A companion's castability can be computed from a SpellRequirement carrying ManaValue = printed + 3, outside the 99 (library size unchanged)"
    - "A partner pair (2 IsCommander cards) yields commanderCount = 2; a companion (IsCommander = false) is NOT counted in commanderCount"
    - "The ramp/draw threshold proxy uses the MAX mana value across command-zone cards (Phase 71/72 agree)"
    - "The swap prompt can append command-zone castability line(s); default off keeps the prompt byte-identical"
  artifacts:
    - path: "DeckFlow.Core/Manabase/ManabaseAnalyzer.cs"
      provides: "Worst-of SelectHeadlineSpell + public SimulateCompanion helper"
      contains: "SimulateCompanion"
    - path: "DeckFlow.Core/Manabase/ManabaseSwapPromptBuilder.cs"
      provides: "Optional command-zone castability rendering (default off = byte-identical)"
  key_links:
    - from: "ManabaseAnalyzer.SimulateCompanion"
      to: "CastabilitySimulator.Simulate"
      via: "single sim call with librarySize = TotalCards - CommanderCount"
      pattern: "CastabilitySimulator.Simulate"
---

<objective>
Land the Core math for command-zone castability: deterministic worst-of headline selection,
a companion castability helper (+3 generic "to hand" heuristic, outside the 99), regression
locks for partner/background commanderCount + companion exclusion + the Phase 71 threshold
proxy, and an opt-in command-zone line in the swap prompt. All additive/opt-in: with the
caller passing defaults, manabase output stays byte-identical.

Purpose: Keep the command-zone simulation math in pure Core (testable in Core.Tests); the
Web service (72-05) only detects + clamps + wires.
Output: Worst-of fix + SimulateCompanion + opt-in prompt line + Core regression locks.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/72-command-zone-commander-castability/72-RESEARCH.md
@.planning/phases/72-command-zone-commander-castability/72-SPEC.md

<interfaces>
<!-- Verified Core contracts. -->
- ManabaseAnalyzer.SelectHeadlineSpell(IReadOnlyList<CardCastability>, CommanderImportance) ~:854-875:
  today `castability.FirstOrDefault(c => c.IsCommander)` for Central importance (non-deterministic with 2 commanders).
- ManabaseAnalyzer.Analyze derives `int librarySize = deck.TotalCards - deck.CommanderCount;` (~:93) and calls
  `CastabilitySimulator.Simulate(deck, librarySize, spell, onCurveTurn, genericReduction, useManaQuantity, colorAwareMulligan, gateRampOnCastable)` (~:293-295).
- CastabilitySimulator.Simulate(ManabaseDeck deck, int librarySize, SpellRequirement spell, int effectiveTurn, int genericReduction, int trials=DefaultTrials, bool useManaQuantity=false, bool colorAwareMulligan=false, bool gateRampOnCastable=false) → CardCastability (~:156-165). The companion has no ManaSource entry, so it is naturally absent from the built library.
- SpellRequirement (sealed record): required Name (string), ManaValue (int), Pips (IReadOnlyDictionary<ManaColor,int>); bool IsGold, IsManaSource, IsCommander, IsCostOverridden; SpellKinds Kinds.
- CardCastability (sealed record): Name, ManaValue, OnCurveTurn, CastPercent, LimitingFactor, IsCommander, ...
- ManabaseClassifier accumulates `commanderCount += card.Quantity` only `if (card.IsCommander)` (~:108-110); CommanderCount set on the deck (~:283).
- ManabaseRampDrawBudgetCalculator.Calculate(deck) / DetermineThreshold already uses `.Max()` over commander mana values (RESEARCH locked decision: no code change — confirm with a test only).
- ManabaseSwapPromptBuilder.Build(ManabaseReport report, string? deckName, string? decklistText, ManabaseMode mode=Casual, ManabaseVerdict? verdict=null, ManabaseRampDrawBudget? budget=null) ~:26.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Deterministic worst-of headline selection (D-01)</name>
  <files>DeckFlow.Core/Manabase/ManabaseAnalyzer.cs, DeckFlow.Core.Tests/Manabase/ManabaseAnalyzerTests.cs</files>
  <read_first>
    - DeckFlow.Core/Manabase/ManabaseAnalyzer.cs (SelectHeadlineSpell ~:854-875)
    - DeckFlow.Core.Tests/Manabase/ManabaseAnalyzerTests.cs (existing analyzer test patterns)
  </read_first>
  <behavior>
    - Two commander rows with different CastPercent, Central importance: headline = the lower-CastPercent commander.
    - Single commander, Central importance: headline = that commander (unchanged).
    - No commander rows: headline = worst non-commander (unchanged).
  </behavior>
  <action>
    In SelectHeadlineSpell, replace the Central-importance `castability.FirstOrDefault(c => c.IsCommander)`
    with `castability.Where(c => c.IsCommander).MinBy(c => c.CastPercent)` (worst-of across all command-zone
    partners — RESEARCH Pattern 6 / Pitfall 5). Leave the non-Central branch unchanged. This makes the
    multi-commander headline deterministic and surfaces the harder-to-cast partner.
  </action>
  <verify>
    <automated>"/mnt/c/Program Files/dotnet/dotnet.exe" test DeckFlow.Core.Tests --filter "FullyQualifiedName~ManabaseAnalyzerTests"</automated>
  </verify>
  <acceptance_criteria>
    - New test: two commanders (e.g. 30% and 80%), Central → headline is the 30% one. Passes.
    - Single-commander and no-commander headline behavior unchanged (existing tests still green).
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: SimulateCompanion helper + command-zone Core regression locks (C-01, C-02, A/B-01, A/B-02, B-03, F-pin)</name>
  <files>DeckFlow.Core/Manabase/ManabaseAnalyzer.cs, DeckFlow.Core.Tests/Manabase/CastabilitySimulatorTests.cs, DeckFlow.Core.Tests/Manabase/ManabaseClassifierTests.cs</files>
  <read_first>
    - DeckFlow.Core/Manabase/ManabaseAnalyzer.cs (librarySize derivation ~:93, Simulate call ~:293-295)
    - DeckFlow.Core/Manabase/CastabilitySimulator.cs (Simulate signature ~:156-165, effectiveGeneric formula ~:178-186)
    - DeckFlow.Core/Manabase/ManabaseClassifier.cs (commanderCount accumulation ~:108-110)
    - DeckFlow.Core/Manabase/ManabaseRampDrawBudget.cs (DetermineThreshold .Max())
  </read_first>
  <behavior>
    - SimulateCompanion(deck, companionSpell) returns a CardCastability whose CastPercent reflects a spell of
      ManaValue = companionSpell.ManaValue (already +3 by the caller), simulated against librarySize = deck.TotalCards - deck.CommanderCount.
    - A companion with the same printed pips but ManaValue+3 has a strictly LOWER CastPercent than the same spell at its printed ManaValue (proves the +3 is applied, not a no-op — Pitfall 4).
    - The companion is NOT in the deck's library/sources: building it does not change librarySize.
    - ManabaseClassifier: 2 IsCommander cards → CommanderCount == 2 (partner / background). A companion modeled as IsCommander=false is NOT counted.
    - ManabaseRampDrawBudgetCalculator: with 2 commanders of MV 4 and MV 6, the threshold proxy == 6 (max MV).
  </behavior>
  <action>
    Add `public static CardCastability SimulateCompanion(ManabaseDeck deck, SpellRequirement companionSpell,
    bool useManaQuantity = false, bool colorAwareMulligan = false, bool gateRampOnCastable = false)` to
    ManabaseAnalyzer: compute librarySize the same way Analyze does (`deck.TotalCards - deck.CommanderCount`),
    set effectiveTurn = companionSpell.ManaValue and genericReduction = 0, and call CastabilitySimulator.Simulate.
    Mark with a `// HEURISTIC:` comment that the +3 "to hand" tax is pre-applied by the caller on
    companionSpell.ManaValue (NOT via genericReduction, which Math.Max(0,..) clamps to a no-op — Pitfall 4).
    Do NOT add the companion to deck.Spells/Sources. Then write Core regression-lock tests (NO production
    change needed for these, they assert existing behavior): partner pair → CommanderCount==2 (A/B-01);
    two IsCommander cards via Background-style classification → CommanderCount==2 (A/B-02); companion
    (IsCommander=false) excluded from CommanderCount (B-03); ramp/draw threshold == max commander MV (F-pin).
  </action>
  <verify>
    <automated>"/mnt/c/Program Files/dotnet/dotnet.exe" test DeckFlow.Core.Tests --filter "FullyQualifiedName~CastabilitySimulatorTests|FullyQualifiedName~ManabaseClassifierTests"</automated>
  </verify>
  <acceptance_criteria>
    - SimulateCompanion exists and returns a lower CastPercent at MV+3 than at printed MV for the same pips.
    - Classifier tests: partner pair and Background-style 2-commander → CommanderCount==2; companion not counted.
    - Threshold-proxy test: 2 commanders MV{4,6} → threshold 6. All green.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Opt-in command-zone line in swap prompt (D-prompt)</name>
  <files>DeckFlow.Core/Manabase/ManabaseSwapPromptBuilder.cs, DeckFlow.Core.Tests/Manabase/ManabaseSwapPromptBuilderTests.cs</files>
  <read_first>
    - DeckFlow.Core/Manabase/ManabaseSwapPromptBuilder.cs (Build ~:26-76)
    - DeckFlow.Core.Tests/Manabase/ManabaseSwapPromptBuilderTests.cs
  </read_first>
  <behavior>
    - Build with the new command-zone params defaulted (off): output byte-identical to today.
    - Build with command-zone enabled + commander rows present: appends a concise "Command-zone castability"
      line naming each commander and its CastPercent, plus the companion line (with the +3 heuristic note) when a companion row is supplied.
  </behavior>
  <action>
    Add optional params to Build: `bool includeCommandZone = false` and `CardCastability? companionRow = null`.
    When includeCommandZone is true, append one concise block summarizing the command-zone cast rates: iterate
    `report.Castability.Where(c => c.IsCommander)` for the commander line(s), and if companionRow is non-null
    append a companion line noting the +3 generic "to hand" tax is a heuristic. Plain ASCII only (no em/en
    dashes per CLAUDE.md). Do NOT re-indent the existing raw-string/StringBuilder literals beyond the added
    lines. Default params off MUST keep the existing prompt bytes unchanged.
  </action>
  <verify>
    <automated>"/mnt/c/Program Files/dotnet/dotnet.exe" test DeckFlow.Core.Tests --filter "FullyQualifiedName~ManabaseSwapPromptBuilderTests"</automated>
  </verify>
  <acceptance_criteria>
    - A byte-identity test: Build(...) with defaults equals the pre-change output for a sample report.
    - A new test asserts the command-zone line appears (commander name + %, companion +3 note) when enabled.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Web service → Core helper | companionSpell crosses into Core; Core trusts its ManaValue (clamped upstream in 72-05) |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-72-04 | Tampering | companionSpell.ManaValue arithmetic | mitigate | Simulate already clamps internal cost (effectiveCost Math.Max); the +3 clamp at the input boundary is enforced in 72-05; helper documents the trust assumption |
| T-72-04b | Tampering | Default-off prompt path | mitigate | Byte-identity test on Build(...) defaults prevents an accidental prompt change |
</threat_model>

<verification>
- `"/mnt/c/Program Files/dotnet/dotnet.exe" test DeckFlow.Core.Tests` green (analyzer, simulator, classifier, budget, prompt).
- Build clean.
</verification>

<success_criteria>
- Worst-of headline deterministic; SimulateCompanion proven to apply +3; commanderCount + threshold locks
  green; swap prompt gains an opt-in command-zone line with a default-off byte-identity guard.
</success_criteria>

<output>
Create `.planning/phases/72-command-zone-commander-castability/72-04-SUMMARY.md` when done.
</output>
