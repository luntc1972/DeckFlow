---
phase: 65-prod-content-artifact-reconcile
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/65-prod-content-artifact-reconcile/65-DATA01-DECISION.md
  - .planning/phases/65-prod-content-artifact-reconcile/65-PROBE-RESULTS.md
autonomous: false
requirements: [DATA-01]
must_haves:
  truths:
    - "It is recorded, with code citations, that the live site serves content-KB body from /data .md files via File.ReadAllTextAsync — not from a DB content/body column."
    - "The Render startup-log resolver path (Content KB content base) is confirmed and recorded."
    - "Read-only prod probe Query A/B/C results are captured, including the published-orphan count that gates Plan 02's reconcile path."
  artifacts:
    - path: ".planning/phases/65-prod-content-artifact-reconcile/65-DATA01-DECISION.md"
      provides: "DATA-01 serving-path decision with ContentKbController.cs + ContentSiteIndexStore.cs citations and confirmed resolver-base log line"
      contains: "File.ReadAllTextAsync"
    - path: ".planning/phases/65-prod-content-artifact-reconcile/65-PROBE-RESULTS.md"
      provides: "Read-only prod probe output (Query A/B/C + SFTP listing) and the computed published-orphan count"
      contains: "published orphan"
  key_links:
    - from: "65-PROBE-RESULTS.md published-orphan count"
      to: "65-02 reconcile path selection"
      via: "decision-tree gate (count > 0 vs = 0)"
      pattern: "published.orphan"
---

<objective>
Settle DATA-01 by recording — with exact code citations — that the live content-KB body is
served from `/data` `.md` files (not a DB column), confirm the prod resolver base via the Render
startup log, and run the READ-ONLY prod probe (SQL Query A/B/C + a `/data/content-kb` file
listing) to compute the **published-orphan count** that drives Plan 02's reconcile-path choice.

Purpose: DATA-01 is already answered in code by the research; this plan converts that finding into
a recorded decision and gathers the read-only prod facts (published-orphan count, resolver base)
that Plan 02 needs to pick a reconcile path. No code changes; no prod writes.

Output: `65-DATA01-DECISION.md` (serving-path decision + resolver-base confirmation) and
`65-PROBE-RESULTS.md` (probe output + published-orphan count).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/65-prod-content-artifact-reconcile/65-RESEARCH.md

<interfaces>
<!-- Code facts already verified by the researcher + planner. Cite these directly; do not re-investigate. -->

DeckFlow.Web/Controllers/ContentKbController.cs (Detail action):
  line 109: var resolved = _resolver.ResolveArtifactFullPath(row.ArtifactPath);
  line 115: if (!System.IO.File.Exists(resolved)) { ... return View("Detail", ... artifactUnavailable: true); }
  line 121: var raw = await System.IO.File.ReadAllTextAsync(resolved, cancellationToken)...
  -> Body comes from the filesystem at request time. No DB content column is read.

DeckFlow.Core/Content/ContentSiteIndexStore.cs (DDL, both Postgres + SQLite variants):
  Columns are metadata only: id, source, title, video_url, artifact_path, published_utc,
  pushed_to_prod_utc, indexed_utc, three tag arrays, natural_key pair, is_visible, is_hidden,
  is_evergreen, approval_status. There is NO `content`, `body`, or `full_text` column.

DeckFlow.Web/Services/Content/ContentKbArtifactPathResolver.cs:
  line 31: logs "Content KB content base resolved to {ContentBase}; content-kb exists: {ContentKbExists}."
  -> This is the startup-log line the operator greps to confirm the prod resolver base is /data.

Prod probe target (READ-ONLY): Render MCP `mcp__render__query_render_postgres`,
  postgresId "dpg-d7oj8iugvqtc73fso0g0-a". Query A/B/C verbatim in 65-RESEARCH.md
  "Prod Probe Specification (READ-ONLY)".
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the DATA-01 serving-path decision document</name>
  <files>.planning/phases/65-prod-content-artifact-reconcile/65-DATA01-DECISION.md</files>
  <read_first>
    - .planning/phases/65-prod-content-artifact-reconcile/65-RESEARCH.md (section "DATA-01 Answer: Confirmed — Served from /data .md Files")
    - DeckFlow.Web/Controllers/ContentKbController.cs lines 105-145 (Detail action serving path)
    - DeckFlow.Core/Content/ContentSiteIndexStore.cs lines 45-138 (EnsureSchemaAsync DDL — confirm no content/body column)
    - DeckFlow.Web/Services/Content/ContentKbArtifactPathResolver.cs lines 25-60 (resolver candidate walk + log line)
  </read_first>
  <action>
    Create `65-DATA01-DECISION.md` recording the DATA-01 finding: the live site serves content-KB
    body from `/data/content-kb/{source-slug}/{video-id}.md` via `System.IO.File.ReadAllTextAsync`,
    and there is NO DB content/body column. Cite `ContentKbController.cs:109,115,121` for the
    serving path and `ContentSiteIndexStore.cs` DDL (state the column list verbatim and assert the
    absence of `content`/`body`/`full_text`). Record the failure modes: missing file with
    `is_visible=TRUE` renders the Detail page with `ArtifactUnavailable: true` (metadata-only),
    and `is_visible=FALSE` returns 404. Add a "Resolver-base confirmation" section that names the
    startup-log line `Content KB content base resolved to {ContentBase}` from
    `ContentKbArtifactPathResolver.cs:31` as the operator check, and note the contingency from
    RESEARCH Pitfall 4 / Open Question 1: if the logged base is not `/data`, set
    `ContentKb__ContentBase=/data` in Render env. Do NOT re-derive the answer from scratch — this
    is a recording task grounded in the cited lines.
  </action>
  <verify>
    <automated>MISSING — doc task, no code test. Acceptance is the file existing with the required citations (see acceptance_criteria).</automated>
  </verify>
  <acceptance_criteria>
    - File `65-DATA01-DECISION.md` exists.
    - It contains the literal string `File.ReadAllTextAsync` and cites `ContentKbController.cs` with line numbers.
    - It states there is no `content`/`body`/`full_text` column in `content_site_index` and cites `ContentSiteIndexStore.cs`.
    - It names the resolver startup-log line `Content KB content base resolved to` as the operator confirmation step and the `ContentKb__ContentBase=/data` contingency.
  </acceptance_criteria>
  <done>DATA-01 serving-path decision recorded with code citations and the resolver-base confirmation procedure.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Confirm Render resolver base from the prod startup log (operator)</name>
  <action>Operator-run confirmation — see &lt;what-built&gt;/&lt;how-to-verify&gt;. Grep the live Render startup log for the resolver-base line and record it; no AI automation possible (no Render log CLI/API in this environment).</action>
  <read_first>
    - .planning/phases/65-prod-content-artifact-reconcile/65-DATA01-DECISION.md (Task 1 output — the procedure to confirm)
    - DeckFlow.Web/Services/Content/ContentKbArtifactPathResolver.cs line 31 (the log template)
  </read_first>
  <what-built>
    Task 1 documented that the resolver base must be confirmed from the Render startup log. This
    checkpoint is the operator-run confirmation — it cannot be automated because it reads live
    Render service logs (no CLI/API access from this environment).
  </what-built>
  <how-to-verify>
    1. Open the Render dashboard for the `mtg-deck-studio` web service → Logs.
    2. Search the most recent startup logs for: `Content KB content base resolved to`.
    3. Record the resolved `{ContentBase}` value and the `content-kb exists:` boolean shown.
    4. If the base is `/data` and `content-kb exists: True` → DATA-01 resolver path confirmed.
    5. If the base is NOT `/data` (e.g. `/app`) → record the remediation: set
       `ContentKb__ContentBase=/data` in Render env vars (operator action; out of AI scope).
    Append the recorded value to `65-DATA01-DECISION.md` under a "Resolver-base confirmation (live)"
    heading. Do not paste connection strings or secrets — only the resolved base path + boolean.
  </how-to-verify>
  <acceptance_criteria>
    - `65-DATA01-DECISION.md` has a "Resolver-base confirmation (live)" entry recording the actual logged `{ContentBase}` and `content-kb exists` value.
    - If base != `/data`, the `ContentKb__ContentBase=/data` remediation is recorded as an operator follow-up.
    - No secrets/connection strings are pasted into the file.
  </acceptance_criteria>
  <resume-signal>Type "confirmed: base=&lt;value&gt;" or describe the resolver-base finding.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Run the read-only prod probe and compute the published-orphan count (operator/AI-read)</name>
  <action>Operator/AI-read step — see &lt;what-built&gt;/&lt;how-to-verify&gt;. Run read-only prod probe Query A/B/C + the /data file listing and record the published-orphan count; no prod writes.</action>
  <read_first>
    - .planning/phases/65-prod-content-artifact-reconcile/65-RESEARCH.md (section "Prod Probe Specification (READ-ONLY)" — Query A/B/C + SFTP listing)
    - .planning/phases/65-prod-content-artifact-reconcile/65-RESEARCH.md (section "Three Reconcile Paths" → decision tree — so the count is interpreted correctly)
  </read_first>
  <what-built>
    A READ-ONLY probe of prod state. The DB queries run via Render MCP
    `mcp__render__query_render_postgres` (postgresId `dpg-d7oj8iugvqtc73fso0g0-a`) — read-only by
    design. The `/data/content-kb` file listing is operator-run via Studio Pull-from-Prod (Phase 60)
    or an SFTP `find /data/content-kb -name "*.md" | sort`. No prod writes occur.
  </what-built>
  <how-to-verify>
    1. Run Query A (summary by source + visibility), Query B (counts by is_visible/is_hidden/
       approval_status), and Query C (full row list with artifact_path) from RESEARCH verbatim
       against postgresId `dpg-d7oj8iugvqtc73fso0g0-a`. These are SELECT-only.
    2. Produce the `/data/content-kb/*.md` file listing (Studio Pull-from-Prod OR SFTP `find`).
    3. Cross-join: a row is a PUBLISHED ORPHAN when `is_visible = TRUE AND is_hidden = FALSE` AND
       its `artifact_path` is NOT in the file listing. Count them.
    4. Write `65-PROBE-RESULTS.md` capturing: Query A/B/C raw output (no secrets), the file-listing
       count, the total orphan count, and the **published-orphan count** called out explicitly as
       the severity gate. State which decision-tree branch the count selects (count > 0 → Option A
       if local artifacts exist else Option B; count = 0 → Option B or C) — but do NOT execute the
       reconcile here (that is Plan 02).
    5. Confirm no connection strings / Npgsql exception text are pasted into the file.
  </how-to-verify>
  <acceptance_criteria>
    - `65-PROBE-RESULTS.md` exists and contains Query A/B/C results plus the `/data` file-listing count.
    - It states a single explicit integer for the **published-orphan count** (is_visible=TRUE AND is_hidden=FALSE AND artifact missing).
    - It names the decision-tree branch that count selects, deferring execution to Plan 02.
    - No secrets/connection strings/raw exception messages appear in the file.
  </acceptance_criteria>
  <resume-signal>Type "probe done: published-orphans=&lt;N&gt;" or describe the probe results.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| AI/operator → prod Postgres | Read-only SELECT via Render MCP `query_render_postgres`; no write path exercised |
| operator → Render `/data` | SFTP/Studio listing of artifact files; read-only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-65-01-INFO | Information Disclosure | Probe output written to 65-PROBE-RESULTS.md / 65-DATA01-DECISION.md | mitigate | Acceptance criteria forbid pasting connection strings, SCP host, or raw Npgsql/SSH exception text (D-07 pattern). Only metadata rows + counts + resolved base path are recorded. |
| T-65-01-PRODWRITE | Tampering | Prod DB / prod `/data` | accept (non-threat) | This plan introduces NO prod write path — all DB access is SELECT-only via the read-only Render MCP tool; the file listing is read-only. Reconcile writes are deferred to operator-run Plan 02. Called out explicitly as a non-threat per phase scope. |
</threat_model>

<verification>
- `65-DATA01-DECISION.md` cites the serving path (`File.ReadAllTextAsync`) and the no-DB-column finding, and records the resolver-base confirmation.
- `65-PROBE-RESULTS.md` records Query A/B/C + file-listing and states the published-orphan count as the Plan-02 gate.
- No secrets recorded in either artifact.
</verification>

<success_criteria>
- DATA-01 documented and inspection-confirmed (SC1).
- Read-only prod probe complete; published-orphan count computed and recorded for Plan 02 (feeds SC2).
</success_criteria>

<output>
Create `.planning/phases/65-prod-content-artifact-reconcile/65-01-SUMMARY.md` when done.
</output>
