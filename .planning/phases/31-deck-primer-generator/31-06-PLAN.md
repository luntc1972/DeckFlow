---
phase: 31-deck-primer-generator
plan: 06
type: execute
wave: 4
depends_on: ["31-03", "31-04", "31-05"]
files_modified:
  - DeckFlow.Web/Controllers/DeckController.cs
  - DeckFlow.Web/Views/Deck/DeckPrimer.cshtml
  - DeckFlow.Web/Views/Shared/_DeckToolTabs.cshtml
  - DeckFlow.Web/wwwroot/ts/primer-selection.ts
  - DeckFlow.Web/wwwroot/css/site-common.css
requirements: [PRM-02, PRM-03, PRM-04, PRM-10, PRM-11, PRM-12]
autonomous: false
must_haves:
  truths:
    - "A Deck Primer tab appears in the Analyze nav group (4th peer of DeckAnalysis/DeckComparison/CedhMetaGap), linking to /deck-primer (PRM-02)"
    - "DeckController has GET /deck-primer, POST /deck-primer, POST /deck-primer/download, POST /deck-primer/upload (mirroring the deck-analysis action group), injecting IDeckPrimerPacketService"
    - "The page reuses the existing _AiSelector partial AS-IS for AI target selection (ChatGPT + Claude always; Gemini hidden unless DECKFLOW_GEMINI_ENABLED; persisted Gemini→ChatGPT rewrite preserved) — the primer does NOT modify _AiSelector or force-expose Gemini (HIGH-3)"
    - "POST /deck-primer displays the SELECTED platform's prompt for pasting (result.PromptTextsByPlatform[normalized TargetAiPlatform]); POST /deck-primer/download builds a zip containing ALL enabled prompt variants the service produced (HIGH-2)"
    - "DeckPrimer.cshtml renders the import flow (URL/paste), bracket selector, and the 31-section catalog as 5 collapsible <details> groups with per-section checkboxes + help text (PRM-04/12) and a selected-count badge per group header (PRM-11)"
    - "The bracket selector exposes PER-BRACKET presets — each <option> carries its own data-preset-ids (or a single serialized bracket→preset-ids map the TS reads); on first visit to ANY bracket (no saved state) the TS applies THAT bracket's own preset, including when switching from the initial bracket to a different one (D-3/MED)"
    - "primer-selection.ts persists section selections per bracket in localStorage (deckflow.primer.sections.<bracket>) with try/catch; first visit to a bracket applies that bracket's server-provided preset, later visits restore the saved set (D-3/PRM-10); bracket-scoped gating (cEDH-only #24/#25, casual-only #26) is enforced client-side regardless of stored toggles"
    - "A no-JS path works: the server renders the preset as the default checked set and NormalizeSelections re-gates server-side"
    - "Layout/badge/collapse CSS lives in site-common.css (NOT site.css/themes); compiled JS in wwwroot/js is NOT committed (gitignored)"
  artifacts:
    - path: "DeckFlow.Web/Controllers/DeckController.cs"
      provides: "Primer GET/POST/download/upload action group + IDeckPrimerPacketService injection; selected-platform prompt mapping + all-enabled-variant zip"
      contains: "IDeckPrimerPacketService"
    - path: "DeckFlow.Web/Views/Deck/DeckPrimer.cshtml"
      provides: "Primer page: import + _AiSelector + bracket + 5-group collapsible section catalog + per-bracket presets + badges + help + size warning"
      contains: "_AiSelector"
    - path: "DeckFlow.Web/Views/Shared/_DeckToolTabs.cshtml"
      provides: "Deck Primer nav link (4th Analyze peer)"
      contains: "DeckPrimer"
    - path: "DeckFlow.Web/wwwroot/ts/primer-selection.ts"
      provides: "per-bracket localStorage persistence + per-bracket preset seed + gating + badge updates"
      contains: "deckflow.primer.sections."
    - path: "DeckFlow.Web/wwwroot/css/site-common.css"
      provides: "primer section-group collapse + badge + help-reveal layout classes"
      contains: "primer-group"
  key_links:
    - from: "DeckController POST /deck-primer"
      to: "IDeckPrimerPacketService.BuildAsync"
      via: "form -> service -> view model (selected-platform prompt)"
      pattern: "BuildAsync"
    - from: "DeckController POST /deck-primer"
      to: "result.PromptTextsByPlatform[selected platform]"
      via: "display the user's selected AI variant for pasting (HIGH-2)"
      pattern: "PromptTextsByPlatform"
    - from: "primer-selection.ts"
      to: "localStorage deckflow.primer.sections.<bracket>"
      via: "per-bracket load/save with try/catch (D-3/PRM-10)"
      pattern: "deckflow.primer.sections."
    - from: "bracket <select> per-bracket data-preset-ids (or serialized map)"
      to: "primer-selection.ts first-visit preset application"
      via: "each bracket seeds its OWN preset on first visit (D-3/MED)"
      pattern: "data-preset-ids|data-bracket-presets"
    - from: "DeckController POST /deck-primer/download|upload"
      to: "PacketArtifactStore.BuildPrimerZip / LoadPrimerFromZip"
      via: "zip download (all enabled variants) + re-upload restore (PRM-09/SC5)"
      pattern: "BuildPrimerZip"
---

<objective>
The user-facing primer workflow: the 4th nav tab, the `DeckController` primer action group (GET/POST/download/upload), the `DeckPrimer.cshtml` page (import flow + the reused `_AiSelector` + bracket selector + 31-section catalog in 5 collapsible groups with help text and selected-count badges + paste-size warning), the `primer-selection.ts` per-bracket localStorage module (per-bracket-preset-seed-then-restore + gating + badges), and the `site-common.css` layout classes. The page shows the user's SELECTED platform prompt for pasting; the downloaded zip carries ALL enabled variants (HIGH-2). Gemini is exposed only through the existing `_AiSelector` gate — never force-exposed (HIGH-3). Ends with a 2-viewport human visual-verify checkpoint (UI-phase requirement).

Purpose: Delivers the remaining user-facing requirements — PRM-02 (tab + import), PRM-03 (per-bracket preset applied + gated sections), PRM-04 (toggle the 31-section catalog in 5 groups), PRM-10 (per-bracket localStorage persistence), PRM-11 (collapsed-group selected-count badges), PRM-12 (per-section help text). The service/variants/zip from earlier waves are wired into the page here.

Output: `DeckController` primer actions, `DeckPrimer.cshtml`, `_DeckToolTabs.cshtml` link, `primer-selection.ts`, `site-common.css` additions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-deck-primer-generator/31-CONTEXT.md
@.planning/phases/31-deck-primer-generator/31-RESEARCH.md
@.planning/phases/31-deck-primer-generator/31-PATTERNS.md

<interfaces>
<!-- Confirmed against live code 2026-06-08. Executor uses these directly. -->

DeckController (DeckFlow.Web/Controllers/DeckController.cs):
  - ctor (lines 40-66): add `IDeckPrimerPacketService _deckPrimerPacketService` field + ctor param (DI auto-resolves; no explicit ThrowIfNull needed)
  - deck-analysis action group to mirror: GET /deck-analysis (158), POST /deck-analysis (461), POST /deck-analysis/download (514), POST /deck-analysis/upload (612). Error handling: catch InvalidOperationException -> ErrorMessage on the view model; catch Exception -> log + generic ErrorMessage (PATTERNS error-handling section).
  - SELECTED-PLATFORM display (HIGH-2): after BuildAsync, set the view model's PrimerPromptText to `result.PromptTextsByPlatform.GetValueOrDefault(AiPlatform.Normalize(request.TargetAiPlatform).Key)` (the variant the user chose). The result holds only enabled platforms; the selected platform is guaranteed enabled because _AiSelector only offers enabled platforms (Gemini→ChatGPT rewrite already handled by the partial). Guard a missing key with a friendly empty fallback.
  - download path (HIGH-2 — ALL enabled variants in the zip): obtain the `DeckPrimerPacketResult` (cache-hit via TryComputeCacheKeyAsync -> _packetCache.TryGet<DeckPrimerPacketResult>; on cache-miss, call BuildAsync to produce it) -> `PacketArtifactStore.BuildPrimerZip(request, result.InputSummary, result.RequestContextText, result.PromptTextsByPlatform.GetValueOrDefault("ChatGPT"), result.PromptTextsByPlatform.GetValueOrDefault("Claude"), result.PromptTextsByPlatform.GetValueOrDefault("Gemini"), result.DecklistText, ...)` — the request-context text comes from `result.RequestContextText` (built by BuildAsync in 31-03), NOT an undefined local; absent platforms pass null → BuildPrimerZip omits them -> `File(bytes, "application/zip", PacketArtifactStore.SuggestPrimerZipFileName(result.ResolvedCommanderName, request.TargetAiPlatform))`
  - upload path: PacketArtifactStore.LoadPrimerFromZip(stream, request) -> re-render view with restored Request

_AiSelector.cshtml (DeckFlow.Web/Views/Shared/_AiSelector.cshtml) — REUSE AS-IS (HIGH-3):
  - `@await Html.PartialAsync("_AiSelector", Model.Request.TargetAiPlatform)` — renders the ChatGPT/Claude/Gemini radio group; Gemini radio hidden unless DECKFLOW_GEMINI_ENABLED; persisted Gemini value rewritten to ChatGPT when disabled. Do NOT copy/modify this partial; do NOT add a separate AI selector. This is the single source of "enabled platforms" the user can choose.

_DeckToolTabs.cshtml (DeckFlow.Web/Views/Shared/_DeckToolTabs.cshtml):
  - analyzeActive group (~line 20) uses `Model is DeckPageTab.DeckAnalysis or DeckPageTab.DeckComparison or DeckPageTab.CedhMetaGap` — extend with `or DeckPageTab.DeckPrimer`
  - add `<a ... href="@Url.Content("~/deck-primer")">Deck Primer</a>` as the 4th Analyze link with `is-active` when Model == DeckPageTab.DeckPrimer

DeckAnalysis.cshtml (DeckFlow.Web/Views/Deck/DeckAnalysis.cshtml) — the page analog:
  - top: @model, ViewData Title, AiPlatform.Normalize, WorkflowStep clamp, CommanderBracketCatalog.Options, the `_AiSelector` partial usage
  - partials: _BusyIndicator, _DeckToolTabs (Model.ActiveTab), _WorkflowStepTabs (WorkflowStepTabsModel)
  - PasteWarningBytes indicator (lines 478-479): byte count via Encoding.UTF8.GetByteCount + aiPlatform.PasteWarningBytes compare
  - download/upload form actions (lines 87/106) + data-* hooks reused by the existing compiled JS

PrimerSectionCatalog (from 31-02): Groups (5), AllSections (31), GetPresetForBracket(bracketValue), CedhOnlySectionIds/CasualOnlySectionIds, NormalizeSelections(ids, bracket). Each PrimerSectionEntry has Number/Title/HelpText/Group/BracketGate. NOTE: GetPresetForBracket is PER-BRACKET — call it for EVERY bracket option to emit each option's own preset (D-3/MED).

CommanderBracketCatalog (from 31-02 interfaces): Options + values (Exhibition/Core/Upgraded/Optimized/cEDH).

kb-selection.ts (DeckFlow.Web/wwwroot/ts/kb-selection.ts) — TS analog:
  - IIFE wrapper + 'use strict'; module:"none"; strict TS (no implicit any)
  - localStorage load/save with try/catch (lines 92-135); hidden-field inject (382-400); DeckFlow namespace export (725-727)
  - compiled output -> wwwroot/js/primer-selection.js (gitignored per .gitignore line 13 — DO NOT commit the .js)

CONSTRAINTS:
  - Layout/badge/collapse CSS -> DeckFlow.Web/wwwroot/css/site-common.css ONLY (never site.css/themes). Token additions (if any) -> :root of each theme file.
  - Compiled JS (wwwroot/js/*.js) is gitignored — do NOT stage it. The MSBuild CompileTypeScriptAssets target regenerates it at build.
  - No feature-flag gate on the tab unless the user asks (PATTERNS note).
  - Reuse _AiSelector as-is for the AI target (HIGH-3) — do not build a bespoke selector and do not force-expose Gemini.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: DeckController primer action group + tab link</name>
  <files>DeckFlow.Web/Controllers/DeckController.cs, DeckFlow.Web/Views/Shared/_DeckToolTabs.cshtml</files>
  <read_first>
    - DeckFlow.Web/Controllers/DeckController.cs (FULL — ctor 40-66, the deck-analysis action group 158/461/514/612, error handling, download/upload cache-hit + zip logic, how it sources per-platform prompt text)
    - DeckFlow.Web/Views/Shared/_DeckToolTabs.cshtml (FULL — analyzeActive group, the 3 existing Analyze links)
    - DeckFlow.Web/Services/DeckPrimerPacketService.cs (IDeckPrimerPacketService + DeckPrimerPacketResult.PromptTextsByPlatform from 31-03)
    - DeckFlow.Web/Services/PacketArtifactStore.cs (BuildPrimerZip/LoadPrimerFromZip/SuggestPrimerZipFileName from 31-05)
    - DeckFlow.Web/Models/DeckPrimerViewModel.cs, DeckPrimerRequest.cs (from 31-02); DeckFlow.Web/Models/AiPlatform.cs (Normalize)
    - .planning/phases/31-deck-primer-generator/31-PATTERNS.md (DeckController.cs + _DeckToolTabs.cshtml sections)
  </read_first>
  <action>
    In `DeckController.cs`: add a `private readonly IDeckPrimerPacketService _deckPrimerPacketService;` field and a corresponding ctor parameter assigned in the body (DI resolves the AddScoped service registered in 31-04). Add the primer action group after the CedhMetaGap group, mirroring the deck-analysis group:
      - `[HttpGet("/deck-primer")] public IActionResult DeckPrimer()` -> `View("DeckPrimer", new DeckPrimerViewModel { ActiveTab = DeckPageTab.DeckPrimer, Request = new DeckPrimerRequest { TargetCommanderBracket = <CommanderBracketCatalog default bracket value> } })`. WARNING-2 fix: the GET request MUST carry a non-empty default bracket (pick the catalog's sensible default — e.g. the most-common "Optimized"/bracket-4 key as it exists in CommanderBracketCatalog.Options) so the no-JS Razor render has a non-empty `GetPresetForBracket(...)` preset and the default-checked section set is not empty. Confirm the exact key string against CommanderBracketCatalog before use.
      - `[HttpPost("/deck-primer")] [ValidateAntiForgeryToken] public async Task<IActionResult> DeckPrimer(DeckPrimerRequest request)` -> guard null request; validation gates mirroring deck-analysis; call `await _deckPrimerPacketService.BuildAsync(request, HttpContext.RequestAborted)`; render the view with the result mapped onto DeckPrimerViewModel. Set `PrimerPromptText` to the user's SELECTED platform variant: `result.PromptTextsByPlatform.GetValueOrDefault(AiPlatform.Normalize(request.TargetAiPlatform).Key)` (HIGH-2 — the page shows the chosen AI's prompt; the result only holds enabled platforms and _AiSelector only offers enabled platforms). catch InvalidOperationException -> ErrorMessage = ex.Message; catch Exception -> `_logger.LogError(ex, "Deck primer build failed.")` + generic ErrorMessage.
      - `[HttpPost("/deck-primer/download")]` mirroring /deck-analysis/download (HIGH-2 — ALL enabled variants in the zip): obtain the `DeckPrimerPacketResult` (cache-hit: TryComputeCacheKeyAsync -> _packetCache.TryGet<DeckPrimerPacketResult>; cache-miss: `await _deckPrimerPacketService.BuildAsync(request, ...)`) -> `PacketArtifactStore.BuildPrimerZip(request, result.InputSummary, result.RequestContextText, result.PromptTextsByPlatform.GetValueOrDefault("ChatGPT"), result.PromptTextsByPlatform.GetValueOrDefault("Claude"), result.PromptTextsByPlatform.GetValueOrDefault("Gemini"), result.DecklistText, ...)` (the request-context comes from `result.RequestContextText` built by BuildAsync in 31-03 — NOT an undefined local; absent platforms arrive null → omitted by BuildPrimerZip) -> `File(bytes, "application/zip", PacketArtifactStore.SuggestPrimerZipFileName(result.ResolvedCommanderName, request.TargetAiPlatform))`.
      - `[HttpPost("/deck-primer/upload")]` mirroring /deck-analysis/upload: read the uploaded zip, `PacketArtifactStore.LoadPrimerFromZip(stream, request)`, re-render the view with the restored Request (bracket + platform + sections).
    Keep the primer group together; use the same SameOriginRequestValidator / antiforgery middleware chain as the other POST actions (no new exemption). Touch only the inserted lines.

    In `_DeckToolTabs.cshtml`: extend `analyzeActive` with `or DeckPageTab.DeckPrimer` and add the `<a>` Deck Primer link as the 4th Analyze peer (href `~/deck-primer`, `is-active` when Model == DeckPageTab.DeckPrimer). Match the existing link markup. Touch only the inserted lines.
  </action>
  <verify>
    <automated>"/mnt/c/Program Files/dotnet/dotnet.exe" build DeckFlow.Web/DeckFlow.Web.csproj -warnaserror:CS1591 2>&1 | grep -E "Build succeeded|error" | head</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE "HttpGet\(\"/deck-primer\"\)|HttpPost\(\"/deck-primer\"\)|/deck-primer/download|/deck-primer/upload" DeckFlow.Web/Controllers/DeckController.cs` == 4
    - `grep -n "IDeckPrimerPacketService" DeckFlow.Web/Controllers/DeckController.cs` matches field + ctor param
    - `grep -n "PromptTextsByPlatform" DeckFlow.Web/Controllers/DeckController.cs` matches BOTH the selected-platform display mapping AND the all-variant zip sourcing (HIGH-2)
    - `grep -n "BuildPrimerZip\|LoadPrimerFromZip" DeckFlow.Web/Controllers/DeckController.cs` both present (download + upload)
    - `grep -n "DeckPrimer" DeckFlow.Web/Views/Shared/_DeckToolTabs.cshtml` matches the nav link + analyzeActive extension
    - The POST actions carry `[ValidateAntiForgeryToken]` (CSRF parity with other POSTs)
    - `dotnet build DeckFlow.Web -warnaserror:CS1591` reports Build succeeded, 0 errors
  </acceptance_criteria>
  <done>The primer GET/POST/download/upload actions exist and wire IDeckPrimerPacketService + PacketArtifactStore; the page shows the selected-platform prompt and the download zips all enabled variants; the Deck Primer tab appears as the 4th Analyze peer; Web builds clean.</done>
</task>

<task type="auto">
  <name>Task 2: DeckPrimer.cshtml page (+ reused _AiSelector + per-bracket presets) + primer-selection.ts + site-common.css</name>
  <files>DeckFlow.Web/Views/Deck/DeckPrimer.cshtml, DeckFlow.Web/wwwroot/ts/primer-selection.ts, DeckFlow.Web/wwwroot/css/site-common.css</files>
  <read_first>
    - DeckFlow.Web/Views/Deck/DeckAnalysis.cshtml (FULL — page top, partials, _AiSelector usage, WorkflowStepTabs, PasteWarningBytes indicator 478-479, download/upload form actions 87/106 + data-* hooks)
    - DeckFlow.Web/Views/Shared/_AiSelector.cshtml (FULL — the partial to reuse as-is; understand its Gemini gating so you don't duplicate it)
    - DeckFlow.Web/wwwroot/ts/kb-selection.ts (FULL — IIFE, localStorage try/catch 92-135, hidden-field inject 382-400, DeckFlow namespace export 725-727)
    - DeckFlow.Web/Models/PrimerSectionCatalog.cs (Groups/AllSections/GetPresetForBracket/gate sets/NormalizeSelections from 31-02)
    - DeckFlow.Web/wwwroot/css/site-common.css (existing layout classes / conventions to extend)
    - DeckFlow.Web/Models/CommanderBracketCatalog.cs (bracket Options + values — iterate to emit per-bracket presets)
    - .planning/phases/31-deck-primer-generator/31-PATTERNS.md (DeckPrimer.cshtml + primer-selection.ts + site-common.css sections)
  </read_first>
  <action>
    Create `DeckFlow.Web/Views/Deck/DeckPrimer.cshtml` (`@model DeckFlow.Web.Models.DeckPrimerViewModel`):
      - Title "Deck Primer"; hero + page-lede ("Generate a paste-ready deck primer prompt for ChatGPT, Claude, or Gemini."); `@await Html.PartialAsync("_BusyIndicator")`; `@await Html.PartialAsync("_DeckToolTabs", Model.ActiveTab)`.
      - WorkflowStepTabs (3 steps: Deck / Build Primer / Results) via _WorkflowStepTabs, mirroring DeckAnalysis.
      - Step 1: import flow (URL/paste) bound to DeckPrimerRequest.DeckSource/DeckInputSource, mirroring DeckAnalysis import markup (PRM-02).
      - AI target: `@await Html.PartialAsync("_AiSelector", Model.Request.TargetAiPlatform)` — REUSE the existing partial as-is (HIGH-3). Do NOT build a custom radio group; do NOT force-expose Gemini. The partial already hides Gemini unless DECKFLOW_GEMINI_ENABLED.
      - Step 2: bracket `<select>` bound to TargetCommanderBracket from CommanderBracketCatalog.Options. PER-BRACKET PRESETS (D-3/MED): emit EACH bracket's own preset so first-visit switching to a DIFFERENT bracket applies THAT bracket's preset. Use ONE of:
          (a) per-`<option>` `data-preset-ids="@Html.Raw(JsonSerializer.Serialize(PrimerSectionCatalog.GetPresetForBracket(option.Value)))"` on each option, OR
          (b) a single serialized bracket→preset-ids MAP attribute on the select, e.g. `data-bracket-presets='@Html.Raw(JsonSerializer.Serialize(brackets.ToDictionary(b => b.Value, b => PrimerSectionCatalog.GetPresetForBracket(b.Value))))'`.
        Also emit the gate sets (CedhOnlySectionIds/CasualOnlySectionIds) as data attributes so the TS seeds/gates without a round-trip. Do NOT emit a single global `data-preset-ids` for only the initial bracket — that was the MED defect.
      - Section catalog: iterate PrimerSectionCatalog.Groups, rendering each group as a collapsible `<details>` with a `<summary>` containing the group Label and a `.primer-group__badge` span (PRM-11). Inside each group, render each PrimerSectionEntry as a checkbox (name `SelectedSectionIds`, value = entry.Id) + Title + a help-text toggle/reveal showing entry.HelpText (PRM-12). Mark cEDH-only / casual-only sections with `data-bracket-gate` so the TS can disable/hide them per bracket; for the no-JS path render the server preset (for the GET default bracket) as the default-checked set.
      - Step 3 / size indicator: render PrimerPromptText (the selected-platform prompt) when present + the PasteWarningBytes byte-count warning (Encoding.UTF8.GetByteCount(Model.PrimerPromptText) vs aiPlatform.PasteWarningBytes). Download/upload form actions to `~/deck-primer/download` and `~/deck-primer/upload` reusing the existing data-* JS hooks.
      - `<script src="@Url.Content("~/js/primer-selection.js")"></script>` near the bottom.

    Create `DeckFlow.Web/wwwroot/ts/primer-selection.ts` (IIFE, 'use strict', strict TS, no implicit any) mirroring kb-selection.ts:
      - `const PRIMER_SECTIONS_KEY_PREFIX = 'deckflow.primer.sections.';` (+ bracket value).
      - `loadSectionsForBracket(bracket)` / `saveSectionsForBracket(bracket, ids)` with try/catch (mobile private-browsing throw).
      - Read the PER-BRACKET preset for the current bracket from the data attribute (per-`<option>` `data-preset-ids` of the selected option, OR the bracket→preset map keyed by the current bracket). `presetForBracket(bracket)` resolves the correct preset for ANY bracket, not just the initial one.
      - On bracket `change`: call loadSectionsForBracket(newBracket); if non-empty restore the saved set (user edits stick); if empty (first visit to THAT bracket) apply `presetForBracket(newBracket)` (D-3 — that bracket's OWN preset, not the initial bracket's). After ANY toggle, save the current checked set under the current bracket key. ALWAYS enforce bracket gating (strip cEDH-only when bracket != cEDH, casual-only when bracket == cEDH) before save and before form submit, disabling/hiding gated checkboxes in the DOM.
      - `updateGroupBadge(groupId)` updates each `.primer-group__badge` to "N/M sections selected" after every checkbox change (PRM-11).
      - hidden-field inject for SelectedSectionIds on submit (progressive enhancement) + DeckFlow namespace export + DOMContentLoaded init.

    In `DeckFlow.Web/wwwroot/css/site-common.css` (NOT site.css/themes): add the primer layout classes — `.primer-group` (details), `.primer-group__badge` (the count badge), section row layout, and the help-text reveal. Use existing token variables; add no theme-specific colors here. Touch only appended rules.

    LF endings on all three files. Do NOT stage `wwwroot/js/primer-selection.js` (gitignored). Allman/2-space per file-type convention.
  </action>
  <verify>
    <automated>"/mnt/c/Program Files/dotnet/dotnet.exe" build DeckFlow.Web/DeckFlow.Web.csproj 2>&1 | grep -E "Build succeeded|error" | head; test -f DeckFlow.Web/wwwroot/js/primer-selection.js && echo TS-COMPILED || (echo TS-NOT-COMPILED; exit 1)</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "_AiSelector" DeckFlow.Web/Views/Deck/DeckPrimer.cshtml` matches (reuses the partial as-is — HIGH-3; no bespoke AI radio group)
    - `grep -n "PrimerSectionCatalog.Groups\|primer-group__badge\|HelpText" DeckFlow.Web/Views/Deck/DeckPrimer.cshtml` matches group iteration + badge + help text (PRM-04/11/12)
    - Per-bracket presets emitted: `grep -nE "data-preset-ids|data-bracket-presets" DeckFlow.Web/Views/Deck/DeckPrimer.cshtml` matches AND the preset is produced per bracket (GetPresetForBracket called inside a bracket loop / map, NOT once for only the initial bracket) (D-3/MED)
    - `grep -n "deckflow.primer.sections." DeckFlow.Web/wwwroot/ts/primer-selection.ts` matches the per-bracket key (PRM-10)
    - The TS resolves the preset for the bracket being switched TO (a `presetForBracket(bracket)`-style lookup), not a single global preset — manual check that first-visit switch to a different bracket applies that bracket's preset (D-3/MED)
    - `grep -nE "try \{|catch" DeckFlow.Web/wwwroot/ts/primer-selection.ts` shows the try/catch localStorage guard
    - `grep -n "primer-group" DeckFlow.Web/wwwroot/css/site-common.css` matches (layout in site-common, not site.css)
    - The MSBuild TS target compiled `wwwroot/js/primer-selection.js` (TS-COMPILED) AND `git status` does NOT stage it (gitignored)
    - `dotnet build DeckFlow.Web` reports Build succeeded, 0 errors
  </acceptance_criteria>
  <done>The primer page renders import + reused _AiSelector + bracket + 5-group collapsible 31-section catalog with help text + badges + size warning; the bracket selector exposes per-bracket presets and primer-selection.ts applies the correct bracket's preset on first visit (incl. switching to a different bracket) + persists per-bracket selections + gating + badges; layout CSS lives in site-common.css; compiled JS is not committed.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>The full Deck Primer page wired end-to-end: the 4th Analyze nav tab, decklist import (URL/paste), the reused _AiSelector AI target, per-bracket-preset section seeding, the 31-section catalog in 5 collapsible groups with help text and selected-count badges, per-bracket localStorage persistence, the paste-size warning, and the zip download/upload round-trip (all enabled variants in the zip).</what-built>
  <how-to-verify>
    Start the dev server yourself (the harness will not auto-launch it), then in a browser:
    1. Visit the app, open the Analyze nav group, confirm a "Deck Primer" tab appears and routes to /deck-primer.
    2. Load a decklist via URL and via paste; confirm Step 1 accepts both.
    3. Confirm the AI selector shows ChatGPT + Claude (and Gemini ONLY if DECKFLOW_GEMINI_ENABLED is on in your env) — it is the shared _AiSelector, not a bespoke control.
    4. PER-BRACKET PRESET CHECK (D-3/MED): on a fresh browser profile (clear localStorage), land on the page (default bracket), then switch DIRECTLY to a DIFFERENT bracket you have not visited — confirm THAT bracket's own preset auto-applies (not the initial bracket's). Select bracket 5 (cEDH): confirm the cEDH preset auto-checks sections and cEDH-only #24/#25 are available; switch to Core: confirm #24/#25 disappear/disable and casual-only #26 appears with the Casual preset.
    5. Toggle a few sections, switch brackets and back: confirm your custom toggles for each bracket are remembered (localStorage, D-3/PRM-10).
    6. Collapse a group: confirm the header badge shows "N/M sections selected" (PRM-11). Hover/expand a section's help text (PRM-12).
    7. Build the primer for ChatGPT and Claude (and Gemini if enabled); confirm the page shows the SELECTED platform's prompt as paste-ready; for a large cEDH primer on Gemini (if enabled), confirm the size warning and/or the Gemini trim disclosure appears (D-4).
    8. Download the zip, then upload it back: confirm bracket + AI platform + section selections are restored, and the zip contains a prompt file per ENABLED platform (PRM-09/SC5/HIGH-2).
    9. Repeat the page render at TWO viewports (desktop ~1280px and mobile ~390px) and capture screenshots; confirm the collapsible groups, badges, and section list are usable at both (UI-phase visual-verify requirement).
  </how-to-verify>
  <resume-signal>Type "approved" with the 2-viewport screenshots attached, or describe the layout/behavior issues to fix.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser form -> POST /deck-primer | Untrusted deck text/URL + bracket + platform + section ids; antiforgery + SameOrigin enforced; normalized server-side |
| uploaded zip -> POST /deck-primer/upload | Untrusted archive; LoadPrimerFromZip allowlist + size caps (31-05) |
| localStorage section ids -> form submit | Client-stored ids; re-validated by NormalizeSelections server-side; rendered values HTML-encoded by Razor |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-18 | Tampering | CSRF on POST /deck-primer(/download/upload) | mitigate | Actions carry [ValidateAntiForgeryToken] and run in the existing SameOriginRequestValidator middleware chain — no new exemption (parity with other workflow POSTs) |
| T-31-19 | Tampering | XSS via section labels/help text, restored deck name, or the serialized per-bracket preset data attributes rendered in Razor | mitigate | Dynamic text values rendered through Razor's default HTML encoding; the preset/gate data attributes are JsonSerializer-serialized catalog-controlled IDs (not free user text) emitted into quoted attributes; no Html.Raw on user-supplied text |
| T-31-20 | Tampering | localStorage-injected/cross-bracket section ids submitted | mitigate | TS gates client-side AND PrimerSectionCatalog.NormalizeSelections strips unknown/gated ids server-side (31-02/31-03); localStorage is not a trust boundary |
| T-31-21 | Tampering | Crafted zip uploaded to /deck-primer/upload | mitigate | LoadPrimerFromZip -> ReadEntries(PrimerAllowedNames) rejects unlisted entries + path traversal + zip bombs (31-05) |
| T-31-22 | Information Disclosure | Service exception surfacing a stack trace on the page | mitigate | Controller catches InvalidOperationException -> friendly ErrorMessage; catch Exception -> log + generic message; no raw exception text to the view |
| T-31-SC | Tampering | npm/pip/cargo installs | accept | Zero package installs (RESEARCH §Package Legitimacy Audit: not applicable); TS compiles via the existing MSBuild target, no npm add |
</threat_model>

<verification>
- `dotnet build DeckFlow.Web` clean (TS compiles via MSBuild target)
- Deck Primer tab present + 4 primer routes wired (greps); page reuses _AiSelector (HIGH-3); selected-platform prompt + all-variant zip mapping present (HIGH-2)
- per-bracket presets emitted + per-bracket localStorage key + try/catch present in primer-selection.ts (D-3/MED)
- Layout CSS in site-common.css; compiled JS NOT staged (gitignored)
- 2-viewport human visual-verify checkpoint approved
</verification>

<success_criteria>
PRM-02 (4th tab + import flow + reused _AiSelector), PRM-03 (per-bracket preset applied — incl. first-visit switch to a different bracket — + bracket-gated sections), PRM-04 (toggle 31 sections in 5 collapsible groups), PRM-10 (per-bracket localStorage persistence across visits), PRM-11 (collapsed-group selected-count badges), and PRM-12 (per-section help text) are delivered and visually verified at two viewports. The page shows the selected-platform prompt; the download/upload zip round-trip carries all enabled variants and restores bracket + platform + sections (SC5/HIGH-2). Gemini is exposed only via the existing _AiSelector gate (HIGH-3). Layout CSS is in site-common.css; compiled JS is not committed.
</success_criteria>

<output>
Create `.planning/phases/31-deck-primer-generator/31-06-SUMMARY.md` when done.
</output>
