---
phase: 82-refactor-review-sweep-ui-baseline-audit
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - tasks/UI-REVIEW.md
autonomous: true
requirements: [UIAUDIT-01]
must_haves:
  truths:
    - "A baseline 6-pillar UI audit has been re-run against the current live/rendered site"
    - "tasks/UI-REVIEW.md carries a per-pillar score for all 6 pillars and an overall /24"
    - "The concrete gap to >=20/24 is enumerated as scoped per-pillar fixes"
    - "Each enumerated gap fix is handed to its owning visual-work phase (Phase 84 theme-token / Phase 85 naming), with any out-of-84/85-scope gap explicitly flagged for assignment — none silently parked on Phase 86, which only re-scores — so fixes are scoped up front, not discovered by Phase 86"
  artifacts:
    - path: "tasks/UI-REVIEW.md"
      provides: "Scored 6-pillar baseline audit with per-pillar breakdown, overall /24, and an enumerated gap-to-20 handoff to Phases 84/85"
      contains: "Overall:"
  key_links:
    - from: "tasks/UI-REVIEW.md"
      to: "Phase 84 / Phase 85 visual work"
      via: "enumerated gap-to-20 items tagged by owning phase"
      pattern: "Phase 84|Phase 85"
---

<objective>
Re-run the 6-pillar UI audit against the current DeckFlow site and produce a scored
`tasks/UI-REVIEW.md` with a per-pillar breakdown, an overall /24, and an enumerated,
phase-tagged list of the concrete fixes needed to reach >=20/24.

Purpose: The last audit scored 16/24 at v1.0 (2026-04-30) and was never re-measured.
Taking the baseline NOW — early in the cycle — lets the Phase 84 theme-token migration
and Phase 85 naming cleanup absorb the concrete visual fixes as scoped work, instead of
Phase 86 discovering them at re-score time. This satisfies UIAUDIT-01. This plan produces
the BASELINE only; the final re-score to clear >=20/24 is Phase 86 (UIAUDIT-02).

Output: refreshed `tasks/UI-REVIEW.md` with current per-pillar scores + gap-to-20 handoff.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@./CLAUDE.md

# Prior baseline (2026-04-30, 16/24) for delta comparison:
@tasks/UI-REVIEW.md

# Known color-pillar debt this audit will re-measure (owned by later phases — the audit
# SCORES it, it does not FIX it here):
#  - --accent-strong overloaded across link/danger/focus/cta roles -> Phase 84 (THEME)
#  - chatgpt-* identifiers predate the AI-agnostic rename -> Phase 85 (AICLEAN)
</context>

<interfaces>
<!-- UI testing MUST NOT open a browser on the Windows host (CLAUDE.md). -->
<!-- Start the headless server, THEN drive checks against it: -->
<!--   scripts/run-web-test.sh  (exports DECKFLOW_DISABLE_AUTO_BROWSER=true, serves http://localhost:5173) -->
<!--   npx --no-install playwright test   (WSL can run Playwright against the headless server) -->
<!-- Do NOT use run-web.sh / run-web-uat.sh for audit (they may auto-launch a Windows browser). -->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Run the 6-pillar UI audit and produce the scored baseline</name>
  <files>tasks/UI-REVIEW.md</files>
  <read_first>
    - tasks/UI-REVIEW.md (the 2026-04-30 16/24 baseline — the prior scores + findings this run compares against)
    - ./CLAUDE.md ("UI testing must NEVER open a browser on Windows"; theme CSS rules — layout in site-common.css, tokens in each theme :root)
    - DeckFlow.Web/wwwroot/css/site-common.css (layout CSS — pillar 5 Spacing / pillar 4 Typography evidence)
    - DeckFlow.Web/wwwroot/css/site.css (token :root — pillar 3 Color, type/spacing scale evidence)
  </read_first>
  <action>
    Start the headless test server with `scripts/run-web-test.sh` (sets
    DECKFLOW_DISABLE_AUTO_BROWSER=true, serves http://localhost:5173) so no Windows browser
    window opens. Invoke the gsd-ui-auditor agent against the running site with output path
    `tasks/UI-REVIEW.md`, auditing against the abstract 6-pillar standards (no UI-SPEC.md exists).
    Capture screenshots at both desktop (1280x900) and a mobile viewport via
    `npx --no-install playwright test` where useful; if the server is unavailable, fall back to
    the code-review audit mode (CSS token audit, string/label audit, state-handling check) and
    note in the artifact that visual screenshots were not captured.
    The auditor scores each of the 6 pillars 1-4 (Copywriting, Visuals, Color, Typography,
    Spacing, Experience Design), records the overall /24, and lists at least one specific
    finding justifying each pillar score. Overwrite the prior baseline; keep the audit date +
    "prior: 16/24 (2026-04-30)" delta line so the movement is visible.
  </action>
  <acceptance_criteria>
    - `tasks/UI-REVIEW.md` contains a per-pillar score for all 6 named pillars: `grep -ciE 'Copywriting|Visuals|Color|Typography|Spacing|Experience' tasks/UI-REVIEW.md` returns >= 6.
    - The file states an overall score out of 24: `grep -qiE 'Overall:.*\/24' tasks/UI-REVIEW.md`.
    - Every pillar has at least one specific finding justifying its score (gsd-ui-auditor contract; no bare score rows).
    - The audit run date is refreshed to the current date (not the stale 2026-04-30 baseline) and a "prior: 16/24" delta line is present.
  </acceptance_criteria>
  <verify>
    <automated>test -f tasks/UI-REVIEW.md && grep -qiE 'Overall:.*/24' tasks/UI-REVIEW.md && [ "$(grep -ciE 'Copywriting|Visuals|Color|Typography|Spacing|Experience' tasks/UI-REVIEW.md)" -ge 6 ]</automated>
  </verify>
  <done>tasks/UI-REVIEW.md re-scored against the current site with all 6 pillar scores, an overall /24, per-pillar justifying findings, and a refreshed audit date + prior-score delta.</done>
</task>

<task type="auto">
  <name>Task 2: Enumerate the gap-to-20 and hand each fix to Phase 84/85 (flag any out-of-84/85-scope gap explicitly)</name>
  <files>tasks/UI-REVIEW.md</files>
  <read_first>
    - tasks/UI-REVIEW.md (the scored baseline from Task 1 — the input to the gap enumeration)
    - .planning/ROADMAP.md (Phase 84 THEME / Phase 85 AICLEAN scope = the VISUAL-WORK phases the gap is handed to; Phase 86 = the RE-SCORE/closeout step, NOT a fix-owner — see Phase 82 success-criterion 5: gap handed to 84/85 "not left for Phase 86 to discover")
  </read_first>
  <action>
    Append a `## Gap to >=20/24 — Handoff to Phases 84/85` section to tasks/UI-REVIEW.md.
    For each pillar scoring below its target, enumerate the concrete, specific fix(es) that would
    raise the score and hand each to a downstream VISUAL-WORK phase: color-token / --accent-strong
    corrections -> `Phase 84`; chatgpt-* identifier-driven items -> `Phase 85`. Per ROADMAP Phase 82
    success-criterion 5 the gap is scoped to Phases 84/85 so they execute known visual work — it is
    NOT parked on Phase 86 (Phase 86 only RE-SCORES after 84/85 land and must not be handed
    discoverable fixes). If a pillar gap genuinely falls outside BOTH Phase 84's (theme-token) and
    Phase 85's (chatgpt rename) declared scope, do NOT silently defer it to 86: record it under an
    explicit `### Out of Phase 84/85 scope — needs assignment` subsection with the concrete fix, so
    the scope tension is surfaced NOW for the operator to slot into a phase — enumerated, not
    discovered later. State the target overall score after the enumerated fixes land (must be
    >= 20/24) and the per-pillar point deltas that get there. Do NOT apply any CSS/markup change in
    this plan — enumeration only; this is a read-only baseline measurement (byte-neutral: the only
    file written is tasks/UI-REVIEW.md).
  </action>
  <acceptance_criteria>
    - tasks/UI-REVIEW.md contains a gap section: `grep -qiE 'Gap to.*20|Handoff to Phase' tasks/UI-REVIEW.md`.
    - The enumerated fixes are handed to the visual-work phases: `grep -qiE 'Phase 84|Phase 85' tasks/UI-REVIEW.md` (fixes owned by 84/85, not 86).
    - No concrete gap fix is assigned to Phase 86 as its owner (86 is re-score only): any pillar gap that fits neither 84 nor 85 appears under the `### Out of Phase 84/85 scope — needs assignment` subsection instead of a `Phase 86` owner tag. (Manual check — read each gap row's owner.)
    - A target-after-fixes overall score of >= 20/24 is stated.
    - No tracked source CSS/markup file was modified by this plan (baseline is measurement-only): `git diff --name-only HEAD -- DeckFlow.Web/wwwroot/css DeckFlow.Web/Views` prints nothing. (Diff against the narrow CSS + Views paths a visual audit would tempt editing — not whole-worktree `git status`, nor the broad wwwroot tree, both sensitive to unrelated local state / gitignored build output.)
  </acceptance_criteria>
  <verify>
    <automated>grep -qiE 'Gap to.*20|Handoff to Phase' tasks/UI-REVIEW.md && grep -qiE 'Phase 84|Phase 85' tasks/UI-REVIEW.md && { git diff --name-only HEAD -- DeckFlow.Web/wwwroot/css DeckFlow.Web/Views | grep -q . && exit 1 || exit 0; }</automated>
  </verify>
  <done>The gap-to-20 is enumerated as concrete per-pillar fixes handed to Phase 84/85 (any out-of-84/85-scope gap flagged for assignment, not parked on Phase 86, which only re-scores), with a stated >=20/24 target; no source file was touched. UIAUDIT-01 satisfied.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none introduced) | Read-only audit over rendered UI + writes one planning doc (tasks/UI-REVIEW.md). No new inputs, endpoints, auth, or data flows. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-82-02 | Information Disclosure | Local test server started with placeholder admin creds during audit | accept | scripts/run-web-test.sh uses documented local-only placeholder creds (never prod); server is localhost-bound and torn down after the audit. No secret exposure. |
| T-82-SC | Tampering | npm/pip/cargo installs | mitigate | Uses `npx --no-install playwright` (no install; existing node_modules only). No new packages — no supply-chain surface. |

Behavior-neutral, read-only baseline audit: no source CSS/markup/TS changes, no new inputs,
endpoints, auth, or data flows. Threat surface is near-zero; the mitigations of record are the
no-source-change discipline and the localhost-only placeholder-cred test server.
</threat_model>

<verification>
- tasks/UI-REVIEW.md re-scored with all 6 pillar scores + overall /24 + justifying findings.
- Gap-to-20 enumerated, fixes handed to Phase 84/85 (out-of-84/85-scope gaps flagged for assignment, not parked on 86), target >= 20/24 stated.
- No source CSS/markup/TS file modified (measurement-only).
</verification>

<success_criteria>
- UIAUDIT-01 satisfied: baseline 6-pillar audit re-run and scored with a per-pillar breakdown;
  the gap to >=20/24 enumerated and handed to Phases 84/85 as scoped work (any out-of-84/85-scope gap flagged for assignment; Phase 86 only re-scores).
</success_criteria>

<output>
Create `.planning/phases/82-refactor-review-sweep-ui-baseline-audit/82-02-SUMMARY.md` when done.
</output>
