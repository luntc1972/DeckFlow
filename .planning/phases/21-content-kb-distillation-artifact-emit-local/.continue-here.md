---
context: phase
phase: 21-content-kb-distillation-artifact-emit-local
task: 0
total_tasks: 0
status: planning-complete-pre-execution
last_updated: 2026-05-27T18:34:05.022Z
---

<current_state>
Phase 21 (Content KB Distillation + Artifact Emit, local) is FULLY PLANNED and Codex-reviewed GREEN. NOTHING executed yet — pre-execution pause. Full pipeline done this session: discuss → ai-integration (AI-SPEC) → research → pattern-map → plan → Codex review convergence (RED→RED→GREEN).

4 plans / 2 waves committed on v1.4 (commits 1d84e08 → cac740c), NOT pushed. Next action is `/gsd-execute-phase 21`.
</current_state>

<completed_work>
- 21-CONTEXT.md — 13 locked decisions (D-01..D-13)
- 21-AI-SPEC.md — system=Extraction, OpenAI .NET SDK 2.10 ChatClient + strict json_schema, 6 eval dims (4 code-gating E1-E4 + 2 human/LLM-judge E5-E6)
- 21-RESEARCH.md — 7 open questions resolved (timestamps, distill-status, ledger, run-record, verb wiring, source mgmt, artifact writer)
- 21-PATTERNS.md — 13 new/changed files, all with in-repo analogs
- 21-01..21-04-PLAN.md — 4 plans, 2 waves
- Reviews: Claude plan-checker PASS; Codex RED→RED→GREEN (3 HIGH fixed across 2 convergence passes)
</completed_work>

<remaining_work>
- /gsd-execute-phase 21 (the ONLY remaining step). Codex implements per CLAUDE.md cross-AI execution; Claude reviews each plan's output.
  - Wave 1 (parallel, disjoint files): 21-01 LlmDistillationService + DistillationSchemas; 21-02 LlmSpendLedger; 21-03 ContentArtifactWriter + ContentVideoStore/ContentSourceStore edits + content_distill_status table + .gitignore checkpoint.
  - Wave 2: 21-04 distill orchestrator + `distill`/`content-source-set-enabled` verbs (depends on all of Wave 1).
</remaining_work>

<decisions_made>
- gpt-4o-mini, THREE separate strict-json_schema calls (summary/clips/tags) — D-02.
- SEPARATE llm_spend_ledger; spend recorded PER-CALL immediately before the next call (Codex HIGH-1; mirrors Phase 20 CR-01).
- NEW content_distill_status table (CREATE TABLE IF NOT EXISTS — schema-safe; distilled|skipped_over_cap|failed) supersedes derived-state idempotency; satisfies SC5 literal + durable D-12.
- source-scoped ListVideosPendingDistillAsync(sourceId) — Codex HIGH-2.
- SC4 amended to enable/disable-only (edit = add-new + disable-old); SC5 implemented literally (--dry-run + per-video skipped_over_cap).
- Artifacts at MTG_DATA_DIR/content-kb/{source-slug}/{video_id}.md, gitignored; best-effort timestamps (timestamp_s=0 storage sentinel, omit [mm:ss] when unknown, render from in-memory clips).
</decisions_made>

<blockers>
None. Three planned autonomous:false checkpoints will pause during execution (not blockers, expected gates) — see Human Actions below.
</blockers>

## Required Reading (in order)
1. `.planning/phases/21-content-kb-distillation-artifact-emit-local/21-CONTEXT.md` — locked decisions
2. `.planning/phases/21-content-kb-distillation-artifact-emit-local/21-AI-SPEC.md` — framework + impl syntax (§3/4/4b) + eval (§5/6/7)
3. `.planning/phases/21-content-kb-distillation-artifact-emit-local/21-RESEARCH.md` — open-question resolutions
4. `.planning/phases/21-content-kb-distillation-artifact-emit-local/21-PATTERNS.md` — analog files per new/changed file
5. The 4 PLAN.md files (21-01..21-04)
6. `CLAUDE.md` — cross-AI delegation (Codex codes, Claude reviews), no new NuGet, no reformat, xUnit

## Critical Anti-Patterns (do NOT repeat these)
- BATCHED SPEND RECORDING → undercounts cap on partial videos. Record each LLM call's usage to the ledger IMMEDIATELY after it returns, before the next call (encoded in 21-04). Same class as Phase 20 CR-01.
- GLOBAL pending-distill query → wrong-source emit. Query is source-scoped; orchestrator filters per enabled source.
- ALTERING transcript_status CHECK / new enum value → existing dbs reject it. NO migration mechanism (CREATE TABLE IF NOT EXISTS only). New tables are fine; ALTER of existing CHECK is not.
- Adding a NuGet package for ChatClient → it is a NEW NAMESPACE (using OpenAI.Chat) from the already-present OpenAI 2.10 assembly. Zero new packages.

## Infrastructure State
- DeckFlow.Core depends on OpenAI 2.10.0 (powers Whisper AudioClient). ChatTokenUsage.InputTokenCount/OutputTokenCount confirmed present in installed DLL.
- UAT db: artifacts/uat-content-kb.db — 5 enabled sources, 10 videos, all transcript_status=captions (from Phase 20 UAT). Ready as the 10-video distill reference/eval set.
- OPENAI_API_KEY NOT set in shell — required for the 21-04 UAT checkpoint (and any live distill run).
- Untracked unrelated stragglers: .planning/phases/19-*/19-PATTERNS.md, .planning/phases/27-*/27-RESEARCH.md — leave them.

## Human Actions Pending (the 3 execution checkpoints, all autonomous:false)
1. 21-02 checkpoint:decision — confirm gpt-4o-mini price constants + DECKFLOW_LLM_MONTHLY_CAP_USD default ($15) before ledger cost code is locked (A1/A2).
2. 21-03 checkpoint:human-action — approve adding `content-kb/` to `.gitignore` (Do-Not-Modify file per CLAUDE.md) (A4).
3. 21-04 checkpoint:human-verify — run end-to-end distill UAT + E5/E6 archetype/bracket/thesis human sampling on the 10-video UAT db (needs OPENAI_API_KEY).

<context>
Phase 20 was shipped + UAT-passed + pushed earlier this same session (per-source harvest isolation + opt-in Whisper + skipped_no_captions). Then started Phase 21 from scratch through the full GSD AI-phase pipeline. Everything is committed but unpushed on v1.4. The plans are execution-ready; resuming means kicking off /gsd-execute-phase 21 and babysitting the 3 checkpoints.
</context>

<next_action>
Start with: /gsd-execute-phase 21 — route to Codex per CLAUDE.md cross-AI execution. Expect the 21-02 and 21-03 checkpoints during Wave 1 and the 21-04 UAT checkpoint after Wave 2; surface each to the user.
</next_action>
