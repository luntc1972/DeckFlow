---
phase: 48-ui-audit-remediation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - DeckFlow.Web/wwwroot/css/site.css
  - DeckFlow.Web/wwwroot/css/site-jeskai.css
  - DeckFlow.Web/wwwroot/css/site-abzan.css
  - DeckFlow.Web/wwwroot/css/site-bant.css
  - DeckFlow.Web/wwwroot/css/site-esper.css
  - DeckFlow.Web/wwwroot/css/site-grixis.css
  - DeckFlow.Web/wwwroot/css/site-jund.css
  - DeckFlow.Web/wwwroot/css/site-mardu.css
  - DeckFlow.Web/wwwroot/css/site-naya.css
  - DeckFlow.Web/wwwroot/css/site-nyx.css
  - DeckFlow.Web/wwwroot/css/site-planeswalker-dark.css
  - DeckFlow.Web/wwwroot/css/site-sultai.css
  - DeckFlow.Web/wwwroot/css/site-commander-table.css
  - DeckFlow.Web/wwwroot/css/site-azorius.css
  - DeckFlow.Web/wwwroot/css/site-boros.css
  - DeckFlow.Web/wwwroot/css/site-dimir.css
  - DeckFlow.Web/wwwroot/css/site-golgari.css
  - DeckFlow.Web/wwwroot/css/site-gruul.css
  - DeckFlow.Web/wwwroot/css/site-izzet.css
  - DeckFlow.Web/wwwroot/css/site-orzhov.css
  - DeckFlow.Web/wwwroot/css/site-rakdos.css
  - DeckFlow.Web/wwwroot/css/site-selesnya.css
  - DeckFlow.Web/wwwroot/css/site-simic.css
  - DeckFlow.Web/wwwroot/css/site-temur.css
autonomous: true
requirements: [UIR-02]

must_haves:
  truths:
    - "Smallest UI text (helper/footer) renders at >= 12.75px in every theme (--fs-xs = 0.85rem = 12.75px at the 15px root)"
    - "Card surfaces visibly separate from page background in every theme"
    - "Helper/muted text meets WCAG AA (>= 4.5:1) against the page background in every theme"
    - "site-commander-table.css small text no longer renders at the old hardcoded 0.75rem floor"
  artifacts:
    - path: "DeckFlow.Web/wwwroot/css/site.css"
      provides: "Classic theme :root tokens (--fs-xs raised to 0.85rem, --muted darkened, --panel/--bg/--line surface delta widened)"
      contains: "--fs-xs:"
    - path: "DeckFlow.Web/wwwroot/css/site-commander-table.css"
      provides: "Full-fork :root gains --fs-xs (0.85rem) so shared site-common var(--fs-xs) selectors resolve; hardcoded 0.75rem small-text literals re-pointed to var(--fs-xs)"
      contains: "--fs-xs:"
  key_links:
    - from: "every full-fork theme :root"
      to: "rendered helper/footer text size"
      via: "--fs-xs token"
      pattern: "--fs-xs:\\s*0\\.85rem"
    - from: "every theme :root"
      to: "card-vs-bg separation"
      via: "--panel / --bg / --line tokens"
      pattern: "--panel:"
---

<objective>
Remediate the three token-only audit findings that lift the score toward >= 20/24. (UIR-01 — the deployed-site audit + final re-score — is owned by Plan 03; this plan does NOT close UIR-01.)

- F3 [Typography]: raise `--fs-xs` to `0.85rem` (= 12.75px exactly at the 15px root) so the smallest text clears the >= 12.75px floor.
- F5 [Color]: darken `--muted` to comfortable WCAG AA on small helper text.
- F2 [Color, token half]: widen the `--panel` vs `--bg` surface delta and strengthen `--line` so cards lift off the page. (The shadow/elevation half of F2 is layout and lives in Plan 02.)

Purpose: Color (3->4) and Typography (3->4) are the two pillars on the closing path. These are pure design-token changes — no layout, no markup.
Output: Updated `:root` token blocks across all 24 user-selectable themes (13 full-forks + 11 @import overrides). No changes to `site-common.css`, no layout rules added to `site.css`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/48-ui-audit-remediation/48-AUDIT.md
@.planning/phases/48-ui-audit-remediation/48-CONTEXT.md
@DeckFlow.Web/wwwroot/css/site.css

<theme_architecture>
<!-- Verified during planning by grepping ALL site-*.css for `--fs-xs:` and `@import`. Definitive sets below. There are TWO theme architectures plus one special full-fork — handle each correctly. -->

DEFINITIVE SETS (re-grepped 2026-06-16):
  `grep -l -- "--fs-xs:" site*.css`  => the 12 files that DEFINE --fs-xs:
    site.css, site-jeskai.css, site-abzan.css, site-bant.css, site-esper.css,
    site-grixis.css, site-jund.css, site-mardu.css, site-naya.css, site-nyx.css,
    site-planeswalker-dark.css, site-sultai.css
  `grep -l "@import" site*.css`  => the 11 user-selectable @import themes (plus site-common.css, which is NOT a user theme):
    site-azorius.css, site-boros.css, site-dimir.css, site-golgari.css, site-gruul.css,
    site-izzet.css, site-orzhov.css, site-rakdos.css, site-selesnya.css, site-simic.css, site-temur.css
  site-commander-table.css  => NEITHER defines --fs-xs NOR @imports — a standalone full-fork.

FULL-FORK themes (define their own complete `:root`; all define `--muted`, `--panel`, `--bg`, `--line`):
  site.css (Classic), site-jeskai.css, site-abzan.css, site-bant.css, site-esper.css,
  site-grixis.css, site-jund.css, site-mardu.css, site-naya.css, site-nyx.css,
  site-planeswalker-dark.css, site-sultai.css, site-commander-table.css  (13 files)

@IMPORT themes (start with `@import url('site.css');` then override a subset of tokens in their own `:root`).
  They DEFINE `--muted`, `--panel`, `--bg`, `--line` (override these) but DO NOT define `--fs-xs` (they inherit it from site.css via @import):
  site-azorius.css, site-boros.css, site-dimir.css, site-golgari.css, site-gruul.css,
  site-izzet.css, site-orzhov.css, site-rakdos.css, site-selesnya.css, site-simic.css,
  site-temur.css  (11 files)

CRITICAL — site-commander-table.css IS in F3 scope (corrected after peer review):
  - site-common.css defines NO type-scale tokens (grep `--fs-` in site-common.css returns only `font-size: var(--fs-*)` CONSUMERS, e.g. lines 193, 347, 370, 388 — never a DEFINITION).
  - site-commander-table.css does NOT @import and does NOT define `--fs-xs`. So in the shared site-common.css selectors that read `var(--fs-xs)`, the var is UNRESOLVED under the commander-table theme (falls back to the browser default / invalid), and the table's own small text is HARDCODED `0.75rem` (verified: line 225 = pip badge `font-size: 0.75rem;`, line 934 = `.sync-column__status` pill `font-size: 0.75rem;`).
  - Therefore the F3 fix MUST reach commander-table. Chosen approach: ADD `--fs-xs: 0.85rem;` to site-commander-table.css `:root` (so the shared site-common `var(--fs-xs)` selectors resolve correctly under this theme) AND change its two hardcoded `0.75rem` small-text literals (lines 225, 934) to `var(--fs-xs)`.

EDIT SETS for this plan:
  - F3 (--fs-xs = 0.85rem): the 12 fs-xs-defining full-forks (change existing value) + site-commander-table.css (ADD the token to :root + re-point its 2 hardcoded small-text literals). The 11 @import themes are NOT edited for F3 (they inherit 0.85rem from site.css).  => 13 files touched for F3.
  - F5 (--muted): ALL 24 user-selectable themes (13 full-fork incl. commander-table + 11 @import; each has its own tinted --muted value).
  - F2 tokens (--panel / --bg / --line): ALL 24 user-selectable themes.
  - Each theme has DIFFERENT hex values tuned to its palette. Do NOT copy one theme's value into another. Adjust each theme's existing value in the same direction (darker muted, wider panel/bg gap, stronger line) while keeping the theme's hue.
</theme_architecture>

Classic (site.css) current values for reference (do NOT replicate verbatim into other themes):
  --bg: #eceef3; --panel: #fafafa; --muted: #5c6478; --line: #c4c9d6; --fs-xs: 0.75rem;
Commander Table (site-commander-table.css) current values for reference (full-fork; line numbers: :root --bg #f0ebe0 / --panel #faf8f3 / --muted #6b6050 / --line #d4cbb8; hardcoded small-text 0.75rem at lines 225 and 934):
  --bg: #f0ebe0; --panel: #faf8f3; --muted: #6b6050; --line: #d4cbb8;
Root font size: `html { font-size: 15px; }` (site.css line 65) => 0.85rem = 12.75px exactly (clears the >= 12.75px floor); 0.82rem would be only 12.3px and FAIL the floor.
</context>

<tasks>

<task type="auto">
  <name>Task 1: F3 + F5 — set --fs-xs to 0.85rem and darken --muted across all themes (incl. commander-table)</name>
  <files>DeckFlow.Web/wwwroot/css/site.css, site-jeskai.css, site-abzan.css, site-bant.css, site-esper.css, site-grixis.css, site-jund.css, site-mardu.css, site-naya.css, site-nyx.css, site-planeswalker-dark.css, site-sultai.css, site-commander-table.css, site-azorius.css, site-boros.css, site-dimir.css, site-golgari.css, site-gruul.css, site-izzet.css, site-orzhov.css, site-rakdos.css, site-selesnya.css, site-simic.css, site-temur.css</files>
  <read_first>
    - .planning/phases/48-ui-audit-remediation/48-AUDIT.md (F3, F5 + grounded token evidence)
    - DeckFlow.Web/wwwroot/css/site.css (:root token block, lines 1-58; html font-size line 65)
    - DeckFlow.Web/wwwroot/css/site-azorius.css (representative @import theme :root — note no --fs-xs present)
    - DeckFlow.Web/wwwroot/css/site-commander-table.css (full-fork :root lines 1-20; hardcoded 0.75rem small-text at lines 225 and 934)
    - The `<theme_architecture>` block above
  </read_first>
  <action>
    F3 (Typography), 13 files:
    - In each of the 12 FULL-FORK files that already DEFINE --fs-xs (site.css, site-jeskai, site-abzan, site-bant, site-esper, site-grixis, site-jund, site-mardu, site-naya, site-nyx, site-planeswalker-dark, site-sultai), change the `--fs-xs` token value from `0.75rem` to `0.85rem`. Since `html { font-size: 15px }`, 0.85rem = 12.75px exactly, which clears the >= 12.75px floor. (Do NOT use 0.82rem — that is 12.3px and FAILS the floor.)
    - site-commander-table.css (full-fork, currently has NO --fs-xs and HARDCODES its small text to 0.75rem): ADD `--fs-xs: 0.85rem;` to its `:root` (alongside the existing --bg/--panel/--muted/--line near line 3-7) so the shared site-common.css `var(--fs-xs)` selectors resolve correctly under this theme, AND change its two hardcoded small-text literals from `font-size: 0.75rem;` to `font-size: var(--fs-xs);` at line 225 (pip badge) and line 934 (`.sync-column__status`). Do NOT touch the other 0.75rem occurrences in that file — they are padding/gap/margin (spacing), not type size.
    - Do NOT add `--fs-xs` to any of the 11 @import themes — they inherit 0.85rem from site.css.

    F5 (Color), all 24 files: darken the existing per-theme `--muted` value so it reaches >= 4.5:1 (WCAG AA) against that theme's `--bg`. Each theme keeps its own hue family — shift the value darker, do not paste a single shared hex. For the cool/blue-gray family (Classic `#5c6478`, Azorius `#50627c`, etc.) target around `#4b5563` or darker; for warm themes (e.g. commander-table `#6b6050`) shift to a darker warm tone; for dark themes pick the theme-appropriate darker tone. The bar is the measured contrast ratio, not a fixed hex. Per F5 + decision D in 48-CONTEXT (Claude's Discretion on exact token values).

    Touch only the changed token/declaration line(s) in each file (changed-lines format gate). Preserve LF endings. Do not reformat surrounding lines, do not inline attributes, do not re-indent.
  </action>
  <verify>
    <automated>grep -c -- "--fs-xs:   0.85rem" DeckFlow.Web/wwwroot/css/site.css DeckFlow.Web/wwwroot/css/site-jeskai.css DeckFlow.Web/wwwroot/css/site-abzan.css DeckFlow.Web/wwwroot/css/site-bant.css DeckFlow.Web/wwwroot/css/site-esper.css DeckFlow.Web/wwwroot/css/site-grixis.css DeckFlow.Web/wwwroot/css/site-jund.css DeckFlow.Web/wwwroot/css/site-mardu.css DeckFlow.Web/wwwroot/css/site-naya.css DeckFlow.Web/wwwroot/css/site-nyx.css DeckFlow.Web/wwwroot/css/site-planeswalker-dark.css DeckFlow.Web/wwwroot/css/site-sultai.css 2>/dev/null | grep -v ":1$" || echo "all 12 fs-xs full-fork files have --fs-xs: 0.85rem"</automated>
    <automated>grep -q -- "--fs-xs:" DeckFlow.Web/wwwroot/css/site-commander-table.css && echo "commander-table now defines --fs-xs"</automated>
    <automated>grep -v '^#' DeckFlow.Web/wwwroot/css/site-commander-table.css | grep -c "font-size: 0.75rem"</automated>
    <automated>test "$(grep -rl -- "--fs-xs" DeckFlow.Web/wwwroot/css/site-azorius.css DeckFlow.Web/wwwroot/css/site-boros.css DeckFlow.Web/wwwroot/css/site-dimir.css DeckFlow.Web/wwwroot/css/site-golgari.css DeckFlow.Web/wwwroot/css/site-gruul.css DeckFlow.Web/wwwroot/css/site-izzet.css DeckFlow.Web/wwwroot/css/site-orzhov.css DeckFlow.Web/wwwroot/css/site-rakdos.css DeckFlow.Web/wwwroot/css/site-selesnya.css DeckFlow.Web/wwwroot/css/site-simic.css DeckFlow.Web/wwwroot/css/site-temur.css 2>/dev/null | wc -l)" -eq 0 && echo "the 11 @import themes do not define fs-xs (correct — inherited from site.css)"</automated>
  </verify>
  <acceptance_criteria>
    - SOURCE: All 12 fs-xs-defining full-fork files have `--fs-xs: 0.85rem` (= 12.75px at the 15px root); the value is NOT 0.82rem anywhere.
    - SOURCE: site-commander-table.css `:root` defines `--fs-xs: 0.85rem`, and `grep -v '^#' site-commander-table.css | grep -c "font-size: 0.75rem"` returns `0` (its two small-text literals at the old lines 225/934 now use var(--fs-xs); spacing 0.75rem values are untouched).
    - SOURCE: The 11 @import themes do NOT define `--fs-xs`.
    - SOURCE: All 24 files have a `--muted` value that the executor has confirmed (via a contrast check noted in SUMMARY) is >= 4.5:1 against that file's `--bg` — site-commander-table.css included.
    - SOURCE: No `--fs-xs` or `--muted` value is identical-by-copy across themes that previously had distinct hues (per-theme hue preserved).
    - CONSTRAINT: Only token/declaration lines changed; no layout rules added to `site.css`; LF endings preserved; changed-lines only.
  </acceptance_criteria>
  <done>Smallest text = 12.75px (0.85rem) and muted text >= AA in every theme; commander-table is a proper F3 participant (defines --fs-xs + no hardcoded 0.75rem small text).</done>
</task>

<task type="auto">
  <name>Task 2: F2 (token half) — widen --panel/--bg surface delta + strengthen --line across all themes</name>
  <files>DeckFlow.Web/wwwroot/css/site.css, site-jeskai.css, site-abzan.css, site-bant.css, site-esper.css, site-grixis.css, site-jund.css, site-mardu.css, site-naya.css, site-nyx.css, site-planeswalker-dark.css, site-sultai.css, site-commander-table.css, site-azorius.css, site-boros.css, site-dimir.css, site-golgari.css, site-gruul.css, site-izzet.css, site-orzhov.css, site-rakdos.css, site-selesnya.css, site-simic.css, site-temur.css</files>
  <read_first>
    - .planning/phases/48-ui-audit-remediation/48-AUDIT.md (F2 + grounded evidence: --bg #eceef3 vs --panel #fafafa near-invisible delta)
    - DeckFlow.Web/wwwroot/css/site.css (:root, --bg/--panel/--line lines 3,4,7)
    - DeckFlow.Web/wwwroot/css/site-commander-table.css (:root, --bg #f0ebe0 vs --panel #faf8f3 — also near-invisible delta; --line #d4cbb8)
    - The `<theme_architecture>` block above
  </read_first>
  <action>
    F2 (token half): In ALL 24 files (including site-commander-table.css), increase the perceived separation between `--panel` (card surface) and `--bg` (page background) and strengthen `--line` (border) so panels visibly lift off the page. Per theme, keep the theme's hue and decide the direction: for LIGHT themes (incl. commander-table's warm parchment), nudge `--bg` slightly cooler/darker and/or keep `--panel` near-white so the panel reads brighter than the page; for DARK themes (e.g. site-planeswalker-dark, dark guild themes), make `--panel` lighter than `--bg`. Strengthen `--line` so the border has clear definition against both surfaces. Target a clearly perceptible delta (the audit notes the current ~`#fafafa` vs `#eceef3` is "near-invisible"; commander-table's `#faf8f3` vs `#f0ebe0` is similarly thin). Exact hex per theme is Claude's Discretion (48-CONTEXT).

    The shadow/elevation portion of F2 is LAYOUT and is handled in Plan 02 (`site-common.css`) — do NOT add box-shadow or any layout rule here. Tokens only.

    Touch only the changed token lines per file. Preserve LF. Changed-lines-only format gate.
  </action>
  <verify>
    <automated>"/mnt/c/Program Files/dotnet/dotnet.exe" build DeckFlow.Web/DeckFlow.Web.csproj -c Release 2>&1 | tail -5</automated>
    <automated>bash scripts/format-check-changed.sh staged 2>/dev/null || echo "run format gate against staged CSS before commit"</automated>
  </verify>
  <acceptance_criteria>
    - SOURCE: Every theme file's `--panel` and `--bg` differ by a clearly perceptible amount (executor records the per-theme before/after delta direction in SUMMARY); `--line` strengthened per theme — site-commander-table.css included.
    - SOURCE: Dark themes have `--panel` lighter than `--bg`; light themes have `--panel` brighter/lighter than `--bg`.
    - CONSTRAINT: NO `box-shadow` / layout rule added to any theme file or to `site.css`; tokens only; LF preserved; changed-lines only.
    - BROWSER (verified in Plan 03, noted here as the bar): cards visibly separate from the page background at both viewports.
  </acceptance_criteria>
  <done>Card surfaces read as raised panels, not one flat plane, in every theme (token half complete; shadow added in Plan 02).</done>
</task>

<task type="auto">
  <name>Task 3: Token-propagation self-check across all 24 themes</name>
  <files>.planning/phases/48-ui-audit-remediation/48-01-SUMMARY.md</files>
  <read_first>
    - DeckFlow.Web/wwwroot/css/site-commander-table.css (confirm --fs-xs added + literals re-pointed)
    - .planning/REQUIREMENTS.md (UIR-02 definition — this plan's requirement)
  </read_first>
  <action>
    Record the token-propagation self-check in the plan SUMMARY: a per-theme table covering ALL 24 user-selectable themes (24 rows) confirming for each file which of {fs-xs, muted, panel/bg/line} were changed and the resulting muted contrast ratio against that theme's --bg. Mark the `--fs-xs` cell as CHANGED for the 12 fs-xs full-forks AND for site-commander-table.css (which gained --fs-xs + re-pointed its 0.75rem literals); mark it INHERITED for the 11 @import themes (they do not define it; they inherit 0.85rem from site.css). This is the source-side propagation evidence the theme-system constraint requires (UIR-02).

    Do NOT claim UIR-01 here. UIR-01 (deployed-deckflow.gg audit + final re-score) is owned by Plan 03. Note in the SUMMARY that the deployed-site re-score and UIR-01 closure are deferred to Plan 03.
  </action>
  <verify>
    <automated>grep -q -- "--fs-xs:" DeckFlow.Web/wwwroot/css/site-commander-table.css && echo "commander-table fs-xs token present (F3 propagation confirmed)"</automated>
  </verify>
  <acceptance_criteria>
    - SOURCE: SUMMARY contains a 24-row token-propagation table with per-theme muted contrast ratio; `--fs-xs` marked CHANGED for the 12 full-forks + commander-table, INHERITED for the 11 @import themes.
    - SOURCE: SUMMARY explicitly states UIR-01 and the deployed-site final re-score are deferred to Plan 03 — this plan does NOT close UIR-01.
  </acceptance_criteria>
  <done>Per-theme token propagation documented for all 24 user-selectable themes; UIR-01 left to Plan 03.</done>
</task>

</tasks>

<verification>
- Build: `DeckFlow.Web` compiles clean (CSS is static content; build confirms no asset-pipeline break).
- Token sweep: `--fs-xs: 0.85rem` in the 12 fs-xs full-fork files + added to site-commander-table.css; absent from the 11 @import themes; `--muted` and surface tokens adjusted in all 24.
- commander-table small-text 0.75rem literals gone: `grep -v '^#' site-commander-table.css | grep -c "font-size: 0.75rem"` == 0.
- Format gate clean on changed CSS lines (LF, no mass reflow).
- No `box-shadow` / layout rule added to any theme file or `site.css`.
- Visual confirmation + UIR-01 closure deferred to Plan 03 (UIR-01/UIR-03, 2 viewports, deployed site).
</verification>

<success_criteria>
- F3 (--fs-xs = 0.85rem = 12.75px), F5, and the token half of F2 are implemented across all 24 user-selectable themes with per-theme hue preserved.
- site-commander-table.css is brought into F3 scope (defines --fs-xs + no remaining hardcoded 0.75rem small-text literals).
- Theme-system constraints honored: tokens only in `:root` (plus the two re-pointed commander-table declarations), zero layout rules in `site.css`.
</success_criteria>

<output>
Create `.planning/phases/48-ui-audit-remediation/48-01-SUMMARY.md` when done.
</output>
</content>
</invoke>
