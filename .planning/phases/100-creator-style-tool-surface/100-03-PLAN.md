---
phase: 100-creator-style-tool-surface
plan: 03
type: execute
wave: 2
depends_on: [100-02]
files_modified:
  - DeckFlow.Core/Content/CreatorStyleProfileSummary.cs
  - DeckFlow.Core/Content/ICreatorStyleProfileStore.cs
  - DeckFlow.Core/Content/CreatorStyleProfileStore.cs
  - DeckFlow.Core.Tests/Content/CreatorStyleProfileStoreTests.cs
  - DeckFlow.Web.Tests/Services/CreatorStyle/CreatorStyleDiRegistrationTests.cs
autonomous: true
requirements: [CS-31]
must_haves:
  truths:
    - "`ICreatorStyleProfileStore` can enumerate all stored creators as lightweight summaries so the picker is populated ONLY from creators present in the store (D-100-09)."
    - "Each summary carries the creator slug, platform, deck count (MinDecks), insufficient-sample flag, and last-updated timestamp — the evidence-depth inputs for the picker label."
    - "The listing query is dialect-guarded (SQLite + Postgres) using the store's existing schema-ensure/open-connection scaffolding."
    - "A round-trip test proves upserting N profiles then calling GetAllAsync returns N summaries with correct deck counts on SQLite."
  artifacts:
    - path: "DeckFlow.Core/Content/CreatorStyleProfileSummary.cs"
      provides: "lightweight picker summary DTO"
      contains: "record CreatorStyleProfileSummary"
    - path: "DeckFlow.Core/Content/ICreatorStyleProfileStore.cs"
      provides: "GetAllAsync listing method"
      contains: "GetAllAsync"
    - path: "DeckFlow.Core/Content/CreatorStyleProfileStore.cs"
      provides: "dialect-guarded list-all implementation"
      contains: "GetAllAsync"
  key_links:
    - from: "CreatorStyleController (Plan 05)"
      to: "ICreatorStyleProfileStore.GetAllAsync"
      via: "server-side picker population (D-100-09)"
      pattern: "GetAllAsync"
---

<objective>
Add a listing method to `ICreatorStyleProfileStore` so the creator picker can be populated server-side from only the creators actually present in the store (D-100-09). Today the interface exposes only single-slug `GetBySlugAsync` — there is no way to enumerate creators for the dropdown.

Purpose: This is the additive contract (RESEARCH.md Pitfall 4 / PATTERNS.md "No Analog Found") that Plan 05's controller depends on. Interface-first ordering: define and test the listing method here so the view-model assembly in Plan 05 has a stable contract.
Output: A `CreatorStyleProfileSummary` DTO, a `GetAllAsync` method on the interface + impl (dialect-guarded), and a round-trip test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/100-creator-style-tool-surface/100-RESEARCH.md
@.planning/phases/100-creator-style-tool-surface/100-PATTERNS.md

Design decision (Claude's Discretion, resolved — RESEARCH.md Open Question 1 / A2):
- The Core listing method returns deck-count evidence only (`MinDecks`), which lives directly on `CreatorStyleProfile`. The video-count half of the picker label ("N decks · M videos") is assembled in Plan 05's controller by grouping `IContentSiteIndexStore.GetPublishedRowsAsync()` by source-slug — it is a Web-tier cross-store concern and does NOT belong in this Core store. Keep this Core change minimal.

Scope fences: LF endings; `{ get; init; }` records not collapsed; no new packages.
- Serialization note (checker BLOCKER 1): this plan is `depends_on: [100-02]` and runs in Wave 2 so it does not share Wave 1 with plan 02 despite both editing `CreatorStyleDiRegistrationTests.cs`. Plan 02 already fixed that file's ctor call; THIS plan adds the new `GetAllAsync` member to its private `FakeCreatorStyleProfileStore` (adding `GetAllAsync` to the interface otherwise breaks that implementer with CS0535). Grep-confirmed this fake + the real `CreatorStyleProfileStore` are the only two implementers.
</context>

<tasks>

<task type="auto">
  <name>Task 1: CreatorStyleProfileSummary DTO + GetAllAsync on interface and store</name>
  <read_first>
    - DeckFlow.Core/Content/ICreatorStyleProfileStore.cs (full — current UpsertAsync/GetBySlugAsync/EnsureSchemaAsync surface)
    - DeckFlow.Core/Content/CreatorStyleProfileStore.cs (full — `EnsureSchemaAsync` ~67, `OpenConnectionAsync` ~129, `GetBySlugAsync` ~112-127 including `CreatorStyleProfileReadColumns.SelectList` and the Postgres/SQLite `IsPostgres` branch; the column names for slug/platform/min_decks/insufficient_sample/updated_utc)
    - DeckFlow.Core/Knowledge/CreatorStyleProfile.cs (Slug, Platform, MinDecks, InsufficientSample, UpdatedUtc fields)
    - DeckFlow.Core/Content/ICreatorDeckCacheStore.cs (the `GetByCreatorAsync` list-return shape — the role analog for a list query on these stores)
    - DeckFlow.Web.Tests/Services/CreatorStyle/CreatorStyleDiRegistrationTests.cs (~line 183: private `FakeCreatorStyleProfileStore : ICreatorStyleProfileStore` implementing only EnsureSchemaAsync/GetBySlugAsync/UpsertAsync — the extra implementer that must gain `GetAllAsync`)
  </read_first>
  <action>
    Create `DeckFlow.Core/Content/CreatorStyleProfileSummary.cs`: a `public sealed record CreatorStyleProfileSummary` with `{ get; init; }` properties `Slug` (required string), `Platform` (required string), `MinDecks` (required int), `InsufficientSample` (bool), and `UpdatedUtc` (required DateTimeOffset), each with an xmldoc summary. This is a lightweight projection — it deliberately omits the heavy StatedRules/MeasuredMetrics/FusedTargets collections so the picker query stays cheap.

    Add `Task<IReadOnlyList<CreatorStyleProfileSummary>> GetAllAsync(CancellationToken cancellationToken = default)` to `ICreatorStyleProfileStore` with xmldoc.

    Implement `GetAllAsync` in `CreatorStyleProfileStore` following the exact scaffolding of `GetBySlugAsync`: call `EnsureSchemaAsync`, `OpenConnectionAsync`, then execute an unparameterized `SELECT slug, platform, min_decks, insufficient_sample, updated_utc FROM creator_style_profile` (use the store's actual column identifiers as read from the file — do not guess), reading each row into a `CreatorStyleProfileSummary`. Reuse the store's existing `IsPostgres`-aware value/reader conventions (boolean + timestamp reads must match how `GetBySlugAsync` decodes `insufficient_sample`/`updated_utc` per dialect). Return the list (empty list when no rows — never null). Order by `updated_utc` descending if trivially expressible, else no ORDER BY.

In `CreatorStyleDiRegistrationTests.cs` implement the new `GetAllAsync` member on the private `FakeCreatorStyleProfileStore` (~line 183) so the interface addition does not break compilation (CS0535). A minimal `Task.FromResult<IReadOnlyList<CreatorStyleProfileSummary>>(Array.Empty<CreatorStyleProfileSummary>())` (or a projection over whatever the fake already stores) is sufficient — the DI test does not exercise listing. Do NOT touch the ctor call site here — plan 02 already fixed it.
  </action>
  <verify>
    <automated>dotnet build DeckFlow.Web.Tests/DeckFlow.Web.Tests.csproj -c Debug 2>&1 | grep -iE "error|warning" | grep -v "0 Warning" || echo "BUILD CLEAN"</automated>
  </verify>
  <acceptance_criteria>
    - `DeckFlow.Core/Content/CreatorStyleProfileSummary.cs` defines `record CreatorStyleProfileSummary` with the five listed init-only properties.
    - `ICreatorStyleProfileStore` declares `GetAllAsync` returning `Task<IReadOnlyList<CreatorStyleProfileSummary>>`.
    - `CreatorStyleProfileStore.GetAllAsync` reuses `EnsureSchemaAsync`/`OpenConnectionAsync` and issues a `SELECT ... FROM creator_style_profile` with no WHERE clause, decoding booleans/timestamps the same way `GetBySlugAsync` does.
    - `dotnet build DeckFlow.Core` clean (0 new warnings); no `{ get; init; }` collapsed.
    - `FakeCreatorStyleProfileStore` in `CreatorStyleDiRegistrationTests.cs` implements `GetAllAsync`; `DeckFlow.Web.Tests` compiles (no CS0535).
  </acceptance_criteria>
  <done>The summary DTO and dialect-guarded `GetAllAsync` exist and the Core project builds clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Round-trip test for GetAllAsync (SQLite)</name>
  <read_first>
    - DeckFlow.Core.Tests/Content/CreatorStyleProfileStoreTests.cs (existing test file if present — the SQLite in-memory/temp-file connection setup + upsert-then-read idiom used for GetBySlugAsync round-trips)
    - DeckFlow.Core/Content/CreatorStyleProfileStore.cs (the constructor/connection-factory seam used by existing tests)
  </read_first>
  <behavior>
    - Test: upsert three distinct `CreatorStyleProfile`s (distinct slugs, distinct MinDecks e.g. 39/12/3, one with InsufficientSample=true) into a fresh SQLite store, then `GetAllAsync` returns exactly three summaries whose Slug/MinDecks/InsufficientSample match the upserted profiles.
    - Test: `GetAllAsync` on an empty store returns an empty (non-null) list.
  </behavior>
  <action>
    Add xUnit cases to `CreatorStyleProfileStoreTests.cs` (or create it mirroring the existing store-test setup) that exercise the two behaviors above against a real SQLite connection using the same test scaffolding the file already uses for `GetBySlugAsync`. Assert count and per-summary field fidelity; assert empty-store returns an empty list. Follow the project single-test-namespace convention (`DeckFlow.Core.Tests`).
  </action>
  <verify>
    <automated>dotnet test DeckFlow.Core.Tests --filter "FullyQualifiedName~CreatorStyleProfileStoreTests" 2>&1 | tail -6</automated>
  </verify>
  <acceptance_criteria>
    - A test upserts 3 profiles and asserts `GetAllAsync` returns 3 summaries with matching Slug/MinDecks/InsufficientSample.
    - A test asserts empty-store `GetAllAsync` returns an empty, non-null list.
    - Both tests pass on SQLite.
  </acceptance_criteria>
  <done>GetAllAsync write-then-read fidelity is proven on SQLite.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| store contents → creator picker | The picker must offer only creators actually present in the store; a free-text creator field is forbidden (D-100-09). GetAllAsync is the source-of-truth enumerator that makes picker-only population possible. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-100-03 | Tampering (prompt injection / enumeration) | Free-text creator input into the artifact | mitigate | GetAllAsync enables a picker-only dropdown (Plan 05) built solely from stored creators — no free-text creator field exists, closing the injection/enumeration vector by construction (defense-in-depth atop the service's existing SanitizeUserText on the slug). |
| T-100-SC | Tampering | npm/pip/cargo installs | accept | No package installs; Core C# + test edits only. |
</threat_model>

<verification>
- `dotnet build DeckFlow.Core` clean.
- `dotnet test DeckFlow.Core.Tests --filter FullyQualifiedName~CreatorStyleProfileStoreTests` green.
</verification>

<success_criteria>
`ICreatorStyleProfileStore.GetAllAsync` returns dialect-guarded lightweight summaries proven by a SQLite round-trip test; the picker (Plan 05) has a stable server-side enumeration contract.
</success_criteria>

<output>
Create `.planning/phases/100-creator-style-tool-surface/100-03-SUMMARY.md` when done.
</output>
