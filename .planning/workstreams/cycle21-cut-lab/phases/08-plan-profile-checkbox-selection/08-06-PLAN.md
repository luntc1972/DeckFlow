---
phase: 08-plan-profile-checkbox-selection
plan: 06
type: execute
wave: 4
depends_on: ["08-03", "08-05"]
files_modified:
  - DeckFlow.Web/Services/CutLab/CutLabPlanAffinityFactory.cs
  - DeckFlow.Web/Services/CutLab/CutLabPageService.cs
  - DeckFlow.Web/Services/CutLab/CutLabUiPatchBuilder.cs
  - DeckFlow.Web/Controllers/Api/CutLabApiController.cs
  - DeckFlow.Web/Program.cs
  - DeckFlow.Web.Tests/CutLabPlanAffinityFactoryTests.cs
autonomous: true
requirements: [PLPR-02, PLPR-04]

must_haves:
  truths:
    - "The page render, the AJAX patch and the decide API all resolve plan affinity from the same factory, so the three paths cannot disagree about which cards are on-plan."
    - "Checking an EDHREC theme causes that theme's card list to be fetched once and its members to resolve as on-plan in the engine."
    - "An EDHREC failure leaves the generic strategy layer fully working and marks the commander section unavailable."
    - "With no plan profile the factory returns null, so every engine path takes the byte-identical no-op branch."
    - "Only checked themes are fetched — an unchecked theme costs no request."
  artifacts:
    - path: "DeckFlow.Web/Services/CutLab/CutLabPlanAffinityFactory.cs"
      provides: "ICutLabPlanAffinityFactory and the async build that fetches checked-theme card lists and delegates to CutLabPlanAffinityResolver"
      min_lines: 110
      contains: "ICutLabPlanAffinityFactory"
    - path: "DeckFlow.Web.Tests/CutLabPlanAffinityFactoryTests.cs"
      provides: "Null-profile, strategy-only, theme fetch, fail-open, lazy-fetch and cancellation coverage"
      min_lines: 120
  key_links:
    - from: "DeckFlow.Web/Services/CutLab/CutLabPageService.cs"
      to: "CutLabCutRoundEngine.BuildFindingsAndRoundPlan"
      via: "planAffinities resolved by ICutLabPlanAffinityFactory and passed as the trailing argument"
      pattern: "planAffinities"
    - from: "CutLabPlanAffinityFactory"
      to: "IEdhrecCommanderThemeService.GetThemeCardNamesAsync"
      via: "one call per CHECKED theme only"
      pattern: "GetThemeCardNamesAsync"
    - from: "DeckFlow.Web/Program.cs"
      to: "ICutLabPlanAffinityFactory"
      via: "scoped registration beside the other Cut Lab services"
      pattern: "ICutLabPlanAffinityFactory"
---

<objective>
Connect the three engine entry points to the plan-affinity resolver through one shared factory that
owns the EDHREC theme-card-list fetch, so the commander-theme layer actually reaches the engine and
all three request paths agree.

Purpose: without this plan the resolver, the theme service and the engine exist but never meet —
PLPR-04's "checking a theme resolves membership from that theme's card lists" is delivered here.
Output: one new factory, three wired call sites, one DI registration, one test file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/cycle21-cut-lab/phases/08-plan-profile-checkbox-selection/08-CONTEXT.md
@DeckFlow.Web/Services/CutLab/CutLabUiPatchBuilder.cs
@DeckFlow.Web/Services/CutLab/CutLabPageService.cs

<consumer_census>
<!-- Every production caller of BuildFindingsAndRoundPlan, verified by grep. All three must be wired,
     or the three paths disagree about proposal order for the same session. -->

| Path | file:line | Currently passes | After this plan |
|---|---|---|---|
| Full page render | `DeckFlow.Web/Services/CutLab/CutLabPageService.cs:505` | 5 args, twins flag last | + `planAffinities` from the factory |
| Live AJAX UI patch | `DeckFlow.Web/Services/CutLab/CutLabUiPatchBuilder.cs:85` | 5 args | + `planAffinities` from the factory |
| Decide API before-state | `DeckFlow.Web/Controllers/Api/CutLabApiController.cs:105` | 5 args | + `planAffinities` from the factory |
| What-if snapshot | `DeckFlow.Web/Services/CutLab/CutLabSimulationService.cs:524` | calls `Compute` directly | DELIBERATELY NOT WIRED — see below |

`CutLabSimulationService` computes findings for a hypothetical swap preview, not for the proposal queue.
Wiring it would fire the stranded-package detector against a pool the user has not committed to. It is
left unwired on purpose; record that decision in the SUMMARY so a later reader does not file it as a gap.

Phase 3 shipped a known WARNING-severity gap on the AJAX patch path (`03-VERIFICATION.md`). Wiring all
three paths here is what keeps this phase from adding a second instance of that same class of defect.
</consumer_census>

<interfaces>
<!-- Consumed -->
CutLabPlanAffinityResolver.ResolveAll(IReadOnlyList<CutLabAnalyzedCard>, CutLabPlanProfile?,
  IReadOnlyDictionary<string, IReadOnlyList<string>>, IReadOnlyList<CutLabCommanderTheme>)   // 08-02
IEdhrecCommanderThemeService.GetCommanderThemesAsync(string, CancellationToken)              // 08-03
IEdhrecCommanderThemeService.GetThemeCardNamesAsync(string, string, CancellationToken)       // 08-03
EdhrecThemeResult(IReadOnlyList<CutLabCommanderTheme> Themes, bool IsUnavailable)            // 08-03
CutLabCutRoundEngine.BuildFindingsAndRoundPlan(..., IReadOnlyDictionary<string, CutLabPlanAffinity>? planAffinities = null)  // 08-05
CutLabAnalysisContext.AnalyzedCards   // the analyzed pool each call site already holds

<!-- Contract this plan CREATES -->
DeckFlow.Web/Services/CutLab/CutLabPlanAffinityFactory.cs, namespace DeckFlow.Web.Services.CutLab:
  public interface ICutLabPlanAffinityFactory
    Task<IReadOnlyDictionary<string, CutLabPlanAffinity>?> BuildAsync(
      CutLabPlanProfile? planProfile,
      IReadOnlyList<CutLabAnalyzedCard> analyzedCards,
      IReadOnlyList<string> commanderNames,
      CancellationToken cancellationToken = default)
  public sealed class CutLabPlanAffinityFactory : ICutLabPlanAffinityFactory
</interfaces>

<constraints>
- The factory returns `null` — not an empty dictionary — when there is nothing to resolve, so the engine
  takes the exact null branch 08-05's no-op tests cover.
- Fetch a theme's card list only for themes present in `planProfile.CommanderThemes`. Never prefetch the
  unchecked ones; that is the design spec's "lazy fetch per checked theme".
- Fail-open: any EDHREC failure yields an empty theme-card map and an empty known-theme list, so the
  generic strategy layer still resolves. The factory never throws except `OperationCanceledException`.
- No new packages. Preserve line endings on the four pre-existing files.
</constraints>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build the shared plan-affinity factory and register it</name>
  <files>DeckFlow.Web/Services/CutLab/CutLabPlanAffinityFactory.cs, DeckFlow.Web/Program.cs</files>
  <read_first>
    - DeckFlow.Web/Services/CutLab/CutLabPlanAffinityResolver.cs (08-02 — the exact `ResolveAll` signature
      and what `allKnownThemes` versus `planProfile.CommanderThemes` mean)
    - DeckFlow.Web/Services/CutLab/EdhrecCommanderThemeService.cs (08-03 — the two async methods, their
      fail-open contract, and that they already memoize successful results)
    - DeckFlow.Web/Services/CutLab/CutLabFloorResolver.cs (the shape of a small shared Cut Lab service with
      an interface, nullable dependencies and a single method — the closest analog for this factory)
    - DeckFlow.Web/Program.cs (the Cut Lab service registration block, so the new registration lands beside
      `ICutLabFloorResolver` and `IEdhrecCommanderThemeService` rather than in a new section)
  </read_first>
  <behavior>
    - A null `planProfile` returns null without touching the theme service.
    - A profile with no strategies and no themes returns null without touching the theme service.
    - A profile with strategies but no themes returns a non-null map and issues zero EDHREC requests.
    - A profile with two checked themes issues exactly one `GetThemeCardNamesAsync` call per checked theme,
      plus one `GetCommanderThemesAsync` call for the full theme list used to derive off-plan themes.
    - When `GetCommanderThemesAsync` reports unavailable, the factory still returns a map built from the
      strategy layer alone, with no off-plan themes.
    - When one theme's card list fetch fails, the other checked theme still resolves.
    - Cancellation propagates as `OperationCanceledException`.
    - The returned map is keyed identically to `CutLabPlanAffinityResolver.ResolveAll`'s output, so
      `CutLabPlanAffinityResolver.For` resolves against it.
  </behavior>
  <action>
    Create `DeckFlow.Web/Services/CutLab/CutLabPlanAffinityFactory.cs` declaring `ICutLabPlanAffinityFactory`
    and `CutLabPlanAffinityFactory` as given in `<interfaces>`. The constructor takes
    `IEdhrecCommanderThemeService` and an optional `ILogger<CutLabPlanAffinityFactory>`, null-guarded with
    `ArgumentNullException.ThrowIfNull`.

    `BuildAsync` short-circuits to null when `planProfile` is null or when both `GenericStrategies` and
    `CommanderThemes` are empty. Otherwise it derives the commander name to query — the first entry of
    `commanderNames`, matching how `CutLabFloorResolver` hands commander names to the role-floor baseline —
    calls `GetCommanderThemesAsync` for the full theme list, and calls `GetThemeCardNamesAsync` once per
    entry in `planProfile.CommanderThemes`. Await the per-theme fetches sequentially rather than with
    `Task.WhenAll`: the checked set is at most a handful, EDHREC is a courtesy-use third party, and
    sequential calls keep the request pattern predictable. Wrap each fetch so a failure yields an empty list
    for that theme and one Warning log rather than aborting the build.

    Assemble the theme-card dictionary keyed by theme slug with `StringComparer.OrdinalIgnoreCase` and hand
    it, the profile, the analyzed cards and the full theme list to `CutLabPlanAffinityResolver.ResolveAll`.
    When the commander-theme lookup was unavailable, pass an empty known-theme list so no card is labelled
    off-plan for a theme the user was never shown.

    Register `ICutLabPlanAffinityFactory` in `Program.cs` with the same lifetime the neighbouring Cut Lab
    services use, in the same block.
  </action>
  <verify>
    <automated>dotnet build DeckFlow.Web/DeckFlow.Web.csproj 2>&amp;1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "ICutLabPlanAffinityFactory" DeckFlow.Web/Program.cs` returns a registration line inside the
      existing Cut Lab block.
    - `grep -n "Task.WhenAll" DeckFlow.Web/Services/CutLab/CutLabPlanAffinityFactory.cs` returns nothing.
    - The short-circuit returns literal `null`, not `new Dictionary<...>()` — verify by reading the early
      return, and by the test `BuildAsync_NullProfile_ReturnsNull` asserting `Assert.Null`.
    - `grep -c "catch" DeckFlow.Web/Services/CutLab/CutLabPlanAffinityFactory.cs` is at least 1 and no catch
      block swallows `OperationCanceledException`.
    - `dotnet build DeckFlow.Web/DeckFlow.Web.csproj` reports 0 errors and no more than 9 warnings.
  </acceptance_criteria>
  <done>One shared factory turns a plan profile plus a pool into an affinity map, fetching only checked themes and degrading to the strategy layer alone on any EDHREC failure.</done>
</task>

<task type="auto">
  <name>Task 2: Wire the page, patch and decide-API paths to the factory</name>
  <files>DeckFlow.Web/Services/CutLab/CutLabPageService.cs, DeckFlow.Web/Services/CutLab/CutLabUiPatchBuilder.cs, DeckFlow.Web/Controllers/Api/CutLabApiController.cs</files>
  <read_first>
    - DeckFlow.Web/Services/CutLab/CutLabPageService.cs around line 505 (the `BuildFindingsAndRoundPlan`
      call and the surrounding `analysisContext` / `derivedWorkingList` locals it must reuse; also the
      constructor's dependency list)
    - DeckFlow.Web/Services/CutLab/CutLabUiPatchBuilder.cs lines 30-100 (constructor dependencies and the
      `context` / `workingList` locals available at the call site on line 85)
    - DeckFlow.Web/Controllers/Api/CutLabApiController.cs lines 60-130 (constructor dependencies and the
      `beforeContext` / `beforeWorkingList` locals available at line 105)
    - DeckFlow.Web/Services/CutLab/CutLabPlanAffinityFactory.cs (as written in Task 1)
  </read_first>
  <action>
    Inject `ICutLabPlanAffinityFactory` into all three types through their existing constructors, following
    each type's current null-guard convention.

    At each of the three call sites, await `BuildAsync` with `state.Intent.PlanProfile`, the analyzed cards
    already available in the local analysis context, the resolved commander names and the ambient
    cancellation token, then pass the result as the new trailing `planAffinities` argument to
    `CutLabCutRoundEngine.BuildFindingsAndRoundPlan`. Place the await immediately before the existing call
    so the local it feeds is obvious. Use `.ConfigureAwait(false)`, matching the surrounding code.

    Do NOT touch `CutLabSimulationService` — see the census above.

    Do NOT change any other behavior at these call sites: no reordering of existing statements, no altered
    warning text, no new early return.
  </action>
  <verify>
    <automated>dotnet build DeckFlow.Web/DeckFlow.Web.csproj 2>&amp;1 | tail -5 &amp;&amp; dotnet test DeckFlow.Web.Tests/DeckFlow.Web.Tests.csproj --filter "FullyQualifiedName~CutLabPageServiceTests|FullyQualifiedName~CutLabApiControllerTests|FullyQualifiedName~CutLabAjaxFloorByRoleRegressionTests"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "planAffinities" DeckFlow.Web/Services/CutLab/CutLabPageService.cs DeckFlow.Web/Services/CutLab/CutLabUiPatchBuilder.cs DeckFlow.Web/Controllers/Api/CutLabApiController.cs`
      shows at least one match in each of the three files.
    - `git diff DeckFlow.Web/Services/CutLab/CutLabSimulationService.cs` is empty.
    - Existing tests for all three types pass. Where a test constructs one of these types directly, it may be
      updated ONLY to supply the new constructor argument — any other edit to an existing test is a
      regression signal, so stop and report it.
    - `dotnet build DeckFlow.Web/DeckFlow.Web.csproj` reports 0 errors and no more than 9 warnings.
  </acceptance_criteria>
  <done>All three engine entry points resolve plan affinity from the same factory and pass it to the round engine, with the what-if path deliberately untouched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Prove the factory's fetch discipline, fail-open behavior and the null short-circuit</name>
  <files>DeckFlow.Web.Tests/CutLabPlanAffinityFactoryTests.cs</files>
  <read_first>
    - DeckFlow.Web/Services/CutLab/CutLabPlanAffinityFactory.cs (as finished in Task 1)
    - DeckFlow.Web.Tests/CutLabAnalysisContextBuilderTests.cs (the `Fake*` stateful double conventions this
      suite uses — the theme-service double must be a `Fake` that records calls, not a mock library)
    - DeckFlow.Web/Services/CutLab/EdhrecCommanderThemeService.cs (the interface the fake implements)
  </read_first>
  <behavior>
    - `BuildAsync_NullProfile_ReturnsNull_AndIssuesNoRequests`
    - `BuildAsync_EmptyProfile_ReturnsNull_AndIssuesNoRequests`
    - `BuildAsync_StrategiesOnly_ReturnsMap_AndIssuesNoThemeCardRequests`
    - `BuildAsync_TwoCheckedThemes_FetchesEachExactlyOnce`
    - `BuildAsync_UncheckedTheme_NeverFetched`
    - `BuildAsync_CommanderThemesUnavailable_StrategyLayerStillResolves_AndNoOffPlanThemes`
    - `BuildAsync_OneThemeFetchFails_OtherThemeStillResolves`
    - `BuildAsync_Cancellation_Throws`
    - `BuildAsync_ResultKeys_ResolveThroughPlanAffinityResolverFor`
  </behavior>
  <action>
    Write `DeckFlow.Web.Tests/CutLabPlanAffinityFactoryTests.cs` with a `FakeEdhrecCommanderThemeService`
    stateful double that records every call it receives (commander name, theme slug) and can be configured
    to return a canned theme list, canned card lists, an unavailable result, or to throw for one specific
    theme slug.

    The fetch-discipline tests must assert on the recorded call log, not on the returned map: a factory that
    fetched every known theme and then filtered would still produce the right map while making N times the
    requests against a courtesy-use third party. Assert both the exact count and the exact slugs requested.

    `BuildAsync_UncheckedTheme_NeverFetched` must be written so the eligible population is larger than the
    checked set — give the fake at least four known themes and check two — and assert both that exactly the
    two checked slugs were requested AND that the two unchecked slugs appear in the fake's known list, so a
    fixture that simply had nothing to over-fetch cannot pass vacuously.
  </action>
  <verify>
    <automated>dotnet test DeckFlow.Web.Tests/DeckFlow.Web.Tests.csproj --filter "FullyQualifiedName~CutLabPlanAffinityFactoryTests"</automated>
  </verify>
  <acceptance_criteria>
    - All tests in `CutLabPlanAffinityFactoryTests` pass.
    - The fake records calls and at least three tests assert against the recorded call log.
    - `BuildAsync_NullProfile_ReturnsNull_AndIssuesNoRequests` uses `Assert.Null` on the result and asserts a
      zero-length call log.
    - The unchecked-theme test's fake exposes strictly more known themes than the profile checks.
    - `dotnet test DeckFlow.Web.Tests/DeckFlow.Web.Tests.csproj --filter "FullyQualifiedName~CutLab"` — no
      pre-existing CutLab test regresses.
  </acceptance_criteria>
  <done>The factory provably fetches only checked themes, degrades to the strategy layer on any EDHREC failure, and returns null when there is nothing to resolve.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP request → Cut Lab page / decide API → EDHREC fetch | A user request now triggers outbound requests to a third party, once per checked theme |
| EDHREC response → affinity map → proposal order | Third-party data influences engine output on every request path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-06-01 | Denial of Service | Server-side request amplification — one user request fanning out to many EDHREC requests | mitigate | Only slugs present in `planProfile.CommanderThemes` are fetched, fetches are sequential rather than parallel, and `EdhrecCommanderThemeService` memoizes successful results in `IMemoryCache` and on disk, so repeated decide-API rounds within a session refetch nothing. A test asserts the exact request count. |
| T-08-06-02 | Denial of Service | A slow or hanging EDHREC stalling the Cut Lab page and the decide API | mitigate | The `edhrec` Polly pipeline's 10s per-attempt timeout and 2-retry cap bound each fetch, and every failure path returns immediately with the strategy-only map. The ambient `CancellationToken` is threaded to every await. |
| T-08-06-03 | Tampering | Client-supplied theme slugs driving outbound requests (server-side request forgery shape) | mitigate | Slugs are validated against `^[a-z0-9-]+$` inside `EdhrecCommanderThemeService` before any request, and the base address is a fixed constant, so a slug cannot change the host or escape the path. |
| T-08-06-04 | Information Disclosure | The commander name being sent to a third party | accept | Only the commander name, already public card data, leaves the app. No user identifier, deck contents, or session data is transmitted. This matches the existing EDHREC usage already shipped for the manabase baseline. |
| T-08-06-05 | Tampering | Divergent affinity between the page path and the AJAX patch path | mitigate | All three paths call the same factory with the same inputs; a single resolution point removes the class of defect that produced Phase 3's WARNING-severity AJAX gap. |
| T-08-06-SC | Tampering | npm / NuGet installs | mitigate | No packages added. Any new package stops the plan pending explicit user approval. |
</threat_model>

<verification>
1. `dotnet build DeckFlow.sln` — 0 errors, at most 9 warnings.
2. `dotnet test DeckFlow.Web.Tests/DeckFlow.Web.Tests.csproj` full suite passes.
3. `grep -rn "BuildFindingsAndRoundPlan" DeckFlow.Web --include=*.cs` shows all three production call sites
   passing a `planAffinities` argument.
4. `git diff --name-only` lists exactly the six files in `files_modified` plus, at most, constructor-argument
   updates in tests that directly instantiate the three wired types.
5. `git diff --stat` vs `git diff --ignore-all-space --stat` agree — no EOL churn.
</verification>

<success_criteria>
- Page, AJAX patch and decide API all resolve affinity through one factory.
- Checking a theme fetches exactly that theme's card list, once; unchecked themes cost nothing.
- Any EDHREC failure leaves the generic strategy layer fully functional.
- No plan profile yields a null affinity map and therefore the byte-identical engine path.
</success_criteria>

<output>
Create `.planning/workstreams/cycle21-cut-lab/phases/08-plan-profile-checkbox-selection/08-06-SUMMARY.md` when done.
</output>
