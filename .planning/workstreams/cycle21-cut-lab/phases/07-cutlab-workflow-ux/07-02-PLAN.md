---
phase: 07-cutlab-workflow-ux
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - DeckFlow.Web/Views/Deck/CutLab.cshtml
autonomous: true
requirements: [CLUX-02]

must_haves:
  truths:
    - "The four tabpanels appear in DOM order 1 -> 2 -> 3 -> 4, so a scrolling or no-JS reader meets Decide before Goals and Export."
    - "Section content is MOVED, not rewritten — every id, data attribute, form action and antiforgery field survives the move byte-for-byte, so the existing TS selectors and the 17 cut-lab specs keep passing."
    - "The anchor nav order matches the new DOM order, so the page and its own nav no longer disagree."
    - "Gate G-1 passes; G-2/G-3/G-4 still fail (they belong to later plans)."
  artifacts:
    - path: "DeckFlow.Web/Views/Deck/CutLab.cshtml"
      provides: "Workflow-ordered document"
      contains: "cut-lab-step-panel-2"
---

<objective>
Reorder the results sections so the document reads in workflow order. Razor only —
no TypeScript, no CSS, no behavior change.

Purpose: this is the half of the fix that does not depend on JavaScript. It is
independently shippable and independently revertable, and it is what makes the
wizard (07-03) a pure enhancement rather than a prerequisite (D-2).

Output: `CutLab.cshtml` with panels in order 1, 2, 3, 4 and the anchor nav
matching.
</objective>

<environment>
- Preserve the file's existing line endings exactly. `.gitattributes` enforces LF here; do not let an editor rewrite them.
- The changed-lines format gate applies (`scripts/format-check-changed.sh staged`). Move blocks intact; do not reflow untouched lines.
</environment>

<context>
Current DOM sequence (07-CONTEXT E-2):

```
panel-1 Process (Lock your pool + Packages)
  -> How your pool competes / By type / By subtype / Structural findings / Role floors
  -> panel-3 Goals
  -> panel-4 Export
  -> Scenarios
  -> What-if swap
  -> panel-2 Decide (Cut rounds + Tune quantities)
  -> Cuts made
  -> Compare to baseline
```
</context>

<tasks>

<task type="auto">
  <name>Task 1: Move the Decide panel above Goals</name>

  Move the `<section role="tabpanel" id="cut-lab-step-panel-2">` block
  (`CutLab.cshtml:1174-1396` at time of writing — re-locate by id, the line
  numbers will have drifted) so it sits immediately after the Role floors
  section and before `cut-lab-step-panel-3`.

  Move `Cuts made` (`#cut-lab-section-cuts-made`) with it, directly after the
  Decide panel — it is the log of the decisions made in that step and currently
  sits after it already, so it just travels along.

  Cut and paste whole blocks. Do not retype, do not reformat, do not "tidy while
  you're in there". Every `data-cut-lab-*` attribute is a live TS selector.
</task>

<task type="auto">
  <name>Task 2: Group the step-3 material</name>

  Move `Scenarios` (`#cut-lab-section-scenarios`), `What-if swap`
  (`#cut-lab-section-whatif`) and `Compare to baseline` so they sit inside or
  immediately after `cut-lab-step-panel-3`, before `cut-lab-step-panel-4`.

  Rationale: all three are "evaluate the current list" tools, and 07-03 will
  need them under one step. Keeping `Compare to baseline` adjacent to `Goals`
  also puts the two things that share the "Recalculate" staleness problem next to
  each other, which is what 07-06's copy fix depends on.

  `Compare to baseline` currently has no `data-cutlab-mobile-collapse` wrapper.
  Leave that as-is in this plan; 07-04 owns collapse behavior.
</task>

<task type="auto">
  <name>Task 3: Reorder the anchor nav to match</name>

  Update the `<ul class="cutlab-anchor-nav-list">` (`:305-319`) so its link order
  matches the new DOM order. Keep every existing link — removing the duplication
  between the anchor nav and the tablist is a separate judgement call and is NOT
  in this phase's scope (D-3).
</task>

<task type="auto">
  <name>Task 4: Verify nothing moved that should not have</name>

  ```bash
  git diff --stat
  git diff --ignore-all-space --stat
  ```

  The two must agree — a gap means whitespace or EOL churn crept in.

  Then confirm the move was content-preserving:

  ```bash
  git show HEAD:DeckFlow.Web/Views/Deck/CutLab.cshtml | grep -c 'data-cut-lab-'
  grep -c 'data-cut-lab-' DeckFlow.Web/Views/Deck/CutLab.cshtml
  ```

  The counts must be identical. Same check for `@Html.AntiForgeryToken()` and for
  `id="cut-lab-`.
</task>

<task type="auto">
  <name>Task 5: Run the gate and the existing suites</name>

  - `cut-lab-workflow-ux.spec.ts` — G-1 must now PASS; G-2/G-3/G-4 still fail.
  - The 9 existing `cut-lab-*.spec.ts` e2e specs must all still pass.
  - The 18 `ts-tests/cut-lab-*.test.ts` vitest files must all still pass.
  - `dotnet build` clean, `CutLabViewRenderTests` green.

  A failure in `cut-lab-jump-nav.test.ts` or `cut-lab-section-collapse.test.ts`
  most likely means an id was lost in the move, not that the test is wrong.
</task>

</tasks>

<acceptance_criteria>
- `[role="tabpanel"]` DOM order is 1, 2, 3, 4.
- `data-cut-lab-*`, `id="cut-lab-`, and antiforgery-token counts are unchanged from HEAD.
- `git diff --stat` and `git diff --ignore-all-space --stat` agree.
- G-1 passes. All pre-existing cut-lab specs and vitest files pass. Build clean.
- Only `CutLab.cshtml` is modified.
</acceptance_criteria>
