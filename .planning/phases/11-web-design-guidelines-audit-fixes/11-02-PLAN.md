---
id: 11-02
phase: 11
plan: 02
type: execute
wave: 2
depends_on: [11-01]
files_modified:
  - DeckFlow.Web/wwwroot/css/admin.css
requirements: [WDG-01]
autonomous: true
verification_command: "dotnet build DeckFlow.sln --configuration Release"
user_setup: []
must_haves:
  truths:
    - "Tab-navigating any page under /Admin/* shows a visible keyboard focus ring on the focused interactive element (a, button, input, select, textarea, summary, [role=tab])."
    - "admin.css contains its own universal :focus-visible block because _AdminLayout.cshtml does NOT load site.css or site-common.css — admin shell is a separate stylesheet."
    - "Numeric columns on .admin-table use tabular-nums (jitter-free AJAX updates) per the WDG-08 utility foundation OR a local admin.css addition."
  artifacts:
    - path: "DeckFlow.Web/wwwroot/css/admin.css"
      provides: "Universal :focus-visible rule mirroring site.css:109-118; color-scheme: dark on :root; tabular-nums for admin table numeric cells"
      contains: ":focus-visible"
  key_links:
    - from: "admin.css :focus-visible block"
      to: "every interactive element in /Admin/* views"
      via: "CSS cascade — admin.css is loaded by _AdminLayout.cshtml:14"
      pattern: ":focus-visible"
---

<objective>
Land Sweep 2 of the WDG audit: add a universal `:focus-visible` block to `admin.css` mirroring the pattern from `site.css:109-118` so admin pages render visible keyboard focus indicators.

Purpose: Admin shell loads only `admin.css` (`_AdminLayout.cshtml:14`) — neither `site.css` nor `site-common.css` is inherited, so the universal `:focus-visible` rule in `site.css:109-118` is not reachable from admin pages. Without this fix, every keyboard-navigation user on `/Admin/*` has zero visible focus ring. Per D-02 this depends on Sweep 1 (WDG-08) landing first so the cross-cutting foundation is in place.

Output: Updated `DeckFlow.Web/wwwroot/css/admin.css` with focus-visible block, color-scheme declaration, and tabular-nums on admin table numeric cells.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-web-design-guidelines-audit-fixes/11-CONTEXT.md
@.planning/phases/11-web-design-guidelines-audit-fixes/11-01-PLAN.md
@.planning/quick/260513-wdg-web-design-guidelines-audit-findings/260513-wdg-FINDINGS.md
@DeckFlow.Web/wwwroot/css/admin.css
@DeckFlow.Web/wwwroot/css/site.css
@DeckFlow.Web/Views/Shared/_AdminLayout.cshtml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add universal :focus-visible block + color-scheme + tabular-nums to admin.css</name>
  <files>DeckFlow.Web/wwwroot/css/admin.css</files>
  <read_first>
    - DeckFlow.Web/wwwroot/css/admin.css (current 152 lines — see existing :root and table rules before inserting)
    - DeckFlow.Web/wwwroot/css/site.css lines 109-118 (the canonical universal :focus-visible block — pattern source)
    - DeckFlow.Web/Views/Shared/_AdminLayout.cshtml line 14 (proves admin.css is the ONLY stylesheet loaded for admin pages — no site-common.css inheritance)
    - .planning/quick/260513-wdg-web-design-guidelines-audit-findings/260513-wdg-FINDINGS.md §"P1 finding A" (admin.css missing focus-visible) and §"Sweep 2" (lines 266-271)
  </read_first>
  <action>
    Edit `DeckFlow.Web/wwwroot/css/admin.css`. Three additions, all to this single file (admin.css does NOT inherit from site-common.css, so the Sweep 1 changes from 11-01 do NOT cover the admin shell — every admin a11y rule must be local).

    1. Mirror the universal `:focus-visible` rule from `site.css:109-118` into admin.css. Use the exact selector set `a, button, input, select, textarea, summary, [role="tab"]` and the same outline declaration values (color, width, offset) as `site.css:109-118` so admin focus rings match the main shell. Place the block at a logical position — near the top after `:root`, or grouped with existing reset-style rules.

    2. Add `color-scheme: dark` to the admin.css `:root` selector. Admin shell is dark-themed; this signals native chrome (scrollbars, native select dropdowns) to render dark. Per finding O (admin row).

    3. Add `font-variant-numeric: tabular-nums;` to admin table numeric cells. Apply selector `.admin-table td:not(.route)` per FINDINGS.md Sweep 2 line 270, OR — preferred — apply the `.tabular` utility class (now available app-wide from Sweep 1 in 11-01) to admin-table numeric cells where appropriate. Pick the local CSS approach (`.admin-table td:not(.route) { font-variant-numeric: tabular-nums; }`) since admin.css does not inherit site-common.css. Per finding R (admin row).

    Group the additions under a banner like `/* === WDG audit fixes (WDG-01, Phase 11 Sweep 2) === */` referencing FINDINGS.md for traceability.

    Do NOT touch any other CSS file. Do NOT remove existing admin.css rules.
  </action>
  <verify>
    <automated>dotnet build DeckFlow.sln --configuration Release</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q ':focus-visible' DeckFlow.Web/wwwroot/css/admin.css`
    - `grep -Eq 'a,[[:space:]]*button,[[:space:]]*input,[[:space:]]*select,[[:space:]]*textarea,[[:space:]]*summary,[[:space:]]*\[role="tab"\]' DeckFlow.Web/wwwroot/css/admin.css` (selector list mirrors site.css:109-118)
    - `grep -q 'color-scheme' DeckFlow.Web/wwwroot/css/admin.css`
    - `grep -Eq 'tabular-nums' DeckFlow.Web/wwwroot/css/admin.css`
    - `git diff --stat DeckFlow.Web/wwwroot/css/` shows only `admin.css` modified
    - `dotnet build DeckFlow.sln --configuration Release` exits 0 with zero new warnings
  </acceptance_criteria>
  <done>
    admin.css contains the universal :focus-visible block (mirroring site.css:109-118), color-scheme on :root, and tabular-nums on admin table numeric cells. Release build clean.
  </done>
</task>

</tasks>

<verification>
- `dotnet build DeckFlow.sln --configuration Release` exits 0 with zero new warnings.
- `git diff --stat` shows admin.css as the only changed file.
- All four grep assertions in acceptance_criteria pass.
- No UAT in this plan per D-03 — batch UAT at phase end will visually verify focus ring on Tab navigation across `/Admin/*` pages.
</verification>

<success_criteria>
- Sweep 2 lands: admin.css has its own universal :focus-visible block, color-scheme, and tabular-nums.
- WDG-01 requirement satisfied: admin shell renders visible keyboard focus indicators on all interactive elements.
- Phase 11 SC #1 will be verifiable at end-of-phase UAT.
</success_criteria>

<output>
After completion, create `.planning/phases/11-web-design-guidelines-audit-fixes/11-02-SUMMARY.md`.
</output>
