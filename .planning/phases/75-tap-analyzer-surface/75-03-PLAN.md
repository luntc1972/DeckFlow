---
phase: 75-tap-analyzer-surface
plan: 03
type: execute
wave: 2
depends_on: ["75-01", "75-02"]
files_modified:
  - DeckFlow.Web/Services/FeatureFlags/FeatureFlagCatalog.cs
  - DeckFlow.Web/Services/FeatureFlags/FeatureFlagStore.cs
  - DeckFlow.Web/Services/Manabase/ManabaseAnalysisService.cs
  - DeckFlow.Web/Controllers/ManabaseController.cs
  - DeckFlow.Web.Tests/Manabase/ManabaseAnalysisServiceTests.cs
  - DeckFlow.Web.Tests/Manabase/ManabaseViewModelTests.cs
  - README.md
autonomous: true
requirements: [TAP-04]

must_haves:
  truths:
    - "The flag analysis.manabase.tap-analyzer is registered in FeatureFlagCatalog with a description and seeded OFF (FALSE/0) in BOTH Postgres and SQLite seed SQL."
    - "ManabaseAnalysisService reads the flag via IsFlagOn (fail-safe OFF) and propagates it to ManabaseAnalysisResult.ShowTapAnalyzer."
    - "The flag-read fail-safe-OFF behavior (missing key → false) and ManabaseViewModel.ShowTapAnalyzer default-false are covered by xUnit, not source inspection alone."
    - "ManabaseController passes tap to the download text builder ONLY when the flag is ON (tap == null when OFF → byte-identical artifact), and assigns ShowTapAnalyzer to the page ViewModel."
    - "With the flag OFF the downloaded .txt artifact contains no 'Untapped Sources:' section."
  artifacts:
    - path: "DeckFlow.Web/Services/FeatureFlags/FeatureFlagCatalog.cs"
      provides: "tap-analyzer flag description"
      contains: "analysis.manabase.tap-analyzer"
    - path: "DeckFlow.Web/Services/FeatureFlags/FeatureFlagStore.cs"
      provides: "seed entries in both dialects, OFF"
      contains: "analysis.manabase.tap-analyzer"
    - path: "DeckFlow.Web/Services/Manabase/ManabaseAnalysisService.cs"
      provides: "TapAnalyzerFlagKey const + flag read + ShowTapAnalyzer propagation"
      contains: "TapAnalyzerFlagKey"
    - path: "DeckFlow.Web/Controllers/ManabaseController.cs"
      provides: "download tap-gate + ViewModel ShowTapAnalyzer wiring"
      contains: "ShowTapAnalyzer"
  key_links:
    - from: "ManabaseAnalysisService.AnalyzeAsync"
      to: "FeatureFlagCache snapshot"
      via: "IsFlagOn(TapAnalyzerFlagKey)"
      pattern: "IsFlagOn\\(TapAnalyzerFlagKey\\)"
    - from: "ManabaseController.Download"
      to: "ManabaseReportTextBuilder.Build"
      via: "tap: result.ShowTapAnalyzer ? result.Report.TapAnalysis : null"
      pattern: "ShowTapAnalyzer \\? result.Report.TapAnalysis : null"
---

<objective>
Register the `analysis.manabase.tap-analyzer` feature flag (OFF by default, both dialects) and wire the flag through the service, result, controller, and download path so the metric surfaces only when an operator enables it. This turns the Wave 0 flag + download tests GREEN and enforces the TAP-04 byte-identity-when-OFF invariant.

Purpose: Ship the capability dark. Flag OFF must be byte-identical to pre-Cycle-13 output on both the page (no card — enforced by the view in 75-04) and the artifact (no section — enforced here via the tap-gate).

Output: catalog description + two-dialect seed; `TapAnalyzerFlagKey` const, flag read, and `ShowTapAnalyzer` propagation in the service; download tap-gate + page ViewModel assignment in the controller; README note for the new flag.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/75-tap-analyzer-surface/75-RESEARCH.md
@.planning/phases/75-tap-analyzer-surface/75-PATTERNS.md
@.planning/phases/75-tap-analyzer-surface/75-02-SUMMARY.md

<interfaces>
<!-- Existing anchors to mirror. ShowTapAnalyzer prop on the result was added in 75-01. -->

FeatureFlagCatalog.cs:
- `Descriptions` dictionary: the manabase FAMILY (`analysis.manabase.*`) ENDS at `["analysis.manabase.commander-castability"] = "..."` (~:67-68). The next entry `["analysis.command-zone-awareness"]` (~:69-70) is a DIFFERENT family (`analysis.*`, not manabase). Insert the new manabase-family tap entry directly AFTER `commander-castability` (between :68 and :69) so it stays grouped with the manabase family.

FeatureFlagStore.cs:
- The `RenamedFlagKeys` old→new map (~:21-37) is for RENAMED keys only; `tap-analyzer` is brand-new (no old key) → it gets NO entry there (note `command-zone-awareness` is absent from that list too).
- PostgresSeedSql (~:218-227): the manabase-family rows run through `('analysis.manabase.commander-castability', FALSE)` (~:225); the LAST row before `ON CONFLICT (key) DO NOTHING;` is the different-family `('analysis.command-zone-awareness', FALSE)` (~:226). Insert `('analysis.manabase.tap-analyzer', FALSE)` right AFTER the commander-castability row (add a trailing comma to it) so command-zone-awareness stays the final row.
- SqliteSeedSql (~:250-259): same shape with `0` not `FALSE` — insert `('analysis.manabase.tap-analyzer', 0)` after `('analysis.manabase.commander-castability', 0)` (~:257).
- Both use `ON CONFLICT (key) DO NOTHING` (operator toggles survive re-bootstrap).

ManabaseAnalysisService.cs:
- Flag-key consts at :133-180 (pattern: `public const string PlainLanguageVerdictFlagKey = "analysis.manabase.plain-language-verdict";`).
- Flag reads at :214-249 (e.g. `bool commanderCastability = IsFlagOn(CommanderCastabilityFlagKey);`).
- IsFlagOn helper :320 (DO NOT MODIFY — `Snapshot().TryGetValue`, missing key → false).
- Result construction :287-291 sets `CommanderCastabilityEnabled = commanderCastability` — add `ShowTapAnalyzer = showTapAnalyzer`.
- ManabaseAnalysisResult.ShowTapAnalyzer property already exists (added in 75-01).

ManabaseController.cs:
- ViewModel build :88-103 sets `ShowCommanderCastability = result.CommanderCastabilityEnabled` (:100) — add `ShowTapAnalyzer = result.ShowTapAnalyzer`.
- Download Build call :127-128 — add `tap: result.ShowTapAnalyzer ? result.Report.TapAnalysis : null`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register flag in catalog + seed OFF in both dialects + README</name>
  <read_first>
    - DeckFlow.Web/Services/FeatureFlags/FeatureFlagCatalog.cs (Descriptions dictionary; manabase family ends at `analysis.manabase.commander-castability` ~:67-68; `analysis.command-zone-awareness` ~:69 is a different family)
    - DeckFlow.Web/Services/FeatureFlags/FeatureFlagStore.cs (PostgresSeedSql ~:218-227, SqliteSeedSql ~:250-259; manabase rows end at commander-castability ~:225/:257; command-zone-awareness ~:226/:258 is the last row, different family; RenamedFlagKeys ~:21-37 needs no new entry)
    - DeckFlow.Web.Tests/FeatureFlagCatalogTests.cs + FeatureFlagStoreSeedTests.cs (the InlineData added in 75-01 this satisfies)
    - README.md (feature-flag section, to add the new flag row)
  </read_first>
  <action>
    Add `["analysis.manabase.tap-analyzer"]` to FeatureFlagCatalog.Descriptions, placed with the manabase family — directly AFTER the `["analysis.manabase.commander-castability"]` entry and BEFORE `["analysis.command-zone-awareness"]` (a different family, NOT the end of the manabase family) — with the operator description: "Surface untapped-source frequency and turn-1 untapped availability on the mana base page and its paste artifact. Off = byte-identical output." In FeatureFlagStore, insert `('analysis.manabase.tap-analyzer', FALSE)` into PostgresSeedSql immediately AFTER the `('analysis.manabase.commander-castability', FALSE)` row (add a trailing comma to that row) so the different-family `('analysis.command-zone-awareness', FALSE)` stays the final row before `ON CONFLICT (key) DO NOTHING;`; mirror in SqliteSeedSql with `('analysis.manabase.tap-analyzer', 0)` after the `commander-castability` `0` row. Do NOT add the new key to the `RenamedFlagKeys` map (it has no old key). Seed OFF per the locked decision. Add a row for the new flag to the README feature-flag documentation noting it is OFF by default and that OFF = byte-identical output. Do NOT touch the ON CONFLICT contract or reorder existing entries. Preserve LF line endings; touch only changed lines.
  </action>
  <verify>
    <automated>dotnet test DeckFlow.Web.Tests --filter "FullyQualifiedName~FeatureFlag"</automated>
  </verify>
  <acceptance_criteria>
    - SOURCE: FeatureFlagCatalog.cs Descriptions contains key `analysis.manabase.tap-analyzer` with a non-empty value.
    - SOURCE: PostgresSeedSql contains `('analysis.manabase.tap-analyzer', FALSE)`; SqliteSeedSql contains `('analysis.manabase.tap-analyzer', 0)`.
    - SOURCE: both seed blocks still end with `ON CONFLICT (key) DO NOTHING`.
    - SOURCE: README.md mentions `analysis.manabase.tap-analyzer` as OFF by default.
    - TEST: FeatureFlagCatalogTests + FeatureFlagStoreSeedTests tap-analyzer cases pass (registered + seeds FALSE in both dialects).
  </acceptance_criteria>
  <done>Flag registered with description and seeded OFF in SQLite + Postgres; operator-discoverable in README.</done>
</task>

<task type="auto">
  <name>Task 2: ManabaseAnalysisService — read flag + propagate ShowTapAnalyzer (+ flag-read tests)</name>
  <read_first>
    - DeckFlow.Web/Services/Manabase/ManabaseAnalysisService.cs (consts :133-180; flag reads :214-249; IsFlagOn :320; result ctor :287-291; ManabaseAnalysisResult :79-95)
    - DeckFlow.Web.Tests/Manabase/ManabaseAnalysisServiceTests.cs (FakeFeatureFlagCache flag-read analogs ~:398-443; "no cache → IsFlagOn false" pattern ~:340; GetResultCommanderCastabilityEnabled reflection helper ~:675)
    - DeckFlow.Web.Tests/Manabase/ManabaseViewModelTests.cs (PlainLanguageProperties default + round-trip facts ~:47-70; GetBoolProperty / GetOptionalProperty helpers)
    - 75-PATTERNS.md "ManabaseAnalysisService" section (flag read + propagation analog)
  </read_first>
  <action>
    Add `public const string TapAnalyzerFlagKey = "analysis.manabase.tap-analyzer";` alongside the existing flag-key consts. In AnalyzeAsync read `bool showTapAnalyzer = IsFlagOn(TapAnalyzerFlagKey);` next to the other flag reads (use IsFlagOn — fail-safe OFF; do NOT use any IsEnabled variant that defaults missing keys ON). Set `ShowTapAnalyzer = showTapAnalyzer` in the `new ManabaseAnalysisResult(...) { ... }` initializer next to CommanderCastabilityEnabled. Do not change IsFlagOn or any other flag logic. Then add automated coverage for the flag-read path (not source-only): in ManabaseAnalysisServiceTests.cs add a `GetResultShowTapAnalyzer` reflection helper mirroring `GetResultCommanderCastabilityEnabled`, and three facts using the FakeFeatureFlagCache constructor pattern — (1) NO cache / key absent → `ShowTapAnalyzer` false (fail-safe OFF), (2) `[ManabaseAnalysisService.TapAnalyzerFlagKey] = false` → false, (3) `[ManabaseAnalysisService.TapAnalyzerFlagKey] = true` → true. In ManabaseViewModelTests.cs add a fact that `new ManabaseViewModel()` has `ShowTapAnalyzer` false by default (via GetBoolProperty) and a round-trip fact that `new ManabaseViewModel { ShowTapAnalyzer = true }` reads true — mirroring the PlainLanguageProperties tests. No fenced code in action.
  </action>
  <verify>
    <automated>dotnet test DeckFlow.Web.Tests --filter "FullyQualifiedName~ManabaseAnalysisService|FullyQualifiedName~ManabaseViewModel"</automated>
  </verify>
  <acceptance_criteria>
    - SOURCE: `public const string TapAnalyzerFlagKey = "analysis.manabase.tap-analyzer";` present.
    - SOURCE: `bool showTapAnalyzer = IsFlagOn(TapAnalyzerFlagKey);` present in AnalyzeAsync.
    - SOURCE: ManabaseAnalysisResult initializer sets `ShowTapAnalyzer = showTapAnalyzer`.
    - NEGATIVE: the flag is read via `IsFlagOn` (not a default-ON path).
    - TEST: ManabaseAnalysisServiceTests covers tap flag absent/no-cache → false, explicit false → false, and true → ShowTapAnalyzer true.
    - TEST: ManabaseViewModelTests covers ShowTapAnalyzer default false + round-trip true.
    - BUILD: `dotnet build DeckFlow.sln` 0/0.
  </acceptance_criteria>
  <done>Service reads the flag fail-safe-OFF and carries it on the result; the flag-read fail-safe and viewmodel default are test-covered.</done>
</task>

<task type="auto">
  <name>Task 3: ManabaseController — download tap-gate + page ViewModel wiring</name>
  <read_first>
    - DeckFlow.Web/Controllers/ManabaseController.cs (ViewModel build :88-103; Download Build call :127-128)
    - DeckFlow.Web.Tests/Manabase/ManabaseControllerDownloadTests.cs (flag ON/OFF download facts from 75-01)
    - DeckFlow.Core/Manabase/ManabaseReportTextBuilder.cs (Build now appends the tap block when tap non-null — from 75-02)
  </read_first>
  <action>
    In the result-path ViewModel construction add `ShowTapAnalyzer = result.ShowTapAnalyzer,` after the ShowCommanderCastability assignment. In the Download action extend the `ManabaseReportTextBuilder.Build(...)` call with the new argument `tap: result.ShowTapAnalyzer ? result.Report.TapAnalysis : null` — the bool gate, NOT a bare `result.Report.TapAnalysis`, so OFF passes null and the artifact stays byte-identical. Do not change RunAnalysisAsync. No fenced code in action.
  </action>
  <verify>
    <automated>dotnet test DeckFlow.Web.Tests --filter "FullyQualifiedName~ManabaseControllerDownload"</automated>
  </verify>
  <acceptance_criteria>
    - SOURCE: ViewModel build sets `ShowTapAnalyzer = result.ShowTapAnalyzer`.
    - SOURCE: Download Build call passes `tap: result.ShowTapAnalyzer ? result.Report.TapAnalysis : null`.
    - TEST: Download_FlagOff fact passes (artifact has no "Untapped Sources:").
    - TEST: Download_FlagOn fact passes (artifact has "Untapped Sources:" + "Turn-1 untapped availability:").
    - BUILD: `dotnet build DeckFlow.sln` 0/0.
  </acceptance_criteria>
  <done>Page ViewModel carries the gate; download artifact gains the section only when the flag is ON.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → flag store | The flag is set only via the existing admin-gated /Admin flag surface (Basic Auth); no new input crosses to end users. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-75-03-I | Information Disclosure / Integrity | Artifact byte-identity broken when flag OFF | mitigate | Download passes `tap` via the `ShowTapAnalyzer ? ... : null` gate; Download_FlagOff test asserts no section; null tap appends zero bytes (75-02). |
| T-75-03-T | Tampering | Re-bootstrap overwriting an operator's enabled toggle | mitigate | Seed uses `ON CONFLICT (key) DO NOTHING`; existing FeatureFlagStoreSeed tests cover the contract. |
| T-75-03-E | Elevation/Confusion | Flag defaulting ON when key absent | mitigate | Read via `IsFlagOn` (Snapshot().TryGetValue → false on missing key); never the IsEnabled default-ON path. |

No package installs. No new auth surface (flag toggles ride the existing admin gate). No high-severity threats; none identified.
</threat_model>

<verification>
- `dotnet test DeckFlow.Web.Tests --filter "FullyQualifiedName~FeatureFlag"` → green.
- `dotnet test DeckFlow.Web.Tests --filter "FullyQualifiedName~ManabaseControllerDownload"` → green (OFF byte-clean, ON has section).
- `dotnet test DeckFlow.Web.Tests --filter "FullyQualifiedName~ManabaseAnalysisService|FullyQualifiedName~ManabaseViewModel"` → green (flag-read OFF/absent/ON + viewmodel default).
- `dotnet build DeckFlow.sln` → 0/0.
</verification>

<success_criteria>
- TAP-04: flag registered + seeded OFF in both dialects; OFF = byte-identical artifact (and page, once 75-04 lands the guarded card).
- Service reads fail-safe OFF; result + ViewModel + download path all carry the gate consistently.
- README documents the new flag.
</success_criteria>

<output>
Create `.planning/phases/75-tap-analyzer-surface/75-03-SUMMARY.md` when done.
</output>
