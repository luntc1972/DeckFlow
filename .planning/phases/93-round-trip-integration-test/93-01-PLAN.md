---
phase: 93-round-trip-integration-test
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - DeckFlow.Web.Tests/DeckFlow.Web.Tests.csproj
  - DeckFlow.Web.Tests/Integration/RoundTrip/RoundTripHarness.cs
  - DeckFlow.Web.Tests/Integration/RoundTrip/RoundTripSeams.cs
  - DeckFlow.Web.Tests/Integration/RoundTrip/RoundTripSmokeTests.cs
autonomous: true
requirements: [SYNC-16]

must_haves:
  truths:
    - "DeckFlow.Web.Tests compiles with ONE new ProjectReference to DeckFlow.Studio, reaching all three app assemblies (Core+Web+Studio); no circular reference exists because DeckFlow.Studio references only DeckFlow.Core (D-01)"
    - "The PG prod store is a REAL ContentSiteIndexStore over the Testcontainers container via the production IProdStoreFactory path (schema-ensure OFF); the harness pre-creates the schema ONCE by constructing a schema-ensuring ContentSiteIndexStore over the SAME RelationalDatabaseConnection and calling EnsureSchemaAsync (mirrors PostgresStorageTests) — D-02"
    - "A test-only git BOOTSTRAP helper prepares the temp repo — `git init`, `git config user.name/user.email`, a LOCAL BARE `origin` repo, an INITIAL commit + `push HEAD:refs/heads/{branch}` + upstream tracking so `origin/{branch}` EXISTS — BEFORE any coordinator runs. This is required because DeckFlow's `GitRepository` has NO `init`/`config` members, and DirectPush's `CommitAndPushBodiesAsync` calls `GetSubjectsAheadOfRemoteAsync(repoRoot,\"origin\",branch)` then `PushAsync`, which throw `DirectPushPushBlockedException` against a bare repo with no `origin/{branch}` (CH1/CH2). PRODUCTION workflow ops (stage/commit/push/behind-count) still go through the REAL `GitRepository`; only the one-time bootstrap (init/config/bare-origin/initial-push/tracking) uses a test-only git shell helper — this is the SOLE carve-out to the no-hand-rolled-ProcessStartInfo rule, and it touches NO production code (D-03)"
    - "STORE DATA-FLOW is explicit and two-store: distill writes the row to the LOCAL Studio store (SQLite) + artifact root ONLY — it NEVER writes prod. A published entry's PROD PG row is created for the first time by the reseed (`ContentKbSeedLoader`), and a DirectPush'd entry's by the DirectPush write path. The harness exposes `CreateLocalStore` (SQLite: distill + Publish-export SOURCE) and `CreateProdStore` (PG: reseed / DirectPush / serve TARGET) as DISTINCT instances, so the SC2 hash chain stays local-artifact → seed-json → PG-row → /app-served and the reseed 'reconstructs prod' proof is faithful (CH3/W3)"
    - "A deploy-copy materializes the committed working tree into a SEPARATE /app stand-in dir — exactly what Render's git checkout does — with `ContentKb:ContentBase` pointed at /app (D-03)"
    - "Prod READS (ReadAllAsync + ReadFlagAsync/TryReadFlagAsync) are served by a FixtureProdReader over the SAME real PG store, because the production ProdContentReader hardcodes Npgsql SslMode.Require and cannot reach the plain (no-TLS) container (D-02a); the prod DATA stays real Postgres, only the read TRANSPORT is a test adapter"
    - "The harness sets `DECKFLOW_REPO_ROOT` = the temp RepoRoot for the test lifetime and RESTORES the prior value in Dispose — `StudioRepoLocator` resolves the repo root from that env var (or CWD), NOT from ctor injection, so without it the coordinators would target the wrong tree (CM3)"
    - "All external effects are deterministic and network-free: a canned ILlmDistillationService yields a fixed body -> fixed body_sha256 (never the real Claude CLI, D-04), a recording ISshArtifactUploader satisfies the coordinator contract without SFTP (D-05), and an app-tree IDeployedBodyConfirmer confirms ONLY once the /app tree carries the matching body_sha256 (D-06)"
  artifacts:
    - path: "DeckFlow.Web.Tests/DeckFlow.Web.Tests.csproj"
      provides: "Single <ProjectReference> to DeckFlow.Studio (no new NuGet package — Testcontainers.PostgreSql already referenced)"
      contains: "DeckFlow.Studio.csproj"
    - path: "DeckFlow.Web.Tests/Integration/RoundTrip/RoundTripHarness.cs"
      provides: "Real git temp-repo lifecycle + deploy-copy to /app dir + PG schema pre-create + prod/local store construction + ContentKb:ContentBase config"
      contains: "EnsureSchemaAsync"
    - path: "DeckFlow.Web.Tests/Integration/RoundTrip/RoundTripSeams.cs"
      provides: "CannedLlmDistillationService + RecordingSshArtifactUploader + AppTreeDeployedBodyConfirmer + FixtureProdReader + FixtureProdStoreFactory (all over the real PG store)"
      contains: "FixtureProdReader"
    - path: "DeckFlow.Web.Tests/Integration/RoundTrip/RoundTripSmokeTests.cs"
      provides: "[PostgresFact] + IClassFixture<PostgresContainerFixture> boot proof: git init + PG schema + one canned distill producing a row with a non-null body_sha256"
      contains: "PostgresFact"
  key_links:
    - from: "DeckFlow.Web.Tests/Integration/RoundTrip/RoundTripHarness.cs"
      to: "PostgresContainerFixture.GetConnectionStringOrSkipAsync"
      via: "IClassFixture<PostgresContainerFixture> consumed by the smoke test, connection string threaded into the harness"
      pattern: "GetConnectionStringOrSkipAsync"
    - from: "DeckFlow.Web.Tests/Integration/RoundTrip/RoundTripSeams.cs FixtureProdReader/FixtureProdStoreFactory"
      to: "the real PG ContentSiteIndexStore"
      via: "both doubles wrap the SAME store instance (D-02a) — reads hit real Postgres, only SslMode transport is bypassed"
      pattern: "IProdContentReader|IProdStoreFactory"
    - from: "DeckFlow.Web.Tests/Integration/RoundTrip/RoundTripSeams.cs AppTreeDeployedBodyConfirmer"
      to: "the /app stand-in tree body_sha256"
      via: "recompute ContentSiteIndexContentSignature.ComputeBodySha256 over the /app body and compare to expected (D-06)"
      pattern: "ComputeBodySha256"
---

<objective>
Build the test-host wiring and the reusable round-trip HARNESS that Plan 93-02's SYNC-16 assertions run on — zero production-code change. Three deliverables:

1. **Test-host edge (D-01):** add ONE `<ProjectReference Include="..\DeckFlow.Studio\DeckFlow.Studio.csproj" />` to `DeckFlow.Web.Tests.csproj`. Web.Tests already references Web(→Core) and carries `Testcontainers.PostgreSql` + `PostgresContainerFixture`; the Studio edge is the only missing link to reach the DirectPush/Pull/Reconcile/Publish coordinators. No new NuGet package. Verify no circular reference (Studio references only Core).
2. **The harness (D-02/D-02a/D-03):** a `RoundTripHarness` that (a) pre-creates the PG schema once via a schema-ensuring `ContentSiteIndexStore`, then hands out prod/local stores over the SAME `RelationalDatabaseConnection` via the schema-ensure-OFF factory path; (b) drives a real `git init` temp repo through the real `GitRepository` with deterministic headless commits, and copies the committed tree into a separate `/app` stand-in dir with `ContentKb:ContentBase` pointed at it.
3. **The seams (D-04/D-05/D-06):** a canned `ILlmDistillationService`, a recording `ISshArtifactUploader`, an app-tree `IDeployedBodyConfirmer`, and a `FixtureProdReader`/`FixtureProdStoreFactory` pair over the real PG store — all deterministic, all network-free.

A minimal `[PostgresFact]` smoke test proves the harness boots so 93-01 is independently verifiable.

Purpose: isolate the heavy infrastructure (real PG schema, real git, transport doubles) from the assertion logic so each plan stays inside its context budget; 93-02 then reads as a straight walk of the loop.
Output: extended `DeckFlow.Web.Tests.csproj`, new `RoundTripHarness.cs`, `RoundTripSeams.cs`, `RoundTripSmokeTests.cs` under `DeckFlow.Web.Tests/Integration/RoundTrip/`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/93-round-trip-integration-test/93-CONTEXT.md

<interfaces>
<!-- Contracts the harness constructs/implements. Extracted from the codebase — no exploration needed. -->

PG container fixture already built (DeckFlow.Web.Tests/Integration/PostgresContainerFixture.cs) — CONSUME via IClassFixture:
  public async Task<string> GetConnectionStringOrSkipAsync();   // auto-skips unless DECKFLOW_POSTGRES_TESTS=1 + Docker up
  // PostgresFactAttribute.cs discovery-time-skips the same way. Copy the PostgresStorageTests IClassFixture pattern verbatim.

PG store construction precedent (DeckFlow.Web.Tests/Integration/PostgresStorageTests.cs:23-24):
  private static RelationalDatabaseConnection CreateConnection(string connectionString)
      => new(RelationalDatabaseProvider.Postgres, connectionString);   // namespace DeckFlow.Core.Storage

ContentSiteIndexStore ctors (DeckFlow.Core/Content/ContentSiteIndexStore.cs:25,37,71):
  public ContentSiteIndexStore(string databasePath);                                   // SQLite — NOT used here
  public ContentSiteIndexStore(RelationalDatabaseConnection connectionInfo, bool ensureSchemaEnabled = true);
  public async Task EnsureSchemaAsync(CancellationToken ct = default);
  // D-02: build ONE store with ensureSchemaEnabled:true over the PG conn, call EnsureSchemaAsync ONCE (pre-create),
  //       then hand the FACTORY store (ensure OFF) to coordinators — mirrors ProdStoreFactory.Create in prod.

Production prod-store factory to MIRROR (DeckFlow.Studio/Services/IProdStoreFactory.cs:9-22):
  public interface IProdStoreFactory { IContentSiteIndexStore Create(string connectionString); }
  public sealed class ProdStoreFactory : IProdStoreFactory {
      public IContentSiteIndexStore Create(string connectionString)
          => new ContentSiteIndexStore(new RelationalDatabaseConnection(...Postgres...), ensureSchemaEnabled:false); }
  // The FixtureProdStoreFactory returns the SAME real PG store instance regardless of connectionString.

Prod-read seam the coordinators call (DeckFlow.Studio/Services/IProdContentReader.cs — the FIXTURE precedent is
DeckFlow.Studio.Tests/ViewModels/ReconcileFixtureDriveTests.cs:276-289, repoint from SQLite to the PG store):
  Task<IReadOnlyList<ContentSiteIndexRow>> ReadAllAsync(string connectionString, CancellationToken ct = default);
  Task<bool> ReadFlagAsync(string connectionString, string key, CancellationToken ct = default);       // DirectPush Stage-4 fail-closed read
  Task<bool?> TryReadFlagAsync(string connectionString, string key, CancellationToken ct = default);    // tri-state
  // FixtureProdReader delegates ReadAllAsync -> store.GetAllRowsAsync; flag reads return a settable bool? (D-02a).

Real git seam to DRIVE for PRODUCTION ops (do NOT use FakeGitRepository — D-03):
  DeckFlow.Core/Integration/GitRepository.cs implements IGitRepository (shell-out via BuildStartInfo, sets
  GIT_TERMINAL_PROMPT=0). It has StageAndCommitAsync/PushAsync/FetchAsync/GetBehindCountAsync/GetSubjectsAheadOfRemoteAsync
  — but NO `init` and NO `config`. WSL has git.
  CRITICAL (CH1/CH2): the temp repo must be BOOTSTRAPPED before any coordinator op:
    (a) `git init` the RepoRoot; (b) `git config user.name`/`user.email` (deterministic identity — a bare WSL/CI env has none);
    (c) create a LOCAL BARE `origin` (`git init --bare` in a sibling temp dir) and `git remote add origin <bare>`;
    (d) make an INITIAL commit and `git push -u origin {branch}` (or push HEAD:refs/heads/{branch} + set tracking) so
        `origin/{branch}` EXISTS — otherwise DirectPush.CommitAndPushBodiesAsync (DirectPushCoordinator.cs:405-425:
        GetSubjectsAheadOfRemoteAsync(...,\"origin\",branch) → PushAsync) throws DirectPushPushBlockedException.
  GitRepository cannot do init/config, so implement these bootstrap steps in a TEST-ONLY git shell helper (a small
  ProcessStartInfo runner scoped to `Integration/RoundTrip/`, ArgumentList-only, GIT_TERMINAL_PROMPT=0). This helper is
  the ONLY permitted hand-rolled git invocation and is confined to one-time bootstrap; every subsequent stage/commit/
  push/behind-count in the loop goes through the REAL GitRepository. Honor ArtifactPathSafety for all path use.

Distiller seam to CAN (DeckFlow.Core/Integration/ILlmDistillationService.cs:8-42; ThrowingLlmDistillationService is the
double precedent):
  Task<SummaryResult> SummarizeAsync(string transcript, CancellationToken ct = default);
  Task<ClassificationResult> ClassifyAsync(string body, CancellationToken ct = default);   // W-1: default impl THROWS
  Task<ClipsResult> ExtractClipsAsync(string transcript, CancellationToken ct = default);
  Task<TagsResult> InferTagsAsync(string transcript, CancellationToken ct = default);
  // W-1 CRITICAL: ContentKbOrchestrator.DistillVideoAsync calls _distiller.ClassifyAsync UNCONDITIONALLY (~:1186) and
  // DROPS/deletes the row when Verdict=="drop". The canned impl MUST implement ClassifyAsync returning a NON-drop
  // Verdict (e.g. "keep") or the row is deleted and the whole loop is vacuous. It must ALSO populate `.Usage` on
  // SummaryResult/ClipsResult/TagsResult (cost math at ~:1224/:1252/:1279 NREs on a null Usage). Canned SummarizeAsync
  // returns a FIXED body so ComputeBodySha256 is deterministic; clips/tags fixed/empty.
  // The distill hop needs REAL inputs: a seeded IContentSourceStore + IContentVideoStore row PLUS a transcript
  // (DistillVideoAsync reads GetLatestTranscriptAsync ~:1181) — seed these in the smoke/round-trip, not just "a video".
  // The distill hop is proven by ContentArtifactWriter + ComputeBodySha256 + UpsertContentColumnsOnlyAsync
  // (ContentKbOrchestrator.cs:1336/1357/1358) against the LOCAL store, NOT by LLM output quality.
  // (Read ILlmDistillationService.cs + ContentKbOrchestrator.cs:1181-1290 for the EXACT result-record shapes/Usage props.)

Repo-root resolution (CM3) — NOT ctor-injected (DeckFlow.Studio/Services/StudioRepoLocator.cs:21):
  StudioRepoLocator resolves the repo root from env `DECKFLOW_REPO_ROOT` (fallback Directory.GetCurrentDirectory()).
  The harness MUST set DECKFLOW_REPO_ROOT = RepoRoot for the test lifetime and restore the prior value in Dispose,
  or the coordinators will operate on the wrong tree.

Confirmer key→/app-path mapping (CM2/Q5) — the authoritative map is the PROD ROW's ArtifactPath, mirroring
DeckFlow.Web/Controllers/Admin/ContentKbDeployedBodyController.cs:71-77:
  GetByNaturalKeyAsync(type,value) → guard row.ArtifactPath StartsWith "content-kb/" → resolve under ContentBase(=/app)
  → read → ComputeBodySha256. AppTreeDeployedBodyConfirmer must therefore look up the prod PG row by natural key,
  take row.ArtifactPath, resolve it under AppRoot, read + hash, and FAIL CLOSED (return false) if the row or /app body
  is missing — never a false confirm.

SFTP seam to RECORD (DeckFlow.Studio/Services/ISshArtifactUploader.cs:9-24):
  Task<IReadOnlyList<SshUploadResult>> UploadArtifactsAsync(
      IReadOnlyList<SshUploadRequest> uploads, IProgress<SshUploadResult>? progress = null, CancellationToken ct = default);
  public sealed record SshUploadRequest(string LocalPath, string RemoteRelativePath);
  // Recording double captures uploads, returns a success SshUploadResult per entry; under directpush-gitbody ON the
  // /data overlay is out of the serving path (D-05) so no real transfer is needed.

Deploy-confirm seam to FAKE (DeckFlow.Studio/Services/IDeployedBodyConfirmer.cs:12-29):
  Task<bool> IsDeployedBodyConfirmedAsync(string naturalKeyType, string naturalKeyValue,
      string expectedBodySha256, CancellationToken cancellationToken);
  // AppTreeDeployedBodyConfirmer: read the /app body for the natural key, recompute ComputeBodySha256,
  // return true IFF it ordinal-equals expectedBodySha256; else false (D-06) — keeps HTTP/BasicAuth out.

Body-hash surface (THE one surface — never re-hash with a second scheme):
  DeckFlow.Core/Content/ContentSiteIndexContentSignature.ComputeBodySha256(string):131  (header-strip + LF-normalize + UTF-8 SHA-256)

The end-to-end DRIVER precedent to model the harness shape on (real store + real git-style tree + real seed +
FixtureProdReader/FixtureProdStoreFactory transport doubles): DeckFlow.Studio.Tests/ViewModels/ReconcileFixtureDriveTests.cs
(this plan upgrades it: SQLite prod -> real PG (D-02); FakeGitRepository -> real GitRepository + git init (D-03)).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add DeckFlow.Studio ProjectReference + PG schema pre-create path (D-01, D-02)</name>
  <files>DeckFlow.Web.Tests/DeckFlow.Web.Tests.csproj, DeckFlow.Web.Tests/Integration/RoundTrip/RoundTripHarness.cs</files>
  <read_first>
    - DeckFlow.Web.Tests/DeckFlow.Web.Tests.csproj (the existing single <ProjectReference> to DeckFlow.Web at :21-23 — match its indentation/style)
    - DeckFlow.Studio/DeckFlow.Studio.csproj (confirm it references ONLY DeckFlow.Core — no Web edge => no cycle)
    - DeckFlow.Web.Tests/Integration/PostgresStorageTests.cs:14-31 (IClassFixture pattern + CreateConnection helper)
    - DeckFlow.Core/Content/ContentSiteIndexStore.cs:25-71 (both ctors + EnsureSchemaAsync)
    - DeckFlow.Studio/Services/IProdStoreFactory.cs (the schema-ensure-OFF factory path to mirror)
  </read_first>
  <behavior>
    - Adding the Studio ProjectReference keeps the solution at 0 warnings / 0 errors and introduces no circular reference.
    - Over a fresh PG container connection, constructing a schema-ensuring ContentSiteIndexStore + EnsureSchemaAsync creates the content-site-index schema exactly once; a subsequent schema-ensure-OFF store over the same connection reads/writes rows without re-running DDL.
  </behavior>
  <action>
    Add one `<ProjectReference Include="..\DeckFlow.Studio\DeckFlow.Studio.csproj" />` to the existing `<ItemGroup>` that holds the DeckFlow.Web reference in `DeckFlow.Web.Tests.csproj` (match the file's existing element style; LF endings; touch only the added line). Do NOT add any NuGet `<PackageReference>` — Testcontainers.PostgreSql is already present. After editing, confirm DeckFlow.Studio.csproj references only DeckFlow.Core so no cycle is created (Web.Tests -> {Web, Studio}, both -> Core, is acyclic).

    Create `DeckFlow.Web.Tests/Integration/RoundTrip/RoundTripHarness.cs` (namespace `DeckFlow.Web.Tests.Integration.RoundTrip`) and add the PG-store plumbing this task owns (the git + config members land in Task 2):
    - A `CreateConnection(string connectionString)` helper returning `new RelationalDatabaseConnection(RelationalDatabaseProvider.Postgres, connectionString)` (mirror PostgresStorageTests:23-24; `using DeckFlow.Core.Storage;`).
    - An async `EnsureProdSchemaAsync(string connectionString)` that constructs a schema-ensuring `ContentSiteIndexStore(CreateConnection(connectionString), ensureSchemaEnabled: true)` and awaits `EnsureSchemaAsync()` ONCE — this is the D-02 pre-create. Add a `// Why:` comment: the production ProdStoreFactory store runs schema-ensure OFF, so the schema must exist before it is used.
    - A `CreateProdStore(string connectionString)` returning `new ContentSiteIndexStore(CreateConnection(connectionString), ensureSchemaEnabled: false)` — the exact prod factory shape (D-02) — plus a `CreateLocalStore(...)` for the Studio-local SQLite store used by the coordinators (temp .db file, mirror ReconcileFixtureDriveTests local-store construction).

    Preserve LF; changed-lines-only formatting; this is TEST code, so new .cs lines must satisfy the changed-lines format gate.
  </action>
  <verify>
    <automated>dotnet.exe build DeckFlow.sln (0 warnings / 0 errors — the Studio ProjectReference resolves and no cycle is reported)</automated>
  </verify>
  <acceptance_criteria>
    - DeckFlow.Web.Tests.csproj contains exactly one new `<ProjectReference>` to `..\DeckFlow.Studio\DeckFlow.Studio.csproj` and NO new `<PackageReference>`.
    - DeckFlow.Studio.csproj is confirmed to reference only DeckFlow.Core (grep: its only `<ProjectReference>` targets DeckFlow.Core) — no circular reference.
    - RoundTripHarness.cs exposes EnsureProdSchemaAsync (schema-ensuring store + EnsureSchemaAsync) and CreateProdStore (ensureSchemaEnabled:false) over a Postgres RelationalDatabaseConnection.
    - `dotnet.exe build DeckFlow.sln` is 0/0.
  </acceptance_criteria>
  <done>The Studio edge is wired with no cycle and the PG prod-store schema pre-create + factory path exists; solution builds 0/0.</done>
</task>

<task type="auto">
  <name>Task 2: Real-git temp-repo + /app deploy-copy + deterministic seams (D-03, D-04, D-05, D-06, D-02a)</name>
  <files>DeckFlow.Web.Tests/Integration/RoundTrip/RoundTripHarness.cs, DeckFlow.Web.Tests/Integration/RoundTrip/RoundTripSeams.cs</files>
  <read_first>
    - DeckFlow.Studio.Tests/ViewModels/ReconcileFixtureDriveTests.cs (whole file — the driver precedent: on-disk repo layout, seed json, FixtureProdReader/FixtureProdStoreFactory at :276-294, Dispose cleanup)
    - DeckFlow.Core/Integration/GitRepository.cs (the real IGitRepository impl the harness drives; note BuildStartInfo sets GIT_TERMINAL_PROMPT=0)
    - DeckFlow.Core/Integration/ILlmDistillationService.cs (SummarizeAsync/ExtractClipsAsync/InferTagsAsync + result records to can) and any ThrowingLlmDistillationService double for shape
    - DeckFlow.Studio/Services/ISshArtifactUploader.cs (UploadArtifactsAsync + SshUploadRequest/SshUploadResult records)
    - DeckFlow.Studio/Services/IDeployedBodyConfirmer.cs (IsDeployedBodyConfirmedAsync signature + contract)
    - DeckFlow.Core/Content/ContentSiteIndexContentSignature.cs:131 (ComputeBodySha256 — the one hash surface the confirmer recomputes)
    - DeckFlow.Web/Services/Content/ContentKbArtifactPathResolver.cs (SeedFilePath + ContentKb:ContentBase resolution — the /app dir the resolver reads)
  </read_first>
  <behavior>
    - `git init` in a fresh temp dir + a per-repo identity config lets the real GitRepository commit headlessly and deterministically (no interactive prompt, no host global git identity dependency).
    - Deploy-copy reproduces the committed working tree into a distinct /app dir; the web resolver's ContentKb:ContentBase points at that dir, NOT the repo root — proving /app (git checkout), not /data, is the serving base under directpush-gitbody ON.
    - CannedLlmDistillationService.SummarizeAsync returns a byte-stable body for a given input, so ComputeBodySha256 over the written artifact is deterministic across runs.
    - AppTreeDeployedBodyConfirmer returns true ONLY when the /app body for the natural key recomputes to the expected body_sha256; a not-yet-deployed or mismatched /app body returns false (never a false confirm).
    - FixtureProdReader.ReadAllAsync/ReadFlagAsync/TryReadFlagAsync serve from the SAME real PG store; the settable flag lets a test drive directpush-gitbody ON.
  </behavior>
  <action>
    In `RoundTripHarness.cs` add the git + deploy + config members (namespace `DeckFlow.Web.Tests.Integration.RoundTrip`), following the ReconcileFixtureDriveTests on-disk-fixture shape:
    - A temp `RepoRoot` (`Path.Combine(Path.GetTempPath(), $"roundtrip-{Guid.NewGuid():N}")`), a distinct `AppRoot` (`.../roundtrip-app-{stamp}`), and a distinct BARE `OriginRoot` (`.../roundtrip-origin-{stamp}.git`); create the `content-kb/{channel}` and `content-kb/seed` subdirs under RepoRoot.
    - `InitRepoAsync()` (CH1/CH2 bootstrap via the TEST-ONLY git shell helper — GitRepository has no init/config): `git init` RepoRoot; `git config user.name`/`user.email` (deterministic identity); `git init --bare` OriginRoot + `git remote add origin <OriginRoot>`; write a placeholder file, make an INITIAL commit, and `git push -u origin {branch}` so `origin/{branch}` EXISTS before any coordinator push. Add a `// Why:` note: a fresh WSL/CI env has no git identity AND a bare temp repo has no `origin/{branch}`, which would trip DirectPush's `GetSubjectsAheadOfRemoteAsync`/`PushAsync` (DirectPushPushBlockedException). This helper is ArgumentList-only, GIT_TERMINAL_PROMPT=0, scoped to `Integration/RoundTrip/`, and is the ONLY hand-rolled git invocation; the loop's stage/commit/push all go through the REAL GitRepository afterward.
    - `SetRepoRootEnv()` / restore in Dispose (CM3): set `DECKFLOW_REPO_ROOT` = RepoRoot for the test lifetime (capture + restore the prior value in Dispose) — `StudioRepoLocator` reads this env var, not a ctor arg, so without it the coordinators target the wrong tree.
    - `DeployToAppAsync()`: copy the committed working tree (the `content-kb/**` files as of the last commit) from RepoRoot into `AppRoot` — this simulates the Render git checkout. Honor ArtifactPathSafety for path joins.
    - `BuildConfiguration()`: an `IConfiguration` (in-memory) setting `ContentKb:ContentBase` to `AppRoot` (web body resolution reads /app) and `Studio:ProdConnectionString` to a fixture-ignored placeholder (mirror ReconcileFixtureDriveTests:85-90).
    - `Dispose`: restore `DECKFLOW_REPO_ROOT`, clear SQLite pools, delete temp .db files, recursively delete RepoRoot + AppRoot + OriginRoot (mirror ReconcileFixtureDriveTests:57-74).

    Create `DeckFlow.Web.Tests/Integration/RoundTrip/RoundTripSeams.cs` with the deterministic test doubles (all `internal sealed`):
    - `CannedLlmDistillationService : ILlmDistillationService` — SummarizeAsync returns a FIXED SummaryResult body (derive it deterministically from the input token so it is stable); **ClassifyAsync returns a NON-drop Verdict (e.g. "keep")** so DistillVideoAsync (~:1186) does NOT delete the row (W-1); ExtractClipsAsync/InferTagsAsync return fixed/empty results. **Populate `.Usage` on every result record** (Summary/Clips/Tags) so the cost math (~:1224/:1252/:1279) does not NRE. Never invokes any process (D-04). Read ILlmDistillationService.cs for the exact result-record + Usage shapes.
    - `RecordingSshArtifactUploader : ISshArtifactUploader` — records each SshUploadRequest into a public list and returns a success SshUploadResult per entry; performs no real transfer (D-05).
    - `AppTreeDeployedBodyConfirmer : IDeployedBodyConfirmer` — (CM2/Q5) looks up the PROD PG row by natural key (`store.GetByNaturalKeyAsync`), takes `row.ArtifactPath`, resolves it under AppRoot (mirror ContentKbDeployedBodyController.cs:71-77 / ContentKbArtifactPathResolver), reads the /app body, recomputes `ContentSiteIndexContentSignature.ComputeBodySha256`, and returns true IFF it ordinal-equals `expectedBodySha256`; **FAIL CLOSED (false) if the row or /app body is missing** — never a false confirm (D-06). Add a `// Why:` note: a hash match against the deployed /app tree is the only correctness proof, keeping HTTP/BasicAuth out of the test.
    - `FixtureProdReader : IProdContentReader` and `FixtureProdStoreFactory : IProdStoreFactory` — lift the pattern from ReconcileFixtureDriveTests:276-294 verbatim, but wrapping the REAL PG `ContentSiteIndexStore` (D-02a). ReadAllAsync delegates to the store's read-all; ReadFlagAsync/TryReadFlagAsync return a settable `bool? Flag`.

    Preserve LF; changed-lines-only formatting.
  </action>
  <verify>
    <automated>dotnet.exe build DeckFlow.sln (0 warnings / 0 errors — harness + seams compile against the real interfaces)</automated>
  </verify>
  <acceptance_criteria>
    - RoundTripHarness bootstraps the temp repo (init/config/bare-origin/initial-push/tracking) so `origin/{branch}` exists, then drives the REAL GitRepository for all loop stage/commit/push (grep: no FakeGitRepository in the RoundTrip folder; the ONLY hand-rolled `ProcessStartInfo` is the scoped test-only git bootstrap helper — every other git op routes through GitRepository), sets+restores `DECKFLOW_REPO_ROOT`, and copies the committed tree into a distinct AppRoot with `ContentKb:ContentBase` = AppRoot.
    - RoundTripSeams contains CannedLlmDistillationService (implements ClassifyAsync→non-drop + populates .Usage, no process launch), RecordingSshArtifactUploader (records + returns success), AppTreeDeployedBodyConfirmer (looks up prod row ArtifactPath → resolves under AppRoot → hash-match-only via ComputeBodySha256, fail-closed if missing), and FixtureProdReader/FixtureProdStoreFactory over the real PG store.
    - No second hashing scheme is introduced — the confirmer uses ContentSiteIndexContentSignature.ComputeBodySha256 (grep: no `SHA256.Create()`/`HashData` in the RoundTrip folder outside a call to ComputeBodySha256).
    - `dotnet.exe build DeckFlow.sln` is 0/0.
  </acceptance_criteria>
  <done>The harness drives a real git temp repo + /app deploy-copy, and all four seams are deterministic and network-free; solution builds 0/0.</done>
</task>

<task type="auto">
  <name>Task 3: Harness boot smoke [PostgresFact] (D-02, D-03, D-07 gate behavior)</name>
  <files>DeckFlow.Web.Tests/Integration/RoundTrip/RoundTripSmokeTests.cs</files>
  <read_first>
    - DeckFlow.Web.Tests/Integration/PostgresStorageTests.cs:14-24 (IClassFixture<PostgresContainerFixture> + fixture field ctor)
    - DeckFlow.Web.Tests/Integration/PostgresFactAttribute.cs (the discovery-time skip attribute)
    - DeckFlow.Core/Orchestration/ContentKbOrchestrator.cs:238 (DistillAsync entry) and DeckFlow.CLI/ContentKbCommandRunners.cs:80 (RunDistillAsync — the real-orchestrator-over-a-store wiring precedent for the 14 ctor deps)
    - RoundTripHarness.cs + RoundTripSeams.cs (the types this smoke test boots — from Tasks 1-2)
  </read_first>
  <behavior>
    - With DECKFLOW_POSTGRES_TESTS unset (or Docker absent) the fact SKIPS — no container, no failure — matching every existing PG test (D-07 CI auto-skip is automatic).
    - With the flag set + Docker up: EnsureProdSchemaAsync creates the PG schema, InitRepoAsync bootstraps the temp repo (init/config/bare-origin/initial-push), and one canned distill run over a seeded source+video+transcript writes an artifact + upserts a row in the LOCAL store whose body_sha256 is non-null and equals ComputeBodySha256 over the written body. (Distill writes LOCAL, NOT prod — the prod PG row only appears after Publish→reseed in 93-02; CH3/W3.)
  </behavior>
  <action>
    Create `DeckFlow.Web.Tests/Integration/RoundTrip/RoundTripSmokeTests.cs` — a `public sealed class RoundTripSmokeTests : IClassFixture<PostgresContainerFixture>` with a single `[PostgresFact]` `Harness_Boots_Distill_Git_And_Postgres_Schema`. It should:
    - Get the connection string via `_fixture.GetConnectionStringOrSkipAsync()`, call `harness.EnsureProdSchemaAsync(conn)`, and `await harness.InitRepoAsync()` (bootstraps init/config/bare-origin/initial-push so the loop can later push).
    - Wire the REAL `ContentKbOrchestrator` over the harness **LOCAL** store + `CannedLlmDistillationService` (use the CLI `RunDistillAsync` wiring as the reference for the 14 ctor deps; back the non-distill stores with the local SQLite/in-memory doubles already available in the test assemblies — do NOT stub the artifact-write/hash/upsert hop, which is what is being proven). Seed the distill INPUTS the orchestrator reads: an `IContentSourceStore` source row + an `IContentVideoStore` video row (pending) + a transcript (`GetLatestTranscriptAsync` ~:1181); the canned `ClassifyAsync` returns non-drop so the row survives. Run distill for that single video.
    - Assert the LOCAL store row exists, its `body_sha256` is non-null, and it equals `ContentSiteIndexContentSignature.ComputeBodySha256` recomputed over the written artifact body (the distill-computed == local-row hop — the prod PG row does not exist yet; it is created by reseed in 93-02). Keep the failure message specific.
    - Add a class-level XML doc stating this is the harness boot proof for the SYNC-16 round-trip; note that the full loop lives in 93-02.

    If wiring the real ContentKbOrchestrator's non-distill dependencies would require a PRODUCTION code change to make a store/service injectable, STOP and record it as an open question in the SUMMARY rather than editing production code (hard constraint: zero production-code change this phase).

    Preserve LF; changed-lines-only formatting.
  </action>
  <verify>
    <automated>dotnet.exe build DeckFlow.sln (0 warnings / 0 errors). NOTE: VSTest is unreliable in WSL — the operator runs `DECKFLOW_POSTGRES_TESTS=1 dotnet.exe test DeckFlow.Web.Tests --filter FullyQualifiedName~RoundTripSmoke` locally with Docker up; without the flag the fact auto-skips.</automated>
  </verify>
  <acceptance_criteria>
    - RoundTripSmokeTests uses `[PostgresFact]` + `IClassFixture<PostgresContainerFixture>` and auto-skips when DECKFLOW_POSTGRES_TESTS!=1 (D-07).
    - The fact drives the REAL ContentKbOrchestrator distill hop (canned distiller only, over a seeded source+video+transcript) and asserts LOCAL-row body_sha256 == ComputeBodySha256 over the written body (prod PG row not yet created).
    - No production .cs file is modified (git diff shows only the csproj + RoundTrip/*.cs test files); if injectability was missing, the SUMMARY records it as an open question instead.
    - `dotnet.exe build DeckFlow.sln` is 0/0.
  </acceptance_criteria>
  <done>The harness boots end-to-end over real PG + real git with a real distill hop; the smoke fact auto-skips without Docker and passes locally with it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test host → `git` CLI | temp-repo remote/branch/path names become subprocess arguments (via the real GitRepository) |
| Test host → Postgres container | DDL + row round-trip over a throwaway container connection string |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-93-01 | Tampering | RoundTripHarness git drive | mitigate | Loop git ops route through GitRepository/BuildStartInfo (ArgumentList-only, GIT_TERMINAL_PROMPT=0). The one carve-out — the test-only bootstrap helper (init/config/bare-origin/push) GitRepository can't do — is ALSO ArgumentList-only + GIT_TERMINAL_PROMPT=0, scoped to Integration/RoundTrip/, and never shell-interpolates paths; honor ArtifactPathSafety for tree/deploy path joins. All paths are throwaway temp dirs |
| T-93-02 | Information Disclosure | PG container connection string | accept | Throwaway Testcontainers container (postgres:16-alpine, ephemeral creds `deckflow`/`deckflow`), disposed with the fixture; no prod credential involved |
| T-93-SC | Tampering | npm/pip/cargo installs | accept | No package installs this phase — the only edge is a `<ProjectReference>` to an in-solution project (D-01); Testcontainers.PostgreSql already referenced. Nothing to verify |
</threat_model>

<verification>
- `dotnet.exe build DeckFlow.sln` → 0 warnings / 0 errors (Studio ProjectReference resolves, no circular reference, harness + seams + smoke compile).
- DeckFlow.Studio.csproj references only DeckFlow.Core (no cycle).
- Operator (Docker up): `DECKFLOW_POSTGRES_TESTS=1 dotnet.exe test DeckFlow.Web.Tests --filter FullyQualifiedName~RoundTripSmoke` → green; without the flag the fact auto-skips.
- No production .cs file modified; EOL clean (LF); changed-lines format gate clean.
</verification>

<success_criteria>
- Web.Tests reaches Core+Web+Studio via one new ProjectReference with no circular reference (D-01).
- The harness pre-creates the PG schema once and hands out real prod/local stores over the same connection (D-02); prod reads are fronted by a fixture reader over the real PG store (D-02a).
- A real `git init` temp repo is bootstrapped (identity + bare `origin` + tracking so DirectPush push doesn't block), driven by the real GitRepository for loop ops, deploy-copies into a distinct /app dir with ContentKb:ContentBase pointed at it, and `DECKFLOW_REPO_ROOT` is set/restored (D-03, CH1/CH2/CM3).
- Canned distiller (ClassifyAsync→non-drop + .Usage), recording SFTP, and app-tree deploy-confirmer (prod-row ArtifactPath → /app hash, fail-closed) are deterministic and network-free (D-04/D-05/D-06).
- Distill writes the LOCAL store only; the smoke `[PostgresFact]` boots the harness (real PG schema + real git + real distill hop → local row) and auto-skips without Docker (D-07); zero production-code change.
</success_criteria>

<output>
Create `.planning/phases/93-round-trip-integration-test/93-01-SUMMARY.md` when done.
</output>
