---
phase: 103-simulation-engine-guided-cut-rounds
plan: 10
type: execute
wave: 7
depends_on: ["103-08", "103-09"]
files_modified:
  - DeckFlow.Web/e2e/cut-lab-structure.spec.ts
autonomous: false
requirements: [CUT-01, CUT-02, CUT-03, SIM-01, SIM-02]
must_haves:
  truths:
    - "CUT-01/CUT-03: an e2e walks an oversized pool through at least one accepted cut — the proposal card, sticky-bar count decrement, round banner, and cuts-made row all update without a page reload."
    - "SIM-02: the Compare-to-baseline panel opens and shows a Metric·Baseline·Current·Delta table."
    - "CUT-02: the delta/proposal copy contains no verdict language (assert absence of 'worse'/'bad'/'better')."
    - "The full C# + Vitest + tsc suite is green and the cut-lab e2e specs pass across the theme×viewport matrix."
    - "A manual human-verify captures delta-panel readability + neutral framing and the perceived accept/reject/defer latency (~1s target / 3s cap)."
  artifacts:
    - path: "DeckFlow.Web/e2e/cut-lab-structure.spec.ts"
      provides: "rounds/proposal/decision/compare e2e coverage extending the existing spec"
      contains: "cutlab-proposal"
  key_links:
    - from: "DeckFlow.Web/e2e/cut-lab-structure.spec.ts"
      to: "the flag-gated /cut-lab page + /api/cut-lab/decide"
      via: "importPool helper + accept-cut interaction + compare-panel open"
      pattern: "importPool"
---

<objective>
Prove the whole Phase 103 loop end-to-end and gate the phase: extend the existing Cut Lab e2e spec to import an oversized pool, accept a cut through the async endpoint, verify the sticky-bar/round-banner/cuts-made updates and the compare panel, assert copy neutrality (CUT-02), capture theme×viewport screenshots, and run the full suite green. Close with the two manual human-verify checks the validation strategy requires.

Purpose: This is the phase's verification gate — everything below the async loop must hold together in a real browser and the copy must stay judgment-free.
Output: Extended e2e spec + a green full-suite run + human-verify sign-off recorded in the SUMMARY.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/cut-lab/phases/103-simulation-engine-guided-cut-rounds/103-VALIDATION.md
@.planning/workstreams/cut-lab/phases/103-simulation-engine-guided-cut-rounds/103-UI-SPEC.md
@.planning/workstreams/cut-lab/phases/103-simulation-engine-guided-cut-rounds/103-PATTERNS.md

<interfaces>
Existing e2e — DeckFlow.Web/e2e/cut-lab-structure.spec.ts: Playwright, test.describe.configure({ mode: 'serial' }), acquireAdminLockForTest/releaseAdminLockForTest for the admin-gated flag lock, importPool(...) helper filling #cut-lab-deck-text, three-theme × two-viewport screenshot matrix. EXTEND this file — do not create a new one.
Test infra (103-VALIDATION): quick = "/mnt/c/Program Files/dotnet/dotnet.exe" test DeckFlow.Web.Tests --filter "FullyQualifiedName~CutLab"; full = "/mnt/c/Program Files/dotnet/dotnet.exe" test DeckFlow.sln + npm test + npx tsc --noEmit. UI server via scripts/run-web-test.sh (DECKFLOW_DISABLE_AUTO_BROWSER=true); e2e via npx --no-install playwright test — NEVER open a browser on the Windows host. WSL headless: env -u DISPLAY -u WAYLAND_DISPLAY.
Stale-server caution (project memory): probe port 5173 before e2e to avoid a stale Windows server.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend cut-lab-structure.spec.ts with the rounds/decision/compare flow</name>
  <read_first>
    - DeckFlow.Web/e2e/cut-lab-structure.spec.ts (importPool helper, admin-lock acquire/release, serial mode, theme×viewport screenshot loop)
    - DeckFlow.Web/playwright.config.ts (projects/viewports)
    - .planning/workstreams/cut-lab/phases/103-simulation-engine-guided-cut-rounds/103-UI-SPEC.md (selectors: .cutlab-sticky-bar, .cutlab-proposal, .cutlab-decision-btn--accept, .cutlab-cuts-made, .cutlab-compare; expected copy)
  </read_first>
  <action>
    Extend `cut-lab-structure.spec.ts` (reuse `importPool` + the admin-flag lock + serial mode) with a test that: imports an oversized (>100) pool, waits for the Cut rounds section, asserts the round banner + sticky bar ("N to cut") render; clicks `.cutlab-decision-btn--accept` on the first proposal and asserts (without a full page navigation) that the sticky bar's remaining count decremented, a `.cutlab-cuts-made__row` appeared, and either the next proposal or the at-100 terminal state rendered; opens the `.cutlab-compare` `<details>` and asserts a Metric·Baseline·Current·Delta table with 7 rows; asserts the visible proposal/delta text contains none of the verdict words `worse`/`bad`/`better` (CUT-02). Add the three new sections to the existing theme×viewport screenshot matrix (desktop + mobile) so themed rendering is captured. Keep the admin-lock + serial-mode + stale-server discipline from the existing file.
  </action>
  <verify>
    <automated>cd DeckFlow.Web && env -u DISPLAY -u WAYLAND_DISPLAY npx --no-install playwright test cut-lab-structure</automated>
  </verify>
  <acceptance_criteria>
    - The new e2e test passes: import → accept cut → sticky/banner/cuts-made update (no reload) → compare panel opens with a 7-row table.
    - Copy-neutrality assertion (no `worse`/`bad`/`better`) passes.
    - Theme×viewport screenshots for the three new sections are produced.
    - Existing cut-lab e2e assertions stay green.
  </acceptance_criteria>
  <done>The full Phase 103 loop is proven in a real headless browser across themes and viewports.</done>
</task>

<task type="auto">
  <name>Task 2: Full-suite phase gate</name>
  <read_first>
    - .planning/workstreams/cut-lab/phases/103-simulation-engine-guided-cut-rounds/103-VALIDATION.md (full-suite commands + sampling rules)
    - CLAUDE.md (VSTest-in-WSL caveat: prefer dotnet build clean + targeted/CI runs; no browser on Windows host)
  </read_first>
  <action>
    Run the full verification gate and record results in the SUMMARY: (a) `"/mnt/c/Program Files/dotnet/dotnet.exe" build DeckFlow.sln -clp:ErrorsOnly` clean (no new warnings); (b) `"/mnt/c/Program Files/dotnet/dotnet.exe" test DeckFlow.sln --filter "FullyQualifiedName~CutLab"` green (or the full test project if stable); (c) `cd DeckFlow.Web && npx tsc --noEmit -p tsconfig.json` + `npm test` green; (d) the cut-lab Playwright specs green. If VSTest is unstable under WSL for the full solution, record the targeted CutLab-filtered result plus a note to confirm via push-and-watch CI. Confirm no `wwwroot/js/*.js` is staged and site.css is untouched.
  </action>
  <verify>
    <automated>"/mnt/c/Program Files/dotnet/dotnet.exe" build DeckFlow.sln -clp:ErrorsOnly && "/mnt/c/Program Files/dotnet/dotnet.exe" test DeckFlow.Web.Tests --filter "FullyQualifiedName~CutLab"</automated>
  </verify>
  <acceptance_criteria>
    - Solution builds clean with no new warnings.
    - All CutLab-filtered xUnit tests green; Vitest + tsc green.
    - Cut-lab e2e specs green.
    - No compiled JS staged; site.css unmodified.
  </acceptance_criteria>
  <done>The full test + build gate is green (or CI-confirmed) for the phase.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verify — delta readability + neutral framing + perceived latency</name>
  <what-built>The Cut rounds workspace (sticky bar, one-at-a-time proposal card with tradeoff deltas, accept/reject/defer), Cuts made restore list, and Compare-to-baseline panel, driven by the async /api/cut-lab/decide endpoint.</what-built>
  <how-to-verify>
    1. Start the app headless: `scripts/run-web-test.sh` (sets DECKFLOW_DISABLE_AUTO_BROWSER=true).
    2. Enable the `tool.cut-lab.enabled` flag for the local session and open `/cut-lab`.
    3. Import an oversized (>100 card) pool + commander; wait for the Cut rounds section.
    4. Walk 5 consecutive decisions (mix of accept/reject/defer). Confirm: (a) each proposal shows changed-only deltas with neutral copy ("cutting {card} lowers/raises {metric} by …") and NO verdict language; (b) directional ▲/▼ colors appear only on the numeric token; (c) the sticky bar count + round banner update in place each time; (d) each decision resolves near ~1s and never past the 3s spinner cap.
    5. Open Compare to baseline; confirm the Baseline·Current·Delta table reads sensibly.
    6. Restore a cut card from the Cuts made list; confirm it returns and counts update.
    7. Check desktop + mobile viewports and at least two guild themes.
  </how-to-verify>
  <acceptance_criteria>
    - Delta panel is readable and framing is judgment-free on both viewports (VALIDATION Manual-Only row 1).
    - Five consecutive decisions each feel within ~1s target / 3s cap (VALIDATION Manual-Only row 2).
    - Restore + compare behave as specified.
  </acceptance_criteria>
  <resume-signal>Type "approved" or describe the readability/latency/framing issues to fix.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| e2e harness → app | Test-only; exercises the same same-origin/flag-gated endpoint a browser would. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-103-10-01 | Tampering | Flag-gated route exposed in a test build | mitigate | e2e acquires the admin flag lock and the route stays behind `tool.cut-lab.enabled` (OFF in prod); no production exposure introduced by the test. |
| T-103-10-SC | Tampering | package installs | accept | No packages installed this phase (playwright/vitest already present). |
</threat_model>

<verification>
- Full build + CutLab xUnit + Vitest + tsc + cut-lab Playwright specs green.
- Human-verify sign-off recorded.
</verification>

<success_criteria>
The full Phase 103 loop is proven end-to-end across themes/viewports, copy stays neutral (CUT-02), the suite is green, and a human confirms readability + latency.
</success_criteria>

<output>
Create `.planning/workstreams/cut-lab/phases/103-simulation-engine-guided-cut-rounds/103-10-SUMMARY.md` when done. Record the full-suite result and the human-verify outcome.
</output>
