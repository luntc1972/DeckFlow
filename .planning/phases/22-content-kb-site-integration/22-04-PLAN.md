---
phase: 22-content-kb-site-integration
plan: 04
type: execute
wave: 3
depends_on: ["22-01", "22-03"]
files_modified:
  - DeckFlow.Web/Controllers/Admin/AdminContentKbController.cs
  - DeckFlow.Web/Models/AdminContentKbViewModel.cs
  - DeckFlow.Web/Views/AdminContentKb/Index.cshtml
  - DeckFlow.Web/Views/AdminContentKb/_ViewStart.cshtml
  - DeckFlow.Web/Views/Shared/_AdminLayout.cshtml
  - DeckFlow.Web/wwwroot/css/admin-common.css
  - DeckFlow.Web/wwwroot/css/admin-mobile.css
  - .planning/phases/22-content-kb-site-integration/22-04-SUMMARY.md
autonomous: false
requirements: [KB-09]
must_haves:
  truths:
    - "Admin sees ALL index rows (published + hidden) in a per-entry grid with a publish/unpublish toggle, plus per-source bulk publish/hide, a content.kb.enabled flag toggle, index status, and reload-from-seed"
    - "EVERY mutating admin POST carries [ValidateAntiForgeryToken] AND SameOriginRequestValidator.IsValid(Request)"
    - "Reload-from-seed re-runs the curation-preserving seed load; previously-published entries stay published"
    - "Admin surface renders at 375px (card-stack) with all CSS scoped to .admin-shell — zero theme bleed"
  artifacts:
    - path: "DeckFlow.Web/Controllers/Admin/AdminContentKbController.cs"
      provides: "Index + SetVisibility + BulkSetVisibility + ReloadSeed, all CSRF-guarded"
      contains: "ValidateAntiForgeryToken"
    - path: "DeckFlow.Web/Views/AdminContentKb/Index.cshtml"
      provides: "status panel + per-source bulk grid + per-entry curation grid"
  key_links:
    - from: "AdminContentKbController.SetVisibility"
      to: "IContentSiteIndexStore.SetVisibilityAsync"
      via: "POST + anti-forgery + same-origin"
      pattern: "SetVisibilityAsync"
    - from: "AdminContentKbController.ReloadSeed"
      to: "IContentKbSeedLoader.LoadIfPresentAsync"
      via: "POST + anti-forgery + same-origin"
      pattern: "LoadIfPresent"
---

<objective>
Ship the admin curation surface under `.admin-shell` (BasicAuth): a per-entry publish/unpublish grid over ALL index rows, per-source bulk publish/hide, the `content.kb.enabled` flag toggle, index status (counts/sources/last-loaded), and a reload-from-seed action — every mutating POST double-guarded by `[ValidateAntiForgeryToken]` + `SameOriginRequestValidator`.

Purpose: The admin decides which distilled prompts become public (KB-09 per-entry curation, added 2026-06-01). Without it the public surface (Plan 03) shows nothing (default-hidden).

Output: AdminContentKbController + view model + curation view + admin sidebar entry + admin CSS (status badges + admin tag chips) scoped to `.admin-shell`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-content-kb-site-integration/22-CONTEXT.md
@.planning/phases/22-content-kb-site-integration/22-UI-SPEC.md
@.planning/phases/22-content-kb-site-integration/22-RESEARCH.md

<read_first>
- 22-UI-SPEC.md Surface 3 (admin curation grid) — authoritative for layout (status panel / per-source bulk / per-entry grid), copy, ARIA labels, the `.admin-shell .kb-status`/`.kb-status--published`/`.kb-status--hidden`/`.admin-shell .kb-tag` CSS specs, the responsive card-stack behavior, and the destructive-action confirmation matrix (reload = `_AdminConfirmModal` modal; bulk-hide = two-click inline confirm; per-entry unpublish = no confirm).
- ANALOG (admin paged grid + .admin-shell table strategy): the Phase 25 admin harvested-decks grid — Views/Admin/AdminHarvest grid + AdminHarvestController. Mirror its `.admin-table` + `.admin-table--card` responsive table and row-action form pattern.
- ANALOG (admin action form + CSRF in Razor): DeckFlow.Web/Views/AdminHarvest/Index.cshtml:50-53 — `<form method="post" asp-action="..." class="admin-action-form">` + `@Html.AntiForgeryToken()` + submit button.
- ANALOG (admin controller CSRF — BOTH mechanisms, STATE invariant #7): DeckFlow.Web/Controllers/Admin/AdminFlagsController.cs — `[ValidateAntiForgeryToken]` attribute + `if (!SameOriginRequestValidator.IsValid(Request)) return Forbid();` inside the action; the Toggle endpoint that already validates the content.kb.enabled key against the snapshot.
- DeckFlow.Web/Security/SameOriginRequestValidator.cs — `IsValid(Request)` + `GetForbiddenMessage()`.
- DeckFlow.Web/Infrastructure/BasicAuthMiddleware.cs + Program.cs:384-386 — the /Admin/* BasicAuth branch already covers the new admin controller (no change needed).
- DeckFlow.Core/Content/IContentSiteIndexStore.cs (after Plan 01) — GetAllRowsAsync, GetByIdAsync, SetVisibilityAsync, SetVisibilityBySourceAsync.
- DeckFlow.Web/Services/IContentKbSeedLoader.cs (after Plan 03) — LoadIfPresentAsync for the reload action (same idempotent curation-preserving load as startup).
- DeckFlow.Web/Views/Shared/_AdminConfirmModal.cshtml + admin-modal.ts (Phase 16) — the reload confirmation modal primitive.
- DeckFlow.Web/Views/Shared/_AdminLayout.cshtml — sidebar nav `.admin-sidebar__link`; add a "Content KB" entry.
- DeckFlow.Web/wwwroot/css/admin-common.css + admin-mobile.css — existing `.admin-shell` rules, `.admin-harvest__panel`, `.admin-action-form`, `.admin-table`/`.admin-table--card`, `.admin-table-scroll`, `.admin-banner--success`, touch-target floor. Add KB-admin rules ONLY under `.admin-shell`. Responsive overrides go in admin-mobile.css. NEVER unscoped selectors (SC5).
</read_first>
</context>

<tasks>

<task type="auto">
  <name>Task 1: AdminContentKbController (Index + SetVisibility + BulkSetVisibility + ReloadSeed) with double CSRF guard</name>
  <files>DeckFlow.Web/Controllers/Admin/AdminContentKbController.cs, DeckFlow.Web/Models/AdminContentKbViewModel.cs</files>
  <action>
    Create AdminContentKbController ([Route("Admin/ContentKb")], sealed). Constructor injects IContentSiteIndexStore, IContentKbSeedLoader, IFeatureFlagCache (status display), IWebHostEnvironment, ILogger.

    Index() [HttpGet("")] and [HttpGet("Index")]: call store.GetAllRowsAsync() (ALL rows regardless of visibility). Build AdminContentKbViewModel: per-entry list (id, title, source, archetype+bracket tags, IsVisible), per-source groups (source name + entry count), status (total count, published count, distinct source count, last-loaded display, content.kb.enabled flag state), and any TempData success banner. View(model).

    SetVisibility(long entryId, bool visible, CancellationToken ct) [HttpPost("SetVisibility")] [ValidateAntiForgeryToken]: FIRST line — if (!SameOriginRequestValidator.IsValid(Request)) return StatusCode(403, SameOriginRequestValidator.GetForbiddenMessage()); then await store.SetVisibilityAsync(entryId, visible, ct); TempData "Visibility updated."; RedirectToAction(Index).

    BulkSetVisibility(string source, bool visible, CancellationToken ct) [HttpPost("BulkSetVisibility")] [ValidateAntiForgeryToken]: same-origin guard first, then store.SetVisibilityBySourceAsync(source, visible, ct); TempData "Bulk visibility updated for {source}."; Redirect.

    ReloadSeed(CancellationToken ct) [HttpPost("ReloadSeed")] [ValidateAntiForgeryToken]: same-origin guard first, then await seedLoader.LoadIfPresentAsync(_environment, ct) (curation-preserving — Pitfall 1; previously-published entries stay published); TempData "Index reloaded from seed."; Redirect.

    The content.kb.enabled flag toggle reuses the EXISTING AdminFlagsController.Toggle endpoint — the view posts to it; do NOT add a new flag-toggle action here.

    AdminContentKbViewModel + nested types are sealed records/classes with { get; init; } (R-6).
  </action>
  <verify>
    <automated>"/mnt/c/Program Files/dotnet/dotnet.exe" build /mnt/c/users/chrislunt/source/personal/deckflow/DeckFlow.Web/DeckFlow.Web.csproj -c Debug 2>&1 | grep -E "error|Build succeeded"</automated>
  </verify>
  <done>Build clean; all three mutating actions have [ValidateAntiForgeryToken] AND a same-origin guard; Index uses GetAllRowsAsync; ReloadSeed uses the curation-preserving loader.</done>
</task>

<task type="auto">
  <name>Task 2: Admin curation view + sidebar entry + admin CSS (scoped)</name>
  <files>DeckFlow.Web/Views/AdminContentKb/Index.cshtml, DeckFlow.Web/Views/AdminContentKb/_ViewStart.cshtml, DeckFlow.Web/Views/Shared/_AdminLayout.cshtml, DeckFlow.Web/wwwroot/css/admin-common.css, DeckFlow.Web/wwwroot/css/admin-mobile.css</files>
  <action>
    Build Index.cshtml to UI-SPEC Surface 3 exactly (copy, classes, ARIA):
    - Status panel (.admin-harvest__panel): heading "Index Status"; "N entries total (M published)"; "S sources"; "Last loaded: {ts|Never}"; the content.kb.enabled flag toggle form posting to AdminFlags Toggle (@Html.AntiForgeryToken(), button "Enable"/"Disable"); a "Reload Index from Seed" button that opens the _AdminConfirmModal (Phase 16) whose confirm submits a form posting to ReloadSeed with @Html.AntiForgeryToken(). Modal title/body/confirm/cancel copy from UI-SPEC.
    - Per-source bulk grid (.admin-table .admin-table--card; caption sr-only "Content sources — bulk publish controls"): per source row → Source / Entries count / Actions (Publish All + Hide All forms to BulkSetVisibility with hidden source+visible + anti-forgery; Hide All uses .danger + two-click inline confirm per UI-SPEC; aria-labels "Publish/Hide all entries from {source}").
    - Per-entry grid (.admin-table-scroll wrapper + .admin-table .admin-table--card; caption sr-only "Content KB entries — per-entry publish controls"): columns Title (ellipsis) / Source / Tags (.admin-shell .kb-tag chips) / Status (.kb-status--published/--hidden span with aria-label) / Action (Publish or Unpublish form to SetVisibility with hidden entryId+visible + anti-forgery; aria-label "Publish/Unpublish '{title}'"; no .danger; no confirm). data-label on every cell for card-stack.
    - Success banner (.admin-banner--success) when TempData present.
    - Empty-state copy when no rows.

    _ViewStart.cshtml sets _AdminLayout. _AdminLayout.cshtml: add a "Content KB" .admin-sidebar__link (active state when on this controller). Touch only the inserted nav item (R-6).

    admin-common.css: add .admin-shell .kb-status, .admin-shell .kb-status--published, .admin-shell .kb-status--hidden, .admin-shell .kb-tag per the UI-SPEC CSS (admin token values; chip paddings as given — intentional per Checker Disposition). All under .admin-shell — zero unscoped selectors. admin-mobile.css: any KB-specific @media (max-width:768px) override only (card-stack is already covered by existing .admin-table--card rules; add only KB-specific deltas).
  </action>
  <verify>
    <automated>"/mnt/c/Program Files/dotnet/dotnet.exe" build /mnt/c/users/chrislunt/source/personal/deckflow/DeckFlow.Web/DeckFlow.Web.csproj -c Debug 2>&1 | grep -E "error|Build succeeded"</automated>
  </verify>
  <done>Build clean; view matches UI-SPEC Surface 3; every mutating form carries @Html.AntiForgeryToken(); KB admin CSS scoped to .admin-shell; sidebar entry added.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>The admin Content KB curation page: per-entry publish/unpublish, per-source bulk publish/hide, flag toggle, index status, reload-from-seed — all CSRF-guarded.</what-built>
  <how-to-verify>
    Ask the user to start the dev server (do not auto-launch). Authenticate to /Admin (BasicAuth). Then:
    1. GET /Admin/ContentKb → status panel shows total/published/source counts + last-loaded; per-source bulk grid + per-entry grid list ALL rows (hidden ones included, shown as "Hidden").
    2. Click "Publish" on one entry → status flips to "Published", success banner shows; reload /content-kb (with flag ON) and confirm that entry now appears publicly.
    3. "Hide All" on a source → first click shows "Confirm Hide All", second click hides all of that source.
    4. Toggle content.kb.enabled via the panel button → public nav link + /content-kb appear/disappear.
    5. "Reload Index from Seed" → modal confirms → after reload, previously-published entries are STILL published (curation preserved — the Pitfall-1 acceptance in the live app).
    6. CSRF negative: from a different origin / without the anti-forgery token, the SetVisibility/BulkSetVisibility/ReloadSeed POSTs are rejected (403 / anti-forgery failure).
    7. 375px: sidebar collapses to disclosure; both grids card-stack; buttons >=44px; switch a guild theme on a public page and confirm no admin KB styling leaked.
  </how-to-verify>
  <resume-signal>Type "approved" (note flag stays OFF in prod until the post-UAT flip), or describe issues.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin browser → mutating POST | visibility/bulk/reload state changes |
| reload action → DB | seed re-load must preserve curation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-13 | Spoofing | CSRF on SetVisibility/BulkSetVisibility/ReloadSeed | mitigate | [ValidateAntiForgeryToken] + SameOriginRequestValidator.IsValid on EVERY mutating action (CI grep gate below) |
| T-22-14 | Spoofing | unauthenticated access to curation | mitigate | BasicAuthMiddleware on /Admin/* (existing; covers new controller) |
| T-22-15 | Tampering | reload clobbers admin curation | mitigate | ReloadSeed uses curation-preserving LoadIfPresentAsync (Plan 01 upsert) |
| T-22-16 | Elevation of Privilege | unknown flag key via toggle | mitigate | reuses AdminFlagsController.Toggle which validates the key against the snapshot; content.kb.enabled is seeded (Plan 01) |
</threat_model>

<verification>
- DeckFlow.Web builds clean.
- CSRF CI grep gate (every mutating admin action double-guarded). Count the mutating POST actions and the two guards:
  - `grep -cE '^\s*\[HttpPost' DeckFlow.Web/Controllers/Admin/AdminContentKbController.cs` → expect 3 (SetVisibility, BulkSetVisibility, ReloadSeed).
  - `grep -cE '^\s*\[ValidateAntiForgeryToken' DeckFlow.Web/Controllers/Admin/AdminContentKbController.cs` → expect 3.
  - `grep -c 'SameOriginRequestValidator.IsValid' DeckFlow.Web/Controllers/Admin/AdminContentKbController.cs` → expect 3.
  - The three counts must be EQUAL and equal 3 (returns non-empty/unequal = unguarded action = FAIL).
- Zero theme bleed: `grep -rl '\.kb-' DeckFlow.Web/wwwroot/css/site.css DeckFlow.Web/wwwroot/css/admin.css` returns NOTHING (KB admin rules live only in admin-common.css under .admin-shell); and every KB rule in admin-common.css is prefixed `.admin-shell`.
- Every mutating form in the view carries the token: `grep -c '@Html.AntiForgeryToken' DeckFlow.Web/Views/AdminContentKb/Index.cshtml` → expect >= 3 (set/bulk/reload; flag toggle reuses AdminFlags form which also tokenizes).
- Human-verify checkpoint passed (curation round-trip, CSRF negative, reload-preserves-curation, 375px).
</verification>

<success_criteria>
- Admin per-entry + per-source curation + flag toggle + status + reload, over ALL rows (KB-09).
- Every mutating admin POST double-CSRF-guarded; CI grep gate balances at 3/3/3 (KB-09; SC4/P11).
- Reload preserves admin curation (KB-09; Pitfall 1).
- 375px responsive, CSS scoped to .admin-shell, zero theme bleed (KB-09; SC5).
</success_criteria>

<output>
Create `.planning/phases/22-content-kb-site-integration/22-04-SUMMARY.md` when done.
</output>
