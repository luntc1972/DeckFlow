---
phase: 96-stated-rules-distiller
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - DeckFlow.Core/Knowledge/StatedRulesExtraction/StatedRuleCandidate.cs
  - DeckFlow.Core/Knowledge/StatedRulesExtraction/StatedRulesMetricVocabulary.cs
  - DeckFlow.Core/Knowledge/StatedRulesExtraction/StatedRuleReducer.cs
  - DeckFlow.Core.Tests/StatedRulesExtraction/StatedRuleCandidateVocabularyTests.cs
  - DeckFlow.Core.Tests/StatedRulesExtraction/StatedRuleReducerTests.cs
autonomous: true
requirements: [CS-13, CS-14, CS-11c]
must_haves:
  truths:
    - "A banded stated rule (37-42 lands) is ONE StatedRuleCandidate with comparator=range, not two rows"
    - "The metric controlled vocabulary is a closed set aligned to Phase 95 MeasuredMetric keys plus stated-only keys"
    - "Duplicate rules for the same (metric,condition,comparator) collapse to one, keeping highest confidence then newest video_date"
    - "D-02: the band-capable StatedRuleCandidate shape ({category,metric,value|band,comparator,condition,clip_ts} plus sourceClip,confidence,video_date) is locked here; D-02a metric allowlist aligns to Phase 95 MeasuredMetric keys"
  artifacts:
    - path: "DeckFlow.Core/Knowledge/StatedRulesExtraction/StatedRuleCandidate.cs"
      provides: "Phase-96-owned band-capable rule DTO (distinct from P94 StatedRule)"
      contains: "sealed record StatedRuleCandidate"
    - path: "DeckFlow.Core/Knowledge/StatedRulesExtraction/StatedRulesMetricVocabulary.cs"
      provides: "Closed metric allowlist joinable to Phase 95 keys in P97"
      contains: "IReadOnlySet<string> Metrics"
    - path: "DeckFlow.Core/Knowledge/StatedRulesExtraction/StatedRuleReducer.cs"
      provides: "Cross-chunk deterministic dedupe (D-01 reduce)"
      contains: "static class StatedRuleReducer"
  key_links:
    - from: "StatedRulesMetricVocabulary.Metrics"
      to: "ContentTagVocabulary.CardCategories"
      via: "union of the 11 category tokens"
      pattern: "CardCategories"
---

<objective>
Establish the pure-Core contract layer for stated-rule extraction: the band-capable `StatedRuleCandidate` DTO (D-02), the closed `metric` controlled vocabulary aligned to Phase 95's emitted `MeasuredMetric` keys (D-02a), and the deterministic cross-chunk `StatedRuleReducer` dedupe pass (D-01 reduce). These are the types every downstream Phase-96 plan builds against.

Purpose: give the LLM stages (96-03/96-04), the store (96-05), the writer (96-05), and the orchestrator (96-07) one locked, unit-tested rule shape and allowlist so no plan re-invents them.
Output: 3 new Core source files + 2 new test files, all pure (no HTTP/AspNet/RestSharp).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/96-stated-rules-distiller/96-CONTEXT.md
@.planning/phases/96-stated-rules-distiller/96-RESEARCH.md
@.planning/phases/96-stated-rules-distiller/96-PATTERNS.md

<interfaces>
<!-- Locked shapes the executor must implement verbatim; do not redesign. -->

StatedRuleCandidate (DeckFlow.Core.Knowledge.StatedRulesExtraction namespace) — properties, all with per-property XML doc, `sealed record`:
  required string Category
  required string Metric
  double? Value        (single-value comparators: gte|lte|eq)
  double? ValueMin      (range comparator)
  double? ValueMax      (range comparator)
  required string Comparator   (one of: gte, lte, eq, range)
  string? Condition     (e.g. "archetype:control", "curve:low", "color:blue", "bracket:2")
  int? ClipTimestampSeconds
  required string SourceClip
  required double Confidence    (0.0..1.0 inclusive)
  string? CardReference  (null for metric-based rules; the card name a rule references when it names a specific card — D-07 grounding target; Decompose populates it, grounding rewrites it to canonical)
  bool? CardGrounded    (null = no card name present or no grounder wired; true = CardReference resolved+rewritten to canonical; false = CardReference kept+flagged unresolved)
  required DateTimeOffset VideoDateUtc

Locked metric allowlist (StatedRulesMetricVocabulary.Metrics, StringComparer.OrdinalIgnoreCase):
  category tokens (bare; P97 prefixes to category_ratio:{token}): ramp, removal, draw, finishers, win-cons, counter, protection, board-wipe, tutor, recursion, utility
  fixed measured keys (verbatim from MeasuredStyleProfileBuilder): karsten:target_lands, karsten:land_delta, karsten:health_score, combo_density:included_per_deck
  stated-only keys (no Phase-95 counterpart yet): land_count, interaction, opener_probability, pip_distribution, power_level_philosophy
  EXCLUDED on purpose: lift:* (creators state absolute counts, not statistical lift)

Locked comparator vocabulary (StatedRulesMetricVocabulary.Comparators): gte, lte, eq, range
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: StatedRuleCandidate DTO + StatedRulesMetricVocabulary allowlist</name>
  <read_first>
    - .planning/phases/96-stated-rules-distiller/96-PATTERNS.md (section "StatedRuleCandidate DTO" lines 84-137 — locked record shape; section on CardCategory idiom)
    - DeckFlow.Core/Knowledge/CreatorStyleProfile.cs (StatedRule/MeasuredMetric record idiom to MIRROR, NOT reuse — required string/required double, per-property XML doc, sealed record)
    - DeckFlow.Core/Knowledge/ContentTagVocabulary.cs (CardCategories set lines 41-55 + the closed-vocabulary HashSet<string>(StringComparer.OrdinalIgnoreCase) idiom to mirror)
    - DeckFlow.Web/Services/CreatorStyle/MeasuredStyleProfileBuilder.cs (metric key literals: category_ratio:{category}, karsten:target_lands/land_delta/health_score, combo_density:included_per_deck — the exact strings the vocabulary must align to)
  </read_first>
  <behavior>
    - StatedRulesMetricVocabulary.Metrics contains exactly the 20 locked tokens (11 categories + 4 fixed measured keys + 5 stated-only), case-insensitive membership.
    - StatedRulesMetricVocabulary.Metrics does NOT contain any "lift:" key.
    - Every ContentTagVocabulary.CardCategories value is present in StatedRulesMetricVocabulary.Metrics (join guarantee for P97).
    - StatedRulesMetricVocabulary.Comparators contains exactly {gte, lte, eq, range}.
  </behavior>
  <action>
    Create `DeckFlow.Core/Knowledge/StatedRulesExtraction/StatedRuleCandidate.cs` with file-scoped namespace `DeckFlow.Core.Knowledge.StatedRulesExtraction`, a `public sealed record StatedRuleCandidate` matching the locked <interfaces> shape exactly (band-capable: Value + ValueMin + ValueMax; nullable Condition/ClipTimestampSeconds/CardReference/CardGrounded), Allman braces, one XML `<summary>` per property. Do NOT reuse `DeckFlow.Core.Knowledge.StatedRule` (P94) — that double-only record cannot hold a band, per RESEARCH Pitfall 2; this phase never writes ICreatorStyleProfileStore.
    Create `DeckFlow.Core/Knowledge/StatedRulesExtraction/StatedRulesMetricVocabulary.cs`: `public static class StatedRulesMetricVocabulary` exposing `public static readonly IReadOnlySet<string> Metrics` and `public static readonly IReadOnlySet<string> Comparators`, both `new HashSet<string>(StringComparer.OrdinalIgnoreCase){...}`. Build `Metrics` as the union of `ContentTagVocabulary.CardCategories` plus the fixed measured keys and stated-only keys from <interfaces> — reference `ContentTagVocabulary.CardCategories` directly so the join guarantee is structural, not copy-pasted. Add a `// Why:` comment noting lift:* is deliberately excluded and stated-only keys have no P95 counterpart yet (P97 CS-17 handles un-measurable philosophy).
  </action>
  <verify>
    <automated>dotnet.exe test DeckFlow.Core.Tests --filter FullyQualifiedName~StatedRuleCandidateVocabularyTests</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet build DeckFlow.Core/DeckFlow.Core.csproj` clean, 0 new warnings.
    - New test file `DeckFlow.Core.Tests/StatedRulesExtraction/StatedRuleCandidateVocabularyTests.cs` asserts: Metrics count == 20; every CardCategories member ∈ Metrics; no member starts with "lift:"; Comparators SetEquals {gte,lte,eq,range}.
    - `grep -c "lift:" DeckFlow.Core/Knowledge/StatedRulesExtraction/StatedRulesMetricVocabulary.cs` returns 0 outside a `// Why:` comment.
    - StatedRuleCandidate namespace is `DeckFlow.Core.Knowledge.StatedRulesExtraction` (NOT bare Knowledge).
    - StatedRuleCandidate exposes a nullable `string? CardReference { get; init; }` (null for the metric-based majority; the D-07 grounding target for card-naming rules).
  </acceptance_criteria>
  <done>Both source files compile; the vocabulary test passes; StatedRuleCandidate carries all 13 locked fields including the band trio and the nullable CardReference.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: StatedRuleReducer deterministic cross-chunk dedupe</name>
  <read_first>
    - DeckFlow.Core/Knowledge/MeasuredStyleExtraction/LiftCalculator.cs (pure static helper + co-located sealed record result idiom + `// Why:` convention to mirror)
    - DeckFlow.Core/Knowledge/MeasuredStyleExtraction/CategoryCounter.cs (ArgumentNullException.ThrowIfNull guard idiom)
    - DeckFlow.Core/Knowledge/StatedRulesExtraction/StatedRuleCandidate.cs (created in Task 1 — the input/output type)
    - .planning/phases/96-stated-rules-distiller/96-RESEARCH.md (Pitfall 1 — reduce is a DeckFlow addition, NOT part of Claimify; dedupe key discretion note)
  </read_first>
  <behavior>
    - Two candidates with identical (Metric, Condition, Comparator) case-insensitive collapse to one output.
    - On collision the survivor is the one with the higher Confidence; ties broken by the later VideoDateUtc.
    - A null Condition and an empty-string Condition are treated as the same dedupe bucket.
    - Candidates differing only by Metric (or only by Condition, or only by Comparator) are NOT merged.
    - Output order is deterministic (stable) for a given input.
  </behavior>
  <action>
    Create `DeckFlow.Core/Knowledge/StatedRulesExtraction/StatedRuleReducer.cs`: `public static class StatedRuleReducer` with `public static IReadOnlyList<StatedRuleCandidate> Reduce(IReadOnlyList<StatedRuleCandidate> candidates)`. Open with `ArgumentNullException.ThrowIfNull(candidates)`. Group by a case-insensitive dedupe key over `(Metric, Condition ?? "", Comparator)`; within each group select the survivor by max Confidence then max VideoDateUtc. Add a `// Why:` comment stating the dedupe key is (metric,condition,comparator) and that this reduce step is a DeckFlow-specific addition (Claimify itself has no cross-sentence merge — RESEARCH Pitfall 1). Return a new list; do not mutate the input. Keep this pure — no LLM, no I/O.
  </action>
  <verify>
    <automated>dotnet.exe test DeckFlow.Core.Tests --filter FullyQualifiedName~StatedRuleReducerTests</automated>
  </verify>
  <acceptance_criteria>
    - New test `DeckFlow.Core.Tests/StatedRulesExtraction/StatedRuleReducerTests.cs` covers: duplicate (metric,condition,comparator) collapses keeping higher confidence; confidence tie broken by newer VideoDateUtc; null vs "" condition same bucket; differing metric NOT merged.
    - `dotnet build DeckFlow.Core/DeckFlow.Core.csproj` clean, 0 new warnings.
    - No `using RestSharp`, `using System.Net.Http`, or `Microsoft.AspNetCore` anywhere in StatedRulesExtraction/ files.
  </acceptance_criteria>
  <done>Reducer collapses genuine duplicates deterministically; all reducer tests green; file is pure Core.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM output → Core types | Deserialized model JSON becomes StatedRuleCandidate; must be validated downstream (96-03) before persist/render |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-96-01 | Tampering | metric field from transcript-driven LLM | mitigate | Closed `StatedRulesMetricVocabulary.Metrics` allowlist is the join gate; membership enforced in 96-03's ValidateStatedRules (this plan supplies the allowlist) |
| T-96-04 | DoS (cost) | unbounded rule count from adversarial transcript | mitigate | Reducer collapses duplicates; a max-rules-per-video cap is enforced in 96-03's Sanitize (this plan keeps the reducer O(n) and allocation-bounded) |
| T-96-SC | Tampering | package installs | accept | No new packages this plan (pure Core, BCL only) — legitimacy gate N/A |
</threat_model>

<verification>
- `dotnet build DeckFlow.Core/DeckFlow.Core.csproj` clean.
- `dotnet.exe test DeckFlow.Core.Tests --filter FullyQualifiedName~StatedRules` green.
- No HTTP/AspNet usings in the new StatedRulesExtraction/ folder (Core purity gate).
</verification>

<success_criteria>
StatedRuleCandidate, StatedRulesMetricVocabulary, and StatedRuleReducer exist as pure Core types with green unit tests; the metric vocabulary structurally includes every card-category token (P97 join guarantee) and excludes lift:*.
</success_criteria>

<output>
Create `.planning/phases/96-stated-rules-distiller/96-01-SUMMARY.md` when done.
</output>
