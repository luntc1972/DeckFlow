---
phase: 100-creator-style-tool-surface
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - DeckFlow.Web/Models/DeckPageTab.cs
  - DeckFlow.Web/Services/Tools/ToolRegistry.cs
  - DeckFlow.Web/Services/FeatureFlags/FeatureFlagStore.cs
  - DeckFlow.Web/Services/FeatureFlags/FeatureFlagCatalog.cs
  - DeckFlow.Web.Tests/FeatureFlagStoreSeedTests.cs
  - DeckFlow.Web.Tests/Tools/ToolRegistryTests.cs
  - DeckFlow.Web.Tests/Tools/ToolFlagSeedConsistencyTests.cs
  - DeckFlow.Web.Tests/FeatureFlagCatalogTests.cs
autonomous: true
requirements: [CS-30]
must_haves:
  truths:
    - "Flag key `tool.creator-style.enabled` is seeded FALSE/0 in BOTH the Postgres and SQLite seed blocks (D-100-05, CS-30)."
    - "The Analyze-section tool `creator-style` at route `/creator-style` is registered in ToolRegistry with craft-first copy (D-100-05, D-100-12)."
    - "`FeatureFlagCatalog` has a non-empty description for `tool.creator-style.enabled` so the catalog-enforcement test passes (D-100-05)."
    - "`DeckPageTab.CreatorStyle = 16` exists so the view model and nav strip can reference the active tab."
  artifacts:
    - path: "DeckFlow.Web/Models/DeckPageTab.cs"
      provides: "CreatorStyle = 16 enum member"
      contains: "CreatorStyle = 16"
    - path: "DeckFlow.Web/Services/Tools/ToolRegistry.cs"
      provides: "creator-style tool registration in the Analyze section"
      contains: "creator-style"
    - path: "DeckFlow.Web/Services/FeatureFlags/FeatureFlagStore.cs"
      provides: "seeded-OFF flag rows on both dialects"
      contains: "tool.creator-style.enabled"
    - path: "DeckFlow.Web/Services/FeatureFlags/FeatureFlagCatalog.cs"
      provides: "catalog description for the new flag"
      contains: "tool.creator-style.enabled"
  key_links:
    - from: "ToolRegistry.cs (creator-style entry)"
      to: "FeatureFlagStore.cs (seed rows)"
      via: "identical flag key string"
      pattern: "tool\\.creator-style\\.enabled"
    - from: "FeatureFlagStore.cs (seeded keys)"
      to: "FeatureFlagCatalog.cs (descriptions)"
      via: "catalog-enforcement test requires a description for every seeded key"
      pattern: "tool\\.creator-style\\.enabled"
---

<objective>
Register the Creator-Style tool and its single feature flag `tool.creator-style.enabled` across every coordinated touch point (enum, ToolRegistry, both dialect seed blocks, catalog description), seeded OFF on both dialects.

Purpose: This is the "four touch points, one commit" checklist wiring (RESEARCH.md Pattern 1). Every touch point must agree on the exact flag key or the flag silently misbehaves and the build's catalog-enforcement test fails. Getting this atomically correct unblocks the controller gate (Plan 05).
Output: A seeded-OFF flag, a registered Analyze-section tool, a catalog description, and a passing seed-key assertion.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/100-creator-style-tool-surface/100-CONTEXT.md
@.planning/phases/100-creator-style-tool-surface/100-RESEARCH.md
@.planning/phases/100-creator-style-tool-surface/100-PATTERNS.md

Scope fences (from CONTEXT.md/RESEARCH.md, binding):
- Flag key is EXACTLY `tool.creator-style.enabled` (D-100-05). The roadmap alias `creator.style-artifact` is retired — do NOT seed or reference it.
- Craft-first copy (D-100-12): lead with measured build style + real exemplars; AI mention limited to "ChatGPT-ready packet"; NO creator-crawl / KB-sourcing claims in any copy.
- LF line endings; `.editorconfig` changed-lines gate; do not mass-reflow existing files.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add DeckPageTab.CreatorStyle enum member + ToolRegistry entry</name>
  <read_first>
    - DeckFlow.Web/Models/DeckPageTab.cs (see the existing `Bracket = 15` member — the last enum value; add the next integer)
    - DeckFlow.Web/Services/Tools/ToolRegistry.cs (see the `manabase` and `bracket` `Create(...)` calls and the private `Create(...)` signature; the Analyze `ToolNavSection` value)
    - .planning/phases/100-creator-style-tool-surface/100-UI-SPEC.md (Copywriting Contract — home-tile title/description)
    - DeckFlow.Web.Tests/Tools/ToolRegistryTests.cs (checker HIGH-1: `Assert.Collection` enumerates EVERY tool AND `Assert.Equal(14, registry.All.Count)` plus 14-count assertions for distinct Key/Route/Tab — all must move to 15 and gain a creator-style `AssertTool` entry at the registry's actual insertion position)
  </read_first>
  <action>
    In DeckPageTab.cs add a new enum member `CreatorStyle = 16` immediately after `Bracket = 15`, with an xmldoc summary line ("Creator-style critique artifact generator page."). Do not renumber existing members.

    In ToolRegistry.cs add one `Create(...)` entry alongside the `manabase`/`bracket` Analyze-section entries with these exact literal values: slug `creator-style`, display name `Creator-Style Critique`, route `/creator-style`, section `ToolNavSection.Analyze`, flag key `tool.creator-style.enabled`, default-enabled `false`, tile title `Creator-Style Critique`, tile description `Critique your deck against a creator's measured build style — real exemplars, weighted targets, no vibes.`, icon/slug `creator-style`, tab `DeckPageTab.CreatorStyle`, and the final trailing bool matching the sibling entries' value (no extra API sub-routes — omit any trailing `params string[] additionalRoutes`). Match the argument ORDER of the existing `manabase`/`bracket` calls exactly; do not invent parameters.

Then update `ToolRegistryTests.cs` (checker HIGH-1): bump every `14` count assertion to `15` (`registry.All.Count` and the distinct Key/Route/Tab counts), bump the routes-union assertion `Assert.Equal(19, registry.All.SelectMany(tool => tool.AdditionalRoutes.Prepend(tool.Route)).Distinct(...).Count())` (ToolRegistryTests.cs:46) to `20` — creator-style contributes exactly one route (`/creator-style`, no AdditionalRoutes) (Codex r2 HIGH) — and add a creator-style `AssertTool(...)` element to the `Assert.Collection` at the SAME ordinal position the new `Create(...)` entry occupies in ToolRegistry (Analyze section, next to bracket) — asserting slug `creator-style`, route `/creator-style`, flag `tool.creator-style.enabled`, tab `DeckPageTab.CreatorStyle`. The collection is position-sensitive; insert at the matching index, not the end (unless bracket is last).
  </action>
  <verify>
    <automated>dotnet build DeckFlow.Web/DeckFlow.Web.csproj -c Debug 2>&1 | grep -iE "error|warning" | grep -v "0 Warning" || echo "BUILD CLEAN"</automated>
  </verify>
  <acceptance_criteria>
    - `DeckFlow.Web/Models/DeckPageTab.cs` contains the exact token `CreatorStyle = 16`.
    - `grep -c "creator-style" DeckFlow.Web/Services/Tools/ToolRegistry.cs` returns >= 1 and the entry uses `ToolNavSection.Analyze` and flag key `tool.creator-style.enabled`.
    - `dotnet build DeckFlow.Web` succeeds with 0 new warnings.
    - The tile description contains no "crawl", "video", "KB", "scrape", or "transcript" wording (D-100-12 legal/messaging rule).
    - `ToolRegistryTests` passes: all count assertions read 15 and the `Assert.Collection` has a creator-style entry at the correct ordinal.
  </acceptance_criteria>
  <done>The enum member and the Analyze-section tool registration exist and the web project builds clean.</done>
</task>

<task type="auto">
  <name>Task 2: Seed the flag OFF on both dialects + catalog description + seed-key test</name>
  <read_first>
    - DeckFlow.Web/Services/FeatureFlags/FeatureFlagStore.cs (both `PostgresSeedSql` and `SqliteSeedSql` blocks; the existing `('tool.bracket.enabled', FALSE)` and `('tool.bracket.enabled', 0)` rows are the exact insertion analog)
    - DeckFlow.Web/Services/FeatureFlags/FeatureFlagCatalog.cs (the `["tool.bracket.enabled"] = "..."` description entry)
    - DeckFlow.Web.Tests/FeatureFlagStoreSeedTests.cs (the existing `[InlineData("tool.bracket.enabled", false)]` row)
    - DeckFlow.Web.Tests/Tools/ToolFlagSeedConsistencyTests.cs (checker HIGH-1: `Assert.Equal(16, expectedKeys.Count)` and a dark-launch false-allowlist containing ONLY `tool.bracket.enabled` + `tool.primer.stale-flag`)
    - DeckFlow.Web.Tests/FeatureFlagCatalogTests.cs (checker MED-1: a static `[InlineData(...)]` seed-key list that must gain the new key)
  </read_first>
  <action>
    In FeatureFlagStore.cs add `('tool.creator-style.enabled', FALSE)` to the Postgres seed VALUES list and `('tool.creator-style.enabled', 0)` to the SQLite seed VALUES list, placed in the `tool.*` cluster next to the `tool.bracket.enabled` rows. Both dialect blocks MUST be edited (CS-30; a one-dialect edit is a documented miss). Preserve each block's existing `ON CONFLICT (key) DO NOTHING` terminator and comma/newline formatting.

    In FeatureFlagCatalog.cs add a `["tool.creator-style.enabled"]` description entry with text: `Enable the Creator-Style Critique tool that builds a ChatGPT-ready packet scoring a submitted deck against a chosen creator's measured build style.` — this is required or `FeatureFlagCatalogTests` fails the build.

    In FeatureFlagStoreSeedTests.cs add an `[InlineData("tool.creator-style.enabled", false)]` row (comment `// CS-30: seeded OFF`) to the existing seeded-default theory so the seeded-OFF default is asserted.

In `ToolFlagSeedConsistencyTests.cs` (checker HIGH-1): bump the `Assert.Equal(16, expectedKeys.Count)` expectation to `17`, and add `tool.creator-style.enabled` to the dark-launch false-allowlist (the set expected to be seeded FALSE), using the same `// Why:` comment convention the existing `tool.bracket.enabled`/`tool.primer.stale-flag` entries use.

In `FeatureFlagCatalogTests.cs` (checker MED-1): add `[InlineData("tool.creator-style.enabled")]` to the static seed-key InlineData list so the catalog-lockstep theory covers the new key.
  </action>
  <verify>
    <automated>dotnet test DeckFlow.Web.Tests --filter "FullyQualifiedName~FeatureFlagStoreSeedTests|FullyQualifiedName~FeatureFlagCatalogTests" 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "tool.creator-style.enabled" DeckFlow.Web/Services/FeatureFlags/FeatureFlagStore.cs` returns >= 2 (one per dialect block).
    - The Postgres row uses `FALSE` and the SQLite row uses `0`.
    - `FeatureFlagCatalog.cs` contains a non-empty description keyed by `tool.creator-style.enabled`.
    - `FeatureFlagStoreSeedTests`, `FeatureFlagCatalogTests`, and `ToolFlagSeedConsistencyTests` all pass (17-key count; new key in the false-allowlist and in the catalog InlineData), proving the flag is seeded OFF, dark-launch-allowlisted, and cataloged.
  </acceptance_criteria>
  <done>The flag is seeded FALSE/0 on both dialects, cataloged, and the seed-default + catalog-enforcement tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → flag store | An operator flips the flag in /Admin/Flags (behind existing BasicAuth); the seed default must be OFF so the tool is unreachable at ship. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-100-01 | Elevation of Privilege | New tool reachable before intended launch | mitigate | Seed `tool.creator-style.enabled` = FALSE/0 on both dialects; the controller `[FeatureFlagGate]` (Plan 05) 404s while OFF. This plan guarantees the OFF default. |
| T-100-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan; all edits are to existing C# source. RESEARCH.md Package Legitimacy Audit: N/A (no external packages). |
</threat_model>

<verification>
- `dotnet build DeckFlow.Web` clean (0 new warnings).
- `dotnet test DeckFlow.Web.Tests --filter FullyQualifiedName~FeatureFlag` green.
- Flag key spelled identically across ToolRegistry, both FeatureFlagStore blocks, and FeatureFlagCatalog.
</verification>

<success_criteria>
The flag `tool.creator-style.enabled` is registered, cataloged, and seeded OFF on both dialects; the Analyze-section tool is registered with craft-first copy; `DeckPageTab.CreatorStyle = 16` exists; the seed-default and catalog-enforcement tests pass.
</success_criteria>

<output>
Create `.planning/phases/100-creator-style-tool-surface/100-01-SUMMARY.md` when done.
</output>
