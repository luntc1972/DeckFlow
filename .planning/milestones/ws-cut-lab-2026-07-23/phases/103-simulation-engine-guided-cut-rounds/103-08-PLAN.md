---
phase: 103-simulation-engine-guided-cut-rounds
plan: 08
type: execute
wave: 5
depends_on: ["103-06"]
files_modified:
  - DeckFlow.Web/Models/CutLabViewModel.cs
  - DeckFlow.Web/Views/Deck/CutLab.cshtml
autonomous: true
requirements: [CUT-01, CUT-02, CUT-03, SIM-02]
must_haves:
  truths:
    - "HIGH-4: the view renders each proposal with the READY-MADE deltas the page service computed server-side (CutLabProcessResult.InitialProposalDeltas) — it NEVER renders a proposal without its deltas and never re-simulates in the view."
    - "HIGH-1/SIM-02: the Compare-to-baseline table renders State.BaselineSnapshot vs the server-provided CurrentSnapshot (derived-working-list snapshot from Plan 103-06); cuts-made rows come from State.Decisions (Accepted only) — the view reads derived data, it never mutates Pool."
    - "HIGH-5: each accept/reject/defer button and each restore button is wrapped in a REAL <form method=\"post\" action=\"/cut-lab/decide\"> carrying the antiforgery token + hidden CutLabStateJson + CardName + Decision, so the no-JS path posts to CutLabController.Decide (Plan 103-07); Plan 103-09 intercepts submit for the fetch enhancement."
    - "D-02/CUT-02: exactly one proposal card renders at a time, with the 'Proposed cut: {card}' heading, 'Flagged by {n} findings' neutral .kb-chip evidence, changed-only compact delta lines (D-05), and a 'Show full metric breakdown' expander revealing all 7 families."
    - "D-14/CUT-03: a sticky round+count bar (round name · N to cut · N cut so far) renders and pins within the Cut rounds section, 44px, theme-tokened, with data-cut-lab-sticky-* hooks."
    - "D-06/CUT-02: delta values carry direction via colored ▲/▼ token (--success/--danger) only on the numeric token, with neutral sentence copy — never a verdict, never a colored card name/heading/row."
    - "D-16/CUT-03: a 'Cuts made' restorable list renders (omitted until first cut; collapsed >5) with per-row Restore buttons (each in its own no-JS form)."
    - "CUT-01/D-03: round banners render from the round plan; empty/at-100 states render the specified copy."
    - "Full-page (no-JS) render works from the server round plan + ready-made deltas; the JS hydration in Plan 103-09 only enhances it."
  artifacts:
    - path: "DeckFlow.Web/Models/CutLabViewModel.cs"
      provides: "round/proposal/deltas/cuts-made/compare view fields + From() mapping consuming ready-made deltas"
      contains: "Proposal"
    - path: "DeckFlow.Web/Views/Deck/CutLab.cshtml"
      provides: "Cut rounds + Cuts made + Compare to baseline sections with no-JS decision forms"
      contains: "cut-lab/decide"
  key_links:
    - from: "DeckFlow.Web/Views/Deck/CutLab.cshtml"
      to: "POST /cut-lab/decide (CutLabController.Decide, Plan 103-07)"
      via: "each decision/restore button wrapped in a real antiforgery form (no-JS fallback)"
      pattern: "cut-lab/decide"
    - from: "DeckFlow.Web/Models/CutLabViewModel.cs"
      to: "CutLabProcessResult.RoundPlan / InitialProposalDeltas / CurrentSnapshot / State"
      via: "From() maps ready-made server data into proposal/compare/cuts-made view rows"
      pattern: "InitialProposalDeltas"
---

<objective>
Render the three new Cut Lab sections the UI spec defines — the Cut rounds workspace (sticky bar + round banner + one-at-a-time proposal card + tradeoff deltas + accept/reject/defer), the Cuts made restore list, and the Compare to baseline panel — appended after the Phase 102 Role floors section. Server-rendered from the round plan + READY-MADE deltas (HIGH-4) + baseline/current snapshots so the no-JS path works; each decision/restore control is a real form posting to `/cut-lab/decide` (HIGH-5). Plan 103-09 adds fetch hydration.

Purpose: This is the CUT-01/02/03 + SIM-02 surface the user actually touches, functional without JS. Copy and color are prescribed verbatim by the approved UI spec.
Output: Extended `CutLabViewModel` (+ From mapping) and `CutLab.cshtml`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/cut-lab/phases/103-simulation-engine-guided-cut-rounds/103-UI-SPEC.md
@.planning/workstreams/cut-lab/phases/103-simulation-engine-guided-cut-rounds/103-PATTERNS.md

<interfaces>
Existing view model (DeckFlow.Web/Models/CutLabViewModel.cs): sealed record with `From(CutLabRequest, CutLabProcessResult)`; nested `*View` records (CutLabRoleGroupView, CutLabFindingView, CutLabFloorRowView) as the shape precedent; DisplayLabelFor(roleKey) helper.
Existing view (DeckFlow.Web/Views/Deck/CutLab.cshtml): @model CutLabViewModel; `<section class="result-panel">` blocks; `<details class="cutlab-role-group">` accordions; hidden `CutLabStateJson`; `data-cut-lab-*` attribute convention; the Process form uses `@Html.AntiForgeryToken()` — reuse that idiom for the decision forms.

From Plan 103-06 (READY-MADE — do NOT recompute in the view):
- CutLabProcessResult.RoundPlan (CutLabRoundPlan: ordered items with card, round key/label, finding count + chips, CardsRemainingToTarget).
- CutLabProcessResult.InitialProposalDeltas (CutLabProposalDeltas for the CURRENT proposal — HIGH-4).
- CutLabProcessResult.CurrentSnapshot (CutLabMetricSnapshot for the derived working list — the Compare 'current' column).
- State.BaselineSnapshot (Compare 'baseline' column) + State.Decisions (Accepted → cuts-made rows).

From Plan 103-07: the no-JS route is POST /cut-lab/decide bound to CutLabController.Decide; the form MUST post hidden fields CutLabStateJson + CardName + Decision (accept|reject|defer|restore) + the antiforgery token. Field names/route are the shared contract with Plan 103-07 (planned together).
From Plan 103-04: round label constants. UI copy: 103-UI-SPEC Copywriting Contract + Component Contracts 1-4.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend CutLabViewModel with round/proposal/deltas/cuts-made/compare fields consuming ready-made server data</name>
  <read_first>
    - DeckFlow.Web/Models/CutLabViewModel.cs (record + From + nested *View record conventions + DisplayLabelFor)
    - DeckFlow.Web/Services/CutLab/CutLabPageService.cs (CutLabProcessResult.RoundPlan / InitialProposalDeltas / CurrentSnapshot / State access — Plan 103-06)
    - DeckFlow.Web/Models/CutLab/CutLabMetrics.cs (snapshot/delta types)
    - .planning/workstreams/cut-lab/phases/103-simulation-engine-guided-cut-rounds/103-UI-SPEC.md (which fields each surface needs + exact labels)
  </read_first>
  <action>
    Add nested display records + fields to `CutLabViewModel`: (a) `CutLabProposalView?` (card name, round key, round banner heading + sub-line, "Flagged by {n} findings" + chip labels or the Round-3 "preference call" line, the changed-only `CutLabDeltaLineView[]` + a full-7-family `CutLabDeltaLineView[]` for the expander — BOTH built from `CutLabProcessResult.InitialProposalDeltas` (HIGH-4: the view never computes deltas), floor-warning message(s), a `HasProposal`/terminal flag); (b) `CutLabDeltaLineView` (metric label, direction enum, formatted value token e.g. "2.1%"/"1 card", IsMeaningful, the neutral sentence "cutting {card} {lowers|raises} {metric} by …"); (c) sticky-bar fields (round label, cards-remaining, cut-so-far count from RoundPlan + State.Decisions Accepted count); (d) `CutLabCutMadeRowView[]` (card, "cut in {round label}", restore hooks) sourced from `State.Decisions` (Accepted only, most-recent-first by Ordinal); (e) `CutLabCompareRowView[]` (metric label, baseline value, current value, delta token+direction) built from `State.BaselineSnapshot` vs `CutLabProcessResult.CurrentSnapshot` (HIGH-1: server-provided derived-working-list snapshot; the view does NOT re-simulate), plus baselineCount→currentCount context. Also expose the current `CutLabStateJson` string for the decision forms' hidden field. Map all in `From(...)`. Use `DisplayLabelFor`/the metric labels; pull copy verbatim from the UI spec. Changed-lines only; XML-doc new members.
  </action>
  <verify>
    <automated>"/mnt/c/Program Files/dotnet/dotnet.exe" build DeckFlow.Web/DeckFlow.Web.csproj -clp:ErrorsOnly</automated>
  </verify>
  <acceptance_criteria>
    - View model exposes proposal, sticky-bar, cuts-made, and compare fields (grep `Proposal`, `Sticky`, `CutMade`/`CutsMade`, `Compare`).
    - Proposal delta lines are built from `InitialProposalDeltas` (grep) — no simulation/Analyze call in the view model.
    - Compare rows use `CurrentSnapshot` vs `BaselineSnapshot` (server-provided); `From` handles empty/at-100 pools with no null-ref.
    - Delta line carries direction as an enum + a separate formatted value token (not baked into one colored string).
    - Build clean, no new warnings.
  </acceptance_criteria>
  <done>The view model carries every datum the three new sections render, all sourced from ready-made server data.</done>
</task>

<task type="auto">
  <name>Task 2: Render Cut rounds + Cuts made + Compare sections with no-JS decision forms (HIGH-5)</name>
  <read_first>
    - DeckFlow.Web/Views/Deck/CutLab.cshtml (existing result-panel + cutlab-role-group accordion sections + hidden CutLabStateJson + @Html.AntiForgeryToken() usage + data-cut-lab-* attributes; the Role floors section is the insertion point)
    - .planning/workstreams/cut-lab/phases/103-simulation-engine-guided-cut-rounds/103-UI-SPEC.md (Component Contracts 1-4 + Copywriting Contract — class names, data-attrs, 44px, directional token classes, all copy strings)
    - DeckFlow.Web/Controllers/CutLabController.cs (the POST /cut-lab/decide route + bound field names from Plan 103-07 — the form contract)
  </read_first>
  <action>
    Append three `<section class="result-panel">` blocks after the Role floors section, only when `Model.HasResult`:
    (1) Cut rounds: `<h2>Cut rounds</h2>` + sub-line; the `.cutlab-sticky-bar` (three spans `data-cut-lab-sticky-round`/`-remaining`/`-accepted`, 44px); the round banner (`.cutlab-round-banner`); the `.cutlab-proposal` card — heading "Proposed cut: {card}", `.cutlab-proposal__evidence` with neutral `.kb-chip` finding tags, `.cutlab-delta` with one `.cutlab-delta__line` per changed family (value token span `.cutlab-delta__value--up|--down|--none` with `▲`/`▼` glyph `aria-hidden="true"`, neutral sentence) — these come from `Model.Proposal` (HIGH-4 ready-made deltas), the "{n} of 7 metric families changed" summary, a `<details data-cut-lab-delta-expander>` "Show full metric breakdown" listing all 7 families, an optional `.cutlab-proposal__floor-warning` (reusing `.cutlab-finding` styling) when a floor warning exists, and `.cutlab-proposal__actions` — HIGH-5: EACH of the three 44px decision buttons (`.cutlab-decision-btn--accept` accent-fill / `--reject` / `--defer` neutral-bordered) is a submit button inside its OWN `<form method="post" action="/cut-lab/decide">` (or `asp-controller/asp-action`) containing `@Html.AntiForgeryToken()`, `<input type="hidden" name="CutLabStateJson" value="@Model.CutLabStateJson">`, `<input type="hidden" name="CardName" value="@Model.Proposal.CardName">`, and `<input type="hidden" name="Decision" value="accept|reject|defer">`, plus the `data-cut-lab-decision` + `data-cut-lab-card` hooks (so Plan 103-09 can intercept submit). Render the empty ("Nothing to cut") and at-100 ("You're at 100 cards") states per the Copywriting Contract when there is no proposal.
    (2) Cuts made: `<details class="cutlab-cuts-made">` (omitted when no accepted cuts; open when ≤5) with summary "Cuts made · {n} cards" and `.cutlab-cuts-made__row`s (card · "cut in {round}" · a `.cutlab-restore-btn` 44px submit button in its OWN `<form method="post" action="/cut-lab/decide">` with the antiforgery token + hidden CutLabStateJson + CardName + `Decision=restore` + `data-cut-lab-restore` + `data-cut-lab-card`).
    (3) Compare to baseline: `<details class="cutlab-compare">` collapsed, summary "Compare to baseline", the count context sub-line, and a `table[data-prompt-cedh-reference-table]` with columns Metric·Baseline·Current·Delta (all 7 family rows from `Model.Compare`; Delta cell uses the same directional token classes). Keep the hidden `CutLabStateJson` field as the source of truth for the no-JS forms. All copy verbatim from the UI spec; layout classes styled in Plan 103-09. LF endings; do not reflow existing markup.
  </action>
  <verify>
    <automated>"/mnt/c/Program Files/dotnet/dotnet.exe" build DeckFlow.Web/DeckFlow.Web.csproj -clp:ErrorsOnly</automated>
  </verify>
  <acceptance_criteria>
    - The view contains `.cutlab-sticky-bar`, `.cutlab-proposal`, `.cutlab-cuts-made`, `.cutlab-compare` blocks with the specified `data-cut-lab-*` attributes (grep each).
    - Each decision/restore button is a submit inside a `<form method="post" action="/cut-lab/decide">` with `@Html.AntiForgeryToken()` + hidden CutLabStateJson/CardName/Decision (grep `cut-lab/decide` + `AntiForgeryToken` in the new blocks) — the no-JS fallback posts to CutLabController.Decide.
    - Exactly one proposal card renders (no list/carousel) — D-02; its deltas come from Model.Proposal (never re-simulated in the view).
    - Directional token classes `--up`/`--down`/`--none` present; glyphs `aria-hidden`. The 7-family expander and compare table both enumerate all 7 families.
    - Empty/at-100/Nothing-to-cut copy matches the UI spec strings verbatim; build clean, Razor compiles.
  </acceptance_criteria>
  <done>The three new sections render server-side with prescribed copy/classes/data hooks AND real no-JS decision forms posting to /cut-lab/decide.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| server → rendered HTML | Card names / metric labels rendered into the page; must be output-encoded (Razor default) to avoid stored/reflected XSS from a crafted pool. |
| rendered form → /cut-lab/decide | The no-JS decision forms carry the state blob back to the server; antiforgery token guards the form path (validated server-side in Plan 103-07). |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-103-08-01 | Tampering (XSS) | Card names / round labels in Razor output | mitigate | Razor auto-encoded `@` output for all card/metric text (never `Html.Raw`); data-attrs + hidden inputs carry encoded values. |
| T-103-08-02 | Spoofing / CSRF | The new no-JS decision forms | mitigate | Every decision/restore form includes `@Html.AntiForgeryToken()`; CutLabController.Decide validates it ([ValidateAntiForgeryToken], Plan 103-07). |
| T-103-08-03 | Information Disclosure | Verdict copy implying a card is "worse" | mitigate | Copy pulled verbatim from the approved neutral UI-spec strings; the CUT-02 e2e/copy check (Plan 103-10) guards against drift. |
</threat_model>

<verification>
- `"/mnt/c/Program Files/dotnet/dotnet.exe" build DeckFlow.sln -clp:ErrorsOnly` clean.
- Existing CutLab controller/view-model tests stay green: `... test DeckFlow.Web.Tests --filter "FullyQualifiedName~CutLab"`.
- No-JS forms + neutral copy verified visually + JS-disabled in Plan 103-10.
</verification>

<success_criteria>
Cut rounds, Cuts made, and Compare-to-baseline sections render server-side with the exact UI-spec copy, ready-made deltas (HIGH-4), directional tokens, 44px targets, data hooks, AND real no-JS decision/restore forms posting to /cut-lab/decide (HIGH-5); no-JS path fully functional.
</success_criteria>

<output>
Create `.planning/workstreams/cut-lab/phases/103-simulation-engine-guided-cut-rounds/103-08-SUMMARY.md` when done.
</output>
</content>
