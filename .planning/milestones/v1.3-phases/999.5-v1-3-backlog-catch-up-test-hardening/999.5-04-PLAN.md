---
phase: 999.5-v1-3-backlog-catch-up-test-hardening
plan: 999.5-04
title: ChatGPT <result> directive strip — Phase 10 cosmetic noise removal
type: execute
wave: 2
depends_on: ["999.5-01-PLAN"]
files_modified:
  - DeckFlow.Web/Services/PromptBuilders/Analysis/ChatGptAnalysisPromptVariant.cs
  - DeckFlow.Web/Services/PromptBuilders/Comparison/ChatGptComparisonPromptVariant.cs
  - DeckFlow.Web/Services/PromptBuilders/FollowUp/ChatGptFollowUpPromptVariant.cs
  - DeckFlow.Web/Services/PromptBuilders/MetaGap/ChatGptMetaGapPromptVariant.cs
  - DeckFlow.Web/Services/PromptBuilders/SetUpgrade/ChatGptSetUpgradePromptVariant.cs
  - DeckFlow.Web.Tests/ResultContractTests.cs
autonomous: true
requirements:
  - D-24
  - D-25
  - D-26
  - D-27
  - D-28
  - D-18
  - D-19
  - D-20
  - D-21
  - D-22
  - D-23
must_haves:
  truths:
    - "ChatGPT pasted prompts no longer instruct the AI to wrap output in <result>...</result>; ChatGPT honors fenced ```json reliably."
    - "Gemini paste-into-gemini.google.com flow still works because Gemini variants and GeminiJsonMandate are byte-identical pre/post P04."
    - "Legacy uploads with literal <result>...</result> wrappers still parse because JsonTextFormatterService.ExtractJsonPayload is untouched and its regex falls through to brace-finding when no wrapper is present."
    - "ResultContractTests.cs encodes the new contract: 5 ChatGpt inverse-contract Facts plus pruned Gemini-only Theory rows; existing Claude facts and Gemini mandate fact stay byte-identical."
  artifacts:
    - path: "DeckFlow.Web/Services/PromptBuilders/Analysis/ChatGptAnalysisPromptVariant.cs"
      provides: "Analysis prompt without ResultWrapInstruction line at former line 206"
      contains: "deck_profile"
    - path: "DeckFlow.Web/Services/PromptBuilders/Comparison/ChatGptComparisonPromptVariant.cs"
      provides: "Comparison prompt without ResultWrapInstruction line at former line 81"
      contains: "deck_comparison"
    - path: "DeckFlow.Web/Services/PromptBuilders/FollowUp/ChatGptFollowUpPromptVariant.cs"
      provides: "FollowUp prompt without the bullet-wrapped ResultWrapInstruction line at former line 48"
      contains: "deck_comparison"
    - path: "DeckFlow.Web/Services/PromptBuilders/MetaGap/ChatGptMetaGapPromptVariant.cs"
      provides: "MetaGap prompt without the bullet-wrapped ResultWrapInstruction line at former line 144"
      contains: "meta_gap"
    - path: "DeckFlow.Web/Services/PromptBuilders/SetUpgrade/ChatGptSetUpgradePromptVariant.cs"
      provides: "SetUpgrade prompt without ResultWrapInstruction line at former line 127"
      contains: "set_upgrade_report"
    - path: "DeckFlow.Web.Tests/ResultContractTests.cs"
      provides: "5 new ChatGpt inverse-contract Facts + Gemini-only Theory rows; Claude facts + helpers + Gemini mandate fact preserved byte-identical"
      contains: "BuildAnalysisPrompt_for_ChatGpt_emits_fenced_json_directive_not_result_wrapper"
  key_links:
    - from: "ChatGpt*PromptVariant.cs (all 5)"
      to: "JsonTextFormatterService.ResultWrapInstruction"
      via: "REMOVED — no remaining call site"
      pattern: "ResultWrapInstruction"
    - from: "Gemini*PromptVariant.cs (all 5)"
      to: "JsonTextFormatterService.ResultWrapInstruction"
      via: "PRESERVED — still emits the const (D-25 invariant)"
      pattern: "ResultWrapInstruction"
    - from: "JsonTextFormatterService.ExtractJsonPayload"
      to: "ResultTagRegex fall-through to brace-finding"
      via: "UNTOUCHED — D-26 invariant preserves backward-compat for Gemini + legacy uploads"
      pattern: "<result>\\\\s*(.*?)\\\\s*</result>"
threat_model:
  stride: none
  asvs_level: 1
  notes: "No new attack surface — prompt directive removal + test contract refactor. No auth boundary, no endpoint added, no DB schema, no external integration touched. The AI's output shape changes (no <result> wrapper from ChatGPT) but the consuming parser (JsonTextFormatterService.ExtractJsonPayload) has fallback brace-finding for unwrapped JSON — already exercised by Claude responses post-Phase 999.2 — so no parser regression. Mitigations: N/A."
---

<objective>
Strip the cosmetic `<result>...</result>` wrap directive from all 5 ChatGPT prompt variants (Analysis, Comparison, FollowUp, MetaGap, SetUpgrade) and refactor the cross-AI test contract in `ResultContractTests.cs` to match the new shape, all in a single atomic commit (D-28).

**Why:** ChatGPT honors the fenced ```json directive reliably; the Phase 10 unified `<result>` wrapper produces a visible literal `<result>` token in the ChatGPT UI before the JSON code block (user-reported 2026-05-21 7:40pm MDT via screenshot). Mirror the Phase 999.2 Claude removal pattern (`ClaudeAnalysisPromptVariant.cs` carries fenced-only output).

**Why atomic (D-28):** The test Theory rows currently assert `<result>` substring presence for ChatGPT. Removing those rows without simultaneously stripping the directive would leave the Theory passing accidentally; stripping the directive without simultaneously updating the test contract would leave the Theory failing. Both changes must land in the same commit to avoid an intermediate red state.

**Why Gemini stays untouched (D-25):** `GeminiJsonMandate` is the last-instruction-wins lever validated by the 2026-05-09 integration test where Gemini omitted `<result>` when given fenced-only instructions. `JsonTextFormatterService.cs:16-25` documents the rationale inline.

**Why parser stays untouched (D-26):** `JsonTextFormatterService.ExtractJsonPayload` regex `<result>\s*(.*?)\s*</result>` falls through to brace-finding on miss (lines 62-66), covering (a) Gemini wrapped responses, (b) ChatGPT fenced-only post-P04, (c) Claude fenced-only post-999.2, (d) legacy uploads with literal `<result>` from before P04.

Purpose: Eliminate visible literal `<result>` token in ChatGPT pasted prompts without regressing Gemini or legacy upload parsing.
Output: 5 prompt-variant files with one AppendLine line removed each; ResultContractTests.cs with 5 Theory rows pruned to Gemini-only and 5 new ChatGpt inverse-contract Facts added; a single commit landing all 6 file edits together.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/999.5-v1-3-backlog-catch-up-test-hardening/999.5-CONTEXT.md
@.planning/phases/999.5-v1-3-backlog-catch-up-test-hardening/999.5-01-PLAN.md
@.planning/codebase/TESTING.md
@CLAUDE.md
@DeckFlow.Web/Services/JsonTextFormatterService.cs
@DeckFlow.Web/Services/PromptBuilders/Analysis/ChatGptAnalysisPromptVariant.cs
@DeckFlow.Web/Services/PromptBuilders/Analysis/ClaudeAnalysisPromptVariant.cs
@DeckFlow.Web/Services/PromptBuilders/Comparison/ChatGptComparisonPromptVariant.cs
@DeckFlow.Web/Services/PromptBuilders/FollowUp/ChatGptFollowUpPromptVariant.cs
@DeckFlow.Web/Services/PromptBuilders/MetaGap/ChatGptMetaGapPromptVariant.cs
@DeckFlow.Web/Services/PromptBuilders/SetUpgrade/ChatGptSetUpgradePromptVariant.cs
@DeckFlow.Web.Tests/ResultContractTests.cs

<interfaces>
<!-- Key contracts the executor needs. Extracted from codebase 2026-05-21. -->

From DeckFlow.Web/Services/JsonTextFormatterService.cs:13-25 (lines DO NOT change):
- `internal const string ResultWrapInstruction = "Wrap the entire JSON response in <result>...</result> tags ..."`
- `internal const string GeminiJsonMandate = "MANDATORY — DO NOT SKIP: Your response MUST end with a <result>...</result> block ..."`

Strip targets (verified via grep 2026-05-21 — 5 ChatGpt + 5 Gemini call sites total; strip ONLY the 5 ChatGpt sites):
- `DeckFlow.Web/Services/PromptBuilders/Analysis/ChatGptAnalysisPromptVariant.cs:206` — `builder.AppendLine(JsonTextFormatterService.ResultWrapInstruction);` (direct shape)
- `DeckFlow.Web/Services/PromptBuilders/Comparison/ChatGptComparisonPromptVariant.cs:81` — `builder.AppendLine(JsonTextFormatterService.ResultWrapInstruction);` (direct shape)
- `DeckFlow.Web/Services/PromptBuilders/FollowUp/ChatGptFollowUpPromptVariant.cs:48` — `builder.AppendLine($"- {JsonTextFormatterService.ResultWrapInstruction}");` (interpolated bullet shape — the leading `- ` is part of an OUTPUT FORMAT bullet list; preceding and following lines are also `builder.AppendLine("- ...")` bullets so the bullet list remains well-formed after removal)
- `DeckFlow.Web/Services/PromptBuilders/MetaGap/ChatGptMetaGapPromptVariant.cs:144` — `builder.AppendLine($"- {JsonTextFormatterService.ResultWrapInstruction}");` (interpolated bullet shape — the leading `- ` is part of an OUTPUT CONTRACT bullet list; preceding line 143 and following line 145 are also `builder.AppendLine("- ...")` bullets so the bullet list remains well-formed after removal)
- `DeckFlow.Web/Services/PromptBuilders/SetUpgrade/ChatGptSetUpgradePromptVariant.cs:127` — `builder.AppendLine(JsonTextFormatterService.ResultWrapInstruction);` (direct shape)

Sites NOT to touch (D-25 invariant — all 5 Gemini variants):
- `DeckFlow.Web/Services/PromptBuilders/Analysis/GeminiAnalysisPromptVariant.cs:213`
- `DeckFlow.Web/Services/PromptBuilders/Comparison/GeminiComparisonPromptVariant.cs:86`
- `DeckFlow.Web/Services/PromptBuilders/FollowUp/GeminiFollowUpPromptVariant.cs:52`
- `DeckFlow.Web/Services/PromptBuilders/MetaGap/GeminiMetaGapPromptVariant.cs:150`
- `DeckFlow.Web/Services/PromptBuilders/SetUpgrade/GeminiSetUpgradePromptVariant.cs:126`

Parser (D-26 invariant — DO NOT TOUCH):
- `DeckFlow.Web/Services/JsonTextFormatterService.cs:43-66` — `ExtractJsonPayload` + `ResultTagRegex`. Regex fall-through to `FindPayloadStart` brace-finder at line 68 covers ChatGPT-fenced-only post-P04.

Test contract canonical references (DeckFlow.Web.Tests/ResultContractTests.cs):
- Lines 71-96: `[Theory]` `BuildAnalysisPrompt_emits_result_wrap_directive_for_every_ai` — current InlineData rows: `[InlineData("ChatGPT")]` (line 72), `[InlineData("Gemini")]` (line 73). DROP the ChatGPT row.
- Lines 101-126: `[Fact]` `BuildAnalysisPrompt_for_Claude_emits_fenced_json_directive_not_result_wrapper` — MIRROR SOURCE for new ChatGpt Fact. Asserts `Assert.Contains("fenced \`\`\`json code block", prompt)`, `Assert.DoesNotContain("<result>", prompt)`, `Assert.DoesNotContain("</result>", prompt)`.
- Lines 130-152: `[Theory]` SetUpgrade. DROP ChatGPT row (line 131).
- Lines 154-177: `[Fact]` SetUpgrade Claude. MIRROR SOURCE. Adds `Assert.Contains("set_upgrade_report", prompt)`.
- Lines 181-202: `[Theory]` Comparison. DROP ChatGPT row (line 182).
- Lines 204-226: `[Fact]` Comparison Claude. MIRROR SOURCE. Adds `Assert.Contains("deck_comparison", prompt)`.
- Lines 230-241: `[Theory]` FollowUp. DROP ChatGPT row (line 231).
- Lines 243-255: `[Fact]` FollowUp Claude. MIRROR SOURCE. Adds `Assert.Contains("deck_comparison", prompt)`.
- Lines 259-276: `[Theory]` MetaGap. DROP ChatGPT row (line 260).
- Lines 278-296: `[Fact]` MetaGap Claude. MIRROR SOURCE. Adds `Assert.Contains("meta_gap", prompt)`.
- Lines 300-326: `Every_dispatcher_returns_distinct_content_per_ai_platform` — UNCHANGED (3 distinct platforms still hold).
- Lines 329-347: `Claude_variant_uses_xml_skeleton_with_no_api_role_blocks` — UNCHANGED.
- Lines 351-361: `AssertContainsResultWrap` helper — UNCHANGED (still serves Gemini Theory rows).
- Lines 363-386: `BuildAnalysisPrompt_for_Gemini_ends_with_mandate_block` — UNCHANGED (D-25 invariant).
- Lines 388-413: `BuildSampleDeckSummary` helper — UNCHANGED.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Strip ChatGPT <result> directive from 5 prompt variants + refactor ResultContractTests.cs (atomic commit per D-28)</name>
  <files>
    DeckFlow.Web/Services/PromptBuilders/Analysis/ChatGptAnalysisPromptVariant.cs,
    DeckFlow.Web/Services/PromptBuilders/Comparison/ChatGptComparisonPromptVariant.cs,
    DeckFlow.Web/Services/PromptBuilders/FollowUp/ChatGptFollowUpPromptVariant.cs,
    DeckFlow.Web/Services/PromptBuilders/MetaGap/ChatGptMetaGapPromptVariant.cs,
    DeckFlow.Web/Services/PromptBuilders/SetUpgrade/ChatGptSetUpgradePromptVariant.cs,
    DeckFlow.Web.Tests/ResultContractTests.cs
  </files>

  <read_first>
    Before editing, read each file in full or at the specified line range to confirm AppendLine shape and surrounding bullet-list context:

    1. `.planning/phases/999.5-v1-3-backlog-catch-up-test-hardening/999.5-CONTEXT.md` §`P04 ChatGPT <result> Directive Strip` (decisions D-24..D-28) — the binding contract.
    2. `.planning/phases/999.5-v1-3-backlog-catch-up-test-hardening/999.5-CONTEXT.md` §`<canonical_refs>` §`ChatGPT <result> directive strip (P04)` — exact file:line refs.
    3. `CLAUDE.md` — touch-only-lines-that-need-touching invariant; DO NOT auto-format the 5 prompt-variant files; DO NOT collapse `{ get; init; }`; DO NOT re-indent C# raw-string literals; preserve LF line endings (`.gitattributes` enforces); plain default-author commits, NO `Co-Authored-By` trailer.
    4. `DeckFlow.Web/Services/JsonTextFormatterService.cs` lines 13-25 (const definition — VERIFY const stays; only call sites change) and lines 43-66 (`ExtractJsonPayload` + `ResultTagRegex` — VERIFY parser is NOT in the edit list).
    5. `DeckFlow.Web/Services/PromptBuilders/Analysis/ChatGptAnalysisPromptVariant.cs` lines 195-215 — confirm line 206 reads `builder.AppendLine(JsonTextFormatterService.ResultWrapInstruction);` (direct shape, no `$"- {...}"` wrapper). Line 198 carries the fenced ```json directive that is the replacement signal.
    6. `DeckFlow.Web/Services/PromptBuilders/Comparison/ChatGptComparisonPromptVariant.cs` lines 70-90 — confirm line 81 reads `builder.AppendLine(JsonTextFormatterService.ResultWrapInstruction);` (direct shape).
    7. `DeckFlow.Web/Services/PromptBuilders/FollowUp/ChatGptFollowUpPromptVariant.cs` lines 38-58 — confirm line 48 reads `builder.AppendLine($"- {JsonTextFormatterService.ResultWrapInstruction}");` (interpolated bullet shape inside an OUTPUT FORMAT list; line 47 ends with the fenced ```json directive, line 49 begins `- Keep the JSON valid...`).
    8. `DeckFlow.Web/Services/PromptBuilders/MetaGap/ChatGptMetaGapPromptVariant.cs` lines 134-154 — confirm line 144 reads `builder.AppendLine($"- {JsonTextFormatterService.ResultWrapInstruction}");` (interpolated bullet shape inside an OUTPUT CONTRACT list; line 143 ends with the fenced ```json directive, line 145 begins `- The prose summary must come before the JSON block.`).
    9. `DeckFlow.Web/Services/PromptBuilders/SetUpgrade/ChatGptSetUpgradePromptVariant.cs` lines 120-135 — confirm line 127 reads `builder.AppendLine(JsonTextFormatterService.ResultWrapInstruction);` (direct shape).
    10. `DeckFlow.Web/Services/PromptBuilders/Analysis/ClaudeAnalysisPromptVariant.cs` (entire file) — Phase 999.2 prior art. The Claude variant carries the fenced ```json directive and does NOT call `ResultWrapInstruction`. P04 produces the same shape for ChatGpt: keep the fenced ```json line that already exists, delete only the `ResultWrapInstruction` line.
    11. `DeckFlow.Web.Tests/ResultContractTests.cs` (entire 414-line file) — the test contract refactor target. Note the existing 5 Claude inverse-contract Facts at lines 102-126, 154-177, 204-226, 243-255, 278-296 are the MIRROR SOURCE for the 5 new ChatGpt Facts.
    12. `.planning/codebase/TESTING.md` — xUnit naming convention `MethodUnderTest_Scenario_ExpectedOutcome`, no FluentAssertions, file-per-type.
  </read_first>

  <behavior>
    After this task:
    - Test 1 (existing, MUST STILL PASS): `BuildAnalysisPrompt_emits_result_wrap_directive_for_every_ai("Gemini")` — Gemini still emits `<result>` substring; ChatGPT InlineData row is REMOVED so it cannot regress.
    - Test 2 (existing, MUST STILL PASS): same shape for `BuildSetUpgradePrompt_…`, `BuildComparisonPrompt_…`, `BuildFollowUpPrompt_…`, `CedhMetaGap_BuildPrompt_…` — Gemini-only after this task.
    - Test 3 (existing, MUST STILL PASS): `BuildAnalysisPrompt_for_Claude_emits_fenced_json_directive_not_result_wrapper` (and the 4 sibling Claude facts) — byte-identical behavior.
    - Test 4 (NEW, MUST PASS): `BuildAnalysisPrompt_for_ChatGpt_emits_fenced_json_directive_not_result_wrapper` — asserts `Assert.Contains("fenced \`\`\`json code block", prompt)`, `Assert.DoesNotContain("<result>", prompt)`, `Assert.DoesNotContain("</result>", prompt)`.
    - Test 5 (NEW, MUST PASS): `BuildSetUpgradePrompt_for_ChatGpt_emits_fenced_json_directive_not_result_wrapper` — same three asserts plus `Assert.Contains("set_upgrade_report", prompt)`.
    - Test 6 (NEW, MUST PASS): `BuildComparisonPrompt_for_ChatGpt_emits_fenced_json_directive_not_result_wrapper` — same three asserts plus `Assert.Contains("deck_comparison", prompt)`.
    - Test 7 (NEW, MUST PASS): `BuildFollowUpPrompt_for_ChatGpt_emits_fenced_json_directive_not_result_wrapper` — same three asserts plus `Assert.Contains("deck_comparison", prompt)`.
    - Test 8 (NEW, MUST PASS): `CedhMetaGap_BuildPrompt_for_ChatGpt_emits_fenced_json_directive_not_result_wrapper` — same three asserts plus `Assert.Contains("meta_gap", prompt)`.
    - Test 9 (existing, MUST STILL PASS): `Every_dispatcher_returns_distinct_content_per_ai_platform` — three platforms still produce distinct prompts.
    - Test 10 (existing, MUST STILL PASS): `BuildAnalysisPrompt_for_Gemini_ends_with_mandate_block` — Gemini still ends with `Nothing else after </result>.`.
  </behavior>

  <action>
    Atomic single-commit change per D-28. The 5 prompt-variant edits and the test refactor MUST land together because each enforces the other; splitting would create an intermediate red state where the existing `[Theory]` rows for ChatGPT fail.

    **Step A — Strip `ResultWrapInstruction` from 5 ChatGpt prompt-variant files.** Each edit is a one-line deletion. Touch ONLY the line that needs touching per `CLAUDE.md`; do NOT re-indent, do NOT reformat, do NOT collapse `{ get; init; }`, do NOT touch any line above or below the strip target except as required by surrounding-line indentation consistency (which is already correct — verify don't modify).

      1. `DeckFlow.Web/Services/PromptBuilders/Analysis/ChatGptAnalysisPromptVariant.cs` line 206: delete the line `        builder.AppendLine(JsonTextFormatterService.ResultWrapInstruction);`. Line 198 already carries `builder.AppendLine("   You MUST return the JSON inside a fenced ```json code block (triple-backtick json). Do not return raw JSON outside a code block.");` — this is the replacement signal per D-24, no new line needed.
      2. `DeckFlow.Web/Services/PromptBuilders/Comparison/ChatGptComparisonPromptVariant.cs` line 81: delete the line `        builder.AppendLine(JsonTextFormatterService.ResultWrapInstruction);`.
      3. `DeckFlow.Web/Services/PromptBuilders/FollowUp/ChatGptFollowUpPromptVariant.cs` line 48: delete the line `        builder.AppendLine($"- {JsonTextFormatterService.ResultWrapInstruction}");`. This is the interpolated-bullet shape. Verified: line 47 ends with the fenced ```json directive bullet and line 49 continues with `- Keep the JSON valid and include every required field from this schema:` — the bullet list remains well-formed after removal, no orphan-bullet rescue needed.
      4. `DeckFlow.Web/Services/PromptBuilders/MetaGap/ChatGptMetaGapPromptVariant.cs` line 144: delete the line `        builder.AppendLine($"- {JsonTextFormatterService.ResultWrapInstruction}");`. This is the interpolated-bullet shape. Verified: line 143 ends with the fenced ```json directive bullet and line 145 continues with `- The prose summary must come before the JSON block.` — the bullet list remains well-formed after removal, no orphan-bullet rescue needed.
      5. `DeckFlow.Web/Services/PromptBuilders/SetUpgrade/ChatGptSetUpgradePromptVariant.cs` line 127: delete the line `        builder.AppendLine(JsonTextFormatterService.ResultWrapInstruction);`.

    **Step B — Refactor `DeckFlow.Web.Tests/ResultContractTests.cs`.** Per D-27:

      6. In each of the 5 `[Theory]` tests at lines 71, 130, 181, 230, 259, remove the `[InlineData("ChatGPT")]` row. Keep the `[InlineData("Gemini")]` row and keep the method body byte-identical. The method name MAY stay as `…_emits_result_wrap_directive_for_every_ai` (the "every_ai" is now Gemini-only post-P04, but the assertion shape is preserved) OR be renamed to `…_for_Gemini_emits_result_wrap_directive` if single-platform reads cleaner per D-27. Executor's discretion (Codex): pick whichever produces a smaller diff. If renaming, also collapse `[Theory] [InlineData("Gemini")] public void X(string platform)` to `[Fact] public void X()` with `var platform = "Gemini";` inside. Diff-size minimization wins — leaving the `[Theory]` shape with one row is acceptable and produces the smallest diff.
      7. Add 5 new `[Fact]` methods, one per prompt builder, mirroring the existing Claude inverse-contract Facts. Names (verbatim per CONTEXT.md D-27):
         - `BuildAnalysisPrompt_for_ChatGpt_emits_fenced_json_directive_not_result_wrapper`
         - `BuildSetUpgradePrompt_for_ChatGpt_emits_fenced_json_directive_not_result_wrapper`
         - `BuildComparisonPrompt_for_ChatGpt_emits_fenced_json_directive_not_result_wrapper`
         - `BuildFollowUpPrompt_for_ChatGpt_emits_fenced_json_directive_not_result_wrapper`
         - `CedhMetaGap_BuildPrompt_for_ChatGpt_emits_fenced_json_directive_not_result_wrapper`

         Each Fact constructs the same registry as the matching Claude Fact at lines 102-126 / 154-177 / 204-226 / 243-255 / 278-296 (use `BuildAnalysisRegistry()`, `BuildSetUpgradeRegistry()`, `BuildComparisonRegistry()`, `BuildFollowUpRegistry()`, `BuildMetaGapRegistry()` respectively), builds a request DTO with `TargetAiPlatform = "ChatGPT"`, calls `registry.Build(AiPlatform.Normalize("ChatGPT"), …)` with the SAME argument shape as the matching Claude Fact, and asserts exactly:
         - `Assert.Contains("fenced \`\`\`json code block", prompt);`
         - `Assert.DoesNotContain("<result>", prompt);`
         - `Assert.DoesNotContain("</result>", prompt);`

         For per-Fact additional assertions, mirror the matching Claude Fact:
         - SetUpgrade ChatGpt also asserts `Assert.Contains("set_upgrade_report", prompt);` (mirror of line 174).
         - Comparison ChatGpt also asserts `Assert.Contains("deck_comparison", prompt);` (mirror of line 223).
         - FollowUp ChatGpt also asserts `Assert.Contains("deck_comparison", prompt);` (mirror of line 252).
         - MetaGap ChatGpt also asserts `Assert.Contains("meta_gap", prompt);` (mirror of line 293).
         - Analysis ChatGpt has no extra schema-label assertion (mirror of lines 102-126 which only has the 3 generic asserts).

         Placement: each new ChatGpt Fact goes immediately AFTER the matching Claude Fact in source order. So the Analysis section becomes Theory at line 71 (Gemini-only) → Claude Fact at line 101 → new ChatGpt Fact, and similarly for the other 4 sections. This keeps related tests adjacent.
      8. Leave UNCHANGED (byte-identical):
         - `Every_dispatcher_returns_distinct_content_per_ai_platform` (line 300).
         - `Claude_variant_uses_xml_skeleton_with_no_api_role_blocks` (line 329).
         - `AssertContainsResultWrap` helper (line 351) — still serves Gemini Theory rows.
         - `BuildAnalysisPrompt_for_Gemini_ends_with_mandate_block` (line 363) — D-25 invariant.
         - `BuildSampleDeckSummary` helper (line 388).
         - All 5 `_for_Claude_…` Facts (lines 102, 154, 204, 243, 278) — byte-identical.

    **Step C — Verify file integrity before committing.** Run all four guard checks before `git add`:

      9. `grep -rn "ResultWrapInstruction" DeckFlow.Web/Services/PromptBuilders/` MUST return exactly 5 hits, all under `Gemini*PromptVariant.cs`. ChatGpt directory must show zero hits.
      10. `grep -rn "ResultWrapInstruction" DeckFlow.Web/Services/PromptBuilders/Analysis/ChatGptAnalysisPromptVariant.cs DeckFlow.Web/Services/PromptBuilders/Comparison/ChatGptComparisonPromptVariant.cs DeckFlow.Web/Services/PromptBuilders/FollowUp/ChatGptFollowUpPromptVariant.cs DeckFlow.Web/Services/PromptBuilders/MetaGap/ChatGptMetaGapPromptVariant.cs DeckFlow.Web/Services/PromptBuilders/SetUpgrade/ChatGptSetUpgradePromptVariant.cs` MUST return zero hits.
      11. `git diff --stat -- DeckFlow.Web/Services/PromptBuilders/Analysis/GeminiAnalysisPromptVariant.cs DeckFlow.Web/Services/PromptBuilders/Comparison/GeminiComparisonPromptVariant.cs DeckFlow.Web/Services/PromptBuilders/FollowUp/GeminiFollowUpPromptVariant.cs DeckFlow.Web/Services/PromptBuilders/MetaGap/GeminiMetaGapPromptVariant.cs DeckFlow.Web/Services/PromptBuilders/SetUpgrade/GeminiSetUpgradePromptVariant.cs DeckFlow.Web/Services/JsonTextFormatterService.cs` MUST return empty (no Gemini variant or parser file modified — D-25 + D-26 invariants).
      12. For each of the 5 new Fact names, `grep -c "<name>" DeckFlow.Web.Tests/ResultContractTests.cs` MUST return 1.

    **Step D — Build + test gate per D-21/D-22.** Run the build and focused test commands and confirm both exit 0 before committing:

      13. `"/mnt/c/Program Files/dotnet/dotnet.exe" build DeckFlow.sln -c Release -nologo` — MUST exit 0 with warning count <= post-Phase-999.4 baseline (commit `4af092a`).
      14. `"/mnt/c/Program Files/dotnet/dotnet.exe" test DeckFlow.sln -c Release --no-build --filter "FullyQualifiedName~ResultContractTests" --nologo` — MUST exit 0 with the 5 new ChatGpt Facts present in the passed-test list, the 5 pruned Theory rows still passing for Gemini, and all existing Claude/Gemini facts passing.
      15. `"/mnt/c/Program Files/dotnet/dotnet.exe" test DeckFlow.sln -c Release --no-build --nologo` — full-suite gate. Must show `failed: 0` for the P04-touched ranges (specifically: zero new failures vs the post-P03 baseline established by 999.5-01/02/03). Pre-existing backlog failures NOT in scope (edhtop16 filter-defaults, Gemini paste-limit) MAY remain.

    **Step E — Commit per D-19/D-20/D-28.** Single atomic commit:

      16. Confirm local git config matches D-20 (`luntc1972` / `luntc1972@yahoo.com`). If `git config --local user.email` returns anything else, STOP and surface the discrepancy. Do NOT touch global git config.
      17. `git add DeckFlow.Web/Services/PromptBuilders/Analysis/ChatGptAnalysisPromptVariant.cs DeckFlow.Web/Services/PromptBuilders/Comparison/ChatGptComparisonPromptVariant.cs DeckFlow.Web/Services/PromptBuilders/FollowUp/ChatGptFollowUpPromptVariant.cs DeckFlow.Web/Services/PromptBuilders/MetaGap/ChatGptMetaGapPromptVariant.cs DeckFlow.Web/Services/PromptBuilders/SetUpgrade/ChatGptSetUpgradePromptVariant.cs DeckFlow.Web.Tests/ResultContractTests.cs`. Do NOT use `git add -A` or `git add .` per the GSD git-safety protocol.
      18. Commit with a HEREDOC body, plain-author (NO `Co-Authored-By` trailer per D-20 and `CLAUDE.md`):

          Subject: `feat(prompts): strip ChatGPT <result> directive — Phase 10 cosmetic noise removal`

          Body: cite the 2026-05-21 7:40pm MDT user-report origin; reference D-24..D-28; list the 5 stripped files + the 1 refactored test file; note Gemini variants and parser stay untouched (D-25/D-26 invariants); note the atomic-commit rationale (D-28); include the build+test deltas per D-23 (e.g., "ResultContractTests: +5 ChatGpt facts, -5 ChatGPT InlineData rows; full-suite delta: +5 facts; `failed: 0` in P04-touched ranges").
      19. Run `git log --pretty="%an <%ae>" -1` — MUST return `luntc1972 <luntc1972@yahoo.com>`. Run `git log --pretty=%B -1 | grep -ci "Co-Authored-By"` — MUST return 0.

    Do NOT include code blocks, full implementations, or formatted snippets in the commit body. Use prose + bullet points.
  </action>

  <verify>
    <automated>
      grep -c "ResultWrapInstruction" DeckFlow.Web/Services/PromptBuilders/Analysis/ChatGptAnalysisPromptVariant.cs DeckFlow.Web/Services/PromptBuilders/Comparison/ChatGptComparisonPromptVariant.cs DeckFlow.Web/Services/PromptBuilders/FollowUp/ChatGptFollowUpPromptVariant.cs DeckFlow.Web/Services/PromptBuilders/MetaGap/ChatGptMetaGapPromptVariant.cs DeckFlow.Web/Services/PromptBuilders/SetUpgrade/ChatGptSetUpgradePromptVariant.cs | grep -v ':0$' | wc -l   # expect 0
      grep -c "ResultWrapInstruction" DeckFlow.Web/Services/PromptBuilders/Analysis/GeminiAnalysisPromptVariant.cs DeckFlow.Web/Services/PromptBuilders/Comparison/GeminiComparisonPromptVariant.cs DeckFlow.Web/Services/PromptBuilders/FollowUp/GeminiFollowUpPromptVariant.cs DeckFlow.Web/Services/PromptBuilders/MetaGap/GeminiMetaGapPromptVariant.cs DeckFlow.Web/Services/PromptBuilders/SetUpgrade/GeminiSetUpgradePromptVariant.cs | grep -v ':0$' | wc -l   # expect 5
      git diff HEAD~1 --stat -- DeckFlow.Web/Services/PromptBuilders/Analysis/GeminiAnalysisPromptVariant.cs DeckFlow.Web/Services/PromptBuilders/Comparison/GeminiComparisonPromptVariant.cs DeckFlow.Web/Services/PromptBuilders/FollowUp/GeminiFollowUpPromptVariant.cs DeckFlow.Web/Services/PromptBuilders/MetaGap/GeminiMetaGapPromptVariant.cs DeckFlow.Web/Services/PromptBuilders/SetUpgrade/GeminiSetUpgradePromptVariant.cs DeckFlow.Web/Services/JsonTextFormatterService.cs   # expect empty
      grep -c "BuildAnalysisPrompt_for_ChatGpt_emits_fenced_json_directive_not_result_wrapper" DeckFlow.Web.Tests/ResultContractTests.cs   # expect 1
      grep -c "BuildSetUpgradePrompt_for_ChatGpt_emits_fenced_json_directive_not_result_wrapper" DeckFlow.Web.Tests/ResultContractTests.cs   # expect 1
      grep -c "BuildComparisonPrompt_for_ChatGpt_emits_fenced_json_directive_not_result_wrapper" DeckFlow.Web.Tests/ResultContractTests.cs   # expect 1
      grep -c "BuildFollowUpPrompt_for_ChatGpt_emits_fenced_json_directive_not_result_wrapper" DeckFlow.Web.Tests/ResultContractTests.cs   # expect 1
      grep -c "CedhMetaGap_BuildPrompt_for_ChatGpt_emits_fenced_json_directive_not_result_wrapper" DeckFlow.Web.Tests/ResultContractTests.cs   # expect 1
      "/mnt/c/Program Files/dotnet/dotnet.exe" build DeckFlow.sln -c Release -nologo   # expect exit 0
      "/mnt/c/Program Files/dotnet/dotnet.exe" test DeckFlow.sln -c Release --no-build --filter "FullyQualifiedName~ResultContractTests" --nologo   # expect exit 0, 5 new ChatGpt facts in passed list
      "/mnt/c/Program Files/dotnet/dotnet.exe" test DeckFlow.sln -c Release --no-build --nologo   # expect exit 0 for P04-touched ranges; pre-existing backlog failures NOT in scope may remain
      git log --pretty="%an <%ae>" -1   # expect "luntc1972 <luntc1972@yahoo.com>"
      git log --pretty=%B -1 | grep -ci "Co-Authored-By"   # expect 0
    </automated>
  </verify>

  <acceptance_criteria>
    - `grep -rn "ResultWrapInstruction" DeckFlow.Web/Services/PromptBuilders/` returns exactly 5 hits, all under `*/Gemini*PromptVariant.cs` paths. Zero hits under `*/ChatGpt*PromptVariant.cs`.
    - `git diff HEAD~1 -- DeckFlow.Web/Services/PromptBuilders/**/Gemini*.cs DeckFlow.Web/Services/JsonTextFormatterService.cs` returns empty output (D-25 + D-26 invariants — Gemini variants and parser are byte-identical pre/post P04).
    - For each of the 5 new ChatGpt Fact names, `grep -c "<name>" DeckFlow.Web.Tests/ResultContractTests.cs` returns 1.
    - The 5 existing Claude inverse-contract Facts (`BuildAnalysisPrompt_for_Claude_…`, etc.) at lines 102, 154, 204, 243, 278 are byte-identical pre/post P04 — verified by `git diff HEAD~1 -- DeckFlow.Web.Tests/ResultContractTests.cs` showing no edits to those line ranges (only additions for the new ChatGpt Facts and InlineData-row removals from the 5 Theory tests).
    - `AssertContainsResultWrap` helper (line 351) is byte-identical pre/post P04.
    - `BuildAnalysisPrompt_for_Gemini_ends_with_mandate_block` (line 363) is byte-identical pre/post P04.
    - `"/mnt/c/Program Files/dotnet/dotnet.exe" build DeckFlow.sln -c Release -nologo` exits 0 with warning count <= post-Phase-999.4 baseline (commit `4af092a`).
    - `"/mnt/c/Program Files/dotnet/dotnet.exe" test DeckFlow.sln -c Release --no-build --filter "FullyQualifiedName~ResultContractTests" --nologo` exits 0; passed-test list includes the 5 new ChatGpt Facts.
    - `"/mnt/c/Program Files/dotnet/dotnet.exe" test DeckFlow.sln -c Release --no-build --nologo` exits 0 with `failed: 0` for P04-touched ranges; full-suite delta is +5 facts vs the post-P03 baseline.
    - `git log --pretty="%an <%ae>" -1` returns exactly `luntc1972 <luntc1972@yahoo.com>` per D-20.
    - `git log --pretty=%B -1 | grep -ci "Co-Authored-By"` returns 0 (no trailer per D-20 and `CLAUDE.md`).
    - The commit message subject starts with `feat(prompts):` and the body cites D-24..D-28 + the 2026-05-21 user-report origin + the build+test delta per D-23.
    - No file in `files_modified` has been auto-formatted (no whitespace-only changes outside the 6 edit targets, no `{ get; init; }` collapses, no inline `[Attribute]` reformats, no re-indented raw strings). `git diff HEAD~1 --check` returns clean.
  </acceptance_criteria>

  <done>
    Single commit shipped containing: (a) 5 ChatGpt prompt-variant files with `JsonTextFormatterService.ResultWrapInstruction` AppendLine deleted, (b) `DeckFlow.Web.Tests/ResultContractTests.cs` with 5 `[InlineData("ChatGPT")]` rows pruned and 5 new ChatGpt inverse-contract Facts added. Build green, ResultContractTests filter exits 0, full-suite shows `failed: 0` in P04-touched ranges. Gemini variants and `JsonTextFormatterService` parser are byte-identical pre/post commit. Commit author is `luntc1972 <luntc1972@yahoo.com>`, no `Co-Authored-By` trailer.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none introduced) | P04 is a prompt-directive removal + test-contract refactor. No new trust boundary crossed. ChatGPT remains an untrusted external AI; user paste-in flow is unchanged. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-999.5-04-01 | (none) | All edits | n/a — no security-relevant change | No new attack surface. The strip reduces a prompt instruction line that was previously emitted to the AI. The AI's output shape changes (no `<result>` wrapper from ChatGPT) but the consuming parser (`JsonTextFormatterService.ExtractJsonPayload`) has fallback brace-finding for unwrapped JSON — already exercised by Claude responses post-Phase 999.2 — so no parser regression. No auth boundary, no endpoint added, no DB schema change, no external integration touched, no input validation removed. ASVS L1: N/A. |

(No `T-999.5-04-SC` package-supply-chain row — no new package installs in P04.)
</threat_model>

<verification>
- Build green: `"/mnt/c/Program Files/dotnet/dotnet.exe" build DeckFlow.sln -c Release -nologo` exits 0 with warning count <= post-Phase-999.4 baseline (commit `4af092a`).
- Full-suite test: `"/mnt/c/Program Files/dotnet/dotnet.exe" test DeckFlow.sln -c Release --no-build --nologo` exits 0 with `failed: 0` in P04-touched ranges; +5 facts delta vs post-P03 baseline.
- Focused test: `"/mnt/c/Program Files/dotnet/dotnet.exe" test DeckFlow.sln -c Release --no-build --filter "FullyQualifiedName~ResultContractTests" --nologo` exits 0 and the 5 new ChatGpt Facts appear in the passed list.
- Grep invariants: 0 hits of `ResultWrapInstruction` under `*/ChatGpt*PromptVariant.cs`; 5 hits under `*/Gemini*PromptVariant.cs`.
- Git invariants: `git diff HEAD~1` empty for all 5 Gemini variants and `JsonTextFormatterService.cs`; commit author is `luntc1972 <luntc1972@yahoo.com>`; commit body contains zero `Co-Authored-By` lines.
- Manual UI smoke (optional, executor's discretion if Codex has browser access): paste an Analysis prompt into chat.openai.com — ChatGPT response should no longer carry a leading literal `<result>` token before the fenced JSON. NOT a gate; the grep + test gates are sufficient.
</verification>

<success_criteria>
- All 6 file edits land in a single atomic commit per D-28.
- Phase 999.5 P04 closes one quality-debt item (visible literal `<result>` token in ChatGPT UI, reported 2026-05-21 7:40pm MDT).
- Phase 999.5 retains full backward compatibility for Gemini (D-25) and legacy `<result>`-wrapped uploads (D-26).
- `ResultContractTests.cs` net delta: -5 InlineData rows, +5 Fact methods (+ ~120 lines, - ~10 lines).
- Phase 999.5 test gate per D-21/D-22 stays green for all shipped-in-999.5 ranges (P01 + P02 + P03 + P04).
</success_criteria>

<output>
Create `.planning/phases/999.5-v1-3-backlog-catch-up-test-hardening/999.5-04-SUMMARY.md` when done. The SUMMARY should include:
- Files modified (6 paths from `files_modified` frontmatter)
- Net diff stats (`git diff HEAD~1 --stat`)
- Build + test deltas per D-23 (build warnings vs baseline; ResultContractTests passed-count vs baseline; full-suite passed/failed vs post-P03 baseline)
- Commit SHA + commit subject
- Confirmation that all four invariants held: (a) zero `ResultWrapInstruction` hits in ChatGpt variants, (b) 5 hits in Gemini variants, (c) Gemini files + `JsonTextFormatterService.cs` byte-identical, (d) commit-author + no-`Co-Authored-By` per D-20
- Reference back to D-24..D-28 and the 2026-05-21 user-report origin
</output>
