---
phase: 32-expert-context-selection
plan: 04
type: execute
wave: 4
depends_on: ["32-01", "32-03"]
files_modified:
  - DeckFlow.Web/Controllers/Admin/AdminContentKbController.cs
  - DeckFlow.Web/Views/AdminContentKb/Index.cshtml
  - DeckFlow.Web/Views/Deck/_ContentKbPanel.cshtml
autonomous: false
requirements: [SEL-05, SEL-06]
must_haves:
  truths:
    - "Admin /Admin/ContentKb shows a per-row Evergreen toggle button"
    - "Toggling Evergreen POSTs to SetEvergreen with CSRF + SameOrigin double-guard and shows a success banner"
    - "Each clip in the What Experts Say panel renders an origin marker (pinned/followed/auto/evergreen)"
    - "The origin CSS class suffix is produced by an allowlist mapper (pinned/followed/evergreen/auto), defaulting to auto — a crafted ClipOrigin from an uploaded zip cannot inject an arbitrary class suffix"
    - "Origin glyphs and aria-labels match the contract (pinned, followed, evergreen, auto)"
    - "Pinned/follow/evergreen selection is visually verified at desktop and mobile viewports"
  artifacts:
    - path: "DeckFlow.Web/Controllers/Admin/AdminContentKbController.cs"
      provides: "SetEvergreen POST action + KbEntryRow.IsEvergreen projection"
      contains: "SetEvergreen"
    - path: "DeckFlow.Web/Views/AdminContentKb/Index.cshtml"
      provides: "Evergreen toggle form per row"
      contains: "SetEvergreen"
    - path: "DeckFlow.Web/Views/Deck/_ContentKbPanel.cshtml"
      provides: "origin marker badge + ClipOriginClass allowlist mapper + ClipOriginGlyph/ClipOriginLabel functions"
      contains: "kb-clip-origin"
  key_links:
    - from: "DeckFlow.Web/Controllers/Admin/AdminContentKbController.cs"
      to: "ContentSiteIndexStore.SetEvergreenAsync"
      via: "SetEvergreen action"
      pattern: "SetEvergreenAsync"
    - from: "DeckFlow.Web/Views/Deck/_ContentKbPanel.cshtml"
      to: "ContentKbExcerpt.ClipOrigin"
      via: "per-clip marker via ClipOriginClass allowlist mapper"
      pattern: "ClipOriginClass"
---

<objective>
Close the loop on the admin-facing and result-facing surfaces: add the per-row Evergreen toggle to the admin KB console (mirroring SetVisibility) and the per-clip origin markers to the What Experts Say panel — with the origin CSS class suffix produced by an allowlist mapper so a crafted ClipOrigin (which can arrive from a user-uploaded zip via the replay restore path) cannot inject an arbitrary class. Then run the mandatory human-verify UI checkpoint at two viewports covering the whole phase's UI.

Purpose: SEL-05 (admin Evergreen flag toggle) and SEL-06 (panel origin markers). The Evergreen toggle must use the same CSRF + SameOrigin double-guard as SetVisibility. The panel consumes the `ClipOrigin` property added in Plan 01 and the `.kb-clip-origin*` CSS authored in Plan 03.

Output: the SetEvergreen action + KbEntryRow.IsEvergreen, the admin toggle form, the panel origin badge + allowlist mapper + @functions, and a passed 2-viewport visual verification.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-expert-context-selection-runs-after-phase-30-before-phase-31/32-CONTEXT.md
@.planning/phases/32-expert-context-selection-runs-after-phase-30-before-phase-31/32-RESEARCH.md
@.planning/phases/32-expert-context-selection-runs-after-phase-30-before-phase-31/32-PATTERNS.md
@.planning/phases/32-expert-context-selection-runs-after-phase-30-before-phase-31/32-UI-SPEC.md
@.planning/phases/32-expert-context-selection-runs-after-phase-30-before-phase-31/32-01-SUMMARY.md
@.planning/phases/32-expert-context-selection-runs-after-phase-30-before-phase-31/32-03-SUMMARY.md

<interfaces>
<!-- From Plans 01/03 + live code confirmed 2026-06-07 -->

ContentSiteIndexStore (Plan 01 added): SetEvergreenAsync(long id, bool evergreen, CancellationToken ct = default); ContentSiteIndexRow.IsEvergreen { get; init; }
ContentKbExcerpt (Plan 01 added): ClipOrigin { get; init; } = "auto"
site-common.css (Plan 03 added): .kb-clip-origin + --pinned + --followed + --evergreen

AdminContentKbController (DeckFlow.Web/Controllers/Admin/AdminContentKbController.cs):
  - BannerKey const (line 21)
  - entries projection (lines 91-100): new KbEntryRow { Id, Title, Source, Tags, IsVisible, RelevanceScore }
  - SetVisibility action (lines 201-213): [HttpPost("SetVisibility")] [ValidateAntiForgeryToken], SameOriginRequestValidator.IsValid guard -> 403, _store.SetVisibilityAsync, TempData[BannerKey] = "Visibility updated.", RedirectToAction(nameof(Index))
  - KbEntryRow view-model record — needs IsEvergreen { get; init; }

AdminContentKb view: DeckFlow.Web/Views/AdminContentKb/Index.cshtml — existing per-row SetVisibility form in the Action <td data-label="Action">

_ContentKbPanel.cshtml (DeckFlow.Web/Views/Deck/_ContentKbPanel.cshtml — confirmed path 2026-06-07):
  - @model IReadOnlyList<DeckFlow.Web.Models.ContentKbExcerpt>?  (UNCHANGED — ClipOrigin is now a property on the excerpt)
  - source-group + clip article loop (lines 8-25): <section class="kb-expert-source-group"> wraps <article class="kb-expert-clip"> with <blockquote> + attribution <p>
  - NOTE (MED-7): clip.ClipOrigin can arrive from a user-uploaded zip. DeckAnalysisPacketService replay restore (clip restore path ~line 541) deserializes ContentKbExcerpt[] (including ClipOrigin) straight from the uploaded 32-expert-context.json. So ClipOrigin is UNTRUSTED in this panel and MUST NOT be interpolated raw into a CSS class suffix.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add SetEvergreen action + KbEntryRow.IsEvergreen + admin toggle form</name>
  <files>DeckFlow.Web/Controllers/Admin/AdminContentKbController.cs, DeckFlow.Web/Views/AdminContentKb/Index.cshtml</files>
  <read_first>
    - DeckFlow.Web/Controllers/Admin/AdminContentKbController.cs (FULL — BannerKey 21, entries projection 91-100, SetVisibility action 201-213, KbEntryRow record)
    - DeckFlow.Web/Views/AdminContentKb/Index.cshtml (FULL — entries table, Action <td>, existing SetVisibility form)
    - .planning/phases/32-expert-context-selection-runs-after-phase-30-before-phase-31/32-PATTERNS.md (AdminContentKbController.cs + Admin/ContentKb/Index.cshtml sections)
    - .planning/phases/32-expert-context-selection-runs-after-phase-30-before-phase-31/32-UI-SPEC.md (C-06 + IC-05 + Copywriting Contract)
  </read_first>
  <action>
    In AdminContentKbController.cs:
      - Add `IsEvergreen = r.IsEvergreen,` to the `new KbEntryRow { ... }` projection (lines 91-100).
      - Add `public bool IsEvergreen { get; init; }` to the `KbEntryRow` record (with XML summary; `{ get; init; }`).
      - Immediately after the `SetVisibility` action, add `SetEvergreen(long entryId, bool evergreen, CancellationToken cancellationToken)` mirroring SetVisibility exactly: `[HttpPost("SetEvergreen")]` + `[ValidateAntiForgeryToken]`, the `SameOriginRequestValidator.IsValid(Request)` guard returning 403 with `GetForbiddenMessage()`, then `await _store.SetEvergreenAsync(entryId, evergreen, cancellationToken)`, `TempData[BannerKey] = "Evergreen status updated.";`, `return RedirectToAction(nameof(Index));`. Include an XML summary ("Marks or unmarks an artifact as evergreen. Double-CSRF-guarded.").

    In AdminContentKb/Index.cshtml, inside the existing `<td data-label="Action">` cell (after the SetVisibility form), add a second `<form method="post" asp-action="SetEvergreen" class="admin-action-form">` with AntiForgeryToken, hidden `entryId=@entry.Id`, hidden `evergreen="@((!entry.IsEvergreen).ToString().ToLowerInvariant())"`, and a submit button labeled "Evergreen: On"/"Evergreen: Off" per `entry.IsEvergreen` with the aria-labels from the Copywriting Contract. Stack both forms in the cell (no new column) per UI-SPEC C-06 preferred layout. Reuse existing admin.css classes (no new admin CSS).

    LF endings; touch only the lines being added; no Format Document.
  </action>
  <verify>
    <automated>"/mnt/c/Program Files/dotnet/dotnet.exe" build DeckFlow.Web/DeckFlow.Web.csproj 2>&1 | grep -E "Build succeeded|error" | head</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "\\[HttpPost(\"SetEvergreen\")\\]" DeckFlow.Web/Controllers/Admin/AdminContentKbController.cs` matches one line
    - `grep -n "SetEvergreenAsync" DeckFlow.Web/Controllers/Admin/AdminContentKbController.cs` matches one line (call in the action)
    - `grep -n "SameOriginRequestValidator.IsValid" DeckFlow.Web/Controllers/Admin/AdminContentKbController.cs` shows the guard in BOTH SetVisibility and SetEvergreen
    - `grep -n "IsEvergreen = r.IsEvergreen" DeckFlow.Web/Controllers/Admin/AdminContentKbController.cs` matches one line
    - `grep -n "asp-action=\"SetEvergreen\"" DeckFlow.Web/Views/AdminContentKb/Index.cshtml` matches one line
    - `grep -n "Evergreen status updated." DeckFlow.Web/Controllers/Admin/AdminContentKbController.cs` matches one line
    - `dotnet build DeckFlow.Web` reports Build succeeded
  </acceptance_criteria>
  <done>Admins can toggle the Evergreen flag per row with the same double-guard + banner as SetVisibility; Web builds.</done>
</task>

<task type="auto">
  <name>Task 2: Add per-clip origin marker badge to _ContentKbPanel.cshtml with an allowlist class mapper</name>
  <files>DeckFlow.Web/Views/Deck/_ContentKbPanel.cshtml</files>
  <read_first>
    - DeckFlow.Web/Views/Deck/_ContentKbPanel.cshtml (FULL — @model line, source-group + clip article loop lines 8-25, closing braces)
    - DeckFlow.Web/Models/ContentKbExcerpt.cs (ClipOrigin property from Plan 01)
    - DeckFlow.Web/wwwroot/css/site-common.css (.kb-clip-origin* classes from Plan 03 — confirm present)
    - DeckFlow.Web/Services/DeckAnalysisPacketService.cs (clip restore path ~line 541 — confirms ClipOrigin comes off the uploaded zip, hence untrusted)
    - .planning/phases/32-expert-context-selection-runs-after-phase-30-before-phase-31/32-UI-SPEC.md (C-05 exact markup + @functions + copy)
    - .planning/phases/32-expert-context-selection-runs-after-phase-30-before-phase-31/32-PATTERNS.md (_ContentKbPanel.cshtml section)
  </read_first>
  <action>
    MED-7 — ALLOWLIST THE CSS CLASS SUFFIX (confirmed: clip.ClipOrigin is deserialized from the user-uploaded 32-expert-context.json in DeckAnalysisPacketService's replay restore path ~line 541, so it is attacker-controllable. Razor encodes attribute VALUES, but `kb-clip-origin--@clip.ClipOrigin` would still let a crafted origin like `auto x"></span><script>` or any junk become part of the class token / break the badge styling. Do NOT interpolate clip.ClipOrigin directly into the class.):

    In _ContentKbPanel.cshtml, inside each `<article class="kb-expert-clip">`, after the attribution `<p>`, add the origin badge span using the MAPPER for the class suffix and the label/glyph helpers:
      `<span class="kb-clip-origin kb-clip-origin--@ClipOriginClass(clip.ClipOrigin)" aria-label="@ClipOriginLabel(clip.ClipOrigin)" title="@ClipOriginLabel(clip.ClipOrigin)">@ClipOriginGlyph(clip.ClipOrigin)</span>`.

    After the closing brace of the existing markup, add an `@functions` block with THREE static helpers (switch expressions, `_` default):
      - `ClipOriginClass(string origin)` — the allowlist mapper. Returns the origin string ONLY for the four known values, normalized: `"pinned" => "pinned", "followed" => "followed", "evergreen" => "evergreen", "auto" => "auto", _ => "auto"`. This is what makes the class suffix safe — any unknown/crafted value collapses to `auto`, so only the four Plan-03 CSS classes can ever be emitted. (Optionally normalize input via `origin?.ToLowerInvariant()` before the switch.)
      - `ClipOriginGlyph(string origin)` — the pinned/followed/evergreen/auto glyphs per UI-SPEC C-05, `_ =>` the auto glyph.
      - `ClipOriginLabel(string origin)` — "Pinned by you"/"From followed creator"/"Evergreen advice"/"Auto-selected", `_ =>` "Auto-selected".

    The `@model` line does NOT change — ClipOrigin is a property on the excerpt. Do NOT add CSS here (the `.kb-clip-origin*` rules live in site-common.css from Plan 03). LF endings; touch only the lines being added; no Format Document.
  </action>
  <verify>
    <automated>"/mnt/c/Program Files/dotnet/dotnet.exe" build DeckFlow.Web/DeckFlow.Web.csproj 2>&1 | grep -E "Build succeeded|error" | head</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "kb-clip-origin--@ClipOriginClass(clip.ClipOrigin)" DeckFlow.Web/Views/Deck/_ContentKbPanel.cshtml` matches one line (class suffix goes through the mapper, NOT raw `@clip.ClipOrigin`)
    - `grep -c "kb-clip-origin--@clip.ClipOrigin" DeckFlow.Web/Views/Deck/_ContentKbPanel.cshtml` returns 0 (raw interpolation absent — MED-7)
    - `grep -n "ClipOriginClass" DeckFlow.Web/Views/Deck/_ContentKbPanel.cshtml` shows BOTH the helper definition and its call site; the helper's `_ =>` default returns `"auto"`
    - `grep -n "ClipOriginGlyph\|ClipOriginLabel" DeckFlow.Web/Views/Deck/_ContentKbPanel.cshtml` shows both the helper definitions and their call sites
    - The `@model` line is unchanged (still `IReadOnlyList<DeckFlow.Web.Models.ContentKbExcerpt>?`)
    - `grep -c ".kb-clip-origin" DeckFlow.Web/wwwroot/css/site-common.css` returns >= 1 (Plan 03 CSS dependency present)
    - `dotnet build DeckFlow.Web` reports Build succeeded
  </acceptance_criteria>
  <done>Each panel clip shows its origin glyph + accessible label sourced from ClipOrigin; the CSS class suffix is produced by an allowlist mapper defaulting to auto (crafted origins from an uploaded zip cannot inject a class); CSS comes from Plan 03; Web builds.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human visual verification at 2 viewports (CLAUDE.md UI-phase requirement)</name>
  <files>(no files — verification only)</files>
  <action>
    AUTOMATION COMPLETE before this checkpoint: Tasks 1-2 build clean and all Plan 01-03 unit/round-trip tests are green. This task is human visual verification only — Claude does not edit files here. Ask the developer to start the dev server (do NOT auto-launch per project memory) and walk the verification steps below, then capture pass/fail.
  </action>
  <what-built>
    The full Phase 32 UI: Pin/Follow buttons + selection tray on /content-kb, the editable Expert Context chip area + typeahead on /deck-analysis, the per-clip origin markers in the What Experts Say panel, and the admin Evergreen toggle on /Admin/ContentKb. All builds pass and all unit/round-trip tests are green from Plans 01-03.
  </what-built>
  <how-to-verify>
    The developer starts the dev server (do NOT auto-launch — per memory, ask the user to run it). Then verify at BOTH >=900px desktop AND <=600px mobile viewports:
    1. /content-kb: each card shows a Pin and a Follow button with 44px touch targets; clicking Pin toggles to "Pinned" + accent border; the tray appears below the filter bar showing pins (N/3) and follows with remove buttons; pinning a 4th video is blocked (Pin buttons disabled, cap tooltip).
    2. Navigate to /deck-analysis: the Expert Context chip area shows the carried-over pins/follows as chips; the remove button removes a chip; the typeahead input returns video/creator suggestions and adding one creates a chip (the picked video pins by its id, not its title); with JS disabled the form still submits (selection empty, empty-hint visible).
    3. Submit an analysis that FAILS validation (e.g., empty decklist) and confirm pins are STILL present afterward (MED-6: pins clear only after success); then run a SUCCESSFUL analysis and confirm pins clear (follows remain).
    4. Run an analysis whose deck matches at least one pinned/followed/evergreen clip: the What Experts Say panel shows the correct origin glyph (pinned/followed/evergreen/auto) per clip with the right aria-label/title.
    5. /Admin/ContentKb: each row has an "Evergreen: Off/On" toggle; clicking it flips the flag and shows the "Evergreen status updated." success banner. Confirm at most 1 evergreen clip ever appears in a prompt/panel.
    6. Confirm guild-theme integrity: switch to at least one non-default guild theme and confirm chips/tray/buttons/markers inherit theme tokens (accent color changes) with no broken layout.
  </how-to-verify>
  <verify>
    <human-check>Developer confirms all 6 verification steps pass at desktop + mobile and on at least one non-default guild theme.</human-check>
  </verify>
  <resume-signal>Type "approved" or describe issues to fix.</resume-signal>
  <done>All 6 UI verification steps pass at 2 viewports and across a non-default guild theme; developer typed "approved".</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin browser → /Admin/ContentKb/SetEvergreen | State-changing POST behind BasicAuth |
| uploaded zip → relevance/replay → panel | ClipOrigin deserialized from a user-uploaded zip is rendered into a CSS class suffix |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-11 | Tampering | CSRF on SetEvergreen toggle | mitigate | [ValidateAntiForgeryToken] + SameOriginRequestValidator.IsValid -> 403, identical double-guard to SetVisibility |
| T-32-12 | Elevation of Privilege | Unauthenticated access to SetEvergreen | mitigate | /Admin/* is gated by existing BasicAuthMiddleware (unchanged) |
| T-32-13 | Cross-Site Scripting / class injection | Crafted ClipOrigin (from uploaded zip replay restore) rendered into a CSS class suffix | mitigate | ClipOriginClass(origin) allowlist mapper emits only pinned/followed/evergreen/auto (defaults to auto); raw `@clip.ClipOrigin` is never interpolated into the class; Razor also encodes attribute values |
| T-32-SC | Tampering | npm/pip/cargo installs | accept | No package installs; phase installs zero external packages (RESEARCH §Package Legitimacy Audit: not applicable) |
</threat_model>

<verification>
- `dotnet build DeckFlow.Web` clean
- Admin SetEvergreen flips is_evergreen and shows the banner; double-guard returns 403 on cross-origin
- Panel renders the correct origin glyph per ClipOrigin; the class suffix is always one of the four allowlisted values (a crafted ClipOrigin collapses to auto)
- Human-verify checkpoint passed at 2 viewports + at least one non-default guild theme
- Full Web test suite green (regression): `dotnet test DeckFlow.Web.Tests/DeckFlow.Web.Tests.csproj`
</verification>

<success_criteria>
SEL-05 (admin Evergreen toggle, max 1 evergreen clip enforced upstream in Plan 01) and SEL-06 (panel origin markers via an allowlist class mapper) are implemented; the phase UI is visually verified at desktop + mobile and across a non-default guild theme.
</success_criteria>

<output>
Create `.planning/phases/32-expert-context-selection-runs-after-phase-30-before-phase-31/32-04-SUMMARY.md` when done.
</output>
