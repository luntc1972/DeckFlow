---
phase: 31-deck-primer-generator
plan: 05
type: execute
wave: 3
depends_on: ["31-02", "31-03"]
files_modified:
  - DeckFlow.Web/Services/PacketArtifactStore.cs
  - DeckFlow.Web.Tests/PacketArtifactStorePrimerTests.cs
  - DeckFlow.Web.Tests/DeckPrimerResultRoundTripTests.cs
requirements: [PRM-09]
autonomous: true
must_haves:
  truths:
    - "BuildPrimerZip writes only entry names present in PrimerAllowedNames (added in 31-02); LoadPrimerFromZip reads against PrimerAllowedNames so an unlisted entry is rejected"
    - "A zip built by BuildPrimerZip and re-loaded by LoadPrimerFromZip round-trips TargetCommanderBracket, TargetAiPlatform, and SelectedSectionIds onto a fresh DeckPrimerRequest (PRM-09)"
    - "LoadPrimerFromZip throws InvalidOperationException on a zip containing a non-primer (e.g. analysis) entry name — allowlist enforced via the existing ReadEntries"
    - "SuggestPrimerZipFileName produces a safe, dated filename mirroring the existing Suggest*ZipFileName helpers"
    - "The new methods reuse existing private helpers (CreateSafePathSegment, ReadEntries, size caps) — no duplicated archive logic"
  artifacts:
    - path: "DeckFlow.Web/Services/PacketArtifactStore.cs"
      provides: "BuildPrimerZip + LoadPrimerFromZip + SuggestPrimerZipFileName using PrimerAllowedNames"
      contains: "BuildPrimerZip"
    - path: "DeckFlow.Web.Tests/PacketArtifactStorePrimerTests.cs"
      provides: "round-trip + allowlist-rejection regression tests (PRM-09)"
      exports: ["BuildPrimerZip_then_LoadPrimerFromZip_round_trips_request_context", "LoadPrimerFromZip_rejects_non_primer_entry_names"]
    - path: "DeckFlow.Web.Tests/DeckPrimerResultRoundTripTests.cs"
      provides: "System.Text.Json round-trip test for DeckPrimerPacketResult { get; init; } preservation"
      exports: ["DeckPrimerPacketResult_JsonRoundTrip_PreservesAllProperties"]
  key_links:
    - from: "BuildPrimerZip"
      to: "PrimerAllowedNames"
      via: "entry names drawn from the allowlist"
      pattern: "PrimerAllowedNames"
    - from: "LoadPrimerFromZip"
      to: "ReadEntries(stream, PrimerAllowedNames)"
      via: "allowlist + size-cap enforcement"
      pattern: "ReadEntries"
---

<objective>
The primer zip round-trip on `PacketArtifactStore`: `BuildPrimerZip` (produces the per-AI artifact zip), `LoadPrimerFromZip` (restores bracket + AI platform + section selections from a re-uploaded zip, allowlist-enforced), and `SuggestPrimerZipFileName`. Plus the PRM-09 regression tests (round-trip + allowlist rejection) and a System.Text.Json round-trip test for `DeckPrimerPacketResult` (the { get; init; } invariant).

Purpose: PRM-09's "zip round-trip with PrimerAllowedNames + round-trip regression test" requirement. `PrimerAllowedNames` already exists (added first in 31-02); this plan adds the methods that produce/consume it. Re-uploading a session must restore bracket and section selections (ROADMAP SC5).

Output: `BuildPrimerZip` / `LoadPrimerFromZip` / `SuggestPrimerZipFileName` on `PacketArtifactStore`, plus two test files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-deck-primer-generator/31-CONTEXT.md
@.planning/phases/31-deck-primer-generator/31-RESEARCH.md
@.planning/phases/31-deck-primer-generator/31-PATTERNS.md

<interfaces>
<!-- Confirmed against live code 2026-06-08. Executor uses these directly. -->

PacketArtifactStore (DeckFlow.Web/Services/PacketArtifactStore.cs) — internal static:
  - PrimerAllowedNames (added in 31-02): 00-primer-input-summary.txt, 01-primer-request-context.txt, 10-primer-deck-list.txt, 10b-primer-deck-original.txt, 30-primer-chatgpt-prompt.txt, 30-primer-claude-prompt.txt, 30-primer-gemini-prompt.txt, all-primer-prompts.txt
  - BuildZip / LoadFromZip (lines 105-348) — the analysis analog to mirror for BuildPrimerZip / LoadPrimerFromZip
  - ReadEntries(stream, allowedNames) (line 625) — THROWS InvalidOperationException on unlisted names; MaxEntryUncompressedBytes=2MB (14), MaxTotalUncompressedBytes=10MB (15); rejects entry.FullName containing '/'
  - CreateSafePathSegment + NormalizeSections — existing private static helpers shared by all Build*Zip; reuse, do not duplicate
  - Suggest*ZipFileName (line ~570) — pattern for SuggestPrimerZipFileName

DeckPrimerRequest (from 31-02): TargetCommanderBracket, TargetAiPlatform (Normalize setter), SelectedSectionIds (List of string, null-guard setter) — the state restored from the zip

DeckPrimerPacketResult (from 31-03): positional record with { get; init; } — round-tripped, needs a System.Text.Json round-trip test per the carried-forward invariant
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: BuildPrimerZip + LoadPrimerFromZip + SuggestPrimerZipFileName</name>
  <files>DeckFlow.Web/Services/PacketArtifactStore.cs</files>
  <read_first>
    - DeckFlow.Web/Services/PacketArtifactStore.cs (FULL — PrimerAllowedNames added by 31-02, BuildZip/LoadFromZip 105-348, ReadEntries 625-659, CreateSafePathSegment + NormalizeSections, Suggest*ZipFileName ~570)
    - DeckFlow.Web/Models/DeckPrimerRequest.cs (bracket/platform/section fields)
    - .planning/phases/31-deck-primer-generator/31-RESEARCH.md (Section 5 — PrimerAllowedNames, round-trip pattern; Pitfall 2)
    - .planning/phases/31-deck-primer-generator/31-PATTERNS.md (PacketArtifactStore.cs section)
  </read_first>
  <action>
    In `PacketArtifactStore.cs`, add three `public static` methods mirroring the analysis BuildZip/LoadFromZip/SuggestZipFileName (reuse CreateSafePathSegment / NormalizeSections / ReadEntries — do NOT duplicate archive logic):
      - `BuildPrimerZip(DeckPrimerRequest request, string inputSummary, string? requestContextText, string? chatGptPromptText, string? claudePromptText, string? geminiPromptText, string? canonicalDeckListText = null, string? originalDeckText = null)` returning byte[]. Write ONLY entry names in PrimerAllowedNames: `00-primer-input-summary.txt` (inputSummary), `01-primer-request-context.txt` (requestContextText — include TargetCommanderBracket, TargetAiPlatform, and the ordered SelectedSectionIds so they can be restored), `10-primer-deck-list.txt` (canonicalDeckListText), `10b-primer-deck-original.txt` (originalDeckText), `30-primer-chatgpt-prompt.txt` / `30-primer-claude-prompt.txt` / `30-primer-gemini-prompt.txt` (each non-null prompt), and `all-primer-prompts.txt` (concatenation). Skip entries whose content is null/empty. Each written name MUST be in PrimerAllowedNames.
      - `LoadPrimerFromZip(Stream zipStream, DeckPrimerRequest request)` returning void. Call `ReadEntries(zipStream, PrimerAllowedNames)` (allowlist + size caps enforced). Parse `01-primer-request-context.txt` to restore `request.TargetCommanderBracket`, `request.TargetAiPlatform`, and `request.SelectedSectionIds` (split the stored ordered IDs). Use a robust key:value parse mirroring how LoadFromZip restores analysis state; tolerate missing keys.
      - `SuggestPrimerZipFileName(string? commanderName, string? targetAiPlatform = null)` returning string, mirroring the existing helper: `$"{CreateSafePathSegment(commanderName, "deck-primer")}-primer-{CreateSafePathSegment(targetAiPlatform, "chatgpt")}-{DateTime.UtcNow:yyyyMMdd-HHmmss}.zip"`.
    XML docs (CS1591). LF; Allman; 4-space; touch only the inserted methods (do not reformat surrounding code).
  </action>
  <verify>
    <automated>"/mnt/c/Program Files/dotnet/dotnet.exe" build DeckFlow.Web/DeckFlow.Web.csproj -warnaserror:CS1591 2>&1 | grep -E "Build succeeded|error" | head</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "BuildPrimerZip\|LoadPrimerFromZip\|SuggestPrimerZipFileName" DeckFlow.Web/Services/PacketArtifactStore.cs` matches all three methods
    - `grep -n "ReadEntries(zipStream, PrimerAllowedNames)" DeckFlow.Web/Services/PacketArtifactStore.cs` matches (allowlist enforced on load)
    - Every entry name BuildPrimerZip writes is present in the PrimerAllowedNames set (manual: cross-check the 8 names)
    - No duplicated ZipArchive open/read logic — ReadEntries + CreateSafePathSegment reused
    - `dotnet build DeckFlow.Web -warnaserror:CS1591` reports Build succeeded, 0 errors
  </acceptance_criteria>
  <done>BuildPrimerZip/LoadPrimerFromZip/SuggestPrimerZipFileName exist, write/read only allowlisted names, restore bracket + platform + sections, and reuse the shared helpers; Web builds clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Primer zip round-trip + allowlist + result JSON round-trip tests</name>
  <files>DeckFlow.Web.Tests/PacketArtifactStorePrimerTests.cs, DeckFlow.Web.Tests/DeckPrimerResultRoundTripTests.cs</files>
  <read_first>
    - DeckFlow.Web.Tests/PacketArtifactStoreTests.cs (FULL — round-trip test pattern lines 15-77, allowlist rejection 44-59, ZipArchive inspection)
    - DeckFlow.Web/Services/PacketArtifactStore.cs (BuildPrimerZip/LoadPrimerFromZip from Task 1)
    - DeckFlow.Web/Models/DeckPrimerRequest.cs; DeckFlow.Web/Services/DeckPrimerPacketService.cs (DeckPrimerPacketResult)
    - .planning/phases/31-deck-primer-generator/31-PATTERNS.md (PacketArtifactStorePrimerTests.cs section; { get; init; } round-trip rule)
  </read_first>
  <behavior>
    - BuildPrimerZip_then_LoadPrimerFromZip_round_trips_request_context: build with bracket "cEDH", platform "ChatGPT", 3 section ids -> load onto a fresh DeckPrimerRequest -> assert all three restored (bracket, platform, the 3 ids in order).
    - BuildPrimerZip_writes_only_allowlisted_entries: inspect the archive entries; assert every entry name is in PrimerAllowedNames.
    - LoadPrimerFromZip_rejects_non_primer_entry_names: a zip with an analysis entry (e.g. "31-analysis-prompt.txt") -> Assert.Throws<InvalidOperationException>.
    - BuildPrimerZip_skips_null_prompts: passing null claude/gemini prompts omits those entries (no empty entry written).
    - DeckPrimerPacketResult_JsonRoundTrip_PreservesAllProperties: serialize a fully-populated DeckPrimerPacketResult, deserialize, assert every property (incl. PrimerPromptText, ResolvedCommanderName) survives (proves { get; init; }).
  </behavior>
  <action>
    Create `DeckFlow.Web.Tests/PacketArtifactStorePrimerTests.cs` (`public sealed class PacketArtifactStorePrimerTests`, namespace `DeckFlow.Web.Tests`, xUnit). Implement the zip behaviors above, mirroring PacketArtifactStoreTests (in-memory MemoryStream + ZipArchive inspection; no mocking). For the allowlist-rejection test, create a zip in-memory containing a non-primer entry name and assert `LoadPrimerFromZip` throws InvalidOperationException.

    Create `DeckFlow.Web.Tests/DeckPrimerResultRoundTripTests.cs` (`public sealed class DeckPrimerResultRoundTripTests`) with `DeckPrimerPacketResult_JsonRoundTrip_PreservesAllProperties`: assert `JsonSerializer.Deserialize<DeckPrimerPacketResult>(JsonSerializer.Serialize(result))` preserves all fields (guards against an accidental { get; } conversion silently dropping properties).

    LF; no formatter sweep.
  </action>
  <verify>
    <automated>"/mnt/c/Program Files/dotnet/dotnet.exe" test DeckFlow.Web.Tests/DeckFlow.Web.Tests.csproj --filter "PacketArtifactStorePrimerTests|DeckPrimerResultRoundTripTests" 2>&1 | grep -E "Passed!|Failed!|error" | head</automated>
  </verify>
  <acceptance_criteria>
    - File DeckFlow.Web.Tests/PacketArtifactStorePrimerTests.cs exists with the 4 zip `[Fact]` methods
    - `BuildPrimerZip_then_LoadPrimerFromZip_round_trips_request_context` asserts bracket + platform + 3 section ids restored (PRM-09 / SC5)
    - `LoadPrimerFromZip_rejects_non_primer_entry_names` uses Assert.Throws<InvalidOperationException>
    - File DeckFlow.Web.Tests/DeckPrimerResultRoundTripTests.cs asserts all DeckPrimerPacketResult properties survive JSON round-trip
    - `dotnet test --filter "PacketArtifactStorePrimerTests|DeckPrimerResultRoundTripTests"` reports Passed with 0 failures
  </acceptance_criteria>
  <done>Primer zip round-trips bracket/platform/sections, rejects non-primer entries, and DeckPrimerPacketResult survives JSON round-trip; all tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| uploaded zip -> LoadPrimerFromZip | Untrusted re-uploaded archive (path traversal, zip bomb, unlisted entries); enforced by ReadEntries |
| restored request state -> downstream | Bracket/platform/sections restored from the zip are re-normalized by the service's NormalizeSelections before use |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-14 | Tampering | Zip path traversal on a re-uploaded primer archive | mitigate | LoadPrimerFromZip delegates to ReadEntries, which rejects entry.FullName containing '/' and enforces PrimerAllowedNames; no custom archive parsing |
| T-31-15 | Denial of Service | Zip bomb / oversized primer upload | mitigate | ReadEntries enforces MaxEntryUncompressedBytes (2MB) + MaxTotalUncompressedBytes (10MB); reused unchanged |
| T-31-16 | Tampering | Spoofed cross-workflow zip (analysis/comparison entries) loaded as a primer | mitigate | PrimerAllowedNames is the only allowlist passed to ReadEntries for the primer load path; non-primer entry names throw; unit-tested |
| T-31-17 | Tampering | Restored SelectedSectionIds containing crafted/cross-bracket IDs | mitigate | Restored IDs are re-run through PrimerSectionCatalog.NormalizeSelections (in the service, 31-03) before use; the zip is not a trust bypass |
| T-31-SC | Tampering | npm/pip/cargo installs | accept | Zero package installs (RESEARCH §Package Legitimacy Audit: not applicable) |
</threat_model>

<verification>
- `dotnet build DeckFlow.Web -warnaserror:CS1591` clean
- `dotnet build DeckFlow.Web.Tests` clean
- `dotnet test --filter "PacketArtifactStorePrimerTests|DeckPrimerResultRoundTripTests"` green
- Allowlist rejection proven; bracket+platform+sections restored on re-upload (PRM-09 / SC5)
</verification>

<success_criteria>
PRM-09's zip-round-trip-with-PrimerAllowedNames + round-trip regression test is delivered: BuildPrimerZip/LoadPrimerFromZip restore bracket, AI platform, and section selections on re-upload (SC5), reject non-primer entries, and DeckPrimerPacketResult survives JSON round-trip. The variant-prompt side of PRM-09 ships in 31-04; this plan ships the artifact-store side.
</success_criteria>

<output>
Create `.planning/phases/31-deck-primer-generator/31-05-SUMMARY.md` when done.
</output>
