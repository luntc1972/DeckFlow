---
phase: 51-verify-v1-7-on-main-non-prod-uat
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/51-verify-v1-7-on-main-non-prod-uat/51-PG-PARITY-RESULTS.md
autonomous: false
requirements: [HARD-03]
must_haves:
  truths:
    - "The Postgres-gated test suite is run with DECKFLOW_POSTGRES_TESTS=1 against a live Postgres (Testcontainers postgres:16-alpine via Docker) and the previously-skipped [PostgresFact] tests execute (not skip) and pass with 0 failures"
    - "The PG parity result (PASS / FAIL / WAIVED-with-reason) is recorded in 51-PG-PARITY-RESULTS.md with the command run and the pass/fail/skip counts"
  artifacts:
    - path: ".planning/phases/51-verify-v1-7-on-main-non-prod-uat/51-PG-PARITY-RESULTS.md"
      provides: "Recorded DECKFLOW_POSTGRES_TESTS=1 parity run result confirming Phase 49 Dapper type-handler parity on Postgres"
      contains: "DECKFLOW_POSTGRES_TESTS"
  key_links:
    - from: "51-PG-PARITY-RESULTS.md"
      to: "49-VERIFICATION.md Postgres Parity Gate"
      via: "the deferred PG parity gate is run and recorded"
      pattern: "DapperTypeHandlerRoundTripTests"
---

<objective>
Run the Postgres-gated test suite (`DECKFLOW_POSTGRES_TESTS=1`) on the v1.7-on-main build to confirm the Phase 49 Dapper provider-aware type-handler parity (DateTime/decimal/bool/Guid/DateTimeOffset round-trip + store integration) holds on the real Postgres path, and record the result. This satisfies HARD-03.

Purpose: Phase 49 fully proved the SQLite path; the 11 Postgres-gated facts skip without Docker + the env var. 49-VERIFICATION.md left this as a documented manual gate. HARD-03 closes it.
Output: A recorded PG parity result in `51-PG-PARITY-RESULTS.md`. No product code changes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/49-dapper-data-access-adoption/49-VERIFICATION.md

<interfaces>
<!-- The Postgres test gating mechanism the executor/operator must drive. -->

From DeckFlow.Web.Tests/Integration/PostgresFactAttribute.cs:
- `[PostgresFact]` facts SKIP unless env var DECKFLOW_POSTGRES_TESTS == "1".

From DeckFlow.Web.Tests/Integration/PostgresContainerFixture.cs:
- When DECKFLOW_POSTGRES_TESTS=1, starts a Testcontainers `postgres:16-alpine` container (requires a running Docker daemon); if Docker is unavailable the fixture sets a skip reason instead of failing.
- Round-trip handler proof: DeckFlow.Web.Tests/Integration/DapperTypeHandlerRoundTripTests.cs (SQLite [Fact] + Postgres [PostgresFact] variants).
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Run + record the Postgres-gated parity suite (HARD-03)</name>
  <read_first>
    - .planning/phases/49-dapper-data-access-adoption/49-VERIFICATION.md ("Postgres Parity Gate" human_verification: the exact command + expected outcome)
    - DeckFlow.Web.Tests/Integration/PostgresFactAttribute.cs (the DECKFLOW_POSTGRES_TESTS=1 gate)
    - DeckFlow.Web.Tests/Integration/PostgresContainerFixture.cs (Docker + postgres:16-alpine container start; skip-on-unavailable behavior)
    - CLAUDE.md "Testing" constraint: VSTest is unreliable in WSL — prefer push-and-watch CI or a targeted harness over an in-WSL VSTest run; note the dotnet path is "/mnt/c/Program Files/dotnet/dotnet.exe".
  </read_first>
  <what-built>Phase 49 converted 13 stores to Dapper with provider-aware type handlers. The SQLite path is fully verified; the Postgres path is gated behind DECKFLOW_POSTGRES_TESTS=1 + a Docker-started Postgres container and was deferred at v1.7 close.</what-built>
  <how-to-verify>
    1. Ensure a Postgres instance is available to the test fixture. Preferred path: a running Docker daemon (the PostgresContainerFixture starts postgres:16-alpine automatically). If Docker is not reachable from the test host, use an alternate operator-provided Postgres and the documented fixture wiring.
    2. Run the gated suite with the env var set. Because VSTest is unreliable under WSL (CLAUDE.md), prefer either: (a) running via the Windows dotnet host (`DECKFLOW_POSTGRES_TESTS=1 "/mnt/c/Program Files/dotnet/dotnet.exe" test DeckFlow.Web.Tests/DeckFlow.Web.Tests.csproj`), or (b) a push-and-watch CI run that sets DECKFLOW_POSTGRES_TESTS=1 with a Postgres service container — whichever reliably executes the [PostgresFact] facts.
    3. Confirm the previously-skipped [PostgresFact] tests (the DapperTypeHandlerRoundTripTests Postgres variants + any PostgresContainerFixture store integration facts) actually RUN (skip count drops) and pass with 0 failures.
  </how-to-verify>
  <action>Execute the PG-gated parity run and capture: the exact command used, the test host (Windows dotnet / CI), the pass/fail/skip counts, and explicit confirmation that the round-trip [PostgresFact] facts ran (were not skipped). Record PASS / FAIL / WAIVED-with-reason with the date under a "## HARD-03 — Postgres parity gate" heading in 51-PG-PARITY-RESULTS.md. If no Postgres/Docker is available this cycle, mark WAIVED-with-reason (state precisely what was unavailable) — HARD-03 is waivable-with-reason per the phase nature. If any PG-gated fact fails, mark FAIL and capture the failing fact + message as a follow-up finding; do NOT modify product code in this phase. Never paste any Postgres connection string into the results file.</action>
  <acceptance_criteria>
    - The command run is recorded and includes DECKFLOW_POSTGRES_TESTS=1.
    - Either: the [PostgresFact] round-trip + store-integration facts ran and passed with 0 failures (PASS), OR a precise unavailability reason is recorded (WAIVED), OR failing facts are captured as follow-up findings (FAIL).
    - 51-PG-PARITY-RESULTS.md "## HARD-03" section records the result, command, counts, and date; no connection string value appears in the file.
  </acceptance_criteria>
  <resume-signal>Type "approved" with the PG parity result (PASS counts / "waived: <reason>" / "fail: <fact>").</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test host → Postgres | A Postgres connection string (Testcontainers-generated or operator-supplied) is used by the test run |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-51-03-01 | Information disclosure | Postgres connection string in env / CI / results file | mitigate | Testcontainers generates an ephemeral throwaway credential; if an operator-supplied PG is used the string comes from env only and is NEVER written into 51-PG-PARITY-RESULTS.md or committed (results record counts/behavior only) |
| T-51-03-02 | Tampering | CI workflow change to enable the gate | accept | If push-and-watch CI is used, any workflow edit is operator-owned (.github/workflows is on the do-not-modify list — operator makes the change, not the AI) |
| T-51-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages — Testcontainers + the test deps already exist in DeckFlow.Web.Tests; verification only |
</threat_model>

<verification>
- 51-PG-PARITY-RESULTS.md exists with a "## HARD-03" result section recording the command, counts, and PASS/FAIL/WAIVED.
- No product source files are modified by this plan (verification-only).
</verification>

<success_criteria>
The Postgres-gated test suite is run (or waived-with-reason) on the v1.7-on-main build and the result confirming Phase 49 Dapper parity on the Postgres path is recorded in 51-PG-PARITY-RESULTS.md.
</success_criteria>

<output>
Create `.planning/phases/51-verify-v1-7-on-main-non-prod-uat/51-03-SUMMARY.md` when done.
</output>
