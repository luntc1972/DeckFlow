---
id: 14-03
phase: 14-broader-codebase-name-vs-behavior-audit
plan: 03
type: execute
wave: 3
depends_on:
  - 14-02
files_modified:
  # Authoritative list lives in 14-AUDIT-REPORT.md `## Doc-comment backfill targets`.
  # Expected per RESEARCH.md "State of the Project — Public type counts":
  - DeckFlow.Core/Models/DeckEntry.cs
  - DeckFlow.Core/Models/DeckDiff.cs
  - DeckFlow.Core/Models/LoadedDecks.cs
  - DeckFlow.Core/Models/PrintingConflict.cs
  # plus ~5 more Core records surfaced by the audit grep
  # plus all 10 DeckFlow.Core.Tests test classes
  # plus ~37 DeckFlow.Web.Tests test classes
  # plus DeckFlow.Web/Models/DeckPageTab.cs (Plan 14-01 discretionary opt-in)
autonomous: true
requirements:
  - AUDIT-02
must_haves:
  truths:
    - "Every file listed in 14-AUDIT-REPORT.md `## Doc-comment backfill targets` carries a class-level `<summary>` on every public class/interface/record declared in it"
    - "Public properties on the renamed records in DeckFlow.Core/Models/ (DeckEntry, DeckDiff, LoadedDecks, PrintingConflict, plus any others surfaced) carry one-line `<summary>` doc comments"
    - "No `{ get; init; }` accessor on any touched Core record was reduced to `{ get; }` (CLAUDE.md preservation, Pitfall 3, Phase 13 T5 lesson)"
    - "DeckPageTab enum + values in DeckFlow.Web/Models/DeckPageTab.cs carry `<summary>` doc comments per Plan 14-01 discretionary opt-in"
    - "After all backfill commits, build remains green and no behavior change is introduced"
    - "Backfill commits are grouped per-project (one commit per logical chunk) per CLAUDE.md `one logical change per commit`"
    - "D-03: XML <summary> backfill scope honored — every public class + interface across all 5 projects that's missing one is backfilled this plan; style anchor CardLookupService.cs + CommanderSpellbookService.cs (terse single-sentence, optional <remarks> for non-obvious behavior); public ctors + public properties on renamed types get summaries in lockstep"
    - path: "DeckFlow.Core/Models/DeckEntry.cs"
      provides: "DeckEntry record with class-level <summary> + 8 property summaries; { get; init; } accessors intact"
      contains: "/// <summary>"
    - path: "DeckFlow.Web/Models/DeckPageTab.cs"
      provides: "Enum + values with one-line summaries per Plan 14-01 discretionary opt-in"
      contains: "/// <summary>"
  key_links:
    - from: "14-AUDIT-REPORT.md `## Doc-comment backfill targets`"
      to: "Each touched .cs file in this plan"
      via: "the audit-report row drives the per-file edit"
      pattern: "MISSING:"
    - from: "DeckFlow.Web/Services/CardLookupService.cs:39-42 (style anchor)"
      to: "Every new class summary"
      via: "tone reference"
      pattern: "/// <summary>"
---

<objective>
Backfill XML `<summary>` doc comments on every public class + interface + record
identified in 14-AUDIT-REPORT.md `## Doc-comment backfill targets` (Plan 14-01
deliverable). Per CONTEXT.md D-03 + D-06: public surface only, excluding the
types Plan 14-02 already touched (those got summaries in lockstep). Public
properties + public ctors on renamed types also get summaries.

Purpose: AUDIT-02 requires every public class and interface to carry a
`<summary>`. This plan is the bulk doc-comment work; csproj `GenerateDocumentationFile`
flags are NOT touched here (that's Plan 14-04). Backfill is correctness work,
not warning-driven (RESEARCH.md "GenerateDocumentationFile Reality Check" —
`.editorconfig` silences CS1591/1573/1587 globally, so warnings cannot tell us
where docs are missing; the audit-report is the source of truth).

CRITICAL — `{ get; init; }` preservation (Phase 13 UAT T5 landmine):
Every Core record under Models/ uses `{ get; init; }` accessors. Stripping `init`
silently breaks System.Text.Json deserialization in .NET 9+. EVERY commit in
this plan must run the preservation grep BEFORE landing.

Output:
- ~9 Core record/model files updated with class + property summaries
- ~10 Core.Tests test classes updated with class summaries
- ~37 Web.Tests test classes updated with class summaries (where not already covered)
- DeckFlow.Web/Models/DeckPageTab.cs updated with enum + value summaries (discretionary opt-in per Plan 14-01)
- One commit per logical chunk (project + theme), not one giant commit
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/14-broader-codebase-name-vs-behavior-audit/14-CONTEXT.md
@.planning/phases/14-broader-codebase-name-vs-behavior-audit/14-RESEARCH.md
@.planning/phases/14-broader-codebase-name-vs-behavior-audit/14-AUDIT-REPORT.md   # canonical input
@.planning/phases/14-broader-codebase-name-vs-behavior-audit/14-BASELINE.md
@.planning/phases/14-broader-codebase-name-vs-behavior-audit/14-BASELINE-PUBLIC-TYPES.txt
@.planning/phases/13-chatgpt-class-rename-summary-doc-comments/13-VERIFICATION.md
@.planning/codebase/CONVENTIONS.md
@CLAUDE.md
@.editorconfig

<interfaces>
<!-- Tone anchors (read once before editing): -->
<!--   Class summary:     DeckFlow.Web/Services/CardLookupService.cs:39-42 -->
<!--   Interface summary: DeckFlow.Web/Services/CardLookupService.cs:13-27 (interface + methods) -->
<!--   Record (positional ctor): DeckFlow.Web/Services/CommanderSpellbookService.cs:14-19 -->
<!--   Record (property-style): DeckFlow.Core/Models/DeckEntry.cs target shape per RESEARCH.md "Code Examples → Record summary with property-style ctor" -->
<!--   Public ctor (multi-line): DeckFlow.Web/Services/ScryfallTaggerService.cs:59-72 (renamed to ScryfallTaggerLookupService.cs in Plan 14-02) -->
<!--   Test class summary (richer floor): DeckFlow.Web.Tests/DeckAnalysisPacketServiceTests.cs:13-17 -->
<!--   Test class summary (acceptable floor): "Tests for <see cref=\"TargetType\"/>." per CONTEXT.md <specifics> -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Backfill summaries on DeckFlow.Core public types (records + Models/) — preserve { get; init; }</name>
  <files>
    DeckFlow.Core/Models/DeckEntry.cs
    DeckFlow.Core/Models/DeckDiff.cs
    DeckFlow.Core/Models/LoadedDecks.cs
    DeckFlow.Core/Models/PrintingConflict.cs
    # Plus any additional DeckFlow.Core/ files listed in 14-AUDIT-REPORT.md `### DeckFlow.Core`
  </files>
  <read_first>
    - .planning/phases/14-broader-codebase-name-vs-behavior-audit/14-AUDIT-REPORT.md `### DeckFlow.Core` doc-comment backfill list
    - .planning/phases/14-broader-codebase-name-vs-behavior-audit/14-AUDIT-REPORT.md `## Plan 14-02 per-commit { get; init; } preservation gate` (gate also required here)
    - .planning/phases/14-broader-codebase-name-vs-behavior-audit/14-RESEARCH.md sections "Doc Backfill Mechanics (AUDIT-02 / Plan 14-03)" + "Code Examples → Record summary with property-style ctor" + "Pitfall 3: { get; init; } accessor stripped"
    - DeckFlow.Core/Models/DeckEntry.cs (full file — read once, derive all 8 property semantics, write all summaries in one pass)
    - DeckFlow.Core/Models/DeckDiff.cs (full file)
    - DeckFlow.Core/Models/LoadedDecks.cs (full file)
    - DeckFlow.Core/Models/PrintingConflict.cs (full file)
    - DeckFlow.Web/Services/CardLookupService.cs:39-42 (class style anchor)
    - DeckFlow.Web/Services/CommanderSpellbookService.cs:14-19 (record style anchor)
    - CLAUDE.md (preserve { get; init; }; preserve Allman braces; preserve switch expressions; preserve raw-string literals; LF line endings; no formatter sweeps; one logical change per commit)
  </read_first>
  <action>
0. **Capture plan-start SHA** before any edits (used by `<verification>` block + Task 2 verify predicate below; replaces unset `PLAN_START_TIMESTAMP` env var):

       PLAN_START_SHA=$(git rev-parse HEAD)
       echo "Plan 14-03 baseline SHA: $PLAN_START_SHA"

   If shell state resets between tasks, re-derive via `git log --grep='^refactor(14-02):' --format=%H | tail -1 | xargs -I {} git rev-list -1 {}~1` (the commit immediately before Plan 14-02's first commit, i.e., Plan 14-01's closing commit). Simpler fallback: `PLAN_START_SHA=$(git log --grep='^refactor(14-02): rename ScryfallTaggerService' --format=%H -1)~0` is not right — use the immediately-prior-to-this-plan SHA marker by reading `.planning/phases/14-broader-codebase-name-vs-behavior-audit/14-BASELINE.md` for the `Phase 14 baseline SHA` line and combining with `git log --grep='^refactor(14-02):' --format=%H | tail -1` (last test-double rename commit) as the BASE.

   For the simplest reliable derivation: this plan's start is HEAD just before its first commit lands. Capture once at step 0 and rely on the shell env var. If executor needs cross-task continuity, store in a scratch file: `echo "$PLAN_START_SHA" > /tmp/p14-03-start-sha`.

For each file listed in 14-AUDIT-REPORT.md `### DeckFlow.Core`, backfill `<summary>` on:
- The class/record/interface declaration (1 summary per type)
- Every public property on the type (one-line summary per property — derive semantics from how the property is used elsewhere in DeckFlow.Core)
- Public ctors only if non-trivial (positional record ctors derive params from the property summaries — no separate ctor summary needed)

Concrete process per file (use DeckEntry.cs as the worked example, then repeat):

1. Read the file once. Note every public property's purpose (search for usages via grep if not obvious):

       grep -rEn '\.PropertyName\b' --include='*.cs' DeckFlow.Core/ DeckFlow.Web/ DeckFlow.CLI/

2. Edit the file in one pass — add `<summary>` lines immediately above each declaration (class + properties + non-trivial ctors). Match the RESEARCH.md "Code Examples → Record summary with property-style ctor" template exactly:

       namespace DeckFlow.Core.Models;

       /// <summary>
       /// A single card entry on one of a deck's boards.
       /// </summary>
       public sealed record DeckEntry
       {
           /// <summary>The card's printed name as supplied by the source.</summary>
           public required string Name { get; init; }    // ← MUST stay { get; init; }

           /// <summary>Lowercased card name with Unicode punctuation collapsed.</summary>
           public required string NormalizedName { get; init; }

           // ...one-line summary per remaining property
       }

   **Single-line `<summary>` style is acceptable** for property summaries when terse — matches the codebase pattern. Use the verbatim shape above (`/// <summary>X</summary>`).

   **Multi-line `<summary>` style** is required for class declarations and any property with non-obvious semantics (≥10 words).

3. **CRITICAL preservation discipline** — after writing the edits but BEFORE committing:

       git diff -- '*.cs' | grep -E "^\-.*\{ get; init; \}" | grep -v "^--"

   Expected: empty. If ANY output: the edit stripped `init`. Restore the affected file via `git checkout -- <file>` and redo by hand without triggering any IDE format-on-save.

   Also visually verify in the diff that:
   - No `[JsonPropertyName]` attribute moved inline onto the property declaration (CLAUDE.md attribute rule).
   - No switch expression was converted to a switch statement (none in these files, but eyeball the diff).
   - No raw-string literal was re-indented (none in these files, but eyeball).
   - No `using` import was rearranged.
   - Line endings are LF (`file <touched-file>` must NOT report CRLF).

4. **Build clean** per RESEARCH.md "Mid-Plan Build Cadence":

       "/mnt/c/Program Files/dotnet/dotnet.exe" build DeckFlow.sln --no-restore --nologo --verbosity quiet | tail -3

   Expected: `Build succeeded. 0 Warning(s) 0 Error(s)`. (Note: build will still succeed even with missing docs due to `.editorconfig` suppression — that's the RESEARCH.md "GenerateDocumentationFile Reality Check" finding. The build-clean check verifies syntactic correctness, not coverage; coverage is Plan 14-04's gate.)

5. **Commit per logical chunk** — group related files into one commit. Suggested grouping:
   - Commit 1: Core `Models/` records (DeckEntry, DeckDiff, LoadedDecks, PrintingConflict + others surfaced by audit)
   - Commit 2 (if applicable): Any non-`Models/` DeckFlow.Core files surfaced by audit

   Commit message format:

       refactor(14-03): backfill <summary> on DeckFlow.Core/Models records (DeckEntry, DeckDiff, LoadedDecks, PrintingConflict)

   Plain author, no Co-Authored-By trailer per CLAUDE.md.

6. **Repeat steps 1-5 for every file** in 14-AUDIT-REPORT.md `### DeckFlow.Core`.
  </action>
  <verify>
    <automated>PLAN_START_SHA=${PLAN_START_SHA:-$(cat /tmp/p14-03-start-sha 2>/dev/null || git log --grep='^refactor(14-02):' --format=%H | tail -1 | xargs -I {} git rev-parse {}~0)}; grep -q '/// <summary>' DeckFlow.Core/Models/DeckEntry.cs && grep -q '/// <summary>' DeckFlow.Core/Models/DeckDiff.cs && grep -q '/// <summary>' DeckFlow.Core/Models/LoadedDecks.cs && grep -q '/// <summary>' DeckFlow.Core/Models/PrintingConflict.cs && grep -c '{ get; init; }' DeckFlow.Core/Models/DeckEntry.cs | grep -qE '^[1-9]' && ! git log -p "${PLAN_START_SHA}..HEAD" -- 'DeckFlow.Core/**/*.cs' | grep -E "^\-.*\{ get; init; \}" | grep -v "^---" && "/mnt/c/Program Files/dotnet/dotnet.exe" build DeckFlow.sln --no-restore --nologo --verbosity quiet 2>&1 | tail -3 | grep -qE '0 Warning\(s\)' && git log "${PLAN_START_SHA}..HEAD" --format='%s' | grep -q 'refactor(14-03):.*DeckFlow.Core'</automated>
  </verify>
  <acceptance_criteria>
    - DeckFlow.Core/Models/DeckEntry.cs has class-level `<summary>` plus 8 property summaries (one per public init property)
    - DeckFlow.Core/Models/DeckDiff.cs, LoadedDecks.cs, PrintingConflict.cs likewise have class + property summaries
    - Every other file listed in 14-AUDIT-REPORT.md `### DeckFlow.Core` has a class-level summary
    - `{ get; init; }` accessor preservation grep returns empty across every commit landed in this task
    - No `[JsonPropertyName]` attribute moved inline onto a property declaration
    - No switch expression converted to switch statement
    - No raw-string literal re-indented
    - `dotnet build DeckFlow.sln --no-restore` exits 0 with 0 Warning(s) 0 Error(s) after each commit
    - LF line endings preserved on every touched file
    - Commit hygiene: each commit `refactor(14-03):` prefix, plain author, no Co-Authored-By
  </acceptance_criteria>
  <done>DeckFlow.Core public types fully documented. Plan 14-04 XML coverage diff for Core can pass.</done>
</task>

<task type="auto">
  <name>Task 2: Backfill summaries on DeckFlow.Core.Tests + DeckFlow.Web.Tests test classes</name>
  <files>
    # Authoritative list lives in 14-AUDIT-REPORT.md `### DeckFlow.Core.Tests` + `### DeckFlow.Web.Tests` doc-comment backfill targets.
    # Per RESEARCH.md "State of the Project": ~10 Core.Tests files + ~37 Web.Tests files lack summaries.
  </files>
  <read_first>
    - .planning/phases/14-broader-codebase-name-vs-behavior-audit/14-AUDIT-REPORT.md `### DeckFlow.Core.Tests` doc-comment backfill list
    - .planning/phases/14-broader-codebase-name-vs-behavior-audit/14-AUDIT-REPORT.md `### DeckFlow.Web.Tests` doc-comment backfill list
    - .planning/phases/14-broader-codebase-name-vs-behavior-audit/14-RESEARCH.md sections "Already-existing test-class summary anchors (CONTEXT.md `Tests for X` question)" + "Test-class summary anchor" + "Code Examples → Test-class summary (formulaic floor)"
    - DeckFlow.Web.Tests/DeckAnalysisPacketServiceTests.cs:13-17 (richer-floor anchor)
    - DeckFlow.Web.Tests/MetaGapServiceTests.cs:19-23 (richer-floor anchor)
    - CONTEXT.md `<specifics>`: "Tests for X" formulaic wording is acceptable for test classes (~55 of them in Web.Tests; pure mechanical work)
    - CLAUDE.md
  </read_first>
  <action>
For each test file listed in 14-AUDIT-REPORT.md, add a class-level `<summary>`. Per CONTEXT.md `<specifics>` and RESEARCH.md "Test-class summary anchor":

- **Preferred shape** (richer floor): one sentence naming the behavior under test, with `<see cref="TargetType"/>` cross-reference. Example:

      /// <summary>
      /// Covers staged prompt generation, validation, and artifact output for the deck-analysis
      /// workflow served by <see cref="DeckAnalysisPacketService"/> across all supported AI platforms.
      /// </summary>
      public sealed class DeckAnalysisPacketServiceTests

- **Acceptable floor** (formulaic) when behavior is mechanical and a richer summary would be redundant:

      /// <summary>
      /// Tests for <see cref="TargetType"/>.
      /// </summary>
      public sealed class TargetTypeTests

- Use the richer floor for test classes covering business logic (services, parsers, importers). Use the formulaic floor for pure CRUD test classes and trivial helper-class tests.

Per-method summaries are NOT required (D-03 says public class + interface; methods are nice-to-have not required).

Execution:

1. Read the audit report's two test-project sections. For each file:
   - Open the file. Find the public test class declaration line.
   - Identify the "type under test" — usually `<TestedType>Tests` naming. Derive `<see cref="TestedType"/>`.
   - Insert a 3-line `<summary>` block immediately above the `public sealed class` declaration. Touch only those lines.

2. After each file edit, run `{ get; init; }` preservation grep (paranoid — test files don't typically use init records, but the IDE auto-format can still bite if a `*Tests` file constructs records):

       git diff -- '*.cs' | grep -E "^\-.*\{ get; init; \}" | grep -v "^--"
       # Expected: empty

3. After each chunk (5-10 files), run build:

       "/mnt/c/Program Files/dotnet/dotnet.exe" build DeckFlow.sln --no-restore --nologo --verbosity quiet | tail -3
       # Expected: 0 Warning(s) 0 Error(s)

4. Commit per logical chunk. Suggested grouping:
   - Commit 1: All DeckFlow.Core.Tests files (~10 files)
   - Commit 2: DeckFlow.Web.Tests controller test classes
   - Commit 3: DeckFlow.Web.Tests service test classes
   - Commit 4: DeckFlow.Web.Tests model/parser/integration test classes
   - Commit 5+ as needed if chunks are large

   Commit message format:

       refactor(14-03): backfill <summary> on DeckFlow.Core.Tests test classes (10 files)
       refactor(14-03): backfill <summary> on DeckFlow.Web.Tests controller test classes (12 files)

   Plain author, no Co-Authored-By.

5. Allowlist (skip — do not modify):
   - Files already carrying `<summary>` per RESEARCH.md "Already-existing test-class summary anchors" (19 Web.Tests files; the audit report should have excluded these from `### DeckFlow.Web.Tests`).
   - Test files for types Plan 14-02 renamed — those already received summary updates in lockstep.
  </action>
  <verify>
    <automated>PLAN_START_SHA=${PLAN_START_SHA:-$(cat /tmp/p14-03-start-sha 2>/dev/null || git log --grep='^refactor(14-02):' --format=%H | tail -1 | xargs -I {} git rev-parse {}~0)}; missing_core=$(for f in $(grep -lE '^public sealed class' DeckFlow.Core.Tests/*.cs); do if ! grep -B 5 'public sealed class' "$f" | grep -q '/// <summary>'; then echo "$f"; fi; done | wc -l); missing_web=$(for f in $(grep -lE '^public sealed class' DeckFlow.Web.Tests/*.cs); do if ! grep -B 5 'public sealed class' "$f" | grep -q '/// <summary>'; then echo "$f"; fi; done | wc -l); echo "Core.Tests missing: $missing_core, Web.Tests missing: $missing_web"; [ "$missing_core" = "0" ] && [ "$missing_web" = "0" ] && "/mnt/c/Program Files/dotnet/dotnet.exe" build DeckFlow.sln --no-restore --nologo --verbosity quiet 2>&1 | tail -3 | grep -qE '0 Warning\(s\)' && [ "$(git log "${PLAN_START_SHA}..HEAD" --format='%b' | grep -c 'Co-Authored-By')" = "0" ]</automated>
  </verify>
  <acceptance_criteria>
    - Every `public sealed class *Tests` declaration in DeckFlow.Core.Tests/ has a `<summary>` block in the 5 lines above it
    - Every `public sealed class *Tests` declaration in DeckFlow.Web.Tests/ listed in the audit report has a `<summary>` block. **Strict 0 missing** in `DeckFlow.Web.Tests/` per D-03 strict reading — the audit report does not carry a Web.Tests allowlist (the only documented allowlist is `TestServiceFactory` from D-05, not a doc-comment allowlist). The previous "≤2 slack" was a silent quality-floor reduction and has been removed per checker revision 1.
    - At least 50% of test-class summaries use the richer-floor form (named behavior + `<see cref="..."/>`), not the formulaic floor — verify by spot-check of 5 random files
    - `{ get; init; }` preservation grep returns empty across all task commits
    - `dotnet build DeckFlow.sln --no-restore` exits 0 after each chunk commit
    - Commit grouping logical (≥3, ≤8 commits across all test-class backfills in this task)
    - No Co-Authored-By trailer; plain author
    - LF line endings preserved
  </acceptance_criteria>
  <done>All test classes carry summaries. Plan 14-04 XML coverage diff for Core.Tests + Web.Tests can pass.</done>
</task>

<task type="auto">
  <name>Task 3: Discretionary opt-in — add summaries to DeckPageTab enum + values</name>
  <files>DeckFlow.Web/Models/DeckPageTab.cs</files>
  <read_first>
    - .planning/phases/14-broader-codebase-name-vs-behavior-audit/14-AUDIT-REPORT.md `### Discretionary additions to Plan 14-03 backfill` (the opt-in decision)
    - .planning/phases/13-chatgpt-class-rename-summary-doc-comments/13-VERIFICATION.md SC2 "Note on DeckPageTab.cs" (Phase 13 intentional skip per 13-01 Pattern 7; Phase 14 reverses for D-03 strict reading)
    - DeckFlow.Web/Models/DeckPageTab.cs (full file — read to count enum values)
    - DeckFlow.Web/Views/Shared/_DeckToolTabs.cshtml (consumer of the enum — informs the per-value summary wording)
    - CLAUDE.md
  </read_first>
  <action>
Per Plan 14-01 audit report `### Discretionary additions to Plan 14-03 backfill`: opt-in DeckPageTab.

1. Open DeckFlow.Web/Models/DeckPageTab.cs. Confirm it's an enum (typically `public enum DeckPageTab` per Phase 13 Pattern 7).
2. Add a class-level `<summary>` describing the enum's purpose. Tone per RESEARCH.md "Public class summary anchor" — terse, present-tense:

       /// <summary>
       /// Identifies the currently-active tab in the Deck workflow strip rendered by
       /// <see href="_DeckToolTabs.cshtml"/>.
       /// </summary>
       public enum DeckPageTab
       {
           // ...
       }

3. Add a single-line `<summary>` to EVERY enum value:

       /// <summary>Deck sync (compare-and-update) page.</summary>
       DeckSync = ...,

       /// <summary>ChatGPT-style deck analysis packet generator.</summary>
       DeckAnalysis = ...,

       /// <summary>Two-deck comparison artifact generator.</summary>
       DeckComparison = ...,

       /// <summary>cEDH meta-gap finder artifact generator.</summary>
       CedhMetaGap = ...,

       // ...continue for every value in the enum at current HEAD

   Touch only the lines added (the `///` comment lines); do NOT change enum values, names, integer assignments, or trailing commas.

4. Run preservation grep (no `{ get; init; }` in an enum, but check anyway for paranoia):

       git diff -- DeckFlow.Web/Models/DeckPageTab.cs | grep -E "^\-.*\{ get; init; \}" | grep -v "^--"
       # Expected: empty

5. Build clean:

       "/mnt/c/Program Files/dotnet/dotnet.exe" build DeckFlow.sln --no-restore --nologo --verbosity quiet | tail -3
       # Expected: 0 Warning(s) 0 Error(s)

6. Commit:

       git add DeckFlow.Web/Models/DeckPageTab.cs
       git commit -m "refactor(14-03): add <summary> to DeckPageTab enum + values (Plan 14-01 discretionary opt-in)"

   Plain author, no Co-Authored-By.

**Note on _DeckToolTabs.cshtml:** Plan 14-01 audit report did NOT plan to touch this file — DO NOT add summary comments to .cshtml files. The Razor view consumes the enum but doesn't itself need an XML doc gloss.
  </action>
  <verify>
    <automated>grep -q '/// <summary>' DeckFlow.Web/Models/DeckPageTab.cs && summaries=$(grep -c '/// <summary>' DeckFlow.Web/Models/DeckPageTab.cs); [ "$summaries" -ge "5" ] && "/mnt/c/Program Files/dotnet/dotnet.exe" build DeckFlow.sln --no-restore --nologo --verbosity quiet 2>&1 | tail -3 | grep -qE '0 Warning\(s\)' && git log -1 --format='%s' | grep -q "DeckPageTab" && [ "$(git log -1 --format='%b' | grep -c 'Co-Authored-By')" = "0" ]</automated>
  </verify>
  <acceptance_criteria>
    - DeckPageTab.cs has a class-level `<summary>` on the enum declaration
    - Every enum value (4+ values at current HEAD) has a one-line `<summary>` comment
    - No enum value name, integer assignment, or ordering changed
    - Build still clean
    - Single commit `refactor(14-03): add <summary> to DeckPageTab enum + values...`
    - Plain author, no Co-Authored-By trailer
    - LF line endings preserved
  </acceptance_criteria>
  <done>Discretionary opt-in committed. Plan 14-04's XML coverage diff over DeckFlow.Web.xml will find DeckPageTab + values documented.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none introduced) | Doc-comment backfill adds `///` comment lines only. Comments do not affect compiled IL, public API contracts, or wire format. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-03-INIT | Tampering (deserialization) | DeckFlow.Core/Models/ records with `{ get; init; }` accessors | mitigate | Per-commit `git diff -- '*.cs' \| grep -E "^\-.*\{ get; init; \}"` gate. If any output: ABORT, restore via `git checkout`. Phase 13 UAT T5 broke when an IDE auto-format stripped `init` from `EdhTop16Client.cs` — System.Text.Json silently skips get-only properties in .NET 9+ and records deserialize as default values. The grep gate per task 1 and 2 is the primary mitigation. CLAUDE.md "preserve `{ get; init; }`" is the source of truth. |
| T-14-03-FMT | Tampering (formatter sweep) | All touched .cs files | mitigate | No Format Document / Code Cleanup / ReSharper-style sweep. Visual diff check at each commit: `git diff --stat` should show small line counts proportional to summary additions (≤ 5x the number of types documented per file). A diff showing 400+/400- on a single file = re-encoding by IDE; abort and redo. |
| T-14-03-CRLF | Tampering (line endings) | All touched .cs files | mitigate | `.gitattributes` enforces LF. Verify via `file <path>` after each edit — must NOT report "CRLF line terminators". |
| T-14-03-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan; doc-comment edits only. |

Per RESEARCH.md "Security Domain" — Phase 14 introduces no new threats to the public API surface. Doc comments are stripped from compiled IL except where consumed by IntelliSense; they have no runtime effect.
</threat_model>

<verification>
Plan 14-03 phase-level verification:

1. **Coverage check** — every type in 14-AUDIT-REPORT.md `## Doc-comment backfill targets` now has a `<summary>`:

       grep -B 5 'public sealed class\|public sealed record\|public interface\|public enum\|public abstract class\|public static class' DeckFlow.Core/Models/*.cs | grep -c '/// <summary>'

   Compare against the count from 14-BASELINE-PUBLIC-TYPES.txt for DeckFlow.Core/Models/. Coverage should be 100% within Models/.

2. **`{ get; init; }` preservation across all Plan 14-03 commits** (SHA-scoped — replaces unset `PLAN_START_TIMESTAMP` env var):

       PLAN_START_SHA=${PLAN_START_SHA:-$(cat /tmp/p14-03-start-sha 2>/dev/null || git log --grep='^refactor(14-02):' --format=%H | tail -1 | xargs -I {} git rev-parse {}~0)}
       git log -p "${PLAN_START_SHA}..HEAD" -- 'DeckFlow.Core/**/*.cs' DeckFlow.Web/**/*.cs DeckFlow.Web.Tests/**/*.cs DeckFlow.Core.Tests/**/*.cs | grep -E "^\-.*\{ get; init; \}" | grep -v "^---"

   Expected: 0 hits.

3. **Build green at HEAD**:

       "/mnt/c/Program Files/dotnet/dotnet.exe" build DeckFlow.sln --configuration Release --no-incremental --nologo

   Expected tail: `Build succeeded. 0 Warning(s) 0 Error(s)`.

4. **Commit hygiene check** (SHA-scoped):

       PLAN_START_SHA=${PLAN_START_SHA:-$(cat /tmp/p14-03-start-sha 2>/dev/null || git log --grep='^refactor(14-02):' --format=%H | tail -1 | xargs -I {} git rev-parse {}~0)}
       git log "${PLAN_START_SHA}..HEAD" --format='%b' | grep -ic 'Co-Authored-By'
       # Expected: 0

       git log "${PLAN_START_SHA}..HEAD" --format='%s' | grep -c '^refactor(14-03):'
       # Expected: 4-8 commits (Core records, Core.Tests batch, multiple Web.Tests batches, DeckPageTab)

5. **DeckPageTab opt-in landed**:

       grep -c '/// <summary>' DeckFlow.Web/Models/DeckPageTab.cs
       # Expected: ≥ 5 (1 enum + ≥4 values)

6. **No formatter sweep** — sanity-check diff sizes (SHA-scoped):

       PLAN_START_SHA=${PLAN_START_SHA:-$(cat /tmp/p14-03-start-sha 2>/dev/null || git log --grep='^refactor(14-02):' --format=%H | tail -1 | xargs -I {} git rev-parse {}~0)}
       git log "${PLAN_START_SHA}..HEAD" --stat --format='%h %s' | awk '/files? changed/ { if ($1 + 0 > 200) print "SUSPICIOUS:", $0 }'

   Any output indicates a file changed >200 lines (possible re-encoding or formatter sweep). Investigate.
</verification>

<success_criteria>
- Every public class + interface + record listed in 14-AUDIT-REPORT.md `## Doc-comment backfill targets` carries `<summary>` documentation at HEAD.
- Public properties on Core records (DeckEntry, DeckDiff, LoadedDecks, PrintingConflict) carry one-line property summaries.
- DeckPageTab enum + values carry summaries (Plan 14-01 discretionary opt-in).
- `{ get; init; }` accessor preservation grep returns 0 across all Plan 14-03 commits.
- Build remains green at HEAD.
- Commits grouped logically per CLAUDE.md "one logical change per commit" — typically 4-8 commits across all of Task 1+2+3.
- All commits plain author, no Co-Authored-By trailer.
- LF line endings preserved on every touched file.
- AUDIT-02 substantively complete (verification mechanism — XML coverage diff — runs in Plan 14-04).
</success_criteria>

<output>
Create `.planning/phases/14-broader-codebase-name-vs-behavior-audit/14-03-SUMMARY.md` documenting:
- Number of files touched per project (Core, Core.Tests, Web.Tests, Web/Models/DeckPageTab.cs)
- Number of class-level summaries added
- Number of property summaries added (Core records only)
- Confirmation of `{ get; init; }` preservation grep returning 0 across all commits
- Confirmation that Web.csproj's existing NoWarn 1591/1573/1587 stays intact (out of Phase 14 scope per CONTEXT.md "Deferred Ideas")
- Confirmation that ~88 v1.1-era still-undoc'd Web public types remain out of scope per CONTEXT.md "Deferred Ideas" (NOT a Plan 14-03 gap)
- Pointer to Plan 14-04 for csproj GenerateDocumentationFile flip + XML coverage diff (final gate)
</output>
</content>
</invoke>