---
phase: 98-card-grounding-guard
plan: 05
type: execute
wave: 4
depends_on: ["98-03"]
gap_closure: true
files_modified:
  - DeckFlow.Web/Program.cs
  - DeckFlow.Web.Tests/Services/CreatorStyle/CreatorStyleDiRegistrationTests.cs
autonomous: true
requirements: []

must_haves:
  truths:
    - "A Development-environment cold start no longer crashes at WebApplicationBuilder.Build() with unresolvable creator-style service descriptors"
    - "ICreatorProfileSourceStore and CategoryKnowledgeRepository are registered in Web DI so the P95 trio (CreatorProfileDeckCrawler, CreatorDeckCategoryResolver, MeasuredStyleProfileBuilder) is resolvable"
    - "A regression test locks the full creator-style dependency graph under ValidateOnBuild so a future unregistered dep fails in CI, not at dev boot"
  artifacts:
    - path: "DeckFlow.Web/Program.cs"
      provides: "Store registrations completing the P95 creator-style DI graph"
      contains: "ICreatorProfileSourceStore"
---

<objective>
Close UAT GAP-98-01: dev cold start crashes because P95 registered three scoped
creator-style services whose store dependencies were never added to Web DI.
Phase 98-03 fixed one (ICreatorDeckCacheStore); this plan fixes the remaining two
and locks the graph with a test.
</objective>

<context>
Diagnosis (98-UAT.md GAP-98-01): Development ValidateOnBuild rejects
CreatorProfileDeckCrawler + MeasuredStyleProfileBuilder (missing
DeckFlow.Core.Content.ICreatorProfileSourceStore) and CreatorDeckCategoryResolver
(missing DeckFlow.Core.Knowledge.CategoryKnowledgeRepository). Pre-existing at
baseline 675b1cf6; dev-only; production unaffected.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register the two missing creator-style store dependencies + DI regression test</name>
  <files>DeckFlow.Web/Program.cs, DeckFlow.Web.Tests/Services/CreatorStyle/CreatorStyleDiRegistrationTests.cs</files>
  <read_first>
    - DeckFlow.Web/Program.cs (lines ~95-105 content-store registrations incl. the 98-03 ICreatorDeckCacheStore block, and lines ~192-194 the P95 scoped trio)
    - DeckFlow.Web/Services/Persistence/DeckFlowDatabaseConnectionFactory.cs (CreateCreatorDeckCacheConnection + CreateCategoryKnowledgeConnection)
    - DeckFlow.Core/Content/CreatorProfileSourceStore.cs (ctor: RelationalDatabaseConnection, ensureSchemaEnabled default true)
    - DeckFlow.Core/Knowledge/CategoryKnowledgeRepository.cs (ctor: RelationalDatabaseConnection, ILogger? optional)
    - DeckFlow.Web.Tests/Services/CreatorStyle/CreatorWhitelistPoolBuilderTests.cs (the 98-03 ValidateOnBuild DI-resolution test — pattern to extend)
  </read_first>
  <action>
    In Program.cs, immediately after the ICreatorDeckCacheStore registration (98-03 block), add:
    (1) builder.Services.AddSingleton&lt;DeckFlow.Core.Content.ICreatorProfileSourceStore&gt;(_ =&gt;
        new DeckFlow.Core.Content.CreatorProfileSourceStore(
            DeckFlowDatabaseConnectionFactory.CreateCreatorDeckCacheConnection(builder.Environment)));
        — creator_profile_source lives in the same creator-deck-cache.db as the deck cache
        (both are P95 crawl-state; table names do not collide). // Why: comment stating db co-location choice.
    (2) builder.Services.AddSingleton&lt;DeckFlow.Core.Knowledge.CategoryKnowledgeRepository&gt;(_ =&gt;
        new DeckFlow.Core.Knowledge.CategoryKnowledgeRepository(
            DeckFlowDatabaseConnectionFactory.CreateCategoryKnowledgeConnection(builder.Environment)));
        — same category-knowledge.db the CategoryKnowledgeStore already wraps.
    New test file CreatorStyleDiRegistrationTests: compose a ServiceCollection registering the
    same creator-style graph shape (temp-SQLite ICreatorDeckCacheStore + ICreatorProfileSourceStore +
    CategoryKnowledgeRepository + fake/minimal Scryfall deps mirroring the trio's ctors), register
    the three scoped services (CreatorProfileDeckCrawler, CreatorDeckCategoryResolver,
    MeasuredStyleProfileBuilder), BuildServiceProvider(ValidateOnBuild=true, ValidateScopes=true),
    and GetRequiredService each within a scope — proving the P95 trio resolves.
    Preserve Program.cs line endings exactly; change only inserted lines.
  </action>
  <acceptance_criteria>
    - `grep -c "ICreatorProfileSourceStore" DeckFlow.Web/Program.cs` >= 1 and `grep -c "CategoryKnowledgeRepository" DeckFlow.Web/Program.cs` >= 1
    - New test passes: `dotnet test DeckFlow.Web.Tests --filter FullyQualifiedName~CreatorStyleDiRegistration`
    - Dev cold start: scripts/run-web-test.sh (on a free port) boots to "Now listening" with zero DI validation errors, homepage 200 from THIS process
    - Full Web suite still green
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
| ID | STRIDE | Threat | Disposition |
|----|--------|--------|-------------|
| T-98-40 | DoS | Dev boot crash blocks all local verification of later phases | Mitigated by this plan (registrations + ValidateOnBuild regression test) |
| T-98-41 | Tampering | Wrong DB file chosen for creator_profile_source could split P95 crawl state | Mitigated: co-locate with creator-deck-cache.db, documented // Why: comment |
</threat_model>

<verification>
- Cold start on free port boots clean (the UAT test 1 re-run).
- CreatorStyleDiRegistration test green; full Web suite green.
</verification>

must_haves derived from GAP-98-01; no phase requirements attached (gap closure).
