---
phase: 26-category-cache-schema-normalization-fresh-start
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - DeckFlow.Core/Storage/IRelationalDialect.cs
  - DeckFlow.Core/Storage/SqliteRelationalDialect.cs
  - DeckFlow.Core/Storage/PostgresRelationalDialect.cs
  - DeckFlow.Core/Knowledge/CategoryKnowledgeRepository.cs
  - DeckFlow.Core.Tests/CategoryKnowledgeRepositoryTests.cs
  - DeckFlow.Core.Tests/CategoryCacheSchemaParityTests.cs
autonomous: true
requirements: [DBO-01]
must_haves:
  truths:
    - "EnsureSchemaAsync creates the normalized schema (cards dimension, deck_queue with integer surrogate id + content_hash, integer-keyed card_category_observations + card_deck_totals) idempotently on a clean SQLite DB"
    - "SQLite accepts an INTEGER PRIMARY KEY AUTOINCREMENT surrogate id alongside a separate UNIQUE grain index on the same fact table (A1 confirmed by a passing harness test, not assumed)"
    - "A fixed-sample parity test (RED in this plan, GREEN in 26-02) pins GetCategoriesAsync('Sol Ring') == ['Artifact'/'Ramp'] and a commander aggregate row, surviving the schema swap"
  artifacts:
    - path: "DeckFlow.Core/Storage/IRelationalDialect.cs"
      provides: "SurrogateIdColumnType member"
      contains: "SurrogateIdColumnType"
    - path: "DeckFlow.Core/Knowledge/CategoryKnowledgeRepository.cs"
      provides: "Normalized dual-dialect DDL in EnsureSchemaAsync (cards, extended deck_queue, slim integer-keyed facts, indexes)"
      contains: "cards"
    - path: "DeckFlow.Core.Tests/CategoryCacheSchemaParityTests.cs"
      provides: "Fixed-sample parity assertions + dimension-interning + SQLite AUTOINCREMENT+UNIQUE harness"
      contains: "Sol Ring"
  key_links:
    - from: "CategoryKnowledgeRepository.EnsureSchemaAsync"
      to: "IRelationalDialect.SurrogateIdColumnType"
      via: "interpolated id-column type in DDL"
      pattern: "SurrogateIdColumnType"
---

<objective>
Lay the dual-dialect schema foundation for the normalized category cache: add an `IRelationalDialect.SurrogateIdColumnType` member, reshape `EnsureSchemaAsync` to create the new integer-keyed star schema on a clean DB (a `cards` dimension, `deck_queue` extended with an integer surrogate `id` + reserved `content_hash`, and slim `card_category_observations` / `card_deck_totals` facts referencing `deck_id INT` + `card_id INT`), and create the failing parity-test scaffold that the write/read port in 26-02 will turn green.

Purpose: This phase is a fresh-start schema reshape (DB wiped + re-harvested, no online migration). The schema + dialect contract must exist and be proven dual-dialect-safe BEFORE the write/read paths are re-pointed at integer keys. The single biggest correctness risk is parity; the single biggest dual-dialect risk is the SQLite AUTOINCREMENT + composite-PK restriction (RESEARCH A1). Both are nailed down here.

Output: New dialect member, new DDL in `EnsureSchemaAsync`, and a RED parity/harness test file. The repository's read/write SQL is NOT yet ported (that is 26-02) — this plan only changes schema-creation DDL and adds tests.

This plan implements DBO-01 (schema half). Codex implements the code AND the tests; Claude reviews. NO new packages. Dual-dialect only via `IRelationalDialect` (never `if (IsSqlite)` string branching in `EnsureSchemaAsync`). Preserve LF, `{ get; init; }`, and raw-string SQL literal indentation — touch only the lines that change. No Format Document / Code Cleanup.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/26-category-cache-schema-normalization-fresh-start/26-RESEARCH.md
@CLAUDE.md

<interfaces>
<!-- Established dual-dialect pattern to mirror. Extracted from codebase — Codex should follow this verbatim. -->

From DeckFlow.Core/Storage/IRelationalDialect.cs (current — add one member):
  string FeedbackIdColumnType { get; }          // SQLite: "INTEGER PRIMARY KEY AUTOINCREMENT"
                                                 // Postgres: "BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY"
  string FeedbackInsertReturningIdSql { get; }   // both dialects end the INSERT with "RETURNING id;"

From DeckFlow.Core/Knowledge/CategoryKnowledgeRepository.cs (current schema — to be replaced):
  - deck_queue: (deck_id TEXT PRIMARY KEY, inserted_utc, processed, skipped, last_checked_utc, commander_name TEXT NULL)
  - card_category_observations: (source TEXT, card_name, normalized_card_name, category, board, deck_count, count, last_seen_utc, PRIMARY KEY(source, normalized_card_name, category, board))
  - card_deck_totals: (source TEXT, card_name, normalized_card_name, board, deck_count, last_seen_utc, PRIMARY KEY(source, normalized_card_name, board))
  - EnsureSchemaAsync calls: CreateCardCategoryObservationsTableAsync, inline deck_queue CREATE, crawl_state CREATE, EnsureDeckQueueColumnsAsync, EnsureCategoryObservationSchemaAsync, CreateCardDeckTotalsTableAsync, then the index command block.
  - AddParameter pattern: RelationalDatabaseConnection.AddParameter(command, "@name", value). Never string-interpolate user data into SQL.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add SurrogateIdColumnType dialect member</name>
  <files>DeckFlow.Core/Storage/IRelationalDialect.cs, DeckFlow.Core/Storage/SqliteRelationalDialect.cs, DeckFlow.Core/Storage/PostgresRelationalDialect.cs</files>
  <read_first>
    - DeckFlow.Core/Storage/IRelationalDialect.cs (full — adding one member)
    - DeckFlow.Core/Storage/SqliteRelationalDialect.cs (full — mirror FeedbackIdColumnType)
    - DeckFlow.Core/Storage/PostgresRelationalDialect.cs (full — mirror FeedbackIdColumnType)
    - .planning/phases/26-category-cache-schema-normalization-fresh-start/26-RESEARCH.md (section "Recommended Schema (concrete DDL)" — IRelationalDialect additions)
  </read_first>
  <action>
    Add a new property `string SurrogateIdColumnType { get; }` to interface `IRelationalDialect`. Implement on `SqliteRelationalDialect` returning the literal `INTEGER PRIMARY KEY AUTOINCREMENT` and on `PostgresRelationalDialect` returning `BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY` — exactly the forms already used for `FeedbackIdColumnType`. Add a `///` `<summary>` doc-comment on the new interface member (per project XML-doc convention; NoWarn is still active in Core but document it for parity with siblings). Do NOT touch the existing `Feedback*` members. Touch only the added lines.
  </action>
  <verify>
    <automated>"/mnt/c/Program Files/dotnet/dotnet.exe" build DeckFlow.Core/DeckFlow.Core.csproj -c Release</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'SurrogateIdColumnType' DeckFlow.Core/Storage/IRelationalDialect.cs` returns 1
    - `grep -F 'INTEGER PRIMARY KEY AUTOINCREMENT' DeckFlow.Core/Storage/SqliteRelationalDialect.cs` matches the new member
    - `grep -F 'BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY' DeckFlow.Core/Storage/PostgresRelationalDialect.cs` matches the new member
    - Build clean: 0 new warnings, 0 errors
    - `git diff DeckFlow.Core/Storage/IRelationalDialect.cs` shows only additions (no reordering / reformatting of existing members)
  </acceptance_criteria>
  <done>Both dialects expose SurrogateIdColumnType; build clean; existing members untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Reshape EnsureSchemaAsync to the normalized dual-dialect schema</name>
  <files>DeckFlow.Core/Knowledge/CategoryKnowledgeRepository.cs</files>
  <read_first>
    - DeckFlow.Core/Knowledge/CategoryKnowledgeRepository.cs (lines 50-207 — EnsureSchemaAsync, CreateCardCategoryObservationsTableAsync, CreateCardDeckTotalsTableAsync, EnsureDeckQueueColumnsAsync, EnsureCategoryObservationSchemaAsync, MigrateCategoryObservationsTableAsync, and the index command block)
    - .planning/phases/26-category-cache-schema-normalization-fresh-start/26-RESEARCH.md (sections "Recommended Schema (concrete DDL — dual-dialect)", Pitfall 1, Pitfall 6, "Forward-Compat with Phase 27")
  </read_first>
  <action>
    Reshape the schema-creation DDL for a FRESH DB. Use the dialect member `_connectionInfo` exposes — resolve the dialect (mirror how `FeedbackIdColumnType` is reached elsewhere in Storage; if the repository does not already hold an `IRelationalDialect`, obtain it from `RelationalDatabaseConnection` the same way the feedback path does) and interpolate `SurrogateIdColumnType` into each surrogate-`id` column. Concrete new shapes (match RESEARCH DDL exactly):

    1. `cards` dimension (NEW table): columns `id {SurrogateIdColumnType}`, `normalized_card_name TEXT NOT NULL`, `display_name TEXT NOT NULL`. Add `CREATE UNIQUE INDEX IF NOT EXISTS ux_cards_normalized ON cards(normalized_card_name)`.
    2. `deck_queue` (reshape): `id {SurrogateIdColumnType}`, `deck_id TEXT NOT NULL`, `inserted_utc TEXT NOT NULL`, `processed INTEGER NOT NULL DEFAULT 0`, `skipped INTEGER NOT NULL DEFAULT 0`, `last_checked_utc TEXT`, `commander_name TEXT NULL`, `content_hash TEXT NULL`. Add `CREATE UNIQUE INDEX IF NOT EXISTS ux_deck_queue_deck_id ON deck_queue(deck_id)`. `content_hash` is the Phase 27 reserve (D7) — declare it, do NOT populate or read it this phase.
    3. `card_category_observations` (reshape to slim integer-keyed fact): `id {SurrogateIdColumnType}`, `deck_id INTEGER NOT NULL`, `card_id INTEGER NOT NULL`, `category TEXT NOT NULL`, `board TEXT NOT NULL DEFAULT 'mainboard'`, `deck_count INTEGER NOT NULL DEFAULT 0`, `count INTEGER NOT NULL`, `last_seen_utc TEXT NOT NULL`. KEEP this exact table name (A5 — preserves the `to_regclass('public.card_category_observations')` reltuples fast-path in CategoryKnowledgeStore). Add `CREATE UNIQUE INDEX IF NOT EXISTS ux_obs_grain ON card_category_observations(deck_id, card_id, category, board)`, plus `CREATE INDEX IF NOT EXISTS ix_obs_card ON card_category_observations(card_id)` and `CREATE INDEX IF NOT EXISTS ix_obs_deck ON card_category_observations(deck_id)`.
    4. `card_deck_totals` (reshape): `id {SurrogateIdColumnType}`, `deck_id INTEGER NOT NULL`, `card_id INTEGER NOT NULL`, `board TEXT NOT NULL DEFAULT 'mainboard'`, `deck_count INTEGER NOT NULL DEFAULT 0`, `last_seen_utc TEXT NOT NULL`. Add `CREATE UNIQUE INDEX IF NOT EXISTS ux_totals_grain ON card_deck_totals(deck_id, card_id, board)` and `CREATE INDEX IF NOT EXISTS ix_totals_card ON card_deck_totals(card_id)`.

    Constraints (RESEARCH A1, A2, Pitfall 1, Pitfall 6):
    - Surrogate `id` is the sole PRIMARY KEY on every table; enforce the natural grain via a separate `CREATE UNIQUE INDEX` (SQLite forbids AUTOINCREMENT-PK + composite PK on one table).
    - NO declared FOREIGN KEY constraints (A2 — uniform dual-dialect behavior, avoids 4M-insert FK cost). Document the relationships in a `// Why:` comment.
    - This is fresh-start: the column-probe ALTER helpers (`EnsureDeckQueueColumnsAsync`, `EnsureCategoryObservationSchemaAsync`, `MigrateCategoryObservationsTableAsync`) target the OLD wide shape. Remove or neuter the ones whose only job was migrating the old `source`/`card_name` columns; keep `crawl_state` creation unchanged. Do NOT attempt `ALTER TABLE ADD COLUMN ... INTEGER PRIMARY KEY AUTOINCREMENT` (impossible on SQLite — fresh-start avoids it; dev SQLite DBs are deleted not migrated, per RESEARCH Pitfall 6).
    - Keep the existing index-creation try/catch resilience wrapper (timeout + DbException swallow) — the new indexes go inside it or an equivalent guarded block, preserving the "continue without optional indexes on failure" behavior (observation: a prior startup timeout regression).
    - Preserve raw-string (`"""`) SQL literal indentation; do not re-indent existing literals you are not changing.
  </action>
  <verify>
    <automated>"/mnt/c/Program Files/dotnet/dotnet.exe" build DeckFlow.Core/DeckFlow.Core.csproj -c Release</automated>
  </verify>
  <acceptance_criteria>
    - `grep -F 'CREATE TABLE IF NOT EXISTS cards' DeckFlow.Core/Knowledge/CategoryKnowledgeRepository.cs` matches
    - `grep -F 'content_hash TEXT NULL' DeckFlow.Core/Knowledge/CategoryKnowledgeRepository.cs` matches (deck_queue reserve)
    - `grep -F 'deck_id INTEGER NOT NULL' DeckFlow.Core/Knowledge/CategoryKnowledgeRepository.cs` matches AND `grep -F 'card_id INTEGER NOT NULL'` matches (facts are integer-keyed)
    - `grep -c 'SurrogateIdColumnType' DeckFlow.Core/Knowledge/CategoryKnowledgeRepository.cs` returns ≥4 (one per surrogate-id table: cards, deck_queue, observations, totals)
    - `grep -F 'ux_obs_grain' DeckFlow.Core/Knowledge/CategoryKnowledgeRepository.cs` AND `ux_cards_normalized` AND `ix_obs_card` all match
    - No `FOREIGN KEY` or `REFERENCES` keyword in the new fact DDL: `grep -ci 'references\|foreign key' DeckFlow.Core/Knowledge/CategoryKnowledgeRepository.cs` returns 0 (excluding comments — verify by inspection if grep counts comment prose)
    - The string-concat marker `card_name TEXT NOT NULL` no longer appears in the observations/totals CREATE DDL (old wide columns removed): inspect that `card_name` and `normalized_card_name` are gone from those two CREATE statements
    - Build clean: 0 new warnings, 0 errors
  </acceptance_criteria>
  <done>EnsureSchemaAsync emits the normalized dual-dialect DDL via SurrogateIdColumnType; old wide migration helpers neutered; build clean. Read/write SQL still references old columns and will NOT compile-break because those are separate methods ported in 26-02 — if any read/write method references a now-removed column at compile time, leave it; 26-02 rewrites them. (If the build fails because a method body references removed DDL columns only at SQL-string level, that is fine — SQL strings are not compile-checked.)</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: RED parity + interning + SQLite-AUTOINCREMENT harness tests</name>
  <files>DeckFlow.Core.Tests/CategoryCacheSchemaParityTests.cs, DeckFlow.Core.Tests/CategoryKnowledgeRepositoryTests.cs</files>
  <read_first>
    - DeckFlow.Core.Tests/CategoryKnowledgeRepositoryTests.cs (full — fixture pattern: temp SQLite file, CreateRepository helper, SeedProcessedDeckAsync, existing Sol Ring + commander tests)
    - DeckFlow.Web.Tests/Integration/PostgresStorageTests.cs (lines 80-121 — CategoryKnowledgeRepository_CrudAndDeckQueue_Roundtrips: the ReplaceSourceRowsAsync → GetCategoriesAsync('Sol Ring') → GetCardDeckTotalsAsync round-trip this parity test mirrors)
    - DeckFlow.Core/Knowledge/CategoryKnowledgeRepository.cs (read methods GetCategoriesAsync, GetCategoryRowsForCommanderAsync — the parity targets)
    - .planning/phases/26-category-cache-schema-normalization-fresh-start/26-RESEARCH.md (Validation Architecture → Wave 0 Gaps; Pitfall 1)
  </read_first>
  <behavior>
    - SQLite AUTOINCREMENT harness: calling EnsureSchemaAsync on a fresh temp SQLite DB succeeds (no "table has more than one primary key" error), and inserting two fact rows yields two distinct surrogate ids while a duplicate natural-grain insert is rejected/upserted by the UNIQUE grain index. This confirms RESEARCH A1.
    - Parity — card categories: after writing a deck containing "Sol Ring" with category "Artifact" (and "Ramp"), GetCategoriesAsync("Sol Ring") returns the same category set the old design returned (CategoryFilter.IncludedOrFallback applied), order-stable.
    - Parity — commander aggregate: after writing one processed deck whose commander is a fixed name, GetCategoryRowsForCommanderAsync(commander) returns exactly one aggregate row per (category, card) with the correct deck_total (COUNT DISTINCT decks), proving the integer `q.id = o.deck_id` join replaces the old `'archidekt_live:'||deck_id` string-concat join.
    - Interning: writing the same card across two different deck sources produces ONE row in `cards` and two fact rows (one per deck_id), with GetCategoriesAsync still returning the single category set.
  </behavior>
  <action>
    Create DeckFlow.Core.Tests/CategoryCacheSchemaParityTests.cs (xUnit, mirror the temp-SQLite-file fixture + IDisposable pattern from CategoryKnowledgeRepositoryTests). Add the four behaviors above as `[Fact]` tests. Write these as the canonical parity guard:
    - `EnsureSchema_OnFreshSqlite_AllowsSurrogateIdPlusUniqueGrain` (A1 harness — calls EnsureSchemaAsync then asserts the schema is usable).
    - `GetCategoriesAsync_SolRing_ReturnsSameCategories` (fixed sample: write Sol Ring/Artifact via the write path; assert the returned category set).
    - `GetCategoryRowsForCommanderAsync_IntegerJoin_ReturnsAggregate` (write one processed deck + commander; assert one aggregate row, correct deck_total).
    - `SameCardAcrossTwoDecks_InternsToOneCardRow` (interning; assert one `cards` row via a small SELECT helper on the test connection).
    These tests EXERCISE the write path and read path. Because the write path is not yet ported to integer keys (26-02), they are EXPECTED TO FAIL (RED) at the end of this plan — that is the point. Mark intent with a class-level `///` comment noting RED-until-26-02. Do NOT weaken assertions to make them pass here. Use a fixed normalized expectation for Sol Ring so the assertion is byte-stable across the swap.
    If the WSL xUnit runner cooperates, capture the RED output; otherwise note that CI / 26-02 GREEN confirms.
  </action>
  <verify>
    <automated>"/mnt/c/Program Files/dotnet/dotnet.exe" build DeckFlow.Core.Tests/DeckFlow.Core.Tests.csproj -c Release</automated>
  </verify>
  <acceptance_criteria>
    - File DeckFlow.Core.Tests/CategoryCacheSchemaParityTests.cs exists with exactly 4 `[Fact]` methods (`grep -c '\[Fact\]'` == 4)
    - `grep -F 'Sol Ring' DeckFlow.Core.Tests/CategoryCacheSchemaParityTests.cs` matches the parity assertion
    - `grep -F 'GetCategoryRowsForCommanderAsync' DeckFlow.Core.Tests/CategoryCacheSchemaParityTests.cs` matches the integer-join test
    - Test project BUILDS clean (compiles against the new schema/methods)
    - These tests are RED (fail) when run now, because the write path still writes old-shape rows — documented as expected; 26-02 turns them GREEN. (If WSL runner is uncooperative, push-and-watch CI is the gate.)
  </acceptance_criteria>
  <done>Parity/interning/A1-harness test file compiles and encodes the fixed-sample expectations; tests are RED pending the 26-02 write/read port.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| harvest/admin → DB write path | card/commander names flow from upstream Archidekt into SQL |
| admin/user lookup → DB read path | card/commander names flow from request into SQL |
| ops → live Postgres (Step A wipe) | destructive full schema drop (runbook; user-run only) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-01 | Tampering (SQL injection) | new DDL + (26-02) read/write SQL | mitigate | All card/commander values bound via `RelationalDatabaseConnection.AddParameter`; DDL is constant raw-string with no user data interpolated; only `SurrogateIdColumnType` (a fixed dialect constant) is interpolated — never user input |
| T-26-02 | Tampering (dialect divergence masking bugs) | SQLite vs Postgres id/PK/index DDL | mitigate | One DDL string via `IRelationalDialect.SurrogateIdColumnType`; no `if (IsSqlite)` string branching; A1 confirmed by harness test (Task 3) |
| T-26-03 | Denial of Service (data wipe window) | full DB reset (runbook Step A) | accept | Empty-cache window explicitly authorized 2026-05-24; data is reset, not migrated; user runs the wipe (Claude/Codex must NOT). Reversible only via pre-wipe backup (runbook mandates backup first) |
| T-26-04 | Information Disclosure | content_hash reserve column | accept | Reserved NULL only; not a security digest (Phase 27 non-security content hash); no PII |
</threat_model>

<verification>
- `"/mnt/c/Program Files/dotnet/dotnet.exe" build DeckFlow.sln -c Release` is clean (0 new warnings / 0 errors).
- New DDL present and dual-dialect via `SurrogateIdColumnType` (no `if (IsSqlite)` DDL branching).
- `card_category_observations` table name preserved (reltuples fast-path intact).
- Parity test scaffold compiles; RED pending 26-02 (the parity-guard contract is in place).
- SQLite AUTOINCREMENT + UNIQUE-grain (A1) confirmed by the harness test (or queued for CI if WSL runner is flaky).
</verification>

<success_criteria>
- IRelationalDialect.SurrogateIdColumnType added on both dialects with the exact RESEARCH literals.
- EnsureSchemaAsync emits the normalized integer-keyed star schema (cards + extended deck_queue + slim facts + compact indexes) on a clean DB, idempotently.
- content_hash reserved (NULL) on deck_queue; not populated/read.
- No declared FK constraints; surrogate id PK + UNIQUE grain index per fact table.
- Parity/interning/A1 test file exists, compiles, and is RED pending 26-02.
</success_criteria>

<output>
Create `.planning/phases/26-category-cache-schema-normalization-fresh-start/26-01-SUMMARY.md` when done.
</output>
