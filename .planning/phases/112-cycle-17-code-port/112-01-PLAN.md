---
phase: 112-cycle-17-code-port
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/112-cycle-17-code-port/112-BASELINE.md
autonomous: true
requirements: [PORT-01, PORT-02]

must_haves:
  truths:
    - "The pre-port build warning count on feat/personal-tools is recorded, so ROADMAP success criterion 2 ('no new warnings') is a provable delta rather than an unfalsifiable claim."
    - "The pre-port test pass/fail counts for DeckFlow.Core.Tests and DeckFlow.Web.Tests are recorded from an ACTUAL run, not a static [Fact] attribute proxy (VALIDATION.md Wave 0 requirement 3)."
    - "Every M-file hunk target named in RESEARCH has been re-verified against current HEAD, so a post-research main drift cannot silently invalidate the port manifest (D-09)."
    - "plan/cycle-17-creator-style is confirmed readable at 6da5eb42 and all 102 allowlisted source paths exist on it, so no checkout in a later plan can half-fail."
  artifacts:
    - path: ".planning/phases/112-cycle-17-code-port/112-BASELINE.md"
      provides: "Recorded pre-port build/test baseline plus M-file drift verdict"
      contains: "Build warnings:"
  key_links:
    - from: ".planning/phases/112-cycle-17-code-port/112-BASELINE.md"
      to: "112-03-PLAN.md and 112-06-PLAN.md gate tasks"
      via: "warning-count delta comparison"
      pattern: "Build warnings"
---

<objective>
Capture the pre-port baseline and re-verify the RESEARCH port manifest against current HEAD, BEFORE any Cycle 17 code lands.

Purpose: ROADMAP success criterion 2 is "no NEW errors and no NEW warnings" — a delta. Without a recorded "before" number, the criterion cannot be proven. RESEARCH also carries an explicit expiry ("re-verify the M-file diffs against HEAD if more than ~3 days have elapsed"), and three of its M-file targets (`Program.cs`, `PacketSessionCache.cs`, `DeckFlowDatabaseConnectionFactory.cs`) are the most-contested files in the repo.

Output: `.planning/phases/112-cycle-17-code-port/112-BASELINE.md` — the recorded baseline and a GO / NO-GO drift verdict that plans 02-06 gate against. No production code is touched by this plan.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/112-cycle-17-code-port/112-RESEARCH.md
@.planning/phases/112-cycle-17-code-port/112-CONTEXT.md
@.planning/phases/112-cycle-17-code-port/112-VALIDATION.md
@CLAUDE.md
</context>

<environment>
- dotnet is the Windows binary invoked from WSL: `DOTNET="/mnt/c/Program Files/dotnet/dotnet.exe"`. Quote the path; it contains a space.
- Do NOT set `MTG_DATA_DIR`.
- VSTest is unreliable under WSL. `dotnet build` is the reliable gate; `dotnet test` runs go through the Windows dotnet above.
- Current branch is `feat/personal-tools`. Do not switch branches in this plan.
- `plan/cycle-17-creator-style` (head `6da5eb42`) is READ-ONLY. Read it only with `git show <ref>:<path>` and `git diff`/`git cat-file`. Never `git checkout` it as a working branch, never rebase it, never merge it.
</environment>

<tasks>

<task type="auto">
  <name>Task 1: Record the pre-port build and test baseline</name>
  <files>.planning/phases/112-cycle-17-code-port/112-BASELINE.md</files>
  <read_first>
    - `.planning/phases/112-cycle-17-code-port/112-VALIDATION.md` §"Baseline (captured on feat/personal-tools BEFORE the port)" and §"Wave 0 Requirements" — item 3 is exactly this task.
    - `.planning/phases/112-cycle-17-code-port/112-RESEARCH.md` §"Current green baseline (feat/personal-tools, BEFORE this port — captured this session)" — states 0 errors / 9 warnings (all `CS8629` in `DeckFlow.Core.Tests/Manabase/ManabaseBaselineWeightingTests.cs`), and flags the 1334/1576 test figures as a static attribute-count PROXY with MEDIUM confidence.
    - `CLAUDE.md` §Constraints (testing / WSL caveats).
  </read_first>
  <action>
    Confirm the working tree is clean (`git status --porcelain` empty apart from untracked `.planning`/`.foreman` noise) and HEAD is on `feat/personal-tools`. If tracked production files are dirty, STOP and report — a baseline captured over uncommitted work is worthless.

    Run a full solution build with the Windows dotnet and capture the complete output to a scratch file:
    `"/mnt/c/Program Files/dotnet/dotnet.exe" build DeckFlow.sln` (no `MTG_DATA_DIR`).
    Extract: the error count, the warning count, and the distinct warning IDs with the file each occurs in.

    Run both test projects to completion with the Windows dotnet and capture the summary lines:
    `"/mnt/c/Program Files/dotnet/dotnet.exe" test DeckFlow.Core.Tests/DeckFlow.Core.Tests.csproj`
    `"/mnt/c/Program Files/dotnet/dotnet.exe" test DeckFlow.Web.Tests/DeckFlow.Web.Tests.csproj`
    Record the actual `Passed!`/`Failed!` line for each: total, passed, failed, skipped, and duration. These replace RESEARCH's static `[Fact]`/`[Theory]` proxy counts (1334 Core / 1576 Web) as the authoritative "before" numbers.

    If either suite has PRE-EXISTING failures, record them by fully-qualified test name in the baseline document and mark them as pre-existing. Do NOT attempt to fix them — they are not this phase's scope, but they must be enumerated so a post-port failure list can be diffed against them.

    Write `.planning/phases/112-cycle-17-code-port/112-BASELINE.md` with LF line endings and these sections: `## Build baseline` (commit SHA of HEAD, error count, warning count, warning-ID/file table), `## Test baseline` (per-project total/passed/failed/skipped, plus the enumerated pre-existing failures if any), and `## Capture commands` (the exact commands run, verbatim, so a later plan re-runs the identical command).
  </action>
  <verify>
    <automated>test -f .planning/phases/112-cycle-17-code-port/112-BASELINE.md &amp;&amp; grep -q '## Build baseline' .planning/phases/112-cycle-17-code-port/112-BASELINE.md &amp;&amp; grep -q '## Test baseline' .planning/phases/112-cycle-17-code-port/112-BASELINE.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/112-cycle-17-code-port/112-BASELINE.md` exists and contains the literal headings `## Build baseline`, `## Test baseline`, and `## Capture commands`.
    - The build baseline section records an explicit numeric error count and warning count, and the HEAD commit SHA the numbers were captured at.
    - The recorded error count is 0. If it is not 0, the plan STOPS and reports — the branch is not green and the port cannot begin.
    - The recorded warning count is a concrete integer (RESEARCH observed 9; a different number is acceptable and becomes the new authoritative baseline, but the deviation from 9 must be noted in the document).
    - The test baseline section records, for BOTH `DeckFlow.Core.Tests` and `DeckFlow.Web.Tests`, an actual runner summary (total / passed / failed / skipped) — not a `grep -c '\[Fact\]'` proxy.
    - Any pre-existing test failure is listed by fully-qualified name under an explicit "pre-existing failures" heading.
    - The file uses LF line endings (`file` reports no CRLF, or `grep -c $'\r'` returns 0).
  </acceptance_criteria>
  <done>The authoritative pre-port build and test numbers are committed to a document that plans 03 and 06 compare against.</done>
</task>

<task type="auto">
  <name>Task 2: Re-verify the RESEARCH port manifest against current HEAD (drift preflight)</name>
  <files>.planning/phases/112-cycle-17-code-port/112-BASELINE.md</files>
  <read_first>
    - `.planning/phases/112-cycle-17-code-port/112-RESEARCH.md` §"M-File Hunk Inventory" (the 13-row table naming every file that takes a hand-applied hunk), §"Critical Findings" #5 (the three files where a wholesale copy silently deletes main-only members), §"Contamination Audit" (the 37 files already on main that must NOT be ported), and §"Metadata" → "Valid until" (the explicit expiry condition this task discharges).
    - `.planning/phases/112-cycle-17-code-port/112-CONTEXT.md` D-05, D-08, D-09.
    - Current content of each drift-check anchor via `git show HEAD:<path>`, specifically: `DeckFlow.Web/Program.cs`, `DeckFlow.Web/Services/PacketSessionCache.cs`, `DeckFlow.Web/Services/Persistence/DeckFlowDatabaseConnectionFactory.cs`, `DeckFlow.Core/Integration/ILlmDistillationService.cs`, `DeckFlow.Core/Knowledge/DistillationResults.cs`, `DeckFlow.Web/Services/Http/ResiliencePipelineFactory.cs`.
  </read_first>
  <action>
    Append a `## Drift preflight` section to `112-BASELINE.md` recording the results of the four checks below. This task changes no code — it produces a GO / NO-GO verdict.

    Check A — port source is readable. Confirm `git rev-parse plan/cycle-17-creator-style` resolves to `6da5eb420b6403b68804bdbd3f2e51d7213ab33c` and `git rev-parse 5709f37c` resolves. Record both SHAs.

    Check B — every allowlisted source path exists on the port branch. For the 64 Commit-1 paths listed in `112-02-PLAN.md` §port_allowlist, the 36 Commit-2 paths plus 2 seed files listed in `112-04-PLAN.md` §port_allowlist, verify each with `git cat-file -e plan/cycle-17-creator-style:<path>`. Record the count verified (expected 102) and enumerate any that fail.

    Check C — M-file drift since the research session. For each of the 13 M-file targets in RESEARCH's hunk-inventory table, run `git log --oneline 8599cd3b..HEAD -- <path>` (i.e. anything landing after the research commit) and record whether the file changed. Independently confirm the specific main-only members RESEARCH warns about are STILL present on HEAD: `CreateManabaseBaselineConnection` in `DeckFlowDatabaseConnectionFactory.cs`, `ExtractCombinedAsync` in `ILlmDistillationService.cs`, `CombinedExtractionResult` in `DistillationResults.cs`. Confirm the three insertion anchors are still where the plans expect: the line `builder.Services.AddDeckFlowScryfallServices();` in `Program.cs`, the sequential pair `EnsureSchemaAsync()` / `GetRequiredService<IContentKbSeedLoader>().LoadIfPresentAsync()` in `Program.cs`, and the five existing `AddResiliencePipeline<string, RestResponse>(services, "<name>", ...)` entries in `ResiliencePipelineFactory.cs`.

    Check D — contamination set is still on main. Spot-verify that the RESEARCH "already PRESENT on main" list is still accurate for the two load-bearing entries: `DeckFlow.Core/Content/ContentKbPaths.cs` exists on HEAD and does NOT yet contain `CreatorStyleProfileSeedRelativePath`; `DeckFlow.Web/Services/Content/WebSeedKeyMembershipSource.cs` exists on HEAD. Also verify the two seed placeholders do NOT already exist on HEAD (`content-kb/seed/creator-style-profiles.json`, `content-kb/seed/creator-deck-cache.json`) — if either exists, the port would overwrite it.

    Write the verdict as the last line of the section: either `VERDICT: GO — manifest matches HEAD` or `VERDICT: NO-GO — <specific drift>`. On NO-GO, STOP the phase and report the exact drift to the orchestrator; do not proceed to plan 02 with a stale manifest.
  </action>
  <verify>
    <automated>grep -qE '^VERDICT: (GO|NO-GO)' .planning/phases/112-cycle-17-code-port/112-BASELINE.md</automated>
  </verify>
  <acceptance_criteria>
    - `112-BASELINE.md` contains a `## Drift preflight` section with subsections for Checks A, B, C, and D.
    - Check A records `plan/cycle-17-creator-style` = `6da5eb420b6403b68804bdbd3f2e51d7213ab33c`. A different SHA is a NO-GO.
    - Check B records the number of allowlisted paths confirmed present on the port branch; the expected value is 102. Any missing path is enumerated by name.
    - Check C records, for each of the 13 M-file targets, whether it changed after commit `8599cd3b`, AND explicitly confirms the strings `CreateManabaseBaselineConnection`, `ExtractCombinedAsync`, and `CombinedExtractionResult` are each present in HEAD's copy of their respective files.
    - Check D records that `grep -c 'CreatorStyleProfileSeedRelativePath' DeckFlow.Core/Content/ContentKbPaths.cs` returns 0, and that neither `content-kb/seed/creator-style-profiles.json` nor `content-kb/seed/creator-deck-cache.json` exists on HEAD.
    - The section ends with a line starting `VERDICT: GO` or `VERDICT: NO-GO`.
    - `git diff --name-status main` at the end of this plan lists ONLY paths under `.planning/` — zero production files touched.
  </acceptance_criteria>
  <done>The RESEARCH manifest is confirmed valid against current HEAD, or the phase is halted with a named drift.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| historical git ref → working tree | Code authored 777 commits ago crosses into the shipping branch; content is not re-reviewed line by line during a port |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-112-01 | Tampering | Port manifest staleness — RESEARCH's file lists could have been invalidated by commits landing after `8599cd3b` | mitigate | Task 2 Check C re-runs the drift detection against current HEAD and halts on any change to a hunk target |
| T-112-02 | Repudiation | Unprovable "no new warnings" claim (ROADMAP criterion 2 is a delta with no recorded "before") | mitigate | Task 1 records the exact pre-port error/warning counts and the HEAD SHA they were captured at, in a committed artifact |
| T-112-SC | Tampering | Package-manager installs (NuGet) | mitigate | No `PackageReference` is added anywhere in Phase 112. RESEARCH verified zero csproj package diff between merge-base and branch head. `Testcontainers.PostgreSql` is explicitly forbidden (D-15). Plans 03 and 06 assert this with a grep gate before each commit. |
</threat_model>

<verification>
- `git status --porcelain` shows no modified tracked production files attributable to this plan.
- `112-BASELINE.md` exists with `## Build baseline`, `## Test baseline`, `## Capture commands`, `## Drift preflight` and a `VERDICT:` line.
- Recorded build error count is 0.
</verification>

<success_criteria>
- Pre-port build warning count and per-project test pass counts are recorded from actual command output.
- All 13 M-file hunk targets are drift-checked against HEAD and the three main-only members RESEARCH warns about are confirmed still present.
- All 102 allowlisted source paths are confirmed to exist on `plan/cycle-17-creator-style`.
- A `VERDICT: GO` line exists, or the phase is halted with a named drift.
</success_criteria>

<output>
Create `.planning/phases/112-cycle-17-code-port/112-01-SUMMARY.md` when done, quoting the recorded warning count, the two test-suite summary lines, and the drift verdict.
</output>

