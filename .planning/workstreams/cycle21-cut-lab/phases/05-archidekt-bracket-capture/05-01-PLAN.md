---
phase: 05-archidekt-bracket-capture
phase_number: "05"
phase_name: Archidekt Bracket Capture
plan: 01
type: execute
status: planned
generated: 2026-07-29
wave: 1
depends_on: []
dependencies: []
files_modified:
  - DeckFlow.Core/Integration/DeckImporterInterfaces.cs
  - DeckFlow.Core/Integration/ArchidektApiDeckImporter.cs
  - DeckFlow.Core.Tests/ArchidektApiDeckImporterTests.cs
autonomous: true
requirements: [BRKT-01]
requirement_ids: [BRKT-01]

must_haves:
  truths:
    - "Archidekt deck-level metadata is parsed from the same deck payload request that already returns cards[] (BRKT-01, D-01, D-02)."
    - "Existing ImportAsync callers still receive List<DeckEntry> without signature churn (D-07)."
    - "Malformed or missing metadata fields become null values while a successfully parsed payload still receives a captured timestamp (D-03)."
  artifacts:
    - path: "DeckFlow.Core/Integration/DeckImporterInterfaces.cs"
      provides: "ArchidektDeckMetadata, ArchidektDeckImportResult, and ImportWithMetadataAsync contract"
      contains: "ArchidektDeckMetadata"
    - path: "DeckFlow.Core/Integration/ArchidektApiDeckImporter.cs"
      provides: "Same-request Archidekt metadata parser"
      contains: "ImportWithMetadataAsync"
    - path: "DeckFlow.Core.Tests/ArchidektApiDeckImporterTests.cs"
      provides: "Importer compatibility and metadata parsing coverage"
  key_links:
    - from: "DeckFlow.Core/Integration/ArchidektApiDeckImporter.cs"
      to: "DeckFlow.Core/Integration/DeckImporterInterfaces.cs"
      via: "IArchidektDeckImporter.ImportWithMetadataAsync"
      pattern: "ImportWithMetadataAsync"
---

<objective>
Create the Archidekt metadata import contract and implementation: a metadata-bearing result that parses curated top-level deck metadata from the already-fetched Archidekt payload while preserving the existing `ImportAsync -> List<DeckEntry>` compatibility surface.

Purpose: BRKT-01 and D-07 require deck-level metadata without a second Archidekt request and without overloading card-level `DeckEntry` rows.
Output: importer contract/model changes and focused importer tests.
</objective>

<execution_context>
@/home/clunt/.codex/get-shit-done/workflows/execute-plan.md
@/home/clunt/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/cycle21-cut-lab/ROADMAP.md
@.planning/workstreams/cycle21-cut-lab/REQUIREMENTS.md
@.planning/workstreams/cycle21-cut-lab/PROJECT.md
@.planning/workstreams/cycle21-cut-lab/phases/05-archidekt-bracket-capture/05-CONTEXT.md
@.planning/workstreams/cycle21-cut-lab/phases/05-archidekt-bracket-capture/05-RESEARCH.md
@.planning/workstreams/cycle21-cut-lab/phases/05-archidekt-bracket-capture/05-PATTERNS.md
@.planning/workstreams/cycle21-cut-lab/phases/05-archidekt-bracket-capture/05-VALIDATION.md

<interfaces>
From `DeckFlow.Core/Integration/DeckImporterInterfaces.cs`:
- `public sealed record MoxfieldImportResult(List<DeckEntry> Entries, MoxfieldImportSource Source, string? FallbackNotice = null, string? DetectedCompanionName = null);`
- `IMoxfieldDeckImporter.ImportWithSourceAsync(...)` is the compatibility-wrapper pattern to mirror.
- `IArchidektDeckImporter.ImportAsync(string urlOrDeckId, CancellationToken cancellationToken = default)` currently returns `Task<List<DeckEntry>>` and must keep doing so.

From `DeckFlow.Core/Integration/ArchidektApiDeckImporter.cs`:
- `ImportAsync` calls `CreateDeckRequest(deckId)` once, parses one `JsonDocument`, then walks `root.GetProperty("cards")`.
- Preserve `TryGetProperty` / `JsonValueKind` guarded parsing and the current error handling for invalid deck ids and non-success responses.
</interfaces>

<source_audit>
GOAL: Phase 5 captures Archidekt bracket metadata prospectively from harvest payloads. Covered by this plan for parsing, then plans 05-02 and 05-03 for persistence and propagation.
REQ: BRKT-01 covered here.
RESEARCH: Compatibility-wrapper pattern, curated fields, same-request parsing, no second request, malformed values as null. Covered here.
CONTEXT: D-01, D-02, D-03, D-07 covered here; D-04, D-05, D-06, D-08 covered in dependent plans. Deferred raw JSON, backfill, commander x bracket derivation, and UI are not planned.
</source_audit>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing importer tests for same-request metadata capture</name>
  <read_first>
    - `DeckFlow.Core.Tests/ArchidektApiDeckImporterTests.cs` — fixture-backed RestClient test pattern.
    - `DeckFlow.Core.Tests/Fixtures/archidekt-background-companion.json` — existing Archidekt payload fixture.
    - `.planning/workstreams/cycle21-cut-lab/phases/05-archidekt-bracket-capture/05-CONTEXT.md` D-01, D-02, D-03, D-07.
  </read_first>
  <files>DeckFlow.Core.Tests/ArchidektApiDeckImporterTests.cs</files>
  <behavior>
    - Test 1: `ImportWithMetadataAsync` returns the same entries as `ImportAsync` and exposes metadata parsed from the same fixture-backed response.
    - Test 2: numeric and numeric-string `edhBracket` / `deckFormat` values parse to nullable integers; missing, null, object, or malformed values parse to null without throwing.
    - Test 3: successful JSON parsing always sets `ArchidektDeckMetadata.CapturedUtc` even when `EdhBracket` is null, proving D-03 captured-absent semantics begin at import time.
    - Test 4: the test message handler records exactly one HTTP request for `ImportWithMetadataAsync`, proving BRKT-01 no-extra-request behavior.
  </behavior>
  <action>
Extend `ArchidektApiDeckImporterTests` before production code changes. Reuse `CreateImporterReturningFixture` and its `FixtureMessageHandler`; add a small counting handler or augment the existing handler so a test can assert one HTTP request. Keep the existing `ImportAsync_BackgroundFixture_*` tests unchanged.

Use the existing fixture for preserved entry behavior. If the fixture has `edhBracket: null`, clone its JSON inside the test with `JsonNode` or string replacement only inside the test body to add representative values for `edhBracket`, `deckFormat`, `theorycrafted`, `createdAt`, and `updatedAt`. Do not add a production fixture unless the test becomes unreadable.

The tests should call the planned `ImportWithMetadataAsync` method and fail to compile or fail red before Task 2 adds the contract. Do not weaken assertions to accommodate the current `ImportAsync` surface.
  </action>
  <verify>
    <automated>dotnet.exe test DeckFlow.Core.Tests/DeckFlow.Core.Tests.csproj --filter FullyQualifiedName~ArchidektApiDeckImporterTests --no-restore</automated>
  </verify>
  <done>Importer tests describe metadata-bearing import behavior, request count, null/malformed handling, captured timestamp semantics, and compatibility with current entry parsing.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement Archidekt metadata result and parser</name>
  <read_first>
    - `DeckFlow.Core/Integration/DeckImporterInterfaces.cs` — rich result and default wrapper pattern.
    - `DeckFlow.Core/Integration/ArchidektApiDeckImporter.cs` — current one-request parser.
    - `.planning/workstreams/cycle21-cut-lab/phases/05-archidekt-bracket-capture/05-PATTERNS.md` Pattern 1 and Pattern 2.
  </read_first>
  <files>DeckFlow.Core/Integration/DeckImporterInterfaces.cs, DeckFlow.Core/Integration/ArchidektApiDeckImporter.cs</files>
  <action>
Add `public sealed record ArchidektDeckMetadata(int? EdhBracket, int? DeckFormat, bool? Theorycrafted, DateTimeOffset? CreatedUtc, DateTimeOffset? UpdatedUtc, DateTimeOffset CapturedUtc);` and `public sealed record ArchidektDeckImportResult(List<DeckEntry> Entries, ArchidektDeckMetadata Metadata);` near the importer contracts. This is the curated deck-level shape per D-01; do not include raw JSON, `viewCount`, or `points` unless a later user decision adds a corpus-hygiene reason.

Extend `IArchidektDeckImporter` with `ImportWithMetadataAsync(string urlOrDeckId, CancellationToken cancellationToken = default)` without fabricating metadata in the interface default. Either require implementations/fakes to provide the metadata-bearing method directly, or make any compatibility default throw `NotSupportedException` so harvest paths cannot mark metadata as captured without parsing the Archidekt payload. Implement the method in `ArchidektApiDeckImporter` so real Archidekt imports parse metadata from the same `JsonDocument` used for `cards[]`. Preserve `ImportAsync` by making it call `ImportWithMetadataAsync(...).Entries`.

Parse only top-level `edhBracket`, `deckFormat`, `theorycrafted`, `createdAt`, and `updatedAt`. Accept JSON numbers and numeric strings for `edhBracket` and `deckFormat`; accept JSON booleans and boolean strings for `theorycrafted`; parse timestamps with `DateTimeOffset.TryParse` and normalize stored values later in the repository. Missing, null, malformed, array, or object values become null. Do not infer bracket locally per D-02.
  </action>
  <verify>
    <automated>dotnet.exe test DeckFlow.Core.Tests/DeckFlow.Core.Tests.csproj --filter FullyQualifiedName~ArchidektApiDeckImporterTests --no-restore</automated>
  </verify>
  <done>`ImportWithMetadataAsync` passes the red tests, `ImportAsync` remains source-compatible, and the production Archidekt importer still performs one deck payload request.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Archidekt API -> importer | Untrusted JSON crosses into DeckFlow metadata parsing. |
| Importer contract -> storage plans | A nullable metadata record becomes the source of truth for later persistence. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | `ArchidektApiDeckImporter` metadata parser | mitigate | Guard every top-level field with `TryGetProperty` and `JsonValueKind`; malformed values become null, never inferred values. |
| T-05-02 | Information Disclosure | metadata scope | mitigate | Persist curated fields only per D-01; no raw payload JSON, `viewCount`, or arbitrary top-level fields. |
| T-05-03 | Repudiation | captured vs absent provenance | mitigate | Tests assert `CapturedUtc` is set for successfully parsed payloads even when `EdhBracket` is null. |
| T-05-SC | Tampering | package installs | accept | No package-manager install is planned. |
</threat_model>

<verification>
Run `dotnet.exe test DeckFlow.Core.Tests/DeckFlow.Core.Tests.csproj --filter FullyQualifiedName~ArchidektApiDeckImporterTests --no-restore`.
</verification>

<success_criteria>
- BRKT-01 is satisfied at the importer layer: no extra request per deck, metadata parsed from the existing payload.
- D-01, D-02, D-03, and D-07 are implemented in the contract and importer behavior.
- No source caller is forced off `ImportAsync -> List<DeckEntry>`.
</success_criteria>

<output>
Create `.planning/workstreams/cycle21-cut-lab/phases/05-archidekt-bracket-capture/05-01-SUMMARY.md` when done.
</output>
