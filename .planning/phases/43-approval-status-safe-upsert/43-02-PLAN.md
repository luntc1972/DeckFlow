---
phase: 43-approval-status-safe-upsert
plan: 02
type: execute
wave: 2
depends_on: ["43-01"]
files_modified:
  - DeckFlow.Core.Tests/Content/ContentSiteIndexStoreApprovalTests.cs
autonomous: true
requirements: [REVQ-01, PUB-01, PUB-02]

must_haves:
  truths:
    - "An integration test proves the self-healing migration adds approval_status to a legacy schema"
    - "An integration test proves the grandfather backfill: visible legacy row -> 'approved', invisible legacy row -> 'pending'"
    - "An integration test proves the grandfather backfill is idempotent / does NOT re-stamp operator-changed statuses: after migrating, manually change one row's approval_status, run EnsureSchemaAsync again on a FRESH store instance, and the changed status is preserved (not re-stamped)"
    - "An integration test proves UpsertContentColumnsOnlyAsync inserts a new row as 'pending'"
    - "An integration test proves UpsertContentColumnsOnlyAsync preserves all four admin fields on re-upsert with NON-DEFAULT values (is_visible=true, is_hidden=true, is_evergreen=true, approval_status='approved')"
    - "An integration test proves GetApprovedRowsAsync returns only approved rows"
    - "A reflection test proves both DDL constants declare approval_status TEXT NOT NULL DEFAULT 'pending'"
  artifacts:
    - path: "DeckFlow.Core.Tests/Content/ContentSiteIndexStoreApprovalTests.cs"
      provides: "Real-SQLite integration tests for the approval_status migration, grandfather idempotency, safe upsert (all four admin fields at non-default values), and approved-only filter"
      contains: "ContentSiteIndexStoreApprovalTests"
      min_lines: 220
  key_links:
    - from: "ContentSiteIndexStoreApprovalTests"
      to: "ContentSiteIndexStore (real SQLite)"
      via: "per-fact temp .db file constructed like ContentSiteIndexStoreVisibilityTests"
      pattern: "new ContentSiteIndexStore"
---

<objective>
Add `DeckFlow.Core.Tests/Content/ContentSiteIndexStoreApprovalTests.cs` — real-SQLite integration tests that lock in the Plan 01 behavior: the self-healing migration + grandfather backfill, grandfather idempotency / no-restamp, the safe content-only upsert (preserves all four admin fields at non-default values, new rows land pending), the approved-only filtered read, and the DDL default. Mirrors the existing `ContentSiteIndexStoreVisibilityTests` pattern exactly.

Purpose: These tests are the goal-backward proof for SC1-SC4 and the security mitigations (T-43-02 grandfather idempotency, T-43-03 export filter). Without them the data-layer change is unverifiable.

Output: One new xUnit test class (DeckFlow.Core.Tests). No production code changes. No new packages, no new test framework. (NOTE: the wave-1 compile-fix stubs in the existing test doubles are added by Plan 01, not here — see 43-01 Task 3. This plan only adds the new integration suite.)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/43-approval-status-safe-upsert/43-CONTEXT.md
@.planning/phases/43-approval-status-safe-upsert/43-PATTERNS.md

<interfaces>
<!-- Methods under test (added in Plan 01). -->
IContentSiteIndexStore:
  Task UpsertContentColumnsOnlyAsync(ContentSiteIndexRow row, CancellationToken ct = default);
  Task<IReadOnlyList<ContentSiteIndexRow>> GetApprovedRowsAsync(CancellationToken ct = default);
  // existing: EnsureSchemaAsync, GetByNaturalKeyAsync, SetVisibilityAsync, SetEvergreenAsync, SetHiddenAsync, GetAllRowsAsync

ContentSiteIndexRow.ApprovalStatus (string, defaults "pending") — added in Plan 01.

ContentSourceType.Youtube = "youtube_channel" (DeckFlow.Core/Knowledge/ContentModels.cs:183).
DDL CHECK allows only natural_key_type IN ('youtube_channel','podcast_rss').

Test seam (mirror DeckFlow.Core.Tests/Content/ContentSiteIndexStoreVisibilityTests.cs):
  - per-fact temp db: Path.Combine(Path.GetTempPath(), $"...-{Guid.NewGuid():N}.db")
  - Dispose(): SqliteConnection.ClearAllPools(); GC.Collect(); GC.WaitForPendingFinalizers(); File.Delete
  - namespace DeckFlow.Core.Tests; (NOT .Content)
  - ColumnExistsAsync(string) via PRAGMA table_info
  - GetPrivateSql(string fieldName) via reflection (BindingFlags.NonPublic | Static) for DDL-constant assertions
  - CreateYoutubeRow(...) static factory (copy from the visibility test class)
  - RelationalDatabaseConnection.FromSqlitePath(path).OpenConnectionAsync() for direct SQL setup
  - A FRESH store instance is `new ContentSiteIndexStore(_dbPath)` against the SAME _dbPath (the no-restamp test needs a second store object to force EnsureSchemaAsync to re-run its schema check)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write ContentSiteIndexStoreApprovalTests integration suite</name>
  <read_first>
    - DeckFlow.Core.Tests/Content/ContentSiteIndexStoreVisibilityTests.cs (entire file — ctor/Dispose @14-33, legacy-schema helper pattern @166-274, ColumnExistsAsync @277-293, GetPrivateSql @296-301, CreateYoutubeRow @303-323)
    - DeckFlow.Core/Content/ContentSiteIndexStore.cs (SqliteCreateTableSql + PostgresCreateTableSql field names for the reflection test; UpsertContentColumnsOnlySql; GetApprovedRowsAsync) — to know exact constant names
    - .planning/phases/43-approval-status-safe-upsert/43-PATTERNS.md (section "Test fixture pattern" + the required tests, lines 337-413)
  </read_first>
  <action>
    Create `DeckFlow.Core.Tests/Content/ContentSiteIndexStoreApprovalTests.cs`, `namespace DeckFlow.Core.Tests;`, `public sealed class ContentSiteIndexStoreApprovalTests : IDisposable`. Copy the ctor/Dispose/temp-db pattern and the private helpers (`ColumnExistsAsync`, `GetPrivateSql`, `CreateYoutubeRow`) from `ContentSiteIndexStoreVisibilityTests` (the new class needs its own copies; do not refactor the existing class). Add a private `CreateLegacySchemaAsync()` helper that builds the OLD DDL by hand WITHOUT the approval_status column (copy the SqliteCreateTableSql column list but drop the approval_status line) and inserts seed rows via direct parameterized SQL through `RelationalDatabaseConnection.FromSqlitePath(_dbPath)`.

    Write these EIGHT `[Fact]` tests (each `async Task`, use `Assert` constraint style consistent with the visibility test):
    1. `EnsureSchemaAsync_AddsApprovalStatusColumn_ToLegacySchema` — build legacy schema (no approval_status), `await _store.EnsureSchemaAsync()`, assert `await ColumnExistsAsync("approval_status")` is true.
    2. `EnsureSchemaAsync_Grandfather_SetsApprovedForVisibleRows_PendingForOthers` — into the legacy schema insert one row with is_visible=1 and one with is_visible=0, run `EnsureSchemaAsync`, then read both via `GetAllRowsAsync` (or `GetByNaturalKeyAsync`) and assert the visible row's `ApprovalStatus == "approved"` and the invisible row's `ApprovalStatus == "pending"`.
    3. `EnsureSchemaAsync_Grandfather_DoesNotRestampOperatorChangedStatus` (MED-3 — REQUIRED, not optional) — build legacy schema with one visible row, run `EnsureSchemaAsync` (grandfathers it to 'approved'), then via direct UPDATE SQL set that row's approval_status to 'rejected' (simulating an operator decision), then construct a FRESH store: `var store2 = new ContentSiteIndexStore(_dbPath);` and call `await store2.EnsureSchemaAsync()` AGAIN. Re-read the row and assert `ApprovalStatus == "rejected"` (NOT re-stamped back to 'approved'). This proves the self-guarded backfill's `WHERE approval_status='pending'` clause never overwrites an operator-changed status across passes.
    4. `UpsertContentColumnsOnlyAsync_NewRow_LandsAsPending` — `await _store.UpsertContentColumnsOnlyAsync(CreateYoutubeRow("yt-new"))`, fetch via `GetByNaturalKeyAsync(ContentSourceType.Youtube, "yt-new")`, assert `ApprovalStatus == "pending"`.
    5. `UpsertContentColumnsOnlyAsync_ExistingRow_PreservesApprovalStatus` — upsert a row, set its status to 'approved' via direct UPDATE SQL, re-upsert the same natural key with a CHANGED title, assert `ApprovalStatus` is still `"approved"` AND `Title` reflects the new title (content updated, admin field preserved — D-04).
    6. `UpsertContentColumnsOnlyAsync_PreservesAllAdminFields` (MED-4 — STRENGTHENED) — upsert a row; set ALL FOUR admin fields to NON-DEFAULT values: is_visible=true (`SetVisibilityAsync`), is_hidden=TRUE (`SetHiddenAsync` — must be set to true, NOT left at default false), is_evergreen=true (`SetEvergreenAsync`), and approval_status='approved' (direct SQL). NOTE the SetVisibility/SetHidden interplay (`SetVisibilityAsync` may reset is_hidden to false per the existing store semantics) — order the calls so the final pre-upsert state has is_visible=true AND is_hidden=true (set hidden LAST, or set hidden via direct SQL after visibility, whichever the store's semantics require — read SetVisibilityAsync/SetHiddenAsync first and order accordingly). Then re-upsert the same natural key with changed content; fetch and assert all four preserved at their non-default values: `IsVisible==true`, `IsHidden==true`, `IsEvergreen==true`, `ApprovalStatus=="approved"` (D-03). If the store semantics make is_visible=true and is_hidden=true mutually exclusive, instead split into the existing visible/evergreen/approved case PLUS a dedicated `UpsertContentColumnsOnlyAsync_PreservesHiddenRow` fact that sets is_hidden=TRUE (and is_visible=false) and asserts is_hidden stays TRUE after re-upsert — so is_hidden=TRUE is proven preserved either way.
    7. `GetApprovedRowsAsync_ReturnsOnlyApprovedRows` — upsert three distinct-natural-key rows; set them to 'approved', 'pending', 'rejected' respectively via direct SQL; call `GetApprovedRowsAsync`; assert exactly one row returned and its natural key matches the approved one (T-43-03 proof). Also assert the returned row's `ApprovalStatus == "approved"` (proves GetApprovedRowsAsync's SELECT populates ApprovalStatus at ordinal 15 — MED-2 read-side proof).
    8. `CreateTableDdl_IncludesApprovalStatusDefault` — use `GetPrivateSql("SqliteCreateTableSql")` and `GetPrivateSql("PostgresCreateTableSql")` and assert both `Contains("approval_status TEXT NOT NULL DEFAULT 'pending'")`.

    (If MED-4 forces the split into two facts as noted in test 6, that is NINE facts total — acceptable; the grep gate below uses `>= 8`.)

    All direct-SQL setup MUST use parameterized commands (`RelationalDatabaseConnection.AddParameter`) for any row values; the only inline literals allowed are the fixed status strings. Use `.ConfigureAwait(false)` consistent with the visibility test. For the Postgres column-presence case: do NOT attempt a live PG connection (no PG harness in CI); add a class-level XML-doc note stating PG migration coverage is deferred (CONTEXT deferred item + Discretion). Do not add a `[Fact(Skip=...)]` stub unless it documents that gap clearly.
  </action>
  <verify>
    <automated>"/mnt/c/Program Files/dotnet/dotnet.exe" build DeckFlow.Core.Tests/DeckFlow.Core.Tests.csproj 2>&1 | grep -E "Build succeeded|error|warning" | head</automated>
  </verify>
  <acceptance_criteria>
    - File `DeckFlow.Core.Tests/Content/ContentSiteIndexStoreApprovalTests.cs` exists, namespace `DeckFlow.Core.Tests`, class `public sealed class ContentSiteIndexStoreApprovalTests : IDisposable`.
    - At least EIGHT `[Fact]` methods present including the no-restamp idempotency test and the strengthened all-admin-fields (non-default) preservation test (verify: `grep -c "\[Fact\]" DeckFlow.Core.Tests/Content/ContentSiteIndexStoreApprovalTests.cs` >= 8).
    - A no-restamp test exists that constructs a SECOND `new ContentSiteIndexStore(_dbPath)` and calls `EnsureSchemaAsync` again after an operator status change, asserting the changed status is preserved (verify: `grep -c "new ContentSiteIndexStore" ...ApprovalTests.cs` >= 2).
    - The preservation test sets is_hidden to TRUE (non-default) and asserts it stays TRUE after re-upsert (verify the file references `SetHiddenAsync` or a direct is_hidden=TRUE setup and an `IsHidden` true assertion).
    - The file references the production methods under test: `grep -c "UpsertContentColumnsOnlyAsync\|GetApprovedRowsAsync" DeckFlow.Core.Tests/Content/ContentSiteIndexStoreApprovalTests.cs` >= 5.
    - `DeckFlow.Core.Tests` builds with 0 errors and 0 new warnings.
    - Targeted run passes (see verification block — run the class by filter; if VSTest is unreliable in WSL, capture the clean build + run via Windows dotnet.exe).
  </acceptance_criteria>
  <done>The integration tests compile and pass against the real ContentSiteIndexStore over SQLite, proving the migration, grandfather mapping, grandfather no-restamp across a fresh store instance, safe-upsert preservation of all four admin fields at non-default values (including is_hidden=TRUE), new-row-pending, approved-only filter (with ApprovalStatus populated), and DDL default behaviors.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test setup SQL → temp SQLite | Test-only direct SQL builds legacy schemas and stamps statuses; isolated per-fact temp DB, deleted on Dispose |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-43-T1 | Tampering / Injection | Direct setup SQL in tests | mitigate (LOW) | Row values bound via AddParameter; only fixed status-string literals inlined. Mirrors the existing visibility test's setup discipline. |
| T-43-T2 | (verification of) Information Disclosure | GetApprovedRowsAsync filter | mitigate (HIGH→verified) | Test 7 is the executable proof of T-43-03 (no pending/rejected leak) and also asserts ApprovalStatus is populated from the SELECT (MED-2). This plan exists to make that mitigation enforceable. |
| T-43-T3 | (verification of) Data integrity | Grandfather idempotency / no-restamp | mitigate (MED→verified) | Test 3 (REQUIRED) now proves the grandfather does NOT re-stamp an operator-changed status across a fresh-store re-run — the executable proof of the self-guarded backfill (T-43-02 / MED-1). Test 2 proves the initial grandfather mapping. |
| T-43-SC | Tampering | npm/pip/cargo installs | n/a | No package installs; no new test framework or NuGet (constraint). Legitimacy gate not applicable. |
</threat_model>

<verification>
- `dotnet build DeckFlow.Core.Tests/DeckFlow.Core.Tests.csproj` → 0 errors, 0 new warnings.
- Targeted test run (Windows dotnet.exe): `"/mnt/c/Program Files/dotnet/dotnet.exe" test DeckFlow.Core.Tests/DeckFlow.Core.Tests.csproj --filter "FullyQualifiedName~ContentSiteIndexStoreApprovalTests"` → all facts pass. If VSTest is flaky in WSL, run via Windows dotnet and capture the pass count; note any environment caveat in the SUMMARY.
- `grep -c "\[Fact\]" DeckFlow.Core.Tests/Content/ContentSiteIndexStoreApprovalTests.cs` >= 8 (includes no-restamp + strengthened preservation).
- The no-restamp test constructs a second `new ContentSiteIndexStore(_dbPath)` (verify >= 2 occurrences).
</verification>

<success_criteria>
- At least eight real-SQLite integration tests cover: migration adds column (SC1), grandfather mapping (SC4), grandfather no-restamp across a fresh store instance (SC4 / T-43-02 / MED-1), new-row-pending (SC4/D-04 INSERT), all-four-admin-field preservation at non-default values including is_hidden=TRUE (SC2/PUB-01/MED-4), approved-only filter with ApprovalStatus populated (SC3/PUB-02/MED-2), DDL default.
- Tests build clean and pass; no production code changed; no new packages.
</success_criteria>

<output>
Create `.planning/phases/43-approval-status-safe-upsert/43-02-SUMMARY.md` when done.
</output>
</content>
