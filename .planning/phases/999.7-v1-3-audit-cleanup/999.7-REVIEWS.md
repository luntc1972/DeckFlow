---
phase: 999.7
reviewers: [codex]
reviewed_at: 2026-05-22T20:30:30Z
plans_reviewed:
  - 999.7-01-PLAN.md
  - 999.7-02-PLAN.md
  - 999.7-03-PLAN.md
  - 999.7-04-PLAN.md
reviewer_skipped:
  - claude (SELF_CLI — running inside Claude Code)
reviewer_unavailable:
  - gemini
  - coderabbit
  - opencode
  - qwen
  - cursor
---

# Cross-AI Plan Review — Phase 999.7

## Codex Review

**Summary**
Overall, the phase is well scoped and mostly execution-ready: four small documentation/state cleanups, strong diff guards, and no runtime behavior changes. The blocker is Plan 03. The repo does not match its evidence inventory: there are 5 Claude prompt variant summaries with `result-wrapped output`, not 3, so Plan 03 cannot satisfy its own final grep gate as written.

### Plan 01: STATE.md Counters

**Strengths**
- Correctly treats Phase 999.7 as a meta-audit phase excluded from the v1.3 11-phase / 46-plan total.
- Good invariant checks for `completed <= total` and `percent = 100`.
- Diff scope is tight: frontmatter only.

**Concerns**
- LOW: Current repo state is `10/12/45/50/83` in `.planning/STATE.md:10`, but the plan still references clearing `completed_plans: 66`. The target is still right, but the evidence text is stale.
- LOW: The `last_updated` acceptance is pinned to `2026-05-22`; fine for same-day execution, brittle if this runs later.

**Suggestions**
- Add `grep -c "^  total_plans: 50$" ... returns 0` or rewrite the stale-value note to say the observed current values may be `10/12/45/50`.
- Keep the literal SC1 target.

**Risk Assessment: LOW**
The plan achieves SC1. The issues are wording and acceptance clarity, not implementation risk.

### Plan 02: WDG Checkboxes

**Strengths**
- Excellent minimal diff: exactly 10 checkbox flips.
- Protects the traceability table from accidental churn.
- Directly matches the audit evidence that WDG-01..10 are satisfied.

**Concerns**
- LOW: REQUIREMENTS still says `Status: active`, and the traceability rows remain `active` at `.planning/REQUIREMENTS.md:71`. That is explicitly out of this plan, but it remains archive drift.

**Suggestions**
- Add a note that milestone-level `Status:` / traceability status cleanup is owned by `/gsd-complete-milestone`, or explicitly defer it.

**Risk Assessment: LOW**
This cleanly achieves SC2.

### Plan 03: Claude Variant Summaries

**Strengths**
- Right instinct: doc-comment-only, no prompt body changes.
- Good protection against formatter spillover and method-body edits.
- Build/test gates are conservative.

**Concerns**
- **HIGH**: Plan says `ClaudeFollowUpPromptVariant.cs` and `ClaudeSetUpgradePromptVariant.cs` do not carry the stale phrase at `999.7-03-PLAN.md:19`, but they do: `ClaudeFollowUpPromptVariant.cs:11`, `ClaudeSetUpgradePromptVariant.cs:12`. **Verified independently** by grep — repo currently has 5 files with `result-wrapped output`, not 3.
- **HIGH**: Verification requires `grep -rln "result-wrapped" DeckFlow.Web/Services/PromptBuilders/` to return 0 at `999.7-03-PLAN.md:156`. Editing only 3 files will leave 2 matches, so the plan fails itself.
- MEDIUM: The forbidden paths are wrong at `999.7-03-PLAN.md:73`: `FollowUp` and `SetUpgrade` are not under `Analysis`.
- MEDIUM: `git diff --stat shows exactly 3 files` conflicts with creating `999.7-03-SUMMARY.md` unless the stat gate runs before summary creation.

**Suggestions**
- Change SC3/Plan 03 to update all 5 Claude variant summaries, or add a small erratum plan for FollowUp and SetUpgrade.
- Replace path checks with correct paths and use `git diff -- <paths>`.
- Use `! rg -n "result-wrapped output" DeckFlow.Web/Services/PromptBuilders` for the final no-match gate.

**Risk Assessment: HIGH**
Not execution-ready. It will either leave stale docs or fail verification.

### Plan 04: DeckController Comment + UAT Status

**Strengths**
- Good handler-anchored placement gate using `awk`; avoids brittle line numbers.
- Correctly documents intentional asymmetry without changing behavior.
- UAT status change is tightly scoped.

**Concerns**
- MEDIUM: Plan 03 and Plan 04 both run full build/test and are both Wave 1. Parallel execution can waste time or create test/build contention.
- LOW: Suggested comment uses non-ASCII punctuation and arrows at `999.7-04-PLAN.md:115`. Source comments should stay ASCII here.
- LOW: `git diff --stat shows exactly 2 files` conflicts with creating `999.7-04-SUMMARY.md` unless ordered before summary creation.

**Suggestions**
- Make one final build/test gate serial after all edits, ideally in Plan 04 with `depends_on: [01, 02, 03]`, or add a tiny final verification plan.
- Use a shorter ASCII comment: `// Audit F-02: DeckAnalysis intentionally skips the response-json tier because it has no paste-response-only re-download path; comparison/meta-gap use the full Phase 999.3 D-10 order.`

**Risk Assessment: LOW to MEDIUM**
The edits are safe, but the duplicated parallel verification is operationally messy.

### Overall Suggestions

- Fix Plan 03 before execution. That is the only blocking issue.
- Commit or otherwise include `.planning/v1.3-MILESTONE-AUDIT.md`; it is currently untracked, but all plans depend on it.
- Clarify whether exact `git diff --stat` gates run before or after creating `999.7-XX-SUMMARY.md`.
- Prefer one final build/test gate after all edits instead of duplicate full-suite gates in parallel plans.

---

## Consensus Summary

Single-reviewer run (Codex only — Claude self-skipped, no other CLIs available locally). Consensus = Codex findings.

### Agreed Strengths
- Phase scope tight and well-defined; 4 plans match the 5 scope items.
- Plans 01 + 02 are execution-ready with minimal diffs.
- Plan 04 awk-bounded F-02 grep gate is drift-proof.
- Anti-reformatting defenses (numstat, init-getter / attribute / raw-string / LF) correctly applied to C# plans.

### Agreed Concerns (BLOCK execution)

| Severity | Finding | Plan | Verification |
|----------|---------|------|--------------|
| HIGH | Plan 03 SC3 + grep gate only covers 3 Claude variants; repo has 5 files with "result-wrapped" phrase (FollowUp + SetUpgrade also affected) | 03 | `grep -rln "result-wrapped" DeckFlow.Web/Services/PromptBuilders/` returns 5 matches, not 3 — independently verified |
| HIGH | Plan 03 final grep `grep -rln returns 0` will FAIL after edit because 2 files remain untouched | 03 | Self-evident from Concern #1 |
| MEDIUM | Plan 03 "forbidden paths" reference wrong directory structure | 03 | `FollowUp/` + `SetUpgrade/` are sibling dirs to `Analysis/`, not children |
| MEDIUM | Plan 03 + Plan 04 both run full build/test in parallel Wave 1 — wasteful contention | 03, 04 | Better: single serial build/test gate post-edit |
| MEDIUM | `git diff --stat shows exactly N files` gates conflict with SUMMARY.md creation order | 03, 04 | Use `git diff -- <paths>` instead |
| LOW | Plan 01 stale-value gate cites `completed_plans: 66`; current STATE.md shows `45` | 01 | `.planning/STATE.md:10` |
| LOW | Plan 01 `last_updated: 2026-05-22` brittle if execution slips a day | 01 | Use date-pattern gate instead |
| LOW | Plan 02 milestone-level `Status: active` + traceability rows untouched (out of scope but archive drift) | 02 | Owned by `/gsd-complete-milestone` |
| LOW | Plan 04 suggested F-02 comment uses non-ASCII punctuation | 04 | ASCII-only |

### Divergent Views
N/A — single reviewer.

### Resolution Path

**Required decision before execution: what is SC3's true scope?**

Option A — **expand SC3 to 5 files** (recommended if Phase 999.2 actually stripped `<result>` wrapper from all 5 Claude variants):
- Update ROADMAP SC3 to list 5 files instead of 3.
- Update Plan 03 to edit all 5 variant summaries.
- Update `forbidden_paths` in Plan 03 to reflect correct sibling directory structure.

Option B — **add erratum plan 999.7-05** (recommended if Phase 999.2 only stripped 3 variants and FollowUp/SetUpgrade still emit `<result>`-wrapped output):
- Verify FollowUp + SetUpgrade prompt body — do they still emit `<result>` wrapper or not?
- If yes (still wrapped): keep Plan 03 at 3 files but FIX the grep gate to scope to the 3 modified paths (not directory-wide).
- If no (also stripped): add Plan 05 covering FollowUp + SetUpgrade summaries.

**Recommended next step:** Re-plan via `/gsd:plan-phase 999.7 --reviews`. The replanner reads this REVIEWS.md and chooses Option A vs Option B after verifying actual Phase 999.2 scope against FollowUp + SetUpgrade prompt-builder behavior.
