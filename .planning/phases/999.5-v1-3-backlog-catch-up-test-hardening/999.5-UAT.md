---
status: testing
phase: 999.5-v1-3-backlog-catch-up-test-hardening
source: [999.5-01-SUMMARY.md, 999.5-02-SUMMARY.md, 999.5-03-SUMMARY.md, 999.5-04-SUMMARY.md]
started: 2026-05-22T14:02:49Z
updated: 2026-05-22T14:02:49Z
---

## Current Test

number: 1
name: Full Test Suite Gate (P01 + P02 + P03 + P04 attestation)
expected: |
  Run `"/mnt/c/Program Files/dotnet/dotnet.exe" test DeckFlow.sln -c Release --nologo`. Suite reports Passed 433, Failed 9, Skipped 3, Total 445 (or better). The 14 P01-targeted failures (FeedbackStore SQLite race x10, ScryfallThrottle 429 timing x2, DeckAnalysisPacket prose x1, DeckComparison CRLF x1) are all green. The 2 P02 new facts (SemanticallyEmptyInput) and 2 P03 regression facts (SkipsCacheSweep_WhenHarvestActive) all pass. ResultContractTests reports 18/0/0. 9 pre-existing residual failures (PacketArtifact x2, ArchidektCacheJob x2, AiPlatformPhase10 x4, BasicAuth x1) remain — these are deferred to v1.4 per D-22, NOT regressions from 999.5.
awaiting: user response

## Tests

### 1. Full Test Suite Gate (P01 + P02 + P03 + P04 attestation)
expected: Run `"/mnt/c/Program Files/dotnet/dotnet.exe" test DeckFlow.sln -c Release --nologo`. Suite reports Passed 433, Failed 9, Skipped 3, Total 445 (or better). 14 P01-targeted failures all green. 2 P02 new facts pass. 2 P03 regression facts pass. ResultContractTests 18/0/0. 9 pre-existing failures remain deferred to v1.4.
result: [pending]

### 2. ChatGPT Prompt Has No <result> Directive (P04 user-observable)
expected: Open `/deck-analysis` in the running web app, paste any valid deck list, set AI platform to ChatGPT, click "Build prompt". The generated prompt text MUST NOT contain the substring `<result>` or `</result>` anywhere. Fenced ```json code-block directive present instead. Repeat for `/deck-comparison`, `/set-upgrade`, `/cedh-meta-gap`, and a deck-analysis follow-up question — all ChatGPT prompts free of `<result>` wrapper.
result: [pending]

### 3. Gemini Prompt Still Wraps in <result> (P04 invariant)
expected: Same `/deck-analysis` page, swap AI platform to Gemini, rebuild prompt. The generated prompt DOES still include the `<result>...</result>` wrap directive (Gemini relies on it). `GeminiJsonMandate` block still present as the final instruction.
result: [pending]

### 4. Legacy <result>-Wrapped Upload Still Parses (P04 backward compat)
expected: On `/deck-analysis` (or any AI-response paste UI), paste a legacy AI response that wraps the JSON in literal `<result>{ ... valid analysis payload ... }</result>` tags. App parses it successfully and renders the analysis report — no "could not parse" or wrong-shape error. Parser regex falls through gracefully.
result: [pending]

### 5. Empty deck_comparison Payload Rejected (P02 D-09 user-observable)
expected: On `/deck-comparison` paste-response step, paste the literal text `{"deck_comparison":{}}` and submit. App rejects with the existing wrong-shape validation error message "The submitted AI response did not contain a valid deck_comparison payload." (NOT a mostly-empty comparison report with blank fields).
result: [pending]

### 6. Empty meta_gap Payload Rejected (P02 D-10 user-observable)
expected: On `/cedh-meta-gap` paste-response step, paste the literal text `{"meta_gap":{}}` and submit. App rejects with the existing wrong-shape validation error "The submitted AI response did not contain a valid meta_gap payload." (NOT a mostly-empty meta_gap report).
result: [pending]

### 7. Harvest Debug Archive + STATE Cleanup (P03 metadata)
expected: `.planning/debug/harvest-killed-by-suggestion.md` and `harvest-killed-by-suggestion-rootcause.md` no longer present at top level; both moved to `.planning/debug/archive/` with git history preserved. `.planning/STATE.md` §Deferred Items table no longer contains the `harvest-killed-by-suggestion (H1 hypothesis parked in .planning/debug/)` row. Sibling deferred rows (Gemini paste-limit, v1.1 phase-dir archive) still present.
result: [pending]

## Summary

total: 7
passed: 0
issues: 0
pending: 7
skipped: 0
blocked: 0

## Gaps

[none yet]
