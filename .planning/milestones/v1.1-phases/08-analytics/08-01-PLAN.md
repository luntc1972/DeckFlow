---
phase: 08-analytics
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - DeckFlow.Web/Security/IpHasher.cs
  - DeckFlow.Web/Services/FeedbackStore.cs
  - DeckFlow.Web/Services/Analytics/RequestMetricEvent.cs
  - DeckFlow.Web/Services/Analytics/IRequestMetricsStore.cs
  - DeckFlow.Web/Services/Analytics/RequestMetricsStore.cs
autonomous: true
requirements: [ANLY-01, ANLY-03]
must_haves:
  truths:
    - "Shared IpHasher exists at DeckFlow.Web/Security/IpHasher.cs and FeedbackStore consumes it (no duplicate SHA-256 site)"
    - "IpHasher.HashRequestIp(HttpContext, salt) honors CF-Connecting-IP > X-Forwarded-For first hop > Connection.RemoteIpAddress"
    - "D-01: request_metrics table schema has columns route_key, day_utc, status_class, hit_count, error_count with PK (route_key, day_utc, status_class) — UPSERT via ON CONFLICT DO UPDATE"
    - "D-02: no cleanup BackgroundService introduced this phase — per-(route, day, status_class) aggregation keeps growth bounded; cleanup deferred to ANLY-NEXT-01"
    - "D-03: request_metric_ip_seen side table has columns route_key, day_utc, ip_hash with PK (route_key, day_utc, ip_hash) — INSERT ON CONFLICT DO NOTHING for unique-IP tracking (no HyperLogLog, no PG extension)"
    - "Neither table has any column named ip_raw or ip — only ip_hash (SC #3)"
    - "RequestMetricsStore.UpsertBatchAsync runs both UPSERTs (D-01 hit/error counters + D-03 ip_seen) in a single Postgres transaction via unnest(@arrays)"
    - "Build is clean: dotnet build DeckFlow.sln succeeds with 0 warnings, 0 errors"
  artifacts:
    - path: "DeckFlow.Web/Security/IpHasher.cs"
      provides: "Static helper: HashRequestIp(HttpContext, string salt) + Hash(string ip, string salt) + ResolveSaltAsync(connection, ct)"
      contains: "CF-Connecting-IP"
    - path: "DeckFlow.Web/Services/Analytics/RequestMetricEvent.cs"
      provides: "sealed record of (RouteKey, DayUtc, StatusClass, IsError, IpHash)"
    - path: "DeckFlow.Web/Services/Analytics/IRequestMetricsStore.cs"
      provides: "Interface: EnsureSchemaAsync(ct) + UpsertBatchAsync(IReadOnlyList<RequestMetricEvent>, ct)"
    - path: "DeckFlow.Web/Services/Analytics/RequestMetricsStore.cs"
      provides: "Postgres-only store; EnsureSchemaAsync creates request_metrics + request_metric_ip_seen; UpsertBatchAsync uses unnest(@routeKeys, @dayUtcs, @statusClasses, @errorInc) ON CONFLICT (route_key, day_utc, status_class) DO UPDATE; second UPSERT is INSERT ON CONFLICT DO NOTHING for ip_seen; takes IServiceProvider per D-14 (lazy resolution mirrors HarvestRunStore)"
      contains: "ON CONFLICT (route_key, day_utc, status_class) DO UPDATE"
    - path: "DeckFlow.Web/Services/FeedbackStore.cs"
      provides: "Modified — HashIpInternal delegates to IpHasher.Hash(ip, salt); ResolveSaltAsync moved to IpHasher.ResolveSaltAsync (or kept calling-shim) so a single salt resolution chain exists"
  key_links:
    - from: "DeckFlow.Web/Security/IpHasher.cs"
      to: "DeckFlow.Web/Services/FeedbackStore.cs"
      via: "FeedbackStore.HashIpInternal calls IpHasher.Hash"
      pattern: "IpHasher\\.Hash"
    - from: "DeckFlow.Web/Services/Analytics/RequestMetricsStore.cs"
      to: "Postgres request_metrics + request_metric_ip_seen tables"
      via: "EnsureSchemaAsync (CREATE TABLE IF NOT EXISTS) + UpsertBatchAsync (unnest UPSERT)"
      pattern: "CREATE TABLE IF NOT EXISTS request_metrics"
---

<objective>
Wave 1 of Phase 8 analytics: extract the shared IP hasher from FeedbackStore so analytics middleware can reuse it without duplicating SHA-256 / salt resolution / Cloudflare-priority logic. In parallel, define the analytics event record, store interface, and Postgres-only store with EnsureSchemaAsync (request_metrics + request_metric_ip_seen) and UpsertBatchAsync (unnest-based bulk UPSERT).

Purpose: Analytics middleware (Wave 3) and flusher (Wave 2) both depend on these contracts. IpHasher extraction also closes a long-standing duplication risk — Phase 5 BUG-02 already proved CF-Connecting-IP priority must be the single source of truth for IP partitioning under Cloudflare + Render.

Output: 4 new files (IpHasher.cs, RequestMetricEvent.cs, IRequestMetricsStore.cs, RequestMetricsStore.cs); 1 modified file (FeedbackStore.cs uses IpHasher).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-analytics/08-CONTEXT.md
@.planning/phases/08-analytics/08-RESEARCH.md
@./CLAUDE.md

<interfaces>
<!-- Existing patterns the executor mirrors. Do NOT explore the codebase further — these are the exact contracts. -->

From DeckFlow.Web/Services/FeedbackStore.cs (current, to be refactored):
```csharp
// HashIpInternal returns: SHA256(ip + "|" + salt) -> hex string, or null when ip is null/whitespace
private string? HashIpInternal(string? ip)
{
    if (string.IsNullOrWhiteSpace(ip)) return null;
    var salt = _ipSalt ?? throw new InvalidOperationException("IP salt not initialised.");
    var bytes = SHA256.HashData(Encoding.UTF8.GetBytes(ip + "|" + salt));
    return Convert.ToHexString(bytes);
}

// ResolveSaltAsync (line 288) reads FEEDBACK_IP_SALT env var first; if absent, reads/inserts feedback_meta.ip_salt row.
```

From DeckFlow.Web/Program.cs:400-409 (CF-Connecting-IP priority):
```csharp
internal static string DeriveCloudflareClientIp(HttpContext context)
{
    var raw = context.Request.Headers["CF-Connecting-IP"].ToString();
    if (string.IsNullOrWhiteSpace(raw))
    {
        Log.Warning("CF-Connecting-IP missing on {Path} -- falling back...", ...);
        return context.Connection.RemoteIpAddress?.ToString() ?? "unknown";
    }
    return raw;
}
```

From DeckFlow.Web/Services/Harvest/HarvestRunStore.cs (D-14 lazy IServiceProvider pattern post-dc66a38):
```csharp
public sealed class HarvestRunStore : IHarvestRunStore
{
    private readonly IServiceProvider? _services;  // lazy resolve, never required at ctor
    public HarvestRunStore(IWebHostEnvironment environment, IServiceProvider? services = null) { ... }
    // EnsureSchemaAsync uses SemaphoreSlim _schemaGate + volatile bool _schemaReady double-check.
}
```

From DeckFlow.Web/Services/FeatureFlags/FeatureFlagStore.cs (Postgres EnsureSchemaAsync template — read this file for the exact CREATE TABLE IF NOT EXISTS shape and connection-open pattern):
```
EnsureSchemaAsync -> _schemaGate.WaitAsync -> open connection -> CREATE TABLE IF NOT EXISTS ... -> set _schemaReady = true.
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Extract IpHasher shared helper + swap FeedbackStore to use it</name>
  <files>DeckFlow.Web/Security/IpHasher.cs, DeckFlow.Web/Services/FeedbackStore.cs</files>
  <read_first>
    - DeckFlow.Web/Services/FeedbackStore.cs (read entire file — see HashIpInternal at line 173, ResolveSaltAsync at line 288, and the _ipSalt field initialization in EnsureSchemaAsync at line 279). This is being refactored, executor MUST see current state.
    - DeckFlow.Web/Program.cs lines 392-410 (DeriveCloudflareClientIp — the CF-Connecting-IP priority logic that IpHasher must encapsulate).
    - .planning/phases/08-analytics/08-CONTEXT.md D-13 (IP capture priority + reuse FEEDBACK_IP_SALT).
    - .planning/phases/08-analytics/08-RESEARCH.md "Pitfall 5: X-Forwarded-Proto / IP capture under PaaS proxy" + "Don't Hand-Roll" table row "IP hashing".
    - ~/.claude/projects/.../memory/feedback_csrf_validator_under_proxy.md (PaaS-proxy lesson — CF-Connecting-IP is the trustworthy source).
  </read_first>
  <action>
    Per D-13 + ANLY-03: create `DeckFlow.Web/Security/IpHasher.cs` (file-scoped namespace `DeckFlow.Web.Security;`, public static class `IpHasher`) with these exact methods:

    1. `public static string? HashRequestIp(Microsoft.AspNetCore.Http.HttpContext context, string salt)` — derives raw IP using priority order: (a) `context.Request.Headers["CF-Connecting-IP"].ToString()` if non-whitespace, (b) first hop of `context.Request.Headers["X-Forwarded-For"].ToString().Split(',')[0].Trim()` if non-whitespace, (c) `context.Connection.RemoteIpAddress?.ToString()`. Returns null if all three are null/whitespace. Otherwise returns `Hash(ip, salt)`.
    2. `public static string Hash(string ip, string salt)` — computes `Convert.ToHexString(SHA256.HashData(Encoding.UTF8.GetBytes(ip + "|" + salt)))`. ArgumentNullException.ThrowIfNull(salt). Returns empty string if ip is null/whitespace.
    3. `public static async Task<string> ResolveSaltAsync(System.Data.Common.DbConnection connection, CancellationToken ct = default)` — moves the existing `ResolveSaltAsync` body from `FeedbackStore.cs:288-313` verbatim: reads `FEEDBACK_IP_SALT` env var first, falls back to `SELECT value FROM feedback_meta WHERE key = 'ip_salt'`, generates+inserts a new `Convert.ToHexString(RandomNumberGenerator.GetBytes(32))` salt if neither exists. Keep the same `feedback_meta` table dependency (FeedbackStore already creates it).

    Keep all XML doc comments per CLAUDE.md "Comments" convention (per-method `<summary>` + `<param>` + `<returns>`).

    Then refactor `DeckFlow.Web/Services/FeedbackStore.cs`:
    - In `HashIpInternal(string? ip)` (line 173): replace `SHA256.HashData(...)` body with a call to `DeckFlow.Web.Security.IpHasher.Hash(ip!, salt)` — keep the existing null-check + InvalidOperationException-on-uninitialised-salt guard.
    - In `EnsureSchemaAsync` at line 279 (`_ipSalt = await ResolveSaltAsync(connection, cancellationToken);`): change the call to `_ipSalt = await DeckFlow.Web.Security.IpHasher.ResolveSaltAsync(connection, cancellationToken);` and DELETE the local private `ResolveSaltAsync` method body (lines 288-313).
    - Add `using DeckFlow.Web.Security;` at top of file (alphabetical with other DeckFlow.* usings per CLAUDE.md "Import Organization").
    - Remove now-unused `using System.Security.Cryptography;` and `using System.Text;` from FeedbackStore.cs IF no other site in the file consumes them (verify with grep before removing).

    Per CLAUDE.md "Naming Patterns": one public type per file, file-scoped namespace, sealed where applicable (IpHasher is `static`, no sealed needed).
  </action>
  <verify>
    <automated>dotnet build /mnt/c/users/chrislunt/source/personal/decksyncworkbench/DeckFlow.sln 2>&amp;1 | tail -20 &amp;&amp; grep -c 'IpHasher\.Hash' /mnt/c/users/chrislunt/source/personal/decksyncworkbench/DeckFlow.Web/Services/FeedbackStore.cs &amp;&amp; grep -c 'CF-Connecting-IP' /mnt/c/users/chrislunt/source/personal/decksyncworkbench/DeckFlow.Web/Security/IpHasher.cs &amp;&amp; ! grep -q 'private.*ResolveSaltAsync' /mnt/c/users/chrislunt/source/personal/decksyncworkbench/DeckFlow.Web/Services/FeedbackStore.cs &amp;&amp; echo OK</automated>
  </verify>
  <acceptance_criteria>
    - File `DeckFlow.Web/Security/IpHasher.cs` exists.
    - `IpHasher.cs` contains exact strings: `public static class IpHasher`, `HashRequestIp`, `CF-Connecting-IP`, `X-Forwarded-For`, `RemoteIpAddress`, `SHA256.HashData`.
    - `IpHasher.cs` contains `public static async Task<string> ResolveSaltAsync` AND `FEEDBACK_IP_SALT` AND `feedback_meta`.
    - `FeedbackStore.cs` contains `IpHasher.Hash` AND does NOT contain `private.*ResolveSaltAsync` (regex; the local method has been removed).
    - `FeedbackStore.cs` contains `using DeckFlow.Web.Security;`.
    - `dotnet build DeckFlow.sln` returns exit code 0 with `Build succeeded` and 0 errors.
  </acceptance_criteria>
  <done>
    IpHasher is the single SHA-256 + salt + CF-Connecting-IP priority site in the codebase. FeedbackStore behavior is byte-identical to pre-refactor (same hash output for same inputs because the algorithm + salt resolution chain are preserved). Build is clean.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Define RequestMetricEvent + IRequestMetricsStore + RequestMetricsStore (schema + bulk UPSERT)</name>
  <files>DeckFlow.Web/Services/Analytics/RequestMetricEvent.cs, DeckFlow.Web/Services/Analytics/IRequestMetricsStore.cs, DeckFlow.Web/Services/Analytics/RequestMetricsStore.cs</files>
  <read_first>
    - DeckFlow.Web/Services/FeatureFlags/FeatureFlagStore.cs (read top 100 lines — copy the EnsureSchemaAsync SemaphoreSlim + _schemaReady double-check pattern + connection-open shape).
    - DeckFlow.Web/Services/Harvest/HarvestRunStore.cs lines 1-110 (the exact `IServiceProvider? _services` lazy DI ctor pattern from D-14 — analytics store mirrors this even though it doesn't currently call back into IServiceProvider; future-proofing per Phase 7.1 dc66a38 errata).
    - .planning/phases/08-analytics/08-CONTEXT.md D-01, D-03, D-04, D-13, D-14 (schema grain + side table + status_class + ip_hash + DI shape).
    - .planning/phases/08-analytics/08-RESEARCH.md "Pattern 3: Postgres bulk UPSERT via UNNEST + arrays" (the exact SQL the executor pastes — both UPSERTs in one transaction).
    - ~/.claude/projects/.../memory/feedback_sqlite_postgres_sql_divergence.md (analytics is PG-only per D-01 narrative; do NOT add a SQLite path).
    - ~/.claude/projects/.../memory/feedback_di_optional_dep_does_not_break_cycle.md (D-14 — store takes IServiceProvider, not direct buffer/flusher refs).
  </read_first>
  <action>
    Create three files under `DeckFlow.Web/Services/Analytics/` (file-scoped namespace `DeckFlow.Web.Services.Analytics;`).

    **(A) `RequestMetricEvent.cs`** — `public sealed record RequestMetricEvent(string RouteKey, DateOnly DayUtc, short StatusClass, bool IsError, string? IpHash);` with XML `<summary>` doc comment.

    **(B) `IRequestMetricsStore.cs`** — interface with two members:
    ```csharp
    Task EnsureSchemaAsync(CancellationToken cancellationToken = default);
    Task UpsertBatchAsync(IReadOnlyList<RequestMetricEvent> events, CancellationToken cancellationToken = default);
    ```
    XML doc comments explain Postgres-only contract per D-01.

    **(C) `RequestMetricsStore.cs`** — `public sealed class RequestMetricsStore : IRequestMetricsStore`. Use the EXACT shape from HarvestRunStore.cs:
    - Fields: `private readonly RelationalDatabaseConnection _connectionInfo; private readonly IServiceProvider? _services; private readonly SemaphoreSlim _schemaGate = new(1, 1); private volatile bool _schemaReady;`
    - Three ctors: `(string databasePath, IServiceProvider? services = null)`, `(RelationalDatabaseConnection connectionInfo, IServiceProvider? services = null)`, `(IWebHostEnvironment environment, IServiceProvider? services = null)`. The `IWebHostEnvironment` ctor delegates to `DeckFlowDatabaseConnectionFactory.CreateHarvestStateConnection(environment)` (analytics shares the harvest/feedback DB — same Postgres instance).
    - `EnsureSchemaAsync` — copy the SemaphoreSlim double-check shape from HarvestRunStore. After lock acquired, open connection and run the Postgres CREATE TABLE IF NOT EXISTS:
      ```sql
      CREATE TABLE IF NOT EXISTS request_metrics (
        route_key      text     NOT NULL,
        day_utc        date     NOT NULL,
        status_class   smallint NOT NULL,
        hit_count      bigint   NOT NULL DEFAULT 0,
        error_count    bigint   NOT NULL DEFAULT 0,
        PRIMARY KEY (route_key, day_utc, status_class)
      );
      CREATE INDEX IF NOT EXISTS ix_request_metrics_day_utc ON request_metrics (day_utc DESC);

      CREATE TABLE IF NOT EXISTS request_metric_ip_seen (
        route_key text NOT NULL,
        day_utc   date NOT NULL,
        ip_hash   text NOT NULL,
        PRIMARY KEY (route_key, day_utc, ip_hash)
      );
      ```
      Set `_schemaReady = true` in the finally block (mirror HarvestRunStore exactly). If `_connectionInfo.IsPostgres == false`, log a warning and skip schema creation (analytics is PG-only per D-01; SQLite local-dev gracefully no-ops).

    - `UpsertBatchAsync` — copy the EXACT body from RESEARCH.md "Pattern 3" (lines 440-505). Two UPSERTs in one transaction:
      1. `INSERT INTO request_metrics (route_key, day_utc, status_class, hit_count, error_count) SELECT ... FROM unnest(@routeKeys, @dayUtcs, @statusClasses, @errorInc) AS u(...) GROUP BY u.route_key, u.day_utc, u.status_class ON CONFLICT (route_key, day_utc, status_class) DO UPDATE SET hit_count = request_metrics.hit_count + EXCLUDED.hit_count, error_count = request_metrics.error_count + EXCLUDED.error_count;`
      2. `INSERT INTO request_metric_ip_seen (route_key, day_utc, ip_hash) SELECT u.route_key, u.day_utc, u.ip_hash FROM unnest(@routeKeys, @dayUtcs, @ipHashes) AS u(...) WHERE u.ip_hash IS NOT NULL AND u.ip_hash <> '' ON CONFLICT (route_key, day_utc, ip_hash) DO NOTHING;`

      Cast Npgsql parameters as `new NpgsqlParameter("routeKeys", routeKeys)` (Npgsql infers array type from the .NET array). Open connection via `(NpgsqlConnection)await OpenConnectionAsync(ct)` mirroring HarvestRunStore's connection-open helper. If `events.Count == 0` return immediately. Wrap the two commands in `await using var tx = await conn.BeginTransactionAsync(ct);` then `await tx.CommitAsync(ct);`.

    Add a private `OpenConnectionAsync(CancellationToken ct)` mirroring HarvestRunStore.cs (calls `_connectionInfo.OpenConnectionAsync` or equivalent — read HarvestRunStore.cs to see the actual signature and copy verbatim).

    Per CLAUDE.md: Allman braces, file-scoped namespace, XML doc comments, `ArgumentNullException.ThrowIfNull` at top of ctors.
  </action>
  <verify>
    <automated>dotnet build /mnt/c/users/chrislunt/source/personal/decksyncworkbench/DeckFlow.sln 2>&amp;1 | tail -10 &amp;&amp; grep -c 'CREATE TABLE IF NOT EXISTS request_metrics' /mnt/c/users/chrislunt/source/personal/decksyncworkbench/DeckFlow.Web/Services/Analytics/RequestMetricsStore.cs &amp;&amp; grep -c 'ON CONFLICT (route_key, day_utc, status_class) DO UPDATE' /mnt/c/users/chrislunt/source/personal/decksyncworkbench/DeckFlow.Web/Services/Analytics/RequestMetricsStore.cs &amp;&amp; grep -c 'ON CONFLICT (route_key, day_utc, ip_hash) DO NOTHING' /mnt/c/users/chrislunt/source/personal/decksyncworkbench/DeckFlow.Web/Services/Analytics/RequestMetricsStore.cs &amp;&amp; ! grep -E '(ip_raw|^[^/]*[[:space:]](ip|raw_ip)[[:space:]]+text)' /mnt/c/users/chrislunt/source/personal/decksyncworkbench/DeckFlow.Web/Services/Analytics/RequestMetricsStore.cs &amp;&amp; echo OK</automated>
  </verify>
  <acceptance_criteria>
    - All three files exist under `DeckFlow.Web/Services/Analytics/`.
    - `RequestMetricEvent.cs` contains `public sealed record RequestMetricEvent` AND `RouteKey` AND `DayUtc` AND `StatusClass` AND `IsError` AND `IpHash`.
    - `IRequestMetricsStore.cs` contains `EnsureSchemaAsync` AND `UpsertBatchAsync`.
    - `RequestMetricsStore.cs` contains exact strings: `CREATE TABLE IF NOT EXISTS request_metrics`, `PRIMARY KEY (route_key, day_utc, status_class)`, `CREATE TABLE IF NOT EXISTS request_metric_ip_seen`, `PRIMARY KEY (route_key, day_utc, ip_hash)`, `unnest(@routeKeys`, `ON CONFLICT (route_key, day_utc, status_class) DO UPDATE`, `ON CONFLICT (route_key, day_utc, ip_hash) DO NOTHING`, `BeginTransactionAsync`.
    - `RequestMetricsStore.cs` contains `IServiceProvider? _services` AND `IServiceProvider? services = null` (D-14 lazy DI ctor parameter).
    - `RequestMetricsStore.cs` does NOT contain the substring `ip_raw` AND does NOT define a column named just `ip text` — only `ip_hash text` (SC #3 PII gate).
    - `dotnet build DeckFlow.sln` returns exit code 0 with `Build succeeded` and 0 errors.
  </acceptance_criteria>
  <done>
    Store is ready to be DI-registered (Wave 3) and consumed by the flusher (Wave 2). EnsureSchemaAsync is idempotent and creates both tables on first call. UpsertBatchAsync correctly batches N events into one transaction with two UPSERTs. PG-only contract is enforced.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser -> Kestrel | Untrusted client headers (CF-Connecting-IP injected by Cloudflare proxy is the only trustworthy source); X-Forwarded-For from random upstreams must NOT be trusted past Cloudflare's edge |
| ASP.NET request thread -> RequestMetricsStore | Internal — store callers (the flusher in Wave 2) own batch shape; store is responsible for SQL parameterization |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Information Disclosure | request_metrics + request_metric_ip_seen tables | mitigate | PII gate: schema has only `ip_hash text`, never `ip` or `ip_raw`. SHA-256 with `FEEDBACK_IP_SALT` makes hashes irreversible. Acceptance criterion greps the schema to enforce. (SC #3) |
| T-08-02 | Spoofing | IpHasher.HashRequestIp | mitigate | CF-Connecting-IP header takes priority; X-Forwarded-For is only used as a fallback first-hop, and Connection.RemoteIpAddress as a last resort. Mirrors Phase 5 BUG-02 / Phase 7.1 CAT-FIX-01 hardening. |
| T-08-03 | Tampering | RequestMetricsStore.UpsertBatchAsync | mitigate | All values bound via `NpgsqlParameter` — no string interpolation in SQL. Routes flowing in are derived from Endpoint.DisplayName (not raw user input), but parameterization defends against future code paths. |
| T-08-04 | Denial of Service (DI cycle) | RequestMetricsStore ctor graph | mitigate | D-14 — store takes `IServiceProvider? services = null`, NOT direct refs to RequestMetricsBuffer or RequestMetricsFlusher. Phase 7.1 dc66a38 errata pattern. Wave 3 mandates a startup smoke-test (D-15). |
| T-08-05 | Repudiation | Drop accounting | accept | This wave does not touch drop accounting — Wave 2 handles. Listed for traceability. |
</threat_model>

<verification>
After both tasks complete:
- `dotnet build DeckFlow.sln` is clean (0 warnings, 0 errors).
- IpHasher is the single SHA-256 + salt + CF-Connecting-IP priority site in the codebase: `grep -rn 'SHA256.HashData' DeckFlow.Web/` should return ONLY `Security/IpHasher.cs` and any pre-existing non-IP sites (e.g., AdminBruteForceTrackerStore for token hashing — verify no functional regression).
- Schema grep proves no PII columns: `grep -E '(ip_raw|raw_ip)' DeckFlow.Web/Services/Analytics/RequestMetricsStore.cs` returns nothing.
- The two new tables are described correctly in EnsureSchemaAsync per D-01 + D-03 + D-04.
</verification>

<success_criteria>
- ANLY-01 partial (schema in place; middleware wiring lands Wave 3).
- ANLY-03 partial (IpHasher available; middleware caller lands Wave 3).
- Build clean.
- No PII columns exist in the analytics schema.
- FeedbackStore behavior unchanged (same hash output, same salt resolution chain).
</success_criteria>

<output>
After completion, create `.planning/phases/08-analytics/08-01-SUMMARY.md` per the template — note any deviations from the schema/DDL (e.g., index name choice, NpgsqlConnection vs DbConnection cast site).
</output>
