---
phase: 97-profile-fusion-conflict-ledger
plan: 06
type: execute
wave: 4
depends_on: [97-01, 97-03, 97-05]
files_modified:
  - DeckFlow.CLI/Program.cs
  - DeckFlow.CLI/ContentKbCommandRunners.cs
  - DeckFlow.Core.Tests/ProfileFusion/FuseProfileRunnerTests.cs
autonomous: true
requirements: [CS-16, CS-20]
must_haves:
  truths:
    - "A new CLI command `fuse-profile --slug <creator> [--db <path>]` loads the creator's measured profile and stated rules, runs ProfileFusionEngine.Fuse, and persists the resulting FusedTarget[] via CreatorStyleProfileStore.UpsertAsync."
    - "The command follows the distill runner's try/catch(exception is not OperationCanceledException) exit-code convention (0 success, 1 failure)."
    - "The Studio ledger page stays strictly read-only — fusion is triggered by this CLI command, not by any Studio button (D-11)."
  artifacts:
    - path: "DeckFlow.CLI/ContentKbCommandRunners.cs"
      provides: "RunFuseProfileAsync — load measured+stated, fuse, persist"
      contains: "RunFuseProfileAsync"
    - path: "DeckFlow.CLI/Program.cs"
      provides: "fuse-profile command declaration + --slug/--db options + handler registration"
      contains: "fuse-profile"
  key_links:
    - from: "DeckFlow.CLI/ContentKbCommandRunners.cs"
      to: "ProfileFusionEngine.Fuse + CreatorStyleProfileStore.UpsertAsync"
      via: "the runner composes read -> fuse -> persist"
      pattern: "ProfileFusionEngine.Fuse"
---

<objective>
Add the fusion TRIGGER: a new `fuse-profile` CLI command mirroring `distill`, which loads a
creator's measured profile (`CreatorStyleProfileStore.GetBySlugAsync`) + stated rules
(`GetStatedRulesBySourceSlugAsync`, Plan 03), runs `ProfileFusionEngine.Fuse` (Plan 05), and
persists the FusedTarget[] back via `UpsertAsync`.

Purpose: No code triggers fusion today. A CLI command is the smallest, convention-consistent
trigger (matches distill/harvest as one-shot operator commands) and keeps the Studio ledger page
strictly read-only per D-11. Wave 4 — needs the read path (03) and the engine (05).

Output: The `fuse-profile` command + runner + a runner-level test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/97-profile-fusion-conflict-ledger/97-PATTERNS.md

<interfaces>
distill command declaration (DeckFlow.CLI/Program.cs:94 + AddOption 160-163 + AddCommand 222 + SetHandler):
  var distillCommand = new Command("distill", "...");
  var distillDbOption = new Option<FileInfo?>("--db") { ... };
  rootCommand.AddCommand(distillCommand);
  distillCommand.SetHandler((...) => { Environment.ExitCode = ContentKbCommandRunners.RunDistillAsync(...).GetAwaiter().GetResult(); }, options...);

distill runner (DeckFlow.CLI/ContentKbCommandRunners.cs:80-122):
  RunDistillAsync -> ArgumentNullException.ThrowIfNull(logger); try { dbPath = ContentKbCliPaths.ResolveDatabasePath(db); ... return 0; }
  catch (Exception e) when (e is not OperationCanceledException) { logger.Error(...); Console.Error.WriteLine(e.Message); return 1; }

Store methods:
  CreatorStyleProfileStore(dbPath); GetBySlugAsync(slug, ct) -> CreatorStyleProfile?; UpsertAsync(profile, ct).
  ContentVideoStore(dbPath); GetStatedRulesBySourceSlugAsync(slug, ct) -> IReadOnlyList<StatedRuleCandidate>.
  ProfileFusionEngine.Fuse(measured, statedRules) -> IReadOnlyList<FusedTarget>.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: RunFuseProfileAsync runner (read -> fuse -> persist)</name>
  <files>DeckFlow.CLI/ContentKbCommandRunners.cs</files>
  <read_first>
    - DeckFlow.CLI/ContentKbCommandRunners.cs (RunDistillAsync full method lines 80-122 — exact try/catch/exit-code + ResolveDatabasePath convention to mirror)
    - DeckFlow.Core/Content/CreatorStyleProfileStore.cs (EnsureSchemaAsync 67, UpsertAsync 88, GetBySlugAsync 112)
    - DeckFlow.Core/Content/ContentVideoStore.cs (GetStatedRulesBySourceSlugAsync — Plan 03)
    - DeckFlow.Core/Knowledge/ProfileFusion/ProfileFusionEngine.cs (Fuse — Plan 05)
  </read_first>
  <action>
    Add `public static async Task<int> RunFuseProfileAsync(FileInfo? db, string slug,
    Serilog.ILogger logger, CancellationToken ct)`. `ArgumentNullException.ThrowIfNull(logger)`;
    `ArgumentException.ThrowIfNullOrWhiteSpace(slug)`. In the try: resolve dbPath via
    `ContentKbCliPaths.ResolveDatabasePath(db)`; construct `CreatorStyleProfileStore` +
    `ContentVideoStore` on dbPath; `EnsureSchemaAsync` where the sibling runners do; load the profile
    via `GetBySlugAsync(slug, ct)` (if null -> log a clear "no measured profile for slug" message and
    return 1); load stated rules via `GetStatedRulesBySourceSlugAsync(slug, ct)`; call
    `ProfileFusionEngine.Fuse(profile.MeasuredMetrics, statedRules)`; persist
    `UpsertAsync(profile with { FusedTargets = fused, UpdatedUtc = DateTimeOffset.UtcNow }, ct)`; log a
    concise summary (N fused targets, M conflicts). Use the SAME
    `catch (Exception exception) when (exception is not OperationCanceledException)` block returning 1.
    Return 0 on success. Preserve LF endings and existing file conventions.
  </action>
  <verify>
    <automated>/mnt/c/Program\ Files/dotnet/dotnet.exe build DeckFlow.CLI</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "RunFuseProfileAsync" DeckFlow.CLI/ContentKbCommandRunners.cs` returns a hit.
    - The runner calls `ProfileFusionEngine.Fuse` and `UpsertAsync` (grep both).
    - Null-profile path returns 1 with a clear message; success returns 0.
    - DeckFlow.CLI builds clean, 0 new warnings.
  </acceptance_criteria>
  <done>The runner reads measured+stated, fuses, and persists FusedTargets end-to-end.</done>
</task>

<task type="auto">
  <name>Task 2: Register the fuse-profile command + runner test</name>
  <files>DeckFlow.CLI/Program.cs, DeckFlow.Core.Tests/ProfileFusion/FuseProfileRunnerTests.cs</files>
  <read_first>
    - DeckFlow.CLI/Program.cs (distillCommand declaration line 94, AddOption 160-163, AddCommand 222, SetHandler pattern ~322-325)
    - DeckFlow.CLI/ContentKbCommandRunners.cs (RunFuseProfileAsync from Task 1)
    - DeckFlow.Core.Tests/ (existing CLI runner / store test setup for constructing a seeded SQLite fixture)
  </read_first>
  <action>
    In Program.cs: declare `var fuseProfileCommand = new Command("fuse-profile", "Fuse a creator's
    measured profile and stated rules into weighted numeric targets + say-vs-do ledger.");` with a
    `--slug` `Option<string>` (required) and a `--db` `Option<FileInfo?>` (mirror distillDbOption).
    `AddOption` both, `rootCommand.AddCommand(fuseProfileCommand)`, and `.SetHandler(...)` calling
    `ContentKbCommandRunners.RunFuseProfileAsync(db, slug, Log.Logger, CancellationToken.None)` via the
    same `Environment.ExitCode = ...GetAwaiter().GetResult()` convention as distill. LF endings.

    Add `FuseProfileRunnerTests.cs` (namespace `DeckFlow.Core.Tests`): seed a temp SQLite DB with a
    measured CreatorStyleProfile (a few MeasuredMetrics) + stated rules (via the stores), invoke
    `RunFuseProfileAsync(new FileInfo(dbPath), "salubrioussnail", <serilog logger>, CancellationToken.None)`,
    assert exit code 0 AND that GetBySlugAsync afterward returns a profile whose FusedTargets is
    non-empty and contains the expected verdicts. Also assert an unknown slug returns 1.
  </action>
  <verify>
    <automated>/mnt/c/Program\ Files/dotnet/dotnet.exe test DeckFlow.Core.Tests --filter "FullyQualifiedName~FuseProfileRunner"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "fuse-profile" DeckFlow.CLI/Program.cs` returns a hit; command is registered via AddCommand.
    - Runner test: exit 0 and persisted FusedTargets non-empty after a successful fuse.
    - Unknown-slug test: exit code 1.
    - DeckFlow.CLI builds clean; solution build stays green.
  </acceptance_criteria>
  <done>`fuse-profile --slug <creator>` runs fusion end-to-end and persists the ledger.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator CLI → local content-kb DB | Operator supplies `--slug` and `--db`; the command writes FusedTargets to the local SQLite/Postgres store. Loopback/local-operator context, no network surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-97-07 | Tampering (SQL injection) | slug flows into GetBySlugAsync / GetStatedRulesBySourceSlugAsync | mitigate | Both store reads parameterize the slug (Plan 03 + existing store); the CLI passes it as a value, never builds SQL. |
| T-97-08 | Denial of service | operator points --db at a huge/wrong DB | accept | Local operator tool; a one-shot command with clear exit codes; no untrusted remote invocation. |
| T-97-SC | Tampering | npm/pip/cargo installs | accept | No package-manager installs in this plan (System.CommandLine already referenced). |
</threat_model>

<verification>
- `dotnet.exe build DeckFlow.CLI` and solution build clean.
- `dotnet.exe test DeckFlow.Core.Tests --filter FullyQualifiedName~FuseProfileRunner` green.
- `fuse-profile` command registered; Studio page remains read-only (no write trigger added there).
</verification>

<success_criteria>
`fuse-profile --slug <creator>` deterministically computes and persists the fused profile +
conflict ledger from the CLI, keeping the Studio surface read-only.
</success_criteria>

<output>
Create `.planning/phases/97-profile-fusion-conflict-ledger/97-06-SUMMARY.md` when done.
</output>
