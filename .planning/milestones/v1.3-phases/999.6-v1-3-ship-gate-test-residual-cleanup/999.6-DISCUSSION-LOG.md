# Phase 999.6 Discussion Log

**Date:** 2026-05-22
**Phase:** 999.6 — v1.3 Ship-Gate Test Residual Cleanup
**Mode:** /gsd-discuss-phase (default, no flags)

---

## Origin

During `/gsd-verify-work 999.5` (2026-05-22 8:02am MDT), full-suite gate confirmed `Failed 9, Passed 433, Skipped 3, Total 445`. User asked: "are the current failing tests invalid or is there a problem with the code that needs to be fixed". Triage produced 6 stale / 1 flaky / 2 ambiguous classification. Same turn, user instructed: "write rule do not ship if any unit tests are failing". Memory `feedback_no_ship_failing_tests.md` created; v1.3 `ready_to_ship` status REVOKED; Phase 999.6 added to ROADMAP to close the residual failures before ship can resume.

## Gray Areas Identified

Phase 999.6 has narrow scope (close 9 known failures); gray areas presented to user:

1. Plan structure — how to slice 9 failures across PLAN.md files
2. Stale-test policy — update assertions vs delete dead-premise tests
3. Archidekt debug-first vs update-only
4. Flaky BasicAuth root-cause vs skip-and-defer

User multiSelect'd all 4 for discussion.

## Round 2 — Deep-Dive

### Q1 Plan structure

**Options:**
- (a) **3-plan split (Recommended):** P01 mechanical / P02 BasicAuth debug+fix / P03 Archidekt debug+fix
- (b) 5-plan fan-out per cluster
- (c) Single omnibus P01

**Selected:** (a) — 3-plan split.
**Rationale captured in CONTEXT D-01:** keeps mechanical edits separate from investigation work per memory rule [[feedback-codex-codes-claude-reviews]]; mirrors 999.5 wave pattern.

### Q2 Stale-test resolution policy

**Options:**
- (a) **Update 5, delete dead one (Recommended):** delete `SuggestComparisonZipFileName_includes_compare2_page_segment` (dead premise; coverage already in `lowercased_ai_name(Gemini)`)
- (b) Update all 6
- (c) User decides per test

**Selected:** (a) — update 5, delete 1.
**Rationale captured in CONTEXT D-07:** premise dead, 3 lowercased_ai_name Theory rows updated by D-05 already cover the `-comparison-` substring contract; keeping the dedicated page-segment test would either duplicate coverage or assert obsolete contract.

### Q3 Archidekt debug-first vs update-only

**Options:**
- (a) **/gsd-debug session first (Recommended):** Claude investigates stale-test-vs-real-bug verdict
- (b) Update assertions only — fast but risky
- (c) Hybrid: source-read first, escalate if unclear

**Selected:** (a) — /gsd-debug first.
**Rationale captured in CONTEXT D-12/D-13:** second test's name vs assertion conflict (`ReturnsNullAfterCompletedJob` asserting NotNull) smells like real lifecycle bug, not just timing. Per memory rule, Claude investigates, Codex fixes.

### Q4 Flaky BasicAuth strategy

**Options:**
- (a) **Root-cause-first (Recommended):** find the shared mutable state via /gsd-debug
- (b) `[Collection("BasicAuthSerial")]` isolate — papers over root cause
- (c) `[Fact(Skip=...)]` — defer to v1.4

**Selected:** (a) — root-cause-first.
**Rationale captured in CONTEXT D-09/D-10:** spirit of [[feedback-no-ship-failing-tests]] — we should know what's wrong, not just hide it. Collection workaround leaves the leak for the next test to bite. Skip violates the rule's intent.

## Round 3 — Lock Specifics

### Q5 Plan ordering / dependency

**Selected:** Strict sequential P01 → P02 → P03.
**Captured in D-02.**

### Q6 Acceptance criteria — does `[Skip]` count for Failed=0?

**Selected:** No `[Skip]` additions allowed; Skipped count stays at pre-phase baseline (3).
**Captured in D-03.**

### Q7 Archidekt /gsd-debug scope — 1 session or 2?

**Selected:** 1 shared session (both failures likely share root cause).
**Captured in D-12.**

### Q8 Real production bug — fix in 999.6 or 999.7?

**Selected:** Fix in 999.6.
**Rationale captured in D-15:** ship-gate work is one phase; mirrors 999.5 D-03 "sweep extension discovered mid-execution" precedent. Carving 999.7 adds orchestration overhead for tight scope.

## Decisions Carried Forward (Not Asked)

These are pre-locked by project memory + 999.5 precedent — no need to re-ask:

- Cross-AI dispatch: Codex codes, Claude reviews + investigates → D-22
- Plain default-author commits → D-19
- Windows-dotnet build/test gate path → D-20/D-21
- Commit-per-cluster atomicity + test-triplet body → D-18
- STATE.md ship-status governance auto-handled by /gsd-verify-work close-out → D-23

## Scope Creep Redirected

None during this discussion. Two ideas surfaced as potentially relevant but deferred to v1.4 per scope guardrail:
- xunit project-wide `[CollectionDefinition]` parallelism strategy (deferred ideas section)
- Background-service deterministic-completion helper pattern (deferred ideas section)
- `/gsd-ship` pre-flight automated gate (deferred ideas section)
- CI bootstrap (deferred ideas section)

## Decisions That Were User-Visionary (Not Claude-Discretion)

- D-03 (no `[Skip]` additions) — user's hard line.
- D-07 (delete dead-premise test) — explicit user authorization for test deletion.
- D-15 (real bug ships in 999.6) — user's scope call.

## Next Steps

`/clear` then `/gsd-plan-phase 999.6` to generate PLAN.md files (3 expected per D-01).

---

## Execution Addendum — 2026-05-22 P01 Task 4 Branch B

**Decision:** D-06 Branch B taken — `PacketArtifactStoreRoundTripTests.cs` DELETED.

**Probe results:**
- `/tmp/arna-test/` — does not exist (No such file or directory)
- `/mnt/c/tmp/arna-test/` — does not exist (No such file or directory)

Both authorized probe paths missing the required 4 fixture files
(`00-input-summary.txt`, `01-request-context.txt`, `40-deck-profile.json`,
`51-set-upgrade-response.json`). No fixture source available to recover.

**Action:** Per D-06 authorized fallback clause ("If files aren't recoverable
from dev machine, delete the test instead with explicit DISCUSSION-LOG
entry — fixture-less integration assertion has no value"):
- Deleted `DeckFlow.Web.Tests/PacketArtifactStoreRoundTripTests.cs` (67 lines, 1 Fact).
- No csproj edit needed (no `Fixtures/arna-test/` directory created).
- No orphaned references — `grep -rn "PacketArtifactStoreRoundTripTests"` returns 0 hits after deletion.

**Total-count outcome:** Total drops from 444 (after D-07 deletion) to 443.
Branch B taken per Codex MEDIUM #7 forecast.

**Threat-model gate:** T-999.6-P01-01 (Information Disclosure on fixture
contents) is N/A under Branch B — no fixture files committed to repo.
