---
phase: 31-deck-primer-generator
plan: 04
type: execute
wave: 3
depends_on: ["31-01", "31-03"]
files_modified:
  - DeckFlow.Web/Services/PromptBuilders/Primer/ChatGptPrimerPromptVariant.cs
  - DeckFlow.Web/Services/PromptBuilders/Primer/ClaudePrimerPromptVariant.cs
  - DeckFlow.Web/Services/PromptBuilders/Primer/GeminiPrimerPromptVariant.cs
  - DeckFlow.Web/Program.cs
  - DeckFlow.Web.Tests/PrimerPromptVariantTests.cs
requirements: [PRM-05, PRM-06, PRM-08, PRM-09]
autonomous: true
must_haves:
  truths:
    - "Three FULLY DECOUPLED variant files (ChatGPT/Claude/Gemini) each own their complete prompt prose — no shared constants, no shared base class, no shared prose helper (ADR 0001 / a1fa5ad revert lesson)"
    - "Each variant renders from the SHARED platform-agnostic grounded payload the service assembles (combos/category/matchups/sections) — variants FORMAT, they do not re-fetch (data sharing allowed, prose sharing forbidden)"
    - "The service renders only ENABLED platform variants (ChatGPT + Claude always; Gemini ONLY when DECKFLOW_GEMINI_ENABLED) per the existing _AiSelector gate — the registry still holds all three variant impls, but 31-03's BuildAsync enumerates the enabled set; this plan does NOT force-expose Gemini"
    - "Each variant renders the D-2 two-block combo grounding (ground-truth heading + separate speculative-synergies heading) from the service-provided CommanderSpellbookResult?, including the null-disclosure path (PRM-05)"
    - "Each variant routes matchups by bracket: bracket 5 uses the service-provided EdhTop16 named archetypes (or, when top16Entries is null because the OQ-2 verdict was per-commander-only, the SAME 5 static generic buckets as brackets 1-4); brackets 1-4 emit the 5 static generic buckets (Aggro/Control/Midrange/Combo/Stax-Hate) (PRM-06)"
    - "Combo ordering honors the PRM-08 branch the service selected from the spike verdict (priority-ranked lines vs AI-rank instruction)"
    - "GeminiPrimerPromptVariant applies a DefensivePromptCharCap (threshold from 31-SPIKE.md, placeholder 50000 chars) trimming lowest-priority sections with a disclosure line — NEVER a hard disable; ChatGPT/Claude unaffected; the cap only matters when Gemini is an enabled platform (D-4)"
    - "Program.cs registers all three IPrimerPromptVariant impls + PrimerPromptVariantRegistry + DeckPrimerPacketService (AddScoped, factory lambda for the internal ctor, resolving IOptions<AiPlatformOptions>) without disturbing existing registrations"
  artifacts:
    - path: "DeckFlow.Web/Services/PromptBuilders/Primer/GeminiPrimerPromptVariant.cs"
      provides: "Gemini variant with DefensivePromptCharCap trim-to-fit + disclosure (D-4); only rendered when Gemini enabled"
      contains: "DefensivePromptCharCap"
    - path: "DeckFlow.Web/Services/PromptBuilders/Primer/ChatGptPrimerPromptVariant.cs"
      provides: "ChatGPT variant — D-2 two-block combos + bracket matchups + section rendering"
      contains: "Known Combos (ground truth — do not speculate)"
    - path: "DeckFlow.Web/Services/PromptBuilders/Primer/ClaudePrimerPromptVariant.cs"
      provides: "Claude variant — XML-tagged structure, no char cap"
      contains: "IPrimerPromptVariant"
    - path: "DeckFlow.Web/Program.cs"
      provides: "DI registration of variants + registry + AddScoped DeckPrimerPacketService (factory resolves IOptions<AiPlatformOptions>)"
      contains: "PrimerPromptVariantRegistry"
    - path: "DeckFlow.Web.Tests/PrimerPromptVariantTests.cs"
      provides: "Per-variant two-block, null-disclosure, bracket-routing, and Gemini-cap-trim tests"
      exports: ["AllVariants_EmitTwoComboBlocks", "AllVariants_NullCombos_EmitDisclosure", "NonCedh_EmitsGenericBuckets", "Gemini_OverCap_TrimsWithDisclosure"]
  key_links:
    - from: "Program.cs"
      to: "ChatGpt/Claude/GeminiPrimerPromptVariant"
      via: "AddSingleton<IPrimerPromptVariant, ...> x3 + AddSingleton<PrimerPromptVariantRegistry>"
      pattern: "IPrimerPromptVariant"
    - from: "GeminiPrimerPromptVariant"
      to: "31-SPIKE.md byte-size verdict"
      via: "DefensivePromptCharCap threshold"
      pattern: "DefensivePromptCharCap"
    - from: "each variant"
      to: "CommanderSpellbookResult? (service-provided shared payload)"
      via: "D-2 two-block render + null disclosure"
      pattern: "Speculative Synergies"
---

<objective>
The three FULLY DECOUPLED prompt-variant files (ChatGPT / Claude / Gemini) that format the SHARED grounding payload `DeckPrimerPacketService` assembles, plus the DI wiring in `Program.cs` (variants + registry + AddScoped service). Each variant renders the D-2 two-block combo grounding with null disclosure (PRM-05), bracket-routed matchups (EdhTop16 named archetypes for bracket 5 when the spike found a meta-wide query, the 5 generic buckets otherwise — including bracket 5 degradation — PRM-06), and combo ordering per the PRM-08 branch the service selected from the spike. The Gemini variant adds the D-4 `DefensivePromptCharCap` trim-to-fit guard (threshold from 31-SPIKE.md). The service (31-03) decides WHICH variants to render — only ENABLED platforms per the existing `_AiSelector` gate (ChatGPT + Claude always, Gemini iff `DECKFLOW_GEMINI_ENABLED`); this plan registers all three impls but does NOT force-expose Gemini.

Purpose: This completes the prompt-generation path. ADR 0001 is the load-bearing invariant: the three files share NO prose, NO constants, NO base class — content changes are hand-applied to all three (the a1fa5ad consolidation attempt was reverted same-day). Code review must NOT flag cross-variant near-duplicate prose as a finding.

Output: `ChatGptPrimerPromptVariant`, `ClaudePrimerPromptVariant`, `GeminiPrimerPromptVariant`, `Program.cs` DI block, and per-variant tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-deck-primer-generator/31-CONTEXT.md
@.planning/phases/31-deck-primer-generator/31-RESEARCH.md
@.planning/phases/31-deck-primer-generator/31-PATTERNS.md
@.planning/phases/31-deck-primer-generator/31-SPIKE.md

<interfaces>
<!-- Confirmed against live code 2026-06-08. Executor uses these directly. -->

IPrimerPromptVariant + PrimerPromptVariantRegistry + CategoryDistributionSummary (from 31-03, DeckFlow.Web/Services/PromptBuilders/Primer/):
  - internal interface; Platform getter; Build(request, decklistText, selectedSections, comboResult, top16Entries, categoryDistribution, bracketNumber, ct)
  - The service assembles the SHARED platform-agnostic payload once and calls Build per enabled platform (31-03 BuildAsync). Variants receive identical block inputs; only the prose framing differs.
  - DeckPrimerPacketService exposes internal BuildComboReferenceText + any NormalizeSingleLine helper the variants reuse for DATA assembly (data sharing is allowed; PROSE sharing is forbidden)

Enabled-platform gate (HIGH-2/HIGH-3 — DECISION LIVES IN 31-03, NOT HERE):
  - 31-03 BuildAsync enumerates AiPlatform.All and skips Gemini when !AiPlatformOptions.GeminiEnabled (mirrors _AiSelector). This plan registers all three variant impls in DI; the registry simply holds them. Do NOT add a flag check inside the variant files, and do NOT modify _AiSelector. The Gemini variant is only ever invoked when the service deems Gemini enabled.

Analysis variant analogs (decoupled prose precedent):
  - ChatGptAnalysisPromptVariant.cs — markdown-headed, fenced JSON output header
  - ClaudeAnalysisPromptVariant.cs — XML-tagged structure; NO <result> wrapper (stripped Phase 999.5-04); MDFC-land guidance intentionally differs from ChatGPT/Gemini (do NOT unify — memory 3186 revert)
  - GeminiAnalysisPromptVariant.cs — line 17 `private const int DefensivePromptCharCap = 50000;`; persona scaffold header ("You are an expert Magic: The Gathering analyst...") + "Think carefully through the problem before responding." (lines 49-52); section-level trim check (lines 259-274); EstimateExpertContextLength helper (279-293)

D-2 two-block structure (each variant emits, sourced from comboResult):
  - "## Known Combos (ground truth — do not speculate)" (null/empty comboResult -> "No verified combos available — treat all synergies as speculative." + "(Commander Spellbook API was unreachable at generation time.)")
  - separate "## Speculative Synergies (you propose)" ask
  - separate "## Near-Combos (one card away)" sub-block (cap 15)
  Prefer to reuse the service's internal BuildComboReferenceText for the data, BUT the surrounding prose/headers framing differs per variant — keep that prose in each variant file.

PRM-06 matchups:
  - bracket == 5 (cEDH) AND top16Entries != null: render named archetypes from top16Entries (archetype Name/Type) and instruct matchup analysis by name
  - bracket == 5 AND top16Entries == null (OQ-2 verdict was per-commander-only, no meta-wide query): emit the SAME 5 static generic buckets as brackets 1-4 (accepted degradation — NOT a special "degraded prose" string; reuse the bucket block)
  - bracket 1-4: 5 STATIC generic buckets — Aggro (go-wide/combat/fast damage), Control (permission/wraths/value), Midrange (creature value/incremental), Combo (infinite loops before turn 8), Stax/Hate (resource denial/tax/lock). These static strings live IN EACH variant (decoupled).

D-4 Gemini cap (units): GeminiPrimerPromptVariant uses CHAR count (builder.Length) for the cap, consistent with the analysis variant. AiPlatform.Gemini.PasteWarningBytes (32_768) is BYTES for the UI warning — a separate check (Pitfall 5). Threshold from 31-SPIKE.md; placeholder 50000 chars if the spike deferred the exact number.

Program.cs DI block (PATTERNS lines 289-326): add after the existing MetaGap variant block:
  - AddSingleton<IPrimerPromptVariant, ChatGptPrimerPromptVariant>(); (x3 for the three variants)
  - AddSingleton<PrimerPromptVariantRegistry>();
  - AddScoped<IDeckPrimerPacketService>(sp => new DeckPrimerPacketService(... resolved deps ...)) — factory lambda because the service uses an internal ctor. Resolve IMoxfieldDeckImporter, IArchidektDeckImporter, MoxfieldParser, ArchidektParser, ICommanderSpellbookService, IEdhTop16Client, ICategoryKnowledgeStore, PrimerPromptVariantRegistry, PacketSessionCache, IOptions<AiPlatformOptions> (HIGH-3 — the enabled-set gate), ILogger<DeckPrimerPacketService>.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ChatGpt + Claude primer variants (decoupled)</name>
  <files>DeckFlow.Web/Services/PromptBuilders/Primer/ChatGptPrimerPromptVariant.cs, DeckFlow.Web/Services/PromptBuilders/Primer/ClaudePrimerPromptVariant.cs</files>
  <read_first>
    - DeckFlow.Web/Services/PromptBuilders/Analysis/ChatGptAnalysisPromptVariant.cs (FULL — header style, fenced-output framing, section rendering)
    - DeckFlow.Web/Services/PromptBuilders/Analysis/ClaudeAnalysisPromptVariant.cs (FULL — XML-tag structure, no <result> wrapper, MDFC guidance divergence)
    - DeckFlow.Web/Services/PromptBuilders/Primer/IPrimerPromptVariant.cs + DeckPrimerPacketService.cs (from 31-03 — Build signature, internal BuildComboReferenceText)
    - DeckFlow.Web/Models/PrimerSectionCatalog.cs (PrimerSectionEntry shape for section rendering)
    - .planning/phases/31-deck-primer-generator/31-SPIKE.md (combo verdict — ordering the service already applied; variant just renders)
    - .planning/phases/31-deck-primer-generator/31-PATTERNS.md (ChatGptPrimerPromptVariant.cs + ClaudePrimerPromptVariant.cs sections; ADR 0001 decoupling)
    - docs/decisions/0001-prompt-variants-decoupled.md
  </read_first>
  <action>
    Create `ChatGptPrimerPromptVariant.cs` (`internal sealed class ChatGptPrimerPromptVariant : IPrimerPromptVariant`, namespace `DeckFlow.Web.Services.PromptBuilders.Primer;`). `public AiPlatform Platform => AiPlatform.ChatGpt;`. `Build(...)` assembles the full ChatGPT-formatted primer prompt:
      - markdown-headed sections, iterating the provided `selectedSections` (render each section's directive + the section's intent; honor section grouping order Identity->Combos->Gameplay->Matchups->Maintenance).
      - D-2 combos: emit "## Known Combos (ground truth — do not speculate)" populated from comboResult (null -> the disclosure line), then a SEPARATE "## Speculative Synergies (you propose)" ask, then a separate "## Near-Combos (one card away)" block. Reuse the service's internal BuildComboReferenceText for the DATA lines if convenient; the ChatGPT-specific framing prose stays in this file.
      - PRM-06 matchups: bracket 5 + top16Entries -> named archetypes; bracket 5 + null (per-commander-only verdict) -> the 5 static generic buckets (same block as brackets 1-4); brackets 1-4 -> the 5 static generic buckets (strings live in THIS file).
      - category distribution: render the CategoryDistributionSummary block only when non-null (PRM-07 already omitted on empty upstream).
      - fenced JSON output instruction at the end (ChatGPT house style).

    Create `ClaudePrimerPromptVariant.cs` (`internal sealed class ClaudePrimerPromptVariant : IPrimerPromptVariant`). `Platform => AiPlatform.Claude`. Build the SAME logical content but in Claude's XML-tagged house style (e.g. `<deck_primer>...</deck_primer>` wrapper, `<primer_output>` not `<result>`). MDFC-land / formatting guidance may differ from ChatGPT deliberately — do NOT unify. NO DefensivePromptCharCap. Same D-2 two-block + bracket-routing (incl. bracket-5 null -> generic buckets) + category rules, with Claude-specific framing prose IN THIS FILE.

    CRITICAL ADR 0001: the two files share NO prose constant, NO base class, NO shared header helper. Generic-bucket strings, section directives, and combo framing are duplicated by design. Only the service's DATA helpers (BuildComboReferenceText, NormalizeSingleLine) are shared. XML docs (CS1591). LF; Allman; 4-space.
  </action>
  <verify>
    <automated>"/mnt/c/Program Files/dotnet/dotnet.exe" build DeckFlow.Web/DeckFlow.Web.csproj -warnaserror:CS1591 2>&1 | grep -E "Build succeeded|error" | head</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "internal sealed class ChatGptPrimerPromptVariant : IPrimerPromptVariant" DeckFlow.Web/Services/PromptBuilders/Primer/ChatGptPrimerPromptVariant.cs` matches; `Platform => AiPlatform.ChatGpt`
    - `grep -n "internal sealed class ClaudePrimerPromptVariant : IPrimerPromptVariant" DeckFlow.Web/Services/PromptBuilders/Primer/ClaudePrimerPromptVariant.cs` matches; `Platform => AiPlatform.Claude`
    - Each file emits both D-2 headings: `grep -c "Known Combos (ground truth — do not speculate)\|Speculative Synergies (you propose)" <file>` >= 2 per file
    - Each file contains the 5 generic bucket names (Aggro/Control/Midrange/Combo/Stax) — duplicated by design (ADR 0001) — and the bracket-5-null path falls through to them
    - No shared base class / shared prose const referenced by both variant files (manual ADR 0001 check)
    - `dotnet build DeckFlow.Web -warnaserror:CS1591` reports Build succeeded, 0 errors
  </acceptance_criteria>
  <done>ChatGPT + Claude primer variants render the D-2 two-block combos (with null disclosure), bracket-routed matchups (bracket-5-null falls back to generic buckets), and section content in their own decoupled house styles; Web builds clean.</done>
</task>

<task type="auto">
  <name>Task 2: Gemini primer variant with DefensivePromptCharCap (D-4) + Program.cs DI wiring</name>
  <files>DeckFlow.Web/Services/PromptBuilders/Primer/GeminiPrimerPromptVariant.cs, DeckFlow.Web/Program.cs</files>
  <read_first>
    - DeckFlow.Web/Services/PromptBuilders/Analysis/GeminiAnalysisPromptVariant.cs (FULL — DefensivePromptCharCap line 17, persona scaffold 49-52, section-level trim check 259-274, EstimateExpertContextLength 279-293)
    - DeckFlow.Web/Services/PromptBuilders/Primer/ChatGptPrimerPromptVariant.cs (from Task 1 — same logical content, DIFFERENT prose)
    - DeckFlow.Web/Program.cs (lines 289-326 — existing analysis/comparison/metagap variant + service registrations to mirror; the DeckAnalysisPacketService factory for the IOptions<AiPlatformOptions> resolution pattern)
    - DeckFlow.Web/Services/DeckPrimerPacketService.cs (from 31-03 — ctor parameter list incl. IOptions<AiPlatformOptions> for the factory lambda)
    - .planning/phases/31-deck-primer-generator/31-SPIKE.md (DefensivePromptCharCap recommended threshold)
    - .planning/phases/31-deck-primer-generator/31-PATTERNS.md (GeminiPrimerPromptVariant.cs + Program.cs sections; Pitfall 5)
  </read_first>
  <action>
    Create `GeminiPrimerPromptVariant.cs` (`internal sealed class GeminiPrimerPromptVariant : IPrimerPromptVariant`). `Platform => AiPlatform.Gemini`. Include the Gemini persona-scaffold header ("You are an expert Magic: The Gathering analyst...") + "Think carefully through the problem before responding." (Gemini-specific — do NOT copy ChatGPT's header). Render the SAME logical primer content (D-2 two-block combos with null disclosure, bracket-routed matchups with the 5 generic buckets incl. bracket-5-null fallthrough, category block, sections) in Gemini's own decoupled prose. (This variant is only invoked when the service deems Gemini enabled — no flag check inside the file.)

    D-4 DefensivePromptCharCap: `private const int DefensivePromptCharCap = 50000;` with a `// Why: D-4 — placeholder; replace with the 31-SPIKE.md recommended char cap` comment (use the spike's recommended value if recorded). Build sections in priority order (Identity -> Combos -> Gameplay -> Matchups -> Maintenance). Use CHAR count (`builder.Length`, NOT bytes — Pitfall 5). When appending a section would exceed the cap, SKIP it and record its label; after processing, if any sections were skipped, append a disclosure: "[Sections omitted due to Gemini paste limit: {list}. Re-run with fewer sections selected.]". NEVER a hard disable — ChatGPT/Claude are unaffected. Trim lowest-priority sections first (Maintenance > Matchups > optional Gameplay), never the combo ground-truth block.

    In `Program.cs`, after the existing MetaGap variant block (~line 308), add:
      - `builder.Services.AddSingleton<IPrimerPromptVariant, ChatGptPrimerPromptVariant>();` `...ClaudePrimerPromptVariant>();` `...GeminiPrimerPromptVariant>();`
      - `builder.Services.AddSingleton<PrimerPromptVariantRegistry>();`
      - `builder.Services.AddScoped<IDeckPrimerPacketService>(sp => new DeckPrimerPacketService(...))` resolving each ctor dep via `sp.GetRequiredService<...>()` (including `sp.GetRequiredService<IOptions<AiPlatformOptions>>()` for the enabled-set gate, and `sp.GetService<ILogger<DeckPrimerPacketService>>()` for the optional logger), mirroring the DeckAnalysisPacketService registration. Factory lambda is required because the service exposes an internal ctor.
    Add any needed `using DeckFlow.Web.Services.PromptBuilders.Primer;` / `using Microsoft.Extensions.Options;` to Program.cs (if not already present). Touch only the inserted lines; do not reorder existing registrations. LF; Allman; 4-space.
  </action>
  <verify>
    <automated>"/mnt/c/Program Files/dotnet/dotnet.exe" build DeckFlow.Web/DeckFlow.Web.csproj -warnaserror:CS1591 2>&1 | grep -E "Build succeeded|error" | head</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "private const int DefensivePromptCharCap" DeckFlow.Web/Services/PromptBuilders/Primer/GeminiPrimerPromptVariant.cs` matches; the cap check uses `builder.Length` (chars, not bytes)
    - `grep -n "Sections omitted due to Gemini paste limit" DeckFlow.Web/Services/PromptBuilders/Primer/GeminiPrimerPromptVariant.cs` matches the disclosure line
    - `grep -c "AddSingleton<IPrimerPromptVariant" DeckFlow.Web/Program.cs` == 3
    - `grep -n "AddSingleton<PrimerPromptVariantRegistry>" DeckFlow.Web/Program.cs` matches; `grep -n "AddScoped<IDeckPrimerPacketService>" DeckFlow.Web/Program.cs` matches; the factory resolves `IOptions<AiPlatformOptions>`
    - The Gemini variant shares no prose const/base class with ChatGpt/Claude variants (ADR 0001) and contains no DECKFLOW_GEMINI_ENABLED / GeminiEnabled flag check (the gate lives in 31-03)
    - `dotnet build DeckFlow.Web -warnaserror:CS1591` reports Build succeeded, 0 errors
  </acceptance_criteria>
  <done>Gemini variant trims lowest-priority sections to its char cap with a disclosure (D-4, never a hard disable); all three variants + the registry + the AddScoped service (factory resolving IOptions<AiPlatformOptions>) are DI-registered; Web builds clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Per-variant prompt tests</name>
  <files>DeckFlow.Web.Tests/PrimerPromptVariantTests.cs</files>
  <read_first>
    - DeckFlow.Web.Tests (existing analysis-variant tests if present — construction + assertion pattern)
    - DeckFlow.Web/Services/PromptBuilders/Primer/*.cs (the three variants from Tasks 1-2)
    - DeckFlow.Web/Services/CommanderSpellbookService.cs (CommanderSpellbookResult/SpellbookCombo fixtures)
    - .planning/phases/31-deck-primer-generator/31-PATTERNS.md (test conventions)
  </read_first>
  <behavior>
    - AllVariants_EmitTwoComboBlocks: each variant, given a comboResult with >=1 combo, emits both the ground-truth heading and the separate speculative heading.
    - AllVariants_NullCombos_EmitDisclosure: each variant, given null comboResult, emits the D-2 disclosure line (PRM-05).
    - Bracket5_WithEntries_EmitsNamedArchetypes: each variant, bracket 5 + top16Entries, includes the archetype names.
    - Bracket5_NullEntries_EmitsGenericBuckets: bracket 5 + null entries (per-commander-only path) emits the 5 generic bucket names, no crash.
    - NonCedh_EmitsGenericBuckets: brackets 1-4 emit all 5 generic bucket names; no EdhTop16 names.
    - Gemini_OverCap_TrimsWithDisclosure: feed enough sections to exceed DefensivePromptCharCap; assert the Gemini output contains the omit-disclosure line and is <= cap (or at most one block over for the protected ground-truth block).
    - ChatGpt_And_Claude_NoCap_DoNotTrim: the same oversized input is NOT trimmed by ChatGPT/Claude variants (D-4 is Gemini-only).
  </behavior>
  <action>
    Create `DeckFlow.Web.Tests/PrimerPromptVariantTests.cs` (`public sealed class PrimerPromptVariantTests`, namespace `DeckFlow.Web.Tests`). Instantiate each variant directly (they are `internal`; the test project already has InternalsVisibleTo). Build fixtures: a DeckPrimerRequest, a selectedSections list (use PrimerSectionCatalog.AllSections), a CommanderSpellbookResult with combos, EdhTop16 entries (named archetypes), and a CategoryDistributionSummary. Implement the behaviors as xUnit `[Fact]`/`[Theory]`. Include `Bracket5_NullEntries_EmitsGenericBuckets` to prove the per-commander-only degradation reuses the generic buckets. For the Gemini cap test, pass all 31 sections + a large combo set to exceed the cap and assert the disclosure + length bound; assert ChatGPT/Claude produce a longer untrimmed output for the same input. LF; no formatter sweep.
  </action>
  <verify>
    <automated>"/mnt/c/Program Files/dotnet/dotnet.exe" test DeckFlow.Web.Tests/DeckFlow.Web.Tests.csproj --filter "PrimerPromptVariantTests" 2>&1 | grep -E "Passed!|Failed!|error" | head</automated>
  </verify>
  <acceptance_criteria>
    - File DeckFlow.Web.Tests/PrimerPromptVariantTests.cs exists with tests covering all three variants
    - `AllVariants_NullCombos_EmitDisclosure` asserts the D-2 disclosure for ChatGPT, Claude, AND Gemini
    - `NonCedh_EmitsGenericBuckets` asserts all 5 bucket names for a 1-4 bracket; `Bracket5_NullEntries_EmitsGenericBuckets` asserts the same for the bracket-5 degradation path
    - `Gemini_OverCap_TrimsWithDisclosure` asserts the omit-disclosure line AND that ChatGPT/Claude do NOT trim the same input
    - `dotnet test --filter "PrimerPromptVariantTests"` reports Passed with 0 failures
  </acceptance_criteria>
  <done>All three variants render D-2 two-block combos + null disclosure + bracket-routed matchups (incl. bracket-5 generic-bucket degradation); the Gemini char-cap trim-with-disclosure is proven and shown to be Gemini-only; tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| service-provided blocks -> variant prose | Combo/archetype/category text (upstream-derived) rendered into the prompt; framed as data, not instructions |
| variant output -> user clipboard/AI | Final prompt string; no execution, no eval |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-11 | Tampering | Prompt-injection text embedded in combo/archetype data masquerading as the speculative directive | mitigate | D-2 structural separation preserved in EVERY variant — ground-truth block is fenced and labeled "do not speculate"; the speculative ask is a distinct heading the variant emits itself, not derived from upstream text |
| T-31-12 | Denial of Service | Oversized primer overflowing Gemini paste limit | mitigate | Gemini DefensivePromptCharCap trims lowest-priority sections with a disclosure; char-count check (not byte) per Pitfall 5; ChatGPT/Claude bounded by the 31-section catalog + AlmostIncluded cap |
| T-31-13 | Tampering | Cross-variant prose consolidation silently altering all three outputs | mitigate | ADR 0001 enforced: no shared prose const/base class; tests assert each variant independently; review must not flag duplication |
| T-31-SC | Tampering | npm/pip/cargo installs | accept | Zero package installs (RESEARCH §Package Legitimacy Audit: not applicable) |
</threat_model>

<verification>
- `dotnet build DeckFlow.Web -warnaserror:CS1591` clean
- `dotnet build DeckFlow.Web.Tests` clean
- `dotnet test --filter "PrimerPromptVariantTests"` green
- 3x AddSingleton<IPrimerPromptVariant> + registry + AddScoped service (factory resolving IOptions<AiPlatformOptions>) registered (greps)
- Gemini cap uses char count; ChatGPT/Claude have no cap (ADR 0001 + D-4); no flag check inside variant files (gate lives in 31-03)
</verification>

<success_criteria>
PRM-05 (D-2 two-block combos + null disclosure in every variant), PRM-06 (bracket-routed matchups per variant, incl. bracket-5 generic-bucket degradation), PRM-08 (rendered ordering honors the service-selected branch), and the variant side of PRM-09 (per-AI artifact prompts for enabled platforms) are delivered across three fully decoupled files (ADR 0001), wired into DI, with the Gemini D-4 char-cap guard. The service renders only enabled platforms (Gemini iff DECKFLOW_GEMINI_ENABLED); ChatGPT/Claude are unaffected by the cap.
</success_criteria>

<output>
Create `.planning/phases/31-deck-primer-generator/31-04-SUMMARY.md` when done.
</output>
