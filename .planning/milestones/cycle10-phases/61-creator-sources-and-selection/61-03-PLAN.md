---
phase: 61-creator-sources-and-selection
plan: 03
type: execute
wave: 2
depends_on: [01, 02]
files_modified:
  - DeckFlow.Studio/Pages/Harvest.razor
  - DeckFlow.Studio/Program.cs
  - DeckFlow.Studio.Tests/HarvestPageTests.cs
  - README.md
autonomous: true
requirements: [HSEL-01, HSEL-02]
must_haves:
  truths:
    - "The browsed channel-video list defaults to showing only NotHarvested videos (excludes Harvested/Distilled/Approved/Published/Blocked/Duplicate)"
    - "A 'Show all' toggle reveals every video regardless of status; toggling back restores the unharvested-only default"
    - "Skipped videos are excluded from the selection list in BOTH default and show-all views"
    - "Each candidate row has a Skip action that adds the video to SkippedVideoStore and removes it from selection, without deleting artifacts or writing blocked_videos (distinct from Block)"
    - "Skip reuses the Plan-01 SkippedVideoStore via DI; the Block action and blocklist are unchanged"
    - "(Codex HIGH) A single canonical visible projection (e.g. GetVisibleChannelVideos) drives rendering, Select-All, AND the set harvested — a row hidden by the filter or by skip can NEVER be harvested even if it was selected before being hidden"
    - "(Codex LOW) The 61-02 creator dropdown + paste-URL fallback still render and function after this plan's edits to Harvest.razor"
  artifacts:
    - path: "DeckFlow.Studio/Pages/Harvest.razor"
      provides: "Unharvested-only default + Show-all toggle + per-row Skip action over the browse list"
      contains: "Show all"
    - path: "DeckFlow.Studio/Program.cs"
      provides: "ISkippedVideoStore singleton + EnsureSchema at startup"
      contains: "SkippedVideoStore"
    - path: "DeckFlow.Studio.Tests/HarvestPageTests.cs"
      provides: "bUnit: default hides harvested + skipped; toggle shows all; Skip removes a row + writes the skip store; Block path unaffected"
      contains: "Skip"
  key_links:
    - from: "DeckFlow.Studio/Pages/Harvest.razor"
      to: "DeckFlow.Core.Content.VideoStatus"
      via: "default filter on NotHarvested; Show-all toggle disables the filter"
      pattern: "VideoStatus"
    - from: "DeckFlow.Studio/Pages/Harvest.razor"
      to: "DeckFlow.Core.Content.ISkippedVideoStore"
      via: "Skip action AddSkipAsync; selection excludes ListSkippedAsync"
      pattern: "ISkippedVideoStore"
---

<objective>
Deliver HSEL-01 + HSEL-02 on the Harvest browse list: default to showing only not-yet-harvested
videos (with a "Show all" toggle), and add a per-row Skip action — lighter than Block — that hides a
candidate from selection by writing the Plan-01 `SkippedVideoStore`, with skipped videos excluded
from both views. Reuse the existing `VideoStatus` the browse already attaches; do not duplicate
status logic; leave Block and the blocklist untouched.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/61-creator-sources-and-selection/61-CONTEXT.md
@./CLAUDE.md
@DeckFlow.Studio/Pages/Harvest.razor
@DeckFlow.Core/Content/VideoStatus.cs
@DeckFlow.Studio/Program.cs
</context>

<tasks>
1. Register `ISkippedVideoStore -> SkippedVideoStore(contentKbDatabasePath)` as a singleton in
   `Program.cs` + EnsureSchemaAsync at startup.
2. Harvest browse — CANONICAL VISIBLE PROJECTION (Codex HIGH): introduce one method, e.g.
   `GetVisibleChannelVideos()`, returning the filtered set (default = status `NotHarvested` AND not
   skipped; "Show all" disables only the status filter, skip stays excluded). Route ALL THREE of these
   through it: (a) the rendered table loop (currently `@foreach (var vm in _channelVideos)` at
   Harvest.razor:83), (b) `ToggleAllChannelSelections()` (Harvest.razor:992), and (c)
   `GetAllSelectedVideos()` (Harvest.razor:1115) so the harvested set is the intersection of selected
   AND visible. A row that was selected in Show-all then hidden by toggling back (or by Skip) must NOT
   be harvested. Load the skip set via `ListSkippedAsync` when building the view.
3. Per-row Skip action (next to Block): calls `AddSkipAsync(videoId)`, then drops the row from the
   visible projection (and clears any selection on it). Sanitized feedback. MUST NOT hard-delete
   artifacts or write blocked_videos. Reuse existing per-status row guards (Block disabled on
   already-blocked) unchanged.
4. README: document the unharvested-only default + Show-all toggle + Skip (vs Block) in the Studio
   section.
5. bUnit (extend HarvestPageTests):
   - default view hides a Harvested row and a skipped row;
   - Show-all reveals the Harvested row but still hides the skipped one;
   - Skip removes a row and calls the skip store;
   - (Codex HIGH) select a row in Show-all, toggle back to default so it is hidden, then trigger
     harvest and assert that row is NOT in the harvested set / count;
   - (Codex LOW) the 61-02 creator dropdown + paste-URL fallback still render and work;
   - the existing Block action/test still passes. Use fake stores.
6. Build Studio + run Studio.Tests green (note the BlockedPageTests isolation flake).
</tasks>

## Trust Boundaries
Skip writes operator-chosen video ids to skipped_videos (Plan 01, parameterized). No external calls
beyond the existing browse. The filter is display-only and must not alter persisted data.

## STRIDE Threat Register
- **Integrity (T-61-06, HSEL-02 invariant):** Skip must be observably distinct from Block — no
  artifact delete, no blocked_videos write. Mitigation: Skip calls only SkippedVideoStore; bUnit
  asserts the Block path/store is untouched by a Skip.
- **Tampering (T-61-07):** the Show-all toggle must not expose a way to act on Blocked/Duplicate rows
  in a manner the current UI forbids. Mitigation: reuse existing per-status row guards (e.g. Block
  disabled on already-blocked) unchanged.
