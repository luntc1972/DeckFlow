{
  "name": "deckflow-web",
  "private": true,
  "version": "1.0.0",
  "description": "Browser TypeScript build + tests for DeckFlow.Web",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "typescript": "6.0.2",
    "vitest": "^3.0.0",
    "jsdom": "^26.0.0"
  }
}
