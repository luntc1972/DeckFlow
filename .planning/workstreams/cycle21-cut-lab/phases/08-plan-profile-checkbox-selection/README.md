# Phase 8: Plan Profile — Checkbox Plan Selection

Adopted 2026-08-02. Not yet planned.

Design spec (approved, research-validated):
`.planning/specs/2026-08-02-cutlab-plan-profile-design.md`

Sequencing: engine plans are independent/parallel; the plan-panel UI plan is gated on
Phase 7 (rewrites the same `CutLab.cshtml` / `wwwroot/ts/cut-lab.ts`). Phase 7 reserves
an empty wizard step slot for this panel.

Internal phasing from the spec: P1 generic strategies + protect/reorder (no HTTP) →
P2 EDHREC commander themes + pre-check → P3 floor deltas + off-plan finding.
