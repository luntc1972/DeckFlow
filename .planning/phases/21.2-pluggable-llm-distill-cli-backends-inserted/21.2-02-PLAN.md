---
phase: 21.2-pluggable-llm-distill-cli-backends-inserted
plan: 02
type: execute
wave: 2
depends_on: ["21.2-01"]
files_modified:
  - DeckFlow.CLI/CommandRunners.cs
  - DeckFlow.Core.Tests/RunDistillAsyncTests.cs
  - docs/ops/content-kb-llm-cli-backends.md
  - README.md
  - .planning/phases/21.2-pluggable-llm-distill-cli-backends-inserted/21.2-02-SUMMARY.md
autonomous: false
requirements: [KB-10, KB-11]

must_haves:
  truths:
    - "With DECKFLOW_LLM_PROVIDER unset, RunDistillAsync behaves byte-identically to shipped Phase 21 — ledger cap-gate + token pricing active, existing RunDistillAsyncTests stay green (regression guard)"
    - "With DECKFLOW_LLM_PROVIDER=claude, the distiller is the CLI backend, the LlmSpendLedger cap-gate is bypassed at all 4 WouldExceedCapAsync sites, AND ComputeProjectedVideoCostUsd / ComputeProjectedCallCostUsd / ComputeCostUsd pricing math is NOT executed for subscription providers (M1)"
    - "Dry-run for a subscription provider reports '$0, subscription' with no cap math (no ComputeProjectedVideoCostUsd call) and no false cap-hit"
    - "The run record still writes a spend=0 row with the LLM call-count for observability (whisper_calls overload)"
    - "A WSL invocation AND a Windows invocation of the claude CLI backend are documented with EXACT commands, including the Windows-dotnet.exe-launched-from-WSL hard case and which process owns PATH/env, using the JSON-array {instruction}-placeholder override form (M4 + ROUND-3 HIGH-1)"
    - "A live claude-backed distill over the 10-video UAT db emits valid artifacts + slim-index rows + a spend=0 run record and an operator confirms E5/E6 quality — satisfying the Phase 21.1 gate"
  artifacts:
    - path: "DeckFlow.CLI/CommandRunners.cs"
      provides: "Provider-factory construction + isSubscriptionProvider-guarded ledger bypass (cap-gate AND pricing math) + dry-run copy"
      contains: "LlmDistillationProviderFactory.Resolve"
    - path: "docs/ops/content-kb-llm-cli-backends.md"
      provides: "Operator guide: provider env (openai|claude; codex→21.3), exact WSL + Windows commands incl. dotnet.exe-from-WSL + JSON-array {instruction} placeholder override, auth, ledger-bypass behavior"
      contains: "DECKFLOW_LLM_PROVIDER"
  key_links:
    - from: "RunDistillAsync (public)"
      to: "LlmDistillationProviderFactory.Resolve"
      via: "construction-site swap of new LlmDistillationService(...)"
      pattern: "LlmDistillationProviderFactory\\.Resolve"
    - from: "RunDistillAsync (public)"
      to: "DistillVideoAsync via isSubscriptionProvider flag"
      via: "provider env read once, threaded down"
      pattern: "isSubscriptionProvider"
    - from: "DistillVideoAsync"
      to: "LlmSpendLedger.WouldExceedCapAsync + ComputeProjectedVideoCostUsd"
      via: "guarded by !isSubscriptionProvider at all 4 cap-gate sites AND the pricing calls (M1)"
      pattern: "!isSubscriptionProvider"
---

<objective>
Wire the Plan 01 provider factory into the CLI distill command and bypass the spend ledger AND its pricing math for the subscription (claude) backend (M1 — not just the cap-gate). Swap the `new LlmDistillationService(...)` construction site for `LlmDistillationProviderFactory.Resolve`, thread an `isSubscriptionProvider` flag through `RunDistillAsync` -> `DistillVideoAsync`, guard all 4 `WouldExceedCapAsync` cap-gates, make `ComputeProjectedVideoCostUsd` / `ComputeProjectedCallCostUsd` / `ComputeCostUsd` conditional (M1), record cost 0 for subscription runs, and document the WSL and Windows claude invocations with EXACT commands including the Windows-dotnet.exe-from-WSL hard case and the JSON-array `{instruction}`-placeholder override (M4 + ROUND-3 HIGH-1). Then run the live claude-backed distill over the 10-video UAT db to clear the Phase 21.1 gate (a blocking human-verify checkpoint).

**SCOPE: CLAUDE-ONLY.** The wiring is provider-agnostic (isSubscriptionProvider = "not openai"), but `codex` is not a runnable backend in this phase — the factory throws for it (Phase 21.3). The ops doc and the UAT cover claude only; do NOT add codex invocation examples, codex isolation notes, or a codex UAT step.

Purpose: Complete KB-10 (factory wired into the run) and KB-11 (cross-platform claude docs + ledger bypass), and unblock the Phase 21.1 live-UAT gate that previously hit HTTP 429 insufficient_quota.
Output: 1 modified CLI runner, 1 updated test file, 1 new operator doc, README note, and a verified live UAT run.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21.2-pluggable-llm-distill-cli-backends-inserted/21.2-CONTEXT.md
@.planning/phases/21.2-pluggable-llm-distill-cli-backends-inserted/21.2-RESEARCH.md
@.planning/phases/21.2-pluggable-llm-distill-cli-backends-inserted/21.2-PATTERNS.md
@.planning/phases/21.2-pluggable-llm-distill-cli-backends-inserted/21.2-REVIEWS.md
@.planning/phases/21.2-pluggable-llm-distill-cli-backends-inserted/21.2-01-SUMMARY.md

<interfaces>
<!-- Exact change sites verified against the codebase at planning time (re-locate by symbol if drifted). -->

DeckFlow.CLI/CommandRunners.cs — change sites:
  - Public RunDistillAsync(FileInfo? db, int limit, bool dryRun, Serilog.ILogger, ct) at L470-510. Construction block at L482-488; `new LlmDistillationService(llmHttpClient)` at L488; calls internal RunDistillAsync at L490-502.
  - Internal RunDistillAsync(12 params: sourceStore, videoStore, indexStore, runStore, ledger, distiller, artifactRoot, limit, dryRun, logger, utcNow, ct) at L512-524 — orchestrates; calls DistillVideoAsync at L615-626; dry-run cap-check at L595-608; dry-run summary line later in the dry-run branch.
  - DistillVideoAsync (11 params) at L952-963 — 4 WouldExceedCapAsync cap-gates at L975, L990, L1019, L1047; 3 RecordCallAsync at L1009, L1037, L1065; each followed by `llmCalls++; llmSpend += <cost>;`.
  - Pricing helpers (openai-only; bypass for subscription per M1): `ComputeProjectedVideoCostUsd(...)` (called at L595 dry-run, L974 in DistillVideoAsync), `ComputeProjectedCallCostUsd(...)` (called INSIDE the L990/L1019/L1047 cap-gate conditions), `LlmSpendLedger.ComputeCostUsd(...)` (called at L1007/L1036/L1064 before each RecordCallAsync).
  - Downstream FilterTags at L1077-1079 — post-hoc silent filter. NOTE: with the claude backend, ValidateTags (Plan 01, H4) already rejected vocab-invalid output upstream inside the retry; FilterTags remains for the openai path. Do NOT remove FilterTags.

LlmDistillationProviderFactory (created in Plan 01, DeckFlow.Core.Integration):
  public const string EnvironmentVariableName = "DECKFLOW_LLM_PROVIDER";
  public static ILlmDistillationService Resolve(string? providerEnvValue, HttpClient httpClient);
  -- openai (default/unset) → LlmDistillationService; claude → CliLlmDistillationService; codex → NotSupportedException(Phase 21.3); unknown → NotSupportedException.

CliLlmDistillationService override contract (Plan 01, DeckFlow.Core.Integration) — for the ops doc:
  DECKFLOW_LLM_CLI_COMMAND is a JSON ARRAY of args (ROUND-3 HIGH-1), e.g. `["wsl.exe","claude","-p","{instruction}","--output-format","json","--allowedTools",""]`. element[0]=FileName, rest=ArgumentList verbatim (empty strings preserved as string.Empty); exactly one element must be the `{instruction}` placeholder. A non-JSON / empty-array / missing-or-duplicate-placeholder override fails fast with a clear error. DECKFLOW_LLM_CLI_TIMEOUT_SECONDS overrides the per-extraction timeout (default 10 min).

LlmSpendLedger (DeckFlow.Core/Content/LlmSpendLedger.cs):
  Task<bool> WouldExceedCapAsync(decimal projectedCallCostUsd, string monthKey, ct);
  Task RecordCallAsync(long videoId, int inputTokens, int outputTokens, decimal costUsd, string monthKey, ct);
  static decimal ComputeCostUsd(int inputTokens, int outputTokens);
  -- DECKFLOW_LLM_MONTHLY_CAP_USD governs the cap (openai only after this change).

RunDistillAsyncTests (DeckFlow.Core.Tests/RunDistillAsyncTests.cs):
  Calls the internal RunDistillAsync via a private RunAsync helper. Uses FakeLlmDistillationService + FakeLlmSpendLedger. These existing tests are the regression guard — they must keep passing with the openai-default branch (isSubscriptionProvider defaults false).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire provider factory + thread isSubscriptionProvider ledger bypass (cap-gate AND pricing math, M1) through the distill runner</name>
  <read_first>
    - DeckFlow.CLI/CommandRunners.cs L470-674 (public + internal RunDistillAsync, dry-run path) and L952-1099 (DistillVideoAsync, all cap-gates + RecordCallAsync + pricing-helper call sites)
    - DeckFlow.Core/Content/LlmSpendLedger.cs (WouldExceedCapAsync / RecordCallAsync / ComputeCostUsd signatures)
    - DeckFlow.Core/Integration/LlmDistillationProviderFactory.cs (from Plan 01 — Resolve signature + EnvironmentVariableName)
    - DeckFlow.Core.Tests/RunDistillAsyncTests.cs (RunAsync helper + FakeLlmSpendLedger/FakeLlmDistillationService — the regression-guard tests)
    - .planning/phases/21.2-pluggable-llm-distill-cli-backends-inserted/21.2-PATTERNS.md lines 435-538 (the 4 change-site edits for CommandRunners — apply the M1 correction below: ALSO guard the pricing calls)
    - .planning/phases/21.2-pluggable-llm-distill-cli-backends-inserted/21.2-REVIEWS.md M1 (RESOLVED in round 2/3 — preserve it) + Pitfall 5 (subscription path must NOT run ComputeProjectedVideoCostUsd)
  </read_first>
  <behavior>
    - Regression: openai default (env unset / "openai") — internal RunDistillAsync called with isSubscriptionProvider=false; all 4 WouldExceedCapAsync calls execute; pricing math runs; RecordCallAsync records real ComputeCostUsd; existing RunDistillAsyncTests stay green.
    - Subscription (claude): isSubscriptionProvider=true — all 4 WouldExceedCapAsync calls skipped; ComputeProjectedVideoCostUsd / ComputeProjectedCallCostUsd / ComputeCostUsd are NOT called (M1); RecordCallAsync records cost 0m; run record still written with whisper_calls=LLM call-count, spend_usd=0.
    - New test: a subscription-provider distill run with a FakeLlmSpendLedger whose WouldExceedCapAsync would return true STILL distills (cap-gate bypassed), records spend 0, and never invokes WouldExceedCapAsync (M1 short-circuit).
    - New test: dry-run with isSubscriptionProvider=true reports "$0, subscription", never reports a cap-hit, and does not call the cost projector.
  </behavior>
  <action>
    Edit `DeckFlow.CLI/CommandRunners.cs` per PATTERNS.md (lines 435-538) WITH the M1 correction. Touch ONLY the lines that change — no Format Document, no reflow.
    1) Construction site (~L488): replace `var distiller = new LlmDistillationService(llmHttpClient);` with `var distiller = LlmDistillationProviderFactory.Resolve(Environment.GetEnvironmentVariable(LlmDistillationProviderFactory.EnvironmentVariableName), llmHttpClient);`. Immediately compute `var providerEnv = Environment.GetEnvironmentVariable(LlmDistillationProviderFactory.EnvironmentVariableName);` and `var isSubscriptionProvider = !string.IsNullOrWhiteSpace(providerEnv) && !string.Equals(providerEnv.Trim(), "openai", StringComparison.OrdinalIgnoreCase);`. Pass `isSubscriptionProvider` into the internal `RunDistillAsync` call. (NOTE: Resolve throws NotSupportedException for codex — that surfaces as a clear startup error pointing at Phase 21.3; do not catch/suppress it.)
    2) Internal `RunDistillAsync` signature: add a NEW parameter `bool isSubscriptionProvider = false` (OPTIONAL with default false — preserves byte-identical openai behavior AND keeps existing RunDistillAsyncTests call sites compiling unchanged; the regression guard depends on this). Thread `isSubscriptionProvider` into the `DistillVideoAsync` call (~L615) and into the dry-run cap-check block.
    3) Dry-run cap-check (~L595-608) + dry-run summary line: guard with `isSubscriptionProvider` — `var projectedCost = isSubscriptionProvider ? 0m : ComputeProjectedVideoCostUsd(transcript.Body);` (M1 — do NOT call the projector when subscription, Pitfall 5); `var wouldSkip = !isSubscriptionProvider && await ledger.WouldExceedCapAsync(projectedCost, monthKey, ct);` ; disposition string adds the "($0, subscription)" branch; summary line shows "$0 (subscription)" when subscription.
    4) `DistillVideoAsync` signature: add the SAME optional `bool isSubscriptionProvider = false` parameter (consistent ordering; default false). Apply M1 at every site:
       - L974 `var projectedVideoCost = ComputeProjectedVideoCostUsd(transcript.Body);` → `var projectedVideoCost = isSubscriptionProvider ? 0m : ComputeProjectedVideoCostUsd(transcript.Body);` (M1).
       - L975 cap-gate → prepend `!isSubscriptionProvider &&` to the condition.
       - L990, L1019, L1047 cap-gates: these call `ComputeProjectedCallCostUsd(...)` INSIDE the WouldExceedCapAsync argument. Guard the whole condition with `!isSubscriptionProvider &&` placed FIRST so `&&` short-circuits and `ComputeProjectedCallCostUsd` is never evaluated when subscription (M1). The projector call must sit to the RIGHT of the `&&`.
       - L1007/L1036/L1064 (before each RecordCallAsync): replace `var <x>Cost = LlmSpendLedger.ComputeCostUsd(...)` with `var <x>Cost = isSubscriptionProvider ? 0m : LlmSpendLedger.ComputeCostUsd(...)`. Keep the `RecordCallAsync` call (records the row with cost 0 for observability — D-04a) and the `llmCalls++; llmSpend += <x>Cost;` lines unchanged. (input/output token counts from a CLI backend are already 0 per Plan 01's TokenUsage(0,0).)
    Add the new tests to `DeckFlow.Core.Tests/RunDistillAsyncTests.cs` (extend the private RunAsync helper with an optional `bool isSubscriptionProvider = false` param passed through to the internal RunDistillAsync):
       (a) subscription run bypasses a would-exceed-cap ledger and records spend 0;
       (b) subscription dry-run reports $0/subscription and no cap-hit;
       (c) M1: assert WouldExceedCapAsync is invoked 0 times on the subscription run while the openai run invokes it > 0 times. If FakeLlmSpendLedger cannot count invocations, add a test-only invocation counter to FakeLlmSpendLedger and assert 0 on subscription / >0 on openai. Also assert the recorded run spend_usd == 0 on the subscription run.
    Reuse FakeLlmSpendLedger (configure WouldExceedCapAsync to return true) + FakeLlmDistillationService.
    DO NOT modify WhisperTranscriptionService or the harvest path — Whisper stays OpenAI (out of scope).
  </action>
  <verify>
    <automated>"/mnt/c/Program Files/dotnet/dotnet.exe" build DeckFlow.sln -c Debug 2>&1 | tail -3</automated>
    <automated>"/mnt/c/Program Files/dotnet/dotnet.exe" test DeckFlow.Core.Tests/DeckFlow.Core.Tests.csproj --filter "FullyQualifiedName~RunDistillAsyncTests" 2>&1 | tail -8 || echo "VSTEST UNRELIABLE IN WSL — build-clean + manual harness fallback; run the new subscription-bypass facts + existing RunDistillAsyncTests manually to confirm the openai-default regression guard"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "LlmDistillationProviderFactory.Resolve" DeckFlow.CLI/CommandRunners.cs` returns 1 (construction site swapped).
    - `grep -c "new LlmDistillationService(" DeckFlow.CLI/CommandRunners.cs` returns 0 (no leftover direct construction — regression-critical).
    - `grep -c "isSubscriptionProvider" DeckFlow.CLI/CommandRunners.cs` >= 10 (1 derive + 2 method signatures + 4 cap-gate guards + dry-run guards + 3 RecordCallAsync ternaries + projectedVideoCost ternary).
    - `grep -c "!isSubscriptionProvider &&" DeckFlow.CLI/CommandRunners.cs` >= 4 (4 cap-gate sites guarded; `&&` short-circuit prevents projector evaluation — M1).
    - M1 — pricing math conditional: `grep -c "isSubscriptionProvider ? 0m : ComputeProjectedVideoCostUsd" DeckFlow.CLI/CommandRunners.cs` >= 2 (dry-run + DistillVideoAsync L974); `grep -c "isSubscriptionProvider ? 0m" DeckFlow.CLI/CommandRunners.cs` >= 5 (2 projector ternaries + 3 RecordCallAsync cost ternaries).
    - `grep -ci "subscription" DeckFlow.CLI/CommandRunners.cs` >= 1 (dry-run "$0, subscription" copy).
    - REGRESSION GUARD: existing RunDistillAsyncTests still pass unchanged (their RunAsync call uses default isSubscriptionProvider=false → openai behavior byte-identical).
    - `git diff DeckFlow.CLI/CommandRunners.cs` shows NO edits inside RunHarvestAsync / WhisperTranscriptionService usage (Whisper/harvest path untouched).
    - `DeckFlow.sln` builds clean (0 new warnings/errors); new RunDistillAsyncTests (incl. the M1 invocation-count assertion) pass (or build-clean + manual harness fallback recorded per WSL VSTest constraint).
  </acceptance_criteria>
  <done>Factory wired; ledger cap-gate bypassed AND pricing math skipped (M1) for subscription at all sites; cost-0 recorded; openai path byte-identical; build clean; regression + new tests (incl. M1) green.</done>
</task>

<task type="auto">
  <name>Task 2: Document cross-platform claude CLI backend operation (WSL + Windows + dotnet.exe-from-WSL hard case + JSON-array {instruction} placeholder override, M4 + ROUND-3 HIGH-1) + README note</name>
  <read_first>
    - .planning/phases/21.2-pluggable-llm-distill-cli-backends-inserted/21.2-RESEARCH.md lines 366-457 (verified claude command + .result envelope), 542-623 (cross-platform PATH reality, env-override strategy, auth), 784-790 (June 15 2026 claude billing note)
    - .planning/phases/21.2-pluggable-llm-distill-cli-backends-inserted/21.2-REVIEWS.md ROUND-3 HIGH-1 (override MUST be a JSON array so the empty `--allowedTools ""` arg and Windows path pass; ops examples become JSON arrays) + M4 + L2 (secret/log hygiene)
    - .planning/phases/21.2-pluggable-llm-distill-cli-backends-inserted/21.2-CONTEXT.md D-02a-override (JSON-array + fail-fast) + D-02d (the authoritative override/placeholder + cross-platform decisions)
    - ~/.claude/projects/.../memory/reference_wsl_dotnet_env_secret.md (canonical session learning on WSL to Windows env passing — referenced by D-02c/D-02d; mirror its guidance)
    - docs/ops/db-full-reset.md (existing ops-doc tone/format to mirror)
    - README.md (locate the Content KB / distill section to add the provider note)
  </read_first>
  <action>
    Create `docs/ops/content-kb-llm-cli-backends.md` covering KB-11's "both invocations documented" requirement with EXACT, EXECUTABLE commands (M4 + ROUND-3 HIGH-1). CLAUDE-ONLY — do NOT document codex commands (codex is Phase 21.3; note it is "not yet supported, see Phase 21.3" if mentioned at all). Sections:
    - Overview: `DECKFLOW_LLM_PROVIDER` = openai (default) | claude (codex deferred to Phase 21.3 — selecting it throws a clear error). openai unchanged. Command parts use ArgumentList (no shell quoting); claude CLI flags auto-detected on Linux/WSL; transcript piped on stdin (never an argument).
    - WSL invocation (most common): EXACT command, e.g. `DECKFLOW_LLM_PROVIDER=claude dotnet run --project DeckFlow.CLI -- distill --db artifacts/uat-content-kb.db` from a WSL bash where `claude` is on PATH. Note the auto-detected default: claude → `claude -p "<instruction>" --output-format json --allowedTools ""` (instruction-as-arg, transcript-on-stdin, tools disabled).
    - Windows invocation (claude NOT on Windows PATH): EXACT, EXECUTABLE JSON-array override (ROUND-3 HIGH-1) — `set DECKFLOW_LLM_PROVIDER=claude` plus `set DECKFLOW_LLM_CLI_COMMAND=["wsl.exe","claude","-p","{instruction}","--output-format","json","--allowedTools",""]` (or `["cmd.exe","/c","claude.cmd","-p","{instruction}","--output-format","json","--allowedTools",""]` if claude is natively installed), then the dotnet run. Explain WHY the override is a JSON array, not a space-separated string: a space split cannot reliably pass the empty `--allowedTools ""` tool-disable arg (it would pass a literal `""` or drop it) and cannot carry a Windows path with spaces; the JSON array passes element[0] as FileName and the rest as ArgumentList VERBATIM, with the empty string preserved exactly. The override MUST include exactly one `{instruction}` element plus `--output-format json` and `--allowedTools ""` so claude gets the instruction in the right position, JSON output, and disabled tools — an override that is non-JSON, an empty array, or missing/duplicating the `{instruction}` token fails fast with a clear error (ROUND-3 HIGH-1 / MED-1). Explain the auto-detect throws a clear error on Windows without the override.
    - THE HARD CASE (M4 — Windows dotnet.exe launched from a WSL shell): when DeckFlow.CLI is built/run via the Windows `/mnt/c/Program Files/dotnet/dotnet.exe` from inside WSL, the spawned claude child inherits the WINDOWS dotnet.exe environment, NOT the WSL bash PATH — so a bare `claude` is not found. State explicitly: the child process's PATH/env is owned by whichever process (Windows dotnet.exe vs a native WSL dotnet) launched DeckFlow.CLI. Give the EXACT working recipe: set `DECKFLOW_LLM_CLI_COMMAND=["wsl.exe","claude","-p","{instruction}","--output-format","json","--allowedTools",""]` (or an absolute WSL binary path as element[0]) so the claude in WSL is invoked with its own PATH/auth; confirm `~/.claude/.credentials.json` is reachable from the resolved WSL process. Reference the reference_wsl_dotnet_env_secret learning.
    - `DECKFLOW_LLM_CLI_COMMAND` override semantics: the value is a JSON ARRAY of strings; element[0] becomes the FileName, the remaining elements become ArgumentList elements VERBATIM (empty strings preserved as empty args — that is how `--allowedTools ""` is expressed); the single `{instruction}` token is substituted in place with the per-extraction instruction (positioned, not appended); a non-JSON value, an empty array, or an override missing or duplicating `{instruction}` is rejected up front with a clear error before any process is spawned.
    - `DECKFLOW_LLM_CLI_TIMEOUT_SECONDS`: optional per-extraction timeout override (default 10 min); on timeout the child process tree is killed and that video is marked failed (re-attemptable next run).
    - Auth: claude uses the operator's OAuth subscription session (`~/.claude/.credentials.json`), NOT `--bare`, NOT an API key. Note the June 15 2026 claude `-p` Agent-SDK-credit billing change.
    - Ledger bypass: when provider != openai, the spend ledger cap-gate AND token pricing are bypassed (M1); run record written with spend=0; `DECKFLOW_LLM_MONTHLY_CAP_USD` governs openai only.
    - Security note (L2): transcripts are untrusted input fed to claude via stdin; never put transcript text into the command/args; claude runs with `--allowedTools ""` (no tools); output is shape-validated + allowlist-constrained; logs never include prompts/transcripts/stdout/full command lines (only a bounded stderr tail); the claude is_error path surfaces a sanitized status without the raw envelope body.
    Add a short pointer in README.md (in the Content KB / distill area) to the new doc and the `DECKFLOW_LLM_PROVIDER` env var. Touch only the relevant README lines (CLAUDE.md: README updated when behavior changes).
  </action>
  <verify>
    <automated>grep -c "DECKFLOW_LLM_PROVIDER" docs/ops/content-kb-llm-cli-backends.md; grep -ci "dotnet.exe" docs/ops/content-kb-llm-cli-backends.md; grep -c "{instruction}" docs/ops/content-kb-llm-cli-backends.md</automated>
  </verify>
  <acceptance_criteria>
    - `docs/ops/content-kb-llm-cli-backends.md` exists and contains `DECKFLOW_LLM_PROVIDER`, `DECKFLOW_LLM_CLI_COMMAND`, a JSON-array `["wsl.exe","claude","-p","{instruction}",...]` Windows example, a `["cmd.exe","/c","claude.cmd","-p","{instruction}",...]` example, and a WSL on-PATH example (>= 1 each).
    - ROUND-3 HIGH-1: the override examples are JSON ARRAYS (`grep -c '\["wsl.exe"' docs/ops/content-kb-llm-cli-backends.md` >= 1) NOT space-separated strings; every override example includes the `{instruction}` element, `--output-format`, `json`, and `--allowedTools` with an empty `""` element (`grep -c "{instruction}" docs/ops/content-kb-llm-cli-backends.md` >= 2; `grep -c "allowedTools" docs/ops/content-kb-llm-cli-backends.md` >= 1); the doc explains why the override is a JSON array (empty-arg/path safety) — `grep -ci "json array" docs/ops/content-kb-llm-cli-backends.md` >= 1.
    - M4: the doc has an explicit "Windows dotnet.exe launched from WSL" section that states which process owns PATH/env and gives an exact working command (`grep -ci "dotnet.exe" docs/ops/content-kb-llm-cli-backends.md` >= 1).
    - The doc covers auth (no `--bare`, OAuth subscription), the `DECKFLOW_LLM_CLI_TIMEOUT_SECONDS` override, and the ledger-bypass (M1) behavior.
    - The doc has a security note covering untrusted transcripts on stdin + log hygiene (L2).
    - CLAUDE-ONLY: no codex invocation examples (`grep -ci "codex exec\|codex -p\|--sandbox" docs/ops/content-kb-llm-cli-backends.md` returns 0); any mention of codex points to Phase 21.3 as "not yet supported".
    - README.md references the new doc and the `DECKFLOW_LLM_PROVIDER` env var (`grep -c DECKFLOW_LLM_PROVIDER README.md` >= 1).
  </acceptance_criteria>
  <done>Operator doc covers claude WSL + Windows + dotnet.exe-from-WSL hard case + JSON-array {instruction} placeholder override (M4 / ROUND-3 HIGH-1) + timeout + auth + ledger bypass (M1) + security (L2); no codex commands; README points to it.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 3: Live claude-backed distill UAT over the 10-video db (clears the Phase 21.1 gate)</name>
  <what-built>
    A pluggable LLM distill backend (claude): with `DECKFLOW_LLM_PROVIDER=claude`, the `distill` CLI verb runs all 3 extractions through the local claude CLI subscription (no OpenAI API key / no billing), emits AI-prompt artifact files + slim-index rows, bypasses the spend ledger and its pricing math (M1), and writes a spend=0 run record. This run is the acceptance run that ALSO satisfies the previously-blocked Phase 21.1 live-UAT gate (which hit HTTP 429 insufficient_quota on the OpenAI backend). (codex is Phase 21.3 — not exercised here.)
  </what-built>
  <how-to-verify>
    Operator runs from a WSL shell (claude on PATH), against the existing 10-video UAT db. Suggested steps (operator adjusts paths/env per the new ops doc):
    1. Confirm `claude` is logged in: `claude -p "say OK" --output-format json --allowedTools ""` returns `is_error:false` with a `result`.
    2. Dry-run first: set `DECKFLOW_LLM_PROVIDER=claude` and run the distill verb with `--dry-run` against `artifacts/uat-content-kb.db` (10 videos). EXPECT: output reports "WOULD distill ($0, subscription)" per video and a summary "projected spend $0 (subscription)" — NO cap-hit, NO OpenAI key required, NO cap math.
    3. Live run (HARD PREREQUISITE: do NOT proceed until step 2's dry-run reported "$0, subscription" for at least one video with NO error — a failed dry-run means the CLI/login/flags are misconfigured and a live run would waste subscription quota on a broken setup): same env, drop `--dry-run`. EXPECT: each of the (up to) 10 videos distills via claude; artifact files appear at the Phase 19 artifact location; slim-index rows are written; a `content_harvest_runs` row records whisper_calls = LLM call-count and spend_usd = 0.
    4. E5 sample: open 2-3 emitted artifacts and confirm archetype + bracket tags are plausible for the video content (within ContentTagVocabulary).
    5. E6 sample: confirm the summary thesis is coherent and grounded in the transcript (not hallucinated), and 3-8 timestamped clips are present.
    6. Confirm the openai default still works unchanged if an OPENAI_API_KEY is available, OR rely on the existing green RunDistillAsyncTests as the regression proof (no live OpenAI call required — that key is exhausted).
    7. Deterministic SQL spot-checks (reviewer suggestion): distilled-video count > 0; slim-index row count matches; latest `content_harvest_runs.spend_usd = 0`; LLM-call ledger `sum(cost_usd) = 0`; whisper_calls == 3 * distilled videos.
    8. (Optional) hang-safety smoke: confirm a deliberately misconfigured command (e.g. a bogus DECKFLOW_LLM_CLI_COMMAND — non-JSON, or missing {instruction}) surfaces a clear fail-fast error BEFORE any spawn (not a hang), and a command that launches but never returns surfaces a timeout/kill — not required for approval, but a useful confidence check of ROUND-3 HIGH-1/HIGH-2.
  </how-to-verify>
  <resume-signal>Type "approved" (E5/E6 plausible; artifacts + index rows + spend=0 run record produced; SQL checks confirm spend=0) or describe issues (e.g., JSON-repair failures, tag-allowlist rejections, hung process, JSON-array-override misfire). On approval this run is recorded as satisfying the Phase 21.1 gate; the executor then flips ROADMAP Phase 21.1 and Phase 21.2, REQUIREMENTS KB-10/KB-11, and the traceability checkboxes (KB-12/Phase 21.3 stays open).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator env (`DECKFLOW_LLM_PROVIDER`, `DECKFLOW_LLM_CLI_COMMAND`) → CLI runner | Operator-controlled provider selection + JSON-array command override ({instruction} placeholder) |
| transcript text → claude subprocess (via stdin) | Untrusted YouTube/podcast transcript content fed to the claude CLI |
| claude output → ledger/run record | Subscription backend reports no per-token cost; run record must not be poisoned with bogus spend |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21.2-05 | Tampering / Injection | `DECKFLOW_LLM_CLI_COMMAND` → CliCommandSpec FileName/ArgumentList | mitigate | The override is parsed as a STRUCTURED JSON array of args (Plan 01 `BuildCommandSpec`, `JsonSerializer.Deserialize<string[]>`) — NOT a space-split string, so empty args (`--allowedTools ""`) and Windows paths pass verbatim and there is no space-split injection surface (ROUND-3 HIGH-1). Command/args come ONLY from the operator's own env or the per-provider const default; the single `{instruction}` placeholder is substituted as one ArgumentList element; transcript content is never concatenated into the command (it goes to stdin). Args passed via `ArgumentList`, `UseShellExecute=false` — no shell interpolation. A malformed override (non-JSON / empty array / missing-or-duplicate placeholder) fails fast before any spawn (MED-1). The override is a trusted local operator boundary, documented in the ops doc with a safe-construction note. |
| T-21.2-06 | Repudiation / Integrity | spend ledger + run record under subscription | mitigate | Cap-gate AND pricing math bypassed only when provider != openai (M1); `RecordCallAsync` still writes a row (cost 0) so the run is auditable (whisper_calls = LLM call-count, spend_usd = 0). openai path unchanged → no integrity regression for the metered backend. The live-UAT SQL checks confirm spend=0. |
| T-21.2-07 | Denial of Service | hung/slow claude subprocess during a real run | mitigate | The per-call claude process (Plan 01) enforces an in-service timeout (default 10 min, env-overridable) created in ExtractWithRetryAsync and passed into the runner seam; `linkedToken.Register` kills the tree (`Kill(entireProcessTree:true)`) immediately after Start and the stdin write is cancellation-aware (`BaseStream.WriteAsync(...AsMemory(), linkedToken)`), so a child that never drains stdin still gets killed at the timeout (ROUND-3 HIGH-2); independent of the caller token; a persistent failure marks the video `failed` (non-terminal) rather than hanging the run. The CLI distill path passes CancellationToken.None and still cannot hang. |
| T-21.2-08 | Information Disclosure | logs / dry-run output | mitigate | Distill logging keeps the existing Phase 21 structured-template patterns (counts/keys, not transcript bodies); dry-run copy reports only counts + "$0, subscription". The claude service failure messages contain only a bounded stderr tail and a sanitized is_error status — never prompts/transcripts/stdout/full command lines/raw .result body (L2). No API key read or echoed for the subscription backend. |
| T-21.2-09 | Tampering / Read-boundary | codex agent (deferred) | transfer | codex is NOT a runnable backend in this phase — `DECKFLOW_LLM_PROVIDER=codex` throws NotSupportedException pointing at Phase 21.3 / KB-12. The unproven codex read-boundary under untrusted transcript input is explicitly deferred; the ops doc and UAT cover claude only. No codex safety claimed. |
| T-21.2-SC | Tampering | npm/pip/cargo installs | mitigate | No packages installed this phase (BCL + existing deps only). RESEARCH Package Legitimacy Audit satisfied by absence; no `[ASSUMED]/[SUS]/[SLOP]` packages → no install checkpoint required. |
</threat_model>

<verification>
- `DeckFlow.sln` builds clean via `"/mnt/c/Program Files/dotnet/dotnet.exe" build` (0 new warnings/errors).
- REGRESSION GUARD: existing RunDistillAsyncTests pass unchanged (openai-default path byte-identical); new subscription-bypass tests (incl. M1 invocation-count) pass.
- M1: subscription path does not invoke WouldExceedCapAsync or the cost projector (asserted by the new test).
- Cross-platform docs present (claude WSL + Windows + dotnet.exe-from-WSL hard case M4 + JSON-array {instruction} placeholder override ROUND-3 HIGH-1 + timeout + auth + security); no codex commands.
- Live UAT (Task 3 checkpoint) confirms a claude-backed run emits valid artifacts + slim-index rows + spend=0 run record and E5/E6 pass.
- No new NuGet packages; Whisper/harvest path untouched.
</verification>

<success_criteria>
- KB-10 satisfied: `DECKFLOW_LLM_PROVIDER` selects the backend via the factory (openai default | claude; codex→21.3 error), wired into `RunDistillAsync`; openai default = no regression.
- KB-11 satisfied: cross-platform claude CLI invocation documented (WSL + Windows + dotnet.exe-from-WSL, M4; JSON-array {instruction}-placeholder override, ROUND-3 HIGH-1); ledger cap-gate AND token-pricing bypassed for the subscription backend (M1); run record written with spend=0; `DECKFLOW_LLM_MONTHLY_CAP_USD` governs openai only.
- ROADMAP SC1-SC5 satisfied (SC5 = the live claude distill over the 10-video UAT db clears the Phase 21.1 gate); SC6 satisfied jointly with Plan 01.
- All review findings landing in this plan resolved: M1 (conditional pricing), M4 + ROUND-3 HIGH-1 (exact, executable cross-platform claude commands with JSON-array {instruction} placeholder override), L2 (log hygiene reflected in docs + threat model). H1/H2/H4, ROUND-3 HIGH-2 (cancel-aware stdin/kill-register), ROUND-3 MED-1 (fail-fast), ROUND-3 MED-2 (single-object), M2, M3, L1, MED-deadlock resolved in Plan 01.
</success_criteria>

<output>
Create `.planning/phases/21.2-pluggable-llm-distill-cli-backends-inserted/21.2-02-SUMMARY.md` when done. Record the live-UAT outcome (claude provider, videos distilled, artifacts produced, E5/E6 sample notes, SQL spend=0 confirmation), and confirm the Phase 21.1 gate is cleared. On approval, flip ROADMAP Phase 21.1 + 21.2 and REQUIREMENTS KB-10/KB-11 + traceability checkboxes (KB-12/Phase 21.3 stays open).
</output>
