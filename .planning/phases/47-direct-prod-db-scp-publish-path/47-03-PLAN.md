---
phase: 47-direct-prod-db-scp-publish-path
plan: 03
type: execute
wave: 3
depends_on: [47-01, 47-02]
files_modified:
  - DeckFlow.Studio/Pages/DirectPush.razor
  - DeckFlow.Studio/Shared/NavMenu.razor
  - DeckFlow.Studio.Tests/DirectPushPageTests.cs
  - DeckFlow.Studio.Tests/TestDoubles/FakeContentSiteIndexStore.cs
autonomous: false
requirements: [PUB-04, PUB-05]
user_setup:
  - service: render-ssh
    why: "Live SCP + prod-Postgres smoke (manual-only, not CI) needs the Studio:Scp:* + Studio:ProdConnectionString user-secrets and a Render-registered SSH key (see Plan 01 user_setup)."

must_haves:
  truths:
    - "Operator sees a prod diff (New/Updated counts + per-row + per-file lists) before any write; no write occurs until the 'reviewed PRODUCTION' checkbox is checked (SC1)"
    - "Stage 2 (SCP) button is disabled until the confirmation checkbox is checked; Stage 3 (DB) button is disabled until every artifact uploads successfully (SC2)"
    - "Each approved row is written to prod via UpsertContentColumnsOnlyAsync exclusively — never UpsertRowAsync/UpsertRowPreservingVisibilityAsync (SC3)"
    - "SCP partial failure shows a per-file list and keeps Stage 3 locked; DB partial failure shows a per-row list without re-locking Stage 2 (SC4)"
    - "No connection string, SSH host/user/key, or remote path ever appears in the rendered markup or any error message — presence-only (SC5)"
    - "When prod or SCP config is absent, the page renders a warning and all action buttons are disabled (not hidden)"
  artifacts:
    - path: "DeckFlow.Studio/Pages/DirectPush.razor"
      provides: "3-stage gated direct-publish page (@page /direct-push)"
      contains: "/direct-push"
      min_lines: 250
    - path: "DeckFlow.Studio/Shared/NavMenu.razor"
      provides: "Direct Push nav entry below Publish"
      contains: "direct-push"
    - path: "DeckFlow.Studio.Tests/DirectPushPageTests.cs"
      provides: "8 implemented bUnit tests (no stubs remaining)"
      contains: "Render<DirectPush>"
  key_links:
    - from: "DeckFlow.Studio/Pages/DirectPush.razor"
      to: "DeckFlow.Studio/Services/IProdStoreFactory.cs"
      via: "ProdStoreFactory.Create(Configuration prod conn string) inside the publish action"
      pattern: "ProdStoreFactory.Create"
    - from: "DeckFlow.Studio/Pages/DirectPush.razor"
      to: "DeckFlow.Core/Content/ContentSiteIndexStore.cs (UpsertContentColumnsOnlyAsync)"
      via: "prodStore.UpsertContentColumnsOnlyAsync(row) per approved row"
      pattern: "UpsertContentColumnsOnlyAsync"
    - from: "DeckFlow.Studio/Pages/DirectPush.razor"
      to: "DeckFlow.Studio/Services/ISshArtifactUploader.cs"
      via: "SshUploader.UploadArtifactsAsync(localPaths, progress, ct) in Stage 2"
      pattern: "UploadArtifactsAsync"
    - from: "DeckFlow.Studio/Shared/NavMenu.razor"
      to: "DeckFlow.Studio/Pages/DirectPush.razor"
      via: "NavLink href direct-push"
      pattern: "direct-push"
---

<objective>
Build the Direct Prod-DB + SCP publish page, add its nav entry, and complete all 8 bUnit
tests. This is the operator-facing surface that delivers PUB-04 (artifact-first SCP then safe
prod upsert) and PUB-05 (diff preview + explicit confirmation + per-item partial-failure
reconcile). It also gates the SSH.NET supply-chain addition behind a blocking-human checkpoint
and ends with a human-verify of the PROD-write danger surface.

The page is a 3-stage variant of the Phase 46 Publish.razor 2-stage pattern: Stage 1 compute
prod diff -> confirmation checkbox -> Stage 2 SCP upload (btn-danger) -> Stage 3 prod DB upsert
(btn-danger, gated on Stage 2 full success). Mirror every structural detail from 47-PATTERNS.md
and the interaction/state contract from 47-UI-SPEC.md exactly.

Purpose: complete the second publish path end-to-end with safe-upsert-only writes, artifact-first
ordering, and per-item reconcile.
Output: 1 page, 1 nav entry, 8 implemented tests; SSH.NET install-gate + PROD-write UI human-verify.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/47-direct-prod-db-scp-publish-path/47-CONTEXT.md
@.planning/phases/47-direct-prod-db-scp-publish-path/47-RESEARCH.md
@.planning/phases/47-direct-prod-db-scp-publish-path/47-PATTERNS.md
@.planning/phases/47-direct-prod-db-scp-publish-path/47-UI-SPEC.md
@.planning/phases/47-direct-prod-db-scp-publish-path/47-VALIDATION.md
@.planning/phases/47-direct-prod-db-scp-publish-path/47-01-SUMMARY.md
@.planning/phases/47-direct-prod-db-scp-publish-path/47-02-SUMMARY.md

<interfaces>
Injected services for DirectPush.razor:
  IContentSiteIndexStore IndexStore  — local store (DI singleton)
  ISshArtifactUploader SshUploader   — Plan 02
  IProdStoreFactory ProdStoreFactory — Plan 01/02
  StudioConfig Config                — IsProdConfigured, IsScpConfigured
  IConfiguration Configuration       — read Studio:ProdConnectionString at action time (D-03/D-07)
  ContentKbOrchestratorOptions Options — ArtifactRoot; dataRoot = Path.GetDirectoryName(ArtifactRoot)

Prod store construction (inside Task.Run, never DI startup — D-03):
  prodStore = ProdStoreFactory.Create(Configuration["Studio:ProdConnectionString"] ?? "")
  await prodStore.EnsureSchemaAsync(ct)          — idempotent; safe on prod
  prodRows = await prodStore.GetAllRowsAsync(ct)  — diff base
  await prodStore.UpsertContentColumnsOnlyAsync(row, ct) — ONLY safe write (SC3/D-08)

Local artifact path resolution (mirror Publish.razor):
  dataRoot = Path.GetDirectoryName(Options.ArtifactRoot)
  localPath = Path.GetFullPath(Path.Combine(dataRoot, row.ArtifactPath))

Natural-key diff (D-04): key = row.YoutubeVideoId ?? row.RssGuid ?? "";
  build prod-by-key dictionary; local-not-in-prod = New; in-both = Updated; no Removed.

Test seams (Plan 01): FakeContentSiteIndexStore.UpsertMethodCalls (records which upsert ran),
FakeSshArtifactUploader.FilesToFail/UploadedFiles, FakeProdStoreFactory(prodFake).
RenderDirectPush helper already stubbed in DirectPushPageTests.cs.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build DirectPush.razor (3-stage gated PROD-write page) + NavMenu entry</name>
  <files>DeckFlow.Studio/Pages/DirectPush.razor, DeckFlow.Studio/Shared/NavMenu.razor</files>
  <read_first>
    - DeckFlow.Studio/Pages/Publish.razor (the exact analog — 2-stage state machine, _operationInFlight guard, _cts disposal, Task.Run, disposal-safe InvokeAsync(StateHasChanged), _diffReviewed checkbox, OnInitializedAsync dataRoot derivation from Options.ArtifactRoot at lines ~264-300)
    - DeckFlow.Studio/Pages/Review.razor (analog for the per-row table-sm table-hover style + aria-label disabled buttons)
    - DeckFlow.Studio/Shared/NavMenu.razor (the file being modified — the Publish nav-item at lines ~27-31 to insert below)
    - DeckFlow.Studio/Services/ActionOrchestratorProgress.cs (progress-sink bridge pattern for streaming per-file/per-row lists)
    - 47-PATTERNS.md section "DirectPush.razor" (the FULL verbatim page: directives, skeleton, not-configured gate, TARGET:PRODUCTION banner, 3 stage cards, checkbox, all @code state vars, OnInitializedAsync, ComputeDiffAsync, UploadArtifactsAsync, WriteRowsAsync, Dispose) and section "NavMenu.razor"
    - 47-UI-SPEC.md "Interaction & State Contract" + "Copywriting Contract" (final prescriptive copy, state machine table, gating rules, danger-button rules, accessibility) — copy strings verbatim
  </read_first>
  <action>
    Create DeckFlow.Studio/Pages/DirectPush.razor at @page "/direct-push", implementing
    IDisposable, following 47-PATTERNS.md "DirectPush.razor" structure and 47-UI-SPEC.md
    copy/state contract VERBATIM. Required structure:

    Directives/usings per PATTERNS (DeckFlow.Core.Content, DeckFlow.Core.Storage,
    DeckFlow.Studio.Services, Microsoft.Extensions.Configuration, System.Text.Json).
    Page title + muted lead (exact UI-SPEC copy "Publish to Production (Direct)" + the lead).
    Init loading/error guard (spinner "Resolving configuration...") mirroring Publish.razor.
    Not-configured gate: if not IsProdConfigured or not IsScpConfigured, render the
    alert alert-warning banner(s) naming which is missing and keep the page inert (buttons
    disabled, not hidden) — D-09. Presence-only; never any value (SC5).
    "TARGET: PRODUCTION" alert alert-danger banner (always shown once configured).
    Empty state when _approvedCount == 0 (exact copy "No approved entries to publish...").
    Stage 1 card "Compute Prod Diff" (btn-outline-primary); on success render the
    card border-primary Diff Preview sub-card with badge bg-success New + badge bg-primary
    Updated counts, a per-row table (Title / natural key / New-vs-Updated badge / artifact file
    name), and the per-file list (relative artifact path is SAFE to show — not a secret).
    Confirmation checkbox id prodReviewed (@bind _prodReviewed) with the exact label
    "I have reviewed what will be written to PRODUCTION above." (D-09).
    Stage 2 card "Upload Artifacts to Prod /data (SCP)" — btn-danger,
    disabled when (not _prodReviewed or _operationInFlight or not _diffReady); per-file reconcile
    table with aria-live polite streaming bg-success/bg-danger/bg-secondary badges + Reason
    column (failure text only; sanitized — never host/key/remote-path); success summary.
    Stage 3 card "Write Approved Rows to Prod DB" — btn-danger, disabled when
    (not _scpSuccess or _operationInFlight), with the locked hint
    "Locked until every artifact has uploaded successfully."; per-row reconcile table; success
    summary noting is_visible/is_evergreen preserved.

    @code: inject the 6 services above. State vars per PATTERNS (init, _operationInFlight, _cts,
    Stage-1 diff fields incl. _approvedRows, _prodReviewed, Stage-2 _fileResults/_scpSuccess,
    Stage-3 _rowResults/_dbSuccess). Implement:
    OnInitializedAsync: Task.Run -> GetApprovedRowsAsync count + derive _dataRoot =
    Path.GetDirectoryName(Options.ArtifactRoot); swallow OperationCanceledException; set
    _initError on other exceptions (never include secret values).
    ComputeDiffAsync (Stage 1): Task.Run -> read local approved rows; build on-demand prod
    store via ProdStoreFactory.Create(Configuration["Studio:ProdConnectionString"] ?? "");
    EnsureSchemaAsync; GetAllRowsAsync; in-memory natural-key diff -> New/Updated counts +
    cache _approvedRows; reset downstream stage state. Catch block surfaces the UI-SPEC
    "Could not read production — {reason}..." copy (reason from a NON-SSH read exception;
    never a conn-string value).
    UploadArtifactsAsync (Stage 2): resolve absolute local paths from _approvedRows via
    Path.GetFullPath(Path.Combine(_dataRoot, r.ArtifactPath)); pass a Progress(SshUploadResult)
    that streams into _fileResults via disposal-safe InvokeAsync; call
    SshUploader.UploadArtifactsAsync; set _scpSuccess = results.All(r => r.Success); on any
    failure set the UI-SPEC SCP-failed error copy and leave Stage 3 locked. Catch surfaces the
    sanitized "SSH connection failed — check SCP configuration and Render SSH access." (never ex.Message).
    WriteRowsAsync (Stage 3, gated on _scpSuccess): build on-demand prod store again; foreach
    approved row call prodStore.UpsertContentColumnsOnlyAsync(row, _cts.Token) ONLY (SC3/D-08 —
    NEVER UpsertRowAsync / UpsertRowPreservingVisibilityAsync); per-row try/catch records
    success/fail into _rowResults streamed via InvokeAsync; set _dbSuccess on all-OK; on
    failure surface the UI-SPEC DB-failed reconcile copy (does NOT re-lock Stage 2).
    Dispose: _cts.Cancel(); _cts.Dispose();.
    All async state updates use the disposal-safe InvokeAsync with try/catch
    (ObjectDisposedException)/(InvalidOperationException). All blocking work inside
    Task.Run(..., _cts.Token) (Pitfall 7). Every spinner gets role=status +
    aria-label="Operation in progress" + visually-hidden "Loading...".

    Then add the Direct Push nav entry to NavMenu.razor immediately below the Publish
    nav-item px-3 (verbatim from 47-PATTERNS.md "NavMenu.razor"): NavLink href direct-push,
    span oi oi-data-transfer-upload aria-hidden plus the label "Direct Push".

    Honor .editorconfig carve-outs (never reflow razor C# raw strings, LF endings, Allman braces).
    Do NOT paste secret values anywhere; the page never reads Studio:Scp:* directly (the
    uploader owns that) and only reads Studio:ProdConnectionString into a local variable.
  </action>
  <verify>
    <automated>"/mnt/c/Program Files/dotnet/dotnet.exe" build DeckFlow.Studio/DeckFlow.Studio.csproj -c Debug 2>&1 | grep -E "Build succeeded|error" | head; grep -q 'UpsertContentColumnsOnlyAsync' DeckFlow.Studio/Pages/DirectPush.razor && ! grep -qE 'UpsertRowAsync|UpsertRowPreservingVisibilityAsync' DeckFlow.Studio/Pages/DirectPush.razor && echo "OK: safe-upsert only"; grep -q 'direct-push' DeckFlow.Studio/Shared/NavMenu.razor && echo "OK: nav entry"</automated>
  </verify>
  <acceptance_criteria>
    - DeckFlow.Studio builds clean with DirectPush.razor present.
    - Source assertion: page calls UpsertContentColumnsOnlyAsync and does NOT reference UpsertRowAsync or UpsertRowPreservingVisibilityAsync (SC3/D-08).
    - Source assertion: Stage 2 button disabled expression includes !_prodReviewed; Stage 3 button disabled expression includes !_scpSuccess (SC2 gates).
    - Source assertion: prod store built via ProdStoreFactory.Create(Configuration prod conn string) inside Task.Run, not at DI startup (D-03).
    - Source assertion: no Studio:Scp value or conn-string substring rendered; SSH catch uses the sanitized literal, never ex.Message (SC5/D-07).
    - NavMenu has the href direct-push entry below Publish.
  </acceptance_criteria>
  <done>DirectPush.razor implements the 3-stage gated PROD-write flow with safe-upsert-only writes, artifact-first gating, per-item reconcile, and secret redaction; nav entry added; Studio builds clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the 8 bUnit tests against DirectPush.razor</name>
  <files>DeckFlow.Studio.Tests/DirectPushPageTests.cs, DeckFlow.Studio.Tests/TestDoubles/FakeContentSiteIndexStore.cs</files>
  <behavior>
    - DirectPush_DiffPreview_ShowsNewUpdatedCounts: local has 2 approved (1 new key, 1 key also in prod) then after Compute Prod Diff markup shows "New: 1" and "Updated: 1".
    - DirectPush_CheckboxGates_ScpButton: after diff ready, Stage-2 (SCP btn-danger) button has disabled until prodReviewed checkbox checked; checking it enables the button.
    - DirectPush_Stage3Locked_UntilScpSuccess: after diff + checkbox, Stage-3 (DB) button disabled; after a fully-successful SCP run, Stage-3 enabled.
    - DirectPush_UsesContentColumnsOnlyUpsert: after a full run, prod FakeContentSiteIndexStore.UpsertMethodCalls contains only "UpsertContentColumnsOnlyAsync" (count == approved rows) and no "UpsertRowAsync"/"UpsertRowPreservingVisibilityAsync".
    - DirectPush_ScpPartialFailure_Stage3Locked: FakeSshArtifactUploader.FilesToFail set for 1 file then per-file list shows a Failed badge + reason; Stage-3 button stays disabled.
    - DirectPush_DbPartialFailure_PerRowListShown: prod store configured to throw on one row then per-row list shows that row Failed; Stage-2 success not re-locked (SCP success summary still present).
    - DirectPush_Secrets_NeverInMarkup: with config present, rendered markup contains no connection-string/host substrings (assert absence of "postgres", "Host=", any sentinel Scp value); only "configured"/"PRODUCTION" presence text.
    - DirectPush_NotConfigured_ButtonsDisabled: StudioConfig(false,false) plus the prod-only / scp-only variants then warning banner shown and Compute/SCP/DB buttons disabled or absent.
  </behavior>
  <read_first>
    - DeckFlow.Studio.Tests/DirectPushPageTests.cs (the file being modified — Plan 01 stub with the 8 named facts + RenderDirectPush helper)
    - DeckFlow.Studio.Tests/PublishPageTests.cs (analog: WaitForAssertion/WaitForState, InvokeAsync(() => Find(...).Click()), HasAttribute("disabled") assertions, Markup substring assertions)
    - DeckFlow.Studio.Tests/TestDoubles/FakeContentSiteIndexStore.cs (the file being modified — UpsertMethodCalls tracking from Plan 01; add a fail-injection hook here)
    - DeckFlow.Studio.Tests/TestDoubles/FakeSshArtifactUploader.cs + FakeProdStoreFactory.cs (Plan 01)
    - DeckFlow.Studio/Pages/DirectPush.razor (Task 1 — the actual element classes/ids to query: button.btn-outline-primary, button.btn-danger, prodReviewed checkbox, the count badges, the reconcile tables)
    - 47-VALIDATION.md "Per-Task Verification Map" (the 8 names + the manual-only items that are NOT automated)
  </read_first>
  <action>
    Un-stub the RenderDirectPush helper (uncomment the Render<DirectPush>() line and the
    DirectPush type references; remove the TODO(47-03) marker). To let a fake prod store throw
    on a chosen row (DB partial-failure test), extend FakeContentSiteIndexStore with an optional
    HashSet<string> KeysToFailOnUpsert: in UpsertContentColumnsOnlyAsync, if the row's natural
    key (YoutubeVideoId ?? RssGuid) is in the set, record the method call then throw an
    InvalidOperationException("Simulated prod upsert failure") instead of adding to Rows. Keep
    this internal to the test project.

    Implement all 8 [Fact] bodies per the behavior block, using WaitForAssertion/WaitForState
    for post-Task.Run renders and InvokeAsync(() => cut.Find(...).Click()) for clicks (mirror
    PublishPageTests). Use HasAttribute("disabled") for gate assertions and cut.Markup substring
    checks for counts/secrets. Remove every "stub — implemented in 47-03" line. Do NOT add
    automated tests for the 3 manual-only items (live SCP, live prod diff/upsert, live
    partial-failure) — those are operator smoke per 47-VALIDATION.md.

    The secrets test seeds a recognizable sentinel into StudioConfig context: since the page
    never receives raw secret values (presence-only), assert the rendered markup does NOT contain
    "postgres", "Host=", "Password", or a sentinel host literal, AND DOES contain "PRODUCTION".
  </action>
  <verify>
    <automated>"/mnt/c/Program Files/dotnet/dotnet.exe" build DeckFlow.Studio.Tests/DeckFlow.Studio.Tests.csproj -c Debug 2>&1 | grep -E "Build succeeded|error" | head; "/mnt/c/Program Files/dotnet/dotnet.exe" test DeckFlow.Studio.Tests/ --filter "DirectPush" 2>&1 | grep -E "Passed!|Failed!|Passed:|Failed:|error" | head</automated>
  </verify>
  <acceptance_criteria>
    - DeckFlow.Studio.Tests builds clean; no stub lines remain (grep for "stub — implemented in 47-03" returns nothing).
    - All 8 DirectPush_* facts pass: `dotnet test DeckFlow.Studio.Tests/ --filter "DirectPush"` reports Passed with 8 tests (per-task gate; if VSTest is flaky in WSL, build-clean + push-and-watch CI is the authoritative fallback per CLAUDE.md).
    - DirectPush_UsesContentColumnsOnlyUpsert asserts UpsertMethodCalls contains only "UpsertContentColumnsOnlyAsync" (SC3).
    - DirectPush_ScpPartialFailure_Stage3Locked asserts Stage-3 button stays disabled after a failed file (SC4/SC2).
    - DirectPush_Secrets_NeverInMarkup asserts no conn-string/host substrings in markup (SC5).
  </acceptance_criteria>
  <done>All 8 bUnit tests implemented and passing against DirectPush.razor; FakeContentSiteIndexStore gains a per-key upsert-fail hook; no stubs remain.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 3: SSH.NET supply-chain confirmation + PROD-write UI human-verify</name>
  <files>(none — verification checkpoint; no file changes)</files>
  <action>Pause for the operator. Present the supply-chain confirmation (SSH.NET identity/version/source) and the PROD-write UI gate verification below; do not advance until the operator types "approved". This checkpoint is blocking-human and is NOT auto-approvable (workflow.auto_advance ignored) per T-47-SC.</action>
  <what-built>
    Plan 01 added the SSH.NET 2025.0.0 NuGet package to DeckFlow.Studio (D-01, operator-approved
    exception to v1.7 "no new packages"). Plans 01-03 built the full Direct Push flow:
    SftpArtifactUploader (SSH.NET SFTP), IProdStoreFactory on-demand Postgres store, the 3-stage
    gated DirectPush.razor page (Compute Prod Diff -> reviewed-PRODUCTION checkbox -> SCP upload
    -> safe prod upsert), and 8 passing bUnit tests. CI tests use fakes; no live SSH/Postgres
    was contacted by any automated step.
  </what-built>
  <how-to-verify>
    A. Supply-chain gate (NOT auto-approvable — T-47-SC):
       1. Confirm DeckFlow.Studio.csproj pins exactly `SSH.NET` Version `2025.0.0` (canonical casing).
       2. Confirm the package is on NuGet at https://www.nuget.org/packages/SSH.NET — author Renci,
          source github.com/sshnet/SSH.NET, MIT license, ~286M downloads, 15-year history.
       3. Confirm SSH.NET appears ONLY in DeckFlow.Studio.csproj (not in DeckFlow.Core or
          DeckFlow.Studio.Tests). This is the package that Codex plan-review was asked to scrutinize.
       Approve only if the package identity matches; otherwise reject and stop.
    B. PROD-write UI danger surface (run Studio locally with DECKFLOW_DISABLE_AUTO_BROWSER=true;
       prod/SCP user-secrets may be unset — verify the inert/gated states without writing to prod):
       1. Navigate to /direct-push via the new "Direct Push" nav entry.
       2. With prod/SCP NOT configured: confirm the warning banner names what is missing and all
          action buttons are disabled (SC2/D-09); no connection string or host appears anywhere.
       3. With config present (if the operator chooses): confirm the "TARGET: PRODUCTION" red
          banner, that the SCP button is disabled until the "I have reviewed... PRODUCTION" checkbox
          is checked, and that the DB button is locked with its hint until SCP fully succeeds.
       4. Confirm Studio startup logs show "Studio SCP: configured/not configured" and
          "Studio prod connection: configured/not configured" ONLY — no secret values (SC5).
    C. (Optional, operator-only live smoke — the 3 manual-only items in 47-VALIDATION.md): with
       real Render SSH + prod Postgres secrets, run a real diff + Stage 2 + Stage 3 and confirm
       files land under /data/content-kb/{slug}/, rows upsert, and is_visible/is_evergreen on
       pre-existing prod rows are unchanged (query prod before/after).
  </how-to-verify>
  <resume-signal>Type "approved" to accept the SSH.NET dependency and confirm the PROD-write gates, or describe issues to fix.</resume-signal>
  <verify>
    <automated>echo "Manual blocking-human checkpoint — operator confirms SSH.NET identity + PROD-write gates; not machine-verifiable"</automated>
  </verify>
  <done>Operator typed "approved": SSH.NET 2025.0.0 identity/source confirmed and the not-configured + checkbox + artifact-first PROD-write gates verified in the running Studio.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator browser -> Studio (Blazor SignalR) | Operator triggers a direct write to live prod |
| Studio -> prod Postgres | On-demand store; only the safe upsert may run |
| Studio -> Render /data (SFTP) | Artifact write; must precede DB write |
| NuGet supply chain | SSH.NET linked into the operator's tool (gated this plan) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-47-SC | Tampering | SSH.NET package | mitigate | Blocking-human checkpoint (Task 3) — NOT auto-approvable (workflow.auto_advance ignored). Verify exact id/version/source against NuGet before acceptance (D-01). |
| T-47-03a | Tampering | DirectPush.razor write path | mitigate | Only UpsertContentColumnsOnlyAsync is called on the prod store; bUnit test DirectPush_UsesContentColumnsOnlyUpsert asserts no other upsert ran (SC3/D-08). |
| T-47-03b | Tampering | Stage gating | mitigate | Stage 2 disabled until reviewed checkbox; Stage 3 disabled until every file uploads; tests DirectPush_CheckboxGates_ScpButton + DirectPush_Stage3Locked_UntilScpSuccess + DirectPush_ScpPartialFailure_Stage3Locked enforce (SC2/D-06/D-09). |
| T-47-03c | Information disclosure | page markup + error copy | mitigate | Presence-only; SSH catch uses sanitized literal; test DirectPush_Secrets_NeverInMarkup asserts no conn-string/host substrings (SC5/D-07). |
| T-47-03d | Repudiation | partial-failure reconcile | mitigate | Per-file and per-row status lists persist after the run so the operator can reconcile the exact failed subset (SC4/D-05). |
| T-47-03e | Tampering | path traversal -> remote write outside /data/content-kb | mitigate | ArtifactPath validated rooted/.. -free by ContentSiteIndexStore.ValidateArtifactPath; SftpArtifactUploader (Plan 02) re-confirms resolved remote path starts with RemoteArtifactRoot (V5). |
</threat_model>

<verification>
- DeckFlow.Studio and DeckFlow.Studio.Tests build clean (0 errors, 0 new warnings).
- `dotnet test DeckFlow.Studio.Tests/ --filter "DirectPush"` reports 8 passing tests.
- Full Studio test suite green (`dotnet test DeckFlow.Studio.Tests/`).
- Page references UpsertContentColumnsOnlyAsync only; nav entry present; no secret substrings in markup.
- New C# / razor satisfies .editorconfig changed-lines gate (LF, Allman, no raw-string reflow).
- Task 3 blocking-human checkpoint approved (SSH.NET identity + PROD-write gates confirmed).
</verification>

<success_criteria>
- DirectPush.razor delivers the 3-stage gated PROD publish flow (SC1 diff+confirm, SC2 gated steps, SC3 safe-upsert-only, SC4 per-item reconcile, SC5 secret redaction).
- Direct Push nav entry added below Publish.
- 8 bUnit tests implemented and passing.
- SSH.NET dependency confirmed via blocking-human checkpoint.
</success_criteria>

<output>
Create `.planning/phases/47-direct-prod-db-scp-publish-path/47-03-SUMMARY.md` when done.
</output>
