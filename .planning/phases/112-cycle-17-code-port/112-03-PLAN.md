---
phase: 112-cycle-17-code-port
plan: 03
type: execute
wave: 3
depends_on: ["112-02"]
files_modified:
  - DeckFlow.Core/Content/ContentKbPaths.cs
  - DeckFlow.Core/AssemblyInfo.cs
  - DeckFlow.Core/Knowledge/ContentTagVocabulary.cs
  - DeckFlow.Core/Loading/CommanderInference.cs
  - DeckFlow.Core/Knowledge/CategoryKnowledgeRepository.cs
  - DeckFlow.Core/Knowledge/CardCategoryRepository.cs
autonomous: true
requirements: [PORT-01]

must_haves:
  truths:
    - "The remaining six Core M-files gained only new members appended onto main's CURRENT content; every existing member is byte-identical (D-09)."
    - "dotnet build DeckFlow.sln completes with 0 errors and a warning count no higher than the recorded pre-port baseline — ROADMAP success criterion 2 for the Core half (PORT-01)."
    - "The ported Core creator-style test suite runs and passes — ROADMAP success criterion 4 (PORT-01)."
    - "scripts/format-check-changed.sh staged passes with the full Commit 1 file set staged, with any fix applied inside the same commit (D-04)."
    - "git diff --name-status main lists only Commit 1 allowlist paths plus the ten declared Core M-files, and none of D-12's never-port strings arrived (D-08, D-12)."
    - "Commit 1 of D-07's two-commit structure exists on feat/personal-tools as a single self-contained Core commit."
  artifacts:
    - path: "DeckFlow.Core/AssemblyInfo.cs"
      provides: "InternalsVisibleTo(\"DeckFlow.Web\") so CreatorProfileDeckCrawler can read StapleStripper.MaxDeckSize"
      contains: "InternalsVisibleTo"
    - path: "DeckFlow.Core/Content/ContentKbPaths.cs"
      provides: "Creator-style seed path constants consumed by CreatorStyleSeedLoader"
      contains: "CreatorStyleProfileSeedRelativePath"
  key_links:
    - from: "DeckFlow.Core/Knowledge/MeasuredStyleExtraction/StapleStripper.cs"
      to: "DeckFlow.Core/Knowledge/ContentTagVocabulary.cs"
      via: "Staples set lookup"
      pattern: "ContentTagVocabulary\\.Staples"
    - from: "DeckFlow.Core/Knowledge/CategoryKnowledgeRepository.cs"
      to: "DeckFlow.Core/Knowledge/CardCategoryRepository.cs"
      via: "GetGlobalCategoryBaselineAsync delegation"
      pattern: "GetGlobalCategoryBaselineAsync"
---

<objective>
Apply the six remaining Core M-file hunks, prove the Core half of the port is green, and create Commit 1 of D-07's two-commit structure.

Purpose: Commit 1 must be self-contained and independently green. RESEARCH build-verified that `DeckFlow.Core` compiles standalone with 0 errors once all nine Core hunks are applied — this plan finishes the set and puts the D-04 format gate, the D-08 path audit, and the targeted Core test run in front of the commit.

Output: one commit on `feat/personal-tools` containing the entire Cycle 17 Core engine (Phases 94-98) and its Core test suite.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/112-cycle-17-code-port/112-RESEARCH.md
@.planning/phases/112-cycle-17-code-port/112-CONTEXT.md
@.planning/phases/112-cycle-17-code-port/112-BASELINE.md
@.planning/phases/112-cycle-17-code-port/112-02-PLAN.md
@CLAUDE.md
</context>

<environment>
- Branch: `feat/personal-tools`. dotnet is `"/mnt/c/Program Files/dotnet/dotnet.exe"`. Do NOT set `MTG_DATA_DIR`.
- VSTest is unreliable under WSL — run `dotnet test` through the Windows dotnet above.
- `plan/cycle-17-creator-style` is READ-ONLY and no checkout from it happens in this plan. Every file here already exists on main and is hunked by hand from `git show HEAD:<path>`.
- `.gitattributes` enforces LF. Commits use the plain default author, Conventional Commits format, NO `Co-Authored-By` trailer.
- No new `PackageReference` anywhere in this phase.
</environment>

<never_port_list>
## Must not appear anywhere in Commit 1's diff (D-12; the ratified `PacketSessionCache.cs` exception belongs to plan 04, not here)
- DeckFlow.Web/Services/FeatureFlags/FeatureFlagCatalog.cs
- DeckFlow.Web/Services/FeatureFlags/FeatureFlagStore.cs
- any ToolRegistry file
- DeckFlow.Web/Models/DeckPageTab.cs
- DeckFlow.Web/Help/creator-style.md
- any SeoPaths / sitemap file
- DeckFlow.Web/Services/PacketSessionCache.cs (belongs to Commit 2)
- the string `tool.creator-style.enabled` anywhere in added content
</never_port_list>

<tasks>

<task type="auto">
  <name>Task 1: Hand-apply the six remaining Core M-file hunks</name>
  <files>
    DeckFlow.Core/Content/ContentKbPaths.cs,
    DeckFlow.Core/AssemblyInfo.cs,
    DeckFlow.Core/Knowledge/ContentTagVocabulary.cs,
    DeckFlow.Core/Loading/CommanderInference.cs,
    DeckFlow.Core/Knowledge/CategoryKnowledgeRepository.cs,
    DeckFlow.Core/Knowledge/CardCategoryRepository.cs
  </files>
  <read_first>
    - `git show HEAD:<path>` for each of the six files — main's CURRENT content is the starting point for every hunk. Do NOT reason from the merge-base.
    - `git show plan/cycle-17-creator-style:<same six paths>` — the source of the NEW members only.
    - `.planning/phases/112-cycle-17-code-port/112-RESEARCH.md` §"M-File Hunk Inventory" rows for `ContentKbPaths.cs`, `AssemblyInfo.cs`, `ContentTagVocabulary.cs`, `CommanderInference.cs`, `CategoryKnowledgeRepository.cs`, `CardCategoryRepository.cs` — each row names the exact members and the drift risk.
    - The consumers that determine the required signatures, all already in the working tree from plan 02: `DeckFlow.Core/Knowledge/MeasuredStyleExtraction/StapleStripper.cs` (needs `ContentTagVocabulary.Staples` and exposes `internal const int MaxDeckSize`), `DeckFlow.Core/Knowledge/GlobalCategoryBaseline.cs` (the return type of the new repository method).
    - `CLAUDE.md` §Conventions — xmldoc on public members, `IReadOnly*` on public surface, `CancellationToken` last, `Async` suffix, 4-space indent, Allman braces, file-scoped namespaces.
  </read_first>
  <action>
    For each file, start from main's current working-tree content and APPEND only the new members. Never `git checkout plan/cycle-17-creator-style -- <these paths>`; never replace whole-file content.

    `DeckFlow.Core/Content/ContentKbPaths.cs` — add two `const string` path constants, `CreatorStyleProfileSeedRelativePath` and `CreatorDeckCacheSeedRelativePath`, pointing at `content-kb/seed/creator-style-profiles.json` and `content-kb/seed/creator-deck-cache.json` respectively (match the existing seed-path constants' exact relative-path style in this file — read them first, do not invent a shape). `CreatorStyleSeedLoader` (ported in plan 04) references both by name.

    `DeckFlow.Core/AssemblyInfo.cs` — add `[assembly: InternalsVisibleTo("DeckFlow.Web")]`. Required because `CreatorProfileDeckCrawler.cs` (Web production code, ported in plan 04) reads `StapleStripper.MaxDeckSize`, an `internal const int` in Core. Place it alongside the existing `InternalsVisibleTo` entries and do not remove or reorder any of them.

    `DeckFlow.Core/Knowledge/ContentTagVocabulary.cs` — add one `IReadOnlySet<string> Staples` member holding the 11 curated card names from the branch version. Copy the card-name strings verbatim from the branch source; do not re-curate the list. `StapleStripper.cs` references `ContentTagVocabulary.Staples`.

    `DeckFlow.Core/Loading/CommanderInference.cs` — append one new public static method `ReflagInferredCommanders(List<DeckEntry>)` at the end of the class. Called by `SubmittedDeckStatsBuilder.cs` (Web, plan 04).

    `DeckFlow.Core/Knowledge/CardCategoryRepository.cs` — append one `internal async Task<GlobalCategoryBaseline> GetGlobalCategoryBaselineAsync(...)` (SQL query plus row mapper), one private helper `BuildCategoryPairKey`, and one nested `GlobalCategoryBaselineRow` class. Copy the SQL text verbatim from the branch source — do not re-derive or "improve" the query. Re-verify both insertion anchors against main's current content before writing.

    `DeckFlow.Core/Knowledge/CategoryKnowledgeRepository.cs` — append one public method `GetGlobalCategoryBaselineAsync` that delegates to the `CardCategoryRepository` method above. Consumed by `MeasuredStyleProfileBuilder.cs` (Web, plan 04).

    Honor the five `.editorconfig` carve-outs: never convert `{ get; init; }` to `{ get; }`, never inline an `[Attribute]` onto the property line, never re-indent a C# raw-string literal, preserve switch expressions, preserve xmldoc single-space indent. LF line endings only.

    Do not stage or commit; task 2 owns Commit 1.
  </action>
  <verify>
    <automated>"/mnt/c/Program Files/dotnet/dotnet.exe" build DeckFlow.Core/DeckFlow.Core.csproj</automated>
  </verify>
  <acceptance_criteria>
    - `"/mnt/c/Program Files/dotnet/dotnet.exe" build DeckFlow.Core/DeckFlow.Core.csproj` exits 0 with 0 errors.
    - `grep -c 'CreatorStyleProfileSeedRelativePath' DeckFlow.Core/Content/ContentKbPaths.cs` returns at least 1, and the same for `CreatorDeckCacheSeedRelativePath`.
    - `grep -c 'InternalsVisibleTo("DeckFlow.Web")' DeckFlow.Core/AssemblyInfo.cs` returns 1, and every `InternalsVisibleTo` line present on HEAD is still present.
    - `grep -c 'Staples' DeckFlow.Core/Knowledge/ContentTagVocabulary.cs` returns at least 1.
    - `grep -c 'ReflagInferredCommanders' DeckFlow.Core/Loading/CommanderInference.cs` returns at least 1.
    - `grep -c 'GetGlobalCategoryBaselineAsync' DeckFlow.Core/Knowledge/CategoryKnowledgeRepository.cs` and the same on `CardCategoryRepository.cs` each return at least 1.
    - `git diff HEAD -- DeckFlow.Core/Content/ContentKbPaths.cs DeckFlow.Core/AssemblyInfo.cs DeckFlow.Core/Knowledge/ContentTagVocabulary.cs DeckFlow.Core/Loading/CommanderInference.cs DeckFlow.Core/Knowledge/CategoryKnowledgeRepository.cs DeckFlow.Core/Knowledge/CardCategoryRepository.cs | grep -c '^-[^-]'` returns 0 — every hunk is additive-only.
    - `grep -c $'\r'` returns 0 for all six files.
  </acceptance_criteria>
  <done>All nine Core M-file hunks are applied and `DeckFlow.Core` builds standalone with 0 errors.</done>
</task>

<task type="auto">
  <name>Task 2: Gate and create Commit 1 (format gate, path audit, solution build, Core test run)</name>
  <files>git index and one new commit on feat/personal-tools</files>
  <read_first>
    - `.planning/phases/112-cycle-17-code-port/112-BASELINE.md` §"Build baseline" — the recorded pre-port error count (expected 0) and warning count (RESEARCH observed 9). The build result of this task is compared against it.
    - `.planning/phases/112-cycle-17-code-port/112-02-PLAN.md` §port_allowlist — the 64 paths that, together with the ten M-files declared across plans 02 and 03, form the ONLY legal contents of Commit 1's diff.
    - This plan's `<never_port_list>`.
    - `scripts/format-check-changed.sh` — read the usage block; the invocation is `scripts/format-check-changed.sh staged` and it operates on the STAGED set, so `git add` must come first.
    - `.planning/phases/112-cycle-17-code-port/112-CONTEXT.md` D-04, D-07, D-08.
    - `CLAUDE.md` §Commit Conventions — Conventional Commits, imperative subject, ≤72 chars, no `Co-Authored-By` trailer, plain default author.
  </read_first>
  <action>
    Stage exactly the Commit 1 file set: the 64 allowlisted paths from plan 02, plus the ten M-files (`DeckFlow.Core.Tests/DeckFlow.Core.Tests.csproj`, `ILlmDistillationService.cs`, `DistillationResults.cs`, `DistillationValidation.cs`, `ContentKbPaths.cs`, `AssemblyInfo.cs`, `ContentTagVocabulary.cs`, `CommanderInference.cs`, `CategoryKnowledgeRepository.cs`, `CardCategoryRepository.cs`). Stage nothing under `DeckFlow.Web/`, `DeckFlow.Web.Tests/`, `DeckFlow.CLI/`, or `content-kb/`. Planning artifacts under `.planning/` are committed separately by the workflow and are not part of this commit's production diff.

    Run the D-04 format gate on the staged set: `scripts/format-check-changed.sh staged`. If it reports violations, fix them and re-stage INSIDE this same commit — never as a follow-up commit. RESEARCH verified all 99 ported files already pass `dotnet format --verify-no-changes` with zero violations, so any violation is expected to come from the hand-written hunks, not the ported files. When fixing, do not violate the five carve-outs: if the formatter wants to turn `{ get; init; }` into `{ get; }`, inline an `[Attribute]`, or re-indent a raw-string literal, that is a formatter overreach and must be refused, not applied.

    Run the full solution build with the Windows dotnet: `"/mnt/c/Program Files/dotnet/dotnet.exe" build DeckFlow.sln`. Record the error count (must be 0) and the warning count, and compare against `112-BASELINE.md`. If the warning count exceeds the baseline, or a warning ID appears that is not in the baseline table, STOP and fix before committing — that is a criterion-2 failure.

    Run the targeted Core suite:
    `"/mnt/c/Program Files/dotnet/dotnet.exe" test DeckFlow.Core.Tests/DeckFlow.Core.Tests.csproj --filter "FullyQualifiedName~CreatorStyle|FullyQualifiedName~StatedRules|FullyQualifiedName~ProfileFusion|FullyQualifiedName~MeasuredStyleExtraction|FullyQualifiedName~CardGrounding"`
    All ported Core tests must pass. Then run the full `DeckFlow.Core.Tests` project and compare its failure list against the baseline's pre-existing-failure list — no NEW failure is permitted.

    Run the D-08 path audit BEFORE committing, against the staged set, and again after committing against the branch:
    (a) `git diff --name-only main..HEAD -- DeckFlow.Core DeckFlow.Core.Tests DeckFlow.Web DeckFlow.Web.Tests DeckFlow.CLI content-kb scripts` must, after the commit, list exactly 74 paths — the 64 allowlist paths plus the ten M-files — and nothing else.
    (b) `git diff --name-only main..HEAD | grep -Ei 'FeatureFlagCatalog|FeatureFlagStore|ToolRegistry|DeckPageTab|SeoPaths|sitemap|PacketSessionCache|Help/creator-style'` must return zero lines.
    (c) `git diff main..HEAD -- DeckFlow.Core DeckFlow.Core.Tests | grep -c 'tool.creator-style.enabled'` must return 0.
    (d) `git diff main..HEAD -- '*.csproj' | grep -c 'PackageReference'` must return 0.
    Build and test passing is NOT sufficient on its own — an older copy of a file main has since improved would compile green while silently reverting Cycle 18/19 work. The audit is the gate that catches that.

    Commit with a Conventional Commits message, plain default author, no `Co-Authored-By` trailer. Subject: `feat(112): port Cycle 17 creator-style Core engine`. Body: note this is Commit 1 of 2 per D-07, list the nine additive Core M-file hunks and their one-line justifications (D-09 requires every M-file touched to carry a justification), and state that no package reference was added.

    README is not updated at 112: no user-visible behavior changes because no route is wired until Phase 114. State this explicitly in the plan summary rather than silently skipping it.
  </action>
  <verify>
    <automated>"/mnt/c/Program Files/dotnet/dotnet.exe" build DeckFlow.sln &amp;&amp; test $(git diff --name-only main..HEAD -- DeckFlow.Web DeckFlow.Web.Tests DeckFlow.CLI content-kb | wc -l) -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/format-check-changed.sh staged` exits 0 with the full Commit 1 set staged, and any fix it required is inside this commit (`git log --oneline -1` shows exactly one new commit, not two).
    - `"/mnt/c/Program Files/dotnet/dotnet.exe" build DeckFlow.sln` exits 0 with 0 errors, and the warning count is less than or equal to the number recorded in `112-BASELINE.md` §"Build baseline".
    - No warning ID appears in the build output that is absent from the baseline warning-ID table.
    - The targeted Core test command in the action exits 0 with 0 failed tests.
    - The full `DeckFlow.Core.Tests` run introduces no failure that is not already listed under the baseline's pre-existing-failures heading.
    - `git diff --name-only main..HEAD -- DeckFlow.Core DeckFlow.Core.Tests DeckFlow.Web DeckFlow.Web.Tests DeckFlow.CLI content-kb scripts | wc -l` returns 74.
    - `git diff --name-only main..HEAD | grep -Ec 'FeatureFlagCatalog|FeatureFlagStore|ToolRegistry|DeckPageTab|SeoPaths|sitemap|PacketSessionCache|Help/creator-style'` returns 0.
    - `git diff main..HEAD -- DeckFlow.Core DeckFlow.Core.Tests | grep -c 'tool.creator-style.enabled'` returns 0.
    - `git diff main..HEAD -- '*.csproj' | grep -c 'PackageReference'` returns 0.
    - `git log -1 --format=%B` starts with `feat(112): port Cycle 17 creator-style Core engine` and contains no `Co-Authored-By` line.
    - `git log -1 --format=%an` is the repo's configured default author (`luntc1972`), not an AI attribution.
  </acceptance_criteria>
  <done>Commit 1 exists on `feat/personal-tools`, format-gated, path-audited, build-green against the baseline, and with the ported Core suite passing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| working tree → git history | Once committed, an unnoticed regression to shipped Cycle 16/18/19 code becomes part of the branch's record |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-112-06 | Tampering | Silent revert of shipped work by a stale file copy that still compiles and still passes tests | mitigate | D-08 path audit as an explicit gate: exact 74-path count, never-port name grep, `tool.creator-style.enabled` content grep, csproj `PackageReference` grep — all run before the commit is accepted |
| T-112-07 | Tampering | Formatter overreach converting `{ get; init; }` to `{ get; }`, which makes System.Text.Json silently skip the property at deserialization time | mitigate | Task 2 explicitly instructs refusal of the five `.editorconfig` carve-out violations rather than applying them; the `CarveOutGuard` test in the suite backstops it |
| T-112-08 | Repudiation | An unprovable "no new warnings" claim | mitigate | Warning count and warning IDs are compared line-by-line against the committed `112-BASELINE.md` table, not asserted from memory |
| T-112-SC | Tampering | Package-manager installs (NuGet) | mitigate | `git diff main..HEAD -- '*.csproj'` piped to `grep -c 'PackageReference'` must return 0 before commit; `Testcontainers.PostgreSql` specifically forbidden |
</threat_model>

<verification>
- `"/mnt/c/Program Files/dotnet/dotnet.exe" build DeckFlow.sln` — 0 errors, warnings <= baseline.
- Targeted Core creator-style test filter — 0 failures.
- `git diff --name-only main..HEAD -- DeckFlow.Core DeckFlow.Core.Tests DeckFlow.Web DeckFlow.Web.Tests DeckFlow.CLI content-kb scripts | wc -l` — 74.
- Never-port name grep and `tool.creator-style.enabled` content grep — 0 matches each.
- `scripts/format-check-changed.sh staged` — exit 0.
</verification>

<success_criteria>
- All nine Core M-file hunks applied additively onto main's current content, zero deleted lines.
- Solution builds with 0 errors and no new warnings versus the recorded baseline (PORT-01, criterion 2).
- The ported Core creator-style test suite runs and passes (PORT-01, criterion 4).
- Commit 1 of D-07's two commits exists, format-gated and path-audited.
</success_criteria>

<output>
Create `.planning/phases/112-cycle-17-code-port/112-03-SUMMARY.md` when done, quoting the post-commit warning count against the baseline, the Core test summary line, the 74-path audit result, and the commit SHA.
</output>
