---
phase: 42-orchestrator-extraction
plan: 02
type: execute
wave: 2
depends_on: ["42-01"]
files_modified:
  - DeckFlow.Core/Orchestration/ContentKbOrchestrator.cs
  - DeckFlow.Core/Knowledge/DistillationValidation.cs
autonomous: true
requirements: [ORCH-01]
user_setup: []

must_haves:
  truths:
    - "ContentKbOrchestrator implements every IContentKbOrchestrator operation by lifting the existing CLI internal-static domain method bodies"
    - "All moved domain logic uses the injected synchronous progress sink (IOrchestratorProgress.Report) + result records instead of Console.WriteLine / Console.Error"
    - "Moved distill validators and constants are consolidated into DistillationValidation.cs with byte-identical semantics (D-07)"
    - "Stores/services are constructor-injected; the host-resolved artifactRoot arrives via a ContentKbOrchestratorOptions record (not a bare string); operation methods take only operation args + progress + ct (D-04)"
    - "AddSource validates the source type and short-circuits to the InvalidType outcome BEFORE constructing any DB/store, preserving today's exit-2-before-store-init behavior + exact message"
    - "DeckFlow.sln builds 0 errors / 0 new warnings with the implementation present"
  artifacts:
    - path: "DeckFlow.Core/Orchestration/ContentKbOrchestrator.cs"
      provides: "Concrete orchestrator: lifted harvest/distill/maintenance/source/export domain logic (ORCH-01)"
      contains: "class ContentKbOrchestrator"
      min_lines: 400
    - path: "DeckFlow.Core/Knowledge/DistillationValidation.cs"
      provides: "Consolidated validators incl. all-zero-timestamp clip rule + projected-cost math + distill constants (D-07)"
      contains: "ValidateClips"
  key_links:
    - from: "DeckFlow.Core/Orchestration/ContentKbOrchestrator.cs"
      to: "DeckFlow.Core/Knowledge/DistillationValidation.cs"
      via: "validator calls"
      pattern: "DistillationValidation\\."
    - from: "DeckFlow.Core/Orchestration/ContentKbOrchestrator.cs"
      to: "IOrchestratorProgress"
      via: "progress.Report for live per-video lines"
      pattern: "progress\\?\\.Report"
---

<objective>
Implement `ContentKbOrchestrator` by lifting the domain bodies from the existing CLI internal-static `Run*Async` overloads into Core, swapping `Console`/`Serilog.ILogger.Information` progress lines for the injected synchronous `IOrchestratorProgress` sink + structured result records (D-02/D-03), and constructor-injecting the stores/services (D-04). Consolidate the moved distill validators, projected-cost math, and distill constants into `DistillationValidation.cs` with byte-identical semantics (D-07).

Purpose: Move the orchestration logic into Core so CLI and Studio share one implementation (ORCH-01); closes the v1.6 ContentKbCommandRunners god-class backlog item.
Output: One `ContentKbOrchestrator` class implementing the facade, plus the consolidated validators in DistillationValidation.cs. CLI adapters are NOT touched yet (that is Wave 3).

Note (review MEDIUM "Plan 02 may create a new god class"): a SINGLE `ContentKbOrchestrator` class implementing the facade is a deliberate, behavior-preserving extraction step. ISP is honored at the CONSUMER boundary (Studio depends on a slice sub-interface), and the facade decomposition (Plan 01) is real. Splitting the IMPLEMENTATION into one class per sub-interface is explicitly future cleanup — not done here because it would mix a structural refactor into a pure relocation and risk parity drift. This is an honest, temporary compromise on implementation SRP, recorded so a later phase can finish it.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/42-orchestrator-extraction/42-CONTEXT.md
@.planning/phases/42-orchestrator-extraction/42-01-SUMMARY.md

<interfaces>
<!-- Contracts from Wave 1 (Plan 01). Implement against these; do not re-design. -->
Facade + sub-interfaces in DeckFlow.Core/Orchestration/:
  IContentKbOrchestrator : IHarvestOrchestrator, IDistillOrchestrator,
    IContentMaintenanceOrchestrator, IContentSourceManager, IContentIndexExporter
Result records: HarvestResult, DistillResult, ContentSourceResult,
  ContentMaintenanceResult, BlockedVideoListResult, ContentIndexExportResult.
  (DistillResult.Success / HarvestResult.Success / etc. are `required` — construct
  every result with an explicit Success value; collections init to Array.Empty<T>().)
Progress: IOrchestratorProgress? progress (D-03) — SYNCHRONOUS `void Report(string)`,
  NOT System.IProgress<string>. Emit live lines via progress?.Report(...).
Options: ContentKbOrchestratorOptions { required string ArtifactRoot } (from Plan 01) —
  the ctor consumes this options record to receive the host-resolved artifactRoot, NOT a
  bare `string artifactRoot` param (round-2 MEDIUM fix: keeps the ctor cleanly DI-resolvable
  so AddContentKbOrchestrator() in Plan 03 needs no global bare-string registration). Read the
  ArtifactRoot off the options record inside the ctor; path resolution still happens host-side (D-06).

<!-- Source domain bodies to LIFT (DeckFlow.CLI/ContentKbCommandRunners.cs):
  internal RunDistillAsync (lines 384-573) — the full distill loop
  internal RunHarvestAsync (lines 647-709) + HarvestExplicitVideoIdsAsync (713-768)
    + HarvestSourceAsync (770-791) + HarvestVideoAsync (793-841)
    + ResolveHarvestVideoIdAsync (843-879) + PersistTranscriptResultAsync (881-914)
    + WarnIfFfmpegUnavailableAsync (916-925) + MarkFailedIfPossibleAsync (927-937)
    + LogFetch/LogFallbackRatio/IsTerminalSuccess/GetCaptionTrackKind (941-968)
  internal RunBlockVideoAsync (250-282), RunUnblockVideoAsync (284-303),
    RunCorpusResetAsync (305-329), RunListBlockedAsync (331-346)
  DistillVideoAsync (970-1177) + MarkSkippedOverCapAsync (1179-1192)
    + FilterTags (1194-1218) + GetContentNaturalKey/Info (1220-1235)
  ParseVideoIds (580-592) — keep in CLI (it parses a CLI option); orchestrator takes IReadOnlyList<string>?
  ContentSource add/set-enabled domain (14-66, 1285-1307 unique-violation handling)
    INCLUDING IsValidContentSourceType — the type check must run FIRST (short-circuit)
Validators to CONSOLIDATE into DistillationValidation.cs (D-07):
  ValidateTranscriptLength (1237-1243), ValidateSummary (1245-1251),
  ValidateClips (1253-1269) INCLUDING the all-zero-timestamp rule (1265-1268)
    — NOTE: Core DistillationValidation.ValidateClips currently MISSING this rule; add it,
  CountWords (already in Core), ComputeProjectedVideoCostUsd (1274-1277),
  ComputeProjectedCallCostUsd (1279-1280), EstimateTokenCount (1282-1283)
Distill constants to move to Core (1391-1403): ShortVideoMaxDuration, SummaryMaxOutputTokens,
  ClipsMaxOutputTokens, TagsMaxOutputTokens, MaxTranscriptInputTokens, DistillationCallCount,
  DistillStatus{Distilled,SkippedOverCap,Failed,Filtered}. (SummaryMaxWords/MinClipCount/
  MaxClipCount already exist in DistillationValidation — do NOT duplicate.)

Core logging note: use ILogger<ContentKbOrchestrator> defaulting to NullLogger<T>.Instance
(Microsoft.Extensions.Logging — Core does NOT reference Serilog). Today's logger.Information
lines that are real-time operator feedback become progress?.Report(...) emissions (parity);
internal structured logs may stay as ILogger calls. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Consolidate distill validators + constants into DistillationValidation.cs (D-07)</name>
  <read_first>
    - DeckFlow.Core/Knowledge/DistillationValidation.cs (current validators — ValidateClips here is MISSING the all-zero-timestamp check that the CLI copy has)
    - DeckFlow.CLI/ContentKbCommandRunners.cs lines 1237-1283 (validators + cost math) and 1391-1403 (constants)
    - DeckFlow.Core/Knowledge/DistillationSchemas.cs (existing constant/prompt home — sibling consolidation target)
    - DeckFlow.Core.Tests/CommandRunnerValidateClipsTests.cs (the anchor that asserts all-zero clips reject before DB writes — semantics must stay identical)
  </read_first>
  <action>
    In `DeckFlow.Core/Knowledge/DistillationValidation.cs`, make the validators byte-identical supersets of the CLI copies (D-07):
    - Extend the existing `ValidateClips(IReadOnlyList<ClipItem>)` to ADD the CLI rule that is currently absent in Core: throw `InvalidOperationException("Clip extraction cannot return every clip with timestamp 0.")` when `clips.All(clip => (clip.TimestampSeconds ?? 0) == 0)`. Preserve the existing count and negative-timestamp checks and their exact message strings. This is the rule the anchor test exercises.
    - Add `ValidateTranscriptLength(string transcript)` throwing `"Transcript too long for the distillation context window."` when `EstimateTokenCount(transcript) > MaxTranscriptInputTokens`. `ValidateSummary` already exists in Core — reuse it; do not duplicate.
    - Add the cost/token helpers with identical math: `EstimateTokenCount(string) => Math.Max(1, (int)Math.Ceiling(text.Length / 4m))`; `ComputeProjectedCallCostUsd(string transcript, int maxOutputTokens) => LlmSpendLedger.ComputeCostUsd(EstimateTokenCount(transcript), maxOutputTokens)`; `ComputeProjectedVideoCostUsd(string transcript) => LlmSpendLedger.ComputeCostUsd(EstimateTokenCount(transcript) * DistillationCallCount, SummaryMaxOutputTokens + ClipsMaxOutputTokens + TagsMaxOutputTokens)`.
    - Add the distill constants that are not already present: `ShortVideoMaxDuration` (TimeSpan.FromSeconds(60) — keep the existing "Why" comment), `SummaryMaxOutputTokens=400`, `ClipsMaxOutputTokens=1200`, `TagsMaxOutputTokens=200`, `MaxTranscriptInputTokens=120_000`, `DistillationCallCount=3`, and the four distill-status string constants (`"distilled"`, `"skipped_over_cap"`, `"failed"`, `"filtered"`). Choose the existing Core home (`DistillationValidation` or `DistillationSchemas`) consistently; do not create a new constants file unless a clean home does not exist. Keep `internal` visibility unless the orchestrator (same assembly) needs them — same assembly, so `internal` is fine.

    Do NOT change any existing message text, threshold value, or rounding. Touch only lines that need touching (project formatting rule).

    Verify the consolidated validator is what the Core-backed distill path actually calls before any writes — this is a consolidation of an already-enforced CLI rule, NOT a new CLI behavior change (it just lives in one place now).
  </action>
  <verify>
    <automated>"/mnt/c/Program Files/dotnet/dotnet.exe" build DeckFlow.sln -warnaserror 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `DeckFlow.Core/Knowledge/DistillationValidation.cs` `ValidateClips` contains the string `"Clip extraction cannot return every clip with timestamp 0."` (`grep -n` on the file, anchored to a `throw` line, matches).
    - The constants `MaxTranscriptInputTokens`, `SummaryMaxOutputTokens`, `ClipsMaxOutputTokens`, `TagsMaxOutputTokens`, `DistillationCallCount`, and the four distill-status strings resolve in Core (build succeeds when the orchestrator references them in Task 2).
    - No existing message string or numeric threshold in DistillationValidation.cs was altered (diff shows only additions, no edits to the existing message-text lines).
    - `dotnet build DeckFlow.sln -warnaserror` = 0 errors / 0 warnings.
  </acceptance_criteria>
  <done>DistillationValidation.cs holds the consolidated validators/constants byte-identically; the all-zero-timestamp rule is present in Core and runs before any distill write.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement ContentKbOrchestrator by lifting CLI domain bodies</name>
  <behavior>
    - Distill: metered-provider + non-dryRun -> DistillResult.Success=false, no run started (parity with CLI exit 1).
    - Distill dry-run: emits "WOULD distill/skip" lines via the synchronous progress sink; populates WouldRun + ProjectedSpendUsd; no rows written.
    - Distill real run: validation failure (e.g. all-zero clips) sets distill status "failed", writes NO summary/clip/index rows, run completes, DistillResult.Success=true (parity with anchor test exit 0).
    - Harvest: short videos and blocked videos skipped; already-harvested terminal-success skipped; explicit --video-ids bypasses recent-N walk; HarvestResult carries caption/whisper counts.
    - BlockVideo: block row written FIRST, then content + site-index rows deleted (partial-failure-safe ordering preserved).
    - CorpusReset dry-run: deletes nothing, reports preserve-intent.
    - AddSource invalid type: validate type FIRST, return ContentSourceResult{ Success=false, Outcome=InvalidType, Message=<exact unsupported-type message> } BEFORE any store/DB construction (parity with today's exit-2-before-store-init).
    - AddSource: unique-violation on same url -> Success with AlreadyExistsSameUrl outcome (exit 0); slug conflict -> SlugConflict outcome (exit 3).
  </behavior>
  <read_first>
    - DeckFlow.CLI/ContentKbCommandRunners.cs (ALL internal-static + private domain helpers listed in the context interfaces block — these bodies are lifted)
    - DeckFlow.Core/Content/IContentVideoStore.cs, IContentSiteIndexStore.cs, IContentSourceStore.cs, IBlockedVideoStore.cs, ILlmSpendLedger.cs, IWhisperSpendLedger.cs, IContentHarvestRunStore.cs
    - DeckFlow.Core/Integration/ILlmDistillationService.cs, IYouTubeChannelVideoLister.cs, ITranscriptSource.cs, IFfmpegAudioChunker.cs
    - DeckFlow.Core/Knowledge/ContentTagVocabulary.cs, ContentArtifactWriter.cs (FilterTags + artifact emit dependencies)
    - DeckFlow.Core/Orchestration/*.cs (the Wave-1 contracts + result records + IOrchestratorProgress + ContentKbOrchestratorOptions)
  </read_first>
  <action>
    Create `DeckFlow.Core/Orchestration/ContentKbOrchestrator.cs`, namespace `DeckFlow.Core.Orchestration`, `public sealed class ContentKbOrchestrator : IContentKbOrchestrator`.

    Constructor-inject the stores/services as interfaces (D-04): `IContentSourceStore`, `IContentVideoStore`, `IContentSiteIndexStore`, `IBlockedVideoStore`, `IContentHarvestRunStore`, `ILlmSpendLedger`, `IWhisperSpendLedger`, `ILlmDistillationService`, `IYouTubeChannelVideoLister`, `ITranscriptSource`, `IFfmpegAudioChunker`, a `Func<DateTimeOffset> utcNow` clock, a `ContentKbOrchestratorOptions options` record (carrying the host-resolved `ArtifactRoot` — round-2 MEDIUM fix: take the typed options record, NOT a bare `string artifactRoot`, so the ctor is cleanly DI-resolvable; read `options.ArtifactRoot` into a private field for the distill/artifact paths — Core does NOT resolve paths, D-06), and an optional `ILogger<ContentKbOrchestrator> logger = null` defaulting to `NullLogger<ContentKbOrchestrator>.Instance`. (`isSubscriptionProvider` is NOT a ctor dep — it is per-run, passed as a DistillAsync operation arg.) `ArgumentNullException.ThrowIfNull` each non-optional dependency (including `options`). Use `IReadOnlyList<T>` on any collection members.

    Lift each domain body from the CLI internal-static/private methods named in the context block into matching orchestrator methods. Every returned result is constructed with an explicit `Success = ...` (records are `required`-Success). Live lines use `progress?.Report(...)` (synchronous `IOrchestratorProgress`):
    - `DistillAsync` <- internal `RunDistillAsync` (the loop body lines 384-573) + `DistillVideoAsync` + `MarkSkippedOverCapAsync` + `FilterTags` + natural-key helpers. Replace `Console.WriteLine($"WOULD ...")`, `Console.WriteLine($"{disposition} ...")`, and `Console.WriteLine($"Dry run complete...")` with `progress?.Report(...)` carrying the identical text (parity = same real-time lines). Replace the metered-provider `Console.Error.WriteLine(message)` + `return 1` with `return new DistillResult { Success = false, AbortedReason = message, ... }`. Replace the final/dry-run return values with a populated `DistillResult`. Call `DistillationValidation.ValidateSummary/ValidateClips/ValidateTranscriptLength` and the consolidated cost helpers instead of the local copies.
    - `HarvestAsync` <- internal `RunHarvestAsync` + `HarvestExplicitVideoIdsAsync` + `HarvestSourceAsync` + `HarvestVideoAsync` + resolve/persist helpers. The two `Console.Error.WriteLine` source-selection errors in HarvestExplicitVideoIdsAsync become `HarvestResult { Success=false, Message=... }`. Real-time `logger.Information` per-video lines that are operator feedback emit via `progress?.Report`; preserve the LogFallbackRatio structured log.
    - `BlockVideoAsync` / `UnblockVideoAsync` / `ResetCorpusAsync` / `ListBlockedAsync` <- the four internal-static methods (250-346). ListBlocked returns a `BlockedVideoListResult` of items (Core does NOT write the tab-delimited lines — the CLI adapter formats them, D-02). Preserve the block-row-first ordering and its "Why" comment.
    - `AddSourceAsync` / `SetSourceEnabledAsync` <- the source add/set-enabled domain (14-66) + `HandleContentSourceUniqueViolationAsync` (1285-1307) + `IsValidContentSourceType`/`IsContentSourceUniqueViolation`/`ExceptionContains` helpers. CRITICAL ordering (review MEDIUM): in `AddSourceAsync`, call `IsValidContentSourceType(type)` FIRST and on failure return `new ContentSourceResult { Success = false, Outcome = ContentSourceOutcome.InvalidType, Message = <the exact unsupported-type message CLI prints today> }` BEFORE touching the injected source store — preserving today's reject-before-store-init behavior (the store interface is ctor-injected, but the validation branch must not call any store method on the invalid-type path). Map the remaining CLI branches to `ContentSourceResult` outcomes: valid->Added (slug + id populated), same-url unique violation->AlreadyExistsSameUrl, slug conflict->SlugConflict, other->Error. The `Console.WriteLine($"Computed slug...")`/`"source already exists"`/`"Added content source"` lines map to result fields + `progress?.Report`. Inject `IContentSourceStore` rather than constructing `ContentSourceStore` (D-04/D-05).
    - `ExportIndexAsync` <- the row-mapping portion of `RunContentIndexExportAsync` (calls `indexStore.EnsureSchemaAsync` + `GetAllRowsAsync`, maps to `ContentIndexExportRow.From`, preserving today's row ORDER); returns `ContentIndexExportResult` with Rows. The JSON serialization + file write + path default stay in the CLI adapter (Wave 3, D-06). Plan 03 adds a golden-fixture test pinning the serialized byte shape — preserve property order + ordering here.

    Move the lifted private helpers (`GetContentNaturalKey`, `GetContentNaturalKeyInfo`, `IsTerminalSuccess`, `GetCaptionTrackKind`, `LogFetch`, `LogFallbackRatio`, `FilterTags`, etc.) into the orchestrator as private methods/static helpers. Do NOT introduce any `Console.` call. Do NOT add a Serilog using-directive. Preserve all existing "Why:" comments verbatim on the lines they annotate.
  </action>
  <verify>
    <automated>"/mnt/c/Program Files/dotnet/dotnet.exe" build DeckFlow.sln -warnaserror 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `DeckFlow.Core/Orchestration/ContentKbOrchestrator.cs` declares `public sealed class ContentKbOrchestrator : IContentKbOrchestrator` and the build confirms every interface member is implemented (no CS0535).
    - `grep -rn "Console\." DeckFlow.Core/Orchestration/ContentKbOrchestrator.cs` returns nothing; `grep -n "using Serilog" DeckFlow.Core/Orchestration/ContentKbOrchestrator.cs` returns nothing (Console-free, Serilog-free Core — D-02).
    - `grep -c "progress?.Report" DeckFlow.Core/Orchestration/ContentKbOrchestrator.cs` (filtered with `grep -v '^[[:space:]]*//'`) > 0 (live progress via the synchronous injected sink — D-03); no `IProgress<` or `new Progress<` appears in the file.
    - Constructor parameters are interface types + `Func<DateTimeOffset>` + a `ContentKbOrchestratorOptions options` record (NOT a bare `string artifactRoot`) + optional `ILogger<ContentKbOrchestrator>`; `isSubscriptionProvider` is NOT a ctor param (it is a DistillAsync arg); no `FileInfo` parameter and no `new ContentVideoStore`/`new ContentSourceStore` appears in the file (D-04/D-05/D-06). `grep -n "ContentKbOrchestratorOptions" DeckFlow.Core/Orchestration/ContentKbOrchestrator.cs` matches the ctor param; `grep -nE "ctor.*string artifactRoot|, string artifactRoot" DeckFlow.Core/Orchestration/ContentKbOrchestrator.cs` returns nothing (no bare-string ctor param).
    - `AddSourceAsync` returns the `InvalidType` outcome from a branch that precedes any source-store call (the type check is the first statement that can return; verified by reading the method — the invalid-type path touches no store member).
    - Orchestrator calls `DistillationValidation.ValidateClips` / `ValidateSummary` / `ValidateTranscriptLength` (the consolidated home — D-07), not local copies.
    - `dotnet build DeckFlow.sln -warnaserror` = 0 errors / 0 warnings.
  </acceptance_criteria>
  <done>ContentKbOrchestrator implements the full facade with all domain logic lifted, Console-free, ctor-injected stores + options-record artifactRoot, synchronous progress-sink emissions, AddSource invalid-type short-circuit preserved, and consolidated validators.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Pure relocation of domain logic from one in-process assembly (CLI) to another (Core). No new external surface, no new input/auth boundary, no new network/DB egress. The same stores write to the same DB. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42-03 | Tampering / Data-integrity | DistillAsync validation short-circuit | mitigate | The lifted ValidateClips/ValidateSummary MUST run before any InsertSummary/InsertClip/UpsertRow so a malformed distill never writes partial rows — exactly the invariant the re-pointed anchor test (Plan 03) verifies. Acceptance criteria require the consolidated validators be called from the orchestrator. |
| T-42-04 | Tampering | Spend-ledger record-before-next-call ordering | mitigate | The lifted DistillVideoAsync must preserve the "record incurred cost BEFORE next call" ordering (HIGH-1/FIX-1) and the WouldExceedCap gates between summary/clips/tags; lifting copies the bodies verbatim rather than re-deriving. |
| T-42-04b | Tampering | AddSource invalid-type short-circuit | mitigate | Invalid source type must reject (InvalidType outcome) before constructing/calling any store, preserving today's exit-2-before-store-init behavior and exact message; acceptance criteria require the type check to precede any store call. |
| T-42-05 | Information Disclosure | progress sink / logger lines | accept | Same per-video ids/counts already in today's logs; no secrets. Core has no connection string (provider selection stays host-side, D-05); the options record carries only the artifactRoot path. |
| T-42-SC | Tampering | npm/pip/cargo installs | n/a | No package installs — 0 new packages; orchestrator composes existing Core interfaces only. |
</threat_model>

<verification>
- `dotnet build DeckFlow.sln -warnaserror` = 0E/0W.
- Console-free + Serilog-free Core orchestrator, synchronous progress sink only (grep gates above).
- Validators consolidated in DistillationValidation.cs incl. all-zero-timestamp rule.
- Ctor takes ContentKbOrchestratorOptions (no bare-string artifactRoot param).
- AddSource invalid-type short-circuits before store init; block-row-first + spend-record-before-next-call ordering preserved (verified by Plan 03 anchor + parity tests + Wave 3 build).
</verification>

<success_criteria>
- ContentKbOrchestrator implements IContentKbOrchestrator with all CLI domain bodies lifted (SC1).
- No Console / Serilog in Core orchestration (D-02).
- Ctor consumes the ContentKbOrchestratorOptions record for the host-resolved artifactRoot (DI-clean, round-2 MEDIUM).
- Validators + constants consolidated byte-identically (D-07).
- Single-implementation extraction recorded as a deliberate temporary compromise (implementation SRP split deferred).
- Solution builds clean.
</success_criteria>

<output>
Create `.planning/phases/42-orchestrator-extraction/42-02-SUMMARY.md` when done.
</output>
</content>
