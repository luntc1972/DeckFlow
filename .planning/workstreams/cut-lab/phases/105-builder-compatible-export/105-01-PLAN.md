---
phase: 105-builder-compatible-export
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - DeckFlow.Web/Models/CutLab/CutLabState.cs
  - DeckFlow.Web/Services/CutLab/CutLabStateSerializer.cs
  - DeckFlow.Web/Services/CutLab/CutLabPageService.cs
  - DeckFlow.Web.Tests/CutLabOriginalEntriesTests.cs
autonomous: true
requirements: [EXPORT-02]
must_haves:
  truths:
    - "The pristine original imported list is captured once at first intake and never overwritten by later Decide/Goals/Whatif posts."
    - "The captured original list survives a Phase-104 saved-scenario reload round-trip."
  artifacts:
    - path: "DeckFlow.Web/Models/CutLab/CutLabState.cs"
      provides: "OriginalEntries capture-once field + CutLabOriginalEntry DTO carrying Name/Quantity/Board/SetCode/CollectorNumber/Category"
      contains: "OriginalEntries"
    - path: "DeckFlow.Web/Services/CutLab/CutLabPageService.cs"
      provides: "Capture-once population of OriginalEntries from the full-fidelity imported DeckEntry list"
    - path: "DeckFlow.Web.Tests/CutLabOriginalEntriesTests.cs"
      provides: "Capture-once + survives-reload coverage"
  key_links:
    - from: "CutLabPageService.ProcessAsync"
      to: "CutLabState.OriginalEntries"
      via: "capture-once guard mirroring BaselineSnapshot"
      pattern: "OriginalEntries is null"
    - from: "CutLabStateSerializer.Deserialize"
      to: "CutLabState.OriginalEntries"
      via: "bounded Take clamp within MaxUploadBytes"
      pattern: "OriginalEntries"
---

<objective>
Close the biggest research gap: Cut Lab's persisted `CutLabState.Pool` drops `Board`/`SetCode`/`CollectorNumber`/`Category` after the first POST, and `RehydrateIntakeRequestFromState` (CutLabController.cs:304-346) reconstructs a lossy synthetic `DeckText` on every later request. Add a **capture-once `OriginalEntries` snapshot** to `CutLabState` populated from the full-fidelity imported `DeckEntry` list, mirroring the existing `BaselineSnapshot` capture-once pattern. This is the immutable diff baseline the add/cut patch (EXPORT-02, Plan 105-03) is computed against.

Purpose: Without a stable original-list snapshot, criterion 2 (patch vs. the builder's original list) cannot be computed — `Pool` is the current post-cut/post-swap set, not the original.
Output: New `OriginalEntries` state field + `CutLabOriginalEntry` DTO, serializer clamp, capture-once wiring, and tests proving capture-once + scenario-reload survival.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/cut-lab/phases/105-builder-compatible-export/105-CONTEXT.md
@.planning/workstreams/cut-lab/phases/105-builder-compatible-export/105-RESEARCH.md
@DeckFlow.Web/Models/CutLab/CutLabState.cs
@DeckFlow.Web/Services/CutLab/CutLabStateSerializer.cs

<interfaces>
<!-- Capture-once pattern to mirror (BaselineSnapshot), CutLabPageService.cs:315-337 -->
if (state.BaselineSnapshot is null) { /* compute once */ state = state with { BaselineSnapshot = ... }; }

<!-- Full-fidelity source list is available in ProcessAsync: -->
<!--   load = await _deckEntryLoader.LoadFromSourceAsync(...)  (CutLabPageService.cs:195) -->
<!--   var entries = ReflagInferredCommanders(load.Entries);   (CutLabPageService.cs:211) -->
<!--   List<DeckEntry> analyzedEntries = analysis.AnalyzedEntries; (CutLabPageService.cs:215) -->
<!-- analyzedEntries is the mainboard + commander + IncludeSideboard/IncludeMaybeboard cards that seed the pool. -->

<!-- DeckEntry fields (DeckEntry.cs:6-30): Name, NormalizedName, Quantity, Board, SetCode?, CollectorNumber?, Category?, IsFoil -->

<!-- Serializer bounds (CutLabStateSerializer.cs:11-15): MaxUploadBytes=262_144, MaxPackages=50, MaxDecisions=500; Take clamps at ~:55-59 -->

<!-- BuildState carries prior fields forward (CutLabPageService.cs:688-714): Packages/Decisions/Goals/BaselineSnapshot from priorState -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add OriginalEntries capture-once state field + serializer clamp</name>
  <files>DeckFlow.Web/Models/CutLab/CutLabState.cs, DeckFlow.Web/Services/CutLab/CutLabStateSerializer.cs</files>
  <behavior>
    - Serialize→Deserialize round-trip preserves OriginalEntries contents (Name/Quantity/Board/SetCode/CollectorNumber/Category).
    - Deserialize of a pre-105 JSON blob with no OriginalEntries yields an empty list (backward compatible).
    - Deserialize clamps OriginalEntries to a bounded count so an oversized payload cannot exceed MaxUploadBytes.
  </behavior>
  <action>
    Add a new `CutLabOriginalEntry` sealed record (Name, Quantity, Board, SetCode?, CollectorNumber?, Category?) to CutLabState.cs and an `IReadOnlyList<CutLabOriginalEntry> OriginalEntries { get; init; } = [];` field on `CutLabState` (mirror the empty-initializer/xmldoc style of the existing `Decisions`/`RoleFloors` fields at CutLabState.cs:19-46 so pre-105 blobs deserialize cleanly). Deliberately store a LIGHT DTO, not full `DeckEntry`, to control serialized size and avoid the `required NormalizedName` field — NormalizedName is re-derived at export time via `CardNormalizer.Normalize` (CardNormalizer.cs:15). In CutLabStateSerializer.cs add a clamp constant `MaxOriginalEntries` (e.g. 200) and a `.Take(MaxOriginalEntries)` in Deserialize mirroring the existing `.Take(MaxPackages)`/`.Take(MaxDecisions)` clamps (CutLabStateSerializer.cs:55-59); the existing `MaxUploadBytes` byte-count guard already covers the total envelope. Per V3 (RESEARCH Security Domain), the new field must not push a realistic 150-card session over the 262,144-byte cap — a ~200-entry light DTO snapshot is well within budget.
  </action>
  <verify>
    <automated>dotnet test DeckFlow.Web.Tests --filter FullyQualifiedName~CutLabStateSerializer</automated>
  </verify>
  <done>OriginalEntries round-trips through the serializer, pre-105 blobs deserialize to an empty list, and the field is clamped in Deserialize.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Capture OriginalEntries once in ProcessAsync + survive scenario reload</name>
  <files>DeckFlow.Web/Services/CutLab/CutLabPageService.cs, DeckFlow.Web.Tests/CutLabOriginalEntriesTests.cs</files>
  <behavior>
    - First successful intake populates state.OriginalEntries from the full-fidelity analyzedEntries (Board/SetCode/CollectorNumber/Category retained).
    - A subsequent Decide/Goals/Whatif post (which arrives with lossy rehydrated DeckText) does NOT overwrite the already-captured OriginalEntries.
    - Reloading a saved scenario whose CutLabStateJson already carries OriginalEntries preserves it unchanged (guard treats a non-empty OriginalEntries as "already captured").
  </behavior>
  <action>
    In `ProcessAsync`, after `state` is materialized from `BuildState` and before results are assembled, add a capture-once guard directly mirroring the BaselineSnapshot block at CutLabPageService.cs:315-337: `if (state.OriginalEntries.Count == 0) { state = state with { OriginalEntries = <snapshot of analyzedEntries> }; }`. Build the snapshot from `analyzedEntries` (CutLabPageService.cs:215) — the mainboard + commander + IncludeSideboard/IncludeMaybeboard cards that actually seed the pool — mapping each `DeckEntry` to a `CutLabOriginalEntry` (Name, Quantity, Board, SetCode, CollectorNumber, Category). Do NOT capture from `request.DeckText`/`DeckUrl` (post-intake these are the lossy synthetic rehydration — RESEARCH Pitfall 2). Ensure `BuildState` carries `OriginalEntries = priorState.OriginalEntries` forward alongside the other preserved fields (CutLabPageService.cs:688-714) so the capture is never lost across posts. Guard's "already captured" condition is `Count > 0` (a non-empty snapshot), which is exactly what a reloaded scenario supplies — this is what makes scenario-reload survival fall out of the same guard.
    Add `DeckFlow.Web.Tests/CutLabOriginalEntriesTests.cs` (flat, alongside the ~19 existing `CutLab*Tests.cs` — no `CutLab/` subfolder) covering the three behaviors above, using the existing `CutLabPageService` test harness/fakes pattern already used in `CutLabPageServiceTests.cs` (same fake deck loader + banlist stubs).
  </action>
  <verify>
    <automated>dotnet test DeckFlow.Web.Tests --filter FullyQualifiedName~CutLabOriginalEntries</automated>
  </verify>
  <done>OriginalEntries is captured once with full fidelity, is not overwritten by later posts, and survives a scenario-reload round-trip — all three asserted by tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| browser → /cut-lab (CutLabStateJson hidden field) | Untrusted serialized state re-posted every round; now carries OriginalEntries |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-105-01 | Denial of Service | Oversized OriginalEntries in posted state | mitigate | `MaxOriginalEntries` Take-clamp in Deserialize + existing `MaxUploadBytes` (262,144) byte guard (CutLabStateSerializer.cs:11,30,43) |
| T-105-02 | Tampering | Client-forged OriginalEntries baseline | accept | Baseline only affects the informational patch text the user copies; no server-side authority or persistence beyond the session envelope |
| T-105-SC | Tampering | package installs | mitigate | No npm/NuGet packages added by this plan; nothing to audit |
</threat_model>

<verification>
- `dotnet build` clean (no new warnings; changed-lines format gate satisfied; LF preserved).
- `dotnet test DeckFlow.Web.Tests --filter FullyQualifiedName~CutLab` green.
</verification>

<success_criteria>
- `CutLabState.OriginalEntries` exists, is capture-once, and is bounded on deserialize.
- Tests prove: full-fidelity capture at first intake, no overwrite by later posts, survival across scenario reload.
</success_criteria>

<output>
Create `.planning/workstreams/cut-lab/phases/105-builder-compatible-export/105-01-SUMMARY.md` when done.
</output>
