---
phase: 110-cut-lab-navigation-and-pool-discovery
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - DeckFlow.Web/Models/CutLabViewModel.cs
  - DeckFlow.Web/Services/CutLab/CutLabPageService.cs
  - DeckFlow.Web.Tests/CutLabPageServiceTests.cs
autonomous: true
requirements: [CLUP-16]
must_haves:
  truths:
    - "Plan 110-05 can render type line, mana cost, set+collector number, and oracle text for any resolved card by looking it up in CardTextByCardName"
    - "A card the cache could not resolve is simply absent from the lookup, so 110-05 renders no disclosure for it (fail-open) — never a crash or blank required field"
    - "Secondary assertion: the lookup lives off CutLabPoolCard, so the serialized CutLabStateJson does not grow and the Phase 108 POST-size work is preserved"
  artifacts:
    - path: "DeckFlow.Web/Models/CutLabViewModel.cs"
      provides: "CardTextByCardName lookup dictionary + CutLabCardTextView record"
      contains: "CardTextByCardName"
    - path: "DeckFlow.Web/Services/CutLab/CutLabPageService.cs"
      provides: "Build of CardTextByCardName from preResolvedCards on CutLabProcessResult"
      contains: "CardTextByCardName"
  key_links:
    - from: "DeckFlow.Web/Services/CutLab/CutLabPageService.cs"
      to: "CutLabViewModel.From"
      via: "CutLabProcessResult.CardTextByCardName"
      pattern: "CardTextByCardName"
---

<objective>
Build the view-only card-text lookup (CLUP-16 data-shaping half): a `CardTextByCardName` dictionary
on `CutLabViewModel` populated from the `ScryfallCardData` already resolved at intake — zero new
fetches, zero new endpoints. Plan 110-05 consumes this dictionary to render the text-first
disclosures in pool rows and under Structural evidence chips.

Purpose: D-28 mandates oracle context live in a view-only lookup mirroring `RoleListByCardName`,
NOT on `CutLabPoolCard` (that record round-trips through `CutLabStateJson` on every mutation; ~100
cards of oracle text would bloat every POST, directly against the Phase 108 timeout work).
Output: `CutLabCardTextView` record + `CardTextByCardName` dictionary, wired end-to-end.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/110-cut-lab-navigation-and-pool-discovery/110-CONTEXT.md
@.planning/phases/110-cut-lab-navigation-and-pool-discovery/110-UI-SPEC.md

<interfaces>
<!-- Established view-only lookup-dictionary pattern to mirror (D-28). Executor uses these directly. -->

CutLabViewModel.cs (existing pattern to follow):
  public IReadOnlyDictionary<string, string> RoleListByCardName { get; init; } =
      new Dictionary<string, string>(StringComparer.OrdinalIgnoreCase);
  public IReadOnlyDictionary<string, string> RoleKeysByCardName { get; init; } = ...;

CutLabViewModel.From(request, result, whatif, export) builds every lookup from `result` and
assigns them in the returned `new CutLabViewModel { ... }` object initializer.

ScryfallCardData (DeckFlow.Core/Manabase/ScryfallCardData.cs) fields available at intake:
  Name (required string), ManaCost (string?), TypeLine (string?), OracleText (string?),
  Set (string?), CollectorNumber (string?).

CutLabPageService.ProcessAsync (CutLabPageService.cs): `preResolvedCards`
  (IReadOnlyList<ScryfallCardData>, built ~line 256) is already in scope where CutLabProcessResult
  is constructed (~line 413). CutLabProcessResult is a sealed record (~line 25) with init-only
  members. CutLabCardNames.ToLastWinsDictionary / CutLabCardNames.Comparer are the existing
  name-normalization helpers used elsewhere in the service.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add CardTextByCardName lookup + CutLabCardTextView record, built from resolved cards</name>
  <files>DeckFlow.Web/Models/CutLabViewModel.cs, DeckFlow.Web/Services/CutLab/CutLabPageService.cs</files>
  <read_first>
    - DeckFlow.Web/Models/CutLabViewModel.cs (RoleListByCardName pattern ~lines 133-140, From factory ~218-305)
    - DeckFlow.Web/Services/CutLab/CutLabPageService.cs (CutLabProcessResult record ~25-90, ProcessAsync preResolvedCards ~256, result construction ~413-436)
    - DeckFlow.Core/Manabase/ScryfallCardData.cs (field names)
    - .planning/phases/110-cut-lab-navigation-and-pool-discovery/110-CONTEXT.md (D-28, D-29, D-31)
  </read_first>
  <behavior>
    - Given a pool with a card whose ScryfallCardData resolved, CardTextByCardName[name] returns a
      CutLabCardTextView carrying TypeLine, ManaCost, SetCode, CollectorNumber, OracleText.
    - Given a card whose data did not resolve, CardTextByCardName has no entry for that name (fail-open).
    - CutLabPoolCard exposes NO new oracle/text member (serialized state shape unchanged).
  </behavior>
  <action>
    Define a `sealed record CutLabCardTextView` (in CutLabViewModel.cs alongside the model) with
    nullable string members TypeLine, ManaCost, SetCode, CollectorNumber, OracleText — all nullable
    so a partially-resolved card never forces a blank required field (D-31). Add
    `public IReadOnlyDictionary<string, CutLabCardTextView> CardTextByCardName { get; init; }`
    defaulting to an empty `StringComparer.OrdinalIgnoreCase` dictionary, mirroring RoleListByCardName.
    In CutLabPageService: add a matching `CardTextByCardName` init member to the CutLabProcessResult
    record, and where the result is built (~line 413) populate it from `preResolvedCards` — key by
    ScryfallCardData.Name using CutLabCardNames normalization (last-wins, OrdinalIgnoreCase), mapping
    ScryfallCardData.Set to SetCode. In CutLabViewModel.From, read `result.CardTextByCardName` into
    the new VM property. Do NOT touch CutLabPoolCard (Models/CutLab/CutLabState.cs) — per D-28 the
    text must NOT live on that serialized record. Preserve LF line endings; touch only changed lines.
  </action>
  <acceptance_criteria>
    - `grep -n "CardTextByCardName" DeckFlow.Web/Models/CutLabViewModel.cs` shows the property and its
      assignment inside From().
    - `grep -n "CardTextByCardName" DeckFlow.Web/Services/CutLab/CutLabPageService.cs` shows the
      CutLabProcessResult member and its population from preResolvedCards.
    - `grep -c "OracleText\|CardText" DeckFlow.Web/Models/CutLab/CutLabState.cs` returns 0 (D-28: no
      text member added to the serialized pool-card record).
    - `sealed record CutLabCardTextView` exists with exactly five nullable string members.
    - `dotnet build DeckFlow.Web/DeckFlow.Web.csproj` completes with no new warnings/errors.
  </acceptance_criteria>
  <verify>
    <automated>dotnet build DeckFlow.Web/DeckFlow.Web.csproj -clp:ErrorsOnly</automated>
  </verify>
  <done>CardTextByCardName is populated in ProcessAsync and surfaced on the view model; build is clean; CutLabPoolCard is unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: xUnit coverage for the card-text lookup (populated + fail-open)</name>
  <files>DeckFlow.Web.Tests/CutLabPageServiceTests.cs</files>
  <read_first>
    - DeckFlow.Web.Tests/CutLabPageServiceTests.cs (BuildPoolEntries helper + existing ProcessAsync happy-path test for the harness/mocking pattern)
    - DeckFlow.Web/Services/CutLab/CutLabPageService.cs (result construction just edited in Task 1)
  </read_first>
  <action>
    Add xUnit `[Fact]` tests that run ProcessAsync through the existing test harness and assert:
    (1) a resolved card's CardTextByCardName entry carries the type line, mana cost, set code,
    collector number, and oracle text sourced from the mocked ScryfallCardData; (2) a card with no
    resolved data has no CardTextByCardName entry (fail-open, no exception). Follow the existing
    fixture/mock-HTTP setup in this file; do not introduce a new mocking library. Preserve LF endings.
  </action>
  <acceptance_criteria>
    - New `[Fact]` methods reference `CardTextByCardName` and assert both the populated and the
      fail-open (missing-entry) cases.
    - `grep -c "CardTextByCardName" DeckFlow.Web.Tests/CutLabPageServiceTests.cs` returns >= 2.
    - `dotnet build DeckFlow.Web.Tests/DeckFlow.Web.Tests.csproj` is clean (VSTest run is unreliable
      in WSL; build-clean is the gate per CLAUDE.md).
  </acceptance_criteria>
  <verify>
    <automated>dotnet build DeckFlow.Web.Tests/DeckFlow.Web.Tests.csproj -clp:ErrorsOnly</automated>
  </verify>
  <done>Two tests lock the populated and fail-open behaviors; test project builds clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server-render → browser | Oracle text is already-cached Scryfall data re-rendered server-side; no new input crosses a boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-110-01 | Information Disclosure | CardTextByCardName payload size | accept | View-only lookup stays server-side; kept OFF CutLabPoolCard (D-28) so it never enters CutLabStateJson — no POST-size regression |
| T-110-02 | Tampering | Oracle text content | accept | Data is already-cached upstream card text; no user input, no new fetch (D-29); Razor HTML-encoding in plan 110-05 handles output |
</threat_model>

<verification>
- `dotnet build` clean across DeckFlow.Web and DeckFlow.Web.Tests.
- CardTextByCardName reachable from CutLabViewModel for plan 110-05.
- CutLabPoolCard / CutLabStateJson shape unchanged (grep gate).
</verification>

<success_criteria>
CutLabViewModel exposes a populated, fail-open `CardTextByCardName` lookup built from intake-resolved
ScryfallCardData, with no growth of the serialized pool-card state.
</success_criteria>

<output>
Create `.planning/phases/110-cut-lab-navigation-and-pool-discovery/110-01-SUMMARY.md` when done.
</output>
