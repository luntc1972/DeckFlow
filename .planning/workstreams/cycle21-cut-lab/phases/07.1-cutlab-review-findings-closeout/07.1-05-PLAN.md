---
phase: 07.1-cutlab-review-findings-closeout
plan: 05
type: execute
wave: 5
depends_on: ["07.1-04"]
files_modified:
  - DeckFlow.Web/wwwroot/css/site-grixis.css
  - DeckFlow.Web/wwwroot/css/site-planeswalker-dark.css
  - DeckFlow.Web/wwwroot/css/site-jund.css
  - DeckFlow.Web/e2e/cut-lab-theme-readability.spec.ts
  - .planning/workstreams/cycle21-cut-lab/phases/07.1-cutlab-review-findings-closeout/07.1-THEME-CONTRAST-CENSUS.md
autonomous: true
requirements: [F-6, BUILD-EOL]

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "grixis, planeswalker-dark and jund each reach at least 4.5:1 for --muted text on --panel, measured on .cutlab-intake-summary by the running spec, not only by arithmetic."
    - "Every one of the 24 themes is covered by the workflow-component contrast test, so the fix and the test no longer share a blind spot."
    - "Each raised --muted value is also at least 4.5:1 against --bg, and the 24-theme named-element sweep still passes for all its existing floors."
    - "A written census records theme -> --muted -> --panel -> ratio -> pass/fail for all 24 themes, including the two nearest passes, so the next person can see how much headroom exists."
    - "No layout CSS is added to site.css or site-common.css; only :root token values in individual theme files change."
  artifacts:
    - "DeckFlow.Web/wwwroot/css/site-grixis.css, site-planeswalker-dark.css, site-jund.css — one raised --muted token each, in :root."
    - "DeckFlow.Web/e2e/cut-lab-theme-readability.spec.ts — workflowComponentThemes no longer exists as a separate narrower list; the workflow-component test iterates the same 24-theme list the named-element sweep uses."
    - ".planning/.../07.1-THEME-CONTRAST-CENSUS.md — the 24-row census with method, raw values and measured ratios."
  key_links:
    - "site-common.css:5692 `summary { color: var(--muted); }` is why .cutlab-intake-summary renders in --muted; site-common.css:5836 gives it `background: var(--panel, #fff)`. Neither rule changes."
    - "site-common.css has 49 var(--muted) uses and site.css has 14, so raising the token moves every one of them in the three edited themes; only two are non-text (borders at site-common.css:4070 and :4096)."
---

<objective>
Close F-6: raise `--muted` in every theme where it fails the 4.5:1 WCAG AA body-text floor against
`--panel`, and widen the workflow-component contrast matrix so the fix and the test stop sharing a
blind spot.

Purpose: `cut-lab-theme-readability.spec.ts` runs its named-element sweep over all 24 themes
(`:10-35`) but the workflow-component test — the one that actually asserts the intake summary and
progress strip at 4.5:1 (`:482-499`) — iterates a separate three-entry list at `:37-41`. That is
why three failures were invisible, and why `efee8599` raised only `site-nyx.css`.

Output: the raised tokens, a 24-row census, and one theme list instead of two.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/cycle21-cut-lab/PROJECT.md
@.planning/workstreams/cycle21-cut-lab/ROADMAP.md
@.planning/workstreams/cycle21-cut-lab/phases/07.1-cutlab-review-findings-closeout/07.1-CONTEXT.md
@.planning/workstreams/cycle21-cut-lab/phases/07.1-cutlab-review-findings-closeout/07.1-RESEARCH.md
@.planning/workstreams/cycle21-cut-lab/phases/07.1-cutlab-review-findings-closeout/07.1-MUTATION-PROTOCOL.md
</context>

<locked_decisions>
**D-6 (test matrix) — locked: delete the separate `workflowComponentThemes` list and iterate the
existing 24-entry `themes` const.**
Adding the three failing themes to the short list would close F-6 and leave the drift mechanism
intact — two lists that can diverge again is the defect, not the three missing entries. One list
cannot drift from itself.
Reversibility: **reversible** — restoring a narrower list is a one-line edit.

**D-7 (runtime) — locked: measure, then set an explicit per-test timeout. Do not discover it as a
flake.**
`playwright.config.ts` declares no global `timeout`, so Playwright's 30s per-test default applies.
The widened workflow-component test performs one full `importPool` per theme per viewport —
24 x 2 = 48 imports where it previously did 6. The executor measures the wall time and sets
`test.setTimeout()` on that test to the measured value with headroom, recording both numbers in a
comment next to the call. Splitting the test per theme is explicitly NOT chosen: `test.beforeEach`
acquires an admin lock (`:398-401`), so 24 separate tests would acquire and release it 24 times and
add contention this plan has no reason to introduce.
Reversibility: **reversible**.

**Not re-theming.** F-6 raises tokens to clear the floor. It does not restyle
`.cutlab-intake-summary`, does not change which token that element uses, and does not touch a
theme the census shows passing. Layout CSS goes in `site-common.css` and never in `site.css`; this
plan adds no layout CSS anywhere — only `:root` token values in individual theme files.
</locked_decisions>

<tasks>

<task type="auto">
  <name>Task 1: Record the pre-change baseline and run the 24-theme contrast census</name>
  <files>.planning/workstreams/cycle21-cut-lab/phases/07.1-cutlab-review-findings-closeout/07.1-THEME-CONTRAST-CENSUS.md</files>
  <read_first>
    - DeckFlow.Web/e2e/cut-lab-theme-readability.spec.ts lines 1-46 (the `themes` const at :10-35, `workflowComponentThemes` at :37-41, `workflowViewports` at :43-46) and lines 396-500 (the beforeEach/afterEach, the named-element sweep at :409, and the workflow-component test at :482-499)
    - DeckFlow.Web/e2e/support/contrast.ts (`resolveContrast` and `contrastRatio` — the measurement the spec actually trusts)
    - DeckFlow.Web/wwwroot/css/site-common.css lines 5688-5700 (`summary { color: var(--muted); }`) and lines 5831-5842 (`.cutlab-intake-summary` and its `background: var(--panel, #fff)`)
    - DeckFlow.Web/wwwroot/css/site-common.css lines 4065-4100 (the only two non-text uses of `var(--muted)`)
    - DeckFlow.Web/wwwroot/css/site-grixis.css, site-planeswalker-dark.css, site-jund.css (lines 1-40 of each — the `:root` block)
    - scripts/run-web-test.sh (how the headless server is started — a browser must never open on the Windows host)
  </read_first>
  <action>
    (1) **Baseline first.** Start the app with `scripts/run-web-test.sh`, then run
    `cd DeckFlow.Web && env -u DISPLAY npx --no-install playwright test e2e/cut-lab-theme-readability.spec.ts --project=chromium-desktop`
    and record the result verbatim: pass/fail per test and the wall time of each. If the spec is
    already failing or already timing out at HEAD, that is a finding — record it in the census and
    in the SUMMARY, and state it plainly rather than folding it into this plan's result. The
    baseline is what every later claim in this plan is measured against.

    (2) **Census the tokens.** The theme population is `site.css` plus the 23 `site-*.css` files
    that are not `site-common.css`, `site-mobile.css`, `site-theme-overrides.css` or an `admin-*`
    file. Enumerate them with
    `ls DeckFlow.Web/wwwroot/css/site*.css | grep -v 'common\|mobile\|theme-overrides\|admin'`
    and confirm the resulting set is exactly the 24 entries the spec's `themes` const already
    lists at `:10-35` — if the two sets differ, that difference is itself a finding and must be
    reported before anything is changed.

    For each theme file extract the `:root` values of `--muted`, `--panel` and `--bg`, and first
    confirm the file declares `--muted` exactly once (`grep -c -- '--muted:' <file>` returns `1`)
    so the `:root` value really is the effective one. Compute the WCAG relative-luminance contrast
    ratio of `--muted` against `--panel` and against `--bg`.

    **Reconciliation floor measured at planning time** — if your census disagrees with any of
    these, re-derive before proceeding, and say which is right:
    - Failing on `--panel` (below 4.5): **grixis 3.75** (`#91889d` on `#2e3244`),
      **planeswalker-dark 3.93** (`#8b92a8` on `#2e3450`), **jund 4.34** (`#a08d7d` on `#352b24`).
    - Nearest passes, worth recording because they have almost no headroom:
      **nyx 4.56**, **sultai 4.62**.
    - All 24 themes pass against `--bg`; the lowest is grixis at **5.21**.
    - Every theme file declares `--muted` exactly once.
    Any theme the census finds failing that is not in that list of three is **in scope** — the
    entire point of this work is that the fix stops sharing the test's blind spot.

    (3) Write
    `.planning/workstreams/cycle21-cut-lab/phases/07.1-cutlab-review-findings-closeout/07.1-THEME-CONTRAST-CENSUS.md`
    with: the enumeration command and its raw output; a 24-row table
    (theme file -> `--muted` -> `--panel` -> ratio -> pass/fail -> `--bg` -> ratio); the
    computation method used; the pre-change Playwright baseline from step (1); and a short section
    naming `site-common.css:5692` and `:5836` as the two rules that make `.cutlab-intake-summary`
    a `--muted`-on-`--panel` surface, with the note that neither rule is being changed.
  </action>
  <verify>
    <automated>CENSUS=.planning/workstreams/cycle21-cut-lab/phases/07.1-cutlab-review-findings-closeout/07.1-THEME-CONTRAST-CENSUS.md; THEMES=$(ls DeckFlow.Web/wwwroot/css/site*.css | xargs -n1 basename | grep -v 'common\|mobile\|theme-overrides\|admin'); N=$(echo "$THEMES" | wc -l); ROWS=$(grep -E '^\|' "$CENSUS"); TOTAL=$(echo "$ROWS" | grep -cE '^\| *site'); test "$N" = "24" &amp;&amp; test "$TOTAL" = "24" &amp;&amp; for t in $THEMES; do C=$(echo "$ROWS" | grep -cF -- "$t"); test "$C" = "1" || { echo "FAIL-ROWCOUNT $t=$C"; exit 1; }; L=$(echo "$ROWS" | grep -F -- "$t"); NUMS=$(echo "$L" | grep -oE '[0-9]+\.[0-9]+' | wc -l); test "$NUMS" -ge 2 || { echo "FAIL-NUMS $t"; exit 1; }; done &amp;&amp; grep -qi 'baseline' "$CENSUS" &amp;&amp; grep -qiE 'pass' "$CENSUS" &amp;&amp; grep -qiE 'fail' "$CENSUS" &amp;&amp; echo CENSUS-VALID</automated>
  </verify>
  <acceptance_criteria>
    - The census file exists and its table has exactly 24 theme rows, each carrying a `--panel` ratio and a `--bg` ratio.
    - Observability proof for the automated check above: it parses the table by theme filename rather than counting bare `site-` occurrences, so it exits non-zero if one theme's table row is deleted (the exact-24 row-count check fails) or if one row's `--bg` ratio number is blanked (that row then carries fewer than two numeric fields and its per-row check fails). Prose repeating `site-` 24 times without a real table does not pass it.
    - The census names grixis, planeswalker-dark and jund as failing and records their measured ratios.
    - The census records nyx and sultai as the nearest passes with their ratios.
    - The census states, for each of the 24 files, that `--muted` is declared exactly once.
    - The census contains the pre-change Playwright run's per-test pass/fail and wall time.
    - `ls DeckFlow.Web/wwwroot/css/site*.css | grep -v 'common\|mobile\|theme-overrides\|admin' | wc -l` returns `24`.
    - No source file has been modified yet: `git diff --stat -- DeckFlow.Web/` produces no output at the end of this task.
  </acceptance_criteria>
  <done>The full 24-theme population is measured and written down, the pre-change Playwright behaviour is on record, and nothing has been changed yet.</done>
</task>

<task type="auto">
  <name>Task 2: Raise --muted in every theme the census found failing</name>
  <files>DeckFlow.Web/wwwroot/css/site-grixis.css, DeckFlow.Web/wwwroot/css/site-planeswalker-dark.css, DeckFlow.Web/wwwroot/css/site-jund.css, .planning/workstreams/cycle21-cut-lab/phases/07.1-cutlab-review-findings-closeout/07.1-THEME-CONTRAST-CENSUS.md</files>
  <read_first>
    - .planning/workstreams/cycle21-cut-lab/phases/07.1-cutlab-review-findings-closeout/07.1-THEME-CONTRAST-CENSUS.md (as written in Task 1)
    - DeckFlow.Web/wwwroot/css/site-grixis.css lines 1-40
    - DeckFlow.Web/wwwroot/css/site-planeswalker-dark.css lines 1-40
    - DeckFlow.Web/wwwroot/css/site-jund.css lines 1-40
    - DeckFlow.Web/wwwroot/css/site-nyx.css lines 1-40 (the theme `efee8599` already raised — read it for the house style of such a change)
    - .gitattributes (line-ending policy per file type)
  </read_first>
  <action>
    Change only the `--muted` value inside each failing theme file's `:root` block. Do not add any
    rule to `site.css`, `site-common.css` or `site-theme-overrides.css`; do not change which token
    `.cutlab-intake-summary` uses; do not touch a theme the census shows passing.

    **Candidate values computed at planning time.** Each is the minimal hue-and-saturation
    preserving lightening of the current token that reaches at least 4.60:1 against `--panel`
    (4.5 plus headroom so a rounding difference in the browser's computed value cannot land the
    measured ratio under the floor):
    - `site-grixis.css` — `--muted` from `#91889d` to `#a199ab`. Predicted 4.63 on `--panel`
      `#2e3244`, 6.42 on `--bg` `#181822`.
    - `site-planeswalker-dark.css` — `--muted` from `#8b92a8` to `#989fb2`. Predicted 4.61 on
      `--panel` `#2e3450`, 6.25 on `--bg` `#1a1e2e`.
    - `site-jund.css` — `--muted` from `#a08d7d` to `#a49282`. Predicted 4.61 on `--panel`
      `#352b24`, 5.80 on `--bg` `#1c1a17`.

    These are candidates, not commands: re-derive each ratio yourself before committing to it, and
    if the census in Task 1 disagreed with the measured starting values, re-derive the candidates
    too. If the census found a fourth failing theme, raise it the same way and record the value.

    Add a one-line `/* Why: ... */` comment above each changed token naming F-6 and the measured
    ratio, matching the house style used in the other theme files.

    After the edits, recompute the full census arithmetic for the changed themes against BOTH
    `--panel` and `--bg` and append the post-change rows to
    `07.1-THEME-CONTRAST-CENSUS.md` under a `## After` heading.

    Preserve each CSS file's existing line endings exactly; `.gitattributes` pins LF by default for
    these files, but detect per file rather than assuming. Change exactly two lines per file: the
    `--muted` declaration and its new `/* Why: */` comment line — nothing else in the file moves.
  </action>
  <verify>
    <automated>python3 -c "import re,sys; CENSUS='.planning/workstreams/cycle21-cut-lab/phases/07.1-cutlab-review-findings-closeout/07.1-THEME-CONTRAST-CENSUS.md'; text=open(CENSUS).read(); rows=[l for l in text.splitlines() if l.strip().startswith('|') and re.search(r'\|\s*fail\s*\|', l)]; themes=[t for t in (r.strip('|').split('|')[0].strip().strip('\`') for r in rows) if t.startswith('site')]; assert themes, 'no failing themes found in census'; hx=lambda v: tuple(int(v.lstrip('#')[i:i+2],16)/255 for i in (0,2,4)); lin=lambda c: c/12.92 if c&lt;=0.03928 else ((c+0.055)/1.055)**2.4; lum=lambda rgb: 0.2126*lin(rgb[0])+0.7152*lin(rgb[1])+0.0722*lin(rgb[2]); ratio=lambda a,b: (lambda la,lb: (max(la,lb)+0.05)/(min(la,lb)+0.05))(lum(hx(a)),lum(hx(b))); css={t: open('DeckFlow.Web/wwwroot/css/'+t).read() for t in themes}; decls={t: re.findall(r'--muted:\s*(#[0-9a-fA-F]{6})', c) for t,c in css.items()}; assert all(len(v)==1 for v in decls.values()), ('decl-count', decls); muted={t: v[0] for t,v in decls.items()}; panel={t: re.search(r'--panel:\s*(#[0-9a-fA-F]{6})', c).group(1) for t,c in css.items()}; bg={t: re.search(r'--bg:\s*(#[0-9a-fA-F]{6})', c).group(1) for t,c in css.items()}; ratios={t: (ratio(muted[t],panel[t]), ratio(muted[t],bg[t])) for t in themes}; bad={t: r for t,r in ratios.items() if r[0]&lt;4.5 or r[1]&lt;4.5}; assert not bad, ('below-floor', bad); import subprocess; diffed=sorted(l.split('/')[-1] for l in subprocess.check_output(['git','diff','--name-only','--','DeckFlow.Web/wwwroot/css/']).decode().splitlines() if l.strip()); assert diffed==sorted(themes), ('diff-scope-mismatch', diffed, sorted(themes)); print('CONTRAST-OK', themes, ratios)"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c -- '--muted: #91889d' DeckFlow.Web/wwwroot/css/site-grixis.css` returns `0` and the file's single `--muted` declaration carries the new value.
    - `grep -c -- '--muted: #8b92a8' DeckFlow.Web/wwwroot/css/site-planeswalker-dark.css` returns `0`.
    - `grep -c -- '--muted: #a08d7d' DeckFlow.Web/wwwroot/css/site-jund.css` returns `0`.
    - Each of the three files still declares `--muted` exactly once: `grep -c -- '--muted:' <file>` returns `1`.
    - Recomputed ratio of the new `--muted` against that theme's `--panel` is at least 4.5 for each changed theme, and against `--bg` is at least 4.5. Both numbers are recorded in the census's `## After` section.
    - `git diff --stat -- DeckFlow.Web/wwwroot/css/` lists exactly the changed theme files and no others — in particular not `site.css`, `site-common.css`, `site-mobile.css` or `site-theme-overrides.css`.
    - `git diff --numstat -- DeckFlow.Web/wwwroot/css/` shows exactly 2 changed lines per file: the declaration plus its new `/* Why: */` comment, agreeing with the "exactly two lines" instruction above — never 1 (the comment was dropped) and never more than 2.
    - `git diff --stat` and `git diff --ignore-all-space --stat` agree for every changed CSS file.
    - Observability proof for the automated check above: it recomputes WCAG contrast from each census-identified failing theme's live `--muted`/`--panel`/`--bg` hex values rather than printing declarations for original inspection, so restoring `site-grixis.css`'s `--muted` to `#91889d` makes it exit non-zero — the recomputed ratio comes back 3.7517.../5.2092..., both below the 4.5 floor.
  </acceptance_criteria>
  <reversibility rating="reversible">Three token values in three standalone theme forks; each revertable independently, and nothing else reads them across theme boundaries.</reversibility>
  <done>Every theme the census found below the floor now clears it against both --panel and --bg by arithmetic, with the before and after numbers written down, and no shared stylesheet was touched.</done>
</task>

<task type="auto">
  <name>Task 3: Widen the workflow-component matrix to all 24 themes and prove it with a mutation</name>
  <files>DeckFlow.Web/e2e/cut-lab-theme-readability.spec.ts, DeckFlow.Web/wwwroot/css/site-grixis.css (mutated and restored in place)</files>
  <read_first>
    - DeckFlow.Web/e2e/cut-lab-theme-readability.spec.ts lines 1-46 and 396-500
    - DeckFlow.Web/playwright.config.ts (no global `timeout` is declared, so the per-test default of 30s applies; `headless: true` is forced)
    - .planning/workstreams/cycle21-cut-lab/phases/07.1-cutlab-review-findings-closeout/07.1-THEME-CONTRAST-CENSUS.md (before and after sections)
    - .planning/workstreams/cycle21-cut-lab/phases/07.1-cutlab-review-findings-closeout/07.1-MUTATION-PROTOCOL.md
  </read_first>
  <behavior>
    After this task, the test named `keeps intake and progress components readable at workflow
    viewports` asserts the 4.5 floor on `.cutlab-intake-summary` and on
    `.cutlab-progress[data-cut-lab-sticky-target]` for all 24 themes at both workflow viewports.
    Reverting any one raised `--muted` token to its pre-change value fails it, and the failure
    message names the theme.
  </behavior>
  <action>
    (1) In `DeckFlow.Web/e2e/cut-lab-theme-readability.spec.ts`, delete the
    `workflowComponentThemes` const at `:37-41` and change the workflow-component test at
    `:482-499` to iterate the existing `themes` const declared at `:10-35`. One list, so it cannot
    drift from itself. Do not change `workflowViewports` at `:43-46`, do not change the two
    `assertContrastFloor` calls or their 4.5 arguments, and do not change the named-element sweep
    at `:409`.

    (2) Measure and pin the runtime. Run the spec, record the wall time of the widened test, then
    add a `test.setTimeout(...)` call as the first statement inside that test, set to roughly twice
    the measured wall time rounded up. Put a comment immediately above it recording the measured
    wall time and the theme x viewport count the number is derived from, so the next person can
    tell a legitimate slowdown from a wrong constant. Do not raise the timeout in
    `playwright.config.ts` — that would loosen every test in the suite to cover one.

    (3) Run the whole spec and confirm both tests pass: the widened workflow-component test and
    the pre-existing 24-theme named-element sweep. The named-element sweep is the regression check
    for the raised tokens — it already asserts several `--muted`-coloured surfaces at their own
    floors, so if a raise broke something else in grixis, planeswalker-dark or jund, it fails here.

    (4) Then run the mutation, following `07.1-MUTATION-PROTOCOL.md`. Fingerprint each file with
    `git hash-object` before editing and confirm byte equality after restoring by inverse edit;
    never `git checkout --`.

    Mutation M13 — positive kill, "revert one raised token": restore `site-grixis.css`'s `--muted`
    to `#91889d`, leaving the 24-entry matrix from step (1) in place. Run the spec. Expected: the
    widened workflow-component test FAILS, and the failure message names `grixis` and reports a
    ratio near 3.75. Before this plan, that same revert left the test green, because grixis was
    not in the three-entry `workflowComponentThemes` list — say so explicitly in the SUMMARY,
    since that contrast is the whole evidence that the widening did something. Restore
    `site-grixis.css` by inverse edit and confirm `git hash-object` equality before starting M14.

    Mutation M14 — paired negative control, "narrow the matrix back, same broken state": mutate
    two things together, not one. (a) Restore `site-grixis.css`'s `--muted` to `#91889d`, the same
    revert as M13. (b) At the same time, temporarily reintroduce a three-entry theme list —
    `classic` (`site.css`), `azorius` (`site-azorius.css`), `nyx` (`site-nyx.css`), the exact
    pre-plan `workflowComponentThemes` contents — as the workflow-component test's theme source,
    in place of the 24-entry `themes` list from step (1). Run the spec once against this narrowed,
    still-broken configuration and record the pass count in the SUMMARY. Expected: the
    workflow-component test PASSES, because grixis is not among the three narrowed entries, not
    because the token is fine. A narrowed-matrix run against a correctly raised token would prove
    nothing; M14 only counts because the token is simultaneously broken. M14 does not kill
    anything by itself and must never be reported as a kill — its evidence value comes only from
    standing next to M13: same reverted token, same spec, the only variable is matrix width, and
    the two runs disagree. That disagreement is what proves the old three-entry matrix could not
    see this exact regression, i.e. why grixis, planeswalker-dark and jund were invisible before
    this plan. Restore both the theme-list edit and `site-grixis.css`'s `--muted` by inverse edit
    and confirm `git hash-object` equality against the pre-mutation fingerprints for both files.

    In one sentence, for the SUMMARY: M13 kills — reverting the token fails the widened test.
    M14 controls — the identical revert only passes when the matrix is narrowed back to what the
    old matrix actually was. Neither stands in for the other.

    Start the server with `scripts/run-web-test.sh` and drive Playwright with
    `env -u DISPLAY npx --no-install playwright test`. A browser must never open on the Windows
    host. Preserve the spec file's existing line endings.
  </action>
  <verify>
    <automated>cd DeckFlow.Web &amp;&amp; env -u DISPLAY npx --no-install playwright test e2e/cut-lab-theme-readability.spec.ts --project=chromium-desktop</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'workflowComponentThemes' DeckFlow.Web/e2e/cut-lab-theme-readability.spec.ts` returns `0` — the second list is gone, not merely extended.
    - `grep -c 'test.setTimeout' DeckFlow.Web/e2e/cut-lab-theme-readability.spec.ts` returns `1`, and the line above it records the measured wall time.
    - `grep -c 'timeout' DeckFlow.Web/playwright.config.ts` is unchanged from its pre-task value — the global config was not loosened.
    - `cd DeckFlow.Web && env -u DISPLAY npx --no-install playwright test e2e/cut-lab-theme-readability.spec.ts --project=chromium-desktop` reports 2 passed / 0 failed.
    - M13 in isolation (grixis reverted to `#91889d`, 24-entry matrix unchanged): the same command reports at least 1 failed, and the failure message contains `grixis` and a ratio below 4.5.
    - M14 in isolation (grixis reverted to `#91889d` AND the matrix narrowed to the pre-plan three entries, applied together): the same command reports the workflow-component test passing, with the pass count recorded in the SUMMARY as a documented non-kill — paired against M13's failing run, not read alone, per the one-sentence kill/control statement above.
    - After each restore, `git hash-object` on the mutated file equals the recorded pre-mutation value.
    - `git status --short DeckFlow.Web/wwwroot/js/` is empty.
    - `git diff --stat` and `git diff --ignore-all-space --stat` agree for every file this plan touched.
  </acceptance_criteria>
  <reversibility rating="reversible">One deleted const, one loop source changed, one explicit timeout. No production code or user-visible behaviour depends on it.</reversibility>
  <done>All 24 themes are asserted at the 4.5 floor for the two workflow components, the runtime is pinned to a measured number rather than a guess, M13 shows the widened matrix catches a reverted token, and M14 shows the old matrix could not.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| theme cookie (`deckflow-theme`) → stylesheet selection | The spec sets this cookie directly; production sets it from the theme picker. This plan changes stylesheet content, not the selection mechanism. |
| static CSS assets → every rendered page | A `:root` token in a theme fork reaches every element in that theme, across all tools, not only Cut Lab. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07.1-14 | Denial of Service | accessibility of `--muted` surfaces in the three edited themes | medium | mitigate | `--muted` has 49 uses in `site-common.css` and 14 in `site.css`, so raising it moves every one of them in the edited themes; an unreadable surface is a real availability defect for a low-vision user. Task 2 requires the raised value to clear 4.5 against `--bg` as well as `--panel`, and Task 3 requires the pre-existing 24-theme named-element sweep — which asserts several `--muted`-coloured surfaces at their own floors — to pass after the change. |
| T-07.1-15 | Tampering | shared stylesheets | medium | mitigate | Writing the fix into `site.css` or `site-common.css` instead of the theme forks would silently change all 24 themes. Task 2's acceptance criteria require `git diff --stat -- DeckFlow.Web/wwwroot/css/` to list only the changed theme files. |
| T-07.1-16 | Spoofing | rendered contrast measurement | low | mitigate | Arithmetic on the declared hex values is not proof that the browser computes the same colour — an inherited or overriding rule could win. The measurement of record is `resolveContrast` inside the running spec; Task 3 requires the spec to pass, and M13 proves the spec can see a reverted token. |
| T-07.1-17 | Information Disclosure | Playwright run environment | low | accept | The spec authenticates with the local admin Basic credentials from `playwright.config.ts` (`admin` / `changeme-local`, dev defaults) against `localhost:5173`. No production host, credential or dataset is reachable from this plan. |
| T-07.1-SC | Tampering | npm/NuGet installs | low | accept | No package is added by this plan; `npx --no-install` is used precisely so Playwright cannot fetch one. |
</threat_model>

<verification>
- `cd DeckFlow.Web && env -u DISPLAY npx --no-install playwright test e2e/cut-lab-theme-readability.spec.ts --project=chromium-desktop` reports 2 passed / 0 failed.
- The census file records all 24 themes before, and the changed themes after.
- `git diff --stat -- DeckFlow.Web/wwwroot/css/` lists only theme forks.
- `git diff --stat` and `git diff --ignore-all-space --stat` agree.
- `dotnet build` still reports 0 errors and no more than 15 warnings (CSS is copied, not compiled, but the build must stay clean for the closeout plan).
</verification>

<success_criteria>
1. grixis, planeswalker-dark and jund reach at least 4.5:1 for `--muted` on `--panel`, measured by the running spec.
2. Every failing theme the census found is fixed, not only the three named in the gate.
3. Each raised value also clears 4.5:1 against `--bg`, and the 24-theme named-element sweep still passes.
4. `workflowComponentThemes` no longer exists; one 24-entry list drives both tests.
5. M13 kills — reverting grixis's raised token fails the widened test, naming grixis. M14 is the
   paired negative control, not an independent second kill — the same revert, applied with the
   matrix narrowed back to the pre-plan three entries, passes; that pairing, not either run alone,
   is what proves the old matrix's blind spot.
6. No shared stylesheet changed and no line-ending churn.
</success_criteria>

<artifacts_this_phase_produces>
- `.planning/workstreams/cycle21-cut-lab/phases/07.1-cutlab-review-findings-closeout/07.1-THEME-CONTRAST-CENSUS.md` (new file)
- `DeckFlow.Web/wwwroot/css/site-grixis.css` — `--muted` raised (candidate `#a199ab`)
- `DeckFlow.Web/wwwroot/css/site-planeswalker-dark.css` — `--muted` raised (candidate `#989fb2`)
- `DeckFlow.Web/wwwroot/css/site-jund.css` — `--muted` raised (candidate `#a49282`)
- `DeckFlow.Web/e2e/cut-lab-theme-readability.spec.ts` — the `workflowComponentThemes` const is DELETED; a new `test.setTimeout(...)` call appears inside `keeps intake and progress components readable at workflow viewports`
- Mutation identifiers `M13`, `M14`
</artifacts_this_phase_produces>

<output>
Create `.planning/workstreams/cycle21-cut-lab/phases/07.1-cutlab-review-findings-closeout/07.1-05-SUMMARY.md` when done, including the `## Mutation evidence` section.
</output>
