---
phase: 110.1-cut-lab-combo-intelligence-inserted-2026-07-23
plan: 03
type: execute
wave: 3
depends_on: [110.1-02]
files_modified:
  - DeckFlow.Web/Views/Deck/CutLab.cshtml
  - DeckFlow.Web/wwwroot/css/site-common.css
  - DeckFlow.Web/wwwroot/ts/cut-lab.ts
  - DeckFlow.Web/ts-tests/cut-lab-lock-interactions.test.ts
  - DeckFlow.Web/ts-tests/cut-lab-structural-cardtext.test.ts
autonomous: true
requirements: [CLUP-18]

must_haves:
  truths:
    - "A matched Structural evidence chip that is a combo piece renders a combo-state badge ('Combo piece' or 'Needs {MissingCard}') inside the existing chip button."
    - "The combo role/context renders INSIDE the Phase 110 disclosure (the existing cutlab-card-text <details>), not a second UI surface."
    - "Chip lock/unlock still toggles when a badge <span> is nested inside the chip button (data-cut-lab-chip-card and aria-pressed stay on the outer button); no cut-lab.ts lock-logic change is required to pass the lock test."
    - "After a what-if / re-decide patch, the badge is re-applied from the patch DTO's per-card combo-state map (does not go stale on the full patch path)."
    - "The badge is readable across guild themes; badge styling lives in site-common.css (not site.css)."
  artifacts:
    - path: "DeckFlow.Web/Views/Deck/CutLab.cshtml"
      provides: "Combo-state badge inside the structural evidence chip + combo-context block inside the disclosure"
      contains: "ComboBadgeByCardName"
    - path: "DeckFlow.Web/wwwroot/css/site-common.css"
      provides: "Theme-readable combo-badge styling"
      contains: "combo"
    - path: "DeckFlow.Web/wwwroot/ts/cut-lab.ts"
      provides: "Badge re-application from the patch DTO map on what-if re-render"
      contains: "comboBadge"
  key_links:
    - from: "CutLab.cshtml structural evidence chip (:610-615)"
      to: "Model.ComboBadgeByCardName"
      via: "badge span nested inside the existing <button>"
      pattern: "ComboBadgeByCardName"
    - from: "cut-lab.ts createStructuralEvidenceChip (:1297-1307)"
      to: "patch.comboBadgeByCardName"
      via: "append badge span after what-if re-render"
      pattern: "comboBadge"
---

<objective>
Render the combo-state badge and combo role/context by consuming the server-authored data
contracts produced in Plan 110.1-02 (the per-card combo-state map on `CutLabViewModel` and
`CutLabUiPatchDto`). The badge ("Combo piece" / "Needs {MissingCard}") renders inside the
EXISTING structural evidence chip `<button>`; the combo role/context renders inside the EXISTING
Phase 110 disclosure (`cutlab-card-text` `<details>`) — reusing 110's spec, no new UI surface,
no lock/unlock regression (LOCKED decision #1, CLUP-18).

Purpose: Wave 3 rendering. Everything it needs already exists as server contracts (Plan 02) and
existing components (Phase 110 disclosure). This plan only wires markup, theme-readable CSS, and
the what-if TS re-render so badges survive patches.

Output: Badge markup in Razor + TS, combo-context inside the disclosure, badge CSS in
site-common.css, and vitest coverage proving lock/unlock survives nested badge markup and the
patch DTO shape parses.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/110.1-cut-lab-combo-intelligence-inserted-2026-07-23/110.1-CONTEXT.md
@.planning/phases/110.1-cut-lab-combo-intelligence-inserted-2026-07-23/110.1-RESEARCH.md
@.planning/phases/110-cut-lab-navigation-and-pool-discovery/110-UI-SPEC.md
@./CLAUDE.md
@.planning/phases/110.1-cut-lab-combo-intelligence-inserted-2026-07-23/110.1-02-SUMMARY.md

<interfaces>
<!-- Depends on Plan 110.1-02:
     CutLabViewModel.ComboBadgeByCardName : IReadOnlyDictionary<string, CutLabComboBadgeView> (badge state + combo-context string), keyed by normalized card name.
     CutLabUiPatchDto.ComboBadgeByCardName : per-card combo-state map (serialized as comboBadgeByCardName in JSON). -->

From DeckFlow.Web/Views/Deck/CutLab.cshtml:
  RenderCardTextDisclosure(cardName) :100-153 — reads Model.CardTextByCardName; early-return guard at :135-138; <details class="cutlab-card-text"> body :140-152.
  Structural evidence chip loop :608-616 — <button class="kb-chip cutlab-role-chip..." data-cut-lab-chip-card="@evidenceCard.Name" aria-pressed="...">@evidence</button> then RenderCardTextDisclosure(evidenceCard.Name) at :615.

From DeckFlow.Web/wwwroot/ts/cut-lab.ts:
  interface CutLabUiPatch :145-155 (has structuralFindings) — ADD comboBadgeByCardName field.
  createStructuralEvidenceChip :1297-1307 — builds the chip button; delegated lock click :3196; syncRoleGroupLockState never touches innerHTML (RESEARCH §5a).
  renderStructuralFindings :1753-1802 — clonePoolRowCardTextDisclosure (:1789) clones the pool-row disclosure into the findings area, so combo-context added to the pool-row disclosure is carried into findings on what-if for free.

From DeckFlow.Web/wwwroot/css/site-common.css:
  .cutlab-role-chip rules :4356-4370; .cutlab-card-text disclosure rules :2619-2650. Badge CSS goes HERE, not site.css.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Render combo badge inside the structural evidence chip + combo context inside the disclosure + theme-readable badge CSS</name>
  <read_first>
    - DeckFlow.Web/Views/Deck/CutLab.cshtml (RenderCardTextDisclosure :100-153; structural evidence chip loop :608-616) — REQUIRED, primary file
    - DeckFlow.Web/wwwroot/css/site-common.css (.cutlab-role-chip :4356-4370; .cutlab-card-text :2619-2650) — REQUIRED, modified
    - .planning/phases/110-cut-lab-navigation-and-pool-discovery/110-UI-SPEC.md — the reused disclosure component contract (badge/context render inside this; no new spec)
    - .planning/phases/110.1-cut-lab-combo-intelligence-inserted-2026-07-23/110.1-RESEARCH.md §5 (chip lock invariant — badge must be a child of the existing button; data-cut-lab-chip-card + aria-pressed stay on the button), §5b (disclosure integration point, guard update)
    - DeckFlow.Web/ts-tests/cut-lab-lock-interactions.test.ts (existing nested-markup lock fixtures :359-360, :427-428) — REQUIRED, vitest file extended
  </read_first>
  <action>
    In CutLab.cshtml structural evidence chip loop (:608-616), when `Model.ComboBadgeByCardName` contains `evidenceCard.Name`, render a badge `&lt;span class="cutlab-combo-badge cutlab-combo-badge--complete|--near"&gt;` INSIDE the existing `&lt;button ...&gt;@evidence&lt;/button&gt;` (nested child, so the click delegate and syncRoleGroupLockState invariants hold — RESEARCH §5a). Badge text is "Combo piece" for complete state and "Needs {MissingCard}" for near state, sourced from the map's badge-state + missing-partner. Do NOT move `data-cut-lab-chip-card` or `aria-pressed` off the outer button. In RenderCardTextDisclosure (:100-152), add a combo-context block inside the existing `&lt;details class="cutlab-card-text"&gt;` body (a `&lt;p class="cutlab-card-text__combo"&gt;` sourced from `Model.ComboBadgeByCardName[cardName]` combo-context string) — NOT a second `&lt;details&gt;`. Update the early-return guard (:135-138) to a third condition so the disclosure still renders when a card has combo context but no oracle text/meta. Add badge styling to site-common.css (`.cutlab-combo-badge` and the two state modifiers) using EXISTING theme tokens already consumed by `.cutlab-role-chip` so the badge is readable across all 20+ guild themes WITHOUT per-theme `:root` additions; only if contrast is insufficient with existing tokens, add a `--cutlab-combo-badge-*` token to each theme file's `:root` (Phase 111 re-verifies readability). Per CLAUDE.md: badge CSS in site-common.css (never site.css); preserve LF endings; changed-lines-only. Extend cut-lab-lock-interactions.test.ts with a fixture that nests a `cutlab-combo-badge` `&lt;span&gt;` inside the chip `&lt;button&gt;` and assert lock/unlock still toggles (must pass with NO cut-lab.ts production change).
  </action>
  <acceptance_criteria>
    - Source: `CutLab.cshtml` references `Model.ComboBadgeByCardName` in both the chip loop and RenderCardTextDisclosure; the badge `&lt;span&gt;` is a descendant of the `&lt;button data-cut-lab-chip-card&gt;` element (not a sibling, not on a wrapping div).
    - Source: badge text strings `"Combo piece"` and `"Needs "` appear in CutLab.cshtml; the disclosure body contains a `cutlab-card-text__combo` element.
    - Source: `site-common.css` contains `.cutlab-combo-badge` rules; `git diff` shows NO badge rules added to `site.css`.
    - Behavior/test: `npx vitest run cut-lab-lock-interactions` exits 0, including the new nested-badge fixture, with zero changes to cut-lab.ts (lock invariant proven).
    - Build: `dotnet build DeckFlow.sln` clean; `npx tsc -p DeckFlow.Web/tsconfig.json --noEmit` clean.
  </acceptance_criteria>
  <verify>
    <automated>cd DeckFlow.Web && npx tsc -p tsconfig.json --noEmit && npx vitest run cut-lab-lock-interactions</automated>
    <human-check>Load /cut-lab in at least one guild theme; confirm the "Combo piece" / "Needs X" badge is legible against the chip background and the combo context appears inside the card-text disclosure (Phase 111 does the full all-theme gate).</human-check>
  </verify>
  <done>The combo badge renders inside the existing chip button and the combo context inside the existing disclosure, styled theme-readably in site-common.css, with lock/unlock proven intact by the nested-badge vitest fixture.</done>
</task>

<task type="auto">
  <name>Task 2: Re-apply the combo badge on what-if / re-decide re-render from the patch DTO map</name>
  <read_first>
    - DeckFlow.Web/wwwroot/ts/cut-lab.ts (interface CutLabUiPatch :145-155; createStructuralEvidenceChip :1297-1307; renderStructuralFindings :1753-1802 incl. clonePoolRowCardTextDisclosure :1789) — REQUIRED, primary file
    - DeckFlow.Web/ts-tests/cut-lab-structural-cardtext.test.ts (fixture interface :4-40, structuralFindings shape :24) — REQUIRED, vitest file extended
    - .planning/phases/110.1-cut-lab-combo-intelligence-inserted-2026-07-23/110.1-RESEARCH.md §4 (round-trip), §6b (which vitest fixture to extend), Open Question 2 (light adjust path stays stale — out of scope)
  </read_first>
  <action>
    Extend the `CutLabUiPatch` TS interface (:145-155) with `comboBadgeByCardName` mirroring the server DTO field (a record of cardName → { state, context/missing-partner }). In `createStructuralEvidenceChip` (:1297-1307), after building the chip button, look up the card in `patch.comboBadgeByCardName` (thread the map in — pass it as an argument from `renderStructuralFindings`, or capture it in closure) and, when present, append a `cutlab-combo-badge`(--complete/--near) `&lt;span&gt;` INSIDE the button with the same "Combo piece" / "Needs {MissingCard}" text as the Razor path — keeping `data-cut-lab-chip-card` + `aria-pressed` on the button unchanged. The disclosure combo-context already carries through on what-if via `clonePoolRowCardTextDisclosure` (it clones the server-rendered pool-row disclosure, which now includes the combo block from Task 1) — no extra TS work needed for the disclosure context; verify this holds. Extend cut-lab-structural-cardtext.test.ts's fixture interface with the `comboBadgeByCardName` field and assert a patch carrying a complete-combo card renders the "Combo piece" badge inside the re-rendered chip button. Per CLAUDE.md: LF endings; changed-lines-only; do NOT change lock/click logic.
  </action>
  <acceptance_criteria>
    - Source: `cut-lab.ts` `CutLabUiPatch` interface contains `comboBadgeByCardName`; `createStructuralEvidenceChip` appends a `cutlab-combo-badge` span inside the button when the card is in the map.
    - Source: the badge span is appended as a child of the chip `&lt;button&gt;` (not a sibling); `aria-pressed`/`data-cut-lab-chip-card` remain on the button.
    - Behavior/test: `npx vitest run cut-lab-structural-cardtext` exits 0 with the extended fixture asserting badge re-application on the patch path.
    - Build: `npx tsc -p DeckFlow.Web/tsconfig.json --noEmit` clean.
  </acceptance_criteria>
  <verify>
    <automated>cd DeckFlow.Web && npx tsc -p tsconfig.json --noEmit && npx vitest run cut-lab-structural-cardtext cut-lab-lock-interactions</automated>
  </verify>
  <done>What-if / re-decide patches re-apply the combo badge from the DTO map so badges stay in sync (full patch path), with the disclosure context carried through by the existing clone, proven by the extended vitest fixture.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Server-authored combo strings → browser DOM | Combo badge text / missing-partner / combo-context strings rendered into HTML (Razor) and injected into DOM (TS) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-110.1-06 | Tampering (XSS) | Badge / combo-context strings in Razor | mitigate | Razor `@`-encodes all interpolated strings; no `Html.Raw`. Badge/context are text nodes only. |
| T-110.1-07 | Tampering (XSS) | Badge injected via TS on what-if | mitigate | Use `createTextElement`/`textContent` (existing helpers) for badge text — never `innerHTML` with untrusted combo strings. |
| T-110.1-08 | Elevation of privilege | Badge markup breaking chip lock affordance | mitigate | Badge is a nested child of the existing button; lock invariant proven by cut-lab-lock-interactions nested-badge fixture (no cut-lab.ts lock-logic change). |
| T-110.1-SC | Tampering | npm/NuGet installs | accept | No new packages this phase. |
</threat_model>

<verification>
- `dotnet build DeckFlow.sln` clean.
- `cd DeckFlow.Web && npx tsc -p tsconfig.json --noEmit` clean.
- `cd DeckFlow.Web && npx vitest run cut-lab-lock-interactions cut-lab-structural-cardtext` green.
- Manual: badge legible + combo context in disclosure on at least one theme (full all-theme gate deferred to Phase 111 / CLUP-19).
</verification>

<success_criteria>
- Combo-state badge renders inside the existing structural evidence chip on initial render AND after what-if patches.
- Combo role/context renders inside the reused Phase 110 disclosure — no second UI surface.
- Lock/unlock behavior unchanged (proven by nested-badge lock fixture, no lock-logic edit).
- Badge styled theme-readably in site-common.css (not site.css); no new packages.
</success_criteria>

<output>
Create `.planning/phases/110.1-cut-lab-combo-intelligence-inserted-2026-07-23/110.1-03-SUMMARY.md` when done.
</output>
