---
phase: 72-command-zone-commander-castability
plan: 05
type: execute
wave: 3
depends_on: [72-01, 72-03, 72-04]
files_modified:
  - DeckFlow.Web/Services/Manabase/ManabaseAnalysisService.cs
  - DeckFlow.Web/Models/ManabaseViewModel.cs
  - DeckFlow.Web/Models/ManabaseRequest.cs
  - DeckFlow.Web/Controllers/ManabaseController.cs
  - DeckFlow.Web.Tests/Manabase/ManabaseAnalysisServiceTests.cs
autonomous: true
requirements: [B-04, B-05, B-06, C-01, C-02, D-02, F-01]
must_haves:
  truths:
    - "Flag ON: a partner pair / commander+Background yields 2 command-zone commanders in the analysis result"
    - "Flag ON: a companion (from manual designator > Moxfield DetectedCompanionName > Archidekt Companion category) is modeled outside the 99 with cost = clamp(printedMv)+3 and returned as CompanionRow"
    - "Flag ON: the companion is excluded from the analyzed 99 (Archidekt) so it is not double-counted; commanderCount excludes the companion"
    - "Flag ON: report.Castability and report.AvgOnCurvePercent/Health are NOT mutated (companion + commander rows are additive/separate)"
    - "Flag OFF: the manabase analysis result is byte-identical to today for the SAME imported deck (no callout, commander stays in the table, no companion modeling)"
  artifacts:
    - path: "DeckFlow.Web/Services/Manabase/ManabaseAnalysisService.cs"
      provides: "Flag gate + companion detection/clamp/model + Background-as-commander reclassification"
      contains: "CommanderCastabilityFlagKey"
    - path: "DeckFlow.Web/Models/ManabaseViewModel.cs"
      provides: "ShowCommanderCastability + CompanionCallout"
      contains: "ShowCommanderCastability"
    - path: "DeckFlow.Web/Models/ManabaseRequest.cs"
      provides: "CompanionName manual designator field"
      contains: "CompanionName"
  key_links:
    - from: "ManabaseAnalysisService.AnalyzeAsync"
      to: "ManabaseAnalyzer.SimulateCompanion"
      via: "build clamped companion SpellRequirement then simulate (flag ON only)"
      pattern: "SimulateCompanion"
    - from: "ManabaseController"
      to: "ManabaseViewModel.ShowCommanderCastability / CompanionCallout"
      via: "map result.CommanderCastabilityEnabled + result.CompanionRow"
      pattern: "CompanionCallout"
---

<objective>
Wire the flag-gated command-zone modeling into the manabase service + controller: detect the
companion (auto-first, manual fallback), model it outside the 99 with the +3 "to hand"
heuristic, reclassify Background as a second commander, and surface a CompanionRow +
ShowCommanderCastability to the view. report.Castability / Health stay untouched (display-only
move-out happens in 72-06). Flag OFF = byte-identical, proven by a regression test.

Purpose: This is the behavioral core of Phase 72; everything else renders or guards it.
Output: Flag-gated companion + Background modeling, controller wiring, flag-OFF byte-identity test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/72-command-zone-commander-castability/72-RESEARCH.md
@.planning/phases/72-command-zone-commander-castability/72-SPEC.md
@.planning/phases/72-command-zone-commander-castability/72-FIXTURE-NOTES.md

<interfaces>
<!-- Verified contracts this plan consumes/extends. -->
- ManabaseAnalysisService: `public const string CommanderCastabilityFlagKey` (added 72-01); `IsFlagOn(key)` fail-safe OFF (~:270-273); AnalyzeAsync (~:186-248) builds the report and ManabaseAnalysisResult; ResolveAndClassifyAsync (~:275-374) sets `IsCommander = Board=="commander"` (~:345), filters AnalyzedBoards = {mainboard, commander} (~:114, :303-305), classifies via ManabaseClassifier.Classify (~:360), and resolves cards through Scryfall (index/ResolveCardsAsync ~:386-432).
- ManabaseAnalysisResult (sealed record ~:75-84): add `bool CommanderCastabilityEnabled` + `CardCastability? CompanionRow` (extend the positional record + its callers).
- ManabaseAnalysisOptions (sealed class ~:50-63): add `string? CompanionDesignator { get; init; }`.
- DeckSourceLoadResult.DetectedCompanionName (added 72-03) reachable via the loader inside ResolveAndClassifyAsync.
- ManabaseAnalyzer.SimulateCompanion(deck, companionSpell, useManaQuantity, colorAwareMulligan, gateRampOnCastable) → CardCastability (added 72-04).
- ManabaseSwapPromptBuilder.Build(..., bool includeCommandZone=false, CardCastability? companionRow=null) (added 72-04).
- ManabaseViewModel (sealed class): existing ShowCastability computed (~:76); add `bool ShowCommanderCastability { get; init; }` + `CardCastability? CompanionCallout { get; init; }`.
- ManabaseRequest (sealed class): add `CompanionName` string property (defaulting to empty, like DeckName ~:46-51).
- ManabaseController: Manabase action builds the view model (~:88-100); RunAnalysisAsync builds ManabaseAnalysisOptions (~:149-159).
- CardFact / DeckCardEntry carry IsCommander into ScryfallCardFactMapper.ToCardFacts → ManabaseClassifier.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Result/option/viewmodel/request fields + companion name resolution</name>
  <files>DeckFlow.Web/Services/Manabase/ManabaseAnalysisService.cs, DeckFlow.Web/Models/ManabaseViewModel.cs, DeckFlow.Web/Models/ManabaseRequest.cs</files>
  <read_first>
    - DeckFlow.Web/Services/Manabase/ManabaseAnalysisService.cs (ManabaseAnalysisResult ~:75-84, ManabaseAnalysisOptions ~:50-63, AnalyzeAsync ~:186-248)
    - DeckFlow.Web/Models/ManabaseViewModel.cs (ShowCastability ~:76)
    - DeckFlow.Web/Models/ManabaseRequest.cs (DeckName property ~:46-51)
  </read_first>
  <action>
    Add `bool CommanderCastabilityEnabled` and `CardCastability? CompanionRow` to the ManabaseAnalysisResult
    record (positional; update both `new ManabaseAnalysisResult(...)` sites — the analyze return ~:245 and
    any test/other caller). Add `string? CompanionDesignator { get; init; }` to ManabaseAnalysisOptions. Add
    `bool ShowCommanderCastability { get; init; }` and `CardCastability? CompanionCallout { get; init; }` to
    ManabaseViewModel. Add a `CompanionName` string property to ManabaseRequest (null-coalesce to empty, same
    backing-field pattern as DeckName). Add a private helper `ResolveCompanionName(string? designator, string?
    detected, IReadOnlyList<DeckEntry> deckCards)` returning the chosen companion name with precedence: a
    non-blank manual designator wins; else the Moxfield DetectedCompanionName; else the first deckCards entry
    whose Category contains the Archidekt Companion string (OrdinalIgnoreCase). Length-bound the returned
    name. No behavior change yet wired into AnalyzeAsync output beyond defaults (defaults keep flag-OFF identical).
  </action>
  <verify>
    <automated>"/mnt/c/Program Files/dotnet/dotnet.exe" build DeckFlow.Web/DeckFlow.Web.csproj</automated>
  </verify>
  <acceptance_criteria>
    - All four types carry the new members; solution builds clean.
    - ResolveCompanionName precedence (designator > detected > Archidekt category) is unit-coverable.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Flag-gated Background-as-commander + companion modeling (B-04..06, C-01, C-02, D-02)</name>
  <files>DeckFlow.Web/Services/Manabase/ManabaseAnalysisService.cs</files>
  <read_first>
    - DeckFlow.Web/Services/Manabase/ManabaseAnalysisService.cs (ResolveAndClassifyAsync IsCommander assignment ~:339-360, AnalyzeAsync flag reads + report build ~:194-248)
    - DeckFlow.Core/Manabase/ManabaseAnalyzer.cs (SimulateCompanion + librarySize derivation, added 72-04)
    - 72-FIXTURE-NOTES.md (exact Background + Companion category strings)
  </read_first>
  <behavior>
    - Flag ON + commander+Background deck → 2 cards IsCommander, deck.CommanderCount == 2, both get castability rows.
    - Flag ON + companion present → CompanionRow non-null with CastPercent reflecting clamp(printedMv)+3; companion excluded from the analyzed 99 (Archidekt) and from commanderCount.
    - Flag ON but no companion detected → CompanionRow null.
    - Flag OFF → no Background reclassification, no companion exclusion, CompanionRow null, CommanderCastabilityEnabled false; report identical to today.
    - Companion is NOT appended to report.Castability (returned as a SEPARATE CompanionRow). BUT for an
      Archidekt companion deck, flag ON EXCLUDES the companion from the analyzed 99, so report.Castability
      legitimately has one fewer row and a recomputed AvgOnCurvePercent vs flag OFF — this is the INTENDED
      modeling change (companion modeled outside-the-99), NOT a regression. The COMMANDER move-out is
      display-only (72-06) and does NOT remove commander rows from report.Castability. The hard guarantee is
      flag-OFF == today (F-01), not flag-ON == flag-OFF.
  </behavior>
  <action>
    Read `bool commanderCastability = IsFlagOn(CommanderCastabilityFlagKey)` in AnalyzeAsync and thread it
    (plus options.CompanionDesignator) into the resolve/classify stage. When commanderCastability is ON:
    (a) when assigning IsCommander (~:345), also set IsCommander = true for a mainboard entry whose Category
    contains the Background string (OrdinalIgnoreCase) so ManabaseClassifier counts it as a second commander
    (A/B-02); (b) determine the companion name via ResolveCompanionName; if found, EXCLUDE the matching
    mainboard deckCards entry (Archidekt Companion) from the analyzed 99 BEFORE classification so it is not
    double-counted, and ensure the companion card is resolved through Scryfall. Resolution mechanism (explicit):
    if the companion came from an Archidekt deckCards entry it is ALREADY resolved in the standard
    ResolveCardsAsync batch — capture its resolved card before excluding the entry from the 99. For the Moxfield
    name-only / manual-designator case (no DeckEntry exists), resolve the companion name through the SAME
    card-resolution dependency ManabaseAnalysisService already uses for the deck batch (72-RESEARCH.md:99) as a
    ONE-OFF single-name resolve (a 1-element resolve, NOT appended to the 99 deck batch) — introduce NO new
    service or DI; reuse the existing resolver field so files_modified need not grow and Program.cs is untouched.
    The companion must stay outside-the-99 and out of the library/draw. If the resolve returns no match
    (unknown name), treat as no companion (CompanionRow
    null) rather than throwing; (c) after the report
    is built, construct the companion SpellRequirement from the resolved companion card: pips from the card,
    `ManaValue = Math.Max(0, Math.Min(20, printedMv)) + 3` (HEURISTIC +3 "to hand" tax; clamp guards
    adversarial API mana values — security V5), IsCommander=false, then `CardCastability companionRow =
    ManabaseAnalyzer.SimulateCompanion(report-deck, companionReq, useManaQuantity, colorAwareMulligan,
    gateRampOnCastable)`. Pass `includeCommandZone: true, companionRow` to ManabaseSwapPromptBuilder.Build.
    Return CommanderCastabilityEnabled=true + CompanionRow on the result. When the flag is OFF, do NONE of
    this (early-bypass) and return the result exactly as today (CompanionRow null, CommanderCastabilityEnabled
    false). Never POST-PROCESS the already-built report: do not mutate the report.Castability list or
    hand-recompute AvgOnCurvePercent after the report is built. (Excluding the companion from the analyzed 99
    BEFORE the report is built is allowed and intended — it legitimately changes the resulting average for a
    companion deck; the prohibition is only on touching the report object afterward.) Add `// HEURISTIC:` on the +3 line.
    Take only the FIRST companion (bounded — DoS).
  </action>
  <verify>
    <automated>"/mnt/c/Program Files/dotnet/dotnet.exe" test DeckFlow.Web.Tests --filter "FullyQualifiedName~ManabaseAnalysisServiceTests"</automated>
  </verify>
  <acceptance_criteria>
    - Flag ON commander+Background → CommanderCount==2; companion deck → CompanionRow set at clamp+3 cost, excluded from 99 + commanderCount.
    - Flag ON, companion deck → report.Castability has one fewer row than flag OFF (companion removed from the analyzed 99) with recomputed AvgOnCurvePercent — INTENDED; companion name is absent from report.Castability and present only as CompanionRow.
    - Flag ON, NO-companion deck (e.g. partner pair only) → report.Castability.Count and AvgOnCurvePercent EQUAL flag-OFF (commander move-out is display-only; nothing removed at report level).
    - Flag OFF → result byte-identical to today (F-01); CompanionRow null, CommanderCastabilityEnabled false.
    - Companion source precedence (designator > Moxfield-detected > Archidekt category) covered by a unit test (see 72-VALIDATION B-precedence).
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Controller wiring + manabase flag-OFF byte-identity test (F-01)</name>
  <files>DeckFlow.Web/Controllers/ManabaseController.cs, DeckFlow.Web.Tests/Manabase/ManabaseAnalysisServiceTests.cs</files>
  <read_first>
    - DeckFlow.Web/Controllers/ManabaseController.cs (Manabase action vm build ~:88-100, RunAnalysisAsync options ~:149-159)
    - DeckFlow.Web.Tests/Manabase/ManabaseAnalysisServiceTests.cs (Phase 71 flag-OFF byte-identity pattern ~:372, e.g. AnalyzeAsync_PlainLanguageFlagOff_*)
  </read_first>
  <behavior>
    - Manabase action maps result.CommanderCastabilityEnabled → vm.ShowCommanderCastability and result.CompanionRow → vm.CompanionCallout, and threads request.CompanionName → options.CompanionDesignator.
    - With the flag OFF, AnalyzeAsync over a companion/Background-bearing deck returns a result whose Report (castability rows, AvgOnCurvePercent, Health) AND swap prompt are byte-identical to a baseline run.
  </behavior>
  <action>
    In ManabaseController: set `CompanionDesignator = request.CompanionName` in the ManabaseAnalysisOptions
    built by RunAnalysisAsync, and in the Manabase action view model set ShowCommanderCastability =
    result.CommanderCastabilityEnabled and CompanionCallout = result.CompanionRow. Then add Web.Tests
    mirroring the Phase 71 flag-OFF byte-identity pattern: load a deck with a companion (and/or Background)
    with the commander-castability flag OFF and assert the report's Castability sequence, AvgOnCurvePercent,
    Health, and ChatGptSwapPrompt match the pre-feature baseline (CompanionRow null, ShowCommanderCastability
    false). Use the existing fake feature-flag cache pattern to toggle the flag.
  </action>
  <verify>
    <automated>"/mnt/c/Program Files/dotnet/dotnet.exe" test DeckFlow.Web.Tests --filter "FullyQualifiedName~ManabaseAnalysisServiceTests|FullyQualifiedName~ManabaseController"</automated>
  </verify>
  <acceptance_criteria>
    - Controller threads CompanionName + maps callout/flag fields; build clean.
    - Flag-OFF byte-identity test green: report rows, AvgOnCurvePercent, Health, and swap prompt unchanged.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| client form → service | request.CompanionName (manual designator) is untrusted user input |
| third-party API → service | companion name + mana value from Moxfield/Archidekt card data |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-72-05a | Tampering / V5 | Companion ManaValue arithmetic | mitigate | `ManaValue = Math.Max(0, Math.Min(20, printedMv)) + 3` clamps adversarial mana values before the +3 |
| T-72-05b | Injection / V5 | Companion name (designator + API) | mitigate | Length-bound in ResolveCompanionName; Razor `@`-encodes at render (72-06), never @Html.Raw |
| T-72-05c | DoS | Multiple companions / large board | mitigate | Take first companion only; bounded resolution |
| T-72-05d | Tampering | Flag-OFF byte-identity | mitigate | Early-bypass when flag OFF + dedicated flag-OFF byte-identity regression test (F-01) |
</threat_model>

<verification>
- `"/mnt/c/Program Files/dotnet/dotnet.exe" test DeckFlow.Web.Tests --filter "FullyQualifiedName~Manabase"` green.
- Flag-OFF byte-identity test passes (report + prompt unchanged). Build clean.
</verification>

<success_criteria>
- Flag-gated Background-as-commander + companion (+3 heuristic, outside the 99) modeling wired through the
  service and controller; report.Castability/Health untouched; flag OFF byte-identical (regression-tested).
</success_criteria>

<output>
Create `.planning/phases/72-command-zone-commander-castability/72-05-SUMMARY.md` when done.
</output>
