---
phase: 42-orchestrator-extraction
plan: 04
type: execute
wave: 4
depends_on: ["42-03"]
files_modified:
  - DeckFlow.Studio/Services/ContentKbOrchestratorSmokeService.cs
  - DeckFlow.Studio/Program.cs
autonomous: true
requirements: [ORCH-02]
user_setup: []

must_haves:
  truths:
    - "DeckFlow.Studio resolves IContentKbOrchestrator from DI and calls it from a Blazor service"
    - "DeckFlow.Studio references DeckFlow.Core only and has NO DeckFlow.CLI project reference"
    - "Studio composition root calls AddContentKbOrchestrator() and registers the stores/services it needs"
    - "dotnet build DeckFlow.sln is 0 errors / 0 new warnings"
  artifacts:
    - path: "DeckFlow.Studio/Services/ContentKbOrchestratorSmokeService.cs"
      provides: "Minimal Studio service that resolves + calls the orchestrator (D-08, proves SC4)"
      contains: "IContentKbOrchestrator"
    - path: "DeckFlow.Studio/Program.cs"
      provides: "DI wiring: AddContentKbOrchestrator() + store registrations + smoke service"
      contains: "AddContentKbOrchestrator"
  key_links:
    - from: "DeckFlow.Studio/Services/ContentKbOrchestratorSmokeService.cs"
      to: "DeckFlow.Core.Orchestration.IContentKbOrchestrator"
      via: "constructor injection + operation call"
      pattern: "IContentKbOrchestrator"
    - from: "DeckFlow.Studio/Program.cs"
      to: "AddContentKbOrchestrator"
      via: "service registration"
      pattern: "AddContentKbOrchestrator"
---

<objective>
Prove SC4 end-to-end (D-08): add a minimal `ContentKbOrchestratorSmokeService` in DeckFlow.Studio that resolves `IContentKbOrchestrator` from DI and calls one read-only orchestrator operation, and wire `AddContentKbOrchestrator()` + the required store registrations in the Studio composition root — confirming Studio -> Core works with NO `DeckFlow.CLI` reference. This is smoke-level only; no Studio UI feature (that is Phase 43+).

Purpose: Demonstrate the extraction achieved its goal — Studio consumes the orchestrator directly without referencing the CLI executable (ORCH-02 / SC4).
Output: One Studio service + Studio Program.cs DI wiring. Solution builds clean.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/42-orchestrator-extraction/42-CONTEXT.md
@.planning/phases/42-orchestrator-extraction/42-03-SUMMARY.md

<interfaces>
<!-- From Wave 3 (Plan 03). Wire these in Studio. -->
DeckFlow.Core.Orchestration:
  IContentKbOrchestrator (facade) — Studio may depend on the slice it needs
  AddContentKbOrchestrator(this IServiceCollection) — registers the orchestrator;
    host must first register the store interfaces it depends on (D-05/D-06)
  Read-only-ish operations safe for a smoke call: ListBlockedAsync (BlockedVideoListResult)
    or ExportIndexAsync (ContentIndexExportResult) — pick one that needs the fewest stores
    and performs no writes / no network. ListBlockedAsync needs only IBlockedVideoStore.

Existing Studio shape (DeckFlow.Studio/Program.cs):
  WebApplication.CreateBuilder; UseSerilog; AddUserSecrets; StudioConfig(isProdConfigured);
  AddRazorPages + AddServerSideBlazor. Studio csproj references DeckFlow.Core only.

Store construction (host-side per D-05/D-06): concrete stores take a SQLite db path or a
RelationalDatabaseConnection. For the smoke service use a local SQLite path (e.g. the same
MTG_DATA_DIR convention the CLI uses, or a Studio-local default) — NOT the prod connection
string (which stays presence-only per StudioConfig). Do not log or surface any connection string.
-->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Studio smoke service that resolves and calls IContentKbOrchestrator</name>
  <read_first>
    - DeckFlow.Studio/Program.cs (current composition root)
    - DeckFlow.Studio/StudioConfig.cs (presence-only prod config pattern — do not surface the connection string)
    - DeckFlow.Core/Orchestration/IContentKbOrchestrator.cs + BlockedVideoListResult.cs (the operation + result to call)
    - DeckFlow.Core/Content/IBlockedVideoStore.cs + BlockedVideoStore.cs (the one store ListBlockedAsync needs + its ctor)
    - DeckFlow.Core/Orchestration/ServiceCollectionExtensions.cs (AddContentKbOrchestrator contract)
  </read_first>
  <action>
    Create `DeckFlow.Studio/Services/ContentKbOrchestratorSmokeService.cs`, namespace `DeckFlow.Studio.Services`, `public sealed class ContentKbOrchestratorSmokeService`. Constructor-inject `IContentKbOrchestrator` (or the narrowest sub-interface that exposes the chosen read-only op, e.g. `IContentMaintenanceOrchestrator` for ListBlocked — demonstrating that Studio can depend on a slice per D-01). Add one method `public Task<int> ProbeAsync(CancellationToken cancellationToken = default)` that calls the chosen read-only operation (recommended `ListBlockedAsync`) and returns a benign count (e.g. `result.Items.Count`) — proving Studio can resolve and invoke Core orchestration without a CLI reference. The smoke call must NOT perform writes, network calls, or touch the prod connection string. Add XML doc-comments.

    This is deliberately minimal — no Blazor page, no UI, no harvest/distill trigger (deferred to Phase 45+). The class existing, resolving, and compiling against Core is the SC4 proof.
  </action>
  <verify>
    <automated>"/mnt/c/Program Files/dotnet/dotnet.exe" build DeckFlow.Studio/DeckFlow.Studio.csproj -warnaserror 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `DeckFlow.Studio/Services/ContentKbOrchestratorSmokeService.cs` contains `IContentKbOrchestrator` (or a sub-interface) as a ctor dependency and a `ProbeAsync` that calls a read-only orchestrator operation.
    - `grep -rn "DeckFlow.CLI" DeckFlow.Studio/` returns nothing (no CLI namespace usage); `grep -n "DeckFlow.CLI" DeckFlow.Studio/DeckFlow.Studio.csproj` returns nothing (no CLI project reference — SC4).
    - The smoke service does not reference any prod connection string and writes nothing to logs about it.
    - `dotnet build DeckFlow.Studio/DeckFlow.Studio.csproj -warnaserror` = 0 errors / 0 warnings.
  </acceptance_criteria>
  <done>A minimal Studio service resolves IContentKbOrchestrator and calls a read-only operation; Studio has no CLI reference.</done>
</task>

<task type="auto">
  <name>Task 2: Wire AddContentKbOrchestrator() + store registrations in Studio Program.cs</name>
  <read_first>
    - DeckFlow.Studio/Program.cs (insertion point: after AddSingleton(StudioConfig), before app.Build())
    - DeckFlow.Core/Orchestration/ServiceCollectionExtensions.cs (what AddContentKbOrchestrator registers + what the host must register first)
    - DeckFlow.Core/Content/*Store.cs ctors (the concrete stores + their path/connection params — host-side construction per D-05/D-06)
    - DeckFlow.Core/Orchestration/ContentKbOrchestrator.cs (full ctor dependency list, so Studio registers everything the orchestrator needs to resolve)
  </read_first>
  <action>
    In `DeckFlow.Studio/Program.cs`, register the orchestrator dependencies host-side (D-05/D-06) then call `builder.Services.AddContentKbOrchestrator()`. Register the store interfaces the chosen smoke operation requires (at minimum `IBlockedVideoStore` for ListBlocked) by constructing them from a Studio-local SQLite db path (resolve a sensible local default — e.g. an `MTG_DATA_DIR`-based path or a Studio data dir — NOT the prod Postgres connection string; prod stays presence-only). Register `ContentKbOrchestratorSmokeService` (scoped/transient). If the orchestrator ctor requires dependencies the smoke path will not exercise, register them with the simplest concrete or throwing-default implementation that still allows the container to build the orchestrator (the smoke op must not invoke them). Keep the existing Serilog/UserSecrets/StudioConfig/Blazor wiring intact — touch only the lines needed to add the registrations.

    Do NOT add a Blazor page or invoke ProbeAsync from a UI in this phase; container-resolvable + buildable is the SC4 bar. If you want a runtime assertion, resolving the smoke service at startup and logging only a benign count (never a connection string) is acceptable but optional.

    Confirm no `DeckFlow.CLI` ProjectReference is added to the Studio csproj.
  </action>
  <verify>
    <automated>"/mnt/c/Program Files/dotnet/dotnet.exe" build DeckFlow.sln -warnaserror 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "AddContentKbOrchestrator" DeckFlow.Studio/Program.cs` matches.
    - `grep -n "ContentKbOrchestratorSmokeService" DeckFlow.Studio/Program.cs` matches (registered).
    - `grep -n "DeckFlow.CLI" DeckFlow.Studio/DeckFlow.Studio.csproj` returns nothing (SC4: Core-only reference).
    - No prod connection string is constructed or logged in Program.cs (the `Studio:ProdConnectionString` value remains presence-only via StudioConfig — the smoke store uses a local SQLite path).
    - Existing Serilog/UserSecrets/StudioConfig/AddRazorPages/AddServerSideBlazor wiring is unchanged (only additive lines).
    - `dotnet build DeckFlow.sln -warnaserror` = 0 errors / 0 warnings.
  </acceptance_criteria>
  <done>Studio composition root resolves the orchestrator + smoke service via AddContentKbOrchestrator() with no CLI reference; solution builds clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Studio process -> local SQLite Content KB DB | The smoke service reads the local KB DB. No write, no network, no prod connection. Same trust level as the existing local CLI. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42-09 | Information Disclosure | prod connection string | mitigate | The smoke service uses a LOCAL SQLite path only; the prod Postgres connection string stays presence-only (StudioConfig) and is never constructed, logged, or surfaced — honoring STU-03 / public-repo no-secrets. Acceptance criteria forbid constructing/logging it. |
| T-42-10 | Elevation of Privilege / accidental scope | Studio gaining a CLI dependency | mitigate | Acceptance criteria assert NO DeckFlow.CLI project reference and no DeckFlow.CLI namespace usage in Studio — the architectural goal (Studio depends on Core only) is grep-enforced (SC4). |
| T-42-11 | Tampering | smoke op performing a write | accept->mitigate | The chosen smoke operation is read-only (ListBlocked/ExportIndex returns rows; no DB mutation); acceptance criteria require a read-only op so the smoke proof cannot mutate local data. |
| T-42-SC | Tampering | npm/pip/cargo installs | n/a | No package installs — Studio already references Core; no new dependency. |
</threat_model>

<verification>
- `dotnet build DeckFlow.sln -warnaserror` = 0E/0W.
- `grep -rn "DeckFlow.CLI" DeckFlow.Studio/` returns nothing (SC4 hard gate).
- AddContentKbOrchestrator() + smoke service registered in Studio Program.cs.
- No prod connection string constructed/logged in Studio.
</verification>

<success_criteria>
- Studio resolves + calls IContentKbOrchestrator from a Blazor service (SC4 / ORCH-02).
- Studio references Core only, no CLI reference (SC4).
- AddContentKbOrchestrator() wired in the Studio composition root (D-08).
- Solution builds clean.
</success_criteria>

<output>
Create `.planning/phases/42-orchestrator-extraction/42-04-SUMMARY.md` when done.
</output>
