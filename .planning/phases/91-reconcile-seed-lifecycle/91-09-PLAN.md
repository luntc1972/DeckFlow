---
phase: 91-reconcile-seed-lifecycle
plan: 09
type: execute
wave: 7
depends_on: [91-08]
files_modified: []
autonomous: false
requirements: [SYNC-11, SYNC-12]

must_haves:
  truths:
    - "The operator can run a dry-run against a fixture checkout, see all four discrepancy classes, and confirm no prod write occurred"
    - "A stale Apply is refused; a matching Apply with sync.reconcile ON soft-hides only seed-owned rows"
    - "Prod-owned (seed_managed=false) rows remain visible after an Apply"
  artifacts: []
  key_links: []
---

<objective>
Operator verification of the full reconcile workflow - the manual-only checks from 91-VALIDATION that
cannot be exercised by unit tests (an operator git checkout, readable report review, dry-run->Apply UX,
stale-state rejection, and the flag flip). This plan gates the phase on a human confirming the
end-to-end Studio behavior the automated classifier/store/coordinator tests underpin.

Purpose: Confirm the operator-facing safety story holds against a real fixture before the phase closes.
Output: operator sign-off (no code).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/91-reconcile-seed-lifecycle/91-VALIDATION.md
@.planning/phases/91-reconcile-seed-lifecycle/91-07-SUMMARY.md
@.planning/phases/91-reconcile-seed-lifecycle/91-08-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Verify dry-run detection (read-only, flag OFF)</name>
  <files>none (operator verification only)</files>
  <what-built>Studio Reconcile page dry-run: reads prod once, walks the git content-kb tree, parses the seed, classifies into published-orphan / file-orphan / seed-drift / body-hash-mismatch, persists to the local store, and writes the D-06 report. Runs with sync.reconcile OFF.</what-built>
  <action>Operator verification only - no code change. Drive the manual dry-run steps in how-to-verify against a fixture checkout and record the outcome in the SUMMARY. Pause for operator sign-off.</action>
  <how-to-verify>
    1. Start Studio headless per CLAUDE.md (`scripts/run-web-test.sh` / DECKFLOW_DISABLE_AUTO_BROWSER=true); point at a fixture git checkout with a known-drifted content-kb tree + index-seed.json.
    2. Ensure sync.reconcile reads OFF in the web DB.
    3. Open the Reconcile page, click "Run dry-run".
    4. Confirm all four discrepancy classes appear with plausible counts (file-orphans in the hundreds order of magnitude per the 2026-07-05 audit is expected, not 0 or tens of thousands).
    5. Confirm the D-06 report file was written under the checkout and no prod write occurred (prod row visibility/state unchanged).
  </how-to-verify>
  <verify>
    <human-check>Operator confirms all four classes render, the D-06 report exists, prod state is unchanged, and the dry-run ran with sync.reconcile OFF.</human-check>
  </verify>
  <acceptance_criteria>
    - All four classes render; the report file exists; prod state is unchanged (read-only confirmed).
    - The dry-run succeeded with sync.reconcile OFF (detection is flag-independent).
  </acceptance_criteria>
  <done>Operator has confirmed the read-only, flag-independent dry-run surfaces all four discrepancy classes with a D-06 report and zero prod writes.</done>
  <resume-signal>Type "approved" or describe issues.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify gated, re-validated Apply + prod-owned safety</name>
  <files>none (operator verification only)</files>
  <what-built>ReconcileCoordinator.ApplyRemovalsAsync: flag-gated (sync.reconcile), fresh-re-validated, seed_managed=true-only soft-hide; stale-Apply rejection; prod-owned rows never hidden.</what-built>
  <action>Operator verification only - no code change. Drive the manual Apply steps in how-to-verify against the fixture and record the outcome in the SUMMARY. Pause for operator sign-off.</action>
  <how-to-verify>
    1. From a completed dry-run, mutate the seed/prod fixture (e.g. re-add a previously-drifted key) and attempt "Apply removals" - confirm the stale Apply is REFUSED (no write).
    2. Re-run the dry-run so the reviewed set matches current state; flip sync.reconcile ON in the web DB.
    3. Click "Apply removals". Confirm only seed_managed=true seed-drift rows are soft-hidden (is_visible=false, row retained), and that a known prod-owned (seed_managed=false) row absent from the seed remains VISIBLE.
    4. With sync.reconcile OFF (or an unreachable prod flag read), confirm Apply refuses.
  </how-to-verify>
  <verify>
    <human-check>Operator confirms stale Apply refused, flag-ON Apply soft-hides only seed-owned drift rows (retained), a prod-owned row stays visible, and flag OFF/indeterminate refuses.</human-check>
  </verify>
  <acceptance_criteria>
    - Stale Apply refused; matching Apply with flag ON soft-hides only seed-owned drift rows (retained, not deleted).
    - A prod-owned row absent from the seed stays visible (SYNC-17 invariant holds live).
    - Flag OFF / indeterminate refuses Apply.
  </acceptance_criteria>
  <done>Operator has confirmed the gated Apply is safe: stale/flag-off/indeterminate refuse, only seed-owned drift rows soft-hide, and prod-owned rows stay visible.</done>
  <resume-signal>Type "approved" or describe issues.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator -> destructive Apply | Human confirmation that the flag/marker/re-validation gates behave against a real fixture |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-91-24 | Tampering | Live behavior diverging from unit-tested gating (mass-hide of prod-owned rows) | mitigate | Blocking human-verify of the prod-owned-stays-visible + stale-reject + flag-gate invariants against a fixture |
</threat_model>

<verification>
- Both blocking checkpoints approved by the operator.
- Full suite (Core / Web / Studio) green before sign-off (per phase gate).
</verification>

<success_criteria>
An operator has confirmed, against a real fixture checkout, that the dry-run detects all four classes
read-only and that the gated Apply soft-hides only seed-owned rows, refuses stale/flag-off states, and
never hides prod-owned content - the phase's safety story verified end-to-end.
</success_criteria>

<output>
Create `.planning/phases/91-reconcile-seed-lifecycle/91-09-SUMMARY.md` when done.
</output>
