---
phase: 62-studio-ui-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - DeckFlow.Studio/Shared/StatusBadge.razor
  - DeckFlow.Studio/Pages/Harvest.razor
  - DeckFlow.Studio/Pages/Review.razor
  - DeckFlow.Studio/Shared/MainLayout.razor
  - DeckFlow.Core/Content/VideoStatusResolver.cs
  - DeckFlow.Core.Tests/VideoStatusFromContentRowTests.cs
  - DeckFlow.Studio.Tests/StatusBadgeTests.cs
  - README.md
  # NOTE: Publish.razor intentionally NOT modified — it has no per-row list (Codex HIGH); SUI-01 = Harvest + Review only.
autonomous: true
requirements: [SUI-01, SUI-06]
must_haves:
  truths:
    - "A single shared StatusBadge component renders the VideoStatus badge; Harvest no longer defines RenderBadge inline and uses the shared component at all former call sites"
    - "Each VideoStatus value (NotHarvested/Harvested/Distilled/Approved/Published/Blocked/Duplicate) renders the same label + Bootstrap class it did before — no visual regression"
    - "Review's per-entry list shows the same consistent status badge via the shared component. (Codex HIGH: Publish has NO per-row list today — only an approved-count + publish-state SUMMARY — so SUI-01 applies to Harvest + Review; Publish is OUT of per-row badge scope, its summary is left as-is. Adding a per-row Publish list would be net-new behavior, out of this polish phase.)"
    - "Review derives each row's VideoStatus via a SHARED pure mapper from (ApprovalStatus, PushedToProdUtc, IsVisible) — NOT ad-hoc badge-text translation — so there is no duplicate status logic (Codex MEDIUM)"
    - "The MainLayout 'About' link points at a real DeckFlow target (deckflow.gg), not the ASP.NET docs scaffold, and the TODO is removed"
  artifacts:
    - path: "DeckFlow.Studio/Shared/StatusBadge.razor"
      provides: "Reusable [Parameter] Status badge component"
      contains: "VideoStatus"
    - path: "DeckFlow.Studio.Tests/StatusBadgeTests.cs"
      provides: "bUnit: each status renders its expected label/class"
      contains: "StatusBadge"
  key_links:
    - from: "DeckFlow.Studio/Shared/StatusBadge.razor"
      to: "DeckFlow.Core.Content.VideoStatus"
      via: "switch over VideoStatus → badge markup"
      pattern: "VideoStatus"
---

<objective>
SUI-01: replace the inline `RenderBadge` switch in `Harvest.razor` with one shared
`Shared/StatusBadge.razor` component (a `[Parameter] VideoStatus Status`), and use it on Harvest and
Review so pipeline status reads consistently at a glance — reusing the Cycle-9 status the resolver
already produces (no duplicate status logic). Publish is EXCLUDED (no per-row list — Codex review).
SUI-06: fix the MainLayout
"About" link (one line). Badge markup stays byte-identical so nothing regresses visually.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/62-studio-ui-polish/62-CONTEXT.md
@./CLAUDE.md
@DeckFlow.Studio/Pages/Harvest.razor
@DeckFlow.Studio/Pages/Review.razor
@DeckFlow.Studio/Pages/Publish.razor
@DeckFlow.Studio/Shared/MainLayout.razor
@DeckFlow.Core/Content/VideoStatus.cs
</context>

<tasks>
1. Create `Shared/StatusBadge.razor`: `[Parameter] public VideoStatus Status { get; set; }`, a switch
   rendering the SAME label + Bootstrap classes the current `Harvest.RenderBadge` emits for all 7
   VideoStatus values (copy verbatim, incl. the Published check icon).
2. Harvest.razor: remove the inline `RenderBadge` RenderFragment and replace its 3 call sites with
   `<StatusBadge Status="vm.Status" />`.
3. Add a SHARED pure mapper (Codex MEDIUM) — a static `VideoStatus FromContentRow(string approvalStatus,
   DateTimeOffset? pushedToProdUtc, bool isVisible)` mirroring the existing `VideoStatusResolver` rules
   (Published if pushed+visible; Approved if approval=="approved"; else Distilled). Put it where both
   the resolver and Review can call it (e.g. a static on `VideoStatusResolver` or a small Core helper),
   and have `VideoStatusResolver` use it too so the rule lives in ONE place.
4. Review.razor: render `<StatusBadge Status="@VideoStatus.FromContentRow(...)" />` per entry using the
   row's persisted (ApprovalStatus, PushedToProdUtc, IsVisible). Publish.razor: OUT of per-row badge
   scope (no row list); leave its summary unchanged.
5. MainLayout.razor: repoint the "About" `<a>` to `https://www.deckflow.gg` (real target), remove the
   Phase-48 TODO comment.
6. bUnit `StatusBadgeTests`: assert each VideoStatus renders its expected label substring + class.
   Add a small test for the shared `FromContentRow` mapper (published/approved/distilled cases).
7. README: note the consistent status badges + the About-link fix in the Studio section.
7. Build Studio + run Studio.Tests green (note the known parallel-isolation flake).
</tasks>

## Trust Boundaries
None new — pure presentation; the badge renders an already-resolved enum. The About link is a static
external URL.

## STRIDE Threat Register
- **Tampering (T-62-01):** badge must not introduce a new status path that misrepresents state.
  Mitigation: render-only over the existing `VideoStatus`; no status recomputation.
- **None** otherwise — no data, no I/O, no secrets.
