---
phase: 05-archidekt-bracket-capture
phase_number: "05"
phase_name: Archidekt Bracket Capture
plan: 03
type: execute
status: planned
generated: 2026-07-29
wave: 3
depends_on: [05-01, 05-02]
dependencies: [05-01, 05-02]
files_modified:
  - DeckFlow.Core/Knowledge/ArchidektDeckCacheSession.cs
  - DeckFlow.Core.Tests/ArchidektDeckCacheSessionTests.cs
  - DeckFlow.Core.Tests/ContentHashDedupTests.cs
  - DeckFlow.Web/Services/Persistence/ICategoryKnowledgeStore.cs
  - DeckFlow.Web/Services/Persistence/CategoryKnowledgeStore.cs
  - DeckFlow.Web/Controllers/Admin/AdminHarvestController.cs
  - DeckFlow.Web.Tests/AdminHarvestControllerTests.cs
  - DeckFlow.Web.Tests/TestDoubles/FakeCategoryKnowledgeStore.cs
  - DeckFlow.Web.Tests/CategoryKnowledgeStoreTests.cs
  - README.md
autonomous: true
requirements: [BRKT-01, BRKT-02, BRKT-03]
requirement_ids: [BRKT-01, BRKT-02, BRKT-03]

must_haves:
  truths:
    - "Bulk harvest uses the metadata-bearing importer and writes metadata through `MarkDeckProcessedAsync` on successful payload parse (D-05)."
    - "One-off admin URL import uses the same metadata-bearing importer and writes metadata through `MarkUrlDeckProcessedAsync` (D-05)."
    - "The URL write path is proven to SQLite end to end, not just to a fake: `CategoryKnowledgeStore`'s 4-arg `MarkUrlDeckProcessedAsync` overload is exercised against a real store and the metadata is read back out of `deck_queue` over a raw connection (D-05)."
    - "Failure and skip paths pass null metadata and do not fabricate capture timestamps (D-06)."
    - "Content hash remains based on card entries only; top-level metadata changes do not force category fact rewrites (D-08)."
    - "No UI, backfill, local bracket inference, or raw JSON storage is added."
    - "URL-import commander extraction moves from the unreachable `Category == \"Commander\"` predicate to `Board == \"commander\"` (D-09). Pre-existing URL-imported rows are NOT backfilled and keep `commander_name IS NULL`, so correct commander attribution for the URL subset begins only at Phase 5 deploy time and a commander-grouped corpus query must not read that subset as a time series."
  artifacts:
    - path: "DeckFlow.Core/Knowledge/ArchidektDeckCacheSession.cs"
      provides: "bulk harvest metadata propagation"
      contains: "ImportWithMetadataAsync"
    - path: "DeckFlow.Web/Controllers/Admin/AdminHarvestController.cs"
      provides: "admin URL metadata propagation"
      contains: "ImportWithMetadataAsync"
    - path: "DeckFlow.Web/Services/Persistence/ICategoryKnowledgeStore.cs"
      provides: "web store metadata contract"
      contains: "ArchidektDeckMetadata?"
  key_links:
    - from: "DeckFlow.Core/Knowledge/ArchidektDeckCacheSession.cs"
      to: "DeckFlow.Core/Knowledge/CategoryKnowledgeRepository.cs"
      via: "MarkDeckProcessedAsync(..., metadata)"
      pattern: "metadata: metadata"
    - from: "DeckFlow.Web/Controllers/Admin/AdminHarvestController.cs"
      to: "DeckFlow.Web/Services/Persistence/ICategoryKnowledgeStore.cs"
      via: "MarkUrlDeckProcessedAsync(..., metadata)"
      pattern: "result.Metadata"
---

<objective>
Wire curated Archidekt metadata through both successful harvest paths: bulk cache session and admin one-off URL import.

Purpose: D-05 requires both write paths to share the same metadata path; D-06 and D-08 require failure rows and content-hash behavior to remain clean.
Output: propagation code, updated web store interface/adapters, and end-to-end focused tests.
</objective>

<execution_context>
@/home/clunt/.codex/get-shit-done/workflows/execute-plan.md
@/home/clunt/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/cycle21-cut-lab/ROADMAP.md
@.planning/workstreams/cycle21-cut-lab/REQUIREMENTS.md
@.planning/workstreams/cycle21-cut-lab/PROJECT.md
@.planning/workstreams/cycle21-cut-lab/phases/05-archidekt-bracket-capture/05-CONTEXT.md
@.planning/workstreams/cycle21-cut-lab/phases/05-archidekt-bracket-capture/05-RESEARCH.md
@.planning/workstreams/cycle21-cut-lab/phases/05-archidekt-bracket-capture/05-PATTERNS.md
@.planning/workstreams/cycle21-cut-lab/phases/05-archidekt-bracket-capture/05-VALIDATION.md
@.planning/workstreams/cycle21-cut-lab/phases/05-archidekt-bracket-capture/05-01-SUMMARY.md
@.planning/workstreams/cycle21-cut-lab/phases/05-archidekt-bracket-capture/05-02-SUMMARY.md

<interfaces>
From plan 05-01:
- `IArchidektDeckImporter.ImportWithMetadataAsync(...)` returns `ArchidektDeckImportResult(List<DeckEntry> Entries, ArchidektDeckMetadata? Metadata)`. `Metadata` is nullable: an unrecognizable payload records no capture, and callers must pass that null straight through rather than substituting a record.
- `ImportWithMetadataAsync` is a **throwing default interface method** real-implemented only on `ArchidektApiDeckImporter`. Consequence for this plan: any fake that is the importer for a code path now calling `ImportWithMetadataAsync` must override it, or the test fails at runtime with `NotSupportedException`. The three fakes in that position are listed in Task 3; the other 10 implementers stay untouched.

From plan 05-02:
- `CategoryKnowledgeRepository.MarkDeckProcessedAsync(..., ArchidektDeckMetadata? metadata = null, ..., CancellationToken cancellationToken = default)` writes metadata only when non-null.
- `CategoryKnowledgeRepository.MarkUrlDeckProcessedAsync(..., ArchidektDeckMetadata? metadata = null, CancellationToken cancellationToken = default)` upserts URL metadata.

From `DeckFlow.Core/Knowledge/ArchidektDeckCacheSession.cs`:
- `PersistDeckAsync` currently computes `DeckCategoryCacheWriter.ComputeCanonicalHash(entries)` and returns early as `Unchanged` when the card-list hash is unchanged.
- `RunAsync` still calls `MarkDeckProcessedAsync` after `PersistDeckAsync`; use that existing post-import mark to write metadata even for unchanged card lists.

From `DeckFlow.Web/Controllers/Admin/AdminHarvestController.cs`:
- `SubmitUrl(string url, CancellationToken cancellationToken)` (`[HttpPost("url")]`, `:229-231`) currently calls `_deckImporter.ImportAsync(url, cancellationToken)` (`:265`), persists entries, extracts commander using `string.Equals(entry.Category, "Commander", StringComparison.OrdinalIgnoreCase)` (`:269`), and calls `_categoryStore.MarkUrlDeckProcessedAsync(deckId, commanderName, cancellationToken)` (`:273`).
- That predicate is unreachable for Archidekt payloads — `IsBoardCategory` (`DeckFlow.Core/Integration/ArchidektApiDeckImporter.cs:150`, applied at `:79` of that same file) strips `Commander` out of `userCategories` before `Category` is built, and the importer sets `Board = "commander"` (`ArchidektApiDeckImporter.cs:130`) — so URL-imported rows persist `commander_name = NULL` always. Fixing it is ratified as **D-09** in `05-CONTEXT.md`; this plan implements it.
- The banner is `TempData["AdminHarvestBanner"] = $"Harvested {commanderName ?? "deck"}: {entries.Count} new observations."` (`:286`, key at `:20`). The format string does not change; the rendered text does, because `commanderName` stops being always-null.

From `DeckFlow.Web/Services/Persistence/ICategoryKnowledgeStore.cs`:
- `MarkUrlDeckProcessedAsync(string deckId, string? commanderName, CancellationToken cancellationToken = default)` (`:71`) is an abstract member with **7 implementers**. It stays byte-for-byte untouched; the metadata surface arrives as a separate 4-arg overload (Task 3).
</interfaces>

<source_audit>
GOAL: successful Archidekt harvests now persist prospectively captured bracket/provenance metadata. Covered here with both write paths.
REQ: BRKT-01, BRKT-02, BRKT-03 covered by combined importer, repository, bulk, and URL tests.
RESEARCH: bulk path, admin URL path, shared metadata record, skip/failure null metadata, content-hash card-list boundary, URL commander extraction correction. Covered here.
CONTEXT: D-01 through D-08 all covered across plans 05-01..05-03; D-09 (ratified URL-import commander extraction, no backfill) covered here; D-10 (per-record URL-upsert gating) is implemented and tested in plan 05-02, and this plan must not defeat it — pass the importer's `Metadata` through unchanged, never substituting a record for null or a null for a record. Deferred raw JSON, backfill, commander x bracket derivation, and UI remain unplanned.
</source_audit>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing propagation tests for bulk and URL imports</name>
  <read_first>
    - `DeckFlow.Core.Tests/ArchidektDeckCacheSessionTests.cs` — fake importer and SQLite session patterns. Note it has **no** raw SQL or `SqliteConnection` access at all today, yet Tests 1-3 must read `deck_queue` metadata columns and plan 05-02 deliberately adds no read API.
    - `DeckFlow.Core.Tests/ContentHashDedupTests.cs` — copy `ReadDeckQueueRowAsync` (`:412-430`) and its `DeckQueueRow` record (`:434`) as the raw-`SqliteConnection` analog for those reads, widened with the six metadata columns.
    - `DeckFlow.Web.Tests/AdminHarvestControllerTests.cs` — controller construction and store/importer fakes; `Build()` (`:170-191`, six call sites at `:36`, `:50`, `:63`, `:78`, `:92`, `:106`) and `StubArchidektDeckImporter` (`:334-338`).
    - `DeckFlow.Web.Tests/TestDoubles/FakeCategoryKnowledgeStore.cs` — shared web store fake signatures.
    - `.planning/workstreams/cycle21-cut-lab/phases/05-archidekt-bracket-capture/05-CONTEXT.md` D-05, D-06, D-08, D-09.
  </read_first>
  <files>DeckFlow.Core.Tests/ArchidektDeckCacheSessionTests.cs, DeckFlow.Web.Tests/AdminHarvestControllerTests.cs, DeckFlow.Web.Tests/TestDoubles/FakeCategoryKnowledgeStore.cs</files>
  <behavior>
    - Test 1: bulk `RunAsync` with a metadata-bearing fake importer stores `archidekt_metadata_captured_utc` and nullable fields in `deck_queue`.
    - Test 2: bulk unchanged-card-list path still writes newer metadata while `ContentHashDedup` behavior remains card-list based and category facts are not rewritten.
    - Test 3: bulk importer failure marks the row skipped and leaves all Archidekt metadata columns null on a **fresh, never-captured** row. This is the propagation-level D-06 check only; the harder guarantee — that a skip cannot wipe an **already-captured** row — is proven at the repository boundary by plan 05-02 Tests 4 and 5b, not here.
    - Test 4: admin `SubmitUrl` calls the metadata-bearing importer and passes the identical metadata instance to `MarkUrlDeckProcessedAsync`. `AdminHarvestControllerTests` contains **zero `SubmitUrl` tests** today, so Tests 4 and 5 are written from scratch.
    - Test 5: admin `SubmitUrl` extracts the commander from `Board == "commander"` rather than `Category == "Commander"` (D-09). Have the fake importer return exactly 2 entries, one of which is the commander named `Kenrith, the Returned King` with `Board = "commander"` and no `Category` of `"Commander"`, then assert `MarkUrlDeckProcessedAsync` received `commanderName == "Kenrith, the Returned King"` **and** that `TempData["AdminHarvestBanner"]` equals the exact string `Harvested Kenrith, the Returned King: 2 new observations.` On the unmodified tree the banner renders `Harvested deck: 2 new observations.` and `commanderName` is null, so both assertions fail red. Do not assert a "banner shape" or a substring — assert the whole string.
  </behavior>
  <action>
Add tests before propagation code. For bulk tests, extend the `FakeDeckImporter` in `ArchidektDeckCacheSessionTests.cs` (`:118`) to implement `ImportWithMetadataAsync`, expose a public `ImportCalls` counter incremented once per metadata-bearing import, and return entries plus a deterministic `ArchidektDeckMetadata`. Prove the no-extra-request guarantee concretely with `Assert.Equal(1, fake.ImportCalls)` — not with prose about "does not need a second call".

For the unchanged-card-list test, process a deck once with one metadata record, then process it again with identical entries and changed top-level metadata. Assert the queue metadata updates on the second pass while the content hash remains the same and existing category fact counts do not grow. This directly protects D-08.

To read the six metadata columns from these tests, add a private raw-`SqliteConnection` reader modelled on `ContentHashDedupTests.ReadDeckQueueRowAsync` (`:412-430`) plus a local record modelled on its `DeckQueueRow` (`:434`), widened to select `archidekt_edh_bracket`, `archidekt_deck_format`, `archidekt_theorycrafted`, `archidekt_created_utc`, `archidekt_updated_utc`, and `archidekt_metadata_captured_utc`. Do not add a production read API to satisfy the tests.

For admin tests, update only the necessary fakes to record the `ArchidektDeckMetadata?` passed into the new 4-arg `MarkUrlDeckProcessedAsync` overload. Do not add UI or admin display assertions; Phase 5 has no UI scope beyond the D-09 banner string already covered by Test 5.

**Parameterize `Build()` here, in this task — Tests 4 and 5 cannot be written without it.** `AdminHarvestControllerTests.Build(ICategoryKnowledgeStore store, bool crossOrigin = false)` (`:170-191`) hard-codes `new StubArchidektDeckImporter()` (`:334-338`), which returns an empty entry list, so no injected importer can supply the commander-bearing, metadata-bearing payload those two tests assert on. Add a third parameter — `IArchidektDeckImporter? importer = null`, falling back to `new StubArchidektDeckImporter()` — so all six existing `Build(...)` call sites (`:36`, `:50`, `:63`, `:78`, `:92`, `:106`) compile untouched. The file is already in this task's `<files>`. Task 3 consumes this and does not repeat it.
  </action>
  <verify>
    <automated>dotnet.exe test DeckFlow.Core.Tests/DeckFlow.Core.Tests.csproj --filter FullyQualifiedName~ArchidektDeckCacheSessionTests --no-restore</automated>
    <automated>dotnet.exe test DeckFlow.Web.Tests/DeckFlow.Web.Tests.csproj --filter FullyQualifiedName~AdminHarvestControllerTests --no-restore</automated>
  </verify>
  <done>Propagation tests fail red against current code and pin bulk success, unchanged-card metadata update, skip/failure null metadata, URL metadata propagation, and URL commander extraction.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire bulk harvest metadata without changing hash semantics</name>
  <read_first>
    - `DeckFlow.Core/Knowledge/ArchidektDeckCacheSession.cs` — `RunAsync` and `PersistDeckAsync`.
    - `DeckFlow.Core.Tests/ContentHashDedupTests.cs` — existing content-hash dedup expectations to keep green; `FakeDeckImporter` (`:436`) and `CreateSession` (`:339`).
    - `.planning/workstreams/cycle21-cut-lab/phases/05-archidekt-bracket-capture/05-PATTERNS.md` bulk success/failure and hash boundary notes.
  </read_first>
  <files>DeckFlow.Core/Knowledge/ArchidektDeckCacheSession.cs, DeckFlow.Core.Tests/ArchidektDeckCacheSessionTests.cs, DeckFlow.Core.Tests/ContentHashDedupTests.cs</files>
  <action>
`ContentHashDedupTests.FakeDeckImporter` (`:436`) implements **only** `ImportAsync` (`:440`) and is the importer supplied to `ArchidektDeckCacheSession` in four tests (`:193`, `:226`, `:256`, `:283`, all via `CreateSession` at `:339`). The moment `PersistDeckAsync` calls `ImportWithMetadataAsync`, those four tests hit the throwing default and fail with `NotSupportedException` — and this task's own verify command runs that file. So implement `ImportWithMetadataAsync` on that fake too, returning the same entries it already returns for the deck id plus a deterministic `ArchidektDeckMetadata`. **No existing expectation in this file changes:** its `FactSnapshot` (declared `:432`, produced by `ReadFactSnapshotAsync` `:371`) and `DeckQueueRow` (`:434`, selected by `ReadDeckQueueRowAsync` `:412-430`) read no metadata columns, so the dedup assertions stand exactly as written. Do not widen them.

Change `PersistDeckAsync` to call `_deckImporter.ImportWithMetadataAsync(deckId, cancellationToken)` and return metadata alongside the existing cache result and commander name. **The shape is decided, not discretionary** — widen the existing return tuple (`ArchidektDeckCacheSession.cs:177`) from `(DeckCacheWriteResult Result, string? CommanderName)` to `(DeckCacheWriteResult Result, string? CommanderName, ArchidektDeckMetadata? Metadata)`, and destructure it in `RunAsync` (`:109`) as `var (cacheResult, commanderName, metadata) = await PersistDeckAsync(deckId, cancellationToken);`. The importer result local (`import`) stays inside `PersistDeckAsync` and is **not** surfaced to `RunAsync`, so the call site reads `metadata: metadata`, not `metadata: import.Metadata`. That literal — `metadata: metadata` — is this plan's `key_links` proxy for the session and is grep-0 across the repo at HEAD `7fe8987b`; do not reshape the code to produce a different string, and do not change the proxy without re-checking it is grep-0. Keep commander extraction from `Entries` by `Board == "commander"` (`ArchidektDeckCacheSession.cs:185-188`), which is already correct.

Keep `DeckCategoryCacheWriter.ComputeCanonicalHash(entries)` exactly card-list based; do not include metadata in hash input. When the stored hash matches and `PersistDeckAsync` returns `Unchanged`, still let `RunAsync` call `MarkDeckProcessedAsync(deckId, commanderName, skip: false, metadata: metadata, cancellationToken: cancellationToken)` so metadata can be captured prospectively without rewriting category facts.

In exception/skip paths, continue to call `MarkDeckProcessedAsync(..., skip: true, metadata: null, ...)`. Do not create a default metadata record outside the importer after failures, and do not substitute a record when the importer captured nothing — pass the null straight through, so plan 05-02's preserve-on-null semantics apply.
  </action>
  <verify>
    <automated>dotnet.exe test DeckFlow.Core.Tests/DeckFlow.Core.Tests.csproj --filter "FullyQualifiedName~ArchidektDeckCacheSessionTests|FullyQualifiedName~ContentHashDedupTests" --no-restore</automated>
    <automated>dotnet.exe build DeckFlow.sln --no-restore</automated>
  </verify>
  <done>Bulk harvest writes metadata on successful imports, writes no metadata on skips, unchanged card lists do not rewrite category facts or change the content-hash contract, all four `ContentHashDedupTests` session tests still pass with their original assertions, and `dotnet.exe build DeckFlow.sln --no-restore` is clean — `DeckFlow.Core.Tests` references only `DeckFlow.Core` and `DeckFlow.CLI`, and `ArchidektDeckCacheSession` is constructed by both `DeckFlow.Web/Services/Persistence/CategoryKnowledgeStore.cs:235` and `DeckFlow.CLI/DeckCommandRunners.cs:392`, so a green Core test run alone cannot prove either consumer still compiles.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wire admin URL metadata through the web store adapter</name>
  <read_first>
    - `DeckFlow.Web/Services/Persistence/ICategoryKnowledgeStore.cs` — store contract.
    - `DeckFlow.Web/Services/Persistence/CategoryKnowledgeStore.cs` — repository adapter forwarding.
    - `DeckFlow.Web/Controllers/Admin/AdminHarvestController.cs` — `SubmitUrl`.
    - `DeckFlow.Web.Tests/AdminHarvestControllerTests.cs` and web test fakes using `IArchidektDeckImporter` / `ICategoryKnowledgeStore`.
  </read_first>
  <files>DeckFlow.Web/Services/Persistence/ICategoryKnowledgeStore.cs, DeckFlow.Web/Services/Persistence/CategoryKnowledgeStore.cs, DeckFlow.Web/Controllers/Admin/AdminHarvestController.cs, DeckFlow.Web.Tests/AdminHarvestControllerTests.cs, DeckFlow.Web.Tests/TestDoubles/FakeCategoryKnowledgeStore.cs, DeckFlow.Web.Tests/CategoryKnowledgeStoreTests.cs, README.md</files>
  <action>
Add a **new 4-arg overload** `MarkUrlDeckProcessedAsync(string deckId, string? commanderName, ArchidektDeckMetadata? metadata, CancellationToken cancellationToken = default)` to `ICategoryKnowledgeStore` as a **throwing default interface method**, and leave the existing 3-arg member (`ICategoryKnowledgeStore.cs:71`) byte-for-byte untouched. Do **not** widen the existing member with an optional parameter: an optional parameter still changes the signature, which would raise CS0535 in five unlisted implementers — `CategorySuggestionServiceTests.cs:149`, `CutLabAnalysisContextBuilderTests.cs:662`, `CutLabPageServiceTests.cs:2918`, `HarvestStatsAggregatorTests.cs:67` and `:138` — and `DeckFlow.Web.Tests` would then fail to compile for a reason unrelated to the code under test. The overload keeps those four files out of this plan's write set entirely. `CategoryKnowledgeStore` (production) must **really** implement the overload, forwarding to `CategoryKnowledgeRepository.MarkUrlDeckProcessedAsync` with the metadata; add `using DeckFlow.Core.Integration;` there.

Note on `CategoryKnowledgeStore.cs`: plan **05-02 already edited this file in wave 2**, converting the existing 3-arg forward at `:110` to a named-argument call (`cancellationToken: cancellationToken`) so it survives the repository widening. That is a different member from the 4-arg overload this task adds, and the waves are sequential, so the overlap is harmless — but do **not** revert 05-02's named-argument form back to a positional call, and do not re-widen the 3-arg forward. Add the overload beside it.

In `AdminHarvestController.SubmitUrl`, call `_deckImporter.ImportWithMetadataAsync(url, cancellationToken)`, persist `result.Entries`, extract the commander from `entry.Board == "commander"` with `StringComparison.OrdinalIgnoreCase` (D-09), and call the 4-arg overload as `_categoryStore.MarkUrlDeckProcessedAsync(deckId, commanderName, result.Metadata, cancellationToken)`. Leave the banner statement at `:286` unchanged — its rendered text changes on its own. Do not add UI surface for metadata, and do not backfill pre-existing rows.

Exactly three `IArchidektDeckImporter` fakes must override `ImportWithMetadataAsync`, because they are the importer for a path that now calls it; every other implementer stays untouched under the throwing default:
- `DeckFlow.Core.Tests/ArchidektDeckCacheSessionTests.cs:118`
- `DeckFlow.Web.Tests/AdminHarvestControllerTests.cs:334` (`StubArchidektDeckImporter`, `:334-338`)
- `DeckFlow.Core.Tests/ContentHashDedupTests.cs:436` (handled in Task 2)

`FakeCategoryKnowledgeStore` must override the 4-arg overload and record the metadata it receives. `AdminHarvestControllerTests.Build()` was already parameterized by Task 1 (`Build(store, crossOrigin, importer)`, importer defaulting to the existing stub); do not re-do that here and do not revert it to the hard-coded stub — Task 3 only adds the production overload it calls into.

**Add the store-level persistence test — this is the URL path's only proof that metadata reaches SQLite.** Everything else on this path stops short of the database: Test 4 asserts against `FakeCategoryKnowledgeStore`, and plan 05-02 proves `CategoryKnowledgeRepository` → `deck_queue`. The hop in between — `CategoryKnowledgeStore`'s new 4-arg overload → the repository — has no test that can fail, and it is the single most likely place for a mistake in this plan set: the overload is written *beside* a one-line forward that plan 05-02 just rewrote (`CategoryKnowledgeStore.cs:110`), so a copy-paste that drops the `metadata` argument produces a compiling, silently metadata-losing production path — and **every command in `05-VALIDATION.md` stays green**. One test closes it:

- In `DeckFlow.Web.Tests/CategoryKnowledgeStoreTests.cs`, add a `[Fact]` modelled on `GetTotalProcessedDeckCountSinceAsync_WithMixedProcessedRows_...` (`:180-228`) — the file's existing real-store-plus-raw-SQL harness. Follow it exactly: save and restore `MTG_DATA_DIR`, set it to null, `CreateStore(Path.Combine(tempRoot, "content"))` (`:261-262`), and clean up in `finally` with `SqliteConnection.ClearAllPools()` + `Directory.Delete(tempRoot, recursive: true)`. The class is already `DisableParallelization`-serialized for this reason; do not add a new collection.
- Call the **4-arg** `MarkUrlDeckProcessedAsync(deckId, commanderName, metadata, ...)` with a fully-populated `ArchidektDeckMetadata` (all five curated values non-null plus `CapturedUtc`).
- Read the row back over a raw `SqliteConnection` on `store.DatabasePath` (public, `CategoryKnowledgeStore.cs:62`; non-null once a store operation has run) and assert `archidekt_edh_bracket` equals the value passed and `archidekt_metadata_captured_utc` is non-null. Asserting those two is enough to kill the drop-the-argument mistake; asserting all six is fine but not required.
- Read-only raw SQL in the test only — plan 05-02's prohibition on adding a production metadata read API stands.

Finally, add one short README line under the harvest/admin documentation noting that Archidekt harvest now records deck-level bracket/provenance metadata on `deck_queue` prospectively (no backfill) and that admin URL imports now attribute a commander. `CLAUDE.md` requires a README update when behavior changes, and D-09 changes user-visible admin output.
  </action>
  <verify>
    <automated>dotnet.exe test DeckFlow.Web.Tests/DeckFlow.Web.Tests.csproj --filter "FullyQualifiedName~AdminHarvestControllerTests|FullyQualifiedName~CategoryKnowledgeStoreTests" --no-restore</automated>
    <automated>dotnet.exe build DeckFlow.sln --no-restore</automated>
    <automated>scripts/format-check-changed.sh staged</automated>
  </verify>
  <done>Admin URL imports use the same metadata record and repository path as bulk harvest; the 3-arg `MarkUrlDeckProcessedAsync` member is unchanged and no implementer outside this plan's write set was edited; commander extraction follows Archidekt board semantics with the exact D-09 banner asserted; the new `CategoryKnowledgeStoreTests` fact proves the 4-arg store overload actually persists metadata to `deck_queue` rather than dropping it; the README carries the behavior note; Web tests compile and pass and `dotnet.exe build DeckFlow.sln --no-restore` is clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Importer result -> harvest repository | Parsed external metadata is written during automated bulk harvest. |
| Admin POST URL -> Archidekt importer -> store | Admin-supplied URL triggers metadata import and persistence. |
| Hash boundary | Top-level metadata changes must not contaminate card-list content hash. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-08 | Tampering | bulk harvest metadata propagation | mitigate | Use only importer-produced metadata; skip/failure tests assert null metadata on failures. |
| T-05-09 | Repudiation | admin URL provenance | mitigate | URL import tests assert the same metadata record reaches `MarkUrlDeckProcessedAsync`. |
| T-05-10 | Tampering | content hash boundary | mitigate | Tests assert metadata-only changes update queue metadata without rewriting category facts or changing the card-list hash contract. |
| T-05-11 | Spoofing | bracket value | mitigate | No local bracket inference; controller/session only forward importer-captured `edhBracket`. |
| T-05-14 | Repudiation | URL-subset commander attribution (D-09) | accept | Pre-existing URL-imported rows keep `commander_name IS NULL` with no backfill, so the URL subset has a hard attribution boundary at deploy time. Recorded in `05-CONTEXT.md` D-09 and in this plan's `must_haves.truths`; any future commander-grouped corpus query must not read that subset as a time series. |
| T-05-SC | Tampering | package installs | accept | No package-manager install is planned. |
</threat_model>

<verification>
Run focused Core and Web propagation tests, then run:
- `dotnet.exe test DeckFlow.Core.Tests/DeckFlow.Core.Tests.csproj --filter "FullyQualifiedName~ArchidektApiDeckImporterTests|FullyQualifiedName~ArchidektDeckCacheSessionTests|FullyQualifiedName~CategoryKnowledgeRepositoryTests|FullyQualifiedName~CategoryCacheSchemaParityTests|FullyQualifiedName~ContentHashDedupTests" --no-restore`
- `dotnet.exe test DeckFlow.Web.Tests/DeckFlow.Web.Tests.csproj --filter "FullyQualifiedName~AdminHarvestControllerTests|FullyQualifiedName~CategoryKnowledgeStoreTests" --no-restore`
- `scripts/format-check-changed.sh staged` — CI `format-gate` is the authoritative enforcer; files stay LF per `.gitattributes` and no `[Attribute]` is inlined onto a property line.
- Before phase closeout: `dotnet.exe test DeckFlow.sln --no-restore`
</verification>

<success_criteria>
- Bulk harvest and admin URL import both write curated Archidekt metadata through shared repository semantics, and both pass `null` through untouched when the importer captured nothing.
- Skip/failure rows never fabricate metadata.
- Metadata capture does not change content-hash/category-fact behavior; the four `ContentHashDedupTests` session tests pass with unmodified assertions.
- D-09 is implemented: commander comes from `Board == "commander"`, the exact new banner string is asserted, and no pre-existing row is backfilled.
- The 3-arg `ICategoryKnowledgeStore.MarkUrlDeckProcessedAsync` member is unchanged, so the five out-of-scope implementers still compile with no edits.
- The URL write path has no untested hop: controller→store is proven against the fake, store→repository is proven against a real store reading `deck_queue` back, and repository→SQL is proven in plan 05-02.
- All BRKT requirements and all locked decisions D-01 through D-10 are covered across plans 05-01 through 05-03 (D-10 is implemented in plan 05-02; this plan must not defeat it).
</success_criteria>

<output>
Create `.planning/workstreams/cycle21-cut-lab/phases/05-archidekt-bracket-capture/05-03-SUMMARY.md` when done.
</output>
