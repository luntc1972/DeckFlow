---
phase: 50-code-style-enforcement
plan: 03
type: execute
wave: 2
depends_on: ["50-01"]
files_modified:
  - DeckFlow.Core.Tests/CarveOutGuardTests.cs
autonomous: true
requirements: [FMT-02]
user_setup: []

must_haves:
  truths:
    - "An xUnit test formats four fixtures with the reconciled .editorconfig and asserts each is byte-identical after formatting"
    - "The four fixtures cover: { get; init; }, a raw-string literal, an own-line [Attribute], and a switch expression"
    - "The guard test runs the SAME dotnet format mode the Plan 02 gate uses (FULL dotnet format by default, per 50-02-SUMMARY) — not a weaker whitespace-only run that would under-protect init/switch when a gate-enforceable style key is adopted"
    - "The test is a regression tripwire on FUTURE .editorconfig edits — if a later edit weakens a carve-out, the test fails loudly"
    - "The test is designed to run in CI (push-and-watch) since VSTest is unreliable in WSL; it self-skips/build-only locally"
  artifacts:
    - path: "DeckFlow.Core.Tests/CarveOutGuardTests.cs"
      provides: "Carve-out byte-identity regression assertions (FMT-02)"
      contains: "init"
      min_lines: 40
  key_links:
    - from: "DeckFlow.Core.Tests/CarveOutGuardTests.cs"
      to: "the reconciled .editorconfig"
      via: "dotnet format (same mode as the gate) over a compilable temp project carrying the repo .editorconfig"
      pattern: "editorconfig|dotnet"
---

<objective>
Add the FMT-02 carve-out guard test: an xUnit test that formats four code fixtures with the reconciled `.editorconfig`, using the SAME `dotnet format` mode the Plan 02 gate uses, and asserts each is byte-identical afterward. It is a regression tripwire — if a future `.editorconfig` edit weakens a carve-out, this test fails loudly in CI.

Purpose: The four code-affecting carve-outs (`{ get; init; }`, raw-string literal, own-line `[Attribute]`, switch expression) must survive the formatter. RESEARCH proved the formatter does not touch them today (resharper_* keys are invisible to dotnet format; init/switch verified inert), so this test guards against FUTURE config drift, not the formatter itself. CRITICAL: it must exercise the SAME mode the gate enforces (FULL `dotnet format` by default per 50-02) — a whitespace-only guard would under-protect init/switch if a gate-enforceable style/severity key is later adopted.

Output: `DeckFlow.Core.Tests/CarveOutGuardTests.cs`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/50-code-style-enforcement/50-SPEC.md
@.planning/phases/50-code-style-enforcement/50-RESEARCH.md
@.planning/phases/50-code-style-enforcement/50-01-SUMMARY.md
@.editorconfig

<interfaces>
<!-- xUnit 2.9.3 is already referenced by DeckFlow.Core.Tests (no new package). Namespace: DeckFlow.Core.Tests. -->
<!-- Test class: public sealed class CarveOutGuardTests (project convention: sealed, *Tests suffix, [Fact]). -->

<!-- Mechanism (RESEARCH Code Examples + Pitfall 1): write each fixture to a temp .cs file in a REPO-LOCAL temp dir -->
<!-- (NOT /tmp — Windows dotnet.exe from WSL can't resolve /tmp), alongside (or under a dir governed by) the repo .editorconfig, -->
<!-- run the SAME dotnet format mode the gate uses, read the file back, assert byte-equality. -->

<!-- MODE MUST MATCH THE GATE (FIX MEDIUM 4): the Plan 02 gate defaults to FULL `dotnet format` (style + analyzer rules), -->
<!-- not `whitespace --folder`. This guard MUST run that same mode, else it under-protects init/switch when a -->
<!-- gate-enforceable style key is adopted. FULL dotnet format needs a COMPILABLE project context (not a bare loose .cs), -->
<!-- so write the fixture into a tiny REPO-LOCAL temporary throwaway project (a minimal *.csproj + the fixture .cs under -->
<!-- a repo-local temp dir, NEVER /tmp), copy the repo .editorconfig into that dir so the real carve-outs apply, run -->
<!-- `dotnet format <thatproj> --verify-no-changes`-equivalent in MUTATING form (format then re-read) OR run the format -->
<!-- and read back the fixture file. Read the gate mode from 50-02-SUMMARY and MIRROR it; record the mode dependency in -->
<!-- a code comment + the SUMMARY. If 50-02-SUMMARY documents whitespace-only mode (only when all enforced rules are -->
<!-- whitespace-only), this guard may use whitespace mode to match — but FULL is the expected default. -->

<!-- dotnet executable: on the authoritative CI runner (ubuntu) `dotnet` is on PATH — Process.StartInfo.FileName = "dotnet". -->
<!-- Do NOT hard-code a WSL->Windows dotnet.exe bridge inside Process.Start (that is a WSL-only concern; FMT-02 is authoritative in CI). -->

<!-- Four fixtures (all must be byte-identical after format): -->
<!--   1. init accessor:     public string Name { get; init; } = ""; -->
<!--   2. raw-string literal: a """ ... """ block whose interior indentation must NOT change -->
<!--   3. own-line attribute: [JsonPropertyName("x")] on its OWN line above a property (must NOT inline) -->
<!--   4. switch expression:  a => switch { ... } expression form (must NOT become a switch statement) -->

<!-- CLAUDE.md: VSTest unreliable in WSL -> target CI execution (push-and-watch). RESEARCH Open Q2: do NOT gate the -->
<!-- pre-commit hook on this test. It is a CI-run regression guard; locally it is build-only (self-skips). -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write CarveOutGuardTests.cs with four byte-identity carve-out assertions (same dotnet format mode as the gate)</name>
  <files>DeckFlow.Core.Tests/CarveOutGuardTests.cs</files>
  <behavior>
    - Test 1 (InitAccessor_SurvivesFormatting_ByteIdentical): a `{ get; init; }` property string is byte-identical after formatting with the repo `.editorconfig` in the gate's mode.
    - Test 2 (RawStringLiteral_SurvivesFormatting_ByteIdentical): a `"""`-delimited raw-string literal with deliberate interior indentation is byte-identical after formatting.
    - Test 3 (OwnLineAttribute_SurvivesFormatting_ByteIdentical): an `[Attribute]` on its own line above a member is NOT inlined — byte-identical after formatting.
    - Test 4 (SwitchExpression_SurvivesFormatting_ByteIdentical): a switch EXPRESSION is not rewritten to a switch statement — byte-identical after formatting.
  </behavior>
  <read_first>
    - .planning/phases/50-code-style-enforcement/50-RESEARCH.md (Code Examples "Carve-out guard test"; Pitfall 1 repo-local temp; Open Q2/Q3; A3 CI-targeting)
    - .editorconfig (the reconciled config the fixtures are formatted against — carve-out lines 45-82)
    - .planning/phases/50-code-style-enforcement/50-01-SUMMARY.md (confirm reconciliation kept carve-outs; lists ADOPT-RS keys)
    - .planning/phases/50-code-style-enforcement/50-02-SUMMARY.md (THE GATE MODE — this guard MUST mirror it; FULL by default)
    - DeckFlow.Core.Tests/DeckFlow.Core.Tests.csproj (confirm xUnit present; no new package)
    - CLAUDE.md (Testing: VSTest unreliable in WSL -> CI-targeted; xUnit for .NET Core projects; no new test deps)
  </read_first>
  <action>
    Create `DeckFlow.Core.Tests/CarveOutGuardTests.cs`, namespace `DeckFlow.Core.Tests`, `public sealed class CarveOutGuardTests` with four `[Fact]` methods (names per the behavior block).
    Shared helper `RunDotnetFormatOnSnippet(string source)`: create a REPO-LOCAL temp directory (e.g. under the test project's bin-adjacent path or a repo `artifacts/`-style dir — NOT `/tmp`, Pitfall 1).
    MODE — MIRROR THE GATE (FIX MEDIUM 4): read 50-02-SUMMARY to learn the gate's `dotnet format` mode and run the SAME mode here. The gate defaults to FULL `dotnet format` (style + analyzer rules), which requires a COMPILABLE project context — so do NOT format a bare loose `.cs` with `whitespace --folder`. Instead generate a tiny REPO-LOCAL throwaway project in the temp dir: a minimal `*.csproj` (net10.0, Nullable enable, no extra packages) plus the fixture `.cs`, and COPY the repo `.editorconfig` into that temp dir so the real carve-outs apply. Run FULL `dotnet format <that.csproj>` (mutating) then read the fixture `.cs` back; OR run the verify-equivalent and re-read. Only if 50-02-SUMMARY documents whitespace-only mode (the rare all-whitespace case) may this guard use `dotnet format whitespace --folder <dir>` instead. Record the chosen mode + the 50-02 dependency in a code comment. Clean up the temp dir/project afterward.
    `dotnet` EXECUTABLE RESOLUTION (FMT-02 is authoritative on the ubuntu CI runner): set `Process.StartInfo.FileName = "dotnet"` (plain, PATH-resolved) for the CI execution path. Do NOT attempt a WSL->Windows `dotnet.exe` bridge inside `Process.Start` — on the ubuntu runner `dotnet` is on PATH and that is where the authoritative pass/fail is recorded; a `.exe` bridge is WSL-only and must not be hard-coded here.
    LOCAL EXECUTION (WSL): these tests are BUILD-ONLY locally (VSTest unreliable in WSL, and PATH `dotnet` resolution is a CI concern). Make each `[Fact]` self-skip when not running under CI — gate the body on an environment signal (e.g. `Environment.GetEnvironmentVariable("CI")` / `GITHUB_ACTIONS`) using `Assert.Skip(...)` (xUnit 2.9.3 supports dynamic skip) so a local `dotnet test` does not execute the Process.Start path. The class also carries `[Trait("Category","CarveOutGuard")]` (below) so a local run can additionally `--filter Category!=CarveOutGuard` to exclude it; either mechanism keeps it build-only locally and run-only in CI.
    Each test passes a fixture (init accessor / raw-string literal / own-line attribute / switch expression — see interfaces) and asserts `Assert.Equal(fixture, RunDotnetFormatOnSnippet(fixture))` (byte-for-byte). Normalize ONLY trailing-newline differences the formatter's `insert_final_newline` would add (account for it in the fixture so the assertion is exact, not lenient).
    Add an xUnit `[Trait("Category","CarveOutGuard")]` (or a class-level filterable trait) so CI can run `--filter Category=CarveOutGuard` and a local run can exclude it. Add an XML doc-comment on the class explaining this is a regression tripwire on `.editorconfig` edits, CI-targeted (VSTest unreliable in WSL; self-skips locally), running the same `dotnet format` mode as the gate, per FMT-02.
    Do NOT add any new NuGet package. Do NOT introduce a mocking library (none needed). Build only — VSTest run is deferred to CI per CLAUDE.md.
  </action>
  <verify>
    <automated>test -f DeckFlow.Core.Tests/CarveOutGuardTests.cs &amp;&amp; grep -q 'public sealed class CarveOutGuardTests' DeckFlow.Core.Tests/CarveOutGuardTests.cs &amp;&amp; grep -q 'init' DeckFlow.Core.Tests/CarveOutGuardTests.cs &amp;&amp; grep -qE 'RawString|"""' DeckFlow.Core.Tests/CarveOutGuardTests.cs &amp;&amp; grep -qiE 'Attribute' DeckFlow.Core.Tests/CarveOutGuardTests.cs &amp;&amp; grep -qiE 'switch' DeckFlow.Core.Tests/CarveOutGuardTests.cs &amp;&amp; grep -c '\[Fact\]' DeckFlow.Core.Tests/CarveOutGuardTests.cs | grep -qE '[4-9]' &amp;&amp; ! grep -q '/tmp' DeckFlow.Core.Tests/CarveOutGuardTests.cs &amp;&amp; grep -q 'CarveOutGuard' DeckFlow.Core.Tests/CarveOutGuardTests.cs &amp;&amp; echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `DeckFlow.Core.Tests/CarveOutGuardTests.cs` exists with `public sealed class CarveOutGuardTests` and four `[Fact]` methods covering init accessor, raw-string literal, own-line attribute, switch expression.  (FMT-02 / SPEC AC-2)
    - The helper runs the SAME `dotnet format` mode as the Plan 02 gate (FULL by default per 50-02-SUMMARY); when FULL, it uses a compilable repo-local throwaway project (minimal *.csproj + fixture .cs + copied .editorconfig), never a bare loose-file whitespace run that under-protects style carve-outs. The mode + 50-02 dependency is noted in a code comment.  (FIX MEDIUM 4)
    - The helper writes fixtures/project and any report to a REPO-LOCAL temp path, never `/tmp` (asserted: no `/tmp` in file).  (RESEARCH Pitfall 1)
    - `Process.StartInfo.FileName` is plain `"dotnet"` (PATH-resolved for the CI runner); NO hard-coded WSL->Windows `dotnet.exe` bridge in the Process.Start path.
    - Each `[Fact]` self-skips when not under CI (env signal + `Assert.Skip`) so a local `dotnet test` is build-only and does not run Process.Start; a `CarveOutGuard` trait also allows local `--filter Category!=CarveOutGuard` exclusion.
    - Each test asserts byte-identity (`Assert.Equal`) of fixture before/after formatting with the repo `.editorconfig`.  (SPEC AC-2)
    - A filterable trait (`CarveOutGuard`) is present so CI can `--filter Category=CarveOutGuard`.
    - No new NuGet package added; xUnit-only (project convention .NET Core -> xUnit).
    - `dotnet build DeckFlow.Core.Tests/DeckFlow.Core.Tests.csproj -c Release` compiles clean (0 new errors/0 new warnings).  (CLAUDE.md Definition of Done)
  </acceptance_criteria>
  <done>The four-fixture carve-out guard test compiles clean and is wired for CI execution (filterable trait + self-skip locally, plain "dotnet" on PATH), running the SAME dotnet format mode as the gate (FULL via a repo-local compilable throwaway project by default), asserting byte-identity against the reconciled .editorconfig.</done>
</task>

</tasks>

<verification>
- `dotnet build DeckFlow.Core.Tests/DeckFlow.Core.Tests.csproj -c Release` clean.
- Source greps confirm four facts + all four carve-out concepts + repo-local temp (no `/tmp`) + plain `"dotnet"` + `CarveOutGuard` trait/skip wiring.
- The helper's `dotnet format` mode matches the Plan 02 gate mode recorded in 50-02-SUMMARY (FULL by default; documented in a comment).
- Actual PASS/FAIL of the four tests is confirmed in CI (push-and-watch) per CLAUDE.md — recorded at the phase gate (Plan 04), not asserted locally. The Plan 04 gate must confirm the four CarveOutGuard fixtures actually RAN (not skipped) and passed in CI.
</verification>

<success_criteria>
- Four code carve-outs proven byte-identical after formatting with the reconciled config, using the SAME mode the gate enforces (so the guard cannot under-protect relative to the gate).
- Test is a durable regression tripwire on future `.editorconfig` edits, CI-runnable (plain `"dotnet"` on PATH), build-only locally.
- No new dependency, compiles clean, no `/tmp` reliance.
</success_criteria>

<output>
Create `.planning/phases/50-code-style-enforcement/50-03-SUMMARY.md` when done. Note that PASS/FAIL is CI-verified (VSTest WSL caveat — tests self-skip locally); WHICH `dotnet format` mode the helper uses and that it MIRRORS the Plan 02 gate mode from 50-02-SUMMARY (FULL via a compilable repo-local throwaway project by default); and that `Process.StartInfo.FileName` is plain `"dotnet"` (no `.exe` bridge). Flag that Plan 04's phase gate must confirm the four fixtures RAN (not skipped) and passed in CI.
</output>
