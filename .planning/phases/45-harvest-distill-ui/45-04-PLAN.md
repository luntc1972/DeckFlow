---
phase: 45-harvest-distill-ui
plan: 04
type: execute
wave: 4
depends_on: ["45-03"]
files_modified:
  - DeckFlow.Studio/Pages/Harvest.razor
autonomous: false
requirements: [HARV-05]
must_haves:
  truths:
    - "Stage A dry-run shows projected spend, would-run count, and monthly cap remaining before any live distill"
    - "Re-distilling already-distilled videos requires two sequential checkbox confirmations before the dry-run button enables"
    - "Stage B requires a reviewed-spend confirmation checkbox before the live Run Distill button enables"
    - "Distill runs off the Blazor sync context with live progress and cancel; actual spend is shown after the run"
    - "When projected spend exceeds the monthly cap, the live distill is blocked until selection is reduced or the cap is raised"
    - "The operator can raise the cap for the session only via SessionCapOverride; it is not persisted"
    - "DistillAsync is called with redistill:true ONLY after both re-distill confirmations; otherwise redistill:false"
  artifacts:
    - path: "DeckFlow.Studio/Pages/Harvest.razor"
      provides: "Two-stage spend-gated distill flow with cap display, session override, and gated redistill flag"
      contains: "DistillAsync"
  key_links:
    - from: "DeckFlow.Studio/Pages/Harvest.razor"
      to: "IDistillOrchestrator.DistillAsync"
      via: "dryRun:true then dryRun:false inside Task.Run with isSubscriptionProvider from StudioDistillConfig and redistill gated on double-confirm"
      pattern: "DistillAsync"
    - from: "DeckFlow.Studio/Pages/Harvest.razor"
      to: "ILlmSpendLedger.GetMonthlyCapUsd / GetMonthlyTotalAsync"
      via: "cap remaining = cap - month total"
      pattern: "GetMonthlyCapUsd|GetMonthlyTotalAsync"
    - from: "DeckFlow.Studio/Pages/Harvest.razor"
      to: "SessionCapOverride.OverrideUsd"
      via: "raise-cap input writes the session override"
      pattern: "OverrideUsd"
---

<objective>
Add the distill spend-gate to the Harvest page (HARV-05): Stage A dry-run projection, the
re-distill double-confirm guard for already-distilled videos, the monthly cap + remaining
display, the session cap-raise control (D-03), and Stage B confirm-and-execute with live
progress and actual-spend reporting. The re-distill confirmations gate the new `redistill:true`
argument to `DistillAsync` (added in Wave 1 — HIGH-4) so re-distillation can actually execute.

This replaces the Wave 3 placeholder distill card in `Harvest.razor`. It reuses the same
`_operationInFlight` lock, `_cts` lifecycle, `ActionOrchestratorProgress` bridge, and Task.Run
background pattern established in Plan 03 — do NOT introduce a second pattern.

This plan is NON-AUTONOMOUS: it ends with a human-verify checkpoint because the multi-step spend
gate and the cap-exceeded block are runtime UI behaviors (no Studio test project; the cap math
and the redistill orchestrator path were unit-tested in Wave 1).

Purpose: Make distillation cost-safe — no silent re-distill, no spend without an explicit
reviewed-and-confirmed dry run, cap enforced.
Output: The distill section of `Harvest.razor`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/45-harvest-distill-ui/45-CONTEXT.md
@.planning/phases/45-harvest-distill-ui/45-RESEARCH.md
@.planning/phases/45-harvest-distill-ui/45-PATTERNS.md
@.planning/phases/45-harvest-distill-ui/45-UI-SPEC.md
@DeckFlow.Studio/Pages/Harvest.razor

<interfaces>
<!-- Confirmed contracts. Do NOT explore the codebase for these. -->

IDistillOrchestrator (DeckFlow.Core/Orchestration) — UPDATED in Wave 1 (Plan 01) with the redistill flag:
  Task<DistillResult> DistillAsync(int limit, bool dryRun, bool isSubscriptionProvider,
      bool redistill = false,
      IReadOnlyList<string>? videoIds = null, IOrchestratorProgress? progress = null, CancellationToken ct = default);
  // NOTE: confirm the exact position of `redistill` from the Plan 01 SUMMARY (it sits with the
  // gating bools after isSubscriptionProvider). Pass redistill by NAME (redistill: ...) so position
  // is irrelevant. When redistill:true, only already-distilled videos present in videoIds are
  // re-processed (their prior output is cleared first); redistill:false preserves the skip.
DistillResult: bool Success; int SourcesProcessed; int VideosDistilled; int VideosFiltered; int DistillFailed;
  int LlmCalls; decimal LlmSpendUsd; int WouldRun; decimal ProjectedSpendUsd;
  IReadOnlyList<string> FailedVideoIds; string? AbortedReason; bool DryRun;

ILlmSpendLedger (Wave 1 + existing):
  decimal GetMonthlyCapUsd();                                  // synchronous (Wave 1)
  Task<decimal> GetMonthlyTotalAsync(string yearMonth, CancellationToken ct = default);
  // month key form "yyyy-MM" (UTC). Remaining = GetMonthlyCapUsd() - GetMonthlyTotalAsync(currentMonth).

StudioDistillConfig.IsSubscriptionProvider (bool) — pass as isSubscriptionProvider (D-01).
  When false and dryRun:false, the orchestrator hard-aborts with Success=false + AbortedReason — surface that text.

SessionCapOverride.OverrideUsd (decimal?) — write to raise the cap for the session (D-03);
  the registered ledger resolver already reads it (Plan 02), so GetMonthlyCapUsd() and the
  orchestrator's internal WouldExceedCapAsync both reflect the new value immediately.

VideoStatus / VideoStatusResolver — DeckFlow.Core.Content, already injected in Harvest.razor (Plan 03);
  VideoStatus.Distilled = already-distilled (re-distill guard applies).

Already in Harvest.razor from Plan 03: _cts, _operationInFlight, _logLines, disposal-safe
ActionOrchestratorProgress sink, Dispose().
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Stage A — dry-run projection, re-distill double-confirm, cap display + session override</name>
  <files>DeckFlow.Studio/Pages/Harvest.razor</files>
  <read_first>
    - DeckFlow.Studio/Pages/Harvest.razor (the Plan 03 file — reuse its _cts/_operationInFlight/_logLines/progress bridge; replace the distill placeholder)
    - .planning/phases/45-harvest-distill-ui/45-01-SUMMARY.md (confirm the redistill parameter name + position on DistillAsync)
    - .planning/phases/45-harvest-distill-ui/45-UI-SPEC.md (Section 4 Stage A lines 203-234, Copywriting Contract re-distill rows lines 314-318, Destructive-actions table lines 321-330)
    - .planning/phases/45-harvest-distill-ui/45-RESEARCH.md (Pitfall 3 re-distill lines 529-539; Pattern 5 cap read lines 411-465; Anti-pattern "DistillAsync(dryRun:false) on metered provider" lines 484-487)
    - .planning/phases/45-harvest-distill-ui/45-CONTEXT.md (D-02 cap+remaining, D-03 session override)
  </read_first>
  <action>
    Replace the placeholder distill card in `Harvest.razor` with Stage A. Compute "Videos ready to
    distill: {N}" as the count of selected videos whose resolved `VideoStatus` is `Harvested`
    (harvested-but-not-distilled). For selected videos whose status is `Distilled`, render the amber
    `alert alert-warning` re-distill banner with the exact copy ("Warning: {K} selected video(s)
    have already been distilled…"). Implement the two-checkbox secondary confirmation (Pitfall 5):
    a first checkbox "Re-distill already-distilled videos (additional spend)"; when checked, reveal
    a second inline confirm checkbox "Yes, I understand — overwrite existing distill output for {K}
    video(s)". The "Estimate Spend (dry run)" `btn-outline-primary` stays disabled until BOTH are
    checked whenever any already-distilled video is selected; if none are selected, the button is
    enabled directly. Bind each checkbox to a backing bool field. Compute a single
    `bool redistillConfirmed = <both checkboxes checked>` gate used for BOTH the button-enable
    condition AND the `redistill:` argument passed to DistillAsync — they must agree.

    Cap display (D-02): compute the current UTC month key `DateTime.UtcNow.ToString("yyyy-MM")`,
    show "Monthly cap: ${GetMonthlyCapUsd():F2}", "Spent this month: ${await
    GetMonthlyTotalAsync(month):F2}", and "Remaining: ${cap - spent:F2}". Read the cap/total at
    page init and refresh after any distill. Add a session cap-raise control (D-03): a numeric
    input + "Raise cap (this session)" `btn-outline-secondary` that sets
    `CapOverride.OverrideUsd = <entered value>` and re-reads cap/remaining. Note inline that the
    raise is session-only and resets on Studio restart. Validate the input is a non-negative decimal
    before writing (V5 input validation); ignore/flag invalid input without throwing.

    Stage A trigger: "Estimate Spend (dry run)" runs the established background pattern —
    `_cts = new CancellationTokenSource()`, `_operationInFlight = true`, clear `_logLines`, build the
    disposal-safe `ActionOrchestratorProgress` sink (same as Plan 03), then
    `await Task.Run(() => DistillOrchestrator.DistillAsync(limit: distillIds.Count, dryRun: true,
    isSubscriptionProvider: DistillConfig.IsSubscriptionProvider, redistill: redistillConfirmed,
    videoIds: distillIds, progress: progress, cancellationToken: _cts.Token), _cts.Token)`. Build
    `distillIds` from the selected Harvested videos PLUS the selected Distilled videos ONLY when
    `redistillConfirmed` is true (otherwise distilled videos are excluded from distillIds entirely).
    Pass `redistill:` by name (HIGH-4 — re-distill executes only on confirm; a dry run with
    redistill:true projects the re-run, with redistill:false the orchestrator still skips distilled).
    Render the dry-run result card (`card border-primary`): WouldRun, ProjectedSpendUsd (F4), and
    "Monthly cap remaining". If `ProjectedSpendUsd` + month total would exceed the cap, show the
    `alert alert-danger` cap-exceeded copy ("Projected spend (${X}) exceeds your monthly cap
    (${Y})…") and keep Stage B disabled (gate variable). Reuse the spinner + log-box markup from
    Plan 03. Same `_operationInFlight` lock; no second CTS pattern. Preserve LF.
  </action>
  <verify>
    <automated>"/mnt/c/Program Files/dotnet/dotnet.exe" build DeckFlow.sln 2>&1 | grep -E "error|warning|Build succeeded" | head -30</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "dryRun: true" DeckFlow.Studio/Pages/Harvest.razor` >= 1 and `grep -c "GetMonthlyCapUsd" Harvest.razor` >= 1 and `grep -c "GetMonthlyTotalAsync" Harvest.razor` >= 1.
    - `grep -c "redistill:" Harvest.razor` >= 1 (the redistill argument is passed by name) and the redistill value is the SAME gate (redistillConfirmed) that enables the dry-run button — distilled videos enter distillIds only when that gate is true.
    - `grep -c "OverrideUsd" Harvest.razor` >= 1 (session cap-raise writes the override).
    - `grep -c "alert alert-warning" Harvest.razor` >= 1 (re-distill banner) and the file contains two distinct re-distill confirmation checkbox bindings (two backing bool fields gating the dry-run button and the redistill flag).
    - `grep -c "alert alert-danger" Harvest.razor` >= 1 (cap-exceeded block) and `grep -c "card border-primary" Harvest.razor` >= 1 (dry-run result card).
    - `grep -c "IsSubscriptionProvider" Harvest.razor` >= 1 (passed to DistillAsync).
    - No new CancellationTokenSource pattern beyond the existing `_cts` (still exactly one `_cts` field; `grep -c "private CancellationTokenSource" Harvest.razor` returns 1).
    - `dotnet build DeckFlow.sln` reports Build succeeded, zero errors, zero new warnings.
  </acceptance_criteria>
  <done>Stage A renders ready-count, re-distill double-confirm gating both the dry-run button and the redistill:true argument, cap+remaining, session cap-raise, and a dry-run result card with cap-exceeded block; reuses the Plan 03 background pattern; builds clean.</done>
</task>

<task type="auto">
  <name>Task 2: Stage B — confirm + execute live distill with actual-spend reporting</name>
  <files>DeckFlow.Studio/Pages/Harvest.razor</files>
  <read_first>
    - DeckFlow.Studio/Pages/Harvest.razor (Stage A from Task 1 — Stage B appears only after a successful dry-run)
    - .planning/phases/45-harvest-distill-ui/45-UI-SPEC.md (Section 4 Stage B lines 236-261, Copywriting Contract distill-complete rows lines 311-313, State Machine lines 334-351)
    - .planning/phases/45-harvest-distill-ui/45-RESEARCH.md (DistillResult fields lines 611-626; Anti-pattern metered-provider abort lines 484-487)
  </read_first>
  <action>
    Add Stage B below the Stage A dry-run result card, visible only after a successful dry-run and
    only when the cap-exceeded gate is clear. Add the required confirmation checkbox "I have
    reviewed the estimated spend above and want to proceed with actual distillation" bound to a
    bool; the "Run Distill" `btn-primary` stays disabled until it is checked (and while
    `_operationInFlight`). On click, run the same background pattern with `dryRun: false`, passing
    the SAME `redistill: redistillConfirmed` gate as Stage A:
    `await Task.Run(() => DistillOrchestrator.DistillAsync(limit: distillIds.Count, dryRun: false,
    isSubscriptionProvider: DistillConfig.IsSubscriptionProvider, redistill: redistillConfirmed,
    videoIds: distillIds, progress: progress, cancellationToken: _cts.Token), _cts.Token)`. Render
    the spinner + log box and a "Cancel Distill" `btn-outline-danger btn-sm` (calls `_cts.Cancel()`);
    catch `OperationCanceledException` → append "Distill cancelled." in `finally` clear
    `_operationInFlight` and `await InvokeAsync(StateHasChanged)`.

    On completion, render the `card border-success` result: VideosDistilled, LlmCalls, LlmSpendUsd
    (F4), VideosFiltered, DistillFailed. If `DistillFailed > 0`, render an `alert alert-warning`
    listing `FailedVideoIds`. If `Success == false` and `AbortedReason` is non-empty (e.g. metered
    provider with isSubscriptionProvider=false, or cap exceeded inside the orchestrator), surface
    `AbortedReason` in an `alert alert-danger` instead of the success card — do not show a false
    "complete". After a successful run, re-resolve the status of distilled videos via
    `VideoStatusResolver.ResolveStatusAsync` so badges flip to `distilled`, and refresh the cap
    "remaining" display from the ledger. Same single `_operationInFlight` lock and `_cts`; no new
    pattern. Preserve LF.
  </action>
  <verify>
    <automated>"/mnt/c/Program Files/dotnet/dotnet.exe" build DeckFlow.sln 2>&1 | grep -E "error|warning|Build succeeded" | head -30</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "dryRun: false" DeckFlow.Studio/Pages/Harvest.razor` >= 1 and `grep -c "card border-success" Harvest.razor` >= 1.
    - The Stage B DistillAsync call passes `redistill: redistillConfirmed` (the same gate as Stage A), so the live re-distill executes only when the double-confirm was given (HIGH-4 wired end-to-end).
    - `grep -c "LlmSpendUsd" Harvest.razor` >= 1 and `grep -c "VideosDistilled" Harvest.razor` >= 1 (actual-spend result rendered).
    - `grep -c "AbortedReason" Harvest.razor` >= 1 (abort surfaced, not a false success).
    - `grep -c "FailedVideoIds" Harvest.razor` >= 1 (partial-failure list).
    - The Run Distill button is gated by a confirmation checkbox bool (a backing field referenced in the button `disabled`/enabled condition).
    - Still exactly one `_cts` field and one `_operationInFlight` lock (`grep -c "private bool _operationInFlight" Harvest.razor` returns 1).
    - `dotnet build DeckFlow.sln` reports Build succeeded, zero errors, zero new warnings.
  </acceptance_criteria>
  <done>Stage B requires a reviewed-spend confirm, runs the live distill (redistill gated on the Stage A double-confirm) off the sync context with cancel, surfaces actual spend / failures / abort reason, and refreshes badges + cap; builds clean.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Browser smoke — full spend-gate flow, re-distill guard, cap enforcement</name>
  <what-built>
    The two-stage distill spend gate (HARV-05): dry-run projection, re-distill double-confirm,
    monthly cap + remaining display, session cap-raise, confirm-and-execute live distill with actual
    spend, and the cap-exceeded block. Runtime multi-step UI flow — no Studio test project; the cap
    math and the redistill orchestrator path were unit-tested in Wave 1.
  </what-built>
  <how-to-verify>
    1. Start Studio (`DECKFLOW_DISABLE_AUTO_BROWSER=true`). For a real live distill set
       `DECKFLOW_LLM_PROVIDER=claude` (subscription / $0). Without it, expect the live run to abort
       with an AbortedReason — confirm that path too. Verify (HIGH-1) that with
       `DECKFLOW_LLM_PROVIDER` unset or `=openai`, the distiller is metered AND the cap is enforced
       (cap math is real, not zeroed); with `=claude` the run reports $0.
    2. On /harvest, select harvested-but-not-distilled videos. Confirm "Videos ready to distill: {N}"
       and "Monthly cap / Spent / Remaining" display correct numbers.
    3. Click "Estimate Spend (dry run)". Confirm the dry-run card shows WouldRun and projected spend;
       log streams live; tab stays responsive.
    4. Confirm Stage B is hidden until the dry-run succeeds, then appears with the "I have reviewed…"
       checkbox. Confirm "Run Distill" is disabled until that box is checked.
    5. Run the live distill (claude backend). Confirm actual spend, distilled/failed counts show, and
       the distilled videos' badges flip to "Distilled"; cap "Remaining" decreases.
    6. Re-distill guard: select an already-distilled video. Confirm the amber re-distill banner
       appears and the dry-run button stays disabled until BOTH the "Re-distill…" and the secondary
       "Yes, I understand…" checkboxes are checked. THEN dry-run + run distill the already-distilled
       video and confirm it is actually re-processed (re-distilled, prior output replaced) — proving
       the redistill:true path works end-to-end (HIGH-4), not silently skipped.
    7. Cap enforcement: lower the effective headroom (or raise selection) so projected spend exceeds
       remaining. Confirm the red cap-exceeded alert appears and Stage B / Run Distill is blocked.
       Then use "Raise cap (this session)" to raise the cap and confirm the block clears. Restart
       Studio and confirm the cap reverts to default/env (override not persisted).
    8. Confirm no secrets, connection strings, or provider values appear in the page or Studio logs.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues (e.g. re-distill guard bypassable, re-distill not actually re-processing, cap not enforced, override persists across restart, abort shown as success).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator browser → distill execution | A single click can incur real LLM spend; the gate (dry-run + confirm + cap) sits on this boundary |
| Page cap-raise input → SessionCapOverride | Operator-supplied numeric value raises the effective spend ceiling for the session (app-scoped) |
| Re-distill confirm → redistill:true | The double-confirm is the only thing that lets already-distilled videos be re-paid-for |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-45-10 | Tampering | Silent re-distill of already-distilled videos | mitigate | Already-distilled videos enter `distillIds` and `redistill:true` is passed ONLY when BOTH the re-distill checkbox and the secondary "Yes, I understand" checkbox are checked (single redistillConfirmed gate); the dry-run button stays disabled otherwise, and the orchestrator (Wave 1) only force-re-distills targeted videos. Verified in checkpoint step 6 (incl. that re-distill actually re-processes). |
| T-45-11 | Elevation of privilege | Unbounded LLM spend | mitigate | Live distill requires a successful dry-run AND a reviewed-spend confirmation checkbox AND a clear cap gate; projected-vs-cap is checked before Stage B enables, and the orchestrator independently enforces WouldExceedCapAsync against the real (single-provider-decision) cost. |
| T-45-12 | Elevation of privilege | Session cap raised to extreme value | accept | The raise writes only the in-memory app-scoped SessionCapOverride (D-03); it resets to env/default on restart (verified in checkpoint step 7). Cost risk bounded to the local operator; numeric input is validated non-negative. |
| T-45-13 | Spoofing / repudiation | Metered-provider distill shown as success | mitigate | When isSubscriptionProvider=false the orchestrator aborts with Success=false + AbortedReason; the page surfaces AbortedReason in a danger alert and never renders the success card for an aborted run. The HIGH-1 single-provider-decision fix (Plan 02) guarantees isSubscriptionProvider matches the actual distiller, so a claude flag can no longer mask metered OpenAI spend. |
| T-45-14 | Information disclosure | Spend/cap/provider in logs | mitigate | The page displays cap/spend to the operator (intended) but logs no connection string, provider value, or ledger key; checkpoint step 8 verifies no secret leakage. |
| T-45-SC | Tampering | npm/pip/cargo installs | accept | No package installs; no new NuGet (CLAUDE.md). |
</threat_model>

<verification>
- `dotnet build DeckFlow.sln` — zero errors, zero new warnings.
- Human-verify checkpoint passes for the full HARV-05 flow incl. re-distill guard (and actual re-processing) and cap block.
- Grep gates satisfied: dryRun true/false, redistill: by name gated on redistillConfirmed, cap read, override write, abort surfaced, single CTS/lock.
</verification>

<success_criteria>
- HARV-05: estimated and actual spend shown; re-distill explicit/opt-in via double-confirm AND actually re-processes via redistill:true; cap enforced; session override raises cap without persisting.
- No silent re-distill; no live spend without dry-run + confirm; aborts never shown as success.
</success_criteria>

<output>
Create `.planning/phases/45-harvest-distill-ui/45-04-SUMMARY.md` when done.
</output>
</output>
