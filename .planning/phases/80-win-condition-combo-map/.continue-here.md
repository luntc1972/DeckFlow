---
context: phase
phase: 80-win-condition-combo-map
task: 3
total_tasks: 3
status: complete
last_updated: 2026-07-02T23:29:10.872Z
---

# Phase 80 — Win-Condition & Combo Map — COMPLETE (paused at phase boundary)

No blocking constraints discovered this session — every gate ran clean. This handoff
marks a completed phase awaiting operator ship-gates + Phase 81 planning, not a mid-task pause.

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Prompt-mutating flag not registered for cache bypass | A flag that bakes a block into `AnalysisPromptText`/artifacts but is NOT in `PromptMutatingAnalysisFlags` gets cached and replayed after the flag flips OFF (Phase-73 replay class; bit P77+P79 latently). | advisory | Any NEW prompt-mutating analysis flag MUST be added to `DeckAnalysisPacketService.PromptMutatingAnalysisFlags`; the registry drives both `ShouldBypassPacketCache()` (read) and `bypassCacheWrite` (write). |
| Trusting raw posted round-trip JSON | Deriving artifacts/zip entries or view state from the raw posted `*Json` hidden field (vs the typed flag-gated result) leaks stale data across a flag ON->OFF flip and skips validation. | advisory | Serialize artifacts from the typed result via `SerializeForArtifact<T>`; clear the round-trip field when its typed result is null. |
| Executing two runs in one worktree | Phase-78 collision — a bg executor + an orchestrator Codex dispatch interleaved commits in one shared worktree. | advisory | One execution run per bench; Cycle 14 runs in the dedicated `deckflow-cycle14` worktree. |

<current_state>
Phase 80 fully delivered on branch `plan/cycle-14-deck-eval-depth` (dedicated cycle-14 worktree).
Working tree CLEAN. 27 commits ahead of origin, NOT pushed. Build 0/0; Core 1028 pass; Web 1144 pass / 12 skip (pre-existing Postgres-integration). Flag `analysis.wincon-map` seeded OFF (both dialects).
</current_state>

<completed_work>

Completed Tasks (all 3 plans + full review chain):
- 80-01 Core WinConMap model + WinConMapAggregator + 19 golden tests — Done (`62dcf365`)
- 80-02 flag seed OFF both dialects + reused combo fetch + hedged block through 3 decoupled variants + parity + unified cache-bypass — Done (`e28c6986`)
- 80-03 Step-3 readout + WinConMapJson round-trip + conditional 61-wincon-map.json zip entry + dual-layer byte-identity — Done (`324c43de`)
- gsd-verifier PASS — 18/18 truths, WINCON-01..04 SATISFIED (`80-VERIFICATION.md`)
- Codex review (gpt-5.4): 3 findings fixed (`cc8e1a86`/`a07f3453`/`0d654f25`) -> APPROVE
- /simplify: shared WinConBandFormatter + FormatQuantityNameList<T> + SerializeForArtifact (`de939fce`)
- /code-review (high effort): 4 fixed incl. 1 HIGH real bug — restore validator rejected ManaValueNeeded>30 while build path unbounded, nulling the whole restored map (`20e3f52a`/`f8c0d224`/`90cf1669`/`eb840b92`)
- Follow-up hardening: PromptMutatingAnalysisFlags cache-bypass registry (closes P77/P79 replay gap) + AnalysisPromptEnrichments param-bag (`92a03660`/`8adb69a4`) -> Codex APPROVE
</completed_work>

<remaining_work>

- No Phase-80 code remaining.
- OPERATOR (pending, see human_actions): push -> CI green; live visual smoke (desktop 1280 + mobile 390, flag ON/OFF, themes); then (after 81) merge cycle-14 -> main + MANUAL prod deploy + flip `analysis.wincon-map` ON.
- NEXT DEV: `/gsd-plan-phase 81` (Opening-Hand / Mulligan Evaluator).
</remaining_work>

<decisions_made>

- Cycle 14 execution delegation FLIPPED: Claude codes (sonnet gsd-executors), Codex gpt-5.4 medium reviews.
- Packet-cache replay fix generalized to `PromptMutatingAnalysisFlags` registry (all 4 flags) rather than a per-flag OR-chain — closes the score/interaction latent gap and future-proofs.
- Download/view state derived from the typed flag-gated result (never raw posted `*Json`) via `SerializeForArtifact<T>` + clear-or-set on the round-trip fields.
</decisions_made>

<blockers>
- None (technical). Only operator ship-gates pending (push/CI/visual-smoke/merge/deploy/flag).
</blockers>

## Required Reading (in order)
1. `.planning/STATE.md` — full Phase-80 verify + review + follow-up record (Current Position + Phase-80 blocks).
2. `.planning/HANDOFF.json` — machine-readable state + pending human actions.
3. `.planning/phases/80-win-condition-combo-map/80-VERIFICATION.md` — gsd-verifier evidence.
4. `.planning/ROADMAP.md` — Phase 81 scope (MULLIGAN-01..06) + phase-ordering rationale.

## Infrastructure State
- Branch `plan/cycle-14-deck-eval-depth`, dedicated worktree `deckflow-cycle14`. Clean tree, 27 commits ahead, unpushed.
- Render autodeploy OFF — prod deploy is MANUAL.
- Flags seeded OFF: `analysis.wincon-map` (P80), `analysis.interaction-audit` (P79 — pushed+CI-green already, prod flip owed).
- No background servers left running from this session.

<context>
Phase 80 mirrors the shipped Phase-77/79 recipe (new block -> 3 decoupled variants -> new flag -> per-surface byte-identity). It went through the full quality gauntlet and is solid. The only substantive surprise was the /code-review HIGH: an asymmetric restore validator (ManaValueNeeded<=30) that the earlier Codex pass + verifier both missed — fixed by removing the artificial bound. The cache-replay class is now fully closed via the registry. Phase 81 is the heaviest of the cycle (mulligan evaluator) and routes to /manabase (not /deck-analysis), reusing the single Monte-Carlo CastabilitySimulator — plan it in a fresh per-run bench.
</context>

<next_action>
Start with: `/gsd-plan-phase 81` (Opening-Hand / Mulligan Evaluator). Independently, tell the operator the Phase-80 push -> CI + live visual smoke is owed before any merge.
</next_action>
