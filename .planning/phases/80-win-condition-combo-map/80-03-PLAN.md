---
phase: 80-win-condition-combo-map
plan: 03
type: execute
wave: 3
depends_on: [80-02]
files_modified:
  - DeckFlow.Web/Models/DeckAnalysisRequest.cs
  - DeckFlow.Web/Models/DeckAnalysisViewModel.cs
  - DeckFlow.Web/Services/DeckAnalysisPacketService.cs
  - DeckFlow.Web/Services/Persistence/PacketArtifactStore.cs
  - DeckFlow.Web/Controllers/DeckPacketController.cs
  - DeckFlow.Web/Views/Deck/DeckAnalysis.cshtml
  - DeckFlow.Web/wwwroot/css/site-common.css
  - DeckFlow.Web.Tests/WinConMapSurfaceContractTests.cs
  - DeckFlow.Web.Tests/DeckAnalysisWinConMapViewTests.cs
  - DeckFlow.Web.Tests/DeckPacketControllerTests.cs
autonomous: true
requirements: [WINCON-01, WINCON-03, WINCON-04]
must_haves:
  truths:
    - "With the flag ON, the /deck-analysis Step-3 on-page readout shows the same ranked combos, near-combos, assembly-path count, band, closing cards, and data-unavailable disclosure as the paste artifact, hedged as an AI-confirmed candidate read"
    - "The computed WinConMap round-trips through a hidden WinConMapJson field so Step-3 re-renders without recomputation, AND is persisted as a 61-wincon-map.json zip entry that LoadFromZip restores on re-upload; a FRESH /deck-analysis/download serializes the freshly-computed map into the entry when the posted WinConMapJson field is empty, so the entry is never dropped"
    - "The round-tripped hidden field is treated as untrusted input: deserialization is size-capped, structurally validated (every combo, near-combo, closing card, the enum bands, AssemblyPathCount, every list + Name + Quantity, EVERY rendered string non-blank, and per-list count caps), and never throws"
    - "With the flag OFF, DeckAnalysisPacketResult.WinConMap is null, request.WinConMapJson is empty, the hidden field + readout region are absent, and the RENDERED page + 3 artifacts + zip are byte-identical to the pre-feature baseline (page proven by IRazorViewEngine excision-equality; zip proven by entry-map + per-platform content comparison)"
    - "New layout CSS lives in site-common.css using existing theme tokens + baked hex; NO new :root tokens are introduced"
  artifacts:
    - path: "DeckFlow.Web/Models/DeckAnalysisViewModel.cs"
      provides: "WinConMap view-model property"
      contains: "WinConMap"
    - path: "DeckFlow.Web/Services/Persistence/PacketArtifactStore.cs"
      provides: "61-wincon-map.json zip writer (conditional) + loader restoring request.WinConMapJson"
      contains: "61-wincon-map.json"
    - path: "DeckFlow.Web/Views/Deck/DeckAnalysis.cshtml"
      provides: "flag-guarded on-page readout block + CONDITIONAL hidden WinConMapJson field"
      contains: "WinConMap"
    - path: "DeckFlow.Web/wwwroot/css/site-common.css"
      provides: "wincon-map readout layout classes"
      contains: "wincon-map"
    - path: "DeckFlow.Web.Tests/WinConMapSurfaceContractTests.cs"
      provides: "flag-OFF artifact/zip byte-identity (entry-map + per-platform content) + download->upload zip round-trip + deserialize hardening"
    - path: "DeckFlow.Web.Tests/DeckAnalysisWinConMapViewTests.cs"
      provides: "page-level flag-OFF byte-identity via IRazorViewEngine render excision-equality"
      contains: "SplitAroundWinConBlock"
  key_links:
    - from: "DeckPacketController Step-3 action"
      to: "DeckAnalysisViewModel.WinConMap + request.WinConMapJson"
      via: "WinConMap = result.WinConMap mapping + request.WinConMapJson = JsonSerializer.Serialize(result.WinConMap) (mirrors InteractionAudit at 164/341 + 180/357)"
      pattern: "WinConMap = result.WinConMap"
    - from: "DeckAnalysis.cshtml @if (Model.WinConMap is not null)"
      to: "conditional hidden WinConMapJson textarea + 61-wincon-map.json zip entry"
      via: "round-trip of the serialized map between Step-2 and Step-3 + into the zip"
      pattern: "WinConMapJson"
    - from: "PacketArtifactStore.BuildZip / LoadFromZip"
      to: "61-wincon-map.json entry <-> request.WinConMapJson"
      via: "conditional write (dropped when empty -> flag-OFF zip byte-identical) + restore on re-upload"
      pattern: "61-wincon-map.json"
    - from: "DeckAnalysisWinConMapViewTests"
      to: "DeckAnalysis.cshtml rendered output"
      via: "IRazorViewEngine render + prefix/suffix excision-equality between null and populated WinConMap"
      pattern: "SplitAroundWinConBlock"
---

<objective>
Surface the Phase 80 win-condition/combo map onto the /deck-analysis Step-3 ON-PAGE readout (mirroring the
paste-artifact content from Plan 80-02), round-trip the computed map through a hidden `WinConMapJson` field
AND a `61-wincon-map.json` zip entry so Step-3 re-renders and the download/re-upload restores it, and lock
the flag-OFF byte-identity contract for the page + three artifacts + zip. Page byte-identity is proven at
the render layer by a dedicated `IRazorViewEngine` excision-equality test mirroring Phase 79's
`DeckAnalysisInteractionAuditViewTests`; artifact/zip byte-identity is proven by an entry-map + per-platform
content contract test.

Purpose: WINCON-01 requires the on-page readout (not just the artifact); WINCON-03 requires the
data-unavailable disclosure + closing cards on the page; WINCON-04 (ROADMAP success criterion #5 = page + 3
artifacts + zip byte-identical when OFF) requires the RENDERED page AND the zip to be byte-identical when
the flag is OFF and to actually restore the map when ON. This mirrors the Phase-79 interaction-audit
hidden-field + zip round-trip (Request.InteractionAuditJson → ViewModel.InteractionAudit → result record →
view guard + hidden field + 60-interaction-audit.json entry, with TryDeserializeInteractionAudit hardening)
EXACTLY — the win-con map is the next entry (61-wincon-map.json) in the same allow-list.
Output: the hidden-field + zip round-trip plumbing, a flag-guarded readout block + CSS, the deep
untrusted-input deserialize guard, an entry-map/per-platform artifact/zip byte-identity + zip round-trip
contract test, and a page-level Razor render byte-identity test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md
@.planning/phases/79-interaction-answers-audit/79-03-PLAN.md
@.planning/phases/80-win-condition-combo-map/80-02-PLAN.md

<interfaces>
<!-- Existing Phase-79 round-trip chain to mirror (InteractionAudit -> WinConMap). Extracted current from the codebase. -->
From DeckFlow.Web/Models/DeckAnalysisRequest.cs:
  public string ScoreJson { get; set; }                    // line 141
  public string InteractionAuditJson { get; set; }         // line 150 — the null-guard hidden-field pattern to mirror; add WinConMapJson after it
From DeckFlow.Web/Models/DeckAnalysisViewModel.cs:
  public DeckMultiAxisScore? Score { get; init; }          // line 34
  public InteractionAudit? InteractionAudit { get; init; } // line 40 — add WinConMap after it
From DeckFlow.Web/Controllers/DeckPacketController.cs (BOTH Step-3 sites — mirror for WinConMap at each):
  if (result.InteractionAudit is not null) { request.InteractionAuditJson = JsonSerializer.Serialize(result.InteractionAudit); }  // lines 164 + 341 (hidden-field SOURCE write)
  InteractionAudit = result.InteractionAudit,               // lines 180 + 357 (result -> view-model mapping)
  interactionAuditJson: request.InteractionAuditJson);      // BuildZip args, lines 246 + 268
From DeckFlow.Web/Services/DeckAnalysisPacketService.cs:
  InteractionAudit? InteractionAudit = null                 // result record trailing param, line 66 — add WinConMap? WinConMap = null after it
  var savedInteractionAudit = interactionAuditFlagEnabled ? TryDeserializeInteractionAudit(request.InteractionAuditJson) : null;  // step-3 saved path, line 417
  ... InteractionAudit: savedInteractionAudit);             // step-3 return, line 430  (computed return: InteractionAudit: interactionAudit, line 849)
  private static InteractionAudit? TryDeserializeInteractionAudit(string? json)          // line 1431: size-cap + IsStructurallyValid... + catch(JsonException) -> null
  private static bool IsStructurallyValidInteractionAudit(InteractionAudit? audit)       // line 1454: null-guards every bucket + inner list + card Name + Quantity 1..99 + CoverageGaps — MIRROR THIS DEPTH
  private const int MaxInteractionAuditJsonLength = 16384;                                // line 1481
  // NOTE: the WinConMap? winConMap local was added in Plan 80-02 near line 573 and assigned in the flag block — PROMOTE it into the result (no recompute).

<!-- Conditional hidden-field + readout render precedent (byte-identity guard). DeckAnalysis.cshtml. -->
  @if (!string.IsNullOrEmpty(Model.Request.InteractionAuditJson)) { <textarea name="InteractionAuditJson" hidden aria-hidden="true" tabindex="-1">@Model.Request.InteractionAuditJson</textarea> }  // lines 522-526 — mirror for WinConMapJson
  @if (Model.InteractionAudit is not null) { <div class="interaction-audit" role="region" ...> ... }  // lines 581-635 — the flag-guarded readout analog; the wincon readout goes AFTER this, still inside the "Analysis Summary" panel before <div class="stack"> (line 636)

<!-- Zip round-trip precedent. PacketArtifactStore.cs (60-interaction-audit.json is the exact analog; 61-wincon-map.json is the next entry). -->
  private static readonly HashSet<string> PacketAllowedNames = { ... "60-interaction-audit.json" };  // line 40 — add "61-wincon-map.json"
  public static byte[] BuildZip(..., string? interactionAuditJson = null)                 // line 120 — add string? winConMapJson = null trailing optional
  ("60-interaction-audit.json", "INTERACTION AUDIT JSON", string.IsNullOrWhiteSpace(interactionAuditJson) ? null : ExtractJsonObject(interactionAuditJson))  // line 140 — mirror as a CONDITIONAL 61-wincon-map.json section (NormalizeSections drops null -> OFF byte-identical)
  entries.TryGetValue("60-interaction-audit.json", out var interactionAuditJson);          // LoadFromZip line 280
  request.InteractionAuditJson = interactionAuditJson?.Trim() ?? string.Empty;             // line 291 — mirror for WinConMapJson
  // BuildZip call sites: DeckPacketController.cs lines 246 (cached) + 268 (fresh) — add winConMapJson: request.WinConMapJson at both.

<!-- Phase-79 render-level byte-identity guard to mirror EXACTLY for the page test (last task). -->
From DeckFlow.Web.Tests/DeckAnalysisInteractionAuditViewTests.cs:
  RenderAsync(...) full IRazorViewEngine scaffold + NeutralizeAntiforgery + SplitAroundInteractionBlock + the two byte-identity [Fact]s — clone as SplitAroundWinConBlock etc.

<!-- Core model from Plan 80-01. -->
From DeckFlow.Core/Analysis/WinConMap.cs:
  public enum WinConBand { Early, Mid, Late, Unknown }
  public sealed record WinConCombo(IReadOnlyList<string> CardNames, IReadOnlyList<string> Results, int? ManaValueNeeded, int? Popularity, WinConBand Band);
  public sealed record WinConNearCombo(string MissingCard, IReadOnlyList<string> CardsInDeck, IReadOnlyList<string> Results);
  public readonly record struct WinConClosingCard(string Name, int Quantity);
  public sealed record WinConMap(IReadOnlyList<WinConCombo> Combos, IReadOnlyList<WinConNearCombo> NearCombos, int AssemblyPathCount, IReadOnlyList<WinConClosingCard> ClosingCards, bool ComboDataAvailable, WinConBand OverallBand);
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Round-trip plumbing (Request field, ViewModel field, result record, deep deserialize hardening, controller mapping + hidden-field source write)</name>
  <read_first>
    - DeckFlow.Web/Models/DeckAnalysisRequest.cs lines 141-158 (the `ScoreJson` + `InteractionAuditJson` null-guard properties — mirror the InteractionAuditJson pattern EXACTLY for WinConMapJson)
    - DeckFlow.Web/Models/DeckAnalysisViewModel.cs lines 34-40 (Score + InteractionAudit `{ get; init; }` — add `WinConMap { get; init; }` beside)
    - DeckFlow.Web/Services/DeckAnalysisPacketService.cs lines 60-66 (result record trailing params — add `WinConMap? WinConMap = null`); lines 405-430 (step-3 saved-path: interactionAuditFlagEnabled + savedInteractionAudit + return — add the wincon sibling); lines 833-849 (computed-path result construction — add `WinConMap: winConMap`); lines 1431-1481 (TryDeserializeInteractionAudit + IsStructurallyValidInteractionAudit + IsStructurallyValidInteractionBucket + IsStructurallyValidInteractionCard + MaxInteractionAuditJsonLength — mirror the FULL VALIDATION DEPTH)
    - DeckFlow.Web/Controllers/DeckPacketController.cs lines 160-180 (the `if (result.InteractionAudit is not null) { request.InteractionAuditJson = ...; }` source write at 164 + `InteractionAudit = result.InteractionAudit,` mapping at 180) AND the SECOND site at lines 337-357 — add BOTH the serialize-source write and the view-model mapping for WinConMap at every site that handles InteractionAudit
    - CLAUDE.md carve-out: new record/view-model members are `{ get; init; }`, never `{ get; }`, never `required`
  </read_first>
  <action>
    In DeckAnalysisRequest.cs add a `_winConMapJson` backing field (init `string.Empty`) + a public `WinConMapJson`
    property with the same null-guard setter as InteractionAuditJson, placed immediately after InteractionAuditJson.
    In DeckAnalysisViewModel.cs add `public WinConMap? WinConMap { get; init; }` after InteractionAudit (XML-doc: the
    win-condition map computed at Step 2; null when the analysis.wincon-map flag is off). In DeckAnalysisPacketService.cs:
    add `WinConMap? WinConMap = null` as a new trailing optional param on the DeckAnalysisPacketResult record; in the
    computed path (line ~849) add `WinConMap: winConMap` (PROMOTE the local Plan 80-02 already computes — no recompute);
    in the step-3 saved path (lines ~414-430) add
    `var winConMapFlagEnabled = _flagCache is not null && _flagCache.Snapshot().TryGetValue(WinConMapFlag, out var winConMapFlagOn) && winConMapFlagOn;`
    then `var savedWinConMap = winConMapFlagEnabled ? TryDeserializeWinConMap(request.WinConMapJson) : null;` and set
    `WinConMap: savedWinConMap` in the step-3 return. Add `private static WinConMap? TryDeserializeWinConMap(string? winConMapJson)`
    and `private const int MaxWinConMapJsonLength = 32768;` mirroring TryDeserializeInteractionAudit: return null on
    null/blank/oversize, `JsonSerializer.Deserialize<WinConMap>`, delegate to a NEW
    `private static bool IsStructurallyValidWinConMap(WinConMap? map)`, `catch (JsonException) -> null` — NEVER throw. The
    validator MUST mirror IsStructurallyValidInteractionAudit's DEPTH and reject untrusted-input degeneracies the Razor
    readout would render: return false unless map is non-null AND `Combos` is non-null with every `WinConCombo` having a
    non-null `CardNames` list of >=1 non-blank names, a non-null `Results` list where EVERY entry is non-blank, a `ManaValueNeeded` that is null or in 0..30,
    a `Popularity` that is null or >= 0, and a `Band` that is `Enum.IsDefined(typeof(WinConBand), combo.Band)` AND `NearCombos`
    is non-null with every `WinConNearCombo` having a non-blank `MissingCard` and non-null `CardsInDeck` + `Results` lists where EVERY entry is non-blank AND
    `ClosingCards` is non-null with every `WinConClosingCard` having a non-blank `Name` and `Quantity` in 1..99 AND EVERY rendered string in EVERY list is non-blank (no empty/whitespace rows reach the Razor readout) AND
    `AssemblyPathCount == Combos.Count` (reject tampered counts) AND `Enum.IsDefined(typeof(WinConBand), map.OverallBand)` AND each
    list is within a small per-list count cap well under the 32 KB payload cap (e.g. Combos <= 50, NearCombos <= 50, per-combo
    CardNames/Results <= 20, ClosingCards <= 100) so degenerate hidden-field JSON cannot render dozens of empty/low-value rows.
    Use small `private static bool` helpers per shape (mirroring the interaction bucket/card helpers). In
    DeckPacketController.cs at BOTH InteractionAudit sites: add the hidden-field source write
    `if (result.WinConMap is not null) { request.WinConMapJson = JsonSerializer.Serialize(result.WinConMap); }` beside the
    InteractionAuditJson guard, and add `WinConMap = result.WinConMap,` beside each `InteractionAudit = result.InteractionAudit,`
    view-model mapping. Changed-lines only; do not re-indent existing raw-string literals.
  </action>
  <verify>
    <automated>dotnet.exe build DeckFlow.Web 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - DeckAnalysisRequest.cs declares `WinConMapJson` with a null-guarding setter (mirrors InteractionAuditJson)
    - DeckAnalysisViewModel.cs declares `public WinConMap? WinConMap { get; init; }` (init, not get-only, not required)
    - DeckAnalysisPacketResult record has a `WinConMap? WinConMap = null` param; both computed and step-3 paths set it
    - `TryDeserializeWinConMap` size-caps at `MaxWinConMapJsonLength`, calls `IsStructurallyValidWinConMap`, and catches `JsonException` returning null
    - `IsStructurallyValidWinConMap` rejects: null/blank combo card names, ANY blank entry in a combo `Results` / near-combo `CardsInDeck` / near-combo `Results` list, out-of-range ManaValueNeeded/Popularity, an undefined Band enum, null near-combo/closing lists, blank MissingCard/closing Name, out-of-range closing Quantity, ANY list exceeding its per-list count cap, and a mismatched `AssemblyPathCount != Combos.Count` (read confirms every guard incl. `Enum.IsDefined` for both combo Band and OverallBand, non-blank on every rendered string, and per-list count caps)
    - DeckPacketController.cs writes `request.WinConMapJson = JsonSerializer.Serialize(result.WinConMap)` guarded by `if (result.WinConMap is not null)` at BOTH InteractionAudit sites, and `grep -c "WinConMap = result.WinConMap" DeckFlow.Web/Controllers/DeckPacketController.cs` matches the number of `InteractionAudit = result.InteractionAudit` sites (2)
    - `dotnet.exe build DeckFlow.Web` reports 0 errors, 0 warnings
  </acceptance_criteria>
  <done>The map round-trips Request→result→ViewModel, the controller writes the hidden-field source, and untrusted-input deserialization is deeply structurally validated (incl. enum-band + assembly-count integrity); Web builds clean.</done>
</task>

<task type="auto">
  <name>Task 2: On-page readout block + CONDITIONAL hidden round-trip field + site-common.css</name>
  <read_first>
    - DeckFlow.Web/Views/Deck/DeckAnalysis.cshtml lines 522-526 (the CONDITIONAL `@if (!string.IsNullOrEmpty(Model.Request.InteractionAuditJson)) { <textarea name="InteractionAuditJson" hidden ...> }` field — mirror for WinConMapJson) and lines 581-635 (the `@if (Model.InteractionAudit is not null) { <div class="interaction-audit" role="region" ...> ... }` readout region ending before `<div class="stack">` at 636 — the wincon readout goes AFTER it, still inside Analysis Summary)
    - DeckFlow.Web/wwwroot/css/site-common.css (search `.interaction-audit`, `.chatgpt-score` — the soft-card + eyebrow + grid + gaps-callout patterns using existing tokens var(--line), var(--panel-soft-bg), var(--muted), var(--accent-strong), var(--warning); NO new :root tokens were added for interaction-audit)
    - CLAUDE.md theme rule: layout CSS goes in site-common.css only; token ADDITIONS would require editing every site-*.css :root — add NO new tokens
  </read_first>
  <action>
    In DeckAnalysis.cshtml add a CONDITIONAL hidden field mirroring the InteractionAuditJson guard EXACTLY —
    `@if (!string.IsNullOrEmpty(Model.Request.WinConMapJson)) { <textarea name="WinConMapJson" hidden aria-hidden="true" tabindex="-1">@Model.Request.WinConMapJson</textarea> }`
    placed beside the InteractionAuditJson field, with a matching comment ("rendered only when present so the flag-OFF page
    stays byte-identical to baseline"). Do NOT emit the textarea unconditionally (an always-present field breaks page
    byte-identity — the Task-4 excision test asserts `name="WinConMapJson"` is ABSENT on the OFF page). Then add a
    flag-guarded readout region `@if (Model.WinConMap is not null) { ... }` inside the Analysis Summary panel, immediately
    AFTER the interaction-audit region and BEFORE `<div class="stack">`. Wrap it in a single container with a STABLE outer
    class `wincon-map` and surround it with stable, contiguous markup on BOTH sides so the Task-4 render test can split
    cleanly. Render a titled region (role="region", aria-label "Win Condition and Combo Map") with a hedge eyebrow stating
    this is DeckFlow's automated first-pass read the AI CONFIRMS for castability/board/color (never "the deck wins"). Then:
    (1) if `Model.WinConMap.ComboDataAvailable == false`, render a distinct "Combo data unavailable" note (Commander
    Spellbook did not respond — explicitly NOT "no win conditions"); (2) else list the ranked `Combos` (each card names +
    results, with the combo's Band as a hedged "typically comes online @band" label) and state
    `Model.WinConMap.AssemblyPathCount` as "approximately N candidate assembly paths; verify castability"; (3) ALWAYS render a
    near-combos sub-list titled exactly "One card away (not currently a win line)" from `NearCombos` (each MissingCard), the
    overall band as a hedged line (omit when Unknown), and a "Non-combo closers to verify" list from `ClosingCards`. Use Razor
    `@` interpolation for all card names / results (auto-encoded — no `Html.Raw`); prose "approximately / candidate / verify",
    NEVER "the deck wins". Add layout classes to site-common.css only (e.g. `.wincon-map`, `.wincon-map__eyebrow`,
    `.wincon-map-combo`, `.wincon-map-near`, `.wincon-map-band`, `.wincon-map-closers`, `.wincon-map-unavailable`) built from
    EXISTING tokens + baked hex where a theme-invariant value is needed; add NO new `:root` tokens (so no per-theme site-*.css
    edits). Include the responsive `@media` breakpoints matching the interaction-audit block's pattern. Changed-lines only.
  </action>
  <verify>
    <automated>dotnet.exe build DeckFlow.Web 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - DeckAnalysis.cshtml guards the hidden field with `@if (!string.IsNullOrEmpty(Model.Request.WinConMapJson))` (CONDITIONAL, mirroring InteractionAuditJson) — the `name="WinConMapJson"` textarea is NOT emitted unconditionally
    - DeckAnalysis.cshtml contains `@if (Model.WinConMap is not null)` guarding the readout, placed after the interaction-audit region and before `<div class="stack">`
    - The readout renders ranked combos, the "One card away (not currently a win line)" near-combo heading, the assembly-path count, the band, closing cards, AND a distinct branch when `ComboDataAvailable == false`
    - The guarded readout is a single contiguous block with a stable `wincon-map` outer class and stable contiguous markup immediately before/after it (so Task 4's excision split is deterministic)
    - The view uses Razor `@` interpolation for card names (auto-encoded); no `Html.Raw` in the block (grep confirms)
    - site-common.css gains `.wincon-map` layout classes and `grep -c ":root" DeckFlow.Web/wwwroot/css/site-common.css` is unchanged from baseline (no new :root tokens); no site-*.css theme file is modified
    - `dotnet.exe build DeckFlow.Web` reports 0 errors
  </acceptance_criteria>
  <done>The Step-3 readout renders the ranked combos + near-combos + count + band + closing cards + data-unavailable branch behind the flag guard as one contiguous suppressible block, round-trips via a CONDITIONAL hidden field absent when OFF, and styles live only in site-common.css using existing tokens.</done>
</task>

<task type="auto">
  <name>Task 3: Persist WinConMapJson through the zip (61-wincon-map.json writer + loader + controller wiring)</name>
  <read_first>
    - DeckFlow.Web/Services/Persistence/PacketArtifactStore.cs line 40 (`"60-interaction-audit.json"` in PacketAllowedNames — add `"61-wincon-map.json"`); lines 109-140 (BuildZip signature ending `string? interactionAuditJson = null` at 120 + the CONDITIONAL `60-interaction-audit.json` responseSection at 140 using `string.IsNullOrWhiteSpace(...) ? null : ExtractJsonObject(...)` — the EXACT dropped-when-empty precedent); lines 280-291 (LoadFromZip `entries.TryGetValue("60-interaction-audit.json", ...)` + `request.InteractionAuditJson = interactionAuditJson?.Trim() ?? string.Empty` — mirror for WinConMapJson)
    - DeckFlow.Web/Controllers/DeckPacketController.cs lines 246 (cached BuildZip call, `interactionAuditJson: request.InteractionAuditJson`) and 268 (fresh BuildZip call) — pass a serialize-FALLBACK winConMapJson to each (prefer `request.WinConMapJson`; else serialize the site's own result -- `cachedResult.WinConMap` at ~246, `result.WinConMap` at ~268), because a fresh /deck-analysis/download recomputes a WinConMap while the posted `request.WinConMapJson` may be EMPTY; `DeckAnalysisPacketResult.WinConMap` (added in Task 1) is available on BOTH `cachedResult` and `result`
  </read_first>
  <action>
    Add `"61-wincon-map.json"` to `PacketAllowedNames`. Add `string? winConMapJson = null` as a new trailing optional param
    on `BuildZip` (after `interactionAuditJson`). Append a CONDITIONAL responseSection
    `("61-wincon-map.json", "WIN CONDITION MAP JSON", string.IsNullOrWhiteSpace(winConMapJson) ? null : ExtractJsonObject(winConMapJson))`
    to the `responseSections` list — because NormalizeSections drops null/blank sections, when the flag is OFF (empty
    WinConMapJson) NO entry is written and the zip (including all-responses.txt) is byte-identical to baseline; when ON, the
    entry is written and folded into all-responses.txt. In LoadFromZip add
    `entries.TryGetValue("61-wincon-map.json", out var winConMapJson);` and
    `request.WinConMapJson = winConMapJson?.Trim() ?? string.Empty;` (do NOT add it to the required-entry guard — optional
    supplemental state, like the interaction-audit entry). In DeckPacketController.cs, at BOTH download BuildZip call sites compute a
    local `winConMapJson` that PREFERS the round-tripped hidden field but FALLS BACK to serializing the site's own result when it
    is empty, so a fresh `/deck-analysis/download` (which recomputes a WinConMap but may receive an empty posted
    `request.WinConMapJson`) still writes the entry: at the cached site (~235-246)
    `var cachedWinConMapJson = !string.IsNullOrWhiteSpace(request.WinConMapJson) ? request.WinConMapJson : (cachedResult.WinConMap is null ? null : JsonSerializer.Serialize(cachedResult.WinConMap));`
    and pass `winConMapJson: cachedWinConMapJson`; at the fresh site (~257-268) the same with `result.WinConMap` ->
    `winConMapJson: freshWinConMapJson`. (`JsonSerializer` is already used in this controller.) Changed-lines only.
  </action>
  <verify>
    <automated>dotnet.exe build DeckFlow.Web 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "61-wincon-map.json" DeckFlow.Web/Services/Persistence/PacketArtifactStore.cs` returns >= 3 (allowed-name, BuildZip section, LoadFromZip TryGetValue)
    - BuildZip gains a trailing `string? winConMapJson = null` param and writes the section CONDITIONALLY (`string.IsNullOrWhiteSpace(...) ? null : ...`) so an empty value produces no entry
    - LoadFromZip restores `request.WinConMapJson` from the `61-wincon-map.json` entry and does NOT add it to the required-entry guard
    - Both BuildZip call sites in DeckPacketController.cs pass a serialize-FALLBACK `winConMapJson` (prefers `request.WinConMapJson`, else serializes the site's own `cachedResult.WinConMap` / `result.WinConMap`) so a fresh download with an empty posted field still emits the entry (grep confirms `JsonSerializer.Serialize(...WinConMap)` at the download sites)
    - A test exercises the DOWNLOAD path (controller `DeckAnalysisDownload`) with the flag ON and an EMPTY posted `request.WinConMapJson`, opens the returned zip, and asserts it CONTAINS `61-wincon-map.json` (the fresh-download entry is not dropped)
    - `dotnet.exe build DeckFlow.Web` reports 0 errors
  </acceptance_criteria>
  <done>The map is persisted as a conditional 61-wincon-map.json zip entry (dropped when OFF → byte-identical) and restored to request.WinConMapJson on re-upload; a fresh download with an empty posted field still emits the entry via the serialize-fallback; Web builds clean.</done>
</task>

<task type="auto">
  <name>Task 4: Per-surface flag-OFF artifact/zip byte-identity + zip round-trip contract test + hardening + live UI smoke</name>
  <read_first>
    - DeckFlow.Web.Tests/InteractionAuditSurfaceContractTests.cs (READ IN FULL — the EXACT template: per-platform flag-OFF prompt byte-identity `[Theory]`, zip entry-map + content `[Fact]`, download→upload round-trip `[Fact]`, and the deserialize-hardening `[Theory]`; clone its flag-cache-fake / BuildAsync setup verbatim, swapping InteractionAudit→WinConMap sentinels)
    - DeckFlow.Web/Services/Persistence/PacketArtifactStore.cs (BuildZip → open with System.IO.Compression.ZipArchive to read the entry-name set + entry text; LoadFromZip for the round-trip restore)
    - DeckFlow.Web/Services/DeckAnalysisPacketService.cs (BuildAsync flag-OFF path — with the flag off, winConMap/WinConMap is null, request.WinConMapJson stays empty, the artifact block is null; the combo gate is unchanged so no extra fetch)
    - scripts/run-web-test.sh + CLAUDE.md testing note (start headless web with DECKFLOW_DISABLE_AUTO_BROWSER=true; drive live checks with `npx --no-install playwright test`; never open a Windows-host browser; CI is the authoritative gate)
  </read_first>
  <action>
    Create DeckFlow.Web.Tests/WinConMapSurfaceContractTests.cs mirroring InteractionAuditSurfaceContractTests:
    (1) PROMPT byte-identity, ALL THREE platforms: a `[Theory]` over TargetAiPlatform ChatGpt/Claude/Gemini asserting that
    with the wincon-map flag OFF, `BuildAsync` yields `result.WinConMap is null` AND the analysis prompt text
    `Assert.DoesNotContain` the win-con block sentinel (per-platform artifact equals the pre-feature null-block path).
    (2) ZIP entry-map byte-identity: with the flag OFF (empty `request.WinConMapJson`), call `PacketArtifactStore.BuildZip(...)`,
    open the bytes as a ZipArchive, and assert the entry-name set does NOT contain `"61-wincon-map.json"` and that
    `31-analysis-prompt.txt` + `all-responses.txt` contents carry no win-con sentinel. Then with a non-empty valid
    `request.WinConMapJson` assert the built zip DOES contain a `"61-wincon-map.json"` entry. ADDITIONALLY exercise the FRESH
    download path THROUGH THE CONTROLLER: with the flag ON and an EMPTY posted `request.WinConMapJson`, invoke
    `DeckPacketController.DeckAnalysisDownload` (mirror the controller harness in DeckPacketControllerTests.cs), open the returned
    zip, and assert it CONTAINS `61-wincon-map.json` -- proving the serialize-fallback keeps a freshly-computed map in the
    fresh-download zip (not only via a direct `PacketArtifactStore.BuildZip` call).
    (3) ZIP download→upload round-trip: build a zip with a valid serialized `WinConMapJson`, then
    `PacketArtifactStore.LoadFromZip(zipStream, freshRequest)` and assert `freshRequest.WinConMapJson` equals the original
    JSON AND it re-materializes a non-null `WinConMap` (via the Step-3 saved path or a direct TryDeserialize seam) —
    proving the map survives the zip round-trip (ROADMAP criterion #5 "round-trip restores").
    (4) hardening: oversize (`> MaxWinConMapJsonLength`), malformed JSON, a combo with a blank card name, a blank combo `Results` entry, a near-combo
    with a blank `CardsInDeck`/`Results` entry, a list that exceeds its per-list count cap, a null NearCombos/
    ClosingCards list, an undefined `Band` enum value (e.g. 99), an out-of-range closing `Quantity`, and a tampered
    `AssemblyPathCount != Combos.Count` each deserialize to null and never throw.
    Use the existing flag-cache fake / snapshot seams the interaction-audit tests use. Extend the existing Playwright
    deck-analysis spec so that, with the flag ON, the Step-3 `wincon-map` readout region is visible at desktop (1280) and
    mobile (390) widths and, with the flag OFF, the region is absent — run against the headless server started via
    scripts/run-web-test.sh. NOTE: this Playwright selector check is a live-UX smoke, NOT the page byte-identity proof (that
    is Task 5's Razor render test). Changed-lines only.
  </action>
  <verify>
    <automated>dotnet.exe test DeckFlow.Web.Tests --filter "FullyQualifiedName~WinConMapSurfaceContract" 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - A `[Theory]` over ChatGpt/Claude/Gemini asserts flag-OFF `BuildAsync` result `WinConMap is null` and the per-platform artifact prompt has no win-con block header
    - A `[Fact]` BUILDS the zip with the flag OFF, opens it, and asserts the entry-name set excludes `61-wincon-map.json` and that `31-analysis-prompt.txt` + `all-responses.txt` carry no win-con sentinel; a companion assertion builds the ON zip and finds the `61-wincon-map.json` entry
    - A `[Fact]` drives the controller `DeckAnalysisDownload` FRESH path with the flag ON and an EMPTY posted `request.WinConMapJson` and asserts the returned zip contains `61-wincon-map.json` (fresh-download entry not dropped)
    - A `[Fact]` builds a zip with valid `WinConMapJson`, calls `LoadFromZip`, and asserts `request.WinConMapJson` restored + re-materializes a non-null `WinConMap` (download→upload round-trip fidelity)
    - A `[Fact]`/`[Theory]` asserts oversize, malformed, blank-card-name, blank-combo-Results-entry, blank-near-combo-entry, over-cap-list, null-list, undefined-Band-enum, out-of-range-Quantity, and mismatched-AssemblyPathCount `WinConMapJson` each yield null and do not throw
    - The Playwright deck-analysis spec asserts the `wincon-map` readout visible (flag ON) at 1280 + 390 and absent (flag OFF) — a live-UX smoke, explicitly NOT the byte-identity proof
    - `dotnet.exe test DeckFlow.Web.Tests --filter "FullyQualifiedName~WinConMapSurfaceContract"` exits 0
    - CI (GitHub Actions) green after push — authoritative gate
  </acceptance_criteria>
  <done>The flag-OFF artifact + zip byte-identity is proven by an actual zip build + entry-map/per-platform content comparison, the download→upload zip round-trip restores the map, the deep untrusted-input hardening (incl. enum + assembly-count integrity) is proven, and live UI smoke is verified at desktop + mobile.</done>
</task>

<task type="auto">
  <name>Task 5: Page-level flag-OFF byte-identity Razor render test (Phase-79 excision-equality)</name>
  <read_first>
    - DeckFlow.Web.Tests/DeckAnalysisInteractionAuditViewTests.cs (READ IN FULL — the EXACT precedent to mirror: the IRazorViewEngine `RenderAsync` scaffold, `BuildModel`, `NeutralizeAntiforgery`, `SplitAroundInteractionBlock`, `CreateHostingEnvironment`/`TestWebHostEnvironment`/`StubTempDataProvider`/`FakeFeatureFlagCache`, and the two byte-identity `[Fact]`s. Copy the whole scaffold + helper types VERBATIM; only swap the model-shaping bits InteractionAudit→WinConMap and the split anchors)
    - DeckFlow.Web/Views/Deck/DeckAnalysis.cshtml (the `@if (Model.WinConMap is not null)` readout region + CONDITIONAL WinConMapJson field added in Task 2 — identify the STABLE literal markup immediately BEFORE and AFTER that region for the two split anchors; the outer `wincon-map` class is a reliable middle sentinel)
    - DeckFlow.Web/Models/DeckAnalysisViewModel.cs + DeckAnalysisRequest.cs (the WinConMap + WinConMapJson members from Task 1 — set both on the test's model/request so the hidden field lives in the shared prefix)
  </read_first>
  <action>
    Create DeckFlow.Web.Tests/DeckAnalysisWinConMapViewTests.cs as a dedicated `IRazorViewEngine` render guard mirroring
    DeckAnalysisInteractionAuditViewTests.cs EXACTLY. Copy the `RenderAsync` ServiceCollection/FindView/StringWriter scaffold,
    the hosting/tempdata/flag-cache helpers, and `NeutralizeAntiforgery` verbatim (only rename the model-shaping bits so
    `BuildModel` sets `WinConMap` and `Request.WinConMapJson` instead of InteractionAudit/InteractionAuditJson). Provide:
    (1) `WinConMapNull_RendersNoWinConMarkup`: render with `WinConMap: null` and `WinConMapJson: string.Empty` and BOTH
    `Assert.DoesNotContain("wincon-map", html, StringComparison.Ordinal)` AND
    `Assert.DoesNotContain("name=\"WinConMapJson\"", html, StringComparison.Ordinal)` (the CONDITIONAL hidden field absent
    on the OFF page).
    (2) `WinConMapNull_MarkupEqualsPopulatedMinusWinConBlock`: render TWICE with an IDENTICAL Request (same NON-EMPTY
    `WinConMapJson` hidden field so it sits in the shared prefix, same AnalysisResponse) — once `WinConMap: null`, once
    `WinConMap: BuildWinConMap()`; `NeutralizeAntiforgery` both; `SplitAroundWinConBlock` each around the two stable anchors;
    `Assert.Equal(nullPrefix, populatedPrefix)`, `Assert.Equal(nullSuffix, populatedSuffix)`,
    `Assert.True(string.IsNullOrWhiteSpace(nullMiddle))`, and `Assert.Contains("wincon-map-combo", populatedMiddle, StringComparison.Ordinal)`.
    Add a `SplitAroundWinConBlock` mirroring `SplitAroundInteractionBlock` (two stable anchors), a `BuildWinConMap()` returning
    a populated `WinConMap` (>=1 ranked WinConCombo with a Band, >=1 WinConNearCombo, AssemblyPathCount == Combos.Count, >=1
    WinConClosingCard, ComboDataAvailable true, a non-Unknown OverallBand), and a `FixedWinConMapJson` round-trip constant
    serialized from it. Optionally add `WinConMapUnavailable_RendersDataUnavailableNote` asserting the ComboDataAvailable=false
    branch renders the distinct disclosure. This Razor render test — NOT Task 4's Playwright check — satisfies ROADMAP
    criterion #5's page byte-identity gate. Deterministic, CI-safe, no browser. Changed-lines only; LF endings preserved.
  </action>
  <verify>
    <automated>dotnet.exe test DeckFlow.Web.Tests --filter "FullyQualifiedName~DeckAnalysisWinConMapView" 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - DeckFlow.Web.Tests/DeckAnalysisWinConMapViewTests.cs exists and renders DeckAnalysis.cshtml through `IRazorViewEngine` (mirrors DeckAnalysisInteractionAuditViewTests.cs; no browser)
    - `WinConMapNull_RendersNoWinConMarkup` asserts `Assert.DoesNotContain("wincon-map", html, StringComparison.Ordinal)` AND `Assert.DoesNotContain("name=\"WinConMapJson\"", html, ...)` on the flag-OFF render
    - `WinConMapNull_MarkupEqualsPopulatedMinusWinConBlock` performs the excision-equality assertion: `Assert.Equal` on prefix AND suffix, `Assert.True(string.IsNullOrWhiteSpace(nullMiddle))`, and a populated-middle sentinel `Assert.Contains`
    - A `SplitAroundWinConBlock` helper mirrors `SplitAroundInteractionBlock` (two stable view anchors); `NeutralizeAntiforgery` reused
    - `dotnet.exe test DeckFlow.Web.Tests --filter "FullyQualifiedName~DeckAnalysisWinConMapView"` exits 0
    - CI (GitHub Actions) green after push — authoritative gate
  </acceptance_criteria>
  <done>The rendered /deck-analysis page is proven byte-identical to baseline when the flag is OFF (readout region AND conditional hidden field absent) via a deterministic Razor IRazorViewEngine excision-equality test mirroring Phase 79 — the page-layer half of the flag-OFF byte-identity gate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| client → WinConMapJson hidden field | Round-tripped serialized map is UNTRUSTED client input re-POSTed on Step-3 and into the download endpoint |
| client → 61-wincon-map.json zip entry | Re-uploaded zip entry is UNTRUSTED; restored into request.WinConMapJson then re-validated by TryDeserializeWinConMap |
| feature-flag store → controller/view | The flag gates whether the readout region + conditional hidden field + zip entry render/emit |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-80-03-01 | Tampering | WinConMapJson deserialization (hidden field + zip entry) | mitigate | `TryDeserializeWinConMap` size-caps at MaxWinConMapJsonLength, then `IsStructurallyValidWinConMap` validates every combo (card names, MV/popularity ranges, `Enum.IsDefined` Band), near-combo, closing card (Name + Quantity 1..99), the OverallBand enum, and `AssemblyPathCount == Combos.Count`, plus every rendered string (combo/near-combo `Results`, near-combo `CardsInDeck`) non-blank and per-list count caps; catches JsonException → null; never throws, never reflects/evals |
| T-80-03-02 | Injection (XSS) | combo/near-combo/closing card names rendered into HTML readout | mitigate | Razor `@` auto-encodes all interpolated card names / results; no `Html.Raw` on map data (acceptance criterion asserts absence) |
| T-80-03-03 | Denial of Service | oversize round-tripped payload (field or zip entry) | mitigate | 32 KB cap rejects oversize WinConMapJson before parse; the zip loader inherits ReadEntries' per-entry + total caps; map is not recomputed on the round-trip path |
| T-80-03-04 | Tampering | flag-OFF byte-identity regression (page + artifact + zip) | mitigate | Three-layer proof: Task 4 WinConMapSurfaceContractTests BUILDS the zip and asserts the entry-map excludes 61-wincon-map.json + per-platform prompt content carries no block when OFF; Task 5 DeckAnalysisWinConMapViewTests renders the real view and asserts prefix/suffix excision-equality (readout AND conditional hidden field absent when OFF); the conditional hidden-field guard keeps the OFF page free of extra bytes |
| T-80-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages (REQUIREMENTS.md zero-new-deps) |
</threat_model>

<verification>
- `dotnet.exe build DeckFlow.Web` clean (0/0)
- WinConMapSurfaceContract test filter GREEN (artifact/zip entry-map + per-platform byte-identity + zip download→upload round-trip + deep hardening)
- DeckAnalysisWinConMapView test filter GREEN (page render excision-equality — the page byte-identity gate; conditional hidden field absent when OFF)
- Playwright deck-analysis spec GREEN at 1280 + 390 (live-UX smoke)
- CI (GitHub Actions) green after push — authoritative gate (WSL VSTest unreliable)
- Changed-lines format gate clean; no new :root tokens; no theme site-*.css modified; LF endings preserved
- Manual confirmation: flag OFF ⇒ readout + hidden field absent + zip has no 61-wincon-map.json + prompt files byte-identical to baseline; flag ON ⇒ readout matches the paste artifact content and the zip round-trips
</verification>

<success_criteria>
The Step-3 on-page readout mirrors the paste-artifact win-condition map (ranked combos + near-combos + assembly
count + band + closing cards + data-unavailable disclosure, hedged), round-trips via a hardened untrusted
`WinConMapJson` hidden field (CONDITIONAL — absent when OFF) AND a conditional `61-wincon-map.json` zip entry that
LoadFromZip restores, and with the flag OFF the page + three artifacts + zip are byte-identical to baseline —
proven at the artifact/zip layer by an entry-map + per-platform content contract test (with a download→upload
round-trip fidelity assertion) AND at the page layer by a Razor IRazorViewEngine render excision-equality test,
with a desktop+mobile Playwright live-UX smoke; new CSS lives only in site-common.css with no new theme tokens.
</success_criteria>

<output>
Create `.planning/phases/80-win-condition-combo-map/80-03-SUMMARY.md` when done
</output>
</content>
</invoke>
