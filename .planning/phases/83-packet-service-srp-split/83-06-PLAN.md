---
phase: 83-packet-service-srp-split
plan: 06
type: execute
wave: 2
depends_on: ["83-01", "83-02", "83-03"]
files_modified:
  - DeckFlow.Web/Services/DeckAnalysisPacketService.cs
  - DeckFlow.Web.Tests/DeckAnalysisPacketServiceTests.cs
autonomous: true
requirements: [PKTSVC-01, PKTSVC-02, PKTSVC-03, PKTSVC-04]
must_haves:
  truths:
    - "DeckAnalysisPacketService delegates Scryfall resolution to ScryfallReferenceResolver (SearchPrintingFallback strategy, normalize ON) and holds no private batch loop or private Chunk."
    - "It delegates sectioned decklist text (includeVersions + oracleNameMap, Possible-Includes stay plain) to PacketTextAssembler, passing its OWN NormalizeSingleLine as the AppendKeyValueLine normalizer delegate (Analysis NormalizeSingleLine is KEPT — the prompt-variant files call it and it is not byte-equivalent to JsonTextFormatterService's)."
    - "Its analysis + set-upgrade paste artifacts are byte-identical pre/post refactor across all 3 AI variants with every prompt-mutating flag ON and OFF."
  artifacts:
    - path: "DeckFlow.Web/Services/DeckAnalysisPacketService.cs"
      provides: "Analysis orchestration shell delegating resolution + decklist assembly to shared collaborators"
      contains: "ScryfallReferenceResolver"
  key_links:
    - from: "DeckFlow.Web/Services/DeckAnalysisPacketService.cs"
      to: "DeckFlow.Web/Services/Packets/ScryfallReferenceResolver.cs"
      via: "instantiated from the already-injected IScryfallCardResolver, SearchPrintingFallback strategy + normalize ON"
      pattern: "new ScryfallReferenceResolver"
---

<objective>
Migrate DeckAnalysisPacketService (2372 LOC — the largest) onto the shared resolver + assembler,
reducing it to an orchestration shell — gated by the 83-01 byte-identity harness across the full
flag matrix. Run LAST among the resolver-consumers because it has the most flag interactions and the
richest resolution path.

Purpose: Remove Analysis's private batch loop + Chunk, and route
its sectioned decklist through PacketTextAssembler — while PRESERVING its private NormalizeSingleLine
(H3: NOT byte-equivalent to JsonTextFormatterService's; still called by the Analysis/SetUpgrade
prompt-variant files — keep verbatim), its partner-aware commander
reflag, its all-printings fallback + NormalizeForScryfall pre-normalization, its full
NormalizeOracleText, and its combo formatter (all divergent, keep).
Output: A materially smaller DeckAnalysisPacketService whose Analysis byte-identity suite still
passes for all 3 platforms x every flag ON/OFF.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/83-packet-service-srp-split/83-RESEARCH.md
@CLAUDE.md

<do_not_unify>
- Keep Analysis's partner-aware commander reflag (`ResolvePreScryfallCommanderState` :229-286 and
  BuildAsync :610-616) — it reflags EVERY inferred-commander name (partner pairs), NOT the first
  match. Do NOT route it through DeckEntryReflagHelper (that would break partner decks).
- Keep `NormalizeOracleText` (:2106-2154, full: name/mana/type/oracle/PT per face) and
  `CollapseWhitespace` (:2156-2161) as-is (Cluster G).
- Keep `BuildComboReferenceText` (:1145-1189) as-is (Cluster F).
- Keep `BuildCanonicalDeckSourceText` (:322-338) as-is (Cluster C — cache-key text).
- Do NOT move/relax the round-tripped-JSON validation guards (IsStructurallyValid* / MaxScoreJsonLength
  et al.) — they are unrelated to the two collaborators and must stay verbatim.
</do_not_unify>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delegate Scryfall resolution to ScryfallReferenceResolver (all-printings + normalize ON)</name>
  <files>DeckFlow.Web/Services/DeckAnalysisPacketService.cs, DeckFlow.Web.Tests/DeckAnalysisPacketServiceTests.cs</files>
  <read_first>
    - DeckFlow.Web/Services/DeckAnalysisPacketService.cs:1950-2043 (LookupCardReferencesAsync), :2163-2176 (Chunk), :2005 (SearchPrintingFallback path), :1965 (NormalizeForScryfall), :2299-2326 (ExtractMechanicNames), :2367 (CardReference record shape)
    - DeckFlow.Web/Services/Packets/ScryfallReferenceResolver.cs (ResolveBatchAsync + ScryfallBatchResolution)
    - DeckFlow.Web.Tests/DeckAnalysisByteIdentityTests.cs (from 83-01 — the gate, full flag matrix)
    - DeckFlow.Web.Tests/DeckAnalysisPacketServiceTests.cs (CreateService seam :1846-1911)
  </read_first>
  <action>
    In the DeckAnalysisPacketService constructor(s) (DI + internal test ctor), instantiate
    `new ScryfallReferenceResolver(cardResolver)` from the already-injected IScryfallCardResolver;
    store in a private readonly field. Replace the batch-chunk-collect core of
    `LookupCardReferencesAsync` with `ResolveBatchAsync(names, SearchPrintingFallbackCardAsync
    delegate, normalizeForScryfall: true, ct)`, then keep Analysis's OWN post-processing verbatim,
    driven off the returned references (keyed by ORIGINAL request name, in original order, each
    carrying the FromFallback flag): the 9-field `CardReference` construction
    (IsMdfcLand/ReleasedAt/Quantity/IsCommander etc.), the fallback-only `displayName`
    (`submitted_name: ... | resolved_card: ...`) branch that keys off FromFallback + NormalizeLookupName,
    and the separate `ExtractMechanicNames` HashSet. Delete the private `Chunk`. Do NOT change the DI
    extension file. Do NOT change which fallback method is used or the normalize behavior.

    Add a DeckAnalysisPacketServiceTests case asserting the resolved CardReference set (incl.
    ReleasedAt/IsMdfcLand recovered via printing search) is unchanged for a collection-miss fixture.
  </action>
  <verify>
    <automated>dotnet.exe test DeckFlow.Web.Tests --filter "FullyQualifiedName~DeckAnalysis" 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - Build 0/0.
    - DeckAnalysisByteIdentityTests (83-01) PASS across all 3 platforms and every flag ON/OFF.
    - No private Chunk / inline batch-chunk-collect loop remain (grep confirms).
    - CardReference construction + ExtractMechanicNames + SearchPrintingFallback + NormalizeForScryfall behavior preserved.
    - DI extension file unchanged.
  </acceptance_criteria>
  <done>Analysis resolves via the shared resolver (all-printings + normalize ON); byte-identity gate green across the full flag matrix.</done>
</task>

<task type="auto">
  <name>Task 2: Delegate sectioned decklist + migrate NormalizeSingleLine; confirm shell reduction</name>
  <files>DeckFlow.Web/Services/DeckAnalysisPacketService.cs, DeckFlow.Web.Tests/DeckAnalysisPacketServiceTests.cs</files>
  <read_first>
    - DeckFlow.Web/Services/DeckAnalysisPacketService.cs:1053-1139 (BuildDecklistText + FormatDecklistLine, includeVersions + oracleNameMap), :2178-2179 (private NormalizeSingleLine), :2232-2264 (BuildRequestContextText)
    - DeckFlow.Web/Services/Packets/PacketTextAssembler.cs (BuildSectionedDecklistText + AppendKeyValueLine)
    - DeckFlow.Web/Services/JsonTextFormatterService.cs:34 (JsonTextFormatter's NormalizeSingleLine — reference only; NOT byte-equivalent to Analysis's, do NOT adopt it)
  </read_first>
  <action>
    Replace Analysis's `BuildDecklistText`/`FormatDecklistLine` calls with
    `PacketTextAssembler.BuildSectionedDecklistText(entries, possibleIncludes, includeVersions:true,
    oracleNameMap: <the existing map>)` (the assembler reproduces the Possible-Includes-stay-plain
    asymmetry); delete the now-unused private builders.

    H3 — KEEP `DeckAnalysisPacketService.NormalizeSingleLine` (:2178-2179) EXACTLY AS-IS. Do NOT delete
    it and do NOT replace it with `JsonTextFormatterService.NormalizeSingleLine` — they are NOT
    byte-equivalent (Analysis's delegates to its CollapseWhitespace; the JsonTextFormatter one only
    replaces `\n`/`\r`), and the Analysis + SetUpgrade prompt-variant files call
    `DeckAnalysisPacketService.NormalizeSingleLine(...)` directly (deleting it breaks their compile and
    changes bytes). When routing `BuildRequestContextText`'s per-field lines through
    `PacketTextAssembler.AppendKeyValueLine`, pass Analysis's OWN `NormalizeSingleLine` as the
    `Func<string?, string, string>` normalizer delegate. Keep Analysis's field list/order. Leave
    Cluster C/F/G methods and the
    partner-aware reflag untouched.
  </action>
  <verify>
    <automated>dotnet.exe test DeckFlow.Web.Tests --filter "FullyQualifiedName~DeckAnalysis" 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - Build 0/0; full DeckAnalysis test set + DeckAnalysisByteIdentityTests pass (3 platforms x flags ON/OFF).
    - Private BuildDecklistText/FormatDecklistLine/Chunk are gone (grep confirms).
    - `DeckAnalysisPacketService.NormalizeSingleLine` REMAINS (still `internal static`, still called by the Analysis/SetUpgrade prompt-variant files); it was NOT deleted or swapped for JsonTextFormatterService's version.
    - Partner-aware reflag, NormalizeOracleText, BuildComboReferenceText, BuildCanonicalDeckSourceText, and the JSON-validation guards remain (Cluster G/F/C + guards untouched).
    - DeckAnalysisPacketService is materially smaller: the extracted mechanical clusters (batch loop, Chunk, sectioned-decklist builder) no longer live in the service. NOTE (W1): 'no service materially larger than its collaborators' (ROADMAP criterion 3) is satisfied by DUPLICATION REMOVED — the mechanical clusters are gone — NOT by literal LOC parity; DeckAnalysisPacketService legitimately stays large (~2000 LOC) because its correctly-fenced divergent clusters (partner reflag, NormalizeOracleText, combo formatter, cache-key text, JSON-validation guards, per-flag prompt blocks) are intentionally NOT extractable. Verification must read criterion 3 this way and not dispute correct work on raw line count.
  </acceptance_criteria>
  <done>DeckAnalysisPacketService is an orchestration shell over the shared collaborators; artifacts byte-identical across the full flag matrix.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → Analysis request model | DeckAnalysisRequest validation + round-tripped-JSON guards (ScoreJson/InteractionAuditJson/WinConMapJson) are untouched; refactor re-routes internal call graph only. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-83-10 | Tampering | folding Analysis's partner-aware reflag onto the first-match helper, breaking partner decks | mitigate | do_not_unify fence keeps the partner reflag inline; byte-identity gate (companion/partner fixture) confirms. |
| T-83-11 | Tampering | a refactor accidentally dropping an IsStructurallyValid*/Max*JsonLength guard | mitigate | Guards are explicitly out of scope for both collaborators; acceptance criterion requires them to remain verbatim. |
| T-83-12 | Tampering | dropped blank line or wrong flag-block ordering changing AnalysisPromptText bytes | mitigate | DeckAnalysisByteIdentityTests asserts Ordinal equality across 3 platforms x every flag ON/OFF after each task. |
</threat_model>

<verification>
- `dotnet.exe test DeckFlow.Web.Tests --filter "FullyQualifiedName~DeckAnalysis"` → all pass (incl. byte-identity, full flag matrix).
- Grep: no private Chunk/BuildDecklistText/FormatDecklistLine in DeckAnalysisPacketService (NormalizeSingleLine intentionally REMAINS — H3).
</verification>

<success_criteria>
DeckAnalysisPacketService delegates resolution + decklist assembly to the shared collaborators,
retains no duplicate resolution path, keeps its divergent reflag/oracle/combo/cache-key/validation
logic, and its analysis + set-upgrade artifacts are byte-identical across all 3 AI variants with
every prompt-mutating flag ON and OFF (PKTSVC-01/02/03; gated by PKTSVC-04).
</success_criteria>

<output>
Create `.planning/phases/83-packet-service-srp-split/83-06-SUMMARY.md` when done.
</output>
