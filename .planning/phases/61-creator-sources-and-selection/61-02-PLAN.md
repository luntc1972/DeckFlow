---
phase: 61-creator-sources-and-selection
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - DeckFlow.Studio/Pages/CreatorSources.razor
  - DeckFlow.Studio/Pages/Harvest.razor
  - DeckFlow.Studio/Shared/NavMenu.razor
  - DeckFlow.Studio/Program.cs
  - DeckFlow.Studio.Tests/CreatorSourcesPageTests.cs
  - DeckFlow.Studio.Tests/HarvestPageTests.cs
  - README.md
autonomous: true
requirements: [SRC-01, SRC-02]
must_haves:
  truths:
    - "Operator can add a creator (display name + channel ref), see it listed, and remove it on the CreatorSources page; the list persists across Studio restarts"
    - "Harvest page shows a creator dropdown populated from the saved list; selecting a creator sets the browse target without pasting a URL"
    - "The paste-URL channel input remains available as a one-off fallback when no creator is selected"
    - "CreatorSourceStore is registered as a singleton over content-kb.db and EnsureSchemaAsync runs at startup, mirroring BlockedVideoStore"
    - "Nav exposes the CreatorSources page"
  artifacts:
    - path: "DeckFlow.Studio/Pages/CreatorSources.razor"
      provides: "Add/view/remove curated creators (SRC-01)"
      contains: "@page \"/creators\""
    - path: "DeckFlow.Studio/Pages/Harvest.razor"
      provides: "Creator dropdown above the channel-URL input (SRC-02), URL paste retained as fallback"
      contains: "creator"
    - path: "DeckFlow.Studio/Program.cs"
      provides: "ICreatorSourceStore singleton + EnsureSchema at startup"
      contains: "CreatorSourceStore"
    - path: "DeckFlow.Studio.Tests/CreatorSourcesPageTests.cs"
      provides: "bUnit: add renders row, remove drops it, empty-state"
      contains: "CreatorSources"
  key_links:
    - from: "DeckFlow.Studio/Pages/Harvest.razor"
      to: "DeckFlow.Core.Content.ICreatorSourceStore"
      via: "dropdown bound to ListAsync; selection feeds the existing browse target"
      pattern: "ICreatorSourceStore"
---

<objective>
Deliver SRC-01 + SRC-02: a `CreatorSources.razor` management page (add / view / remove curated
creators, persisted via the Plan-01 `CreatorSourceStore`) and a creator dropdown on `Harvest.razor`
that lets the operator pick a saved creator instead of pasting a channel URL — with the existing
paste-URL input retained as a one-off fallback. Wire DI + nav, update README, cover with bUnit.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/61-creator-sources-and-selection/61-CONTEXT.md
@./CLAUDE.md
@DeckFlow.Studio/Pages/Blocked.razor
@DeckFlow.Studio/Pages/Harvest.razor
@DeckFlow.Studio/Shared/NavMenu.razor
@DeckFlow.Studio/Program.cs
</context>

<tasks>
1. Register `ICreatorSourceStore -> CreatorSourceStore(contentKbDatabasePath)` as a singleton in
   `Program.cs` and call `EnsureSchemaAsync()` at startup beside the other content-kb stores.
2. `CreatorSources.razor` (`/creators`): a form to add (display name + channel ref, validated
   non-empty), a table of saved creators with a Remove button each, and an empty-state. Sanitized
   error/success feedback. Reuse the visual conventions of `Blocked.razor`.
3. `Harvest.razor` browse section: add a creator `<select>` above the channel-URL input, populated
   from `ListAsync()`. Selecting a creator sets the browse target (its channel_ref) used by
   `BrowseChannelAsync`; a "— paste a URL instead —" option / empty selection keeps the existing
   paste-URL path as the fallback. Do not remove the URL input.
4. Nav: add a `NavLink` to `/creators` in `NavMenu.razor`.
5. README: document the curated-creator list + dropdown in the Studio section.
6. bUnit: `CreatorSourcesPageTests` (add renders a row; remove drops it; empty-state). Extend
   `HarvestPageTests` to assert the dropdown renders saved creators and that selection populates the
   browse target while URL paste still works. Use a fake/temp store.
7. Build Studio + run Studio.Tests (PullFromProd/Harvest/CreatorSources green; note the known
   BlockedPageTests isolation flake).
</tasks>

## Trust Boundaries
Creator dropdown values originate from operator-entered channel_refs (Plan 01). They feed the
existing YouTube browse path — same trust as today's pasted URL. No new external surface.

## STRIDE Threat Register
- **Tampering (T-61-04):** a creator_ref edited on disk flows into browse — same as a pasted URL
  today; existing browse validation applies. Mitigation: reuse the existing channel-input validation
  path; do not bypass it for dropdown-sourced refs.
- **DoS (T-61-05):** an absurd creator list is operator-self-inflicted on a local tool; out of scope.
