---
phase: 20-content-kb-ingestion-transcription-local
plan: 04
type: execute
wave: 3
depends_on: ["20-01", "20-02", "20-03"]
files_modified:
  - DeckFlow.CLI/Program.cs
  - DeckFlow.CLI/CommandRunners.cs
  - DeckFlow.Core/Integration/YouTubeTranscriptSource.cs
  - DeckFlow.Core/Integration/IYouTubeChannelVideoLister.cs
  - DeckFlow.Core/Integration/YouTubeChannelVideoLister.cs
  - DeckFlow.Core.Tests/YouTubeTranscriptSourceTests.cs
autonomous: false
requirements: [KB-03, KB-04, KB-05]
notes:
  - "RunHarvestAsync is the SINGLE persistence owner (D-11): it writes content_videos, content_transcripts, transcript_status, AND the whisper_spend_ledger row (on Whisper success only). The Core/Integration services it composes persist nothing."
  - "DURATION + MONTH-KEY WIRING (Codex iter2 NEW-HIGH + MEDIUM): the verb derives ONE monthKey per video (yyyy-MM) and passes the authoritative video.Duration AND that monthKey into FetchTranscriptAsync; YouTubeTranscriptSource forwards both into whisper.TranscribeAsync; the verb reuses the SAME monthKey for RecordCallAsync so cap-check month == ledger month and the cap projection never sees a 0 duration for a real video."
user_setup:
  - service: openai
    why: "Whisper transcription fallback for caption-less videos (KB-04)"
    env_vars:
      - name: OPENAI_API_KEY
        source: "OpenAI Dashboard → API keys (local environment only; never committed)"
  - service: ffmpeg
    why: "Chunk audio >24MB before Whisper upload (D-05 local prerequisite)"
    dashboard_config:
      - task: "Install ffmpeg and put it on PATH (absent → videos >24MB marked failed, run continues)"
        location: "Local machine PATH"
must_haves:
  truths:
    - "A single CLI invocation (harvest) reads enabled content_sources, lists recent videos per source (bounded), fetches captions first, downloads audio + falls back to Whisper only when captions are absent and the cap allows, and is the SINGLE writer of all persistence (D-11)"
    - "YouTubeTranscriptSource implements ITranscriptSource as a PURE composition: captions → audio-download → Whisper fallback → non-null TranscriptFetchResult; persists nothing; forwards the authoritative knownDuration AND the verb-supplied monthKey into whisper.TranscribeAsync"
    - "Channel video listing uses YoutubeExplode CollectAsync(limit) (NOT .Take().ToListAsync — no async-LINQ package) so a run never drains an entire channel history (P5)"
    - "A rerun resumes pending/failed videos via GetVideoByYoutubeIdAsync instead of blanket-skipping by youtube_video_id (MEDIUM-d)"
    - "The harvester verifies ffmpeg presence at start and warns (does not abort) when absent (P7)"
    - "The harvest verb maps TranscriptOutcome directly to a transcript_status (Captions→captions, Whisper→whisper, Failed→failed, SkippedOverCap→skipped_over_cap) — never collapsing skipped/failed (HIGH-3)"
    - "The verb derives ONE monthKey per video and passes it (with the authoritative video.Duration) into FetchTranscriptAsync; on Whisper success it writes exactly one whisper_spend_ledger row using that SAME monthKey — cap-check month == ledger month (Codex iter2 MEDIUM); on cap-skip/failure it writes none"
    - "The authoritative video.Duration from the lister flows through FetchTranscriptAsync → whisper.TranscribeAsync so a caption-less video is never projected at $0 and transcribed free (Codex iter2 NEW-HIGH — KB-05)"
    - "Per fetch the verb logs transcript_source (captions|whisper) AND caption_track_kind (manual|auto_generated) (MEDIUM-e)"
    - "A local UAT over the 5 seeded MTG channels (2 videos/channel) reports per-channel AND aggregate whisper_fallback_ratio < 25% (SC2/P2)"
  artifacts:
    - path: "DeckFlow.Core/Integration/YouTubeTranscriptSource.cs"
      provides: "Pure ITranscriptSource composing caption fetcher + audio source + Whisper (no persistence); forwards knownDuration + monthKey to whisper"
      contains: "ITranscriptSource"
    - path: "DeckFlow.CLI/CommandRunners.cs"
      provides: "RunHarvestAsync — single persistence owner; one monthKey per video for cap-check + ledger; ledger write on Whisper success only"
      contains: "RunHarvestAsync"
    - path: "DeckFlow.Core/Integration/YouTubeChannelVideoLister.cs"
      provides: "CollectAsync(limit) recent-video lister (P5 bounded, no async-LINQ)"
      contains: "CollectAsync"
  key_links:
    - from: "DeckFlow.Core/Integration/YouTubeTranscriptSource.cs"
      to: "IYouTubeTranscriptFetcher + IYouTubeAudioSource + IWhisperTranscriptionService"
      via: "captions-first → audio download → Whisper fallback (forwards knownDuration + monthKey; returns TranscriptFetchResult)"
      pattern: "TranscribeAsync"
    - from: "DeckFlow.CLI/CommandRunners.cs"
      to: "DeckFlow.Core/Content (ContentVideoStore + WhisperSpendLedger)"
      via: "InsertVideoAsync/InsertTranscriptAsync/UpdateTranscriptStatusAsync + RecordCallAsync on Whisper success with the per-video monthKey"
      pattern: "RecordCallAsync"
    - from: "DeckFlow.CLI/CommandRunners.cs"
      to: "GetVideoByYoutubeIdAsync (resume)"
      via: "lookup existing video → resume pending/failed instead of skip"
      pattern: "GetVideoByYoutubeIdAsync"
    - from: "DeckFlow.CLI/CommandRunners.cs"
      to: "ffmpeg presence warning"
      via: "IFfmpegAudioChunker.IsAvailableAsync at harvest start"
      pattern: "IsAvailableAsync"
---

<objective>
Compose the Plan 20-02/20-03 PURE ingestion services into the `harvest` CLI verb (the SINGLE persistence owner per D-11) and the `YouTubeTranscriptSource` (the first `ITranscriptSource` implementation, also pure), then prove KB-03's P1/P2 success criteria with a local UAT over the 5 seeded MTG channels. This is the phase boundary: captions/transcripts fetched + persisted, ledger rows written on Whisper success, cap gating Whisper. (Phase 21 layers distillation/artifact-emit on top of `RunHarvestAsync`.)

Purpose: SC1 (5 real third-party channels via YoutubeExplode, not Google.Apis), SC2 (proxy-pluggable provider factory + per-channel/aggregate whisper_fallback_ratio < 25% + transcript_source/caption_track_kind log). The orchestration here is the ingestion-only slice: list videos (bounded via CollectAsync) → resume-or-insert content_videos → derive one monthKey → captions → audio-download → Whisper fallback (cap-gated with the authoritative duration) → map outcome → persist transcript + status + ledger (ledger under the same monthKey). Phase 21 owns summary/clips/tags/artifact emit/run-record.
Output: `YouTubeTranscriptSource` (pure ITranscriptSource, forwards knownDuration + monthKey); bounded channel video lister (CollectAsync); `harvest` CLI verb + `RunHarvestAsync` (single persistence owner; one monthKey per video); the 5-channel UAT checkpoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-content-kb-ingestion-transcription-local/20-CONTEXT.md
@.planning/phases/20-content-kb-ingestion-transcription-local/20-RESEARCH.md
@.planning/phases/20-content-kb-ingestion-transcription-local/20-01-PLAN.md
@.planning/phases/20-content-kb-ingestion-transcription-local/20-02-PLAN.md
@.planning/phases/20-content-kb-ingestion-transcription-local/20-03-PLAN.md

NOTE: PLANS SUPERSEDE 20-RESEARCH.md where they differ. Specifically: channel listing uses `CollectAsync(limit)` (RESEARCH P5/Anti-Patterns still show `.Take().ToListAsync()` — DO NOT use it; no async-LINQ package); missing ffmpeg warns+marks-failed (never throws); the result type is the non-null `TranscriptFetchResult`. RESEARCH carries a top supersede note.

<interfaces>
<!-- From Plan 20-02 (all PURE — persist nothing) -->
- `ITranscriptSource { string SourceType { get; } Task<TranscriptFetchResult> FetchTranscriptAsync(string naturalKey, TimeSpan? knownDuration, string monthKey, CancellationToken) }` (NON-NULL result; knownDuration authoritative for the cap; monthKey verb-supplied)
- `enum TranscriptOutcome { Captions, Whisper, Failed, SkippedOverCap }`
- `record TranscriptFetchResult { TranscriptOutcome Outcome; string? Body; string? Source; string? FailureReason; bool? IsAutoGenerated; int? SecondsBilled; decimal? CostUsd; }` + factories FromCaptions/FromWhisper(body,secondsBilled,costUsd)/Failed/SkippedOverCap. SecondsBilled/CostUsd set ONLY on Outcome==Whisper — the verb writes the ledger row from these.
- `IYouTubeTranscriptFetcher { Task<YouTubeCaptionResult> FetchCaptionsAsync(string videoId, CancellationToken) }`; `YouTubeCaptionResult { HasCaptions; Body; IsAutoGenerated; LanguageCode }`
- `TranscriptProviderFactory.Resolve(string? providerEnvValue, HttpClient) → IYouTubeTranscriptFetcher` (direct wired; unsupported value throws)
- `IYouTubeAudioSource { Task<AudioDownloadResult> DownloadAudioAsync(string videoUrlOrId, CancellationToken) }`; `record AudioDownloadResult : IDisposable { TempFilePath; FileName; SizeBytes; DurationSeconds (best-effort, may be 0); }`

<!-- From Plan 20-03 (PURE — persists nothing; returns cost + monthKey) -->
- `IWhisperTranscriptionService { Task<WhisperTranscriptionResult> TranscribeAsync(AudioDownloadResult audio, TimeSpan? knownDuration, string monthKey, CancellationToken) }` (knownDuration authoritative for the cap projection; monthKey used for the cap check and echoed back)
- `record WhisperTranscriptionResult { TranscriptOutcome Outcome; int SecondsBilled; decimal CostUsd; string MonthKey; string? Body; string? FailureReason; }`
- `IFfmpegAudioChunker { Task<bool> IsAvailableAsync(CancellationToken); Task<IReadOnlyList<string>> ChunkAsync(...) }`

<!-- From Phase 19 + Plan 20-01 stores (the verb writes these — D-11) -->
- `ContentSourceStore.ListEnabledSourcesAsync(CancellationToken)` → IReadOnlyList<ContentSource> { Id, SourceSlug, DisplayName, SourceType, SourceUrl, IsEnabled }
- `ContentVideoStore.InsertVideoAsync(sourceId, youtubeVideoId, rssGuid, title, videoUrl, publishedUtc, transcriptStatus, ct)` → long; initial status TranscriptStatus.Pending
- `ContentVideoStore.GetVideoByYoutubeIdAsync(sourceId, youtubeVideoId, ct)` → existing row (id + status) or null (Plan 20-01 — resume)
- `ContentVideoStore.InsertTranscriptAsync(videoId, source, body, ct)` / `UpdateTranscriptStatusAsync(videoId, status, ct)` (Plan 20-01)
- `WhisperSpendLedger.RecordCallAsync(videoId, secondsBilled, costUsd, monthKey, ct)` — the verb calls this ONCE on a Whisper-success result with the SAME monthKey it passed into FetchTranscriptAsync (D-11 + Codex iter2 MEDIUM)

<!-- YoutubeExplode 6.6.0 (RESEARCH + Tyrrrz README) -->
- `youtube.Channels.GetUploadsAsync(channelUrl)` → IAsyncEnumerable<PlaylistVideo>; bound with `.CollectAsync(limit)` (NO async-LINQ package — HIGH-6) — returns IReadOnlyList<PlaylistVideo>
- `PlaylistVideo` has `.Id` (VideoId; `.Value` is the string), `.Url`, `.Title`, `.Duration` (TimeSpan?) — use `.Url` (or `.Id.Value`) for downstream caption/audio calls (Open Q3 resolution); `.Duration` feeds the cap projection as the AUTHORITATIVE knownDuration

<!-- CLI host (DeckFlow.CLI/Program.cs) -->
- flat command registration on rootCommand; handler form `Environment.ExitCode = CommandRunners.RunXAsync(...).GetAwaiter().GetResult();`
- artifacts DB path convention: artifacts/content-kb.db
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: YouTubeChannelVideoLister (CollectAsync-bounded) + pure YouTubeTranscriptSource (forwards knownDuration + monthKey)</name>
  <files>DeckFlow.Core/Integration/IYouTubeChannelVideoLister.cs, DeckFlow.Core/Integration/YouTubeChannelVideoLister.cs, DeckFlow.Core/Integration/YouTubeTranscriptSource.cs, DeckFlow.Core.Tests/YouTubeTranscriptSourceTests.cs</files>
  <read_first>
    - DeckFlow.Core/Integration/YouTubeTranscriptFetcher.cs (Plan 20-02 — FetchCaptionsAsync + Func<> ctor seam to mirror)
    - DeckFlow.Core/Integration/IYouTubeAudioSource.cs (Plan 20-02 — DownloadAudioAsync + AudioDownloadResult)
    - DeckFlow.Core/Integration/IWhisperTranscriptionService.cs + WhisperTranscriptionService.cs (Plan 20-03 — TranscribeAsync(AudioDownloadResult, knownDuration, monthKey); the fallback path)
    - DeckFlow.Core/Integration/ITranscriptSource.cs (Plan 20-02 — the non-null TranscriptFetchResult contract + TranscriptOutcome + factories; FetchTranscriptAsync carries knownDuration + monthKey)
    - .planning/phases/20-content-kb-ingestion-transcription-local/20-RESEARCH.md Pitfall P5 (CollectAsync bound — NOTE the plan SUPERSEDES the `.Take().ToListAsync()` shown there), Open Question 3 (PlaylistVideo.Url)
  </read_first>
  <action>
    Create `IYouTubeChannelVideoLister` exposing `Task<IReadOnlyList<YouTubeChannelVideo>> ListRecentAsync(string channelUrl, int limit, CancellationToken ct = default)` and `record YouTubeChannelVideo { get; init; }` (VideoId, Url, Title, Duration (TimeSpan?), PublishedUtc?). Implement `YouTubeChannelVideoLister` over `youtube.Channels.GetUploadsAsync(channelUrl)` bounded with `.CollectAsync(limit)` (Codex HIGH-6 — YoutubeExplode's own collector; NEVER `.Take(limit).ToListAsync()`, which needs an async-LINQ package this project does not have); HttpClient ctor-injected + a Func<> override seam for tests. Map each PlaylistVideo to a YouTubeChannelVideo (`.Id.Value`, `.Url`, `.Title`, `.Duration`).

    Create `YouTubeTranscriptSource : ITranscriptSource` — PURE (D-11), persists nothing. `SourceType => ContentSourceType.Youtube`; ctor deps `IYouTubeTranscriptFetcher fetcher, IYouTubeAudioSource audioSource, IWhisperTranscriptionService whisper`. `FetchTranscriptAsync(naturalKey, knownDuration, monthKey, ct)`:
    1. `var captions = await fetcher.FetchCaptionsAsync(naturalKey, ct)`; if `captions.HasCaptions` return `TranscriptFetchResult.FromCaptions(captions.Body!, captions.IsAutoGenerated)` (Outcome=Captions, Source=captions). The caller persists.
    2. Else (no captions) download audio + run Whisper: `using var audio = await audioSource.DownloadAudioAsync(naturalKey, ct)` (the AudioDownloadResult is IDisposable — `using` cleans the temp file in a `finally`). `var w = await whisper.TranscribeAsync(audio, knownDuration, monthKey, ct)` — FORWARD the authoritative knownDuration AND the verb-supplied monthKey (Codex iter2 NEW-HIGH + MEDIUM: the cap projection uses knownDuration so a 0-duration audio stream cannot bypass the cap, and the monthKey makes the cap-check month match the verb's ledger month). Map `w.Outcome` to a non-null TranscriptFetchResult using the 20-02 factories — `TranscriptFetchResult` already carries `int? SecondsBilled` and `decimal? CostUsd` (defined in 20-02 Task 1):
       - `TranscriptOutcome.Whisper` → `TranscriptFetchResult.FromWhisper(w.Body!, w.SecondsBilled, w.CostUsd)` (populates SecondsBilled/CostUsd so the verb writes the ledger row).
       - `TranscriptOutcome.SkippedOverCap` → `TranscriptFetchResult.SkippedOverCap()` (NOT Failed — HIGH-3).
       - `TranscriptOutcome.Failed` → `TranscriptFetchResult.Failed(w.FailureReason ?? "whisper failed")`.
       Never collapse SkippedOverCap into Failed. Keep methods ≤30 lines.

    Add `YouTubeTranscriptSourceTests` via the Func<> seams of injected fetcher/audio/whisper fakes: (a) captions present → Outcome=Captions, Source=captions, fetcher hit, audioSource + whisper NOT invoked; (b) no captions + whisper success → Outcome=Whisper, Body set, SecondsBilled/CostUsd surfaced from the whisper result, audio downloaded then disposed, AND assert the fake whisper received the SAME knownDuration and monthKey the source was called with (Codex iter2 wiring proof); (c) no captions + whisper SkippedOverCap → Outcome=SkippedOverCap (NOT Failed); (d) no captions + whisper Failed → Outcome=Failed with FailureReason. Assert the source writes to no store (pure). No live HTTP.
  </action>
  <verify>
    <automated>"/mnt/c/Program Files/dotnet/dotnet.exe" build DeckFlow.Core/DeckFlow.Core.csproj -c Debug 2>&1 | grep -E "Build succeeded|error"; "/mnt/c/Program Files/dotnet/dotnet.exe" test DeckFlow.Core.Tests/DeckFlow.Core.Tests.csproj --filter "FullyQualifiedName~YouTubeTranscriptSourceTests" 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c ": ITranscriptSource" DeckFlow.Core/Integration/YouTubeTranscriptSource.cs` ≥1
    - `grep -c "CollectAsync(" DeckFlow.Core/Integration/YouTubeChannelVideoLister.cs` ≥1 AND `grep -c "ToListAsync" DeckFlow.Core/Integration/YouTubeChannelVideoLister.cs` == 0 (HIGH-6 — no async-LINQ)
    - `grep -c "TranscribeAsync(" DeckFlow.Core/Integration/YouTubeTranscriptSource.cs` ≥1 AND that call passes both knownDuration and monthKey through (Codex iter2 NEW-HIGH + MEDIUM) — proven by test (b) asserting the fake whisper received the same knownDuration + monthKey
    - `grep -ci "ContentVideoStore\|WhisperSpendLedger\|InsertTranscript\|RecordCallAsync" DeckFlow.Core/Integration/YouTubeTranscriptSource.cs` == 0 (D-11 — source is pure)
    - captions-present test proves audioSource.DownloadAudioAsync and whisper.TranscribeAsync were NOT invoked (captions-first, free path)
    - skipped-over-cap test proves Outcome==SkippedOverCap (distinct from Failed — HIGH-3)
    - Build clean; YouTubeTranscriptSourceTests pass (or build-clean fallback per WSL VSTest caveat)
  </acceptance_criteria>
  <done>YouTubeTranscriptSource composes captions-first → audio-download → Whisper-fallback behind ITranscriptSource as a pure (no-persistence) service mapping to non-null distinct outcomes and forwarding the authoritative knownDuration + verb-supplied monthKey to whisper; channel listing is bounded by CollectAsync; covered by seam tests including the duration/monthKey forwarding proof.</done>
</task>

<task type="auto">
  <name>Task 2: harvest CLI verb + RunHarvestAsync (single persistence owner; one monthKey per video)</name>
  <files>DeckFlow.CLI/Program.cs, DeckFlow.CLI/CommandRunners.cs</files>
  <read_first>
    - DeckFlow.CLI/Program.cs (flat command registration; SetHandler GetAwaiter().GetResult())
    - DeckFlow.CLI/CommandRunners.cs lines 375-415 (RunArchidektCacheAsync — Serilog logger + artifacts/ DB path convention to mirror)
    - DeckFlow.Core/Content/ContentSourceStore.cs (ListEnabledSourcesAsync) + ContentVideoStore.cs (InsertVideoAsync pending + GetVideoByYoutubeIdAsync + InsertTranscriptAsync + UpdateTranscriptStatusAsync from 20-01) + WhisperSpendLedger.cs (RecordCallAsync)
    - DeckFlow.Core/Integration/YouTubeTranscriptSource.cs (Task 1) + YouTubeChannelVideoLister.cs + TranscriptProviderFactory.cs (20-02) + FfmpegAudioChunker.cs (IsAvailableAsync, 20-03)
    - .planning/phases/20-content-kb-ingestion-transcription-local/20-RESEARCH.md Pattern 5 (CLI wiring) + P7 (ffmpeg warn-not-abort)
  </read_first>
  <action>
    Register a flat `harvest` command on rootCommand. Options: `--db` (Option&lt;FileInfo?&gt; default artifacts/content-kb.db), `--limit` (Option&lt;int&gt; default 5 — recent videos per source, bounds P5). Add `public static async Task<int> RunHarvestAsync(FileInfo? db, int limit, Serilog.ILogger logger, CancellationToken ct)`.

    Compose the dependency graph by hand (CLI has no DI container): build `HttpClient` instances (one for YouTube caption/audio, one with `Timeout = TimeSpan.FromMinutes(15)` set HERE in the composition root for Whisper). The `new HttpClient()` instances created in `RunHarvestAsync` are the CLI composition root — the project anti-pattern ban on `new HttpClient()` applies ONLY to `DeckFlow.Core/Integration/*.cs` service method bodies, NOT to `DeckFlow.CLI/CommandRunners.cs`. Resolve the caption fetcher via `TranscriptProviderFactory.Resolve(Environment.GetEnvironmentVariable("DECKFLOW_YOUTUBE_TRANSCRIPT_PROVIDER"), youtubeHttpClient)` (SC2 — real provider toggle; an unsupported env value throws a clear error before any work). Construct `YouTubeAudioSource`, `FfmpegAudioChunker`, `WhisperTranscriptionService` (with a WhisperSpendLedger over the resolved DB for the cap READ; the chunker; the 15min Whisper HttpClient), `YouTubeChannelVideoLister`, and `YouTubeTranscriptSource(fetcher, audioSource, whisper)`. Also resolve a `ContentSourceStore`, `ContentVideoStore`, and `WhisperSpendLedger` over the DB — these are the verb's PERSISTENCE handles (D-11: the verb is the single writer).

    At start: call `chunker.IsAvailableAsync` and if false, `logger.Warning("ffmpeg not found on PATH — audio >24MB will be marked failed.")` and CONTINUE (P7 — do NOT abort).

    Flow: `ListEnabledSourcesAsync`; for each youtube_channel source, `lister.ListRecentAsync(source.SourceUrl, limit, ct)`; for each video:
    - RESUME-OR-INSERT (Codex MEDIUM-d): `var existing = await videoStore.GetVideoByYoutubeIdAsync(source.Id, video.VideoId, ct)`. If existing != null and its status is `captions` or `whisper` (terminal-success) → skip (already harvested, log "already harvested"). If existing != null with status `pending`/`failed`/`skipped_over_cap` → reuse its id and RESUME (re-fetch). If null → `videoId = await videoStore.InsertVideoAsync(source.Id, video.VideoId, null, video.Title, video.Url, video.PublishedUtc, TranscriptStatus.Pending, ct)`.
    - DERIVE ONE MONTHKEY (Codex iter2 MEDIUM): `var monthKey = DateTime.UtcNow.ToString("yyyy-MM");` ONCE here, before FetchTranscriptAsync. This same `monthKey` local is used for the cap check (passed through FetchTranscriptAsync → whisper) AND for the ledger write below, so the cap-check month and the ledger month are identical even if the long transcribe crosses a UTC month boundary.
    - `var result = await transcriptSource.FetchTranscriptAsync(video.VideoId, video.Duration, monthKey, ct)` (non-null) — pass the AUTHORITATIVE `video.Duration` (Codex iter2 NEW-HIGH — so a caption-less video is never projected at $0) AND the per-video `monthKey`.
    - MAP OUTCOME DIRECTLY (Codex HIGH-3 — never `?? Failed`): switch on `result.Outcome`:
      - Captions → `InsertTranscriptAsync(videoId, TranscriptSource.Captions, result.Body!, ct)`; `UpdateTranscriptStatusAsync(videoId, TranscriptStatus.Captions, ct)`.
      - Whisper → `InsertTranscriptAsync(videoId, TranscriptSource.Whisper, result.Body!, ct)`; `UpdateTranscriptStatusAsync(videoId, TranscriptStatus.Whisper, ct)`; and WRITE THE LEDGER ROW (D-11) using the SAME per-video monthKey (Codex iter2 MEDIUM): `await ledger.RecordCallAsync(videoId, result.SecondsBilled!.Value, result.CostUsd!.Value, monthKey, ct)` — exactly one row, only on Whisper success, recorded under the identical month the cap was checked against. DO NOT recompute `DateTime.UtcNow.ToString("yyyy-MM")` at the RecordCallAsync site — reuse the `monthKey` local.
      - SkippedOverCap → `UpdateTranscriptStatusAsync(videoId, TranscriptStatus.SkippedOverCap, ct)` (NO ledger row, NO transcript row).
      - Failed → `UpdateTranscriptStatusAsync(videoId, TranscriptStatus.Failed, ct)` (NO ledger row).
    - SPLIT LOGGING (Codex MEDIUM-e): emit a structured log per fetch with BOTH `transcript_source` (captions|whisper, from result.Source) AND `caption_track_kind` (manual|auto_generated, derived from result.IsAutoGenerated when Outcome==Captions; omit/null for whisper). Example template: `logger.Information("harvested {VideoId} transcript_source={TranscriptSource} caption_track_kind={CaptionTrackKind} outcome={Outcome}", ...)`.
    - Wrap per-video work in try/catch so one bad video never aborts the run (on exception: `UpdateTranscriptStatusAsync(videoId, TranscriptStatus.Failed, ct)` + log + continue).

    Tally per-source captions-count vs whisper-count AND a run-wide total; at end log per-channel AND aggregate `whisper_fallback_ratio` (whisper / (captions+whisper), guard divide-by-zero). Scope note in a code comment: distillation/artifact-emit/run-record are Phase 21 — RunHarvestAsync here stops at transcript+ledger persistence. Wire SetHandler with GetAwaiter().GetResult(). No new packages.
  </action>
  <verify>
    <automated>"/mnt/c/Program Files/dotnet/dotnet.exe" build DeckFlow.CLI/DeckFlow.CLI.csproj -c Debug 2>&1 | grep -E "Build succeeded|error"; "/mnt/c/Program Files/dotnet/dotnet.exe" run --project DeckFlow.CLI -- harvest --help 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '"harvest"' DeckFlow.CLI/Program.cs` ≥1; `grep -c "RunHarvestAsync" DeckFlow.CLI/CommandRunners.cs` ≥1
    - `grep -c "RecordCallAsync" DeckFlow.CLI/CommandRunners.cs` ≥1 (the VERB writes the ledger — D-11) AND it is on the Whisper-success branch only
    - One monthKey per video (Codex iter2 MEDIUM): `grep -c 'ToString("yyyy-MM")' DeckFlow.CLI/CommandRunners.cs` == 1 (derived ONCE per video and reused for both FetchTranscriptAsync and RecordCallAsync — NOT recomputed at the ledger-write site) AND the `monthKey` local is passed into both FetchTranscriptAsync and RecordCallAsync
    - The verb passes `video.Duration` into FetchTranscriptAsync (Codex iter2 NEW-HIGH): `grep -c "video.Duration\|\.Duration" DeckFlow.CLI/CommandRunners.cs` ≥1 on the FetchTranscriptAsync call
    - `grep -c "GetVideoByYoutubeIdAsync" DeckFlow.CLI/CommandRunners.cs` ≥1 (resume — MEDIUM-d)
    - `grep -c "TranscriptProviderFactory" DeckFlow.CLI/CommandRunners.cs` ≥1 (SC2 real toggle resolved at composition)
    - `grep -c "caption_track_kind" DeckFlow.CLI/CommandRunners.cs` ≥1 AND `grep -c "transcript_source" DeckFlow.CLI/CommandRunners.cs` ≥1 (split logging — MEDIUM-e)
    - `grep -c "IsAvailableAsync" DeckFlow.CLI/CommandRunners.cs` ≥1 (ffmpeg start-check) AND the absent path logs a warning and continues (no early return/throw on missing ffmpeg)
    - `grep -c "whisper_fallback_ratio" DeckFlow.CLI/CommandRunners.cs` ≥1 (per-channel + aggregate ratio for SC2)
    - `grep -c "FromMinutes(15)" DeckFlow.CLI/CommandRunners.cs` ≥1 (Whisper HttpClient.Timeout owned by the composition root, not the service ctor)
    - No `?? TranscriptStatus.Failed` collapse on the outcome map (HIGH-3): outcome is switched explicitly; `grep -c "?? TranscriptStatus.Failed\|?? \"failed\"" DeckFlow.CLI/CommandRunners.cs` == 0
    - `harvest --help` lists --db and --limit; build clean
    - The CLI `new HttpClient()` in RunHarvestAsync is EXPECTED (composition root); do NOT gate it to 0 — the `new HttpClient()==0` grep gates apply to DeckFlow.Core/Integration/*.cs only
    - git diff touches only the Program.cs registration block + the new RunHarvestAsync method
  </acceptance_criteria>
  <done>harvest verb reads enabled sources, lists CollectAsync-bounded recent videos, resumes pending/failed via GetVideoByYoutubeIdAsync, derives one monthKey per video (used for both the cap check and the ledger write), passes the authoritative video.Duration through to the cap projection, runs captions-first/Whisper-fallback per video, is the single writer of transcript+status+ledger (ledger on Whisper success only, under the per-video monthKey), maps outcomes directly (no skipped→failed collapse), splits transcript_source/caption_track_kind logging, warns on missing ffmpeg without aborting, and logs per-channel + aggregate fallback ratio.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: 5-channel local caption-coverage UAT (SC1 + SC2)</name>
  <files>artifacts/uat-content-kb.db (runtime UAT DB — not committed)</files>
  <action>HUMAN-VERIFY checkpoint (no code change). The operator runs the seeded harvest over the 5 real MTG channels from the local environment and confirms SC1 (third-party captions fetched via YoutubeExplode, no Google.Apis 403) and SC2 (transcript_source + caption_track_kind logged per fetch + per-channel AND aggregate whisper_fallback_ratio < 0.25). Exact commands are in how-to-verify below. ffmpeg is optional here (captions path needs none); a missing-ffmpeg warning must not abort the run (P7).</action>
  <verify><human-check>Operator confirms captions fetched for the majority of the 5 channels and aggregate whisper_fallback_ratio < 0.25 in the CLI log, reporting both per-channel and aggregate ratios.</human-check></verify>
  <done>Operator types "approved" with the observed per-channel + aggregate whisper_fallback_ratio; SC1 + SC2 confirmed from the local environment.</done>
  <what-built>
    The `harvest` CLI verb + pure `YouTubeTranscriptSource` now fetch captions from the local environment via YoutubeExplode 6.6.0 over the 5 seeded MTG channels and persist via the single-owner verb. Plan 20-01's `content-source-add` seeds the sources.
  </what-built>
  <how-to-verify>
    Run from the repo root (requires internet; OPENAI_API_KEY only needed if any video lacks captions):
    1. Seed the 5 channels (idempotent) into a UAT DB:
       `dotnet run --project DeckFlow.CLI -- content-source-add --name "MTGGoldfish" --url https://www.youtube.com/@MTGGoldfish --type youtube_channel --db artifacts/uat-content-kb.db`
       repeat for: The Command Zone (https://www.youtube.com/@TheCommandZone), EDHRECast (https://www.youtube.com/@EDHRECast), Tolarian Community College (https://www.youtube.com/@TolarianCommunityCollege), Playing With Power (https://www.youtube.com/@PlayingwithPowerMTG)
    2. Run a bounded harvest with 2 videos per channel (Codex MEDIUM-f — 2/channel is more robust to shorts/live/premieres than 1): `dotnet run --project DeckFlow.CLI -- harvest --db artifacts/uat-content-kb.db --limit 2`  (≈10 videos)
    3. Confirm in the CLI log / logs/cli-*.log:
       - captions fetched for the majority of the 5 channels via YoutubeExplode (SC1 — third-party caption fetch works; no Google.Apis 403)
       - both a `transcript_source` (captions|whisper) AND a `caption_track_kind` (manual|auto_generated) value are logged per fetch (SC2 / MEDIUM-e)
       - the run prints a per-channel `whisper_fallback_ratio` for each of the 5 channels AND an aggregate run-wide ratio, and the aggregate is `< 0.25` (SC2/P2)
       - if ffmpeg is absent, a single warning was logged and the run did NOT abort (P7)
    4. (Optional) inspect artifacts/uat-content-kb.db: content_transcripts rows have source='captions' for captioned videos; whisper_spend_ledger has a row ONLY for any video that actually fell through to Whisper success (none if all captioned), and any such row's month_key matches the run month.
  </how-to-verify>
  <resume-signal>Type "approved" with the observed per-channel + aggregate whisper_fallback_ratio, or describe which channels failed caption fetch / what error appeared.</resume-signal>
</task>

</tasks>

<verification>
- SC1 (P1): captions fetched for the 5 real third-party MTG channels via YoutubeExplode 6.6.0; Google.Apis.YouTube never used (grep across phase = 0).
- SC2 (P2): provider factory real (20-02) + transcript_source/caption_track_kind logged + per-channel/aggregate whisper_fallback_ratio < 25% asserted in UAT.
- KB-03/KB-04/KB-05 exercised end-to-end through the harvest verb (caption path always; Whisper path when captions absent + cap allows; cap-skip + failed mapped distinctly).
- D-11: the verb is the single persistence owner (transcript + status + ledger-on-Whisper-success); all composed services persist nothing.
- Duration + month-key (Codex iter2): the verb passes the authoritative video.Duration through to the cap projection so caption-less videos cannot bypass the cap at $0; one monthKey per video drives both the cap check and the ledger row.
- P5 bound (CollectAsync(limit), no async-LINQ) and P7 (ffmpeg warn-not-abort) honored; reruns resume pending/failed via GetVideoByYoutubeIdAsync.
</verification>

<success_criteria>
- One CLI invocation harvests captions/transcripts for the enabled sources and persists them locally with correct status; the ledger gets a row only on real Whisper success, under the same monthKey the cap was checked against.
- The 5-channel UAT confirms per-channel + aggregate whisper_fallback_ratio < 25% with transcript_source + caption_track_kind logged per fetch.
- Nothing runs on Render; no new packages beyond the two approved.
</success_criteria>

<output>
Create `.planning/phases/20-content-kb-ingestion-transcription-local/20-04-SUMMARY.md` when done.
</output>
