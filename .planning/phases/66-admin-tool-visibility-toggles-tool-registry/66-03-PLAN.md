---
phase: 66-admin-tool-visibility-toggles-tool-registry
plan: 03
type: execute
wave: 2
depends_on: [66-01]
files_modified:
  - DeckFlow.Web/Views/Shared/_DeckToolTabs.cshtml
  - DeckFlow.Web/Views/Deck/Home.cshtml
  - DeckFlow.Web/Views/Shared/_ToolTileIcon.cshtml
  - DeckFlow.Web.Tests/Tools/DeckToolTabsViewTests.cs
  - DeckFlow.Web.Tests/Tools/HomeTilesViewTests.cs
autonomous: true
requirements: [TOGGLE-03, TOGGLE-05]
must_haves:
  truths:
    - "The nav strip renders only enabled tools, grouped by section, iterating the registry — no hardcoded per-tool @if remains."
    - "A nav section whose tools are all disabled renders neither its trigger button nor its dropdown."
    - "The home tile grid renders only enabled tools from the registry; a disabled tool's tile disappears."
    - "The categories 'Suggestions offline' muted nav span and the 'Temporarily offline' home placeholder card are deleted."
  artifacts:
    - path: "DeckFlow.Web/Views/Shared/_DeckToolTabs.cshtml"
      provides: "Registry-driven nav with section collapse"
      contains: "ToolVisibility"
    - path: "DeckFlow.Web/Views/Deck/Home.cshtml"
      provides: "Registry-driven home tiles"
      contains: "ToolVisibility"
    - path: "DeckFlow.Web/Views/Shared/_ToolTileIcon.cshtml"
      provides: "Inline SVG icon partial keyed by tool icon key"
  key_links:
    - from: "_DeckToolTabs.cshtml"
      to: "IToolRegistry + IFeatureFlagCache"
      via: "ToolVisibility.VisibleBySection"
      pattern: "VisibleBySection"
---

<objective>
Replace the hardcoded per-tool `@if` blocks in the nav strip and home hub with registry-driven loops,
so disabling a tool removes its nav tab and home tile, and an all-disabled section collapses entirely.

Purpose: TOGGLE-03 (nav + tile hide with the tool) and TOGGLE-05 (empty section collapses). Decision 3
(hide everywhere; drop the categories "offline" placeholders — no muted span, no status card).
Output: rewritten `_DeckToolTabs.cshtml` + `Home.cshtml`, a `_ToolTileIcon` SVG partial, view tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/66-admin-tool-visibility-toggles-tool-registry/66-CONTEXT.md
@DeckFlow.Web/Views/Shared/_DeckToolTabs.cshtml
@DeckFlow.Web/Views/Deck/Home.cshtml

<interfaces>
<!-- From Plan 01 (Wave 1) — already in the codebase by the time this runs: -->
  DeckFlow.Web.Services.Tools.IToolRegistry          → IReadOnlyList&lt;ToolDefinition&gt; All
  DeckFlow.Web.Services.Tools.ToolDefinition         → Key, Label, Route, Section, FlagKey, Core,
                                                        TileTitle, TileDescription, HelpSlug, Tab,
                                                        IsPrimaryTile, IconKey
  DeckFlow.Web.Services.Tools.ToolNavSection         → Analyze, Build, Reference, Categories
  DeckFlow.Web.Services.Tools.ToolVisibility.VisibleBySection(registry.All, FlagCache)
                                                     → IReadOnlyList&lt;ToolSection(Section, Tools)&gt;
                                                       (empty sections omitted)

Existing view injection pattern (keep): `@inject DeckFlow.Web.Services.FeatureFlags.IFeatureFlagCache FlagCache`
Add: `@inject DeckFlow.Web.Services.Tools.IToolRegistry ToolRegistry`
Nav model is `@model DeckPageTab` (the active tab). Section is active when any of its tools has Tab == Model.
Section display names: Analyze, Build, Reference, Categories (match enum). Section header markup + dropdown
structure (tool-nav__group / __trigger / __dropdown / __link) must be preserved for the existing CSS + JS
(`data-tool-nav-*` hooks). Home section markup uses `hub-group` / `hub-group__title` / `hub-grid` / `hub-card`
(+ `hub-card--primary` for IsPrimaryTile). The 13 inline SVGs currently in Home.cshtml move into the partial.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Registry-driven nav strip with section collapse</name>
  <files>DeckFlow.Web/Views/Shared/_DeckToolTabs.cshtml, DeckFlow.Web.Tests/Tools/DeckToolTabsViewTests.cs</files>
  <action>
    Rewrite `_DeckToolTabs.cshtml` to inject IToolRegistry, compute
    `var sections = ToolVisibility.VisibleBySection(ToolRegistry.All, FlagCache);` and loop:
    for each ToolSection render the existing `tool-nav__group` + `tool-nav__trigger` (section name) +
    `tool-nav__dropdown`, and inside loop its Tools rendering `tool-nav__link` with
    `href="@Url.Content("~" + tool.Route)"`, the `is-active` modifier when `tool.Tab == Model`, and the
    Label as link text. Section `is-active` modifier = any tool in the section has Tab == Model. Because
    VisibleBySection already omits empty sections, an all-disabled section renders nothing — no trigger,
    no dropdown (TOGGLE-05). DELETE the "Suggestions offline" muted span branch entirely (Decision 3) and
    all the hardcoded per-tool `@if (FlagCache.IsEnabled(...))` blocks. Preserve the `data-tool-nav`,
    `data-tool-nav-menu-toggle`, `data-tool-nav-group`, `data-tool-nav-trigger` attributes and the
    Knowledge Base link's title tooltip text. Keep the top-level `<nav id="deck-tool-nav">` + menu-toggle button.
    Add `DeckToolTabsViewTests` (file-content regex assertions, mirroring AdminConfirmModalPartialTests): assert
    the file no longer contains "Suggestions offline"; no longer contains the literal flag-key strings
    `feature.manabase.enabled` / `content.kb.enabled` / `feature.categories.enabled` (proves no hardcoded @if);
    contains `VisibleBySection`; contains `data-tool-nav-trigger`.
  </action>
  <verify>
    <automated>dotnet build DeckFlow.Web/DeckFlow.Web.csproj -c Debug 2>&1 | grep -E "error|Build succeeded" ; dotnet test DeckFlow.Web.Tests/DeckFlow.Web.Tests.csproj --filter "FullyQualifiedName~Tools.DeckToolTabsViewTests"</automated>
  </verify>
  <done>Nav iterates registry; empty section collapses; "Suggestions offline" span gone; hardcoded flag @ifs gone; build clean; view test green.</done>
</task>

<task type="auto">
  <name>Task 2: Registry-driven home tiles + SVG icon partial</name>
  <files>DeckFlow.Web/Views/Deck/Home.cshtml, DeckFlow.Web/Views/Shared/_ToolTileIcon.cshtml, DeckFlow.Web.Tests/Tools/HomeTilesViewTests.cs</files>
  <action>
    Create `_ToolTileIcon.cshtml` (model `string` = icon key) containing a switch over the 13 icon keys, each
    arm emitting the exact inline `<svg ...>` currently used for that tool in Home.cshtml (move the markup
    verbatim — do not redesign; SVG stays in Razor, never C#). Default arm: a neutral fallback icon.
    Rewrite the four `hub-group` sections in `Home.cshtml` to inject IToolRegistry and loop
    `ToolVisibility.VisibleBySection(ToolRegistry.All, FlagCache)`: for each ToolSection render the existing
    `hub-group` + `hub-group__title` (section icon + name) + `hub-grid`, and for each tool an `<a class="hub-card
    @(tool.IsPrimaryTile ? "hub-card--primary" : "")" href="@Url.Content("~" + tool.Route)">` with
    `@await Html.PartialAsync("_ToolTileIcon", tool.IconKey)`, `hub-card__title` = tool.TileTitle, and
    `hub-card__description` = tool.TileDescription. DELETE the categories "Temporarily offline" `hub-card--status`
    placeholder branch and all hardcoded `@if (FlagCache.IsEnabled(...))` tile blocks (Decision 3). Keep the
    `hub-hero` primary banner and `hub-lede` intro untouched. Keep the section header icon SVGs (those are
    section-level, not tool-level) — either inline as today or move into the loop keyed by section.
    Add `HomeTilesViewTests` (file-content assertions): Home.cshtml no longer contains "Temporarily offline" or
    "hub-card--status"; no longer contains the three existing flag-key literals; contains `VisibleBySection` and
    `_ToolTileIcon`. Assert `_ToolTileIcon.cshtml` contains an `<svg` arm for each of the 13 icon keys.
  </action>
  <verify>
    <automated>dotnet build DeckFlow.Web/DeckFlow.Web.csproj -c Debug 2>&1 | grep -E "error|Build succeeded" ; dotnet test DeckFlow.Web.Tests/DeckFlow.Web.Tests.csproj --filter "FullyQualifiedName~Tools.HomeTilesViewTests"</automated>
  </verify>
  <done>Home tiles iterate registry; offline placeholder gone; icon partial covers 13 keys; build clean; view test green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server → rendered HTML | tool labels/routes/copy are static registry data, not user input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-66-05 | Information Disclosure | nav/tile leaking a disabled tool | mitigate | VisibleBySection filters by flag; no disabled tool link rendered |
| T-66-06 | Tampering | XSS via tile copy | accept | copy is static registry constants, Razor auto-encodes; SVG markup is fixed literals in the partial |
| T-66-SC | Tampering | npm/nuget installs | mitigate | no new packages |
</threat_model>

<success_criteria>
- Nav + home iterate the registry; disabling a tool removes its tab and tile.
- All-disabled section renders nothing (verified live in 66-06).
- Both "offline" placeholders deleted; no hardcoded flag @if remains in either view.
- dotnet build clean; view tests green.
</success_criteria>

<output>
Create `.planning/phases/66-admin-tool-visibility-toggles-tool-registry/66-03-SUMMARY.md` when done.
</output>
