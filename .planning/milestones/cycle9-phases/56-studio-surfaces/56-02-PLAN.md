---
phase: 56-studio-surfaces
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - DeckFlow.Studio/Program.cs
  - DeckFlow.Studio/Pages/Review.razor
  - DeckFlow.Studio/Pages/Publish.razor
  - DeckFlow.Studio.Tests/ReviewPageTests.cs
  - DeckFlow.Studio.Tests/PublishPageTests.cs
autonomous: true
requirements: [PUB-03]
must_haves:
  truths:
    - "PublishStateDeriver is resolvable from Studio DI (no runtime injection failure)"
    - "Review page shows a Publish State column with the derived state per row"
    - "Publish page shows a publish-state summary across approved rows"
    - "Publish-state derivation always goes through PublishStateDeriver.Derive — never inline if/else"
  artifacts:
    - path: "DeckFlow.Studio/Program.cs"
      provides: "AddSingleton<PublishStateDeriver> registration"
      contains: "AddSingleton<PublishStateDeriver>"
    - path: "DeckFlow.Studio/Pages/Review.razor"
      provides: "Publish State column + RenderPublishStateBadge"
      contains: "Publish State"
    - path: "DeckFlow.Studio/Pages/Publish.razor"
      provides: "publish-state summary using the deriver"
      contains: "PublishStateDeriver"
  key_links:
    - from: "DeckFlow.Studio/Pages/Review.razor"
      to: "PublishStateDeriver.Derive(row.PushedToProdUtc, row.IsVisible, row.IndexedUtc)"
      via: "injected Deriver called per row"
      pattern: "Deriver\\.Derive\\("
    - from: "DeckFlow.Studio/Pages/Publish.razor"
      to: "PublishStateDeriver.Derive over GetApprovedRowsAsync rows"
      via: "GroupBy on derived state in OnInitializedAsync"
      pattern: "Deriver\\.Derive\\("
---

<objective>
Surface the Phase-55 derived publish-state (`Never published` / `Pushed-hidden` / `Published` /
`Local-newer`) in Studio's Review and Publish pages. Register `PublishStateDeriver` in Studio DI
(currently unregistered — runtime injection would fail), add a `Publish State` column to Review.razor,
and add a per-state count summary to Publish.razor. All derivation routes through the single
`PublishStateDeriver.Derive(...)` — no inline four-state logic anywhere.

Purpose: PUB-03 — operator sees each entry's derived publish-state in Studio Review + Publish.
Output: DI registration, Review column, Publish summary, bUnit coverage for both pages.

This plan is independent of Plan 01 (disjoint files) and runs in the same wave.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/56-studio-surfaces/56-RESEARCH.md
@.planning/phases/56-studio-surfaces/56-PATTERNS.md
@.planning/phases/56-studio-surfaces/56-UI-SPEC.md

<interfaces>
<!-- Phase 55 deriver — already built in Core. Source: 56-RESEARCH.md §3 -->
PublishStateDeriver (DeckFlow.Core/Content/PublishStateDeriver.cs):
  PublishState Derive(DateTimeOffset? pushedToProdUtc, bool isVisible, DateTimeOffset localIndexedUtc)

PublishState (enum) members + ToDisplayString():
  NeverPublished -> "Never published"   (pushedToProdUtc null)
  PushedHidden   -> "Pushed-hidden"     (pushed, !isVisible)
  Published      -> "Published"         (pushed, visible, local not newer)
  LocalNewer     -> "Local-newer"       (pushed, visible, local strictly after push)

ContentSiteIndexRow fields used: PushedToProdUtc (DateTimeOffset?), IsVisible (bool), IndexedUtc (DateTimeOffset).
Review.razor reads GetAllRowsAsync(); Publish.razor reads GetApprovedRowsAsync().

PublishState badge classes (LOCKED — 56-UI-SPEC.md "PublishState Display"):
  NeverPublished -> badge bg-secondary
  PushedHidden   -> badge bg-warning text-dark
  Published      -> badge bg-success text-white + leading <span class="oi oi-check me-1" aria-hidden="true"></span>
  LocalNewer     -> badge bg-info text-dark
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register PublishStateDeriver in Studio DI</name>
  <files>DeckFlow.Studio/Program.cs</files>
  <read_first>
    - DeckFlow.Studio/Program.cs (existing AddSingleton block around line 107; confirms VideoStatusResolver registered, PublishStateDeriver NOT)
    - .planning/phases/56-studio-surfaces/56-PATTERNS.md (section "DeckFlow.Studio/Program.cs" — exact placement after VideoStatusResolver)
  </read_first>
  <action>
    Add `builder.Services.AddSingleton<PublishStateDeriver>();` immediately after the existing
    `builder.Services.AddSingleton<VideoStatusResolver>();` line. Add a one-line `// Why:` comment noting
    PublishStateDeriver is a pure stateless class, safe as a singleton. Ensure the `DeckFlow.Core.Content`
    namespace is in scope (add a `using` only if the file does not already reference it). Do not modify any
    other registration. Preserve LF; touch only the inserted lines.
  </action>
  <verify>
    <automated>"/mnt/c/Program Files/dotnet/dotnet.exe" build DeckFlow.Studio/DeckFlow.Studio.csproj -nologo</automated>
  </verify>
  <acceptance_criteria>
    - Source: Program.cs contains `AddSingleton<PublishStateDeriver>()`.
    - Source: the new line appears after `AddSingleton<VideoStatusResolver>()`.
    - Build: DeckFlow.Studio compiles with 0 errors, no new warnings.
  </acceptance_criteria>
  <done>PublishStateDeriver is DI-registered; Studio builds clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add Publish State column to Review.razor + bUnit coverage</name>
  <files>DeckFlow.Studio/Pages/Review.razor, DeckFlow.Studio.Tests/ReviewPageTests.cs</files>
  <read_first>
    - DeckFlow.Studio/Pages/Review.razor (existing thead/tbody, ReviewViewModel, RenderApprovalBadge static method, colspan on the expand row)
    - DeckFlow.Studio.Tests/ReviewPageTests.cs (existing RenderReview helper + MakeYoutubeRow + WaitForAssertion pattern)
    - .planning/phases/56-studio-surfaces/56-PATTERNS.md (section "DeckFlow.Studio/Pages/Review.razor" — inject, @using, header, cell, RenderPublishStateBadge, ViewModel ctor, colspan update)
    - .planning/phases/56-studio-surfaces/56-UI-SPEC.md (section 6 "Publish-State Column" + "PublishState Display" badge table)
  </read_first>
  <behavior>
    - Test ReviewPage_PublishStateColumn_ShowsNeverPublishedForUnpushedRow: an approved row with PushedToProdUtc null renders "Never published".
    - The Publish State column header renders exactly once between Status and Actions.
  </behavior>
  <action>
    Inject `PublishStateDeriver Deriver` into Review.razor (private `[Inject]` property). Add
    `@using DeckFlow.Core.Content` if not already present (needed for the PublishState enum). Add a
    `<th scope="col">Publish State</th>` header placed after the existing `Status` `<th>` and before the
    `Actions` `<th>`. Add a matching `<td>` per row that renders
    `RenderPublishStateBadge(Deriver.Derive(vm.PushedToProdUtc, vm.IsVisible, vm.IndexedUtc))`. Add the
    `RenderPublishStateBadge` static `RenderFragment` switch using the LOCKED badge classes from the
    interfaces block (Published includes the `oi oi-check` prefix + `text-white`). Add three read-only
    properties to `ReviewViewModel` — `PushedToProdUtc`, `IsVisible`, `IndexedUtc` — populated in its
    constructor from the `ContentSiteIndexRow`. Increment the expand-row `colspan` by one (it grows by the
    one added column). Never inline the four-state logic — always call `Deriver.Derive`.
    In ReviewPageTests.cs, register `PublishStateDeriver` as a singleton in the RenderReview helper and add
    the `ReviewPage_PublishStateColumn_ShowsNeverPublishedForUnpushedRow` `[Fact]` using `WaitForAssertion`.
    Preserve LF; touch only changed lines.
  </action>
  <verify>
    <automated>"/mnt/c/Program Files/dotnet/dotnet.exe" test DeckFlow.Studio.Tests/DeckFlow.Studio.Tests.csproj --filter "FullyQualifiedName~ReviewPageTests" -nologo</automated>
  </verify>
  <acceptance_criteria>
    - Source: Review.razor contains a `<th scope="col">Publish State</th>` and a `RenderPublishStateBadge` method.
    - Source: Review.razor calls `Deriver.Derive(` — no inline `if (... PushedToProdUtc ...)` four-state logic.
    - Source: ReviewViewModel exposes PushedToProdUtc, IsVisible, IndexedUtc populated from the row.
    - Behavior: bUnit test asserts "Never published" appears for an approved-but-unpushed row.
    - Test: `dotnet test --filter ReviewPageTests` exits 0.
  </acceptance_criteria>
  <done>Review page shows a derived Publish State badge per row via the deriver; bUnit test green; colspan adjusted.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add publish-state summary to Publish.razor + bUnit coverage</name>
  <files>DeckFlow.Studio/Pages/Publish.razor, DeckFlow.Studio.Tests/PublishPageTests.cs</files>
  <read_first>
    - DeckFlow.Studio/Pages/Publish.razor (existing inject block, OnInitializedAsync with GetApprovedRowsAsync + _approvedCount, Task.Run + InvokeAsync pattern)
    - DeckFlow.Studio.Tests/ReviewPageTests.cs (bUnit render-helper pattern to mirror for a new PublishPageTests render helper)
    - .planning/phases/56-studio-surfaces/56-PATTERNS.md (section "DeckFlow.Studio/Pages/Publish.razor" — inject, summary field, OnInitializedAsync GroupBy, summary markup, RenderPublishStateBadge; and "PublishPageTests.cs" render helper)
    - .planning/phases/56-studio-surfaces/56-UI-SPEC.md (section 6 Publish.razor change — summary indicator, not a full table)
  </read_first>
  <behavior>
    - Test PublishPage_PublishStateSummary_RendersCountsForApprovedRows: given approved rows in mixed publish states, the summary badges render with counts.
    - When there are no approved rows, no summary block renders.
  </behavior>
  <action>
    Inject `PublishStateDeriver Deriver` into Publish.razor. Add `@using DeckFlow.Core.Content` if missing.
    Add a private field `List<(PublishState State, int Count)> _publishStateSummary = new();`. In
    `OnInitializedAsync`, inside the existing `Task.Run` that fetches `GetApprovedRowsAsync()` rows, compute
    the summary by `GroupBy` on `Deriver.Derive(r.PushedToProdUtc, r.IsVisible, r.IndexedUtc)` producing
    `(State, Count)` tuples ordered by state; assign it via the existing disposal-safe `InvokeAsync` path so
    the count is set before `StateHasChanged`. Render the summary markup (badge + muted count per state)
    after the existing `_approvedCount` paragraph, guarded by `_publishStateSummary.Count > 0`. Add the same
    `RenderPublishStateBadge` static method used in Review.razor (copy exactly; identical badge classes).
    Do not add a per-entry table — per 56-UI-SPEC.md §Publish.razor (line 360+), Publish.razor is
    deliberately summary-only (it has no per-entry table), so the per-state count summary IS the PUB-03
    contract for Publish.razor (this is intentional, not a coverage gap). Never
    inline the four-state logic. Create `PublishPageTests.cs` with a `RenderPublish` helper that registers
    `FakeContentSiteIndexStore`, `FakeContentKbOrchestrator`, `FakeGitRepository`, `PublishStateDeriver`,
    and `ContentKbOrchestratorOptions`, plus the `PublishPage_PublishStateSummary_RendersCountsForApprovedRows`
    `[Fact]` using `WaitForAssertion`. Preserve LF; touch only changed lines.
  </action>
  <verify>
    <automated>"/mnt/c/Program Files/dotnet/dotnet.exe" test DeckFlow.Studio.Tests/DeckFlow.Studio.Tests.csproj --filter "FullyQualifiedName~PublishPageTests" -nologo</automated>
  </verify>
  <acceptance_criteria>
    - Source: Publish.razor contains `_publishStateSummary` and calls `Deriver.Derive(` inside the rows-fetch Task.Run.
    - Source: Publish.razor renders the summary only when `_publishStateSummary.Count > 0`; no per-entry table added.
    - Source: PublishPageTests.cs exists with a `RenderPublish` helper registering PublishStateDeriver.
    - Behavior: bUnit test asserts publish-state summary badges render for approved rows.
    - Test: `dotnet test --filter PublishPageTests` exits 0.
  </acceptance_criteria>
  <done>Publish page shows a per-state count summary derived via the deriver; PublishPageTests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local index store -> Studio page | Page reads ContentSiteIndexRow fields; local-only operator tool, no auth surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-56-02-01 | Tampering | Duplicate publish-state logic drift | mitigate | Both pages call PublishStateDeriver.Derive; acceptance criteria forbid inline four-state if/else, pinning the single source of truth from Phase 55 |
| T-56-02-02 | Information Disclosure | Operator misreads prod publish status | mitigate | Badge always carries the text label (color never sole differentiator); Published badge adds checkmark + text per UI-SPEC accessibility contract |
| T-56-02-03 | Denial of Service | Store call on Blazor sync context blocks circuit | mitigate | Summary computed inside the existing Task.Run that already fetches rows; no second store call; UI updates via disposal-safe InvokeAsync |
| T-56-02-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs; bUnit 2.7.2 + xUnit 2.9.3 already present (approved Phase 46) |
</threat_model>

<verification>
- `dotnet build DeckFlow.Studio/DeckFlow.Studio.csproj` — 0 errors, no new warnings.
- `dotnet test DeckFlow.Studio.Tests --filter "ReviewPageTests|PublishPageTests"` — green.
- `grep -c "Deriver.Derive(" DeckFlow.Studio/Pages/Review.razor DeckFlow.Studio/Pages/Publish.razor` — both > 0.
</verification>

<success_criteria>
- PublishStateDeriver registered as a Studio singleton.
- Review.razor shows a Publish State column; Publish.razor shows a per-state summary.
- All publish-state rendering flows through PublishStateDeriver.Derive (no duplicated logic).
- Both new/extended bUnit test classes pass.
</success_criteria>

<output>
Create `.planning/phases/56-studio-surfaces/56-02-SUMMARY.md` when done.
</output>
