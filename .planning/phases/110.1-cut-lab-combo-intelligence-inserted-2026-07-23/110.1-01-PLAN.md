---
phase: 110.1-cut-lab-combo-intelligence-inserted-2026-07-23
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - DeckFlow.Web/Services/CutLab/CutLabAnalysisContextBuilder.cs
  - DeckFlow.Web/Services/CommanderSpellbookService.cs
  - DeckFlow.Web.Tests/CutLabAnalysisContextBuilderTests.cs
autonomous: true
requirements: [CLUP-17, CLUP-18]

must_haves:
  truths:
    - "A matched pool card resolves to the full complete-combo(s) it participates in (name, results, missing partner for near-combos), not just a yes/no name-membership flag."
    - "The name-only ComboNames HashSet is fully removed; grep for ComboNames returns zero hits in DeckFlow.Web/DeckFlow.Web.Tests source."
    - "CutLabRoleAssigner.AssignRoles still receives a correct true/false combo-membership signal after the field replacement (no behavior change to role assignment)."
    - "The Commander Spellbook requires/of/includes/variantCount API shape is recorded durably in-source so the deferred candidate-matching phase can start from it without re-discovery."
  artifacts:
    - path: "DeckFlow.Web/Services/CutLab/CutLabAnalysisContextBuilder.cs"
      provides: "New per-card combo membership record + lookup on CutLabClassificationContext replacing ComboNames"
      contains: "CutLabClassificationContext"
    - path: "DeckFlow.Web/Services/CommanderSpellbookService.cs"
      provides: "Durable reference comment recording requires/of/includes/variantCount shape (SC-4)"
      contains: "requires"
  key_links:
    - from: "CutLabAnalysisContextBuilder.LoadSpellbookFailOpenAsync"
      to: "CutLabClassificationContext card->combo lookup"
      via: "single walk of combos.IncludedCombos / combos.AlmostIncludedCombos"
      pattern: "IncludedCombos"
    - from: "CutLabAnalysisContextBuilder.cs:173 (AssignRoles combo-membership arg)"
      to: "new card->combo lookup ContainsKey"
      via: "replaces classification.ComboNames.Contains(...)"
      pattern: "AssignRoles"
---

<objective>
Thread the full `IncludedCombos` / `AlmostIncludedCombos` Commander Spellbook data into a
per-card combo-membership lookup on `CutLabClassificationContext`, replacing the name-only
`IReadOnlySet<string> ComboNames` HashSet (LOCKED decision #3). Also record the Commander
Spellbook `requires` (template-slot) and `of`/`includes` (variant) API shape durably in-source
as the SC-4 research deliverable — WITHOUT implementing candidate enumeration or matching.

Purpose: The current classification path only knows combo *names*, which caps what CLUP-17/18
can show. This plan builds the data foundation (Wave 1) that Waves 2-3 consume to produce the
combo-protected finding kind, badges, and disclosure context.

Output: A richer `CutLabClassificationContext` carrying a `IReadOnlyDictionary<string, CutLabCardComboMembership>`
(keyed by normalized card name), the updated `AssignRoles` call site, xUnit coverage, and a
durable API-shape reference comment.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/110.1-cut-lab-combo-intelligence-inserted-2026-07-23/110.1-CONTEXT.md
@.planning/phases/110.1-cut-lab-combo-intelligence-inserted-2026-07-23/110.1-RESEARCH.md
@./CLAUDE.md

<interfaces>
<!-- Extracted from codebase — executor should use these directly, no exploration needed. -->

From DeckFlow.Web/Services/CommanderSpellbookService.cs (records already defined here):
  public sealed record SpellbookCombo(IReadOnlyList<string> CardNames, IReadOnlyList<string> Results, string Instructions, int? Popularity, int? ManaValueNeeded);  // approx :16-21
  public sealed record SpellbookAlmostCombo(string MissingCard, IReadOnlyList<string> CardsInDeck, IReadOnlyList<string> Results, string Instructions);  // approx :26-30
  CommanderSpellbookResult exposes: IncludedCombos (IReadOnlyList<SpellbookCombo>), AlmostIncludedCombos (IReadOnlyList<SpellbookAlmostCombo>)

From DeckFlow.Web/Services/CutLab/CutLabAnalysisContextBuilder.cs:
  public sealed record CutLabClassificationContext(                       // :78-83
      IReadOnlyList<SpellbookAlmostCombo> AlmostIncludedCombos,
      bool ComboDataAvailable,
      bool CategoryDataAvailable,
      IReadOnlyDictionary<string, IReadOnlyList<string>> CategoriesByName,
      IReadOnlySet<string> ComboNames);                                    // <- REPLACE this field
  private sealed record SpellbookLookupResult(                            // :603-606
      IReadOnlyList<SpellbookAlmostCombo> AlmostIncludedCombos,
      bool ComboDataAvailable,
      IReadOnlySet<string> ComboNames);                                    // <- REPLACE this field
  // Consumer: CutLabAnalysisContextBuilder.cs:173  classification.ComboNames.Contains(normalizedEntryName)
  // Producer: CutLabAnalysisContextBuilder.cs:527-535 (walks combos.IncludedCombos, keeps only names)
  // CutLabCardNames.Comparer / CutLabCardNames.Normalize are the OrdinalIgnoreCase-family helpers used throughout this file.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace ComboNames with a per-card combo-membership lookup threaded from full IncludedCombos</name>
  <read_first>
    - DeckFlow.Web/Services/CutLab/CutLabAnalysisContextBuilder.cs (record defs :70-83; AssignRoles call site :173; producer :495-548; SpellbookLookupResult :603-606) — REQUIRED, the file being modified
    - DeckFlow.Web/Services/CommanderSpellbookService.cs (:16-30) — source of truth for SpellbookCombo / SpellbookAlmostCombo record shapes
    - .planning/phases/110.1-cut-lab-combo-intelligence-inserted-2026-07-23/110.1-RESEARCH.md §2 "Card->combo lookup design" (proposed CutLabCardComboMembership shape; Pitfall 3 rename blast radius)
    - DeckFlow.Web.Tests/CutLabAnalysisContextBuilderTests.cs (existing LocalSpellbookService test double that feeds SpellbookCombo/SpellbookAlmostCombo without HTTP) — REQUIRED, test file being extended
  </read_first>
  <behavior>
    - Test 1: a card belonging to a complete combo resolves via the new lookup to a membership entry whose CompleteCombos is non-empty and carries the combo Results/CardNames.
    - Test 2: a card belonging only to a near-combo resolves to a membership entry whose NearCombos is non-empty and exposes the SpellbookAlmostCombo.MissingCard.
    - Test 3: a card in no combo has no lookup entry (ContainsKey == false), and AssignRoles' combo-membership boolean is false for that card.
    - Test 4: a card present in a complete combo still yields AssignRoles combo-membership boolean == true (parity with old ComboNames.Contains behavior).
  </behavior>
  <action>
    Define a new public sealed record `CutLabCardComboMembership(IReadOnlyList&lt;SpellbookCombo&gt; CompleteCombos, IReadOnlyList&lt;SpellbookAlmostCombo&gt; NearCombos)` in CutLabAnalysisContextBuilder.cs near `CutLabClassificationContext`. Replace the `IReadOnlySet&lt;string&gt; ComboNames` field on BOTH `CutLabClassificationContext` (:83) and the private `SpellbookLookupResult` (:606) with `IReadOnlyDictionary&lt;string, CutLabCardComboMembership&gt; CardComboMembership` keyed by normalized card name using `CutLabCardNames.Comparer`. In `LoadSpellbookFailOpenAsync` (:520-547), build the dictionary from the SAME single walk of `combos.IncludedCombos` (for CompleteCombos, keyed off each `combo.CardNames`) AND `combos.AlmostIncludedCombos` (for NearCombos, keyed off each `almost.CardsInDeck`) — no new Spellbook call, no new caching. Update the two `new SpellbookLookupResult(...)` constructions (:517, :547) and the `new CutLabClassificationContext(...)` construction (:495-503) accordingly. Update the ONLY consumer at :173 from `classification.ComboNames.Contains(normalizedEntryName)` to `classification.CardComboMembership.ContainsKey(normalizedEntryName)`. Update the three xmldoc `<param name="ComboNames">` comments (:77, and the two record xmldoc blocks) to describe the new field. Preserve normalization via `CutLabCardNames.Normalize` on every dictionary key exactly as the old code normalized names. Per CLAUDE.md: change only the lines whose content changes; preserve LF endings; respect .editorconfig carve-outs (do not convert `{ get; init; }`, no attribute inlining). Add the xUnit cases in CutLabAnalysisContextBuilderTests.cs using the existing LocalSpellbookService double.
  </action>
  <acceptance_criteria>
    - Source: `CutLabAnalysisContextBuilder.cs` contains `record CutLabCardComboMembership` with members `CompleteCombos` and `NearCombos`.
    - Source: `CutLabAnalysisContextBuilder.cs` contains `CardComboMembership` on both `CutLabClassificationContext` and `SpellbookLookupResult`; contains NO occurrence of the identifier `ComboNames`.
    - Source: `grep -rn "ComboNames" DeckFlow.Web/ DeckFlow.Web.Tests/ --include=*.cs | grep -v bin/ | grep -v obj/` returns zero rows (Pitfall 3 final check).
    - Source: line 173 reads `classification.CardComboMembership.ContainsKey(` (no `.ComboNames.Contains`).
    - Behavior/test: `dotnet test DeckFlow.Web.Tests --filter FullyQualifiedName~CutLabAnalysisContextBuilderTests` exits 0 with the four new cases green.
    - Build: `dotnet build DeckFlow.sln` completes with 0 new warnings.
  </acceptance_criteria>
  <verify>
    <automated>dotnet build DeckFlow.sln && dotnet test DeckFlow.Web.Tests --filter "FullyQualifiedName~CutLabAnalysisContextBuilderTests"</automated>
  </verify>
  <done>The card->combo lookup replaces ComboNames end-to-end, the single AssignRoles consumer compiles against the new dictionary, grep for ComboNames is clean, and the four xUnit cases pass.</done>
</task>

<task type="auto">
  <name>Task 2: Record the Commander Spellbook requires/of/includes/variantCount API shape in-source (SC-4)</name>
  <read_first>
    - DeckFlow.Web/Services/CommanderSpellbookService.cs (ExtractCardNames ~:251-265; ParseVariants ~:182-218) — REQUIRED, the file being modified
    - .planning/phases/110.1-cut-lab-combo-intelligence-inserted-2026-07-23/110.1-RESEARCH.md §1b field-by-field table + §1c "Deliverable framing for the plan"
  </read_first>
  <action>
    Add a durable reference comment block immediately above `ExtractCardNames` in CommanderSpellbookService.cs that records the deferred-phase API shape WITHOUT adding any parsing code. The comment must name each currently-dropped field and its meaning: `requires[]` (template slot: `template.name`, `template.scryfallQuery`, `template.scryfallApi` — the "View N Cards" enumeration source), `of[].id` (parent/grouping key for "variants of this combo"), `includes[].id` (sub-combo composition — distinct from `of`), `variantCount` (sibling variant count), and note that `popularity` is ALREADY parsed and IS the EDHREC deck-count source. Include a pointer marker `// TODO(follow-on phase): requires/of/includes shape — see 110.1-RESEARCH.md §1b` so the shape is not re-lost. Do NOT read `variant.requires`, `variant.of`, `variant.includes`, or `variant.id` in code this phase — comment only. Per CLAUDE.md: preserve LF endings; changed-lines-only; xmldoc single-space carve-out preserved.
  </action>
  <acceptance_criteria>
    - Source: `CommanderSpellbookService.cs` contains the strings `requires`, `of[].id`, `includes`, `variantCount`, and `TODO(follow-on phase)` within a comment block above `ExtractCardNames`.
    - Source: no new `.GetProperty("requires")` / `.GetProperty("of")` / `.GetProperty("includes")` parsing call is added (grep for `"requires"`/`"of"`/`"includes"` as JSON property reads returns no new hits) — this is documentation only.
    - Build: `dotnet build DeckFlow.sln` completes with 0 new warnings.
  </acceptance_criteria>
  <verify>
    <automated>dotnet build DeckFlow.sln</automated>
  </verify>
  <done>The requires/of/includes/variantCount shape is recorded in-source as a reference comment with a follow-on TODO marker; no parsing behavior changed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Commander Spellbook API → Cut Lab classification | Combo card names / results are untrusted upstream text folded into the classification context; already fetched today, unchanged fetch path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-110.1-01 | Tampering | Upstream combo text keyed into CardComboMembership dictionary | accept | No rendering in this plan (data layer only); Razor auto-encoding at render (Plan 03) is the sink-side control. Keys are normalized names, not executed. |
| T-110.1-02 | Denial of Service | Extra dictionary allocation per request | accept | Pure re-projection of data already in memory (existing 30-min IMemoryCache in CommanderSpellbookService); no new fetch, no new caching concern. |
| T-110.1-SC | Tampering | npm/NuGet installs | accept | No new packages this phase (RESEARCH §"Project Constraints"); nothing to legitimacy-check. |
</threat_model>

<verification>
- `dotnet build DeckFlow.sln` clean (0 new warnings).
- `dotnet test DeckFlow.Web.Tests --filter "FullyQualifiedName~CutLabAnalysisContextBuilderTests"` green.
- `grep -rn "ComboNames" DeckFlow.Web/ DeckFlow.Web.Tests/ --include=*.cs | grep -v bin/ | grep -v obj/` returns zero rows.
</verification>

<success_criteria>
- `CutLabClassificationContext` exposes a per-card combo-membership lookup; `ComboNames` is gone repo-wide (source).
- AssignRoles combo-membership signal is behavior-preserving.
- SC-4 Spellbook shape is recorded durably in-source with a follow-on marker.
- No new packages; LF endings and format-gate respected.
</success_criteria>

<output>
Create `.planning/phases/110.1-cut-lab-combo-intelligence-inserted-2026-07-23/110.1-01-SUMMARY.md` when done.
</output>
