---
phase: 66-admin-tool-visibility-toggles-tool-registry
plan: 04
type: execute
wave: 2
depends_on: [66-01]
files_modified:
  - DeckFlow.Web/Help/content-kb.md
  - DeckFlow.Web/Help/category-suggestions.md
  - DeckFlow.Web/Help/deck-analysis.md
  - DeckFlow.Web/Help/deck-comparison.md
  - DeckFlow.Web/Help/cedh-meta-gap.md
  - DeckFlow.Web/Help/deck-sync.md
  - DeckFlow.Web/Help/deck-primer.md
  - DeckFlow.Web/Help/card-lookup.md
  - DeckFlow.Web/Help/ask-a-judge.md
  - DeckFlow.Web/Help/commander-categories.md
  - DeckFlow.Web.Tests/Tools/HelpFlagHeaderConsistencyTests.cs
autonomous: true
requirements: [TOGGLE-03]
must_haves:
  truths:
    - "Every tool that has a help topic declares a requires_flag header equal to its registry flag key, so its help entry hides when the tool is disabled."
    - "The content-kb.md and category-suggestions.md gap (header missing today) is closed."
    - "Tools without a help topic (convert, mechanic-lookup) are not given one."
  artifacts:
    - path: "DeckFlow.Web/Help/content-kb.md"
      provides: "requires_flag: content.kb.enabled header"
      contains: "requires_flag"
    - path: "DeckFlow.Web/Help/category-suggestions.md"
      provides: "requires_flag: feature.categories.enabled header"
      contains: "requires_flag"
    - path: "DeckFlow.Web.Tests/Tools/HelpFlagHeaderConsistencyTests.cs"
      provides: "Registry↔help proof every tool help slug carries the matching requires_flag"
  key_links:
    - from: "Help/*.md requires_flag"
      to: "registry FlagKey"
      via: "HelpContentService.RequiresFlag → HelpController IsTopicVisible → IFeatureFlagCache"
      pattern: "requires_flag:"
---

<objective>
Close the help-visibility gap so a disabled tool's help topic disappears with its nav tab and tile.
Add a `requires_flag` front-matter header to every tool help markdown file, set to that tool's registry
flag key, and lock the registry↔help alignment with a consistency test.

Purpose: TOGGLE-03 (help entry hides with the tool). Decision 3 literally requires adding `requires_flag`
to content-kb.md and category-suggestions.md (today they lack it and stay visible); this plan also extends
the same proven header mechanism to the other newly-gated tools that have help topics, so all tools hide
uniformly. The existing HelpContentService/HelpController already consume `requires_flag` — no service change.
Output: 10 help-file header edits, registry↔help consistency test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/66-admin-tool-visibility-toggles-tool-registry/66-CONTEXT.md
@DeckFlow.Web/Services/HelpContentService.cs

<interfaces>
<!-- HelpContentService already reads `requires_flag` from each .md front-matter into HelpTopic.RequiresFlag;
     HelpController.IsTopicVisible hides a topic when RequiresFlag is non-null and FlagCache.IsEnabled is false.
     manabase.md already demonstrates the exact header format:

       ---
       title: Mana Base
       summary: ...
       order: 35
       requires_flag: feature.manabase.enabled
       ---

     Tool → help slug (filename) → flag key to add: -->
  deck-analysis.md        → tool.deck-analysis.enabled
  deck-comparison.md      → tool.deck-comparison.enabled
  cedh-meta-gap.md        → tool.cedh-meta-gap.enabled
  deck-sync.md            → tool.deck-sync.enabled
  deck-primer.md          → tool.deck-primer.enabled
  card-lookup.md          → tool.card-lookup.enabled
  ask-a-judge.md          → tool.judge-questions.enabled
  commander-categories.md → tool.commander-categories.enabled
  content-kb.md           → content.kb.enabled            (gap — Decision 3)
  category-suggestions.md → feature.categories.enabled    (gap — Decision 3)
  manabase.md             → feature.manabase.enabled       (ALREADY present — leave as-is)
  (convert, mechanic-lookup have NO help topic — do not create one)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add requires_flag headers to the 10 tool help files</name>
  <files>DeckFlow.Web/Help/content-kb.md, DeckFlow.Web/Help/category-suggestions.md, DeckFlow.Web/Help/deck-analysis.md, DeckFlow.Web/Help/deck-comparison.md, DeckFlow.Web/Help/cedh-meta-gap.md, DeckFlow.Web/Help/deck-sync.md, DeckFlow.Web/Help/deck-primer.md, DeckFlow.Web/Help/card-lookup.md, DeckFlow.Web/Help/ask-a-judge.md, DeckFlow.Web/Help/commander-categories.md</files>
  <action>
    In each of the 10 files, add a `requires_flag: &lt;key&gt;` line inside the existing `---` front-matter block
    (after the `order:` line, matching manabase.md format), using the key mapping in &lt;interfaces&gt;. Add ONLY the
    one header line per file — do not alter title/summary/order or body. Preserve each file's existing line
    endings (do not normalize EOL). Do not create help topics for convert or mechanic-lookup.
  </action>
  <verify>
    <automated>cd DeckFlow.Web/Help && for f in content-kb category-suggestions deck-analysis deck-comparison cedh-meta-gap deck-sync deck-primer card-lookup ask-a-judge commander-categories; do grep -q "^requires_flag:" "$f.md" || echo "MISSING $f"; done; echo done</automated>
  </verify>
  <done>All 10 files carry a requires_flag header with the correct key; grep loop prints only "done".</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Registry↔help consistency test</name>
  <files>DeckFlow.Web.Tests/Tools/HelpFlagHeaderConsistencyTests.cs</files>
  <behavior>
    - For every ToolDefinition whose HelpSlug is non-null, the corresponding `DeckFlow.Web/Help/{HelpSlug}.md` exists and its parsed front-matter requires_flag equals the tool's FlagKey.
    - Tools whose HelpSlug is null (convert, mechanic-lookup) are skipped (no help file required).
  </behavior>
  <action>
    Add `HelpFlagHeaderConsistencyTests`: locate the Help directory relative to the web project (mirror how
    HelpContentService / existing help tests resolve the path, e.g. walk up from AppContext.BaseDirectory to the
    DeckFlow.Web/Help source, or read the copied-to-output Help folder). For each tool in `new ToolRegistry().All`
    with a non-null HelpSlug, read `{HelpSlug}.md`, parse the `requires_flag:` line from the front-matter, and
    assert it equals tool.FlagKey. This makes the registry the enforced source of truth for help gating and fails
    if a future tool is added without wiring its help flag.
  </action>
  <verify>
    <automated>dotnet test DeckFlow.Web.Tests/DeckFlow.Web.Tests.csproj --filter "FullyQualifiedName~Tools.HelpFlagHeaderConsistencyTests"</automated>
  </verify>
  <done>Consistency test green: every registry help slug carries the matching requires_flag; null-help tools skipped.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| static help content → render | help md is repo-controlled, not user input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-66-07 | Information Disclosure | help topic of a disabled tool stays visible | mitigate | requires_flag header hides the topic + 404s the /help/{slug} route (existing HelpController behaviour) |
| T-66-SC | Tampering | npm/nuget installs | mitigate | no new packages |
</threat_model>

<success_criteria>
- Disabling a tool hides its help index entry and 404s its /help/{slug} page (existing mechanism, now wired).
- content-kb + category-suggestions gaps closed (Decision 3).
- Consistency test proves registry↔help alignment.
</success_criteria>

<output>
Create `.planning/phases/66-admin-tool-visibility-toggles-tool-registry/66-04-SUMMARY.md` when done.
</output>
