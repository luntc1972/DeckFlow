---
phase: 75-tap-analyzer-surface
plan: 04
type: execute
wave: 3
depends_on: ["75-01", "75-02", "75-03"]
files_modified:
  - DeckFlow.Web/wwwroot/css/site-common.css
  - DeckFlow.Web/Views/Deck/Manabase.cshtml
  - DeckFlow.Web.Tests/Manabase/ManabaseViewRenderTests.cs
autonomous: false
requirements: [TAP-01, TAP-02, TAP-04]

must_haves:
  truths:
    - "When the flag is ON and a result is present, /manabase renders a full-width tap-analyzer card under the two-lens grid showing turn-1 untapped availability + overall + per-color untapped frequency."
    - "When the flag is OFF the @if block emits NO element, whitespace, or comment — the page is byte-identical to pre-Cycle-13."
    - "An automated xUnit render test proves the rendered Manabase view contains NO tap-analyzer markup (manabase-taplens) when ShowTapAnalyzer is false and DOES contain it when true — the OFF page invariant is CI-enforced, not manual-only."
    - "The two new CSS classes live in site-common.css (layout-only, no new color tokens, no per-theme fork edits) and collapse to one column at max-width 640px."
    - "The card re-skins correctly across guild themes (Jeskai/Azorius/Nyx) and stacks cleanly on mobile."
  artifacts:
    - path: "DeckFlow.Web/wwwroot/css/site-common.css"
      provides: ".manabase-taplens + .manabase-taplens-split + 640px collapse"
      contains: "manabase-taplens"
    - path: "DeckFlow.Web/Views/Deck/Manabase.cshtml"
      provides: "flag-guarded tap-analyzer card between .manabase-twolens and .manabase-context"
      contains: "manabase-taplens"
    - path: "DeckFlow.Web.Tests/Manabase/ManabaseViewRenderTests.cs"
      provides: "render-asserts tap card absent when ShowTapAnalyzer=false, present when true"
      contains: "manabase-taplens"
  key_links:
    - from: "Manabase.cshtml"
      to: "ManabaseViewModel.ShowTapAnalyzer + report.TapAnalysis"
      via: "@if (Model.ShowTapAnalyzer && Model.HasResult && report?.TapAnalysis is { } tap)"
      pattern: "ShowTapAnalyzer"
    - from: "Manabase.cshtml"
      to: "ManabaseDisplay.TapMarker"
      via: "per-color ✓/⚠ marker"
      pattern: "TapMarker"
    - from: "DeckFlow.Web.Tests/Manabase/ManabaseViewRenderTests.cs"
      to: "DeckFlow.Web/Views/Deck/Manabase.cshtml"
      via: "IRazorViewEngine render of ManabaseViewModel with ShowTapAnalyzer false/true"
      pattern: "manabase-taplens"
---

<objective>
Surface the tap-analyzer card on the /manabase page (TAP-01, TAP-02) behind the flag, reusing the established `.manabase-lens` chrome, and verify it renders correctly across themes and on mobile. The card emits nothing when the flag is OFF (TAP-04 page byte-identity).

Purpose: Complete the on-page surface so the metric is visible to operators who enable the flag, with the single-source-of-truth values already computed in 75-02.

Output: two layout-only classes in `site-common.css`; the flag-guarded card in `Manabase.cshtml` (full UI-SPEC wireframe + a11y); and a human verification of theme + mobile rendering.

Honors locked decisions: cEDH renders the FULL card (D2); per-color list is omitted for single-color decks; ✓/⚠ via the flat-80% TapMarker (D4, informational only).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/75-tap-analyzer-surface/75-PATTERNS.md
@.planning/phases/75-tap-analyzer-surface/UI-SPEC.md
@.planning/phases/75-tap-analyzer-surface/75-03-SUMMARY.md

<interfaces>
<!-- View-layer anchors. ShowTapAnalyzer/TapAnalysis/TapMarker/TapAnalyzerGloss all exist (75-01..03). -->

Manabase.cshtml:
- `.manabase-twolens` grid opens ~:180, closes ~:226; `<p class="manabase-context">` at ~:228. Insert the card between :226 and :228.
- Existing byte-identical guard analog (`@if (showPlainLanguage) { <p class="manabase-lens-gloss">@ManabaseDisplay.KarstenSourceGloss</p> }`) at ~:205-209.
- In-scope locals already bound above :180: `report`, `showPlainLanguage`.

ManabaseDisplay (consumed):
- `(string Css, string Marker) TapMarker(int untappedPercent)` → met/✓ at ≥80 else short/⚠.
- `const string TapAnalyzerGloss`.

Reused CSS classes (site-common.css, all already themed): `.manabase-lens`, `-label`, `-big` (+child span), `-pill`, `-row`, `-color`, `-muted`, `-met`, `-short`, `-note`, `-gloss`.

New CSS (site-common.css; insert near the `.manabase-twolens` 640px rule ~:2769):
```css
.manabase-taplens { margin: -0.25rem 0 1rem; }
.manabase-taplens-split {
  display: grid;
  grid-template-columns: minmax(0, 1fr) minmax(0, 1.4fr);
  gap: 1rem 1.5rem;
  align-items: start;
}
@media (max-width: 640px) {
  .manabase-taplens-split { grid-template-columns: 1fr; }
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the two layout-only CSS classes to site-common.css</name>
  <read_first>
    - DeckFlow.Web/wwwroot/css/site-common.css (existing `.manabase-twolens` rule + its `@media (max-width: 640px)` collapse ~:2769; the `.manabase-lens*` chrome ~:2594-2773)
    - UI-SPEC.md Section 3 (net-new classes table) + Section 7 (responsive)
    - CLAUDE.md theme constraint (layout CSS belongs in site-common.css, NOT site.css or any fork)
  </read_first>
  <action>
    In site-common.css add `.manabase-taplens` (margin: -0.25rem 0 1rem) and `.manabase-taplens-split` (grid, columns minmax(0,1fr) minmax(0,1.4fr), gap 1rem 1.5rem, align-items start), plus a `@media (max-width: 640px)` rule collapsing `.manabase-taplens-split` to `grid-template-columns: 1fr`. Place them adjacent to the existing `.manabase-twolens` responsive rule so they share the 640px breakpoint. Introduce NO new color tokens and edit NO per-theme fork file — all visible color comes from the reused `.manabase-lens` classes. Touch only added lines.
  </action>
  <verify>
    <automated>grep -nE "manabase-taplens(\b|-split)" DeckFlow.Web/wwwroot/css/site-common.css</automated>
  </verify>
  <acceptance_criteria>
    - SOURCE: site-common.css contains `.manabase-taplens` and `.manabase-taplens-split` rules plus a `max-width: 640px` collapse for the split.
    - NEGATIVE: no `site.css` or `site-*.css` per-theme fork modified (`git status --porcelain DeckFlow.Web/wwwroot/css | grep -vE "site-common.css" ` shows no theme-fork changes).
    - NEGATIVE: no new CSS custom property (`--`) declarations introduced by these rules.
  </acceptance_criteria>
  <done>Two layout-only classes present in site-common.css; no theme forks touched.</done>
</task>

<task type="auto">
  <name>Task 2: Render the flag-guarded tap-analyzer card in Manabase.cshtml</name>
  <read_first>
    - DeckFlow.Web/Views/Deck/Manabase.cshtml (`.manabase-twolens` :180-226; `.manabase-context` :228; `@if (showPlainLanguage)` gloss :205-209)
    - 75-PATTERNS.md "Manabase.cshtml" section (full wireframe markup)
    - UI-SPEC.md Sections 2, 4, 5, 8 (wireframe, states, microcopy, a11y)
  </read_first>
  <action>
    Insert the tap card between the `.manabase-twolens` closing `</div>` (~:226) and `<p class="manabase-context">` (~:228), wholly inside `@if (Model.ShowTapAnalyzer && Model.HasResult && report?.TapAnalysis is { } tap)` with NO trailing whitespace, empty line, `@* *@`, or HTML comment outside the block (byte-identity when OFF — Pitfall 3). Build the card per UI-SPEC: root `<div class="manabase-lens manabase-taplens" role="group" aria-label="Untapped sources">`; eyebrow `.manabase-lens-label` "Untapped sources"; a `.manabase-taplens-split` with the turn-1 headline column (`.manabase-lens-big` = `@tap.Turn1UntappedPercent%` + unit span "turn-1 untapped" + `.manabase-lens-pill` "share of games with an untapped source to spend on turn 1") and the colors column (an "Overall" `.manabase-lens-row` showing `@tap.OverallUntappedPercent% untapped`, then — only when `report.ColorFindings.Count > 1` — one `.manabase-lens-row` per `tap.ColorTap` entry with color name, `@tf.UntappedPercent% untapped`, muted `(F1 / F1)` counts, and the TapMarker ✓/⚠ glyph with `aria-hidden="true"` plus an `.sr-only` "meets target"/"below target" word). Add the always-on `.manabase-lens-note` (UI-SPEC Section 5 sentence) and, only when `showPlainLanguage`, a `.manabase-lens-gloss` rendering `@ManabaseDisplay.TapAnalyzerGloss`. cEDH renders the full card (D2). Percentages are whole numbers + "%"; source counts use F1. No fenced code in action — follow the PATTERNS wireframe.
  </action>
  <verify>
    <automated>dotnet build DeckFlow.Web/DeckFlow.Web.csproj  # Razor compiles; then render test (Task 3) + manual checkpoint (Task 4)</automated>
  </verify>
  <acceptance_criteria>
    - SOURCE: Manabase.cshtml contains `@if (Model.ShowTapAnalyzer && Model.HasResult && report?.TapAnalysis is { } tap)` wrapping the entire card.
    - SOURCE: card root has `class="manabase-lens manabase-taplens"` `role="group"` `aria-label="Untapped sources"`.
    - SOURCE: per-color list is gated by `report.ColorFindings.Count > 1`.
    - SOURCE: ✓/⚠ markers use `ManabaseDisplay.TapMarker` and are paired with `.sr-only` text + `aria-hidden` on the glyph (never color-alone).
    - SOURCE: gloss line gated by `showPlainLanguage` and renders `ManabaseDisplay.TapAnalyzerGloss`.
    - NEGATIVE: no markup, whitespace, or comment outside the `@if` block was added between :226 and :228.
    - BUILD: `dotnet build DeckFlow.Web/DeckFlow.Web.csproj` 0/0.
  </acceptance_criteria>
  <done>Card renders when flag ON + result present; emits nothing when OFF.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Render-assert the tap card across flag OFF/ON (xUnit, CI)</name>
  <read_first>
    - DeckFlow.Web.Tests/AdminHarvestControllerTests.cs (the `RenderPartialViewAsync(viewName, model)` Razor render harness ~:193-234 — service-collection setup, `AddControllersWithViews().AddApplicationPart(...)`, `IRazorViewEngine.FindView` + `RenderAsync` to a StringWriter; `CreateHostingEnvironment` ~:236-249)
    - DeckFlow.Web/Views/Deck/Manabase.cshtml (the flag-guarded card from Task 2; the `report` / `Model.HasResult` locals the card depends on)
    - DeckFlow.Web/Models/ManabaseViewModel.cs (ShowTapAnalyzer, HasResult, Report) + DeckFlow.Core/Manabase/ManabaseModels.cs (ManabaseReport.TapAnalysis / ManabaseTapAnalysis / ColorTapFinding for the fixture)
  </read_first>
  <behavior>
    - Rendering Deck/Manabase with a populated ManabaseViewModel (HasResult true, Report.TapAnalysis non-null, multi-color ColorTap, ColorFindings.Count > 1) and ShowTapAnalyzer=false → output does NOT contain `manabase-taplens`, `aria-label="Untapped sources"`, nor the turn-1 microcopy.
    - Same model with ShowTapAnalyzer=true → output DOES contain `manabase-taplens`, the turn-1 headline value, and the Overall untapped line.
  </behavior>
  <action>
    Create DeckFlow.Web.Tests/Manabase/ManabaseViewRenderTests.cs (namespace DeckFlow.Web.Tests, sealed class) reusing the Razor render harness pattern from AdminHarvestControllerTests: build the service collection the same way (`AddControllersWithViews().AddApplicationPart(typeof(ManabaseController).Assembly)`, the diagnostic/object-pool/hosting-env singletons, `CreateHostingEnvironment`), resolve `IRazorViewEngine`, `FindView` with RouteData controller="Deck" and viewName="Manabase" and `isMainPage: false` (so no `_Layout` is required), then `RenderAsync` to a StringWriter. Build a valid ManabaseViewModel fixture with `HasResult = true`, a minimal populated `Report` whose `TapAnalysis` is a multi-color `ManabaseTapAnalysis` (Turn1UntappedPercent, OverallUntappedPercent, and a ColorTap dictionary with ≥2 ColorTapFinding entries) and `ColorFindings.Count > 1`. Add two facts: `OffState` (ShowTapAnalyzer=false) asserts `DoesNotContain("manabase-taplens")` and `DoesNotContain` the turn-1 microcopy; `OnState` (ShowTapAnalyzer=true) asserts `Contains("manabase-taplens")` plus the Overall untapped line + the turn-1 headline value. If the full-page view cannot render standalone because of an unrelated dependency (a required section/inject), surface the exact failure and fix the harness — do NOT fall back to a source-text-only scan (a source scan cannot distinguish OFF from ON because the markup literal always exists in the .cshtml). Match xUnit + the harness's service-collection setup exactly. No fenced code in action.
  </action>
  <verify>
    <automated>dotnet test DeckFlow.Web.Tests --filter "FullyQualifiedName~ManabaseViewRender"</automated>
  </verify>
  <acceptance_criteria>
    - FILE: DeckFlow.Web.Tests/Manabase/ManabaseViewRenderTests.cs exists with two render facts (OFF + ON).
    - TEST: OFF fact renders Deck/Manabase with ShowTapAnalyzer=false and asserts no `manabase-taplens` markup and no tap microcopy.
    - TEST: ON fact renders with ShowTapAnalyzer=true and asserts `manabase-taplens` + the Overall/turn-1 lines are present.
    - NEGATIVE: the assertion is render-based (IRazorViewEngine), NOT a `File.ReadAllText` source-text scan of the .cshtml.
    - BUILD: `dotnet build DeckFlow.sln` 0/0; the test runs in CI (not a manual checkpoint).
  </acceptance_criteria>
  <done>The OFF page byte-identity invariant and ON card presence are enforced by an automated render test, not manual verification alone.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Verify tap card across themes + mobile (and OFF byte-identity)</name>
  <what-built>
    A full-width "Untapped sources" card on /manabase (under the two-lens grid) showing turn-1 untapped availability, an Overall untapped %, and per-color untapped % with ✓/⚠ markers. Visible only when `analysis.manabase.tap-analyzer` is ON. Two layout-only CSS classes were added to site-common.css; no theme forks were touched.
  </what-built>
  <how-to-verify>
    1. Start the web app headless: `scripts/run-web-test.sh` (sets DECKFLOW_DISABLE_AUTO_BROWSER=true — never opens a Windows browser). Drive checks with `npx --no-install playwright test` or a manually-opened browser against the headless server.
    2. Enable the flag via /Admin flag console (Basic Auth) — set `analysis.manabase.tap-analyzer` ON.
    3. On /manabase, analyze a MULTI-COLOR deck (Casual). Confirm: the tap card appears under the two-lens grid and before the mode/context line; turn-1 headline + pill render; Overall row + one row per color with ✓ (≥80%) / ⚠ (<80%) markers; numbers do not contradict the cast-rate lens.
    4. Analyze a SINGLE-COLOR deck: confirm the per-color list is omitted (only Turn-1 headline + Overall row).
    5. Analyze in cEDH mode: confirm the FULL card still renders (decision D2).
    6. Theme check — repeat step 3 on Jeskai (site.css default), Azorius (site-azorius.css), Nyx (site-nyx.css): the card re-skins with theme tokens; ✓/⚠ colors correct; no broken/contrast-less chrome.
    7. Mobile check — at ≤640px width (e.g. 390px): the headline | colors split collapses to a single stacked column; rows wrap gracefully.
    8. Capture screenshots desktop + mobile for at least 2 themes (per project UI-verify rule).
    9. Flag-OFF byte-identity — toggle the flag OFF, reload: confirm the card is gone and no stray whitespace/element remains (view source).
  </how-to-verify>
  <resume-signal>Type "approved" (attach/notes the screenshots), or describe rendering/theme/mobile issues to fix.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server-rendered view → browser | Razor emits the card only inside the flag+result guard; no new user input is accepted. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-75-04-I | Information Disclosure / Integrity | Page not byte-identical when flag OFF (stray whitespace/comment) | mitigate | Entire card inside `@if`, no comment/whitespace outside; automated render test (ManabaseViewRenderTests asserts no `manabase-taplens` when OFF) + manual view-source byte check at OFF (Task 4 step 9); Pitfall-3 guard. |
| T-75-04-X | Cross-site scripting | Rendering tap values into HTML | accept/mitigate | All values are server-computed ints/doubles + literal microcopy; Razor `@` auto-encodes; no raw/Html.Raw usage, no user-supplied strings injected. |
| T-75-04-A | Accessibility regression | ✓/⚠ conveyed by color alone | mitigate | Glyph paired with `.sr-only` word + `aria-hidden` on the symbol; `aria-label` on the group (UI-SPEC Section 8). |

No package installs. No high-severity threats; none identified. Theme-fork constraint enforced (Task 1 negative checks).
</threat_model>

<verification>
- `dotnet build DeckFlow.Web/DeckFlow.Web.csproj` → Razor compiles 0/0.
- `grep -nE "manabase-taplens" DeckFlow.Web/Views/Deck/Manabase.cshtml DeckFlow.Web/wwwroot/css/site-common.css` → present in both.
- `dotnet test DeckFlow.Web.Tests --filter "FullyQualifiedName~ManabaseViewRender"` → green (OFF no markup, ON markup present — CI-enforced page invariant).
- Manual checkpoint (Task 4): theme + mobile screenshots; OFF byte-identity confirmed.
- Full suite green before /gsd:verify-work.
</verification>

<success_criteria>
- TAP-01 + TAP-02 visible on /manabase when the flag is ON, using single-source-of-truth values.
- TAP-04 page byte-identity confirmed when the flag is OFF (automated render test ManabaseViewRenderTests enforces OFF=no markup / ON=markup in CI; the manual checkpoint adds theme + mobile + view-source confirmation).
- Card re-skins across Jeskai/Azorius/Nyx and stacks on mobile; no theme-fork edits.
- a11y: grouped, non-color-alone markers, screen-reader words present.
</success_criteria>

<output>
Create `.planning/phases/75-tap-analyzer-surface/75-04-SUMMARY.md` when done.
</output>
