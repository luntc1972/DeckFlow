---
phase: 26-category-cache-schema-normalization-fresh-start
plan: 02
type: execute
wave: 2
depends_on: ["26-01"]
files_modified:
  - DeckFlow.Core/Knowledge/CategoryKnowledgeRepository.cs
  - DeckFlow.Web.Tests/Integration/PostgresStorageTests.cs
  - docs/ops/db-full-reset.md
autonomous: true
requirements: [DBO-01]
must_haves:
  truths:
    - "Every write method (ReplaceSourceRowsAsync, PersistObservedCategoriesAsync, PersistCardDeckTotalsAsync, DeleteSourceDataAsync, HasSourceDataAsync) interns deck identity and card name to integer keys and writes the slim integer-keyed facts"
    - "Every read method (GetCategoriesAsync, GetCategoryRowsForCardAsync, GetCardDeckTotalsAsync, GetCategoryRowsForCommanderAsync) returns IDENTICAL results to the old design, via integer joins (no 'archidekt_live:'||deck_id string-concat)"
    - "The 26-01 RED parity/interning tests are GREEN"
    - "ICategoryKnowledgeStore method signatures are UNCHANGED — the 4 store implementors + 3 inline test fakes need no edits and existing repo tests act as the parity guard"
  artifacts:
    - path: "DeckFlow.Core/Knowledge/CategoryKnowledgeRepository.cs"
      provides: "Integer-keyed write + read paths with intern-on-write memoization (RETURNING id)"
      contains: "RETURNING id"
    - path: "docs/ops/db-full-reset.md"
      provides: "Updated TRUNCATE table list reflecting the new normalized schema (adds cards; notes integer-keyed facts)"
      contains: "cards"
  key_links:
    - from: "GetCategoryRowsForCommanderAsync"
      to: "deck_queue.id = card_category_observations.deck_id"
      via: "integer join replacing string-concat"
      pattern: "q.id = o.deck_id"
    - from: "PersistObservedCategoriesAsync write path"
      to: "cards dimension"
      via: "INSERT ... ON CONFLICT(normalized_card_name) ... RETURNING id"
      pattern: "ON CONFLICT\\(normalized_card_name\\)"
---

<objective>
Port the `CategoryKnowledgeRepository` write and read paths onto the normalized integer-keyed schema created in 26-01, proving byte-identical lookup results (parity) and removing the non-sargable `o.source = 'archidekt_live:' || q.deck_id` string-concat join in favor of an integer `q.id = o.deck_id` join. Then update the full-reset runbook's table list and the Postgres integration test for the new shape.

Purpose: Parity is the central requirement of this phase — every read method must reproduce identical results to today, and every write method must populate the new dimensions + integer-keyed facts. The `ICategoryKnowledgeStore` interface signature stays UNCHANGED (A6), so the 4 implementors + 3 inline fakes need zero edits and the existing repository/PG tests become the parity guard. This is a SQL-only internal change.

Output: Re-pointed read/write SQL with intern-on-write (get-or-insert dimension id via `INSERT ... ON CONFLICT ... RETURNING id`, memoized per write session), GREEN parity tests, updated PG integration test, updated runbook.

This plan completes DBO-01 (write+read port). Codex implements the code AND updates the tests; Claude reviews. NO new packages. Dual-dialect only. Reuse `CardNormalizer.Normalize` and `CategoryFilter.IncludedOrFallback` / `FilterGenericCategoryRowsWithFallback` VERBATIM — parity depends on identical normalization + filtering. Preserve LF, `{ get; init; }`, raw-string SQL indentation; touch only changed lines; no Format Document.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/26-category-cache-schema-normalization-fresh-start/26-RESEARCH.md
@.planning/phases/26-category-cache-schema-normalization-fresh-start/26-01-PLAN.md
@CLAUDE.md

<interfaces>
<!-- The exact methods to port. Extracted from CategoryKnowledgeRepository.cs — do NOT change ICategoryKnowledgeStore signatures (A6). -->

Write methods (key off `source` TEXT today → intern to deck_id INT + card_id INT):
  ReplaceSourceRowsAsync(string source, IReadOnlyList<CategoryKnowledgeRow> rows, string board, int deckCount, ct)
  PersistObservedCategoriesAsync(string source, string cardName, IReadOnlyList<string> categories, int quantity, string board, int deckCountIncrement, ct)
  PersistCardDeckTotalsAsync(string source, string cardName, string board, int deckCountIncrement, ct)
  DeleteSourceDataAsync(string source, ct)
  HasSourceDataAsync(string source, ct) → bool

Read methods (must return IDENTICAL results):
  GetCategoriesAsync(string cardName, ct) → IReadOnlyList<string>          // WHERE normalized=@n GROUP BY category, then CategoryFilter.IncludedOrFallback
  GetCategoryRowsForCardAsync(string cardName, string? boardFilter, ct)    // SUM(count), SUM(deck_count), display card_name; FilterGenericCategoryRowsWithFallback
  GetCardDeckTotalsAsync(string cardName, string? boardFilter, ct)         // card_deck_totals SUM by board
  GetCategoryRowsForCommanderAsync(string commanderName, ct)              // join deck_queue, COUNT(DISTINCT deck), FilterGenericCategoryRowsWithFallback

Caller sites (unchanged — confirm parity): CategorySuggestionService.cs:114 (GetCategoriesAsync), :118 (GetCardDeckTotalsAsync); CommanderCategoryService.cs:54 (GetCategoryRowsForCommanderAsync), :57 (GetCommanderDeckCountAsync — deck_queue only, UNCHANGED).

Unchanged read methods (deck_queue only, no fact join — DO NOT touch their SQL):
  GetCommanderDeckCountAsync, GetPagedProcessedCommanderRowsAsync, GetDistinctProcessedCommanderCountAsync, GetProcessedDeckCountAsync, GetTotalObservationCountAsync (reltuples), GetTopCommandersAsync (in CategoryKnowledgeStore), AddDeckIdsAsync, MarkDeck(s)ProcessedAsync, MarkUrlDeckProcessedAsync, GetNextUnprocessedDeckIdsAsync, crawl_state methods.

Source→deck_id resolution: ArchidektDeckCacheSession builds source = "archidekt_live:{deckId}". The write path must resolve that source string to a deck_queue surrogate `id`. The deck_queue row is created/updated by AddDeckIdsAsync / MarkDeck*ProcessedAsync keyed on the TEXT `deck_id` (e.g. "{deckId}"). NOTE the prefix: facts arrive with `source="archidekt_live:{deckId}"` but deck_queue stores the bare `deck_id`. The port must map source→bare deckId→deck_queue.id consistently (strip the "archidekt_live:" prefix, OR resolve a deck_queue row by the natural key the harvest already uses). Confirm the mapping against ArchidektDeckCacheSession + AddDeckIdsAsync so the commander aggregate join lands.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Port the write path to integer keys (intern-on-write)</name>
  <files>DeckFlow.Core/Knowledge/CategoryKnowledgeRepository.cs</files>
  <read_first>
    - DeckFlow.Core/Knowledge/CategoryKnowledgeRepository.cs (lines 413-634 — ReplaceSourceRowsAsync, DeleteSourceDataAsync, PersistObservedCategoriesAsync, PersistCardDeckTotalsAsync, HasSourceDataAsync; and lines 641-682 AddDeckIdsAsync for the deck_id natural-key idiom)
    - DeckFlow.Core/Knowledge/DeckCategoryCacheWriter.cs (full — the caller; PersistDeckEntriesAsync / ReplaceDeckEntriesAsync drive these methods per group)
    - DeckFlow.Core/Knowledge/ArchidektDeckCacheSession.cs (lines 161-176 — source = "archidekt_live:{deckId}", commander extraction)
    - DeckFlow.Core/Normalization/CardNormalizer.cs (Normalize — reuse verbatim)
    - .planning/phases/26-category-cache-schema-normalization-fresh-start/26-RESEARCH.md (Pattern 1 intern-on-write; Pitfall 2 display-name drift; "Result-Parity: Write methods to port")
  </read_first>
  <behavior>
    - PersistObservedCategoriesAsync writes a fact row keyed by (deck_id INT, card_id INT, category, board), upserting on the UNIQUE grain (deck_id, card_id, category, board): count/deck_count accumulate, last_seen_utc + cards.display_name follow last-writer-wins (matching old `card_name = excluded.card_name`).
    - Card name interned: first write of a normalized name creates one `cards` row; repeat writes reuse the same card_id (memoized in-session via Dictionary<string,long>).
    - Deck source interned: source string resolved to deck_queue.id once per write session.
    - PersistCardDeckTotalsAsync upserts on UNIQUE (deck_id, card_id, board), deck_count accumulates.
    - DeleteSourceDataAsync deletes facts for the resolved deck_id from BOTH fact tables.
    - HasSourceDataAsync returns true iff card_category_observations has a row for the resolved deck_id.
    - ReplaceSourceRowsAsync = delete-by-deck_id then insert (semantics preserved for Phase 27 to gate later).
  </behavior>
  <action>
    Add a private intern-on-write helper pair on the repository:
    - `ResolveCardIdAsync(connection, transaction, cardName, ct)`: `INSERT INTO cards (normalized_card_name, display_name) VALUES (@normalized, @display) ON CONFLICT(normalized_card_name) DO UPDATE SET display_name = excluded.display_name RETURNING id;` — last-writer-wins display_name (Pitfall 2 / A3). Memoize normalized→id in a per-call `Dictionary<string,long>`.
    - `ResolveDeckIdAsync(connection, transaction, source, ct)`: map the `source` string to the bare deck natural key (strip the `archidekt_live:` prefix per ArchidektDeckCacheSession), then upsert/select the deck_queue surrogate `id` via `INSERT INTO deck_queue (deck_id, inserted_utc, processed, skipped, last_checked_utc) VALUES (...) ON CONFLICT(deck_id) DO UPDATE SET ... RETURNING id;` — do NOT clobber processed/commander_name state if the row already exists (use a no-op DO UPDATE that preserves existing columns but RETURNs id). Memoize source→id once per write session (deck id is stable for the whole deck).
    Rewrite the five write methods to use `deck_id INT` + `card_id INT`:
    - PersistObservedCategoriesAsync: resolve card_id + deck_id, then `INSERT INTO card_category_observations (deck_id, card_id, category, board, deck_count, count, last_seen_utc) VALUES (...) ON CONFLICT(deck_id, card_id, category, board) DO UPDATE SET count = card_category_observations.count + excluded.count, deck_count = ... + excluded.deck_count, last_seen_utc = excluded.last_seen_utc;` (display moved to cards — drop the old `card_name = excluded.card_name` from this fact upsert).
    - PersistCardDeckTotalsAsync: same intern, `ON CONFLICT(deck_id, card_id, board)`.
    - ReplaceSourceRowsAsync: DELETE FROM card_category_observations WHERE deck_id = @deckId; then insert rows by integer keys.
    - DeleteSourceDataAsync: resolve deck_id, DELETE both fact tables WHERE deck_id = @deckId.
    - HasSourceDataAsync: resolve deck_id (SELECT id FROM deck_queue WHERE deck_id=@bare — if absent, return false WITHOUT inserting), then SELECT COUNT(1) FROM card_category_observations WHERE deck_id = @deckId.
    Use `RelationalDatabaseConnection.AddParameter` for every value. `RETURNING id` works on SQLite ≥3.35 + Postgres (A7) — already used by FeedbackInsertReturningIdSql. Preserve transaction scoping (BeginTransactionAsync) exactly as today. Do NOT change ICategoryKnowledgeStore or the public method signatures (A6). Preserve raw-string SQL indentation; touch only changed lines.
  </action>
  <verify>
    <automated>"/mnt/c/Program Files/dotnet/dotnet.exe" build DeckFlow.Core/DeckFlow.Core.csproj -c Release</automated>
  </verify>
  <acceptance_criteria>
    - `grep -F 'ON CONFLICT(normalized_card_name)' DeckFlow.Core/Knowledge/CategoryKnowledgeRepository.cs` matches (cards intern)
    - `grep -F 'ON CONFLICT(deck_id, card_id, category, board)' DeckFlow.Core/Knowledge/CategoryKnowledgeRepository.cs` matches (observations grain)
    - `grep -F 'ON CONFLICT(deck_id, card_id, board)' DeckFlow.Core/Knowledge/CategoryKnowledgeRepository.cs` matches (totals grain)
    - `grep -c 'RETURNING id' DeckFlow.Core/Knowledge/CategoryKnowledgeRepository.cs` returns ≥2 (cards + deck_queue resolvers)
    - The write INSERTs into card_category_observations / card_deck_totals no longer name a `source` or `card_name` column: `grep -F 'INSERT INTO card_category_observations (source' DeckFlow.Core/Knowledge/CategoryKnowledgeRepository.cs` returns nothing
    - Every value still bound via AddParameter (no string interpolation of card/commander names): inspect — no `$"...{cardName}"` or `+ cardName` inside CommandText
    - Public method signatures unchanged: `git diff DeckFlow.Core/Knowledge/CategoryKnowledgeRepository.cs` shows no change to any `public async Task...` signature line for the ported methods
    - Build clean: 0 new warnings, 0 errors
  </acceptance_criteria>
  <done>All five write methods intern deck+card to integer keys and write slim facts via grain upserts; memoization in place; signatures unchanged; build clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Port the read path to integer joins + prove parity GREEN</name>
  <files>DeckFlow.Core/Knowledge/CategoryKnowledgeRepository.cs, DeckFlow.Core.Tests/CategoryCacheSchemaParityTests.cs</files>
  <read_first>
    - DeckFlow.Core/Knowledge/CategoryKnowledgeRepository.cs (lines 209-324 — GetCategoriesAsync, GetCategoryRowsForCardAsync, GetCategoryRowsForCommanderAsync; lines 574-611 GetCardDeckTotalsAsync; lines 931-943 FilterGenericCategoryRowsWithFallback)
    - DeckFlow.Core.Tests/CategoryCacheSchemaParityTests.cs (the 26-01 RED tests — turn GREEN)
    - DeckFlow.Core.Tests/CategoryKnowledgeRepositoryTests.cs (existing parity-guard tests — must stay/become green)
    - DeckFlow.Core/Reporting/CategoryFilter.cs (IncludedOrFallback — reuse verbatim)
    - .planning/phases/26-category-cache-schema-normalization-fresh-start/26-RESEARCH.md (Code Examples — ported SQL; Result-Parity: Read methods to port; Pitfall 2)
  </read_first>
  <behavior>
    - GetCategoriesAsync: join cards on normalized name, facts by card_id, GROUP BY category, ORDER BY LOWER(category), category; then CategoryFilter.IncludedOrFallback — identical to old output for the Sol Ring sample.
    - GetCategoryRowsForCardAsync: SELECT category, cards.display_name AS card_name, SUM(count), SUM(deck_count); optional board filter; FilterGenericCategoryRowsWithFallback unchanged.
    - GetCardDeckTotalsAsync: join cards by normalized name → card_deck_totals by card_id, SUM(deck_count) GROUP BY board; identical CardDeckTotals shape.
    - GetCategoryRowsForCommanderAsync: integer join `JOIN deck_queue q ON q.id = o.deck_id JOIN cards c ON c.id = o.card_id WHERE LOWER(q.commander_name)=LOWER(@commanderName) AND q.processed=1`, COUNT(DISTINCT q.id) AS deck_total — the string-concat join is GONE.
    - All four 26-01 parity/interning tests pass; existing CategoryKnowledgeRepositoryTests pass.
  </behavior>
  <action>
    Rewrite the four read-method SQL bodies to join the dimensions by integer key, reusing the existing C# row-materialization + `CategoryFilter`/`FilterGenericCategoryRowsWithFallback` logic verbatim (only the SQL string and any column-ordinal reads change). Use the RESEARCH "Code Examples" SQL as the template:
    - GetCategoriesAsync: `FROM card_category_observations o JOIN cards c ON c.id = o.card_id WHERE c.normalized_card_name = @normalized GROUP BY o.category ORDER BY LOWER(o.category), o.category`.
    - GetCategoryRowsForCardAsync: select `c.display_name AS card_name` (Pitfall 2 — read returns the interned display name; the parity sample must still match); keep the `{0}` board-filter template + the `AND o.board = @board` clause; GROUP BY o.category, c.display_name.
    - GetCardDeckTotalsAsync: `FROM card_deck_totals t JOIN cards c ON c.id = t.card_id WHERE c.normalized_card_name = @normalized {boardFilter} GROUP BY t.board`.
    - GetCategoryRowsForCommanderAsync: the integer-join form above; `COUNT(DISTINCT q.id)`.
    Then ensure the 26-01 RED tests pass: run them (or push-and-watch CI if the WSL xUnit runner is flaky). If a parity test reveals a real divergence (e.g. display-name drift, ordering), FIX the SQL — do NOT weaken the test. Keep every value parameterized via AddParameter; the only `string.Format`/interpolation permitted is the constant board-filter fragment (V5 boundary — preserve it). Preserve raw-string SQL indentation; touch only changed lines.
  </action>
  <verify>
    <automated>"/mnt/c/Program Files/dotnet/dotnet.exe" build DeckFlow.sln -c Release</automated>
  </verify>
  <acceptance_criteria>
    - `grep -F 'q.id = o.deck_id' DeckFlow.Core/Knowledge/CategoryKnowledgeRepository.cs` matches (integer commander join)
    - The string-concat join is gone: `grep -F "'archidekt_live:' || q.deck_id" DeckFlow.Core/Knowledge/CategoryKnowledgeRepository.cs` returns nothing AND `grep -F 'archidekt_live' DeckFlow.Core/Knowledge/CategoryKnowledgeRepository.cs` returns nothing inside read SQL (the prefix may legitimately appear only in the write-path source→deckId mapping)
    - `grep -F 'JOIN cards c ON c.id = o.card_id' DeckFlow.Core/Knowledge/CategoryKnowledgeRepository.cs` matches
    - `grep -F 'c.display_name' DeckFlow.Core/Knowledge/CategoryKnowledgeRepository.cs` matches (interned display name on read)
    - The 4 parity tests in CategoryCacheSchemaParityTests pass AND CategoryKnowledgeRepositoryTests pass — run `"/mnt/c/Program Files/dotnet/dotnet.exe" test DeckFlow.Core.Tests/DeckFlow.Core.Tests.csproj --filter "FullyQualifiedName~CategoryCacheSchemaParityTests|FullyQualifiedName~CategoryKnowledgeRepositoryTests"` (targeted filter works in this WSL per project memory); if the runner is uncooperative, push-and-watch CI is the gate and the SUMMARY records the CI run id
    - Full solution build clean: 0 new warnings, 0 errors
  </acceptance_criteria>
  <done>Read methods use integer joins; string-concat join removed; interned display_name returned; all parity + existing repo tests GREEN (or CI-confirmed); solution build clean.</done>
</task>

<task type="auto">
  <name>Task 3: Update PG integration test + full-reset runbook for the new schema</name>
  <files>DeckFlow.Web.Tests/Integration/PostgresStorageTests.cs, docs/ops/db-full-reset.md</files>
  <read_first>
    - DeckFlow.Web.Tests/Integration/PostgresStorageTests.cs (lines 80-160 — CategoryKnowledgeRepository_CrudAndDeckQueue_Roundtrips and the deck_queue roundtrip test)
    - docs/ops/db-full-reset.md (lines 49-72 — the TRUNCATE table list + the "table set changes" note + the indexes note)
    - .planning/phases/26-category-cache-schema-normalization-fresh-start/26-RESEARCH.md (Runtime State Inventory — "the runbook's TRUNCATE list ... must be updated"; State of the Art — cat-01 doc superseded)
  </read_first>
  <action>
    1. PostgresStorageTests: confirm the existing `CategoryKnowledgeRepository_CrudAndDeckQueue_Roundtrips` test (which already writes Sol Ring via ReplaceSourceRowsAsync and reads via GetCategoriesAsync / GetCategoryRowsForCardAsync / GetCardDeckTotalsAsync) still asserts correct results against the NEW schema. If the source→deck_id resolution requires a deck_queue row to exist for the `source` used (e.g. "test:..."), seed/adjust the test's source string and/or pre-create the deck_queue row so the integer join resolves — WITHOUT weakening the assertions. Add one `[PostgresFact]` asserting `GetCategoryRowsForCommanderAsync` returns the aggregate via the integer join on Postgres (Docker-gated; auto-skips without a container). Keep `[PostgresFact]` gating so it skips cleanly in WSL.
    2. docs/ops/db-full-reset.md: update the `TRUNCATE TABLE` list to the post-redesign table set — add `cards`; keep `card_category_observations`, `card_deck_totals`, `deck_queue`, `crawl_state` and the rest. Update the parenthetical note to state the category facts are now integer-keyed (deck_id/card_id) and the `cards` dimension is new. Update the indexes note to confirm `EnsureSchemaAsync` now creates all category indexes on a clean DB (cat-01-suggestion-indexes.md out-of-band build is superseded for the new schema). This is a planning/ops doc edit (allowed per CLAUDE.md — not infra config). Do NOT run the wipe; do NOT touch render.yaml / Dockerfile / fly.toml.
  </action>
  <verify>
    <automated>"/mnt/c/Program Files/dotnet/dotnet.exe" build DeckFlow.Web.Tests/DeckFlow.Web.Tests.csproj -c Release</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE '^\s*cards,' docs/ops/db-full-reset.md` matches (cards added to TRUNCATE list)
    - `grep -iF 'integer-keyed' docs/ops/db-full-reset.md` OR `grep -iF 'deck_id' docs/ops/db-full-reset.md` matches the updated note
    - PostgresStorageTests builds clean and the new commander `[PostgresFact]` exists: `grep -F 'GetCategoryRowsForCommanderAsync' DeckFlow.Web.Tests/Integration/PostgresStorageTests.cs` matches
    - `[PostgresFact]` gating preserved (test skips without Docker, does not fail WSL build/run)
    - render.yaml / Dockerfile / fly.toml unchanged: `git diff --name-only | grep -E 'render.yaml|Dockerfile|fly.toml'` returns nothing
    - Test project build clean
  </acceptance_criteria>
  <done>PG integration test covers the new integer-keyed schema (incl. commander aggregate) and skips cleanly without Docker; runbook TRUNCATE list + notes reflect the normalized schema; no infra files touched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| harvest/admin → DB write path | card/commander names flow into the new intern-on-write SQL |
| admin/user lookup → DB read path | card/commander names flow into the new integer-join read SQL |
| ops → live Postgres (Step A wipe) | destructive reset; user-run per runbook |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-05 | Tampering (SQL injection) | ported read/write SQL | mitigate | All card/commander/source values bound via `RelationalDatabaseConnection.AddParameter`; the only `string.Format`/interpolation is the constant board-filter fragment (no user data) — preserved exactly (RESEARCH V5 boundary) |
| T-26-06 | Information Disclosure (parity regression returning wrong card data) | read-path port | mitigate | Fixed-sample parity tests (Sol Ring + commander) + reused CardNormalizer/CategoryFilter verbatim guarantee identical results; display-name last-writer-wins documented (A3); divergence fixes the SQL, never weakens the test |
| T-26-07 | Repudiation/Integrity (no FK = orphan facts) | integer-keyed facts without FK | accept | A2 decision: integrity maintained in code (intern-on-write always resolves a real dimension id before writing a fact); uniform dual-dialect behavior preferred over per-dialect FK enforcement divergence |
| T-26-08 | DoS (data wipe window) | runbook Step A | accept | Empty-cache window authorized; user runs the wipe; backup mandated first |
</threat_model>

<verification>
- `"/mnt/c/Program Files/dotnet/dotnet.exe" build DeckFlow.sln -c Release` clean.
- 26-01 parity/interning tests GREEN; existing CategoryKnowledgeRepositoryTests GREEN (parity guard).
- Commander read uses `q.id = o.deck_id` integer join; no `'archidekt_live:'||deck_id` in read SQL.
- ICategoryKnowledgeStore signatures unchanged → 4 implementors + 3 inline fakes need no edits.
- Runbook TRUNCATE list updated (adds `cards`; notes integer-keyed facts); no infra files touched.
- PG integration test covers the new schema incl. commander aggregate (Docker-gated, skips in WSL).
- EXPLAIN index-scan evidence (SC #3) is captured POST-deploy by the user on a re-harvested Postgres sample (`EXPLAIN (ANALYZE, BUFFERS) ...` per RESEARCH Code Examples) — out-of-band manual gate, recorded in the SUMMARY as a follow-up, since it requires live re-harvested prod data.
</verification>

<success_criteria>
- All five write methods intern deck+card to integer keys; all four fact-joined read methods return identical results via integer joins.
- 26-01 RED parity tests are GREEN; existing repo tests stay GREEN.
- String-concat commander join eliminated.
- Interface signatures unchanged (SQL-only change); fakes/implementors untouched.
- Runbook updated; PG integration test updated; no infra config changed.
- Solution build clean (except known AdminCssPhase1Tests debt, which is unrelated Web.Tests CSS debt).
</success_criteria>

<output>
Create `.planning/phases/26-category-cache-schema-normalization-fresh-start/26-02-SUMMARY.md` when done.
</output>
