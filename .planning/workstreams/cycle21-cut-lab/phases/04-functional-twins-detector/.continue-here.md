---
context: phase
workstream: cycle21-cut-lab
phase: 04-functional-twins-detector
plan: 4
total_plans: 4
task: 3
total_tasks: 3
status: paused
activity: all four plans coded and committed; only 04-04 Task 3 (human UI checkpoint) remains. Phase 4 is NOT closed.
branch: gsd/cycle21-cut-lab
head: 778995a5
last_updated: 2026-08-02T20:31:50.771Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> Each was discovered through actual failure. Acknowledge each explicitly before proceeding.

- [ ] CONSTRAINT: **Executor pass-claims are not evidence.** Self-reported test state has now been
      wrong on **three consecutive dispatches** in this phase. Re-run every affected suite yourself
      and quote the real `Failed: N, Passed: N` line before committing.
- [ ] CONSTRAINT: **Passing tests do not prove a fixture measures the right population.** The 04-04
      density fixture declared 130 cards and passed all 6 tests while only **28** were eligible —
      64 carried `roles: []`, which the detector's `Roles.Contains(roleKey)` filter can never select.
      Count the entries that survive the production filter, and prove a numeric guard by **mutating
      the guarded constant** until it breaches.
- [ ] CONSTRAINT: **The flag-OFF panel legitimately differs from pre-Phase-4.** 04-04 Task 1
      deliberately replaced `BuildFindingGroups`' capture-index-then-insert scheme, fixing a
      pre-existing reversal of `WeakFloorCase`/`ComboProtected` order. That fix is
      **flag-independent**. When verifying OFF, check for "no twins output" — **never** for "exactly
      the pre-Phase-4 panel". An earlier plan draft said the latter, and a human following it would
      have reported a correct build as broken.
- [ ] CONSTRAINT: **A stale Windows-held server on port 5173 silently voids a UI verification.**
      WSL's `ss` cannot see it; it serves pre-Phase-4 code while looking healthy. Probe with
      `netstat` from a **Windows** shell and kill it before starting, every time.
- [ ] CONSTRAINT: **The production flag stays OFF.** Turn `analysis.cut-lab.functional-twins` on via
      `/Admin/Flags` **locally only**. Do not edit seed SQL, do not flip the production row. This
      phase deploys dark; flipping prod is a separate post-UAT developer decision.
- [ ] CONSTRAINT: **Do not hand-fold review findings.** Three hand-folds introduced new defects,
      including the Phase 5 round-8 TRX regex whose trailing wildcard opened the hole round 9 then
      flagged. Dispatch folds unless the edit is a single literal substitution verifiable by grep.
- [ ] CONSTRAINT: **Never let two full-access Codex processes share this worktree.** Read-only
      reviews may run concurrently with an execution dispatch; writers may not.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Executor reports success it did not verify | 04-02's dispatch reported "filtered structural-findings suite passed" when 3/42 failed; 04-01's reported a CRLF `grep -Fxc` gate failure that does not reproduce | blocking | Re-run the suite yourself and quote the actual `Failed: N, Passed: N` into the commit body. Cross-check the suite total moved by exactly the planned test count |
| Fixture bounds the wrong population | 04-04's density fixture: 130 declared entries, 64 with empty roles, 38 lands — only 28 eligible. All 6 tests passed. The bound would have stayed green through the exact regression it was written to catch | blocking | Count entries surviving the production filter, not entries declared. Prove the guard by mutating the guarded constant — `TwinGroupMinimumCards` 3→2 now yields 14 findings against a bound of 12 |
| One-sided string parity | 04-04 required the AJAX help note to be character-identical to the Razor one; mutating the **Razor** copy alone passed all 633 CutLab C# tests and all vitest tests | advisory (fixed) | Pin **both** sides or neither is pinned |
| Truncated xUnit output disguises a test defect as a production defect | Two failures printed Expected and Actual as visually identical with the differ marker at pos 0 — the real difference lived inside the `···` truncation | blocking | When both sides project to the same type and still differ, diff the raw strings before touching production code |
| Over-broad formatting rules corrupt runtime strings | A brief's "ASCII only, no em dashes" rule (aimed at prose) produced `\\u2014` double-escapes in test expectations | advisory | Scope the rule: it governs prose, comments and markdown. A C# escape producing a required runtime character is correct |
| Asserting existing behavior works without a consumer census | The plan claimed twin evidence chips get combo badges "for free"; they never rendered at all, and three broken readers surfaced one per review round across rounds 8, 9, 10 | blocking | Before any "X already does Y" claim, grep the structure's name across C#, Razor, TypeScript and the test projects; put the census in the plan as reader → file:line → works/broken |
| Gates that cannot distinguish success from absence | Five inverted gates this milestone, plus 04-04's Task 1 verify command which **cannot exit 0** because it runs vitest from the repo root where no config exists | advisory | Assert the gate's *input* before trusting its verdict — a named test with `outcome="Passed"`, a non-empty staged set, each planned name present exactly once |
| Trusting the plan's cited line numbers | 04-04-PLAN.md's anchors were written 2026-07-29; the branch was rebased since and three had drifted | advisory | Grep for the construct; never trust a cited line number in this plan |

<current_state>
**Phase 4 is code-complete and green, but NOT closed.** All four plans (04-01 … 04-04) are coded and
committed. The single outstanding item is **04-04 Task 3**, a `checkpoint:human-verify` gate that is
Success Criterion 5's only authoritative check.

Worktree clean apart from pre-existing untracked `_edhrec-brackets/` and `_role-floor-research/`
(**never stage these**). Branch `gsd/cycle21-cut-lab` at `778995a5`, **3 ahead of `main`**.

Phase 5 and Phase 6 state is carried forward from the previous handoff and was **not re-verified
this session** — see Remaining Work.
</current_state>

<completed_work>

**Earlier sessions**
- 04-01 — dedicated `analysis.cut-lab.functional-twins` flag seeded OFF in both SQL dialects;
  `CutLabAnalyzedCard` gains `TypeLine`, `IsLocked`, `IsCommander`; scope-fence regression proving
  the five existing detectors are unchanged.
- 04-02 — the `FunctionalTwins` kind and detector: groups unlocked, non-commander, non-land cards by
  role ∩ exact mana value ∩ primary card type; fires at 3+ distinct normalized identities; findings
  emitted highest mana value first. 21 mutation-proofed tests.
- 04-03 (`b508f27e`) — engine wiring, flag read on all three transports, D-23 `FunctionalTwins`-only
  normalized tally join with raw-name dedupe.
- Phase 4 convergence rounds 8-12; Phase 5 round-8 fold; GSD/Codex config repair.

**This session (2026-08-02)**
- 04-04 Task 1 (`9594b2f6`) — merged twins section; `BuildFindingGroups` rewritten to a single-pass
  placeholder build; twins help note in Razor and `cut-lab.ts`; both Razor readers normalize at
  lookup; AJAX DTO re-keyed to raw pool names (D-24). 9 files, +502/−81.
- 04-04 Task 2 (`0a9e7cc9`) — diverse 130-card density fixture, 6 tests.
- 04-04 summary (`778995a5`) — density evidence and owed gates.

**Gates at pause, all re-run independently rather than taken from worker reports:** Release build
0 errors / 9 pre-existing `CS8629` in `ManabaseBaselineWeightingTests.cs`; `DeckFlow.Web.Tests`
**2286 passed / 0 failed / 16 skipped** (2266 → **+20**, exactly the tests added); `DeckFlow.Core.Tests`
2011/2011; vitest 3/3; every touched file LF with `CR=0` on both sides.

**Density evidence:** 130 entries, 38 lands (zero twin groups), 92 eligible role-bearing non-lands,
15 multi-role. Output **6 findings / 20 distinct evidence cards** against bounds 12 / 40. The
homogeneous control fires **fewer** groups (3) but **65** evidence cards — which is exactly why the
bound is on distinct evidence names rather than group count.
</completed_work>

<remaining_work>

**Phase 4 — the only thing blocking closure**
- **04-04 Task 3** — the human UI checkpoint. Steps 0-7 in `04-04-PLAN.md`. `autonomous: false`.

**Owed but not blocking Phase 4's code**
- **Codex review of `9594b2f6..0a9e7cc9`** — `gpt-5.6-sol`, medium, **stage-2 `codex exec` with a
  written brief** (not just `codex review`), triggered on 3 of its 4 conditions. Second consecutive
  Phase 4 plan not Codex-written.
- **Two corrections to `04-04-PLAN.md` itself** (plan text, not code defects) — see Blockers.

**Carried forward from the previous handoff — NOT re-verified this session**
- **Phase 5 round-9 HIGH** — replace the permissive TRX regex with exact `FullyQualifiedName=`
  filters plus per-name counters and a total, then dispatch round 10. ⚠ `05-REVIEWS.md` still ends at
  **Round 8**, so this is either open or was fixed without being written up. **Confirm before acting.**
- **Phase 6** — only `06-CONTEXT.md` exists (verified). Still needs `06-RESEARCH.md`,
  `06-VALIDATION.md`, the wave plans, then Codex plan review.
- **Ratify `SCRY-01..04` into `REQUIREMENTS.md`** before milestone closeout — **verified still absent
  (0 hits)**. Phases 01.1 and 01.2 also lack requirement IDs.
</remaining_work>

<decisions_made>

**This session**
- **Claude authored 04-04 instead of Codex**, with a Codex review recorded as an OWED gate. Codex was
  out of credits; the user was told and explicitly chose this over pausing. No silent fallback, and
  no fallback to the local Ollama tier (never an auto-fallback, and it cannot edit files or build).
- **Committed before the human checkpoint**, on the user's instruction "commit work" — overriding the
  default of holding functional changes until manually tested.
- **Fixed the pre-existing merged-section ordering reversal** rather than preserving it through the
  rewrite. Flag-independent, locked by a dedicated no-twins regression test.
- **Density bound set to `<= 12`** where the plan's action text literally says `<= 8`. Measured is 6;
  the same plan sentence says "actual count plus generous headroom". Disclosed, not silent.
- **Renamed `Bramble Elemental` to synthetic `Thicket Golem Alpha`** rather than correcting its mana
  value — the real card is cmc 5, and changing the fixture to 5 would have destroyed the load-bearing
  `TypeGroupOrder` tie that test 4 depends on.

**Earlier, still binding**
- **D-24** — each combo-badge transport keys for its own consumer. The AJAX DTO re-keys onto raw pool
  names server-side under `StringComparer.Ordinal`; Razor keeps normalized keys and normalizes at
  lookup. A TypeScript port of `CardNormalizer.Normalize` was rejected — duplicating it in a second
  language with nothing binding the two would silently recreate this defect class.
- **Phase 4 scope widened** to repair three pre-existing combo-badge readers; the badge never
  rendered anywhere.
- **F-04-07 recorded, not fixed** — `CardTextByCardName` carries the same case-collapse hazard but is
  pre-existing, built by two builders, and read server-side.
- **F-04-08 newly recorded** — `renderStructuralFindings` emits no `EnablerStarved` help note, so that
  section loses it after an AJAX patch while the Razor render keeps one. Follow-up count is now seven.
- **Phase 6 wave 1 superseded** — 500ms *is* Scryfall's documented per-endpoint limit, so the fix is
  per-endpoint gate state (14 call sites, 8 services), not restoring 200ms.
- **Phase 6 wave 2 scoped** to `SearchFallbackCardAsync`; the printing fallback is not batchable
  without redesigning its matching semantics.
</decisions_made>

<blockers>

- **Codex out of credits** — probed 2026-08-02: `ERROR: Your workspace is out of credits.`
  Deterministic, not transient; do not retry as if a retry could help.
- **04-04 Task 3 needs a human** — no workaround; that is the gate by design. Ask permission before
  opening any browser or Playwright session.
- **Two errors in `04-04-PLAN.md` itself** (plan text, not code):
  1. Task 1's `<verify><automated>` command **cannot exit 0 as written** — it runs vitest from the
     repo root, but the config lives at `DeckFlow.Web/vitest.config.ts`. Proven with an untouched
     control file that fails identically. Working form:
     `cd DeckFlow.Web && npx --no-install vitest run ts-tests/<file>`.
  2. It describes the adjust-patch dictionary's comparer as `OrdinalIgnoreCase`; it is actually
     `CutLabCardNames.Comparer`, which **already is** `StringComparer.Ordinal`
     (`CutLabCardNames.cs:7`). The prescribed change is therefore a no-op rename, and
     `BuildComboBadgeByCardName_PreservesDistinctRawPoolNamesThatDifferOnlyByCase` proves the
     pool-iteration re-key rather than a comparer flip.
- **Phase 5 round-9 HIGH** — status uncertain, see Remaining Work. Confirm before acting.

</blockers>

## Required Reading (in order)

1. `.planning/HANDOFF.json` — machine-readable state, blockers, decisions.
2. `04-04-PLAN.md`, the `<task type="checkpoint:human-verify">` element — Task 3's steps 0-7. This is
   the work.
3. `04-04-SUMMARY.md` — what shipped, the measured density numbers, everything still owed.
4. `.foreman/ledger.md` (gitignored, local to this worktree) — the orchestration record: seats, the
   11 blind-verifier mutations, and the LEAD-independent gate results.
5. `CLAUDE.md` — the UI-testing constraints governing Task 3: never open a browser on the Windows
   host; use `scripts/run-web-test.sh`; Playwright headless from WSL with `env -u DISPLAY`; never the
   `gstack` daemon (observed unstable, will not follow form-POST navigation).

## Infrastructure State

- **No server running.** Nothing was started this session; Task 3 will be the first.
- **Codex CLI** reachable but the workspace is **out of credits**. Re-probe with
  `codex exec -m gpt-5.6-terra -s read-only --ephemeral` before assuming it is back. Seats when it
  returns: `gpt-5.6-terra` coding, `gpt-5.6-sol` review; both configs verified correct.
- **Git**: branch `gsd/cycle21-cut-lab` at `778995a5`, 3 ahead of `main`, clean. `.foreman/` is
  gitignored at this tip, so the ledger is local only and was not committed.
- Untracked `_edhrec-brackets/` and `_role-floor-research/` — **never stage these**; always use
  explicit `git add -- <paths>`.
- Docker was down earlier, skipping 16 Postgres integration tests; the 16 skips persist and are
  expected, not a regression. (Note: `project_cycle21_full_milestone_plan` records that starting
  Docker Desktop — **not** WSL integration — is the whole prerequisite, since the test host is a
  Windows `dotnet.exe` process.)
- `dotnet` is Windows: `"/mnt/c/Program Files/dotnet/dotnet.exe"`. **Do not set `MTG_DATA_DIR`.**
- vitest must run from `DeckFlow.Web/`, never the repo root.
- **Production `analysis.cut-lab.functional-twins` is OFF and must stay OFF.**

<context>
The interesting work this session was not the feature — the plan specified it thoroughly — but the
verification. Two workers and an independent LEAD gate re-run all reported green on a density fixture
that was measuring a 28-card population while claiming 130. Only the blind verifier, running its own
11 mutations rather than grading the reports, caught it. The fix was then proven the only way that
means anything: mutate the guarded constant and watch the bound break (3→2 → 14 findings vs a bound
of 12).

Carry that into Task 3. The automated bound cannot answer "is this reviewable" — that is *why* Task 3
exists, and it is a judgement about a real deck, not a number. If the density reads as noise, report
it and propose a specific tightening (raise `TwinGroupMinimumCards` to 4, or cap emitted groups) for
the developer to decide. **Do not loosen the automated bound to match a noisy result** — the plan
forbids it explicitly, and threshold tuning is a product decision, not an executor one.
</context>

<next_action>
Probe port 5173 from a **Windows** shell (`netstat`) and kill any stale server — WSL's `ss` is blind
to it and a stale server serves pre-Phase-4 code, silently invalidating everything that follows. Then
`scripts/run-web-test.sh` (sets `DECKFLOW_DISABLE_AUTO_BROWSER=true`), turn
`analysis.cut-lab.functional-twins` ON via `/Admin/Flags` **locally only**, and work
`04-04-PLAN.md` Task 3 steps 0-7 in order. Ask the developer before opening any browser or Playwright
session.
</next_action>

## Resume

Run `/gsd-resume-work`, or start directly at `<next_action>` above.
