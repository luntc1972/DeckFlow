---
phase: 72-command-zone-commander-castability
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - DeckFlow.Web/Services/FeatureFlags/FeatureFlagStore.cs
  - DeckFlow.Web/Services/FeatureFlags/FeatureFlagCatalog.cs
  - DeckFlow.Web.Tests/FeatureFlagCatalogTests.cs
  - DeckFlow.Web.Tests/FeatureFlagStoreSeedTests.cs
  - DeckFlow.Web/Services/Manabase/ManabaseAnalysisService.cs
autonomous: true
requirements: [G-flag]
must_haves:
  truths:
    - "A new feature flag manabase.commander-castability exists, seeded OFF in both Postgres and SQLite dialects"
    - "The flag has an operator-facing catalog description"
    - "FeatureFlagCatalogTests and FeatureFlagStoreSeedTests both green (no CI break from the 4-file sync)"
  artifacts:
    - path: "DeckFlow.Web/Services/FeatureFlags/FeatureFlagStore.cs"
      provides: "Seed of manabase.commander-castability = FALSE/0 in both PostgresSeedSql and SqliteSeedSql"
      contains: "manabase.commander-castability"
    - path: "DeckFlow.Web/Services/FeatureFlags/FeatureFlagCatalog.cs"
      provides: "Operator description for the new flag key"
      contains: "manabase.commander-castability"
    - path: "DeckFlow.Web/Services/Manabase/ManabaseAnalysisService.cs"
      provides: "public const string CommanderCastabilityFlagKey"
      contains: "CommanderCastabilityFlagKey"
  key_links:
    - from: "FeatureFlagStore seed SQL"
      to: "FeatureFlagStoreSeedTests InlineData"
      via: "seed-consistency test"
      pattern: "manabase.commander-castability"
    - from: "FeatureFlagCatalog.Descriptions"
      to: "FeatureFlagCatalogTests InlineData"
      via: "describe-returns-nonempty test"
      pattern: "manabase.commander-castability"
---

<objective>
Register the `manabase.commander-castability` feature flag as a four-file synchronized
change, seeded OFF in both dialects, and declare its key constant. This is the [BLOCKING]
foundation for every flag-gated behavior in Phase 72. Flag OFF = prod byte-identical.

Purpose: A missed file in the 4-file sync fails CI (RESEARCH Pitfall 3). Land it atomically
and isolated so downstream waves consume a known-good flag.
Output: Seeded OFF flag + catalog description + key constant; both flag test classes green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/72-command-zone-commander-castability/72-RESEARCH.md
@.planning/phases/72-command-zone-commander-castability/72-SPEC.md

<interfaces>
<!-- Existing flag pattern the executor mirrors. Verified file:line. -->
- FeatureFlagStore.cs: PostgresSeedSql ends with `('manabase.plain-language-verdict', FALSE)` then `ON CONFLICT (key) DO NOTHING;` (~:176); SqliteSeedSql ends with `('manabase.plain-language-verdict', 0)` (~:206). Add the new row to BOTH, preserving the trailing ON CONFLICT clause.
- FeatureFlagCatalog.cs: `Descriptions` dictionary; `["manabase.plain-language-verdict"] = "..."` (~:65). Add a new entry.
- FeatureFlagCatalogTests.cs: `[InlineData("manabase.plain-language-verdict")]` (~:40) on the describe-returns-nonempty theory.
- FeatureFlagStoreSeedTests.cs: `[InlineData("manabase.plain-language-verdict", false)]` (~:38) on the seed-default theory.
- ManabaseAnalysisService.cs: existing `public const string PlainLanguageVerdictFlagKey = "manabase.plain-language-verdict";` (~:162). Add the new const beside it.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Four-file flag registration (atomic)</name>
  <files>DeckFlow.Web/Services/FeatureFlags/FeatureFlagStore.cs, DeckFlow.Web/Services/FeatureFlags/FeatureFlagCatalog.cs, DeckFlow.Web.Tests/FeatureFlagCatalogTests.cs, DeckFlow.Web.Tests/FeatureFlagStoreSeedTests.cs</files>
  <read_first>
    - DeckFlow.Web/Services/FeatureFlags/FeatureFlagStore.cs (PostgresSeedSql + SqliteSeedSql blocks, ~:150-207)
    - DeckFlow.Web/Services/FeatureFlags/FeatureFlagCatalog.cs (Descriptions dictionary)
    - DeckFlow.Web.Tests/FeatureFlagCatalogTests.cs (InlineData theory)
    - DeckFlow.Web.Tests/FeatureFlagStoreSeedTests.cs (InlineData seed-default theory)
  </read_first>
  <action>
    Add the key `manabase.commander-castability` in all four places as ONE atomic change (RESEARCH
    Pitfall 3 — any miss fails CI): (1) PostgresSeedSql new row `('manabase.commander-castability', FALSE)`
    inserted before the closing `ON CONFLICT (key) DO NOTHING;`; (2) SqliteSeedSql new row
    `('manabase.commander-castability', 0)` likewise; (3) FeatureFlagCatalog.Descriptions add an entry
    keyed `manabase.commander-castability` describing it as: shows command-zone castability — individual
    cast probability for each commander/partner/background, plus (Casual only) a companion's on-curve
    chance including the +3 generic "to hand" rule tax (a heuristic); off by default. Use plain ASCII
    hyphens, no em/en dashes. (4) Add `[InlineData("manabase.commander-castability")]` to
    FeatureFlagCatalogTests and `[InlineData("manabase.commander-castability", false)]` to
    FeatureFlagStoreSeedTests. Preserve LF endings and existing SQL raw-string indentation (do NOT
    re-indent the raw-string literals — CLAUDE.md carve-out).
  </action>
  <verify>
    <automated>"/mnt/c/Program Files/dotnet/dotnet.exe" test DeckFlow.Web.Tests --filter "FullyQualifiedName~FeatureFlagCatalogTests|FullyQualifiedName~FeatureFlagStoreSeedTests"</automated>
  </verify>
  <acceptance_criteria>
    - Both FeatureFlagCatalogTests and FeatureFlagStoreSeedTests pass with the new InlineData rows.
    - `grep -c "manabase.commander-castability"` across the four files returns >= 1 in EACH file.
    - The new seed row is FALSE (Postgres) / 0 (SQLite); the seed-default test asserts `false`.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Declare CommanderCastabilityFlagKey constant</name>
  <files>DeckFlow.Web/Services/Manabase/ManabaseAnalysisService.cs</files>
  <read_first>
    - DeckFlow.Web/Services/Manabase/ManabaseAnalysisService.cs (the block of existing `public const string *FlagKey` declarations, ~:118-162; the `IsFlagOn` helper, ~:270-273)
  </read_first>
  <action>
    Add `public const string CommanderCastabilityFlagKey = "manabase.commander-castability";` beside the
    existing FlagKey constants, with an XML-doc summary noting it is seeded OFF and gates the command-zone
    castability callout + companion modeling (Casual). Do NOT add any read of the flag here yet (the gate
    behavior lands in plan 72-05); this task only publishes the constant so downstream plans reference it
    without a literal string. No behavior change — flag-OFF analysis stays byte-identical.
  </action>
  <verify>
    <automated>"/mnt/c/Program Files/dotnet/dotnet.exe" build DeckFlow.Web/DeckFlow.Web.csproj</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "CommanderCastabilityFlagKey" DeckFlow.Web/Services/Manabase/ManabaseAnalysisService.cs` returns >= 1.
    - Build clean (0 new warnings); no flag read added in AnalyzeAsync this plan.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| operator → flag store | Admin toggles the flag; seed is server-controlled |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-72-01 | Tampering | Flag seed default | mitigate | Seed FALSE/0 in both dialects; seed-default test asserts `false` so an accidental ON cannot ship |
| T-72-SC | Tampering | npm/nuget installs | n/a | No package installs in this plan |
</threat_model>

<verification>
- `"/mnt/c/Program Files/dotnet/dotnet.exe" test DeckFlow.Web.Tests --filter "FullyQualifiedName~FeatureFlag"` green.
- Build clean.
</verification>

<success_criteria>
- Flag `manabase.commander-castability` seeded OFF in both dialects; catalog description present;
  both flag test classes green; key constant published. Flag-OFF behavior byte-identical.
</success_criteria>

<output>
Create `.planning/phases/72-command-zone-commander-castability/72-01-SUMMARY.md` when done.
</output>
