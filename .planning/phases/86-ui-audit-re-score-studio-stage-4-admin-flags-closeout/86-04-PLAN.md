---
phase: 86-ui-audit-re-score-studio-stage-4-admin-flags-closeout
plan: 04
type: execute
wave: 4
depends_on: [86-03]
files_modified:
  - DeckFlow.Web/wwwroot/css/site.css
  - DeckFlow.Web/wwwroot/css/site-abzan.css
  - DeckFlow.Web/wwwroot/css/site-bant.css
  - DeckFlow.Web/wwwroot/css/site-commander-table.css
  - DeckFlow.Web/wwwroot/css/site-esper.css
  - DeckFlow.Web/wwwroot/css/site-grixis.css
  - DeckFlow.Web/wwwroot/css/site-jeskai.css
  - DeckFlow.Web/wwwroot/css/site-jund.css
  - DeckFlow.Web/wwwroot/css/site-mardu.css
  - DeckFlow.Web/wwwroot/css/site-naya.css
  - DeckFlow.Web/wwwroot/css/site-nyx.css
  - DeckFlow.Web/wwwroot/css/site-planeswalker-dark.css
  - DeckFlow.Web/wwwroot/css/site-sultai.css
autonomous: true
requirements: [UIAUDIT-02]

must_haves:
  truths:
    - "Switching the layout picker from Full to Compact to Advanced produces an UNMISTAKABLE, measurable layout change even on the empty Step-1 landing."
    - "Full (guided) mode has a positive style, not a do-nothing default."
  artifacts:
    - path: "DeckFlow.Web/wwwroot/css/site.css"
      provides: "distinct focused/expert layout with an always-present structural delta + positive guided style"
      contains: "data-prompt-ui-mode"
  key_links:
    - from: ".prompt-packets-form[data-prompt-ui-mode='expert']"
      to: "an always-rendered element (e.g. .prompt-instructions)"
      via: "structural collapse/hide producing a measurable box delta"
      pattern: "data-prompt-ui-mode=\\\"expert\\\""
---

<objective>
Bug D — make the Full / Compact / Advanced layout picker visibly, measurably distinct (CSS/UX only, NO JS).

The wiring is intact — `deck-sync.ts` sets `data-prompt-ui-mode` on body/form and `.is-active` on the button; `site.css:388-434` already hides secondary text in focused/expert. The root cause is that the effect is IMPERCEPTIBLE on the empty Step-1 landing: little secondary text exists there, `guided`/Full is a do-nothing default, and one targeted selector (`.prompt-global-note`) is absent from the view.

Locked fix (CONTEXT Bug D): make the modes produce an UNMISTAKABLE difference by keying off elements that are ALWAYS present (collapse whole panels / switch layout), and give Full a positive style. NO JavaScript change. The base rules live in `site.css`; the 12 standalone forks duplicate them and must be mirrored (11 @import themes inherit the base).

Purpose: Pillar-6 (Experience) — the control must obviously do something; counts toward UIAUDIT-02. This is also the interaction the new e2e in 86-05 asserts.
Output: three perceptibly-distinct modes with a guaranteed measurable delta on the empty landing, across base + 12 forks.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/86-ui-audit-re-score-studio-stage-4-admin-flags-closeout/86-CONTEXT.md
@.planning/phases/86-ui-audit-re-score-studio-stage-4-admin-flags-closeout/86-RESEARCH.md
@.planning/phases/86-ui-audit-re-score-studio-stage-4-admin-flags-closeout/86-VALIDATION.md

<interfaces>
<!-- Existing mode rules — site.css:388-434. `.prompt-instructions` is ALWAYS rendered on -->
<!-- Step 1 (site.css:370, a dashed-border ordered-list panel) — key it for a guaranteed delta. -->
<!-- guided = default (nothing hidden today). focused hides eyebrow/badge/notes/context. -->
<!-- expert hides those + helper-panel + nested result-panels. -->
<!-- The form carries data-prompt-ui-mode="guided" by default (DeckAnalysis.cshtml:91). -->

<!-- 12 forks duplicating the focused/expert rules (grep-confirmed) — mirror ALL: -->
<!--   abzan, bant, commander-table, esper, grixis, jeskai, jund, mardu, naya, nyx, planeswalker-dark, sultai -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Base — guaranteed layout delta per mode + positive Full style</name>
  <files>DeckFlow.Web/wwwroot/css/site.css</files>
  <action>Per UIAUDIT-02 / CONTEXT Bug D, edit the mode rules at site.css:388-434 (CSS only, NO JS): (1) Give `expert` an ALWAYS-VISIBLE structural collapse that does not depend on sparse text — e.g. `.prompt-packets-form[data-prompt-ui-mode="expert"] .prompt-instructions { display: none; }` (the instructions panel is always rendered on Step 1) and/or tighten the form to a denser single-column layout. This guarantees a measurable box delta on the empty landing. (2) Give `focused` a clearly denser but still-instructional look — keep hiding eyebrow/badge/notes and reduce `.prompt-instructions` padding + step-panel gaps so its box measurably shrinks vs guided. (3) Give `guided`/Full a POSITIVE marker rather than a do-nothing default — add an explicit `.prompt-packets-form[data-prompt-ui-mode="guided"] .prompt-instructions` accent style (e.g. an accent left-border and a subtle accent-tinted background using the `color-mix(in srgb, var(--accent) …%, transparent)` pattern from plan 86-01) so Full reads as the fully-guided mode and is regression-testable. Ensure the guided→focused→expert transitions each change a property that is present regardless of deck content. Changed-lines only; preserve LF.</action>
  <verify>
    <automated>grep -n 'data-prompt-ui-mode="expert"\|data-prompt-ui-mode="focused"\|data-prompt-ui-mode="guided"' DeckFlow.Web/wwwroot/css/site.css | head</automated>
  </verify>
  <done>site.css defines three perceptibly-distinct modes; expert hides/collapses an always-rendered element, focused is measurably denser, guided has a positive accent style keyed to an always-present element.</done>
</task>

<task type="auto">
  <name>Task 2: Mirror the mode styling into the 12 standalone forks</name>
  <files>DeckFlow.Web/wwwroot/css/site-abzan.css, DeckFlow.Web/wwwroot/css/site-bant.css, DeckFlow.Web/wwwroot/css/site-commander-table.css, DeckFlow.Web/wwwroot/css/site-esper.css, DeckFlow.Web/wwwroot/css/site-grixis.css, DeckFlow.Web/wwwroot/css/site-jeskai.css, DeckFlow.Web/wwwroot/css/site-jund.css, DeckFlow.Web/wwwroot/css/site-mardu.css, DeckFlow.Web/wwwroot/css/site-naya.css, DeckFlow.Web/wwwroot/css/site-nyx.css, DeckFlow.Web/wwwroot/css/site-planeswalker-dark.css, DeckFlow.Web/wwwroot/css/site-sultai.css</files>
  <action>Each of these 12 forks duplicates the focused/expert mode rules. Mirror the Task 1 changes into each: the always-present expert collapse, the denser focused box, and the positive guided accent marker (use each fork's own var(--accent) in the color-mix tint). The 11 @import themes inherit the base — do not add rules to them. Keep each fork's guild accent; only add/adjust the mode-delta declarations. Changed-lines only; preserve LF; no mass reflow.</action>
  <verify>
    <automated>test $(grep -rl 'data-prompt-ui-mode="expert"' DeckFlow.Web/wwwroot/css/site-abzan.css DeckFlow.Web/wwwroot/css/site-bant.css DeckFlow.Web/wwwroot/css/site-commander-table.css DeckFlow.Web/wwwroot/css/site-esper.css DeckFlow.Web/wwwroot/css/site-grixis.css DeckFlow.Web/wwwroot/css/site-jeskai.css DeckFlow.Web/wwwroot/css/site-jund.css DeckFlow.Web/wwwroot/css/site-mardu.css DeckFlow.Web/wwwroot/css/site-naya.css DeckFlow.Web/wwwroot/css/site-nyx.css DeckFlow.Web/wwwroot/css/site-planeswalker-dark.css DeckFlow.Web/wwwroot/css/site-sultai.css | wc -l) -eq 12</automated>
  </verify>
  <done>All 12 forks produce the same measurable per-mode delta; no theme leaves the layout picker looking inert.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser ← static CSS | Static stylesheet; no JS change, no untrusted input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-86-04-A | Tampering | data-prompt-ui-mode attribute selectors | accept | Mode value is set by first-party TS from a fixed 3-value set; CSS only reacts, adds no logic. |
| T-86-SC | Tampering | package installs | mitigate | No package installs; CSS only, no JS touched. |
</threat_model>

<verification>
- The measurable-delta requirement is proven by the interaction e2e in plan 86-05 (toggling Compact/Advanced changes a boundingBox/visibility on the empty landing; guided positive-style asserted too).
- Consolidated human-verify in 86-05 confirms the three modes look distinct across representative themes at desktop + mobile.
- NO JS change (deck-sync.ts untouched); do NOT commit compiled wwwroot/js/*.js.
</verification>

<success_criteria>
- Full / Compact / Advanced are unmistakably distinct on the empty Step-1 landing; the delta keys off always-present elements.
- Full has a positive style; base + 12 forks consistent.
- Changed-lines gate clean; LF preserved.
</success_criteria>

<output>
Create `.planning/phases/86-ui-audit-re-score-studio-stage-4-admin-flags-closeout/86-04-SUMMARY.md` when done.
</output>
