---
phase: 110.1-cut-lab-combo-intelligence-inserted-2026-07-23
verified: 2026-07-24T00:00:00Z
status: human_needed
score: 7/7 must-haves verified
overrides_applied: 0
deferred:
  - truth: "Combo-state badge readable across ALL guild themes"
    addressed_in: "Phase 111 / CLUP-19"
    evidence: "110.1-CONTEXT.md line 32: 'theme-token/CSS coverage is a planned task and is re-verified by the Phase 111 regression gate.' Badge CSS is token-driven (var(--accent)/--gold-warning/--line/--panel/--ink via color-mix) mirroring the already-shipped .cutlab-finding rule."
human_verification:
  - test: "Import a pool that produces a complete combo and a near-combo; open Cut Lab structural findings on at least one guild theme (e.g. Classic)."
    expected: "Complete-combo chips show a legible 'Combo piece' pill; near-combo chips show a legible 'Needs {MissingCard}' pill; both badges have sufficient contrast against the chip/panel background and the disclosure shows the combo context line."
    why_human: "Visual contrast/readability of the new badge pill cannot be asserted programmatically. This is the 03-T1 single-theme eyeball; the all-theme sweep is deferred to Phase 111."
---

# Phase 110.1: Cut Lab Combo Intelligence Verification Report

**Phase Goal:** Deliver combo intelligence for Cut Lab structural evidence — per-card
complete-vs-near combo distinction with badge + disclosure context (CLUP-17), badged
structural chips round-tripping through the what-if patch without changing lock/unlock
behavior (CLUP-18), under the LOCKED copy-only / new-finding-kind / card→combo-lookup /
research-only constraints.
**Verified:** 2026-07-24
**Status:** human_needed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | `CardComboMembership` dictionary replaces `ComboNames` repo-wide | ✓ VERIFIED | `grep -rn "\bComboNames\b" *.cs` (excl. bin/obj) = **0 rows**. Dict declared `CutLabAnalysisContextBuilder.cs:90`; new `CutLabCardComboMembership` record `:75-77`; role-assigner consumes it via `.ContainsKey(...)` `:215`. |
| 2 | New `ComboProtected` finding kind + `ComputeComboProtected` detector (complete "Combo piece", near "Needs {MissingCard}", order-insensitive variant grouping, documented weak-floor precedence) | ✓ VERIFIED | Enum `CutLabStructuralFindings.cs:21`; detector `:301-383`: complete evidence w/ `ComboBadgeState.CompletePiece` `:337`, near w/ `NeedsPartner` `:375`, variant grouping via `GroupBy` on `OrderBy` of `CardsInDeck` `:342-349`, weak-floor precedence comment `:380-382`. |
| 3 | `ComboProtected` in `ExcludedFindingKindsFromTally` AND NO new engine ranking/sort/branch rule (advisory copy is a Lead string only) | ✓ VERIFIED | Tally opt-out `CutLabCutRoundEngine.cs:98`. `git diff 5e1e763c..HEAD` on engine = **only** the tally add + `completeCombos:` arg at the `Compute(...)` call site; no branch/sort. Advisory copy is a string appended to `Lead` at `CutLabStructuralFindings.cs:326`. |
| 4 | Per-card badge map on BOTH `CutLabViewModel` (initial render via ProcessAsync→CutLabProcessResult→ViewModel.From) AND `CutLabUiPatchDto` (populated in BuildAsync, empty in BuildAdjustPatch) | ✓ VERIFIED | ViewModel prop `CutLabViewModel.cs:176`, carried in `From` `:277,:343`; ProcessResult prop `CutLabPageService.cs:78`, built `:422/:443`. DTO prop `CutLabUiPatchDto.cs:37`; `BuildAsync` populates `CutLabUiPatchBuilder.cs:119`; `BuildAdjustPatch` empty `:158`. Tests: `From_AssignsComboBadgeMapForInitialRender`, `BuildAdjustPatch_LeavesComboBadgeMapEmpty`. |
| 5 | Razor renders badge INSIDE chip `<button>` + combo context inside existing `cutlab-card-text` disclosure; CSS in site-common.css not site.css; lock/unlock unchanged | ✓ VERIFIED | Badge `<span class="cutlab-combo-badge...">` nested in `<button data-cut-lab-chip-card ... aria-pressed>` `CutLab.cshtml:630-639`; combo `<p class="cutlab-card-text__combo">` in disclosure `:157`. CSS only in `site-common.css:4377-4397`; `git diff` on `site.css` = empty. Button keeps lock attributes; badge is a passive child span. |
| 6 | What-if re-render re-applies badge + refreshes disclosure combo-context from `patch.comboBadgeByCardName` (HIGH-3 stale-clone fix) | ✓ VERIFIED | `cut-lab.ts`: `appendComboBadge` `:1306-1317` (called `:1356`), `syncDisclosureComboContext` `:1319-1342` uses `textContent` (never innerHTML), removes stale paragraph when absent. Wiring: clone pool-row disclosure then sync `:1845-1848`. Field on `CutLabUiPatch` `:155`. |
| 7 | SC-4: `requires`/`of`/`includes`/`variantCount`/`popularity` Spellbook shape recorded as documentation comment only (no parsing added) | ✓ VERIFIED | Comment block `CommanderSpellbookService.cs:251-258`; `git diff` = +8 lines, all comment; `ExtractCardNames` body unchanged (still `uses`-only). No new egress/parse. |

**Score:** 7/7 truths verified

### Deferred Items

| # | Item | Addressed In | Evidence |
|---|------|-------------|----------|
| 1 | Combo-state badge readability across ALL guild themes | Phase 111 / CLUP-19 | 110.1-CONTEXT.md:32 defers theme CSS coverage to the Phase 111 regression gate. Badge CSS is token-driven and mirrors the shipped `.cutlab-finding` rule. |

### Key Link Verification

| From | To | Via | Status | Details |
|------|-----|-----|--------|---------|
| `CutLabAnalysisContextBuilder` | `CutLabClassificationContext.CardComboMembership` | dict built from single Spellbook walk | ✓ WIRED | builder `:615-618`, threaded to classification `:545` |
| `CutLabCutRoundEngine` | `CutLabStructuralFindings.Compute` | `completeCombos:` from membership `.CompleteCombos` | ✓ WIRED | engine `:296-303` |
| `CutLabPageService.ProcessAsync` | `CutLabViewModel.ComboBadgeByCardName` | ProcessResult → `ViewModel.From` | ✓ WIRED | service `:422/:443`, VM `:277/:343` |
| `CutLabUiPatchBuilder.BuildAsync` | `CutLabUiPatchDto.ComboBadgeByCardName` | `BuildComboBadgeByCardName(membership)` | ✓ WIRED | `:119/:285-317` |
| `cut-lab.ts` structural re-render | badge span + disclosure combo `<p>` | `patch.comboBadgeByCardName` | ✓ WIRED | `:1841-1848` |

### Behavioral Spot-Checks

Full suites confirmed green by orchestrator (DeckFlow.Web.Tests 2009 pass / 16 skip; vitest 93 pass). Source-level assertions confirmed present for each CLUP-17/18 behavior:

| Behavior | Test | Status |
|----------|------|--------|
| Complete-combo pieces + round-1 advisory + CompletePiece badge | `Compute_ComboProtected_ReportsCompleteComboPiecesWithBadgeStateAndRoundOneAdvisory` | ✓ PASS |
| Near-combo NeedsPartner badge + "Needs {MissingCard}" | `Compute_ComboProtected_ReportsNearComboMissingPartnerBadgeState` | ✓ PASS |
| Order-insensitive variant grouping into ONE finding listing alternatives | `Compute_ComboProtected_GroupsNearComboVariantsByOrderInsensitiveCardsInDeck` (Assert.Single, both missing pieces in Lead) | ✓ PASS |
| Weak-floor overlap carries combo context (both findings coexist) | `Compute_ComboProtected_CarriesComboContextWhenWeakFloorOverlapExists` | ✓ PASS |
| ComboProtected excluded from tally (no engine ranking effect) | `BuildQueue_ComboProtectedOnlyEvidence_DoesNotIncreaseFindingTallies` | ✓ PASS |
| Initial-render VM badge map | `From_AssignsComboBadgeMapForInitialRender` | ✓ PASS |
| Patch round-trip populated / adjust empty | `CutLabUiPatchBuilderTests` (`BuildAdjustPatch_LeavesComboBadgeMapEmpty`) | ✓ PASS |
| Nested-badge lock invariant + what-if badge/disclosure refresh | `cut-lab-lock-interactions.test.ts`, `cut-lab-structural-cardtext.test.ts` | ✓ PASS |

### Requirements Coverage

| Requirement | Description | Status | Evidence |
|-------------|-------------|--------|----------|
| CLUP-17 | Distinguish complete-combo vs near-combo per structural card, weak-floor combo protection, interchangeable-card variant grouping into one finding | ✓ ACHIEVED (code) | Truths 2, 7 + detector/tests above. Visual badge readability is the deferred/human item. |
| CLUP-18 | Badge matched chips + expose combo role/context in the 110 disclosure without changing lock/unlock; round-trip through the 108 patch DTO | ✓ ACHIEVED (code) | Truths 1, 3, 4, 5, 6 + tests. Lock/unlock attrs intact; badge is passive nested span. |

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| `CommanderSpellbookService.cs` | 258 | `TODO(follow-on phase)` | ℹ️ Info | Warning-tier marker (not TBD/FIXME/XXX). References `110.1-RESEARCH.md §1b` + "follow-on phase" — this IS the intentional SC-4 deferral (candidate matching out of scope). Auditable follow-up; not a gap. |

No stubs, empty-data returns, hardcoded-empty rendering props, or unreferenced debt markers found in the phase's changed files.

### Human Verification Required

#### 1. Badge visual readability (single theme)

**Test:** Import a pool that yields a complete combo and a near-combo; open Cut Lab structural findings on at least one guild theme.
**Expected:** "Combo piece" pill and "Needs {MissingCard}" pill render with legible contrast on the chip; the disclosure shows the combo-context line.
**Why human:** Visual contrast is not programmatically assertable. This is the 03-T1 single-theme eyeball; the full all-theme sweep is formally deferred to Phase 111 / CLUP-19.

### Gaps Summary

No engineering gaps. All seven goal-backward truths are verified in committed source and backed by
targeted tests; the two LOCKED boundaries most likely to hide regressions — (a) engine copy-only
(no ranking rule) and (b) SC-4 research-only (no parsing) — were confirmed by direct `git diff` of
the engine and Spellbook service. The only outstanding item is the inherently-visual badge-readability
check, which drives `status: human_needed`; the all-theme regression is deferred to Phase 111/CLUP-19
per the phase contract.

---

_Verified: 2026-07-24_
_Verifier: Claude (gsd-verifier)_
