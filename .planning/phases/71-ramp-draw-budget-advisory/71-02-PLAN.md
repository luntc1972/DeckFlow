---
phase: 71-ramp-draw-budget-advisory
plan: 02
type: execute
wave: 2
depends_on: ["71-01"]
files_modified:
  - DeckFlow.Core/Manabase/ManabaseVerdict.cs
  - DeckFlow.Core/Manabase/ManabaseVerdictSynthesizer.cs
  - DeckFlow.Core/Manabase/ManabaseSwapPromptBuilder.cs
  - DeckFlow.Core/Manabase/ManabaseReportTextBuilder.cs
  - DeckFlow.Core.Tests/Manabase/ManabaseVerdictSynthesizerTests.cs
  - DeckFlow.Core.Tests/Manabase/ManabaseSwapPromptBuilderTests.cs
  - DeckFlow.Core.Tests/Manabase/ManabaseReportTextBuilderTests.cs
autonomous: true
requirements: [PLV-VERDICT, PLV-BUDGET]
must_haves:
  truths:
    - "When issues exist, the synthesizer emits prioritized add/remove guidance ordered land/color -> ramp -> draw, capped at 3"
    - "When no issues exist, the synthesizer emits a specific why-it's-fine drawn from actual numbers (colors cleared + cast rate + mode)"
    - "The single manabase prompt builder appends the verdict + ramp/draw budget block when supplied, and is byte-identical when not supplied"
  artifacts:
    - path: "DeckFlow.Core/Manabase/ManabaseVerdictSynthesizer.cs"
      provides: "Synthesize(report, mode, budget?) -> ManabaseVerdict"
      contains: "Synthesize"
    - path: "DeckFlow.Core/Manabase/ManabaseVerdict.cs"
      provides: "ManabaseVerdict record (Lines, NoIssueReason, HasIssues)"
  key_links:
    - from: "ManabaseSwapPromptBuilder.Build"
      to: "ManabaseVerdict"
      via: "optional verdict/budget params append a deterministic block"
      pattern: "ManabaseVerdict"
---

<objective>
Synthesize the deterministic "Reading your deck" verdict in DeckFlow.Core from already-computed
report data, and mirror it (plus the ramp/draw budget block) into the single manabase prompt
builder and the downloadable text report.

Purpose: Deliverable B. Turns the report's bare numbers into prioritized plain-language guidance,
or a specific why-it's-fine — with NO LLM call (this IS the on-page/prompt text).
Output: `ManabaseVerdict`, `ManabaseVerdictSynthesizer`, and augmented prompt/text builders.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/71-ramp-draw-budget-advisory/71-CONTEXT.md
@.planning/phases/71-ramp-draw-budget-advisory/71-SPEC.md
@.planning/phases/71-ramp-draw-budget-advisory/71-01-SUMMARY.md

<interfaces>
<!-- Already-computed report data the synthesizer must read (no recomputation). -->

From DeckFlow.Core/Manabase/ManabaseModels.cs (ManabaseReport):
- int ActualLands; double TargetLands; double LandDelta; bool LandShortfallCoveredByRamp;
- IReadOnlyList<ColorSourceFinding> ColorFindings; ColorSourceFinding? WeakestColor;
- ManabaseHealth Health; int AvgOnCurvePercent; ManabasePrimaryFix PrimaryFix;
- IReadOnlyList<DemandingCard> DemandingCards; ManabaseMode Mode;
- ColorSourceFinding { ManaColor Color; double ActualSources; int RequiredSources; double Deficit;
  bool IsAdequate; string DrivingSpell; }
- ManabasePrimaryFix { ManabaseFixKind Kind; ManaColor? Color; int Amount; double ActualSources;
  int RequiredSources; string Spell; int DemandingCount; }

From 71-01 (Plan 01): ManabaseRampDrawBudget { double RampCount; double DrawCount; int OverlapCount;
  double Threshold; ThresholdSource; int TargetRamp; int TargetDraw; bool IsBalanced; bool IsRampLight;
  int RampShort; bool IsDrawLight; int DrawShort; }

From DeckFlow.Core/Manabase/ManabaseLabels.cs: Health(ManabaseHealth), Mode(ManabaseMode).

From ManabaseSwapPromptBuilder.cs: `Build(ManabaseReport report, string? deckName, string? decklistText,
  ManabaseMode mode = Casual)` — the SINGLE manabase prompt builder (NOT the 3 deck-analysis variants).
From ManabaseReportTextBuilder.cs: `Build(report, deckName, decklistText, mode)` — the .txt download.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ManabaseVerdict + ManabaseVerdictSynthesizer</name>
  <files>DeckFlow.Core/Manabase/ManabaseVerdict.cs, DeckFlow.Core/Manabase/ManabaseVerdictSynthesizer.cs, DeckFlow.Core.Tests/Manabase/ManabaseVerdictSynthesizerTests.cs</files>
  <behavior>
    - Signature: `ManabaseVerdict Synthesize(ManabaseReport report, ManabaseMode mode, ManabaseRampDrawBudget? budget = null)`.
      `budget` is non-null only in Casual (the caller suppresses it for cEDH).
    - Issue collection, in priority order land/color -> ramp -> draw, then capped to the first 3:
      1. COLOR: for each ColorFinding with Deficit > 1 (real source shortfall), ordered by Deficit desc,
         line = "You're ~{ceil(Deficit)} {Color} source(s) short - add ~{ceil(Deficit)} {Color}-producing
         lands/rocks; consider cutting a colorless utility land." (uses ActualSources vs RequiredSources).
      2. LAND: if LandDelta <= -2 AND NOT LandShortfallCoveredByRamp, line = "Add ~{ceil(-LandDelta)}
         more land(s) - the base is short for this curve." (Skip when ramp covers it.)
      3. RAMP (budget != null && IsRampLight): "Ramp looks light: you run ~{RampCount:0.#} ramp vs a
         ~{TargetRamp}/{TargetDraw} split for a ~MV{Threshold:0.#} threshold ({proxy phrase}) - add
         ~{RampShort} ramp piece(s) (e.g. a 2-mana rock). (community heuristic, not Karsten math)"
      4. DRAW (budget != null && IsDrawLight): symmetric draw line with DrawShort + an example
         (e.g. a repeatable card-draw enchantment).
      Proxy phrase: ThresholdSource==CommanderManaValue -> "your commander's mana value"; CurveProxy ->
      "your curve's 75th-percentile mana value, since you have no single commander".
    - No-issue path (Issues empty): HasIssues=false; build NoIssueReason naming the colors that cleared
      their Karsten target (ColorFindings where IsAdequate), the AvgOnCurvePercent, the mode, and the
      ramp/draw balance when budget present. Example: "White and Blue both clear their Karsten source
      targets and your 87% avg on-curve cast rate is healthy for Casual - and ramp/draw (12 / 12) is
      in balance - no changes needed." Must be specific (no generic "looks good").
    - ManabaseVerdict record: bool HasIssues; IReadOnlyList<string> Lines (the ordered issue lines,
      empty when none); string NoIssueReason (empty when HasIssues); string Headline
      (e.g. "Reading your deck").
    - Fixtures (build ManabaseReport instances directly, no Scryfall): (a) color-short deck (White
      Deficit 3.2) -> first line is the White shortfall; (b) ramp-light Casual deck with budget
      RampCount 6 / TargetRamp 12 -> a ramp line appears AFTER any color/land lines and within the cap;
      (c) clean deck (all colors adequate, AvgOnCurve 87, balanced budget) -> HasIssues false and
      NoIssueReason names the cleared colors + 87% + Casual; (d) >3 issues -> Lines.Count == 3 in the
      land/color -> ramp -> draw priority order.
  </behavior>
  <action>Create the `ManabaseVerdict` sealed record and `ManabaseVerdictSynthesizer` static class.
    Read ONLY already-computed fields off ManabaseReport (and the optional budget) - never re-run the
    analyzer or classifier. Implement the ordered issue collection, the 3-issue cap, and the specific
    no-issue reason exactly as in the behavior block. Use ManabaseLabels for mode/color wording and
    InvariantCulture for all numeric formatting. Deterministic templated prose only - NO LLM call,
    no randomness. Keep tone terse (the Web view renders Lines as a list; the prompt renders them as
    prose).</action>
  <verify>
    <automated>cd /mnt/c/users/chrislunt/source/personal/deckflow && cmd.exe /c "dotnet test DeckFlow.Core.Tests\DeckFlow.Core.Tests.csproj --filter FullyQualifiedName~ManabaseVerdictSynthesizer" 2>&1 | tail -20</automated>
  </verify>
  <done>Synthesizer selects + orders issues correctly per fixture, caps at 3, and produces a
    specific why-it's-fine for the clean deck - all green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Append verdict + budget block to the prompt and text builders</name>
  <files>DeckFlow.Core/Manabase/ManabaseSwapPromptBuilder.cs, DeckFlow.Core/Manabase/ManabaseReportTextBuilder.cs, DeckFlow.Core.Tests/Manabase/ManabaseSwapPromptBuilderTests.cs, DeckFlow.Core.Tests/Manabase/ManabaseReportTextBuilderTests.cs</files>
  <behavior>
    - Add an optional trailing parameter `ManabaseVerdict? verdict = null` (and, for the budget block,
      `ManabaseRampDrawBudget? budget = null`) to BOTH builders' `Build(...)`. When `verdict` is null
      (and budget null), output is BYTE-IDENTICAL to today (existing ManabaseSwapPromptBuilderTests /
      ManabaseReportTextBuilderTests pass unchanged).
    - When `verdict` is non-null, insert a "Reading your deck:" block (the Headline + either the
      numbered Lines or the NoIssueReason) into the prompt BEFORE the "Please recommend SPECIFIC lands"
      instruction, and into the text report after the Summary section. When `budget` is non-null,
      append a one-line ramp/draw budget summary ("Ramp/draw: ~{RampCount:0.#} ramp / ~{DrawCount:0.#}
      draw vs a ~{TargetRamp}/{TargetDraw} community target for a ~MV{Threshold:0.#} threshold
      ({proxy}); ({N} do both). community heuristic, not Karsten math.").
    - Tests: with a verdict containing two issue Lines, both appear in the prompt output AND in the
      text output; with verdict null the byte-identical assertion holds (compare against the current
      Build output); the budget line includes "(N do both)" and the "community heuristic" label.
  </behavior>
  <action>Extend `ManabaseSwapPromptBuilder.Build` and `ManabaseReportTextBuilder.Build` with the two
    optional nullable params appended last (preserving all existing call sites - defaults make them
    no-ops). Render the verdict block and budget line as specified, using StringBuilder + InvariantCulture
    consistent with the existing code. Do NOT alter any existing line when the new params are null.
    Per CONTEXT, the manabase prompt artifact is THIS single builder only - do not touch deck-analysis
    prompt variants.</action>
  <verify>
    <automated>cd /mnt/c/users/chrislunt/source/personal/deckflow && cmd.exe /c "dotnet test DeckFlow.Core.Tests\DeckFlow.Core.Tests.csproj --filter FullyQualifiedName~Manabase" 2>&1 | tail -25</automated>
  </verify>
  <done>Both builders append the verdict + budget block when supplied and are byte-identical when not;
    all manabase Core tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
Pure DeckFlow.Core text generation; no new trust boundary. Output is deterministic templated prose
fed (by the user) into an external LLM downstream - but the builder itself makes no network call.

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-71-02 | Information disclosure | Verdict/prompt text | accept | Only echoes the user's own deck + already-displayed numbers; nothing new is exposed. |
| T-71-03 | Tampering | Optional params on shared builders | mitigate | Byte-identical regression test guards the flag-OFF (null-param) path. |
| T-71-SC | Tampering | npm/pip/cargo installs | n/a | No package installs. |
</threat_model>

<verification>
- `dotnet test` filter `Manabase` green; the byte-identical (null-param) prompt/text assertions pass.
- Synthesizer never reads outside ManabaseReport + the optional budget (no analyzer re-run).
</verification>

<success_criteria>
The "Reading your deck" verdict and ramp/draw budget block are synthesized deterministically and
mirrored into the single manabase prompt builder and the .txt report, with a guaranteed
byte-identical path when not supplied.
</success_criteria>

<output>
Create `.planning/phases/71-ramp-draw-budget-advisory/71-02-SUMMARY.md` when done.
</output>
