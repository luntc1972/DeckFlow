---
phase: 30-content-kb-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/30-content-kb-integration/30-TAG-AUDIT.md
# Operational note (NOT executor file edits): the D-11 incremental harvest also
# commits refreshed content-kb/*.md artifacts plus the regenerated site-index seed.
# Those land via the user's local-harvest commit (Task 1), not via executor Edit/Write
# in this plan. files_modified above lists only the audit doc the executor authors;
# the harvest artifacts are an operator commit, but this plan no longer implies that
# ONLY the audit doc changes during the wave (LOW review item).
autonomous: false
requirements: [KBI-01]
user_setup: []

must_haves:
  truths:
    - "content.kb.enabled is ON in production after a fresh incremental harvest (D-09 flip-first, D-11 incremental top-up)"
    - "At least one curated clip is visible on the public KB browse page in prod"
    - "A live tag-distribution audit of the prod KB has been captured as numeric counts"
  artifacts:
    - path: ".planning/phases/30-content-kb-integration/30-TAG-AUDIT.md"
      provides: "Prod bracket + archetype tag distribution counts that calibrate relevance thresholds in plan 02"
      contains: "Bracket"
  key_links:
    - from: "user (local CLI harvest)"
      to: "content-kb/ artifacts committed + deployed"
      via: "DeckFlow.CLI incremental harvest + git commit + Render deploy"
      pattern: "content-kb/"
    - from: "/Admin/Flags toggle (prod)"
      to: "feature_flags row content.kb.enabled = true"
      via: "live admin UI flag flip"
      pattern: "content.kb.enabled"
---

<objective>
Take the Content KB live in production and capture the tag-distribution audit that calibrates all relevance thresholds in later plans. This is the D-09 "flip first" execution unit and the mandatory pre-implementation guard from STATE.md / RESEARCH.md.

Purpose: SC1 (KBI-01) must be TRUE — flag ON, content visible live — BEFORE any relevance-matching code is written, so the corpus the scoring is calibrated against is the real, expanded, post-harvest prod corpus, not the 10-artifact dev corpus.
Output: `content.kb.enabled = true` in prod, at least one clip live on the KB browse page, and `30-TAG-AUDIT.md` with real prod tag-distribution counts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-content-kb-integration/30-CONTEXT.md
@.planning/phases/30-content-kb-integration/30-RESEARCH.md

<interfaces>
<!-- Harvest + flag-flip surfaces the user/operator drives. No code changes in this plan. -->

D-09 sequence (binding ordering): incremental harvest -> commit artifacts -> deploy -> admin curates rows visible -> flip content.kb.enabled via live /Admin/Flags -> verify browse page (SC1 done).
D-11: "fresh harvest" = incremental top-up over the existing 5 channels via local DeckFlow.CLI; existing artifacts unchanged; user runs harvest locally (never auto-launched — standing rule).

Tag-distribution audit queries (RESEARCH.md "Live Tag-Distribution Audit"):
  - Bracket tag distribution across visible rows (content_site_index WHERE is_visible = true GROUP BY tags_bracket)
  - Archetype tag distribution across visible rows
  - Count of rows with empty/null bracket tag

Prod DB: Render Postgres (DECKFLOW_DATABASE_PROVIDER=Postgres). Table: content_site_index. Columns: tags_bracket, tags_archetype, is_visible.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Run incremental harvest locally and commit fresh artifacts (D-11)</name>
  <read_first>
    - .planning/phases/30-content-kb-integration/30-CONTEXT.md (D-09, D-11)
    - .planning/phases/30-content-kb-integration/30-RESEARCH.md (Runtime State Inventory: content-kb/ tree under MTG_DATA_DIR; CLI harvest tool available)
    - CLAUDE.md (DeckFlow constraints: never auto-launch; plain default-author commits)
  </read_first>
  <what-built>Nothing automated — this is a user-manual harvest step. The DeckFlow web server is never auto-launched (standing rule); the CLI harvest is operator-run. NOTE: this step commits refreshed content-kb/*.md artifacts and the regenerated site-index seed (operator commit, not executor file edits).</what-built>
  <how-to-verify>
    1. Run the local DeckFlow.CLI incremental harvest+distill over the existing 5 channels to pick up videos published since the v1.4 run (existing artifacts unchanged — incremental top-up only).
    2. Commit the new/updated `content-kb/` artifacts with a plain default-author commit (no Co-Authored-By trailer).
    3. Deploy to Render (push to main per the v1.4 deploy model, or trigger a manual deploy) so prod picks up the new artifacts and the seed loader populates index rows.
    4. Confirm the deploy succeeded and the app started clean (no schema-validation startup failure).
  </how-to-verify>
  <acceptance_criteria>
    - New or refreshed `content-kb/*.md` artifacts are committed to git (operator confirms commit hash).
    - Render deploy completed and the app is serving (operator confirms prod is up).
  </acceptance_criteria>
  <resume-signal>Type "harvested" with the commit hash, or describe any harvest/deploy failure.</resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Curate visible rows and flip content.kb.enabled in prod (D-09)</name>
  <read_first>
    - .planning/phases/30-content-kb-integration/30-CONTEXT.md (D-09, D-10)
    - .planning/phases/30-content-kb-integration/30-RESEARCH.md (Runtime State Inventory: content.kb.enabled flag row value = false in prod feature_flags)
  </read_first>
  <what-built>Operator-driven prod curation + flag flip via the live admin UI. No code change.</what-built>
  <how-to-verify>
    1. In prod, open `/Admin/ContentKb`, review the seeded rows, and set the intended rows to visible (IsVisible = true) so at least one clip-bearing artifact is published.
    2. Open the live `/Admin/Flags` page and flip `content.kb.enabled` to ON (the flag row currently exists with value false).
    3. Open the public KB browse page (the route behind `[FeatureFlagGate("content.kb.enabled", ...)]`) and confirm at least one curated clip is visible.
  </how-to-verify>
  <acceptance_criteria>
    - `content.kb.enabled` = true in the prod feature_flags table (operator confirms via /Admin/Flags showing ON).
    - The public KB browse page renders at least one curated clip live (operator confirms — this is SC1 / KBI-01).
  </acceptance_criteria>
  <resume-signal>Type "live" once the browse page shows a clip, or describe what failed.</resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: Run the live tag-distribution audit and record results in 30-TAG-AUDIT.md</name>
  <read_first>
    - .planning/phases/30-content-kb-integration/30-RESEARCH.md ("Live Tag-Distribution Audit" section — the three SQL queries and the research-time dev-corpus baseline table)
    - .planning/phases/30-content-kb-integration/30-CONTEXT.md (pre-implementation guard wording)
  </read_first>
  <what-built>The audit query result captured to `30-TAG-AUDIT.md`. This file is the calibration input plan 02 consumes — relevance thresholds are NOT specced until this exists.</what-built>
  <how-to-verify>
    1. Against the prod Postgres DB, run the three audit queries from RESEARCH.md: bracket distribution over visible rows, archetype distribution over visible rows, and count of rows with empty/null bracket tag.
    2. Record the numeric results in `.planning/phases/30-content-kb-integration/30-TAG-AUDIT.md` with a `## Bracket Distribution`, `## Archetype Distribution`, and `## Empty-Bracket Count` section, plus the total visible-row count.
    3. Add a `## Calibration Implications` note stating, given the post-harvest distribution, whether bracket-match can be a hard gate dimension or must be a score bonus (RESEARCH.md flags 60% empty-bracket in the dev corpus; the prod corpus may differ).
    4. Add a `## Corpus Size` line recording the total visible-row count — plan 02's per-request artifact-read performance assumption (corpus <= ~50 artifacts read as local files through the resolver) is grounded on this number. If the count materially exceeds ~50, flag it in this section so plan 02 adds the IMemoryCache TTL path instead of assuming bare per-request reads.
  </how-to-verify>
  <acceptance_criteria>
    - `.planning/phases/30-content-kb-integration/30-TAG-AUDIT.md` exists and contains the three section headers (`## Bracket Distribution`, `## Archetype Distribution`, `## Empty-Bracket Count`) with real numeric counts (not placeholders).
    - `grep -c "Bracket" .planning/phases/30-content-kb-integration/30-TAG-AUDIT.md` returns at least 1.
    - The file states the total count of visible rows in prod (the `## Corpus Size` figure plan 02's perf assumption depends on).
  </acceptance_criteria>
  <resume-signal>Type "audited" once 30-TAG-AUDIT.md is written with real counts.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator -> prod /Admin | Authenticated admin actions (curation, flag flip) cross into prod state |
| operator -> prod DB (audit read) | Read-only SELECT against prod Postgres |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-01 | Elevation of Privilege | /Admin/Flags + /Admin/ContentKb | mitigate | Admin surface already behind existing BasicAuthMiddleware /Admin branch; no new endpoint added in this plan |
| T-30-02 | Tampering | Harvested clip text (untrusted YouTube transcript content) entering the corpus | accept | KB is admin-curated before publish; existing ContentKbController renders via Markdig.DisableHtml(); prompt-injection risk into AI prompts is carried and mitigated in plan 03 (Expert Context preamble frames clips as third-party evidence, not instructions) |
| T-30-03 | Information Disclosure | Prod DB audit read | accept | Read-only SELECT; no secrets echoed; results are tag counts only (no PII) |
| T-30-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan; no install tasks present |
</threat_model>

<verification>
- SC1 (KBI-01): `content.kb.enabled` ON in prod AND at least one clip visible on the public KB browse page (operator-confirmed at Task 2).
- `30-TAG-AUDIT.md` exists with real bracket + archetype counts, a total visible-row count, and the `## Corpus Size` figure (Task 3).
- App started clean post-deploy (no schema-validation startup failure).
- Test execution gate: this plan ships no code (operator-manual harvest + flag flip + SQL audit only), so the dotnet-build / push-and-watch CI gate used by plans 02-04 does not apply here; SC1 is operator-confirmed live in prod.
</verification>

<success_criteria>
- KBI-01 is TRUE: flag flipped ON in prod after a fresh harvest; at least one clip live.
- The calibration input (`30-TAG-AUDIT.md`) exists for plan 02 to consume, including the corpus-size figure.
</success_criteria>

<output>
Create `.planning/phases/30-content-kb-integration/30-01-SUMMARY.md` when done. Record: harvest commit hash, confirmation flag is ON in prod, the total visible-row corpus size, and a copy of (or pointer to) the tag-audit counts so downstream plans can calibrate thresholds.
</output>
</content>
</invoke>
