---
phase: 15-aiplatform-value-object-refactor
plan: 02
type: execute
wave: 2
depends_on:
  - 15-01
files_modified:
  - DeckFlow.Web/Services/PromptBuilders/Analysis/IAnalysisPromptVariant.cs
  - DeckFlow.Web/Services/PromptBuilders/Analysis/ChatGptAnalysisPromptVariant.cs
  - DeckFlow.Web/Services/PromptBuilders/Analysis/ClaudeAnalysisPromptVariant.cs
  - DeckFlow.Web/Services/PromptBuilders/Analysis/GeminiAnalysisPromptVariant.cs
  - DeckFlow.Web/Services/PromptBuilders/Analysis/AnalysisPromptVariantRegistry.cs
  - DeckFlow.Web/Services/PromptBuilders/SetUpgrade/ISetUpgradePromptVariant.cs
  - DeckFlow.Web/Services/PromptBuilders/SetUpgrade/ChatGptSetUpgradePromptVariant.cs
  - DeckFlow.Web/Services/PromptBuilders/SetUpgrade/ClaudeSetUpgradePromptVariant.cs
  - DeckFlow.Web/Services/PromptBuilders/SetUpgrade/GeminiSetUpgradePromptVariant.cs
  - DeckFlow.Web/Services/PromptBuilders/SetUpgrade/SetUpgradePromptVariantRegistry.cs
  - DeckFlow.Web/Services/PromptBuilders/Comparison/IComparisonPromptVariant.cs
  - DeckFlow.Web/Services/PromptBuilders/Comparison/ChatGptComparisonPromptVariant.cs
  - DeckFlow.Web/Services/PromptBuilders/Comparison/ClaudeComparisonPromptVariant.cs
  - DeckFlow.Web/Services/PromptBuilders/Comparison/GeminiComparisonPromptVariant.cs
  - DeckFlow.Web/Services/PromptBuilders/Comparison/ComparisonPromptVariantRegistry.cs
  - DeckFlow.Web/Services/PromptBuilders/FollowUp/IFollowUpPromptVariant.cs
  - DeckFlow.Web/Services/PromptBuilders/FollowUp/ChatGptFollowUpPromptVariant.cs
  - DeckFlow.Web/Services/PromptBuilders/FollowUp/ClaudeFollowUpPromptVariant.cs
  - DeckFlow.Web/Services/PromptBuilders/FollowUp/GeminiFollowUpPromptVariant.cs
  - DeckFlow.Web/Services/PromptBuilders/FollowUp/FollowUpPromptVariantRegistry.cs
  - DeckFlow.Web/Services/PromptBuilders/MetaGap/IMetaGapPromptVariant.cs
  - DeckFlow.Web/Services/PromptBuilders/MetaGap/ChatGptMetaGapPromptVariant.cs
  - DeckFlow.Web/Services/PromptBuilders/MetaGap/ClaudeMetaGapPromptVariant.cs
  - DeckFlow.Web/Services/PromptBuilders/MetaGap/GeminiMetaGapPromptVariant.cs
  - DeckFlow.Web/Services/PromptBuilders/MetaGap/MetaGapPromptVariantRegistry.cs
  - DeckFlow.Web/Services/DeckAnalysisPacketService.cs
  - DeckFlow.Web/Services/DeckComparisonService.cs
  - DeckFlow.Web/Services/MetaGapService.cs
  - DeckFlow.Web/Program.cs
autonomous: true
requirements:
  - AIPLATFORM-02
must_haves:
  truths:
    - "All 5 prompt-builder families have a strategy interface (IXxxPromptVariant) with AiPlatform Platform getter"
    - "Each family has 3 sealed variant classes — ChatGpt/Claude/Gemini — one per AiPlatform.All entry"
    - "Each family has a registry class that ToDictionary(v => v.Platform) at construction and dispatches in Build(AiPlatform platform, ...)"
    - "DeckAnalysisPacketService injects AnalysisPromptVariantRegistry + SetUpgradePromptVariantRegistry; old BuildAnalysisPrompt/BuildSetUpgradePrompt switch arms deleted"
    - "DeckComparisonService injects ComparisonPromptVariantRegistry + FollowUpPromptVariantRegistry; old switches deleted"
    - "MetaGapService injects MetaGapPromptVariantRegistry; old switch deleted"
    - "Program.cs registers 15 IXxxPromptVariant singletons + 5 XxxPromptVariantRegistry singletons (20 new DI lines)"
    - "Each registry falls back to AiPlatform.Default variant when an unknown AiPlatform is passed (defensive — Normalize at the setter should prevent this, but registry must be safe)"
    - "Variant Build(...) bodies are byte-identical copies of the pre-refactor private static switch-arm method bodies (no behavior change, no string mutation)"
    - "Existing string Keys '\"ChatGPT\"'/'\"Claude\"'/'\"Gemini\"' contract preserved end-to-end (D-06)"
  artifacts:
    - path: "DeckFlow.Web/Services/PromptBuilders/Analysis/AnalysisPromptVariantRegistry.cs"
      provides: "Per-platform analysis prompt dispatch"
      contains: "ToDictionary(v => v.Platform)"
    - path: "DeckFlow.Web/Services/PromptBuilders/SetUpgrade/SetUpgradePromptVariantRegistry.cs"
      provides: "Per-platform set-upgrade prompt dispatch"
      contains: "ToDictionary(v => v.Platform)"
    - path: "DeckFlow.Web/Services/PromptBuilders/Comparison/ComparisonPromptVariantRegistry.cs"
      provides: "Per-platform comparison prompt dispatch"
      contains: "ToDictionary(v => v.Platform)"
    - path: "DeckFlow.Web/Services/PromptBuilders/FollowUp/FollowUpPromptVariantRegistry.cs"
      provides: "Per-platform follow-up prompt dispatch"
      contains: "ToDictionary(v => v.Platform)"
    - path: "DeckFlow.Web/Services/PromptBuilders/MetaGap/MetaGapPromptVariantRegistry.cs"
      provides: "Per-platform meta-gap prompt dispatch"
      contains: "ToDictionary(v => v.Platform)"
    - path: "DeckFlow.Web/Program.cs"
      provides: "DI registration block for 15 variants + 5 registries"
      contains: "AnalysisPromptVariantRegistry"
  key_links:
    - from: "DeckAnalysisPacketService"
      to: "AnalysisPromptVariantRegistry"
      via: "ctor parameter"
      pattern: "AnalysisPromptVariantRegistry"
    - from: "DeckComparisonService"
      to: "ComparisonPromptVariantRegistry + FollowUpPromptVariantRegistry"
      via: "ctor parameters"
      pattern: "ComparisonPromptVariantRegistry|FollowUpPromptVariantRegistry"
    - from: "MetaGapService"
      to: "MetaGapPromptVariantRegistry"
      via: "ctor parameter"
      pattern: "MetaGapPromptVariantRegistry"
    - from: "Program.cs DI block"
      to: "5 registries + 15 variants"
      via: "services.AddSingleton<IXxx, ChatGptXxx>() etc"
      pattern: "AddSingleton.*PromptVariant"
---

<objective>
Extract the 5 internal-static prompt-builder switches into per-family strategy registries (25 new files under `Services/PromptBuilders/{Family}/`) and wire them through DI. After this plan: adding a 4th AI is "1 new variant class per family + 1 DI line per variant + 1 entry in `AiPlatform.All`" — zero edits to host services, request setters, Razor partial, or RequestContextParser. This is the core OCP-score jump from 3/10 to 8/10.

Purpose: Implements design doc Layer 2 verbatim. Each family follows the same pattern: 1 interface + 3 sealed variants + 1 registry. Host services move from `request.TargetAiPlatform switch { "Claude" => ClaudeArm, ... }` to `_registry.Build(AiPlatform.Normalize(request.TargetAiPlatform), ...)`.

Output: 25 new files (5 families × 5 files each); 4 modified files (3 host services + Program.cs). Build green after each family's commit cluster. Byte-identical artifact behavior preserved (variant Build bodies are mechanical copies of the original switch-arm bodies).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/15-aiplatform-value-object-refactor/15-CONTEXT.md
@.planning/milestones/v1.2-phases/10-claude-gemini-artifact-optimization/10-AISEL-PLATFORM-DESIGN.md
@.planning/phases/15-aiplatform-value-object-refactor/15-01-PLAN.md
@CLAUDE.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/ARCHITECTURE.md

<interfaces>
<!-- Existing 5 switch dispatcher signatures the executor must copy verbatim per Claude's Discretion in 15-CONTEXT.md. -->

From DeckFlow.Web/Services/DeckAnalysisPacketService.cs:842 (Analysis family — internal static dispatcher):
```
internal static string BuildAnalysisPrompt(
    DeckAnalysisRequest request,
    string decklistText,
    string referenceText,
    string deckProfileSchemaJson,
    string? commanderName,
    IReadOnlyList<string> selectedQuestionIds,
    IReadOnlyList<string> bannedCards,
    CommanderSpellbookResult? comboResult = null,
    bool includeCardVersions = false)
```
Three corresponding private static implementations: BuildAnalysisPromptChatGpt, BuildAnalysisPromptClaude, BuildAnalysisPromptGemini (same parameter list).

From DeckFlow.Web/Services/DeckAnalysisPacketService.cs:1499 (SetUpgrade family):
```
internal static string BuildSetUpgradePrompt(
    DeckAnalysisRequest request,
    string decklistText,
    string deckProfileJson,
    string? commanderName,
    string? generatedSetPacket,
    IReadOnlyList<string> bannedCards)
```
Three implementations: BuildSetUpgradePromptChatGpt, BuildSetUpgradePromptClaude, BuildSetUpgradePromptGemini.

From DeckFlow.Web/Services/DeckComparisonService.cs:680-696 area (Comparison family — internal dispatcher; check exact signature in file before extracting):
```
internal static string BuildComparisonPrompt(
    DeckComparisonDeckSummary deckA,
    DeckComparisonDeckSummary deckB,
    string deckAListText,
    string deckBListText,
    string deckAComboText,
    string deckBComboText,
    string comparisonContextText,
    string comparisonSchemaJson,
    string targetAiPlatform)
```
Three implementations: BuildComparisonPromptChatGpt, BuildComparisonPromptClaude, BuildComparisonPromptGemini.

From DeckFlow.Web/Services/DeckComparisonService.cs:1000 (FollowUp family):
```
internal static string BuildFollowUpPrompt(
    string comparisonSchemaJson,
    string targetAiPlatform)
```
Three implementations: BuildFollowUpPromptChatGpt, BuildFollowUpPromptClaude, BuildFollowUpPromptGemini.

From DeckFlow.Web/Services/MetaGapService.cs:455-470 area (MetaGap family — check exact signature):
```
internal static string BuildPrompt(   // note: NOT prefixed "BuildMetaGapPrompt" — local name is just "BuildPrompt" in this service
    string commanderName,
    IReadOnlyList<DeckEntry> myDeckEntries,
    CommanderSpellbookResult? myDeckCombos,
    IReadOnlyList<EdhTop16Entry> selectedEntries,
    IReadOnlyList<CommanderSpellbookResult?> referenceDeckCombos,
    IReadOnlyDictionary<string, string> oracleNameMap,
    string schemaJson,
    string targetAiPlatform)
```
Three implementations: BuildPromptChatGpt, BuildPromptClaude, BuildPromptGemini.

NOTE: Analysis + SetUpgrade dispatchers take a `DeckAnalysisRequest request` parameter (they read `request.TargetAiPlatform` inside the switch). Comparison + FollowUp + MetaGap take a bare `string targetAiPlatform` parameter (the request type isn't passed in). The variant interfaces' Build methods must mirror their family's parameter shape — DO NOT homogenize them to a common signature. Each family is allowed to have its own Build signature, identical to the original internal static method MINUS the `targetAiPlatform`/`request.TargetAiPlatform` argument (the registry takes AiPlatform separately).

Registry signature per design doc §"Layer 2" lines 122-135 (verbatim):
```
internal sealed class XxxPromptVariantRegistry
{
    private readonly IReadOnlyDictionary<AiPlatform, IXxxPromptVariant> _variants;

    public XxxPromptVariantRegistry(IEnumerable<IXxxPromptVariant> variants)
    {
        _variants = variants.ToDictionary(v => v.Platform);
    }

    public string Build(AiPlatform platform, /* same args as IXxxPromptVariant.Build minus platform */)
        => _variants.TryGetValue(platform, out var variant)
            ? variant.Build(/* args */)
            : _variants[AiPlatform.Default].Build(/* args */);
}
```

Interface signature shape (per design doc §"Layer 2" lines 103-116):
```
internal interface IXxxPromptVariant
{
    AiPlatform Platform { get; }
    string Build(/* family-specific parameter list */);
}
```

DI registration shape (design doc §"Migration of existing code" line 142):
```
services.AddSingleton<IAnalysisPromptVariant, ChatGptAnalysisPromptVariant>();
services.AddSingleton<IAnalysisPromptVariant, ClaudeAnalysisPromptVariant>();
services.AddSingleton<IAnalysisPromptVariant, GeminiAnalysisPromptVariant>();
services.AddSingleton<AnalysisPromptVariantRegistry>();
// repeat for the other 4 families = 20 total lines
```
DI container resolves `IEnumerable<IAnalysisPromptVariant>` for the registry ctor automatically when 3 implementations are registered against the same interface.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract the Analysis prompt-builder family</name>
  <files>DeckFlow.Web/Services/PromptBuilders/Analysis/IAnalysisPromptVariant.cs, DeckFlow.Web/Services/PromptBuilders/Analysis/ChatGptAnalysisPromptVariant.cs, DeckFlow.Web/Services/PromptBuilders/Analysis/ClaudeAnalysisPromptVariant.cs, DeckFlow.Web/Services/PromptBuilders/Analysis/GeminiAnalysisPromptVariant.cs, DeckFlow.Web/Services/PromptBuilders/Analysis/AnalysisPromptVariantRegistry.cs, DeckFlow.Web/Services/DeckAnalysisPacketService.cs</files>
  <read_first>
    - DeckFlow.Web/Services/DeckAnalysisPacketService.cs (current full file — confirm the Analysis dispatcher at line 842, plus the 3 private static implementations BuildAnalysisPromptChatGpt/Claude/Gemini that follow. Identify exact byte ranges to extract.)
    - DeckFlow.Web/Models/AiPlatform.cs (Plan 15-01 output — confirms AiPlatform.ChatGpt/Claude/Gemini references for variant `Platform` getter, AiPlatform.Default for registry fallback)
    - .planning/milestones/v1.2-phases/10-claude-gemini-artifact-optimization/10-AISEL-PLATFORM-DESIGN.md (Layer 2 lines 98-135 — interface + registry shape binding)
    - .planning/phases/15-aiplatform-value-object-refactor/15-CONTEXT.md (D-04 — per-family subdir, file-per-type; D-06 — variant Build bodies must be byte-identical copies)
    - .planning/codebase/CONVENTIONS.md (interface co-location vs file-per-type: D-04 binds file-per-type for variants; namespace = `DeckFlow.Web.Services.PromptBuilders.Analysis`)
    - DeckFlow.Web/Services/CardLookupService.cs (XML <summary> tone anchor)
    - CLAUDE.md (DO NOT format/cleanup; preserve raw-string literals — variant Build bodies CONTAIN raw-string literals from the original switch arms, copy them verbatim including indentation)
  </read_first>
  <action>
    Create directory `DeckFlow.Web/Services/PromptBuilders/Analysis/` and produce 5 new files inside:

    **File 1: `IAnalysisPromptVariant.cs`**
    - File-scoped namespace `DeckFlow.Web.Services.PromptBuilders.Analysis;`
    - Required usings: `using DeckFlow.Web.Models;` (for AiPlatform), `using DeckFlow.Web.Services;` (for CommanderSpellbookResult — currently in this assembly's Services namespace; verify the exact location and add the right using).
    - `internal interface IAnalysisPromptVariant` with:
      - `AiPlatform Platform { get; }`
      - `string Build(DeckAnalysisRequest request, string decklistText, string referenceText, string deckProfileSchemaJson, string? commanderName, IReadOnlyList<string> selectedQuestionIds, IReadOnlyList<string> bannedCards, CommanderSpellbookResult? comboResult, bool includeCardVersions);`
    - XML `<summary>` on interface + Platform property + Build method per CardLookupService tone anchor.
    - Note: drop default-parameter values from the interface signature (`= null`, `= false`) — interfaces don't use default-params; the registry's pass-through Build method keeps the optional params as a convenience for callers.

    **File 2: `ChatGptAnalysisPromptVariant.cs`**
    - Namespace `DeckFlow.Web.Services.PromptBuilders.Analysis;`
    - `internal sealed class ChatGptAnalysisPromptVariant : IAnalysisPromptVariant`
    - `public AiPlatform Platform => AiPlatform.ChatGpt;`
    - `public string Build(...) { ... }` — Body is the EXACT byte-for-byte copy of the existing `private static string BuildAnalysisPromptChatGpt(...)` body from `DeckAnalysisPacketService.cs` starting at line 852 (or wherever it lives — verify with current file state). DO NOT modify any string literals, raw-string indentation, parameter ordering, local variable usage, or method-internal helper calls. The variant class still has access to static helpers on the host service via fully-qualified `DeckAnalysisPacketService.SomeHelper(...)` calls if those helpers are still `internal`/`internal static` — if any helper is `private`, you have two options: (a) bump it to `internal static` so the variant can call it, OR (b) duplicate the helper into the variant class as a `private static` member. Pick (a) where the helper is generic/reusable across variants; pick (b) where the helper is one-off per-AI. Document each promotion to internal with a short `<remarks>` note for future readers.
    - XML `<summary>` on class + Platform + Build per anchor.

    **File 3: `ClaudeAnalysisPromptVariant.cs`** — same pattern, `Platform => AiPlatform.Claude`, Build body = byte-for-byte copy of `BuildAnalysisPromptClaude`.

    **File 4: `GeminiAnalysisPromptVariant.cs`** — same pattern, `Platform => AiPlatform.Gemini`, Build body = byte-for-byte copy of `BuildAnalysisPromptGemini`.

    **File 5: `AnalysisPromptVariantRegistry.cs`**
    - Namespace `DeckFlow.Web.Services.PromptBuilders.Analysis;`
    - `internal sealed class AnalysisPromptVariantRegistry`
    - Private field: `private readonly IReadOnlyDictionary<AiPlatform, IAnalysisPromptVariant> _variants;`
    - Ctor: `public AnalysisPromptVariantRegistry(IEnumerable<IAnalysisPromptVariant> variants) { _variants = variants.ToDictionary(v => v.Platform); }`
    - Public Build method matching the original `BuildAnalysisPrompt` signature, but FIRST parameter is `AiPlatform platform` (extracted from `request.TargetAiPlatform` by the caller):
      ```
      public string Build(AiPlatform platform, DeckAnalysisRequest request, string decklistText, string referenceText, string deckProfileSchemaJson, string? commanderName, IReadOnlyList<string> selectedQuestionIds, IReadOnlyList<string> bannedCards, CommanderSpellbookResult? comboResult = null, bool includeCardVersions = false)
      ```
    - Body: `=> _variants.TryGetValue(platform, out var variant) ? variant.Build(request, decklistText, referenceText, deckProfileSchemaJson, commanderName, selectedQuestionIds, bannedCards, comboResult, includeCardVersions) : _variants[AiPlatform.Default].Build(request, decklistText, referenceText, deckProfileSchemaJson, commanderName, selectedQuestionIds, bannedCards, comboResult, includeCardVersions);` (or use a temporary variable + helper method to avoid duplicating the call site twice — author's call; both are acceptable).
    - XML `<summary>` on class + ctor + Build per anchor.

    **Modify `DeckAnalysisPacketService.cs`:**
    - Add ctor parameter `AnalysisPromptVariantRegistry analysisPromptRegistry` to the existing public constructor. Store in a new private readonly field `_analysisPromptRegistry`. Add `ArgumentNullException.ThrowIfNull(analysisPromptRegistry);` at the top of the ctor (matches CardLookupService argument-validation pattern).
    - Replace the body of `internal static string BuildAnalysisPrompt(...)` with a delegation to the registry. But: it's currently `static`, and the registry is an instance field. Two options:
      - Option A (recommended): Change `BuildAnalysisPrompt` to an instance method on the service. Update all call sites (search the file for `BuildAnalysisPrompt(` — confirm count before editing). Body becomes `_analysisPromptRegistry.Build(AiPlatform.Normalize(request.TargetAiPlatform), request, decklistText, referenceText, deckProfileSchemaJson, commanderName, selectedQuestionIds, bannedCards, comboResult, includeCardVersions);`
      - Option B: Keep `BuildAnalysisPrompt` static; pass the registry as an additional argument from every caller. More call-site churn but preserves the existing signature pattern. Reject if call-site count is high.
    - Pick Option A by default. Document the visibility change in a one-line comment above the (now instance) method.
    - DELETE the 3 private static implementations `BuildAnalysisPromptChatGpt`, `BuildAnalysisPromptClaude`, `BuildAnalysisPromptGemini` from `DeckAnalysisPacketService.cs` — they now live in the variant files. If any of those private statics call other now-private helpers in the host service that have moved with them, promote those host helpers to `internal static` or copy them into the variants per the earlier guidance.
    - Update the `internal static` test seam if any test directly invokes `BuildAnalysisPromptChatGpt` etc. Plan 15-01 already migrated the round-trip tests; this plan's variant-extraction may break older test references. Run `grep -rn "BuildAnalysisPromptChatGpt\|BuildAnalysisPromptClaude\|BuildAnalysisPromptGemini" DeckFlow.Web.Tests/` and reroute any hits to either the registry or the variant class directly (variants are `internal`, so `InternalsVisibleTo` already covers test access).

    Single-purpose commit for this family — commit message: `refactor(15-02): extract Analysis prompt-builder family to per-platform variants`

    DO NOT touch the SetUpgrade dispatcher yet — that's Task 2. The build will stay GREEN after this task because (a) BuildAnalysisPrompt's NEW body still produces identical output, (b) BuildSetUpgradePrompt's switch is untouched, (c) Razor + setters from Plan 15-01 are independent.
  </action>
  <verify>
    <automated>dotnet build DeckFlow.Web/DeckFlow.Web.csproj --configuration Release</automated>
  </verify>
  <acceptance_criteria>
    - Directory `DeckFlow.Web/Services/PromptBuilders/Analysis/` exists with 5 files.
    - `IAnalysisPromptVariant.cs` contains `internal interface IAnalysisPromptVariant` and `AiPlatform Platform { get; }`.
    - 3 variant files each contain `internal sealed class XxxAnalysisPromptVariant : IAnalysisPromptVariant` and `public AiPlatform Platform => AiPlatform.{ChatGpt|Claude|Gemini};`.
    - `AnalysisPromptVariantRegistry.cs` contains the substring `ToDictionary(v => v.Platform)`.
    - `AnalysisPromptVariantRegistry.cs` contains a fallback to `AiPlatform.Default` for unknown-platform lookups.
    - `DeckFlow.Web/Services/DeckAnalysisPacketService.cs` no longer contains the substrings `BuildAnalysisPromptChatGpt`, `BuildAnalysisPromptClaude`, `BuildAnalysisPromptGemini` (the 3 private static implementations are gone).
    - `DeckFlow.Web/Services/DeckAnalysisPacketService.cs` no longer contains the substring `"Claude" => BuildAnalysisPromptClaude` (old dispatcher body gone).
    - `DeckFlow.Web/Services/DeckAnalysisPacketService.cs` contains the substring `AnalysisPromptVariantRegistry` (new ctor param + field).
    - `dotnet build DeckFlow.Web/DeckFlow.Web.csproj --configuration Release` exits 0 — note: Program.cs has NOT been updated yet, so DI resolution will fail at runtime BUT compilation succeeds because the ctor parameter is satisfied by reflection-based DI at app start, not at compile time. If compilation fails because something else broke, fix in this task before commit.
    - Commit log shows one commit titled `refactor(15-02): extract Analysis prompt-builder family to per-platform variants` with no `Co-Authored-By` trailer.
  </acceptance_criteria>
  <done>Analysis family extracted (5 new files + DeckAnalysisPacketService trimmed); build green; SetUpgrade switch still in place (Task 2).</done>
</task>

<task type="auto">
  <name>Task 2: Extract the SetUpgrade prompt-builder family</name>
  <files>DeckFlow.Web/Services/PromptBuilders/SetUpgrade/ISetUpgradePromptVariant.cs, DeckFlow.Web/Services/PromptBuilders/SetUpgrade/ChatGptSetUpgradePromptVariant.cs, DeckFlow.Web/Services/PromptBuilders/SetUpgrade/ClaudeSetUpgradePromptVariant.cs, DeckFlow.Web/Services/PromptBuilders/SetUpgrade/GeminiSetUpgradePromptVariant.cs, DeckFlow.Web/Services/PromptBuilders/SetUpgrade/SetUpgradePromptVariantRegistry.cs, DeckFlow.Web/Services/DeckAnalysisPacketService.cs</files>
  <read_first>
    - DeckFlow.Web/Services/DeckAnalysisPacketService.cs (Task 1 has trimmed Analysis; SetUpgrade dispatcher is still at line ~1499, with 3 private statics BuildSetUpgradePromptChatGpt/Claude/Gemini following)
    - DeckFlow.Web/Services/PromptBuilders/Analysis/ (Task 1's outputs — use as structural template for this family)
    - DeckFlow.Web/Models/AiPlatform.cs
    - .planning/phases/15-aiplatform-value-object-refactor/15-CONTEXT.md (D-04 file-per-type; D-06 byte-identical Build bodies)
    - .planning/milestones/v1.2-phases/10-claude-gemini-artifact-optimization/10-AISEL-PLATFORM-DESIGN.md (Layer 2)
  </read_first>
  <action>
    Mirror Task 1 structure exactly for the SetUpgrade family. Create 5 files in `DeckFlow.Web/Services/PromptBuilders/SetUpgrade/`:

    - `ISetUpgradePromptVariant.cs` — interface with `AiPlatform Platform { get; }` and `string Build(DeckAnalysisRequest request, string decklistText, string deckProfileJson, string? commanderName, string? generatedSetPacket, IReadOnlyList<string> bannedCards);` (signature derived from existing `BuildSetUpgradePrompt` at DeckAnalysisPacketService.cs:1499).
    - `ChatGptSetUpgradePromptVariant.cs`, `ClaudeSetUpgradePromptVariant.cs`, `GeminiSetUpgradePromptVariant.cs` — each `internal sealed`, each `Platform => AiPlatform.{platform};`, each Build body is byte-for-byte copy of `BuildSetUpgradePrompt{Platform}` from `DeckAnalysisPacketService.cs`.
    - `SetUpgradePromptVariantRegistry.cs` — same ctor/Build pattern as `AnalysisPromptVariantRegistry`.

    **Modify `DeckAnalysisPacketService.cs`:**
    - Add 2nd new ctor parameter `SetUpgradePromptVariantRegistry setUpgradePromptRegistry` and store in `_setUpgradePromptRegistry` field with `ArgumentNullException.ThrowIfNull(...)`.
    - Convert `BuildSetUpgradePrompt` to instance method (same Option A as Task 1).
    - Replace body with: `_setUpgradePromptRegistry.Build(AiPlatform.Normalize(request.TargetAiPlatform), request, decklistText, deckProfileJson, commanderName, generatedSetPacket, bannedCards);`.
    - DELETE the 3 private statics `BuildSetUpgradePromptChatGpt/Claude/Gemini`. Promote any shared helpers to `internal static` or duplicate into variants per Task 1's guidance.

    Commit message: `refactor(15-02): extract SetUpgrade prompt-builder family to per-platform variants`
  </action>
  <verify>
    <automated>dotnet build DeckFlow.Web/DeckFlow.Web.csproj --configuration Release</automated>
  </verify>
  <acceptance_criteria>
    - Directory `DeckFlow.Web/Services/PromptBuilders/SetUpgrade/` exists with 5 files.
    - `ISetUpgradePromptVariant.cs` has `internal interface ISetUpgradePromptVariant` + `AiPlatform Platform { get; }`.
    - 3 variant files each contain `internal sealed class XxxSetUpgradePromptVariant : ISetUpgradePromptVariant`.
    - `SetUpgradePromptVariantRegistry.cs` contains `ToDictionary(v => v.Platform)` + `AiPlatform.Default` fallback.
    - `DeckFlow.Web/Services/DeckAnalysisPacketService.cs` no longer contains `BuildSetUpgradePromptChatGpt`, `BuildSetUpgradePromptClaude`, `BuildSetUpgradePromptGemini`.
    - `DeckFlow.Web/Services/DeckAnalysisPacketService.cs` contains both `AnalysisPromptVariantRegistry` AND `SetUpgradePromptVariantRegistry` (Task 1 + Task 2 cumulative).
    - `dotnet build DeckFlow.Web/DeckFlow.Web.csproj --configuration Release` exits 0.
    - Commit: `refactor(15-02): extract SetUpgrade prompt-builder family to per-platform variants` — single logical change.
  </acceptance_criteria>
  <done>SetUpgrade extracted; DeckAnalysisPacketService now injects 2 registries; ctor argument count increased by 2 (acknowledged in CONTEXT.md "Risks identified" — pushes the service toward 9 deps, acceptable for v1.3 per CONTEXT.md).</done>
</task>

<task type="auto">
  <name>Task 3: Extract the Comparison + FollowUp prompt-builder families</name>
  <files>DeckFlow.Web/Services/PromptBuilders/Comparison/IComparisonPromptVariant.cs, DeckFlow.Web/Services/PromptBuilders/Comparison/ChatGptComparisonPromptVariant.cs, DeckFlow.Web/Services/PromptBuilders/Comparison/ClaudeComparisonPromptVariant.cs, DeckFlow.Web/Services/PromptBuilders/Comparison/GeminiComparisonPromptVariant.cs, DeckFlow.Web/Services/PromptBuilders/Comparison/ComparisonPromptVariantRegistry.cs, DeckFlow.Web/Services/PromptBuilders/FollowUp/IFollowUpPromptVariant.cs, DeckFlow.Web/Services/PromptBuilders/FollowUp/ChatGptFollowUpPromptVariant.cs, DeckFlow.Web/Services/PromptBuilders/FollowUp/ClaudeFollowUpPromptVariant.cs, DeckFlow.Web/Services/PromptBuilders/FollowUp/GeminiFollowUpPromptVariant.cs, DeckFlow.Web/Services/PromptBuilders/FollowUp/FollowUpPromptVariantRegistry.cs, DeckFlow.Web/Services/DeckComparisonService.cs</files>
  <read_first>
    - DeckFlow.Web/Services/DeckComparisonService.cs (current file — Comparison dispatcher at line ~698 with 9-arg signature `(DeckComparisonDeckSummary deckA, DeckComparisonDeckSummary deckB, string deckAListText, string deckBListText, string deckAComboText, string deckBComboText, string comparisonContextText, string comparisonSchemaJson, string targetAiPlatform)`; FollowUp dispatcher at line ~1000 with 2-arg signature `(string comparisonSchemaJson, string targetAiPlatform)`)
    - DeckFlow.Web/Services/PromptBuilders/Analysis/ (Tasks 1 + 2 patterns)
    - DeckFlow.Web/Models/AiPlatform.cs
    - .planning/phases/15-aiplatform-value-object-refactor/15-CONTEXT.md
  </read_first>
  <action>
    Two families in one task (Comparison + FollowUp) because they share the same host service `DeckComparisonService.cs`. Split into TWO commits so git-blame stays clean — one commit per family.

    **Commit 3a — Comparison family:**

    Create 5 files in `DeckFlow.Web/Services/PromptBuilders/Comparison/`:
    - `IComparisonPromptVariant.cs` — interface with `AiPlatform Platform { get; }` + `string Build(DeckComparisonDeckSummary deckA, DeckComparisonDeckSummary deckB, string deckAListText, string deckBListText, string deckAComboText, string deckBComboText, string comparisonContextText, string comparisonSchemaJson);` (drop the `string targetAiPlatform` parameter — that's the registry's dispatch key now).
    - 3 sealed variant classes with verbatim Build bodies from `BuildComparisonPromptChatGpt/Claude/Gemini` in `DeckComparisonService.cs`.
    - `ComparisonPromptVariantRegistry.cs` — same shape as Analysis registry. Build signature: `public string Build(AiPlatform platform, DeckComparisonDeckSummary deckA, ...)` mirroring the variant interface.

    Modify `DeckComparisonService.cs`:
    - Add ctor param `ComparisonPromptVariantRegistry comparisonPromptRegistry`; field `_comparisonPromptRegistry`; ArgumentNullException.
    - `BuildComparisonPrompt` becomes instance method (Option A from Task 1).
    - Body: `_comparisonPromptRegistry.Build(AiPlatform.Normalize(targetAiPlatform), deckA, deckB, deckAListText, deckBListText, deckAComboText, deckBComboText, comparisonContextText, comparisonSchemaJson);`.
    - DELETE `BuildComparisonPromptChatGpt/Claude/Gemini` private statics. Promote shared helpers per Task 1's guidance.

    Commit: `refactor(15-02): extract Comparison prompt-builder family to per-platform variants`

    **Commit 3b — FollowUp family:**

    Create 5 files in `DeckFlow.Web/Services/PromptBuilders/FollowUp/`:
    - `IFollowUpPromptVariant.cs` — interface with `AiPlatform Platform { get; }` + `string Build(string comparisonSchemaJson);` (the only argument is `comparisonSchemaJson` — `targetAiPlatform` is the registry dispatch key).
    - 3 sealed variant classes with verbatim Build bodies from `BuildFollowUpPromptChatGpt/Claude/Gemini`.
    - `FollowUpPromptVariantRegistry.cs` — same shape; Build signature `public string Build(AiPlatform platform, string comparisonSchemaJson)`.

    Modify `DeckComparisonService.cs`:
    - Add ctor param `FollowUpPromptVariantRegistry followUpPromptRegistry`; field `_followUpPromptRegistry`; ArgumentNullException.
    - `BuildFollowUpPrompt` becomes instance method.
    - Body: `_followUpPromptRegistry.Build(AiPlatform.Normalize(targetAiPlatform), comparisonSchemaJson);`.
    - DELETE `BuildFollowUpPromptChatGpt/Claude/Gemini` private statics.

    Commit: `refactor(15-02): extract FollowUp prompt-builder family to per-platform variants`

    After both commits: DeckComparisonService has 2 new ctor params (Comparison + FollowUp registries), 2 instance methods replacing static dispatchers, 6 deleted private statics.
  </action>
  <verify>
    <automated>dotnet build DeckFlow.Web/DeckFlow.Web.csproj --configuration Release</automated>
  </verify>
  <acceptance_criteria>
    - Directories `DeckFlow.Web/Services/PromptBuilders/Comparison/` and `DeckFlow.Web/Services/PromptBuilders/FollowUp/` each exist with 5 files.
    - `IComparisonPromptVariant.cs` and `IFollowUpPromptVariant.cs` each declare `AiPlatform Platform { get; }`.
    - 6 variant files (3 per family) each `internal sealed`.
    - `ComparisonPromptVariantRegistry.cs` + `FollowUpPromptVariantRegistry.cs` each contain `ToDictionary(v => v.Platform)` + `AiPlatform.Default` fallback.
    - `DeckFlow.Web/Services/DeckComparisonService.cs` no longer contains `BuildComparisonPromptChatGpt`, `BuildComparisonPromptClaude`, `BuildComparisonPromptGemini`, `BuildFollowUpPromptChatGpt`, `BuildFollowUpPromptClaude`, `BuildFollowUpPromptGemini`.
    - `DeckFlow.Web/Services/DeckComparisonService.cs` contains BOTH `ComparisonPromptVariantRegistry` AND `FollowUpPromptVariantRegistry` as ctor parameters / fields.
    - `dotnet build DeckFlow.Web/DeckFlow.Web.csproj --configuration Release` exits 0 after each of the 2 commits in this task.
    - Commit log contains TWO commits for this task (Comparison + FollowUp) — single-purpose per CLAUDE.md.
  </acceptance_criteria>
  <done>Comparison + FollowUp families extracted; DeckComparisonService injects 2 registries; 6 private statics removed.</done>
</task>

<task type="auto">
  <name>Task 4: Extract the MetaGap prompt-builder family</name>
  <files>DeckFlow.Web/Services/PromptBuilders/MetaGap/IMetaGapPromptVariant.cs, DeckFlow.Web/Services/PromptBuilders/MetaGap/ChatGptMetaGapPromptVariant.cs, DeckFlow.Web/Services/PromptBuilders/MetaGap/ClaudeMetaGapPromptVariant.cs, DeckFlow.Web/Services/PromptBuilders/MetaGap/GeminiMetaGapPromptVariant.cs, DeckFlow.Web/Services/PromptBuilders/MetaGap/MetaGapPromptVariantRegistry.cs, DeckFlow.Web/Services/MetaGapService.cs</files>
  <read_first>
    - DeckFlow.Web/Services/MetaGapService.cs (current file — `BuildPrompt` dispatcher at line ~471 with 8-arg signature `(string commanderName, IReadOnlyList<DeckEntry> myDeckEntries, CommanderSpellbookResult? myDeckCombos, IReadOnlyList<EdhTop16Entry> selectedEntries, IReadOnlyList<CommanderSpellbookResult?> referenceDeckCombos, IReadOnlyDictionary<string, string> oracleNameMap, string schemaJson, string targetAiPlatform)`)
    - DeckFlow.Web/Services/PromptBuilders/Analysis/ (template)
    - DeckFlow.Web/Models/AiPlatform.cs
    - .planning/phases/15-aiplatform-value-object-refactor/15-CONTEXT.md
  </read_first>
  <action>
    Create 5 files in `DeckFlow.Web/Services/PromptBuilders/MetaGap/`:
    - `IMetaGapPromptVariant.cs` — interface with `AiPlatform Platform { get; }` + `string Build(string commanderName, IReadOnlyList<DeckEntry> myDeckEntries, CommanderSpellbookResult? myDeckCombos, IReadOnlyList<EdhTop16Entry> selectedEntries, IReadOnlyList<CommanderSpellbookResult?> referenceDeckCombos, IReadOnlyDictionary<string, string> oracleNameMap, string schemaJson);`
    - 3 sealed variant classes with verbatim Build bodies from `BuildPromptChatGpt/Claude/Gemini` (note: the host service's local naming is `BuildPrompt*` not `BuildMetaGap*` — match the existing private static names when locating).
    - `MetaGapPromptVariantRegistry.cs` — same shape as Analysis registry; class name uses `MetaGap` prefix even though the host service's local method is `BuildPrompt` (registry namespace makes the family clear; D-04 alignment).

    Modify `MetaGapService.cs`:
    - Add ctor param `MetaGapPromptVariantRegistry metaGapPromptRegistry`; field `_metaGapPromptRegistry`; ArgumentNullException.
    - `BuildPrompt` becomes instance method.
    - Body: `_metaGapPromptRegistry.Build(AiPlatform.Normalize(targetAiPlatform), commanderName, myDeckEntries, myDeckCombos, selectedEntries, referenceDeckCombos, oracleNameMap, schemaJson);`.
    - DELETE `BuildPromptChatGpt/Claude/Gemini` private statics from `MetaGapService.cs`.

    Commit message: `refactor(15-02): extract MetaGap prompt-builder family to per-platform variants`
  </action>
  <verify>
    <automated>dotnet build DeckFlow.Web/DeckFlow.Web.csproj --configuration Release</automated>
  </verify>
  <acceptance_criteria>
    - Directory `DeckFlow.Web/Services/PromptBuilders/MetaGap/` exists with 5 files.
    - `IMetaGapPromptVariant.cs` has `internal interface IMetaGapPromptVariant` + `AiPlatform Platform { get; }`.
    - 3 variant files each `internal sealed class XxxMetaGapPromptVariant : IMetaGapPromptVariant`.
    - `MetaGapPromptVariantRegistry.cs` contains `ToDictionary(v => v.Platform)` + `AiPlatform.Default` fallback.
    - `DeckFlow.Web/Services/MetaGapService.cs` no longer contains `BuildPromptChatGpt`, `BuildPromptClaude`, `BuildPromptGemini`.
    - `DeckFlow.Web/Services/MetaGapService.cs` contains the substring `MetaGapPromptVariantRegistry` (ctor param + field).
    - `dotnet build DeckFlow.Web/DeckFlow.Web.csproj --configuration Release` exits 0.
    - All 5 host services (`DeckAnalysisPacketService`, `DeckComparisonService`, `MetaGapService`) have ZERO remaining `switch` statements on `targetAiPlatform` / `request.TargetAiPlatform` for prompt-builder dispatch.
  </acceptance_criteria>
  <done>All 5 prompt-builder families extracted; 15 variant files + 5 registry files + 5 interfaces = 25 new files in PromptBuilders/. Build green.</done>
</task>

<task type="auto">
  <name>Task 5: Wire 15 variants + 5 registries into Program.cs DI container</name>
  <files>DeckFlow.Web/Program.cs</files>
  <read_first>
    - DeckFlow.Web/Program.cs (current file — find the service registration block; ARCHITECTURE.md notes lines 50-189 hold the DI block, with the `// AI platform toggles` comment at line 67. Identify the best insertion point — likely after the AiPlatformOptions Configure at line 74, before the HTTP client registrations at line 81)
    - .planning/codebase/ARCHITECTURE.md (Program.cs DI patterns)
    - .planning/phases/15-aiplatform-value-object-refactor/15-CONTEXT.md (D-02 manual DI; "Group under a single `// AiPlatform prompt-builder strategy registries (Phase 15)` comment block")
    - The 25 files created in Tasks 1-4 (variant + registry namespace paths)
    - CLAUDE.md (touch only the lines needed — no other edits to Program.cs)
  </read_first>
  <action>
    Add a single block of 20 DI registrations to `Program.cs`, with one leading comment line:

    ```
    // AiPlatform prompt-builder strategy registries (Phase 15). Each family
    // gets 3 variants (one per AiPlatform.All entry) + 1 registry that
    // dispatches at runtime. Adding a 4th platform = N new variant classes
    // + N new AddSingleton lines + 1 entry in AiPlatform.All (zero edits
    // to host services, request setters, Razor partial, or context parser).
    builder.Services.AddSingleton<IAnalysisPromptVariant, ChatGptAnalysisPromptVariant>();
    builder.Services.AddSingleton<IAnalysisPromptVariant, ClaudeAnalysisPromptVariant>();
    builder.Services.AddSingleton<IAnalysisPromptVariant, GeminiAnalysisPromptVariant>();
    builder.Services.AddSingleton<AnalysisPromptVariantRegistry>();
    builder.Services.AddSingleton<ISetUpgradePromptVariant, ChatGptSetUpgradePromptVariant>();
    builder.Services.AddSingleton<ISetUpgradePromptVariant, ClaudeSetUpgradePromptVariant>();
    builder.Services.AddSingleton<ISetUpgradePromptVariant, GeminiSetUpgradePromptVariant>();
    builder.Services.AddSingleton<SetUpgradePromptVariantRegistry>();
    builder.Services.AddSingleton<IComparisonPromptVariant, ChatGptComparisonPromptVariant>();
    builder.Services.AddSingleton<IComparisonPromptVariant, ClaudeComparisonPromptVariant>();
    builder.Services.AddSingleton<IComparisonPromptVariant, GeminiComparisonPromptVariant>();
    builder.Services.AddSingleton<ComparisonPromptVariantRegistry>();
    builder.Services.AddSingleton<IFollowUpPromptVariant, ChatGptFollowUpPromptVariant>();
    builder.Services.AddSingleton<IFollowUpPromptVariant, ClaudeFollowUpPromptVariant>();
    builder.Services.AddSingleton<IFollowUpPromptVariant, GeminiFollowUpPromptVariant>();
    builder.Services.AddSingleton<FollowUpPromptVariantRegistry>();
    builder.Services.AddSingleton<IMetaGapPromptVariant, ChatGptMetaGapPromptVariant>();
    builder.Services.AddSingleton<IMetaGapPromptVariant, ClaudeMetaGapPromptVariant>();
    builder.Services.AddSingleton<IMetaGapPromptVariant, GeminiMetaGapPromptVariant>();
    builder.Services.AddSingleton<MetaGapPromptVariantRegistry>();
    ```

    Insertion point: AFTER the `AiPlatformOptions` Configure block (line 74-ish) and BEFORE the `AddHttpClient` calls at line 81. Reasoning: prompt-builder registries depend conceptually on the AiPlatform domain, which is configured immediately above; HTTP infrastructure is a different concern.

    Add the necessary `using` directives at the top of `Program.cs`:
    - `using DeckFlow.Web.Services.PromptBuilders.Analysis;`
    - `using DeckFlow.Web.Services.PromptBuilders.SetUpgrade;`
    - `using DeckFlow.Web.Services.PromptBuilders.Comparison;`
    - `using DeckFlow.Web.Services.PromptBuilders.FollowUp;`
    - `using DeckFlow.Web.Services.PromptBuilders.MetaGap;`

    DO NOT reorder existing usings. DO NOT reformat Program.cs. Add only what's needed.

    After this task, the DI container resolves:
    - `IEnumerable<IAnalysisPromptVariant>` → 3 variants → `AnalysisPromptVariantRegistry` ctor populates dictionary.
    - Same for the other 4 families.
    - `DeckAnalysisPacketService` ctor receives `AnalysisPromptVariantRegistry` + `SetUpgradePromptVariantRegistry` (resolved by DI).
    - `DeckComparisonService` ctor receives `ComparisonPromptVariantRegistry` + `FollowUpPromptVariantRegistry`.
    - `MetaGapService` ctor receives `MetaGapPromptVariantRegistry`.

    Commit message: `refactor(15-02): register 15 prompt-builder variants + 5 registries in DI`
  </action>
  <verify>
    <automated>dotnet build DeckFlow.sln --configuration Release</automated>
  </verify>
  <acceptance_criteria>
    - `DeckFlow.Web/Program.cs` contains EXACTLY 15 lines matching `AddSingleton<IAnalysisPromptVariant|ISetUpgradePromptVariant|IComparisonPromptVariant|IFollowUpPromptVariant|IMetaGapPromptVariant,` (15 variant registrations).
    - `DeckFlow.Web/Program.cs` contains EXACTLY 5 lines matching `AddSingleton<(?:Analysis|SetUpgrade|Comparison|FollowUp|MetaGap)PromptVariantRegistry>` (5 registry registrations).
    - `DeckFlow.Web/Program.cs` contains the 5 `using DeckFlow.Web.Services.PromptBuilders.*` directives at the top of the file.
    - `dotnet build DeckFlow.sln --configuration Release` exits 0 with zero new warnings.
    - Manual smoke check (user-driven per CLAUDE.md feedback memory — do NOT auto-launch dev server): user runs `scripts/run-web.sh`, hits `/deck-analysis`, generates a deck-analysis packet for ChatGPT and Claude, and confirms the artifacts download. Plan 15-03 owns the full T1-T8 verification; this is a one-page smoke check that DI wiring resolves.
    - Final cumulative build state from Plan 15-02: all 5 prompt-builder switches gone from host services; 25 new files exist under PromptBuilders/; 20 DI lines added to Program.cs.
  </acceptance_criteria>
  <done>DI wiring complete; full solution builds clean; Plan 15-03 (4th-platform extension proof + T1-T8) is unblocked.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| AiPlatform.Normalize call site → registry dispatch | The Normalize call ensures only known-set AiPlatform values reach the registry's `_variants.TryGetValue`. Defense-in-depth: registry falls back to `AiPlatform.Default` even if an unknown value somehow arrives. |

## STRIDE Threat Register

Refactor only — no new attack surface. Preserves Phase 10 Normalize defense (unrecognized `target_ai_platform` → ChatGPT default). Variant Build bodies are byte-identical copies of pre-refactor switch arms (no logic mutation, no new code paths).

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-03 | Tampering | Registry lookup with crafted AiPlatform | mitigate | Registry's `TryGetValue` + fallback to `_variants[AiPlatform.Default]` ensures no `KeyNotFoundException` even if an unregistered AiPlatform instance reaches the registry. Production path: `AiPlatform.Normalize` at the host service call site guarantees only `AiPlatform.All` members reach the registry. |
| T-15-04 | Denial of Service | DI resolution failure if a variant is missing | accept | DI container fails fast at app start if `IEnumerable<IXxxPromptVariant>` resolves to fewer than 3 entries (the registry ctor would build a dictionary with missing keys and the fallback masks the gap silently in production). Mitigation accepted: Plan 15-03's SC5 test exercises the registry with `AiPlatform.Test`, which would surface a missing-variant bug during test runs. |
| T-15-SC | Tampering | (No new package installs) | n/a | This plan creates only first-party source files; no npm/pip/cargo install tasks. Package legitimacy gate not applicable. |
</threat_model>

<verification>
- Plan 15-02 final build: `dotnet build DeckFlow.sln --configuration Release` exits 0 with zero new warnings vs. Phase 14 baseline.
- Grep gate: `grep -rn "switch" DeckFlow.Web/Services/DeckAnalysisPacketService.cs DeckFlow.Web/Services/DeckComparisonService.cs DeckFlow.Web/Services/MetaGapService.cs | grep -v "^#" | grep -iE "ChatGPT|Claude|Gemini"` returns 0 matches (all prompt-builder switches gone from host services).
- Grep gate: `ls DeckFlow.Web/Services/PromptBuilders/*/` lists exactly 5 family subdirectories, each with 5 files (25 files total).
- Grep gate: `grep -c "AddSingleton.*PromptVariant" DeckFlow.Web/Program.cs` returns 20 (15 variants + 5 registries).
- Grep gate: `grep -c "PromptVariantRegistry" DeckFlow.Web/Services/DeckAnalysisPacketService.cs` returns at least 4 (2 ctor params + 2 fields, possibly more if used in multiple methods). Similar non-zero counts in DeckComparisonService.cs (Comparison + FollowUp = at least 4) and MetaGapService.cs (MetaGap = at least 2).
- Manual smoke (user-driven): one round-trip per builder family confirms artifacts still generate. Full T1-T8 happens in Plan 15-03.
</verification>

<success_criteria>
- 25 new files under `DeckFlow.Web/Services/PromptBuilders/` organized by family (5 subdirs × 5 files).
- All 5 prompt-builder switches removed from host services.
- 3 host services (`DeckAnalysisPacketService`, `DeckComparisonService`, `MetaGapService`) inject the appropriate registries via ctor.
- 20 new DI registration lines in `Program.cs` (15 variants + 5 registries).
- Variant Build bodies are byte-identical copies of the pre-refactor switch-arm bodies — no behavior change.
- D-06 preservation invariants intact: `"ChatGPT"`/`"Claude"`/`"Gemini"` Key string contract; `request.TargetAiPlatform` property name; form field name; `"chatgpt"` zip filename fallback; `DECKFLOW_GEMINI_ENABLED`; 22 guild theme CSS forks; no `Co-Authored-By` trailer.
- `dotnet build DeckFlow.sln --configuration Release` exits 0 with zero new warnings.
- 5 single-purpose commits (one per family extraction), grouped under one plan in the git log.
</success_criteria>

<output>
Create `.planning/phases/15-aiplatform-value-object-refactor/15-02-SUMMARY.md` when done. Summary records: 25 new files created, 5 commits (one per family + one for the DI wiring task = 6 commits total — Analysis, SetUpgrade, Comparison, FollowUp, MetaGap, DI), final build status, smoke-check outcome per family, and confirmation that Plan 15-03 (extension test + T1-T8 verification + byte-identical hash check) is unblocked.
</output>
