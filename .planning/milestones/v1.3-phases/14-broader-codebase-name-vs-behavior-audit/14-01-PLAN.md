---
id: 14-01
phase: 14-broader-codebase-name-vs-behavior-audit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/14-broader-codebase-name-vs-behavior-audit/14-BASELINE.md
  - .planning/phases/14-broader-codebase-name-vs-behavior-audit/14-AUDIT-REPORT.md
autonomous: true
requirements:
  - AUDIT-01
  - AUDIT-02
  - AUDIT-03
must_haves:
  truths:
    - "14-BASELINE.md exists with pre-phase warning count, public type count per project, and test-double prefix census captured against current v1.3 HEAD"
    - "14-AUDIT-REPORT.md exists listing every production-code rename candidate (with old→new mapping and rationale), every test-double rename (with Fake/Stub/Throwing canonical choice and any collision notes), and every public type missing <summary> per project"
    - "Plan 14-02 + Plan 14-03 can be executed without re-running discovery; they read the two artifacts and act"
    - "No source-code edits made in this plan; only .planning/phases/14-broader-codebase-name-vs-behavior-audit/*.md and a single git commit landing them"
    - "D-01: Audit method implemented as scripted grep-for-smells + targeted manual review of the 3 REQUIREMENTS.md candidates (ScryfallTaggerService, CommanderSpellbookService, test-double scoping); smell-grep produces the 14-AUDIT-REPORT.md rename worklist"
    - "D-07: Wave decomposition honored — Plan 14-01 baseline+audit (wave 1), Plan 14-02 renames (wave 2), Plan 14-03 doc backfill (wave 3), Plan 14-04 GenDocFile flip + final gate (wave 4); sequential depends_on per CONTEXT.md D-07"
  artifacts:
    - path: ".planning/phases/14-broader-codebase-name-vs-behavior-audit/14-BASELINE.md"
      provides: "Pre-phase build state + warning baseline + public type counts + test-double census"
      contains: "warning count: 0"
    - path: ".planning/phases/14-broader-codebase-name-vs-behavior-audit/14-AUDIT-REPORT.md"
      provides: "Authoritative rename + backfill worklist for Plans 14-02 and 14-03"
      contains: "## Renames (executed in Plan 14-02)"
  key_links:
    - from: "14-01-PLAN.md"
      to: "14-AUDIT-REPORT.md"
      via: "Plan 14-01 emits the report; Plans 14-02 and 14-03 cite it as their input"
      pattern: "## Renames \\(executed in Plan 14-02\\)"
    - from: "14-AUDIT-REPORT.md"
      to: "RESEARCH.md test-double census + named-candidate checklist"
      via: "Smell-grep + named-candidate verdicts re-run live and cross-checked against RESEARCH.md sections"
      pattern: "Test doubles \\(D-05 canonicalization\\)"
---

<objective>
Plan 14-01 captures the pre-phase build/warning baseline and emits the authoritative
audit report that Plans 14-02 and 14-03 will consume. No source-code commits beyond
the two markdown deliverables (14-BASELINE.md + 14-AUDIT-REPORT.md).

Purpose: Plans 14-02 (renames) and 14-03 (doc backfill) must be mechanical. They
read this plan's artifacts and execute; they do not re-discover. This plan also
overrides CONTEXT.md D-04's broken warning-gate assumption with the XML coverage
diff approach that Plan 14-04 will codify (per RESEARCH.md "GenerateDocumentationFile
Reality Check" — `.editorconfig` lines 93-96 silence CS1591/1573/1587 repo-wide,
so warnings cannot be the AUDIT-03 gate).

Output:
- `.planning/phases/14-broader-codebase-name-vs-behavior-audit/14-BASELINE.md`
- `.planning/phases/14-broader-codebase-name-vs-behavior-audit/14-AUDIT-REPORT.md`
- One commit landing both files: `docs(14-01): capture baseline + audit report`
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/14-broader-codebase-name-vs-behavior-audit/14-CONTEXT.md
@.planning/phases/14-broader-codebase-name-vs-behavior-audit/14-RESEARCH.md
@.planning/phases/13-chatgpt-class-rename-summary-doc-comments/13-CONTEXT.md
@.planning/phases/13-chatgpt-class-rename-summary-doc-comments/13-VERIFICATION.md
@.planning/codebase/CONVENTIONS.md
@CLAUDE.md
@.editorconfig

<interfaces>
<!-- Canonical test-double taxonomy (CONVENTIONS.md "Code Style"): -->
<!-- Fake*    — stateful behavior fakes (e.g., FakeCategoryKnowledgeStore) -->
<!-- Stub*    — queue/canned-return stubs (e.g., StubHttpMessageHandler) -->
<!-- Throwing* — exception injection (e.g., ThrowingCardSearchService) -->

<!-- Style anchors for required summary tone in 14-AUDIT-REPORT.md examples: -->
<!--   DeckFlow.Web/Services/CardLookupService.cs:39-42 (class) -->
<!--   DeckFlow.Web/Services/CardLookupService.cs:13-27 (interface + methods) -->
<!--   DeckFlow.Web/Services/CommanderSpellbookService.cs:14-19 (sealed record) -->
<!--   DeckFlow.Web.Tests/DeckAnalysisPacketServiceTests.cs:13-17 (test class) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Capture 14-BASELINE.md against live v1.3 HEAD</name>
  <files>.planning/phases/14-broader-codebase-name-vs-behavior-audit/14-BASELINE.md</files>
  <read_first>
    - .planning/phases/14-broader-codebase-name-vs-behavior-audit/14-CONTEXT.md (D-09 baseline command literal text)
    - .planning/phases/14-broader-codebase-name-vs-behavior-audit/14-RESEARCH.md sections "State of the Project (Live Probes 2026-05-17)" + "Baseline + AUDIT-03 Build Gate" (live commands and result format)
    - CLAUDE.md (dotnet path: `/mnt/c/Program Files/dotnet/dotnet.exe`; LF line endings; VSTest WSL constraint)
    - .editorconfig (lines 93-96 silence CS1591/1573/1587 — drives the XML-coverage approach codified here)
  </read_first>
  <action>
Implement per D-09 literal text and RESEARCH.md "Baseline + AUDIT-03 Build Gate" template.

1. Run D-09's literal baseline command verbatim and capture stdout/stderr + exit code:

       "/mnt/c/Program Files/dotnet/dotnet.exe" build DeckFlow.sln --configuration Release --verbosity quiet 2>&1 | tee /tmp/p14-baseline-build.log
       BASELINE_WARN_COUNT=$(grep -cE '^.*warning ' /tmp/p14-baseline-build.log)

2. Capture authoritative public type counts per project (RESEARCH.md command):

       grep -rE "^[[:space:]]*public +(sealed +)?(class|interface|record|abstract +class|static +class|partial +class) +[A-Z]" --include="*.cs" $PROJECT/

   Run for each of: DeckFlow.Core/, DeckFlow.Web/, DeckFlow.CLI/, DeckFlow.Core.Tests/, DeckFlow.Web.Tests/. Record the wc -l per project AND save the full grep output as 14-BASELINE-PUBLIC-TYPES.txt (sibling file) so 14-AUDIT-REPORT.md can derive the missing-summary list deterministically.

3. Capture test-double prefix census (RESEARCH.md "Test-Double Census" section). Run both:

       grep -rEn "(private|public|internal) +sealed +class +(Fake|Stub|Throwing)[A-Z]" --include="*.cs" DeckFlow.Web.Tests/ DeckFlow.Core.Tests/ | wc -l
       grep -rEn "(private|public|internal) +sealed +class +(Null|Test|Configurable|Capturing|Dummy|Successful|Failing|Mock|Empty|Spy|Recording)[A-Z]" --include="*.cs" DeckFlow.Web.Tests/ DeckFlow.Core.Tests/

   Record canonical-prefix totals and the full per-line list of non-canonical hits.

4. Capture pre-phase test discovery baseline (used to gate Plan 14-04 push-and-watch fallback). Per CLAUDE.md "VSTest unreliable in WSL", this is best-effort — if --list-tests hangs past 90s, document the timeout and rely on Render auto-deploy as the CI fallback:

       timeout 90 "/mnt/c/Program Files/dotnet/dotnet.exe" test DeckFlow.sln --no-build --configuration Release --list-tests 2>&1 | tee /tmp/p14-baseline-discovery.log
       BASELINE_TEST_COUNT=$(grep -cE "^    [A-Z]" /tmp/p14-baseline-discovery.log || echo "DISCOVERY-TIMEOUT")

5. Write 14-BASELINE.md using exactly the template at RESEARCH.md "Baseline + AUDIT-03 Build Gate" → "Capture in 14-BASELINE.md". Include the captured numbers, the HEAD SHA from `git rev-parse HEAD`, and a one-line note: "AUDIT-03 verification approach overridden — see 14-AUDIT-REPORT.md → XML Coverage Diff section. `.editorconfig` silences CS1591/1573/1587 globally, so warning-count alone is necessary-but-not-sufficient."

   Within 14-BASELINE.md, persist the Phase 14 baseline SHA on its own line so downstream Plans 14-02 / 14-03 / 14-04 can scope `git log` queries without relying on unset timestamp env vars:

       PHASE_14_START_SHA=$(git rev-parse HEAD)
       printf '\nPhase 14 baseline SHA: %s\n' "$PHASE_14_START_SHA" >> .planning/phases/14-broader-codebase-name-vs-behavior-audit/14-BASELINE.md

   Downstream plans read this SHA via:

       PHASE_14_START_SHA=$(grep -oE 'Phase 14 baseline SHA: [0-9a-f]+' .planning/phases/14-broader-codebase-name-vs-behavior-audit/14-BASELINE.md | awk '{print $NF}')

   and scope cross-plan history queries as `git log "${PHASE_14_START_SHA}..HEAD"` (SHA-based, immune to `--since=""` returning all history).

6. Do NOT commit yet — Task 2 + 3 produce the report; one commit covers all three files at end.
  </action>
  <verify>
    <automated>test -f .planning/phases/14-broader-codebase-name-vs-behavior-audit/14-BASELINE.md && grep -q "warning count" .planning/phases/14-broader-codebase-name-vs-behavior-audit/14-BASELINE.md && grep -qE "DeckFlow\.Core" .planning/phases/14-broader-codebase-name-vs-behavior-audit/14-BASELINE.md && grep -q "HEAD" .planning/phases/14-broader-codebase-name-vs-behavior-audit/14-BASELINE.md && grep -qE "Phase 14 baseline SHA: [0-9a-f]+" .planning/phases/14-broader-codebase-name-vs-behavior-audit/14-BASELINE.md</automated>
  </verify>
  <acceptance_criteria>
    - File exists at .planning/phases/14-broader-codebase-name-vs-behavior-audit/14-BASELINE.md
    - Contains the literal D-09 command (so Plan 14-04 can re-run identically)
    - Contains BASELINE_WARN_COUNT value (expected 0 per RESEARCH.md live probe)
    - Contains per-project public type counts (5 projects)
    - Contains canonical + non-canonical test-double counts
    - Contains BASELINE_TEST_COUNT or DISCOVERY-TIMEOUT marker
    - Contains HEAD SHA at moment of capture
    - Contains a `Phase 14 baseline SHA: <40-char SHA>` line for downstream SHA-based `git log` scoping (used by Plans 14-02/03/04 in lieu of unset timestamp env vars)
    - LF line endings (validate via `file 14-BASELINE.md` showing no CRLF)
    - No source-code files outside .planning/ touched
  </acceptance_criteria>
  <done>14-BASELINE.md committed-ready (commit lands at end of Task 3). Baseline reproducible by re-running the literal commands in the file.</done>
</task>

<task type="auto">
  <name>Task 2: Run smell-grep sweep + named-candidate manual review; compose 14-AUDIT-REPORT.md production-code + test-double sections</name>
  <files>.planning/phases/14-broader-codebase-name-vs-behavior-audit/14-AUDIT-REPORT.md</files>
  <read_first>
    - .planning/phases/14-broader-codebase-name-vs-behavior-audit/14-CONTEXT.md (D-01 smell-grep list, D-02 loose rename trigger, D-05 test-double consolidation, D-06 internal-scope rule)
    - .planning/phases/14-broader-codebase-name-vs-behavior-audit/14-RESEARCH.md sections "Audit Mechanics (AUDIT-01 / Plan 14-01)" (smell-grep script + named-candidate verdicts), "Test-Double Census (D-05) — Actual Distribution" (8 doubles, not 4; TestServiceFactory allowlist; FakeCardLookupService collision)
    - .planning/codebase/CONVENTIONS.md (canonical Fake/Stub/Throwing taxonomy)
    - DeckFlow.Web/Services/ScryfallTaggerService.cs (manual review subject — named in REQUIREMENTS.md AUDIT-01 example list)
    - DeckFlow.Web/Services/CommanderSpellbookService.cs (manual review subject — named in REQUIREMENTS.md AUDIT-01 example list; also a style anchor)
    - DeckFlow.Web.Tests/DeckControllerTests.cs lines 800-1000 (where 5 of the 8 non-canonical doubles live + the FakeCardLookupService name-collision risk at L914)
    - DeckFlow.Web.Tests/AdminFeedbackControllerTests.cs:144 (NullTempDataProvider)
    - DeckFlow.Web.Tests/CommanderControllerTests.cs:117 (DummyCommanderSearchService)
    - DeckFlow.Core.Tests/ArchidektDeckCacheSessionTests.cs:116 (FailingRecentDecksImporter)
  </read_first>
  <action>
Run the smell-grep script verbatim from RESEARCH.md "Smell-grep script (cheap, runs in <2s)" — Smells 1-6 — and compose the audit report's `## Renames (executed in Plan 14-02)` section.

Concrete rules:

1. **Smell 6 (non-canonical test-double prefixes)** — RESEARCH.md "Test-Double Census" enumerated 8 non-canonical doubles against live HEAD; the checker (revision 1) surfaced a 9th preliminary rename required to resolve a hard collision (see step 1c below). Enumerate **9** renames in the report verbatim with old→new mapping. Honor the planner pre-decisions surfaced by RESEARCH.md plus the collision resolution:

   a. **NEW (preliminary collision-resolution rename — added in revision 1):** `DeckFlow.Web.Tests/DeckControllerTests.cs:810` :: `FakeMetaGapService` (existing, canned-response semantics — returns a hardcoded `MetaGapResult` regardless of input per L810-828 body) → `StubMetaGapService`. Rationale: per CONVENTIONS.md "Code Style" taxonomy, canned-response = Stub, not Fake. This rename frees the `FakeMetaGapService` name for the rename in row (c) below and ends with a clean three-pattern set (Stub / Fake / Throwing) for the MetaGap test surface (`StubMetaGapService` L810, `FakeMetaGapService` L831 post-rename, `ThrowingMetaGapService` L844 unchanged). This is Option A from the checker (preliminary rename, full semantic correctness, one extra rename) — not Option B (qualifier-preserving). Documented under `### Name-collision notes` below.
   b. `DeckFlow.Web.Tests/AdminFeedbackControllerTests.cs:144` :: `NullTempDataProvider` → `StubTempDataProvider` (no-op stub semantics)
   c. `DeckFlow.Web.Tests/DeckControllerTests.cs:831` :: `ConfigurableMetaGapService` → `FakeMetaGapService` (configurable = stateful fake; depends on row (a) freeing the name)
   d. `DeckFlow.Web.Tests/DeckControllerTests.cs:870` :: `CapturingDeckAnalysisPacketService` → `FakeDeckAnalysisPacketService` (state-capture is stateful fake; document capture semantics in the new `<summary>`)
   e. `DeckFlow.Web.Tests/DeckControllerTests.cs:939` :: `SuccessfulCardLookupService` → `StubSuccessfulCardLookupService` (Stub semantics; preserves "Successful" qualifier to disambiguate from the pre-existing `FakeCardLookupService` at L914 — see collision note below)
   f. `DeckFlow.Web.Tests/DeckControllerTests.cs:948` :: `SuccessfulSingleCardLookupService` → `StubSuccessfulSingleCardLookupService`
   g. `DeckFlow.Web.Tests/DeckControllerTests.cs:987` :: `SuccessfulMechanicLookupService` → `StubSuccessfulMechanicLookupService`
   h. `DeckFlow.Web.Tests/CommanderControllerTests.cs:117` :: `DummyCommanderSearchService` → `StubCommanderSearchService` (no-op stub; verify the body returns empty/canned results, not stateful captures)
   i. `DeckFlow.Core.Tests/ArchidektDeckCacheSessionTests.cs:116` :: `FailingRecentDecksImporter` → `ThrowingRecentDecksImporter` (matches existing `Throwing*` taxonomy)

   **Plan 14-02 execution order constraint:** Row (a) MUST execute BEFORE row (c). Rows (b), (d)-(i) can run in any order. The audit report rename table must list row (a) first (or otherwise call out the ordering dependency explicitly) so Plan 14-02 executor can not collide mid-rename.

   Add a `## Allowlist` subsection that explicitly names `TestServiceFactory` (in `DeckFlow.Web.Tests/TestDoubles/TestServiceFactory.cs`) as NOT a rename target — it is a legitimate test-only factory pattern, not a test double.

   Add a `### Name-collision notes` block stating:
   - `DeckControllerTests.cs:914` already has `private sealed class FakeCardLookupService`. The chosen `Stub` prefix on the three `Successful*` doubles (rows e/f/g) plus preserving the "Successful" qualifier avoids the collision documented in RESEARCH.md "Naming-collision risk in DeckControllerTests.cs".
   - `DeckControllerTests.cs:810` already has `private sealed class FakeMetaGapService` (canned-response semantics) and `DeckControllerTests.cs:831` `ConfigurableMetaGapService` is targeted to rename to `FakeMetaGapService` (configurable/stateful-fake semantics). Two nested classes with the same name in the same outer class will not compile; ABORT path = build breaks on first commit. **Resolution: Option A — preliminary rename L810 to `StubMetaGapService` first (row a above), then L831 to `FakeMetaGapService` (row c).** Rationale: D-02 trigger is loose ("any reader benefits"); L810's `FakeMetaGapService` is mis-prefixed (canned-response = Stub per CONVENTIONS.md); Phase 14 already has the test-double consolidation in scope; Option A is one extra rename for full semantic correctness; Option B (qualifier-preserving `ConfigurableMetaGapService` → `FakeConfigurableMetaGapService`) leaves a known-wrong prefix in place and counts as a deferred-cleanup smell.

2. **Smell 5 (file-vs-type-name mismatch)** — Run the grep snippet verbatim. Filter false positives per the snippet's own `I$base` check. Any genuine mismatches surfaced go in the report under `### File-rename candidates` (e.g., `CardLookupService.cs` contains `ScryfallCardLookupService` as the public class — verify this is the historical "co-located impl with prefixed type name" pattern from CONVENTIONS.md and DO NOT propose renaming it; allowlist with rationale).

3. **Smells 1, 2, 3, 4 (production-code candidates)** — Run all four grep blocks. For each hit, perform a 30-second header inspection (read the class summary line + ctor parameter list) and decide:
   - Rename per D-02 loose trigger? → list under `### Production code` with old→new + 1-line rationale.
   - Leave as-is? → omit from report (no negative ledger needed).
   - Responsibility split too large for Phase 14? → list under `## Deferred (NOT executed; captured as future refactor candidates)`.

4. **Named candidates from REQUIREMENTS.md AUDIT-01** — apply RESEARCH.md verdicts:
   - **`ScryfallTaggerService`** — RESEARCH.md verdict template (section "ScryfallTaggerService"): the class does Scryfall REST + Tagger GraphQL + CSRF session lookup + flag-gating. Choose the planner pre-decision per CONTEXT.md "Claude's Discretion": **rename to `ScryfallTaggerLookupService`** with `<summary>` that enumerates the three responsibilities. Capture the alternative ("split into IScryfallTaggerLookup + ITaggerSessionGate") under `## Deferred` with explicit "out of Phase 14 scope per CONTEXT.md AUDIT-01 boundary; renames-only this phase, no responsibility splits".
   - **`CommanderSpellbookService`** — RESEARCH.md verdict (section "CommanderSpellbookService"): name is accurate, no rename. Document this in the report under `### Production code not renamed (manual review pass)` with one-line rationale.

5. **DeckPageTab enum allowlist** — RESEARCH.md "Phase 13 Landmines" item 3 + 13-VERIFICATION.md SC2 note: `DeckPageTab.cs` in `DeckFlow.Web/Models/` carries zero summaries by design (Phase 13 Pattern 7). Web.csproj `NoWarn 1591` still covers it (CONTEXT.md D-04 leaves Web.csproj NoWarn intact). Honor RESEARCH.md "Open questions for planner" #2 — make the explicit decision: **opt-in** the 5 one-line summaries (enum type + 4 values: `DeckSync`, `DeckAnalysis`, `DeckComparison`, `CedhMetaGap`, plus any additional values present at HEAD). Add to Plan 14-03's backfill list. Cite this decision in the audit report under `### Discretionary additions to Plan 14-03 backfill`.

6. Write the report using the template at RESEARCH.md "Audit report deliverable shape (Plan 14-01 → Plan 14-02 handoff)". Use one-line-per-rename format. Keep deferred items short and actionable.
  </action>
  <verify>
    <automated>test -f .planning/phases/14-broader-codebase-name-vs-behavior-audit/14-AUDIT-REPORT.md && grep -q "## Renames (executed in Plan 14-02)" .planning/phases/14-broader-codebase-name-vs-behavior-audit/14-AUDIT-REPORT.md && grep -q "Test doubles (D-05 canonicalization)" .planning/phases/14-broader-codebase-name-vs-behavior-audit/14-AUDIT-REPORT.md && grep -q "TestServiceFactory" .planning/phases/14-broader-codebase-name-vs-behavior-audit/14-AUDIT-REPORT.md && grep -qE "NullTempDataProvider|ConfigurableMetaGapService|CapturingDeckAnalysisPacketService|SuccessfulCardLookupService|DummyCommanderSearchService|FailingRecentDecksImporter" .planning/phases/14-broader-codebase-name-vs-behavior-audit/14-AUDIT-REPORT.md && grep -q "StubMetaGapService" .planning/phases/14-broader-codebase-name-vs-behavior-audit/14-AUDIT-REPORT.md && grep -qE "ScryfallTaggerService|ScryfallTaggerLookupService" .planning/phases/14-broader-codebase-name-vs-behavior-audit/14-AUDIT-REPORT.md && grep -q "CommanderSpellbookService" .planning/phases/14-broader-codebase-name-vs-behavior-audit/14-AUDIT-REPORT.md</automated>
  </verify>
  <acceptance_criteria>
    - 14-AUDIT-REPORT.md contains `## Renames (executed in Plan 14-02)` section with both `### Production code` and `### Test doubles (D-05 canonicalization)` subsections
    - **9** test-double renames enumerated (8 non-canonical hits + 1 preliminary collision-resolution rename for L810 `FakeMetaGapService` → `StubMetaGapService`) with file:line + old→new + canonical-taxonomy rationale
    - Plan 14-02 execution order constraint documented: L810 `FakeMetaGapService` → `StubMetaGapService` MUST run before L831 `ConfigurableMetaGapService` → `FakeMetaGapService`
    - TestServiceFactory explicitly allowlisted as NOT a rename target with one-line rationale
    - `### Name-collision notes` block documents BOTH collisions: (1) FakeCardLookupService at L914 (resolved via Stub prefix + "Successful" qualifier on rows e/f/g); (2) FakeMetaGapService at L810 (resolved via preliminary rename to StubMetaGapService — Option A per checker revision 1)
    - ScryfallTaggerService rename decision documented (rename to ScryfallTaggerLookupService) with deferred split note for future
    - CommanderSpellbookService leave-as-is decision documented with rationale
    - DeckPageTab opt-in decision documented under `### Discretionary additions to Plan 14-03 backfill`
    - `## Deferred (NOT executed; captured as future refactor candidates)` section present even if empty (signals scope discipline)
    - One-line-per-rename format throughout
    - Smell 5 file-vs-type-name allowlist documented (CONVENTIONS.md "co-located impl with prefixed type name" pattern)
    - No source-code files touched (only this .md)
  </acceptance_criteria>
  <done>Plan 14-02 executor can read 14-AUDIT-REPORT.md `### Production code` and `### Test doubles` sections and execute rename commits in order without re-deriving any names.</done>
</task>

<task type="auto">
  <name>Task 3: Compose 14-AUDIT-REPORT.md doc-comment backfill section + commit all three artifacts</name>
  <files>
    .planning/phases/14-broader-codebase-name-vs-behavior-audit/14-AUDIT-REPORT.md
    .planning/phases/14-broader-codebase-name-vs-behavior-audit/14-BASELINE.md
    .planning/phases/14-broader-codebase-name-vs-behavior-audit/14-BASELINE-PUBLIC-TYPES.txt
  </files>
  <read_first>
    - .planning/phases/14-broader-codebase-name-vs-behavior-audit/14-CONTEXT.md (D-03 scope: every public class + interface across 5 projects; D-06 internal-rename gets lockstep summary)
    - .planning/phases/14-broader-codebase-name-vs-behavior-audit/14-RESEARCH.md sections "State of the Project — Public type counts per project" + "Doc Backfill Mechanics (AUDIT-02 / Plan 14-03)" + "GenerateDocumentationFile Reality Check"
    - The 14-BASELINE-PUBLIC-TYPES.txt produced in Task 1 (the authoritative public-type grep)
    - CLAUDE.md (commit hygiene: plain author, no Co-Authored-By, one logical change per commit)
  </read_first>
  <action>
1. Build the `## Doc-comment backfill targets (executed in Plan 14-03)` section of 14-AUDIT-REPORT.md by computing the "needs summary" set per project:

   a. From 14-BASELINE-PUBLIC-TYPES.txt (full per-line list of all `public ... class|interface|record|abstract|static|partial` declarations across all 5 projects), derive the unique file list.

   b. For each file, check whether the type-level declaration is preceded by `/// <summary>` via:

          for f in $FILES; do
            type_line=$(grep -nE "^[[:space:]]*public +(sealed +)?(class|interface|record|abstract +class|static +class|partial +class) +[A-Z]" "$f" | head -1 | cut -d: -f1)
            if [ -n "$type_line" ]; then
              prev=$((type_line - 1))
              if ! sed -n "${prev}p" "$f" | grep -q '/// </summary>'; then
                echo "MISSING: $f (line $type_line)"
              fi
            fi
          done

      (Note: prev-line check is approximate — multi-line `<summary>` blocks end with `/// </summary>` on the line right before the declaration. Adjust window to 2-line lookback if the prev line is a `<remarks>` or attribute line. This is a discovery script; Plan 14-03 executor verifies precisely per file.)

   c. Group missing-summary hits by project. Expected (per RESEARCH.md):
      - DeckFlow.Core: ~9 files (records under `Models/`: DeckEntry, DeckDiff, LoadedDecks, PrintingConflict, plus 5 others surfaced by grep)
      - DeckFlow.Web: small set (Phase 13 backfilled the renamed types; remaining undoc'd v1.1-era types still gated by `NoWarn 1591` in Web.csproj — these are out of scope per CONTEXT.md "Deferred Ideas")
      - DeckFlow.CLI: 0 public types — empty
      - DeckFlow.Core.Tests: 10 files
      - DeckFlow.Web.Tests: ~37 files

   For DeckFlow.Web specifically, scope the backfill list to:
      - Any type renamed in Plan 14-02 (gets summary in lockstep — already covered, do NOT duplicate-list in Plan 14-03)
      - DeckPageTab + values (per Task 2 discretionary opt-in)
      - Otherwise: out of Plan 14-03 scope per CONTEXT.md "Deferred Ideas" (`NoWarn 1591` removal from Web.csproj is a future hygiene phase, not Phase 14)

2. Append the `## XML Coverage Diff (AUDIT-03 verification mechanism)` section to 14-AUDIT-REPORT.md. This OVERRIDES CONTEXT.md D-04's broken warning-gate. Cite RESEARCH.md "GenerateDocumentationFile Reality Check" Option A. Write the section to contain:
   - One-sentence finding: `.editorconfig` lines 93-96 set `dotnet_diagnostic.CS1591/CS1573/CS1587.severity = none` repo-wide (committed `0f38cce`, pre-Phase-14), so flipping `GenerateDocumentationFile=true` in 4 csprojs produces zero warnings even with deliberately-missing summaries.
   - The Option A 3-step coverage-diff script verbatim from RESEARCH.md (grep public types → grep documented types from `bin/Release/net10.0/*.xml` → comm -23 diff).
   - The Plan 14-04 pass criteria: `/tmp/missing-docs.txt` MUST be empty modulo the explicit allowlist (currently: DeckPageTab if Task 2 chose NOT to opt-in; here we opt-IN so the allowlist is empty for Phase 14).
   - Note: also keep the strict-equality warning-count check from D-09 as a necessary-but-not-sufficient gate. Two gates: warning count AND XML coverage diff.

3. Append the `## Plan 14-02 reference-propagation checklist` section using the "Always check" + "Sometimes check" lists from RESEARCH.md "File-Rename Propagation Map (Plan 14-02 Checklist)" verbatim. This is the canonical reference Plan 14-02 cites for every rename commit.

4. Append the `## Plan 14-02 per-commit `{ get; init; }` preservation gate` subsection. Verbatim from RESEARCH.md Pitfall 3:

       git diff --cached -- '*.cs' | grep -E "^\-.*\{ get; init; \}" | grep -v "^--"
       # If any output: ABORT the commit. The IDE/edit stripped init. Restore and redo.

   Note: this gate is also required in Plan 14-03 (which touches every `{ get; init; }` record under `DeckFlow.Core/Models/`).

5. Validate completeness with a final pass: confirm 14-AUDIT-REPORT.md sections list — in order — `## Renames`, `## Allowlist`, `### Discretionary additions to Plan 14-03 backfill`, `## Doc-comment backfill targets`, `## XML Coverage Diff`, `## Plan 14-02 reference-propagation checklist`, `## Plan 14-02 per-commit init preservation gate`, `## Deferred`.

6. Commit all three artifacts in a single logical commit (CLAUDE.md hygiene):

       git add .planning/phases/14-broader-codebase-name-vs-behavior-audit/14-BASELINE.md \
               .planning/phases/14-broader-codebase-name-vs-behavior-audit/14-BASELINE-PUBLIC-TYPES.txt \
               .planning/phases/14-broader-codebase-name-vs-behavior-audit/14-AUDIT-REPORT.md
       git commit -m "docs(14-01): capture baseline + audit report"

   No Co-Authored-By trailer. Plain default author per CLAUDE.md.
  </action>
  <verify>
    <automated>grep -q "## Doc-comment backfill targets" .planning/phases/14-broader-codebase-name-vs-behavior-audit/14-AUDIT-REPORT.md && grep -q "## XML Coverage Diff" .planning/phases/14-broader-codebase-name-vs-behavior-audit/14-AUDIT-REPORT.md && grep -q "Plan 14-02 reference-propagation checklist" .planning/phases/14-broader-codebase-name-vs-behavior-audit/14-AUDIT-REPORT.md && grep -q "get; init;" .planning/phases/14-broader-codebase-name-vs-behavior-audit/14-AUDIT-REPORT.md && git log -1 --format='%s' | grep -q "docs(14-01):" && git log -1 --format='%b' | grep -ic "Co-Authored-By" | grep -q "^0$"</automated>
  </verify>
  <acceptance_criteria>
    - 14-AUDIT-REPORT.md `## Doc-comment backfill targets` section enumerates files per project
    - `## XML Coverage Diff` section codifies Option A 3-step script and identifies it as the AUDIT-03 verification (overriding D-04's broken warning-gate assumption)
    - `## Plan 14-02 reference-propagation checklist` lists all "Always check" items from RESEARCH.md
    - `## Plan 14-02 per-commit { get; init; } preservation gate` subsection present with the grep command
    - `## Deferred (NOT executed; captured as future refactor candidates)` section present
    - Single git commit `docs(14-01): capture baseline + audit report` lands all three artifacts
    - Commit author is plain (no Co-Authored-By trailer): `git log -1 --format='%b' | grep -c 'Co-Authored-By'` returns 0
    - All three files use LF line endings
    - No source code (*.cs, *.cshtml, *.csproj) modified in this plan
  </acceptance_criteria>
  <done>Plans 14-02, 14-03, and 14-04 can begin against the committed audit report + baseline.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none introduced) | This plan emits only markdown deliverables; no code, no public API surface, no auth/authz, no data flow change. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-01-DOC | Information disclosure | 14-AUDIT-REPORT.md content | accept | Public repo per CLAUDE.md; the audit report enumerates internal class names already visible in source. No secrets, no env vars, no credentials. |
| T-14-01-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan; only markdown writes and git commit. |

Per RESEARCH.md "Security Domain" — Phase 14 changes no auth/session/access-control/input-validation/output-encoding/data/error-handling/integration code. Phase 14 is a rename + doc phase; the D-10 preservation list (CONTEXT.md) explicitly prohibits any public API key, route, DTO property name, or wire-format change. No threats introduced.
</threat_model>

<verification>
Plan 14-01 completion checks:

1. Both artifacts exist and are committed:

       test -f .planning/phases/14-broader-codebase-name-vs-behavior-audit/14-BASELINE.md
       test -f .planning/phases/14-broader-codebase-name-vs-behavior-audit/14-AUDIT-REPORT.md
       git log -1 --format='%s' | grep -q '^docs(14-01):'

2. Audit report has all required sections (Task 3 acceptance criteria).

3. No source code modified:

       git diff HEAD~1 --name-only | grep -vE '^\.planning/phases/14-broader-codebase-name-vs-behavior-audit/' || true
       # Expected: empty (only files under the phase dir changed)

4. LF line endings preserved on the new markdown files (per `.gitattributes`):

       file .planning/phases/14-broader-codebase-name-vs-behavior-audit/14-BASELINE.md
       file .planning/phases/14-broader-codebase-name-vs-behavior-audit/14-AUDIT-REPORT.md
       # Expected: no "CRLF line terminators" in output
</verification>

<success_criteria>
- 14-BASELINE.md captures D-09 baseline (warning count, public type counts, test-double census, HEAD SHA, test-discovery baseline or timeout marker) verifiable by re-running the documented commands against the same HEAD.
- 14-AUDIT-REPORT.md is the single source of truth for Plan 14-02 (renames) and Plan 14-03 (doc backfill) — both downstream plans cite specific sections in their `<read_first>` blocks.
- 14-AUDIT-REPORT.md codifies the XML Coverage Diff as the AUDIT-03 verification mechanism, overriding CONTEXT.md D-04's broken warning-gate assumption (and citing `.editorconfig` lines 93-96 as the reason).
- Single commit `docs(14-01): capture baseline + audit report` with plain author, no Co-Authored-By trailer, no source-code changes.
- AUDIT-01 advanced (discovery complete); AUDIT-02 advanced (backfill set enumerated); AUDIT-03 advanced (verification mechanism finalized).
</success_criteria>

<output>
Create `.planning/phases/14-broader-codebase-name-vs-behavior-audit/14-01-SUMMARY.md` documenting:
- Baseline numbers captured (warning count, type counts per project, test-double census)
- Final rename count from 14-AUDIT-REPORT.md `### Production code` + `### Test doubles`
- Final doc-backfill count from 14-AUDIT-REPORT.md `## Doc-comment backfill targets`
- Decision log: ScryfallTaggerService rename name choice, DeckPageTab opt-in/opt-out, TestServiceFactory allowlist
- Pointer to 14-AUDIT-REPORT.md as Plan 14-02 and Plan 14-03 input
</output>
</content>
</invoke>