---
phase: 01-visual-system-tokens
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - DeckFlow.Web/wwwroot/css/site.css
  - DeckFlow.Web/wwwroot/css/site-common.css
autonomous: true
requirements:
  - UI-VS-01
must_haves:
  truths:
    - "site.css :root declares the 6-step type scale --fs-xs/sm/base/lg/xl/2xl"
    - "Every literal font-size in site.css references var(--fs-*) or an em/% derivative"
    - "Every literal font-size in site-common.css references var(--fs-*) or an em/% derivative"
    - "Classic theme renders identically post-migration on home, /feedback, /help, /about, DeckSync (visual smoke check)"
  artifacts:
    - path: "DeckFlow.Web/wwwroot/css/site.css"
      provides: "6-step type-scale token block in :root and var(--fs-*) consumers"
      contains: "--fs-xs"
    - path: "DeckFlow.Web/wwwroot/css/site-common.css"
      provides: "All font-size declarations migrated to var(--fs-*) or em/%"
  key_links:
    - from: "DeckFlow.Web/wwwroot/css/site.css :root"
      to: "site.css body / .hero h1 / .lede / etc."
      via: "var(--fs-*) substitution"
      pattern: "font-size:\\s*var\\(--fs-"
    - from: "DeckFlow.Web/wwwroot/css/site.css :root"
      to: "site-common.css consumers"
      via: "CSS cascade — site-common.css loads after site.css; tokens inherit"
      pattern: "font-size:\\s*var\\(--fs-"
---

<objective>
Establish a 6-step semantic type scale on `site.css :root` and migrate every literal `font-size` declaration in `site.css` and `site-common.css` to the new tokens (or to `em` / `%` derivatives where the value is intentionally relative).

Purpose: Eliminate the 18-value font-size sprawl that drove Typography 2/4 in the UI audit. After this plan, type-size decisions are made at the token layer, not scattered across hundreds of declarations.

Output: `site.css :root` exposes `--fs-xs`, `--fs-sm`, `--fs-base`, `--fs-lg`, `--fs-xl`, `--fs-2xl`. Both `site.css` and `site-common.css` reference those tokens for every absolute font-size. Classic theme renders identically on the live site.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@CLAUDE.md
@DeckFlow.Web/wwwroot/css/site.css
@DeckFlow.Web/wwwroot/css/site-common.css

<interfaces>
<!-- Existing site.css :root tokens (lines 1-32) — preserved, NOT replaced -->
<!-- New tokens added to the SAME :root block; do not create a second :root -->

Existing tokens in site.css :root:
  --bg, --panel, --ink, --muted, --line
  --accent, --accent-strong (KEEP — backward compat per STATE.md)
  --theme-secondary, --theme-secondary-soft, --theme-secondary-bg
  --panel-soft-bg, --theme-surface
  --warning, --error, --info
  --panel-inferred-* / --panel-live-* / --panel-edhrec-* / --panel-retry-* / --panel-summary-* / --panel-fallback-*

New tokens to add in this plan:
  --fs-xs:   0.75rem
  --fs-sm:   0.85rem
  --fs-base: 0.95rem
  --fs-lg:   1.05rem
  --fs-xl:   1.5rem
  --fs-2xl:  1.9rem

Distinct font-size LITERAL values currently in site.css (24 occurrences, 12 distinct):
  15px (one occurrence on `html { font-size: 15px; }` — KEEP AS-IS, this is the rem base)
  0.65rem, 0.75rem, 0.8rem, 0.85rem, 0.9rem, 0.95rem, 1.05rem, 1.5rem, 1.9rem
  0.75em, 0.85em (relative — KEEP AS em)

Distinct font-size LITERAL values currently in site-common.css (21 occurrences, 11 distinct):
  0.68rem, 0.75em, 0.78rem, 0.8rem, 0.85rem, 0.875rem, 0.9rem, 0.95em, 0.95rem, 1rem, 1.4rem
  (em values KEEP AS em; rem values map to nearest token)

Mapping table (rem literal -> token):
  0.65rem  -> var(--fs-xs)         (0.75rem) — SLIGHT bump up; only one site.css use; verify visual parity
  0.68rem  -> var(--fs-xs)         (site-common.css; same — verify)
  0.75rem  -> var(--fs-xs)
  0.78rem  -> var(--fs-sm)         (round up to 0.85rem; only one use; verify)
  0.8rem   -> var(--fs-sm)         (round to 0.85rem; verify visual parity at site.css:318,339,927 + site-common.css uses)
  0.85rem  -> var(--fs-sm)
  0.875rem -> var(--fs-sm)         (round down to 0.85rem; appears in site-common.css:590 .feedback-error)
  0.9rem   -> var(--fs-base)       (round to 0.95rem; verify visual parity)
  0.95rem  -> var(--fs-base)
  1rem     -> var(--fs-base)       (round down to 0.95rem; site-common.css; verify)
  1.05rem  -> var(--fs-lg)
  1.4rem   -> var(--fs-xl)         (round to 1.5rem; site-common.css; verify)
  1.5rem   -> var(--fs-xl)
  1.9rem   -> var(--fs-2xl)
  em values -> KEEP AS-IS (intentionally relative)

NOTE on rounding: the executor MUST flag any rounding the user has not pre-approved. If a literal does not map cleanly to one of the 6 tokens, the executor stops and asks rather than silently shifting visual weight. Acceptable canonical mappings are listed above; everything else is an open question.

`html { font-size: 15px; }` at site.css:39 is the rem base — DO NOT touch. Tokens express rem multiples; the 15px base preserves classic theme parity.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add 6-step type-scale tokens to site.css :root</name>
  <files>DeckFlow.Web/wwwroot/css/site.css</files>
  <read_first>
    - DeckFlow.Web/wwwroot/css/site.css (read lines 1-40 to see current :root and html { font-size: 15px } base)
    - CLAUDE.md (theme-system constraint at top: token additions go in :root of each theme file)
    - .planning/ROADMAP.md (Phase 1 success criterion #1)
  </read_first>
  <action>
    Add a `/* type scale (UI-VS-01) */` comment-grouped block of 6 tokens to the EXISTING `:root` block at the top of `DeckFlow.Web/wwwroot/css/site.css` (before the closing `}` on line 32). Insert AFTER the existing `--info: #e4eef8;` line and BEFORE the existing `/* semantic panel tokens` comment on line 19, OR at the very end of `:root` before the closing `}` — pick whichever placement keeps related tokens grouped. Do NOT create a second `:root` block.

    Exact tokens to insert:
    ```css
      /* type scale (UI-VS-01) */
      --fs-xs:   0.75rem;
      --fs-sm:   0.85rem;
      --fs-base: 0.95rem;
      --fs-lg:   1.05rem;
      --fs-xl:   1.5rem;
      --fs-2xl:  1.9rem;
    ```

    Do NOT add any layout rules in this task. Do NOT touch `html { font-size: 15px; }` at line 39 — that is the rem base for classic theme parity and MUST stay.

    Do NOT add fallback values like `var(--fs-base, 0.95rem)` in this task — direct token reference only. Themes inherit via the cascade.
  </action>
  <verify>
    <automated>
      cd /mnt/c/users/chrislunt/source/personal/decksyncworkbench &amp;&amp; \
      grep -E '^\s*--fs-(xs|sm|base|lg|xl|2xl):' DeckFlow.Web/wwwroot/css/site.css | wc -l
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -E '^\s*--fs-(xs|sm|base|lg|xl|2xl):' DeckFlow.Web/wwwroot/css/site.css | wc -l` returns exactly `6`
    - `grep -c '^:root' DeckFlow.Web/wwwroot/css/site.css` returns exactly `1` (no duplicate :root)
    - `grep -E '^\s*html\s*\{' -A1 DeckFlow.Web/wwwroot/css/site.css | grep -c 'font-size: 15px'` returns `1` (rem base preserved)
    - `dotnet build DeckFlow.Web/DeckFlow.Web.csproj --no-restore` exits 0
  </acceptance_criteria>
  <done>The 6 type-scale tokens exist in site.css :root, the rem base is untouched, the build is clean.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Replace font-size literals in site.css with var(--fs-*) references</name>
  <files>DeckFlow.Web/wwwroot/css/site.css</files>
  <read_first>
    - DeckFlow.Web/wwwroot/css/site.css (full file — 1340 lines)
    - .planning/phases/01-visual-system-tokens/01-01-PLAN.md (this plan, the &lt;interfaces&gt; mapping table)
  </read_first>
  <action>
    Replace every literal `font-size: <rem>` value in `DeckFlow.Web/wwwroot/css/site.css` with the matching `var(--fs-*)` token, per the mapping table in `<interfaces>`.

    Specific replacements (line numbers from current site.css):
      Line 110:  `font-size: 1.9rem;`  -> `font-size: var(--fs-2xl);`
      Line 152:  `font-size: 0.85rem;` -> `font-size: var(--fs-sm);`
      Line 222:  `font-size: 0.75rem;` -> `font-size: var(--fs-xs);`
      Line 318:  `font-size: 0.8rem;`  -> `font-size: var(--fs-sm);`   (visual: 0.8 -> 0.85; review)
      Line 339:  `font-size: 0.8rem;`  -> `font-size: var(--fs-sm);`
      Line 511:  `font-size: 0.65rem;` -> `font-size: var(--fs-xs);`   (visual: 0.65 -> 0.75; review — flag if visually material)
      Line 590:  `font-size: 0.95rem;` -> `font-size: var(--fs-base);`
      Line 646:  `font-size: 0.85rem;` -> `font-size: var(--fs-sm);`
      Line 726:  `font-size: 0.9rem;`  -> `font-size: var(--fs-base);` (visual: 0.9 -> 0.95)
      Line 733:  `font-size: 0.75em;`  -> KEEP (em value)
      Line 775:  `font-size: 0.9rem;`  -> `font-size: var(--fs-base);`
      Line 893:  `font-size: 1.05rem;` -> `font-size: var(--fs-lg);`
      Line 900:  `font-size: 0.75rem;` -> `font-size: var(--fs-xs);`
      Line 910:  `font-size: 0.85rem;` -> `font-size: var(--fs-sm);`
      Line 927:  `font-size: 0.8rem;`  -> `font-size: var(--fs-sm);`
      Line 994:  `font-size: 0.85rem;` -> `font-size: var(--fs-sm);`
      Line 1134: `font-size: 1.5rem;`  -> `font-size: var(--fs-xl);`
      Line 1142: `font-size: 0.9rem;`  -> `font-size: var(--fs-base);`
      Line 1194: `font-size: 0.95rem;` -> `font-size: var(--fs-base);`
      Line 1249: `font-size: 0.85em;`  -> KEEP (em value)
      Line 1255: `font-size: 0.95rem;` -> `font-size: var(--fs-base);`
      Line 1285: `font-size: 0.9rem;`  -> `font-size: var(--fs-base);`
      Line 1303: `font-size: 0.95rem;` -> `font-size: var(--fs-base);`

    DO NOT touch:
      - Line 39: `html { font-size: 15px; }` — rem base; stays literal
      - `0.75em` and `0.85em` declarations — em is intentionally relative

    For the two flagged values (`0.65rem` -> `--fs-xs` 0.75rem at line 511; `0.8rem` family rounding to 0.85rem) the executor SHOULD apply the canonical mapping, then flag the change in the plan SUMMARY's "Risks/Visual Drift" section so the user can spot-check at smoke-test time.
  </action>
  <verify>
    <automated>
      cd /mnt/c/users/chrislunt/source/personal/decksyncworkbench &amp;&amp; \
      bash -c '
        rem_literals=$(grep -E "font-size:\s*[0-9.]+rem" DeckFlow.Web/wwwroot/css/site.css | wc -l)
        token_refs=$(grep -E "font-size:\s*var\(--fs-" DeckFlow.Web/wwwroot/css/site.css | wc -l)
        rem_base=$(grep -cE "^\s*font-size:\s*15px;" DeckFlow.Web/wwwroot/css/site.css)
        echo "rem_literals=$rem_literals token_refs=$token_refs rem_base=$rem_base"
        [ "$rem_literals" -eq 0 ] &amp;&amp; [ "$token_refs" -ge 21 ] &amp;&amp; [ "$rem_base" -eq 1 ]
      '
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE "font-size:\s*[0-9.]+rem" DeckFlow.Web/wwwroot/css/site.css` returns `0` (no rem literals remain)
    - `grep -cE "font-size:\s*var\(--fs-" DeckFlow.Web/wwwroot/css/site.css` returns >= `21` (all rem-based literals replaced; 24 total minus 1 px base minus 2 em = 21)
    - `grep -cE "^\s*font-size:\s*15px;" DeckFlow.Web/wwwroot/css/site.css` returns `1` (html base preserved)
    - `grep -cE "font-size:\s*[0-9.]+em" DeckFlow.Web/wwwroot/css/site.css` returns `2` (the two em values preserved)
    - `dotnet build DeckFlow.Web/DeckFlow.Web.csproj --no-restore` exits 0
  </acceptance_criteria>
  <done>site.css contains zero font-size rem literals (only `15px` base + 2 em values + N var(--fs-*) refs); build is clean.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Replace font-size literals in site-common.css with var(--fs-*) references</name>
  <files>DeckFlow.Web/wwwroot/css/site-common.css</files>
  <read_first>
    - DeckFlow.Web/wwwroot/css/site-common.css (full file — 1004 lines)
    - DeckFlow.Web/wwwroot/css/site.css (just the :root block, lines 1-40, to confirm token names exist)
    - .planning/phases/01-visual-system-tokens/01-01-PLAN.md (this plan, the &lt;interfaces&gt; mapping table)
  </read_first>
  <action>
    Replace every literal `font-size: <rem>` declaration in `DeckFlow.Web/wwwroot/css/site-common.css` with the matching `var(--fs-*)` token using the mapping table from `<interfaces>`.

    Distinct rem literals to replace (find each via `grep -nE 'font-size:\s*[0-9.]+rem' site-common.css` — current count: 19 declarations, 9 distinct rem values):
      0.68rem  -> var(--fs-xs)     (round up 0.68 -> 0.75; flag in SUMMARY)
      0.78rem  -> var(--fs-sm)     (round up 0.78 -> 0.85; flag in SUMMARY)
      0.8rem   -> var(--fs-sm)
      0.85rem  -> var(--fs-sm)
      0.875rem -> var(--fs-sm)     (round 0.875 -> 0.85; visible on .feedback-error line 590; flag)
      0.9rem   -> var(--fs-base)
      0.95rem  -> var(--fs-base)
      1rem     -> var(--fs-base)   (round 1 -> 0.95; flag — visible drift on body copy)
      1.4rem   -> var(--fs-xl)     (round 1.4 -> 1.5; flag)

    DO NOT touch em values (`0.75em`, `0.95em`).

    DO NOT add layout rules. DO NOT introduce new selectors. This is a pure value substitution pass.

    Executor MUST run `grep -nE 'font-size:\s*[0-9.]+rem' site-common.css` BEFORE editing to enumerate every declaration with line number, edit each, then re-grep to confirm zero rem literals remain. Any rem literal that doesn't match the table -> stop and ask the user.
  </action>
  <verify>
    <automated>
      cd /mnt/c/users/chrislunt/source/personal/decksyncworkbench &amp;&amp; \
      bash -c '
        rem_literals=$(grep -cE "font-size:\s*[0-9.]+rem" DeckFlow.Web/wwwroot/css/site-common.css)
        token_refs=$(grep -cE "font-size:\s*var\(--fs-" DeckFlow.Web/wwwroot/css/site-common.css)
        em_preserved=$(grep -cE "font-size:\s*[0-9.]+em" DeckFlow.Web/wwwroot/css/site-common.css)
        echo "rem_literals=$rem_literals token_refs=$token_refs em_preserved=$em_preserved"
        [ "$rem_literals" -eq 0 ] &amp;&amp; [ "$token_refs" -ge 19 ]
      '
      cd /mnt/c/users/chrislunt/source/personal/decksyncworkbench &amp;&amp; dotnet build DeckFlow.Web/DeckFlow.Web.csproj --no-restore
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE "font-size:\s*[0-9.]+rem" DeckFlow.Web/wwwroot/css/site-common.css` returns `0`
    - `grep -cE "font-size:\s*var\(--fs-" DeckFlow.Web/wwwroot/css/site-common.css` returns >= `19` (rem-based replacements; 19 total rem declarations)
    - `grep -cE "font-size:\s*[0-9.]+em" DeckFlow.Web/wwwroot/css/site-common.css` returns >= `2` (em values preserved)
    - `dotnet build DeckFlow.Web/DeckFlow.Web.csproj --no-restore` exits 0
    - SUMMARY notes any rounding flags (e.g. "0.875rem -> 0.85rem on .feedback-error") for manual smoke-check
    - `grep -q "^## Rounding Flags" .planning/phases/01-visual-system-tokens/01-01-SUMMARY.md` exits 0 (SUMMARY structure enforced — section header present even if list is empty)
  </acceptance_criteria>
  <done>site-common.css contains zero font-size rem literals; only em values + var(--fs-*) refs remain; build clean.</done>
</task>

</tasks>

<verification>
After all 3 tasks:

1. `dotnet build DeckFlow.Web/DeckFlow.Web.csproj --no-restore` exits 0.
2. `grep -cE "font-size:\s*[0-9.]+rem" DeckFlow.Web/wwwroot/css/site.css` returns 0.
3. `grep -cE "font-size:\s*[0-9.]+rem" DeckFlow.Web/wwwroot/css/site-common.css` returns 0.
4. `grep -cE "^\s*font-size:\s*15px;" DeckFlow.Web/wwwroot/css/site.css` returns 1 (rem base preserved).
5. `grep -E '^\s*--fs-(xs|sm|base|lg|xl|2xl):' DeckFlow.Web/wwwroot/css/site.css | wc -l` returns 6.

Manual smoke check (executor logs in SUMMARY, user runs at end of phase, NOT this plan):
  - Load classic theme on /, /feedback, /help, /about, /sync — confirm visual parity vs production.
  - Pay attention to lines flagged with rounding deltas (0.875 -> 0.85, 1rem -> 0.95rem, 1.4rem -> 1.5rem, etc.).
</verification>

<success_criteria>
- 6-step type-scale tokens declared in `site.css :root`
- Zero font-size rem literals remain in `site.css` or `site-common.css` (only `html { font-size: 15px; }` base + intentional em values)
- All previous rem literals are reachable as `var(--fs-*)` references
- `dotnet build DeckFlow.Web` clean
- ROADMAP success criterion #1 satisfied (UI-VS-01 complete)
</success_criteria>

<output>
After completion, create `.planning/phases/01-visual-system-tokens/01-01-SUMMARY.md` covering:
  - Token block exact location in site.css :root
  - Count of literals replaced per file
  - Any rounding flags (literal -> token deltas) for the user's smoke-check
  - Build status
  - Open questions / visual drift risks
</output>
</content>
</invoke>