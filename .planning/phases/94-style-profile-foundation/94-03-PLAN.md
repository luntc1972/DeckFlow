---
phase: 94-style-profile-foundation
plan: 03
type: execute
wave: 3
depends_on: [94-02]
files_modified:
  - DeckFlow.Core.Tests/DeckFlow.Core.Tests.csproj
  - DeckFlow.Core.Tests/Integration/PostgresFactAttribute.cs
  - DeckFlow.Core.Tests/Integration/PostgresContainerFixture.cs
  - DeckFlow.Core.Tests/CreatorStyleProfileStoreTests.cs
  - DeckFlow.Core.Tests/Integration/CreatorStyleProfileStorePostgresTests.cs
autonomous: true
requirements: [CS-04, CS-03]
tags: [creator-style, tests, round-trip, postgres, sqlite]

must_haves:
  truths:
    - "SQLite round-trip tests prove write-then-read fidelity of the full profile shape and run unconditionally"
    - "A below-floor profile's insufficient_sample=true survives write→read (CS-03)"
    - "measured-only, stated-only, and fully-fused partial profiles each round-trip with empty-not-null section reads (D-07)"
    - "Postgres round-trip tests exist, gated on DECKFLOW_POSTGRES_TESTS=1 + Docker, and prove the same fidelity on the Postgres dialect"
  artifacts:
    - path: "DeckFlow.Core.Tests/CreatorStyleProfileStoreTests.cs"
      provides: "unconditional SQLite round-trip tests (schema idempotency, full-shape, insufficient_sample, partial-profile, unknown-slug null, re-upsert overwrite)"
      contains: "CreatorStyleProfileStore"
    - path: "DeckFlow.Core.Tests/Integration/PostgresContainerFixture.cs"
      provides: "Testcontainers Postgres fixture (mirror of DeckFlow.Web.Tests) local to Core.Tests"
      contains: "PostgreSqlBuilder"
    - path: "DeckFlow.Core.Tests/Integration/PostgresFactAttribute.cs"
      provides: "DECKFLOW_POSTGRES_TESTS=1 gating FactAttribute"
      contains: "DECKFLOW_POSTGRES_TESTS"
    - path: "DeckFlow.Core.Tests/Integration/CreatorStyleProfileStorePostgresTests.cs"
      provides: "gated Postgres round-trip tests using the fixture"
      contains: "PostgresFact"
  key_links:
    - from: "CreatorStyleProfileStoreTests"
      to: "CreatorStyleProfileStore"
      via: "temp-file SQLite ctor"
      pattern: "new CreatorStyleProfileStore\\("
    - from: "CreatorStyleProfileStorePostgresTests"
      to: "PostgresContainerFixture"
      via: "IClassFixture + GetConnectionStringOrSkipAsync"
      pattern: "GetConnectionStringOrSkipAsync"
    - from: "DeckFlow.Core.Tests.csproj"
      to: "Testcontainers.PostgreSql"
      via: "PackageReference (in-solution, already used by DeckFlow.Web.Tests)"
      pattern: "Testcontainers.PostgreSql"
---

<objective>
Prove CS-04: xUnit round-trip fidelity of the full CreatorStyleProfile shape on BOTH dialects. SQLite tests
run unconditionally; Postgres tests run against a Testcontainers container gated on DECKFLOW_POSTGRES_TESTS=1
(Cycle 16 SYNC-16 pattern). Includes the CS-03 assertion that a below-floor profile's insufficient_sample=true
survives write→read, and D-07 partial-profile coverage (measured-only / stated-only / fully-fused).

Purpose: Locks the substrate before any downstream phase builds on it.
Output: csproj package add, project-local Postgres infra (fixture + gating attribute mirrored from
DeckFlow.Web.Tests), SQLite test class, gated Postgres test class.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/94-style-profile-foundation/94-CONTEXT.md
@.planning/phases/94-style-profile-foundation/94-PATTERNS.md
@DeckFlow.Core.Tests/ContentSiteIndexStoreTests.cs
@DeckFlow.Web.Tests/Integration/PostgresContainerFixture.cs
@DeckFlow.Web.Tests/Integration/PostgresFactAttribute.cs

<interfaces>
<!-- Contracts under test. Extracted — use directly. -->

Store under test (Plan 94-02, namespace DeckFlow.Core.Content):
  new CreatorStyleProfileStore(string databasePath)                       // SQLite temp-file
  new CreatorStyleProfileStore(RelationalDatabaseConnection, bool ensureSchemaEnabled = true)  // Postgres
  Task EnsureSchemaAsync(CancellationToken = default)
  Task UpsertAsync(CreatorStyleProfile profile, CancellationToken = default)
  Task<CreatorStyleProfile?> GetBySlugAsync(string slug, CancellationToken = default)

Records (Plan 94-01, namespace DeckFlow.Core.Knowledge):
  CreatorStyleProfile { Slug, Platform, MinDecks, InsufficientSample, StatedRules, MeasuredMetrics, FusedTargets, UpdatedUtc; const int MinDeckFloor = 5 }
  StatedRule { Category, TargetMetric, TargetValue, Comparator, SourceClip, Confidence }
  MeasuredMetric { Metric, Value, NumDecks, Distribution(MetricDistribution?) }
  FusedTarget { Metric, Value, Weight, Source, Conflict(FusedConflict?) }
  MetricDistribution { Mean, Min, Max, StdDev }   FusedConflict { StatedValue, MeasuredValue, Delta }

Postgres connection (DeckFlow.Core/Storage):
  new RelationalDatabaseConnection(RelationalDatabaseProvider.Postgres, connectionString)
  // NOTE: PostgresContainerFixture.GetConnectionString() from Testcontainers may need SSL Mode=Disable —
  // see observation 16276 (Studio Postgres SSL handshake). If the container connection fails on SSL, append
  // ";SSL Mode=Disable" (or "Ssl Mode=Disable") to the connection string before constructing the descriptor.
</interfaces>

<locked_overrides>
Postgres round-trip tests live IN DeckFlow.Core.Tests (not DeckFlow.Web.Tests). Add the
`Testcontainers.PostgreSql` PackageReference (version 3.10.0, matching DeckFlow.Web.Tests — in-solution,
so NO new-package approval needed per the house "in-solution packages OK" rule) and RE-CREATE the
PostgresFactAttribute + PostgresContainerFixture pair inside DeckFlow.Core.Tests (they are project-local,
non-shared classes; DeckFlow.Web.Tests does not reference DeckFlow.Core.Tests so they cannot be reused
cross-project). SQLite round-trip tests run unconditionally.
</locked_overrides>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add Testcontainers.PostgreSql + project-local Postgres infra to Core.Tests</name>
  <files>DeckFlow.Core.Tests/DeckFlow.Core.Tests.csproj, DeckFlow.Core.Tests/Integration/PostgresFactAttribute.cs, DeckFlow.Core.Tests/Integration/PostgresContainerFixture.cs</files>
  <read_first>
    - DeckFlow.Web.Tests/Integration/PostgresFactAttribute.cs (full file to mirror — 21 lines)
    - DeckFlow.Web.Tests/Integration/PostgresContainerFixture.cs (full file to mirror — 95 lines, lazy semaphore-gated start, GetConnectionStringOrSkipAsync)
    - DeckFlow.Core.Tests/DeckFlow.Core.Tests.csproj (current package list — add Testcontainers.PostgreSql alongside xunit)
    - DeckFlow.Web.Tests/DeckFlow.Web.Tests.csproj (confirm the exact Testcontainers.PostgreSql version = 3.10.0)
  </read_first>
  <action>
    1. In `DeckFlow.Core.Tests/DeckFlow.Core.Tests.csproj`, add
       `<PackageReference Include="Testcontainers.PostgreSql" Version="3.10.0" />` to the existing package
       `<ItemGroup>` (match the version DeckFlow.Web.Tests uses). Touch ONLY that line — preserve all other
       lines and the file's existing line endings. Do NOT modify the lockfile beyond what restore does.
    2. Create `DeckFlow.Core.Tests/Integration/PostgresFactAttribute.cs` in namespace
       `DeckFlow.Core.Tests.Integration` — a byte-for-byte behavioral copy of the DeckFlow.Web.Tests version
       (sealed `PostgresFactAttribute : FactAttribute`; sets `Skip` unless env `DECKFLOW_POSTGRES_TESTS == "1"`),
       adjusting only the namespace.
    3. Create `DeckFlow.Core.Tests/Integration/PostgresContainerFixture.cs` in namespace
       `DeckFlow.Core.Tests.Integration` — mirror the DeckFlow.Web.Tests fixture verbatim (IAsyncLifetime,
       SemaphoreSlim-gated lazy `EnsureStartedAsync`, `PostgreSqlBuilder().WithImage("postgres:16-alpine")
       .WithDatabase("deckflow_tests").WithUsername("deckflow").WithPassword("deckflow")`,
       `GetConnectionStringOrSkipAsync()` throwing `SkipException.ForSkip(...)` when Docker/flag unavailable),
       adjusting only the namespace.
    Use LF line endings on the two new files.
  </action>
  <verify>
    <automated>dotnet build DeckFlow.Core.Tests -c Debug (0 errors; Testcontainers.PostgreSql restores)</automated>
  </verify>
  <acceptance_criteria>
    - DeckFlow.Core.Tests.csproj contains `<PackageReference Include="Testcontainers.PostgreSql" Version="3.10.0" />`.
    - PostgresFactAttribute.cs exists in namespace DeckFlow.Core.Tests.Integration and sets Skip unless DECKFLOW_POSTGRES_TESTS=1.
    - PostgresContainerFixture.cs exists in the same namespace with PostgreSqlBuilder + GetConnectionStringOrSkipAsync + SkipException path.
    - `dotnet build DeckFlow.Core.Tests` succeeds with 0 errors.
  </acceptance_criteria>
  <done>Core.Tests can spin up a gated Postgres container; the package and infra compile.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: SQLite round-trip tests (unconditional) — full shape, insufficient_sample, partial profiles</name>
  <files>DeckFlow.Core.Tests/CreatorStyleProfileStoreTests.cs</files>
  <read_first>
    - DeckFlow.Core.Tests/ContentSiteIndexStoreTests.cs (temp-file SQLite ctor + IDisposable cleanup with SqliteConnection.ClearAllPools()/GC before File.Delete; [Fact] round-trip shape)
    - DeckFlow.Core/Content/CreatorStyleProfileStore.cs (store under test — Plan 94-02)
    - DeckFlow.Core/Knowledge/CreatorStyleProfile.cs (records + MinDeckFloor — Plan 94-01)
    - .planning/phases/94-style-profile-foundation/94-CONTEXT.md (D-06 flag survives, D-07 partial-profile cases)
  </read_first>
  <behavior>
    - EnsureSchemaAsync is idempotent (called twice, no throw).
    - GetBySlugAsync returns null for an unknown slug.
    - A fully-populated profile (all three sections non-empty, nested Distribution + Conflict set) round-trips: every scalar equal, every section list equal element-by-element, nested objects equal.
    - A below-floor profile (MinDecks < CreatorStyleProfile.MinDeckFloor, InsufficientSample = true) reads back with InsufficientSample still true.
    - A measured-only profile (StatedRules empty, FusedTargets empty) reads back with those two sections as empty (Count==0, non-null) and MeasuredMetrics populated.
    - A stated-only profile reads back symmetrically (StatedRules populated, other two empty-not-null).
    - Re-upserting the same slug with changed data updates in place — a second GetBySlugAsync returns the new values, and the row count for that slug stays 1 (assert via a distinct field change, e.g. Platform or MinDecks).
  </behavior>
  <action>
    Create `DeckFlow.Core.Tests/CreatorStyleProfileStoreTests.cs` in namespace `DeckFlow.Core.Tests`,
    `using DeckFlow.Core.Content; using DeckFlow.Core.Knowledge; using Microsoft.Data.Sqlite; using Xunit;`
    plus `System.IO`. Mirror `ContentSiteIndexStoreTests`: `public sealed class CreatorStyleProfileStoreTests : IDisposable`
    with a temp `_dbPath = Path.Combine(Path.GetTempPath(), $"creator-style-profile-test-{Guid.NewGuid():N}.db")`
    and `_store = new CreatorStyleProfileStore(_dbPath)`; `Dispose()` copies the ClearAllPools + GC.Collect +
    GC.WaitForPendingFinalizers + File.Delete cleanup.

    Add a private helper `CreateFullProfile(string slug)` that returns a CreatorStyleProfile with Platform,
    MinDecks (>= 5), InsufficientSample=false, one StatedRule, one MeasuredMetric (Distribution set), one
    FusedTarget (Conflict set), UpdatedUtc = a fixed DateTimeOffset (use `DateTimeOffset.UtcNow` truncated or a
    literal so the assertion is deterministic; assert with a tolerance if sub-second precision differs across
    the SQLite TEXT round-trip).

    Write `[Fact]` tests covering every bullet in <behavior>:
    - `EnsureSchemaAsync_IsIdempotent`
    - `GetBySlugAsync_UnknownSlug_ReturnsNull`
    - `UpsertAsync_ThenGetBySlug_RoundTripsFullShape` (assert all scalars + section elements + nested Distribution/Conflict)
    - `UpsertAsync_BelowFloor_InsufficientSampleSurvivesRoundTrip` (MinDecks below MinDeckFloor, InsufficientSample=true → reads back true)
    - `UpsertAsync_MeasuredOnly_EmptySectionsReadBackEmptyNotNull`
    - `UpsertAsync_StatedOnly_EmptySectionsReadBackEmptyNotNull`
    - `UpsertAsync_ReupsertSameSlug_OverwritesSingleRow` (change Platform/MinDecks, re-upsert, assert updated value)

    Use `Assert.Empty(...)` (not Assert.Null) for the empty sections to prove empty-not-null. Use LF line endings.
  </action>
  <verify>
    <automated>dotnet build DeckFlow.Core.Tests -c Debug (0 errors, 0 new warnings)</automated>
    <human-check>Run the SQLite tests via the WSL Windows dotnet host (VSTest is unreliable in native WSL): the CreatorStyleProfileStoreTests class passes all facts. In CI (push-and-watch) these run unconditionally with no Docker.</human-check>
  </verify>
  <acceptance_criteria>
    - CreatorStyleProfileStoreTests.cs exists in namespace DeckFlow.Core.Tests with the seven [Fact] tests named above.
    - The insufficient-sample test constructs a profile with MinDecks < CreatorStyleProfile.MinDeckFloor and asserts InsufficientSample==true after round-trip.
    - The measured-only and stated-only tests use Assert.Empty on the empty sections (empty-not-null).
    - The full-shape test asserts the nested Distribution and Conflict objects survive.
    - `dotnet build DeckFlow.Core.Tests` succeeds with 0 errors and no new warnings.
  </acceptance_criteria>
  <done>Unconditional SQLite round-trip tests prove full-shape fidelity, the insufficient_sample flag (CS-03), and partial-profile empty-not-null reads (D-07).</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Gated Postgres round-trip tests via Testcontainers</name>
  <files>DeckFlow.Core.Tests/Integration/CreatorStyleProfileStorePostgresTests.cs</files>
  <read_first>
    - DeckFlow.Web.Tests/Integration/PostgresContainerFixture.cs (IClassFixture usage + GetConnectionStringOrSkipAsync)
    - DeckFlow.Core.Tests/Integration/PostgresContainerFixture.cs (the local copy from Task 1)
    - DeckFlow.Core.Tests/CreatorStyleProfileStoreTests.cs (reuse the CreateFullProfile shape + assertions from Task 2)
    - DeckFlow.Core/Content/CreatorStyleProfileStore.cs (store under test; Postgres ctor path)
    - DeckFlow.Core/Storage/RelationalDatabaseConnection.cs (RelationalDatabaseProvider.Postgres descriptor)
  </read_first>
  <behavior>
    - When DECKFLOW_POSTGRES_TESTS != "1" or Docker is unavailable, the tests SKIP (never fail).
    - When enabled, a fully-populated profile round-trips on the Postgres dialect exactly as on SQLite (scalars + sections + nested objects equal).
    - When enabled, insufficient_sample=true survives write→read on Postgres.
    - When enabled, a measured-only profile reads its empty sections back as empty-not-null on Postgres.
    - Each test uses a unique slug (or the fixture is per-class) so container state does not cross-contaminate.
  </behavior>
  <action>
    Create `DeckFlow.Core.Tests/Integration/CreatorStyleProfileStorePostgresTests.cs` in namespace
    `DeckFlow.Core.Tests.Integration`, implementing `IClassFixture<PostgresContainerFixture>` (constructor
    receives the fixture). Mark each test method with `[PostgresFact]` (the local gating attribute from Task 1),
    NOT plain `[Fact]`. In each test: `var conn = await fixture.GetConnectionStringOrSkipAsync();` (this throws
    SkipException when Docker/flag is unavailable — the test skips), then build
    `new RelationalDatabaseConnection(RelationalDatabaseProvider.Postgres, conn)` (append `;SSL Mode=Disable`
    to `conn` if the Testcontainers connection fails the SSL handshake — see observation 16276), then
    `new CreatorStyleProfileStore(descriptor)` (ensureSchemaEnabled defaults true so the container gets the
    table created), `await store.EnsureSchemaAsync()`, upsert, and GetBySlugAsync.

    Reuse the profile-construction + assertion shape from Task 2 (copy a local CreateFullProfile helper — the
    two test files do not share a base class). Cover at minimum:
    - `UpsertAsync_ThenGetBySlug_RoundTripsFullShape_OnPostgres`
    - `UpsertAsync_BelowFloor_InsufficientSampleSurvives_OnPostgres`
    - `UpsertAsync_MeasuredOnly_EmptySectionsReadBackEmptyNotNull_OnPostgres`

    Use a unique slug per test (e.g. `$"round-trip-{Guid.NewGuid():N}"`) to isolate rows within the shared
    container. Use LF line endings.
  </action>
  <verify>
    <automated>dotnet build DeckFlow.Core.Tests -c Debug (0 errors, 0 new warnings)</automated>
    <human-check>With DECKFLOW_POSTGRES_TESTS=1 and Docker running locally, the three [PostgresFact] tests PASS against a real postgres:16-alpine container; without the env var they SKIP (default CI + local runs stay green with no Docker).</human-check>
  </verify>
  <acceptance_criteria>
    - CreatorStyleProfileStorePostgresTests.cs exists in namespace DeckFlow.Core.Tests.Integration, implements IClassFixture<PostgresContainerFixture>, and every test uses [PostgresFact].
    - Each test calls GetConnectionStringOrSkipAsync and constructs a RelationalDatabaseProvider.Postgres descriptor.
    - The three named tests cover full-shape, insufficient_sample survival, and measured-only empty-not-null on Postgres.
    - `dotnet build DeckFlow.Core.Tests` succeeds with 0 errors; the tests SKIP (not fail) when DECKFLOW_POSTGRES_TESTS is unset.
  </acceptance_criteria>
  <done>Gated Postgres round-trip tests prove the same write-then-read fidelity on the Postgres dialect, satisfying CS-04's both-dialects requirement.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test → ephemeral DB | Tests drive a temp SQLite file and a throwaway Testcontainers Postgres. No production data, no network egress beyond the local Docker container. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-94-SC | Tampering | Testcontainers.PostgreSql install | mitigate | In-solution package (already referenced by DeckFlow.Web.Tests at the same version 3.10.0) — not a new/unaudited dependency, so no legitimacy checkpoint required. Pinned to 3.10.0. |
| T-94-05 | Denial of Service | container start | accept | Postgres tests are opt-in (DECKFLOW_POSTGRES_TESTS=1); default CI/local runs skip them, so no Docker dependency is forced on the normal path. |

No untrusted-input or secret-handling surface — test-only code exercising the Phase 94 substrate.
</threat_model>

<verification>
- `dotnet build DeckFlow.Core.Tests` clean.
- SQLite tests run unconditionally and pass (CI push-and-watch).
- Postgres tests skip cleanly with no Docker; pass with DECKFLOW_POSTGRES_TESTS=1 + Docker.
- Testcontainers.PostgreSql pinned to 3.10.0 (in-solution).
</verification>

<success_criteria>
- xUnit round-trip tests pass on both dialects proving full-shape write-then-read fidelity (CS-04).
- insufficient_sample=true survives round-trip on both dialects (CS-03).
- Partial profiles (measured-only / stated-only / fully-fused) round-trip with empty-not-null sections (D-07).
</success_criteria>

<output>
Create `.planning/phases/94-style-profile-foundation/94-03-SUMMARY.md` when done.
</output>
