---
phase: 105-builder-compatible-export
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - DeckFlow.Core/Manabase/ScryfallCardData.cs
  - DeckFlow.Web/Services/Manabase/ScryfallCardDataMapper.cs
  - DeckFlow.Web.Tests/Manabase/ScryfallCardDataMapperTests.cs
autonomous: true
requirements: [EXPORT-03]
must_haves:
  truths:
    - "Every card Cut Lab already resolves carries its Scryfall color_identity through to the cached ScryfallCardData with no new HTTP call."
  artifacts:
    - path: "DeckFlow.Core/Manabase/ScryfallCardData.cs"
      provides: "ColorIdentity property mirroring the existing ProducedMana handling"
      contains: "ColorIdentity"
    - path: "DeckFlow.Web/Services/Manabase/ScryfallCardDataMapper.cs"
      provides: "Copies ScryfallCard.ColorIdentity into ScryfallCardData"
  key_links:
    - from: "ScryfallCardDataMapper.ToCardData"
      to: "ScryfallCardData.ColorIdentity"
      via: "card.ColorIdentity assignment"
      pattern: "ColorIdentity = card.ColorIdentity"
---

<objective>
Close the color-identity data gap. `ScryfallCardData` (the model Cut Lab already resolves and caches for every pool card) has no `ColorIdentity`, though the raw `ScryfallCard` DTO does (`color_identity`, ScryfallDtos.cs:47). Extend the cached model + populate it in the mapper, mirroring the existing `ProducedMana` field. This lets the export validation (EXPORT-03, Plan 105-03) run a commander-color-identity subset check against already-resolved cards with **no new Scryfall round-trip** (RESEARCH "Don't Hand-Roll" + Anti-Patterns).

Purpose: The color-identity leg of the pre-export validation needs each card's identity; re-fetching from Scryfall at export time would be a redundant, rate-limited round-trip.
Output: `ColorIdentity` on `ScryfallCardData`, copied by the mapper, with a mapper test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/cut-lab/phases/105-builder-compatible-export/105-RESEARCH.md
@DeckFlow.Core/Manabase/ScryfallCardData.cs
@DeckFlow.Web/Services/Manabase/ScryfallCardDataMapper.cs

<interfaces>
<!-- Raw DTO already exposes the field: ScryfallDtos.cs:47 -->
[property: JsonPropertyName("color_identity")] IReadOnlyList<string>? ColorIdentity,

<!-- Mirror the existing ProducedMana handling: -->
<!--   model:  ScryfallCardData.cs:33-35  [JsonPropertyName("produced_mana")] public IReadOnlyList<string>? ProducedMana { get; init; } -->
<!--   mapper: ScryfallCardDataMapper.cs:26  ProducedMana = card.ProducedMana, -->

<!-- Existing subset-check logic to reuse verbatim in Plan 105-03 (do NOT duplicate here): -->
<!--   ScryfallSetService.IsPlayableInCommanderIdentity (private), ScryfallSetService.cs:548-563 -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Thread color_identity through ScryfallCardData + mapper</name>
  <files>DeckFlow.Core/Manabase/ScryfallCardData.cs, DeckFlow.Web/Services/Manabase/ScryfallCardDataMapper.cs, DeckFlow.Web.Tests/Manabase/ScryfallCardDataMapperTests.cs</files>
  <behavior>
    - ToCardData copies a populated ScryfallCard.ColorIdentity (e.g. ["W","U"]) into ScryfallCardData.ColorIdentity.
    - A null/absent color_identity maps to null without throwing.
  </behavior>
  <action>
    Add `[JsonPropertyName("color_identity")] public IReadOnlyList<string>? ColorIdentity { get; init; }` to `ScryfallCardData` immediately after `ProducedMana` (ScryfallCardData.cs:33-35), matching that property's xmldoc/attribute style exactly (carve-out: keep the `[JsonPropertyName(...)]` attribute on its own line, do not inline it). In `ScryfallCardDataMapper.ToCardData` add `ColorIdentity = card.ColorIdentity,` alongside `ProducedMana = card.ProducedMana,` (ScryfallCardDataMapper.cs:26). This is purely additive and non-breaking (RESEARCH Assumption A3). Add a case to `ScryfallCardDataMapperTests.cs` asserting ColorIdentity is copied (populated + null cases). Run the existing manabase mapper tests to confirm no regression.
  </action>
  <verify>
    <automated>dotnet test DeckFlow.Web.Tests --filter FullyQualifiedName~ScryfallCardDataMapper</automated>
  </verify>
  <done>ScryfallCardData exposes ColorIdentity, the mapper copies it, and the mapper test passes for populated and null inputs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Scryfall JSON → ScryfallCardData | Upstream card payload deserialized into cached model (existing boundary; one additive field) |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-105-03 | Tampering | Malformed/absent color_identity in upstream payload | mitigate | Nullable `IReadOnlyList<string>?` tolerates absent/empty arrays; consumer (105-03) treats null as "unverified", not "legal" (RESEARCH Pitfall 4) |
| T-105-SC | Tampering | package installs | mitigate | No npm/NuGet packages added by this plan; nothing to audit |
</threat_model>

<verification>
- `dotnet build` clean (no new warnings; LF preserved; changed-lines format gate satisfied).
- `dotnet test DeckFlow.Web.Tests --filter FullyQualifiedName~ScryfallCardDataMapper` green.
- `dotnet test DeckFlow.Core.Tests` green (no downstream manabase consumer regressions — Assumption A3).
</verification>

<success_criteria>
- `ScryfallCardData.ColorIdentity` present and populated by the mapper from already-resolved cards; no new Scryfall call introduced.
</success_criteria>

<output>
Create `.planning/workstreams/cut-lab/phases/105-builder-compatible-export/105-02-SUMMARY.md` when done.
</output>
