---
context: phase
phase: 22-content-kb-site-integration
task: 3
total_tasks: 4
status: code_complete_pending_uat
last_updated: 2026-06-02T17:36:04.257Z
---

<current_state>
Phase 22 (Content KB Site Integration) is CODE-COMPLETE and TESTED across all 4 plans.
25 commits on branch v1.4 (679b0df..25fe892), 23 ahead of origin — NONE pushed.
Whole-solution build clean. Web.Tests 528/0 pass, Core 257/0 pass.
KB-08 + KB-09 closed. HIGH-4 CSRF gap closed. All locked decisions D-22A..F honored.
The ONLY remaining work is two blocking human-UAT checkpoints (need a user-started dev
server), then the ROADMAP [x] flip and an optional push. Nothing left to CODE.
</current_state>

<completed_work>
- 22-01 visibility data layer (is_visible column + additive migration both dialects;
  UpsertRowPreservingVisibilityAsync; published/all/byId/setVisibility; content.kb.enabled
  seed OFF; Core.Tests visibility fixture). Codex impl + cavecrew review + IsVisible XML doc.
  commits 679b0df..a956101.
- 22-02 CLI content-index-export verb + hand-authored 10-row seed (Codex, Task 1) +
  operator-APPROVED protected-file edits (.gitignore/.dockerignore/Dockerfile) + 10 artifacts
  copied into tracked content-kb/ + README (Codex, Task 3). commits 9786333, 1d6ddd2.
- 22-03 public surface (Codex): ContentArtifactParser, ContentKbArtifactPathResolver
  (ordered candidates), ContentKbSeedLoader (Id=0, preserving upsert), ContentKbController
  (browse + detail with D-22F subtree guards), 2 views, content-kb.ts filter+copy, nav link,
  Program.cs DI (builder.Environment before build; schema+seed post-build). cavecrew review:
  no issues. commits fe1345a..e8ebcf6.
- 22-04 admin curation (CLAUDE impl, user override): AdminContentKbController (3 POSTs each
  double-CSRF-guarded) + AdminFlags.Toggle same-origin fix (HIGH-4) + view + .admin-shell CSS
  + sidebar + content-kb-admin.ts (modal + two-click confirm). cavecrew review: no issues.
  commits 79c62f4..b6be9bc.
- Web-layer unit tests (CLAUDE): 32 tests across 6 files + 3 fakes. commit 13f61a2.
</completed_work>

<remaining_work>
- 22-03 Task 4 (human-verify, blocking): public UAT — see how-to-verify in 22-03-PLAN.md.
- 22-04 Task 3 (human-verify, blocking): admin UAT — see how-to-verify in 22-04-PLAN.md.
- After "approved": flip ROADMAP phase 22 to [x]; update STATE.
- Optional: push the 23 unpushed v1.4 commits to origin as backup.
- Then phase 23 (doc-comment backfill + strip NoWarn) — last v1.4 phase, depends on 22.
</remaining_work>

<decisions_made>
- All 4 plans forced through Codex cross-AI (plans lacked cross_ai frontmatter, but
  cross_ai_execution=true + CLAUDE.md Codex-codes rule + prior continue-here directed it).
- 22-04 implemented by Claude per explicit user override ("cancel codex and you finish
  coding"); Codex had already completed 22-03 before the cancel landed.
- Added content-kb-admin.ts beyond the plan's allowed_files — REQUIRED because CSP is
  script-src 'self' (no inline), and the reload-modal + two-click Hide-All confirm need a
  compiled module. Documented in 22-04-SUMMARY.
- Compiled wwwroot/js is NOT committed (.gitignore:13); Docker compiles TS at build. .ts is
  the tracked source of truth. CLAUDE.md "js git-tracked" note is stale.
- Tests written by Claude (continuation of the same override; user chose "Claude writes all").
</decisions_made>

<blockers>
- 2 blocking human-UAT checkpoints need a user-started dev server (do NOT auto-launch per MEMORY).
  No workaround — phase cannot close without UAT.
</blockers>

## Required Reading (in order)
1. `.planning/phases/22-content-kb-site-integration/22-03-PLAN.md` Task 4 + `22-04-PLAN.md` Task 3 — the exact UAT steps / resume-signals.
2. `.planning/phases/22-content-kb-site-integration/22-0{1,2,3,4}-SUMMARY.md` — what shipped + the 22-04 content-kb-admin.ts deviation note.
3. `.planning/phases/22-content-kb-site-integration/22-CONTEXT.md` — locked decisions D-01..05 + D-22A..F.

## Critical Anti-Patterns (do NOT repeat these)

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| JQ-NOT-INSTALLED | jq absent in this WSL env; plan acceptance gates written as jq fail on missing binary | advisory | Use dotnet/grep/git assertions instead (already applied throughout phase 22) |
| WWWROOT-JS-FORCE-ADD | wwwroot/js/*.js is gitignored; do NOT git add -f compiled JS — Docker compiles TS at build, .ts is source of truth | advisory | Commit only the .ts; never force-add the .js |
| CSP-INLINE-SCRIPT | CSP is script-src 'self' — inline <script> in cshtml is blocked; admin JS must be a compiled module | advisory | Any new client behavior needs a wwwroot/ts/*.ts file, not inline script |

## Infrastructure State
- Branch v1.4, 23 commits ahead of origin (unpushed). HEAD 25fe892.
- content.kb.enabled flag seeds OFF; stays OFF in prod until a post-UAT operator flip.
- Build via "/mnt/c/Program Files/dotnet/dotnet.exe". VSTest mostly works but flaked once on a
  Core test (passed 3x on retry) — treat single Core failures as WSL-VSTest flake, re-run.
- Docker unavailable in this WSL distro — 22-02 Task-4 docker-build ls verification deferred to
  deploy startup (Plan 03 logs ContentBase + content-kb existence in the resolver ctor).

<context>
Phase fully de-risked and verified at the code+test level. The remaining gates are purely
experiential (browser UAT at 2 viewports incl. 375px, per MEMORY ui-phases-need-visual-verify),
which only the user can drive (dev server is user-started). On resume: do NOT re-execute any
plan — everything is committed. Go straight to surfacing the UAT steps.
</context>

<next_action>
Ask the user to start the dev server (`! scripts/run-web.sh`), then walk the combined 11-step
UAT (public 22-03 + admin 22-04). On "approved": flip ROADMAP phase 22 to [x], update STATE,
and offer to push the 23 commits to origin/v1.4.
</next_action>
