---
phase: 100-creator-style-tool-surface
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - DeckFlow.Web/Services/CreatorStyle/CreatorStylePacketService.cs
  - DeckFlow.Web/Extensions/PacketServiceCollectionExtensions.cs
  - DeckFlow.Core/Knowledge/CreatorStyleRubric/CreatorStyleRubricScorer.cs
  - DeckFlow.Web.Tests/Services/CreatorStyle/CreatorStylePacketServiceTests.cs
  - DeckFlow.Web.Tests/Services/CreatorStyle/CreatorStyleDiRegistrationTests.cs
  - DeckFlow.Core.Tests/Knowledge/CreatorStyleRubric/CreatorStyleRubricScorerTests.cs
autonomous: true
requirements: [CS-30]
must_haves:
  truths:
    - "`CreatorStylePacketService` exposes a read-side `TryComputeCacheKeyAsync` and participates in `PacketSessionCache` via a latched write-side bypass, with `tool.creator-style.enabled` as the sole entry in its prompt-mutating bypass list (D-100-06, CS-30)."
    - "A stale cached creator-style packet can never be served across a flag flip — the bypass is read once at the top of BuildAsync and reused at the write site (D-100-06, Pitfall 5)."
    - "Degraded-notice wording branches on whether cards were actually excluded vs an upstream-only outage (D-100-08 / IN-03, D-100-15)."
    - "Profile-unavailable is a distinct typed status on `CreatorStylePacketResult`, never dressed as grounding degradation (D-100-08 / IN-04, D-100-16)."
    - "Exemplar card-name lists are de-duplicated (D-100-08 / IN-08)."
    - "The rubric on-target verdict uses an epsilon comparison, not exact double equality (D-100-08 / IN-01)."
  artifacts:
    - path: "DeckFlow.Web/Services/CreatorStyle/CreatorStylePacketService.cs"
      provides: "PacketSessionCache wiring + TryComputeCacheKeyAsync + IN-03/IN-04/IN-08 fixes"
      contains: "TryComputeCacheKeyAsync"
    - path: "DeckFlow.Web/Extensions/PacketServiceCollectionExtensions.cs"
      provides: "updated DI registration passing PacketSessionCache + IFeatureFlagCache into CreatorStylePacketService"
      contains: "PacketSessionCache"
    - path: "DeckFlow.Core/Knowledge/CreatorStyleRubric/CreatorStyleRubricScorer.cs"
      provides: "epsilon on-target comparison"
      contains: "Abs"
  key_links:
    - from: "CreatorStyleController (Plan 05)"
      to: "CreatorStylePacketService.TryComputeCacheKeyAsync"
      via: "read-side cache-key computation before BuildAsync"
      pattern: "TryComputeCacheKeyAsync"
    - from: "ShouldBypassPacketCache()"
      to: "PromptMutatingCreatorStyleFlags"
      via: "any-flag-on predicate over the bypass list"
      pattern: "PromptMutatingCreatorStyleFlags"
---

<objective>
Add net-new `PacketSessionCache` cache-bypass wiring to `CreatorStylePacketService` (it currently has none) and fold the four cheap P99 correctness items (IN-01, IN-03, IN-04, IN-08) that touch this service and the Core rubric scorer.

Purpose: CS-30 requires the tool's prompt-mutating flag to join the packet cache-bypass set so a stale packet is never replayed across a flag flip (T-100-02). This plan also DEFINES the read-side interface contract (`TryComputeCacheKeyAsync`) that Plan 05's controller consumes — interface-first ordering across plans. The IN-* folds make the degraded/unavailable states correct before the view (Plan 05) renders them.
Output: A cache-aware `CreatorStylePacketService` with correct degraded/unavailable/exemplar behavior and an epsilon-safe rubric scorer, all unit-tested.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/100-creator-style-tool-surface/100-RESEARCH.md
@.planning/phases/100-creator-style-tool-surface/100-PATTERNS.md
@.planning/phases/99-creator-style-artifact-engine/99-REVIEW.md

Pattern source (replicate semantics exactly):
@DeckFlow.Web/Services/DeckAnalysisPacketService.cs

Scope fences:
- Do NOT modify any shared/pre-existing prompt-builder text path — every existing artifact must stay byte-identical. This plan only touches creator-style code + the Core rubric scorer's verdict comparison.
- `PacketSessionCache.ComputeKey(object)` is the shared hashing primitive; per-service field-bag composition lives in this service's own `TryComputeCacheKeyAsync` (mirror `DeckAnalysisPacketService`).
- Pitfall 5 (intentional): `tool.creator-style.enabled` is BOTH the visibility flag and the sole bypass-list entry — caching is effectively a no-op today. Implement as specified; add one rationale comment; do NOT "optimize away" the wiring.
- LF endings; `{ get; init; }` records must NOT be collapsed to `{ get; }` (System.Text.Json carve-out).
- Shared-file note (checker BLOCKER 1): `CreatorStyleDiRegistrationTests.cs` is a THIRD construction site of the service — its direct `new CreatorStylePacketService(...)` (5 args) breaks (CS7036) when this plan adds the required `PacketSessionCache` param. This plan owns the CTOR fix in that file. Plan 03 (serialized after this one) owns the separate `GetAllAsync` fake-store fix in the same file — no same-wave overlap because 03 `depends_on` 02.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: PacketSessionCache cache-bypass wiring + read-side TryComputeCacheKeyAsync + DI</name>
  <read_first>
    - DeckFlow.Web/Services/CreatorStyle/CreatorStylePacketService.cs (full: production ctor ~122-142, internal test-seam ctor ~144-160, BuildAsync ~163-275)
    - DeckFlow.Web/Services/DeckAnalysisPacketService.cs (lines 148-166 flag registry, 280-364 IsAnalysisFlagOn/ShouldBypassPacketCache, 379-428 TryComputeCacheKeyAsync read-side gate, ~589 and ~960-976 write-side latched-local comments)
    - DeckFlow.Web/Services/PacketSessionCache.cs (ComputeKey/TryGet/Set surface)
    - DeckFlow.Web/Extensions/PacketServiceCollectionExtensions.cs (the `AddScoped<ICreatorStylePacketService>` registration ~103-111 and how `DeckAnalysisPacketService` receives `PacketSessionCache` + `IFeatureFlagCache`)
    - DeckFlow.Web/Controllers/DeckPacketController.cs (lines ~235-237: read-side pattern `TryComputeCacheKeyAsync` then `_packetCache.TryGet` — the contract Plan 05 will mirror)
    - DeckFlow.Web.Tests/Services/CreatorStyle/CreatorStyleDiRegistrationTests.cs (the direct `new CreatorStylePacketService(...)` construction ~line 60 in a local ServiceCollection — the third call site that must absorb the new required ctor param)
  </read_first>
  <behavior>
    - Test: with `IFeatureFlagCache` reporting `tool.creator-style.enabled` = true, `TryComputeCacheKeyAsync(request)` returns null (read-side bypass active) so the caller never serves a cached packet.
    - Test: `ShouldBypassPacketCache()` returns true when the flag snapshot has the key true; false when the flag cache is null or the key is absent/false.
    - Test: the write-side latch is read once — a flag flip simulated between BuildAsync start and the write point does not change the write decision (use the test-seam override to observe no cache Set when latched-bypass is true).
  </behavior>
  <action>
    Add to `CreatorStylePacketService`: a `internal static readonly IReadOnlyList<string> PromptMutatingCreatorStyleFlags` containing exactly the single string `tool.creator-style.enabled`; private fields `_packetCache` (PacketSessionCache) and `_flagCache` (IFeatureFlagCache?); a private `IsCreatorStyleFlagOn(string)` predicate reading `_flagCache?.Snapshot().TryGetValue(...)`; and a private `ShouldBypassPacketCache()` that returns `PromptMutatingCreatorStyleFlags.Any(IsCreatorStyleFlagOn)` — mirror `DeckAnalysisPacketService`'s `IsAnalysisFlagOn`/`ShouldBypassPacketCache` semantics verbatim.

    Add `PacketSessionCache packetCache` as a REQUIRED param and `IFeatureFlagCache? flagCache = null` as an OPTIONAL param to the production constructor (with `ArgumentNullException.ThrowIfNull(packetCache)`), and matching optional override params to the internal test-seam constructor so cache behavior can be test-doubled.

    Add a new interface method `Task<string?> TryComputeCacheKeyAsync(CreatorStyleRequest request, CancellationToken cancellationToken)` on `ICreatorStylePacketService` and implement it: return null immediately when `ShouldBypassPacketCache()` is true (read-side gate, mirror DeckAnalysis line ~394); otherwise compose a deterministic field bag from the canonical request inputs (creator slug + normalized deck source + format) and return `PacketSessionCache.ComputeKey(fieldBag)`.

    In `BuildAsync`, latch `bool bypassCacheWrite = ShouldBypassPacketCache();` ONCE at the top; at the point the result is produced, only call `_packetCache.Set/SetAsync(cacheKey, result, ...)` when `!bypassCacheWrite`. Compute the same cache key via the shared helper used by `TryComputeCacheKeyAsync` so read and write keys agree. Add one `// Why:` comment noting Pitfall 5 (single-flag topology makes caching a present-day no-op but keeps parity with DeckAnalysisPacketService and prevents cross-flip replay).

    In `PacketServiceCollectionExtensions.cs`, update the `AddScoped<ICreatorStylePacketService>` factory to pass `sp.GetRequiredService<PacketSessionCache>()` and `sp.GetService<IFeatureFlagCache>()` into the new constructor params (mirror the `DeckAnalysisPacketService` registration).

In `CreatorStyleDiRegistrationTests.cs` fix the direct `new CreatorStylePacketService(...)` construction (~line 60) so the Web.Tests assembly still compiles: register a `PacketSessionCache` (and, if the test resolves the service from its local `ServiceCollection`, add `PacketSessionCache` — and any `IFeatureFlagCache` the resolution path needs — to that collection) and pass the new required `PacketSessionCache` arg (the `IFeatureFlagCache?` is optional, leave null or pass a stub). Do NOT add `GetAllAsync` to the fake store here — that belongs to Plan 03.
  </action>
  <verify>
    <automated>dotnet test DeckFlow.Web.Tests --filter "FullyQualifiedName~CreatorStylePacketServiceTests" 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - `ICreatorStylePacketService` declares `Task<string?> TryComputeCacheKeyAsync(CreatorStyleRequest, CancellationToken)`.
    - `grep -c "PromptMutatingCreatorStyleFlags" DeckFlow.Web/Services/CreatorStyle/CreatorStylePacketService.cs` returns >= 2 (declaration + use in ShouldBypassPacketCache).
    - New xUnit cases prove: flag-on → TryComputeCacheKeyAsync returns null; flag-off → returns a 64-char key; write-side latch prevents a cache Set when bypass is latched true.
    - `PacketServiceCollectionExtensions.cs` passes `PacketSessionCache` and `IFeatureFlagCache` into the creator-style registration; `dotnet build DeckFlow.Web` clean.
    - No `{ get; init; }` property was collapsed to `{ get; }`; no shared prompt-builder text path modified.
    - `CreatorStyleDiRegistrationTests.cs` compiles and passes: its direct ctor call supplies the new required `PacketSessionCache`; the whole `DeckFlow.Web.Tests` assembly builds (no CS7036).
  </acceptance_criteria>
  <done>The service is cache-aware with a read-side key method and a latched write-side bypass; DI passes the cache + flag snapshot; bypass unit tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fold IN-03 (notice branch) + IN-04 (unavailable status) + IN-08 (exemplar dedup) + IN-01 (epsilon verdict)</name>
  <read_first>
    - DeckFlow.Web/Services/CreatorStyle/CreatorStylePacketService.cs (lines ~226-230 exemplar CardNames; ~244-246 degraded notice; ~277-291 CreateUnavailableResult; CreatorStylePacketResult record definition)
    - DeckFlow.Core/Knowledge/CreatorStyleRubric/CreatorStyleRubricScorer.cs (the `GetVerdict(double delta)` helper, ~lines 75-83)
    - .planning/phases/99-creator-style-artifact-engine/99-REVIEW.md (IN-01, IN-03, IN-04, IN-08 exact text)
    - .planning/phases/100-creator-style-tool-surface/100-UI-SPEC.md (Copywriting Contract: IN-03 branch A "cards withheld" vs branch B "upstream outage" exact banner strings)
    - DeckFlow.Web.Tests/Services/CreatorStyle/CreatorStylePacketServiceTests.cs (existing degraded/rejection test idiom at ~91-145)
  </read_first>
  <behavior>
    - Test (IN-01): `GetVerdict` returns "on-target" for a fractional delta of magnitude < 0.0005 (e.g. 1e-6) and "over"/"under" outside the epsilon band.
    - Test (IN-03): when grounding degraded with excludedCount > 0, the notice uses the cards-withheld wording; when degraded via upstream failure only (excludedCount == 0), the notice uses the upstream-outage wording.
    - Test (IN-04): a missing/insufficient profile produces a result whose typed unavailable status is set (distinct from GroundingDegraded), with empty ArtifactText.
    - Test (IN-08): exemplar CardNames contain no duplicate entries (deduplicated by ordinal).
  </behavior>
  <action>
    IN-01: In `CreatorStyleRubricScorer.GetVerdict`, replace `delta == 0` with an epsilon check `Math.Abs(delta) < 0.0005` returning "on-target"; keep the `delta < 0 ? "under" : "over"` fallthrough.

    IN-08: Where exemplar `CardNames` are assembled (~226-230), apply `.Distinct(StringComparer.Ordinal)` after resolution so duplicate printings collapse. Do not otherwise change exemplar selection.

    IN-03: Branch the degraded `Notice` text (~244-246) on whether cards were actually excluded. Use the two exact banner strings from the UI-SPEC Copywriting Contract: branch A (excludedCount > 0) = the "Some cards couldn't be validated and were left out of this packet…" wording; branch B (excludedCount == 0, upstream-only) = the "Card validation is temporarily unavailable, so this packet uses a reduced card pool…" wording.

    IN-04: Add an explicit typed unavailable discriminator to `CreatorStylePacketResult` (e.g. a `bool ProfileUnavailable` init-only property, defaulting false, with xmldoc) and set it in `CreateUnavailableResult` INSTEAD of overloading `GroundingDegraded` for the profile-unavailable case. Keep `GroundingDegraded` meaning what its xmldoc says (grounding only). Do not remove the existing unavailable-notice strings — the view (Plan 05) still reads them, but the new flag is the primary IN-04 signal. Preserve the record's `{ get; init; }` accessors (carve-out).
  </action>
  <verify>
    <automated>dotnet test DeckFlow.Web.Tests --filter "FullyQualifiedName~CreatorStylePacketServiceTests" 2>&1 | tail -6 && dotnet test DeckFlow.Core.Tests --filter "FullyQualifiedName~CreatorStyleRubricScorerTests" 2>&1 | tail -6</automated>
  </verify>
  <acceptance_criteria>
    - `CreatorStyleRubricScorer.GetVerdict` uses `Math.Abs(delta) < 0.0005`; a scorer test asserts on-target for a sub-epsilon fractional delta.
    - `CreatorStylePacketResult` has a typed unavailable discriminator (not `GroundingDegraded`) set by `CreateUnavailableResult`; an xUnit case asserts it.
    - Notice-wording tests pass for BOTH the excluded-cards branch and the upstream-only branch (IN-03), using the exact UI-SPEC strings.
    - An exemplar-dedup test asserts `CardNames` has no ordinal duplicates (IN-08).
    - All `{ get; init; }` accessors preserved; no shared prompt-builder text path touched; `dotnet build` clean.
  </acceptance_criteria>
  <done>IN-01/03/04/08 are folded and each is covered by a passing unit test.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator flag flip → in-memory packet cache | A flag flip between two requests must not replay a pre-flip packet from `PacketSessionCache`. |
| upstream Scryfall/Spellbook → packet content | Grounding degradation must be reported truthfully (withheld-cards vs outage) without fabricating a "cards withheld" claim. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-100-02 | Tampering | Stale cached packet replayed across a flag flip | mitigate | Register `tool.creator-style.enabled` in `PromptMutatingCreatorStyleFlags`; read-side `TryComputeCacheKeyAsync` returns null and write-side latched-local skips the cache Set while the flag is on (mirrors `DeckAnalysisPacketService`). |
| T-100-05 | Information Disclosure (misleading) | Degraded-notice wording | mitigate | IN-03 branch guarantees the "cards withheld" claim only appears when exclusions actually occurred; upstream-only outages use outage wording. |
| T-100-SC | Tampering | npm/pip/cargo installs | accept | No package installs; edits are to existing C# source only. |
</threat_model>

<verification>
- `dotnet build` clean (0 new warnings).
- `dotnet test DeckFlow.Web.Tests --filter FullyQualifiedName~CreatorStylePacketServiceTests` green (bypass + IN-03/04/08 cases).
- `dotnet test DeckFlow.Core.Tests --filter FullyQualifiedName~CreatorStyleRubricScorerTests` green (IN-01 epsilon case).
- Existing byte-identity suites still pass (no shared prompt path touched).
</verification>

<success_criteria>
`CreatorStylePacketService` participates in `PacketSessionCache` with a latched flag-bypass and a read-side key method; degraded/unavailable/exemplar behavior is correct and typed; the rubric verdict is epsilon-safe; all new unit tests pass.
</success_criteria>

<output>
Create `.planning/phases/100-creator-style-tool-surface/100-02-SUMMARY.md` when done.
</output>
