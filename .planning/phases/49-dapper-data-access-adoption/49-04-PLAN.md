---
phase: 49-dapper-data-access-adoption
plan: 04
type: execute
wave: 4
depends_on: ["49-01b", "49-03"]
files_modified:
  - DeckFlow.Web/Services/CategoryKnowledgeStore.cs
  - DeckFlow.Core/Knowledge/CategoryKnowledgeRepository.cs
  - DeckFlow.Web/Services/Analytics/RequestMetricsStore.cs
autonomous: true
requirements: [DAP-01, DAP-02, DAP-03]

must_haves:
  truths:
    - "(CONTINGENT ON 49-01 PASS verdict) CategoryKnowledgeStore and CategoryKnowledgeRepository execute all non-DDL paths through Dapper with no store-local coercion"
    - "Every Dapper call inside a transaction scope passes transaction: transaction (or transaction: null only at the documented read-outside-tx call site); the card-id cache loop logic is preserved"
    - "UPSERT (ON CONFLICT), RETURNING, COALESCE, and dialect SQL fragments are kept verbatim"
    - "RequestMetricsStore.UpsertBatchAsync (unnest array batch) is unchanged and carries an intentional-raw // Why: comment"
    - "All DDL/migration methods stay raw; the full Core.Tests + Web.Tests suites show 0 new failures on SQLite"
  artifacts:
    - path: "DeckFlow.Core/Knowledge/CategoryKnowledgeRepository.cs"
      provides: "Heaviest store converted; transactions + UPSERT + RETURNING + card-id cache via Dapper"
      contains: "transaction:"
    - path: "DeckFlow.Web/Services/Analytics/RequestMetricsStore.cs"
      provides: "Carve-out documented; UpsertBatchAsync unnest-batch unchanged"
      contains: "Why:"
  key_links:
    - from: "DeckFlow.Core/Knowledge/CategoryKnowledgeRepository.cs"
      to: "Dapper ExecuteAsync/ExecuteScalarAsync with transaction: transaction"
      via: "BeginTransactionAsync scope; ResolveCardIdAsync forwards connection + transaction"
      pattern: "transaction:"
    - from: "DeckFlow.Web/Services/CategoryKnowledgeStore.cs"
      to: "Dapper execution"
      via: "AddTimestampParameter replaced by DateTimeTypeHandler; CoerceCount preserved"
      pattern: "QueryAsync|ExecuteAsync|ExecuteScalarAsync"
---

<objective>
After a PASS spike verdict, convert the two heaviest stores last, document the RequestMetricsStore carve-out, and prove full-suite parity (REQ-4/REQ-5/REQ-6, DAP-03).

This plan is CONTINGENT on `VERDICT: PASS` in 49-GATE-VERDICT.md and depends on 49-03. It is the last conversion plan: CategoryKnowledgeStore + CategoryKnowledgeRepository (transactions, UPSERT, RETURNING, in-memory card-id cache), the RequestMetricsStore carve-out comment, and a final phase-wide parity gate.

IMPORTANT (sequencing): CategoryKnowledgeRepository.cs is also touched by Phase 44 (re-sequenced to run after 49). Phase 49 converts it first; Phase 44 plans must be re-checked against the converted code before they execute. Note this in the summary.

Output: both knowledge stores converted; RequestMetricsStore carve-out documented; ExecuteReaderAsync grep == 0 across all eligible files; full SQLite suites green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/49-dapper-data-access-adoption/49-SPEC.md
@.planning/phases/49-dapper-data-access-adoption/49-CONTEXT.md
@.planning/phases/49-dapper-data-access-adoption/49-RESEARCH.md
@.planning/phases/49-dapper-data-access-adoption/49-PATTERNS.md
@.planning/phases/49-dapper-data-access-adoption/49-GATE-VERDICT.md
@CLAUDE.md

<interfaces>
<!-- Reference shapes: converted FeedbackStore.cs (49-01), ContentSourceStore.cs (49-02),
     ContentHarvestRunStore.cs / HarvestRunStore.cs (49-03). -->

CategoryKnowledgeRepository (24 pub methods, DeckFlow.Core/Knowledge/CategoryKnowledgeRepository.cs):
  - Coerced type: DateTime (text timestamps); NO decimal/bool/Guid in result records.
  - Connection-open: uses CreateConnection() + OpenAsync() DIRECTLY (NOT _connectionInfo.OpenConnectionAsync())
    — preserve this (no FK PRAGMA variant). PATTERNS.md finding #2.
  - Transactions: today every in-tx command sets `command.Transaction = transaction;` (e.g. lines 445, 499,
    505, 744, 1032) and the transaction-aware private helpers receive a `DbTransaction transaction` and set it.
    After Dapper conversion, EACH of these becomes a Dapper call that MUST pass `transaction: transaction`.
    A missing `transaction:` runs the call OUTSIDE the tx (silent correctness bug). See the explicit
    per-call-site checklist in Task 2 — that checklist, not a bare grep, is the acceptance bar (MEDIUM-2 fix).
  - ResolveSourceIdForReadAsync is called BOTH inside a tx (lines 436, 491) AND with `null` outside a tx
    (line 710). Its Dapper calls must accept a nullable transaction and pass `transaction: transaction`
    (which is correctly `null` at the read-outside-tx site). Do NOT hard-code a non-null transaction in it.
  - ResolveCardIdAsync: in-memory Dictionary<string,long> cache + INSERT OR IGNORE / ON CONFLICT DO NOTHING
    then read-back id; forwards connection + transaction to its Dapper calls; cache logic UNCHANGED.

CategoryKnowledgeStore (16 pub methods, DeckFlow.Web/Services/CategoryKnowledgeStore.cs):
  - Coerced type: DateTime. AddTimestampParameter helper uses SpecifyKind(Utc).ToString("O")
    -> replaced by DateTimeTypeHandler (pass DateTime directly).
  - CoerceCount internal helper is used by tests — KEEP it (do not delete).
  - Some methods open via OpenConnectionAsync() directly — verify per call site, preserve.

CARVE-OUT (REQ-5, stays raw permanently):
  RequestMetricsStore.UpsertBatchAsync (DeckFlow.Web/Services/Analytics/RequestMetricsStore.cs) —
  NpgsqlParameter unnest array binding, no Dapper equivalent. Do NOT convert. Add `// Why:` comment ONLY.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Convert CategoryKnowledgeStore</name>
  <files>DeckFlow.Web/Services/CategoryKnowledgeStore.cs</files>
  <read_first>
    - .planning/phases/49-dapper-data-access-adoption/49-GATE-VERDICT.md (CONFIRM VERDICT: PASS; if FAIL, stop and report)
    - DeckFlow.Web/Services/FeedbackStore.cs + DeckFlow.Core/Content/ContentSourceStore.cs (converted reference shapes)
    - .planning/phases/49-dapper-data-access-adoption/49-RESEARCH.md §Eligible Store Inventory Wave 3 CategoryKnowledgeStore row (AddTimestampParameter; CoerceCount stays; direct OpenConnectionAsync calls)
    - DeckFlow.Web/Services/CategoryKnowledgeStore.cs (full — 16 methods, mostly delegating; AddTimestampParameter helper; CoerceCount internal helper used by tests)
    - DeckFlow.Web.Tests CategoryKnowledgeStore test class (+ note MTG_DATA_DIR env-mutating [Collection] pattern)
  </read_first>
  <action>
    Confirm VERDICT: PASS first. Add using Dapper. Convert each non-delegating direct-query method's reader/scalar/non-query patterns to connection.QueryAsync&lt;T&gt; / ExecuteScalarAsync&lt;T&gt; / ExecuteAsync over verbatim SQL, passing DateTime values directly (DateTimeTypeHandler encodes) — delete the AddTimestampParameter helper. KEEP the CoerceCount internal helper (tests depend on it). Keep any DDL/EnsureSchemaAsync raw with a `// Why:` comment. Preserve each method's existing connection-open call. SQL verbatim; touch only changed lines (LF, no reformat).
  </action>
  <verify>
    <automated>"/mnt/c/Program Files/dotnet/dotnet.exe" build DeckFlow.sln 2>&1 | tail -5; grep -v '^[[:space:]]*//' DeckFlow.Web/Services/CategoryKnowledgeStore.cs | grep -c ExecuteReaderAsync; grep -c "CoerceCount" DeckFlow.Web/Services/CategoryKnowledgeStore.cs; "/mnt/c/Program Files/dotnet/dotnet.exe" test DeckFlow.Web.Tests/DeckFlow.Web.Tests.csproj --filter "FullyQualifiedName~CategoryKnowledgeStore" 2>&1 | tail -12</automated>
  </verify>
  <acceptance_criteria>
    - build 0/0
    - Comment-filtered ExecuteReaderAsync count == 0 (DDL excepted)
    - AddTimestampParameter deleted; CoerceCount RETAINED (grep >= 1)
    - CategoryKnowledgeStore tests pass on SQLite; no public signature change
  </acceptance_criteria>
  <done>CategoryKnowledgeStore runs direct-query paths through Dapper; CoerceCount preserved for tests.</done>
</task>

<task type="auto">
  <name>Task 2: Convert CategoryKnowledgeRepository (transactions + UPSERT + RETURNING + card-id cache) — LAST store</name>
  <files>DeckFlow.Core/Knowledge/CategoryKnowledgeRepository.cs</files>
  <read_first>
    - .planning/phases/49-dapper-data-access-adoption/49-PATTERNS.md lines 397-431 (CategoryKnowledgeRepository before/after: transaction passing, card-id cache loop preservation) + Shared Patterns "OpenConnectionAsync vs CreateConnection" table (this store uses CreateConnection + OpenAsync directly)
    - .planning/phases/49-dapper-data-access-adoption/49-RESEARCH.md Pitfall 6 (card-id cache across Dapper calls — all calls in tx scope pass transaction:) + Code Examples "Dapper with transaction"
    - DeckFlow.Core/Knowledge/CategoryKnowledgeRepository.cs (full — 24 methods; transaction scopes; ResolveCardIdAsync; DDL/index methods)
    - DeckFlow.Core/Knowledge/ContentModels.cs (repository result record shapes — keep { get; init; })
    - DeckFlow.Core.Tests/CategoryKnowledgeRepositoryTests.cs (17 facts + parity + dedup — the parity bar)
  </read_first>
  <action>
    Confirm VERDICT: PASS first. Add using Dapper. Preserve the store's connection-open pattern (CreateConnection() + OpenAsync() directly — do NOT switch to _connectionInfo.OpenConnectionAsync()). For every method: convert reader loops to QueryAsync&lt;T&gt;/QuerySingleOrDefaultAsync&lt;T&gt;, RETURNING-id / scalar to ExecuteScalarAsync&lt;long&gt;, non-query writes to ExecuteAsync, passing DateTime values directly (DateTimeTypeHandler encodes).

    TRANSACTION CORRECTNESS — explicit per-call-site checklist (MEDIUM-2 fix; the bar is this checklist, NOT a bare grep count). Today each in-tx command sets `command.Transaction = transaction;`; after conversion EACH must carry `transaction: transaction` on its Dapper call. Walk and convert every transaction-scoped call site below, ticking each in the SUMMARY as `transaction:` carried (line numbers are pre-conversion anchors — re-locate by method as you edit):

    | # | Method (tx scope opened at) | Converted Dapper call(s) that MUST pass transaction: |
    |---|------------------------------|------------------------------------------------------|
    | 1 | ReplaceSourceRowsAsync (BeginTransactionAsync ~433) | ResolveSourceIdForReadAsync(~436)/ResolveSourceIdAsync(~437) helper calls; deleteCommand DELETE (~445); ResolveCardIdAsync(~455); UpsertCategoryObservationAsync(~457) |
    | 2 | DeleteSourceDataAsync (~489) | ResolveSourceIdForReadAsync(~491); deleteObservationsCommand (~499); deleteTotalsCommand (~505) |
    | 3 | (scope ~533) | ResolveSourceIdAsync(~535); ResolveCardIdAsync(~537); UpsertCategoryObservationAsync(~542) |
    | 4 | (scope ~572) | ResolveSourceIdAsync(~574); ResolveCardIdAsync(~576) + the write at ~580s |
    | 5 | (scope ~604) | ResolveSourceIdAsync(~606); ResolveCardIdAsync(~617,~639); UpsertCategoryObservationAsync(~618) |
    | 6 | (scope ~739) | the in-loop command at ~744 |
    | 7 | (scope ~1027) | the command at ~1032 |
    | H1 | ResolveCardIdAsync (helper, ~1049) | its INSERT OR IGNORE / ON CONFLICT + read-back (cmd ~1063) — pass the forwarded transaction |
    | H2 | ResolveSourceIdForReadAsync (helper, ~1080) | its read cmd (~1089) — transaction is NULLABLE here (called with null at ~710); pass `transaction: transaction` so null flows through unchanged |
    | H3 | ResolveSourceIdAsync (helper, ~1099) | ResolveDeckQueueIdForSourceAsync(~1105) + cmd (~1107) |
    | H4 | ResolveDeckQueueIdForSourceAsync (helper, ~1124) | cmd (~1140) |
    | H5 | UpsertCategoryObservationAsync (helper, ~1148) | cmd (~1162) |
    | H6 | (helper, ~1186) | cmd (~1195) |

    Note the read-outside-tx case: ResolveSourceIdForReadAsync is also called with `null` at ~710 (no surrounding transaction) — its converted Dapper call must accept a nullable transaction and pass `transaction: transaction` (correctly `null` there). Do NOT hard-code a non-null transaction in the helper.

    In ResolveCardIdAsync, keep the in-memory Dictionary&lt;string,long&gt; cache and the helper signature unchanged; forward connection + transaction to its Dapper ExecuteAsync / ExecuteScalarAsync&lt;long&gt; calls; keep INSERT OR IGNORE / ON CONFLICT DO NOTHING SQL verbatim. Delete redundant reader/format helpers. Keep ALL DDL/index methods (CREATE TABLE/INDEX, the partial expression index area Phase 44 will touch) raw with `// Why:` comments. SQL verbatim; touch only changed lines (LF, no reformat); do not convert { get; init; } to { get; }.
  </action>
  <verify>
    <automated>"/mnt/c/Program Files/dotnet/dotnet.exe" build DeckFlow.sln 2>&1 | tail -5; echo "=== ExecuteReaderAsync (expect 0, DDL excepted) ==="; grep -v '^[[:space:]]*//' DeckFlow.Core/Knowledge/CategoryKnowledgeRepository.cs | grep -c ExecuteReaderAsync; echo "=== transaction-correctness counters (reviewer reconciles, see acceptance) ==="; echo -n "Dapper calls (Query/Execute/ExecuteScalar): "; grep -v '^[[:space:]]*//' DeckFlow.Core/Knowledge/CategoryKnowledgeRepository.cs | grep -Ec "QueryAsync|QuerySingleOrDefaultAsync|ExecuteAsync|ExecuteScalarAsync"; echo -n "calls carrying transaction: "; grep -v '^[[:space:]]*//' DeckFlow.Core/Knowledge/CategoryKnowledgeRepository.cs | grep -c "transaction:"; echo -n "leftover command.Transaction = (expect 0 in non-DDL): "; grep -v '^[[:space:]]*//' DeckFlow.Core/Knowledge/CategoryKnowledgeRepository.cs | grep -c "\.Transaction = transaction"; "/mnt/c/Program Files/dotnet/dotnet.exe" test DeckFlow.Core.Tests/DeckFlow.Core.Tests.csproj --filter "FullyQualifiedName~CategoryKnowledgeRepository" 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - build 0/0
    - Comment-filtered ExecuteReaderAsync count == 0 (DDL/index methods excepted)
    - TRANSACTION CHECKLIST (MEDIUM-2): the SUMMARY contains the per-call-site table from the action with EVERY row (1-7, H1-H6) ticked as carrying `transaction: transaction` (with H2's nullable-transaction note). The reviewer step (below) lists every Dapper call that lives between a `BeginTransactionAsync` and its `CommitAsync` (directly or via a transaction-forwarding helper) and FAILS the task if ANY such call omits `transaction:`. A bare `grep -c "transaction:"` number alone is NOT sufficient evidence — it must reconcile against the enumerated checklist
    - No `command.Transaction = transaction` ADO.NET-style assignment remains in non-DDL converted methods (the verify counter for it is 0); transaction is carried via the Dapper `transaction:` argument instead
    - ResolveSourceIdForReadAsync still works at the read-outside-tx call site (~710) with a null transaction
    - ResolveCardIdAsync cache logic + signature unchanged; INSERT OR IGNORE / ON CONFLICT SQL verbatim
    - CategoryKnowledgeRepositoryTests (17 facts + parity + dedup) pass on SQLite; no public signature change
    - Summary records the Phase 44 re-check note (44 touches this file next)
  </acceptance_criteria>
  <done>The heaviest store runs transactional UPSERT/RETURNING/card-id-cache paths through Dapper with transaction: on every in-scope call (verified against the enumerated per-call-site checklist, not a bare grep); parity tests green.</done>
</task>

<task type="auto">
  <name>Task 3: Document RequestMetricsStore carve-out + final phase-wide parity gate</name>
  <files>DeckFlow.Web/Services/Analytics/RequestMetricsStore.cs</files>
  <read_first>
    - .planning/phases/49-dapper-data-access-adoption/49-SPEC.md §Boundaries (out-of-scope carve-outs) + REQ-5/REQ-6 acceptance
    - .planning/phases/49-dapper-data-access-adoption/49-PATTERNS.md lines 355-363 (carve-out comment template)
    - DeckFlow.Web/Services/Analytics/RequestMetricsStore.cs (full — UpsertBatchAsync NpgsqlParameter unnest array batch; this method stays raw)
    - .planning/phases/49-dapper-data-access-adoption/49-VALIDATION.md (full-suite + PG parity commands)
  </read_first>
  <action>
    Add a `// Why:` intentional-raw comment at RequestMetricsStore.UpsertBatchAsync stating it uses NpgsqlParameter unnest array binding which has no Dapper equivalent and is intentionally kept raw (cite Phase 49 SPEC §Boundaries) — make NO functional change to the method body (its git diff must be comment-only). Then run the final phase-wide parity gate: (1) `dotnet build DeckFlow.sln` 0/0; (2) full `DeckFlow.Core.Tests` + `DeckFlow.Web.Tests` on SQLite with 0 new failures vs the pre-phase baseline; (3) the ExecuteReaderAsync grep across ALL eligible store files returns 0 non-DDL occurrences. Record the results, plus the gated-manual Postgres parity command (from 49-VALIDATION.md) for the developer to run before /gsd:verify-work, in the plan summary. Confirm RequestMetricsStore.UpsertBatchAsync git diff is comment-only.
  </action>
  <verify>
    <automated>"/mnt/c/Program Files/dotnet/dotnet.exe" build DeckFlow.sln 2>&1 | tail -5; grep -c "Why:" DeckFlow.Web/Services/Analytics/RequestMetricsStore.cs; echo "=== eligible-file reader grep (expect 0) ==="; for f in DeckFlow.Web/Services/FeedbackStore.cs DeckFlow.Core/Content/BlockedVideoStore.cs DeckFlow.Core/Content/ContentSourceStore.cs DeckFlow.Core/Content/SpendLedgerBase.cs DeckFlow.Core/Content/LlmSpendLedger.cs DeckFlow.Core/Content/WhisperSpendLedger.cs DeckFlow.Web/Services/AdminBruteForceTrackerStore.cs DeckFlow.Web/Services/FeatureFlags/FeatureFlagStore.cs DeckFlow.Web/Services/Harvest/HarvestScheduleStore.cs DeckFlow.Core/Content/ContentHarvestRunStore.cs DeckFlow.Web/Services/Harvest/HarvestRunStore.cs DeckFlow.Core/Content/ContentVideoStore.cs DeckFlow.Core/Content/ContentSiteIndexStore.cs DeckFlow.Web/Services/CategoryKnowledgeStore.cs DeckFlow.Core/Knowledge/CategoryKnowledgeRepository.cs; do grep -v '^[[:space:]]*//' "$f" | grep -c ExecuteReaderAsync | tr '\n' ' '; done; echo; git diff --stat DeckFlow.Web/Services/Analytics/RequestMetricsStore.cs; "/mnt/c/Program Files/dotnet/dotnet.exe" test DeckFlow.Core.Tests/DeckFlow.Core.Tests.csproj 2>&1 | tail -5; "/mnt/c/Program Files/dotnet/dotnet.exe" test DeckFlow.Web.Tests/DeckFlow.Web.Tests.csproj 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - RequestMetricsStore.UpsertBatchAsync carries a `// Why:` intentional-raw comment; method body unchanged (git diff comment-only)
    - The eligible-file ExecuteReaderAsync grep prints 0 for every one of the 15 eligible files (DDL methods excepted by comment filter)
    - `dotnet build DeckFlow.sln` 0/0
    - Full Core.Tests + Web.Tests pass on SQLite with 0 new failures vs pre-phase baseline
    - Summary records the gated-manual Postgres parity command and the Phase 44 re-check note
  </acceptance_criteria>
  <done>The single non-DDL carve-out is documented (comment-only), all 15 eligible stores are reader-loop-free, and the full SQLite suites are green — the phase-wide parity bar is met (PG side is the documented manual gate).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| application code → SQL execution | Repository binds card names, deck ids, category observations into UPSERT/RETURNING queries |
| transaction scope → Dapper calls | A missed transaction: arg would run a write outside the atomic scope |
| DB reader → CLR object (type handlers) | DateTime timestamps decoded by the global handler |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-49-10 | Tampering | CategoryKnowledgeRepository params (SQL injection regression) | mitigate | UPSERT/RETURNING/INSERT OR IGNORE SQL kept verbatim and parameterized via Dapper anonymous objects; no interpolation introduced |
| T-49-11 | Tampering | Transaction-scope correctness (missing transaction: arg) | mitigate | Every in-scope Dapper call passes transaction: transaction, verified against an ENUMERATED per-call-site checklist (rows 1-7, H1-H6) reconciled in the SUMMARY — NOT a bare grep count; a reviewer lists all Dapper calls inside tx scopes and fails on any omission; the leftover `command.Transaction =` grep must be 0 in non-DDL; CategoryKnowledgeRepositoryTests parity/dedup facts assert atomic behavior |
| T-49-12 | Tampering | Carve-out drift (UpsertBatchAsync rewritten) | mitigate | RequestMetricsStore.UpsertBatchAsync git diff is comment-only; unnest-array batch body unchanged; `// Why:` comment records intent |
| T-49-13 | Information Disclosure | Type-handler malformed-value handling on repository reads (D-05 parity) | mitigate | DateTimeTypeHandler proven in REQ-2 round-trip (incl. raw on-disk write-path assertion); fail-closed fallback; repository parity tests assert no behavior change |
</threat_model>

<verification>
- `dotnet build DeckFlow.sln` 0/0
- ExecuteReaderAsync grep == 0 across all 15 eligible store files (DDL excepted)
- Transaction correctness verified against the enumerated per-call-site checklist (not a bare grep); leftover `command.Transaction =` == 0 in non-DDL
- RequestMetricsStore.UpsertBatchAsync diff comment-only
- Full Core.Tests + Web.Tests green on SQLite; PG parity is the documented gated-manual step before verify
</verification>

<success_criteria>
- Both knowledge stores converted; transactions pass transaction: on every in-scope call (verified against the enumerated checklist); card-id cache preserved
- RequestMetricsStore carve-out documented (comment-only diff); all DDL stays raw
- ExecuteReaderAsync grep == 0 across every eligible file; full SQLite suites 0 new failures
- No public store signature changed; Phase 44 re-check note recorded
- Only proceeds on a PASS spike verdict
</success_criteria>

<output>
Create `.planning/phases/49-dapper-data-access-adoption/49-04-SUMMARY.md` when done. Record: the completed transaction per-call-site checklist (all rows ticked), the eligible-file grep results, the full-suite baseline comparison, the gated-manual Postgres parity command, and the Phase 44 (CategoryKnowledgeRepository) re-check note.
</output>
