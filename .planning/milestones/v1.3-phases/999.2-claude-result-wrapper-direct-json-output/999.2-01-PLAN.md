---
phase: 999.2-claude-result-wrapper-direct-json-output
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - DeckFlow.Web/Services/PromptBuilders/Analysis/ClaudeAnalysisPromptVariant.cs
  - DeckFlow.Web/Services/PromptBuilders/Comparison/ClaudeComparisonPromptVariant.cs
  - DeckFlow.Web/Services/PromptBuilders/FollowUp/ClaudeFollowUpPromptVariant.cs
  - DeckFlow.Web/Services/PromptBuilders/MetaGap/ClaudeMetaGapPromptVariant.cs
  - DeckFlow.Web/Services/PromptBuilders/SetUpgrade/ClaudeSetUpgradePromptVariant.cs
  - DeckFlow.Web.Tests/ResultContractTests.cs
autonomous: false
requirements:
  - D-01
  - D-02
  - D-03
  - D-04
  - D-05
  - D-06
  - D-07
  - D-08
  - D-09
  - D-10
  - D-11
  - D-12
  - D-13
  - D-14

must_haves:
  truths:
    - "(D-01, D-02) None of the 5 Claude prompt variants instruct Claude to wrap JSON in <result>...</result>; the ResultWrapInstruction AppendLine call AND the follow-on 'Wrap your final structured output in <result>...' AppendLine are gone from every Claude variant."
    - "(D-04, D-05) Each Claude variant now emits a fenced ```json code-block directive, verbatim-copied from its ChatGPT family counterpart (Analysis/Comparison/FollowUp/MetaGap/SetUpgrade), with the leading numbered-list whitespace prefix stripped to match Claude's flatter instructional voice (D-05 exception clause from code_context)."
    - "(D-06) The new fenced-block directive lands in the exact same line position the deleted lines occupied — immediately before builder.AppendLine(\"</\" + \"task>\")."
    - "(D-07) Per-version deck_profile sub-object fenced-block directives in ClaudeAnalysisPromptVariant.cs (lines ~180, ~195, ~200) and the analogous lines in ClaudeComparisonPromptVariant.cs / ClaudeMetaGapPromptVariant.cs are untouched."
    - "(D-08) No new property on AiPlatform record, no new IResultWrapPolicy interface, no new helper method on JsonTextFormatterService — Phase 15 D-01 data-only-record invariant preserved."
    - "(D-12) JsonTextFormatterService.cs is byte-identical to its pre-phase state; ResultWrapInstruction const + ExtractJsonPayload <result>-first-fallback-to-brace-scan behavior are unchanged, so legacy zip artifacts containing pre-999.2 Claude responses still parse via the <result> branch."
    - "(D-13) ResultContractTests.cs is updated so the 5 [InlineData(\"Claude\")] rows asserting <result> in Claude prompt body either become Claude-excluded theories OR a separate Claude-specific theory asserts the new fenced-block directive instead; the test file builds and the Claude_variant_uses_xml_skeleton_with_no_api_role_blocks fact (lines 220-239, no <result> assertion) is untouched."
    - "(D-11) Manual UAT T3 (Claude / deck-analysis), T7 (Claude / cEDH meta-gap), and ad-hoc Claude / deck-comparison all show: a single fenced ```json block in Claude's chat, NO <result> tags anywhere visible, AND the response pastes back into the page and round-trips JSON cleanly."
  artifacts:
    - path: "DeckFlow.Web/Services/PromptBuilders/Analysis/ClaudeAnalysisPromptVariant.cs"
      provides: "Claude deck-analysis prompt variant emitting fenced-json directive instead of <result> wrapper"
      contains: "fenced ```json code block"
    - path: "DeckFlow.Web/Services/PromptBuilders/Comparison/ClaudeComparisonPromptVariant.cs"
      provides: "Claude deck-comparison prompt variant emitting fenced-json directive instead of <result> wrapper"
      contains: "fenced ```json code block"
    - path: "DeckFlow.Web/Services/PromptBuilders/FollowUp/ClaudeFollowUpPromptVariant.cs"
      provides: "Claude follow-up prompt variant emitting fenced-json directive instead of <result> wrapper"
      contains: "fenced ```json code block"
    - path: "DeckFlow.Web/Services/PromptBuilders/MetaGap/ClaudeMetaGapPromptVariant.cs"
      provides: "Claude cEDH meta-gap prompt variant emitting fenced-json directive instead of <result> wrapper"
      contains: "fenced ```json code block"
    - path: "DeckFlow.Web/Services/PromptBuilders/SetUpgrade/ClaudeSetUpgradePromptVariant.cs"
      provides: "Claude set-upgrade prompt variant emitting fenced-json directive instead of <result> wrapper"
      contains: "fenced ```json code block"
    - path: "DeckFlow.Web.Tests/ResultContractTests.cs"
      provides: "Updated test contract reflecting Claude's divergence from ChatGPT/Gemini <result>-wrap convention"
  key_links:
    - from: "DeckFlow.Web/Services/PromptBuilders/Analysis/ClaudeAnalysisPromptVariant.cs"
      to: "DeckFlow.Web/Services/PromptBuilders/Analysis/ChatGptAnalysisPromptVariant.cs:198"
      via: "verbatim-copy fenced-block directive (leading whitespace stripped per D-05 exception)"
      pattern: "You MUST return the JSON inside a fenced .*json code block"
    - from: "DeckFlow.Web/Services/PromptBuilders/Comparison/ClaudeComparisonPromptVariant.cs"
      to: "DeckFlow.Web/Services/PromptBuilders/Comparison/ChatGptComparisonPromptVariant.cs:79"
      via: "verbatim-copy fenced-block directive (top-level object named deck_comparison)"
      pattern: "fenced .*json code block"
    - from: "DeckFlow.Web/Services/PromptBuilders/FollowUp/ClaudeFollowUpPromptVariant.cs"
      to: "DeckFlow.Web/Services/PromptBuilders/FollowUp/ChatGptFollowUpPromptVariant.cs:47"
      via: "verbatim-copy fenced-block directive (regenerate JSON inside fenced block)"
      pattern: "fenced .*json code block"
    - from: "DeckFlow.Web/Services/PromptBuilders/MetaGap/ClaudeMetaGapPromptVariant.cs"
      to: "DeckFlow.Web/Services/PromptBuilders/MetaGap/ChatGptMetaGapPromptVariant.cs:143"
      via: "verbatim-copy fenced-block directive (top-level object meta_gap)"
      pattern: "fenced .*json code block"
    - from: "DeckFlow.Web/Services/PromptBuilders/SetUpgrade/ClaudeSetUpgradePromptVariant.cs"
      to: "DeckFlow.Web/Services/PromptBuilders/SetUpgrade/ChatGptSetUpgradePromptVariant.cs:125"
      via: "verbatim-copy fenced-block directive (set_upgrade_report schema)"
      pattern: "fenced .*json code block"
    - from: "All 5 modified Claude variants"
      to: "DeckFlow.Web/Services/JsonTextFormatterService.cs:50-81 ExtractJsonPayload"
      via: "brace-scan fall-through branch parses fenced-only Claude output (D-12)"
      pattern: "FindPayloadStart"
---

<objective>
Stop the 5 Claude prompt variants from instructing Claude to wrap JSON in `<result>...</result>` tags. Claude empirically emits BOTH the wrapper AND a duplicate fenced ```json block, cluttering chat output (observed during Phase 13 UAT T4, 2026-05-17). Phase removes the two wrap-instruction AppendLine calls from each Claude variant and replaces them in-place with the verbatim fenced-block directive used by the matching ChatGPT counterpart variant. Parser stays untouched (it already falls through `<result>`-regex to brace-scan, so fenced-only Claude responses parse correctly — D-12). ChatGPT + Gemini variants stay untouched (D-02, anti-scope from CONTEXT.md `<domain>`). Test file `ResultContractTests.cs` is updated so its 5 Claude-platform theory rows reflect the new Claude convention (D-13 audit hit confirmed during planning).

Purpose: Clean Claude chat output. Removes a visible UX defect with a 15-line-delta change that exercises the Phase 15 "registries are the per-platform strategy surface" claim — Claude prompts now diverge from ChatGPT/Gemini at the variant-class level without touching `AiPlatform` record or adding any new strategy interface (D-08).

Output: 5 modified Claude prompt-variant files (one logical change each = 5 commits per D-09) + 1 updated test file (commit cluster bundled with the variant it asserts against per D-13) + green `dotnet.exe build DeckFlow.sln -c Release` + manual UAT sign-off.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/999.2-claude-result-wrapper-direct-json-output/999.2-CONTEXT.md
@.planning/phases/999.2-claude-result-wrapper-direct-json-output/999.2-DISCUSSION-LOG.md
@.planning/phases/15-aiplatform-value-object-refactor/15-CONTEXT.md
@.planning/phases/999.1-ai-agnostic-prose-adaptation-razor-views/999.1-CONTEXT.md
@CLAUDE.md

<interfaces>
<!-- Concrete contracts the executor uses verbatim — no codebase exploration needed. -->
<!-- Source: JsonTextFormatterService.cs lines 11-25 (DO NOT MODIFY). -->

The shared `JsonTextFormatterService.ResultWrapInstruction` const at
`DeckFlow.Web/Services/JsonTextFormatterService.cs:13-14` stays as-is. After
Phase 999.2 it is used ONLY by 5 ChatGPT variants + 5 Gemini variants (10
references; was 15). Do not remove the const, do not rename it, do not add a
brand-prefix.

The `JsonTextFormatterService.ExtractJsonPayload` method at lines 50-81 stays
byte-identical. It already tries `<result>...</result>` regex first (line 62)
and falls through to brace-scan (lines 68-81) on miss. Legacy zip artifacts
containing pre-999.2 Claude `<result>` responses parse via the regex branch;
post-999.2 fenced-only Claude responses parse via the brace-scan branch (D-12).

<!-- Source: 5 ChatGPT counterpart files. Verbatim fenced-block directive per
     family. Executor copies the bolded string into the matching Claude
     variant, stripping the leading 3-space whitespace prefix per D-05
     exception clause from code_context. -->

Family: Analysis
ChatGptAnalysisPromptVariant.cs:198 contains:
  builder.AppendLine("   You MUST return the JSON inside a fenced ```json code block (triple-backtick json). Do not return raw JSON outside a code block.");
Strip the leading "   " (3 spaces inside the string literal). Insertion text for
ClaudeAnalysisPromptVariant.cs:
  builder.AppendLine("You MUST return the JSON inside a fenced ```json code block (triple-backtick json). Do not return raw JSON outside a code block.");

Family: Comparison
ChatGptComparisonPromptVariant.cs:79 contains:
  builder.AppendLine("D. You MUST return the JSON inside a fenced ```json code block (triple-backtick json). Do not return raw JSON outside a code block. The top-level object must be named deck_comparison.");
Strip the leading "D. " numbered-list prefix. Insertion text for
ClaudeComparisonPromptVariant.cs:
  builder.AppendLine("You MUST return the JSON inside a fenced ```json code block (triple-backtick json). Do not return raw JSON outside a code block. The top-level object must be named deck_comparison.");

Family: FollowUp
ChatGptFollowUpPromptVariant.cs:47 contains:
  builder.AppendLine("- Then regenerate the full JSON inside a fenced ```json code block (triple-backtick json) with the top-level object named deck_comparison. Do not return raw JSON outside a code block.");
Strip the leading "- Then " list-item prefix; rephrase to match Claude's flatter
instructional voice. Insertion text for ClaudeFollowUpPromptVariant.cs:
  builder.AppendLine("Regenerate the full JSON inside a fenced ```json code block (triple-backtick json) with the top-level object named deck_comparison. Do not return raw JSON outside a code block.");

Family: MetaGap
ChatGptMetaGapPromptVariant.cs:143 contains:
  builder.AppendLine("- Then return the JSON inside a fenced ```json code block (triple-backtick json) whose top-level object is meta_gap. Do not return raw JSON outside a code block.");
Strip the leading "- Then " list-item prefix. Insertion text for
ClaudeMetaGapPromptVariant.cs:
  builder.AppendLine("Return the JSON inside a fenced ```json code block (triple-backtick json) whose top-level object is meta_gap. Do not return raw JSON outside a code block.");

Family: SetUpgrade
ChatGptSetUpgradePromptVariant.cs:125 contains:
  builder.AppendLine("C. Return a complete set_upgrade_report JSON matching the schema at the end of this prompt. You MUST return the JSON inside a fenced ```json code block (triple-backtick json). Do not return raw JSON outside a code block.");
Strip the leading "C. " numbered-list prefix and drop the redundant
"matching the schema at the end of this prompt" (Claude variants already
have <output_schema> XML-tagged the schema). Insertion text for
ClaudeSetUpgradePromptVariant.cs:
  builder.AppendLine("Return a complete set_upgrade_report JSON. You MUST return the JSON inside a fenced ```json code block (triple-backtick json). Do not return raw JSON outside a code block.");

<!-- Verbatim deletion targets — current text at each Claude variant site. -->

ClaudeAnalysisPromptVariant.cs (lines 230-231) — DELETE both:
  builder.AppendLine(JsonTextFormatterService.ResultWrapInstruction);
  builder.AppendLine("Wrap your final structured output in <result>...</result> tags. Inside <result>, return a single JSON object matching <output_schema>. Place the readable answer prose BEFORE the <result> tag (outside it). Do not put prose inside <result>; do not put JSON outside <result>.");

ClaudeComparisonPromptVariant.cs (lines 90-91) — DELETE both: same two lines.
ClaudeFollowUpPromptVariant.cs (lines 47-48) — DELETE both: same two lines.
ClaudeMetaGapPromptVariant.cs (lines 109-110) — DELETE both: same two lines.
ClaudeSetUpgradePromptVariant.cs (lines 188-189) — DELETE both: same two lines.

In each file the two deleted lines sit IMMEDIATELY before
`builder.AppendLine("</" + "task>");`. The new family-specific AppendLine
inserted in their place lands in that exact same slot (D-06).

<!-- Test impact — D-13 audit hit confirmed during planning. -->

DeckFlow.Web.Tests/ResultContractTests.cs has a shared helper
`AssertContainsResultWrap(string prompt, string platform)` at line 243-253 that
asserts `Assert.Contains("<result>", prompt)` and `Assert.Contains("</result>",
prompt)`. It is called from 5 `[Theory]` methods (lines 96, 123, 149, 165, 187),
each with `[InlineData("ChatGPT")]`, `[InlineData("Claude")]`, and
`[InlineData("Gemini")]` rows. After 999.2 the 5 Claude rows will fail because
Claude variants no longer reference `<result>` anywhere.

Resolution (executor picks the simpler of two options — both are D-13-compliant;
recommend Option A for minimum diff):

Option A (recommended) — remove the 5 `[InlineData("Claude")]` rows from the
5 theories AND add 5 new fact-style assertions (or a new
`[Theory] [InlineData("Claude")]` set) asserting that Claude variants instead
contain "fenced ```json code block". Keeps ChatGPT + Gemini coverage intact.

Option B — change `AssertContainsResultWrap` to branch on platform: for
"ChatGPT"/"Gemini" assert `<result>` substring, for "Claude" assert
"fenced ```json code block" substring. Rename helper to
`AssertContainsJsonOutputDirective`.

The unrelated `Claude_variant_uses_xml_skeleton_with_no_api_role_blocks` fact
at lines 220-239 does NOT assert `<result>` — leave it byte-identical.

The unrelated `BuildAnalysisPrompt_for_Gemini_ends_with_mandate_block` fact at
lines 255-278 asserts the Gemini-only mandate; leave it byte-identical.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Drop &lt;result&gt; wrapper from ClaudeAnalysisPromptVariant</name>
  <files>DeckFlow.Web/Services/PromptBuilders/Analysis/ClaudeAnalysisPromptVariant.cs</files>
  <read_first>
    - DeckFlow.Web/Services/PromptBuilders/Analysis/ClaudeAnalysisPromptVariant.cs (full file — confirm lines 230-231 still match the verbatim two-AppendLine deletion target; confirm lines 180/195/200 sub-object directives are present and NOT touched per D-07)
    - DeckFlow.Web/Services/PromptBuilders/Analysis/ChatGptAnalysisPromptVariant.cs (lines 195-205 — confirm line 198 fenced-block directive text matches the &lt;interfaces&gt; block above)
    - DeckFlow.Web/Services/JsonTextFormatterService.cs (lines 1-90 — confirm `ResultWrapInstruction` const at 13-14 and `ExtractJsonPayload` at 50-81 stay untouched per D-12; this read happens ONCE in Task 1 and is not repeated in tasks 2-5 because the parser is the same shared file)
    - .planning/phases/999.2-claude-result-wrapper-direct-json-output/999.2-CONTEXT.md (full file — locked decisions D-01..D-14, especially D-05 exception clause in `<code_context>` about stripping leading whitespace prefix)
  </read_first>
  <action>
    Per D-01 + D-02 + D-03 + D-06: at ClaudeAnalysisPromptVariant.cs lines 230-231, DELETE both AppendLine calls listed verbatim in the `<interfaces>` block above (the `ResultWrapInstruction` reference and the immediate follow-on "Wrap your final structured output in &lt;result&gt;..." string literal).

    Per D-04 + D-05 + D-06: in the exact same line position (immediately before `builder.AppendLine("</" + "task>");` on what is currently line 232), INSERT the Analysis-family fenced-block directive verbatim from the `<interfaces>` Family: Analysis insertion text (D-05 exception clause: leading 3-space prefix stripped to match Claude's flatter style). The diff per file is exactly: 2 lines deleted, 1 line inserted, net -1 line.

    Per D-07: DO NOT touch ClaudeAnalysisPromptVariant.cs:180, :195, :200 — those are sub-object `deck_profile` fenced-block directives, orthogonal to the main envelope this phase addresses.

    Per CLAUDE.md formatting guardrails: do not auto-format / re-indent surrounding lines, preserve `{ get; init; }` style on any property the editor surfaces, preserve LF line endings (.gitattributes enforces). Touch only the 2-line-delete + 1-line-insert.

    After the edit: run the grep gate locally — `grep -c 'ResultWrapInstruction' DeckFlow.Web/Services/PromptBuilders/Analysis/ClaudeAnalysisPromptVariant.cs` MUST return 0, `grep -c '<result>' DeckFlow.Web/Services/PromptBuilders/Analysis/ClaudeAnalysisPromptVariant.cs` MUST return 0, `grep -c 'fenced .*json code block' DeckFlow.Web/Services/PromptBuilders/Analysis/ClaudeAnalysisPromptVariant.cs` MUST return at least 1.

    Per D-09 + CLAUDE.md "commit per logical change" + "plain default-author commits no Co-Authored-By trailer": commit this file's change as one commit with subject `phase(999.2): drop <result> wrapper from ClaudeAnalysisPromptVariant`. Stage only the one variant file. Do NOT amend / squash with later tasks' commits.
  </action>
  <verify>
    <automated>cd /mnt/c/users/chrislunt/source/personal/deckflow &amp;&amp; grep -c 'ResultWrapInstruction' DeckFlow.Web/Services/PromptBuilders/Analysis/ClaudeAnalysisPromptVariant.cs | grep -qx 0 &amp;&amp; grep -c '&lt;result&gt;' DeckFlow.Web/Services/PromptBuilders/Analysis/ClaudeAnalysisPromptVariant.cs | grep -qx 0 &amp;&amp; grep -c 'fenced .*json code block' DeckFlow.Web/Services/PromptBuilders/Analysis/ClaudeAnalysisPromptVariant.cs | grep -vq '^0$' &amp;&amp; dotnet.exe build DeckFlow.Web/DeckFlow.Web.csproj -c Release --nologo /v:m</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'ResultWrapInstruction' DeckFlow.Web/Services/PromptBuilders/Analysis/ClaudeAnalysisPromptVariant.cs` returns 0
    - `grep -c '<result>' DeckFlow.Web/Services/PromptBuilders/Analysis/ClaudeAnalysisPromptVariant.cs` returns 0
    - `grep -c 'fenced .*json code block' DeckFlow.Web/Services/PromptBuilders/Analysis/ClaudeAnalysisPromptVariant.cs` returns 1 or more
    - `grep -c 'deck_profile' DeckFlow.Web/Services/PromptBuilders/Analysis/ClaudeAnalysisPromptVariant.cs` returns the same count as before the edit (D-07 sub-object directives preserved)
    - `dotnet.exe build DeckFlow.Web/DeckFlow.Web.csproj -c Release` exits 0 with 0 new warnings vs baseline
    - `git log -1 --pretty=%s` matches subject `phase(999.2): drop <result> wrapper from ClaudeAnalysisPromptVariant`
    - `git log -1 --pretty=%an` does NOT equal "Claude" / "Anthropic" (plain default-author per CLAUDE.md)
    - `git log -1 --pretty=%B | grep -c 'Co-Authored-By'` returns 0
    - `git diff HEAD~1 HEAD --stat` shows exactly 1 file changed, net -1 line
  </acceptance_criteria>
  <done>
    ClaudeAnalysisPromptVariant.cs no longer references `ResultWrapInstruction` or `<result>` anywhere; it emits the Analysis-family fenced-block directive in the same slot the deleted lines occupied; one commit landed; build clean.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Drop &lt;result&gt; wrapper from ClaudeComparisonPromptVariant</name>
  <files>DeckFlow.Web/Services/PromptBuilders/Comparison/ClaudeComparisonPromptVariant.cs</files>
  <read_first>
    - DeckFlow.Web/Services/PromptBuilders/Comparison/ClaudeComparisonPromptVariant.cs (full file — confirm lines 90-91 still match the verbatim two-AppendLine deletion target)
    - DeckFlow.Web/Services/PromptBuilders/Comparison/ChatGptComparisonPromptVariant.cs (lines 75-90 — confirm line 79 fenced-block directive matches the `<interfaces>` Family: Comparison source text including the "deck_comparison" top-level-object reference)
    - .planning/phases/999.2-claude-result-wrapper-direct-json-output/999.2-CONTEXT.md (full file — D-01..D-14, especially D-05 exception clause)
  </read_first>
  <action>
    Per D-01 + D-02 + D-03 + D-06: at ClaudeComparisonPromptVariant.cs lines 90-91, DELETE both AppendLine calls (the `ResultWrapInstruction` reference and the immediate follow-on "Wrap your final structured output in &lt;result&gt;..." string literal — same two-line block as Task 1's target, identical text).

    Per D-04 + D-05 + D-06: in the exact same line position (immediately before `builder.AppendLine("</" + "task>");`), INSERT the Comparison-family fenced-block directive verbatim from the `<interfaces>` Family: Comparison insertion text (leading "D. " numbered-list prefix stripped; "The top-level object must be named deck_comparison." retained because it's a Comparison-family-specific schema invariant Claude needs to know).

    Per D-07: DO NOT touch any sub-object directives elsewhere in the file.

    Per CLAUDE.md: no auto-format, LF line endings, touch only the 2-line-delete + 1-line-insert.

    Commit subject: `phase(999.2): drop <result> wrapper from ClaudeComparisonPromptVariant`. Stage only this one variant file.
  </action>
  <verify>
    <automated>cd /mnt/c/users/chrislunt/source/personal/deckflow &amp;&amp; grep -c 'ResultWrapInstruction' DeckFlow.Web/Services/PromptBuilders/Comparison/ClaudeComparisonPromptVariant.cs | grep -qx 0 &amp;&amp; grep -c '&lt;result&gt;' DeckFlow.Web/Services/PromptBuilders/Comparison/ClaudeComparisonPromptVariant.cs | grep -qx 0 &amp;&amp; grep -c 'fenced .*json code block' DeckFlow.Web/Services/PromptBuilders/Comparison/ClaudeComparisonPromptVariant.cs | grep -vq '^0$' &amp;&amp; grep -c 'deck_comparison' DeckFlow.Web/Services/PromptBuilders/Comparison/ClaudeComparisonPromptVariant.cs | grep -vq '^0$' &amp;&amp; dotnet.exe build DeckFlow.Web/DeckFlow.Web.csproj -c Release --nologo /v:m</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'ResultWrapInstruction' DeckFlow.Web/Services/PromptBuilders/Comparison/ClaudeComparisonPromptVariant.cs` returns 0
    - `grep -c '<result>' DeckFlow.Web/Services/PromptBuilders/Comparison/ClaudeComparisonPromptVariant.cs` returns 0
    - `grep -c 'fenced .*json code block' DeckFlow.Web/Services/PromptBuilders/Comparison/ClaudeComparisonPromptVariant.cs` returns 1 or more
    - `grep -c 'deck_comparison' DeckFlow.Web/Services/PromptBuilders/Comparison/ClaudeComparisonPromptVariant.cs` returns 1 or more (Comparison-family schema invariant preserved in the new directive)
    - `dotnet.exe build DeckFlow.Web/DeckFlow.Web.csproj -c Release` exits 0 with 0 new warnings vs baseline
    - `git log -1 --pretty=%s` matches subject `phase(999.2): drop <result> wrapper from ClaudeComparisonPromptVariant`
    - `git log -1 --pretty=%B | grep -c 'Co-Authored-By'` returns 0
    - `git diff HEAD~1 HEAD --stat` shows exactly 1 file changed, net -1 line
  </acceptance_criteria>
  <done>
    ClaudeComparisonPromptVariant.cs no longer references `ResultWrapInstruction` or `<result>` anywhere; emits the Comparison-family fenced-block directive (including the `deck_comparison` top-level-object name); one commit landed; build clean.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Drop &lt;result&gt; wrapper from ClaudeFollowUpPromptVariant</name>
  <files>DeckFlow.Web/Services/PromptBuilders/FollowUp/ClaudeFollowUpPromptVariant.cs</files>
  <read_first>
    - DeckFlow.Web/Services/PromptBuilders/FollowUp/ClaudeFollowUpPromptVariant.cs (full file — confirm lines 47-48 still match the verbatim two-AppendLine deletion target)
    - DeckFlow.Web/Services/PromptBuilders/FollowUp/ChatGptFollowUpPromptVariant.cs (lines 40-55 — confirm line 47 fenced-block directive matches the `<interfaces>` Family: FollowUp source text including "regenerate" verb and "deck_comparison" top-level-object reference)
    - .planning/phases/999.2-claude-result-wrapper-direct-json-output/999.2-CONTEXT.md (full file — D-01..D-14, especially D-05 exception clause)
  </read_first>
  <action>
    Per D-01 + D-02 + D-03 + D-06: at ClaudeFollowUpPromptVariant.cs lines 47-48, DELETE both AppendLine calls (`ResultWrapInstruction` + follow-on `<result>`-wrap directive — same text as Tasks 1-2).

    Per D-04 + D-05 + D-06: in the exact same line position (immediately before `builder.AppendLine("</" + "task>");`), INSERT the FollowUp-family fenced-block directive verbatim from the `<interfaces>` Family: FollowUp insertion text. Note: the ChatGPT source uses the leading "- Then " list-prefix; the Claude insertion uses "Regenerate" verb-first to match Claude's flatter instructional voice (per D-05 exception clause). The "deck_comparison" top-level-object reference is preserved because FollowUp inherits the Comparison schema.

    Per D-07: DO NOT touch any other lines.

    Per CLAUDE.md: no auto-format, LF line endings, touch only the 2-line-delete + 1-line-insert.

    Commit subject: `phase(999.2): drop <result> wrapper from ClaudeFollowUpPromptVariant`. Stage only this one variant file.
  </action>
  <verify>
    <automated>cd /mnt/c/users/chrislunt/source/personal/deckflow &amp;&amp; grep -c 'ResultWrapInstruction' DeckFlow.Web/Services/PromptBuilders/FollowUp/ClaudeFollowUpPromptVariant.cs | grep -qx 0 &amp;&amp; grep -c '&lt;result&gt;' DeckFlow.Web/Services/PromptBuilders/FollowUp/ClaudeFollowUpPromptVariant.cs | grep -qx 0 &amp;&amp; grep -c 'fenced .*json code block' DeckFlow.Web/Services/PromptBuilders/FollowUp/ClaudeFollowUpPromptVariant.cs | grep -vq '^0$' &amp;&amp; dotnet.exe build DeckFlow.Web/DeckFlow.Web.csproj -c Release --nologo /v:m</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'ResultWrapInstruction' DeckFlow.Web/Services/PromptBuilders/FollowUp/ClaudeFollowUpPromptVariant.cs` returns 0
    - `grep -c '<result>' DeckFlow.Web/Services/PromptBuilders/FollowUp/ClaudeFollowUpPromptVariant.cs` returns 0
    - `grep -c 'fenced .*json code block' DeckFlow.Web/Services/PromptBuilders/FollowUp/ClaudeFollowUpPromptVariant.cs` returns 1 or more
    - `grep -c 'deck_comparison' DeckFlow.Web/Services/PromptBuilders/FollowUp/ClaudeFollowUpPromptVariant.cs` returns 1 or more (FollowUp inherits Comparison schema reference)
    - `dotnet.exe build DeckFlow.Web/DeckFlow.Web.csproj -c Release` exits 0 with 0 new warnings vs baseline
    - `git log -1 --pretty=%s` matches subject `phase(999.2): drop <result> wrapper from ClaudeFollowUpPromptVariant`
    - `git log -1 --pretty=%B | grep -c 'Co-Authored-By'` returns 0
    - `git diff HEAD~1 HEAD --stat` shows exactly 1 file changed, net -1 line
  </acceptance_criteria>
  <done>
    ClaudeFollowUpPromptVariant.cs no longer references `ResultWrapInstruction` or `<result>` anywhere; emits the FollowUp-family fenced-block directive (including the `deck_comparison` top-level-object name); one commit landed; build clean.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Drop &lt;result&gt; wrapper from ClaudeMetaGapPromptVariant</name>
  <files>DeckFlow.Web/Services/PromptBuilders/MetaGap/ClaudeMetaGapPromptVariant.cs</files>
  <read_first>
    - DeckFlow.Web/Services/PromptBuilders/MetaGap/ClaudeMetaGapPromptVariant.cs (full file — confirm lines 109-110 still match the verbatim two-AppendLine deletion target; confirm any sub-object directives in the file are NOT touched per D-07)
    - DeckFlow.Web/Services/PromptBuilders/MetaGap/ChatGptMetaGapPromptVariant.cs (lines 135-150 — confirm line 143 fenced-block directive matches the `<interfaces>` Family: MetaGap source text including "meta_gap" top-level-object reference)
    - .planning/phases/999.2-claude-result-wrapper-direct-json-output/999.2-CONTEXT.md (full file — D-01..D-14, especially D-05 exception clause)
  </read_first>
  <action>
    Per D-01 + D-02 + D-03 + D-06: at ClaudeMetaGapPromptVariant.cs lines 109-110, DELETE both AppendLine calls (`ResultWrapInstruction` + follow-on `<result>`-wrap directive — same text as Tasks 1-3).

    Per D-04 + D-05 + D-06: in the exact same line position (immediately before `builder.AppendLine("</" + "task>");`), INSERT the MetaGap-family fenced-block directive verbatim from the `<interfaces>` Family: MetaGap insertion text (leading "- Then " list-prefix stripped; "meta_gap" top-level-object reference retained because it's MetaGap-family-specific).

    Per D-07: DO NOT touch any sub-object directives elsewhere in the file.

    Per CLAUDE.md: no auto-format, LF line endings, touch only the 2-line-delete + 1-line-insert.

    Commit subject: `phase(999.2): drop <result> wrapper from ClaudeMetaGapPromptVariant`. Stage only this one variant file.
  </action>
  <verify>
    <automated>cd /mnt/c/users/chrislunt/source/personal/deckflow &amp;&amp; grep -c 'ResultWrapInstruction' DeckFlow.Web/Services/PromptBuilders/MetaGap/ClaudeMetaGapPromptVariant.cs | grep -qx 0 &amp;&amp; grep -c '&lt;result&gt;' DeckFlow.Web/Services/PromptBuilders/MetaGap/ClaudeMetaGapPromptVariant.cs | grep -qx 0 &amp;&amp; grep -c 'fenced .*json code block' DeckFlow.Web/Services/PromptBuilders/MetaGap/ClaudeMetaGapPromptVariant.cs | grep -vq '^0$' &amp;&amp; grep -c 'meta_gap' DeckFlow.Web/Services/PromptBuilders/MetaGap/ClaudeMetaGapPromptVariant.cs | grep -vq '^0$' &amp;&amp; dotnet.exe build DeckFlow.Web/DeckFlow.Web.csproj -c Release --nologo /v:m</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'ResultWrapInstruction' DeckFlow.Web/Services/PromptBuilders/MetaGap/ClaudeMetaGapPromptVariant.cs` returns 0
    - `grep -c '<result>' DeckFlow.Web/Services/PromptBuilders/MetaGap/ClaudeMetaGapPromptVariant.cs` returns 0
    - `grep -c 'fenced .*json code block' DeckFlow.Web/Services/PromptBuilders/MetaGap/ClaudeMetaGapPromptVariant.cs` returns 1 or more
    - `grep -c 'meta_gap' DeckFlow.Web/Services/PromptBuilders/MetaGap/ClaudeMetaGapPromptVariant.cs` returns 1 or more (MetaGap-family schema invariant preserved)
    - `dotnet.exe build DeckFlow.Web/DeckFlow.Web.csproj -c Release` exits 0 with 0 new warnings vs baseline
    - `git log -1 --pretty=%s` matches subject `phase(999.2): drop <result> wrapper from ClaudeMetaGapPromptVariant`
    - `git log -1 --pretty=%B | grep -c 'Co-Authored-By'` returns 0
    - `git diff HEAD~1 HEAD --stat` shows exactly 1 file changed, net -1 line
  </acceptance_criteria>
  <done>
    ClaudeMetaGapPromptVariant.cs no longer references `ResultWrapInstruction` or `<result>` anywhere; emits the MetaGap-family fenced-block directive (including the `meta_gap` top-level-object name); one commit landed; build clean.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 5: Drop &lt;result&gt; wrapper from ClaudeSetUpgradePromptVariant</name>
  <files>DeckFlow.Web/Services/PromptBuilders/SetUpgrade/ClaudeSetUpgradePromptVariant.cs</files>
  <read_first>
    - DeckFlow.Web/Services/PromptBuilders/SetUpgrade/ClaudeSetUpgradePromptVariant.cs (full file — confirm lines 188-189 still match the verbatim two-AppendLine deletion target)
    - DeckFlow.Web/Services/PromptBuilders/SetUpgrade/ChatGptSetUpgradePromptVariant.cs (lines 120-135 — confirm line 125 fenced-block directive matches the `<interfaces>` Family: SetUpgrade source text including "set_upgrade_report" reference)
    - .planning/phases/999.2-claude-result-wrapper-direct-json-output/999.2-CONTEXT.md (full file — D-01..D-14, especially D-05 exception clause)
  </read_first>
  <action>
    Per D-01 + D-02 + D-03 + D-06: at ClaudeSetUpgradePromptVariant.cs lines 188-189, DELETE both AppendLine calls (`ResultWrapInstruction` + follow-on `<result>`-wrap directive — same text as Tasks 1-4).

    Per D-04 + D-05 + D-06: in the exact same line position (immediately before `builder.AppendLine("</" + "task>");`), INSERT the SetUpgrade-family fenced-block directive verbatim from the `<interfaces>` Family: SetUpgrade insertion text (leading "C. " numbered-list prefix stripped; "matching the schema at the end of this prompt" phrase dropped because Claude variants use `<output_schema>` XML-tagged delivery and this would be redundant in Claude's instructional voice — D-05 exception clause).

    Per D-07: DO NOT touch any other lines.

    Per CLAUDE.md: no auto-format, LF line endings, touch only the 2-line-delete + 1-line-insert.

    Commit subject: `phase(999.2): drop <result> wrapper from ClaudeSetUpgradePromptVariant`. Stage only this one variant file.
  </action>
  <verify>
    <automated>cd /mnt/c/users/chrislunt/source/personal/deckflow &amp;&amp; grep -c 'ResultWrapInstruction' DeckFlow.Web/Services/PromptBuilders/SetUpgrade/ClaudeSetUpgradePromptVariant.cs | grep -qx 0 &amp;&amp; grep -c '&lt;result&gt;' DeckFlow.Web/Services/PromptBuilders/SetUpgrade/ClaudeSetUpgradePromptVariant.cs | grep -qx 0 &amp;&amp; grep -c 'fenced .*json code block' DeckFlow.Web/Services/PromptBuilders/SetUpgrade/ClaudeSetUpgradePromptVariant.cs | grep -vq '^0$' &amp;&amp; grep -c 'set_upgrade_report' DeckFlow.Web/Services/PromptBuilders/SetUpgrade/ClaudeSetUpgradePromptVariant.cs | grep -vq '^0$' &amp;&amp; dotnet.exe build DeckFlow.Web/DeckFlow.Web.csproj -c Release --nologo /v:m</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'ResultWrapInstruction' DeckFlow.Web/Services/PromptBuilders/SetUpgrade/ClaudeSetUpgradePromptVariant.cs` returns 0
    - `grep -c '<result>' DeckFlow.Web/Services/PromptBuilders/SetUpgrade/ClaudeSetUpgradePromptVariant.cs` returns 0
    - `grep -c 'fenced .*json code block' DeckFlow.Web/Services/PromptBuilders/SetUpgrade/ClaudeSetUpgradePromptVariant.cs` returns 1 or more
    - `grep -c 'set_upgrade_report' DeckFlow.Web/Services/PromptBuilders/SetUpgrade/ClaudeSetUpgradePromptVariant.cs` returns 1 or more (SetUpgrade-family schema invariant preserved)
    - `dotnet.exe build DeckFlow.Web/DeckFlow.Web.csproj -c Release` exits 0 with 0 new warnings vs baseline
    - `git log -1 --pretty=%s` matches subject `phase(999.2): drop <result> wrapper from ClaudeSetUpgradePromptVariant`
    - `git log -1 --pretty=%B | grep -c 'Co-Authored-By'` returns 0
    - `git diff HEAD~1 HEAD --stat` shows exactly 1 file changed, net -1 line
  </acceptance_criteria>
  <done>
    ClaudeSetUpgradePromptVariant.cs no longer references `ResultWrapInstruction` or `<result>` anywhere; emits the SetUpgrade-family fenced-block directive (including the `set_upgrade_report` top-level-object name); one commit landed; build clean.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 6: Update ResultContractTests to reflect Claude divergence (D-13)</name>
  <files>DeckFlow.Web.Tests/ResultContractTests.cs</files>
  <read_first>
    - DeckFlow.Web.Tests/ResultContractTests.cs (full file — confirm the 5 `[Theory]` methods at lines ~85, ~105, ~132, ~158, ~174 each have an `[InlineData("Claude")]` row; confirm the shared `AssertContainsResultWrap` helper at lines 243-253 is the only place asserting `<result>` substring; confirm the `Claude_variant_uses_xml_skeleton_with_no_api_role_blocks` fact at lines 220-239 does NOT assert `<result>` — leave it byte-identical; confirm `BuildAnalysisPrompt_for_Gemini_ends_with_mandate_block` at lines 255-278 asserts Gemini-only mandate — leave it byte-identical)
    - DeckFlow.Web/Services/PromptBuilders/Analysis/ClaudeAnalysisPromptVariant.cs (read post-Task-1 state to confirm the new fenced-block substring to assert against)
    - DeckFlow.Web/Services/PromptBuilders/Comparison/ClaudeComparisonPromptVariant.cs (read post-Task-2 state)
    - DeckFlow.Web/Services/PromptBuilders/FollowUp/ClaudeFollowUpPromptVariant.cs (read post-Task-3 state)
    - DeckFlow.Web/Services/PromptBuilders/MetaGap/ClaudeMetaGapPromptVariant.cs (read post-Task-4 state)
    - DeckFlow.Web/Services/PromptBuilders/SetUpgrade/ClaudeSetUpgradePromptVariant.cs (read post-Task-5 state)
    - .planning/phases/999.2-claude-result-wrapper-direct-json-output/999.2-CONTEXT.md (full file — D-13 test impact audit clause)
  </read_first>
  <action>
    Per D-13: the planning-time grep audit hit ResultContractTests.cs — its `AssertContainsResultWrap` helper (lines 243-253) is called from 5 `[Theory]` methods each parameterized with `[InlineData("ChatGPT")]`, `[InlineData("Claude")]`, and `[InlineData("Gemini")]`. After Tasks 1-5 ship, the 5 Claude rows will fail because Claude variants no longer reference `<result>`.

    Choose Option A (recommended per `<interfaces>` block — keeps ChatGPT + Gemini coverage intact with minimum diff):

    For each of the 5 `[Theory]` methods (BuildAnalysisPrompt_..., BuildSetUpgradePrompt_..., BuildComparisonPrompt_..., BuildFollowUpPrompt_..., CedhMetaGap_BuildPrompt_...):
    1. REMOVE the `[InlineData("Claude")]` row from the `[Theory]` attribute list (one line per theory).
    2. ADD a sibling `[Fact]` method (or one parameterized `[Fact]` per family) that:
       - Builds the same prompt via `registry.Build(AiPlatform.Normalize("Claude"), ...)` using the same fixture args as the corresponding theory.
       - Asserts the prompt contains the family-specific fenced-block substring (e.g., for Analysis: `Assert.Contains("fenced ```json code block", prompt);`; for Comparison: also assert `Assert.Contains("deck_comparison", prompt);`; for MetaGap: `Assert.Contains("meta_gap", prompt);`; for SetUpgrade: `Assert.Contains("set_upgrade_report", prompt);`).
       - Asserts the prompt does NOT contain `<result>` and does NOT contain `ResultWrapInstruction`: `Assert.DoesNotContain("<result>", prompt);` and `Assert.DoesNotContain("</result>", prompt);`.

    DO NOT touch:
    - `AssertContainsResultWrap` helper (lines 243-253) — still used by ChatGPT + Gemini theory rows; rename ONLY if Option B is chosen instead.
    - `Claude_variant_uses_xml_skeleton_with_no_api_role_blocks` fact (lines 220-239) — does not assert `<result>`, stays byte-identical.
    - `BuildAnalysisPrompt_for_Gemini_ends_with_mandate_block` fact (lines 255-278) — Gemini-specific, stays byte-identical.

    If executor prefers Option B (branch the helper on platform), document the choice in the commit body. Both options are D-13-compliant; Option A produces smaller per-method diffs and 5 cleanly-named new `[Fact]` methods that document Claude's divergence.

    Per CLAUDE.md: no auto-format, LF line endings, touch only the lines that need touching, preserve `{ get; init; }` style on any property the editor surfaces, preserve raw-string literals (none in this file but the rule still applies if the editor adds any).

    Commit subject: `phase(999.2): update ResultContractTests for Claude direct-JSON output (D-13)`. Stage only this one test file.

    Per D-11 reminder: VSTest unreliable in WSL. After committing, build via `dotnet.exe build DeckFlow.sln -c Release --nologo /v:m` — that's the gate. Do NOT attempt `dotnet test` in WSL. If a Render push-and-watch CI gate exists for this branch, the executor MAY note it in SUMMARY.md but it is not a phase-blocker.
  </action>
  <verify>
    <automated>cd /mnt/c/users/chrislunt/source/personal/deckflow &amp;&amp; grep -c 'InlineData("Claude")' DeckFlow.Web.Tests/ResultContractTests.cs | grep -qx 1 &amp;&amp; grep -c '&lt;result&gt;' DeckFlow.Web.Tests/ResultContractTests.cs | grep -vq '^0$' &amp;&amp; grep -c 'fenced .*json code block' DeckFlow.Web.Tests/ResultContractTests.cs | grep -vq '^0$' &amp;&amp; grep -c 'DoesNotContain.*&lt;result&gt;' DeckFlow.Web.Tests/ResultContractTests.cs | grep -vq '^0$' &amp;&amp; dotnet.exe build DeckFlow.sln -c Release --nologo /v:m</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'InlineData("Claude")' DeckFlow.Web.Tests/ResultContractTests.cs` returns 1 (the unrelated `Claude_variant_uses_xml_skeleton_with_no_api_role_blocks` fact does NOT use InlineData; only the prompt-body-asserting Claude rows are removed — final count is 1 from the unrelated `BuildAnalysisPrompt_for_Gemini_ends_with_mandate_block` fact if it was using InlineData for "Claude"; verify the actual final count post-edit and document in SUMMARY.md if it differs from 1)
    - `grep -c 'fenced .*json code block' DeckFlow.Web.Tests/ResultContractTests.cs` returns 5 or more (5 new Claude-specific Assert.Contains lines, one per family)
    - `grep -c 'DoesNotContain.*<result>' DeckFlow.Web.Tests/ResultContractTests.cs` returns 5 or more (5 new Claude-specific Assert.DoesNotContain lines)
    - `grep -c 'AssertContainsResultWrap' DeckFlow.Web.Tests/ResultContractTests.cs` still returns 6 or more (5 theory call sites + 1 helper definition; ChatGPT + Gemini coverage intact)
    - `grep -c 'Claude_variant_uses_xml_skeleton_with_no_api_role_blocks' DeckFlow.Web.Tests/ResultContractTests.cs` returns 1 (untouched per `<read_first>`)
    - `grep -c 'BuildAnalysisPrompt_for_Gemini_ends_with_mandate_block' DeckFlow.Web.Tests/ResultContractTests.cs` returns 1 (untouched)
    - `dotnet.exe build DeckFlow.sln -c Release` exits 0 with 0 new warnings vs baseline (full solution build, not just DeckFlow.Web project, because this commit touches the test project)
    - `git log -1 --pretty=%s` matches subject `phase(999.2): update ResultContractTests for Claude direct-JSON output (D-13)`
    - `git log -1 --pretty=%B | grep -c 'Co-Authored-By'` returns 0
    - `git diff HEAD~1 HEAD --stat` shows exactly 1 file changed (DeckFlow.Web.Tests/ResultContractTests.cs)
  </acceptance_criteria>
  <done>
    ResultContractTests.cs builds clean; the 5 Claude theory rows that asserted `<result>` in prompt body are replaced by 5 Claude-specific facts asserting fenced-block substrings + `Assert.DoesNotContain("<result>", ...)`; ChatGPT + Gemini coverage is intact via the unchanged `AssertContainsResultWrap` helper; one commit landed.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 7: Manual UAT — T3 + T7 + ad-hoc Claude / deck-comparison (D-11)</name>
  <files>None — manual browser-driven verification.</files>
  <read_first>
    - .planning/phases/999.2-claude-result-wrapper-direct-json-output/999.2-CONTEXT.md (full file — D-11 acceptance gate clause + D-12 legacy-zip parser fall-through claim)
    - README.md (full file — D-deferred README brand sweep; grep for "result>" / "wrapper" tied to Claude; expected zero hits but the sweep is mandatory per CLAUDE.md "README updated when behavior changes")
  </read_first>
  <what-built>
    All 5 Claude prompt variants now emit a fenced ```json directive instead of the `<result>` wrapper instruction (5 commits from Tasks 1-5). The test contract has been updated to match (commit from Task 6). ChatGPT + Gemini variants are unchanged. The `JsonTextFormatterService` parser is unchanged — it still tries `<result>` regex first and falls through to brace-scan, so legacy zip artifacts (containing pre-999.2 Claude responses with `<result>` wrappers) still parse cleanly.
  </what-built>
  <how-to-verify>
    ASK USER TO START THE DEV SERVER (per `feedback_user_starts_server` memory entry — never auto-launch DeckFlow web). Once running on http://localhost:5173:

    **End-to-end phase grep gate (run BEFORE manual UAT, fail-fast):**
    1. `cd /mnt/c/users/chrislunt/source/personal/deckflow`
    2. `grep -rn 'ResultWrapInstruction' DeckFlow.Web/Services/PromptBuilders/*/Claude*.cs` → expect ZERO hits.
    3. `grep -rn '<result>' DeckFlow.Web/Services/PromptBuilders/*/Claude*.cs` → expect ZERO hits.
    4. `grep -rn 'ResultWrapInstruction' DeckFlow.Web/Services/PromptBuilders/` → expect exactly 10 hits (5 ChatGPT + 5 Gemini variants, unchanged).
    5. `grep -rn 'ResultWrapInstruction' DeckFlow.Web/Services/PromptBuilders/*/Gemini*.cs` → expect 5 hits.
    6. `grep -rn 'GeminiJsonMandate' DeckFlow.Web/Services/PromptBuilders/*/Gemini*.cs` → expect 5 hits.
    7. `grep -rn 'fenced .*json code block' DeckFlow.Web/Services/PromptBuilders/*/Claude*.cs` → expect at least 5 hits (one per Claude variant).
    8. `dotnet.exe build DeckFlow.sln -c Release --nologo /v:m` → expect exit 0, 0 new warnings vs Phase 999.1 baseline.
    9. `git log --oneline | head -6` → confirm 6 commits in order: ClaudeAnalysisPromptVariant, ClaudeComparisonPromptVariant, ClaudeFollowUpPromptVariant, ClaudeMetaGapPromptVariant, ClaudeSetUpgradePromptVariant, ResultContractTests-update. Plain default-author (no Co-Authored-By trailer).

    **README brand sweep (D-deferred item in CONTEXT.md `<deferred>`):**
    10. `grep -ni 'result>\|wrapper' README.md` → review hits. Expected outcome: zero hits tied to Claude prompt behavior (the two existing hits at lines 146 + 606 are unrelated: a bash-script wrapper + an HTTP wrapper docs reference). If new Claude-tied hits exist (none expected), update them in a 7th commit `phase(999.2): README brand sweep for Claude direct-JSON output`. If zero hits, log "README sweep clean — no Claude-tied wrapper references" in SUMMARY.md and skip the 7th commit.

    **Manual T3 — Claude / deck-analysis (covers ClaudeAnalysisPromptVariant + ClaudeSetUpgradePromptVariant via the `requiresFullDecklists` toggle + ClaudeFollowUpPromptVariant via the "Ask Claude a follow-up question" panel):**
    11. Navigate to http://localhost:5173/deck-analysis (Step 1 — paste deck).
    12. Step 2: select **Claude** in the `_AiSelector` partial.
    13. Step 3: paste a real deck (or use whatever the user has bookmarked for T3 — Atraxa or similar).
    14. Step 4: click "Generate Packet" and download the zip.
    15. Open the generated `*.txt` packet; confirm:
        - The prompt body contains "fenced ```json code block" (search the file).
        - The prompt body does NOT contain `<result>` anywhere.
        - The prompt body does NOT contain "Wrap your final structured output in".
    16. Paste the prompt body into claude.ai (or whatever Claude UI the user prefers for UAT).
    17. Observe Claude's response: confirm there is ONE fenced ```json block in the visible chat, NO `<result>` tags anywhere.
    18. Copy Claude's full response, paste into DeckFlow's "Paste response" textarea, click submit.
    19. Confirm the parser extracts JSON cleanly and the downstream UI renders `deck_profile` fields (game_plan, speed, strengths, weaknesses, deck_needs, weak_slots).
    20. (Sub-coverage for SetUpgrade) If T3 deck has `requiresFullDecklists` toggled, the prompt body will also include the set-upgrade variant — re-run the Claude paste + verify the same clean-output property on the set-upgrade JSON.
    21. (Sub-coverage for FollowUp) Click "Ask Claude a follow-up question" if the page exposes it; verify the follow-up prompt body also passes the fenced-only / no-`<result>` check.

    **Manual T7 — Claude / cEDH meta-gap (covers ClaudeMetaGapPromptVariant):**
    22. Navigate to http://localhost:5173/cedh-meta-gap.
    23. Step 2: select **Claude**.
    24. Fill in commander + filters (use whatever combination the user has cached as a working T7 fixture; "Atraxa" + default filters usually returns hits).
    25. Generate packet, paste into Claude, verify the same three properties: prompt has "fenced ```json code block", no `<result>` tags in prompt or response, response pastes back and parses cleanly into the meta_gap JSON.

    **Manual ad-hoc — Claude / deck-comparison (covers ClaudeComparisonPromptVariant):**
    26. Navigate to http://localhost:5173/deck-comparison.
    27. Step 2: select **Claude**.
    28. Paste two real decks (any pair the user has cached).
    29. Generate packet, paste into Claude, verify: prompt has "fenced ```json code block" + "deck_comparison", no `<result>` tags in prompt or response, response pastes back and parses cleanly into deck_comparison JSON.

    **Legacy zip round-trip (D-12 regression check):**
    30. If the user has any pre-999.2 zip artifacts on disk (downloaded during Phase 13 UAT T4 or earlier), drag one into the "Upload previous packet" flow on any of the three pages. Confirm the legacy artifact still loads and round-trips correctly — this exercises the `ExtractJsonPayload` `<result>`-regex branch on legacy content. If no legacy artifacts available, skip step 30 and note in SUMMARY.md (D-12 is verified by code inspection during planning, not by mandatory regression artifact).

    **ChatGPT + Gemini non-regression sanity check (D-02 invariant):**
    31. On any one of the three pages, re-run a packet generation with **ChatGPT** selected. Confirm the prompt body STILL contains `ResultWrapInstruction` const output (substring "Wrap the entire JSON response in <result>...</result>") — Phase 999.2 must NOT have touched ChatGPT variants.
    32. If `DECKFLOW_GEMINI_ENABLED` is set, repeat with **Gemini**. Confirm the prompt body STILL ends with the `GeminiJsonMandate` block ("MANDATORY — DO NOT SKIP" + ending "Nothing else after </result>.") — Phase 999.2 must NOT have touched Gemini variants.
  </how-to-verify>
  <resume-signal>
    Type `approved` if all 32 checks pass.
    Type `failed: <step number> <description>` to report a specific failure (executor will halt and surface the failure in SUMMARY.md; per-commit revert is available via `git revert <commit-sha>` per D-09 + D-14).
    Type `partial: <step numbers passed>` if some checks pass but legacy zip (step 30) or Gemini (step 32) coverage is unavailable — note the gap in SUMMARY.md and proceed.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none — no new attack surface) | Phase 999.2 modifies only the text content of 5 prompt-builder string literals and 1 xUnit test file. No new I/O, no new authentication path, no new persistence write, no new user-input parsing surface, no new HTTP egress, no new env-var read, no DI lifetime change, no new serializer entry, no new file-system path. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-999.2-01 | (none) | Prompt text literals in `Services/PromptBuilders/*/Claude*PromptVariant.cs` | accept | No threat — prompt text is stored as compile-time string constants assembled into a `StringBuilder` and returned by `BuildPrompt(...)`. Output is rendered into a Razor view and offered as a downloadable artifact; no execution sink, no eval, no template injection vector. The change replaces one literal block with another literal block. |
| T-999.2-02 | Tampering | `JsonTextFormatterService.ExtractJsonPayload` (unchanged input handling boundary at `DeckFlow.Web/Services/JsonTextFormatterService.cs:50-81`) | accept | Parser is byte-identical to its Phase 10 / Phase 15 state. The `<result>`-regex-first-with-brace-scan-fallback contract continues to handle both shapes: legacy zip artifacts containing pre-999.2 Claude `<result>` responses parse via the regex branch (D-12); post-999.2 fenced-only Claude responses parse via the brace-scan branch. No new untrusted-input path. |
| T-999.2-03 | Information Disclosure | Generated artifact zip filenames + body | accept | No PII / no secrets / no env-var values appear in prompt body. The deck data the user pastes is the same data the prompt has always been built around. Filename sanitizer in `PacketArtifactStore` is untouched (Phase 12 D-15 sweep covered that surface). |
| T-999.2-SC | Tampering | Package installs / dependency surface | accept | Phase 999.2 introduces ZERO new NuGet / npm / external package dependencies. No `<PackageReference>` adds, no `package.json` edits, no `Dockerfile` changes. Package legitimacy gate is not triggered. |

ASVS L1 baseline maintained. Threat surface delta = 0 vs Phase 999.1 HEAD.
</threat_model>

<verification>
## Phase-Wide Acceptance Gate (D-11 — must hold at end of phase)

Run from repo root after Task 7 manual UAT signs off:

```bash
# Grep invariants (CONTEXT.md D-11)
grep -rn 'ResultWrapInstruction' DeckFlow.Web/Services/PromptBuilders/*/Claude*.cs
# Expect: ZERO hits.

grep -rn '<result>' DeckFlow.Web/Services/PromptBuilders/*/Claude*.cs
# Expect: ZERO hits.

grep -rn 'ResultWrapInstruction' DeckFlow.Web/Services/PromptBuilders/
# Expect: exactly 10 hits (5 ChatGPT + 5 Gemini variants, unchanged).

grep -rn 'ResultWrapInstruction' DeckFlow.Web/Services/PromptBuilders/*/Gemini*.cs
# Expect: 5 hits.

grep -rn 'GeminiJsonMandate' DeckFlow.Web/Services/PromptBuilders/*/Gemini*.cs
# Expect: 5 hits.

grep -rn 'fenced .*json code block' DeckFlow.Web/Services/PromptBuilders/*/Claude*.cs
# Expect: at least 5 hits (1 per Claude variant from this phase).

# Parser invariant (D-12)
grep -c 'ResultWrapInstruction' DeckFlow.Web/Services/JsonTextFormatterService.cs
# Expect: 1 hit (const declaration, unchanged).

# Build clean (D-11)
dotnet.exe build DeckFlow.sln -c Release --nologo /v:m
# Expect: exit 0, 0 new warnings vs Phase 999.1 baseline.

# Commit shape (D-09 + CLAUDE.md plain-author rule)
git log --oneline -6
# Expect 6 commits in order: ClaudeAnalysisPromptVariant, ClaudeComparisonPromptVariant, ClaudeFollowUpPromptVariant, ClaudeMetaGapPromptVariant, ClaudeSetUpgradePromptVariant, ResultContractTests-update.

git log -6 --pretty=%B | grep -c 'Co-Authored-By'
# Expect: 0.

# Test impact audit re-verification (D-13)
grep -c 'AssertContainsResultWrap' DeckFlow.Web.Tests/ResultContractTests.cs
# Expect: 6 or more (5 ChatGPT+Gemini theory call sites + 1 helper definition).

grep -c 'DoesNotContain.*<result>' DeckFlow.Web.Tests/ResultContractTests.cs
# Expect: 5 or more (Claude-specific assertions added in Task 6).
```

Manual UAT sign-off from Task 7 captured in SUMMARY.md as a `## UAT Results` section quoting the user's `approved` signal (or `partial:`/`failed:` text verbatim if encountered).
</verification>

<success_criteria>
- (D-01) Zero `ResultWrapInstruction` references in any `DeckFlow.Web/Services/PromptBuilders/*/Claude*PromptVariant.cs`; zero `<result>` substrings in any Claude variant.
- (D-02) All 5 Claude prompt-variant families edited (Analysis, Comparison, FollowUp, MetaGap, SetUpgrade) — none deferred to a follow-on phase.
- (D-03) The follow-on "Wrap your final structured output in `<result>...</result>` tags..." AppendLine deleted in every Claude variant alongside the `ResultWrapInstruction` line.
- (D-04 + D-05) Each Claude variant now emits a fenced ```json code-block directive verbatim-copied from its ChatGPT family counterpart, with the leading numbered-list / bullet whitespace prefix stripped to match Claude's flatter instructional voice.
- (D-06) The new directive lands in the exact same line slot the deleted lines occupied — immediately before `builder.AppendLine("</" + "task>");`.
- (D-07) Per-version `deck_profile` and similar sub-object fenced-block directives in Claude variants are untouched.
- (D-08) No new property on `AiPlatform` record; no new `IResultWrapPolicy` interface; no new helper method on `JsonTextFormatterService`; Phase 15 D-01 data-only-record invariant preserved.
- (D-09) 5 commits land, one per Claude variant, with the suggested subjects from CONTEXT.md `<decisions>` Commit decomposition section; plus 1 additional commit for the Task 6 test update (D-13).
- (D-10) Single plan `999.2-01`, single sequential worktree execution, no parallel waves.
- (D-11) Grep gate passes (zero hits in Claude variants; 10 hits across ChatGPT + Gemini variants; 5 hits each for Gemini `ResultWrapInstruction` and `GeminiJsonMandate`); `dotnet.exe build DeckFlow.sln -c Release` exits 0 with 0 new warnings; manual UAT T3 + T7 + ad-hoc Claude / deck-comparison signed off in Task 7.
- (D-12) `JsonTextFormatterService.cs` byte-identical to pre-phase state; legacy zip artifacts with pre-999.2 Claude `<result>` responses still parse via the regex branch.
- (D-13) `ResultContractTests.cs` updated: 5 `[InlineData("Claude")]` rows removed from the prompt-body-asserting theories; 5 new Claude-specific facts assert the fenced-block substring AND assert `<result>` is absent; ChatGPT + Gemini coverage intact via the unchanged `AssertContainsResultWrap` helper.
- (D-14) Rollback path documented in SUMMARY.md: `git revert <commit-sha>` for any single variant if a future model regression forces partial rollback.
- (Discretion) Commit order: alphabetical by family (Analysis → Comparison → FollowUp → MetaGap → SetUpgrade) for predictable git-blame; test-update commit last.
- (CLAUDE.md) README sweep run in Task 7; if no Claude-tied "result>" / "wrapper" hits, documented in SUMMARY.md and no 7th commit added.
</success_criteria>

<output>
Create `.planning/phases/999.2-claude-result-wrapper-direct-json-output/999.2-01-SUMMARY.md` when done.

The SUMMARY must include:
- The 6 commit SHAs in execution order
- The phase-wide grep-gate output from `<verification>` proving all invariants hold
- The Task 7 manual UAT user `approved` / `partial:` / `failed:` text verbatim
- A note on the README sweep outcome (Claude-tied hits found vs zero)
- A note on the legacy zip round-trip outcome from Task 7 step 30 (verified vs skipped-no-artifact-available)
- A note confirming `ResultContractTests.cs` builds clean and the unchanged `Claude_variant_uses_xml_skeleton_with_no_api_role_blocks` + `BuildAnalysisPrompt_for_Gemini_ends_with_mandate_block` facts are still present
</output>
