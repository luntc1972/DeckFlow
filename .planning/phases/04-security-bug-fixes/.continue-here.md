---
context: phase
phase: 04-security-bug-fixes
task: 3
total_tasks: 3
status: paused_awaiting_live_uat
last_updated: 2026-05-01T19:45:27.217Z
---

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| WSL/MSBuild parallel solution build silently fails | `dotnet build DeckFlow.sln` (default parallel path) returned non-zero with no compiler errors emitted in this WSL environment during phase 04 execution. Serialized path always succeeded. | advisory | When build looks broken with empty output, retry once with `dotnet build DeckFlow.sln -m:1 -p:BuildInParallel=false` before assuming a code defect. Same workaround applied across all 8 build runs in phase 04 — environmental, not code. |

<current_state>
Phase 04 (security-bug-fixes) implementation complete and pushed to origin/main (9 commits, bcc1693..2ce8052). Render auto-deploy was triggered ~13:12 MDT 2026-05-01. Static verification PASS via gsd-verifier (13/13 must-haves). 5 live UAT items pending in 04-HUMAN-UAT.md — they require Render deploy to complete + human walk through the curl loop and browser tests.
</current_state>

<completed_work>

Completed:

- Task 04-01-T1: AdminBruteForceTracker singleton + DerivePeerIpKey helper (commit 7b3c1d6)
- Task 04-01-T2: BasicAuthMiddleware throttle wiring — 429 + Retry-After + RecordFailure inside Challenge (commit 50849e9)
- Task 04-01-T3: Admin throttle tests (7 xunit) + README operations blurb (commits 7e08d8c + aed9ead)
- 04-01-SUMMARY.md (commit de0e6e7)
- Task 04-02-T1: ScryfallTagger ResolveCardPrintingAsync iterate-printings + IMemoryCache + Program.cs factory closure (commit 06ea1a7)
- Task 04-02-T2: Tagger printings test coverage — CreateService 5-arg + 5 new test cases (commit 4585269)
- 04-02-SUMMARY.md + 04-HUMAN-UAT.md scaffold (commit 818dc05)
- 04-VERIFICATION.md (status: human_needed; 13/13 static must-haves PASS) (commit 2ce8052)
- `git push origin main` bcc1693..2ce8052
- ROADMAP.md plan progress flipped to Complete for both plans
- STATE.md current focus → Phase 04
</completed_work>

<remaining_work>

Pending — all live UAT (gated on Render auto-deploy completion ~17min from push):

1. Sniff Render deploy completion: `curl -sS -o /dev/null -w '%{http_code}\n' https://www.deckflow.gg/` must return 200
2. UAT 1 — BUG-02 11-attempt curl loop (10×401 + 1×429 + non-empty Retry-After)
3. UAT 2 — BUG-02 15-min window-reset re-curl (returns 401, not stuck on 429)
4. UAT 3 — BUG-01 browser walk: `/suggest-categories` mode=ScryfallTagger Sol Ring → non-empty real oracle tags
5. UAT 4 — BUG-01 repeat with second cEDH staple (Counterspell / Mana Crypt / Cyclonic Rift)
6. UAT 5 — SC #3 regression matrix: `/sync` deck reconcile + `/chatgpt-packets` artifact + `/suggest-categories` mode=All
7. Record evidence in 04-HUMAN-UAT.md, flip `status: partial` → `status: resolved` if all PASS
8. Mark phase complete via `gsd-sdk query phase.complete 04` (advances STATE/ROADMAP)
</remaining_work>

<decisions_made>

- Route coding through Codex MCP per global CLAUDE.md: orchestrator dispatched each plan task to `mcp__codex__codex`/`codex-reply` with mandatory twice-QA, committed locally.
- Sequential within Wave 1 because Plan 04-01 and Plan 04-02 both touch DeckFlow.Web/Program.cs (intra-wave files_modified overlap). Parallelization not safe.
- No `order=` parameter on Scryfall search per RESEARCH.md correction to D-10 — default ordering surfaces Tagger-indexed mainline-set printings; explicit `released-desc` surfaces Secret Lair drops first which are unindexed.
- Skipped code-review and regression-gate steps from execute-phase workflow — both advisory; `dotnet build` clean is the WSL gate per project CLAUDE.md (VSTest unreliable in WSL).
- Combined HUMAN-UAT.md for BUG-01 + BUG-02 + SC#3 (per Plan 04-02 Task 3 explicit instruction to append, not overwrite).
- README blurb added for BUG-02 (admin throttle behavior change visible to operators); no README change for BUG-01 (Tagger lookups already nominally "best effort" — internal fix only).
</decisions_made>

<blockers>
- Render auto-deploy in progress (external; resolves itself ~17min after push) — workaround: wait + sniff
- 5 live UAT items require human action (curl + browser walks) — workaround: user runs and pastes evidence into 04-HUMAN-UAT.md
</blockers>

## Required Reading (in order)

1. `.planning/phases/04-security-bug-fixes/04-HUMAN-UAT.md` — full UAT procedure for all 5 pending items, exact curl recipes, expected results, evidence format
2. `.planning/phases/04-security-bug-fixes/04-VERIFICATION.md` — gsd-verifier static-PASS report + the human_verification routing
3. `.planning/phases/04-security-bug-fixes/04-01-SUMMARY.md` and `04-02-SUMMARY.md` — what was built, exact commit SHAs, file paths
4. `.planning/PROJECT.md` — core value (ChatGPT-paste-ready output), constraints (no Co-Authored-By, README updated when behavior changes, plain-author commits)
5. `CLAUDE.md` (project + global) — coding-task routing through Codex MCP; gpt-5.4-mini default; mandatory twice-QA per Codex prompt

## Critical Anti-Patterns (do NOT repeat these)

- ANTI-PATTERN: assuming `dotnet build DeckFlow.sln` parallel path failure means a code defect → use `-m:1 -p:BuildInParallel=false` first; only investigate code if serialized path also fails. Discovered 8 times in this phase.

## Infrastructure State

- Render: auto-deploy triggered by push to main; expected complete ~13:29 MDT 2026-05-01 (push was 13:12 MDT)
- Origin: `https://github.com/luntc1972/DeckFlow.git` is up to date with local main (HEAD 2ce8052)
- Local working tree: clean except `.claude/` (untracked Claude Code config) and `tasks/UI-REVIEW.md` (carryover from prior session, unrelated to phase 04)
- Codex MCP thread: 019de4f1-fd64-7550-a400-f30882746854 (stale on resume — start fresh threads per task)

<context>
Phase 04 was the smallest phase in milestone v1.0 — 2 bug fixes (BUG-01 Tagger printings + BUG-02 admin brute-force throttle). Both plans were planned and discussed cleanly; execution went through Codex MCP for every code edit per global CLAUDE.md routing rule. The hard part was just orchestration: ensuring per-task QA discipline, atomic commits, sequential execution because of Program.cs overlap, and that the existing Phase 03 TD-04 Path B-rawpeer pattern (Connection.RemoteIpAddress only, never X-Forwarded-For) was reused rather than reinvented for the admin partition key.

The verifier's verdict was human_needed — 13/13 static must-haves PASS, only live walk against deckflow.gg remains. That walk is the hand-off point: user runs the curl loop + browser tests after Render finishes deploying, pastes evidence into 04-HUMAN-UAT.md, then phase closes.

Resuming agent: do NOT re-execute any plan tasks. The code is on main. Your job is to (a) confirm deploy, (b) drive the 5 UAT items, (c) record evidence, (d) close the phase.
</context>

<next_action>
Run `curl -sS -o /dev/null -w '%{http_code}\n' https://www.deckflow.gg/` to confirm Render deploy completed (expect 200). If still mid-deploy, wait or retry. Once green, proceed sequentially through 04-HUMAN-UAT.md tests 1 → 5, recording PASS evidence after each.
</next_action>
