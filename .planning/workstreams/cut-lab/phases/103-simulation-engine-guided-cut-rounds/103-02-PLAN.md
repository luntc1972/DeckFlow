---
phase: 103-simulation-engine-guided-cut-rounds
plan: 02
type: execute
wave: 2
depends_on: ["103-01"]
files_modified:
  - DeckFlow.Web/Models/CutLab/CutLabState.cs
  - DeckFlow.Web/Services/CutLab/CutLabStateSerializer.cs
  - DeckFlow.Web/Services/CutLab/CutLabWorkingList.cs
  - DeckFlow.Web.Tests/CutLabStateSerializerTests.cs
  - DeckFlow.Web.Tests/CutLabWorkingListTests.cs
autonomous: true
requirements: [CUT-03, SIM-02]
must_haves:
  truths:
    - "HIGH-1: Pool is IMMUTABLE for the whole session — accepting a cut NEVER removes an entry from CutLabState.Pool, so every card keeps its name/quantity/type-line/lock/package metadata. The WORKING LIST is DERIVED (Pool minus cards whose net decision is Accepted, quantity-aware) and is never stored."
    - "HIGH-1/D-16: because Pool is never mutated, restoring a card = removing its decision records only; the card re-enters the derived working list with ALL its original metadata intact (no metadata loss)."
    - "D-16: every accepted cut is recorded as a compact, individually-restorable decision record in CutLabState (card + decision kind + round + ordinal), surviving the hidden-field round-trip."
    - "D-12: the original-pool baseline metric snapshot is stored inside CutLabState as a compact numeric snapshot, survives restarts, and is never a full ManabaseReport."
    - "D-15: deferred and rejected decisions persist distinctly so the loop-around pass (Plan 103-04/07) can re-propose them in the right order."
    - "Pitfall 5: adding Decisions + BaselineSnapshot never blows the 262_144-byte MaxUploadBytes cap for a 150-card pool carrying MULTIPLE decision records per card; the Decisions list is bounded on deserialize. (Pool never shrinks — sizing is unchanged by the immutability rule since Pool was always full-size.)"
    - "Old (pre-103) CutLabStateJson blobs still deserialize cleanly (empty-default initializers)."
  artifacts:
    - path: "DeckFlow.Web/Models/CutLab/CutLabState.cs"
      provides: "Decisions history + BaselineSnapshot fields"
      contains: "BaselineSnapshot"
    - path: "DeckFlow.Web/Services/CutLab/CutLabWorkingList.cs"
      provides: "pure Pool+Decisions → derived working-list helper (HIGH-1 single source of truth)"
      contains: "Derive"
    - path: "DeckFlow.Web/Services/CutLab/CutLabStateSerializer.cs"
      provides: "bounded/clamped deserialize for the new fields"
      contains: "MaxDecisions"
  key_links:
    - from: "DeckFlow.Web/Models/CutLab/CutLabState.cs"
      to: "DeckFlow.Web/Models/CutLab/CutLabMetrics.cs"
      via: "BaselineSnapshot typed as CutLabMetricSnapshot (Plan 103-01)"
      pattern: "CutLabMetricSnapshot"
    - from: "DeckFlow.Web/Services/CutLab/CutLabWorkingList.cs"
      to: "CutLabState.Pool / CutLabState.Decisions"
      via: "Derive(pool, decisions) = Pool minus net-Accepted cards; every other consumer (round engine, sim, endpoint, view) calls this one helper"
      pattern: "Derive"
    - from: "DeckFlow.Web/Services/CutLab/CutLabStateSerializer.cs"
      to: "CutLabState.Decisions"
      via: "Take(MaxDecisions) clamp on Deserialize, mirroring the existing Take(MaxPackages)"
      pattern: "Take\\(Max"
---

<objective>
Extend the persisted Cut Lab working-session envelope to carry the decision history (D-16 restore-any) and the original-pool baseline metric snapshot (D-12 SIM-02), establish the HIGH-1 rule that **Pool is immutable and the working list is a derived projection**, and provide the single pure `CutLabWorkingList.Derive` helper every downstream consumer uses — all with the same defensive bounding the existing serializer already applies to packages and floors, without breaching the 256KB cap even when cards accrue multiple decision records across defer→reject→accept loop-around passes.

Purpose: The decision loop (Plan 103-07) and the before/after view (Plan 103-08) both read these fields; they must round-trip through the hidden `CutLabStateJson` field for the no-JS fallback and survive restarts. Because Pool is never mutated, restore is lossless (HIGH-1).
Output: Extended `CutLabState`, the `CutLabWorkingList` derivation helper, hardened serializer, round-trip + cap + derivation tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/cut-lab/phases/103-simulation-engine-guided-cut-rounds/103-PATTERNS.md
@.planning/workstreams/cut-lab/phases/103-simulation-engine-guided-cut-rounds/103-RESEARCH.md

<interfaces>
From DeckFlow.Web/Models/CutLab/CutLabMetrics.cs (Plan 103-01):
- record CutLabMetricSnapshot { IReadOnlyList<CutLabMetricValue> Metrics { get; init; } = []; }

Existing state shape (DeckFlow.Web/Models/CutLab/CutLabState.cs):
- CutLabState { string Commander; IReadOnlyList<CutLabPoolCard> Pool; IReadOnlyList<CutLabPackage> Packages; IReadOnlyList<CutLabRoleFloor> RoleFloors; CutLabIntent Intent }
- CutLabPoolCard { string Name; int Quantity; string TypeLine; bool IsCommander; bool IsLocked; string? PackageId }

Existing serializer invariants (DeckFlow.Web/Services/CutLab/CutLabStateSerializer.cs):
- const int MaxUploadBytes = 262_144;  const int MaxPackages = 50;
- Deserialize already: size-guards, deserializes, Take(MaxPackages), then CutLabFloorRules.ClampFloors(CutLabLockRules.EnforceCommanderLock(state)).
- Serialize throws InvalidOperationException("The Cut Lab working session is too large to save.") above the cap.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Decisions + BaselineSnapshot to CutLabState (Pool stays immutable)</name>
  <read_first>
    - DeckFlow.Web/Models/CutLab/CutLabState.cs (the record + the RoleFloors extension precedent, XML doc "recomputed per POST, never persisted" framing)
    - DeckFlow.Web/Models/CutLab/CutLabMetrics.cs (CutLabMetricSnapshot shape from Plan 103-01)
    - .planning/workstreams/cut-lab/phases/103-simulation-engine-guided-cut-rounds/103-CONTEXT.md (D-12, D-15, D-16 wording)
    - .planning/workstreams/cut-lab/phases/103-simulation-engine-guided-cut-rounds/103-RESEARCH.md (Pitfall 5 sizing guard: ~40 bytes/decision × 150 ≈ 6KB)
  </read_first>
  <action>
    In `DeckFlow.Web/Models/CutLab/CutLabState.cs`:
    (a) Add a `CutLabDecisionKind` enum (`Accepted`, `Rejected`, `Deferred`) — the three states from D-02/D-15.
    (b) Add a `CutLabDecision` sealed record with COMPACT fields only (Pitfall 5): `string CardName`, `CutLabDecisionKind Kind`, `string Round` (round key/name the card was decided in, for the "cut in {round name}" copy and D-15 loop ordering), `int Ordinal` (monotonic acceptance/decision order for D-16 order-independent restore + most-recent-first display). Do NOT store card data, delta snapshots, or `ManabaseReport` anywhere in this record. Note: a single card may carry MULTIPLE `CutLabDecision` records over loop-around passes (defer, then reject, then accept) — the list is a decision LOG keyed by (CardName, Ordinal), not a per-card map.
    (c) Add to `CutLabState`: `public IReadOnlyList<CutLabDecision> Decisions { get; init; } = [];` and `public CutLabMetricSnapshot? BaselineSnapshot { get; init; }` — both with empty/null defaults so pre-103 JSON deserializes cleanly. XML-doc each field. On `Pool`, ADD to its existing XML-doc summary a one-line `// Why:`-style note (HIGH-1): "Pool is immutable for the session — cuts are recorded in Decisions, never by removing a Pool entry; the working list is derived via CutLabWorkingList.Derive so restore is lossless." On `Decisions`, note it is bounded on deserialize; on `BaselineSnapshot`, note it is computed once at pool intake (D-12), mirroring the RoleFloors "recomputed per POST, never persisted" style.
    Do NOT add any Pool-mutation helper — the immutability rule is enforced by convention + the derived-working-list helper in Task 3.
  </action>
  <verify>
    <automated>"/mnt/c/Program Files/dotnet/dotnet.exe" build DeckFlow.Web/DeckFlow.Web.csproj -clp:ErrorsOnly</automated>
  </verify>
  <acceptance_criteria>
    - `CutLabState` has `Decisions` (empty-default) and `BaselineSnapshot` (nullable) properties.
    - `CutLabDecision` contains only `CardName`, `Kind`, `Round`, `Ordinal` — grep confirms no `ManabaseReport`/`CutLabMetricDelta`/card-data field on the record.
    - `CutLabDecisionKind` has `Accepted`, `Rejected`, `Deferred`.
    - The `Pool` XML-doc carries the immutability note (grep `immutable` near Pool).
    - Build clean, no new warnings.
  </acceptance_criteria>
  <done>State envelope carries decision history + baseline snapshot; Pool is documented immutable.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Bound + round-trip the new fields in the serializer</name>
  <read_first>
    - DeckFlow.Web/Services/CutLab/CutLabStateSerializer.cs (MaxUploadBytes, MaxPackages, the Deserialize Take/clamp chain, Serialize cap-throw)
    - DeckFlow.Web.Tests/CutLabStateSerializerTests.cs (existing round-trip/clamp test patterns to extend)
    - DeckFlow.Web/Models/CutLab/CutLabState.cs (the fields added in Task 1)
  </read_first>
  <behavior>
    - Test 1: Serialize→Deserialize a state with Accepted/Rejected/Deferred decisions + a BaselineSnapshot; the decisions and snapshot survive byte-for-byte in value, and Pool is unchanged (immutability preserved through the round-trip).
    - Test 2: A deserialized state with more than `MaxDecisions` decisions is truncated to `MaxDecisions` (mirrors the existing over-`MaxPackages` truncation test).
    - Test 3: A pre-103 JSON blob (no `decisions`/`baselineSnapshot` keys) deserializes to a state with an empty Decisions list and null BaselineSnapshot (backward compat).
    - Test 4 (worst-case cap guard, Pitfall 5): construct the realistic WORST case — a 150-card pool where at least 50 cards each carry THREE decision records (a defer→reject→accept loop-around history), and the remaining ~100 cards carry one Accepted record each (≈250 decision records total) — plus a full 7-family BaselineSnapshot. Assert `Serialize` does NOT throw and the serialized byte length is `< MaxUploadBytes` (262_144). This proves multi-record-per-card accrual over the FULL (never-shrunk) Pool stays under the cap.
  </behavior>
  <action>
    In `CutLabStateSerializer.cs`: add `private const int MaxDecisions = 500;` (`// Why:` a 150-card pool can accrue MULTIPLE records per card over defer→reject→accept loop-around passes — up to ~3 per cut card, so ~450 worst-case records; 500 keeps legitimate history intact while staying comfortably under the byte cap per Pitfall 5's ~40 bytes/record estimate (~500 × 40 ≈ 20KB ≪ 256KB)). Extend `Deserialize`'s `state = state with { ... }` block to also clamp `Decisions = state.Decisions.Where(d => !string.IsNullOrWhiteSpace(d.CardName)).Take(MaxDecisions).ToArray()`. Leave the existing `MaxUploadBytes` size-guard and the `ClampFloors(EnforceCommanderLock(...))` tail intact (BaselineSnapshot needs no clamp — it is server-produced numeric data, but it still passes through the size cap). Extend `CutLabStateSerializerTests.cs` with the four behaviors above (do not rewrite existing tests).
  </action>
  <verify>
    <automated>"/mnt/c/Program Files/dotnet/dotnet.exe" test DeckFlow.Web.Tests --filter "FullyQualifiedName~CutLabStateSerializer"</automated>
  </verify>
  <acceptance_criteria>
    - All four new behaviors pass plus all pre-existing serializer tests stay green.
    - `MaxDecisions` clamp present (grep `Take(MaxDecisions)`).
    - Test 4 worst-case (≥50 cards × 3 records each + ~100 single-record cards + full snapshot ≈ 250 records over a full 150-card Pool) serializes under `MaxUploadBytes` and does not throw.
    - Pre-103 blob deserializes to empty Decisions + null BaselineSnapshot.
  </acceptance_criteria>
  <done>New state fields round-trip, are bounded against the 256KB cap even with multiple decision records per card, and stay backward-compatible.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: CutLabWorkingList.Derive — the single Pool→working-list projection (HIGH-1)</name>
  <read_first>
    - DeckFlow.Web/Models/CutLab/CutLabState.cs (Pool + Decisions from Task 1; CutLabPoolCard shape)
    - DeckFlow.Web/Services/CutLab/CutLabLockRules.cs (static-pure Cut Lab state-derivation precedent + namespace DeckFlow.Web.Services.CutLab)
    - .planning/workstreams/cut-lab/phases/103-simulation-engine-guided-cut-rounds/103-CONTEXT.md (D-16 order-independent restore semantics)
  </read_first>
  <behavior>
    - Test 1: with no decisions, `Derive(pool, [])` returns the full Pool unchanged (same names + quantities).
    - Test 2: a card whose most-recent decision (highest Ordinal) is `Accepted` is EXCLUDED from the derived working list; its Pool entry is untouched (quantity/type/lock metadata still present in Pool).
    - Test 3 (D-16 order independence + restore): a card with history Deferred→Rejected→Accepted is excluded; after the Accepted record is removed (leaving Deferred→Rejected, or none), `Derive` re-includes it with its ORIGINAL Pool quantity and metadata (proving lossless restore, since Pool never changed).
    - Test 4 (net-state, quantity-aware): a card whose latest decision is `Rejected` or `Deferred` (not Accepted) REMAINS in the working list at its full Pool quantity; only a net-Accepted card is removed.
    - Test 5: locked/commander cards are always present in the derived list (they are never Accepted) — the helper does not special-case them, they simply have no Accepted record.
  </behavior>
  <action>
    Create `DeckFlow.Web/Services/CutLab/CutLabWorkingList.cs` (namespace `DeckFlow.Web.Services.CutLab`, `public static class CutLabWorkingList`, mirroring `CutLabLockRules`'s static-pure shape). Expose `public static IReadOnlyList<CutLabPoolCard> Derive(IReadOnlyList<CutLabPoolCard> pool, IReadOnlyList<CutLabDecision> decisions)`: for each card name, find its most-recent decision by `Ordinal` (max); a card is "cut" iff that latest decision `Kind == Accepted`. Return the Pool cards (preserving Name/Quantity/TypeLine/IsCommander/IsLocked/PackageId — quantity-aware) whose name is NOT net-Accepted. Also expose a small `public static IReadOnlySet<string> AcceptedCardNames(IReadOnlyList<CutLabDecision> decisions)` (OrdinalIgnoreCase) so the round engine and endpoint can share the exclusion set. Add a `// Why:` comment: "HIGH-1 — Pool is immutable; this is the ONLY place the working list is derived, so every consumer (round engine, simulation, endpoint, view) agrees and restore is lossless." Pure function, no I/O.
  </action>
  <verify>
    <automated>"/mnt/c/Program Files/dotnet/dotnet.exe" test DeckFlow.Web.Tests --filter "FullyQualifiedName~CutLabWorkingList"</automated>
  </verify>
  <acceptance_criteria>
    - All five behaviors pass.
    - `Derive` never mutates the input Pool (returns a filtered view; input list reference-equal contents unchanged).
    - Net-Accepted (latest-by-Ordinal) is the exclusion rule; Rejected/Deferred remain in the list.
    - `AcceptedCardNames` exposed for reuse by the round engine + endpoint.
  </acceptance_criteria>
  <done>A single pure Pool→working-list projection exists; downstream plans derive the working list only through it (no ad-hoc Pool mutation).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| hidden field → server | `CutLabStateJson` is attacker-controllable (client hidden input); Deserialize must never trust its size or contents. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-103-02-01 | Tampering | CutLabStateJson deserialize (oversized/over-long Decisions) | mitigate | Existing `MaxUploadBytes` byte-cap guard runs first; new `Take(MaxDecisions)` bounds the list; malformed JSON returns `new CutLabState()` (existing catch). |
| T-103-02-02 | Denial of Service | Serialize of an unbounded working session | mitigate | Serialize throws `InvalidOperationException` above `MaxUploadBytes`; `MaxDecisions` keeps the realistic worst case (multi-record-per-card over the full immutable Pool) under cap (test-proven). |
| T-103-02-03 | Tampering | Forged BaselineSnapshot in a submitted blob | accept | Baseline is display-only reference data; a forged snapshot only mis-renders the user's own before/after view (no privilege/data crossing). Server recomputes-at-intake authoritatively in Plan 103-06. |
| T-103-02-04 | Tampering | Forged Decisions steering the derived working list | accept | `Derive` only hides the user's own cards from their own working list; a forged Accepted record cannot cross a trust boundary, and the authoritative floor gate (Plan 103-07) still runs on every cut. |
</threat_model>

<verification>
- `"/mnt/c/Program Files/dotnet/dotnet.exe" test DeckFlow.Web.Tests --filter "FullyQualifiedName~CutLab"` green.
- Backward-compat + worst-case cap + truncation + derivation/restore behaviors proven by test.
</verification>

<success_criteria>
CutLabState round-trips Decisions + BaselineSnapshot bounded under 256KB even with multiple decision records per card; Pool is immutable and the working list is derived exclusively through `CutLabWorkingList.Derive`, making restore lossless; backward-compatible with pre-103 blobs.
</success_criteria>

<output>
Create `.planning/workstreams/cut-lab/phases/103-simulation-engine-guided-cut-rounds/103-02-SUMMARY.md` when done.
</output>
</content>
</invoke>
