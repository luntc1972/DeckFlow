---
phase: 09-bracket-ux-ai-selector-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - DeckFlow.Web/Models/ChatGptDeckRequest.cs
  - DeckFlow.Web/Models/ChatGptDeckComparisonRequest.cs
  - DeckFlow.Web/Models/ChatGptCedhMetaGapRequest.cs
  - DeckFlow.Web/Services/ChatGptDeckPacketService.cs
  - DeckFlow.Web/Services/ChatGptRequestContextParser.cs
  - DeckFlow.Web/Services/ChatGptPacketArtifactStore.cs
autonomous: true
requirements:
  - AISEL-01
  - AISEL-04

must_haves:
  truths:
    - "ChatGptDeckRequest.TargetAiPlatform property exists, defaults to ChatGPT, null-guard setter"
    - "ChatGptDeckComparisonRequest.TargetAiPlatform and ChatGptCedhMetaGapRequest.TargetAiPlatform exist for UI POST binding"
    - "BuildRequestContextText emits target_ai_platform: line immediately after the target_commander_bracket line"
    - "ChatGptRequestContextParser.Parse reads target_ai_platform key and populates ParsedRequestContext.TargetAiPlatform"
    - "ChatGptPacketArtifactStore.LoadFromZip restores TargetAiPlatform; legacy zips without the key silently default to ChatGPT"
  artifacts:
    - path: "DeckFlow.Web/Models/ChatGptDeckRequest.cs"
      provides: "TargetAiPlatform property with ChatGPT default and null-guard"
      contains: "TargetAiPlatform"
    - path: "DeckFlow.Web/Services/ChatGptRequestContextParser.cs"
      provides: "Parser switch case for target_ai_platform; ParsedRequestContext.TargetAiPlatform init property"
      contains: "target_ai_platform"
    - path: "DeckFlow.Web/Services/ChatGptPacketArtifactStore.cs"
      provides: "LoadFromZip restores TargetAiPlatform from parsed context"
      contains: "parsed.TargetAiPlatform"
  key_links:
    - from: "ChatGptDeckPacketService.BuildRequestContextText"
      to: "01-request-context.txt"
      via: "builder.AppendLine($\"target_ai_platform: ...\")"
      pattern: "target_ai_platform"
    - from: "ChatGptRequestContextParser.Parse switch"
      to: "ParsedRequestContext.TargetAiPlatform"
      via: "case target_ai_platform"
      pattern: "case.*target_ai_platform"
    - from: "ChatGptPacketArtifactStore.LoadFromZip"
      to: "ChatGptDeckRequest.TargetAiPlatform"
      via: "if (parsed.TargetAiPlatform is not null)"
      pattern: "parsed.TargetAiPlatform"
---

<objective>
Add TargetAiPlatform to all three request models and wire the full round-trip for the Packets page:
model property -> zip writer -> zip parser -> zip loader.

Comparison and CEDH request models get the property for POST binding only — their zips do not gain
round-trip support until Phase 10.

Purpose: Provides the C# surface the Razor partials from Plan 09-01 bind against (POST form
binding), and delivers AISEL-04 (zip round-trip on the Packets page).

Output: Three model files updated; three service files updated; existing round-trip test still passes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/09-bracket-ux-ai-selector-foundation/09-RESEARCH.md

<interfaces>
From DeckFlow.Web/Models/ChatGptDeckRequest.cs (verified):
  All string properties use null-guard backing-field pattern:
    private string _fieldName = "default";
    public string FieldName { get => _fieldName; set => _fieldName = value ?? "default"; }
  TargetCommanderBracket is at line 91 — add TargetAiPlatform after it (or after last string prop)

From DeckFlow.Web/Services/ChatGptDeckPacketService.cs BuildRequestContextText (verified):
  Line 1655: builder.AppendLine($"format: {NormalizeSingleLine(request.Format, "Commander")}");
  Line 1656: builder.AppendLine($"deck_name: ...");
  Line 1657: builder.AppendLine($"commander: ...");
  Line 1658: builder.AppendLine($"target_commander_bracket: {NormalizeSingleLine(request.TargetCommanderBracket, string.Empty)}");
  INSERT after line 1658: builder.AppendLine($"target_ai_platform: {NormalizeSingleLine(request.TargetAiPlatform, "ChatGPT")}");

From DeckFlow.Web/Services/ChatGptRequestContextParser.cs (verified):
  switch(key) block at lines 68-92 handles inline scalar values
  case "target_commander_bracket": targetCommanderBracket = inlineValue.Trim(); break;
  ADD after it: case "target_ai_platform": targetAiPlatform = inlineValue.Trim(); break;
  Local variable declarations at top of Parse method (lines 40-52) — add: string? targetAiPlatform = null;
  Return constructor (lines 134-148) — add: TargetAiPlatform = string.IsNullOrEmpty(targetAiPlatform) ? null : targetAiPlatform,

From ParsedRequestContext sealed record (lines 243-272, verified):
  All properties use: public Type? PropName { get; init; }
  ADD: public string? TargetAiPlatform { get; init; }

From DeckFlow.Web/Services/ChatGptPacketArtifactStore.cs LoadFromZip (verified):
  Restore pattern ends at lines 217-221:
    if (parsed.DeckSource is not null) { request.DeckSource = parsed.DeckSource; }
  ADD after that block:
    if (parsed.TargetAiPlatform is not null) { request.TargetAiPlatform = parsed.TargetAiPlatform; }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add TargetAiPlatform property to all three request models</name>
  <files>
    DeckFlow.Web/Models/ChatGptDeckRequest.cs
    DeckFlow.Web/Models/ChatGptDeckComparisonRequest.cs
    DeckFlow.Web/Models/ChatGptCedhMetaGapRequest.cs
  </files>
  <read_first>
    - DeckFlow.Web/Models/ChatGptDeckRequest.cs (full file — confirm property insertion point, verify null-guard pattern on TargetCommanderBracket)
    - DeckFlow.Web/Models/ChatGptDeckComparisonRequest.cs (full file)
    - DeckFlow.Web/Models/ChatGptCedhMetaGapRequest.cs (full file)
  </read_first>
  <action>
Add a TargetAiPlatform string property with null-guard backing field to all three classes,
following the exact null-guard pattern used by every other string property in each file.

CHATGPTDECKREQUEST.CS — add backing field with the other private string fields:
    private string _targetAiPlatform = "ChatGPT";

Add public property after TargetCommanderBracket (or after the last string property):
    /// summary: The AI platform the user intends to paste the generated artifact into.
    /// Defaults to "ChatGPT". Accepted values: "ChatGPT", "Claude", "Gemini".
    public string TargetAiPlatform
    {
        get => _targetAiPlatform;
        set => _targetAiPlatform = value ?? "ChatGPT";
    }

CHATGPTDECKCOMPARISONREQUEST.CS — same backing field and property pattern:
    private string _targetAiPlatform = "ChatGPT";
    /// summary: The AI platform the user intends to paste the generated artifact into.
    /// Defaults to "ChatGPT". UI field only in Phase 9 — zip round-trip added in Phase 10.
    public string TargetAiPlatform
    {
        get => _targetAiPlatform;
        set => _targetAiPlatform = value ?? "ChatGPT";
    }

CHATGPTCEDHMETAGAPREQUEST.CS — same backing field and property pattern:
    private string _targetAiPlatform = "ChatGPT";
    /// summary: The AI platform the user intends to paste the generated artifact into.
    /// Defaults to "ChatGPT". UI field only in Phase 9 — zip round-trip added in Phase 10.
    public string TargetAiPlatform
    {
        get => _targetAiPlatform;
        set => _targetAiPlatform = value ?? "ChatGPT";
    }

Do NOT alter any existing property. Do NOT add TargetAiPlatform to any other model.
  </action>
  <verify>
    <automated>grep -n "TargetAiPlatform" /mnt/c/users/chrislunt/source/personal/decksyncworkbench/DeckFlow.Web/Models/ChatGptDeckRequest.cs /mnt/c/users/chrislunt/source/personal/decksyncworkbench/DeckFlow.Web/Models/ChatGptDeckComparisonRequest.cs /mnt/c/users/chrislunt/source/personal/decksyncworkbench/DeckFlow.Web/Models/ChatGptCedhMetaGapRequest.cs</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "TargetAiPlatform" ChatGptDeckRequest.cs returns >= 3 (backing field + getter + setter line)
    - grep -c "TargetAiPlatform" ChatGptDeckComparisonRequest.cs returns >= 3
    - grep -c "TargetAiPlatform" ChatGptCedhMetaGapRequest.cs returns >= 3
    - grep "_targetAiPlatform = \"ChatGPT\"" appears in all three files (default value)
    - grep "value ?? \"ChatGPT\"" appears in all three files (null-guard)
    - dotnet build DeckFlow.Web passes clean after this task
  </acceptance_criteria>
  <done>TargetAiPlatform property with ChatGPT default and null-guard added to all three request models; dotnet build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Wire round-trip — writer, parser, loader</name>
  <files>
    DeckFlow.Web/Services/ChatGptDeckPacketService.cs
    DeckFlow.Web/Services/ChatGptRequestContextParser.cs
    DeckFlow.Web/Services/ChatGptPacketArtifactStore.cs
  </files>
  <read_first>
    - DeckFlow.Web/Services/ChatGptDeckPacketService.cs (read lines 1650-1680 — BuildRequestContextText emit block)
    - DeckFlow.Web/Services/ChatGptRequestContextParser.cs (read lines 40-55 for local var declarations; read lines 65-95 for switch block; read lines 130-150 for return constructor)
    - DeckFlow.Web/Services/ChatGptPacketArtifactStore.cs (read lines 210-225 — end of restore block in LoadFromZip)
    - DeckFlow.Web.Tests/ChatGptPacketArtifactStoreRoundTripTests.cs (read full file to understand what the existing test asserts — must not break it)
  </read_first>
  <action>
Make three precise edits across three files. Read each file section before editing.

EDIT 1 — ChatGptDeckPacketService.cs, BuildRequestContextText:
Find the line (around line 1658):
    builder.AppendLine($"target_commander_bracket: {NormalizeSingleLine(request.TargetCommanderBracket, string.Empty)}");
Insert IMMEDIATELY AFTER it (no blank line needed):
    builder.AppendLine($"target_ai_platform: {NormalizeSingleLine(request.TargetAiPlatform, "ChatGPT")}");

EDIT 2 — ChatGptRequestContextParser.cs, three sub-edits:

2a. In the local variable declaration block at the top of Parse() (around lines 40-52), add:
    string? targetAiPlatform = null;
    (Add it alongside the other nullable string locals like targetCommanderBracket, deckName, etc.)

2b. In the switch(key) block for inline scalar values (around lines 68-92), find:
    case "target_commander_bracket":
        targetCommanderBracket = inlineValue.Trim();
        break;
Add AFTER that case:
    case "target_ai_platform":
        targetAiPlatform = inlineValue.Trim();
        break;

2c. In the return new ParsedRequestContext { ... } constructor (around lines 134-148), find:
    DeckSource = string.IsNullOrEmpty(deckSource) ? null : deckSource
Add a new field on the next line:
    TargetAiPlatform = string.IsNullOrEmpty(targetAiPlatform) ? null : targetAiPlatform,

Also add TargetAiPlatform as an init property on the ParsedRequestContext sealed record
(in the same file, around lines 268-272, after DeckSource):
    /// summary: The AI platform from the request context, if present. Null means absent in zip (legacy zip — caller defaults to "ChatGPT").
    public string? TargetAiPlatform { get; init; }

EDIT 3 — ChatGptPacketArtifactStore.cs, LoadFromZip:
Find the last restore block inside the if (!string.IsNullOrWhiteSpace(requestContextText)) section:
    if (parsed.DeckSource is not null)
    {
        request.DeckSource = parsed.DeckSource;
    }
Add IMMEDIATELY AFTER that block:
    if (parsed.TargetAiPlatform is not null)
    {
        request.TargetAiPlatform = parsed.TargetAiPlatform;
    }

SCOPE CONSTRAINT: Do NOT touch BuildComparisonZip, BuildCedhMetaGapZip, LoadComparisonFromZip,
or LoadCedhMetaGapFromZip — those methods have no 01-request-context.txt and are out of Phase 9 scope.
  </action>
  <verify>
    <automated>grep -n "target_ai_platform" /mnt/c/users/chrislunt/source/personal/decksyncworkbench/DeckFlow.Web/Services/ChatGptDeckPacketService.cs /mnt/c/users/chrislunt/source/personal/decksyncworkbench/DeckFlow.Web/Services/ChatGptRequestContextParser.cs /mnt/c/users/chrislunt/source/personal/decksyncworkbench/DeckFlow.Web/Services/ChatGptPacketArtifactStore.cs</automated>
  </verify>
  <acceptance_criteria>
    - grep -n "target_ai_platform" ChatGptDeckPacketService.cs returns exactly 1 result in BuildRequestContextText (the AppendLine)
    - grep -n "target_ai_platform" ChatGptRequestContextParser.cs returns at least 2 results (the case label and the return constructor field)
    - grep -n "TargetAiPlatform" ChatGptRequestContextParser.cs shows the init property in ParsedRequestContext
    - grep -n "parsed.TargetAiPlatform" ChatGptPacketArtifactStore.cs returns 1 result inside LoadFromZip
    - grep does NOT find "target_ai_platform" in BuildComparisonZip or BuildCedhMetaGapZip
    - grep does NOT find "TargetAiPlatform" in LoadComparisonFromZip or LoadCedhMetaGapFromZip
    - dotnet build DeckFlow.Web passes clean (0 errors, 0 new warnings)
    - dotnet build DeckFlow.Web.Tests passes clean
    - The line emitting target_ai_platform appears on the line AFTER target_commander_bracket in the source file
  </acceptance_criteria>
  <done>Round-trip chain complete: writer emits target_ai_platform in BuildRequestContextText; parser reads it in the switch case; ParsedRequestContext has TargetAiPlatform init property; LoadFromZip restores it. Comparison and CEDH load methods untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser form POST -> ASP.NET model binding | TargetAiPlatform is user-supplied string |
| Zip upload -> LoadFromZip -> ChatGptDeckRequest | TargetAiPlatform is read from user-supplied zip content |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-03 | Tampering | TargetAiPlatform in zip | accept | Value is a UI hint only in Phase 9; no authorization decision depends on it; the null-guard setter normalizes unexpected values to "ChatGPT"; Phase 10 will add a value allowlist when the field drives artifact format branching |
| T-09-04 | Tampering | TargetAiPlatform form POST | accept | Same rationale — no security-sensitive branch on this value in Phase 9 |
| T-09-05 | Information Disclosure | 01-request-context.txt in zip | accept | Already emitting user-supplied deck metadata (deck name, commander, strategy notes); adding a platform preference string adds no new PII surface |
</threat_model>

<verification>
dotnet build DeckFlow.Web — must produce 0 errors.
dotnet build DeckFlow.Web.Tests — must produce 0 errors.

grep -n "target_ai_platform" DeckFlow.Web/Services/ChatGptDeckPacketService.cs
grep -n "TargetAiPlatform" DeckFlow.Web/Services/ChatGptRequestContextParser.cs
grep -n "parsed.TargetAiPlatform" DeckFlow.Web/Services/ChatGptPacketArtifactStore.cs
</verification>

<success_criteria>
- All three request models have TargetAiPlatform with "ChatGPT" default and null-guard setter
- BuildRequestContextText emits target_ai_platform: line after target_commander_bracket:
- ChatGptRequestContextParser.Parse handles target_ai_platform key; ParsedRequestContext has TargetAiPlatform init property
- LoadFromZip restores TargetAiPlatform; legacy zips (no key) leave the default "ChatGPT" in place
- Comparison and CEDH zip methods are untouched
- dotnet build clean on both DeckFlow.Web and DeckFlow.Web.Tests
</success_criteria>

<output>
After completion, create .planning/phases/09-bracket-ux-ai-selector-foundation/09-02-SUMMARY.md
</output>
