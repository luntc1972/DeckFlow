---
phase: 69-studio-ui-design-pass-shell-dashboard-responsive
plan: 03
type: execute
wave: 2
depends_on: ["69-01"]
files_modified:
  - DeckFlow.Studio/Pages/Home.razor
  - DeckFlow.Studio.Tests/HomePageTests.cs
autonomous: true
requirements: [STUI-02]
must_haves:
  truths:
    - "The Studio Home page shows pipeline counts by VideoStatus (Harvested/Distilled/Approved/Published) at a glance"
    - "The Home page shows publish-state counts (Never published/Pushed-hidden/Published/Local-newer) with the locked badge palette"
    - "The Home page has quick links to Harvest / Review / Publish and keeps the prod-connection indicator"
    - "Counts derive ONLY from already-reachable data (IContentSiteIndexStore + VideoStatusResolver + PublishStateDeriver) — no new store method or query"
  artifacts:
    - path: "DeckFlow.Studio/Pages/Home.razor"
      provides: "Dashboard composition: count cards, publish-state summary, quick links, prod indicator, load/error/empty states"
      min_lines: 60
    - path: "DeckFlow.Studio.Tests/HomePageTests.cs"
      provides: "bUnit tests: count correctness, zero-bucket render, quick-link presence, generic error copy on store failure"
      min_lines: 50
  key_links:
    - from: "DeckFlow.Studio/Pages/Home.razor"
      to: "IContentSiteIndexStore.GetAllRowsAsync"
      via: "Task.Run + EnsureSchemaAsync + GetAllRowsAsync (mirrors Review.razor)"
      pattern: "GetAllRowsAsync"
    - from: "DeckFlow.Studio/Pages/Home.razor"
      to: "VideoStatusResolver.FromContentRow / PublishStateDeriver.Derive"
      via: "count bucketing"
      pattern: "VideoStatusResolver\\.FromContentRow"
---

<objective>
Rebuild the stock placeholder `Home.razor` (currently "Studio is running.") into a real
dashboard that surfaces pipeline state at a glance: VideoStatus count cards
(Harvested/Distilled/Approved/Published), a PublishState summary using the locked badge palette,
quick links to Harvest/Review/Publish, and the existing prod-connection indicator — backed by
bUnit tests in `HomePageTests.cs`.

Purpose: STUI-02 — turn Home into the operator's pipeline overview.
Output: dashboard `Home.razor` + `HomePageTests.cs`.

HARD CONSTRAINT: presentation-only composition over ALREADY-REACHABLE data. No new store method,
no new query, no behavior change. The C# added is read-only wiring of existing
store/resolver/deriver output into the view — nothing more. Error copy must NEVER surface
`ex.Message` (D-07).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/69-studio-ui-design-pass-shell-dashboard-responsive/69-UI-SPEC.md
@DeckFlow.Studio/Pages/Review.razor
@DeckFlow.Studio.Tests/ReviewPageTests.cs

<interfaces>
<!-- Contracts the executor needs — extracted from the codebase. Use directly, do not re-explore. -->

From DeckFlow.Core/Content/VideoStatus.cs:
  enum VideoStatus { NotHarvested, Harvested, Distilled, Approved, Published, Blocked, Duplicate }

From DeckFlow.Core/Content/VideoStatusResolver.cs:
  public static VideoStatus FromContentRow(string approvalStatus, DateTimeOffset? pushedToProdUtc, bool isVisible)

From DeckFlow.Core/Content/PublishState.cs:
  enum PublishState { NeverPublished, PushedHidden, Published, LocalNewer }
  // PublishStateExtensions.ToDisplayString(state) → locked display strings

From DeckFlow.Core/Content/PublishStateDeriver.cs:
  public PublishState Derive(DateTimeOffset? pushedToProdUtc, bool isVisible, DateTimeOffset localIndexedUtc)   // instance method, DI singleton

From DeckFlow.Studio/StudioConfig.cs:
  public sealed record StudioConfig(bool IsProdConfigured, bool IsScpConfigured)   // DI singleton

From DeckFlow.Core/Knowledge (IContentSiteIndexStore):
  Task EnsureSchemaAsync(CancellationToken ct)
  Task<IReadOnlyList<ContentSiteIndexRow>> GetAllRowsAsync(CancellationToken ct)
  // ContentSiteIndexRow fields used: ApprovalStatus, PushedToProdUtc, IsVisible, IndexedUtc

DI already registered in DeckFlow.Studio/Program.cs:
  IContentSiteIndexStore, PublishStateDeriver, StudioConfig, ContentKbOrchestratorOptions
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rebuild Home.razor as a pipeline dashboard</name>
  <files>DeckFlow.Studio/Pages/Home.razor</files>
  <behavior>
    - Given a set of ContentSiteIndexRows, the dashboard renders a count card per VideoStatus bucket (Harvested/Distilled/Approved/Published) whose number equals the count of rows resolving to that status via VideoStatusResolver.FromContentRow.
    - A bucket with zero rows still renders, showing `0`.
    - The dashboard renders publish-state counts (NeverPublished/PushedHidden/Published/LocalNewer) via PublishStateDeriver.Derive, each with the locked badge class.
    - Quick links to `/harvest`, `/review`, `/publish` are present (navigation-only hrefs).
    - The prod-connection indicator reflects StudioConfig.IsProdConfigured.
    - When the store throws, the page shows the generic copy "Could not load pipeline status — check the Studio data directory and retry." and never the exception message.
  </behavior>
  <read_first>
    - DeckFlow.Studio/Pages/Home.razor (current 19-line placeholder)
    - DeckFlow.Studio/Pages/Review.razor (lines 300-330 load discipline: Task.Run → EnsureSchemaAsync → GetAllRowsAsync, OperationCanceledException guard, generic error copy, IDisposable CTS; lines 717-724 publish-state badge markup to MIRROR)
    - DeckFlow.Core/Content/VideoStatusResolver.cs, PublishStateDeriver.cs, PublishState.cs, VideoStatus.cs
    - DeckFlow.Studio/StudioConfig.cs
    - .planning/phases/69-studio-ui-design-pass-shell-dashboard-responsive/69-UI-SPEC.md (Home Dashboard Contract: composition cards, load discipline, Copywriting Contract copy strings, Typography 28px/600 display numbers + 14px/400 labels, hub-card visual pattern)
  </read_first>
  <action>
    Replace the placeholder body of `Home.razor` with a dashboard. Add `@implements IDisposable`
    and inject `IContentSiteIndexStore`, `PublishStateDeriver`, and the existing `StudioConfig`.

    Load discipline — copy Review.razor's pattern EXACTLY (do not invent a new one): a private
    `CancellationTokenSource _cts`, `OnInitializedAsync` wrapping store calls in `Task.Run` that
    calls `EnsureSchemaAsync` then `GetAllRowsAsync`; catch `OperationCanceledException` (swallow);
    catch generic `Exception` and set a `_loadError` to the EXACT copy
    "Could not load pipeline status — check the Studio data directory and retry." (never assign
    `ex.Message`); `finally` sets `_loading = false` and `InvokeAsync(StateHasChanged)`. Implement
    `Dispose()` to cancel + dispose `_cts`.

    Compute counts in the `@code` block from the loaded rows (no new store calls): for VideoStatus,
    project each row through `VideoStatusResolver.FromContentRow(row.ApprovalStatus,
    row.PushedToProdUtc, row.IsVisible)` and count into the four displayed buckets
    (Harvested, Distilled, Approved, Published). For PublishState, project through
    `Deriver.Derive(row.PushedToProdUtc, row.IsVisible, row.IndexedUtc)` and count into the four
    PublishState buckets. Zero buckets still render `0`.

    Markup composition per the Home Dashboard Contract:
    1. A page title `<h1 class="studio-page-title">` (the shared utility DEFINED in 69-01's
       studio-theme.css) — text "DeckFlow Studio". Below it, a "Pipeline at a glance" section label
       per the Copywriting Contract. NOTE (scope): only Home adopts `.studio-page-title` this phase;
       migrating the other pages' existing `h1.h4 fw-semibold` titles is explicitly OUT of scope
       (descoped in 69-01) so the uniform-title intent isn't left dangling but page churn stays minimal.
    2. Pipeline-state count cards — one card per VideoStatus bucket, reusing the `hub-card` visual
       pattern (bordered card, 16px padding, 10px radius). The big number uses the Display role
       (28px/600 — apply via an inline `studio-count` class hook that downstream CSS can size; you
       MAY add the minimal card/count CSS to `studio-theme.css` is OWNED BY PLAN 01 — instead put
       dashboard-card scoped rules in a NEW `Home.razor.css` scoped file if styling is needed, since
       Plan 01's file is off-limits to this plan). Label uses 14px/400 meta.
    3. A publish-state summary rendering each PublishState count with the LOCKED badge classes
       exactly as Review.razor's `RenderPublishStateBadge` does (bg-secondary / bg-warning text-dark /
       bg-success text-white + check / bg-info text-dark). Do not invent new badge colors.
    4. Quick links: `Open Harvest` (primary CTA, accent) → `/harvest`; `Review Queue` → `/review`;
       `Publish` → `/publish` (outline). These are plain `href` navigation only.
    5. The prod-connection indicator: keep the existing "configured / not configured" line from
       StudioConfig.IsProdConfigured, restyled small/meta.
    6. Loading state ("Loading pipeline status…" + spinner-border-sm) and the generic error alert.
    7. Empty state when zero rows: heading "No videos in the pipeline yet" + body
       "Start by harvesting a creator's videos." with a link to `/harvest`.

    If you add a `Home.razor.css` scoped file for card styling, reference Plan-01 tokens
    (`var(--sp-3)`, `var(--studio-surface)`, `var(--studio-border)`, `var(--fs-display)`,
    `var(--studio-accent)`) — do NOT hardcode colors. Counts that are zero MUST still render.
  </action>
  <verify>
    <automated>grep -q 'GetAllRowsAsync' DeckFlow.Studio/Pages/Home.razor && grep -q 'VideoStatusResolver.FromContentRow' DeckFlow.Studio/Pages/Home.razor && grep -q 'Could not load pipeline status' DeckFlow.Studio/Pages/Home.razor && grep -q 'href="/harvest"\|href="harvest"' DeckFlow.Studio/Pages/Home.razor && ! grep -q 'ex.Message' DeckFlow.Studio/Pages/Home.razor && /mnt/c/Program\ Files/dotnet/dotnet.exe build DeckFlow.Studio/DeckFlow.Studio.csproj -nologo 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - Home injects IContentSiteIndexStore + PublishStateDeriver + StudioConfig; implements IDisposable with a CTS.
    - Load path mirrors Review.razor (Task.Run / EnsureSchemaAsync / GetAllRowsAsync / OperationCanceledException guard / generic error copy / finally StateHasChanged). No new store method or query added.
    - VideoStatus counts use `VideoStatusResolver.FromContentRow`; PublishState counts use `Deriver.Derive`. Zero buckets render `0`.
    - Publish-state badges use the locked Bootstrap classes byte-identical to Review.razor; no new badge colors.
    - Home's page title is `<h1 class="studio-page-title">` consuming the shared utility from 69-01 (other pages intentionally not migrated this phase).
    - Quick links to /harvest, /review, /publish present; prod indicator retained; loading + generic-error + empty states present.
    - `ex.Message` never assigned to display state; only the generic copy is shown on failure.
    - Any added card CSS lives in a NEW `Home.razor.css` scoped file referencing Plan-01 tokens — Plan 01's `studio-theme.css` is NOT modified by this plan.
    - `dotnet build DeckFlow.Studio` clean.
  </acceptance_criteria>
  <done>Home is a dashboard with VideoStatus cards, PublishState badge summary, quick links, prod indicator, and load/error/empty states — all from already-reachable data; build clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: bUnit HomePageTests</name>
  <files>DeckFlow.Studio.Tests/HomePageTests.cs</files>
  <behavior>
    - Rendering Home with a known row set shows the correct per-VideoStatus counts.
    - A status bucket with zero matching rows renders `0`, not a hidden/absent card.
    - Quick links to /harvest, /review, /publish are present in the rendered markup.
    - When the injected store throws on GetAllRowsAsync, the rendered output contains the generic error copy and does NOT contain the thrown exception's message text.
  </behavior>
  <read_first>
    - DeckFlow.Studio.Tests/ReviewPageTests.cs (BunitContext pattern, MakeYoutubeRow helper, FakeContentSiteIndexStore + ContentKbOrchestratorOptions + PublishStateDeriver registration, Render<T>())
    - DeckFlow.Studio.Tests/TestDoubles/FakeContentSiteIndexStore.cs (Rows list + whether it supports a throw seam; if no throw seam exists, add a minimal one consistent with the fake's style)
    - DeckFlow.Studio/StudioConfig.cs (record ctor: StudioConfig(bool IsProdConfigured, bool IsScpConfigured))
    - DeckFlow.Studio/Pages/Home.razor (the component under test from Task 1)
  </read_first>
  <action>
    Create `HomePageTests.cs` in namespace `DeckFlow.Studio.Tests` as a `sealed class : BunitContext`,
    mirroring `ReviewPageTests` setup. Register `FakeContentSiteIndexStore` (seeded with a known mix
    of rows whose ApprovalStatus / PushedToProdUtc / IsVisible produce a deterministic spread across
    Distilled / Approved / Published buckets), a `ContentKbOrchestratorOptions` with a temp
    ArtifactRoot, a real `PublishStateDeriver`, and a `StudioConfig(true, false)`. Reuse a
    `MakeYoutubeRow`-style helper.

    Write these facts:
    1. `Counts_RenderPerVideoStatusBucket` — seed N rows with a known status spread; assert the
       rendered text shows the expected count for each bucket (find the count cards by their label
       text / a `studio-count` markup hook).
    2. `ZeroBucket_RendersZero` — seed rows that leave at least one displayed bucket empty; assert
       that bucket's card renders `0` (is present, not omitted).
    3. `QuickLinks_PresentForHarvestReviewPublish` — assert the markup contains anchors to
       `/harvest`, `/review`, `/publish` (or the relative equivalents Home emits).
    4. `StoreFailure_ShowsGenericError_NoLeak` — configure the fake store to throw on
       GetAllRowsAsync; assert the rendered markup contains
       "Could not load pipeline status — check the Studio data directory and retry." and does NOT
       contain the thrown exception's message string.

    If `FakeContentSiteIndexStore` lacks a throw seam, add a minimal `ThrowOnGetAllRows` boolean (or
    a `Func` hook) following the fake's existing conventions — test-double only, no production change.
    Do not introduce a new mocking library; use the existing fakes (per project testing rules).
  </action>
  <verify>
    <automated>/mnt/c/Program\ Files/dotnet/dotnet.exe build DeckFlow.Studio.Tests/DeckFlow.Studio.Tests.csproj -nologo 2>&1 | tail -3 && grep -q 'class HomePageTests' DeckFlow.Studio.Tests/HomePageTests.cs && grep -q 'Could not load pipeline status' DeckFlow.Studio.Tests/HomePageTests.cs</automated>
  </verify>
  <acceptance_criteria>
    - `HomePageTests.cs` exists in `DeckFlow.Studio.Tests` with at least the four facts above.
    - Tests use the existing `FakeContentSiteIndexStore` + bUnit `BunitContext` pattern; no new test framework or mocking library added.
    - The error-leak test asserts BOTH presence of the generic copy and absence of the exception message.
    - `dotnet build DeckFlow.Studio.Tests` clean. (VSTest is unreliable in WSL per project constraint; build-clean + push-and-watch CI is the gate — the executor SHOULD run the HomePageTests facts if the local runner is available, else note CI will run them.)
  </acceptance_criteria>
  <done>HomePageTests covers count correctness, zero-bucket render, quick links, and the no-leak error path; test project builds clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → Studio (local Blazor Server) | Operator-only local tool. The dashboard reads the local content_site_index DB via the SAME already-reachable store path Review.razor already uses — no new data source. |
| store error → operator UI | Store/DB exceptions can carry filesystem/connection detail. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-69-01 | Information disclosure | Home.razor dashboard error path | mitigate | On store failure the dashboard shows the fixed generic copy "Could not load pipeline status…" and never assigns `ex.Message` to display state (D-07). Asserted by the `StoreFailure_ShowsGenericError_NoLeak` test. |
| T-69-NA | (other STRIDE) | dashboard read wiring | accept | Read-only composition of already-reachable data; no new query, no write, no new input surface, no package install on a local operator tool. |
</threat_model>

<verification>
- `dotnet build DeckFlow.Studio` and `dotnet build DeckFlow.Studio.Tests` clean.
- Grep gates confirm existing-store-only wiring, generic error copy, quick links, no `ex.Message`.
- HomePageTests facts pass (locally if runner available, else CI).
- Manual (operator, folds into Plan 04 sweep): dashboard shows real counts at a glance, zero buckets show 0, quick links navigate.
</verification>

<success_criteria>
- Home is a real pipeline dashboard (STUI-02) — VideoStatus cards + PublishState summary + quick links + prod indicator.
- All counts derive from already-reachable data; no new store method/query.
- Error path is leak-safe (generic copy only); tests prove it.
- Build clean; presentation-only.
</success_criteria>

<output>
Create `.planning/phases/69-studio-ui-design-pass-shell-dashboard-responsive/69-03-SUMMARY.md` when done.
</output>
