---
phase: 91-reconcile-seed-lifecycle
plan: 05
type: execute
wave: 3
depends_on: [91-03, 91-04]
files_modified:
  - DeckFlow.Studio/Services/IContentKbReconcileStore.cs
  - DeckFlow.Studio/Services/ContentKbReconcileStore.cs
  - DeckFlow.Studio/Program.cs
  - DeckFlow.Studio.Tests/Services/ContentKbReconcileStoreTests.cs
autonomous: true
requirements: [SYNC-11]

must_haves:
  truths:
    - "Persisting the same discrepancy twice creates zero duplicate rows (idempotent upsert by deterministic ID)"
    - "A previously persisted discrepancy absent on a re-run is marked resolved (resolved_utc set), not deleted or re-created"
    - "A scope-tagged run never resolves discrepancies outside its scope"
    - "Stored discrepancies expose their Kind (mapped from the persisted kind column) so downstream removal-scoping (91-08) can filter to seed-drift"
  artifacts:
    - path: "DeckFlow.Studio/Services/ContentKbReconcileStore.cs"
      provides: "Local SQLite discrepancy store in content-kb.db (mirrors ContentHarvestRunStore)"
      contains: "content_kb_reconcile_discrepancy"
      min_lines: 60
  key_links:
    - from: "ContentKbReconcileStore.UpsertDiscrepancies"
      to: "ON CONFLICT (discrepancy_id) DO UPDATE"
      via: "idempotent upsert"
      pattern: "ON CONFLICT"
    - from: "ContentKbReconcileStore.ResolveAbsent"
      to: "resolved_utc = @now WHERE scope_tag=@scopeTag AND resolved_utc IS NULL AND discrepancy_id NOT IN"
      via: "resolution-by-absence"
      pattern: "resolved_utc"
---

<objective>
Ship the D-05 persistent, local discrepancy store: a new SQLite table in Studio's `content-kb.db`
following the exact `ContentHarvestRunStore` schema-ensure pattern, with an idempotent upsert keyed by
the deterministic discrepancy ID and a scope-tagged resolution-by-absence pass. This is the source of
truth for reconcile state (the D-06 report is a layered view on top of it).

Purpose: Re-running a dry-run produces zero duplicates and marks vanished discrepancies resolved
(not deleted), while scoped/partial runs never false-resolve out-of-scope entries (VALIDATION rows
91-XX-10..12).
Output: `IContentKbReconcileStore` + `ContentKbReconcileStore` + Studio registration.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/91-reconcile-seed-lifecycle/91-CONTEXT.md
@.planning/phases/91-reconcile-seed-lifecycle/91-RESEARCH.md
@.planning/phases/91-reconcile-seed-lifecycle/91-PATTERNS.md
@.planning/phases/91-reconcile-seed-lifecycle/91-04-SUMMARY.md

<interfaces>
<!-- From 91-04 (dependency). Executor USES these. -->
ContentKbReconcileDiscrepancy(Id, Kind, NaturalKeyType, NaturalKeyValue, ArtifactPath, Title)
ContentKbReconcileKind enum

<!-- New contract this plan defines -->
interface IContentKbReconcileStore {
  Task EnsureSchemaAsync(ct);
  Task PersistRunAsync(string scopeTag, IReadOnlyList<ContentKbReconcileDiscrepancy> seen, DateTimeOffset now, ct); // upsert-seen + resolve-absent in one pass
  Task<IReadOnlyList<StoredReconcileDiscrepancy>> GetOpenAsync(string? scopeTag, ct);                                // resolved_utc IS NULL
}
// StoredReconcileDiscrepancy MUST expose Kind (ContentKbReconcileKind, mapped from the persisted
// kind column) alongside Id/natural-key/artifact-path/title, so 91-08 can filter GetOpenAsync results
// to the seed-drift (removal) class for the reviewed-removal set.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ContentKbReconcileStore - schema-ensure, idempotent upsert, resolution-by-absence, scope tags</name>
  <files>DeckFlow.Studio/Services/IContentKbReconcileStore.cs, DeckFlow.Studio/Services/ContentKbReconcileStore.cs, DeckFlow.Studio.Tests/Services/ContentKbReconcileStoreTests.cs</files>
  <read_first>
    - DeckFlow.Core/Content/ContentHarvestRunStore.cs (full file - constructor + SemaphoreSlim-gated EnsureSchemaAsync + dialect CREATE TABLE IF NOT EXISTS shape ~11-62)
    - DeckFlow.Web/Services/FeatureFlags/FeatureFlagStore.cs (ON CONFLICT (key) DO UPDATE upsert shape ~280-294 - the upsert template)
    - 91-RESEARCH.md Pattern 3 (schema DDL) + Pattern 4 (resolution-by-absence SQL); 91-PATTERNS.md "ContentKbReconcileStore.cs" section
  </read_first>
  <behavior>
    - EnsureSchemaAsync creates content_kb_reconcile_discrepancy (discrepancy_id PK, kind, natural_key_type/value, artifact_path, title, scope_tag, first_seen_utc, last_seen_utc, resolved_utc) idempotently.
    - PersistRunAsync upserts each seen discrepancy (insert new with first_seen=last_seen=now; on conflict update last_seen and clear resolved_utc); running it twice on the same set leaves the row count unchanged.
    - After PersistRunAsync with a set that omits a previously-open discrepancy in the SAME scope, that discrepancy's resolved_utc is set (row retained, not deleted).
    - A PersistRunAsync scoped to 'youtube' never touches an open 'podcast'-scoped discrepancy.
    - GetOpenAsync returns StoredReconcileDiscrepancy rows that expose Kind (mapped from the kind column), so callers can filter by class.
  </behavior>
  <action>
    Create `ContentKbReconcileStore` mirroring ContentHarvestRunStore exactly (dialect-capable but
    SQLite-constructed via RelationalDatabaseConnection.FromSqlitePath; SemaphoreSlim-gated
    EnsureSchemaAsync; PostgresCreateTableSql/SqliteCreateTableSql for the schema in RESEARCH Pattern 3).
    `PersistRunAsync(scopeTag, seen, now)` runs in one logical pass: (1) upsert each seen discrepancy
    `INSERT ... ON CONFLICT (discrepancy_id) DO UPDATE SET last_seen_utc=EXCLUDED.last_seen_utc,
    resolved_utc=NULL, ...`; (2) resolution-by-absence `UPDATE ... SET resolved_utc=@now WHERE
    scope_tag=@scopeTag AND resolved_utc IS NULL AND discrepancy_id NOT IN @seenIds` (Dapper IN-clause
    expansion; guard the empty-seen case so an empty set resolves the whole scope, which is correct).
    `GetOpenAsync` selects resolved_utc IS NULL (optionally scope-filtered) and materializes
    `StoredReconcileDiscrepancy` with a Kind property parsed from the persisted kind column (store the
    kind as its enum name and parse back, mirroring how existing stores persist enums) so 91-08 can
    filter the open set to the seed-drift removal class. Register in Program.cs mirroring the
    ContentHarvestRunStore singleton line with contentKbDatabasePath. Write ContentKbReconcileStoreTests:
    idempotent-upsert (re-run twice, assert count unchanged), resolution-by-absence (row retained +
    resolved_utc set), scope isolation, and a Kind-round-trip assertion (a persisted seed-drift
    discrepancy reads back with Kind==SeedDrift via GetOpenAsync).
  </action>
  <acceptance_criteria>
    - Test asserts re-running PersistRunAsync on the same set yields the same row count (zero duplicates).
    - Test asserts a discrepancy absent on the second run has resolved_utc set and the row still exists (SELECT returns it with resolved_utc not null).
    - Test asserts a 'youtube'-scoped run leaves an open 'podcast'-scoped discrepancy unresolved.
    - Test asserts GetOpenAsync round-trips Kind (a persisted seed-drift discrepancy reads back Kind==SeedDrift).
    - Build clean, 0 new warnings; Studio host still starts (new singleton resolves).
  </acceptance_criteria>
  <verify>
    <automated>dotnet.exe test DeckFlow.Studio.Tests/DeckFlow.Studio.Tests.csproj --no-restore --filter "FullyQualifiedName~ReconcileStore"</automated>
  </verify>
  <done>The local discrepancy store persists idempotently, resolves-by-absence within scope, isolates scope tags, exposes Kind on GetOpenAsync for downstream removal-scoping, and is registered as the 10th content-kb.db sibling; tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| reconcile run -> local SQLite store | Local-only Studio persistence (D-05); no prod DDL, no destructive prod write |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-91-11 | Tampering | A scoped run false-resolving out-of-scope discrepancies | mitigate | scope_tag filter on the resolution-by-absence UPDATE + scope-isolation test |
| T-91-12 | Repudiation | Losing discrepancy history via delete-instead-of-resolve | mitigate | Resolution sets resolved_utc, never deletes (retained-row test) |
| T-91-13 | Information Disclosure | New prod DDL for a discrepancy table | mitigate | Store is local content-kb.db only (D-05); no prod schema change |
</threat_model>

<verification>
- `dotnet build DeckFlow.sln` clean.
- ReconcileStore tests green (idempotency, resolution-by-absence, scope isolation, Kind round-trip).
</verification>

<success_criteria>
A durable, local, idempotent discrepancy store exists as the reconcile source of truth - zero-duplicate
re-runs, resolution-by-absence that retains history, scope-tag isolation for partial runs, and Kind
exposed on the read model for downstream removal-scoping - with no new prod DDL.
</success_criteria>

<output>
Create `.planning/phases/91-reconcile-seed-lifecycle/91-05-SUMMARY.md` when done.
</output>
</content>
</invoke>
