---
phase: 101-intake-protection-foundation
plan: 03
type: execute
wave: 2
depends_on: [101-01, 101-02]
files_modified:
  - DeckFlow.Web/Services/CutLab/CutLabStateSerializer.cs
  - DeckFlow.Web/Services/CutLab/CutLabPageService.cs
  - DeckFlow.Web/Models/CutLabViewModel.cs
  - DeckFlow.Web/Controllers/CutLabController.cs
  - DeckFlow.Web/Program.cs
  - DeckFlow.Web.Tests/CutLabStateSerializerTests.cs
  - DeckFlow.Web.Tests/CutLabPageServiceTests.cs
  - DeckFlow.Web.Tests/CutLabControllerTests.cs
autonomous: true
requirements: [INTAKE-01, INTAKE-02, LOCK-01]
must_haves:
  truths:
    - "Submitting a 101–150 pool returns a rendered card count and a format-legality (banned-cards-present) summary"
    - "Declared intent (primary/secondary plan, bracket, play experience) round-trips in the CutLabStateJson hidden field and re-renders into the form"
    - "The commander is auto-detected (or picked via the fallback) and always returned locked"
    - "A tampered CutLabStateJson that unlocks the commander is corrected server-side on deserialize"
    - "A ≤100 / >150 / parse-failure pool renders a clear message, not a broken page"
  artifacts:
    - path: "DeckFlow.Web/Controllers/CutLabController.cs"
      provides: "Feature-flag-gated GET/POST /cut-lab with CSRF + RequestSizeLimit"
      contains: "tool.cut-lab.enabled"
    - path: "DeckFlow.Web/Services/CutLab/CutLabPageService.cs"
      provides: "Load→validate→resolve→legality→commander auto-lock→serialize orchestration"
      exports: ["ICutLabPageService"]
    - path: "DeckFlow.Web/Services/CutLab/CutLabStateSerializer.cs"
      provides: "JSON round-trip with size cap and commander re-lock on deserialize"
      contains: "EnforceCommanderLock"
  key_links:
    - from: "CutLabController.Process"
      to: "ICutLabPageService.ProcessAsync"
      via: "constructor-injected service"
      pattern: "_pageService.ProcessAsync"
    - from: "CutLabStateSerializer.Deserialize"
      to: "CutLabLockRules.EnforceCommanderLock"
      via: "post-deserialize tamper defense"
      pattern: "EnforceCommanderLock"
    - from: "CutLabPageService"
      to: "IDeckEntryLoader.LoadFromSourceAsync"
      via: "no-exact-size deck load"
      pattern: "LoadFromSourceAsync"
---

<objective>
Wire the Cut Lab backend: a JSON round-trip serializer (with size cap + commander re-lock tamper
defense), a page-service orchestrator that loads the pool without the exact-100 gate, validates the
101–150 range, resolves Scryfall type lines, builds a banlist legality summary, auto-detects and
auto-locks the commander, and serializes the working session; the view model; the feature-flag-gated
GET/POST controller; and DI registration. This is the wave that makes /cut-lab a working page behind
its OFF flag.

Purpose: INTAKE-01 (parse + count + legality), INTAKE-02 (intent persists via the round-trip), and the
server side of LOCK-01 (commander auto-lock enforced on every POST). Consumes the contracts from plan
101-02 (CutLabRequest, CutLabState, CutLabPoolValidator, CutLabLockRules) and plan 101-01
(DeckPageTab.CutLab, the seeded flag).
Output: CutLabStateSerializer, ICutLabPageService + CutLabPageService, CutLabViewModel, CutLabController,
Program.cs DI line, and three xUnit test files.

DECISION (commander auto-detection, RESEARCH.md Open Question 2): reuse the leading-card commander
inference heuristic (as ManabaseAnalysisService.ReflagInferredCommanders does) for the common case; when
inference is ambiguous, surface a CommanderSelectionRequired fallback (SelectedCommander on the request,
Manabase pattern). Whichever commander is resolved is force-locked via CutLabLockRules.EnforceCommanderLock
on every POST — a wrong/edited commander can never be left unlocked.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/cut-lab/phases/101-intake-protection-foundation/101-RESEARCH.md
@.planning/workstreams/cut-lab/phases/101-intake-protection-foundation/101-PATTERNS.md
@DeckFlow.Web/Models/CutLabRequest.cs
@DeckFlow.Web/Models/CutLab/CutLabState.cs
@DeckFlow.Web/Services/CutLab/CutLabPoolValidator.cs
@DeckFlow.Web/Services/CutLab/CutLabLockRules.cs

<interfaces>
<!-- DeckHistoryController shape (DeckFlow.Web/Controllers/DeckHistoryController.cs) — mirror GET/POST,
     [FeatureFlagGate], [ValidateAntiForgeryToken], [RequestSizeLimit(2*1024*1024)], the private
     *View(request, error) helper, and OperationCanceledException/Exception catches. Cut Lab adds a
     distinct `catch (InvalidOperationException ex) => CutLabView(request, error: ex.Message)` branch so
     the validator's ≤100/>150/parse messages surface verbatim. -->

<!-- DeckHistoryPageService (DeckFlow.Web/Services/DeckHistoryPageService.cs) — interface + result-record +
     DeckInputReconciler.Reconcile + the
     LoadFromSourceAsync try/catch(DeckParseException|InvalidOperationException|HttpRequestException)
     translation via an Error(...) helper. Mirror those parts. Do NOT mirror its ctor shape: Cut Lab
     uses exactly ONE public ctor (nullable optional logger, no internal test-seam ctor) per the
     locked decision in this task's action. -->

<!-- Deck load WITHOUT size gate (ManabaseAnalysisService.cs ~660-707):
  var entries = ReflagInferredCommanders(load.Entries);  // leading-card commander heuristic
  deckCards = entries.Where(e => AnalyzedBoards.Contains(e.Board)).ToList();
  // then ResolveCardsAsync -> ScryfallCardNameIndex (batches of 75 via IScryfallCardResolver).
  IScryfallCardResolver.ExecuteCollectionAsync builds an index; ScryfallCardDataMapper.ToCardData(card)
  yields a CardData carrying TypeLine (used for land detection + legality). -->

<!-- Banlist (DeckFlow.Web/Services/CommanderBanListService.cs):
  public interface ICommanderBanListService {
    Task<IReadOnlyList<string>> GetBannedCardsAsync(CancellationToken cancellationToken = default); }
  Legality summary = intersection of pool card names with this list (case-insensitive). -->

<!-- DI registration site: DeckFlow.Web/Program.cs:180
  builder.Services.AddScoped<IDeckHistoryPageService, DeckHistoryPageService>();  // add the CutLab twin nearby -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: CutLabStateSerializer — JSON round-trip, size cap, commander re-lock on deserialize</name>
  <files>DeckFlow.Web/Services/CutLab/CutLabStateSerializer.cs, DeckFlow.Web.Tests/CutLabStateSerializerTests.cs</files>
  <behavior>
    - Serialize(state) → Deserialize(json) round-trips Pool, Packages, Intent, and Commander losslessly
    - Deserialize on a JSON blob where the commander card has IsLocked=false → returns a state whose commander card IsLocked=true (EnforceCommanderLock applied)
    - Deserialize(null or "" or whitespace) → returns an empty CutLabState (no throw)
    - Serialize output over MaxUploadBytes is rejected (Serialize throws or the caller-facing guard trips) — a state that serializes larger than the cap surfaces a user-facing message rather than silently truncating
    - Deserialize of malformed JSON → returns an empty CutLabState or throws a translated InvalidOperationException (choose one and test it), never an unhandled JsonException escaping to the controller
  </behavior>
  <read_first>
    - DeckFlow.Core/History/DeckHistorySerializer.cs (MaxUploadBytes size-cap pattern + System.Text.Json usage)
    - DeckFlow.Web/Services/CutLab/CutLabLockRules.cs (EnforceCommanderLock — call after deserialize)
    - DeckFlow.Web/Models/CutLab/CutLabState.cs (records to (de)serialize)
    - CLAUDE.md project section (System.Text.Json { get; init; } carve-out: NEVER convert init to get-only — get-only props are silently skipped in .NET 9+)
  </read_first>
  <action>
    Create CutLabStateSerializer.cs (namespace DeckFlow.Web.Services.CutLab) as a `public static class`
    with `public const int MaxUploadBytes = 262_144;` (256 KB — sized for 150 cards with type lines but
    without oracle text, comfortably under DeckHistory's 1 MB and the controller's 2 MB RequestSizeLimit,
    per RESEARCH.md Pitfall 5). Provide:
      - `public static string Serialize(CutLabState state)` using System.Text.Json configured with
        JsonSerializerDefaults.Web (camelCase property names) — pin camelCase EXPLICITLY (do not rely on the
        default) so the client cut-lab.ts can emit a byte-compatible CutLabStateJson. BLOCKER dependency:
        client (101-04) and server MUST agree on casing; camelCase is the contract. If the UTF-8 byte count
        exceeds MaxUploadBytes, throw
        InvalidOperationException("The Cut Lab working session is too large to save.").
      - `public static CutLabState Deserialize(string? json)` — null/empty/whitespace returns
        `new CutLabState()`; on malformed JSON catch JsonException and return `new CutLabState()` (log-free,
        graceful) OR rethrow as InvalidOperationException (pick and test). On success, ALWAYS return
        `CutLabLockRules.EnforceCommanderLock(deserialized)` so a tampered client blob cannot leave the
        commander unlocked (RESEARCH.md Security Domain tamper row).
    Do NOT change any CutLabState record property from init to get-only (System.Text.Json carve-out).

    Write CutLabStateSerializerTests.cs (xUnit, public sealed class) covering every behavior row,
    especially the tamper case: construct a CutLabState whose commander card has IsLocked=false, Serialize,
    then Deserialize, and assert the round-tripped commander card is IsLocked=true.
  </action>
  <verify>
    <automated>dotnet test DeckFlow.Web.Tests --filter "FullyQualifiedName~CutLabStateSerializerTests" --nologo</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'EnforceCommanderLock' DeckFlow.Web/Services/CutLab/CutLabStateSerializer.cs` returns ≥1
    - `grep -c 'MaxUploadBytes' DeckFlow.Web/Services/CutLab/CutLabStateSerializer.cs` returns ≥1
    - Serializer pins camelCase (`grep -Ec 'JsonSerializerDefaults.Web|CamelCase' DeckFlow.Web/Services/CutLab/CutLabStateSerializer.cs` returns ≥1) so client/server JSON casing matches
    - No CutLabState record property was converted from `init` to a get-only accessor (`git diff DeckFlow.Web/Models/CutLab/CutLabState.cs` shows no such change; ideally no change to that file at all)
    - A test proves the deserialize tamper-reversal (commander re-locked)
    - A test proves malformed/empty JSON does not throw an unhandled JsonException
    - All CutLabStateSerializerTests pass
  </acceptance_criteria>
  <done>The serializer round-trips the working session under a 256 KB cap and re-applies the commander auto-lock on every deserialize, defending against a tampered hidden field; graceful on empty/malformed input.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: CutLabPageService orchestrator + CutLabViewModel (INTAKE-01/02, server LOCK-01)</name>
  <files>DeckFlow.Web/Services/CutLab/CutLabPageService.cs, DeckFlow.Web/Models/CutLabViewModel.cs, DeckFlow.Web.Tests/CutLabPageServiceTests.cs</files>
  <behavior>
    - A 120-card NON-COMMANDER pool (121 total with commander) → result has CardCount=120 (the non-commander pool count, matching what ValidateCardCount gated on) and a legality summary listing any banned cards present (empty when clean), no error
    - A pool intersecting the banlist → BannedCardsPresent contains the banned card name(s); IsLegal=false
    - Intent fields from the request (PrimaryPlan/SecondaryPlan/Bracket/PlayExperience) are copied into the serialized CutLabState.Intent and echoed back in the view model
    - The resolved commander card is IsLocked=true in the returned state (EnforceCommanderLock applied)
    - Ambiguous commander inference → result flags CommanderSelectionRequired=true with CommanderChoices populated, no hard error
    - A ≤100 or >150 pool → result.ErrorMessage carries the CutLabPoolValidator message (service catches the InvalidOperationException, returns an Error result — does not throw out)
    - A DeckParseException from the loader → result.ErrorMessage carries the parse-failure copy
  </behavior>
  <read_first>
    - DeckFlow.Web/Services/DeckHistoryPageService.cs (interface + result record + Error helper + DeckInputReconciler + Load try/catch translation — structural template EXCEPT ctor shape: Cut Lab uses one public ctor with optional nullable logger, no internal test-seam ctor)
    - DeckFlow.Web/Services/Manabase/ManabaseAnalysisService.cs (ReflagInferredCommanders ~934, ResolveCardsAsync ~1100, CommanderSelectionRequired/CommanderChoices result fields ~118-124, AnalyzedBoards)
    - DeckFlow.Web/Services/CommanderBanListService.cs (ICommanderBanListService.GetBannedCardsAsync)
    - DeckFlow.Web/Services/Scryfall/ScryfallCardResolver.cs (IScryfallCardResolver)
    - DeckFlow.Web/Services/Manabase/ScryfallCardDataMapper.cs (ToCardData — maps a ScryfallCard to the TypeLine-carrying record; using DeckFlow.Web.Services.Manabase)
    - DeckFlow.Core/Manabase/ScryfallCardNameIndex.cs (name→card index with TypeLine for land detection + legality; using DeckFlow.Core.Manabase)
    - DeckFlow.Web.Tests/ManabaseBracketResolutionTests.cs (private sealed FakeLoader : IDeckEntryLoader and FakeResolver : IScryfallCardResolver — the fake convention to copy) and DeckFlow.Web.Tests/Manabase/ManabaseAnalysisServiceTests.cs (how a resolver fake serves a fixed card list so an absent name is a fallback miss)
    - DeckFlow.Web/Services/CommanderSpellbookService.cs (the optional-nullable-logger ctor precedent: `ILogger<T>? logger = null` + NullLogger fallback — copy this single-ctor shape, do NOT add a second internal ctor)
    - DeckFlow.Web/AssemblyInfo.cs (line 3: [assembly: InternalsVisibleTo("DeckFlow.Web.Tests")] lets the test project reference the internal CutLabPageService TYPE — construction uses the public ctor, so no internal-ctor access is required)
    - DeckFlow.Web/Models/DeckHistoryViewModel.cs (From(request, result) factory + record init conventions)
    - DeckFlow.Web/Services/CutLab/CutLabPoolValidator.cs, CutLabLockRules.cs, CutLabStateSerializer.cs (Task 1)
  </read_first>
  <action>
    Create CutLabPageService.cs (namespace DeckFlow.Web.Services.CutLab) with a `public interface
    ICutLabPageService { Task<CutLabProcessResult> ProcessAsync(CutLabRequest request, CancellationToken
    cancellationToken = default); }` and a `public sealed record CutLabProcessResult` with init members:
    CutLabState? State, string? SerializedStateJson, int CardCount, IReadOnlyList<string> BannedCardsPresent
    (= []), bool IsLegal, bool CommanderSelectionRequired, IReadOnlyList<string> CommanderChoices (= []),
    IReadOnlyList<string> Warnings (= []), string? ErrorMessage, and bool HasResult.

    Implement `internal sealed class CutLabPageService : ICutLabPageService`. LOCKED DECISION for the test
    seam: fake the EXISTING interfaces only — NO new delegate soup, and NO internal test-seam constructor.
    Provide exactly ONE constructor with an optional nullable logger (the codebase's established
    optional-nullable-logger convention, CommanderSpellbookService precedent):
      `public CutLabPageService(IDeckEntryLoader deckEntryLoader, IScryfallCardResolver cardResolver,
       ICommanderBanListService banListService, ILogger<CutLabPageService>? logger = null)`
    with ArgumentNullException.ThrowIfNull on the three interface dependencies and
    `_logger = logger ?? NullLogger<CutLabPageService>.Instance;`. Do NOT declare a second (internal)
    constructor — nullable annotations don't change a C# signature, so two ctors differing only by `?` are
    duplicate signatures and won't compile. Do NOT add Func<...> execute-override delegates.
    Because all four parameters are interfaces (logger optional), tests construct the service DIRECTLY via
    this public constructor, passing `private sealed` fakes of IDeckEntryLoader, IScryfallCardResolver, and
    ICommanderBanListService (copy the FakeLoader/FakeResolver shapes from ManabaseBracketResolutionTests.cs)
    and omitting the logger — no InternalsVisibleTo access is needed for construction (the existing
    [InternalsVisibleTo("DeckFlow.Web.Tests")] grant still lets the test project reference the internal class
    type). A Scryfall fallback miss is simulated by giving the FakeResolver a fixed card list that omits one
    pool card's name so its collection lookup + fallback resolve to no match (exactly how the Manabase
    resolver fake does it). Orchestration in ProcessAsync:
      1. DeckInputReconciler.Reconcile the input; if deckSource is blank return an empty (no-result) result.
      2. CutLabPoolValidator.ValidateSourceLength(deckSource.Length) — call this FIRST, BEFORE the expensive
         LoadFromSourceAsync (DoS-shape guard; matches ManabaseAnalysisService ordering and T-101-02-02).
         Catch InvalidOperationException → Error(message).
      3. try LoadFromSourceAsync (NEVER ValidateCommanderDeckSize); catch DeckParseException/InvalidOperationException
         → Error(message); catch HttpRequestException → Error(UpstreamErrorMessageBuilder.BuildScryfallMessage(ex)).
      4. ReflagInferredCommanders-style commander inference; filter to analyzed boards; then compute
         nonCommanderCardCount = analyzed pool count EXCLUDING the resolved/inferred commander card(s).
         Result.CardCount = nonCommanderCardCount (the pool being trimmed; the commander is the "plus one").
      5. CutLabPoolValidator.ValidateCardCount(nonCommanderCardCount) — catch InvalidOperationException →
         Error(message). LOCKED DECISION: gate the 101–150 range on the NON-COMMANDER count so a 150-pool +
         commander (151 total) is valid.
      6. Resolve Scryfall type lines for the pool (batches of 75) to populate CutLabPoolCard.TypeLine; on
         Scryfall failure, translate via UpstreamErrorMessageBuilder.
      7. Legality summary: BannedCardsPresent = pool names ∩ ICommanderBanListService.GetBannedCardsAsync
         (OrdinalIgnoreCase); IsLegal = none present.
      8. Build CutLabState. If a prior CutLabStateJson was submitted, deserialize it via CutLabStateSerializer
         FIRST and treat the client-submitted per-card lock flags, package assignments, and package lock states
         as AUTHORITATIVE (carry them forward onto the freshly-resolved pool by card Name, OrdinalIgnoreCase) —
         this is how browser lock edits survive a resubmit (BLOCKER 1). Then set IsCommander flags and Intent
         from the request, and ALWAYS finish with CutLabLockRules.EnforceCommanderLock so a tampered/blank
         commander flag can never leave the commander unlocked.
      9. Handle ambiguous commander: set CommanderSelectionRequired + CommanderChoices when inference is
         inconclusive and no valid SelectedCommander was supplied.
      10. SerializedStateJson = CutLabStateSerializer.Serialize(state).
    Provide `private static CutLabProcessResult Error(string message, IReadOnlyList<string>? warnings = null)`.

    Create CutLabViewModel.cs (namespace DeckFlow.Web.Models) as a `public sealed record` mirroring
    DeckHistoryViewModel: ActiveTab (DeckPageTab.CutLab), Request (CutLabRequest), ErrorMessage, plus
    projected display fields (CardCount, BannedCardsPresent, IsLegal, CommanderSelectionRequired,
    CommanderChoices, Warnings, HasResult, CutLabStateJson, and the resolved Pool/Packages needed by the
    view). Provide `public static CutLabViewModel From(CutLabRequest request, CutLabProcessResult result)`
    and a helper to render the running-count copy "{CardCount} cards in pool · {lockedCount} locked" where CardCount is the non-commander pool count and lockedCount counts every locked card (including the always-locked commander).

    Write CutLabPageServiceTests.cs (xUnit, public sealed class) constructing the service directly via its
    public constructor (logger omitted) with private sealed fakes of IDeckEntryLoader + IScryfallCardResolver
    + ICommanderBanListService (no delegate overrides, no internal ctor), covering the behavior rows above (happy path count+legality, banned-present, intent
    round-trip, commander auto-lock, ambiguous-commander flag, ≤100/>150 error surfacing, parse-failure
    error), INCLUDING a boundary-total test: a 150-card non-commander pool plus a commander (151 analyzed
    total) passes validation and yields CardCount=150 — proving the commander is excluded from the gated count.
  </action>
  <verify>
    <automated>dotnet test DeckFlow.Web.Tests --filter "FullyQualifiedName~CutLabPageServiceTests" --nologo</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'LoadFromSourceAsync' DeckFlow.Web/Services/CutLab/CutLabPageService.cs` returns ≥1 and `grep -c 'ValidateCommanderDeckSize' DeckFlow.Web/Services/CutLab/CutLabPageService.cs` returns 0
    - In ProcessAsync source order, ValidateSourceLength is invoked BEFORE LoadFromSourceAsync (source assertion by reading the method)
    - `grep -c 'ValidateSourceLength' DeckFlow.Web/Services/CutLab/CutLabPageService.cs` returns ≥1 and `grep -c 'ValidateCardCount' DeckFlow.Web/Services/CutLab/CutLabPageService.cs` returns ≥1
    - A test proves a submitted CutLabStateJson carrying a locked non-commander card survives a resubmit (the resolved pool re-renders that card IsLocked=true)
    - `grep -c 'EnforceCommanderLock' DeckFlow.Web/Services/CutLab/CutLabPageService.cs` returns ≥1
    - `grep -c 'GetBannedCardsAsync' DeckFlow.Web/Services/CutLab/CutLabPageService.cs` returns ≥1
    - A test proves a 150-card non-commander pool + commander (151 analyzed total) passes and returns CardCount=150 (commander excluded from the 101–150 gate)
    - CutLabViewModel exposes ActiveTab set to DeckPageTab.CutLab (grep returns ≥1)
    - A test proves the ≤100/>150 validator error becomes result.ErrorMessage (not an unhandled throw)
    - A test proves the resolved commander card is returned IsLocked=true
    - All CutLabPageServiceTests pass
  </acceptance_criteria>
  <done>The page service loads without the exact-100 gate, validates 101–150, resolves type lines, produces a card-count + banlist legality summary, auto-detects and force-locks the commander, round-trips intent through the state envelope, and translates all failure modes to user-facing messages — proven by seam-injected unit tests.</done>
</task>

<task type="auto">
  <name>Task 3: CutLabController (flag-gated GET/POST) + DI registration + controller tests</name>
  <files>DeckFlow.Web/Controllers/CutLabController.cs, DeckFlow.Web/Program.cs, DeckFlow.Web.Tests/CutLabControllerTests.cs</files>
  <read_first>
    - DeckFlow.Web/Controllers/DeckHistoryController.cs (GET/POST shape, attributes, private *View helper, catches)
    - DeckFlow.Web.Tests/Tools/... and DeckFlow.Web.Tests/DeckHistoryControllerTests.cs (Fake*PageService test-double convention; Index/Process assertions)
    - DeckFlow.Web/Program.cs line ~180 (AddScoped<IDeckHistoryPageService, DeckHistoryPageService>() — registration site)
    - DeckFlow.Web/Infrastructure/FeatureFlagGateAttribute.cs (gate behavior: 404 when flag OFF)
    - DeckFlow.Web/Services/CutLab/CutLabPageService.cs + DeckFlow.Web/Models/CutLabViewModel.cs (Task 2)
  </read_first>
  <action>
    Create CutLabController.cs (namespace DeckFlow.Web.Controllers) as `public sealed class CutLabController
    : Controller` with a constructor taking (ICutLabPageService pageService, ILogger<CutLabController> logger)
    and ArgumentNullException.ThrowIfNull on the service. Actions:
      - `[HttpGet("/cut-lab")] [FeatureFlagGate("tool.cut-lab.enabled")] public IActionResult Index()` →
        returns CutLabView(new CutLabRequest(), null).
      - `[HttpPost("/cut-lab")] [FeatureFlagGate("tool.cut-lab.enabled")] [ValidateAntiForgeryToken]
        [RequestSizeLimit(2 * 1024 * 1024)] public async Task<IActionResult> Process(CutLabRequest request)`:
        request ??= new CutLabRequest(); try { result = await _pageService.ProcessAsync(request,
        HttpContext.RequestAborted); return View("CutLab", CutLabViewModel.From(request, result)); }
        catch (InvalidOperationException ex) { return CutLabView(request, error: ex.Message); }
        catch (OperationCanceledException) { return CutLabView(request, error: "The request timed out. Try again."); }
        catch (Exception ex) { _logger.LogError(ex, "Cut Lab processing failed."); return CutLabView(request,
        error: "Something went wrong processing the pool. Try again."); }
      - private ViewResult CutLabView(CutLabRequest request, string? error) => View("CutLab", new
        CutLabViewModel { ActiveTab = DeckPageTab.CutLab, Request = request, ErrorMessage = error }).
    View name "CutLab" resolves to Views/Deck/CutLab.cshtml (created in plan 101-04); this plan does NOT
    create the .cshtml — the controller compiles against the view name regardless.

    In Program.cs, add `builder.Services.AddScoped<ICutLabPageService, CutLabPageService>();` adjacent to the
    existing DeckHistory registration (line ~180). Preserve LF endings; touch only the added line.

    Write CutLabControllerTests.cs (xUnit, public sealed class) mirroring DeckHistoryControllerTests: a
    FakeCutLabPageService (stateful Fake with CallCount + a settable Result), asserting Index returns a
    ViewResult named "CutLab" with ActiveTab=DeckPageTab.CutLab; Process happy-path returns the "CutLab"
    view with the mapped model; Process surfaces an InvalidOperationException message via the error banner
    path; Process maps OperationCanceledException to the timeout copy.
  </action>
  <verify>
    <automated>dotnet build DeckFlow.Web/DeckFlow.Web.csproj -c Debug --nologo -clp:ErrorsOnly && dotnet test DeckFlow.Web.Tests --filter "FullyQualifiedName~CutLabControllerTests" --nologo</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'FeatureFlagGate("tool.cut-lab.enabled")' DeckFlow.Web/Controllers/CutLabController.cs` returns 2 (GET + POST)
    - `grep -c 'ValidateAntiForgeryToken' DeckFlow.Web/Controllers/CutLabController.cs` returns 1
    - `grep -c 'RequestSizeLimit' DeckFlow.Web/Controllers/CutLabController.cs` returns 1
    - `grep -c 'AddScoped<ICutLabPageService, CutLabPageService>' DeckFlow.Web/Program.cs` returns 1
    - CutLabControllerTests assert Index returns view "CutLab" with ActiveTab DeckPageTab.CutLab
    - Solution builds clean; CutLabControllerTests pass
  </acceptance_criteria>
  <done>/cut-lab has a feature-flag-gated GET/POST controller with CSRF + a 2 MB request cap, InvalidOperationException/timeout/generic error branches, DI-registered page service, and passing controller tests. Route is dark until plan 101-04 supplies the view and the flag is flipped.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser form → /cut-lab POST | Untrusted: deck source, intent fields, and the CutLabStateJson hidden field all cross here |
| server → Scryfall / mtgcommander.net | Outbound upstream calls; failures must degrade to user messages, not stack traces |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-101-03-01 | Spoofing/Tampering | CSRF on POST /cut-lab | mitigate | [ValidateAntiForgeryToken] on Process + @Html.AntiForgeryToken() in the form (plan 101-04) — the standard mutating-action convention |
| T-101-03-02 | Tampering | Tampered CutLabStateJson unlocking the commander | mitigate | CutLabStateSerializer.Deserialize always applies CutLabLockRules.EnforceCommanderLock; CutLabPageService re-enforces after carrying forward prior locks — server never trusts client lock state for the commander |
| T-101-03-03 | Denial of Service | Oversized POST body / hidden-field bloat on 512 MB tier | mitigate | [RequestSizeLimit(2 MB)] on the action + CutLabPoolValidator char cap (100,000) + serializer MaxUploadBytes (256 KB) |
| T-101-03-04 | Information Disclosure | Upstream failure leaking internals | mitigate | Scryfall/HttpRequestException translated via UpstreamErrorMessageBuilder; generic catch logs server-side and returns neutral copy |
| T-101-03-05 | Elevation of Privilege | Route reachable before ready | mitigate | [FeatureFlagGate("tool.cut-lab.enabled")] on both actions 404s while the flag is seeded OFF |
| T-101-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase — zero new dependencies |
</threat_model>

<verification>
- `dotnet build DeckFlow.Web/DeckFlow.Web.csproj` clean
- `dotnet test DeckFlow.Web.Tests --filter "CutLabStateSerializerTests|CutLabPageServiceTests|CutLabControllerTests"` green
- Full `dotnet test DeckFlow.Web.Tests` green (regression: ToolRegistry/FeatureFlag guards from plan 101-01 unaffected)
- LF endings + changed-lines format gate satisfied; System.Text.Json init-property carve-out respected
</verification>

<success_criteria>
- POST /cut-lab parses a 101–150 pool and returns card count + banlist legality summary (INTAKE-01)
- Declared intent round-trips through CutLabStateJson and re-renders (INTAKE-02)
- Commander is auto-detected/selected and always returned locked; tampered state is corrected (LOCK-01 server side)
- ≤100/>150/parse/timeout/upstream failures all render clear messages, never a broken page
- Controller flag-gated + CSRF + size-capped; page service DI-registered; three test files pass
</success_criteria>

<output>
Create `.planning/workstreams/cut-lab/phases/101-intake-protection-foundation/101-03-SUMMARY.md` when done
</output>
