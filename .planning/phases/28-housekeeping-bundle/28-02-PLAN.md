---
phase: 28-housekeeping-bundle
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - DeckFlow.CLI/CommandRunners.cs
  - .planning/milestones/v1.4-phases/26-category-cache-schema-normalization-fresh-start/26-01-SUMMARY.md
  - .planning/milestones/v1.4-phases/26-category-cache-schema-normalization-fresh-start/26-02-SUMMARY.md
  - .planning/milestones/v1.4-phases/24-card-category-lookup-fix-colorless-staple-cards/24-SUMMARY.md
  - .planning/milestones/v1.4-phases/24-card-category-lookup-fix-colorless-staple-cards/24-VERIFICATION.md
  - .planning/v1.4-MILESTONE-AUDIT.md
  - .planning/phases/28-housekeeping-bundle/28-02-SUMMARY.md
autonomous: true
requirements: [HSK-04]

must_haves:
  truths:
    - "CLI default content-kb artifact root resolves to repo-root content-kb/ when MTG_DATA_DIR is unset (single tree, drift impossible) (D-11)"
    - "Phase 26 has retro SUMMARY files for both 26-01 and 26-02 (D-12)"
    - "Phase 24 has a retro SUMMARY and VERIFICATION citing the live-smoke UAT evidence (D-12)"
    - "The duplicate root-level v1.4-MILESTONE-AUDIT.md is deleted; only the milestones/ copy remains (D-13)"
  artifacts:
    - path: "DeckFlow.CLI/CommandRunners.cs"
      provides: "ResolveContentKbArtifactRoot default branch fixed to repo-root content-kb/"
      contains: "content-kb"
    - path: ".planning/milestones/v1.4-phases/26-category-cache-schema-normalization-fresh-start/26-01-SUMMARY.md"
      provides: "Retro SUMMARY for DBO-01 schema foundation"
      contains: "retroactive: true"
    - path: ".planning/milestones/v1.4-phases/24-card-category-lookup-fix-colorless-staple-cards/24-VERIFICATION.md"
      provides: "Retro verification for CAT-01 quick-fix chain"
      contains: "retroactive: true"
  key_links:
    - from: "ResolveContentKbArtifactRoot default branch"
      to: "repo-root content-kb/ tree"
      via: "Path.Combine(Directory.GetCurrentDirectory(), content-kb)"
      pattern: "GetCurrentDirectory.*content-kb"
---

<objective>
Resolve the HSK-04 v1.4 artifact-hygiene debt: (1) eliminate the dual content-kb artifact-tree drift via a bounded CLI code fix (D-11), (2) write retro SUMMARYs for Phase 26 (26-01, 26-02) and a retro SUMMARY + VERIFICATION for the Phase 24 quick-fix chain (D-12), and (3) delete the duplicated root-level milestone-audit file (D-13).

Purpose: Close the weakest evidence chains and the structural drift risk flagged by the v1.4 milestone audit, so the v1.4 archive matches the v1.2/v1.3 archive convention and the deploy source of truth is the only artifact tree.
Output: One bounded CLI code change with a build-clean gate, 3 retro planning docs, 1 file deletion.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-housekeeping-bundle/28-CONTEXT.md
@.planning/milestones/v1.4-MILESTONE-AUDIT.md

# Exemplars to mirror:
@.planning/milestones/v1.4-phases/21.2-pluggable-llm-distill-cli-backends-inserted/21.2-01-SUMMARY.md
@.planning/milestones/v1.4-phases/27-deck-cache-content-hash-dedup-5-day-refresh/27-VERIFICATION.md

<interfaces>
From DeckFlow.CLI/CommandRunners.cs (current state — the D-11 fix site):

ResolveContentKbDatabasePath(FileInfo db)  // line 1653 — NOT changed; stays artifacts/content-kb.db
ResolveContentKbArtifactRoot(FileInfo db)  // lines 1656-1668 — only the MTG_DATA_DIR-unset branch changes

Current unset branch (lines 1664-1667) resolves the artifact root relative to the DB file directory inside artifacts/.
Target: when MTG_DATA_DIR is unset, return Path.GetFullPath(Path.Combine(Directory.GetCurrentDirectory(), "content-kb")).
The MTG_DATA_DIR-set branch (lines 1658-1662, returns Path.Combine(dataDir, "content-kb")) is unchanged.
</interfaces>
</context>

<scope_fence>
ALLOWED FILE SET (Codex: touch ONLY these; ignore all other tasks/files):
- EDIT (single method, unset-branch only): DeckFlow.CLI/CommandRunners.cs — ResolveContentKbArtifactRoot
- CREATE: 26-01-SUMMARY.md, 26-02-SUMMARY.md (in 26-* dir); 24-SUMMARY.md, 24-VERIFICATION.md (in 24-* dir)
- DELETE: .planning/v1.4-MILESTONE-AUDIT.md (root copy only — keep .planning/milestones/v1.4-MILESTONE-AUDIT.md)
- CREATE (end-of-plan summary): .planning/phases/28-housekeeping-bundle/28-02-SUMMARY.md
Do NOT touch ResolveContentKbDatabasePath, any other CommandRunners method, any .csproj, the ROADMAP, STATE.md, or any other source/planning file.
Pre-dispatch: confirm git status is clean; verify both audit copies are byte-identical before deleting the root copy.
</scope_fence>

<tasks>

<task type="auto">
  <name>Task 1: Fix dual-artifact-tree drift (D-11) and delete duplicate audit file (D-13)</name>
  <files>
    DeckFlow.CLI/CommandRunners.cs
    .planning/v1.4-MILESTONE-AUDIT.md
  </files>
  <read_first>
    - DeckFlow.CLI/CommandRunners.cs lines 1653-1668 (ResolveContentKbDatabasePath plus ResolveContentKbArtifactRoot — the only region to edit)
    - .planning/milestones/v1.4-MILESTONE-AUDIT.md line 28 (Dual artifact trees note)
    - .planning/phases/28-housekeeping-bundle/28-CONTEXT.md D-11 and D-13
  </read_first>
  <behavior>
    - When MTG_DATA_DIR is set: ResolveContentKbArtifactRoot returns Path.Combine(dataDir, "content-kb") (unchanged).
    - When MTG_DATA_DIR is unset or whitespace: ResolveContentKbArtifactRoot returns the repo-root content-kb/ absolute path (Directory.GetCurrentDirectory joined with "content-kb"), NOT a path derived from the DB file location inside artifacts/.
    - ResolveContentKbDatabasePath behavior is unchanged (still artifacts/content-kb.db).
  </behavior>
  <action>
    In DeckFlow.CLI/CommandRunners.cs, modify ONLY the portion of ResolveContentKbArtifactRoot reached when MTG_DATA_DIR is null or whitespace (currently lines 1664-1667, which compute dbDirectory from ResolveContentKbDatabasePath and return Path.Combine(dbDirectory, "content-kb")). Replace that branch body so it returns Path.GetFullPath(Path.Combine(Directory.GetCurrentDirectory(), "content-kb")) — the repo-root deploy source of truth. Leave the MTG_DATA_DIR-set branch (returns Path.Combine(dataDir, "content-kb")) and ResolveContentKbDatabasePath untouched. Add a Why-prefixed inline comment at the changed line noting this collapses the dual artifact tree to one (D-11, HSK-04) so drift is impossible. Use Allman braces and 4-space indent per project style; do not reformat surrounding lines. For D-13: confirm the two audit copies are byte-identical (both 6017 bytes per pre-check via cmp), then delete ONLY the root copy .planning/v1.4-MILESTONE-AUDIT.md, keeping the milestones/ copy. Preserve LF line endings. After editing, build DeckFlow.CLI with the Windows dotnet and confirm 0 new warnings and 0 errors.
  </action>
  <verify>
    <automated>"/mnt/c/Program Files/dotnet/dotnet.exe" build DeckFlow.CLI/DeckFlow.CLI.csproj -c Debug</automated>
  </verify>
  <acceptance_criteria>
    - DeckFlow.CLI build reports 0 errors and 0 new warnings.
    - ResolveContentKbArtifactRoot unset branch returns a path built from Directory.GetCurrentDirectory and "content-kb" (grep shows GetCurrentDirectory inside the method).
    - ResolveContentKbDatabasePath is byte-identical to its prior form (unchanged).
    - .planning/v1.4-MILESTONE-AUDIT.md no longer exists; .planning/milestones/v1.4-MILESTONE-AUDIT.md still exists.
    - A Why-prefixed comment citing D-11/HSK-04 is present at the changed line.
  </acceptance_criteria>
  <done>The CLI default artifact root resolves to repo-root content-kb/; build is clean; the duplicate root-level audit file is deleted while the milestones/ copy remains.</done>
</task>

<task type="auto">
  <name>Task 2: Retro SUMMARYs for P26 and P24 plus P24 retro VERIFICATION (D-12)</name>
  <files>
    .planning/milestones/v1.4-phases/26-category-cache-schema-normalization-fresh-start/26-01-SUMMARY.md
    .planning/milestones/v1.4-phases/26-category-cache-schema-normalization-fresh-start/26-02-SUMMARY.md
    .planning/milestones/v1.4-phases/24-card-category-lookup-fix-colorless-staple-cards/24-SUMMARY.md
    .planning/milestones/v1.4-phases/24-card-category-lookup-fix-colorless-staple-cards/24-VERIFICATION.md
  </files>
  <read_first>
    - .planning/milestones/v1.4-phases/21.2-pluggable-llm-distill-cli-backends-inserted/21.2-01-SUMMARY.md (SUMMARY frontmatter + body exemplar)
    - .planning/milestones/v1.4-phases/27-deck-cache-content-hash-dedup-5-day-refresh/27-VERIFICATION.md (VERIFICATION exemplar)
    - .planning/milestones/v1.4-phases/26-category-cache-schema-normalization-fresh-start/26-01-PLAN.md and 26-02-PLAN.md (plan scope to summarize)
    - .planning/ROADMAP.md lines 214-242 (Phase 26 verification block, SC2 amendment commit b1a5cc8, SC3 0.66ms result)
    - .planning/milestones/v1.4-phases/24-card-category-lookup-fix-colorless-staple-cards/24-UAT.md (live smoke PASSED 2026-05-25)
    - .planning/milestones/v1.4-MILESTONE-AUDIT.md lines 19-24, 59 (P24 weak-chain + P26 missing-SUMMARY findings; CAT-01 wired at CategoryKnowledgeRepository.cs:227)
  </read_first>
  <action>
    Write 4 retro planning docs, each mirroring the named exemplar and each carrying retroactive: true plus an evidence: list (D-08/D-12 convention). (1) 26-01-SUMMARY.md: summarize 26-01-PLAN scope (IRelationalDialect.SurrogateIdColumnType, normalized integer-keyed star schema, RED parity + SQLite-AUTOINCREMENT harness), requirements-completed [DBO-01]; cite 26-01-PLAN.md and the ROADMAP Phase 26 verification block. (2) 26-02-SUMMARY.md: summarize 26-02-PLAN scope (port write+read paths to integer keys, intern-on-write RETURNING id, integer commander join, parity GREEN, PG coverage + full-reset runbook), requirements-completed [DBO-01]; cite 26-02-PLAN.md, the SC2 amendment commit b1a5cc8, and the SC3 0.66ms result. In key-decisions note SC2's original >=50% index-footprint target was NOT met and was amended to the achieved ~38% grain-key scope (cite b1a5cc8). (3) 24-SUMMARY.md: summarize the CAT-01 quick-fix (read-time CategoryFilter wired in CategoryKnowledgeRepository.GetCategoriesAsync, which returns CategoryFilter.IncludedOrFallback — currently around CategoryKnowledgeRepository.cs:227, but cite the method/behavior as primary since the line number is volatile — surfacing Sol Ring + colorless-staple categories), requirements-completed [CAT-01]; cite 24-UAT.md and the integration-checker evidence from the audit. (4) 24-VERIFICATION.md: mirror the 27-VERIFICATION exemplar, status passed, retroactive: true, evidence_source list including 24-UAT.md and .planning/milestones/v1.4-MILESTONE-AUDIT.md; SC-table verdicts drawn from the 24-UAT live smoke (PASSED 2026-05-25) and the wired-filter integration evidence. All 4 files: explicit retroactive marker, every verdict/claim cites a concrete source, LF line endings, no invented results beyond the UAT + audit + ROADMAP evidence.
  </action>
  <verify>
    <automated>cd .planning/milestones/v1.4-phases && grep -lc 'retroactive: true' 26-*/26-01-SUMMARY.md 26-*/26-02-SUMMARY.md 24-*/24-SUMMARY.md 24-*/24-VERIFICATION.md | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - All 4 files exist and each contains the literal frontmatter line `retroactive: true`.
    - 26-01-SUMMARY.md and 26-02-SUMMARY.md both list `requirements-completed` including DBO-01.
    - 26-02-SUMMARY.md key-decisions note the SC2 amendment (cite b1a5cc8) and the SC3 0.66ms result.
    - 24-SUMMARY.md lists `requirements-completed` including CAT-01 and cites the wired read-time filter by method/behavior (CategoryKnowledgeRepository.GetCategoriesAsync returning CategoryFilter.IncludedOrFallback, around CategoryKnowledgeRepository.cs:227).
    - 24-VERIFICATION.md has an SC table with non-empty Evidence cells and an `evidence_source` list naming 24-UAT.md and the milestone audit.
    - No source code or file outside this task's files list is modified.
  </acceptance_criteria>
  <done>Phase 26 has both retro SUMMARYs (DBO-01, SC2-amendment noted), and Phase 24 has a retro SUMMARY + VERIFICATION citing the 2026-05-25 live smoke and the wired read-time filter.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI invocation env (MTG_DATA_DIR) → artifact write path | operator-controlled env var selects the artifact output directory |
| planning-doc author → repo | Markdown docs; no untrusted input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-03 | Tampering | artifact root resolves outside intended tree | mitigate | Default branch hard-codes Directory.GetCurrentDirectory() + "content-kb" via Path.GetFullPath; no user-string concatenation in the unset branch; MTG_DATA_DIR branch unchanged |
| T-28-04 | Repudiation | retro P24/P26 docs misstate shipped state | mitigate | Every verdict cites a concrete evidence source (24-UAT.md live smoke, ROADMAP verification block, b1a5cc8 commit, audit row); retroactive marker + evidence list required |
| T-28-SC | Tampering | npm/pip/cargo installs | accept | No package installs — one bounded BCL-only code change plus docs; no dependency surface |
</threat_model>

<verification>
- DeckFlow.CLI build clean (0 errors, 0 new warnings) after the ResolveContentKbArtifactRoot fix.
- Root-level .planning/v1.4-MILESTONE-AUDIT.md deleted; milestones/ copy intact.
- 26-01/26-02 SUMMARYs, 24 SUMMARY + VERIFICATION all exist with retroactive markers and evidence citations.
- git status shows only the 6 files in files_modified changed (1 deletion + 1 edit + 4 creations).
