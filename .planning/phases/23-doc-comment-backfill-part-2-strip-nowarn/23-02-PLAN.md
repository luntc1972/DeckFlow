---
phase: 23-doc-comment-backfill-part-2-strip-nowarn
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - DeckFlow.Web/Models/DeckDiffViewModel.cs
  - DeckFlow.Web/Models/FeedbackItem.cs
  - DeckFlow.Web/Models/WorkflowStepTabsModel.cs
  - DeckFlow.Web/Models/DeckDiffRequest.cs
  - DeckFlow.Web/Models/CommanderCategoryViewModel.cs
  - DeckFlow.Web/Models/CommanderBracketCatalog.cs
  - DeckFlow.Web/Models/DeckConvertRequest.cs
  - DeckFlow.Web/Models/CedhMetaTimePeriod.cs
  - DeckFlow.Web/Models/ScryfallSetOption.cs
  - DeckFlow.Web/Models/DeckConvertViewModel.cs
  - DeckFlow.Web/Models/FeedbackSubmission.cs
  - DeckFlow.Web/Models/FeedbackListQuery.cs
  - DeckFlow.Web/Models/CategorySuggestionMode.cs
  - DeckFlow.Web/Models/FeedbackType.cs
  - DeckFlow.Web/Models/FeedbackStatus.cs
  - DeckFlow.Web/Models/CommanderCategorySummary.cs
  - DeckFlow.Web/Models/DeckInputSource.cs
  - DeckFlow.Web/Models/CedhMetaSortBy.cs
  - DeckFlow.Web/Models/AnalysisQuestionCatalog.cs
  - DeckFlow.Web/Models/Api/SuggestionResponses.cs
  - DeckFlow.Web/Models/Admin/AdminHarvestViewModel.cs
  - DeckFlow.Web/Models/Admin/MaintenanceViewModel.cs
autonomous: true
requirements: [DOC-01]
must_haves:
  truths:
    - "Every public type AND public member in the remaining Models/, Models/Api/, Models/Admin/ files carries an attached XML doc-comment"
    - "A from-clean suppressors-off build emits zero CS1591/CS1573/CS1587 for these files"
    - "Enum members (FeedbackType/Status, CategorySuggestionMode, CedhMetaSortBy/TimePeriod, DeckInputSource) each carry a <summary> — CS1591 fires on public enum members too"
    - "No { get; init; }→{ get; } mutation; no [Attribute] inlined; no raw-string re-indented; LF preserved"
  artifacts:
    - path: "DeckFlow.Web/Models/Api/SuggestionResponses.cs"
      provides: "Type + member <summary> (36 sites)"
    - path: "DeckFlow.Web/Models/DeckDiffViewModel.cs"
      provides: "Type + member <summary> (24 sites)"
    - path: "DeckFlow.Web/Models/Admin/AdminHarvestViewModel.cs"
      provides: "Type + member <summary> (10 sites)"
  key_links:
    - from: "these Models files"
      to: "the Wave-2 suppressor-flip gate (23-05)"
      via: "0 CS1591/1573/1587 when both suppressors are removed"
      pattern: "warning CS(1591|1573|1587)"
---

<objective>
Backfill XML doc-comments on the REMAINING Models surface: the rest of Models/ (the ~19 non-response
DTO/viewmodel/enum files), all of Models/Api/ (SuggestionResponses 36 sites), and Models/Admin/
(AdminHarvestViewModel 10 + MaintenanceViewModel 4). This is the second non-overlapping Models
partition — ~183 of the 475 authoritative warning sites (23-RESEARCH.md per-file inventory).

Purpose: DOC-01 (finish the remaining Models / Api / Admin DTO + enum + viewmodel surface).
Output: all listed files fully documented; zero doc-warnings on them under a suppressors-off build.

NOTE on enums: CS1591 fires on public ENUM MEMBERS as well as the enum type. FeedbackType (4),
FeedbackStatus (4), CategorySuggestionMode (5), CedhMetaSortBy (3), CedhMetaTimePeriod (7),
DeckInputSource (3) each need a `<summary>` on the type AND each public member.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/23-doc-comment-backfill-part-2-strip-nowarn/23-RESEARCH.md
@.planning/phases/17-doc-comment-backfill-part-1-controllers-services/17-CONTEXT.md
@./CLAUDE.md

<conventions>
Phase 17 D-01a/D-03/D-04 (see 23-RESEARCH "Convention Reuse"):
- D-01a: standalone records/classes/enums get a full `<summary>` written directly.
- D-04: every record + every public property documented (member-level — CS1591 is per-member).
- Enums: type `<summary>` + a `<summary>` on each public member.
- D-03 voice: WHY/what-it-represents; seed from CLAUDE.md Component-Responsibilities where listed.

MEMBER-COVERAGE RULE (mandatory — CS1591 fires on EVERY public declaration, not just the type):
Document EVERY public declaration in the allowed files, including public classes, records,
interfaces, enums, enum members, constructors, methods, properties, and positional record
parameters — not just the top-level type. The per-file site counts in 23-RESEARCH.md are
compiler-derived (the compiler counts members), so each listed count already reflects the full
per-member inventory; documenting only the top-level type will leave warnings behind and fail the
23-05 gate.
</conventions>

<formatting_landmines>
CLAUDE.md R-6 — touch ONLY the `///` lines you add:
- NEVER `{ get; init; }` → `{ get; }`. NEVER inline `[Attribute]`. NEVER re-indent raw-strings.
- Preserve LF; NO Format Document. Attached (no blank line between `///` and decl) — else it doesn't count.
</formatting_landmines>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Document Models/Api/ + Models/Admin/ + the largest Models viewmodels</name>
  <files>DeckFlow.Web/Models/Api/SuggestionResponses.cs, DeckFlow.Web/Models/Admin/AdminHarvestViewModel.cs, DeckFlow.Web/Models/Admin/MaintenanceViewModel.cs, DeckFlow.Web/Models/DeckDiffViewModel.cs, DeckFlow.Web/Models/CommanderCategoryViewModel.cs, DeckFlow.Web/Models/DeckDiffRequest.cs, DeckFlow.Web/Models/FeedbackItem.cs, DeckFlow.Web/Models/WorkflowStepTabsModel.cs</files>
  <action>
ALLOWED FILE SET (this task may edit ONLY these 8 files — ignore every other file/task):
  Models/Api/SuggestionResponses.cs (36), Models/Admin/AdminHarvestViewModel.cs (10),
  Models/Admin/MaintenanceViewModel.cs (4), Models/DeckDiffViewModel.cs (24),
  Models/CommanderCategoryViewModel.cs (9), Models/DeckDiffRequest.cs (10),
  Models/FeedbackItem.cs (11), Models/WorkflowStepTabsModel.cs (10).
Add an attached `<summary>` to every public type AND every undocumented public member/property.
Touch ONLY the `///` lines you add (R-6 landmines above).
  </action>
  <verify>
    <automated>for f in Models/Api/SuggestionResponses Models/Admin/AdminHarvestViewModel Models/Admin/MaintenanceViewModel Models/DeckDiffViewModel Models/CommanderCategoryViewModel Models/DeckDiffRequest Models/FeedbackItem Models/WorkflowStepTabsModel; do echo "== $f =="; grep -c '/// <summary>\|/// <inheritdoc' "DeckFlow.Web/$f.cs"; done</automated>
  </verify>
  <done>All 8 files documented at type + member level; git diff shows only added `///` lines.</done>
</task>

<task type="auto">
  <name>Task 2: Document remaining Models DTOs + enums</name>
  <files>DeckFlow.Web/Models/CommanderBracketCatalog.cs, DeckFlow.Web/Models/DeckConvertRequest.cs, DeckFlow.Web/Models/CedhMetaTimePeriod.cs, DeckFlow.Web/Models/ScryfallSetOption.cs, DeckFlow.Web/Models/DeckConvertViewModel.cs, DeckFlow.Web/Models/FeedbackSubmission.cs, DeckFlow.Web/Models/FeedbackListQuery.cs, DeckFlow.Web/Models/CategorySuggestionMode.cs, DeckFlow.Web/Models/FeedbackType.cs, DeckFlow.Web/Models/FeedbackStatus.cs, DeckFlow.Web/Models/CommanderCategorySummary.cs, DeckFlow.Web/Models/DeckInputSource.cs, DeckFlow.Web/Models/CedhMetaSortBy.cs, DeckFlow.Web/Models/AnalysisQuestionCatalog.cs</files>
  <action>
ALLOWED FILE SET (this task may edit ONLY these 14 files — ignore every other file/task):
  CommanderBracketCatalog.cs (8), DeckConvertRequest.cs (7), CedhMetaTimePeriod.cs (7),
  ScryfallSetOption.cs (6), DeckConvertViewModel.cs (6), FeedbackSubmission.cs (5),
  FeedbackListQuery.cs (5), CategorySuggestionMode.cs (5), FeedbackType.cs (4),
  FeedbackStatus.cs (4), CommanderCategorySummary.cs (4), DeckInputSource.cs (3),
  CedhMetaSortBy.cs (3), AnalysisQuestionCatalog.cs (2). All under DeckFlow.Web/Models/.
Add attached `<summary>` on every public type AND every public member. For the ENUM files
(CedhMetaTimePeriod, CategorySuggestionMode, FeedbackType, FeedbackStatus, DeckInputSource,
CedhMetaSortBy) document the enum type AND each enum member. Touch ONLY the `///` lines you add.
  </action>
  <verify>
    <automated>for f in CommanderBracketCatalog DeckConvertRequest CedhMetaTimePeriod ScryfallSetOption DeckConvertViewModel FeedbackSubmission FeedbackListQuery CategorySuggestionMode FeedbackType FeedbackStatus CommanderCategorySummary DeckInputSource CedhMetaSortBy AnalysisQuestionCatalog; do echo "== $f =="; grep -c '/// <summary>' "DeckFlow.Web/Models/$f.cs"; done</automated>
  </verify>
  <done>All 14 files documented at type + member level (incl. each enum member); git diff shows only added `///` lines.</done>
</task>

</tasks>

<verification>
SMOKE CHECK ONLY (anchored grep, blind-spot acknowledged — it confirms a `<summary>` exists somewhere
in each file but does NOT prove every public member carries an attached doc-comment): `grep -L
'<summary>'` over all 22 files in this plan returns empty. The AUTHORITATIVE per-member gate is
23-05's suppressors-off, all-three-code build (CS1591/CS1573/CS1587 promoted).
`dotnet build -c Release` (Windows dotnet.exe) stays 0/0 with NoWarn still present.
</verification>

<success_criteria>
- All 22 Models/Models.Api/Models.Admin files carry attached doc-comments on EVERY public declaration (~183 sites).
- Enum members individually documented.
- `git diff` shows only added `///` lines.
- `dotnet build -c Release` stays 0 warnings / 0 errors.
- Smoke grep is non-authoritative; the real per-member gate is 23-05's suppressors-off all-three-code build.
</success_criteria>

<output>
Create `.planning/phases/23-doc-comment-backfill-part-2-strip-nowarn/23-02-SUMMARY.md` when done.
</output>
