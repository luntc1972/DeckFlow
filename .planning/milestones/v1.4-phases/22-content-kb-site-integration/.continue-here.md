---
context: phase
phase: 22-content-kb-site-integration
task: 3
total_tasks: 4
status: paused
last_updated: 2026-06-02T18:20:21.314Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> Discovered through failure this phase. Acknowledge before proceeding.

- [ ] CONSTRAINT: Do NOT auto-launch the dev server — user starts it (`! scripts/run-web.sh`). UAT cannot proceed without a user-started server.
- [ ] CONSTRAINT: `content.kb.enabled` stays OFF in prod until UAT passes. Do not flip it on speculatively.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Auto-launch dev server | Project rule: never auto-start DeckFlow web; ask user | blocking | Prompt user to run server; wait for confirmation before UAT |
| Compiled wwwroot/js commit | `.gitignore:13` ignores `js`; Docker compiles TS at build | advisory | Edit `.ts` only; never commit compiled JS |
| jq in WSL | jq absent in this WSL env | advisory | Use dotnet/grep assertions, not jq |

<current_state>
Phase 22 (Content KB Site Integration) is CODE-COMPLETE + TESTED + PUSHED. All 4 plans done, 32 web tests green (Web.Tests 528/0, Core 257/0). Whole-solution build clean. All v1.4 commits pushed to origin/v1.4 (b923ffc, 0 unpushed). The ONLY remaining work is 2 human-gated UAT checkpoints — no code changes possible until UAT informs adjustments.
</current_state>

<completed_work>

Completed Tasks:
- 22-01: visibility data layer (is_visible + preserving upsert + flag seed + tests) — Done (4e6462a)
- 22-01-doc: XML doc on IsVisible (review fix) — Done (a956101)
- 22-02: CLI content-index-export + seed + Docker delivery — Done (1d6ddd2)
- 22-03: public browse/detail + resolver + seed loader + nav (Codex impl) — Done (e8ebcf6)
- 22-04: admin curation + AdminFlags.Toggle CSRF fix (Claude impl) — Done (b6be9bc)
- 22-tests: web-layer unit tests (32, all green) — Done (13f61a2)
</completed_work>

<remaining_work>

- 22-03 Task4 UAT (public): 503-when-off, browse/facets, detail copy frontmatter-strip, 375px, 404 hidden/missing
- 22-04 Task3 UAT (admin): curation round-trip, CSRF-negative on all 4 POSTs, reload-preserves-curation, honest timestamp, 375px
- Post-UAT: flip ROADMAP phase 22 → [x], update STATE, flip content.kb.enabled ON in prod
</remaining_work>

<decisions_made>

- Forced all 4 plans through Codex cross-AI (config cross_ai_execution=true + CLAUDE.md Codex-codes rule)
- Plan 22-04 implemented by Claude not Codex (user override "cancel codex and you finish coding")
- Added content-kb-admin.ts beyond plan allowed_files (CSP script-src 'self' blocks inline; modal + Hide-All confirm need compiled module)
- Compiled wwwroot/js NOT committed (.gitignore:13; Docker compiles TS at build)
- Web-layer tests written by Claude (continuation of "you finish coding" override)
- content.kb.enabled defaults OFF in prod until post-UAT flip
</decisions_made>

<blockers>
- 2 blocking human-UAT checkpoints — require user-started dev server. No workaround; phase cannot close without UAT.
</blockers>

## Required Reading (in order)
1. `.planning/phases/22-content-kb-site-integration/22-03-SUMMARY.md` — public publish workflow, UAT Task4 spec
2. `.planning/phases/22-content-kb-site-integration/22-04-SUMMARY.md` — admin curation, UAT Task3 spec, content-kb-admin.ts deviation
3. `.planning/phases/22-content-kb-site-integration/22-REVIEWS.md` — closed HIGH/MED items (HIGH-4 CSRF, D-22F subtree)

## Infrastructure State
- Dev server: NOT running. User must start (`! scripts/run-web.sh`).
- content.kb.enabled: OFF (default). Stays OFF in prod until UAT passes.
- origin/v1.4: up to date (b923ffc), 0 unpushed.

<context>
Nothing left to code. Build clean, 785 tests green, all locked decisions D-22A..F honored, KB-08/KB-09 closed, HIGH-4 CSRF gap closed (AdminFlags.Toggle double-guarded 4/4/4). Phase 23 (doc-comment backfill + strip NoWarn) is the last v1.4 phase and depends on 22 closing.
</context>

<next_action>
Start with: Ask user to start dev server, then walk the 11-step UAT (public 22-03 Task4 + admin 22-04 Task3). On "approved": flip ROADMAP phase 22 → [x], update STATE, flip content.kb.enabled ON in prod.
</next_action>
