---
phase: 44-admin-grid-lazy-paging
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - DeckFlow.Core/Knowledge/CategoryKnowledgeRepository.cs
  - DeckFlow.Core.Tests/CategoryKnowledgeRepositoryTests.cs
autonomous: true
requirements: [GRID-02]
must_haves:
  truths:
    - "After EnsureSchemaAsync runs, the single partial expression index ix_deck_queue_commander_lower_processed exists on deck_queue"
    - "The two old composite indexes ix_deck_queue_processed_commander and ix_deck_queue_processed_commander_lower no longer exist after schema init"
    - "The distinct-count and paged-commander queries are served by an index (EXPLAIN shows SEARCH USING INDEX, not SCAN TABLE)"
  artifacts:
    - path: "DeckFlow.Core/Knowledge/CategoryKnowledgeRepository.cs"
      provides: "Index consolidation DDL in EnsureSchemaAsync index block"
      contains: "ix_deck_queue_commander_lower_processed"
    - path: "DeckFlow.Core.Tests/CategoryKnowledgeRepositoryTests.cs"
      provides: "Updated index assertions"
      contains: "ix_deck_queue_commander_lower_processed"
  key_links:
    - from: "DeckFlow.Core/Knowledge/CategoryKnowledgeRepository.cs"
      to: "deck_queue table"
      via: "CREATE INDEX ... ON deck_queue(LOWER(commander_name)) WHERE processed = 1"
      pattern: "ix_deck_queue_commander_lower_processed.*LOWER\\(commander_name\\).*WHERE processed = 1"
---

<objective>
Consolidate the two overlapping `deck_queue` commander indexes in `CategoryKnowledgeRepository.EnsureSchemaAsync` into a single partial expression index `ix_deck_queue_commander_lower_processed ON deck_queue(LOWER(commander_name)) WHERE processed = 1`, so the distinct-count + paged-commander queries are index-backed and intent-revealing (GRID-02 / SC3, amended by D-10).

Purpose: Fix the slow `/Admin/Harvest` commander query at the source. The current indexes at lines 114-115 are a non-partial composite pair; D-09 mandates verify-then-consolidate to the partial expression form SC3 specifies. This is a pure data-tier change with no behavior change to query results.

Output: Modified `EnsureSchemaAsync` index DDL block + updated repository index test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/44-admin-grid-lazy-paging/44-RESEARCH.md
@.planning/phases/44-admin-grid-lazy-paging/44-PATTERNS.md
@.planning/phases/44-admin-grid-lazy-paging/44-VALIDATION.md

<interfaces>
<!-- Existing index block in EnsureSchemaAsync — extracted for the executor. -->
<!-- The two lines to REPLACE are at CategoryKnowledgeRepository.cs:114-115. -->

Current (CategoryKnowledgeRepository.cs:107-123) — a single C# raw string literal (""" ... """):
  CREATE INDEX IF NOT EXISTS ix_deck_queue_processed_commander ON deck_queue(processed, commander_name);          // line 114 — DROP+remove
  CREATE INDEX IF NOT EXISTS ix_deck_queue_processed_commander_lower ON deck_queue(processed, LOWER(commander_name)); // line 115 — DROP+remove

The index block runs inside an existing try/catch (lines 127-136) that swallows DbException / OperationCanceledException / TimeoutException — DO NOT add a separate try/catch.
The block runs against BOTH SQLite and Postgres with identical SQL (no dialect branch). DROP INDEX IF EXISTS <name> and partial expression CREATE INDEX are valid in both dialects — no IsSqlite branch needed.

Query access patterns served by the new index (from RESEARCH.md):
  GetDistinctProcessedCommanderCountAsync: COUNT(DISTINCT LOWER(commander_name)) FROM deck_queue WHERE processed = 1 AND commander_name IS NOT NULL
  GetPagedProcessedCommanderRowsAsync:     ... WHERE processed = 1 AND commander_name IS NOT NULL GROUP BY LOWER(commander_name) ...
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update repository index test to assert the consolidated partial index (RED)</name>
  <read_first>
    - DeckFlow.Core.Tests/CategoryKnowledgeRepositoryTests.cs (read the full `EnsureSchemaAsync_CreatesDeckQueueIndexes` test at lines 275-287 and the `GetDeckQueueIndexNamesAsync` helper it calls)
    - .planning/phases/44-admin-grid-lazy-paging/44-PATTERNS.md (section "CategoryKnowledgeRepositoryTests.cs — update index test")
  </read_first>
  <action>
    In `EnsureSchemaAsync_CreatesDeckQueueIndexes`, keep the two existing assertions for `ix_deck_queue_processed` and `ix_deck_queue_processed_inserted_deck`. Replace the two `Assert.Contains("ix_deck_queue_processed_commander", ...)` and `Assert.Contains("ix_deck_queue_processed_commander_lower", ...)` lines with: `Assert.DoesNotContain("ix_deck_queue_processed_commander", indexNames)`, `Assert.DoesNotContain("ix_deck_queue_processed_commander_lower", indexNames)`, and `Assert.Contains("ix_deck_queue_commander_lower_processed", indexNames)`. Touch only those lines. This test MUST fail before Task 2 (the new index does not exist yet) — that failing state is intentional per VALIDATION.md Wave 0.
  </action>
  <verify>
    <automated>"/mnt/c/Program Files/dotnet/dotnet.exe" build DeckFlow.Core.Tests/DeckFlow.Core.Tests.csproj</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `CategoryKnowledgeRepositoryTests.cs` contains `Assert.Contains("ix_deck_queue_commander_lower_processed", indexNames)` and `Assert.DoesNotContain("ix_deck_queue_processed_commander_lower", indexNames)`.
    - Behavior assertion: `DeckFlow.Core.Tests` compiles; `EnsureSchemaAsync_CreatesDeckQueueIndexes` is RED until Task 2 lands (the new index name is absent before consolidation).
    - No assertions for `ix_deck_queue_processed` or `ix_deck_queue_processed_inserted_deck` were removed.
  </acceptance_criteria>
  <done>Test asserts the new partial index name and the absence of the two old names; project builds.</done>
</task>

<task type="auto">
  <name>Task 2: Consolidate the deck_queue commander indexes in EnsureSchemaAsync (GREEN)</name>
  <read_first>
    - DeckFlow.Core/Knowledge/CategoryKnowledgeRepository.cs (read the index block lines 106-137 — the raw string literal and its try/catch)
    - .planning/phases/44-admin-grid-lazy-paging/44-PATTERNS.md (section "CategoryKnowledgeRepository.cs — EnsureSchemaAsync index consolidation")
    - ./CLAUDE.md (constraint: never re-indent C# raw-string literals — touch only the lines that change)
  </read_first>
  <action>
    Inside the existing `indexCommand.CommandText` raw string literal (the `""" ... """` block), remove the two lines that create `ix_deck_queue_processed_commander` and `ix_deck_queue_processed_commander_lower` (current lines 114-115). In their place add three statements, preserving the literal's existing indentation exactly: `DROP INDEX IF EXISTS ix_deck_queue_processed_commander;`, `DROP INDEX IF EXISTS ix_deck_queue_processed_commander_lower;`, and `CREATE INDEX IF NOT EXISTS ix_deck_queue_commander_lower_processed ON deck_queue(LOWER(commander_name)) WHERE processed = 1;`. Keep all other index statements unchanged. Do NOT add a dialect branch (`DROP INDEX IF EXISTS <name>` and the partial expression CREATE are identical for SQLite and Postgres). Do NOT add a separate try/catch — the block stays inside the existing one. Do NOT use `CREATE INDEX CONCURRENTLY`. Preserve the existing `// Why:` comment at lines 125-126.
  </action>
  <verify>
    <automated>"/mnt/c/Program Files/dotnet/dotnet.exe" test DeckFlow.Core.Tests/DeckFlow.Core.Tests.csproj --filter "FullyQualifiedName~CategoryKnowledgeRepositoryTests"</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `CategoryKnowledgeRepository.cs` index literal contains `ix_deck_queue_commander_lower_processed ON deck_queue(LOWER(commander_name)) WHERE processed = 1` and two `DROP INDEX IF EXISTS` lines for the old names; the two old `CREATE INDEX` lines for `ix_deck_queue_processed_commander` / `_lower` are gone.
    - Behavior assertion: `EnsureSchemaAsync_CreatesDeckQueueIndexes` passes (GREEN) — new index present, two old names absent.
    - No-regression: full `CategoryKnowledgeRepositoryTests` fixture passes.
    - No dialect `IsSqlite`/`IsPostgres` branch was added; no separate try/catch added.
  </acceptance_criteria>
  <done>The consolidated partial index is created and the two old indexes dropped; the updated repository test is green.</done>
</task>

<task type="auto">
  <name>Task 3: Verify the queries are index-backed via EXPLAIN (manual confirmation, documented in SUMMARY)</name>
  <read_first>
    - .planning/phases/44-admin-grid-lazy-paging/44-PATTERNS.md (section with the two EXPLAIN QUERY PLAN statements)
    - .planning/phases/44-admin-grid-lazy-paging/44-VALIDATION.md (Manual-Only Verifications — distinct-count uses index, not scan)
  </read_first>
  <action>
    Against a local SQLite category-knowledge DB that has run `EnsureSchemaAsync` (so the new index exists), run `EXPLAIN QUERY PLAN` for both queries documented in PATTERNS.md: the `COUNT(DISTINCT LOWER(commander_name)) ... WHERE processed = 1 AND commander_name IS NOT NULL` count query, and the `GROUP BY LOWER(commander_name) ... LIMIT 25 OFFSET 0` paged query. Confirm the plan references `ix_deck_queue_commander_lower_processed` (a `SEARCH ... USING INDEX` line), not `SCAN TABLE deck_queue`. If a local KB DB is not available, construct a throwaway SQLite DB in a temp dir, create the `deck_queue` table + run the schema, then EXPLAIN. Record the verbatim EXPLAIN output for both queries in the plan SUMMARY. If the plan unexpectedly shows a full scan, document the actual output and flag it (do not silently mark SC3 satisfied) — per D-10 fallback, the consolidation still stands but the discrepancy must be surfaced.
  </action>
  <verify>
    <automated>MISSING — EXPLAIN QUERY PLAN inspection is manual (not an xUnit assertion); evidence captured in SUMMARY per VALIDATION.md Manual-Only Verifications</automated>
  </verify>
  <acceptance_criteria>
    - CLI output: EXPLAIN QUERY PLAN for the count query shows a line containing `USING INDEX ix_deck_queue_commander_lower_processed` and no `SCAN TABLE deck_queue`.
    - CLI output: EXPLAIN QUERY PLAN for the paged query references `ix_deck_queue_commander_lower_processed`.
    - SUMMARY contains the verbatim EXPLAIN output for both queries (GRID-02 SC3 evidence).
  </acceptance_criteria>
  <done>Both queries are confirmed index-backed; EXPLAIN output recorded in SUMMARY as SC3 evidence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| application → database | DDL executed against SQLite/Postgres at startup; no untrusted input reaches the index block |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-44-DDL-01 | Denial of Service | EnsureSchemaAsync index DDL on large prod tables | accept | DDL uses `CREATE INDEX IF NOT EXISTS` (non-concurrent) inside the existing try/catch that swallows DbException; matches the established startup best-effort pattern. `DROP INDEX IF EXISTS` is metadata-only and fast. Prod table is small (harvested commanders). No new attack surface — pure data-tier change. |
| T-44-DDL-02 | Tampering | static DDL string | accept | The index DDL is a compile-time C# raw string literal with no interpolation/parameters — no injection vector. |
| T-44-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan; RESEARCH.md Package Legitimacy Audit = N/A (zero new packages). |
</threat_model>

<verification>
- `DeckFlow.Core.Tests` quick run green: `"/mnt/c/Program Files/dotnet/dotnet.exe" test DeckFlow.Core.Tests/DeckFlow.Core.Tests.csproj`
- `EnsureSchemaAsync_CreatesDeckQueueIndexes` asserts the new partial index and the absence of both old indexes.
- EXPLAIN QUERY PLAN evidence (both queries) captured in SUMMARY showing index use, not table scan.
</verification>

<success_criteria>
- GRID-02 SC3 (amended by D-10): the optimal partial expression index `LOWER(commander_name) WHERE processed=1` serves the count + paged queries; EXPLAIN shows index use, not a full scan.
- The two redundant composite indexes are dropped; one intent-revealing partial expression index remains.
- Dual-dialect parity preserved (no branch needed — identical syntax for SQLite + Postgres).
</success_criteria>

<output>
Create `.planning/phases/44-admin-grid-lazy-paging/44-01-SUMMARY.md` when done. Include the verbatim EXPLAIN QUERY PLAN output for both queries.
</output>
