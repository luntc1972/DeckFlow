---
phase: 999.1-ai-agnostic-prose-adaptation-razor-views
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - DeckFlow.Web/Views/Deck/CedhMetaGap.cshtml
  - DeckFlow.Web/Views/Deck/Home.cshtml
autonomous: true
requirements: [D-01, D-02, D-03, D-05]
must_haves:
  truths:
    - "CedhMetaGap.cshtml above-selector prose uses universal noun 'your AI' except the line-22 Phase-12 enumerated lede."
    - "CedhMetaGap.cshtml below-selector prose injects @aiPlatform.DisplayName."
    - "Home.cshtml DeckComparison hub-card description (line 26) is brand-neutral."
    - "Home.cshtml JudgeQuestions hub-card description (line 62) stays ChatGPT-named per D-03 carve-out."
    - "Home.cshtml hub-hero line 14 enumerated form 'ChatGPT, Claude, or Gemini' is preserved per D-05 lede convention (hub-level lede)."
  artifacts:
    - path: "DeckFlow.Web/Views/Deck/CedhMetaGap.cshtml"
      provides: "Hybrid-prose Razor view with AiPlatform.Normalize block"
      contains: "AiPlatform.Normalize(Model.Request.TargetAiPlatform)"
    - path: "DeckFlow.Web/Views/Deck/Home.cshtml"
      provides: "Hub cards with brand-neutral DeckComparison description and preserved JudgeQuestions card"
      contains: "hub-card__description"
  key_links:
    - from: "DeckFlow.Web/Views/Deck/CedhMetaGap.cshtml"
      to: "DeckFlow.Web.Models.AiPlatform.Normalize"
      via: "Razor @{ var aiPlatform = AiPlatform.Normalize(...); } + @aiPlatform.DisplayName"
      pattern: "AiPlatform\\.Normalize"
---

<objective>
Rewrite the 8 ChatGPT hits in `CedhMetaGap.cshtml` using the Hybrid pattern (D-01) anchored on the Step 2 `_AiSelector` boundary at line 184, and generalize the DeckComparison hub-card description in `Home.cshtml` (line 26). Preserve:
- Line-22 enumerated page-lede in CedhMetaGap (D-05 lede exception).
- Line-14 enumerated hub-hero in Home.cshtml (D-05 — hub-level lede).
- Line-62 JudgeQuestions hub-card description in Home.cshtml (D-03 carve-out).

Purpose: Strip the ChatGPT brand from the cEDH Meta-Gap page and the DeckComparison hub card while honoring the JudgeQuestions D-03 carve-out.
Output: Edited `CedhMetaGap.cshtml` (Hybrid prose) and edited `Home.cshtml` (one card line generalized).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/999.1-ai-agnostic-prose-adaptation-razor-views/999.1-CONTEXT.md
@DeckFlow.Web/Models/AiPlatform.cs
@DeckFlow.Web/Views/Deck/CedhMetaGap.cshtml
@DeckFlow.Web/Views/Deck/Home.cshtml
@DeckFlow.Web/Views/Shared/_AiSelector.cshtml
@CLAUDE.md

<interfaces>
CedhMetaGap.cshtml selector boundary:
- Line 184 `@await Html.PartialAsync("_AiSelector", Model.Request.TargetAiPlatform)`
- Above 184: ABOVE-selector → universal noun.
- Below 184: BELOW-selector → `@aiPlatform.DisplayName` injection.

CedhMetaGap.cshtml ChatGPT hits (8):
- Line 22 `<p class="page-lede">...ChatGPT, Claude, or Gemini</p>` → KEEP (D-05 lede exception).
- Line 23 `<p class="lede">...strict ChatGPT prompt...` → ABOVE-selector.
- Line 35 step strip text "Step 2 generates the ChatGPT prompt from 1 to 3 selected lists" → ABOVE-selector.
- Line 180 step instructions "paste it into ChatGPT and request a readable response..." → ABOVE-selector (still pre-184).
- Line 280 "<li>Paste it into ChatGPT.</li>" → ABOVE-selector.
- Line 281 "Have ChatGPT respond with a readable meta-gap summary..." → ABOVE-selector.
- Line 297 "Paste this into ChatGPT to get a readable meta-gap summary..." → BELOW-selector → `@aiPlatform.DisplayName`.
- Line 312 "ChatGPT should return a top-level <code>meta_gap</code> object matching this shape." → BELOW-selector.

Home.cshtml ChatGPT hits (3):
- Line 14 hub-hero `"...paste into ChatGPT, Claude, or Gemini..."` → KEEP (hub-level enumerated lede; D-05).
- Line 26 DeckComparison hub-card description `"...with a ChatGPT-authored breakdown..."` → GENERALIZE (universal noun; no selector context, no `_AiSelector` on Home).
- Line 62 JudgeQuestions hub-card description `"...with a ChatGPT prompt generator as backup."` → KEEP (D-03 carve-out per CONTEXT.md).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add AiPlatform.Normalize Razor block + rewrite ChatGPT prose in CedhMetaGap.cshtml</name>
  <files>DeckFlow.Web/Views/Deck/CedhMetaGap.cshtml</files>
  <read_first>
    - .planning/phases/999.1-ai-agnostic-prose-adaptation-razor-views/999.1-CONTEXT.md (D-01 hybrid, D-05 lede preservation)
    - DeckFlow.Web/Views/Deck/CedhMetaGap.cshtml (full file)
    - DeckFlow.Web/Models/AiPlatform.cs (Normalize signature)
    - DeckFlow.Web/Views/Shared/_AiSelector.cshtml (reference)
  </read_first>
  <action>
    1. Add `var aiPlatform = AiPlatform.Normalize(Model.Request.TargetAiPlatform);` inside the existing top-of-file `@{ }` block. Confirm the model path matches the `_AiSelector` invocation on line 184. Match `@using` style (`@using DeckFlow.Web.Models` or fully-qualify) to the file's existing convention.

    2. Apply Hybrid substitutions. KEEP line 22 enumerated lede unchanged.

    ABOVE-SELECTOR (lines 23–281, universal `your AI`):
    - Line 23 "...generate a strict ChatGPT prompt..." → "...generate a strict AI prompt..." (the surrounding clause already has structure; "AI prompt" reads naturally; OR "...generate a prompt for your AI..."; Claude's Discretion).
    - Line 35 "Step 2 generates the ChatGPT prompt from 1 to 3 selected lists" → "Step 2 generates the prompt from 1 to 3 selected lists" (the noun is redundant once "ChatGPT" is dropped; reads cleanly without filler).
    - Line 180 "paste it into ChatGPT and request a readable response..." → "paste it into your AI and request a readable response..."
    - Line 280 "<li>Paste it into ChatGPT.</li>" → "<li>Paste it into your AI.</li>"
    - Line 281 "Have ChatGPT respond with a readable meta-gap summary first..." → "Have your AI respond with a readable meta-gap summary first..."

    BELOW-SELECTOR (lines 297, 312, inject `@aiPlatform.DisplayName`):
    - Line 297 "Paste this into ChatGPT to get a readable meta-gap summary..." → "Paste this into @aiPlatform.DisplayName to get a readable meta-gap summary..."
    - Line 312 "ChatGPT should return a top-level <code>meta_gap</code> object matching this shape." → "@aiPlatform.DisplayName should return a top-level <code>meta_gap</code> object matching this shape."

    Do NOT touch:
    - Line 22 lede `ChatGPT, Claude, or Gemini`.
    - Phase 13 D-08 identifiers on line 40 (`data-cache-key="chatgpt-cedh-meta-gap"`, `data-chatgpt-cedh-form`, `data-chatgpt-cedh-current-step`).
    - `chatgpt-step-eyebrow` CSS class on line 178.

    Touch only the prose lines. Preserve LF line endings.
  </action>
  <verify>
    <automated>grep -c 'AiPlatform.Normalize(Model.Request.TargetAiPlatform)' DeckFlow.Web/Views/Deck/CedhMetaGap.cshtml</automated>
    <automated>bash -c "count=$(grep -c 'ChatGPT' DeckFlow.Web/Views/Deck/CedhMetaGap.cshtml); test $count -le 1 || { echo \"FAIL: $count ChatGPT hits (expect <=1 for line-22 lede)\"; exit 1; }"</automated>
    <automated>grep -c '@aiPlatform.DisplayName' DeckFlow.Web/Views/Deck/CedhMetaGap.cshtml</automated>
    <automated>grep -c 'ChatGPT, Claude, or Gemini' DeckFlow.Web/Views/Deck/CedhMetaGap.cshtml</automated>
    <automated>grep -c 'data-cache-key="chatgpt-cedh-meta-gap"' DeckFlow.Web/Views/Deck/CedhMetaGap.cshtml</automated>
    <automated>grep -c 'data-chatgpt-cedh-form' DeckFlow.Web/Views/Deck/CedhMetaGap.cshtml</automated>
    <automated>grep -c 'chatgpt-step-eyebrow' DeckFlow.Web/Views/Deck/CedhMetaGap.cshtml</automated>
    <automated>dotnet build DeckFlow.Web/DeckFlow.Web.csproj -c Debug --nologo -v quiet</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'AiPlatform.Normalize(Model.Request.TargetAiPlatform)' DeckFlow.Web/Views/Deck/CedhMetaGap.cshtml` returns `1`.
    - `grep -c 'ChatGPT' DeckFlow.Web/Views/Deck/CedhMetaGap.cshtml` returns exactly `1` (the line-22 lede).
    - `grep -c '@aiPlatform.DisplayName' DeckFlow.Web/Views/Deck/CedhMetaGap.cshtml` returns at least `2` (the two below-selector sites).
    - `grep -c 'ChatGPT, Claude, or Gemini' DeckFlow.Web/Views/Deck/CedhMetaGap.cshtml` returns `1` (lede preserved).
    - `grep -c 'data-cache-key="chatgpt-cedh-meta-gap"' DeckFlow.Web/Views/Deck/CedhMetaGap.cshtml` returns `1` (Phase 13 D-08 preserved).
    - `grep -c 'data-chatgpt-cedh-form' DeckFlow.Web/Views/Deck/CedhMetaGap.cshtml` returns `1` (Phase 13 D-08 preserved).
    - `grep -c 'chatgpt-step-eyebrow' DeckFlow.Web/Views/Deck/CedhMetaGap.cshtml` returns `1` (Phase 13 D-08 preserved).
    - `dotnet build DeckFlow.Web/DeckFlow.Web.csproj -c Debug` exits 0.
  </acceptance_criteria>
  <done>CedhMetaGap.cshtml reads correctly for any AiPlatform; Phase 10/13 invariants intact; build clean.</done>
</task>

<task type="auto">
  <name>Task 2: Generalize DeckComparison hub-card description in Home.cshtml</name>
  <files>DeckFlow.Web/Views/Deck/Home.cshtml</files>
  <read_first>
    - .planning/phases/999.1-ai-agnostic-prose-adaptation-razor-views/999.1-CONTEXT.md (D-02 #1 — Home.cshtml hub-card scope; D-03 JudgeQuestions carve-out; D-05 lede preservation)
    - DeckFlow.Web/Views/Deck/Home.cshtml (full file — confirm line 14, 26, 62 ChatGPT locations)
  </read_first>
  <action>
    Edit ONLY line 26 in `DeckFlow.Web/Views/Deck/Home.cshtml` — the DeckComparison hub-card description.

    Substitute:
    - From: `<p class="hub-card__description">Side-by-side comparison of two decks with a ChatGPT-authored breakdown of strengths, weaknesses, and trade-offs.</p>`
    - To: `<p class="hub-card__description">Side-by-side comparison of two decks with an AI-authored breakdown of strengths, weaknesses, and trade-offs.</p>`

    Reason: hub cards render without selector context (Home.cshtml has no `_AiSelector` partial). Universal noun applies. "AI-authored" reads naturally here as a hyphenated adjective; the alternative "your AI's breakdown" is awkward; Claude's Discretion picks "AI-authored".

    DO NOT touch:
    - Line 14 hub-hero `Five-step workflow: load your deck, pick your questions, copy the prompt, paste into ChatGPT, Claude, or Gemini, review the structured response.` — KEEP (D-05 hub-level enumerated lede).
    - Line 62 JudgeQuestions hub-card description `"...with a ChatGPT prompt generator as backup."` — KEEP per D-03 carve-out (the JudgeQuestions page is intentionally ChatGPT-named; its hub card describes that page accurately).
    - Any other line. Preserve LF line endings.
  </action>
  <verify>
    <automated>grep -c 'AI-authored breakdown' DeckFlow.Web/Views/Deck/Home.cshtml</automated>
    <automated>grep -c 'ChatGPT-authored breakdown' DeckFlow.Web/Views/Deck/Home.cshtml</automated>
    <automated>grep -c 'ChatGPT prompt generator as backup' DeckFlow.Web/Views/Deck/Home.cshtml</automated>
    <automated>grep -c 'ChatGPT, Claude, or Gemini' DeckFlow.Web/Views/Deck/Home.cshtml</automated>
    <automated>grep -c 'ChatGPT' DeckFlow.Web/Views/Deck/Home.cshtml</automated>
    <automated>dotnet build DeckFlow.Web/DeckFlow.Web.csproj -c Debug --nologo -v quiet</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'AI-authored breakdown' DeckFlow.Web/Views/Deck/Home.cshtml` returns `1`.
    - `grep -c 'ChatGPT-authored breakdown' DeckFlow.Web/Views/Deck/Home.cshtml` returns `0`.
    - `grep -c 'ChatGPT prompt generator as backup' DeckFlow.Web/Views/Deck/Home.cshtml` returns `1` (D-03 carve-out preserved).
    - `grep -c 'ChatGPT, Claude, or Gemini' DeckFlow.Web/Views/Deck/Home.cshtml` returns `1` (hub-hero lede preserved per D-05).
    - `grep -c 'ChatGPT' DeckFlow.Web/Views/Deck/Home.cshtml` returns exactly `2` (hub-hero enumerated lede on line 14 + JudgeQuestions card on line 62 — the two intentionally preserved hits).
    - `dotnet build DeckFlow.Web/DeckFlow.Web.csproj -c Debug` exits 0.
  </acceptance_criteria>
  <done>Home.cshtml DeckComparison card is brand-neutral; JudgeQuestions carve-out and hub-hero lede preserved; build clean.</done>
</task>

</tasks>

<verification>
- `grep -c 'ChatGPT' DeckFlow.Web/Views/Deck/CedhMetaGap.cshtml` returns `1` (line-22 lede only).
- `grep -c 'ChatGPT' DeckFlow.Web/Views/Deck/Home.cshtml` returns `2` (line-14 lede + line-62 carve-out).
- Phase 13 D-08 identifiers on CedhMetaGap.cshtml line 40 unchanged from baseline.
- `dotnet build DeckFlow.Web/DeckFlow.Web.csproj -c Release` exits 0.
</verification>

<success_criteria>
- CedhMetaGap.cshtml reads correctly for any AiPlatform.
- Home.cshtml DeckComparison hub card is brand-neutral.
- D-03 JudgeQuestions carve-out and D-05 lede exceptions preserved.
- Build clean.
</success_criteria>

<output>
Create `.planning/phases/999.1-ai-agnostic-prose-adaptation-razor-views/999.1-03-SUMMARY.md` when done.
</output>
