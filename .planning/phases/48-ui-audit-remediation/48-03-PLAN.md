---
phase: 48-ui-audit-remediation
plan: 03
type: execute
wave: 2
depends_on: ["48-01", "48-02"]
files_modified:
  - .planning/phases/48-ui-audit-remediation/48-AUDIT.md
autonomous: false
requirements: [UIR-01, UIR-02, UIR-03]

must_haves:
  truths:
    - "Every remediated finding (F1-F7) is verified with browser screenshots at mobile (<= 768px) AND desktop (>= 1024px)"
    - "The re-scored 6-pillar total reaches >= 20/24"
    - "Token changes are spot-checked in a sample of guild themes (light + dark), including site-commander-table, with RENDERED elevation confirmed per theme (theme box-shadow overrides acceptable)"
    - "The 6-pillar audit/score is produced against the DEPLOYED deckflow.gg site, closing UIR-01 (the requirement explicitly scopes the audit to the deployed public site, not a local build)"
  artifacts:
    - path: ".planning/phases/48-ui-audit-remediation/48-AUDIT.md"
      provides: "Deployed-deckflow.gg, screenshot-backed re-score section appended (>= 20/24) closing UIR-01 + UIR-03, with per-finding 2-viewport evidence and per-theme rendered-elevation spot-check"
      contains: "Deployed-Site Re-Score"
  key_links:
    - from: "deployed deckflow.gg screenshots in logs/audit-shots/"
      to: "48-AUDIT.md deployed-site re-score section"
      via: "per-finding + per-theme evidence references"
      pattern: "Deployed-Site Re-Score"
---

<objective>
Verify the Plan 01 + Plan 02 remediations in the browser at two viewports, re-score the 6-pillar audit against the DEPLOYED deckflow.gg site to confirm >= 20/24, and CLOSE UIR-01 (which mandates the audit/score against the deployed public site, not a local build).

Purpose: UIR-01 requires the 6-pillar audit + score against the deployed deckflow.gg site; the pre-existing 48-AUDIT.md was run against the LOCAL build (audit scope caveat 1), so UIR-01 is NOT yet satisfied. UIR-03 mandates browser-screenshot verification at >= 2 viewports for every CSS/layout change. This plan is the goal-backward gate: it produces the deployed-site, screenshot-backed re-score that closes UIR-01 + UIR-03 and proves the phase goal (score >= 20/24, 2-viewport verified on the deployed site) is actually achieved, not just coded.
Output: A "Deployed-Site Re-Score" section appended to `48-AUDIT.md` with per-finding evidence, per-theme rendered-elevation spot-checks, and a blocking checkpoint where the operator deploys + confirms the deployed-site result before close.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/48-ui-audit-remediation/48-AUDIT.md
@.planning/phases/48-ui-audit-remediation/48-CONTEXT.md
@.planning/phases/48-ui-audit-remediation/48-01-SUMMARY.md
@.planning/phases/48-ui-audit-remediation/48-02-SUMMARY.md

<verification_notes>
- UIR-01 SCOPE (the reason this plan owns it): REQUIREMENTS.md UIR-01 and ROADMAP.md Phase 48 require the 6-pillar audit/score against the DEPLOYED deckflow.gg public site. The pre-existing 48-AUDIT.md was run against the LOCAL build (it says so in its header + scope caveat 1). So UIR-01 is closed ONLY when this plan appends a deployed-site, screenshot-backed re-score. Plan 01 does NOT close UIR-01.
- The original audit used gstack headless Chromium, 8 pages x 2 viewports (desktop 1280x720, mobile 375x812), shots in logs/audit-shots/. Reuse that harness/dimensions for an apples-to-apples re-score.
- For the LOCAL pre-check (Task 1) start the local web app WITHOUT popping a Windows browser: set DECKFLOW_DISABLE_AUTO_BROWSER=true (per project memory — this is the live env var name; do NOT use the stale MTGDECKSTUDIO_ name). The FINAL UIR-01 + UIR-03 re-score (Task 2 deployed-site shots) must screenshot the DEPLOYED deckflow.gg, which only the operator can deploy (push to main -> Render auto-deploy; AI does not push main).
- Theme spot-check: there are 24 user-selectable themes (13 full-forks incl. site-commander-table + 11 @import). Capture at least TWO LIGHT themes — one @import/site.css-family (e.g. Classic or Jeskai) AND site-commander-table (a separate full-fork with its own warm parchment --bg/--panel/--muted/--line that Plan 01 edited independently AND that Plan 01 brought into F3 scope by adding --fs-xs) — plus one DARK theme (site-planeswalker-dark), to confirm token changes propagated across BOTH theme architectures and dark-mode panel/bg inversion (Plan 01 Task 2) reads correctly.
- RENDERED-ELEVATION caveat (from Plan 02): most theme files set their own box-shadow on `.deck-form/.result-panel/.hub-card` AFTER site-common.css, so a theme's shadow may override the site-common baseline. That is acceptable. For EACH spot-checked theme, verify in the RENDERED screenshot that cards actually read as elevated — do NOT assume the site-common baseline wins the cascade. Note per theme whether the visible elevation came from site-common or the theme's own shadow.
- Pages to recapture (closing-path coverage): Home (F1/F2/F4), Card Lookup + Ask a Judge (F6/F7), one tool page with panels (Deck Comparison or Primer for F2/F4). Mobile + desktop each.
</verification_notes>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Local pre-check — capture post-remediation screenshots at 2 viewports + per-finding + per-theme verification</name>
  <files>.planning/phases/48-ui-audit-remediation/48-AUDIT.md</files>
  <read_first>
    - .planning/phases/48-ui-audit-remediation/48-AUDIT.md (Findings F1-F7 + original screenshot method/dimensions + scope caveat 1: local vs deployed)
    - .planning/phases/48-ui-audit-remediation/48-01-SUMMARY.md (token propagation table)
    - .planning/phases/48-ui-audit-remediation/48-02-SUMMARY.md (layout/markup changes)
  </read_first>
  <action>
    Build + run the local web app with `DECKFLOW_DISABLE_AUTO_BROWSER=true` (background) on its dev URL. Using the same gstack/headless-Chromium harness and dimensions as the original audit (desktop 1280x720, mobile 375x812), capture post-remediation screenshots into `logs/audit-shots/` (post-remediation subfolder) for: Home, Card Lookup, Ask a Judge, and one panel-heavy tool page (Deck Comparison or Deck Primer) — each at desktop AND mobile. Repeat Home + one tool page under a DARK theme (site-planeswalker-dark) AND under TWO light themes: one site.css-family theme (Classic or a guild @import theme) AND site-commander-table (the separate warm-parchment full-fork edited independently in Plan 01) — to spot-check token propagation across BOTH theme architectures (full-fork + @import) and the commander-table fork specifically.

    For each remediated finding F1-F7, record a one-line verdict + the screenshot path proving it: F1 icons+elevation visible (light+dark); F2 cards separate from bg (token delta + RENDERED resting shadow — note per theme whether the elevation comes from site-common baseline or the theme's own box-shadow override) light+dark incl. commander-table; F3 smallest text >= 12.75px (0.85rem); F4 distinct heading/label tiers; F5 muted text legible/AA (confirm in commander-table's warm palette too); F6 no dead band on short pages; F7 example/next-step panel present. No finding may be marked done on grep alone.

    Append a "Local Pre-Check Re-Score" subsection to 48-AUDIT.md with the 6-pillar table re-scored (target Visuals 3, Color 4, Typography 4 -> total >= 20/24), the per-finding evidence table, and the 2-viewport + light/dark + commander-table theme spot-check note (with the per-theme rendered-elevation source). This LOCAL re-score is the pre-check; the BINDING UIR-01 closure is the deployed-site re-score appended after the Task 2 checkpoint.
  </action>
  <verify>
    <automated>ls logs/audit-shots/ 2>/dev/null | grep -i "post\|remed\|desktop\|mobile" | head</automated>
    <automated>ls logs/audit-shots/ 2>/dev/null | grep -i "commander-table\|commander_table\|commandertable" | head || echo "ensure a site-commander-table light-theme shot exists"</automated>
    <automated>grep -q "Local Pre-Check Re-Score" .planning/phases/48-ui-audit-remediation/48-AUDIT.md && echo "local pre-check re-score subsection appended"</automated>
  </verify>
  <acceptance_criteria>
    - SCREENSHOT: Post-remediation shots exist in logs/audit-shots/ for Home + Card Lookup + Ask a Judge + one panel page, at BOTH desktop (>= 1024px) and mobile (<= 768px).
    - SCREENSHOT: At least one DARK theme (planeswalker-dark) AND two LIGHT themes captured — one site.css-family theme AND site-commander-table (the independent warm-parchment full-fork) — to spot-check token propagation across both theme architectures.
    - SOURCE: 48-AUDIT.md has a "Local Pre-Check Re-Score" subsection: re-scored 6-pillar table totaling >= 20/24 + a per-finding (F1-F7) evidence table referencing screenshot paths; commander-table noted in the F2/F5 token spot-check; F2 row records, per spot-checked theme, whether the RENDERED elevation came from the site-common baseline or the theme's own box-shadow.
    - No finding closed on grep/static analysis alone (UIR-03).
  </acceptance_criteria>
  <done>All closing-path findings are browser-verified locally at 2 viewports across light (incl. commander-table) + dark themes with per-theme rendered elevation confirmed; local pre-check re-score >= 20/24 recorded. (UIR-01 closure is the deployed-site re-score after the checkpoint.)</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Operator deploys + confirms deployed deckflow.gg; close UIR-01 (deployed-site audit) + UIR-03</name>
  <what-built>
    Plans 01+02 implemented the token + layout/markup remediations (F1-F7). Task 1 of this plan re-scored the LOCAL build to >= 20/24 with 2-viewport, light+dark, per-theme-rendered-elevation screenshot evidence (including the site-commander-table fork) appended to 48-AUDIT.md. UIR-01 explicitly requires the 6-pillar audit/score against the DEPLOYED deckflow.gg public site (REQUIREMENTS.md UIR-01 + ROADMAP Phase 48 SC1); the pre-existing 48-AUDIT.md was a LOCAL audit, so UIR-01 is NOT yet closed. The deployed-site re-score requires the operator to deploy (push to main -> Render auto-deploy is an explicit operator action; AI does not push main).
  </what-built>
  <how-to-verify>
    1. Operator pushes the v1.7 branch / merges so the CSS+markup changes deploy to deckflow.gg (operator's explicit push — AI does not push main).
    2. After Render deploy completes, visit https://www.deckflow.gg at desktop (>= 1024px) and mobile (<= 768px):
       - Home: hub cards show icons + elevation; cards clearly separate from the page background; section labels read as a distinct tier.
       - Switch to a dark theme (e.g. planeswalker-dark) and to site-commander-table (warm parchment) and one other guild theme: confirm panels still lift (dark inversion correct, commander-table panel/bg delta now visible, RENDERED elevation present whether from site-common or the theme's own shadow) and icons inherit theme color.
       - Card Lookup / Ask a Judge: content is capped/centered and ends with an example/next-step panel (no large dead band).
       - Smallest helper/footer text is comfortably legible (>= ~12.75px / 0.85rem) and muted text is readable (incl. commander-table's warm muted).
    3. Confirm the deployed result matches the local re-score (>= 20/24) and no finding regressed.
    4. After confirmation, the executor appends a binding "Deployed-Site Re-Score" section to 48-AUDIT.md (deployed-deckflow.gg 6-pillar table >= 20/24 + per-finding + per-theme rendered-elevation evidence, scoped to the deployed site) — this section is what CLOSES UIR-01.
  </how-to-verify>
  <acceptance_criteria>
    - Operator confirms the deployed deckflow.gg site reflects the remediations at both viewports across light (incl. commander-table) + dark themes, with rendered card elevation present per spot-checked theme.
    - 48-AUDIT.md gains a "Deployed-Site Re-Score" section scoped to deckflow.gg showing >= 20/24 — closing UIR-01 (deployed-site audit/score) and UIR-03 (2-viewport browser verification).
    - Operator confirms the deployed score is >= 20/24 (SC2) — or reports specific regressions for a gap-closure pass.
  </acceptance_criteria>
  <resume-signal>Type "approved" once deckflow.gg is verified at >= 20/24 at both viewports (light incl. commander-table + dark) and the Deployed-Site Re-Score section is appended, or describe the regressions to fix.</resume-signal>
</task>

</tasks>

<verification>
- Post-remediation screenshots exist at 2 viewports for the closing-path pages, including a dark theme + two light themes (one site.css-family + site-commander-table) for the fork spot-check, with per-theme rendered elevation confirmed.
- 48-AUDIT.md "Local Pre-Check Re-Score" subsection shows >= 20/24, and a binding "Deployed-Site Re-Score" section (scoped to deckflow.gg) shows >= 20/24 — the latter closes UIR-01.
- Operator has confirmed the DEPLOYED deckflow.gg result (UIR-01 / UIR-03 / SC3) at both viewports.
</verification>

<success_criteria>
- UIR-01 closed: a 6-pillar audit/score against the DEPLOYED deckflow.gg site is recorded (>= 20/24), per the requirement's deployed-site scope.
- UIR-02 closing-path verified achieved: total audit score >= 20/24.
- UIR-03 satisfied: every remediated finding screenshot-verified at mobile + desktop; final score confirmed on the deployed site; per-theme rendered elevation confirmed (no finding closed on grep/assumed cascade alone).
</success_criteria>

<output>
Create `.planning/phases/48-ui-audit-remediation/48-03-SUMMARY.md` when done.
</output>
</content>
