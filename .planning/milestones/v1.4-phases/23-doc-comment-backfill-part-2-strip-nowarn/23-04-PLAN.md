---
phase: 23-doc-comment-backfill-part-2-strip-nowarn
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - DeckFlow.Web/Services/ICategoryKnowledgeStore.cs
  - DeckFlow.Web/Services/CategoryKnowledgeStore.cs
  - DeckFlow.Web/Services/IFeedbackStore.cs
  - DeckFlow.Web/Services/FeedbackStore.cs
  - DeckFlow.Web/Services/EdhTop16Client.cs
  - DeckFlow.Web/Services/ScryfallSetService.cs
  - DeckFlow.Web/Services/DeckFlowDatabaseConnectionFactory.cs
  - DeckFlow.Web/Services/AdminBruteForceTrackerStore.cs
  - DeckFlow.Web/Services/HelpContentService.cs
  - DeckFlow.Web/Services/VersionService.cs
  - DeckFlow.Web/Services/IVersionService.cs
  - DeckFlow.Web/Services/Harvest/HarvestStatsAggregator.cs
  - DeckFlow.Web/Services/CardLookupService.cs
  - DeckFlow.Web/Services/MetaGapService.cs
  - DeckFlow.Web/Services/DeckComparisonService.cs
  - DeckFlow.Web/Infrastructure/BasicAuthMiddleware.cs
autonomous: true
requirements: [DOC-01]
must_haves:
  truths:
    - "Every public Service/Infrastructure type AND public member carries an attached XML doc-comment (<summary> or <inheritdoc/>)"
    - "All 22 CS1573 sites get a COMPLETE <param> set — every positional param / method param documented when ANY is documented (overrides Phase 17 D-02 partial-param policy per LD-1)"
    - "A from-clean suppressors-off build emits zero CS1591/CS1573/CS1587 for Services + Infrastructure"
    - "No { get; init; }→{ get; } mutation; no [Attribute] inlined; LF preserved"
  artifacts:
    - path: "DeckFlow.Web/Services/ICategoryKnowledgeStore.cs"
      provides: "Complete <param> sets closing CS1573 gaps (9 sites)"
    - path: "DeckFlow.Web/Infrastructure/BasicAuthMiddleware.cs"
      provides: "Type + member <summary> (3 sites)"
  key_links:
    - from: "Services + Infrastructure"
      to: "the Wave-2 suppressor-flip gate (23-05)"
      via: "0 CS1591/1573/1587 (incl. 0 CS1573) when both suppressors are removed"
      pattern: "warning CS(1591|1573|1587)"
---

<objective>
Backfill XML doc-comments across Services (49 sites) + Infrastructure (3 sites) = 52 of the 475
authoritative sites (23-RESEARCH.md per-file inventory). This plan owns the entire CS1573 class —
the 22 missing-`<param>` sites concentrated on Phase-17-"done" interfaces.

CS1573 is the one place this phase OVERRIDES Phase 17's D-02 partial-`<param>` policy (LD-1):
when a method/record carries ANY `<param>`, the compiler demands EVERY param be documented
(all-or-nothing). Complete the set (23-RESEARCH "CS1573 fix shape" + Pitfall 2). The affected files
are the Phase-17 interfaces: ICategoryKnowledgeStore, IFeedbackStore, EdhTop16Client,
ScryfallSetService, CategoryKnowledgeStore.

Purpose: DOC-01 (Services + Infrastructure complete + CS1573 cleared before the gate flips in 23-05).
Output: all listed files documented at type + member level with complete param sets; 0 CS1573.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/23-doc-comment-backfill-part-2-strip-nowarn/23-RESEARCH.md
@.planning/phases/17-doc-comment-backfill-part-1-controllers-services/17-CONTEXT.md
@.planning/phases/17-doc-comment-backfill-part-1-controllers-services/17-02-SUMMARY.md
@./CLAUDE.md

<cs1573_fix_shape>
From 23-RESEARCH.md "CS1573 fix shape (complete the param set)":

  BEFORE (CS1573 — only Ip documented):
  /// <summary>Captures request-side metadata stored with a feedback submission.</summary>
  /// <param name="Ip">Raw client IP; salted+hashed before persistence.</param>
  public sealed record FeedbackRequestContext(string? Ip, string? UserAgent, string? PageUrl, string? AppVersion);

  AFTER (every positional param documented):
  /// <summary>...</summary>
  /// <param name="Ip">...</param>
  /// <param name="UserAgent">Client User-Agent header, or null when absent.</param>
  /// <param name="PageUrl">Page the feedback was submitted from, or null.</param>
  /// <param name="AppVersion">App version string captured at submit time, or null.</param>
  public sealed record FeedbackRequestContext(string? Ip, string? UserAgent, string? PageUrl, string? AppVersion);
</cs1573_fix_shape>

<conventions>
Phase 17 D-01 (interface owns prose; impl uses `<inheritdoc/>`) + the LD-1 amendment to D-02 above.
The interface/impl pairs here (ICategoryKnowledgeStore/CategoryKnowledgeStore,
IFeedbackStore/FeedbackStore, IVersionService/VersionService, IScryfallSetService/ScryfallSetService,
IEdhTop16Client/EdhTop16Client) carry prose on the interface; impl members use `<inheritdoc/>`.
Phase 17 already documented these TYPES — complete only the bare members / incomplete param sets.

MEMBER-COVERAGE RULE (mandatory — CS1591 fires on EVERY public declaration, not just the type):
Document EVERY public declaration in the allowed files, including public classes, records,
interfaces, enums, enum members, constructors, methods, properties, and positional record
parameters — not just the top-level type. The per-file site counts in 23-RESEARCH.md are
compiler-derived (the compiler counts members), so each listed count already reflects the full
per-member inventory; documenting only the top-level type will leave warnings behind and fail the
23-05 gate.

STRICT-PARAM RULE (the CS1573 invariant this plan owns — restate it on every method you touch): If
ANY `<param>` is present on a member, EVERY parameter on that member must have a matching `<param>`
tag (all-or-nothing — this overrides Phase 17 D-02's partial-param policy per LD-1). A partial
`<param>` set IS a CS1573. Either document ALL params on a member or NONE; never a partial set.
</conventions>

<formatting_landmines>
CLAUDE.md R-6 — touch ONLY the `///` lines you add:
- NEVER `{ get; init; }` → `{ get; }`. NEVER inline `[Attribute]`. NEVER re-indent raw-strings.
- Preserve LF; NO Format Document. Attached (no blank line between `///` and decl).
</formatting_landmines>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Close all 22 CS1573 param gaps on the Phase-17 interface/impl pairs</name>
  <files>DeckFlow.Web/Services/ICategoryKnowledgeStore.cs, DeckFlow.Web/Services/CategoryKnowledgeStore.cs, DeckFlow.Web/Services/IFeedbackStore.cs, DeckFlow.Web/Services/FeedbackStore.cs, DeckFlow.Web/Services/EdhTop16Client.cs, DeckFlow.Web/Services/ScryfallSetService.cs</files>
  <action>
ALLOWED FILE SET (this task may edit ONLY these 6 files — ignore every other file/task):
  ICategoryKnowledgeStore.cs (9), CategoryKnowledgeStore.cs (6), IFeedbackStore.cs (6),
  FeedbackStore.cs (3), EdhTop16Client.cs (2), ScryfallSetService.cs (1).
For every CS1573 site, COMPLETE the `<param>` set (all-or-nothing — see <cs1573_fix_shape>): every
method/record param documented wherever any param is documented. Add any missing member `<summary>`
/ `<inheritdoc/>` too. Prose on the interface, `<inheritdoc/>` on the impl (D-01). Touch ONLY the
`///` lines you add.
  </action>
  <verify>
    <automated>for f in ICategoryKnowledgeStore CategoryKnowledgeStore IFeedbackStore FeedbackStore EdhTop16Client ScryfallSetService; do echo "== $f =="; echo "param tags:"; grep -c '/// <param' "DeckFlow.Web/Services/$f.cs"; echo "summary/inheritdoc:"; grep -c '/// <summary>\|/// <inheritdoc' "DeckFlow.Web/Services/$f.cs"; done</automated>
  </verify>
  <done>Every documented method/record in these 6 files has a complete `<param>` set; git diff shows only added `///` lines.</done>
</task>

<task type="auto">
  <name>Task 2: Document remaining Services + Infrastructure types/members</name>
  <files>DeckFlow.Web/Services/DeckFlowDatabaseConnectionFactory.cs, DeckFlow.Web/Services/AdminBruteForceTrackerStore.cs, DeckFlow.Web/Services/HelpContentService.cs, DeckFlow.Web/Services/VersionService.cs, DeckFlow.Web/Services/IVersionService.cs, DeckFlow.Web/Services/Harvest/HarvestStatsAggregator.cs, DeckFlow.Web/Services/CardLookupService.cs, DeckFlow.Web/Services/MetaGapService.cs, DeckFlow.Web/Services/DeckComparisonService.cs, DeckFlow.Web/Infrastructure/BasicAuthMiddleware.cs</files>
  <action>
ALLOWED FILE SET (this task may edit ONLY these 10 files — ignore every other file/task):
  Services/AdminBruteForceTrackerStore.cs (5), Services/HelpContentService.cs (4),
  Services/VersionService.cs + IVersionService.cs (3+1), Services/Harvest/HarvestStatsAggregator.cs (3),
  Services/DeckFlowDatabaseConnectionFactory.cs (2, member-level — type was done in Phase 17),
  Services/CardLookupService.cs (2), Services/MetaGapService.cs (1), Services/DeckComparisonService.cs (1),
  Infrastructure/BasicAuthMiddleware.cs (3).
Add attached `<summary>`/`<inheritdoc/>` on EVERY undocumented public declaration per the
MEMBER-COVERAGE RULE — public types, constructors, methods, properties, any co-located public
classes/records/enums + their members. DeckFlowDatabaseConnectionFactory's TYPE was documented in
Phase 17 — only its bare members remain. D-02 `<param>`/`<returns>` on non-trivial methods, subject
to the STRICT-PARAM RULE: if you add ANY `<param>` to a method, document EVERY parameter on that
method (all-or-nothing) — a partial set IS a fresh CS1573 the moment the 23-05 suppressors flip.
Touch ONLY the `///` lines you add.
  </action>
  <verify>
    <automated>for f in Services/AdminBruteForceTrackerStore Services/HelpContentService Services/VersionService Services/IVersionService Services/Harvest/HarvestStatsAggregator Services/DeckFlowDatabaseConnectionFactory Services/CardLookupService Services/MetaGapService Services/DeckComparisonService Infrastructure/BasicAuthMiddleware; do echo "== $f =="; grep -c '/// <summary>\|/// <inheritdoc' "DeckFlow.Web/$f.cs"; done</automated>
  </verify>
  <done>All 10 files documented at type + member level; git diff shows only added `///` lines.</done>
</task>

</tasks>

<verification>
The per-file grep counts in the task verify blocks are SMOKE checks only — they confirm a `<summary>`
exists but do NOT prove every public member is documented or every `<param>` set is complete. The
AUTHORITATIVE per-member gate is 23-05's suppressors-off build promoting ALL THREE codes
(CS1591/CS1573/CS1587) and returning 0 for Services + Infrastructure (in particular 0 CS1573).
`dotnet build -c Release` (Windows dotnet.exe) stays 0/0 with NoWarn still present this wave.
</verification>

<success_criteria>
- All Services + Infrastructure files documented on EVERY public declaration (52 sites).
- 0 CS1573 remaining — every documented method/record carries a complete `<param>` set (LD-1 override; STRICT-PARAM RULE).
- `git diff` shows only added `///` lines.
- `dotnet build -c Release` stays 0 warnings / 0 errors.
- The grep checks are SMOKE checks only; the authoritative per-member gate is 23-05's suppressors-off all-three-code build.
</success_criteria>

<output>
Create `.planning/phases/23-doc-comment-backfill-part-2-strip-nowarn/23-04-SUMMARY.md` when done.
</output>
