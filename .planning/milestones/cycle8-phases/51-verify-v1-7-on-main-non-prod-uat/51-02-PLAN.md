---
phase: 51-verify-v1-7-on-main-non-prod-uat
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/51-verify-v1-7-on-main-non-prod-uat/51-STUDIO-UAT-RESULTS.md
autonomous: false
requirements: [HARD-01]
must_haves:
  truths:
    - "Operator confirms DeckFlow.Studio boots, renders its first page at http://localhost:5271, and logs 'Studio prod connection: configured/not configured' with no raw connection string in stdout (P41)"
    - "Operator confirms the Harvest page re-distill double-confirm runs a real re-distill, the monthly cap blocks/raises correctly and the session override does NOT persist across a Studio restart, and a mid-harvest tab close produces zero ObjectDisposedException / unobserved exceptions (P45)"
    - "Operator confirms the Review queue interactive flow (filter/badge/expand/batch approve-reject) works and the Publish page exports an LF-normalized approved-only seed, shows a HEAD diff, gates Commit behind the reviewed-diff checkbox, and never pushes (P46)"
    - "Each of the P41/P45/P46 smoke results (PASS / FAIL / WAIVED-with-reason) is recorded in 51-STUDIO-UAT-RESULTS.md"
  artifacts:
    - path: ".planning/phases/51-verify-v1-7-on-main-non-prod-uat/51-STUDIO-UAT-RESULTS.md"
      provides: "Recorded P41 / P45 / P46 Studio operator smoke results"
      contains: "P41"
  key_links:
    - from: "51-STUDIO-UAT-RESULTS.md"
      to: "45-VERIFICATION.md / 46-VERIFICATION.md human_verification"
      via: "smokes recorded against the deferred P41/P45/P46 gates"
      pattern: "P45"
---

<objective>
Close the three deferred Studio non-prod operator-UAT smokes on the v1.7-on-main build: Studio runtime render (P41), re-distill E2E + harvest-cap-persist-on-restart + cancel-on-circuit-drop (P45), and Review queue interactive + real-git Publish commit flow (P46). This is the Studio-side slice of HARD-01.

Purpose: HARD-01 requires these deferred Studio smokes to pass and be recorded. The bUnit/unit suites already pass (Studio 34/34); these gates cover live browser interaction, a live LLM re-distill, restart-non-persistence, circuit-drop disposal safety, and real on-disk git/LF behavior that automation cannot reach.
Output: Recorded P41/P45/P46 results in `51-STUDIO-UAT-RESULTS.md`. No product code changes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/41-studio-scaffold-secrets-wiring/41-VERIFICATION.md
@.planning/phases/45-harvest-distill-ui/45-VERIFICATION.md
@.planning/phases/46-review-queue-commit-publish-path/46-VERIFICATION.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Operator smoke — Studio runtime render (P41)</name>
  <read_first>
    - .planning/phases/41-studio-scaffold-secrets-wiring/41-VERIFICATION.md (the two human_verification steps + expected log line)
    - DeckFlow.Studio/Properties/launchSettings.json (applicationUrl is http://localhost:5271, launchBrowser:false)
    - DeckFlow.Studio/Program.cs:118-119 (the presence-only "Studio prod connection: {Status}" / "Studio SCP: {Status}" log lines)
  </read_first>
  <what-built>Phase 41 scaffolded DeckFlow.Studio (Blazor Server, net10.0) with the prod connection string routed only through user-secrets and a presence-only startup log. The runtime render + log-hygiene check was deferred to operator UAT.</what-built>
  <how-to-verify>
    1. From the repo root on `main`, optionally set the Studio prod connection string via `dotnet user-secrets set "Studio:ProdConnectionString" "<conn>" --project DeckFlow.Studio/DeckFlow.Studio.csproj` (or leave unset to verify the "not configured" path).
    2. Start Studio with the auto-browser suppressed: DECKFLOW_DISABLE_AUTO_BROWSER=true, run DeckFlow.Studio, open http://localhost:5271.
    3. Confirm the first page renders the Studio heading and "Studio is running." content.
    4. In the `dotnet run` stdout, confirm the line "Studio prod connection: configured" (or "not configured") appears and that NO raw connection string value (no "Host=", "Password=", "postgres") is present anywhere in stdout.
  </how-to-verify>
  <action>Run the P41 smoke above on the v1.7-on-main build. Record PASS / FAIL / WAIVED-with-reason, the date, the observed render text, and an explicit "no raw connection string in stdout" confirmation under a "## P41 — Studio runtime render" heading in 51-STUDIO-UAT-RESULTS.md. Do NOT paste any secret value into the results file.</action>
  <acceptance_criteria>
    - Studio reachable at http://localhost:5271 with the first page rendered.
    - stdout contains "Studio prod connection: configured|not configured" and contains no "Host="/"Password="/"postgres" connection-string fragment.
    - 51-STUDIO-UAT-RESULTS.md has a "## P41" section with a result line (PASS/FAIL/WAIVED) and date.
  </acceptance_criteria>
  <resume-signal>Type "approved" with the P41 result (or "fail: <defect>").</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Operator smoke — re-distill E2E + cap-persist + cancel-on-dispose (P45)</name>
  <read_first>
    - .planning/phases/45-harvest-distill-ui/45-VERIFICATION.md (the three human_verification items: re-distill end-to-end, cap-exceeded block + session-override non-persistence, cancel-on-dispose)
    - .planning/phases/45-harvest-distill-ui/45-03-SUMMARY.md (the "no enabled source" caveat that left these non-conclusive at phase close)
  </read_first>
  <what-built>Phase 45 shipped the Studio Harvest page: channel browse, paste queue, harvested/distilled badges, live harvest progress, a dry-run spend gate, a re-distill double-confirm flow, a monthly-cap block with a session override, and CancellationToken wired to component disposal. The three live behaviors below were deferred (no already-distilled video / no enabled source / restart-non-persistence cannot be automated).</what-built>
  <how-to-verify>
    1. Start Studio with DECKFLOW_DISABLE_AUTO_BROWSER=true against a local data dir that has at least one enabled YouTube source and at least one already-distilled video (auto-ensure will create a source from a browsed channel; distill one video first if needed). Use the subscription distill provider (DECKFLOW_LLM_PROVIDER=claude) so live spend is $0.
    2. Re-distill E2E: select an already-distilled video, confirm the amber re-distill banner, check both confirm checkboxes, run Estimate Spend (dry-run) and confirm WouldRun=1; then run the live distill and confirm VideosDistilled=1 with the "re-distilling {videoId} (redistill=true)" console line and fresh distill output (prior output cleared and replaced via ClearDistillOutputAsync).
    3. Cap block + override non-persistence: configure a selection whose projected spend exceeds remaining cap; confirm the red cap-exceeded alert blocks Stage B / Run Distill; use "Raise cap (this session)" to clear the block; then restart Studio and confirm the cap display reverts to $15.00 (or the DECKFLOW_LLM_MONTHLY_CAP_USD env value), NOT the raised value.
    4. Cancel-on-dispose: start a harvest, close the browser tab mid-run, and confirm the Studio console logs OperationCanceledException with ZERO ObjectDisposedException and ZERO unobserved TaskException from late progress callbacks.
  </how-to-verify>
  <action>Run all three P45 sub-smokes on the v1.7-on-main build. **Prerequisite (data setup):** the re-distill smoke needs at least one enabled YouTube source + one already-distilled video. If the local data dir lacks them, the operator runs a one-off browse+distill to seed the fixture FIRST — this setup run (subscription provider `DECKFLOW_LLM_PROVIDER=claude`, $0 spend) is a WAIVED-setup-step, recorded as such, and is NOT counted as a smoke pass or fail. Record each sub-result (PASS / FAIL / WAIVED-with-reason) with date and observed evidence (WouldRun/VideosDistilled counts, cap value before/after restart, console-exception cleanliness) under a "## P45 — re-distill / cap / cancel" heading in 51-STUDIO-UAT-RESULTS.md. If a sub-smoke is genuinely unrunnable (e.g. no distillable source available and setup not possible), mark it WAIVED with the specific reason rather than PASS. Any defect becomes a follow-up finding — do NOT fix product code here.</action>
  <acceptance_criteria>
    - Re-distill: dry-run WouldRun=1 and live VideosDistilled=1 with the "redistill=true" console line on an already-distilled video.
    - Cap: block enforced when projected>remaining; block clears on session raise; cap reverts to env/default after Studio restart (override did NOT persist).
    - Cancel: Studio console clean after mid-harvest tab close — zero ObjectDisposedException / zero unobserved exceptions.
    - 51-STUDIO-UAT-RESULTS.md "## P45" section records all three sub-results with date and evidence; WAIVED entries name the blocking reason.
  </acceptance_criteria>
  <resume-signal>Type "approved" with the three P45 sub-results (or "fail/waived: <detail>").</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Operator smoke — Review queue interactive + Publish git/LF flow (P46)</name>
  <read_first>
    - .planning/phases/46-review-queue-commit-publish-path/46-VERIFICATION.md (the two human_verification items: Review interactive behavior, Publish branch/seed/LF/diff/checkbox/no-push behavior)
    - .planning/phases/46-review-queue-commit-publish-path/46-04-SUMMARY.md (Publish export+diff+gated-commit, never-pushes design D-04)
  </read_first>
  <what-built>Phase 46 shipped the Studio Review queue (status tabs, badges, expand with artifact preview, optimistic + batch approve/reject) and the Publish page (Export & Preview Diff writes an LF-normalized approved-only index-seed.json + copies artifacts into the repo tree, shows a HEAD diff, gates Commit behind a reviewed-diff checkbox, and never runs git push). Interactive browser + real on-disk git/LF behavior was deferred.</what-built>
  <how-to-verify>
    1. Start Studio with DECKFLOW_DISABLE_AUTO_BROWSER=true from the repo root and open /review. With distilled rows present: confirm the Pending tab is active on load, tab count badges match row counts, tab switch filters + clears selections, Approve flips a row's badge to Approved with a table-success tint and disables its Approve button, Reject tints red, expanding a row with a real artifact shows the markdown in the Content Preview <pre> (NOT "Artifact missing"), expanding a renamed/removed-artifact row shows the alert-warning, and the batch bar appears + Approve Selected updates all + clears selections.
    2. Open /publish. Confirm the current branch + resolved repo root + approved count display. Click Export & Preview Diff, then in WSL confirm `git status --short content-kb/` shows the seed + copied artifacts in the working tree, and `file content-kb/seed/index-seed.json` reports "ASCII text" (NOT "with CRLF line terminators").
    3. Missing-source block: rename one approved artifact source under the Studio data dir, re-export, and confirm a publish-blocking alert-danger naming the missing artifact (no diff/commit shown). Re-export with no approval changes → Updated count = 0.
    4. Commit gate + no-push: confirm Commit is disabled until "I have reviewed the diff above" is checked; stage an unrelated file then click Commit and confirm the foreign-staged guard fires (or `git log -1 --stat` shows only seed + artifacts); after a successful commit confirm a SHA is shown, the push reminder reads "git push origin {branch}", and NO push button exists anywhere. IMPORTANT: do not push; reset/discard any throwaway commit afterward if it was made only to exercise the gate.
  </how-to-verify>
  <action>Run the P46 Review + Publish smokes on the v1.7-on-main build. Record PASS / FAIL / WAIVED-with-reason with date and observed evidence (LF `file` output, `git status` materialization, Updated=0 on re-export, no-push confirmation) under a "## P46 — Review + Publish" heading in 51-STUDIO-UAT-RESULTS.md. Any throwaway commit made only to exercise the commit gate MUST be discarded; this phase commits no seed/content changes. Defects become follow-up findings — do NOT fix product code here.</action>
  <acceptance_criteria>
    - Review: Pending active on load; badges match counts; approve/reject + tint + batch flow work; real artifact previews render (no false "Artifact missing").
    - Publish: `file content-kb/seed/index-seed.json` reports LF "ASCII text"; `git status` shows repo-tree materialization; missing-source blocks publish; Updated=0 on no-change re-export; Commit gated by checkbox; no push button; foreign-staged guard fires.
    - 51-STUDIO-UAT-RESULTS.md "## P46" section records the result with date and evidence; any gate-exercising commit was discarded.
  </acceptance_criteria>
  <resume-signal>Type "approved" with the P46 result (or "fail/waived: <detail>"); confirm no throwaway commit remains.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → Studio (localhost) | Studio binds localhost only; secrets via user-secrets |
| Studio → local git working tree | Publish writes seed/artifacts + may commit (never pushes) |
| Studio stdout → results artifact | Presence-only logging must not leak the prod connection string |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-51-02-01 | Information disclosure | Prod connection string in user-secrets / stdout | mitigate | P41 smoke explicitly asserts no "Host="/"Password="/"postgres" in stdout; no secret value is ever written into 51-STUDIO-UAT-RESULTS.md |
| T-51-02-02 | Tampering | Accidental git push / stray commit from Publish smoke | mitigate | P46 smoke confirms no push button exists; any gate-exercising commit is discarded; AI never pushes main per project rules |
| T-51-02-03 | Repudiation | Re-distill LLM spend | accept | Smoke uses the subscription provider (DECKFLOW_LLM_PROVIDER=claude) so live re-distill spend is $0; dry-run gate precedes any live run |
| T-51-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan — verification only |
</threat_model>

<verification>
- 51-STUDIO-UAT-RESULTS.md exists with "## P41", "## P45", and "## P46" result sections.
- No files under DeckFlow.Studio/ or content-kb/ are committed by this plan (verification-only; any throwaway Publish commit discarded).
</verification>

<success_criteria>
The deferred P41, P45, and P46 Studio operator smokes are run on the v1.7-on-main build and each result (PASS/FAIL/WAIVED-with-reason) is recorded in 51-STUDIO-UAT-RESULTS.md.
</success_criteria>

<output>
Create `.planning/phases/51-verify-v1-7-on-main-non-prod-uat/51-02-SUMMARY.md` when done.
</output>
