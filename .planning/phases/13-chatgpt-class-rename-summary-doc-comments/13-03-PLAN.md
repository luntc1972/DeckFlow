---
phase: 13-chatgpt-class-rename-summary-doc-comments
plan: 03
type: execute
wave: 3
depends_on:
  - 13-01
  - 13-02
files_modified:
  - DeckFlow.Web/Controllers/DeckController.cs
  - DeckFlow.Web/Views/Deck/DeckAnalysis.cshtml
  - DeckFlow.Web/Views/Deck/DeckComparison.cshtml
  - DeckFlow.Web/Views/Deck/CedhMetaGap.cshtml
  - DeckFlow.Web/Views/Shared/_DeckToolTabs.cshtml
  - DeckFlow.Web/Views/Shared/_BracketCallout.cshtml
autonomous: true
requirements:
  - CLASSRENAME-01
  - CLASSRENAME-02
  - CLASSRENAME-03

must_haves:
  truths:
    - "DeckController.cs has all 12 action methods renamed in lockstep with Wave 2 service renames per the action-method map (3 GET + 9 POST = ChatGptPackets to DeckAnalysis, ChatGptPacketsDownload to DeckAnalysisDownload, etc.)"
    - "DeckController.cs has all ChatGpt-prefixed type identifiers in method bodies, ctor parameters, private fields, and View(...) second-arg constructions renamed to Wave-1 and Wave-2 names"
    - "All 39 View(\"DeckAnalysis\"|\"DeckComparison\"|\"CedhMetaGap\", ...) literal-string view names stay BYTE-IDENTICAL (Phase 12 invariant per Pitfall 2)"
    - "All [HttpGet(\"/deck-analysis\")] / [HttpPost(\"/deck-comparison\")] / etc. route attributes stay byte-identical (URL slugs locked by Phase 12)"
    - "3 Razor view @model directives updated to renamed ViewModel types (DeckAnalysis.cshtml to DeckAnalysisViewModel, DeckComparison.cshtml to DeckComparisonViewModel, CedhMetaGap.cshtml to MetaGapViewModel)"
    - "_DeckToolTabs.cshtml's 4 references to old DeckPageTab enum values replaced with new enum values (DeckAnalysis / DeckComparison / CedhMetaGap)"
    - "_BracketCallout.cshtml's 3 prose-comment references to ChatGptPackets.cshtml updated to DeckAnalysis.cshtml"
    - "_AiSelector.cshtml is NOT touched (read-only in Phase 13 per Pitfall 4 — preserves id=ai-chatgpt, value=ChatGPT, etc.)"
    - "Form-field name=... attributes on Razor forms stay BYTE-IDENTICAL (DeckText, TargetAiPlatform, WorkflowStep, etc. — property names not renamed in Phase 13)"
    - "All chatgpt-prefixed CSS classes (chatgpt-packets-form, chatgpt-layout-picker, etc.) and data attributes (data-chatgpt-*, data-cache-key=chatgpt-packets) inside the 3 main views stay BYTE-IDENTICAL (D-08 — deferred to Phase 16)"
  artifacts:
    - path: "DeckFlow.Web/Controllers/DeckController.cs"
      provides: "12 action methods with AI-agnostic names + all ChatGpt-prefixed type identifiers in method bodies replaced; ctor parameter types reference renamed service interfaces (IDeckAnalysisPacketService, IDeckComparisonService, IMetaGapService)"
      contains: "public IActionResult DeckAnalysis"
    - path: "DeckFlow.Web/Views/Deck/DeckAnalysis.cshtml"
      provides: "@model DeckFlow.Web.Models.DeckAnalysisViewModel directive on line 1"
      contains: "@model DeckFlow.Web.Models.DeckAnalysisViewModel"
    - path: "DeckFlow.Web/Views/Deck/DeckComparison.cshtml"
      provides: "@model DeckFlow.Web.Models.DeckComparisonViewModel directive on line 1"
      contains: "@model DeckFlow.Web.Models.DeckComparisonViewModel"
    - path: "DeckFlow.Web/Views/Deck/CedhMetaGap.cshtml"
      provides: "@model DeckFlow.Web.Models.MetaGapViewModel directive on line 1"
      contains: "@model DeckFlow.Web.Models.MetaGapViewModel"
    - path: "DeckFlow.Web/Views/Shared/_DeckToolTabs.cshtml"
      provides: "Enum-value checks reference DeckPageTab.DeckAnalysis / DeckPageTab.DeckComparison / DeckPageTab.CedhMetaGap"
      contains: "DeckPageTab.DeckAnalysis"
  key_links:
    - from: "DeckFlow.Web/Controllers/DeckController.cs"
      to: "DeckFlow.Web/Services/DeckAnalysisPacketService.cs"
      via: "ctor parameter IDeckAnalysisPacketService _deckAnalysisPacketService"
      pattern: "IDeckAnalysisPacketService"
    - from: "DeckFlow.Web/Controllers/DeckController.cs"
      to: "DeckFlow.Web/Services/DeckComparisonService.cs"
      via: "ctor parameter IDeckComparisonService"
      pattern: "IDeckComparisonService"
    - from: "DeckFlow.Web/Controllers/DeckController.cs"
      to: "DeckFlow.Web/Services/MetaGapService.cs"
      via: "ctor parameter IMetaGapService"
      pattern: "IMetaGapService"
    - from: "DeckFlow.Web/Views/Deck/DeckAnalysis.cshtml"
      to: "DeckFlow.Web/Models/DeckAnalysisViewModel.cs"
      via: "@model directive"
      pattern: "@model DeckFlow.Web.Models.DeckAnalysisViewModel"
---

<objective>
Update `DeckFlow.Web/Controllers/DeckController.cs` (142 ChatGpt hits) to reference the Wave-1 + Wave-2 renamed type identifiers: 12 action methods renamed in lockstep with the URL slugs locked by Phase 12 (3 GET + 9 POST), all ctor parameter types + private field types + method-body identifier references updated, all `View("...", new XViewModel { ... })` second-arg constructions updated to renamed Wave-1 ViewModel names while the `View("DeckAnalysis"|"DeckComparison"|"CedhMetaGap", ...)` LITERAL view-name strings stay BYTE-IDENTICAL (Phase 12 invariant per Pitfall 2). Update 3 main Razor views' `@model` directives, the `_DeckToolTabs.cshtml` enum-value references, and the `_BracketCallout.cshtml` prose comment references. `_AiSelector.cshtml` stays READ-ONLY per Pitfall 4.

Purpose: With Wave 1 (Models) + Wave 2 (Services) renames in place, the controller is the single remaining code-side consumer of the old type names. After this wave, the build should compile clean except for test-project errors (Wave 4 closes the loop).

Output: 1 updated DeckController.cs, 3 updated `.cshtml` `@model` directives, 1 updated `_DeckToolTabs.cshtml` enum-value sweep, 1 updated `_BracketCallout.cshtml` prose-comment update. **Build at end of this wave should compile DeckFlow.Web clean** (test project still red — Wave 4 closes it).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/13-chatgpt-class-rename-summary-doc-comments/13-CONTEXT.md
@.planning/phases/13-chatgpt-class-rename-summary-doc-comments/13-RESEARCH.md
@.planning/phases/13-chatgpt-class-rename-summary-doc-comments/13-PATTERNS.md
@.planning/phases/13-chatgpt-class-rename-summary-doc-comments/13-01-SUMMARY.md
@.planning/phases/13-chatgpt-class-rename-summary-doc-comments/13-02-SUMMARY.md
@.planning/codebase/CONVENTIONS.md
@CLAUDE.md

<interfaces>
Wave 1 + Wave 2 renamed identifiers available to this wave:

Service interfaces (Wave 2):
- IDeckAnalysisPacketService (was IChatGptDeckPacketService)
- IDeckComparisonService (was IChatGptDeckComparisonService)
- IMetaGapService (was IChatGptCedhMetaGapService)

Service classes (Wave 2):
- DeckAnalysisPacketService (was ChatGptDeckPacketService)
- DeckComparisonService (was ChatGptDeckComparisonService)
- MetaGapService (was ChatGptCedhMetaGapService)

Service result records (Wave 2):
- DeckAnalysisPacketResult (was ChatGptDeckPacketResult)
- DeckComparisonResult (was ChatGptDeckComparisonResult)
- MetaGapResult (was ChatGptCedhMetaGapResult)

Static helper classes (Wave 2):
- PacketArtifactStore (was ChatGptPacketArtifactStore)
- RequestContextParser (was ChatGptRequestContextParser)
- ResponseParsers (was ChatGptResponseParsers)
- JsonTextFormatterService (was ChatGptJsonTextFormatterService)
- Constant: ResultWrapInstruction (was ChatGptResultWrapInstruction)

Model types (Wave 1):
- Requests: DeckAnalysisRequest, DeckComparisonRequest, MetaGapRequest
- ViewModels: DeckAnalysisViewModel, DeckComparisonViewModel, MetaGapViewModel
- Responses: DeckAnalysisResponse (+ WeakSlot, QuestionAnswer, DeckVersion); SetUpgradeResponse (+ SetUpgradeSet, SetUpgradeTopAdd, SetUpgradeCardNote, SetUpgradeShortlist); DeckComparisonResponse (+ DeckComparisonRecommendation); MetaGapResponse (+ MetaGapData, WinLineSet, WinLines, Interaction, Speed, ManaEfficiency, CoreConvergenceCard, MissingStaple, PotentialCut, TopAdd, TopCut)

Enum (Wave 1):
- DeckPageTab.DeckAnalysis = 5 (was ChatGptPackets)
- DeckPageTab.DeckComparison = 8 (was ChatGptDeckComparison)
- DeckPageTab.CedhMetaGap = 9 (was ChatGptCedhMetaGap)
</interfaces>

<action_method_rename_map>
Source: 13-RESEARCH.md L478-490

| Old action | New action | Line | Route attr (UNCHANGED) |
|---|---|---|---|
| ChatGptPackets() [GET] | DeckAnalysis() | 155 | [HttpGet("/deck-analysis")] |
| ChatGptDeckComparison() [GET] | DeckComparison() | 168 | [HttpGet("/deck-comparison")] |
| ChatGptCedhMetaGap() [GET] | CedhMetaGap() | 181 | [HttpGet("/cedh-meta-gap")] |
| ChatGptPackets(ChatGptDeckRequest request) [POST] | DeckAnalysis(DeckAnalysisRequest request) | 459 | [HttpPost("/deck-analysis")] |
| ChatGptPacketsDownload(ChatGptDeckRequest) | DeckAnalysisDownload(DeckAnalysisRequest) | 506 | [HttpPost("/deck-analysis/download")] |
| ChatGptPacketsUpload(IFormFile zipFile) | DeckAnalysisUpload(IFormFile zipFile) | 557 | [HttpPost("/deck-analysis/upload")] |
| ChatGptDeckComparison(ChatGptDeckComparisonRequest) [POST] | DeckComparison(DeckComparisonRequest) | 628 | [HttpPost("/deck-comparison")] |
| ChatGptDeckComparisonDownload(ChatGptDeckComparisonRequest) | DeckComparisonDownload(DeckComparisonRequest) | 690 | [HttpPost("/deck-comparison/download")] |
| ChatGptDeckComparisonUpload(IFormFile) | DeckComparisonUpload(IFormFile) | 781 | [HttpPost("/deck-comparison/upload")] |
| ChatGptCedhMetaGap(ChatGptCedhMetaGapRequest) [POST] | CedhMetaGap(MetaGapRequest) | 879 | [HttpPost("/cedh-meta-gap")] |
| ChatGptCedhMetaGapDownload(ChatGptCedhMetaGapRequest) | CedhMetaGapDownload(MetaGapRequest) | 941 | [HttpPost("/cedh-meta-gap/download")] |
| ChatGptCedhMetaGapUpload(IFormFile) | CedhMetaGapUpload(IFormFile) | 1012 | [HttpPost("/cedh-meta-gap/upload")] |
</action_method_rename_map>

<preservation_list_wave3>
- `View("DeckAnalysis", ...)`, `View("DeckComparison", ...)`, `View("CedhMetaGap", ...)` LITERAL view-name strings — ALL stay byte-identical (Phase 12 invariant per Pitfall 2 in 13-RESEARCH.md L307-315). ONLY the second argument (the `new XViewModel { ... }` portion) changes to use the renamed Wave-1 ViewModel type names.
- All `[HttpGet("/deck-analysis")]`, `[HttpPost("/deck-comparison")]`, etc. route attribute strings — byte-identical (URL slugs are Phase 12 locked).
- All form-field `name="..."` attributes on Razor views — byte-identical (DeckText, DeckUrl, StrategyNotes, MetaNotes, Format, DeckName, IncludeSideboardInAnalysis, IncludeMaybeboardInAnalysis, SelectedAnalysisQuestions, BudgetUpgradeAmount, IncludeCardVersions, PreferredCategories, ProtectedCards, FreeformQuestion, DeckProfileJson, SetPacketText, SetUpgradeResponseJson, TargetCommanderBracket, TargetAiPlatform, WorkflowStep). These bind to property names on the renamed Request DTOs — property names are NOT renamed in Phase 13 per D-07 #2.
- `_AiSelector.cshtml` is READ-ONLY in Phase 13 — preserves `var selected = string.IsNullOrEmpty(Model) ? "ChatGPT" : Model;`, `if (selected == "Gemini" && !geminiEnabled) selected = "ChatGPT";`, `<input ... id="ai-chatgpt" value="ChatGPT" ...>`, `checked="@(selected == "ChatGPT" ? "checked" : null)"`, `<label for="ai-chatgpt" ...>ChatGPT</label>` (D-07 #1, D-07 #6, D-08, Pitfall 4).
- All chatgpt-prefixed CSS classes (`chatgpt-packets-form`, `chatgpt-layout-picker`, `chatgpt-step-panel`, `chatgpt-sticky-download__*`) and data attributes (`data-cache-key="chatgpt-packets"`, `data-chatgpt-*-form`, `data-chatgpt-ui-mode-picker`, `data-chatgpt-ui-mode-button`, `data-chatgpt-current-step`, `data-chatgpt-workflow-step`, etc.) inside DeckAnalysis.cshtml / DeckComparison.cshtml / CedhMetaGap.cshtml — byte-identical (D-08; TS/CSS sweep deferred to Phase 16).
</preservation_list_wave3>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename DeckController.cs — action methods + ctor params + private fields + method-body type refs</name>
  <files>DeckFlow.Web/Controllers/DeckController.cs</files>
  <read_first>
    DeckFlow.Web/Controllers/DeckController.cs (full file — 142 ChatGpt hits per 13-RESEARCH.md L124; verify ctor parameter list, private field declarations, all 12 action methods at the line numbers listed in the action_method_rename_map, all 39 View(...) second-arg constructions),
    DeckFlow.Web/Services/DeckAnalysisPacketService.cs (Wave 2 — verify interface name IDeckAnalysisPacketService and class name DeckAnalysisPacketService that the controller will reference),
    DeckFlow.Web/Services/DeckComparisonService.cs (Wave 2),
    DeckFlow.Web/Services/MetaGapService.cs (Wave 2),
    DeckFlow.Web/Models/DeckAnalysisRequest.cs (Wave 1 — verify type name DeckAnalysisRequest is what POST action method params will reference),
    DeckFlow.Web/Models/DeckAnalysisViewModel.cs (Wave 1 — verify type name DeckAnalysisViewModel),
    DeckFlow.Web/Models/DeckComparisonRequest.cs (Wave 1),
    DeckFlow.Web/Models/DeckComparisonViewModel.cs (Wave 1),
    DeckFlow.Web/Models/MetaGapRequest.cs (Wave 1),
    DeckFlow.Web/Models/MetaGapViewModel.cs (Wave 1),
    DeckFlow.Web/Models/DeckPageTab.cs (Wave 1 — verify new enum values DeckAnalysis/DeckComparison/CedhMetaGap),
    DeckFlow.Web/Services/PacketArtifactStore.cs (Wave 2 — controller probably calls its static methods),
    DeckFlow.Web/Services/RequestContextParser.cs (Wave 2),
    DeckFlow.Web/Services/ResponseParsers.cs (Wave 2),
    DeckFlow.Web/Services/JsonTextFormatterService.cs (Wave 2),
    .planning/phases/13-chatgpt-class-rename-summary-doc-comments/13-CONTEXT.md (D-01 naming map, Deferred Ideas note allowing action-method rename in lockstep without splitting controller),
    .planning/phases/13-chatgpt-class-rename-summary-doc-comments/13-RESEARCH.md (Wave 3 file enumeration L466-473; action method map L478-490; Pitfall 1 routing ambiguity L297-306; Pitfall 2 View() literal preservation L307-315; Open Question 2 test-method-name lockstep L739-742)
  </read_first>
  <action>
    Single commit ("controller rename swept"). Make all edits in one pass, then commit.

    Step A — Ctor parameters + private fields:
    Update the controller's constructor parameter list to use the renamed Wave-2 service interface types — `IChatGptDeckPacketService` to `IDeckAnalysisPacketService`, `IChatGptDeckComparisonService` to `IDeckComparisonService`, `IChatGptCedhMetaGapService` to `IMetaGapService`. Update the corresponding `private readonly` field declarations and the field-assignment lines inside the ctor body. Field names may include the old prefix (e.g., `_chatGptDeckPacketService`) — rename those to drop the prefix as well (e.g., `_deckAnalysisPacketService`) for clarity per CONTEXT.md Claude's Discretion #1 ("strip the prefix if it clarifies"). Update every usage of those private fields throughout the method bodies.

    Step B — Action methods (12 total per the action_method_rename_map above):
    Rename each method name AND its request DTO parameter type in lockstep. Examples:
    - `public IActionResult ChatGptPackets()` (line ~155) becomes `public IActionResult DeckAnalysis()`
    - `public async Task<IActionResult> ChatGptPackets(ChatGptDeckRequest request)` (line ~459) becomes `public async Task<IActionResult> DeckAnalysis(DeckAnalysisRequest request)`
    - `public async Task<IActionResult> ChatGptPacketsDownload(ChatGptDeckRequest request)` (line ~506) becomes `public async Task<IActionResult> DeckAnalysisDownload(DeckAnalysisRequest request)`
    - `public async Task<IActionResult> ChatGptPacketsUpload(IFormFile zipFile)` (line ~557) becomes `public async Task<IActionResult> DeckAnalysisUpload(IFormFile zipFile)`
    - Repeat for DeckComparison + CedhMetaGap variants per the map.

    The `[HttpGet("/deck-analysis")]` / `[HttpPost("/deck-analysis/download")]` route attribute strings stay byte-identical — DO NOT modify them.

    Per Pitfall 1: build will show CS0111 if two methods accidentally share name + signature. The action_method_rename_map produces unique names for each method (each old name maps to one new name, and the HTTP-verb + route-attribute combination disambiguates GET vs POST on the same URL).

    Step C — Method-body type references:
    Replace every ChatGpt-prefixed type identifier in method bodies, including:
    - `new ChatGptDeckViewModel { ... }` becomes `new DeckAnalysisViewModel { ... }` (this is the SECOND argument to `View("DeckAnalysis", ...)` — the FIRST argument literal string `"DeckAnalysis"` stays byte-identical per Pitfall 2)
    - `new ChatGptDeckComparisonViewModel { ... }` becomes `new DeckComparisonViewModel { ... }`
    - `new ChatGptCedhMetaGapViewModel { ... }` becomes `new MetaGapViewModel { ... }`
    - `new ChatGptDeckRequest { ... }` becomes `new DeckAnalysisRequest { ... }` (request DTO defaults inside the controller)
    - `new ChatGptDeckComparisonRequest { ... }` becomes `new DeckComparisonRequest { ... }`
    - `new ChatGptCedhMetaGapRequest { ... }` becomes `new MetaGapRequest { ... }`
    - Response shape refs (`ChatGptDeckAnalysisResponse`, `ChatGptWeakSlot`, `ChatGptQuestionAnswer`, `ChatGptDeckVersion`, `ChatGptSetUpgradeResponse`, `ChatGptSetUpgradeSet`, etc., `ChatGptDeckComparisonResponse`, `ChatGptDeckComparisonRecommendation`, `ChatGptCedhMetaGapResponse`, `ChatGptCedhMetaGapData`, and all 11 nested `ChatGptCedh*` shapes) become renamed Wave-1 names per the naming map in 13-CONTEXT.md.
    - Result record refs (`ChatGptDeckPacketResult`, `ChatGptDeckComparisonResult`, `ChatGptCedhMetaGapResult`) become `DeckAnalysisPacketResult`, `DeckComparisonResult`, `MetaGapResult`.
    - Static helper refs (`ChatGptPacketArtifactStore.X(...)`, `ChatGptRequestContextParser.Y(...)`, `ChatGptResponseParsers.Z(...)`, `ChatGptJsonTextFormatterService.W(...)`) become `PacketArtifactStore.X(...)`, `RequestContextParser.Y(...)`, `ResponseParsers.Z(...)`, `JsonTextFormatterService.W(...)`. If any of those static helpers exposes the const `JsonTextFormatterService.ChatGptResultWrapInstruction` — it's now `JsonTextFormatterService.ResultWrapInstruction`.
    - DeckPageTab enum value refs (`DeckPageTab.ChatGptPackets`, `DeckPageTab.ChatGptDeckComparison`, `DeckPageTab.ChatGptCedhMetaGap`) become `DeckPageTab.DeckAnalysis`, `DeckPageTab.DeckComparison`, `DeckPageTab.CedhMetaGap`.

    Step D — PRESERVE:
    - All `View("DeckAnalysis", ...)`, `View("DeckComparison", ...)`, `View("CedhMetaGap", ...)` first-arg LITERAL STRINGS byte-identical (per Pitfall 2 — these resolve to view filenames at `Views/Deck/DeckAnalysis.cshtml` etc. set by Phase 12).
    - All route attribute strings byte-identical (`[HttpGet("/deck-analysis")]`, etc.).
    - All `RedirectToAction(nameof(X))` calls — UPDATE the `nameof(...)` argument to the renamed action method name. E.g., `RedirectToAction(nameof(ChatGptPackets))` becomes `RedirectToAction(nameof(DeckAnalysis))`. (This is a C# identifier reference, must match the new method name to compile.)
    - All `View(nameof(X))` calls — likewise update the `nameof(...)` argument to the renamed action method name. EXCEPT if the call passes a literal string instead of `nameof(...)` (e.g., `View("DeckAnalysis")`), preserve the literal as-is per Pitfall 2.

    Step E — Edit approach:
    Read the file, identify each rename site, edit deliberately, re-grep to confirm. AVOID a blind `sed -i 's/ChatGpt//g'` — that would break preservation rules (e.g., it would corrupt the literal string `"ChatGPT"` inside any docs/comments and would not handle `ChatGptCedh*` to just `*` correctly since the `Cedh` prefix also needs stripping).

    Commit: `refactor(13): rename ChatGpt* identifiers in DeckController.cs (12 action methods + ctor params + body refs)`.
  </action>
  <verify>
    <automated>! grep -qE "ChatGpt[A-Z]" DeckFlow.Web/Controllers/DeckController.cs &amp;&amp; grep -q "IDeckAnalysisPacketService" DeckFlow.Web/Controllers/DeckController.cs &amp;&amp; grep -q "IDeckComparisonService" DeckFlow.Web/Controllers/DeckController.cs &amp;&amp; grep -q "IMetaGapService" DeckFlow.Web/Controllers/DeckController.cs &amp;&amp; grep -q "public IActionResult DeckAnalysis()" DeckFlow.Web/Controllers/DeckController.cs &amp;&amp; grep -q "DeckAnalysisDownload" DeckFlow.Web/Controllers/DeckController.cs &amp;&amp; grep -q "DeckAnalysisUpload" DeckFlow.Web/Controllers/DeckController.cs &amp;&amp; grep -q "DeckComparisonDownload" DeckFlow.Web/Controllers/DeckController.cs &amp;&amp; grep -q "DeckComparisonUpload" DeckFlow.Web/Controllers/DeckController.cs &amp;&amp; grep -q "CedhMetaGapDownload" DeckFlow.Web/Controllers/DeckController.cs &amp;&amp; grep -q "CedhMetaGapUpload" DeckFlow.Web/Controllers/DeckController.cs &amp;&amp; grep -q 'View("DeckAnalysis"' DeckFlow.Web/Controllers/DeckController.cs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE "ChatGpt[A-Z]" DeckFlow.Web/Controllers/DeckController.cs` returns 0 (zero identifier-level ChatGpt prefixes remain).
    - `grep -c "IDeckAnalysisPacketService" DeckFlow.Web/Controllers/DeckController.cs` returns at least 2 (ctor param + private field type).
    - `grep -c "IDeckComparisonService" DeckFlow.Web/Controllers/DeckController.cs` returns at least 2.
    - `grep -c "IMetaGapService" DeckFlow.Web/Controllers/DeckController.cs` returns at least 2.
    - `grep -c "public IActionResult DeckAnalysis()" DeckFlow.Web/Controllers/DeckController.cs` returns 1 (the GET action).
    - `grep -cE "(DeckAnalysisDownload|DeckAnalysisUpload|DeckComparisonDownload|DeckComparisonUpload|CedhMetaGapDownload|CedhMetaGapUpload)\(" DeckFlow.Web/Controllers/DeckController.cs` returns 6 (the 6 download/upload POST actions).
    - `grep -c 'View("DeckAnalysis"' DeckFlow.Web/Controllers/DeckController.cs` returns at least 1 (Phase 12 invariant — literal view-name string preserved).
    - `grep -c 'View("DeckComparison"' DeckFlow.Web/Controllers/DeckController.cs` returns at least 1.
    - `grep -c 'View("CedhMetaGap"' DeckFlow.Web/Controllers/DeckController.cs` returns at least 1.
    - `grep -cE '\[HttpGet\("/deck-analysis"\)\]' DeckFlow.Web/Controllers/DeckController.cs` returns 1 (route attribute preserved).
    - `grep -cE '\[HttpPost\("/deck-analysis/download"\)\]' DeckFlow.Web/Controllers/DeckController.cs` returns 1.
    - `grep -c "new DeckAnalysisViewModel" DeckFlow.Web/Controllers/DeckController.cs` returns at least 1 (View second-arg construction).
    - `grep -c "new MetaGapViewModel" DeckFlow.Web/Controllers/DeckController.cs` returns at least 1.
    - `grep -cE "DeckPageTab\.(DeckAnalysis|DeckComparison|CedhMetaGap)" DeckFlow.Web/Controllers/DeckController.cs` returns at least 3.
    - `grep -c "PacketArtifactStore\." DeckFlow.Web/Controllers/DeckController.cs` returns at least 1 (renamed static helper called).
    - `grep -c "RedirectToAction(nameof(ChatGpt" DeckFlow.Web/Controllers/DeckController.cs` returns 0 (nameof refs updated).
    - `git log --oneline -1` shows `refactor(13): rename ChatGpt* identifiers in DeckController.cs ...`, plain-author.
  </acceptance_criteria>
  <done>DeckController.cs has all 142 ChatGpt-prefix sites resolved: 12 action methods renamed (3 GET + 9 POST), ctor params + private fields reference renamed Wave-2 service interfaces, all method-body type refs (new XViewModel, new XRequest, response/result/helper class refs, DeckPageTab enum value refs, nameof(...) action-method refs) renamed to Wave-1/Wave-2 names, View(...) first-arg LITERAL strings + route attribute strings BYTE-IDENTICAL, single plain-author commit.</done>
</task>

<task type="auto">
  <name>Task 2: Update Razor views @model directives + shared partials enum + comment refs + Wave 3 grep gate</name>
  <files>
    DeckFlow.Web/Views/Deck/DeckAnalysis.cshtml,
    DeckFlow.Web/Views/Deck/DeckComparison.cshtml,
    DeckFlow.Web/Views/Deck/CedhMetaGap.cshtml,
    DeckFlow.Web/Views/Shared/_DeckToolTabs.cshtml,
    DeckFlow.Web/Views/Shared/_BracketCallout.cshtml
  </files>
  <read_first>
    DeckFlow.Web/Views/Deck/DeckAnalysis.cshtml (current state — verify @model DeckFlow.Web.Models.ChatGptDeckViewModel directive on line 1; check that all data-chatgpt-* attrs, class=chatgpt-* CSS refs, and data-cache-key=chatgpt-packets strings stay UNTOUCHED per D-08),
    DeckFlow.Web/Views/Deck/DeckComparison.cshtml (current state — verify @model DeckFlow.Web.Models.ChatGptDeckComparisonViewModel),
    DeckFlow.Web/Views/Deck/CedhMetaGap.cshtml (current state — verify @model DeckFlow.Web.Models.ChatGptCedhMetaGapViewModel),
    DeckFlow.Web/Views/Shared/_DeckToolTabs.cshtml (current state — 4 hits per 13-RESEARCH.md L472: L6 multi-arm enum check, L18-20 three Model == DeckPageTab.ChatGpt* checks),
    DeckFlow.Web/Views/Shared/_BracketCallout.cshtml (current state — 3 comment-prose hits per 13-RESEARCH.md L473 mentioning the OLD view filename ChatGptPackets.cshtml),
    DeckFlow.Web/Views/Shared/_AiSelector.cshtml (READ-ONLY in Phase 13 — read it ONLY to confirm post-edit grep result includes 5 expected hits at L13-26 with literal ChatGPT/id=ai-chatgpt/for=ai-chatgpt),
    DeckFlow.Web/Models/DeckAnalysisViewModel.cs (Wave 1 — confirm new ViewModel type name),
    DeckFlow.Web/Models/DeckComparisonViewModel.cs (Wave 1),
    DeckFlow.Web/Models/MetaGapViewModel.cs (Wave 1),
    DeckFlow.Web/Models/DeckPageTab.cs (Wave 1 — confirm new enum values),
    .planning/phases/13-chatgpt-class-rename-summary-doc-comments/13-CONTEXT.md (D-08 internal HTML/JS identifier preservation; D-07 #6 Razor visible-prose ChatGPT preservation),
    .planning/phases/13-chatgpt-class-rename-summary-doc-comments/13-RESEARCH.md (Wave 3 view enumeration L469-473; Pitfall 4 _AiSelector.cshtml READ-ONLY L327-335; D-08 deferred identifier list L108-112)
  </read_first>
  <action>
    Two commits — group the 3 main view `@model` directives in commit 1 (one logical change), group the 2 shared partials in commit 2 (one logical change).

    Commit 1 edits:
    - DeckAnalysis.cshtml line 1: change `@model DeckFlow.Web.Models.ChatGptDeckViewModel` to `@model DeckFlow.Web.Models.DeckAnalysisViewModel`. DO NOT touch any other line. The body of the view contains many `data-chatgpt-*` attributes, `class="chatgpt-packets-form"` etc. — these stay byte-identical per D-08 (Phase 16 candidate).
    - DeckComparison.cshtml line 1: change `@model DeckFlow.Web.Models.ChatGptDeckComparisonViewModel` to `@model DeckFlow.Web.Models.DeckComparisonViewModel`. DO NOT touch any other line.
    - CedhMetaGap.cshtml line 1: change `@model DeckFlow.Web.Models.ChatGptCedhMetaGapViewModel` to `@model DeckFlow.Web.Models.MetaGapViewModel`. DO NOT touch any other line.
    Commit message: `refactor(13): update Razor @model directives for renamed view models (DeckAnalysis/DeckComparison/CedhMetaGap)`.

    Commit 2 edits:
    - _DeckToolTabs.cshtml enum-value refs (4 hits per 13-RESEARCH.md L472): L6 multi-arm enum-check pattern `Model is DeckPageTab.ChatGptPackets or DeckPageTab.ChatGptDeckComparison or DeckPageTab.ChatGptCedhMetaGap` becomes `Model is DeckPageTab.DeckAnalysis or DeckPageTab.DeckComparison or DeckPageTab.CedhMetaGap`. L18-20: three `Model == DeckPageTab.ChatGptPackets` / `Model == DeckPageTab.ChatGptDeckComparison` / `Model == DeckPageTab.ChatGptCedhMetaGap` checks — replace each with the renamed enum value (`DeckPageTab.DeckAnalysis` / `DeckPageTab.DeckComparison` / `DeckPageTab.CedhMetaGap`). Read the file before edit to verify exact line numbers (may have shifted by plus/minus 1 line). Preserve all surrounding markup (anchors, class names, prose).
    - _BracketCallout.cshtml prose-comment refs (3 hits per 13-RESEARCH.md L473): read lines 3, 8, 11 (or thereabouts — may shift slightly) and update the prose to reference `DeckAnalysis.cshtml` (the Phase 12 renamed view filename) instead of `ChatGptPackets.cshtml`. These are prose comments inside `@* ... *@` blocks or HTML `<!-- ... -->` — not active code. The change is purely for in-comment accuracy.
    Commit message: `refactor(13): update shared Razor partials for renamed DeckPageTab enum values and view filenames`.

    DO NOT TOUCH `_AiSelector.cshtml` — read-only per Pitfall 4.

    After both commits, run the Wave 3 cshtml grep verification:
    ```
    grep -nE "ChatGpt" DeckFlow.Web/Views/Shared/_DeckToolTabs.cshtml DeckFlow.Web/Views/Shared/_BracketCallout.cshtml DeckFlow.Web/Views/Deck/DeckAnalysis.cshtml DeckFlow.Web/Views/Deck/DeckComparison.cshtml DeckFlow.Web/Views/Deck/CedhMetaGap.cshtml
    ```
    Expected: ZERO hits across those 5 files. Any `chatgpt-*` lowercase data-attribute or CSS-class hits inside the 3 main views are SEPARATE — they don't match the regex `ChatGpt` (capital C, lowercase h-a-t, capital G-p-t).

    Then run the broader Wave 3 grep to confirm controller cleanup is intact:
    ```
    grep -rEn "ChatGpt[A-Z]" --include="*.cs" DeckFlow.Web/Controllers/
    ```
    Expected: ZERO hits.
  </action>
  <verify>
    <automated>grep -q "@model DeckFlow.Web.Models.DeckAnalysisViewModel" DeckFlow.Web/Views/Deck/DeckAnalysis.cshtml &amp;&amp; grep -q "@model DeckFlow.Web.Models.DeckComparisonViewModel" DeckFlow.Web/Views/Deck/DeckComparison.cshtml &amp;&amp; grep -q "@model DeckFlow.Web.Models.MetaGapViewModel" DeckFlow.Web/Views/Deck/CedhMetaGap.cshtml &amp;&amp; ! grep -qE "ChatGpt" DeckFlow.Web/Views/Deck/DeckAnalysis.cshtml &amp;&amp; ! grep -qE "ChatGpt" DeckFlow.Web/Views/Deck/DeckComparison.cshtml &amp;&amp; ! grep -qE "ChatGpt" DeckFlow.Web/Views/Deck/CedhMetaGap.cshtml &amp;&amp; grep -q "DeckPageTab.DeckAnalysis" DeckFlow.Web/Views/Shared/_DeckToolTabs.cshtml &amp;&amp; grep -q "DeckPageTab.DeckComparison" DeckFlow.Web/Views/Shared/_DeckToolTabs.cshtml &amp;&amp; grep -q "DeckPageTab.CedhMetaGap" DeckFlow.Web/Views/Shared/_DeckToolTabs.cshtml &amp;&amp; ! grep -qE "ChatGpt" DeckFlow.Web/Views/Shared/_DeckToolTabs.cshtml &amp;&amp; ! grep -qE "ChatGpt" DeckFlow.Web/Views/Shared/_BracketCallout.cshtml</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "@model DeckFlow.Web.Models.DeckAnalysisViewModel" DeckFlow.Web/Views/Deck/DeckAnalysis.cshtml` returns 1.
    - `grep -c "@model DeckFlow.Web.Models.DeckComparisonViewModel" DeckFlow.Web/Views/Deck/DeckComparison.cshtml` returns 1.
    - `grep -c "@model DeckFlow.Web.Models.MetaGapViewModel" DeckFlow.Web/Views/Deck/CedhMetaGap.cshtml` returns 1.
    - `grep -cE "ChatGpt" DeckFlow.Web/Views/Deck/DeckAnalysis.cshtml DeckFlow.Web/Views/Deck/DeckComparison.cshtml DeckFlow.Web/Views/Deck/CedhMetaGap.cshtml` returns 0 (3 main views — note the regex is `ChatGpt` capital-C/lowercase-h-a-t/capital-G-p-t, which does NOT match lowercase `chatgpt-*` data attrs).
    - `grep -c "DeckPageTab.DeckAnalysis" DeckFlow.Web/Views/Shared/_DeckToolTabs.cshtml` returns at least 1.
    - `grep -c "DeckPageTab.DeckComparison" DeckFlow.Web/Views/Shared/_DeckToolTabs.cshtml` returns at least 1.
    - `grep -c "DeckPageTab.CedhMetaGap" DeckFlow.Web/Views/Shared/_DeckToolTabs.cshtml` returns at least 1.
    - `grep -cE "ChatGpt" DeckFlow.Web/Views/Shared/_DeckToolTabs.cshtml DeckFlow.Web/Views/Shared/_BracketCallout.cshtml` returns 0.
    - `grep -c "DeckAnalysis.cshtml" DeckFlow.Web/Views/Shared/_BracketCallout.cshtml` returns at least 1 (prose updated to new view filename).
    - `grep -c "ChatGptPackets.cshtml" DeckFlow.Web/Views/Shared/_BracketCallout.cshtml` returns 0.
    - `grep -rEn "ChatGpt[A-Z]" --include="*.cs" DeckFlow.Web/Controllers/` returns ZERO lines (re-verify controller cleanup from Task 1 still intact).
    - `grep -c "ai-chatgpt\|value=\"ChatGPT\"" DeckFlow.Web/Views/Shared/_AiSelector.cshtml` returns at least 3 (Pitfall 4 — read-only file preserved its 5 expected ChatGpt/chatgpt literals).
    - `git log --oneline -2` shows 2 plain-author commits: `refactor(13): update Razor @model directives ...` and `refactor(13): update shared Razor partials ...`.
  </acceptance_criteria>
  <done>3 main Razor view `@model` directives point to renamed Wave-1 ViewModel types, _DeckToolTabs.cshtml's 4 DeckPageTab enum-value references use renamed values, _BracketCallout.cshtml prose comments reference DeckAnalysis.cshtml (new filename), _AiSelector.cshtml UNTOUCHED, all chatgpt-prefixed CSS/data attrs inside the 3 main views preserved byte-identical (D-08 deferred), 2 plain-author commits.</done>
</task>

</tasks>

<verification>
**Wave 3 verification (run AT END of plan, before declaring complete):**

```bash
# 1. Controller is clean
grep -cE "ChatGpt[A-Z]" DeckFlow.Web/Controllers/DeckController.cs
# Expected: 0

# 2. 3 main views have updated @model directives
head -1 DeckFlow.Web/Views/Deck/DeckAnalysis.cshtml      # @model DeckFlow.Web.Models.DeckAnalysisViewModel
head -1 DeckFlow.Web/Views/Deck/DeckComparison.cshtml    # @model DeckFlow.Web.Models.DeckComparisonViewModel
head -1 DeckFlow.Web/Views/Deck/CedhMetaGap.cshtml       # @model DeckFlow.Web.Models.MetaGapViewModel

# 3. Shared partials enum-value sweep
grep -nE "DeckPageTab\.(DeckAnalysis|DeckComparison|CedhMetaGap)" DeckFlow.Web/Views/Shared/_DeckToolTabs.cshtml
# Expected: at least 4 hits (1 multi-arm `is` check + 3 single-arm `==` checks)

# 4. _AiSelector.cshtml NOT touched (Pitfall 4)
grep -nE "ChatGPT|ai-chatgpt|chatgpt" DeckFlow.Web/Views/Shared/_AiSelector.cshtml
# Expected: 5 expected hits at L13/L15/L22/L23/L24 (per 13-RESEARCH.md Wave 3 _AiSelector READ-ONLY section)

# 5. Wave 3 grep gate — DeckController.cs + the 5 touched cshtml files all clean of ChatGpt[A-Z]
grep -rEn "ChatGpt[A-Z]" DeckFlow.Web/Controllers/DeckController.cs DeckFlow.Web/Views/Deck/DeckAnalysis.cshtml DeckFlow.Web/Views/Deck/DeckComparison.cshtml DeckFlow.Web/Views/Deck/CedhMetaGap.cshtml DeckFlow.Web/Views/Shared/_DeckToolTabs.cshtml DeckFlow.Web/Views/Shared/_BracketCallout.cshtml
# Expected: ZERO output

# 6. Larger-scope Web/ grep — what remains should ONLY be the allowlist sites
grep -rEn "ChatGpt[A-Z]" --include="*.cs" DeckFlow.Web/ | grep -vE '"ChatGPT"' | grep -vE 'ChatGPT/Claude/Gemini'
# Expected: ZERO output (Wave 3 should leave only the request DTO `"ChatGPT"` literals + AiPlatformOptions.cs doc-comment narrative, both pre-allowlisted)

# 7. Sanity: DeckFlow.Web should now build clean (test project will still be red — Wave 4 fixes)
dotnet build DeckFlow.Web/DeckFlow.Web.csproj --configuration Release
# Expected: 0 errors. Warnings possible (NoWarn 1591 still suppresses missing-summary on untouched types).

# 8. Git log
git log --oneline -3 | grep -cE '^[a-f0-9]+ refactor\(13\):'
# Expected: 3 (1 controller commit + 2 cshtml commits)
```

**Note:** The `dotnet build` in step 7 is a SOFT verification — Wave 3 SHOULD compile DeckFlow.Web clean per 13-CONTEXT.md D-05 ("build SHOULD compile clean except for test fixture errors at end of Wave 3"). If it doesn't, the executor likely missed a controller-body ChatGpt identifier — re-grep and fix before declaring complete. The HARD build-clean gate (full solution including tests) fires only at end of Wave 4 (plan 13-04).
</verification>

<success_criteria>
- DeckController.cs is fully clean of `ChatGpt[A-Z]` identifier-level references.
- 12 action methods renamed in lockstep with action_method_rename_map (3 GET + 9 POST).
- Ctor parameters + private fields reference renamed Wave-2 service interface types.
- All method-body type identifiers reference Wave-1 model types and Wave-2 service-result/helper types.
- All `nameof(...)` action-method refs updated to renamed action names.
- All `View("...", ...)` first-arg LITERAL view-name strings preserved byte-identical (Phase 12 invariant).
- All `[HttpGet/Post(...)]` route attribute strings preserved byte-identical (URL slugs from Phase 12).
- 3 Razor view `@model` directives point to renamed Wave-1 ViewModel types.
- _DeckToolTabs.cshtml uses renamed DeckPageTab enum values.
- _BracketCallout.cshtml prose comments reference DeckAnalysis.cshtml.
- _AiSelector.cshtml UNTOUCHED — 5 ChatGPT/chatgpt literals preserved at L13/L15/L22/L23/L24 per Pitfall 4.
- DeckFlow.Web compiles clean as a soft check (test project may still be red — Wave 4 closes it).
- 3 plain-author commits (1 controller + 2 cshtml).
</success_criteria>

<output>
Create `.planning/phases/13-chatgpt-class-rename-summary-doc-comments/13-03-SUMMARY.md` when done. Capture:
- DeckController.cs sweep complete: 12 action methods renamed + ctor + fields + body refs
- 3 Razor `@model` directives updated
- _DeckToolTabs.cshtml + _BracketCallout.cshtml swept
- _AiSelector.cshtml verified UNTOUCHED with 5 expected ChatGpt/chatgpt literal preservations
- Wave 3 grep gate result (ZERO `ChatGpt[A-Z]` hits in DeckFlow.Web/Controllers/ and the 5 touched cshtml files)
- DeckFlow.Web build result (soft check — clean expected; if errors, list them so plan 13-04 can fix)
- Git commit log (3 plain-author commits)
- Forward-looking note: Wave 4 (plan 13-04) renames 9 test files + 2 test edits + runs the final solution build-clean gate + emits HUMAN-UAT.md for T1-T8 manual integration suite.
</output>
