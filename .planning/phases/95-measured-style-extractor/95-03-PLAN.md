---
phase: 95-measured-style-extractor
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - DeckFlow.Core/Knowledge/ContentTagVocabulary.cs
  - DeckFlow.Core/Knowledge/GlobalCategoryBaseline.cs
  - DeckFlow.Core/Knowledge/CardCategoryRepository.cs
  - DeckFlow.Core/Knowledge/CategoryKnowledgeRepository.cs
  - DeckFlow.Core.Tests/ContentTagVocabularyTests.cs
  - DeckFlow.Core.Tests/CategoryKnowledgeRepositoryTests.cs
autonomous: true
requirements: [CS-05, CS-07]
must_haves:
  truths:
    - "A curated staple-card set (ubiquitous lands/rocks) exists in ContentTagVocabulary and is queryable (D-05)"
    - "The repository can answer, corpus-wide, Pr(category A present) and Pr(A and B present together) via a single server-side aggregate — not a client-side scan of 322k rows"
    - "The lift baseline read returns a typed GlobalCategoryBaseline (total decks, per-category deck counts, per-pair deck counts)"
  artifacts:
    - path: "DeckFlow.Core/Knowledge/ContentTagVocabulary.cs"
      provides: "Staples IReadOnlySet<string> (D-05 curated staple set)"
      contains: "Staples"
    - path: "DeckFlow.Core/Knowledge/GlobalCategoryBaseline.cs"
      provides: "Typed lift-baseline result (TotalDecks, DecksWithCategory, DecksWithCategoryPair)"
      min_lines: 20
    - path: "DeckFlow.Core/Knowledge/CardCategoryRepository.cs"
      provides: "New server-side corpus aggregate read for Pr(A)/Pr(A∩B)"
      contains: "GROUP BY"
  key_links:
    - from: "CategoryKnowledgeRepository"
      to: "CardCategoryRepository global-baseline aggregate"
      via: "one-line facade delegation"
      pattern: "=> _cardCategory\\."
---

<objective>
Author the two missing inputs the lift metric needs: (1) the curated `ContentTagVocabulary` staple set (D-05 — it does NOT exist today; Pitfall 4), and (2) a server-side corpus-wide aggregate read on `CardCategoryRepository` that computes the D-07 lift denominator `Pr(A)`, `Pr(A∩B)`, and total-deck count across the whole `card_category_observations` corpus — returned as a typed `GlobalCategoryBaseline`. Both are consumed by the pure extraction (Plans 04/05) and the orchestrator (Plan 07).

Purpose: CS-05 (staple-strip vocabulary prereq) and CS-07 (lift baseline denominator, currently a confirmed gap).
Output: A new staple set, a new baseline read method + facade delegate, a new result record, all tested.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/95-measured-style-extractor/95-CONTEXT.md
@.planning/phases/95-measured-style-extractor/95-RESEARCH.md
@DeckFlow.Core/Knowledge/ContentTagVocabulary.cs
@DeckFlow.Core/Knowledge/CategoryKnowledgeRepository.cs

<interfaces>
<!-- Existing staple-set idiom to imitate (add a 4th dimension). -->
From DeckFlow.Core/Knowledge/ContentTagVocabulary.cs:
  public static readonly IReadOnlySet<string> Archetypes = new HashSet<string>(StringComparer.OrdinalIgnoreCase){ ... };
  public static readonly IReadOnlySet<string> CardCategories = ...;   // functional buckets, NOT staple card names

<!-- Corpus schema for the aggregate (verified). -->
  card_category_observations(source_id, card_id, category, board)
  sources(id, source, deck_queue_id)     -- deck_queue_id NULL for non-deck sources
  deck_queue(id, deck_id, processed)     -- processed = 1 means harvested
  cards(id, normalized_card_name)

<!-- Facade delegation idiom (CategoryKnowledgeRepository → CardCategoryRepository). -->
  public Task<IReadOnlyList<string>> GetCategoriesAsync(string cardName, CancellationToken ct = default)
      => _cardCategory.GetCategoriesAsync(cardName, ct);
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the curated Staples set to ContentTagVocabulary (D-05)</name>
  <read_first>
    - DeckFlow.Core/Knowledge/ContentTagVocabulary.cs (the existing Archetypes/Brackets/CardCategories HashSet idiom)
    - 95-PATTERNS.md §"ContentTagVocabulary.cs (MODIFIED — extend, not reuse)" (Pitfall 4 + the P88 observed-staple seed list)
    - 95-RESEARCH.md Pitfall 4 (seed staples: Command Tower, Sol Ring, basics, Exotic Orchard, Negate, Arcane Signet, Rogue's Passage, ...)
  </read_first>
  <action>Add a new `public static readonly IReadOnlySet<string> Staples = new HashSet<string>(StringComparer.OrdinalIgnoreCase){ ... }` to `ContentTagVocabulary`, following the exact existing `HashSet<string>(OrdinalIgnoreCase)` idiom. This is card-name data (NOT a `ContentTagDimension` validation dimension), so it is its own property and is NOT routed through `IsValid(dimension, value)`. Seed it from the P88 prototype's observed ubiquitous staples: the five basic land names (Plains, Island, Swamp, Mountain, Forest), Command Tower, Sol Ring, Arcane Signet, Exotic Orchard, Rogue's Passage, and the other ubiquitous rocks/lands enumerated in 95-RESEARCH Pitfall 4. Add an XML doc comment noting this is the D-05 curated "always-strip" staple set, distinct from the per-creator >60%-frequency cut computed later (Plan 04's StapleStripper). Preserve LF line endings.</action>
  <verify>
    <automated>dotnet test DeckFlow.Core.Tests --filter "FullyQualifiedName~ContentTagVocabularyTests"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "Staples" DeckFlow.Core/Knowledge/ContentTagVocabulary.cs` shows the new `IReadOnlySet<string>` property
    - The set contains the five basic land names and Sol Ring / Command Tower (seed staples)
    - New `ContentTagVocabularyTests` asserts membership (case-insensitive) of at least Sol Ring, Command Tower, and a basic land, and non-membership of a non-staple like "Thassa's Oracle"
    - `dotnet test DeckFlow.Core.Tests --filter "FullyQualifiedName~ContentTagVocabularyTests"` passes
  </acceptance_criteria>
  <done>ContentTagVocabulary.Staples exists as a curated case-insensitive card-name set, tested.</done>
</task>

<task type="auto">
  <name>Task 2: GlobalCategoryBaseline record + server-side corpus aggregate read (CS-07 denominator)</name>
  <read_first>
    - DeckFlow.Core/Knowledge/CardCategoryRepository.cs (GetCategoriesAsync Dapper idiom, lines 36-57 — server-side GROUP BY, EnsureSchemaAsync, CreateConnection)
    - DeckFlow.Core/Knowledge/CategoryKnowledgeRepository.cs (facade one-line delegation idiom)
    - DeckFlow.Core/Knowledge/CategoryCacheSchema.cs (confirms card_category_observations / sources / deck_queue columns)
    - 95-RESEARCH.md "Code Examples" §"Missing global lift-baseline query shape" + Pitfall 2 (aggregate in SQL, never pull 322k raw rows)
  </read_first>
  <action>Create `DeckFlow.Core/Knowledge/GlobalCategoryBaseline.cs`: `sealed record GlobalCategoryBaseline { required int TotalDecks; required IReadOnlyDictionary<string,int> DecksWithCategory; required IReadOnlyDictionary<string,int> DecksWithCategoryPair; }` where the pair-dictionary key is the two categories joined lexicographically as `"catA|catB"` (sorted so A|B == B|A canonicalizes to one key) — document this key convention in XML doc so Plan 05's LiftCalculator reads it the same way. Add an `internal async Task<GlobalCategoryBaseline> GetGlobalCategoryBaselineAsync(CancellationToken)` to `CardCategoryRepository` that computes everything SERVER-SIDE (Pitfall 2 — no raw-row pull): (a) TotalDecks = `COUNT(DISTINCT s.deck_queue_id)` over `sources s` with `deck_queue_id IS NOT NULL` joined to `deck_queue` where `processed = 1`; (b) per-category deck presence = `SELECT o.category, COUNT(DISTINCT s.deck_queue_id) FROM card_category_observations o JOIN sources s ON s.id = o.source_id WHERE s.deck_queue_id IS NOT NULL GROUP BY o.category` (filter categories through `CategoryFilter.IsIncluded` in memory after the aggregate); (c) per-pair co-occurrence via a self-join of the deck→category presence set on `deck_queue_id` with `category_a < category_b`, `GROUP BY category_a, category_b` (a CTE/subquery that first reduces observations to distinct (deck, category) is acceptable and cheaper). Expose it via a one-line facade delegate `GetGlobalCategoryBaselineAsync` on `CategoryKnowledgeRepository` mirroring the existing `=> _cardCategory.` pattern. Preserve LF line endings.</action>
  <verify>
    <automated>dotnet build DeckFlow.Core/DeckFlow.Core.csproj</automated>
  </verify>
  <acceptance_criteria>
    - `GlobalCategoryBaseline.cs` exists with TotalDecks + the two dictionaries; XML doc states the `"catA|catB"` sorted-key convention
    - `CardCategoryRepository` gains `GetGlobalCategoryBaselineAsync` whose SQL uses `GROUP BY` and `COUNT(DISTINCT ...)` (server-side) — `grep -c "IReadOnlyList<CardCategoryObservationRow>" ` for the new method == 0 (no raw-row return, Pitfall 2)
    - `CategoryKnowledgeRepository` exposes a one-line `=> _cardCategory.GetGlobalCategoryBaselineAsync(...)` facade delegate
    - `dotnet build DeckFlow.Core/DeckFlow.Core.csproj` succeeds, 0 new warnings
  </acceptance_criteria>
  <done>A typed, server-side corpus-wide Pr(A)/Pr(A∩B)/TotalDecks aggregate is exposed through the repository facade.</done>
</task>

<task type="auto">
  <name>Task 3: Multi-deck fixture test proving the aggregate is correct</name>
  <read_first>
    - DeckFlow.Core.Tests/CategoryKnowledgeRepositoryTests.cs (existing repository-level test harness — how it seeds card_category_observations / sources / deck_queue)
    - DeckFlow.Core/Knowledge/GlobalCategoryBaseline.cs (result shape — from Task 2)
  </read_first>
  <action>Extend `DeckFlow.Core.Tests/CategoryKnowledgeRepositoryTests.cs` with a `[Fact]` that seeds a small deterministic corpus (e.g., 4 decks: deck1 has categories {ramp, draw}, deck2 {ramp, removal}, deck3 {ramp}, deck4 {draw}) into the same tables the existing tests write, calls `GetGlobalCategoryBaselineAsync`, and asserts: TotalDecks == 4; DecksWithCategory["ramp"] == 3, ["draw"] == 2, ["removal"] == 1; DecksWithCategoryPair["draw|ramp"] == 1 (only deck1), ["ramp|removal"] == 1 (only deck2). Prove the pair key canonicalizes regardless of insertion order. Follow the existing test's seeding helpers; do not add a new mocking library. Preserve LF line endings.</action>
  <verify>
    <automated>dotnet test DeckFlow.Core.Tests --filter "FullyQualifiedName~CategoryKnowledgeRepositoryTests"</automated>
  </verify>
  <acceptance_criteria>
    - The new fixture asserts exact TotalDecks, per-category, and per-pair counts against a hand-computed corpus
    - `dotnet test DeckFlow.Core.Tests --filter "FullyQualifiedName~CategoryKnowledgeRepositoryTests"` passes (including pre-existing cases)
  </acceptance_criteria>
  <done>The corpus aggregate is proven correct on a hand-verifiable fixture; existing repository tests remain green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repository → SQLite/Postgres corpus | Read-only aggregate over the shared 322k-observation corpus |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-95-03-01 | DoS (memory exhaustion on 512MB web tier) | global baseline read | mitigate | Aggregate server-side (GROUP BY / COUNT DISTINCT); never SELECT raw rows into memory (Pitfall 2) — enforced by acceptance-criterion grep |
| T-95-03-02 | Information disclosure | corpus categories | accept | Aggregate category statistics only, no per-user/PII data |
| T-95-03-03 | Injection | new aggregate SQL | mitigate | Static SQL text with no interpolated user input; category filtering done in memory via CategoryFilter, not string-built SQL |
| T-95-03-SC | Tampering | package installs | mitigate | No new packages this plan |
</threat_model>

<verification>
- `dotnet build DeckFlow.Core/DeckFlow.Core.csproj` clean
- `dotnet test DeckFlow.Core.Tests --filter "FullyQualifiedName~ContentTagVocabularyTests"` green
- `dotnet test DeckFlow.Core.Tests --filter "FullyQualifiedName~CategoryKnowledgeRepositoryTests"` green
</verification>

<success_criteria>
- ContentTagVocabulary.Staples curated set exists (CS-05 prereq)
- Server-side corpus Pr(A)/Pr(A∩B)/TotalDecks aggregate exposed via the facade and proven on a fixture (CS-07 denominator gap closed)
</success_criteria>

<output>
Create `.planning/phases/95-measured-style-extractor/95-03-SUMMARY.md` when done.
</output>
