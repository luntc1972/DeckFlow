---
phase: 98-card-grounding-guard
plan: 02
type: execute
wave: 2
depends_on: ["98-01"]
files_modified:
  - DeckFlow.Web/Services/Scryfall/ScryfallDtos.cs
  - DeckFlow.Web/Services/Scryfall/ScryfallErrorResponse.cs
  - DeckFlow.Web/Services/Scryfall/ScryfallCardResolver.cs
  - DeckFlow.Web/Services/Scryfall/CardGroundingGuard.cs
  - DeckFlow.Web/Extensions/ScryfallServiceCollectionExtensions.cs
  - DeckFlow.Web.Tests/Services/Scryfall/CardGroundingGuardTests.cs
autonomous: true
requirements: [CS-21, CS-24]

must_haves:
  truths:
    - "A card name with exactly one Scryfall match is accepted and the verdict carries the canonical Scryfall name, never the raw input"
    - "A fuzzy 404 with no match is rejected as NotFound; a fuzzy 404 with an ambiguous body is rejected as Ambiguous"
    - "A card whose commander legality is not 'legal' is rejected as NotLegal"
    - "A Scryfall outage/timeout yields UpstreamUnavailable (fail-closed), never an accept and never an unhandled crash"
    - "The same normalized name resolves against Scryfall only once across repeated calls (cached); the cache stores card resolution data, not the deck-context-dependent verdict"
    - "The guard is registered once in DI as a reusable singleton consumed by Phase 99 (CS-24)"
  artifacts:
    - path: "DeckFlow.Web/Services/Scryfall/CardGroundingGuard.cs"
      provides: "Strict ICardGroundingGuard implementation (single + batch, cache, fail-closed)"
      contains: "class CardGroundingGuard"
    - path: "DeckFlow.Web/Services/Scryfall/ScryfallErrorResponse.cs"
      provides: "404 error-body DTO to distinguish ambiguous from not_found (Pitfall 1)"
      contains: "record ScryfallErrorResponse"
    - path: "DeckFlow.Web/Services/Scryfall/ScryfallDtos.cs"
      provides: "ScryfallCard with new Legalities field (D-09)"
      contains: "legalities"
    - path: "DeckFlow.Web.Tests/Services/Scryfall/CardGroundingGuardTests.cs"
      provides: "Strict accept/reject/cache/ambiguous unit tests"
  key_links:
    - from: "DeckFlow.Web/Services/Scryfall/CardGroundingGuard.cs"
      to: "DeckFlow.Core.Knowledge.CardGrounding.CardGroundingRules"
      via: "static rule calls after fetch"
      pattern: "CardGroundingRules\\."
    - from: "DeckFlow.Web/Extensions/ScryfallServiceCollectionExtensions.cs"
      to: "ICardGroundingGuard"
      via: "AddSingleton registration"
      pattern: "AddSingleton<ICardGroundingGuard"
    - from: "DeckFlow.Web/Services/Scryfall/CardGroundingGuard.cs"
      to: "IScryfallCardResolver.ExecuteNamedFuzzyAsync"
      via: "strict fuzzy fallback call"
      pattern: "ExecuteNamedFuzzyAsync"
---

<objective>
Build the strict card-grounding guard: the Web-hosted `CardGroundingGuard` implementing the Core `ICardGroundingGuard` seam, backed by a net-new strict fuzzy resolver method, the `legalities` DTO field, a `ScryfallErrorResponse` DTO to split ambiguous from not-found on 404, an `IMemoryCache` resolution cache, and fail-closed semantics — plus DI registration so it is one reusable singleton (CS-24).

Purpose: This is CS-21 (strict fuzzy validator: one match = ok, 404/ambiguous/none = reject) and CS-24 (one reusable cached service consumed by Phase 99). The guard composes the existing throttled `IScryfallCardResolver` and the Core `CardGroundingRules` — no new HTTP plumbing, no new packages.
Output: 3 new/modified Web source files, 1 modified resolver, 1 DI edit, 1 new test file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/98-card-grounding-guard/98-CONTEXT.md
@.planning/phases/98-card-grounding-guard/98-RESEARCH.md
@.planning/phases/98-card-grounding-guard/98-PATTERNS.md
@.planning/phases/98-card-grounding-guard/98-01-SUMMARY.md

<interfaces>
<!-- Contracts from 98-01 (Core) + existing Scryfall plumbing. Use directly; do not re-derive. -->

From 98-01 (DeckFlow.Core.Knowledge.CardGrounding):
  interface ICardGroundingGuard { Task<CardGroundingVerdict> TryValidateAsync(string, CardGroundingDeckContext, CancellationToken); Task<CardGroundingBatchResult> ValidateAllAsync(IReadOnlyList<string>, CardGroundingDeckContext, CancellationToken); }
  record CardGroundingVerdict { bool Accepted; string CanonicalName; CardGroundingRejectReason RejectReason; }  // required/init
  enum CardGroundingRejectReason { None, NotFound, Ambiguous, NotLegal, IdentityViolation, SingletonDuplicate, Uncastable, UpstreamUnavailable }
  record CardGroundingDeckContext { IReadOnlySet<string> CommanderColorIdentity; IReadOnlySet<char> DeckProducedColors; IReadOnlySet<string> DeckCardNames; }
  record CardGroundingBatchResult { IReadOnlyList<CardGroundingVerdict> Verdicts; bool HasUpstreamFailure; }
  static class CardGroundingRules { bool IsLegalForCommander(IReadOnlyDictionary<string,string>?); bool IsWithinColorIdentity(IReadOnlyList<string>?, IReadOnlySet<string>); bool IsSingletonViolation(string, string, IReadOnlySet<string>); bool IsCastable(string?, IReadOnlySet<char>); }

Existing resolver (DeckFlow.Web/Services/Scryfall/ScryfallCardResolver.cs — read in full before editing):
  interface IScryfallCardResolver { Task<RestResponse<ScryfallCollectionResponse>> ExecuteCollectionAsync(RestRequest, CancellationToken); Task<ScryfallCard?> SearchFallbackCardAsync(...); Task<ScryfallCard?> SearchPrintingFallbackCardAsync(...); Task<ScryfallCard?> ResolveSingleAsync(...); }
  static string NormalizeLookupName(string); static string NormalizeForScryfall(string);
  The private _executeNamedAsync field already wraps ScryfallThrottle + the "scryfall" Polly pipeline around a cards/named GET.

Existing grounder cache pattern to mirror (DeckFlow.Web/Services/Scryfall/ScryfallCardNameGrounder.cs):
  PositiveCacheTtl = 24h; NegativeCacheTtl = 1h; cache key "card-grounder:" + name.Trim().ToLowerInvariant().
  NOTE: the guard MUST use a DIFFERENT key prefix "card-grounding-guard:" so its entries never collide with the grounder's in the shared IMemoryCache singleton.

Existing strict-fuzzy request shape (ScryfallCardResolver.SearchPrintingFallbackCardAsync last step, lines 195-204):
  RestRequest("cards/named", Method.Get); AddQueryParameter("fuzzy", NormalizeForScryfall(cardName)); ScryfallThrottle.ThrowIfUpstreamUnavailable(response.StatusCode);
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Legalities to ScryfallCard, add ScryfallErrorResponse DTO, add strict-fuzzy resolver method</name>
  <files>DeckFlow.Web/Services/Scryfall/ScryfallDtos.cs, DeckFlow.Web/Services/Scryfall/ScryfallErrorResponse.cs, DeckFlow.Web/Services/Scryfall/ScryfallCardResolver.cs</files>
  <read_first>
    - DeckFlow.Web/Services/Scryfall/ScryfallDtos.cs (current ScryfallCard shape — the Legalities field must be appended as the LAST optional positional parameter)
    - DeckFlow.Web/Services/Scryfall/ScryfallCardResolver.cs (full — the strict-fuzzy method must reuse the existing _executeNamedAsync throttled path and ThrowIfUpstreamUnavailable)
    - .planning/phases/98-card-grounding-guard/98-RESEARCH.md (Pitfall 1 captured 404 bodies; D-09 legalities shape verified live)
    - .planning/phases/98-card-grounding-guard/98-PATTERNS.md (ScryfallErrorResponse + Legalities shapes, lines 266-317)
  </read_first>
  <action>
    Three edits, all in `DeckFlow.Web/Services` namespace conventions:

    (1) `ScryfallDtos.cs` — append one optional positional parameter to the `ScryfallCard` record, AFTER the existing last parameter (`Rarity`): `[property: JsonPropertyName("legalities")] IReadOnlyDictionary<string, string>? Legalities = null`. Keep it LAST so existing positional/named call sites are unaffected. Verified live shape: lowercase format keys (`"commander"`, `"standard"`, ...), lowercase string values (`"legal"`, `"not_legal"`, `"restricted"`, `"banned"`).

    (2) New file `ScryfallErrorResponse.cs` in namespace `DeckFlow.Web.Services` (same namespace/file conventions as the other Scryfall DTOs, `using System.Text.Json.Serialization;`): `public sealed record ScryfallErrorResponse([property: JsonPropertyName("object")] string? Object, [property: JsonPropertyName("code")] string? Code, [property: JsonPropertyName("type")] string? Type = null, [property: JsonPropertyName("status")] int? Status = null, [property: JsonPropertyName("details")] string? Details = null);`. `Type` is nullable — it is present ("ambiguous") only on the ambiguous 404 and omitted on the plain not-found 404 (Pitfall 1). Add a doc comment noting the discriminator is the presence of `Type == "ambiguous"`, NOT `Code` (both cases return `"not_found"`).

    (3) `ScryfallCardResolver.cs` — add a new member to `IScryfallCardResolver`: `Task<RestResponse<ScryfallCard>> ExecuteNamedFuzzyAsync(string cardName, CancellationToken cancellationToken)`. Declare it as a throwing default interface method (throw `NotSupportedException` with a message pointing at the concrete resolver) so existing `IScryfallCardResolver` test doubles compile unchanged — this mirrors the project's throwing-default-interface-method precedent. Override it in `ScryfallCardResolver` with a real body: build `new RestRequest("cards/named", Method.Get)`, `AddQueryParameter("fuzzy", NormalizeForScryfall(cardName))`, call the existing `_executeNamedAsync(request, cancellationToken)`, then `ScryfallThrottle.ThrowIfUpstreamUnavailable(response.StatusCode)` (so 429/5xx surface as HttpRequestException before returning), and return the raw `RestResponse<ScryfallCard>` (the guard needs `.StatusCode` and `.Content` to parse the 404 body). Add XML doc. Do NOT reuse the lenient `SearchPrintingFallbackCardAsync` search cascade — the guard needs the STRICT single fuzzy call only (CS-21).
  </action>
  <verify>
    <automated>cd /mnt/c/users/chrislunt/source/personal/deckflow-cycle17 && dotnet build DeckFlow.Web/DeckFlow.Web.csproj -warnaserror 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "legalities" DeckFlow.Web/Services/Scryfall/ScryfallDtos.cs` returns >= 1 and the parameter is the last one in the `ScryfallCard` record.
    - `ScryfallErrorResponse.cs` exists with a nullable `Type` property mapped to JSON `"type"`.
    - `IScryfallCardResolver` declares `ExecuteNamedFuzzyAsync` and `ScryfallCardResolver` provides a concrete override that calls `ThrowIfUpstreamUnavailable` and returns the raw response.
    - `dotnet build DeckFlow.Web/DeckFlow.Web.csproj -warnaserror` succeeds, 0 warnings — confirming no existing `IScryfallCardResolver` implementer was broken by the new interface member.
    - The strict method uses `AddQueryParameter` for the fuzzy value (no string concatenation into the URL — V5 input validation).
  </acceptance_criteria>
  <done>ScryfallCard carries Legalities; ScryfallErrorResponse parses the 404 discriminator; the resolver exposes a strict throttled fuzzy call returning the raw response.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement CardGroundingGuard (strict single + batch, resolution cache, fail-closed) + tests + DI</name>
  <files>DeckFlow.Web/Services/Scryfall/CardGroundingGuard.cs, DeckFlow.Web/Extensions/ScryfallServiceCollectionExtensions.cs, DeckFlow.Web.Tests/Services/Scryfall/CardGroundingGuardTests.cs</files>
  <read_first>
    - DeckFlow.Web/Services/Scryfall/ScryfallCardNameGrounder.cs (cache TTL pattern, primary-ctor DI shape — mirror structure, NOT the lenient SearchPrintingFallbackCardAsync call)
    - DeckFlow.Web/Services/Scryfall/ScryfallCardResolver.cs (ExecuteCollectionAsync for the batch exact path; the new ExecuteNamedFuzzyAsync from Task 1; NormalizeLookupName/NormalizeForScryfall)
    - DeckFlow.Web.Tests/Services/Scryfall/ScryfallCardNameGrounderTests.cs (FakeResolver seam + Card() factory to copy)
    - DeckFlow.Web/Extensions/ScryfallServiceCollectionExtensions.cs (register right after the ICardNameGrounder line)
    - .planning/phases/98-card-grounding-guard/98-PATTERNS.md (CardGroundingGuard deviations, lines 203-263; DI, lines 384-405)
    - .planning/phases/98-card-grounding-guard/98-CONTEXT.md (D-03, D-04, D-12, D-13, D-14)
    - .planning/phases/98-card-grounding-guard/98-RESEARCH.md (Pitfall 1 error-body parse; Pitfall 3 deck-context)
  </read_first>
  <behavior>
    - TryValidateAsync accepts a card with exactly one Scryfall match and returns Accepted=true, CanonicalName=the Scryfall card's Name, RejectReason=None — even on an exact hit, the canonical Name is returned, never the raw input (D-03).
    - A fuzzy 404 whose error body has no `type` (or type != "ambiguous") returns Accepted=false, RejectReason=NotFound.
    - A fuzzy 404 whose error body has `"type": "ambiguous"` returns Accepted=false, RejectReason=Ambiguous.
    - A resolved card with commander legality != "legal" (or null/missing legalities) returns RejectReason=NotLegal (fail-closed).
    - A resolved, legal card off the commander color identity returns IdentityViolation; already in the deck (non-basic) returns SingletonDuplicate; not castable by the deck's produced colors returns Uncastable. Reject precedence is fixed: NotFound/Ambiguous (resolution) -> NotLegal -> IdentityViolation -> SingletonDuplicate -> Uncastable, so a fixture can assert the exact first-failing reason.
    - A resolver exception (upstream outage) yields RejectReason=UpstreamUnavailable and is NOT cached (so a later call retries), never Accepted.
    - The same normalized name hits the resolver only once across repeated TryValidateAsync calls with different deck contexts (resolution is cached; deck-dependent rules re-run each call against the fresh context).
    - ValidateAllAsync returns one verdict per input name in order, with HasUpstreamFailure=true iff any verdict is UpstreamUnavailable.
  </behavior>
  <action>
    Create `public sealed class CardGroundingGuard(IScryfallCardResolver resolver, IMemoryCache cache) : ICardGroundingGuard` in `DeckFlow.Web.Services.Scryfall`, mirroring the grounder's primary-constructor + TTL-constant structure but with strict, deck-aware behavior:

    Resolution vs verdict caching (CRITICAL — do NOT cache the full verdict). Define a small `private sealed record CardResolution` holding ONLY deck-independent, verdict-relevant fields (D-14, 512MB RAM cap): CanonicalName, ColorIdentity (IReadOnlyList<string>?), CommanderLegalityStatus (string?), ManaCost (string?), TypeLine (string), and a `CardGroundingRejectReason ResolutionReason` (None when the card resolved, else NotFound/Ambiguous). Cache THIS record keyed by `"card-grounding-guard:" + name.Trim().ToLowerInvariant()` — 24h TTL when resolved, 1h when NotFound/Ambiguous. Never cache full `ScryfallCard` JSON; never cache the deck-dependent verdict (identity/singleton/castability vary per request and would cross-contaminate decks — this is the cache-poisoning threat T-98-11).

    Resolution path (get-or-fetch): on cache miss, do an exact `cards/collection` lookup (build the request per ScryfallCardResolver.ResolveSingleAsync's shape and call `resolver.ExecuteCollectionAsync`); if a normalized-name match is found, project it into CardResolution (ResolutionReason=None). On a collection miss, call the new `resolver.ExecuteNamedFuzzyAsync(name, ct)`: 200 with Data => project the card (ResolutionReason=None); 404 => deserialize `response.Content` into `ScryfallErrorResponse` via `System.Text.Json.JsonSerializer.Deserialize<ScryfallErrorResponse>` and set ResolutionReason = (Type == "ambiguous" ? Ambiguous : NotFound), CanonicalName = raw input. Wrap the whole fetch in try/catch; a caught exception (incl. the HttpRequestException thrown by ThrowIfUpstreamUnavailable) returns an UpstreamUnavailable verdict WITHOUT caching (D-12 fail-closed) — do NOT collapse it into NotFound.

    Verdict path: if ResolutionReason != None, return that as the verdict. Else apply the Core rules in the fixed precedence: `CardGroundingRules.IsLegalForCommander(legalities-as-dictionary)` — but note the cached record stores only the commander legality STATUS string; reconstruct a single-entry dictionary `{ "commander": status }` (or overload the rule usage) so the fail-closed null check still holds when status is null. Then `IsWithinColorIdentity`, `IsSingletonViolation`, `IsCastable`, each producing the corresponding RejectReason on failure; if all pass, Accepted=true with the canonical name. Normalize deck names and the candidate consistently (the same trim+lowercase the rules assume).

    ValidateAllAsync: batch the exact `cards/collection` lookups at 75 identifiers per call (mirror the batch shape), resolve the not-found subset via the per-name strict fuzzy path, then run the verdict path per name; assemble `CardGroundingBatchResult { Verdicts = <in input order>, HasUpstreamFailure = Verdicts.Any(v => v.RejectReason == UpstreamUnavailable) }`. Reuse the same get-or-fetch cache so repeat names across the batch and across requests do not re-hit Scryfall.

    DI: in `ScryfallServiceCollectionExtensions.AddDeckFlowScryfallServices`, add `services.AddSingleton<ICardGroundingGuard, CardGroundingGuard>();` immediately after the existing `services.AddSingleton<ICardNameGrounder, ScryfallCardNameGrounder>();` line, and add `using DeckFlow.Core.Knowledge.CardGrounding;` to the top import block. Both ctor deps (IScryfallCardResolver, IMemoryCache) are already registered, so the `AddSingleton<TInterface,TImpl>` shorthand resolves automatically.

    Tests (`CardGroundingGuardTests.cs`, namespace `DeckFlow.Web.Tests`): copy the `FakeResolver` + `Card()` factory pattern from `ScryfallCardNameGrounderTests.cs`, extending the fake to also drive `ExecuteNamedFuzzyAsync` (return canned `RestResponse<ScryfallCard>` with a chosen StatusCode and Content string) and `ExecuteCollectionAsync`. Cover every bullet in the behavior block: exact/fuzzy accept returns canonical name; NotFound vs Ambiguous split via distinct 404 Content bodies; NotLegal (banned/null legalities); IdentityViolation; SingletonDuplicate (with a basic-land exempt case); Uncastable; UpstreamUnavailable on a thrown resolver + assert it is NOT cached (second call re-invokes the resolver); cache single-fetch across two contexts (assert resolver call count == 1). These are the CS-21 + CS-24 tests.
  </action>
  <verify>
    <automated>cd /mnt/c/users/chrislunt/source/personal/deckflow-cycle17 && dotnet build DeckFlow.Web.Tests/DeckFlow.Web.Tests.csproj -warnaserror 2>&1 | tail -5 && dotnet test DeckFlow.Web.Tests/DeckFlow.Web.Tests.csproj --filter "FullyQualifiedName~CardGroundingGuard" 2>&1 | tail -6</automated>
  </verify>
  <acceptance_criteria>
    - `CardGroundingGuard.cs` uses cache key prefix `"card-grounding-guard:"` — `grep -c "card-grounding-guard:" DeckFlow.Web/Services/Scryfall/CardGroundingGuard.cs` returns >= 1 and `grep -c "card-grounder:"` returns 0 (no collision with the grounder key).
    - The cached type is the internal resolution record, NOT `CardGroundingVerdict` and NOT `ScryfallCard`: a test asserts the same name across two different `CardGroundingDeckContext` values invokes the resolver exactly once yet can produce different verdicts (accept in one deck, IdentityViolation in another).
    - A test with two distinct fuzzy-404 Content bodies (one containing `"type":"ambiguous"`, one without) yields `RejectReason.Ambiguous` and `RejectReason.NotFound` respectively.
    - A test with a thrown resolver yields `RejectReason.UpstreamUnavailable` AND a subsequent call re-invokes the resolver (upstream failures are not cached).
    - An accepted verdict's `CanonicalName` equals the Scryfall card Name even when the input was a different-cased/trimmed string.
    - `grep -c "AddSingleton<ICardGroundingGuard" DeckFlow.Web/Extensions/ScryfallServiceCollectionExtensions.cs` returns 1.
    - `dotnet test ... --filter "FullyQualifiedName~CardGroundingGuard"` all passed, 0 failed; `dotnet build DeckFlow.Web.Tests -warnaserror` 0 warnings.
  </acceptance_criteria>
  <done>Strict guard implemented with resolution-only caching, deterministic reject precedence, fail-closed upstream handling; registered as a reusable singleton; CS-21 + CS-24 tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → guard | Untrusted candidate card names (possibly LLM-hallucinated, crafted, or containing Scryfall query operators) enter the guard |
| guard → Scryfall API | Card names cross into upstream HTTP query parameters |
| shared IMemoryCache | Guard and the P96 grounder share one cache singleton; keys must not collide |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-98-10 | Tampering (query injection) | `ExecuteNamedFuzzyAsync` / collection request build | mitigate | All card names passed via `RestRequest.AddQueryParameter` / JSON body objects — never string-concatenated into a URL (V5); asserted in Task 1 criteria |
| T-98-11 | Tampering / Information Disclosure (cross-deck cache contamination) | Verdict caching | mitigate | Cache only deck-INDEPENDENT resolution data (name/identity/legality/cost); deck-dependent rules (identity/singleton/castability) re-run per request — asserted by the same-name-two-contexts test |
| T-98-12 | Denial of Service | `ValidateAllAsync` batch fetch | mitigate | Existing `ScryfallThrottle` process-wide pacing + 75-per-batch chunking + shared cache bound total Scryfall traffic |
| T-98-13 | Spoofing (fake/illegal card accepted) | Strict fuzzy + legality path | mitigate | 404/ambiguous reject (CS-21); commander legality fail-closed on null/missing/`!= "legal"`; deterministic reject precedence |
| T-98-14 | Elevation (fail-open on outage) | Upstream exception handling | mitigate | Caught exceptions become UpstreamUnavailable (reject), never Accepted, and are not cached (D-12) |
| T-98-15 | Tampering (cache key collision) | Shared IMemoryCache | mitigate | Distinct `"card-grounding-guard:"` key prefix vs the grounder's `"card-grounder:"` |
| T-98-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs (RESEARCH Package Legitimacy Audit: zero new dependencies); composes existing RestSharp/Polly/Memory cache only |
</threat_model>

<verification>
- `dotnet build DeckFlow.Web/DeckFlow.Web.csproj -warnaserror` and `DeckFlow.Web.Tests` clean (0 warnings).
- `dotnet test DeckFlow.Web.Tests --filter "FullyQualifiedName~CardGroundingGuard"` all green.
- Existing Scryfall service tests still green (shared resolver/throttle untouched behaviorally): `dotnet test DeckFlow.Web.Tests --filter "FullyQualifiedName~Scryfall"`.
- EOL check per CLAUDE.md if any file shows churn (`git diff --stat` vs `git diff --ignore-all-space --stat`).
</verification>

<success_criteria>
- CS-21: strict fuzzy validator accepts exactly-one-match (returning canonical name) and rejects 404/ambiguous/none with the correct distinct reason.
- CS-24: one reusable, DI-registered singleton guard with a resolution cache consumed by Phase 99; upstream failures fail closed and are not cached.
- No new packages; all Scryfall access flows through the existing throttled resolver.
</success_criteria>

<output>
Create `.planning/phases/98-card-grounding-guard/98-02-SUMMARY.md` when done.
</output>
