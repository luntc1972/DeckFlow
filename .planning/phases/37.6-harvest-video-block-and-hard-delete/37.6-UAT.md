---
status: complete
phase: 37.6-harvest-video-block-and-hard-delete
source: [37.6-01-SUMMARY.md]
started: 2026-06-11T17:07:44Z
updated: 2026-06-11T17:18:00Z
---

## Current Test

[testing complete]

## Tests

### 1. block-video — block + two-DB hard-delete
expected: block-video <id> --db <content-kb.db> exits 0; deletes content_videos (+cascaded transcripts/summaries/clips) keyed on the globally-unique youtube id, deletes the content_site_index row, and writes a blocked_videos row.
result: pass
evidence: |
  Live-run on a throwaway COPY of artifacts/uat-content-kb.db (block is irreversible).
  Target 00-irk7cuzc (vid 696). BEFORE video=1 transcripts=1 summaries=1 clips=8 site_index=1.
  `block-video 00-irk7cuzc --reason "uat smoke" --db <copy>` exit 0.
  AFTER all → 0; blocked_videos=1. Log: "blocked video 00-irk7cuzc content_rows_deleted=1 site_index_rows_deleted=1".

### 2. harvest skips blocked id, ingests sibling
expected: After blocking an id, a harvest including it skips it (no re-insert); a non-blocked sibling still ingests.
result: pass
evidence: |
  Re-blocked 00-irk7cuzc, then `harvest --video-ids 00-irk7cuzc --source-id 9 --db <copy>` exit 0,
  returned immediately (skip short-circuits BEFORE network fetch), content_videos for the id stayed 0
  (no re-insert). Log: "skipped blocked 00-irk7cuzc". Sibling-ingest half not run live (needs YouTube
  network) — covered by xUnit RunHarvestAsync skip/ingest regression + verified chokepoint placement.

### 3. unblock-video re-permits ingest
expected: unblock-video removes the blocked_videos row (exits 0); never-blocked id is a clean no-op exit 0.
result: pass
evidence: |
  `unblock-video 00-irk7cuzc --db <copy>` exit 0, blocked_videos count → 0 (log "unblocked video 00-irk7cuzc").
  `unblock-video never-blocked-xyz` exit 0, no row removed (D2/A3 no-op).

### 4. list-blocked shows entries with correct UTC time
expected: list-blocked prints blocked ids with reason + blocked_utc; timestamp reads as correct UTC (not local-shifted).
result: pass
evidence: |
  `list-blocked --db <copy>` exit 0 →
  "00-irk7cuzc  2026-06-11T17:16:20.0000000+00:00  uat smoke" — +00:00 UTC offset, matches stored value
  (the review-fixed AssumeUniversal|AdjustToUniversal parse; not shifted by local -06:00).

### 5. blocked_videos survives a content_* reset (name carve-out)
expected: block-list table named exactly `blocked_videos`, NOT prefixed content_, so a Phase 37.5 content_* reset won't drop it.
result: pass
evidence: |
  sqlite_master query returned table name `blocked_videos` (no content_ prefix). Carve-out // Why: comment
  present in BlockedVideoStore.cs.

## Summary

total: 5
passed: 5
issues: 0
pending: 0
skipped: 0

## Gaps

[none]

## Notes

- All five tests run LIVE via DeckFlow.CLI (Windows dotnet) against a throwaway copy of
  artifacts/uat-content-kb.db — the real 777-video corpus was NOT mutated (block is an
  irreversible hard-delete). Build green (0/0, no CS0535); Codex review caught + fixed
  2 real defects (blocked_utc UTC parse, weak D5 ordering test) before this UAT.
