---
phase: 92-pull-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - DeckFlow.Core/Integration/IGitRepository.cs
  - DeckFlow.Core/Integration/GitRepository.cs
  - DeckFlow.Core/Content/SyncDiffEntry.cs
  - DeckFlow.Core/Content/ContentSyncDiffClassifier.cs
  - DeckFlow.Studio.Tests/TestDoubles/FakeGitRepository.cs
  - DeckFlow.Core.Tests/Content/ContentSyncDiffClassifierTests.cs
autonomous: true
requirements: [SYNC-14, SYNC-15]

must_haves:
  truths:
    - "IGitRepository exposes FetchAsync + GetBehindCountAsync; GitRepository implements both via the shared BuildStartInfo shell-out (SYNC-14 behind-detection seam)"
    - "A stale checkout is knowable: GetBehindCountAsync runs `git rev-list --count HEAD..{remote}/{branch}` — the exact operand-reversal of the existing GetSubjectsAheadOfRemoteAsync ahead-check"
    - "SyncDiffEntry carries a BodyDivergenceStatus BodyDivergence stamp defaulting to NotApplicable — an ORTHOGONAL axis to SyncDiffKind, NOT a fifth kind value (A2 ratified per D-01)"
    - "The classifier stays pure/I-O-free: BuildEntry sets BodyDivergence = NotApplicable, leaving the real stamp to the coordinator (same convention as ArtifactDownloaded)"
  artifacts:
    - path: "DeckFlow.Core/Integration/IGitRepository.cs"
      provides: "FetchAsync + GetBehindCountAsync throwing default-interface-members (SYNC-14)"
      contains: "GetBehindCountAsync"
    - path: "DeckFlow.Core/Integration/GitRepository.cs"
      provides: "Real fetch + rev-list --count shell-out via BuildStartInfo (GIT_TERMINAL_PROMPT=0)"
      contains: "rev-list"
    - path: "DeckFlow.Core/Content/SyncDiffEntry.cs"
      provides: "BodyDivergenceStatus enum + BodyDivergence property (SYNC-15)"
      contains: "enum BodyDivergenceStatus"
    - path: "DeckFlow.Studio.Tests/TestDoubles/FakeGitRepository.cs"
      provides: "Explicit Fetch/GetBehindCount canned-return + fault-injection fields for downstream tests"
      contains: "CannedBehindCount"
  key_links:
    - from: "DeckFlow.Studio.Tests/TestDoubles/FakeGitRepository.cs"
      to: "IGitRepository.FetchAsync/GetBehindCountAsync"
      via: "explicit implementation (NOT the throwing default) so 92-02 can exercise behind + fetch-fail branches"
      pattern: "public Task.*FetchAsync|GetBehindCountAsync"
    - from: "DeckFlow.Core/Integration/GitRepository.cs FetchAsync/GetBehindCountAsync"
      to: "BuildStartInfo(repoRoot)"
      via: "shared ProcessStartInfo builder (ArgumentList-only, GIT_TERMINAL_PROMPT=0)"
      pattern: "BuildStartInfo"
---

<objective>
Lay the two Core foundations both later plans build on, with zero coupling between them so they land in one wave:

1. **SYNC-14 git seam** — add `FetchAsync` + `GetBehindCountAsync` to `IGitRepository` (throwing default-interface-methods) and implement them for real in `GitRepository` via the existing `BuildStartInfo`/`RunAndCaptureAsync` shell-out plumbing. This is the missing capability to know a local checkout is *behind its remote* (today's `GetSubjectsAheadOfRemoteAsync` only reads local-ahead and never fetches).
2. **SYNC-15 model** — add a `BodyDivergenceStatus` enum + `BodyDivergence` property to `SyncDiffEntry`, defaulted to `NotApplicable` by the pure classifier. This ratifies RESEARCH assumption **A2**: divergence is an *orthogonal* stamp (like `ArtifactDownloaded`), NOT a fifth `SyncDiffKind` value.

Purpose: both are pure-Core, side-effect-free additions with exact in-repo analogs; keeping them in one wave-1 plan unblocks the coordinator (92-02) and page (92-03) immediately.
Output: extended `IGitRepository`/`GitRepository`, extended `SyncDiffEntry`/classifier, extended `FakeGitRepository`, new classifier test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/92-pull-hardening/92-CONTEXT.md
@.planning/phases/92-pull-hardening/92-RESEARCH.md
@.planning/phases/92-pull-hardening/92-PATTERNS.md

<interfaces>
<!-- Contracts the executor implements/extends. Extracted from the codebase — no exploration needed. -->

Existing IGitRepository member to MIRROR (DeckFlow.Core/Integration/IGitRepository.cs:156-160):
  Task<IReadOnlyList<string>> GetSubjectsAheadOfRemoteAsync(
      string repoRoot, string remote, string branch, CancellationToken ct = default);
  // impl at GitRepository.cs:260-285 — runs `git log --format=%s {remote}/{branch}..HEAD`

Throwing default-interface-method idiom (DeckFlow.Studio/Services/IProdContentReader.cs:44-64):
  Task<bool?> TryReadFlagAsync(...) => throw new NotSupportedException("...does not support flag reads.");

Shared shell-out plumbing in GitRepository.cs (route ALL new members through these — do NOT hand-roll ProcessStartInfo):
  private static ProcessStartInfo BuildStartInfo(string workingDirectory)   // line 289; sets Environment["GIT_TERMINAL_PROMPT"]="0" at line 304, UseShellExecute=false, ArgumentList-only
  private static async Task<string> RunAndCaptureAsync(ProcessStartInfo, CancellationToken)  // line 309; throws GitCommandException on non-zero exit
  // File header currently has ONLY `using System.Diagnostics;` (line 1) — no System.Globalization.

SyncDiffEntry today (DeckFlow.Core/Content/SyncDiffEntry.cs) — existing orthogonal-stamp property to MIRROR (lines 68-72):
  public bool ArtifactDownloaded { get; init; }   // classifier leaves false; coordinator stamps post-classify

ContentSyncDiffClassifier.BuildEntry object initializer (DeckFlow.Core/Content/ContentSyncDiffClassifier.cs:124-135) currently sets:
  ArtifactDownloaded = false   // add BodyDivergence = BodyDivergenceStatus.NotApplicable alongside it

FakeGitRepository existing canned/fault shape to MIRROR (DeckFlow.Studio.Tests/TestDoubles/FakeGitRepository.cs:35-38, 106-114):
  public List<string> CannedSubjectsAhead { get; set; } = new();
  public Exception? ThrowOnSubjectsAhead { get; set; }
  public Task<IReadOnlyList<string>> GetSubjectsAheadOfRemoteAsync(...) { if (ThrowOnSubjectsAhead is not null) throw ...; return Task.FromResult(...); }
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add FetchAsync + GetBehindCountAsync git behind-detection seam (SYNC-14)</name>
  <files>DeckFlow.Core/Integration/IGitRepository.cs, DeckFlow.Core/Integration/GitRepository.cs, DeckFlow.Studio.Tests/TestDoubles/FakeGitRepository.cs</files>
  <read_first>
    - DeckFlow.Core/Integration/IGitRepository.cs (the interface being extended — read the GetSubjectsAheadOfRemoteAsync member doc-comment style at lines 136-160)
    - DeckFlow.Core/Integration/GitRepository.cs (analog: GetSubjectsAheadOfRemoteAsync impl 260-285, PushAsync 206-225, BuildStartInfo 289-307, RunAndCaptureAsync 309-321)
    - DeckFlow.Studio/Services/IProdContentReader.cs:44-64 (throwing-DIM precedent)
    - DeckFlow.Studio.Tests/TestDoubles/FakeGitRepository.cs (the CannedSubjectsAhead/ThrowOnSubjectsAhead triplet to replicate)
    - .planning/phases/92-pull-hardening/92-PATTERNS.md (Pattern for GitRepository — the two recommended impls)
  </read_first>
  <behavior>
    - FakeGitRepository.GetBehindCountAsync returns CannedBehindCount (default 0) → downstream test can assert "behind" path.
    - FakeGitRepository.FetchAsync records the (repoRoot, remote, branch) call and throws ThrowOnFetch when set → downstream test can assert "fetch failed" path.
    - GitRepository members throw GitCommandException on non-zero git exit (inherited from RunAndCaptureAsync) — no new exception type.
  </behavior>
  <action>
    On `IGitRepository` add two members as THROWING default-interface-methods (mirror the IProdContentReader.cs:44-64 idiom exactly — a default body that `throw new NotSupportedException(...)`, NOT abstract members) so the interface's doubles compile unchanged:
    `Task FetchAsync(string repoRoot, string remote, string branch, CancellationToken ct = default)` and
    `Task<int> GetBehindCountAsync(string repoRoot, string remote, string branch, CancellationToken ct = default)`.
    Give each an XML doc `<summary>` plus a `<remarks>` "Why:" paragraph in the style of PushAsync/GetSubjectsAheadOfRemoteAsync: for FetchAsync note it is a SCOPED fetch (`git fetch {remote} {branch}`, NOT `--all`) that updates only `refs/remotes/{remote}/{branch}`; for GetBehindCountAsync note it is the operand-reversal (`HEAD..{remote}/{branch}`) of GetSubjectsAheadOfRemoteAsync's ahead-check and requires a prior fetch to be meaningful. Document that both throw GitCommandException on failure and the caller treats that as "cannot determine, proceed best-effort."

    On `GitRepository` implement both concretely, routing through `BuildStartInfo(repoRoot)` (never a hand-rolled ProcessStartInfo — Pitfall 6) and `RunAndCaptureAsync`:
    - `FetchAsync`: `ArgumentException.ThrowIfNullOrWhiteSpace` on all three string args (matching PushAsync); `startInfo.ArgumentList.Add("fetch"); .Add(remote); .Add(branch);` then `await RunAndCaptureAsync(startInfo, ct)`.
    - `GetBehindCountAsync`: same arg guards; `ArgumentList.Add("rev-list"); .Add("--count"); .Add($"HEAD..{remote}/{branch}");` then `int.Parse(stdout.Trim(), CultureInfo.InvariantCulture)`. Add `using System.Globalization;` to the file header (currently only `using System.Diagnostics;` — verify no CultureInfo usage exists first).

    On `FakeGitRepository` implement BOTH explicitly (do NOT rely on the throwing default — that would make the downstream branches untestable): add `public int CannedBehindCount { get; set; }`, `public Exception? ThrowOnFetch { get; set; }`, `public List<(string RepoRoot, string Remote, string Branch)> FetchCalls { get; } = new();`. `FetchAsync` records the tuple into FetchCalls, throws ThrowOnFetch when non-null, else returns `Task.CompletedTask`. `GetBehindCountAsync` returns `Task.FromResult(CannedBehindCount)`. Match the existing file's field-grouping comment style.

    Preserve LF line endings; touch only changed lines (changed-lines format gate). New interface members MUST be the throwing-DIM shape — the CarveOutGuard/`{ get; init; }` rules are unaffected here.
  </action>
  <verify>
    <automated>dotnet.exe build DeckFlow.sln (0 warnings / 0 errors — the throwing-DIM keeps every existing IGitRepository double compiling)</automated>
  </verify>
  <acceptance_criteria>
    - IGitRepository.cs declares FetchAsync + GetBehindCountAsync as `=> throw new NotSupportedException(...)` default members.
    - GitRepository.cs FetchAsync uses `ArgumentList` with exactly `fetch`, `{remote}`, `{branch}` (scoped, no `--all`); GetBehindCountAsync uses `rev-list`, `--count`, `HEAD..{remote}/{branch}`.
    - Both GitRepository members go through BuildStartInfo(repoRoot) (grep: no `new ProcessStartInfo` inside either new method).
    - FakeGitRepository implements both explicitly with CannedBehindCount + ThrowOnFetch + FetchCalls; `dotnet.exe build DeckFlow.sln` is 0/0.
  </acceptance_criteria>
  <done>Solution builds 0/0; the git seam exists and is fake-injectable for behind + fetch-fail branches.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add orthogonal BodyDivergenceStatus stamp to SyncDiffEntry (SYNC-15, A2)</name>
  <files>DeckFlow.Core/Content/SyncDiffEntry.cs, DeckFlow.Core/Content/ContentSyncDiffClassifier.cs, DeckFlow.Core.Tests/Content/ContentSyncDiffClassifierTests.cs</files>
  <read_first>
    - DeckFlow.Core/Content/SyncDiffEntry.cs (mirror the ArtifactDownloaded property + its doc comment at 68-72; mirror the SyncDiffKind enum doc style at 5-28)
    - DeckFlow.Core/Content/ContentSyncDiffClassifier.cs (BuildEntry initializer at 112-135, especially `ArtifactDownloaded = false` at 134)
    - DeckFlow.Core.Tests/Content/ContentSyncDiffClassifierTests.cs (existing `[Fact]` conventions, e.g. Classify_KeyInProdOnly_IsMissingLocally at 47-59)
    - .planning/phases/92-pull-hardening/92-RESEARCH.md (Pattern 3 + Assumptions A2 — orthogonal, not a 5th SyncDiffKind)
  </read_first>
  <behavior>
    - A newly-classified entry (any Kind) carries BodyDivergence == BodyDivergenceStatus.NotApplicable straight from the classifier (the classifier never evaluates hashes — it stays pure/I-O-free).
  </behavior>
  <action>
    In SyncDiffEntry.cs add a new `public enum BodyDivergenceStatus` with exactly four members in this order — `NotApplicable`, `Clean`, `Confirmed`, `Indeterminate` — each with an XML doc `<summary>` matching the file's SyncDiffKind doc density: NotApplicable = no resolved git body or no ProdRow (LocalOnly), Clean = git-body hash matches prod body_sha256, Confirmed = git-body hash DIFFERS from prod body_sha256 (D-01, excluded from default adopt), Indeterminate = prod body_sha256 is null/legacy (D-01a, treated like Confirmed for adopt-exclusion). Add a class-level `<remarks>` on the enum stating it is ORTHOGONAL to SyncDiffKind (a temporal axis) — a ProdNewer or MissingLocally entry can independently be divergent.

    On the `SyncDiffEntry` record add `public BodyDivergenceStatus BodyDivergence { get; init; }` immediately after `ArtifactDownloaded`, with a doc comment mirroring ArtifactDownloaded's ("classifier leaves the default; the coordinator stamps it post-classify — SYNC-15"). Keep it `{ get; init; }` (CarveOut: never convert to get-only).

    In ContentSyncDiffClassifier.BuildEntry, add `BodyDivergence = BodyDivergenceStatus.NotApplicable,` to the object initializer alongside `ArtifactDownloaded = false,`. Do NOT add any hashing/file I/O to the classifier — it must stay pure per its own class doc comment.

    In ContentSyncDiffClassifierTests.cs add one `[Fact]` (e.g. `Classify_DefaultsBodyDivergence_ToNotApplicable`) asserting that a classified prod-only (MissingLocally) entry has `BodyDivergence == BodyDivergenceStatus.NotApplicable`, following the existing fixture/`[Fact]` conventions in the file.

    Preserve LF; changed-lines-only formatting.
  </action>
  <verify>
    <automated>dotnet.exe test DeckFlow.Core.Tests --filter "FullyQualifiedName~ContentSyncDiffClassifier"</automated>
  </verify>
  <acceptance_criteria>
    - SyncDiffEntry.cs contains `enum BodyDivergenceStatus` with members NotApplicable, Clean, Confirmed, Indeterminate (that exact order/spelling) and a `BodyDivergence { get; init; }` property.
    - ContentSyncDiffClassifier.BuildEntry sets `BodyDivergence = BodyDivergenceStatus.NotApplicable` and adds NO file/hash I/O (grep: classifier has no ComputeBodySha256/File.ReadAllText).
    - New `[Fact]` asserts a classified entry defaults BodyDivergence to NotApplicable; the ContentSyncDiffClassifier test filter passes.
  </acceptance_criteria>
  <done>The divergence stamp exists on the model, defaults correctly, and the classifier remains pure; scoped test passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Studio host → `git` CLI | remote/branch names become subprocess arguments |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-92-01 | Tampering | GitRepository.FetchAsync / GetBehindCountAsync | mitigate | Pass args via `ProcessStartInfo.ArgumentList` (never string-concatenated `Arguments`), `UseShellExecute=false` — inherited from `BuildStartInfo`; no shell interpolation of remote/branch |
| T-92-05 | Denial of Service | GitRepository.FetchAsync (network op) | mitigate | Route through `BuildStartInfo` which sets `GIT_TERMINAL_PROMPT=0` (no hanging credential prompt) and pass `cancellationToken` to `RunAndCaptureAsync` |
| T-92-SC | Tampering | npm/pip/cargo installs | accept | No package installs this phase (RESEARCH §Standard Stack: BCL + existing `git` CLI only) — nothing to verify |
</threat_model>

<verification>
- `dotnet.exe build DeckFlow.sln` → 0 warnings / 0 errors (throwing-DIM keeps all existing IGitRepository doubles compiling).
- `dotnet.exe test DeckFlow.Core.Tests --filter "FullyQualifiedName~ContentSyncDiffClassifier"` → green.
- EOL clean (LF); changed-lines format gate clean.
</verification>

<success_criteria>
- IGitRepository has FetchAsync + GetBehindCountAsync (throwing DIMs); GitRepository implements both via BuildStartInfo shell-out.
- SyncDiffEntry has BodyDivergenceStatus + BodyDivergence, defaulted NotApplicable by the pure classifier.
- FakeGitRepository can drive behind-count and fetch-fault for downstream coordinator tests.
</success_criteria>

<output>
Create `.planning/phases/92-pull-hardening/92-01-SUMMARY.md` when done.
</output>
