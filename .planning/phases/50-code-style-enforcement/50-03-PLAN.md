---
phase: 50-code-style-enforcement
plan: 03
type: execute
wave: 2
depends_on: ["50-01"]
files_modified:
  - DeckFlow.Core.Tests/CarveOutGuardTests.cs
autonomous: true
requirements: [FMT-02]
user_setup: []

must_haves:
  truths:
    - "An xUnit test formats four fixtures with the reconciled .editorconfig and asserts each is byte-identical after formatting"
    - "The four fixtures cover: { get; init; }, a raw-string literal, an own-line [Attribute], and a switch expression"
    - "The test is a regression tripwire on FUTURE .editorconfig edits — if a later edit weakens a carve-out, the test fails loudly"
    - "The test is designed to run in CI (push-and-watch) since VSTest is unreliable in WSL"
  artifacts:
    - path: "DeckFlow.Core.Tests/CarveOutGuardTests.cs"
      provides: "Carve-out byte-identity regression assertions (FMT-02)"
      contains: "init"
      min_lines: 40
  key_links:
    - from: "DeckFlow.Core.Tests/CarveOutGuardTests.cs"
      to: "the reconciled .editorconfig"
      via: "dotnet format shell-out over a temp .cs carrying the repo .editorconfig"
      pattern: "editorconfig|dotnet"
---

<objective>
Add the FMT-02 carve-out guard test: an xUnit test that formats four code fixtures with the reconciled `.editorconfig` and asserts each is byte-identical afterward. It is a regression tripwire — if a future `.editorconfig` edit weakens a carve-out, this test fails loudly in CI.

Purpose: The four code-affecting carve-outs (`{ get; init; }`, raw-string literal, own-line `[Attribute]`, switch expression) must survive the formatter. RESEARCH proved the formatter does not touch them today (resharper_* keys are invisible to dotnet format; init/switch verified inert), so this test guards against FUTURE config drift, not the formatter itself.

Output: `DeckFlow.Core.Tests/CarveOutGuardTests.cs`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/50-code-style-enforcement/50-SPEC.md
@.planning/phases/50-code-style-enforcement/50-RESEARCH.md
@.planning/phases/50-code-style-enforcement/50-01-SUMMARY.md
@.editorconfig

<interfaces>
<!-- xUnit 2.9.3 is already referenced by DeckFlow.Core.Tests (no new package). Namespace: DeckFlow.Core.Tests. -->
<!-- Test class: public sealed class CarveOutGuardTests (project convention: sealed, *Tests suffix, [Fact]). -->

<!-- Mechanism (RESEARCH Code Examples + Pitfall 1): write each fixture to a temp .cs file in a REPO-LOCAL temp dir -->
<!-- (NOT /tmp — Windows dotnet.exe from WSL can't resolve /tmp), alongside (or under a dir governed by) the repo .editorconfig, -->
<!-- run `dotnet format whitespace --folder <dir>` (or full `dotnet format`), read the file back, assert byte-equality. -->

<!-- Four fixtures (all must be byte-identical after format): -->
<!--   1. init accessor:     public string Name { get; init; } = ""; -->
<!--   2. raw-string literal: a """ ... """ block whose interior indentation must NOT change -->
<!--   3. own-line attribute: [JsonPropertyName("x")] on its OWN line above a property (must NOT inline) -->
<!--   4. switch expression:  a => switch { ... } expression form (must NOT become a switch statement) -->

<!-- CLAUDE.md: VSTest unreliable in WSL -> target CI execution (push-and-watch). RESEARCH Open Q2: do NOT gate the -->
<!-- pre-commit hook on this test. It is a CI-run regression guard. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write CarveOutGuardTests.cs with four byte-identity carve-out assertions</name>
  <files>DeckFlow.Core.Tests/CarveOutGuardTests.cs</files>
  <behavior>
    - Test 1 (InitAccessor_SurvivesFormatting_ByteIdentical): a `{ get; init; }` property string is byte-identical after formatting with the repo `.editorconfig`.
    - Test 2 (RawStringLiteral_SurvivesFormatting_ByteIdentical): a `"""`-delimited raw-string literal with deliberate interior indentation is byte-identical after formatting.
    - Test 3 (OwnLineAttribute_SurvivesFormatting_ByteIdentical): an `[Attribute]` on its own line above a member is NOT inlined — byte-identical after formatting.
    - Test 4 (SwitchExpression_SurvivesFormatting_ByteIdentical): a switch EXPRESSION is not rewritten to a switch statement — byte-identical after formatting.
  </behavior>
  <read_first>
    - .planning/phases/50-code-style-enforcement/50-RESEARCH.md (Code Examples "Carve-out guard test"; Pitfall 1 repo-local temp; Open Q2/Q3; A3 CI-targeting)
    - .editorconfig (the reconciled config the fixtures are formatted against — carve-out lines 45-82)
    - .planning/phases/50-code-style-enforcement/50-01-SUMMARY.md (confirm reconciliation kept carve-outs; informs which format subcommand the gate uses)
    - DeckFlow.Core.Tests/DeckFlow.Core.Tests.csproj (confirm xUnit present; no new package)
    - CLAUDE.md (Testing: VSTest unreliable in WSL -> CI-targeted; xUnit for .NET Core projects; no new test deps)
  </read_first>
  <action>
    Create `DeckFlow.Core.Tests/CarveOutGuardTests.cs`, namespace `DeckFlow.Core.Tests`, `public sealed class CarveOutGuardTests` with four `[Fact]` methods (names per the behavior block).
    Shared helper `RunDotnetFormatOnSnippet(string source)`: create a REPO-LOCAL temp directory (e.g. under the test project's bin-adjacent path or a repo `artifacts/`-style dir — NOT `/tmp`, Pitfall 1), copy the repo `.editorconfig` into it (so the formatter applies the real carve-outs), write `source` to a `.cs` file there, shell out to the same `dotnet format` invocation the gate uses (`dotnet format whitespace --folder <dir>` is the higher-fidelity match to the gate per RESEARCH; full `dotnet format` is acceptable if the snippet is a compilable file), read the file back, return its content. Resolve the `dotnet` executable in a way that works on the CI runner (and locally on Windows from WSL if run there). Clean up the temp dir.
    Each test passes a fixture (init accessor / raw-string literal / own-line attribute / switch expression — see interfaces) and asserts `Assert.Equal(fixture, RunDotnetFormatOnSnippet(fixture))` (byte-for-byte). Normalize ONLY trailing-newline differences the formatter's `insert_final_newline` would add (account for it in the fixture so the assertion is exact, not lenient).
    Add an xUnit `[Trait("Category","CarveOutGuard")]` (or a class-level filterable trait) so CI can run `--filter CarveOutGuard`. Add an XML doc-comment on the class explaining this is a regression tripwire on `.editorconfig` edits, CI-targeted (VSTest unreliable in WSL), per FMT-02.
    Do NOT add any new NuGet package. Do NOT introduce a mocking library (none needed). Build only — VSTest run is deferred to CI per CLAUDE.md.
  </action>
  <verify>
    <automated>test -f DeckFlow.Core.Tests/CarveOutGuardTests.cs &amp;&amp; grep -q 'public sealed class CarveOutGuardTests' DeckFlow.Core.Tests/CarveOutGuardTests.cs &amp;&amp; grep -q 'init' DeckFlow.Core.Tests/CarveOutGuardTests.cs &amp;&amp; grep -qE 'RawString|"""' DeckFlow.Core.Tests/CarveOutGuardTests.cs &amp;&amp; grep -qiE 'Attribute' DeckFlow.Core.Tests/CarveOutGuardTests.cs &amp;&amp; grep -qiE 'switch' DeckFlow.Core.Tests/CarveOutGuardTests.cs &amp;&amp; grep -c '\[Fact\]' DeckFlow.Core.Tests/CarveOutGuardTests.cs | grep -qE '[4-9]' &amp;&amp; ! grep -q '/tmp' DeckFlow.Core.Tests/CarveOutGuardTests.cs &amp;&amp; echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `DeckFlow.Core.Tests/CarveOutGuardTests.cs` exists with `public sealed class CarveOutGuardTests` and four `[Fact]` methods covering init accessor, raw-string literal, own-line attribute, switch expression.  (FMT-02 / SPEC AC-2)
    - The helper writes fixtures and the report to a REPO-LOCAL temp path, never `/tmp` (asserted: no `/tmp` in file).  (RESEARCH Pitfall 1)
    - Each test asserts byte-identity (`Assert.Equal`) of fixture before/after formatting with the repo `.editorconfig`.  (SPEC AC-2)
    - A filterable trait (`CarveOutGuard`) is present so CI can `--filter CarveOutGuard`.
    - No new NuGet package added; xUnit-only (project convention .NET Core -> xUnit).
    - `dotnet build DeckFlow.Core.Tests/DeckFlow.Core.Tests.csproj -c Release` compiles clean (0 new errors/0 new warnings).  (CLAUDE.md Definition of Done)
  </acceptance_criteria>
  <done>The four-fixture carve-out guard test compiles clean and is wired for CI execution (filterable trait), asserting byte-identity against the reconciled .editorconfig.</done>
</task>

</tasks>

<verification>
- `dotnet build DeckFlow.Core.Tests/DeckFlow.Core.Tests.csproj -c Release` clean.
- Source greps confirm four facts + all four carve-out concepts + repo-local temp (no `/tmp`).
- Actual PASS/FAIL of the four tests is confirmed in CI (push-and-watch) per CLAUDE.md — recorded at the phase gate, not asserted locally.
</verification>

<success_criteria>
- Four code carve-outs proven byte-identical after formatting with the reconciled config.
- Test is a durable regression tripwire on future `.editorconfig` edits, CI-runnable.
- No new dependency, compiles clean, no `/tmp` reliance.
</success_criteria>

<output>
Create `.planning/phases/50-code-style-enforcement/50-03-SUMMARY.md` when done. Note that PASS/FAIL is CI-verified (VSTest WSL caveat) and which `dotnet format` subcommand the helper uses.
</output>
