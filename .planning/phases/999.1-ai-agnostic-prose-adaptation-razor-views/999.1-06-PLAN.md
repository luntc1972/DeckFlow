---
phase: 999.1-ai-agnostic-prose-adaptation-razor-views
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - DeckFlow.Web/Services/DeckAnalysisPacketService.cs
autonomous: true
requirements: [D-02]
must_haves:
  truths:
    - "Five _logger.LogInformation prefix strings in DeckAnalysisPacketService.cs (lines 210, 386, 409, 417, 458) read 'Deck Analysis packet ...' instead of 'ChatGPT packet ...' so log output matches the post-Phase-13 class naming."
    - "Doc-comment strings mentioning 'ChatGPT' in this file are NOT touched (Phase 13 D-07 preservation: lines 534 and 936 doc comments stay)."
    - "The Phase 10 fallback Key 'ChatGPT' passed as the second arg to NormalizeSingleLine on line 1309 is NOT touched (Phase 10 invariant)."
  artifacts:
    - path: "DeckFlow.Web/Services/DeckAnalysisPacketService.cs"
      provides: "Service with log prefix aligned to Phase 13 class rename"
      contains: "\"Deck Analysis packet"
  key_links:
    - from: "Serilog file sink (web-{date}.log) + console sink"
      to: "DeckAnalysisPacketService log statements"
      via: "_logger.LogInformation structured templates"
      pattern: "Deck Analysis packet"
---

<objective>
Rename the 5 `_logger.LogInformation` prefix strings in `DeckAnalysisPacketService.cs` from "ChatGPT packet ..." to "Deck Analysis packet ..." per D-02 #5. These log lines are NOT user-visible (Serilog console + file sinks only) but are included in this phase's scope as a cheap consistency follow-on so the log output matches the Phase 13 class name (`DeckAnalysisPacketService`, formerly `ChatGptDeckPacketService`).

Doc comments mentioning ChatGPT on lines 534 and 936 are PRESERVED per Phase 13 D-07. The Phase 10 default Key fallback on line 1309 (`NormalizeSingleLine(request.TargetAiPlatform, "ChatGPT")`) is PRESERVED per Phase 10 invariant.

Purpose: Align log output with the post-Phase-13 class naming so log lines and class names match (operator-friendly).
Output: Edited `DeckAnalysisPacketService.cs` with renamed log prefixes; build clean.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/999.1-ai-agnostic-prose-adaptation-razor-views/999.1-CONTEXT.md
@DeckFlow.Web/Services/DeckAnalysisPacketService.cs
@CLAUDE.md

<interfaces>
ChatGPT hits in DeckAnalysisPacketService.cs (grep ground truth 2026-05-18) — 8 total:

IN SCOPE (5 log lines):
- Line 210: `_logger.LogInformation("ChatGPT packet deck load completed in {ElapsedMs}ms.", ...);`
- Line 386: `_logger.LogInformation("ChatGPT packet banned-list + set-packet fetch completed in {ElapsedMs}ms.", ...);`
- Line 409: `"ChatGPT packet card reference lookup completed in {ElapsedMs}ms for {CardCount} cards and {MechanicCount} mechanics.",`
- Line 417: `"ChatGPT packet mechanic lookup completed in {ElapsedMs}ms for {MechanicCount} mechanics.",`
- Line 458: `"ChatGPT packet build completed in {ElapsedMs}ms. AnalysisGenerated={AnalysisGenerated} SetPacketGenerated={SetPacketGenerated}.",`

OUT OF SCOPE (3 hits, Phase 13 D-07 + Phase 10 invariants — DO NOT TOUCH):
- Line 534: `/// Builds the short deck summary shown above the generated ChatGPT packets.` (doc comment — Phase 13 D-07 preserved narrative)
- Line 936: `/// Returns the deck-profile schema that ChatGPT should follow during analysis.` (doc comment — Phase 13 D-07 preserved narrative)
- Line 1309: `builder.AppendLine($"target_ai_platform: {NormalizeSingleLine(request.TargetAiPlatform, \"ChatGPT\")}");` (Phase 10 default Key fallback)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename ChatGPT log prefix to Deck Analysis in DeckAnalysisPacketService.cs (5 log lines)</name>
  <files>DeckFlow.Web/Services/DeckAnalysisPacketService.cs</files>
  <read_first>
    - .planning/phases/999.1-ai-agnostic-prose-adaptation-razor-views/999.1-CONTEXT.md (D-02 #5 logger scope; Phase 13 D-07 doc-comment preservation; Phase 10 default Key fallback preservation)
    - DeckFlow.Web/Services/DeckAnalysisPacketService.cs lines 200-220, 380-420, 450-470, 530-540, 930-940, 1300-1315 (the surrounding context of every ChatGPT hit, so the executor confirms each location before editing)
    - CLAUDE.md (preserve LF, touch only the lines that need touching, no Format Document)
  </read_first>
  <action>
    Substitute the literal token `"ChatGPT packet` with `"Deck Analysis packet` in exactly 5 places — the `_logger.LogInformation` template strings at lines 210, 386, 409, 417, 458. Each substitution preserves the rest of the structured template (placeholders, semicolons, trailing punctuation).

    - Line 210: `"ChatGPT packet deck load completed in {ElapsedMs}ms."` → `"Deck Analysis packet deck load completed in {ElapsedMs}ms."`
    - Line 386: `"ChatGPT packet banned-list + set-packet fetch completed in {ElapsedMs}ms."` → `"Deck Analysis packet banned-list + set-packet fetch completed in {ElapsedMs}ms."`
    - Line 409: `"ChatGPT packet card reference lookup completed in {ElapsedMs}ms for {CardCount} cards and {MechanicCount} mechanics."` → `"Deck Analysis packet card reference lookup completed in {ElapsedMs}ms for {CardCount} cards and {MechanicCount} mechanics."`
    - Line 417: `"ChatGPT packet mechanic lookup completed in {ElapsedMs}ms for {MechanicCount} mechanics."` → `"Deck Analysis packet mechanic lookup completed in {ElapsedMs}ms for {MechanicCount} mechanics."`
    - Line 458: `"ChatGPT packet build completed in {ElapsedMs}ms. AnalysisGenerated={AnalysisGenerated} SetPacketGenerated={SetPacketGenerated}."` → `"Deck Analysis packet build completed in {ElapsedMs}ms. AnalysisGenerated={AnalysisGenerated} SetPacketGenerated={SetPacketGenerated}."`

    Do NOT touch:
    - Line 534 doc comment "Builds the short deck summary shown above the generated ChatGPT packets." — Phase 13 D-07 preserved.
    - Line 936 doc comment "Returns the deck-profile schema that ChatGPT should follow during analysis." — Phase 13 D-07 preserved.
    - Line 1309 `NormalizeSingleLine(request.TargetAiPlatform, "ChatGPT")` — Phase 10 invariant preserved.
    - Any other line. Preserve LF line endings.
  </action>
  <verify>
    <automated>grep -c '"ChatGPT packet' DeckFlow.Web/Services/DeckAnalysisPacketService.cs</automated>
    <automated>grep -c '"Deck Analysis packet' DeckFlow.Web/Services/DeckAnalysisPacketService.cs</automated>
    <automated>grep -c 'NormalizeSingleLine(request.TargetAiPlatform, "ChatGPT")' DeckFlow.Web/Services/DeckAnalysisPacketService.cs</automated>
    <automated>grep -c 'generated ChatGPT packets' DeckFlow.Web/Services/DeckAnalysisPacketService.cs</automated>
    <automated>grep -c 'ChatGPT should follow during analysis' DeckFlow.Web/Services/DeckAnalysisPacketService.cs</automated>
    <automated>grep -c 'ChatGPT' DeckFlow.Web/Services/DeckAnalysisPacketService.cs</automated>
    <automated>dotnet build DeckFlow.Web/DeckFlow.Web.csproj -c Debug --nologo -v quiet</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '"ChatGPT packet' DeckFlow.Web/Services/DeckAnalysisPacketService.cs` returns `0`.
    - `grep -c '"Deck Analysis packet' DeckFlow.Web/Services/DeckAnalysisPacketService.cs` returns `5`.
    - `grep -c 'NormalizeSingleLine(request.TargetAiPlatform, "ChatGPT")' DeckFlow.Web/Services/DeckAnalysisPacketService.cs` returns `1` (Phase 10 invariant preserved).
    - `grep -c 'generated ChatGPT packets' DeckFlow.Web/Services/DeckAnalysisPacketService.cs` returns `1` (line 534 doc comment preserved per Phase 13 D-07).
    - `grep -c 'ChatGPT should follow during analysis' DeckFlow.Web/Services/DeckAnalysisPacketService.cs` returns `1` (line 936 doc comment preserved per Phase 13 D-07).
    - `grep -c 'ChatGPT' DeckFlow.Web/Services/DeckAnalysisPacketService.cs` returns `3` (2 preserved doc-comment hits + 1 preserved Phase 10 fallback Key).
    - `dotnet build DeckFlow.Web/DeckFlow.Web.csproj -c Debug` exits 0.
  </acceptance_criteria>
  <done>Log prefix matches Phase 13 class name; Phase 13 D-07 doc comments + Phase 10 fallback Key preserved; build clean.</done>
</task>

</tasks>

<verification>
- `grep -nE '"ChatGPT packet' DeckFlow.Web/Services/DeckAnalysisPacketService.cs` returns no hits.
- `grep -nE 'ChatGPT' DeckFlow.Web/Services/DeckAnalysisPacketService.cs` returns exactly the 3 expected preserved lines (534, 936, 1309).
- `dotnet build DeckFlow.Web/DeckFlow.Web.csproj -c Release` exits 0.
</verification>

<success_criteria>
- Log output reads `Deck Analysis packet ...` matching the Phase 13 class name.
- Doc comments and Phase 10 fallback Key preserved byte-identical.
- Build clean.
</success_criteria>

<output>
Create `.planning/phases/999.1-ai-agnostic-prose-adaptation-razor-views/999.1-06-SUMMARY.md` when done.
</output>
