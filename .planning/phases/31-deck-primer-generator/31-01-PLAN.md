---
phase: 31-deck-primer-generator
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/31-deck-primer-generator/31-SPIKE.md
autonomous: false
requirements: [PRM-01]
must_haves:
  truths:
    - "31-SPIKE.md records a Spellbook Instructions richness verdict of exactly one of: sufficient | needs-enrichment | fallback"
    - "31-SPIKE.md records the full-31-section synthetic cEDH primer byte count and a recommended Gemini DefensivePromptCharCap"
    - "31-SPIKE.md records the EdhTop16 bracket-5 top-commanders query verdict (OQ-2): a meta-wide top-commanders query is available | only per-commander query is available → degrade"
    - "The combo-ranking branch selected by the verdict (priority-ranked vs AI-ranked fallback) is named explicitly so the variant plan (31-04) can read it at execution time"
  artifacts:
    - path: ".planning/phases/31-deck-primer-generator/31-SPIKE.md"
      provides: "Recorded spike verdicts that gate execution of 31-03/31-04 (combo ranking branch + Gemini cap threshold + EdhTop16 matchup routing)"
      contains: "Verdict"
  key_links:
    - from: "31-SPIKE.md combo verdict"
      to: "31-04-PLAN.md combo-ranking branch selection"
      via: "execution-time branch select per D-1"
      pattern: "Verdict"
    - from: "31-SPIKE.md byte-size measurement"
      to: "GeminiPrimerPromptVariant DefensivePromptCharCap"
      via: "recommended cap threshold"
      pattern: "DefensivePromptCharCap"
---

<objective>
PRM-01 combo-data spike — the FIRST execution unit of Phase 31 (D-1). Record three verdicts in `31-SPIKE.md` that gate (not replan) downstream execution:
(a) Commander Spellbook `Instructions` richness — does the raw API response carry machine-readable ranking fields (piece count / assembly cost / immediacy), enabling PRM-08 priority ranking, or must we fall back to AI-ranked combos?
(b) representative cEDH primer byte-size — measure a full 31-section synthetic primer against paste caps and recommend the Gemini `DefensivePromptCharCap` threshold (D-4);
(c) EdhTop16 bracket-5 top-commanders query path (OQ-2) — does the GraphQL schema support a meta-wide top-commanders query (named archetypes for PRM-06 bracket 5), or must bracket-5 matchups degrade to generic "current cEDH meta" prose?

Purpose: D-1 mandates ALL plans be authored up front with BOTH combo-ranking branches specified; this spike's recorded verdict selects the branch at execution time with no replan cycle. 31-03 and 31-04 read `31-SPIKE.md` before building.

Output: `.planning/phases/31-deck-primer-generator/31-SPIKE.md` with the three verdicts and their downstream consequences.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-deck-primer-generator/31-CONTEXT.md
@.planning/phases/31-deck-primer-generator/31-RESEARCH.md
@.planning/phases/31-deck-primer-generator/31-PATTERNS.md

<interfaces>
<!-- Confirmed against live code 2026-06-08. Spike probes these read-only paths. -->

CommanderSpellbookService (DeckFlow.Web/Services/CommanderSpellbookService.cs):
  - SpellbookCombo record (lines 16-19): (IReadOnlyList<string> CardNames, IReadOnlyList<string> Results, string Instructions)
  - ExtractInstructions (lines 266-274): reads JSON "description" field, caps at 300 chars + "…"
  - Results from produces[].feature.name (lines 256-263); CardNames from uses[].card.name (lines 241-244)
  - FindCombosAsync returns null on any failure (lines 119-146) — the null contract D-2 depends on
  - MaxAlmostIncluded = 15 (line 58) — near-combo cap
  - SPIKE FOCUS: enumerate ALL top-level keys on each variant JSON object (JsonDocument.EnumerateObject) to detect prerequisite / steps / manaNeeded / speed / edhrecSaltScore fields beyond description/uses/produces

EdhTop16Client (DeckFlow.Web/Services/EdhTop16Client.cs):
  - interface IEdhTop16Client (line 10); SearchCommanderEntriesAsync (line 23/78) takes a REQUIRED commanderName — query($name:String!) at the GraphQL layer
  - SPIKE FOCUS (OQ-2): does the GraphQL schema expose a root query for top commanders WITHOUT a name filter (e.g. topCommanders(first:N) / commanders(first:N, sortBy:...))? curl the GraphQL introspection or a candidate query to confirm.

AiPlatform (DeckFlow.Web/Models/AiPlatform.cs):
  - Gemini.PasteWarningBytes = 32_768 (line 30) — UTF-8 BYTES, UI warning threshold
  - NOTE units (D-4): PasteWarningBytes is bytes; GeminiAnalysisPromptVariant.DefensivePromptCharCap is CHARS (string.Length). Spike must report BOTH the byte count and a char-based cap recommendation; do not conflate.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Probe Spellbook Instructions richness, EdhTop16 top-commanders query, and synthetic primer byte-size</name>
  <files>.planning/phases/31-deck-primer-generator/31-SPIKE.md</files>
  <read_first>
    - DeckFlow.Web/Services/CommanderSpellbookService.cs (FULL — request shape, ParseResponse, ExtractInstructions, the variant JSON parsing at lines 161-276, null contract 119-146)
    - DeckFlow.Web/Services/EdhTop16Client.cs (lines 23-144 — GraphQL query string, the $name:String! parameter, response mapping)
    - DeckFlow.Web/Models/AiPlatform.cs (lines 11-30 — PasteWarningBytes)
    - .planning/phases/31-deck-primer-generator/31-RESEARCH.md (Section 1 spike design, Section 4 matchup routing, Open Questions OQ-2)
  </read_first>
  <action>
    DISPOSABLE PROBE — do NOT ship production code, do NOT modify any service. Use throwaway harness paths only (curl + a scratch script or a temporary CLI probe verb that is reverted before commit). The ONLY committed artifact is `31-SPIKE.md`.

    (a) Spellbook Instructions richness — capture a live JSON response from `backend.commanderspellbook.com/find-my-combos` for a known cEDH commander pairing (e.g. Thrasios + Tymna). Enumerate every top-level key present on each variant/result object (use `JsonDocument.Parse(...).RootElement` + `EnumerateObject()`, or `jq 'keys'`). Specifically look for: `prerequisite`, `steps`, `manaNeeded`, `manaCost`, `speed`, `type`, `edhrecSaltScore`, `popularity`. Record which (if any) are present and whether they give a machine-readable piece-count / assembly-cost / immediacy signal. Also sample 3-5 actual `description` strings to judge whether sentence-split heuristics could derive a step count. Pick the verdict:
      - `sufficient` — API returns a structured step/piece count AND at least one cost or speed field → PRM-08 priority-rank branch usable directly.
      - `needs-enrichment` — only `description` text, but rich enough for a heuristic (sentence-count / "mana" mentions) parse → heuristic-rank branch.
      - `fallback` — instructions sparse / inconsistent → AI-ranked fallback branch (emit combos in API order + instruct the AI to rank by ease of assembly and speed).

    (b) Synthetic byte-size — build a representative full-31-section cEDH primer prompt string (all sections, bracket 5, a commander with ~15-20 combos and the near-combo block populated) and compute `Encoding.UTF8.GetByteCount(prompt)`. Also estimate a typical-use count (12-18 sections). Compare against Gemini `PasteWarningBytes = 32_768` (bytes) and the practical ChatGPT (~100k chars) / Claude (no hard limit) ceilings. Recommend a Gemini `DefensivePromptCharCap` value in CHARS (string.Length units, NOT bytes — D-4 / Pitfall 5) that keeps the typical-use primer comfortably under cap while allowing the full primer to trim gracefully. If you cannot assemble a faithful full prompt yet (variants not built), approximate from the section catalog draft + measured combo/category block sizes and clearly label the number as an estimate to be re-confirmed in 31-04.

    (c) EdhTop16 top-commanders query (OQ-2) — probe the EdhTop16 GraphQL endpoint (introspection query or a candidate `topCommanders(first:N)` / `commanders(first:N, sortBy:TOP)` query via curl) to determine whether a meta-wide top-commanders list (no `name` filter) is retrievable. Record the verdict:
      - `meta-query-available` — schema supports a top-commanders query → PRM-06 bracket-5 matchups inject named archetypes; record the exact query/field names for 31-03 to use.
      - `per-commander-only` — only `query($name:String!)` exists → bracket-5 matchups degrade to a generic "reason about the current top cEDH archetypes" prompt with NO injected names (still useful; documented in 31-03/31-04 as the chosen path).

    Write `31-SPIKE.md` with a header (date, commander used, raw-fixture provenance) and three clearly labeled `## Verdict` sections (Combo Ranking, Primer Byte-Size + Gemini Cap, EdhTop16 Matchup Routing). For each verdict state the consequence sentence the downstream plan acts on (e.g. "Combo verdict = fallback → 31-04 emits combos in API order and adds an AI-rank instruction; the priority-rank branch stays dormant"). Revert any throwaway probe code; confirm `git status` shows only `31-SPIKE.md` added.
  </action>
  <verify>
    <automated>test -f .planning/phases/31-deck-primer-generator/31-SPIKE.md && grep -ciE "sufficient|needs-enrichment|fallback" .planning/phases/31-deck-primer-generator/31-SPIKE.md | grep -qvE '^0$' && grep -qiE "DefensivePromptCharCap|cap" .planning/phases/31-deck-primer-generator/31-SPIKE.md && grep -qiE "meta-query-available|per-commander-only" .planning/phases/31-deck-primer-generator/31-SPIKE.md && echo SPIKE-OK</automated>
  </verify>
  <acceptance_criteria>
    - `31-SPIKE.md` exists and contains exactly one combo-ranking verdict token (sufficient / needs-enrichment / fallback)
    - `31-SPIKE.md` contains a numeric byte count for the full-31-section synthetic primer AND a recommended Gemini DefensivePromptCharCap in chars (with a note distinguishing bytes vs chars per Pitfall 5)
    - `31-SPIKE.md` contains exactly one EdhTop16 matchup verdict token (meta-query-available / per-commander-only); if meta-query-available, the exact GraphQL query/field is recorded
    - Each verdict has a one-sentence downstream-consequence statement naming which 31-03/31-04 branch it activates
    - `git status` shows only `31-SPIKE.md` added (no production code committed; all probe code reverted)
  </acceptance_criteria>
  <done>31-SPIKE.md records the three gating verdicts with downstream consequences; no production code shipped; 31-03 and 31-04 can select their branches at execution time without a replan.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>The disposable PRM-01 spike and its recorded verdicts in 31-SPIKE.md — combo-ranking branch, Gemini cap threshold, and EdhTop16 matchup routing.</what-built>
  <how-to-verify>
    1. Open `.planning/phases/31-deck-primer-generator/31-SPIKE.md`.
    2. Confirm the combo-ranking verdict (sufficient / needs-enrichment / fallback) matches your read of the captured Spellbook JSON and that you accept the resulting PRM-08 branch.
    3. Confirm the recommended Gemini `DefensivePromptCharCap` (chars) looks reasonable against the measured byte count and the 32,768-byte Gemini paste warning.
    4. Confirm the EdhTop16 matchup verdict (meta-query-available vs per-commander-only) and accept whether bracket-5 matchups will inject named archetypes or degrade to generic meta prose.
  </how-to-verify>
  <resume-signal>Type "approved" to lock the verdicts and unblock Wave 2 (31-03/31-04), or describe which verdict to re-probe.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| spike harness → external APIs (Spellbook, EdhTop16) | Read-only probe of upstream JSON/GraphQL; no production surface touched |
| (no user input) | Spike is operator-run only; no untrusted request path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-01 | Information Disclosure | Probe code accidentally committed to the public repo | mitigate | Only `31-SPIKE.md` is committed; all throwaway harness/CLI verbs reverted; acceptance criteria asserts clean `git status` |
| T-31-02 | Tampering | A captured live fixture treated as authoritative beyond spike window | accept | RESEARCH §Valid-until = 2026-07-08; verdicts re-checkable; no fixture checked into production code |
| T-31-SC | Tampering | npm/pip/cargo installs | accept | Zero package installs in this plan (RESEARCH §Package Legitimacy Audit: not applicable — no new packages in phase) |
</threat_model>

<verification>
- `31-SPIKE.md` exists with all three labeled verdicts and downstream consequences
- `git status` shows no production code changes (probe reverted)
- Human checkpoint approved the verdicts
</verification>

<success_criteria>
PRM-01 is satisfied: the combo-data spike decision (Instructions richness + cEDH primer byte-size + EdhTop16 matchup routing) is recorded in `31-SPIKE.md` before `DeckPrimerPacketService` and the prompt variants are built. The recorded verdicts deterministically select the combo-ranking branch, the Gemini cap threshold, and the bracket-5 matchup source at execution time (D-1, no replan).
</success_criteria>

<output>
Create `.planning/phases/31-deck-primer-generator/31-01-SUMMARY.md` when done.
</output>
