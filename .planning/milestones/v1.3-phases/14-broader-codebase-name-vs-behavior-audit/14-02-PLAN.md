---
id: 14-02
phase: 14-broader-codebase-name-vs-behavior-audit
plan: 02
type: execute
wave: 2
depends_on:
  - 14-01
files_modified:
  # NOTE: exact file list depends on the audit report from Plan 14-01.
  # The expected set, from RESEARCH.md "Test-Double Census" + the planner-pre-decided ScryfallTaggerService rename:
  - DeckFlow.Web/Services/ScryfallTaggerService.cs
  - DeckFlow.Web/Services/ScryfallTaggerLookupService.cs  # post-rename target
  - DeckFlow.Web/Program.cs
  - DeckFlow.Web/Controllers/DeckController.cs            # if any DeckController call site references ScryfallTaggerService
  - DeckFlow.Web.Tests/AdminFeedbackControllerTests.cs
  - DeckFlow.Web.Tests/DeckControllerTests.cs
  - DeckFlow.Web.Tests/CommanderControllerTests.cs
  - DeckFlow.Core.Tests/ArchidektDeckCacheSessionTests.cs
  - README.md                                              # only if a renamed name is referenced
  - .planning/codebase/CONVENTIONS.md                      # only if Plan 14-01 audit surfaced a new prefix worth codifying (not expected per RESEARCH.md)
autonomous: true
requirements:
  - AUDIT-01
must_haves:
  truths:
    - "Every rename listed in 14-AUDIT-REPORT.md `### Production code` and `### Test doubles (D-05 canonicalization)` has been executed as a single logical commit (9 test-double renames + 1 production rename = 10 commits expected; the 9th test-double rename is the preliminary L810 FakeMetaGapService → StubMetaGapService per Plan 14-01 collision-resolution row a)"
    - "Every rename commit is preceded by `dotnet build DeckFlow.sln --no-restore --nologo --verbosity quiet` exiting 0 with `0 Warning(s) 0 Error(s)` (D-08 mid-plan green invariant)"
    - "No `{ get; init; }` accessor in any touched .cs file was reduced to `{ get; }` (CLAUDE.md preservation + Pitfall 3)"
    - "All renamed types carry an XML `<summary>` on the new declaration (D-06 lockstep summary for any touched type, whether public or internal)"
    - "After all rename commits, `grep -rEn '<OldName>' --include='*.cs' --include='*.cshtml' --include='*.md' DeckFlow.Web/ DeckFlow.Core/ DeckFlow.CLI/ DeckFlow.Core.Tests/ DeckFlow.Web.Tests/ README.md` returns 0 hits per rename (modulo Plan 14-01 allowlist + D-10 preservation list)"
    - "TestServiceFactory.cs was NOT renamed (Plan 14-01 allowlist)"
    - "FakeCardLookupService at DeckControllerTests.cs:914 was NOT collided with by a SuccessfulCardLookupService rename (preserved Successful qualifier with Stub prefix)"
    - "D-02: Loose rename trigger applied — renames executed whenever a reader benefits from a more descriptive name (responsibility scope, side effects, integration partners), not only on actively misleading names; git-blame churn cost accepted"
  artifacts:
    - path: "DeckFlow.Web/Services/ScryfallTaggerLookupService.cs"
      provides: "Renamed Scryfall Tagger service + summary enumerating responsibilities"
      contains: "public sealed class ScryfallTaggerLookupService"
    - path: "DeckFlow.Web/Program.cs"
      provides: "DI registration updated for renamed Scryfall Tagger service"
      pattern: "AddSingleton<IScryfallTaggerLookupService, ScryfallTaggerLookupService>"
  key_links:
    - from: "DeckFlow.Web/Services/ScryfallTaggerLookupService.cs"
      to: "DeckFlow.Web/Program.cs DI registration"
      via: "interface name update"
      pattern: "IScryfallTaggerLookupService"
    - from: "DeckFlow.Web.Tests/DeckControllerTests.cs (renamed inner classes)"
      to: "consuming test methods"
      via: "in-file usages of the renamed nested test doubles"
      pattern: "StubMetaGapService|FakeMetaGapService|FakeDeckAnalysisPacketService|StubSuccessfulCardLookupService|StubSuccessfulSingleCardLookupService|StubSuccessfulMechanicLookupService"
---

<objective>
Execute every rename listed in 14-AUDIT-REPORT.md `### Production code` and
`### Test doubles (D-05 canonicalization)`. Each rename = one commit. Build stays
GREEN after every commit (D-08, non-negotiable — Phase 14 D-08 explicitly forbids
the Phase-13-Rule-1-deviation "fix-up commit" anti-pattern).

Per the planner-pre-decisions surfaced by RESEARCH.md and frozen by Plan 14-01:

Production code (1 rename):
- `ScryfallTaggerService` → `ScryfallTaggerLookupService` (Plan 14-01 / D-02 loose trigger; responsibility-enumeration deferred as future split — see deferred section in 14-AUDIT-REPORT.md)

Test doubles (9 renames — 8 per RESEARCH.md "Test-Double Census" + 1 preliminary collision-resolution rename per Plan 14-01 revision-1 audit report row a):
- `FakeMetaGapService` (L810, canned-response — Stub semantics per CONVENTIONS.md) → `StubMetaGapService`  **[PRELIMINARY — MUST run before the L831 rename below]**
- `NullTempDataProvider` → `StubTempDataProvider`
- `ConfigurableMetaGapService` (L831) → `FakeMetaGapService`  **[depends on the L810 preliminary rename above]**
- `CapturingDeckAnalysisPacketService` → `FakeDeckAnalysisPacketService`
- `SuccessfulCardLookupService` → `StubSuccessfulCardLookupService`     [collision-avoiding for FakeCardLookupService L914]
- `SuccessfulSingleCardLookupService` → `StubSuccessfulSingleCardLookupService`
- `SuccessfulMechanicLookupService` → `StubSuccessfulMechanicLookupService`
- `DummyCommanderSearchService` → `StubCommanderSearchService`
- `FailingRecentDecksImporter` → `ThrowingRecentDecksImporter`

Purpose: Bring the codebase's user-facing class names in line with the
responsibility they describe (D-02 loose trigger), and consolidate test-double
prefixes to the {Fake, Stub, Throwing} taxonomy from CONVENTIONS.md. No
behavior changes, no public API changes, no responsibility splits.

Output: 10 rename commits (1 production + 9 test-double — the 9th is the
preliminary L810 `FakeMetaGapService` → `StubMetaGapService` collision-resolution
rename per Plan 14-01 revision-1 audit report) plus, if applicable, a
`.planning/codebase/CONVENTIONS.md` update commit (only if Plan 14-01 audit
report surfaced a new prefix worth codifying — RESEARCH.md says not expected).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/14-broader-codebase-name-vs-behavior-audit/14-CONTEXT.md
@.planning/phases/14-broader-codebase-name-vs-behavior-audit/14-RESEARCH.md
@.planning/phases/14-broader-codebase-name-vs-behavior-audit/14-AUDIT-REPORT.md   # Plan 14-01 deliverable; canonical input
@.planning/phases/14-broader-codebase-name-vs-behavior-audit/14-BASELINE.md
@.planning/phases/13-chatgpt-class-rename-summary-doc-comments/13-VERIFICATION.md
@.planning/codebase/CONVENTIONS.md
@CLAUDE.md
@.editorconfig

<interfaces>
<!-- Canonical test-double taxonomy (CONVENTIONS.md): Fake* (stateful), Stub* (canned/no-op), Throwing* (exception injection). -->
<!-- D-08 mid-plan green: every commit ends with `dotnet build DeckFlow.sln --no-restore --nologo --verbosity quiet` exiting 0 with 0 Warning(s) 0 Error(s). -->
<!-- D-10 preservation list (NEVER touch): "ChatGPT"/"Claude"/"Gemini" AI keys, request.TargetAiPlatform, targetAiPlatform form field, "chatgpt" zip filename fallback, internal HTML/JS identifiers, Razor visible prose, 22 guild themes. -->

<!-- Style anchor for summary on renamed types: -->
<!--   DeckFlow.Web/Services/CardLookupService.cs:39-42 (class) -->
<!--   DeckFlow.Web/Services/ScryfallTaggerService.cs:59-72 (multi-line ctor summary for ScryfallTaggerLookupService) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename ScryfallTaggerService → ScryfallTaggerLookupService (production code)</name>
  <files>
    DeckFlow.Web/Services/ScryfallTaggerService.cs (deleted via git mv)
    DeckFlow.Web/Services/ScryfallTaggerLookupService.cs (created via git mv)
    DeckFlow.Web/Program.cs
    DeckFlow.Web.Tests/ScryfallTaggerServiceTests.cs (deleted via git mv if exists)
    DeckFlow.Web.Tests/ScryfallTaggerLookupServiceTests.cs (created via git mv if exists)
    .planning/codebase/INTEGRATIONS.md (if it cites the old name)
    .planning/codebase/STRUCTURE.md (if it cites the old name)
    README.md (if it cites the old name)
  </files>
  <read_first>
    - .planning/phases/14-broader-codebase-name-vs-behavior-audit/14-AUDIT-REPORT.md `### Production code` row for ScryfallTaggerService
    - .planning/phases/14-broader-codebase-name-vs-behavior-audit/14-AUDIT-REPORT.md `## Plan 14-02 reference-propagation checklist`
    - .planning/phases/14-broader-codebase-name-vs-behavior-audit/14-AUDIT-REPORT.md `## Plan 14-02 per-commit { get; init; } preservation gate`
    - .planning/phases/14-broader-codebase-name-vs-behavior-audit/14-RESEARCH.md sections "File-Rename Propagation Map (Plan 14-02 Checklist)" + "Mid-Plan Build Cadence (D-08)"
    - DeckFlow.Web/Services/ScryfallTaggerService.cs (full file — to understand interface, ctor, summary, and current responsibility set)
    - DeckFlow.Web/Services/CardLookupService.cs:13-42 (style anchor for the new summary)
    - DeckFlow.Web/Program.cs (search for `IScryfallTaggerService` and `ScryfallTaggerService` to find DI registration site)
    - CLAUDE.md (one logical change per commit; no Co-Authored-By trailer; preserve { get; init; }; preserve switch expressions; preserve Allman braces; preserve raw-string indentation; LF line endings; no formatter sweeps)
  </read_first>
  <action>
Execute the rename atomically — one commit covers file rename + all reference updates. Per 14-AUDIT-REPORT.md and RESEARCH.md "File-Rename Propagation Map":

0. **Capture plan-start SHA** before any edits (used by `<verification>` block below for SHA-scoped `git log` queries — replaces unset `PLAN_START_TIMESTAMP` env var):

       PLAN_START_SHA=$(git rev-parse HEAD)
       echo "Plan 14-02 baseline SHA: $PLAN_START_SHA"

   Persist by exporting in the same shell session for all subsequent commits in Task 1 + Task 2; if shell state resets between tasks, re-derive via `git log --grep='^docs(14-01):' --format=%H -1` (the Plan 14-01 commit is the immediate predecessor).

0b. **New-name collision pre-check** — ensure none of the proposed NEW names already exist as a class declaration outside the source file we're renaming. This guards against the 14-01-AUDIT-REPORT.md rename table containing a hidden collision the audit missed (the checker revision 1 surfaced one such collision for `FakeMetaGapService` — pre-check ensures no further collisions remain):

       grep -rEn 'class\s+(StubTempDataProvider|StubMetaGapService|FakeMetaGapService|FakeDeckAnalysisPacketService|StubSuccessfulCardLookupService|StubSuccessfulSingleCardLookupService|StubSuccessfulMechanicLookupService|StubCommanderSearchService|ThrowingRecentDecksImporter|ScryfallTaggerLookupService|IScryfallTaggerLookupService)\b' --include='*.cs' DeckFlow.Web.Tests/ DeckFlow.Core.Tests/ DeckFlow.Web/ DeckFlow.Core/ DeckFlow.CLI/

   Expected: 0 hits (no proposed new name already exists as a `class` declaration anywhere). If ANY hit returns an existing `class` declaration outside the renamed source file's body, ABORT and require Plan 14-01 audit-report revision (record the collision, then return to /gsd:plan-phase --reviews to resolve).

   Note: `FakeMetaGapService` will be a hit at `DeckFlow.Web.Tests/DeckControllerTests.cs:810` PRE-Task-2 step (a). That hit is EXPECTED and resolved by the preliminary L810 → `StubMetaGapService` rename being row (a) of Task 2's 9-rename worklist (per Plan 14-01 audit report). After row (a) commits, `FakeMetaGapService` no longer matches at L810. Task 2 verifies this ordering at step (a) before proceeding to row (c).

1. **Search-before-edit** to enumerate all reference sites:

       grep -rEn "IScryfallTaggerService\b|ScryfallTaggerService\b" --include='*.cs' --include='*.cshtml' --include='*.md' DeckFlow.Web/ DeckFlow.Core/ DeckFlow.CLI/ DeckFlow.Core.Tests/ DeckFlow.Web.Tests/ README.md .planning/codebase/

   Build a mental map of every reference site. Verify the new name `IScryfallTaggerLookupService` / `ScryfallTaggerLookupService` doesn't already exist in the codebase (collision check):

       grep -rEn "IScryfallTaggerLookupService\b|ScryfallTaggerLookupService\b" --include='*.cs' DeckFlow.Web/ DeckFlow.Core/ DeckFlow.CLI/ DeckFlow.Core.Tests/ DeckFlow.Web.Tests/

   Expected: 0 hits.

2. **Rename the source file** with `git mv` to preserve blame:

       git mv DeckFlow.Web/Services/ScryfallTaggerService.cs DeckFlow.Web/Services/ScryfallTaggerLookupService.cs

3. **Edit the renamed file in-place** — touch ONLY the lines that need touching per CLAUDE.md "When editing a file, touch only the lines that need touching":
   - Rename the interface: `public interface IScryfallTaggerService` → `public interface IScryfallTaggerLookupService`
   - Rename the class: `public sealed class ScryfallTaggerService : IScryfallTaggerService` → `public sealed class ScryfallTaggerLookupService : IScryfallTaggerLookupService`
   - Rename the ctor and any internal-test-seam ctor: `ScryfallTaggerService(...)` → `ScryfallTaggerLookupService(...)`
   - Rename `ILogger<ScryfallTaggerService>` → `ILogger<ScryfallTaggerLookupService>` if present
   - Update the class-level `<summary>` to enumerate the three responsibilities accurately (per RESEARCH.md "ScryfallTaggerService" verdict + 14-AUDIT-REPORT.md row). Use the style anchor at CardLookupService.cs:39-42; multi-sentence acceptable here per RESEARCH.md "Public ctor summary anchor" precedent. Example tone (verify against current method bodies):

         /// <summary>
         /// Resolves a card to its Scryfall set+number, then looks up Tagger oracle tags
         /// via Scryfall Tagger's GraphQL endpoint. Manages the 270s CSRF session cache
         /// (HIGH-2) and respects the FLAG-04 kill-switch via in-process feature-flag cache.
         /// </summary>

   - Update any internal `<see cref="ScryfallTaggerService"/>` cross-references in the doc comments to point to the new name.

   Do NOT touch any other line. Do NOT trigger a Format Document save. Do NOT alter brace style, switch expressions, raw-string literals, or { get; init; } accessors. Do NOT inline attributes.

4. **Update DI registration** in `DeckFlow.Web/Program.cs`:
   - Find the line registering `IScryfallTaggerService` / `ScryfallTaggerService` — `AddSingleton<IScryfallTaggerService, ScryfallTaggerService>()` at `DeckFlow.Web/Program.cs:262` per live grep.
   - Replace both type arguments with the new names. Touch only that one line.
   - Verify the line still ends with `;` and that surrounding lines are unmodified.

5. **Update any test file references**:
   - If `DeckFlow.Web.Tests/ScryfallTaggerServiceTests.cs` exists, `git mv` it to `ScryfallTaggerLookupServiceTests.cs` and update class declaration + any inline references.
   - Inside both renamed source and test file, update XML doc `<see cref="..."/>` and any `[InternalsVisibleTo]` consumer comments.

6. **Update codebase docs and README** only if they cite the old name:

       grep -l "ScryfallTaggerService" .planning/codebase/*.md README.md

   For each hit, replace `ScryfallTaggerService` → `ScryfallTaggerLookupService`. Touch only the line(s) that mention the type.

7. **Run the { get; init; } preservation grep** per the gate from 14-AUDIT-REPORT.md `## Plan 14-02 per-commit { get; init; } preservation gate`:

       git diff -- '*.cs' | grep -E "^\-.*\{ get; init; \}" | grep -v "^--"
       # If any output: ABORT. The edit stripped init. Restore the line via `git checkout -- <file>` and redo by hand.

   Expected: empty.

8. **Build green** per D-08 mid-plan invariant + RESEARCH.md "Mid-Plan Build Cadence":

       "/mnt/c/Program Files/dotnet/dotnet.exe" build DeckFlow.sln --no-restore --nologo --verbosity quiet | tail -3

   Expected last line: `Build succeeded.` with `0 Warning(s) 0 Error(s)`. If WARN > 0 or ERROR > 0: the rename missed a reference. Fix the missed reference in the SAME uncommitted edit. Do not commit a red state (D-08 invariant; this is the Pitfall-6 anti-pattern from RESEARCH.md to avoid).

9. **Stale-reference grep** (RESEARCH.md "Smoke verification at end of every rename commit" #2):

       grep -rEn "IScryfallTaggerService\b|ScryfallTaggerService\b" --include='*.cs' --include='*.cshtml' --include='*.md' DeckFlow.Web/ DeckFlow.Core/ DeckFlow.CLI/ DeckFlow.Core.Tests/ DeckFlow.Web.Tests/ README.md .planning/codebase/

   Expected: 0 hits, or only allowlisted preservation literals (none in this rename — old name is a pure C# identifier).

10. **Commit** with plain author, no Co-Authored-By, one logical change:

        git add -A
        git commit -m "refactor(14-02): rename ScryfallTaggerService → ScryfallTaggerLookupService with three-responsibility summary"

    Verify the commit body has zero `Co-Authored-By`:

        git log -1 --format='%b' | grep -c 'Co-Authored-By'
        # Expected: 0
  </action>
  <verify>
    <automated>test -f DeckFlow.Web/Services/ScryfallTaggerLookupService.cs && ! test -f DeckFlow.Web/Services/ScryfallTaggerService.cs && grep -q "public sealed class ScryfallTaggerLookupService" DeckFlow.Web/Services/ScryfallTaggerLookupService.cs && grep -q "IScryfallTaggerLookupService" DeckFlow.Web/Program.cs && ! grep -rEn 'IScryfallTaggerService\b|class ScryfallTaggerService\b' --include='*.cs' DeckFlow.Web/ DeckFlow.Web.Tests/ DeckFlow.Core/ DeckFlow.Core.Tests/ DeckFlow.CLI/ && "/mnt/c/Program Files/dotnet/dotnet.exe" build DeckFlow.sln --no-restore --nologo --verbosity quiet 2>&1 | tail -3 | grep -qE 'Build succeeded\.\s*\n?\s*0 Warning' && git log -1 --format='%s' | grep -q "^refactor(14-02):" && [ "$(git log -1 --format='%b' | grep -c 'Co-Authored-By')" = "0" ]</automated>
  </verify>
  <acceptance_criteria>
    - File renamed via `git mv`: ScryfallTaggerService.cs → ScryfallTaggerLookupService.cs (blame survives `git log --follow`)
    - Interface, class, ctor, ILogger<T> type parameter, and class-level summary all updated in the renamed file
    - Class-level summary enumerates all three current responsibilities (Scryfall REST resolution, Tagger GraphQL, session/flag management) — accurate to current method bodies
    - DI registration in Program.cs updated (one line)
    - All test-file + README + codebase-doc references updated
    - { get; init; } preservation grep returns empty (no accessor stripped)
    - `dotnet build DeckFlow.sln --no-restore` exits 0 with `0 Warning(s) 0 Error(s)`
    - Stale-reference grep returns 0 hits for old name as C# identifier
    - Single commit `refactor(14-02): rename ScryfallTaggerService → ScryfallTaggerLookupService...`
    - Commit author plain, no Co-Authored-By trailer
    - LF line endings preserved on every touched file
  </acceptance_criteria>
  <done>Rename committed and build green. Plan 14-02 Task 2 (test-double consolidation) can proceed.</done>
</task>

<task type="auto">
  <name>Task 2: Consolidate 9 test-double prefixes to {Fake, Stub, Throwing} taxonomy — 9 commits, one per rename (8 non-canonical hits + 1 preliminary L810 collision-resolution rename per Plan 14-01 revision 1)</name>
  <files>
    DeckFlow.Web.Tests/AdminFeedbackControllerTests.cs
    DeckFlow.Web.Tests/DeckControllerTests.cs
    DeckFlow.Web.Tests/CommanderControllerTests.cs
    DeckFlow.Core.Tests/ArchidektDeckCacheSessionTests.cs
  </files>
  <read_first>
    - .planning/phases/14-broader-codebase-name-vs-behavior-audit/14-AUDIT-REPORT.md `### Test doubles (D-05 canonicalization)` (canonical old→new mapping for all 8)
    - .planning/phases/14-broader-codebase-name-vs-behavior-audit/14-AUDIT-REPORT.md `## Plan 14-02 reference-propagation checklist`
    - .planning/phases/14-broader-codebase-name-vs-behavior-audit/14-AUDIT-REPORT.md `### Name-collision notes` (FakeCardLookupService preservation in DeckControllerTests.cs:914)
    - .planning/phases/14-broader-codebase-name-vs-behavior-audit/14-RESEARCH.md sections "Test-Double Census" + "Naming-collision risk in DeckControllerTests.cs" + "Pitfall 6 Phase 13 fix-up commit anti-pattern" + "Pitfall 7 TestServiceFactory false-positive rename"
    - .planning/codebase/CONVENTIONS.md (Fake/Stub/Throwing definitions)
    - DeckFlow.Web.Tests/DeckControllerTests.cs full file (most renames here; verify collision allowlist around L914)
    - DeckFlow.Web.Tests/AdminFeedbackControllerTests.cs (around L144)
    - DeckFlow.Web.Tests/CommanderControllerTests.cs (around L117)
    - DeckFlow.Core.Tests/ArchidektDeckCacheSessionTests.cs (around L116)
    - DeckFlow.Web/Services/CardLookupService.cs:13-42 (summary style anchor for the new test-double class summaries)
    - CLAUDE.md
  </read_first>
  <action>
Execute one commit per rename. Each follows the same template:

1. Search-before-edit: `grep -rEn '\b<OldClassName>\b' --include='*.cs' DeckFlow.Web.Tests/ DeckFlow.Core.Tests/` — confirm every use site is within the SAME test file (nested private sealed class declarations + their consuming test methods). If a use site appears in a DIFFERENT file, expand the edit set.
2. Edit ONE test file: rename the nested private sealed class declaration AND every use site within that file in one pass.
3. Add or update the `/// <summary>` on the renamed class. Tone per CardLookupService anchor: terse, present-tense, single-sentence describing the test-double's role. For Fake* doubles, note the state-capture/configurable semantics in the summary (since the prefix no longer telegraphs them). Example for `FakeDeckAnalysisPacketService` (the renamed `CapturingDeckAnalysisPacketService`):

       /// <summary>
       /// Test double that captures the last DeckAnalysisRequest passed to BuildAsync
       /// so the consuming test can assert call arguments.
       /// </summary>
       private sealed class FakeDeckAnalysisPacketService : IDeckAnalysisPacketService

4. Run `{ get; init; }` preservation grep — empty expected.
5. Run `dotnet build DeckFlow.sln --no-restore --nologo --verbosity quiet | tail -3` — expect `Build succeeded. 0 Warning(s) 0 Error(s)`.
6. Stale-reference grep: `grep -rEn '\b<OldClassName>\b' --include='*.cs' DeckFlow.Web.Tests/ DeckFlow.Core.Tests/` — expect 0 hits.
7. Commit with plain author, no Co-Authored-By:

       git add <single-file-touched>
       git commit -m "refactor(14-02): rename <OldClassName> → <NewClassName> (D-05 <Fake|Stub|Throwing> canonicalization)"

**Capture Task 2 plan-start SHA** in the same shell session (or re-derive as in Task 1 step 0):

    PLAN_START_SHA=${PLAN_START_SHA:-$(git rev-parse HEAD)}

**New-name collision pre-check** — re-run before the rename loop to catch any collisions introduced between Task 1 and Task 2:

    grep -rEn 'class\s+(StubTempDataProvider|StubMetaGapService|FakeMetaGapService|FakeDeckAnalysisPacketService|StubSuccessfulCardLookupService|StubSuccessfulSingleCardLookupService|StubSuccessfulMechanicLookupService|StubCommanderSearchService|ThrowingRecentDecksImporter)\b' --include='*.cs' DeckFlow.Web.Tests/ DeckFlow.Core.Tests/

Expected: ONE hit — `DeckFlow.Web.Tests/DeckControllerTests.cs:810 class FakeMetaGapService`. This is the pre-rename target for row (a). Any OTHER hit = ABORT (audit-report missed a collision; return to /gsd:plan-phase --reviews).

Apply this loop in order, ONE rename per commit, to ALL **9** doubles per 14-AUDIT-REPORT.md (8 non-canonical hits + 1 preliminary collision-resolution rename, Plan 14-01 revision 1):

a. **PRELIMINARY (collision-resolution; MUST be first):** `DeckFlow.Web.Tests/DeckControllerTests.cs:~810` — `FakeMetaGapService` (existing, canned-response Stub semantics — body at L810-828 returns hardcoded MetaGapResult regardless of input) → `StubMetaGapService`. After this commit, the name `FakeMetaGapService` is FREE for use by row (c). Re-verify collision-free state:

       grep -c 'class FakeMetaGapService' DeckFlow.Web.Tests/DeckControllerTests.cs
       # Expected: 0 (the old name is gone)
       grep -c 'class StubMetaGapService' DeckFlow.Web.Tests/DeckControllerTests.cs
       # Expected: 1 (the new name in place)

   Commit message: `refactor(14-02): rename FakeMetaGapService → StubMetaGapService (D-05 Stub canonicalization for canned-response semantics — collision-resolution preliminary per Plan 14-01 revision 1)`

b. `DeckFlow.Web.Tests/AdminFeedbackControllerTests.cs:~144` — `NullTempDataProvider` → `StubTempDataProvider`
c. **(depends on row (a)):** `DeckFlow.Web.Tests/DeckControllerTests.cs:~831` — `ConfigurableMetaGapService` → `FakeMetaGapService` (configurable = stateful fake; only safe to perform AFTER row (a) frees the name)
d. `DeckFlow.Web.Tests/DeckControllerTests.cs:~870` — `CapturingDeckAnalysisPacketService` → `FakeDeckAnalysisPacketService` (note capture semantics in summary)
e. `DeckFlow.Web.Tests/DeckControllerTests.cs:~939` — `SuccessfulCardLookupService` → `StubSuccessfulCardLookupService` (Stub prefix + retained "Successful" qualifier per AUDIT-REPORT collision allowlist; do NOT collapse into the pre-existing `FakeCardLookupService` at L914)
f. `DeckFlow.Web.Tests/DeckControllerTests.cs:~948` — `SuccessfulSingleCardLookupService` → `StubSuccessfulSingleCardLookupService`
g. `DeckFlow.Web.Tests/DeckControllerTests.cs:~987` — `SuccessfulMechanicLookupService` → `StubSuccessfulMechanicLookupService`
h. `DeckFlow.Web.Tests/CommanderControllerTests.cs:~117` — `DummyCommanderSearchService` → `StubCommanderSearchService` (verify body returns canned/no-op results — Stub semantics confirmed by reading the class body in Task 2 read_first list before edit)
i. `DeckFlow.Core.Tests/ArchidektDeckCacheSessionTests.cs:~116` — `FailingRecentDecksImporter` → `ThrowingRecentDecksImporter`

After each commit:
- Check `git log -1 --format='%b' | grep -c 'Co-Authored-By'` is 0.
- Check `git log --diff-filter=AD --name-only -1 -- DeckFlow.Web.Tests/ DeckFlow.Core.Tests/ | wc -l` matches the single file edited (this is the "one logical change per commit" assertion).

**Allowlist non-renames (do NOT rename, do NOT touch):**
- `DeckFlow.Web.Tests/TestDoubles/TestServiceFactory.cs` — legitimate factory pattern per RESEARCH.md Pitfall 7 + Plan 14-01 audit report allowlist.
- `DeckFlow.Web.Tests/DeckControllerTests.cs:914` `FakeCardLookupService` — pre-existing, semantically distinct from the renamed Stub*CardLookupService neighbor at L939.

**TaggerSessionGate consideration (skip):** if the audit report happens to surface additional production renames not in this plan's scope (e.g., from Smells 1-4), they are tracked under `### Production code` in 14-AUDIT-REPORT.md and executed in this same Task pattern. If 14-AUDIT-REPORT.md `### Production code` contains additional rows beyond ScryfallTaggerService, add a Task-1-style loop for each before moving to test-double renames. Phase 14-02 covers EVERY row in `## Renames`, in alphabetical order by old name within each subsection.
  </action>
  <verify>
    <automated>! grep -rEn '\bNullTempDataProvider\b|\bConfigurableMetaGapService\b|\bCapturingDeckAnalysisPacketService\b|\bDummyCommanderSearchService\b|\bFailingRecentDecksImporter\b' --include='*.cs' DeckFlow.Web.Tests/ DeckFlow.Core.Tests/ && grep -q 'StubTempDataProvider' DeckFlow.Web.Tests/AdminFeedbackControllerTests.cs && grep -q 'class StubMetaGapService' DeckFlow.Web.Tests/DeckControllerTests.cs && grep -q 'class FakeMetaGapService' DeckFlow.Web.Tests/DeckControllerTests.cs && [ "$(grep -c 'class FakeMetaGapService' DeckFlow.Web.Tests/DeckControllerTests.cs)" = "1" ] && grep -q 'FakeDeckAnalysisPacketService' DeckFlow.Web.Tests/DeckControllerTests.cs && grep -q 'StubSuccessfulCardLookupService' DeckFlow.Web.Tests/DeckControllerTests.cs && grep -q 'StubCommanderSearchService' DeckFlow.Web.Tests/CommanderControllerTests.cs && grep -q 'ThrowingRecentDecksImporter' DeckFlow.Core.Tests/ArchidektDeckCacheSessionTests.cs && grep -q 'class FakeCardLookupService' DeckFlow.Web.Tests/DeckControllerTests.cs && test -f DeckFlow.Web.Tests/TestDoubles/TestServiceFactory.cs && "/mnt/c/Program Files/dotnet/dotnet.exe" build DeckFlow.sln --no-restore --nologo --verbosity quiet 2>&1 | tail -3 | grep -qE '0 Warning\(s\)' && [ "$(git log -n 9 --format='%s' | grep -c '^refactor(14-02):')" -ge "9" ] && [ "$(git log -n 10 --format='%b' | grep -c 'Co-Authored-By')" = "0" ]</automated>
  </verify>
  <acceptance_criteria>
    - All 8 non-canonical test-double class names are gone from the test source tree (grep returns 0 hits for the OLD names; the new `StubMetaGapService` and the post-rename `FakeMetaGapService` at L831 each appear exactly once)
    - All 9 new canonical names are present in the expected files (including `StubMetaGapService` at the formerly-L810 site and `FakeMetaGapService` at the formerly-L831 site)
    - Row (a) preliminary rename committed BEFORE row (c) — verify via `git log --format='%s' --reverse | grep -E '(StubMetaGapService|FakeMetaGapService)'` showing StubMetaGapService commit earlier than the row-c rename
    - Pre-existing FakeCardLookupService at DeckControllerTests.cs:914 still exists (collision allowlist preserved)
    - TestServiceFactory.cs file still exists (legitimate factory allowlist preserved)
    - Each rename is its OWN commit (9 commits, sequential, all `refactor(14-02):` prefix)
    - No commit body contains `Co-Authored-By` trailer
    - After each commit, `dotnet build DeckFlow.sln --no-restore` exits 0 with `0 Warning(s) 0 Error(s)` — D-08 mid-plan invariant
    - Every renamed test-double class has a `<summary>` describing its semantics (capture/canned-return/throw) per CONVENTIONS.md taxonomy
    - `{ get; init; }` accessor preservation grep returns empty after every commit
    - LF line endings preserved
  </acceptance_criteria>
  <done>All test-double renames committed. Plan 14-02 complete. Plan 14-03 can begin doc-comment backfill.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none introduced) | Renames preserve public API surface byte-for-byte at the wire (no string literal, route, or DTO property names changed per D-10). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-02-RENAME | Tampering (wire format) | ScryfallTaggerService → ScryfallTaggerLookupService | accept | D-10 preservation list explicitly lists what cannot change. The Scryfall Tagger service's wire format (Scryfall REST URL, Tagger GraphQL endpoint, Polly pipeline names, route attributes, JSON property names) are NOT class identifiers — none change. DI registration only swaps the C# type argument; the singleton lifecycle and registered key are unchanged. |
| T-14-02-DI | Spoofing (DI substitution) | DeckFlow.Web/Program.cs | accept | Single line edit (the AddSingleton<IScryfallTaggerLookupService, ScryfallTaggerLookupService> registration at line 262). No new lifetime, no new key. Compile-time type check catches any inconsistency between the interface type and impl type. |
| T-14-02-TESTS | Information disclosure | DeckFlow.Web.Tests/* internal renames | accept | Test doubles are `private sealed` nested classes — they have no surface outside their host test class. Renaming them changes no observable behavior. |
| T-14-02-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan; renames only. |

Per RESEARCH.md "Security Domain" — Phase 14 introduces no new threats. The D-10 preservation list (ChatGPT/Claude/Gemini AI keys, request.TargetAiPlatform, targetAiPlatform form field, "chatgpt" zip filename fallback, internal HTML/JS identifiers, Razor visible prose, 22 guild themes, Co-Authored-By) is the assurance that wire format and public API contracts hold byte-identical.
</threat_model>

<verification>
Plan 14-02 phase-level verification:

1. **All renames executed** — grep all 8 old test-double names + the production rename across the source tree:

       grep -rEn '\b(NullTempDataProvider|ConfigurableMetaGapService|CapturingDeckAnalysisPacketService|SuccessfulCardLookupService|SuccessfulSingleCardLookupService|SuccessfulMechanicLookupService|DummyCommanderSearchService|FailingRecentDecksImporter|class ScryfallTaggerService|IScryfallTaggerService)\b' --include='*.cs' --include='*.cshtml' --include='*.md' DeckFlow.Web/ DeckFlow.Core/ DeckFlow.CLI/ DeckFlow.Core.Tests/ DeckFlow.Web.Tests/ README.md

   Expected: 0 hits. (Note SuccessfulCardLookupService grep uses `\b` boundary so the new `StubSuccessfulCardLookupService` is NOT a false match — the `\b` after `l` matches only against word boundary, not `l|S`. Verify on first run.)

2. **TestServiceFactory + FakeCardLookupService preserved** — allowlist check:

       test -f DeckFlow.Web.Tests/TestDoubles/TestServiceFactory.cs
       grep -q 'private sealed class FakeCardLookupService' DeckFlow.Web.Tests/DeckControllerTests.cs

3. **Build green at HEAD** (full release build, not just --no-restore):

       "/mnt/c/Program Files/dotnet/dotnet.exe" build DeckFlow.sln --configuration Release --no-incremental --nologo

   Expected tail: `Build succeeded. 0 Warning(s) 0 Error(s)`.

4. **Commit hygiene check** (SHA-based scoping — replaces unset `PLAN_START_TIMESTAMP` env var; use the `PLAN_START_SHA` captured in Task 1 step 0, or re-derive via `git log --grep='^docs(14-01):' --format=%H -1`):

       PLAN_START_SHA=${PLAN_START_SHA:-$(git log --grep='^docs(14-01):' --format=%H -1)}
       git log "${PLAN_START_SHA}..HEAD" --format='%s%n%b%n---' | grep -ic 'Co-Authored-By'
       # Expected: 0

       git log "${PLAN_START_SHA}..HEAD" --format='%s' | grep -c '^refactor(14-02):'
       # Expected: 10 (9 test-double renames + 1 production rename); higher only if additional production renames came out of the audit

5. **`{ get; init; }` post-plan preservation** — full-tree sanity:

       PLAN_START_SHA=${PLAN_START_SHA:-$(git log --grep='^docs(14-01):' --format=%H -1)}
       git log "${PLAN_START_SHA}..HEAD" -p -- '*.cs' | grep -E "^\-.*\{ get; init; \}" | grep -v "^---"
       # Expected: 0 hits across all Plan 14-02 commits
</verification>

<success_criteria>
- Every row in 14-AUDIT-REPORT.md `### Production code` and `### Test doubles (D-05 canonicalization)` is committed as its own `refactor(14-02): rename ...` commit (10 commits total: 1 production + 9 test-double, where the 9th double is the preliminary L810 collision-resolution rename per Plan 14-01 revision 1).
- D-08 mid-plan green invariant held across every commit (build green after every rename, never red between commits).
- D-10 preservation list intact byte-for-byte (no AI Key, route, DTO property, zip filename fallback, internal JS/CSS identifier, or Razor prose touched).
- TestServiceFactory.cs and the pre-existing FakeCardLookupService at DeckControllerTests.cs:914 still exist (Plan 14-01 allowlist honored).
- `{ get; init; }` accessors preserved across every commit (CLAUDE.md + Pitfall 3).
- All commits plain default-author, no Co-Authored-By trailer.
- AUDIT-01 satisfied for renames; AUDIT-02 advanced (renamed types carry summaries in lockstep per D-06); AUDIT-03 build-clean intermediate gate passed.
</success_criteria>

<output>
Create `.planning/phases/14-broader-codebase-name-vs-behavior-audit/14-02-SUMMARY.md` documenting:
- List of rename commits (one row per `git log --oneline` entry with `refactor(14-02):` prefix)
- Confirmation of D-08 mid-plan green invariant (paste the final `dotnet build Release` output)
- Confirmation of D-10 preservation list intact (paste a few representative grep results for "ChatGPT", `TargetAiPlatform`, etc., showing the live count unchanged from 14-BASELINE.md baseline counts)
- Confirmation that TestServiceFactory + the pre-existing FakeCardLookupService L914 were preserved
- Final stale-old-name grep result (0 hits expected)
- Pointer to Plan 14-03 for doc-comment backfill (next wave)
</output>
</content>
</invoke>