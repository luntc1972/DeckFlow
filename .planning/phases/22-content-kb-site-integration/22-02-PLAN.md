---
phase: 22-content-kb-site-integration
plan: 02
type: execute
wave: 2
depends_on: ["22-01"]
files_modified:
  - .gitignore
  - Dockerfile
  - DeckFlow.Web/DeckFlow.Web.csproj
  - DeckFlow.CLI/Program.cs
  - DeckFlow.CLI/CommandRunners.cs
  - content-kb/seed/index-seed.json
  - content-kb/README.md
  - .planning/phases/22-content-kb-site-integration/22-02-SUMMARY.md
autonomous: true
requirements: [KB-08]
must_haves:
  truths:
    - "Running the CLI content-index-export verb writes a tracked JSON seed of ALL local index rows containing ONLY index columns (no transcript/audio/spend, no is_visible)"
    - "The tracked content-kb/ publish dir (artifacts + seed) reaches the Render runtime image"
    - ".gitignore tracks content-kb/ but keeps artifacts/ (local distill output) ignored"
  artifacts:
    - path: "DeckFlow.CLI/CommandRunners.cs"
      provides: "RunContentIndexExportAsync — reads local content_site_index, writes index-seed.json"
      contains: "RunContentIndexExportAsync"
    - path: "content-kb/seed/index-seed.json"
      provides: "committed seed of the 10 UAT index rows"
    - path: "Dockerfile"
      provides: "content-kb/ present in the runtime image"
      contains: "content-kb"
  key_links:
    - from: "content-index-export CLI verb"
      to: "content-kb/seed/index-seed.json"
      via: "GetAllRowsAsync then System.Text.Json serialize"
      pattern: "index-seed"
    - from: "Dockerfile runtime stage"
      to: "/app/content-kb"
      via: "published content OR explicit COPY"
      pattern: "content-kb"
---

<objective>
Build the commit-then-deploy supply line: a new `content-index-export` CLI verb that serializes the local `content_site_index` to a tracked JSON seed (index columns only — SC1), un-gitignore a tracked `content-kb/` publish directory holding the artifact files + seed, and make that directory reach the Render runtime image.

Purpose: Phase 22 has NO upload endpoint (D-02) — artifacts and index rows reach Render exclusively via git + deploy. This plan creates the export tool and proves the deploy path actually delivers the files to the container.

Output: CLI verb + runner + populated `content-kb/` (10 UAT artifacts already exist under `artifacts/content-kb/`) + seed JSON + the authorized `.gitignore` and `Dockerfile` edits.

CRITICAL FINDING (verified, not assumed): the current Dockerfile runtime stage copies ONLY `/app/publish` (the `dotnet publish` output) — it does NOT copy the repo root. A bare `content-kb/` at repo root will NOT reach the runtime image. This plan MUST close that gap (Task 2) or all artifact reads return "unavailable" in prod (Pitfall 4).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-content-kb-site-integration/22-CONTEXT.md
@.planning/phases/22-content-kb-site-integration/22-RESEARCH.md

<read_first>
- Dockerfile (whole file, 60 lines) — build stage publishes to `/app/publish`; runtime stage `COPY --from=build /app/publish .` into `/app`. `WORKDIR /app`, so `ContentRootPath` = `/app` at runtime. There is NO `COPY . .` into the runtime image. This is the load-bearing fact for Task 2.
- .gitignore (whole file) — line 4 `artifacts/`, line 5 `content-kb/`. Remove line 5. `artifacts/` (line 4) already keeps the local distill output (including `artifacts/content-kb/`) ignored. Net effect per RESEARCH Pitfall 7: track `content-kb/` at repo root; keep `artifacts/` ignored.
- ANALOG (CLI verb registration): DeckFlow.CLI/Program.cs — the existing `distillCommand` / `content-source-add` verb registration (Command + Option of FileInfo + AddOption + rootCommand.AddCommand + SetHandler). Mirror it for `content-index-export` with `--db` and `--output` options.
- ANALOG (CLI runner): DeckFlow.CLI/CommandRunners.cs — `RunDistillAsync` for connection-open + store-call + stdout reporting shape. Use `CreateLocalContentKbConnection` (always-SQLite, D-14) to open the local DB; default `--db` is `artifacts/content-kb.db`.
- DeckFlow.Core/Content/IContentSiteIndexStore.cs (AFTER Plan 01) — `GetAllRowsAsync()` is the source of export rows.
- DeckFlow.Core/Knowledge/ContentArtifactSpec.cs — `ContentSiteIndexRow` shape. Export object shape: naturalKeyType / naturalKeyValue / source / title / videoUrl / artifactPath / publishedUtc / indexedUtc / archetypeTags / bracketTags / cardCategoryTags. Do NOT emit IsVisible.
- Existing artifacts on disk: artifacts/content-kb/{edhrecast,mtggoldfish,playing-with-power,the-command-zone,tolarian-community-college}/*.md (10 files from the Phase 21.2 UAT).
- DeckFlow.Web/DeckFlow.Web.csproj — existing `<Content>` item patterns (Help/**/*.md copied to output). If Task 2 chooses the publish-as-content route, mirror that item.
</read_first>
</context>

<tasks>

<task type="auto">
  <name>Task 1: content-index-export CLI verb + runner + committed seed</name>
  <files>DeckFlow.CLI/Program.cs, DeckFlow.CLI/CommandRunners.cs, content-kb/seed/index-seed.json</files>
  <action>
    Register a new `content-index-export` Command in DeckFlow.CLI/Program.cs mirroring the existing distill verb: description "Exports the local content_site_index to a tracked JSON seed file for commit-then-deploy."; `--db` (Option of FileInfo, default artifacts/content-kb.db); `--output` (Option of FileInfo, default content-kb/seed/index-seed.json); AddOption both; rootCommand.AddCommand; SetHandler routes to CommandRunners.RunContentIndexExportAsync(db, output).

    Add RunContentIndexExportAsync(FileInfo? db, FileInfo? output) to CommandRunners.cs: resolve db path (default artifacts/content-kb.db), open via CreateLocalContentKbConnection, construct ContentSiteIndexStore, call EnsureSchemaAsync then GetAllRowsAsync(). Project each row to an export DTO with ONLY index columns: naturalKeyType, naturalKeyValue, source, title, videoUrl, artifactPath, publishedUtc, indexedUtc, archetypeTags, bracketTags, cardCategoryTags. EXPLICITLY exclude is_visible (admin-only state — must never be seeded), transcripts, audio, spend (SC1). Serialize with System.Text.Json (indented, camelCase to match the RESEARCH seed shape). Ensure the output directory exists, write the file with LF newlines (.gitattributes), print "Exported N rows to {path}" to stdout.

    Then produce content-kb/seed/index-seed.json. Run the verb against the local DB if present; if no local content-kb.db exists in this environment, hand-author the JSON from the 10 artifacts' frontmatter so the seed is committed for Plan 03/04 to load. Each artifact maps to naturalKeyType `youtube_channel` + its video_id; artifactPath is `{source-slug}/{video_id}.md` relative to content-kb/.
  </action>
  <verify>
    <automated>"/mnt/c/Program Files/dotnet/dotnet.exe" build /mnt/c/users/chrislunt/source/personal/deckflow/DeckFlow.CLI/DeckFlow.CLI.csproj -c Debug 2>&1 | grep -E "error|Build succeeded"; test -f /mnt/c/users/chrislunt/source/personal/deckflow/content-kb/seed/index-seed.json && echo SEED_EXISTS</automated>
  </verify>
  <done>CLI build clean; content-index-export verb registered; seed JSON committed with all 10 rows, index columns only, no is_visible/transcript/audio/spend keys.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Authorized `.gitignore` + `Dockerfile` edits so the tracked `content-kb/` (artifacts + seed) reaches the Render runtime image, plus moved/copied the 10 UAT artifacts into the tracked `content-kb/` tree and a `content-kb/README.md` documenting the export → copy → commit flow.</what-built>
  <how-to-verify>
    Codex (executor) must FIRST decide and implement ONE delivery mechanism, then this checkpoint verifies it:

    Edits to apply (authorized for this phase only — both files are on the CLAUDE.md Do-Not-Modify list):
    1. .gitignore: remove the `content-kb/` line (line 5). Leave `artifacts/` ignored.
    2. Artifacts: copy artifacts/content-kb/{source-slug}/{video_id}.md into content-kb/{source-slug}/{video_id}.md (tracked), preserving the relative layout that matches each seed row's artifactPath. Add content-kb/README.md documenting: run `content-index-export`, copy artifacts/content-kb/* into content-kb/*, git commit, deploy.
    3. Delivery to runtime image — pick ONE and implement it:
       (a) Dockerfile route: add `COPY content-kb/ ./content-kb/` to the RUNTIME stage (after `COPY --from=build /app/publish .`), so /app/content-kb exists. The controller (Plan 03) resolves artifacts at `Path.Combine(ContentRootPath, "content-kb", ...)` where ContentRootPath=/app. NOTE: if this route is chosen, Plan 03's path-resolution base is ContentRootPath/content-kb (NOT ContentRootPath/../content-kb) — record this in the SUMMARY so Plan 03 uses the matching base.
       (b) Publish-as-content route: add a `<Content Include="..\content-kb\**\*">` item (link-preserving) to DeckFlow.Web.csproj so `dotnet publish` carries content-kb into /app/publish → /app/content-kb. Then NO Dockerfile change is needed.
    Whichever route is chosen, the resolved runtime artifact base directory MUST be recorded in 22-02-SUMMARY.md as `runtime_artifact_base:` so Plan 03 binds the controller's path resolution to the same base (removes RESEARCH assumption A1/A3 ambiguity about ContentRootPath = /app vs /app/DeckFlow.Web).

    Human verification steps:
    1. `git check-ignore content-kb/seed/index-seed.json` returns NOTHING (file is tracked).
    2. `git check-ignore artifacts/content-kb/edhrecast` still reports it ignored.
    3. `git status --short content-kb/` shows the 10 artifacts + seed staged/untracked-new.
    4. Confirm the SUMMARY records which delivery route + the exact `runtime_artifact_base` path.
    5. (Optional, recommended) `docker build` then `docker run ... ls /app/content-kb` shows the artifact tree — or confirm via the next deploy's startup logs (Plan 03 logs ContentRootPath + a content-kb existence check).
  </how-to-verify>
  <resume-signal>Type "approved" with the chosen delivery route + runtime_artifact_base, or describe issues.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local export → git repo | seed/artifact files committed to a PUBLIC repo |
| git repo → Render image | deploy must deliver content-kb/ into the container |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-05 | Information Disclosure | seed leaks transcript/audio/spend into public repo | mitigate | export DTO whitelists index columns only; SC1; manual review of seed JSON keys |
| T-22-06 | Information Disclosure | secrets in committed artifacts (public repo) | mitigate | artifacts are LLM distillations of public YouTube/podcast content; no keys/PII; reviewer scans the 10 files |
| T-22-07 | Tampering / Availability | content-kb/ absent from runtime image (Dockerfile copies publish only) | mitigate | explicit Dockerfile COPY or publish-as-content item; verified via runtime ls / startup existence check; runtime_artifact_base recorded for Plan 03 |
| T-22-SC | Tampering | npm/nuget installs | mitigate | none — Phase 22 adds zero packages (RESEARCH confirmed). If any install appears, STOP and flag. |
</threat_model>

<verification>
- DeckFlow.CLI builds clean.
- content-kb/seed/index-seed.json exists, tracked (`git check-ignore` returns nothing), contains exactly the index columns. Grep gate (no leaked sensitive keys): `grep -ciE "transcript|audio|spend|is_visible|isVisible" content-kb/seed/index-seed.json` → expect `0`.
- `.gitignore` no longer ignores content-kb/; still ignores artifacts/.
- The chosen runtime-delivery route is implemented and `runtime_artifact_base` is recorded in the SUMMARY.
</verification>

<success_criteria>
- content-index-export CLI verb produces an index-only tracked seed (KB-08; SC1).
- content-kb/ is tracked and reaches the Render runtime image (KB-08; SC3; Pitfall 4 closed).
- No transcript/audio/spend/is_visible in the seed (SC1).
</success_criteria>

<output>
Create `.planning/phases/22-content-kb-site-integration/22-02-SUMMARY.md` when done — MUST record the chosen delivery route and the `runtime_artifact_base` path for Plan 03.
</output>
