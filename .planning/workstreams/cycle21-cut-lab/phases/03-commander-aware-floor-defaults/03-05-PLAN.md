---
phase: 03-commander-aware-floor-defaults
plan: 05
type: execute
wave: 5
depends_on: ["03-04"]
files_modified:
  - DeckFlow.Web/Models/CutLabViewModel.cs
  - DeckFlow.Web/Views/Deck/CutLab.cshtml
  - DeckFlow.Web/wwwroot/css/site-common.css
  - DeckFlow.Web.Tests/CutLabViewModelTests.cs
  - DeckFlow.Web.Tests/CutLabPageServiceTests.cs
  - README.md
autonomous: false
requirements: [RFLR-08]

must_haves:
  truths:
    - "The role-floors table renders six columns in the order Role, In pool, Bracket, Commander, Floor, Source (D-11)."
    - "Lands, interaction-mass and protection show 'n/a' in the Commander column because they are structurally out of scope (D-12)."
    - "A GO role with no commander match shows an em-dash empty marker, distinct from 'n/a' (D-12)."
    - "The commander number is shown at every bracket, including when the bracket value wins the max (RFLR-08)."
    - "Reset-to-default restores the effective max, not the bracket-only value (D-04, discretion item)."
    - "Every new cell carries a data-label so the stacked mobile layout keeps working (D-11)."
    - "No guild theme CSS file is modified; layout CSS lives only in site-common.css."
  artifacts:
    - path: "DeckFlow.Web/Models/CutLabViewModel.cs"
      provides: "CutLabFloorRowView bracket/commander fields and the two empty-cell states"
      contains: "CommanderDisplay"
    - path: "DeckFlow.Web/Views/Deck/CutLab.cshtml"
      provides: "Six-column role-floors table"
      contains: "data-label=\"Commander\""
    - path: "DeckFlow.Web.Tests/CutLabViewModelTests.cs"
      provides: "Wave 0 gap 1 — BuildFloorRows coverage"
  key_links:
    - from: "DeckFlow.Web/Models/CutLabViewModel.cs"
      to: "DeckFlow.Web/Views/Deck/CutLab.cshtml"
      via: "CutLabFloorRowView.CommanderDisplay rendered into the Commander cell"
      pattern: "CommanderDisplay"
    - from: "DeckFlow.Web/Views/Deck/CutLab.cshtml"
      to: "DeckFlow.Web/wwwroot/ts/cut-lab.ts"
      via: "data-cut-lab-floor-default now carries the max"
      pattern: "data-cut-lab-floor-default"
---

<objective>
Put both numbers on screen: the role-floors table grows from four columns to six —
`Role | In pool | Bracket | Commander | Floor | Source` — with two distinct empty-cell states and the
Reset button restoring the effective max.

Purpose: RFLR-08's literal ask, as designed in D-11 and D-12. Both numbers stay visible because they answer
different questions — the bracket band is prescriptive product opinion, the commander p25 is descriptive of
what people build.
Output: the six-column table, the view-model fields behind it, and the first `CutLabViewModelTests` file.

**Scope fence — do NOT touch `CutLabUiPatchBuilder` or `CutLabUiPatchDto` in this plan.** The role-floors
table renders on full page load only; it is not on the AJAX patch path, and no floor-row DTO exists there
today. Adding one would be new scope with no reader. The genuine patch-builder work in this phase belongs
to plan 03-07's locked-overshoot advisory.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/cycle21-cut-lab/phases/03-commander-aware-floor-defaults/03-CONTEXT.md
@.planning/workstreams/cycle21-cut-lab/phases/03-commander-aware-floor-defaults/03-04-SUMMARY.md
@CLAUDE.md

<design_contract>
The table mock from CONTEXT.md `<specifics>`, to be matched literally in column order and empty-cell
treatment:

```
Role      In pool  Bracket  Commander  Floor  Source
Engines    5        6         9        [9]   Commander
Payoffs    3        6         —        [6]   Bracket
Ramp       9       12        14       [14]   Commander
Wincons    2        3         —        [3]   Bracket
```

`[n]` is the existing editable number input. `—` is the em-dash empty marker for a GO role with no
commander match. Not shown in the mock, and required by D-12: lands, interaction-mass and protection render
the literal text `n/a` instead, because the column is structurally never populated for them.

There is no separate UI-SPEC document. This mock plus D-11/D-12 is the design contract.
</design_contract>

<interfaces>
`CutLabResolvedFloor` (after plan 03-04): `Role`, `Floor`, `IsUserSet`, `DefaultValue` (the effective max),
`BracketValue`, `CommanderValue` (`int?`), `ResolvedBracket`, `BracketWasFallback`.

`RoleFloorBaseline.AdoptedRoleKeys` (`DeckFlow.Core.Research`): ramp, draw, interaction-targeted, engines,
payoffs, wincons.

`CutLabViewModel.BuildFloorRows` (`CutLabViewModel.cs:463-487`) today is `private static`, takes
`(IReadOnlyList<CutLabResolvedFloor> resolvedFloors, IReadOnlyDictionary<string,int> countsByRole, string playExperience)`,
and populates `RoleKey`, `DisplayLabel`, `InPoolCount`, `Floor`, `DefaultValue`, `IsUserSet`, `AtFloor`
(`inPoolCount <= floor + 1`), `SourceLabel`.

`CutLabFloorRowView` (`CutLabViewModel.cs:1050-1075`): `RoleKey`, `DisplayLabel`, `InPoolCount`, `Floor`,
`DefaultValue`, `IsUserSet`, `AtFloor`, `SourceLabel` — all `{ get; init; }`.

`CutLab.cshtml` role-floors table at lines 779-821: `<table class="conflicts-table" data-prompt-cedh-reference-table role="table">`,
a four-`<th>` head, and a `<tbody>` loop emitting `<tr data-cut-lab-floor-row data-cut-lab-floor-count
data-cut-lab-floor-default data-cut-lab-floor-user-set>` with four `<td data-label="...">` cells. The
`data-cut-lab-floor-default` attribute appears twice: on the row (line 793) and on the Reset button (line 816).

`cut-lab.ts`: the only reader of `data-cut-lab-floor-default` is the reset handler near line 3944, which
assigns the attribute's value into the floor input verbatim. It needs **no change** — the attribute already
means "the default to reset to"; only its value changes, because `DefaultValue` is now the max.

`site-common.css` lines 1164-1283: the mobile stacked layout scoped to `table[data-prompt-cedh-reference-table]`.
It is generic — every `td` that is not `Select`/`#`/`Player` becomes a full-width `label: value` row via
`content: attr(data-label)`. New cells with `data-label` stack automatically; no new mobile rule is required.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the bracket/commander fields and the two empty-cell states to the view model</name>
  <read_first>
    - `DeckFlow.Web/Models/CutLabViewModel.cs` — `BuildFloorRows` (463-487), the private `FallbackSource` and `DisplayLabelFor` helpers it calls, and `CutLabFloorRowView` (1050-1075).
    - `DeckFlow.Web/Services/CutLab/CutLabFloorDefaults.cs` — the `CutLabResolvedFloor` shape produced by plan 03-04.
    - `DeckFlow.Core/Research/RoleFloorBaseline.cs` — `AdoptedRoleKeys`, the list that decides `n/a` versus the em dash.
    - `.planning/workstreams/cycle21-cut-lab/phases/03-commander-aware-floor-defaults/03-CONTEXT.md` §D-11, D-12.
  </read_first>
  <files>DeckFlow.Web/Models/CutLabViewModel.cs</files>
  <action>
Add five properties to `CutLabFloorRowView`, all `{ get; init; }` with xmldoc, never `{ get; }`:
- `int BracketValue` — the bracket-and-plan derived number for the Bracket column.
- `int? CommanderValue` — the commander p25 floor when one was found, otherwise null.
- `bool SupportsCommanderFloor` — true only for the six roles that can ever carry a commander floor.
- `string CommanderDisplay` — prebuilt cell text, default `string.Empty`.
- `string SourceDetail` — the provenance sentence, default `string.Empty`.

Keep `SourceLabel` but change what it holds: the single word rendered in the Source column, per the D-11
mock. Update its xmldoc accordingly.

Change `BuildFloorRows` from `private static` to `internal static` so tests can call it directly — this is
the project's established test-seam idiom and `[assembly: InternalsVisibleTo("DeckFlow.Web.Tests")]` is
already present in `DeckFlow.Web/AssemblyInfo.cs`. Do not change its parameter list.

Inside `BuildFloorRows`, for each resolved floor:
- `bool supportsCommanderFloor = RoleFloorBaseline.AdoptedRoleKeys.Contains(floor.Role, StringComparer.OrdinalIgnoreCase);`
- `CommanderDisplay` is:
  - `"n/a"` when `supportsCommanderFloor` is false. Add a `// Why:` comment recording D-12's reason: a bare
    dash here would imply the tool looked and found nothing, when lands was deliberately pulled at the
    Phase 2 checkpoint and interaction-mass/protection were ruled out for insufficient breadth.
  - the em-dash character `—` when `supportsCommanderFloor` is true and `floor.CommanderValue` is null.
    Add a `// Why:` comment recording that per D-08 the shipped snapshot genuinely cannot distinguish
    "commander absent from the corpus" from "commander present, role did not clear the bar", so the UI must
    not claim to.
  - otherwise the invariant-culture integer string of `floor.CommanderValue`.
- `SourceLabel` is `"Commander"` when `floor.CommanderValue is int commander && commander > floor.BracketValue`,
  otherwise `"Bracket"`. Add a `// Why:` comment: the label names which number actually drove the effective
  default, so a tie reads as `Bracket` because the bracket band alone already produced that number.
- `SourceDetail` carries the sentence `SourceLabel` used to hold, unchanged in wording and still keyed off
  `floor.BracketWasFallback`: `Default: {floor.DefaultValue} — based on {FallbackSource(playExperience)}`
  when the bracket was a fallback, otherwise `Default for B{floor.ResolvedBracket}: {floor.DefaultValue}`.
  Because `DefaultValue` is now the effective max, this sentence automatically reports the number the Reset
  button restores.
- `BracketValue` and `CommanderValue` copy straight across from the resolved floor.
- `DefaultValue`, `Floor`, `IsUserSet` and `AtFloor` keep their existing assignments untouched.

Change nothing else in this file.

**Line endings:** this file already exists — detect its existing line endings and preserve them
byte-for-byte; change only the lines whose content actually changes. Do not re-indent, do not reflow, do not
inline any `[Attribute]` onto a property line, and preserve the xmldoc single-space indent.
  </action>
  <verify>
    <automated>"/mnt/c/Program Files/dotnet/dotnet.exe" build DeckFlow.sln -c Release</automated>
  </verify>
  <acceptance_criteria>
    - `CutLabFloorRowView` declares `BracketValue`, `CommanderValue`, `SupportsCommanderFloor`, `CommanderDisplay` and `SourceDetail`, all `{ get; init; }`.
    - `BuildFloorRows` is `internal static` and its parameter list is unchanged.
    - `grep -c 'n/a' DeckFlow.Web/Models/CutLabViewModel.cs` returns at least 1 and the literal appears only on the `SupportsCommanderFloor == false` branch.
    - The em-dash branch is reachable only when `SupportsCommanderFloor` is true and `CommanderValue` is null — verify by reading the branch, not by counting substrings.
    - `CutLabViewModel.cs` references `RoleFloorBaseline.AdoptedRoleKeys` and declares no second literal six-role list.
    - `git diff --stat` and `git diff --ignore-all-space --stat` report the same changed-line count for this file.
    - `dotnet build DeckFlow.sln -c Release` clean, no new warnings.
  </acceptance_criteria>
  <done>The view model carries both numbers, both empty-cell states, and a Source label that names the driving number.</done>
</task>

<task type="auto">
  <name>Task 2: Render six columns and scope the layout CSS</name>
  <read_first>
    - `DeckFlow.Web/Views/Deck/CutLab.cshtml` lines 770-825 — the whole role-floors table being modified.
    - `DeckFlow.Web/wwwroot/css/site-common.css` lines 1164-1283 — the existing mobile stacked-layout block, to confirm no new mobile rule is needed.
    - `DeckFlow.Web/wwwroot/ts/cut-lab.ts` around line 3944 — the reset handler, to confirm it needs no change.
    - `DeckFlow.Web/Models/CutLabViewModel.cs` — `CutLabFloorRowView` after Task 1.
  </read_first>
  <files>DeckFlow.Web/Views/Deck/CutLab.cshtml, DeckFlow.Web/wwwroot/css/site-common.css</files>
  <action>
In `CutLab.cshtml`:
- Add `data-cut-lab-role-floors-table` as an additional attribute on the existing role-floors `<table>`
  element. Keep `class="conflicts-table"`, `data-prompt-cedh-reference-table` and `role="table"` exactly as
  they are — the mobile stacking depends on `data-prompt-cedh-reference-table`, so it must not be removed
  or replaced. The new attribute exists solely to scope desktop CSS to this table.
- In `<thead>`, insert two `<th scope="col">` elements between the existing `In pool` and `Floor` headers,
  reading `Bracket` and `Commander`, producing the header order
  `Role`, `In pool`, `Bracket`, `Commander`, `Floor`, `Source`.
- In the `<tbody>` loop, insert two `<td>` elements in the same position:
  `<td data-label="Bracket">@row.BracketValue</td>` and
  `<td data-label="Commander">@row.CommanderDisplay</td>`.
  Both MUST carry `data-label` — every existing cell does, and the stacked mobile layout is driven entirely
  by `content: attr(data-label)`.
- Add `title="@row.SourceDetail"` to the existing `<span ... data-cut-lab-floor-source-default>` element and
  change its rendered content from `@row.SourceLabel` to `@row.SourceLabel` (unchanged expression — the
  value is now the single word). The detail sentence moves to the tooltip.
- Leave `data-cut-lab-floor-default="@row.DefaultValue"` unchanged in both places (row and Reset button).
  Its value is now the effective max because plan 03-04 redefined `DefaultValue`, which is exactly the
  discretion decision recorded in CONTEXT.md: Reset restores the max. Add a Razor comment at the row
  element saying so, so the next reader does not "fix" it back to the bracket value.
- Do not add, remove, or reorder any other element in this file.

In `site-common.css`, add ONE new rule block, scoped to `table[data-cut-lab-role-floors-table]`, applying
only to the two new numeric columns: right-align and give a narrow `width` to
`td[data-label="Bracket"]`, `td[data-label="Commander"]`, `th` counterparts as needed. Place it immediately
after the existing role-floors-related block so related rules stay together. Wrap it in the same
`@media (max-width: 600px)`-free desktop scope — i.e. put it OUTSIDE the existing mobile media query, so the
stacked layout is unaffected. Add a CSS comment stating that no mobile rule is needed because the stacked
layout is generic over `data-label`.

**Do not modify any `site-*.css` guild theme file and do not add any `:root` token.** The theme files only
set `.conflicts-table` colours and borders; they carry no column-count assumptions. If a theme visibly
breaks, stop and report rather than editing a theme file.

**Line endings:** both files already exist — detect and preserve each file's existing line endings
byte-for-byte, changing only the lines that change. Do not reformat surrounding markup or CSS.
  </action>
  <verify>
    <automated>"/mnt/c/Program Files/dotnet/dotnet.exe" build DeckFlow.sln -c Release</automated>
  </verify>
  <acceptance_criteria>
    - The role-floors `<thead>` has exactly six `<th>` elements in the order `Role`, `In pool`, `Bracket`, `Commander`, `Floor`, `Source`.
    - The `<tbody>` row emits exactly six `<td>` elements and every one has a `data-label` attribute.
    - `grep -c 'data-label="Commander"' DeckFlow.Web/Views/Deck/CutLab.cshtml` returns 1 and `grep -c 'data-label="Bracket"' ...` returns 1.
    - `data-cut-lab-floor-default="@row.DefaultValue"` still appears exactly twice and is otherwise unchanged.
    - `git diff --name-only -- 'DeckFlow.Web/wwwroot/css/site-*.css'` lists only `site-common.css`.
    - `git diff -- DeckFlow.Web/wwwroot/ts/` is empty — no TypeScript change was needed.
    - `git diff --stat` and `git diff --ignore-all-space --stat` agree for both modified files.
  </acceptance_criteria>
  <done>The table renders six labelled columns, the new CSS is scoped and lives only in site-common.css, and no TypeScript or theme file changed.</done>
</task>

<task type="auto">
  <name>Task 3: Add CutLabViewModelTests, update the moved SourceLabel assertions, and document the columns</name>
  <read_first>
    - `DeckFlow.Web/Models/CutLabViewModel.cs` — `BuildFloorRows` after Task 1, now `internal static`.
    - `DeckFlow.Web.Tests/CutLabPageServiceTests.cs` lines 2228-2246 — the two existing assertions that read `SourceLabel` as a full sentence, at **line 2236** (`Default for B4: 7`, on `targetedRow`) and **line 2245** (`Default for B4: 3`, on `massRow`).
    - `DeckFlow.Web.Tests/CutLabFloorDefaultsTests.cs` — the `CutLabResolvedFloor` construction style to reuse when building fixtures.
    - `README.md` — the Cut Lab section, to match its existing depth.
  </read_first>
  <files>DeckFlow.Web.Tests/CutLabViewModelTests.cs, DeckFlow.Web.Tests/CutLabPageServiceTests.cs, README.md</files>
  <action>
Create `DeckFlow.Web.Tests/CutLabViewModelTests.cs`, namespace `DeckFlow.Web.Tests`, xUnit,
`public sealed class CutLabViewModelTests`, LF line endings. This file is Wave 0 gap 1 from
`03-VALIDATION.md` — no `CutLabViewModelTests.cs` exists today. Call `CutLabViewModel.BuildFloorRows`
directly with hand-built `CutLabResolvedFloor` fixtures; do not stand up the whole page service. Add no
package or mocking library.

Tests:
1. `BuildFloorRows_OutOfScopeRole_ShowsNotApplicable` — `lands`, `interaction-mass` and `protection` each
   produce `CommanderDisplay == "n/a"` and `SupportsCommanderFloor == false`, even when `CommanderValue`
   is somehow non-null (defensive: the display must key off role scope, not off the value).
2. `BuildFloorRows_GoRoleWithNoCommanderMatch_ShowsEmptyMarker` — a GO role with `CommanderValue` null
   produces the em dash, and explicitly asserts it is NOT `"n/a"`. These two states must be provably distinct.
3. `BuildFloorRows_GoRoleWithCommanderMatch_ShowsTheNumber` — `CommanderValue` 9 renders `"9"`.
4. `BuildFloorRows_CommanderBelowBracket_StillShowsTheCommanderNumber` — `BracketValue` 6,
   `CommanderValue` 2 renders `"2"` in the Commander column while `Floor` stays 6.
5. `BuildFloorRows_SourceLabel_NamesTheDrivingNumber` — commander above bracket yields `"Commander"`;
   commander below bracket, commander equal to bracket, and no commander value all yield `"Bracket"`.
6. `BuildFloorRows_SourceDetail_ReportsTheEffectiveDefault` — with `BracketWasFallback` false and
   `ResolvedBracket` 4, `SourceDetail` equals `Default for B4: {DefaultValue}` using the effective max;
   with `BracketWasFallback` true it uses the fallback wording.
7. `BuildFloorRows_PreservesInPoolAndAtFloorBehavior` — `AtFloor` is still `inPoolCount <= Floor + 1`.
8. `BuildFloorRows_EmitsOneRowPerResolvedFloorInOrder` — the output role sequence equals the input sequence.

In `CutLabPageServiceTests.cs`, update the two assertions that read `SourceLabel` as a full sentence: they
now assert `SourceLabel` equals `"Bracket"` and `SourceDetail` equals the sentence they previously expected
(`Default for B4: 7` and `Default for B4: 3`). Do not change the numeric expectations. Change nothing else
in that file — plan 03-04 already updated everything else it needed.

Update `README.md`'s Cut Lab section to describe the six-column role-floors table: name the two new columns,
state that the commander floor comes from that commander's own 25th-percentile corpus data and can only
raise a floor, and state the two empty-cell meanings (`n/a` = the role is out of scope for commander data;
the em dash = no commander data for this commander and role). Keep it to the depth of the surrounding
entries — no tutorial.

**Line endings:** `CutLabPageServiceTests.cs` and `README.md` already exist — detect and preserve each
file's existing line endings byte-for-byte, changing only the lines that change. The new test file is LF.
  </action>
  <verify>
    <automated>"/mnt/c/Program Files/dotnet/dotnet.exe" test DeckFlow.sln -c Release --filter "FullyQualifiedName~CutLabViewModelTests|FullyQualifiedName~CutLabPageServiceTests"</automated>
  </verify>
  <acceptance_criteria>
    - `DeckFlow.Web.Tests/CutLabViewModelTests.cs` exists with all eight named tests, all passing.
    - Test 2 asserts the empty marker is not equal to `"n/a"`.
    - The two updated `CutLabPageServiceTests` assertions check `SourceLabel == "Bracket"` and the sentence on `SourceDetail`, with the numbers 7 and 3 unchanged.
    - The filtered test run reports zero failures.
    - `README.md` names both new columns and both empty-cell meanings.
    - No test `.csproj` changed (`git diff --quiet -- '*Tests.csproj'`).
  </acceptance_criteria>
  <done>Wave 0 gap 1 is closed, the two empty states are provably distinct, and the new columns are documented.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Verify the six-column table across viewports and themes</name>
  <read_first>
    - `.planning/workstreams/cycle21-cut-lab/phases/03-commander-aware-floor-defaults/03-05-PLAN.md` `<design_contract>` — the mock the rendered table must match.
    - `DeckFlow.Web/Views/Deck/CutLab.cshtml` — the table as modified by Task 2, so the reviewer knows which cells to look at.
    - `scripts/run-web-test.sh` — confirm it sets `DECKFLOW_DISABLE_AUTO_BROWSER=true` before starting anything.
    - `DeckFlow.Web/Data/role-floor-baseline/latest.json` — pick a commander key present in the snapshot to test with.
  </read_first>
  <action>
Stop and hand control to the developer. Tasks 1-3 are complete and the automated suite is green; what
remains cannot be asserted from a test run. Do not proceed to the plan summary, and do not start a
browser or a Playwright run, until the developer has answered the permission question in
<how-to-verify> step 1 and then returned an approval.
  </action>
  <what-built>
    The Cut Lab role-floors table now renders six columns — Role, In pool, Bracket, Commander, Floor, Source —
    with `n/a` for lands / interaction-mass / protection, an em dash for a GO role with no commander match, and
    the Reset button restoring the effective `max(bracket, commander)` value. Layout CSS was added only to
    `site-common.css`; no guild theme file and no TypeScript file was modified.
  </what-built>
  <how-to-verify>
    Automated checks cannot see rendering. Two things need eyes.

    1. **Ask before starting a browser or Playwright run** — this is a standing project rule.
    2. Start the app headless with `scripts/run-web-test.sh` (it sets `DECKFLOW_DISABLE_AUTO_BROWSER=true`;
       the server must never open a browser window on the Windows host). Before starting, check nothing is
       already bound to port 5173 from a stale Windows-side run.
    3. Drive it with `npx --no-install playwright test` headless, or a manually-opened browser against the
       headless server. Do not use the WSL `gstack` daemon.
    4. Load Cut Lab with a deck whose commander is present in
       `DeckFlow.Web/Data/role-floor-baseline/latest.json` (pick any key from that file), at bracket 4.
    5. Confirm on a desktop viewport: six columns in the mock's order; at least one role showing a commander
       number; lands / interaction-mass / protection showing `n/a`; at least one GO role showing the em dash;
       the Floor input equal to the larger of the Bracket and Commander numbers on every row.
    6. Confirm on a mobile viewport (<= 600px): the row stacks into label/value pairs and the new
       `Bracket` and `Commander` labels appear.
    7. Adjust one floor, then press `Reset to default`. The restored number must equal the Floor column's
       original value (the max), NOT the Bracket column's value.
    8. Repeat step 5 on one dark and one light guild theme.
  </how-to-verify>
  <resume-signal>Type "approved", or describe what rendered wrong (column, viewport, theme).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| view model -> Razor output | Server-rendered values written into HTML text nodes and attributes. All values are integers, a fixed role-key list, or strings this plan composes from those. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-18 | Tampering | Cross-site scripting via the new cells | mitigate | `CommanderDisplay`, `SourceLabel` and `SourceDetail` are composed only from integers, the `n/a` literal, the em dash, and `FallbackSource`'s fixed strings. Razor `@` expressions HTML-encode by default; do not introduce `Html.Raw` anywhere in this change. Commander *names* are not rendered into the new cells. |
| T-03-19 | Information Disclosure | Corpus data exposed in page source | accept | The commander floor is a small integer derived from public deck-composition aggregates already committed to a public repo. No new sensitive value reaches the page. |
| T-03-20 | Tampering | A client editing `data-cut-lab-floor-default` to force an arbitrary reset value | accept | Pre-existing surface, unchanged by this plan. Floors are user-editable advisory numbers, persisted through the existing `CutLabFloorRules.Clamp` path which already bounds untrusted persisted values. |
| T-03-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs; no `package.json` or `.csproj` change. |
</threat_model>

<verification>
- `"/mnt/c/Program Files/dotnet/dotnet.exe" build DeckFlow.sln -c Release` clean, no new warnings.
- `"/mnt/c/Program Files/dotnet/dotnet.exe" test DeckFlow.sln -c Release` full suite green.
- `git diff --name-only` lists no `site-*.css` file other than `site-common.css`, and no file under `DeckFlow.Web/wwwroot/ts/` or `DeckFlow.Web/wwwroot/js/`.
- `git diff --stat` and `git diff --ignore-all-space --stat` agree for every modified file.
- Human checkpoint approved for desktop, mobile, one dark theme, one light theme, and the Reset behavior.
</verification>

<success_criteria>
- Six columns in the mock's order, every cell carrying `data-label`.
- `n/a` and the em dash are two distinct, tested states.
- The commander number appears at every bracket, including when it loses the max.
- Reset restores the effective max.
- `CutLabViewModelTests.cs` exists and passes; no theme file or TypeScript file was touched.
</success_criteria>

<output>
Create `.planning/workstreams/cycle21-cut-lab/phases/03-commander-aware-floor-defaults/03-05-SUMMARY.md` when done.
</output>
