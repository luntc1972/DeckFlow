---
phase: 91-reconcile-seed-lifecycle
plan: 03
type: execute
wave: 2
depends_on: [91-01]
files_modified:
  - DeckFlow.Core/Content/SeedManagedBackfill.cs
  - DeckFlow.Web/Services/Content/WebSeedKeyMembershipSource.cs
  - DeckFlow.Web/Program.cs
  - DeckFlow.Studio/Services/StudioSeedKeyMembershipSource.cs
  - DeckFlow.Studio/Program.cs
  - DeckFlow.Core.Tests/Content/SeedManagedBackfillTests.cs
autonomous: true
requirements: [SYNC-17]

must_haves:
  truths:
    - "On startup, legacy rows with seed_managed IS NULL are classified true if their natural key is in the current seed, false otherwise"
    - "Re-running the backfill writes zero rows (already-classified rows are untouched)"
    - "The backfill never crashes host startup on a read/parse failure"
    - "Backfill runs on both the web (prod-pointed) and Studio (local) hosts"
  artifacts:
    - path: "DeckFlow.Core/Content/SeedManagedBackfill.cs"
      provides: "Host-agnostic D-02 backfill (mirrors ContentBodyHashBackfill)"
      contains: "SeedManagedBackfill"
      min_lines: 40
  key_links:
    - from: "SeedManagedBackfill.RunAsync"
      to: "IContentSiteIndexStore.SetSeedManagedIfNullAsync"
      via: "per-unclassified-row write"
      pattern: "SetSeedManagedIfNullAsync"
    - from: "DeckFlow.Web/Program.cs"
      to: "SeedManagedBackfill.RunAsync"
      via: "startup invocation after seed load"
      pattern: "SeedManagedBackfill"
---

<objective>
Ship the D-02 legacy backfill: a host-agnostic `SeedManagedBackfill` that classifies every
`seed_managed IS NULL` row by CURRENT `index-seed.json` membership (present -> true, absent -> false),
idempotently and without ever crashing host startup, wired at both the web (prod-pointed - where the
~70 prod-only rows live) and Studio (local) startups. Mirrors the P89 `ContentBodyHashBackfill`
dual-host design exactly.

Purpose: So no reconcile run ever sees an unclassified row, and the ~70 prod-only rows become
distinguishable from seed-owned rows (the SYNC-17 safety guarantee).
Output: the backfill class + a per-host seed-membership seam + dual-host wiring.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/91-reconcile-seed-lifecycle/91-CONTEXT.md
@.planning/phases/91-reconcile-seed-lifecycle/91-RESEARCH.md
@.planning/phases/91-reconcile-seed-lifecycle/91-PATTERNS.md
@.planning/phases/91-reconcile-seed-lifecycle/91-01-SUMMARY.md

<interfaces>
<!-- From 91-01 (dependency). Executor USES these. -->
IContentSiteIndexStore.GetAllRowsAsync() : IReadOnlyList<ContentSiteIndexRow>
IContentSiteIndexStore.SetSeedManagedIfNullAsync(long id, bool seedManaged, ct) : Task<int>
ContentSiteIndexRow.SeedManaged : bool?
SeedIndexFileReader.ReadNaturalKeys(string seedFilePath) : IReadOnlySet<string>   // U+0000-joined keys
ContentNaturalKey.TryDerive(ContentSiteIndexRow, out (string Type,string Value)) : bool

<!-- New seam this plan defines -->
ISeedKeyMembershipSource { IReadOnlySet<string> GetSeedNaturalKeys(); }   // host provides the seed key set
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SeedManagedBackfill (host-agnostic, idempotent, startup-safe)</name>
  <files>DeckFlow.Core/Content/SeedManagedBackfill.cs, DeckFlow.Core.Tests/Content/SeedManagedBackfillTests.cs</files>
  <read_first>
    - DeckFlow.Core/Content/ContentBodyHashBackfill.cs (full file - exact host-agnostic/idempotent/never-crash-startup shape, esp. the OperationCanceledException-rethrow + swallow-other try/catch ~70-93)
    - DeckFlow.Core.Tests/Content/ContentBodyHashBackfillTests.cs (test template)
    - 91-PATTERNS.md "SeedManagedBackfill.cs" section; 91-RESEARCH.md Pitfall 1 (idempotency), Code Examples
  </read_first>
  <behavior>
    - A null-seed_managed row whose natural key IS in the membership set -> classified true.
    - A null-seed_managed row whose natural key is NOT in the set -> classified false.
    - A row with no derivable natural key -> logged + skipped (never crashes).
    - Second RunAsync on the same data writes zero rows (idempotent; already-classified rows filtered by seed_managed IS NULL at the SQL level and skipped in memory).
    - An injected membership source that throws is caught, logged, and does not crash startup.
  </behavior>
  <action>
    Create `SeedManagedBackfill` in DeckFlow.Core/Content mirroring ContentBodyHashBackfill's shape: a
    sealed class taking `IContentSiteIndexStore store`, `ISeedKeyMembershipSource membership`, and
    `ILogger<SeedManagedBackfill> logger` (ArgumentNullException.ThrowIfNull each). Define the
    `ISeedKeyMembershipSource` seam (single `IReadOnlySet<string> GetSeedNaturalKeys()`) in the same
    file. `RunAsync`: fetch GetAllRowsAsync, get the seed key set once, then per row where
    `SeedManaged is null` derive the natural key via ContentNaturalKey.TryDerive, classify
    `membership.Contains($"{type}U+0000{value}")`, and call SetSeedManagedIfNullAsync(row.Id, classified).
    Copy the OperationCanceledException-rethrow / swallow-other-exceptions-and-continue discipline
    verbatim; log a structured summary (classified-true / classified-false / skipped counts) at the end.
    Write SeedManagedBackfillTests: present->true, absent->false, no-key->skip, and a re-run-writes-zero
    idempotency case (using a fake store or in-memory SQLite store).
  </action>
  <acceptance_criteria>
    - Test asserts present-key row -> true, absent-key row -> false after RunAsync.
    - Test asserts a second RunAsync writes 0 rows (idempotency - the Pitfall 1 guard).
    - Test asserts a throwing membership source / unreadable row does not propagate out of RunAsync.
    - Build clean, 0 new warnings.
  </acceptance_criteria>
  <verify>
    <automated>dotnet.exe test DeckFlow.Core.Tests/DeckFlow.Core.Tests.csproj --no-restore --filter "FullyQualifiedName~SeedManagedBackfill"</automated>
  </verify>
  <done>SeedManagedBackfill classifies unclassified rows by seed membership, is idempotent on re-run, never crashes startup, and is fully unit-tested against the ContentBodyHashBackfill template.</done>
</task>

<task type="auto">
  <name>Task 2: Dual-host wiring (web prod-pointed + Studio local)</name>
  <files>DeckFlow.Web/Services/Content/WebSeedKeyMembershipSource.cs, DeckFlow.Web/Program.cs, DeckFlow.Studio/Services/StudioSeedKeyMembershipSource.cs, DeckFlow.Studio/Program.cs</files>
  <read_first>
    - DeckFlow.Web/Program.cs (ContentBodyHashBackfill registration ~101 + RunAsync invocation ~274 - after schema-ensure + seed load)
    - DeckFlow.Studio/Program.cs (ContentBodyHashBackfill registration ~97 + RunAsync ~220; content-kb.db path ~75)
    - DeckFlow.Web/Services/Content/ContentKbSeedLoader.cs (how the deployed seed file path is resolved)
    - 91-RESEARCH.md Architectural Responsibility Map ("must run on the PROD-pointed store via web startup") + Pitfall 6 (repoRoot/content-kb on Studio)
  </read_first>
  <behavior>
    - Web host: backfill runs AFTER the seed load, against the prod-pointed content_site_index store, using the deployed index-seed.json membership.
    - Studio host: backfill runs at startup against the local content-kb.db store, using the operator git-checkout seed membership.
    - Startup completes even if the seed file is absent (empty membership -> all null rows classified false).
  </behavior>
  <action>
    Create `WebSeedKeyMembershipSource` (Web) that resolves the deployed index-seed.json path the same
    way ContentKbSeedLoader does and returns `SeedIndexFileReader.ReadNaturalKeys(path)`. Create
    `StudioSeedKeyMembershipSource` (Studio) that resolves the operator git-checkout seed path (the same
    repoRoot/content-kb/seed/index-seed.json GitBodyCoverageAudit-style resolution) and returns the same.
    Register `ISeedKeyMembershipSource` + `SeedManagedBackfill` in both Program.cs files mirroring the
    ContentBodyHashBackfill registration lines, and invoke `SeedManagedBackfill.RunAsync()` at the same
    startup point ContentBodyHashBackfill runs - on web, AFTER seed load (so membership reflects the
    just-loaded seed). Preserve the existing await/order; do not reorder ContentBodyHashBackfill.
  </action>
  <acceptance_criteria>
    - Both Program.cs files register ISeedKeyMembershipSource + SeedManagedBackfill and invoke RunAsync at startup.
    - Web invocation is ordered after the seed-load call (verified by reading Program.cs order).
    - `dotnet build DeckFlow.sln` clean; both hosts still start (no new required-service resolution failure).
  </acceptance_criteria>
  <verify>
    <automated>dotnet build DeckFlow.sln --no-restore 2>&1 | grep -viE '^\s*#' | grep -icE 'error|warning' | grep -qx 0 && echo WIRING_BUILD_CLEAN</automated>
  </verify>
  <done>SeedManagedBackfill is wired and invoked on both the web (prod, post-seed-load) and Studio (local) hosts with a per-host membership source; solution builds clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| seed file -> membership source | Operator/deploy-authored JSON read into a key set; no destructive action |
| backfill -> prod DB (web) | Idempotent null-only classification UPDATEs against prod at boot |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-91-06 | Denial of Service | Backfill crashing host startup on an unreadable seed/row | mitigate | OperationCanceledException-rethrow + swallow-other pattern (ContentBodyHashBackfill template); startup-safe test |
| T-91-07 | Tampering | Misclassification of a genuinely seed-owned row as prod-owned | accept (guarded downstream) | D-02 tail risk is caught by the SYNC-12 dry-run + flag gate before any destructive apply; backfill itself is non-destructive |
| T-91-08 | Elevation of Privilege | Backfill running DDL against prod | mitigate | SetSeedManagedIfNullAsync is UPDATE-only; the prod store schema-ensure is governed by the web host's existing switch, not this backfill |
</threat_model>

<verification>
- `dotnet build DeckFlow.sln` clean, 0 new warnings.
- SeedManagedBackfill unit tests green (classification + idempotency + startup-safety).
- Program.cs wiring reviewed for correct ordering (web: after seed load).
</verification>

<success_criteria>
Every unclassified content_site_index row is classified by current seed membership on both hosts,
idempotently and without startup risk - so the reconciler never encounters a NULL-marker row and the
~70 prod-only rows are provably distinguishable from seed-owned rows.
</success_criteria>

<output>
Create `.planning/phases/91-reconcile-seed-lifecycle/91-03-SUMMARY.md` when done.
</output>
