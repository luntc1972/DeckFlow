---
phase: 32-expert-context-selection
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - DeckFlow.Core/Content/IContentSiteIndexStore.cs
  - DeckFlow.Core/Content/ContentSiteIndexStore.cs
  - DeckFlow.Core/Knowledge/ContentArtifactSpec.cs
  - DeckFlow.Web/Models/ContentKbExcerpt.cs
  - DeckFlow.Web/Services/ContentKbRelevanceService.cs
  - DeckFlow.Web.Tests/TestDoubles/FakeContentSiteIndexStore.cs
  - DeckFlow.Web.Tests/ContentKbRelevanceServiceTests.cs
  - DeckFlow.Web.Tests/ContentKbMergedClipsTests.cs
  - DeckFlow.Web.Tests/ContentSiteIndexStoreTests.cs
  - DeckFlow.Web.Tests/ContentKbExcerptTests.cs
autonomous: true
requirements: [SEL-02, SEL-05, SEL-06]
must_haves:
  truths:
    - "An is_evergreen column exists on content_site_index on both fresh and migrated databases (SQLite + Postgres)"
    - "ContentSiteIndexRow.IsEvergreen round-trips true/false through UpsertRowAsync + GetByIdAsync"
    - "UpsertSql AND UpsertPreservingVisibilitySql both write is_evergreen; the preserving variant never clears admin-set evergreen on seed reload"
    - "GetMergedClipsAsync injects pinned-video clips first in document order regardless of score"
    - "Followed-creator clips with >=1 scoring dimension outrank auto matches"
    - "At most 1 evergreen clip fills a leftover slot"
    - "Budget trim removes tier 4, then 3, then 2, then 1; at least one tier-1 clip always survives"
    - "Pin cap of 3 videos is enforced inside the merge method"
    - "Every clip carries a ClipOrigin of pinned/followed/auto/evergreen"
    - "SetEvergreenAsync is declared in the separate IContentSiteIndexStore.cs interface file AND implemented in ContentSiteIndexStore.cs; adding it keeps every existing implementer (shared fake + nested relevance fake) compiling"
  artifacts:
    - path: "DeckFlow.Core/Content/IContentSiteIndexStore.cs"
      provides: "SetEvergreenAsync interface declaration (with XML doc) — the member admin code calls via the interface"
      contains: "SetEvergreenAsync"
    - path: "DeckFlow.Core/Content/ContentSiteIndexStore.cs"
      provides: "is_evergreen migration + UpsertSql/UpsertPreservingVisibilitySql writes + SetEvergreenAsync impl + ReadRow ordinal 13"
      contains: "is_evergreen"
    - path: "DeckFlow.Web/Services/ContentKbRelevanceService.cs"
      provides: "GetMergedClipsAsync tier-fill method + ExpertSelection record + ClipOrigin assignment"
      contains: "GetMergedClipsAsync"
    - path: "DeckFlow.Web/Models/ContentKbExcerpt.cs"
      provides: "ClipOrigin property with { get; init; }"
      contains: "ClipOrigin"
    - path: "DeckFlow.Web.Tests/TestDoubles/FakeContentSiteIndexStore.cs"
      provides: "SetEvergreenAsync implementation on the shared fake"
      contains: "SetEvergreenAsync"
    - path: "DeckFlow.Web.Tests/ContentKbMergedClipsTests.cs"
      provides: "tier-fill unit tests"
      exports: ["Tier1_PinsInjectedFirst", "Tier2_FollowedCreator_GateRelaxed", "Tier4_Evergreen_MaxOneClip", "TrimOrder", "PinSurvivesTrim", "PinCap_MaxThree"]
  key_links:
    - from: "DeckFlow.Web/Services/ContentKbRelevanceService.cs"
      to: "DeckFlow.Core/Content/ContentSiteIndexStore.cs"
      via: "GetPublishedRowsAsync + row.IsEvergreen"
      pattern: "GetPublishedRowsAsync"
    - from: "GetMergedClipsAsync tier 4"
      to: "ContentSiteIndexRow.IsEvergreen"
      via: "evergreen filter"
      pattern: "IsEvergreen"
    - from: "ContentSiteIndexStore.UpsertRowAsync"
      to: "is_evergreen column"
      via: "@isEvergreen bound from row.IsEvergreen"
      pattern: "@isEvergreen"
---

<objective>
Lay the data + service foundation for expert-context selection: add the artifact-level `IsEvergreen` flag (DB column, store method, model property) and the new `GetMergedClipsAsync` tier-fill method on `ContentKbRelevanceService`, plus the `ClipOrigin` marker on `ContentKbExcerpt`.

Purpose: Everything downstream (zip persistence, UI, admin toggle, panel markers) consumes these contracts. The merge method is the engineering core of the phase (SEL-02). The existing auto-only path (`GetRelevantClipsAsync`, `ScoreArtifact`, `ScoreAllAsync`) must remain completely untouched.

Output: `is_evergreen` column (written by BOTH upsert SQL variants) + `SetEvergreenAsync`, `ContentSiteIndexRow.IsEvergreen`, `ContentKbExcerpt.ClipOrigin`, `IContentKbRelevanceService.GetMergedClipsAsync` + `ExpertSelection` record, full tier-fill unit tests, and a store round-trip test for the new column. Every existing `IContentSiteIndexStore` implementer is updated for the new interface member.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-expert-context-selection-runs-after-phase-30-before-phase-31/32-CONTEXT.md
@.planning/phases/32-expert-context-selection-runs-after-phase-30-before-phase-31/32-RESEARCH.md
@.planning/phases/32-expert-context-selection-runs-after-phase-30-before-phase-31/32-PATTERNS.md

<interfaces>
<!-- Confirmed against live code 2026-06-07. Executor uses these directly. -->

IContentKbRelevanceService (DeckFlow.Web/Services/ContentKbRelevanceService.cs):
  - line 15: public interface IContentKbRelevanceService
  - line 27: Task<IReadOnlyList<ContentKbExcerpt>?> GetRelevantClipsAsync(string? commanderName, string? bracket, IReadOnlySet<string>? deckArchetypes = null, int maxRenderedChars = 4500, CancellationToken ct = default)  -- DO NOT MODIFY
  - line 41: Task<IReadOnlyList<(ContentSiteIndexRow Row, double Score)>> ScoreAllAsync(...)  -- DO NOT MODIFY (admin preview)
  - line 74: internal const int DefaultMaxRenderedChars = 4500;
  - Private helpers to reuse (do not duplicate): ParseRowsAsync, SelectTopClips (lines 317-347), EstimateRenderedChars (lines 349-363), ScoreArtifact (lines 200-238; gate at line 237: `return dimensionsHit >= 2 ? score : 0d;` -- DO NOT CHANGE this gate)
  - Flag gate (line ~157): `if (!_flagCache.IsEnabled("content.kb.enabled")) return null;`

IContentSiteIndexStore (DeckFlow.Core/Content/IContentSiteIndexStore.cs) -- SEPARATE FILE from the implementation:
  - line 8: public interface IContentSiteIndexStore
  - SetVisibilityAsync declaration: lines 64-71 (XML <summary> + <param> + <returns>) — this is the EXACT shape to mirror for the new SetEvergreenAsync interface member
  - SetEvergreenAsync MUST be DECLARED here (interface) and IMPLEMENTED in ContentSiteIndexStore.cs; admin code (Plan 04) calls `_store.SetEvergreenAsync(...)` via this interface, so a missing interface declaration is a compile blocker

ContentSiteIndexStore impl (DeckFlow.Core/Content/ContentSiteIndexStore.cs):
  - is_visible migration block: lines 56-64
  - UpsertRowAsync: lines 75-105 (binds @source..@naturalKeyValue; NO @isVisible — plain upsert relies on column DEFAULT)
  - UpsertRowPreservingVisibilityAsync: lines 108-139 (binds @isVisible = FormatVisibility(false) at line 137)
  - SetVisibilityAsync: lines 277-292
  - GetPublishedRowsAsync: line 184
  - GetByIdAsync: line 242
  - ReadRow IsVisible = ReadVisibility(reader, 12): line 458
  - ReadVisibility helper: lines 462-474
  - FormatVisibility helper: line 387
  - UpsertSql constant: line 488 (writes neither is_visible nor is_evergreen)
  - UpsertPreservingVisibilitySql constant: line 525 (writes is_visible on INSERT; OMITS it from DO UPDATE SET to preserve curation)
  - Postgres/Sqlite CREATE TABLE constants: lines 564-600 (is_visible currently last data column)
  - 4 SELECT statements listing columns ending in is_visible: lines ~153-264

ContentSiteIndexRow (DeckFlow.Core/Knowledge/ContentArtifactSpec.cs): sealed record, all props `required ... { get; init; }`

ContentKbExcerpt (DeckFlow.Web/Models/ContentKbExcerpt.cs): sealed record; line 8 comment documents the { get; init; } System.Text.Json constraint

EXISTING IContentSiteIndexStore IMPLEMENTERS (HIGH-2 — adding SetEvergreenAsync breaks all of these unless updated in THIS plan):
  - DeckFlow.Web.Tests/TestDoubles/FakeContentSiteIndexStore.cs (line 15) — the SHARED stateful fake; SetVisibilityAsync at lines 59-72 is the exact mirror to copy
  - DeckFlow.Web.Tests/ContentKbRelevanceServiceTests.cs (nested class TrackingContentSiteIndexStore at line 425) — relevance-only fake; its SetVisibilityAsync throws NotSupportedException (line 456)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add is_evergreen column (incl. both UPSERT writes), SetEvergreenAsync, and ContentSiteIndexRow.IsEvergreen</name>
  <files>DeckFlow.Core/Content/IContentSiteIndexStore.cs, DeckFlow.Core/Content/ContentSiteIndexStore.cs, DeckFlow.Core/Knowledge/ContentArtifactSpec.cs</files>
  <read_first>
    - DeckFlow.Core/Content/IContentSiteIndexStore.cs (FULL — the interface; SetVisibilityAsync declaration lines 64-71 is the mirror for the new SetEvergreenAsync declaration)
    - DeckFlow.Core/Content/ContentSiteIndexStore.cs (FULL — migration block lines 56-64, UpsertRowAsync 75-105, UpsertRowPreservingVisibilityAsync 108-139, SetVisibilityAsync 277-292, FormatVisibility 387, ReadRow + ReadVisibility 458-474, UpsertSql 488, UpsertPreservingVisibilitySql 525, CREATE TABLE constants 564-600, all SELECT column lists ~153-264, IContentSiteIndexStore interface)
    - DeckFlow.Core/Knowledge/ContentArtifactSpec.cs (FULL — ContentSiteIndexRow record)
    - DeckFlow.Web/Models/ContentKbExcerpt.cs (line 8 { get; init; } constraint comment)
    - .planning/phases/32-expert-context-selection-runs-after-phase-30-before-phase-31/32-RESEARCH.md (Pattern 1, Pitfall 2, Code Examples)
    - .planning/phases/32-expert-context-selection-runs-after-phase-30-before-phase-31/32-PATTERNS.md (ContentSiteIndexStore.cs section)
  </read_first>
  <action>
    In ContentSiteIndexStore.cs, immediately after the existing `is_visible` migration block (lines 56-64), add a parallel `is_evergreen` migration block: `if (!columns.Contains("is_evergreen"))` running `ALTER TABLE content_site_index ADD COLUMN is_evergreen BOOLEAN NOT NULL DEFAULT FALSE;` for Postgres and `ALTER TABLE content_site_index ADD COLUMN is_evergreen INTEGER NOT NULL DEFAULT 0;` for SQLite (use the existing `_connectionInfo.IsPostgres` discriminator).

    Add `is_evergreen` to BOTH `PostgresCreateTableSql` and `SqliteCreateTableSql` constants as the final data column, immediately after `is_visible`, so fresh installs place it at ordinal 13 (Postgres: `is_evergreen BOOLEAN NOT NULL DEFAULT FALSE`; SQLite: `is_evergreen INTEGER NOT NULL DEFAULT 0`). CRITICAL per Pitfall 2: the CREATE TABLE and ALTER paths must agree so ordinal 13 holds on both fresh and migrated DBs.

    Add `is_evergreen` to the column list of all four SELECT statements (~lines 153-264) immediately after `is_visible`, preserving column order. Do NOT change the `GetPublishedRowsAsync` WHERE clause (it still filters `is_visible = @visible`).

    CRITICAL — UPSERT WRITES (HIGH-1, confirmed against live code 2026-06-07: `UpsertSql` at line 488 writes NEITHER `is_visible` NOR `is_evergreen`; `UpsertPreservingVisibilitySql` at line 525 writes `is_visible` on INSERT but deliberately OMITS it from `DO UPDATE SET` so reloads preserve curation). Without these edits, `UpsertRowAsync(row with IsEvergreen=true)` silently never persists the flag and the Task 3 round-trip test fails. Apply BOTH:
      1. `UpsertSql` (used by `UpsertRowAsync` — the path the Task 3 round-trip test exercises): add `is_evergreen` to the INSERT column list (after `natural_key_value`), add `@isEvergreen` to the VALUES list, and add `is_evergreen = EXCLUDED.is_evergreen` to the `ON CONFLICT ... DO UPDATE SET` clause (an explicit upsert both inserts AND updates the flag, mirroring how every other field is updated). Then in `UpsertRowAsync` (lines 75-105) add `RelationalDatabaseConnection.AddParameter(command, "@isEvergreen", FormatVisibility(row.IsEvergreen));` after the `@naturalKeyValue` binding (line ~103). Bind from `row.IsEvergreen` (NOT hardcoded false) — this is what makes the round-trip test pass.
      2. `UpsertPreservingVisibilitySql` (used by `UpsertRowPreservingVisibilityAsync` — the seed reload path): add `is_evergreen` to the INSERT column list and `@isEvergreen` to the VALUES list, but DO NOT add `is_evergreen` to the `DO UPDATE SET` clause (mirror exactly how `is_visible` is preserved here — insert-only, never overwrite-on-conflict) so reloading the seed CANNOT clear an admin's evergreen curation. Then in `UpsertRowPreservingVisibilityAsync` (lines 108-139) add `RelationalDatabaseConnection.AddParameter(command, "@isEvergreen", FormatVisibility(false));` after the `@isVisible` binding (line ~137) — bind a literal false, matching `@isVisible`; the conflict row's evergreen is preserved by the SQL, new rows default to false.

    In `ReadRow`, after `IsVisible = ReadVisibility(reader, 12),` add `IsEvergreen = ReadVisibility(reader, 13)` (reuse the existing `ReadVisibility` helper unchanged).

    Add `SetEvergreenAsync(long id, bool evergreen, CancellationToken cancellationToken = default)` in TWO places (the interface and the implementation live in SEPARATE files): (1) DECLARE it in `IContentSiteIndexStore` (DeckFlow.Core/Content/IContentSiteIndexStore.cs) directly after the `SetVisibilityAsync` declaration (lines 64-71), copying that method's full XML `<summary>`/`<param>`/`<returns>` doc shape (swap "visibility"→"evergreen flag"); (2) IMPLEMENT it in `ContentSiteIndexStore` (DeckFlow.Core/Content/ContentSiteIndexStore.cs) mirroring `SetVisibilityAsync` exactly: `EnsureSchemaAsync`, open connection, `UPDATE content_site_index SET is_evergreen = @evergreen WHERE id = @id;`, params via `RelationalDatabaseConnection.AddParameter` with `FormatVisibility(evergreen)` for `@evergreen` and the raw `id` for `@id`, return rows affected. Include an XML `<summary>` doc comment (Core gate requires CS1591 clean).

    In ContentArtifactSpec.cs, add to `ContentSiteIndexRow`: `public bool IsEvergreen { get; init; }` with an XML `<summary>`. NOT `required` (defaults to false so seed rows and existing data without the value still deserialize). Use `{ get; init; }` per the round-tripped-record constraint.

    Touch only the lines that need touching — no Format Document, no reindent of surrounding blocks. Preserve LF line endings.
  </action>
  <verify>
    <automated>"/mnt/c/Program Files/dotnet/dotnet.exe" build DeckFlow.Core/DeckFlow.Core.csproj -warnaserror:CS1591 2>&1 | grep -E "Build succeeded|error" | head</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "is_evergreen" DeckFlow.Core/Content/ContentSiteIndexStore.cs` returns >= 11 (migration block x2 SQL strings, both CREATE TABLE constants, 4 SELECT lists, UpsertSql column+value+DO-UPDATE, UpsertPreservingVisibilitySql column+value, SetEvergreenAsync UPDATE)
    - Both `UpsertSql` AND `UpsertPreservingVisibilitySql` mention `is_evergreen`: `grep -c "is_evergreen" DeckFlow.Core/Content/ContentSiteIndexStore.cs` within those two constants is non-zero each (manual inspection: UpsertSql has it in INSERT cols + VALUES + DO UPDATE SET; UpsertPreservingVisibilitySql has it in INSERT cols + VALUES but NOT in DO UPDATE SET)
    - `grep -c "@isEvergreen" DeckFlow.Core/Content/ContentSiteIndexStore.cs` returns >= 4 (UpsertSql VALUES, UpsertPreservingVisibilitySql VALUES, UpsertRowAsync binding, UpsertRowPreservingVisibilityAsync binding)
    - `grep -n "IsEvergreen = ReadVisibility(reader, 13)" DeckFlow.Core/Content/ContentSiteIndexStore.cs` matches exactly one line
    - `grep -n "SetEvergreenAsync" DeckFlow.Core/Content/IContentSiteIndexStore.cs` matches the interface declaration (the method with its XML doc comment in the SEPARATE interface file)
    - `grep -n "SetEvergreenAsync" DeckFlow.Core/Content/ContentSiteIndexStore.cs` matches the implementation (the public method body running the UPDATE)
    - `grep -n "public bool IsEvergreen { get; init; }" DeckFlow.Core/Knowledge/ContentArtifactSpec.cs` matches one line; the line is NOT prefixed with `required`
    - The `is_evergreen = EXCLUDED.is_evergreen` line appears in `UpsertSql` ONLY (NOT in `UpsertPreservingVisibilitySql`'s DO UPDATE SET — that one preserves curation)
    - `dotnet build DeckFlow.Core -warnaserror:CS1591` reports Build succeeded with 0 errors
    - The existing `GetPublishedRowsAsync` WHERE clause still reads `is_visible = @visible` (not changed to is_evergreen)
  </acceptance_criteria>
  <done>is_evergreen column self-heals on both providers, both upsert variants write it (plain updates on conflict, preserving variant insert-only), SetEvergreenAsync exists, ContentSiteIndexRow.IsEvergreen round-trips, Core builds CS1591-clean.</done>
</task>

<task type="auto">
  <name>Task 2: Add ClipOrigin to ContentKbExcerpt, GetMergedClipsAsync tier-fill, and SetEvergreenAsync to existing test fakes</name>
  <files>DeckFlow.Web/Models/ContentKbExcerpt.cs, DeckFlow.Web/Services/ContentKbRelevanceService.cs, DeckFlow.Web.Tests/TestDoubles/FakeContentSiteIndexStore.cs, DeckFlow.Web.Tests/ContentKbRelevanceServiceTests.cs</files>
  <read_first>
    - DeckFlow.Web/Models/ContentKbExcerpt.cs (FULL — { get; init; } constraint at line 8, Score property at line 29)
    - DeckFlow.Web/Services/ContentKbRelevanceService.cs (FULL — interface 15-44, GetRelevantClipsAsync 149-179, ScoreArtifact 200-238 incl. gate line 237, SelectTopClips 317-347, EstimateRenderedChars 349-363, flag gate, ParseRowsAsync)
    - DeckFlow.Web.Tests/TestDoubles/FakeContentSiteIndexStore.cs (FULL — SetVisibilityAsync at lines 59-72 is the mirror to copy for SetEvergreenAsync)
    - DeckFlow.Web.Tests/ContentKbRelevanceServiceTests.cs (nested TrackingContentSiteIndexStore class at line 425; SetVisibilityAsync NotSupportedException at line 456 is the mirror for the relevance-only fake)
    - .planning/phases/32-expert-context-selection-runs-after-phase-30-before-phase-31/32-RESEARCH.md (Pattern 4, Pitfall 1, Pitfall 4)
    - .planning/phases/32-expert-context-selection-runs-after-phase-30-before-phase-31/32-PATTERNS.md (ContentKbRelevanceService.cs + ContentKbExcerpt.cs sections)
  </read_first>
  <action>
    HIGH-2 — UPDATE EXISTING IMPLEMENTERS FIRST (Task 1 added `SetEvergreenAsync` to `IContentSiteIndexStore`; the test project will NOT compile until BOTH existing fakes implement it):
      - DeckFlow.Web.Tests/TestDoubles/FakeContentSiteIndexStore.cs: add a stateful `SetEvergreenAsync(long id, bool evergreen, CancellationToken cancellationToken = default)` that mirrors the existing `SetVisibilityAsync` (lines 59-72) exactly but flips the evergreen flag: iterate `Rows`, on `Rows[i].Id == id` do `Rows[i] = Rows[i] with { IsEvergreen = evergreen };` and increment a count, return `Task.FromResult(count)`. Add an XML `<summary>` (this assembly does not gate CS1591, but match the file's existing doc-comment style).
      - DeckFlow.Web.Tests/ContentKbRelevanceServiceTests.cs nested `TrackingContentSiteIndexStore` (line 425): add `public Task<int> SetEvergreenAsync(long id, bool evergreen, CancellationToken cancellationToken = default) => throw new NotSupportedException();` — this fake is relevance-only and never toggles evergreen, so the NotSupportedException mirror (matching its SetVisibilityAsync at line 456) is correct.

    In ContentKbExcerpt.cs, after the `Score` property (line 29), add `public string ClipOrigin { get; init; } = "auto";` with an XML `<summary>` ("How this clip entered the selection (pinned / followed / auto / evergreen)."). NOT `required` — the default `"auto"` keeps existing serialized 32-expert-context.json entries deserializing correctly. Use `{ get; init; }` per the line-8 constraint.

    In SelectTopClips (existing auto path), set `ClipOrigin = "auto"` on each `new ContentKbExcerpt { ... }` so the existing path is explicit. Do NOT otherwise modify GetRelevantClipsAsync, ScoreArtifact, or ScoreAllAsync.

    Add an internal record at the bottom of the file (with the other internal records): `internal sealed record ExpertSelection(IReadOnlyList<string> PinnedVideoIds, IReadOnlySet<string> FollowedCreators);` — PinnedVideoIds matched against `row.YoutubeVideoId ?? row.RssGuid`, max 3 enforced inside the method; FollowedCreators is an OrdinalIgnoreCase set matched against `row.Source`.

    Add `GetMergedClipsAsync(ExpertSelection selection, string? commanderName, string? bracket, IReadOnlySet<string>? deckArchetypes = null, int maxRenderedChars = DefaultMaxRenderedChars, CancellationToken ct = default)` to BOTH the interface and the implementation, returning `Task<IReadOnlyList<ContentKbExcerpt>?>`. Implementation:
      1. Flag gate first: `if (!_flagCache.IsEnabled("content.kb.enabled")) return null;`
      2. Normalize commander/bracket and resolve archetypes using the same private helpers GetRelevantClipsAsync uses. Load `rows = await _store.GetPublishedRowsAsync(ct)`.
      3. Cap pins: `var pinIds = selection.PinnedVideoIds.Distinct().Take(3).ToList();` (SEL-02 pin cap of 3).
      4. Tier 1 (pinned): rows whose `YoutubeVideoId ?? RssGuid` is in pinIds, ordered to match pinIds document order; build their clips in document order with NO score gate; set `ClipOrigin = "pinned"`. Track which row ids are now consumed.
      5. Tier 2 (followed): rows where `selection.FollowedCreators.Contains(row.Source)` (OrdinalIgnoreCase) AND not already consumed; keep only rows with `dimensionsHit >= 1` (RELAXED from 2 — see Pitfall 1: do NOT filter on `ScoreArtifact` return value, which returns 0 for <2 dims; compute the raw dimension count via a NEW private helper `CountDimensionsHit(...)` that mirrors ScoreArtifact's dimension-counting sub-expressions WITHOUT changing ScoreArtifact). Order by score descending; set `ClipOrigin = "followed"`.
      6. Tier 3 (auto): remaining un-consumed rows scored with existing logic, keep `score >= 2.0`, order by score descending; set `ClipOrigin = "auto"`. This MUST reproduce the existing auto behavior (reuse ScoreArtifact + SelectTopClips logic).
      7. Tier 4 (evergreen): rows where `row.IsEvergreen` AND not consumed; take at most 1 clip total regardless of score; set `ClipOrigin = "evergreen"`.
      8. Build the flat clip list by appending tier 1, 2, 3, 4 in order while respecting the K=5 clip cap and `maxRenderedChars` budget. Track tier-boundary indices in the flat list.
      9. Budget trim waterfall (Pitfall 4): while `EstimateRenderedChars(list) > maxRenderedChars`, remove the last clip belonging to the highest remaining tier (4 -> 3 -> 2 -> 1). Never remove the final tier-1 clip: if only one tier-1 clip remains and it still busts the budget, keep it (at least 1 clip survives).
      10. Return null if the final list is empty, else the list.
    Reuse `EstimateRenderedChars`, `ParseRowsAsync`, `SelectTopClips`, and `ScoreArtifact` rather than re-implementing. Do NOT change the `ScoreArtifact` gate at line 237. Include XML `<summary>` docs on the new interface method.

    Touch only the lines that need touching; no formatter sweep; LF endings.
  </action>
  <verify>
    <automated>"/mnt/c/Program Files/dotnet/dotnet.exe" build DeckFlow.Web/DeckFlow.Web.csproj 2>&1 | grep -E "Build succeeded|error" | head; "/mnt/c/Program Files/dotnet/dotnet.exe" build DeckFlow.Web.Tests/DeckFlow.Web.Tests.csproj 2>&1 | grep -E "Build succeeded|error" | head</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "public string ClipOrigin { get; init; } = \"auto\";" DeckFlow.Web/Models/ContentKbExcerpt.cs` matches one line
    - `grep -c "GetMergedClipsAsync" DeckFlow.Web/Services/ContentKbRelevanceService.cs` returns >= 2 (interface + impl)
    - `grep -n "internal sealed record ExpertSelection" DeckFlow.Web/Services/ContentKbRelevanceService.cs` matches one line
    - `grep -n "SetEvergreenAsync" DeckFlow.Web.Tests/TestDoubles/FakeContentSiteIndexStore.cs` matches one line (stateful `with { IsEvergreen = evergreen }` mirror)
    - `grep -n "SetEvergreenAsync" DeckFlow.Web.Tests/ContentKbRelevanceServiceTests.cs` matches one line (NotSupportedException mirror on the nested fake)
    - `grep -n "return dimensionsHit >= 2 ? score : 0d;" DeckFlow.Web/Services/ContentKbRelevanceService.cs` STILL matches exactly one line (ScoreArtifact gate unchanged)
    - `grep -n "GetRelevantClipsAsync" DeckFlow.Web/Services/ContentKbRelevanceService.cs` interface signature line is byte-identical to before (auto path untouched)
    - `dotnet build DeckFlow.Web` AND `dotnet build DeckFlow.Web.Tests` both report Build succeeded with 0 errors (proves HIGH-2: the whole test project compiles after the interface grew)
  </acceptance_criteria>
  <done>ClipOrigin exists on the excerpt; GetMergedClipsAsync implements all four tiers with tier-aware budget trim and pin cap; BOTH existing IContentSiteIndexStore fakes implement SetEvergreenAsync; the auto path and ScoreArtifact gate are byte-unchanged; Web AND the full test project build.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Tier-fill unit tests + is_evergreen store round-trip test</name>
  <files>DeckFlow.Web.Tests/ContentKbMergedClipsTests.cs, DeckFlow.Web.Tests/ContentSiteIndexStoreTests.cs, DeckFlow.Web.Tests/ContentKbExcerptTests.cs</files>
  <read_first>
    - DeckFlow.Web.Tests/ContentKbRelevanceServiceTests.cs (FULL — TrackingContentSiteIndexStore fake, CreateRow helper, BuildArtifact helper, test structure lines 1-155)
    - DeckFlow.Web/Services/ContentKbRelevanceService.cs (the GetMergedClipsAsync impl from Task 2)
    - DeckFlow.Core/Content/ContentSiteIndexStore.cs (UpsertRowAsync + GetByIdAsync for round-trip test)
    - DeckFlow.Web.Tests/ContentKbExcerptTests.cs (FULL — existing JSON round-trip test to EXTEND with ClipOrigin coverage)
    - DeckFlow.Web/Models/ContentKbExcerpt.cs (ClipOrigin property from Task 2)
    - .planning/phases/32-expert-context-selection-runs-after-phase-30-before-phase-31/32-PATTERNS.md (ContentKbMergedClipsTests.cs section)
  </read_first>
  <behavior>
    - Tier1_PinsInjectedFirst_InDocumentOrder: 3 pinned videos + 2 high-score auto rows → first clips in result are the pinned ones in pin order, ClipOrigin == "pinned".
    - Tier2_FollowedCreator_GateRelaxedToOneDimension: a followed-creator row with exactly 1 matching dimension is INCLUDED (would be excluded by the >=2 auto gate), ClipOrigin == "followed".
    - Tier3_AutoUnchanged: with empty selection, GetMergedClipsAsync returns the same clip set GetRelevantClipsAsync returns for the same inputs.
    - Tier4_EvergreenFills_MaxOneClip: an IsEvergreen row contributes at most 1 clip and only when slots remain; ClipOrigin == "evergreen".
    - TrimOrder_Tier4ThenTier3ThenTier2ThenTier1: with a tight budget, clips are removed evergreen-first, pinned-last.
    - PinSurvivesTrim_LastTier1ClipKept: a single pinned video with many clips against a tiny budget keeps >= 1 clip.
    - PinCap_MaxThreePinnedVideos_Enforced: 5 pinned ids → at most 3 videos' clips are tier-1.
    - StoreRoundTrip_IsEvergreenTrue: UpsertRowAsync with IsEvergreen=true then GetByIdAsync returns IsEvergreen==true (and false for a default row). This is the regression guard that BOTH the UpsertSql write AND the ReadRow ordinal landed (HIGH-1).
    - ContentKbExcerpt_JsonRoundTrip_PreservesClipOrigin: serialize a ContentKbExcerpt with ClipOrigin = "pinned", deserialize, assert ClipOrigin == "pinned" (proves the new { get; init; } property survives System.Text.Json round-trip).
  </behavior>
  <action>
    Create DeckFlow.Web.Tests/ContentKbMergedClipsTests.cs as `public sealed class ContentKbMergedClipsTests` in namespace `DeckFlow.Web.Tests`, reusing the `TrackingContentSiteIndexStore` fake, `CreateRow`, and `BuildArtifact` helpers from ContentKbRelevanceServiceTests (copy/extend the `CreateRow` helper to accept a `bool isEvergreen = false` parameter and set `IsEvergreen = isEvergreen`). Enable `content.kb.enabled` in the test flag fake the same way the existing relevance tests do. Implement xUnit `[Fact]` tests for each behavior above, asserting ClipOrigin values and ordering. For the gate-relax test, construct a followed-creator artifact with exactly one matching dimension (e.g., commander match only) and assert it appears.

    Create or extend DeckFlow.Web.Tests for the store round-trip: add `ContentSiteIndexStoreTests.cs` (or extend an existing store test file if one exists — check first) with `StoreRoundTrip_IsEvergreenTrue` using an in-memory/temp SQLite store (mirror however existing store tests construct the store). Assert IsEvergreen survives UpsertRowAsync → GetByIdAsync (this is the HIGH-1 regression guard — it fails if UpsertSql doesn't bind @isEvergreen or ReadRow doesn't read ordinal 13).

    Extend DeckFlow.Web.Tests/ContentKbExcerptTests.cs (the existing JSON round-trip test class) with a new `[Fact] ContentKbExcerpt_JsonRoundTrip_PreservesClipOrigin` that constructs a `ContentKbExcerpt` with `ClipOrigin = "pinned"` (plus the other required props mirroring the existing test), serializes via `JsonSerializer.Serialize`, deserializes via `JsonSerializer.Deserialize<ContentKbExcerpt>`, and asserts `roundTripped.ClipOrigin == "pinned"`. This satisfies the CONTEXT.md standing constraint that every `{ get; init; }` property on a round-tripped record has round-trip coverage. Touch only the lines being added — do not reformat the existing test.

    Use `{ get; init; }` for any test DTOs. LF endings; no formatter sweep.
  </action>
  <verify>
    <automated>"/mnt/c/Program Files/dotnet/dotnet.exe" test DeckFlow.Web.Tests/DeckFlow.Web.Tests.csproj --filter "ContentKbMergedClipsTests|IsEvergreen|ContentKbExcerptTests" 2>&1 | grep -E "Passed!|Failed!|error" | head</automated>
  </verify>
  <acceptance_criteria>
    - File DeckFlow.Web.Tests/ContentKbMergedClipsTests.cs exists with >= 7 `[Fact]` methods
    - A store round-trip test asserting IsEvergreen true/false survives Upsert→GetById exists
    - `dotnet test --filter "ContentKbMergedClipsTests|IsEvergreen"` reports Passed with 0 failures
    - The gate-relax test (`Tier2_FollowedCreator_GateRelaxedToOneDimension`) asserts a 1-dimension followed clip is present (proves Pitfall 1 is avoided)
    - The pin-survives-trim test asserts the result has >= 1 clip with ClipOrigin == "pinned" under a sub-300-char budget
    - `grep -n "ClipOrigin" DeckFlow.Web.Tests/ContentKbExcerptTests.cs` matches the new round-trip test asserting `ClipOrigin == "pinned"` survives serialize/deserialize
  </acceptance_criteria>
  <done>All tier-fill behaviors and the is_evergreen round-trip are covered by passing tests; VSTest note: if WSL VSTest is unreliable, build clean + run via Windows dotnet.exe per CLAUDE.md.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| relevance service → DB | Reads published rows; new is_evergreen column read by ordinal |
| (no untrusted input in this plan) | Selection input arrives in Plan 02/03; this plan only defines the merge contract |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-01 | Tampering | ReadRow ordinal drift after adding is_evergreen | mitigate | CREATE TABLE constants + ALTER migration both place is_evergreen at ordinal 13; UpsertSql binds @isEvergreen; store round-trip test asserts correct write+read on a migrated DB |
| T-32-02 | Denial of Service | Excessive pins/follows bloating the merged set | mitigate | Pin cap (3) enforced inside GetMergedClipsAsync via Take(3); K=5 clip cap + maxRenderedChars budget enforced regardless of input |
| T-32-03 | Information Disclosure | Spoofed/unknown video ids in selection | mitigate | Tier 1 matches ids against GetPublishedRowsAsync only; unrecognized ids produce no clips (no error surface) |
| T-32-14 | Tampering | Seed reload silently clearing admin-curated evergreen | mitigate | UpsertPreservingVisibilitySql omits is_evergreen from DO UPDATE SET (insert-only); only SetEvergreenAsync mutates the flag for existing rows |
| T-32-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan; phase installs zero external packages (RESEARCH §Package Legitimacy Audit: not applicable) |
</threat_model>

<verification>
- `dotnet build DeckFlow.Core -warnaserror:CS1591` clean
- `dotnet build DeckFlow.Web` clean
- `dotnet build DeckFlow.Web.Tests` clean (proves both fakes implement SetEvergreenAsync — HIGH-2)
- `dotnet test --filter "ContentKbMergedClipsTests|IsEvergreen|ContentKbExcerptTests"` green
- Existing ContentKbRelevanceServiceTests still pass (auto path regression guard): `dotnet test --filter "ContentKbRelevanceServiceTests"`
</verification>

<success_criteria>
SEL-02 (tier-fill merge), SEL-05 (IsEvergreen flag + max-1 evergreen clip), and SEL-06 (ClipOrigin marker on every clip) are implemented and unit-tested. Both upsert SQL variants write is_evergreen (plain updates on conflict, preserving variant insert-only). All IContentSiteIndexStore implementers compile. Existing auto path and ScoreArtifact gate are byte-unchanged.
</success_criteria>

<output>
Create `.planning/phases/32-expert-context-selection-runs-after-phase-30-before-phase-31/32-01-SUMMARY.md` when done.
</output>
