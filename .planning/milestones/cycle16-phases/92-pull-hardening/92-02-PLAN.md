---
phase: 92-pull-hardening
plan: 02
type: execute
wave: 2
depends_on: [92-01]
files_modified:
  - DeckFlow.Studio/ViewModels/PullFromProdCoordinator.cs
  - DeckFlow.Studio.Tests/ViewModels/PullFromProdCoordinatorTests.cs
  - DeckFlow.Studio/Pages/PullFromProd.razor
  - DeckFlow.Studio/Pages/PullFromProd.razor.cs
  - DeckFlow.Studio.Tests/PullFromProdPageTests.cs
autonomous: true
requirements: [SYNC-13, SYNC-14, SYNC-15]

must_haves:
  truths:
    - "SC2 (SYNC-14): PullAndClassifyAsync fetches origin/{branch} + computes behind-count and WARNS-then-proceeds (never hard-refuses) — distinct copy for behind>0 vs GitCommandException (Pitfall 3)"
    - "The Stage-0 fetch + behind-count is bounded by a short linked-CTS timeout (CancelAfter): a slow/hung fetch (DNS/TCP) yields Freshness=Unverified + warn-then-proceed and never blocks Pull; a genuine page-token cancel still propagates (MED-1)"
    - "Always-on, no sync.* flag gate — Pull writes LOCAL-only, so the staleness + divergence guards ship unconditionally (no FeatureFlagCatalog entry, no Studio flag read) — D-03"
    - "SC3 (SYNC-15): every entry with a resolved git body AND a non-null ProdRow is stamped — null prod body_sha256 => Indeterminate (D-01a, the INVERSE of P91's null-skip), mismatch => Confirmed (D-01), match => Clean; computed for ALL such kinds, not only Kind==Diverged (Pitfall 4)"
    - "A non-null ProdRow whose git body is ABSENT/unreadable is stamped Indeterminate (body-less) — surfaced, EXCLUDED from the default adopt set, opt-in only — so prod's body_sha256 is never adopted without a matching local body (D-02a); this HARDENS the prior R4 'body-less prod-only row auto-adopts' behavior to 'surfaced, opt-in' (safety-tightening, consistent with SYNC-13/15, NOT a D-01a contradiction)"
    - "SC1 (SYNC-13/D-02): ApplyAdoptionsAsync adopts body←git (File.Copy), index columns←prod (UpsertContentColumnsOnlyAsync), approval←prod-mirror (SetApprovalStatusAsync), and NEVER writes is_visible/is_hidden — ratified + regression-locked"
    - "prod's body_sha256 is only adopted into the local index when it MATCHES the git body being copied, keeping the local index row and local body file coherent as a result of Pull — D-02a"
    - "Defense-in-depth (D-01): a Confirmed/Indeterminate entry that is NOT in acknowledgedDivergentKeys is skipped inside ApplyAdoptionsAsync even if it reaches the coordinator — belt to the page's suspenders"
    - "The page never reports success on a vacuously-empty adopt set: if the operator selected AdoptProd on >=1 entry but every one was excluded as un-acknowledged Confirmed/Indeterminate, Apply surfaces a 'no eligible entries — acknowledge divergent entries' warning and does NOT claim resolutions applied (MED-2)"
    - "SYNC-14/15 surfaced to the operator: a persistent freshness banner renders Behind/Unverified from PullClassifyResult.Freshness, and each Confirmed/Indeterminate entry shows a divergence badge + a REQUIRED per-entry opt-in checkbox (D-01) whose acknowledged set is forwarded into ApplyAdoptionsAsync"
  artifacts:
    - path: "DeckFlow.Studio/ViewModels/PullFromProdCoordinator.cs"
      provides: "Staleness pre-check stage (linked-CTS bounded), PullClassifyResult freshness wrapper return, divergence stamping loop (body-absent => Indeterminate), acknowledged adopt-exclusion guard"
      contains: "GetBehindCountAsync"
    - path: "DeckFlow.Studio.Tests/ViewModels/PullFromProdCoordinatorTests.cs"
      provides: "Tests for warn-then-proceed staleness + fetch-timeout, divergence stamping (Clean/Confirmed/Indeterminate/body-absent), SYNC-13 field-authority regression, adopt defense-in-depth"
      contains: "BodyDivergenceStatus"
    - path: "DeckFlow.Studio/Pages/PullFromProd.razor"
      provides: "Freshness banner + Body-divergence badge column + per-entry opt-in checkbox"
      contains: "BodyDivergence"
    - path: "DeckFlow.Studio/Pages/PullFromProd.razor.cs"
      provides: "PullClassifyResult consumption, _freshness field, _divergenceOverrides set, adopt-filter change, empty-eligible-set guard, acknowledged-set forwarding"
      contains: "_divergenceOverrides"
    - path: "DeckFlow.Studio.Tests/PullFromProdPageTests.cs"
      provides: "Tests for banner render + opt-in gating of a divergent entry + empty-eligible-set warning"
      contains: "BodyDivergenceStatus"
  key_links:
    - from: "PullFromProdCoordinator.PullAndClassifyAsync"
      to: "IGitRepository.FetchAsync + GetBehindCountAsync"
      via: "new first stage 'check local checkout freshness' wrapped in a linked-CTS timeout + try/catch that warns, never throws (except page-token cancel)"
      pattern: "FetchAsync|GetBehindCountAsync"
    - from: "PullFromProdCoordinator divergence stamp"
      to: "ContentSiteIndexContentSignature.ComputeBodySha256"
      via: "the ONE hash surface, compared to ProdRow.BodySha256, guarded by ArtifactPathSafety.TryBuildContainedPath"
      pattern: "ComputeBodySha256"
    - from: "PullFromProdCoordinator.ApplyAdoptionsAsync"
      to: "UpsertContentColumnsOnlyAsync + SetApprovalStatusAsync (never SetVisibility/SetHidden)"
      via: "content-only upsert + approval mirror, is_visible/is_hidden untouched (D-02)"
      pattern: "UpsertContentColumnsOnlyAsync"
    - from: "PullFromProd.razor.cs PullAndClassifyAsync handler"
      to: "PullFromProdCoordinator.PullAndClassifyAsync -> PullClassifyResult"
      via: "_diffEntries = result.Entries.ToList(); _freshness = result.Freshness;"
      pattern: "result.Entries|_freshness"
    - from: "PullFromProd.razor.cs ApplyResolutionsAsync adopt filter"
      to: "_divergenceOverrides + ApplyAdoptionsAsync(acknowledgedDivergentKeys)"
      via: "exclude Confirmed/Indeterminate unless EntryKey in _divergenceOverrides; forward that set to the coordinator; refuse false-success on empty eligible set"
      pattern: "_divergenceOverrides"
---

<objective>
Harden the `PullFromProdCoordinator` AND surface both guards on the `PullFromProd` page — merged into one plan so every signature change lands atomically with its page call-site fix and the full solution stays 0/0 after every task commit (92-VALIDATION "clean build after every task commit"). This consumes the 92-01 Core seam and closes the operator-facing half of the phase.

1. **SYNC-14 staleness pre-check + freshness UI** — new first stage in `PullAndClassifyAsync` that resolves the branch, fetches `origin/{branch}`, computes behind-count, and WARNS-then-proceeds (behind vs unverified distinct, never hard-refuse per D-03). The fetch+behind work is bounded by a short linked-CTS timeout so a slow/hung network never blocks Pull (MED-1). Return type widens to a `PullClassifyResult` wrapper; the page consumes it and renders a persistent freshness banner. The return-type change and its page consumption land in the SAME task commit.
2. **SYNC-15 divergence stamping + operator opt-in** — post-classify pass stamps `BodyDivergence` via `ComputeBodySha256` (null prod hash ⇒ `Indeterminate`, D-01a; body ABSENT while a ProdRow exists ⇒ `Indeterminate`, D-02a). `ApplyAdoptionsAsync` gains a required `IReadOnlySet<string> acknowledgedDivergentKeys` and skips un-acknowledged Confirmed/Indeterminate entries (D-01 defense-in-depth). The page renders a divergence badge + required per-entry opt-in, forwards the acknowledged set, and refuses to claim success when the eligible adopt set is empty (MED-2). The signature change and its page call-site fix land in the SAME task commit.
3. **SYNC-13 field-authority** — ratify + regression-test `ApplyAdoptionsAsync`'s existing split (body←git, index←prod, approval←prod-mirror, is_visible/is_hidden preserved), including the now-hardened body-absent path. Tests only; no production change.

Purpose: keep every write-path decision in one testable coordinator with the page as thin UI glue, while never leaving the build red between commits (the whole reason 92-02 and the old 92-03 are merged).
Output: extended coordinator (freshness wrapper + timeout + stamping + exclusion), extended page (banner + badge + opt-in + empty-set guard), and their extended unit + page tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/92-pull-hardening/92-CONTEXT.md
@.planning/phases/92-pull-hardening/92-RESEARCH.md
@.planning/phases/92-pull-hardening/92-PATTERNS.md
@.planning/phases/92-pull-hardening/92-01-SUMMARY.md

<interfaces>
<!-- The coordinator + page being edited and their collaborators. Extracted — no exploration needed. -->

PullFromProdCoordinator.PullAndClassifyAsync (DeckFlow.Studio/ViewModels/PullFromProdCoordinator.cs:71-127):
  Task<IReadOnlyList<SyncDiffEntry>> PullAndClassifyAsync(string stagingRoot, IProgress<string> log, Action<string> onStage, CancellationToken)
  // line 92: var repoRoot = await _git.ResolveRepoRootAsync(StudioRepoLocator.ResolveStartDirectory(), ct)  — REUSE, do not re-resolve
  // lines 93-110: existing exists-check loop (calls ArtifactPathSafety.TryBuildContainedPath + File.Exists, emits per-row log) — KEEP SEPARATE (Pitfall 5); it produces availableSet by ArtifactPath
  // lines 119-121: the .Select(e => e with { ArtifactDownloaded = ... }) stamp — the pattern to EXTEND

PullFromProdCoordinator.ApplyAdoptionsAsync (lines 137-217) — already-correct field split to RATIFY:
  await _indexStore.UpsertContentColumnsOnlyAsync(prodRow, ct);                       // index cols ← prod
  await _indexStore.SetApprovalStatusAsync(keyType, keyValue, prodRow.ApprovalStatus, ct);  // approval ← prod
  // File.Copy(repoBody -> liveDest) body ← git tree; is_visible/is_hidden NEVER written
  // existing defensive skip at 150-153: if (entry.Kind == SyncDiffKind.LocalOnly || entry.ProdRow is null) continue;
  // R4 body-missing branch at 191-195: today it STILL upserts (row + approval, body not copied) — the new
  //   acknowledged-set guard sits ABOVE this so R4 only executes for an explicitly opted-in Indeterminate entry
  // OperationCanceledException-rethrow-first ordering at 199-202 — mirror it (plus a page-token `when` filter) in the freshness catch

New Core seam from 92-01 (available):
  Task IGitRepository.FetchAsync(string repoRoot, string remote, string branch, CancellationToken)  // explicit +refs/heads/... refspec, advances the tracking ref
  Task<int> IGitRepository.GetBehindCountAsync(string repoRoot, string remote, string branch, CancellationToken)
  string GetCurrentBranchAsync(string repoRoot, CancellationToken)   // existing
  enum BodyDivergenceStatus { NotApplicable, Clean, Confirmed, Indeterminate }  // Indeterminate covers null body_sha256 AND body-absent
  SyncDiffEntry.BodyDivergence { get; init; }

Hash + path helpers (reuse, do NOT re-hand-roll):
  string ContentSiteIndexContentSignature.ComputeBodySha256(string rawArtifactText)   // DeckFlow.Core.Content — already imported
  bool ArtifactPathSafety.TryBuildContainedPath(string root, string artifactPath, out string resolvedPath)
  ContentSiteIndexRow.BodySha256 { get; init; }  // string? — null on legacy/unbackfilled rows

FakeContentSiteIndexStore capture surface for SYNC-13 assertions (DeckFlow.Studio.Tests/TestDoubles/FakeContentSiteIndexStore.cs):
  List<string> UpsertMethodCalls           // records "UpsertContentColumnsOnlyAsync"; the visibility-writing paths UpsertRowAsync / UpsertRowPreservingVisibilityAsync THROW + record their names
  List<(string Type, string Value, string Status)> SingleApprovalCalls
  // NOTE: UpsertContentColumnsOnlyAsync APPENDS (Rows.Add) rather than replace-by-key — so a visibility-preservation
  //   test must assert the ABSENCE of any visibility-writing upsert call (UpsertRowAsync / UpsertRowPreservingVisibilityAsync),
  //   NOT inspect a stored row's is_visible (which can read a stale appended row and pass falsely — LOW finding)

PullFromProd.razor.cs apply handler (DeckFlow.Studio/Pages/PullFromProd.razor.cs):
  lines 132-136: `var entries = await Task.Run(() => Coordinator.PullAndClassifyAsync(...)); _diffEntries = entries.ToList();`  — CHANGE to consume PullClassifyResult
  lines 106-115: pull reset block (`_diffEntries = new(); _resolutions.Clear();` etc.) — reset `_freshness` + `_divergenceOverrides` here
  lines 188-192: adopt-set filter (`GetResolution(e)==AdoptProd && Kind!=LocalOnly && ProdRow is not null`) — EXTEND with the divergence exclusion
  lines 207-212: `_applySuccess = results.All(r => r.Success)` — vacuously TRUE on empty results; add the empty-eligible-set guard BEFORE dispatch (MED-2)
  line 204: `Coordinator.ApplyAdoptionsAsync(adoptEntries, _stagingRoot, _dataRoot, progress, Cts.Token)` — add the acknowledged set arg
  line 237: `private static string EntryKey(SyncDiffEntry entry) => $"{entry.NaturalKeyType}:{entry.NaturalKeyValue}";`  — REUSE for _divergenceOverrides
  line 265: `internal Task InvokePullApplyForTest() => ApplyResolutionsAsync();`  — existing apply-side test seam

Page markup touch points (DeckFlow.Studio/Pages/PullFromProd.razor):
  lines 42-45: existing "READ-ONLY toward production" persistent banner — the precedent for a persistent freshness banner
  lines 124-128: diff table headers (Title, Natural key, Difference, Artifact, Resolution) — add a "Body" <th>
  lines 138-158: `@switch (entry.Kind)` badge cell — add a sibling `@switch (entry.BodyDivergence)` cell
  lines 173-198: per-entry Resolution radios — add the conditional opt-in checkbox
  Persistent-banner analog: DeckFlow.Studio/Pages/Reconcile.razor:113-124 (seed-unavailable warning card).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Staleness pre-check (linked-CTS bounded) + PullClassifyResult return + freshness banner (SYNC-14, coordinator+page atomic)</name>
  <files>DeckFlow.Studio/ViewModels/PullFromProdCoordinator.cs, DeckFlow.Studio.Tests/ViewModels/PullFromProdCoordinatorTests.cs, DeckFlow.Studio/Pages/PullFromProd.razor.cs, DeckFlow.Studio/Pages/PullFromProd.razor, DeckFlow.Studio.Tests/PullFromProdPageTests.cs</files>
  <read_first>
    - DeckFlow.Studio/ViewModels/PullFromProdCoordinator.cs (full — the file being edited; note PullPaths/PullApplyRowResult sibling records at 221-233 as the home for the new records; OCE-rethrow-first at 199-202)
    - DeckFlow.Studio.Tests/ViewModels/PullFromProdCoordinatorTests.cs (Build helper + FakeGitRepository injection at 71-90)
    - DeckFlow.Studio/ViewModels/DirectPushCoordinator.cs:429-452 (the no-fetch fail-CLOSED precedent this phase deliberately does NOT copy — Pull is warn-then-proceed)
    - DeckFlow.Studio/Pages/PullFromProd.razor.cs (PullAndClassifyAsync handler 99-168; field block 33-63; reset block 106-115)
    - DeckFlow.Studio/Pages/PullFromProd.razor (READ-ONLY banner 42-45 as the persistent-card precedent)
    - DeckFlow.Studio/Pages/Reconcile.razor:113-124 (seed-unavailable warning card shape)
    - DeckFlow.Studio.Tests/PullFromProdPageTests.cs (RenderPull harness + code-behind consumption)
    - .planning/phases/92-pull-hardening/92-RESEARCH.md (Pattern 2 + Pitfall 3 distinct-copy rule; Open Question 2 return-type)
  </read_first>
  <behavior>
    - GetBehindCountAsync returns > 0 → a WARNING progress line naming the behind-count + branch is emitted; the pull PROCEEDS and still returns entries; Freshness.Kind == Behind.
    - FetchAsync (or GetBehindCountAsync) throws GitCommandException → a DISTINCT "could not verify freshness" progress line is emitted; the pull PROCEEDS and still returns entries; Freshness.Kind == Unverified.
    - The fetch/behind work exceeds the short linked-CTS timeout (slow DNS/TCP) → the timeout fires an OperationCanceledException whose token is the LINKED (not page) token; it is caught, Freshness.Kind == Unverified, the DISTINCT "could not verify freshness" line is emitted, and the pull PROCEEDS (never blocks) — MED-1.
    - behindCount == 0, fetch OK → no warning line; Freshness.Kind == Fresh.
    - A genuine PAGE-token cancellation (operator cancel) propagates (never swallowed by the freshness catch).
    - Page: Freshness.Kind == Behind renders a persistent behind-warning banner (count + branch); Unverified renders a DISTINCT "could not verify freshness" banner; Fresh renders no banner. No exception detail rendered (D-07).
  </behavior>
  <action>
    COORDINATOR — Introduce freshness result types as sibling records in PullFromProdCoordinator.cs (next to PullPaths/PullApplyRowResult): `public enum PullFreshnessKind { Fresh, Behind, Unverified }` and `public sealed record PullFreshnessStatus(PullFreshnessKind Kind, int BehindCount, string Branch)`. Keep the tri-state distinct per Pitfall 3 (Behind vs Unverified are NOT the same). Widen `PullAndClassifyAsync`'s return type from `Task<IReadOnlyList<SyncDiffEntry>>` to `Task<PullClassifyResult>` where `public sealed record PullClassifyResult(IReadOnlyList<SyncDiffEntry> Entries, PullFreshnessStatus Freshness)` (also a sibling record) — the lower-risk wrapper (RESEARCH Open Question 2).

    Add a NEW first stage before "read production content_site_index": `onStage("check local checkout freshness");`. Resolve `repoRoot` ONCE up front (reuse the existing resolution for both freshness and the body loop — do NOT resolve twice). Then BOUND the git work with a linked-CTS timeout (MED-1): `using var freshnessCts = CancellationTokenSource.CreateLinkedTokenSource(cancellationToken); freshnessCts.CancelAfter(TimeSpan.FromSeconds(5));`. Inside a try: get the branch via `_git.GetCurrentBranchAsync(repoRoot, freshnessCts.Token)`, `await _git.FetchAsync(repoRoot, "origin", branch, freshnessCts.Token)`, then `var behindCount = await _git.GetBehindCountAsync(repoRoot, "origin", branch, freshnessCts.Token)`. If `behindCount > 0`, `log.Report(...)` a WARNING naming the count + `origin/{branch}` + "consider running 'git pull' before adopting. Proceeding with the current git tree." and set freshness to `Behind`; otherwise `Fresh`.

    Catch ordering (preserves the OCE-rethrow-first idiom, extended with a page-token filter so the TIMEOUT OCE is treated as Unverified rather than propagated):
    - `catch (OperationCanceledException) when (cancellationToken.IsCancellationRequested) { throw; }` — genuine page/operator cancel propagates.
    - `catch (OperationCanceledException) { /* freshness timeout: linked CTS fired, page token NOT cancelled */ }` — emit the DISTINCT "Could not verify checkout freshness (fetch timed out — offline, VPN, or slow network). Proceeding with the local git tree as-is." and set freshness `Unverified`.
    - `catch (GitCommandException ex) { }` — emit the SAME distinct "Could not verify checkout freshness (fetch failed — offline, VPN, or auth). Proceeding with the local git tree as-is." line, set freshness `Unverified`, and log full `ex` detail via `_logger` (D-07 sanitized UI).
    Default freshness is `Fresh` when behindCount==0. Return `new PullClassifyResult(entries, freshness)` at the end. Use the fixed remote literal `"origin"`.

    PAGE (atomic with the return-type change so the solution stays 0/0) — In PullFromProd.razor.cs add `private PullFreshnessStatus? _freshness;` in the Stage-1 field group (near `_diffEntries`). Change the pull handler (132-136) so the `Task.Run` result is a `PullClassifyResult`: set `_diffEntries = result.Entries.ToList();` and `_freshness = result.Freshness;`. Reset `_freshness = null;` in the pull reset block (106-115, alongside `_diffEntries = new();`). Add no git/hash logic — the page only reads the coordinator's result. In PullFromProd.razor add a persistent banner (rendered when `_freshness is not null && _freshness.Kind != PullFreshnessKind.Fresh`) styled like Reconcile.razor:113-124 (`class="card border-warning"`, `data-testid="freshness-banner"`), placed near the READ-ONLY banner / above the diff table. Branch copy on `_freshness.Kind`: Behind → "Local checkout is @_freshness.BehindCount commit(s) behind origin/@_freshness.Branch — run 'git pull' before adopting"; Unverified → "Could not verify checkout freshness (fetch failed or timed out) — proceeding against the local git tree as-is." Never render exception detail.

    TESTS — In PullFromProdCoordinatorTests.cs add cases via `Build(...)` passing `FakeGitRepository { CannedRepoRoot = _repoRoot, CannedBehindCount = N, ThrowOnFetch = ... }`: behind>0 → assert a progress line contains the behind-count, `result.Freshness.Kind == Behind`, `result.Entries` still populated; `ThrowOnFetch = new GitCommandException(...)` → `result.Freshness.Kind == Unverified`, a distinct "could not verify" line, Entries still populated (pull not blocked); `ThrowOnFetch = new OperationCanceledException()` with the page token NOT cancelled (simulates the freshness-timeout branch) → `result.Freshness.Kind == Unverified`, Entries still populated, pull NOT blocked (MED-1); clean → `Freshness.Kind == Fresh`, no warning line. Update every existing test that read `PullAndClassifyAsync`'s old list to read `.Entries`. In PullFromProdPageTests.cs add a banner case: after a Behind pull the render contains `data-testid="freshness-banner"` with the behind copy; after an Unverified pull the distinct copy renders; after Fresh no banner renders.

    Preserve LF; changed-lines-only formatting; keep the OperationCanceledException-rethrow-first ordering (page-token `when` filter first), matching ApplyAdoptionsAsync:199-202.
  </action>
  <verify>
    <automated>dotnet.exe build DeckFlow.sln (0/0) then dotnet.exe test DeckFlow.Studio.Tests --filter "FullyQualifiedName~PullFromProd"</automated>
  </verify>
  <acceptance_criteria>
    - PullAndClassifyAsync returns PullClassifyResult(Entries, Freshness); FetchAsync + GetBehindCountAsync are called with remote "origin" and the current branch, through a linked-CTS whose `CancelAfter` bounds the network work (grep: `CreateLinkedTokenSource` + `CancelAfter` around the fetch/behind calls).
    - behind>0 emits a warning line and proceeds (Entries non-empty); GitCommandException emits a DISTINCT line and proceeds; a freshness-timeout OperationCanceledException (page token not cancelled) ALSO yields Unverified + proceeds; the two copies (Behind vs Unverified) differ.
    - No hard-refuse path exists (grep: the freshness catch never rethrows GitCommandException; only an OperationCanceledException guarded by `when (cancellationToken.IsCancellationRequested)` rethrows).
    - repoRoot resolved once (grep: a single ResolveRepoRootAsync call in PullAndClassifyAsync).
    - The page reads result.Entries + result.Freshness (grep: `_freshness =`) and renders `data-testid="freshness-banner"` only for Behind/Unverified with distinct copy; no ex.Message rendered.
    - A coordinator test proves the fetch-timeout (OCE, page token not cancelled) yields Unverified and still returns Entries (MED-1).
    - `dotnet.exe build DeckFlow.sln` is 0/0 (page compiles against the widened return type in this same commit); the PullFromProd test filter passes.
  </acceptance_criteria>
  <done>Staleness is the first stage, warns distinctly (behind vs unverified), is bounded by a short timeout so a hung fetch never blocks Pull, and never hard-refuses; the freshness result flows to a persistent banner; the solution builds 0/0 after this commit.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Divergence stamping (body-absent => Indeterminate) + adopt defense-in-depth + empty-set guard + badge/opt-in UI (SYNC-15, coordinator+page atomic)</name>
  <files>DeckFlow.Studio/ViewModels/PullFromProdCoordinator.cs, DeckFlow.Studio.Tests/ViewModels/PullFromProdCoordinatorTests.cs, DeckFlow.Studio/Pages/PullFromProd.razor, DeckFlow.Studio/Pages/PullFromProd.razor.cs, DeckFlow.Studio.Tests/PullFromProdPageTests.cs</files>
  <read_first>
    - DeckFlow.Studio/ViewModels/PullFromProdCoordinator.cs (the .Select stamp at 119-121; the exists-check loop at 93-110 that builds availableSet; ApplyAdoptionsAsync defensive skip at 150-153; the R4 body-missing branch at 191-195)
    - DeckFlow.Core/Content/ContentKbReconcileClassifier.cs:101-109 (the P91 null-skip analog this phase INVERTS — do NOT copy the `!IsNullOrEmpty` guard)
    - DeckFlow.Core/Content/ContentSiteIndexContentSignature.cs:131 (ComputeBodySha256 signature)
    - DeckFlow.Studio/Pages/PullFromProd.razor (Kind badge switch 138-158; Artifact cell 159-172; Resolution radios 173-198; headers 124-128)
    - DeckFlow.Studio/Pages/PullFromProd.razor.cs (adopt-set filter 188-192; success reporting `results.All` 207-212; ApplyAdoptionsAsync call 204; EntryKey 237; InvokePullApplyForTest 265)
    - DeckFlow.Studio.Tests/PullFromProdPageTests.cs (RenderPull harness + InvokePullApplyForTest usage)
    - .planning/phases/92-pull-hardening/92-PATTERNS.md (divergence-stamp + badge/opt-in recommended shapes) + 92-RESEARCH.md Pitfall 1/4 + 92-CONTEXT.md D-01/D-02a
  </read_first>
  <behavior>
    - Entry with resolved git body + non-null ProdRow + prod BodySha256 that MATCHES the git body hash → BodyDivergence == Clean.
    - Same but prod BodySha256 DIFFERS → Confirmed.
    - Same but prod BodySha256 is null → Indeterminate (NOT skipped, NOT Clean — the D-01a inverse of P91).
    - Entry with a non-null ProdRow but NO resolved git body (downloaded==false) OR an unreadable/path-unsafe body → Indeterminate (body-less: prod's body_sha256 cannot be shown to match a local body — D-02a). This HARDENS the prior behavior where such rows silently adopted.
    - Entry LocalOnly (no ProdRow) → NotApplicable.
    - ApplyAdoptionsAsync: a Confirmed/Indeterminate entry whose key is absent from acknowledgedDivergentKeys is skipped (not upserted); a Clean/NotApplicable entry adopts normally. An ACKNOWLEDGED body-less Indeterminate entry reaches the R4 branch and upserts the row without copying a body (operator explicitly accepted the incoherence).
    - Page: a Confirmed entry renders a danger "divergent" badge + an unchecked opt-in checkbox and is EXCLUDED from the adopt set while unchecked; checking the opt-in adds its key to _divergenceOverrides so it enters the adopt set and ApplyAdoptionsAsync receives its key. Indeterminate behaves identically. Clean/NotApplicable need no opt-in.
    - Page (MED-2): if the operator selected AdoptProd on >=1 entry but EVERY such entry was excluded as un-acknowledged Confirmed/Indeterminate (eligible adopt set empty), Apply does NOT report success — it surfaces a "no eligible entries — acknowledge divergent entries to adopt them" warning and dispatches nothing.
  </behavior>
  <action>
    COORDINATOR — Replace the existing `.Select(e => e with { ArtifactDownloaded = availableSet.Contains(e.ArtifactPath) })` stamp (119-121) with a `.Select(e => { ... })` block computing both flags. Compute `downloaded = availableSet.Contains(e.ArtifactPath)`; initialize `divergence = BodyDivergenceStatus.NotApplicable`. Then:
    - If `e.ProdRow is null` leave `divergence = NotApplicable` (LocalOnly — nothing to adopt).
    - Else if `downloaded && ArtifactPathSafety.TryBuildContainedPath(repoRoot, e.ArtifactPath, out var repoBody) && File.Exists(repoBody)`: read + hash the body inside a `try` — `var bodyText = File.ReadAllText(repoBody)`, `var computedHash = ContentSiteIndexContentSignature.ComputeBodySha256(bodyText)`, then `divergence = e.ProdRow.BodySha256 is null ? BodyDivergenceStatus.Indeterminate : (string.Equals(e.ProdRow.BodySha256, computedHash, StringComparison.Ordinal) ? BodyDivergenceStatus.Clean : BodyDivergenceStatus.Confirmed)`. Wrap that read+hash in `catch (Exception ex) when (ex is IOException or UnauthorizedAccessException)` → `_logger.LogWarning(ex, "Unreadable git body for {ArtifactPath}; stamping Indeterminate.", e.ArtifactPath)` (sanitized — never surface ex.Message to the UI, D-07) and `divergence = BodyDivergenceStatus.Indeterminate` (an exists-but-unreadable body must NOT fault the whole Pull — same fail-safe posture as the P89 ContentBodyHashBackfill read guard `61b87f36`; do NOT catch OperationCanceledException here).
    - Else (ProdRow present but git body ABSENT/path-unsafe): `divergence = BodyDivergenceStatus.Indeterminate` with a `// Why:` comment — body-less prod row: prod's body_sha256 cannot be shown to match a local body, so adopting would leave an incoherent index (D-02a); surface + opt-in only, never silent-adopt (HARDENS the old R4 auto-adopt).
    Return `e with { ArtifactDownloaded = downloaded, BodyDivergence = divergence }`. Compute divergence for EVERY qualifying kind — do NOT gate on `Kind == Diverged` (Pitfall 4). Reuse the `repoRoot` resolved in Task 1; keep this pass SEPARATE from the exists-check loop at 93-110 (Pitfall 5).

    Add the D-01 defense-in-depth: change `ApplyAdoptionsAsync`'s signature to accept `IReadOnlySet<string> acknowledgedDivergentKeys` (keyed `$"{NaturalKeyType}:{NaturalKeyValue}"` to match the page's EntryKey). After the existing LocalOnly/ProdRow-null skip (150-153), add: if `entry.BodyDivergence is BodyDivergenceStatus.Confirmed or BodyDivergenceStatus.Indeterminate` and the entry's key is NOT in `acknowledgedDivergentKeys`, record a skipped `PullApplyRowResult` (Success=true, Action "Skipped (divergent, not acknowledged)", sanitized note) and `continue` — never upsert. This guard sits ABOVE the existing R4 body-missing branch (191-195), so a body-less Indeterminate row now upserts ONLY when explicitly acknowledged. Do not otherwise change the field-authority body of the loop.

    PAGE (atomic with the ApplyAdoptionsAsync signature change so the solution stays 0/0) — In PullFromProd.razor.cs add `private readonly HashSet<string> _divergenceOverrides = new(StringComparer.Ordinal);` near `_resolutions`; clear it in the pull reset block wherever `_resolutions.Clear()` runs. Add `private bool IsDivergenceAck(SyncDiffEntry e) => _divergenceOverrides.Contains(EntryKey(e));` and `private void SetDivergenceAck(SyncDiffEntry e, bool ack)` (add/remove `EntryKey(e)`). In ApplyResolutionsAsync compute the SELECTED-adopt set (`GetResolution(e)==Resolution.AdoptProd && e.Kind != SyncDiffKind.LocalOnly && e.ProdRow is not null`) and the ELIGIBLE-adopt set = the selected set FURTHER filtered by `(e.BodyDivergence is BodyDivergenceStatus.NotApplicable or BodyDivergenceStatus.Clean || _divergenceOverrides.Contains(EntryKey(e)))`. **MED-2 guard**: if `selected.Any() && eligible.Count == 0`, do NOT dispatch and do NOT set `_applySuccess = true`; set the warning message "No eligible entries to apply — check the 'adopt anyway' opt-in on divergent/indeterminate entries to adopt them. Production was not modified.", reset the in-flight flags, and return. Otherwise pass `eligible` to `Coordinator.ApplyAdoptionsAsync(...)` and pass `_divergenceOverrides` as the `IReadOnlySet<string> acknowledgedDivergentKeys` arg at 204.

    In PullFromProd.razor add a `<th>Body</th>` header (after "Artifact") and a matching `<td>` per row with `@switch (entry.BodyDivergence)` — Confirmed → `<span class="badge bg-danger">divergent</span>`, Indeterminate → `<span class="badge bg-warning text-dark">unverified body</span>`, Clean → `<span class="badge bg-success">matches</span>`, NotApplicable → nothing. In the Resolution cell, when `entry.BodyDivergence is BodyDivergenceStatus.Confirmed or BodyDivergenceStatus.Indeterminate`, render a REQUIRED opt-in checkbox (`data-testid="divergence-optin-@key"`) bound to `IsDivergenceAck(entry)` / `SetDivergenceAck(entry, ...)` with copy "I understand this body diverges from prod — adopt anyway", so the operator must independently check it beyond the "adopt prod" radio (D-01). Add no hashing/git/store logic to the page.

    TESTS — In PullFromProdCoordinatorTests.cs (write repo body files under `_repoRoot` with content whose ComputeBodySha256 matches/mismatches the prod row's BodySha256): matching hash → entry BodyDivergence == Clean; mismatch → Confirmed; prod BodySha256 null → Indeterminate (assert NOT Clean and NOT dropped from the diff list); a NON-downloaded (no git body) entry with a non-null ProdRow → Indeterminate (assert NOT NotApplicable, NOT dropped — the D-02a body-less case); if practical, an exists-but-unreadable body (e.g. a directory placed at the body path so File.ReadAllText throws IOException) → Indeterminate and the Pull does NOT fault (the read-guard case); a ProdNewer-kind mismatch shows Confirmed (kind-independent); ApplyAdoptionsAsync with a Confirmed entry + EMPTY acknowledged set → `UpsertMethodCalls` excludes it (skipped); with its key in the set → it IS upserted. Update all existing ApplyAdoptionsAsync call sites in the tests to pass an acknowledged set. In PullFromProdPageTests.cs via `RenderPull(...)`: a prod row whose BodySha256 mismatches the written repo body (→ Confirmed) renders the `divergent` badge / `divergence-optin` control, and `InvokePullApplyForTest()` with the opt-in UNCHECKED does NOT adopt that entry (key absent from UpsertMethodCalls / apply results) while checking the opt-in first DOES adopt it; a null-BodySha256 (→ Indeterminate) case asserts the same exclusion-until-opt-in; a MED-2 case where the ONLY AdoptProd-selected entry is a Confirmed one left un-acknowledged asserts Apply surfaces the "no eligible entries" warning, does NOT report success, and `UpsertMethodCalls` is empty.

    Preserve LF; changed-lines-only.
  </action>
  <verify>
    <automated>dotnet.exe build DeckFlow.sln (0/0) then dotnet.exe test DeckFlow.Studio.Tests --filter "FullyQualifiedName~PullFromProd"</automated>
  </verify>
  <acceptance_criteria>
    - Divergence stamp uses ComputeBodySha256 (grep: the ONE hash surface) guarded by ArtifactPathSafety.TryBuildContainedPath; computed regardless of SyncDiffKind (a ProdNewer mismatch shows Confirmed).
    - Null prod BodySha256 yields Indeterminate (a test asserts null-hash is NOT Clean and NOT dropped); a non-null ProdRow with NO resolved git body ALSO yields Indeterminate (a test asserts body-absent is NOT NotApplicable and NOT auto-adopted) — the explicit inverse of ContentKbReconcileClassifier's null-skip and the D-02a hardening. An exists-but-unreadable body (IOException/UnauthorizedAccessException on read/hash) is caught, logged sanitized (D-07), and stamped Indeterminate — it never faults the whole Pull.
    - ApplyAdoptionsAsync signature has `IReadOnlySet<string> acknowledgedDivergentKeys`; a Confirmed/Indeterminate entry absent from it is skipped (UpsertMethodCalls excludes it) and the guard sits above the R4 body-missing branch; present → adopted.
    - The page renders a Body-divergence badge (danger=Confirmed, warning=Indeterminate) + a per-entry opt-in for Confirmed/Indeterminate; the adopt filter excludes them unless their EntryKey is in `_divergenceOverrides`, which is passed to ApplyAdoptionsAsync.
    - MED-2: when the selected-adopt set is non-empty but the eligible set is empty, Apply reports a "no eligible entries" warning, does NOT set `_applySuccess = true`, and dispatches nothing (a page test asserts UpsertMethodCalls is empty and success is not claimed).
    - A page test proves a Confirmed entry is NOT adopted opt-in-unchecked and IS once checked; an Indeterminate (null hash) entry behaves identically; Clean/NotApplicable adopt without opt-in.
    - `dotnet.exe build DeckFlow.sln` is 0/0 (page compiles against the new param in this same commit); the PullFromProd test filter passes.
  </acceptance_criteria>
  <done>Every comparable entry is stamped Clean/Confirmed/Indeterminate (null hash OR body-absent ⇒ Indeterminate); divergent/indeterminate bodies are visibly flagged and require explicit per-entry acknowledgement, the coordinator independently refuses to adopt un-acknowledged ones, and the page never falsely reports success on an empty eligible set; the solution builds 0/0 after this commit.</done>
</task>

<task type="auto">
  <name>Task 3: Ratify + regression-lock the field-authority split incl. hardened body-absent path (SYNC-13, D-02) — tests only</name>
  <files>DeckFlow.Studio.Tests/ViewModels/PullFromProdCoordinatorTests.cs</files>
  <read_first>
    - DeckFlow.Studio/ViewModels/PullFromProdCoordinator.cs:137-217 (ApplyAdoptionsAsync — the behavior being locked; note it is UNCHANGED here except the Task-2 signature/guard; R4 body-missing branch 191-195 now only runs for acknowledged entries)
    - DeckFlow.Studio.Tests/TestDoubles/FakeContentSiteIndexStore.cs (UpsertMethodCalls, SingleApprovalCalls; UpsertContentColumnsOnlyAsync APPENDS rather than replace-by-key; UpsertRowAsync / UpsertRowPreservingVisibilityAsync are the visibility-writing paths that THROW + record)
    - DeckFlow.Studio.Tests/ViewModels/PullFromProdCoordinatorTests.cs (AdoptEntry helper + repo-body-write convention)
    - .planning/phases/92-pull-hardening/92-CONTEXT.md (D-02 / D-02a field authority)
  </read_first>
  <behavior>
    - Adopting a Clean/NotApplicable prod entry calls UpsertContentColumnsOnlyAsync (index columns ← prod) exactly once for that entry.
    - Adopting calls SetApprovalStatusAsync with the PROD row's ApprovalStatus value (approval ← prod-mirror), keyed by the entry's NaturalKeyType/NaturalKeyValue.
    - Adopting NEVER invokes a visibility-writing upsert (is_visible/is_hidden preserved-local, D-02).
    - The git-tree body is copied into the live tree (body ← git) when present.
    - A missing git body classifies the entry Indeterminate → it is NOT default-adopted (skipped with an empty acknowledged set); it upserts the row (body not copied, partial-repo path) ONLY when its key is in the acknowledged set — the D-02a hardening.
  </behavior>
  <action>
    Add regression tests (no production change — this task RATIFIES the already-correct ApplyAdoptionsAsync) using the existing `Build`/`AdoptEntry` helpers and `FakeContentSiteIndexStore`. Fixtures use Clean/NotApplicable entries unless noted; pass the Task-2 `acknowledgedDivergentKeys` arg (empty for the Clean/NotApplicable cases):
    - `Adopt_UpsertsContentColumnsOnly_NotFullRow`: assert `localStore.UpsertMethodCalls` contains exactly `"UpsertContentColumnsOnlyAsync"` for the adopted entry and NOT any full-row upsert method name.
    - `Adopt_MirrorsProdApprovalStatus`: seed the prod row with a specific ApprovalStatus (e.g. "approved") and assert `localStore.SingleApprovalCalls` contains `(keyType, keyValue, "approved")` matching the entry's natural key.
    - `Adopt_PreservesLocalVisibility`: adopt a Clean prod entry and assert `localStore.UpsertMethodCalls` contains NO visibility-writing upsert call (`"UpsertRowAsync"` / `"UpsertRowPreservingVisibilityAsync"` absent) — the robust invariant, because the fake APPENDS on content-only upsert so inspecting a stored row's is_visible can read a stale appended row and pass falsely (LOW finding). Additionally assert the newly-upserted (content-only) row's content columns equal the prod row's, confirming only content columns crossed.
    - `Adopt_CopiesGitBody_WhenPresent`: with a repo body file written under `_repoRoot` (Clean entry), adopt and assert the live-tree destination file exists / body copied.
    - `Adopt_MissingBody_IsExcludedUnlessAcknowledged`: construct a body-less entry stamped `with { BodyDivergence = BodyDivergenceStatus.Indeterminate }` (no repo body file); with an EMPTY acknowledged set assert it is skipped (`UpsertMethodCalls` excludes it, no live-tree file written); with its key in the acknowledged set assert the row IS upserted (`UpsertContentColumnsOnlyAsync` recorded) but the body is NOT copied (note wording "not copied"). This locks the D-02a hardening in place of the old "missing body still upserts" auto-adopt.

    Preserve LF; changed-lines-only. Follow the file's existing xUnit `[Fact]` naming (Method_Scenario_ExpectedResult).
  </action>
  <verify>
    <automated>dotnet.exe build DeckFlow.sln (0/0) then dotnet.exe test DeckFlow.Studio.Tests --filter "FullyQualifiedName~PullFromProdCoordinator"</automated>
  </verify>
  <acceptance_criteria>
    - A test asserts UpsertContentColumnsOnlyAsync is the only upsert method invoked on adopt (index ← prod, content-only).
    - A test asserts SetApprovalStatusAsync is called with the prod row's ApprovalStatus (approval ← prod-mirror) under the entry's natural key.
    - A test asserts NO visibility-writing upsert (UpsertRowAsync / UpsertRowPreservingVisibilityAsync) is called on adopt (is_visible/is_hidden preserved-local, D-02) — the append-safe assertion, not a stored-row is_visible read (LOW).
    - A test asserts the git-tree body is copied on adopt when present (body ← git).
    - A test asserts a missing-body Indeterminate entry is EXCLUDED with an empty acknowledged set and only upserts (body not copied) when acknowledged (D-02a hardening).
    - Coordinator test filter passes; full solution build 0/0.
  </acceptance_criteria>
  <done>D-02's field-authority split is regression-locked: body←git, index←prod, approval←prod-mirror, visibility preserved-local, and the body-absent path is opt-in-only (D-02a); the solution builds 0/0 after this commit.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Studio host → `git` CLI | branch name from GetCurrentBranchAsync flows into fetch/rev-list args |
| prod `artifact_path` → local file read | untrusted path used for the divergence-check File.ReadAllText |
| Studio → prod Postgres | read-only SELECT (IProdContentReader has no write method) |
| operator (browser) → Blazor Server page | operator toggles per-entry opt-in; page forwards the acknowledged set to the coordinator |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-92-02 | Tampering / Information Disclosure | divergence-check File.ReadAllText(repoBody) | mitigate | Path goes through `ArtifactPathSafety.TryBuildContainedPath(repoRoot, artifactPath, ...)` — the same guard the exists-check already applies; a path-unsafe/unreadable body falls to Indeterminate (surfaced, opt-in), never an unguarded read |
| T-92-03 | Information Disclosure | freshness-check catch (GitCommandException / timeout OCE) + banner copy | mitigate | Log full detail via `_logger`; surface only sanitized "could not verify freshness" copy (behind-count/branch or generic) — never `ex.Message` (D-07) |
| T-92-04 | Tampering | adopting a body-vs-index divergent OR body-less row without operator awareness | mitigate | Confirmed/Indeterminate excluded from the adopt set unless the operator checks a SEPARATE per-entry opt-in (D-01); coordinator re-enforces via the acknowledged set (defense-in-depth); page refuses false-success on an empty eligible set (MED-2) |
| T-92-05 | Denial of Service | PullAndClassifyAsync freshness fetch (network op) | mitigate | Fetch + behind-count bounded by a linked-CTS `CancelAfter` timeout → Unverified + proceed on a hang; never blocks the Pull (MED-1) |
| T-92-06 | Elevation / clobber | ApplyAdoptionsAsync writing is_visible/is_hidden | mitigate | Content-only upsert (UpsertContentColumnsOnlyAsync) never touches visibility; regression-locked by Task 3 (D-02) via absence-of-visibility-writer assertion |
| T-92-SC | Tampering | npm/pip/cargo installs | accept | No package installs this phase (RESEARCH §Standard Stack); this plan edits only coordinator/page/tests |
</threat_model>

<verification>
- `dotnet.exe build DeckFlow.sln` → 0/0 after EVERY task commit — coordinator signature changes and their PullFromProd.razor(.cs) call-site fixes land in the same commit (Tasks 1 & 2), so the build is never red between commits (92-VALIDATION contract).
- `dotnet.exe test DeckFlow.Studio.Tests --filter "FullyQualifiedName~PullFromProd"` → green (coordinator + page tests).
- Manual pre-flip gate (VALIDATION Manual-Only): live `/pull` against real prod — confirm the behind-warning banner fires on a stale checkout (and that a previously-stale `origin/{branch}` is advanced by the fetch before the behind-count reads, HIGH-1), a divergent/body-less entry is excluded until opt-in, an empty eligible set does not claim success, and no prod write occurs. Recorded for the operator; not blocking for this plan.
- EOL clean (LF); changed-lines format gate clean.
</verification>

<success_criteria>
- Staleness pre-check warns-then-proceeds (behind vs unverified distinct), is bounded by a short timeout, and never hard-refuses; a persistent freshness banner surfaces it (SYNC-14).
- Every comparable entry stamped Clean/Confirmed/Indeterminate; null prod hash OR body-absent ⇒ Indeterminate (inverse of P91, D-02a); a per-entry divergence badge + required opt-in prevents silent adoption, and the page refuses false-success on an empty eligible set (SYNC-15/MED-2).
- ApplyAdoptionsAsync excludes un-acknowledged divergent/indeterminate entries (defense-in-depth) and its field split (body←git, index←prod, approval←prod, visibility preserved) is regression-locked (SYNC-13/D-02).
- The solution builds 0/0 after every task commit; the page stays thin UI glue forwarding the acknowledged set.
</success_criteria>

<output>
Create `.planning/phases/92-pull-hardening/92-02-SUMMARY.md` when done. Record the exact `PullClassifyResult`/`PullFreshnessStatus` shape, the freshness-timeout duration, and the `ApplyAdoptionsAsync` signature change.
</output>
