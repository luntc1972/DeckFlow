---
phase: 20-content-kb-ingestion-transcription-local
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - DeckFlow.Core/Content/IContentVideoStore.cs
  - DeckFlow.Core/Content/ContentVideoStore.cs
  - DeckFlow.Core/Content/IContentSourceStore.cs
  - DeckFlow.Core/Content/ContentSourceStore.cs
  - DeckFlow.CLI/Program.cs
  - DeckFlow.CLI/CommandRunners.cs
  - DeckFlow.Core.Tests/ContentVideoStoreTests.cs
autonomous: true
requirements: [KB-03, KB-04, KB-05]
must_haves:
  truths:
    - "A pending content_videos row can be transitioned to captions | whisper | failed | skipped_over_cap by id"
    - "A user can seed a content source from the CLI: content-source-add --url --type --name writes a row to local content_sources"
    - "The CLI-computed source slug is URL-safe and derived from --name, displayed before insert"
    - "A re-listed source is returned by ContentSourceStore.ListEnabledSourcesAsync after add"
  artifacts:
    - path: "DeckFlow.Core/Content/ContentVideoStore.cs"
      provides: "UpdateTranscriptStatusAsync(long videoId, string status) UPDATE of transcript_status"
      contains: "UpdateTranscriptStatusAsync"
    - path: "DeckFlow.CLI/CommandRunners.cs"
      provides: "RunContentSourceAddAsync seeding wrapper over ContentSourceStore.InsertSourceAsync"
      contains: "RunContentSourceAddAsync"
    - path: "DeckFlow.Core.Tests/ContentVideoStoreTests.cs"
      provides: "xUnit coverage of UpdateTranscriptStatusAsync transitions + invalid status guard"
      contains: "UpdateTranscriptStatusAsync"
  key_links:
    - from: "DeckFlow.CLI/CommandRunners.cs"
      to: "DeckFlow.Core/Content/ContentSourceStore.cs"
      via: "InsertSourceAsync(slug, name, type, url)"
      pattern: "InsertSourceAsync"
    - from: "DeckFlow.Core/Content/ContentVideoStore.cs"
      to: "content_videos.transcript_status"
      via: "UPDATE ... SET transcript_status"
      pattern: "UPDATE content_videos"
---

<objective>
Close the Wave-0 store gap and seed the harvester's source list so the ingestion services (Plans 20-02, 20-03) and the harvest UAT (Plan 20-04) have a place to write status and a populated `content_sources` table.

Purpose: Research Assumption A6 (HIGH risk) — `ContentVideoStore` is insert-only; Phase 20 cannot mark a video's final `transcript_status` without `UpdateTranscriptStatusAsync`. D-08 also pulls a minimal `content-source-add` CLI verb forward to seed the 5 UAT channels (real + reusable, not a throwaway fixture).
Output: `IContentVideoStore.UpdateTranscriptStatusAsync` + impl + tests; a `content-source-add` CLI verb wrapping `ContentSourceStore.InsertSourceAsync` with `--name`-derived slug.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-content-kb-ingestion-transcription-local/20-CONTEXT.md
@.planning/phases/20-content-kb-ingestion-transcription-local/20-RESEARCH.md

<interfaces>
<!-- Extracted from codebase — executor uses these directly, no exploration needed. -->

From DeckFlow.Core/Content/ContentVideoStore.cs (existing insert pattern to mirror):
- `EnsureSchemaAsync(CancellationToken)` — idempotent CREATE TABLE; call before any query
- Parameter helper: `RelationalDatabaseConnection.AddParameter(command, "@name", value)`
- Connection: `await using var connection = await OpenConnectionAsync(ct)` (private helper already present)
- transcript_status CHECK constraint values (both dialects, lines 330/379): `'pending','captions','whisper','failed','skipped_over_cap'`
- `RETURNING id` is used on inserts; UPDATE does not need RETURNING

From DeckFlow.Core/Knowledge/ContentModels.cs:
- `public static class TranscriptStatus` — `Pending`, `Captions`, `Whisper`, `Failed`, `SkippedOverCap` (string consts; values match the CHECK list)
- `public static class ContentSourceType` — `Youtube = "youtube_channel"`, `Podcast = "podcast_rss"`

From DeckFlow.Core/Content/ContentSourceStore.cs:
- `Task<long> InsertSourceAsync(string sourceSlug, string displayName, string sourceType, string sourceUrl, CancellationToken)` — RETURNING id; UNIQUE(source_url), UNIQUE(source_slug)
- `Task<IReadOnlyList<ContentSource>> ListEnabledSourcesAsync(CancellationToken)`

From DeckFlow.CLI/Program.cs (flat command-tree convention):
- All commands registered on `rootCommand` directly; handler form `Environment.ExitCode = CommandRunners.RunXAsync(...).GetAwaiter().GetResult();`
- artifacts DB path convention: `Path.Combine(Directory.GetCurrentDirectory(), "artifacts", "content-kb.db")` (matches RunArchidektCacheAsync `artifacts/` convention)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add UpdateTranscriptStatusAsync to ContentVideoStore (Wave-0 store gap)</name>
  <files>DeckFlow.Core/Content/IContentVideoStore.cs, DeckFlow.Core/Content/ContentVideoStore.cs, DeckFlow.Core.Tests/ContentVideoStoreTests.cs</files>
  <read_first>
    - DeckFlow.Core/Content/ContentVideoStore.cs (mirror InsertTranscriptAsync shape: EnsureSchemaAsync → OpenConnectionAsync → CreateCommand → AddParameter → ExecuteNonQueryAsync; reuse the existing private const SQL string pattern)
    - DeckFlow.Core/Content/IContentVideoStore.cs (add the interface member with XML doc-comment matching existing members)
    - DeckFlow.Core/Knowledge/ContentModels.cs lines 156-172 (TranscriptStatus consts — the only legal status values)
    - DeckFlow.Core.Tests/ContentSourceStoreTests.cs (xUnit fixture convention: temp SQLite path per test, IDisposable cleanup with SqliteConnection.ClearAllPools)
  </read_first>
  <behavior>
    - Test: InsertVideoAsync (pending) then UpdateTranscriptStatusAsync(id, TranscriptStatus.Captions) → row's transcript_status reads back "captions"
    - Test: UpdateTranscriptStatusAsync(id, TranscriptStatus.SkippedOverCap) succeeds and reads back "skipped_over_cap"
    - Test: UpdateTranscriptStatusAsync with a status not in the TranscriptStatus allowlist throws ArgumentException BEFORE issuing SQL (guard, not DB CHECK failure)
    - Test: UpdateTranscriptStatusAsync against a non-existent videoId completes without throwing and affects 0 rows
  </behavior>
  <action>
    Add `Task UpdateTranscriptStatusAsync(long videoId, string status, CancellationToken cancellationToken = default)` to `IContentVideoStore` with an XML `<summary>`/`<param>` doc-comment matching the existing member style. Implement in `ContentVideoStore` following the `InsertTranscriptAsync` shape exactly: validate `status` against the `TranscriptStatus` allowlist (Pending, Captions, Whisper, Failed, SkippedOverCap) and throw `ArgumentException` for any other value (this keeps the guard in C# rather than relying on the DB CHECK and gives a clear message); call `EnsureSchemaAsync`; open a connection via the existing `OpenConnectionAsync` helper; run `UPDATE content_videos SET transcript_status = @status WHERE id = @videoId;` as a new `private const string` (Allman, raw-string `"""` literal, byte-preserved). Use `RelationalDatabaseConnection.AddParameter` for both params and `ExecuteNonQueryAsync`. Do NOT add RETURNING. Add the four behavior tests to a new `ContentVideoStoreTests` xUnit fixture mirroring `ContentSourceStoreTests` (temp SQLite file per fixture, IDisposable cleanup). Preserve LF endings, file-scoped namespace, `{ get; init; }` (no records changed here).
  </action>
  <verify>
    <automated>"/mnt/c/Program Files/dotnet/dotnet.exe" build DeckFlow.Core/DeckFlow.Core.csproj -c Debug 2>&1 | grep -E "Build succeeded|error" ; "/mnt/c/Program Files/dotnet/dotnet.exe" test DeckFlow.Core.Tests/DeckFlow.Core.Tests.csproj --filter "FullyQualifiedName~ContentVideoStoreTests" 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "UpdateTranscriptStatusAsync" DeckFlow.Core/Content/IContentVideoStore.cs` returns ≥1 and same in ContentVideoStore.cs
    - `grep "UPDATE content_videos" DeckFlow.Core/Content/ContentVideoStore.cs` matches (new UPDATE SQL present)
    - Build succeeds 0 errors; new ContentVideoStoreTests pass Failed:0 (or, if VSTest flaky in WSL per CLAUDE.md, build is clean and the test file compiles — note the push-and-watch fallback in SUMMARY)
    - No `{ get; }` mutations, no Format-Document reflow of unrelated lines (git diff touches only the new member + new SQL const + new test file)
  </acceptance_criteria>
  <done>UpdateTranscriptStatusAsync exists on interface + impl with allowlist guard and is covered by xUnit tests; build clean.</done>
</task>

<task type="auto">
  <name>Task 2: Add content-source-add CLI verb (D-08 source seeding)</name>
  <files>DeckFlow.CLI/Program.cs, DeckFlow.CLI/CommandRunners.cs</files>
  <read_first>
    - DeckFlow.CLI/Program.cs (flat command registration on rootCommand; Option<T> + AddOption + SetHandler with Environment.ExitCode = ...GetAwaiter().GetResult())
    - DeckFlow.CLI/CommandRunners.cs lines 375-415 (RunArchidektCacheAsync — the artifacts/ DB-path + Serilog logger convention to mirror)
    - DeckFlow.Core/Content/ContentSourceStore.cs (InsertSourceAsync signature + UNIQUE(source_slug)/UNIQUE(source_url) constraints)
    - DeckFlow.Core/Knowledge/ContentModels.cs lines 177-184 (ContentSourceType.Youtube / .Podcast)
  </read_first>
  <action>
    Register a flat `content-source-add` command on `rootCommand` (resolves Open Question 2 by deriving slug from `--name`). Options: `--url` (Option&lt;string&gt;, IsRequired), `--name` (Option&lt;string&gt;, IsRequired), `--type` (Option&lt;string&gt; default `ContentSourceType.Youtube` = "youtube_channel", Description "youtube_channel | podcast_rss"), `--db` (Option&lt;FileInfo?&gt; defaulting to `artifacts/content-kb.db` when null). Add `public static async Task<int> RunContentSourceAddAsync(string url, string name, string type, FileInfo? db)` to CommandRunners. In the runner: resolve the DB path (default `Path.Combine(Directory.GetCurrentDirectory(), "artifacts", "content-kb.db")`); validate `type` is one of the two `ContentSourceType` consts and return exit code 2 with a clear message otherwise; derive the slug via `Regex.Replace(name.ToLowerInvariant(), "[^a-z0-9]+", "-").Trim('-')`; print the computed slug to the console before insert; construct `new ContentSourceStore(dbPath)` and call `InsertSourceAsync(slug, name, type, url)`; catch the duplicate-key case (UNIQUE violation on slug/url) and print a friendly "source already exists" message returning 0 (idempotent re-seed), other exceptions return 1. Wire the SetHandler exactly like the other commands (`GetAwaiter().GetResult()`). No new packages — `System.Text.RegularExpressions` is BCL.
  </action>
  <verify>
    <automated>"/mnt/c/Program Files/dotnet/dotnet.exe" build DeckFlow.CLI/DeckFlow.CLI.csproj -c Debug 2>&1 | grep -E "Build succeeded|error"; "/mnt/c/Program Files/dotnet/dotnet.exe" run --project DeckFlow.CLI -- content-source-add --url https://www.youtube.com/@MTGGoldfish --name "MTGGoldfish" --type youtube_channel --db artifacts/uat-content-kb.db 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "content-source-add" DeckFlow.CLI/Program.cs` ≥1; `grep -c "RunContentSourceAddAsync" DeckFlow.CLI/CommandRunners.cs` ≥1
    - CLI run prints the computed slug (e.g. "mtggoldfish") and reports success; re-running the same command is idempotent (exit 0, "already exists")
    - `--type bogus` returns a non-zero exit with a clear error and writes no row
    - git diff touches only Program.cs registration block + the new CommandRunners method (no reflow)
  </acceptance_criteria>
  <done>content-source-add seeds a content_sources row from --url/--name/--type with a derived slug; idempotent on re-run; build clean.</done>
</task>

</tasks>

<verification>
- `UpdateTranscriptStatusAsync` present on interface + impl, allowlist-guarded, tested (closes Research A6 HIGH gap).
- `content-source-add` verb usable to seed the 5 UAT channels (D-08).
- DeckFlow.Core gains NO new package and NO Microsoft.AspNetCore.* reference.
- Build clean; touch-only-what-you-touch preserved (CLAUDE.md R-6).
</verification>

<success_criteria>
- A pending video can be transitioned to any of the four terminal statuses by id.
- The CLI can seed all five UAT channels into local content_sources with stable slugs.
- No formatting drift, no new dependencies, LF endings preserved.
</success_criteria>

<output>
Create `.planning/phases/20-content-kb-ingestion-transcription-local/20-01-SUMMARY.md` when done.
</output>
