---
phase: 22-content-kb-site-integration
plan: 03
type: execute
wave: 3
depends_on: ["22-01", "22-02"]
files_modified:
  - DeckFlow.Web/Services/ContentArtifactParser.cs
  - DeckFlow.Web/Services/ContentKbArtifactPathResolver.cs
  - DeckFlow.Web/Services/ContentKbSeedLoader.cs
  - DeckFlow.Web/Services/IContentKbSeedLoader.cs
  - DeckFlow.Web/Controllers/ContentKbController.cs
  - DeckFlow.Web/Models/ContentKbBrowseViewModel.cs
  - DeckFlow.Web/Models/ContentKbDetailViewModel.cs
  - DeckFlow.Web/Views/ContentKb/Index.cshtml
  - DeckFlow.Web/Views/ContentKb/Detail.cshtml
  - DeckFlow.Web/Views/ContentKb/_ViewStart.cshtml
  - DeckFlow.Web/Views/Shared/_Layout.cshtml
  - DeckFlow.Web/wwwroot/ts/content-kb.ts
  - DeckFlow.Web/wwwroot/css/site-common.css
  - DeckFlow.Web/Program.cs
  - .planning/phases/22-content-kb-site-integration/22-03-SUMMARY.md
autonomous: true
requirements: [KB-08, KB-09]
must_haves:
  truths:
    - "GET /content-kb returns 503 maintenance when content.kb.enabled is OFF, and a hub-card grid of PUBLISHED-only entries when ON (D-04 published-only browse)"
    - "GET /content-kb/{id} renders the artifact (Markdig) with a Copy-for-ChatGPT button yielding clean frontmatter-stripped text; returns 404 when the entry is hidden or missing (D-03 detail page + copy)"
    - "The artifact base resolves correctly in BOTH local dev and Docker via an ordered-candidate resolver, and the chosen base is logged at startup"
    - "Render startup ensures the content_site_index schema and idempotently loads the committed seed without clobbering admin curation"
    - "Client-side facet filter (source/archetype/bracket/card_category + text search) shows/hides cards; both empty states render; all surfaces work at 375px with zero theme bleed"
  artifacts:
    - path: "DeckFlow.Web/Controllers/ContentKbController.cs"
      provides: "flag-gated public browse + detail with content-kb subtree-guarded artifact serving keyed by row id"
      contains: "FeatureFlagGate"
    - path: "DeckFlow.Web/Services/ContentKbArtifactPathResolver.cs"
      provides: "ordered-candidate content base resolution (local dev + Docker) + logged chosen base"
      contains: "ContentRootPath"
    - path: "DeckFlow.Web/Services/ContentKbSeedLoader.cs"
      provides: "reads index-seed.json, builds rows with Id=0, calls UpsertRowPreservingVisibilityAsync per row"
      contains: "UpsertRowPreservingVisibilityAsync"
    - path: "DeckFlow.Web/wwwroot/ts/content-kb.ts"
      provides: "client-side multi-facet filter + clear-filters + local copy-to-clipboard"
  key_links:
    - from: "ContentKbController.Index"
      to: "IContentSiteIndexStore.GetPublishedRowsAsync"
      via: "server query is_visible=true"
      pattern: "GetPublishedRowsAsync"
    - from: "ContentKbController.Detail"
      to: "artifact file under {ContentBase}/content-kb"
      via: "StartsWith('content-kb/') gate + Path.GetFullPath prefix check against ContentBase/content-kb"
      pattern: "StartsWith"
    - from: "Program.cs startup"
      to: "ContentKbSeedLoader.LoadIfPresentAsync"
      via: "after EnsureSchemaAsync, before RunAsync"
      pattern: "LoadIfPresent"
---

<objective>
Ship the public Content KB surface: a flag-gated browse page (published-only hub-card grid + client-side faceted filter + empty states), a per-entry artifact detail page keyed by row id (Markdig render + Copy-for-ChatGPT), an artifact-base resolver that works in both local dev and Docker, the startup seed-load that materializes the index on Render without clobbering admin curation, and the conditional nav link.

Purpose: This is the primary deliverable — it serves the project's core value (one-round-trip ChatGPT paste). It consumes the Plan 01 store contracts AND the Plan 02 seed file + tracked artifact tree.

Output: ContentKbController (public) + artifact-path resolver + seed loader + 2 views + filter/copy TS + public CSS additions + Program.cs wiring + conditional nav link.
</objective>

<locked_decisions>
## D-22A — artifact_path canonical convention (LOCKED, inherited from Plan 01/02)
`artifact_path` = `content-kb/{source-slug}/{id}.md` — the `content-kb/` prefix is PART of the stored/seeded value. The controller resolves `resolved = Path.Combine(contentBase, artifact_path)` where `contentBase` CONTAINS `content-kb/` — NOT the `content-kb/` dir itself. Combining a `content-kb/`-prefixed path against a `content-kb/` base would yield `/app/content-kb/content-kb/...` — DO NOT do that (review HIGH-3).

## D-22B — content base resolver (LOCKED, ordered candidates; resolves review HIGH-6)
No single hardcoded base works (Docker ContentRootPath=/app; local dev ContentRootPath=DeckFlow.Web project dir). `ContentKbArtifactPathResolver` resolves `contentBase` as the FIRST existing candidate, in order:
  1. config override `ContentKb:ContentBase` if set and non-empty,
  2. `env.ContentRootPath` (Docker route, D-22B: COPY puts files at /app/content-kb, base = /app),
  3. `Path.GetFullPath(Path.Combine(env.ContentRootPath, ".."))` (local dev: DeckFlow.Web/.. = repo root, which contains content-kb/),
  4. `Directory.GetCurrentDirectory()` fallback.
A candidate "exists" when `Directory.Exists(Path.Combine(candidate, "content-kb"))`. Log the chosen base + whether content-kb/ exists under it at startup (one info line) so Pitfall 4 is diagnosable in deploy logs. The resolver is the single source of truth for BOTH the seed-file path (`{base}/content-kb/seed/index-seed.json`) and artifact reads (`Path.Combine(base, row.ArtifactPath)`).
The resolver takes `IWebHostEnvironment` via constructor injection — it is a DI singleton resolved from the container, so it gets the live environment regardless of build-vs-runtime phase (see DI note in Task 1: register with `builder.Environment` OR resolve `IWebHostEnvironment` inside the factory lambda — NOT `app.Environment`, which is out of scope during service registration — review HIGH-2).

## D-22C — detail route keyed by row id (LOCKED; resolves MED route-safety)
The detail route is `/content-kb/{id:long}` using the surrogate `content_site_index.id` (always a safe integer) — NOT `{sourceSlug}--{naturalKey}`. RSS podcast GUIDs can contain `/ ? # --` and are unsafe in a path segment; the slug--key scheme is dropped. The controller resolves the row via `store.GetByIdAsync(id)` (added in Plan 01). Browse-page links emit `/content-kb/{row.Id}`.

## D-22F — artifact serving guards the content-kb/ SUBTREE, not just ContentBase (LOCKED; resolves review MED-1)
The store's `ValidateArtifactPath` (ContentSiteIndexStore.cs:169) rejects only rooted/`..` paths — it does NOT prevent a row whose artifact_path is e.g. `appsettings.json` from resolving to a real file UNDER ContentBase (`/app/appsettings.json`). A bad DB/seed row could therefore exfiltrate non-artifact files that happen to live under the base. The controller MUST add TWO guards on top of the store validation:
  (a) require `row.ArtifactPath.StartsWith("content-kb/", StringComparison.Ordinal)` (forward-slash form, matching D-22A); a row that does not begin with the content-kb/ prefix → 404, never read.
  (b) prefix-check the RESOLVED absolute path against `{ContentBase}/content-kb` (NOT just `{ContentBase}`): `var allowedRoot = Path.GetFullPath(Path.Combine(resolver.ContentBase, "content-kb")); if (!resolved.StartsWith(allowedRoot + Path.DirectorySeparatorChar, StringComparison.OrdinalIgnoreCase)) return NotFound();`.
Both checks run BEFORE File.Exists / File.Read. Never log or echo the resolved path. This is security-relevant — see T-22-09 in the threat model.
</locked_decisions>

<dependency_note>
This plan depends on 22-02 (review HIGH-1). Rationale: it reads Plan 02's committed seed file (index-seed.json) at startup and serves Plan 02's tracked artifact tree; running before the seed/artifacts/Dockerfile delivery exist would leave nothing to load or serve. Wave moved 2→3; 22-04 (admin) moves to wave 4. The prior "wave 2 plans 02-03 parallel-safe" claim is RETRACTED — the artifact-base + seed data dependency makes them sequential.
</dependency_note>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-content-kb-site-integration/22-CONTEXT.md
@.planning/phases/22-content-kb-site-integration/22-UI-SPEC.md
@.planning/phases/22-content-kb-site-integration/22-RESEARCH.md

<read_first>
- 22-UI-SPEC.md Surface 1 (public browse) + Surface 2 (detail) — authoritative for classes, copy, ARIA, responsive breakpoints, empty states. Honor the Checker Disposition appendix. NOTE: UI-SPEC says "404 when flag off" (lines 123, 212, 370) — INACCURATE; `FeatureFlagGateAttribute` returns 503. Treat 503 as correct; ignore the UI-SPEC "404" flag-off wording.
- DeckFlow.Web/Program.cs — CRITICAL DI PHASE FACT (review HIGH-2): `app` is built at line 349 (`var app = builder.Build();`), AFTER all service registration. `app.Environment` is NOT in scope during `builder.Services.AddSingleton(...)` calls; `builder.Environment` IS (it is used at line 45 for the log path and lines 178-179 for `AddDeckFlowHarvest(builder.Environment)` / `AddDeckFlowAnalytics(builder.Environment)`). Existing store singletons use the `builder.Services.AddSingleton<IFoo, Foo>()` form (line 175 IFeedbackStore). The post-build startup-schema block (lines 425-430) uses `app.Services.GetRequiredService<...>().EnsureSchemaAsync()`. So: REGISTER the resolver/store/loader in the DI block with `builder.Environment` (or a factory lambda resolving `IWebHostEnvironment`); CALL EnsureSchemaAsync + LoadIfPresentAsync in the post-build block via `app.Services.GetRequiredService(...)`. NEVER reference `app.Environment` inside a service registration.
- DeckFlow.Web/Infrastructure/FeatureFlagGateAttribute.cs:66-71 — flag OFF short-circuits with HTTP 503 + Retry-After:300 + maintenance view (D-17). LOCKED. Apply `[FeatureFlagGate("content.kb.enabled", ...)]` to BOTH Index and Detail. (MED 503-vs-404: ACCEPT 503. The HIDDEN-entry 404 is a SEPARATE concern: an is_visible=false row returns NotFound from Detail — real 404 logic, not the flag gate.)
- DeckFlow.Web/Services/HelpContentService.cs:66-88 — `SplitHeader(raw)` frontmatter splitter + the Markdig `Pipeline` config (`UseAdvancedExtensions().DisableHtml()`). Extract SplitHeader into a NEW static `ContentArtifactParser` so this controller (and Plan 04) reuse it; reuse the same Markdig pipeline config for rendering the body.
- DeckFlow.Web/wwwroot/ts/card-lookup.ts:134-163 — `attachDynamicCopyButton` is a MODULE-LOCAL `const` (NOT exported), and the detail layout does NOT load card-lookup.ts. So it CANNOT be reused cross-file. The detail copy button uses LOCAL copy logic in content-kb.ts. Mirror its idle/copied/failed UX but implement it in content-kb.ts against `data-copy-target`.
- ANALOG (controller + view placement): DeckFlow.Web/Controllers/DeckController.cs + Views/Deck/*.cshtml.
- DeckFlow.Core/Content/ContentSiteIndexStore.cs:169-187 — `ValidateArtifactPath` rejects ONLY rooted + `..` paths; it does NOT confine to the content-kb/ subtree (review MED-1). The controller compensates with the D-22F subtree guards.
- DeckFlow.Core/Knowledge/ContentArtifactSpec.cs:110 — `ContentSiteIndexRow.Id` is `required`. The export DTO (Plan 02) omits id, so the seed JSON has NO id field. The loader MUST set `Id = 0` explicitly when constructing each row (review MED-2); the store assigns/ignores the surrogate id on natural-key upsert.
- DeckFlow.Core/Content/IContentSiteIndexStore.cs (after Plan 01) — GetPublishedRowsAsync, GetByIdAsync (carries IsVisible), UpsertRowPreservingVisibilityAsync.
- DeckFlow.Web/Views/Shared/_Layout.cshtml — the main nav `<ul>`; add the conditional "Knowledge Base" link. DeckFlow.Web/Services/FeatureFlags/IFeatureFlagCache.cs:20 — `IsEnabled(string key)`.
- DeckFlow.Web/wwwroot/css/site-common.css — existing `.hub-card`, `.hub-grid`, `.help-prose`, `.page-shell`, `.page-frame`, `.page-lede`, `.maintenance-page__action`, `.copy-button`/`.run-button`, df-select rules. Add ONLY `.kb-filter-bar`, `.kb-tag`, `.kb-empty`, `.kb-artifact-prose` here. NEVER add KB rules to site.css or any site-*.css theme fork (SC5).
</read_first>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ContentArtifactParser + ContentKbArtifactPathResolver + seed loader + Program.cs startup wiring + DI</name>
  <files>DeckFlow.Web/Services/ContentArtifactParser.cs, DeckFlow.Web/Services/ContentKbArtifactPathResolver.cs, DeckFlow.Web/Services/IContentKbSeedLoader.cs, DeckFlow.Web/Services/ContentKbSeedLoader.cs, DeckFlow.Web/Program.cs</files>
  <read_first>
HelpContentService.cs:66-88 (SplitHeader + Markdig pipeline). Program.cs — DI block (line 175 IFeedbackStore singleton; lines 178-179 `builder.Environment` usage) AND the post-build startup-schema block (lines 423-430, `app.Services.GetRequiredService<...>().EnsureSchemaAsync()`). DeckFlowDatabaseConnectionFactory.cs:63 (CreateContentSiteIndexConnection). IContentSiteIndexStore.cs after Plan 01 (UpsertRowPreservingVisibilityAsync). ContentArtifactSpec.cs:110 (Id is required).
  </read_first>
  <action>
    Create `ContentArtifactParser` (static class) with `public static (IReadOnlyDictionary<string,string> Header, string Body) SplitHeader(string raw)` — move the logic from HelpContentService.SplitHeader (handles `---` delimiters, CRLF/LF, returns header dict + body). Leave HelpContentService working: prefer having it delegate to the new helper; if touching HelpContentService risks R-6 raw-string drift, keep its private copy and document the duplication in the SUMMARY.

    Create `ContentKbArtifactPathResolver` (sealed, DI-singleton) implementing D-22B: constructor takes `IWebHostEnvironment`, `IConfiguration`, `ILogger<ContentKbArtifactPathResolver>`. Expose `string ContentBase { get; }` resolved once (ordered candidates per D-22B: config `ContentKb:ContentBase` → env.ContentRootPath → ContentRootPath/.. → CWD; pick the first whose `Directory.Exists(Path.Combine(candidate,"content-kb"))`; if none exist, fall back to env.ContentRootPath and log a warning). Also expose `string SeedFilePath => Path.Combine(ContentBase, "content-kb", "seed", "index-seed.json")` and `string ResolveArtifactFullPath(string artifactPath) => Path.GetFullPath(Path.Combine(ContentBase, artifactPath))`. Log the chosen ContentBase + whether content-kb/ exists under it (one info line) in the ctor.

    Create IContentKbSeedLoader + ContentKbSeedLoader (singleton): `Task<int> LoadIfPresentAsync(CancellationToken = default)` (NO IWebHostEnvironment param — it depends on the resolver via DI). Resolve the seed path from `ContentKbArtifactPathResolver.SeedFilePath`. If the file is absent, log info and return 0 (non-fatal). If present, deserialize the JSON array (System.Text.Json, camelCase) and for EACH entry call `IContentSiteIndexStore.UpsertRowPreservingVisibilityAsync` (NOT UpsertRowAsync — preserves admin curation, Pitfall 1). Build each ContentSiteIndexRow from the seed fields, setting `Id = 0` EXPLICITLY (review MED-2 — `ContentSiteIndexRow.Id` is `required` and the seed DTO omits id; the store assigns/ignores the surrogate id on natural-key upsert, so 0 is the correct sentinel); copy artifactPath verbatim (keeps the content-kb/ prefix, D-22A); set YoutubeVideoId/RssGuid from naturalKeyType (`youtube_channel`→YoutubeVideoId, `podcast_rss`→RssGuid). Log "Content KB seed load complete: N rows" and return N.

    In Program.cs DI block (BEFORE `var app = builder.Build()` at line 349, review HIGH-2): register `IContentSiteIndexStore` as a singleton mirroring IFeedbackStore — use `builder.Services.AddSingleton<IContentSiteIndexStore>(sp => new ContentSiteIndexStore(DeckFlowDatabaseConnectionFactory.CreateContentSiteIndexConnection(builder.Environment)))` (or the existing AddSingleton<IFoo,Foo> form if the store ctor takes no env). Use `builder.Environment` here, NEVER `app.Environment` (it does not exist yet). Register `ContentKbArtifactPathResolver` as a singleton (it gets IWebHostEnvironment from the container — the live env — automatically). Register `IContentKbSeedLoader`→ContentKbSeedLoader singleton. THEN in the POST-BUILD startup store-schema block (lines 425-430, before app.RunAsync), add: `await app.Services.GetRequiredService<IContentSiteIndexStore>().EnsureSchemaAsync();` then `await app.Services.GetRequiredService<IContentKbSeedLoader>().LoadIfPresentAsync();`. (The resolver logs ContentBase + content-kb existence in its ctor — first resolved when GetRequiredService instantiates it — satisfying Pitfall-4 diagnosability.)
  </action>
  <verify>
    <automated>"/mnt/c/Program Files/dotnet/dotnet.exe" build /mnt/c/users/chrislunt/source/personal/deckflow/DeckFlow.Web/DeckFlow.Web.csproj -c Debug 2>&1 | grep -E "error|Build succeeded"</automated>
  </verify>
  <acceptance_criteria>
    - Build clean.
    - DI PHASE (review HIGH-2): the registration block for the resolver/store references `builder.Environment` OR resolves `IWebHostEnvironment` from the factory `sp`/container; it does NOT reference `app.Environment`. Gate: in the registration region of Program.cs, `grep -c 'app.Environment' <registration-lines>` → 0 for the new KB registrations; the new singletons appear BEFORE `var app = builder.Build()` (line 349). EnsureSchemaAsync + LoadIfPresentAsync appear AFTER the build, in the post-build block.
    - `grep -c "ContentRootPath" DeckFlow.Web/Services/ContentKbArtifactPathResolver.cs` >= 2 and the resolver checks `Directory.Exists` for the content-kb subdir (grep `Directory.Exists` → >=1).
    - `grep -c "UpsertRowPreservingVisibilityAsync" DeckFlow.Web/Services/ContentKbSeedLoader.cs` >= 1; `grep -c "UpsertRowAsync" DeckFlow.Web/Services/ContentKbSeedLoader.cs` → 0.
    - SEED ID (review MED-2): the loader sets Id explicitly — `grep -c "Id = 0" DeckFlow.Web/Services/ContentKbSeedLoader.cs` >= 1.
    - Program.cs registers the store + resolver + loader and calls EnsureSchemaAsync + LoadIfPresentAsync at startup (grep `LoadIfPresentAsync` in Program.cs → 1).
    - Resolver logs the chosen base (grep `LogInformation`/`_logger` in resolver → >=1).
  </acceptance_criteria>
  <allowed_files>
ALLOWED FILE SET (Task 1): DeckFlow.Web/Services/ContentArtifactParser.cs, DeckFlow.Web/Services/ContentKbArtifactPathResolver.cs, DeckFlow.Web/Services/IContentKbSeedLoader.cs, DeckFlow.Web/Services/ContentKbSeedLoader.cs, DeckFlow.Web/Program.cs (DI block + post-build startup block only), DeckFlow.Web/Services/HelpContentService.cs (ONLY if delegating SplitHeader). Touch nothing else. Ignore other phase-22 tasks' files. Audit HEAD before dispatch; if unrelated staged changes exist, STOP.
  </allowed_files>
  <done>Build clean; resolver implements ordered candidates + logs base; seed loader uses UpsertRowPreservingVisibilityAsync and sets Id=0 explicitly; Program.cs registers store+resolver+loader with builder.Environment (NOT app.Environment) BEFORE build, and calls EnsureSchemaAsync + LoadIfPresentAsync in the post-build block.</done>
</task>

<task type="auto">
  <name>Task 2: Public ContentKbController (browse + detail-by-id) with content-kb subtree-guarded artifact serving</name>
  <files>DeckFlow.Web/Controllers/ContentKbController.cs, DeckFlow.Web/Models/ContentKbBrowseViewModel.cs, DeckFlow.Web/Models/ContentKbDetailViewModel.cs</files>
  <read_first>
FeatureFlagGateAttribute.cs:55-75. ContentArtifactParser (Task 1). ContentKbArtifactPathResolver (Task 1: ContentBase + ResolveArtifactFullPath). IContentSiteIndexStore.cs after Plan 01 (GetPublishedRowsAsync, GetByIdAsync). ContentSiteIndexStore.cs:169 (store validation does NOT confine to content-kb/ subtree — D-22F is the compensating guard). DeckController.cs (controller conventions).
  </read_first>
  <action>
    Create ContentKbController ([Route("content-kb")], sealed). Constructor injects IContentSiteIndexStore, ContentKbArtifactPathResolver, ILogger. Both actions carry `[FeatureFlagGate("content.kb.enabled", Title="Knowledge Base unavailable", Message="The Knowledge Base is not currently available.")]`.

    Index() [HttpGet("")]: call `store.GetPublishedRowsAsync()`, project to ContentKbBrowseViewModel (entries with Id, title, source, video_url, archetype/bracket/card_category tags, and the detail URL `/content-kb/{row.Id}`). View(model).

    Detail(long id) [HttpGet("{id:long}")] (D-22C — keyed by surrogate id): resolve the row via `store.GetByIdAsync(id)`. If row is null OR `row.IsVisible == false` → return NotFound() (hidden entries 404 BEFORE reading any file). Then apply the D-22F SUBTREE guards (review MED-1):
      (a) PREFIX KEY: if `!row.ArtifactPath.StartsWith("content-kb/", StringComparison.Ordinal)` → NotFound() (a row whose artifact_path is not under the content-kb/ prefix — e.g. `appsettings.json` — is rejected before any path math).
      (b) RESOLVED SUBTREE: `var allowedRoot = Path.GetFullPath(Path.Combine(resolver.ContentBase, "content-kb")); var resolved = resolver.ResolveArtifactFullPath(row.ArtifactPath);` then if `!resolved.StartsWith(allowedRoot + Path.DirectorySeparatorChar, StringComparison.OrdinalIgnoreCase)` → NotFound(). NOTE the prefix check is against `{ContentBase}/content-kb`, NOT just `{ContentBase}` — that is the MED-1 hardening. Never log/echo the resolved path.
    Then if `!File.Exists(resolved)` → return View("Detail", new ContentKbDetailViewModel { ArtifactUnavailable = true, ... }) (UI-SPEC "temporarily unavailable" copy; copy button hidden). Read file, `ContentArtifactParser.SplitHeader` → body; render body via the Markdig pipeline → HtmlString; set `CleanBodyText = body`. View model also carries breadcrumb title, source name + external url, published date, bracket, archetype. View(model).

    ViewModels are sealed records/classes with `{ get; init; }` (R-6). ContentKbBrowseViewModel entries carry Id. ContentKbDetailViewModel includes: Title, SourceName, SourceUrl, PublishedDisplay, Bracket, Archetype, RenderedHtml (HtmlString), CleanBodyText, ArtifactUnavailable (bool).
  </action>
  <verify>
    <automated>"/mnt/c/Program Files/dotnet/dotnet.exe" build /mnt/c/users/chrislunt/source/personal/deckflow/DeckFlow.Web/DeckFlow.Web.csproj -c Debug 2>&1 | grep -E "error|Build succeeded"</automated>
  </verify>
  <acceptance_criteria>
    - Build clean.
    - `grep -c "FeatureFlagGate" DeckFlow.Web/Controllers/ContentKbController.cs` >= 2 (both actions).
    - Detail route is id-keyed: `grep -c '{id:long}' DeckFlow.Web/Controllers/ContentKbController.cs` >= 1.
    - SUBTREE GUARDS (review MED-1 / D-22F): `grep -c 'StartsWith("content-kb/"' DeckFlow.Web/Controllers/ContentKbController.cs` >= 1 (the prefix key) AND the resolved-path prefix check is against a `content-kb`-joined root — `grep -c 'Path.Combine(.*ContentBase.*content-kb' DeckFlow.Web/Controllers/ContentKbController.cs` >= 1 (allowedRoot includes content-kb). Overall `grep -c "StartsWith" ...` >= 2.
    - Detail 404s when `IsVisible == false` AND when the artifact_path is not under content-kb/, BOTH BEFORE any File.Exists/File read (order verifiable in source).
  </acceptance_criteria>
  <allowed_files>
ALLOWED FILE SET (Task 2): DeckFlow.Web/Controllers/ContentKbController.cs, DeckFlow.Web/Models/ContentKbBrowseViewModel.cs, DeckFlow.Web/Models/ContentKbDetailViewModel.cs. Touch nothing else. Ignore other phase-22 tasks' files. Audit HEAD before dispatch; STOP on unrelated staged changes.
  </allowed_files>
  <done>Build clean; both actions flag-gated (503 off); Detail keyed by id; 404s on hidden/missing rows AND on non-content-kb/ artifact paths BEFORE file read; resolved-path prefix check confines to {ContentBase}/content-kb (not just ContentBase); clean text computed for copy.</done>
</task>

<task type="auto">
  <name>Task 3: Browse + Detail views, content-kb.ts (filter + local copy), public CSS, conditional nav link</name>
  <files>DeckFlow.Web/Views/ContentKb/Index.cshtml, DeckFlow.Web/Views/ContentKb/Detail.cshtml, DeckFlow.Web/Views/ContentKb/_ViewStart.cshtml, DeckFlow.Web/wwwroot/ts/content-kb.ts, DeckFlow.Web/wwwroot/css/site-common.css, DeckFlow.Web/Views/Shared/_Layout.cshtml</files>
  <read_first>
22-UI-SPEC.md Surface 1 + Surface 2 (copy/classes/ARIA/empty states). card-lookup.ts:134-163 (copy UX to MIRROR, not import). site-common.css existing primitives. _Layout.cshtml nav ul. IFeatureFlagCache.IsEnabled.
  </read_first>
  <action>
    Build all three surfaces to the UI-SPEC contract exactly (copy, classes, ARIA). Detail links use `/content-kb/{Model entry Id}` (D-22C).

    Index.cshtml: `.page-shell` + `.page-frame`; H1 "Knowledge Base"; `.page-lede` lede; `.kb-filter-bar` `<fieldset>` with `<legend class="sr-only">Filter knowledge base entries</legend>` containing the search `<input type="search" aria-label="Search entries">` + four df-select dropdowns (Source/Archetype/Bracket/Card category), each first option "Any …". `.hub-grid` of `.hub-card` per entry (title `<a href="/content-kb/@entry.Id">`, `.hub-card__description` = source · bracket · archetype, decorative `.kb-tag` chips `aria-hidden="true"` with `data-archetype`/`data-bracket`/`data-card-category`/`data-source`). Both empty-state blocks (`.kb-empty` zero-published — copy A; and the no-match block with "Clear filters" `.maintenance-page__action` button — copy B), and a visually-hidden `role="status" aria-live="polite"` match-count element.

    Detail.cshtml: `.help-breadcrumb` "Knowledge Base / {title}"; metadata header line; sticky toolbar with the Copy button (`class="copy-button run-button" data-copy-target="kb-artifact-text" aria-label="Copy this artifact for ChatGPT"`) + hidden `<textarea id="kb-artifact-text" readonly hidden>@Model.CleanBodyText</textarea>`; `.kb-artifact-prose` panel rendering `@Model.RenderedHtml`. If `Model.ArtifactUnavailable`, render "temporarily unavailable" copy and HIDE the copy button.

    content-kb.ts (under ~80 lines, no deps): on DOMContentLoaded — (1) FILTER across the four facets + text (debounced 200ms, Escape clears search), toggle each `.hub-card` `display` (do not remove from DOM); update the aria-live match count; wire "Clear filters". (2) COPY (LOCAL logic, do NOT import card-lookup.ts): find `button[data-copy-target]`; on click read the target textarea's value, `navigator.clipboard.writeText(value)`, swap text to "Copied" then reset after ~1.5s; on failure "Copy failed". Compiles via the MSBuild TypeScript target to wwwroot/js; the detail view references content-kb.js.

    site-common.css: add ONLY `.kb-filter-bar`, `.kb-tag`, `.kb-empty`, `.kb-artifact-prose` per UI-SPEC. Tokens only; no hex.

    _Layout.cshtml: inject `IFeatureFlagCache` and render `<li><a asp-controller="ContentKb" asp-action="Index">Knowledge Base</a></li>` ONLY when `FeatureFlagCache.IsEnabled("content.kb.enabled")`. Touch only the nav insertion + @inject line (R-6).
  </action>
  <verify>
    <automated>"/mnt/c/Program Files/dotnet/dotnet.exe" build /mnt/c/users/chrislunt/source/personal/deckflow/DeckFlow.Web/DeckFlow.Web.csproj -c Debug 2>&1 | grep -E "error|Build succeeded"; grep -c "\.kb-" /mnt/c/users/chrislunt/source/personal/deckflow/DeckFlow.Web/wwwroot/css/site.css 2>/dev/null</automated>
  </verify>
  <acceptance_criteria>
    - Build clean (TS compiles to wwwroot/js/content-kb.js).
    - Zero theme bleed: `grep -rl "\.kb-" DeckFlow.Web/wwwroot/css/site.css DeckFlow.Web/wwwroot/css/site-*.css` returns NOTHING.
    - content-kb.ts contains LOCAL copy logic (`navigator.clipboard.writeText` grep → >=1) and does NOT reference card-lookup (grep `card-lookup` in content-kb.ts → 0).
    - Both empty-state blocks present in Index.cshtml (`.kb-empty` + a "Clear filters" button).
    - Nav link conditional on `IsEnabled("content.kb.enabled")` in _Layout.cshtml.
  </acceptance_criteria>
  <allowed_files>
ALLOWED FILE SET (Task 3): DeckFlow.Web/Views/ContentKb/Index.cshtml, DeckFlow.Web/Views/ContentKb/Detail.cshtml, DeckFlow.Web/Views/ContentKb/_ViewStart.cshtml, DeckFlow.Web/wwwroot/ts/content-kb.ts, DeckFlow.Web/wwwroot/css/site-common.css, DeckFlow.Web/Views/Shared/_Layout.cshtml. NEVER touch site.css or any site-*.css theme fork. Audit HEAD before dispatch; STOP on unrelated staged changes.
  </allowed_files>
  <done>Build clean (TS compiles); views match UI-SPEC; both empty states; filter shows/hides cards; local copy works; nav link conditional; zero `.kb-` rules in site.css/theme forks.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: VERIFY public browse + detail + copy + 375px + path guards</name>
  <what-built>The public Content KB browse + detail pages behind the content.kb.enabled flag, with client-side faceting and copy-for-ChatGPT.</what-built>
  <how-to-verify>
    Ask the user to start the dev server (do not auto-launch — see MEMORY). Then:
    1. Flag OFF (default): GET /content-kb returns the 503 maintenance page (NOT a crash, NOT 404); no "Knowledge Base" nav link.
    2. Enable content.kb.enabled via /Admin/Flags. Reload /content-kb: hub-card grid of published entries (publish a couple via the Plan-04 admin grid or temporarily via SQL). Nav link visible.
    3. Type in the search box and change each facet dropdown — cards show/hide live; match-count announces; "Clear filters" resets.
    4. Click an entry → detail page (`/content-kb/{id}`) renders the artifact; click "Copy for ChatGPT" → clipboard has clean text WITHOUT frontmatter (`---` block absent).
    5. Resize to 375px: grid single-column, controls full-width, copy button full-width, touch targets >=44px; switch guild theme → no leaked KB styling.
    6. Negative: GET /content-kb/{id} for a hidden entry → 404; GET /content-kb/999999 → 404. (D-22F subtree guard is unit-of-source verified; live test would require a crafted bad row.)
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues (375px screenshots appreciated).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| anonymous client → public controller | untrusted route id + flag state |
| DB artifact_path → filesystem read | path could escape the content-kb/ subtree |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-08 | Information Disclosure | hidden entries reachable via direct detail URL | mitigate | Detail() returns 404 when row null OR IsVisible==false, BEFORE any file read |
| T-22-09 | Tampering / Information Disclosure | bad DB/seed artifact_path resolves a NON-artifact file under ContentBase (e.g. `appsettings.json`) — store ValidateArtifactPath only blocks rooted/`..`, NOT subtree escape (review MED-1) | mitigate | controller D-22F guards: (a) require artifact_path StartsWith("content-kb/"); (b) prefix-check the RESOLVED absolute path against `{ContentBase}/content-kb` (NOT just ContentBase); 404 silently on violation; never echo path. Both run before File.Exists |
| T-22-10 | Information Disclosure | flag-off route leaks data via direct URL | mitigate | FeatureFlagGate on BOTH actions → 503 maintenance when OFF |
| T-22-11 | Tampering | seed-load clobbers admin curation | mitigate | loader uses UpsertRowPreservingVisibilityAsync (Plan 01); rows built with Id=0 (MED-2) |
| T-22-12 | Information Disclosure | copy button serves raw frontmatter | mitigate | server-side SplitHeader strip; CleanBodyText holds body only |

No HIGH-severity unmitigated threats. The MED-1 path-traversal subtree hardening (T-22-09 / D-22F) is reflected here per the review. ASVS L1 satisfied.
</threat_model>

<verification>
- DeckFlow.Web builds clean; content-kb.ts compiles to wwwroot/js.
- DI PHASE (review HIGH-2): KB service registrations use `builder.Environment` (or factory-resolved IWebHostEnvironment), NOT `app.Environment`; they sit BEFORE `var app = builder.Build()`; EnsureSchemaAsync + LoadIfPresentAsync sit in the post-build block.
- SEED ID (review MED-2): loader sets `Id = 0` explicitly (grep → >=1).
- SUBTREE GUARD (review MED-1): controller requires `StartsWith("content-kb/")` AND prefix-checks the resolved path against `{ContentBase}/content-kb` (grep both).
- Zero theme bleed: `grep -rl "\.kb-" DeckFlow.Web/wwwroot/css/site.css DeckFlow.Web/wwwroot/css/site-*.css` returns NOTHING.
- Flag gate on both actions; Detail keyed by `{id:long}`.
- Resolver candidates + logging present (ContentRootPath x2, Directory.Exists, LogInformation).
- Human-verify checkpoint passed.
</verification>

<success_criteria>
- Public browse lists published-only entries with client-side facets + both empty states (KB-08; SC2).
- Detail page (id-keyed) renders the artifact via Markdig with a clean-text Copy-for-ChatGPT (KB-08; SC2/SC3).
- Artifact base resolves in local dev AND Docker via ordered candidates + logged base; DI uses builder.Environment, not app.Environment (HIGH-6; HIGH-2 closed).
- Startup materializes the index + idempotent curation-preserving seed load with Id=0 rows (KB-08; SC1; MED-2 closed).
- Artifact serving confined to the content-kb/ subtree (MED-1 closed).
- Flag-gated (503 OFF), 375px responsive, zero theme bleed (KB-09; SC5).
</success_criteria>

<output>
Create `.planning/phases/22-content-kb-site-integration/22-03-SUMMARY.md` when done. Record the resolved ContentBase observed in local dev logs and any SplitHeader-duplication choice.
</output>
