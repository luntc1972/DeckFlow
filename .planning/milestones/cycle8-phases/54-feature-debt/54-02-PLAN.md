---
phase: 54-feature-debt
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - DeckFlow.Web.Tests/GeminiVariantSizeTests.cs
  - .planning/phases/54-feature-debt/54-VERIFICATION.md
autonomous: true
requirements: [FEAT-01]
must_haves:
  truths:
    - "Each of the four Gemini workflow prompt artifacts (analysis, comparison, meta-gap, primer) has a measured char count recorded against the ~30,000-char Gemini paste ceiling"
    - "A repeatable automated test measures Gemini prompt char counts for a representative deck (no live HTTP/DB)"
    - "If any artifact exceeds 30,000 chars it is surfaced as a recorded FINDING, not silently shipped and not trimmed"
    - "The DECKFLOW_GEMINI_ENABLED default remains false (no default change)"
    - "The flag-flipped path produces a Gemini prompt for all four workflows — Primer via the DeckPrimerPacketService fan-out with geminiEnabled:true (GetEnabledPlatforms includes Gemini), analysis/comparison/meta-gap via TargetAiPlatform=Gemini routing already proven by AiPlatformPhase10RoundTripTests; the new size test measures the routed/built prompt"
  artifacts:
    - path: "DeckFlow.Web.Tests/GeminiVariantSizeTests.cs"
      provides: "Repeatable char-count measurement across the four Gemini variants"
      min_lines: 40
    - path: ".planning/phases/54-feature-debt/54-VERIFICATION.md"
      provides: "Recorded size table + findings + default-off confirmation"
      contains: "FEAT-01"
  key_links:
    - from: "GeminiVariantSizeTests"
      to: "GeminiAnalysis/Comparison/MetaGap/PrimerPromptVariant.Build"
      via: "direct synchronous Build() call asserting prompt.Length"
      pattern: "\\.Build\\("
---

<objective>
Verify the Gemini paste-limit path (FEAT-01) is genuinely usable across all four workflows that emit a Gemini variant — deck analysis, deck comparison, cEDH meta-gap, and Deck Primer — and record the evidence. Per CONTEXT.md (LOCKED): this is VERIFY-ONLY. Keep `DECKFLOW_GEMINI_ENABLED` default FALSE. Do NOT change the default to true. Do NOT implement packet trimming — if an artifact exceeds Gemini's paste limit, that is a RECORDED FINDING.

Purpose: Gemini was hidden in the UI since v1.4 because the full packet frequently exceeds Gemini's ~30,000-char paste warning (truncates instructions → degraded output). All Gemini infra is wired (Phase 15). We confirm the four workflows generate Gemini prompts and measure their real char sizes so the operator can flip the flag in prod with eyes open. Research flags `GeminiAnalysisPromptVariant` (50,000-char cap, NO trimming) as the likely failing case.
Output: a repeatable xUnit size-measurement test (Wave 0 per VALIDATION.md) over the four Gemini variants, plus `54-VERIFICATION.md` with a per-workflow size table, pass/fail vs the 30,000-char ceiling, recorded findings for any overage, and confirmation the default stays false.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/54-feature-debt/54-CONTEXT.md
@.planning/phases/54-feature-debt/54-RESEARCH.md
@.planning/phases/54-feature-debt/54-VALIDATION.md
@CLAUDE.md

<interfaces>
<!-- Verified against live source 2026-06-17. -->

Gemini paste ceiling (CONTEXT/RESEARCH): ~30,000 chars (community-sourced; codebase Primer cap = 32,000 aligns). Measure with C# string.Length (char count, NOT UTF-8 bytes — Pitfall 3). Pass threshold for THIS verification = 30,000 chars (conservative).

Per-variant internal caps (RESEARCH §"Per-variant cap constants"):
  GeminiAnalysisPromptVariant   — Analysis/GeminiAnalysisPromptVariant.cs:17  DefensivePromptCharCap=50000, NO section-drop  ← likely failing case
  GeminiComparisonPromptVariant — Comparison/GeminiComparisonPromptVariant.cs  no cap, no drop
  GeminiMetaGapPromptVariant    — MetaGap/GeminiMetaGapPromptVariant.cs        no cap, no drop
  GeminiPrimerPromptVariant     — Primer/GeminiPrimerPromptVariant.cs:13       DefensivePromptCharCap=32000, HAS AppendIfFits section-drop

Build signatures (all synchronous, pure CPU — no HTTP/DB; mirror PrimerPromptVariantTests harness):
  GeminiAnalysisPromptVariant.Build(...)   : IAnalysisPromptVariant
  GeminiComparisonPromptVariant.Build(deckA, deckB, deckAListText, deckBListText, deckAComboText, deckBComboText, comparisonContextText, comparisonSchemaJson)
  GeminiMetaGapPromptVariant.Build(commanderName, myDeckEntries, myDeckCombos, selectedEntries, referenceDeckCombos, oracleNameMap, ...)
  GeminiPrimerPromptVariant.Build(...)     : IPrimerPromptVariant

Existing analog harness to copy: DeckFlow.Web.Tests/PrimerPromptVariantTests.cs (SampleRequest, SampleCategoryDistribution, SampleComboResult, SampleTop16Entries fixtures; Gemini_OverCap_TrimsWithDisclosure asserts prompt.Length <= 32000). For analysis-variant fixtures, see AnalysisPromptVariantNoExpertContextTests.cs.

Flag default (RESEARCH §"Gemini flag wiring"): Program.cs:78-82 reads DECKFLOW_GEMINI_ENABLED, default false. _AiSelector.cshtml:13,25 hides the radio when false. Server still accepts Gemini if posted directly (documented, existing — NOT a new risk). DeckPrimerPacketService.GetEnabledPlatforms(bool) at :512-518 includes Gemini only when flag true.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add a repeatable Gemini variant size-measurement test (Wave 0)</name>
  <files>DeckFlow.Web.Tests/GeminiVariantSizeTests.cs</files>
  <read_first>
    - DeckFlow.Web.Tests/PrimerPromptVariantTests.cs (full file — the SampleRequest/SampleCategoryDistribution/SampleComboResult/SampleTop16Entries fixtures and Gemini_OverCap_TrimsWithDisclosure prompt.Length assertion pattern to copy)
    - DeckFlow.Web.Tests/AnalysisPromptVariantNoExpertContextTests.cs (analysis-variant construction + Build fixture inputs)
    - DeckFlow.Web.Tests/DeckPrimerPacketServiceTests.cs (CreateService(geminiEnabled:..., includeAllPlatforms:...) helper at ~:141 + :192 — the flag-on fan-out seam to measure the ROUTED Primer-Gemini prompt, not just the bare variant)
    - DeckFlow.Web.Tests/AiPlatformPhase10RoundTripTests.cs (existing proof that TargetAiPlatform="Gemini" routes analysis/comparison/meta-gap to the Gemini variant — cite, do not duplicate)
    - DeckFlow.Web/Services/PromptBuilders/Analysis/GeminiAnalysisPromptVariant.cs (:15 class, :17 cap, :26 Build signature)
    - DeckFlow.Web/Services/PromptBuilders/Comparison/GeminiComparisonPromptVariant.cs (:22 Build signature)
    - DeckFlow.Web/Services/PromptBuilders/MetaGap/GeminiMetaGapPromptVariant.cs (:24 Build signature)
    - DeckFlow.Web/Services/PromptBuilders/Primer/GeminiPrimerPromptVariant.cs (:13 cap, :19 Build, :90 AppendIfFits)
    - 54-RESEARCH.md §"Testing approach for FEAT-01", §"What verified+unblocked means", Pitfall 3 (chars not bytes), Pitfall 5 (analysis 50k cap), Open Question 1 (analysis size unknown)
    - 54-VALIDATION.md Wave 0 Requirements
  </read_first>
  <action>
    Create `DeckFlow.Web.Tests/GeminiVariantSizeTests.cs` (namespace `DeckFlow.Web.Tests`, `public sealed class GeminiVariantSizeTests`). Inject `ITestOutputHelper` via the ctor (xUnit pattern) for emitting measurements. Build a single representative ~100-card cEDH fixture set reused across all four variants — reuse the existing fixtures from PrimerPromptVariantTests / AnalysisPromptVariantNoExpertContextTests where signatures allow (SampleComboResult, SampleTop16Entries, a Commander deck of realistic size). Add one `[Fact]` per Gemini workflow that produces the prompt and captures `prompt.Length`. For ANALYSIS, COMPARISON, META-GAP: instantiate the real variant and call `Build(...)` synchronously (these route to the Gemini variant by user-selected `TargetAiPlatform`, routing already proven by AiPlatformPhase10RoundTripTests — measuring the variant is the representative size). For PRIMER: measure the prompt produced through the FLAG-ON service fan-out — reuse the `CreateService(geminiEnabled: true, includeAllPlatforms: true)` seam from DeckPrimerPacketServiceTests so the measured prompt is the one the real `GetEnabledPlatforms` path emits when the flag is flipped (this is the only workflow whose Gemini emission is gated by the flag fan-out, GetEnabledPlatforms:512-518). Add a Primer assertion that the enabled-platform set actually includes Gemini when the flag is true (proves the flag-flipped path), then measure that prompt's length.

    **DISPOSITION (LOCKED — recorded-measurement, not hard-fail).** Each fact uses char count `prompt.Length` (NOT `Encoding.UTF8.GetByteCount` — Pitfall 3), emits a line via `_output.WriteLine($"{workflow}: {prompt.Length} chars — {(prompt.Length <= 30000 ? "WITHIN" : "OVER")} 30000")`, and the ONLY hard assertion is that Build produced usable output: `Assert.False(string.IsNullOrWhiteSpace(prompt))`. Do NOT add an `Assert.True(prompt.Length <= 30000)` — the ≤30,000 PASS/FAIL verdict is RECORDED in 54-VERIFICATION.md (Task 2), not enforced as a red test, so the suite gate stays deterministic-green while the sizes are surfaced. This honors CONTEXT.md "surface, don't trim": surfacing is the emitted measurement + the VERIFICATION.md table, not a failing assertion. Do NOT add trimming. Name the facts so the workflow is obvious: e.g. `GeminiAnalysis_RepresentativeDeck_MeasuresPromptSize`, `GeminiComparison_...`, `GeminiMetaGap_...`, `GeminiPrimer_...`.

    Expectation per RESEARCH Open Question 1 / Pitfall 5: the Analysis workflow will likely emit "OVER 30000" (its variant has a 50,000 cap and no trimming). That OVER measurement IS the recorded FEAT-01 finding captured in 54-VERIFICATION.md — do NOT loosen, do NOT trim. No production code change. LF line endings; new file may use repo C# conventions.
  </action>
  <verify>
    <automated>"/mnt/c/Program Files/dotnet/dotnet.exe" build DeckFlow.sln</automated>
    <automated>"/mnt/c/Program Files/dotnet/dotnet.exe" test DeckFlow.Web.Tests --filter "FullyQualifiedName~GeminiVariantSizeTests"</automated>
  </verify>
  <acceptance_criteria>
    - File `DeckFlow.Web.Tests/GeminiVariantSizeTests.cs` exists with four facts, one per workflow (analysis, comparison, meta-gap, primer), each calling the real Gemini variant `Build(...)`.
    - `grep -c "prompt.Length" DeckFlow.Web.Tests/GeminiVariantSizeTests.cs` ≥ 4; `grep -c "GetByteCount" DeckFlow.Web.Tests/GeminiVariantSizeTests.cs` == 0 (char-count measure, Pitfall 3).
    - `grep -c "Length <= 30000\|Length<=30000\|Assert.True(prompt.Length" DeckFlow.Web.Tests/GeminiVariantSizeTests.cs` == 0 (no hard size assertion — the ≤30k verdict is recorded in VERIFICATION.md, not enforced as a red test).
    - `dotnet build DeckFlow.sln` clean (0 errors, 0 new warnings).
    - `dotnet test DeckFlow.Web.Tests --filter "FullyQualifiedName~GeminiVariantSizeTests"` exits GREEN (all four facts pass — they assert only non-empty prompt output) and the test output emits one `<workflow>: <N> chars — WITHIN|OVER 30000` line per workflow.
    - The Primer fact measures the prompt via the DeckPrimerPacketService fan-out with `geminiEnabled: true` and asserts the enabled-platform set includes Gemini (proves the flag-flipped path), not just a bare variant Build.
    - SUMMARY/VERIFICATION cites AiPlatformPhase10RoundTripTests as the existing proof that analysis/comparison/meta-gap route to the Gemini variant under TargetAiPlatform=Gemini (must-have #18 satisfied for all four without duplicating routing tests).
    - No production source file modified (only the new test file).
  </acceptance_criteria>
  <done>A repeatable, HTTP-free xUnit test measures each of the four Gemini workflow prompt char counts for a representative deck against the 30,000-char ceiling; build clean; measured sizes observable.</done>
</task>

<task type="auto">
  <name>Task 2: Record verification evidence + findings in 54-VERIFICATION.md</name>
  <files>.planning/phases/54-feature-debt/54-VERIFICATION.md</files>
  <read_first>
    - DeckFlow.Web.Tests/GeminiVariantSizeTests.cs (the measured char counts produced in Task 1)
    - 54-RESEARCH.md §"What verified+unblocked means" (the 7-step record), §"Gemini paste limit — the concrete figure", Pitfall 5
    - 54-CONTEXT.md FEAT-01 LOCKED decisions (default stays false; oversize is a finding not a trim; cover all four workflows)
    - DeckFlow.Web/Program.cs:78-82 (confirm DECKFLOW_GEMINI_ENABLED default false is unchanged)
  </read_first>
  <action>
    Create `.planning/phases/54-feature-debt/54-VERIFICATION.md` recording FEAT-01 evidence. Include: (1) a table with one row per workflow — workflow name, Gemini variant file, measured char count (from Task 1), the 30,000-char ceiling, PASS/FAIL; (2) a Findings section listing any workflow over 30,000 chars (research expects GeminiAnalysisPromptVariant likely over due to its 50,000 cap + no trimming) explicitly noted as a recorded finding with the decision that trimming is DEFERRED this cycle (per CONTEXT.md), NOT implemented here; (3) a confirmation line that `DECKFLOW_GEMINI_ENABLED` default remains `false` in Program.cs (cite line) and was NOT changed; (4) the manual-paste confirmation as a REQUIRED evidence-or-waiver item (NOT an open-ended checklist): a "Manual paste verification" subsection that is either (a) FILLED with operator evidence — flag set true locally, the 4 packets generated, each pasted into Gemini, per-workflow no-truncation result recorded — or (b) explicitly WAIVED-WITH-REASON (e.g. "operator paste deferred this cycle; automated char-count measurement + AiPlatformPhase10RoundTripTests routing coverage stand in"), mirroring the HARD-02 waive-with-reason precedent. State which of (a)/(b) applies so the requirement is not silently left open. Keep this a planning/docs artifact — no code. LF line endings (this repo's planning files follow repo conventions).
  </action>
  <verify>
    <automated>test -f .planning/phases/54-feature-debt/54-VERIFICATION.md && grep -c "FEAT-01" .planning/phases/54-feature-debt/54-VERIFICATION.md</automated>
  </verify>
  <acceptance_criteria>
    - `54-VERIFICATION.md` exists and references FEAT-01.
    - Contains a per-workflow size table with all four workflows (analysis, comparison, meta-gap, primer) and a measured char count + PASS/FAIL each.
    - Any workflow > 30,000 chars is recorded in a Findings section as a deferred-trim finding (not silently shipped, not trimmed).
    - States `DECKFLOW_GEMINI_ENABLED` default remains false (citing Program.cs line) and was not changed.
    - "Manual paste verification" subsection is RESOLVED: either filled with per-workflow operator paste evidence, OR explicitly waived-with-reason (HARD-02 precedent). Not left as an open checkbox.
  </acceptance_criteria>
  <done>54-VERIFICATION.md records the four measured Gemini artifact sizes vs the 30,000 ceiling, surfaces any overage as a deferred finding, confirms default-off unchanged, and carries the operator manual-paste step.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator → DECKFLOW_GEMINI_ENABLED flag | Operator flips the flag in prod. This plan does not change the default; flag is a UI-hide only (server already accepts Gemini posts — existing, documented). |
| Gemini prompt artifact → user clipboard / gemini.google.com | Generated prompt pasted into Gemini; oversize → silent truncation by Gemini (the degradation FEAT-01 exists to surface). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-54-02 | Tampering/Info disclosure | Oversized Gemini artifact silently truncated by Gemini on paste | mitigate | Size-measurement test EMITS each workflow's char count and a WITHIN/OVER-30000 label (recorded-measurement disposition — LOCKED); it does NOT assert `prompt.Length <= 30000` (no red test, suite stays green per CONTEXT.md). Any overage is recorded as a finding in 54-VERIFICATION.md rather than shipped. Trimming deferred per CONTEXT.md — the mitigation is VISIBILITY (emitted measurement + VERIFICATION.md table), not reduction and not a failing assertion. |
| T-54-04 | Elevation/Repudiation | Flag default flipped to true unintentionally | mitigate | Plan explicitly does not modify Program.cs; acceptance criteria + VERIFICATION.md assert DECKFLOW_GEMINI_ENABLED default stays false (cite Program.cs:78-82). No production code touched. |
| T-54-05 | Info disclosure | Raw POST with TargetAiPlatform=Gemini reaching the server when UI hides it | accept | Pre-existing behavior since 2026-05-13 (AiPlatformOptions.cs:16 — UI-hide only). No new attack surface; no PII; output is a prompt string the user already controls. Documented, out of FEAT-01 scope. |
| T-54-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan — RESEARCH.md confirms no new dependencies. N/A; no install task present. |
</threat_model>

<verification>
- `dotnet build DeckFlow.sln` clean (0 errors, 0 new warnings).
- `GeminiVariantSizeTests` runs and emits a char count for each of the four workflows.
- 54-VERIFICATION.md records the size table, findings, and default-off confirmation.
- No production source modified; DECKFLOW_GEMINI_ENABLED default unchanged (false).
- Manual operator paste step left as a documented checklist (external to automation).
</verification>

<success_criteria>
- A repeatable HTTP-free test measures all four Gemini workflow prompt char counts against the 30,000-char ceiling.
- 54-VERIFICATION.md captures the per-workflow sizes, surfaces any overage (esp. analysis) as a deferred-trim finding, and confirms default-off unchanged.
- No code change to enable Gemini; no packet trimming implemented; flag default stays false.
</success_criteria>

<output>
Create `.planning/phases/54-feature-debt/54-02-SUMMARY.md` when done.
</output>
