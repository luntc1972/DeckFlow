# Phase 999.6: v1.3 Ship-Gate Test Residual Cleanup - Context

**Gathered:** 2026-05-22
**Status:** Ready for planning

<domain>
## Phase Boundary

Drive `"/mnt/c/Program Files/dotnet/dotnet.exe" test DeckFlow.sln -c Release --nologo` to `Failed 0` so v1.3 can ship per the new memory rule `[[feedback-no-ship-failing-tests]]` (established 2026-05-22 during Phase 999.5 UAT). Phase 999.5 closed v1.3 quality debt but accepted 9 residual failures under a D-22 "out-of-scope deferral" rationale; that deferral is **revoked** by this phase.

Pre-phase baseline (2026-05-22): `Failed 9, Passed 433, Skipped 3, Total 445`. Triage of the 9:

1. **6 stale tests** — drifted behind shipped production renames.
   - `PacketArtifactStoreTests.LoadFromZip_throws_when_no_response_json_present` — expects msg `"zip did not contain 40-deck-profile.json "`; SUT now emits `"zip did not contain a recognized DeckFlow"` (generalization shipped with AI-agnostic prose work, Phase 999.1-adjacent).
   - 3× `AiPlatformPhase10RoundTripTests.SuggestComparisonZipFileName_includes_lowercased_ai_name(Claude|ChatGPT|Gemini)` — expect filename prefix `atraxa-compare2-`; production now emits `atraxa-comparison-{platform}-{timestamp}` per Phase 12 URL/page rename.
   - `AiPlatformPhase10RoundTripTests.SuggestComparisonZipFileName_includes_compare2_page_segment` — expects substring `-compare2-`; premise dead (no surviving production path emits `compare2`).
   - `PacketArtifactStoreRoundTripTests.LoadFromZip_AlsoRestoresUserInputs_FromArnaFixture` — reads hard-coded `C:\tmp\arna-test\00-input-summary.txt`; dev-machine fixture path, not in repo, fails on every other machine.

2. **1 flaky test** — `BasicAuthMiddlewareTests.CorrectCredentials_InvokesNext` passes 5/5 when filter-isolated (`--filter "FullyQualifiedName~BasicAuthMiddlewareTests"`), fails in full-suite ordering. State leak between facts (shared static / env var / leftover middleware state).

3. **2 ambiguous (debug-needed)** — `ArchidektCacheJobServiceTests.BackgroundService_SucceedsAndUpdatesProcessedCounts` (line 118 `Assert.NotNull`) and `ArchidektCacheJobServiceTests.GetActiveJob_ReturnsNullAfterCompletedJob` (line 170 `Assert.NotNull`). Smells like race (job polled before scheduler runs SUT) but the second test's name vs assertion mismatch ("ReturnsNullAfterCompletedJob" yet asserts NotNull) strongly suggests genuine test-logic confusion OR a real `GetActiveJob` lifecycle bug. Cannot triage stale-vs-real without `/gsd-debug` pass.

**Not in scope:** New test additions beyond regression-locking the fixes themselves, production refactor of `ArchidektCacheJobService` beyond the minimal fix that closes the 2 failures (if a real bug is found), test-framework upgrades (xunit version stays), test parallelism toggles beyond what's required to stabilize the flaky BasicAuth fact (no global `[CollectionDefinition]` overhaul).

</domain>

<decisions>
## Implementation Decisions

### Plan Structure & Sequencing

- **D-01:** **3-plan split.** P01 mechanical stale-test cleanup (6 fixes — Codex single-dispatch unit, no investigation needed). P02 BasicAuth flaky-test root-cause + fix (Claude `/gsd-debug` → Codex fix). P03 Archidekt cache-job /gsd-debug + fix (Claude `/gsd-debug` → Codex fix). Rationale: keeps mechanical edits separate from investigation work per memory rule `[[feedback-codex-codes-claude-reviews]]`. Mirrors 999.5 wave pattern.
- **D-02:** **Strict sequential P01 → P02 → P03.** P01 lands first to clear noise (6/9 failures gone, full-suite gate becomes interpretable). P02 follows (1/9). P03 last (2/9 — may include a production fix). Each plan's full-suite gate confirms incremental progress toward `Failed 0`. No parallel dispatch.
- **D-03:** **No `[Skip]` additions.** Phase closes with `Failed 0` AND `Skipped` count unchanged from pre-phase baseline (3 skipped). Spirit of `[[feedback-no-ship-failing-tests]]`: we must know the code works, not hide failures behind skip directives. If a test genuinely cannot be made deterministic, document + delete instead of skipping (the deletion path is explicitly authorized for the `compare2_page_segment` dead-premise test under D-07; any further deletions need explicit user sign-off).

### P01 — Mechanical Stale-Test Cleanup (6 fixes)

- **D-04:** **`PacketArtifactStoreTests.LoadFromZip_throws_when_no_response_json_present`** — update expected message string. Old: `"zip did not contain 40-deck-profile.json "`. New: verbatim copy from current production SUT (read `PacketArtifactStore.LoadFromZip` source first; do not paraphrase). Single-line `Assert.Equal` update. No SUT change.
- **D-05:** **`AiPlatformPhase10RoundTripTests.SuggestComparisonZipFileName_includes_lowercased_ai_name(Claude|ChatGPT|Gemini)`** — update Theory `Assert.StartsWith` expected prefix from `"atraxa-compare2-"` to `"atraxa-comparison-"` (matches Phase 12 URL/page rename). Single-line edit covers all 3 InlineData rows. Confirm exact prefix by reading the production filename emitter (likely `ChatGptComparisonPromptVariant.cs` or comparison-zip naming helper) before commit.
- **D-06:** **`PacketArtifactStoreRoundTripTests.LoadFromZip_AlsoRestoresUserInputs_FromArnaFixture`** — convert from hard-coded `C:\tmp\arna-test\00-input-summary.txt` to embedded fixture under `DeckFlow.Web.Tests/Fixtures/arna-test/` checked into git. Add `<EmbeddedResource Include="Fixtures\arna-test\**" />` or `<Content CopyToOutput="PreserveNewest">` to `DeckFlow.Web.Tests.csproj`. Reference via `Path.Combine(AppContext.BaseDirectory, "Fixtures", "arna-test", "00-input-summary.txt")`. Source files: copy from current dev `C:\tmp\arna-test\` (orchestrator runs the copy since Codex sandbox can't reach Windows drive C). If files aren't recoverable from dev machine, delete the test instead with explicit DISCUSSION-LOG entry — fixture-less integration assertion has no value.
- **D-07:** **`AiPlatformPhase10RoundTripTests.SuggestComparisonZipFileName_includes_compare2_page_segment`** — **DELETE**. Premise is dead (no production path emits `compare2`). The 3 `lowercased_ai_name(Claude|ChatGPT|Gemini)` Theory rows updated by D-05 already cover the `-comparison-` substring contract via their `Assert.StartsWith` checks. Keeping a dedicated "page segment" test would either duplicate coverage or assert an obsolete contract. Test deletion is authorized; remove the `[Fact]` block at `AiPlatformPhase10RoundTripTests.cs:751` cleanly (no orphaned helpers).
- **D-08:** **P01 atomicity** — one commit per cluster, not one mega-commit. Suggested commit sequence: (1) PacketArtifactStore msg update (D-04), (2) AiPlatform Theory prefix update (D-05, 3 platforms in one commit since they're a single Theory), (3) `compare2_page_segment` deletion (D-07, separate commit so the deletion is reviewable), (4) PacketArtifactStoreRoundTripTests embedded-fixture conversion (D-06, includes csproj + fixture files). Each commit body carries the focused-test triplet per `[[feedback-run-and-update-tests]]`.

### P02 — Flaky BasicAuth State-Leak (1 fix)

- **D-09:** **Root-cause-first investigation via `/gsd-debug`.** Claude runs `/gsd-debug 999.6-P02-basicauth-flaky` to identify the shared mutable state that leaks between facts in full-suite ordering. Hypotheses to test in order: (1) `BasicAuthMiddleware` reads `IConfiguration` / env vars at first request and caches; another test mutates the same env vars; (2) shared static field in middleware or test fixture; (3) `IMemoryCache` / `IDistributedCache` retains auth state across facts; (4) test ordering itself — earlier fact in same class leaves response/context state. Debug session writes to `.planning/debug/999.6-basicauth-flaky.md` per project debug convention.
- **D-10:** **Fix scope determined by debug result.** Codex applies the fix once debug session yields a verdict. Acceptable fix shapes: (a) explicit reset in fact `Arrange` (per-fact env var clear, fresh `IConfiguration`); (b) test fixture redesign (move from static to instance per-fact); (c) middleware change if a real production state-management bug surfaces (escalate per D-12). NOT acceptable: `[Collection("BasicAuthSerial")]` workaround (papers over root cause), `[Fact(Skip=...)]` (violates D-03).
- **D-11:** **Verification gate:** after fix lands, run both `--filter "FullyQualifiedName~BasicAuthMiddlewareTests"` AND full suite. Both must show 0 failures across 5 consecutive runs to declare flaky-test stable.

### P03 — Archidekt Cache-Job Ambiguous Failures (2 fixes, possibly 1 prod bug)

- **D-12:** **1 shared `/gsd-debug` session.** Both failures are in `ArchidektCacheJobServiceTests` and both fire `Assert.NotNull` on background-service job state. Likely same root cause (poll-before-scheduler-runs timing race, OR shared `GetActiveJob` lifecycle bug). Investigate together; one fix may close both. Debug session writes to `.planning/debug/999.6-archidekt-cache-job.md`.
- **D-13:** **Investigation priorities, in order:**
   1. Re-read both test bodies + the SUT (`ArchidektCacheJobService`) to confirm the expected contract for `GetActiveJob()` immediately after a job's lifecycle event. Specific question: does `GetActiveJob_ReturnsNullAfterCompletedJob` ACTUALLY assert NotNull at line 170, or is line 170 a setup `Assert.NotNull` on a job-creation step BEFORE the named "returns null after completed" assertion? (The test name and the failing assertion smell contradictory — read the file in full.)
   2. If the test logic is internally consistent but timing-dependent → race in `GetActiveJob` polling vs background scheduler. Likely fix: deterministic wait via `TaskCompletionSource` or `await job.RunAsync()` direct call instead of `await Task.Delay`.
   3. If there's a real `GetActiveJob` lifecycle bug (e.g., it returns a stale snapshot of `_activeJob` field) → escalate per D-15.
- **D-14:** **No skip-by-collection escape hatch.** Same as D-10 — `[Collection]` workarounds papering over a race are not acceptable. Either the race is fixed in the SUT (deterministic completion signal), or the test is rewritten to await the completion signal explicitly.
- **D-15:** **Real production bug → ship fix in 999.6.** If `/gsd-debug` finds a real `ArchidektCacheJobService` lifecycle bug (not a test-timing issue), the production fix lands inside Phase 999.6 as part of P03. Capture as a sub-plan (P03b) if scope warrants. Mirrors 999.5 D-03 "sweep extension discovered mid-execution" precedent. Do NOT carve out Phase 999.7 — ship-gate work is one phase.
- **D-16:** **Test rename if assertion vs name mismatch confirmed.** If `GetActiveJob_ReturnsNullAfterCompletedJob` is verified to assert NotNull on a job-creation precondition (not the post-completion null check), rename to disambiguate (e.g., `JobLifecycle_ReturnsNullAfterCompletion_AndNotNullDuringRun`) OR split into two facts. Update happens in the same commit as the SUT/test logic fix.

### Cross-Cutting (carried forward from project memory & 999.5 D-18..D-23)

- **D-17:** **Wave dependency.** 999.6 depends on 999.5 closure (already shipped). Within 999.6, strict P01→P02→P03 sequential per D-02. No phase 999.6 dispatch overlaps with v1.3 ship sequence.
- **D-18:** **Atomic commits per cluster.** D-08 governs P01. P02 and P03 each commit fix-and-test-update together — splitting would create intermediate red state. Plus any debug-doc commits (`/gsd-debug` produces `.planning/debug/*.md`).
- **D-19:** **Plain default-author commits.** Per `[[feedback-deckflow-commit-author]]` — `luntc1972 <luntc1972@yahoo.com>` local git config. No `Co-Authored-By` trailer.
- **D-20:** **Build gate** — `"/mnt/c/Program Files/dotnet/dotnet.exe" build DeckFlow.sln -c Release -nologo` exits 0 warnings 0 errors before commit. Same Windows-dotnet path as 999.5 D-21.
- **D-21:** **Test gate per task** — focused gate via `--filter` on the affected test class(es); full-suite gate as the closing step of each PLAN. Commit body carries the focused triplet `Passed X, Failed Y, Skipped Z, filter: <expression>` per `[[feedback-run-and-update-tests]]` + 999.5 D-23.
- **D-22:** **Cross-AI dispatch** — Codex codes test edits + production fixes; Claude orchestrates `/gsd-debug` investigations + reviews per memory rule `[[feedback-codex-codes-claude-reviews]]`. Hybrid pattern from 999.5 (orchestrator owns Windows-dotnet gate if Codex sandbox can't reach it via WSL vsock) carries forward unchanged.
- **D-23:** **STATE.md ship-status governance.** v1.3 `status: ready_to_ship` REVOKED at phase open (already done 2026-05-22 — STATE.md now reads `status: planning`). Restored to `ready_to_ship` only after the closing full-suite gate confirms `Failed 0` and the phase SUMMARY ships. Auto-transition handled by `/gsd-verify-work 999.6` per the standard close-out workflow.

</decisions>

<acceptance>
## Acceptance Criteria

1. `"/mnt/c/Program Files/dotnet/dotnet.exe" test DeckFlow.sln -c Release --nologo` exits 0 with `Failed: 0`.
2. `Skipped` count unchanged from pre-phase baseline (3). No new `[Skip]` directives added.
3. Total test count: 444 (deletes the 1 dead-premise test per D-07) or 445 (if D-06 produces an embedded-fixture variant instead of delete). Acceptance allows either.
4. `dotnet build DeckFlow.sln -c Release` exits 0 warnings 0 errors (baseline preserved from 999.5 close).
5. `.planning/STATE.md` `status` field reverted to `ready_to_ship` only after 1–4 are confirmed by the closing full-suite gate.
6. If `/gsd-debug` (D-09 or D-12) surfaces a real production bug, that bug's fix is shipped inside 999.6 (per D-15). The debug `.md` artifact is committed alongside the fix.
7. No regressions in 999.1–999.5 facts. Specifically: 14 P01-999.5 fixes stay green, 2 P02-999.5 semantic guards stay green, 2 P03-999.5 regression facts stay green, 18 P04-999.5 ResultContractTests stay green.

</acceptance>

<canonical_refs>
## Canonical References

- `.planning/ROADMAP.md` — Phase 999.6 entry at v1.3 milestone list + Phase Details + Progress table (added 2026-05-22).
- `.planning/STATE.md` — `status: planning` (reverted from `ready_to_ship` 2026-05-22); `Current Position` section reflects 999.6 active.
- `.planning/PROJECT.md` — v1.3 milestone goals.
- `.planning/CLAUDE.md` — global codex-codes-claude-reviews + commit-author rules.
- `/home/clunt/.claude/projects/-mnt-c-users-chrislunt-source-personal-deckflow/memory/feedback_no_ship_failing_tests.md` — ship-gate rule established 2026-05-22 (this phase's existence justification).
- `/home/clunt/.claude/projects/-mnt-c-users-chrislunt-source-personal-deckflow/memory/feedback_codex_codes_claude_reviews.md` — Codex/Claude split applied to D-22.
- `/home/clunt/.claude/projects/-mnt-c-users-chrislunt-source-personal-deckflow/memory/feedback_run_and_update_tests.md` — test-triplet commit-body convention.
- `.planning/codebase/TESTING.md` — fake patterns (Pattern 1/2/3), sibling-local doubles convention, file-per-type layout.
- `.planning/phases/999.5-v1-3-backlog-catch-up-test-hardening/999.5-CONTEXT.md` — D-22 deferral rationale (now revoked); D-19/D-20/D-21/D-23 cross-cutting precedent carried forward.
- `.planning/phases/999.5-v1-3-backlog-catch-up-test-hardening/999.5-01-SUMMARY.md` — full-suite delta evidence (445/433/9/3 close-out state).
- Specific test files touched: `DeckFlow.Web.Tests/PacketArtifactStoreTests.cs`, `PacketArtifactStoreRoundTripTests.cs`, `AiPlatformPhase10RoundTripTests.cs:704+751`, `BasicAuthMiddlewareTests.cs`, `ArchidektCacheJobServiceTests.cs:118+170`.
- Specific production files potentially touched (P03 only if real bug confirmed): `DeckFlow.Web/Services/ArchidektCacheJobService.cs`, `DeckFlow.Web/Infrastructure/BasicAuthMiddleware.cs`.

</canonical_refs>

<code_context>
## Reusable Patterns from Codebase

- **Embedded fixture pattern (D-06):** No project-wide convention exists yet for test-bundled fixtures in `DeckFlow.Web.Tests` — first such use. Recommendation: `DeckFlow.Web.Tests/Fixtures/<scope>/<files>` with `<Content Include="Fixtures\**\*" CopyToOutputDirectory="PreserveNewest" />` in csproj, referenced via `AppContext.BaseDirectory`. Mirrors `DeckFlow.Web/Help/**/*.md` pattern from the production project (`<Content>` items copied to output).
- **Background-service test pattern (D-13):** `ArchidektCacheJobService` is registered as both `AddSingleton<ArchidektCacheJobService>` and `AddHostedService<...>(p => p.GetRequiredService<ArchidektCacheJobService>())` per `Program.cs:178-180`. Tests likely instantiate without the hosted-service shell — read `ArchidektCacheJobServiceTests.cs` setup to confirm. Deterministic completion signal (e.g., exposing a `Task CompletedSignal { get; }` for tests) is a candidate fix shape.
- **Middleware test pattern (D-09):** `BasicAuthMiddleware` reads `FEEDBACK_ADMIN_USER` / `FEEDBACK_ADMIN_PASSWORD` env vars per `Program.cs:120-128`. State-leak hypothesis: another test sets these vars and doesn't clear them; full-suite ordering exposes the leak. Check `Environment.SetEnvironmentVariable` calls in any test that touches admin auth.
- **`/gsd-debug` artifact convention:** outputs land in `.planning/debug/<topic>.md`. Phase 999.6 will produce 2 such files (D-09 + D-12). Both committed alongside their respective fix per D-18.

</code_context>

<deferred>
## Deferred Ideas

- **Test-parallelism overhaul** — if D-09 root-cause traces to xunit's default test-collection parallelism, a project-wide `[CollectionDefinition]` strategy could prevent future flaky failures. Out of scope for 999.6 (single-fact stabilization only). Captured for v1.4 test-infrastructure hardening backlog.
- **Background-service deterministic-completion helper** — if D-13 finds the race-fix shape involves exposing a `Task CompletedSignal` from `ArchidektCacheJobService`, the same pattern likely applies to other hosted services in the project. Out of scope; promote to v1.4 if pattern recurs.
- **`/gsd-ship` pre-flight gate** — concept: add an automated pre-flight that runs `dotnet test` and refuses to proceed unless `Failed 0`. Stronger enforcement than memory rule. Backlog candidate; user judgment runs the rule today.
- **CI gate** — same concept but on a GitHub Actions workflow that blocks merge to `main` if any tests fail. Currently no CI exists (local + Render deploy only); CI bootstrap is its own phase.

</deferred>
