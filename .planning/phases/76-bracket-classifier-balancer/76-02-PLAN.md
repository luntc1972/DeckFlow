---
phase: 76-bracket-classifier-balancer
plan: 02
type: execute
wave: 1
depends_on: ["76-01"]
files_modified:
  - DeckFlow.Web/Services/Bracket/IGameChangerCatalogService.cs
  - DeckFlow.Web/Services/Bracket/GameChangerCatalogService.cs
  - DeckFlow.Web/Models/CommanderBracketCatalog.cs
  - DeckFlow.Web/Program.cs
  - DeckFlow.Web.Tests/Bracket/GameChangerCatalogServiceTests.cs
  - DeckFlow.Web.Tests/DeckFlow.Web.Tests.csproj
  - DeckFlow.Web/Services/FeatureFlags/FeatureFlagStore.cs
  - DeckFlow.Web/Services/FeatureFlags/FeatureFlagCatalog.cs
  - DeckFlow.Web.Tests/FeatureFlagCatalogTests.cs
  - DeckFlow.Web.Tests/FeatureFlagStoreSeedTests.cs
  - DeckFlow.Web.Tests/Tools/ToolFlagSeedConsistencyTests.cs
autonomous: true
requirements: [BRACKET-02, BRACKET-05]
must_haves:
  truths:
    - "GameChangerCatalogService loads bracket-data.json into IMemoryCache and returns a populated GameChangerCatalog with 53 Game Changers, 5 non-empty Tiers, and EffectiveDate 2026-02-09"
    - "The catalog is WARM-LOADED at application startup into IMemoryCache (BRACKET-02 'loaded at startup'), via an explicit GetCatalog() call in Program.cs's startup sequence — not lazily on first request"
    - "CommanderBracketCatalog no longer holds a .cs literal of bracket tiers; its Options/Find/IsCedh resolve from bracket-data.json, and the file is copied into BOTH the Web and the Web.Tests output so existing CommanderBracketCatalog-dependent tests still pass from the test bin"
    - "Existing analysis/primer/set-upgrade prompt output is byte-identical after the CommanderBracketCatalog migration (existing parity/size tests stay green)"
    - "tool.bracket.enabled is seeded FALSE in both SQLite and Postgres via ON CONFLICT DO NOTHING and has a non-empty FeatureFlagCatalog description"
    - "ToolFlagSeedConsistencyTests is updated to expect 15 tool flags and to treat tool.bracket.enabled as the first intentionally-dark-launched tool flag (seeded present + disabled); the registry->seed subset contract tolerates a seeded flag with no registry tool yet, so the suite stays green before 76-05 adds the registry entry"
  artifacts:
    - path: "DeckFlow.Web/Services/Bracket/GameChangerCatalogService.cs"
      provides: "JSON->IMemoryCache catalog loader, warm-loaded at startup"
      contains: "IGameChangerCatalogService"
    - path: "DeckFlow.Web/Models/CommanderBracketCatalog.cs"
      provides: "Compatibility shim backed by the versioned catalog (no tier literal)"
      contains: "CommanderBracketCatalog"
    - path: "DeckFlow.Web/Services/FeatureFlags/FeatureFlagStore.cs"
      provides: "tool.bracket.enabled seed row, both dialects"
      contains: "tool.bracket.enabled"
  key_links:
    - from: "DeckFlow.Web/Services/Bracket/GameChangerCatalogService.cs"
      to: "DeckFlow.Web/Data/bracket-data.json"
      via: "File.ReadAllText(Path.Combine(env.ContentRootPath, \"Data\", \"bracket-data.json\"))"
      pattern: "bracket-data\\.json"
    - from: "DeckFlow.Web/Program.cs"
      to: "GameChangerCatalogService"
      via: "AddSingleton + startup warm: app.Services.GetRequiredService<IGameChangerCatalogService>().GetCatalog()"
      pattern: "IGameChangerCatalogService"
---

<objective>
Make the versioned catalog usable at runtime and complete the BRACKET-02 data migration: a `GameChangerCatalogService` that loads `bracket-data.json` into `IMemoryCache` and is WARM-LOADED at application startup, a refactor of the existing `CommanderBracketCatalog` from a `.cs` literal into a thin shim backed by that versioned data (preserving byte-identical prompt output, with the data file copied into the test bin too), and the four-file atomic `tool.bracket.enabled` flag wiring (seeded OFF) plus the seed-consistency test update.

Purpose: BRACKET-02 requires the hardcoded tiers leave the `.cs` literal AND the list be loaded at startup into IMemoryCache; BRACKET-05 requires the flag registered and seeded OFF in both dialects.
Output: catalog service (+ interface, + tests), startup warm call, CommanderBracketCatalog shim, test-bin data copy, flag seed/description + 3 test updates, DI registration.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/76-bracket-classifier-balancer/76-RESEARCH.md
@.planning/phases/76-bracket-classifier-balancer/76-PATTERNS.md
@.planning/phases/76-bracket-classifier-balancer/76-01-SUMMARY.md

<interfaces>
GameChangerCatalog / BracketTier (DeckFlow.Core/Bracket/GameChangerCatalog.cs) — from plan 76-01.
  JSON binding: bracket-data.json uses camelCase keys bound case-insensitively to the PascalCase record
  properties. The bracket-tier array key is `tiers` (matches `Tiers`); `bracketTiers` would NOT bind.
HelpContentService (DeckFlow.Web/Services/HelpContentService.cs) — file-load singleton analog.
Startup warm precedent (DeckFlow.Web/Program.cs ~lines 254-267): a sequence of
  `app.Services.GetRequiredService<...>()` calls warm/ensure schemas+seeds during boot
  (IContentSiteIndexStore.EnsureSchemaAsync, IContentKbSeedLoader.LoadIfPresentAsync,
  IHarvestRunStore/IHarvestScheduleStore/IRequestMetricsStore.EnsureSchemaAsync), each wrapped in an
  info log. Add the catalog warm in that block.
FeatureFlagStore seed idiom: ON CONFLICT (key) DO NOTHING; last existing rows are
  'analysis.manabase.tap-analyzer' and 'analysis.command-zone-awareness' (Postgres FALSE / SQLite 0).
ToolFlagSeedConsistencyTests (DeckFlow.Web.Tests/Tools/ToolFlagSeedConsistencyTests.cs):
  - EnsureSchemaAsync_SeedsAllNewToolFlags: extracts every 'tool.*' key from SqliteSeedSql, asserts
    `Assert.Equal(14, expectedKeys.Count)` and that EVERY tool flag is seeded AND enabled. Adding a tool
    flag bumps the count; a tool flag seeded OFF violates the all-enabled loop unless carved out.
  - RegistryFlagKeys_AreSeeded: `Assert.All(registry.All, tool => Assert.Contains(tool.FlagKey, allowedKeys))`
    — direction is REGISTRY -> SEED (subset). A seeded flag with NO registry tool is tolerated, so seeding
    tool.bracket.enabled here (wave 1) before 76-05 adds the registry entry (wave 4) does NOT make this RED.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: GameChangerCatalogService (JSON -> IMemoryCache) + interface + DI + startup warm + tests</name>
  <read_first>
    - DeckFlow.Web/Services/HelpContentService.cs (analog: file-load singleton, IWebHostEnvironment ContentRootPath)
    - DeckFlow.Web/Services/IHelpContentService.cs (analog: interface shape)
    - DeckFlow.Web/Program.cs (find the HelpContentService AddSingleton line for registration; and the startup warm block ~lines 254-267 — IContentKbSeedLoader.LoadIfPresentAsync / IRequestMetricsStore.EnsureSchemaAsync — for the warm-at-startup call site)
    - .planning/phases/76-bracket-classifier-balancer/76-PATTERNS.md (GameChangerCatalogService.cs section — exact ctor + GetCatalog pattern, incl. internal test-seam ctor)
  </read_first>
  <files>DeckFlow.Web/Services/Bracket/IGameChangerCatalogService.cs, DeckFlow.Web/Services/Bracket/GameChangerCatalogService.cs, DeckFlow.Web/Program.cs, DeckFlow.Web.Tests/Bracket/GameChangerCatalogServiceTests.cs</files>
  <action>
    Create `IGameChangerCatalogService` with one member `GameChangerCatalog GetCatalog();` in namespace `DeckFlow.Web.Services.Bracket`. Implement `GameChangerCatalogService` as a `sealed` singleton: DI ctor `(IWebHostEnvironment env, IMemoryCache cache)` setting `_dataFilePath = Path.Combine(env.ContentRootPath, "Data", "bracket-data.json")`; an `internal` test-seam ctor `(string dataFilePath, IMemoryCache cache)`; `ArgumentNullException.ThrowIfNull` guards. `GetCatalog()` returns the cached `GameChangerCatalog` under cache key `"bracket:game-changer-catalog"`, else `File.ReadAllText` + `JsonSerializer.Deserialize<GameChangerCatalog>` using `new JsonSerializerOptions(JsonSerializerDefaults.Web)` (this sets `PropertyNameCaseInsensitive = true`, which binds the camelCase JSON keys to the PascalCase record properties; the tier array key is `tiers` so it binds to `Tiers`). System.Text.Json binds ISO `yyyy-MM-dd` to `DateOnly` natively in net10.0. On a null/failed deserialize, throw `InvalidOperationException("bracket-data.json could not be loaded")` (fail-closed, do not cache a null), then `_cache.Set(key, catalog, TimeSpan.FromHours(24))`. Register in Program.cs `builder.Services.AddSingleton<IGameChangerCatalogService, GameChangerCatalogService>();` next to the HelpContentService registration. THEN warm the cache at startup: in the Program.cs startup sequence (after `IRequestMetricsStore.EnsureSchemaAsync()` ~line 267, mirroring the existing `app.Services.GetRequiredService<IContentKbSeedLoader>().LoadIfPresentAsync()` warm-at-startup precedent), add an info log and `app.Services.GetRequiredService<IGameChangerCatalogService>().GetCatalog();` so the JSON is parsed into IMemoryCache during boot (satisfies BRACKET-02 "loaded at startup"). `[assembly: InternalsVisibleTo("DeckFlow.Web.Tests")]` is already present (AssemblyInfo.cs) — use the internal test-seam ctor in tests. Write `GameChangerCatalogServiceTests` that points the service at the repo's `DeckFlow.Web/Data/bracket-data.json` (or a temp JSON copy), constructs the service with a real `MemoryCache`, and asserts: GetCatalog returns 53 GameChangers, EffectiveDate == new DateOnly(2026,2,9), exactly 5 Tiers with every tier's Name/Label/Summary non-empty (proves the `tiers`->`Tiers` binding populated every field, not just that load didn't throw), and a second call hits the cache (same reference). Add a fail-closed test pointing the service at a missing/garbage file asserting InvalidOperationException.
  </action>
  <verify>
    <automated>"/mnt/c/Program Files/dotnet/dotnet.exe" test DeckFlow.Web.Tests/DeckFlow.Web.Tests.csproj --filter "FullyQualifiedName~GameChangerCatalogServiceTests"</automated>
  </verify>
  <acceptance_criteria>
    - Test command: `GameChangerCatalogServiceTests` pass — load count 53, effective-date 2026-02-09, exactly 5 Tiers each with non-empty Name/Label/Summary (binding-populated assertion), cache-hit returns same instance, fail-closed throws InvalidOperationException.
    - Source assertion: `grep -c "AddSingleton<IGameChangerCatalogService" DeckFlow.Web/Program.cs` returns 1.
    - Source assertion: Program.cs startup block contains `GetRequiredService<IGameChangerCatalogService>().GetCatalog()` (warm-loaded at startup, not lazy-on-request).
  </acceptance_criteria>
  <done>Catalog service loads the versioned JSON into IMemoryCache, is DI-registered AND warm-loaded at startup, and is covered by passing tests including the populated-Tiers binding assertion and the fail-closed path.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate CommanderBracketCatalog to versioned-data shim + copy data into test bin</name>
  <read_first>
    - DeckFlow.Web/Models/CommanderBracketCatalog.cs (the literal being migrated; preserve the exact public API: Options, Find(string?), IsCedh(string?), CommanderBracketOption record)
    - DeckFlow.Web/Services/PromptBuilders/Analysis/ChatGptAnalysisPromptVariant.cs and DeckFlow.Web/Services/PromptBuilders/Primer/ChatGptPrimerPromptVariant.cs (representative callers — confirm they only call Find/IsCedh/Options and read Value/Label/Summary/TurnsExpectation)
    - DeckFlow.Web/Data/bracket-data.json (the new source of tier text, created in 76-01; array key is `tiers`)
    - DeckFlow.Web.Tests/DeckFlow.Web.Tests.csproj (existing `<Content Include="Manabase\avatar-facts.json">` fixture-copy idiom to mirror for a LINKED Web data file)
  </read_first>
  <files>DeckFlow.Web/Models/CommanderBracketCatalog.cs, DeckFlow.Web.Tests/DeckFlow.Web.Tests.csproj, DeckFlow.Web/Data/bracket-data.json</files>
  <action>
    (A) Refactor `CommanderBracketCatalog` so the 5-tier data is no longer a `.cs` list literal. Replace the literal `Options` initializer with a `Lazy<IReadOnlyList<CommanderBracketOption>>` that reads `bracket-data.json` once from `Path.Combine(AppContext.BaseDirectory, "Data", "bracket-data.json")` (the file is copied to output by 76-01's csproj content item), deserializes the `tiers` array (NOT `bracketTiers`), and projects each tier to `new CommanderBracketOption(Value: tier.Name, Label: tier.Label, Summary: tier.Summary, TurnsExpectation: tier.TurnsExpectation)`. Keep `CommanderBracketOption` record, `Options` (now `=> _options.Value`), `Find(string?)`, and `IsCedh(string?)` with IDENTICAL signatures and semantics so all 17 existing callers compile unchanged. The projected Value/Label/Summary/TurnsExpectation strings MUST equal the previous literal exactly (guaranteed because 76-01 copied them verbatim into the JSON). Do NOT use DI here (static class) — the lazy static file read is intentional and serves the 17 existing flag-INDEPENDENT callers (analysis/primer prompts). Add a `// Why:` comment explaining the literal was migrated to bracket-data.json per BRACKET-02. Preserve existing line endings; touch only changed lines.
    (B) FIX 3 — the shim reads `AppContext.BaseDirectory/Data/bracket-data.json`, but the `<Content>` copy item is only on DeckFlow.Web.csproj, so DeckFlow.Web.Tests' existing CommanderBracketCatalog-dependent tests would hit FileNotFound from the test bin. Add a LINKED content item to DeckFlow.Web.Tests.csproj (mirror the existing avatar-facts.json `<Content>` item) so the Web data file lands in the test output under `Data/`: `<Content Include="..\DeckFlow.Web\Data\bracket-data.json" Link="Data\bracket-data.json"><CopyToOutputDirectory>PreserveNewest</CopyToOutputDirectory></Content>`. Touch only the new ItemGroup/lines; preserve CRLF and existing csproj style.
  </action>
  <verify>
    <automated>"/mnt/c/Program Files/dotnet/dotnet.exe" build DeckFlow.Web.Tests/DeckFlow.Web.Tests.csproj -c Debug && test -f DeckFlow.Web.Tests/bin/Debug/net10.0/Data/bracket-data.json && "/mnt/c/Program Files/dotnet/dotnet.exe" test DeckFlow.Web.Tests/DeckFlow.Web.Tests.csproj --filter "FullyQualifiedName~ResultContractTests|FullyQualifiedName~GeminiVariantSizeTests|FullyQualifiedName~PrimerPromptVariantTests|FullyQualifiedName~AnalysisPromptVariantNoExpertContextTests"</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -c "new(" DeckFlow.Web/Models/CommanderBracketCatalog.cs` returns 0 (no inline tier literals remain) and the file contains `bracket-data.json` and reads the `tiers` array.
    - Test-bin copy: `DeckFlow.Web.Tests/bin/Debug/net10.0/Data/bracket-data.json` exists after build (the linked content item copied it) — proves the existing CommanderBracketCatalog-dependent tests can resolve the file from the test host.
    - Behavior assertion (byte-identity): the existing analysis/primer/size parity tests listed in the verify command all pass unchanged from the test bin — the migration produced no prompt-text drift AND the test host found the data file.
    - `CommanderBracketCatalog.Find("cEDH")` and `IsCedh("cEDH")` behave as before (covered by the existing passing suite).
  </acceptance_criteria>
  <done>CommanderBracketCatalog holds no tier literal, resolves tiers from bracket-data.json, the data file is copied into the test bin, and every existing prompt parity/size test remains green from the test host (byte-identical output).</done>
</task>

<task type="auto">
  <name>Task 3: tool.bracket.enabled flag wiring (seeded OFF) + seed-consistency test update</name>
  <read_first>
    - DeckFlow.Web/Services/FeatureFlags/FeatureFlagStore.cs (PostgresSeedSql + SqliteSeedSql blocks; the last rows are analysis.manabase.tap-analyzer / analysis.command-zone-awareness; ON CONFLICT DO NOTHING idiom)
    - DeckFlow.Web/Services/FeatureFlags/FeatureFlagCatalog.cs (Descriptions dictionary; add after the command-zone-awareness entry)
    - DeckFlow.Web.Tests/FeatureFlagCatalogTests.cs (the [InlineData] theory asserting every seeded flag has a non-empty description)
    - DeckFlow.Web.Tests/FeatureFlagStoreSeedTests.cs (the [InlineData] theory asserting seeded default values)
    - DeckFlow.Web.Tests/Tools/ToolFlagSeedConsistencyTests.cs (EnsureSchemaAsync_SeedsAllNewToolFlags: `Assert.Equal(14, expectedKeys.Count)` + the all-tool-flags-enabled loop; RegistryFlagKeys_AreSeeded is registry->seed subset)
    - .planning/phases/76-bracket-classifier-balancer/76-PATTERNS.md (FeatureFlag* modified sections — exact insert text)
  </read_first>
  <files>DeckFlow.Web/Services/FeatureFlags/FeatureFlagStore.cs, DeckFlow.Web/Services/FeatureFlags/FeatureFlagCatalog.cs, DeckFlow.Web.Tests/FeatureFlagCatalogTests.cs, DeckFlow.Web.Tests/FeatureFlagStoreSeedTests.cs, DeckFlow.Web.Tests/Tools/ToolFlagSeedConsistencyTests.cs</files>
  <action>
    ALL EDITS IN ONE COMMIT (CI fails otherwise). ROADMAP success-criterion 5 requires `tool.bracket.enabled` seeded OFF (flag OFF = prod byte-identical on deploy); this is the FIRST tool-visibility flag deliberately dark-launched, so the seed-consistency test must be updated to allow it.
    (A) In FeatureFlagStore.cs add `('tool.bracket.enabled', FALSE),` to PostgresSeedSql and `('tool.bracket.enabled', 0),` to SqliteSeedSql, placed before the `ON CONFLICT` line, preserving the existing trailing-comma/last-row formatting in each block.
    (B) In FeatureFlagCatalog.cs add the Descriptions entry `["tool.bracket.enabled"] = "Enable the Bracket Check tool — auto-classify a Commander deck into its official 1-5 bracket and generate a balancer prompt. Off = byte-identical to pre-Phase-76.",`.
    (C) In FeatureFlagCatalogTests.cs add `[InlineData("tool.bracket.enabled")]` to the every-seeded-flag-has-description theory.
    (D) In FeatureFlagStoreSeedTests.cs add `[InlineData("tool.bracket.enabled", false)]` to the seed-default theory (bracket is seeded OFF).
    (E) FIX 4 — In ToolFlagSeedConsistencyTests.cs update `EnsureSchemaAsync_SeedsAllNewToolFlags_AndPreservesExistingOverrides`: bump `Assert.Equal(14, expectedKeys.Count)` to `Assert.Equal(15, expectedKeys.Count)`, and adjust the all-tool-flags-enabled loop so `tool.bracket.enabled` is treated as the one intentionally dark-launched tool flag — assert it is SEEDED (present) and DISABLED (false), while every OTHER tool flag remains seeded AND enabled (e.g. iterate expectedKeys: for `tool.bracket.enabled` assert `seeded[key] == false`; for all others assert `seeded[key] == true`). Add a `// Why:` comment that this is the first tool seeded OFF (BRACKET-05 dark-launch). Leave `RegistryFlagKeys_AreSeeded` UNCHANGED — its registry->seed subset direction already tolerates a seeded flag that has no registry tool yet (the registry entry arrives in 76-05), so the suite stays green between waves.
    Do not renumber or reorder existing rows; append/adjust only. The flag is NOT consumed by any controller yet (controller + registry tool arrive in 76-05) — seeding it now is safe and keeps the flag OFF.
  </action>
  <verify>
    <automated>"/mnt/c/Program Files/dotnet/dotnet.exe" test DeckFlow.Web.Tests/DeckFlow.Web.Tests.csproj --filter "FullyQualifiedName~FeatureFlagCatalogTests|FullyQualifiedName~FeatureFlagStoreSeedTests|FullyQualifiedName~ToolFlagSeedConsistencyTests"</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -c "tool.bracket.enabled" DeckFlow.Web/Services/FeatureFlags/FeatureFlagStore.cs` returns 2 (one per dialect).
    - Source assertion: FeatureFlagCatalog.cs contains `["tool.bracket.enabled"]` with a non-empty description string.
    - Source assertion: ToolFlagSeedConsistencyTests.cs asserts `Assert.Equal(15, expectedKeys.Count)` and asserts `tool.bracket.enabled` seeded-and-disabled.
    - Test command: FeatureFlagCatalogTests + FeatureFlagStoreSeedTests + ToolFlagSeedConsistencyTests all pass (description present; bracket seeded false; 15 tool flags; registry subset contract still green with no registry tool yet).
  </acceptance_criteria>
  <done>tool.bracket.enabled is seeded OFF in both dialects with a catalog description; the seed-default, description, and seed-consistency (count=15 + dark-launch carve-out) tests all pass with no RED before 76-05 lands the registry entry.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| bracket-data.json → catalog | Local data file read at startup (service warm) and on first CommanderBracketCatalog access |
| operator flag state → app | tool.bracket.enabled value read from feature-flag store |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-76-03 | Tampering | bracket-data.json parse (GameChangerCatalogService + CommanderBracketCatalog) | mitigate | Fail-closed: deserialize failure throws InvalidOperationException (service) / lazy throws on bad file; never caches null. File is repo-controlled, public card names only. |
| T-76-04 | Denial of Service | repeated file reads | mitigate | IMemoryCache (24h) in the service warmed once at startup; Lazy<T> one-shot read in the shim — no per-request disk I/O. |
| T-76-05 | Elevation of Privilege | flag default leaks tool ON | mitigate | Seeded FALSE/0 in both dialects with ON CONFLICT DO NOTHING; FeatureFlagStoreSeedTests asserts default false; ToolFlagSeedConsistencyTests asserts bracket seeded-and-disabled. |
| T-76-SC | Tampering | npm/pip/cargo installs | accept | No package installs; in-solution only. |
</threat_model>

<verification>
- `dotnet build DeckFlow.sln` clean.
- `dotnet test --filter "FullyQualifiedName~GameChangerCatalogServiceTests"` green (incl. populated-Tiers binding + warm-at-startup).
- Existing analysis/primer/size parity tests green from the test bin (byte-identity preserved; data file copied to test output).
- `dotnet test --filter "FullyQualifiedName~FeatureFlag|FullyQualifiedName~ToolFlagSeedConsistencyTests"` green (description present + seeded false + 15-tool-flag seed contract).
</verification>

<success_criteria>
- BRACKET-02: GameChangerCatalogService loads the versioned JSON into IMemoryCache AND is warm-loaded at startup; CommanderBracketCatalog no longer holds a tier literal and resolves from bracket-data.json (copied into the test bin); existing prompts byte-identical.
- BRACKET-05 (partial): tool.bracket.enabled registered + seeded OFF in both dialects with a catalog description and guard tests; seed-consistency contract updated for the dark-launched tool flag without breaking the registry subset contract across waves.
</success_criteria>

<output>
Create `.planning/phases/76-bracket-classifier-balancer/76-02-SUMMARY.md` when done.
</output>
</content>
