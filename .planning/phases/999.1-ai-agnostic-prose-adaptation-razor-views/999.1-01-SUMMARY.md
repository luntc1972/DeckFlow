---
phase: 999.1-ai-agnostic-prose-adaptation-razor-views
plan: "01"
subsystem: razor-views
tags: [ai-agnostic, prose, DeckAnalysis, hybrid-pattern]
dependency_graph:
  requires: [phase-15-aiplatform-value-object]
  provides: [DeckAnalysis-ai-agnostic-prose]
  affects: [DeckFlow.Web/Views/Deck/DeckAnalysis.cshtml]
tech_stack:
  added: []
  patterns: [AiPlatform.Normalize injection, hybrid above/below selector prose pattern]
key_files:
  modified:
    - DeckFlow.Web/Views/Deck/DeckAnalysis.cshtml
decisions:
  - "Used fully-qualified DeckFlow.Web.Models.AiPlatform.Normalize to match existing file style"
  - "data-chatgpt-step-eyebrow count remained 5 (one per step panel) — plan criterion of 1 was counting one known instance; all 5 preserved byte-identical per D-08"
  - "Line 227 'your AI will infer more' uses lowercase per plan spec to satisfy grep -c 'your AI' >= 10"
metrics:
  duration: "~6 minutes"
  completed: "2026-05-18T18:47:00Z"
  tasks_completed: 3
  tasks_total: 3
  files_modified: 1
---

# Phase 999.1 Plan 01: DeckAnalysis.cshtml Hybrid Prose Adaptation Summary

Adapted `DeckAnalysis.cshtml` to use AI-agnostic prose using the Hybrid pattern — universal
`your AI` noun above the Step 2 `_AiSelector` partial, and `@aiPlatform.DisplayName` injection
below — stripping 27 of 28 ChatGPT prose hits while preserving the Phase 12 enumerated lede and
all Phase 13 D-08 TS/CSS-coupled identifiers.

## Tasks Completed

| Task | Name | Commit | Files |
|------|------|--------|-------|
| 1 | Add AiPlatform.Normalize Razor block | b6d059d | DeckFlow.Web/Views/Deck/DeckAnalysis.cshtml |
| 2 | Rewrite ChatGPT prose using Hybrid pattern | e34366e | DeckFlow.Web/Views/Deck/DeckAnalysis.cshtml |
| 3 | Rename data-busy-title | db88212 | DeckFlow.Web/Views/Deck/DeckAnalysis.cshtml |

## What Was Built

- Added `var aiPlatform = DeckFlow.Web.Models.AiPlatform.Normalize(Model.Request.TargetAiPlatform);` to the top `@{ }` block in `DeckAnalysis.cshtml`, enabling `@aiPlatform.DisplayName` injection throughout below-selector prose.
- Rewrote 11 above-selector ChatGPT hits to use the universal noun `your AI` (info banner, aria-label for workflow tabs, Step 2 instructions list, push-back callout, follow-up panel, analysis context notes).
- Rewrote 15 below-selector ChatGPT hits to inject `@aiPlatform.DisplayName` (deck-versioning hints, conversation title heading + rename prompt, analysis.txt paste instruction, reference.txt hint, deck-profile-schema description, Step 3 intro, set-upgrade instructions, set-select hint, set-upgrade focus hint, set-upgrade-analysis.txt panel with 2 hits, Step 5 intro).
- Renamed `data-busy-title="Building ChatGPT Packets"` to `data-busy-title="Building Deck Analysis Packet"` to match Phase 13 class naming and sibling page neutral titles.

## Acceptance Criteria Results

| Criterion | Expected | Actual | Status |
|-----------|----------|--------|--------|
| `grep -c 'ChatGPT'` after all tasks | 1 | 1 | PASS |
| `grep -c 'AiPlatform.Normalize(Model.Request.TargetAiPlatform)'` | 1 | 1 | PASS |
| `grep -c '@aiPlatform.DisplayName'` | ≥13 | 15 | PASS |
| `grep -c 'your AI'` | ≥10 | 10 | PASS |
| `grep -c 'chatgpt-packets-form'` | 1 | 1 | PASS |
| `grep -c 'data-cache-key="chatgpt-packets"'` | 1 | 1 | PASS |
| `grep -c 'chatgpt-step-eyebrow'` | 5 (baseline) | 5 | PASS |
| `grep -c 'ChatGPT, Claude, or Gemini'` | 1 | 1 | PASS |
| `grep -c 'data-busy-title="Building Deck Analysis Packet"'` | 1 | 1 | PASS |
| `grep -c 'data-busy-title="Building ChatGPT Packets"'` | 0 | 0 | PASS |
| Build (Debug) | success | success | PASS |

## Deviations from Plan

### Auto-noted discrepancies

**1. chatgpt-step-eyebrow baseline count**
- **Found during:** Task 2 verification
- **Issue:** Plan acceptance criterion stated `grep -c 'chatgpt-step-eyebrow'` returns `1`, but the file has 5 (one `<p class="chatgpt-step-eyebrow">Step N</p>` per step panel). The baseline count was also 5 before any edits.
- **Fix:** No fix needed — all 5 instances preserved byte-identical per D-08. The plan criterion was tracking one known instance; reality was 5. Verified no instances were modified.

**2. Windows junction for node_modules (build environment)**
- **Found during:** Task 1 build verification
- **Issue:** The git worktree does not have `DeckFlow.Web/node_modules/typescript` (not tracked in git), so the Windows `dotnet.exe` build failed on the TypeScript compilation step.
- **Fix:** Created a Windows junction point from `<worktree>/DeckFlow.Web/node_modules` → `<main-repo>/DeckFlow.Web/node_modules` using PowerShell `New-Item -ItemType Junction`. The junction was NOT committed (it appears as an untracked directory `??` in git status and is `.gitignore`d via the standard `node_modules` ignore).
- **Impact:** Build environment only; no code changes.

## Phase 13 D-08 Invariants Verified

All Phase 13 D-08 identifiers survive byte-identical:
- `class="deck-form chatgpt-packets-form"` — preserved
- `data-cache-key="chatgpt-packets"` — preserved  
- `data-chatgpt-packets-form` — preserved
- `data-chatgpt-current-step="@currentStep"` — preserved
- `data-chatgpt-ui-mode="guided"` — preserved
- `chatgpt-step-eyebrow` (×5) — preserved
- All other `data-chatgpt-*` attributes — preserved

## Phase 10 Invariants Verified

- `request.TargetAiPlatform` property name — unchanged
- `"ChatGPT"`, `"Claude"`, `"Gemini"` as `AiPlatform.Key` values — untouched (only in `AiPlatform.cs`)
- `targetAiPlatform` form-field name — unchanged (no changes to form fields)

## Known Stubs

None. All prose substitutions are wired to live `AiPlatform.Normalize()` call via `@aiPlatform.DisplayName`.

## Threat Flags

None. This plan modifies only user-visible prose in a Razor view. No new network endpoints, auth paths, file access patterns, or schema changes were introduced.

## Self-Check: PASSED

- [x] `DeckFlow.Web/Views/Deck/DeckAnalysis.cshtml` exists and contains all changes
- [x] Commits b6d059d, e34366e, db88212 exist in git log
- [x] Build exits 0
- [x] `grep -c 'ChatGPT' DeckFlow.Web/Views/Deck/DeckAnalysis.cshtml` = 1
