---
phase: 88-index-row-integrity-hotfix
plan: 03
type: execute
wave: 2
depends_on: [88-01, 88-02]
files_modified:
  - DeckFlow.Studio/ViewModels/DirectPushCoordinator.cs
  - DeckFlow.Studio/ViewModels/PullFromProdCoordinator.cs
  - DeckFlow.Studio/Services/IProdStoreFactory.cs
  - DeckFlow.Studio.Tests/ViewModels/DirectPushCoordinatorTests.cs
  - DeckFlow.Studio.Tests/ViewModels/PullFromProdCoordinatorTests.cs
autonomous: true
requirements: [SYNC-05, SYNC-06]

must_haves:
  truths:
    - "D-05: DirectPushCoordinator.ClassifyDiff keys via the shared ContentNaturalKey helper; the coordinator's private DeriveNaturalKey no longer exists"
    - "ClassifyDiff's dictionary key keeps the shipped U+0000 NULL separator — the Codex MED anti-collision format is preserved, not reverted to a space"
    - "D-10 / D-11: CreateProdStore/ProdStoreFactory builds a ContentSiteIndexStore with schema-ensure disabled — Studio never runs DDL against prod; wiring proven by a factory-seam test here, zero-DDL invariant proven by 88-01's REQUIRED recording-connection test"
    - "D-07: PullFromProdCoordinator reads entry.NaturalKeyType directly (now the stored youtube_channel/podcast_rss vocabulary) instead of re-deriving it"
    - "D-08: not silent in live paths — PullFromProdCoordinator passes its _logger into Classify, and DirectPushCoordinator logs missing-key skips in ClassifyDiff (Codex MED-3)"
    - "D-12: No sync-path comment claims 'no DDL against prod' or an H3 behavior unless that claim is now true"
    - "D-16: After completion the phase folds to local main via fast-forward (early ship cadence) — operator pushes, autodeploy kills the prod bugs ahead of the cycle squash"
  artifacts:
    - path: "DeckFlow.Studio/Services/IProdStoreFactory.cs"
      provides: "ProdStoreFactory.Create constructing a schema-ensure-disabled prod store"
      contains: "ensureSchemaEnabled: false"
    - path: "DeckFlow.Studio/ViewModels/DirectPushCoordinator.cs"
      provides: "Deduped diff keying via ContentNaturalKey + corrected ComputeDiffAsync no-DDL comment"
      contains: "ContentNaturalKey"
  key_links:
    - from: "DeckFlow.Studio/Services/IProdStoreFactory.cs"
      to: "ContentSiteIndexStore ctor"
      via: "ensureSchemaEnabled: false argument"
      pattern: "ensureSchemaEnabled:\\s*false"
    - from: "DeckFlow.Studio/ViewModels/DirectPushCoordinator.cs"
      to: "ContentNaturalKey.TryDerive"
      via: "ClassifyDiff"
      pattern: "ContentNaturalKey"
---

<objective>
Complete the composite-key fix and the DDL guard on the Studio side: delete DirectPushCoordinator's duplicate private `DeriveNaturalKey` so both diff paths call the one shared `ContentNaturalKey` helper (SYNC-05), simplify PullFromProdCoordinator to the stored vocabulary `SyncDiffEntry` now emits, make `ProdStoreFactory` always build a schema-ensure-disabled prod store so Studio never runs DDL against prod (SYNC-06, D-10), and sweep every stale "no DDL against prod" / H3 comment in the sync path so each claim is true (D-12).

Purpose: This wave-2 plan depends on the ctor switch from 88-01 and the helper + vocabulary from 88-02. It wires the consumers together and closes the C4-comment bug.
Output: A deduped, correctly-keyed DirectPushCoordinator; a simplified PullFromProdCoordinator; a `ProdStoreFactory` that disables schema-ensure; corrected comments; updated Studio tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/88-index-row-integrity-hotfix/88-CONTEXT.md
@.planning/phases/88-index-row-integrity-hotfix/88-PATTERNS.md
@.planning/phases/88-index-row-integrity-hotfix/88-01-PLAN.md
@.planning/phases/88-index-row-integrity-hotfix/88-02-PLAN.md
@CLAUDE.md

<interfaces>
<!-- From Plan 88-01 and 88-02 (must be complete before this plan runs). Confirm via read_first. -->

ContentSiteIndexStore ctor (after 88-01): ContentSiteIndexStore(RelationalDatabaseConnection connectionInfo, bool ensureSchemaEnabled = true). Passing false makes EnsureSchemaAsync a no-op — no CREATE/ALTER/DROP.

ContentNaturalKey (after 88-02, DeckFlow.Core/Content/ContentNaturalKey.cs):
  - public static bool TryDerive(ContentSiteIndexRow row, out (string Type, string Value) key)  — returns stored vocabulary; false when neither id present.

SyncDiffEntry.NaturalKeyType (after 88-02): now carries "youtube_channel"/"podcast_rss" (ContentSourceType vocabulary).

Current Studio state to change:
  - DirectPushCoordinator.cs:362-373 — private DeriveNaturalKey (row.YoutubeVideoId is not null ? "youtube" : "podcast", row.PinId ?? "") — DELETE it.
  - DirectPushCoordinator.cs:119-136 — ClassifyDiff builds a Dictionary<string,ContentSiteIndexRow> keyed by the C# interpolation written literally as $"{keyType}\u0000{keyValue}" — the separator is the \u0000 escape for U+0000 NULL, a shipped Codex MED anti-collision fix with an explanatory code comment. PRESERVE this separator exactly; only the per-row derivation call changes to ContentNaturalKey.TryDerive.
  - DirectPushCoordinator.cs:87-91 — ComputeDiffAsync xmldoc falsely claims "the read issues no DDL against prod (H3)"; becomes true once CreateProdStore builds a schema-ensure-off store.
  - DirectPushCoordinator.cs:362-365 — CreateProdStore delegates to _prodStoreFactory.Create(...).
  - IProdStoreFactory.cs — ProdStoreFactory.Create returns `new ContentSiteIndexStore(conn)`; the only place a prod store is constructed (usage limited to Program.cs DI + DirectPushCoordinator + test doubles). No interface signature change needed.
  - PullFromProdCoordinator.cs:154-161 — re-derives keyType from prodRow.YoutubeVideoId; comment at :156 references the short "youtube"/"podcast" vocabulary. Simplify to entry.NaturalKeyType now that it carries the stored vocabulary.

Vocabulary-literal sweep targets (grep confirmed): DirectPushCoordinator.cs:368,372; PullFromProdCoordinator.cs:156; DirectPushCoordinatorTests.cs (collision test); PullFromProdCoordinatorTests.cs:95,279 (NaturalKeyType = "youtube" → "youtube_channel").
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Dedup diff keying + vocabulary ripple (D-05/D-07)</name>
  <files>DeckFlow.Studio/ViewModels/DirectPushCoordinator.cs, DeckFlow.Studio/ViewModels/PullFromProdCoordinator.cs, DeckFlow.Studio.Tests/ViewModels/DirectPushCoordinatorTests.cs, DeckFlow.Studio.Tests/ViewModels/PullFromProdCoordinatorTests.cs</files>
  <read_first>
    - DeckFlow.Studio/ViewModels/DirectPushCoordinator.cs — ClassifyDiff (~109-152, note the \u0000-escape dictionary keys at 122 and 132 and the comment explaining the NULL-separator anti-collision choice) and private DeriveNaturalKey (~362-373)
    - DeckFlow.Studio/ViewModels/PullFromProdCoordinator.cs — lines 154-161 (keyType re-derivation + comment) and line ~118, the bare `ContentSyncDiffClassifier.Classify(prodRows, localRows)` call that must gain the `_logger` argument (Codex MED-3); the coordinator already holds `ILogger<PullFromProdCoordinator> _logger` (field :27)
    - DeckFlow.Core/Content/ContentNaturalKey.cs — the shared helper (from 88-02): TryDerive signature and the ("youtube_channel"/"podcast_rss") vocabulary + false-on-missing contract
    - DeckFlow.Studio.Tests/ViewModels/DirectPushCoordinatorTests.cs — ClassifyDiff_YoutubeAndPodcastShareKeyValue_DoNotCollide (~line 120): the proven collision-test pattern to keep passing (only the call target moves)
    - DeckFlow.Studio.Tests/ViewModels/PullFromProdCoordinatorTests.cs — lines 95, 279 (NaturalKeyType = "youtube" test literals)
  </read_first>
  <behavior>
    - DirectPushCoordinator's existing collision test (YouTube id == podcast key value do not collide) still passes with keying now delegated to ContentNaturalKey.
    - A prod row with neither id is skipped by ClassifyDiff (TryDerive false) exactly as the classifier does — no crash, and the skip emits a structured LogWarning naming the row (D-08).
    - PullFromProdCoordinator applies the approval mirror against the correct prod row using entry.NaturalKeyType directly ("youtube_channel"/"podcast_rss"), with no local YoutubeVideoId re-derivation.
    - Studio tests that assert NaturalKeyType expect the stored vocabulary ("youtube_channel").
  </behavior>
  <action>
    Delete the private DeriveNaturalKey method from DirectPushCoordinator (lines ~362-373) entirely. In ClassifyDiff, replace both per-row derivation calls (the prod-index build and the local-row lookup) with ContentNaturalKey.TryDerive(row, out var nk); skip rows where it returns false; PRESERVE the existing dictionary-key interpolation's U+0000 NULL separator exactly as shipped at lines 122/132 (the \u0000 escape between the type and value components — a Codex MED anti-collision fix; do NOT replace it with a space or any printable character, which would silently revert the hardening). Only the key-derivation call changes; the separator, its explanatory comment (update the comment's vocabulary wording since the key type is now the stored discriminator), and the Dictionary structure stay. Keying remains identical to the 88-02 classifier, which adopts this same NULL-separator format. In PullFromProdCoordinator (lines 154-161), remove the local `keyType = prodRow.YoutubeVideoId is not null ? ContentSourceType.Youtube : ContentSourceType.Podcast` re-derivation and its now-stale comment; use `entry.NaturalKeyType` directly since 88-02 makes it carry the stored vocabulary (keyValue stays `entry.NaturalKeyValue`). Update the Studio test literals: in DirectPushCoordinatorTests keep the collision test's structure but expect the stored vocabulary where it asserts NaturalKeyType; in PullFromProdCoordinatorTests change `NaturalKeyType = "youtube"` at lines ~95 and ~279 to `NaturalKeyType = ContentSourceType.Youtube` ("youtube_channel"). Add `using DeckFlow.Core.Content;` where needed for ContentNaturalKey. D-08 LIVE WIRING (Codex MED-3 — the optional logger seam from 88-02 must not stay unwired): in PullFromProdCoordinator change the line-118 call to `ContentSyncDiffClassifier.Classify(prodRows, localRows, _logger)` (the field already exists at :27). DirectPushCoordinator currently has NO logger (verified — zero ILogger references in the file): add an optional `ILogger<DirectPushCoordinator>? logger = null` parameter to its ctor stored as `_logger = logger ?? NullLogger<DirectPushCoordinator>.Instance` (house optional-logger convention, e.g. CommanderSpellbookService — the optional default keeps every existing construction site and test compiling); add an optional `ILogger? logger = null` parameter to the static ClassifyDiff and emit a structured LogWarning naming the row (title + source) when ContentNaturalKey.TryDerive returns false; pass `_logger` at the ComputeDiffAsync call site. Add a Studio test fact asserting the skip-log path: ClassifyDiff with a row lacking both ids plus a recording ILogger fake records exactly one warning naming the row.
  </action>
  <verify>
    <automated>/mnt/c/Program\ Files/dotnet/dotnet.exe test DeckFlow.Studio.Tests --filter "FullyQualifiedName~DirectPushCoordinatorTests|FullyQualifiedName~PullFromProdCoordinatorTests"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "DeriveNaturalKey" DeckFlow.Studio/ViewModels/DirectPushCoordinator.cs` returns NOTHING (private method deleted)
    - `grep -n "ContentNaturalKey.TryDerive" DeckFlow.Studio/ViewModels/DirectPushCoordinator.cs` returns hits in the ClassifyDiff paths
    - `grep -c 'u0000' DeckFlow.Studio/ViewModels/DirectPushCoordinator.cs` is >= 2 — the U+0000 NULL-separator escape is still present in both ClassifyDiff dictionary-key interpolations (separator preserved, not reverted to a space)
    - `grep -n "\"youtube\"\|\"podcast\"" DeckFlow.Studio/ViewModels/DirectPushCoordinator.cs DeckFlow.Studio/ViewModels/PullFromProdCoordinator.cs` returns NOTHING (short-string literals swept)
    - PullFromProdCoordinator uses `entry.NaturalKeyType` for keyType (no YoutubeVideoId re-derivation remains)
    - `grep -n "Classify(prodRows, localRows, _logger)" DeckFlow.Studio/ViewModels/PullFromProdCoordinator.cs` returns a hit (D-08 logger wired into the live Pull path)
    - `grep -n "ILogger" DeckFlow.Studio/ViewModels/DirectPushCoordinator.cs` returns hits (coordinator gained a logger), ClassifyDiff has an optional `ILogger? logger` parameter, and the skip-log fact passes
    - `grep -n "NaturalKeyType = \"youtube\"" DeckFlow.Studio.Tests/ViewModels/PullFromProdCoordinatorTests.cs` returns NOTHING
    - The DirectPush collision test and Pull tests pass under `dotnet test`
  </acceptance_criteria>
  <done>Both diff paths key through the single ContentNaturalKey helper with the preserved U+0000 NULL separator; the coordinator duplicate is gone; the stored vocabulary flows through Pull; Studio tests green.</done>
</task>

<task type="auto">
  <name>Task 2: ProdStoreFactory schema-ensure-off + sync-path comment sweep (D-10/D-12)</name>
  <files>DeckFlow.Studio/Services/IProdStoreFactory.cs, DeckFlow.Studio/ViewModels/DirectPushCoordinator.cs, DeckFlow.Studio/ViewModels/PullFromProdCoordinator.cs, DeckFlow.Studio.Tests/ViewModels/DirectPushCoordinatorTests.cs</files>
  <read_first>
    - DeckFlow.Studio/Services/IProdStoreFactory.cs — ProdStoreFactory.Create returning `new ContentSiteIndexStore(conn)`; the D-03 on-demand construction comment
    - DeckFlow.Core/Content/ContentSiteIndexStore.cs — the ensureSchemaEnabled ctor parameter added by 88-01 (confirm signature/default)
    - DeckFlow.Studio/ViewModels/DirectPushCoordinator.cs — ComputeDiffAsync xmldoc (~87-91, false "no DDL against prod (H3)" claim) and CreateProdStore (~362-365)
    - DeckFlow.Studio/ViewModels/PullFromProdCoordinator.cs — any no-DDL / H3-style behavioral comments (grep the file)
    - .planning/phases/88-index-row-integrity-hotfix/88-CONTEXT.md — D-10 (ALL prod-pointed stores, always) and D-12 (sweep ALL sync-path no-DDL/H3 claims, not just the cited one)
  </read_first>
  <action>
    In ProdStoreFactory.Create, change the store construction to `new ContentSiteIndexStore(conn, ensureSchemaEnabled: false)` so every prod-pointed store built by the factory disables schema-ensure — Studio never runs DDL against prod on reads OR writes (D-10); prod schema is owned by the web app's startup/seed path. Keep the IProdStoreFactory interface signature unchanged (no overload needed — the factory is the only prod-store construction site). Add a short `// Why:` comment at the construction point stating prod schema is web-app-owned and the factory intentionally disables schema-ensure (SYNC-06). Then sweep the sync path for stale behavioral claims (D-12): correct DirectPushCoordinator.ComputeDiffAsync's xmldoc (~87-91) so the "no DDL against prod (H3)" statement is now accurate (it is true once the factory disables schema-ensure — keep and correct the wording, don't just delete); grep DirectPushCoordinator and PullFromProdCoordinator for any other "no DDL"/"H3"/"issues no DDL"/"intentionally absent" style claims and make each true or delete it. D-11 division of labor (Codex MED-2): the REQUIRED recording-connection no-DDL test (zero CREATE/ALTER/DROP on the DirectPush read+write paths, catching attempted DDL against an existing-but-older schema) lives at the STORE level in Plan 88-01 Task 3 — do not duplicate it here. This task adds the complementary FACTORY-WIRING test proving `ensureSchemaEnabled: false` actually reaches the store ProdStoreFactory builds: add a Studio test fact that calls `new ProdStoreFactory().Create(...)` with an unreachable-but-well-formed Postgres connection string (e.g. Host=127.0.0.1;Port=1;Database=x;Username=x;Password=x;Timeout=1) and asserts `EnsureSchemaAsync()` completes WITHOUT throwing — with the switch off it early-returns before opening any connection, whereas a regression to schema-ensure-ON would attempt the dead connection and throw. The 88-03 SUMMARY must cross-reference 88-01's recording-connection test as the D-11 closer, with this fact as the wiring proof.
  </action>
  <verify>
    <automated>/mnt/c/Program\ Files/dotnet/dotnet.exe test DeckFlow.Studio.Tests --filter "FullyQualifiedName~DirectPushCoordinatorTests"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "ensureSchemaEnabled: false" DeckFlow.Studio/Services/IProdStoreFactory.cs` returns a hit in ProdStoreFactory.Create
    - `grep -rn "no DDL against prod\|issues no DDL\|(H3)" DeckFlow.Studio/ViewModels/DirectPushCoordinator.cs DeckFlow.Studio/ViewModels/PullFromProdCoordinator.cs` — every remaining hit describes behavior that is now actually true (schema-ensure disabled); no false claim survives
    - The ComputeDiffAsync xmldoc no-DDL statement is retained and accurate (not silently deleted)
    - A Studio factory-wiring fact passes: `new ProdStoreFactory().Create(<unreachable Postgres conn string>)` then `EnsureSchemaAsync()` completes without throwing (early return proves the switch reached the store; a regression to ensure-ON would attempt the dead connection and throw)
    - The 88-03 SUMMARY cross-references 88-01's recording-connection test as the D-11 closer (this plan proves wiring, 88-01 proves the zero-DDL invariant)
    - `dotnet build` of DeckFlow.Studio is clean, no new warnings
  </acceptance_criteria>
  <done>The prod store factory disables schema-ensure so Studio never DDLs prod; every sync-path no-DDL comment is true; factory wiring is test-proven and D-11's zero-DDL invariant is closed by 88-01's recording-connection test.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Studio (operator workstation) → prod Postgres | DirectPush read+write and Pull cross into prod; unintended DDL mutates the live schema; a mis-keyed diff targets the wrong row |
| shared keying contract (Core) ↔ two Studio consumers | If the coordinator re-implements keying, the two diff paths can silently diverge again |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-88-02 | Tampering | ProdStoreFactory / CreateProdStore → prod schema | mitigate | Factory always builds ContentSiteIndexStore(ensureSchemaEnabled:false); Studio never issues CREATE/ALTER/DROP against prod (D-10); factory-seam wiring fact here + 88-01 recording-connection test lock it |
| T-88-03 | Tampering | DirectPushCoordinator.ClassifyDiff keying | mitigate | Delete the duplicate DeriveNaturalKey; route through the shared ContentNaturalKey helper while preserving the U+0000 NULL-separator dictionary key so keying can never diverge from the classifier and the anti-collision hardening is not reverted (D-05) |
| T-88-05 | Repudiation | Stale "no DDL against prod" comments | mitigate | Sweep D-12: every sync-path no-DDL/H3 claim is made true or deleted so the code's documented behavior matches reality (C4-comment closed) |
| T-88-SC | Tampering (supply chain) | npm/NuGet installs | accept | No new packages; reuses existing xUnit Studio test project |
</threat_model>

<verification>
- `dotnet test DeckFlow.Studio.Tests` passes (dedup collision test, Pull vocabulary tests, skip-log fact, factory wiring fact).
- `dotnet build` of the full solution clean, no new warnings.
- `bash scripts/format-check-changed.sh` passes — changed-lines format gate (CLAUDE.md; run against the staged/changed C# lines before commit).
- Grep confirms zero short-string `"youtube"`/`"podcast"` literals remain in the Studio sync path, DeriveNaturalKey is gone, the U+0000 separator escape is still present in ClassifyDiff, and `ensureSchemaEnabled: false` is present in ProdStoreFactory.
- Cross-check: with 88-01 + 88-02 + this plan complete, all three phase success criteria hold — approval mirror + serve filter (C1), composite keying end to end (C4-collision), no-DDL + true comments (C4-comment).
</verification>

<success_criteria>
- The coordinator's duplicate keying is gone; both diff paths share ContentNaturalKey with the preserved U+0000 NULL-separator key format (SYNC-05 fully closed).
- Studio never runs DDL against prod, and every sync-path no-DDL comment is accurate (SYNC-06 fully closed).
</success_criteria>

<output>
Create `.planning/phases/88-index-row-integrity-hotfix/88-03-SUMMARY.md` when done.
</output>
