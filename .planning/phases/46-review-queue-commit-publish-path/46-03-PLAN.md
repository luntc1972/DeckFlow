---
phase: 46-review-queue-commit-publish-path
plan: 03
type: execute
wave: 2
depends_on: ["46-01"]
files_modified:
  - DeckFlow.Studio/Pages/Review.razor
  - DeckFlow.Studio/Shared/NavMenu.razor
autonomous: false
requirements: [REVQ-02, REVQ-03]
user_setup: []
must_haves:
  truths:
    - "The review queue lists distilled site-index entries with a per-row approval badge, row checkboxes + Approve/Reject-selected bulk buttons, and status filter tabs (Pending/Approved/Rejected/All) (D-07)"
    - "Clicking Approve/Reject on a row writes approval_status immediately (optimistic UI, no separate Save step) and updates the badge + row tint in place (D-05)"
    - "Selecting rows reveals a batch bar; Approve/Reject Selected writes all checked rows in one round-trip"
    - "Expanding a row renders an inline expand/collapse preview that reads the markdown artifact via the CORRECT resolver (no doubled content-kb segment) combined with the DB tag sets, cached per expand (D-08/D-09); a genuinely missing artifact shows a warning and disables Approve while still allowing Reject (D-10)"
    - "Review and Publish nav entries appear below Harvest in the order Home / Harvest / Review / Publish"
  artifacts:
    - path: "DeckFlow.Studio/Pages/Review.razor"
      provides: "Review queue page (REVQ-02/03)"
      min_lines: 200
      contains: "SetApprovalStatusAsync"
    - path: "DeckFlow.Studio/Shared/NavMenu.razor"
      provides: "Review + Publish nav entries"
      contains: "href=\"review\""
  key_links:
    - from: "Review.razor approve/reject handlers"
      to: "IContentSiteIndexStore.SetApprovalStatusAsync"
      via: "@inject IContentSiteIndexStore + per-row natural key"
      pattern: "SetApprovalStatusAsync"
    - from: "Review.razor expand handler"
      to: "stored artifact_path resolved under the data root (parent of ArtifactRoot)"
      via: "Path.Combine(dataRoot, row.ArtifactPath) + containment guard, Task.Run File.ReadAllText cached in _expandCache"
      pattern: "GetFullPath"
---

<objective>
Build `DeckFlow.Studio/Pages/Review.razor` — the review queue (REVQ-02/03) — and add both new nav
entries (Review + Publish) to `NavMenu.razor`. The operator filters distilled entries by approval
status, previews each entry's markdown content + tags inline, and approves/rejects individually or in
filtered batches. Every approve/reject writes `approval_status` immediately (D-05, optimistic UI) via
the Plan 01 `SetApprovalStatusAsync`.

Purpose: This is the human gate that decides what ships to deckflow.gg. It mirrors `Harvest.razor`
exactly for page shape, status badges, checkbox/select-all, disposal-cancel, and disposal-safe
StateHasChanged marshalling.

Output: One Blazor page implementing the full UI-SPEC Review contract + the NavMenu entries.

**This plan owns NavMenu** (adds BOTH Review and Publish nav entries) so Plan 04 does not touch it —
keeping Plan 03 and Plan 04 free of file overlap within Wave 2.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/46-review-queue-commit-publish-path/46-CONTEXT.md
@.planning/phases/46-review-queue-commit-publish-path/46-PATTERNS.md
@.planning/phases/46-review-queue-commit-publish-path/46-UI-SPEC.md
@DeckFlow.Studio/Pages/Harvest.razor
@DeckFlow.Studio/Shared/NavMenu.razor

<interfaces>
<!-- Use directly. -->

IContentSiteIndexStore (already DI-registered in Studio Program.cs:50). Methods this page uses:
  Task EnsureSchemaAsync(ct)
  Task<IReadOnlyList<ContentSiteIndexRow>> GetAllRowsAsync(ct)   ← load the full queue, group/filter by ApprovalStatus client-side
  Task<int> SetApprovalStatusAsync(string naturalKeyType, string naturalKeyValue, string status, ct)   ← per-row (Plan 01)
  Task<int> SetApprovalStatusAsync(IReadOnlyList<(string Type,string Value)> keys, string status, ct)   ← batch (Plan 01)

ContentSiteIndexRow (DeckFlow.Core/Knowledge/ContentArtifactSpec.cs):
  long Id; string Source; string Title; string VideoUrl; string ArtifactPath;
  string ApprovalStatus (pending/approved/rejected); IReadOnlyList<string> ArchetypeTags, BracketTags, CardCategoryTags;
  string? YoutubeVideoId; string? RssGuid.
  Natural key for SetApprovalStatusAsync: YoutubeVideoId non-blank → (ContentSourceType.Youtube, YoutubeVideoId); else (ContentSourceType.Podcast, RssGuid).

ARTIFACT PATH RESOLUTION (verified live — the previous plan double-prefixed and resolved every artifact MISSING):
  ContentArtifactWriter.ComputeRelativeArtifactPath (ContentArtifactWriter.cs:18-22) returns
  "content-kb/{sourceSlug}/{id}.md" — row.ArtifactPath ALREADY carries the leading "content-kb/" segment.
  Studio configures ContentKbOrchestratorOptions.ArtifactRoot = {studioDataDir}/content-kb (Program.cs:42,95),
  and ContentArtifactWriter.WriteFile writes the file at {ArtifactRoot}/{sourceSlug}/{id}.md =
  {studioDataDir}/content-kb/{sourceSlug}/{id}.md (ContentArtifactWriter.cs:100-113).
  Therefore the on-disk file lives at:  {studioDataDir}/content-kb/{sourceSlug}/{id}.md
  And the stored ArtifactPath equals:    content-kb/{sourceSlug}/{id}.md
  CORRECT resolver: combine the stored path with the DATA ROOT (the PARENT of ArtifactRoot), so the
  single "content-kb/" segment is honored exactly ONCE:
      dataRoot      = Directory.GetParent(Options.ArtifactRoot)!.FullName   // {studioDataDir}
      artifactAbs   = Path.GetFullPath(Path.Combine(dataRoot, row.ArtifactPath))
  WRONG (the bug being fixed): Path.Combine(Options.ArtifactRoot, row.ArtifactPath) →
      {studioDataDir}/content-kb/content-kb/{sourceSlug}/{id}.md  (doubled segment → always missing).

Harvest.razor patterns to copy (see 46-PATTERNS.md for exact line cites):
  @page/@implements IDisposable/@using/@inject header; [Inject] props = default!;
  checkbox table + select-all (:81-120); RenderFragment status-badge switch (:1450-1458);
  spinner-locked batch button (:484-510); off-context IO via Task.Run (:800);
  disposal-safe InvokeAsync sink (:1097-1107); Dispose cancels CTS (:1469-1479);
  optimistic immediate-write no-spinner (:1224-1239).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: NavMenu Review + Publish entries</name>
  <files>DeckFlow.Studio/Shared/NavMenu.razor</files>
  <read_first>
    - DeckFlow.Studio/Shared/NavMenu.razor (existing Home + Harvest entries, :12-21)
    - .planning/phases/46-review-queue-commit-publish-path/46-UI-SPEC.md (lines 137-154, exact nav markup)
  </read_first>
  <action>
    Insert two nav-item blocks immediately AFTER the existing Harvest nav-item, using the exact UI-SPEC markup:
      review entry: NavLink href="review", icon span oi oi-task, label " Review".
      publish entry: NavLink href="publish", icon span oi oi-cloud-upload, label " Publish".
    Match the surrounding indentation/markup of the Harvest entry exactly. Final order: Home / Harvest / Review / Publish. Touch only the inserted lines (changed-lines format gate). Preserve LF.
  </action>
  <verify>
    <automated>cd /mnt/c/users/chrislunt/source/personal/deckflow && grep -c 'href="review"\|href="publish"' DeckFlow.Studio/Shared/NavMenu.razor</automated>
  </verify>
  <acceptance_criteria>
    - href="review" with oi-task and href="publish" with oi-cloud-upload both present.
    - The review/publish blocks appear after the harvest block (order Home/Harvest/Review/Publish).
  </acceptance_criteria>
  <done>NavMenu shows Review and Publish below Harvest in the specified order.</done>
</task>

<task type="auto">
  <name>Task 2: Review.razor — queue, filter tabs, badges, expand (correct artifact resolver), optimistic approve/reject, batch</name>
  <files>DeckFlow.Studio/Pages/Review.razor</files>
  <read_first>
    - DeckFlow.Studio/Pages/Harvest.razor (FULL — the canonical Studio page shell, badge/checkbox/Task.Run/Dispose/InvokeAsync patterns)
    - .planning/phases/46-review-queue-commit-publish-path/46-UI-SPEC.md (Review.razor sections: Page Structure, Filter Tabs, Queue Table, Inline Expand Row, Batch Action Bar, State Machine, Copywriting, Accessibility — implement these exactly)
    - .planning/phases/46-review-queue-commit-publish-path/46-PATTERNS.md (Review.razor pattern assignments + "No Analog Found" expand-row + filter-tab + _expandCache guidance)
    - DeckFlow.Core/Knowledge/ContentArtifactSpec.cs:107-163 (ContentSiteIndexRow surface)
    - DeckFlow.Core/Knowledge/ContentArtifactWriter.cs:18-22,100-113 (proves ArtifactPath already includes the leading content-kb/ segment — drives the resolver below)
  </read_first>
  <action>
    Create DeckFlow.Studio/Pages/Review.razor: @page "/review", @implements IDisposable, @using DeckFlow.Core.Content / .Knowledge / .Orchestration / DeckFlow.Studio.Services as needed. Inject IContentSiteIndexStore IndexStore and ContentKbOrchestratorOptions Options (= default!). Wrap content in <article class="content px-4"> with the UI-SPEC h1 "Review Queue" + subtitle.

    LOAD: in OnInitializedAsync await IndexStore.EnsureSchemaAsync then GetAllRowsAsync; project each row to a small view-model record/class (Id, Title, VideoUrl, ApprovalStatus, the three tag lists, the derived natural key (Type,Value), ArtifactPath, plus mutable bool Selected and bool Expanded). Hold them in a List field. Do the load inside Task.Run off the sync context, then InvokeAsync(StateHasChanged).

    ARTIFACT PATH RESOLVER (FIX — do NOT double the content-kb segment): add a private helper that resolves the absolute artifact path for a row from the stored ArtifactPath, which ALREADY begins with "content-kb/". Resolve against the DATA ROOT = the parent of Options.ArtifactRoot, then normalize + containment-guard:
        var dataRoot = Directory.GetParent(Options.ArtifactRoot)?.FullName ?? Options.ArtifactRoot;
        var artifactAbs = Path.GetFullPath(Path.Combine(dataRoot, row.ArtifactPath));
      CONTAINMENT GUARD: treat the artifact as missing/invalid (cache null) — never throw, never read — if row.ArtifactPath is rooted (Path.IsPathRooted), contains a ".." traversal segment, or the normalized artifactAbs does NOT start with Path.GetFullPath(dataRoot) + Path.DirectorySeparatorChar. Add a `// Why: stored ArtifactPath already carries the content-kb/ prefix; resolve under the data root (parent of ArtifactRoot) so the segment isn't doubled — combining with ArtifactRoot directly resolves every file MISSING. Containment guard rejects rooted/.. paths.` comment at this helper.

    FILTER TABS: Bootstrap nav nav-tabs with Pending/Approved/Rejected/All, Pending active on load. Each tab label carries a count badge (Pending bg-secondary, Approved bg-success, Rejected bg-danger, All bg-secondary), recomputed from the in-memory list grouped by ApprovalStatus. The visible queue = the list filtered by the active tab. Switching tabs clears all Selected flags and dismisses the batch bar.

    QUEUE TABLE: table table-sm table-hover align-middle. Columns: select-all-header checkbox | Title (text-truncate max-width 300px, with external <a href=VideoUrl target=_blank rel="noopener noreferrer">) | Tags (up to 3 badge bg-light text-dark border me-1, then "+N more") | Approval badge | Approve Entry / Reject Entry buttons | Expand toggle. Row tint: approved→table-success, rejected→table-danger, pending→none. Approval badge via a RenderFragment switch (pending→badge bg-secondary "Pending", approved→bg-success "Approved", rejected→bg-danger "Rejected"). When the row's artifact is known-missing (cache value null after an expand attempt), also render a badge bg-warning text-dark "Artifact missing" beside it.

    PER-ROW APPROVE/REJECT (D-05 optimistic, no spinner): handler calls IndexStore.SetApprovalStatusAsync(naturalKeyType, naturalKeyValue, status, _cts.Token); on success mutate the vm.ApprovalStatus in place, recompute tab counts, StateHasChanged. Approve Entry disabled when row already approved OR artifact known-missing (D-10); its aria-label switches to "Approve Entry disabled — artifact file missing" when disabled for missing artifact. Reject Entry disabled when already rejected. Reject is ALWAYS allowed even when artifact missing.

    EXPAND (D-08/D-09): an expand toggle (btn btn-sm btn-link, oi-chevron-bottom/top, aria-expanded, aria-label per copy contract). On expand: if not in _expandCache (Dictionary<string,string?> keyed by natural key value), resolve the artifact path via the ARTIFACT PATH RESOLVER helper above, then read the file inside Task.Run; on FileNotFoundException/IOException/containment-rejection store null (graceful degradation — never crash). A full-width <tr colspan=...> below the parent (bg-light, p-3) shows: <h3 class="h6 fw-semibold mb-1">Content Preview</h3> + a <pre bg-white border rounded p-2 style max-height:400px;overflow-y:auto;font-size:0.875rem;font-family:monospace> of the cached text; a <dl class="row mb-0"> of Archetype/Bracket/Card-category tags (dt col-sm-3, dd col-sm-9, each tag a badge bg-light text-dark border me-1); OR, when cache value is null, the alert alert-warning py-2 (oi-warning) with the exact missing-artifact copy from the UI-SPEC instead of the pre block.

    BATCH BAR (visible only when >=1 Selected): d-flex gap-2 align-items-center mb-3 with "Approve Selected (N)" (btn btn-primary btn-sm) + "Reject Selected (N)" (btn btn-outline-danger btn-sm) + "N entries selected". (N) = count of eligible checked rows (rows the action would actually change; Approve excludes already-approved and artifact-known-missing; Reject excludes already-rejected). Handlers gather the eligible checked rows' natural keys, set _operationInFlight=true, call the BATCH SetApprovalStatusAsync(keys, status, _cts.Token) inside Task.Run; in finally clear _operationInFlight and InvokeAsync(StateHasChanged). After completion: clear all Selected, recompute counts, re-render. Show the spinner-locked button state during the batch op (visually-hidden Loading + aria-label "Operation in progress").

    EMPTY STATES: per active tab, render the exact UI-SPEC empty-state copy in <p class="text-muted"> when the filtered list is empty.

    DISPOSAL: private CancellationTokenSource _cts = new(); Dispose() cancels + disposes it. All async store/file ops use _cts.Token and swallow ObjectDisposedException/InvalidOperationException from a dropped circuit inside the InvokeAsync sink.

    Do NOT reference DeckFlow.CLI. Do NOT add any spend/distill logic. Preserve LF; follow Bootstrap-only styling (no new CSS files, no npm). All sizes/colors per the UI-SPEC color + typography tables.
  </action>
  <verify>
    <automated>cd /mnt/c/users/chrislunt/source/personal/deckflow && "/mnt/c/Program Files/dotnet/dotnet.exe" build DeckFlow.Studio/DeckFlow.Studio.csproj -c Debug 2>&1 | grep -E "error|Build succeeded"</automated>
  </verify>
  <acceptance_criteria>
    - Review.razor exists, @page "/review", @implements IDisposable, injects IContentSiteIndexStore and ContentKbOrchestratorOptions.
    - Calls both SetApprovalStatusAsync overloads (grep: single per-row call + batch list call).
    - Artifact resolver uses the DATA ROOT (parent of ArtifactRoot), NOT Path.Combine(Options.ArtifactRoot, row.ArtifactPath): grep proves Directory.GetParent(Options.ArtifactRoot) (or equivalent parent-of-ArtifactRoot derivation) is used AND there is NO Path.Combine(Options.ArtifactRoot, row.ArtifactPath) occurrence (the doubled-prefix bug must be absent).
    - Containment guard present: grep Path.GetFullPath AND a rejection of rooted/".." paths; missing/invalid path stores null and renders the alert-warning (grep "alert-warning" + a catch for FileNotFoundException/IOException).
    - Filter tabs Pending/Approved/Rejected/All present with count badges; Pending active on load.
    - Dispose() cancels the CTS; no DeckFlow.CLI using/reference (grep -c "DeckFlow.CLI" Review.razor == 0).
    - DeckFlow.Studio builds 0 errors / 0 new warnings.
  </acceptance_criteria>
  <done>Review queue renders, filters, previews via the CORRECT (non-doubled) artifact resolver with a containment guard so real artifacts resolve present, degrades on genuine missing artifacts, and writes approval status per-row and in batch via Plan 01; Studio builds clean.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human-verify Review queue + nav</name>
  <action>Operator verifies the Review queue behaviors in the browser per the how-to-verify steps. DeckFlow.Studio has no test project, so this checkpoint is the sole verification of Review.razor's interactive behavior — including the artifact-resolution fix (a real distilled artifact must preview PRESENT, not falsely "missing").</action>
  <what-built>Review.razor review queue + Review/Publish nav entries. Per UI-SPEC: status filter tabs with live counts, queue table with badges + row tinting, inline expand showing artifact content + tags (with missing-artifact warning), per-row optimistic Approve/Reject, and batch Approve/Reject Selected.</what-built>
  <how-to-verify>
    1. Start Studio without launching a browser window: set DECKFLOW_DISABLE_AUTO_BROWSER=true and run `dotnet run --project DeckFlow.Studio` (it will need a local content-kb.db with at least a few distilled rows under the Studio data dir; if empty, the All tab should show the "No distilled entries..." empty state — that alone verifies the page renders).
    2. Open http://localhost:{port}/review. Confirm: Pending tab active by default; tab count badges match row counts; clicking a tab filters the table and clears selections.
    3. Approve a pending row → its badge flips to Approved (green), row tints table-success, Approve button disables, Reject stays enabled. Reject another → Rejected badge/red tint.
    4. Expand a row that HAS a distilled artifact on disk → the Content Preview <pre> shows the real markdown artifact (NOT the "Artifact missing" warning — this confirms the resolver no longer doubles the content-kb segment); tag definition list shows archetype/bracket/card-category. THEN temporarily rename that artifact file and re-expand a fresh row → that row shows the "Artifact missing" warning, Approve is disabled, Reject still works.
    5. Check 2+ rows → batch bar appears with eligible counts; Approve Selected updates all, deselects, refreshes counts.
    6. Confirm NavMenu shows Home / Harvest / Review / Publish in order.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues (badge/tint/filter/expand-resolves-present/missing-artifact/batch/nav).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator (browser) → Blazor circuit → IContentSiteIndexStore | Approve/reject clicks become approval_status writes |
| Review page → local filesystem (artifact read) | Reads markdown resolved under the data root (parent of ArtifactRoot) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-46-03-01 | Tampering | approval write from UI | mitigate | Status values are fixed constants from the page (never operator-typed); store validates the allow-list (Plan 01); writes are parameterized. |
| T-46-03-02 | Denial of Service | unreadable/missing artifact | mitigate | File read wrapped in Task.Run with catch → null; renders warning and disables Approve (D-10); never crashes the circuit. |
| T-46-03-03 | Information disclosure / path traversal | artifact path read | mitigate | Resolver combines the stored ArtifactPath with the data root (parent of ArtifactRoot — segment honored once, not doubled), Path.GetFullPath-normalizes, and a containment guard rejects rooted/".." paths and any path escaping the data root before reading (also defended at write-time by the store's ValidateArtifactPath). Studio is local single-operator. |
| T-46-03-04 | Tampering | stale circuit StateHasChanged | mitigate | InvokeAsync sink swallows ObjectDisposedException/InvalidOperationException; Dispose cancels the CTS. |
</threat_model>

<verification>
- DeckFlow.Studio builds 0/0.
- NavMenu grep gate passes (review + publish entries).
- Artifact resolver uses parent-of-ArtifactRoot (no doubled content-kb), with a containment guard; the doubled Path.Combine(ArtifactRoot, ArtifactPath) form is absent.
- Human-verify checkpoint confirms a real artifact previews PRESENT, plus filter/badge/tint/expand/missing-artifact/batch/nav behaviors.
</verification>

<success_criteria>
- Queue lists distilled entries with badge + status filter (REVQ-02/03 SC1/SC2).
- Per-row + batch approve/reject write immediately and update in place (D-05/D-06).
- Inline preview reads via the correct resolver (real artifacts present, no false-missing) with a containment guard; a genuinely missing artifact warns + blocks approve, allows reject (D-08/D-09/D-10).
- Review + Publish nav entries in order.
</success_criteria>

<output>
Create `.planning/phases/46-review-queue-commit-publish-path/46-03-SUMMARY.md` when done.
</output>
</content>
