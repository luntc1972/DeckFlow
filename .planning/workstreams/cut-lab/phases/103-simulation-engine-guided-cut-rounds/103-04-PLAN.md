---
phase: 103-simulation-engine-guided-cut-rounds
plan: 04
type: execute
wave: 2
depends_on: ["103-01"]
files_modified:
  - DeckFlow.Web/Services/CutLab/CutLabCutRoundEngine.cs
  - DeckFlow.Web.Tests/CutLabCutRoundEngineTests.cs
autonomous: true
requirements: [CUT-01]
must_haves:
  truths:
    - "CUT-01/D-01: Round 1 (obvious cuts) = unlocked cards flagged by 2+ discriminating structural findings, framed as 'flagged by N findings', never 'objectively worse'."
    - "D-04: Round 2 = cards flagged by exactly one structural finding; Round 3 = all remaining unlocked cards ordered weakest-tradeoff-first (as a preference call)."
    - "D-04/D-15: when still over 100 after Round 3, the queue loops over deferred cards first, then rejected cards."
    - "D-03: rounds auto-advance — the engine yields a single ordered proposal sequence with round labels, never a dead-end/blank state."
    - "Pitfall 3: WeakFloorCase and RedundantFinishers evidence (which flags an entire role uniformly) is handled explicitly and does NOT inflate a card's finding tally the way discriminating findings (CurveCongestion, StrandedSubtheme, EnablerStarved) do."
  artifacts:
    - path: "DeckFlow.Web/Services/CutLab/CutLabCutRoundEngine.cs"
      provides: "pure round-queue construction + ordering + loop-around"
      contains: "BuildQueue"
  key_links:
    - from: "DeckFlow.Web/Services/CutLab/CutLabCutRoundEngine.cs"
      to: "DeckFlow.Web/Services/CutLab/CutLabStructuralFindings.cs"
      via: "consumes CutLabFinding[].Evidence to build a per-card finding tally"
      pattern: "CutLabFinding"
    - from: "DeckFlow.Web/Services/CutLab/CutLabCutRoundEngine.cs"
      to: "CutLabState.Decisions / locks"
      via: "excludes locked cards and already-cut cards; incorporates deferred/rejected for the loop pass"
      pattern: "Locked|Deferred"
---

<objective>
Build the pure, deterministic round engine that decides WHICH card is proposed next and in WHICH round — the CUT-01 fixed order (obvious → structural → preference) with D-04 loop-around. It consumes Phase 102 structural findings + role/lock data and produces an ordered proposal sequence with round labels. No simulation runs here (ordering is finding-driven); Round 3's weakest-tradeoff ordering accepts an optional externally-supplied delta-magnitude map so the engine stays pure and testable.

Purpose: This is the CUT-01 sequencing brain the endpoint (Plan 103-07) and page render (Plan 103-06) drive.
Output: `CutLabCutRoundEngine` + exhaustive ordering/loop tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/cut-lab/phases/103-simulation-engine-guided-cut-rounds/103-PATTERNS.md
@.planning/workstreams/cut-lab/phases/103-simulation-engine-guided-cut-rounds/103-RESEARCH.md

<interfaces>
From DeckFlow.Web/Services/CutLab/CutLabStructuralFindings.cs (Phase 102, read-only):
- enum CutLabFindingKind { CurveCongestion, StrandedSubtheme, RedundantFinishers, WeakFloorCase, EnablerStarved }
- record CutLabFindingEvidence(string CardName, double? ManaValue)
- record CutLabFinding(CutLabFindingKind Kind, string Heading, string Lead, IReadOnlyList<CutLabFindingEvidence> Evidence)
- record CutLabStructuralFindingsResult(IReadOnlyList<CutLabFinding> Findings, bool ComboDataAvailable, bool CategoryDataAvailable)

From DeckFlow.Web/Models/CutLab/CutLabState.cs:
- CutLabPoolCard { string Name; int Quantity; string TypeLine; bool IsCommander; bool IsLocked; string? PackageId }
- CutLabDecision { string CardName; CutLabDecisionKind Kind; string Round; int Ordinal }  (Plan 103-02)

Fixed round labels come from 103-UI-SPEC Round-banner copy:
  Round 1 · Obvious cuts / Round 2 · Structural choices / Round 3 · Preference calls /
  Second pass · Revisiting deferred cards / Second pass · Revisiting earlier decisions
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: CutLabCutRoundEngine — round bucketing, ordering, loop-around</name>
  <read_first>
    - DeckFlow.Web/Services/CutLab/CutLabStructuralFindings.cs (finding/evidence record shapes + the WeakFloorCase/RedundantFinishers whole-role-membership behavior that Pitfall 3 warns about)
    - DeckFlow.Web/Services/CutLab/CutLabStructuralFindingsTests.cs (test conventions for this file family)
    - DeckFlow.Web/Models/CutLab/CutLabState.cs (CutLabPoolCard locks/quantity, CutLabDecision from Plan 103-02)
    - .planning/workstreams/cut-lab/phases/103-simulation-engine-guided-cut-rounds/103-CONTEXT.md (D-01, D-02, D-03, D-04, D-15 exact mechanics)
    - .planning/workstreams/cut-lab/phases/103-simulation-engine-guided-cut-rounds/103-RESEARCH.md (Pitfall 3 + Assumption A4: the finding-tally semantics decision)
  </read_first>
  <behavior>
    - Test 1 (D-01): a card whose name appears in the Evidence of 2+ DISCRIMINATING findings (CurveCongestion, StrandedSubtheme, EnablerStarved) lands in Round 1.
    - Test 2 (Pitfall 3 / A4): a card that sits in a role flagged only by WeakFloorCase (or RedundantFinishers) — where every role-mate shares that same finding — does NOT get counted as a discriminating finding for the D-01 tally; it does not flood Round 1. Document the chosen rule in-code.
    - Test 3 (D-04): a card flagged by exactly one discriminating finding lands in Round 2; an unlocked card with zero findings lands in Round 3.
    - Test 4: locked cards (IsLocked or IsCommander) and already-Accepted cards never appear in any queue.
    - Test 5 (D-04 Round 3 ordering): given an optional delta-magnitude map, Round 3 orders weakest-tradeoff-first; with no map, it uses a stable deterministic fallback (e.g. ascending mana value then name) — ordering is reproducible across calls.
    - Test 6 (D-15 loop): when the number needed to reach 100 exceeds the union of rounds 1-3, the sequence appends Deferred cards (in decision order) then Rejected cards for a second pass with the "Second pass" labels.
    - Test 7 (D-03): the engine returns the NEXT single proposal (top of the ordered sequence) plus its round label, and the count remaining — no gaps, no blank between rounds.
  </behavior>
  <action>
    Create `DeckFlow.Web/Services/CutLab/CutLabCutRoundEngine.cs` (namespace `DeckFlow.Web.Services.CutLab`, `public static class`, mirroring `CutLabStructuralFindings`'s static-pure-function shape with `// Why:`-commented threshold constants). Expose a `BuildQueue` (or `BuildRoundPlan`) entry point taking: the analyzed/pool cards (with lock + quantity + role data), the `CutLabStructuralFindingsResult`, the current `IReadOnlyList<CutLabDecision>` history, the cards-to-cut target (pool count minus 100 minus already-accepted), and an optional `IReadOnlyDictionary<string, double>? round3DeltaMagnitudes = null`. Return an ordered record — e.g. `CutLabRoundPlan` with `IReadOnlyList<CutLabRoundQueueItem>` (each item: card name, round key, round label, finding count, the discriminating finding kinds for the "flagged by N findings" chips) and a computed `CardsRemainingToTarget`. Build a `Dictionary<string, int>` discriminating-finding tally by iterating `Findings[].Evidence`, EXCLUDING WeakFloorCase/RedundantFinishers from the tally per the Pitfall-3 decision (encode this exclusion set as a named `// Why:`-commented constant). Round labels are the fixed UI-SPEC strings above — put them in `public const string` fields so the view model can reuse them. Everything is a pure function of the inputs (no I/O, no simulation, deterministic ordering).
  </action>
  <verify>
    <automated>"/mnt/c/Program Files/dotnet/dotnet.exe" test DeckFlow.Web.Tests --filter "FullyQualifiedName~CutLabCutRoundEngine"</automated>
  </verify>
  <acceptance_criteria>
    - All seven behaviors pass.
    - The WeakFloorCase/RedundantFinishers exclusion set is a named constant with a `// Why:` comment (grep `WeakFloorCase` in the engine file finds the exclusion, not a tally-inclusion).
    - Round labels exist as `public const string` matching the UI-SPEC copy verbatim (grep "Obvious cuts", "Structural choices", "Preference calls", "Second pass").
    - Two `BuildQueue` calls with identical inputs return identical ordering (determinism).
    - The engine file contains no HTTP/simulation/`ManabaseAnalyzer` reference (pure).
  </acceptance_criteria>
  <done>The CUT-01 fixed-order sequencing (with D-04 loop-around and Pitfall-3 tally semantics) is implemented and fully test-covered.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| none new | Pure in-memory function over already-validated state; no new input surface. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-103-04-01 | Tampering | Forged decision history steering the round order | accept | The engine only decides proposal ordering over the user's own pool; a forged history cannot cross a trust boundary — the authoritative floor-break gate (Plan 103-07 via CutLabFloorRules.Evaluate) still runs on every cut. |
| T-103-04-02 | Denial of Service | Pathological pool causing unbounded queue construction | mitigate | Queue size is bounded by the pool (≤150 cards, validated at intake by CutLabPoolValidator); ordering is O(n log n). |
</threat_model>

<verification>
- `"/mnt/c/Program Files/dotnet/dotnet.exe" test DeckFlow.Web.Tests --filter "FullyQualifiedName~CutLab"` green.
</verification>

<success_criteria>
Deterministic, pure round engine implementing CUT-01 order + D-04 loop + Pitfall-3 tally semantics, fully unit-tested.
</success_criteria>

<output>
Create `.planning/workstreams/cut-lab/phases/103-simulation-engine-guided-cut-rounds/103-04-SUMMARY.md` when done.
</output>
