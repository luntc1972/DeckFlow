---
phase: 48-ui-audit-remediation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - DeckFlow.Web/wwwroot/css/site-common.css
  - DeckFlow.Web/Views/Deck/Home.cshtml
  - DeckFlow.Web/Views/Shared/_ShortFormFooter.cshtml
  - DeckFlow.Web/Views/Deck/CardLookup.cshtml
  - DeckFlow.Web/Views/Deck/JudgeQuestions.cshtml
autonomous: true
requirements: [UIR-02]

must_haves:
  truths:
    - "Home hub cards and section headers show lightweight inline-SVG icons"
    - "Cards/panels show subtle elevation and a hover lift affordance"
    - "Section labels and field labels have a distinct weight/letter-spacing treatment"
    - "Short-form pages (Card Lookup, Ask a Judge) no longer end in large dead space — content is capped/centered and followed by an example/next-step panel"
  artifacts:
    - path: "DeckFlow.Web/wwwroot/css/site-common.css"
      provides: "Icon sizing, card/panel elevation + hover, heading/label typography treatment, short-form max-width + empty-state panel layout"
      contains: "box-shadow"
    - path: "DeckFlow.Web/Views/Shared/_ShortFormFooter.cshtml"
      provides: "Reusable example/next-step empty-state panel partial (F7)"
      min_lines: 5
    - path: "DeckFlow.Web/Views/Deck/Home.cshtml"
      provides: "Inline-SVG icons on hub cards + section headers (F1)"
      contains: "<svg"
  key_links:
    - from: "Home.cshtml hub-card markup"
      to: "site-common.css .hub-card__icon"
      via: "svg + css class"
      pattern: "hub-card__icon"
    - from: "short-form views"
      to: "_ShortFormFooter partial"
      via: "Html.PartialAsync"
      pattern: "_ShortFormFooter"
---

<objective>
Remediate the layout + markup audit findings on the closing path: F1 (iconography + elevation), the shadow/elevation half of F2, F4 (typography weight/letter-spacing), F6 (short-form dead space), F7 (empty-state guidance).

Purpose: F1 lifts Visuals 2->3 and F4 (with Plan 01's F3) lifts Typography 3->4 — the remaining two steps to >= 20/24. F2's shadow reinforces Color/Visuals. F6/F7 reinforce Visuals/Experience Design.
Output: All layout CSS in `site-common.css`; inline-SVG icons + a new reusable empty-state partial in markup. NO token changes (Plan 01 owns tokens); NO layout rules in `site.css`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/48-ui-audit-remediation/48-AUDIT.md
@.planning/phases/48-ui-audit-remediation/48-CONTEXT.md
@DeckFlow.Web/Views/Deck/Home.cshtml

<interfaces>
<!-- Existing CSS hooks the executor builds on (verified during planning against site-common.css + the views). Use these; do not re-explore. -->

site-common.css existing classes (extend, don't duplicate):
  .hub-grid                  (line ~287) grid of hub cards
  .hub-card                  (line ~293) flex column, border 1px var(--line), radius 10px, bg var(--panel); NO resting box-shadow today
  .hub-card:hover/:focus     (line ~306) already does translateY(-1px) + box-shadow + border-color
  .hub-card__title           (line ~313) fs-base / 600
  .hub-card__description     (line ~319) fs-sm / var(--muted)
  .hub-group__title          (line ~278) fs-sm / 600 / letter-spacing 0.08em / uppercase / var(--muted)  (section label)
  .hub-hero / .hub-hero__*   (line ~612+) headline card
  --shell-max-width: 1120px  (site-common.css :root line 2) — layout var lives in site-common, OK to add more here

  IMPORTANT — there is NO standalone `.panel` class. Verified: site-common.css only defines
  `.panel-heading` and `.panel-footer-actions` (BEM sub-classes), not a `.panel` container.
  The REAL outer card container on tool pages is `.deck-form` (and `.result-panel`), styled
  per-theme (each theme file sets `border:1px solid var(--line); border-radius:10px; background:var(--panel)`
  on `.deck-form, .result-panel`). Tool pages also open with `<section class="hero">`.
  => For the F2 resting elevation, target the REAL existing card containers in site-common.css:
     `.hub-card`, `.deck-form`, and `.result-panel`. Do NOT reference a non-existent `.panel`.

Home.cshtml structure: .hub-hero (headline) then repeated <section class="hub-group"> each with an <h2 class="hub-group__title"> and a .hub-grid of <a class="hub-card"> (some .hub-card--primary). Each hub-card has <h3 class="hub-card__title"> + <p class="hub-card__description">.

Short-form pages (CardLookup.cshtml, JudgeQuestions.cshtml): open with <section class="hero"><h1>..</h1><p class="lede">..</p></section>, then a small form, then nothing — large dead space below (F6/F7).
</interfaces>

Constraints (DeckFlow CLAUDE.md + 48-CONTEXT):
  - ALL layout/elevation/spacing CSS goes in site-common.css. NEVER add layout rules to site.css or any theme file.
  - NO new dependencies: icons are hand-authored inline <svg> (currentColor stroke/fill so they inherit theme color). No icon font, no SVG library, no npm.
  - Typography wins via existing system font stack + font-weight + letter-spacing only — no web-font.
  - LF endings; changed-lines-only format gate; .cshtml/.css are not C# but keep LF and minimal diffs.
</context>

<tasks>

<task type="auto">
  <name>Task 1: F1 + F2(shadow) — inline-SVG icons on hub cards/section headers + card elevation</name>
  <files>DeckFlow.Web/Views/Deck/Home.cshtml, DeckFlow.Web/wwwroot/css/site-common.css</files>
  <read_first>
    - .planning/phases/48-ui-audit-remediation/48-AUDIT.md (F1, F2)
    - DeckFlow.Web/Views/Deck/Home.cshtml (full file — hub-hero + 4 hub-group sections)
    - DeckFlow.Web/wwwroot/css/site-common.css lines 278-325 (.hub-group__title, .hub-grid, .hub-card, hover) and lines 612-700 (.hub-hero)
    - The `<interfaces>` block above — note there is NO `.panel` class; the real card containers are `.hub-card`, `.deck-form`, `.result-panel`
  </read_first>
  <action>
    F1 (icons): Add a small hand-authored inline `<svg>` icon to each hub-card and to each `.hub-group__title` section header in Home.cshtml. Use one simple geometric glyph per tool/section (e.g. magnifier for Card Lookup/Reference, two-columns for Comparison, sync-arrows for Deck Sync, document for Primer/Convert, question for Ask a Judge, book for Knowledge Base, tag for Categories, chart for Analysis/Meta Gap). Each svg: `width="20" height="20"` (card) / `width="16" height="16"` (section label), `fill="none"` with `stroke="currentColor"` (or fill currentColor) so it inherits theme color, `aria-hidden="true"` and `focusable="false"` (decorative — the text label already names the tool). Wrap card icons in a `<span class="hub-card__icon">` placed before `.hub-card__title`; wrap section-label icons in `<span class="hub-group__icon">` before the heading text. Keep the existing title/description markup intact. Every inline svg added MUST use `stroke="currentColor"`/`fill="currentColor"` (no hard-coded theme hex) and carry `aria-hidden="true"`.

    site-common.css: Add `.hub-card__icon` and `.hub-group__icon` rules — size the svg, set `flex-shrink:0`, vertical-align/gap so the icon sits beside or above the title cleanly; have the icon `color: var(--accent)` (or var(--muted) for the section label) so it picks up the theme. Restructure `.hub-card` to a row/column that accommodates the icon (e.g. icon + a text column) without breaking the existing flex column — use a small header row.

    F2 (shadow/elevation half): Give the RESTING (non-hover) `.hub-card` AND the tool-page card containers `.deck-form` and `.result-panel` a subtle `box-shadow` (e.g. `0 1px 2px rgba(0,0,0,0.04), 0 2px 8px rgba(0,0,0,0.04)`) so panels lift off the page even before hover. Add this in site-common.css (NOT in theme files) — the theme files set the border/background on `.deck-form, .result-panel`; the resting elevation is a shared layout concern and belongs in site-common.css. The existing `.hub-card:hover` rule already raises the shadow; keep the hover lift but ensure the resting state now also has elevation. Combined with Plan 01's widened `--panel`/`--bg` delta this produces clear card separation. Layout only — no token edits.
  </action>
  <verify>
    <automated>grep -c "<svg" DeckFlow.Web/Views/Deck/Home.cshtml</automated>
    <automated>grep -c 'currentColor' DeckFlow.Web/Views/Deck/Home.cshtml</automated>
    <automated>grep -c 'aria-hidden="true"' DeckFlow.Web/Views/Deck/Home.cshtml</automated>
    <automated>grep -q "hub-card__icon" DeckFlow.Web/wwwroot/css/site-common.css && grep -q "hub-group__icon" DeckFlow.Web/wwwroot/css/site-common.css && echo "icon css present"</automated>
    <automated>grep -Eq "\.hub-card[^_]|\.deck-form|\.result-panel" DeckFlow.Web/wwwroot/css/site-common.css && echo "resting-elevation target selectors present in site-common.css"</automated>
    <automated>"/mnt/c/Program Files/dotnet/dotnet.exe" build DeckFlow.Web/DeckFlow.Web.csproj -c Release 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - SOURCE: Home.cshtml has an inline `<svg ... aria-hidden="true" focusable="false">` for every hub-card and every section header; every svg uses currentColor (no hard-coded theme hex). The `grep -c 'currentColor'` and `grep -c 'aria-hidden="true"'` counts in Home.cshtml are >= the `grep -c '<svg'` count.
    - SOURCE: site-common.css defines `.hub-card__icon` and `.hub-group__icon`; the resting (non-hover) `.hub-card`, `.deck-form`, and `.result-panel` selectors carry a non-hover box-shadow added in site-common.css; the `.hub-card` hover lift is preserved. No reference to a non-existent `.panel` class.
    - CONSTRAINT: All new CSS is in site-common.css; ZERO layout rules added to site.css or theme files; build clean; LF; minimal diffs.
    - SCREENSHOT (Plan 03): icons render and inherit color across light + dark themes at both viewports; cards visibly elevated.
  </acceptance_criteria>
  <done>Home hub cards + section headers carry theme-colored inline icons; resting `.hub-card`/`.deck-form`/`.result-panel` read as raised, with a hover lift (Visuals 2->3).</done>
</task>

<task type="auto">
  <name>Task 2: F4 — heading/label typography treatment (weight + letter-spacing)</name>
  <files>DeckFlow.Web/wwwroot/css/site-common.css</files>
  <read_first>
    - .planning/phases/48-ui-audit-remediation/48-AUDIT.md (F4: weight + face monotony)
    - DeckFlow.Web/wwwroot/css/site-common.css lines 160-200 (existing h-weight/letter-spacing), 278-285 (.hub-group__title), 1336/1450 (field label weights)
  </read_first>
  <action>
    F4 (Typography): Introduce a deliberate heading/label treatment using ONLY the existing system font stack + weight + letter-spacing (no web-font). Concretely in site-common.css:
    - Section labels (`.hub-group__title` and equivalent eyebrow/section-label classes like `.hub-hero__eyebrow`): ensure 700 weight + a slight positive letter-spacing (~0.08em, already partly present — make it consistent and bump weight to 700 where it is 600) and uppercase where it already is, so section labels read as a distinct tier.
    - Field labels / badges (form `<label>`, badge/pill classes): 600 medium weight + tiny letter-spacing (~0.02em) so labels separate from body copy.
    - Page `<h1>`/`.hero h1`: confirm 700 and add a small negative or zero letter-spacing tightening for display headings so the largest text reads as a display tier vs body.
    Keep changes additive and scoped to existing selectors; do not introduce new font-family. This + Plan 01's F3 lifts Typography 3->4.
  </action>
  <verify>
    <automated>grep -c "letter-spacing" DeckFlow.Web/wwwroot/css/site-common.css</automated>
    <automated>"/mnt/c/Program Files/dotnet/dotnet.exe" build DeckFlow.Web/DeckFlow.Web.csproj -c Release 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - SOURCE: site-common.css section-label selectors use 700 + letter-spacing; field-label/badge selectors use 600 + small letter-spacing; no new font-family declared anywhere.
    - CONSTRAINT: All in site-common.css; no token files touched; no web-font/dependency; LF; changed-lines only.
    - SCREENSHOT (Plan 03): three visible type tiers (display heading / section label / body) at both viewports.
  </acceptance_criteria>
  <done>Headings and labels have a clear weight/letter-spacing hierarchy distinct from body copy (Typography 3->4 with F3).</done>
</task>

<task type="auto">
  <name>Task 3: F6 + F7 — cap/center short-form pages + reusable example/next-step panel</name>
  <files>DeckFlow.Web/Views/Shared/_ShortFormFooter.cshtml, DeckFlow.Web/Views/Deck/CardLookup.cshtml, DeckFlow.Web/Views/Deck/JudgeQuestions.cshtml, DeckFlow.Web/wwwroot/css/site-common.css</files>
  <read_first>
    - .planning/phases/48-ui-audit-remediation/48-AUDIT.md (F6 dead space, F7 empty-state guidance)
    - DeckFlow.Web/Views/Deck/CardLookup.cshtml (top of file — .hero + form, no trailing guidance)
    - DeckFlow.Web/Views/Deck/JudgeQuestions.cshtml (same shape)
    - DeckFlow.Web/Views/Shared/_BracketCallout.cshtml (existing small partial — match its style/conventions)
  </read_first>
  <action>
    F7 (empty-state, reusable): Create `DeckFlow.Web/Views/Shared/_ShortFormFooter.cshtml` — a small server-side partial rendering an example/next-step panel (a `.short-form-footer` container with a heading like "Example" or "Next step" and 1-3 lines of guidance). Make it accept the guidance text via a parameter (ViewData or a simple model string) so each page passes its own copy; if a parameterized partial is awkward, render generic next-step guidance and let each caller pass page-specific text via ViewData. Match the existing partial conventions seen in `_BracketCallout.cshtml`. Keep copy concise (D-F9 jargon is out of scope/LOW).

    Wire it in: at the END of CardLookup.cshtml and JudgeQuestions.cshtml, after the form section, add `@await Html.PartialAsync("_ShortFormFooter", ...)` with page-appropriate example/next-step copy (e.g. Card Lookup: "Try pasting 'Sol Ring' to see Oracle text + rulings"; Ask a Judge: a pointer to the prompt generator / judge resources).

    F6 (cap/center): In site-common.css add a `.short-form` / content-cap rule (e.g. `max-width: ~64ch` centered) usable by the short tool pages so the form column does not stretch and the page does not end in a wide empty band; the `.short-form-footer` panel uses the resting `.deck-form`/`.result-panel`-style elevation added in Task 1 (or its own subtle box-shadow in site-common.css). Apply the cap class on the relevant container in the two short-form views (wrap the form region, or add the class to its existing wrapper) without disturbing the desktop list-mode layout on Card Lookup.
  </action>
  <verify>
    <automated>test -f DeckFlow.Web/Views/Shared/_ShortFormFooter.cshtml && echo "partial exists"</automated>
    <automated>grep -l "_ShortFormFooter" DeckFlow.Web/Views/Deck/CardLookup.cshtml DeckFlow.Web/Views/Deck/JudgeQuestions.cshtml</automated>
    <automated>grep -q "short-form" DeckFlow.Web/wwwroot/css/site-common.css && echo "short-form css present"</automated>
    <automated>"/mnt/c/Program Files/dotnet/dotnet.exe" build DeckFlow.Web/DeckFlow.Web.csproj -c Release 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - SOURCE: `_ShortFormFooter.cshtml` exists and is rendered at the end of both CardLookup.cshtml and JudgeQuestions.cshtml with page-specific copy.
    - SOURCE: site-common.css defines `.short-form`/`.short-form-footer` layout; cap applied to the short-form containers.
    - CONSTRAINT: layout CSS only in site-common.css; no token changes; build clean; LF; minimal diffs; Card Lookup desktop list-mode unaffected.
    - SCREENSHOT (Plan 03): short pages have centered/capped content and a closing example panel — no large dead band — at both viewports.
  </acceptance_criteria>
  <done>Short-form pages end with a helpful example/next-step panel and capped content instead of dead space (Visuals/Experience Design reinforced).</done>
</task>

</tasks>

<verification>
- Build: `DeckFlow.Web` compiles clean (validates Razor partial + asset references).
- F1: inline svgs present in Home.cshtml (currentColor + aria-hidden="true" counts >= svg count); `.hub-card__icon`/`.hub-group__icon` + resting box-shadow on `.hub-card`/`.deck-form`/`.result-panel` in site-common.css.
- F4: heading/label weight+letter-spacing treatment in site-common.css; no new font-family.
- F6/F7: `_ShortFormFooter` partial rendered on both short pages; `.short-form` cap in site-common.css.
- ZERO layout rules added to site.css or any theme file (all layout in site-common.css); no reference to a non-existent `.panel` class.
- Visual confirmation deferred to Plan 03 (UIR-03, 2 viewports, deployed site).
</verification>

<success_criteria>
- F1, F2(shadow), F4, F6, F7 implemented; all layout CSS in site-common.css; icons are inline SVG with currentColor; no new dependency.
- Combined with Plan 01: closing path (Visuals 2->3, Color 3->4, Typography 3->4) is implemented and ready for re-score.
</success_criteria>

<output>
Create `.planning/phases/48-ui-audit-remediation/48-02-SUMMARY.md` when done.
</output>
</content>
