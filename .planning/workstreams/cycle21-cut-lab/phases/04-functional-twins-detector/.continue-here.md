---
context: phase
workstream: cycle21-cut-lab
phase: 04-functional-twins-detector
task: 0
total_tasks: 4
status: in_progress
activity: plan-review convergence loop (execution NOT started)
branch: gsd/cycle21-cut-lab
head: 2bf28b4b
last_updated: 2026-08-01T16:18:40.422Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> Each was discovered through actual failure in the 2026-08-01 session. Not predictions.

- [ ] **CONSTRAINT: Phase 4 plans are NOT converged.** Rounds 1, 2 and 3 all returned CHANGES
  REQUIRED. **Do not execute Phase 4 until a review returns CONVERGED.** Mitigation is structural:
  the convergence rule ([[feedback_plan_review_convergence_loop]]) gates execution, not judgement.

- [ ] **CONSTRAINT: the incomplete-fold anti-pattern.** Three consecutive rounds failed for the same
  reason — the operative instruction was corrected while a stale contradicting copy survived in
  frontmatter `must_haves`, `<verification>`, `<success_criteria>`, `<acceptance_criteria>`, a task
  `<name>`, a threat-model row, or a `read_first` bullet. Round 3 found the plans' *substance* had
  converged at round 2; only consistency kept failing. **Structural mitigation: after folding any
  finding, grep every one of those surfaces for the old claim before declaring the fold done, and
  reconcile every numeric count against the actual list length mechanically.** Do not eyeball it.

**Do not proceed until both boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---|---|---|---|
| Incomplete fold | Operative text fixed, stale copy left in a parallel surface. Cost 3 extra review rounds — and round 4 found **two more** the round-3 sweep missed ("the 7 enumerated above", "18 more executed tests"), because the grep patterns were too literal. | blocking | Grep all 8 surfaces, and search by **number word AND digit in every phrasing** (`seven\|7`, `eighteen\|18`), not just the exact sentence you fixed. Reconcile counts against actual list lengths. |
| Stale state asserted as fact | I repeated "main is 46 ahead, unpushed" several times from memory. It was already pushed. User made decisions against a wrong picture. | blocking | `git fetch --prune` and compare against the remote **before** asserting any push/branch state |
| `codex exec … \| tail -N` | Truncates the **head** of the output. Lost BLOCK 1-3 of the round-1 review; needed a session resume to recover them. | advisory | Redirect to a file (`> file.txt`) or use `-o`. Never pipe a review through `tail`. |
| `codex exec resume` ignores `-m` | Silently falls back to the config default (ran `gpt-5.6-luna` on a `gpt-5.6-sol` session, warning only). | advisory | Treat resume as unreliable for analysis; re-dispatch when the model matters |
| Postgres flag lost at the WSL boundary | Without `WSLENV=DECKFLOW_POSTGRES_TESTS/w` the var never reaches the Windows test host; every `[PostgresFact]` skips and the run still prints **Passed!** | blocking | Always pass `/w`, and verify by reading the **skip count**, not the pass line |

<current_state>
Phase 4 **planning**, not execution. Zero of its four waves have run; no Phase 4 production code
exists yet.

Round 4 of the plan-review convergence loop was **dispatched and still running** when work paused:
- output: `/tmp/claude-1000/-mnt-c-users-chrislunt-source-personal-deckflow/46dc0886-1a16-4417-8efc-668df2ec69b6/scratchpad/p4-round4.txt`
- background task id `bqaqnr2vu` (does **not** survive the session)

Everything is committed and pushed. `main == origin/main == c0da20bc`; branch head `b1d25ed1`.
Only two untracked research dirs remain (`_edhrec-brackets/`, `_role-floor-research/`).
</current_state>

<completed_work>

Merge / rebase sequence (steps 1-3 of the user's 8-step full-milestone plan):
- Step 1: ff `fix/cutlab-scryfall-burst` → main — `938cbe4a`
- Step 2: rebase + ff `fix/cutlab-combo-seam` → main — `c0da20bc`
- Phase 6 inserted into the roadmap — `0e667893`
- Step 3: rebase `gsd/cycle21-cut-lab` onto main — `c8eae7bb` (155 commits, 2 conflicts, 1 correct skip)

Phase 4 plan review (step 4), three rounds:
- R1 `cd3788f9` — 2 BLOCK / 5 HIGH / 6 MED / 1 LOW, all folded
- R2 `13144f11` — 5 incomplete folds + 5 new defects, all folded
- R3 `b1d25ed1` — 9 contradictions, **zero new substantive defects**, all folded

Phase 5 (step 5, partial):
- Postgres blocker **DISCHARGED** — suite `2231 passed / 0 failed / 1 skipped` (was 2216/0/16)
- F4-1 upgraded from a disclosed NOT-VERIFIED risk to a **required `[PostgresFact]`** — `f7756740`

Push: all 12 branches in sync; cycle21 force-pushed with lease; `fix/cutlab-combo-seam` published.
</completed_work>

<remaining_work>

- **Round 4 review** — in flight at pause; read or re-dispatch
- **Phase 5 owed Codex convergence review** — never run against the current baseline
- **Step 6** — execute Phase 4 (4 waves, ~2.3k LOC) → `gpt-5.6-terra`, major coding
- **Step 7** — execute Phase 5
- **Phase 01.2 Protection-Vocabulary Widening** — ⚠ NEVER STARTED and easy to miss; sits between
  01.1 and 2 in the roadmap, and Phase 2 shipped without it
- **Phase 6 Scryfall Throughput** — roadmap block only, no phase dir. ⚠ `mkdir` the phase dir
  BEFORE `/gsd-plan-phase` or it silently returns an archived milestone phase
  ([[reference_gsd_phase_dir_collision_archive]])
- **Step 8** — UAT dark, then **the user** flips `tool.cut-lab.enabled`
</remaining_work>

<decisions_made>

- **Re-ran the Phase 4 review from scratch** rather than folding the lost 2026-07-29 output — it was
  graded against pre-rebase code. Vindicated: BLOCK 1 existed *only* because `31042a36` landed in
  that window.
- **Exclusion-set test asserts `FunctionalTwins` ABSENCE only**, never exact membership. A count pin
  makes any future legitimate addition fail, and the cheapest "fix" is deleting a load-bearing
  exclusion — exactly how the round-1 combo-bias defect would return.
- **Replaced `BuildFindingGroups`' capture-index-then-insert with a single-pass build.** It reverses
  merged-section order *today*: `[Curve, WeakFloor, ComboProtected]` renders
  `[Curve, ComboProtected, WeakFloor]`. Confirmed independently by the reviewer. Follow-up F-04-03
  had deferred this exact refactor as cosmetic — the triplication was hiding unchecked arithmetic.
- **Test 9 narrowed** from "proves not invoked" to a paired OFF/ON assertion: `Snapshot()` takes no
  key, so the original spec was unbuildable without a production seam.
- **Full-suite runs are the reviewer's gate, not the executor's** — filtered gates are reliable; a
  Codex executor failed the full suite 5 consecutive times.
- **Phase 5 F4-1 upgraded, not merely disclosed** — its premise ("Postgres provable in zero
  environments") became false the moment Docker started.
- **Force-push authorized by the user**, with lease, after verifying all 158 remote-only commits'
  content exists on the rebased branch.
</decisions_made>

<blockers>
- **Phase 4 not converged** — rounds 1-3 all CHANGES REQUIRED. Gates step 6.
- **Phase 5 owed Codex review** — never run against the current plan baseline.
- **Docker Desktop must be running** for Postgres-gated tests. Not auto-started.
</blockers>

## Required Reading (in order)

1. `04-REVIEWS.md` (this directory) — all three rounds, every finding, and how each was folded.
   **Read this before touching any Phase 4 plan**; it explains why several instructions look
   counter-intuitive (absence-only assertions, the withdrawn refactor ban, the untestable tiebreak).
2. `04-01-PLAN.md` … `04-04-PLAN.md` — the four waves, in order.
3. `04-PLAN-CHECK.md` — **historical only**, banner-marked superseded. Do not execute from it.
4. `.planning/workstreams/cycle21-cut-lab/ROADMAP.md` — Phase 6 block and release posture.
5. `../05-archidekt-bracket-capture/05-REVIEWS.md` — read the 2026-08-01 banner first; it invalidates
   the shared premise behind R-H1 and F4-1.

## Critical Anti-Patterns (do NOT repeat these)

- **[ANTI-PATTERN] Declaring a fold done after fixing one site** → grep frontmatter, `<objective>`,
  `<interfaces>`, every task `<name>`/`<read_first>`/`<action>`/`<acceptance_criteria>`/`<done>`,
  `<verification>`, `<success_criteria>` and the threat table for the old claim.
- **[ANTI-PATTERN] Trusting remembered git state** → `git fetch --prune` and diff against the remote
  before asserting anything about pushes or branch position.
- **[ANTI-PATTERN] Reading a Codex review through `tail`** → redirect to a file; the head is lost.
- **[ANTI-PATTERN] Reading "Passed!" as proof the Postgres tests ran** → read the skip count.

## Infrastructure State

- **Docker Desktop**: was running (engine 29.4.3), `docker-desktop` WSL distro up. Will not survive a
  reboot. WSL integration is **off and not needed** — no Linux dotnet exists, so tests run under
  Windows `dotnet.exe` and reach the engine via the named pipe.
- **Postgres gate command** (verified):
  `WSLENV=DECKFLOW_POSTGRES_TESTS/w DECKFLOW_POSTGRES_TESTS=1 "/mnt/c/Program Files/dotnet/dotnet.exe" test DeckFlow.Web.Tests -c Release`
- **Suite baselines**: Web `2231/0/1` with the PG gate on, `2216/0/16` without. Core `2011/0/0`.
  Build: 0 errors, **9 CS8629** — the known pre-existing baseline in `ManabaseBaselineWeightingTests`.
- **node_modules**: NTFS junction in this worktree (`mklink /J`). Required or the TS `BeforeTargets`
  step fails the *test* project build. Exclude it from commits.
- **Backup refs (local only, not pushed)**: `backup/cycle21-cut-lab-pre-rebase-2026-08-01`
  (= `0e667893`) and `…-2026-07-28`. Delete once the force-push is trusted.
- **Prod flag**: `tool.cut-lab.enabled` is **false**. Cut Lab deploys dark. Render autodeploys `main`.

## Pre-Execution Critique Required

- Design artifact: `04-01-PLAN.md` … `04-04-PLAN.md`
- Critique focus: whether any contradiction survives across the eight surfaces listed above;
  whether the newly-specified tests actually fail under the mutations they name.
- **Gate: do NOT begin Phase 4 execution until a review returns CONVERGED.**

<context>
This session wrote no production code. It was merge/rebase plumbing, a plan-review convergence loop,
and discharging Phase 5's Postgres blocker.

The single most useful thing learned: round 3 established that the plans' **substance** converged at
round 2 — every substantive fix verified correct against source — and that the remaining failures
were purely self-contradiction. That reframes the remaining work. If round 4 still says CHANGES
REQUIRED, expect contradictions, not design problems, and sweep mechanically rather than reasoning
about the design again.

Model routing for what comes next: plan review and re-review are `gpt-5.6-sol` at **medium** (plan
review deliberately keeps medium even though the session default dropped to low). Phase 4 execution
is major, multi-file, interface-touching work → `gpt-5.6-terra`.
</context>

<next_action>
Start with: read
`/tmp/claude-1000/-mnt-c-users-chrislunt-source-personal-deckflow/46dc0886-1a16-4417-8efc-668df2ec69b6/scratchpad/p4-round4.txt`
and grep it for `VERDICT`.

- **Has a VERDICT line** → act on it. Note: round 4's own reasoning trace already flagged two
  surviving stale counts, and **both were fixed at pause** (`04-03:448` "the 7 enumerated above" →
  8; `04-02:547` "18 more executed tests" → 19). Its verdict may therefore cite issues that are
  already resolved — re-check each against the current file before folding. `CONVERGED` unblocks step 6 (execute Phase 4 via
  `gpt-5.6-terra`). `CHANGES REQUIRED` → fold, expecting contradictions rather than design defects,
  then re-review.
- **No VERDICT line** → the background task died with the session. Re-dispatch the round-4 review;
  the exact prompt is reconstructable from `04-REVIEWS.md`'s round-3 section.
</next_action>
