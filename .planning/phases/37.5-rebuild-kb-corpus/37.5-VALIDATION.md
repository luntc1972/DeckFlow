---
phase: 37.5
slug: rebuild-kb-corpus
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-06-11
---

# Phase 37.5 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | xUnit 2.9.3 (DeckFlow.Core.Tests / DeckFlow.Web.Tests) |
| **Config file** | none — projects pre-exist |
| **Quick run command** | `dotnet build DeckFlow.sln` (VSTest unreliable in WSL — build-clean is the fast gate per CLAUDE.md) |
| **Full suite command** | `dotnet test` (push-and-watch CI when WSL VSTest flakes) |
| **Estimated runtime** | ~60-120 seconds build |

---

## Sampling Rate

- **After every task commit:** Run `dotnet build DeckFlow.sln` (0 errors / 0 new warnings)
- **After every plan wave:** Run targeted `dotnet test` for the touched test project, or push-and-watch CI
- **Before `/gsd:verify-work`:** Full build green + targeted distill/reset tests green
- **Max feedback latency:** ~120 seconds

---

## Per-Task Verification Map

> Filled by planner against the final task IDs. Anchors below map requirements to the verifiable proof for each.

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| TBD | 01 | 1 | REBUILD-01 | T-37.5-01 | Reset purges content_* but preserves blocked_videos + content_sources | unit | `dotnet test --filter CorpusReset` | ❌ W0 | ⬜ pending |
| TBD | 01 | 1 | REBUILD-03 | — | Distill with all-zero clip timestamps is rejected/flagged | unit | `dotnet test --filter Distill` | ❌ W0 | ⬜ pending |
| TBD | 02 | 2 | REBUILD-02 | — | Junk-class transcript classified `filtered`, not indexed | unit | `dotnet test --filter Classif` | ❌ W0 | ⬜ pending |

---

## Wave 0 Requirements

- [ ] `DeckFlow.Core.Tests` — corpus-reset test: seed content_* + blocked_videos + content_sources, run reset, assert content_* empty AND blocked_videos/content_sources intact (REBUILD-01)
- [ ] `DeckFlow.Core.Tests` — clip-timestamp tests: (a) timestamped transcript fetch retains `[mm:ss]`; (b) `ValidateClips` rejects all-zero distill (REBUILD-03)
- [ ] `DeckFlow.Core.Tests` — classifier tests: advice/philosophy → keep, trivia/news/meta/intro → `filtered` (REBUILD-02)

*Existing xUnit infrastructure covers these; no new framework needed.*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Live Salubrious Snail re-harvest under new bar | REBUILD-02 | Needs live YouTube + claude CLI distill (WSL→Windows dotnet env quirk) | Run harvest+distill for snail; confirm a sampled junk video is dropped (status `filtered`) |
| Clips show real distinct mid-video timestamps | REBUILD-03 | Spot-check against actual video playback | Open 2-3 published entries; verify clip times point at the advice moment, not `[00:00]` |
| Operator publish via admin curation grid | REBUILD-04 | Human-in-loop hybrid gate | In admin grid, publish survivors; confirm only published appear on `/content-kb` |
| Prod Postgres corpus reset | REBUILD-01 | Destructive prod op — coordinate | Run reset against prod; confirm content_* empty, blocked_videos preserved |
| Browse + Copy artifact | REBUILD-05 | Visual/clipboard | `/content-kb` lists entries; Detail renders HTML-encoded; Copy artifact copies clean text |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 120s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
