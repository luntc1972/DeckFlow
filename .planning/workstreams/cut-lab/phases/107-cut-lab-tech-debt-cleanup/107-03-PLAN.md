---
phase: 107-cut-lab-tech-debt-cleanup
plan: 03
type: execute
wave: 2
depends_on: ["107-02"]
files_modified:
  - DeckFlow.Web/Models/CutLabViewModel.cs
  - DeckFlow.Web/Views/Deck/CutLab.cshtml
  - DeckFlow.Web/wwwroot/ts/cut-lab.ts
  - DeckFlow.Web.Tests/CutLabViewModelWordingTests.cs
autonomous: true
requirements: [CLEANUP-2, CLEANUP-5]
must_haves:
  truths:
    - "The 'Lock your pool' chip and the Compare panel agree on their pool-card count basis (both commander-inclusive)"
    - "The pool-status chip string has a single server-side source (a fresh commander-inclusive ViewModel member); the client twin matches it — no 4th copy"
    - "Cut Lab pluralization uses the existing ManabaseWording.Pluralize helper (no second server-side pluralizer)"
    - "Neither the client decide FORM action nor the JS decide FETCH endpoint hardcodes an absolute /cut-lab path (both path-base safe)"
  artifacts:
    - path: "DeckFlow.Web/Models/CutLabViewModel.cs"
      provides: "Fresh single-source, commander-inclusive pool-status chip string; ManabaseWording.Pluralize reuse"
      contains: "ManabaseWording"
    - path: "DeckFlow.Web/wwwroot/ts/cut-lab.ts"
      provides: "Commander-inclusive chip twin + path-base-safe decide form action AND fetch endpoint"
  key_links:
    - from: "CutLabViewModel fresh chip member"
      to: "Compare panel BaselineCount basis (commander-inclusive)"
      via: "shared commander-inclusive count"
      pattern: "cards in pool"
    - from: "cut-lab.ts decide form + fetch endpoint"
      to: "CutLab.cshtml Url.Content(\"~/cut-lab/decide\") and Url.Content(\"~/api/cut-lab/decide\") via data attributes"
      via: "read both paths from data-* attributes, not literals"
      pattern: "data-cut-lab-decide-(action|api)"
---

<objective>
Item 2 + Item 5 (mechanical display + dedup). Reconcile the "Lock your pool" pool-status chip so it
uses the SAME commander-inclusive count basis as the Compare panel (they currently disagree by 1 —
the commander). Give the chip string a single server-side source (a FRESH ViewModel member) with a
matching client twin. Consolidate Cut Lab's hand-rolled pluralizer onto the existing
`ManabaseWording.Pluralize`. Make BOTH client decide paths (form action AND fetch endpoint)
path-base safe. Close the cacheKey-fallback simplify note.

Purpose: Retire the counting-convention split + 104-simplify tech-debt.
Output: One count basis, one chip source, one pluralizer, path-base-safe client form + fetch.

Direction chosen (Claude's discretion per CONTEXT / RESEARCH A1): make the CHIP commander-inclusive
to match the deeply-embedded, load-bearing Compare/engine convention (Convention B). Do NOT change
`Model.CardCount` (non-commander) — it stays the basis for the 101-150 validation and its messages.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/cut-lab/phases/107-cut-lab-tech-debt-cleanup/107-CONTEXT.md
@.planning/workstreams/cut-lab/phases/107-cut-lab-tech-debt-cleanup/107-RESEARCH.md

Counting facts (RESEARCH Item 2, traced to source):
- `Model.CardCount` = NON-commander count (CutLabViewModel.cs:33; validated 101-150) — DO NOT CHANGE.
- `Model.BaselineCount` = COMMANDER-INCLUSIVE pool total (CutLabViewModel.cs:120, `pool.Sum(Quantity)`),
  rendered in the Compare panel (CutLab.cshtml:1121). This is the basis the chip should match.
- Chip today: CutLab.cshtml:240 `@Model.CardCount cards in pool · @lockedCount locked`; client twin
  cut-lab.ts:597-609 `updateLockedCountChip` filters OUT the commander before summing the pool count.

IMPORTANT — the old dead `PoolStatusText` property NO LONGER EXISTS: it was removed in commit
`edb7c21bc` (`rg PoolStatusText` = 0 hits repo-wide; CutLabViewModel.cs has no such member and no
test asserts it). So this is a CREATE-fresh task, not a repurpose-dead-prop task. The 101-VERIFICATION
"copy triplication" open item is therefore already partly resolved by that removal; the remaining
work is to introduce ONE fresh server-side chip source + keep the one unavoidable TS twin (C#/TS
language boundary) — do NOT create a 4th copy.

Pluralizer (RESEARCH Item 5): `DeckFlow.Core/Manabase/ManabaseWording.Pluralize(singular, count)`
(:19) is the existing shared helper; `CutLabViewModel.FormatCountLabel` (:798, used by
`FormatCutsMadeCount` :243 and `FormatCutsAcceptedSoFar` :246) hand-rolls the same naive suffix.
All Cut Lab words are regular → drop-in. Note: `ManabaseWording` lives in `DeckFlow.Core.Manabase`;
reusing it is a minor cross-namespace smell but IN SCOPE as mechanical — do NOT move/rename it.
Client `formatCountLabel` (cut-lab.ts:192) is the only TS impl (no TS duplication to remove).

Path-base (RESEARCH Item 5 + review fix): TWO hardcoded absolute paths in cut-lab.ts break under a
non-root path base — `form.action = '/cut-lab/decide';` (:1145) AND
`const cutLabDecisionApiEndpoint = '/api/cut-lab/decide';` (:177, used by the JS decide fetch at
:2098). BOTH must read the server-resolved `Url.Content(...)` value from a data attribute.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add a fresh commander-inclusive single-source chip member + reconcile the client twin</name>
  <read_first>
    - DeckFlow.Web/Models/CutLabViewModel.cs (CardCount :33; BaselineCount :120 with its
      commander-inclusive computation; the `From(...)`/construction path where BaselineCount is
      assigned ~:233 — the natural place to compute the new chip member).
    - DeckFlow.Web/Views/Deck/CutLab.cshtml:236-244 (chip markup — note the "(protected from any
      future cut)" text is a SEPARATE span, NOT part of the count string) and :1121 (Compare panel).
    - DeckFlow.Web/wwwroot/ts/cut-lab.ts:594-609 (`updateLockedCountChip`) + `getPoolRows`.
    - DeckFlow.Web.Tests/CutLabViewModelWordingTests.cs (existing BaselineCount/CurrentCount wording
      assertions — these must stay green unchanged).
  </read_first>
  <behavior>
    - The new chip member (name it `PoolStatusText`, freshly CREATED — no prior member of that name
      exists) returns "{poolCount} cards in pool · {lockedCount} locked" where poolCount is
      COMMANDER-INCLUSIVE (equals BaselineCount at intake) and lockedCount is the initial locked total.
    - For a pool of 149 non-commander cards + 1 commander: chip reads "150 cards in pool ..." and the
      Compare panel reads "150 cards → ..." — the two now match.
    - Model.CardCount is unchanged (still non-commander) and still drives validation/max attributes.
  </behavior>
  <action>
    Server: CREATE a fresh chip-string member `PoolStatusText` on `CutLabViewModel`, computed at
    construction (in the same path that assigns BaselineCount, ~:233) as
    "{poolCount} cards in pool · {lockedCount} locked", where poolCount is COMMANDER-INCLUSIVE (reuse
    the BaselineCount basis — the pool sum INCLUDING the commander) and lockedCount is the initial
    locked total. Render it at CutLab.cshtml:240 via `@Model.PoolStatusText` INSIDE the existing
    `<span data-cut-lab-lock-count>`, replacing the inline `@Model.CardCount cards in pool · @lockedCount locked`
    literal. Leave the separate "(protected...)" span untouched. Do NOT alter Model.CardCount or the
    Compare panel. Do NOT re-introduce any second server-side copy of the chip format (no 4th copy).
    Client: in `updateLockedCountChip`, compute the pool count COMMANDER-INCLUSIVE — drop the
    `.filter(row => row.dataset.cutLabCommander !== 'true')` on the POOL-count reduce so it sums all
    rows (keep the locked-count reduce as-is). Keep the exact "{n} cards in pool · {m} locked" format
    to match the server source.
    Tests: ADD a NEW unit assertion in CutLabViewModelWordingTests that `PoolStatusText` equals the
    commander-inclusive count string for a representative pool (assert poolCount == BaselineCount).
    Confirm the existing BaselineCount/CurrentCount assertions stay green (unchanged basis).
    Document (code `// Why:` comment + SUMMARY): the chip reflects the imported/locked pool; it is NOT
    re-summed after the Phase 106 +/- quantity tuner (`handleAdjustSubmit` does not call
    updateLockedCountChip) — the live-tuned total is surfaced by the sticky bar. This adjust-path
    staleness is an ACCEPTED, documented limitation, out of scope for this item.
    LF endings; changed-lines-only; preserve `{get;init;}` / switch / xmldoc carve-outs.
  </action>
  <acceptance_criteria>
    - `grep -n "@Model.CardCount cards in pool" DeckFlow.Web/Views/Deck/CutLab.cshtml` returns zero;
      the chip now renders `@Model.PoolStatusText`.
    - `PoolStatusText` is a fresh member on CutLabViewModel AND referenced in CutLab.cshtml (not dead);
      a NEW unit test asserts its value is commander-inclusive (equals the BaselineCount-basis count).
    - `updateLockedCountChip` no longer filters the commander out of the POOL-count reduce; format
      string unchanged.
    - `Model.CardCount` definition + all validation/`max="@Model.CardCount"` usages unchanged.
    - No second server-side chip-format string exists (`grep -c "cards in pool" DeckFlow.Web/Models/CutLabViewModel.cs` == 1).
    - `dotnet test DeckFlow.sln --filter "FullyQualifiedName~CutLabViewModel"` passes;
      `npx --no-install vitest run` (in DeckFlow.Web/) passes for cut-lab specs.
  </acceptance_criteria>
  <verify>
    <automated>dotnet build DeckFlow.sln && dotnet test DeckFlow.sln --filter "FullyQualifiedName~CutLabViewModel"</automated>
  </verify>
  <done>Fresh commander-inclusive PoolStatusText single-sources the chip; client twin matches; CardCount/validation untouched; no 4th copy; adjust-path staleness documented; tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Consolidate pluralizer + path-base-safe decide form AND fetch endpoint + cacheKey note</name>
  <read_first>
    - DeckFlow.Core/Manabase/ManabaseWording.cs:19 (`Pluralize`) — the shared helper to reuse.
    - DeckFlow.Web/Models/CutLabViewModel.cs (`FormatCountLabel` :798; callers `FormatCutsMadeCount`
      :243, `FormatCutsAcceptedSoFar` :246; existing `using` block).
    - DeckFlow.Web.Tests/CutLabViewModelWordingTests.cs (`FormatCutsMadeCount_ReturnsExpectedCardWording`,
      `FormatCutsAcceptedSoFar_ReturnsExpectedCutWording` — assert output strings only).
    - DeckFlow.Web/wwwroot/ts/cut-lab.ts:177 (`const cutLabDecisionApiEndpoint = '/api/cut-lab/decide';`
      used by the fetch at :2098) and :1145 (`form.action = '/cut-lab/decide';` in
      `buildDecisionFormBase`) and :1059 (`form.dataset.cacheKey?.trim() || 'cut-lab'`).
    - DeckFlow.Web/Views/Deck/CutLab.cshtml:38 (`Url.Content("~/cut-lab/decide")`) and :116 (the main
      form carrying data-cache-key — the anchor for the new data attributes).
  </read_first>
  <behavior>
    - FormatCutsMadeCount(1) → "1 card ..."; FormatCutsMadeCount(2) → "2 cards ..."; cut/cuts likewise
      — identical output to today, now produced via ManabaseWording.Pluralize.
    - After a decide response, the client-rebuilt form's action equals the server-resolved
      "~/cut-lab/decide" path read from a data attribute.
    - The JS decide fetch posts to the server-resolved "~/api/cut-lab/decide" path read from a data
      attribute (works under a non-root path base), not the hardcoded "/api/cut-lab/decide".
  </behavior>
  <action>
    (a) Pluralizer: add `using DeckFlow.Core.Manabase;` to CutLabViewModel.cs; replace the two
    `FormatCountLabel(count, "card", "cards")` / `("cut", "cuts")` call sites (:243, :246) with
    `ManabaseWording.Pluralize`, preserving the exact rendered string (`"{count} {word}"` and
    `"{count} {word} so far"`). Delete the now-unused private `FormatCountLabel` (:798). The existing
    wording tests must stay green unchanged.
    (b) Path-base safety — BOTH client decide paths:
      - Add `data-cut-lab-decide-action="@Url.Content("~/cut-lab/decide")"` AND
        `data-cut-lab-decide-api="@Url.Content("~/api/cut-lab/decide")"` to the main Cut Lab form in
        CutLab.cshtml (~:116, the form already carrying data-cache-key).
      - In `buildDecisionFormBase` (cut-lab.ts:1145) read `data-cut-lab-decide-action` from the main
        form and assign it to `form.action` instead of the literal `'/cut-lab/decide'`.
      - Replace the module-level `cutLabDecisionApiEndpoint` literal (:177) so the decide fetch
        (:2098) resolves the endpoint from `data-cut-lab-decide-api` on the main form (read it once at
        submit time, or initialize the constant from the attribute). Keep a safe fallback to the
        respective literal only if the attribute is missing.
    (c) cacheKey fallback (cut-lab.ts:1059): keep the `|| 'cut-lab'` defensive default but add a
    `// Why:` comment noting the data-cache-key attribute is always server-rendered (CutLab.cshtml:116)
    so this branch is defensive-only — closes the simplify note as intentional defensive code.
    Never stage compiled wwwroot/js. LF endings; changed-lines-only; preserve C# switch expressions /
    xmldoc indent / `{get;init;}` carve-outs.
  </action>
  <acceptance_criteria>
    - `grep -n "FormatCountLabel" DeckFlow.Web/Models/CutLabViewModel.cs` returns zero (helper removed);
      `grep -n "ManabaseWording.Pluralize" DeckFlow.Web/Models/CutLabViewModel.cs` returns >=2.
    - BOTH hardcoded paths gone / path-base-safe:
      `grep -n "'/cut-lab/decide'" DeckFlow.Web/wwwroot/ts/cut-lab.ts` returns zero at the form-action
      assignment (:1145 reads the data attribute), and `grep -n "'/api/cut-lab/decide'" DeckFlow.Web/wwwroot/ts/cut-lab.ts`
      shows no hardcoded endpoint literal driving the fetch (endpoint resolved from the data attribute,
      literal retained only as a fallback).
    - `grep -c "data-cut-lab-decide-action\|data-cut-lab-decide-api" DeckFlow.Web/Views/Deck/CutLab.cshtml` == 2.
    - cut-lab.ts:1059 fallback retained WITH a `// Why:` comment.
    - `dotnet build DeckFlow.sln` clean; `dotnet test DeckFlow.sln --filter "FullyQualifiedName~CutLabViewModelWording"` passes.
    - `npx --no-install tsc -p DeckFlow.Web/tsconfig.json --noEmit` (or the project's TS build) is clean.
  </acceptance_criteria>
  <verify>
    <automated>dotnet build DeckFlow.sln && dotnet test DeckFlow.sln --filter "FullyQualifiedName~CutLabViewModelWording"</automated>
  </verify>
  <done>Single server pluralizer via ManabaseWording; BOTH decide form action and fetch endpoint path-base safe via data attributes; cacheKey fallback documented; build + wording tests + TS compile clean.</done>
</task>

</tasks>

<threat_model>
No new threats. Display-string reconciliation, a server-helper reuse, two added same-origin decide
paths sourced from `Url.Content` (server-trusted, not user input), and a comment. No new inputs,
endpoints, deserialization, or auth surface. The data-attribute values are server-rendered from route
constants, so no injection vector is introduced.
</threat_model>

<verification>
- `dotnet build DeckFlow.sln` clean; CutLab ViewModel/wording tests green; Vitest cut-lab green; TS compiles.
- Chip count (commander-inclusive) matches Compare panel BaselineCount at intake; single server source (no 4th copy).
- Both decide form action and fetch endpoint resolve from data attributes (path-base safe).
- `git diff --ignore-all-space --stat` vs `git diff --stat` gap small (no EOL churn); no compiled .js staged.
</verification>

<success_criteria>
Pool-status chip and Compare panel agree (commander-inclusive), single-sourced via a fresh member;
one server pluralizer; path-base-safe decide form + fetch; cacheKey fallback closed with reason; no
behavior regression.
</success_criteria>

<output>
Create `.planning/workstreams/cut-lab/phases/107-cut-lab-tech-debt-cleanup/107-03-SUMMARY.md` when done
(record the accepted adjust-path chip-staleness limitation).
</output>
