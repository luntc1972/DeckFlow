---
phase: 83-packet-service-srp-split
plan: 04
type: execute
wave: 2
depends_on: ["83-01", "83-02", "83-03"]
files_modified:
  - DeckFlow.Web/Services/DeckComparisonService.cs
  - DeckFlow.Web.Tests/DeckComparisonServiceTests.cs
autonomous: true
requirements: [PKTSVC-01, PKTSVC-02, PKTSVC-03, PKTSVC-04]
must_haves:
  truths:
    - "DeckComparisonService delegates Scryfall resolution to ScryfallReferenceResolver (SearchFallback strategy, normalize OFF) and holds no private batch-chunk-collect-fallback path or private Chunk."
    - "It delegates sectioned decklist text to PacketTextAssembler and commander reflag to DeckEntryReflagHelper."
    - "Its comparison/follow-up paste artifacts are byte-identical pre/post refactor across all 3 AI variants."
  artifacts:
    - path: "DeckFlow.Web/Services/DeckComparisonService.cs"
      provides: "Comparison orchestration shell delegating to shared collaborators"
      contains: "ScryfallReferenceResolver"
  key_links:
    - from: "DeckFlow.Web/Services/DeckComparisonService.cs"
      to: "DeckFlow.Web/Services/Packets/ScryfallReferenceResolver.cs"
      via: "instantiated from the already-injected IScryfallCardResolver"
      pattern: "new ScryfallReferenceResolver"
---

<objective>
Migrate DeckComparisonService (1033 LOC) onto the shared collaborators, reducing it to an
orchestration shell — gated by the 83-01 byte-identity harness.

Purpose: Remove Comparison's copy of the Scryfall batch loop, private Chunk, sectioned-decklist
builder, and the byte-identical ReflagCommanderEntry — delegating to ScryfallReferenceResolver,
PacketTextAssembler, and DeckEntryReflagHelper — with zero change to its paste artifacts.
Output: A slimmer DeckComparisonService whose Comparison byte-identity suite still passes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/83-packet-service-srp-split/83-RESEARCH.md
@CLAUDE.md

<do_not_unify>
- Keep Comparison's `NormalizeOracleText` (:919-936, oracle-only) — do NOT replace with Analysis's
  fuller version (Cluster G).
- Keep `BuildComboArtifactText` (:996-1032) as-is (Cluster F — divergent combo formatter).
- Keep `BuildCanonicalDeckSourceText` (:125-143) including its leading `commander|Name` prefix line
  (Cluster C — cache-key text, per-service; NOT routed through the assembler).
</do_not_unify>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delegate Scryfall resolution to ScryfallReferenceResolver</name>
  <files>DeckFlow.Web/Services/DeckComparisonService.cs, DeckFlow.Web.Tests/DeckComparisonServiceTests.cs</files>
  <read_first>
    - DeckFlow.Web/Services/DeckComparisonService.cs:385-445 (LookupCardDetailsAsync), :943-956 (Chunk), :430 (SearchFallbackCardAsync path)
    - DeckFlow.Web/Services/Packets/ScryfallReferenceResolver.cs (from 83-03 — ResolveBatchAsync signature + ScryfallBatchResolution)
    - DeckFlow.Web.Tests/DeckComparisonByteIdentityTests.cs (from 83-01 — the gate)
    - DeckFlow.Web.Tests/DeckComparisonServiceTests.cs (CreateService seam)
  </read_first>
  <action>
    In the DeckComparisonService constructor(s) (both the DI ctor and the internal test ctor),
    instantiate `new ScryfallReferenceResolver(cardResolver)` from the `IScryfallCardResolver` the
    service already injects; store in a private readonly field. Replace the body of
    `LookupCardDetailsAsync` with a call to `ResolveBatchAsync(names, SearchFallbackCardAsync
    delegate, normalizeForScryfall: false, ct)`, then keep Comparison's existing post-processing
    (mapping the returned references — keyed by ORIGINAL request name — to its ScryfallCard list +
    oracleNameMap usage). Delete the now-unused private `Chunk`. Do NOT change the DI extension file.
    Do NOT alter which fallback method is used.

    Add a DeckComparisonServiceTests case asserting the resolved-card/oracle-name outcome is unchanged
    for a fixture that exercises a collection-miss recovered via SearchFallback.
  </action>
  <verify>
    <automated>dotnet.exe test DeckFlow.Web.Tests --filter "FullyQualifiedName~DeckComparison" 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - Build 0/0.
    - DeckComparisonByteIdentityTests (83-01) still PASS across all 3 platforms.
    - No private Chunk and no inline batch-chunk-collect loop remain in DeckComparisonService (grep confirms).
    - The DI extension file is unchanged.
  </acceptance_criteria>
  <done>Comparison resolves via the shared resolver; its byte-identity gate is still green.</done>
</task>

<task type="auto">
  <name>Task 2: Delegate decklist text + reflag; confirm shell reduction</name>
  <files>DeckFlow.Web/Services/DeckComparisonService.cs, DeckFlow.Web.Tests/DeckComparisonServiceTests.cs</files>
  <read_first>
    - DeckFlow.Web/Services/DeckComparisonService.cs:864-916 (BuildDecklistText + FormatDecklistLine), :363-383 (ReflagCommanderEntry), :289-300 (BuildRequestContextText)
    - DeckFlow.Web/Services/Packets/PacketTextAssembler.cs (BuildSectionedDecklistText + AppendKeyValueLine)
    - DeckFlow.Web/Services/Packets/DeckEntryReflagHelper.cs (ReflagCommanderEntry)
  </read_first>
  <action>
    Replace Comparison's `BuildDecklistText`/`FormatDecklistLine` calls with
    `PacketTextAssembler.BuildSectionedDecklistText(entries, possibleIncludes, includeVersions:false,
    oracleNameMap: <the existing map>)`, delete the now-unused private builders. Replace the private
    `ReflagCommanderEntry` calls with `DeckEntryReflagHelper.ReflagCommanderEntry(...)` and delete the
    private copy (the no-commander-section reflag fixture in 83-01 gates this path). Optionally route
    `BuildRequestContextText`'s per-field lines through `PacketTextAssembler.AppendKeyValueLine`,
    passing `JsonTextFormatterService.NormalizeSingleLine` as the normalizer delegate (Comparison's
    CURRENT normalizer — do NOT substitute a different one) and keeping Comparison's own field
    list/order. Leave Cluster C/F/G methods untouched.
  </action>
  <verify>
    <automated>dotnet.exe test DeckFlow.Web.Tests --filter "FullyQualifiedName~DeckComparison" 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - Build 0/0; full DeckComparison test set + DeckComparisonByteIdentityTests pass (3 platforms).
    - Private BuildDecklistText/FormatDecklistLine/ReflagCommanderEntry/Chunk are gone from the service (grep confirms).
    - NormalizeOracleText, BuildComboArtifactText, BuildCanonicalDeckSourceText remain (Cluster G/F/C untouched).
    - DeckComparisonService is materially smaller and delegates text/reflag to the collaborators.
  </acceptance_criteria>
  <done>DeckComparisonService is an orchestration shell over the shared collaborators; artifacts byte-identical.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → Comparison request model | Existing DeckComparisonRequest validation is untouched; the refactor only re-routes internal call graphs. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-83-07 | Tampering | dropped blank-line / swapped fallback changing comparison prompt bytes | mitigate | DeckComparisonByteIdentityTests (83-01) asserts Ordinal equality across 3 platforms after each task. |
</threat_model>

<verification>
- `dotnet.exe test DeckFlow.Web.Tests --filter "FullyQualifiedName~DeckComparison"` → all pass (incl. byte-identity).
- Grep: no private Chunk/BuildDecklistText/FormatDecklistLine/ReflagCommanderEntry in DeckComparisonService.
</verification>

<success_criteria>
DeckComparisonService delegates resolution + decklist assembly + reflag to the shared collaborators,
retains no duplicate resolution path, and its paste artifacts are byte-identical across all 3 AI
variants (PKTSVC-02/03; gated by PKTSVC-04).
</success_criteria>

<output>
Create `.planning/phases/83-packet-service-srp-split/83-04-SUMMARY.md` when done.
</output>
