---
phase: 53-architecture-backlog-burn-down
plan: 02
type: execute
wave: 2
depends_on: ["53-03", "53-04"]
files_modified:
  - DeckFlow.Web/Program.cs
  - DeckFlow.Web/Extensions/HttpClientServiceCollectionExtensions.cs
  - DeckFlow.Web/Extensions/ScryfallServiceCollectionExtensions.cs
  - DeckFlow.Web/Extensions/PromptVariantServiceCollectionExtensions.cs
  - DeckFlow.Web/Extensions/PacketServiceCollectionExtensions.cs
  - DeckFlow.Web/Services/Scryfall/
  - DeckFlow.Web/Services/Content/
  - DeckFlow.Web/Services/Persistence/
  - DeckFlow.Web.Tests/Extensions/DiCompositionExtensionsTests.cs
autonomous: true
requirements: [ARCH-01]

must_haves:
  truths:
    - "The app boots and every route/endpoint behaves identically — DI graph is unchanged, only its declaration site moved"
    - "Program.cs Main is materially shorter — inline HTTP-client + service-factory wiring lives in AddDeckFlowXxx() extension methods"
    - "The empty Services/Content/ folder is filled and remaining flat Services root files are foldered by concern (Scryfall, Persistence, Content)"
    - "Namespaces are unchanged (DeckFlow.Web.Services) so the moves cause zero using-directive churn"
  artifacts:
    - path: "DeckFlow.Web/Extensions/HttpClientServiceCollectionExtensions.cs"
      provides: "AddDeckFlowHttpClients() — the named HttpClient + tagger typed-client + cookie container registrations"
      contains: "AddDeckFlowHttpClients"
    - path: "DeckFlow.Web/Extensions/ScryfallServiceCollectionExtensions.cs"
      provides: "AddDeckFlowScryfallServices() — Scryfall lookup/search/set/commander/tagger service factories"
      contains: "AddDeckFlowScryfallServices"
    - path: "DeckFlow.Web/Extensions/PromptVariantServiceCollectionExtensions.cs"
      provides: "AddDeckFlowPromptVariants() — the 6 prompt-variant families + registries"
      contains: "AddDeckFlowPromptVariants"
    - path: "DeckFlow.Web/Extensions/PacketServiceCollectionExtensions.cs"
      provides: "AddDeckFlowPacketServices() — the 4 packet services + supporting registrations"
      contains: "AddDeckFlowPacketServices"
  key_links:
    - from: "DeckFlow.Web/Program.cs"
      to: "AddDeckFlowHttpClients / ScryfallServices / PromptVariants / PacketServices"
      via: "builder.Services.AddDeckFlowXxx() calls"
      pattern: "AddDeckFlow(HttpClients|ScryfallServices|PromptVariants|PacketServices)"
---

<objective>
Extract the inline DI wiring from `Program.cs` (552 LOC) into `AddDeckFlowXxx()` extension methods following the established `DeckFlow.Web/Extensions/*ServiceCollectionExtensions.cs` pattern, and finish the stalled `Services/` concern-foldering (fill the empty `Services/Content/`, move Scryfall + persistence + content flat root files into concern folders). Closes Phase 39 audit Finding D.

Purpose: Lowest-risk hygiene win — build + test is full proof. Namespaces stay `DeckFlow.Web.Services`, so foldering is pure file moves with no using churn.
Output: 4 new extension files, a shorter Program.cs Main, and foldered Services subfolders (Scryfall, Persistence, Content filled).

Behavior-preservation proof: the full solution build + the existing Web test suite (DI graph resolves at boot; route/endpoint behavior unchanged). This plan adds NO new behavior.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/53-architecture-backlog-burn-down/53-CONTEXT.md
@./CLAUDE.md

<interfaces>
<!-- Established pattern to mirror. Each is `public static IServiceCollection AddDeckFlowXxx(this IServiceCollection services[, IWebHostEnvironment env])` in namespace DeckFlow.Web.Extensions, returning `services` for chaining. -->
From DeckFlow.Web/Extensions/HarvestServiceCollectionExtensions.cs:
  public static IServiceCollection AddDeckFlowHarvest(this IServiceCollection services, IWebHostEnvironment env)
From DeckFlow.Web/Extensions/AnalyticsServiceCollectionExtensions.cs:
  public static IServiceCollection AddDeckFlowAnalytics(this IServiceCollection services, IWebHostEnvironment env)
From DeckFlow.Web/Extensions/FeatureFlagsServiceCollectionExtensions.cs:
  public static IServiceCollection AddDeckFlowFeatureFlags(this IServiceCollection services)

Already-extracted call sites in Program.cs Main (do not duplicate):
  AddDeckFlowResiliencePipelines(), AddDeckFlowFeatureFlags(), AddDeckFlowHarvest(env), AddDeckFlowAnalytics(env)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract AddDeckFlowHttpClients() + AddDeckFlowScryfallServices()</name>
  <read_first>
    DeckFlow.Web/Program.cs (lines ~84-291 — HTTP clients, tagger typed client, cookie container, Scryfall service factories),
    DeckFlow.Web/Extensions/HarvestServiceCollectionExtensions.cs (the pattern to mirror)
  </read_first>
  <files>DeckFlow.Web/Extensions/HttpClientServiceCollectionExtensions.cs, DeckFlow.Web/Extensions/ScryfallServiceCollectionExtensions.cs, DeckFlow.Web/Program.cs</files>
  <action>
    Create `HttpClientServiceCollectionExtensions.AddDeckFlowHttpClients(this IServiceCollection)` (namespace DeckFlow.Web.Extensions) holding the named-client registrations: commander-banlist, commander-spellbook, scryfall-rest, the CookieContainer singleton, the scryfall-tagger typed client with its SocketsHttpHandler + ConfigurePrimaryHttpMessageHandler + SetHandlerLifetime, the ScryfallTaggerHttpClient + IScryfallTaggerHttpClient singletons, and the youtube-metadata client + IYouTubeChannelVideoLister transient. Move every load-bearing comment verbatim (Phase 5 BUG-01 BIC-bypass headers, the HIGH-2 TaggerSessionCache TTL/HandlerLifetime invariant note, AutomaticDecompression rationale). Create `ScryfallServiceCollectionExtensions.AddDeckFlowScryfallServices(this IServiceCollection)` holding the ICommanderSearchService, ICardSearchService, CardLookupCache, ICardLookupService, IScryfallCardResolver, IScryfallSetService, IScryfallRestClientFactory, IScryfallTaggerLookupService, IMechanicLookupService factory registrations. In Program.cs replace the moved blocks with `builder.Services.AddDeckFlowHttpClients();` and `builder.Services.AddDeckFlowScryfallServices();` placed at the original position so ordering relative to AddDeckFlowResiliencePipelines() is preserved. Do NOT change handler lifetimes, base addresses, headers, or service lifetimes (Singleton stays Singleton). Namespaces of moved-out registrations are not relevant (these are call expressions, not type moves).
  </action>
  <verify>Solution builds clean; the two extension methods are called from Program.cs Main</verify>
  <acceptance_criteria>
    - source: HttpClientServiceCollectionExtensions.cs + ScryfallServiceCollectionExtensions.cs exist with the `AddDeckFlow*` signatures returning IServiceCollection
    - source: Program.cs no longer contains `AddHttpClient("scryfall-tagger"` or `AddSingleton<ICardLookupService>` inline (grep returns 0 in Program.cs); both now appear in the extension files
    - source: the HIGH-2 TaggerSessionCache TTL invariant comment is present verbatim in HttpClientServiceCollectionExtensions.cs
    - test-command: `"/mnt/c/Program Files/dotnet/dotnet.exe" build DeckFlow.sln` exits 0, 0 new warnings
  </acceptance_criteria>
  <done>HTTP-client + Scryfall wiring lives in two extension methods; Program.cs calls them at the original position; solution builds clean.</done>
</task>

<task type="auto">
  <name>Task 2: Extract AddDeckFlowPromptVariants() + AddDeckFlowPacketServices(); verify boot</name>
  <read_first>
    DeckFlow.Web/Program.cs (lines ~292-374 — prompt-variant registries + packet service factories + remaining service registrations),
    DeckFlow.Web/Extensions/AnalyticsServiceCollectionExtensions.cs
  </read_first>
  <files>DeckFlow.Web/Extensions/PromptVariantServiceCollectionExtensions.cs, DeckFlow.Web/Extensions/PacketServiceCollectionExtensions.cs, DeckFlow.Web/Program.cs, DeckFlow.Web.Tests/Extensions/DiCompositionExtensionsTests.cs</files>
  <action>
    Create `PromptVariantServiceCollectionExtensions.AddDeckFlowPromptVariants(this IServiceCollection)` holding the six prompt-variant families (Analysis, SetUpgrade, Comparison, FollowUp, MetaGap, Primer) — each ChatGpt/Claude/Gemini IXxxPromptVariant registration plus its XxxPromptVariantRegistry. Create `PacketServiceCollectionExtensions.AddDeckFlowPacketServices(this IServiceCollection)` holding the four scoped packet-service factories (IDeckAnalysisPacketService, IDeckComparisonService, IMetaGapService, IDeckPrimerPacketService) plus PacketSessionCache. In Program.cs replace the moved blocks with `builder.Services.AddDeckFlowPromptVariants();` and `builder.Services.AddDeckFlowPacketServices();` at the original positions. Leave the remaining inline registrations (ICategoryKnowledgeStore, ArchidektCacheJobService + hosted service, suggestion/sync/convert services, importers, parsers, AiPlatformOptions, ForwardedHeaders, rate limiter, Swagger) in Program.cs — those are not part of the four named groups and moving them risks reordering the hosted-service + schema-validation startup sequence. Preserve scoped vs singleton lifetimes exactly.
    Then ADD a DI-composition smoke test `DeckFlow.Web.Tests/Extensions/DiCompositionExtensionsTests.cs` (xUnit, namespace DeckFlow.Web.Tests, mirroring the existing `DeckFlow.Web.Tests/Extensions/HarvestServiceCollectionExtensionsTests.cs` ServiceCollection pattern) that builds a `ServiceCollection`, registers the framework prerequisites the extracted groups depend on (AddLogging, AddMemoryCache, AddHttpClient, AddDeckFlowResiliencePipelines) plus every AddDeckFlowXxx() group including the two new ones, calls `BuildServiceProvider(new ServiceProviderOptions { ValidateOnBuild = true, ValidateScopes = true })`, and from a created scope asserts `GetRequiredService` resolves the four packet services (IDeckAnalysisPacketService, IDeckComparisonService, IMetaGapService, IDeckPrimerPacketService) and the six prompt-variant registries. Why: the existing Web suite does NOT boot the host/DI graph, so a dropped or misordered extracted registration would otherwise pass a plain build silently (Codex 53-02 MED). No new package — ServiceCollection + BuildServiceProvider are already used across DeckFlow.Web.Tests (e.g. HarvestServiceCollectionExtensionsTests).
  </action>
  <verify>Solution builds clean; the new DiCompositionExtensionsTests passes (ValidateOnBuild resolves the extracted graph); Web test suite green</verify>
  <acceptance_criteria>
    - source: PromptVariantServiceCollectionExtensions.cs + PacketServiceCollectionExtensions.cs exist with `AddDeckFlow*` signatures
    - source: Program.cs no longer contains `AddSingleton<IAnalysisPromptVariant` or `AddScoped<IDeckComparisonService` inline (grep returns 0 in Program.cs)
    - source: Program.cs Main is materially shorter (target: under ~400 lines vs the original 552)
    - source: DiCompositionExtensionsTests.cs builds a ServiceProvider with ValidateOnBuild=true and resolves the 4 packet services + 6 prompt-variant registries (proves the extracted registrations compose; no new package — uses ServiceCollection/BuildServiceProvider already present in Web.Tests)
    - test-command: `"/mnt/c/Program Files/dotnet/dotnet.exe" build DeckFlow.sln` exits 0, 0 new warnings
    - test-command: `"/mnt/c/Program Files/dotnet/dotnet.exe" test DeckFlow.Web.Tests/DeckFlow.Web.Tests.csproj` — 0 failed (excluding pre-existing PG skips); DiCompositionExtensionsTests included
  </acceptance_criteria>
  <done>Prompt-variant + packet-service wiring lives in two extension methods; Program.cs is shorter; build + Web tests green.</done>
</task>

<task type="auto">
  <name>Task 3: Finish Services/ concern-foldering (Scryfall, Persistence, Content)</name>
  <read_first>
    DeckFlow.Web/Services/ (the flat root file list),
    DeckFlow.Web/Services/Scryfall/ (existing destination folder),
    DeckFlow.Web/DeckFlow.Web.csproj (confirm no explicit per-file includes that would break on move)
  </read_first>
  <files>DeckFlow.Web/Services/Scryfall/, DeckFlow.Web/Services/Persistence/, DeckFlow.Web/Services/Content/</files>
  <action>
    Move flat Services root files into concern folders WITHOUT changing their `namespace DeckFlow.Web.Services;` declarations (SDK-style project globs *.cs, so moving the file changes its on-disk path only — no namespace edit, no using churn). Move into Services/Scryfall/: ScryfallCommanderSearchService.cs, ScryfallDtos.cs, ScryfallRestClientFactory.cs, ScryfallSetService.cs, ScryfallTaggerHttpClient.cs, ScryfallTaggerLookupService.cs, ScryfallTaggerParsers.cs, ScryfallThrottle.cs, TaggerSessionCache.cs, CardLookupService.cs, CardLookupCache.cs, CardSearchService.cs. Move into Services/Persistence/: FeedbackStore.cs, IFeedbackStore.cs, FeedbackDialect.cs (created by 53-04 in Wave 1 at flat Services/ root — fold it here so it sits with its FeedbackStore sibling; include any IFeedbackDialect.cs 53-04 emits), CategoryKnowledgeStore.cs, ICategoryKnowledgeStore.cs, DeckFlowDatabaseConnectionFactory.cs, AdminBruteForceTrackerStore.cs, PacketArtifactStore.cs. Fill Services/Content/: ContentArtifactParser.cs, ContentKbArtifactPathResolver.cs, ContentKbSeedLoader.cs, IContentKbSeedLoader.cs. Use `git mv` so history is preserved. Do NOT move files that other plans in this phase edited as content (DeckComparisonService.cs stays at root this phase to avoid a cross-plan churn collision; note that in the summary as a deliberate carry-forward). Do NOT touch namespaces, do NOT reflow the moved files (CLAUDE.md no-mass-reflow rule — moving a file is not editing its lines). After moving, confirm the build still globs them.
  </action>
  <verify>Solution builds clean after moves; folders are populated; namespaces unchanged</verify>
  <acceptance_criteria>
    - source: Services/Content/ is no longer empty (4 content files present); Services/Scryfall/ and Services/Persistence/ populated; FeedbackDialect.cs (from 53-04) lands in Services/Persistence/ alongside FeedbackStore.cs — no Feedback persistence file left at flat Services/ root
    - source: moved files retain `namespace DeckFlow.Web.Services;` (grep on a moved file confirms the namespace line is unchanged)
    - source: `git status` shows renames (R) not delete+add, confirming `git mv` preserved history
    - test-command: `"/mnt/c/Program Files/dotnet/dotnet.exe" build DeckFlow.sln` exits 0, 0 new warnings
  </acceptance_criteria>
  <done>Services/ is foldered by concern with Content/ filled; namespaces unchanged; solution builds clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Pure refactor: DI registrations move declaration site; files move on disk with unchanged namespaces. No new input surface, dependency, or behavior. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-53-02 | Tampering | DI graph + startup sequence integrity across the move | mitigate | Extension calls placed at original positions; hosted-service + schema-validation startup left inline; clean build + Web test suite (DI resolves at boot) is the guard. |

No new threats. No package installs.
</threat_model>

<verification>
- `dotnet build DeckFlow.sln` clean, 0 new warnings.
- Web test suite passes (DI graph resolves; routes/endpoints unchanged).
- Program.cs Main is shorter; the four AddDeckFlowXxx() calls appear; Services/Content/ is filled.
</verification>

<success_criteria>
Program.cs DI wiring is extracted into four AddDeckFlowXxx() extension methods following the established pattern, Services/ is concern-foldered with Content/ filled, namespaces are unchanged, and the solution builds + tests clean.
</success_criteria>

<output>
Create `.planning/phases/53-architecture-backlog-burn-down/53-02-SUMMARY.md` when done.
</output>
