---
phase: 110-cut-lab-navigation-and-pool-discovery
plan: 04
type: execute
wave: 3
depends_on: ["110-03"]
files_modified:
  - DeckFlow.Web/Views/Deck/CutLab.cshtml
  - DeckFlow.Web/wwwroot/ts/cut-lab.ts
  - DeckFlow.Web/wwwroot/css/site-common.css
  - DeckFlow.Web/ts-tests/cut-lab-pool-filter.test.ts
autonomous: true
requirements: [CLUP-11, CLUP-12]
must_haves:
  truths:
    - "The Lock your pool table can filter to All / Locked / Unlocked cards"
    - "The Lock your pool table can be searched by card name (case-insensitive substring)"
    - "Filtering/search hides non-matching rows without detaching them, so lock and package state still serialize"
    - "A live match count shows 'Showing N of M cards' and a 'No cards match.' row appears at zero matches"
    - "Filter/search controls are hidden with no JS; whole-pool status counts never reflect the filtered view"
  artifacts:
    - path: "DeckFlow.Web/Views/Deck/CutLab.cshtml"
      provides: "Hidden-by-default filter/search controls + match-count + empty row in the Lock-your-pool panel-heading"
      contains: "cutlab-pool-filter"
    - path: "DeckFlow.Web/wwwroot/ts/cut-lab.ts"
      provides: "Filter/search logic that hides rows via [hidden] and never detaches them"
      contains: "cutlab-pool-filter"
  key_links:
    - from: "DeckFlow.Web/wwwroot/ts/cut-lab.ts"
      to: "tr[data-cut-lab-card] rows"
      via: "toggle [hidden], never remove"
      pattern: "data-cut-lab-card"
    - from: "DeckFlow.Web/wwwroot/ts/cut-lab.ts"
      to: "getPoolRows()/buildSnapshotFromDom()"
      via: "rows remain in DOM under filter"
      pattern: "hidden"
---

<objective>
Add locked-state filtering and card-name search to the "Lock your pool" table (CLUP-11, CLUP-12)
without changing any card's lock or package state. Rows are hidden, never detached (D-13), which makes
CLUP-11's "without changing card state" true by construction: `getPoolRows()` /
`buildSnapshotFromDom()` keep walking every row on the next submit.

Purpose: on a ~100-card pool the user needs to quickly show only unlocked cards or find a card by
name. Scope is deliberately the pool table ONLY (D-17, D-20) — role-group accordions and Structural
chips are different lenses and stay whole.
Output: hidden-by-default filter/search controls + live match count + empty-state row.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/110-cut-lab-navigation-and-pool-discovery/110-CONTEXT.md
@.planning/phases/110-cut-lab-navigation-and-pool-discovery/110-UI-SPEC.md
@.planning/phases/110-cut-lab-navigation-and-pool-discovery/110-03-SUMMARY.md

<interfaces>
<!-- The serialization path filtering must not break, and the DOM it filters. -->

Pool table (CutLab.cshtml ~236-350): panel-heading with <span data-cut-lab-lock-count> pool status;
  4-column table (Lock | Card | Type/role | Package assignment); each row:
  <tr data-cut-lab-card="@card.Name" data-cut-lab-type-line data-cut-lab-role data-cut-lab-quantity
      data-cut-lab-commander>
    <td><input type="checkbox" data-cut-lab-lock-card="@card.Name" ...></td>
    <td><strong>@qty × @name</strong>...</td> ...

cut-lab.ts serialization (must keep seeing every row — D-13):
  getPoolRows() ~490: Array.from(document.querySelectorAll('tr[data-cut-lab-card]'))
  buildSnapshotFromDom() walks getPoolRows() to build CutLabStateJson on submit.

Status counts that must stay whole-pool (D-18): data-cut-lab-lock-count, role-group
  data-cut-lab-group-locked counts — filtering must NOT recompute these.

Existing form-control styling to match: input[data-cut-lab-goal] { min-height:44px; border:
  1px solid var(--line); background: var(--panel-soft-bg, var(--panel)); }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Filter/search controls, match-count, and empty row markup + CSS</name>
  <files>DeckFlow.Web/Views/Deck/CutLab.cshtml, DeckFlow.Web/wwwroot/css/site-common.css</files>
  <read_first>
    - DeckFlow.Web/Views/Deck/CutLab.cshtml (Lock-your-pool panel-heading ~237-247; table thead ~289-297; tbody)
    - DeckFlow.Web/wwwroot/css/site-common.css (input[data-cut-lab-goal] control styling; .prompt-size-note)
    - .planning/phases/110-cut-lab-navigation-and-pool-discovery/110-CONTEXT.md (D-14, D-16, D-19, D-20)
    - .planning/phases/110-cut-lab-navigation-and-pool-discovery/110-UI-SPEC.md (Copywriting Contract rows for filter/search/empty; Component Inventory Pool filter/search + Match-count rows)
  </read_first>
  <action>
    Inside the existing "Lock your pool" panel-heading (D-19, so it collapses with the section), add a
    `<div class="cutlab-pool-filter" hidden>` containing: a "Show:" control group
    (`.cutlab-pool-filter__group`) with three options All / Locked / Unlocked (radio group or
    segmented buttons — executor's choice within D-13/D-14), a text input `.cutlab-pool-search`
    (placeholder "Search card name…"), and a `.cutlab-pool-match-count` element (initial "Showing {M}
    of {M} cards"). Add a hidden empty-state row as the last tbody row:
    `<tr class="cutlab-pool-empty-row" hidden><td colspan="4">No cards match.</td></tr>`. The whole
    `.cutlab-pool-filter` container is `hidden` by default (revealed by JS in Task 2 — no dead
    controls with no JS, D-14). New CSS in site-common.css: `.cutlab-pool-filter*`,
    `.cutlab-pool-search`, `.cutlab-pool-match-count` — reuse the existing form-control treatment
    (44px min-height touch targets, `var(--line)` border, `var(--panel-soft-bg, var(--panel))`
    background), match-count text in `--muted` at `var(--fs-sm)`. Use COMPOUND selectors so the theme
    forks (loaded after site-common.css) cannot override on source order. Layout CSS in
    site-common.css only, never site.css. LF endings; changed lines only.
  </action>
  <acceptance_criteria>
    - `grep -c 'class="cutlab-pool-filter"' DeckFlow.Web/Views/Deck/CutLab.cshtml` returns 1 and the
      element carries the `hidden` attribute.
    - The three Show options (All/Locked/Unlocked) and a search input with placeholder
      "Search card name…" are present in the Lock-your-pool panel-heading (grep confirms).
    - `grep -c 'cutlab-pool-empty-row' DeckFlow.Web/Views/Deck/CutLab.cshtml` returns 1 with
      `colspan="4"` and `hidden`.
    - New `.cutlab-pool-*` CSS lives in site-common.css only
      (`grep -rc "cutlab-pool-filter\|cutlab-pool-search" DeckFlow.Web/wwwroot/css/site.css` == 0) and
      every new selector is compound (diff inspection).
    - Interactive controls have `min-height: 44px` (or reuse a class that does) — grep confirms.
    - `dotnet build DeckFlow.Web/DeckFlow.Web.csproj -clp:ErrorsOnly` is clean.
  </acceptance_criteria>
  <verify>
    <automated>dotnet build DeckFlow.Web/DeckFlow.Web.csproj -clp:ErrorsOnly</automated>
  </verify>
  <done>Filter/search controls + match-count + empty row exist, hidden without JS, styled as quiet secondary chrome with 44px targets.</done>
</task>

<task type="auto">
  <name>Task 2: Filter/search logic — hide (never detach), live count, whole-pool counts untouched</name>
  <files>DeckFlow.Web/wwwroot/ts/cut-lab.ts, DeckFlow.Web/ts-tests/cut-lab-pool-filter.test.ts</files>
  <read_first>
    - DeckFlow.Web/wwwroot/ts/cut-lab.ts (getPoolRows ~490, buildSnapshotFromDom ~910, DOMContentLoaded init ~3198, cssEscape helper)
    - DeckFlow.Web/ts-tests/cut-lab-lock-interactions.test.ts (existing pool-row DOM test harness to mirror)
    - .planning/phases/110-cut-lab-navigation-and-pool-discovery/110-CONTEXT.md (D-13, D-14, D-15, D-16, D-17, D-18, D-20)
    - .planning/phases/110-cut-lab-navigation-and-pool-discovery/110-UI-SPEC.md (Interaction Contract items 1,2,4,5)
  </read_first>
  <action>
    On init, remove `hidden` from `.cutlab-pool-filter` to reveal the controls (D-14). Wire the Show
    group and search input so they combine with AND over `tr[data-cut-lab-card]` rows: match by
    locked-state read from each row's `input[data-cut-lab-lock-card]` checkbox (`.checked`) for
    Locked/Unlocked, and by case-insensitive substring of the card name (`data-cut-lab-card`) for
    search. Non-matching rows get the `hidden` attribute (or a hiding class) — NEVER
    `remove()`/detach, so getPoolRows()/buildSnapshotFromDom() still see every row (D-13). Update the
    `.cutlab-pool-match-count` text to "Showing {N} of {M} cards" where M is the full row count and N
    the visible count (D-16); toggle the `.cutlab-pool-empty-row` visible only when N==0. Do NOT touch
    `data-cut-lab-lock-count` or any role-group `data-cut-lab-group-locked` count (D-18). Scope every
    query to the pool table — do not filter role-group accordions or Structural chips (D-17, D-20). No
    persistence: state is not saved and resets on load (D-15). Never include the commander row's
    disabled state in a way that drops it from serialization. LF endings. Add a vitest (jsdom) spec
    asserting: (a) after filtering to Unlocked, getPoolRows() still returns ALL rows (hidden ones
    included); (b) match count reflects visible/total; (c) empty row shows at zero matches; (d)
    data-cut-lab-lock-count text is unchanged by filtering.
  </action>
  <acceptance_criteria>
    - The filter path toggles `hidden` on rows and contains NO `.remove(`/`removeChild`/detach on
      `tr[data-cut-lab-card]` (grep the new code path).
    - vitest asserts `getPoolRows().length` is constant regardless of active filter (D-13 by construction).
    - vitest asserts the match-count string matches `Showing \d+ of \d+ cards` and updates on
      filter/search change, and that the empty row toggles at zero.
    - vitest asserts `data-cut-lab-lock-count` textContent is byte-identical before and after applying
      a filter (D-18).
    - `grep -c "cutlab-pool-filter\|cutlab-pool-search\|cutlab-pool-match-count" DeckFlow.Web/wwwroot/ts/cut-lab.ts` >= 3.
    - `cd DeckFlow.Web && npx --no-install vitest run ts-tests/cut-lab-pool-filter.test.ts` passes.
    - `cd DeckFlow.Web && npx --no-install tsc -p tsconfig.json --noEmit` is clean.
  </acceptance_criteria>
  <verify>
    <automated>cd DeckFlow.Web && npx --no-install vitest run ts-tests/cut-lab-pool-filter.test.ts</automated>
  </verify>
  <done>Filter + search hide rows without detaching, show a live count and empty-state, leave whole-pool status counts and serialization intact, and reset on load.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client filter → serialized state | Filtering must not alter the lock/package state that round-trips to the server on submit |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-110-07 | Tampering | Silent loss of lock/package state via detached rows | mitigate | Rows are hidden via [hidden], never removed (D-13); vitest asserts getPoolRows() count invariant so buildSnapshotFromDom() still serializes every card |
| T-110-08 | Information Disclosure | "Empty pool" confusion hiding still-locked cards | mitigate | Whole-pool status counts stay filter-independent (D-18); match count + "No cards match." make it clear hidden cards still exist and are still locked (D-16) |
</threat_model>

<verification>
- Razor + tsc + vitest clean.
- getPoolRows() invariant proven under filter; status counts untouched.
- Controls hidden with no JS; scope limited to the pool table.
</verification>

<success_criteria>
The Lock your pool table filters by lock state and searches by card name, hiding non-matching rows
without changing lock/package state or whole-pool counts, with a live match count and empty-state and
no persistence.
</success_criteria>

<output>
Create `.planning/phases/110-cut-lab-navigation-and-pool-discovery/110-04-SUMMARY.md` when done.
</output>
