---
phase: 45-harvest-distill-ui
plan: 02
type: execute
wave: 2
depends_on: ["45-01"]
files_modified:
  - DeckFlow.Studio/StudioDistillConfig.cs
  - DeckFlow.Studio/SessionCapOverride.cs
  - DeckFlow.Studio/VideoStatus.cs
  - DeckFlow.Studio/Services/VideoStatusResolver.cs
  - DeckFlow.Studio/Program.cs
  - DeckFlow.Core.Tests/VideoStatusResolverTests.cs
autonomous: true
requirements: [HARV-03, HARV-05]
must_haves:
  truths:
    - "StudioDistillConfig carries a resolved IsSubscriptionProvider bool injectable into Blazor components"
    - "SessionCapOverride is a registered singleton whose OverrideUsd, when set, raises the ledger cap for the whole circuit (orchestrator included)"
    - "A single ILlmSpendLedger singleton is registered with a resolver closure that reads SessionCapOverride"
    - "VideoStatusResolver maps (blocked, distilled, harvested) signals to the UI-SPEC badge vocabulary deterministically"
  artifacts:
    - path: "DeckFlow.Studio/StudioDistillConfig.cs"
      provides: "Resolved subscription-provider flag (D-01)"
      contains: "public sealed record StudioDistillConfig"
    - path: "DeckFlow.Studio/SessionCapOverride.cs"
      provides: "Session-only mutable cap override (D-03)"
      contains: "public sealed class SessionCapOverride"
    - path: "DeckFlow.Studio/VideoStatus.cs"
      provides: "Badge state enum matching UI-SPEC vocabulary"
      contains: "enum VideoStatus"
    - path: "DeckFlow.Studio/Services/VideoStatusResolver.cs"
      provides: "Testable badge-resolution logic over the three stores"
      contains: "VideoStatusResolver"
    - path: "DeckFlow.Studio/Program.cs"
      provides: "DI wiring for the config record, override singleton, override-aware ledger, and resolver"
      contains: "SessionCapOverride"
  key_links:
    - from: "DeckFlow.Studio/Program.cs"
      to: "SessionCapOverride"
      via: "ledger resolver closure reads OverrideUsd for DECKFLOW_LLM_MONTHLY_CAP_USD"
      pattern: "OverrideUsd"
    - from: "DeckFlow.Studio/Services/VideoStatusResolver.cs"
      to: "IBlockedVideoStore / IContentSiteIndexStore"
      via: "ResolveStatusAsync queries blocked then distilled proxy"
      pattern: "IsBlockedAsync|GetByNaturalKeyAsync"
---

<objective>
Wire the Studio composition root for the Harvest page (Wave 3/4) and extract the per-video
badge-resolution logic into a testable Studio service.

This wave adds: (1) `StudioDistillConfig` — the host-resolved `isSubscriptionProvider` flag
(D-01); (2) `SessionCapOverride` — the in-memory session cap holder (D-03); (3) an
override-aware `ILlmSpendLedger` registration so a raised cap is seen by both the page and the
orchestrator's internal `WouldExceedCapAsync` (Pitfall 6); (4) a `VideoStatus` enum + a
`VideoStatusResolver` service implementing the UI-SPEC badge vocabulary (HARV-03), pulled out of
the Razor page so it can be unit-tested in Core.Tests.

Purpose: Give Wave 3/4 ready-to-inject services and make the only non-trivial display logic
(badge resolution) automatically verifiable, since Studio has no test project and Razor cannot
be unit-tested in this repo.
Output: Four new Studio files, a modified `Program.cs`, and one new Core.Tests file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/45-harvest-distill-ui/45-CONTEXT.md
@.planning/phases/45-harvest-distill-ui/45-RESEARCH.md
@.planning/phases/45-harvest-distill-ui/45-PATTERNS.md
@.planning/phases/45-harvest-distill-ui/45-UI-SPEC.md
@DeckFlow.Studio/Program.cs
@DeckFlow.Studio/StudioConfig.cs

<interfaces>
<!-- Confirmed contracts. Do NOT explore the codebase for these. -->

StudioConfig.cs analog (exact record pattern to copy):
  namespace DeckFlow.Studio;
  public sealed record StudioConfig(bool IsProdConfigured);

Stores already registered as singletons in Program.cs (lines 47-56):
  IContentSourceStore, IContentVideoStore, IContentSiteIndexStore, IBlockedVideoStore,
  IContentHarvestRunStore, ILlmSpendLedger (currently `_ => new LlmSpendLedger(contentKbDatabasePath)` line 52),
  IWhisperSpendLedger, IYouTubeChannelVideoLister.

LlmSpendLedger ctor (confirmed):
  public LlmSpendLedger(string databasePath, Func<string, string?>? configurationValueResolver = null);

LlmDistillationProviderFactory (confirmed):
  public const string EnvironmentVariableName = "DECKFLOW_LLM_PROVIDER";   // value "DECKFLOW_LLM_PROVIDER"

Badge stores (confirmed signatures):
  IBlockedVideoStore.IsBlockedAsync(string youtubeVideoId, CancellationToken ct = default) -> Task<bool>
  IContentSiteIndexStore.GetByNaturalKeyAsync(string naturalKeyType, string naturalKeyValue, CancellationToken ct = default) -> Task<ContentSiteIndexRow?>
  ContentSourceType.Youtube  (DeckFlow.Core.Knowledge) — the naturalKeyType value "youtube"
  IContentVideoStore.GetVideoByYoutubeIdAsync(long sourceId, string youtubeVideoId, CancellationToken ct = default) -> Task<ContentVideo?>  // requires sourceId — see badge note

UI-SPEC badge vocabulary (45-UI-SPEC.md lines 110-126):
  not-harvested | harvested | distilled | blocked | duplicate
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add StudioDistillConfig, SessionCapOverride, and override-aware DI wiring</name>
  <files>DeckFlow.Studio/StudioDistillConfig.cs, DeckFlow.Studio/SessionCapOverride.cs, DeckFlow.Studio/Program.cs</files>
  <read_first>
    - DeckFlow.Studio/Program.cs (lines 35-72 — configuration read, store registrations, AddContentKbOrchestrator)
    - DeckFlow.Studio/StudioConfig.cs (the exact sealed-record analog)
    - .planning/phases/45-harvest-distill-ui/45-PATTERNS.md (sections "StudioDistillConfig.cs" lines 239-260, "SessionCapOverride.cs" lines 264-285, "Program.cs" lines 192-235 — copy-ready registration blocks)
    - .planning/phases/45-harvest-distill-ui/45-RESEARCH.md (Pattern 4 lines 384-407, Pattern 5 lines 411-447, Pitfall 6 lines 562-572 — single ledger singleton must be shared with orchestrator)
  </read_first>
  <action>
    Create `DeckFlow.Studio/StudioDistillConfig.cs`: a `public sealed record
    StudioDistillConfig(bool IsSubscriptionProvider);` in `namespace DeckFlow.Studio;`, with an
    XML `<summary>` matching the StudioConfig analog (one type per file, file-scoped namespace).

    Create `DeckFlow.Studio/SessionCapOverride.cs`: a `public sealed class SessionCapOverride`
    (class, not record — it has mutable state) in `namespace DeckFlow.Studio;` exposing
    `public decimal? OverrideUsd { get; set; }` with XML docs noting it is session-only and
    resets to env/default ($15.00) on restart (D-03). Do NOT convert the auto-property to
    get-only (carve-out).

    In `Program.cs`: REPLACE the existing single-line `ILlmSpendLedger` registration (line 52)
    with override-aware wiring: construct `var capOverride = new SessionCapOverride();`, register
    it via `builder.Services.AddSingleton(capOverride);`, then register
    `builder.Services.AddSingleton<ILlmSpendLedger>(_ => new LlmSpendLedger(contentKbDatabasePath, key => ...))`
    where the resolver closure returns `capOverride.OverrideUsd.Value.ToString("F2", CultureInfo.InvariantCulture)`
    when `key == "DECKFLOW_LLM_MONTHLY_CAP_USD"` and `capOverride.OverrideUsd.HasValue`, otherwise
    `null` (fall through to env var / default). This single shared singleton is also injected into
    the orchestrator, so the override reaches the orchestrator's internal `WouldExceedCapAsync`
    (Pitfall 6) — do NOT register a second ledger instance.

    Also in `Program.cs`, before `AddContentKbOrchestrator()`, derive the subscription flag:
    read `builder.Configuration["DECKFLOW_LLM_PROVIDER"] ?? Environment.GetEnvironmentVariable(LlmDistillationProviderFactory.EnvironmentVariableName)`;
    set `isSubscriptionProvider = !string.IsNullOrWhiteSpace(providerEnv) && !string.Equals(providerEnv.Trim(), "openai", StringComparison.OrdinalIgnoreCase)`
    (replicates DeckFlow.CLI/ContentKbCommandRunners derivation — D-01); register
    `builder.Services.AddSingleton(new StudioDistillConfig(isSubscriptionProvider));`.

    Add required `using System.Globalization;` (for `CultureInfo`). `LlmDistillationProviderFactory`
    is in `DeckFlow.Core.Integration` which is already imported (line 2). Keep using-directive
    order (System.* first). Touch only changed lines; preserve LF; no mass reflow.
  </action>
  <verify>
    <automated>"/mnt/c/Program Files/dotnet/dotnet.exe" build DeckFlow.sln 2>&1 | grep -E "error|Build succeeded" | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "public sealed record StudioDistillConfig(bool IsSubscriptionProvider)" DeckFlow.Studio/StudioDistillConfig.cs` returns 1.
    - `grep -c "public decimal? OverrideUsd { get; set; }" DeckFlow.Studio/SessionCapOverride.cs` returns 1.
    - `grep -c "AddSingleton(capOverride)" DeckFlow.Studio/Program.cs` returns 1.
    - `grep -c "capOverride.OverrideUsd" DeckFlow.Studio/Program.cs` returns at least 1, and `grep -c "new LlmSpendLedger(contentKbDatabasePath" DeckFlow.Studio/Program.cs` returns 1 (single ledger registration, override-aware).
    - `grep -c "new StudioDistillConfig(isSubscriptionProvider)" DeckFlow.Studio/Program.cs` returns 1.
    - `dotnet build DeckFlow.sln` reports Build succeeded, zero errors, zero new warnings.
  </acceptance_criteria>
  <done>StudioDistillConfig + SessionCapOverride exist; Program.cs registers both plus a single override-aware ILlmSpendLedger singleton and the resolved subscription flag; solution builds clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extract VideoStatus enum + VideoStatusResolver with Core.Tests coverage</name>
  <files>DeckFlow.Studio/VideoStatus.cs, DeckFlow.Studio/Services/VideoStatusResolver.cs, DeckFlow.Studio/Program.cs, DeckFlow.Core.Tests/VideoStatusResolverTests.cs</files>
  <read_first>
    - .planning/phases/45-harvest-distill-ui/45-UI-SPEC.md (lines 110-126 — Status Badge Vocabulary + resolution rules)
    - .planning/phases/45-harvest-distill-ui/45-RESEARCH.md (Pattern 3 lines 341-382 — ResolveStatusAsync rules; Open Question 1 lines 721-730 — use GetByNaturalKeyAsync as the harvested/distilled proxy to avoid sourceId)
    - DeckFlow.Core/Content/IBlockedVideoStore.cs (IsBlockedAsync)
    - DeckFlow.Core/Content/IContentSiteIndexStore.cs (GetByNaturalKeyAsync)
    - DeckFlow.Core.Tests/LlmSpendLedgerTests.cs (xUnit sealed-class + [Fact] convention to mirror; no mocking library present)
  </read_first>
  <behavior>
    - Blocked video (IsBlockedAsync == true) resolves to VideoStatus.Blocked regardless of other signals (blocked checked first).
    - Not blocked + a content_site_index row exists (GetByNaturalKeyAsync returns non-null) resolves to VideoStatus.Distilled.
    - Not blocked + no site-index row + a harvested signal present resolves to VideoStatus.Harvested.
    - Not blocked + no site-index row + no harvested signal resolves to VideoStatus.NotHarvested.
    - The enum members map one-to-one to the five UI-SPEC badge labels (NotHarvested, Harvested, Distilled, Blocked, Duplicate).
  </behavior>
  <action>
    Create `DeckFlow.Studio/VideoStatus.cs`: a `public enum VideoStatus { NotHarvested, Harvested,
    Distilled, Blocked, Duplicate }` in `namespace DeckFlow.Studio;` with XML docs; member order
    mirrors the UI-SPEC vocabulary table.

    Create `DeckFlow.Studio/Services/VideoStatusResolver.cs`: a `public sealed class
    VideoStatusResolver` in `namespace DeckFlow.Studio.Services;`. Constructor takes
    `IBlockedVideoStore`, `IContentSiteIndexStore` (and `IContentVideoStore` only if the harvested
    proxy needs it — see note). Add `ArgumentNullException.ThrowIfNull` guards (project
    convention). Expose `public async Task<VideoStatus> ResolveStatusAsync(string youtubeVideoId,
    CancellationToken ct = default)` implementing the resolution rules: (1) if
    `BlockedStore.IsBlockedAsync(youtubeVideoId, ct)` → `Blocked`; (2) if
    `IndexStore.GetByNaturalKeyAsync(ContentSourceType.Youtube, youtubeVideoId, ct)` is non-null →
    `Distilled`. For the "harvested but not yet distilled" signal, per RESEARCH Open Question 1
    use `GetByNaturalKeyAsync` as the primary distilled proxy; to detect harvested-only, accept a
    `harvestedProbe` strategy — the simplest correct path is a method overload
    `ResolveStatusAsync(string youtubeVideoId, Func<string, CancellationToken, Task<bool>> isHarvestedProbe, CancellationToken ct)`
    OR have the resolver own a `Func<string, Task<bool>>` harvested-check supplied at construction.
    Choose the construction-injected `Func<string, CancellationToken, Task<bool>>` harvested-probe
    so the resolver stays decoupled from the sourceId problem and is trivially testable. When no
    harvested probe is configured, default a harvested signal to false (returns NotHarvested when
    no site-index row). Document the chosen approach in the SUMMARY.

    Register the resolver in `Program.cs` as a singleton:
    `builder.Services.AddSingleton<VideoStatusResolver>(...)` resolving the two/three store
    singletons plus the harvested probe (the probe can call `IContentVideoStore` across enabled
    sources, or be a no-op returning false if harvested-vs-distilled distinction is deferred to the
    page; keep it simple and document the choice).

    Create `DeckFlow.Core.Tests/VideoStatusResolverTests.cs`: `public sealed class
    VideoStatusResolverTests`. Since no mocking library is present (CLAUDE.md: do not add one),
    write small in-test fake implementations of `IBlockedVideoStore` and `IContentSiteIndexStore`
    (Fake* convention — return configured values, throw NotSupportedException on unused members) to
    drive the four behavior cases above as `[Fact]` methods named `Method_Scenario_ExpectedResult`.
    The fakes live in the test file. Cover: Blocked-wins, Distilled-when-index-row, Harvested-when-probe-true,
    NotHarvested-when-no-signals.

    Preserve LF; one public type per file; touch only changed lines in Program.cs.
  </action>
  <verify>
    <automated>"/mnt/c/Program Files/dotnet/dotnet.exe" test DeckFlow.Core.Tests/ --filter "FullyQualifiedName~VideoStatusResolverTests" 2>&1 | grep -E "Passed!|Failed!|error" | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "public enum VideoStatus" DeckFlow.Studio/VideoStatus.cs` returns 1 and the enum lists NotHarvested, Harvested, Distilled, Blocked, Duplicate.
    - `grep -c "public sealed class VideoStatusResolver" DeckFlow.Studio/Services/VideoStatusResolver.cs` returns 1.
    - `grep -c "IsBlockedAsync" DeckFlow.Studio/Services/VideoStatusResolver.cs` >= 1 and `grep -c "GetByNaturalKeyAsync" DeckFlow.Studio/Services/VideoStatusResolver.cs` >= 1 (blocked-first, distilled-proxy resolution wired).
    - `grep -c "AddSingleton<VideoStatusResolver>" DeckFlow.Studio/Program.cs` returns 1.
    - `VideoStatusResolverTests` contains four [Fact] methods covering blocked / distilled / harvested / not-harvested, using in-file fakes (no mocking-library `using`).
    - `dotnet test DeckFlow.Core.Tests/ --filter "FullyQualifiedName~VideoStatusResolverTests"` reports all passed. WSL fallback: `dotnet build DeckFlow.sln` clean + flag manual test run in SUMMARY.
  </acceptance_criteria>
  <done>VideoStatus enum + VideoStatusResolver exist, registered in DI, with four passing Core.Tests cases driven by in-file fakes; solution builds clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator browser → Studio circuit | Cap override value enters via the page (Wave 4); this wave creates the in-memory holder it lands in |
| Studio config → ledger / orchestrator | The single shared ledger singleton is the only cap source; a duplicate would bypass the override |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-45-03 | Elevation of privilege | SessionCapOverride raised to extreme value | accept | Override is in-memory only and resets to env/default on restart (D-03). LLM-cost exposure is bounded to a single operator session; no persistence. Wave 4 still gates spend behind dry-run + confirm. |
| T-45-04 | Tampering | Two separate ILlmSpendLedger instances | mitigate | Register exactly one override-aware ledger singleton; the orchestrator injects the same instance so its internal WouldExceedCapAsync sees the override (Pitfall 6). Acceptance asserts a single `new LlmSpendLedger` registration. |
| T-45-05 | Information disclosure | Provider/cap values in logs | mitigate | No new log statements emit DECKFLOW_LLM_PROVIDER, the connection string, or cap values; existing Program.cs logs only "configured / not configured" (line 82). New code adds no secret logging. |
| T-45-SC | Tampering | npm/pip/cargo installs | accept | No package installs; no mocking library added (in-file fakes only) per CLAUDE.md. |
</threat_model>

<verification>
- `dotnet build DeckFlow.sln` — zero errors, zero new warnings.
- `dotnet test DeckFlow.Core.Tests/ --filter "FullyQualifiedName~VideoStatusResolverTests"` — all pass.
- Exactly one `new LlmSpendLedger(...)` registration in Program.cs.
- No secret/provider/cap value logged in new code.
</verification>

<success_criteria>
- StudioDistillConfig + SessionCapOverride + VideoStatus + VideoStatusResolver exist and are DI-registered.
- The ledger singleton is override-aware and shared with the orchestrator.
- Badge-resolution logic is unit-tested (HARV-03 automated signal).
</success_criteria>

<output>
Create `.planning/phases/45-harvest-distill-ui/45-02-SUMMARY.md` when done.
</output>
