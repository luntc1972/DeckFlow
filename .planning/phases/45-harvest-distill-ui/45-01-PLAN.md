---
phase: 45-harvest-distill-ui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - DeckFlow.Core/Content/ILlmSpendLedger.cs
  - DeckFlow.Core/Content/SpendLedgerBase.cs
  - DeckFlow.Core.Tests/LlmSpendLedgerTests.cs
autonomous: true
requirements: [HARV-05]
must_haves:
  truths:
    - "ILlmSpendLedger exposes a synchronous GetMonthlyCapUsd() returning the configured monthly cap"
    - "GetMonthlyCapUsd() returns $15.00 when no cap is configured and no env var is set"
    - "GetMonthlyCapUsd() returns the configured value when a configurationValueResolver supplies one"
    - "A resolver-supplied cap override flows into WouldExceedCapAsync (raised cap permits previously-blocked spend)"
  artifacts:
    - path: "DeckFlow.Core/Content/ILlmSpendLedger.cs"
      provides: "GetMonthlyCapUsd() interface member"
      contains: "decimal GetMonthlyCapUsd()"
    - path: "DeckFlow.Core/Content/SpendLedgerBase.cs"
      provides: "public GetMonthlyCapUsd() implementation; ReadMonthlyCapUsd promoted to protected"
      contains: "public decimal GetMonthlyCapUsd()"
    - path: "DeckFlow.Core.Tests/LlmSpendLedgerTests.cs"
      provides: "Cap getter + session-override-affects-cap coverage"
      contains: "GetMonthlyCapUsd"
  key_links:
    - from: "DeckFlow.Core/Content/SpendLedgerBase.cs"
      to: "ReadMonthlyCapUsd()"
      via: "GetMonthlyCapUsd() delegates to ReadMonthlyCapUsd()"
      pattern: "GetMonthlyCapUsd\\(\\) => ReadMonthlyCapUsd\\(\\)"
---

<objective>
Add a read surface to the LLM spend ledger so the Harvest page can display "monthly cap"
and "remaining" (HARV-05 / D-02), and prove that a resolver-supplied cap override actually
changes `WouldExceedCapAsync` behavior (the foundation for the Studio session override, D-03).

This is a Core-only enabler wave. No Studio code, no UI. It unblocks the spend-gate UI in
Wave 4 by giving the page a cap getter that does not re-implement the env-var read logic.

Purpose: Expose the configured monthly cap through `ILlmSpendLedger` without breaking the
existing `WouldExceedCapAsync` / `GetMonthlyTotalAsync` contract or the resolver mechanism.
Output: One new interface member, one promoted method + one new public method on the base
class, and two new xUnit tests in the existing `LlmSpendLedgerTests` fixture.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/45-harvest-distill-ui/45-CONTEXT.md
@.planning/phases/45-harvest-distill-ui/45-RESEARCH.md
@.planning/phases/45-harvest-distill-ui/45-PATTERNS.md

<interfaces>
<!-- Confirmed contracts the executor edits. Do NOT explore the codebase for these. -->

Existing ILlmSpendLedger members (DeckFlow.Core/Content/ILlmSpendLedger.cs) — keep all:
  Task RecordCallAsync(long videoId, int inputTokens, int outputTokens, decimal costUsd, string monthKey, CancellationToken ct = default);
  Task<decimal> GetMonthlyTotalAsync(string yearMonth, CancellationToken ct = default);
  Task<bool> WouldExceedCapAsync(decimal projectedCallCostUsd, string monthKey, CancellationToken ct = default);

SpendLedgerBase (DeckFlow.Core/Content/SpendLedgerBase.cs):
  - private static readonly decimal DefaultMonthlyCapUsd = 15.00m;
  - private decimal ReadMonthlyCapUsd()  // lines 135-151 — env/resolver read, defaults to 15.00m
  - WouldExceedCapAsync (lines 116-127) already calls ReadMonthlyCapUsd()
  - Concrete subclasses LlmSpendLedger and WhisperSpendLedger inherit; neither file changes.

LlmSpendLedger ctors (confirmed, DeckFlow.Core/Content/LlmSpendLedger.cs):
  public LlmSpendLedger(string databasePath, Func<string, string?>? configurationValueResolver = null);
  public LlmSpendLedger(RelationalDatabaseConnection connectionInfo, Func<string, string?>? configurationValueResolver = null);

Test fixture (DeckFlow.Core.Tests/LlmSpendLedgerTests.cs, confirmed):
  - public sealed class LlmSpendLedgerTests : IDisposable
  - field `_ledger = new LlmSpendLedger(_dbPath);` (no resolver, no env) — line 24
  - helper `private static Func<string, string?> BuildConfiguration(string capUsd) => key => key == "DECKFLOW_LLM_MONTHLY_CAP_USD" ? capUsd : null;` — lines 120-121
  - existing tests construct `new LlmSpendLedger(_dbPath, BuildConfiguration("0.50"))` — line 73
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Expose GetMonthlyCapUsd() on the ledger interface and base class</name>
  <files>DeckFlow.Core/Content/ILlmSpendLedger.cs, DeckFlow.Core/Content/SpendLedgerBase.cs</files>
  <read_first>
    - DeckFlow.Core/Content/ILlmSpendLedger.cs (the interface being modified)
    - DeckFlow.Core/Content/SpendLedgerBase.cs (lines 116-151 — WouldExceedCapAsync + ReadMonthlyCapUsd)
    - .planning/phases/45-harvest-distill-ui/45-PATTERNS.md (sections "ILlmSpendLedger.cs" lines 289-313 and "SpendLedgerBase.cs" lines 317-353 — exact member text + promotion instructions)
    - .planning/phases/45-harvest-distill-ui/45-RESEARCH.md (Pattern 5, lines 411-465 — "Simplest path: add decimal GetMonthlyCapUsd()")
  </read_first>
  <behavior>
    - GetMonthlyCapUsd() returns 15.00m when no resolver value and no DECKFLOW_LLM_MONTHLY_CAP_USD env var are present.
    - GetMonthlyCapUsd() returns the resolver-supplied value when a configurationValueResolver returns a parseable decimal for the cap key.
    - GetMonthlyCapUsd() is synchronous (no Task), matching the page's render-time display need.
  </behavior>
  <action>
    Add a new member `decimal GetMonthlyCapUsd();` to the `ILlmSpendLedger` interface in
    DeckFlow.Core/Content/ILlmSpendLedger.cs, appended after `WouldExceedCapAsync` before the
    closing brace, with a `<summary>` XML doc and `<returns>` tag matching the existing member
    documentation style (XML doc on every member — project convention).

    In DeckFlow.Core/Content/SpendLedgerBase.cs: change the visibility of `ReadMonthlyCapUsd()`
    from `private` to `protected` (visibility-only change on line 135 — no logic change, do not
    touch the method body or the `DefaultMonthlyCapUsd` field). Then add a new public method
    `public decimal GetMonthlyCapUsd() => ReadMonthlyCapUsd();` with an `<inheritdoc />` doc
    comment. Place it adjacent to `WouldExceedCapAsync`. Because `LlmSpendLedger` and
    `WhisperSpendLedger` derive from `SpendLedgerBase`, both satisfy the new interface member
    without editing their files. Do NOT add the member to the concrete ledger files.

    Honor the format-gate carve-outs: touch only the changed lines, preserve LF, do not
    re-indent surrounding code, do not inline attributes, do not convert `init` accessors.
  </action>
  <verify>
    <automated>"/mnt/c/Program Files/dotnet/dotnet.exe" build DeckFlow.sln 2>&1 | grep -E "error|Build succeeded" | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "decimal GetMonthlyCapUsd()" DeckFlow.Core/Content/ILlmSpendLedger.cs` returns 1.
    - `grep -c "public decimal GetMonthlyCapUsd() => ReadMonthlyCapUsd();" DeckFlow.Core/Content/SpendLedgerBase.cs` returns 1.
    - `grep -c "protected decimal ReadMonthlyCapUsd()" DeckFlow.Core/Content/SpendLedgerBase.cs` returns 1 (promotion applied; no `private decimal ReadMonthlyCapUsd()` remains).
    - `DeckFlow.Core/Content/LlmSpendLedger.cs` and `WhisperSpendLedger.cs` are unchanged (`git diff --name-only` does not list them).
    - `dotnet build DeckFlow.sln` reports Build succeeded with zero errors and zero new warnings.
  </acceptance_criteria>
  <done>ILlmSpendLedger declares GetMonthlyCapUsd(); SpendLedgerBase implements it publicly via the promoted ReadMonthlyCapUsd(); solution builds clean; both concrete ledger files untouched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add Core.Tests coverage for cap getter and resolver-override-affects-cap</name>
  <files>DeckFlow.Core.Tests/LlmSpendLedgerTests.cs</files>
  <read_first>
    - DeckFlow.Core.Tests/LlmSpendLedgerTests.cs (the file being modified — constructor, Dispose, BuildConfiguration helper, existing [Fact] structure)
    - .planning/phases/45-harvest-distill-ui/45-PATTERNS.md (section "LlmSpendLedgerTests.cs" lines 357-394 — two new test bodies + reuse rules; "ConfigurationValueResolver Helper" lines 440-447)
    - .planning/phases/45-harvest-distill-ui/45-VALIDATION.md (Wave 0 Requirements lines 59-66 — D-02 and D-03 unit coverage)
  </read_first>
  <behavior>
    - Test: GetMonthlyCapUsd on a ledger built with no resolver returns 15.00m (default).
    - Test: GetMonthlyCapUsd on a ledger built with BuildConfiguration("25.00") returns 25.00m.
    - Test (D-03 override semantics): with a base cap of $0.50, WouldExceedCapAsync(0.40, month) after recording a $0.40 call returns true; a ledger built with a raised cap ($5.00) for the same recorded spend returns false — proving a resolver-supplied cap (the mechanism the Studio SessionCapOverride uses) flows into the cap check.
  </behavior>
  <action>
    Append xUnit `[Fact]` methods to the existing `LlmSpendLedgerTests` class. Follow the
    established `Method_Scenario_ExpectedResult` naming. Reuse the existing `_ledger` field for
    the default case and the existing `BuildConfiguration(string capUsd)` helper for resolver
    cases — do NOT add a new temp-file fixture or duplicate the helper.

    1) `GetMonthlyCapUsd_ReturnsDefaultWhenNoConfigurationSet`: assert `_ledger.GetMonthlyCapUsd()`
       equals `15.00m`.
    2) `GetMonthlyCapUsd_ReturnsConfiguredValueWhenResolverProvided`: construct
       `new LlmSpendLedger(_dbPath, BuildConfiguration("25.00"))`, assert `GetMonthlyCapUsd()`
       equals `25.00m`.
    3) `WouldExceedCapAsync_RespectsRaisedCapFromResolver` (D-03 mechanism): pick a month key
       (e.g. `"2026-06"`); on a `new LlmSpendLedger(_dbPath, BuildConfiguration("0.50"))` record
       a call via `RecordCallAsync(videoId, inputTokens, outputTokens, 0.40m, monthKey)` using an
       inserted video id (reuse the existing `InsertVideoAsync` helper that the current tests use
       for `RecordCallAsync`), then assert `WouldExceedCapAsync(0.40m, monthKey)` is `true`
       against the $0.50 cap; construct a second ledger over the same `_dbPath` with
       `BuildConfiguration("5.00")` and assert `WouldExceedCapAsync(0.40m, monthKey)` is `false`,
       proving the resolver-supplied cap raises the effective ceiling.

    If the recorded-spend setup needs an existing helper not named above, mirror exactly how the
    current `WouldExceedCapAsync` tests (around line 73-100) seed spend rows. Do not introduce a
    mocking library. Preserve LF and the file's existing formatting; touch only added lines.
  </action>
  <verify>
    <automated>"/mnt/c/Program Files/dotnet/dotnet.exe" test DeckFlow.Core.Tests/ --filter "FullyQualifiedName~LlmSpendLedgerTests" 2>&1 | grep -E "Passed!|Failed!|error" | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "GetMonthlyCapUsd_ReturnsDefaultWhenNoConfigurationSet" DeckFlow.Core.Tests/LlmSpendLedgerTests.cs` returns 1.
    - `grep -c "WouldExceedCapAsync_RespectsRaisedCapFromResolver" DeckFlow.Core.Tests/LlmSpendLedgerTests.cs` returns 1.
    - The three new tests reference no new temp-file path and call the existing `BuildConfiguration` helper (no duplicate `Func<string, string?> BuildConfiguration` definition added).
    - `dotnet test DeckFlow.Core.Tests/ --filter "FullyQualifiedName~LlmSpendLedgerTests"` reports all tests passed (Passed!), zero failures.
    - If WSL VSTest is unreliable, a clean `dotnet build DeckFlow.Core.Tests/DeckFlow.Core.Tests.csproj` is the fallback signal; note the build-only result in the SUMMARY and flag the test run for the reviewer.
  </acceptance_criteria>
  <done>Two cap-getter tests and one resolver-raises-cap test exist in the LlmSpendLedgerTests fixture, reuse the existing helpers, and pass (or build clean with a flagged manual test run under WSL).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Studio composition root → ledger | The configurationValueResolver closure (Wave 2) supplies the cap; this wave proves a resolver-supplied cap is honored without persistence |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-45-01 | Elevation of privilege | GetMonthlyCapUsd / cap read | mitigate | Cap getter is read-only; it never writes env or any store. A raised cap can only come from the in-memory resolver, which has no persistence path in this wave. |
| T-45-02 | Tampering | WouldExceedCapAsync cap source | mitigate | Tests assert the cap actually changes the WouldExceedCapAsync decision, so the spend guard cannot be silently bypassed by a no-op getter. |
| T-45-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan; no new dependency added (CLAUDE.md no-new-packages constraint). |
</threat_model>

<verification>
- `dotnet build DeckFlow.sln` — zero errors, zero new warnings.
- `dotnet test DeckFlow.Core.Tests/ --filter "FullyQualifiedName~LlmSpendLedgerTests"` — all pass.
- `git diff --name-only` lists only the three files in `files_modified` (LlmSpendLedger.cs / WhisperSpendLedger.cs NOT listed).
</verification>

<success_criteria>
- `ILlmSpendLedger.GetMonthlyCapUsd()` exists and returns the configured/default cap.
- A resolver-supplied cap demonstrably changes `WouldExceedCapAsync` (D-03 mechanism proven).
- Solution builds clean; Core.Tests pass; concrete ledger files untouched.
</success_criteria>

<output>
Create `.planning/phases/45-harvest-distill-ui/45-01-SUMMARY.md` when done.
</output>
