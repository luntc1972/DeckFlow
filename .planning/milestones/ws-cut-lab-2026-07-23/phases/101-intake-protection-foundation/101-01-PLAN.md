---
phase: 101-intake-protection-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - DeckFlow.Web/Models/DeckPageTab.cs
  - DeckFlow.Web/Services/Tools/ToolRegistry.cs
  - DeckFlow.Web.Tests/Tools/ToolRegistryTests.cs
  - DeckFlow.Web/Services/FeatureFlags/FeatureFlagCatalog.cs
  - DeckFlow.Web/Services/FeatureFlags/FeatureFlagStore.cs
  - DeckFlow.Web.Tests/FeatureFlagCatalogTests.cs
  - DeckFlow.Web.Tests/FeatureFlagStoreSeedTests.cs
  - DeckFlow.Web/Views/Shared/_ToolTileIcon.cshtml
autonomous: true
requirements: [INTAKE-01]
must_haves:
  truths:
    - "The Cut Lab tool is registered and gated behind tool.cut-lab.enabled seeded OFF in both SQLite and Postgres"
    - "The /Admin/Flags page shows a human-readable description for tool.cut-lab.enabled"
    - "A padlock tool-tile icon renders for the cut-lab IconKey"
  artifacts:
    - path: "DeckFlow.Web/Services/Tools/ToolRegistry.cs"
      provides: "cut-lab ToolDefinition entry in ToolNavSection.Build"
      contains: "cut-lab"
    - path: "DeckFlow.Web/Models/DeckPageTab.cs"
      provides: "CutLab enum member"
      contains: "CutLab = 17"
    - path: "DeckFlow.Web/Services/FeatureFlags/FeatureFlagStore.cs"
      provides: "tool.cut-lab.enabled seed row in both PostgresSeedSql and SqliteSeedSql"
      contains: "tool.cut-lab.enabled"
  key_links:
    - from: "ToolRegistry cut-lab entry"
      to: "tool.cut-lab.enabled flag"
      via: "FlagKey field"
      pattern: "tool\\.cut-lab\\.enabled"
---

<objective>
Register the Cut Lab tool as a dark-launched (flag OFF) tool in DeckFlow's single-source-of-truth
registration surfaces so Waves 2–3 can wire a controller/view behind it. This plan touches ONLY the
config/registration layer plus its guard tests. It ships no behavior — the route and page arrive in
plan 101-03/101-04.

Purpose: Every DeckFlow tool is gated by a seeded-OFF feature flag and enumerated in ToolRegistry,
DeckPageTab, and the flag catalog/store. Adding these together in one commit is mandatory because the
registration tests hand-enumerate every tool and the flag seed lives in two SQL dialect blocks.

GROUNDWORK-ONLY NOTE (requirements tagging): this plan delivers NO user-facing INTAKE-01 behavior itself — it registers a reachable, flag-gated route as the precondition for INTAKE-01. INTAKE-01 is tagged here only because parse/count/legality (101-03) and the intake form/UI (101-04) cannot exist until the tool is registered and gated. Every INTAKE-01 acceptance ultimately verifies in 101-03 and 101-04, and INTAKE-01 also appears in both of those plans' requirements fields.
Output: Cut Lab appears in ToolRegistry (Build section), has a DeckPageTab.CutLab tab identity, a
tool.cut-lab.enabled flag seeded FALSE/0 in both dialects with an operator description, a padlock
tool-tile glyph, and all four guard tests updated to match.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/cut-lab/ROADMAP.md
@.planning/workstreams/cut-lab/phases/101-intake-protection-foundation/101-RESEARCH.md
@.planning/workstreams/cut-lab/phases/101-intake-protection-foundation/101-PATTERNS.md

<interfaces>
<!-- ToolRegistry.Create signature (DeckFlow.Web/Services/Tools/ToolRegistry.cs:32-44):
Create(string key, string label, string route, ToolNavSection section, string flagKey, bool core,
       string tileTitle, string tileDescription, string? helpSlug, DeckPageTab tab,
       bool isPrimaryTile, params string[] additionalRoutes)
IconKey is auto-set to key. -->

<!-- Existing deck-history registration entry (the exact analog to mirror):
Create("deck-history", "Deck History", "/deck-history", ToolNavSection.Build,
    "tool.deck-history.enabled", false, "Deck History",
    "Track your deck's evolution ...", "deck-history", DeckPageTab.DeckHistory, true,
    "/deck-history/download"), -->

<!-- FeatureFlagStore seed blocks: PostgresSeedSql (last data row currently line 244:
  ('tool.deck-history.enabled', FALSE)) then ON CONFLICT (key) DO NOTHING;
SqliteSeedSql (last data row currently line 294: ('tool.deck-history.enabled', 0)) then
  ON CONFLICT (key) DO NOTHING; -->

<!-- ToolRegistryTests current count assertions (line 43-47):
  Assert.Equal(15, registry.All.Count); + Key/Route/Tab Distinct == 15;
  Assert.Equal(21, route+additionalRoutes distinct); coreKeys == {deck-analysis,deck-comparison,cedh-meta-gap} -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register cut-lab in ToolRegistry, DeckPageTab, tool-tile icon, and update ToolRegistryTests</name>
  <files>DeckFlow.Web/Models/DeckPageTab.cs, DeckFlow.Web/Services/Tools/ToolRegistry.cs, DeckFlow.Web/Views/Shared/_ToolTileIcon.cshtml, DeckFlow.Web.Tests/Tools/ToolRegistryTests.cs</files>
  <read_first>
    - DeckFlow.Web/Models/DeckPageTab.cs (existing members; DeckHistory = 16 is the last)
    - DeckFlow.Web/Services/Tools/ToolRegistry.cs (Definitions array + Create signature)
    - DeckFlow.Web.Tests/Tools/ToolRegistryTests.cs (BOTH All_ReturnsExpectedToolDefinitions Assert.Collection AND All_HasUniqueKeysRoutesAndTabs count assertions)
    - DeckFlow.Web/Views/Shared/_ToolTileIcon.cshtml (switch on IconKey; add a new case before default)
    - DeckFlow.Web/Services/Tools/ToolDefinition.cs (field meanings)
  </read_first>
  <action>
    Add enum member `CutLab = 17` to DeckPageTab.cs immediately after `DeckHistory = 16`, with an
    XML doc summary "Cut Lab intake, protection, and cut-recommendation page."

    Add a new ToolRegistry.Definitions entry AFTER the deck-history entry (keeping ToolNavSection.Build
    grouping). This placement — Cut Lab immediately after Deck History in the Build section — is DELIBERATE
    product intent (both are build-stage flagship flow: track history → cut the pool down to 100), so the
    corresponding ToolRegistryTests registration-order change is expected, not an accidental nav reshuffle.
    Use Create with these exact literal arguments: key "cut-lab", label "Cut Lab",
    route "/cut-lab", section ToolNavSection.Build, flagKey "tool.cut-lab.enabled", core false,
    tileTitle "Cut Lab", tileDescription "Bring an oversized 101–150 card Commander pool into a
    workspace, declare your build intent, and lock the cards, packages, and roles that must never be
    cut — before any trimming begins.", helpSlug "cut-lab", tab DeckPageTab.CutLab, isPrimaryTile false,
    and NO additionalRoutes (omit the params argument entirely).

    Add a `case "cut-lab":` branch to _ToolTileIcon.cshtml (before the default case) rendering a padlock
    outline glyph in the exact existing SVG convention: width/height 20, viewBox "0 0 20 20", fill none,
    stroke currentColor, stroke-width 1.5, stroke-linecap/linejoin round, aria-hidden true,
    focusable false — a rounded rect lock body plus a shackle arc (for example a `rect` for the body
    around y=9 and a `path` shackle arc above it). Follow the byte-shape style of the sibling cases.

    Update ToolRegistryTests.cs in the SAME edit:
    (a) In All_ReturnsExpectedToolDefinitions, add a new `tool => AssertTool(...)` row for cut-lab in
        registration order (immediately after the deck-history row) with arguments matching the Create
        call above (helpSlug "cut-lab", tab DeckPageTab.CutLab, isPrimaryTile false, no additionalRoutes).
    (b) In All_HasUniqueKeysRoutesAndTabs_AndExactlyThreeCoreTools, bump the four count assertions from
        15 to 16 (Count, distinct Keys, distinct Routes, distinct Tabs) and the route+additionalRoutes
        distinct assertion from 21 to 22. Leave the coreKeys assertion unchanged (still 3 core tools).
  </action>
  <verify>
    <automated>dotnet build DeckFlow.Web/DeckFlow.Web.csproj -c Debug --nologo -clp:ErrorsOnly && dotnet test DeckFlow.Web.Tests --filter "FullyQualifiedName~ToolRegistryTests" --nologo</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'CutLab = 17' DeckFlow.Web/Models/DeckPageTab.cs` returns 1
    - `grep -c '"cut-lab", "Cut Lab", "/cut-lab"' DeckFlow.Web/Services/Tools/ToolRegistry.cs` returns 1
    - `grep -c 'case "cut-lab":' DeckFlow.Web/Views/Shared/_ToolTileIcon.cshtml` returns 1
    - `grep -c 'Assert.Equal(16, registry.All.Count)' DeckFlow.Web.Tests/Tools/ToolRegistryTests.cs` returns 1
    - `grep -c 'Assert.Equal(22,' DeckFlow.Web.Tests/Tools/ToolRegistryTests.cs` returns 1
    - ToolRegistryTests pass (both All_ReturnsExpectedToolDefinitions and the counts test)
    - No new build warnings introduced
  </acceptance_criteria>
  <done>Cut Lab is enumerated in ToolRegistry (Build section), DeckPageTab.CutLab exists, a padlock icon renders for the cut-lab IconKey, and ToolRegistryTests are green with counts 16/16/16/16/22 and 3 core tools.</done>
</task>

<task type="auto">
  <name>Task 2: Seed tool.cut-lab.enabled OFF in both SQL dialects + catalog description + flag guard tests</name>
  <files>DeckFlow.Web/Services/FeatureFlags/FeatureFlagStore.cs, DeckFlow.Web/Services/FeatureFlags/FeatureFlagCatalog.cs, DeckFlow.Web.Tests/FeatureFlagCatalogTests.cs, DeckFlow.Web.Tests/FeatureFlagStoreSeedTests.cs</files>
  <read_first>
    - DeckFlow.Web/Services/FeatureFlags/FeatureFlagStore.cs (PostgresSeedSql ~198-246 and SqliteSeedSql ~248-296 — BOTH end their VALUES list with the deck-history row then ON CONFLICT)
    - DeckFlow.Web/Services/FeatureFlags/FeatureFlagCatalog.cs (Descriptions dictionary; tool.deck-history.enabled entry ~43-44)
    - DeckFlow.Web.Tests/FeatureFlagCatalogTests.cs (Describe_EverySeededFlag_HasNonEmptyDescription [InlineData] list, lockstep contract comment)
    - DeckFlow.Web.Tests/FeatureFlagStoreSeedTests.cs (EnsureSchema_SeedsManabaseFlags_AtExpectedDefault [InlineData] list + PostgresSeedSql reflection test pattern)
  </read_first>
  <action>
    In FeatureFlagStore.PostgresSeedSql, change the current last data row so the list continues past
    deck-history: make the deck-history row end with a comma and append a new final row
    `('tool.cut-lab.enabled', FALSE)` immediately before the `ON CONFLICT (key) DO NOTHING;` line.
    In FeatureFlagStore.SqliteSeedSql, likewise append `('tool.cut-lab.enabled', 0)` as the new final
    row before its `ON CONFLICT (key) DO NOTHING;`. Preserve the existing 2-space SQL indentation and
    do not reorder any existing row.

    In FeatureFlagCatalog.Descriptions, add the entry
    `["tool.cut-lab.enabled"] = "Cut Lab tool: intake an oversized (101–150 card) Commander pool,
    declare deck intent (primary/secondary plan, bracket, play experience), and lock cards, named
    packages, and role groups so later cut rounds can never propose them. Seeded OFF."`.

    In FeatureFlagCatalogTests.cs, add `[InlineData("tool.cut-lab.enabled")]` to the
    Describe_EverySeededFlag_HasNonEmptyDescription theory list.

    In FeatureFlagStoreSeedTests.cs, add `[InlineData("tool.cut-lab.enabled", false)] // seeded OFF`
    to the EnsureSchema_SeedsManabaseFlags_AtExpectedDefault theory list. If that file's reflection
    test asserts specific Postgres rows, do NOT add cut-lab there unless the pattern already enumerates
    every new flag — the InlineData runtime assertion is the required coverage.
  </action>
  <verify>
    <automated>dotnet test DeckFlow.Web.Tests --filter "FullyQualifiedName~FeatureFlagCatalogTests|FullyQualifiedName~FeatureFlagStoreSeedTests" --nologo</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "('tool.cut-lab.enabled', FALSE)" DeckFlow.Web/Services/FeatureFlags/FeatureFlagStore.cs` returns 1
    - `grep -c "('tool.cut-lab.enabled', 0)" DeckFlow.Web/Services/FeatureFlags/FeatureFlagStore.cs` returns 1
    - `grep -c 'tool.cut-lab.enabled' DeckFlow.Web/Services/FeatureFlags/FeatureFlagCatalog.cs` returns 1
    - `grep -c 'InlineData("tool.cut-lab.enabled")' DeckFlow.Web.Tests/FeatureFlagCatalogTests.cs` returns 1
    - `grep -c 'InlineData("tool.cut-lab.enabled", false)' DeckFlow.Web.Tests/FeatureFlagStoreSeedTests.cs` returns 1
    - Both SQL blocks still end with `ON CONFLICT (key) DO NOTHING;` and no existing row was reordered (verify with `git diff` shows only additive lines + one trailing-comma change per block)
    - FeatureFlagCatalogTests and FeatureFlagStoreSeedTests pass
  </acceptance_criteria>
  <done>tool.cut-lab.enabled is seeded FALSE (Postgres) and 0 (SQLite), has a non-empty operator description, and both flag guard test theories include it and pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → /Admin/Flags | Operator toggles the seeded flag; this plan only registers the (OFF) row |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-101-01-01 | Elevation of Privilege | Premature exposure of unfinished Cut Lab | mitigate | Flag seeded FALSE/0 in BOTH dialects; the route/view do not exist until plans 03/04, so the feature is dark until an operator explicitly flips it post-UAT |
| T-101-01-02 | Tampering | Dialect-skew (flag seeded in one backend only) | mitigate | Row added to BOTH PostgresSeedSql and SqliteSeedSql in the same commit; FeatureFlagStoreSeedTests + FeatureFlagCatalogTests guard the contract |
| T-101-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase — zero new dependencies (RESEARCH.md §Package Legitimacy Audit: not applicable) |
</threat_model>

<verification>
- `dotnet build DeckFlow.Web/DeckFlow.Web.csproj` clean (WSL baseline, VSTest unreliable in WSL per CLAUDE.md)
- `dotnet test DeckFlow.Web.Tests --filter "ToolRegistryTests|FeatureFlagCatalogTests|FeatureFlagStoreSeedTests"` green
- LF line endings preserved on every touched file; changed-lines format gate satisfied
</verification>

<success_criteria>
- Cut Lab registered in ToolRegistry (Build section) with flag tool.cut-lab.enabled and DeckPageTab.CutLab
- tool.cut-lab.enabled seeded OFF in both SQL dialects with an operator description
- Padlock tool-tile glyph renders for the cut-lab IconKey
- All four guard tests (ToolRegistryTests ×2, FeatureFlagCatalogTests, FeatureFlagStoreSeedTests) updated and green
- No new route or page is exposed by this plan (flag OFF, no controller yet)
</success_criteria>

<output>
Create `.planning/workstreams/cut-lab/phases/101-intake-protection-foundation/101-01-SUMMARY.md` when done
</output>
