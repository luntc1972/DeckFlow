---
phase: 06-admin-shell-flags-foundation
plan: 03
type: execute
wave: 2
depends_on: [01]
files_modified:
  - DeckFlow.Web/Views/AdminFeedback/_ViewStart.cshtml
autonomous: false
requirements:
  - ADMIN-03
  - ADMIN-04

must_haves:
  truths:
    - "/Admin/feedback continues working unchanged inside the new admin shell — inbox view + mark-read POST flow + Detail view all render with the dark slate sidebar instead of guild-themed public chrome (ADMIN-04 no regression)."
    - "Existing AdminFeedbackController code is NOT touched — D-15 layout-swap-only constraint enforced (verified by `git diff` showing zero changes outside the new _ViewStart.cshtml file)."
    - "BasicAuth still gates /Admin/Feedback unchanged — Program.cs:330-332 MapWhen branch covers all /Admin paths including the layout-swapped page (ADMIN-03)."
    - "Threat T-06-C1 (regression on existing route) — route Admin/Feedback unchanged, mark-read POST verb unchanged, antiforgery token still valid; verified by curl to /Admin/Feedback returning 401 unauthenticated and 200 with sidebar after auth."
  artifacts:
    - path: "DeckFlow.Web/Views/AdminFeedback/_ViewStart.cshtml"
      provides: "Per-folder layout override for AdminFeedback views (3 lines, sets Layout = '_AdminLayout')"
      contains: "Layout = \"_AdminLayout\""
  key_links:
    - from: "Views/AdminFeedback/_ViewStart.cshtml"
      to: "Views/Shared/_AdminLayout.cshtml"
      via: "Layout = \"_AdminLayout\""
      pattern: "Layout\\s*=\\s*\"_AdminLayout\""
---

<objective>
Migrate the existing `/Admin/Feedback` page from the public-site `_Layout.cshtml` (with guild theme leakage) into the new `_AdminLayout.cshtml` shell — by adding ONLY a single 3-line `_ViewStart.cshtml` to the AdminFeedback views folder. Zero controller edits, zero view-body edits, zero route changes.

Purpose: D-15 is explicit — "layout-swap only, keep route Admin/Feedback, keep Views/AdminFeedback/ folder, ADMIN-04 'no regression' stays trivially true." The existing `Views/AdminFeedback/Index.cshtml` and `Detail.cshtml` have NO `Layout = ...` line in their `@{}` blocks (verified in PATTERNS.md), so the new `_ViewStart.cshtml` takes effect with no edits to the views themselves.

Output: Operator visits `/Admin/Feedback`, sees the existing inbox + filters + per-row archive form unchanged in functionality but rendered inside the dark slate sidebar shell with Feedback link highlighted active.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-admin-shell-flags-foundation/06-CONTEXT.md
@.planning/phases/06-admin-shell-flags-foundation/06-PATTERNS.md
@.planning/phases/06-admin-shell-flags-foundation/06-01-SUMMARY.md
@CLAUDE.md

<interfaces>
<!-- Existing AdminFeedback views (already in context). They have NO `Layout = ` line in their @{} block -->
<!-- so the new _ViewStart.cshtml takes effect immediately. Confirmed by reading Views/AdminFeedback/Index.cshtml. -->

From DeckFlow.Web/Views/AdminFeedback/Index.cshtml header (existing):
```cshtml
@model DeckFlow.Web.Controllers.Admin.AdminFeedbackListViewModel
@using DeckFlow.Web.Models
@{
    ViewData["Title"] = "Admin — Feedback";
    var actionMessage = TempData["AdminFeedbackAction"] as string;
}
```
NOT touched by this plan. The @{} block contains no Layout assignment — the per-folder _ViewStart wins.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create Views/AdminFeedback/_ViewStart.cshtml (3-line layout override)</name>
  <files>DeckFlow.Web/Views/AdminFeedback/_ViewStart.cshtml</files>
  <read_first>
    - DeckFlow.Web/Views/AdminFeedback/Index.cshtml (already in context — confirms @{} has NO Layout = line, so per-folder _ViewStart will win)
    - DeckFlow.Web/Views/AdminFeedback/Detail.cshtml (run a Read on this file first; if it has `Layout = "_Layout";` in its @{} block, this plan must be expanded to remove it — otherwise leave as-is)
    - DeckFlow.Web/Views/_ViewStart.cshtml (root viewstart — sets Layout = "_Layout"; per-folder file overrides)
    - .planning/phases/06-admin-shell-flags-foundation/06-CONTEXT.md (D-15 layout-swap-only)
    - .planning/phases/06-admin-shell-flags-foundation/06-PATTERNS.md (§"Controllers/Admin/AdminFeedbackController.cs + Views/AdminFeedback/{Index,Detail}.cshtml (modify — layout swap only, D-15)")
  </read_first>
  <behavior>
    - File `Views/AdminFeedback/_ViewStart.cshtml` exists and is exactly 3 lines.
    - Content:
      ```
      @{
          Layout = "_AdminLayout";
      }
      ```
    - No edits to AdminFeedbackController.cs (`git diff DeckFlow.Web/Controllers/Admin/AdminFeedbackController.cs` empty).
    - No edits to Views/AdminFeedback/Index.cshtml or Detail.cshtml (`git diff` on both empty).
    - If Detail.cshtml's `@{}` block already contained an explicit `Layout = "_Layout";` (i.e. overriding the root), this plan would need to also remove that line — but per PATTERNS.md the views have no Layout = line. Verify before writing.
  </behavior>
  <action>
1. First run a Read on `DeckFlow.Web/Views/AdminFeedback/Detail.cshtml` to confirm it does NOT have `Layout = "_Layout";` in its `@{}` block. If it does, STOP and report — D-15 says zero view-body edits, but if this single line exists it must be removed (and only that line) so the per-folder _ViewStart can take effect.

2. Assuming the verification passes (no explicit Layout = line in either Index.cshtml or Detail.cshtml), create `DeckFlow.Web/Views/AdminFeedback/_ViewStart.cshtml` with EXACTLY this content (3 lines, no trailing comment, no extra blank lines):

```cshtml
@{
    Layout = "_AdminLayout";
}
```

3. Run `dotnet build DeckFlow.sln` — exit code 0 expected.

Use the Write tool. Do NOT touch any other file.
  </action>
  <verify>
    <automated>test -f DeckFlow.Web/Views/AdminFeedback/_ViewStart.cshtml &amp;&amp; grep -q 'Layout = "_AdminLayout"' DeckFlow.Web/Views/AdminFeedback/_ViewStart.cshtml &amp;&amp; [ "$(wc -l &lt; DeckFlow.Web/Views/AdminFeedback/_ViewStart.cshtml)" -le 4 ] &amp;&amp; [ -z "$(git diff --name-only HEAD -- DeckFlow.Web/Controllers/Admin/AdminFeedbackController.cs DeckFlow.Web/Views/AdminFeedback/Index.cshtml 2>/dev/null)" -o -z "$(git diff HEAD -- DeckFlow.Web/Controllers/Admin/AdminFeedbackController.cs DeckFlow.Web/Views/AdminFeedback/Index.cshtml 2>/dev/null)" ] &amp;&amp; dotnet build DeckFlow.sln 2>&amp;1 | grep -q 'Build succeeded'</automated>
  </verify>
  <done>
    `_ViewStart.cshtml` created in AdminFeedback folder with 3 lines setting Layout = "_AdminLayout". `dotnet build DeckFlow.sln` clean. No other file touched (git diff empty for AdminFeedbackController.cs and Index.cshtml; if Detail.cshtml needed a Layout line removed, that single removal is justified — but only if verification in step 1 found one).
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Operator verifies /Admin/Feedback inbox + mark-read flow inside new shell</name>
  <what-built>
    AdminFeedback layout swap: _ViewStart.cshtml in Views/AdminFeedback/ overrides the root layout so /Admin/Feedback now renders inside _AdminLayout (dark slate sidebar) instead of the public-site _Layout (guild themes + theme picker + footer).
  </what-built>
  <how-to-verify>
    1. Run the app locally: `cd DeckFlow.Web &amp;&amp; dotnet run --no-launch-profile` (or use existing scripts/run-web.sh).
    2. Open `http://localhost:5173/Admin/Feedback` in a browser. Authenticate with the local FEEDBACK_ADMIN_USER / FEEDBACK_ADMIN_PASSWORD env vars.
    3. Expected: Page renders inside the dark slate `_AdminLayout` shell. Sidebar shows Feedback / Harvest / Analytics / Flags with **Feedback** highlighted (left-border accent + bold).
    4. Existing inbox table renders: status filter pills (New / Read / Archived / All), Type select, paginated table with Created / Type / Message / Email / Status / Actions columns.
    5. Click any item's "View" link — Detail page also renders inside _AdminLayout.
    6. From the Detail page (or inbox row), submit the Archive form. Expected: POST goes through, page redirects with success TempData banner, item moves to Archived status. No 400 / 403 / 500.
    7. Confirm guild theme picker is NOT visible anywhere on the admin page; the public-site footer is NOT visible; the back-to-top button is NOT visible.
    8. Confirm the build version stamp (`v{X.Y.Z}`) appears in the top bar on the right.
  </how-to-verify>
  <resume-signal>Type "approved" if all 8 checks pass, or describe issues (which step failed, what you saw instead).</resume-signal>
  <files>(human verification — no file changes in this task)</files>
  <action>(no executor action — operator performs the manual verification steps above)</action>
  <verify>
    <automated>echo "Manual checkpoint — operator-verified" &amp;&amp; true</automated>
  </verify>
  <done>Operator typed "approved" after completing all verification steps in &lt;how-to-verify&gt;.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public internet → /Admin/Feedback | Already protected by BasicAuthMiddleware (Program.cs:330-332). This plan changes only the rendered layout, not the route, controller, or auth surface. No new boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-C1 | Tampering / Repudiation (regression on existing live route) | /Admin/Feedback inbox + mark-read POST | mitigate | D-15 explicitly forbids touching the controller or view bodies — only the _ViewStart layout-resolution file is added. The mark-read antiforgery POST flow uses `@Html.AntiForgeryToken()` in the view body (untouched) and `[ValidateAntiForgeryToken]` on the controller action (untouched). Task 2 checkpoint manually verifies the round-trip works end-to-end inside the new layout. Tied to must_haves.truths #1 and #4. |
| T-06-C2 | Information Disclosure | _AdminLayout vs leaked _Layout chrome | mitigate | Plan 01 already verified _AdminLayout loads only ~/css/admin.css with zero site-*.css references. By switching the layout for AdminFeedback views, this plan inherits that mitigation — the public-site theme + theme picker no longer render on /Admin/Feedback. |
| T-06-C3 | Spoofing | BasicAuth coverage of layout-swapped path | accept | Path /Admin/Feedback is unchanged. The MapWhen branch (`ctx.Request.Path.StartsWithSegments("/Admin")` at Program.cs:330) still matches. No middleware reorder. |
</threat_model>

<verification>
- `dotnet build DeckFlow.sln` exits 0.
- `git diff --stat HEAD -- DeckFlow.Web/Controllers/Admin/AdminFeedbackController.cs DeckFlow.Web/Views/AdminFeedback/Index.cshtml` shows zero lines changed (or, if Detail.cshtml had a Layout = line that needed removal, exactly 1 line removed from Detail.cshtml and zero elsewhere).
- Manual checkpoint Task 2 completes "approved."
</verification>

<success_criteria>
- `/Admin/Feedback` renders inside dark slate sidebar with Feedback link active.
- Mark-read / Archive POST flows work end-to-end (no antiforgery break, no route 404, no 500).
- ADMIN-03 BasicAuth gate still triggers (curl without creds returns 401).
- ADMIN-04 "no regression" satisfied — inbox functionality preserved.
</success_criteria>

<output>
After completion, create `.planning/phases/06-admin-shell-flags-foundation/06-03-SUMMARY.md` documenting:
- Single file added (Views/AdminFeedback/_ViewStart.cshtml).
- D-15 enforced (zero controller / view-body edits, confirmed by git diff).
- ADMIN-03 + ADMIN-04 satisfied (manual checkpoint passed).
- Threat T-06-C1 mitigation verified (mark-read POST round-trip works inside new shell).
</output>
