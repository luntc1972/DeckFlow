---
context: phase
phase: 66-admin-tool-visibility-toggles-tool-registry
task: 0
total_tasks: 0
status: paused
last_updated: 2026-06-22T22:47:41Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> Discovered this session.

- [ ] CONSTRAINT: Wrong-worktree planning — Phase 66 exists ONLY on the `cycle11` worktree (`/mnt/c/users/chrislunt/source/personal/deckflow-cycle11`). Main's `.planning/ROADMAP.md` has no Phase 66; `init.plan-phase 66` from main returns `phase_found: false`. Mitigation: run every Phase 66 GSD command with the cycle11 worktree as cwd.

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Wrong-worktree GSD run | Running `/gsd:*phase 66` from the main checkout silently finds no phase (separate `.planning/` per worktree) | blocking | cd to `/mnt/c/users/chrislunt/source/personal/deckflow-cycle11` before any Phase 66 command |
| Fabricated decisions | Writing CONTEXT.md decisions the user never made | advisory | User paused before choosing; do NOT invent the 4 gray-area answers — either ask or explicitly delegate to planner |

<current_state>
Phase 66 (Admin Tool-Visibility Toggles + Tool Registry), cycle11. discuss-phase was running and paused at the `present_gray_areas` step — user selected "Pause for now", so NO gray areas were discussed and NO CONTEXT.md was written. Codebase scout completed; findings below must not be re-gathered.
</current_state>

<completed_work>

- Scouted the full existing tool-visibility wiring (see Required Reading + map below). Done.
- Confirmed plan-phase gate choices: research SKIPPED, UI-SPEC SKIPPED.
</completed_work>

<remaining_work>

- Lock (or delegate) the 4 open gray areas.
- Write 66-CONTEXT.md.
- /gsd:plan-phase 66 --skip-research --skip-ui.
</remaining_work>

<decisions_made>

- Skip research (internal wiring over existing infra, no new integration).
- Skip UI-SPEC (reuses existing admin-table + nav/tile patterns, no net-new visual design).
</decisions_made>

<blockers>
- None. User simply paused.
</blockers>

## Required Reading (in order)
1. `.planning/ROADMAP.md` Phase 66 section — goal + 5 success criteria + reqs TOGGLE-01..07
2. `.planning/REQUIREMENTS.md` (TOGGLE-01..07) — exact requirement text
3. Scouted wiring map below — existing infra, do NOT re-scout

## Scouted Wiring Map (do NOT re-gather)

**Existing feature-flag infra (reuse, don't rebuild):**
- `DeckFlow.Web/Services/FeatureFlags/FeatureFlagStore.cs` — Postgres/SQLite `feature_flags` table (key/enabled/updated_at); seed uses `ON CONFLICT DO NOTHING`; currently seeds: `scryfall.tagger.enabled`, `page.help.enabled`, `harvest.cron.enabled`, `feature.categories.enabled`, `content.kb.enabled`, `feature.manabase.enabled`, `analysis.reference.full-oracle-text`, `analysis.reference.deck-stats(FALSE)`. Key naming = dotted lowercase.
- `IFeatureFlagStore`: `GetAllAsync`, `SetEnabledAsync`, `EnsureSchemaAsync`.
- `FeatureFlagCache` (Singleton + IHostedService): lock-free `IsEnabled(key)` → default TRUE (D-13); sync load in StartAsync; 30s poll; `ReloadAsync` for same-round-trip admin visibility.
- `DeckFlow.Web/Infrastructure/FeatureFlagGateAttribute.cs` — `[FeatureFlagGate("key", Title, Message)]`; **when OFF returns HTTP 503 + Retry-After:300 + `_MaintenancePage.cshtml`** (NOT 404/redirect). Resolves cache per-invocation.

**Admin console (existing):**
- `Controllers/Admin/AdminFlagsController.cs` + `Views/AdminFlags/Index.cshtml` at `/Admin/Flags` (BasicAuth). Renders ALL raw flag keys as a table; POST `/Admin/Flags/{key}/toggle` (anti-forgery + SameOrigin); only toggles keys already in snapshot; calls `ReloadAsync`. Shows raw keys incl. non-tool flags (tagger, harvest cron, analysis.reference.*).

**Nav (hardcoded, per-tool @if):** `Views/Shared/_DeckToolTabs.cshtml` — 4 groups (Analyze/Build/Reference/Categories). Flag checks: manabase (hide), content.kb (hide), categories (shows "Suggestions offline" muted span).

**Home tiles (hardcoded, per-tool @if):** `Views/Deck/Home.cshtml` — same 4 sections. manabase/KB hide; categories shows "Temporarily offline" status card.

**Help (markdown-derived):** `Services/HelpContentService.cs` scans `Help/*.md`; `HelpTopic.RequiresFlag` optional header drives visibility; `HelpController` 404s hidden topics. Only `manabase.md` has `requires_flag`. GAPS: `content-kb.md` + `category-suggestions.md` lack the header (help stays visible when tool off); `convert` + `mechanic-lookup` have NO help topic.

**Full tool inventory (13 public tools, route | nav section | flag | gated?):**
- Deck Analysis `/deck-analysis` Analyze — no flag, not gated
- Deck Comparison `/deck-comparison` Analyze — no flag, not gated
- cEDH Meta Gap `/cedh-meta-gap` Analyze — no flag, not gated
- Mana Base `/manabase` Analyze — `feature.manabase.enabled`, gated (GET+2 POST)
- Deck Sync `/sync` Build — no flag
- Convert Deck `/convert` Build — no flag, no help topic
- Deck Primer `/deck-primer` Build — no flag
- Card Lookup `/card-lookup` Reference — no flag
- Mechanic Rules `/mechanic-lookup` Reference — no flag, no help topic
- Ask a Judge `/judge-questions` Reference — no flag
- Knowledge Base `/content-kb` Reference — `content.kb.enabled`, gated
- Category Suggestions `/suggest-categories` Categories — `feature.categories.enabled`, gated
- Category Reference `/commander-categories` Categories — no flag

**Test patterns to follow:** `DeckFlow.Web.Tests/FeatureFlagGateAttributeTests.cs`, `AdminFlagsControllerToggleTests.cs`, `Manabase/ManabaseControllerFlagGateTests.cs` (reflection lock on gate attrs); `TestDoubles/FakeFeatureFlagStore.cs`, `FakeFeatureFlagCache.cs` (defaults enabled).

## 4 OPEN GRAY AREAS (decide or delegate to planner)
1. **Disabled-route behavior** — TOGGLE-04/roadmap want unreachable (404 or redirect); today gate = 503 maintenance page. Pick 404 / redirect-to-home / keep 503. Changes existing manabase/KB/categories behavior. (Planner default if delegated: 404 NotFound.)
2. **Admin toggle UI** — new dedicated registry-driven "Tools" admin page (friendly names, grouped by nav section, core warning) vs extend existing raw-key `/Admin/Flags`. (Planner default: new Tools page; leave raw flags page for non-tool flags.)
3. **Offline UX unification** — TOGGLE-03 hide tile+help+nav together, no orphan. Today inconsistent (manabase/KB hide; categories placeholder). Pick hide-everywhere vs keep placeholders. (Planner default: hide everywhere, drop categories placeholder.)
4. **Core-tool warning** — TOGGLE-07 warn-not-block on disabling core tool. Which tools = core, and inline banner vs JS confirm. (Planner default: Deck Analysis core; inline warning.)

## Infrastructure State
- node_modules present in worktree (untracked, expected — needed for tsc build; do not commit).
- No code changes made this session. No CONTEXT/RESEARCH/PLAN written.

<context>
Was driving GSD discuss-phase for Phase 66. Scouted deeply to ground real (not generic) gray areas. Presented 4; user chose "Pause for now". Did not fabricate decisions. The scout map above is the expensive artifact — preserved so resume is cheap. Phase 66 depends on Phase 64 (done); 65 executed.
</context>

<next_action>
Start with: cd /mnt/c/users/chrislunt/source/personal/deckflow-cycle11, then either /gsd:discuss-phase 66 (lock the 4 gray areas) or /gsd:plan-phase 66 --skip-research --skip-ui (delegate them to the planner).
</next_action>
