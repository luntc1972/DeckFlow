---
phase: 98-card-grounding-guard
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - DeckFlow.Core/Knowledge/CardGrounding/ICardGroundingGuard.cs
  - DeckFlow.Core/Knowledge/CardGrounding/CardGroundingVerdict.cs
  - DeckFlow.Core/Knowledge/CardGrounding/CardGroundingRejectReason.cs
  - DeckFlow.Core/Knowledge/CardGrounding/CardGroundingDeckContext.cs
  - DeckFlow.Core/Knowledge/CardGrounding/CardGroundingBatchResult.cs
  - DeckFlow.Core/Knowledge/CardGrounding/CardGroundingRules.cs
  - DeckFlow.Core.Tests/Knowledge/CardGrounding/CardGroundingRulesTests.cs
autonomous: true
requirements: [CS-23]

must_haves:
  truths:
    - "A never-legal or banned card is rejected by the legality rule (commander != legal, or missing/null legalities)"
    - "A card whose color identity is not a subset of the commander's identity is rejected"
    - "A non-basic card already present in the submitted deck is rejected; a basic land is not"
    - "A card whose colored mana pips are not all produced by the deck's manabase is rejected"
    - "Every rule is a pure static function with no HTTP/Scryfall dependency, directly xUnit-testable in DeckFlow.Core.Tests"
  artifacts:
    - path: "DeckFlow.Core/Knowledge/CardGrounding/ICardGroundingGuard.cs"
      provides: "Core-facing guard seam (TryValidateAsync + ValidateAllAsync)"
      contains: "interface ICardGroundingGuard"
    - path: "DeckFlow.Core/Knowledge/CardGrounding/CardGroundingRejectReason.cs"
      provides: "Typed reject-reason enum (D-13)"
      contains: "enum CardGroundingRejectReason"
    - path: "DeckFlow.Core/Knowledge/CardGrounding/CardGroundingRules.cs"
      provides: "Pure legality/identity/singleton/castability decision functions (CS-23)"
      contains: "static class CardGroundingRules"
    - path: "DeckFlow.Core.Tests/Knowledge/CardGrounding/CardGroundingRulesTests.cs"
      provides: "Unit tests for all four pure rules incl. fail-closed cases"
  key_links:
    - from: "DeckFlow.Core/Knowledge/CardGrounding/CardGroundingVerdict.cs"
      to: "DeckFlow.Core/Knowledge/CardGrounding/CardGroundingRejectReason.cs"
      via: "RejectReason property of enum type"
      pattern: "CardGroundingRejectReason"
---

<objective>
Create the Core-side substrate for the card-grounding guard: the guard interface seam, the rich verdict record + typed reject-reason enum (D-13), the deck-context input record (Pitfall 3), the batch-result aggregate (D-04), and the pure legality/color-identity/singleton/castability decision functions (CS-23).

Purpose: These are the contracts every later plan builds against (interface-first). The pure rules are the falsifiable safety net — legality, identity, singleton, and pip-castability checks that reject any unsafe suggestion, deterministically and without any HTTP dependency so they test directly in `DeckFlow.Core.Tests`.
Output: 6 new Core files under `DeckFlow.Core/Knowledge/CardGrounding/` + 1 new Core test file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/98-card-grounding-guard/98-CONTEXT.md
@.planning/phases/98-card-grounding-guard/98-RESEARCH.md
@.planning/phases/98-card-grounding-guard/98-PATTERNS.md

<interfaces>
<!-- Existing Core analogs the executor must mirror. Do not explore the codebase for these. -->

The P96 Core seam pattern this phase MUST replicate structurally (but NOT reuse — D-01 requires a fully separate interface):
  From DeckFlow.Core/Knowledge/StatedRulesExtraction/ICardNameGrounder.cs:
    public interface ICardNameGrounder
    {
        Task<CardGroundingResult> TryGroundAsync(string candidateName, CancellationToken cancellationToken = default);
    }
    public sealed record CardGroundingResult(bool Resolved, string CanonicalName);
  (CardGroundingResult is the "minimal-shape counterexample" — the new verdict must ALSO carry the reject reason.)

Sealed-record-with-required/init convention (project-wide):
  From DeckFlow.Core/Content/CreatorDeckCacheEntry.cs — `public sealed record X { public required T Prop { get; init; } }`

Pure-static-decision-class convention:
  From DeckFlow.Core/Knowledge/ProfileFusion/ConflictCalculator.cs — static class, `ArgumentNullException.ThrowIfNull` guards, private helpers, `// Why:` comments on magic values.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define the Core guard contracts (interface, verdict, reason enum, deck context, batch result)</name>
  <files>DeckFlow.Core/Knowledge/CardGrounding/ICardGroundingGuard.cs, DeckFlow.Core/Knowledge/CardGrounding/CardGroundingVerdict.cs, DeckFlow.Core/Knowledge/CardGrounding/CardGroundingRejectReason.cs, DeckFlow.Core/Knowledge/CardGrounding/CardGroundingDeckContext.cs, DeckFlow.Core/Knowledge/CardGrounding/CardGroundingBatchResult.cs</files>
  <read_first>
    - DeckFlow.Core/Knowledge/StatedRulesExtraction/ICardNameGrounder.cs (the seam pattern to mirror; do NOT reuse it — D-01)
    - DeckFlow.Core/Content/CreatorDeckCacheEntry.cs (sealed record + required/init convention)
    - DeckFlow.Core/Manabase/ManabaseVerdict.cs (verdict-record naming/shape precedent)
    - .planning/phases/98-card-grounding-guard/98-PATTERNS.md (exact shapes for each record, lines 28-137)
    - .planning/phases/98-card-grounding-guard/98-CONTEXT.md (D-04, D-13)
  </read_first>
  <action>
    Create a new sub-namespace `DeckFlow.Core.Knowledge.CardGrounding` (one folder `DeckFlow.Core/Knowledge/CardGrounding/`, mirroring how `StatedRulesExtraction` and `ProfileFusion` each own a folder+namespace under `DeckFlow.Core/Knowledge/`). Author these five files, all with XML `<summary>` doc comments (GenerateDocumentationFile is on):

    `ICardGroundingGuard.cs` — `public interface ICardGroundingGuard` with two members: `Task<CardGroundingVerdict> TryValidateAsync(string candidateName, CardGroundingDeckContext deckContext, CancellationToken cancellationToken = default)` and `Task<CardGroundingBatchResult> ValidateAllAsync(IReadOnlyList<string> candidateNames, CardGroundingDeckContext deckContext, CancellationToken cancellationToken = default)`. Include an XML `<remarks>` explaining the Core-cannot-reach-the-Web-Scryfall-throttle rationale (load-bearing for maintainers), same as the `ICardNameGrounder` analog. Do NOT extend or reference `ICardNameGrounder`/`CardGroundingResult` (D-01).

    `CardGroundingRejectReason.cs` — `public enum CardGroundingRejectReason` with members exactly: `None, NotFound, Ambiguous, NotLegal, IdentityViolation, SingletonDuplicate, Uncastable, UpstreamUnavailable` (D-13). `None` is the value when a verdict is Accepted.

    `CardGroundingVerdict.cs` — `public sealed record CardGroundingVerdict` with `required bool Accepted { get; init; }`, `required string CanonicalName { get; init; }`, `required CardGroundingRejectReason RejectReason { get; init; }`. Use `{ get; init; }` — NEVER `{ get; }` (CLAUDE.md carve-out: System.Text.Json skips get-only properties). CanonicalName carries the Scryfall-canonical name when Accepted (D-03) or the raw input echoed back when rejected.

    `CardGroundingDeckContext.cs` — `public sealed record CardGroundingDeckContext` carrying the deck-dependent inputs the pure rules need (Pitfall 3): `required IReadOnlySet<string> CommanderColorIdentity { get; init; }` (WUBRG letters as strings, matching Scryfall `color_identity` shape), `required IReadOnlySet<char> DeckProducedColors { get; init; }` (the WUBRG chars the submitted deck's manabase already produces — a set is sufficient per D-11's ">=1 source per color", no counts), and `required IReadOnlySet<string> DeckCardNames { get; init; }` (normalized names already present in the submitted deck, for the singleton check). Add doc comments noting these are populated by the P99 caller.

    `CardGroundingBatchResult.cs` — `public sealed record CardGroundingBatchResult` with `required IReadOnlyList<CardGroundingVerdict> Verdicts { get; init; }` and `required bool HasUpstreamFailure { get; init; }` (true when any verdict's RejectReason is UpstreamUnavailable — lets P99 escalate to the 503 usable-floor path per D-12 without Phase 98 referencing UpstreamErrorMessageBuilder). Do not add accepted/rejected convenience collections; P99 filters `Verdicts` itself.
  </action>
  <verify>
    <automated>cd /mnt/c/users/chrislunt/source/personal/deckflow-cycle17 && dotnet build DeckFlow.Core/DeckFlow.Core.csproj -warnaserror 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `DeckFlow.Core/Knowledge/CardGrounding/ICardGroundingGuard.cs` exists; `grep -c "TryValidateAsync\|ValidateAllAsync" <file>` returns 2.
    - `CardGroundingRejectReason.cs` declares exactly the 8 members listed (None, NotFound, Ambiguous, NotLegal, IdentityViolation, SingletonDuplicate, Uncastable, UpstreamUnavailable); no others.
    - All records use `{ get; init; }` — `grep -c "get; }" DeckFlow.Core/Knowledge/CardGrounding/*.cs` (excluding `init`) returns 0 for get-only auto-props.
    - `dotnet build DeckFlow.Core/DeckFlow.Core.csproj -warnaserror` succeeds with 0 warnings.
    - No file under `DeckFlow.Core/Knowledge/CardGrounding/` references `ICardNameGrounder`, `CardGroundingResult`, RestSharp, HttpClient, or any `DeckFlow.Web` type.
  </acceptance_criteria>
  <done>Five Core contract files compile clean; interface seam, verdict, reason enum, deck-context, and batch-result records exist with the exact shapes above.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement CardGroundingRules pure decision logic + tests (CS-23)</name>
  <files>DeckFlow.Core/Knowledge/CardGrounding/CardGroundingRules.cs, DeckFlow.Core.Tests/Knowledge/CardGrounding/CardGroundingRulesTests.cs</files>
  <read_first>
    - DeckFlow.Core/Knowledge/ProfileFusion/ConflictCalculator.cs (pure static class shape, guards, // Why: comments)
    - DeckFlow.Core/Knowledge/DistillationValidation.cs (constants-at-top + per-rule static methods)
    - .planning/phases/98-card-grounding-guard/98-PATTERNS.md (CardGroundingRules shapes, lines 141-199)
    - .planning/phases/98-card-grounding-guard/98-RESEARCH.md (Pitfall 2 basic-land exemption, Pitfall 3 pip check, A2 fail-closed legality)
    - .planning/phases/98-card-grounding-guard/98-CONTEXT.md (D-09, D-10, D-11, D-12)
  </read_first>
  <behavior>
    - IsLegalForCommander returns true only when legalities is non-null, contains key "commander", and its value equals "legal" (OrdinalIgnoreCase). null legalities => false; missing "commander" key => false; value "banned"/"not_legal"/"restricted" => false (fail-closed, D-12/A2).
    - IsWithinColorIdentity returns true when every color in the card's color identity is contained in the commander identity set; a null/empty card identity (colorless) => true; a card color outside the commander set => false.
    - IsSingletonViolation returns false when the card's type line contains "Basic Land" (OrdinalIgnoreCase, Pitfall 2) regardless of presence; otherwise returns true when the (normalized) canonical name is in the deck's card-name set. "Any number of copies" cards (Shadowborn Apostle) are NOT exempt — they still return true if present (D-10).
    - IsCastable returns true when every colored pip letter (W/U/B/R/G) appearing in the mana cost is present in the deck's produced-colors set; a null/empty mana cost or a colorless-only cost (e.g. "{3}") => true. A cost requiring a color the deck cannot produce => false. Generic/numeric and {X} pips are ignored. (Mana-value sanity is deferred per D-11 "optional" + RESEARCH Open Question 3 — do NOT implement it.)
  </behavior>
  <action>
    Write `public static class CardGroundingRules` in namespace `DeckFlow.Core.Knowledge.CardGrounding`. Four pure static methods, each taking plain data (never a `ScryfallCard` — that type is Web-side; the guard projects the needed fields before calling, keeping this class HTTP-free and Core-testable per Pitfall 3):

    - `public static bool IsLegalForCommander(IReadOnlyDictionary<string, string>? legalities)` — fail-closed per the behavior block.
    - `public static bool IsWithinColorIdentity(IReadOnlyList<string>? cardColorIdentity, IReadOnlySet<string> commanderIdentity)` — subset check.
    - `public static bool IsSingletonViolation(string canonicalName, string typeLine, IReadOnlySet<string> deckCardNames)` — basic-land exemption via `typeLine.Contains("Basic Land", StringComparison.OrdinalIgnoreCase)` first, then membership. Compare names case-insensitively; assume the caller passes an already-normalized deckCardNames set and normalize the candidate consistently (trim + ToLowerInvariant) inside the method so the guard and the rule agree.
    - `public static bool IsCastable(string? manaCost, IReadOnlySet<char> deckProducedColors)` — parse pip letters from the mana-cost string. Scan for the characters W/U/B/R/G (Scryfall renders pips as `{U}` etc.; scanning the raw string for those five uppercase letters is sufficient — hybrid/phyrexian pips also contain the color letter, which is the conservative correct behavior). Guard args with `ArgumentNullException.ThrowIfNull` where the parameter is a non-nullable reference (the sets). Add a `// Why:` comment on the basic-land substring choice (authoritative vs. a hardcoded name list) per the DistillationValidation convention.

    Then write `CardGroundingRulesTests.cs` in `DeckFlow.Core.Tests` (namespace `DeckFlow.Core.Tests`, folder `Knowledge/CardGrounding/`) covering every branch in the behavior block: legal/banned/not_legal/missing-key/null legalities; in-identity/off-identity/colorless card; basic-land-exempt/non-basic-present/non-basic-absent singleton; castable/uncastable/colorless-cost/null-cost. Use `[Theory]`/`[InlineData]` where it reduces duplication. These are the CS-23 tests.
  </action>
  <verify>
    <automated>cd /mnt/c/users/chrislunt/source/personal/deckflow-cycle17 && dotnet build DeckFlow.Core.Tests/DeckFlow.Core.Tests.csproj -warnaserror 2>&1 | tail -5 && dotnet test DeckFlow.Core.Tests/DeckFlow.Core.Tests.csproj --filter "FullyQualifiedName~CardGroundingRules" 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `CardGroundingRules.cs` declares exactly four public static methods: IsLegalForCommander, IsWithinColorIdentity, IsSingletonViolation, IsCastable.
    - `IsLegalForCommander(null)` returns false and a test asserts it (fail-closed).
    - A test asserts a "Basic Land — Forest" type line is exempt from singleton violation even when "forest" is in the deck set, AND a non-basic card present in the set returns true.
    - A test asserts a card costing "{1}{U}" is uncastable when deckProducedColors lacks 'U' and castable when it contains 'U'.
    - `dotnet test ... --filter "FullyQualifiedName~CardGroundingRules"` reports all tests passed, 0 failed.
    - `dotnet build DeckFlow.Core.Tests -warnaserror` succeeds (0 warnings); no reference to any Web/HTTP type in `CardGroundingRules.cs`.
  </acceptance_criteria>
  <done>All four pure rules implemented, fail-closed on legality, basic-exempt on singleton; CS-23 unit tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Scryfall card data → guard decision | Card attributes (legality, identity, cost) originate upstream and feed reject/accept decisions; a missing/null field must never silently pass |
| Suggested card → submitted deck | An unsafe suggestion (illegal/off-identity/duplicate/uncastable) crossing into an artifact is the phase's core threat |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-98-01 | Tampering / Elevation | `CardGroundingRules.IsLegalForCommander` | mitigate | Fail-closed: null/missing/`!= "legal"` legalities returns false; asserted by a dedicated null-legalities test (A2) |
| T-98-02 | Spoofing (fake card slips through) | `CardGroundingRules` identity/singleton/castability | mitigate | Deterministic reject on any failed check; full-branch unit coverage incl. off-identity and duplicate cases |
| T-98-03 | Tampering | Basic-land singleton exemption | accept | Basics are legitimately non-singleton; exemption scoped to `TypeLine` containing "Basic Land" only, so "any number of copies" cards still reject (D-10) |
| T-98-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan (pure Core code); RESEARCH Package Legitimacy Audit confirms zero new dependencies |
</threat_model>

<verification>
- `dotnet build DeckFlow.Core/DeckFlow.Core.csproj -warnaserror` and `DeckFlow.Core.Tests` both clean (0 warnings, per CLAUDE.md gate).
- `dotnet test DeckFlow.Core.Tests --filter "FullyQualifiedName~CardGrounding"` all green.
- No `DeckFlow.Web`/RestSharp/HttpClient reference anywhere under `DeckFlow.Core/Knowledge/CardGrounding/`.
</verification>

<success_criteria>
- Six Core contract/rules files exist under the new `CardGrounding` namespace with the exact record/enum shapes.
- CS-23 legality/identity/singleton/castability checks are implemented as pure functions and are fully unit-tested, including the fail-closed legality case and the basic-land singleton exemption.
- Nothing in this plan touches HTTP or the Web project.
</success_criteria>

<output>
Create `.planning/phases/98-card-grounding-guard/98-01-SUMMARY.md` when done.
</output>
