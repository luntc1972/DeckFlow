---
phase: 86-ui-audit-re-score-studio-stage-4-admin-flags-closeout
plan: 05
type: execute
wave: 5
depends_on: [86-01, 86-02, 86-03, 86-04]
files_modified:
  - DeckFlow.Web/e2e/theme-active-affordance.spec.ts
  - DeckFlow.Web/e2e/layout-mode-interaction.spec.ts
  - tasks/UI-REVIEW.md
autonomous: false
requirements: [UIAUDIT-02]

must_haves:
  truths:
    - "A visual-regression e2e proves the active step-tab background differs from inactive AND from var(--panel-soft-bg) across representative light + dark themes on desktop and mobile."
    - "The e2e proves active-tab text meets WCAG 4.5:1 on the previously-failing dark themes."
    - "The e2e proves the clear-cache-button hover background is not the old blue on a non-Jeskai theme (Bug B site.css:623)."
    - "An interaction e2e proves toggling Compact/Advanced changes a measurable layout property vs guided, and that guided has its positive style."
    - "The 6-pillar UI re-score in tasks/UI-REVIEW.md is >= 20/24 with a per-pillar delta."
  artifacts:
    - path: "DeckFlow.Web/e2e/theme-active-affordance.spec.ts"
      provides: "per-theme active-tab background + WCAG + accent-leak (segment + ui-mode + clear-cache) + bucket-toggle a11y assertions"
      min_lines: 40
    - path: "DeckFlow.Web/e2e/layout-mode-interaction.spec.ts"
      provides: "Compact/Advanced measurable layout-delta + guided positive-style assertion"
      min_lines: 25
    - path: "tasks/UI-REVIEW.md"
      provides: "re-scored 6-pillar audit >= 20/24"
      contains: "20/24"
  key_links:
    - from: "theme-active-affordance.spec.ts"
      to: ".prompt-step-tab.is-active computed background-color"
      via: "getComputedStyle vs inactive tab + --panel-soft-bg + --accent"
      pattern: "prompt-step-tab"
    - from: "layout-mode-interaction.spec.ts"
      to: "data-prompt-ui-mode-button click -> boundingBox delta"
      via: "measured layout property before/after toggle"
      pattern: "data-prompt-ui-mode-button"
---

<objective>
Close the test gap that let A–D ship green, then re-score the UI audit (requirement UIAUDIT-02).

The current 256 e2e assert DOM/selectors EXIST — not visual state (which tab is active, contrast) nor interaction OUTCOMES (does Compact change layout). That is the root reason bugs A–D shipped green. This plan adds the two missing test classes, enforces the accent-leak grep gate, and re-runs the 6-pillar UI audit to the >=20/24 target.

SCOPE NOTE: This pass satisfies UIAUDIT-02 only. UIAUDIT-03 (DirectPush Stage 4 verification + `DirectPush.razor:441` no-op copy) and ADMIN-01 (`/Admin/Flags` on/off sorting) are OUT OF SCOPE and remain NOT satisfied — a later planning pass must close them before Phase 86 fully closes.

Purpose: makes A–D regression-proof and confirms the phase acceptance bar.
Output: two new Playwright specs, a re-scored `tasks/UI-REVIEW.md`, and a human-verified visual pass across representative themes at two viewports.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/86-ui-audit-re-score-studio-stage-4-admin-flags-closeout/86-CONTEXT.md
@.planning/phases/86-ui-audit-re-score-studio-stage-4-admin-flags-closeout/86-RESEARCH.md
@.planning/phases/86-ui-audit-re-score-studio-stage-4-admin-flags-closeout/86-VALIDATION.md
@tasks/UI-REVIEW.md

<interfaces>
<!-- Reuse the proven theme-cookie + webServer pattern: -->
<!--   DeckFlow.Web/e2e/print-button-appearance.spec.ts — per-theme addCookies({name:'deckflow-theme', value:'site-<theme>.css'}) -->
<!--   DeckFlow.Web/e2e/bracket-smoke.spec.ts — theme loop -->
<!--   playwright.config.ts — baseURL http://localhost:5173; webServer runs `dotnet run --launch-profile http-no-browser` (WSL-safe, no Windows browser); projects chromium-desktop(1280) + chromium-mobile(390). -->

<!-- Navigating to an active tab (from print-button-appearance.spec.ts): -->
<!--   page.goto('/deck-analysis'); page.locator('[data-prompt-show-step="2"][role="tab"]').click(); -->
<!--   active tab selector: .prompt-step-tab.is-active ; inactive: .prompt-step-tab:not(.is-active) -->

<!-- Layout picker buttons (DeckAnalysis.cshtml:84-86): -->
<!--   [data-prompt-ui-mode-button="guided"|"focused"|"expert"]  ; always-present target: .prompt-instructions -->

<!-- Representative theme subset (Claude's discretion, must cover @import inherit + fork + failing darks). -->
<!-- IMPORTANT: Classic MUST use an EXPLICIT cookie `deckflow-theme=site.css`, NOT a null/absent cookie -->
<!-- (a null cookie is brittle and leaks whatever theme a prior test left set): -->
<!--   azorius -> site-azorius.css (light @import), jund -> site-jund.css (dark fork), -->
<!--   dimir -> site-dimir.css (dark @import, needs --accent-contrast), -->
<!--   planeswalker-dark -> site-planeswalker-dark.css (dark fork, needs --accent-contrast), -->
<!--   Classic -> site.css (explicit). -->
<!-- WCAG >=4.5:1 active-text check runs for the previously-failing set: dimir, golgari, planeswalker-dark, nyx -->
<!-- (jund/sultai pass white per Codex sweep; still assert >=4.5:1 for them as a guard). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Visual-regression spec — active pill, WCAG, accent-leak (segment+ui-mode+clear-cache), bucket a11y (Bugs A/B/C)</name>
  <files>DeckFlow.Web/e2e/theme-active-affordance.spec.ts</files>
  <action>Create a new Playwright spec copying the theme-cookie + webServer pattern from print-button-appearance.spec.ts. It runs under both existing projects (chromium-desktop 1280, chromium-mobile 390) so desktop+mobile are covered. For the representative theme set — set an EXPLICIT `deckflow-theme` cookie for EVERY theme including Classic (`site.css`; never rely on a null/absent cookie): {azorius=site-azorius.css, jund=site-jund.css, dimir=site-dimir.css, planeswalker-dark=site-planeswalker-dark.css, Classic=site.css}. For each: go to /deck-analysis, click a step tab to make it active, then via getComputedStyle assert `.prompt-step-tab.is-active` background-color (a) differs from an inactive `.prompt-step-tab` background-color AND (b) differs from the resolved value of var(--panel-soft-bg) AND (c) equals the resolved var(--accent) — proving the filled pill. For the previously-failing themes {dimir, golgari, planeswalker-dark, nyx} (plus jund, sultai as guards) compute the WCAG contrast ratio of the active tab's computed color vs its computed background-color and assert >= 4.5:1. Add THREE accent-leak checks (Bug B) on a non-Jeskai theme: activate a `.prompt-layout-segment` (hover/is-active), activate a `[data-prompt-ui-mode-button]`, AND hover the `.clear-cache-button` — assert each computed background-color is NOT in the rgb(43,108,176) family (this proves site-common.css, site.css:308, AND site.css:623 respectively). Add a bucket-toggle a11y check (Bug C): assert `.prompt-question-bucket__toggle` has a non-empty accessible name (aria-label) and computed border-width 0 (no bordered pill) — render an analysis result first if needed to reach a bucket. Keep the spec deterministic and offline (no external HTTP). Do NOT commit compiled wwwroot/js/*.js.</action>
  <verify>
    <automated>cd DeckFlow.Web && npx --no-install playwright test e2e/theme-active-affordance.spec.ts --reporter=line</automated>
  </verify>
  <done>The new spec passes on both projects: active pill proven, WCAG >=4.5:1 for the failing darks, no blue leak on segment/ui-mode/clear-cache, bucket toggle labelled + borderless, Classic uses an explicit site.css cookie.</done>
</task>

<task type="auto">
  <name>Task 2: Interaction spec (Bug D) + accent-leak grep gate + 6-pillar re-score</name>
  <files>DeckFlow.Web/e2e/layout-mode-interaction.spec.ts, tasks/UI-REVIEW.md</files>
  <action>(1) Create layout-mode-interaction.spec.ts: with an explicit deckflow-theme cookie (site.css + one dark theme), on /deck-analysis measure a layout property of an always-present element (e.g. `.prompt-instructions` boundingBox / visibility) in guided, then click `[data-prompt-ui-mode-button="focused"]` and assert a measurable change, then click `[data-prompt-ui-mode-button="expert"]` and assert a further measurable change (e.g. instructions hidden / box height delta) — proving the modes are perceptible on the empty landing. ALSO assert Bug D's guided positive style: in guided mode `.prompt-instructions` carries the accent marker (e.g. a non-transparent accent-derived border-left / background from plan 86-04) so "give Full a positive style" is regression-tested, not just the focused/expert geometry. (2) Run the accent-leak grep gate and confirm it is clean: `grep -rn "43, *108, *176" DeckFlow.Web/wwwroot/css/*.css | grep -v site-jeskai.css` returns nothing. (3) Re-run the 6-pillar UI audit and update tasks/UI-REVIEW.md: capture headless screenshots via scripts/run-web-test.sh + Playwright at desktop(1280) + mobile(390) for the affected surfaces on representative themes, re-score each pillar with a delta note (expect Pillar-3 Color 2->3 from Bug B; Pillar-2 Visuals and Pillar-6 Experience improved by A/C/D), and record the new overall score, which must be >= 20/24. If a pillar is still short, enumerate the concrete remaining gap.</action>
  <verify>
    <automated>( cd DeckFlow.Web && npx --no-install playwright test e2e/layout-mode-interaction.spec.ts --reporter=line ); test $(grep -rn "43, *108, *176" DeckFlow.Web/wwwroot/css/*.css | grep -v site-jeskai.css | wc -l) -eq 0; grep -n "20/24\|21/24\|22/24\|23/24\|24/24" tasks/UI-REVIEW.md</automated>
  </verify>
  <done>Interaction spec passes (mode delta + guided positive style); grep gate clean; tasks/UI-REVIEW.md records a re-score >= 20/24 with per-pillar deltas.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Bugs A–D landed across base + 13-fork theme set, plus new visual-regression and interaction e2e. Full build + full xUnit + full Playwright e2e all green; UI re-score recorded >= 20/24. UIAUDIT-03 + ADMIN-01 remain deferred (not satisfied this pass).</what-built>
  <how-to-verify>
    1. Start the headless server: `scripts/run-web-test.sh` (sets DECKFLOW_DISABLE_AUTO_BROWSER=true — no Windows browser opens). Visit http://localhost:5173/deck-analysis.
    2. Switch themes via the theme picker (or the deckflow-theme cookie) and confirm on Classic + Azorius (light) + Jund + Dimir + Planeswalker-Dark (dark), at desktop AND mobile widths:
       - A: the active step-tab is an obvious FILLED accent pill with readable text (no low-contrast rust-on-brown / green / blue-on-navy).
       - B: the layout picker hover/active, the ui-mode button, and the clear-cache hover show THIS theme's accent, not a fixed blue.
       - C: each analysis-questions bucket toggle is a clean chevron (no orphan grey pill) and screen-reader-labelled.
       - D: clicking Full / Compact / Advanced produces an unmistakable layout change even on the empty Step-1 landing, and Full has a positive accent style.
    3. Confirm the full suite is green: build via dotnet.exe, xUnit, and `npx --no-install playwright test`.
  </how-to-verify>
  <resume-signal>Type "approved" if all four render correctly at both viewports across themes, or describe the theme/viewport where a fix is wrong.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test runner ← local headless server | Playwright drives a local Kestrel over loopback; no external network, no secrets in specs. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-86-05-A | Information Disclosure | e2e spec fixtures | accept | Specs use synthetic deck JSON; no PII/secret; offline. |
| T-86-05-B | Denial of Service | headless server in CI | accept | Existing webServer pattern already gated in CI; no new surface. |
| T-86-SC | Tampering | package installs | mitigate | Uses existing @playwright/test (npx --no-install); NO new npm/nuget package — a new dependency would require explicit user approval per CLAUDE.md. |
</threat_model>

<verification>
- `npx --no-install playwright test` (full suite, both projects) green, including the two new specs.
- Full build 0/0 via `"/mnt/c/Program Files/dotnet/dotnet.exe" build DeckFlow.sln`; full xUnit green (run via dotnet.exe per WSL constraints).
- Accent-leak grep gate clean (jeskai excluded).
- tasks/UI-REVIEW.md overall score >= 20/24.
- Human-verify checkpoint approved for desktop + mobile across representative themes.
</verification>

<success_criteria>
- Two new e2e specs assert visual state (Bug A/B/C incl. clear-cache leak) and interaction outcome (Bug D incl. guided positive style) and pass on desktop + mobile.
- Full build + full xUnit + full Playwright e2e green.
- UI re-score >= 20/24 recorded with per-pillar deltas.
- Human visual pass approved.
</success_criteria>

<output>
Create `.planning/phases/86-ui-audit-re-score-studio-stage-4-admin-flags-closeout/86-05-SUMMARY.md` when done.

NOTE: Phase 86 remains PARTIALLY OPEN after this plan set. Only UIAUDIT-02 is satisfied this pass.
Two roadmap items are intentionally NOT planned here and remain NOT satisfied — a later planning
pass must close them before the phase fully closes:
- UIAUDIT-03 — Studio DirectPush Stage 4 verification (+ DirectPush.razor:441 no-op success-copy fix).
- ADMIN-01 — /Admin/Flags on/off (enabled-state) sorting.
</output>
