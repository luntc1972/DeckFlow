---
phase: 15-aiplatform-value-object-refactor
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - DeckFlow.Web/Models/AiPlatform.cs
  - DeckFlow.Web/Models/DeckAnalysisRequest.cs
  - DeckFlow.Web/Models/DeckComparisonRequest.cs
  - DeckFlow.Web/Models/MetaGapRequest.cs
  - DeckFlow.Web/Views/Shared/_AiSelector.cshtml
  - DeckFlow.Web/Services/RequestContextParser.cs
  - DeckFlow.Web/Services/PacketArtifactStore.cs
  - DeckFlow.Web.Tests/AiPlatformPhase10RoundTripTests.cs
autonomous: true
requirements:
  - AIPLATFORM-01
must_haves:
  truths:
    - "AiPlatform sealed record with (Key, DisplayName, Description) signature exists in DeckFlow.Web.Models"
    - "AiPlatform.All exposes [ChatGpt, Claude, Gemini] in canonical display order"
    - "AiPlatform.Normalize(string?) returns AiPlatform.Default (ChatGPT) for null/empty/unrecognized input"
    - "All three request DTO setters delegate to AiPlatform.Normalize(value).Key"
    - "_AiSelector.cshtml iterates AiPlatform.All instead of three hardcoded radio blocks"
    - "RequestContextParser + PacketArtifactStore zip-load sites defensively call AiPlatform.Normalize before assigning to request.TargetAiPlatform"
    - "Existing AiPlatformPhase10RoundTripTests still pass — string round-trip behavior preserved byte-identically"
    - "5 prompt-builder switches in DeckAnalysisPacketService/DeckComparisonService/MetaGapService remain unchanged (Plan 15-02 territory)"
    - "DECKFLOW_GEMINI_ENABLED gate still wraps Gemini radio rendering only (not the loop iteration)"
    - "Filename helper in PacketArtifactStore.cs (grep 'SuggestPacketZipFileName|SuggestComparisonZipFileName|SuggestCedhMetaGapZipFileName' for anchor) fallback string \"chatgpt\" unchanged (D-06)"
  artifacts:
    - path: "DeckFlow.Web/Models/AiPlatform.cs"
      provides: "Sealed record + All + Default + Normalize + AllForTesting seam-ready"
      contains: "public sealed record AiPlatform"
    - path: "DeckFlow.Web/Models/DeckAnalysisRequest.cs"
      provides: "TargetAiPlatform setter delegating to AiPlatform.Normalize"
      contains: "AiPlatform.Normalize(value).Key"
    - path: "DeckFlow.Web/Models/DeckComparisonRequest.cs"
      provides: "TargetAiPlatform setter delegating to AiPlatform.Normalize"
      contains: "AiPlatform.Normalize(value).Key"
    - path: "DeckFlow.Web/Models/MetaGapRequest.cs"
      provides: "TargetAiPlatform setter delegating to AiPlatform.Normalize"
      contains: "AiPlatform.Normalize(value).Key"
    - path: "DeckFlow.Web/Views/Shared/_AiSelector.cshtml"
      provides: "Loop over AiPlatform.All; Gemini conditional wraps option render only"
      contains: "foreach"
    - path: "DeckFlow.Web/Services/RequestContextParser.cs"
      provides: "Defensive Normalize before assignment (if applied)"
    - path: "DeckFlow.Web/Services/PacketArtifactStore.cs"
      provides: "Three zip-load sites call AiPlatform.Normalize defensively"
      contains: "AiPlatform.Normalize"
  key_links:
    - from: "DeckFlow.Web/Models/DeckAnalysisRequest.cs setter"
      to: "DeckFlow.Web/Models/AiPlatform.cs Normalize"
      via: "AiPlatform.Normalize(value).Key"
      pattern: "AiPlatform\\.Normalize"
    - from: "DeckFlow.Web/Views/Shared/_AiSelector.cshtml"
      to: "DeckFlow.Web/Models/AiPlatform.cs All"
      via: "@foreach (var platform in AiPlatform.All)"
      pattern: "AiPlatform\\.All"
---

<objective>
Land the `AiPlatform` sealed record value object and migrate all string-touchpoint sites (3 request DTO setters, Razor partial, parser, zip-load defensive Normalize, existing test harness) to use it. Keep the 5 prompt-builder switches intact — Plan 15-02 owns that extraction. String round-trip stays byte-identical so existing tests pass without modification (beyond MemberData migration).

Purpose: Establishes the value object foundation per `10-AISEL-PLATFORM-DESIGN.md` Layer 1. Single source of truth for the AI platform set moves from a switch expression buried in 3 request setters to `AiPlatform.All`. Sets up Plan 15-02's registry extraction.

Output: 1 new model file (`Models/AiPlatform.cs`); 7 modified files (3 setters, 1 Razor partial, parser, store, existing test). Build green at end of plan.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/15-aiplatform-value-object-refactor/15-CONTEXT.md
@.planning/milestones/v1.2-phases/10-claude-gemini-artifact-optimization/10-AISEL-PLATFORM-DESIGN.md
@.planning/phases/14-broader-codebase-name-vs-behavior-audit/14-CONTEXT.md
@CLAUDE.md
@.planning/codebase/CONVENTIONS.md

<interfaces>
<!-- Reference shapes the executor needs. AiPlatform.cs is created by Task 1 from 10-AISEL-PLATFORM-DESIGN.md §"Layer 1" lines 42-95 verbatim (with minor sealing). -->
<!-- Line-number anchors avoided: source files have drifted across phases. Use grep anchors below to locate current line for each touchpoint. -->

From DeckFlow.Web/Configuration/AiPlatformOptions.cs (UNCHANGED this phase):
- public sealed class AiPlatformOptions { public bool GeminiEnabled { get; set; } }
- Bound from DECKFLOW_GEMINI_ENABLED env var in Program.cs (grep 'DECKFLOW_GEMINI_ENABLED' for current line)

From DeckFlow.Web/AssemblyInfo.cs (UNCHANGED this phase):
- [assembly: InternalsVisibleTo("DeckFlow.Web.Tests")]
- Plan 15-03 leverages this to put `internal static AllForTesting` on AiPlatform.cs

Existing setter pattern (3 sites — locate via grep, NOT line numbers; files drift):
- DeckFlow.Web/Models/DeckAnalysisRequest.cs (grep 'TargetAiPlatform' in setter context)
- DeckFlow.Web/Models/DeckComparisonRequest.cs (grep 'TargetAiPlatform' in setter context)
- DeckFlow.Web/Models/MetaGapRequest.cs (grep 'TargetAiPlatform' in setter context)

Shape (verbatim per scout — all 3 files share this pattern):
- public string TargetAiPlatform
- {
-     get => _targetAiPlatform;
-     set => _targetAiPlatform = value switch
-     {
-         "ChatGPT" or "Claude" or "Gemini" => value,
-         _ => "ChatGPT"
-     };
- }

Existing _AiSelector.cshtml hardcoded radios:
- 3 input/label pairs (ai-chatgpt, ai-claude, ai-gemini) with the Gemini block wrapped in @if (geminiEnabled).
- Razor @model is `string` (currently-selected platform Key).

Existing zip-load assigns in PacketArtifactStore.cs (3 sites — locate via `grep -n 'request.TargetAiPlatform = parsed.TargetAiPlatform' DeckFlow.Web/Services/PacketArtifactStore.cs`):
- if (parsed.TargetAiPlatform is not null) { request.TargetAiPlatform = parsed.TargetAiPlatform; }
- The request setter already normalizes via the switch — defensive Normalize at the parser/store layer is belt-and-suspenders per CONTEXT.md but recommended by design doc §"Migration of existing code" item 4.

Existing filename helper site (DO NOT modify — D-06 preservation):
- DeckFlow.Web/Services/PacketArtifactStore.cs (grep 'SuggestPacketZipFileName\|SuggestComparisonZipFileName\|SuggestCedhMetaGapZipFileName' for anchor)
- These 3 helpers pass a `"chatgpt"` literal as a fallback default to `CreateSafePathSegment` — that literal stays.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create AiPlatform sealed record value object</name>
  <files>DeckFlow.Web/Models/AiPlatform.cs</files>
  <read_first>
    - .planning/phases/15-aiplatform-value-object-refactor/15-CONTEXT.md (D-01 record surface scope — Key/DisplayName/Description only, no Enabled/Strategy)
    - .planning/milestones/v1.2-phases/10-claude-gemini-artifact-optimization/10-AISEL-PLATFORM-DESIGN.md (Layer 1 lines 35-96 — binding shape)
    - DeckFlow.Web/Services/CardLookupService.cs (XML <summary> tone anchor; terse single-sentence per Phase 13/14 D-03)
    - DeckFlow.Web/Configuration/AiPlatformOptions.cs (sealed-class style anchor; confirms Enabled stays here NOT on the record)
    - DeckFlow.Web/AssemblyInfo.cs (confirm InternalsVisibleTo grant already in place — no edit needed; Plan 15-03 piggybacks)
    - CLAUDE.md (preserve `{ get; init; }` accessors NEVER auto-convert — record positional accessors are init-style by default; `<NoWarn>1591;1573;1587</NoWarn>` already on in Web.csproj but new public types MUST carry summaries because they're new code)
    - .planning/codebase/CONVENTIONS.md (file-per-type, sealed leaf types, file-scoped namespace, Allman braces)
  </read_first>
  <action>
    Create new file `DeckFlow.Web/Models/AiPlatform.cs` in namespace `DeckFlow.Web.Models` per 10-AISEL-PLATFORM-DESIGN.md §"Layer 1" lines 35-96 — implement verbatim with the following structure:

    - File-scoped `namespace DeckFlow.Web.Models;`
    - `public sealed record AiPlatform(string Key, string DisplayName, string Description)`
    - Three static readonly fields on the record: `ChatGpt`, `Claude`, `Gemini` using exact Key strings `"ChatGPT"`, `"Claude"`, `"Gemini"` (D-06 preservation invariant — Key strings are user-facing contract on form-post AND zip round-trip).
    - DisplayName values: `"ChatGPT"`, `"Claude"`, `"Gemini"` (display label equals Key in v1.3 baseline).
    - Description values per design doc lines 47, 52, 57 verbatim:
      - ChatGpt: `"OpenAI's GPT-family models — markdown-headed prompts with fenced JSON output."`
      - Claude: `"Anthropic's Claude models — XML-tagged prompts with <result>-wrapped output."`
      - Gemini: `"Google's Gemini models — markdown prompts with persona scaffold and schema-strictness language."`
    - `public static readonly IReadOnlyList<AiPlatform> All = [ChatGpt, Claude, Gemini];` (collection expression; canonical display order including Gemini regardless of DECKFLOW_GEMINI_ENABLED — the flag gates Razor render only, not the registry).
    - `public static AiPlatform Default => ChatGpt;` (expression-bodied property — keeps a single Default reference; do NOT make it a static readonly field collision with ChatGpt).
    - `public static AiPlatform Normalize(string? key)` — per design doc lines 76-92: null/empty → Default; for-loop over All with `string.Equals(platform.Key, key, StringComparison.Ordinal)`; out-of-set → Default. Use `StringComparison.Ordinal` (D-06 preservation: existing setter is case-sensitive `"ChatGPT" or "Claude" or "Gemini"` so Normalize must be case-sensitive too — Phase 10 hardening behavior).
    - `public override string ToString() => Key;` (per design doc line 94).
    - XML `<summary>` on the record + each static field + each static method per CardLookupService.cs tone anchor (terse single sentence). Match design doc lines 38-41, 60-63, 65-68, 71-75 wording where it fits the file's local style.

    DO NOT add: `Enabled` property, `ResponseExtractor` delegate, `Strategy` interface reference (D-01 rejects these). DO NOT add: `internal static AllForTesting` helper — that's Plan 15-03's responsibility (this task creates the production-clean surface; Plan 15-03 adds the test seam in its own commit).

    Preserve `init` accessors on positional record syntax. Preserve LF line endings. Allman braces on the for-loop body in Normalize.

    Commit message: `feat(15-01): add AiPlatform sealed record value object` (no Co-Authored-By trailer per CLAUDE.md; plain default author).
  </action>
  <verify>
    <automated>dotnet build DeckFlow.Web/DeckFlow.Web.csproj --configuration Release</automated>
  </verify>
  <acceptance_criteria>
    - `DeckFlow.Web/Models/AiPlatform.cs` exists.
    - File contains `namespace DeckFlow.Web.Models;` (file-scoped).
    - File contains `public sealed record AiPlatform(string Key, string DisplayName, string Description)`.
    - File contains three static readonly fields: `public static readonly AiPlatform ChatGpt`, `public static readonly AiPlatform Claude`, `public static readonly AiPlatform Gemini`.
    - File contains the literal strings `"ChatGPT"`, `"Claude"`, `"Gemini"` as Key values (case-sensitive — case-preserving D-06).
    - File contains `public static readonly IReadOnlyList<AiPlatform> All`.
    - File contains `public static AiPlatform Default`.
    - File contains `public static AiPlatform Normalize(string? key)`.
    - File contains `StringComparison.Ordinal` (case-sensitive lookup matches existing setter behavior).
    - File contains `public override string ToString() => Key;`.
    - File does NOT contain the identifier `Enabled` (D-01 rejection).
    - File does NOT contain `AllForTesting` (Plan 15-03 territory).
    - `dotnet build DeckFlow.Web/DeckFlow.Web.csproj --configuration Release` exits 0 with no new warnings.
    - Commit log shows one commit titled `feat(15-01): add AiPlatform sealed record value object` with no `Co-Authored-By` trailer (single logical change per CLAUDE.md).
  </acceptance_criteria>
  <done>AiPlatform.cs compiles clean; All has Count=3; Normalize unit-tested implicitly via existing Phase 10 round-trip tests in Task 5 below.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate 3 request DTO setters to delegate to AiPlatform.Normalize</name>
  <files>DeckFlow.Web/Models/DeckAnalysisRequest.cs, DeckFlow.Web/Models/DeckComparisonRequest.cs, DeckFlow.Web/Models/MetaGapRequest.cs</files>
  <read_first>
    - DeckFlow.Web/Models/DeckAnalysisRequest.cs (current setter — grep `TargetAiPlatform` in setter context for anchor)
    - DeckFlow.Web/Models/DeckComparisonRequest.cs (current setter — grep `TargetAiPlatform` in setter context for anchor)
    - DeckFlow.Web/Models/MetaGapRequest.cs (current setter — grep `TargetAiPlatform` in setter context for anchor)
    - DeckFlow.Web/Models/AiPlatform.cs (created in Task 1 — confirms Normalize signature)
    - .planning/phases/15-aiplatform-value-object-refactor/15-CONTEXT.md (D-06 preservation list — property name `TargetAiPlatform`, form field name `targetAiPlatform`, `_targetAiPlatform` backing-field name all stay)
    - CLAUDE.md (preserve switch expressions in other locations; touch ONLY the setter body for each file; do NOT run Format Document / Code Cleanup on the rest of these files)
  </read_first>
  <action>
    For each of the 3 DTO files, replace ONLY the setter body of `public string TargetAiPlatform`:

    Old (verbatim per scout — all 3 files share this shape; locate via `grep -nE 'public string TargetAiPlatform' DeckFlow.Web/Models/*Request.cs`):
        public string TargetAiPlatform
        {
            get => _targetAiPlatform;
            set => _targetAiPlatform = value switch
            {
                "ChatGPT" or "Claude" or "Gemini" => value,
                _ => "ChatGPT"
            };
        }

    New (3 files, identical structure):
        public string TargetAiPlatform
        {
            get => _targetAiPlatform;
            set => _targetAiPlatform = AiPlatform.Normalize(value).Key;
        }

    Keep the `_targetAiPlatform` backing-field declaration UNCHANGED. Keep the XML `<summary>` block above the property UNCHANGED (it still describes the behavior accurately — defaults to ChatGPT, accepted values ChatGPT/Claude/Gemini, out-of-set normalizes to ChatGPT). Keep `using DeckFlow.Web.Models;` (already same namespace; no using needed because each file is already in `DeckFlow.Web.Models`).

    Touch ONLY those exact lines per file. Do NOT re-indent surrounding code. Do NOT reformat XML doc-comments. Preserve `{ get; init; }` on any neighboring properties. Preserve raw-string literals elsewhere in the file (CLAUDE.md anti-formatter pin).

    Single commit covering all 3 files:
    Commit message: `refactor(15-01): migrate 3 request DTO setters to AiPlatform.Normalize`
  </action>
  <verify>
    <automated>dotnet build DeckFlow.Web/DeckFlow.Web.csproj --configuration Release</automated>
  </verify>
  <acceptance_criteria>
    - `DeckFlow.Web/Models/DeckAnalysisRequest.cs` contains the substring `_targetAiPlatform = AiPlatform.Normalize(value).Key`.
    - `DeckFlow.Web/Models/DeckComparisonRequest.cs` contains the substring `_targetAiPlatform = AiPlatform.Normalize(value).Key`.
    - `DeckFlow.Web/Models/MetaGapRequest.cs` contains the substring `_targetAiPlatform = AiPlatform.Normalize(value).Key`.
    - None of the 3 DTO files contains the substring `"ChatGPT" or "Claude" or "Gemini"` (the old switch arm body is gone).
    - All 3 files still contain the property declaration `public string TargetAiPlatform`.
    - All 3 files still contain the private field `_targetAiPlatform` (D-06 backing-field name preservation).
    - `dotnet build DeckFlow.Web/DeckFlow.Web.csproj --configuration Release` exits 0 with zero new warnings vs. Task 1's baseline.
    - `git diff --stat HEAD~1 HEAD` shows exactly 3 files changed (the 3 DTOs); no incidental edits to other files in the same commit.
    - Existing Phase 10 round-trip tests still pass when run (verified later in Task 5 by re-running `AiPlatformPhase10RoundTripTests`).
  </acceptance_criteria>
  <done>3 setter bodies reduced to one-liner each; build green; string round-trip behavior byte-identical to pre-refactor (Normalize is case-sensitive Ordinal match per the old switch).</done>
</task>

<task type="auto">
  <name>Task 3: Migrate _AiSelector.cshtml to iterate AiPlatform.All</name>
  <files>DeckFlow.Web/Views/Shared/_AiSelector.cshtml</files>
  <read_first>
    - DeckFlow.Web/Views/Shared/_AiSelector.cshtml (current 37-line file — 3 hardcoded radio blocks with Gemini wrapped in @if (geminiEnabled))
    - DeckFlow.Web/Models/AiPlatform.cs (Task 1 output — confirms All iteration shape)
    - .planning/milestones/v1.2-phases/10-claude-gemini-artifact-optimization/10-AISEL-PLATFORM-DESIGN.md (lines 181-194 — binding Razor pattern; note the design doc snippet is a guide, not byte-for-byte — preserve DeckFlow's existing class names: `ai-selector`, `ai-selector__options`, `ai-selector__option-label`, `sr-only ai-selector__option`)
    - .planning/phases/15-aiplatform-value-object-refactor/15-CONTEXT.md (D-03 — Gemini @if (geminiEnabled) gate wraps the OPTION's render block, not the loop; AiPlatform.All canonical order includes Gemini; CSS classnames preserved so 22 guild themes match)
  </read_first>
  <action>
    Replace the current 37-line `_AiSelector.cshtml` body. Preserve all 4 lines of the header block (`@using` / `@inject` / `@model string` / leading comment).

    New file content shape:

    - Keep lines 1-4 verbatim: `@using Microsoft.Extensions.Options`, `@using DeckFlow.Web.Configuration`, `@inject IOptions<AiPlatformOptions> AiPlatformOptionsAccessor`, `@model string`.
    - Keep the multi-line `@*…*@` comment (lines 5-10) — update it to reference `AiPlatform.All` iteration if helpful for future readers, but keep the "Gemini radio is hidden unless DECKFLOW_GEMINI_ENABLED=true" sentence.
    - Add `@using DeckFlow.Web.Models` to the using block so `AiPlatform.All` resolves in the cshtml.
    - Preserve the `@{ var geminiEnabled = ...; var selected = ...; if (selected == "Gemini" && !geminiEnabled) { selected = "ChatGPT"; } }` code block at lines 11-18 (D-06: "Gemini" string literal in this fallback stays for Razor-level safety; AiPlatform.Normalize is server-side, this is the view-side safety net for stale form posts).
    - Replace the `<div class="ai-selector__options" role="radiogroup" ...>` body (current lines 21-36) with a `@foreach (var platform in AiPlatform.All)` loop emitting one `<input>` + `<label>` pair per platform.
    - Wrap the Gemini-only option in `@if (geminiEnabled || platform.Key != "Gemini") { ... }` (or equivalent: skip the iteration body for Gemini when !geminiEnabled). The cleanest form: `@if (platform.Key == "Gemini" && !geminiEnabled) { continue; }` BEFORE the input emission, then unconditionally emit input+label for the remaining platforms. Either form is acceptable so long as the Gemini option DOES NOT render when DECKFLOW_GEMINI_ENABLED is false.

    Inside the loop body, emit input with attributes:
    - `type="radio"`
    - `name="TargetAiPlatform"` (D-06: form field name preservation — case + spelling MUST match existing form binding)
    - `id="ai-@platform.Key.ToLowerInvariant()"` (preserves existing `id="ai-chatgpt"` / `ai-claude` / `ai-gemini` pattern for theme CSS hooks)
    - `value="@platform.Key"`
    - `class="sr-only ai-selector__option"`
    - `checked="@(selected == platform.Key ? "checked" : null)"`

    Then a sibling `<label for="ai-@platform.Key.ToLowerInvariant()" class="ai-selector__option-label">@platform.DisplayName</label>`.

    Keep `<p class="ai-selector__heading">Analyze with</p>` and the `aria-label="AI analysis target"` on the radiogroup div — accessibility surface preserved.

    Preserve LF line endings. Do NOT run formatter on the file.

    Commit message: `refactor(15-01): iterate AiPlatform.All in _AiSelector.cshtml`
  </action>
  <verify>
    <automated>dotnet build DeckFlow.Web/DeckFlow.Web.csproj --configuration Release</automated>
  </verify>
  <acceptance_criteria>
    - `DeckFlow.Web/Views/Shared/_AiSelector.cshtml` contains the substring `@foreach (var platform in AiPlatform.All)` (or `foreach(var platform in AiPlatform.All)` — whitespace tolerant).
    - File contains `@using DeckFlow.Web.Models`.
    - File no longer contains the literal `value="ChatGPT"` (was line 22 — replaced by Razor expression `value="@platform.Key"`).
    - File no longer contains the literal `value="Claude"` (was line 26).
    - File no longer contains the literal `value="Gemini"` (was line 32).
    - File DOES still contain `name="TargetAiPlatform"` (D-06 form field name preservation).
    - File DOES still contain `class="sr-only ai-selector__option"` (CSS classname preservation for 22 guild theme forks).
    - File DOES still contain `class="ai-selector__option-label"` (CSS classname preservation).
    - File DOES still contain the literal `"Gemini"` inside the `geminiEnabled` conditional check (Razor-level fallback safety net per D-06 — Gemini guard wraps the option's render path, not the loop iteration).
    - File DOES still contain `aria-label="AI analysis target"` (a11y preservation).
    - `dotnet build DeckFlow.Web/DeckFlow.Web.csproj --configuration Release` exits 0 with zero new warnings.
  </acceptance_criteria>
  <done>Razor partial renders 2 or 3 radios (depending on DECKFLOW_GEMINI_ENABLED) by iterating AiPlatform.All; class names + form field name unchanged; theme CSS keeps matching.</done>
</task>

<task type="auto">
  <name>Task 4: Add defensive AiPlatform.Normalize at zip-load + parser sites</name>
  <files>DeckFlow.Web/Services/RequestContextParser.cs, DeckFlow.Web/Services/PacketArtifactStore.cs</files>
  <read_first>
    - DeckFlow.Web/Services/RequestContextParser.cs (current file — locate `targetAiPlatform` parse field, switch case, record-property assign, record property declaration via `grep -nE 'targetAiPlatform|TargetAiPlatform' DeckFlow.Web/Services/RequestContextParser.cs`)
    - DeckFlow.Web/Services/PacketArtifactStore.cs (current file — locate the 3 zip-load assigns via `grep -n 'request.TargetAiPlatform = parsed.TargetAiPlatform' DeckFlow.Web/Services/PacketArtifactStore.cs`; locate the 3 SuggestZipFileName helpers via `grep -nE 'SuggestPacketZipFileName|SuggestComparisonZipFileName|SuggestCedhMetaGapZipFileName' DeckFlow.Web/Services/PacketArtifactStore.cs` — those filename helpers' `"chatgpt"` fallback MUST NOT be touched per D-06)
    - DeckFlow.Web/Models/AiPlatform.cs (Task 1 output — Normalize signature)
    - .planning/phases/15-aiplatform-value-object-refactor/15-CONTEXT.md (CONTEXT.md scout 2026-05-17 confirms 3 zip-load sites; filename helper `"chatgpt"` fallback stays UNCHANGED — D-06)
  </read_first>
  <action>
    Two-file change — defensive Normalize before the request setter receives the value. The setter ALREADY normalizes (Task 2), so the zip-load Normalize is belt-and-suspenders per design doc §"Migration of existing code" item 4; the value reaching the setter is then already canonical, eliminating the chance of a downstream consumer seeing an out-of-set string between the parser-read and the setter-assign window.

    **`DeckFlow.Web/Services/PacketArtifactStore.cs` — 3 sites:**

    First site (analysis zip-load), currently:
        if (parsed.TargetAiPlatform is not null)
        {
            request.TargetAiPlatform = parsed.TargetAiPlatform;
        }

    Replace with (apply the same shape to all 3 sites — locate via `grep -n 'request.TargetAiPlatform = parsed.TargetAiPlatform' DeckFlow.Web/Services/PacketArtifactStore.cs` and patch each match):
        if (parsed.TargetAiPlatform is not null)
        {
            request.TargetAiPlatform = AiPlatform.Normalize(parsed.TargetAiPlatform).Key;
        }

    Add `using DeckFlow.Web.Models;` at the top of the file if not already present (the file already references model types so likely present; verify and add if missing).

    **DO NOT touch the `SuggestPacketZipFileName` / `SuggestComparisonZipFileName` / `SuggestCedhMetaGapZipFileName` helpers and their `"chatgpt"` second-arg defaults to `CreateSafePathSegment`** — D-06 preservation: the filename helper's `"chatgpt"` fallback (when caller passes `null` targetAiPlatform) is intentional and tested. Leave the string literal `"chatgpt"` in those 3 lines untouched.

    **`DeckFlow.Web/Services/RequestContextParser.cs`:**

    The parser builds an immutable record `RequestContext { ..., TargetAiPlatform = string.IsNullOrEmpty(targetAiPlatform) ? null : targetAiPlatform, ... }` — leave this assign UNCHANGED (the record stays string-typed; forward-tolerant per design doc §"Migration of existing code" item 4). Defensive Normalize happens at the CONSUMER side (PacketArtifactStore zip-load sites above) where the value actually crosses into a request DTO.

    All other lines in RequestContextParser.cs (local var declaration, switch case to capture inline value, record property `public string? TargetAiPlatform { get; init; }`) — all unchanged. Parser remains string-typed end-to-end.

    Touch ONLY the 3 PacketArtifactStore zip-load sites in the way described. Do not modify any other code in either file. Do not re-indent. Do not reformat XML doc comments.

    Single commit:
    Commit message: `refactor(15-01): defensive AiPlatform.Normalize at 3 PacketArtifactStore zip-load sites`
  </action>
  <verify>
    <automated>dotnet build DeckFlow.Web/DeckFlow.Web.csproj --configuration Release</automated>
    <automated>test "$(grep -c 'AiPlatform.Normalize(parsed.TargetAiPlatform).Key' DeckFlow.Web/Services/PacketArtifactStore.cs)" -eq 3</automated>
  </verify>
  <acceptance_criteria>
    - `DeckFlow.Web/Services/PacketArtifactStore.cs` contains the substring `AiPlatform.Normalize(parsed.TargetAiPlatform).Key` exactly 3 times (3 zip-load sites — analysis, comparison, cedh-meta-gap). The Task 4 `<verify>` block enforces this via `grep -c` gate.
    - `DeckFlow.Web/Services/PacketArtifactStore.cs` still contains the substrings `"deck-analysis"`, `"deck-comparison"`, `"cedh-meta-gap"` AND `"chatgpt"` (the filename helper fallback — D-06 preservation; the `"chatgpt"` arg fallback to `CreateSafePathSegment` stays).
    - `DeckFlow.Web/Services/PacketArtifactStore.cs` still has `SuggestPacketZipFileName`, `SuggestComparisonZipFileName`, `SuggestCedhMetaGapZipFileName` method declarations unchanged in signature.
    - `DeckFlow.Web/Services/RequestContextParser.cs` is UNCHANGED (no diff vs. pre-task — Task description preserves parser surface).
    - `dotnet build DeckFlow.Web/DeckFlow.Web.csproj --configuration Release` exits 0 with zero new warnings.
    - `git diff --stat HEAD~1 HEAD` shows exactly 1 file changed (`PacketArtifactStore.cs` only).
  </acceptance_criteria>
  <done>Three zip-load sites defensively normalize before assignment to request.TargetAiPlatform; the filename helper "chatgpt" fallback preserved per D-06; parser stays string-typed end-to-end.</done>
</task>

<task type="auto">
  <name>Task 5: Migrate existing AiPlatformPhase10RoundTripTests from [InlineData] to [MemberData(AllPlatforms)]</name>
  <files>DeckFlow.Web.Tests/AiPlatformPhase10RoundTripTests.cs</files>
  <read_first>
    - DeckFlow.Web.Tests/AiPlatformPhase10RoundTripTests.cs (current 200+ line test file — uses `[Fact]` + hardcoded "ChatGPT"/"Claude"/"Gemini" assertion strings)
    - DeckFlow.Web/Models/AiPlatform.cs (Task 1 output — AiPlatform.All shape)
    - .planning/phases/15-aiplatform-value-object-refactor/15-CONTEXT.md (D-03 — "Migrate existing AiPlatformPhase10RoundTripTests.cs [InlineData] → [MemberData(AllPlatforms)]")
    - .planning/codebase/CONVENTIONS.md (xUnit conventions; Fake/Stub/Throwing taxonomy)
    - CLAUDE.md (VSTest unreliable in WSL — rely on `dotnet build` clean + push-and-watch CI for the test pass gate)
  </read_first>
  <action>
    **Test-count preservation rule (W9 binding):** Before editing, run `dotnet test --list-tests --no-build 2>/dev/null | grep -c AiPlatformPhase10` (or equivalent test discovery) to capture the pre-migration test count for the `AiPlatformPhase10RoundTripTests` class. This count is the post-migration FLOOR — no test case may be lost in the migration. Record the pre-migration count in the task commit message body if convenient.

    Scan the file first. If existing tests are `[Fact]` with one fact per platform (e.g., `LoadComparisonFromZip_restores_target_ai_platform_when_present` with `"Gemini"` hardcoded), MERGE those facts into `[Theory] [MemberData(nameof(AllPlatforms))]` parameterized tests where the platform name is now a parameter. Where existing tests are intentionally testing the NORMALIZE-TO-CHATGPT behavior (e.g., `LoadComparisonFromZip_normalizes_invalid_target_ai_platform_to_chatgpt`), KEEP them as standalone facts — those test the boundary, not the round-trip.

    Add a `public static IEnumerable<object[]> AllPlatforms()` member data source:

        public static IEnumerable<object[]> AllPlatforms()
        {
            foreach (var platform in AiPlatform.All)
            {
                yield return new object[] { platform.Key };
            }
        }

    Convert facts that hardcode a single AI Key into Theory tests. Concretely (verify against the file's current state):

    - Tests asserting `Assert.Equal("Gemini", loaded.TargetAiPlatform)` paired with `"target_ai_platform: Gemini\n"` round-trip become a Theory parameterized by the platform Key, with the round-trip assertion using the parameter on both sides.
    - Tests asserting normalization fallback behavior (out-of-set input → "ChatGPT") stay as `[Fact]` since they test the boundary, not the round-trip matrix.

    Add `using DeckFlow.Web.Models;` if not already present (it is per current import block).

    **Pre-migration test-case preservation invariant:** Each pre-migration `[InlineData("ChatGPT")]` / `[InlineData("Claude")]` / `[InlineData("Gemini")]` row that previously generated a test case MUST still generate a corresponding test case post-migration via the `AllPlatforms()` MemberData source. The post-migration `dotnet test --list-tests | grep -c AiPlatformPhase10` count MUST be greater than or equal to the pre-migration count for the same class.

    Search the rest of `DeckFlow.Web.Tests/` for any other `[InlineData("ChatGPT")]` + `[InlineData("Claude")]` + `[InlineData("Gemini")]` triple-platform patterns. If found, migrate those to `[MemberData(nameof(AllPlatforms))]` from a shared source (or inline the member data source per-file — pick the lower-friction option for the executor; CONTEXT.md doesn't require centralization, just migration).

    Preserve test class names. Preserve `Assert.Equal` semantics. Preserve LF line endings. Do NOT add new test methods beyond the data-source migration — Plan 15-02 + Plan 15-03 add their own tests.

    Commit message: `test(15-01): migrate AiPlatform round-trip tests to MemberData(AllPlatforms)`
  </action>
  <verify>
    <automated>dotnet build DeckFlow.sln --configuration Release</automated>
  </verify>
  <acceptance_criteria>
    - `DeckFlow.Web.Tests/AiPlatformPhase10RoundTripTests.cs` contains the substring `MemberData(nameof(AllPlatforms))`.
    - File contains a `public static IEnumerable<object[]> AllPlatforms()` method.
    - The `AllPlatforms()` source references `AiPlatform.All` (so adding a 4th platform automatically extends the theory matrix — SC5 forward-compat invariant).
    - File still contains the boundary test asserting that an out-of-set zip value (e.g., `"target_ai_platform: SomethingInvalid"`) results in `loaded.TargetAiPlatform == "ChatGPT"` (Phase 10 hardening preservation — this test verifies AiPlatform.Normalize's fallback contract).
    - `dotnet build DeckFlow.sln --configuration Release` (full solution including DeckFlow.Web.Tests) exits 0 with zero new warnings.
    - All test method declarations still have `[Fact]` OR `[Theory]` (no orphaned/uncategorized tests).
    - **Test-count preservation (W9 binding):** Pre-migration `[InlineData("ChatGPT")]`, `[InlineData("Claude")]`, `[InlineData("Gemini")]` cases all still produce test cases post-migration via `[MemberData(nameof(AllPlatforms))]`. Post-build `dotnet test --list-tests 2>/dev/null | grep -c AiPlatformPhase10` returns a count greater than or equal to the pre-migration count for the `AiPlatformPhase10RoundTripTests` class (no test cases lost in the migration).
    - WSL VSTest constraint: local `dotnet test` invocation is NOT required to pass — CLAUDE.md exempts the executor from VSTest in WSL. Build clean is sufficient locally; CI push-and-watch verifies the actual test pass post-commit. The `--list-tests` discovery check above does NOT execute tests, only enumerates them, so it works in WSL.
  </acceptance_criteria>
  <done>Existing round-trip suite is now AiPlatform.All-driven; build green; adding a 4th platform (Plan 15-03) auto-extends the theory matrix without test edits; pre-migration test case count preserved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser form-post → Request DTO setter | User-supplied `targetAiPlatform` field crosses untrusted boundary; Normalize is the guard. |
| Uploaded zip → PacketArtifactStore zip-load → Request DTO | User-supplied `target_ai_platform` in `01-request-context.txt` envelope crosses untrusted boundary; defensive Normalize at zip-load + setter Normalize provide depth-in-defense. |

## STRIDE Threat Register

Refactor only — no new attack surface. Preserves Phase 10 Normalize defense (unrecognized `target_ai_platform` → ChatGPT default). Zip-load defensively calls `AiPlatform.Normalize` before assignment (D-06 preservation).

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-01 | Tampering | Crafted zip with arbitrary `target_ai_platform` value | mitigate | `AiPlatform.Normalize` at 3 zip-load sites + setter normalizes again; out-of-set → `Default` (ChatGPT). Preserves Phase 10 hardening behavior byte-identically. |
| T-15-02 | Information Disclosure | XML doc comments on new public type | accept | No PII; Description strings describe AI behavior, not user data. Public surface is intentional. |
</threat_model>

<verification>
- Plan 15-01 final build: `dotnet build DeckFlow.sln --configuration Release` exits 0 with zero new warnings vs. the Phase 14 baseline.
- Grep gate: `grep -rn "\"ChatGPT\" or \"Claude\" or \"Gemini\"" DeckFlow.Web/Models/` returns 0 matches (the old setter switch arm is gone from all 3 DTOs).
- Grep gate: `grep -rn "AiPlatform.Normalize" DeckFlow.Web/` returns at least 6 matches (3 setter sites + 3 zip-load sites). Exact count depends on whether the Razor partial references Normalize too (it should NOT — Razor uses AiPlatform.All for iteration, not Normalize).
- Grep gate: `grep -n "\"chatgpt\"" DeckFlow.Web/Services/PacketArtifactStore.cs` returns 3 matches (3 SuggestZipFileName helpers preserved — D-06).
- Test-count preservation (W9): `dotnet test --list-tests 2>/dev/null | grep -c AiPlatformPhase10` returns a count greater than or equal to the pre-migration count recorded before Task 5 edits.
- Smoke test (manual, user-driven per CLAUDE.md feedback memory — do NOT auto-launch dev server): user runs `scripts/run-web.sh` and hits `/deck-analysis` to confirm AI selector renders ChatGPT + Claude radios (Gemini hidden when DECKFLOW_GEMINI_ENABLED unset). Optional T1 ChatGPT round-trip if user wants extra confidence — full T1-T8 happens in Plan 15-03.
</verification>

<success_criteria>
- AiPlatform sealed record value object exists at `DeckFlow.Web/Models/AiPlatform.cs` with All, Default, Normalize per design doc §"Layer 1".
- All 3 request DTO setters delegate to `AiPlatform.Normalize(value).Key`.
- `_AiSelector.cshtml` iterates `AiPlatform.All` with Gemini render gated by `DECKFLOW_GEMINI_ENABLED`.
- 3 zip-load sites in `PacketArtifactStore` defensively call `AiPlatform.Normalize`.
- Existing `AiPlatformPhase10RoundTripTests` migrated to `[MemberData(AllPlatforms)]`; boundary normalization test preserved as `[Fact]`.
- **Existing-test-count preservation (W9):** All existing test cases in `AiPlatformPhase10RoundTripTests.cs` are preserved post-migration. Pre-migration `[InlineData("ChatGPT")]`, `[InlineData("Claude")]`, `[InlineData("Gemini")]` test cases all still run as `[MemberData(nameof(AllPlatforms))]` rows. Test discovery count for `AiPlatformPhase10RoundTripTests` does not decrease (verify post-build with `dotnet test --list-tests 2>/dev/null | grep -c AiPlatformPhase10` returns at least the pre-migration count).
- `dotnet build DeckFlow.sln --configuration Release` exits 0 with zero new warnings.
- D-06 preservation invariants intact: `"ChatGPT"`/`"Claude"`/`"Gemini"` Key string literals; `request.TargetAiPlatform` property name; `name="TargetAiPlatform"` form field; `"chatgpt"` zip filename fallback at the 3 SuggestZipFileName helpers; `DECKFLOW_GEMINI_ENABLED` env var; 22 guild theme CSS forks; no `Co-Authored-By` trailer on any commit.
- 5 prompt-builder switches in `DeckAnalysisPacketService.cs` (grep `internal static string BuildAnalysisPrompt(` and `internal static string BuildSetUpgradePrompt(` for anchors), `DeckComparisonService.cs` (grep `internal static string BuildComparisonPrompt(` and `internal static string BuildFollowUpPrompt(`), `MetaGapService.cs` (grep `internal static string BuildMetaGapPrompt(` or `internal static string BuildPrompt(` — naming may vary) are UNCHANGED (Plan 15-02 owns those).
</success_criteria>

<output>
Create `.planning/phases/15-aiplatform-value-object-refactor/15-01-SUMMARY.md` when done. Summary should record: file count modified, commit count (target: 5 commits — one per task above, each single-purpose per CLAUDE.md), final build status, any preservation invariants verified, pre/post-migration test-count comparison for `AiPlatformPhase10RoundTripTests`, and a note confirming Plan 15-02 is unblocked.
</output>
</content>
</invoke>