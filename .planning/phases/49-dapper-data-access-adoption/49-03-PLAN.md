---
phase: 49-dapper-data-access-adoption
plan: 03
type: execute
wave: 3
depends_on: ["49-01b", "49-02"]
files_modified:
  - DeckFlow.Core/Content/ContentHarvestRunStore.cs
  - DeckFlow.Web/Services/Harvest/HarvestRunStore.cs
  - DeckFlow.Core/Content/ContentVideoStore.cs
  - DeckFlow.Core/Content/ContentSiteIndexStore.cs
autonomous: true
requirements: [DAP-01, DAP-02, DAP-03]

must_haves:
  truths:
    - "(CONTINGENT ON 49-01 PASS verdict) The four mid-complexity stores execute all non-DDL paths through Dapper with no store-local coercion"
    - "HarvestRunStore Guid read/write goes through GuidTypeHandler (no IsPostgres ternary, no manual Guid.Parse switch)"
    - "decimal, DateTimeOffset, bool, and Guid coercions are all handled by the global handlers from waves 1-2"
    - "All DDL/migration/constraint methods (including table-rebuild migrations) stay raw ADO.NET with intentional-raw // Why: comments"
    - "Each converted store's existing test class passes on SQLite with no new failures"
  artifacts:
    - path: "DeckFlow.Web/Services/Harvest/HarvestRunStore.cs"
      provides: "Guid + nullable DateTimeOffset run records via Dapper; constraint-migration stays raw"
      contains: "QueryAsync|ExecuteAsync|ExecuteScalarAsync"
    - path: "DeckFlow.Core/Content/ContentVideoStore.cs"
      provides: "Largest content store (~20 methods) converted; filtered-distill-status constraint DDL stays raw"
      contains: "QueryAsync|ExecuteAsync"
  key_links:
    - from: "DeckFlow.Web/Services/Harvest/HarvestRunStore.cs"
      to: "GuidTypeHandler"
      via: "pass Guid directly in anon param; read maps to Guid Id property"
      pattern: "QueryAsync|ExecuteAsync"
    - from: "DeckFlow.Core/Content/ContentSiteIndexStore.cs"
      to: "BoolTypeHandler + DateTimeOffsetTypeHandler"
      via: "FormatVisibility/ReadVisibility removed; PRAGMA/information_schema DDL stays raw"
      pattern: "QueryAsync|ExecuteAsync"
---

<objective>
After a PASS spike verdict, convert the four mid-complexity eligible stores to Dapper (REQ-4/DAP-03).

This plan is CONTINGENT on `VERDICT: PASS` in 49-GATE-VERDICT.md, and depends on 49-02 (which registered the DateTimeOffset handler and proved the sweep template on simple stores).

Purpose: These stores have more methods and the distinctive Guid (HarvestRunStore) and table-rebuild migration concerns. They sit between the simple stores (49-02) and the transactional heavyweights (49-04).

Output: ContentHarvestRunStore, HarvestRunStore, ContentVideoStore, ContentSiteIndexStore converted; constraint/migration DDL left raw with `// Why:` comments.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/49-dapper-data-access-adoption/49-SPEC.md
@.planning/phases/49-dapper-data-access-adoption/49-CONTEXT.md
@.planning/phases/49-dapper-data-access-adoption/49-RESEARCH.md
@.planning/phases/49-dapper-data-access-adoption/49-PATTERNS.md
@.planning/phases/49-dapper-data-access-adoption/49-GATE-VERDICT.md
@CLAUDE.md

<interfaces>
<!-- Reference shapes: converted FeedbackStore.cs (49-01) + ContentSourceStore.cs (49-02). -->

Coerced types + distinctive concern per store (RESEARCH §Eligible Store Inventory Wave 2):
  - ContentHarvestRunStore: decimal + DateTimeOffset; FormatDecimal+ReadDecimal+FormatTimestamp+
    ReadDateTimeOffset all replaced by handlers; StartRunAsync uses RETURNING id.
  - HarvestRunStore (10 methods): Guid + DateTime; Guid write 118-121 (IsPostgres ternary),
    Guid read 383-389 (manual switch); BindNullableTimestamp for nullable DateTime;
    DDL migration EnsureStateConstraintAllowsInterruptedAsync (table rebuild in a transaction) STAYS RAW.
  - ContentVideoStore (~20 methods, largest): DateTimeOffset; FormatTimestamp + ReadDateTimeOffset;
    EnsureFilteredDistillStatusConstraintAsync DDL STAYS RAW.
  - ContentSiteIndexStore (16 methods): bool + DateTimeOffset; FormatVisibility+ReadVisibility;
    multiple ALTER TABLE in EnsureSchemaAsync + GetTableColumnsAsync (PRAGMA/information_schema) STAY RAW.

Connection-open: these stores use _connectionInfo.OpenConnectionAsync() (FK PRAGMA) — verify
  per call site and preserve. ContentStoreGeneratedId.Read(id) is replaced by ExecuteScalarAsync<long>
  at converted RETURNING-id sites; do NOT delete the ContentStoreGeneratedId helper if other
  unconverted sites still use it.

GuidTypeHandler (from 49-01): pass Guid directly in the anon param (encodes to string for SQLite);
  read maps the column to a Guid property automatically — remove the manual Guid.Parse switch.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Convert ContentHarvestRunStore</name>
  <files>DeckFlow.Core/Content/ContentHarvestRunStore.cs</files>
  <read_first>
    - .planning/phases/49-dapper-data-access-adoption/49-GATE-VERDICT.md (CONFIRM VERDICT: PASS; if FAIL, stop and report)
    - .planning/phases/49-dapper-data-access-adoption/49-PATTERNS.md lines 275-313 (ContentHarvestRunStore before/after: StartRunAsync ExecuteScalarAsync<long>, GetRunAsync QuerySingleOrDefaultAsync, helper deletions)
    - DeckFlow.Web/Services/FeedbackStore.cs + DeckFlow.Core/Content/ContentSourceStore.cs (converted reference shapes)
    - DeckFlow.Core/Content/ContentHarvestRunStore.cs (full — FormatDecimal 164, FormatTimestamp 167, ReadDateTimeOffset 186, ReadDecimal 198; EnsureSchemaAsync DDL 43-61; GetRunAsync reader loop 130-158)
    - DeckFlow.Core/Content/ContentModels.cs (ContentHarvestRun record shape — keep { get; init; })
    - DeckFlow.Core.Tests ContentHarvestRunStore test class
  </read_first>
  <action>
    Confirm VERDICT: PASS first. Add using Dapper. Convert StartRunAsync to connection.ExecuteScalarAsync&lt;long&gt; over the verbatim INSERT ... RETURNING id, passing DateTimeOffset and decimal directly (handlers encode); drop ContentStoreGeneratedId.Read at this site. Convert GetRunAsync and any list methods from reader loops to QueryAsync&lt;ContentHarvestRun&gt; / QuerySingleOrDefaultAsync&lt;ContentHarvestRun&gt;. Convert update/non-query methods to ExecuteAsync. Delete the FormatDecimal / FormatTimestamp / ReadDateTimeOffset / ReadDecimal helpers and any private ReadRun(reader). Keep EnsureSchemaAsync raw with a `// Why:` comment. Preserve the connection-open pattern. SQL verbatim; touch only changed lines (LF, no reformat).
  </action>
  <verify>
    <automated>"/mnt/c/Program Files/dotnet/dotnet.exe" build DeckFlow.sln 2>&1 | tail -5; grep -v '^[[:space:]]*//' DeckFlow.Core/Content/ContentHarvestRunStore.cs | grep -c ExecuteReaderAsync; "/mnt/c/Program Files/dotnet/dotnet.exe" test DeckFlow.Core.Tests/DeckFlow.Core.Tests.csproj --filter "FullyQualifiedName~ContentHarvestRun" 2>&1 | tail -12</automated>
  </verify>
  <acceptance_criteria>
    - build 0/0
    - Comment-filtered ExecuteReaderAsync count == 0 (DDL excepted)
    - FormatDecimal/FormatTimestamp/ReadDateTimeOffset/ReadDecimal helpers deleted
    - EnsureSchemaAsync carries `// Why:` and is structurally unchanged
    - ContentHarvestRun tests pass on SQLite; no public signature change
  </acceptance_criteria>
  <done>ContentHarvestRunStore reads/writes decimal + DateTimeOffset through Dapper with global handlers.</done>
</task>

<task type="auto">
  <name>Task 2: Convert HarvestRunStore (Guid + nullable DateTime; constraint-migration stays raw)</name>
  <files>DeckFlow.Web/Services/Harvest/HarvestRunStore.cs</files>
  <read_first>
    - .planning/phases/49-dapper-data-access-adoption/49-PATTERNS.md lines 361-394 (HarvestRunStore before/after: Guid write/read, BindNullableTimestamp deletion)
    - .planning/phases/49-dapper-data-access-adoption/49-RESEARCH.md §Coercion Semantics Guid + Pitfall (nullable timestamp -> DBNull automatically)
    - DeckFlow.Web/Services/Harvest/HarvestRunStore.cs (full — 10 methods; Guid write 118-121, Guid read 383-389, BindNullableTimestamp; EnsureStateConstraintAllowsInterruptedAsync table-rebuild migration)
    - DeckFlow.Web/Services/Harvest/HarvestRunModels.cs (HarvestRun record — Guid Id + DateTimeOffset?/DateTimeOffset fields; positional record, keep as-is)
    - DeckFlow.Web.Tests HarvestRunStore test class
  </read_first>
  <action>
    Confirm VERDICT: PASS first. Add using Dapper. Replace the Guid write IsPostgres ternary with the direct Guid value (GuidTypeHandler encodes). Remove the manual Guid.Parse switch in the read path — let Dapper map the column to the Guid Id property via the handler. Convert all reader loops to QueryAsync&lt;HarvestRun&gt; / QuerySingleOrDefaultAsync&lt;HarvestRun&gt;, scalar/RETURNING paths to ExecuteScalarAsync&lt;T&gt;, and non-query writes to ExecuteAsync, passing nullable DateTimeOffset?/DateTime? directly (Dapper sends DBNull for null; handler encodes non-null) — delete BindNullableTimestamp. Keep EnsureStateConstraintAllowsInterruptedAsync (the table-rebuild-in-transaction migration) entirely RAW and add a `// Why:` intentional-raw comment noting it is an immutable DDL migration. Preserve the connection-open pattern. SQL verbatim; touch only changed lines (LF, no reformat).
  </action>
  <verify>
    <automated>"/mnt/c/Program Files/dotnet/dotnet.exe" build DeckFlow.sln 2>&1 | tail -5; grep -v '^[[:space:]]*//' DeckFlow.Web/Services/Harvest/HarvestRunStore.cs | grep -c "ExecuteReaderAsync\|Guid.Parse\|IsPostgres ?"; "/mnt/c/Program Files/dotnet/dotnet.exe" test DeckFlow.Web.Tests/DeckFlow.Web.Tests.csproj --filter "FullyQualifiedName~HarvestRun" 2>&1 | tail -12</automated>
  </verify>
  <acceptance_criteria>
    - build 0/0
    - Comment-filtered grep for `ExecuteReaderAsync|Guid.Parse|IsPostgres ?` in non-DDL methods == 0
    - BindNullableTimestamp deleted; Guid handled by GuidTypeHandler
    - EnsureStateConstraintAllowsInterruptedAsync left raw with `// Why:` comment (table-rebuild migration unchanged)
    - HarvestRunStore tests pass on SQLite; no public signature change
  </acceptance_criteria>
  <done>HarvestRunStore maps Guid + nullable timestamps through global handlers; the constraint-migration DDL stays raw and intact.</done>
</task>

<task type="auto">
  <name>Task 3: Convert ContentVideoStore (largest content store, ~20 methods)</name>
  <files>DeckFlow.Core/Content/ContentVideoStore.cs</files>
  <read_first>
    - DeckFlow.Web/Services/FeedbackStore.cs + DeckFlow.Core/Content/ContentSourceStore.cs (converted reference shapes)
    - .planning/phases/49-dapper-data-access-adoption/49-PATTERNS.md lines 435-449 (common transformation template) + 475-489 (DDL stays raw + ContentStoreGeneratedId note)
    - .planning/phases/49-dapper-data-access-adoption/49-RESEARCH.md §Eligible Store Inventory Wave 2 ContentVideoStore row (EnsureFilteredDistillStatusConstraintAsync stays raw)
    - DeckFlow.Core/Content/ContentVideoStore.cs (full — ~20 methods; FormatTimestamp + ReadDateTimeOffset; EnsureFilteredDistillStatusConstraintAsync)
    - DeckFlow.Core/Content/ContentModels.cs (ContentVideo record shape — keep { get; init; })
    - DeckFlow.Core.Tests ContentVideoStoreTests + ContentVideoStoreDistillTests
  </read_first>
  <action>
    Confirm VERDICT: PASS first. For ContentVideoStore (~20 methods): add using Dapper; methodically convert every non-DDL read loop to QueryAsync&lt;T&gt;/QuerySingleOrDefaultAsync&lt;T&gt;, scalar/RETURNING to ExecuteScalarAsync&lt;long&gt; (drop ContentStoreGeneratedId.Read at converted sites; keep the helper while any unconverted site remains), and writes to ExecuteAsync with anonymous-object params passing DateTimeOffset directly (DateTimeOffsetTypeHandler encodes); delete FormatTimestamp + ReadDateTimeOffset; keep EnsureFilteredDistillStatusConstraintAsync and EnsureSchemaAsync raw with `// Why:` comments. Preserve the store's connection-open pattern. SQL verbatim; touch only changed lines (LF, no reformat); do not convert any { get; init; } to { get; }.
  </action>
  <verify>
    <automated>"/mnt/c/Program Files/dotnet/dotnet.exe" build DeckFlow.sln 2>&1 | tail -5; echo "ContentVideoStore.cs:"; grep -v '^[[:space:]]*//' DeckFlow.Core/Content/ContentVideoStore.cs | grep -c ExecuteReaderAsync; "/mnt/c/Program Files/dotnet/dotnet.exe" test DeckFlow.Core.Tests/DeckFlow.Core.Tests.csproj --filter "FullyQualifiedName~ContentVideoStore" 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - build 0/0
    - Comment-filtered ExecuteReaderAsync count == 0 in ContentVideoStore.cs (DDL excepted)
    - FormatTimestamp/ReadDateTimeOffset deleted
    - EnsureFilteredDistillStatusConstraintAsync + EnsureSchemaAsync left raw with `// Why:` comments
    - `ContentVideoStoreTests` + `ContentVideoStoreDistillTests` pass on SQLite (both confirmed real classes; the `~ContentVideoStore` filter matches both); no public signature change
  </acceptance_criteria>
  <done>The largest content store runs non-DDL paths through Dapper; its constraint/DDL stays raw and documented.</done>
</task>

<task type="auto">
  <name>Task 4: Convert ContentSiteIndexStore (16 methods)</name>
  <files>DeckFlow.Core/Content/ContentSiteIndexStore.cs</files>
  <read_first>
    - DeckFlow.Web/Services/FeedbackStore.cs + DeckFlow.Core/Content/ContentSourceStore.cs (converted reference shapes)
    - .planning/phases/49-dapper-data-access-adoption/49-PATTERNS.md lines 435-449 (common transformation template) + 475-489 (DDL stays raw + ContentStoreGeneratedId note)
    - .planning/phases/49-dapper-data-access-adoption/49-RESEARCH.md §Eligible Store Inventory Wave 2 ContentSiteIndexStore row (ALTER TABLE + GetTableColumnsAsync stay raw)
    - DeckFlow.Core/Content/ContentSiteIndexStore.cs (full — 16 methods; FormatVisibility/ReadVisibility bool; ALTER TABLE in EnsureSchemaAsync; GetTableColumnsAsync PRAGMA/information_schema)
    - DeckFlow.Core/Content/ContentModels.cs (site-index record shape — keep { get; init; })
    - DeckFlow.Core.Tests ContentSiteIndexStoreTests + ContentSiteIndexStoreVisibilityTests + ContentSiteIndexStoreApprovalTests
  </read_first>
  <action>
    Confirm VERDICT: PASS first. For ContentSiteIndexStore (16 methods): add using Dapper; convert every non-DDL read loop to QueryAsync&lt;T&gt;/QuerySingleOrDefaultAsync&lt;T&gt;, scalar/RETURNING to ExecuteScalarAsync&lt;long&gt;, and writes to ExecuteAsync with anonymous-object params; replace FormatVisibility (bool write) with the direct bool value and delete ReadVisibility (BoolTypeHandler handles both); pass DateTimeOffset values directly (DateTimeOffsetTypeHandler encodes). Keep ALL ALTER TABLE migrations in EnsureSchemaAsync AND GetTableColumnsAsync (PRAGMA table_info / information_schema introspection) raw with `// Why:` comments — these are schema-introspection/DDL, not eligible. Preserve the store's connection-open pattern. SQL verbatim; touch only changed lines (LF, no reformat); do not convert any { get; init; } to { get; }.
  </action>
  <verify>
    <automated>"/mnt/c/Program Files/dotnet/dotnet.exe" build DeckFlow.sln 2>&1 | tail -5; echo "ContentSiteIndexStore.cs:"; grep -v '^[[:space:]]*//' DeckFlow.Core/Content/ContentSiteIndexStore.cs | grep -c ExecuteReaderAsync; "/mnt/c/Program Files/dotnet/dotnet.exe" test DeckFlow.Core.Tests/DeckFlow.Core.Tests.csproj --filter "FullyQualifiedName~ContentSiteIndexStore" 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - build 0/0
    - Comment-filtered ExecuteReaderAsync count == 0 in ContentSiteIndexStore.cs (DDL + GetTableColumnsAsync introspection excepted)
    - FormatVisibility/ReadVisibility deleted
    - EnsureSchemaAsync ALTER TABLEs + GetTableColumnsAsync left raw with `// Why:` comments
    - `ContentSiteIndexStoreTests` + `ContentSiteIndexStoreVisibilityTests` + `ContentSiteIndexStoreApprovalTests` pass on SQLite (all confirmed real classes; the `~ContentSiteIndexStore` filter matches all three); no public signature change
  </acceptance_criteria>
  <done>ContentSiteIndexStore runs non-DDL paths through Dapper; its ALTER/introspection DDL stays raw and documented.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| application code → SQL execution | Store methods bind harvested-content fields (video metadata, run state, site-index rows) into queries |
| DB reader → CLR object (type handlers) | Encoded decimal/DateTimeOffset/bool/Guid values decoded by global handlers |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-49-07 | Tampering | Converted store params (SQL injection regression) | mitigate | SQL kept verbatim and parameterized via Dapper anonymous objects; UPSERT/RETURNING/ON CONFLICT unchanged; no interpolation introduced |
| T-49-08 | Tampering | HarvestRunStore Guid coercion correctness (D-05 parity) | mitigate | GuidTypeHandler round-trip proven in REQ-2; HarvestRunStore existing tests assert run-id round-trip; fail-closed InvalidCastException on malformed values |
| T-49-09 | Tampering | DDL/constraint migration accidentally rewritten | mitigate | EnsureFilteredDistillStatusConstraintAsync / EnsureStateConstraint* / ALTER TABLE / GetTableColumnsAsync kept raw with `// Why:` comments; grep confirms no CREATE/ALTER rewritten |
</threat_model>

<verification>
- `dotnet build DeckFlow.sln` 0/0
- Comment-filtered ExecuteReaderAsync == 0 across the four files (DDL/introspection excepted)
- Per-store test classes green on SQLite; PG side via gated-manual run before phase verify
</verification>

<success_criteria>
- Four mid-complexity stores converted; Guid + nullable timestamps + bool + decimal + DateTimeOffset all via global handlers
- Constraint/ALTER/introspection DDL preserved raw with `// Why:` comments
- No public store signature changed; existing tests green on SQLite
- Only proceeds on a PASS spike verdict
</success_criteria>

<output>
Create `.planning/phases/49-dapper-data-access-adoption/49-03-SUMMARY.md` when done. Note any per-query AS alias required and any method left raw beyond the documented carve-outs.
</output>
