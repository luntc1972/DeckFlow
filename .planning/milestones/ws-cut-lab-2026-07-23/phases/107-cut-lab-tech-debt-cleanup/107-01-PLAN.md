---
phase: 107-cut-lab-tech-debt-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - DeckFlow.Web/Services/CutLab/CutLabPageService.cs
  - DeckFlow.Web.Tests/CutLabPageServiceTests.cs
  - DeckFlow.Web.Tests/CutLabOriginalEntriesTests.cs
autonomous: true
requirements: [CLEANUP-1]
must_haves:
  truths:
    - "CutLabPageService no longer declares or accepts _spellbook / _categoryKnowledge"
    - "All structural-analysis-dependent tests still exercise combo/category analysis (not silently degraded)"
    - "dotnet build is clean and the full CutLab test filter passes"
  artifacts:
    - path: "DeckFlow.Web/Services/CutLab/CutLabPageService.cs"
      provides: "CutLabPageService with the two dead DI fields/params removed and HasStructuralAnalysisDependencies rewritten"
      contains: "HasStructuralAnalysisDependencies"
    - path: "DeckFlow.Web.Tests/CutLabPageServiceTests.cs"
      provides: "Updated ctor call sites + DI-guard tests"
  key_links:
    - from: "DeckFlow.Web.Tests/CutLabPageServiceTests.cs (10 fallback-reliant tests)"
      to: "CutLabAnalysisContextBuilder"
      via: "explicit builder passed by named analysisContextBuilder: argument"
      pattern: "analysisContextBuilder:"
---

<objective>
Item 1 — Remove the dead `_spellbook` + `_categoryKnowledge` fields and their ctor params from
`CutLabPageService` (they are unused by this class; `CutLabAnalysisContextBuilder` legitimately
uses its own copies and is OUT OF SCOPE). Rewrite the internal `HasStructuralAnalysisDependencies`
DI-shape guard so it no longer references the removed fields, and rework the test suite's 43
constructor call sites so structural-analysis coverage is preserved rather than silently degraded.

Purpose: Cleaner service surface; retire a known tech-debt item without weakening test coverage.
Output: Slimmer `CutLabPageService` ctor; all CutLab tests green with equivalent assertions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/cut-lab/phases/107-cut-lab-tech-debt-cleanup/107-CONTEXT.md
@.planning/workstreams/cut-lab/phases/107-cut-lab-tech-debt-cleanup/107-RESEARCH.md

Scope decisions (locked, from CONTEXT D-01 + RESEARCH open-question 4):
- Remove the two fields/params from `CutLabPageService` ONLY. Do NOT touch
  `CutLabAnalysisContextBuilder.cs` (:81-82, :499-546) — it uses them for real classification.
- `HasStructuralAnalysisDependencies` is KEPT but re-scoped to the three remaining real
  dependencies (`_manabaseBaseline`, `_cedhBaseline`, `_simulationService`); the two removed-field
  `&&` conditions are dropped. Do NOT delete the property or its DI-guard tests — update them.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove dead fields/params and re-scope the DI-shape guard</name>
  <read_first>
    - DeckFlow.Web/Services/CutLab/CutLabPageService.cs (fields :105-106; ctor params :128-129;
      assignments :143-144; ctor fallback construction :147-149; guard :159-164)
    - DeckFlow.Web/Services/CutLab/CutLabAnalysisContextBuilder.cs (ctor signature only — confirm
      which tail params are optional/nullable, so the fallback can construct without the removed args)
    - DeckFlow.Web/Extensions/CutLabServiceCollectionExtensions.cs (:22 confirms
      ICutLabAnalysisContextBuilder is DI-registered → production fallback branch is dead)
  </read_first>
  <action>
    Delete the `_categoryKnowledge` (:105) and `_spellbook` (:106) fields, the `categoryKnowledge`
    (:128) and `spellbook` (:129) ctor params, and their assignments (:143-144). In the ctor
    fallback construction (`_analysisContextBuilder = analysisContextBuilder ?? new
    CutLabAnalysisContextBuilder(...)`, :147-149) drop the two removed arguments — construct the
    builder from the remaining args, relying on `CutLabAnalysisContextBuilder`'s optional/nullable
    tail params (confirmed via read_first). Rewrite `HasStructuralAnalysisDependencies` (:159-164)
    to remove the `_categoryKnowledge is not null && _spellbook is not null &&` clauses, keeping the
    three surviving checks (`_manabaseBaseline is not null && _cedhBaseline is not null &&
    !ReferenceEquals(_simulationService, NoOpCutLabSimulationService.Instance)`). Preserve xmldoc
    single-space indent and all five editorconfig carve-outs; touch only changed lines (LF endings).
    No Program.cs change is needed — the container resolves the remaining registered params.
  </action>
  <acceptance_criteria>
    - `grep -n "_spellbook\|_categoryKnowledge" DeckFlow.Web/Services/CutLab/CutLabPageService.cs`
      returns zero matches.
    - `grep -c "spellbook\|categoryKnowledge" DeckFlow.Web/Services/CutLab/CutLabPageService.cs`
      (case-sensitive, excluding comments) shows the ctor no longer names either param.
    - `HasStructuralAnalysisDependencies` remains present and references exactly
      `_manabaseBaseline`, `_cedhBaseline`, and `_simulationService` (three checks).
    - `dotnet build DeckFlow.sln` is clean (no new warnings/errors).
    - `CutLabAnalysisContextBuilder.cs` is unchanged (`git diff --stat` shows no entry for it).
  </acceptance_criteria>
  <verify>
    <automated>dotnet build DeckFlow.sln</automated>
  </verify>
  <done>Fields/params/assignments removed; fallback constructs without the removed args; guard re-scoped to 3 deps; build clean; CutLabAnalysisContextBuilder untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Rework the 43 constructor call sites so structural-analysis coverage is preserved</name>
  <read_first>
    - DeckFlow.Web.Tests/CutLabPageServiceTests.cs — ALL `new CutLabPageService(` call sites
      (42 sites). The model-to-copy pattern already lives here at ~:1637 and ~:1668
      (`ProcessAsync_SpellbookFailure_FailsOpenAndLogsWarning`,
      `ProcessAsync_CategoryLookupFailure_FailsOpenAndLogsWarning`): they build an explicit
      `CutLabAnalysisContextBuilder analysisBuilder` wired with fakes and pass it via the NAMED
      `analysisContextBuilder:` argument.
    - The 3 DI-guard tests + `BuildDiGuardProvider` helper (~:2136):
      `CutLabPageService_DiContainerMirrorsProgramRegistrationAndSuppliesOptionalAnalysisDependencies`,
      `CutLabPageService_DiGuardFailsWhenOptionalAnalysisRegistrationDrops`,
      `CutLabPageService_DiGuardFailsWhenSimulationRegistrationDrops`, plus
      `ProcessAsync_WithOptionalAnalysisDependenciesOmitted_BehavesAsUnavailable` (~:1692).
    - DeckFlow.Web.Tests/CutLabOriginalEntriesTests.cs (~:250 shared test-service factory helper).
  </read_first>
  <action>
    Update all 43 `new CutLabPageService(...)` call sites for the removed params:
    (a) ~33 sites that pass nothing/null in the categoryKnowledge/spellbook slots → simply drop the
    two arguments.
    (b) The 10 fallback-reliant sites that pass live `FakeCategoryKnowledgeStore`/`FakeSpellbookService`
    positionally (CutLabPageServiceTests.cs ~:946, ~:1050, ~:1119, ~:1359, ~:1409, ~:1472, ~:1714,
    ~:1734, ~:1826; CutLabOriginalEntriesTests.cs ~:250) → rewrite to construct an explicit
    `CutLabAnalysisContextBuilder` wired with those same fakes and pass it via the named
    `analysisContextBuilder:` argument (copy the existing ~:1637/~:1668 pattern). Do NOT just delete
    the two args on these — that would drop combo/category coverage silently.
    (c) The 3 DI-guard tests + `BuildDiGuardProvider`: update their expected DI shape to match the
    re-scoped `HasStructuralAnalysisDependencies` (three deps). Keep asserting the guard flips
    false when the analysis registration or simulation registration drops; remove any assertion tied
    specifically to the removed spellbook/categoryKnowledge slots. Verify
    `ProcessAsync_WithOptionalAnalysisDependenciesOmitted_BehavesAsUnavailable` still expresses the
    intended omitted-dependencies behavior after the signature change.
    Optional (nice-to-have, not required): extract a private `BuildServiceWithAnalysis(...)` factory
    helper in the test file to reduce future ctor-signature churn — only if it does not obscure the diff.
    Touch only changed lines; LF endings.
  </action>
  <acceptance_criteria>
    - `grep -c "new CutLabPageService(" DeckFlow.Web.Tests/CutLabPageServiceTests.cs` still returns
      42 (call sites updated, not removed); `CutLabOriginalEntriesTests.cs` still has its 1 site.
    - Zero call site passes a positional `FakeCategoryKnowledgeStore`/`FakeSpellbookService` into the
      removed slots; the 10 previously-fallback-reliant tests now reference `analysisContextBuilder:`.
    - `dotnet test DeckFlow.sln --filter "FullyQualifiedName~CutLabPageServiceTests"` passes.
    - `dotnet test DeckFlow.sln --filter "FullyQualifiedName~CutLabOriginalEntries"` passes.
    - The 3 DI-guard tests pass and still assert both failure branches (analysis-drop, simulation-drop).
  </acceptance_criteria>
  <verify>
    <automated>dotnet test DeckFlow.sln --filter "FullyQualifiedName~CutLab"</automated>
  </verify>
  <done>All 43 sites updated; 10 fallback-reliant tests preserve analysis coverage via explicit builder; DI-guard tests match re-scoped guard; CutLab test filter green.</done>
</task>

</tasks>

<threat_model>
No new threats. Internal C# refactor + test edits only — removes DI fields/params, adds no new
inputs, endpoints, deserialization surface, or auth paths. Blast radius is compile-time (ctor
signature) + test coverage; no runtime attack surface change.
</threat_model>

<verification>
- `dotnet build DeckFlow.sln` clean.
- `dotnet test DeckFlow.sln --filter "FullyQualifiedName~CutLab"` green.
- `git diff --stat` shows NO change to `CutLabAnalysisContextBuilder.cs`.
- `git diff --ignore-all-space --stat` vs `git diff --stat` gap is small (no EOL churn).
</verification>

<success_criteria>
CutLabPageService carries no dead spellbook/categoryKnowledge state; structural-analysis test
coverage is preserved (not degraded to null-fed fail-open); build + CutLab tests green.
</success_criteria>

<output>
Create `.planning/workstreams/cut-lab/phases/107-cut-lab-tech-debt-cleanup/107-01-SUMMARY.md` when done
</output>
