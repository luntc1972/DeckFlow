---
phase: 82-refactor-review-sweep-ui-baseline-audit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/82-refactor-review-sweep-ui-baseline-audit/82-REVIEW.md
  - .planning/phases/82-refactor-review-sweep-ui-baseline-audit/REFACTOR-TRIAGE.md
autonomous: true
requirements: [REVIEW-01]
must_haves:
  truths:
    - "A code-review sweep has been run over the largest/most-duplicated files (including deck-sync.ts 2877 LOC and Harvest.razor.cs 1225 LOC)"
    - "Every surfaced SRP/duplication candidate has a recorded triage decision: in-scope-this-cycle OR backlog"
    - "Every triage decision carries a written reason and a risk-budget assessment"
    - "The pre-scoped PKTSVC/THEME/AICLEAN families are excluded from this sweep's new candidate list (they are already owned by Phases 83/84/85)"
  artifacts:
    - path: ".planning/phases/82-refactor-review-sweep-ui-baseline-audit/82-REVIEW.md"
      provides: "Severity-classified structured code-review findings over the swept files"
      contains: "deck-sync.ts"
    - path: ".planning/phases/82-refactor-review-sweep-ui-baseline-audit/REFACTOR-TRIAGE.md"
      provides: "Per-candidate triage table with in-scope|backlog decision, reason, and risk-budget note"
      contains: "in-scope"
  key_links:
    - from: ".planning/phases/82-refactor-review-sweep-ui-baseline-audit/REFACTOR-TRIAGE.md"
      to: "82-03-PLAN.md refactor execution"
      via: "triage table consumed by Wave 2 refactor tasks"
      pattern: "in-scope|backlog"
---

<objective>
Run a code-review sweep over the largest and most-duplicated source files in the
codebase to surface remaining SRP/duplication debt BEYOND the pre-scoped
PKTSVC / THEME / AICLEAN families, then record an explicit triage decision
(in-scope-this-cycle vs. backlog) with reasoning for every surfaced candidate.

Purpose: The rest of Cycle 15 locks its scope after this phase. Surfacing and
triaging remaining refactor debt now — as a recorded artifact — lets the confirmed
in-scope targets be executed in Wave 2 (plan 82-03) and lets over-budget targets be
deferred deliberately instead of discovered late. This satisfies REVIEW-01.

Output: `82-REVIEW.md` (structured findings) and `REFACTOR-TRIAGE.md` (the
per-candidate decision table that plan 82-03 consumes).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@./CLAUDE.md

# Scope-fence: these families are ALREADY owned by later phases — do NOT re-surface
# them as new candidates in this sweep:
#  - PKTSVC (DeckAnalysisPacketService / DeckComparisonService / MetaGapService /
#    DeckPrimerPacketService) -> Phase 83
#  - THEME (--accent-strong semantic-token migration across 27 theme forks) -> Phase 84
#  - AICLEAN (chatgpt-* identifier rename) -> Phase 85
#  - Manabase engine (CastabilitySimulator / ManabaseAnalyzer / ManabaseClassifier)
#    -> explicitly deferred out of Cycle 15 (needs a numeric-parity harness first)
#  - DeckController god-class split -> already DONE (verified 2026-07-04)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run the code-review sweep over the largest/most-duplicated files</name>
  <files>.planning/phases/82-refactor-review-sweep-ui-baseline-audit/82-REVIEW.md</files>
  <read_first>
    - DeckFlow.Web/wwwroot/ts/deck-sync.ts (2877 LOC — named REVIEW-01 candidate)
    - DeckFlow.Studio/Pages/Harvest.razor.cs (1225 LOC — named REVIEW-01 candidate)
    - ./CLAUDE.md (byte-identical/behavior-neutral gate, carve-outs, SOLID review criteria)
    - .planning/REQUIREMENTS.md (REVIEW-01 wording + the "Future Requirements (Deferred)" and "Out of Scope" lists that fence the sweep)
  </read_first>
  <action>
    Enumerate the largest/most-duplicated source files to sweep. Start from the two
    named candidates (DeckFlow.Web/wwwroot/ts/deck-sync.ts, DeckFlow.Studio/Pages/Harvest.razor.cs)
    and widen using a size + duplication pass:
    run `find DeckFlow.Web DeckFlow.Studio DeckFlow.Core -type f \( -name '*.cs' -o -name '*.ts' \) -not -path '*/obj/*' -not -path '*/bin/*' -not -path '*/node_modules/*' -print0 | xargs -0 wc -l | sort -rn | head -30`
    to rank by LOC, and note obvious duplication clusters (e.g. df-select.ts 845 LOC,
    card-lookup.ts 557, DirectPush.razor.cs 468).
    Invoke the gsd-code-reviewer agent with an explicit file list scoped to the ranked
    candidates, depth `standard`, phase_dir `.planning/phases/82-refactor-review-sweep-ui-baseline-audit`,
    review_path `.planning/phases/82-refactor-review-sweep-ui-baseline-audit/82-REVIEW.md`.
    EXCLUDE from the candidate list every file whose PRIMARY debt is one of the four fenced
    families listed in this plan's `<context>` (PKTSVC packet services, theme CSS forks /
    --accent-strong migration, chatgpt-* identifier rename, manabase engine) and the
    already-split DeckController — those are owned by later phases and must not be re-surfaced
    as NEW candidates here. Fence by candidate TYPE, not by substring: a finding must NOT propose
    "rename chatgpt-* identifiers" or "migrate --accent-strong" as its action. It is FINE for a
    structural/SRP finding on an in-scope file (e.g. deck-sync.ts, which legitimately contains
    ~65 `chatgpt-*` selectors) to mention those selectors incidentally — only a finding whose
    primary action IS the fenced rename/migration is excluded.
    The reviewer produces 82-REVIEW.md with severity-classified (BLOCKER/HIGH/MEDIUM/LOW)
    SRP and duplication findings per file.
  </action>
  <acceptance_criteria>
    - `.planning/phases/82-refactor-review-sweep-ui-baseline-audit/82-REVIEW.md` exists.
    - `grep -c "deck-sync.ts" .planning/phases/82-refactor-review-sweep-ui-baseline-audit/82-REVIEW.md` returns >= 1 and the file also references Harvest.razor.cs (both named candidates were swept).
    - Every finding in 82-REVIEW.md carries a severity classification (BLOCKER|HIGH|MEDIUM|LOW), per gsd-code-reviewer contract.
    - No PKTSVC packet-service or manabase-engine CLASS appears as a NEW refactor candidate in 82-REVIEW.md (these class names never appear incidentally in a deck-sync/df-select structural review, so a substring ban is safe here): `grep -viE '^#' .planning/phases/82-refactor-review-sweep-ui-baseline-audit/82-REVIEW.md | grep -ciE 'CastabilitySimulator|ManabaseAnalyzer|ManabaseClassifier|DeckAnalysisPacketService|DeckComparisonService|MetaGapService|DeckPrimerPacketService'` returns 0.
    - THEME/AICLEAN fence (manual review criterion — NOT an automated grep, because a raw substring/regex over free-form reviewer prose false-trips on legitimate incidental mentions and on backlog notes like "defer the chatgpt-* rename to Phase 85"): no finding in 82-REVIEW.md proposes a `chatgpt-*` identifier rename or a `--accent-strong` migration as the Phase-82 refactor ACTION. Incidental mentions of those selectors while describing structural/SRP debt, and explicit "owned by Phase 84/85" deferral notes, are allowed and expected. The executor confirms this by reading each finding's recommended action, not by grepping.
  </acceptance_criteria>
  <verify>
    <automated>test -f .planning/phases/82-refactor-review-sweep-ui-baseline-audit/82-REVIEW.md && grep -q "deck-sync.ts" .planning/phases/82-refactor-review-sweep-ui-baseline-audit/82-REVIEW.md && grep -q "Harvest.razor.cs" .planning/phases/82-refactor-review-sweep-ui-baseline-audit/82-REVIEW.md</automated>
  </verify>
  <done>82-REVIEW.md exists with severity-classified SRP/duplication findings covering both named candidates plus the widened ranked file set, with the four fenced families excluded.</done>
</task>

<task type="auto">
  <name>Task 2: Record a triage decision + reason for every surfaced candidate</name>
  <files>.planning/phases/82-refactor-review-sweep-ui-baseline-audit/REFACTOR-TRIAGE.md</files>
  <read_first>
    - .planning/phases/82-refactor-review-sweep-ui-baseline-audit/82-REVIEW.md (the findings produced in Task 1 — the input to triage)
    - .planning/STATE.md ("Resolved Decisions (Cycle 15)" — byte-identical gate, coarse granularity, cycle risk posture)
    - ./CLAUDE.md (byte-identical/behavior-neutral gate — the risk-budget yardstick: a refactor is in-scope only if a behavior-neutrality proof is achievable with existing tests/gates)
    - DeckFlow.Web/package.json (test script — the repo HAS a `vitest` TS test runner; do NOT assume TS files are untested)
    - DeckFlow.Web/vitest.config.ts (vitest config — targets ts-tests/**/*.test.ts)
    - DeckFlow.Web/ts-tests/ (existing TS unit tests — busy-overlay-pageshow.test.ts, cedh-commander-typeahead.test.ts, primer-copy.test.ts already import wwwroot/ts/deck-sync.ts; determine real coverage from these, not from an assumption)
  </read_first>
  <action>
    Write `REFACTOR-TRIAGE.md` as a decision table. For EVERY candidate surfaced in
    82-REVIEW.md (one row per file/finding cluster) record: (a) file + short finding
    summary, (b) decision = `in-scope` or `backlog`, (c) written reason, (d) risk-budget
    note stating whether a byte-identical/behavior-neutral proof is achievable this cycle
    with the existing gates — for C# targets: dotnet build clean + xUnit suite + CarveOutGuard +
    changed-lines format-gate; for TS targets (deck-sync.ts, df-select.ts, …): the Vitest suite
    (`npm --prefix DeckFlow.Web test`) + Playwright e2e (functional parity, no pixel-diff) — and
    (e) if in-scope, the concrete refactor approach
    (extract-method / extract-collaborator / dedupe) and the exact files it will touch.
    Apply the risk-budget rule explicitly: a target is `in-scope` only when its refactor is
    behavior-neutral AND provable with existing automated coverage without building new
    harness infrastructure; a target that would need net-new test scaffolding or that risks
    observable behavior change is `backlog` with the reason spelled out — never silently dropped.
    Determine each target's ACTUAL existing coverage from the read_first files before judging
    (the repo has a vitest TS runner and deck-sync.ts is already imported by 3 ts-tests files, so
    "no test harness" is NOT a valid deferral reason for it) — do not deduce coverage from
    assumptions. Add a one-line summary count at the top:
    "N candidates surfaced — X in-scope, Y backlog." This file is the contract 82-03 consumes.
  </action>
  <acceptance_criteria>
    - `.planning/phases/82-refactor-review-sweep-ui-baseline-audit/REFACTOR-TRIAGE.md` exists.
    - Every candidate row carries a decision token: `grep -ciE 'in-scope|backlog' REFACTOR-TRIAGE.md` (path-qualified) equals the number of candidate rows, and there are zero rows without one of those two tokens.
    - Every `backlog` row has a non-empty reason string (no bare deferrals).
    - The header summary count line "candidates surfaced" is present: `grep -qi 'candidates surfaced' .planning/phases/82-refactor-review-sweep-ui-baseline-audit/REFACTOR-TRIAGE.md`.
    - Both named candidates (deck-sync.ts, Harvest.razor.cs) appear as explicit rows with an explicit decision.
  </acceptance_criteria>
  <verify>
    <automated>test -f .planning/phases/82-refactor-review-sweep-ui-baseline-audit/REFACTOR-TRIAGE.md && grep -qiE 'in-scope|backlog' .planning/phases/82-refactor-review-sweep-ui-baseline-audit/REFACTOR-TRIAGE.md && grep -qi 'candidates surfaced' .planning/phases/82-refactor-review-sweep-ui-baseline-audit/REFACTOR-TRIAGE.md && grep -q 'deck-sync.ts' .planning/phases/82-refactor-review-sweep-ui-baseline-audit/REFACTOR-TRIAGE.md && grep -q 'Harvest.razor.cs' .planning/phases/82-refactor-review-sweep-ui-baseline-audit/REFACTOR-TRIAGE.md</automated>
  </verify>
  <done>REFACTOR-TRIAGE.md lists every surfaced candidate with an in-scope|backlog decision, a written reason, and a risk-budget note; header carries the surfaced/in-scope/backlog counts; both named candidates are explicit rows. REVIEW-01 satisfied.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none introduced) | This plan is read-only over source + writes two planning docs. No new inputs, endpoints, auth surfaces, or data flows. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-82-01 | Information Disclosure | 82-REVIEW.md / REFACTOR-TRIAGE.md committed to a public repo | accept | Artifacts contain only file paths and refactor reasoning — no secrets, credentials, or PII. Repo is already public; no new exposure. |
| T-82-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan — sweep + triage are read-only + doc writes. No supply-chain surface. |

Behavior-neutral, read-only sweep: no new inputs, endpoints, auth, or data flows are
introduced. The realistic threat surface is near-zero; the mitigations of record are the
public-repo no-secrets discipline (CLAUDE.md) and the fact that no source behavior changes here.
</threat_model>

<verification>
- `82-REVIEW.md` exists with severity-classified findings covering both named candidates.
- `REFACTOR-TRIAGE.md` exists; every candidate row has an in-scope|backlog decision + reason.
- No fenced-family file (PKTSVC/THEME/AICLEAN/manabase) is surfaced as a new candidate.
</verification>

<success_criteria>
- REVIEW-01 satisfied: sweep run over the largest/most-duplicated files; every surfaced
  candidate triaged in-scope|backlog with recorded reasoning and a risk-budget note.
- The triage artifact is structured so plan 82-03 can consume the in-scope rows directly.
</success_criteria>

<output>
Create `.planning/phases/82-refactor-review-sweep-ui-baseline-audit/82-01-SUMMARY.md` when done.
</output>
