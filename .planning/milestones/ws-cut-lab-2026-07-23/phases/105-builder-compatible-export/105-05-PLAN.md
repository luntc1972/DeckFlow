---
phase: 105-builder-compatible-export
plan: 05
type: execute
wave: 4
depends_on: [105-04]
files_modified:
  - DeckFlow.Web/e2e/cut-lab-export.spec.ts
autonomous: false
requirements: [EXPORT-01, EXPORT-02, EXPORT-03]
must_haves:
  truths:
    - "An end-to-end run reaches 100 cards, activates the Export step tab (submitting the Export form — a server POST /cut-lab/export, not a client toggle), and sees both-dialect full-list text + a CUT/ADD patch + the validation summary render in the re-rendered panel."
    - "Full unit + e2e suites are green and the Export panel renders correctly across themes on desktop and mobile."
  artifacts:
    - path: "DeckFlow.Web/e2e/cut-lab-export.spec.ts"
      provides: "Headless Playwright coverage of the Export surface, mirroring cut-lab-whatif.spec.ts"
  key_links:
    - from: "cut-lab-export.spec.ts"
      to: "/cut-lab export panel"
      via: "reach-100 → export → copy blocks + validation summary assertions"
      pattern: "export"
---

<objective>
Prove the whole phase end-to-end and gate it. Add a headless Playwright spec that drives Cut Lab to exactly 100 cards, opens the Export step, and asserts the four copy blocks + the 3-check validation summary. Run the full unit + e2e suites, capture theme × viewport screenshots (new Export tab is a UI change), and hold a human-verify checkpoint before closing the phase.

Purpose: EXPORT-01/02/03 are user-facing and UI-bearing; the phase's core value ("paste straight back into the builder, zero reformatting") must be observed working, not assumed.
Output: New e2e spec, green full suites, theme/viewport screenshots, and a human sign-off.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/cut-lab/phases/105-builder-compatible-export/105-CONTEXT.md
@DeckFlow.Web/e2e/cut-lab-whatif.spec.ts

<interfaces>
<!-- Mirror this spec's structure/fixtures exactly: cut-lab-whatif.spec.ts -->
<!--   baseUrl = 'http://localhost:5173'; acquireAdminLockForTest / releaseAdminLockForTest (support/admin-lock) -->
<!--   setToolEnabled (support/admin-tools) to flip tool.cut-lab.enabled -->
<!--   oversizedPool fixture (Commander + Deck lines) already defined in the whatif spec -->

<!-- Server start (CLAUDE.md): scripts/run-web-test.sh sets DECKFLOW_DISABLE_AUTO_BROWSER=true -->
<!-- Live UI checks: npx --no-install playwright test  (WSL headless; env -u DISPLAY -u WAYLAND_DISPLAY). NEVER open a Windows-host browser. -->
<!-- Probe port 5173 with netstat before starting a server (a stale Windows server may hold it). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: cut-lab-export.spec.ts + full-suite gate + theme/viewport screenshots</name>
  <files>DeckFlow.Web/e2e/cut-lab-export.spec.ts</files>
  <action>
    Create `DeckFlow.Web/e2e/cut-lab-export.spec.ts` mirroring `cut-lab-whatif.spec.ts` structure (admin-lock, `setToolEnabled('cut-lab', ...)`, oversized-pool fixture, headless). Coverage:
    1. Below 100: Export step is disabled/greyed (aria-disabled) and the final-list copy is blocked with the "reach 100 to export" hint.
    2. Reach exactly 100 (accept cuts / or seed a 100-card fixture): the Export step tab unlocks (no longer `aria-disabled`); CLICK/activate it to submit the Export form (`POST /cut-lab/export`) and wait for the server re-render, THEN assert the Export panel + the always-visible validation summary are populated — the Moxfield and Archidekt full-list textareas are non-empty; the Moxfield and Archidekt patch blocks show a CUT section (and ADD noted empty for a pure-cut run). (The panel is empty until this server round-trip — the tab is a submit control, not a client toggle.)
    3. Validation summary is always visible with the three checks; a knowingly-off-identity or banned card (if seeded) surfaces a named warning WITHOUT disabling export (D2). If feasible with the fixture, assert an illegal color-identity card and an unverified card render as distinct statuses (WARNING 7 distinction) rather than one merged offender line.
    Start the app with `scripts/run-web-test.sh` (DECKFLOW_DISABLE_AUTO_BROWSER=true); probe port 5173 with netstat first. Run the full unit suites (`dotnet test DeckFlow.Core.Tests`, `dotnet test DeckFlow.Web.Tests`) and the full e2e suite; capture Export-panel screenshots at desktop + mobile viewports across at least two guild themes (per CLAUDE.md web-page-change rule) and save them under the phase's ui-design folder for the checkpoint.
  </action>
  <verify>
    <automated>cd DeckFlow.Web && npx --no-install playwright test e2e/cut-lab-export.spec.ts</automated>
  </verify>
  <done>cut-lab-export.spec.ts passes headless; full unit + e2e suites green; theme × viewport screenshots captured.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification of the Export surface</name>
  <what-built>The Cut Lab Export surface: a Process→Decide→Goals→Export step tab that unlocks at exactly 100 cards, Moxfield + Archidekt full-list copy blocks, a both-dialect CUT/ADD patch, and an always-visible count/color-identity/banlist validation summary (count is the only hard block).</what-built>
  <how-to-verify>
    1. Start the app headless: `scripts/run-web-test.sh` (confirm port 5173 free first via netstat).
    2. Enable the tool via /Admin/Flags (tool.cut-lab.enabled) or the e2e admin helper.
    3. Visit http://localhost:5173/cut-lab, import an oversized pool, and cut down to exactly 100. Confirm the Export tab was greyed with a "reach 100" hint before 100 and unlocks at 100; click the Export tab and confirm the page re-renders (server POST) with the Export panel + validation summary populated.
    4. Copy the Moxfield full list and paste it into Moxfield's import; copy the Archidekt full list and paste into Archidekt's import — confirm zero reformatting is needed (core value).
    5. Copy each dialect's CUT/ADD patch and confirm the CUT list names exactly the cards removed vs. your original import; make a what-if swap and confirm an ADD appears.
    6. Confirm the validation summary shows all three checks; if you seed an off-identity or banned card, confirm it is named as a warning but export is NOT disabled — and that a merely-unresolved card is labeled "could not verify," not "illegal."
    7. Eyeball the panel on desktop + mobile in at least two themes using the captured screenshots.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| test harness → app | Headless e2e drives the same public /cut-lab endpoints; no new runtime surface introduced by this plan |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-105-09 | Tampering | e2e admin-flag flip leaking into other specs | mitigate | Reuse the existing admin-lock (support/admin-lock) + setToolEnabled teardown pattern from cut-lab-whatif.spec.ts |
| T-105-SC | Tampering | package installs | mitigate | No npm/NuGet packages added (Playwright already present); nothing to audit |
</threat_model>

<verification>
- `dotnet test DeckFlow.Core.Tests` and `dotnet test DeckFlow.Web.Tests` green.
- `npx --no-install playwright test` full Cut Lab e2e green (no Windows-host browser).
- Theme × viewport screenshots captured for the Export panel.
- Human verification approved.
</verification>

<success_criteria>
- End-to-end: reach 100 → export → both-dialect full list + CUT/ADD patch + validation summary, all observed.
- All suites green; UI verified across themes and viewports; human sign-off recorded.
</success_criteria>

<output>
Create `.planning/workstreams/cut-lab/phases/105-builder-compatible-export/105-05-SUMMARY.md` when done.
</output>
