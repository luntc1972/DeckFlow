---
phase: 43-approval-status-safe-upsert
plan: 02
type: execute
wave: 2
depends_on: ["43-01"]
files_modified:
  - DeckFlow.Core.Tests/Content/ContentSiteIndexStoreApprovalTests.cs
autonomous: true
requirements: [REVQ-01, PUB-01, PUB-02]

must_haves:
  truths:
    - "An integration test proves the self-healing migration adds approval_status to a legacy schema"
    - "An integration test proves the grandfather backfill: visible legacy row -> 'approved', invisible legacy row -> 'pending'"
    - "An integration test proves UpsertContentColumnsOnlyAsync inserts a new row as 'pending'"
    - "An integration test proves UpsertContentColumnsOnlyAsync preserves all four admin fields on re-upsert (is_visible, is_hidden, is_evergreen, approval_status)"
    - "An integration test proves GetApprovedRowsAsync returns only approved rows"
    - "A reflection test proves both DDL constants declare approval_status TEXT NOT NULL DEFAULT 'pending'"
  artifacts:
    - path: "DeckFlow.Core.Tests/Content/ContentSiteIndexStoreApprovalTests.cs"
      provides: "Real-SQLite integration tests for the approval_status migration, safe upsert, and approved-only filter"
      contains: "ContentSiteIndexStoreApprovalTests"
      min_lines: 200
  key_links:
    - from: "ContentSiteIndexStoreApprovalTests"
      to: "ContentSiteIndexStore (real SQLite)"
      via: "per-fact temp .db file constructed like ContentSiteIndexStoreVisibilityTests"
      pattern: "new ContentSiteIndexStore"
---

<objective>
Add `DeckFlow.Core.Tests/Content/ContentSiteIndexStoreApprovalTests.cs` — real-SQLite integration tests that lock in the Plan 01 behavior: the self-healing migration + grandfather backfill, the safe content-only upsert (preserves all four admin fields, new rows land pending), the approved-only filtered read, and the DDL default. Mirrors the existing `ContentSiteIndexStoreVisibilityTests` pattern exactly.

Purpose: These tests are the goal-backward proof for SC1-SC4 and the security mitigations (T-43-02 idempotency, T-43-03 export filter). Without them the data-layer change is unverifiable.

Output: One new xUnit test class (DeckFlow.Core.Tests). No production code changes. No new packages, no new test framework.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/43-approval-status-safe-upsert/43-CONTEXT.md
@.planning/phases/43-approval-status-safe-upsert/43-PATTERNS.md

<interfaces>
<!-- Methods under test (added in Plan 01). -->
IContentSiteIndexStore:
  Task UpsertContentColumnsOnlyAsync(ContentSiteIndexRow row, CancellationToken ct = default);
  Task<IReadOnlyList<ContentSiteIndexRow>> GetApprovedRowsAsync(CancellationToken ct = default);
  // existing: EnsureSchemaAsync, GetByNaturalKeyAsync, SetVisibilityAsync, SetEvergreenAsync, SetHiddenAsync, GetAllRowsAsync

ContentSiteIndexRow.ApprovalStatus (string, defaults "pending") — added in Plan 01.

ContentSourceType.Youtube = "youtube_channel" (DeckFlow.Core/Knowledge/ContentModels.cs:183).
DDL CHECK allows only natural_key_type IN ('youtube_channel','podcast_rss').

Test seam (mirror DeckFlow.Core.Tests/Content/ContentSiteIndexStoreVisibilityTests.cs):
  - per-fact temp db: Path.Combine(Path.GetTempPath(), $"...-{Guid.NewGuid():N}.db")
  - Dispose(): SqliteConnection.ClearAllPools(); GC.Collect(); GC.WaitForPendingFinalizers(); File.Delete
  - namespace DeckFlow.Core.Tests; (NOT .Content)
  - ColumnExistsAsync(string) via PRAGMA table_info
  - GetPrivateSql(string fieldName) via reflection (BindingFlags.NonPublic | Static) for DDL-constant assertions
  - CreateYoutubeRow(...) static factory (copy from the visibility test class)
  - RelationalDatabaseConnection.FromSqlitePath(path).OpenConnectionAsync() for direct SQL setup
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write ContentSiteIndexStoreApprovalTests integration suite</name>
  <read_first>
    - DeckFlow.Core.Tests/Content/ContentSiteIndexStoreVisibilityTests.cs (entire file — ctor/Dispose @14-33, legacy-schema helper pattern @166-274, ColumnExistsAsync @277-293, GetPrivateSql @296-301, CreateYoutubeRow @303-323)
    - DeckFlow.Core/Content/ContentSiteIndexStore.cs (SqliteCreateTableSql + PostgresCreateTableSql field names for the reflection test; UpsertContentColumnsOnlySql; GetApprovedRowsAsync) — to know exact constant names
    - .planning/phases/43-approval-status-safe-upsert/43-PATTERNS.md (section "Test fixture pattern" + the 7 required tests, lines 337-413)
  </read_first>
  <action>
    Create `DeckFlow.Core.Tests/Content/ContentSiteIndexStoreApprovalTests.cs`, `namespace DeckFlow.Core.Tests;`, `public sealed class ContentSiteIndexStoreApprovalTests : IDisposable`. Copy the ctor/Dispose/temp-db pattern and the private helpers (`ColumnExistsAsync`, `GetPrivateSql`, `CreateYoutubeRow`) from `ContentSiteIndexStoreVisibilityTests` (the new class needs its own copies; do not refactor the existing class). Add a private `CreateLegacySchemaAsync()` helper that builds the OLD DDL by hand WITHOUT the approval_status column (copy the SqliteCreateTableSql column list but drop the approval_status line) and inserts seed rows via direct parameterized SQL through `RelationalDatabaseConnection.FromSqlitePath(_dbPath)`.

    Write these seven `[Fact]` tests (each `async Task`, use `Assert` constraint style consistent with the visibility test):
    1. `EnsureSchemaAsync_AddsApprovalStatusColumn_ToLegacySchema` — build legacy schema (no approval_status), `await _store.EnsureSchemaAsync()`, assert `await ColumnExistsAsync("approval_status")` is true.
    2. `EnsureSchemaAsync_Grandfather_SetsApprovedForVisibleRows_PendingForOthers` — into the legacy schema insert one row with is_visible=1 and one with is_visible=0, run `EnsureSchemaAsync`, then read both via `GetAllRowsAsync` (or `GetByNaturalKeyAsync`) and assert the visible row's `ApprovalStatus == "approved"` and the invisible row's `ApprovalStatus == "pending"`.
    3. `UpsertContentColumnsOnlyAsync_NewRow_LandsAsPending` — `await _store.UpsertContentColumnsOnlyAsync(CreateYoutubeRow("yt-new"))`, fetch via `GetByNaturalKeyAsync(ContentSourceType.Youtube, "yt-new")`, assert `ApprovalStatus == "pending"`.
    4. `UpsertContentColumnsOnlyAsync_ExistingRow_PreservesApprovalStatus` — upsert a row, set its status to 'approved' via direct UPDATE SQL, re-upsert the same natural key with a CHANGED title, assert `ApprovalStatus` is still `"approved"` AND `Title` reflects the new title (content updated, admin field preserved — D-04).
    5. `UpsertContentColumnsOnlyAsync_PreservesAllAdminFields` — upsert a row; set is_visible=true (`SetVisibilityAsync`), is_evergreen=true (`SetEvergreenAsync`), and approval_status='approved' (direct SQL); re-upsert with changed content; fetch and assert all of IsVisible==true, IsHidden==false, IsEvergreen==true, ApprovalStatus=="approved" preserved (D-03).
    6. `GetApprovedRowsAsync_ReturnsOnlyApprovedRows` — upsert three distinct-natural-key rows; set them to 'approved', 'pending', 'rejected' respectively via direct SQL; call `GetApprovedRowsAsync`; assert exactly one row returned and its natural key matches the approved one (T-43-03 proof).
    7. `CreateTableDdl_IncludesApprovalStatusDefault` — use `GetPrivateSql("SqliteCreateTableSql")` and `GetPrivateSql("PostgresCreateTableSql")` and assert both `Contains("approval_status TEXT NOT NULL DEFAULT 'pending'")`.

    All direct-SQL setup MUST use parameterized commands (`RelationalDatabaseConnection.AddParameter`) for any row values; the only inline literals allowed are the fixed status strings. Use `.ConfigureAwait(false)` consistent with the visibility test. For the Postgres column-presence case: do NOT attempt a live PG connection (no PG harness in CI); add a class-level XML-doc note stating PG migration coverage is deferred (CONTEXT deferred item + Discretion). Do not add a `[Fact(Skip=...)]` stub unless it documents that gap clearly.
  </action>
  <verify>
    <automated>"/mnt/c/Program Files/dotnet/dotnet.exe" build DeckFlow.Core.Tests/DeckFlow.Core.Tests.csproj 2>&1 | grep -E "Build succeeded|error|warning" | head</automated>
  </verify>
  <acceptance_criteria>
    - File `DeckFlow.Core.Tests/Content/ContentSiteIndexStoreApprovalTests.cs` exists, namespace `DeckFlow.Core.Tests`, class `public sealed class ContentSiteIndexStoreApprovalTests : IDisposable`.
    - All seven `[Fact]` methods named above are present (verify: `grep -c "\[Fact\]" DeckFlow.Core.Tests/Content/ContentSiteIndexStoreApprovalTests.cs` >= 7).
    - The file references the production methods under test: `grep -c "UpsertContentColumnsOnlyAsync\|GetApprovedRowsAsync" DeckFlow.Core.Tests/Content/ContentSiteIndexStoreApprovalTests.cs` >= 4.
    - `DeckFlow.Core.Tests` builds with 0 errors and 0 new warnings.
    - Targeted run passes (see verification block — run the class by filter; if VSTest is unreliable in WSL, capture the clean build + run via Windows dotnet.exe).
  </acceptance_criteria>
  <done>The seven integration tests compile and pass against the real ContentSiteIndexStore over SQLite, proving the migration, grandfather, safe-upsert preservation, new-row-pending, approved-only filter, and DDL default behaviors.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test setup SQL → temp SQLite | Test-only direct SQL builds legacy schemas and stamps statuses; isolated per-fact temp DB, deleted on Dispose |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-43-T1 | Tampering / Injection | Direct setup SQL in tests | mitigate (LOW) | Row values bound via AddParameter; only fixed status-string literals inlined. Mirrors the existing visibility test's setup discipline. |
| T-43-T2 | (verification of) Information Disclosure | GetApprovedRowsAsync filter | mitigate (HIGH→verified) | Test 6 is the executable proof of T-43-03 (no pending/rejected leak). This plan exists to make that mitigation enforceable. |
| T-43-T3 | (verification of) Data integrity | Grandfather idempotency | mitigate (MED→verified) | Test 2 proves grandfather mapping. (Optional: a second EnsureSchemaAsync call asserting no re-stamp is encouraged but the WHERE/guard in Plan 01 is the primary control.) |
| T-43-SC | Tampering | npm/pip/cargo installs | n/a | No package installs; no new test framework or NuGet (constraint). Legitimacy gate not applicable. |
</threat_model>

<verification>
- `dotnet build DeckFlow.Core.Tests/DeckFlow.Core.Tests.csproj` → 0 errors, 0 new warnings.
- Targeted test run (Windows dotnet.exe): `"/mnt/c/Program Files/dotnet/dotnet.exe" test DeckFlow.Core.Tests/DeckFlow.Core.Tests.csproj --filter "FullyQualifiedName~ContentSiteIndexStoreApprovalTests"` → all 7 pass. If VSTest is flaky in WSL, run via Windows dotnet and capture the pass count; note any environment caveat in the SUMMARY.
- `grep -c "\[Fact\]" DeckFlow.Core.Tests/Content/ContentSiteIndexStoreApprovalTests.cs` >= 7.
</verification>

<success_criteria>
- Seven real-SQLite integration tests cover: migration adds column (SC1), grandfather mapping (SC4), new-row-pending (SC4/D-04 INSERT), all-four-admin-field preservation (SC2/PUB-01), approved-only filter (SC3/PUB-02), DDL default.
- Tests build clean and pass; no production code changed; no new packages.
</success_criteria>

<output>
Create `.planning/phases/43-approval-status-safe-upsert/43-02-SUMMARY.md` when done.
</output>
