---
phase: 96-stated-rules-distiller
plan: 03
type: execute
wave: 2
depends_on: ["96-01"]
files_modified:
  - DeckFlow.Core/Knowledge/DistillationSchemas.cs
  - DeckFlow.Core/Knowledge/DistillationResults.cs
  - DeckFlow.Core/Knowledge/DistillationValidation.cs
  - DeckFlow.Core.Tests/DistillationPromptRegressionTests.cs
  - DeckFlow.Core.Tests/StatedRulesExtraction/ValidateStatedRulesTests.cs
autonomous: true
requirements: [CS-12, CS-13]
must_haves:
  truths:
    - "Four new constrained-decoding schemas + system prompts (Select/Disambiguate/Decompose/Reduce) exist and are byte-locked by a regression test"
    - "Every extracted rule validates against a strict allowlist-driven contract; malformed rules are sanitized out, contract violations throw"
    - "A range-comparator rule requires ValueMin<=ValueMax; a non-range comparator requires a single Value; a rule with unknown metric or missing provenance is rejected"
  artifacts:
    - path: "DeckFlow.Core/Knowledge/DistillationSchemas.cs"
      provides: "StatedRules Select/Disambiguate/Decompose/Reduce schemas + prompts"
      contains: "StatedRulesDecomposeSchema"
    - path: "DeckFlow.Core/Knowledge/DistillationValidation.cs"
      provides: "ValidateStatedRules + SanitizeStatedRules + payload records"
      contains: "ValidateStatedRules"
  key_links:
    - from: "ValidateStatedRules"
      to: "StatedRulesMetricVocabulary.Metrics / .Comparators"
      via: "allowlist membership check (mirrors ValidateTagDimension)"
      pattern: "StatedRulesMetricVocabulary"
---

<objective>
Add the fourth distill dimension — stated rules — as a constrained-decoding extension of the existing pipeline (CS-13), following the exact `schema + prompt + payload + Validate/Sanitize` shape used by summary/clips/tags. This plan defines the CONTRACTS the CLI service (96-04) implements against: 4 strict JSON schemas, 4 fresh system prompts, 4 result records, the raw payload records, and the allowlist-driven `ValidateStatedRules`/`SanitizeStatedRules` surface. The Claimify Select→Disambiguate→Decompose stages plus the DeckFlow-specific Reduce pass are encoded here as prompts/schemas (CS-12).

Purpose: interface-first — lock the schema/validation surface before any service or orchestrator code consumes it.
Output: extended DistillationSchemas/DistillationResults/DistillationValidation + a byte-exact regression test + a validation unit test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/96-stated-rules-distiller/96-CONTEXT.md
@.planning/phases/96-stated-rules-distiller/96-RESEARCH.md
@.planning/phases/96-stated-rules-distiller/96-PATTERNS.md
@.planning/phases/96-stated-rules-distiller/96-01-SUMMARY.md

<interfaces>
<!-- Locked JSON shapes. snake_case field names. All schemas additionalProperties:false. -->
<!-- video_date and card_grounded are NOT in any LLM schema — the orchestrator stamps video_date (D-04) and the grounding pass sets card_grounded (D-07). -->

StatedRulesSelectSchema  : {"claims": [string, ...]}  required:["claims"]
StatedRulesDisambiguateSchema : {"claims": [string, ...]}  required:["claims"]   (irreducibly-ambiguous sentences are OMITTED by the model = dropped, CS-12/D-01a)
StatedRulesDecomposeSchema / StatedRulesReduceSchema : {"rules": [ RULE, ... ]}  required:["rules"]
  RULE object (additionalProperties:false), required = all 10 keys:
    category                : string
    metric                  : string
    value                   : number|null
    value_min               : number|null
    value_max               : number|null
    comparator              : string   (gte|lte|eq|range)
    condition               : string|null
    clip_timestamp_seconds  : integer|null
    source_clip             : string
    confidence              : number

Result records (DistillationResults.cs), each wraps its payload + TokenUsage:
  sealed record SelectResult(IReadOnlyList<string> Claims, TokenUsage Usage)
  sealed record DisambiguateResult(IReadOnlyList<string> Claims, TokenUsage Usage)
  sealed record DecomposeResult(IReadOnlyList<StatedRuleCandidate> Rules, TokenUsage Usage)
  sealed record ReduceResult(IReadOnlyList<StatedRuleCandidate> Rules, TokenUsage Usage)

Payload records (DistillationValidation.cs, internal sealed, snake_case-mapped via existing JsonOpts):
  SelectPayload(IReadOnlyList<string> Claims)
  DisambiguatePayload(IReadOnlyList<string> Claims)
  StatedRulePayload(...)   — the 10 RULE keys above
  RulesPayload(IReadOnlyList<StatedRulePayload> Rules)   (shared by Decompose + Reduce)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Four StatedRules schemas + fresh system prompts + result records</name>
  <read_first>
    - DeckFlow.Core/Knowledge/DistillationSchemas.cs (ENTIRE file — mirror the `const string XSchema = """..."""` and `public static string XSystemPrompt { get; } = """..."""` idioms + the FormatAllowlist private helper)
    - DeckFlow.Core/Knowledge/StatedRulesExtraction/StatedRulesMetricVocabulary.cs (from 96-01 — interpolate Metrics + Comparators into the Decompose/Reduce prompts via FormatAllowlist)
    - DeckFlow.Core/Knowledge/DistillationResults.cs (SummaryResult/ClipsResult/TagsResult record idiom to mirror for the 4 new result records)
    - DeckFlow.Core.Tests/DistillationPromptRegressionTests.cs (byte-exact Assert.Equal fixture pattern to extend)
    - .planning/phases/96-stated-rules-distiller/96-PATTERNS.md (section "DistillationSchemas.cs" — CRITICAL raw-string carve-out; section "Golden Regression Test" Analog 1)
    - .planning/phases/96-stated-rules-distiller/96-RESEARCH.md (Claimify Method table lines 328-342 — stage input/output contracts; Anti-Patterns — write prompts FRESH, do not transcribe the paper, do not attribute Reduce to Claimify)
  </read_first>
  <action>
    Extend `DistillationSchemas.cs` with 4 `public const string` schemas (`StatedRulesSelectSchema`, `StatedRulesDisambiguateSchema`, `StatedRulesDecomposeSchema`, `StatedRulesReduceSchema`) matching the locked <interfaces> JSON exactly, using the existing `"""..."""` raw-string idiom. CRITICAL per CLAUDE.md carve-out: do NOT re-indent raw string literals — the exact whitespace ships to the LLM. Add 4 `public static string XSystemPrompt { get; } = """..."""` prompts written FRESH in the tone/constraint style of the existing SummarySystemPrompt/ClipsSystemPrompt (constrain to JSON-only output, ground only in transcript, never invent card names/numbers). The Select prompt strips unverifiable content (opinions/questions/instructions); Disambiguate resolves pronouns/vague refs using local context and OMITS anything irreducibly ambiguous (CS-12/D-01a); Decompose breaks into atomic measurable rules choosing `metric` ONLY from the allowlist (interpolate `FormatAllowlist(StatedRulesMetricVocabulary.Metrics)` and `.Comparators`); Reduce dedupes near-duplicate rules across chunks. Add a one-line code comment on the Reduce prompt that the reduce step is a DeckFlow addition, not part of Claimify (RESEARCH Anti-Patterns).
    Add the 4 result records to `DistillationResults.cs` per the locked <interfaces> shapes (add `using DeckFlow.Core.Knowledge.StatedRulesExtraction;` for StatedRuleCandidate).
    Extend `DistillationPromptRegressionTests.cs` with a `[Fact] StatedRulesPrompts_MatchShippedFixtures` asserting all 4 prompts + 4 schemas byte-exact via `const string expected... = """..."""; Assert.Equal(expected, DistillationSchemas.X)`.
  </action>
  <verify>
    <automated>dotnet.exe test DeckFlow.Core.Tests --filter FullyQualifiedName~DistillationPromptRegressionTests</automated>
  </verify>
  <acceptance_criteria>
    - All 4 schemas contain `"additionalProperties":false`; the Decompose/Reduce RULE object `required` array lists all 10 keys.
    - The Decompose and Reduce prompts contain the interpolated metric + comparator allowlists (grep shows a category token like `board-wipe` and a comparator token like `range` in the built prompt strings).
    - No LLM schema includes `video_date` or `card_grounded` (those are post-LLM fields).
    - `DistillationPromptRegressionTests` green (byte-exact lock).
    - `dotnet build DeckFlow.Core/DeckFlow.Core.csproj` clean, 0 new warnings; existing summary/clips/tags prompt fixtures still pass unchanged.
  </acceptance_criteria>
  <done>4 schemas + 4 fresh prompts + 4 result records exist and are byte-locked by regression; existing dimensions untouched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ValidateStatedRules + SanitizeStatedRules + payload records</name>
  <read_first>
    - DeckFlow.Core/Knowledge/DistillationValidation.cs (ENTIRE file — mirror ValidateTags/ValidateTagDimension throw-on-violation idiom, SanitizeClips null-coalesce+Where+Take idiom, the internal const bounds block, and the colocated payload-record idiom at file bottom + shared JsonOpts)
    - DeckFlow.Core/Knowledge/StatedRulesExtraction/StatedRuleCandidate.cs (96-01 — the sanitized output type)
    - DeckFlow.Core/Knowledge/StatedRulesExtraction/StatedRulesMetricVocabulary.cs (96-01 — Metrics + Comparators allowlists)
    - .planning/phases/96-stated-rules-distiller/96-PATTERNS.md (section "DistillationValidation.cs" — exact Validate/Sanitize checks + payload idiom)
    - .planning/phases/96-stated-rules-distiller/96-RESEARCH.md (section "value/Band Handling" lines 396-419 — the exact comparator/band consistency rules; "Recency/Provenance" — fail closed on missing video_date)
  </read_first>
  <behavior>
    - Comparator "range" requires ValueMin and ValueMax non-null with ValueMin<=ValueMax; Value must be null.
    - Comparator gte|lte|eq requires Value non-null; ValueMin and ValueMax must both be null.
    - metric not in StatedRulesMetricVocabulary.Metrics → throw; comparator not in .Comparators → throw.
    - Confidence outside [0.0,1.0] → throw; empty/whitespace SourceClip → throw; default(DateTimeOffset) VideoDateUtc → throw (fail closed on unknown provenance).
    - SanitizeStatedRules drops structurally-broken payload rows (never throws), stamps VideoDateUtc from the caller, defaults CardGrounded=null, and caps output at MaxStatedRulesPerVideo.
  </behavior>
  <action>
    Add to `DistillationValidation.cs`: `internal const int MaxStatedRulesPerVideo` (e.g. 40) alongside the existing bounds; the payload records `SelectPayload`, `DisambiguatePayload`, `StatedRulePayload`, `RulesPayload` at the file bottom (snake_case-mapped) per <interfaces>; `internal static IReadOnlyList<StatedRuleCandidate> SanitizeStatedRules(RulesPayload? payload, DateTimeOffset videoDateUtc)` following the SanitizeClips null-coalesce→Where→Take idiom (map each StatedRulePayload → StatedRuleCandidate, stamping VideoDateUtc, CardGrounded=null; drop rows failing basic structural checks; `.Take(MaxStatedRulesPerVideo)`); and `internal static void ValidateStatedRules(IReadOnlyList<StatedRuleCandidate> rules)` mirroring ValidateTagDimension — throw `InvalidOperationException` for each violation in the <behavior> list, using `StatedRulesMetricVocabulary.Metrics.Contains(...)` / `.Comparators.Contains(...)` for allowlist membership. Add a `// Why:` comment on the range/single-value branch and on the fail-closed video_date check.
    Create `DeckFlow.Core.Tests/StatedRulesExtraction/ValidateStatedRulesTests.cs` (note: DistillationValidation is `internal` — DeckFlow.Core.Tests already has InternalsVisibleTo; confirm the existing DistillationValidationTests or equivalent uses it and follow that access pattern).
  </action>
  <verify>
    <automated>dotnet.exe test DeckFlow.Core.Tests --filter FullyQualifiedName~ValidateStatedRulesTests</automated>
  </verify>
  <acceptance_criteria>
    - ValidateStatedRulesTests covers every <behavior> branch: valid range rule passes; range with ValueMin>ValueMax throws; gte with null Value throws; unknown metric throws; unknown comparator throws; confidence 1.5 throws; empty SourceClip throws; default VideoDateUtc throws.
    - A SanitizeStatedRules test proves a malformed payload row is dropped (not thrown) and output is capped at MaxStatedRulesPerVideo.
    - `dotnet build DeckFlow.Core/DeckFlow.Core.csproj` clean, 0 new warnings.
  </acceptance_criteria>
  <done>Validate throws on every locked violation; Sanitize defensively drops+caps+stamps; tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM JSON → persisted rule | Model output (CLI has NO tooling-enforced schema conformance — post-hoc parse only) crosses into DeckFlow types |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-96-01 | Tampering | prompt injection via transcript → out-of-schema/malicious rule JSON | mitigate | additionalProperties:false schemas + post-hoc allowlist ValidateStatedRules (metric/comparator are CLOSED allowlists like ContentTagVocabulary, never free-text); Sanitize drops non-conforming rows before Validate |
| T-96-04 | DoS (cost) | adversarial transcript inflating rule count | mitigate | MaxStatedRulesPerVideo cap in SanitizeStatedRules bounds fan-out; reduce+dedupe (96-01) upstream |
| T-96-05 | Tampering | control chars in source_clip/condition | mitigate | Rules are rendered later only via JsonSerializer/Quote (96-05); this plan stores them as validated string content, never as path segments or raw SQL |
| T-96-SC | Tampering | package installs | accept | No new packages (System.Text.Json BCL only) — legitimacy gate N/A |
</threat_model>

<verification>
- `dotnet build DeckFlow.Core/DeckFlow.Core.csproj` clean.
- `dotnet.exe test DeckFlow.Core.Tests --filter FullyQualifiedName~"DistillationPromptRegressionTests|ValidateStatedRulesTests"` green.
- Existing summary/clips/tags regression fixtures unchanged.
</verification>

<success_criteria>
The stated-rules dimension's schema/prompt/validation contract exists, is byte-locked, and enforces the D-02 band shape + D-02a metric allowlist + fail-closed provenance — ready for the CLI service (96-04) and orchestrator (96-07) to consume.
</success_criteria>

<output>
Create `.planning/phases/96-stated-rules-distiller/96-03-SUMMARY.md` when done.
</output>
