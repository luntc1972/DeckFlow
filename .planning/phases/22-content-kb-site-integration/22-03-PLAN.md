---
phase: 22-content-kb-site-integration
plan: 03
type: execute
wave: 2
depends_on: ["22-01"]
files_modified:
  - DeckFlow.Web/Services/ContentArtifactParser.cs
  - DeckFlow.Web/Services/ContentKbSeedLoader.cs
  - DeckFlow.Web/Services/IContentKbSeedLoader.cs
  - DeckFlow.Web/Controllers/ContentKbController.cs
  - DeckFlow.Web/Models/ContentKbBrowseViewModel.cs
  - DeckFlow.Web/Models/ContentKbDetailViewModel.cs
  - DeckFlow.Web/Views/ContentKb/Index.cshtml
  - DeckFlow.Web/Views/ContentKb/Detail.cshtml
  - DeckFlow.Web/Views/ContentKb/_ViewStart.cshtml
  - DeckFlow.Web/Views/Shared/_Layout.cshtml
  - DeckFlow.Web/wwwroot/ts/content-kb.ts
  - DeckFlow.Web/wwwroot/css/site-common.css
  - DeckFlow.Web/Program.cs
  - .planning/phases/22-content-kb-site-integration/22-03-SUMMARY.md
autonomous: true
requirements: [KB-08, KB-09]
must_haves:
  truths:
    - "GET /content-kb returns 503 maintenance when content.kb.enabled is OFF, and a hub-card grid of PUBLISHED-only entries when ON"
    - "GET /content-kb/{key} renders the artifact (Markdig) with a Copy-for-ChatGPT button yielding clean frontmatter-stripped text; returns 404 when the entry is hidden or missing"
    - "Render startup ensures the content_site_index schema and idempotently loads the committed seed without clobbering admin curation"
    - "Client-side facet filter (source/archetype/bracket/card_category + text search) shows/hides cards; both empty states render; all surfaces work at 375px with zero theme bleed"
  artifacts:
    - path: "DeckFlow.Web/Controllers/ContentKbController.cs"
      provides: "flag-gated public browse + detail with path-traversal-guarded artifact serving"
      contains: "FeatureFlagGate"
    - path: "DeckFlow.Web/Services/ContentKbSeedLoader.cs"
      provides: "reads index-seed.json, calls UpsertRowPreservingVisibilityAsync per row"
      contains: "UpsertRowPreservingVisibilityAsync"
    - path: "DeckFlow.Web/wwwroot/ts/content-kb.ts"
      provides: "client-side multi-facet filter + clear-filters"
  key_links:
    - from: "ContentKbController.Index"
      to: "IContentSiteIndexStore.GetPublishedRowsAsync"
      via: "server query is_visible=true"
      pattern: "GetPublishedRowsAsync"
    - from: "ContentKbController.Detail"
      to: "artifact file under runtime_artifact_base"
      via: "Path.GetFullPath prefix check + SplitHeader strip"
      pattern: "StartsWith"
    - from: "Program.cs startup"
      to: "ContentKbSeedLoader.LoadIfPresentAsync"
      via: "after EnsureSchemaAsync, before RunAsync"
      pattern: "LoadIfPresent"
---

<objective>
Ship the public Content KB surface: a flag-gated browse page (published-only hub-card grid + client-side faceted filter + empty states), a per-entry artifact detail page (Markdig render + Copy-for-ChatGPT), the startup seed-load that materializes the index on Render without clobbering admin curation, and the conditional nav link.

Purpose: This is the primary deliverable — it serves the project's core value (one-round-trip ChatGPT paste). It depends only on the Plan 01 store contracts.

Output: ContentKbController (public) + seed loader + 2 views + filter TS + public CSS additions + Program.cs wiring + conditional nav link.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-content-kb-site-integration/22-CONTEXT.md
@.planning/phases/22-content-kb-site-integration/22-UI-SPEC.md
@.planning/phases/22-content-kb-site-integration/22-RESEARCH.md

<read_first>
- 22-UI-SPEC.md Surface 1 (public browse) + Surface 2 (detail) — authoritative for routes, classes, copy, ARIA, responsive breakpoints, empty states. Honor the Checker Disposition appendix: reused weights 400/600/700 and the chip paddings that mirror existing siblings ARE intentional — do not "normalize" them.
- DeckFlow.Web/Infrastructure/FeatureFlagGateAttribute.cs — `[FeatureFlagGate("content.kb.enabled", Title=..., Message=...)]`. Returns HTTP 503 + maintenance view when OFF (NOT 404 — UI-SPEC's "404" wording is inaccurate per RESEARCH Pitfall 5; 503 is correct behavior). Apply to BOTH Index and Detail actions.
- DeckFlow.Web/Services/HelpContentService.cs:66-88 — `SplitHeader(raw)` frontmatter splitter + the Markdig `Pipeline` config (`UseAdvancedExtensions().DisableHtml()`). Extract SplitHeader into a NEW static `ContentArtifactParser` so both this controller and Plan 04 can reuse it; reuse the same Markdig pipeline config for rendering the body.
- ANALOG (copy button TS): DeckFlow.Web/wwwroot/ts/card-lookup.ts — `attachDynamicCopyButton` (data-copy-target pattern → navigator.clipboard.writeText, idle/copied/failed states). Reuse it for the detail page copy button (data-copy-target="kb-artifact-text").
- ANALOG (controller + view placement): DeckFlow.Web/Controllers/DeckController.cs + Views/Deck/*.cshtml — public tool controller/view conventions, `_ViewStart.cshtml`.
- DeckFlow.Web/Program.cs:423-452 — the startup EnsureSchemaAsync block (harvest/analytics stores) BEFORE `app.RunAsync()` (line 452). Insert content-index EnsureSchemaAsync + seed-load here. `IContentSiteIndexStore` must be registered (singleton, matching IFeedbackStore/ICategoryKnowledgeStore) via `DeckFlowDatabaseConnectionFactory.CreateContentSiteIndexConnection` (provider-aware — Postgres on Render, SQLite local).
- DeckFlow.Web/Services/DeckFlowDatabaseConnectionFactory.cs — `CreateContentSiteIndexConnection` (provider selection).
- DeckFlow.Web/wwwroot/css/site-common.css — existing `.hub-card`, `.hub-grid`, `.help-prose`, `.help-breadcrumb`, `.page-shell`, `.page-frame`, `.page-lede`, `.maintenance-page__action`, `.copy-button`/`.run-button`, df-select rules. Add ONLY `.kb-filter-bar`, `.kb-tag`, `.kb-empty`, `.kb-artifact-prose` here. NEVER add KB rules to site.css or any site-*.css theme fork (SC5).
- Plan 02 SUMMARY `runtime_artifact_base:` — the exact directory the artifact files live in at runtime (e.g. /app/content-kb). Bind the controller's path resolution base to THIS value (do not guess ContentRootPath/.. vs ContentRootPath).
- DeckFlow.Core/Content/SlugifySourceName.cs — `Slugify(name)` for building/parsing the `{source-slug}--{video_id}` URL key.
- DeckFlow.Core/Content/IContentSiteIndexStore.cs (after Plan 01) — GetPublishedRowsAsync, GetByNaturalKeyAsync (now carries IsVisible), UpsertRowPreservingVisibilityAsync.
- DeckFlow.Web/Views/Shared/_Layout.cshtml — the main nav `<ul>`; add the conditional "Knowledge Base" link.
</read_first>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ContentArtifactParser + seed loader + Program.cs startup wiring + DI registration</name>
  <files>DeckFlow.Web/Services/ContentArtifactParser.cs, DeckFlow.Web/Services/IContentKbSeedLoader.cs, DeckFlow.Web/Services/ContentKbSeedLoader.cs, DeckFlow.Web/Program.cs</files>
  <action>
    Create `ContentArtifactParser` (static class) with `public static (IReadOnlyDictionary<string,string> Header, string Body) SplitHeader(string raw)` — move the logic from HelpContentService.SplitHeader (handles `---` delimiters, CRLF/LF, returns header dict + body). Leave HelpContentService working (either have it call the new helper or keep its private copy — prefer delegating to avoid divergence, but a duplicate is acceptable if touching HelpContentService risks R-6 drift; document the choice).

    Create IContentKbSeedLoader + ContentKbSeedLoader (singleton): `Task LoadIfPresentAsync(IWebHostEnvironment env, CancellationToken = default)`. Resolve the seed path from the SAME runtime base recorded in Plan 02's SUMMARY (e.g. Path.Combine(runtimeArtifactBase, "seed", "index-seed.json")). If the file is absent, log info and return (non-fatal — local dev or first deploy). If present, deserialize the JSON array (System.Text.Json, camelCase) and for EACH entry call `IContentSiteIndexStore.UpsertRowPreservingVisibilityAsync` (NOT UpsertRowAsync — preserves admin curation, Pitfall 1). Build each ContentSiteIndexRow from the seed fields; set YoutubeVideoId/RssGuid from naturalKeyType. Log "Content KB seed load complete: N rows".

    In Program.cs: register `IContentSiteIndexStore` as a singleton using `CreateContentSiteIndexConnection` (mirror IFeedbackStore registration) and register `IContentKbSeedLoader` as a singleton. In the startup block at ~lines 423-452 (after the harvest/analytics EnsureSchemaAsync calls, before app.RunAsync), add: log + `await app.Services.GetRequiredService<IContentSiteIndexStore>().EnsureSchemaAsync();` + `await app.Services.GetRequiredService<IContentKbSeedLoader>().LoadIfPresentAsync(app.Environment);`. Also log `app.Environment.ContentRootPath` + whether the runtime artifact base exists (one info line) to make Pitfall 4 diagnosable in deploy logs.
  </action>
  <verify>
    <automated>"/mnt/c/Program Files/dotnet/dotnet.exe" build /mnt/c/users/chrislunt/source/personal/deckflow/DeckFlow.Web/DeckFlow.Web.csproj -c Debug 2>&1 | grep -E "error|Build succeeded"</automated>
  </verify>
  <done>Build clean; seed loader uses UpsertRowPreservingVisibilityAsync; Program.cs registers the store + loader and calls EnsureSchemaAsync + LoadIfPresentAsync at startup; ContentRootPath + base existence logged.</done>
</task>

<task type="auto">
  <name>Task 2: Public ContentKbController (browse + detail) with path-traversal-guarded artifact serving</name>
  <files>DeckFlow.Web/Controllers/ContentKbController.cs, DeckFlow.Web/Models/ContentKbBrowseViewModel.cs, DeckFlow.Web/Models/ContentKbDetailViewModel.cs</files>
  <action>
    Create ContentKbController ([Route("content-kb")], sealed). Both actions carry `[FeatureFlagGate("content.kb.enabled", Title="Knowledge Base unavailable", Message="The Knowledge Base is not currently available.")]`.

    Index() [HttpGet("")]: call `store.GetPublishedRowsAsync()`, project to ContentKbBrowseViewModel (list of entries with title, source, video_url, tags, and the URL key `{Slugify(source)}--{video_id}`). View(model).

    Detail(string key) [HttpGet("{key}")]: parse key into (sourceSlug, videoId) by splitting on the LAST `--` (video IDs are URL-safe and contain no `--`; if no `--`, treat whole as natural_key_value). Resolve the row via `store.GetByNaturalKeyAsync(ContentSourceType.Youtube/Podcast, videoId)` — determine key type from the value or try youtube first then podcast. If row is null OR `row.IsVisible == false` → return NotFound() (info disclosure guard — hidden entries 404 BEFORE reading any file). Resolve the artifact path defensively (Pitfall 3): `var baseDir = Path.GetFullPath(<runtime_artifact_base from Plan 02>); var resolved = Path.GetFullPath(Path.Combine(baseDir, row.ArtifactPath));` then if `!resolved.StartsWith(baseDir + Path.DirectorySeparatorChar, StringComparison.OrdinalIgnoreCase)` → NotFound() (never log/echo the resolved path). If `!File.Exists(resolved)` → return View("Detail", new ContentKbDetailViewModel { ArtifactUnavailable = true, ... }) (UI-SPEC "temporarily unavailable" copy; copy button hidden). Read file, `ContentArtifactParser.SplitHeader` → body; render body via the Markdig pipeline → HtmlString; set `CleanBodyText = body` (the frontmatter-stripped text for the hidden textarea). View model also carries breadcrumb title, source name + external url, published date, bracket, archetype. View(model).

    ViewModels are sealed records or classes with `{ get; init; }` (R-6). ContentKbDetailViewModel includes: Title, SourceName, SourceUrl, PublishedDisplay, Bracket, Archetype, RenderedHtml (HtmlString), CleanBodyText, ArtifactUnavailable (bool).
  </action>
  <verify>
    <automated>"/mnt/c/Program Files/dotnet/dotnet.exe" build /mnt/c/users/chrislunt/source/personal/deckflow/DeckFlow.Web/DeckFlow.Web.csproj -c Debug 2>&1 | grep -E "error|Build succeeded"</automated>
  </verify>
  <done>Build clean; both actions flag-gated; Detail 404s on hidden/missing rows BEFORE file read; path-traversal prefix check present; clean text computed for copy.</done>
</task>

<task type="auto">
  <name>Task 3: Browse + Detail views, content-kb.ts filter, public CSS, conditional nav link</name>
  <files>DeckFlow.Web/Views/ContentKb/Index.cshtml, DeckFlow.Web/Views/ContentKb/Detail.cshtml, DeckFlow.Web/Views/ContentKb/_ViewStart.cshtml, DeckFlow.Web/wwwroot/ts/content-kb.ts, DeckFlow.Web/wwwroot/css/site-common.css, DeckFlow.Web/Views/Shared/_Layout.cshtml</files>
  <action>
    Build all three surfaces to the UI-SPEC contract exactly (copy, classes, ARIA from Surface 1/2 + Copywriting + Accessibility sections).

    Index.cshtml: `.page-shell` + `.page-frame`; H1 "Knowledge Base"; `.page-lede` lede copy; `.kb-filter-bar` `<fieldset>` with `<legend class="sr-only">Filter knowledge base entries</legend>` containing the search `<input type="search" aria-label="Search entries">` + four df-select dropdowns (Source/Archetype/Bracket/Card category) populated from ContentTagVocabulary + distinct sources, each first option "Any …". `.hub-grid` of `.hub-card` per entry (title `<a>` to /content-kb/{key}, `.hub-card__description` = source · bracket · archetype, decorative `.kb-tag` chips `aria-hidden="true"` with `data-archetype`/`data-bracket`/`data-card-category`/`data-source` attributes for the filter). Both empty-state blocks (`.kb-empty` for zero-published — copy A; and the no-match block with "Clear filters" `.maintenance-page__action` button — copy B), and a visually-hidden `role="status" aria-live="polite"` match-count element.

    Detail.cshtml: `.help-breadcrumb` "Knowledge Base / {title}"; metadata header line (`var(--muted)`, source links external `rel="noopener noreferrer"`); sticky toolbar with the Copy button (markup from UI-SPEC: `class="copy-button run-button" data-copy-target="kb-artifact-text" aria-label="Copy this artifact for ChatGPT"`) + hidden `<textarea id="kb-artifact-text" readonly hidden>@Model.CleanBodyText</textarea>`; `.kb-artifact-prose` panel rendering `@Model.RenderedHtml`. If `Model.ArtifactUnavailable`, render the "temporarily unavailable" copy and HIDE the copy button.

    content-kb.ts (under ~60 lines, no deps): on DOMContentLoaded, read filter controls; on input/change (text search debounced 200ms, Escape clears search), AND across the four facets + text, toggle each `.hub-card` `display` (do not remove from DOM); update the aria-live match count "N entries shown"; wire the "Clear filters" button to reset controls to "Any"/empty and re-run; reuse `attachDynamicCopyButton` for the detail copy button (or ensure the detail page loads card-lookup.ts's exported helper). It compiles via the existing MSBuild TypeScript target (output to wwwroot/js).

    site-common.css: add ONLY `.kb-filter-bar`, `.kb-tag`, `.kb-empty`, `.kb-artifact-prose` per the UI-SPEC specs (chip paddings as given — intentional per Checker Disposition). Consume existing tokens only; no hex.

    _Layout.cshtml: inject `IFeatureFlagCache` and render `<li><a asp-controller="ContentKb" asp-action="Index">Knowledge Base</a></li>` ONLY when `FeatureFlagCache.IsEnabled("content.kb.enabled")`. Touch only the nav insertion + the @inject line (R-6).
  </action>
  <verify>
    <automated>"/mnt/c/Program Files/dotnet/dotnet.exe" build /mnt/c/users/chrislunt/source/personal/deckflow/DeckFlow.Web/DeckFlow.Web.csproj -c Debug 2>&1 | grep -E "error|Build succeeded"; grep -c "\.kb-" /mnt/c/users/chrislunt/source/personal/deckflow/DeckFlow.Web/wwwroot/css/site.css 2>/dev/null | grep -q '^0$' && echo "NO_KB_IN_SITE_CSS"</automated>
  </verify>
  <done>Build clean (TS compiles); views match UI-SPEC copy/classes/ARIA; both empty states present; filter shows/hides cards; nav link conditional; zero `.kb-` rules in site.css.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>The public Content KB browse + detail pages behind the content.kb.enabled flag, with client-side faceting and copy-for-ChatGPT.</what-built>
  <how-to-verify>
    Ask the user to start the dev server (do not auto-launch — see MEMORY). Then:
    1. Flag OFF (default): GET /content-kb returns the 503 maintenance page (NOT a crash); no "Knowledge Base" nav link is shown.
    2. Enable content.kb.enabled via /Admin/Flags. Reload /content-kb: hub-card grid of published entries appears (publish a couple via the admin grid from Plan 04, or temporarily publish via SQL for this check). Nav link now visible.
    3. Type in the search box and change each facet dropdown — cards show/hide live; match-count announces; "Clear filters" resets.
    4. Click an entry → detail page renders the artifact (summary/clips/tags); click "Copy for ChatGPT" → clipboard contains clean text WITHOUT frontmatter (`---` block absent).
    5. Resize to 375px (DevTools): grid single-column, filter controls full-width, copy button full-width, touch targets >=44px; visually confirm guild theme switch (e.g. Rakdos vs Azorius) shows no broken/leaked KB styling.
    6. Negative: hit /content-kb/{key} for a hidden entry → 404; hit a bogus key → 404.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues (screenshots at 375px appreciated for the UI gate).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| anonymous client → public controller | untrusted URL key + flag state |
| DB artifact_path → filesystem read | path could escape the content-kb root |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-08 | Information Disclosure | hidden entries reachable via direct detail URL | mitigate | Detail() returns 404 when row null OR IsVisible==false, BEFORE any file read |
| T-22-09 | Tampering | path traversal via artifact_path (`../../appsettings.json`) | mitigate | store ValidateArtifactPath at write + controller Path.GetFullPath prefix check; 404 silently on escape; never echo path |
| T-22-10 | Information Disclosure | flag-off route leaks data via direct URL | mitigate | FeatureFlagGate on BOTH actions → 503 maintenance when OFF |
| T-22-11 | Tampering | seed-load clobbers admin curation | mitigate | loader uses UpsertRowPreservingVisibilityAsync (Plan 01) |
| T-22-12 | Information Disclosure | copy button serves raw frontmatter | mitigate | server-side SplitHeader strip; CleanBodyText holds body only |
</threat_model>

<verification>
- DeckFlow.Web builds clean; content-kb.ts compiles to wwwroot/js.
- Zero theme bleed: `grep -rl "\.kb-" DeckFlow.Web/wwwroot/css/site.css DeckFlow.Web/wwwroot/css/site-*.css` returns NOTHING (KB classes live only in site-common.css).
- Flag gate present on both actions: `grep -c "FeatureFlagGate" DeckFlow.Web/Controllers/ContentKbController.cs` → expect >= 2.
- Path guard present: `grep -c "StartsWith" DeckFlow.Web/Controllers/ContentKbController.cs` → expect >= 1.
- Human-verify checkpoint passed (browse/detail/copy/375px/negative cases).
</verification>

<success_criteria>
- Public browse lists published-only entries with client-side facets + both empty states (KB-08; SC2).
- Detail page renders the artifact via Markdig with a clean-text Copy-for-ChatGPT (KB-08; SC2/SC3).
- Startup materializes the index + idempotent curation-preserving seed load (KB-08; SC1).
- Flag-gated (503 OFF), 375px responsive, zero theme bleed (KB-09; SC5).
</success_criteria>

<output>
Create `.planning/phases/22-content-kb-site-integration/22-03-SUMMARY.md` when done.
</output>
