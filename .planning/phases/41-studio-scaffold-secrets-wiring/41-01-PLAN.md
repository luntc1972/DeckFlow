---
phase: 41-studio-scaffold-secrets-wiring
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - DeckFlow.Studio/DeckFlow.Studio.csproj
  - DeckFlow.Studio/Program.cs
  - DeckFlow.Studio/StudioConfig.cs
  - DeckFlow.Studio/Components/Pages/Home.razor
  - DeckFlow.Studio/Properties/launchSettings.json
  - DeckFlow.Studio/appsettings.json
  - DeckFlow.Studio/appsettings.Development.json
  - .gitignore
  - Dockerfile
  - DeckFlow.sln
autonomous: false
requirements: [STU-01, STU-02, STU-03]
user_setup:
  - service: dotnet-user-secrets
    why: "Prod Postgres connection string + Render SSH target must live in the OS user-secrets store, never the repo"
    env_vars: []
    dashboard_config:
      - task: "Run dotnet user-secrets set for Studio:ProdConnectionString and Studio:RenderSshAddress"
        location: "Local shell — operator runs once; values never committed"

must_haves:
  truths:
    - "Operator runs `dotnet run --project DeckFlow.Studio` and reaches a rendered Blazor Server page at http://localhost:5271"
    - "The prod Postgres connection string has exactly one safe home: the OS user-secrets store (dotnet user-secrets), never a tracked file"
    - "The Studio startup log states 'configured'/'not configured' only — the connection string value never appears in any log line, UI text, or error message"
    - "The prod connection string value never enters DI or any UI-reachable object — StudioConfig carries only a presence bool"
    - "`dotnet build DeckFlow.sln` succeeds with Studio present, with ZERO new package references; existing Core/Web test suites still pass"
    - "`dotnet restore DeckFlow.Web/DeckFlow.Web.csproj` (the Dockerfile path) is unchanged and does not pull in Studio"
  artifacts:
    - path: "DeckFlow.Studio/DeckFlow.Studio.csproj"
      provides: "net10.0 Microsoft.NET.Sdk.Web project referencing DeckFlow.Core, carrying the <UserSecretsId> GUID, ZERO PackageReference entries"
      contains: "net10.0"
    - path: "DeckFlow.Studio/Program.cs"
      provides: "Composition root: built-in console logging, AddUserSecrets, StudioConfig singleton (presence bool only), app.Logger secret-presence logging, Blazor Server services"
      contains: "AddUserSecrets"
    - path: "DeckFlow.Studio/StudioConfig.cs"
      provides: "Singleton record carrying ONLY IsProdConfigured bool (presence flag — never the connection string value)"
      contains: "IsProdConfigured"
    - path: "DeckFlow.Studio/Components/Pages/Home.razor"
      provides: "Stub home page rendering 'Studio is running.' + prod connection presence status"
      contains: "@page \"/\""
    - path: "DeckFlow.Studio/Properties/launchSettings.json"
      provides: "Single http profile, localhost:5271, launchBrowser false"
      contains: "http://localhost:5271"
    - path: ".gitignore"
      provides: "Studio secret-capable file patterns ignored before first git add"
      contains: "DeckFlow.Studio/secrets.json"
  key_links:
    - from: "DeckFlow.Studio/DeckFlow.Studio.csproj"
      to: "DeckFlow.Core/DeckFlow.Core.csproj"
      via: "ProjectReference"
      pattern: "ProjectReference Include=\"..\\\\DeckFlow.Core"
    - from: "DeckFlow.Studio/Program.cs"
      to: "user-secrets store"
      via: "AddUserSecrets<Program>() then Configuration[\"Studio:ProdConnectionString\"] (local var only)"
      pattern: "Studio:ProdConnectionString"
    - from: "DeckFlow.Studio/Components/Pages/Home.razor"
      to: "StudioConfig"
      via: "injected singleton, reads IsProdConfigured"
      pattern: "Config\\.IsProdConfigured"
---

<objective>
Scaffold the `DeckFlow.Studio` standalone Blazor Server project, wire `dotnet user-secrets` as the only safe home for the prod Postgres connection string, and harden `.gitignore` so no secret or local-config artifact can ever enter git history. Studio binds to localhost only, references `DeckFlow.Core`, adds ZERO new packages, and is excluded from the Render container build.

Purpose: This is the first phase of v1.7 — secrets have no safe home until `.gitignore` + user-secrets are wired, and no Studio feature work can begin until the project exists in the solution and builds. Closes STU-01, STU-02, STU-03.

Output: A buildable `DeckFlow.Studio` project on disk and in `DeckFlow.sln`, a hardened `.gitignore`, a documented Dockerfile constraint, and a stub Home page proving the app runs and reports secret-presence status.

Logging note: Phase 41 uses ONLY ASP.NET Core's built-in console logging (the default provider in `Microsoft.NET.Sdk.Web`). Serilog is intentionally NOT used this phase — `Serilog.AspNetCore` / `Serilog.Sinks.Console` are referenced only by DeckFlow.Web and do not flow transitively through DeckFlow.Core, so wiring them would require new package references (forbidden by CLAUDE.md no-new-packages) and would not compile. Full Serilog file-sink wiring is deferred to a later v1.7 phase (Phase 45), matching RESEARCH.md Open Question 2 resolution: console-only in Phase 41.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/41-studio-scaffold-secrets-wiring/41-RESEARCH.md
@.planning/phases/41-studio-scaffold-secrets-wiring/41-PATTERNS.md
@./CLAUDE.md

<interfaces>
<!-- Analog property block — copy these four exactly into the Studio csproj. From DeckFlow.Web/DeckFlow.Web.csproj lines 34-41. -->
<!-- Studio takes ONLY these four PropertyGroup entries plus the Core ProjectReference. Strip everything else (TypeScript targets, Markdig/Polly/RestSharp/Swashbuckle/Serilog PackageReferences, ZipDeckFlowBridge, GenerateDocumentationFile). -->

DeckFlow.Web/DeckFlow.Web.csproj (PropertyGroup analog):
  <TargetFramework>net10.0</TargetFramework>
  <Nullable>enable</Nullable>
  <ImplicitUsings>enable</ImplicitUsings>

DeckFlow.Web/DeckFlow.Web.csproj (ProjectReference analog, line 4):
  <ProjectReference Include="..\DeckFlow.Core\DeckFlow.Core.csproj" />

Built-in logging (NOT Serilog) — Microsoft.Extensions.Logging console provider, present by default in Microsoft.NET.Sdk.Web:
  // No `using Serilog;`, no Serilog package reference, no builder.Host.UseSerilog(...).
  // Default console logging is already registered by WebApplication.CreateBuilder(args).
  // Emit the presence line AFTER `var app = builder.Build();` via `app.Logger.LogInformation(...)`.
  // 41-PATTERNS.md shows a Serilog snippet — that snippet is superseded by this built-in pattern for Phase 41.

DeckFlow.Web/Properties/launchSettings.json (http profile analog, lines 11-19):
  "http": { "commandName": "Project", "dotnetRunMessages": true, "launchBrowser": false, "applicationUrl": "http://localhost:5173", ... }
  // Studio: single http profile only, port 5271, no IIS/https profiles, no iisSettings block
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Studio project, retarget to net10.0, reference Core, harden .gitignore — IN THIS ORDER</name>
  <files>DeckFlow.Studio/DeckFlow.Studio.csproj, DeckFlow.Studio/appsettings.json, DeckFlow.Studio/appsettings.Development.json, .gitignore, DeckFlow.sln</files>
  <read_first>
    - DeckFlow.Web/DeckFlow.Web.csproj (csproj analog — copy ONLY the four PropertyGroup props + Core ProjectReference; strip everything else per 41-PATTERNS.md)
    - .gitignore (current 27 lines — append-only; the gap to close)
    - .planning/phases/41-studio-scaffold-secrets-wiring/41-PATTERNS.md (Critical Pre-Commit Checklist steps 1-5; exact .gitignore entries)
    - .planning/phases/41-studio-scaffold-secrets-wiring/41-RESEARCH.md (Pitfall 1 net6.0 trap; Pitfall 2 gitignore gap; Open Question 2 console-only logging)
  </read_first>
  <action>
Execute in this exact order — the sequence is the security control, not incidental:

1. Run `dotnet new blazorserver --output DeckFlow.Studio --name DeckFlow.Studio` from the repo root. This template defaults to net6.0 on this SDK (confirmed via dry-run) and generates demo files (`Data/`, `Pages/Counter.razor`, `Pages/FetchData.razor`, `Shared/SurveyPrompt.razor`, `Pages/Index.razor`) — those are deleted in Task 2.

2. IMMEDIATELY rewrite `DeckFlow.Studio/DeckFlow.Studio.csproj` to the minimal shape: `<Project Sdk="Microsoft.NET.Sdk.Web">` with a single PropertyGroup containing exactly `<TargetFramework>net10.0</TargetFramework>`, `<Nullable>enable</Nullable>`, `<ImplicitUsings>enable</ImplicitUsings>` (the `<UserSecretsId>` is added in step 4), and a single ItemGroup with `<ProjectReference Include="..\DeckFlow.Core\DeckFlow.Core.csproj" />`. Strip ALL template `<PackageReference>` blocks — every dependency arrives transitively via Core (no new NuGet packages, per CLAUDE.md). In particular do NOT add `Serilog.AspNetCore` or `Serilog.Sinks.Console`: they are referenced only by DeckFlow.Web, do not flow through Core, and are not in `Microsoft.NET.Sdk.Web` — adding them is a forbidden new package, and using their APIs without the reference would not compile. Strip any `<TargetFramework>net6.0</TargetFramework>`, TypeScript targets, `ZipDeckFlowBridge`, and `<GenerateDocumentationFile>`. Setting net10.0 BEFORE `dotnet sln add` and the first build avoids `error NU1202: Package DeckFlow.Core ... is not compatible with net6.0` (Pitfall 1).

3. IMMEDIATELY append to repo-root `.gitignore`, before any `git add` touches Studio files (Pitfall 2 — secret-leak prevention is order-dependent), a new named section with exactly these three patterns: `DeckFlow.Studio/appsettings*.local.json`, `DeckFlow.Studio/appsettings.Local.json`, `DeckFlow.Studio/secrets.json`. Add a comment noting `appsettings.Development.json` IS committed (logging only) and these patterns cover secret-capable files. Touch only appended lines — do NOT reformat existing entries (CLAUDE.md: preserve LF, edit only needed lines).

4. Run `dotnet user-secrets init --project DeckFlow.Studio/DeckFlow.Studio.csproj`. This writes a `<UserSecretsId>` GUID into the csproj PropertyGroup — the GUID is public and safe to commit. Do NOT set any secret VALUES in this task (operator does that per the human checkpoint).

5. Run `dotnet sln DeckFlow.sln add DeckFlow.Studio/DeckFlow.Studio.csproj`. The .sln now has 6 projects.

6. Trim template `appsettings.json` and `appsettings.Development.json` so they contain ONLY logging config — assert no connection string, password, or `Host=` appears in either. These two files ARE committed.

Per STU-02 / D (STATE.md "Studio binds to localhost only"). Use the Windows dotnet path `"/mnt/c/Program Files/dotnet/dotnet.exe"` for the build verification command in WSL.
  </action>
  <verify>
    <automated>"/mnt/c/Program Files/dotnet/dotnet.exe" build DeckFlow.sln 2>&1 | grep -E "Build succeeded|error" ; grep -c "net10.0" DeckFlow.Studio/DeckFlow.Studio.csproj ; grep -c "UserSecretsId" DeckFlow.Studio/DeckFlow.Studio.csproj ; grep -c "PackageReference\|Serilog" DeckFlow.Studio/DeckFlow.Studio.csproj || echo "0 package refs / no Serilog in csproj" ; grep -F "DeckFlow.Studio/secrets.json" .gitignore ; grep -ivE '^#' DeckFlow.Studio/appsettings.json DeckFlow.Studio/appsettings.Development.json | grep -ciE "postgres|password|Host=" || echo "0 secrets in appsettings"</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet build DeckFlow.sln` reports `Build succeeded` with 0 errors and 0 new warnings.
    - `grep net10.0 DeckFlow.Studio/DeckFlow.Studio.csproj` matches; `grep net6.0` matches nothing.
    - The csproj contains exactly one `<ProjectReference>` (to DeckFlow.Core) and zero `<PackageReference>` entries — `grep -c "PackageReference" DeckFlow.Studio/DeckFlow.Studio.csproj` is 0; `grep -i Serilog DeckFlow.Studio/DeckFlow.Studio.csproj` returns nothing.
    - The csproj contains a `<UserSecretsId>` GUID.
    - `.gitignore` contains the three Studio patterns including `DeckFlow.Studio/secrets.json`.
    - `grep -ivE '^#' DeckFlow.Studio/appsettings.json DeckFlow.Studio/appsettings.Development.json | grep -iE "postgres|password|Host="` returns nothing (no secrets in committed config).
    - `DeckFlow.sln` lists the DeckFlow.Studio project (6 total).
  </acceptance_criteria>
  <done>Studio project exists on disk and in DeckFlow.sln, targets net10.0, references Core only, carries zero PackageReference entries (no Serilog) and a UserSecretsId GUID; .gitignore blocks Studio secret-capable files; committed appsettings files carry zero secrets; `dotnet build DeckFlow.sln` succeeds.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Wire Program.cs composition root (built-in logging), presence-only StudioConfig, stub Home page, launchSettings, and Dockerfile constraint comment</name>
  <files>DeckFlow.Studio/Program.cs, DeckFlow.Studio/StudioConfig.cs, DeckFlow.Studio/Components/Pages/Home.razor, DeckFlow.Studio/Properties/launchSettings.json, Dockerfile</files>
  <read_first>
    - DeckFlow.Web/Program.cs lines 1-65 (startup shape analog — copy the CreateBuilder/Build/RunAsync structure, but use BUILT-IN logging not Serilog; ignore the UseSerilog/Log.* lines)
    - DeckFlow.Web/Properties/launchSettings.json (http profile shape — Studio takes one profile, port 5271, launchBrowser false)
    - Dockerfile lines 21-29 (restore is project-scoped DeckFlow.Web/DeckFlow.Web.csproj — must stay unchanged; SC4)
    - .planning/phases/41-studio-scaffold-secrets-wiring/41-PATTERNS.md (Program.cs section; Home.razor stub; launchSettings; Dockerfile comment-only mod) — NOTE: the PATTERNS Serilog snippet is superseded for Phase 41; use built-in console logging per the objective's logging note and RESEARCH Open Question 2.
  </read_first>
  <action>
1. Delete the template demo files left by `dotnet new blazorserver`: `DeckFlow.Studio/Data/` (WeatherForecast*), `DeckFlow.Studio/Pages/Counter.razor`, `DeckFlow.Studio/Pages/FetchData.razor`, `DeckFlow.Studio/Shared/SurveyPrompt.razor`, and the template `Pages/Index.razor`. They add noise and no value.

2. Create `DeckFlow.Studio/StudioConfig.cs`: a `public sealed record StudioConfig(bool IsProdConfigured)` in `namespace DeckFlow.Studio;` with a `/// <summary>` doc comment. STU-03 / HIGH-2: the record carries ONLY the presence bool — it must NOT carry the connection string value. The raw connection string never enters DI or any UI-reachable object. Use Allman braces, file-scoped namespace, LF line endings (project conventions; new Studio files use LF because `.gitattributes` enforces LF — the CRLF-for-new-files default does NOT apply in this repo).

3. Rewrite `DeckFlow.Studio/Program.cs` as the composition root, mirroring the DeckFlow.Web/Program.cs startup STRUCTURE (CreateBuilder → configure → Build → map → RunAsync) but using BUILT-IN ASP.NET Core logging only — NO `using Serilog;`, NO `builder.Host.UseSerilog(...)`, NO static `Log.*` calls. Shape: `namespace DeckFlow.Studio; public partial class Program` with `public static async Task Main(string[] args)`. Inside the `try`:
   - build `WebApplication.CreateBuilder(args)` (this already registers the default console logging provider from `Microsoft.NET.Sdk.Web` — do not add a logging package);
   - wire `builder.Configuration.AddUserSecrets<Program>().AddEnvironmentVariables()` (env vars override user-secrets for CI compatibility);
   - read `builder.Configuration["Studio:ProdConnectionString"]` into a LOCAL variable only (HIGH-2: it stays a local, never stored in DI or any object) and compute `var isProdConfigured = !string.IsNullOrEmpty(prodConnStr);`;
   - register `builder.Services.AddSingleton(new StudioConfig(isProdConfigured));` (presence bool only — the connection string is NOT passed to the record);
   - register Blazor Server via `builder.Services.AddRazorPages()` + `builder.Services.AddServerSideBlazor()` (do NOT add MVC `AddControllersWithViews`);
   - `var app = builder.Build();`
   - AFTER build, emit exactly `app.Logger.LogInformation("Studio prod connection: {Status}", isProdConfigured ? "configured" : "not configured");` (MEDIUM-1: use `app.Logger`, not a static logger). STU-03: never pass the connection string value (or any `PostgresConnectionStringNormalizer.Normalize()` output) to any logger;
   - map Blazor (`MapBlazorHub` + `MapFallbackToPage("/_Host")` or the template's component routing), then `await app.RunAsync();`.
   Wrap the whole Main body in `try { ... } catch (Exception exception) { ... }` where the catch logs the fatal error and then RETHROWS so a failed startup exits non-zero (MEDIUM-2 — do NOT swallow). Because there is no host logger available in the catch on a failed build, log via `Console.Error.WriteLine($"DeckFlow Studio host terminated: {exception}");` then `throw;` (re-throw preserves the stack and the non-zero exit code). No Serilog `Log.CloseAndFlushAsync()` finally block is needed (built-in console logging flushes on shutdown).

4. Create `DeckFlow.Studio/Components/Pages/Home.razor` (or `Pages/Home.razor` matching the template's routing layout) with `@page "/"`, a `<PageTitle>DeckFlow Studio</PageTitle>`, an `<h1>DeckFlow Studio</h1>`, a `<p>Studio is running.</p>`, and a paragraph rendering `Prod connection: <strong>@(_isProdConfigured ? "configured" : "not configured")</strong>`. In `@code`, `[Inject] private StudioConfig Config { get; set; } = default!;` and set `_isProdConfigured = Config.IsProdConfigured;` in `OnInitialized()`. STU-03 / HIGH-2: render the presence flag only — StudioConfig has no connection string value to expose.

5. Create `DeckFlow.Studio/Properties/launchSettings.json` with a single `http` profile: `commandName: Project`, `dotnetRunMessages: true`, `launchBrowser: false` (MEMORY: never auto-launch — operator opens the URL), `applicationUrl: http://localhost:5271` (localhost bind only — no 0.0.0.0, no LAN exposure per T-41-03; port 5271 verified free), `ASPNETCORE_ENVIRONMENT: Development`. No IIS/https profiles, no iisSettings block.

6. Edit `Dockerfile` line 29: insert a two-line comment directly above `RUN dotnet restore DeckFlow.Web/DeckFlow.Web.csproj` documenting the SC4 constraint — that restore stays project-scoped and must NOT become `dotnet restore DeckFlow.sln` (Studio is local-only, not deployed; solution-level restore would pull Blazor into the container build). Comment-only; do not change the RUN command's behavior. Touch only the inserted lines (CLAUDE.md: edit only needed lines, preserve LF).

Use `"/mnt/c/Program Files/dotnet/dotnet.exe"` for build/run verification commands in WSL.
  </action>
  <verify>
    <automated>"/mnt/c/Program Files/dotnet/dotnet.exe" build DeckFlow.sln 2>&1 | grep -E "Build succeeded|error" ; grep -c "AddUserSecrets" DeckFlow.Studio/Program.cs ; grep -c "using Serilog\|UseSerilog\|Log\.Information\|Log\.Fatal" DeckFlow.Studio/Program.cs || echo "0 Serilog usage" ; grep -c "app.Logger.LogInformation" DeckFlow.Studio/Program.cs ; grep -c "throw;" DeckFlow.Studio/Program.cs ; grep -niE "Studio:ProdConnectionString" DeckFlow.Studio/Program.cs ; grep -ni "LogInformation\|LogDebug\|LogWarning\|LogError" DeckFlow.Studio/Program.cs | grep -iE "ConnectionString}|prodConnStr|{Conn" || echo "no conn-string in log templates" ; grep -c "ProdConnectionString" DeckFlow.Studio/StudioConfig.cs || echo "0 conn-string in StudioConfig (presence-only)" ; grep -F "http://localhost:5271" DeckFlow.Studio/Properties/launchSettings.json ; grep -c "dotnet restore DeckFlow.sln" Dockerfile ; grep -c "dotnet restore DeckFlow.Web/DeckFlow.Web.csproj" Dockerfile</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet build DeckFlow.sln` reports `Build succeeded`, 0 errors, 0 new warnings.
    - `Program.cs` contains NO `using Serilog`, NO `UseSerilog`, NO static `Log.*` calls — `grep -E "using Serilog|UseSerilog|Log\.(Information|Fatal)" DeckFlow.Studio/Program.cs` returns nothing.
    - `Program.cs` contains `AddUserSecrets`, reads `Studio:ProdConnectionString` into a local, and emits the presence line via `app.Logger.LogInformation(...)` with only the literal `"configured"`/`"not configured"` — no log/UI line interpolates the connection string value.
    - The Main catch block rethrows (`grep -c "throw;" DeckFlow.Studio/Program.cs` ≥ 1) so a failed startup exits non-zero; the failure is not swallowed.
    - `StudioConfig.cs` exists with `IsProdConfigured` and NO `ProdConnectionString` member (`grep -c "ProdConnectionString" DeckFlow.Studio/StudioConfig.cs` is 0); `Home.razor` injects `StudioConfig` and renders the presence flag only.
    - `launchSettings.json` has a single `http` profile, `applicationUrl: http://localhost:5271`, `launchBrowser: false`, and no IIS/https/iisSettings.
    - Dockerfile still contains `RUN dotnet restore DeckFlow.Web/DeckFlow.Web.csproj` and does NOT contain `dotnet restore DeckFlow.sln`; the added text is comment-only.
    - Template demo files (`Data/`, `Counter.razor`, `FetchData.razor`, `SurveyPrompt.razor`, template `Index.razor`) are deleted.
  </acceptance_criteria>
  <done>`dotnet build DeckFlow.sln` succeeds with zero new packages; Studio has a working composition root using built-in console logging, user-secrets wiring (connection string kept as a local), a presence-only StudioConfig singleton, a stub Home page showing presence-only status, a rethrowing fatal-error catch, a localhost-only launch profile on port 5271, and a Dockerfile comment locking the project-scoped restore.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Operator sets secrets, runs Studio, and verifies SC1-SC4 + no-leak</name>
  <what-built>
A standalone `DeckFlow.Studio` Blazor Server project: net10.0, references Core, zero new packages, in DeckFlow.sln, with user-secrets wired, a localhost:5271 launch profile, built-in console secret-presence-only logging, a presence-only StudioConfig (no connection string in DI/UI), and a hardened .gitignore. The connection string has exactly one safe home (user-secrets); committed files carry no secrets. SC4 keeps the Dockerfile restore project-scoped.
  </what-built>
  <how-to-verify>
Claude has automated everything it can (build clean is green). This checkpoint is the operator-run verification of SC1-SC4 and the no-secret-leak guarantee, because they require running the server, inspecting the OS secret store, and confirming runtime log output.

1. SET SECRETS (one-time; values never committed):
   `dotnet user-secrets set "Studio:ProdConnectionString" "<your prod Postgres connection string>" --project DeckFlow.Studio/DeckFlow.Studio.csproj`
   `dotnet user-secrets set "Studio:RenderSshAddress" "<srv-xxx@ssh.region.render.com>" --project DeckFlow.Studio/DeckFlow.Studio.csproj`

2. SC2 — secrets live only in user-secrets:
   `dotnet user-secrets list --project DeckFlow.Studio/DeckFlow.Studio.csproj`
   Expect both keys listed. Confirm no appsettings file in the project tree contains the connection string.

3. SC1 — Studio runs and renders:
   `dotnet run --project DeckFlow.Studio`
   Open `http://localhost:5271` in a browser. Expect a page reading "Studio is running." and "Prod connection: configured".

4. SC3 — no secret leak (run from repo root, TRACKED files only):
   `git log --all -- "**/secrets.json"` → expect EMPTY (no commits).
   `git grep -niE "postgres|password|Host=" -- DeckFlow.Studio/` → expect EMPTY. (Use `git grep` / `git ls-files`, NOT `grep -r`, so only git-tracked files are scanned and gitignored/local/generated files do not produce false positives.)

5. STU-03 — no secret in logs:
   In the `dotnet run` startup output, confirm the line reads `Studio prod connection: configured` (emitted via the built-in console logger) and the connection string value appears NOWHERE in stdout.

6. SC4 — Docker restore isolation:
   `dotnet restore DeckFlow.Web/DeckFlow.Web.csproj` → succeeds; Studio packages do not appear in restore output.

If all six pass, the scaffold is correct and secrets are safely homed.
  </how-to-verify>
  <resume-signal>Type "approved" if SC1-SC4 + no-leak all pass, or describe which check failed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo working tree → git history | Files staged with `git add` cross into version control; a secret committed here is permanently exposed in the public `luntc1972/DeckFlow` repo |
| application config → log/UI output | Connection string read from user-secrets must never cross into any log line, UI text, or error message |
| application config → DI / UI scope | The raw connection string stays a local in Program.cs; it is never stored in a DI-registered object (StudioConfig carries a presence bool only), keeping it out of Blazor component scope |
| Studio process → network | Studio binds a listening socket; localhost-only bind keeps it off the LAN |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-41-01 | Information Disclosure | `.gitignore` + git working tree (secrets.json, appsettings*.local.json) | mitigate | Task 1 adds `.gitignore` entries (`DeckFlow.Studio/secrets.json`, `appsettings*.local.json`, `appsettings.Local.json`) BEFORE any `git add`; all secret VALUES live only in the OS user-secrets store via `dotnet user-secrets`; SC3 verified by `git log --all -- "**/secrets.json"` (empty) + `git grep -niE "postgres|password|Host=" -- DeckFlow.Studio/` (empty, tracked files only). Committed appsettings files asserted secret-free in Task 1 verify. |
| T-41-02 | Information Disclosure | `DeckFlow.Studio/Program.cs` logging + `StudioConfig` + `Home.razor` UI | mitigate | Task 2 logs only the literal `"configured"`/`"not configured"` via `app.Logger.LogInformation("Studio prod connection: {Status}", ...)`; the connection string is read into a LOCAL variable, used only to compute the presence bool, and never stored in DI/StudioConfig or rendered in the UI (presence flag only). Any `PostgresConnectionStringNormalizer.Normalize()` output is likewise never logged. STU-03 / HIGH-2. |
| T-41-03 | Elevation of Privilege | `DeckFlow.Studio/Properties/launchSettings.json` bind address | mitigate | Task 2 sets `applicationUrl: http://localhost:5271` — localhost bind only, no `0.0.0.0` / LAN exposure (STATE.md decision "Studio binds to localhost only"). Single-operator local tool; no auth needed (ASVS V2/V4 N/A). |
| T-41-SC | Tampering | npm/NuGet installs | accept | No new packages installed this phase — all deps arrive transitively via the Core `<ProjectReference>`; logging uses the built-in `Microsoft.NET.Sdk.Web` console provider (Serilog explicitly NOT added) (RESEARCH Package Legitimacy Audit: no new packages; CLAUDE.md no-new-deps rule). Task 1 verify asserts zero `<PackageReference>` and no Serilog in the Studio csproj. No legitimacy checkpoint required. |
</threat_model>

<verification>
Phase-level checks (run after both auto tasks, before the human checkpoint):
- `"/mnt/c/Program Files/dotnet/dotnet.exe" build DeckFlow.sln` → Build succeeded, 0 errors, 0 new warnings, ZERO new package references.
- Existing regression suites unchanged: `"/mnt/c/Program Files/dotnet/dotnet.exe" test DeckFlow.Core.Tests/DeckFlow.Core.Tests.csproj` and `... DeckFlow.Web.Tests/DeckFlow.Web.Tests.csproj` (VSTest unreliable in WSL — push-and-watch CI is the fallback gate per project constraints).
- SC4 isolation: `"/mnt/c/Program Files/dotnet/dotnet.exe" restore DeckFlow.Web/DeckFlow.Web.csproj` succeeds; Studio not pulled in.
- SC1/SC2/SC3/STU-03: operator-run in Task 3 checkpoint.
</verification>

<success_criteria>
- SC1: `dotnet run --project DeckFlow.Studio` starts a Blazor Server app reachable at `http://localhost:5271` with a first page rendered. [Task 3 manual]
- SC2: `dotnet user-secrets list --project DeckFlow.Studio` is the only place the prod Postgres connection string can be stored; no appsettings file in the project tree contains it. [Task 1 build-side assert + Task 3 manual]
- SC3: `git log --all -- "**/secrets.json"` returns no commits; `git grep -niE "postgres|password|Host=" -- DeckFlow.Studio/` returns nothing in tracked files. [Task 3 manual]
- SC4: `dotnet build DeckFlow.sln` succeeds with Studio present and zero new packages; `dotnet restore DeckFlow.Web/DeckFlow.Web.csproj` (Dockerfile path) is unchanged and does not pull in Studio. [Task 1/2 build + verification]
</success_criteria>

<output>
Create `.planning/phases/41-studio-scaffold-secrets-wiring/41-01-SUMMARY.md` when done.
</output>
