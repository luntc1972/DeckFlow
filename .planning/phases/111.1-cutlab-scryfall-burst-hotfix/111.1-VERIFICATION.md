---
phase: 111.1-cutlab-scryfall-burst-hotfix
verified: 2026-07-31T00:00:00Z
status: passed
score: 9/9 must-haves verified
overrides_applied: 0
---

# Phase 111.1: Cut Lab Scryfall Burst Hotfix (INSERTED) Verification Report

**Phase Goal:** A Cut Lab "Import pool" of a normal Commander pool resolves without tripping
Scryfall rate limiting and without rendering an error banner, by cutting the redundant per-miss
Scryfall call and by not failing the whole import closed on a transient 429.

**Verified:** 2026-07-31
**Status:** passed
**Re-verification:** No — initial verification

## Method

This is goal-backward, code-first verification. For every SUMMARY.md claim below, the actual
source at HEAD (`16f746ea`, worktree `/mnt/c/users/chrislunt/source/personal/deckflow-cutlab-scryfall`,
branch `fix/cutlab-scryfall-burst`) was read directly and, where a claim was testable, independently
re-executed (build + targeted filter + a from-scratch mutation test on the B-1 fix), rather than
accepted from the SUMMARY narrative.

## Goal Achievement

### Observable Truths / Success Criteria

| # | Truth (Success Criterion) | Status | Evidence |
|---|---|---|---|
| SC-1 | Redundant per-miss `cards/collection` POST removed | ✓ VERIFIED | `CutLabAnalysisContextBuilder.cs:443` dispatches `_cardResolver.SearchFallbackCardAsync(name, ct)`, not `ResolveSingleAsync`, in the chunk-loop fallback delegate — confirmed by direct read. Independently re-ran `ResolvePoolCardsAsync_BatchMiss_DispatchesSearchFallbackNotResolveSingle` and `ResolvePoolCardsAsync_BatchMiss_IssuesExactlyOneLiveCallPerMiss` (real `ScryfallCardResolver` with counting delegate overrides): `collectionCalls==1`, `searchCalls==3`, `ResolveSingleCalls==0`. `ScryfallCardResolver.cs` and `ScryfallReferenceResolver.cs` are untouched by plan 01's commits (`git log -- <path>` shows only plan 03's `d2fac7321` touching them) — the two other `ResolveSingleAsync` callers (`CutLabSimulationService.cs`, `ManabaseAnalysisService.cs`) are provably unaffected. |
| SC-2 | Transient 429 during pool intake fails open (no user-facing banner) | ✓ VERIFIED | `CutLabAnalysisContextBuilder.cs:467`: `if (!failOpenOnLookupErrors && !IsTransientRateLimit(exception)) { throw; }`, with `IsTransientRateLimit` (line 530) matching `HttpRequestException { StatusCode: HttpStatusCode.TooManyRequests }`, covering both a per-card fallback 429 and a batch-collection 429 (`ScryfallReferenceCollectionException` derives from `HttpRequestException`). Non-429 (503) still throws — verified by the permanent guard test `..._NonRateLimitDuringFallback_StillThrows`, which is required to stay green before AND after the fix. |
| SC-3 | 429 banner locked as unreachable end-to-end at `ProcessAsync` | ✓ VERIFIED | End-to-end tests in `CutLabPageServiceTests.cs` (`ProcessAsync_ScryfallRateLimitsDuringFallback_ImportSucceedsWithoutBanner`, `ProcessAsync_ScryfallRateLimitsOnBatchCollection_ImportSucceedsWithoutBanner`) assert `result.ErrorMessage` is null/empty on a 429 on either route, using a legitimate 101-card pool (inside `CutLabPoolValidator`'s 101-150 window, so pool-size validation cannot manufacture a false pass). Inverse locks (`..._ScryfallServerErrorDuringFallback_StillRendersBanner`, `..._ScryfallServerErrorOnBatchCollection_StillRendersBanner`) assert the EXACT, route-correct 503 strings still populate `ErrorMessage`, proving the fix did not blanket-suppress upstream errors. All 8 tests present and independently re-run GREEN (see Spot-Checks below). |
| SC-4 | Punctuation-tolerant second pass over the batch response + ADR 0004 | ✓ VERIFIED | `ScryfallReferenceResolver.cs:152-193` inserts an additive second pass between the raw match-back loop and the fallback loop: groups still-unmatched request names and still-unclaimed cards by `BatchMatchKey` (trim/lowercase/delete `[^\p{L}\p{N}\s/]`/collapse whitespace — `/` deliberately preserved), acts only on 1:1 unambiguous keys, writes `FromFallback: false` keyed by the original request name. Zero new `await`/network calls in the added block (confirmed by direct read). `docs/decisions/0004-scryfall-batch-match-key-asymmetry.md` exists with `## Context` / `## Decision` / `## Consequences`, evaluates all four options, cites the live-probe evidence. |
| SC-5 | `NormalizeForScryfall` xmldoc corrected | ✓ VERIFIED | `ScryfallCardResolver.cs:222-234`: the false "so DFC cards resolve on the first /cards/collection attempt" claim is gone; replaced with the measured truth (normalization changes only the submitted identifier, never the match key; neither slash form resolves at `cards/collection`) and a `<remarks>` cross-referencing ADR 0004. `git diff -U0` on this file (via `git show d2fac7321 -- ...ScryfallCardResolver.cs`) touches only `///` lines. |
| SC-6 | Full Web + Core suite gate | ✓ VERIFIED | Independently re-ran (not just re-read the SUMMARY): `dotnet build -c Release` → 0 warnings/0 errors. Targeted phase filter → 280/280 passed. `dotnet test DeckFlow.Web.Tests` → 2072 passed / 16 skipped (pre-existing Postgres-integration skips, unrelated) / 0 failed. Matches `111.1-GATES.md` Section 3b exactly. Core suite previously confirmed 1843/1843 by the orchestrator; not re-run here to save time but nothing in this phase touches `DeckFlow.Core`. |
| SC-7 | `ScryfallThrottle.MinInterval` pacing corrected 200ms → 500ms | ✓ VERIFIED | `ScryfallThrottle.cs:21`: `private static readonly TimeSpan MinInterval = TimeSpan.FromMilliseconds(500);`. Both stale "10 req/sec" claims corrected (class summary line 9, inline comment lines 13-20) with a citation to `https://scryfall.com/docs/api/rate-limits`. Regression test `ExecuteAsync_SpacesConsecutiveCallsByAtLeastTheDocumentedPerEndpointLimit` (asserts a >=450ms lower bound only, correctly avoiding a flaky upper-bound assertion on process-wide static state) independently re-run GREEN. `RetryAfterCap`/`FallbackRetryDelay`/`MaxRetryAttempts` untouched, as scoped. |
| B-1 | Rate-limited pool not cached as Scryfall-confirmed misses | ✓ VERIFIED (mutation-tested independently) | `PrimeResolvedCardsCache` (`CutLabAnalysisContextBuilder.cs:315-331`) subtracts `_unattemptedNamesByPoolKey[poolKey]` from the caller-supplied unresolved-names list before it reaches the 30-minute cache. The blind `TryGetCachedResolvedCards` short-circuit was removed from `CutLabPageService.ResolveEntriesAsync` (confirmed: that call no longer exists there; the two OTHER legitimate callers, `CutLabExportService.cs` and `CutLabWhatifService.cs`, are untouched). **Independently reproduced the mutation test**: temporarily disabled the subtraction in `PrimeResolvedCardsCache`, rebuilt, re-ran `ProcessAsync_SecondImportAfterSwallowedRateLimit_DoesNotPoisonKnownMissingCache` — observed RED (`Assert.True() Failure` at line 561, matching the SUMMARY's claimed line/message exactly), then restored the file byte-for-byte (`git diff` empty) and confirmed GREEN again. This directly answers the verification-stance instruction to hunt for a test that "claimed to lock the fix but went green without it" — it did not; the fix is load-bearing. |
| W-1 | Degraded imports visible to the user | ✓ VERIFIED | `BuildUnresolvedCardsWarning` (`CutLabPageService.cs:538-555`) names up to 5 unresolved cards with an "and N more" overflow, no HTTP/Scryfall internals. Wired into `ProcessAsync` on the SUCCESS path (`warnings.Add(...)` before `BuildAsync`, not inside `Error(...)`). Full-contract tests (`ProcessAsync_DegradedImportWarning_NamesUpToFiveCardsWithOverflowOnSuccessResult`, `ProcessAsync_FullyResolvedPool_HasNoDegradedImportWarning`) assert the overflow cap, the `HasResult=true`/no-`ErrorMessage` shape, and the inverse (no warning when nothing is unresolved) — read directly and confirmed present/passing in the independent test run. |

**Score:** 9/9 truths verified (SC-1 through SC-7, B-1, W-1)

### Required Artifacts

| Artifact | Expected | Status | Details |
|---|---|---|---|
| `DeckFlow.Web/Services/CutLab/CutLabAnalysisContextBuilder.cs` | SC-1/SC-2/B-1 fixes | ✓ VERIFIED | Delegate swap, `IsTransientRateLimit`, `_unattemptedNamesByPoolKey`, post-loop unattempted diff, `PrimeResolvedCardsCache` subtraction — all present and read directly at cited line numbers. |
| `DeckFlow.Web/Services/CutLab/CutLabPageService.cs` | B-1 short-circuit removal + W-1 warning | ✓ VERIFIED | `ResolveEntriesAsync` unconditionally calls `ResolvePoolCardsAsync`; `BuildUnresolvedCardsWarning` present and wired on the success path. |
| `DeckFlow.Web/Services/Packets/ScryfallReferenceResolver.cs` | SC-4 second pass | ✓ VERIFIED | `BatchMatchKey`-gated additive second pass present, zero network calls in the added block. |
| `DeckFlow.Web/Services/Scryfall/ScryfallCardResolver.cs` | SC-5 xmldoc fix | ✓ VERIFIED | Corrected xmldoc + `<remarks>` cross-referencing ADR 0004. |
| `DeckFlow.Web/Services/Scryfall/ScryfallThrottle.cs` | SC-7 pacing fix | ✓ VERIFIED | `MinInterval` = 500ms; both stale comments corrected. |
| `docs/decisions/0004-scryfall-batch-match-key-asymmetry.md` | SC-4 ADR | ✓ VERIFIED | Context/Decision/Consequences present, 4 options evaluated. |
| `DeckFlow.Web.Tests/CutLabAnalysisContextBuilderTests.cs`, `CutLabPageServiceTests.cs`, `ScryfallReferenceResolverTests.cs`, `ScryfallThrottleTests.cs` | Regression locks | ✓ VERIFIED | All named test methods present at cited line numbers; targeted filter independently re-run 280/280 green. |
| `.planning/phases/111.1-cutlab-scryfall-burst-hotfix/111.1-GATES.md` | Criterion map + gate results | ✓ VERIFIED | Sections 1-3b present; Section 3b figures (280/280, 2072/16/2088, 1843/1843) match this verifier's independent re-run. |
| `.planning/phases/111.1-cutlab-scryfall-burst-hotfix/111.1-PACING-MEASUREMENT.md` | SC-7 measurement precondition | ✓ VERIFIED | Steps 1-6 present with per-flow census, latency formula, highest-volume-caller finding, concurrency + two-sided timeout census, recommendation. |

### Key Link Verification

| From | To | Via | Status | Details |
|---|---|---|---|---|
| `CutLabAnalysisContextBuilder.ResolveCardsAsync` | `IScryfallCardResolver.SearchFallbackCardAsync` | fallback delegate in `ResolveBatchAsync` call | ✓ WIRED | `CutLabAnalysisContextBuilder.cs:443`. |
| `CutLabAnalysisContextBuilder.ResolveCardsAsync` catch | `HttpStatusCode.TooManyRequests` | `IsTransientRateLimit` predicate | ✓ WIRED | Line 467 guard; predicate at 530. |
| `CutLabPageService.ProcessAsync` | `CutLabProcessResult.ErrorMessage` | `catch (HttpRequestException) -> UpstreamErrorMessageBuilder.BuildScryfallMessage` | ✓ WIRED, and provably unreachable for 429 | `CutLabPageService.cs:229-233`; end-to-end tests assert both directions. |
| `ScryfallReferenceResolver.ResolveBatchAsync` second pass | `response.Data.Data` (already received) | `BatchMatchKey` grouping, 1:1 only | ✓ WIRED | No new HTTP calls in the added block; ambiguous keys correctly fall through to `fallbackStrategy`. |
| `ScryfallThrottle.MinInterval` | Scryfall's documented 500ms per-endpoint limit | constant + cited comment | ✓ WIRED | Independently re-run pacing test confirms >=450ms spacing. |

### Behavioral Spot-Checks (independently executed by this verifier, not taken from SUMMARY)

| Behavior | Command | Result | Status |
|---|---|---|---|
| Clean build | `dotnet build DeckFlow.sln -c Release` | `Build succeeded. 0 Warning(s). 0 Error(s).` | ✓ PASS |
| Targeted phase filter (8 suites) | `dotnet test DeckFlow.sln -c Release --no-build --filter "...CutLabAnalysisContextBuilderTests\|...CutLabPageServiceTests\|...CutLabWhatifTests\|...CutLabExportServiceTests\|...ScryfallReferenceResolverTests\|...ScryfallCardResolverTests\|...ScryfallThrottleTests\|...DeckAnalysisPacketServiceTests"` | `Passed! - Failed: 0, Passed: 280, Total: 280` | ✓ PASS (matches GATES.md Section 3b exactly) |
| Full Web suite | `dotnet test DeckFlow.Web.Tests -c Release --no-build` | `Passed! - Failed: 0, Passed: 2072, Skipped: 16, Total: 2088` | ✓ PASS |
| `ScryfallThrottleTests` alone | `dotnet test ... --filter "FullyQualifiedName~ScryfallThrottleTests"` | `Passed! - Failed: 0, Passed: 11, Total: 11` | ✓ PASS |
| Mutation test on B-1's `PrimeResolvedCardsCache` subtraction | Manually disabled the subtraction, rebuilt, ran `ProcessAsync_SecondImportAfterSwallowedRateLimit_DoesNotPoisonKnownMissingCache` | RED: `Assert.True() Failure`, `Expected: True, Actual: False` at line 561 — then restored byte-for-byte (`git diff` empty) and re-confirmed GREEN | ✓ PASS — the fix is load-bearing, not a hollow-passing test |
| Scope check | `git diff --name-only 8d185b5c..HEAD -- DeckFlow.Web DeckFlow.Web.Tests DeckFlow.Core DeckFlow.CLI docs` | Exactly the 10 files declared across all 5 plans' `files_modified`, no more, no less | ✓ PASS |
| Debt-marker scan | `grep -nE "TBD\|FIXME\|XXX\|TODO\|HACK\|PLACEHOLDER"` across all 10 changed files | No matches | ✓ PASS |

### Requirements Coverage

No `.planning/REQUIREMENTS.md` entries reference Phase 111.1 (it is an inserted prod hotfix with its
requirement source recorded as `.planning/debug/cutlab-import-scryfall-429.md`, not the milestone
requirements ledger). Traceability instead runs through ROADMAP.md's 7 numbered Success Criteria,
all independently verified above.

### Anti-Patterns Found

None. No debt markers (`TBD`/`FIXME`/`XXX`/`TODO`/`HACK`/`PLACEHOLDER`) in any of the 10 files this
phase touched. No stub returns, no empty handlers, no hardcoded empty data flowing to a rendered
surface. The one place a prior wave-1 comment was found to be imprecise (the `break` comment
overclaiming "actively rate-limiting" for every swallowed exception) was itself caught and corrected
by this phase's own blind-verification pass in plan 05 — confirmed present at
`CutLabAnalysisContextBuilder.cs:479-484`, comment-only, `break;` behavior unchanged.

### Human Verification Required

None outstanding for phase-goal achievement. `111.1-VALIDATION.md` names three Manual-Only items
(SC-7 acceptance — already answered "accept" and recorded verbatim in `111.1-04-SUMMARY.md`;
production banner confirmation — post-deploy only, not blocking this local-worktree verification;
degraded-warning copy/theme eyeball — cosmetic, not goal-blocking). None of these gate phase
completion; all are either already discharged or are explicitly deferred to post-deploy observation.

### Known Open Items (not gaps — recorded per phase record and orchestrator brief)

- Manabase's 20s server-side timeout budget narrows to ~2.6x margin after the pacing change (named
  post-ship watch item in `111.1-04-SUMMARY.md`).
- Cut Lab's own pool-import path has no server- or client-side timeout on either side (explicitly
  out of scope for this hotfix, named in `111.1-04-SUMMARY.md` and `111.1-GATES.md`).
- Codex cross-AI review is OWED, not skipped — deferred to on/after 2026-08-04 per the standing
  outage-window directive in `CLAUDE.md`.
- Untracked `.foreman/ledger.md`, `COPILOT-REVIEW-REQUEST.md`, `PHASE-111.1-REVIEW-DIFF.patch` are
  orchestrator scratch files, not part of the phase's change set (confirmed: `git status --short`
  shows these as the only untracked entries, and none are referenced by any plan's `files_modified`).

### Gaps Summary

None. Every ROADMAP Success Criterion (SC-1 through SC-7) plus both round-1-review blockers (B-1,
W-1) resolve to VERIFIED against the actual codebase at HEAD, not merely against SUMMARY.md
narrative. Independent re-execution (clean build, 280/280 targeted filter, 2072/2088 full Web suite,
an from-scratch mutation test on the B-1 fix, and a file-diff scope check) corroborates every claim
this verifier chose to spot-check, including the specific claim flagged by the orchestrator brief as
highest-risk (whether the B-1 `PrimeResolvedCardsCache` subtraction test would still pass if the
underlying fix were removed — it does not; RED reproduced independently).

---

*Verified: 2026-07-31*
*Verifier: Claude (gsd-verifier)*
