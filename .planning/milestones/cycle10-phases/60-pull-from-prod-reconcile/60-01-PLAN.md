---
phase: 60-pull-from-prod-reconcile
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - DeckFlow.Core/Content/SyncDiffEntry.cs
  - DeckFlow.Core/Content/ContentSyncDiffClassifier.cs
  - DeckFlow.Core.Tests/Content/ContentSyncDiffClassifierTests.cs
autonomous: true
requirements: [SYNC-02]
must_haves:
  truths:
    - "Classifier returns ProdNewer when the same key exists both sides and prod IndexedUtc is newer"
    - "Classifier returns MissingLocally when a key exists in prod but not local"
    - "Classifier returns LocalOnly when a key exists in local but not prod"
    - "Classifier returns Diverged (with LocalIsNewer=true) when local IndexedUtc is newer than prod"
    - "Classifier returns Diverged (with LocalIsNewer=false) when timestamps are equal but content differs"
    - "Classifier OMITS a key entirely when prod and local rows are identical (same content fingerprint) — in-sync is not a diff (R3)"
    - "Classifier handles empty prod set, empty local set, and both-empty without throwing"
  artifacts:
    - path: "DeckFlow.Core/Content/SyncDiffEntry.cs"
      provides: "SyncDiffKind enum (4 kinds) + SyncDiffEntry sealed record with LocalIsNewer + ArtifactDownloaded"
      contains: "enum SyncDiffKind"
    - path: "DeckFlow.Core/Content/ContentSyncDiffClassifier.cs"
      provides: "Pure static Classify(prodRows, localRows) -> IReadOnlyList<SyncDiffEntry>, omitting identical pairs"
      contains: "static class ContentSyncDiffClassifier"
    - path: "DeckFlow.Core.Tests/Content/ContentSyncDiffClassifierTests.cs"
      provides: "xUnit coverage of all 4 kinds + LocalIsNewer + identical-pair-omitted + empty/one-sided sets"
      contains: "ContentSyncDiffClassifier.Classify"
  key_links:
    - from: "DeckFlow.Core/Content/ContentSyncDiffClassifier.cs"
      to: "DeckFlow.Core.Knowledge.ContentSiteIndexRow"
      via: "compares IndexedUtc + content fingerprint by natural key"
      pattern: "IndexedUtc"
---

<objective>
Build the pure, testable diff classifier that compares prod and local `content_site_index`
rows by natural key and labels each *differing* entry with exactly one of the four SYNC-02 kinds.
This is the heart of SYNC-02 and the one piece that is pure CPU logic — it lives in `DeckFlow.Core`
beside `PublishStateDeriver` and is unit-tested in `DeckFlow.Core.Tests`. No I/O, no DI.

Purpose: Give the Studio page (Plan 03) a verified, side-effect-free function so the operator
sees an accurate per-entry classification of what is out of sync — and is NOT shown rows that are
already in sync (R3).
Output: `SyncDiffEntry`/`SyncDiffKind`, `ContentSyncDiffClassifier`, and full xUnit coverage.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/60-pull-from-prod-reconcile/60-CONTEXT.md
@.planning/phases/60-pull-from-prod-reconcile/60-RESEARCH.md
@./CLAUDE.md

# Pure-Core classifier pattern to mirror exactly (sibling file in same folder):
@DeckFlow.Core/Content/PublishStateDeriver.cs

<interfaces>
<!-- The row type the classifier consumes. Extracted from codebase — do not re-explore. -->

From DeckFlow.Core/Knowledge/ContentArtifactSpec.cs (namespace DeckFlow.Core.Knowledge):
```csharp
public sealed record ContentSiteIndexRow
{
    public required long Id { get; init; }
    public required string Source { get; init; }
    public required string Title { get; init; }
    public required string VideoUrl { get; init; }
    public required string ArtifactPath { get; init; }           // RELATIVE: content-kb/{slug}/{key}.md
    public DateTimeOffset? PublishedUtc { get; init; }
    public DateTimeOffset? PushedToProdUtc { get; init; }
    public required DateTimeOffset IndexedUtc { get; init; }      // non-nullable — the "freshness" axis
    public bool IsVisible { get; init; }
    public bool IsHidden { get; init; }
    public bool IsEvergreen { get; init; }
    public string ApprovalStatus { get; init; } = "pending";     // pending|approved|rejected
    public required IReadOnlyList<string> ArchetypeTags { get; init; }
    public required IReadOnlyList<string> BracketTags { get; init; }
    public required IReadOnlyList<string> CardCategoryTags { get; init; }
    public string? YoutubeVideoId { get; init; }
    public string? RssGuid { get; init; }
    public string? PinId => YoutubeVideoId ?? RssGuid;           // natural key value
}
```

Natural key convention (used throughout DirectPush/stores): key VALUE = `YoutubeVideoId ?? RssGuid`;
key TYPE = "youtube" when YoutubeVideoId is non-null, else "podcast".
`ContentSourceType.Youtube` / `ContentSourceType.Podcast` are the canonical type discriminators.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define SyncDiffKind + SyncDiffEntry contract</name>
  <files>DeckFlow.Core/Content/SyncDiffEntry.cs</files>
  <behavior>
    - SyncDiffKind enum has EXACTLY four members: ProdNewer, MissingLocally, LocalOnly, Diverged. No 5th LocalNewer kind (per CONTEXT D — local-newer folds into Diverged).
    - SyncDiffEntry exposes: NaturalKeyType (string), NaturalKeyValue (string), Kind (SyncDiffKind), Title (string), ProdRow (ContentSiteIndexRow?), LocalRow (ContentSiteIndexRow?), ArtifactPath (string), LocalIsNewer (bool), ArtifactDownloaded (bool).
  </behavior>
  <action>
    Create `DeckFlow.Core/Content/SyncDiffEntry.cs` in namespace `DeckFlow.Core.Content`.
    Define `public enum SyncDiffKind` with exactly the four members in the behavior block — XML-doc each. Define `public sealed record SyncDiffEntry` as an immutable record with `required` init properties matching the behavior block. `ProdRow` is null for LocalOnly; `LocalRow` is null for MissingLocally. `LocalIsNewer` is only meaningful when Kind==Diverged (default false otherwise). `ArtifactDownloaded` defaults to false (the page sets it after the SCP step in Plan 03 — the classifier leaves it false). Reference `ContentSiteIndexRow` from `DeckFlow.Core.Knowledge` (already in the same assembly). Preserve `{ get; init; }` — do NOT convert to `{ get; }` (.editorconfig carve-out; init-only is load-bearing for the record contract). LF line endings, file-scoped namespace, XML doc on every public member (GenerateDocumentationFile is on).
  </action>
  <verify>
    <automated>cd "$WORKTREE" && dotnet build DeckFlow.Core/DeckFlow.Core.csproj 2>&1 | grep -E "error|Build succeeded" | head -5</automated>
  </verify>
  <done>DeckFlow.Core builds clean; SyncDiffKind has 4 members; SyncDiffEntry record compiles with all listed properties.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement ContentSyncDiffClassifier + xUnit tests (RED then GREEN)</name>
  <files>DeckFlow.Core.Tests/Content/ContentSyncDiffClassifierTests.cs, DeckFlow.Core/Content/ContentSyncDiffClassifier.cs</files>
  <behavior>
    - Classify(prodRows, localRows): both-empty -> empty list.
    - Key in prod only -> one entry, Kind=MissingLocally, ProdRow set, LocalRow null.
    - Key in local only -> one entry, Kind=LocalOnly, LocalRow set, ProdRow null.
    - Key both sides, prod IndexedUtc strictly newer -> Kind=ProdNewer, LocalIsNewer=false.
    - Key both sides, local IndexedUtc strictly newer -> Kind=Diverged, LocalIsNewer=true.
    - Key both sides, equal IndexedUtc but differing content fingerprint -> Kind=Diverged, LocalIsNewer=false.
    - R3 — Key both sides, prod and local rows IDENTICAL (equal IndexedUtc AND identical content fingerprint) -> NO entry emitted for that key (in-sync = not a diff). The result list contains only real differences.
    - Mixed-source set (youtube + podcast keys) classified independently by natural key.
    - Title on each entry comes from the prod row when present, else the local row.
    - Timestamps compared in UTC (normalize via .ToUniversalTime()) so equal instants across offsets compare equal — guards against the F-51-PG-01 timestamptz-vs-text class of bug at the comparison boundary.
  </behavior>
  <action>
    FIRST (RED): create `DeckFlow.Core.Tests/Content/ContentSyncDiffClassifierTests.cs` (namespace `DeckFlow.Core.Tests`, xUnit `[Fact]`/`[Theory]`) covering every case in the behavior block. Include an EXPLICIT R3 test: when a prod row and a local row share the same natural key, the same IndexedUtc, AND an identical content fingerprint, `Classify` returns ZERO entries for that key (assert the result is empty / does not contain that key). Also assert a mixed set where one key is identical (omitted) and another differs (emitted) returns only the differing entry. Build a small `ContentSiteIndexRow` factory helper in the test file (all `required` props populated; vary `YoutubeVideoId`/`RssGuid`, `IndexedUtc`, `Title`, and the tag lists for the fingerprint cases). Run and confirm RED (classifier does not exist yet). Commit `test(60-01): add failing ContentSyncDiffClassifier tests`.

    THEN (GREEN): create `DeckFlow.Core/Content/ContentSyncDiffClassifier.cs` (namespace `DeckFlow.Core.Content`) as a `public static class` with `public static IReadOnlyList<SyncDiffEntry> Classify(IReadOnlyList<ContentSiteIndexRow> prodRows, IReadOnlyList<ContentSiteIndexRow> localRows)`. Build dictionaries keyed by natural key (value = `YoutubeVideoId ?? RssGuid`). For each prod key: MissingLocally if absent locally, else classify the pair. For each local-only key: LocalOnly. Pair classification using `IndexedUtc.ToUniversalTime()`:
    - prod > local => ProdNewer.
    - local > prod => Diverged + LocalIsNewer=true.
    - equal timestamps => compare content fingerprint: if the fingerprints are IDENTICAL, the pair is in-sync — **emit NOTHING (skip this key entirely, R3)**; if they differ, Diverged + LocalIsNewer=false.
    Content fingerprint helper concatenates `Title + ArtifactPath + ArchetypeTags + BracketTags + CardCategoryTags` (ordinal join) — used both for the equal-timestamp Diverged-vs-omit decision (R3) and for the equal-timestamp differing-content case. Set `Title` from prod row when non-null else local row. Pure — no I/O, no exceptions for normal inputs (null-arg guard via `ArgumentNullException.ThrowIfNull`). `ArtifactPath` from prod row when available, else local row. `ArtifactDownloaded` left default false. Run tests GREEN. Commit `feat(60-01): implement ContentSyncDiffClassifier`.

    Preserve switch expressions if used (carve-out); LF endings; changed-lines-only formatting.
  </action>
  <verify>
    <automated>cd "$WORKTREE" && dotnet test DeckFlow.Core.Tests/DeckFlow.Core.Tests.csproj --filter "FullyQualifiedName~ContentSyncDiffClassifier" 2>&1 | grep -E "Passed!|Failed!|error" | head -5</automated>
  </verify>
  <done>All ContentSyncDiffClassifier tests pass; every one of the 4 kinds + LocalIsNewer + identical-pair-omitted (R3) + empty/one-sided cases is asserted; classifier is pure (no I/O).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| prod DB rows -> classifier | Prod row values (incl. timestamps, ArtifactPath) are untrusted input crossing into pure Core logic; classifier must not crash on adversarial/edge values |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-60-01 | Denial of Service | ContentSyncDiffClassifier.Classify | mitigate | Pure function, O(n) dictionary build, no recursion/unbounded loops; null-arg guard via ArgumentNullException.ThrowIfNull; tolerates empty/one-sided sets (tested) |
| T-60-02 | Tampering | IndexedUtc comparison (F-51-PG-01 class) | mitigate | Normalize both sides with .ToUniversalTime() before comparing so a timestamptz-vs-text representation mismatch at the boundary cannot misclassify; comparison is in-memory only (no SQL WHERE on timestamps), so the original 42883 bug cannot recur here |
| T-60-03 | Information Disclosure | classifier output | accept | Classifier returns row references the page already holds; no secrets, no logging in Core; the page (Plan 03) owns all secret-sanitization |
</threat_model>

<verification>
- `dotnet build DeckFlow.Core/DeckFlow.Core.csproj` clean (0 errors).
- `dotnet test DeckFlow.Core.Tests --filter ContentSyncDiffClassifier` green.
- `grep -c "SyncDiffKind\." DeckFlow.Core.Tests/Content/ContentSyncDiffClassifierTests.cs` shows all four kinds asserted.
- An identical-pair test asserts ZERO entries for an in-sync key (R3).
- No `{ get; }` regressions on the new record (init-only preserved).
</verification>

<success_criteria>
- `ContentSyncDiffClassifier.Classify` deterministically labels every *differing* paired/unpaired key with exactly one SYNC-02 kind, and OMITS identical in-sync pairs (R3).
- Diverged carries a `LocalIsNewer` direction hint; no 5th kind exists.
- Pure: zero I/O, zero DI, no exceptions for valid inputs.
- xUnit tests cover all 4 kinds + LocalIsNewer + identical-pair-omitted + empty/one-sided sets and pass.
</success_criteria>

<output>
Create `.planning/phases/60-pull-from-prod-reconcile/60-01-SUMMARY.md` when done.
</output>
