---
phase: 91-reconcile-seed-lifecycle
plan: 07
type: execute
wave: 5
depends_on: [91-06]
files_modified:
  - DeckFlow.Studio/ViewModels/ReconcileCoordinator.cs
  - DeckFlow.Studio/Pages/Reconcile.razor
  - DeckFlow.Studio/Pages/Reconcile.razor.cs
  - DeckFlow.Studio/Program.cs
  - DeckFlow.Studio.Tests/ViewModels/ReconcileCoordinatorTests.cs
  - README.md
autonomous: true
requirements: [SYNC-11]

must_haves:
  truths:
    - "The operator can trigger a reconcile dry-run from a Studio page and see the four discrepancy classes"
    - "The dry-run runs with sync.reconcile OFF (detection is always available, read-only)"
    - "The dry-run performs no destructive write"
    - "When the seed is unavailable, the page shows a 'seed unavailable - seed-drift/removal skipped' notice rather than presenting an empty seed-drift group as 'no drift'"
  artifacts:
    - path: "DeckFlow.Studio/ViewModels/ReconcileCoordinator.cs"
      provides: "Operator-action coordinator (dry-run) mirroring DirectPushCoordinator shape; surfaces ReconcileDryRunResult (SeedAvailable + discrepancies)"
      contains: "ReconcileCoordinator"
    - path: "DeckFlow.Studio/Pages/Reconcile.razor"
      provides: "Operator reconcile page (dry-run)"
      contains: "Reconcile"
  key_links:
    - from: "ReconcileCoordinator.RunDryRunAsync"
      to: "IContentKbReconcileOrchestrator.RunDryRunAsync"
      via: "coordinator delegates to orchestrator, returns ReconcileDryRunResult"
      pattern: "RunDryRunAsync"
---

<objective>
Ship the operator-facing dry-run: a `ReconcileCoordinator` (mirroring `DirectPushCoordinator`'s shape)
that runs the orchestrator dry-run and exposes its `ReconcileDryRunResult` (SeedAvailable + the four
discrepancy classes), plus a `Reconcile.razor` page (mirroring `DirectPush.razor`) where the operator
triggers it and reviews the results. Dry-run only this plan - no Apply, no flag gate (detection is always
available per D-09).

Purpose: Give the operator the visibility that would have caught the 63 unpublished rows + CP437
mojibake, before any flag flip. "Dry-run ships first." The page also surfaces a "seed unavailable"
notice when SeedAvailable is false, so an unreadable seed reads as a warning, not phantom drift or a
false "no drift" (Codex BLOCK).
Output: `ReconcileCoordinator` (dry-run methods) + `Reconcile.razor(.cs)` + registration + README.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/91-reconcile-seed-lifecycle/91-CONTEXT.md
@.planning/phases/91-reconcile-seed-lifecycle/91-RESEARCH.md
@.planning/phases/91-reconcile-seed-lifecycle/91-PATTERNS.md
@.planning/phases/91-reconcile-seed-lifecycle/91-06-SUMMARY.md

<interfaces>
<!-- From 91-06 (dependency). Executor USES these. -->
IContentKbReconcileOrchestrator.RunDryRunAsync(scopeTag, ct) : Task<ReconcileDryRunResult>   // ReconcileDryRunResult(bool SeedAvailable, IReadOnlyList<ContentKbReconcileDiscrepancy> Discrepancies) from 91-06
IContentKbReconcileStore.GetOpenAsync(scopeTag, ct)
<!-- Coordinator convention -->
DirectPushCoordinator: optional-logger (NullLogger fallback), IConfiguration["Studio:ProdConnectionString"] for prod conn
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ReconcileCoordinator (dry-run)</name>
  <files>DeckFlow.Studio/ViewModels/ReconcileCoordinator.cs, DeckFlow.Studio/Program.cs, DeckFlow.Studio.Tests/ViewModels/ReconcileCoordinatorTests.cs</files>
  <read_first>
    - DeckFlow.Studio/ViewModels/DirectPushCoordinator.cs (constructor + optional-logger convention ~72-106; ComputeDiffAsync "compute fresh on demand" shape ~119-133; prod conn-string access)
    - DeckFlow.Studio/Services/IContentKbReconcileOrchestrator.cs (ReconcileDryRunResult record - from 91-06)
    - DeckFlow.Studio.Tests/ViewModels/DirectPushCoordinatorTests.cs (coordinator test template with fakes)
    - 91-PATTERNS.md "ReconcileCoordinator.cs" section
  </read_first>
  <behavior>
    - RunDryRunAsync delegates to the orchestrator and returns the ReconcileDryRunResult (SeedAvailable + discrepancy list), exposing both for the page.
    - The dry-run does NOT read or require sync.reconcile (detection is flag-independent).
    - The dry-run performs no destructive write (no visibility change, no prod DDL).
    - When the orchestrator reports SeedAvailable==false, the coordinator surfaces it so the page can show "seed unavailable - seed-drift/removal skipped" rather than an empty seed-drift list read as "no drift".
  </behavior>
  <action>
    Create `ReconcileCoordinator` in ViewModels mirroring DirectPushCoordinator's constructor/optional-logger
    convention (ArgumentNullException.ThrowIfNull; `_logger = logger ?? NullLogger<ReconcileCoordinator>.Instance`).
    Depend on IContentKbReconcileOrchestrator + IContentKbReconcileStore + IConfiguration. Implement
    `RunDryRunAsync(scopeTag = "full", ct)` delegating to the orchestrator and returning the orchestrator's
    `ReconcileDryRunResult` (SeedAvailable + discrepancies), plus a `GetOpenDiscrepanciesAsync` pass-through
    to the store for page rendering. Do NOT add any Apply or flag-read logic here (that is 91-08). Register
    the coordinator in Program.cs. Add ReconcileCoordinatorTests asserting the dry-run returns the fake
    orchestrator's ReconcileDryRunResult, that a SeedAvailable==false result is surfaced intact, and that no
    destructive call occurs (fake orchestrator/store spies).
  </action>
  <acceptance_criteria>
    - Test asserts RunDryRunAsync returns the fake orchestrator's ReconcileDryRunResult (SeedAvailable + discrepancies).
    - Test asserts a SeedAvailable==false result is surfaced (so the page can render the seed-unavailable notice).
    - Test asserts no flag read and no visibility/destructive write occurs during dry-run.
    - Build clean; coordinator resolves at Studio startup.
  </acceptance_criteria>
  <verify>
    <automated>dotnet.exe test DeckFlow.Studio.Tests/DeckFlow.Studio.Tests.csproj --no-restore --filter "FullyQualifiedName~ReconcileCoordinator"</automated>
  </verify>
  <done>ReconcileCoordinator runs a flag-independent, non-destructive dry-run delegating to the orchestrator, returns and surfaces the ReconcileDryRunResult (incl. SeedAvailable); tests green.</done>
</task>

<task type="auto">
  <name>Task 2: Reconcile.razor operator page (dry-run) + README</name>
  <files>DeckFlow.Studio/Pages/Reconcile.razor, DeckFlow.Studio/Pages/Reconcile.razor.cs, README.md</files>
  <read_first>
    - DeckFlow.Studio/Pages/DirectPush.razor + DirectPush.razor.cs (page/coordinator wiring, action-button + result-render shape)
    - DeckFlow.Studio/Pages/PullFromProd.razor (secondary layout analog)
    - README.md (Studio operator-tooling section - where DirectPush/Pull are documented)
  </read_first>
  <behavior>
    - Navigating to the Reconcile page and clicking "Run dry-run" renders the four discrepancy classes with counts.
    - If the run's SeedAvailable is false, the page renders a "seed unavailable - seed-drift/removal skipped" banner instead of presenting the empty seed-drift group as "no drift".
    - The page makes clear this is read-only (no removal happens here).
  </behavior>
  <action>
    Create `Reconcile.razor` + `.razor.cs` mirroring DirectPush.razor's page/coordinator wiring: a
    "Run dry-run" action calling ReconcileCoordinator.RunDryRunAsync and a results panel grouping
    discrepancies by class with counts and the D-06 report location. When the ReconcileDryRunResult's
    SeedAvailable is false, render a prominent "seed unavailable - seed-drift/removal skipped" banner in
    place of the seed-drift group (do not let an empty seed-drift group read as "no drift"). Label the
    page read-only / detection-only (Apply arrives in 91-08). Add a route + nav entry consistent with the
    other Studio operator pages. Update README.md's Studio operator-tooling section to document the
    Reconcile dry-run and the seed-unavailable notice (behavior change -> README update per project rules).
    No Apply button this plan.
  </action>
  <acceptance_criteria>
    - Reconcile page compiles and is reachable via a route/nav entry alongside DirectPush/Pull.
    - The page renders a seed-unavailable notice when SeedAvailable is false (not an empty "no drift" seed-drift group).
    - README documents the Reconcile dry-run and the seed-unavailable notice.
    - `dotnet build DeckFlow.sln` clean, 0 new warnings.
  </acceptance_criteria>
  <verify>
    <automated>dotnet build DeckFlow.sln --no-restore 2>&1 | grep -viE '^\s*#' | grep -icE 'error|warning' | grep -qx 0 && echo RECONCILE_PAGE_BUILD_CLEAN</automated>
  </verify>
  <done>An operator Reconcile page renders the dry-run's four classes read-only (with a seed-unavailable notice when SeedAvailable is false), is routed/nav-linked like the other operator pages, and README documents it; solution builds clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator (local Blazor) -> coordinator | Same single-operator trust boundary as DirectPush/Pull; dry-run is read-only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-91-17 | Elevation of Privilege | Dry-run accidentally triggering a destructive write | mitigate | No Apply/flag/visibility-write path in this plan; test asserts dry-run is non-destructive |
| T-91-18 | Repudiation | Dry-run results not recorded for later review | mitigate | Results persisted to the local store (91-05) + D-06 report (91-06) surfaced on the page |
| T-91-28 | Tampering | An unavailable seed rendered as an empty seed-drift group read as "no drift" | mitigate | Coordinator surfaces ReconcileDryRunResult.SeedAvailable; page renders a "seed unavailable - skipped" banner instead of an empty seed-drift group |
</threat_model>

<verification>
- `dotnet build DeckFlow.sln` clean, 0 new warnings.
- ReconcileCoordinator dry-run tests green (incl. SeedAvailable==false surfaced).
- README updated (behavior change).
</verification>

<success_criteria>
The operator can run a read-only reconcile dry-run from Studio and review all four discrepancy classes
before any flag flip - the always-available detection surface that "ships first" - with a clear
"seed unavailable" notice when the seed cannot be read (never phantom drift or a false "no drift").
</success_criteria>

<output>
Create `.planning/phases/91-reconcile-seed-lifecycle/91-07-SUMMARY.md` when done.
</output>
