---
phase: 101-intake-protection-foundation
plan: 04
type: execute
wave: 3
depends_on: [101-03]
files_modified:
  - DeckFlow.Web/Views/Deck/CutLab.cshtml
  - DeckFlow.Web/wwwroot/ts/cut-lab.ts
  - DeckFlow.Web/wwwroot/css/site-common.css
  - DeckFlow.Web/ts-tests/cut-lab-lock-interactions.test.ts
  - DeckFlow.Web/e2e/cut-lab-smoke.spec.ts
autonomous: true
requirements: [INTAKE-01, INTAKE-02, LOCK-01, LOCK-02, LOCK-03]
must_haves:
  truths:
    - "The Cut Lab page renders the split URL/paste intake form, the four intent controls, and (after import) the card-count + legality summary"
    - "The commander row is rendered checked + disabled with a persistent 'Commander · Always locked' badge and cannot be unlocked"
    - "A user can lock individual cards, assign cards to named packages and lock/unlock a package as a unit, and bulk-lock all lands with one control"
    - "Browser lock/package/commander edits are serialized into the hidden CutLabStateJson field so they survive a form resubmit (round-trip to the server and back)"
    - "With the flag OFF, /cut-lab returns 404 and no Home tile links to it"
  artifacts:
    - path: "DeckFlow.Web/Views/Deck/CutLab.cshtml"
      provides: "Cut Lab Razor page (hero, split-input, intent pills, lock table with per-row data attributes, packages, commander fallback)"
      contains: "CutLabStateJson"
    - path: "DeckFlow.Web/wwwroot/ts/cut-lab.ts"
      provides: "Lock/unlock + package indeterminate + bulk role lock + inline new-package + delete confirm + DOM→CutLabStateJson serialization on submit"
      contains: "CutLabStateJson"
    - path: "DeckFlow.Web/ts-tests/cut-lab-lock-interactions.test.ts"
      provides: "Vitest coverage of indeterminate calculation + land bulk-lock selection + camelCase state serialization"
      contains: "DeckFlowCutLab"
    - path: "DeckFlow.Web/e2e/cut-lab-smoke.spec.ts"
      provides: "Flag-ON render + import/lock/resubmit-persistence/screenshot + flag-OFF 404 e2e"
      contains: "cut-lab"
  key_links:
    - from: "cut-lab.ts submit handler"
      to: "input[name=CutLabStateJson]"
      via: "live DOM lock/package/commander state serialized to camelCase CutLabState JSON before POST"
      pattern: "CutLabStateJson"
    - from: "CutLab.cshtml land rows"
      to: "cut-lab.ts bulk land-lock"
      via: "data-cut-lab-role=\"land\" attribute"
      pattern: "data-cut-lab-role"
    - from: "commander lock row"
      to: ".cutlab-lock-badge--commander"
      via: "always-locked visual signal"
      pattern: "cutlab-lock-badge--commander"
---

<objective>
Build the Cut Lab UI: the Razor page (hero, split URL/paste intake, the four intent controls, the
card-count + legality summary, the responsive lock table with per-row data attributes, named-package
assignment, the bulk "lock all lands" control, and the ambiguous-commander fallback), the cut-lab.ts
interaction module (lock toggles, package indeterminate state, bulk role-group pill, inline new-package,
delete confirm, AND — critically — serialization of live DOM lock/package/commander state back into the
hidden CutLabStateJson field on submit so user edits survive a resubmit), two additive site-common.css
component classes, a Vitest unit test for the pure interaction logic, and the cut-lab-smoke.spec.ts e2e
suite (including a resubmit-persistence assertion).

Purpose: The visible surface of INTAKE-01/02 and the end-to-end persistence of LOCK-01/02/03. Consumes
plan 101-03's CutLabController (/cut-lab route + "CutLab" view), CutLabViewModel, and the camelCase
CutLabState JSON contract (101-03 serializer pins JsonSerializerDefaults.Web). The server (101-03 step 8)
treats client-submitted CutLabStateJson locks as authoritative and re-applies EnforceCommanderLock; this
plan makes the browser actually WRITE those locks into CutLabStateJson.
Output: Views/Deck/CutLab.cshtml, wwwroot/ts/cut-lab.ts, two new site-common.css classes,
ts-tests/cut-lab-lock-interactions.test.ts, e2e/cut-lab-smoke.spec.ts. Compiled wwwroot/js/cut-lab.js is
gitignored — do NOT stage it.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/cut-lab/phases/101-intake-protection-foundation/101-UI-SPEC.md
@.planning/workstreams/cut-lab/phases/101-intake-protection-foundation/101-PATTERNS.md
@.planning/workstreams/cut-lab/phases/101-intake-protection-foundation/101-RESEARCH.md
@DeckFlow.Web/Models/CutLabViewModel.cs
@DeckFlow.Web/Models/CutLabRequest.cs
@DeckFlow.Web/Models/CutLab/CutLabState.cs

<interfaces>
<!-- View model available to CutLab.cshtml is CutLabViewModel (from plan 101-03): ActiveTab,
     Request (CutLabRequest with DeckInputSource/DeckUrl/DeckText/PrimaryPlan/SecondaryPlan/Bracket/
     PlayExperience/SelectedCommander/CutLabStateJson), ErrorMessage, CardCount, BannedCardsPresent,
     IsLegal, CommanderSelectionRequired, CommanderChoices, Warnings, HasResult, CutLabStateJson,
     and the resolved Pool/Packages for rendering. -->

<!-- CutLabState JSON contract (101-03 serializer pins camelCase via JsonSerializerDefaults.Web):
     { "commander": string,
       "pool": [ { "name": string, "quantity": int, "typeLine": string, "isCommander": bool,
                   "isLocked": bool, "packageId": string|null } ],
       "packages": [ { "id": string, "name": string, "locked": bool } ],
       "intent": { "primaryPlan": string, "secondaryPlan": string|null, "bracket": int|null,
                   "playExperience": string } }
     cut-lab.ts MUST emit exactly this camelCase shape when it writes CutLabStateJson. -->

<!-- Razor analogs (read them): DeckHistory.cshtml (hero + _DeckToolTabs + error-banner + <form
     data-cache-key + @Html.AntiForgeryToken() + hidden round-trip input + @section Scripts);
     Manabase.cshtml:38-57 split-input; :87-123 CommanderSelectionRequired; :160-219 segmented pills.
     Lock table: table[data-prompt-cedh-reference-table] (site-common.css:1204-1230), mobile data-label. -->

<!-- Vitest convention (DeckFlow.Web/ts-tests/flag-filter.test.ts + vitest.config.ts):
     jsdom env; include ts-tests/**/*.test.ts. Source modules (module:none) expose a testable API by
     assigning `globalThis.DeckFlow<Name>`; the test does `import '../wwwroot/ts/<module>';` for the
     side effect, then reads `(globalThis as ...).DeckFlow<Name>`. cut-lab.ts must expose
     `globalThis.DeckFlowCutLab` with the pure helpers so Vitest can exercise them. Run: `npm test`
     (= vitest run) from DeckFlow.Web. -->

<!-- Client persistence (no new code): deck-input-store.ts + deck-sync.ts data-cache-key handle URL/paste
     restore ONLY — they do NOT serialize lock state; that is this plan's new cut-lab.ts responsibility.
     Known pre-existing restore-desync bug (RESEARCH.md Pitfall 1) is OUT OF SCOPE. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: CutLab.cshtml — intake form, intent controls, lock table with per-row data attributes, packages, commander fallback</name>
  <files>DeckFlow.Web/Views/Deck/CutLab.cshtml</files>
  <read_first>
    - DeckFlow.Web/Views/Deck/DeckHistory.cshtml (page shell, hero, _DeckToolTabs, error banner, form with data-cache-key + AntiForgeryToken + hidden round-trip field + @section Scripts)
    - DeckFlow.Web/Views/Deck/Manabase.cshtml (split-input 38-57, CommanderSelectionRequired 87-123, segmented pills 160-219)
    - DeckFlow.Web/Views/Deck/Bracket.cshtml lines ~16-20 (official tier names for the bracket pill labels)
    - .planning/workstreams/cut-lab/phases/101-intake-protection-foundation/101-UI-SPEC.md (Copywriting Contract + Lock UI Component Contract — the binding spec)
    - DeckFlow.Web/Models/CutLabViewModel.cs (fields to bind)
  </read_first>
  <action>
    Create Views/Deck/CutLab.cshtml bound to `@model DeckFlow.Web.Models.CutLabViewModel`. Structure per
    UI-SPEC:
      - <section class="hero"> with <h1>Cut Lab</h1> and a lede describing the oversized-pool → 100 workflow.
      - @await Html.PartialAsync("_BusyIndicator") and @await Html.PartialAsync("_DeckToolTabs", Model.ActiveTab).
      - error-banner div bound to Model.ErrorMessage (hidden when empty), role="alert".
      - <form method="post" action="@Url.Content("~/cut-lab")" class="result-panel" data-cache-key="cut-lab"
        ...busy attributes...> with @Html.AntiForgeryToken() and
        <input type="hidden" name="CutLabStateJson" value="@Model.CutLabStateJson" />.
      - Split URL/paste block (ids cut-lab-input-source / cut-lab-deck-url / cut-lab-deck-text; names
        DeckInputSource / DeckUrl / DeckText; include _DeckFlowBridgeHint partial on the URL field like Manabase).
      - Intent controls (INTAKE-02): Primary plan (required) and Secondary plan (optional, "(optional)"
        label) as <textarea rows="2"> in .field (names PrimaryPlan / SecondaryPlan); Target bracket as a
        5-way manabase-pill radiogroup (name Bracket, values 1-5, tier sub-labels Exhibition/Core/Upgraded/
        Optimized/cEDH); Desired play experience as a Casual/Focused/cEDH manabase-pill radiogroup
        (name PlayExperience) reusing Manabase copy.
      - Primary CTA button labelled "Import pool".
      - Empty state (when !Model.HasResult and no error): heading "No pool imported yet" + the UI-SPEC
        empty-state body copy.
      - Results section (when Model.HasResult): a running-count summary chip
        "{CardCount} cards in pool · {lockedCount} locked (protected from any future cut)" where CardCount is
        the NON-COMMANDER pool count (the 101–150 being trimmed; the commander is the always-locked "plus
        one"), consistent with the empty-state "101 to 150 cards plus your commander" copy; a legality
        summary line (banned cards present, or "No banned cards found"); the CommanderSelectionRequired
        nested-panel (Manabase 87-123 pattern, Cut Lab copy) when Model.CommanderSelectionRequired; and the
        lock table.
      - Lock table: table[data-prompt-cedh-reference-table]-style responsive table, columns
        lock-checkbox · card name · type/role · package assignment, with mobile data-label fallback. EACH
        row MUST carry machine-readable data attributes so cut-lab.ts can serialize the live DOM into
        CutLabStateJson: `data-cut-lab-card="@card.Name"`, `data-cut-lab-type-line="@card.TypeLine"`,
        `data-cut-lab-role="land"` on rows whose front-face type line is a Land (bind from the model's
        per-card land flag / type line), `data-cut-lab-commander="true"` on the commander row, and the
        per-row lock checkbox + package <select> carrying the card name so a value maps to a card. The
        commander row renders its checkbox checked + disabled AND a
        <span class="cutlab-lock-badge--commander"> reading "Commander · Always locked" with inline copy
        "Your commander is always locked and can't be cut." The per-row package
        <select data-df-select> lists existing package names plus a trailing "+ New package…" option.
      - Above the table: a "Lock all lands" bulk control rendered as a .manabase-pill/button (44×44 min hit
        area) that cut-lab.ts binds to (it selects rows via data-cut-lab-role="land").
      - @section Scripts: include deck-input-store.js, busy-indicator.js, moxfield-extension-bridge.js,
        deck-sync.js (as DeckHistory does) PLUS <script src="~/js/cut-lab.js" asp-append-version="true"></script>.
    Use var(--panel) not var(--theme-surface) for any panel background. Introduce NO new CSS tokens. Do not
    add Cut Lab to SeoPaths.Indexable (deferred to Phase 105).
  </action>
  <verify>
    <automated>dotnet build DeckFlow.Web/DeckFlow.Web.csproj -c Debug --nologo -clp:ErrorsOnly</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'data-cache-key="cut-lab"' DeckFlow.Web/Views/Deck/CutLab.cshtml` returns 1
    - `grep -c 'name="CutLabStateJson"' DeckFlow.Web/Views/Deck/CutLab.cshtml` returns 1
    - `grep -c 'Html.AntiForgeryToken' DeckFlow.Web/Views/Deck/CutLab.cshtml` returns 1
    - `grep -c 'name="DeckInputSource"' DeckFlow.Web/Views/Deck/CutLab.cshtml` returns 1
    - Bracket + PlayExperience radiogroups present (grep name="Bracket" and name="PlayExperience" each ≥1)
    - `grep -c 'data-cut-lab-role="land"' DeckFlow.Web/Views/Deck/CutLab.cshtml` returns ≥1
    - `grep -c 'data-cut-lab-commander="true"' DeckFlow.Web/Views/Deck/CutLab.cshtml` returns ≥1
    - `grep -c 'cutlab-lock-badge--commander' DeckFlow.Web/Views/Deck/CutLab.cshtml` returns ≥1
    - `grep -c 'cut-lab.js' DeckFlow.Web/Views/Deck/CutLab.cshtml` returns 1
    - `grep -c 'theme-surface' DeckFlow.Web/Views/Deck/CutLab.cshtml` returns 0 (uses --panel)
    - Razor page compiles (build clean)
  </acceptance_criteria>
  <done>Views/Deck/CutLab.cshtml renders the split intake form, the four intent controls, the card-count + legality summary, the responsive lock table (with per-row data-cut-lab-* attributes and the always-locked commander badge), the bulk "lock all lands" control, and the ambiguous-commander fallback — all reusing existing components and tokens.</done>
</task>

<task type="auto">
  <name>Task 2: cut-lab.ts interactions + DOM→CutLabStateJson serialization + two site-common.css classes</name>
  <files>DeckFlow.Web/wwwroot/ts/cut-lab.ts, DeckFlow.Web/wwwroot/css/site-common.css</files>
  <read_first>
    - DeckFlow.Web/wwwroot/ts/flag-filter.ts (the globalThis.DeckFlow<Name> API-exposure convention Vitest imports)
    - DeckFlow.Web/wwwroot/ts/deck-sync.ts lines ~505-563 (data-cache-key form-state cache — reuse, do NOT reimplement URL/paste persistence)
    - DeckFlow.Web/wwwroot/ts/moxfield-extension-bridge.ts line ~135 (the only public-tool window.confirm precedent)
    - DeckFlow.Web/tsconfig.json (glob include wwwroot/ts/**/*.ts; strict:true, module:none)
    - DeckFlow.Web/Models/CutLab/CutLabState.cs (the C# shape the emitted JSON must match, camelCase)
    - DeckFlow.Web/wwwroot/css/site-common.css lines ~569-581 (.kb-chip), ~2598-2604 (.manabase-row--weakest left-accent), ~2554-2559 (--commander-gold)
    - CLAUDE.md project section (theme CSS: layout in site-common.css only; no new :root tokens; --panel not --theme-surface; LF endings)
  </read_first>
  <action>
    Create wwwroot/ts/cut-lab.ts (strict, module:none, DOMContentLoaded init like sibling TS modules). It
    must NOT reimplement URL/paste persistence. Expose a `globalThis.DeckFlowCutLab` object holding the PURE,
    DOM-free helper functions (so Vitest in Task 3 can test them without a browser):
      - `computePackageCheckboxState(memberLocked: boolean[]): 'checked' | 'unchecked' | 'indeterminate'`
        — all true → 'checked', all false → 'unchecked', mixed → 'indeterminate'.
      - `isLandRole(role: string | null | undefined): boolean` — true only when role === 'land'
        (case-insensitive); the view already computed land-ness server-side via CardTypeLine, so the client
        trusts data-cut-lab-role, not a re-parse of the type line.
      - `buildCutLabStateJson(snapshot): string` — takes a plain snapshot object gathered from the DOM
        (commander name, array of {name, quantity, typeLine, isCommander, isLocked, packageId}, packages
        array, intent object) and returns JSON with EXACTLY the camelCase keys of the C# CutLabState
        contract (commander, pool[{name,quantity,typeLine,isCommander,isLocked,packageId}],
        packages[{id,name,locked}], intent{primaryPlan,secondaryPlan,bracket,playExperience}). Never emit
        the commander with isLocked=false (force true) as a client-side courtesy — but the server re-enforces
        regardless (101-03).
    Wire the impure DOM layer:
      - On the form's `submit` event (addEventListener 'submit'), gather the live DOM state (read each lock
        row's checkbox, its data-cut-lab-* attributes, package assignments, intent fields), call
        buildCutLabStateJson, and write the result into `input[name="CutLabStateJson"].value` BEFORE the POST
        proceeds. RECOMMENDED: also re-serialize after each lock/package toggle so the field is always fresh.
        This is BLOCKER 1's fix: without it, lock/package edits are discarded on any resubmit.
      - Per-row lock/unlock updates the running "locked" count chip; the commander checkbox is disabled
        server-side and the script must never enable it.
      - Package "select-all" checkbox: use computePackageCheckboxState on its members; set
        element.indeterminate = true for the mixed case, checked/unchecked otherwise; toggling it cascades to
        member card checkboxes.
      - Bulk "Lock all lands": clicking it checks every row where isLandRole(row.dataset.cutLabRole) and
        updates the count chip + re-serializes.
      - Package assignment select "+ New package…": reveal an inline text input (mirror .card-picker__row
        inline-add) to name the new package; no modal.
      - Package deletion: guard with window.confirm() using the UI-SPEC copy
        "Delete package '{name}'? Cards return to the unlocked pool — this doesn't discard them."
      Keep functions short and fully typed (no `any`).

    In site-common.css add EXACTLY two additive component classes (layout only, no new :root tokens):
      - `.cutlab-lock-badge--commander` — reuse the .kb-chip shape plus
        `border-left: 3px solid var(--commander-gold, #d4af37);`.
      - `.cutlab-package--locked` — `border-left: 3px solid var(--accent);` (positive mirror of
        .manabase-row--weakest). Do NOT touch site.css or any guild theme fork; add no new tokens. Do NOT
        stage the compiled wwwroot/js/cut-lab.js (gitignored).
  </action>
  <verify>
    <automated>cd DeckFlow.Web && npx --no-install tsc -p tsconfig.json --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'DeckFlowCutLab' DeckFlow.Web/wwwroot/ts/cut-lab.ts` returns ≥1 (global API exposed for Vitest)
    - `grep -c 'CutLabStateJson' DeckFlow.Web/wwwroot/ts/cut-lab.ts` returns ≥1 AND a submit-event listener writes it (`grep -Ec "addEventListener\('submit'|addEventListener\(\"submit\"" DeckFlow.Web/wwwroot/ts/cut-lab.ts` returns ≥1)
    - `grep -c 'computePackageCheckboxState' DeckFlow.Web/wwwroot/ts/cut-lab.ts` returns ≥1
    - `grep -c 'indeterminate' DeckFlow.Web/wwwroot/ts/cut-lab.ts` returns ≥1
    - `grep -c 'confirm(' DeckFlow.Web/wwwroot/ts/cut-lab.ts` returns ≥1
    - `grep -c ': any' DeckFlow.Web/wwwroot/ts/cut-lab.ts` returns 0
    - `grep -c 'cutlab-lock-badge--commander' DeckFlow.Web/wwwroot/css/site-common.css` returns 1
    - `grep -c 'commander-gold' DeckFlow.Web/wwwroot/css/site-common.css` returns ≥1
    - `git status --porcelain DeckFlow.Web/wwwroot/js/cut-lab.js` shows nothing staged
    - tsc --noEmit passes
  </acceptance_criteria>
  <done>cut-lab.ts drives lock/unlock, package indeterminate/cascade, bulk land lock, inline new-package, confirm-on-delete, AND serializes live DOM lock/package/commander state into CutLabStateJson on submit (camelCase, matching the C# contract) so edits survive a resubmit — under TS strict with no any types and a globalThis.DeckFlowCutLab API for unit testing; two additive site-common.css classes use only existing tokens.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: cut-lab-lock-interactions.test.ts (Vitest) — indeterminate calc, land selection, camelCase serialization</name>
  <files>DeckFlow.Web/ts-tests/cut-lab-lock-interactions.test.ts</files>
  <behavior>
    - computePackageCheckboxState([true,true,true]) === 'checked'
    - computePackageCheckboxState([false,false]) === 'unchecked'
    - computePackageCheckboxState([true,false]) === 'indeterminate'
    - isLandRole('land') true; isLandRole('Land') true; isLandRole('ramp') false; isLandRole(null) false
    - buildCutLabStateJson(snapshot) emits camelCase keys exactly matching the C# CutLabState contract
      (commander, pool[{name,quantity,typeLine,isCommander,isLocked,packageId}], packages[{id,name,locked}],
      intent{primaryPlan,secondaryPlan,bracket,playExperience})
    - buildCutLabStateJson forces the commander entry isLocked=true even if the snapshot passed isLocked=false
  </behavior>
  <read_first>
    - DeckFlow.Web/ts-tests/flag-filter.test.ts (exact import pattern: `import '../wwwroot/ts/<module>'` for side effect, then read `globalThis.DeckFlow<Name>`; beforeAll to capture the API)
    - DeckFlow.Web/vitest.config.ts (jsdom env, include ts-tests/**/*.test.ts)
    - DeckFlow.Web/wwwroot/ts/cut-lab.ts (the DeckFlowCutLab API from Task 2)
    - DeckFlow.Web/Models/CutLab/CutLabState.cs (the camelCase key contract to assert against)
  </read_first>
  <action>
    Create ts-tests/cut-lab-lock-interactions.test.ts mirroring flag-filter.test.ts: import 'vitest'
    primitives, `import '../wwwroot/ts/cut-lab';` for the side effect, capture
    `(globalThis as unknown as { DeckFlowCutLab: CutLabApi }).DeckFlowCutLab` in beforeAll, and declare a
    local `interface CutLabApi` typing computePackageCheckboxState, isLandRole, and buildCutLabStateJson.
    Write describe/it blocks covering every behavior row above. For buildCutLabStateJson, JSON.parse the
    result and assert the exact camelCase keys are present (e.g. `expect(Object.keys(parsed.pool[0]))` contains
    name/quantity/typeLine/isCommander/isLocked/packageId) and that a commander entry comes back isLocked=true.
    No `any` types (mirror the FlagFilterApi typing convention).
  </action>
  <verify>
    <automated>cd DeckFlow.Web && npx --no-install vitest run ts-tests/cut-lab-lock-interactions.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "DeckFlowCutLab" DeckFlow.Web/ts-tests/cut-lab-lock-interactions.test.ts` returns ≥1
    - `grep -c "import '../wwwroot/ts/cut-lab'" DeckFlow.Web/ts-tests/cut-lab-lock-interactions.test.ts` returns 1
    - A test asserts computePackageCheckboxState([true,false]) === 'indeterminate'
    - A test asserts buildCutLabStateJson output has camelCase pool keys and a locked commander
    - `grep -c ': any' DeckFlow.Web/ts-tests/cut-lab-lock-interactions.test.ts` returns 0
    - `npx --no-install vitest run ts-tests/cut-lab-lock-interactions.test.ts` (or `npm --prefix DeckFlow.Web test`) passes
  </acceptance_criteria>
  <done>Vitest proves the pure interaction logic — package indeterminate calculation, land bulk-lock selection, and camelCase CutLabState serialization with forced commander lock — in the established ts-tests suite.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: cut-lab-smoke.spec.ts end-to-end (flag ON render/import/lock, resubmit persistence, flag OFF 404, theme screenshots)</name>
  <files>DeckFlow.Web/e2e/cut-lab-smoke.spec.ts</files>
  <behavior>
    - With tool.cut-lab.enabled ON, GET /cut-lab renders the form and the form carries data-cache-key="cut-lab"
    - Importing a 101–150 card pool shows the card-count summary and the legality line
    - The commander row checkbox is disabled AND checked and cannot be unlocked; the "Commander · Always locked" badge is visible
    - Locking a card and bulk-locking lands updates the visible locked count
    - After locking a non-commander card and triggering a REAL form resubmission, the lock survives server-side (the re-rendered page shows that card's checkbox still checked) — proving CutLabStateJson carried the edit browser→server→browser
    - Per theme (classic, azorius, nyx) a screenshot of the loaded page is captured at desktop + mobile viewports
    - With tool.cut-lab.enabled OFF, GET /cut-lab returns HTTP 404 and no Home tile links to /cut-lab
  </behavior>
  <read_first>
    - DeckFlow.Web/e2e/deck-history-smoke.spec.ts (the exact template: admin-lock acquire/release, setToolEnabled, serial mode, theme list, per-theme×viewport screenshot loop, flag-OFF 404 + absent-tile assertions)
    - DeckFlow.Web/e2e/support/admin-lock.ts + admin-tools.ts (helpers: acquireAdminLockForTest, releaseAdminLockForTest, setToolEnabled)
    - CLAUDE.md project section (UI testing: never open a browser on the Windows host; run via scripts/run-web-test.sh; DECKFLOW_DISABLE_AUTO_BROWSER; admin flag lock serialization) and memory reference_stale_windows_server_5173
  </read_first>
  <action>
    Create e2e/cut-lab-smoke.spec.ts mirroring deck-history-smoke.spec.ts. Use
    test.describe.configure({ mode: 'serial' }); acquire the admin lock and setToolEnabled(page, 'Cut Lab',
    true) in beforeEach; set it back false and release the lock in afterEach. Screenshot dir
    '../../.planning/ui-design/cut-lab/screenshots'. Tests:
      1. '/cut-lab renders the form when the flag is ON' — assert the form has
         toHaveAttribute('data-cache-key', 'cut-lab').
      2. 'imports a pool, shows count + legality, auto-locks commander, screenshots per theme' — fill the
         split input (paste a ≥101-card decklist fixture with a leading commander), submit "Import pool",
         assert the card-count summary and legality line render, assert the commander checkbox is disabled
         and checked and the badge is visible, exercise a card lock + "Lock all lands" and assert the locked
         count changes, then loop themes×{desktop,mobile} capturing screenshots.
      3. 'a locked card survives a form resubmit' — after import, check a specific non-commander card's lock
         checkbox, then trigger a real resubmission (e.g. edit the Primary plan textarea and click "Import
         pool" again, OR click a re-render/lock-apply submit), and assert on the re-rendered page that the
         same card's lock checkbox is still checked (server-rendered state, not just the client count). This
         is the BLOCKER 1 end-to-end guard.
      4. 'with tool.cut-lab.enabled OFF, /cut-lab returns 404 and the Home tile is absent' — setToolEnabled
         false, expect page.goto('/cut-lab') status .toBe(404), then goto('/') and expect
         locator('.hub-card[href$="/cut-lab"]').toHaveCount(0).
    Do NOT assert against the known restore-desync bug (Pitfall 1, out of scope). Provide a small inline
    decklist fixture (≥101 mainboard cards + 1 leading commander) or reuse an existing e2e fixture if one fits.
  </action>
  <verify>
    <automated>MISSING — requires the web server on :5173 via scripts/run-web-test.sh; run `npx --no-install playwright test cut-lab-smoke.spec.ts` (WSL headless, never a Windows-host browser) at wave merge. Static gate now: `npx --no-install playwright test cut-lab-smoke.spec.ts --list` must enumerate the four tests without a parse error.</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "data-cache-key', 'cut-lab'" DeckFlow.Web/e2e/cut-lab-smoke.spec.ts` returns ≥1
    - A `.toBe(404)` assertion is present for the flag-OFF case
    - The spec asserts the commander checkbox is both disabled and checked
    - The spec includes the resubmit-persistence test (lock a card → real resubmit → assert still checked server-side)
    - The spec includes a theme×viewport screenshot loop (grep for the themes array + screenshot call)
    - `npx --no-install playwright test cut-lab-smoke.spec.ts --list` enumerates the four tests without a parse error
    - Full run green at wave merge via scripts/run-web-test.sh (desktop + mobile, ≥2 themes) — recorded in SUMMARY
  </acceptance_criteria>
  <done>cut-lab-smoke.spec.ts proves the flag-ON render (with the cut-lab cache key), the import→count→legality→commander-auto-lock→bulk-land flow with per-theme×viewport screenshots, the resubmit-persistence of a locked card (BLOCKER 1), and the flag-OFF 404 + absent-tile behavior, all under the admin-lock serialization convention.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser DOM → /cut-lab POST | The lock checkboxes, package fields, and the serialized CutLabStateJson are user-editable before submit; the server is the authority (plan 101-03), the view/script only present + package the payload |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-101-04-01 | Spoofing/Tampering | CSRF on the Cut Lab form POST | mitigate | @Html.AntiForgeryToken() in the form pairs with the controller's [ValidateAntiForgeryToken] (plan 101-03) |
| T-101-04-02 | Tampering | Client editing CutLabStateJson (or the DOM) to unlock the commander | mitigate | cut-lab.ts forces the commander isLocked=true as a courtesy, but the real defense is server-side: CutLabStateSerializer.Deserialize + CutLabPageService both re-apply EnforceCommanderLock (plans 101-02/03). The client payload is never trusted for the commander |
| T-101-04-03 | Elevation of Privilege | Page visible before ready | mitigate | Route stays 404 while tool.cut-lab.enabled is OFF; e2e asserts the 404 + absent Home tile |
| T-101-SC | Tampering | npm/pip/cargo installs | accept | No package installs — Playwright/Vitest/TypeScript already present; zero new dependencies |
</threat_model>

<verification>
- `dotnet build DeckFlow.Web/DeckFlow.Web.csproj` clean (Razor + TS compile via MSBuild target)
- `npx --no-install tsc -p DeckFlow.Web/tsconfig.json --noEmit` clean
- `npm --prefix DeckFlow.Web test` (Vitest) green — includes cut-lab-lock-interactions.test.ts
- `npx --no-install playwright test cut-lab-smoke.spec.ts` green via scripts/run-web-test.sh (WSL headless, DECKFLOW_DISABLE_AUTO_BROWSER=true, never a Windows-host browser), desktop + mobile, ≥2 themes, including the resubmit-persistence test
- Compiled wwwroot/js/cut-lab.js NOT staged/committed (gitignored)
- LF endings preserved; layout CSS in site-common.css only; no new :root tokens; --panel not --theme-surface
</verification>

<success_criteria>
- Cut Lab page renders the split intake, four intent controls, card-count + legality summary, lock table, packages, and commander fallback (INTAKE-01/02, LOCK-01/02/03 surface)
- Commander row is checked+disabled with the always-locked badge and cannot be unlocked from the UI
- Individual lock, package unit lock/unlock, and bulk "lock all lands" work in the browser AND survive a resubmit via CutLabStateJson serialization (BLOCKER 1 closed)
- Vitest covers the pure interaction logic; e2e proves flag-ON render + import/lock/resubmit-persistence/screenshots and flag-OFF 404 + absent tile
- No new packages, no committed compiled JS, no theme-token additions
</success_criteria>

<output>
Create `.planning/workstreams/cut-lab/phases/101-intake-protection-foundation/101-04-SUMMARY.md` when done
</output>
