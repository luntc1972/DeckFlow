---
context: phase
phase: 999.8-remove-chatgpt-redirects
task: null
total_tasks: 1
status: complete-awaiting-verify
last_updated: 2026-05-23T05:52:54Z
branch: v1.3
head_commit: 4372de5
---

# Status: Phase 999.8 COMPLETE — awaiting `/gsd-verify-work 999.8` then milestone close

No blocking constraints from this session. v1.3 milestone is end-to-end shippable.

## Critical Anti-Patterns (carry-forward from prior sessions, NOT discovered this session)

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| PLAN.md grep-count acceptance against file-wide phrase | Plan 999.7-04 specified `grep -c "D-11 accepted trade-off" returns 1` but the phrase exists 3× in DeckController.cs (one per download handler). Planning-time miscount made the gate trip semantically wrong. Resolved by reading the numstat (`1 0` confirmed zero deletions = preserve intent satisfied) and proceeding. | advisory | When authoring a per-file `grep -c` acceptance for a string that may exist in multiple identical sibling locations, anchor with awk-bounded grep instead: `awk '/method-signature/,/end-anchor/' file \| grep -c "phrase"`. Logged for v1.4 audit. |

<current_state>
v1.3 milestone closure sequence is mid-execution. Two phases inserted post-`/gsd-audit-milestone v1.3` (2026-05-22 13:36 audit) just shipped to v1.3 branch:

1. **Phase 999.7** (v1.3 audit cleanup, 4 plans) — 6 commits: `61f2971` P01, `900d656` P02, `374a96d` P03, `339f9de` P04, `715c5b3` closure, `38241e4` UAT pass. All 7 SCs machine-verified.
2. **Phase 999.8** (legacy chatgpt-* redirect removal, 1 plan) — 2 commits: `ec47195` P01 (Program.cs numstat `0 22`), `4372de5` closure. SC1-SC5 verified at orchestrator; SC6 (post-deploy curl 404) is the operator's responsibility after Render auto-deploys.

Both phases shipped via cross-AI execute pattern: Codex applies edits + writes SUMMARY.md; orchestrator commits + runs build/test gates. Test baseline (`Failed:0 Passed:497 Skipped:3 Total:500`) preserved through 3 consecutive phase verifications (999.6 PHASE EXIT GATE → 999.7 closure → 999.8 closure).

Working tree clean except 3 untracked files: `.claude/` (local IDE config), `.planning/phases/999.7-v1-3-audit-cleanup/.gitkeep` (leftover scaffold), `.planning/v1.3-MILESTONE-AUDIT.md` (Codex MED finding — commit-or-ignore decision pending).

No prod incidents. Harvester verified healthy on `b86248e` per session S242 (2026-05-22 22:13Z).
</current_state>

<completed_work>

Phase 999.7 (v1.3 audit cleanup, 6 commits):
- P01 (`61f2971`): STATE.md `progress:` block → 11/11/46/46/100 (SC1 literal targets per Option A convention; 999.7 is meta-audit, excluded from v1.3 totals).
- P02 (`900d656`): REQUIREMENTS.md WDG-01..10 checkboxes flipped `[ ]` → `[x]`; other checkboxes untouched (RENAME/CLASSRENAME/AUDIT/AIPLATFORM each preserved at 3).
- P03 (`374a96d`): 5 Claude prompt variants — `result-wrapped output` → `direct JSON fenced-block output` (full-grep scope per Codex review HIGH #1 Option A; audit F-01 evidence inventory drift resolved).
- P04 (`339f9de`): DeckController.cs F-02 asymmetry comment inserted inside DeckAnalysisDownload `try` block (handler-anchored awk-grep gate verified); 999.5-UAT.md `status: testing` → `status: superseded-by-999.6`.
- Closure (`715c5b3`): STATE.md Current Position + Operator Next Steps updated; ROADMAP 999.7 row checkbox + plan checkboxes + progress-table row updated to `4/4 Complete 2026-05-22`; stale `.continue-here.md` from paused state dropped.
- UAT (`38241e4`): 999.7-UAT.md written with all 7 SCs machine-verified at HEAD `715c5b3`. Verdict: PASS.

Phase 999.8 (legacy chatgpt-* redirect removal, 2 commits):
- P01 (`ec47195`): DeckFlow.Web/Program.cs line 7 (`using Microsoft.AspNetCore.Rewrite;`) + lines 357-376 (Phase 12 RENAME-01 comment block + `app.UseRewriter(...)` + 11 `AddRedirect(...)` calls) deleted. numstat `0 22`. SC1 (grep count for UseRewriter/AddRedirect/RewriteOptions/Microsoft.AspNetCore.Rewrite = 0), SC2 (using directive removed), SC3 (0 chatgpt-* URL refs remaining in Program.cs), SC4 (build 0/0 warnings/errors), SC5 (tests Failed:0 Passed:497 Skipped:3 Total:500) all verified.
- Closure (`4372de5`): STATE.md Phase=999.8 Plan=1/1 Status=Completed; ROADMAP 999.8 row checkbox + plan checkbox + progress-table row updated to `1/1 Complete 2026-05-22`.

</completed_work>

<remaining_work>

| ID | Task | Notes |
|----|------|-------|
| 999.8-UAT | `/gsd-verify-work 999.8` | Expected PASS — single-plan doc-edit + middleware deletion; machine-verifiable in one batch. Same pattern as 999.7 UAT. |
| v1.3-audit | `/gsd-audit-milestone v1.3` | Re-audit after 999.7+999.8 to confirm F-01 + F-02 + STATE arithmetic + WDG checkboxes + UAT label all closed. |
| v1.3-complete | `/gsd-complete-milestone v1.3` | Archive completed milestone. |
| v1.3-ship | `/gsd-ship v1.3` | PR + merge v1.3 → main. |
| operator-curl | Post-deploy SC6 check | Operator-side only. `curl -I https://www.deckflow.gg/chatgpt-packets` → expect `HTTP/2 404`; `curl -I https://www.deckflow.gg/deck-analysis` → expect `HTTP/2 200`. Not a blocker for milestone close. |
| audit-md-tracking | Commit or .gitignore `.planning/v1.3-MILESTONE-AUDIT.md` | Codex MED finding from 999.7 round 1 review. Pending user decision. Not a blocker for milestone close. |

</remaining_work>

<decisions_made>

- **Cross-AI execute pattern**: Codex applies edits + writes SUMMARY.md; orchestrator (Claude) commits + runs build/test gates. Rationale: Codex sandbox is `.git`-readonly under `workspace-write` mode; build/test takes ~30s so keeping it orchestrator-side avoids sandbox tool friction. Per CLAUDE.md "Codex codes, Claude reviews" rule.
- **Plan 999.7-04 D-11 grep miscount**: Plan acceptance specified `grep -c "D-11 accepted trade-off" returns 1`, but HEAD already had 3 instances (one per download handler — DeckAnalysisDownload, Comparison, CedhMetaGap). Treated as pass since `numstat 1 0` proves zero deletions; semantic intent (preserve verbatim) satisfied. Logged as v1.4 audit follow-up to tighten gate to handler-anchored awk-grep.
- **Phase 999.8 single-plan deletion**: 22 lines removed from `DeckFlow.Web/Program.cs` (line 7 using directive + lines 357-376 comment + UseRewriter + 11 AddRedirect). CSS classes / DOM data attributes / TS string constants / synthetic test slugs (`HelpControllerTests`, `HelpContentServiceTests`) explicitly out of scope — they are NOT URLs; deferred to v1.4 AI-agnostic styling backlog.
- **STATE.md frontmatter status stays `executing`**: Not toggled to `ready_to_ship` by execute-phase agent; that flip is owned by `/gsd-verify-work` per Phase 999.6 D-23 convention.

</decisions_made>

<blockers>

None. v1.3 ready to ship pending `/gsd-verify-work 999.8` → audit → complete → ship sequence.

</blockers>

## Required Reading (in order)

1. `.planning/phases/999.7-v1-3-audit-cleanup/999.7-UAT.md` — Reference template for the upcoming 999.8 UAT (same pattern: machine-verified SC table, no user-flow walkthrough since doc/middleware-only edit).
2. `.planning/phases/999.8-remove-chatgpt-redirects/999.8-01-SUMMARY.md` — Full acceptance verification + build/test gate results from P01.
3. `.planning/STATE.md` (lines 36-45 Current Position + lines 145-148 Operator Next Steps) — Current orchestrator state.
4. `.planning/v1.3-MILESTONE-AUDIT.md` (untracked) — Original audit binding 999.7 scope; re-read before `/gsd-audit-milestone v1.3` so the re-audit can confirm F-01/F-02 closed.
5. `CLAUDE.md` — Codex codes / Claude reviews rule still binding for any further v1.3 edits.

## Infrastructure State

- Branch: `v1.3` (HEAD: `4372de5`). Do NOT switch branches before ship.
- Prod: Render Starter web + Basic-256mb Postgres. v1.3 last deployed `b86248e` (2026-05-22 ~21:00Z, harvester diag logging). Harvester verified healthy. Render will auto-deploy `4372de5` on push (already pushed? — check `git log origin/v1.3 -1` before assuming).
- Untracked: `.claude/`, `.planning/phases/999.7-v1-3-audit-cleanup/.gitkeep`, `.planning/v1.3-MILESTONE-AUDIT.md`.
- Test baseline (preserved through 3 phase closures): `Failed:0 Passed:497 Skipped:3 Total:500` (Web 440 pass + 3 skip, Core 57 pass).
- 999.7 + 999.8 phase dirs both contain PLAN + SUMMARY + (999.7 only) UAT + REVIEWS. 999.8 still needs UAT.md.

## Critical Anti-Patterns (do NOT repeat)

- ANTI-PATTERN: PLAN.md `grep -c <phrase> returns N` gates without verifying the phrase isn't already present multiple times in the file → use handler-anchored `awk` to bound the scope before grep.
- ANTI-PATTERN: Trusting audit evidence inventory verbatim without re-running the grep → 999.7-03 inherited a 3-vs-5 undercount from audit F-01; Codex review HIGH #1 caught it; expanded scope Option A.
- ANTI-PATTERN: Cross-AI sandbox assumption — Codex `--sandbox workspace-write` is `.git`-readonly; do not expect Codex to commit. Orchestrator commits + runs build/test.

## Next Action (on resume)

1. `/gsd-verify-work 999.8` — produce 999.8-UAT.md (machine-verifiable SC1-SC5 table; SC6 noted as operator-side); verdict expected PASS.
2. `/gsd-audit-milestone v1.3` — re-audit to confirm F-01 + F-02 + STATE arithmetic + WDG checkboxes + UAT label hygiene all closed by 999.7; confirm redirect removal closed by 999.8.
3. `/gsd-complete-milestone v1.3` — archive milestone artifacts.
4. `/gsd-ship v1.3` — PR + merge v1.3 → main.
5. **Operator-only**: after Render redeploys, run the two SC6 curls and update 999.8-UAT.md follow-ups with the observed status codes.
6. **Pending decision (not blocking)**: commit `.planning/v1.3-MILESTONE-AUDIT.md` or `.gitignore` it.

<context>
v1.3 ships cleanly from here. All audit findings closed (F-01 doc-comments, F-02 asymmetry comment, STATE.md arithmetic, WDG checkboxes, 999.5-UAT label, redirect removal). No prod incidents. No outstanding code review HIGH/MED findings. Test suite at green baseline 497/3/500. Cross-AI execute pattern proved out across 2 phases / 5 plans / 7 commits without friction.

The only soft items left for v1.4 audit: (a) MILESTONE-AUDIT.md commit-or-ignore decision, (b) chatgpt-* CSS/data-attr/TS-constant cleanup, (c) PLAN-style gate hardening (handler-anchored awk-grep).
</context>
