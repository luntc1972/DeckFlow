---
phase: 999.3-packet-download-session-cache
plan: 03
type: execute
wave: 3
depends_on:
  - 999.3-01
  - 999.3-02
files_modified:
  - DeckFlow.Web/Controllers/DeckController.cs
autonomous: true
requirements:
  - D-10
  - D-11
  - D-12
tags:
  - controller
  - cache
  - dotnet
  - mvc

must_haves:
  truths:
    - "DeckController.DeckAnalysisDownload (line 504 area) calls PacketSessionCache.TryGet BEFORE _deckAnalysisPacketService.BuildAsync at line 512. On hit, the handler builds and returns the zip from the cached DeckAnalysisPacketResult without calling BuildAsync (D-10 precedence step 2)."
    - "DeckController.DeckComparisonDownload (line 727 area) preserves the existing response-json-only short-circuit at lines 705-725 BYTE-IDENTICALLY (D-10 precedence step 1 unchanged), then calls PacketSessionCache.TryGet BEFORE _deckComparisonService.BuildAsync at line 727. On hit, the handler builds and returns the zip from the cached DeckComparisonResult without calling BuildAsync."
    - "DeckController.CedhMetaGapDownload (line 971 area) preserves the existing response-json-only short-circuit at lines 956-969 BYTE-IDENTICALLY, then calls PacketSessionCache.TryGet BEFORE _metaGapService.BuildAsync at line 971. On hit, the handler builds and returns the zip from the cached MetaGapResult without calling BuildAsync."
    - "On cache miss, all three download handlers fall through SILENTLY to their existing BuildAsync call — no user-facing 'session expired' error, no 4xx response, no log noise beyond the structured 'Packet cache miss' line emitted internally by PacketSessionCache (D-11 silent fall-through)."
    - "DeckController constructor accepts PacketSessionCache via DI; the field is `private readonly PacketSessionCache _packetCache;` adjacent to the existing service fields at lines 29-32."
    - "Preview endpoints (DeckAnalysis line 465, DeckComparison line 643, CedhMetaGap line 894) and the upload endpoint (DeckAnalysisUpload line 584) are NOT modified to read from the cache. Reads happen ONLY on the three download handlers (D-10 precedence applies to downloads only)."
    - "Cache-key parity with Plan 02's write site is enforced by code locality: each download handler calls the corresponding service's PUBLIC STATIC `ComputeCacheKey(...)` helper (defined by Plan 02 — DeckAnalysisPacketService.ComputeCacheKey, DeckComparisonService.ComputeCacheKey, MetaGapService.ComputeCacheKey). The controller does NOT construct DeckAnalysisCacheInputs / DeckComparisonCacheInputs / MetaGapCacheInputs records directly — the helper encapsulates that. This guarantees write↔read parity by construction (B4 fix)."
    - "Plan 03 modifies EXACTLY ONE FILE: DeckController.cs. No service file is touched (B4 fix — Option C was deleted; cache-key composition lives in Plan 02's service-side helpers, NOT in the controller, NOT in a Plan 03 service-side refactor)."
    - "dotnet build DeckFlow.sln -c Release exits 0 with current warning count ≤ baseline (read from .planning/phases/999.3-packet-download-session-cache/_baseline-warnings.txt; B8 + W5)."
    - "Grep gate: `grep -c '_packetCache.TryGet' DeckFlow.Web/Controllers/DeckController.cs` returns exactly 3 (one read per download handler — D-14 grep gate)."
  artifacts:
    - path: "DeckFlow.Web/Controllers/DeckController.cs"
      provides: "Three download handlers gain cache-read precedence per D-10; calls Plan 02's static ComputeCacheKey helpers"
      contains: "_packetCache.TryGet"
  key_links:
    - from: "DeckFlow.Web/Controllers/DeckController.cs"
      to: "DeckFlow.Web/Services/PacketSessionCache.cs"
      via: "constructor injection + _packetCache.TryGet in three download handlers"
      pattern: "_packetCache\\.TryGet"
    - from: "DeckFlow.Web/Controllers/DeckController.cs"
      to: "DeckFlow.Web/Services/DeckAnalysisPacketService.ComputeCacheKey"
      via: "static helper call for cache-key composition"
      pattern: "DeckAnalysisPacketService\\.ComputeCacheKey"
    - from: "DeckFlow.Web/Controllers/DeckController.cs"
      to: "DeckFlow.Web/Services/DeckComparisonService.ComputeCacheKey"
      via: "static helper call"
      pattern: "DeckComparisonService\\.ComputeCacheKey"
    - from: "DeckFlow.Web/Controllers/DeckController.cs"
      to: "DeckFlow.Web/Services/MetaGapService.ComputeCacheKey"
      via: "static helper call"
      pattern: "MetaGapService\\.ComputeCacheKey"
    - from: "DeckFlow.Web/Controllers/DeckController.cs"
      to: "PacketArtifactStore (existing — unchanged)"
      via: "Build*Zip helpers called with cached result on hit, fresh result on miss"
      pattern: "PacketArtifactStore\\.Build"
---

<objective>
Layer cache lookups onto the three download endpoints in `DeckController.cs` between the existing response-json-only short-circuits and the BuildAsync fallthrough. This delivers the READ side of the cache — Plan 02 already lands the WRITE side at line 467 / 198 / 196 of each service's BuildAsync, plus the public static `ComputeCacheKey(...)` helper that this plan calls.

Purpose: D-10 mandates download-endpoint precedence as (1) response-json-only short-circuit FIRST (already present at lines 705-725 for deck-comparison and 956-969 for cedh-meta-gap; deck-analysis has the analogous short-circuit INSIDE the service itself so the controller download path needs no controller-side short-circuit edits), (2) packet cache lookup SECOND (this plan), (3) BuildAsync fallthrough THIRD (already present; unchanged). The existing fast paths must remain byte-identical (D-10 + the deck-comparison/cedh-meta-gap "ComparisonResponseJson / MetaGapResponseJson set AND deck source empty" branches).

Per checker B4 + B5: this plan is now Wave 3 (depends_on: [999.3-01, 999.3-02]). The previous "Option C" service-side refactor was deleted; cache-key composition lives in Plan 02's public static `ComputeCacheKey(...)` helpers on each service, and Plan 03 calls those helpers from the controller. Single file modified (DeckController.cs) — no service file is touched by this plan.

Output: One file modified — `DeckController.cs`. Three download handlers gain a TryGet block; the DeckController ctor gains one parameter; one new private readonly field is declared. No service or non-download code changes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/999.3-packet-download-session-cache/999.3-CONTEXT.md
@.planning/phases/999.3-packet-download-session-cache/999.3-PATTERNS.md
@.planning/phases/999.3-packet-download-session-cache/999.3-01-PLAN.md
@.planning/phases/999.3-packet-download-session-cache/999.3-02-PLAN.md
@CLAUDE.md

<interfaces>
<!-- Identifiers and shapes the executor needs. -->

From DeckFlow.Web/Services/PacketSessionCache.cs (Plan 01):
  - `public bool TryGet<TResult>(string key, out TResult? result) where TResult : class` — returns false on miss, true + result on hit.

From DeckFlow.Web/Services/DeckAnalysisPacketService.cs (Plan 02):
  - `public static string ComputeCacheKey(DeckAnalysisRequest request, string normalizedDeckSource, string commander)` — the canonical key-composition helper. Plan 03 calls this from the download handler.

From DeckFlow.Web/Services/DeckComparisonService.cs (Plan 02):
  - `public static string ComputeCacheKey(DeckComparisonRequest request, string normalizedDeckASource, string normalizedDeckBSource)` — controller passes pre-normalized A and B canonical text.

From DeckFlow.Web/Services/MetaGapService.cs (Plan 02):
  - `public static string ComputeCacheKey(MetaGapRequest request, string normalizedDeckSource, string commanderName)` — controller passes pre-normalized deck text and commander name.

From DeckFlow.Web/Controllers/DeckController.cs:
  - Constructor at lines 38-62 takes 12 dependencies, none of which is PacketSessionCache yet.
  - Service fields at lines 29-31: `_deckAnalysisPacketService`, `_deckComparisonService`, `_metaGapService`. The new `_packetCache` field declaration goes adjacent.
  - Existing BuildAsync call sites confirmed by grep:
    * Line 465 — DeckAnalysis (preview) — NOT touched in this plan.
    * Line 512 — DeckAnalysisDownload — gets cache lookup BEFORE this line.
    * Line 584 — DeckAnalysisUpload — NOT touched (Plan 02's service-side write handles D-09 automatically).
    * Line 643 — DeckComparison (preview) — NOT touched.
    * Line 727 — DeckComparisonDownload — gets cache lookup BEFORE this line, AFTER the existing short-circuit at lines 705-725.
    * Line 894 — CedhMetaGap (preview) — NOT touched.
    * Line 971 — CedhMetaGapDownload — gets cache lookup BEFORE this line, AFTER the existing short-circuit at lines 956-969.
  - Existing response-json-only short-circuit excerpts:
    * Lines 705-725 (deck-comparison): `if (string.IsNullOrWhiteSpace(request.DeckASource) && string.IsNullOrWhiteSpace(request.DeckBSource) && !string.IsNullOrWhiteSpace(request.ComparisonResponseJson)) { ... return File(fallbackBytes, ...); }`
    * Lines 956-969 (cedh-meta-gap): analogous shape using `MetaGapResponseJson` and `DeckSource`.
  - Existing post-BuildAsync zip-build pattern (right after each download's BuildAsync call): builds zip bytes via `PacketArtifactStore.Build*Zip(request, result.X, result.Y, ...)` and returns `File(bytes, "application/zip", fileName);`.

From the existing controller pattern (DeckController.cs:38-62) — the canonical ctor injection pattern:
  - Each dependency: typed interface or class via parameter, `_field = parameter;` assignment in body, no null guard (the controller doesn't currently call `ArgumentNullException.ThrowIfNull` on its deps — ASP.NET Core DI guarantees non-null resolution by default for required services). DO NOT add new null guards; match existing style.

URL normalization in the controller (D-02 parity requirement):
  - The download handler MUST normalize DeckSource (URL → canonical text) BEFORE calling ComputeCacheKey, using the same resolution path BuildAsync uses internally. The controller already has access to `IDeckEntryLoader` (or its equivalent loader interface) via the existing DI graph — verify by reading DeckController.cs's existing ctor at lines 38-62.
  - If `IDeckEntryLoader` is NOT already in the controller's ctor, add it as a NEW ctor parameter alongside PacketSessionCache and inject via DI. This is a minor addition — adds one ctor parameter and one field. Read DeckController.cs lines 38-62 to confirm whether the loader is already injected.
  - The controller calls the loader to resolve URL → canonical text, then passes that text into `ComputeCacheKey(request, normalizedDeckSource, ...)`. The commander name is derived from the resolved deck text (same logic the service uses) — if extracting the commander client-side is complex, expose a SECOND tiny helper from the service: `public static string ExtractCommanderName(string canonicalDeckText)` (one-liner that calls the existing parser). However, the recommended path is to use the request's `CommanderName` field directly IF it's populated on the download request (verify by reading DeckAnalysisRequest.cs's CommanderName property — most workflows populate it client-side).
  - If both URL normalization AND commander extraction would force significant controller-side logic, the executor MAY (as a minor structural fallback) lift the existing service-internal normalization step into ANOTHER service-side static helper `ResolveCacheInputsAsync(TRequest request, CancellationToken)` that returns `(string normalizedDeckSource, string commanderName)` — but Plan 03 prefers NOT to touch service files. The cleanest path: reuse `IDeckEntryLoader` injected into the controller for normalization, and use `request.CommanderName` (or analog) directly for the commander.

Compile note: `using DeckFlow.Web.Services;` is already in scope from existing `_deckAnalysisPacketService` field type — no new using directive required.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add PacketSessionCache (+ IDeckEntryLoader if not already injected) to DeckController via DI</name>
  <files>DeckFlow.Web/Controllers/DeckController.cs</files>
  <read_first>
    - DeckFlow.Web/Controllers/DeckController.cs lines 1-80 (top of file — usings, field declarations, constructor signature + body). Understand the field-declaration ordering at lines 29-32 and the ctor-body assignment ordering at lines 57-62. Note whether `IDeckEntryLoader` or analogous URL→canonical resolver is already a ctor dependency.
    - DeckFlow.Web/Services/PacketSessionCache.cs (Plan 01 output).
    - DeckFlow.Web/Services/DeckAnalysisPacketService.cs / DeckComparisonService.cs / MetaGapService.cs (Plan 02 — confirm the `public static string ComputeCacheKey(...)` signatures and what they require for `normalizedDeckSource` / `commander`).
    - .planning/phases/999.3-packet-download-session-cache/999.3-PATTERNS.md lines 396-409 (the existing DeckController ctor pattern and where to insert the new dep).
    - CLAUDE.md "Formatting" — touch ONLY the new field declaration line, the new ctor parameter, and the new field-assignment line. Do NOT reformat the surrounding ctor.
  </read_first>
  <action>
    Modify `DeckFlow.Web/Controllers/DeckController.cs`. Apply additive changes to the top region of the file.

    **CHANGE 1 — Add the private readonly field for the cache.**
    Adjacent to the existing service-field declarations near line 29-32, add:
      `private readonly PacketSessionCache _packetCache;`
    Match the surrounding 4-space indentation. Position: after the three workflow-service fields (`_deckAnalysisPacketService`, `_deckComparisonService`, `_metaGapService`) since the cache logically belongs in the same group — it serves all three.

    **CHANGE 2 — (Conditional) Add `IDeckEntryLoader` field if not already present.**
    Read the existing ctor (lines 38-62) to determine if a deck-source loader is already injected (look for `IDeckEntryLoader`, `IMoxfieldDeckImporter`, `IArchidektDeckImporter`, or similar — controllers in this codebase often delegate URL normalization to a loader interface that wraps both importers).

    - **If the controller already has a loader:** Skip CHANGE 2. Reuse the existing loader instance for the URL→canonical text normalization in Task 2.
    - **If the controller does NOT have a loader:** Add `private readonly IDeckEntryLoader _deckEntryLoader;` alongside `_packetCache`. The corresponding ctor parameter (`IDeckEntryLoader deckEntryLoader`) goes next to `PacketSessionCache packetCache` in CHANGE 3. Use `IDeckEntryLoader` if that's the canonical loader interface name in this codebase — otherwise use the actual interface (read DeckSyncService.cs or analogous service file to confirm the correct loader interface name; the loader is the same one BuildAsync uses internally for URL resolution per D-02).

    **CHANGE 3 — Add the constructor parameter(s) + field assignment.**
    The existing public ctor at lines 38-62 takes 12 dependencies (each on its own line per the existing style). Insert `PacketSessionCache packetCache` as a NEW PARAMETER, positioned after `IMetaGapService metaGapService` (since `_packetCache` is grouped with the workflow services in CHANGE 1). If CHANGE 2 added a loader, insert it adjacent. The new parameter line uses the same indentation as its siblings (8 spaces from start-of-line per the existing pattern at line 38-62).

    Inside the ctor body, add `_packetCache = packetCache;` adjacent to the existing `_metaGapService = metaGapService;` assignment (~line 59). Same indentation as siblings. If CHANGE 2 was applied, also add `_deckEntryLoader = deckEntryLoader;` adjacent.

    DO NOT add `ArgumentNullException.ThrowIfNull(packetCache);` here — the existing controller ctor does NOT use ThrowIfNull on its deps (verified by reading the ctor body). Match existing style.

    DO NOT add a `using DeckFlow.Web.Services;` directive — it is already in scope from the existing `_deckAnalysisPacketService` etc. references (same namespace).

    CLAUDE.md invariants:
    - Touch ONLY the new field declaration(s), the new ctor parameter(s), and the new field-assignment line(s). Do NOT reformat any other line in the ctor.
    - Do NOT reorder the existing 12 dependencies; only INSERT the new one(s) in the position described.
    - Preserve LF endings.

    No build can be run yet — the three download handlers still call BuildAsync directly without consulting `_packetCache`. The field is wired but unused until Task 2 lands.
  </action>
  <verify>
    <automated>
      cd /mnt/c/users/chrislunt/source/personal/deckflow &amp;&amp;
      grep -c "private readonly PacketSessionCache _packetCache" DeckFlow.Web/Controllers/DeckController.cs | grep -qx 1 &amp;&amp;
      grep -c "_packetCache = packetCache;" DeckFlow.Web/Controllers/DeckController.cs | grep -qx 1 &amp;&amp;
      grep -c "PacketSessionCache packetCache" DeckFlow.Web/Controllers/DeckController.cs | grep -qx 1
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "private readonly PacketSessionCache _packetCache" DeckFlow.Web/Controllers/DeckController.cs` returns exactly 1.
    - `grep -c "_packetCache = packetCache;" DeckFlow.Web/Controllers/DeckController.cs` returns exactly 1.
    - `grep -c "PacketSessionCache packetCache" DeckFlow.Web/Controllers/DeckController.cs` returns exactly 1.
    - The new parameter `PacketSessionCache packetCache` appears in the ctor signature after `IMetaGapService metaGapService` (verify by inspection of lines 38-62 area).
    - The new field assignment appears after `_metaGapService = metaGapService;` in the ctor body.
    - If a deck loader was added: corresponding field + parameter + assignment present.
    - `git diff --stat DeckFlow.Web/Controllers/DeckController.cs` shows ~3 additions (or ~6 if loader added), 0 deletions.
  </acceptance_criteria>
  <done>DeckController has `_packetCache` (and optionally `_deckEntryLoader`) available for the three download handlers. Plan 03 Task 2 layers the lookups onto each handler. NO commit yet — Task 2 finishes the logical change and commits together as one unit.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add cache-read precedence to all 3 download handlers (calls Plan 02's static ComputeCacheKey helpers)</name>
  <files>DeckFlow.Web/Controllers/DeckController.cs</files>
  <read_first>
    - DeckFlow.Web/Controllers/DeckController.cs lines 453-552 (deck-analysis preview/download/upload region).
    - DeckFlow.Web/Controllers/DeckController.cs lines 627-810 (deck-comparison preview/download region — pay close attention to the existing short-circuit at lines 705-725 which Plan 03 must preserve verbatim).
    - DeckFlow.Web/Controllers/DeckController.cs lines 878-985 (cedh-meta-gap preview/download region — short-circuit at lines 956-969 must be preserved verbatim).
    - DeckFlow.Web/Services/DeckAnalysisPacketService.cs (Plan 02 Task 1 output — read the `public static string ComputeCacheKey(DeckAnalysisRequest, string, string)` signature and confirm the parameter names).
    - DeckFlow.Web/Services/DeckComparisonService.cs (Plan 02 Task 2 output — read `public static string ComputeCacheKey(DeckComparisonRequest, string, string)`).
    - DeckFlow.Web/Services/MetaGapService.cs (Plan 02 Task 3 output — read `public static string ComputeCacheKey(MetaGapRequest, string, string)`).
    - .planning/phases/999.3-packet-download-session-cache/999.3-PATTERNS.md lines 345-431 (the D-10 layering excerpts for each download handler — the load-bearing structural template).
    - .planning/phases/999.3-packet-download-session-cache/999.3-CONTEXT.md (D-10 precedence rules, D-11 silent fall-through, D-02 URL normalization required for cache-key parity).
    - .planning/phases/999.3-packet-download-session-cache/_baseline-warnings.txt (warning baseline for the build gate).
    - CLAUDE.md "Formatting" — touch ONLY the inside of each download handler. Do NOT reformat surrounding handler bodies.
  </read_first>
  <action>
    Modify `DeckFlow.Web/Controllers/DeckController.cs`. Apply THREE additive changes — one per download handler.

    **THE CARDINAL CACHE-KEY PARITY RULE (B4 fix):**
    Each download handler in this task MUST call the corresponding service's PUBLIC STATIC `ComputeCacheKey(...)` helper to compute the cache key. The controller does NOT construct DeckAnalysisCacheInputs / DeckComparisonCacheInputs / MetaGapCacheInputs records directly — the helper encapsulates that. Single source of truth = parity by construction.

    Calls look like:
    - `var cacheKey = DeckAnalysisPacketService.ComputeCacheKey(request, normalizedDeckSource, commander);`
    - `var cacheKey = DeckComparisonService.ComputeCacheKey(request, normalizedDeckASource, normalizedDeckBSource);`
    - `var cacheKey = MetaGapService.ComputeCacheKey(request, normalizedDeckSource, commanderName);`

    **URL normalization (D-02):** Before calling ComputeCacheKey, the controller normalizes the deck source(s) using the same loader path BuildAsync uses internally. Use the `_deckEntryLoader` injected in Task 1 (or the existing loader if one was already in the ctor). The normalization call is:
    ```csharp
    var normalizedDeckSource = await _deckEntryLoader.ResolveCanonicalTextAsync(request.DeckSource, HttpContext.RequestAborted).ConfigureAwait(false);
    ```
    Confirm the exact method name on the loader interface by reading the loader source file (it may be `ResolveDeckTextAsync`, `LoadAsync`, etc.). Use the EXACT method name the loader actually exposes.

    **Commander source for cache-key parity:**
    - For DeckAnalysis: `request.CommanderName` (the request property — verified by reading DeckAnalysisRequest.cs). If absent or blank, the cache simply misses (D-11 silent fall-through is the correct outcome).
    - For DeckComparison: no single "commander" parameter (the ComputeCacheKey signature is `(request, normalizedA, normalizedB)` only).
    - For MetaGap: `request.CommanderName`.

    **CHANGE 1 — DeckAnalysisDownload (line 504 area).**

    Locate the action method `public async Task<IActionResult> DeckAnalysisDownload(DeckAnalysisRequest request)` (verify name by reading lines 504+). The existing body's relevant region is:
    ```
    try
    {
        var result = await _deckAnalysisPacketService.BuildAsync(request, HttpContext.RequestAborted);  // line 512
        // ... build zip from result ...
        return File(...);
    }
    catch (...) { ... }
    ```

    Insert the cache lookup INSIDE the `try` block, BEFORE the line-512 BuildAsync call:

    ```csharp
    // Phase 999.3 D-10 precedence step 2: cache lookup before BuildAsync.
    var normalizedDeckSource = await _deckEntryLoader.ResolveCanonicalTextAsync(request.DeckSource, HttpContext.RequestAborted).ConfigureAwait(false);
    var cacheKey = DeckAnalysisPacketService.ComputeCacheKey(request, normalizedDeckSource, request.CommanderName ?? string.Empty);

    if (_packetCache.TryGet<DeckAnalysisPacketResult>(cacheKey, out var cachedResult) && cachedResult is not null)
    {
        // Build the zip from the cached result using the SAME PacketArtifactStore call site as the post-BuildAsync block below.
        // Copy lines ~515-530's zip-build code verbatim, substituting `cachedResult` for `result`.
        var bytes = PacketArtifactStore.Build...(request, cachedResult.X, cachedResult.Y, ...);
        return File(bytes, "application/zip", fileName);
    }

    // Cache miss — silent fall-through (D-11). Existing BuildAsync at line 512 runs:
    var result = await _deckAnalysisPacketService.BuildAsync(request, HttpContext.RequestAborted);
    // ... existing zip-build code unchanged ...
    ```

    Read lines 515-530 in the existing source to copy the EXACT PacketArtifactStore.Build call shape and the `File(...)` return statement form.

    DO NOT touch the existing line-512 BuildAsync call or the catch blocks. The cache-hit branch returns early; cache-miss control flow falls through to the existing post-BuildAsync block.

    **CHANGE 2 — DeckComparisonDownload (line 727 area).**

    Locate the action method `public async Task<IActionResult> DeckComparisonDownload(DeckComparisonRequest request)`. The existing body has the response-json-only short-circuit at lines 705-725 (PATTERNS.md lines 347-365 — KEEP VERBATIM, no edits). Then at line 727 the existing BuildAsync call.

    Insert the cache lookup IMMEDIATELY AFTER the existing short-circuit's closing brace at line ~725, BEFORE the line-727 BuildAsync call:

    ```csharp
    // Phase 999.3 D-10 precedence step 2: cache lookup AFTER the existing response-json-only short-circuit, BEFORE BuildAsync.
    var normalizedDeckASource = await _deckEntryLoader.ResolveCanonicalTextAsync(request.DeckASource, HttpContext.RequestAborted).ConfigureAwait(false);
    var normalizedDeckBSource = await _deckEntryLoader.ResolveCanonicalTextAsync(request.DeckBSource, HttpContext.RequestAborted).ConfigureAwait(false);
    var cacheKey = DeckComparisonService.ComputeCacheKey(request, normalizedDeckASource, normalizedDeckBSource);

    if (_packetCache.TryGet<DeckComparisonResult>(cacheKey, out var cachedResult) && cachedResult is not null)
    {
        // Build zip from cachedResult using the same PacketArtifactStore call site the post-line-727 code uses.
        var bytes = PacketArtifactStore.Build...(request, cachedResult.X, cachedResult.Y, ...);
        return File(bytes, "application/zip", fileName);
    }

    // Cache miss — silent fall-through. Existing line-727 BuildAsync call runs:
    var result = await _deckComparisonService.BuildAsync(request, HttpContext.RequestAborted);
    ```

    DO NOT touch lines 705-725 or the line-727 BuildAsync or the catch blocks.

    **CHANGE 3 — CedhMetaGapDownload (line 971 area).**

    Locate the action method `public async Task<IActionResult> CedhMetaGapDownload(MetaGapRequest request)`. Existing body has the response-json-only short-circuit at lines 956-969 — KEEP VERBATIM.

    Insert the cache lookup IMMEDIATELY AFTER the short-circuit's closing brace at line ~969, BEFORE the line-971 BuildAsync call:

    ```csharp
    // Phase 999.3 D-10 precedence step 2: cache lookup AFTER short-circuit, BEFORE BuildAsync.
    var normalizedDeckSource = await _deckEntryLoader.ResolveCanonicalTextAsync(request.DeckSource, HttpContext.RequestAborted).ConfigureAwait(false);
    var cacheKey = MetaGapService.ComputeCacheKey(request, normalizedDeckSource, request.CommanderName ?? string.Empty);

    if (_packetCache.TryGet<MetaGapResult>(cacheKey, out var cachedResult) && cachedResult is not null)
    {
        var bytes = PacketArtifactStore.Build...(request, cachedResult.X, cachedResult.Y, ...);
        return File(bytes, "application/zip", fileName);
    }

    var result = await _metaGapService.BuildAsync(request, HttpContext.RequestAborted);
    ```

    DO NOT touch lines 956-969 or the line-971 BuildAsync or the catch blocks.

    CLAUDE.md invariants:
    - Touch ONLY the three download handlers, and ONLY by INSERTING new blocks. Do NOT modify the existing zip-build blocks after BuildAsync — copy them inside the new cache-hit if-blocks; the post-BuildAsync blocks remain identical for the cache-miss path.
    - Do NOT add new catch blocks. The cache lookup doesn't throw `HttpRequestException` or `InvalidOperationException`. The PacketArtifactStore.Build*Zip helpers called on cache hit are the same helpers the post-BuildAsync path uses — any exceptions there are already handled by the surrounding existing catch blocks.
    - Async method name DOES NOT need an Async suffix — controller actions are exempt per MVC convention (PATTERNS.md line 427-428).
    - Preserve `{ get; init; }` on the three Result records (CLAUDE.md "Formatting" — those records live in service files, not this file, but the rule applies generally).
    - LF endings.

    Build-gate procedure (B8 + W5 — MSBuild summary, not per-line regex):
    1. `dotnet.exe build DeckFlow.sln -c Release > /tmp/999.3-03-build.log 2>&1`
    2. Confirm "Build succeeded." appears.
    3. `CURRENT=$(grep -oP '\d+(?= Warning\(s\))' /tmp/999.3-03-build.log | tail -1)`
    4. `BASELINE=$(cat .planning/phases/999.3-packet-download-session-cache/_baseline-warnings.txt)`
    5. Assert `CURRENT -le BASELINE`.
  </action>
  <verify>
    <automated>
      cd /mnt/c/users/chrislunt/source/personal/deckflow &amp;&amp;
      grep -c "_packetCache.TryGet" DeckFlow.Web/Controllers/DeckController.cs | grep -qx 3 &amp;&amp;
      grep -c "_packetCache.TryGet<DeckAnalysisPacketResult>" DeckFlow.Web/Controllers/DeckController.cs | grep -qx 1 &amp;&amp;
      grep -c "_packetCache.TryGet<DeckComparisonResult>" DeckFlow.Web/Controllers/DeckController.cs | grep -qx 1 &amp;&amp;
      grep -c "_packetCache.TryGet<MetaGapResult>" DeckFlow.Web/Controllers/DeckController.cs | grep -qx 1 &amp;&amp;
      grep -q "DeckAnalysisPacketService.ComputeCacheKey" DeckFlow.Web/Controllers/DeckController.cs &amp;&amp;
      grep -q "DeckComparisonService.ComputeCacheKey" DeckFlow.Web/Controllers/DeckController.cs &amp;&amp;
      grep -q "MetaGapService.ComputeCacheKey" DeckFlow.Web/Controllers/DeckController.cs &amp;&amp;
      grep -c "_deckAnalysisPacketService.BuildAsync" DeckFlow.Web/Controllers/DeckController.cs | grep -qx 3 &amp;&amp;
      grep -c "_deckComparisonService.BuildAsync" DeckFlow.Web/Controllers/DeckController.cs | grep -qx 2 &amp;&amp;
      grep -c "_metaGapService.BuildAsync" DeckFlow.Web/Controllers/DeckController.cs | grep -qx 2 &amp;&amp;
      grep -q "ComparisonResponseJson" DeckFlow.Web/Controllers/DeckController.cs &amp;&amp;
      grep -q "MetaGapResponseJson" DeckFlow.Web/Controllers/DeckController.cs &amp;&amp;
      dotnet.exe build DeckFlow.sln -c Release > /tmp/999.3-03-build.log 2>&amp;1 &amp;&amp;
      grep -q "Build succeeded" /tmp/999.3-03-build.log &amp;&amp;
      CURRENT=$(grep -oP '\d+(?= Warning\(s\))' /tmp/999.3-03-build.log | tail -1) &amp;&amp;
      BASELINE=$(cat .planning/phases/999.3-packet-download-session-cache/_baseline-warnings.txt) &amp;&amp;
      [ "$CURRENT" -le "$BASELINE" ]
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "_packetCache.TryGet" DeckFlow.Web/Controllers/DeckController.cs` returns exactly 3 (one per download handler).
    - `grep -c "_packetCache.TryGet<DeckAnalysisPacketResult>"` returns exactly 1.
    - `grep -c "_packetCache.TryGet<DeckComparisonResult>"` returns exactly 1.
    - `grep -c "_packetCache.TryGet<MetaGapResult>"` returns exactly 1.
    - File contains `DeckAnalysisPacketService.ComputeCacheKey`, `DeckComparisonService.ComputeCacheKey`, `MetaGapService.ComputeCacheKey` — Plan 02's helpers are the cache-key source (B4 fix; controller does NOT construct *CacheInputs records directly).
    - `grep -c "_deckAnalysisPacketService.BuildAsync"` returns exactly 3 (preview line 465, download line 512, upload line 584 — all UNCHANGED).
    - `grep -c "_deckComparisonService.BuildAsync"` returns exactly 2 (preview line 643, download line 727 — both UNCHANGED).
    - `grep -c "_metaGapService.BuildAsync"` returns exactly 2 (preview line 894, download line 971 — both UNCHANGED).
    - The existing short-circuit strings `ComparisonResponseJson` and `MetaGapResponseJson` are still present (response-json-only fast paths preserved — D-10 precedence step 1).
    - `dotnet.exe build DeckFlow.sln -c Release` exits 0 with "Build succeeded." and current warning count ≤ baseline (per `_baseline-warnings.txt`; B8 + W5).
    - Each cache-hit if-block returns `File(bytes, "application/zip", fileName)` (verify by inspection — the cache-hit path delivers the same response shape as the existing post-BuildAsync path).
    - Cache miss does NOT add an `else` branch (D-11 silent fall-through — control flow drops to the existing BuildAsync line below).
    - Plan 03 modifies EXACTLY ONE FILE (DeckController.cs). `git diff --name-only` for Plan 03 shows only `DeckFlow.Web/Controllers/DeckController.cs` (B4 fix — no service files touched).
  </acceptance_criteria>
  <done>All three download handlers consult the cache between the existing short-circuit and BuildAsync, using Plan 02's public static ComputeCacheKey helpers for cache-key parity (B4). Build is green with no new warnings vs baseline. Plan 03 lands as ONE commit per CONTEXT line 142: `feat(999.3): DeckController download handlers consult packet session cache` (covering both Task 1's controller-ctor edit and Task 2's three handler edits — one logical change). Plain default-author commit, no `Co-Authored-By` trailer.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → controller download endpoint | User-submitted request (DeckSource, CommanderName, filters, ResponseJson round-trip) flows into the cache-key composition. Untrusted input is hashed; cache value returned to the user is the SAME server-constructed zip the uncached path would build. |
| controller → service | Cache hit short-circuits the call to BuildAsync (the existing trust boundary between controller and Scryfall pipeline). Cache miss preserves the existing trust boundary (D-11 silent fall-through). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-999.3-12 | Spoofing | Bookmarked download URL serves a stale or wrong-user cache entry | mitigate | Cache key is content-addressed via SHA-256 of the Scryfall-pipeline-relevant fields. A bookmarked URL replay either (a) hits the same cache entry produced by the original user's preview (identical inputs = identical output, same user-perceptible result), (b) hits a TTL-expired entry → falls through to BuildAsync silently, or (c) hits process-restart cold cache → falls through. None of these is a spoofing escalation. |
| T-999.3-13 | Tampering | User crafts ResponseJson that bypasses cache and triggers BuildAsync to write a tampered result | accept | The response-json-only short-circuit (lines 705-725, 956-969) executes BEFORE the cache lookup. A request with ResponseJson set and DeckSource empty goes through the fast path WITHOUT touching the cache — neither write nor read happens. A request with ResponseJson set AND DeckSource set hits the full BuildAsync pipeline (Scryfall validates the deck contents from the DeckSource regardless of ResponseJson) and the cache stores the legitimate result. The user-supplied ResponseJson cannot poison cache contents because BuildAsync ignores it on the Scryfall-pipeline side. |
| T-999.3-14 | Repudiation | n/a | accept | Standard request logging via Serilog (Program.cs:210). Cache lookup events logged structurally per D-13. |
| T-999.3-15 | Information Disclosure | Bookmarked URL bypasses preview/auth — cache returns artifact directly | accept | DeckFlow has no auth on these endpoints (anonymous public app). Cache-hit response is byte-identical to cache-miss response (both are the same zip artifact built from the same inputs). No new disclosure surface. |
| T-999.3-16 | Denial of Service | Adversary spams download URLs to keep the cache populated and starve memory | mitigate | SizeLimit (D-06) caps memory at 10 MB. PostEvictionCallback (D-13) logs the eviction trail. Render Starter 512MB tier safe: 10MB is 2% of total RAM. Existing rate-limiting infrastructure on /feedback (5/hr per IP) is not extended to download endpoints in Phase 999.3 — out of scope per CONTEXT.md exclusions. |
| T-999.3-17 | Elevation of Privilege | Cache hit serves a result that was built under different privilege context | accept | All download endpoints are anonymous (no auth surface). Cache contains anonymous-built artifacts only. No privilege boundary to elevate across. |
| T-999.3-SC | Tampering | npm/pip/cargo installs | accept | No new package installs in Plan 03. |
</threat_model>

<verification>
- `grep -c "_packetCache.TryGet" DeckFlow.Web/Controllers/DeckController.cs` returns exactly 3.
- Each TryGet is typed to the corresponding service Result record (DeckAnalysisPacketResult, DeckComparisonResult, MetaGapResult).
- Each download handler calls Plan 02's `<Service>.ComputeCacheKey(...)` static helper (B4 — Option C deleted; controller does not construct CacheInputs records directly).
- The existing response-json-only short-circuit blocks at lines 705-725 and 956-969 are preserved BYTE-IDENTICALLY (verify via `git diff DeckFlow.Web/Controllers/DeckController.cs` shows zero modifications to those line ranges — additions only).
- All seven existing BuildAsync call sites (preview ×3, download ×3, upload ×1) remain in the file (Plan 03 INSERTS new code; does not REMOVE any existing call sites).
- `dotnet.exe build DeckFlow.sln -c Release` exits 0 with current warning count ≤ baseline (B8 + W5).
- Plan 03 modifies EXACTLY ONE FILE (`git diff --name-only` shows only DeckController.cs).
- D-10, D-11, D-12 truths satisfied per the must_haves list.
</verification>

<success_criteria>
- Plan 03 ships the READ side of the cache on all three download handlers (D-10 precedence step 2).
- Existing response-json-only short-circuits preserved verbatim (D-10 precedence step 1 unchanged).
- BuildAsync fallthrough on miss preserved verbatim (D-10 precedence step 3 unchanged).
- Cache miss is silent — no user-visible error UX, no log noise beyond PacketSessionCache's internal structured log (D-11).
- Cache-key parity with Plan 02's write site is enforced by code locality: controller calls `<Service>.ComputeCacheKey(...)` static helpers; controller does NOT construct CacheInputs records (B4 fix).
- Plan 03 is Wave 3 (depends_on: [999.3-01, 999.3-02]); no service files touched (B5 fix).
- Build is green: `dotnet.exe build DeckFlow.sln -c Release` succeeds with current warning count ≤ baseline (B8 + W5).
- One commit `feat(999.3): DeckController download handlers consult packet session cache`. Plain default-author commit.
</success_criteria>

<output>
Create `.planning/phases/999.3-packet-download-session-cache/999.3-03-SUMMARY.md` when done summarizing:
- Files modified — should be ONLY `DeckFlow.Web/Controllers/DeckController.cs` (B4 fix verified).
- Whether a deck loader was added to the ctor (Task 1 conditional change), and which loader interface name was used.
- Git commit SHA + message.
- `dotnet build` output (one-line success confirmation + current warning count vs baseline).
- Confirmation that the existing response-json-only short-circuit blocks at lines 705-725 and 956-969 are byte-identical pre/post Plan 03 (verify via `git diff` line ranges).
- Confirmation that all seven existing BuildAsync call sites still exist.
- Confirmation that each download handler calls the corresponding `<Service>.ComputeCacheKey(...)` static helper (not constructing CacheInputs records directly).
- Note that Plan 04 (manual UAT + grep gate + README) can now run.
</output>
</content>