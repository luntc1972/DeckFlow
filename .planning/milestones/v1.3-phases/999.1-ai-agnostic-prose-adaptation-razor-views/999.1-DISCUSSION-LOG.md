# Phase 999.1 Discussion Log

**Date:** 2026-05-18
**Mode:** interactive `discuss` (default mode, 1 focused question per area + 1 phrasing refinement)
**Phase:** 999.1 — AI-Agnostic Prose Adaptation in Razor Views

---

## Area selection

**Q:** Phase 999.1 domain: Razor visible prose stops hardcoding 'ChatGPT' so the 3 AI workflow pages read correctly for whichever platform the user picks. Which gray areas do you want to discuss?

**Options presented:**
- Adaptation strategy — Dynamic per-platform vs Universal phrasing vs Hybrid
- Scope boundary — Only .cshtml visible text? Also C# user-facing strings, Help/*.md, data-busy-title, logger lines?
- JudgeQuestions handling — Generalize to multi-AI or keep ChatGPT-only carve-out?
- Per-page prompt-content prose — Touch raw-string prompt bodies in PromptVariants, or strict view-side narration only?

**User selection:** all four areas.

---

## Area 1 — Adaptation strategy

**Q:** Adaptation strategy — how should prose adapt to the user's chosen AI? Phase 15 just shipped `AiPlatform.Normalize(string).DisplayName` so views can read `Model.TargetAiPlatform` and inject the human name. Note: AiPlatform is picked in Step 2 of each page; prose appears both ABOVE and BELOW the selector.

**Options presented:**
- Hybrid (Recommended) — universal phrasing above selector, selected-platform name below
- Dynamic everywhere — every ChatGPT mention becomes `@selectedPlatform.DisplayName`
- Universal everywhere — neutral phrasing throughout, no per-platform branching

**User selection:** **Hybrid (Recommended)**.

**Notes:** Lines up cleanly with the existing 3-step page architecture; avoids the "first render still says ChatGPT" race that Dynamic-everywhere reintroduces.

---

## Area 2 — Scope boundary

**Q:** Scope boundary — the roadmap entry names Razor visible prose. But 'visible prose' bleeds into adjacent surfaces. Which are in scope for 999.1?

**Options presented (multiSelect):**
- C# exception messages (Recommended) — `ResponseParsers` / `MetaGapService` / `DeckComparisonService` throw with hardcoded ChatGPT prose, renders into UI error banner
- `data-busy-title` HTML attributes (Recommended) — visible loading-overlay title
- Help/*.md content (Recommended) — Markdig-rendered help pages, same user-visible surface
- Logger info messages — `_logger.LogInformation("ChatGPT packet ...")`, NOT user-visible; cheap consistency follow-on

**User selection:** all four.

**Notes:** Full scope expansion. Includes logger lines despite not being user-visible — matches Phase 13's class-name rename and reads better in logs.

---

## Area 3 — JudgeQuestions handling

**Q:** `JudgeQuestions.cshtml` — this page has no `_AiSelector` partial. It is positioned as 'real volunteer judges first; ChatGPT as a fast fallback'. The 6 ChatGPT hits include section H2 'ChatGPT prompt generator', warning 'ChatGPT can be confidently wrong about MTG rules', and inline pitches. How should 999.1 treat this page?

**Options presented:**
- Carve out — keep ChatGPT-only (Recommended)
- Generalize — add `_AiSelector` + per-AI plumbing (meaningful scope creep)
- Soften without selector — generalize copy without adding the selector

**User selection:** **Carve out — keep ChatGPT-only (Recommended)**.

**Notes:** Page intent is ChatGPT-the-product-specific (a fast fallback for users who already have ChatGPT open). No selector, no per-AI plumbing. `Home.cshtml` hub card for JudgeQuestions also stays ChatGPT-named for consistency. `Help/ask-a-judge.md` follows the same carve-out.

---

## Area 4 — Per-page prompt-content prose

**Q:** Per-page prompt-content prose — the prompt FILES the user copies into AI (analysis.txt, comparison prompt, follow-up prompt, etc.) are emitted by Phase 15 PromptVariants. The variants per family are already per-platform, so the prompt BODY already knows which AI it targets. But the surrounding Razor view text describes what the prompt is. Does 999.1 touch the view prose describing what the prompt does?

**Options presented:**
- View-side narration only (Recommended) — clean boundary; PromptVariants raw-string literals stay Phase 15 responsibility
- Also sweep raw-string prompts — re-opens Phase 15 surface; CLAUDE.md forbids re-indenting raw-string literals
- Sweep PromptVariants only if grep finds drift — verification-gate hybrid

**User selection:** **View-side narration only (Recommended)**.

**Notes:** Verification grep `grep -nE 'ChatGPT|ChatGpt' DeckFlow.Web/Services/PromptBuilders/` runs at end of phase; any hits in non-ChatGPT-variant files = Phase 15 follow-up backlog item, NOT 999.1 fix.

---

## Refinement — Universal noun convention

**Q:** Universal-phrasing convention — in Hybrid mode, the ABOVE-selector prose needs a consistent neutral noun. Three lede lines already use 'ChatGPT, Claude, or Gemini' (Phase 12 work). But sub-prose like 'paste into ChatGPT' needs a replacement noun. Which token convention should downstream agents standardize on?

**Options presented:**
- `your AI` (Recommended)
- `the AI`
- `ChatGPT, Claude, or Gemini` enumerated
- `your AI assistant`

**User selection:** **`your AI` (Recommended)**.

**Notes:** Short, possessive, implies user agency. Page-lede enumerated form stays (Phase 12 work); `your AI` applies to sub-prose only.

---

## Claude's Discretion items (captured in CONTEXT.md, recorded here for audit)

- Exact phrasing per substituted sentence — pick the variant that reads naturally; "the AI" sometimes wins over "your AI" in C# exception messages already starting with "The submitted X response..."
- Where in each view to place the `var aiPlatform = AiPlatform.Normalize(Model.TargetAiPlatform);` Razor block — top with other `@{` blocks
- Whether to inline `@aiPlatform.DisplayName` or use a local string variable for repeated injection
- Order of file edits — alphabetical or page-by-page (with help.md sibling) — plan author picks
- Audit `data-busy-message` alongside `data-busy-title` rename; both attributes need verification

---

## Deferred ideas

- Generalize JudgeQuestions to multi-AI (would require new `JudgeQuestionPromptVariants` family) — v1.4 backlog candidate if telemetry warrants
- Phase 13 D-08 TS/CSS-coupled identifier sweep (`data-cache-key="chatgpt-packets"`, `chatgpt-packets-form` class, `parseChatGptDownloadFilename` TS const) — separate refactor with TS test surface
- Remove `NoWarn 1591;1573;1587` from `DeckFlow.Web.csproj` after assembly-wide doc-comment coverage
- Phase 15 follow-up if PromptVariants grep gate finds ChatGPT mentions in non-ChatGPT-variant files
