---
phase: 110-cut-lab-navigation-and-pool-discovery
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - DeckFlow.Web/Views/Deck/CutLab.cshtml
  - DeckFlow.Web/wwwroot/ts/cut-lab.ts
  - DeckFlow.Web/wwwroot/css/site-common.css
  - DeckFlow.Web/ts-tests/cut-lab-section-collapse.test.ts
autonomous: true
requirements: [CLUP-13]
must_haves:
  truths:
    - "Each primary Cut Lab section (Lock your pool, Structural findings, Role floors, Cut rounds, Tune quantities, Cuts made) can collapse and expand"
    - "Collapsed state is remembered across page reloads via localStorage"
    - "With JavaScript disabled, sections still render and the native <details> still collapse/expand"
    - "Desktop defaults to everything open; mobile keeps the three auxiliary sections collapsed as today"
  artifacts:
    - path: "DeckFlow.Web/Views/Deck/CutLab.cshtml"
      provides: "Stable section ids + <details class='cutlab-collapsible'> wrappers on the D-03/D-22 section list"
      contains: "cutlab-collapsible"
    - path: "DeckFlow.Web/wwwroot/ts/cut-lab.ts"
      provides: "Section-collapse persistence under deckflow.cutlab.sections"
      contains: "deckflow.cutlab.sections"
  key_links:
    - from: "DeckFlow.Web/wwwroot/ts/cut-lab.ts"
      to: "localStorage['deckflow.cutlab.sections']"
      via: "getLocalStorage() read/write on toggle"
      pattern: "deckflow\\.cutlab\\.sections"
---

<objective>
Make the primary Cut Lab sections collapsible with remembered state (CLUP-13), and give every
section on the D-03/D-22 list a stable id. This plan owns the page's section structure; plan 110-03
(jump nav) links to the ids created here, and plan 110-03's jump handler auto-expands the collapsed
`<details>` created here (D-24).

Purpose: the ~14,000px mobile scroll is unmanageable; collapse lets users fold sections they are done
with. Reuse the existing `.cutlab-collapsible` / `data-cutlab-mobile-collapse` `<details>` pattern
(already wrapping Packages, Scenarios, What-if swap) rather than inventing a mechanism (D-22).
Output: collapsible sections + page-scoped persistence.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/110-cut-lab-navigation-and-pool-discovery/110-CONTEXT.md
@.planning/phases/110-cut-lab-navigation-and-pool-discovery/110-UI-SPEC.md

<interfaces>
<!-- Existing patterns the executor reuses directly. -->

CutLab.cshtml existing collapsible (do NOT re-invent):
  <details class="cutlab-collapsible" data-cutlab-mobile-collapse open>
    <summary class="cutlab-collapsible__summary">Packages</summary>
    ...
  (also at Scenarios ~line 813 and What-if swap ~line 835)

Section targets for ids + collapse wrappers (D-03/D-22):
  Step panels: Process / Decide / Goals / Export. NOTE: _WorkflowStepTabs renders
  aria-controls="cut-lab-step-panel-{1..4}" but only cut-lab-step-panel-4 (Export, ~line 654) has
  a matching id today. Panels 1-3 need those ids added (fixes the dangling aria-controls too).
  Sub-sections: "Lock your pool" (~236), "Structural findings" (~474), "Role floors" (~539),
  "Cut rounds" (round-plan section), "Tune quantities" (tuner section), "Cuts made".
  Already collapsible (leave as-is, just confirm ids): Packages, Scenarios, What-if swap.

cut-lab.ts existing helpers to reuse:
  collapseMobileCollapsiblesOnLoad() ~line 242 — collapses all details[data-cutlab-mobile-collapse]
    on (max-width:767px). New restore logic must reconcile with this default (D-23).
  getLocalStorage() ~line 254 (try/catch Storage|null), isQuotaExceededError() ~line 262.
  primer-selection.ts ~line 27 — 'deckflow.*.sections.*' naming + JSON string-array parse pattern.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add stable section ids and wrap the target sections in collapsible <details></name>
  <files>DeckFlow.Web/Views/Deck/CutLab.cshtml, DeckFlow.Web/wwwroot/css/site-common.css</files>
  <read_first>
    - DeckFlow.Web/Views/Deck/CutLab.cshtml (existing .cutlab-collapsible at ~354/813/835; the target sections at ~236, ~474, ~539, ~654 and the Cut rounds / Tune quantities / Cuts made sections)
    - DeckFlow.Web/wwwroot/css/site-common.css (.cutlab-collapsible ~4474, .cutlab-collapsible__summary)
    - .planning/phases/110-cut-lab-navigation-and-pool-discovery/110-CONTEXT.md (D-03, D-21, D-22, D-23)
    - .planning/phases/110-cut-lab-navigation-and-pool-discovery/110-UI-SPEC.md (Component Inventory: Collapsible sections row — no new collapse CSS, only new <details> wrappers)
  </read_first>
  <action>
    Give every section on the D-03/D-22 list a stable id (use descriptive kebab ids, e.g.
    `cut-lab-section-lock-pool`, `cut-lab-section-structural`, `cut-lab-section-role-floors`,
    `cut-lab-section-cut-rounds`, `cut-lab-section-tune`, `cut-lab-section-cuts-made`) and ALSO add the
    missing `cut-lab-step-panel-1`, `cut-lab-step-panel-2`, `cut-lab-step-panel-3` ids to the sections
    the existing step tablist already points at via aria-controls (panel-4 already has its id). Wrap
    each not-yet-collapsible target section body in
    `<details class="cutlab-collapsible" data-cutlab-mobile-collapse open>` with the section's existing
    `<h2>` promoted into `<summary class="cutlab-collapsible__summary">` (trigger label = section name
    only, no instructional copy — Copywriting Contract). Reuse the existing `.cutlab-collapsible` CSS —
    add only minimal spacing rules to site-common.css if a nested `<details>` needs it, using compound
    selectors (e.g. `.result-panel > details.cutlab-collapsible`) so the 11 theme forks loaded after
    site-common.css do not win on source order. Preserve the D-23 defaults: keep the `open` attribute
    server-rendered (desktop-open); the existing mobile-collapse-on-load JS keeps mobile auxiliary
    sections collapsed. No-JS parity: native `<details>` collapse/expand with no script. LF endings;
    changed lines only — do not reflow untouched markup.
  </action>
  <acceptance_criteria>
    - `grep -c "cutlab-collapsible__summary" DeckFlow.Web/Views/Deck/CutLab.cshtml` increases by the
      number of newly wrapped sections (>= 6 total including the 3 pre-existing).
    - `grep -o 'id="cut-lab-step-panel-[1-4]"' DeckFlow.Web/Views/Deck/CutLab.cshtml | sort -u` lists
      all four panel ids (1,2,3,4) — the dangling aria-controls now resolve.
    - `grep -c 'id="cut-lab-section-' DeckFlow.Web/Views/Deck/CutLab.cshtml` returns >= 6.
    - Any new site-common.css rule for these `<details>` uses a compound selector (not a bare
      `.cutlab-collapsible { }` restyle) — verify by inspecting the diff of site-common.css.
    - `dotnet build DeckFlow.Web/DeckFlow.Web.csproj -clp:ErrorsOnly` is clean (Razor compiles).
  </acceptance_criteria>
  <verify>
    <automated>dotnet build DeckFlow.Web/DeckFlow.Web.csproj -clp:ErrorsOnly</automated>
  </verify>
  <done>Target sections have stable ids and native collapsible <details>; no-JS collapse works; Razor compiles clean.</done>
</task>

<task type="auto">
  <name>Task 2: Persist and restore collapse state under deckflow.cutlab.sections</name>
  <files>DeckFlow.Web/wwwroot/ts/cut-lab.ts, DeckFlow.Web/ts-tests/cut-lab-section-collapse.test.ts</files>
  <read_first>
    - DeckFlow.Web/wwwroot/ts/cut-lab.ts (getLocalStorage ~254, isQuotaExceededError ~262, collapseMobileCollapsiblesOnLoad ~242, DOMContentLoaded init ~3198)
    - DeckFlow.Web/wwwroot/ts/primer-selection.ts (~27 key-prefix + parseJsonStringArray/dedupe pattern)
    - DeckFlow.Web/ts-tests/cut-lab-mobile-collapse.test.ts (existing collapse test harness/DOM-setup pattern to mirror; use a distinct filename)
    - .planning/phases/110-cut-lab-navigation-and-pool-discovery/110-CONTEXT.md (D-21, D-23, D-24, D-25)
  </read_first>
  <action>
    Add a page-scoped collapse-persistence module in cut-lab.ts keyed on the single literal
    `deckflow.cutlab.sections` (D-21 — page-scoped, NOT per-deck; store a JSON array of collapsed
    section ids). On init: read via getLocalStorage() (fail-open to defaults on parse/quota error,
    reusing isQuotaExceededError); apply open/closed to `details[data-cutlab-mobile-collapse]` by
    matching the section id. Reconcile with the existing collapseMobileCollapsiblesOnLoad default so a
    stored preference overrides it, but when there is NO stored value the D-23 defaults hold (desktop:
    all open; mobile: three auxiliary sections collapsed). On every `<details>` toggle, write the
    current collapsed-id set back to storage. Do not add a parallel storage wrapper (D-25). LF endings.
    Add a vitest spec (jsdom) asserting: a stored collapsed-id list closes the matching sections on
    init; toggling a section updates storage; a quota/parse failure falls back to defaults without
    throwing.
  </action>
  <acceptance_criteria>
    - `grep -c "deckflow.cutlab.sections" DeckFlow.Web/wwwroot/ts/cut-lab.ts` returns >= 1 and the key
      is a page-scoped literal with no deck-id interpolation (no template-string deck dimension).
    - cut-lab.ts reuses `getLocalStorage(` and `isQuotaExceededError(` (grep both present in the new code path).
    - `DeckFlow.Web/ts-tests/cut-lab-section-collapse.test.ts` exists with tests for restore, persist,
      and fail-open-to-defaults.
    - `cd DeckFlow.Web && npx --no-install vitest run ts-tests/cut-lab-section-collapse.test.ts` passes.
    - `cd DeckFlow.Web && npx --no-install tsc -p tsconfig.json --noEmit` is clean (strict TS).
  </acceptance_criteria>
  <verify>
    <automated>cd DeckFlow.Web && npx --no-install vitest run ts-tests/cut-lab-section-collapse.test.ts</automated>
  </verify>
  <done>Collapse state persists per page across reloads via the reused safe storage wrappers; defaults preserved when unset; vitest + tsc clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser localStorage → page JS | User-scoped, non-authoritative viewing preference read back on load |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-110-03 | Tampering | localStorage 'deckflow.cutlab.sections' value | mitigate | Parse defensively (JSON array of ids only); any malformed/quota/tampered value fails open to D-23 defaults — never throws, never affects lock/package state |
| T-110-04 | Denial of Service | localStorage quota | mitigate | Reuse isQuotaExceededError guard; quota failure is swallowed, collapse simply not persisted |
</threat_model>

<verification>
- Razor compiles; all target sections have ids + collapsible <details>.
- vitest + tsc clean; persistence round-trips and fails open.
- No-JS: native <details> still collapse (manual/e2e coverage lands in plan 110-06).
- Existing cut-lab mobile-collapse e2e assumptions (D-23) remain valid.
</verification>

<success_criteria>
Primary Cut Lab sections collapse/expand, remember state in localStorage per page, and degrade to
working native `<details>` with no JS.
</success_criteria>

<output>
Create `.planning/phases/110-cut-lab-navigation-and-pool-discovery/110-02-SUMMARY.md` when done.
</output>
