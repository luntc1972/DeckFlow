---
phase: 96-stated-rules-distiller
plan: 04
type: execute
wave: 3
depends_on: ["96-03"]
files_modified:
  - DeckFlow.Core/Integration/ILlmDistillationService.cs
  - DeckFlow.Core/Integration/CliLlmDistillationService.cs
  - DeckFlow.Core.Tests/CliLlmDistillationServiceTests.cs
autonomous: true
requirements: [CS-12, CS-15]
must_haves:
  truths:
    - "Four new LLM stage methods (Select/Disambiguate/Decompose/Reduce) exist on the CLI provider and route through the existing UTF-8 process harness"
    - "The OpenAI provider needs zero changes — it inherits the NotSupportedException default, exactly like ClassifyAsync"
    - "Each CLI stage deserializes via the shared retry+balanced-brace path and sanitizes through DistillationValidation"
  artifacts:
    - path: "DeckFlow.Core/Integration/ILlmDistillationService.cs"
      provides: "4 default-throw stage methods (subscription-CLI-only gate)"
      contains: "SelectStatedClaimsAsync"
    - path: "DeckFlow.Core/Integration/CliLlmDistillationService.cs"
      provides: "CLI implementations of the 4 stages via ExtractWithRetryAsync"
      contains: "DecomposeStatedClaimsAsync"
  key_links:
    - from: "CliLlmDistillationService stage methods"
      to: "ExtractWithRetryAsync / BuildStartInfo"
      via: "shared UTF-8 process harness (D-06/CS-15, zero new harness code)"
      pattern: "ExtractWithRetryAsync"
---

<objective>
Implement the four Claimify-stage LLM calls (CS-12) on the CLI provider, routing every call through the SAME `ExtractWithRetryAsync` → `BuildStartInfo` UTF-8 process harness the existing summary/clips/tags calls use — so the CP437 fix (D-06/CS-15) applies by construction, not by new code. The four methods land on `ILlmDistillationService` as default-interface-method throws (subscription-CLI-only gate, mirroring `ClassifyAsync`), so the OpenAI `LlmDistillationService` compiles unchanged and the existing orchestrator `isSubscriptionProvider` gate already covers cost containment.

Purpose: turn the 96-03 schema/prompt/validation contracts into callable, deterministically-testable service methods.
Output: extended interface + CLI adapter + CLI unit tests using the canned process-runner-override seam (no live subprocess).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/96-stated-rules-distiller/96-CONTEXT.md
@.planning/phases/96-stated-rules-distiller/96-RESEARCH.md
@.planning/phases/96-stated-rules-distiller/96-PATTERNS.md
@.planning/phases/96-stated-rules-distiller/96-03-SUMMARY.md

<interfaces>
<!-- Method signatures (LOCKED). Decompose/Reduce thread videoDateUtc so SanitizeStatedRules can stamp provenance (D-04). -->

On ILlmDistillationService, each as a default-interface-method throwing
  NotSupportedException("Stated-rules extraction requires the subscription LLM CLI provider."):

  Task<SelectResult>       SelectStatedClaimsAsync(string transcriptChunk, CancellationToken ct = default)
  Task<DisambiguateResult> DisambiguateStatedClaimsAsync(IReadOnlyList<string> selectedClaims, CancellationToken ct = default)
  Task<DecomposeResult>    DecomposeStatedClaimsAsync(IReadOnlyList<string> disambiguatedClaims, DateTimeOffset videoDateUtc, CancellationToken ct = default)
  Task<ReduceResult>       ReduceStatedRulesAsync(IReadOnlyList<StatedRuleCandidate> allChunkRules, DateTimeOffset videoDateUtc, CancellationToken ct = default)

CLI method bodies mirror ExtractClipsAsync's 3-line shape:
  build instruction from the matching StatedRules* prompt+schema pair → ExtractWithRetryAsync<TPayload> → map/sanitize → return result with new TokenUsage(0,0).
  Disambiguate/Decompose/Reduce serialize their input list (claims or rules) as the "transcript" user-content argument to ExtractWithRetryAsync (JSON via the existing JsonOpts).
  Decompose/Reduce call DistillationValidation.SanitizeStatedRules(payload, videoDateUtc) to produce dated StatedRuleCandidate[].
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the 4 default-interface-method stage gates</name>
  <read_first>
    - DeckFlow.Core/Integration/ILlmDistillationService.cs (ENTIRE file — the ClassifyAsync default-interface-method throw idiom at lines 24-26 to replicate)
    - DeckFlow.Core/Knowledge/DistillationResults.cs (SelectResult/DisambiguateResult/DecomposeResult/ReduceResult from 96-03 — the return types)
    - .planning/phases/96-stated-rules-distiller/96-PATTERNS.md (section "ILlmDistillationService + CliLlmDistillationService" — Analog 1 default gate)
    - .planning/phases/96-stated-rules-distiller/96-RESEARCH.md (Pattern 2 + Open Question 3 — subscription-only gate; the existing DistillAsync isSubscriptionProvider refusal already covers non-dry-run distills)
  </read_first>
  <action>
    Add the 4 methods to `ILlmDistillationService` per the locked <interfaces>, each with the exact `=> Task.FromException<TResult>(new NotSupportedException("Stated-rules extraction requires the subscription LLM CLI provider."))` default body copied from `ClassifyAsync`. Add XML doc on each. Confirm via build that `LlmDistillationService` (OpenAI) requires NO override (it inherits the throw, exactly as it does for ClassifyAsync today) and no existing ILlmDistillationService test double breaks (default bodies keep them compiling).
  </action>
  <verify>
    <automated>dotnet build DeckFlow.Core/DeckFlow.Core.csproj && dotnet build DeckFlow.Web/DeckFlow.Web.csproj</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "NotSupportedException" DeckFlow.Core/Integration/ILlmDistillationService.cs` == 5 (ClassifyAsync + 4 new).
    - `LlmDistillationService.cs` has NO new method (diff shows it untouched).
    - Both Core and Web build clean, 0 new warnings.
  </acceptance_criteria>
  <done>Interface carries 4 new subscription-only stage methods; OpenAI adapter unchanged; solution builds.</done>
</task>

<task type="auto">
  <name>Task 2: Implement the 4 CLI stages + canned-response unit tests</name>
  <read_first>
    - DeckFlow.Core/Integration/CliLlmDistillationService.cs (ENTIRE file — ExtractClipsAsync 3-line body idiom, ExtractWithRetryAsync, BuildInstruction, BuildStartInfo UTF-8 pinning, the internal test-seam constructor with processRunnerOverride)
    - DeckFlow.Core/Knowledge/DistillationValidation.cs (SanitizeStatedRules + payload records from 96-03; the shared JsonOpts for serializing stage inputs)
    - DeckFlow.Core/Knowledge/DistillationSchemas.cs (the 4 StatedRules* schema+prompt pairs from 96-03)
    - DeckFlow.Core.Tests/CliLlmDistillationServiceTests.cs (ENTIRE file — CreateService(Queue<string>), ClaudeEnvelope, WithCommandOverrideAsync helpers to reuse)
    - .planning/phases/96-stated-rules-distiller/96-PATTERNS.md (sections "ILlmDistillationService + CliLlmDistillationService" Analogs 2-3 + "Golden Regression Test" Analog 2 for the seam)
  </read_first>
  <action>
    Implement the 4 methods on `CliLlmDistillationService` per the locked <interfaces>. Each: `ArgumentException.ThrowIfNullOrWhiteSpace`/`ArgumentNullException.ThrowIfNull` guard on input; build user content (raw chunk for Select; `JsonSerializer.Serialize(input, JsonOpts)` of the claims/rules list for the other three); `await ExtractWithRetryAsync<TPayload>(BuildInstruction(DistillationSchemas.StatedRulesXSystemPrompt, DistillationSchemas.StatedRulesXSchema), userContent, ct)`; map/sanitize (Select/Disambiguate → payload.Claims; Decompose/Reduce → `DistillationValidation.SanitizeStatedRules(payload, videoDateUtc)`); `return new XResult(..., new TokenUsage(0, 0))`. Reuse `ExtractWithRetryAsync`/`BuildStartInfo` verbatim — do NOT add a new process-launch path (the UTF-8 harness applies for free, D-06).
    Extend `CliLlmDistillationServiceTests.cs` with one `[Fact]` per stage: queue a `ClaudeEnvelope("""{...}""")` canned response through `CreateService`/`WithCommandOverrideAsync`, invoke the method, assert the deserialized/sanitized result (Select/Disambiguate → claim strings; Decompose/Reduce → StatedRuleCandidate[] with VideoDateUtc stamped from the passed date). No real subprocess.
  </action>
  <verify>
    <automated>dotnet.exe test DeckFlow.Core.Tests --filter FullyQualifiedName~CliLlmDistillationServiceTests</automated>
  </verify>
  <acceptance_criteria>
    - 4 new `[Fact]` tests (one per stage) pass using canned ClaudeEnvelope responses via the process-runner-override seam — zero live `claude` subprocess calls.
    - A Decompose or Reduce test asserts VideoDateUtc on the returned candidates equals the date argument passed in (provenance stamped, D-04).
    - `grep -c "new ProcessStartInfo" DeckFlow.Core/Integration/CliLlmDistillationService.cs` unchanged from before this task (no new process-launch path added).
    - `dotnet build DeckFlow.Core/DeckFlow.Core.csproj` clean, 0 new warnings.
  </acceptance_criteria>
  <done>All 4 CLI stages implemented over the shared UTF-8 harness; canned-response unit tests green; provenance stamped.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI subprocess stdout → payload | Untrusted model JSON parsed post-hoc (no tooling-enforced schema on CLI path) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-96-01 | Tampering | model emits out-of-schema/malicious rule JSON | mitigate | ExtractWithRetryAsync balanced-brace scan + retry; SanitizeStatedRules drops malformed rows; ValidateStatedRules (orchestrator, 96-07) enforces allowlist before persist |
| T-96-04 | DoS (cost) | metered provider running the token-heavy multi-pass | mitigate | Default NotSupportedException gates the 4 stages to the subscription CLI provider; existing DistillAsync isSubscriptionProvider gate already refuses non-dry-run distills on OpenAI |
| T-96-SC | Tampering | package installs | accept | No new packages (BCL + existing OpenAI/CLI plumbing) — legitimacy gate N/A |
</threat_model>

<verification>
- `dotnet build DeckFlow.sln` clean (Core + Web + tests).
- `dotnet.exe test DeckFlow.Core.Tests --filter FullyQualifiedName~CliLlmDistillationServiceTests` green.
- LlmDistillationService.cs untouched.
</verification>

<success_criteria>
The 4 Claimify-stage CLI methods exist, route through the UTF-8 harness, sanitize through DistillationValidation, and are proven by deterministic canned-response tests; the OpenAI adapter is unchanged and the subscription-only gate holds.
</success_criteria>

<output>
Create `.planning/phases/96-stated-rules-distiller/96-04-SUMMARY.md` when done.
</output>
