---
phase: 13-chatgpt-class-rename-summary-doc-comments
plan: 02
type: execute
wave: 2
depends_on:
  - 13-01
files_modified:
  - DeckFlow.Web/Services/ChatGptDeckPacketService.cs
  - DeckFlow.Web/Services/ChatGptDeckComparisonService.cs
  - DeckFlow.Web/Services/ChatGptCedhMetaGapService.cs
  - DeckFlow.Web/Services/ChatGptPacketArtifactStore.cs
  - DeckFlow.Web/Services/ChatGptRequestContextParser.cs
  - DeckFlow.Web/Services/ChatGptResponseParsers.cs
  - DeckFlow.Web/Services/ChatGptJsonTextFormatterService.cs
  - DeckFlow.Web/Services/DeckAnalysisPacketService.cs
  - DeckFlow.Web/Services/DeckComparisonService.cs
  - DeckFlow.Web/Services/MetaGapService.cs
  - DeckFlow.Web/Services/PacketArtifactStore.cs
  - DeckFlow.Web/Services/RequestContextParser.cs
  - DeckFlow.Web/Services/ResponseParsers.cs
  - DeckFlow.Web/Services/JsonTextFormatterService.cs
  - DeckFlow.Web/Program.cs
  - README.md
autonomous: true
requirements:
  - CLASSRENAME-01
  - CLASSRENAME-02
  - CLASSRENAME-03

must_haves:
  truths:
    - "All 7 ChatGpt-prefixed service files in DeckFlow.Web/Services/ are renamed via git mv to AI-agnostic filenames per D-01"
    - "All 13 ChatGpt-prefixed type declarations across those files (3 interfaces + 3 sealed service classes + 3 sealed result records + 4 static helper classes) are renamed in lockstep"
    - "Service constructor signatures (parameter order + parameter types) are preserved byte-identical — only class names + ILogger<T> generic arg + result record names change"
    - "DI registration block in Program.cs L263-295 has every IChatGpt* interface arg and ChatGpt* implementation name + ILogger<ChatGpt*> generic arg renamed in lockstep; constructor parameter list and AddScoped lifetime are byte-identical"
    - "README.md L605/L636/L637 ChatGptDeckPacketService / ChatGptDeckComparisonService references updated to DeckAnalysisPacketService / DeckComparisonService"
    - "Every renamed public type carries a one-sentence /// <summary> doc comment anchored to its actual responsibility, matching the tone of DeckFlow.Web/Services/CardLookupService.cs and CommanderSpellbookService.cs"
    - "ChatGptRequestContextParser preserves its `partial` modifier (uses [GeneratedRegex] source generation per RESEARCH.md Pitfall) — renamed RequestContextParser stays `internal static partial class`"
    - "ChatGptResultWrapInstruction internal const in JsonTextFormatterService is renamed to ResultWrapInstruction (recommended per Open Question 1 in 13-RESEARCH.md L734) — const value string preserved byte-identical"
    - "PacketArtifactStore.cs preserves the 3 `\"chatgpt\"` lowercase string fallback literals at L537/540/543 (D-07 #4 — Phase 10 commit 00e5bdd invariant)"
  artifacts:
    - path: "DeckFlow.Web/Services/DeckAnalysisPacketService.cs"
      provides: "IDeckAnalysisPacketService + sealed class DeckAnalysisPacketService + sealed record DeckAnalysisPacketResult (formerly IChatGptDeckPacketService + ChatGptDeckPacketService + ChatGptDeckPacketResult)"
      contains: "public interface IDeckAnalysisPacketService"
    - path: "DeckFlow.Web/Services/DeckComparisonService.cs"
      provides: "IDeckComparisonService + DeckComparisonService + DeckComparisonResult"
      contains: "public interface IDeckComparisonService"
    - path: "DeckFlow.Web/Services/MetaGapService.cs"
      provides: "IMetaGapService + MetaGapService + MetaGapResult"
      contains: "public interface IMetaGapService"
    - path: "DeckFlow.Web/Services/PacketArtifactStore.cs"
      provides: "Static helper class for zip Build/Load + filename sanitizer (shared across all 3 pages — single bare-helper name per D-01)"
      contains: "internal static class PacketArtifactStore"
    - path: "DeckFlow.Web/Services/RequestContextParser.cs"
      provides: "Internal static partial class with [GeneratedRegex] for parsing 01-request-context.txt zip payload"
      contains: "internal static partial class RequestContextParser"
    - path: "DeckFlow.Web/Services/ResponseParsers.cs"
      provides: "Internal static class for parsing analysis + set-upgrade JSON returns"
      contains: "internal static class ResponseParsers"
    - path: "DeckFlow.Web/Services/JsonTextFormatterService.cs"
      provides: "Public static class for extracting <result>...</result> or fenced-JSON payload"
      contains: "public static class JsonTextFormatterService"
  key_links:
    - from: "DeckFlow.Web/Program.cs"
      to: "DeckFlow.Web/Services/DeckAnalysisPacketService.cs"
      via: "builder.Services.AddScoped<IDeckAnalysisPacketService>(sp => new DeckAnalysisPacketService(...));"
      pattern: "AddScoped<IDeckAnalysisPacketService>"
    - from: "DeckFlow.Web/Program.cs"
      to: "DeckFlow.Web/Services/DeckComparisonService.cs"
      via: "builder.Services.AddScoped<IDeckComparisonService>(sp => new DeckComparisonService(...));"
      pattern: "AddScoped<IDeckComparisonService>"
    - from: "DeckFlow.Web/Program.cs"
      to: "DeckFlow.Web/Services/MetaGapService.cs"
      via: "builder.Services.AddScoped<IMetaGapService>(sp => new MetaGapService(...));"
      pattern: "AddScoped<IMetaGapService>"
---

<objective>
Rename all 7 `ChatGpt*` service files in `DeckFlow.Web/Services/` per D-01, rename all 13 public/internal type declarations inside (3 interfaces + 3 sealed service classes + 3 sealed result records + 4 static helper classes), update the 3 DI registrations in `Program.cs` L263-295 in lockstep, update README.md mentions at L605/L636/L637, and backfill `/// <summary>` XML doc comments on every renamed type. Also rename the internal const `ChatGptResultWrapInstruction` → `ResultWrapInstruction` per Open Question 1 in 13-RESEARCH.md.

Purpose: Bring the service layer in line with the renamed Wave 1 model types (request DTOs + view models + response shapes already renamed). After this wave, all service ctors, all DI registrations, and all internal service code reference the new model type names — but the controller (`DeckController.cs`) and tests still reference the old service/interface names, so the build remains RED until Wave 3 + Wave 4 close the loop.

Output: 7 renamed service files (via `git mv`), 1 updated `Program.cs` DI block, 1 updated `README.md`, 13 renamed types with `<summary>` doc-comments, 1 renamed internal const. **Intermediate red build is acceptable** per D-05.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/13-chatgpt-class-rename-summary-doc-comments/13-CONTEXT.md
@.planning/phases/13-chatgpt-class-rename-summary-doc-comments/13-RESEARCH.md
@.planning/phases/13-chatgpt-class-rename-summary-doc-comments/13-PATTERNS.md
@.planning/phases/13-chatgpt-class-rename-summary-doc-comments/13-01-SUMMARY.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/INTEGRATIONS.md
@CLAUDE.md

<interfaces>
<!-- Canonical service-triplet layout — interface + result record + sealed class in one file. -->
<!-- Source: DeckFlow.Web/Services/CardLookupService.cs (lines 13-42 of current code). -->

```csharp
namespace DeckFlow.Web.Services;

/// <summary>
/// Looks up pasted card names against Scryfall and returns formatted outputs plus missing lines.
/// </summary>
public interface ICardLookupService
{
    /// <summary>
    /// Looks up the provided card list using Scryfall.
    /// </summary>
    Task<CardLookupResult> LookupAsync(string cardList, CancellationToken cancellationToken = default);
}

/// <summary>
/// Returns the results of a card lookup.
/// </summary>
public sealed record CardLookupResult(IReadOnlyList<string> VerifiedOutputs, IReadOnlyList<string> MissingLines);

/// <summary>
/// Looks up card lists via Scryfall's collection endpoint.
/// </summary>
public sealed class ScryfallCardLookupService : ICardLookupService
{
    // ... ctor + methods
}
```

<!-- Canonical static helper layout — public static class with `<summary>`. -->
<!-- Source: DeckFlow.Web/Services/UpstreamErrorMessageBuilder.cs:1-32. -->

```csharp
namespace DeckFlow.Web.Services;

/// <summary>
/// Builds user-facing error messages for failures coming from third-party upstream services.
/// </summary>
public static class UpstreamErrorMessageBuilder
{
    /// <summary>
    /// Builds a deck-sync error message that highlights the upstream site when possible.
    /// </summary>
    /// <param name="request">Original deck sync request.</param>
    /// <param name="exception">Failure to translate.</param>
    public static string BuildDeckSyncMessage(DeckDiffRequest request, Exception exception) { /* ... */ }
}
```

<!-- Canonical `partial` static class with [GeneratedRegex] — for RequestContextParser. -->
<!-- Source: DeckFlow.Core/Normalization/CardNormalizer.cs:5. -->

```csharp
public static partial class CardNormalizer
{
    [GeneratedRegex("...")]
    private static partial Regex SomethingRegex();
}
```

Tone rules:
- Service class summary: verb-object pair, e.g., "Builds analysis + set-upgrade prompt packets for the deck-analysis page."
- Service interface summary: state the verb-object, e.g., "Builds prompt packets for the deck-analysis page."
- Result record summary: state what's returned, e.g., "Returns the results of a deck-analysis packet build."
- Static helper class summary: verb-first, e.g., "Builds, saves, and loads session zip artifacts for the deck-analysis, deck-comparison, and cEDH meta-gap pages."
- `/// <inheritdoc/>` on interface-implementing methods is acceptable per CommanderSpellbookService.cs:84.
- "ChatGPT" as a narrative WORD inside `<summary>` is permitted per D-07 #5.
</interfaces>

<naming_map_D-01_wave2>
File renames (use `git mv`):
| Old path | New path |
|---|---|
| DeckFlow.Web/Services/ChatGptDeckPacketService.cs | DeckFlow.Web/Services/DeckAnalysisPacketService.cs |
| DeckFlow.Web/Services/ChatGptDeckComparisonService.cs | DeckFlow.Web/Services/DeckComparisonService.cs |
| DeckFlow.Web/Services/ChatGptCedhMetaGapService.cs | DeckFlow.Web/Services/MetaGapService.cs |
| DeckFlow.Web/Services/ChatGptPacketArtifactStore.cs | DeckFlow.Web/Services/PacketArtifactStore.cs |
| DeckFlow.Web/Services/ChatGptRequestContextParser.cs | DeckFlow.Web/Services/RequestContextParser.cs |
| DeckFlow.Web/Services/ChatGptResponseParsers.cs | DeckFlow.Web/Services/ResponseParsers.cs |
| DeckFlow.Web/Services/ChatGptJsonTextFormatterService.cs | DeckFlow.Web/Services/JsonTextFormatterService.cs |

Type renames inside the renamed files:
| Old type | New type | Modifier |
|---|---|---|
| IChatGptDeckPacketService | IDeckAnalysisPacketService | public interface |
| ChatGptDeckPacketService | DeckAnalysisPacketService | public sealed class (may be partial — preserve) |
| ChatGptDeckPacketResult | DeckAnalysisPacketResult | sealed record |
| IChatGptDeckComparisonService | IDeckComparisonService | public interface |
| ChatGptDeckComparisonService | DeckComparisonService | public sealed class |
| ChatGptDeckComparisonResult | DeckComparisonResult | sealed record |
| IChatGptCedhMetaGapService | IMetaGapService | public interface |
| ChatGptCedhMetaGapService | MetaGapService | public sealed class |
| ChatGptCedhMetaGapResult | MetaGapResult | sealed record |
| ChatGptPacketArtifactStore | PacketArtifactStore | internal static class |
| ChatGptRequestContextParser | RequestContextParser | internal static partial class (preserve `partial`) |
| ChatGptResponseParsers | ResponseParsers | internal static class |
| ChatGptJsonTextFormatterService | JsonTextFormatterService | public static class |

Internal const rename (per Open Question 1 in 13-RESEARCH.md L734):
- `ChatGptResultWrapInstruction` → `ResultWrapInstruction` (internal const in JsonTextFormatterService.cs:11; const VALUE string preserved byte-identical)
</naming_map_D-01_wave2>

<preservation_list_wave2>
- Service constructor parameter ORDER and parameter TYPES preserved across rename (only the implementing class name changes — `IScryfallRestClientFactory`, `ResiliencePipelineProvider<string>`, `IMoxfieldDeckImporter`, `IArchidektDeckImporter`, `MoxfieldParser`, `ArchidektParser`, `IMechanicLookupService`, `ICommanderBanListService`, `IScryfallSetService`, `ICommanderSpellbookService`, `IEdhTop16Client`, `ILogger<...>` all preserved).
- PacketArtifactStore.cs `"chatgpt"` lowercase fallbacks at L537/540/543 (D-07 #4 — these are AI-segment defaults in zip filename sanitizer, MUST stay).
- RequestContextParser.cs MUST keep `partial` modifier (uses `[GeneratedRegex]` source-generated regex — removing `partial` breaks the build).
- `ParsedRequestContext` (sealed record inside RequestContextParser.cs) and `RestoredComparisonArtifacts` / `RestoredCedhMetaGapArtifacts` (sealed records inside PacketArtifactStore.cs) already DO NOT carry the ChatGpt prefix (verified in 13-RESEARCH.md L399-400) — leave their names unchanged.
- `[InternalsVisibleTo("DeckFlow.Web.Tests")]` in `DeckFlow.Web/AssemblyInfo.cs:3` — DO NOT touch (assembly name reference, not a renamed type).
- All XML doc-comment narrative use of "ChatGPT" describing the AI's role inside `<summary>` blocks is permitted per D-07 #5.
</preservation_list_wave2>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename the 3 page-scoped service triplets (PacketService + DeckComparisonService + MetaGapService) + XML summaries</name>
  <files>
    DeckFlow.Web/Services/ChatGptDeckPacketService.cs → DeckFlow.Web/Services/DeckAnalysisPacketService.cs,
    DeckFlow.Web/Services/ChatGptDeckComparisonService.cs → DeckFlow.Web/Services/DeckComparisonService.cs,
    DeckFlow.Web/Services/ChatGptCedhMetaGapService.cs → DeckFlow.Web/Services/MetaGapService.cs
  </files>
  <read_first>
    DeckFlow.Web/Services/ChatGptDeckPacketService.cs (current state — interface + sealed class [may be `partial`] + result record triplet; ~47 ChatGpt hits per 13-RESEARCH.md L408),
    DeckFlow.Web/Services/ChatGptDeckComparisonService.cs (current state — same triplet; ~38 hits),
    DeckFlow.Web/Services/ChatGptCedhMetaGapService.cs (current state — same triplet; ~27 hits),
    DeckFlow.Web/Services/CardLookupService.cs (canonical analog — interface + result record + sealed class layout per 13-PATTERNS.md Pattern 1),
    DeckFlow.Web/Services/CommanderSpellbookService.cs (canonical analog — records-above-interface variant; use for MetaGapService if existing layout matches),
    .planning/phases/13-chatgpt-class-rename-summary-doc-comments/13-CONTEXT.md (D-01 naming map for service triplets, D-07 preservation list),
    .planning/phases/13-chatgpt-class-rename-summary-doc-comments/13-RESEARCH.md (Wave 2 file enumeration L394-409; DI registration target shape L412-449),
    .planning/phases/13-chatgpt-class-rename-summary-doc-comments/13-PATTERNS.md (Pattern 1 — interface + sealed class + result record triplet rules),
    DeckFlow.Web/Models/DeckAnalysisRequest.cs (renamed in Wave 1 plan 13-01 — verify type name DeckAnalysisRequest is what the renamed packet service will reference),
    DeckFlow.Web/Models/DeckAnalysisResponse.cs (renamed in Wave 1 — verify type names DeckAnalysisResponse / WeakSlot / QuestionAnswer / DeckVersion),
    DeckFlow.Web/Models/SetUpgradeResponse.cs (renamed in Wave 1),
    DeckFlow.Web/Models/DeckComparisonRequest.cs (renamed in Wave 1),
    DeckFlow.Web/Models/DeckComparisonResponse.cs (renamed in Wave 1),
    DeckFlow.Web/Models/MetaGapRequest.cs (renamed in Wave 1),
    DeckFlow.Web/Models/MetaGapResponse.cs (renamed in Wave 1)
  </read_first>
  <action>
    Three commits — one per service file. Each: `git mv` then in-file rename of interface + class + result record + XML summaries + intra-file references to Wave-1-renamed model types.

    File 1 — DeckAnalysisPacketService.cs:
    `git mv DeckFlow.Web/Services/ChatGptDeckPacketService.cs DeckFlow.Web/Services/DeckAnalysisPacketService.cs`. Inside the moved file: rename `IChatGptDeckPacketService` → `IDeckAnalysisPacketService`, rename `ChatGptDeckPacketService` → `DeckAnalysisPacketService` (preserve `sealed` and any `partial` modifier already present — verify before/after that the modifier set matches), rename `ChatGptDeckPacketResult` (sealed record) → `DeckAnalysisPacketResult`. Update every intra-file reference: method signatures using old type names, ILogger<ChatGptDeckPacketService> → ILogger<DeckAnalysisPacketService>, references to renamed Wave-1 model types — `ChatGptDeckRequest` → `DeckAnalysisRequest`, `ChatGptDeckAnalysisResponse` → `DeckAnalysisResponse`, `ChatGptWeakSlot` → `WeakSlot`, `ChatGptQuestionAnswer` → `QuestionAnswer`, `ChatGptDeckVersion` → `DeckVersion`, `ChatGptSetUpgradeResponse` → `SetUpgradeResponse`, `ChatGptSetUpgradeSet` → `SetUpgradeSet`, `ChatGptSetUpgradeTopAdd` → `SetUpgradeTopAdd`, `ChatGptSetUpgradeCardNote` → `SetUpgradeCardNote`, `ChatGptSetUpgradeShortlist` → `SetUpgradeShortlist`. Add `<summary>` doc comments per Pattern 1 — interface: "Builds analysis + set-upgrade prompt packets for the deck-analysis page" (read the method signatures to derive accurate wording); sealed class: "Builds analysis + set-upgrade prompt packets by hydrating decks via Scryfall, banlist, and Commander Spellbook lookups, then composing the JSON-bound prompt artifacts saved to the session zip"; result record: "Returns the results of a deck-analysis packet build." Use `/// <inheritdoc/>` on the class's interface-implementing methods if the existing file did so for analogs. Preserve ctor parameter order + types byte-identical (all 10-11 params: `IScryfallRestClientFactory`, `ResiliencePipelineProvider<string>`, `IMoxfieldDeckImporter`, `IArchidektDeckImporter`, `MoxfieldParser`, `ArchidektParser`, `IMechanicLookupService`, `ICommanderBanListService`, `IScryfallSetService`, `ICommanderSpellbookService`, `ILogger<...>?`). Permitted narrative use of "ChatGPT" in `<summary>` for AI-role wording (D-07 #5). Commit: `refactor(13): rename ChatGptDeckPacketService triplet to DeckAnalysisPacketService with XML summaries`.

    File 2 — DeckComparisonService.cs:
    `git mv DeckFlow.Web/Services/ChatGptDeckComparisonService.cs DeckFlow.Web/Services/DeckComparisonService.cs`. Inside: rename `IChatGptDeckComparisonService` → `IDeckComparisonService`, `ChatGptDeckComparisonService` → `DeckComparisonService`, `ChatGptDeckComparisonResult` → `DeckComparisonResult`. Update intra-file references: ILogger<...> generic arg, Wave-1 model refs `ChatGptDeckComparisonRequest` → `DeckComparisonRequest`, `ChatGptDeckComparisonResponse` → `DeckComparisonResponse`, `ChatGptDeckComparisonRecommendation` → `DeckComparisonRecommendation`. Add `<summary>` per Pattern 1 — interface: "Builds the deck-comparison prompt packet by hydrating two decks side-by-side"; class: anchor to the actual two-deck pipeline logic; result record: "Returns the results of a deck-comparison packet build." Preserve ctor signature byte-identical (8 params per L427-436 in 13-RESEARCH.md). Commit: `refactor(13): rename ChatGptDeckComparisonService triplet to DeckComparisonService with XML summaries`.

    File 3 — MetaGapService.cs:
    `git mv DeckFlow.Web/Services/ChatGptCedhMetaGapService.cs DeckFlow.Web/Services/MetaGapService.cs`. Inside: rename `IChatGptCedhMetaGapService` → `IMetaGapService`, `ChatGptCedhMetaGapService` → `MetaGapService`, `ChatGptCedhMetaGapResult` → `MetaGapResult`. Update intra-file references: ILogger<...>, Wave-1 model refs `ChatGptCedhMetaGapRequest` → `MetaGapRequest`, `ChatGptCedhMetaGapResponse` → `MetaGapResponse`, `ChatGptCedhMetaGapData` → `MetaGapData`, plus all 11 nested-shape names (`ChatGptCedhWinLineSet` → `WinLineSet`, `ChatGptCedhWinLines` → `WinLines`, `ChatGptCedhInteraction` → `Interaction`, `ChatGptCedhSpeed` → `Speed`, `ChatGptCedhManaEfficiency` → `ManaEfficiency`, `ChatGptCedhCoreConvergenceCard` → `CoreConvergenceCard`, `ChatGptCedhMissingStaple` → `MissingStaple`, `ChatGptCedhPotentialCut` → `PotentialCut`, `ChatGptCedhTopAdd` → `TopAdd`, `ChatGptCedhTopCut` → `TopCut`). Add `<summary>` per Pattern 1 — interface: "Builds the cEDH meta-gap prompt packet using edhtop16 reference decks"; class: anchor to actual edhtop16-fetch pipeline; result record: "Returns the results of a cEDH meta-gap packet build." Preserve ctor signature byte-identical (8 params per L437-446 in 13-RESEARCH.md). Do NOT rename `IEdhTop16Client` or `EdhTop16Entry` (those are out of CLASSRENAME scope per D-08). Commit: `refactor(13): rename ChatGptCedhMetaGapService triplet to MetaGapService with XML summaries`.
  </action>
  <verify>
    <automated>test ! -f DeckFlow.Web/Services/ChatGptDeckPacketService.cs && test ! -f DeckFlow.Web/Services/ChatGptDeckComparisonService.cs && test ! -f DeckFlow.Web/Services/ChatGptCedhMetaGapService.cs && test -f DeckFlow.Web/Services/DeckAnalysisPacketService.cs && test -f DeckFlow.Web/Services/DeckComparisonService.cs && test -f DeckFlow.Web/Services/MetaGapService.cs && [ "$(grep -cE 'public interface (IDeckAnalysisPacketService|IDeckComparisonService|IMetaGapService)\b' DeckFlow.Web/Services/DeckAnalysisPacketService.cs DeckFlow.Web/Services/DeckComparisonService.cs DeckFlow.Web/Services/MetaGapService.cs)" = "3" ] && [ "$(grep -rE 'ChatGpt(Deck|Cedh)' DeckFlow.Web/Services/DeckAnalysisPacketService.cs DeckFlow.Web/Services/DeckComparisonService.cs DeckFlow.Web/Services/MetaGapService.cs | wc -l)" = "0" ]</automated>
  </verify>
  <acceptance_criteria>
    - 3 old paths gone, 3 new paths exist (`ls DeckFlow.Web/Services/ChatGptDeck*.cs DeckFlow.Web/Services/ChatGptCedh*.cs 2>&1` returns "No such file" for all three).
    - `grep -c "public interface IDeckAnalysisPacketService" DeckFlow.Web/Services/DeckAnalysisPacketService.cs` returns 1.
    - `grep -c "public interface IDeckComparisonService" DeckFlow.Web/Services/DeckComparisonService.cs` returns 1.
    - `grep -c "public interface IMetaGapService" DeckFlow.Web/Services/MetaGapService.cs` returns 1.
    - `grep -cE "public sealed (partial )?class DeckAnalysisPacketService\b" DeckFlow.Web/Services/DeckAnalysisPacketService.cs` returns 1.
    - `grep -cE "public sealed class DeckComparisonService\b" DeckFlow.Web/Services/DeckComparisonService.cs` returns 1.
    - `grep -cE "public sealed class MetaGapService\b" DeckFlow.Web/Services/MetaGapService.cs` returns 1.
    - `grep -rE "ChatGpt(Deck|Cedh|SetUpgrade|Weak|Question)" DeckFlow.Web/Services/DeckAnalysisPacketService.cs DeckFlow.Web/Services/DeckComparisonService.cs DeckFlow.Web/Services/MetaGapService.cs` returns ZERO lines.
    - `grep -cE '^\s*///\s*<summary>' DeckFlow.Web/Services/DeckAnalysisPacketService.cs` returns at least 3 (interface + class + result record).
    - `grep -c "EdhTop16Entry\|IEdhTop16Client" DeckFlow.Web/Services/MetaGapService.cs` returns at least 1 (preserved, NOT renamed).
    - `git log --oneline -3` shows 3 commits matching `refactor(13): rename ChatGpt.*Service triplet to .*Service with XML summaries`, plain-author.
  </acceptance_criteria>
  <done>Three service file triplets renamed via git mv, 9 types renamed (3 interfaces + 3 sealed classes + 3 result records), all carry one-sentence `<summary>` doc comments per Pattern 1, all ctor signatures + ILogger<T> generic args + intra-file model-type refs updated to Wave-1 renamed names, 3 separate plain-author commits.</done>
</task>

<task type="auto">
  <name>Task 2: Rename the 4 static helper services + XML summaries + ChatGptResultWrapInstruction const rename</name>
  <files>
    DeckFlow.Web/Services/ChatGptPacketArtifactStore.cs → DeckFlow.Web/Services/PacketArtifactStore.cs,
    DeckFlow.Web/Services/ChatGptRequestContextParser.cs → DeckFlow.Web/Services/RequestContextParser.cs,
    DeckFlow.Web/Services/ChatGptResponseParsers.cs → DeckFlow.Web/Services/ResponseParsers.cs,
    DeckFlow.Web/Services/ChatGptJsonTextFormatterService.cs → DeckFlow.Web/Services/JsonTextFormatterService.cs
  </files>
  <read_first>
    DeckFlow.Web/Services/ChatGptPacketArtifactStore.cs (current state — internal static class with zip Build/Load methods; ~13 ChatGpt hits; verify `"chatgpt"` lowercase fallback literals at L537/540/543 will be preserved; verify `RestoredComparisonArtifacts` + `RestoredCedhMetaGapArtifacts` nested records already lack ChatGpt prefix per 13-RESEARCH.md L399),
    DeckFlow.Web/Services/ChatGptRequestContextParser.cs (current state — internal static PARTIAL class with [GeneratedRegex]; ~2 ChatGpt hits; verify `partial` modifier present),
    DeckFlow.Web/Services/ChatGptResponseParsers.cs (current state — internal static class; ~10 ChatGpt hits),
    DeckFlow.Web/Services/ChatGptJsonTextFormatterService.cs (current state — public static class; ~4 ChatGpt hits including the const `ChatGptResultWrapInstruction` at L11),
    DeckFlow.Web/Services/UpstreamErrorMessageBuilder.cs (canonical analog for Pattern 5 static helper),
    DeckFlow.Web/Services/CategorySuggestionMessageBuilder.cs (smaller static-helper variant),
    DeckFlow.Core/Normalization/CardNormalizer.cs (canonical analog for `public static partial class` with `[GeneratedRegex]` — applies to RequestContextParser),
    .planning/phases/13-chatgpt-class-rename-summary-doc-comments/13-CONTEXT.md (D-01 helper-class names; D-07 #4 PacketArtifactStore "chatgpt" preservation; Claude's Discretion #1 on internal helper renames),
    .planning/phases/13-chatgpt-class-rename-summary-doc-comments/13-RESEARCH.md (Wave 2 file enumeration L399-403; Open Question 1 ChatGptResultWrapInstruction rename L734-738; Anti-Pattern: do NOT remove `partial` modifier),
    .planning/phases/13-chatgpt-class-rename-summary-doc-comments/13-PATTERNS.md (Pattern 5 static helper class rules)
  </read_first>
  <action>
    Four commits — one per file. Each: `git mv` then rename type + XML `<summary>` + intra-file refs to Wave-1 renamed model types.

    File 1 — PacketArtifactStore.cs:
    `git mv DeckFlow.Web/Services/ChatGptPacketArtifactStore.cs DeckFlow.Web/Services/PacketArtifactStore.cs`. Inside: rename `internal static class ChatGptPacketArtifactStore` → `internal static class PacketArtifactStore`. Update intra-file refs to Wave-1 model types (`ChatGptDeckRequest` → `DeckAnalysisRequest`, `ChatGptDeckComparisonRequest` → `DeckComparisonRequest`, `ChatGptCedhMetaGapRequest` → `MetaGapRequest`, and all response types). Do NOT rename `RestoredComparisonArtifacts` or `RestoredCedhMetaGapArtifacts` — those nested records already lack the prefix and stay byte-identical per 13-RESEARCH.md L399. Preserve byte-identical the 3 `"chatgpt"` lowercase string literals at L537/540/543 (these are AI-segment fallbacks in zip filename sanitizer — D-07 #4 / Phase 10 commit 00e5bdd invariant). Add class-level `<summary>` per Pattern 5: "Builds, saves, and loads session zip artifacts for the deck-analysis, deck-comparison, and cEDH meta-gap pages, including AI-segment filename sanitization." Add `<summary>` on each public static method using verb-first voice. Commit: `refactor(13): rename ChatGptPacketArtifactStore to PacketArtifactStore with XML summaries (preserve chatgpt AI-segment fallback)`.

    File 2 — RequestContextParser.cs:
    `git mv DeckFlow.Web/Services/ChatGptRequestContextParser.cs DeckFlow.Web/Services/RequestContextParser.cs`. Inside: rename `internal static partial class ChatGptRequestContextParser` → `internal static partial class RequestContextParser`. PRESERVE the `partial` modifier exactly — this class uses `[GeneratedRegex]` source generation; removing `partial` breaks the build (13-RESEARCH.md Anti-Pattern). Do NOT rename `ParsedRequestContext` (sealed record inside the file — already prefixless per 13-RESEARCH.md L400). Add class-level `<summary>` per Pattern 5 with `partial` analog: "Parses the YAML-like `01-request-context.txt` payload from session zips into a `ParsedRequestContext` record." Preserve all `[GeneratedRegex(...)]` attributes byte-identical. Commit: `refactor(13): rename ChatGptRequestContextParser to RequestContextParser (preserve partial modifier) with XML summary`.

    File 3 — ResponseParsers.cs:
    `git mv DeckFlow.Web/Services/ChatGptResponseParsers.cs DeckFlow.Web/Services/ResponseParsers.cs`. Inside: rename `internal static class ChatGptResponseParsers` → `internal static class ResponseParsers`. Update intra-file refs to renamed response model types from Wave 1 (`ChatGptDeckAnalysisResponse` → `DeckAnalysisResponse`, `ChatGptSetUpgradeResponse` → `SetUpgradeResponse`, etc.). Add class-level `<summary>` per Pattern 5: "Parses the analysis and set-upgrade JSON returns from the AI into the strongly-typed response shapes used by the deck-analysis page." Add `<summary>` on each public method. Commit: `refactor(13): rename ChatGptResponseParsers to ResponseParsers with XML summaries`.

    File 4 — JsonTextFormatterService.cs:
    `git mv DeckFlow.Web/Services/ChatGptJsonTextFormatterService.cs DeckFlow.Web/Services/JsonTextFormatterService.cs`. Inside: rename `public static class ChatGptJsonTextFormatterService` → `public static class JsonTextFormatterService`. Per Open Question 1 in 13-RESEARCH.md L734-738 and CONTEXT.md Claude's Discretion #1: rename the internal const `ChatGptResultWrapInstruction` → `ResultWrapInstruction` (the new name is descriptive enough — qualified by the class it lives in). Preserve the const VALUE string byte-identical (it mentions "ChatGPT/Claude/Gemini" as narrative prose — that's permitted under D-07 #5). Add class-level `<summary>` per Pattern 5: "Extracts the `<result>...</result>` payload or fenced-JSON block from AI text returns so the response parsers can consume well-formed JSON." Add `<summary>` on each public static method including any public methods using the renamed const. Commit: `refactor(13): rename ChatGptJsonTextFormatterService to JsonTextFormatterService and ChatGptResultWrapInstruction to ResultWrapInstruction with XML summaries`.
  </action>
  <verify>
    <automated>test ! -f DeckFlow.Web/Services/ChatGptPacketArtifactStore.cs && test ! -f DeckFlow.Web/Services/ChatGptRequestContextParser.cs && test ! -f DeckFlow.Web/Services/ChatGptResponseParsers.cs && test ! -f DeckFlow.Web/Services/ChatGptJsonTextFormatterService.cs && test -f DeckFlow.Web/Services/PacketArtifactStore.cs && test -f DeckFlow.Web/Services/RequestContextParser.cs && test -f DeckFlow.Web/Services/ResponseParsers.cs && test -f DeckFlow.Web/Services/JsonTextFormatterService.cs && grep -q "internal static class PacketArtifactStore" DeckFlow.Web/Services/PacketArtifactStore.cs && grep -q "internal static partial class RequestContextParser" DeckFlow.Web/Services/RequestContextParser.cs && grep -q "internal static class ResponseParsers" DeckFlow.Web/Services/ResponseParsers.cs && grep -q "public static class JsonTextFormatterService" DeckFlow.Web/Services/JsonTextFormatterService.cs && grep -q "ResultWrapInstruction" DeckFlow.Web/Services/JsonTextFormatterService.cs && ! grep -q "ChatGptResultWrapInstruction" DeckFlow.Web/Services/JsonTextFormatterService.cs && [ "$(grep -c '"chatgpt"' DeckFlow.Web/Services/PacketArtifactStore.cs)" -ge "3" ]</automated>
  </verify>
  <acceptance_criteria>
    - 4 old service helper paths gone, 4 new paths exist.
    - `grep -c "internal static class PacketArtifactStore" DeckFlow.Web/Services/PacketArtifactStore.cs` returns 1.
    - `grep -c "internal static partial class RequestContextParser" DeckFlow.Web/Services/RequestContextParser.cs` returns 1 (verify `partial` preserved).
    - `grep -c "internal static class ResponseParsers" DeckFlow.Web/Services/ResponseParsers.cs` returns 1.
    - `grep -c "public static class JsonTextFormatterService" DeckFlow.Web/Services/JsonTextFormatterService.cs` returns 1.
    - `grep -c "ResultWrapInstruction" DeckFlow.Web/Services/JsonTextFormatterService.cs` returns at least 1.
    - `grep -c "ChatGptResultWrapInstruction" DeckFlow.Web/Services/JsonTextFormatterService.cs` returns 0.
    - `grep -cE '"chatgpt"' DeckFlow.Web/Services/PacketArtifactStore.cs` returns at least 3 (D-07 #4 preservation).
    - `grep -rE "ChatGpt" DeckFlow.Web/Services/PacketArtifactStore.cs DeckFlow.Web/Services/RequestContextParser.cs DeckFlow.Web/Services/ResponseParsers.cs DeckFlow.Web/Services/JsonTextFormatterService.cs` returns ZERO matches (except possibly inside doc-comment narrative "ChatGPT" usage which is D-07 #5 permitted — the regex `ChatGpt` would still catch it, but if it only appears inside `///` lines, that's acceptable per allowlist; verify with `grep -vE "^\\s*///"` filter).
    - `grep -rE "ChatGpt" --include="*.cs" DeckFlow.Web/Services/PacketArtifactStore.cs DeckFlow.Web/Services/RequestContextParser.cs DeckFlow.Web/Services/ResponseParsers.cs DeckFlow.Web/Services/JsonTextFormatterService.cs | grep -vE "^[^:]+:\\s*///"` returns ZERO lines (identifier-level ChatGpt eliminated; doc-comment narrative permitted).
    - `git log --oneline -4` shows 4 commits matching `refactor(13): rename ChatGpt.*` for these 4 helper files, plain-author.
  </acceptance_criteria>
  <done>Four static helper service files renamed via git mv, 4 types renamed, `partial` modifier preserved on RequestContextParser, `"chatgpt"` lowercase fallback literals preserved byte-identical in PacketArtifactStore (≥3 hits), internal const `ChatGptResultWrapInstruction` renamed to `ResultWrapInstruction`, all carry one-sentence `<summary>` doc comments, 4 separate plain-author commits.</done>
</task>

<task type="auto">
  <name>Task 3: Update Program.cs DI block + README.md mentions + Wave 2 grep verification</name>
  <files>
    DeckFlow.Web/Program.cs,
    README.md
  </files>
  <read_first>
    DeckFlow.Web/Program.cs (lines 250-310 — verify exact DI block layout at L263-295 per 13-RESEARCH.md L412-449; also verify L300-308 sibling registrations + L322-340 301-redirect block stays untouched),
    README.md (lines 600-640 — verify exact wording at L605/L636/L637 per 13-RESEARCH.md L453-458),
    DeckFlow.Web/Services/DeckAnalysisPacketService.cs (renamed in Task 1 — verify interface/class identifiers exist before referencing in DI),
    DeckFlow.Web/Services/DeckComparisonService.cs (renamed in Task 1),
    DeckFlow.Web/Services/MetaGapService.cs (renamed in Task 1),
    .planning/phases/13-chatgpt-class-rename-summary-doc-comments/13-CONTEXT.md (D-05 wave grouping mentions DI block + README sweep are part of Wave 2),
    .planning/phases/13-chatgpt-class-rename-summary-doc-comments/13-RESEARCH.md (DI registration target shape L412-449; README hits L451-458; CLAUDE.md "README updated when behavior changes" cited at L32),
    .planning/phases/13-chatgpt-class-rename-summary-doc-comments/13-PATTERNS.md (Pattern 6 DI registration rules — identifier-only changes, no structural diff)
  </read_first>
  <action>
    Two file edits + one final grep verification, in one or two commits (commit DI separately from README per "one logical change per commit").

    Edit 1 — Program.cs DI block (L263-295 per 13-RESEARCH.md):
    In each of the 3 `AddScoped<IChatGptX>(sp => new ChatGptX(...))` factory blocks, rename ONLY the three identifier surfaces per Pattern 6:
    - Block 1: `AddScoped<IChatGptDeckPacketService>` → `AddScoped<IDeckAnalysisPacketService>`; `new ChatGptDeckPacketService(` → `new DeckAnalysisPacketService(`; `ILogger<ChatGptDeckPacketService>` → `ILogger<DeckAnalysisPacketService>`.
    - Block 2: `AddScoped<IChatGptDeckComparisonService>` → `AddScoped<IDeckComparisonService>`; `new ChatGptDeckComparisonService(` → `new DeckComparisonService(`; `ILogger<ChatGptDeckComparisonService>` → `ILogger<DeckComparisonService>`.
    - Block 3: `AddScoped<IChatGptCedhMetaGapService>` → `AddScoped<IMetaGapService>`; `new ChatGptCedhMetaGapService(` → `new MetaGapService(`. (No ILogger on MetaGapService block per 13-RESEARCH.md L437-446.)

    Preserve byte-identical: all `sp.GetRequiredService<T>()` and `sp.GetService<ILogger<...>>()` calls (only the ILogger generic arg changes); the `AddScoped<>` lifetime (no change to Singleton); the constructor parameter ORDER and the closing `));` on each block; the lines around the block (L260-262 before, L296+ after); the 301-redirect block at L322-340 (per 13-RESEARCH.md, those reference URL slugs like `chatgpt-packets` which are Phase 12 redirect strings, NOT renamed types — leave untouched). Commit: `refactor(13): update Program.cs DI registrations for renamed ChatGpt* services`.

    Edit 2 — README.md (L605, L636, L637):
    - L605: change `\`ChatGptDeckPacketService\` throttles all Scryfall calls` → `\`DeckAnalysisPacketService\` throttles all Scryfall calls` (preserve surrounding prose byte-identical).
    - L636: change `\`ChatGptDeckPacketService\` parallelizes` → `\`DeckAnalysisPacketService\` parallelizes`.
    - L637: change `\`ChatGptDeckComparisonService\` parses two decklists` → `\`DeckComparisonService\` parses two decklists`.

    DO NOT touch any other line in README.md. Verify with `grep -n "ChatGpt" README.md` after edit — if any hits remain that aren't preserved-D-07 literals (e.g., the `targetAiPlatform` form-field name, AI Platform name strings inside example payloads), surface them in the SUMMARY for the next planner pass. (CLAUDE.md "README updated when behavior changes" cited; per Phase 12 verification log, L637 was the documented Phase-13 surface.)

    Commit: `docs(13): update README.md service names to DeckAnalysisPacketService and DeckComparisonService`.

    Wave 2 final grep verification — run AFTER both edits:
    ```bash
    grep -rEn "ChatGpt[A-Z]" --include="*.cs" DeckFlow.Web/Services/ DeckFlow.Web/Program.cs
    ```
    Expected: ZERO output across `DeckFlow.Web/Services/*.cs` and `DeckFlow.Web/Program.cs`. (Note: doc-comment narrative "ChatGPT" inside `///` lines is permitted but the regex `ChatGpt[A-Z]` requires CamelCase identifier boundaries — narrative "ChatGPT" usually appears as full-word `"ChatGPT"` so it would NOT match `ChatGpt[A-Z]` — see 13-RESEARCH.md L558-562 for the regex semantics.)

    If the verification grep finds anything beyond what allowlist L562-575 in 13-RESEARCH.md permits, fix the leak before declaring complete.
  </action>
  <verify>
    <automated>grep -q "AddScoped<IDeckAnalysisPacketService>" DeckFlow.Web/Program.cs && grep -q "new DeckAnalysisPacketService(" DeckFlow.Web/Program.cs && grep -q "ILogger<DeckAnalysisPacketService>" DeckFlow.Web/Program.cs && grep -q "AddScoped<IDeckComparisonService>" DeckFlow.Web/Program.cs && grep -q "new DeckComparisonService(" DeckFlow.Web/Program.cs && grep -q "AddScoped<IMetaGapService>" DeckFlow.Web/Program.cs && grep -q "new MetaGapService(" DeckFlow.Web/Program.cs && ! grep -qE "AddScoped<IChatGpt" DeckFlow.Web/Program.cs && ! grep -qE "new ChatGpt(Deck|Cedh)" DeckFlow.Web/Program.cs && grep -q "DeckAnalysisPacketService" README.md && grep -q "DeckComparisonService" README.md && ! grep -qE "ChatGptDeck(Packet|Comparison)Service" README.md && [ "$(grep -rEn 'ChatGpt[A-Z]' --include='*.cs' DeckFlow.Web/Services/ DeckFlow.Web/Program.cs | wc -l)" = "0" ]</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "AddScoped<IDeckAnalysisPacketService>" DeckFlow.Web/Program.cs` returns 1.
    - `grep -c "AddScoped<IDeckComparisonService>" DeckFlow.Web/Program.cs` returns 1.
    - `grep -c "AddScoped<IMetaGapService>" DeckFlow.Web/Program.cs` returns 1.
    - `grep -c "new DeckAnalysisPacketService(" DeckFlow.Web/Program.cs` returns 1.
    - `grep -c "new DeckComparisonService(" DeckFlow.Web/Program.cs` returns 1.
    - `grep -c "new MetaGapService(" DeckFlow.Web/Program.cs` returns 1.
    - `grep -cE "AddScoped<IChatGpt" DeckFlow.Web/Program.cs` returns 0.
    - `grep -cE "new ChatGpt(Deck|Cedh)" DeckFlow.Web/Program.cs` returns 0.
    - `grep -c "ILogger<DeckAnalysisPacketService>" DeckFlow.Web/Program.cs` returns 1.
    - `grep -c "ILogger<DeckComparisonService>" DeckFlow.Web/Program.cs` returns 1.
    - `grep -c "DeckAnalysisPacketService" README.md` returns at least 2 (L605 + L636).
    - `grep -c "DeckComparisonService" README.md` returns at least 1 (L637).
    - `grep -cE "ChatGptDeck(Packet|Comparison)Service" README.md` returns 0.
    - Wave 2 grep gate: `grep -rEn "ChatGpt[A-Z]" --include="*.cs" DeckFlow.Web/Services/ DeckFlow.Web/Program.cs` returns ZERO lines.
    - `git log --oneline -2` shows 2 commits: `refactor(13): update Program.cs DI registrations for renamed ChatGpt* services` and `docs(13): update README.md service names to DeckAnalysisPacketService and DeckComparisonService`, plain-author.
    - Program.cs 301-redirect block at L322-340 is BYTE-IDENTICAL to its pre-edit state (the URL strings `chatgpt-packets`, `chatgpt-deck-comparison`, `chatgpt-cedh-meta-gap` are Phase 12 invariants — verify `grep -c "chatgpt-packets" DeckFlow.Web/Program.cs` is unchanged).
  </acceptance_criteria>
  <done>Program.cs DI block at L263-295 has all 3 service registration triplets renamed (interface arg + implementation `new X(...)` + ILogger<T> generic), README.md L605/L636/L637 updated, Wave 2 grep gate ZERO lines, Program.cs 301-redirect block preserved, 2 separate plain-author commits.</done>
</task>

</tasks>

<verification>
**Wave 2 verification (run AT END of plan, before declaring complete):**

```bash
# 1. All 7 old service paths gone, 7 new paths exist
ls DeckFlow.Web/Services/ChatGpt*.cs 2>&1   # Expected: "No such file"
ls DeckFlow.Web/Services/DeckAnalysisPacketService.cs DeckFlow.Web/Services/DeckComparisonService.cs DeckFlow.Web/Services/MetaGapService.cs DeckFlow.Web/Services/PacketArtifactStore.cs DeckFlow.Web/Services/RequestContextParser.cs DeckFlow.Web/Services/ResponseParsers.cs DeckFlow.Web/Services/JsonTextFormatterService.cs

# 2. Wave 2 grep gate
grep -rEn "ChatGpt[A-Z]" --include="*.cs" DeckFlow.Web/Services/ DeckFlow.Web/Program.cs
# Expected: ZERO output

# 3. Preservation grep — `"chatgpt"` lowercase fallback in PacketArtifactStore.cs (D-07 #4)
grep -c '"chatgpt"' DeckFlow.Web/Services/PacketArtifactStore.cs
# Expected: ≥ 3 (L537, L540, L543 per 13-RESEARCH.md)

# 4. Preservation grep — RequestContextParser keeps `partial` modifier
grep "internal static partial class RequestContextParser" DeckFlow.Web/Services/RequestContextParser.cs
# Expected: 1 match (line)

# 5. Const rename confirmed
grep -c "ResultWrapInstruction" DeckFlow.Web/Services/JsonTextFormatterService.cs
# Expected: ≥ 1
grep -c "ChatGptResultWrapInstruction" DeckFlow.Web/Services/JsonTextFormatterService.cs
# Expected: 0

# 6. DI block updated, 301-redirect block preserved
grep -c "AddScoped<IDeckAnalysisPacketService>\|AddScoped<IDeckComparisonService>\|AddScoped<IMetaGapService>" DeckFlow.Web/Program.cs   # Expected: 3
grep -c "chatgpt-packets\|chatgpt-deck-comparison\|chatgpt-cedh-meta-gap" DeckFlow.Web/Program.cs   # Expected: ≥ 3 (Phase 12 redirect-source URLs preserved)

# 7. README updated
grep -cE "ChatGptDeck(Packet|Comparison)Service" README.md   # Expected: 0
grep -c "DeckAnalysisPacketService\|DeckComparisonService" README.md   # Expected: ≥ 3 (L605+L636+L637)

# 8. Git log — 9 plain-author commits (3 service triplets + 4 helpers + 1 DI + 1 README = 9; or fewer if DI+README merged)
git log --oneline -10 | grep -cE '^[a-f0-9]+ (refactor|docs)\(13\):'
# Expected: 9 (or 8 if DI + README merged)
```

**DO NOT run `dotnet build` as a pass criterion in this plan.** Per D-05 the build is RED at end of Wave 2 (controller + views + tests still reference old service interface names). Build-clean gate fires only at end of Wave 4.
</verification>

<success_criteria>
- All 7 ChatGpt-prefixed service files renamed via `git mv` to AI-agnostic names per D-01.
- All 13 type declarations renamed (3 interfaces + 3 sealed service classes + 3 sealed result records + 4 static helper classes).
- `RequestContextParser` retains `partial` modifier.
- `PacketArtifactStore.cs` retains 3 `"chatgpt"` lowercase string literals at L537/540/543 (D-07 #4).
- `ChatGptResultWrapInstruction` const renamed to `ResultWrapInstruction` per Open Question 1.
- Service constructor signatures preserved byte-identical (parameter order + types unchanged).
- Program.cs DI block at L263-295 has 3 service triplets renamed (interface arg + `new X(...)` + `ILogger<T>` generic), no other lines touched.
- Program.cs 301-redirect block at L322-340 BYTE-IDENTICAL (Phase 12 invariant preserved).
- README.md L605/L636/L637 reference renamed service names.
- Every renamed public type carries one-sentence `<summary>` XML doc comment.
- Wave 2 grep gate `grep -rEn "ChatGpt[A-Z]" --include="*.cs" DeckFlow.Web/Services/ DeckFlow.Web/Program.cs` returns ZERO lines.
- 8-9 plain-author commits (no Co-Authored-By trailer).
- Intermediate red build expected per D-05 — DO NOT block on `dotnet build`.
</success_criteria>

<output>
Create `.planning/phases/13-chatgpt-class-rename-summary-doc-comments/13-02-SUMMARY.md` when done. Capture:
- All 7 service file renames + 13 type renames
- 3 DI registration updates in Program.cs
- 3 README.md line updates
- Const `ChatGptResultWrapInstruction` → `ResultWrapInstruction` (decision per Open Question 1)
- Confirmation `partial` modifier preserved on RequestContextParser
- Confirmation 3 `"chatgpt"` literals preserved in PacketArtifactStore
- Wave 2 grep gate result (ZERO hits in Services/ + Program.cs)
- Git commit log (8-9 plain-author commits)
- Forward-looking note: Wave 3 (plan 13-03) renames DeckController.cs action methods + 3 Razor `@model` directives + 2 shared partial enum-value refs; build should compile clean at end of Wave 3 except for test fixture errors.
</output>
