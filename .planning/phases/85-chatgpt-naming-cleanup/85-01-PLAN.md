---
phase: 85-chatgpt-naming-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/85-chatgpt-naming-cleanup/render-baseline-pre85.json
autonomous: true
requirements: [AICLEAN-01, AICLEAN-02, AICLEAN-03]
must_haves:
  truths:
    - "A pre-rename rendered-HTML + computed-style baseline is captured to render-baseline-pre85.json BEFORE any css/ts/Views/.cs edit, so plan 85-05's byte-identical proof has a real prior-state artifact to diff against"
    - "The baseline covers the 6 prompt-building routes and a representative renamed-selector set with chatgpt-substrings normalized to a placeholder token, so the post-rename diff proves only the intended identifier tokens changed"
    - "Captured via the headless run-web-test.sh server only — no Windows-host browser opened"
  artifacts:
    - path: ".planning/phases/85-chatgpt-naming-cleanup/render-baseline-pre85.json"
      provides: "Pre-rename rendered-HTML/computed-style snapshot consumed by 85-05 Task 1 byte-identical diff"
      contains: "prompt-building-routes"
  key_links:
    - from: ".planning/phases/85-chatgpt-naming-cleanup/render-baseline-pre85.json"
      to: "85-05 Task 1 byte-identical proof"
      via: "normalized snapshot diff"
      pattern: "render-baseline-pre85"
---

<objective>
Capture a pre-rename rendered-output baseline for the Phase 85 `chatgpt-*` → `prompt-*` identifier
rename, BEFORE any source edit, so the byte-identical render proof (plan 85-05) is a concrete
before/after diff rather than executor discretion. Mirrors Phase 84's Task-0 baseline pattern
(headless `scripts/run-web-test.sh`, `DECKFLOW_DISABLE_AUTO_BROWSER=true`, one-off
Node + playwright-core scratch script — NOT a tracked dependency).

Purpose: a pure identifier rename must change class/attr/symbol NAMES while leaving every rendered
byte otherwise identical. The only rigorous proof is a normalized snapshot diff against the current
tree. That snapshot must be captured while the tree is still at its pre-rename state.
Output: `render-baseline-pre85.json` committed as the sole change on its commit, before any of the
rename plans (85-02/03/04) run.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/85-chatgpt-naming-cleanup/85-CONTEXT.md
@.planning/phases/85-chatgpt-naming-cleanup/85-RESEARCH.md
@.planning/phases/85-chatgpt-naming-cleanup/85-VALIDATION.md
@CLAUDE.md

<interfaces>
<!-- Reuse Phase 84's proven headless-capture harness. -->
<!-- 84-01-PLAN.md Task 0 captured theme-baseline-pre84.json via a one-off Node + playwright-core -->
<!-- scratch script driving scripts/run-web-test.sh (DECKFLOW_DISABLE_AUTO_BROWSER=true). -->
<!-- Reuse: DeckFlow.Web/e2e/theming.spec.ts (readThemeSnapshot / setThemedViewport / deckflow-theme cookie). -->

The 6 prompt-building routes to snapshot (RESEARCH "Byte-Identical Render Proof Strategy"):
  /deck-analysis  /deck-comparison  /cedh-meta-gap  /deck-primer  /manabase  /judge-questions

Representative renamed selectors to probe getComputedStyle on (values must be pixel-identical
pre/post since only the NAME changes): .chatgpt-score, .chatgpt-step-tab, .chatgpt-packets-form,
.chatgpt-sticky-download, .chatgpt-print-button, [data-chatgpt-ui-mode].
</interfaces>
</context>

<tasks>

<task type="auto" gate="blocking">
  <name>Task 0: Capture pre-rename rendered-output baseline (BEFORE any source edit)</name>
  <files>.planning/phases/85-chatgpt-naming-cleanup/render-baseline-pre85.json</files>
  <read_first>
    - DeckFlow.Web/e2e/theming.spec.ts (readThemeSnapshot / setThemedViewport / deckflow-theme cookie mechanism to reuse)
    - .planning/phases/84-theme-semantic-token-migration/84-01-PLAN.md (Task 0 headless-capture pattern)
    - .planning/phases/85-chatgpt-naming-cleanup/85-RESEARCH.md ("Byte-Identical Render Proof Strategy")
  </read_first>
  <action>
    This task MUST run and commit its artifact BEFORE plans 85-02/03/04 touch any css/ts/Views/.cs file.
    Using a one-off Node + playwright-core scratch script (NOT a tracked dependency) against the headless
    scripts/run-web-test.sh server (DECKFLOW_DISABLE_AUTO_BROWSER=true, NEVER a Windows-host browser),
    capture from the CURRENT (unedited) tree, for each of the 6 prompt-building routes (/deck-analysis,
    /deck-comparison, /cedh-meta-gap, /deck-primer, /manabase, /judge-questions) across a representative
    theme set (Classic + 2-3 forks — sufficient for a pure identifier swap since CSS VALUES never change):
    (a) document.documentElement.outerHTML with every `chatgpt`/`ChatGpt` substring normalized to a fixed
    PLACEHOLDER token (case-insensitively) BEFORE storing, so 85-05's post-rename diff — which normalizes
    `prompt`/`Prompt` to the same token — proves "only the intended identifier tokens changed" rather than
    failing on every intentional rename; (b) getComputedStyle for the representative renamed selectors
    listed in <interfaces> (resolved color/border/background/display/outline), proving the RULE VALUES are
    identical pre/post while only the class NAME changes. Write deterministic sorted JSON (LF endings) to
    .planning/phases/85-chatgpt-naming-cleanup/render-baseline-pre85.json. Commit this file as the FIRST
    and ONLY change on its commit, before any rename edit. Do NOT commit the scratch script (untracked,
    per Phase 84 precedent) and do NOT touch DeckFlow.Web/wwwroot/js (gitignored compiled output).
  </action>
  <acceptance_criteria>
    - render-baseline-pre85.json exists with an entry for each of the 6 routes and the representative selector set, each with a resolved computed value.
    - outerHTML entries have chatgpt/ChatGpt substrings normalized to the placeholder token (no raw `chatgpt` substring remains in the stored HTML) so the post-rename diff is token-symmetric.
    - `git show <baseline commit> --stat` lists ONLY render-baseline-pre85.json (no css/ts/Views/.cs file), proving it was captured pre-rename.
    - Captured via the headless run-web-test.sh server only — no Windows-host browser opened.
  </acceptance_criteria>
  <verify>
    <automated>cd /mnt/c/users/chrislunt/source/personal/deckflow-cycle15 && test -f .planning/phases/85-chatgpt-naming-cleanup/render-baseline-pre85.json && python3 -c "import json,sys; d=json.load(open('.planning/phases/85-chatgpt-naming-cleanup/render-baseline-pre85.json')); s=json.dumps(d).lower(); sys.exit(0 if (len(s)>500 and 'chatgpt' not in s) else 1)" && echo BASELINE_CAPTURED_AND_NORMALIZED</automated>
  </verify>
  <done>Pre-rename normalized render/computed-style baseline captured to a known path and committed before any rename edit.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

N/A — this plan runs a read-only headless snapshot of the app's own rendered output and writes one
JSON artifact under `.planning/`. No source code is modified, no attacker-controllable input is
introduced, no new trust boundary is created.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-85-01-01 | (all) | headless render-snapshot capture + JSON artifact | accept | ASVS L1: read-only local capture; no trust boundary crossed; no package installs (playwright-core scratch script is one-off, not added to any lockfile — supply-chain vector unchanged). |
</threat_model>

<verification>
- Baseline artifact committed BEFORE any rename edit (git show confirms that commit lists only the JSON).
- JSON is normalized (no raw `chatgpt` substring) so 85-05's token-symmetric diff is valid.
- Captured via the headless run-web-test.sh server only.
</verification>

<success_criteria>
- A concrete pre-rename baseline exists for 85-05's byte-identical proof, captured while the tree is
  still at its pre-rename state.
</success_criteria>

<output>
Create `.planning/phases/85-chatgpt-naming-cleanup/85-01-SUMMARY.md` when done. Record the baseline
artifact path, the routes/selectors captured, and confirm the commit contained only the JSON.
</output>
