---
phase: 103-simulation-engine-guided-cut-rounds
plan: 06
type: execute
wave: 4
depends_on: ["103-02", "103-03", "103-04", "103-05"]
files_modified:
  - DeckFlow.Web/Services/CutLab/CutLabPageService.cs
  - DeckFlow.Web.Tests/CutLabPageServiceTests.cs
autonomous: true
requirements: [SIM-01, SIM-02]
must_haves:
  truths:
    - "D-09 gap closed: at pool intake, the resolved ScryfallCardData for the working pool is written to CutLabResolvedCardCache (keyed by pool hash) so no later decision re-fetches Scryfall."
    - "D-12/SIM-02: the original-pool baseline snapshot is computed once at intake and stored in CutLabState.BaselineSnapshot, surviving the serialized round-trip."
    - "CUT-01: the initial round plan is computed at page render via CutLabCutRoundEngine so the first proposal + sticky-bar counts render on the no-JS full-page path too."
    - "The extension reuses the SAME resolved ScryfallCardData the existing role-assignment pass already touches — no duplicate resolution."
  artifacts:
    - path: "DeckFlow.Web/Services/CutLab/CutLabPageService.cs"
      provides: "intake-time cache population + baseline compute/store + initial round plan"
      contains: "CutLabResolvedCardCache"
  key_links:
    - from: "DeckFlow.Web/Services/CutLab/CutLabPageService.cs"
      to: "DeckFlow.Web/Services/CutLab/CutLabBaselineSnapshot.cs"
      via: "compute baseline once at intake, store in CutLabState.BaselineSnapshot"
      pattern: "Baseline"
    - from: "DeckFlow.Web/Services/CutLab/CutLabPageService.cs"
      to: "DeckFlow.Web/Services/CutLab/CutLabResolvedCardCache.cs"
      via: "Set(poolKey, resolvedCards) after resolution"
      pattern: "CutLabResolvedCardCache"
---

<objective>
Extend the existing Cut Lab page-service orchestration so that at pool intake it (1) populates the resolved-card cache from the `ScryfallCardData` it already resolves for role assignment — closing the D-09 performance gap — (2) computes and stores the D-12 baseline snapshot in `CutLabState`, and (3) computes the initial CUT-01 round plan for first render (no-JS path). This is the seam that makes the fast async decision loop possible without re-resolving cards.

Purpose: Without intake-time caching + baseline, the endpoint (Plan 103-07) can't hit the latency budget and the before/after view has no baseline.
Output: Extended `CutLabPageService` + tests, plus the process-result fields the view model needs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/cut-lab/phases/103-simulation-engine-guided-cut-rounds/103-PATTERNS.md
@.planning/workstreams/cut-lab/phases/103-simulation-engine-guided-cut-rounds/103-RESEARCH.md

<interfaces>
Existing (DeckFlow.Web/Services/CutLab/CutLabPageService.cs):
- interface ICutLabPageService { Task<CutLabProcessResult> ProcessAsync(CutLabRequest request, CancellationToken); }
- record CutLabProcessResult { CutLabState? State; string? SerializedStateJson; int CardCount; ...; CutLabStructuralFindingsResult Findings; IReadOnlyDictionary<string, IReadOnlyList<string>> RoleAssignmentsByCardName; ...; bool HasResult; }
- Private pipeline already present: ResolveEntriesAsync → ResolveCardsAsync (ScryfallCardNameIndex) → BuildRoleAssignments(out IReadOnlyList<CutLabAnalyzedCard> analyzedCards, ...) using ScryfallCardFactMapper.ToCardFact per entry. ResolvedCutLabEntry holds ScryfallCardData? Card. BuildState(...) constructs CutLabState.
- Registered Scoped in Program.cs:181.

From prior plans:
- CutLabResolvedCardCache.Set(poolKey, IReadOnlyList<ScryfallCardData>) (Plan 103-03)
- ICutLabSimulationService + CutLabBaselineSnapshot.Build(...) (Plan 103-05)
- CutLabCutRoundEngine.BuildQueue(...) (Plan 103-04)
- CutLabState.BaselineSnapshot / Decisions (Plan 103-02)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Populate resolved-card cache + compute/store baseline at intake</name>
  <read_first>
    - DeckFlow.Web/Services/CutLab/CutLabPageService.cs (ProcessAsync, ResolveEntriesAsync, ResolveCardsAsync, BuildRoleAssignments, BuildState, ResolvedCutLabEntry, the constructor's injected dependencies)
    - DeckFlow.Web/Services/CutLab/CutLabResolvedCardCache.cs (Set/Get + ComputePoolKey)
    - DeckFlow.Web/Services/CutLab/CutLabBaselineSnapshot.cs + CutLabSimulationService.cs (BuildSnapshot inputs)
    - DeckFlow.Web/Models/CutLab/CutLabState.cs (BaselineSnapshot field)
    - DeckFlow.Web.Tests/CutLabPageServiceTests.cs (FakeLoader/FakeResolver/FakeBanListService wiring — add fakes for the new cache + sim/baseline deps)
  </read_first>
  <behavior>
    - Test 1 (D-09 cache population): after `ProcessAsync` on a valid pool, `CutLabResolvedCardCache.TryGet(poolKey)` returns the resolved cards — proving intake populated the cache.
    - Test 2 (D-12 baseline): the returned `CutLabProcessResult.State.BaselineSnapshot` is non-null with 7 families, and the serialized state (SerializedStateJson) round-trips it under the cap.
    - Test 3 (no duplicate resolution): resolution is not performed twice — the cache is populated from the SAME `ScryfallCardData` already resolved for role assignment (assert the resolver fake sees one resolution pass for the pool).
    - Test 4 (fail-open): if baseline computation throws, ProcessAsync still returns a usable result with the pool/findings (baseline null + a warning), never a hard failure — mirrors the existing classification fail-open behavior.
  </behavior>
  <action>
    Extend `CutLabPageService` constructor to inject `CutLabResolvedCardCache`, `ICutLabSimulationService`, and the baseline builder (add parameters; keep existing ones; update `Program.cs` DI only if the constructor signature requires it — the Plan 103-05 extension already registers these, and CutLabPageService is resolved via DI so new ctor params bind automatically). In `ProcessAsync` (or a new private stage called from it, matching the existing stage-A–F structure): after resolution + role assignment succeeds and BEFORE/AS `BuildState` runs, (a) compute the pool key from the resolved (name, quantity) set and `CutLabResolvedCardCache.Set(poolKey, resolvedCards)` reusing the `ScryfallCardData` already in `ResolvedCutLabEntry.Card` (do NOT re-resolve); (b) compute the baseline snapshot via the baseline builder over the original locked pool and set it on the state (`state with { BaselineSnapshot = ... }`); (c) fail open per the existing pattern if baseline computation throws (log + warning + null baseline). Keep the change to changed-lines only; preserve LF; do not reflow untouched code.
  </action>
  <verify>
    <automated>"/mnt/c/Program Files/dotnet/dotnet.exe" test DeckFlow.Web.Tests --filter "FullyQualifiedName~CutLabPageService"</automated>
  </verify>
  <acceptance_criteria>
    - All four behaviors pass; existing CutLabPageService tests stay green.
    - `ProcessAsync` calls `CutLabResolvedCardCache.Set(...)` once per intake (grep).
    - `State.BaselineSnapshot` non-null on success and round-trips under the cap.
    - No second Scryfall resolution pass is introduced (resolver fake call-count unchanged for the pool).
    - Baseline failure is fail-open (result still HasResult).
  </acceptance_criteria>
  <done>Intake populates the resolved-card cache and stores the D-12 baseline, without duplicate resolution.</done>
</task>

<task type="auto">
  <name>Task 2: Expose initial round plan on CutLabProcessResult for first render</name>
  <read_first>
    - DeckFlow.Web/Services/CutLab/CutLabPageService.cs (CutLabProcessResult record + where Findings/RoleAssignments are set)
    - DeckFlow.Web/Services/CutLab/CutLabCutRoundEngine.cs (BuildQueue + CutLabRoundPlan shape from Plan 103-04)
    - DeckFlow.Web/Services/CutLab/CutLabStructuralFindings.cs (findings input)
  </read_first>
  <action>
    Add a `CutLabRoundPlan? RoundPlan { get; init; }` (or equivalent initial-proposal fields) to `CutLabProcessResult`, and populate it in `ProcessAsync` by calling `CutLabCutRoundEngine.BuildQueue(analyzedCards + locks, Findings, state.Decisions, cardsToCutTarget)` after findings are computed. This gives the view model the first proposal + sticky-bar counts for the no-JS full-page render (Plan 103-08 consumes it). When the pool is already ≤100 after locks/floors, RoundPlan reflects "nothing to cut" (empty queue). Changed-lines only; XML-doc the new field.
  </action>
  <verify>
    <automated>"/mnt/c/Program Files/dotnet/dotnet.exe" test DeckFlow.Web.Tests --filter "FullyQualifiedName~CutLabPageService"</automated>
  </verify>
  <acceptance_criteria>
    - `CutLabProcessResult` exposes the initial round plan (grep `RoundPlan`).
    - For an oversized pool the plan yields a first proposal + cards-remaining count; for an ≤100 pool it yields an empty/nothing-to-cut plan.
    - Build clean.
  </acceptance_criteria>
  <done>First-render round plan is available to the view model on the server path.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| intake request → cache/state | Client-supplied pool drives cache population + baseline; must not enable a raw fetch or oversized persisted state. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-103-06-01 | Denial of Service | Baseline sim at intake adding latency to the full-page POST | mitigate | Baseline runs once per intake (not per action) and is fail-open; pool bounded ≤150; existing 2MB request-size limit on the controller stands. |
| T-103-06-02 | Tampering | Oversized state after adding BaselineSnapshot | mitigate | Serializer cap (Plan 103-02) throws above 256KB; snapshot is compact numeric only. |
| T-103-06-03 | Spoofing | Cache populated from untrusted resolution | mitigate | Cards resolved only through the existing `IScryfallCardResolver` pipeline already used for role assignment; no new HTTP surface. |
</threat_model>

<verification>
- `"/mnt/c/Program Files/dotnet/dotnet.exe" test DeckFlow.Web.Tests --filter "FullyQualifiedName~CutLab"` green.
- Full-page intake still renders (build clean) with baseline + round plan populated.
</verification>

<success_criteria>
Intake populates the resolved-card cache and D-12 baseline without duplicate resolution; initial round plan available for first render.
</success_criteria>

<output>
Create `.planning/workstreams/cut-lab/phases/103-simulation-engine-guided-cut-rounds/103-06-SUMMARY.md` when done.
</output>
