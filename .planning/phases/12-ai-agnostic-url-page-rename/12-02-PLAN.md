---
phase: 12-ai-agnostic-url-page-rename
plan: 02
type: execute
wave: 2
depends_on: [12-01]
files_modified:
  - DeckFlow.Web/Views/Deck/ChatGptPackets.cshtml
  - DeckFlow.Web/Views/Deck/ChatGptDeckComparison.cshtml
  - DeckFlow.Web/Views/Deck/ChatGptCedhMetaGap.cshtml
  - DeckFlow.Web/Views/Deck/DeckAnalysis.cshtml
  - DeckFlow.Web/Views/Deck/DeckComparison.cshtml
  - DeckFlow.Web/Views/Deck/CedhMetaGap.cshtml
  - DeckFlow.Web/Controllers/DeckController.cs
autonomous: true
requirements: [RENAME-02]
must_haves:
  truths:
    - "Razor view file `Views/Deck/DeckAnalysis.cshtml` exists; `Views/Deck/ChatGptPackets.cshtml` is gone — per D-12 view file rename via git mv"
    - "Razor view file `Views/Deck/DeckComparison.cshtml` exists; `Views/Deck/ChatGptDeckComparison.cshtml` is gone — per D-12 view file rename via git mv"
    - "Razor view file `Views/Deck/CedhMetaGap.cshtml` exists; `Views/Deck/ChatGptCedhMetaGap.cshtml` is gone — per D-12 view file rename via git mv"
    - "DeckController.cs no longer contains the literal strings `\"ChatGptPackets\"`, `\"ChatGptDeckComparison\"`, or `\"ChatGptCedhMetaGap\"` as View() arguments — per D-13 View() arg updates"
    - "DeckController.cs `return View(...)` calls reference `\"DeckAnalysis\"`, `\"DeckComparison\"`, `\"CedhMetaGap\"` — per D-13 (separate commit from D-12 file rename per CLAUDE.md one-logical-change-per-commit rule)"
    - "Razor `@model` directives in the three view files remain on `ChatGpt*ViewModel` classes (Phase 13 owns the class rename — D-14)"
  artifacts:
    - path: "DeckFlow.Web/Views/Deck/DeckAnalysis.cshtml"
      provides: "Renamed Page 1 view; @model still ChatGptDeckViewModel"
      contains: "@model DeckFlow.Web.Models.ChatGptDeckViewModel"
    - path: "DeckFlow.Web/Views/Deck/DeckComparison.cshtml"
      provides: "Renamed Page 2 view; @model still ChatGptDeckComparisonViewModel"
      contains: "@model DeckFlow.Web.Models.ChatGptDeckComparisonViewModel"
    - path: "DeckFlow.Web/Views/Deck/CedhMetaGap.cshtml"
      provides: "Renamed Page 3 view; @model still ChatGptCedhMetaGapViewModel"
      contains: "@model DeckFlow.Web.Models.ChatGptCedhMetaGapViewModel"
  key_links:
    - from: "DeckController.cs return View(\"DeckAnalysis\", ...)"
      to: "DeckFlow.Web/Views/Deck/DeckAnalysis.cshtml"
      via: "Razor view-name lookup (explicit string argument) — per D-13"
      pattern: "return View\\(\"DeckAnalysis\""
    - from: "DeckController.cs return View(\"DeckComparison\", ...)"
      to: "DeckFlow.Web/Views/Deck/DeckComparison.cshtml"
      via: "Razor view-name lookup (explicit string argument) — per D-13"
      pattern: "return View\\(\"DeckComparison\""
    - from: "DeckController.cs return View(\"CedhMetaGap\", ...)"
      to: "DeckFlow.Web/Views/Deck/CedhMetaGap.cshtml"
      via: "Razor view-name lookup (explicit string argument) — per D-13"
      pattern: "return View\\(\"CedhMetaGap\""
---

<objective>
Rename the three Razor view files from their `ChatGpt*` filenames to AI-agnostic filenames matching the URL slugs landed in Plan 01, and update every `return View("…", …)` literal string in `DeckController.cs` to reference the new view names.

Purpose: Aligns view filenames with the new URLs (closes the Phase 11 verification note that flagged the `CedhMetaGap.cshtml` filename mismatch). Per D-12/D-13/D-14 in `12-CONTEXT.md`. Strictly stays at the user-visible/URL layer — Razor `@model` directives keep pointing at `ChatGpt*ViewModel` classes (Phase 13 owns the C# class rename).
Output: Three new view files exist, three old view files are deleted (use `git mv` to preserve history), DeckController View() calls all point at the new names.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/STATE.md
@.planning/phases/12-ai-agnostic-url-page-rename/12-CONTEXT.md
@.planning/phases/12-ai-agnostic-url-page-rename/12-PATTERNS.md
@.planning/phases/12-ai-agnostic-url-page-rename/12-01-SUMMARY.md

<interfaces>
<!-- File rename mapping (D-12). Use `git mv` to preserve history. -->
- DeckFlow.Web/Views/Deck/ChatGptPackets.cshtml         → DeckFlow.Web/Views/Deck/DeckAnalysis.cshtml
- DeckFlow.Web/Views/Deck/ChatGptDeckComparison.cshtml  → DeckFlow.Web/Views/Deck/DeckComparison.cshtml
- DeckFlow.Web/Views/Deck/ChatGptCedhMetaGap.cshtml     → DeckFlow.Web/Views/Deck/CedhMetaGap.cshtml

<!-- View() argument literal-string mapping (D-13). Hits ALL return-View call sites — GET handlers AND POST handlers AND error-fallback paths. -->
- `return View("ChatGptPackets", ...)`        → `return View("DeckAnalysis", ...)`
- `return View("ChatGptDeckComparison", ...)` → `return View("DeckComparison", ...)`
- `return View("ChatGptCedhMetaGap", ...)`    → `return View("CedhMetaGap", ...)`

<!-- D-14 invariant: `@model DeckFlow.Web.Models.ChatGptDeckViewModel` etc. STAY. Phase 13 owns the class rename. -->
<!-- D-14 invariant: action method names like `public IActionResult ChatGptPackets()` STAY. Phase 13 owns the class/method rename. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Git-mv the three view files to AI-agnostic filenames</name>
  <files>
    DeckFlow.Web/Views/Deck/ChatGptPackets.cshtml,
    DeckFlow.Web/Views/Deck/ChatGptDeckComparison.cshtml,
    DeckFlow.Web/Views/Deck/ChatGptCedhMetaGap.cshtml,
    DeckFlow.Web/Views/Deck/DeckAnalysis.cshtml,
    DeckFlow.Web/Views/Deck/DeckComparison.cshtml,
    DeckFlow.Web/Views/Deck/CedhMetaGap.cshtml
  </files>
  <read_first>
    - DeckFlow.Web/Views/Deck/ChatGptPackets.cshtml (first 10 lines — confirm @model directive present)
    - DeckFlow.Web/Views/Deck/ChatGptDeckComparison.cshtml (first 10 lines)
    - DeckFlow.Web/Views/Deck/ChatGptCedhMetaGap.cshtml (first 10 lines)
    - .planning/phases/12-ai-agnostic-url-page-rename/12-CONTEXT.md (D-12, D-14)
  </read_first>
  <action>
Run three `git mv` commands (NOT `mv` — preserves Git history for blame/log of these files) per D-12:
1. `git mv DeckFlow.Web/Views/Deck/ChatGptPackets.cshtml DeckFlow.Web/Views/Deck/DeckAnalysis.cshtml`
2. `git mv DeckFlow.Web/Views/Deck/ChatGptDeckComparison.cshtml DeckFlow.Web/Views/Deck/DeckComparison.cshtml`
3. `git mv DeckFlow.Web/Views/Deck/ChatGptCedhMetaGap.cshtml DeckFlow.Web/Views/Deck/CedhMetaGap.cshtml`

Do NOT modify the contents of any of the three files in this task. The `@model` directives (`@model DeckFlow.Web.Models.ChatGptDeckViewModel` etc.) MUST stay unchanged per D-14 — those are Phase 13's responsibility.

Per CLAUDE.md "one logical change per commit": the `git mv` of the three files ships as ONE commit (a single logical change: "rename three Razor view files to AI-agnostic names"). Task 2 (DeckController View() arg updates per D-13) ships as a SECOND commit.

NOTE: The MSBuild build will fail BETWEEN this task's commit and Task 2's commit because the controller's `View("ChatGptPackets", …)` strings will no longer resolve to a file. This is acceptable INTRA-plan because the plan-level acceptance gate (the final `dotnet build`) runs AFTER both tasks complete. Do not be alarmed by an intermediate broken build state.
  </action>
  <verify>
    <automated>
      cd /mnt/c/users/chrislunt/source/personal/deckflow &amp;&amp; \
      [ -f DeckFlow.Web/Views/Deck/DeckAnalysis.cshtml ] &amp;&amp; \
      [ -f DeckFlow.Web/Views/Deck/DeckComparison.cshtml ] &amp;&amp; \
      [ -f DeckFlow.Web/Views/Deck/CedhMetaGap.cshtml ] &amp;&amp; \
      [ ! -f DeckFlow.Web/Views/Deck/ChatGptPackets.cshtml ] &amp;&amp; \
      [ ! -f DeckFlow.Web/Views/Deck/ChatGptDeckComparison.cshtml ] &amp;&amp; \
      [ ! -f DeckFlow.Web/Views/Deck/ChatGptCedhMetaGap.cshtml ] &amp;&amp; \
      grep -c "@model DeckFlow.Web.Models.ChatGptDeckViewModel" DeckFlow.Web/Views/Deck/DeckAnalysis.cshtml | grep -qE '^1$' &amp;&amp; \
      grep -c "@model DeckFlow.Web.Models.ChatGptDeckComparisonViewModel" DeckFlow.Web/Views/Deck/DeckComparison.cshtml | grep -qE '^1$' &amp;&amp; \
      grep -c "@model DeckFlow.Web.Models.ChatGptCedhMetaGapViewModel" DeckFlow.Web/Views/Deck/CedhMetaGap.cshtml | grep -qE '^1$'
    </automated>
  </verify>
  <acceptance_criteria>
    - `DeckFlow.Web/Views/Deck/DeckAnalysis.cshtml` exists (D-12)
    - `DeckFlow.Web/Views/Deck/DeckComparison.cshtml` exists (D-12)
    - `DeckFlow.Web/Views/Deck/CedhMetaGap.cshtml` exists (D-12)
    - `DeckFlow.Web/Views/Deck/ChatGptPackets.cshtml` does NOT exist
    - `DeckFlow.Web/Views/Deck/ChatGptDeckComparison.cshtml` does NOT exist
    - `DeckFlow.Web/Views/Deck/ChatGptCedhMetaGap.cshtml` does NOT exist
    - `git log --diff-filter=R --name-status -- DeckFlow.Web/Views/Deck/` shows three rename entries (proves `git mv` was used, not `rm` + `add`)
    - `DeckAnalysis.cshtml` line 1 contains `@model DeckFlow.Web.Models.ChatGptDeckViewModel` (unchanged per D-14)
    - `DeckComparison.cshtml` line 1 contains `@model DeckFlow.Web.Models.ChatGptDeckComparisonViewModel` (unchanged per D-14)
    - `CedhMetaGap.cshtml` line 1 contains `@model DeckFlow.Web.Models.ChatGptCedhMetaGapViewModel` (unchanged per D-14)
  </acceptance_criteria>
  <done>Three views renamed via git mv with history preserved (D-12), @model directives untouched, ready for Task 2 to fix DeckController View() args per D-13.</done>
</task>

<task type="auto">
  <name>Task 2: Update DeckController return View() literal strings to new view names</name>
  <files>DeckFlow.Web/Controllers/DeckController.cs</files>
  <read_first>
    - DeckFlow.Web/Controllers/DeckController.cs (entire file; grep first for all `return View("ChatGpt` literal strings)
    - .planning/phases/12-ai-agnostic-url-page-rename/12-CONTEXT.md (D-13, D-14)
    - .planning/phases/12-ai-agnostic-url-page-rename/12-PATTERNS.md (DeckController.cs section — "View name string update pattern")
  </read_first>
  <action>
Replace every literal `View("ChatGptPackets"`, `View("ChatGptDeckComparison"`, and `View("ChatGptCedhMetaGap"` occurrence with the AI-agnostic equivalent (per D-13). Run the grep below first to enumerate every site (GET handlers + POST handlers + error-fallback paths):

`grep -n 'View("ChatGpt' DeckFlow.Web/Controllers/DeckController.cs`

For each match, replace the literal string argument ONLY (do not touch the `new ChatGptDeckViewModel { ... }` argument that follows — that's the C# view-model class instantiation, owned by Phase 13). Exact mapping (D-13):
- `View("ChatGptPackets"` → `View("DeckAnalysis"`
- `View("ChatGptDeckComparison"` → `View("DeckComparison"`
- `View("ChatGptCedhMetaGap"` → `View("CedhMetaGap"`

DO NOT modify:
- Action method names (e.g., `public IActionResult ChatGptPackets()`) — Phase 13
- View-model class names (e.g., `new ChatGptDeckViewModel { ... }`) — Phase 13
- Request DTO names (e.g., `new ChatGptDeckRequest()`) — Phase 13
- `ActiveTab = DeckPageTab.ChatGptPackets` enum values — Phase 13

Per CLAUDE.md "one logical change per commit" and per D-13 "View() arg updates ship per-commit, independent from D-12 file rename": commit message for this task should describe "update DeckController View() arguments to match renamed view files" as one logical change. After this commit, the build is GREEN again (the view files exist at their new names per Task 1's commit, and the View() args now find them).
  </action>
  <verify>
    <automated>
      cd /mnt/c/users/chrislunt/source/personal/deckflow &amp;&amp; \
      dotnet build DeckFlow.Web/DeckFlow.Web.csproj --nologo --verbosity quiet &amp;&amp; \
      grep -c 'View("ChatGptPackets"' DeckFlow.Web/Controllers/DeckController.cs | grep -qE '^0$' &amp;&amp; \
      grep -c 'View("ChatGptDeckComparison"' DeckFlow.Web/Controllers/DeckController.cs | grep -qE '^0$' &amp;&amp; \
      grep -c 'View("ChatGptCedhMetaGap"' DeckFlow.Web/Controllers/DeckController.cs | grep -qE '^0$' &amp;&amp; \
      grep -cE 'View\("(DeckAnalysis|DeckComparison|CedhMetaGap)"' DeckFlow.Web/Controllers/DeckController.cs | grep -qE '^[1-9][0-9]*$'
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'View("ChatGptPackets"' DeckFlow.Web/Controllers/DeckController.cs` returns `0` (D-13)
    - `grep -c 'View("ChatGptDeckComparison"' DeckFlow.Web/Controllers/DeckController.cs` returns `0` (D-13)
    - `grep -c 'View("ChatGptCedhMetaGap"' DeckFlow.Web/Controllers/DeckController.cs` returns `0` (D-13)
    - `grep -c 'View("DeckAnalysis"' DeckFlow.Web/Controllers/DeckController.cs` returns at least `1` (D-13)
    - `grep -c 'View("DeckComparison"' DeckFlow.Web/Controllers/DeckController.cs` returns at least `1` (D-13)
    - `grep -c 'View("CedhMetaGap"' DeckFlow.Web/Controllers/DeckController.cs` returns at least `1` (D-13)
    - Action method names UNCHANGED: `grep -c "public IActionResult ChatGptPackets()" DeckFlow.Web/Controllers/DeckController.cs` returns `1` (the GET action) — confirms Phase 13 surface is untouched
    - View-model class names UNCHANGED: `grep -c "new ChatGptDeckViewModel" DeckFlow.Web/Controllers/DeckController.cs` returns the same count as pre-edit (Phase 13 owns these)
    - `dotnet build DeckFlow.Web/DeckFlow.Web.csproj` exits 0 — proves view lookup resolves to renamed files
  </acceptance_criteria>
  <done>All View() literal strings reference the new view file names (D-13), build is clean and green, action method bodies + view-model class instantiations unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Razor view-lookup (controller → filesystem) | View-name string flows from compiled C# into Razor view engine; filesystem path resolved server-side. No user input crosses this boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-04 | Tampering / Path traversal | Razor `View("…")` string argument | accept | All view name strings are HARDCODED literals in DeckController.cs (`"DeckAnalysis"`, `"DeckComparison"`, `"CedhMetaGap"`). No user data interpolated into the path. Razor view engine constrains lookup to the `Views/{Controller}/` and `Views/Shared/` search paths. |
| T-12-05 | Repudiation | Loss of git blame on renamed view files | mitigate | Use `git mv` (verified via `git log --diff-filter=R --name-status`) — preserves file rename detection so `git log --follow` and `git blame` continue to trace history across the rename boundary. |
</threat_model>

<verification>
After Plan 02 completes (manual smoke test — user-launched dev server only):
- `curl -i http://localhost:5173/deck-analysis` → expect HTTP 200, body contains `<h1>` text from `DeckAnalysis.cshtml` (Phase-1 H1 text rewrite ships in Plan 03; for now should still show the old "ChatGPT Analysis" H1 — verifies view file resolution works)
- `curl -i http://localhost:5173/deck-comparison` → expect HTTP 200
- `curl -i http://localhost:5173/cedh-meta-gap` → expect HTTP 200
- All three pages render with full session-bar / step-strip / form UI intact (no Razor view-not-found exception)
</verification>

<success_criteria>
- 3 view files renamed via `git mv` (history preserved)
- 0 occurrences of `View("ChatGpt(Packets|DeckComparison|CedhMetaGap)"` remain in DeckController.cs
- `dotnet build DeckFlow.sln --configuration Release` exits 0 with no NEW warnings
- All 3 new slug routes render their renamed view file successfully
- No `@model` directive changes (Phase 13 invariant preserved)
- No action method name changes (Phase 13 invariant preserved)
</success_criteria>

<output>
Create `.planning/phases/12-ai-agnostic-url-page-rename/12-02-SUMMARY.md` documenting:
- 3 view files renamed (with git mv command output)
- N View() literal strings updated (with exact before→after counts per page)
- Build clean
- Manual smoke-test results
- Defer note: H1 text + explainer-line additions + .page-lede CSS ship in Plan 03
</output>
</output>
