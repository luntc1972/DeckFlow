---
context: phase
phase: 999.1-ai-agnostic-prose-adaptation-razor-views
task: 3
total_tasks: 3
status: paused_at_human_verify_checkpoint
last_updated: 2026-05-18T19:09:02.152Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> Each constraint below was discovered through failure during this session.
> Acknowledge each one explicitly before proceeding.

- [ ] CONSTRAINT: WSL `node_modules` symlinks block `worktree.cleanup-wave` — Each agent worktree contains a symlink at `DeckFlow.Web/node_modules` → main tree's `node_modules`. `.gitignore` pattern `node_modules/` (trailing slash) does NOT match the symlink-as-file, so `git status` reports it as untracked. The `gsd-sdk query worktree.cleanup-wave` helper aborts with `worktree_dirty` / `?? DeckFlow.Web/node_modules`. Structural mitigation: BEFORE invoking `worktree.cleanup-wave`, iterate the wave's worktree paths and `rm "$WT/DeckFlow.Web/node_modules"` for each symlink (test with `[ -L … ]`). Symlinks are regenerated by MSBuild on the next build; removal is safe.
- [ ] CONSTRAINT: Claude Code worktree locking blocks `git worktree remove` — Worktrees spawned with `isolation="worktree"` are marked locked (`lock reason: claude agent agent-…`) and the cleanup-wave helper's first removal attempt fails with `cannot remove a locked working tree`. Structural mitigation: when cleanup blocks on `worktree_remove_failed` with `locked`, run `git worktree unlock <path>` for each worktree in the manifest, then re-invoke `worktree.cleanup-wave`. The helper's manifest pointer is stateful — drop already-merged entries (e.g., remove `agent_id`-of-first-success from the manifest JSON) before retry.
- [ ] CONSTRAINT: WSL has no native `dotnet` — Build gate must invoke `dotnet.exe` from Windows host SDK at `/mnt/c/Program Files/dotnet/dotnet.exe`. The literal command `dotnet build ...` fails with `No such file or directory`. CLAUDE.md additionally pins: VSTest is unreliable in WSL; rely on `dotnet build` clean + targeted manual harness or push-and-watch CI. Structural mitigation: build gate uses `dotnet.exe build DeckFlow.sln -c Release`; skip the post-merge **test** gate in WSL and treat `TEST_EXIT=0` (build-only).

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Auto-launching DeckFlow dev server | Plan 07 Task 3 is `checkpoint:human-verify`; agent MUST NOT auto-start `dotnet run --project DeckFlow.Web`. User MEMORY.md `feedback_user_starts_server.md` is explicit. | blocking | Pre-checkpoint prompt MUST state "DO NOT auto-launch the dev server" verbatim. UAT instructions live in `999.1-HUMAN-UAT.md`; orchestrator presents checklist + waits for user `approved`/`issues:` reply. |
| Running `cleanup-wave` from inside a worktree | Last completed background agent leaves CWD pinned to its worktree. Cleanup must run from primary worktree or merges target wrong tree. | advisory | Before `worktree.cleanup-wave`, `cd` to `$(git worktree list --porcelain \| awk '/^worktree /{print substr($0,10); exit}')` and verify branch == orchestrator branch (v1.3). |
| Committing `node_modules` symlinks | The symlink in `DeckFlow.Web/` is untracked by design (recreated by MSBuild). Never `git add` it. | advisory | Removal-before-cleanup pattern above; do NOT broaden `.gitignore` pattern unless explicitly desired (would also ignore real `node_modules` in nested dirs). |

<current_state>
Phase 999.1 wave 1 (plans 01–06) merged into `v1.3`. Wave 2 plan 07 reached the `checkpoint:human-verify` task. Two task commits exist in the executor's worktree (not yet merged into v1.3):

- `621549a` — Task 1: README brand audit (29 ChatGPT → AI/your AI generalizations; D-03 Ask-a-Judge carve-out paragraph at line 493 preserved verbatim)
- `da1cb34` — Task 3 (partial): `.planning/phases/999.1-ai-agnostic-prose-adaptation-razor-views/999.1-HUMAN-UAT.md` written

Task 2 was gate-only (no diff); all invariant grep gates PASS; `dotnet.exe build DeckFlow.sln -c Release` exits 0 with 0 warnings.

`999.1-07-SUMMARY.md` is intentionally NOT written yet — the continuation agent must write it after the user signs off on UAT.

Worktree: `/mnt/c/users/chrislunt/source/personal/deckflow/.claude/worktrees/agent-a83bb674291ad3297` (branch `worktree-agent-a83bb674291ad3297`, base `40164da2e3707a1e64030c1f9c2654e9c9386cbe`).

Wave 2 manifest: `/tmp/gsd-worktree-wave2-FM8ixm.json` (single-entry; in /tmp — may not survive WSL restart, rebuild if missing).

Executor agent ID for continuation via SendMessage: `a83bb674291ad3297`.
</current_state>

<completed_work>

**Wave 1 — all 6 plans merged into v1.3:**

- 999.1-01 DeckAnalysis.cshtml hybrid prose (`65c7b63` merge commit)
- 999.1-02 DeckComparison.cshtml hybrid prose (`faf8520`)
- 999.1-03 CedhMetaGap + Home hub-card (`f44b85d`)
- 999.1-04 Service exception messages (`984e5b5`)
- 999.1-05 Help markdown rewrites (`f887703`)
- 999.1-06 Logger prefix renames (`05739e2`)
- Wave-1 tracking commit: `40164da` (ROADMAP + STATE)

**Wave 2 plan 07 — in worktree (not merged):**

- Task 1 README brand audit committed (`621549a`)
- Task 2 invariants grep gate + `dotnet.exe build DeckFlow.sln -c Release` → all PASS / 0 warnings (gate-only, no diff)
- Task 3 UAT checklist file written + committed (`da1cb34`)
</completed_work>

<remaining_work>

1. User performs UAT walkthrough (T1 / T4 / T5 / Hub+Help / JudgeQuestions carve-out regression) in `999.1-HUMAN-UAT.md`.
2. User types `approved` (or `issues: <desc>`) in chat.
3. Orchestrator routes via workflow's `checkpoint_handling` — preferred path: `SendMessage` to executor agent `a83bb674291ad3297` with the user's response so it writes `999.1-07-SUMMARY.md` AND commits it (the executor still has the worktree-aware git context). Fallback: spawn a fresh continuation gsd-executor with `isolation="worktree"`-equivalent context that writes the SUMMARY in the same worktree.
4. Merge `worktree-agent-a83bb674291ad3297` via `gsd-sdk query worktree.cleanup-wave --manifest /tmp/gsd-worktree-wave2-FM8ixm.json` (rebuild manifest if /tmp wiped). Remember to `rm` the `DeckFlow.Web/node_modules` symlink in the worktree first and `git worktree unlock` if locked.
5. Update ROADMAP: `gsd-sdk query roadmap.update-plan-progress 999.1 999.1-07 complete` then commit tracking.
6. Run remaining phase gates from `execute-phase.md`:
   - `code_review_gate` (Skill invocation `gsd-code-review 999.1`)
   - `regression_gate` (prior-phase test files — likely skip in WSL per CLAUDE.md)
   - `schema_drift_gate` (no schema files changed — should be no-op)
   - `codebase_drift_gate` (non-blocking)
   - `verify_phase_goal` (spawn `gsd-verifier` subagent)
7. `gsd-sdk query phase.complete 999.1` → marks roadmap + STATE.
8. `auto_copy_learnings`, `close_phase_todos`, `update_project_md` final steps.
9. Present `offer_next` routing (no `--auto`, no `--no-transition` → present next-phase options to user).
</remaining_work>

<decisions_made>

- D-01..D-05 from `999.1-CONTEXT.md` are the binding contract (no formal REQ-IDs).
- D-01 Hybrid pattern: prose above `_AiSelector` uses universal noun `your AI`; prose below uses `@aiPlatform.DisplayName` injection.
- D-03 carve-out: `JudgeQuestions.cshtml`, `Home.cshtml` line 62 hub-card, `Help/ask-a-judge.md` keep literal "ChatGPT" (the product, not the workflow).
- D-04: PromptVariants raw-string literals deferred to a separate phase — Plan 07 surveys drift only, does NOT fix.
- D-05 universal noun fallback applies where selector state isn't accessible (exception messages, static Markdig help docs, README prose).
- Build gate uses `dotnet.exe` (Windows host SDK) not native Linux dotnet; test gate skipped per CLAUDE.md (VSTest unreliable in WSL).
- Plan 07 acceptance criterion `grep -c 'secondary \*\*ChatGPT' README.md` was mis-quoted in the plan; actual file content is `**secondary** ChatGPT`. Carve-out paragraph preserved verbatim regardless.
- Plan 07 also said `chatgpt-step-eyebrow = 3 hits / view`; actual baseline is 5/3/3. These are Phase 13 D-08 deferred identifiers — invariant satisfied (they remain byte-identical).
</decisions_made>

<blockers>

- BLOCKER: Human UAT not yet performed — type `approved` (or `issues: <desc>`) in chat to unblock continuation.
- INFRASTRUCTURE: `/tmp/gsd-worktree-wave2-FM8ixm.json` may not survive WSL restart. If missing on resume, rebuild from this template:
  ```json
  {"worktrees":[{"agent_id":"a83bb674291ad3297","worktree_path":"/mnt/c/users/chrislunt/source/personal/deckflow/.claude/worktrees/agent-a83bb674291ad3297","branch":"worktree-agent-a83bb674291ad3297","expected_base":"40164da2e3707a1e64030c1f9c2654e9c9386cbe"}]}
  ```
</blockers>

## Required Reading (in order)

1. `.planning/phases/999.1-ai-agnostic-prose-adaptation-razor-views/999.1-CONTEXT.md` — D-01..D-05 binding decisions.
2. `.planning/phases/999.1-ai-agnostic-prose-adaptation-razor-views/999.1-07-PLAN.md` — checkpoint protocol + Task 3 `<resume-signal>` semantics.
3. `.planning/phases/999.1-ai-agnostic-prose-adaptation-razor-views/999.1-HUMAN-UAT.md` — the UAT checklist body.
4. `CLAUDE.md` (project root) — pinned formatting + WSL/dotnet/test constraints + commit hygiene (no Co-Authored-By trailer).
5. `$HOME/.claude/get-shit-done/workflows/execute-phase.md` — `checkpoint_handling` block + step 5.5 cleanup loop + verification / completion steps.

## Critical Anti-Patterns (do NOT repeat these)

- ANTI-PATTERN: Running `worktree.cleanup-wave` without first removing the `DeckFlow.Web/node_modules` symlink in each worktree → cleanup aborts with `worktree_dirty`. Structural mitigation: see BLOCKING CONSTRAINTS section above.
- ANTI-PATTERN: Spawning a fresh `gsd-executor` for the continuation in a NEW worktree → leaves the existing worktree's two task commits orphaned and the SUMMARY.md split from the work it documents. Structural mitigation: use `SendMessage` to executor `a83bb674291ad3297` with the user's UAT response, OR if it's expired, write SUMMARY.md directly in the existing worktree via shell + commit, then merge.
- ANTI-PATTERN: Auto-launching `dotnet run --project DeckFlow.Web` to "help" with UAT → directly contradicts user MEMORY.md feedback (`feedback_user_starts_server.md`). User starts the server; orchestrator waits.

## Infrastructure State

- Primary worktree: `/mnt/c/users/chrislunt/source/personal/deckflow` on branch `v1.3` at `40164da`.
- Active wave-2 worktree: `/mnt/c/users/chrislunt/source/personal/deckflow/.claude/worktrees/agent-a83bb674291ad3297` on branch `worktree-agent-a83bb674291ad3297` at `da1cb34` (locked).
- Stale (predates this session, leave alone): `agent-a3fe5b9e7d31e947c` on `worktree-agent-a3fe5b9e7d31e947c` at `b7a94a2` (locked).
- Wave 2 manifest: `/tmp/gsd-worktree-wave2-FM8ixm.json`.
- dotnet SDK: `/mnt/c/Program Files/dotnet/dotnet.exe` (10.0.300).
- DeckFlow dev URLs (when user starts server): `http://localhost:5173/` (HTTP) / `https://localhost:7173/` (HTTPS) per `DeckFlow.Web/Properties/launchSettings.json`.

<context>
Wave 1 ran 6 parallel executors with isolation="worktree". All 6 returned with SUMMARY.md committed. Cleanup tripped twice — once on dirty worktree (node_modules symlink), once on locked worktree (Claude Code lock). After workarounds, all 6 merged cleanly into v1.3. Post-merge `dotnet.exe build Release` was clean.

Plan 07 executor reached `checkpoint:human-verify` at Task 3 as designed (autonomous=false). It wrote `999.1-HUMAN-UAT.md` per the plan's `<how-to-verify>` body, committed it, and returned checkpoint state. The orchestrator presented the checkpoint to the user, then the user invoked /gsd-pause-work.

When resuming: the cleanest path is to `SendMessage` to executor agent `a83bb674291ad3297` with the user's `approved` (or `issues:`) reply once UAT is done. The executor still has full plan context and a live worktree. It will write `999.1-07-SUMMARY.md`, commit, and exit. THEN run the cleanup-wave helper (after removing the node_modules symlink) and proceed with phase-completion gates.

If SendMessage isn't available or the agent has expired, the fallback is: write `999.1-07-SUMMARY.md` directly in the worktree from the orchestrator, `git -C <worktree> add` + commit it, then cleanup-wave. The summary should capture: tasks 1-3 commits, UAT result, build/grep gate results, and any drift findings (none expected — D-04 surveillance was clean per executor return).
</context>

<next_action>
On resume:

1. Ask the user whether UAT has been performed and read back their reply.
2. If `approved`: `SendMessage to: a83bb674291ad3297` with the user's full reply so the executor writes `999.1-07-SUMMARY.md` + commits. Verify the commit landed in the worktree.
3. If executor agent expired: write SUMMARY.md directly in the worktree at `/mnt/c/users/chrislunt/source/personal/deckflow/.claude/worktrees/agent-a83bb674291ad3297/.planning/phases/999.1-ai-agnostic-prose-adaptation-razor-views/999.1-07-SUMMARY.md` using the template from `$HOME/.claude/get-shit-done/templates/summary.md` and the executor's return data captured in the conversation transcript.
4. Remove the symlink: `rm /mnt/c/users/chrislunt/source/personal/deckflow/.claude/worktrees/agent-a83bb674291ad3297/DeckFlow.Web/node_modules` (if exists).
5. Unlock the worktree if needed: `git worktree unlock /mnt/c/users/chrislunt/source/personal/deckflow/.claude/worktrees/agent-a83bb674291ad3297`.
6. `cd /mnt/c/users/chrislunt/source/personal/deckflow` (primary), verify branch is `v1.3`, then `gsd-sdk query worktree.cleanup-wave --manifest /tmp/gsd-worktree-wave2-FM8ixm.json`.
7. `gsd-sdk query roadmap.update-plan-progress 999.1 999.1-07 complete`; commit ROADMAP+STATE if diff.
8. Resume execute-phase.md from `code_review_gate` onward.
</next_action>
