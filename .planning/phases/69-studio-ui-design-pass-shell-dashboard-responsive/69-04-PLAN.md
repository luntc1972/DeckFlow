---
phase: 69-studio-ui-design-pass-shell-dashboard-responsive
plan: 04
type: execute
wave: 2
depends_on: ["69-01"]
files_modified:
  - DeckFlow.Studio/Pages/DirectPush.razor
  - DeckFlow.Studio/Pages/PullFromProd.razor
autonomous: false
requirements: [STUI-03]
must_haves:
  truths:
    - "Every data table on every Studio page is wrapped so it scrolls within its container instead of forcing horizontal page scroll"
    - "Status and publish badges keep AA-legible contrast under OS dark mode on the dark table surface"
    - "Dark mode is consistent across all Studio pages with no per-page dark hacks"
  artifacts:
    - path: "DeckFlow.Studio/Pages/DirectPush.razor"
      provides: "All 3 tables wrapped in .table-responsive"
      contains: "table-responsive"
    - path: "DeckFlow.Studio/Pages/PullFromProd.razor"
      provides: "Both tables wrapped in .table-responsive"
      contains: "table-responsive"
  key_links:
    - from: "DeckFlow.Studio/Pages/DirectPush.razor"
      to: "Bootstrap .table-responsive + studio-content min-width:0"
      via: "wrapper div"
      pattern: "table-responsive"
---

<objective>
Close STUI-03 (responsive + dark consistency): wrap the only two pages whose data tables are NOT
yet inside a scroll container — `DirectPush.razor` (3 tables) and `PullFromProd.razor` (2 tables) —
in Bootstrap `.table-responsive`, then run the dark-mode + badge-contrast verification sweep across
all Studio pages and the locked `StatusBadge` palette, confirming the OS-driven dark tokens from
Plan 01 produce a consistent, legible result with no per-page dark rules.

Purpose: STUI-03 — no table forces horizontal page scroll; dark mode reads consistently everywhere.
Output: wrapped tables on DirectPush + PullFromProd; a human-verified dark/responsive sweep.

HARD CONSTRAINT: presentation-only. Wrapping a table in a div changes no behavior. `StatusBadge.razor`
stays BYTE-STABLE (verify-only, not modified). No new badge colors.

Note: Harvest, Review, Blocked, Skipped, CreatorSources already wrap their tables in
`.table-responsive` (confirmed) — they need verification only, not edits.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/69-studio-ui-design-pass-shell-dashboard-responsive/69-UI-SPEC.md
@DeckFlow.Studio/Shared/StatusBadge.razor
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wrap DirectPush + PullFromProd tables in .table-responsive</name>
  <files>DeckFlow.Studio/Pages/DirectPush.razor, DeckFlow.Studio/Pages/PullFromProd.razor</files>
  <read_first>
    - DeckFlow.Studio/Pages/DirectPush.razor (3 tables at lines ~158, ~245, ~331 — currently un-wrapped)
    - DeckFlow.Studio/Pages/PullFromProd.razor (2 tables at lines ~128, ~253 — currently un-wrapped)
    - DeckFlow.Studio/Pages/Review.razor (line ~88 — the canonical `<div class="table-responsive mt-3">` wrapper pattern to mirror)
    - .planning/phases/69-studio-ui-design-pass-shell-dashboard-responsive/69-UI-SPEC.md (Responsive & Dark-Mode Contract: wrap each table in Bootstrap `.table-responsive`; min-width:0 on the content container already handled by Plan 02)
  </read_first>
  <action>
    For each of the 3 tables in `DirectPush.razor` and the 2 tables in `PullFromProd.razor`, wrap the
    `<table>...</table>` element in a `<div class="table-responsive">` wrapper, mirroring the
    Review.razor pattern. Preserve any existing margin utility on the table by moving it to the
    wrapper (e.g. a table with `mt-2`/`mt-3` → wrapper gets the margin class, or keep it on the
    table; do not drop spacing). Do NOT alter table contents, columns, `@code`, bindings, conditional
    `@if` blocks, or any data/behavior — this is a pure structural wrap. Keep existing CRLF/LF line
    endings of each file; indent the wrapped block consistently and touch only the wrapped regions
    (changed-lines `.editorconfig` gate).
  </action>
  <verify>
    <automated>test $(grep -c 'table-responsive' DeckFlow.Studio/Pages/DirectPush.razor) -ge 3 && test $(grep -c 'table-responsive' DeckFlow.Studio/Pages/PullFromProd.razor) -ge 2 && /mnt/c/Program\ Files/dotnet/dotnet.exe build DeckFlow.Studio/DeckFlow.Studio.csproj -nologo 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - DirectPush.razor has ≥3 `.table-responsive` wrappers (one per table); PullFromProd.razor has ≥2.
    - Each wrapped table's columns, `@code`, bindings, and conditional blocks are unchanged (diff shows only added wrapper divs + indentation).
    - No table content, no data flow, no behavior changed.
    - `dotnet build DeckFlow.Studio` clean, no new warnings.
    - Existing per-page tests (DirectPushPageTests, PullFromProdPageTests) still build and pass.
  </acceptance_criteria>
  <done>All tables on DirectPush + PullFromProd scroll within their container; the other five pages already comply; build + existing page tests green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Dark-mode + responsive + badge-contrast operator sweep</name>
  <what-built>
    Plan 01 added OS-driven dark tokens (color-scheme: light dark + prefers-color-scheme override).
    Plans 02/03 restyled the shell + dashboard onto those tokens. Task 1 of this plan wrapped the last
    un-wrapped tables. StatusBadge.razor is byte-stable (locked vocabulary). This checkpoint confirms
    the whole pass reads correctly in light + dark at mobile + desktop with no table blowout and no
    illegible badge.
  </what-built>
  <how-to-verify>
    Start the Studio with the no-browser-window guard so it does NOT pop a Windows browser:
    run `scripts/run-web-test.sh` (or the Studio run script) with `DECKFLOW_DISABLE_AUTO_BROWSER=true`,
    then drive checks with `npx --no-install playwright test` against the headless server OR a
    manually-opened browser (per CLAUDE.md UI-testing constraint — never auto-open a browser on the
    Windows host).

    For EACH viewport (mobile ≈ 390px wide, desktop ≈ 1280px) AND EACH OS theme (light, dark):
    1. Home dashboard: count cards + publish-state badges legible; numbers readable; quick links work.
    2. Harvest, Review, Publish, DirectPush, PullFromProd, Blocked, Skipped, CreatorSources: each data
       table scrolls inside its container — NO horizontal page scrollbar on the whole page at 390px.
    3. Sidebar: branded panel surface, accent left-border on the active link, links ≥44px tall;
       at ≤640.98px the sidebar collapses to the top navbar toggler and the toggle still opens/closes.
    4. Badges (every status + publish state) keep readable contrast on the dark `#1e293b` table
       surface — `bg-info`/`bg-warning` text-dark badges stay dark-on-light and legible; no badge
       becomes invisible. StatusBadge markup is unchanged (confirm no new badge colors appeared).
    5. Reduced-motion: with OS "reduce motion" on, hover-lift transitions are suppressed (no jank).

    Confirm there is NO functional/behavior change anywhere — this was a presentation-only pass.
  </how-to-verify>
  <resume-signal>Type "approved" if all viewports/themes pass, or describe the specific page + viewport + theme issue to fix.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → Studio (local Blazor Server) | Operator-only local tool. Wrapping tables and verifying dark tokens introduces no input, data flow, or route change. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-69-NA | (all) | DirectPush/PullFromProd table wraps + dark/badge verify | accept | Pure structural `<div>` wraps + a visual verification checkpoint. StatusBadge byte-stable. No new input surface, no data flow, no package install on a local operator tool. No new attack surface. |
</threat_model>

<verification>
- `dotnet build DeckFlow.Studio` clean; DirectPushPageTests + PullFromProdPageTests still green.
- Grep confirms ≥3 / ≥2 `.table-responsive` wrappers added.
- Operator sweep (Task 2) confirms responsive + dark + badge-contrast across all pages at mobile + desktop, light + dark, with no behavior change.
</verification>

<success_criteria>
- No Studio data table forces horizontal page scroll (STUI-03).
- Dark mode is consistent across all pages via OS-driven tokens — no per-page dark hacks; badges stay legible.
- StatusBadge stays byte-stable; no new badge colors.
- Presentation-only; build + existing tests green.
</success_criteria>

<output>
Create `.planning/phases/69-studio-ui-design-pass-shell-dashboard-responsive/69-04-SUMMARY.md` when done.
</output>
