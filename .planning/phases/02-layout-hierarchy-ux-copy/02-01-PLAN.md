---
phase: 02-layout-hierarchy-ux-copy
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - DeckFlow.Web/wwwroot/css/site-common.css
autonomous: true
requirements: [UI-LH-01, UI-LH-02, UX-02]
tags: [css, layout, hub, feedback, admin]

must_haves:
  truths:
    - "site-common.css declares .hub-hero, .hub-hero__eyebrow, .hub-hero__title, .hub-hero__description, and .hub-card--primary rules consuming Phase 01 tokens (--cta-border, --panel, --line, --ink, --muted, --fs-xs, --fs-xl, --fs-sm)"
    - ".feedback-panel rule in site-common.css contains background: var(--panel) and border: 1px solid var(--line) (amended, not duplicated)"
    - "site-common.css declares a new .admin-feedback-detail rule block with the six properties moved from the Detail.cshtml inline style"
    - "site-common.css declares a new .admin-action-form rule with display: inline (purpose-named, not a generic utility class)"
    - "site-common.css declares .feedback-submit--busy and .feedback-submit--busy::before rules; the ::before rule references the existing busy-spin keyframes (no new @keyframes declaration)"
    - "site.css :root block is unchanged — zero new tokens added"
    - "All Phase 02 CSS rules live in site-common.css; site.css contains zero new rule blocks introduced by this phase"
  artifacts:
    - path: "DeckFlow.Web/wwwroot/css/site-common.css"
      provides: "Hub hero band, primary card modifier, feedback/admin panel rules, admin inline-form class, feedback submit busy state"
      contains: ".hub-hero, .hub-card--primary, .admin-feedback-detail, .admin-action-form, .feedback-submit--busy"
  key_links:
    - from: ".hub-hero"
      to: "var(--cta-border) (Phase 01 token)"
      via: "border-left: 4px solid var(--cta-border)"
      pattern: "border-left:\\s*4px solid var\\(--cta-border\\)"
    - from: ".hub-card--primary"
      to: "var(--cta-border)"
      via: "border-color"
      pattern: "\\.hub-card--primary\\s*\\{[^}]*border-color:\\s*var\\(--cta-border\\)"
    - from: ".feedback-submit--busy::before"
      to: "busy-spin keyframes (defined in site.css:1107)"
      via: "animation: busy-spin"
      pattern: "animation:\\s*busy-spin"
---

<objective>
Land every CSS rule needed for Phase 02 in site-common.css in a single atomic edit. This plan owns the hub hero band styling, the per-group primary card modifier, the inline-style → class migration targets (feedback panel amend, admin detail panel new rule, admin inline-form new rule), and the feedback submit button busy-state visual.

Purpose: Phase 02 markup work (Plan 02) and the TS busy-state handler (Plan 03) both depend on these classes existing. Centralising every CSS change in a single file/plan makes the diff reviewable and avoids cross-plan style drift. All rules consume Phase 01 tokens — zero new :root tokens are introduced (verifier gate #4).

Output: site-common.css with seven new/amended rule blocks. site.css is not touched. dotnet build remains clean.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-layout-hierarchy-ux-copy/02-CONTEXT.md
@.planning/phases/02-layout-hierarchy-ux-copy/02-UI-SPEC.md
@.planning/phases/02-layout-hierarchy-ux-copy/02-PATTERNS.md
@./CLAUDE.md
@DeckFlow.Web/wwwroot/css/site-common.css

<interfaces>
<!-- Phase 01 tokens this plan consumes (already declared in site.css :root). DO NOT redeclare. -->

From DeckFlow.Web/wwwroot/css/site.css :root (Phase 01 outcome):
  --bg, --panel, --line, --ink, --muted
  --accent, --accent-strong, --accent-default, --on-accent
  --cta-border, --link, --danger, --focus
  --fs-xs, --fs-sm, --fs-base, --fs-lg, --fs-xl, --fs-2xl

Existing keyframes in site.css (DO NOT redeclare):
  @keyframes busy-spin { from { transform: rotate(0deg); } to { transform: rotate(360deg); } }
  -- defined at site.css:1107-1115; this plan REUSES via animation: busy-spin reference.

Existing rule that this plan AMENDS (not duplicates):
  site-common.css:567 -> .feedback-panel { padding: 1.5rem; border-radius: 8px; }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add hub hero + primary card rules to site-common.css</name>
  <files>DeckFlow.Web/wwwroot/css/site-common.css</files>
  <read_first>
    - DeckFlow.Web/wwwroot/css/site-common.css (lines 161-200 — existing .hub-card block is the structural analog)
    - .planning/phases/02-layout-hierarchy-ux-copy/02-UI-SPEC.md §"1. .hub-hero" and §"2. .hub-card--primary"
    - .planning/phases/02-layout-hierarchy-ux-copy/02-PATTERNS.md §"7a. .hub-hero"
  </read_first>
  <action>
Implements UI-LH-01 (per D-01, D-02, D-03, D-04). All values are verbatim from UI-SPEC §1 and §2 — do not deviate.

Insert this block into site-common.css **immediately after the existing .hub-card rule cluster** (after the last `.hub-card__description` rule, before the next unrelated rule — locate by grep `.hub-card__description` and insert two blank lines below the closing brace). Match the surrounding 2-space indent style.

```css
.hub-hero {
  display: block;
  padding: 1rem 1.25rem;
  border: 1px solid var(--line);
  border-left: 4px solid var(--cta-border);
  border-radius: 10px;
  background: var(--panel);
  color: inherit;
  text-decoration: none;
  margin-bottom: 1.5rem;
  transition: border-color 120ms ease, box-shadow 120ms ease;
}

.hub-hero:hover,
.hub-hero:focus-visible {
  border-color: var(--accent-strong, var(--line));
  box-shadow: 0 8px 24px rgba(26, 31, 46, 0.08);
}

.hub-hero__eyebrow {
  display: block;
  font-size: var(--fs-xs);
  font-weight: 600;
  letter-spacing: 0.08em;
  text-transform: uppercase;
  color: var(--muted);
  margin-bottom: 0.35rem;
}

.hub-hero__title {
  display: block;
  font-size: var(--fs-xl);
  font-weight: 700;
  margin: 0 0 0.35rem;
  color: var(--ink);
}

.hub-hero__description {
  display: block;
  font-size: var(--fs-sm);
  color: var(--muted);
  margin: 0;
  line-height: 1.4;
}

.hub-card--primary {
  border-color: var(--cta-border);
}

.hub-card--primary:hover,
.hub-card--primary:focus-visible {
  border-color: var(--accent-strong, var(--cta-border));
}
```

Notes:
- Hover for `.hub-hero` intentionally only sets `border-color` (which Chrome applies to the per-side colors set in shorthand). The explicit `border-left: 4px solid var(--cta-border)` on the base rule keeps the left stripe fixed in `--cta-border` because the hover rule does not redeclare the shorthand. Manual browser verify happens in Plan 02 / phase verification.
- No size bump, no background tint, no badge on `.hub-card--primary` — D-03 locked the single-signal accent border treatment.
- Do not introduce any new :root tokens.
  </action>
  <verify>
    <automated>cd /mnt/c/users/chrislunt/source/personal/decksyncworkbench && grep -E '^\.hub-hero(__|--|:|\s|,)' DeckFlow.Web/wwwroot/css/site-common.css | grep -v '^#' | wc -l | awk '{ if ($1 < 5) { print "FAIL hub-hero rules missing"; exit 1 } else { print "PASS hub-hero rules present" } }' && grep -E '^\.hub-card--primary' DeckFlow.Web/wwwroot/css/site-common.css | grep -v '^#' | wc -l | awk '{ if ($1 < 2) { print "FAIL hub-card--primary missing"; exit 1 } else { print "PASS" } }' && grep -c '^\s*border-left:\s*4px solid var(--cta-border)' DeckFlow.Web/wwwroot/css/site-common.css | awk '{ if ($1 < 1) { print "FAIL hero accent stripe missing"; exit 1 } else { print "PASS" } }'</automated>
  </verify>
  <done>
    - site-common.css contains all 7 rule selectors: .hub-hero, .hub-hero:hover/focus-visible, .hub-hero__eyebrow, .hub-hero__title, .hub-hero__description, .hub-card--primary, .hub-card--primary:hover/focus-visible
    - Each rule references only Phase 01 tokens (no hex literals, no new tokens)
    - .hub-hero contains border-left: 4px solid var(--cta-border)
    - dotnet build clean
  </done>
</task>

<task type="auto">
  <name>Task 2: Amend .feedback-panel + add .admin-feedback-detail + add .admin-action-form</name>
  <files>DeckFlow.Web/wwwroot/css/site-common.css</files>
  <read_first>
    - DeckFlow.Web/wwwroot/css/site-common.css (line 567 — existing .feedback-panel; lines 596-609 — existing .admin-feedback cluster)
    - .planning/phases/02-layout-hierarchy-ux-copy/02-UI-SPEC.md §"3. .feedback-panel", §"4. .admin-feedback-detail", §"5. .admin-action-form"
    - .planning/phases/02-layout-hierarchy-ux-copy/02-PATTERNS.md §"7b/7c/7d"
    - DeckFlow.Web/Views/AdminFeedback/Detail.cshtml line 6 (source of truth for inline-style values being moved)
  </read_first>
  <action>
Implements UI-LH-02 CSS-side (per D-12, D-13, D-14). The Razor markup swap happens in Plan 02. Three discrete edits in site-common.css:

**Edit 2a — Amend .feedback-panel at site-common.css:567.**

Current single-line rule:
```css
.feedback-panel { padding: 1.5rem; border-radius: 8px; }
```

Replace with multi-line expanded rule (matches surrounding multi-line style of `.feedback-submit` at lines 579-588):
```css
.feedback-panel {
  padding: 1.5rem;
  border-radius: 8px;
  background: var(--panel);
  border: 1px solid var(--line);
}
```

**Edit 2b — Insert new .admin-feedback-detail rule after the .admin-feedback-* cluster.**

Locate `.admin-feedback-empty` (around line 609) and insert immediately after it (one blank line separator, matching surrounding style):

```css
.admin-feedback-detail {
  background: var(--panel);
  border: 1px solid var(--line);
  padding: 1.5rem;
  border-radius: 8px;
  max-width: 800px;
  margin: 2rem auto;
}

.admin-action-form {
  display: inline;
}
```

The six `.admin-feedback-detail` properties are copied verbatim from Detail.cshtml line 6's inline style — values are token-driven already, no substitutions.

**.admin-action-form is exactly two lines of body — D-12 forbids generalising to a shared `.inline-form` utility.**
  </action>
  <verify>
    <automated>cd /mnt/c/users/chrislunt/source/personal/decksyncworkbench && grep -A3 '^\.feedback-panel\s*{' DeckFlow.Web/wwwroot/css/site-common.css | grep -E 'background:\s*var\(--panel\)|border:\s*1px solid var\(--line\)' | wc -l | awk '{ if ($1 != 2) { print "FAIL feedback-panel amend incomplete: got "$1" expected 2"; exit 1 } else { print "PASS feedback-panel amended" } }' && grep -c '^\.admin-feedback-detail\s*{' DeckFlow.Web/wwwroot/css/site-common.css | awk '{ if ($1 != 1) { print "FAIL admin-feedback-detail rule count "$1" expected 1"; exit 1 } else { print "PASS" } }' && grep -c '^\.admin-action-form\s*{' DeckFlow.Web/wwwroot/css/site-common.css | awk '{ if ($1 != 1) { print "FAIL admin-action-form rule count "$1" expected 1"; exit 1 } else { print "PASS" } }'</automated>
  </verify>
  <done>
    - .feedback-panel rule body now contains 4 declarations (padding, border-radius, background, border)
    - .admin-feedback-detail rule exists with all 6 declarations from the inline style
    - .admin-action-form rule exists with display: inline as its only declaration
    - No duplicate .feedback-panel rule blocks (must be amend, not append)
  </done>
</task>

<task type="auto">
  <name>Task 3: Add .feedback-submit--busy spinner rules</name>
  <files>DeckFlow.Web/wwwroot/css/site-common.css</files>
  <read_first>
    - DeckFlow.Web/wwwroot/css/site-common.css (lines 579-589 — existing .feedback-submit cluster, the insertion neighbourhood)
    - DeckFlow.Web/wwwroot/css/site.css (lines 1107-1115 — existing @keyframes busy-spin; verify it is present)
    - .planning/phases/02-layout-hierarchy-ux-copy/02-UI-SPEC.md §"6. Feedback Busy State"
    - .planning/phases/02-layout-hierarchy-ux-copy/02-PATTERNS.md §"7e. .feedback-submit--busy"
  </read_first>
  <action>
Implements UX-02 CSS-side (per D-08, D-09, D-11). Plan 03 wires the TS handler that adds/removes the `.feedback-submit--busy` class.

Insert this block in site-common.css **immediately after the existing `.feedback-submit:hover` rule** (after `.feedback-submit:hover { filter: brightness(1.1); }` at line 589). Group adjacent to the existing `.feedback-submit` cluster for grep-ability.

```css
.feedback-submit--busy {
  opacity: 0.75;
  cursor: not-allowed;
  position: relative;
  padding-left: 2.25rem;
}

.feedback-submit--busy::before {
  content: "";
  position: absolute;
  left: 0.65rem;
  top: 50%;
  transform: translateY(-50%);
  width: 0.9rem;
  height: 0.9rem;
  border: 2px solid rgba(255, 255, 255, 0.35);
  border-top-color: var(--on-accent);
  border-radius: 999px;
  animation: busy-spin 0.75s linear infinite;
}
```

**CRITICAL — do NOT add a `@keyframes busy-spin { ... }` declaration to site-common.css.** The keyframes already live in site.css:1107-1115. Adding a second declaration would cause a duplicate-keyframe collision. The `animation: busy-spin ...` reference here resolves against the site.css definition because both files load on every page.

Notes:
- 0.75 opacity (not the 0.55 used by `.run-button:disabled`) is intentional per UI-SPEC §6 — busy state is short-lived, milder visual is preferred.
- The 2.25rem left padding makes room for the absolutely positioned spinner so the "Sending…" text shifts right rather than overlapping the ring.
- The spinner uses `--on-accent` for its top-color, which gives high contrast on the `--accent-strong` button background across all 22 themes.
  </action>
  <verify>
    <automated>cd /mnt/c/users/chrislunt/source/personal/decksyncworkbench && grep -c '^\.feedback-submit--busy\s*{' DeckFlow.Web/wwwroot/css/site-common.css | awk '{ if ($1 != 1) { print "FAIL feedback-submit--busy base rule count "$1; exit 1 } else { print "PASS base" } }' && grep -c '^\.feedback-submit--busy::before\s*{' DeckFlow.Web/wwwroot/css/site-common.css | awk '{ if ($1 != 1) { print "FAIL ::before rule count "$1; exit 1 } else { print "PASS pseudo" } }' && grep -c '@keyframes busy-spin' DeckFlow.Web/wwwroot/css/site-common.css | awk '{ if ($1 != 0) { print "FAIL duplicate keyframes added to site-common.css (must reuse site.css)"; exit 1 } else { print "PASS no duplicate keyframes" } }' && grep -c 'animation:\s*busy-spin' DeckFlow.Web/wwwroot/css/site-common.css | awk '{ if ($1 < 1) { print "FAIL animation reference missing"; exit 1 } else { print "PASS reuses busy-spin" } }'</automated>
  </verify>
  <done>
    - .feedback-submit--busy rule exists with opacity, cursor, position, padding-left
    - .feedback-submit--busy::before rule exists with content, position, dimensions, border, animation
    - Zero @keyframes declarations added to site-common.css
    - animation: busy-spin reference present (resolves to site.css keyframes)
  </done>
</task>

<task type="auto">
  <name>Task 4: Phase 02 CSS verification + :root immutability check + build</name>
  <files>(verification only — no edits)</files>
  <read_first>
    - DeckFlow.Web/wwwroot/css/site.css :root block (lines 1-150 approx — current Phase 01 token inventory)
    - .planning/phases/02-layout-hierarchy-ux-copy/02-UI-SPEC.md §"Verification Checklist" items 3 and 4
  </read_first>
  <action>
Run all phase-level CSS gates in one shot. Each fail aborts the plan; report all results before exiting.

1. site-common.css owns all new selectors (verifier gate #3):
```bash
cd /mnt/c/users/chrislunt/source/personal/decksyncworkbench
for sel in '\.hub-hero[^a-z-]' '\.hub-card--primary' '\.admin-action-form' '\.feedback-submit--busy'; do
  in_common=$(grep -E "$sel" DeckFlow.Web/wwwroot/css/site-common.css | wc -l)
  in_main=$(grep -E "$sel" DeckFlow.Web/wwwroot/css/site.css | wc -l)
  echo "selector $sel: site-common.css=$in_common site.css=$in_main"
  [ "$in_main" -ne 0 ] && { echo "FAIL: $sel found in site.css (must be in site-common.css only)"; exit 1; }
  [ "$in_common" -lt 1 ] && { echo "FAIL: $sel missing from site-common.css"; exit 1; }
done
echo "PASS: all new selectors live in site-common.css only"
```

2. :root token immutability — diff against last commit baseline (verifier gate #4):
```bash
cd /mnt/c/users/chrislunt/source/personal/decksyncworkbench
git diff HEAD -- DeckFlow.Web/wwwroot/css/site.css | grep -E '^\+\s*--[a-z]' | wc -l | awk '{ if ($1 != 0) { print "FAIL: "$1" new :root token line(s) added in site.css"; exit 1 } else { print "PASS: zero new :root tokens" } }'
```

3. Build clean:
```bash
cd /mnt/c/users/chrislunt/source/personal/decksyncworkbench && dotnet build DeckFlow.sln -c Debug 2>&1 | tail -20
```

If `dotnet build` reports any error or any warning that did not exist on main pre-plan, fail the verification and surface the diff.
  </action>
  <verify>
    <automated>cd /mnt/c/users/chrislunt/source/personal/decksyncworkbench && for sel in '\.hub-hero[^a-z-]' '\.hub-card--primary' '\.admin-action-form' '\.feedback-submit--busy'; do c=$(grep -E "$sel" DeckFlow.Web/wwwroot/css/site-common.css | wc -l); m=$(grep -E "$sel" DeckFlow.Web/wwwroot/css/site.css | wc -l); [ "$m" -ne 0 ] && { echo "FAIL $sel in site.css"; exit 1; }; [ "$c" -lt 1 ] && { echo "FAIL $sel missing from site-common.css"; exit 1; }; done; echo "PASS selectors" && git diff HEAD -- DeckFlow.Web/wwwroot/css/site.css 2>/dev/null | grep -E '^\+\s*--[a-zA-Z]' | wc -l | awk '{ if ($1 != 0) { print "FAIL "$1" new tokens"; exit 1 } else { print "PASS no new tokens" } }' && dotnet build DeckFlow.sln -c Debug --nologo 2>&1 | tail -3 | grep -qE 'Build succeeded|0 Error' && echo "PASS build" || { echo "FAIL build"; exit 1; }</automated>
  </verify>
  <done>
    - Zero new selectors in site.css; all four new selector families present in site-common.css
    - Zero net new :root token lines in site.css vs HEAD
    - dotnet build succeeds with zero errors
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Phase 02 Plan 01 introduces only static CSS rules; no new endpoints, no input handling, no auth surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | I (Information Disclosure) | site-common.css public asset | accept | CSS file is public by design (loaded on every request); contains no secrets, no environment-specific data, no user PII. Render reverse proxy serves the file with normal cache headers. No mitigation required. |
| T-02-02 | T (Tampering) | guild theme overrides | accept | New rules use semantic tokens (var(--cta-border), etc.) so guild-theme overrides retain authority. Adversarial theme that nukes --cta-border falls back to var(--line) — no broken layout, just a less prominent CTA. Acceptable. |
</threat_model>

<verification>
- All four `<verify>` blocks above are automated and run in <1s combined.
- No manual browser verification in this plan — Plan 02 owns the markup wiring and surfaces the visual changes for the phase-level browser walkthrough.
- The CSS rules are inert without Plan 02's markup — visual regression is impossible until cards/forms reference the new classes.
</verification>

<success_criteria>
1. site-common.css gains exactly seven new rule blocks: .hub-hero, .hub-hero:hover/focus-visible, .hub-hero__eyebrow, .hub-hero__title, .hub-hero__description, .hub-card--primary, .hub-card--primary:hover/focus-visible — plus .admin-feedback-detail, .admin-action-form, .feedback-submit--busy, .feedback-submit--busy::before. (.feedback-panel is amended in place, not added.)
2. site.css gains zero new selectors and zero new :root tokens.
3. `dotnet build DeckFlow.sln -c Debug` is clean.
4. All Task 1–4 automated verifies pass.
</success_criteria>

<output>
After completion, create `.planning/phases/02-layout-hierarchy-ux-copy/02-01-SUMMARY.md` capturing:
- Final line-number locations of each new rule in site-common.css
- Confirmation that site.css :root is unchanged (paste the `git diff` head/tail showing no token additions)
- `dotnet build` exit summary
- Forward signal to Plan 02: classes are ready to be referenced in markup
</output>
