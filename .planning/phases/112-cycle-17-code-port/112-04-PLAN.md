---
phase: 112-cycle-17-code-port
plan: 04
type: execute
wave: 4
depends_on: ["112-03"]
files_modified:
  - DeckFlow.Web/Models/CreatorStyleRequest.cs
  - DeckFlow.Web/Services/Content/CreatorStyleSeedLoader.cs
  - DeckFlow.Web/Services/Content/ICreatorStyleSeedLoader.cs
  - DeckFlow.Web/Services/CreatorStyle/*.cs
  - DeckFlow.Web/Services/Scryfall/CachedNameResolution.cs
  - DeckFlow.Web/Services/Scryfall/CardGroundingGuard.cs
  - DeckFlow.Web/Services/Scryfall/ScryfallBatching.cs
  - DeckFlow.Web/Services/Scryfall/ScryfallCardNameGrounder.cs
  - DeckFlow.Web/Services/Scryfall/ScryfallCollectionResolver.cs
  - DeckFlow.Web/Services/Scryfall/ScryfallErrorResponse.cs
  - DeckFlow.Web/Services/Scryfall/ScryfallLimits.cs
  - DeckFlow.Web/Services/SeedJson.cs
  - DeckFlow.Web.Tests/CreatorStyleSeedLoaderTests.cs
  - DeckFlow.Web.Tests/ProgramStartupTests.cs
  - DeckFlow.Web.Tests/Services/CreatorStyle/*.cs
  - DeckFlow.Web.Tests/Services/Scryfall/CardGroundingGuardTests.cs
  - DeckFlow.Web.Tests/Services/Scryfall/CardGroundingHallucinationFixtureTests.cs
  - DeckFlow.Web.Tests/Services/Scryfall/ScryfallCardNameGrounderTests.cs
  - content-kb/seed/creator-style-profiles.json
  - content-kb/seed/creator-deck-cache.json
  - DeckFlow.Web/Services/Scryfall/ScryfallDtos.cs
  - DeckFlow.Web/Services/Scryfall/ScryfallCardResolver.cs
  - DeckFlow.Web/Services/PacketSessionCache.cs
  - DeckFlow.Web/Services/Persistence/DeckFlowDatabaseConnectionFactory.cs
  - DeckFlow.Web/Services/Http/ResiliencePipelineFactory.cs
autonomous: true
requirements: [PORT-02]

must_haves:
  truths:
    - "The four Scryfall helpers (ScryfallCollectionResolver, ScryfallLimits, CachedNameResolution, ScryfallBatching) exist as NEW files, consumed only by newly ported creator-style code (D-01)."
    - "CardLookupService.cs and ScryfallReferenceResolver.cs are byte-identical to main — every rewire of a pre-existing consumer stays in Phase 113 (D-02, D-16)."
    - "ScryfallDtos.cs and ScryfallCardResolver.cs each gained exactly one additive member (Legalities, ExecuteNamedFuzzyAsync) so CardGroundingGuard compiles (D-16)."
    - "PacketSessionCache.cs gained only CreatorStyleCacheInputs plus one PacketSizeEstimator overload — the single ratified exception to D-12's never-port list (D-18)."
    - "The archidekt Polly pipeline is registered alongside the existing five, so ArchidektOwnerClient's constructor cannot throw KeyNotFoundException at DI-resolution time (D-17)."
    - "Both creator-style seed placeholders exist under content-kb/seed/ containing exactly an empty array, with no real creator data committed to a public repo (D-11)."
    - "DeckFlowDatabaseConnectionFactory.cs still exposes CreateManabaseBaselineConnection and CreateLocalContentKbConnection (D-09, RESEARCH Finding 5)."
    - "DeckFlow.Web builds with 0 errors."
  artifacts:
    - path: "DeckFlow.Web/Services/CreatorStyle/CreatorStylePacketService.cs"
      provides: "Creator-style prompt packet assembly"
    - path: "DeckFlow.Web/Services/Content/CreatorStyleSeedLoader.cs"
      provides: "Startup hydration of creator-style profiles and deck cache from git-shipped seeds"
    - path: "content-kb/seed/creator-style-profiles.json"
      provides: "Empty-array seed placeholder, overwritten with real data at Phase 115"
    - path: "content-kb/seed/creator-deck-cache.json"
      provides: "Empty-array seed placeholder"
  key_links:
    - from: "DeckFlow.Web/Services/Scryfall/CardGroundingGuard.cs"
      to: "DeckFlow.Web/Services/Scryfall/ScryfallCardResolver.cs"
      via: "ExecuteNamedFuzzyAsync"
      pattern: "ExecuteNamedFuzzyAsync"
    - from: "DeckFlow.Web/Services/CreatorStyle/ArchidektOwnerClient.cs"
      to: "DeckFlow.Web/Services/Http/ResiliencePipelineFactory.cs"
      via: "ResiliencePipelineProvider GetPipeline of RestResponse keyed archidekt"
      pattern: "\"archidekt\""
    - from: "DeckFlow.Web/Services/CreatorStyle/CreatorStylePacketService.cs"
      to: "DeckFlow.Web/Services/PacketSessionCache.cs"
      via: "CreatorStyleCacheInputs"
      pattern: "CreatorStyleCacheInputs"
---

<objective>
Land the creator-style Web services, the four Scryfall helpers, the seed loader, the Web test suite, and the empty seed placeholders — then apply the four Web M-file hunks and register the `archidekt` resilience pipeline.

Purpose: this is the first two-thirds of D-07's Commit 2. The M-file hunks and the pipeline registration are the load-bearing corrections RESEARCH proved with an actual build. Without `ScryfallCard.Legalities` and `IScryfallCardResolver.ExecuteNamedFuzzyAsync`, `CardGroundingGuard` does not compile. Without `CreatorStyleCacheInputs`, `CreatorStylePacketService` does not compile. Without the `archidekt` pipeline, `ArchidektOwnerClient`'s constructor throws `KeyNotFoundException` at DI-resolution time and ROADMAP success criterion 3 fails.

Output: `DeckFlow.Web` builds with 0 errors. `DeckFlow.Web.Tests` will NOT build until plan 05 adds the DI extension and the `Program.cs` edits — that is expected at this point and is not a defect. No commit is created here; plan 06 owns Commit 2.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/112-cycle-17-code-port/112-RESEARCH.md
@.planning/phases/112-cycle-17-code-port/112-CONTEXT.md
@.planning/phases/112-cycle-17-code-port/112-BASELINE.md
@CLAUDE.md
</context>

<environment>
- Branch: `feat/personal-tools`, Commit 1 already landed. dotnet is `"/mnt/c/Program Files/dotnet/dotnet.exe"`. Do NOT set `MTG_DATA_DIR`.
- `plan/cycle-17-creator-style` (head `6da5eb42`) is READ-ONLY. `git checkout <ref> -- <explicit paths>` is permitted ONLY for the added-file paths published below.
- NEVER `git checkout plan/cycle-17-creator-style -- <file>` for a file that already exists on main. `DeckFlowDatabaseConnectionFactory.cs` is the canonical trap: main added `CreateManabaseBaselineConnection` after the fork, and a wholesale copy deletes it and breaks `ManabaseBaselineStore.cs`.
- `.gitattributes` enforces LF line endings.
- No new `PackageReference` anywhere in this phase.
- This repo is PUBLIC. The seed placeholders must contain an empty JSON array and nothing else — no creator names, deck ids, or crawled data.
</environment>

<port_allowlist>
## Commit 2 allowlist — 36 code paths + 2 seed files, checkout from `plan/cycle-17-creator-style` (D-05)

Web production (21):
DeckFlow.Web/Models/CreatorStyleRequest.cs
DeckFlow.Web/Services/Content/CreatorStyleSeedLoader.cs
DeckFlow.Web/Services/Content/ICreatorStyleSeedLoader.cs
DeckFlow.Web/Services/CreatorStyle/ArchidektOwnerClient.cs
DeckFlow.Web/Services/CreatorStyle/ArchidektOwnerUrl.cs
DeckFlow.Web/Services/CreatorStyle/CreatorDeckCategoryResolver.cs
DeckFlow.Web/Services/CreatorStyle/CreatorDeckExemplarSelector.cs
DeckFlow.Web/Services/CreatorStyle/CreatorProfileDeckCrawler.cs
DeckFlow.Web/Services/CreatorStyle/CreatorStyleDeckAnalysis.cs
DeckFlow.Web/Services/CreatorStyle/CreatorStylePacketService.cs
DeckFlow.Web/Services/CreatorStyle/CreatorWhitelistPoolBuilder.cs
DeckFlow.Web/Services/CreatorStyle/MeasuredStyleProfileBuilder.cs
DeckFlow.Web/Services/CreatorStyle/SubmittedDeckStatsBuilder.cs
DeckFlow.Web/Services/Scryfall/CachedNameResolution.cs
DeckFlow.Web/Services/Scryfall/CardGroundingGuard.cs
DeckFlow.Web/Services/Scryfall/ScryfallBatching.cs
DeckFlow.Web/Services/Scryfall/ScryfallCardNameGrounder.cs
DeckFlow.Web/Services/Scryfall/ScryfallCollectionResolver.cs
DeckFlow.Web/Services/Scryfall/ScryfallErrorResponse.cs
DeckFlow.Web/Services/Scryfall/ScryfallLimits.cs
DeckFlow.Web/Services/SeedJson.cs

Web tests (15):
DeckFlow.Web.Tests/CreatorStyleSeedLoaderTests.cs
DeckFlow.Web.Tests/ProgramStartupTests.cs
DeckFlow.Web.Tests/Services/CreatorStyle/ArchidektOwnerClientTests.cs
DeckFlow.Web.Tests/Services/CreatorStyle/CreatorDeckExemplarSelectorTests.cs
DeckFlow.Web.Tests/Services/CreatorStyle/CreatorProfileDeckCrawlerTests.cs
DeckFlow.Web.Tests/Services/CreatorStyle/CreatorStyleDeckAnalysisTests.cs
DeckFlow.Web.Tests/Services/CreatorStyle/CreatorStyleDiRegistrationTests.cs
DeckFlow.Web.Tests/Services/CreatorStyle/CreatorStylePacketServiceTests.cs
DeckFlow.Web.Tests/Services/CreatorStyle/CreatorWhitelistPoolBuilderTests.cs
DeckFlow.Web.Tests/Services/CreatorStyle/MeasuredStyleProfileBuilderTests.cs
DeckFlow.Web.Tests/Services/CreatorStyle/SnailSeedCorpusFixture.cs
DeckFlow.Web.Tests/Services/CreatorStyle/SubmittedDeckStatsBuilderTests.cs
DeckFlow.Web.Tests/Services/Scryfall/CardGroundingGuardTests.cs
DeckFlow.Web.Tests/Services/Scryfall/CardGroundingHallucinationFixtureTests.cs
DeckFlow.Web.Tests/Services/Scryfall/ScryfallCardNameGrounderTests.cs

Seed placeholders (2, D-11):
content-kb/seed/creator-style-profiles.json
content-kb/seed/creator-deck-cache.json
</port_allowlist>

<never_port_list>
## Must NOT arrive at Phase 112 — no exceptions

D-06 / Phase 114 (admin surface, out of scope here):
- DeckFlow.Web/Controllers/CreatorStyleController.cs
- DeckFlow.Web/Models/CreatorStyleViewModel.cs
- DeckFlow.Web/Views/Deck/CreatorStyle.cshtml
- DeckFlow.Web/Help/creator-style.md
- DeckFlow.Web/e2e/creator-style.spec.ts
- DeckFlow.Web.Tests/CreatorStyleControllerTests.cs
- DeckFlow.Web.Tests/CreatorStyleViewRenderTests.cs

Phase 115 (CLI + real seed data):
- DeckFlow.CLI/CreatorStyleCommandRunners.cs

D-12 never-port list — the ONLY ratified exception is the additive `PacketSessionCache.cs` hunk in task 2 of this plan:
- DeckFlow.Web/Services/FeatureFlags/FeatureFlagCatalog.cs
- DeckFlow.Web/Services/FeatureFlags/FeatureFlagStore.cs
- any ToolRegistry file
- DeckFlow.Web/Models/DeckPageTab.cs
- any SeoPaths or sitemap file
- the string `tool.creator-style.enabled` anywhere in added content

D-02 / D-16 byte-identical files — must NOT be modified at 112 (Phase 113 owns them):
- DeckFlow.Web/Services/CardLookupService.cs
- DeckFlow.Web/Services/Scryfall/ScryfallReferenceResolver.cs
- DeckFlow.Web/Services/ManabaseAnalysisService.cs
- DeckFlow.Web/Extensions/HttpClientServiceCollectionExtensions.cs (the archidekt-owner client goes in the NEW extension in plan 05, never here)
- DeckFlow.Web/Extensions/PacketServiceCollectionExtensions.cs
- DeckFlow.Web/Extensions/ScryfallServiceCollectionExtensions.cs
</never_port_list>

<tasks>

<task type="auto">
  <name>Task 1: Path-allowlist checkout of the 36 Web files and the two seed placeholders</name>
  <files>The 36 code paths and 2 seed paths in the port_allowlist above</files>
  <read_first>
    - The `<port_allowlist>` and `<never_port_list>` sections of THIS plan — the authoritative manifest.
    - `.planning/phases/112-cycle-17-code-port/112-RESEARCH.md` §"Port Allowlist — Commit 2 (Web)", §"Test Port Inventory" (Web.Tests port list and the Phase-114 exclusions), and §"Seed Placeholder Mechanics".
    - `git show plan/cycle-17-creator-style:content-kb/seed/creator-style-profiles.json` and the deck-cache equivalent — confirm each is an empty array on the branch before checkout.
    - `content-kb/seed/index-seed.json` on HEAD — the existing shipping precedent. RESEARCH confirmed `content-kb/**` needs no csproj Content item: it is un-ignored in `.dockerignore` and copied verbatim by the Dockerfile.
  </read_first>
  <action>
    Run a single `git checkout plan/cycle-17-creator-style -- <paths>` invocation listing exactly the 38 paths published in this plan's `<port_allowlist>` (36 code plus 2 seed). One command, explicit paths, no globs and no directory arguments — a glob could sweep in a Phase-114 controller or view. Do NOT use `git cherry-pick`, `git apply`, `git merge`, or `git rebase`.

    Inspect the two seed files immediately after checkout. Each must be exactly an empty JSON array. If either arrived with real crawled creator data, replace its contents with an empty array — this repo is public and Phase 115 is where real data lands, after the user decides what is publishable. RESEARCH verified `CreatorStyleSeedLoader.LoadProfilesIfPresentAsync` and `LoadDeckCacheIfPresentAsync` deserialize an empty array to a zero-length array with no throw, so no defensive guard code is needed at 112.

    Confirm no csproj change is needed for the seeds: `content-kb/**` ships via the `.dockerignore` un-ignore plus a verbatim COPY in the Dockerfile, exactly as `index-seed.json` already does.

    Confirm none of this plan's `<never_port_list>` paths appeared, and that `CardLookupService.cs`, `ScryfallReferenceResolver.cs`, `ManabaseAnalysisService.cs`, and the three shared DI extension files are still byte-identical to HEAD.

    Do not stage or commit. Plan 06 owns Commit 2.
  </action>
  <verify>
    <automated>test "$(tr -d '[:space:]' &lt; content-kb/seed/creator-style-profiles.json)" = "[]" &amp;&amp; test "$(tr -d '[:space:]' &lt; content-kb/seed/creator-deck-cache.json)" = "[]" &amp;&amp; test -f DeckFlow.Web/Services/CreatorStyle/CreatorStylePacketService.cs</automated>
  </verify>
  <acceptance_criteria>
    - All 38 allowlisted paths exist in the working tree: a loop over the 38 paths testing `test -e` prints no MISSING line.
    - `content-kb/seed/creator-style-profiles.json` with all whitespace stripped equals exactly the two characters `[]`, and the same for `content-kb/seed/creator-deck-cache.json`.
    - `grep -Ec '[A-Za-z]' content-kb/seed/creator-style-profiles.json` returns 0 and the same for the deck-cache seed — no creator slug, deck id, or card name is committed to this public repo.
    - None of these exist in the working tree: `DeckFlow.Web/Controllers/CreatorStyleController.cs`, `DeckFlow.Web/Models/CreatorStyleViewModel.cs`, `DeckFlow.Web/Views/Deck/CreatorStyle.cshtml`, `DeckFlow.Web/Help/creator-style.md`, `DeckFlow.Web/e2e/creator-style.spec.ts`, `DeckFlow.Web.Tests/CreatorStyleControllerTests.cs`, `DeckFlow.Web.Tests/CreatorStyleViewRenderTests.cs`, `DeckFlow.CLI/CreatorStyleCommandRunners.cs`.
    - `git diff --name-only HEAD -- DeckFlow.Web/Services/CardLookupService.cs DeckFlow.Web/Services/Scryfall/ScryfallReferenceResolver.cs DeckFlow.Web/Services/ManabaseAnalysisService.cs DeckFlow.Web/Extensions/HttpClientServiceCollectionExtensions.cs DeckFlow.Web/Extensions/PacketServiceCollectionExtensions.cs DeckFlow.Web/Extensions/ScryfallServiceCollectionExtensions.cs` returns zero lines — all six untouched.
    - `git status --porcelain` shows zero entries matching `FeatureFlagCatalog`, `FeatureFlagStore`, `ToolRegistry`, `DeckPageTab`, `SeoPaths`, or `sitemap`.
    - `git status --porcelain` shows zero entries under `DeckFlow.Core/` or `DeckFlow.Core.Tests/` — Core is finished and committed.
    - No csproj file was modified by this task.
  </acceptance_criteria>
  <done>The creator-style Web services, the four Scryfall helpers, the seed loader, the 15 Web test files, and two empty seed placeholders are in the working tree, with every excluded path absent.</done>
</task>

<task type="auto">
  <name>Task 2: Hand-apply the four Web M-file hunks</name>
  <files>
    DeckFlow.Web/Services/Scryfall/ScryfallDtos.cs,
    DeckFlow.Web/Services/Scryfall/ScryfallCardResolver.cs,
    DeckFlow.Web/Services/PacketSessionCache.cs,
    DeckFlow.Web/Services/Persistence/DeckFlowDatabaseConnectionFactory.cs
  </files>
  <read_first>
    - `git show HEAD:<path>` for each of the four files — main's CURRENT content is the starting point. `DeckFlowDatabaseConnectionFactory.cs` on HEAD already contains `CreateManabaseBaselineConnection` (added after the fork) and `CreateLocalContentKbConnection` (Cycle 16); both must survive.
    - `git show plan/cycle-17-creator-style:<same four paths>` — the source of the NEW members only.
    - `.planning/phases/112-cycle-17-code-port/112-RESEARCH.md` §"Critical Findings" #1 (the exact `Legalities` record parameter and the `ExecuteNamedFuzzyAsync` interface default plus concrete override, including its `ScryfallThrottle.ThrowIfUpstreamUnavailable` call), #3 (`CreatorStyleCacheInputs` and the `PacketSizeEstimator` overload), #5 (the wholesale-copy trap), and the M-File Hunk Inventory rows for these four files.
    - `DeckFlow.Web/Services/Scryfall/CardGroundingGuard.cs` (just ported) — the consumer that dictates both Scryfall signatures. It reads `ScryfallCard.Legalities` and calls `IScryfallCardResolver.ExecuteNamedFuzzyAsync`.
    - `DeckFlow.Web/Services/CreatorStyle/CreatorStylePacketService.cs` (just ported) — the consumer that requires `CreatorStyleCacheInputs`.
    - `CLAUDE.md` §Anti-Patterns — no `new HttpClient()`, no per-call Polly pipelines, no Scryfall call bypassing `ScryfallThrottle`.
  </read_first>
  <action>
    For each file, start from main's current content and APPEND only new members. Never `git checkout plan/cycle-17-creator-style -- <these paths>` and never replace whole-file content.

    `DeckFlow.Web/Services/Scryfall/ScryfallDtos.cs` (D-16) — add ONE optional record parameter to the `ScryfallCard` record, in LAST position so every existing positional and named caller keeps working: a `Legalities` parameter of nullable `IReadOnlyDictionary<string, string>` carrying a `[property: JsonPropertyName("legalities")]` attribute and defaulted to null. Keep the attribute on its own line — never inline it onto the property line (carve-out). Change nothing else in this file.

    `DeckFlow.Web/Services/Scryfall/ScryfallCardResolver.cs` (D-16) — add ONE method named `ExecuteNamedFuzzyAsync` taking a card name and a `CancellationToken` and returning `Task<RestResponse<ScryfallCard>>`, in two places: on the `IScryfallCardResolver` interface as a default implementation that throws `NotSupportedException` (so no other implementer breaks), and as the concrete implementation on `ScryfallCardResolver`. The concrete version validates with `ArgumentException.ThrowIfNullOrWhiteSpace`, builds a `cards/named` GET request with a `fuzzy` query parameter run through the file's existing `NormalizeForScryfall` helper, executes through the class's existing execute delegate, and calls `ScryfallThrottle.ThrowIfUpstreamUnavailable` on the response status before returning. Do not add a new `HttpClient`, do not build a Polly pipeline inline, and do not bypass `ScryfallThrottle`.

    `DeckFlow.Web/Services/PacketSessionCache.cs` (D-18 — the SINGLE ratified exception to D-12's never-port list) — add ONE `internal sealed record CreatorStyleCacheInputs` with three string members `CreatorSlug`, `NormalizedDeckSource`, `Format`; and ONE `PacketSizeEstimator.EstimateSizeBytes` overload taking a `CreatorStylePacketResult` that sums string and collection lengths, matching the shape of the existing overloads in this file. Add NOTHING else: no entry to any shared bypass list, no feature-flag reference. Creator-style has no public feature flags, so the `PromptMutatingCreatorStyleFlags` list lives inside `CreatorStylePacketService.cs` and does not touch this file.

    `DeckFlow.Web/Services/Persistence/DeckFlowDatabaseConnectionFactory.cs` — append ONE public method `CreateCreatorDeckCacheConnection` taking an `IWebHostEnvironment` and returning a `RelationalDatabaseConnection` bound to `creator-deck-cache.db`, modelled on the existing `CreateLocalContentKbConnection` in this file. `CreateManabaseBaselineConnection` and `CreateLocalContentKbConnection` must both still be present afterwards.

    Honor the five `.editorconfig` carve-outs throughout: never convert `{ get; init; }` to `{ get; }` (System.Text.Json silently skips get-only properties in .NET 9+ and this has broken `EdhTop16Client` before), never inline an `[Attribute]` onto the property line, never re-indent a C# raw-string literal, preserve switch expressions, preserve xmldoc single-space indent. LF line endings only.

    Do not stage or commit.
  </action>
  <verify>
    <automated>grep -q 'CreateManabaseBaselineConnection' DeckFlow.Web/Services/Persistence/DeckFlowDatabaseConnectionFactory.cs &amp;&amp; grep -q 'CreateCreatorDeckCacheConnection' DeckFlow.Web/Services/Persistence/DeckFlowDatabaseConnectionFactory.cs &amp;&amp; grep -q 'CreatorStyleCacheInputs' DeckFlow.Web/Services/PacketSessionCache.cs &amp;&amp; grep -q 'ExecuteNamedFuzzyAsync' DeckFlow.Web/Services/Scryfall/ScryfallCardResolver.cs</automated>
  </verify>
  <acceptance_criteria>
    - `DeckFlow.Web/Services/Scryfall/ScryfallDtos.cs` contains the literal string `JsonPropertyName("legalities")` and the identifier `Legalities`.
    - `DeckFlow.Web/Services/Scryfall/ScryfallCardResolver.cs` contains `ExecuteNamedFuzzyAsync` at least twice (interface default plus concrete override) and contains `ThrowIfUpstreamUnavailable`.
    - `DeckFlow.Web/Services/PacketSessionCache.cs` contains `CreatorStyleCacheInputs`, and `grep -c 'tool.creator-style.enabled\|FeatureFlag' DeckFlow.Web/Services/PacketSessionCache.cs` returns 0 additions relative to HEAD.
    - `DeckFlow.Web/Services/Persistence/DeckFlowDatabaseConnectionFactory.cs` contains ALL of `CreateManabaseBaselineConnection`, `CreateLocalContentKbConnection`, and `CreateCreatorDeckCacheConnection`.
    - `git diff HEAD -- DeckFlow.Web/Services/Scryfall/ScryfallDtos.cs DeckFlow.Web/Services/Scryfall/ScryfallCardResolver.cs DeckFlow.Web/Services/PacketSessionCache.cs DeckFlow.Web/Services/Persistence/DeckFlowDatabaseConnectionFactory.cs` piped to `grep -c '^-[^-]'` returns 0 — all four hunks are additive-only with zero deleted lines.
    - `grep -c $'\r'` returns 0 for all four files.
    - `git diff HEAD` shows no line adding `new HttpClient(` and no line adding a Polly pipeline built inside a method body.
  </acceptance_criteria>
  <done>All four Web M-file hunks are applied additively; `CardGroundingGuard` and `CreatorStylePacketService` have the members they compile against, and no main-only member was lost.</done>
</task>

<task type="auto">
  <name>Task 3: Register the archidekt resilience pipeline (D-17)</name>
  <files>DeckFlow.Web/Services/Http/ResiliencePipelineFactory.cs</files>
  <read_first>
    - `DeckFlow.Web/Services/Http/ResiliencePipelineFactory.cs` on HEAD — read the five existing `AddResiliencePipeline` entries and the five `Build*` private methods, especially `BuildScryfall`, whose total-budget shape the new builder matches. RESEARCH verified this file is byte-identical to the merge-base, so the Cycle 17 hunk applies with zero adaptation.
    - `DeckFlow.Web/Services/CreatorStyle/ArchidektOwnerClient.cs` (just ported) — its constructor calls `GetPipeline<RestResponse>("archidekt")`. The key string must match exactly.
    - `.planning/phases/112-cycle-17-code-port/112-RESEARCH.md` §"Critical Findings" #2 — the Polly 8.6.6 repro proving `GetPipeline<T>` throws `KeyNotFoundException` on an unregistered key (so the client's `?? ResiliencePipeline<RestResponse>.Empty` fallback is unreachable dead code), that the throw fires in the CONSTRUCTOR, and the exact strategy parameters for the new builder.
    - `.planning/phases/112-cycle-17-code-port/112-CONTEXT.md` D-17 — the ratified flip of D-03.
    - `CLAUDE.md` §Anti-Patterns — never build a Polly pipeline per call.
  </read_first>
  <action>
    Append one registration entry to `AddDeckFlowResiliencePipelines()` alongside the existing five, using the pipeline key `archidekt` and delegating to a new private builder `BuildArchidekt`, matching the exact call shape of the `scryfall` line already in that method.

    Add the new private static `BuildArchidekt(ResiliencePipelineBuilder<RestResponse> builder)` method next to the existing `Build*` methods, following the Scryfall-shaped total-budget pattern: a timeout strategy with a 30-second total budget named `archidekt-total`, then a retry strategy with `MaxRetryAttempts` 2, exponential backoff, jitter enabled, and a predicate that handles results whose status code is 500 or above plus exceptions accepted by this file's existing `IsTransientException` helper. Reuse `IsTransientException` — do not write a second copy.

    Do NOT touch `DeckFlow.Web/Extensions/HttpClientServiceCollectionExtensions.cs`. The named `archidekt-owner` `HttpClient` that `ArchidektOwnerClient`'s public constructor requires is registered inside the NEW `AddDeckFlowCreatorStyle()` extension in plan 05, per D-10 and D-17 — one line in one new file rather than an edit to a shared, contested extension.

    After this task, run `"/mnt/c/Program Files/dotnet/dotnet.exe" build DeckFlow.Web/DeckFlow.Web.csproj` and confirm 0 errors. `DeckFlow.Web.Tests` is expected to still fail to build at this point (its `ProgramStartupTests.cs` references helpers plan 05 adds) — do not attempt to fix that here, and do not delete or stub the test to make it build.

    Do not stage or commit.
  </action>
  <verify>
    <automated>"/mnt/c/Program Files/dotnet/dotnet.exe" build DeckFlow.Web/DeckFlow.Web.csproj</automated>
  </verify>
  <acceptance_criteria>
    - `"/mnt/c/Program Files/dotnet/dotnet.exe" build DeckFlow.Web/DeckFlow.Web.csproj` exits 0 with 0 errors and introduces no warning ID absent from `112-BASELINE.md`.
    - `grep -c '"archidekt"' DeckFlow.Web/Services/Http/ResiliencePipelineFactory.cs` returns at least 1, and `grep -c 'BuildArchidekt' ` on the same file returns at least 2 (the registration call and the method declaration).
    - The five pre-existing pipeline keys `banlist`, `spellbook`, `tagger`, `tagger-post`, `scryfall` are all still registered in that file.
    - `grep -c 'IsTransientException' DeckFlow.Web/Services/Http/ResiliencePipelineFactory.cs` shows the helper is referenced by the new builder and was not duplicated (only one method declaration of it exists).
    - `git diff --name-only HEAD -- DeckFlow.Web/Extensions/HttpClientServiceCollectionExtensions.cs` returns zero lines.
    - `git diff HEAD -- DeckFlow.Web/Services/Http/ResiliencePipelineFactory.cs` piped to `grep -c '^-[^-]'` returns 0 — additive only.
    - The new pipeline has a bounded total timeout and a capped retry count; it is not registered with unlimited retries or no timeout.
  </acceptance_criteria>
  <done>The `archidekt` Polly pipeline is registered with a bounded timeout and capped retries, so `ArchidektOwnerClient`'s constructor cannot throw at DI-resolution time, and `DeckFlow.Web` builds clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DeckFlow.Web → archidekt.com | A new named HttpClient calls a third-party API; upstream availability and response shape are untrusted |
| LLM output → deck-analysis surface | Free-text card names produced by an LLM cross into user-facing output |
| working tree → public GitHub repo | `luntc1972/DeckFlow` is public; anything committed under content-kb/seed is world-readable |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-112-01 | Denial of Service | `archidekt-owner` HttpClient calling archidekt.com with no resilience pipeline — unbounded hang or retry storm on a flaky upstream | mitigate | Task 3 registers the `archidekt` Polly pipeline with a 30s total timeout and 2 capped exponential-backoff retries, matching the five existing pipelines. Acceptance criteria assert bounded timeout and capped retries. |
| T-112-09 | Tampering | LLM-hallucinated card names reaching deck-analysis output | mitigate | `CardGroundingGuard` and `ScryfallCardNameGrounder` are ported unmodified in task 1 and validate names against live Scryfall data. Task 2 supplies the `Legalities` and `ExecuteNamedFuzzyAsync` members the guard needs to function rather than silently degrade. Existing reviewed Cycle 17 logic; no redesign at 112. |
| T-112-10 | Information Disclosure | Real crawled creator/deck data committed to a PUBLIC repo via the seed placeholders | mitigate | Task 1 asserts both seed files contain exactly an empty array and that `grep -Ec '[A-Za-z]'` returns 0 on each. Real data is deferred to Phase 115 where the user decides what is publishable. |
| T-112-11 | Tampering | Wholesale copy of `DeckFlowDatabaseConnectionFactory.cs` deleting main-only `CreateManabaseBaselineConnection` and breaking `ManabaseBaselineStore.cs` | mitigate | Task 2 forbids branch checkout for M-files; acceptance criteria assert all three connection methods coexist and the four-file diff has zero deleted lines. |
| T-112-SC | Tampering | Package-manager installs (NuGet) | mitigate | Zero packages added. Task 1 asserts no csproj was modified; plan 06's commit gate greps the whole branch diff for `PackageReference`. |

## Not applicable at 112 (per RESEARCH §Security Domain)
- ASVS V2 Authentication and V4 Access Control: no route is wired at 112. The admin controller and its BasicAuth gate land at Phase 114.
- ASVS V6 Cryptography: nothing in this phase handles secrets or crypto.
</threat_model>

<verification>
- `"/mnt/c/Program Files/dotnet/dotnet.exe" build DeckFlow.Web/DeckFlow.Web.csproj` — 0 errors.
- Both seed files contain exactly an empty array; no alphabetic character in either.
- The six D-02/D-16 hands-off files show zero diff against HEAD.
- All four Web M-file hunks are additive-only (zero deleted lines) and `CreateManabaseBaselineConnection` survives.
- `archidekt` pipeline registered with bounded timeout and capped retries; `HttpClientServiceCollectionExtensions.cs` untouched.
- Nothing staged, nothing committed.
</verification>

<success_criteria>
- Creator-style Web services, the four Scryfall helpers, the seed loader, `CreatorStyleRequest`, and the 15 Web test files are present (PORT-02, criterion 1).
- `DeckFlow.Web` builds with 0 errors and no new warning IDs (PORT-02, criterion 2).
- The `archidekt` pipeline is registered, removing the constructor-time `KeyNotFoundException` that would fail criterion 3.
- Two empty seed placeholders are committed-ready with no real data.
- No never-port file arrived and no hands-off file was modified.
</success_criteria>

<output>
Create `.planning/phases/112-cycle-17-code-port/112-04-SUMMARY.md` when done, listing the 38 paths checked out, the four hunks applied with their additive-diff line counts, the pipeline key registered, and the `DeckFlow.Web` build result.
</output>
